---
phase: 175-navigation-app-shell-runtime-theme-picker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/operator_surface/surface_header_csp_test.exs
  - test/threadline/operator_surface/page_header_test.exs
  - test/threadline/operator_surface/breadcrumb_test.exs
  - test/threadline/operator_surface/pager_test.exs
  - test/threadline/operator_surface/skip_link_test.exs
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]
must_haves:
  truths:
    - "Each NAV behavior has an automated test file that fails RED until the implementation waves land"
    - "The CSP guard test reads surface_header.ex as a string and fails while inline handlers remain"
    - "The single-aria-current and one-h1-per-page assertions exist before page_header is built"
  artifacts:
    - path: "test/threadline/operator_surface/surface_header_csp_test.exs"
      provides: "NAV-03/NAV-04 CSP source-string guard (no on*= handlers; form posts /theme + _csrf_token)"
      contains: "onclick="
    - path: "test/threadline/operator_surface/page_header_test.exs"
      provides: "NAV-01 one-<h1>-per-page render assertion"
      contains: "page_header"
    - path: "test/threadline/operator_surface/breadcrumb_test.exs"
      provides: "NAV-01 breadcrumb landmark + single aria-current render assertion"
      contains: "Breadcrumb"
    - path: "test/threadline/operator_surface/pager_test.exs"
      provides: "NAV-02 hide-at-zero / disable-not-hide / range caption render assertion"
      contains: "role=\"status\""
    - path: "test/threadline/operator_surface/skip_link_test.exs"
      provides: "NAV-04 every page <main> has tabindex=-1 render assertion"
      contains: "tabindex"
  key_links:
    - from: "surface_header_csp_test.exs"
      to: "lib/threadline/operator_surface/components/surface_header.ex"
      via: "File.read! source-string contract"
      pattern: "File\\.read!"
    - from: "skip_link_test.exs"
      to: "all 11 LiveView pages"
      via: "LiveViewTest mount + tabindex assertion"
      pattern: "tabindex"
---

<objective>
Create the Wave 0 test scaffolds for every NAV behavior per 175-VALIDATION.md so the
implementation waves (Plans 02–04) have a RED→GREEN feedback loop. Five new test files
covering NAV-01..04. These tests assert the *target* behavior and are expected to fail
(RED) until the corresponding implementation lands; they must compile and run today.

Purpose: Nyquist sampling — no implementation task runs without an automated verify it can
turn green. The CSP guard (the phase headline) gets its dedicated source-string contract here.
Output: 5 new ExUnit test files under test/threadline/operator_surface/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/175-navigation-app-shell-runtime-theme-picker/175-VALIDATION.md
@.planning/phases/175-navigation-app-shell-runtime-theme-picker/175-PATTERNS.md

<interfaces>
<!-- Established test idioms the scaffolds copy. No exploration needed. -->

Contract source-string idiom (test/threadline/operator_surface/style_contract_test.exs:5,9):
  @style_path "lib/threadline/operator_surface/style.ex"
  src = File.read!(@style_path)
  refute String.contains?(src, "theme-toggle")

aria-current counting idiom (test/threadline/operator_surface/surface_header_test.exs:48):
  assert aria_current_count(html) == 1   # helper counts occurrences of aria-current="page"

Standalone component render (test/threadline/operator_surface/ui_test.exs:8-17):
  use ExUnit.Case, async: true
  import Phoenix.Component
  import Phoenix.LiveViewTest
  html = rendered_to_string(~H"""<UI.button>…</UI.button>""")

Full LiveView mount harness (test/threadline/operator_surface/live/timeline_live_test.exs:1-134):
  Defines ...Test.Layouts (2-19), ...Test.Router requiring Threadline.OperatorSurface.Router
  and calling threadline_operator_surface("/audit") (21-40), and an Endpoint (~80-98),
  all guarded by `if Code.ensure_loaded?(Phoenix.LiveView) do`.

Existing surface_header.ex facts (so RED is honest, not a compile error):
  - skip-link onclick at line 34; nav-toggle onclick at line 74; theme radio onchange at 110/114/118
  - hidden _csrf_token input at line 107; form action `#{@base_path}/theme`
  - tl-sr-only on theme radios; tl-segmented__item--active (dead markup class)

Existing page facts:
  - All 11 LiveView <main id="tl-main"> already carry tabindex="-1" (verified) — skip_link_test
    should PASS today for that assertion and stays green as a regression lock.
  - Breadcrumb markup only on transaction_live.ex:117 and actor_live.ex:117
    (aria-label="Investigation path"); row_history has none yet.
  - Title conventions: tl-transaction__title (transaction:120, actor:123),
    tl-timeline-command__title (timeline:456), tl-home__headline (start:135), tl-page__title (rest).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSP guard + page_header + breadcrumb scaffolds</name>
  <files>test/threadline/operator_surface/surface_header_csp_test.exs, test/threadline/operator_surface/page_header_test.exs, test/threadline/operator_surface/breadcrumb_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/style_contract_test.exs:1-30 (File.read! + String.contains? idiom)
    - test/threadline/operator_surface/surface_header_test.exs:1-66 (render_header helper, aria_current_count idiom, async:true module shape)
    - test/threadline/operator_surface/ui_test.exs:1-30 (rendered_to_string standalone component idiom)
    - lib/threadline/operator_surface/components/surface_header.ex (lines 34, 74, 107, 110/114/118 — the handlers being guarded)
  </read_first>
  <action>
    Create surface_header_csp_test.exs as a source-string contract (copy the @path + File.read!
    idiom from style_contract_test.exs). Define @header_path
    "lib/threadline/operator_surface/components/surface_header.ex". Assert NAV-03/NAV-04/D-27:
    `refute String.contains?(src, "onclick=")`, `refute String.contains?(src, "onchange=")`,
    and a broad no-inline-handler check (refute a regex/substring for ` on` + event-name like
    "oninput="/"onsubmit="); AND `assert String.contains?(src, "/theme")` and
    `assert String.contains?(src, "_csrf_token")`. These will be RED today (onclick/onchange
    present) except the two asserts. Tag the module async: true.

    Create page_header_test.exs for NAV-01/D-11: a render test using rendered_to_string against
    the not-yet-existing UI.page_header component (reference Threadline.OperatorSurface.UI.page_header
    or the chosen module per Plan 03's contract). Assert the rendered markup contains exactly one
    `<h1` occurrence and the class "tl-page__title", a `<header class="tl-page__header"`, and that
    when given a breadcrumbs assign it renders a `<nav aria-label="Breadcrumb"`. Wrap in
    `if Code.ensure_loaded?(Phoenix.LiveView) do`. RED today (component undefined) — acceptable
    Wave 0 scaffold; document the expected failure in the file header comment.

    Create breadcrumb_test.exs for NAV-01/D-12/D-13: assert that on a drill-down page the trail
    landmark is `<nav aria-label="Breadcrumb"` (NOT "Investigation path"), root link label is
    "Timeline", the final segment is plain text (no `aria-current="page"` on it), and
    aria_current_count over the combined page render == 1 (nav link only). Copy the
    aria_current_count helper from surface_header_test.exs. Drive via the LiveView mount harness
    (timeline_live_test.exs shape) or a component render of the breadcrumb + nav together.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/surface_header_csp_test.exs` runs (compiles); the two
      `assert "/theme"` / `assert "_csrf_token"` assertions PASS; the `refute onclick=`/`refute onchange=`
      assertions FAIL today (RED — handlers still present). File contains `File.read!` and `onclick=`.
    - page_header_test.exs and breadcrumb_test.exs compile and run under `if Code.ensure_loaded?`;
      they reference `aria-label="Breadcrumb"` and `tl-page__title`; expected RED until Plan 03.
    - No test uses watch mode; all modules are `async: true`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/surface_header_csp_test.exs test/threadline/operator_surface/page_header_test.exs test/threadline/operator_surface/breadcrumb_test.exs 2>&1 | tail -20; mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>Three test files exist, compile, and run; CSP guard is RED on handlers and GREEN on the /theme + CSRF asserts; page_header/breadcrumb tests are RED pending Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: pager + skip-link scaffolds</name>
  <files>test/threadline/operator_surface/pager_test.exs, test/threadline/operator_surface/skip_link_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/live/timeline_live_test.exs:1-134 (Layouts+Router+Endpoint LV harness)
    - test/threadline/operator_surface/ui_test.exs:1-55 (rendered_to_string component idiom)
    - lib/threadline/operator_surface/live/timeline_live.ex:601-603 (tl-status role=status aria-live=polite caption; format_count)
    - lib/threadline/operator_surface/live/{actor,coverage,evidence,export_status,policy_redaction,retention_history,row_history,start,transaction,timeline}_live.ex (all <main id="tl-main" tabindex="-1">)
  </read_first>
  <action>
    Create pager_test.exs for NAV-02/D-16..D-18: render the not-yet-existing
    Threadline.OperatorSurface.UI.pager component (per Plan 04's contract) with
    rendered_to_string. Assert: (a) when total/shown == 0 the pager renders nothing (hide entire
    pager at zero results) — `refute html =~ "tl-pager"`; (b) when shown on a boundary the
    unavailable directional control is `disabled` (present, not absent) — assert the markup
    contains "Older" and "Newer" and a `disabled` attribute on the boundary control, and
    `refute` that the control element is dropped; (c) the range caption container has
    `role="status"` and `aria-live="polite"`; (d) the copy uses "matching changes" and, when
    capped (total at/over 10_001), renders "10,000+" and NOT an exact deep total. Use the
    "Showing {N} of …" copy from 175-UI-SPEC.md. Wrap in `if Code.ensure_loaded?`.

    Create skip_link_test.exs for NAV-04/D-26: using the LiveView mount harness, mount each of the
    10 operator pages (Home/start, Timeline, Transaction, Row history, Actor, Coverage, Evidence,
    Redaction, Retention, Exports) and assert the rendered `<main id="tl-main"` carries
    `tabindex="-1"`. (stress_live is dev/test-only — include it if convenient, not required.)
    This assertion PASSES today (all pages already have it) and stays as a regression lock so a
    future page can't silently drop the skip-link target.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/skip_link_test.exs` PASSES today (all 10 pages assert
      tabindex="-1" on <main id="tl-main">); file contains `tabindex`.
    - pager_test.exs compiles and runs; it references `role="status"`, `aria-live="polite"`,
      "10,000+", and "matching changes"; expected RED until Plan 04 (component undefined).
    - Both modules `async: true` (or async:false only if the LV harness requires it); no watch mode.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/skip_link_test.exs 2>&1 | tail -10; mix test test/threadline/operator_surface/pager_test.exs 2>&1 | tail -10</automated>
  </verify>
  <done>Both files exist and run; skip_link_test is GREEN (regression lock); pager_test is RED pending Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → source files | Tests read source as strings + render components; no untrusted runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-01 | Tampering | CSP guard test could be written too loosely (miss a new on*= handler) | mitigate | Assert both specific (`onclick=`/`onchange=`) and a broad on-event substring; this test is the standing CSP gate for Plans 02–04. |
| T-175-SC | Tampering | npm/Hex/cargo installs | accept | No installs in this plan; zero new runtime deps is a hard milestone invariant. No package legitimacy gate needed. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- New test files run under `mix test`; RED/GREEN matches the per-task expectations above.
- `mix verify.format` clean.
</verification>

<success_criteria>
Five Wave 0 test files exist, compile, and run. The CSP guard, page_header, breadcrumb, and pager
tests provide the RED targets Plans 02–04 turn GREEN; skip_link_test is a GREEN regression lock.
No test uses watch mode; feedback latency < 60s per file.
</success_criteria>

<output>
Create `.planning/phases/175-navigation-app-shell-runtime-theme-picker/175-01-SUMMARY.md` when done
</output>
