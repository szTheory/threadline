---
phase: 175-navigation-app-shell-runtime-theme-picker
plan: 04
type: execute
wave: 4
depends_on: ["175-01", "175-02", "175-03"]
files_modified:
  - lib/threadline/operator_surface/ui.ex
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - lib/threadline/operator_surface/live/actor_live.ex
  - lib/threadline/operator_surface/live/export_status_live.ex
  - lib/threadline/operator_surface/live/retention_history_live.ex
  - lib/threadline/query.ex
autonomous: true
requirements: [NAV-02]
must_haves:
  truths:
    - "Timeline and Actor pages show a de-emphasized accessible pager (Older / range-caption / Newer) over the existing keyset events"
    - "The pager hides entirely at zero results; at a boundary the unavailable directional control is disabled, not removed (no layout shift)"
    - "The range caption is honest and relative ('Showing N of 10,000+ matching changes' when capped), in a role=status aria-live=polite region, never a fabricated page total"
    - "Exports and Retention show an honest cap caption ('Showing latest N …'); Coverage and Redaction show no pager"
    - "The keyset (captured_at, id) tiebreaker is documented as currently unbacked capture-layer perf debt; the capture layer is left untouched"
  artifacts:
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "Internal pager/1 (Older/Newer controls + role=status range caption, disable-not-hide)"
      contains: "def pager"
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "De-emphasized pager styles using existing --tl-* tokens (no new token)"
    - path: "lib/threadline/query.ex"
      provides: "Doc note: (captured_at, id) keyset tiebreaker is unbacked perf debt; composite index deferred (capture layer untouched)"
  key_links:
    - from: "UI.pager Older/Newer controls"
      to: "timeline/actor next-page/prev-page handlers"
      via: "phx-click emitting the existing events"
      pattern: "phx-click"
    - from: "UI.pager range caption"
      to: "format_count/match_count"
      via: "role=status aria-live=polite live region reusing the capped-count path"
      pattern: "role=\"status\""
---

<objective>
Add ONE reusable, de-emphasized internal `pager/1` ("Newer / range-caption / Older") over the
EXISTING keyset engine (D-15/D-16), adopt it on Timeline (next-only) and Actor (bidirectional),
add honest cap captions on Exports and Retention (D-20), and document the D-15 keyset tiebreaker
perf debt in query.ex (per the resolved Q1: defer the composite index, leave the capture layer
untouched). The pager fixes the infinite-scroll a11y gap — keyboard/SR users get explicit controls
plus an end-of-stream signal — while infinite scroll stays the primary interaction.

Purpose: pagination is clear when present and disappears at zero results; the audit-trust range
caption is honest and never fabricates a deep total. No engine change (D-15); the pager emits the
existing `next-page`/`prev-page` events. No public component API (v1.31 freeze).
Output: UI.pager/1 + style.ex pager CSS + Timeline/Actor adoption + Exports/Retention captions +
query.ex perf-debt note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/175-navigation-app-shell-runtime-theme-picker/175-CONTEXT.md
@.planning/phases/175-navigation-app-shell-runtime-theme-picker/175-PATTERNS.md
@.planning/phases/175-navigation-app-shell-runtime-theme-picker/175-UI-SPEC.md

<interfaces>
<!-- Exact assign/event names + analogs. Cite verbatim; no exploration needed. -->

button/1 disable-not-hide analog (ui.ex:7-30):
  attr :variant, :string, default: "secondary", values: ~w(primary secondary quiet-primary danger ghost icon)
  attr :rest, :global, include: ~w(disabled form name value phx-disable-with)   # `disabled` rides on :rest
  Disable the boundary control via the `disabled` global attr — never conditionally drop the element (D-18).

Live-region caption idiom to copy verbatim (timeline_live.ex:601-603):
  <div class="tl-status tl-timeline-command__status" role="status" aria-live="polite">
    <strong><%= @shown_count %> shown</strong>
    <span><%= format_count(@match_count) %> matching changes</span>
  </div>
  Reuse format_count/1 (timeline_live.ex:797-799; 10_001 -> "10,000+" cap; D-17). Domain noun = "matching changes".

Timeline events/assigns (one-directional — next only):
  handle_event("next-page", _, socket) at timeline_live.ex:297 -> stream(:changes, ..., at: -1) + assign(:cursor, next_cursor)
  phx-viewport-bottom={@cursor && "next-page"} at timeline_live.ex:382
  shown_count={length(@streams.changes.inserts)} (344); match_count={@match_count} (345); cap branches 369/375
  -> pager: older_event="next-page", older_enabled={@cursor != nil}, NO newer control on timeline (or disabled).

Actor events/assigns (bidirectional):
  handle_event("next-page")/"prev-page" at actor_live.ex:222/255; assigns :next_cursor / :prev_cursor (64-65, 217-218, 248, 281)
  phx-viewport-top="prev-page" / phx-viewport-bottom="next-page" at actor_live.ex:161-162
  -> pager: newer_event="prev-page" newer_enabled={@prev_cursor != nil}; older_event="next-page" older_enabled={@next_cursor != nil}.

Exports / Retention cap (D-20):
  export_status_live.ex @default_limit 100 (line 15); retention_history_live.ex @default_limit 40 (line 14, query order_by limit at 218)
  -> honest cap caption "Showing latest {N} …" in a role=status region; NOT a keyset pager.

Coverage (snapshot) + Policy/Redaction (single view) -> NO pager (D-20).

style.ex de-emphasized control analogs: tl-button--secondary / tl-button--ghost / tl-button--compact (ui.ex:7-30);
  tl-status live-region styling already present. Use existing --tl-* tokens (--tl-color-text-muted for disabled,
  --tl-space-2 control gap per UI-SPEC). Zero new tokens.

query.ex keyset tiebreaker sites (D-15 doc target — DO NOT change query logic):
  timeline order: query.ex:349-350 (desc captured_at, desc id); row history: 423-424; cursor type: 51
  capture migration single-col index: lib/threadline/capture/migration.ex:57 (captured_at only) — composite (captured_at,id) does NOT exist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build internal pager/1 in ui.ex + de-emphasized styles in style.ex</name>
  <files>lib/threadline/operator_surface/ui.ex, lib/threadline/operator_surface/style.ex</files>
  <read_first>
    - lib/threadline/operator_surface/ui.ex:7-30 (button + disabled global include), :719-759 (field_group/radio attr idiom), :143-165 (card slots)
    - lib/threadline/operator_surface/live/timeline_live.ex:597-605 (tl-status caption + format_count)
    - lib/threadline/operator_surface/style.ex:983-1021 (de-emphasized control styling references), and the tl-status block
    - test/threadline/operator_surface/pager_test.exs (the RED target from Plan 01)
  </read_first>
  <action>
    Add `@doc false` `pager/1` (no public API — v1.31 freeze) following the button/field_group attr
    idiom. Signature: `attr :newer_event, :string, default: nil`; `attr :newer_enabled, :boolean,
    default: false`; `attr :older_event, :string, default: nil`; `attr :older_enabled, :boolean,
    default: false`; `attr :shown, :integer, required: true`; `attr :total, :integer, required: true`;
    `attr :capped, :boolean, default: false`; `attr :class, :any, default: nil`; `attr :rest, :global`.
    Render NOTHING (return empty) when `@total == 0` (hide entire pager at zero results — D-18).
    Otherwise render a de-emphasized `<nav class="tl-pager">` (or `<div>`) containing: a "Newer"
    `<button phx-click={@newer_event} disabled={!@newer_enabled}>` (omit the Newer control entirely
    only when `@newer_event == nil`, e.g. timeline next-only; when present at a boundary, render it
    disabled — never drop it, D-18), a range caption, and an "Older" `<button phx-click={@older_event}
    disabled={!@older_enabled}>`. Copy = "Older" / "Newer" (time-axis, D-17), never "Next/Previous" or
    page numbers. The range caption is a `<span role="status" aria-live="polite">` rendering "Showing
    {shown} of 10,000+ matching changes" when `@capped`, else "Showing {shown} of {total} matching
    changes" (D-17). Domain noun "matching changes". Use the `disabled` global-attr idiom from
    button/1; the disabled boundary control must be visibly distinct from enabled (PAGE-02
    "disabled-looks-enabled" footgun) via `--tl-color-text-muted`/reduced opacity, not removal.

    style.ex: add de-emphasized `.tl-pager` styles using ONLY existing `--tl-*` tokens (control gap
    `--tl-space-2`, muted disabled state `--tl-color-text-muted`, comfortable ≥44px touch targets via
    `--tl-space-*` padding per A11Y-02). Zero new tokens (brand-parity must stay green). The caption
    text renders at the Label role (14px). The pager is de-emphasized relative to the primary content
    (secondary/ghost button treatment).
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/pager_test.exs` GREEN: pager renders nothing at
      `total == 0`; at a boundary the unavailable control is present with a `disabled` attribute (not
      dropped); range caption container has `role="status"` and `aria-live="polite"`; capped render
      shows "10,000+" and "matching changes" with no exact deep total.
    - pager is `@doc false`; source contains "Older" and "Newer"; no new `--tl-*` token added.
    - `brandbook_token_parity_test` GREEN; `mix test test/threadline/operator_surface/style_contract_test.exs` GREEN.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/pager_test.exs test/threadline/operator_surface/style_contract_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>Internal pager/1 with Older/Newer controls (disable-not-hide), a role=status range caption, hide-at-zero, capped "10,000+" copy, and de-emphasized styling on existing tokens; pager_test green.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt pager on Timeline + Actor; cap captions on Exports + Retention; document D-15 keyset perf debt</name>
  <files>lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/operator_surface/live/actor_live.ex, lib/threadline/operator_surface/live/export_status_live.ex, lib/threadline/operator_surface/live/retention_history_live.ex, lib/threadline/query.ex</files>
  <read_first>
    - lib/threadline/operator_surface/live/timeline_live.ex:340-385, 597-605 (main + viewport-bottom + caption); actor_live.ex:155-170, 210-285 (events/assigns)
    - lib/threadline/operator_surface/live/export_status_live.ex:15, ~129-135; retention_history_live.ex:14, ~88-92, 218
    - lib/threadline/query.ex:45-55, 345-355, 420-425 (keyset order + cursor type — read-only)
    - test/threadline/operator_surface/pager_test.exs, transaction_live_test.exs (regression)
  </read_first>
  <action>
    Timeline (next-only): render `<.pager>` near the existing caption (timeline_live.ex:597-605),
    keeping infinite scroll (`phx-viewport-bottom`) primary. Pass `older_event="next-page"`,
    `older_enabled={@cursor != nil}`, NO newer control (`newer_event={nil}`), `shown={@shown_count}`
    or `length(@streams.changes.inserts)`, `total={@match_count}`, `capped={@match_count >= 10_001}`.
    Do NOT serialize the cursor into the URL — it stays in assign (D-19).

    Actor (bidirectional): render `<.pager>` with `newer_event="prev-page"`,
    `newer_enabled={@prev_cursor != nil}`, `older_event="next-page"`, `older_enabled={@next_cursor != nil}`,
    and the actor's shown/total counts. Keep `phx-viewport-top/bottom` infinite scroll primary.

    Exports + Retention (D-20): add an honest cap caption "Showing latest {N} …" (N = the page's
    `@default_limit`, 100 / 40) in a `role="status" aria-live="polite"` region near the list header;
    NOT a keyset pager. Make the caption honest about the cap (recent-only, low-volume).

    Coverage + Policy/Redaction: confirm NO pager is added (D-20) — no change beyond Plan 03's
    page_header adoption.

    query.ex (D-15 reframe per resolved Q1): add a doc comment near the timeline keyset order_by
    (query.ex:349-350) and/or the cursor `@type` (line 51) documenting that the keyset tiebreaker
    `ORDER BY captured_at DESC, id DESC` is currently backed only by the single-column
    `audit_changes (captured_at)` index (capture/migration.ex:57); the composite `(captured_at, id)`
    index is DEFERRED as capture-layer perf debt (capture layer stays untouched this milestone), and
    ties on `captured_at` are rare (timestamp + random UUID PK). Leave a clear backlog pointer. Do NOT
    add a migration or change any query logic. The capture layer is untouched.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/pager_test.exs` and `transaction_live_test.exs` stay GREEN.
    - A Timeline render shows the pager with an "Older" control and the range caption; an Actor render
      shows both "Older" and "Newer" controls.
    - Exports and Retention renders contain a "Showing latest" cap caption in a `role="status"` region;
      no `tl-pager` keyset control on those pages.
    - `grep -n 'CREATE INDEX' lib/threadline/capture/migration.ex` is unchanged (capture layer untouched);
      query.ex contains a doc note referencing the deferred composite index and no new migration.
    - `mix ci.all` green (includes Playwright `verify.example_browser`).
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/pager_test.exs test/threadline/operator_surface/transaction_live_test.exs 2>&1 | tail -15; git diff --quiet lib/threadline/capture/migration.ex && echo "capture migration UNTOUCHED" || echo "ERROR: capture migration changed"</automated>
  </verify>
  <done>Timeline and Actor consume the accessible pager over existing events (cursor stays in assign); Exports/Retention show honest cap captions; Coverage/Redaction have none; the keyset perf debt is documented in query.ex with the capture layer untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → pager controls (phx-click) | LiveView events cross here; same handlers as the existing infinite scroll. |
| LiveView → Query keyset engine | Cursor (assign, not URL) seeds the next keyset page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-08 | Tampering | Cursor serialized into URL could be replayed/forged to point at a stale mid-scroll position (misleading in audit context) | mitigate | Cursor stays in socket assign, never the URL (D-19); only filters are URL-serialized. Pager emits the existing trusted events. |
| T-175-09 | Information Disclosure | Exact deep total via `COUNT(*)` would leak volume + add a perf cliff | mitigate | Reuse the capped `10_001` -> "10,000+" path (D-17); never reintroduce an exact deep total. |
| T-175-10 | Denial of Service | Unbacked `(captured_at, id)` tiebreaker is a latent perf cliff on deep timelines | accept | Documented as deferred capture-layer perf debt per resolved Q1 (capture layer untouched this milestone); ties on `captured_at` are rare (UUID PK). Backlog pointer left in query.ex. |
| T-175-SC | Tampering | npm/Hex/cargo installs | accept | No installs; zero new runtime deps invariant. No package legitimacy gate needed. |
</threat_model>

<verification>
- `mix test` for pager + transaction_live green; `mix verify.test` green.
- `mix ci.all` green before `/gsd:verify-work` (adds Playwright `verify.example_browser`).
- `brandbook_token_parity_test` green (no new tokens).
- Capture layer (`lib/threadline/capture/migration.ex`) byte-for-byte unchanged.
</verification>

<success_criteria>
Timeline and Actor show a de-emphasized accessible pager over the existing keyset events (Older/Newer +
honest role=status range caption, disable-not-hide, hide-at-zero, capped "10,000+"). Exports and
Retention show honest cap captions; Coverage and Redaction have no pager. The keyset `(captured_at, id)`
tiebreaker perf debt is documented and deferred; the capture layer is untouched.
</success_criteria>

<output>
Create `.planning/phases/175-navigation-app-shell-runtime-theme-picker/175-04-SUMMARY.md` when done
</output>
