---
phase: 171-audit-baseline-stress-lab-harness-idempotency-ledger
plan: 02
type: execute
wave: 2
depends_on:
  - 171-01
files_modified:
  - .planning/design-system-ledger.json
  - DESIGN-SYSTEM.md
  - test/threadline/operator_surface/stress_ledger_test.exs
autonomous: true
requirements:
  - DS-02
  - DS-03
  - DS-04
must_haves:
  truths:
    - "JSON is the canonical audit ledger and `DESIGN-SYSTEM.md` is the deterministic human projection per D-11 and D-15."
    - "Ledger entries are sorted, schema-checked, and carry score/ratchet metadata per D-12."
    - "ExUnit blocks markdown drift, unordered IDs, score drops, and silent removal of locked entries per D-13 and D-14."
    - "Ledger entries include DS-04 fixture keys from Plan 171-01 and folded future-owned baseline cases per D-21, D-22, D-23."
  artifacts:
    - path: ".planning/design-system-ledger.json"
      provides: "Canonical machine-readable design-system audit ledger"
      contains: "entries"
    - path: "DESIGN-SYSTEM.md"
      provides: "Human-reviewed inventory projection"
      contains: "Ratchet Rule"
    - path: "test/threadline/operator_surface/stress_ledger_test.exs"
      provides: "Ledger schema, ratchet, and projection freshness tests"
  key_links:
    - from: ".planning/design-system-ledger.json"
      to: "Threadline.OperatorSurface.StressFixtures.all/0"
      via: "ledger story_id and fixture_key coverage"
      pattern: "story_id"
    - from: "Threadline.OperatorSurface.StressFixtures.all/0"
      to: ".planning/design-system-ledger.json"
      via: "fixture ledger_id, story_id, and fixture_key round-trip"
      pattern: "fixture_key"
    - from: "DESIGN-SYSTEM.md"
      to: ".planning/design-system-ledger.json"
      via: "deterministic projection freshness test"
      pattern: "Ratchet Rule"
---

<objective>
Create the canonical JSON ledger, `DESIGN-SYSTEM.md` projection, and ratchet/freshness contracts.

Purpose: Later phases need a machine-checkable score ledger that can only ratchet quality upward while still giving humans a readable inventory.
Output: `.planning/design-system-ledger.json`, `DESIGN-SYSTEM.md`, and `stress_ledger_test.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-CONTEXT.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-RESEARCH.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-PATTERNS.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-UI-SPEC.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-VALIDATION.md
@lib/threadline/operator_surface/stress_fixtures.ex
@test/threadline/operator_surface/stress_fixtures_test.exs
@test/threadline/brandbook_token_parity_test.exs
@test/threadline/operator_surface/style_contract_test.exs

<interfaces>
Canonical ledger path: `.planning/design-system-ledger.json`.
Projection path: `DESIGN-SYSTEM.md`.
Required top-level JSON keys: `version`, `phase`, `ratchet_rule`, `required_inventory`, `screenshot_allowlist`, `ratchet`, `entries`.
Required entry keys: `id`, `kind`, `category`, `status`, `current_score`, `target_score`, `ratchet_score`, `stress_path`, `story_id`, `fixture_key`, `screenshot_baseline_refs`, `notes`, `owner_phase`, `source`.
Allowed entry kinds: `foundation`, `primitive`, `form_control`, `group`, `page`, `state`, `footgun`, `future_reserved`.
Every entry with `story_id`, `fixture_key`, or `stress_path` must resolve to `Threadline.OperatorSurface.StressFixtures.by_id/1` and must be renderable or explicitly reserved in `/audit/__stress`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write ledger schema and projection contract tests</name>
  <files>test/threadline/operator_surface/stress_ledger_test.exs</files>
  <read_first>
    test/threadline/operator_surface/stress_ledger_test.exs
    test/threadline/brandbook_token_parity_test.exs
    test/threadline/operator_surface/style_contract_test.exs
    lib/threadline/operator_surface/stress_fixtures.ex
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-CONTEXT.md
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-RESEARCH.md
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `.planning/design-system-ledger.json` parses with `Jason.decode!/1` and has top-level keys `version`, `phase`, `ratchet_rule`, `required_inventory`, `screenshot_allowlist`, `ratchet`, and `entries`.
    - Test 2: `entries` are sorted by `id`, and each entry contains exactly the required entry keys listed in the interfaces block plus optional `reserved_for_phase` and `reset_rationale`.
    - Test 3: each entry has integer `current_score`, `target_score`, and `ratchet_score`; `current_score >= ratchet_score`; `target_score >= current_score`; a lower current score is allowed only when `reset_rationale` is a non-empty string and the ID is listed in `ratchet.resets`.
    - Test 4: every `ratchet.locked_ids` value appears in `entries`, and every `ratchet.minimum_scores` key has an entry whose `current_score` is greater than or equal to that minimum.
    - Test 5: every `StressFixtures.all/0` story ledger ID appears in the JSON ledger and links back through `story_id` and `fixture_key`.
    - Test 6: every ledger entry with non-empty `story_id`, `fixture_key`, or `stress_path` round-trips through `StressFixtures.by_id(entry["story_id"])`; the returned story has matching `ledger_id` and `fixture_key`, and either renderable assigns through `assigns_for/1` or `status: "reserved"` with `reserved_for_phase`.
    - Test 7: `screenshot_allowlist` is an object with `ci` and `local_review` arrays; every allowlist item references an existing ledger entry and names `story_id`, `theme`, `viewport`, and `baseline_ref`.
    - Test 8: `DESIGN-SYSTEM.md` contains deterministic sections `Ratchet Rule`, `Foundations`, `Primitives`, `Form Controls`, `Groups`, `Pages`, `Known Footguns`, and `Future Reserved Cases`.
    - Test 9: `DESIGN-SYSTEM.md` is fresh: the test projects a deterministic inventory table from the ledger and fails if a ledger entry ID/status/score row is absent.
    - Test 10: source text for the ledger and markdown contains no YAML frontmatter, `PhoenixStorybook`, `Tailwind`, external visual service names, or `immutable ledger` copy.
  </behavior>
  <action>Create `test/threadline/operator_surface/stress_ledger_test.exs` first. Follow `brandbook_token_parity_test.exs`: `use ExUnit.Case, async: true`, module attributes for file paths, `File.read!/1`, `Jason.decode!/1`, one concern per test, and direct custom failure messages that name stale files and IDs. Do not create ledger JSON, markdown, Playwright tests, or route code in this task.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_ledger_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: running `mix test test/threadline/operator_surface/stress_ledger_test.exs` before Task 2 fails on missing or invalid `.planning/design-system-ledger.json` and `DESIGN-SYSTEM.md`.
    - Source assertion: `test/threadline/operator_surface/stress_ledger_test.exs` contains `@ledger_path ".planning/design-system-ledger.json"` and `@design_system_path "DESIGN-SYSTEM.md"`.
    - Source assertion: the test file asserts `current_score >= ratchet_score` and locked ID presence.
  </acceptance_criteria>
  <done>Ledger tests define the JSON schema, ratchet rule, fixture coverage link, screenshot allowlist shape, and markdown freshness contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create canonical JSON ledger and DESIGN-SYSTEM projection</name>
  <files>.planning/design-system-ledger.json, DESIGN-SYSTEM.md, test/threadline/operator_surface/stress_ledger_test.exs</files>
  <read_first>
    .planning/design-system-ledger.json
    DESIGN-SYSTEM.md
    test/threadline/operator_surface/stress_ledger_test.exs
    lib/threadline/operator_surface/stress_fixtures.ex
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: ledger JSON is deterministic, sorted, schema-valid, covers all fixture ledger IDs, and every ledger-owned entry round-trips back to a renderable or reserved fixture story.
    - Test 2: `DESIGN-SYSTEM.md` inventories foundations, primitives, form controls, groups/meta-components, pages, known footguns, and future reserved cases with current status and score.
    - Test 3: ratchet tests block score decreases and locked-entry deletions unless an explicit reset rationale is present.
  </behavior>
  <action>Create `.planning/design-system-ledger.json` as the canonical JSON source. Use `version: 1`, `phase: 171`, and a `ratchet_rule` string that says scores may only stay level or increase unless an explicit reset with rationale is recorded. Use deterministic key order in every entry. Seed entries from the Plan 171-01 fixture registry plus the current milestone inventory: foundation token groups `color`, `typography`, `spacing`, `radius`, `shadow`, `z-index`, `density`, `motion`; primitive inventory from DS primitive names; form-control inventory from DS form names; group inventory from GROUP-01 names; page inventory for Home, Timeline, Transaction, Row history, Actor, Coverage, Evidence, Redaction, Retention, Exports, and shell. Every ledger entry must carry a `story_id` and `fixture_key` that resolves through `StressFixtures.by_id/1`; entries for not-yet-implemented controls/groups/pages must point at explicit reserved placeholder stories rather than ledger-only rows. Include the folded todo entries with IDs `future.theme-picker-idiomatic-ui`, `footgun.coverage-schema-card-declutter`, and `footgun.transaction-page-left-push-desktop`, owner phases 175, 176, and 178, and copy `Reserved for Phase {phase}. This baseline records the current issue; do not fix it in Phase 171.`. Create `DESIGN-SYSTEM.md` as the deterministic human projection with no generated timestamp and with sections named in Task 1.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_ledger_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `mix test test/threadline/operator_surface/stress_ledger_test.exs` exits 0.
    - CLI assertion: `node -e "JSON.parse(require('fs').readFileSync('.planning/design-system-ledger.json','utf8')); console.log('ledger json ok')"` prints `ledger json ok`.
    - Behavior assertion: every ledger entry with `story_id` passes `StressFixtures.by_id(entry["story_id"])`, matches `fixture_key`, and is either renderable through `assigns_for/1` or explicitly reserved with `reserved_for_phase`.
    - Source assertion: `DESIGN-SYSTEM.md` contains `# Threadline Operator Surface Design System`, `Ratchet Rule`, `Foundations`, `Primitives`, `Form Controls`, `Groups`, `Pages`, `Known Footguns`, and `Future Reserved Cases`.
    - Source assertion: `rg -n 'PhoenixStorybook|Tailwind|Chromatic|Percy|Applitools|immutable ledger' .planning/design-system-ledger.json DESIGN-SYSTEM.md` returns no matches.
  </acceptance_criteria>
  <done>The JSON ledger is canonical, the markdown projection is fresh, and DS-02/DS-03 source contracts are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON ledger -> CI/tests | Repository JSON controls scores, locked IDs, and screenshot allowlist. |
| JSON ledger -> `DESIGN-SYSTEM.md` | Machine data becomes human-reviewed markdown. |
| Fixture registry -> ledger | Fixture story IDs become ledger entries and ratchet IDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-05 | Tampering | `.planning/design-system-ledger.json` | mitigate | Contract tests assert sorted IDs, required schema, `current_score >= ratchet_score`, locked ID presence, and explicit reset rationale for permitted decreases. |
| T-171-06 | Repudiation | `DESIGN-SYSTEM.md` projection | mitigate | Freshness test projects ledger rows into markdown expectations; stale or manually divergent inventory fails ExUnit. |
| T-171-10 | Information Disclosure | ledger notes and fixture refs | mitigate | Ledger entries reference synthetic fixture keys and story IDs only; no real user, DB, or secret values are stored. |
| T-171-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 171 installs no new packages; no package-manager install task exists in this plan. |
</threat_model>

<verification>
Focused command: `mix test test/threadline/operator_surface/stress_ledger_test.exs`.

Phase source-contract command after Plan 171-02: `mix test test/threadline/operator_surface/stress_fixtures_test.exs test/threadline/operator_surface/stress_ledger_test.exs`.
</verification>

<success_criteria>
DS-02 and the source-ledger portion of DS-03 are satisfied when `.planning/design-system-ledger.json` is deterministic and tested, `DESIGN-SYSTEM.md` inventories every required item class with statuses, markdown freshness is enforced, and ratchet tests block score regressions/deletions.
</success_criteria>

<output>
Create `.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-02-SUMMARY.md` when done.
</output>
