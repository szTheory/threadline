---
phase: 174-form-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/ui.ex
  - test/threadline/operator_surface/ui_test.exs
  - lib/threadline/operator_surface/stress_fixtures.ex
autonomous: true
requirements: 
  - COMP-04
  - COMP-06
must_haves:
  truths:
    - "Developers can use a monolithic `<.field>` component that correctly connects labels, inputs, and errors via WAI-ARIA."
    - "Zero new external dependencies are required for forms; native HTML5 controls are used."
    - "Form components use explicit `name`, `value`, and `errors` rather than requiring `Phoenix.HTML.Form` structs."
    - "Form primitives render correctly on the `/audit/__stress` test route."
  artifacts:
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "<.field>, <.input>, <.label>, <.error>, <.help> function components"
    - path: "test/threadline/operator_surface/ui_test.exs"
      provides: "Render assertions for form components and aria linkages"
  key_links:
    - from: "lib/threadline/operator_surface/ui.ex"
      to: "LiveView"
      via: "Explicit function components rather than struct-binding macros"
---

<objective>
Implement the core internal form components (`<.field>`, `<.input>`, `<.label>`, `<.error>`, `<.help>`) for the operator surface and ensure they are covered by both contract tests and stress fixtures.

Purpose: Provide a bulletproof, accessible baseline for forms that doesn't couple the application strictly to Ecto or `Phoenix.HTML.Form`, adhering to the `optional: true` contract.
Output: The `UI` module gains form primitives that automatically map WAI-ARIA linkages (e.g. `aria-describedby`), and the `/audit/__stress` route gains form fixtures.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-form-components/174-CONTEXT.md
@.planning/phases/174-form-components/174-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Browser | HEEx auto-escapes all strings rendered via `<%= @value %>`. Form components inherit standard LiveView constraints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01 | Tampering | `<.field>` inputs | mitigate | Let Phoenix HEEx automatically escape output. No unescaped rendering `raw()` is used in component implementation. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add form primitives to UI module</name>
  <files>lib/threadline/operator_surface/ui.ex, test/threadline/operator_surface/ui_test.exs</files>
  <behavior>
    - Test 1: `<UI.field>` renders label, input, and connected `aria-describedby` when errors/help are present.
    - Test 2: Non-color validation applies to errors (e.g. error icon prefix).
    - Test 3: Standard `<UI.input>` variations (text, textarea, select, checkbox, radio, date) output correct HTML5 markup with `tl-control`, `tl-checkbox` classes.
  </behavior>
  <action>
    Implement `<.label>`, `<.error>`, `<.help>`, `<.input>`, and a monolithic `<.field>` component in `UI`.
    - **Crucial:** Use explicit `name`, `value`, and `errors` as attributes instead of depending on `Phoenix.HTML.FormField`.
    - Automatically compute an ID using the input `id` to bind `aria-describedby` for help and errors (e.g. `aria-describedby={"#{id}-help #{id}-error"}`).
    - Use native HTML5 controls (`<select>`, `<input type="date">`). Avoid custom JS dropdowns.
    - Write exact unit tests verifying ARIA bindings and BEM class structures (`tl-field`, `tl-control`, etc) in `ui_test.exs`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <done>All new form components render correctly and WAI-ARIA bindings are verified via test.</done>
</task>

<task type="auto">
  <name>Task 2: Register form controls in stress fixtures</name>
  <files>lib/threadline/operator_surface/stress_fixtures.ex</files>
  <action>
    Register the new form primitives so they show up in the `/audit/__stress` test route.
    - Locate the `form_control` entries or primitive maps in `StressFixtures`.
    - Add explicit story maps for the newly created components (e.g., text, textarea, select, checkbox, radio, date, disabled/readonly states, error states).
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_fixtures_test.exs</automated>
  </verify>
  <done>Fixtures added and unit tests for stress_fixtures remain green.</done>
</task>

</tasks>

<verification>
Ensure all UI unit tests pass and test coverage remains high. WAI-ARIA linkages MUST be verified in the DOM.
</verification>

<success_criteria>
`mix test test/threadline/operator_surface/ui_test.exs` passes and components are successfully registered in `stress_fixtures.ex`.
</success_criteria>

<output>
Create `.planning/phases/174-form-components/174-01-SUMMARY.md` when done
</output>
