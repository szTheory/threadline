---
phase: 174-form-components
plan: "06"
type: execute
wave: 2
depends_on: ["174-05"]
files_modified:
  - lib/threadline/operator_surface/live/timeline_live.ex
  - test/threadline/operator_surface/formless_pages_test.exs
autonomous: true
requirements: [COMP-05]
gap_closure: true

must_haves:
  truths:
    - "timeline_live renders its two filter groups via the new field_group component, not raw <fieldset>"
    - "The 8 formless operator-surface pages contain no <input>/<select>/<textarea>/<form> (the hidden CSRF token aside) — locked by an assertion so future verification cannot re-flag it"
  artifacts:
    - path: "lib/threadline/operator_surface/live/timeline_live.ex"
      provides: "field_group adoption for primary + advanced filter groups"
      contains: "UI.field_group"
    - path: "test/threadline/operator_surface/formless_pages_test.exs"
      provides: "Guard asserting the 8 formless pages stay formless"
      contains: "formless"
  key_links:
    - from: "lib/threadline/operator_surface/live/timeline_live.ex"
      to: "lib/threadline/operator_surface/ui.ex field_group"
      via: "UI.field_group component call"
      pattern: "UI\\.field_group|<\\.field_group"
---

<objective>
Close Gap 2 (COMP-05): migrate the only genuine adoption gap and lock the "the rest are truly formless" conclusion so verification stops re-flagging it.

The orchestrator's verified gap analysis is authoritative: the components are ALREADY adopted everywhere a `<form>` exists (start_live, timeline_live, row_history_component, surface_header). The single remaining raw markup is `timeline_live.ex` using `<fieldset class="tl-filter-group ...">` at line 489 (primary) and line 554 (advanced). Migrate both onto the `field_group` component from 174-05. The 8 display-only pages (actor_live, coverage_live, evidence_live, export_status_live, policy_redaction_live, retention_history_live, row_history_live, transaction_live) are confirmed formless — do NOT add forms to them; instead assert they stay formless.

Purpose: COMP-05 marked complete with a regression guard; the surface_header hidden `_csrf_token` stays raw (legitimate).
Output: timeline_live filter groups using `field_group`; a test that fails CI if a form control sneaks into a formless page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-form-components/174-CONTEXT.md

<interfaces>
<!-- Component produced by 174-05 that this plan consumes. Source: lib/threadline/operator_surface/ui.ex after 174-05 -->
UI.field_group(assigns): attr `legend` (:string, required), `class`, `rest` (:global); slot `inner_block`.
  Renders <fieldset class={["tl-filter-group", @class]}><legend class="tl-filter-group__legend">{@legend}</legend>{inner_block}</fieldset>.

<!-- Current raw markup to replace. Source: lib/threadline/operator_surface/live/timeline_live.ex -->
Line ~489: <fieldset class="tl-filter-group tl-filter-group--primary"> ... <legend class="tl-filter-group__legend">Search</legend> ... </fieldset>
Line ~554: <fieldset class="tl-filter-group tl-filter-group--advanced"> ... <legend class="tl-filter-group__legend">Advanced filters</legend> ... </fieldset>
The fields inside each fieldset already use <UI.field ...> — leave those untouched; only the fieldset/legend wrapper changes.
</interfaces>

<formless_pages>
<!-- These 8 pages are display-only and MUST stay formless. Confirmed by grep (0 form controls each). -->
lib/threadline/operator_surface/live/actor_live.ex
lib/threadline/operator_surface/live/coverage_live.ex
lib/threadline/operator_surface/live/evidence_live.ex
lib/threadline/operator_surface/live/export_status_live.ex
lib/threadline/operator_surface/live/policy_redaction_live.ex
lib/threadline/operator_surface/live/retention_history_live.ex
lib/threadline/operator_surface/live/row_history_live.ex
lib/threadline/operator_surface/live/transaction_live.ex
<!-- Legitimately raw and EXCLUDED from the assertion: surface_header.ex <input type="hidden" name="_csrf_token"> -->
</formless_pages>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate timeline_live filter groups to UI.field_group</name>
  <files>lib/threadline/operator_surface/live/timeline_live.ex</files>
  <read_first>
    - lib/threadline/operator_surface/live/timeline_live.ex lines 480-600 (the two <fieldset> blocks at ~489 and ~554, including legend text and the --primary/--advanced modifier classes and the inner <UI.field> children)
    - lib/threadline/operator_surface/ui.ex def field_group (from 174-05 — confirm the legend attr name and class passthrough before calling it)
  </read_first>
  <action>
    Replace the raw `<fieldset class="tl-filter-group tl-filter-group--primary">` ... `</fieldset>` block (~line 489) with `<UI.field_group legend="Search" class="tl-filter-group--primary">` wrapping the existing `<div class="tl-filter-grid ...">` and its `<UI.field>` children; drop the now-duplicated raw `<legend class="tl-filter-group__legend">Search</legend>` since field_group renders the legend. Do the same for the advanced block (~line 554): `<UI.field_group legend="Advanced filters" class="tl-filter-group--advanced">`, removing its raw `<legend>`. Preserve the modifier classes (`--primary`/`--advanced`) by passing them through `class`, and keep all inner `<UI.field>` filter controls, the datalist, and the actions row exactly as-is. Do NOT change the `<form id="timeline-filters" role="search">` wrapper or the `<details>` disclosure.
  </action>
  <verify>
    <automated>grep -c '<fieldset' lib/threadline/operator_surface/live/timeline_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<fieldset' lib/threadline/operator_surface/live/timeline_live.ex` returns 0 (both raw fieldsets removed).
    - `grep -c 'UI.field_group\|<\.field_group' lib/threadline/operator_surface/live/timeline_live.ex` returns 2.
    - The legend strings "Search" and "Advanced filters" are passed via the `legend` attr (no remaining raw `<legend` in the two migrated blocks).
    - `--primary` and `--advanced` modifier classes still present (passed through `class`).
    - `mix compile --warnings-as-errors` clean.
    - Existing timeline LiveView test(s) still pass: `mix test test/threadline/operator_surface/live/timeline_live_test.exs` exits 0 (if the file exists; otherwise `mix test` for the operator_surface live dir).
  </acceptance_criteria>
  <done>Both timeline filter groups render through UI.field_group; no raw <fieldset> remains; tests/compile pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add a guard test locking the 8 formless pages as formless</name>
  <files>test/threadline/operator_surface/formless_pages_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/ui_test.exs (test module conventions: ExUnit.Case async: true, module naming)
    - lib/threadline/operator_surface/components/surface_header.ex (the legitimate hidden `_csrf_token` input — the ONE allowed exclusion; confirm its shape so the assertion excludes shared header markup, not the page templates)
    - the 8 page modules listed in <formless_pages> (confirm each has a render/1 or HEEx the test can scan as source)
  </read_first>
  <action>
    Create `test/threadline/operator_surface/formless_pages_test.exs` with `Threadline.OperatorSurface.FormlessPagesTest` (ExUnit.Case, async: true). The test reads the source of each of the 8 formless page modules from `lib/threadline/operator_surface/live/` and asserts the page-owned template contains no `<input`, `<select`, `<textarea`, or `<form` token. Read source via File.read! of the known absolute-relative module paths (mirror how the page list is enumerated). Because the shared `surface_header` (with its hidden CSRF input) is rendered by these pages but defined in a separate module, scanning each page module's OWN source naturally excludes the header — assert that fact in a comment. Use a per-page assertion (one assert per file, with the filename in the message) so a failure names the offending page. This locks the verified "they truly don't have forms" conclusion (COMP-05) so re-verification does not re-flag them.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/formless_pages_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `test/threadline/operator_surface/formless_pages_test.exs` exists and defines `Threadline.OperatorSurface.FormlessPagesTest`.
    - The test enumerates all 8 page paths (actor_live, coverage_live, evidence_live, export_status_live, policy_redaction_live, retention_history_live, row_history_live, transaction_live).
    - Each assertion checks the page source has no `<input`/`<select`/`<textarea`/`<form` and names the file on failure.
    - `mix test test/threadline/operator_surface/formless_pages_test.exs` exits 0 against current code.
    - The test does NOT assert anything about surface_header.ex (the hidden CSRF token is excluded by scanning page-own source).
  </acceptance_criteria>
  <done>A passing guard test fails CI if a form control is added to any of the 8 formless display pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → timeline filter form | filter values cross into the LiveView and are echoed back; unchanged by this refactor (wrapper-only migration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-03 | Tampering | timeline fieldset→field_group migration | mitigate | Pure markup-wrapper swap; inner <UI.field> escaping/behavior unchanged; existing timeline tests must stay green to prove no behavior drift. |
| T-174-04 | Repudiation/Drift | formless pages silently gaining a form later | mitigate | Guard test fails CI if any of the 8 pages gains <input>/<select>/<textarea>/<form>. |
| T-174-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; zero new runtime deps. Nothing to audit. |
</threat_model>

<verification>
- `grep -c '<fieldset' lib/threadline/operator_surface/live/timeline_live.ex` returns 0.
- `grep -c 'field_group' lib/threadline/operator_surface/live/timeline_live.ex` returns 2.
- `mix test test/threadline/operator_surface/formless_pages_test.exs` exits 0.
- `mix compile --warnings-as-errors` clean; full `mix test` for operator_surface live dir green.
</verification>

<success_criteria>
- timeline_live's two filter groups consume UI.field_group (last raw form-wrapper markup eliminated) — COMP-05 adoption gap closed.
- 8 formless pages are asserted formless by a CI guard, locking the verified conclusion against future re-flagging.
- surface_header hidden CSRF input left untouched (legitimately raw).
- No forms added to display-only pages.
</success_criteria>

<output>
Create `.planning/phases/174-form-components/174-06-SUMMARY.md` when done.
</output>
