---
phase: 174-form-components
plan: "02"
type: execute
wave: 2
depends_on: ["174-01"]
files_modified:
  - lib/threadline/operator_surface/live/start_live.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - lib/threadline/operator_surface/live/row_history_component.ex
  - lib/threadline/operator_surface/components/surface_header.ex
autonomous: true
requirements:
  - COMP-05
must_haves:
  truths:
    - "LiveView pages use the newly created form components instead of raw inline HTML."
    - "Template duplication for forms is reduced across the operator surface."
    - "Form component replacement introduces no regressions in application functionality."
  artifacts:
    - path: "lib/threadline/operator_surface/live/start_live.ex"
      provides: "Forms refactored to use `<UI.field>` / `<UI.input>`"
    - path: "lib/threadline/operator_surface/live/timeline_live.ex"
      provides: "Filter/search controls refactored to use `<UI.field>` / `<UI.input>`"
  key_links:
    - from: "LiveView templates"
      to: "lib/threadline/operator_surface/ui.ex"
      via: "Function component calls `<UI.field ... />`"
---

<objective>
Adopt the newly created form components (`<.field>`, `<.input>`) across the core operator surface LiveView pages to eliminate inline class-soup and consolidate form logic.

Purpose: Reduce template duplication across the 11 operator-surface LiveView pages and guarantee WAI-ARIA compliance in production flows.
Output: Core LiveView pages (`start_live`, `timeline_live`, `row_history_component`, `surface_header`) use structured form components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-form-components/174-CONTEXT.md
@.planning/phases/174-form-components/174-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator Input → Server | User input in forms must be passed to standard LiveView event handlers, retaining existing structural validations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-02 | Spoofing | Form submission | mitigate | Existing `phx-submit` and `phx-change` bindings use LiveView's secure WebSocket channels with built-in token verification. Do not bypass these standard mechanisms. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Adopt form components in LiveView pages</name>
  <files>lib/threadline/operator_surface/live/start_live.ex, lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/operator_surface/live/row_history_component.ex, lib/threadline/operator_surface/components/surface_header.ex</files>
  <action>
    Replace raw inline `<input>`, `<select>`, `<textarea>` tags with `<UI.input>` or `<UI.field>`.
    - In `start_live.ex`: Refactor the `record_lookup` and `correlation_lookup` forms.
    - In `timeline_live.ex`: Refactor the `timeline-filters` search form and `save-view-form`.
    - In `row_history_component.ex`: Refactor the `as_of` datetime-local input.
    - In `surface_header.ex`: Refactor the theme radio buttons (system/light/dark) using the `<UI.input type="radio">` component.
    - Maintain existing `phx-*` bindings, `name`, and `value` fields exactly as they are so application logic is unaffected.
    - Remove manually coded `<label>` and `<div class="tl-field">` wrappers where `<UI.field>` can absorb them.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/</automated>
  </verify>
  <done>All forms in these files use the new UI primitives and the entire operator_surface test suite passes.</done>
</task>

</tasks>

<verification>
Run `mix test test/threadline/operator_surface/` to confirm no regressions were introduced during template refactoring. WAI-ARIA and class validations were checked in Wave 1.
</verification>

<success_criteria>
Core operator surface LiveViews successfully render and operate with the new internal `<.field>` and `<.input>` primitives. Template code duplication is tangibly reduced.
</success_criteria>

<output>
Create `.planning/phases/174-form-components/174-02-SUMMARY.md` when done
</output>
