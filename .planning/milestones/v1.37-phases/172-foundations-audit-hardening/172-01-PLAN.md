---
phase: 172
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tokens.json
  - brandbook/tokens.css
  - DESIGN-SYSTEM.md
  - lib/threadline/operator_surface/style.ex
autonomous: true
requirements: [DS-05, DS-06]
must_haves:
  truths:
    - Tokens maintain visual consistency across components without brand deviations (D-01).
    - The design system documentation aligns with actual rendered styles (D-02).
    - Reduced motion users experience smooth state transitions without jarring jumps (D-03).
  artifacts:
    - path: brandbook/tokens.json
      provides: Primitive brand tokens adhering to the brand book
    - path: brandbook/tokens.css
      provides: CSS variables for primitive brand tokens
    - path: DESIGN-SYSTEM.md
      provides: Decision briefs for token overrides and motion reductions
    - path: lib/threadline/operator_surface/style.ex
      provides: Semantic token mapping and cross-fade motion media queries
  key_links:
    - from: lib/threadline/operator_surface/style.ex
      to: DESIGN-SYSTEM.md
      via: Code comments referencing ledger sections
---

<objective>
Audit foundation tokens against the brand book, document decisions in the ledger, and implement motion reduction cross-fades.
Purpose: Fix foundation drift, preserve motion accessibility, and prevent undocumented token overrides (DS-05, DS-06).
Output: Audited tokens in brandbook, updated DESIGN-SYSTEM.md ledger, and style.ex with token mapping comments and robust reduced-motion CSS.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-foundations-audit-hardening/172-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Foundation Audit & Token Gap Fixes</name>
  <read_first>
    - brandbook/brand-book.md
    - brandbook/tokens.json
    - brandbook/tokens.css
    - lib/threadline/operator_surface/style.ex
  </read_first>
  <files>
    - brandbook/tokens.json
    - brandbook/tokens.css
    - lib/threadline/operator_surface/style.ex
  </files>
  <action>
    Audit foundation tokens (color, typography, spacing, radius, motion) against `brandbook/brand-book.md`. Fix off-brand, contrast, and scale gaps in `tokens.json` and `tokens.css`. Update `style.ex` to ensure semantic tokens map correctly to the updated primitive values, fixing any hardcoded hex values or contrast issues.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <done>
    - `tokens.json` and `tokens.css` exactly match `brand-book.md`.
    - `style.ex` relies on primitive tokens without off-brand values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tokens Ledger & Semantic Comments</name>
  <read_first>
    - DESIGN-SYSTEM.md
    - .planning/phases/172-foundations-audit-hardening/172-CONTEXT.md
  </read_first>
  <files>
    - DESIGN-SYSTEM.md
    - lib/threadline/operator_surface/style.ex
  </files>
  <action>
    Append decision briefs to `DESIGN-SYSTEM.md` capturing the foundations decisions:
    1. **Semantic Token Mapping**: Keep `brandbook/tokens.json` strict to primitive brand tokens. Map primitives to functional UI tokens in `style.ex`.
    2. **Motion Reductions**: For `prefers-reduced-motion: reduce`, do not use universal 0ms. Zero out positional transitions but preserve opacity fades (`120ms ease`).
    In `style.ex`, add concise 1-2 line pointer comments (e.g., `/* See DESIGN-SYSTEM.md § "Semantic Token Mapping" */`) above the main token definition block to explain why component-specific variables are defined there.
  </action>
  <verify>
    <automated>grep -q "Semantic Token Mapping" DESIGN-SYSTEM.md</automated>
  </verify>
  <done>
    - `DESIGN-SYSTEM.md` contains sections for "Semantic Token Mapping" and "Motion Reductions".
    - `style.ex` contains a comment referencing `DESIGN-SYSTEM.md`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-fade Fallback for Reduced Motion</name>
  <read_first>
    - lib/threadline/operator_surface/style.ex
  </read_first>
  <files>
    - lib/threadline/operator_surface/style.ex
  </files>
  <action>
    Locate the `@media (prefers-reduced-motion: reduce)` block in `style.ex` (or create one if missing).
    Update it to use the cross-fade fallback pattern:
    - Zero out positional animations (`transform`, `translate`, `scale`) or set their durations to `0.01ms`.
    - Keep `opacity` transitions intact, explicitly setting them to a fast cross-fade like `transition: opacity 120ms ease !important` for elements that need state-change visibility.
    Avoid universal `transition: none !important;` or `transition: all 0ms;`.
  </action>
  <verify>
    <automated>grep -A 15 "prefers-reduced-motion: reduce" lib/threadline/operator_surface/style.ex | grep -q "opacity"</automated>
  </verify>
  <done>
    - `@media (prefers-reduced-motion: reduce)` block zero-outs or disables `transform`/`scale` but explicitly preserves `opacity` fades.
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS Parser | No untrusted input; tokens are static and compiled into the CSS macro. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-01 | Tampering | Tokens JSON | accept | Changes are source-controlled and part of internal design system assets. |
</threat_model>

<verification>
mix test test/threadline/operator_surface/style_contract_test.exs passes cleanly.
</verification>

<success_criteria>
Design decisions are recorded in DESIGN-SYSTEM.md, style.ex cross-references the decisions, and reduced motion respects opacity transitions.
</success_criteria>

<output>
Create `.planning/phases/172-foundations-audit-hardening/172-01-SUMMARY.md` when done
</output>