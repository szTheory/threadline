---
phase: 176-data-display-operator-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/presentation.ex
  - lib/threadline/operator_surface/components/icon.ex
  - test/threadline/operator_surface/presentation_test.exs
  - test/threadline/operator_surface/data_state_mapping_wave0_test.exs
  - test/threadline/operator_surface/live/retention_history_live_test.exs
  - test/threadline/operator_surface/card_nesting_regression_test.exs
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "An operator always recovers the exact full value: truncate_middle never drops the discriminating tail (>= 8 chars)"
    - "Presentation.ref/2 returns the three faces {visible, title, full} with full == the complete value"
    - "The data-state, T3-security, card-nesting, and ref-copy Wave-0 tests exist and fail RED against current code"
  artifacts:
    - path: "lib/threadline/operator_surface/presentation.ex"
      provides: "ref/2 (3-face), truncate_middle/2 :tail_min, value_token/1 truncation"
      contains: "def ref("
    - path: "lib/threadline/operator_surface/components/icon.ex"
      provides: "eye_off, plug (or cloud_off), lock, funnel glyphs"
      contains: "defp paths(:eye_off)"
    - path: "test/threadline/operator_surface/card_nesting_regression_test.exs"
      provides: "Wave-0 RED: no card-family class nested under another card-family class across 11 pages"
      contains: "tl-card"
  key_links:
    - from: "Presentation.ref/2"
      to: "Presentation.truncate_middle/2"
      via: "per-kind truncate_for/2 delegation"
      pattern: "truncate_middle"
    - from: "Presentation.ref/2"
      to: "Presentation.secondary_ref_value/1"
      via: "reuse existing value extraction (do not rebuild)"
      pattern: "secondary_ref_value"
---

<objective>
Establish the Wave-0 test scaffolds and the `Presentation` core + icon contracts that every downstream plan in Phase 176 builds against. This plan creates the deterministic forensic primitive (`ref/2` returning `%{visible, title, full}`, tail-safe middle truncation) and the four MISSING Wave-0 tests demanded by VALIDATION.md, then stops — it deliberately leaves the consuming LiveView/UI changes to later waves so those tests start RED and turn GREEN as the phase proceeds.

Purpose: the forensic non-negotiable (an operator must always recover the exact full value) lives entirely in `Presentation`; if the core helper or the tail-min guarantee is wrong, every copy button in the surface silently copies a truncated value. Interface-first: define the helper contract and the security/regression test contracts before any component or page consumes them.
Output: extended `presentation.ex` (`ref/2`, `truncate_middle/2` `:tail_min`, `value_token/1` truncation), new icon glyphs (`eye_off`, `plug`/`cloud_off`, `lock`, `funnel`), and four RED Wave-0 test files (T3 security, card-nesting regression, typed-reason→state mapping, ref-copy-equals-full).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-data-display-operator-patterns/176-CONTEXT.md
@.planning/phases/176-data-display-operator-patterns/176-RESEARCH.md
@.planning/phases/176-data-display-operator-patterns/176-PATTERNS.md
@.planning/phases/176-data-display-operator-patterns/176-VALIDATION.md
@.planning/phases/176-data-display-operator-patterns/176-UI-SPEC.md

<interfaces>
<!-- Extracted from working tree; executor uses these directly, no exploration needed. -->

From lib/threadline/operator_surface/presentation.ex (verified anchors):
- `truncate_middle(value, max_length \\ 34)` at L58 — current: `keep = max(div(max_length - 3, 2), 4)`; `export_summary/1` at L151 calls `truncate_middle(correlation, 28)` (backward-compat constraint).
- `secondary_ref(value, max_length \\ 34)` at L229 → returns `%{visible, title}`; delegates value extraction to `secondary_ref_value/1` at L360 (handles `%ActorRef{}` → "type/id", `%{"type","id"}`, JSON maps, to_string).
- `value_token/1` at L239 — returns `%{text, modifier, title}`; renders untruncated today.
- `change_value_token/2`, `human_time/2` (L10), `exact_time/1` (L29), `checked_label/1` (L33), `status_modifier/1` (L70), `status_label/1` (L90).

From lib/threadline/operator_surface/components/icon.ex (verified):
- `defp paths(:archive)`, `:copy`, `:history`, `:filter_x`, `:refresh`, `:shield`, `:trash`, `:warning`, `:search` exist.
- MISSING: `:eye_off`, `:plug` (or `:cloud_off`), `:lock`, `:funnel`. `defp paths(_), do: []` fallback at L44.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Presentation core — ref/2 three faces, tail-safe truncation, value_token truncation</name>
  <read_first>
    - lib/threadline/operator_surface/presentation.ex (the file being modified — read truncate_middle/2 L58, secondary_ref/2 L229, secondary_ref_value/1 L360, value_token/1 L239, export_summary/1 L151)
    - .planning/phases/176-data-display-operator-patterns/176-PATTERNS.md §"Presentation.ref/2" (copy-ready analog + the exact 2-face→3-face shape, file:line anchors L36-71)
    - .planning/phases/176-data-display-operator-patterns/176-UI-SPEC.md §"Truncation Rules" (per-type max table)
    - test/threadline/operator_surface/presentation_test.exs (the existing describe blocks to extend)
  </read_first>
  <behavior>
    - truncate_middle/2 with a `:tail_min` option (default preserving today's `max(div(max_length-3,2),4)` so export_summary/1's `truncate_middle(correlation, 28)` is byte-for-byte unchanged) guarantees >= 8 tail chars when `:tail_min` is set: for a 40-char value with max 34 and tail_min 8, the last 8 chars of the input appear verbatim in the result.
    - ref/2 returns `%{visible, title, full}` where `full == secondary_ref_value(value)` (the exact complete value), `title == full`, and `visible` is the per-kind truncation. For a value short enough to not truncate, visible == full.
    - Per-kind truncation (D-03) routed by a `:kind` opt through truncate_for/2: uuid/correlation → middle ~34; arn/actor → tail-weighted; hash → ~24; path → keep filename tail ~42; email → keep full domain; url → keep host head + last segment tail; timestamp → never truncated (returns full).
    - value_token/1 gains truncation (D-04, max ~56) while keeping its `%{text, modifier, title}` shape; title carries the full untruncated value.
  </behavior>
  <action>
    Add `ref/2` to `presentation.ex` returning `%{visible, title, full}` — `full = secondary_ref_value(value)` (REUSE the existing dispatch at L360; do NOT rebuild value extraction per D-01), `title = full`, `visible = truncate_for(value, opts)`. Add private `truncate_for/2` that dispatches on a `:kind` opt (`uuid|correlation|arn|actor|hash|path|email|url|timestamp`) to per-type rules from UI-SPEC "Truncation Rules"; `:timestamp` returns `full` unchanged. Extend `truncate_middle/2` with a `:tail_min` option (keyword opts or a 3rd arg) guaranteeing >= `:tail_min` tail chars from the input survive; default the split to today's `max(div(max_length - 3, 2), 4)` so `export_summary/1` (L151) is unaffected per D-03. Add truncation to `value_token/1` (L239) with max 56 (D-04), preserving the `%{text, modifier, title}` shape and putting the full value in `title`. Write the failing-then-passing unit tests in `presentation_test.exs` covering `:tail_min` (>= 8 tail), the three faces, each `:kind` rule, and the timestamp-never-truncates case. Do NOT touch `secondary_ref/2` callers yet (migration is Wave 3).
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/presentation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/presentation_test.exs` passes including new `:tail_min` (>= 8 tail), three-face `ref/2`, per-kind, and timestamp cases.
    - Source assertion: `presentation.ex` contains `def ref(` and `truncate_for`; `value_token/1` returns a map still containing `:text`, `:modifier`, `:title`.
    - Backward-compat: existing `export_summary/1` / `secondary_ref/2` tests still green (no regression from the `:tail_min` default).
    - `mix verify.format` clean.
  </acceptance_criteria>
  <done>ref/2 returns three faces with full==complete value; truncate_middle guarantees >=8 tail under :tail_min; value_token truncates at 56 keeping full in title; presentation_test green; format clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing data-state icons to the registry</name>
  <read_first>
    - lib/threadline/operator_surface/components/icon.ex (the file being modified — the `defp paths(:atom)` table L27-44 and `defp paths(_), do: []` fallback)
    - .planning/phases/176-data-display-operator-patterns/176-UI-SPEC.md §"Data-State Taxonomy" (which glyph each state uses: lock/shield permission, funnel no-data, plug/cloud-off down, eye-off redacted, archive pruned)
    - .planning/phases/176-data-display-operator-patterns/176-PATTERNS.md §"No Analog Found" (missing-icons row: eye_off, plug/cloud_off, lock, funnel)
  </read_first>
  <action>
    Add `defp paths(:eye_off)`, `defp paths(:funnel)`, `defp paths(:lock)`, and one of `defp paths(:plug)` / `defp paths(:cloud_off)` (pick the glyph that reads as "source down") to `icon.ex`, matching the existing pure-path `["M..."]` list shape of the neighbours (e.g. `:shield` at L41, `:archive` at L27). Keep them in alpha order with the rest. `:archive` (pruned) already exists — reuse it, do not re-add. Add no new dependency; paths are inline SVG `d` strings only.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'defp paths(:eye_off)\|defp paths(:funnel)\|defp paths(:lock)' lib/threadline/operator_surface/components/icon.ex` returns 3, plus exactly one of `:plug`/`:cloud_off` present.
    - `mix compile --warnings-as-errors` succeeds.
    - Each new clause returns a non-empty list of `"M..."` path strings (no empty list, which would fall through to the `_` fallback).
  </acceptance_criteria>
  <done>eye_off, funnel, lock, and one source-down glyph added as inline pure-path clauses; compile clean; archive reused for pruned.</done>
</task>

<task type="auto">
  <name>Task 3: Author the four RED Wave-0 tests (T3 security, card-nesting, typed-reason mapping, ref-copy-equals-full)</name>
  <read_first>
    - .planning/phases/176-data-display-operator-patterns/176-VALIDATION.md §"Wave 0 Requirements" (the six Wave-0 items + per-task verification map)
    - .planning/phases/176-data-display-operator-patterns/176-RESEARCH.md §"Common Pitfalls" (Pitfall 1 redact-no-backend, Pitfall 3 typed-reason normalization, Pitfall 4 copy-the-field) and §"Security Domain" (the STRIDE table)
    - test/threadline/operator_surface/style_contract_test.exs (assertion-style analog: reads source, asserts String.contains?)
    - test/threadline/operator_surface/live/retention_history_live_test.exs (if present) OR a sibling live test for the LiveView render harness
    - lib/threadline/operator_surface/live/retention_history_live.ex (handle_event "prune_now" L37, data-confirm L115/L159 — the code these tests target)
  </read_first>
  <behavior>
    - card_nesting_regression_test: renders each of the 11 operator pages and `refute`s any `tl-card*` class appearing nested inside another `tl-card*` element (DATA-05/D-12). RED today because coverage nests a hand-rolled header region.
    - retention T3 security test: asserts `prune_now` fails closed when (a) the typed confirmation token mismatches the canonical policy name, (b) a forged `phx-value-id`/scope is supplied, (c) authz is absent; and asserts a successful prune records an `AuditAction`. RED today because the handler has no secure_compare/authz/audit.
    - typed-reason→state mapping test: a `<:failed :let={reason}>` style harness asserting `:unauthorized`→permission, `:source_down`→unavailable(down), `:redacted`→unavailable(redacted), `:pruned`→unavailable(pruned), other→error — each rendering a DISTINCT icon shape + heading (DATA-03/D-16). RED today (no async dispatch, no variants).
    - ref-copy-equals-full contract test: for a value long enough to truncate, the rendered `data-tl-copy` equals the full value and `!=` the visible text, reusable across consuming LiveViews (DATA-01/Pitfall 4). RED today (transaction copies `.title`).
  </behavior>
  <action>
    Create `test/threadline/operator_surface/card_nesting_regression_test.exs` rendering all 11 pages (Home/start, timeline, transaction, row_history, actor, coverage, evidence, redaction, retention, exports, plus shell) and `refute`-ing card-family-under-card-family nesting (D-12). Create/extend `test/threadline/operator_surface/live/retention_history_live_test.exs` with the three T3 fail-closed cases + the audit-the-action assertion (use `Threadline.Semantics.AuditAction` — `audit_actions` schema, `changeset/2` L57 — and the `record_action` path referenced at `lib/threadline/audit.ex:196`). Create `test/threadline/operator_surface/data_state_mapping_wave0_test.exs` asserting each typed reason → distinct state component + icon + heading. Add the ref-copy-equals-full assertion as a shared helper (in the data_state or a new file) and reference it from the retention/transaction live tests. These tests MUST be RED now (they assert behavior this plan does not yet implement) — mark each with a module doc noting "Wave-0 RED until Plan 02/03/05". Do NOT implement the production behavior here.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/card_nesting_regression_test.exs test/threadline/operator_surface/data_state_mapping_wave0_test.exs test/threadline/operator_surface/live/retention_history_live_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All three new/extended test files COMPILE (no syntax/alias errors) and RUN.
    - The new behavioral assertions are RED (failing) against current code — confirmed in the run output — proving they bind real requirements, not tautologies. (A test that passes today would be a false scaffold.)
    - Source assertion: `card_nesting_regression_test.exs` references all 11 page modules; `retention_history_live_test.exs` asserts `secure_compare`/fail-closed semantics and an `AuditAction` record; `data_state_mapping_wave0_test.exs` covers `:unauthorized`/`:source_down`/`:redacted`/`:pruned`/other.
    - `mix verify.format` clean.
  </acceptance_criteria>
  <done>Four Wave-0 tests authored and RED against current code; compile + run cleanly; cover T3 fail-closed+audit, card-nesting, typed-reason mapping, ref-copy-equals-full; format clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → LiveView handle_event | Untrusted client signals (`phx-value-id`, typed confirmation) cross here; the server is the only authority |
| stored audit value → rendered copy target | Forensic integrity boundary: a truncated value must never become the copied/recovered value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-01 | Tampering (evidence) | `Presentation.ref/2` / `truncate_middle/2` | mitigate | `:tail_min` guarantees >= 8 tail chars survive; `full` always == complete value; unit-tested this plan. The ref-copy-equals-full Wave-0 test (Task 3) locks it across consumers. |
| T-176-02 | Tampering / EoP | retention `prune_now` (client-only `data-confirm`) | mitigate | This plan authors the RED fail-closed + audit test (Task 3); the server enforcement lands in Plan 05. The test is the gate that blocks merge until enforcement exists. |
| T-176-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new runtime deps, v1.37 invariant); RESEARCH.md "Package Legitimacy Audit: Not applicable". |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/presentation_test.exs` green (Task 1).
- `mix compile --warnings-as-errors` clean (Task 2 icons).
- New Wave-0 tests compile and run RED (Task 3) — they are intentionally failing scaffolds.
- `mix verify.format` clean.
- Capture & semantics layers untouched (this plan edits only `presentation.ex`, `icon.ex`, and test files — exploration layer only).
</verification>

<success_criteria>
- `Presentation.ref/2` returns `%{visible, title, full}` with `full` == the exact complete value; `truncate_middle/2` guarantees >= 8 tail chars under `:tail_min`; `value_token/1` truncates at 56 keeping full in title.
- Missing icons (`eye_off`, `funnel`, `lock`, one source-down glyph) added as inline pure-path clauses.
- All four MISSING Wave-0 tests from VALIDATION.md exist and are RED against current code.
- No capture/semantics-layer file touched.
</success_criteria>

<output>
Create `.planning/phases/176-data-display-operator-patterns/176-01-SUMMARY.md` when done.
</output>
