---
phase: 177-component-groups-meta-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/operator_surface/style_contract_test.exs
  - test/threadline/operator_surface/ui_test.exs
  - test/threadline/brandbook_token_parity_test.exs
autonomous: true
requirements: [GROUP-01, GROUP-02]

must_haves:
  truths:
    - "The LiveView connection-class CSS anchor is confirmed to be the `.threadline-ui` root element (where phoenix_live_view applies phx-loading/phx-error), recorded as the planned selector anchor."
    - "The page_header breadcrumbs reconciliation (keep the existing list attr, do NOT add a same-named slot) is recorded as the binding decision."
    - "A failing (RED) source assertion exists for the missing overlay JS-transition utility classes in style.ex."
    - "A failing (RED) source assertion exists requiring offline CSS keyed off `.threadline-ui.phx-loading`/`.phx-error` and refusing `body.phx-`/`.phx-disconnected`."
    - "A failing (RED) parity assertion exists requiring --tl-gap-inline/--tl-gap-stack/--tl-gap-section in tokens.css, tokens.json, and style.ex."
    - "A failing (RED) render assertion exists for stack/1, cluster/1, data_panel/1, toolbar/1, detail_header/1."
  prohibitions:
    - "MUST NOT add production code (ui.ex, style.ex, stress_fixtures.ex, tokens.*) in this plan — Wave 0 is tests + investigation only."
    - "MUST NOT make the new tests pass in this plan — they are RED scaffolds turned GREEN by Plans 02-05."
  artifacts:
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "RED source assertions for offline-class anchor + overlay JS-transition utility classes"
      contains: "phx-loading"
    - path: "test/threadline/operator_surface/ui_test.exs"
      provides: "RED render/coordination assertions for the 5 new components + breadcrumbs"
      contains: "data_panel"
    - path: "test/threadline/brandbook_token_parity_test.exs"
      provides: "RED parity assertion for the three --tl-gap-* tokens across all three sources"
      contains: "tl-gap-section"
  key_links:
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "File.read! source assertion on connection + overlay CSS"
      pattern: "threadline-ui.*phx-loading"
---

<objective>
Wave 0: resolve the two locked-decision-vs-reality conflicts surfaced by RESEARCH.md and lay down all the failing (RED) test scaffolds the rest of the phase turns GREEN. Zero production code.

Purpose: The phase has three verified conflicts between locked decisions and codebase reality (offline classes are on the LiveView root not `<body>` and `.phx-disconnected` does not exist; `page_header` already has a breadcrumbs *attr* not the slot D-04 asks for; the overlay JS-transition utility classes the overlays reference are not defined). This plan binds the resolutions and writes the tests that hold the implementation honest (Nyquist: tests precede code).
Output: Three test files extended with RED assertions; the two conflicts resolved in writing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-component-groups-meta-components/177-CONTEXT.md
@.planning/phases/177-component-groups-meta-components/177-RESEARCH.md
@.planning/phases/177-component-groups-meta-components/177-VALIDATION.md
@lib/threadline/operator_surface/ui.ex
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/style_contract_test.exs
@test/threadline/operator_surface/ui_test.exs
@test/threadline/brandbook_token_parity_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the LiveView connection-class anchor and bind the two locked-decision conflicts</name>
  <files>test/threadline/operator_surface (investigation; no source files modified)</files>
  <read_first>
    - lib/threadline/operator_surface/live/coverage_live.ex (L95 — `.threadline-ui` wrapper as the LiveView render root)
    - lib/threadline/operator_surface/live/timeline_live.ex (L329 — same wrapper)
    - lib/threadline/operator_surface/live/stress_live.ex (L73 — `.threadline-ui` wrapper)
    - lib/threadline/operator_surface/ui.ex (L184-244 — existing `breadcrumbs` ATTR + private `breadcrumb_trail/1`)
    - .planning/phases/177-component-groups-meta-components/177-RESEARCH.md (Pitfall 1, Pitfall 4, Open Questions 1-2, Assumption A2)
  </read_first>
  <action>
    Investigation + decision-binding task — produces the SUMMARY notes that govern Plans 03/04, no source change.

    (a) LiveView root anchor (resolves Pitfall 1 / Open Question 1 / A2, per D-08 corrected): Confirm by grep that all 11 audit LiveViews render their top-level element as `<div class="threadline-ui" data-tl-theme=...>` — this `.threadline-ui` element is the LiveView container element that phoenix_live_view v1.1 decorates with `phx-connected`/`phx-loading`/`phx-error`/`phx-client-error`/`phx-server-error`. Record the binding decision: the offline-group CSS (Plan 04) keys off `.threadline-ui.phx-loading` and `.threadline-ui.phx-error`, NOT `<body>`, and NEVER `.phx-disconnected` (does not exist in LiveView 1.x). Note in the SUMMARY that if any audit page's LiveView root is NOT `.threadline-ui`, the selector anchor must be adjusted — but the grep shows all 11 use it.

    (b) Breadcrumbs attr-vs-slot (resolves Pitfall 4, per D-14 discretion overriding D-04's literal wording): Record the binding decision: KEEP the existing `attr(:breadcrumbs, :list)` on `page_header/1` (breadcrumbs are location DATA `%{label, href}`, not arbitrary markup; a same-named slot would fail to compile). Plan 03 audits/extends `breadcrumb_trail/1` for narrow-viewport truncation only. Note the deviation from D-04's literal "slot" wording with the D-14 justification.

    Write both decisions into the plan SUMMARY so the checker does not flag the D-04 deviation and Plan 04 uses the right selector.
  </action>
  <verify>
    <automated>grep -c 'class="threadline-ui"' lib/threadline/operator_surface/live/coverage_live.ex lib/threadline/operator_surface/live/timeline_live.ex</automated>
  </verify>
  <acceptance_criteria>
    - Grep confirms all 11 audit LiveViews use `<div class="threadline-ui"...>` as their render root.
    - SUMMARY records: offline CSS anchor = `.threadline-ui.phx-loading`/`.phx-error` (not body, not `.phx-disconnected`).
    - SUMMARY records: keep breadcrumbs list attr (D-14), do NOT add a slot; note D-04 deviation.
  </acceptance_criteria>
  <done>Both locked-decision conflicts are bound in writing with the codebase-verified anchor and the D-14 justification recorded.</done>
</task>

<task type="auto">
  <name>Task 2: RED token-parity + style source scaffolds (gap tokens, offline anchor, overlay utilities)</name>
  <files>test/threadline/brandbook_token_parity_test.exs, test/threadline/operator_surface/style_contract_test.exs</files>
  <read_first>
    - test/threadline/brandbook_token_parity_test.exs (full — File.read! + String.contains? idiom; curated intersection; no token-count asserts)
    - test/threadline/operator_surface/style_contract_test.exs (full — File.read! source-assertion idiom, custom failure messages, refute for forbidden patterns)
    - lib/threadline/operator_surface/style.ex (L175-176 existing `--tl-gap-inline`/`--tl-gap-stack`; L3084-3160 keyframes; grep-confirmed: zero `.tl-fade-in`/`.translate-x-full`/`.tl-modal-container` class selectors)
    - brandbook/tokens.css (L6-16 `--tl-space-*`; no `--tl-gap-*`), brandbook/tokens.json (spacing block; no gap)
    - .planning/phases/177-component-groups-meta-components/177-RESEARCH.md (Pattern 2, Pitfall 1, Pitfall 2)
  </read_first>
  <action>
    Add RED assertions in the existing File.read! idiom. These MUST fail today and turn GREEN in Plans 02/04.

    (a) brandbook_token_parity_test.exs — add one test block asserting all three semantic gap tokens are present and value-aligned across the three sources: `--tl-gap-inline` maps to `--tl-space-2` (8px), `--tl-gap-stack` to `--tl-space-4` (16px), `--tl-gap-section` to `--tl-space-8` (32px). Assert the literal declarations exist in tokens.css and the matching keys exist in tokens.json, and assert `--tl-gap-section:` is present in style.ex (the other two already exist there at L175-176). Use the test's existing custom-failure-message convention. (RED: gap tokens absent from tokens.css/tokens.json; `--tl-gap-section` absent from style.ex.)

    (b) style_contract_test.exs — add one test block for the offline/reconnect group anchor: assert style.ex contains `.threadline-ui.phx-loading` and `.threadline-ui.phx-error`; REFUTE `.phx-disconnected` and REFUTE `body.phx-` (per the corrected D-08 / Pitfall 1). (RED: no offline CSS exists yet.)

    (c) style_contract_test.exs — add one test block for the overlay JS-transition utility classes that `show_modal`/`show_drawer`/`hide_*` reference but which are MISSING: assert style.ex defines class selectors for `.tl-fade-in`, `.tl-fade-out`, `.tl-rise-in`, `.tl-rise-out`, `.tl-slide-in-right`, `.tl-slide-out-right`, `.opacity-0`, `.opacity-100`, `.translate-y-0`, `.translate-y-4`, `.translate-x-0`, `.translate-x-full`, and the `.tl-modal-container`/`.tl-drawer-container` shells. Match against the class-selector form (e.g. `.tl-fade-in {` or `.tl-fade-in,`) so the existing `@keyframes tl-fade-in`/`tl-rise-in` do NOT false-green the assertion. Add an assertion that the data-region cross-fade rule `.tl-data-panel__region` carries a `transition: opacity var(--tl-motion-fast)` declaration. (RED: none of these class selectors exist.)

    Do NOT add a literal that a later refute-grep negative-gates on inside any prose `<action>` body. Keep these in test files only.
  </action>
  <verify>
    <automated>mix test test/threadline/brandbook_token_parity_test.exs test/threadline/operator_surface/style_contract_test.exs 2>&1 | grep -E "test|failure" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The new parity test block fails today (gap tokens absent in tokens.css/json; `--tl-gap-section` absent in style.ex).
    - The new style_contract offline block fails today and refutes `.phx-disconnected` + `body.phx-`.
    - The new style_contract overlay block fails today and is keyed to class-selector form (not keyframes).
    - All pre-existing tests in both files still pass (only the new blocks are RED).
  </acceptance_criteria>
  <done>Three RED source/parity scaffolds exist for the token + style work of Plans 02/04, in the established File.read! idiom.</done>
</task>

<task type="auto">
  <name>Task 3: RED render + coordination scaffolds for the 5 new components</name>
  <files>test/threadline/operator_surface/ui_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/ui_test.exs (full — `render_component/2` idiom for the existing UI components; assertion style)
    - lib/threadline/operator_surface/ui.ex (L155-182 `card/1` class-list idiom; L409-655 state family `kv`/`data_table`/`empty_state`/`error_state`/`loading_state`/`stale_banner`/`data_state`; L291 `pager/1`)
    - .planning/phases/177-component-groups-meta-components/177-RESEARCH.md (Patterns 1,3,4,5; State Coordination Contract)
    - .planning/phases/177-component-groups-meta-components/177-UI-SPEC.md (State Coordination Contract table; Group Spacing & Hierarchy Contract)
  </read_first>
  <action>
    Add RED `render_component/2` assertions to ui_test.exs for the five components Plans 02/03 will build. These MUST fail today (the functions do not exist) and turn GREEN there.

    (a) `stack/1`: rendering with `gap: "section"` emits `tl-stack tl-stack--section`; default emits `tl-stack--stack`; assert no raw inline `margin` on the element (it owns gap via flex). (b) `cluster/1`: emits `tl-cluster`, wraps, `justify: "between"` → `tl-cluster--between`. (c) `data_panel/1`: with `state: :ok` renders the `:data` slot; with `state: :loading` renders the loading region and the toolbar-disable signal is the page's job (assert the data slot is NOT rendered when loading); with `state: :permission` collapses the body to one message and the `:data` slot is NOT rendered; `as_of` present renders a stale banner ABOVE the region regardless of state; pager slot renders only when `state == :ok` (hide-when-not-ok). Assert the focus-move on error/permission is delegated (the rendered output carries a `tabindex="-1"` heading or `phx-mounted` focus from the existing state family — assert presence, do not reimplement). (d) `toolbar/1`: `disabled: true` emits `aria-disabled="true"` AND the container's `is-disabled` class; assert the contract that controls also carry the HTML `disabled` attribute is documented by passing a control through the slot and asserting it is rendered (the page sets `disabled` on controls from the same state assign). (e) `detail_header/1`: renders an `<h2>` title (NOT `<h1>` — page_header owns the single h1), a `kv` metadata block when `:metadata` slot present, and an actions cluster.

    Also add a `page_header` breadcrumbs assertion (extends the existing attr, per Task 1 decision): passing `breadcrumbs: [%{label: "Timeline", href: "/audit/timeline"}, %{label: "tx_…"}]` renders the `<nav aria-label="Breadcrumb">` trail with the last crumb non-linked. (This one may already partially pass via the existing attr; assert the trail renders — the truncation behavior is added in Plan 03 and gets its own assertion there.)
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs 2>&1 | grep -E "test|failure|undefined" | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - ui_test.exs has RED render assertions for stack/cluster/data_panel/toolbar/detail_header that fail today (functions undefined).
    - The data_panel assertions encode: ok→data slot; loading/permission→data slot suppressed; stale banner above region; pager only when :ok; focus-move delegated to the existing state family.
    - The toolbar assertion encodes aria-disabled + is-disabled + the HTML-disabled-on-controls contract.
    - The detail_header assertion encodes `<h2>` (not h1).
    - Pre-existing ui_test.exs assertions still pass.
  </acceptance_criteria>
  <done>The component render contract is pinned by RED tests before any component is written, so Plans 02/03 are test-driven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → LiveView | connection-class CSS is client-applied; tests assert the source CSS contract, not runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-01 | Tampering (CSP) | new style/source assertions | mitigate | Wave-0 refutes `body.phx-`/`.phx-disconnected`; the positive CSP posture (no inline `on*=`) is asserted in Plan 04 |
| T-177-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this phase (v1.37 zero-new-dep invariant); legitimacy gate vacuously satisfied |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/ui_test.exs test/threadline/operator_surface/style_contract_test.exs test/threadline/brandbook_token_parity_test.exs` — the new blocks are RED, everything else GREEN.
- `mix format --check-formatted` on the three touched test files.
</verification>

<success_criteria>
Both locked-decision conflicts are bound in writing; six RED scaffolds exist (3 source/parity + 5 component renders + 1 breadcrumbs trail) and fail today for the documented reasons; no production source changed.
</success_criteria>

<output>
Create `.planning/phases/177-component-groups-meta-components/177-01-SUMMARY.md` when done, recording the LiveView-root anchor decision, the breadcrumbs attr-keep decision (D-14 deviation from D-04), and the list of RED scaffolds with their owning downstream plan.
</output>
