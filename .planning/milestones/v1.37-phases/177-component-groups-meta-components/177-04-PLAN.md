---
phase: 177-component-groups-meta-components
plan: 04
type: execute
wave: 4
depends_on: ["177-03"]
files_modified:
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/ui.ex
autonomous: true
requirements: [GROUP-01, GROUP-02]

must_haves:
  truths:
    - "The reconnect/offline banner + action-disable CSS is keyed off the LiveView root `.threadline-ui.phx-loading`/`.phx-error` (NOT body, NEVER `.phx-disconnected`) — zero new JS, pure CSS, CSP-clean (D-08, corrected per Pitfall 1)."
    - "The previously-missing overlay JS-transition utility classes (tl-fade-in/out, tl-rise-in/out, tl-slide-in/out-right, opacity-0/100, translate-y-0/4, translate-x-0/full, tl-modal-container, tl-drawer-container) are defined in style.ex so the overlay enter/exit motion actually animates (D-10.1, Pitfall 2)."
    - "Overlay JS.show/hide transitions pass an explicit `time:` matching the motion token (180ms base for overlays) so the token stays the single source of truth (D-11, Pitfall 3)."
    - "All new group motion is GPU-only (transform/opacity) using the motion tokens and inherits the reduced-motion blanket — no per-component reduced-motion handling (D-11/D-12)."
  prohibitions:
    - "MUST NOT key offline CSS off `<body>` or `.phx-disconnected` (does not exist in LiveView 1.x) — anchor is `.threadline-ui` (Pitfall 1)."
    - "MUST NOT introduce any inline `on*=` handler or new JS dependency — motion is Phoenix.LiveView.JS + CSS only (CSP, ASVS V14; zero-new-dep invariant)."
    - "MUST NOT animate high-frequency timeline stream per-row inserts — only the data-region container cross-fades on a state change (D-11)."
    - "MUST NOT animate overlays from scale(0) — use opacity + small translate (MOTION-01)."
    - "MUST NOT disable mutating controls with pointer-events:none alone — add aria-disabled and (for links) tabindex=-1 (Pitfall 6)."
  artifacts:
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "offline-group CSS on .threadline-ui.phx-* + overlay JS-transition utility classes + reconnect banner"
      contains: "threadline-ui.phx-loading"
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "explicit time: on overlay JS.show/hide; reconnect banner markup + data-tl-mutating marker"
      contains: "data-tl-mutating"
  key_links:
    - from: "lib/threadline/operator_surface/style.ex (.threadline-ui.phx-loading)"
      to: "phoenix_live_view connection lifecycle"
      via: "client-applied connection classes on the LiveView root drive the banner + disable"
      pattern: "threadline-ui\\.phx-(loading|error)"
    - from: "lib/threadline/operator_surface/ui.ex (show_modal/show_drawer)"
      to: "lib/threadline/operator_surface/style.ex (overlay utilities)"
      via: "JS transition tuples reference the now-defined utility classes with explicit time:"
      pattern: "transition:.*tl-(fade|rise|slide)"
---

<objective>
Wave 4: complete the motion + connection-lifecycle layer. Define the missing overlay JS-transition utility CSS (so overlays actually animate), sync the JS.show/hide `time:` to the motion tokens, and build the reconnect/offline group on the LiveView root connection classes — all CSP-clean, GPU-only, inheriting the reduced-motion blanket.

Purpose: GROUP-02 "motion clarifies state transitions" + the reconnect/offline-banner+disabled-actions configuration (GROUP-01). RESEARCH.md found the overlay transition classes are referenced but undefined (motion is silently broken) and the offline classes were specified against the wrong element/name. This plan fixes both against the verified truth. Depends on Plan 03 (the data-region cross-fade rule landed with data_panel; this plan completes the overlay + offline CSS).
Output: overlay utility classes, `time:`-synced overlay transitions, `.threadline-ui.phx-*` offline CSS + reconnect banner; turns the Plan-01 offline + overlay source RED scaffolds GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-component-groups-meta-components/177-RESEARCH.md
@.planning/phases/177-component-groups-meta-components/177-UI-SPEC.md
@.planning/phases/177-component-groups-meta-components/177-01-SUMMARY.md
@lib/threadline/operator_surface/ui.ex
@lib/threadline/operator_surface/style.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the missing overlay JS-transition utility classes + sync JS.show/hide time:</name>
  <files>lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/ui.ex</files>
  <read_first>
    - lib/threadline/operator_surface/ui.ex (L676-843 — `modal`/`drawer`/`toast` + `show_modal`/`hide_modal`/`show_drawer`/`hide_drawer`/`hide_toast`; the JS.show/hide transition tuples referencing tl-fade-in, tl-rise-in, opacity-0/100, translate-y-4/0, tl-slide-in-right, translate-x-full/0, tl-fade-out, tl-rise-out, tl-slide-out-right)
    - lib/threadline/operator_surface/style.ex (L180-192 motion tokens `--tl-motion-fast` 120ms / `--tl-motion-base` 180ms / `--tl-ease-standard`; L3084-3160 existing `@keyframes tl-fade-in`/`tl-rise-in` — these are animation keyframes, NOT the class selectors the JS tuples need; L3868 reduced-motion blanket)
    - test/threadline/operator_surface/style_contract_test.exs (Plan-01 RED overlay-utility-class block, keyed to class-selector form not keyframes)
    - .planning/phases/177-component-groups-meta-components/177-RESEARCH.md (Pitfall 2 with the exact CSS sketch; Pitfall 3 time: default 200ms)
  </read_first>
  <action>
    Define the overlay JS-transition utility class SELECTORS in style.ex (they are grep-confirmed absent; only same-named `@keyframes` exist). A JS `transition:` tuple `{transition_classes, from, to}` needs the transition class to set `transition: <prop> <duration> <ease>` and the from/to classes to set the start/end values. Add: `.tl-fade-in`/`.tl-fade-out { transition: opacity var(--tl-motion-base) var(--tl-ease-standard); }`; `.tl-rise-in`/`.tl-rise-out` and `.tl-slide-in-right`/`.tl-slide-out-right { transition: opacity ... , transform ...; }`; `.opacity-0 {opacity:0} .opacity-100 {opacity:1}`; `.translate-y-0 {transform:translateY(0)} .translate-y-4 {transform:translateY(var(--tl-space-1))}` (or an existing motion-distance token — use `--tl-space-*`, no off-scale literal); `.translate-x-0 {transform:translateX(0)} .translate-x-full {transform:translateX(100%)}`; and the `.tl-modal-container`/`.tl-drawer-container` shells (positioning + the `.hidden { display:none }` utility the `if(!@show, do: "hidden")` toggle relies on). GPU-only (transform/opacity); the reduced-motion blanket at L3868 collapses these to ~1ms automatically (D-12) — no per-component media query.

    Then sync `time:` (Pitfall 3): `JS.show`/`JS.hide` default to 200ms, silently violating "reuse the motion tokens" (D-11). Pass an explicit `time: 180` (matching `--tl-motion-base`) to every overlay `JS.show`/`JS.hide` call in `show_modal`/`hide_modal`/`show_drawer`/`hide_drawer`/`hide_toast` so the token is the single source of truth.

    Add the toast fade-up ENTRANCE (Open Question 3): wire a `phx-mounted` fade-up on `toast/1` using the same utility classes + `time: 180` (auto-dismiss stays out of scope — manual/`phx-click` dismiss only).
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs 2>&1 | grep -iE "overlay|transition|fade|failure" | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.tl-fade-in' lib/threadline/operator_surface/style.ex` ≥ 1 as a class selector (not only the keyframe); same for the other listed utilities + `.tl-modal-container`/`.tl-drawer-container`/`.hidden`.
    - Every overlay `JS.show`/`JS.hide` passes an explicit `time:` (no bare 200ms default).
    - The Plan-01 overlay-utility-class source assertion is GREEN.
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>Overlay enter/exit motion is real (utility classes defined), token-synced (explicit time:), and reduced-motion-safe; the Plan-01 overlay RED scaffold is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Build the reconnect/offline group on the LiveView root connection classes</name>
  <files>lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/ui.ex</files>
  <read_first>
    - .planning/phases/177-component-groups-meta-components/177-01-SUMMARY.md (binding: anchor is `.threadline-ui`, the LiveView root — NOT body, NEVER `.phx-disconnected`)
    - lib/threadline/operator_surface/style.ex (existing `.threadline-ui[data-tl-theme=...]` selectors as the wrapper-scope precedent; L1431-1454 disabled CSS; L180-192 gap/motion tokens)
    - lib/threadline/operator_surface/ui.ex (where a reconnect banner + `data-tl-mutating` marker belongs — a small `@doc false` helper or documented markup the shell uses)
    - test/threadline/operator_surface/style_contract_test.exs (Plan-01 RED offline block: assert `.threadline-ui.phx-loading`/`.phx-error`, refute `.phx-disconnected`/`body.phx-`)
    - .planning/phases/177-component-groups-meta-components/177-RESEARCH.md (Pitfall 1, Pitfall 6, "Reconnect/offline group" code example, Don't Hand-Roll: no connected?/1 heartbeat)
  </read_first>
  <action>
    Add the connection-lifecycle group CSS to style.ex keyed off the LiveView ROOT `.threadline-ui` element (per the Plan-01 binding decision and Pitfall 1): the reconnect banner is hidden by default (`.tl-reconnect-banner { display:none }`) and shown via `.threadline-ui.phx-loading .tl-reconnect-banner, .threadline-ui.phx-error .tl-reconnect-banner { display:flex; gap: var(--tl-gap-inline); }` as a `role="status"` warning-tinted strip (icon + text, never color alone, A11Y-02). Disable mutating controls while disconnected/erroring: `.threadline-ui.phx-loading [data-tl-mutating], .threadline-ui.phx-error [data-tl-mutating] { pointer-events:none; opacity:0.55; }`. Per Pitfall 6, for mutating LINKS (which cannot take `disabled`) also set `aria-disabled` and `tabindex="-1"` via the markup. Do NOT use `<body>`, do NOT reference `.phx-disconnected` (it does not exist in LiveView 1.x — use `phx-loading`). Zero new JS, zero new deps — this rides phoenix_live_view's own client-applied classes, which catch a dropped socket mid-session (unlike a mount-time `connected?/1` assign — Don't Hand-Roll).

    Add the reconnect banner markup as an `@doc false` helper (e.g. `reconnect_banner/1`) in ui.ex rendering `<div class="tl-reconnect-banner" role="status">` with an icon + "Reconnecting…" copy (UI-SPEC Copywriting: status, calm, not an error, no "!"). Document that mutating controls carry `data-tl-mutating` so the CSS disables them. This is the shell-level group; it is audited as a stress story in Plan 05 (do not migrate the 11 pages here — that is Phase 178).
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs 2>&1 | grep -iE "offline|phx-loading|disconnect|reconnect|failure" | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'threadline-ui.phx-loading' lib/threadline/operator_surface/style.ex` ≥ 1; `.phx-error` anchor present.
    - `grep -c 'phx-disconnected' lib/threadline/operator_surface/style.ex` == 0 and no `body.phx-` selector.
    - Reconnect banner is `role="status"` with icon + text (not color alone); mutating controls/links disable via `[data-tl-mutating]` + aria-disabled.
    - The Plan-01 offline source assertion is GREEN; zero new JS / inline `on*=` introduced.
  </acceptance_criteria>
  <done>The reconnect/offline group rides the LiveView root connection classes (corrected anchor + class names), pure CSS, CSP-clean; the Plan-01 offline RED scaffold is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → LiveView | connection classes are client-applied on the LiveView root; CSS-only response, no server trust placed in them |
| disconnected session → mutating action | a dropped socket must not allow a mutating action to fire against stale state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-07 | Tampering (CSP) | overlay motion | mitigate | motion via Phoenix.LiveView.JS + CSS utility classes only; no inline `on*=`; source posture asserted (carried from D-175 CSP) |
| T-177-08 | Elevation / Tampering | offline action-disable | mitigate | `[data-tl-mutating]` + aria-disabled + tabindex=-1 for links; pointer-events:none is affordance only (Pitfall 6) |
| T-177-09 | — (supply chain) | motion lib | mitigate | zero new JS dep; motion is framework JS + CSS (zero-new-dep invariant) |
| T-177-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs (v1.37 zero-new-dep invariant) |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/style_contract_test.exs test/threadline/operator_surface/ui_test.exs` — overlay + offline source assertions GREEN, overlays still render.
- `mix ci.all` green at end of wave.
- Manual (`/gsd-verify-work`): drop the LiveView socket on `/audit/__stress`; confirm `phx-loading` reconnect banner appears (role=status) and mutating controls disable; observe overlay enter/exit + data-region cross-fade; re-check under `prefers-reduced-motion: reduce`.
</verification>

<success_criteria>
Overlay motion animates with token-synced timing; the reconnect/offline group rides the correct LiveView-root connection classes purely in CSS; all motion is GPU-only and reduced-motion-safe; no CSP regression, no new deps.
</success_criteria>

<output>
Create `.planning/phases/177-component-groups-meta-components/177-04-SUMMARY.md` when done.
</output>
