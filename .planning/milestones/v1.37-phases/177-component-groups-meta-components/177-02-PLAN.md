---
phase: 177-component-groups-meta-components
plan: 02
type: execute
wave: 2
depends_on: ["177-01"]
files_modified:
  - brandbook/tokens.css
  - brandbook/tokens.json
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/ui.ex
autonomous: true
requirements: [GROUP-01]

must_haves:
  truths:
    - "stack/1 renders a flex column owning vertical rhythm via --tl-gap-stack/--tl-gap-section, with no raw child margins (GROUP-01 spacing rule, D-02)."
    - "cluster/1 renders a flex-wrap row owning horizontal grouping via --tl-gap-inline (D-02)."
    - "The three semantic gap tokens (--tl-gap-inline/--tl-gap-stack/--tl-gap-section) exist and are value-aligned across tokens.css, tokens.json, and style.ex (D-09)."
  prohibitions:
    - "MUST NOT add a public/host-facing component API — stack/cluster are @doc false internal UI (v1.31 freeze, ASVS V1)."
    - "MUST NOT introduce any new runtime dependency (v1.37 invariant)."
    - "MUST NOT introduce off-scale or fractional spacing values — gap tokens alias the existing --tl-space-* ladder only."
    - "MUST NOT use raw child margins for rhythm — stack/cluster own gap via flexbox `gap`."
  artifacts:
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "UI.stack/1 + UI.cluster/1 layout primitives"
      contains: "def stack(assigns)"
    - path: "lib/threadline/operator_surface/style.ex"
      provides: ".tl-stack / .tl-cluster CSS + --tl-gap-section token"
      contains: "tl-gap-section"
    - path: "brandbook/tokens.css"
      provides: "--tl-gap-inline / --tl-gap-stack / --tl-gap-section declarations"
      contains: "tl-gap-section"
  key_links:
    - from: "lib/threadline/operator_surface/ui.ex"
      to: "lib/threadline/operator_surface/style.ex"
      via: "stack/cluster class names map to .tl-stack/.tl-cluster gap rules"
      pattern: "tl-stack|tl-cluster"
    - from: "lib/threadline/operator_surface/style.ex"
      to: "brandbook/tokens.css"
      via: "--tl-gap-* token parity (brandbook_token_parity_test)"
      pattern: "tl-gap-section"
---

<objective>
Wave 2: ship the layout primitives `UI.stack/1` + `UI.cluster/1` and the semantic gap tokens they consume. Pure layout, zero state coordination — this is the spacing foundation every group composes on.

Purpose: "Intentional spacing/hierarchy" (GROUP-01) stops being per-call-site class-soup once stack/cluster own the gap rhythm via named semantic tokens. This must land before the meta-components (Plan 03) compose them.
Output: `UI.stack/1`, `UI.cluster/1`, `--tl-gap-inline`/`--tl-gap-stack`/`--tl-gap-section`, `.tl-stack`/`.tl-cluster` CSS. Turns the Plan 01 gap-parity + stack/cluster RED scaffolds GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-component-groups-meta-components/177-RESEARCH.md
@.planning/phases/177-component-groups-meta-components/177-UI-SPEC.md
@.planning/phases/177-component-groups-meta-components/177-01-SUMMARY.md
@lib/threadline/operator_surface/ui.ex
@brandbook/tokens.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the three semantic gap tokens across all three sources (parity-first)</name>
  <files>brandbook/tokens.css, brandbook/tokens.json, lib/threadline/operator_surface/style.ex</files>
  <read_first>
    - brandbook/tokens.css (L6-16 `--tl-space-*` ladder; no `--tl-gap-*` yet)
    - brandbook/tokens.json (spacing block: keys "2"=8px, "4"=16px, "8"=32px)
    - lib/threadline/operator_surface/style.ex (L175-176 `--tl-gap-inline: var(--tl-space-2)`, `--tl-gap-stack: var(--tl-space-4)` already present; `--tl-gap-section` MISSING)
    - test/threadline/brandbook_token_parity_test.exs (the Plan-01 RED gap-parity block + the curated-intersection idiom)
  </read_first>
  <behavior>
    - tokens.css declares `--tl-gap-inline: var(--tl-space-2)`, `--tl-gap-stack: var(--tl-space-4)`, `--tl-gap-section: var(--tl-space-8)`.
    - tokens.json carries matching gap entries (8px/16px/32px) consistent with the parity test's expectations.
    - style.ex gains `--tl-gap-section: var(--tl-space-8)` alongside the existing two.
    - The Plan-01 gap-parity test goes GREEN; brandbook_token_parity_test stays GREEN in both dark and light lanes.
  </behavior>
  <action>
    Per D-09 (discretion, names match the `--tl-*` BEM idiom): add the three semantic gap aliases. Order matters for parity — land them in tokens.css + tokens.json (the brandbook SSOT mirror) AND style.ex in this one task so `brandbook_token_parity_test` never goes red mid-change (DS-05 rule). `--tl-gap-inline`/`--tl-gap-stack` already exist in style.ex (L175-176); add only `--tl-gap-section: var(--tl-space-8)` there, and add all three to tokens.css/tokens.json with values 8px/16px/32px. Map: inline→space-2 (8px), stack→space-4 (16px), section→space-8 (32px). Do not add the unused off-set steps (space-10/space-20) to any group consumer.
  </action>
  <verify>
    <automated>mix test test/threadline/brandbook_token_parity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' brandbook/tokens.css | grep -c 'tl-gap-section'` ≥ 1.
    - All three gap tokens present and value-aligned in tokens.css, tokens.json, style.ex.
    - `mix test test/threadline/brandbook_token_parity_test.exs` is fully green (no drift in either lane).
  </acceptance_criteria>
  <done>The semantic gap tokens exist and pass parity across all three sources; the Plan-01 gap RED scaffold is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement UI.stack/1 + UI.cluster/1 and their .tl-stack/.tl-cluster CSS</name>
  <files>lib/threadline/operator_surface/ui.ex, lib/threadline/operator_surface/style.ex</files>
  <read_first>
    - lib/threadline/operator_surface/ui.ex (L155-182 `card/1` — the canonical `["tl-x", @variant && "tl-x--#{@variant}", @class]` class-list + `attr(:rest, :global)` + `slot(:inner_block, required: true)` idiom; L23-38 `button/1` variant idiom)
    - lib/threadline/operator_surface/style.ex (L175-192 token block; flex/gap usage elsewhere for house style)
    - test/threadline/operator_surface/ui_test.exs (the Plan-01 RED stack/cluster render assertions)
    - .planning/phases/177-component-groups-meta-components/177-RESEARCH.md (Pattern 1 — flex+gap, no margin-collapse)
  </read_first>
  <behavior>
    - `stack/1` with `gap: "section"` → `class` contains `tl-stack tl-stack--section`; default gap → `tl-stack--stack`; element is `display:flex; flex-direction:column` with `gap: var(--tl-gap-{variant})` and NO raw child margins.
    - `cluster/1` → `tl-cluster`, `display:flex; flex-wrap:wrap; align-items:center; gap: var(--tl-gap-inline)`; `justify: "between"` → `tl-cluster--between`; `justify: "start"` → `tl-cluster--start`.
    - Both `@doc false`, strict `attr`/`slot`, trailing `attr(:rest, :global)`, `slot(:inner_block, required: true)`.
  </behavior>
  <action>
    Add `stack/1` and `cluster/1` to ui.ex following the existing `card/1` idiom exactly. `stack/1`: `attr(:gap, :string, default: "stack", values: ~w(stack section inline tight))`, `attr(:class, :any, default: nil)`, `attr(:rest, :global)`, `slot(:inner_block, required: true)`; renders `<div class={["tl-stack", "tl-stack--#{@gap}", @class]} {@rest}>`. `cluster/1`: `attr(:justify, :string, default: "start", values: ~w(start between end))`, plus class/rest/inner_block; renders `<div class={["tl-cluster", "tl-cluster--#{@justify}", @class]} {@rest}>`. Both `@doc false`.

    Add the matching CSS to style.ex near the other layout rules: `.tl-stack { display:flex; flex-direction:column; }` with `--stack`/`--section`/`--inline`/`--tight` modifiers each setting `gap: var(--tl-gap-*)` (tight maps to `--tl-space-1`/4px); `.tl-cluster { display:flex; flex-wrap:wrap; align-items:center; gap: var(--tl-gap-inline); }` with `--start`/`--between`/`--end` justify modifiers. No raw child-margin resets — flexbox `gap` owns the rhythm. Use only `--tl-*` tokens, no off-scale literals.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs -k "stack" 2>&1 | tail -5; mix test test/threadline/operator_surface/ui_test.exs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `stack/1` and `cluster/1` exist as `@doc false` function components matching the `card/1` class-list idiom.
    - The Plan-01 stack/cluster render assertions are GREEN.
    - `.tl-stack`/`.tl-cluster` CSS uses flexbox `gap` with `--tl-gap-*` tokens only; no raw child margins.
    - `mix compile --warnings-as-errors` clean; `mix format --check-formatted` clean.
  </acceptance_criteria>
  <done>stack/cluster ship as internal layout primitives owning the gap rhythm; the Plan-01 layout RED scaffolds are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app → UI module | components are `@doc false` internal; no public/host-facing API crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-02 | Elevation (API surface) | stack/cluster | mitigate | `@doc false`, internal `OperatorSurface.UI` only; no public component API (v1.31 freeze, ASVS V1) |
| T-177-03 | Tampering | token parity | mitigate | gap tokens land in all three sources in one task so `brandbook_token_parity_test` never drifts |
| T-177-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs (v1.37 zero-new-dep invariant) |
</threat_model>

<verification>
- `mix test test/threadline/brandbook_token_parity_test.exs test/threadline/operator_surface/ui_test.exs` — gap parity + stack/cluster GREEN.
- `mix ci.all` (format + credo + test) green at end of wave.
</verification>

<success_criteria>
stack/cluster ship as internal layout primitives over named semantic gap tokens; spacing rhythm is token-driven, not per-call-site margins; gap-token parity holds across all three sources.
</success_criteria>

<output>
Create `.planning/phases/177-component-groups-meta-components/177-02-SUMMARY.md` when done.
</output>
