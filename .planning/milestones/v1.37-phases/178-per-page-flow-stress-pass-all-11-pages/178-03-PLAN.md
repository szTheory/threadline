---
phase: 178-per-page-flow-stress-pass-all-11-pages
plan: 03
type: execute
wave: 3
depends_on: [178-01, 178-02]
files_modified:
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/style_contract_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts
autonomous: true
requirements: [PAGE-03]
tags: [operator-surface, page-03, centering, grid, justify-self, latent-twin, tier-a, tier-b]

must_haves:
  truths:
    - "Per D-09, `.tl-container` (style.ex:675-678) carries `justify-self: center` while keeping `max-width: 1000px` — centering the transaction page's grid-item `<main>` at >=768px"
    - "Per RESEARCH Pitfall 1 / Open Q1, `.tl-home` (style.ex:689-692) ALSO carries `justify-self: center` (the explicit latent-twin fix — same root cause, one line, fully guarded)"
    - "Per D-09, the Wave-0 Tier A `justify-self` guards for BOTH `.tl-container` and `.tl-home` are now GREEN"
    - "Per D-09, the Tier B geometry spec asserts the transaction (and Home) content bounding box is horizontally centered within grid column 2 at 1024 and 1440"
  prohibitions:
    - "MUST NOT remove `max-width: 1000px` from `.tl-container` or `.tl-home` (D-09 keeps the cap)"
    - "MUST NOT revert to bare `margin: 0 auto` as the centering mechanism on the grid item (that IS the bug — the guard exists to forbid it)"
    - "MUST NOT touch the transaction_live.ex `<main>` markup — the fix is CSS-only (D-09 markup-untouched)"
    - "MUST NOT add a new --tl-* token (justify-self is a layout property, not a spacing token; v1.37 invariant)"
    - "MUST NOT silently extend the fix beyond `.tl-container` and `.tl-home` without a visible note — and MUST NOT silently drop the Home twin"
  artifacts:
    - path: "lib/threadline/operator_surface/style.ex"
      provides: ".tl-container and .tl-home with justify-self:center (grid-native centering)"
      contains: "justify-self: center"
  key_links:
    - from: "lib/threadline/operator_surface/style.ex"
      to: "test/threadline/operator_surface/style_contract_test.exs"
      via: "the Wave-0 justify-self guards now match the source"
      pattern: "justify-self.*center"
---

<objective>
Fix the PAGE-03 transaction-page desktop centering bug (`transaction-page-left-push-desktop`) the grid-native way: add `justify-self: center` to `.tl-container`, keeping `max-width: 1000px`. Extend the identical one-line fix to `.tl-home` — RESEARCH confirmed it is a latent twin of the same bug (identical `max-width:1000px; margin:0 auto` on a grid-item `<main>`, style.ex:689-692). This turns the Wave-0 Tier A justify-self guards GREEN and the Tier B geometry cells pass.

Purpose: `margin: 0 auto` does not center a CSS-grid item under default `justify-self: stretch` — once `max-width` caps it, it anchors to the column start (left). `justify-self: center` is the correct grid-native centering (D-08/D-09). Guarding it makes silent reversion impossible.
Output: Two one-line CSS additions in style.ex; the Wave-0 guards green; the Tier B centering specs (transaction + Home) finalized.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add justify-self:center to .tl-container and .tl-home (PAGE-03 fix + latent-twin fix)</name>
  <files>lib/threadline/operator_surface/style.ex, test/threadline/operator_surface/style_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/style.ex:673-695 (`.tl-container` at :675-678 = `max-width:1000px; margin:0 auto`; `.tl-home` at :689-692 = identical) and :3891-3936 (the grid shell: `#tl-main` / the `.threadline-ui > :not(...)` catch-all is `grid-column:2`)
    - lib/threadline/operator_surface/live/transaction_live.ex:100 (`<main id="tl-main" class="tl-page tl-container">` — the only page putting tl-container on its grid-item main) and start_live.ex (`tl-page tl-home` main)
    - test/threadline/operator_surface/style_contract_test.exs (the Wave-0 RED justify-self guards for `.tl-container` and `.tl-home` this task turns green)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md § "Pattern 2" (grid-item centering) and § "Pitfall 1" (the Home twin)
  </read_first>
  <behavior>
    - GREEN: the Wave-0 assertion requiring `justify-self: center` in the `.tl-container` block passes
    - GREEN: the Wave-0 assertion requiring `justify-self: center` in the `.tl-home` block passes
    - `max-width: 1000px` remains in both blocks; `margin: 0 auto` may remain (harmless on a grid item) — the centering now comes from `justify-self`
  </behavior>
  <action>
    Add `justify-self: center;` to the `.tl-container` rule block at style.ex:675-678, keeping `max-width: 1000px` (D-09). Add the same `justify-self: center;` to the `.tl-home` rule block at style.ex:689-692 (RESEARCH Pitfall 1 / Open Q1 — the latent twin; this is the deliberate, visibly-noted extension the latent-Home-twin decision requires). This is the ENTIRE fix: do NOT edit transaction_live.ex or start_live.ex markup (D-09 markup-untouched), do NOT remove `max-width`, do NOT add any `--tl-*` token. Note in the SUMMARY that the Home twin was fixed in the same task with its own guard, per the explicit planner decision (RESEARCH recommendation), so it is neither silently dropped nor silently expanded.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs 2>&1 | grep -iE "justify-self|tl-container|tl-home|fail|test" | tail -15</automated>
  </verify>
  <done>Both `.tl-container` and `.tl-home` carry `justify-self: center` with `max-width:1000px` retained; both Wave-0 Tier A guards GREEN; no markup change; no new token; `mix compile --warnings-as-errors` + `mix format --check-formatted` clean; brand-token parity green.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the Tier B centering geometry specs (transaction + Home, 1024 + 1440)</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts (the Wave-0 scaffolded centering specs to finalize)
    - examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts:54,83 (boundingBox / within-viewport helpers) and playwright.config.ts (dark + desktop-chromium-light lanes)
    - lib/threadline/operator_surface/style.ex:3891-3936 (grid column 2 geometry: nav column `minmax(196px,232px)` offsets content right of true viewport center — RESEARCH Pitfall 4)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md § "Pitfall 4" (tolerance / measure within column 2, not raw viewport/2) and § "Open Question 2"
  </read_first>
  <action>
    Finalize the Wave-0 Tier B centering specs so they assert real centering after the fix: load `/audit/transactions/:id` at 1024 and 1440, measure the `.tl-container` bounding box, and assert it is horizontally centered WITHIN grid column 2 — measure symmetric left/right gutters of `.tl-container` inside its available content column, NOT against raw `viewport/2` (RESEARCH Pitfall 4: the nav column offsets content right of true viewport center by design). Pick a tolerance (D-14 discretion — document the chosen px value and the rationale, e.g. accounting for sub-pixel rounding + scrollbar gutter). Add the matching Home (`/audit`) centering cell at 1024+1440 for the latent twin. Run on both the dark and `desktop-chromium-light` lanes per the D-02 sample. NO pixel-diff. Confirm the specs PASS now that Task 1 landed the fix (un-`.fixme` any scaffold markers).
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && npx playwright test e2e/tests/operator-phase-178-uat.spec.ts -g "center" 2>&1 | tail -20</automated>
  </verify>
  <done>The transaction and Home centering specs at 1024+1440 (dark + light) PASS, measuring centering within grid column 2 with a documented tolerance; no pixel-diff; no scaffold markers left on the centering specs.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
NEW symbols created by this plan (excluded from drift verification):
- `justify-self: center` declarations added to `.tl-container` and `.tl-home` in style.ex (new property values on existing selectors)
- Finalized centering specs in `operator-phase-178-uat.spec.ts` (transaction + Home, column-2 measurement)
</artifacts_this_phase_produces>

<verification>
- `mix test test/threadline/operator_surface/style_contract_test.exs` — justify-self guards for both `.tl-container` and `.tl-home` GREEN.
- Tier B `operator-phase-178-uat.spec.ts` centering cells PASS at 1024+1440 on dark + light.
- `mix compile --warnings-as-errors`, `mix format --check-formatted`, brand-token parity all green; no `--tl-*` token added.
- `git diff lib/threadline/operator_surface/live/transaction_live.ex` empty (markup untouched).
</verification>

<success_criteria>
The transaction page centers at desktop widths (PAGE-03 resolved); the Home latent twin is fixed in the same guarded breath; reverting to bare `margin:0 auto` on the grid item is now structurally forbidden by the Tier A guards.
</success_criteria>

<output>
Create `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-03-SUMMARY.md` when done, explicitly noting the Home twin was fixed alongside transaction (visible decision, not silent), the chosen Tier B tolerance, and that markup was untouched.
</output>
