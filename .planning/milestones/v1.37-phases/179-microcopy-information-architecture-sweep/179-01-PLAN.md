---
phase: 179-microcopy-information-architecture-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/operator_surface/copy_contract_test.exs
  - lib/threadline/operator_surface/components/surface_header.ex
  - lib/threadline/operator_surface/live/start_live.ex
  - test/threadline/operator_surface/surface_header_test.exs
  - test/threadline/operator_surface/live/start_live_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
requirements_addressed:
  - COPY-01
  - COPY-02
  - COPY-03
must_haves:
  truths:
    - "D-01/D-02: The operator shell uses task-led/domain-led visible group labels while routes, URL shapes, current atoms, and data-testids remain unchanged."
    - "D-01/D-03: Overview remains reachable and tests assert the renamed Home jobs in the same change."
    - "D-04/D-16: Copy-contract tests reject primary UI CamelCase model names while allowing code-ish and advanced contexts."
    - "COPY-01/COPY-02/COPY-03: Guard-first coverage exists before broad copy edits proceed."
  artifacts:
    - path: "test/threadline/operator_surface/copy_contract_test.exs"
      provides: "Rendered copy-contract guard module for banned vocabulary, IA labels, title-case state leaks, proof terminology, and full-value copy affordances"
      exports:
        - "Threadline.OperatorSurface.CopyContractTest"
    - path: "lib/threadline/operator_surface/components/surface_header.ex"
      provides: "Shell navigation visible group labels"
      contains: "SurfaceHeader.surface_header/1"
    - path: "lib/threadline/operator_surface/live/start_live.ex"
      provides: "Task-led operator Home copy and validation messages"
      contains: "Threadline.OperatorSurface.Live.StartLive"
  key_links:
    - from: "test/threadline/operator_surface/copy_contract_test.exs"
      to: "lib/threadline/operator_surface/components/surface_header.ex"
      via: "rendered shell nav label assertions"
      pattern: "SurfaceHeader\\.surface_header"
    - from: "test/threadline/operator_surface/copy_contract_test.exs"
      to: "lib/threadline/operator_surface/live/start_live.ex"
      via: "rendered Home job-card and validation assertions"
      pattern: "StartLive"
    - from: "examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts"
      to: "lib/threadline/operator_surface/live/start_live.ex"
      via: "mobile Home and nav assertions"
      pattern: "Find what changed|Check audit readiness|Use evidence and exports"
---

<objective>
Create the guard-first copy contract and apply the first visible IA relabeling slice for Phase 179.

Purpose: COPY-01, COPY-02, and COPY-03 need executable protection before copy moves across the operator surface, and D-01 through D-03 make the shell/Home IA the first user-visible proof point.
Output: a new copy-contract test module plus updated shell and Home labels/tests that preserve routes, current atoms, data-testids, URL shapes, and power-user entry points.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/179-microcopy-information-architecture-sweep/179-CONTEXT.md
@.planning/phases/179-microcopy-information-architecture-sweep/179-RESEARCH.md
@.planning/phases/179-microcopy-information-architecture-sweep/179-VALIDATION.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-01-SUMMARY.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-02-SUMMARY.md
@CLAUDE.md
@brandbook/brand-book.md
@prompts/audit-lib-domain-model-reference.md
</context>

## Artifacts this phase produces

- `test/threadline/operator_surface/copy_contract_test.exs` creates `Threadline.OperatorSurface.CopyContractTest`.
- Copy-contract test-local helpers expected in that module: rendered HTML/text extraction, scoped shell group label extraction, scoped primary-heading/button/card checks, and allowlisted code-ish/domain-token predicates.
- No runtime dependencies, copy DSL, CMS, i18n layer, route, public API, LiveComponent, CSS class contract, or CLI flag is created.

<source_audit>
## Multi-Source Coverage Audit

| Source item | Plan coverage |
|-------------|---------------|
| GOAL: brand voice, banned vocabulary, domain language, GOV.UK least-surprise, progressive disclosure, power-user efficiency | Plans 179-01 through 179-06 cover this end to end through guard tests, copy normalization, IA relabeling, scoped page sweeps, stress evidence, and browser validation. |
| REQ COPY-01: brand voice, banned vocabulary, state/destructive/error copy | Plan 179-01 creates guard tests; Plan 179-02 normalizes shared state and unsupported templates; Plan 179-03 handles investigation/readiness page states; Plan 179-05 handles governance/export/destructive copy; Plan 179-06 refreshes stress evidence. |
| REQ COPY-02: domain language consistency | Plan 179-01 guards primary CamelCase/model leakage; Plan 179-02 normalizes helper terminology; Plan 179-03 handles actor/transaction/row/coverage copy; Plan 179-05 handles evidence/export/redaction/retention terminology. |
| REQ COPY-03: IA least surprise plus power-user efficiency | Plan 179-01 relabels shell/Home without route churn; Plan 179-04 preserves dense Timeline filters, URL state, copyable IDs, and direct links; Plan 179-06 proves stress-route coverage. |
| RESEARCH: guard-first editorial refactor, no new packages, avoid overbroad scans | Plan 179-01 creates rendered/scoped copy-contract tests and no install tasks. |
| CONTEXT D-01 through D-17 | D-01 through D-03 and D-16 are implemented here; D-04 through D-10 and D-15 are implemented in 179-02 and 179-03; D-11, D-12, and D-14 are implemented in 179-04; D-05, D-08, D-09, D-11, D-12, D-14, and D-15 are implemented in 179-05; D-17 is implemented in 179-06; D-13 is enforced by all implementation plans by not adding a novice/expert or density mode. |
| Deferred ideas | Not planned: novice/expert mode, i18n/copy registry, new filters/export flows, Phase 180 formal accessibility/motion/adversarial audit, and coverage-schema card declutter. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add guard-first rendered copy-contract tests</name>
  <files>test/threadline/operator_surface/copy_contract_test.exs</files>
  <behavior>
    - Shell nav contract: rendered group labels are exactly `Investigate`, `Audit readiness`, and `Evidence & exports`.
    - Home IA contract: rendered job titles are exactly `Find what changed`, `Check audit readiness`, and `Use evidence and exports`.
    - Copy safety contract: rendered primary UI rejects unallowed CamelCase model names, exclamation marks, title-case state leaks, broad proof wording, and missing full-value copy targets.
  </behavior>
  <read_first>
    - .planning/phases/179-microcopy-information-architecture-sweep/179-CONTEXT.md
    - .planning/phases/179-microcopy-information-architecture-sweep/179-VALIDATION.md
    - brandbook/brand-book.md
    - prompts/audit-lib-domain-model-reference.md
    - lib/threadline/operator_surface/components/surface_header.ex
    - lib/threadline/operator_surface/live/start_live.ex
    - lib/threadline/operator_surface/ui.ex
    - lib/threadline/operator_surface/unsupported.ex
    - lib/threadline/operator_surface/live/evidence_live.ex
    - lib/threadline/operator_surface/live/export_status_live.ex
    - test/threadline/operator_surface/surface_header_test.exs
    - test/threadline/operator_surface/live/start_live_test.exs
  </read_first>
  <action>Create `Threadline.OperatorSurface.CopyContractTest` per D-16 before copy edits. Use rendered/component HTML and scoped source extraction, not broad repository scans, so comments, tests, CSP-proof phrases, phone-proof fixtures, code tokens, operation badges, and evidence verdict terms remain allowlisted where D-04 and D-05 permit them. Cover these contracts: shell group visible labels are exactly `Investigate`, `Audit readiness`, and `Evidence & exports`; Home job titles are exactly `Find what changed`, `Check audit readiness`, and `Use evidence and exports`; primary headings/buttons/cards do not leak CamelCase model names except allowlisted advanced/error/code-ish contexts; visible operator copy has no exclamation marks; title-case state headings from D-06 examples do not render; broad proof terminology is limited to append-only proof-history links/details and evidence verdicts; copy affordances keep truncated visible IDs paired with full `data-tl-copy` values per D-10. Make the tests fail against the current shell/Home copy before Tasks 2 and 3 apply changes.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/copy_contract_test.exs --max-failures 6</automated>
  </verify>
  <acceptance_criteria>
    - `test/threadline/operator_surface/copy_contract_test.exs` defines `Threadline.OperatorSurface.CopyContractTest`.
    - The new test module includes assertions for D-01, D-02, D-04, D-05, D-06, D-10, and D-16.
    - The first run before source edits fails for the current standalone shell group labels and Home job labels, proving guard-first coverage exists.
    - The tests use rendered/scoped assertions and documented allowlists instead of repository-wide negative scans.
  </acceptance_criteria>
  <done>Copy-contract coverage exists and is intentionally red for the current IA/copy mismatches before implementation edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relabel shell navigation without route or atom churn</name>
  <files>lib/threadline/operator_surface/components/surface_header.ex, test/threadline/operator_surface/surface_header_test.exs</files>
  <behavior>
    - Rendering the shell shows the new D-01 group labels and existing destination nouns.
    - Every existing shell href, `current` atom, data-testid, section id, destination order, and mobile/keyboard behavior remains stable.
    - Component tests fail if copy changes arrive with route, atom, or test-id churn.
  </behavior>
  <read_first>
    - .planning/phases/179-microcopy-information-architecture-sweep/179-CONTEXT.md
    - lib/threadline/operator_surface/components/surface_header.ex
    - test/threadline/operator_surface/surface_header_test.exs
    - test/threadline/operator_surface/copy_contract_test.exs
  </read_first>
  <action>Update only visible shell group copy per D-01, D-02, D-03, and D-15: group headings become `Investigate`, `Audit readiness`, and `Evidence & exports`; destination nouns remain visible as `Overview`, `Timeline`, `Coverage`, `Evidence`, `Redaction`, `Retention`, and `Exports`. Preserve every route href, URL shape, `current` atom, data-testid, destination order, existing nav section id, and keyboard/mobile behavior. Update `surface_header_test.exs` in the same change so it asserts the new visible labels while also asserting the unchanged hrefs/current atoms/data-testids.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/surface_header_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Rendered shell nav contains group headings `Investigate`, `Audit readiness`, and `Evidence & exports`.
    - `Overview` remains present and links to the same operator start route.
    - Existing shell destination hrefs, `current` atoms, data-testids, and nav order remain unchanged.
    - `surface_header_test.exs` fails if visible labels change without also preserving href/current/test-id behavior.
  </acceptance_criteria>
  <done>Shell IA copy is renamed per D-01/D-02 while the operator routing and test contracts remain stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rename Home jobs and validation microcopy</name>
  <files>lib/threadline/operator_surface/live/start_live.ex, test/threadline/operator_surface/live/start_live_test.exs, examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts</files>
  <behavior>
    - Operator Home renders the three D-01 job titles and keeps the same navigation destinations.
    - Home validation messages name the invalid object and provide the next action without blame.
    - Saved searches, row history, correlation lookup, copy, keyboard, and direct-link workflows keep their existing behavior.
  </behavior>
  <read_first>
    - .planning/phases/179-microcopy-information-architecture-sweep/179-CONTEXT.md
    - .planning/phases/179-microcopy-information-architecture-sweep/179-VALIDATION.md
    - brandbook/brand-book.md
    - lib/threadline/operator_surface/live/start_live.ex
    - test/threadline/operator_surface/live/start_live_test.exs
    - examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
    - test/threadline/operator_surface/copy_contract_test.exs
  </read_first>
  <action>Update the task-led operator Home copy per D-01, D-03, D-04, D-06, D-08, D-10, D-11, and D-15. The three job titles are exactly `Find what changed`, `Check audit readiness`, and `Use evidence and exports`. Use plain nouns in primary copy, reserve exact model names for code-ish/advanced/error contexts, and keep saved search, row history, correlation, copy, keyboard, and direct-link workflows unchanged. Normalize validation messages to concise action templates: table selection asks the operator to select a table for row history; unmapped table asks for a mapped table; record id asks for a record id; empty correlation asks for a correlation id to open timeline; overlong correlation states the 256-byte limit. Update ExUnit and the Home mobile E2E spec so assertions match the new copy and still prove the same links/buttons/forms work.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/surface_header_test.exs test/threadline/operator_surface/live/start_live_test.exs &amp;&amp; ./examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-home-nav-mobile.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Home renders `Find what changed`, `Check audit readiness`, and `Use evidence and exports`.
    - Buttons/forms still route to Timeline, Coverage, Evidence, Redaction, Retention, Exports, row history, and correlation flows without URL or data-testid churn.
    - Empty/invalid Home form messages state the object and next action without blame.
    - The copy-contract test module is green after the shell/Home edits in this plan.
  </acceptance_criteria>
  <done>Operator Home uses the Phase 179 IA language and keeps all existing power-user workflows reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser to LiveView | Untrusted user input crosses through Home forms, filters, and LiveView events before copy is rendered back to operators. |
| Rendered copy to operator decisions | UI wording influences audit, retention, export, redaction, and evidence interpretation. |
| Copy affordance to clipboard | Truncated visible IDs must still expose exact full values only through intended copy targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-01 | Spoofing/Repudiation | Shell and Home IA | mitigate | Guard rendered labels and route/current/data-testid stability in copy-contract, component, and E2E tests so renamed groups do not imply new workflows. |
| T-179-02 | Information Disclosure | Home validation | mitigate | Validation copy names the object and next action without exposing additional audit data or blaming the operator. |
| T-179-03 | Tampering/Repudiation | ID copy affordances | mitigate | Copy-contract tests assert full `data-tl-copy` values remain paired with truncated visible refs per D-10. |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks exist; RESEARCH marks package legitimacy audit not applicable. |
</threat_model>

<verification>
Plan-local verification:
- `mix test test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/surface_header_test.exs test/threadline/operator_surface/live/start_live_test.exs`
- `./examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-home-nav-mobile.spec.ts`

Phase validation commands carried forward from `179-VALIDATION.md`:
- `mix test test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/ui_test.exs`
- `mix test test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/live`
- `mix ci.all` before verify-work.
</verification>

<success_criteria>
COPY-01, COPY-02, and COPY-03 have guard-first coverage for shell/Home IA; shell labels and Home task cards match D-01; broad D-02 shell group language is gone from primary nav; routes/current atoms/data-testids/full copy values remain unchanged; Home validation copy follows D-08 templates.
</success_criteria>

<output>
Create `.planning/phases/179-microcopy-information-architecture-sweep/179-01-SUMMARY.md` when done.
</output>
