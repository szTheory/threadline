---
phase: 173
plan: 03
type: execute
wave: 3
depends_on: ["173-02"]
files_modified:
  - lib/threadline/operator_surface/ui.ex
  - test/threadline/operator_surface/ui_test.exs
autonomous: true
requirements:
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - "Overlay primitives are fully driven by Phoenix.LiveView.JS"
    - "Accessibility semantics (aria-expanded, role=dialog, focus traps) are correct"
  artifacts:
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "Overlay components (modals, drawers, disclosures)"
    - path: "test/threadline/operator_surface/ui_test.exs"
      provides: "Unit tests for JS toggles and ARIA output"
  key_links: []
---

<objective>
Implement JS-driven overlay and disclosure primitives (modal/dialog, drawer/sheet, toast/flash, tooltip, popover, dropdown/menu, tabs, segmented control, accordion/disclosure).

Purpose: Establish JS-free (custom hooks) accessible transitions and state management for all floating elements using native `Phoenix.LiveView.JS`.
Output: Overlays in `ui.ex` with correct focus trapping and ARIA bindings.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/173-primitive-components-extract-audit-each-in-isolation/173-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement modal/dialog, drawer/sheet, and toast/flash</name>
  <files>lib/threadline/operator_surface/ui.ex, test/threadline/operator_surface/ui_test.exs</files>
  <behavior>
    - Test 1: Modal component renders with `role="dialog"` and `aria-modal="true"`
    - Test 2: Drawer uses JS macros for show/hide toggling
    - Test 3: Toast includes `phx-click-away` and escape dismiss mechanisms
  </behavior>
  <action>Add `modal`, `drawer`, and `toast` (flash) components to `ui.ex`. Implement supporting LiveView JS macros (`show_modal`, `hide_modal`, etc.) to enforce strict focus trapping (`JS.focus_first`/`JS.pop_focus`) and class-based open/close without custom Javascript hooks (per D-03).</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <done>Modals, drawers, and toasts have accessible JS-driven toggles passing HEEx unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement tooltips, popovers, dropdowns, and disclosures</name>
  <files>lib/threadline/operator_surface/ui.ex, test/threadline/operator_surface/ui_test.exs</files>
  <behavior>
    - Test 1: Dropdowns and popovers render `aria-expanded` and use `JS.set_attribute`
    - Test 2: Tabs, segmented control, and accordion render accessible semantics
  </behavior>
  <action>Add `tooltip`, `popover`, `dropdown`, `tabs`, `segmented_control`, and `accordion` to `ui.ex`. Use `phx-click-away` and declarative ARIA toggles via `JS.set_attribute({"aria-expanded", "true"})`. Avoid `window.addEventListener` anti-patterns.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <done>All disclosure and overlay primitives exist and are verified in unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component APIs | Internal HEEx bindings only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-03 | Denial of Service | Overlays | mitigate | Ensure focus traps have robust escape (`phx-window-keydown` mapped to `JS.hide()`) so the UI cannot be permanently locked. |
</threat_model>

<verification>
- `ui_test.exs` validates proper ARIA bindings and roles for interactive components
</verification>

<success_criteria>
Overlay primitives from COMP-02 use native Phoenix.LiveView.JS for focus trap, escape, and toggle semantics.
</success_criteria>

<output>
Create `.planning/phases/173-primitive-components-extract-audit-each-in-isolation/173-03-SUMMARY.md` when done
</output>