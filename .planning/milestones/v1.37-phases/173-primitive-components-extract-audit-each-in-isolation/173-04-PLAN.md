---
phase: 173
plan: 04
type: execute
wave: 4
depends_on: ["173-03"]
files_modified:
  - lib/threadline/operator_surface/live/stress_live.ex
  - test/threadline/operator_surface/ui_stress_test.exs
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
must_haves:
  truths:
    - "Every primitive is audited in isolation with a full interaction-state matrix"
    - "Non-interactive elements expose no misleading affordances on the stress route"
  artifacts:
    - path: "lib/threadline/operator_surface/live/stress_live.ex"
      provides: "Stress lab mounting of all new primitives"
    - path: "test/threadline/operator_surface/ui_stress_test.exs"
      provides: "Integration test for component state permutations"
  key_links:
    - from: "lib/threadline/operator_surface/live/stress_live.ex"
      to: "lib/threadline/operator_surface/ui.ex"
      via: "component rendering"
      pattern: "Threadline\\.OperatorSurface\\.UI\\."
---

<objective>
Audit every primitive by integrating them into the existing `/audit/__stress` route with a full interaction-state matrix, proving correct behavior across all states.

Purpose: Guarantee every state (hover, focus-visible, active, disabled) functions globally in dark, light, and system themes before rolling out across operator pages.
Output: Updated `stress_live.ex` and new `ui_stress_test.exs`.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/173-primitive-components-extract-audit-each-in-isolation/173-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create stress integration tests for UI permutations</name>
  <files>test/threadline/operator_surface/ui_stress_test.exs</files>
  <behavior>
    - Test 1: ui_stress_test.exs validates full matrix of component instances
    - Test 2: Ensures non-interactive badges/states don't output cursor-pointer or button-like bindings
  </behavior>
  <action>Create `test/threadline/operator_surface/ui_stress_test.exs`. Add integration or comprehensive unit tests rendering `Threadline.OperatorSurface.UI` primitives with all structural permutations (disabled/loading/selected/etc). Assert that interactive classes apply exclusively where expected.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_stress_test.exs</automated>
  </verify>
  <done>Permutation coverage exists in `ui_stress_test.exs`.</done>
</task>

<task type="auto">
  <name>Task 2: Mount all primitives on the stress lab route</name>
  <files>lib/threadline/operator_surface/live/stress_live.ex</files>
  <action>Update `StressLive` (the dev/test-only `/audit/__stress` harness) to visually mount all atoms, containers, and overlays created in Phase 173. Render the full interaction-state matrix (default/hover/focus-visible/active/pressed/disabled/loading/selected/current) so they are inspectable natively across dark/light/system themes.</action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>All primitives from `Threadline.OperatorSurface.UI` are visible on the stress route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Stress Route | Dev/test only, strictly gated and unavailable in production mounts. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-04 | Elevation of Privilege | `/audit/__stress` | mitigate | Ensure the stress route retains its existing fail-closed development constraints. |
</threat_model>

<verification>
- Full suite `mix test` passes
</verification>

<success_criteria>
All Phase 173 requirements (COMP-01, COMP-02, COMP-03) are satisfied with a complete audit matrix running on the stress route.
</success_criteria>

<output>
Create `.planning/phases/173-primitive-components-extract-audit-each-in-isolation/173-04-SUMMARY.md` when done
</output>