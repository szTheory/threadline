---
phase: 173
plan: 02
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - lib/threadline/operator_surface/ui.ex
  - test/threadline/operator_surface/ui_test.exs
autonomous: true
requirements:
  - COMP-01
  - COMP-03
must_haves:
  truths:
    - "Container and data-display primitives exist"
    - "Layout primitives resolve structural alignment constraints"
  artifacts:
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "Container components"
    - path: "test/threadline/operator_surface/ui_test.exs"
      provides: "Unit tests for UI container primitives"
  key_links: []
---

<objective>
Build container, layout, and data-display primitives (card/panel, stat tile, empty state, error state, code/log/JSON) and add them to the unified UI module.

Purpose: Extract layout-specific class-soup into reusable internal components that establish correct alignment properties system-wide.
Output: Enhanced `ui.ex` with containers and `ui_test.exs` test coverage.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/173-primitive-components-extract-audit-each-in-isolation/173-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement card/panel and stat tile</name>
  <files>lib/threadline/operator_surface/ui.ex, test/threadline/operator_surface/ui_test.exs</files>
  <behavior>
    - Test 1: card/panel component renders with correct boundary wrappers
    - Test 2: stat tile renders structured data slots
  </behavior>
  <action>Add `card` (or `panel`) and `stat_tile` components to `Threadline.OperatorSurface.UI`. Include structural foundation attributes to fix global left-pushed alignment issues (preparing for the transaction page fix). Enforce explicit `attr` definition.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <done>Card and stat_tile primitives pass unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement empty state, error state, and code blocks</name>
  <files>lib/threadline/operator_surface/ui.ex, test/threadline/operator_surface/ui_test.exs</files>
  <behavior>
    - Test 1: empty_state and error_state components render with correct messaging and slots
    - Test 2: code/log/JSON components render wrapped blocks safely
  </behavior>
  <action>Add `empty_state`, `error_state`, and `code_block` components to `ui.ex`. Ensure these components support necessary slots for inner content without exposing misleading active interactions if they contain static text.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <done>Empty state, error state, and code blocks exist and pass tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component APIs | Internal UI components processing pre-cleared operator UI data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-02 | Spoofing | `code_block` | accept | Uses built-in Phoenix HEEx safe HTML escaping; no raw output permitted. |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/ui_test.exs` passes for the new components
</verification>

<success_criteria>
Container primitives from COMP-01 exist with explicitly typed slots/attrs and proper structural wrappers.
</success_criteria>

<output>
Create `.planning/phases/173-primitive-components-extract-audit-each-in-isolation/173-02-SUMMARY.md` when done
</output>