---
phase: 180-accessibility-verification-guardrails-adversarial-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - 180-01
files_modified:
  - test/threadline/operator_surface/component_contract_test.exs
  - test/threadline/operator_surface/ui_test.exs
  - test/threadline/operator_surface/style_contract_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts
  - lib/threadline/operator_surface/ui.ex
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/components/surface_header.ex
autonomous: true
requirements:
  - A11Y-02
must_haves:
  truths:
    - "Custom widgets follow implementation-specific WAI-ARIA APG semantics or document native/non-applicable status per D-07."
    - "Color is never the only signal for state, status, navigation, validation, or disabled affordances per A11Y-02."
    - "Interactive controls keep comfortable target sizing without loosening dense operator workflows per D-06."
  artifacts:
    - path: "test/threadline/operator_surface/component_contract_test.exs"
      provides: "Shared component semantics and APG guardrails"
      contains: "OperatorSurface.ComponentContractTest"
    - path: "test/threadline/operator_surface/ui_test.exs"
      provides: "Rendered HTML component assertions for widgets, fields, alerts, tables, and data states"
      contains: "OperatorSurface.UITest"
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "Non-color, focus, target sizing, and style contract checks"
      contains: "OperatorSurface.StyleContractTest"
  key_links:
    - from: "test/threadline/operator_surface/ui_test.exs"
      to: "lib/threadline/operator_surface/ui.ex"
      via: "component rendering tests assert semantics on shared UI helpers"
      pattern: "rendered_to_string"
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "style contract scans generated operator CSS"
      pattern: "@style_path"
---

<objective>
Create the A11Y-02 semantic and non-color guardrail plan for Phase 180.

Purpose: Convert APG and non-color requirements into executable checks tied to actual Threadline components, not a generic checklist, per D-03, D-06, and D-07.
Output: Tightened existing ExUnit and Playwright checks plus only narrow component/style fixes required for A11Y-02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/180-accessibility-verification-guardrails-adversarial-closeout/180-CONTEXT.md
@.planning/phases/180-accessibility-verification-guardrails-adversarial-closeout/180-RESEARCH.md
@.planning/phases/180-accessibility-verification-guardrails-adversarial-closeout/180-VALIDATION.md
@examples/threadline_phoenix/AGENTS.md
@test/threadline/operator_surface/component_contract_test.exs
@test/threadline/operator_surface/ui_test.exs
@test/threadline/operator_surface/style_contract_test.exs
@examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts
@lib/threadline/operator_surface/ui.ex
@lib/threadline/operator_surface/style.ex
@lib/threadline/operator_surface/components/surface_header.ex
</context>

<source_audit>
## Phase 180 Coverage

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Verify WCAG 2.2 AA rendered accessibility, motion compliance, guardrails, and adversarial closeout | Plans 180-01 through 180-04 |
| REQ | A11Y-01 rendered state, keyboard, visible focus, restored focus, screen-reader-ready evidence | Plan 180-01 and final manual evidence in 180-04 |
| REQ | A11Y-02 APG semantics, non-color cues, touch target checks | Plan 180-02 |
| REQ | MOTION-01 motion token and reduced-motion contract | Plan 180-03 |
| REQ | MOTION-02 guardrail matrix and adversarial review | Plan 180-04 |
| RESEARCH | Actual APG map for modal, drawer, disclosure, tabs, dropdown, combobox, tooltip, popover, table, alert, status | Plan 180-02 per D-07 |
| RESEARCH | No new dependency or framework; extend existing harnesses | Plan 180-02 per D-03 |
| CONTEXT | D-03 existing harnesses, D-06 dense workflows, D-07 actual APG mapping, D-13 regression-only coverage card | Plan 180-02 and 180-04 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add implementation-specific APG and non-applicability checks</name>
  <files>test/threadline/operator_surface/component_contract_test.exs, test/threadline/operator_surface/ui_test.exs, examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts</files>
  <behavior>
    - Modal and drawer assertions cover dialog role, label source, modal state, close affordance, focus behavior, and dismissal semantics.
    - Disclosure, accordion, tabs, segmented control, dropdown/menu, combobox/select/search, tooltip, popover, table/list/data panel, alert, status, stale, loading, and error summary assertions map to actual `UI.*` components or native HTML.
    - Each custom-widget category is either verified against actual implementation or marked non-applicable with a concrete reason from current source, per D-07.
  </behavior>
  <action>Extend existing ExUnit component/UI tests and the existing browser accessibility spec only. Build the APG map from current `UI.*` component implementations and actual stress/operator usage; do not add generic APG fixtures, new sample apps, or new packages per D-03. Where Threadline uses native HTML, assert the native element and label behavior instead of forcing custom roles. Where a category is absent, add a narrowly named non-applicability assertion that cites the missing actual implementation rather than leaving the category untested. Keep the coverage-schema-card-declutter story as regression-only evidence, not a new layout target, per D-13.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/component_contract_test.exs test/threadline/operator_surface/ui_test.exs</automated>
    <automated>./examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-accessibility.spec.ts</automated>
  </verify>
  <done>APG-related categories in the research map have executable checks tied to real Threadline components, native elements, or explicit non-applicability statements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten non-color and target-size style guardrails</name>
  <files>test/threadline/operator_surface/style_contract_test.exs, lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/ui.ex, lib/threadline/operator_surface/components/surface_header.ex</files>
  <behavior>
    - State, status, validation, current navigation, disabled controls, pager state, and alert surfaces expose non-color signals through icons, text, borders, roles, names, or disabled attributes.
    - Buttons, shell navigation, pager controls, theme controls, filter controls, and compact toolbar controls keep bounded target sizing compatible with the existing dense operator UI.
    - Style checks avoid file-wide negative scans that would conflict with legitimate component cases elsewhere in the same file.
  </behavior>
  <action>Add style and component contract checks before fixes. Use existing contract helpers and region-scoped scans where possible so comments or unrelated selectors do not create false failures. If checks fail, update only shared UI/style definitions needed to provide non-color state cues or comfortable hit targets. Preserve compact spacing, existing markup names, LiveView route behavior, and public APIs per D-06. Do not introduce new design tokens unless the current token set cannot express the required hit area or cue.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs test/threadline/operator_surface/component_contract_test.exs test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <done>A11Y-02 non-color and target-size checks are green without reducing information density or reopening the coverage card layout scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component render to browser DOM | Shared component attributes become browser-visible semantics and must not imply unavailable behavior. |
| CSS affordances to user perception | Visual state cues cross into user decision-making and must not rely on color alone. |
| Stress/test harness to production code | Test-only APG coverage must not create new production routes or public APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-04 | Spoofing | APG roles and names in `UI.*` | mitigate | Assert only semantics backed by actual behavior; document native/non-applicable cases instead of adding misleading roles. |
| T-180-05 | Tampering | Style contract scans | mitigate | Use bounded selectors and existing style helpers so checks guard intended affordances without broad false positives. |
| T-180-06 | Denial of Service | Browser and ExUnit guardrails | accept | Existing harnesses run bounded files only; no new test runner or dependency increases CI blast radius. |
| T-180-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are allowed; if a dependency is proposed, stop for human verification before changing the supply chain. |
</threat_model>

<verification>
Run `mix test test/threadline/operator_surface/style_contract_test.exs test/threadline/operator_surface/component_contract_test.exs test/threadline/operator_surface/ui_test.exs` and `./examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-accessibility.spec.ts`.
</verification>

<rollback>
Revert only this plan's test and shared UI/style changes. If an APG assertion is found to describe behavior Threadline does not implement, roll back the assertion and replace it with a documented native/non-applicable check rather than adding misleading semantics.
</rollback>

<success_criteria>
A11Y-02 is enforced by existing ExUnit and browser harnesses, maps to actual Threadline components, keeps color from being the only signal, preserves dense operator workflows, and adds no dependencies.
</success_criteria>

<output>
Create `.planning/phases/180-accessibility-verification-guardrails-adversarial-closeout/180-02-SUMMARY.md` when done.
</output>
