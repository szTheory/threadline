---
phase: 98-mounted-evidence-views-on-audit
plan: 02
type: execute
wave: 2
depends_on:
  - "98-01"
files_modified:
  - lib/threadline/operator_surface/auth.ex
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/live/evidence_live.ex
  - lib/threadline/evidence/proof.ex
  - lib/mix/tasks/threadline.evidence.show.ex
  - test/threadline/operator_surface/auth_test.exs
  - test/threadline/operator_surface/live/evidence_live_test.exs
  - test/mix/tasks/threadline.evidence_show_test.exs
autonomous: true
requirements:
  - SURF-02
  - SURF-03
must_haves:
  truths:
    - "Mounted evidence stays behind an explicit host-owned capability gate instead of inheriting broad timeline access."
    - "The mounted page, proof module, and Mix task preserve the same verdict vocabulary and unsupported boundary language."
    - "Denied mounted access remains visible as an explicit unsupported state with CLI fallback rather than a hidden route."
    - "Phase 98 does not introduce Threadline-owned RBAC, tenant DSL semantics, or a second evidence reducer."
  artifacts:
    - path: lib/threadline/operator_surface/auth.ex
      provides: "Fail-closed evidence capability assignment from a host-owned callback"
      contains: "threadline_evidence_enabled"
    - path: test/threadline/operator_surface/auth_test.exs
      provides: "Executable proof that evidence gating defaults false and only enables for explicit host approval"
      contains: "evidence_authorize_fn"
    - path: test/threadline/operator_surface/live/evidence_live_test.exs
      provides: "Mounted parity proof for verdict labels, unsupported fallback, and denied access behavior"
      contains: "proven"
    - path: test/mix/tasks/threadline.evidence_show_test.exs
      provides: "CLI parity proof that mounted and CLI surfaces keep the same semantic labels"
      contains: "unsupported"
  key_links:
    - from: lib/threadline/operator_surface/auth.ex
      to: lib/threadline/operator_surface/router.ex
      via: "router option and mounted assign must use the same host-owned evidence capability seam"
      pattern: "evidence_authorize_fn|threadline_evidence_enabled"
    - from: lib/threadline/operator_surface/live/evidence_live.ex
      to: lib/threadline/evidence/proof.ex
      via: "mounted rows and unsupported language must remain semantically aligned with the proof contract"
      pattern: "proven|inferred_posture|unsupported"
    - from: lib/mix/tasks/threadline.evidence.show.ex
      to: lib/threadline/evidence/proof.ex
      via: "CLI output remains the no-Phoenix fallback and must preserve the same shared verdict mapping used by the mounted surface"
      pattern: "render_human|claim_assessment"
---

<objective>
Lock mounted/API/CLI parity and wire explicit host-owned authorization for the
evidence surface.

Purpose: satisfy the parity and gating half of Phase 98 so the mounted proof
surface is truthful, fail-closed, and semantically aligned with the existing
library and Mix-task paths.
Output: explicit evidence capability wiring in router/auth, shared verdict
semantics across mounted and CLI output, and tests proving denial behavior plus
verdict parity.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-CONTEXT.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-RESEARCH.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-PATTERNS.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-01-PLAN.md
@lib/threadline/operator_surface/auth.ex
@lib/threadline/operator_surface/router.ex
@lib/threadline/operator_surface/live/evidence_live.ex
@lib/threadline/evidence/proof.ex
@lib/mix/tasks/threadline.evidence.show.ex
@test/threadline/operator_surface/auth_test.exs
@test/threadline/operator_surface/live/evidence_live_test.exs
@test/mix/tasks/threadline.evidence_show_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a fail-closed host-owned evidence capability seam</name>
  <files>lib/threadline/operator_surface/auth.ex, lib/threadline/operator_surface/router.ex, test/threadline/operator_surface/auth_test.exs, test/threadline/operator_surface/live/evidence_live_test.exs</files>
  <read_first>
    <file>lib/threadline/operator_surface/auth.ex</file>
    <file>lib/threadline/operator_surface/router.ex</file>
    <file>test/threadline/operator_surface/auth_test.exs</file>
    <file>test/threadline/operator_surface/live/coverage_live_test.exs</file>
    <file>test/threadline/operator_surface/live/policy_redaction_live_test.exs</file>
    <file>test/threadline/operator_surface/live/evidence_live_test.exs</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-CONTEXT.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Test 1: evidence capability defaults to disabled when no host callback is provided.
    - Test 2: explicit host approval enables the mounted evidence surface.
    - Test 3: denial or callback failure renders the unsupported evidence state rather than timeline data or a hidden route.
  </behavior>
  <action>Add a dedicated host-owned evidence capability seam by extending `threadline_operator_surface/2` with an `:evidence_authorize_fn` option and extending `Threadline.OperatorSurface.Auth` with a fail-closed `assign_evidence_enabled/2` helper that mirrors the existing coverage/policy boolean rules: `:ok`, `true`, and `{:ok, _scope}` enable; anything else or any exception disables. Assign the result to `:threadline_evidence_enabled`, consume that assign in `EvidenceLive`, and extend `test/threadline/operator_surface/auth_test.exs` plus `test/threadline/operator_surface/live/evidence_live_test.exs` to prove default denial, explicit enablement, and unsupported fallback behavior. Do not collapse evidence into `policy_authorize_fn` silently and do not add any Threadline-owned role or tenant semantics.</action>
  <acceptance_criteria>
    <criterion>`lib/threadline/operator_surface/router.ex` documents or reads `evidence_authorize_fn`.</criterion>
    <criterion>`lib/threadline/operator_surface/auth.ex` contains `threadline_evidence_enabled` and a fail-closed evidence capability helper parallel to coverage/policy handling.</criterion>
    <criterion>`test/threadline/operator_surface/auth_test.exs` contains tests for `evidence_authorize_fn` default false, enable on `:ok`/`true`/`{:ok, _scope}`, and false on denial or exception.</criterion>
    <criterion>`test/threadline/operator_surface/live/evidence_live_test.exs` proves denied sessions render the unsupported evidence state instead of overview rows.</criterion>
    <criterion>`MIX_ENV=test mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1</automated>
  </verify>
  <done>The mounted evidence surface is explicitly host-gated and fails closed to an unsupported state on the current tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock mounted and CLI parity on shared verdict semantics and boundary language</name>
  <files>lib/threadline/operator_surface/live/evidence_live.ex, lib/threadline/evidence/proof.ex, lib/mix/tasks/threadline.evidence.show.ex, test/threadline/operator_surface/live/evidence_live_test.exs, test/mix/tasks/threadline.evidence_show_test.exs</files>
  <read_first>
    <file>lib/threadline/operator_surface/live/evidence_live.ex</file>
    <file>lib/threadline/evidence/proof.ex</file>
    <file>lib/mix/tasks/threadline.evidence.show.ex</file>
    <file>test/threadline/operator_surface/live/evidence_live_test.exs</file>
    <file>test/mix/tasks/threadline.evidence_show_test.exs</file>
    <file>guides/domain-reference.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-CONTEXT.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md</file>
  </read_first>
  <action>Refine the mounted evidence rendering so it consumes one shared verdict/presentation seam rooted in `Threadline.Evidence.Proof` instead of re-deriving `proven`, `inferred_posture`, or `unsupported` locally. If a small presenter helper is needed, add it adjacent to `Proof` or inside `Proof`-owned helpers and update `Mix.Tasks.Threadline.Evidence.Show.render_human` plus `EvidenceLive` to use the same label/status mapping. Extend mounted and Mix-task tests to assert the same semantic labels, unsupported behavior, and explicit latest/history boundary copy. Keep machine-contract fields such as `format_version` and `proof_type` out of primary UI chrome.</action>
  <acceptance_criteria>
    <criterion>`lib/threadline/operator_surface/live/evidence_live.ex` and `lib/mix/tasks/threadline.evidence.show.ex` both rely on `Threadline.Evidence.Proof` or a proof-adjacent shared helper for verdict semantics.</criterion>
    <criterion>No file added by this task introduces a second verdict vocabulary beyond `proven`, `inferred_posture`, and `unsupported`.</criterion>
    <criterion>`test/threadline/operator_surface/live/evidence_live_test.exs` asserts the exact rendered labels `proven`, `inferred_posture`, and `unsupported`.</criterion>
    <criterion>`test/mix/tasks/threadline.evidence_show_test.exs` asserts the CLI fallback/output path preserves the same semantic labels and unsupported handling.</criterion>
    <criterion>`MIX_ENV=test mix test test/threadline/operator_surface/live/evidence_live_test.exs test/mix/tasks/threadline.evidence_show_test.exs --max-failures 1` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/threadline/operator_surface/live/evidence_live_test.exs test/mix/tasks/threadline.evidence_show_test.exs --max-failures 1</automated>
  </verify>
  <done>The mounted and CLI evidence surfaces now share the same verdict semantics and unsupported boundary language on the current tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host callback -> mounted capability boolean | Evidence visibility meaning remains host-owned; Threadline only consumes allow/deny/scope results. |
| Proof semantics -> mounted / CLI presentation | Semantic drift here would create conflicting claims across transports. |
| Denied support lane -> mounted fallback copy | The system must never silently escalate evidence visibility by omission or broad timeline inheritance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | Elevation of privilege | `Threadline.OperatorSurface.Auth` evidence capability | mitigate | Default evidence gating to false, require explicit host callback approval, and test denial/exception paths. |
| T-98-05 | Information disclosure | `EvidenceLive` + route wiring | mitigate | Use the explicit evidence capability assign instead of broad timeline access and render unsupported fallback when denied. |
| T-98-06 | Repudiation | proof semantic parity across transports | mitigate | Centralize verdict mapping in `Threadline.Evidence.Proof` or a proof-adjacent helper and assert parity in mounted + CLI tests. |
</threat_model>

<verification>
Follow `98-VALIDATION.md`: first the auth plus mounted-denial slice, then the
mounted/CLI parity slice, then `mix verify.test`. The outcome must prove both
host-owned gating and one shared verdict vocabulary.
</verification>

<success_criteria>
The evidence page is explicitly host-gated, fail-closed, and semantically
aligned with the existing evidence API and Mix-task proof path.
</success_criteria>

<output>
After completion, create `.planning/phases/98-mounted-evidence-views-on-audit/98-02-SUMMARY.md`
</output>
