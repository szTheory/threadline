---
phase: 101-phase-96-verification-backfill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md
  - .planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "Phase 96 closes against the current tree: `Threadline.Evidence` exposes exactly six subject-focused public `record_*` helpers, append-only list-vs-singular read helpers with explicit filter allow-lists, mechanical-only defaults with caller-explicit semantic fields, and a Phoenix-optional install boundary with no ambient state."
    - "The authoritative Phase 96 proof bundle is the focused band `mix test test/threadline/evidence_test.exs test/threadline/governance/evidence_record_test.exs --max-failures 1` plus the tightened D-09 structural grep and the anchored D-12 closed-set grep — not `mix verify.test`, which carries Phase 99-owned alias-drift outside Phase 96 ownership."
    - "Phase 101 makes only the smallest literal-truth repair allowed by D-15 if a mismatch is found; it does not redesign the evidence API, add helpers, expand the closed subject inventory, or add new tests."
  artifacts:
    - path: .planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md
      provides: "Current-tree verification artifact that closes the missing Phase 96 verification chain with 4 numbered bands (per D-03) and a single-row `PROOF-01` closure (per D-14)"
      contains: "PROOF-01"
    - path: lib/threadline/evidence.ex
      provides: "The public `Threadline.Evidence` context whose write/read/defaults/Phoenix-optional behavior the verification artifact cites"
      contains: "def record_redaction_policy"
    - path: lib/threadline/evidence/subject.ex
      provides: "The closed supported-subject inventory the Write-contract band asserts as one-to-one with the six public `record_*` helpers"
      contains: "@supported_subjects"
    - path: lib/threadline/governance/evidence_record.ex
      provides: "The append-only schema whose `@required_fields` lock enforces the Defaults-band claim that semantic fields stay caller-explicit"
      contains: "@required_fields"
  key_links:
    - from: lib/threadline/evidence.ex
      to: lib/threadline/evidence/subject.ex
      via: "the six public `record_*` helpers funnel through private `record_subject/4` which calls `Subject.validate/1`; the public helper names match the `@supported_subjects` strings one-to-one"
      pattern: "record_redaction_policy|record_trigger_coverage|record_retention_run|record_retention_policy|record_export_delivery|record_support_scope_posture"
    - from: lib/threadline/evidence.ex
      to: test/threadline/evidence_test.exs
      via: "the targeted public-context tests prove the Write-contract closed family, the Read-contract list-vs-singular shape discipline, the Defaults+provenance contract, and the explicit-`repo:` arity claim"
      pattern: "exposes the supported write helper family|requires explicit repo handling|lists append-only history|returns explicit latest projections|persists normalized defaults and stable provenance"
    - from: lib/threadline/governance/evidence_record.ex
      to: test/threadline/governance/evidence_record_test.exs
      via: "the schema-required fields and repeated-insert posture must be proven by the focused-bundle test surface"
      pattern: "validate_required|append-only|two inserts for the same logical subject"
    - from: .planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md
      to: lib/threadline/evidence.ex
      via: "each of the four numbered bands cites a specific line or module in `Threadline.Evidence` as evidence, with the focused rerun bundle and the two structural greps as the executable proofs"
      pattern: "PROOF-01|Result: PASS|Current-tree preflight|Not closed here"
---

<objective>
Re-verify the Phase 96 evidence persistence and public API contract on the
current tree and create the missing `96-VERIFICATION.md` artifact that closes
`PROOF-01` honestly with four numbered truth bands (Write contract / Read
contract / Defaults+provenance / Phoenix-optional+no-ambient-state). Apply the
smallest literal-truth repair to `96-VALIDATION.md`'s authority band per D-06
and D-15 if the focused bundle is still labeled with `mix verify.test`.

Purpose: replace summary-only Phase 96 closure claims with current-tree proof
backed by the focused rerun bundle (D-05), the tightened D-09 structural grep,
and the anchored D-12 closed-set grep.

Output: a finalized `96-VERIFICATION.md` plus only the smallest literal-truth
repair to `96-VALIDATION.md`'s authority band (per D-06). No code or test edits
are expected; if the focused rerun reveals an unrelated regression, the planner
escalates rather than expanding scope (per D-15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.22-MILESTONE-AUDIT.md
@.planning/phases/101-phase-96-verification-backfill/101-CONTEXT.md
@.planning/phases/101-phase-96-verification-backfill/101-RESEARCH.md
@.planning/phases/101-phase-96-verification-backfill/101-PATTERNS.md
@.planning/phases/101-phase-96-verification-backfill/101-VALIDATION.md
@.planning/phases/96-evidence-persistence-and-public-api/96-CONTEXT.md
@.planning/phases/96-evidence-persistence-and-public-api/96-RESEARCH.md
@.planning/phases/96-evidence-persistence-and-public-api/96-01-SUMMARY.md
@.planning/phases/96-evidence-persistence-and-public-api/96-02-SUMMARY.md
@.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md
@.planning/phases/95-evidence-model-lock-and-scope-guard/95-VERIFICATION.md
@.planning/phases/95-evidence-model-lock-and-scope-guard/95-VALIDATION.md
@.planning/phases/99-contract-lock-docs-and-final-verification/99-VERIFICATION.md
@.planning/phases/89-contract-lock-final-verification/89-VALIDATION.md
@lib/threadline/evidence.ex
@lib/threadline/evidence/subject.ex
@lib/threadline/governance/evidence_record.ex
@test/threadline/evidence_test.exs
@test/threadline/governance/evidence_record_test.exs
@CLAUDE.md

<interfaces>
From `lib/threadline/evidence.ex` (the six public `record_*` helpers — D-11 closed inventory):
```elixir
def record_redaction_policy(subject_ref, attrs, opts \\ [])
def record_trigger_coverage(subject_ref, attrs, opts \\ [])
def record_retention_run(subject_ref, attrs, opts \\ [])
def record_retention_policy(subject_ref, attrs, opts \\ [])
def record_export_delivery(subject_ref, attrs, opts \\ [])
def record_support_scope_posture(subject_ref, attrs, opts \\ [])
```

From `lib/threadline/evidence.ex` (the read-contract list-vs-singular helpers — D-13):
```elixir
def list_history(filters, opts \\ [])
def list_subject_ref_history(subject, subject_ref, filters, opts)
def list_latest_subject_refs(subject, filters, opts)
def list_overview(filters, opts \\ [])
def get_latest_subject_ref(subject, subject_ref, opts)
```

From `lib/threadline/evidence/subject.ex`:
```elixir
def supported_subjects
```

From `lib/threadline/governance/evidence_record.ex`:
```elixir
schema "threadline_evidence_records"
```

From `.planning/REQUIREMENTS.md` line 57:
```text
| PROOF-01 | Phase 101 | Pending |
```

From `.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md` line 4 (the drift site for D-06 repair):
```text
status: planned
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the focused Phase 96 rerun bundle and the two structural greps on the current tree</name>
  <files>lib/threadline/evidence.ex, lib/threadline/evidence/subject.ex, lib/threadline/governance/evidence_record.ex, test/threadline/evidence_test.exs, test/threadline/governance/evidence_record_test.exs</files>
  <read_first>
    <file>lib/threadline/evidence.ex</file>
    <file>lib/threadline/evidence/subject.ex</file>
    <file>lib/threadline/governance/evidence_record.ex</file>
    <file>test/threadline/evidence_test.exs</file>
    <file>test/threadline/governance/evidence_record_test.exs</file>
    <file>.planning/phases/101-phase-96-verification-backfill/101-CONTEXT.md</file>
    <file>.planning/phases/101-phase-96-verification-backfill/101-RESEARCH.md</file>
  </read_first>
  <action>Use the current working tree (HEAD) as authority. Per D-05 run the focused rerun bundle `mix test test/threadline/evidence_test.exs test/threadline/governance/evidence_record_test.exs --max-failures 1` and record the literal ExUnit count line (expected `12 tests, 0 failures` per 101-RESEARCH.md focused-rerun-bundle confirmation; record what is actually printed, not what research predicted — Pitfall 4). Then run the two structural proofs from D-09 and D-12: (a) the tightened negative-assertion grep `rg -n '^\s*(import|alias|require|use)\s+(Plug|Phoenix)\.|Process\.(put|get)\(|Logger\.metadata\(|:ets\.' lib/threadline/evidence.ex` — expected EMPTY (exit 1); the naive pattern `'Plug\.|Phoenix|Process\.put|Process\.get|Logger\.metadata|:ets'` is forbidden because it matches the moduledoc at `lib/threadline/evidence.ex:5` (D-09, Pitfall 1); (b) the anchored closed-set grep `rg -n '^\s*def record_' lib/threadline/evidence.ex` — expected exactly six matches naming `record_redaction_policy`, `record_trigger_coverage`, `record_retention_run`, `record_retention_policy`, `record_export_delivery`, `record_support_scope_posture` (D-11, D-12, Pitfall 3 — leading `def ` excludes the private `defp record_subject/4` at line 161). Capture exact stdout for all three commands; these become the Evidence blocks in Task 2's `96-VERIFICATION.md`. Per D-15, do NOT modify any code or test file; if the focused bundle fails for an unrelated reason, surface the failure as a blocking finding (do not add tests, rename helpers, expand the subject inventory, or add new behavior).</action>
  <acceptance_criteria>
    <criterion>`mix test test/threadline/evidence_test.exs test/threadline/governance/evidence_record_test.exs --max-failures 1` exits 0 and prints a `N tests, 0 failures` line (literal count captured for Task 2).</criterion>
    <criterion>`rg -n '^\s*(import|alias|require|use)\s+(Plug|Phoenix)\.|Process\.(put|get)\(|Logger\.metadata\(|:ets\.' lib/threadline/evidence.ex` returns no matches (exit code 1).</criterion>
    <criterion>`rg -n '^\s*def record_' lib/threadline/evidence.ex` returns exactly six matches, one each for `record_redaction_policy`, `record_trigger_coverage`, `record_retention_run`, `record_retention_policy`, `record_export_delivery`, `record_support_scope_posture`.</criterion>
    <criterion>No code or test file in `lib/threadline/evidence*`, `lib/threadline/governance/evidence_record.ex`, or the two test files has been modified (per D-15 literal-truth-repair scope — repairs are paperwork-only in this phase).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/evidence_test.exs test/threadline/governance/evidence_record_test.exs --max-failures 1 && rg -n '^\s*def record_' lib/threadline/evidence.ex | wc -l | grep -q '^[[:space:]]*6$' && ! rg -n '^\s*(import|alias|require|use)\s+(Plug|Phoenix)\.|Process\.(put|get)\(|Logger\.metadata\(|:ets\.' lib/threadline/evidence.ex</automated>
  </verify>
  <done>Focused rerun bundle passes on the current tree, tightened D-09 negative-assertion grep returns empty, anchored D-12 closed-set grep returns exactly six matches, and the literal stdout for all three is captured for Task 2 to cite in `96-VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Write 96-VERIFICATION.md and repair 96-VALIDATION.md authority band</name>
  <files>.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md, .planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/95-evidence-model-lock-and-scope-guard/95-VERIFICATION.md</file>
    <file>.planning/phases/99-contract-lock-docs-and-final-verification/99-VERIFICATION.md</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-CONTEXT.md</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-01-SUMMARY.md</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-02-SUMMARY.md</file>
    <file>.planning/phases/101-phase-96-verification-backfill/101-CONTEXT.md</file>
  </read_first>
  <action>Create `.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md` (NEW) using `95-VERIFICATION.md` lines 1-94 as the verbatim structural template (per 101-PATTERNS.md `Pattern Assignments` table). Frontmatter (per D-01): `phase: 96-evidence-persistence-and-public-api`, `verified: <ISO-8601 UTC timestamp from Task 1>`, `status: passed`, `score: 4/4 requirement bands verified`, `overrides_applied: 0`. Body sections in order: (1) header `# Phase 96: Evidence Persistence And Public API Verification Report` with `**Phase Goal:**` / `**Verified:**` / `**Status:** passed` / `**Re-verification:** Yes - gap closure for missing phase verification`; (2) `## Current-tree preflight` section per D-02 with `**Result:** PASS` and three bullets naming the missing-artifact gap, the working-tree-as-authority claim, and the Phase 103 authority-surface disclaimer; (3) four numbered bands per D-03 in order Write -> Read -> Defaults -> Phoenix-optional, each with the candidate titles from 101-RESEARCH.md lines 180-184: `## 1. Threadline.Evidence public context and subject-focused write helpers`, `## 2. Append-only read contract — history, latest, and list_latest semantics`, `## 3. Mechanical-only defaults and explicit provenance contract`, `## 4. Phoenix-optional install and no-ambient-state boundary`. Each band must have `**Requirement:** ` + two trailing spaces + newline + `PROOF-01` line, a `**Result:** PASS` line, three to four bullets citing specific line numbers in `lib/threadline/evidence.ex` / `lib/threadline/evidence/subject.ex` / `lib/threadline/governance/evidence_record.ex`, and one or more `### Evidence` sub-sections with a fenced bash block plus a `Result: PASS (...)` line. Band 1 cites `mix test ...` AND `rg -n '^\s*def record_' lib/threadline/evidence.ex` (D-11, D-12). Band 4 cites the tightened D-09 negative grep with a short prose note explaining why the naive pattern is rejected (matches moduledoc at `lib/threadline/evidence.ex:5`) and closes with an `### Authority statement` sub-section listing the three authoritative commands and naming Phase 99 commit `b636c17` as the owner of `mix verify.test` alias topology (per D-07). Then `## Requirement closure` with a single `PROOF-01` row per D-14 using the prose pattern from 101-CONTEXT.md `<specifics>`: "Threadline still exposes a Phoenix-optional `Threadline.Evidence` public context with subject-focused create helpers for the closed Phase 95 subject set, append-only history reads, and list-vs-singular latest projections, with all defaults remaining mechanical and no ambient runtime state in the read or write path." Finally `## Not closed here` per D-18 with three bullets naming `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` as intentionally unreconciled, plus the closing sentence "Phase 101 closes the missing Phase 96 verification and validation chain only; milestone authority-surface reconciliation remains Phase 103 work." All literal test counts and grep outputs MUST come from Task 1's captured stdout — do NOT copy `12 tests, 0 failures` from research notes (Pitfall 4). Separately, apply the smallest literal-truth repair to `.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md` per D-06 and D-15: swap every occurrence of `mix verify.test` in the Test Infrastructure table, the Sampling Rate second bullet, and the Per-Task Verification Map Automated Command cells (lines 21, 30, 37, 39, 47-48 per 101-RESEARCH.md) over to `mix test test/threadline/evidence_test.exs test/threadline/governance/evidence_record_test.exs --max-failures 1`. Do NOT change the frontmatter `status`, the `## Commands Actually Used` section, or the `## Phase Boundary Guard` section in this task — those are Plan 101-02's scope per D-16.</action>
  <acceptance_criteria>
    <criterion>`.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md` exists and contains the literal strings `PROOF-01`, `Current-tree preflight`, `Not closed here`, `Result: PASS`, `## 1.`, `## 2.`, `## 3.`, `## 4.`, `Requirement closure`, and `Phase 101 closes the missing Phase 96 verification and validation chain only`.</criterion>
    <criterion>`96-VERIFICATION.md` frontmatter contains `phase: 96-evidence-persistence-and-public-api`, `status: passed`, `score: 4/4 requirement bands verified`, and `overrides_applied: 0` (per D-01).</criterion>
    <criterion>`96-VERIFICATION.md` cites the focused-bundle command `mix test test/threadline/evidence_test.exs test/threadline/governance/evidence_record_test.exs --max-failures 1` at least once and does NOT cite `mix verify.test` in any band's Evidence block (per D-05, D-06; Pitfall 2).</criterion>
    <criterion>`96-VERIFICATION.md` band 1 cites the closed six-function set by name: `record_redaction_policy`, `record_trigger_coverage`, `record_retention_run`, `record_retention_policy`, `record_export_delivery`, `record_support_scope_posture` (per D-11).</criterion>
    <criterion>`96-VERIFICATION.md` band 4 uses the tightened D-09 grep pattern `^\s*(import|alias|require|use)\s+(Plug|Phoenix)\.|Process\.(put|get)\(|Logger\.metadata\(|:ets\.` and does NOT cite the naive `'Plug\.|Phoenix|Process\.put|Process\.get|Logger\.metadata|:ets'` pattern in its Evidence block (per D-09, Pitfall 1).</criterion>
    <criterion>`96-VERIFICATION.md` band 4 closes with an Authority statement that names commit `b636c17` as the most recent Phase 99 alias-topology fix (per D-07).</criterion>
    <criterion>`96-VERIFICATION.md` `## Requirement closure` table contains exactly one data row covering `PROOF-01` (per D-14).</criterion>
    <criterion>`.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md` no longer contains `mix verify.test` in the Test Infrastructure Quick-run row, the Sampling Rate bullets, or the Per-Task Verification Map Automated Command cells (per D-06 literal-truth repair).</criterion>
    <criterion>`96-VALIDATION.md` frontmatter `status:` still reads `planned` at this point (101-02 flips it to `validated` per D-16); `nyquist_compliant: true` and `wave_0_complete: true` remain unchanged.</criterion>
    <criterion>No file under `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, or `.planning/STATE.md` has been modified (per D-17).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md && rg -n 'PROOF-01|Current-tree preflight|Not closed here|Result: PASS|## 1\.|## 2\.|## 3\.|## 4\.|Requirement closure|Authority statement|Phase 101 closes the missing Phase 96 verification and validation chain only|record_redaction_policy|record_trigger_coverage|record_retention_run|record_retention_policy|record_export_delivery|record_support_scope_posture|b636c17|score: 4/4 requirement bands verified' .planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md && ! rg -n 'mix verify\.test' .planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md && ! rg -n "'Plug\\\\\\.\\|Phoenix\\|Process\\\\\\.put\\|Process\\\\\\.get\\|Logger\\\\\\.metadata\\|:ets'" .planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md && ! rg -n '^\s*\| `mix verify\.test`' .planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md</automated>
  </verify>
  <done>`96-VERIFICATION.md` exists with the four-band structure per D-03, single-row `PROOF-01` closure per D-14, "Not closed here" boilerplate per D-18, and Authority statement naming commit `b636c17` per D-07. `96-VALIDATION.md` has had its authority band swapped from `mix verify.test` to the focused bundle per D-06 with no other changes (status flip and new sections are 101-02 scope per D-16). No milestone-authority surface has been touched per D-17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 96 summary artifacts -> current implementation tree | Phase 101 must verify what is on disk now, not inherit Phase 96 summary claims automatically (the summaries are non-authoritative per the same posture Phase 100 used for Phase 95). |
| `Threadline.Evidence` public-helper closed inventory -> public API surface | Any expansion or contraction of the six-function set without matching `Subject.@supported_subjects` would silently widen the milestone boundary. |
| `Threadline.Evidence` module body -> Phoenix-optional install claim | If any future PR imports Plug/Phoenix or calls `Process.put`/`Logger.metadata`/`:ets` inside `lib/threadline/evidence.ex`, the no-ambient-state band silently breaks. |
| `mix verify.test` alias topology (Phase 99-owned) -> Phase 96 verification scope | Pulling carry-forward alias-drift into Phase 96's authority statement would reopen Phase 99 territory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Tampering | `Threadline.Evidence` public write-helper closed inventory | mitigate | Band 1 of `96-VERIFICATION.md` cites the anchored D-12 grep `^\s*def record_` returning exactly six matches and asserts each helper name by literal string (D-11). Future expansion would fail this band. |
| T-101-02 | Repudiation | Provenance default contract in `record_subject/4` | mitigate | Band 3 cites the `"writer" => "threadline"` and `"entrypoint" => "record_<subject>"` envelope built by `provenance/2` (lines 186-192) and the test at `test/threadline/evidence_test.exs:57` (`"persists normalized defaults and stable provenance"`); caller extras merge but never override library-owned keys. |
| T-101-03 | Elevation of scope | Closed subject inventory in `lib/threadline/evidence/subject.ex` | mitigate | Band 1 cites the `@supported_subjects` constant (lines 10-17) and `Subject.validate/1` (line 36) as the boundary validator. |
| T-101-04 | Tampering | Read-shape discipline (list vs singular) | mitigate | Band 2 cites the explicit shape table for `list_history`/`list_subject_ref_history`/`list_latest_subject_refs`/`list_overview` (always list) vs `get_latest_subject_ref` (always one record or `nil`) and the filter allow-lists (lines 15-17) that raise `ArgumentError` on unknown keys. |
| T-101-05 | Repudiation | No-ambient-state proof method (structural grep + arity citation) | mitigate | Band 4 cites the tightened D-09 negative-assertion grep returning EMPTY against `lib/threadline/evidence.ex` and the arity table showing every public entrypoint enforces explicit `:repo` (lines 269-282); the naive grep that matches the moduledoc is explicitly rejected per Pitfall 1. |
</threat_model>

<verification>
Run the focused Phase 96 contract bundle and the two structural greps from
Task 1, then write `96-VERIFICATION.md` as the authority for `PROOF-01` and
apply the D-06 authority-band repair to `96-VALIDATION.md`. The verification
artifact must cite only the current-tree truths the rerun bundle and the
structural greps actually proved.
</verification>

<success_criteria>
Phase 96 has a truthful current-tree verification artifact (`96-VERIFICATION.md`)
that closes `PROOF-01` against four explicit truth bands with executable
evidence, and `96-VALIDATION.md`'s authority band has been swapped from
`mix verify.test` to the focused bundle per D-06. No milestone authority
surface has been touched (per D-17); the status/Commands-Actually-Used/Phase
Boundary Guard finalization remains Plan 101-02's scope per D-16.
</success_criteria>

<output>
After completion, create `.planning/phases/101-phase-96-verification-backfill/101-01-SUMMARY.md`
</output>
</content>
</invoke>