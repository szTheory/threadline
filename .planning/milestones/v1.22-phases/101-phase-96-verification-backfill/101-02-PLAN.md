---
phase: 101-phase-96-verification-backfill
plan: "02"
type: execute
wave: 2
depends_on:
  - "101-01"
files_modified:
  - .planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "Phase 96 has a final Nyquist-compliant validation artifact (`96-VALIDATION.md`) synchronized to the actual focused-bundle commands recorded in `96-VERIFICATION.md` (D-05, D-06, D-16)."
    - "The Phase 101 closure chain remains narrow: it finalizes Phase 96 verification evidence and Nyquist sign-off only; it does not reconcile `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, or `.planning/STATE.md` (D-17)."
    - "The validation artifact records exactly the three commands used to close `PROOF-01` — the focused-bundle `mix test ...`, the tightened D-09 negative-assertion grep, and the anchored D-12 closed-set grep — and `mix verify.test` does not appear as an authority anywhere in the artifact (D-06, D-07)."
  artifacts:
    - path: .planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md
      provides: "Final Nyquist validation contract and execution evidence for the Phase 96 closure with the new `## Commands Actually Used` and `## Phase Boundary Guard` sections per D-16"
      contains: "nyquist_compliant: true"
  key_links:
    - from: .planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md
      to: .planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md
      via: "the validation artifact must record the same focused-bundle command, the same tightened D-09 grep, and the same anchored D-12 closed-set grep that the verification artifact's Evidence blocks claim"
      pattern: "PROOF-01|evidence_test\\.exs|evidence_record_test\\.exs|## Commands Actually Used|^\\\\s\\*\\(import\\|alias\\|require\\|use\\)\\\\s\\+\\(Plug\\|Phoenix\\)\\\\\\.|^\\\\s\\*def record_"
---

<objective>
Finalize the Phase 96 validation chain by converting `96-VALIDATION.md` from
its current draft state (`status: planned`, post-101-01 authority-band repair
but no executed-evidence sections) into a Nyquist-compliant closure artifact
synchronized to the focused-bundle commands and structural greps `96-VERIFICATION.md`
actually ran in 101-01.

Purpose: turn the current-tree Phase 96 verdict into durable validation
evidence with a `## Commands Actually Used` section and a `## Phase Boundary
Guard` section that explicitly disclaim milestone authority-surface
reconciliation (D-16, D-17).

Output: a finalized `96-VALIDATION.md` synchronized to `96-VERIFICATION.md`
with `status: validated`, per-task verification map statuses flipped from
`planned` to `✅ green`, the new `## Commands Actually Used` and `## Phase
Boundary Guard` sections, and the updated `**Approval:**` line. The frontmatter
fields `nyquist_compliant: true` and `wave_0_complete: true` remain unchanged
because they were already true at planning time per D-16.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md
@.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md
@.planning/phases/95-evidence-model-lock-and-scope-guard/95-VALIDATION.md
@.planning/phases/89-contract-lock-final-verification/89-VALIDATION.md
@.planning/phases/99-contract-lock-docs-and-final-verification/99-VERIFICATION.md
@.planning/phases/101-phase-96-verification-backfill/101-CONTEXT.md
@.planning/phases/101-phase-96-verification-backfill/101-RESEARCH.md
@.planning/phases/101-phase-96-verification-backfill/101-PATTERNS.md
@.planning/phases/101-phase-96-verification-backfill/101-01-SUMMARY.md

<interfaces>
From `.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md` (current draft state at 101-02 start — D-16 transition points):
```text
status: planned
updated: 2026-05-25T19:50:00Z
```

From `.planning/phases/95-evidence-model-lock-and-scope-guard/95-VALIDATION.md` (the verbatim modern Nyquist template — 101-PATTERNS.md Pattern Assignments table):
```text
status: validated
nyquist_compliant: true
wave_0_complete: true
## Commands Actually Used
## Phase Boundary Guard
**Approval:** finalized
```

From `.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md` (the executed-evidence source that 101-02 mirrors):
```text
PROOF-01
Result: PASS
Current-tree preflight
Not closed here
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize 96-VALIDATION.md as a Nyquist closure artifact synchronized to 96-VERIFICATION.md</name>
  <files>.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md</file>
    <file>.planning/phases/95-evidence-model-lock-and-scope-guard/95-VALIDATION.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-VALIDATION.md</file>
    <file>.planning/phases/101-phase-96-verification-backfill/101-01-SUMMARY.md</file>
    <file>.planning/phases/101-phase-96-verification-backfill/101-CONTEXT.md</file>
  </read_first>
  <action>Update `.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md` in place to the modern Nyquist format using `95-VALIDATION.md` lines 1-102 as the verbatim structural template (per 101-PATTERNS.md `Pattern Assignments` for `96-VALIDATION.md` MODIFY rows). Apply the seven concrete edits enumerated in 101-RESEARCH.md `Smallest Literal-Truth Repair Candidates` (lines 511-526) that 101-01 did not complete: (1) Frontmatter `status: planned` -> `status: validated` (D-16); keep `phase: 96` unchanged per 101-RESEARCH.md Open Question 1 recommendation; keep `nyquist_compliant: true` and `wave_0_complete: true` unchanged (already true per D-16). (2) Frontmatter `updated:` bump to 101-01's actual execution timestamp from `96-VERIFICATION.md` `verified:` field (mirror `95-VALIDATION.md` line 8 ISO-8601 UTC discipline per 101-PATTERNS.md `Frontmatter date discipline`). (3) Flip the `## Per-Task Verification Map` Status column from `planned` to `✅ green` for all rows (lines 37-40 in current draft) using the legend `*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*` immediately under the table per 101-PATTERNS.md `Key shape constraints`. (4) Promote the Per-Task Verification Map from its current 9-column shape to the modern 10-column shape by adding a `File Exists` column (per 101-RESEARCH.md A3 and 101-PATTERNS.md supplementary `89-VALIDATION.md` reference); apply `✅` to rows whose artifact existed before 101-01, `❌ W0 (created in 101-01)` to the `96-VERIFICATION.md` row. (5) Add a new `## Commands Actually Used` section immediately after `## Per-Task Verification Map` using the verbatim template at 101-RESEARCH.md lines 210-223: numbered list `1.` / `2.` / `3.` with each command on its first line and `Result: PASS (...)` indented three spaces on the second line; cite (a) `mix test test/threadline/evidence_test.exs test/threadline/governance/evidence_record_test.exs --max-failures 1` with the literal `N tests, 0 failures` count from 101-01, (b) `rg -n '^\s*(import|alias|require|use)\s+(Plug|Phoenix)\.|Process\.(put|get)\(|Logger\.metadata\(|:ets\.' lib/threadline/evidence.ex` with `Result: PASS (no matches — negative assertion)`, (c) `rg -n '^\s*def record_' lib/threadline/evidence.ex` with `Result: PASS (exactly six matches — record_redaction_policy, record_trigger_coverage, record_retention_run, record_retention_policy, record_export_delivery, record_support_scope_posture)`. (6) Add a new `## Phase Boundary Guard` section after `## Manual-Only Verifications` mirroring `95-VALIDATION.md` lines 83-89 verbatim with five bullets: bullet 1 scopes the artifact to `PROOF-01` only; bullets 2-4 name `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` as intentionally unreconciled in this phase (D-17); bullet 5 names "Phase 97, Phase 98, and milestone closeout" as out of scope (per 101-PATTERNS.md Forbidden patterns — do NOT claim Phase 102 or Phase 103 progress here). (7) Rewrite the closing `**Approval:**` line from `planned on 2026-05-25 for Phase 96 execution.` to `finalized on <101-01 execution date> after Phase 101-01 produced 96-VERIFICATION.md and the focused-bundle rerun passed.` mirroring `95-VALIDATION.md` line 101. Preserve all existing sections that 101-01 already reconciled: `## Test Infrastructure` Quick-run row, `## Sampling Rate` bullets, and Per-Task Verification Map Automated Command cells must already cite the focused bundle (not `mix verify.test`) because 101-01 swapped them per D-06. Per 101-PATTERNS.md Forbidden patterns: do NOT include an `## Authoritative-Surface Drift` section (89-VALIDATION.md-specific). Do NOT modify `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, or `.planning/STATE.md` (D-17). Do NOT re-edit `96-VERIFICATION.md` (read-only at this point — it is the truth source 101-02 mirrors).</action>
  <acceptance_criteria>
    <criterion>`.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md` frontmatter contains `phase: 96`, `status: validated`, `nyquist_compliant: true`, and `wave_0_complete: true` (D-16).</criterion>
    <criterion>`96-VALIDATION.md` contains a `## Commands Actually Used` section that cites `test/threadline/evidence_test.exs`, `test/threadline/governance/evidence_record_test.exs`, the tightened grep pattern `^\s*(import|alias|require|use)\s+(Plug|Phoenix)\.`, and the anchored closed-set grep `^\s*def record_` (D-09, D-12, D-16).</criterion>
    <criterion>`96-VALIDATION.md` `## Commands Actually Used` cites all six `record_*` helper names by literal string: `record_redaction_policy`, `record_trigger_coverage`, `record_retention_run`, `record_retention_policy`, `record_export_delivery`, `record_support_scope_posture` (D-11).</criterion>
    <criterion>`96-VALIDATION.md` contains a `## Phase Boundary Guard` section naming `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` as intentionally unreconciled in this phase (D-17).</criterion>
    <criterion>`96-VALIDATION.md` `## Per-Task Verification Map` table shows `✅ green` (not `planned`) in the Status column for every executed row, and includes a `File Exists` column (10-column modern shape).</criterion>
    <criterion>`96-VALIDATION.md` does NOT contain `mix verify.test` anywhere (D-05, D-06, D-07).</criterion>
    <criterion>`96-VALIDATION.md` does NOT contain an `## Authoritative-Surface Drift` section (per 101-PATTERNS.md Forbidden patterns — that section is 89-VALIDATION.md-specific).</criterion>
    <criterion>`96-VALIDATION.md` closing `**Approval:**` line contains the word `finalized` (replacing the prior `planned on 2026-05-25 for Phase 96 execution.` text per 101-RESEARCH.md drift table).</criterion>
    <criterion>`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` are unchanged at task end (D-17 — milestone authority surfaces remain Phase 103 scope).</criterion>
    <criterion>`.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md` is unchanged at task end (101-02 mirrors 96-VERIFICATION.md but does not edit it).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n '^phase: 96|^status: validated|^nyquist_compliant: true|^wave_0_complete: true|PROOF-01|## Commands Actually Used|## Phase Boundary Guard|evidence_test\.exs|evidence_record_test\.exs|def record_|record_redaction_policy|record_trigger_coverage|record_retention_run|record_retention_policy|record_export_delivery|record_support_scope_posture|✅ green|finalized' .planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md && ! rg -n 'mix verify\.test' .planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md && ! rg -n '## Authoritative-Surface Drift' .planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md</automated>
  </verify>
  <done>Phase 96 has a final Nyquist artifact tied to the focused-bundle proof commands actually used for closure: `status: validated`, `## Commands Actually Used` and `## Phase Boundary Guard` sections present, Per-Task Verification Map statuses flipped to `✅ green` with the 10-column modern shape, closing `**Approval:**` line rewritten to the executed-finalization pattern. `96-VERIFICATION.md` is unchanged (read-only). `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` are unchanged (D-17 — Phase 103 scope).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `96-VERIFICATION.md` -> `96-VALIDATION.md` | If the Nyquist artifact cites different commands or different requirement closures than `96-VERIFICATION.md`, the closure chain becomes unreliable. |
| Phase closure evidence -> milestone authority surfaces | If `96-VALIDATION.md` starts claiming `.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` / `.planning/STATE.md` reconciliation, Phase 101 will overstep its intended boundary (D-17). |
| `mix verify.test` carry-forward alias-drift -> Phase 96 authority | If any section of the final `96-VALIDATION.md` reverts to citing `mix verify.test`, Phase 99's alias-topology scope leaks back into Phase 96 closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-06 | Repudiation | validation evidence in `96-VALIDATION.md` | mitigate | Record the exact three proof commands from `96-VERIFICATION.md` in `## Commands Actually Used` and keep the requirement map synchronized to them (D-16). |
| T-101-07 | Tampering | phase boundary between Phase 101 and Phase 103 | mitigate | Add `## Phase Boundary Guard` listing `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` as intentionally unreconciled here (D-17, D-18); the bullets are non-negotiable per 101-PATTERNS.md `Phase-boundary discipline`. |
</threat_model>

<verification>
Verify the finalized `96-VALIDATION.md` with literal grep checks against the
frontmatter, the `## Commands Actually Used` section, the `## Phase Boundary
Guard` section, and the closed-set helper inventory. The result should make
the Phase 96 closure chain auditable without overstating broader milestone
progress (no `## Authoritative-Surface Drift`, no `mix verify.test`, no
REQUIREMENTS.md / ROADMAP.md / STATE.md edits).
</verification>

<success_criteria>
Phase 96 has both verification (`96-VERIFICATION.md`) and Nyquist closure
(`96-VALIDATION.md`) artifacts synchronized to the focused-bundle proof
commands, while the active milestone authority surfaces (`.planning/REQUIREMENTS.md`,
`.planning/ROADMAP.md`, `.planning/STATE.md`) remain intentionally untouched
until Phase 103 (D-17, D-18).
</success_criteria>

<output>
After completion, create `.planning/phases/101-phase-96-verification-backfill/101-02-SUMMARY.md`
</output>
</content>
</invoke>