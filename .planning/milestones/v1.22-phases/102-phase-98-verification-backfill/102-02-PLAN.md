---
phase: 102-phase-98-verification-backfill
plan: "02"
type: execute
wave: 2
depends_on:
  - "102-01"
files_modified:
  - .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md
  - .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
autonomous: true
requirements:
  - SURF-01
  - SURF-02
  - SURF-03
must_haves:
  truths:
    - "`98-VERIFICATION.md` exists as a current-tree closure artifact with the exact Phase 100/101 frontmatter shape (`phase: 98-mounted-evidence-views-on-audit`, `verified: <102-01 timestamp>`, `status: passed`, `score: 3/3 requirement bands verified`, `overrides_applied: 0`), a `## Current-tree preflight` PASS section, three numbered bands 1:1 with SURF-01/SURF-02/SURF-03 (per D-03), an Authority statement disclaiming `mix verify.test` per D-10 citing commit `b636c17`, a three-row `## Requirement closure` table per D-13, and a `## Not closed here` section per D-20 with four bullets + closing line."
    - "`98-VALIDATION.md` is finalized as a Nyquist closure artifact: frontmatter flipped to `status: validated` / `nyquist_compliant: true` / `wave_0_complete: true` with an `updated:` timestamp added; the opening block-quote carries the load-bearing `**Retroactive backfill note:**` block per D-14 (literal phrase mandatory per PATTERNS.md and RESEARCH.md §4.2 — `rg -nF 'Retroactive backfill note'` must return exactly one match); `## Commands Actually Used` lands per D-15 with a single numbered entry citing the focused bundle + observed count; `## Phase Boundary Guard` lands with SIX bullets (five from the Phase 95 analog plus the new UI-SPEC Manual-Only bullet per D-11/D-20 + PATTERNS.md key pattern); Validation Sign-Off checkboxes flip to `[x]` per RESEARCH.md §4.8 with the vestigial `No watch-mode flags` line deleted; the closing `**Approval:**` line is rewritten to `finalized on 2026-05-27 after Phase 102-01 produced 98-VERIFICATION.md and the current-tree rerun bundle passed.` per RESEARCH.md §4.9."
    - "Phase 102 closes the missing Phase 98 verification and validation chain only — `.planning/REQUIREMENTS.md` (SURF-01/02/03 rows stay `Pending`), `.planning/ROADMAP.md` (Phase 102 plan checkboxes stay `[ ]`), and `.planning/STATE.md` (Position stays `Phase 102 Not started` from Phase 102's perspective) are intentionally unreconciled here per D-19; visual hierarchy, spacing tokens, typography sizing, color palette, and scanability portions of `98-UI-SPEC.md` remain Manual-Only per D-11. Phase 103 owns all of that reconciliation."
  artifacts:
    - path: .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md
      provides: "Current-tree verification artifact (NEW) that closes the missing Phase 98 verification chain with three numbered bands 1:1 with SURF-01/02/03, the Authority statement disclaiming mix verify.test (per D-10), the three-row Requirement closure table (per D-13), and the four-bullet + closing-line Not closed here section (per D-20)"
      contains: "SURF-01"
    - path: .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
      provides: "Final Nyquist validation contract with the load-bearing Retroactive backfill note (per D-14), the single-entry Commands Actually Used section (per D-15), the six-bullet Phase Boundary Guard (per D-11/D-20 + PATTERNS.md), and the executed-finalization Approval line (per RESEARCH.md §4.9)"
      contains: "nyquist_compliant: true"
  key_links:
    - from: .planning/phases/102-phase-98-verification-backfill/102-01-SUMMARY.md
      to: .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md
      via: "every literal test count and grep stdout cited in `98-VERIFICATION.md` Band 1/2/3 Evidence blocks MUST come from 102-01's captured output, NOT from RESEARCH.md predictions (per Risk 5 in RESEARCH.md §8 — research can predict but cannot lock the count); the executor re-reads 102-01-SUMMARY.md and uses the recorded numbers"
      pattern: "34 tests, 0 failures|live\\(\"/evidence\"|def handle_event|alias Threadline\\.Evidence\\.Proof|@semantic_statuses|evidence_authorize_fn|Threadline\\.RBAC"
    - from: .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md
      to: .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
      via: "the validation artifact's `## Commands Actually Used` section MUST cite the same focused-bundle command that the verification artifact's Band 1/2/3 Evidence blocks cite, with the literal `N tests, 0 failures` count matching across both artifacts (per D-15 + PATTERNS.md `## Commands Actually Used` section pattern)"
      pattern: "mix test test/threadline/operator_surface/auth_test\\.exs test/threadline/operator_surface/live/evidence_live_test\\.exs --max-failures 1|34 tests, 0 failures|SURF-01|SURF-02|SURF-03"
    - from: .planning/phases/95-evidence-model-lock-and-scope-guard/95-VERIFICATION.md
      to: .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md
      via: "the 95-VERIFICATION.md frontmatter, header, preflight, band, requirement closure, and Not closed here shapes are the VERBATIM template (3-band 1:1 layout per D-03 + PATTERNS.md primary analog); 98 substitutes for 95 and SURF-01/02/03 substitute for EVID-01/02/03"
      pattern: "phase:|verified:|status: passed|score: 3/3 requirement bands verified|overrides_applied: 0|Current-tree preflight|Requirement closure|Not closed here"
    - from: .planning/phases/95-evidence-model-lock-and-scope-guard/95-VALIDATION.md
      to: .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
      via: "the 95-VALIDATION.md finalized shape (frontmatter flip + Commands Actually Used + Phase Boundary Guard + Approval line) is the VERBATIM template per PATTERNS.md primary analog; Phase 102 EXTENDS the analog with the new Retroactive backfill note (D-14) and the new UI-SPEC Manual-Only bullet in Phase Boundary Guard (D-11/D-20 — 6 bullets vs Phase 95's 5)"
      pattern: "status: validated|nyquist_compliant: true|wave_0_complete: true|## Commands Actually Used|## Phase Boundary Guard|\\*\\*Approval:\\*\\* finalized"
---

<objective>
Create the new `98-VERIFICATION.md` artifact and finalize the existing
`98-VALIDATION.md` artifact so the Phase 98 verification and validation chain
closes against the current tree with explicit proof for `SURF-01`, `SURF-02`,
and `SURF-03`. Mirror the Phase 100 plan pair shape exactly: this plan writes
the verification artifact's three numbered bands 1:1 with the requirements
(per D-03), embeds the literal stdout 102-01 captured, applies the Authority
statement disclaiming `mix verify.test` per D-10 (citing Phase 99 commit
`b636c17`), then flips the validation artifact to its Nyquist-final shape
with the load-bearing `**Retroactive backfill note:**` block per D-14 + the
single-entry `## Commands Actually Used` section per D-15 + the six-bullet
`## Phase Boundary Guard` per D-11/D-20.

Purpose: turn the live evidence 102-01 captured into the durable closure
chain Phase 98 was supposed to ship — and do it honestly, so the
`nyquist_compliant: true` frontmatter flip reads as retroactive backfill
(not merge-theater per Phase 101 D-16 / RESEARCH.md Risk 3).

Output: a NEW `.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md`
matching the Phase 100/101 frontmatter + structure exactly, and a MODIFIED
`.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` with
the frontmatter flipped, the retroactive-backfill note added, the
`## Commands Actually Used` and `## Phase Boundary Guard` sections inserted,
the Validation Sign-Off checkboxes flipped, and the Approval line rewritten.
Milestone authority surfaces (REQUIREMENTS.md / ROADMAP.md / STATE.md) and
the UI-SPEC body remain untouched per D-19 / D-11 / T-102-08 (Phase 103 scope).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.22-MILESTONE-AUDIT.md
@.planning/phases/102-phase-98-verification-backfill/102-CONTEXT.md
@.planning/phases/102-phase-98-verification-backfill/102-RESEARCH.md
@.planning/phases/102-phase-98-verification-backfill/102-PATTERNS.md
@.planning/phases/102-phase-98-verification-backfill/102-VALIDATION.md
@.planning/phases/102-phase-98-verification-backfill/102-01-SUMMARY.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-CONTEXT.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-01-SUMMARY.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-02-SUMMARY.md
@.planning/phases/95-evidence-model-lock-and-scope-guard/95-VERIFICATION.md
@.planning/phases/95-evidence-model-lock-and-scope-guard/95-VALIDATION.md
@.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md
@.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md
@.planning/phases/84-export-delivery-and-scale-adapter-integration-repair/84-VERIFICATION.md
@.planning/phases/99-contract-lock-docs-and-final-verification/99-VERIFICATION.md
@.planning/phases/100-phase-95-verification-backfill/100-02-PLAN.md
@.planning/phases/101-phase-96-verification-backfill/101-02-PLAN.md

<interfaces>
<!-- The interface contracts 102-02 writes against. All literal stdout values
     come from 102-01's captured output, not from RESEARCH.md predictions
     (per Risk 5 — research predicts 34/0 but the executor must record what
     was actually printed). -->

From `.planning/phases/95-VERIFICATION.md` lines 1-7 (the verbatim frontmatter template per D-01 + PATTERNS.md primary analog):
```yaml
---
phase: 95-evidence-model-lock-and-scope-guard
verified: 2026-05-26T14:54:49Z
status: passed
score: 3/3 requirement bands verified
overrides_applied: 0
---
```
Apply to `98-VERIFICATION.md` with these substitutions: `phase:` → `98-mounted-evidence-views-on-audit`; `verified:` → 102-01's actual execution timestamp; `score:` unchanged (`3/3` per D-03 — DO NOT use `4/4` from 96-VERIFICATION.md per PATTERNS.md `Frontmatter pattern` NOTE).

From `.planning/phases/95-VERIFICATION.md` lines 24-39 (the verbatim band shape — repeat 3× for SURF-01/02/03 per D-03):
```markdown
## N. <descriptive title — NO requirement ID in heading>

**Requirement:** `SURF-NN`<two-space hard break>
**Result:** PASS

- <bullet 1 citing a specific line or module>
- <bullet 2 citing a specific line or module>
- <bullet 3 citing a specific line or module>

### Evidence

\`\`\`bash
<the exact command>
\`\`\`

Result: PASS (`<N tests, 0 failures>` or `<grep result description>`)
```

From `.planning/phases/95-VERIFICATION.md` lines 81-87 (the three-row Requirement closure table shape per D-13):
```markdown
## Requirement closure

| Requirement | Status | Why it closes on the current tree |
| --- | --- | --- |
| `SURF-01` | ✓ SATISFIED | <prose per CONTEXT.md `<specifics>`> |
| `SURF-02` | ✓ SATISFIED | <prose per CONTEXT.md `<specifics>`> |
| `SURF-03` | ✓ SATISFIED | <prose per CONTEXT.md `<specifics>`> |
```

From `.planning/phases/95-VALIDATION.md` lines 1-9 (the verbatim Nyquist-final frontmatter — per RESEARCH.md §4.1):
```yaml
---
phase: 98
slug: mounted-evidence-views-on-audit
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-26
updated: 2026-05-27T<HH:MM:SSZ>
---
```

From `.planning/phases/95-VALIDATION.md` lines 83-89 (Phase Boundary Guard — 5 bullets):
```markdown
## Phase Boundary Guard

- `95-VALIDATION.md` closes `EVID-01`, `EVID-02`, and `EVID-03` only.
- `.planning/REQUIREMENTS.md` was not reconciled here.
- `.planning/ROADMAP.md` was not reconciled here.
- `.planning/STATE.md` was not reconciled here.
- Phase 96, Phase 98, and milestone closeout work remain outside this validation artifact.
```
Apply to `98-VALIDATION.md` with SIX bullets per D-20 + PATTERNS.md key pattern — Phase 102's additional bullet is the UI-SPEC Manual-Only call-out per D-11.

From `.planning/REQUIREMENTS.md` lines 22-24 + Traceability lines 60-62 (the requirement rows; 102-02 MUST NOT edit these per D-19):
```text
- [ ] **SURF-01**: Read-only evidence views live on the existing `/audit` surface rather than a new operator UI family.
- [ ] **SURF-02**: Mounted evidence views show the same evidence facts and boundary language as the library API and Mix-task paths.
- [ ] **SURF-03**: Host-owned authorization remains the gate for mounted evidence views; Threadline does not introduce RBAC or tenant DSL semantics.
| SURF-01 | Phase 102 | Pending |
| SURF-02 | Phase 102 | Pending |
| SURF-03 | Phase 102 | Pending |
```

From `.planning/phases/102-phase-98-verification-backfill/102-01-SUMMARY.md` (the load-bearing evidence source — Risk 5 says cite this, NOT RESEARCH.md):
```text
- Focused-bundle command + observed ExUnit count (expected `34 tests, 0 failures`; record what was actually printed)
- Six structural grep outputs from RESEARCH.md §5 (mount, no-handler, presenter alias + call site, @semantic_statuses, negative RBAC, positive evidence_authorize_fn)
- Five locked-literal grep outputs from RESEARCH.md §5.2 (D-12 inventory)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 102-02-01: Write the new 98-VERIFICATION.md SURF-01 band (read-only /audit/evidence mount inside the existing operator family)</name>
  <files>.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md</files>
  <read_first>
    <file>.planning/phases/102-phase-98-verification-backfill/102-CONTEXT.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-RESEARCH.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-PATTERNS.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-01-SUMMARY.md</file>
    <file>.planning/phases/95-evidence-model-lock-and-scope-guard/95-VERIFICATION.md</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-CONTEXT.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-01-SUMMARY.md</file>
    <file>lib/threadline/operator_surface/router.ex</file>
    <file>lib/threadline/operator_surface/live/evidence_live.ex</file>
  </read_first>
  <action>Create `.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md` (NEW) using `95-VERIFICATION.md` lines 1-22 as the verbatim template for the front matter, header, and preflight per PATTERNS.md primary analog. Write the file in this exact order with these exact literal values. (1) **Frontmatter (per D-01 + RESEARCH.md §3.1):** `phase: 98-mounted-evidence-views-on-audit`, `verified: <102-01 ISO-8601 UTC execution timestamp from 102-01-SUMMARY.md>`, `status: passed`, `score: 3/3 requirement bands verified` (DO NOT use `4/4` from 96-VERIFICATION.md per PATTERNS.md frontmatter NOTE — Phase 96 had a single PROOF-01 row spanning four contract surfaces; Phase 102 has three plural SURF-XX requirements), `overrides_applied: 0`. (2) **Header (per RESEARCH.md §3.2):** `# Phase 98: Mounted Evidence Views On \`/audit\` Verification Report` followed by `**Phase Goal:** Re-prove the current-tree mounted \`/audit/evidence\` surface with explicit verification evidence instead of inherited summary claims.`, `**Verified:** <same timestamp as frontmatter>`, `**Status:** passed`, `**Re-verification:** Yes - gap closure for missing phase verification`. (3) **`## Current-tree preflight` section (per D-02 + RESEARCH.md §3.3 + PATTERNS.md preflight pattern):** `**Result:** PASS` followed by three bullets verbatim: "The Phase 98 implementation files, tests, and summaries are present on disk, but `98-VERIFICATION.md` was missing before this run." / "This verification treats the current working tree as the authority and closes that missing artifact gap directly." / "Milestone authority surfaces remain intentionally unreconciled here; `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` stay Phase 103 work." (4) **Band 1 — SURF-01 (per RESEARCH.md §3.5 candidate title + §5.1 evidence commands):** Heading `## 1. Read-only /audit/evidence mount inside the existing operator family` (NO requirement ID in heading per PATTERNS.md band-shape key constraint #1). Body: `**Requirement:** \`SURF-01\`` followed by two trailing spaces + newline + `**Result:** PASS`. Then three bullets citing specific lines: bullet 1 names `lib/threadline/operator_surface/router.ex:100` `live("/evidence", EvidenceLive, :index)` mount inside the `live_session :threadline` block (lines 89-109) as a sibling alongside `live("/", TimelineLive, :index)` at line 99 — no new UI family introduced; bullet 2 names `lib/threadline/operator_surface/live/evidence_live.ex` defining only `mount/3` (line 12), `handle_params/3` (line 21), and `render/1` (line 49) with no `handle_event/3` defined — URL-driven navigation per D-19 of 98-CONTEXT.md; bullet 3 names the focused-bundle test file `test/threadline/operator_surface/live/evidence_live_test.exs` as the LiveView-scope behavioral authority for the mount + navigation + URL-driven assertions. Then TWO `### Evidence` sub-blocks (multiple Evidence blocks per band allowed per PATTERNS.md key constraint #5): Evidence 1 fenced `bash` block with `rg -n 'live("/evidence"' lib/threadline/operator_surface/router.ex` and below it `Result: PASS (exactly one match at line 100, inside the live_session :threadline block opened at line 89)`; Evidence 2 fenced `bash` block with `rg -n '^\s*def handle_event' lib/threadline/operator_surface/live/evidence_live.ex` and below it `Result: PASS (exit code 1 — zero matches; negative assertion proving no mutation handlers are defined)`; Evidence 3 fenced `bash` block with `mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` and below it `Result: PASS (\`<observed count from 102-01 SUMMARY — expected 5 tests, 0 failures>\`)`. Use the literal count 102-01 actually recorded — do NOT copy `5 tests, 0 failures` from research notes if 102-01 captured a different number (Risk 5). DO NOT include a `## Behavioral Spot-Checks` table — that is verifier-output shape, not phase-artifact shape per RESEARCH.md §3.7 + PATTERNS.md `Anti-pattern: Behavioral Spot-Checks table — do NOT include`. STOP HERE — Bands 2, 3, the Authority statement, the Requirement closure table, and Not closed here are owned by Tasks 102-02-02 and 102-02-03 (Band 2/3) and 102-02-04 (closure table + Not closed here). At task end, `98-VERIFICATION.md` contains frontmatter + header + preflight + Band 1 only.</action>
  <acceptance_criteria>
    <criterion>`.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md` exists and contains the frontmatter keys `phase: 98-mounted-evidence-views-on-audit`, `verified:`, `status: passed`, `score: 3/3 requirement bands verified`, and `overrides_applied: 0` (per D-01 + RESEARCH.md §3.1).</criterion>
    <criterion>`98-VERIFICATION.md` header reads `# Phase 98: Mounted Evidence Views On \`/audit\` Verification Report` and contains the literal phrases `Phase Goal:`, `Re-prove the current-tree mounted`, `**Status:** passed`, and `**Re-verification:** Yes - gap closure for missing phase verification` (per RESEARCH.md §3.2).</criterion>
    <criterion>`98-VERIFICATION.md` contains the `## Current-tree preflight` heading with `**Result:** PASS` and the three preflight bullets explicitly naming `98-VERIFICATION.md`, `working tree`, and `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` as Phase 103 work (per D-02 + RESEARCH.md §3.3).</criterion>
    <criterion>`98-VERIFICATION.md` contains the literal Band 1 heading `## 1. Read-only /audit/evidence mount inside the existing operator family` (per RESEARCH.md §3.5 candidate 1; NO requirement ID in heading per PATTERNS.md band-shape key constraint #1).</criterion>
    <criterion>Band 1 body contains `**Requirement:** \`SURF-01\`` and `**Result:** PASS` on separate lines (per PATTERNS.md band-shape key constraint #2).</criterion>
    <criterion>Band 1 Evidence blocks cite all three commands verbatim: `rg -n 'live("/evidence"' lib/threadline/operator_surface/router.ex`, `rg -n '^\s*def handle_event' lib/threadline/operator_surface/live/evidence_live.ex`, and `mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` (per RESEARCH.md §5.1).</criterion>
    <criterion>Band 1 does NOT cite `mix verify.test` anywhere (per D-10 + Risk 2 in RESEARCH.md §8).</criterion>
    <criterion>`98-VERIFICATION.md` does NOT yet contain a `## Behavioral Spot-Checks` heading (per RESEARCH.md §3.7 + PATTERNS.md anti-pattern — that table is verifier-output shape, not phase-artifact shape).</criterion>
    <criterion>`git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/` reports zero changes (T-102-08 boundary).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && grep -E '^phase: 98-mounted-evidence-views-on-audit$' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && grep -E '^status: passed$' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && grep -E '^score: 3/3 requirement bands verified$' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && grep -E '^overrides_applied: 0$' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '# Phase 98: Mounted Evidence Views On `/audit` Verification Report' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '## Current-tree preflight' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '## 1. Read-only /audit/evidence mount inside the existing operator family' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '**Requirement:** `SURF-01`' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF "live(\"/evidence\"" .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'def handle_event' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && ! rg -n 'mix verify\.test' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && ! rg -nF '## Behavioral Spot-Checks' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/ | (! grep -E '^\s*[MADRCU?]')</automated>
  </verify>
  <done>`98-VERIFICATION.md` exists with frontmatter (`phase: 98-mounted-evidence-views-on-audit` / `status: passed` / `score: 3/3 requirement bands verified` / `overrides_applied: 0`), header, three-bullet `## Current-tree preflight`, and Band 1 (SURF-01) carrying the read-only mount + no-handler structural proofs plus the LiveView focused-bundle test command — citing 102-01-SUMMARY.md's literal counts, NOT research predictions. No Behavioral Spot-Checks table per RESEARCH.md §3.7. No milestone-authority surface or UI-SPEC body has been modified.</done>
</task>

<task type="auto">
  <name>Task 102-02-02: Append the SURF-02 band (mounted parity through Threadline.Evidence.Proof + locked copy literals) to 98-VERIFICATION.md</name>
  <files>.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md</files>
  <read_first>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-CONTEXT.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-RESEARCH.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-PATTERNS.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-01-SUMMARY.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md</file>
    <file>.planning/phases/84-export-delivery-and-scale-adapter-integration-repair/84-VERIFICATION.md</file>
    <file>lib/threadline/operator_surface/live/evidence_live.ex</file>
    <file>lib/threadline/operator_surface/unsupported.ex</file>
    <file>lib/threadline/evidence/proof.ex</file>
    <file>test/threadline/operator_surface/live/evidence_live_test.exs</file>
  </read_first>
  <action>Append Band 2 to the existing `98-VERIFICATION.md` (created in Task 1) following the `95-VERIFICATION.md` band shape per PATTERNS.md band-shape key constraints. (1) **Heading (per RESEARCH.md §3.5 candidate 2):** `## 2. Mounted parity through Threadline.Evidence.Proof and locked copy literals` (NO requirement ID in heading per PATTERNS.md key constraint #1). (2) **Body:** `**Requirement:** \`SURF-02\`` with two trailing spaces + newline + `**Result:** PASS`. (3) **Bullets — three to four bullets citing specific lines** (per PATTERNS.md key constraint #3): bullet 1 names `lib/threadline/operator_surface/live/evidence_live.ex:8` `alias Threadline.Evidence.Proof` and `evidence_live.ex:253` `Proof.present_record(record)` inside `defp build_row/1` — the same presenter `lib/threadline/evidence/proof.ex:75` exposes for Mix-task `render_human/1` and `to_json_iodata/2` paths; bullet 2 names the canonical verdict-vocabulary source `lib/threadline/evidence/proof.ex:10` `@semantic_statuses ~w(proven inferred_posture unsupported)` per RESEARCH.md §2.2 nuance (the dynamic render site at `evidence_live.ex:116-118` consumes this vocabulary via `{row.verdict_status}` — both must be cited because the render site is NOT the literal-defining site per Risk 6); bullet 3 names the five locked Phase 98 UI-SPEC copy literals from D-12, citing `98-UI-SPEC.md` alongside the lib/test paths per the supplementary `84-VERIFICATION.md:24` precedent (D-11): `What can Threadline prove right now?` at `evidence_live.ex:67` asserted by `evidence_live_test.exs:115` (refute, denied-state) and `:150` (assert, overview); `View history` at `evidence_live.ex:142` asserted at `evidence_live_test.exs:152`; `No evidence records yet` at `evidence_live.ex:89-95` asserted at `evidence_live_test.exs:213`; `Evidence view unavailable.` at `unsupported.ex:25` rendered via `evidence_live.ex:154` and asserted at `evidence_live_test.exs:113`; verdict triple asserted at `evidence_live_test.exs:153-155`. (Per CONTEXT.md Claude's Discretion bullet 3 — render the D-12 inventory either as a verbatim sub-table inside this band or as inline-per-row Result bullets, as long as every literal is paired with both a structural and a behavioral citation; recommended posture is the inline-bullet form because it keeps band length proportional to Bands 1 and 3.) (4) **`### Evidence` sub-blocks (per PATTERNS.md key constraint #4 + #5 — multiple Evidence blocks per band):** Evidence 1 fenced `bash` block `rg -n 'alias Threadline\.Evidence\.Proof|Proof\.present_record' lib/threadline/operator_surface/live/evidence_live.ex` with `Result: PASS (exactly two matches: line 8 alias, line 253 Proof.present_record call site)`; Evidence 2 fenced `bash` block `rg -n '@semantic_statuses' lib/threadline/evidence/proof.ex` with `Result: PASS (exactly one match at line 10 — @semantic_statuses ~w(proven inferred_posture unsupported); canonical verdict-vocabulary source per RESEARCH.md §2.2)`; Evidence 3 fenced `bash` block `mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` with `Result: PASS (\`<observed count from 102-01 SUMMARY — expected 5 tests, 0 failures>\`)`. Optionally, Evidence 4 fenced `bash` block with one or more of the five `rg -nF` locked-literal greps from RESEARCH.md §5.2 (Claude's Discretion — include if it helps a reviewer trace one literal end-to-end without re-reading the source). Per Risk 1 + D-17, do NOT propose any UI-SPEC body weakening — if a literal does not appear at the cited source line, the smallest repair is to update the SOURCE, not the UI-SPEC; in 102-01 this was confirmed to be unnecessary (every D-12 literal is present at the cited source AND test lines). Per Risk 6 / Pitfall in PATTERNS.md, do NOT cite ONLY `rg -nF 'proven' lib/threadline/operator_surface/live/evidence_live.ex` (zero matches because the verdict is dynamic) — always pair with the `@semantic_statuses` grep against `proof.ex` AND the test-line assertions. Per D-10, do NOT cite `mix verify.test` anywhere in this band.</action>
  <acceptance_criteria>
    <criterion>`98-VERIFICATION.md` contains the literal Band 2 heading `## 2. Mounted parity through Threadline.Evidence.Proof and locked copy literals` (per RESEARCH.md §3.5 candidate 2; NO requirement ID in heading per PATTERNS.md key constraint #1).</criterion>
    <criterion>Band 2 body contains `**Requirement:** \`SURF-02\`` and `**Result:** PASS` on separate lines.</criterion>
    <criterion>Band 2 explicitly cites the shared-presenter wiring: the literal strings `alias Threadline.Evidence.Proof`, `Proof.present_record`, and `lib/threadline/operator_surface/live/evidence_live.ex` ALL appear in the band (per D-06 + RESEARCH.md §5.2).</criterion>
    <criterion>Band 2 explicitly cites the canonical verdict-vocabulary source: the literal strings `@semantic_statuses` and `lib/threadline/evidence/proof.ex` AND the literal phrase `proven`, `inferred_posture`, and `unsupported` (per Risk 6 + RESEARCH.md §2.2 nuance — the verdict triple lives in proof.ex:10, NOT only at the dynamic render site evidence_live.ex:116-118).</criterion>
    <criterion>Band 2 explicitly cites at least four of the five D-12 locked UI-SPEC literals by name: `What can Threadline prove right now?`, `View history`, `No evidence records yet`, and `Evidence view unavailable.` (plus the verdict triple already covered above) — alongside `98-UI-SPEC.md` per D-11 + PATTERNS.md UI-SPEC-citation precedent.</criterion>
    <criterion>Band 2 Evidence blocks cite at least `rg -n 'alias Threadline\.Evidence\.Proof|Proof\.present_record' lib/threadline/operator_surface/live/evidence_live.ex`, `rg -n '@semantic_statuses' lib/threadline/evidence/proof.ex`, and `mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` (per RESEARCH.md §5.2 + D-06).</criterion>
    <criterion>Band 2 does NOT cite `mix verify.test` anywhere (per D-10 + Risk 2).</criterion>
    <criterion>Band 2 does NOT propose any modification to `98-UI-SPEC.md` (per D-17 + Risk 1 — UI-SPEC is the design contract; code is the realization).</criterion>
    <criterion>`98-VERIFICATION.md` Band 1 (Task 1's output) is unchanged at task end (this task appends only).</criterion>
    <criterion>`git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/` reports zero changes (T-102-08 boundary).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -nF '## 2. Mounted parity through Threadline.Evidence.Proof and locked copy literals' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '**Requirement:** `SURF-02`' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'alias Threadline.Evidence.Proof' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'Proof.present_record' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '@semantic_statuses' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'lib/threadline/evidence/proof.ex' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'proven' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'inferred_posture' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'unsupported' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'What can Threadline prove right now?' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'View history' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'No evidence records yet' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'Evidence view unavailable.' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '98-UI-SPEC.md' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && ! rg -n 'mix verify\.test' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '## 1. Read-only /audit/evidence mount inside the existing operator family' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/ | (! grep -E '^\s*[MADRCU?]')</automated>
  </verify>
  <done>`98-VERIFICATION.md` now contains Band 2 (SURF-02) carrying the shared-presenter alias + call-site proof, the canonical `@semantic_statuses` verdict-vocabulary source (per RESEARCH.md §2.2 dual-grep nuance), and the four named D-12 locked copy literals plus the verdict triple — all paired with both structural (`rg`) and behavioral (`mix test`) citations and the `98-UI-SPEC.md` design-contract reference (per D-11 + 84-VERIFICATION.md precedent). Band 1 from Task 1 is unchanged. No `mix verify.test`, no UI-SPEC body edit, no milestone-authority surface change.</done>
</task>

<task type="auto">
  <name>Task 102-02-03: Append the SURF-03 band (host-owned evidence_authorize_fn gate, no Threadline RBAC) + Authority statement to 98-VERIFICATION.md</name>
  <files>.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md</files>
  <read_first>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-CONTEXT.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-RESEARCH.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-PATTERNS.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-01-SUMMARY.md</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-VERIFICATION.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-02-SUMMARY.md</file>
    <file>lib/threadline/operator_surface/auth.ex</file>
    <file>test/threadline/operator_surface/auth_test.exs</file>
    <file>test/threadline/operator_surface/live/evidence_live_test.exs</file>
  </read_first>
  <action>Append Band 3 + the Authority statement to the existing `98-VERIFICATION.md`. (1) **Heading (per RESEARCH.md §3.5 candidate 3):** `## 3. Host-owned evidence_authorize_fn gate with no Threadline RBAC` (NO requirement ID in heading per PATTERNS.md key constraint #1). (2) **Body:** `**Requirement:** \`SURF-03\`` with two trailing spaces + newline + `**Result:** PASS`. (3) **Bullets — three to four citing specific lines per PATTERNS.md key constraint #3:** bullet 1 names `lib/threadline/operator_surface/auth.ex:253-254` `defp assign_evidence_enabled(socket, opts) do` + `evidence_authorize_fn = Keyword.get(opts, :evidence_authorize_fn, fn _ -> false end)` — the fail-closed default that denies access when the host omits the opt entirely; bullet 2 cites the callback shape arity: `evidence_authorize_fn` is a host-supplied function value of shape `(%{assigns: map()} -> boolean | :ok | {:ok, scope} | _)` dispatched at `auth.ex:269` (`evidence_authorize_fn.(mirror)`) where `mirror = %{assigns: socket.assigns}` (`auth.ex:267`) — host-supplied callback, NOT a Threadline-owned module dispatch or behaviour implementation or protocol consumer (per RESEARCH.md §5.3 arity citation); bullet 3 names the absence of Threadline-owned RBAC: zero matches for `Threadline.RBAC|Threadline.Permissions|Threadline.Policy.RBAC` under `lib/threadline/operator_surface/`, PAIRED with the positive-control grep above so a path typo would fail loudly (per D-07 + RESEARCH.md §5.3); bullet 4 names the 6 unit tests in `describe "assign_evidence_enabled"` at `test/threadline/operator_surface/auth_test.exs:337-394` (SURF-03 capability-boolean fan-out at unit scope) plus the denied-state HTML assertion at `test/threadline/operator_surface/live/evidence_live_test.exs:106-116` (Test 1 at line 106 owns the SURF-03 denied-state HTML rendering — `assert html =~ "Evidence view unavailable."` at line 113). (4) **`### Evidence` sub-blocks:** Evidence 1 fenced `bash` block `rg -n 'Threadline\.RBAC|Threadline\.Permissions|Threadline\.Policy\.RBAC' lib/threadline/operator_surface/` with `Result: PASS (exit code 1 — zero matches; negative assertion that no Threadline-owned RBAC modules are referenced under the operator surface)`; Evidence 2 fenced `bash` block `rg -n 'evidence_authorize_fn' lib/threadline/operator_surface/auth.ex` with `Result: PASS (5 matches at lines 254, 259, 265, 266, 269; line 254 is the canonical Keyword.get(opts, :evidence_authorize_fn, fn _ -> false end) fail-closed default; positive-control PAIRED with the negative grep above per D-07)`; Evidence 3 fenced `bash` block `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` with `Result: PASS (\`<observed count from 102-01 SUMMARY — expected 34 tests, 0 failures>\`)`. (5) **`### Authority statement` sub-section (per RESEARCH.md §5.4 + PATTERNS.md authority-statement pattern):** Lead sentence "The authoritative Phase 98 rerun bundle is:" followed by a numbered list of the six authoritative commands: (1) `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1`; (2) `rg -n 'live("/evidence"' lib/threadline/operator_surface/router.ex` (Band 1); (3) `rg -n '^\s*def handle_event' lib/threadline/operator_surface/live/evidence_live.ex` (Band 1); (4) `rg -n 'alias Threadline\.Evidence\.Proof|Proof\.present_record' lib/threadline/operator_surface/live/evidence_live.ex` (Band 2); (5) `rg -n 'Threadline\.RBAC|Threadline\.Permissions|Threadline\.Policy\.RBAC' lib/threadline/operator_surface/` (Band 3 negative); (6) `rg -n 'evidence_authorize_fn' lib/threadline/operator_surface/auth.ex` (Band 3 positive control). Then the mix-verify-test disclaim paragraph verbatim per CONTEXT.md D-10 / RESEARCH.md §5.4 / PATTERNS.md authority-statement pattern: "`mix verify.test` is intentionally not the authority for Phase 98. The Phase 98-02 summary (`98-02-SUMMARY.md:70-74`) records a pre-existing alias-drift failure in `Threadline.CiTopologyContractTest` that is outside Phase 98 ownership; Phase 99 owns the named-alias topology, and commit `b636c17` (\"fix(99-02): update ci.all topology contract to expanded doc_contract alias\") is the most recent fix on that surface. Phase 102 disclaims rather than reopens that scope." (If 102-01 found that the drift has been repaired post-commit `b636c17`, update the disclaimer to cite the newer fix but KEEP the disclaim shape — per D-10.) STOP HERE — `## Requirement closure` and `## Not closed here` are Task 102-02-04's scope.</action>
  <acceptance_criteria>
    <criterion>`98-VERIFICATION.md` contains the literal Band 3 heading `## 3. Host-owned evidence_authorize_fn gate with no Threadline RBAC` (per RESEARCH.md §3.5 candidate 3; NO requirement ID in heading).</criterion>
    <criterion>Band 3 body contains `**Requirement:** \`SURF-03\`` and `**Result:** PASS` on separate lines.</criterion>
    <criterion>Band 3 cites the fail-closed default `Keyword.get(opts, :evidence_authorize_fn, fn _ -> false end)` at `auth.ex:254` AND names the callback shape as host-supplied (NOT a Threadline-owned module dispatch or behaviour implementation).</criterion>
    <criterion>Band 3 explicitly pairs the negative grep `rg -n 'Threadline\.RBAC|Threadline\.Permissions|Threadline\.Policy\.RBAC' lib/threadline/operator_surface/` with the positive-control grep `rg -n 'evidence_authorize_fn' lib/threadline/operator_surface/auth.ex` (per D-07 — silent-pass on path typo prevention).</criterion>
    <criterion>Band 3 cites the focused-bundle test command `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` with the literal count 102-01 captured (expected `34 tests, 0 failures`).</criterion>
    <criterion>`98-VERIFICATION.md` contains a `### Authority statement` section listing all six authoritative commands from RESEARCH.md §5.4 verbatim AND including the `mix verify.test` disclaimer naming commit `b636c17` and Phase 99 as the alias-topology owner (per D-10 + PATTERNS.md authority-statement pattern + 96-VERIFICATION.md analog).</criterion>
    <criterion>The Authority statement uses the literal phrase `Phase 102 disclaims rather than reopens that scope.` (per RESEARCH.md §5.4 closing prose + CONTEXT.md D-10).</criterion>
    <criterion>Bands 1 and 2 from Tasks 1 and 2 are unchanged (this task appends only).</criterion>
    <criterion>`git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/` reports zero changes (T-102-08 boundary).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -nF '## 3. Host-owned evidence_authorize_fn gate with no Threadline RBAC' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '**Requirement:** `SURF-03`' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'Keyword.get(opts, :evidence_authorize_fn, fn _ -> false end)' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'Threadline.RBAC' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'Threadline.Permissions' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'Threadline.Policy.RBAC' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'evidence_authorize_fn' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '### Authority statement' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'b636c17' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'Phase 102 disclaims rather than reopens that scope.' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '## 1. Read-only /audit/evidence mount inside the existing operator family' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '## 2. Mounted parity through Threadline.Evidence.Proof and locked copy literals' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/ | (! grep -E '^\s*[MADRCU?]')</automated>
  </verify>
  <done>`98-VERIFICATION.md` now contains Band 3 (SURF-03) with the fail-closed default citation, the host-supplied callback arity statement, the paired negative-plus-positive-control greps per D-07, and the focused-bundle test command with 102-01's literal count. The `### Authority statement` lists all six RESEARCH.md §5.4 authoritative commands and disclaims `mix verify.test` per D-10 naming commit `b636c17`. Bands 1 and 2 are unchanged. No milestone-authority or UI-SPEC body edit.</done>
</task>

<task type="auto">
  <name>Task 102-02-04: Add Requirement closure table + Not closed here section to 98-VERIFICATION.md, then finalize 98-VALIDATION.md (frontmatter flip + Retroactive backfill note + Commands Actually Used + Phase Boundary Guard + Validation Sign-Off + Approval line)</name>
  <files>.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md, .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-CONTEXT.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-RESEARCH.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-PATTERNS.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-01-SUMMARY.md</file>
    <file>.planning/phases/95-evidence-model-lock-and-scope-guard/95-VERIFICATION.md</file>
    <file>.planning/phases/95-evidence-model-lock-and-scope-guard/95-VALIDATION.md</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md</file>
  </read_first>
  <action>Two-part task: first close out `98-VERIFICATION.md`, then finalize `98-VALIDATION.md`. **Part A — finish `98-VERIFICATION.md`.** Append `## Requirement closure` (per D-13 + RESEARCH.md §3.6 + PATTERNS.md table) as a three-row table — verbatim prose from CONTEXT.md `<specifics>` block: header row `| Requirement | Status | Why it closes on the current tree |`, separator `| --- | --- | --- |`, then exactly three data rows: `| \`SURF-01\` | ✓ SATISFIED | Threadline mounts the read-only evidence surface as a sibling route inside the existing \`/audit\` operator family, with no new UI family, no mutation handlers, and URL-driven navigation via \`handle_params/3\`. |`; `| \`SURF-02\` | ✓ SATISFIED | The mounted view presents the same evidence facts and verdict vocabulary (\`proven\`, \`inferred_posture\`, \`unsupported\`) as the library API and Mix-task paths via the shared \`Threadline.Evidence.Proof\` presenter, with the locked Phase 98 copy literals (per \`98-UI-SPEC.md\` Copywriting Contract) rendered at the cited source lines and asserted by the existing LiveView test suite. |`; `| \`SURF-03\` | ✓ SATISFIED | Host-owned authorization remains the gate via \`evidence_authorize_fn\`, defaulting fail-closed to \`fn _ -> false end\`, with no Threadline-owned RBAC, tenant DSL, or persona semantics introduced in \`lib/threadline/operator_surface/\`. |`. Then append `## Not closed here` per D-20 + RESEARCH.md §3.8 + PATTERNS.md (FOUR bullets + closing line — note this is the verification artifact's section; the validation artifact's `## Phase Boundary Guard` is separately handled in Part B with SIX bullets): bullet 1 `\`.planning/REQUIREMENTS.md\` remains intentionally unreconciled in this phase.`; bullet 2 `\`.planning/ROADMAP.md\` remains intentionally unreconciled in this phase.`; bullet 3 `\`.planning/STATE.md\` remains intentionally unreconciled in this phase.`; bullet 4 `The visual hierarchy, spacing tokens (4/8/16/24/32/48px), typography sizing, color palette, and scanability portions of \`98-UI-SPEC.md\` remain Manual-Only per \`98-VALIDATION.md\` and are not grep-anchored here.` (per D-11); closing line `Phase 102 closes the missing Phase 98 verification and validation chain only; milestone authority-surface reconciliation remains Phase 103 work.` (per D-20 verbatim). **Part B — finalize `98-VALIDATION.md`.** Apply ALL of RESEARCH.md §4.1, §4.2, §4.4, §4.5, §4.6, §4.7, §4.8, §4.9 + PATTERNS.md retroactive-backfill-note, Commands-Actually-Used, Phase-Boundary-Guard, and Validation-Sign-Off patterns. (1) **Frontmatter flip (per §4.1):** line 4 `status: draft` → `status: validated`; line 5 `nyquist_compliant: false` → `nyquist_compliant: true`; line 6 `wave_0_complete: false` → `wave_0_complete: true`; INSERT after line 7 a new line `updated: 2026-05-27T<HH:MM:SSZ>` using 102-01's execution timestamp (mirrors `95-VALIDATION.md:8`). (2) **Retroactive-backfill note (per §4.2 + D-14 — the load-bearing piece; PATTERNS.md says the literal phrase `**Retroactive backfill note:**` is mandatory; Risk 3 in RESEARCH.md §8 says omitting it makes the frontmatter flip read as merge-theater):** REPLACE the existing opening block-quote at line 12 (currently `> Per-phase validation contract for feedback sampling during execution.`) with the verbatim three-paragraph block from RESEARCH.md §4.2:
```
> Per-phase validation contract for feedback sampling during execution.
> Phase 98 is now closed against the current-tree rerun bundle recorded in
> `98-VERIFICATION.md`, not against summary prose alone.
>
> **Retroactive backfill note:** Wave 0 evidence was reconstructed retroactively
> from the current tree as part of Phase 102; original Phase 98 execution did
> not produce a Nyquist-compliant artifact. The `nyquist_compliant: true` and
> `wave_0_complete: true` flags reflect post-hoc verification of pre-existing
> implementation, not in-line Wave 0 execution. The focused two-file rerun
> bundle named below is the authority, not `mix verify.test`.
```
The literal phrase `**Retroactive backfill note:**` MUST appear exactly once and exactly as written (verify-work grep target per Risk 3). (3) **Per-Task Verification Map status flip (per §4.4):** flip all three Status cells (lines 41/42/43) from `⬜ pending` to `✅ green`. Flip the three File Exists cells: line 41 `❌ W0` → `✅`, line 42 `❌ W0` → `✅`, line 43 `✅ / ❌ W0` → `✅`. DO NOT restructure the 10-column shape — it already matches the modern Nyquist convention from `95-VALIDATION.md:43`. The 102-01-applied focused-bundle commands (no `MIX_ENV=test` prefix) stay as 102-01 wrote them. (4) **`## Commands Actually Used` (per §4.5 + D-15 + PATTERNS.md):** REPLACE the `---` separator at current line 47 with the verbatim block:
```
---

## Commands Actually Used

1. `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1`
   Result: PASS (`<observed count from 102-01 SUMMARY — expected 34 tests, 0 failures>`)

---
```
SINGLE numbered entry per D-15 (NOT three like `95-VALIDATION.md` and NOT three like `96-VALIDATION.md` — Phase 102's structural greps live in `98-VERIFICATION.md`, not the validation command ledger per D-15). Cite the literal count 102-01 actually recorded, NOT research's `34 tests, 0 failures` prediction (Risk 5). (5) **Wave 0 Requirements checkbox flip (per §4.7):** lines 50-52, flip both `[ ]` checkboxes to `[x]`. Optionally add a fourth `[x]` bullet `- [x] `98-VERIFICATION.md` now exists and records the authoritative current-tree rerun bundle.` (mirrors `95-VALIDATION.md:70` last bullet). (6) **`## Phase Boundary Guard` (per §4.6 + D-11/D-20 + PATTERNS.md key pattern — SIX bullets, NOT five like Phase 95):** INSERT after `## Manual-Only Verifications` (current line 56) and before `## Validation Sign-Off` (current line 64). Lead with a `---` separator, then heading, then SIX bullets verbatim: `- \`98-VALIDATION.md\` closes \`SURF-01\`, \`SURF-02\`, and \`SURF-03\` only.`; `- \`.planning/REQUIREMENTS.md\` was not reconciled here.`; `- \`.planning/ROADMAP.md\` was not reconciled here.`; `- \`.planning/STATE.md\` was not reconciled here.`; `- Visual hierarchy, spacing tokens, typography sizing, color palette, and scanability portions of \`98-UI-SPEC.md\` remain Manual-Only and are not grep-anchored in \`98-VERIFICATION.md\`.` (THIS is the new sixth bullet that distinguishes Phase 102 from Phase 95 per D-11 + PATTERNS.md key pattern + Quality Gate item); `- Phase 99 (already shipped contract-lock + final verification), Phase 100 (Phase 95 backfill), Phase 101 (Phase 96 backfill), and Phase 103 (authority-surface reconciliation) work remains outside this validation artifact.`. (7) **Validation Sign-Off checkbox flips (per §4.8):** line 66 `- [ ] All tasks have...` → `- [x] All executed tasks have explicit automated verification coverage.`; line 67 `- [ ] Sampling continuity...` → `- [x] Sampling continuity stayed below the three-task Nyquist gap.`; line 68 `- [ ] Wave 0 covers all MISSING references` → `- [x] The validation artifact records the exact rerun bundle used to close SURF-01, SURF-02, and SURF-03.`; **DELETE** line 69 `- [ ] No watch-mode flags` (vestigial draft-template line per RESEARCH.md §4.8 — `95-VALIDATION.md:93-99` does not include it); line 70 `- [ ] Feedback latency < 45s` → `- [x] Feedback latency under 30s confirmed by live measurement (0.2s warm).`; line 71 `- [ ] nyquist_compliant: true set in frontmatter` → `- [x] nyquist_compliant: true set in frontmatter (per D-14 retroactive-backfill posture).`; ADD as a new sixth `[x]` line `- [x] Phase-boundary limits are stated explicitly so this artifact does not overclaim authority-surface reconciliation.` (per `95-VALIDATION.md:99` optional sixth checkbox). (8) **Approval line (per §4.9):** line 73 `**Approval:** pending` → `**Approval:** finalized on 2026-05-27 after Phase 102-01 produced 98-VERIFICATION.md and the current-tree rerun bundle passed.` (mirrors `95-VALIDATION.md:101` and `96-VALIDATION.md:65` shape). (9) **Critical boundary checks:** per D-19 / Risk 4 / T-102-08, do NOT modify `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, or `.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md` body. Do NOT modify any file under `lib/` or `test/` (D-16). Do NOT add a `## Authoritative-Surface Drift` section (per PATTERNS.md forbidden patterns — that section is 89-VALIDATION.md-specific). Do NOT widen the SURF-XX wording (D-16).</action>
  <acceptance_criteria>
    <criterion>`98-VERIFICATION.md` contains the literal heading `## Requirement closure` with a three-row table whose Requirement column reads exactly `\`SURF-01\``, `\`SURF-02\``, `\`SURF-03\`` in that order (per D-13 + RESEARCH.md §3.6 + REQUIREMENTS.md traceability sequence).</criterion>
    <criterion>`98-VERIFICATION.md` contains the literal heading `## Not closed here` with the four bullets naming `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `98-UI-SPEC.md`, plus the closing line `Phase 102 closes the missing Phase 98 verification and validation chain only; milestone authority-surface reconciliation remains Phase 103 work.` (per D-20 verbatim).</criterion>
    <criterion>`98-VALIDATION.md` frontmatter contains `phase: 98`, `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, and a new `updated:` line with an ISO-8601 UTC timestamp (per D-14 + RESEARCH.md §4.1).</criterion>
    <criterion>`98-VALIDATION.md` opening block-quote contains the load-bearing literal phrase `**Retroactive backfill note:**` exactly once (per D-14 + PATTERNS.md + Risk 3 — the verify-work grep `rg -nF 'Retroactive backfill note' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` returns exactly one match).</criterion>
    <criterion>`98-VALIDATION.md` opening block-quote contains the literal phrase `reconstructed retroactively` (per RESEARCH.md §4.2 verbatim shape).</criterion>
    <criterion>`98-VALIDATION.md` Per-Task Verification Map (lines 41-43) Status column reads `✅ green` for all three rows (per RESEARCH.md §4.4 — flipped after 102-01 confirmed the bundle passes).</criterion>
    <criterion>`98-VALIDATION.md` contains a `## Commands Actually Used` section with EXACTLY ONE numbered entry citing `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` and the literal count 102-01 captured (per D-15 — single entry, not three like 95/96).</criterion>
    <criterion>`98-VALIDATION.md` contains a `## Phase Boundary Guard` section with EXACTLY SIX bullets (per D-11/D-20 + PATTERNS.md key pattern — `bash -c "awk '/^## Phase Boundary Guard$/,/^## Validation Sign-Off$/' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md | grep -cE '^- '" returns 6`), including the literal Manual-Only UI-SPEC bullet starting `Visual hierarchy, spacing tokens, typography sizing, color palette, and scanability portions of \`98-UI-SPEC.md\` remain Manual-Only` (the sixth-bullet differentiator from Phase 95's five).</criterion>
    <criterion>`98-VALIDATION.md` Validation Sign-Off section has all checkboxes flipped to `[x]` with the vestigial `- [ ] No watch-mode flags` line removed (per RESEARCH.md §4.8) and a sixth `[x]` bullet stating `Phase-boundary limits are stated explicitly`.</criterion>
    <criterion>`98-VALIDATION.md` closing `**Approval:**` line reads `finalized on 2026-05-27 after Phase 102-01 produced 98-VERIFICATION.md and the current-tree rerun bundle passed.` (per RESEARCH.md §4.9).</criterion>
    <criterion>`98-VALIDATION.md` does NOT contain `mix verify.test` anywhere (carried forward from 102-01's swap + D-10 + Risk 2).</criterion>
    <criterion>`98-VALIDATION.md` does NOT contain a `## Authoritative-Surface Drift` section (per PATTERNS.md forbidden patterns — that section is 89-VALIDATION.md-specific).</criterion>
    <criterion>`git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/` reports zero changes (T-102-08 boundary — REQUIREMENTS.md SURF-01/02/03 rows remain `[ ]` and `Pending` per D-19; ROADMAP.md Phase 102 plan checkboxes remain `[ ]`; STATE.md untouched; UI-SPEC body untouched).</criterion>
    <criterion>`98-VERIFICATION.md` Bands 1, 2, 3 and Authority statement from Tasks 1, 2, 3 are unchanged at task end (Part A appends only the closure table and Not closed here).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -nF '## Requirement closure' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '`SURF-01` | ✓ SATISFIED' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '`SURF-02` | ✓ SATISFIED' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '`SURF-03` | ✓ SATISFIED' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF '## Not closed here' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && rg -nF 'Phase 102 closes the missing Phase 98 verification and validation chain only; milestone authority-surface reconciliation remains Phase 103 work.' .planning/phases/98-mounted-evidence-views-on-audit/98-VERIFICATION.md && grep -E '^phase: 98$' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && grep -E '^status: validated$' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && grep -E '^nyquist_compliant: true$' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && grep -E '^wave_0_complete: true$' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && grep -E '^updated: ' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && [ "$(rg -nFc '**Retroactive backfill note:**' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md)" = "1" ] && rg -nF 'reconstructed retroactively' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && rg -nF '## Commands Actually Used' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && rg -nF '## Phase Boundary Guard' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && [ "$(awk '/^## Phase Boundary Guard$/,/^## Validation Sign-Off$/' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md | grep -cE '^- ')" = "6" ] && rg -nF 'Visual hierarchy, spacing tokens, typography sizing, color palette, and scanability portions of `98-UI-SPEC.md` remain Manual-Only' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && rg -nF '**Approval:** finalized on 2026-05-27 after Phase 102-01 produced 98-VERIFICATION.md and the current-tree rerun bundle passed.' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && ! rg -n 'mix verify\.test' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && ! rg -nF '## Authoritative-Surface Drift' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && ! rg -nF '- [ ] No watch-mode flags' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/ | (! grep -E '^\s*[MADRCU?]')</automated>
  </verify>
  <done>`98-VERIFICATION.md` is complete with all five sections: frontmatter, header, `## Current-tree preflight`, three numbered bands 1:1 with SURF-01/SURF-02/SURF-03 each carrying a PASS Result + structural + behavioral evidence, `### Authority statement` listing the six authoritative commands and disclaiming `mix verify.test` per D-10, three-row `## Requirement closure` table per D-13, and `## Not closed here` with four bullets + closing line per D-20. `98-VALIDATION.md` is the Nyquist-final closure artifact: frontmatter flipped (`status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `updated:` timestamp added), opening block-quote carries the load-bearing `**Retroactive backfill note:**` block per D-14 (single match), Per-Task Verification Map statuses flipped to `✅ green`, `## Commands Actually Used` has a single entry per D-15, `## Phase Boundary Guard` has six bullets per D-11/D-20 + PATTERNS.md, Validation Sign-Off checkboxes all `[x]` (vestigial watch-mode line deleted, optional sixth phase-boundary checkbox added), Approval line rewritten to the executed-finalization pattern. `git status` confirms zero changes to REQUIREMENTS.md / ROADMAP.md / STATE.md / 98-UI-SPEC.md body / lib/ / test/ per D-19 + D-11 + D-16 + T-102-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `98-VERIFICATION.md` ↔ `98-VALIDATION.md` truth synchronization | If the validation artifact's `## Commands Actually Used` cites a different command (or a different count) than the verification artifact's Band Evidence blocks, the closure chain becomes unreliable. The same focused-bundle command must appear in both, with the literal count from 102-01-SUMMARY.md matching across both artifacts. |
| 102-01-SUMMARY.md captured stdout ↔ 102-02 artifact citations | Per Risk 5 in RESEARCH.md §8, research can predict `34 tests, 0 failures` but cannot lock the count. The executor must read 102-01-SUMMARY.md and copy the actually-observed count into the artifacts — copying from RESEARCH.md predictions would silently drift the closure record. |
| Phase 102 paperwork edits → milestone authority surfaces (REQUIREMENTS / ROADMAP / STATE / 98-UI-SPEC body) | A well-meaning checkbox flip in `.planning/REQUIREMENTS.md:22-24` or `.planning/ROADMAP.md:118-119` would pre-empt Phase 103 reconciliation and create the same audit-drift Phase 102 is meant to repair (Risk 4 + PATTERNS.md `Pattern: Milestone authority-surface boundary disclaim`). A UI-SPEC body edit would invert the design-contract relationship per D-17. T-102-08 enforces both via `git status` acceptance criteria in every task. |
| Phase 99 `mix verify.test` alias-topology scope ↔ Phase 102 authority statement | Per D-10 + Risk 2, `mix verify.test` is intentionally NOT the authority for Phase 98 because of carry-forward alias-drift (98-02-SUMMARY.md:70-74). The Authority statement must disclaim it explicitly and cite commit `b636c17` as the most recent Phase 99 fix — never re-run it as authority. |
| Retroactive-backfill honesty (D-14) ↔ merge-theater risk | Per Risk 3 + Phase 101 D-16, flipping `nyquist_compliant: false → true` without the explicit `**Retroactive backfill note:**` reads as merge-theater. The note is non-negotiable. The verify-work grep `rg -nF 'Retroactive backfill note' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` MUST return exactly one match. |
| Verification artifact shape ↔ verifier-output shape | Per RESEARCH.md §3.7 + PATTERNS.md anti-pattern, the `## Behavioral Spot-Checks` table belongs in verifier-output artifacts (e.g. the downstream `102-VERIFICATION.md` that `/gsd:verify-work` will produce after Phase 102 ships) — NOT in the phase-VERIFICATION artifact (`98-VERIFICATION.md`). Confusing the two pollutes the phase artifact with verifier conventions and creates double-evidence inflation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | mount-shape proof in `98-VERIFICATION.md` Band 1 | mitigate | Task 1 writes Band 1 citing the `live("/evidence"` mount grep at router.ex:100, the negative `def handle_event` grep against evidence_live.ex, and the LiveView focused-bundle test command. Acceptance criteria verify all three commands appear verbatim and the literal Band 1 heading matches RESEARCH.md §3.5 candidate 1. |
| T-102-02 | Tampering | EvidenceLive read-only arity citation in `98-VERIFICATION.md` Band 1 | mitigate | Task 1 Band 1 bullet 2 names `mount/3`, `handle_params/3`, `render/1` lines explicitly and pairs the negative `def handle_event` grep as executable proof. |
| T-102-03 | Spoofing | Shared-presenter parity proof in `98-VERIFICATION.md` Band 2 | mitigate | Task 2 writes Band 2 citing both the `alias Threadline.Evidence.Proof` + `Proof.present_record` greps against evidence_live.ex AND the `@semantic_statuses` grep against proof.ex (per RESEARCH.md §2.2 dual-grep nuance + Risk 6 — the verdict triple lives in proof.ex:10, not at the dynamic render site). |
| T-102-04 | Spoofing | Locked UI-SPEC copy literals in `98-VERIFICATION.md` Band 2 | mitigate | Task 2 cites all four named D-12 literals (`What can Threadline prove right now?`, `View history`, `No evidence records yet`, `Evidence view unavailable.`) plus the verdict triple alongside `98-UI-SPEC.md` per D-11 + 84-VERIFICATION.md precedent. Per D-17 + Risk 1, no UI-SPEC body edit is proposed even if a literal mismatch is found — repair source, not UI-SPEC. |
| T-102-05 | Elevation of Privilege | Fail-closed host gate proof in `98-VERIFICATION.md` Band 3 | mitigate | Task 3 writes Band 3 citing the line-254 fail-closed default verbatim and the positive-control `evidence_authorize_fn` grep PAIRED with the negative `Threadline.RBAC` grep per D-07. |
| T-102-06 | Elevation of Privilege | No Threadline-owned RBAC proof in `98-VERIFICATION.md` Band 3 | mitigate | Task 3 Band 3 cites the negative grep `rg -n 'Threadline\.RBAC\|Threadline\.Permissions\|Threadline\.Policy\.RBAC' lib/threadline/operator_surface/` PAIRED with the positive control to prevent silent-pass on path typo (D-07). |
| T-102-07 | Repudiation | Retroactive-backfill honesty in `98-VALIDATION.md` | mitigate | Task 4 inserts the load-bearing `**Retroactive backfill note:**` block per D-14 + Risk 3; acceptance criterion `rg -nFc '**Retroactive backfill note:**'` returns exactly 1 to prevent the merge-theater pattern Phase 101 D-16 warned against. |
| T-102-08 | Tampering | Milestone authority-surface boundary (REQUIREMENTS / ROADMAP / STATE / 98-UI-SPEC body / lib / test) | mitigate | Every task carries a `git status --short` acceptance criterion confirming zero changes to `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md`, `lib/`, and `test/`. Phase 103 owns the milestone reconciliation; Phase 99 + commit `b636c17` own the `mix verify.test` alias-topology. |
| T-102-09 | Repudiation | Verification-artifact shape (no Behavioral Spot-Checks table) | mitigate | Task 1 acceptance criterion `! rg -nF '## Behavioral Spot-Checks'` against `98-VERIFICATION.md` blocks the verifier-output anti-pattern per RESEARCH.md §3.7 + PATTERNS.md anti-pattern — Behavioral Spot-Checks belongs in the downstream `/gsd:verify-work` output, not the phase-VERIFICATION artifact. |
| T-102-10 | Spoofing | Validation-artifact shape (no Authoritative-Surface Drift section) | mitigate | Task 4 acceptance criterion `! rg -nF '## Authoritative-Surface Drift'` against `98-VALIDATION.md` blocks the 89-VALIDATION.md-specific section per PATTERNS.md forbidden patterns. |
| T-102-SC | Tampering | Package legitimacy | accept | No npm/pip/cargo installs in this plan — verification-backfill is paperwork-only against existing Elixir/Hex deps already vetted by Phases 95-101. Slopcheck not applicable. |
</threat_model>

<verification>
Mirror Phase 100's plan-pair shape: create `98-VERIFICATION.md` band-by-band
(Bands 1/2/3 → Authority statement → Requirement closure → Not closed here)
citing 102-01-SUMMARY.md's captured stdout for every literal test count and
grep result, then finalize `98-VALIDATION.md` (frontmatter flip + retroactive
backfill note + Per-Task Verification Map statuses + Commands Actually Used
+ Phase Boundary Guard + Validation Sign-Off + Approval line). Verify both
artifacts with literal `rg`/`grep`/`git status` assertions that confirm the
truth synchronization between them AND the absence of any
milestone-authority-surface, UI-SPEC body, or source/test edit.
</verification>

<success_criteria>
Phase 102 has both verification (`98-VERIFICATION.md`) and Nyquist closure
(`98-VALIDATION.md`) artifacts on disk. `98-VERIFICATION.md` mirrors the
`95-VERIFICATION.md` 3-band 1:1 layout with SURF-01/SURF-02/SURF-03 closure
including the Authority statement disclaiming `mix verify.test` per D-10
naming commit `b636c17`. `98-VALIDATION.md` is finalized with the load-bearing
`**Retroactive backfill note:**` block per D-14, the single-entry
`## Commands Actually Used` per D-15, the six-bullet `## Phase Boundary Guard`
per D-11/D-20 + PATTERNS.md key pattern, and the executed-finalization
`**Approval:**` line. Milestone authority surfaces (REQUIREMENTS.md /
ROADMAP.md / STATE.md) and the UI-SPEC body remain untouched per D-19 / D-11
/ T-102-08 — Phase 103 scope. No file under `lib/threadline/operator_surface/`
or `test/threadline/operator_surface/` is modified per D-16. The downstream
`/gsd:verify-work` invocation will produce its own `102-VERIFICATION.md`
(verifier output) with the Behavioral Spot-Checks table — that artifact is
downstream of Phase 102, not authored by it.
</success_criteria>

<output>
After completion, create `.planning/phases/102-phase-98-verification-backfill/102-02-SUMMARY.md` recording: (a) the verification artifact's final structure (band headings, requirement-closure row count, "Not closed here" bullet count); (b) the validation artifact's flipped frontmatter values and the verbatim Retroactive backfill note location; (c) a one-line confirmation that `git status` against the milestone authority surfaces and UI-SPEC body and lib/test trees is empty (T-102-08 enforcement); (d) the literal focused-bundle test count cited in both artifacts (must match across `98-VERIFICATION.md` Evidence blocks and `98-VALIDATION.md` `## Commands Actually Used`).
</output>
</content>
