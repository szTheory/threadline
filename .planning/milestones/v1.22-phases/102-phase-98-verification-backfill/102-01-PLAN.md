---
phase: 102-phase-98-verification-backfill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
autonomous: true
requirements:
  - SURF-01
  - SURF-02
  - SURF-03
must_haves:
  truths:
    - "The current working tree at planning time is the authority for closing SURF-01/02/03 — the focused two-file rerun bundle passes (34 tests, 0 failures) and every CONTEXT.md D-05/D-06/D-07/D-12 source citation matches the live tree before any artifact is written."
    - "The Phase 98 read-only `/audit/evidence` mount lives as a sibling inside `live_session :threadline` at `router.ex:100`; `EvidenceLive` defines only `mount/3`, `handle_params/3`, and `render/1`; the shared `Threadline.Evidence.Proof.present_record/1` presenter is the parity hinge; `evidence_authorize_fn` defaults fail-closed at `auth.ex:254`; and there is zero Threadline-owned RBAC under `lib/threadline/operator_surface/`."
    - "`98-VALIDATION.md` receives the smallest literal-truth repair allowed by D-16/D-18: the `Full suite command` line is swapped from `mix verify.test` to the focused two-file bundle, the `Quick run command` and Per-Task Verification Map command cells drop their `MIX_ENV=test` prefixes for symmetry with the finalized `95-VALIDATION.md`/`96-VALIDATION.md` analogs, and nothing else changes (no frontmatter flip, no new sections, no `## Commands Actually Used`, no retroactive-backfill note — those are 102-02 scope)."
  artifacts:
    - path: .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
      provides: "Authority-band literal-truth repair: focused bundle replaces `mix verify.test`; `MIX_ENV=test` prefix dropped for symmetry with finalized analogs"
      contains: "mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1"
  key_links:
    - from: .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
      to: .planning/phases/102-phase-98-verification-backfill/102-RESEARCH.md
      via: "the authority-band swap (line 23) MUST mirror the focused bundle named in 102-RESEARCH.md §4.3 and D-18 verbatim; no other 98-VALIDATION.md line changes in 102-01"
      pattern: "mix test test/threadline/operator_surface/auth_test\\.exs test/threadline/operator_surface/live/evidence_live_test\\.exs --max-failures 1"
    - from: lib/threadline/operator_surface/router.ex
      to: lib/threadline/operator_surface/live/evidence_live.ex
      via: "the canonical Phase 98 mount-shape proof — exactly one `live(\"/evidence\"` route at router.ex:100, inside `live_session :threadline` opened at line 89; EvidenceLive defines only mount/3 + handle_params/3 + render/1, no handle_event/3"
      pattern: "live\\(\"/evidence\"|def handle_event"
    - from: lib/threadline/operator_surface/live/evidence_live.ex
      to: lib/threadline/evidence/proof.ex
      via: "the SURF-02 parity hinge — EvidenceLive aliases Threadline.Evidence.Proof at line 8 and calls Proof.present_record/1 at line 253; the verdict triple originates from @semantic_statuses at proof.ex:10 (NOT at the dynamic render site evidence_live.ex:116-118 per RESEARCH.md §2.2 nuance)"
      pattern: "alias Threadline\\.Evidence\\.Proof|Proof\\.present_record|@semantic_statuses"
    - from: lib/threadline/operator_surface/auth.ex
      to: lib/threadline/operator_surface/
      via: "the SURF-03 boundary — host-supplied `evidence_authorize_fn` defaults to `fn _ -> false end` at auth.ex:254 (positive-control grep); zero matches for Threadline.RBAC|Threadline.Permissions|Threadline.Policy.RBAC under lib/threadline/operator_surface/ (negative grep, paired per D-07 to prevent silent-pass on path typo)"
      pattern: "evidence_authorize_fn|Threadline\\.RBAC|Threadline\\.Permissions|Threadline\\.Policy\\.RBAC"
---

<objective>
Re-verify the current-tree mounted `/audit/evidence` surface against the
Phase 98 contract (mount-shape, shared-presenter parity, fail-closed host
gate, no Threadline RBAC), capture literal stdout from the focused two-file
rerun bundle plus the six structural greps named in `102-RESEARCH.md` §5,
and apply only the smallest literal-truth repair (per D-16/D-18) to
`98-VALIDATION.md`'s authority-band lines so the validation artifact stops
naming `mix verify.test` as the Phase 98 authority.

Purpose: produce the verifiable evidence base (test counts + grep results)
that 102-02 will encode into `98-VERIFICATION.md`, and remove the one
literal-truth misalignment in `98-VALIDATION.md`'s authority band so a
reviewer running the cited Full suite command does not trip on Phase 99's
alias-drift (per D-10, owned by commit `b636c17`).

Output: live-captured ExUnit count + six grep outputs for 102-02 to cite,
plus a `98-VALIDATION.md` whose Test Infrastructure table, Sampling Rate
bullets, and Per-Task Verification Map command cells all name the focused
two-file bundle (no `mix verify.test`, no `MIX_ENV=test` prefix). No
frontmatter flip, no new sections, no retroactive-backfill note in this
plan — those are 102-02 scope per D-16 and RESEARCH.md §9 plan-boundary
split.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.22-MILESTONE-AUDIT.md
@.planning/phases/102-phase-98-verification-backfill/102-CONTEXT.md
@.planning/phases/102-phase-98-verification-backfill/102-RESEARCH.md
@.planning/phases/102-phase-98-verification-backfill/102-PATTERNS.md
@.planning/phases/102-phase-98-verification-backfill/102-VALIDATION.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-CONTEXT.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-01-SUMMARY.md
@.planning/phases/98-mounted-evidence-views-on-audit/98-02-SUMMARY.md
@.planning/phases/95-evidence-model-lock-and-scope-guard/95-VERIFICATION.md
@.planning/phases/95-evidence-model-lock-and-scope-guard/95-VALIDATION.md
@.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md
@.planning/phases/100-phase-95-verification-backfill/100-01-PLAN.md
@.planning/phases/101-phase-96-verification-backfill/101-01-PLAN.md
@lib/threadline/operator_surface/router.ex
@lib/threadline/operator_surface/live/evidence_live.ex
@lib/threadline/operator_surface/auth.ex
@lib/threadline/operator_surface/unsupported.ex
@lib/threadline/evidence/proof.ex
@test/threadline/operator_surface/auth_test.exs
@test/threadline/operator_surface/live/evidence_live_test.exs
@CLAUDE.md

<interfaces>
<!-- Current-tree fingerprint already validated at research time on HEAD 1db8e8e.
     The executor MUST re-confirm by reading the files (not by trusting research). -->

From `lib/threadline/operator_surface/router.ex` line 100 (inside `live_session :threadline` opened at line 89):
```elixir
live("/evidence", EvidenceLive, :index)
```

From `lib/threadline/operator_surface/live/evidence_live.ex`:
```elixir
# line 8
alias Threadline.Evidence.Proof
# line 12: def mount(_params, session, socket)
# line 21: def handle_params(params, _uri, socket)
# line 49: def render(assigns)
# line 67: <h2>What can Threadline prove right now?</h2>
# line 89: <h3>No evidence records yet</h3>
# line 116-118: dynamic <span class={…"evidence-verdict--#{row.verdict_status}"}>{row.verdict_status}</span>
# line 142: View history
# line 154: Unsupported.descriptor(:evidence_unavailable)
# line 253: presented = Proof.present_record(record)
# NO def handle_event/3 — confirmed by `rg -n '^\s*def handle_event' …` exit 1
```

From `lib/threadline/operator_surface/auth.ex`:
```elixir
# line 253-254
defp assign_evidence_enabled(socket, opts) do
  evidence_authorize_fn = Keyword.get(opts, :evidence_authorize_fn, fn _ -> false end)
```

From `lib/threadline/operator_surface/unsupported.ex` line 25 (the `:evidence_unavailable` descriptor body):
```elixir
"Evidence view unavailable. This mounted proof surface is not available…"
```

From `lib/threadline/evidence/proof.ex` line 10 (the canonical verdict-vocabulary source — per RESEARCH.md §2.2 Band 2 dual-grep nuance):
```elixir
@semantic_statuses ~w(proven inferred_posture unsupported)
```

From `.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` (the literal-truth-repair sites for 102-01):
```text
# line 22 (current):
| **Quick run command** | `MIX_ENV=test mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` |
# line 23 (current — the canonical D-18 swap site):
| **Full suite command** | `mix verify.test` |
# line 41/42/43 (current — Per-Task Verification Map Automated Command cells with the same prefix):
| `MIX_ENV=test mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` |
| `MIX_ENV=test mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` |
| `MIX_ENV=test mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` |
```

From `.planning/REQUIREMENTS.md` lines 22-24 + Traceability lines 60-62 (the requirement rows 102-02 closure table cites; 102-01 MUST NOT edit these per D-19):
```text
- [ ] **SURF-01**: Read-only evidence views live on the existing `/audit` surface rather than a new operator UI family.
- [ ] **SURF-02**: Mounted evidence views show the same evidence facts and boundary language as the library API and Mix-task paths.
- [ ] **SURF-03**: Host-owned authorization remains the gate for mounted evidence views; Threadline does not introduce RBAC or tenant DSL semantics.
| SURF-01 | Phase 102 | Pending |
| SURF-02 | Phase 102 | Pending |
| SURF-03 | Phase 102 | Pending |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 102-01-01: Re-verify the current-tree fingerprint and capture literal stdout for the focused rerun bundle plus six structural greps</name>
  <files>(read-only verification — no source/test file is modified)</files>
  <read_first>
    <file>.planning/phases/102-phase-98-verification-backfill/102-CONTEXT.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-RESEARCH.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-PATTERNS.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-CONTEXT.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-01-SUMMARY.md</file>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-02-SUMMARY.md</file>
    <file>lib/threadline/operator_surface/router.ex</file>
    <file>lib/threadline/operator_surface/live/evidence_live.ex</file>
    <file>lib/threadline/operator_surface/auth.ex</file>
    <file>lib/threadline/operator_surface/unsupported.ex</file>
    <file>lib/threadline/evidence/proof.ex</file>
    <file>test/threadline/operator_surface/auth_test.exs</file>
    <file>test/threadline/operator_surface/live/evidence_live_test.exs</file>
  </read_first>
  <action>Treat the current working tree (HEAD) as authority per D-02 and RESEARCH.md §1. Re-confirm every CONTEXT.md citation by reading the seven source/test files listed in `<read_first>` once (no re-reads — extract everything in one pass per the critical rules). Then execute the focused two-file rerun bundle EXACTLY as named in CONTEXT.md D-08 / RESEARCH.md §5 — do NOT add `MIX_ENV=test`, do NOT widen to `mix verify.test` (per D-10 and Risk 2 in RESEARCH.md §8 — Phase 99 owns alias-topology; commit `b636c17` is the most recent fix on that surface): `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1`. Capture the literal ExUnit count line printed (expected `34 tests, 0 failures` per RESEARCH.md §2.4 and §6 — but record what is actually printed, NOT what research predicted, per Risk 5 in RESEARCH.md §8). Also execute `mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` (LiveView-only single-file run; expected `5 tests, 0 failures`) and capture the literal `N tests, M failures` line in 102-01-SUMMARY.md so 102-02 Bands 1 and 2 Evidence blocks can cite the per-file count verbatim without re-running. Next execute the six structural greps from RESEARCH.md §5 verbatim and capture each literal stdout (these become Band 1/2/3 Evidence blocks in 102-02): (1) Band 1 mount-shape: `rg -n 'live\("/evidence"' lib/threadline/operator_surface/router.ex` — expect exactly one match at line 100, inside the `live_session :threadline` block opened at line 89. (2) Band 1 read-only: `rg -n '^\s*def handle_event' lib/threadline/operator_surface/live/evidence_live.ex` — expect exit code 1 / zero matches (negative assertion proving no mutation handlers). (3) Band 2 presenter wiring: `rg -n 'alias Threadline\.Evidence\.Proof|Proof\.present_record' lib/threadline/operator_surface/live/evidence_live.ex` — expect exactly two matches at lines 8 + 253. (4) Band 2 canonical verdict source (per RESEARCH.md §2.2 dual-grep nuance — the verdict triple lives in proof.ex:10, NOT at the dynamic render site evidence_live.ex:116-118): `rg -n '@semantic_statuses' lib/threadline/evidence/proof.ex` — expect exactly one match at line 10. (5) Band 3 negative (no Threadline-owned RBAC): `rg -n 'Threadline\.RBAC|Threadline\.Permissions|Threadline\.Policy\.RBAC' lib/threadline/operator_surface/` — expect exit code 1 / zero matches. (6) Band 3 positive control (paired per D-07 to prevent silent-pass on path typo): `rg -n 'evidence_authorize_fn' lib/threadline/operator_surface/auth.ex` — expect 5 matches starting at line 254 (the canonical `Keyword.get(opts, :evidence_authorize_fn, fn _ -> false end)` fail-closed default). Additionally execute the five `rg -nF` locked-literal greps from RESEARCH.md §5.2 to confirm the D-12 inventory (each literal is paired with both a source location AND a test assertion): `What can Threadline prove right now?` (source `evidence_live.ex:67` + test `evidence_live_test.exs:115,150`); `View history` (source `evidence_live.ex:142` + test `evidence_live_test.exs:152`); `No evidence records yet` (source `evidence_live.ex:89` + test `evidence_live_test.exs:213`); `Evidence view unavailable.` (source `unsupported.ex:25` rendered via `evidence_live.ex:154` + test `evidence_live_test.exs:113`); verdict triple via the assertions at `evidence_live_test.exs:153-155`. If ANY citation drifts (test line moved, source line moved, literal renamed, helper renamed), do NOT modify source/test files in this task — surface the drift as a blocking finding so 102-02 can decide between the smallest literal-truth repair (per D-16/D-17 — repair source to match UI-SPEC, NOT UI-SPEC to match source) and escalation. Per D-19 / Risk 4 / `T-102-08`, this task MUST NOT modify `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md` body, or any file under `lib/threadline/operator_surface/`, `lib/threadline/evidence/`, or `test/threadline/operator_surface/` (D-16 forbids new tests, source edits, and UI-SPEC edits — current tree is the authority).</action>
  <acceptance_criteria>
    <criterion>`mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` exits 0 and prints `34 tests, 0 failures` (or the literal count actually observed — captured verbatim for 102-02 to cite per Risk 5).</criterion>
    <criterion>`mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` exits 0 and prints `5 tests, 0 failures` (or the literal count actually observed — captured verbatim in 102-01-SUMMARY.md so 102-02 Bands 1 and 2 Evidence blocks can cite the per-file LiveView-only count).</criterion>
    <criterion>`rg -n 'live\("/evidence"' lib/threadline/operator_surface/router.ex` returns exactly one match: `100:            live("/evidence", EvidenceLive, :index)`.</criterion>
    <criterion>`rg -n '^\s*def handle_event' lib/threadline/operator_surface/live/evidence_live.ex` exits 1 with zero matches (negative assertion — no mutation handlers).</criterion>
    <criterion>`rg -n 'alias Threadline\.Evidence\.Proof|Proof\.present_record' lib/threadline/operator_surface/live/evidence_live.ex` returns exactly two matches: line 8 (`alias Threadline.Evidence.Proof`) and line 253 (`Proof.present_record(record)`).</criterion>
    <criterion>`rg -n '@semantic_statuses' lib/threadline/evidence/proof.ex` returns exactly one match at line 10 (`@semantic_statuses ~w(proven inferred_posture unsupported)`).</criterion>
    <criterion>`rg -n 'Threadline\.RBAC|Threadline\.Permissions|Threadline\.Policy\.RBAC' lib/threadline/operator_surface/` exits 1 with zero matches (negative assertion — no Threadline-owned RBAC under the operator surface).</criterion>
    <criterion>`rg -n 'evidence_authorize_fn' lib/threadline/operator_surface/auth.ex` returns 5 matches including the line-254 fail-closed default `Keyword.get(opts, :evidence_authorize_fn, fn _ -> false end)` (positive-control PAIRED with the negative grep above per D-07).</criterion>
    <criterion>All five locked UI-SPEC copy literals from D-12 are present at the cited source AND test lines (verified by the five `rg -nF` commands from RESEARCH.md §5.2); no source/test file modification is required.</criterion>
    <criterion>`git status --short lib/ test/ .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md` reports zero changes (T-102-08 boundary; only `.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` is allowed to change in 102-01, and that change is the Task 2 scope).</criterion>
    <criterion>The literal stdout for the focused bundle command and all six structural greps is captured for 102-02 to cite verbatim in `98-VERIFICATION.md` Evidence blocks (per RESEARCH.md §9 plan-boundary split and Risk 5).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1 && mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1 && rg -n 'live\("/evidence"' lib/threadline/operator_surface/router.ex | grep -F '100:            live("/evidence", EvidenceLive, :index)' && ! rg -n '^\s*def handle_event' lib/threadline/operator_surface/live/evidence_live.ex && [ "$(rg -nc 'alias Threadline\.Evidence\.Proof|Proof\.present_record' lib/threadline/operator_surface/live/evidence_live.ex)" = "2" ] && rg -n '@semantic_statuses' lib/threadline/evidence/proof.ex | grep -F '10:  @semantic_statuses ~w(proven inferred_posture unsupported)' && ! rg -n 'Threadline\.RBAC|Threadline\.Permissions|Threadline\.Policy\.RBAC' lib/threadline/operator_surface/ && rg -n 'evidence_authorize_fn' lib/threadline/operator_surface/auth.ex | grep -F 'Keyword.get(opts, :evidence_authorize_fn, fn _ -> false end)' && git status --short lib/ test/ .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md | (! grep -E '^\s*[MADRCU?]')</automated>
  </verify>
  <done>Focused rerun bundle passes on the current tree (literal count captured verbatim), the per-file LiveView-only run `mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` passes with its literal `N tests, M failures` line captured for 102-02 Bands 1 and 2 to cite, all six RESEARCH.md §5 structural greps return their expected results, all five D-12 UI-SPEC locked literals are confirmed present at the cited source and test lines, no source/test/UI-SPEC/REQUIREMENTS/ROADMAP/STATE file has been modified, and the literal stdout for every command is captured for 102-02 to cite in `98-VERIFICATION.md` Band 1/2/3 Evidence blocks per RESEARCH.md §9 plan-boundary split.</done>
</task>

<task type="auto">
  <name>Task 102-01-02: Apply the smallest literal-truth repair to 98-VALIDATION.md authority-band lines (Quick/Full run swap + drop MIX_ENV=test prefix)</name>
  <files>.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-RESEARCH.md</file>
    <file>.planning/phases/102-phase-98-verification-backfill/102-PATTERNS.md</file>
    <file>.planning/phases/95-evidence-model-lock-and-scope-guard/95-VALIDATION.md</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-VALIDATION.md</file>
  </read_first>
  <action>Edit `.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` in place. Apply ONLY the literal-truth repairs allowed by D-16/D-18 and enumerated in RESEARCH.md §4.3 + §4.4; nothing else changes in this task. (1) **Line 22 Quick run command:** drop the `MIX_ENV=test` prefix so the cell value becomes the verbatim command `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` (per RESEARCH.md §4.3 row 1 — symmetry with finalized `95-VALIDATION.md:25` and `96-VALIDATION.md:21`; this is cosmetic literal-truth alignment, not a behavioral change). (2) **Line 23 Full suite command — the canonical D-18 swap:** replace `mix verify.test` with the same focused-bundle command `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` (per D-08 the focused bundle IS the authority; per D-10 / Risk 2 in RESEARCH.md §8, `mix verify.test` is intentionally NOT the authority because it carries Phase 99-owned alias-drift outside Phase 98 scope — commit `b636c17` is the most recent fix on that surface and Phase 102 disclaims rather than reopens it). (3) **Line 24 Estimated runtime:** replace `~45 seconds` with `~10-30 seconds warm` (per RESEARCH.md §4.3 row 3 — live measurement at research time was 0.2s; the `~10-30 seconds warm` range mirrors `96-VALIDATION.md:24` and accounts for cold-start variation). (4) **Line 31 Sampling Rate (`After every plan wave:`):** replace `mix verify.test` with the focused-bundle command `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` so the sampling-rate bullets stop directing the reader to the disclaimed alias (per CONTEXT.md D-10 + RESEARCH.md §4.3 + PATTERNS.md `## Sampling Rate` "ensure the `Before $gsd-verify-work` and `After every plan wave` bullets are updated to reference the focused bundle, not `mix verify.test`"). (5) **Per-Task Verification Map command cells (lines 41-43):** drop the `MIX_ENV=test` prefix from all three Automated Command cells so each cell reads `mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` (rows 41 + 42) or `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` (row 43) — per RESEARCH.md §4.4 same-symmetry rationale; do NOT change any other column in these rows (do not flip the Status column from `⬜ pending` to `✅ green` — that is 102-02 scope per RESEARCH.md §9 / §4.4 and Task 102-02-04). DO NOT change frontmatter (`status: draft` stays draft in 102-01; the flip to `validated` is 102-02 scope per D-14 + RESEARCH.md §9). DO NOT add `## Commands Actually Used`, `## Phase Boundary Guard`, or the `**Retroactive backfill note:**` block — those are 102-02 scope per D-14/D-15 and RESEARCH.md §9. DO NOT modify the Wave 0 Requirements checkboxes (lines 50-52), the Manual-Only Verifications table (lines 56-65), or the Validation Sign-Off section (lines 69-77) — all 102-02 scope per RESEARCH.md §4.7/§4.8. Per D-19 / Risk 4 / `T-102-08`, this task MUST NOT modify `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, or `.planning/STATE.md`.</action>
  <acceptance_criteria>
    <criterion>`.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` no longer contains the literal `mix verify.test` anywhere (per D-18 + Risk 2 + PATTERNS.md `## Sampling Rate`).</criterion>
    <criterion>`.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` no longer contains the literal `MIX_ENV=test` anywhere (per RESEARCH.md §4.3 row 1 + §4.4 — symmetry with finalized 95/96 analogs).</criterion>
    <criterion>`.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` line 23 (Full suite command cell) names the focused bundle verbatim: `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` (per D-08 + D-18).</criterion>
    <criterion>`.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` line 24 (Estimated runtime) reads `~10-30 seconds warm` (per RESEARCH.md §4.3 row 3).</criterion>
    <criterion>`.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` frontmatter STILL reads `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false` at task end (the flip is 102-02 scope per D-14 + RESEARCH.md §9; 102-01 makes ONLY the literal-truth repair).</criterion>
    <criterion>`.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` does NOT yet contain `## Commands Actually Used`, `## Phase Boundary Guard`, or the literal phrase `Retroactive backfill note` (all 102-02 scope per D-14/D-15 + RESEARCH.md §9).</criterion>
    <criterion>The Per-Task Verification Map Status column for all three rows (lines 41-43) still reads `⬜ pending` (the flip to `✅ green` is 102-02 scope per RESEARCH.md §4.4).</criterion>
    <criterion>`git diff --stat .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` shows ONLY the five literal-truth-repair lines changed (Quick run command line, Full suite command line, Estimated runtime line, Sampling Rate `After every plan wave:` line, and the three Per-Task Verification Map Automated Command cells); no other line in the file has been modified.</criterion>
    <criterion>`git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/` reports zero changes (T-102-08 boundary; only `98-VALIDATION.md` is allowed to change in this entire plan).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>! rg -n 'mix verify\.test' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && ! rg -n 'MIX_ENV=test' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && rg -nF 'mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && rg -nF '~10-30 seconds warm' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && grep -E '^status: draft$' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && grep -E '^nyquist_compliant: false$' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && grep -E '^wave_0_complete: false$' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && ! rg -n '## Commands Actually Used|## Phase Boundary Guard|Retroactive backfill note' .planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md && git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md lib/ test/ | (! grep -E '^\s*[MADRCU?]')</automated>
  </verify>
  <done>`98-VALIDATION.md` carries the D-16/D-18 smallest-literal-truth repair: the Full suite command names the focused two-file bundle instead of `mix verify.test`; the Quick run command, all three Per-Task Verification Map Automated Command cells, and the `After every plan wave:` Sampling Rate bullet drop the `MIX_ENV=test` prefix; the Estimated runtime cell shows `~10-30 seconds warm`. Frontmatter still reads `status: draft` / `nyquist_compliant: false` / `wave_0_complete: false`; no new sections, no retroactive-backfill note, no Status-column flip; nothing else in the file has been touched (102-02 owns all those changes per RESEARCH.md §9). No milestone-authority surface (REQUIREMENTS.md / ROADMAP.md / STATE.md / 98-UI-SPEC.md) has been modified per D-19 / Risk 4 / T-102-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 98 implementation tree → Phase 102 paperwork artifacts | 102-01 must verify what is on disk now, not inherit Phase 98 summary claims automatically. If any source/test/UI-SPEC literal has drifted since CONTEXT.md was written, the planner must surface it as a blocking finding rather than write a false claim into `98-VERIFICATION.md` (Risk 1, Risk 5, Risk 6 per RESEARCH.md §8). |
| `mix verify.test` alias-topology (Phase 99-owned, fixed by commit `b636c17`) → Phase 102 authority scope | If any line in `98-VALIDATION.md` continues to name `mix verify.test` as authority, Phase 99 alias-drift leaks back into the Phase 98 closure chain and a reviewer running the cited Full suite command trips on an unrelated `Threadline.CiTopologyContractTest` failure (98-02-SUMMARY.md:70-74). The D-18 swap (Task 2) is the single line of defense. |
| Phase 102 paperwork edits → milestone authority surfaces (REQUIREMENTS / ROADMAP / STATE / 98-UI-SPEC) | A well-meaning checkbox flip in REQUIREMENTS.md / ROADMAP.md / STATE.md would pre-empt Phase 103 reconciliation and create the same audit-drift Phase 102 is meant to repair (Risk 4 per RESEARCH.md §8 + Phase 101 D-17). A UI-SPEC body edit would invert the design-contract relationship (D-17 says repair source to match UI-SPEC, never UI-SPEC to match source). T-102-08 enforces both. |
| `lib/threadline/operator_surface/` source tree → Phase 102 scope | 102 is verification-backfill only (D-16); any source/test edit other than a forced literal-truth repair under D-17 widens scope. No source/test edit is expected in 102-01 because RESEARCH.md §2 confirmed every CONTEXT.md citation matches the live tree on HEAD `1db8e8e`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | mount-shape (`router.ex` / `EvidenceLive` read-only handler set) | mitigate | Task 1 executes `rg -n 'live\("/evidence"' lib/threadline/operator_surface/router.ex` (expect 1 match at line 100, inside `live_session :threadline`) AND `rg -n '^\s*def handle_event' lib/threadline/operator_surface/live/evidence_live.ex` (expect exit 1 / zero matches); both commands and their literal stdout are captured for 102-02 to cite in `98-VERIFICATION.md` Band 1 Evidence block. |
| T-102-02 | Tampering | EvidenceLive read-only arity (`mount/3`, `handle_params/3`, `render/1` only) | mitigate | Task 1 reads `lib/threadline/operator_surface/live/evidence_live.ex` once and confirms only those three callbacks are defined (the negative-`handle_event` grep above is the executable proof; the arity citation is inline prose in the Band 1 bullet list). |
| T-102-03 | Spoofing | Shared-presenter parity (`Threadline.Evidence.Proof.present_record/1`) | mitigate | Task 1 executes `rg -n 'alias Threadline\.Evidence\.Proof\|Proof\.present_record' lib/threadline/operator_surface/live/evidence_live.ex` (expect 2 matches at lines 8 + 253) AND `rg -n '@semantic_statuses' lib/threadline/evidence/proof.ex` (expect 1 match at line 10 — the canonical verdict-vocabulary source per RESEARCH.md §2.2 nuance). Band 2 in 102-02 MUST cite both because the verdict triple at `evidence_live.ex:116-118` is the dynamic render site, not the literal-defining site (Risk 6). |
| T-102-04 | Spoofing | Locked UI-SPEC copy literals (D-12 five-row inventory) | mitigate | Task 1 executes the five `rg -nF` greps from RESEARCH.md §5.2 against `evidence_live.ex` / `unsupported.ex` and the test file, confirming each literal is present at the cited source location AND asserted at the cited test line. Per D-17, if drift exists the smallest repair is to update the SOURCE to render the UI-SPEC's locked literal — NOT to update the UI-SPEC; in that case Task 1 surfaces the drift as a blocking finding so 102-02 can decide. |
| T-102-05 | Elevation of Privilege | Fail-closed host gate (`evidence_authorize_fn` defaults to `fn _ -> false end`) | mitigate | Task 1 executes the positive-control grep `rg -n 'evidence_authorize_fn' lib/threadline/operator_surface/auth.ex` (expect 5 matches including the line-254 default) PAIRED with the negative grep T-102-06 so a path typo would fail loudly (D-07). The 6 unit tests in `describe "assign_evidence_enabled"` at `auth_test.exs:337-394` are captured by the focused bundle's behavioral run. |
| T-102-06 | Elevation of Privilege | No Threadline-owned RBAC under `lib/threadline/operator_surface/` | mitigate | Task 1 executes `rg -n 'Threadline\.RBAC\|Threadline\.Permissions\|Threadline\.Policy\.RBAC' lib/threadline/operator_surface/` (expect exit 1 / zero matches) and PAIRS this negative grep with T-102-05 per D-07 so silent-pass on path typo is impossible. |
| T-102-08 | Tampering | Milestone authority-surface boundary (REQUIREMENTS / ROADMAP / STATE / 98-UI-SPEC body) | mitigate | Both Task 1 and Task 2 carry a `git status --short` acceptance criterion confirming zero changes to `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md`, `lib/`, and `test/`; only `.planning/phases/98-mounted-evidence-views-on-audit/98-VALIDATION.md` is allowed to change in this plan. Phase 103 owns the milestone reconciliation; Phase 99 + commit `b636c17` own the `mix verify.test` alias-topology that the D-18 swap disclaims. |
| T-102-SC | Tampering | Package legitimacy | accept | No npm/pip/cargo installs in this plan — verification-backfill is paperwork-only against existing Elixir/Hex deps already vetted by Phases 95-101. Slopcheck not applicable. |
</threat_model>

<verification>
Execute the focused two-file rerun bundle and the six structural greps from
`102-RESEARCH.md` §5 to re-prove the SURF-01/02/03 surfaces on the current
tree, then apply the D-16/D-18 smallest literal-truth repair to
`98-VALIDATION.md`'s authority-band lines. The captured stdout from Task 1
becomes the Evidence-block source material 102-02 cites in `98-VERIFICATION.md`
Bands 1/2/3.
</verification>

<success_criteria>
102-01 produces (a) live-captured stdout for the focused two-file bundle
(expected `34 tests, 0 failures` — actual count recorded verbatim) and all
six RESEARCH.md §5 structural greps (mount, no-handler, presenter, canonical
verdict source, negative RBAC, positive auth gate) plus the five D-12 locked
copy-literal greps; and (b) a `98-VALIDATION.md` whose Test Infrastructure
table, Sampling Rate bullets, and Per-Task Verification Map command cells all
name the focused two-file bundle (no `mix verify.test`, no `MIX_ENV=test`),
with the frontmatter still reading `status: draft` / `nyquist_compliant: false`
/ `wave_0_complete: false`. Milestone authority surfaces
(`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`,
`.planning/phases/98-mounted-evidence-views-on-audit/98-UI-SPEC.md` body) are
unchanged (T-102-08 boundary; Phase 103 scope). No source/test file under
`lib/threadline/operator_surface/` or `test/threadline/operator_surface/` has
been modified (D-16 boundary).
</success_criteria>

<output>
After completion, create `.planning/phases/102-phase-98-verification-backfill/102-01-SUMMARY.md` recording the literal stdout for the focused-bundle test command, the per-file LiveView-only test count (`mix test test/threadline/operator_surface/live/evidence_live_test.exs --max-failures 1` — for 102-02 Bands 1 and 2 to cite without re-running), the six structural greps from RESEARCH.md §5, and the five D-12 locked-literal greps from RESEARCH.md §5.2. This summary becomes 102-02's evidence source (per RESEARCH.md §9 plan-boundary split — 102-02 mirrors 102-01's captured output verbatim into `98-VERIFICATION.md` Band Evidence blocks; do not let 102-02 re-derive counts from RESEARCH.md per Risk 5).
</output>
</content>
