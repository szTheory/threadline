---
phase: 52-docs-and-contract-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/getting-started-saas.md
  - guides/integrations/sigra.md
  - guides/domain-reference.md
  - guides/incident-playbook.md
  - guides/adoption-pilot-backlog.md
  - examples/threadline_phoenix/README.md
autonomous: true
requirements:
  - ADOPT-03
must_haves:
  truths:
    - "Getting-started, Sigra, domain-reference, incident-playbook, adoption-backlog, and example README docs all teach one native host-wiring story."
    - "Every touched doc says `actor_fn` owns identity, `context_overrides_fn` is additive `request_id` / `correlation_id` metadata only, and the incident drill-down example requires an authenticated actor."
    - "No touched doc claims Threadline ships tenancy, membership, or richer authorization policy; those remain host-owned everywhere."
  artifacts:
    - path: "guides/getting-started-saas.md"
      provides: "Copy-paste adopter onboarding with canonical router wiring and the authenticated incident baseline"
    - path: "guides/integrations/sigra.md"
      provides: "Authoritative direct Sigra callback vocabulary and additive-only override semantics"
    - path: "guides/domain-reference.md"
      provides: "Public incident JSON contract marker aligned with the authenticated baseline and host-owned authz boundary"
    - path: "guides/incident-playbook.md"
      provides: "Operator-facing incident narrative that describes the shipped auth minimum without over-claiming policy scope"
    - path: "guides/adoption-pilot-backlog.md"
      provides: "Adoption evidence row that accurately reflects CI proof versus host-owned authorization work"
    - path: "examples/threadline_phoenix/README.md"
      provides: "Runnable example README aligned with the router/controller truth for host wiring and incident security"
  key_links:
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      to: "guides/getting-started-saas.md"
      via: "direct callback snippet and vocabulary"
      pattern: "actor_fn: &Threadline.Integrations.Sigra.actor_ref_from_conn/1"
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      to: "guides/integrations/sigra.md"
      via: "canonical router pipeline contract"
      pattern: "context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1"
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex"
      to: "guides/domain-reference.md"
      via: "authenticated incident baseline phrased in normalized Threadline terms"
      pattern: "authenticated actor"
    - from: "guides/getting-started-saas.md"
      to: "examples/threadline_phoenix/README.md"
      via: "shared host-wiring and incident-boundary wording"
      pattern: "actor_fn"
---

<objective>
Converge the adopter-facing docs on one truthful v1.15 host-integration story before any new contract locks are added.

Purpose: satisfy ADOPT-03 per D-01 through D-07 and D-14 by making the public docs say the same thing about native host wiring and the authenticated incident baseline, without reopening runtime scope.
Output: updated guide and README literals that match the current router/controller behavior and preserve host-owned authz/tenancy boundaries in the dirty v1.15 worktree.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.15-phases/52-docs-and-contract-alignment/52-CONTEXT.md
@.planning/milestones/v1.15-phases/52-docs-and-contract-alignment/52-PATTERNS.md
@.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-CONTEXT.md
@.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-CONTEXT.md
@.planning/milestones/v1.15-phases/51-authenticated-incident-drill-down/51-CONTEXT.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex
@guides/getting-started-saas.md
@guides/integrations/sigra.md
@guides/domain-reference.md
@guides/incident-playbook.md
@guides/adoption-pilot-backlog.md
@examples/threadline_phoenix/README.md

<interfaces>
From examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex:
```elixir
plug(Threadline.Plug,
  actor_fn: &Threadline.Integrations.Sigra.actor_ref_from_conn/1,
  context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1
)
```

From examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex:
```elixir
**Reference-app contract:** requests must arrive with an authenticated actor.
Real hosts still own their tenancy and policy rules.
```

From guides/getting-started-saas.md and guides/integrations/sigra.md:
```markdown
`actor_fn` remains the only actor-authority path.
`context_overrides_fn` is for additive `request_id` and `correlation_id` metadata only.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the canonical host-wiring docs with the direct callback contract</name>
  <read_first>.planning/milestones/v1.15-phases/52-docs-and-contract-alignment/52-CONTEXT.md, .planning/milestones/v1.15-phases/52-docs-and-contract-alignment/52-PATTERNS.md, .planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-CONTEXT.md, .planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-CONTEXT.md, guides/getting-started-saas.md, guides/integrations/sigra.md, examples/threadline_phoenix/README.md, examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</read_first>
  <files>guides/getting-started-saas.md, guides/integrations/sigra.md, examples/threadline_phoenix/README.md</files>
  <action>Read the live dirty file state first and preserve any unrelated in-flight edits; do not reset or rewrite from a clean baseline. Update these three adopter entry points per D-01 through D-06 and D-14 so they all use the same canonical router vocabulary from `router.ex`: `Threadline.Plug` wired directly with `actor_fn: &Threadline.Integrations.Sigra.actor_ref_from_conn/1` and `context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1`. Make each surface state concretely that `actor_fn` decides identity, `context_overrides_fn` is additive `request_id` / `correlation_id` metadata only, and existing inbound/request-derived values stay authoritative. Keep proxy/IP normalization explicitly upstream and host-owned. In the README and quickstart incident sections, keep the authenticated incident baseline line present and normalized around an authenticated actor rather than Sigra-private request fields. Do not add new runtime instructions, new callback names, delegate seams, or tenancy walkthroughs.</action>
  <verify>
    <automated>rg -n "Threadline\\.Integrations\\.Sigra\\.actor_ref_from_conn/1|Threadline\\.Integrations\\.Sigra\\.audit_context_overrides_from_conn/1|actor-authority|authenticated actor|tenancy|authorization" guides/getting-started-saas.md guides/integrations/sigra.md examples/threadline_phoenix/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/getting-started-saas.md` contains both canonical Sigra callback names and states that `context_overrides_fn` is for additive `request_id` and `correlation_id` metadata only.
    - `guides/integrations/sigra.md` preserves the direct router snippet and still states that `actor_fn` decides who acted while `context_overrides_fn` is not a second actor path.
    - `examples/threadline_phoenix/README.md` says the callbacks are wired directly into `Threadline.Plug` and keeps the incident section's authenticated-actor boundary.
    - None of the three files introduces an alternate blessed wiring seam or implies `context_overrides_fn` can replace actor identity.
  </acceptance_criteria>
  <done>The highest-risk copy-paste docs all teach the same direct host-wiring contract and preserve the incident auth baseline without contradicting the live router.</done>
</task>

<task type="auto">
  <name>Task 2: Align the incident/reference docs and adoption evidence with the same host-owned boundary story</name>
  <read_first>.planning/milestones/v1.15-phases/52-docs-and-contract-alignment/52-CONTEXT.md, .planning/milestones/v1.15-phases/52-docs-and-contract-alignment/52-PATTERNS.md, .planning/milestones/v1.15-phases/51-authenticated-incident-drill-down/51-CONTEXT.md, guides/domain-reference.md, guides/incident-playbook.md, guides/adoption-pilot-backlog.md, examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex, examples/threadline_phoenix/README.md</read_first>
  <files>guides/domain-reference.md, guides/incident-playbook.md, guides/adoption-pilot-backlog.md</files>
  <action>Read the current dirty file contents and layer edits onto the in-flight wording; do not discard or normalize unrelated changes. Update the reference/operator/adoption surfaces per D-02, D-06, D-07, and D-11 through D-13 so they repeat the same honest line: the shipped reference path requires an authenticated actor before serving `GET /api/audit_transactions/:id/changes`, while tenancy scoping, membership checks, concealment choices, and richer authorization remain host-owned. Preserve each surface's existing contract structure from 52-PATTERNS: keep `COMP-EXAMPLE-INCIDENT-JSON` and its numbered block in `guides/domain-reference.md`, keep the incident playbook's scenario layout, and keep the adoption backlog evidence-row format and CI-class framing. If these docs mention host wiring at all, use the same native callback story from Task 1 instead of inventing alternate naming.</action>
  <verify>
    <automated>rg -n "COMP-EXAMPLE-INCIDENT-JSON|authenticated actor|tenancy|authorization|Host teams still own tenancy and richer authorization review" guides/domain-reference.md guides/incident-playbook.md guides/adoption-pilot-backlog.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/domain-reference.md` keeps the `COMP-EXAMPLE-INCIDENT-JSON` marker and states that the reference app requires an authenticated actor before drill-down.
    - `guides/incident-playbook.md` keeps the opening auth-baseline paragraph and still frames tenancy/policy as host-owned follow-on work.
    - `guides/adoption-pilot-backlog.md` keeps the incident evidence row and notes that host teams still own tenancy and richer authorization review.
    - None of these files claims Threadline ships full authorization policy or a tenancy framework.
  </acceptance_criteria>
  <done>The domain reference, incident playbook, and adoption backlog all match the shipped incident baseline and the host-owned policy boundary reflected by the live controller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> adopters | External teams may copy the host-wiring and incident-security story directly into production apps. |
| router/controller truth -> docs | If docs drift from the live example wiring or auth baseline, adopters receive false integration guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | S | host-wiring docs | mitigate | Use the exact canonical callback names from `router.ex` per D-03 so docs cannot bless an app-local or fake integration seam. |
| T-52-02 | T | incident/reference guides | mitigate | Repeat the same authenticated-baseline/host-owned-policy line across all touched docs per D-07 and D-10. |
| T-52-03 | I | public security narrative | mitigate | State explicitly that tenancy, membership checks, and richer authorization remain host-owned to avoid over-claiming the shipped security model. |
| T-52-04 | E | docs phase scope | mitigate | Keep edits documentation-only per D-01 and avoid new runtime or authorization features. |
</threat_model>

<verification>
Run:
- `rg -n "Threadline\\.Integrations\\.Sigra\\.actor_ref_from_conn/1|Threadline\\.Integrations\\.Sigra\\.audit_context_overrides_from_conn/1|actor-authority|authenticated actor|tenancy|authorization" guides/getting-started-saas.md guides/integrations/sigra.md examples/threadline_phoenix/README.md`
- `rg -n "COMP-EXAMPLE-INCIDENT-JSON|authenticated actor|tenancy|authorization|Host teams still own tenancy and richer authorization review" guides/domain-reference.md guides/incident-playbook.md guides/adoption-pilot-backlog.md`
</verification>

<success_criteria>
- All six touched docs describe one native host-wiring story using the canonical Sigra callback names.
- All incident-facing surfaces state that the example ships an authenticated baseline only and leave tenancy/richer authorization host-owned.
- The docs remain structurally compatible with the existing narrow doc-contract test style rather than moving to snapshots or broad editorial rewrites.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.15-phases/52-docs-and-contract-alignment/52-01-SUMMARY.md`
</output>
