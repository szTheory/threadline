---
phase: 50-direct-sigra-host-wiring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/audit_actor.ex
  - lib/threadline/integrations/sigra.ex
  - test/threadline/integrations/sigra_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix_web/posts_audit_path_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs
autonomous: true
requirements:
  - SIGRA-04
  - SIGRA-05
must_haves:
  truths:
    - "The shipped Sigra adapter remains the single canonical direct callback path into `Threadline.Plug`."
    - "The example app does not preserve an example-only Sigra rename seam or companion pre-plug."
    - "Adapter-level tests keep the richer Sigra semantics, while example request-path tests prove the shortest honest router-driven golden path."
  artifacts:
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      provides: "Visible direct callback wiring point in the Phoenix `:api` pipeline"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix/audit_actor.ex"
      provides: "Removed or justified example-local delegate seam"
    - path: "test/threadline/integrations/sigra_test.exs"
      provides: "Library-level proof for direct callback composition and soft-dependency-safe behavior"
    - path: "examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs"
      provides: "Router-driven proof that Sigra fallback correlation wiring still works when the header is absent"
  key_links:
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      to: "test/threadline/integrations/sigra_test.exs"
      via: "same direct callback pair"
      pattern: "context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1"
    - from: "examples/threadline_phoenix/test/support/conn_case.ex"
      to: "examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs"
      via: "Sigra-shaped request fixture through the real router pipeline"
      pattern: "sigra_conn"
---

<objective>
Converge the runtime code and request-path proofs on one canonical direct Sigra host-wiring contract.

Purpose: satisfy SIGRA-04 and SIGRA-05 by removing leftover example-only indirection, preserving the soft-dependency adapter posture, and proving the direct callback path through both library-level and example-level tests.
Output: a direct-wiring example app, tightened Sigra integration tests, and one explicit router-driven fallback-path proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-CONTEXT.md
@.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-RESEARCH.md
@.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-PATTERNS.md
@.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-CONTEXT.md
@lib/threadline/integrations/sigra.ex
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@examples/threadline_phoenix/lib/threadline_phoenix/audit_actor.ex
@examples/threadline_phoenix/test/support/conn_case.ex
@test/threadline/integrations/sigra_test.exs
@examples/threadline_phoenix/test/threadline_phoenix_web/posts_audit_path_test.exs
@examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs

<interfaces>
From examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex:
```elixir
plug(Threadline.Plug,
  actor_fn: &Threadline.Integrations.Sigra.actor_ref_from_conn/1,
  context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1
)
```

From lib/threadline/integrations/sigra.ex:
```elixir
@spec actor_ref_from_conn(Plug.Conn.t()) :: ActorRef.t() | nil
@spec audit_context_overrides_from_conn(Plug.Conn.t()) :: %{optional(:correlation_id) => String.t()}
```

From examples/threadline_phoenix/test/support/conn_case.ex:
```elixir
def sigra_conn(conn, attrs \\ %{}) do
  ...
  conn
  |> Plug.Conn.assign(:current_scope, scope)
  |> then(&%{&1 | private: Map.put(&1.private, :sigra_session, session)})
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove leftover example-only Sigra seams and preserve one canonical adapter name</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex, examples/threadline_phoenix/lib/threadline_phoenix/audit_actor.ex, lib/threadline/integrations/sigra.ex</files>
  <action>Work against the current dirty worktree rather than resetting it. Follow the exact-file analogs mapped in `50-PATTERNS.md`: keep the direct callback wiring visible in the Phoenix `:api` router pipeline, do not introduce or preserve an example-local wrapper plug, and treat `examples/threadline_phoenix/lib/threadline_phoenix/audit_actor.ex` as a delete candidate unless it carries real host-owned behavior. Keep `Threadline.Integrations.Sigra` as the single canonical module name across runtime surfaces, preserve the soft-dependency guard and tolerant request-edge reads in `lib/threadline/integrations/sigra.ex`, and do not expand this plan into Phase 51 incident-auth work. If the current router block is already correct, make the minimum cleanup needed to remove dead indirection and keep the direct callback path explicit.</action>
  <verify>
    <automated>mix test test/threadline/integrations/sigra_test.exs</automated>
    <automated>cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_audit_path_test.exs test/threadline_phoenix_web/posts_correlation_path_test.exs</automated>
  </verify>
  <done>The runtime code shows one canonical direct Sigra callback path through `Threadline.Plug`, with no example-only rename seam left behind.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the direct callback contract at both adapter and router levels</name>
  <files>test/threadline/integrations/sigra_test.exs, examples/threadline_phoenix/test/threadline_phoenix_web/posts_audit_path_test.exs, examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs</files>
  <action>Extend or tighten tests without broadening the example app into a full auth matrix. Follow the exact-file analogs in `50-PATTERNS.md`: keep richer Sigra semantics in `test/threadline/integrations/sigra_test.exs`, continue proving direct `Threadline.Plug` composition there, and add or tighten one real router-driven no-header fallback proof in `posts_correlation_path_test.exs` so the example demonstrates correlation derivation through `context_overrides_fn` when `x-correlation-id` is absent. Preserve `posts_audit_path_test.exs` as the shortest honest session-backed golden path. Do not move impersonation policy, token issuance semantics, tenancy, or incident authorization concerns into the example path tests; those remain library-level or Phase 51 concerns.</action>
  <verify>
    <automated>mix test test/threadline/integrations/sigra_test.exs</automated>
    <automated>cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_audit_path_test.exs test/threadline_phoenix_web/posts_correlation_path_test.exs</automated>
  </verify>
  <done>The direct Sigra wiring contract is protected by adapter tests and by one router-driven fallback-path example proof, without inflating the example scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sigra-shaped request state -> `Threadline.Integrations.Sigra` | Foreign request-edge data is interpreted into Threadline actor and correlation semantics. |
| example app docs/tests -> adopters | The shipped example teaches a host-wiring contract that external users may copy directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | S | example-local delegate seam | mitigate | Remove the dead delegate so adopters see one canonical adapter name instead of multiple “authority” surfaces. |
| T-50-02 | T | direct callback contract | mitigate | Keep adapter-level tests and router-driven tests aligned on the same `Threadline.Plug` callback pair. |
| T-50-03 | R | example proof surface | mitigate | Add one explicit no-header fallback proof so the example demonstrates the real golden path instead of relying only on library-level tests. |
| T-50-04 | E | phase scoping | mitigate | Exclude incident auth/tenancy work from this plan and keep Phase 51 ownership explicit. |
</threat_model>

<verification>
Run `mix test test/threadline/integrations/sigra_test.exs`.

Run `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_audit_path_test.exs test/threadline_phoenix_web/posts_correlation_path_test.exs`.
</verification>

<success_criteria>
- The example app’s visible host-wiring surface uses `Threadline.Integrations.Sigra` directly with `Threadline.Plug`.
- No example-only Sigra delegate or pre-plug remains as part of the canonical path.
- The Sigra adapter and example request-path tests together protect the direct-wiring contract, especially the no-header correlation fallback path.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-01-SUMMARY.md`
</output>
