---
phase: 50-direct-sigra-host-wiring
plan: "02"
type: execute
wave: 2
depends_on:
  - "50-01"
files_modified:
  - guides/integrations/sigra.md
  - examples/threadline_phoenix/README.md
  - test/threadline/integrations/sigra_doc_contract_test.exs
  - test/threadline/example_phoenix_readme_contract_test.exs
autonomous: true
requirements:
  - SIGRA-04
  - SIGRA-05
must_haves:
  truths:
    - "Sigra-facing docs teach one canonical direct callback story through `Threadline.Plug`."
    - "The example README no longer implies an app-local delegate seam is part of the blessed path."
    - "Doc-contract tests fail if the direct callback names or narrowed responsibilities drift."
  artifacts:
    - path: "guides/integrations/sigra.md"
      provides: "Authoritative Sigra guide for the direct callback contract and adapter-owned semantics"
    - path: "examples/threadline_phoenix/README.md"
      provides: "Runnable example-app explanation aligned with the shipped router wiring"
    - path: "test/threadline/integrations/sigra_doc_contract_test.exs"
      provides: "Drift guard for the canonical direct callback names and behavior wording"
    - path: "test/threadline/example_phoenix_readme_contract_test.exs"
      provides: "Lightweight drift guard for the example README's direct Sigra wiring language"
  key_links:
    - from: "guides/integrations/sigra.md"
      to: "test/threadline/integrations/sigra_doc_contract_test.exs"
      via: "literal locks for direct callback names and narrowed responsibilities"
      pattern: "Threadline.Integrations.Sigra"
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      to: "examples/threadline_phoenix/README.md"
      via: "example README should describe the shipped callback pair, not an app-local alias"
      pattern: "router-pipeline-actor-fn"
---

<objective>
Align the adopter-facing Sigra docs and drift guards around the canonical direct host-wiring path.

Purpose: make the direct callback contract executable as documentation so the guide, example README, and doc-contract tests all describe the same blessed adapter path.
Output: updated Sigra docs and doc-contract assertions that preserve one module name and one explanation of responsibilities.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-CONTEXT.md
@.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-RESEARCH.md
@.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-PATTERNS.md
@guides/integrations/sigra.md
@examples/threadline_phoenix/README.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@test/threadline/integrations/sigra_doc_contract_test.exs

<interfaces>
From guides/integrations/sigra.md:
```markdown
## Plug callback wire-up
...
actor_fn: &Threadline.Integrations.Sigra.actor_ref_from_conn/1,
context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1
```

From test/threadline/integrations/sigra_doc_contract_test.exs:
```elixir
assert String.contains?(doc, "actor_fn: &Threadline.Integrations.Sigra.actor_ref_from_conn/1")
assert String.contains?(
         doc,
         "context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1"
       )
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite example-facing prose around the canonical direct callback path</name>
  <files>guides/integrations/sigra.md, examples/threadline_phoenix/README.md, test/threadline/example_phoenix_readme_contract_test.exs</files>
  <action>Update the Sigra guide and example README together so they teach the same direct callback story, and add a lightweight README drift guard for the direct-wiring wording. Follow the exact-file analogs in `50-PATTERNS.md`: keep `guides/integrations/sigra.md` as the authoritative place for the adapter contract and broader supported semantics, while `examples/threadline_phoenix/README.md` stays the shortest honest runnable example. Replace any wording that says the example “delegates to” or otherwise implies an app-local seam with wording that says the app wires directly to `Threadline.Integrations.Sigra.actor_ref_from_conn/1` and `audit_context_overrides_from_conn/1`. Preserve the Phase 49 authority split: `actor_fn` decides identity and `context_overrides_fn` fills additive request metadata only. Mirror the repo’s existing doc-contract pattern with a small root-side test that reads the example README and locks the direct callback naming plus the absence of app-local delegate framing. Do not expand this plan into the broader Phase 52 docs alignment set or Phase 51 incident-auth guidance beyond keeping wording non-contradictory.</action>
  <verify>
    <automated>mix test test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs</automated>
  </verify>
  <done>The guide and example README describe one canonical direct Sigra wiring path with no leftover “delegate seam” language.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the direct callback doc contract against drift</name>
  <files>test/threadline/integrations/sigra_doc_contract_test.exs</files>
  <action>Refresh the Sigra doc-contract assertions so future edits cannot silently reintroduce alternate wiring names or muddled responsibility boundaries. Follow the exact-file analog in `50-PATTERNS.md`: preserve the current marker, section-order, and literal-lock structure, then add or tighten assertions for the canonical direct callback names, the additive-only responsibility split, and any wording that distinguishes the shipped adapter path from host-owned auth or later-phase incident concerns. Keep this task focused on the authoritative Sigra guide, while Task 1's README test protects the example-app wording that the guide does not cover directly.</action>
  <verify>
    <automated>mix test test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs</automated>
  </verify>
  <done>The Sigra doc-contract test protects the direct callback names and the narrowed direct-wiring narrative from drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public integration docs -> adopters | External hosts may copy the wiring pattern and semantics directly from these docs. |
| doc-contract tests -> docs | Drift tests are the last automated guard before conflicting guidance ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | S | example README wording | mitigate | Remove “delegates to” language so the README does not imply an app-local adapter seam is part of the contract. |
| T-50-06 | T | Sigra guide drift | mitigate | Keep the guide’s direct callback literals and responsibility split locked by doc-contract assertions. |
| T-50-07 | R | public guidance scope | mitigate | Keep Phase 50 docs focused on direct wiring and avoid incident-auth or tenancy claims that belong to later phases. |
</threat_model>

<verification>
Run `mix test test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs`.
</verification>

<success_criteria>
- `guides/integrations/sigra.md` remains the authoritative source for the direct Sigra callback contract.
- `examples/threadline_phoenix/README.md` describes the same direct path without implying an app-local delegate seam.
- Doc-contract tests fail on drift in the canonical callback names or responsibility split.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-02-SUMMARY.md`
</output>
