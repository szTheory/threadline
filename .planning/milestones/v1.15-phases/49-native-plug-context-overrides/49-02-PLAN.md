---
phase: 49-native-plug-context-overrides
plan: "02"
type: execute
wave: 2
depends_on:
  - "49-01"
files_modified:
  - guides/integrations/sigra.md
  - guides/getting-started-saas.md
  - test/threadline/integrations/sigra_doc_contract_test.exs
  - test/threadline/getting_started_saas_doc_contract_test.exs
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
must_haves:
  truths:
    - "Adopter-facing docs teach `context_overrides_fn` as additive request metadata, not a second actor or transport-normalization path."
    - "Docs state where `Threadline.Plug` belongs, which values win, and what invalid callback shapes raise."
    - "Doc-contract tests fail if the narrowed host-wiring contract drifts in guides."
  artifacts:
    - path: "guides/integrations/sigra.md"
      provides: "Canonical worked example of direct `Threadline.Plug` wiring with additive-only context overrides"
    - path: "guides/getting-started-saas.md"
      provides: "Adopter quickstart language aligned with the narrowed Phase 49 callback contract"
    - path: "test/threadline/integrations/sigra_doc_contract_test.exs"
      provides: "Literal locks for placement, precedence, and failure behavior in the Sigra guide"
    - path: "test/threadline/getting_started_saas_doc_contract_test.exs"
      provides: "Literal locks for quickstart wording and direct-callback wiring"
  key_links:
    - from: "test/threadline/integrations/sigra_doc_contract_test.exs"
      to: "guides/integrations/sigra.md"
      via: "literal and section-order assertions"
      pattern: "context_overrides_fn"
    - from: "test/threadline/getting_started_saas_doc_contract_test.exs"
      to: "guides/getting-started-saas.md"
      via: "guide literal assertions plus extracted router block"
      pattern: "router-pipeline-actor-fn"
---

<objective>
Align the public docs and doc-contract tests with the narrowed native override contract shipped in Plan 49-01.

Purpose: make the Phase 49 behavior executable as documentation, not just code, so adopters and future phases inherit the same contract.
Output: updated guides and doc-contract tests that explicitly lock placement, precedence, and failure semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-CONTEXT.md
@.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-RESEARCH.md
@.planning/milestones/v1.14-phases/44-sigra-integration-adapter/44-CONTEXT.md
@.planning/milestones/v1.14-phases/47-saas-adopter-onramp/47-CONTEXT.md
@guides/integrations/sigra.md
@guides/getting-started-saas.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@test/threadline/integrations/sigra_doc_contract_test.exs
@test/threadline/getting_started_saas_doc_contract_test.exs

<interfaces>
From examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex:
```elixir
plug(Threadline.Plug,
  actor_fn: &Threadline.Integrations.Sigra.actor_ref_from_conn/1,
  context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1
)
```

From test/threadline/integrations/sigra_doc_contract_test.exs:
```elixir
assert String.contains?(
  doc,
  "context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1"
)
```

From test/threadline/getting_started_saas_doc_contract_test.exs:
```elixir
assert String.contains?(doc, router_block())
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite adopter-facing guides around additive-only override semantics</name>
  <files>guides/integrations/sigra.md, guides/getting-started-saas.md</files>
  <action>Update the guide prose per D-14 through D-18 without widening scope. Anchor the edits to the exact-file analogs mapped in `49-PATTERNS.md`: keep `guides/integrations/sigra.md` as the canonical direct-callback walkthrough, preserve the shipped callback snippet/section structure already used there, and keep `guides/getting-started-saas.md` aligned with the example-app quickstart/router-block pattern rather than inventing a second host-wiring story. Teach `context_overrides_fn` as a generic `Threadline.Plug` host-wiring capability first (D-14), keep actor identity primary and owned by `actor_fn` (D-16), describe the override surface narrowly as additive `request_id` / `correlation_id` metadata only (D-04, D-15), and make placement, precedence, host-owned IP normalization, and failure behavior explicit (D-17). Preserve the direct Sigra callback pattern as the worked example, but do not describe the hook as a special correlation escape hatch and do not claim `AuditContext` or proxy handling changed in this phase.</action>
  <verify>
    <automated>mix test test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs</automated>
  </verify>
  <done>The guides describe the same direct callback pattern as the code, clearly explain who owns actor extraction vs additive request metadata, and explicitly document precedence and raising behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the narrowed docs contract with targeted drift tests</name>
  <files>test/threadline/integrations/sigra_doc_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs</files>
  <action>Refresh the doc-contract assertions so the public contract cannot drift silently after Plan 49-01 lands. Follow the exact-file analogs mapped in `49-PATTERNS.md` for `test/threadline/integrations/sigra_doc_contract_test.exs` and `test/threadline/getting_started_saas_doc_contract_test.exs`: extend the current literal-assertion style for the Sigra guide and preserve the extracted router-block assertion pattern for the quickstart so both tests stay tied to shipped docs/examples. Add literal checks for additive-only wording, explicit header/base precedence, immediate `ArgumentError` failure semantics for invalid callback shapes, and the fact that hosts keep IP normalization upstream. Keep the existing extracted router block assertion so the quickstart remains grounded in the shipped example-app wiring, but avoid asserting broader Phase 50 behavior that belongs to later execution.</action>
  <verify>
    <automated>mix test test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs</automated>
  </verify>
  <done>Doc-contract tests fail on any future drift in the narrowed host-wiring contract, especially around override scope, precedence, failure mode, and the direct callback example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library docs -> host adopters | Public docs instruct external hosts how to wire audit context into their apps. |
| doc-contract tests -> guides | Drift detection is the last guard before incorrect public guidance ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05 | S | `guides/integrations/sigra.md` | mitigate | Keep actor extraction explicitly on `actor_fn` and describe `context_overrides_fn` as additive metadata only so docs do not imply a second authority path. |
| T-49-06 | T | `guides/getting-started-saas.md` | mitigate | Document transport/base precedence and host-owned IP normalization explicitly so adopters do not overwrite trusted request metadata or move proxy logic into Threadline. |
| T-49-07 | R | doc-contract tests | mitigate | Assert the narrowed literals and section order so future edits cannot silently contradict the shipped contract. |
| T-49-08 | I | public examples | accept | The docs reference request metadata fields already exposed by the API contract; no new sensitive fields or examples are introduced in this phase. |
</threat_model>

<verification>
Run `mix test test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs`.
</verification>

<success_criteria>
- Sigra and quickstart guides match the narrowed Phase 49 semantics exactly.
- Doc-contract tests lock the direct callback story, additive-only override scope, precedence, and failure behavior.
- Phase 50 can build on the docs without re-clarifying actor authority or request-metadata precedence.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-02-SUMMARY.md`
</output>
