---
phase: 49-native-plug-context-overrides
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/plug.ex
  - test/threadline/plug_test.exs
  - test/threadline/integrations/sigra_test.exs
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
must_haves:
  truths:
    - "Hosts can supply additive request metadata through `context_overrides_fn` without introducing a companion pre-plug solely for request/correlation overrides."
    - "`actor_fn` remains the only actor-authority path; `context_overrides_fn` cannot replace actor identity or remote IP."
    - "Header- or conn-derived `request_id` and `correlation_id` values win over callback output; overrides only fill missing values."
    - "Unknown override keys and non-map callback returns raise `ArgumentError` deterministically."
  artifacts:
    - path: "lib/threadline/plug.ex"
      provides: "Narrowed `context_overrides_fn` contract and nil-only supplement merge semantics"
      contains: "@allowed_override_keys [:request_id, :correlation_id]"
    - path: "test/threadline/plug_test.exs"
      provides: "Unit coverage for precedence, validation, and non-destructive nil behavior"
    - path: "test/threadline/integrations/sigra_test.exs"
      provides: "Integration coverage for Sigra composition and explicit-header precedence"
  key_links:
    - from: "lib/threadline/plug.ex"
      to: "test/threadline/plug_test.exs"
      via: "targeted callback-contract assertions"
      pattern: "context_overrides_fn"
    - from: "lib/threadline/plug.ex"
      to: "test/threadline/integrations/sigra_test.exs"
      via: "Threadline.Plug init with Sigra callbacks"
      pattern: "context_overrides_fn: &SigraAdapter.audit_context_overrides_from_conn/1"
---

<objective>
Tighten the in-flight `Threadline.Plug` override implementation into the locked Phase 49 contract and prove it with focused tests.

Purpose: satisfy PLUG-01 and PLUG-02 without expanding scope beyond the existing dirty-worktree implementation.
Output: narrowed plug behavior, updated moduledoc, and green unit/integration tests for precedence and validation.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-CONTEXT.md
@.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-RESEARCH.md
@.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-PATTERNS.md
@lib/threadline/plug.ex
@lib/threadline/semantics/audit_context.ex
@test/threadline/plug_test.exs
@test/threadline/integrations/sigra_test.exs

<interfaces>
From lib/threadline/plug.ex:
```elixir
def init(opts) do
  %{
    actor_fn: Keyword.get(opts, :actor_fn),
    context_overrides_fn: Keyword.get(opts, :context_overrides_fn)
  }
end

def call(conn, %{actor_fn: actor_fn, context_overrides_fn: context_overrides_fn}) do
  context =
    %AuditContext{
      actor_ref: extract_actor(conn, actor_fn),
      request_id: extract_request_id(conn),
      correlation_id: get_req_header(conn, "x-correlation-id") |> List.first(),
      remote_ip: format_ip(conn.remote_ip)
    }
    |> apply_context_overrides(conn, context_overrides_fn)

  assign(conn, :audit_context, context)
end
```

From lib/threadline/semantics/audit_context.ex:
```elixir
defstruct [:actor_ref, :request_id, :correlation_id, :remote_ip]
```

From lib/threadline/integrations/sigra.ex:
```elixir
@spec audit_context_overrides_from_conn(Plug.Conn.t()) :: %{optional(:correlation_id) => String.t()}
def audit_context_overrides_from_conn(conn)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow `Threadline.Plug` override authority and precedence</name>
  <files>lib/threadline/plug.ex, test/threadline/plug_test.exs</files>
  <behavior>
    - Test 1: `context_overrides_fn` fills `request_id` or `correlation_id` only when the baseline value is `nil`.
    - Test 2: explicit `x-request-id`, explicit `x-correlation-id`, and derived actor/remote_ip values remain authoritative over override attempts.
    - Test 3: returned `%{request_id: nil}` or `%{correlation_id: nil}` is non-destructive and does not delete baseline values.
    - Test 4: unknown keys, `:actor_ref`, `:remote_ip`, and non-map returns raise `ArgumentError`.
  </behavior>
  <action>Work against the existing in-flight implementation in `lib/threadline/plug.ex`; do not reset unrelated dirty changes. Follow the exact-file analogs mapped in `49-PATTERNS.md` for `lib/threadline/plug.ex`, specifically the existing init/baseline-context shape and the `apply_context_overrides` + `validate_context_overrides!` helper pattern, while rewriting the public contract per D-01 through D-13 and D-19: keep `actor_fn` as the sole actor-authority path (D-03), narrow `@allowed_override_keys` to `[:request_id, :correlation_id]` only (D-04, D-05, D-06), preserve transport/base-value precedence so overrides supplement only when the derived value is `nil` (D-07, D-08, D-09), keep validation deterministic with immediate `ArgumentError` for unknown keys and non-map returns (D-10, D-11), keep nil values non-destructive (D-13), and do not expand `AuditContext` or add proxy/IP normalization logic. Mirror the mapped `test/threadline/plug_test.exs` analog by replacing broad override cases with narrow allowlist, precedence, forbidden-key, and nil-preservation coverage. Update the moduledoc/options prose in the same file so the source-level contract matches the implemented behavior and phase decisions.</action>
  <verify>
    <automated>mix test test/threadline/plug_test.exs</automated>
  </verify>
  <done>`Threadline.Plug` only accepts additive `request_id`/`correlation_id` overrides, base values win when present, invalid shapes fail loudly, and `test/threadline/plug_test.exs` proves the narrowed contract.</done>
</task>

<task type="auto">
  <name>Task 2: Lock Sigra composition against the narrowed plug contract</name>
  <files>test/threadline/integrations/sigra_test.exs</files>
  <action>Revise the Sigra integration tests to assert the Phase 49 contract from the consumer side without widening the adapter surface. Follow the exact-file analog mapped in `49-PATTERNS.md` for `test/threadline/integrations/sigra_test.exs`: keep the direct `Threadline.Plug` + Sigra callback composition and extend the existing header-wins integration style rather than inventing a new harness. `Threadline.Plug` composed with `SigraAdapter.actor_fn/0` and `audit_context_overrides_from_conn/1` must still derive actor identity through `actor_fn` only (per D-03), fill correlation metadata when absent (per D-04 and D-08), and preserve explicit `x-correlation-id` headers over adapter output (per D-07 through D-09). Add a regression case that demonstrates a misbehaving override callback cannot replace an already-derived request/correlation value, while keeping the existing soft-dependency and header-wins expectations intact.</action>
  <verify>
    <automated>mix test test/threadline/plug_test.exs test/threadline/integrations/sigra_test.exs</automated>
  </verify>
  <done>The Sigra integration matrix proves the narrowed `Threadline.Plug` semantics, especially additive-only correlation filling and explicit-header precedence, with no second actor path introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host callback -> `Threadline.Plug` | Untrusted host-supplied callback output crosses into Threadline's public plug contract. |
| inbound headers -> `AuditContext` | Request metadata from the client crosses into durable audit semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | S | `context_overrides_fn` | mitigate | Keep actor ownership in `actor_fn` only and reject `:actor_ref` overrides per D-03 and D-05. |
| T-49-02 | T | `apply_context_overrides/3` | mitigate | Merge only when baseline `request_id` / `correlation_id` is `nil`; explicit inbound values remain authoritative per D-07 through D-09. |
| T-49-03 | D | `validate_context_overrides!/1` | mitigate | Raise `ArgumentError` immediately for non-map returns or unknown keys so bad host wiring fails fast instead of silently degrading audit context. |
| T-49-04 | I | `AuditContext` enrichment | accept | `request_id` and `correlation_id` are already request metadata fields in `AuditContext`; this phase does not widen the struct or add new sensitive fields. |
</threat_model>

<verification>
Run `mix test test/threadline/plug_test.exs test/threadline/integrations/sigra_test.exs`.
</verification>

<success_criteria>
- `Threadline.Plug` enforces the locked additive-only override surface for `request_id` and `correlation_id`.
- Existing hosts that only use `actor_fn` or omit `context_overrides_fn` retain unchanged behavior.
- The test suite catches precedence drift, forbidden-key drift, and non-map return drift deterministically.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-01-SUMMARY.md`
</output>
