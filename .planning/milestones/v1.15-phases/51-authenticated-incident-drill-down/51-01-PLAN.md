---
phase: 51-authenticated-incident-drill-down
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex
  - examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs
  - examples/threadline_phoenix/test/support/conn_case.ex
autonomous: true
requirements:
  - INCIDENT-03
must_haves:
  truths:
    - "Anonymous callers receive `401` with the exact JSON detail `authentication required for incident drill-down` before the example app serves incident drill-down data."
    - "Authenticated callers still receive the existing `audit_transaction_id` plus ordered `changes` response shape for valid transaction ids."
    - "Malformed `audit_transaction_id` values still return `400` for authenticated callers instead of being collapsed into new `403` or `404` policy semantics."
  artifacts:
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex"
      provides: "Endpoint-local authenticated actor gate and the preserved drill-down success payload"
    - path: "examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs"
      provides: "Golden request-path proof for authenticated success, anonymous rejection, and malformed id handling"
    - path: "examples/threadline_phoenix/test/support/conn_case.ex"
      provides: "Sigra-shaped authenticated request fixture when helper cleanup is needed to keep the proof honest"
  key_links:
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex"
      to: "examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs"
      via: "controller-local `authenticated_actor/1` gate plus UUID validation branches"
      pattern: "authentication required for incident drill-down"
    - from: "examples/threadline_phoenix/test/support/conn_case.ex"
      to: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      via: "Sigra-shaped request state flowing through the real `Threadline.Plug` path"
      pattern: "sigra_conn"
---

<objective>
Converge the incident drill-down runtime path on one explicit authenticated baseline in the Phoenix example app.

Purpose: satisfy INCIDENT-03 by tightening the in-flight controller guard and request-path proofs around the normalized `audit_context.actor_ref` contract while keeping malformed-id and success-path behavior stable.
Output: an endpoint-local auth contract in `AuditTransactionController` plus request tests that prove authenticated success, anonymous `401`, and authenticated malformed-id `400`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.15-phases/51-authenticated-incident-drill-down/51-CONTEXT.md
@.planning/milestones/v1.15-phases/51-authenticated-incident-drill-down/51-RESEARCH.md
@.planning/milestones/v1.15-phases/51-authenticated-incident-drill-down/51-PATTERNS.md
@.planning/milestones/v1.15-phases/50-direct-sigra-host-wiring/50-CONTEXT.md
@.planning/milestones/v1.15-phases/49-native-plug-context-overrides/49-CONTEXT.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs
@examples/threadline_phoenix/test/threadline_phoenix_web/posts_audit_path_test.exs
@examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs
@examples/threadline_phoenix/test/support/conn_case.ex

<interfaces>
From examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex:
```elixir
def changes(conn, %{"id" => id}) do
  case authenticated_actor(conn) do
    nil ->
      conn
      |> put_status(:unauthorized)
      |> json(%{errors: %{detail: "authentication required for incident drill-down"}})

    _actor_ref ->
      case Ecto.UUID.cast(id) do
        :error ->
          conn
          |> put_status(:bad_request)
          |> json(%{errors: %{detail: "invalid audit transaction id"}})
```

From examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex:
```elixir
plug(Threadline.Plug,
  actor_fn: &Threadline.Integrations.Sigra.actor_ref_from_conn/1,
  context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1
)
```

From examples/threadline_phoenix/test/support/conn_case.ex:
```elixir
def sigra_conn(conn, attrs \\ %{}) do
  ...
  conn
  |> Plug.Conn.assign(:current_scope, scope)
  |> then(&%{&1 | private: Map.put(&1.private, :sigra_session, session)})
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the endpoint-local authenticated actor contract in `AuditTransactionController`</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex</files>
  <action>Work against the existing dirty Phase 51 baseline rather than resetting it. Keep the auth gate endpoint-local in `AuditTransactionController` per D-01 and D-02; do not introduce a reusable auth plug, router pipeline, or library-owned authorization seam. Authenticate only by checking normalized `conn.assigns.audit_context.actor_ref` per D-04 and D-05, return `401` with `%{errors: %{detail: "authentication required for incident drill-down"}}` for anonymous requests per D-07 and D-08, preserve `400` with the current invalid-id detail for malformed UUIDs per D-09, and preserve the existing successful JSON response shape for authenticated callers per D-14. Do not add `403` or concealment-oriented `404` branches per D-10. If the current controller already matches the contract, keep edits minimal and limited to clarifying helper naming or branch ordering without changing the shipped response surface.</action>
  <verify>
    <automated>cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_incident_json_path_test.exs</automated>
  </verify>
  <done>`AuditTransactionController` remains the sole Phase 51 auth boundary, using `audit_context.actor_ref` for auth and preserving the existing `401`/`400`/`200` semantics exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the request-path proofs for authenticated success, anonymous rejection, and malformed ids</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs, examples/threadline_phoenix/test/support/conn_case.ex</files>
  <action>Treat `posts_incident_json_path_test.exs` as the golden Phase 51 proof file. Keep the authenticated `POST /api/posts` plus authenticated `GET /api/audit_transactions/:id/changes` round-trip intact, preserve the anonymous `401` assertion with the exact JSON body, and add or tighten an explicit authenticated malformed-id proof so `GET /api/audit_transactions/not-a-uuid/changes` returns `400` without regressing the auth gate contract. Route every request through the real example router path using `sigra_conn/2`; do not manually assign `audit_context` in tests, and touch `conn_case.ex` only if helper cleanup is required to keep the Sigra-shaped auth fixture honest with the Phase 50 direct-wiring path. Do not broaden this plan into tenancy, role matrices, `403`/`404` behavior, or doc work.</action>
  <verify>
    <automated>cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_incident_json_path_test.exs test/threadline_phoenix_web/posts_audit_path_test.exs test/threadline_phoenix_web/posts_correlation_path_test.exs</automated>
  </verify>
  <done>The example request-path suite proves authenticated success, exact anonymous rejection, and authenticated malformed-id handling through the real `Threadline.Plug` router path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request -> `AuditTransactionController.changes/2` | Untrusted HTTP callers may attempt anonymous or malformed incident drill-down requests. |
| Sigra-shaped request state -> normalized `audit_context` | Foreign request-edge auth state is translated into the Threadline-owned actor contract before the controller checks it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | S | incident drill-down endpoint | mitigate | Require normalized `audit_context.actor_ref` in `AuditTransactionController` and return the exact `401` body when absent. |
| T-51-02 | T | `audit_transaction_id` input handling | mitigate | Preserve the authenticated malformed-id `400` branch and lock it with request-path coverage. |
| T-51-03 | I | drill-down response contract | mitigate | Preserve the successful JSON payload shape so only authenticated callers receive `changes` data and incident tooling does not regress. |
| T-51-04 | E | phase scope | mitigate | Reject reusable auth plugs, tenancy policy, and `403`/`404` concealment behavior in this phase per D-01 through D-10. |
</threat_model>

<verification>
Run `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_incident_json_path_test.exs`.

Run `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_incident_json_path_test.exs test/threadline_phoenix_web/posts_audit_path_test.exs test/threadline_phoenix_web/posts_correlation_path_test.exs`.
</verification>

<success_criteria>
- `GET /api/audit_transactions/:id/changes` rejects anonymous requests with the exact `401` JSON detail `authentication required for incident drill-down`.
- Authenticated requests with malformed ids still get `400`, and authenticated requests with valid ids still get the existing drill-down payload shape.
- The request-path proof runs through the real Sigra-backed router flow rather than a test-only shortcut.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.15-phases/51-authenticated-incident-drill-down/51-01-SUMMARY.md`
</output>
