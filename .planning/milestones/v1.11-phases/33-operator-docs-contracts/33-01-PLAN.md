---
wave: 1
depends_on: []
files_modified:
  - guides/domain-reference.md
  - guides/production-checklist.md
  - test/threadline/exploration_routing_doc_contract_test.exs
autonomous: true
requirements_addressed: [XPLO-03]
---

# Plan 33-01: Exploration API routing docs & doc contracts

**Phase:** 33 — Operator docs & contracts  
**Plan:** 33-01 of 01  
**Status:** Complete (2026-04-24)

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-33-01 | **Misrouting:** wrong public function named for an intent, misleading integrators | medium | Every row cites functions from **`lib/threadline.ex`** / **`lib/threadline/query.ex`** `@doc`; executor cross-checks names with `grep` before merge. |
| T-33-02 | **Contract bypass:** heading or marker renamed without updating tests | low | Single module **`ExplorationRoutingDocContractTest`** asserts exact heading string **`## Exploration API routing (v1.10+)`** and marker **`XPLO-03-API-ROUTING`**. |

---

## Objective

Deliver **XPLO-03** and roadmap Phase **33** items **6–7**: skimmable **exploration API routing** in **`guides/domain-reference.md`**, one **production-checklist** cross-link, and **ExUnit** doc contracts locking anchors and table cues.

---

## must_haves

- [x] `guides/domain-reference.md` contains **`## Exploration API routing (v1.10+)`** immediately before **`## Support incident queries`**, with contract marker **`XPLO-03-API-ROUTING`** and a compact table covering the five mandatory intents from **33-CONTEXT** D-2 (single row over time, incident window, correlation slice, one transaction, field diff) plus optional actor row.
- [x] Routing prose links to **detail and SQL** at **`#support-incident-queries`** (same-file fragment).
- [x] `guides/production-checklist.md` includes at least one link to **`domain-reference.md#exploration-api-routing-v110`** (verify slug matches rendered anchor for the exact `##` title).
- [x] `mix test test/threadline/exploration_routing_doc_contract_test.exs` exits **0**.

---

## Task 33-01-01 — Routing section in `domain-reference.md`

<read_first>
- `.planning/phases/33-operator-docs-contracts/33-CONTEXT.md` (D-1, D-2, D-3)
- `guides/domain-reference.md` (lines ~165–190: **Correlation** through **Support incident queries** heading)
- `lib/threadline.ex` (delegators: `history/3`, `timeline/2`, `actor_history/2`, `audit_changes_for_transaction/2`, `change_diff/2`, export helpers)
- `lib/threadline/query.ex` (`timeline/2`, `audit_changes_for_transaction/2` moduledoc if present)
</read_first>

<action>
1. Open **`guides/domain-reference.md`** and locate the line **`## Support incident queries`**.
2. **Immediately before** that line, insert a new **`##`** section titled exactly **`Exploration API routing (v1.10+)`** (per **33-CONTEXT** D-1).
3. On its own visible line (same style as LOOP-04), add contract marker **`XPLO-03-API-ROUTING`** (plain text, not HTML comment unless project prefers HTML markers — match **LOOP-04** style: bold one-line token after intro sentence).
4. Add one short intro paragraph stating this block answers **“which public API first?”** and that **SQL and golden queries** live under **[Support incident queries](#support-incident-queries)**.
5. Add one **Markdown table** with columns **`Intent`** (or **Question**) **`Primary API`** **`Notes / pointer`** — **≤ ~8 data rows** beyond header (per D-2), including mandatory rows:
   - Single domain row over time → **`Threadline.history/3`** vs **`Threadline.Query.timeline/2`** with one sentence on filters; mention **T0 / brownfield** pointer to **`guides/brownfield-continuity.md`** or existing brownfield subsection in-domain.
   - Incident / time window → default **`Threadline.Query.timeline/2`**; mention **`timeline_repo!/2`** only if already documented elsewhere in this file (grep first; omit if absent).
   - Correlation-scoped → **`Threadline.Query.timeline/2`** and **`Threadline.Export`** / **`mix threadline.export`** with **`:correlation_id`**; one sentence strict join → defer to correlation / subsection links.
   - One DB transaction’s changes → **`Threadline.Query.audit_changes_for_transaction/2`** and **`Threadline.audit_changes_for_transaction/2`**; **`opts[:repo]`** required; ordering same as timeline (**`captured_at`**, **`id`** DESC).
   - Field-level diff for one change → **`Threadline.change_diff/2`** and **`Threadline.ChangeDiff`**; note INSERT/UPDATE/DELETE and **`changed_from` nil** without duplicating full semantics.
6. **Optional row:** actor-scoped window → **`Threadline.actor_history/2`** + **`timeline/2`** with **`:actor_ref`**.
7. Do **not** modify the existing **`| 1 |` … `| 5 |`** LOOP-04 table body except if a single cross-reference sentence in the new section points to it.
</action>

<acceptance_criteria>
- `grep -nF '## Exploration API routing (v1.10+)' guides/domain-reference.md` exits 0 and line number is **strictly less** than the line number for **`## Support incident queries`** in the same file (`grep -nF '## Support incident queries' guides/domain-reference.md`).
- `grep -q 'XPLO-03-API-ROUTING' guides/domain-reference.md` exits 0.
- `grep -q 'audit_changes_for_transaction' guides/domain-reference.md` exits 0.
- `grep -qE 'change_diff|ChangeDiff' guides/domain-reference.md` exits 0.
- `grep -q 'support-incident-queries' guides/domain-reference.md` exits 0 (link to Support section).
</acceptance_criteria>

---

## Task 33-01-02 — `production-checklist.md` cross-link

<read_first>
- `.planning/phases/33-operator-docs-contracts/33-CONTEXT.md` (D-4)
- `guides/production-checklist.md` (**## Support incident queries** block ~lines 61–80)
- `guides/domain-reference.md` (new heading from task 33-01-01)
</read_first>

<action>
1. In **`guides/production-checklist.md`**, inside or immediately adjacent to **## Support incident queries**, add **one explicit markdown link** whose target is **`domain-reference.md#exploration-api-routing-v110`** (relative path from `guides/` as used elsewhere in that file).
2. Link text should make clear it is the **API routing** skimmer (not duplicating the five-question table).
3. Keep the existing link to **`domain-reference.md#support-incident-queries`** intact.
</action>

<acceptance_criteria>
- `grep -q 'domain-reference.md#exploration-api-routing-v110' guides/production-checklist.md` exits 0.
- `grep -q 'domain-reference.md#support-incident-queries' guides/production-checklist.md` exits 0.
</acceptance_criteria>

---

## Task 33-01-03 — `ExplorationRoutingDocContractTest`

<read_first>
- `.planning/phases/33-operator-docs-contracts/33-CONTEXT.md` (D-5)
- `test/threadline/support_playbook_doc_contract_test.exs` (full file)
- `guides/domain-reference.md` and `guides/production-checklist.md` after tasks 33-01-01 and 33-01-02
</read_first>

<action>
1. Create **`test/threadline/exploration_routing_doc_contract_test.exs`** with **`use ExUnit.Case, async: true`**, **`@moduledoc false`**, **`@repo_root File.cwd!()`**, and private **`read_rel!/1`** matching **`SupportPlaybookDocContractTest`**.
2. Test **`domain-reference`** asserts:
   - **`String.contains?(doc, "## Exploration API routing (v1.10+)")`**
   - **`String.contains?(doc, "XPLO-03-API-ROUTING")`**
   - **`String.contains?(doc, "audit_changes_for_transaction")`**
   - **`String.contains?(doc, "change_diff")`** OR **`String.contains?(doc, "ChangeDiff")`** (at least one true).
3. Test **`production-checklist`** asserts **`String.contains?(doc, "domain-reference.md#exploration-api-routing-v110")`** (strengthens D-4 / end-to-end discovery).
4. Run **`mix format`** on the new file.
</action>

<acceptance_criteria>
- `test -f test/threadline/exploration_routing_doc_contract_test.exs` exits 0.
- `mix test test/threadline/exploration_routing_doc_contract_test.exs` exits 0.
- `mix format --check-formatted` exits 0.
</acceptance_criteria>

---

## Verification

1. `mix format`
2. `mix test test/threadline/exploration_routing_doc_contract_test.exs`
3. `mix test test/threadline/support_playbook_doc_contract_test.exs` (ensure checklist edits did not break LOOP-04 contract)
4. `mix compile --warnings-as-errors`

---

## Success criteria

- **XPLO-03** satisfied: routing guide + checklist cross-link + doc contract tests with stable anchors.

## PLANNING COMPLETE
