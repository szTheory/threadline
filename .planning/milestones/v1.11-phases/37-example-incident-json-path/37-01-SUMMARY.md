# Summary: Phase 37 — Example incident JSON path

## Outcome
Implemented the "incident JSON path" example in the Phoenix example app. This demonstrates how integrators can compose Threadline's query and diffing primitives into a standard REST/JSON API.

## Requirements Satisfied
- **COMP-01**: Verified in `PostsIncidentJsonPathTest`.
- **COMP-02**: Verified in `PostsIncidentJsonPathTest`.
- **COMP-03**: Verified in `ExplorationRoutingDocContractTest`.
