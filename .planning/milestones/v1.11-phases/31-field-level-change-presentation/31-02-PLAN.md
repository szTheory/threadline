---
wave: 2
depends_on: [31-01-PLAN.md]
files_modified:
  - test/threadline/change_diff_test.exs
  - lib/threadline.ex
autonomous: true
requirements_addressed: [XPLO-01]
---

# Plan 31-02: Tests + optional `Threadline` delegator

**Phase:** 31 — Field-level change presentation  
**Plan:** 31-02 of 02  
**Status:** Ready to execute

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-31-03 | **Test oracle drift:** fixtures assert fantasy shapes not producible by triggers | medium | Build `%AuditChange{}` structs using only fields present in **`audit_changes`** schema; include one fixture with `:mask` atom in map values matching redaction placeholder semantics documented in capture. |

---

## Objective

Lock representative **JSON** shapes with **ExUnit** (including masked / sparse / nil `changed_from`), and optionally add at most **one** **`defdelegate`** on **`Threadline`** for REPL discoverability per CONTEXT D-1 / D-7.

---

## must_haves

- [ ] `test/threadline/change_diff_test.exs` exists and **`mix test test/threadline/change_diff_test.exs`** exits 0.
- [ ] Tests call **`Jason.encode!/1`** on results of **`Threadline.ChangeDiff.from_audit_change/2`** for at least one case per op kind (INSERT, UPDATE, DELETE).
- [ ] Golden assertions cover: UPDATE + `changed_from: nil` (no `before` keys on fields); UPDATE + sparse map missing one changed column key (`prior_state: "omitted"`); mask placeholder pass-through; lexicographic order of two+ fields; INSERT default vs `expand_insert_fields: true`; DELETE.
- [ ] If a delegator is added: exactly **`Threadline.change_diff/1`** or **`Threadline.change_diff/2`** delegating to **`Threadline.ChangeDiff.from_audit_change/2`** with documented arity in **`lib/threadline.ex` `@moduledoc`** — **or** explicitly skip delegator with comment in plan execution summary if root module should stay unchanged (CONTEXT allows zero delegators).

---

## Task 31-02-01 — ExUnit coverage

<read_first>
- `lib/threadline/change_diff.ex`
- `.planning/phases/31-field-level-change-presentation/31-CONTEXT.md` (D-3, D-4, D-6)
- `test/threadline/export_test.exs` (first 80 lines for fixture / assertion style)
</read_first>

<action>
1. Add **`test/threadline/change_diff_test.exs`** using `ExUnit.Case`, `async: true` unless shared global state requires false.
2. Build minimal **`%Threadline.Capture.AuditChange{}`** structs with `Ecto.put_meta(..., state: :built)` or valid struct literals — **no** DB calls.
3. Implement test cases listed in **must_haves**; for ordering, assert field name list equals `Enum.sort(names)`.
4. Add one test that **`format: :export_compat`** map contains string keys `"op"`, `"data_after"`, `"changed_fields"`, `"changed_from"` and encodes with Jason.
</action>

<acceptance_criteria>
- `test -f test/threadline/change_diff_test.exs` exits 0.
- `mix test test/threadline/change_diff_test.exs` exits 0.
- `grep -q 'Jason.encode!' test/threadline/change_diff_test.exs` exits 0.
- `grep -q 'prior_state' test/threadline/change_diff_test.exs` exits 0.
</acceptance_criteria>

---

## Task 31-02-02 — Optional delegator + docs cross-check

<read_first>
- `lib/threadline.ex` (delegator patterns for `history`, `timeline`)
- `lib/threadline/change_diff.ex` (`@moduledoc`)
</read_first>

<action>
1. **Either** add **`defdelegate change_diff(audit_change, opts \\\\ []), to: Threadline.ChangeDiff, as: :from_audit_change`** (or arity-1 wrapper) immediately after related exploration delegators in **`lib/threadline.ex`**, with a short `@doc` pointing to **`Threadline.ChangeDiff`**, **or** leave **`lib/threadline.ex`** unchanged and record **“delegator skipped”** in the wave-2 commit body / SUMMARY when executing.
2. Ensure **`Threadline.ChangeDiff` @moduledoc** mentions **`Threadline.Export`** relationship per CONTEXT D-7.
</action>

<acceptance_criteria>
- If delegator added: `grep -q 'change_diff' lib/threadline.ex` exits 0 and `mix test test/threadline/change_diff_test.exs` exits 0.
- If not added: `31-02` execution summary states delegator intentionally omitted; `grep -q 'Threadline.Export' lib/threadline/change_diff.ex` exits 0 (cross-link in moduledoc).
- `mix compile --warnings-as-errors && mix test` exits 0.
</acceptance_criteria>

---

## Verification

1. `mix format`
2. `mix compile --warnings-as-errors`
3. `mix test`

---

## Success criteria

- Phase 31 roadmap success criteria item **1–3** are evidenced by passing tests and ExDoc on **`Threadline.ChangeDiff`**.
