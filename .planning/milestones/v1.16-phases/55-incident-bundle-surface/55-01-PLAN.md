---
phase: 55
plan: "55-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline.ex
  - lib/threadline/investigation.ex
  - lib/threadline/investigation/incident_bundle.ex
  - lib/threadline/query.ex
  - test/threadline/investigation_test.exs
  - test/threadline/query_test.exs
autonomous: true
requirements:
  - INCIDENT-06
must_haves:
  truths:
    - "Adopters can load one transaction-focused incident bundle from `Threadline.incident_bundle/2` instead of hand-assembling raw changes, linked context, and diffs."
    - "The new bundle contract distinguishes a missing `AuditTransaction` from an existing transaction whose captured change list is empty."
    - "Existing raw helpers remain valid: `transaction_context/2` stays the lower-level linked primitive and `audit_changes_for_transaction/2` stays unchanged at its public contract."
  artifacts:
    - path: "lib/threadline.ex"
      provides: "Top-level public `incident_bundle/2` entrypoint and docs alongside `transaction_context/2`"
    - path: "lib/threadline/investigation.ex"
      provides: "Incident bundle orchestration that packages linked context plus `change_diff` while preserving existing ordering"
    - path: "lib/threadline/investigation/incident_bundle.ex"
      provides: "Explicit `IncidentBundle` and `IncidentChange` structs for the new library contract"
    - path: "lib/threadline/query.ex"
      provides: "Existence-aware singular transaction lookup support without changing `audit_changes_for_transaction/2`"
    - path: "test/threadline/investigation_test.exs"
      provides: "Focused bundle semantics coverage for ordered diffs, not-found, and empty-change cases"
    - path: "test/threadline/query_test.exs"
      provides: "Backward-compatibility proof that the raw transaction drill-down path remains unchanged"
  key_links:
    - from: "lib/threadline.ex"
      to: "lib/threadline/investigation.ex"
      via: "public `incident_bundle/2` delegator"
      pattern: "def incident_bundle\\("
    - from: "lib/threadline/investigation.ex"
      to: "lib/threadline/investigation/incident_bundle.ex"
      via: "typed incident bundle result structs"
      pattern: "Incident(Bundle|Change)"
    - from: "lib/threadline/investigation.ex"
      to: "lib/threadline/query.ex"
      via: "parent transaction existence lookup plus ordered child change loading"
      pattern: "audit_changes_for_transaction|repo\\.get|Repo\\.get"
    - from: "lib/threadline/investigation.ex"
      to: "lib/threadline/change_diff.ex"
      via: "packaging `Threadline.change_diff/2` into each bundled change"
      pattern: "change_diff"
---

<objective>
Ship the first-class library incident bundle contract on top of the Phase 54 transaction-context foundation.

Purpose: Satisfy `INCIDENT-06` by turning one transaction drill-down into a tagged, typed, existence-aware bundle contract with ordered linked changes and packaged JSON-ready diffs.
Output: Public `Threadline.incident_bundle/2`, explicit incident bundle structs, focused semantics tests, and compatibility proof for the unchanged raw helpers.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.16-phases/55-incident-bundle-surface/55-CONTEXT.md
@.planning/milestones/v1.16-phases/55-incident-bundle-surface/55-RESEARCH.md
@.planning/milestones/v1.16-phases/55-incident-bundle-surface/PATTERNS.md
@.planning/milestones/v1.16-phases/54-investigation-slice-apis/54-VERIFICATION.md
@lib/threadline.ex
@lib/threadline/investigation.ex
@lib/threadline/investigation/linked_change.ex
@lib/threadline/change_diff.ex
@lib/threadline/query.ex
@test/threadline/investigation_test.exs
@test/threadline/query_test.exs

<interfaces>
From lib/threadline.ex:
```elixir
def transaction_context(transaction_id, opts \\ []),
  do: Investigation.transaction_context(transaction_id, opts)

def audit_changes_for_transaction(transaction_id, opts),
  do: Threadline.Query.audit_changes_for_transaction(transaction_id, opts)

defdelegate change_diff(audit_change, opts \\ []),
  to: Threadline.ChangeDiff,
  as: :from_audit_change
```

From lib/threadline/investigation.ex:
```elixir
def transaction_context(transaction_id, opts \\ []) do
  changes =
    Query.audit_changes_for_transaction(
      transaction_id,
      Keyword.put(opts, :preload, transaction: :action)
    )

  linked_changes = to_linked_changes(changes)
  transaction = linked_transaction(linked_changes)

  %LinkedTransaction{
    transaction: transaction,
    action: linked_action(transaction),
    changes: linked_changes
  }
end
```

From lib/threadline/investigation/linked_change.ex:
```elixir
defmodule Threadline.Investigation.LinkedChange do
  @enforce_keys [:audit_change, :transaction]
  defstruct [:audit_change, :transaction, :action]
end

defmodule Threadline.Investigation.LinkedTransaction do
  defstruct [:transaction, :action, changes: []]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce the typed incident bundle contract at the library surface</name>
  <read_first>
    .planning/milestones/v1.16-phases/55-incident-bundle-surface/55-CONTEXT.md
    .planning/milestones/v1.16-phases/55-incident-bundle-surface/55-RESEARCH.md
    lib/threadline.ex
    lib/threadline/investigation.ex
    lib/threadline/investigation/linked_change.ex
    lib/threadline/query.ex
  </read_first>
  <files>lib/threadline.ex, lib/threadline/investigation.ex, lib/threadline/investigation/incident_bundle.ex, lib/threadline/query.ex</files>
  <behavior>
    - `Threadline.incident_bundle/2` returns `{:ok, %IncidentBundle{}}` for an existing `AuditTransaction`, even when the bundled `changes` list is empty.
    - `Threadline.incident_bundle/2` returns `{:error, :not_found}` when the parent `audit_transactions` row does not exist.
    - Bundled changes preserve Phase 54 ordering and include both the raw linked structs and packaged `change_diff`.
    - `transaction_context/2` remains raw and unchanged; `audit_changes_for_transaction/2` remains raw and unchanged.
  </behavior>
  <action>Implement D-01 through D-11, D-16, and D-17 exactly. Add a top-level `Threadline.incident_bundle/2` delegator beside `transaction_context/2` per D-01, D-02, and D-04. Create explicit incident bundle structs in a new `lib/threadline/investigation/incident_bundle.ex` module per D-03, with one parent `%IncidentBundle{transaction, action, changes}` wrapper and one `%IncidentChange{linked_change, change_diff}` wrapper that preserves raw linked structs while packaging `Threadline.change_diff/2` output per D-05 and D-07. In `lib/threadline/investigation.ex`, build `incident_bundle/2` on top of the Phase 54 raw transaction drill-down seam rather than mutating `transaction_context/2`; keep `transaction_context/2` untouched per D-04. Add existence-aware parent lookup support in `lib/threadline/query.ex` or a sibling helper only as needed to distinguish D-08 through D-10: missing parent row must produce `{:error, :not_found}`, while an existing transaction with zero retained child changes must still return `{:ok, bundle}` with `changes: []`. Reuse `audit_changes_for_transaction/2` as the ordering authority and do not change its public contract per D-11. Do not add `include_change_diff?` or any shape-changing option flags per D-06.</action>
  <verify>
    <automated>mix test test/threadline/investigation_test.exs --max-failures 1</automated>
    <automated>rg "def incident_bundle\\(|defmodule Threadline\\.Investigation\\.Incident(Bundle|Change)|\\{:error, :not_found\\}" lib/threadline.ex lib/threadline/investigation.ex lib/threadline/investigation/incident_bundle.ex</automated>
  </verify>
  <done>The library exposes one discoverable `incident_bundle/2` path, the typed incident bundle modules exist, the result is tagged and existence-aware, and no shape-changing diff option was introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock incident bundle semantics and raw-helper compatibility with focused tests</name>
  <read_first>
    test/threadline/investigation_test.exs
    test/threadline/query_test.exs
    lib/threadline/change_diff.ex
    .planning/milestones/v1.16-phases/54-investigation-slice-apis/54-VERIFICATION.md
  </read_first>
  <files>test/threadline/investigation_test.exs, test/threadline/query_test.exs</files>
  <behavior>
    - Existing transaction with captured changes returns ordered `%IncidentChange{}` entries whose `change_diff` maps are JSON-ready and whose `linked_change` still exposes raw `AuditChange`, `AuditTransaction`, and `AuditAction`.
    - Existing transaction with no child changes returns `{:ok, %IncidentBundle{changes: []}}`.
    - Missing transaction returns `{:error, :not_found}`.
    - `transaction_context/2` and `audit_changes_for_transaction/2` keep their prior raw contracts unchanged.
  </behavior>
  <action>Implement D-18 and D-19 for the library proof while preserving the Phase 54 boundary. Extend `test/threadline/investigation_test.exs` with focused cases that prove the new bundle shape, diff presence, descending order, not-found semantics, and empty-change semantics. Assert that each bundled change carries the raw linked wrapper plus a `change_diff` map that includes the shipped diff contract fields (for example `schema_version` and `field_changes`) without inventing controller JSON. Expand `test/threadline/query_test.exs` with a compatibility block showing `audit_changes_for_transaction/2` still returns raw `%AuditChange{}` rows and `transaction_context/2` still returns `%LinkedTransaction{}` without `change_diff`, while `incident_bundle/2` is the richer tagged contract. Keep these tests narrow and structural; do not snapshot whole JSON payloads and do not teach the Phoenix example shape here.</action>
  <verify>
    <automated>mix test test/threadline/investigation_test.exs test/threadline/query_test.exs --max-failures 1</automated>
    <automated>rg "incident_bundle|IncidentBundle|IncidentChange|not_found|changes: \\[\\]|transaction_context" test/threadline/investigation_test.exs test/threadline/query_test.exs</automated>
  </verify>
  <done>The focused library tests pass together, proving the incident bundle contract is stable and that the older raw helpers remain backward-compatible and distinct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller -> `incident_bundle/2` | Untrusted transaction ids and options cross into singular lookup validation and parent existence resolution |
| investigation layer -> query layer | Bundle packaging must not mutate ordering semantics or collapse missing-parent vs empty-child cases |
| investigation layer -> diff projection | Bundled diffs must reflect persisted capture truth without inventing data not stored in `audit_changes` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | T | `lib/threadline/investigation.ex` | mitigate | Reuse `audit_changes_for_transaction/2` as the ordering authority and keep the missing-parent check separate so packaging cannot silently change drill-down semantics. |
| T-55-02 | I | `lib/threadline/investigation/incident_bundle.ex` | mitigate | Preserve raw linked structs on each bundled change per D-17 while limiting added data to `change_diff` output already derived from persisted capture columns. |
| T-55-03 | R | `test/threadline/query_test.exs` | mitigate | Add explicit compatibility assertions proving `transaction_context/2` and `audit_changes_for_transaction/2` stay unchanged at their public contracts. |
</threat_model>

<verification>
Run the focused library suites and confirm the new public surface exists:

- `mix test test/threadline/investigation_test.exs --max-failures 1`
- `mix test test/threadline/investigation_test.exs test/threadline/query_test.exs --max-failures 1`
- `rg "def incident_bundle\\(|defmodule Threadline\\.Investigation\\.Incident(Bundle|Change)|\\{:error, :not_found\\}" lib/threadline.ex lib/threadline/investigation.ex lib/threadline/investigation/incident_bundle.ex`
</verification>

<success_criteria>
Phase 55 plan 01 is complete when Threadline exposes one explicit incident bundle contract that packages linked transaction/action context and JSON-ready diffs together, distinguishes not-found from empty-change outcomes, and leaves the raw Phase 54 primitives backward-compatible.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.16-phases/55-incident-bundle-surface/55-01-SUMMARY.md`.
</output>
