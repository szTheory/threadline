---
phase: 55
plan: "55-02"
type: execute
wave: 2
depends_on:
  - "55-01"
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_json.ex
  - examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs
  - examples/threadline_phoenix/README.md
autonomous: true
requirements:
  - INCIDENT-07
must_haves:
  truths:
    - "The Phoenix reference incident endpoint uses `Threadline.incident_bundle/2` directly instead of controller-local diff assembly."
    - "The example HTTP contract is rendered through a dedicated JSON module and returns `401`, `400`, `404`, and `200` on the exact paths locked for incident drill-down."
    - "The example continues to treat authentication as the endpoint-local minimum boundary while leaving tenancy and richer authorization host-owned."
  artifacts:
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex"
      provides: "Controller migration from raw change enumeration to the public incident bundle surface"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_json.ex"
      provides: "Dedicated JSON renderer for the bundled incident response shape"
    - path: "examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs"
      provides: "Request-path proof for authenticated 200, anonymous 401, malformed-id 400, and missing-transaction 404"
    - path: "examples/threadline_phoenix/README.md"
      provides: "Minimal example-contract wording aligned to the bundled incident drill-down path if a doc touch is needed"
  key_links:
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex"
      to: "lib/threadline.ex"
      via: "controller calls `Threadline.incident_bundle/2`"
      pattern: "incident_bundle"
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex"
      to: "examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_json.ex"
      via: "Phoenix render path"
      pattern: "render\\(conn, :show"
    - from: "examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs"
      to: "controller + JSON renderer"
      via: "request-path assertions"
      pattern: "401|400|404|200|change_diff|audit_transaction_id"
---

<objective>
Converge the Phoenix reference incident drill-down endpoint on the shipped library incident bundle surface and prove the request paths.

Purpose: Satisfy `INCIDENT-07` by showing the new public contract is sufficient for a real host endpoint without reopening auth policy or broad doc cleanup.
Output: Example controller migration, dedicated JSON renderer, request-path coverage for all required outcomes, and only minimal example-doc wording if needed for contract proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.16-phases/55-incident-bundle-surface/55-CONTEXT.md
@.planning/milestones/v1.16-phases/55-incident-bundle-surface/55-RESEARCH.md
@.planning/milestones/v1.16-phases/55-incident-bundle-surface/55-01-PLAN.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex
@examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/post_json.ex
@examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs
@examples/threadline_phoenix/README.md
@guides/domain-reference.md

<interfaces>
From examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex:
```elixir
case Ecto.UUID.cast(id) do
  :error ->
    ...

  {:ok, uuid} ->
    changes = Threadline.audit_changes_for_transaction(uuid, repo: Repo)

    json(conn, %{
      audit_transaction_id: uuid,
      changes:
        Enum.map(changes, fn ac ->
          %{
            audit_change_id: to_string(ac.id),
            change_diff: Threadline.change_diff(ac, [])
          }
        end)
    })
end
```

From examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/post_json.ex:
```elixir
def post(%{post: post} = assigns) do
  base = %{id: post.id, title: post.title, slug: post.slug, inserted_at: post.inserted_at}

  case Map.get(assigns, :audit_transaction_id) do
    nil -> base
    at_id -> Map.put(base, :audit_transaction_id, at_id)
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the Phoenix incident endpoint to the bundled public surface</name>
  <read_first>
    .planning/milestones/v1.16-phases/55-incident-bundle-surface/55-CONTEXT.md
    examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex
    examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/post_json.ex
    examples/threadline_phoenix/README.md
  </read_first>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex, examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_json.ex, examples/threadline_phoenix/README.md</files>
  <behavior>
    - Anonymous requests still return `401`.
    - Authenticated requests with malformed UUIDs return `400`.
    - Authenticated requests for missing transactions return `404`.
    - Authenticated requests for existing transactions return `200` with the curated incident bundle JSON shape, including empty `changes` lists when appropriate.
  </behavior>
  <action>Implement D-12 through D-18 on the example side, but keep Phase 56 docs cleanup deferred. Update `AuditTransactionController.changes/2` to call `Threadline.incident_bundle/2` rather than `audit_changes_for_transaction/2` plus controller-local `Enum.map`, per D-12 and D-16. Preserve the Phase 51 auth boundary exactly per D-15: anonymous requests stay `401`, authenticated malformed UUIDs stay `400`, and authenticated lookups for missing transactions map `{:error, :not_found}` to `404` per D-14. Add a dedicated Phoenix JSON module, following the `PostJSON` separation pattern, so the library stays Elixir-native while the HTTP shape stays curated per D-13. Render the bundled response from that module rather than dumping structs directly. Only touch `examples/threadline_phoenix/README.md` if needed to replace the old controller-local diff description with one sentence that points to the new bundled library contract; do not start broad guide cleanup here, per the deferred Phase 56 boundary.</action>
  <verify>
    <automated>cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_incident_json_path_test.exs --max-failures 1</automated>
    <automated>rg "incident_bundle|render\\(conn, :show|def show\\(|not_found|authentication required" examples/threadline_phoenix/lib/threadline_phoenix_web/controllers examples/threadline_phoenix/README.md</automated>
  </verify>
  <done>The example controller no longer assembles incident diffs locally, the endpoint renders through a dedicated JSON module, the auth boundary stays unchanged, and docs only move as much as needed to describe the example contract honestly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the request-path contract for 401, 400, 404, and 200</name>
  <read_first>
    examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs
    examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex
    .planning/milestones/v1.16-phases/55-incident-bundle-surface/55-RESEARCH.md
  </read_first>
  <files>examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs</files>
  <behavior>
    - One end-to-end POST-then-GET path proves the `200` response exposes the bundled incident contract, including at least one change with a packaged `change_diff`.
    - Anonymous GET remains `401`.
    - Authenticated GET with malformed UUID remains `400`.
    - Authenticated GET for a well-formed but missing transaction id returns `404`.
  </behavior>
  <action>Implement D-14, D-18, and D-19 with focused request-path coverage. Expand `PostsIncidentJsonPathTest` so it proves the canonical 200 path now returns the bundled incident contract rather than only a thin `audit_change_id + change_diff` projection. Assert the top-level payload carries `audit_transaction_id` plus whatever curated bundle fields the new renderer exposes for transaction/action context, and that each returned change still includes a JSON-ready `change_diff`. Add or update separate path tests for anonymous `401`, malformed-id `400`, and authenticated missing-transaction `404`. Keep the test anchored on real HTTP requests through the Phoenix router rather than calling renderer helpers directly, and keep the assertions structural instead of snapshotting the full response body.</action>
  <verify>
    <automated>cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_incident_json_path_test.exs --max-failures 1</automated>
    <automated>rg "401|400|404|200|change_diff|audit_transaction_id" examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs</automated>
  </verify>
  <done>The request-path suite passes and proves the public `incident_bundle/2` contract is sufficient for the reference Phoenix endpoint across all required status paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> example controller | Untrusted path params and unauthenticated requests cross into the incident drill-down endpoint |
| example controller -> library incident bundle | Host code must map library tagged outcomes to stable HTTP semantics without reintroducing controller-local assembly |
| example JSON renderer -> public API contract | HTTP output must remain curated and explicit rather than leaking raw structs or internal implementation details |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | S | `AuditTransactionController` | mitigate | Preserve the Phase 51 authenticated-actor gate exactly so anonymous requests fail before incident lookup work begins. |
| T-55-05 | T | controller -> library mapping | mitigate | Map malformed UUIDs to `400` and `{:error, :not_found}` to `404` explicitly, then route successful bundles through one JSON renderer instead of controller-local response assembly. |
| T-55-06 | I | `audit_transaction_json.ex` | mitigate | Curate the response shape in a dedicated renderer so only intended transaction/action/change fields become part of the HTTP contract. |
</threat_model>

<verification>
Run the focused Phoenix request-path suite and confirm the controller now uses the bundled public surface:

- `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/posts_incident_json_path_test.exs --max-failures 1`
- `rg "incident_bundle|render\\(conn, :show|def show\\(|not_found" examples/threadline_phoenix/lib/threadline_phoenix_web/controllers`
</verification>

<success_criteria>
Phase 55 plan 02 is complete when the reference Phoenix incident endpoint consumes `Threadline.incident_bundle/2` directly, renders a dedicated curated JSON contract, preserves the existing auth boundary, and has request-path proof for the exact `401`/`400`/`404`/`200` outcomes.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.16-phases/55-incident-bundle-surface/55-02-SUMMARY.md`.
</output>
