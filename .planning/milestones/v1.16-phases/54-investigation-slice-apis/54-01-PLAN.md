---
phase: 54
plan: "54-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline.ex
  - lib/threadline/investigation.ex
  - lib/threadline/query.ex
  - test/threadline/investigation_test.exs
autonomous: true
requirements:
  - EXPLORE-02
must_haves:
  truths:
    - "Adopters can ask the canonical row-history, actor-window, and correlation-bundle questions from the top-level Threadline API instead of assembling low-level filters and joins manually."
    - "Large actor-window and correlation-bundle reads advance with the existing Phase 53 keyset contract rather than introducing offset or bespoke cursor behavior."
    - "Helper results keep the existing descending (captured_at, id) order and strict correlation semantics."
  artifacts:
    - path: "lib/threadline.ex"
      provides: "Public Threadline delegators for the new investigation helper surface"
    - path: "lib/threadline/investigation.ex"
      provides: "Higher-level helper implementation for row, actor, and correlation reads"
    - path: "lib/threadline/query.ex"
      provides: "Helper-specific query composition that reuses existing paging and ordering rules without widening the public filter vocabulary"
    - path: "test/threadline/investigation_test.exs"
      provides: "Focused helper semantics coverage for row, actor, and correlation entrypoints"
  key_links:
    - from: "lib/threadline.ex"
      to: "lib/threadline/investigation.ex"
      via: "public delegator functions"
      pattern: "def (row_history|row_history_page|actor_window|actor_window_page|correlation_bundle|correlation_bundle_page)"
    - from: "lib/threadline/investigation.ex"
      to: "lib/threadline/query.ex"
      via: "composition of history/timeline/timeline_page primitives"
      pattern: "Threadline\\.Query\\.(history|timeline|timeline_page)"
    - from: "lib/threadline/query.ex"
      to: "Phase 53 keyset contract"
      via: "captured_at desc + id desc traversal"
      pattern: "captured_at.*desc|id.*desc|timeline_page"
---

<objective>
Package the first public investigation helper layer for row-history, actor-window, and correlation-bundle reads on top of the shipped Phase 53 paging contract.

Purpose: Implement D-01 through D-09 for the user-facing helper entrypoints so EXPLORE-02 stops depending on bespoke adopter composition.
Output: New top-level Threadline investigation helpers, supporting query composition, and focused helper tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.16-phases/54-investigation-slice-apis/54-CONTEXT.md
@.planning/milestones/v1.16-phases/53-timeline-paging-contract/53-01-SUMMARY.md
@.planning/milestones/v1.16-phases/53-timeline-paging-contract/53-02-SUMMARY.md
@lib/threadline.ex
@lib/threadline/query.ex
@test/threadline/query_test.exs
@guides/domain-reference.md
@guides/getting-started-saas.md

<interfaces>
From lib/threadline.ex:
```elixir
def history(schema_module, id, opts), do: Threadline.Query.history(schema_module, id, opts)
def actor_history(actor_ref, opts), do: Threadline.Query.actor_history(actor_ref, opts)
def timeline(filters \\ [], opts \\ []), do: Threadline.Query.timeline(filters, opts)
def timeline_page(filters \\ [], opts \\ []), do: Threadline.Query.timeline_page(filters, opts)
def audit_changes_for_transaction(transaction_id, opts),
  do: Threadline.Query.audit_changes_for_transaction(transaction_id, opts)
```

From lib/threadline/query.ex:
```elixir
def timeline_page(filters \\ [], opts \\ []) :: TimelinePage.t()
def history(schema_module, id, opts)
def actor_history(%ActorRef{} = actor_ref, opts)
def timeline(filters \\ [], opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the public investigation helper entrypoints</name>
  <read_first>
    .planning/milestones/v1.16-phases/54-investigation-slice-apis/54-CONTEXT.md
    lib/threadline.ex
    lib/threadline/query.ex
    guides/domain-reference.md
  </read_first>
  <files>lib/threadline.ex, lib/threadline/investigation.ex, lib/threadline/query.ex</files>
  <action>Implement the Phase 54 surface split from D-07 explicitly: add public top-level helpers for row-history, actor-window, and correlation-bundle reads per D-01, D-02, and D-08. Use exact helper pairs that make eager-vs-paged usage obvious, for example `row_history/4` plus `row_history_page/4`, `actor_window/3` plus `actor_window_page/3`, and `correlation_bundle/3` plus `correlation_bundle_page/3`. Put helper logic in `lib/threadline/investigation.ex`, keep `lib/threadline.ex` as the adopter discovery surface, and reuse existing `history/3`, `timeline/2`, and `timeline_page/2` building blocks per D-09 instead of widening `validate_timeline_filters!/1` with new general-purpose filter keys. For row-history, add helper-specific query composition in `Threadline.Query` that constrains table plus primary-key JSON and optional time bounds without exposing a new low-level public filter vocabulary. For actor-window and correlation-bundle, delegate large windows through the shipped keyset path per D-04 and preserve strict `captured_at DESC, id DESC` order plus strict correlation semantics per D-05 and D-06. Do not touch docs beyond inline moduledoc/docstring discoverability, and do not add any Phase 55 incident-bundle packaging here.</action>
  <verify>
    <automated>mix test test/threadline/investigation_test.exs --max-failures 1</automated>
    <automated>rg "def (row_history|row_history_page|actor_window|actor_window_page|correlation_bundle|correlation_bundle_page)\\(" lib/threadline.ex lib/threadline/investigation.ex</automated>
  </verify>
  <acceptance_criteria>The repo exposes one discoverable top-level helper path for each canonical operator question in scope, each helper has a clear eager or paged contract, and every paged path reuses Phase 53 semantics instead of offset pagination or ad-hoc cursor logic.</acceptance_criteria>
  <done>`mix test test/threadline/investigation_test.exs --max-failures 1` passes and grep finds the new top-level helper functions in `lib/threadline.ex`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock helper semantics with focused investigation tests</name>
  <read_first>
    test/threadline/query_test.exs
    .planning/milestones/v1.16-phases/53-timeline-paging-contract/53-VERIFICATION.md
    guides/domain-reference.md
  </read_first>
  <files>test/threadline/investigation_test.exs</files>
  <action>Create a focused `test/threadline/investigation_test.exs` file modeled after the narrow behavior-level assertions already used in `test/threadline/query_test.exs`, per D-12 and D-13. Add fixture-backed tests that prove: row-history helpers constrain one row instead of all rows from the table; actor-window helpers return change rows across tables for one actor; correlation-bundle helpers preserve strict inner-join semantics and return only changes linked to the requested `correlation_id`; paged helper variants concatenate back to the eager order when consuming all pages; and the new helpers reduce the manual composition currently taught in docs. Keep the assertions small and structural, not snapshot-heavy.</action>
  <verify>
    <automated>mix test test/threadline/investigation_test.exs --max-failures 1</automated>
    <automated>rg "row_history|actor_window|correlation_bundle|next_cursor|correlation_id" test/threadline/investigation_test.exs</automated>
  </verify>
  <acceptance_criteria>The tests prove all three helper families answer the documented operator questions, preserve filtering and ordering semantics, and show the paged paths round-trip through the existing keyset contract.</acceptance_criteria>
  <done>`test/threadline/investigation_test.exs` exists, references the new helper APIs directly, and passes on its own.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller -> investigation helpers | Untrusted filter values and IDs cross into helper validation and query composition |
| helper layer -> query layer | Helper abstractions must not silently change ordering, correlation, or paging semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | T | `lib/threadline/investigation.ex` | mitigate | Reuse existing query validation and keep helper-specific row predicates explicit so helpers do not broaden the allowed filter surface or corrupt correlation semantics. |
| T-54-02 | I | `correlation_bundle*_` helpers | mitigate | Preserve strict inner-join semantics from `Threadline.Query` so callers cannot accidentally read orphan or unrelated changes for a correlation id. |
| T-54-03 | D | paged helper entrypoints | mitigate | Delegate large windows to `Threadline.timeline_page/2` semantics so callers use bounded keyset traversal instead of eager unbounded reads by accident. |
</threat_model>

<verification>
Run the focused helper suite first, then confirm the exported public entrypoints exist:

- `mix test test/threadline/investigation_test.exs --max-failures 1`
- `rg "def (row_history|row_history_page|actor_window|actor_window_page|correlation_bundle|correlation_bundle_page)\\(" lib/threadline.ex lib/threadline/investigation.ex`
</verification>

<success_criteria>
Phase 54 now has a real public helper layer for row, actor, and correlation investigations, all paged paths honor the Phase 53 contract, and focused tests pin the intended semantics before result-shape enrichment work begins.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.16-phases/54-investigation-slice-apis/54-01-SUMMARY.md`.
</output>
