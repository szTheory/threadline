---
phase: 54
plan: "54-02"
type: execute
wave: 2
depends_on:
  - "54-01"
files_modified:
  - lib/threadline.ex
  - lib/threadline/investigation.ex
  - lib/threadline/investigation/linked_change.ex
  - lib/threadline/query.ex
  - test/threadline/investigation_test.exs
  - test/threadline/query_test.exs
autonomous: true
requirements:
  - EXPLORE-02
must_haves:
  truths:
    - "Investigation helpers return linked transaction and action context in one library contract instead of forcing callers to hand-wire joins around raw AuditChange structs."
    - "Transaction-oriented investigation reads are packaged at the library level without collapsing into the final Phase 55 incident bundle contract."
    - "Focused tests lock both the new helper result shapes and the backward compatibility of the older primitives."
  artifacts:
    - path: "lib/threadline.ex"
      provides: "Public Threadline transaction-oriented helper delegator for the linked investigation surface"
    - path: "lib/threadline/investigation.ex"
      provides: "Final helper orchestration and public result-shape contract"
    - path: "lib/threadline/investigation/linked_change.ex"
      provides: "Explicit linked transaction/action wrapper for helper results"
    - path: "lib/threadline/query.ex"
      provides: "Shared preload or join support for helper result enrichment"
    - path: "test/threadline/investigation_test.exs"
      provides: "Focused shape and linkage coverage for the enriched helper surface"
    - path: "test/threadline/query_test.exs"
      provides: "Backward-compatibility proof for pre-existing primitives"
  key_links:
    - from: "lib/threadline.ex"
      to: "lib/threadline/investigation.ex"
      via: "public transaction helper delegator path"
      pattern: "def transaction_(slice|context)"
    - from: "lib/threadline/investigation.ex"
      to: "lib/threadline/investigation/linked_change.ex"
      via: "explicit result structs"
      pattern: "Linked(Change|Transaction|Action)"
    - from: "lib/threadline/investigation.ex"
      to: "lib/threadline/query.ex"
      via: "transaction/action preloads or joins"
      pattern: "preload|join.*Audit(Action|Transaction)"
    - from: "lib/threadline.ex"
      to: "Phase 55 boundary"
      via: "transaction-oriented helper without diff bundling"
      pattern: "def transaction_(slice|context)"
---

<objective>
Align the new investigation helper contracts around linked transaction and action context, and add the focused proof that keeps Phase 54 useful without swallowing Phase 55 incident-bundle scope.

Purpose: Implement D-07, D-10, D-11, D-12, and D-13 by turning the helper surface into a reusable linked investigation contract instead of another thin wrapper over raw changes.
Output: Explicit helper result shapes, a transaction-oriented library helper, and focused compatibility plus semantics tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.16-phases/54-investigation-slice-apis/54-CONTEXT.md
@.planning/milestones/v1.16-phases/53-timeline-paging-contract/53-01-SUMMARY.md
@.planning/milestones/v1.16-phases/53-timeline-paging-contract/53-02-SUMMARY.md
@lib/threadline.ex
@lib/threadline/query.ex
@lib/threadline/change_diff.ex
@test/threadline/query_test.exs
@examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex

<interfaces>
From lib/threadline.ex:
```elixir
def audit_changes_for_transaction(transaction_id, opts),
  do: Threadline.Query.audit_changes_for_transaction(transaction_id, opts)
defdelegate change_diff(audit_change, opts \\ []),
  to: Threadline.ChangeDiff,
  as: :from_audit_change
```

From examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex:
```elixir
changes = Threadline.audit_changes_for_transaction(uuid, repo: Repo)
Threadline.change_diff(ac, [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce explicit linked investigation result shapes</name>
  <read_first>
    .planning/milestones/v1.16-phases/54-investigation-slice-apis/54-CONTEXT.md
    lib/threadline/investigation.ex
    lib/threadline/query.ex
    examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/audit_transaction_controller.ex
  </read_first>
  <files>lib/threadline.ex, lib/threadline/investigation.ex, lib/threadline/investigation/linked_change.ex, lib/threadline/query.ex</files>
  <action>Extend the helper layer from 54-01 so it returns explicit linked result shapes per D-10 instead of bare `%AuditChange{}` lists everywhere. Create a dedicated result wrapper such as `%Threadline.Investigation.LinkedChange{audit_change, transaction, action}` and use it from the row, actor, and correlation helpers whenever transaction/action context is part of the contract. Add one transaction-oriented helper on the public `Threadline` surface, for example `transaction_slice/2` or `transaction_context/2`, that packages a single transaction drill-down with linked transaction and optional action metadata using the existing `audit_changes_for_transaction/2` primitive per D-09. Keep Phase 55 out of scope: do not attach `change_diff/2`, JSON-ready bundle payloads, or the final incident response document shape in this plan. Preserve the old raw primitives untouched per D-11.</action>
  <verify>
    <automated>mix test test/threadline/investigation_test.exs test/threadline/query_test.exs --max-failures 1</automated>
    <automated>rg "def(transaction_| transaction_)|defstruct .*LinkedChange|audit_changes_for_transaction" lib/threadline.ex lib/threadline/investigation.ex lib/threadline/investigation/linked_change.ex</automated>
    <automated>rg -L "change_diff" lib/threadline/investigation.ex lib/threadline/investigation/linked_change.ex</automated>
  </verify>
  <acceptance_criteria>The helper surface exposes a reusable linked investigation shape with transaction and action context, includes one transaction-focused helper, and proves via focused tests or grepable code checks that Phase 55-only diff or incident-bundle rendering logic is not yet part of the helper implementation.</acceptance_criteria>
  <done>The new linked result struct exists, the transaction-oriented helper is present on `Threadline`, `rg -L "change_diff" lib/threadline/investigation.ex lib/threadline/investigation/linked_change.ex` succeeds, and the focused helper plus compatibility tests pass together.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove shape alignment and backward compatibility with focused tests</name>
  <read_first>
    test/threadline/investigation_test.exs
    test/threadline/query_test.exs
    lib/threadline/change_diff.ex
    .planning/milestones/v1.16-phases/53-timeline-paging-contract/53-VERIFICATION.md
  </read_first>
  <files>test/threadline/investigation_test.exs, test/threadline/query_test.exs</files>
  <action>Expand the helper test file to prove the enriched result shapes stay ordered, preserve strict correlation semantics, and surface linked transaction plus action context in one library read per D-12 and D-13. Add a focused compatibility block in `test/threadline/query_test.exs` that proves `history/3`, `actor_history/2`, `timeline/2`, `timeline_page/2`, and `audit_changes_for_transaction/2` still return their pre-Phase-54 raw structs unchanged per D-11 while the new helpers expose richer contracts. Also add one test that demonstrates the new transaction-oriented helper covers the composition seam currently visible in the Phoenix example controller without yet producing Phase 55’s `change_diff/2` bundle, for example by asserting the helper returns linked change/action/transaction context fields but no `change_diff` key or pre-rendered incident payload field.</action>
  <verify>
    <automated>mix test test/threadline/investigation_test.exs test/threadline/query_test.exs --max-failures 1</automated>
    <automated>rg "LinkedChange|transaction_(slice|context)|change_diff|backward-compatible|timeline_page" test/threadline/investigation_test.exs test/threadline/query_test.exs</automated>
  </verify>
  <acceptance_criteria>Tests lock the richer helper shapes, prove the transaction/action linkage is explicit, show that older public APIs remain backward-compatible, and include one concrete assertion that the new transaction helper stops before `change_diff/2` or Phase 55-style incident payload rendering.</acceptance_criteria>
  <done>The two focused test files pass together, clearly distinguish the richer helper surface from the unchanged raw primitives, and include one explicit assertion that the transaction helper does not yet expose Phase 55 diff/bundle behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library caller -> linked investigation helpers | Callers rely on helper result shapes to preserve scope and not over-disclose unrelated transaction/action data |
| helper layer -> example-inspired transaction drill-down | The library must reduce composition pain without silently becoming an incident rendering layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04 | I | linked helper result shapes | mitigate | Include only the linked transaction and optional action context needed for investigation; leave JSON-ready field diff expansion to Phase 55 so the boundary stays explicit. |
| T-54-05 | T | transaction-oriented helper | mitigate | Build on `audit_changes_for_transaction/2` and existing ordering rules rather than inventing a new transaction query path that could drift semantically. |
| T-54-06 | R | backward-compatibility of existing APIs | mitigate | Add focused regression tests proving older primitives still return raw structs unchanged while new helpers carry the richer wrapper contract. |
</threat_model>

<verification>
Run the enriched helper suite plus the compatibility block together:

- `mix test test/threadline/investigation_test.exs test/threadline/query_test.exs --max-failures 1`
- `rg "def(transaction_| transaction_)|defstruct .*LinkedChange" lib/threadline.ex lib/threadline/investigation.ex lib/threadline/investigation/linked_change.ex`
</verification>

<success_criteria>
Phase 54 ends with a public helper surface that answers the documented operator questions, exposes linked transaction and action context in reusable library contracts, and stops short of the final incident-bundle payload that belongs to Phase 55.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.16-phases/54-investigation-slice-apis/54-02-SUMMARY.md`.
</output>
