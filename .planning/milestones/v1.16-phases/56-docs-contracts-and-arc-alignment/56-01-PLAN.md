---
phase: 56
plan: "56-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/domain-reference.md
  - guides/getting-started-saas.md
  - guides/incident-playbook.md
  - guides/production-checklist.md
  - examples/threadline_phoenix/README.md
autonomous: true
requirements:
  - ADOPT-04
must_haves:
  truths:
    - "A new adopter can see one canonical investigation routing story across the README, reference docs, quickstart, playbook, and Phoenix example."
    - "`Threadline.incident_bundle/2` is taught as the default transaction drill-down path, while `audit_changes_for_transaction/2`, `transaction_context/2`, and `change_diff/2` remain documented as advanced building blocks."
    - "The docs continue to state that authenticated drill-down is only the host minimum and that tenancy and richer policy stay host-owned."
  artifacts:
    - path: "README.md"
      provides: "Concise top-level routing map with the v1.16 investigation hierarchy"
    - path: "guides/domain-reference.md"
      provides: "Canonical 'which API first?' routing table and incident JSON reference aligned to `incident_bundle/2`"
    - path: "guides/getting-started-saas.md"
      provides: "First-hour adopter walkthrough that uses timeline paging plus bundled transaction drill-down"
    - path: "guides/incident-playbook.md"
      provides: "Operator recipes that treat `incident_bundle/2` as the default single-transaction incident path"
    - path: "examples/threadline_phoenix/README.md"
      provides: "Runnable example wording that matches the bundled incident endpoint and host-owned auth boundary"
  key_links:
    - from: "README.md"
      to: "guides/domain-reference.md"
      via: "compact investigation routing map"
      pattern: "Threadline\\.timeline/2.*Threadline\\.timeline_page/2.*Threadline\\.incident_bundle/2"
    - from: "guides/domain-reference.md"
      to: "examples/threadline_phoenix/README.md"
      via: "reference incident JSON section"
      pattern: "COMP-EXAMPLE-INCIDENT-JSON"
    - from: "guides/getting-started-saas.md"
      to: "guides/incident-playbook.md"
      via: "shared incident drill-down and host-owned policy wording"
      pattern: "authenticated actor"
---

<objective>
Converge the public docs on one canonical investigation story now that the v1.16 APIs are shipped.

Purpose: satisfy the narrative half of `ADOPT-04` by implementing D-01 through D-06 and D-16 directly in the public docs before contract tests freeze the final literals.
Output: aligned README, guides, and Phoenix example README that teach one routing hierarchy and one default transaction drill-down path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/milestones/v1.16-phases/56-docs-contracts-and-arc-alignment/56-CONTEXT.md
@.planning/milestones/v1.16-phases/56-docs-contracts-and-arc-alignment/56-RESEARCH.md
@.planning/milestones/v1.16-phases/56-docs-contracts-and-arc-alignment/56-PATTERNS.md
@.planning/milestones/v1.16-phases/53-timeline-paging-contract/53-02-SUMMARY.md
@.planning/milestones/v1.16-phases/54-investigation-slice-apis/54-02-SUMMARY.md
@.planning/milestones/v1.16-phases/55-incident-bundle-surface/55-02-SUMMARY.md
@README.md
@guides/domain-reference.md
@guides/getting-started-saas.md
@guides/incident-playbook.md
@guides/production-checklist.md
@examples/threadline_phoenix/README.md

<interfaces>
From README.md:
```markdown
Use `Threadline.timeline/2` for smaller eager slices. When an investigation window is
large enough that you want stable incremental traversal, switch to `Threadline.timeline_page/2`
and continue with the returned `next_cursor` instead of offset pagination.
```

From guides/domain-reference.md:
```markdown
## Exploration API routing (v1.10+)
Contract marker for automated doc checks: **XPLO-03-API-ROUTING**
```

From examples/threadline_phoenix/README.md:
```markdown
Call **`GET /api/audit_transactions/:id/changes`** with that UUID to fetch the curated
incident bundle rendered from **`Threadline.incident_bundle/2`**
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the top-level routing docs around the canonical investigation hierarchy</name>
  <read_first>README.md, guides/domain-reference.md, guides/production-checklist.md, .planning/milestones/v1.16-phases/56-docs-contracts-and-arc-alignment/56-CONTEXT.md, .planning/milestones/v1.16-phases/56-docs-contracts-and-arc-alignment/56-RESEARCH.md, examples/threadline_phoenix/README.md</read_first>
  <files>README.md, guides/domain-reference.md, guides/production-checklist.md</files>
  <action>Update the routing copy per D-01 through D-06. In `README.md`, keep the file concise per D-04: preserve the short value proposition and quick start, then add a compact investigation routing map that explicitly states `Threadline.timeline/2` for smaller eager slices, `Threadline.timeline_page/2` for large stable windows, higher-level investigation helpers for common support questions, and `Threadline.incident_bundle/2` for one transaction incident, with deeper details pushed into guides rather than duplicated. In `guides/domain-reference.md`, keep `## Exploration API routing (v1.10+)` and `COMP-EXAMPLE-INCIDENT-JSON`, but replace the transaction row and reference incident section so `Threadline.incident_bundle/2` is the default transaction drill-down story per D-01 and D-03, while `Threadline.audit_changes_for_transaction/2`, `Threadline.transaction_context/2`, and `Threadline.change_diff/2` are described as advanced/custom composition building blocks per D-02. Keep the authenticated-actor / host-owned tenancy-policy honesty line intact. In `guides/production-checklist.md`, keep it as a pointer hub, not a second routing spec: update the support-question table so the transaction incident row points to the domain-reference routing / playbook flow using the bundled story and preserve the exploration-routing anchor link.</action>
  <verify>
    <automated>rg -n "incident_bundle/2|timeline_page/2|which public API first|authenticated actor|host-owned|tenancy|policy" README.md guides/domain-reference.md guides/production-checklist.md</automated>
    <automated>mix test test/threadline/readme_doc_contract_test.exs test/threadline/exploration_routing_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/incident_playbook_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1</automated>
    <automated>mix test test/threadline/investigation_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `Threadline.timeline/2`, `Threadline.timeline_page/2`, and `Threadline.incident_bundle/2` in one compact routing section.
    - `README.md` does not become a second domain guide: it still points readers to `guides/domain-reference.md` and other guides for detail rather than duplicating SQL or long policy caveats.
    - `guides/domain-reference.md` retains `XPLO-03-API-ROUTING` and `COMP-EXAMPLE-INCIDENT-JSON` while teaching `Threadline.incident_bundle/2` as the default transaction incident API.
    - `guides/domain-reference.md` still mentions `Threadline.audit_changes_for_transaction/2`, `Threadline.transaction_context/2`, and `Threadline.change_diff/2`, but only as lower-level or advanced composition options.
    - `guides/production-checklist.md` still links `domain-reference.md#exploration-api-routing-v110` and now points transaction drill-down readers at the bundled incident story rather than raw composition as the first choice.
  </acceptance_criteria>
  <done>The README and top-level routing docs tell one default investigation story and preserve the host-owned boundary language.</done>
</task>

<task type="auto">
  <name>Task 2: Converge the adopter and operator guides on the bundled incident path</name>
  <read_first>guides/getting-started-saas.md, guides/incident-playbook.md, examples/threadline_phoenix/README.md, .planning/milestones/v1.16-phases/56-docs-contracts-and-arc-alignment/56-CONTEXT.md, test/threadline/investigation_test.exs</read_first>
  <files>guides/getting-started-saas.md, guides/incident-playbook.md, examples/threadline_phoenix/README.md</files>
  <action>Align the walkthrough and playbook copy to the final bundled story. In `guides/getting-started-saas.md`, keep the existing numbered headings and copy-paste posture, but replace the step-8 transaction drill-down snippet so it uses `{:ok, bundle} = Threadline.incident_bundle(audit_transaction_id, repo: MyApp.Repo)` as the default incident read per D-01, while still showing `timeline` and `timeline_page` for slice navigation; move raw `change_diff` discussion to a short advanced-building-block note rather than the main happy path per D-02 and D-03. In `guides/incident-playbook.md`, preserve the five scenario headings and raw SQL sections, but rewrite `## Scenario: single-transaction drilldown` so the API diagnosis uses `Threadline.incident_bundle/2` first and explains that `audit_changes_for_transaction/2` plus `change_diff/2` remain available for custom projections. Keep the top auth disclaimer exactly honest about the host-owned boundary per D-16. In `examples/threadline_phoenix/README.md`, keep the existing bundled endpoint wording as the canonical anchor, tightening phrasing only where needed so it matches the final domain-reference and quickstart literals without reintroducing the old composition story.</action>
  <verify>
    <automated>rg -n "incident_bundle|audit_changes_for_transaction|transaction_context|change_diff|authenticated actor|tenancy|policy" guides/getting-started-saas.md guides/incident-playbook.md examples/threadline_phoenix/README.md</automated>
    <automated>mix test test/threadline/readme_doc_contract_test.exs test/threadline/exploration_routing_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/incident_playbook_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1</automated>
    <automated>mix test test/threadline/investigation_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `guides/getting-started-saas.md` step 8 includes `Threadline.incident_bundle(` and still includes both `Threadline.timeline(filters)` and `Threadline.timeline_page(filters, page_size: 100)`.
    - `guides/getting-started-saas.md` still says the example endpoint requires an authenticated actor and that tenancy rules stay with the host app.
    - `guides/incident-playbook.md` keeps all five `## Scenario:` headings and rewrites the single-transaction API diagnosis to start with `Threadline.incident_bundle(`.
    - `guides/incident-playbook.md` still mentions `Threadline.audit_changes_for_transaction(` or `Threadline.change_diff(` only as lower-level follow-up building blocks, not the default first step for the transaction scenario.
    - `examples/threadline_phoenix/README.md` still contains the bundled endpoint section, the authenticated baseline, and the host-owned tenancy/policy line.
  </acceptance_criteria>
  <done>The quickstart, playbook, and example README all converge on `incident_bundle/2` as the default transaction incident path while preserving the advanced-building-block escape hatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> adopters | Incorrect routing guidance sends adopters toward stale or overly low-level APIs. |
| example README -> other guides | The runnable example is the strongest proof surface; drift between it and other docs creates false expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | T | README and guide routing text | mitigate | Rewrite all touched docs to teach the exact same hierarchy per D-01 through D-06, with `incident_bundle/2` as the default transaction incident API. |
| T-56-02 | I | auth/policy boundary wording | mitigate | Preserve the authenticated-minimum and host-owned tenancy/policy lines verbatim or near-verbatim across the quickstart, playbook, domain reference, and example README. |
| T-56-03 | R | advanced-building-block documentation | mitigate | Keep `audit_changes_for_transaction/2`, `transaction_context/2`, and `change_diff/2` visible as advanced composition tools so maintainers do not think they were removed. |
</threat_model>

<verification>
Run:
- `rg -n "incident_bundle/2|timeline_page/2|which public API first|authenticated actor|host-owned|tenancy|policy" README.md guides/domain-reference.md guides/getting-started-saas.md guides/incident-playbook.md guides/production-checklist.md examples/threadline_phoenix/README.md`
- `mix test test/threadline/readme_doc_contract_test.exs test/threadline/exploration_routing_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/incident_playbook_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1`
- `mix test test/threadline/investigation_test.exs --max-failures 1`
- `mix verify.test`
</verification>

<success_criteria>
- All six public docs teach one investigation routing hierarchy.
- `Threadline.incident_bundle/2` is the default transaction drill-down story everywhere touched in this plan.
- Raw transaction primitives remain documented only as advanced building blocks.
- The host-owned auth/policy boundary stays explicit across the adopter and operator docs.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.16-phases/56-docs-contracts-and-arc-alignment/56-01-SUMMARY.md`
</output>
