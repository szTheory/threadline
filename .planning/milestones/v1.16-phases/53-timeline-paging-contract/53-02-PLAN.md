---
phase: 53-timeline-paging-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "53-01"
files_modified:
  - lib/threadline.ex
  - README.md
  - guides/domain-reference.md
  - guides/getting-started-saas.md
  - test/threadline/query_test.exs
  - test/support/readme_quickstart_fixtures.ex
  - test/threadline/readme_doc_contract_test.exs
  - test/threadline/exploration_routing_doc_contract_test.exs
  - test/threadline/getting_started_saas_doc_contract_test.exs
autonomous: true
requirements:
  - EXPLORE-01
must_haves:
  truths:
    - "The intended paged investigation path is exposed cleanly at the top-level `Threadline` API without changing the behavior or return shape of existing `Threadline.timeline/2` callers."
    - "README and guide surfaces teach one canonical paging story for large investigation windows while preserving `timeline/2` as the simple eager path for smaller slices."
    - "Doc and API tests fail if the new public surface disappears, if docs keep teaching only eager timeline reads for large windows, or if the routing/docs drift from the shipped contract."
  artifacts:
    - path: "lib/threadline.ex"
      provides: "The public Threadline entrypoint for the new paged timeline usage path"
    - path: "README.md"
      provides: "Top-level public API inventory and first-look guidance including the paging surface"
    - path: "guides/domain-reference.md"
      provides: "Canonical exploration routing guidance that names when to use the paged timeline path"
    - path: "guides/getting-started-saas.md"
      provides: "Adopter-facing example of the new investigation paging path"
    - path: "test/threadline/query_test.exs"
      provides: "Top-level API proof that the public surface delegates cleanly without regressing eager timeline behavior"
    - path: "test/support/readme_quickstart_fixtures.ex"
      provides: "Compile-checked README mirror of the new public paged call shape"
    - path: "test/threadline/readme_doc_contract_test.exs"
      provides: "README drift guard for the new public API inventory"
    - path: "test/threadline/exploration_routing_doc_contract_test.exs"
      provides: "Domain-reference drift guard for the canonical investigation routing story"
    - path: "test/threadline/getting_started_saas_doc_contract_test.exs"
      provides: "Quickstart drift guard for the new paging usage path"
  key_links:
    - from: "lib/threadline.ex"
      to: "test/threadline/query_test.exs"
      via: "public delegator and compatibility proof"
      pattern: "timeline"
    - from: "README.md"
      to: "test/support/readme_quickstart_fixtures.ex"
      via: "compile-checked public call shape plus API surface literals"
      pattern: "Threadline.timeline"
    - from: "guides/domain-reference.md"
      to: "test/threadline/exploration_routing_doc_contract_test.exs"
      via: "Exploration API routing section"
      pattern: "XPLO-03-API-ROUTING"
    - from: "guides/getting-started-saas.md"
      to: "test/threadline/getting_started_saas_doc_contract_test.exs"
      via: "investigation walkthrough wording"
      pattern: "Investigate the captured timeline"
---

<objective>
Expose the approved paging contract at the public library surface and teach one canonical usage path for adopters.

Purpose: complete EXPLORE-01 by turning the query-layer paging work into a first-class `Threadline` API and aligning the public docs/contracts around when to use eager timeline reads versus the new paged path.
Output: a top-level public paging entrypoint plus README/guide/test updates that make the contract discoverable and hard to regress.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/milestones/v1.16-phases/53-timeline-paging-contract/53-CONTEXT.md
@.planning/milestones/v1.16-phases/53-timeline-paging-contract/53-01-PLAN.md
@lib/threadline.ex
@lib/threadline/query.ex
@README.md
@guides/domain-reference.md
@guides/getting-started-saas.md
@test/threadline/query_test.exs
@test/support/readme_quickstart_fixtures.ex
@test/threadline/readme_doc_contract_test.exs
@test/threadline/exploration_routing_doc_contract_test.exs
@test/threadline/getting_started_saas_doc_contract_test.exs

<interfaces>
From lib/threadline.ex:
```elixir
def timeline(filters \\ [], opts \\ []), do: Threadline.Query.timeline(filters, opts)
```

From README.md:
```markdown
Threadline combines PostgreSQL trigger capture with semantic actions, then exposes the audit trail through `Threadline.Plug`, `Threadline.record_action/2`, `Threadline.history/3`, `Threadline.timeline/2`, `Threadline.export_json/2`, and `Threadline.as_of/4`.
```

From guides/domain-reference.md:
```markdown
| Incident / time window across rows | `Threadline.Query.timeline/2` | Default listing API for bounded windows |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose the approved paging path at the top-level `Threadline` surface without breaking eager timeline callers</name>
  <read_first>.planning/milestones/v1.16-phases/53-timeline-paging-contract/53-CONTEXT.md, .planning/milestones/v1.16-phases/53-timeline-paging-contract/53-01-PLAN.md, lib/threadline.ex, lib/threadline/query.ex, test/threadline/query_test.exs</read_first>
  <files>lib/threadline.ex, test/threadline/query_test.exs</files>
  <action>Build directly on the query-layer contract from Plan 53-01. Add a clear top-level public entrypoint for paged timeline reads in `Threadline` rather than forcing adopters through `Threadline.Query` internals only; prefer an explicit name such as `timeline_page/2` if no stronger in-repo naming precedent appears during execution. Document the relationship between the eager `timeline/2` path and the new paged path in the `@doc`, and keep `timeline/2` behavior unchanged for compatibility. Extend `test/threadline/query_test.exs` with top-level API proof that the public entrypoint delegates correctly and that eager `timeline/2` still returns the same list semantics it does today.</action>
  <verify>
    <automated>mix test test/threadline/query_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/threadline.ex` exposes the new paged investigation API explicitly and documents when it should be used.
    - `Threadline.timeline/2` keeps its eager `%AuditChange{}` list contract unchanged.
    - `test/threadline/query_test.exs` proves the top-level paged API works and that the legacy eager path still behaves as before.
    - The public entrypoint does not force callers into offset/page-number semantics.
  </acceptance_criteria>
  <done>The paged timeline contract is discoverable from `Threadline` itself and does not regress existing eager callers.</done>
</task>

<task type="auto">
  <name>Task 2: Teach and lock the canonical paging story across README and investigation docs</name>
  <read_first>README.md, guides/domain-reference.md, guides/getting-started-saas.md, test/support/readme_quickstart_fixtures.ex, test/threadline/readme_doc_contract_test.exs, test/threadline/exploration_routing_doc_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs</read_first>
  <files>README.md, guides/domain-reference.md, guides/getting-started-saas.md, test/support/readme_quickstart_fixtures.ex, test/threadline/readme_doc_contract_test.exs, test/threadline/exploration_routing_doc_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs</files>
  <action>Update the top-level README and the two investigation-facing guides so they all teach one canonical story: use the eager `Threadline.timeline/2` path for small/simple slices, and use the new paged timeline API for large investigation windows where stable traversal matters. Keep the docs concrete and product-facing; do not teach offset pagination or raw Ecto cursor internals. In `guides/domain-reference.md`, update the Exploration API routing table and any relevant investigation section pointers so the public routing story names the new paged path honestly. In `guides/getting-started-saas.md`, add only the minimum additional guidance needed to make the new path discoverable without bloating the quickstart. Extend the existing narrow drift-guard posture: add a compile-checked README fixture for the public paged call shape and tighten the doc-contract suites so they fail if the public API inventory or exploration routing story drifts away from the shipped paging contract.</action>
  <verify>
    <automated>mix test test/threadline/readme_doc_contract_test.exs test/threadline/exploration_routing_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/getting_started_fixtures_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` lists the new public paging entrypoint alongside the existing audit trail APIs.
    - `guides/domain-reference.md` teaches when to use the paged path in the exploration routing story instead of leaving large-window investigations on the eager API alone.
    - `guides/getting-started-saas.md` mentions the new path clearly without turning the quickstart into a deep operator manual.
    - The README fixture/doc-contract posture fails if the public paged call shape or public-surface/routing literals drift.
  </acceptance_criteria>
  <done>The public docs all point adopters to the same paged investigation path, and focused tests keep that guidance from drifting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library public API -> adopters | External teams rely on top-level `Threadline` docs and functions first; if the paging path stays hidden, they will keep composing large eager reads manually. |
| docs routing story -> shipped code | README and investigation guides must reflect the real public contract or adopters will learn a stale/non-canonical usage path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-06 | S | public API discoverability | mitigate | Add the paging entrypoint to `Threadline` and README so adopters do not have to infer it from `Threadline.Query` internals. |
| T-53-07 | T | docs routing story | mitigate | Update the exploration routing and quickstart docs to name the new paged path explicitly for large windows. |
| T-53-08 | R | compatibility narrative | mitigate | State clearly that `timeline/2` remains the eager/simple path and avoid implying a breaking return-shape change. |
| T-53-09 | I | offset-pagination guidance | mitigate | Keep docs and examples on keyset semantics only; do not introduce page-number or offset teaching. |
| T-53-10 | E | future helper layering | mitigate | Expose a clean top-level paging API now so Phase 54 can build helper slices on a stable public surface instead of private query details. |
</threat_model>

<verification>
Run:
- `mix test test/threadline/query_test.exs`
- `mix test test/threadline/readme_doc_contract_test.exs test/threadline/exploration_routing_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/getting_started_fixtures_test.exs`
- `mix verify.test`
</verification>

<success_criteria>
- The new timeline paging contract is exposed at the top-level `Threadline` surface and is discoverable in the README.
- Investigation-facing docs teach one canonical eager-vs-paged story for timeline reads.
- Focused API/doc tests catch regressions in both public surface and public guidance.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.16-phases/53-timeline-paging-contract/53-02-SUMMARY.md`
</output>
