# Milestone archive: v1.9 — Production confidence at volume

**Status:** SHIPPED 2026-04-24  
**Phases:** 28–30  
**Total plans:** 6 (execution trees: `.planning/milestones/v1.9-phases/28-telemetry-health-operators-narrative/`, `29-audit-table-indexing-cookbook/`, `30-retention-at-scale-discovery/`)

## Overview

Credible **ops-at-volume** narrative for existing **`Threadline.Telemetry`** events and **`Threadline.Health.trigger_coverage/1`** / **`mix threadline.verify_coverage`**, a durable **`guides/audit-indexing.md`** cookbook with **doc contract** anchors, and **retention-at-scale** guidance in **`guides/production-checklist.md`** tied to **`Threadline.Retention`** plus a **discovery hub** in **`guides/domain-reference.md`** and **README** Maintainer-band pointer. **Docs-first**; **`v0.2.0`** / **`threadline` 0.2.0** unchanged unless a separate release decision is made.

## Phase table

| Phase | Name | Requirements |
| ----- | ---- | -------------- |
| 28 | Telemetry & health operators' narrative | OPS-01, OPS-02 |
| 29 | Audit table indexing cookbook | IDX-01, IDX-02 |
| 30 | Retention at scale & discovery | SCALE-01, SCALE-02 |

**Living snapshot:** requirements copied to this directory as `v1.9-REQUIREMENTS.md`; canonical close state is git history on `main`.

## Phases (detail)

### Phase 28 — Telemetry & health operators' narrative

**Goal:** Per-event operator narrative (**when it fires**, **what to measure**, **degraded signals**) for **`[:threadline, :transaction, :committed]`**, **`[:threadline, :action, :recorded]`**, **`[:threadline, :health, :checked]`**; **`## Trigger coverage (operational)`** for **`Threadline.Health.trigger_coverage/1`** and **`mix threadline.verify_coverage`**.

**Plans:** 28-01, 28-02 — both complete 2026-04-24.

### Phase 29 — Audit table indexing cookbook

**Goal:** **`guides/audit-indexing.md`** with index strategies aligned to **`Threadline.Query`**, **`Threadline.Export`**, optional **`:correlation_id`**, retention/purge; ExDoc extra; **`test/threadline/audit_indexing_doc_contract_test.exs`**.

**Plans:** 29-01, 29-02 — both complete 2026-04-24.

### Phase 30 — Retention at scale & discovery

**Goal:** **`guides/production-checklist.md`** volume / purge cadence H3; export §5 + support hooks; **`guides/domain-reference.md`** **`## Operating at scale (v1.9+)`** hub; **README** discovery paragraph.

**Plans:** 30-01, 30-02 — both complete 2026-04-24.

## Milestone summary

**Key decisions**

- **Docs-only** milestone: no new capture semantics; anchor IDs where heading slugs are ambiguous (**Phase 28**).
- **Indexing guide** frames DDL as **integrator-owned** tradeoffs, not library-mandated migrations.

**Deferred / follow-up**

- Next planning milestone opens on **`.planning/ROADMAP.md`** with fresh **`.planning/REQUIREMENTS.md`** via **`/gsd-new-milestone`** when scope is ready.

**Technical debt**

- **`gsd-sdk query milestone.complete`** still returns **`version required for phases archive`** in this environment; archives written manually to match **v1.7**/**v1.8** closes.
- **`30-HUMAN-UAT.md`** may still list **`mix test`** with Postgres as convenience verification; default CI path unchanged in spirit.

---

_For current project status, see `.planning/ROADMAP.md`._
