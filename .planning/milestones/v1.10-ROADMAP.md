# Milestone archive: v1.10 — Support-grade exploration primitives

**Status:** SHIPPED 2026-04-24  
**Phases:** 31–36 (31–33 core exploration; 34–36 milestone audit + planning hygiene)  
**Total plans:** 4 execution plans with `SUMMARY.md` (31-01, 31-02, 32-01, 33-01); phases **34–36** closed via `VERIFICATION.md` only.

## Overview

Faster **time-to-answer** for support and integrators building **JSON/API** surfaces: **field-level** change presentation (`Threadline.ChangeDiff`, `change_diff/2`), **transaction-scoped** listing (`Threadline.Query.audit_changes_for_transaction/2` + delegator, stable ordering), and **operator routing** docs in **`guides/domain-reference.md`** with doc contracts — **without** LiveView or new capture semantics. Phases **34–36** closed **INT-DOC-01**, **FLOW-TEST-01**, written verification gates, **`PROJECT.md`** alignment (**PLANNING-PROJECT-01**), and Nyquist/SUMMARY matrix hygiene per **`.planning/milestones/v1.10-MILESTONE-AUDIT.md`**.

## Phase table

| Phase | Name | Requirements / scope |
| ----- | ---- | -------------------- |
| 31 | Field-level change presentation | XPLO-01 |
| 32 | Transaction-scoped change listing | XPLO-02 |
| 33 | Operator docs & contracts | XPLO-03 |
| 34 | v1.10 audit hygiene (doc + composed CI) | INT-DOC-01, FLOW-TEST-01; `ChangeDiff` lowercase `op` |
| 35 | v1.10 audit unblock | `34-VERIFICATION.md`; PLANNING-PROJECT-01 |
| 36 | Planning matrix & Nyquist hygiene | SUMMARY `requirements-completed`; `32-VALIDATION` ↔ `32-VERIFICATION` |

**Living snapshot:** requirements copied as **`v1.10-REQUIREMENTS.md`**; canonical close state is git history on **`main`**.

## Phases (detail)

### Phase 31 — Field-level change presentation

**Goal:** Deterministic, JSON-serializable structure per `%AuditChange{}`; documented INSERT / UPDATE / DELETE and absence of **`changed_from`**.

**Plans:** 31-01, 31-02 — both complete 2026-04-24.

### Phase 32 — Transaction-scoped change listing

**Goal:** Documented **`Threadline.Query.audit_changes_for_transaction/2`** and **`Threadline`** delegator; stable ordering; tests for multi-change transactions.

**Plans:** 32-01 — complete 2026-04-24.

### Phase 33 — Operator docs & contracts

**Goal:** **`guides/domain-reference.md`** exploration routing section; checklist cross-link; **`Threadline.ExplorationRoutingDocContractTest`**.

**Plans:** 33-01 — complete 2026-04-24.

### Phase 34 — v1.10 audit hygiene (doc + composed CI)

**Goal:** Close **INT-DOC-01** (`timeline/2` @doc vs query ordering), **FLOW-TEST-01** (txn listing → **`change_diff/2`** in CI); **`ChangeDiff`** lowercase **`op`** normalization for trigger-shaped rows.

**Plans:** — (hygiene; **`34-VERIFICATION.md`**).

### Phase 35 — v1.10 audit unblock

**Goal:** **`34-VERIFICATION.md`**; align **`.planning/PROJECT.md`** with shipped **XPLO-03** (**PLANNING-PROJECT-01**).

**Plans:** — (hygiene; **`35-VERIFICATION.md`**).

### Phase 36 — Planning matrix & Nyquist hygiene

**Goal:** SUMMARY **`requirements-completed`** on **31-01**, **31-02**, **33-01**; **`32-VALIDATION.md`** sign-off aligned with **`32-VERIFICATION.md`**.

**Plans:** — (hygiene; **`36-VERIFICATION.md`**).

## Milestone summary

**Key decisions**

- **Library-first exploration:** JSON/API consumers use **`ChangeDiff`** + **`audit_changes_for_transaction`** instead of a hosted operator UI this cycle.
- **Stable doc anchors:** HTML fragment **`exploration-api-routing-v110`** for ambiguous slugs (same pattern as **LOOP-04**).

**Deferred / follow-up**

- Optional **`*-VALIDATION.md`** for phases **34–36** if Nyquist policy requires audit-only phases to carry templates (**audit** noted *partial* Nyquist for 34–36).
- **integrator-surface:** example app may not expose HTTP for raw txn diff payloads — document if HTTP-only integrators ask.

**Technical debt**

- **`gsd-sdk query milestone.complete`** returned **`version required for phases archive`** — archives written manually (same as **v1.9**).

---

_For current project status, see `.planning/ROADMAP.md`._
