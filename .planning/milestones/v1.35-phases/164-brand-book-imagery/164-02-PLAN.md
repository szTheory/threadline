---
phase: 164-brand-book-imagery
plan: 02
type: execute
wave: 2
depends_on: [164-01]
files_modified:
  - brandbook/index.html
  - .planning/phases/164-brand-book-imagery/evidence/
autonomous: true
requirements: [IMG-01]

must_haves:
  truths:
    - "brandbook/index.html has a Dark / Light / System preview toggle in the page header; dark default, no persistence"
    - "Light page chrome uses values copied verbatim from tokens.css .tl-theme-light (the SSOT), inlined like the existing dark values — zero external requests"
    - "Demonstration panels that show a SPECIFIC surface are theme-invariant (pinned); the toggle flips page chrome only"
    - "Light chrome passes the AA pass: steel/text-soft small labels map to the light lane's text-muted #3B4762"
    - "All existing gates stay green (brand-gate, zero-network, <=300KB)"
  artifacts:
    - path: "brandbook/index.html"
      provides: "data-tl-theme light/system property lanes + header toggle control"
      contains: "data-tl-theme"
---

<objective>
Add a dark/light/system preview toggle to brandbook/index.html so the brand book can be eyeballed on light chrome, mirroring the approved v1.36 `data-tl-theme` triad vocabulary. Page chrome flips; surface-specific demonstration panels stay pinned.
</objective>

<context>
@brandbook/tokens.css (.tl-theme-light is the SSOT for the light values — copy verbatim, no @import/link)
@.planning/phases/164-brand-book-imagery/164-01-SUMMARY.md (section structure, gate suite, evidence pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Light property lane, toggle control, panel pinning, a11y pass</name>
  <files>brandbook/index.html</files>
  <action>
1. Add `:root[data-tl-theme="light"] { ... }` redefining the page-chrome custom properties with values copied VERBATIM from tokens.css `.tl-theme-light`, plus `color-scheme: light`. Add the system variant: `@media (prefers-color-scheme: light) { :root[data-tl-theme="system"] { ...same values... } }`. Zero external requests.
2. Small toggle control in the page header (topbar): Dark / Light / System, dark default, brand-styled (calm, mono caps labels, no emoji). ~10 lines of inline JS setting/removing `data-tl-theme` on `<html>`. NO persistence — reload returns to dark.
3. Pin demonstration panels: audit every panel. Dark-lockup chips, light-lockup chip, favicon context tiles, misuse specimens, imagery specimen grounds, README/docs/social app strips, banned-imagery tiles, picture-demo get explicit literal grounds so they never flip. Toggle flips page chrome only (body, TOC, headers, cards, code blocks, callouts, captions).
4. Light-chrome accessibility: small mono labels on steel/text-soft (#73819C ~= 3.93:1 on white — AA fail) map to the light lane's text-muted #3B4762. Verify code blocks and micro/callout tints with the light values.
  </action>
  <verify>
    <automated>sh -c 'grep -q "data-tl-theme" brandbook/index.html && grep -cE "(src|href)[[:space:]]*=[[:space:]]*[\"'"'"']https?://" brandbook/index.html | grep -qx 0 && node .planning/phases/162-brand-book-v2/tools/brand-gate.mjs brandbook >/dev/null && find brandbook -type f ! -name ".DS_Store" -print0 | xargs -0 du -ck | tail -1 | awk "\$1<=300{exit 0}{exit 1}"'</automated>
  </verify>
  <done>Light/system lanes live, toggle works, surface panels pinned, light chrome AA-clean, gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Evidence + commit + summary</name>
  <files>.planning/phases/164-brand-book-imagery/evidence/</files>
  <action>
Playwright file:// screenshots (e2e createRequire pattern) at 1440 and 390 widths in BOTH modes (set the attribute via page.evaluate before capture) into evidence/ as toggle-{dark,light}-{desktop,mobile}.png. Eyeball all four in both modes. Stage by explicit path (brandbook/index.html + phase dir only; pre-existing lib//examples//test/ changes never staged). Commit "feat(164-02): ...". Write 164-02-SUMMARY.md noting the combined mini-UAT with the imagery section is the pending phase gate.
  </action>
  <verify>
    <automated>sh -c 'for f in toggle-dark-desktop toggle-dark-mobile toggle-light-desktop toggle-light-mobile; do test -s .planning/phases/164-brand-book-imagery/evidence/$f.png || exit 1; done; git diff --cached --name-only | grep -cvE "^(\.planning/|brandbook/)" | grep -qx 0'</automated>
  </verify>
  <done>Four screenshots captured and inspected; staging scoped; SUMMARY written.</done>
</task>

</tasks>

<success_criteria>
- IMG-01 (extension): preview toggle live; light chrome verbatim from tokens.css; surface panels theme-invariant; gates green; evidence in both modes; combined mini-UAT pending as the phase gate.
</success_criteria>
