---
phase: 165-light-mode-strategy
plan: 01
type: research
wave: 1
depends_on: []
files_modified:
  - .planning/phases/165-light-mode-strategy/165-RESEARCH-ECOSYSTEM.md
  - .planning/phases/165-light-mode-strategy/165-RESEARCH-LESSONS.md
  - .planning/phases/165-light-mode-strategy/165-RESEARCH-SURFACE.md
  - .planning/phases/165-light-mode-strategy/165-LIGHT-MODE-RECOMMENDATION.md
autonomous: false
requirements: [LIGHT-01, LIGHT-02]
---

<objective>
Close UAT gap 1 with a decision, not an implementation: one coherent, research-backed recommendation for how Threadline supports light mode — dark stays brand-primary — followed by an explicit user decision. The operator surface is dark-only by recorded decision [136-01] and the v1.31/Phase-144 contract tests literally ban `prefers-color-scheme`/`theme-toggle`; unfreezing that is deliberate, milestone-scale work (candidate v1.36). NOTHING under lib/ or any contract test changes in this phase.

User verbatim driving this: "we probably should support both dark and light mode… dark mode i know we kind of made that central… yeah we should have that be the main way to use it but it's also nice to have light mode in UI… dark mode can cause people eye strain during daytime… think deeply on this… one-shot a perfect set of recommendations so i dont have to think".
</objective>

<protocol>
1. Orchestrator spawns THREE parallel researchers (web access), each writing its own file:
   - **165-RESEARCH-ECOSYSTEM.md**: Phoenix/LiveView/Elixir admin-surface theming idioms — Phoenix LiveDashboard, Oban Web, Kaffy, Backpex, Phoenix 1.7/1.8 generator posture; CSS-custom-property theming for embedded/mountable UIs that don't own `<html>`; `prefers-color-scheme` vs toggle vs host-config; FOUC avoidance; preference persistence options for a LIBRARY surface; how `theme: :dark | :light | :system` would sit beside existing router macro opts; principle of least surprise for adopters. Cited sources.
   - **165-RESEARCH-LESSONS.md**: cross-ecosystem lessons — GitHub, Grafana, Linear, Datadog, Stripe Dashboard, VS Code, rails_admin/avo: default postures, semantic-token architectures that paid dividends, "designed not recolored" discipline, contrast retuning (accent/signal hues darken for white — our brand light lane already does this), WCAG deltas between modes, eye-strain/daytime-readability evidence, footguns (toggle flash, single-bg imagery, hardcoded shadows/overlays). Cited sources.
   - **165-RESEARCH-SURFACE.md**: Threadline change surface (repo reading, no web needed) — style.ex `--tl-*` single-scope seam (themable architecture, dark lane only), component-by-component risk list, `style_contract_test.exs` + Phase-144 antipattern bans to consciously amend, brandbook tokens.json light lane as starting values, Playwright/screenshot-regression implications, example-app/docs surfaces, decision [136-01] unfreeze procedure per GSD conventions.
2. Orchestrator synthesizes **165-LIGHT-MODE-RECOMMENDATION.md**: ONE opinionated recommendation — default posture; selection mechanism + host API shape; token architecture; scope (what stays dark-only); v1.36 phase breakdown with sizing; freeze-amendment procedure; brand-book posture statement now vs post-v1.36. No options menu; alternatives only as brief "rejected because" notes.
3. **Checkpoint (human)**: user reads the recommendation; AskUserQuestion → approve (seed/open v1.36) / adjust / defer. Decision recorded in the phase SUMMARY verbatim.
</protocol>

<verification>
- All three research files exist with cited sources (web lanes) / file-path evidence (surface lane).
- Recommendation document is single-voiced, covers every synthesis bullet, and contains zero hedge constructions ("could consider", "one option might be") outside the rejected-alternatives notes.
- `git status --porcelain lib/ | grep -c '^[MADRC]'` = 0 (nothing staged from lib/); no contract-test files touched.
- User decision recorded verbatim.
</verification>
