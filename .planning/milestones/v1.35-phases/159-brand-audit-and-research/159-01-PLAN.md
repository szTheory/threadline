---
phase: 159-brand-audit-and-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/159-brand-audit-and-research/159-AUDIT.md
autonomous: true
requirements: [AUD-01, AUD-02]

must_haves:
  truths:
    - "User can read a complete 14-section AUDIT.md with a KEEP/TIGHTEN/REWORK/ADD/REMOVE verdict per section"
    - "User can read a 15-dimension 1-10 scorecard establishing the baseline Phase 162's rerun must meet or beat"
    - "User can see a stress-test matrix executed against actual current assets across 8 surfaces"
    - "User can read explicit, evidence-backed findings on the icon-left-of-text antipattern, the baked-in subtitle, and the SVG <text> portability bug"
  artifacts:
    - path: ".planning/phases/159-brand-audit-and-research/159-AUDIT.md"
      provides: "14-section pressure-test audit with verdicts, scorecard, stress-test matrix, and antipattern evidence"
      min_lines: 300
  key_links:
    - from: ".planning/phases/159-brand-audit-and-research/159-AUDIT.md"
      to: "brandbook/logo-primary.svg"
      via: "grep evidence of <text> elements quoted in the audit"
      pattern: "logo-primary"
    - from: ".planning/phases/159-brand-audit-and-research/159-AUDIT.md"
      to: "brandbook/pressure-test.md"
      via: "scorecard baseline comparison (BOOK-07 baseline)"
      pattern: "pressure-test"
---

<objective>
Execute a senior-brand-designer pressure-test of the existing `brandbook/` using the user's 14-section audit framework, producing `159-AUDIT.md` with a KEEP/TIGHTEN/REWORK/ADD/REMOVE verdict per section, a 15-dimension 1-10 scorecard, and an 8-surface stress-test matrix run against the actual current assets — including concrete demonstration of the `<text>` portability bug and the icon-left-of-text + baked-subtitle antipatterns.

Purpose: AUD-01 and AUD-02. This audit is one of the two inputs to the Phase 159 design brief and sets the scorecard baseline Phase 162's pressure-test rerun must meet or beat (BOOK-07).
Output: `.planning/phases/159-brand-audit-and-research/159-AUDIT.md` (analysis artifact only — no modifications to `brandbook/` or `lib/`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-brand-audit-and-research/159-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@brandbook/README.md
@brandbook/brand-book.md
@brandbook/pressure-test.md
@brandbook/tokens.json
@brandbook/logo-primary.svg
@brandbook/logo-primary-light.svg
@brandbook/logo-mark.svg
@brandbook/logo-monochrome.svg
@brandbook/favicon.svg
@brandbook/social-card.svg

Also read during execution (not all inlined here to preserve context): `brandbook/index.html`, `brandbook/tokens.css`, and all seven `brandbook/examples/*.svg` files (components, docs-page, landing-hero, palette, readme-header, terminal, typography).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather hard evidence and execute the AUD-02 stress-test matrix</name>
  <files>.planning/phases/159-brand-audit-and-research/159-AUDIT.md</files>
  <action>
    Read every artifact in `brandbook/` (all 14 top-level files plus the 7 `examples/*.svg`). Then create `159-AUDIT.md` with its evidence-bearing sections first, so Task 2's judgments rest on facts already in the document.

    Part A — Asset inventory and concrete evidence (per CONTEXT.md "Audit (Plan A)" locked decisions):
    1. Inventory table: every brandbook file with size, format, and one-line role.
    2. The `<text>` portability bug, demonstrated — not asserted. Run `grep -n '<text' brandbook/*.svg brandbook/examples/*.svg` and paste the actual output into the audit (11 SVGs contain `<text>` elements, including logo-primary.svg with 2). Explain the mechanism precisely: GitHub's SVG sandbox (Camo proxy + CSP) loads no external fonts and no @font-face, so any `<text font-family="Geist">` silently falls back to the viewer's generic sans — the wordmark the user sees on GitHub is not the wordmark in the file. Name which surfaces are affected (GitHub README, any img-embedded context) and which are not (local browser with fonts installed).
    3. The icon-left-of-text antipattern, named plainly per CONTEXT.md specifics: quote the actual structure of `logo-primary.svg` (squiggly line-with-dots mark at left + plain Geist `<text>` "Threadline") and state why this is the exact pattern v1.35 exists to kill — mark and type share no geometry, the lockup reads as clip-art-plus-label, removing either half loses nothing.
    4. The baked-in subtitle antipattern: show that "FOLLOW WHAT HAPPENED" is hardcoded in the primary lockup as mono `<text>`, violating the v1.35 hard constraint that the primary carries no subtitle (LOGO-03 downstream).

    Part B — Stress-test matrix (AUD-02), executed against actual current assets across exactly these 8 surfaces: GitHub README render, Hex.pm logo slot, HexDocs logo slot, 16px favicon, dark mode, light mode, monochrome, social card. Render as a markdown table with columns: Surface | Asset tested | Behavior on this surface | Evidence | PASS/DEGRADED/FAIL | Severity. Ground each row in the real file: e.g. for 16px favicon, analyze `favicon.svg` geometry (stroke widths and dot radii relative to its viewBox, scaled to a 16px canvas — state the effective px stroke weight); for GitHub README, the `<text>` fallback plus `examples/readme-header.svg` behavior; for monochrome, whether `logo-monochrome.svg` actually survives one color or depends on opacity/multi-tone; for dark/light, the logo-primary vs logo-primary-light split and what happens with no `<picture>` switching. Per CONTEXT.md discretion, evidence is described analysis grounded in file contents — screenshots optional, Phase 162 owns final visual verification.

    Do NOT modify anything in `brandbook/` or `lib/` — this plan only reads those files and writes the audit artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/159-brand-audit-and-research/159-AUDIT.md && grep -q 'logo-primary.svg' .planning/phases/159-brand-audit-and-research/159-AUDIT.md && grep -qi 'FOLLOW WHAT HAPPENED' .planning/phases/159-brand-audit-and-research/159-AUDIT.md && [ "$(grep -ci 'GitHub README\|Hex.pm\|HexDocs\|16px\|dark mode\|light mode\|monochrome\|social card' .planning/phases/159-brand-audit-and-research/159-AUDIT.md)" -ge 8 ]</automated>
  </verify>
  <done>159-AUDIT.md exists containing: the brandbook inventory, pasted grep output proving `<text>` elements in the SVGs, named findings for icon-left-of-text and baked-subtitle antipatterns, and an 8-row stress-test matrix (one row per AUD-02 surface) with per-row evidence and PASS/DEGRADED/FAIL + severity. brandbook/ is unmodified (git status clean for that path).</done>
</task>

<task type="auto">
  <name>Task 2: Write the 14-section pressure-test with verdicts and the 15-dimension scorecard</name>
  <files>.planning/phases/159-brand-audit-and-research/159-AUDIT.md</files>
  <action>
    Expand `159-AUDIT.md` into the complete 14-section framework locked in CONTEXT.md, in this order, each as a numbered `## N. <name>` heading: 1 executive judgment; 2 brand DNA extraction; 3 pressure-test scorecard; 4 stress tests across real surfaces (fold in or reference Task 1's matrix); 5 gaps and risks by severity; 6 recommended upgrades; 7 design token spec; 8 logo and mark system; 9 visual examples/screenshot guidance; 10 brand voice and microcopy; 11 landing page and docs blueprint; 12 repo-ready artifact plan; 13 prioritized action plan; 14 final quality gate.

    Verdict rule (locked): every one of the 14 sections ends with a line formatted exactly `**Verdict:** KEEP` (or TIGHTEN/REWORK/ADD/REMOVE) plus a one-sentence justification. Exactly 14 verdict lines total.

    Scorecard rule (locked): section 3 contains a markdown table of exactly 15 dimensions scored 1-10 with a one-line rationale each, plus a stated total/average. Choose dimensions covering at minimum: distinctiveness, mark/type integration, 16px survival, monochrome survival, dark/light versatility, portability (no font dependency), scalability, voice, palette, typography, token rigor, application coverage, misuse guidance, consistency, and craft. Note explicitly that this scorecard is the baseline Phase 162's pressure-test.md rerun must meet or beat (BOOK-07), and record the existing `brandbook/pressure-test.md` self-assessment as prior art the new baseline supersedes.

    Tone rule (locked, CONTEXT.md specifics): do not flatter, do not create churn for no reason. Brand voice, positioning, palette philosophy, and tokens are believed strong — KEEP/TIGHTEN where evidence supports it. The logo system is the known weak point — be candid; sections 8 and 4 should carry the REWORK weight. "All killer no filler."

    Section 5 (gaps and risks) and section 6 (recommended upgrades) must itemize findings with stable IDs (GAP-01..n, UP-01..n) and severity (critical/major/minor) so Plan 03 can route every REWORK/ADD item to a downstream requirement or descope it with a reason — make each item one routable sentence, not an essay.
  </action>
  <verify>
    <automated>[ "$(grep -c '^\*\*Verdict:\*\*' .planning/phases/159-brand-audit-and-research/159-AUDIT.md)" -eq 14 ] && [ "$(grep -cE '^## (1[0-4]|[1-9])\.' .planning/phases/159-brand-audit-and-research/159-AUDIT.md)" -eq 14 ] && [ "$(grep -cE '\| *[0-9]+ */ *10 *\|' .planning/phases/159-brand-audit-and-research/159-AUDIT.md)" -eq 15 ] && grep -qE 'GAP-0[0-9]' .planning/phases/159-brand-audit-and-research/159-AUDIT.md</automated>
  </verify>
  <done>159-AUDIT.md contains exactly 14 numbered sections each closing with one of the five verdicts (14 verdict lines), a 15-row 1-10 scorecard with rationale and a stated baseline note for BOOK-07, severity-tagged GAP-nn/UP-nn items routable by Plan 03, and the explicit antipattern + `<text>`-bug findings carried from Task 1. AUD-01 and AUD-02 success criteria from ROADMAP Phase 159 items 1-2 are satisfiable by reading this single file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Plan reads committed repo files and writes a planning artifact; no untrusted input, no installs, no code execution paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-01 | Tampering | brandbook/ source assets | mitigate | Tasks are read-only against brandbook/; verify via `git status --porcelain brandbook/` showing no changes |
</threat_model>

<verification>
- `159-AUDIT.md` exists in the phase directory; `brandbook/` and `lib/` untouched (`git status --porcelain brandbook/ lib/` empty for this plan's changes).
- 14 sections, 14 verdicts, 15 scorecard rows, 8 stress-test surfaces — all countable via the task greps.
- The three named antipatterns appear with file-level evidence, not adjectives.
</verification>

<success_criteria>
- AUD-01 satisfied: complete 14-section audit with per-section verdicts and 1-10 scorecard, including explicit icon-left-of-text and `<text>` portability findings.
- AUD-02 satisfied: stress-test matrix over GitHub README, Hex.pm, HexDocs, 16px favicon, dark, light, monochrome, social card — executed against the actual current assets.
- Scorecard usable as the BOOK-07 baseline; GAP/UP items routable by Plan 03.
</success_criteria>

<output>
Create `.planning/phases/159-brand-audit-and-research/159-01-SUMMARY.md` when done.
</output>
