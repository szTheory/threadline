---
phase: 195-validated-adversarial-critic-runner-panel
plan: 07
type: execute
wave: 3
depends_on: [195-05, 195-04, 195-06]
files_modified:
  - examples/threadline_phoenix/e2e/critic/report.ts
  - examples/threadline_phoenix/e2e/critic/label.ts
  - .planning/CRITIQUE.md
  - CONTRIBUTING.md
autonomous: false
requirements: [CRITIC-01]

must_haves:
  truths:
    - "critic label is a guided single-annotator TypeScript CLI: reads a committed Tier-A cell, shows its screenshot, masks ledger_id/persona/prior-label behind an ephemeral opaque token, and prompts good/borderline/bad/broken (or better/worse+clear/subtle for pairs) + a REQUIRED evidence string."
    - "Blind test-retest is physically enforced: r1 writes .planning/golden/rounds/r1.json which --round r2 never reads or displays; r2 re-presents reshuffled + re-masked; --round r2 refuses until r1 is committed; --reconcile is the ONLY writer of golden-set.json (keeps only r1==r2 items)."
    - "held_out_ids are frozen — the CLI refuses to enqueue them; --bootstrap seeds poles-first (6 pole anchors) then A/B pairs then mid-range off the 3 target pages (transaction/coverage/retention); --status shows per-lens N vs the >=20 bar and flags provisional/ratchet-blocked lenses."
    - "report.ts regenerates .planning/CRITIQUE.md (never hand-edited) with one row per (cell_id x persona), one column per lens, min() rollup as the leading column, Betterer new/fixed/same/regression idiom, a printed symbol+word+reason legend, and a stated baseline in the header."
    - "The maintainer has run the local labeling + LLM scoring + trust computation and committed the result (golden set exists with verdicts; critic_trust reflects measured per-lens alpha/n/raw); CRITIQUE.md is fresh."
  artifacts:
    - "e2e/critic/report.ts + e2e/critic/label.ts (+ rubric bump/lint subcommands)"
    - ".planning/CRITIQUE.md (regenerated projection)"
    - "CONTRIBUTING.md local labeling + validation runbook"
  key_links:
    - "critic label --reconcile -> golden-set.json -> Plan 04 golden guard + Krippendorff alpha"
    - "report.ts reads .planning/critic-scores/ -> CRITIQUE.md -> Plan 04 conditional freshness guard"
  prohibitions:
    - "NO hand-editing golden-set.json or CRITIQUE.md (the CLI / report.ts are the only writers)."
    - "NO enqueueing held_out_ids (Phase-196 true-north)."
    - "NO dashboard / LiveView route / HTML report / charts / stored praise-narrative (D-08); text, diffable, terminal-first only."
    - "NO color-only signals (symbol + word + reason; survives grep / screen readers / monochrome)."
    - "NO external SaaS visual-diff product name in CRITIQUE.md."
---

<objective>
Deliver the maintainer's two surfaces — the input side (`critic label` blind-round authoring CLI, D-09) and the output side (`CRITIQUE.md` projection, D-08) — then run the local validation loop that makes the golden set actually exist with verdicts and populates the measured `critic_trust` block. This closes CRITIC-01 and lights up the whole gate; the labeling + scoring + validation is a maintainer-local act (needs `ANTHROPIC_API_KEY` + human judgment), gated by a human checkpoint.

Purpose: CRITIC-01 — the hand-labeled golden set with maintainer verdicts exists, authored through the guided CLI, projected into the reviewable CRITIQUE.md.
Output: `report.ts`, `label.ts`, committed `CRITIQUE.md`, the runbook, and a validated (or explicitly provisional) `critic_trust` block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-CONTEXT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-RESEARCH.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-PATTERNS.md
@brandbook/pressure-test.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-05-SUMMARY.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: report.ts — CRITIQUE.md projection</name>
  <read_first>195-CONTEXT.md D-08 (three surfaces one primary, Betterer idiom, per-finding located line, symbol+word+reason signal design, baseline-in-header, do-NOT-build list); 195-PATTERNS.md "report.ts" + "CRITIQUE.md" (DESIGN-SYSTEM.md freshness mechanic, row-is-substring guard); brandbook/pressure-test.md (no-praise/cite-evidence microcopy)</read_first>
  <files>examples/threadline_phoenix/e2e/critic/report.ts, .planning/CRITIQUE.md</files>
  <action>Implement report.ts (invoked by run.ts): read .planning/critic-scores/ and regenerate .planning/CRITIQUE.md via the same freshness-tested projection mechanic as DESIGN-SYSTEM.md — one row per (cell_id x persona), one column per lens, min() rollup as the leading column, deltas + flags in-cell (Betterer new/fixed/same/regression). Each finding is one located line: score + band + delta-vs-floor + cited evidence (region/selector/mechanical line) + plain "why" + a suggested direction (NOT an auto-fix). Print the symbol+word+reason legend once at top (up-triangle/down-triangle + signed delta + gain/regression; "~ unstable" + reason, value shown null not 0, excluded from net-positive; the vetoed marker + "not scored" + tripped token; the untrusted marker + "lens not validated" with the number shown but told not to act on it) — never color-only; "unknown" must never read as "bad". State the baseline in the header. Microcopy per pressure-test doctrine (no praise, cite evidence, imperative). Generate an initial committed CRITIQUE.md against current critic-scores (may be near-empty until Task 3 scoring); never hand-edit; no external SaaS tool names. Do NOT build any dashboard/LiveView/HTML/chart surface.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx tsc --noEmit -p tsconfig.json && test -f ../../../../.planning/CRITIQUE.md</automated>
  </verify>
  <acceptance_criteria>report.ts type-checks; CRITIQUE.md generated with legend + baseline header + (cell x persona) rows + min() leading column + Betterer flags; text/diffable only; no color-only signals; no external SaaS names.</acceptance_criteria>
  <done>The reviewable CRITIQUE.md projection surface exists and regenerates from critic-scores.</done>
</task>

<task type="auto">
  <name>Task 2: label.ts — blind-round authoring CLI + rubric bump/lint</name>
  <read_first>195-CONTEXT.md D-09 (guided CLI, ID masking, blind r1/r2 enforcement, --reconcile/--status/--bootstrap/--add/--revalidate, rubric bump prints invalidation blast radius, rubric lint, refute --check runs only verify.mechanical, held_out refuse, first-run empty state) + D-01 (verdict buckets + pairwise margin) + D-05 (per-lens semver+sha8); 195-PATTERNS.md "label.ts" (no analog — follow D-09 directly) + "rubrics/*.md" (version header + sha8)</read_first>
  <files>examples/threadline_phoenix/e2e/critic/label.ts</files>
  <action>Implement label.ts (routed from run.ts label + rubric subcommands). critic label [--round r1|r2] [--lens] [--page] [--pairs] [--resume]: reads a committed Tier-A cell, shows its screenshot (iTerm2 inline or OS viewer — exact mechanism is Claude's discretion), masks ledger_id/persona/prior-label behind an ephemeral opaque token, prompts keyboard-driven good/borderline/bad/broken (or better/worse + clear/subtle for --pairs) + a REQUIRED evidence string (Prodigy doctrine: one decision per screen, keystroke-driven, batched). Blind enforcement: r1 writes .planning/golden/rounds/r1.json; --round r2 never reads/displays r1, re-presents reshuffled + re-masked, and refuses to run until r1 is committed; --reconcile presents only r1!=r2 disagreements (keep/drop/tiebreak) and is the ONLY writer of golden-set.json (keeps r1==r2 items). --status shows per-lens N vs the >=20 bar and flags provisional/ratchet-blocked lenses. --bootstrap seeds poles-first (6 pole anchors) -> A/B pairs -> mid-range off the 3 locked target pages (transaction/coverage/retention); --add appends a cell; --revalidate --lens re-queues only the bumped lens. Refuse to enqueue held_out_ids ("Phase-196 true-north"). critic rubric bump <lens> [--patch|--minor|--major] bumps semver, recomputes sha8, and prints the invalidation blast radius; critic rubric lint asserts hash==disk, <=3 dims, poles resolve; critic refute --check <fixture> runs only verify.mechanical ($0 LLM). First-run empty state prints the guided path and exits 0.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx tsc --noEmit -p tsconfig.json && node --import tsx critic/run.ts rubric lint; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>label.ts type-checks; blind r1/r2 enforced (r2 cannot see r1; refuses until r1 committed); --reconcile is sole golden-set.json writer; held_out refused; --bootstrap poles-first; rubric bump prints blast radius; rubric lint passes against the 6 committed rubrics.</acceptance_criteria>
  <done>The guided blind-round authoring lane + rubric maintenance commands exist; the CLI is the only golden-set writer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Local golden-set labeling, refute proof, and trust measurement (human checkpoint)</name>
  <action>Halt for the maintainer-local validation act (requires ANTHROPIC_API_KEY + human judgment; runs outside CI). Follow the steps in how-to-verify: seed + blind-label the golden set via the CLI, prove the refute battery, score the golden cells and record measured per-lens trust, regenerate CRITIQUE.md, and confirm ci.all stays green — committed as one reviewed commit. Do not auto-advance past this checkpoint.</action>
  <what-built>The full critic machinery is in place: runner core (Plan 05), panel + veto + refute (Plan 06), rubrics (Plan 02), refute fixtures (Plan 03), trust gate + Krippendorff alpha (Plan 04), and the authoring + report surfaces (this plan). What remains is the maintainer-local act that needs ANTHROPIC_API_KEY + human judgment: labeling the golden set, running the critic, and recording measured trust.</what-built>
  <how-to-verify>
    1. Load the key into the shell (nothing auto-sources .env): `set -a; source .env; set +a` (never commit .env / the key).
    2. Seed and label the oracle: run `cd examples/threadline_phoenix/e2e && npm run critic:label -- --bootstrap`, complete round 1 (`--round r1`), commit rounds/r1.json, then round 2 (`--round r2`) reshuffled, then `--reconcile`. Target >=20 lens-judgments per critic-bearing lens (`--status` shows the bar; lenses under 20 stay `provisional` and cannot ratchet — that is acceptable for this phase).
    3. Prove the critic (refute battery, ~$1–3): `npm run critic:validate` (dry-run first: `-- --dry-run`) — footguns low, polished high, prefers known-better A/B, detects injected regression, veto-ordering twin emits no aesthetic score.
    4. Score the golden cells + write critic_trust (two steps): `npm run critic:score -- --golden` (dry-run first; scores only labeled (cell,lens) pairs into critic-scores/), then `mix critic.measure` which computes per-lens {alpha, raw_agreement, pairwise_acc, n, ci95, golden_rubric_version, model_id, validated} into design-system-ledger.json. A lens is validated:true ONLY if alpha>=0.67 AND n>=20 AND raw>=80% at the current rubric+model (pairwise_acc is null until the label CLI persists pair margins; never gating).
    5. Regenerate the report: CRITIQUE.md auto-regens at the end of `critic:score`; standalone `node --import tsx critic/run.ts report` -> confirm .planning/CRITIQUE.md is fresh and reviewable.
    6. Confirm CI stays honest: `mix ci.all` green (verify.critic_trust enforces whatever was recorded; verify.ui_critique and critic.measure remain excluded).
    Commit the golden set, rounds, critic-scores, CRITIQUE.md, and the updated critic_trust block as a single reviewed commit (rescoring is never auto-green).
  </how-to-verify>
  <resume-signal>Type "approved" once the golden set exists with verdicts, the refute battery passes, critic_trust reflects measured per-lens agreement (validated or explicitly provisional), CRITIQUE.md is fresh, and ci.all is green — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer labels -> golden-set.json -> trust gate | oracle integrity gates the whole ratchet; blind test-retest makes solo labels defensible |
| screenshot content in labeler + scoring -> third-party API | dev-only local tooling sends captured UI to Anthropic |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-22 | Tampering | golden set authored to pass (label leakage) | high | mitigate | CLI physically masks IDs + prior labels; r2 cannot read r1; --reconcile keeps only agreeing items; CLI is the sole writer |
| T-195-23 | Tampering | held_out_ids labeled/tuned (Goodhart on Phase-196 true-north) | high | mitigate | CLI refuses to enqueue held_out_ids; Plan-04 guard asserts held_out never appear in items |
| T-195-24 | Repudiation | rescore silently flipping validated:true | high | mitigate | --update-golden / trust recompute lands as a separate reviewed commit, never auto-green; verify.critic_trust re-asserts the bar in ci.all |
| T-195-25 | Information Disclosure | screenshot/DOM to third-party API during labeling/scoring | medium | accept | Documented dev-only boundary; synthetic stress fixtures + committed scorecards, no production data; opt-in via key |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `run.ts rubric lint` exits 0.
- `.planning/CRITIQUE.md` exists, regenerated, freshness-guarded by Plan 04.
- Human checkpoint approved: golden set exists with verdicts, refute passes, critic_trust measured, ci.all green.

Wave note: this plan stays in Wave 3 but depends on Plan 06 (`refute.ts`) — Task 3 runs `npm run critic:validate` (the refute battery), so 195-06 and 195-07 run sequentially within Wave 3 (not in parallel).
</verification>

<success_criteria>
The hand-labeled golden set with maintainer good/bad/rank verdicts exists (authored via the guided blind-round CLI), the reviewable CRITIQUE.md projection regenerates from critic-scores, and the measured per-lens critic_trust is recorded (validated or provisional) — CRITIC-01 closed and the gate lit.
</success_criteria>

## Artifacts this plan produces
- `examples/threadline_phoenix/e2e/critic/report.ts` (CRITIQUE.md projection)
- `examples/threadline_phoenix/e2e/critic/label.ts` (blind-round authoring CLI + rubric bump/lint + refute --check)
- `.planning/CRITIQUE.md` (regenerated projection)
- CONTRIBUTING.md local labeling + validation runbook
- Maintainer-committed: populated `.planning/golden/golden-set.json`, `rounds/r1.json`+`r2.json`, `.planning/critic-scores/`, measured `critic_trust` block

<output>
Create `.planning/phases/195-validated-adversarial-critic-runner-panel/195-07-SUMMARY.md` when done.
</output>
