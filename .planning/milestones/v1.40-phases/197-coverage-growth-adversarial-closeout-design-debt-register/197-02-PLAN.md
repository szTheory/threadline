---
phase: 197-coverage-growth-adversarial-closeout-design-debt-register
plan: 02
type: execute
wave: 2
depends_on: [197-01]
files_modified:
  - lib/threadline/operator_surface/live/actor_live.ex
  - test/threadline/operator_surface/live/actor_live_test.exs
autonomous: false
requirements: [PROOF-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A maintainer-ratified candidate list of at least 4 (page, lens) cells exists, ranked from a FRESH cache-busted route.* score run — never from the stale local medians in RESEARCH"
    - "Iteration-1 edit is landed on the ratified top candidate page with its LiveView test file flipped in the c6f9355e assert-to-refute pattern, and mix verify.mechanical + verify.critic_trust + the page's LiveView tests are green before any paid gate run"
    - "The maintainer's gate verdict (ACCEPT / REJECT / VOID) for iteration 1 is recorded verbatim in the checkpoint record with delta and noise floor numbers"
    - { statement: "A gate delta exactly equal to the IQR noise floor is treated as NOT-ACCEPT (ACCEPT requires delta strictly greater than noise), per gate.ts ACCEPT_REJECT_RULE and the 196 precedent (+7 > 4.5)", verification: backstop }
    - "A gate run with no before-pole snapshot returns VOID with an instruction, never a fabricated verdict (guarded by 197-01 and gate.ts's existing missing-before path)"
    - { statement: "When two candidate cells tie on blocking-lens score, selection order is fixed by the maintainer's explicit ratification at the Task-1 checkpoint, not by implicit sort order", verification: backstop }
  artifacts:
    - "lib/threadline/operator_surface/live/actor_live.ex (or the ratified top candidate's LiveView) — iteration-1 density/typography edit with a HEEx rationale comment"
    - "test/threadline/operator_surface/live/actor_live_test.exs — assert-to-refute flips naming where each removed signal now lives"
  key_links:
    - "edit in <page>_live.ex → captured by npm run capture:pages → scored by critic:gate against the cache-busted before pole from Task 1"
    - "committed page.<x>.happy twin → mix verify.mechanical floor (must stay green; never recapture Tier-A page.* scorecards)"
  prohibitions:
    - statement: "Candidate selection and gate verdicts must never be driven by page.* or story.* cells, nor by advisory-lens (hierarchy / color_contrast) findings — route.* cells and the 4-lens blocking panel only (196-D8, 196-D2)"
      status: flagged
---

<objective>
PROOF-02 iteration 1: select the phase's gate targets from a fresh maintainer-run score sweep, land one real presentation improvement on the weakest ratified (page, lens) cell — expected `route.actor` × `density` (blocking-lens median 54.5 in the last local sweep) — and obtain the maintainer's gate verdict. The signoff/pivot bookkeeping for this verdict is Plan 03 Task 1 (196-05/196-06 precedent split).

Per OQ-2: retention's 196 accept counts as bonus only — Phase 197 targets 2–3 NEW accepted pages.

Purpose: prove the loop drives real UI change on the lowest-scoring pages (PROOF-02), with pivot capacity (≥4 ranked candidates) because honest REJECT is a valid outcome.
Output: ratified candidate ranking, one committed candidate edit + flipped tests, one recorded gate verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-PATTERNS.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-06-SUMMARY.md
@CONTRIBUTING.md
</context>

## Artifacts this phase produces

Plan-02 scope symbols:
- Edited HEEx render blocks in `actor_live.ex` (or ratified alternate) — no new public functions, no component API (invariant). Rationale comment prefix convention: `<%!-- Density (197-02, signal-to-chrome): ... --%>`.
- Renamed/flipped ExUnit tests in the page's `_live_test.exs` describing where each signal now lives.
- Checkpoint record (in SUMMARY): ranked candidate table + verbatim gate verdict (delta, noise floor, per-lens panel numbers).

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Maintainer runs fresh capture + cache-busted before scores; ratifies the ranked candidate list (≥4)</name>
  <files>none (maintainer-local, gitignored score outputs)</files>
  <read_first>
    - CONTRIBUTING.md "Forward-only gate — run one iteration" (runbook, ~lines 312-383)
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md (§Current Score Landscape — the STALE pre-ranking to be confirmed or corrected)
  </read_first>
  <action>
    Paid steps are maintainer-local by design ([196-D9]: ANTHROPIC_API_KEY is unset in the executor env; the LLM never runs in CI). Present the maintainer these paste-ready commands and wait:

    1. `cd examples/threadline_phoenix && ./e2e/run-e2e.sh` (seeded dev server up)
    2. `cd e2e && npm run capture:pages`
    3. For each of the five routes (timeline, coverage, retention, actor, evidence): `rm .planning/critic-verdict-cache/route.<x>__dark-1280__*.json` then `npm run critic:score -- --page route.<x>` — this is the BEFORE pole; it must never be re-run after any edit (the 197-01 guard now enforces this; use --fresh-before only when deliberately restarting a cell's iteration).
    4. Maintainer pastes back the per-route blocking-panel numbers (brand_fidelity, density, typography, rhythm only — advisory lenses never drive selection per [196-D2]).

    Then present the ranked candidate list for ratification, seeded from RESEARCH but re-ranked by the fresh numbers. Expected shape (≥4 candidates, per Pitfall 4): (1) actor×density, (2) evidence×density as an IA restructure NOT chrome removal (196-06 caveat), (3) timeline×typography, (4) coverage×density, (5, stretch) timeline×rhythm only with --n 7 escalation (rhythm is the least stable lens; VOID = REJECT-equivalent per [196-D1] practice). Ties between cells are broken by the maintainer's explicit ordering, recorded in the checkpoint. Maintainer ratifies the ordered list and confirms iteration-1 target.
  </action>
  <instructions>
    Run these locally (paid, maintainer-only per 196-D9):
    1. `cd examples/threadline_phoenix && ./e2e/run-e2e.sh`
    2. `cd e2e && npm run capture:pages`
    3. Per route x in {timeline, coverage, retention, actor, evidence}: `rm .planning/critic-verdict-cache/route.<x>__dark-1280__*.json && npm run critic:score -- --page route.<x>` (BEFORE pole — never re-run after any edit; use --fresh-before only to deliberately restart a cell)
    4. Paste back the per-route blocking-panel numbers (brand_fidelity, density, typography, rhythm) and your ratified ordered candidate list (≥4) with the iteration-1 target named.
  </instructions>
  <verification>Fresh blocking-panel numbers exist for all 5 routes with a capture timestamp; the ordered candidate list has ≥4 (route.* cell, blocking lens) entries; the iteration-1 target is explicitly named.</verification>
  <resume-signal>Maintainer replies with the score table and the words "candidates ratified, iteration-1 target: route.<x> / <lens>".</resume-signal>
  <verify>Maintainer has pasted fresh blocking-panel scores for all 5 routes and an explicit ordered candidate list with iteration-1 target named.</verify>
  <acceptance_criteria>
    - Checkpoint record contains 5 routes × 4 blocking lenses of FRESH numbers (not the RESEARCH medians) with capture timestamp.
    - Ordered candidate list has ≥4 entries, each a (route.* cell, blocking lens) pair; no advisory lens and no page.*/story.* cell appears as a driver.
    - Iteration-1 target explicitly ratified by the maintainer.
  </acceptance_criteria>
  <done>Fresh before poles exist for all five routes; ranked candidates ratified; iteration-1 target locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author iteration-1 edit on the ratified target + flip its LiveView tests (deterministic gates green)</name>
  <files>lib/threadline/operator_surface/live/actor_live.ex, test/threadline/operator_surface/live/actor_live_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/live/actor_live.ex (current render — the file being edited; if the ratified target is a different page, substitute that page's _live.ex and _live_test.exs throughout)
    - test/threadline/operator_surface/live/actor_live_test.exs (current assertions to flip)
    - Output of `git show c6f9355e` — the gate-ACCEPTED retention density edit and its test hunk (the exact edit + test-flip pattern per 197-PATTERNS.md)
    - lib/threadline/operator_surface/live/retention_history_live.ex (the landed precedent in place, including the HEEx rationale-comment style)
  </read_first>
  <behavior>
    - Test 1: page still renders every load-bearing signal (statuses, counts, deep links) via its surviving carrier — assert semantic attributes (e.g. data-status) and surviving copy.
    - Test 2: each removed duplicated-chrome string no longer renders — refute the removed copy, with a comment naming where that duty now lives.
    - Test 3: failed/danger-state twin asserts the semantic attribute rather than banner prose.
  </behavior>
  <action>
    Author the human-quality density edit on the ratified target page in the c6f9355e shape: remove chrome that restates signal owned elsewhere on the page (duplicated status banners, self-labels beside already-marked controls, redundant helper prose), and leave a HEEx `<%!-- ... --%>` rationale comment prefixed `Density (197-02, signal-to-chrome):` naming the lens and where each removed duty now lives. Respect the `if Code.ensure_loaded?(Phoenix.LiveView) do` wrapper; touch presentation only — capture/query/auth semantics untouched (standing invariant). If the ratified target is evidence×density, the edit must be the IA restructure (merge the six one-row sections' scaffolding), not chrome removal (196-06 caveat). Flip the page's tests in the assert-to-refute pattern (197-PATTERNS.md §LiveView test edits): rename tests to describe the surviving carrier, refute removed copy, assert semantic data attributes. Do NOT recapture or touch committed `page.*` Tier-A scorecards (known drift, 196-06); do NOT touch the ledger, floors, or targets in this task. Commit as `feat(197-02): <what was removed/merged and why>`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/actor_live_test.exs && mix verify.mechanical && mix verify.critic_trust && mix compile --warnings-as-errors && mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - Target page's LiveView test file passes with 0 failures, containing at least one refute on removed copy and at least one assertion on a semantic data attribute.
    - `mix verify.mechanical` 18/0 and `mix verify.critic_trust` 22/0.
    - `git diff HEAD~1 --name-only` shows only the target page's _live.ex + _live_test.exs (no ledger, no scorecards, no floors).
    - The edited template contains a HEEx rationale comment with the `197-02` phase-lens prefix.
  </acceptance_criteria>
  <done>Iteration-1 edit committed with flipped tests; all deterministic gates green; nothing but the two page files changed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Maintainer re-captures, busts cache, runs the gate, and ratifies the iteration-1 verdict</name>
  <files>none (maintainer-local)</files>
  <read_first>
    - CONTRIBUTING.md runbook (~lines 337-367 — the iteration pipeline)
  </read_first>
  <action>
    Present paste-ready commands (target cell = the ratified iteration-1 cell, e.g. route.actor / density):

    1. `cd examples/threadline_phoenix/e2e && npm run capture:pages` (AFTER capture)
    2. `rm .planning/critic-verdict-cache/route.<x>__dark-1280__*.json` (mandatory cache bust — belt-and-braces even with the 197-01 screenshot-keyed cache)
    3. `npm run critic:gate -- --page route.<x> --lens <lens>` — the ONLY post-edit scoring command (never critic:score; the before pole is protected by 197-01). Use `--n 7` if the verdict is unstable.
    4. `mix verify.mechanical` (deterministic floor on the committed page.<x>.happy twin)

    Maintainer pastes the full gate output (verdict, delta, noise floor, per-lens panel, regressions) and ratifies ACCEPT / REJECT / VOID. Record verbatim. Reminder for ratification: ACCEPT requires delta strictly greater than the IQR noise floor AND no blocking-lens regression AND floor green AND oracle stable; VOID counts as REJECT for planning. Do NOT append the signoff here — the signoff+pin (on ACCEPT) or pivot bookkeeping (on REJECT) is Plan 197-03 Task 1, so the ledger append and its GATE-04 pin land as one atomic commit there.
  </action>
  <instructions>
    Run these locally against the ratified iteration-1 cell (route.<x> / <lens>):
    1. `cd examples/threadline_phoenix/e2e && npm run capture:pages`
    2. `rm .planning/critic-verdict-cache/route.<x>__dark-1280__*.json`
    3. `npm run critic:gate -- --page route.<x> --lens <lens>` (only post-edit scoring command; add --n 7 if unstable)
    4. `mix verify.mechanical`
    5. Paste the full gate output and state your verdict: ACCEPT / REJECT / VOID.
  </instructions>
  <verification>The verbatim gate output is recorded with numeric delta and noise floor; verify.mechanical at gate time is green; no ledger or critic_trust_test change has been made.</verification>
  <resume-signal>Maintainer replies with the gate output and the word ACCEPT, REJECT, or VOID for the iteration-1 cell.</resume-signal>
  <verify>Checkpoint record contains the verbatim gate output and an explicit maintainer verdict word (ACCEPT / REJECT / VOID) for the iteration-1 cell.</verify>
  <acceptance_criteria>
    - Recorded verdict includes numeric delta and numeric noise floor.
    - `mix verify.mechanical` result at gate time recorded as green.
    - No ledger / critic_trust_test change exists yet (`git status --porcelain .planning/design-system-ledger.json test/threadline/operator_surface/critic_trust_test.exs` empty).
  </acceptance_criteria>
  <done>Iteration-1 gate verdict ratified and recorded; ledger untouched pending Plan 03's atomic signoff commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM verdict → ratchet ledger | a fabricated/stale verdict could mint an unearned signoff |
| executor ↔ maintainer checkpoint | executor must not simulate paid steps or verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-197-04 | Spoofing | gate verdict provenance | high | mitigate | verdict pasted verbatim by maintainer at a blocking checkpoint; executor never fabricates scores (196-D9) |
| T-197-05 | Tampering | before/after poles + cache | high | mitigate | 197-01 screenshot-keyed cache + pole guard; manual cache-bust retained in commands |
| T-197-06 | Repudiation | ACCEPT/REJECT decision | medium | mitigate | verdict + numbers recorded verbatim in checkpoint record and SUMMARY |
</threat_model>

<verification>
- Ranked candidate list (≥4) ratified from fresh scores; iteration-1 edit committed with green LiveView tests, mechanical 18/0, critic_trust 22/0; gate verdict recorded verbatim.
</verification>

<success_criteria>
One real candidate improvement is landed on the ratified weakest page with honest, cache-busted before/after evidence and a maintainer-ratified verdict — ready for Plan 03's signoff-or-pivot.
</success_criteria>

<output>
Create `.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-02-SUMMARY.md` when done (include the ranked candidate table and the verbatim verdict).
</output>
