---
phase: 116-example-first-hour-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 116-01
files_modified:
  - examples/threadline_phoenix/README.md
  - test/threadline/readme_doc_contract_test.exs
autonomous: true
requirements:
  - EXAMPLE-02
  - EXAMPLE-03
  - EXAMPLE-04
must_haves:
  truths:
    - "README opens with `## Choose your path` decision table (Track A / Track B / getting-started) before long install prose."
    - "Single `## Base install (all paths)` runbook skips generators on committed checkout; Track A needs no `mix demo.seed`."
    - "`## Mix task reference` and `## Mix task ownership` tables disambiguate `ecto.setup` vs `demo.seed` vs generators."
    - "`## Demo walkthrough data` heading preserved; body compresses to Track B pointer."
    - "`mix verify.doc_contract` and `mix verify.example` green at phase closeout."
  artifacts:
    - path: examples/threadline_phoenix/README.md
      provides: "EXAMPLE-02/03 first-hour runbook restructure"
    - path: test/threadline/readme_doc_contract_test.exs
      provides: "EXAMPLE-04 root README / example demo + ownership literal locks"
---

<objective>
Restructure `examples/threadline_phoenix/README.md` for unambiguous clean-clone vs walkthrough-fiction paths (EXAMPLE-02) and generator/task ownership clarity (EXAMPLE-03), extend root doc-contract tests (EXAMPLE-04 remainder), and run full phase verification.

Purpose: Install steps currently imply generators on every clone and blur `ecto.setup` vs `demo.seed` — evaluators hit README traps before reaching the auth-staged curl from Plan 01.
Output: README runbook restructure (~150–250 LOC doc delta) + extended `readme_doc_contract_test.exs` + green verify aliases.
</objective>

<context>
@.planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-02, D-116-03, D-116-04c–e)
@.planning/phases/116-example-first-hour-fixes/116-RESEARCH.md (§2.3 README friction, §2.9 mix task ground truth, plan split §8)
@.planning/phases/116-example-first-hour-fixes/116-01-PLAN.md (auth subsection + curl already landed — avoid merge conflicts)
@examples/threadline_phoenix/README.md (Prerequisites, Installation, Demo walkthrough data)
@examples/threadline_phoenix/WALKTHROUGH.md §1 (skip generators on clean clone vocabulary)
@examples/threadline_phoenix/mix.exs (`ecto.setup`, `demo.seed`, `setup` aliases)
@test/threadline/readme_doc_contract_test.exs (`demo seed and reset`, `REF-01` tests)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-116-07 | Removing `## Demo walkthrough data` breaks root doc contract | high | Keep exact heading; compress body only per D-116-02f |
| T-116-08 | Removing `mix threadline.install` from README breaks REF-01 contract | high | Retain generator commands in `## Regenerating the skeleton` + `## Mix task ownership` table |
| T-116-09 | Two full duplicated install tracks increase drift | medium | D-116-02b — one Base install, short Track A/B forks only |
| T-116-10 | Plan 01 auth subsection overwritten during restructure | medium | Depends on 116-01; do not edit Audited HTTP auth subsection or curl block |
| T-116-11 | Terminology regression — bare "seed" means demo fiction | low | Lock `neutral` + `walkthrough fiction` pair in contract |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add `## Choose your path` + `## Base install (all paths)`</name>
  <files>examples/threadline_phoenix/README.md</files>
  <read_first>
    - examples/threadline_phoenix/README.md (## Prerequisites through ## Installation step 7)
    - .planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-02a, D-116-02c, D-116-03b)
    - examples/threadline_phoenix/WALKTHROUGH.md §1 skip-generators language
  </read_first>
  <action>
1. Immediately after `## Prerequisites`, add **`## Choose your path`** with this decision table (copy column structure; wording may tighten for readability):

| Goal | Start here | Requires `mix demo.seed`? |
|------|------------|---------------------------|
| First audited write (`POST /api/posts`) on migrated DB | **Track A** | **No** |
| Maintainer walk, seeded operators, `/audit` exercises | **Track B** + [WALKTHROUGH.md](./WALKTHROUGH.md) | **Yes** |
| Threadline in your own Phoenix app | [getting-started-saas.md](../../guides/getting-started-saas.md) | N/A |

2. Replace `## Installation (Threadline capture + first audited table)` with **`## Base install (all paths)`** containing a **single** numbered list:

   1. `mix deps.get` + `mix compile`
   2. `pg_isready -h "${DB_HOST:-localhost}" -p "${DB_PORT:-5432}"`
   3. `mix ecto.create` (first machine only)
   4. `mix ecto.migrate`
   5. Optional convenience: `mix ecto.setup` (create + migrate + neutral `priv/repo/seeds.exs`)

3. Add **committed-checkout callout** at top of Base install (prose blockquote or bold lead-in) with literal phrase:

`skip generators on a normal clean clone`

Explain: migrations for Threadline capture, triggers, and Sigra auth are **already committed** — do **not** run `mix threadline.install`, `mix threadline.gen.triggers`, or `mix sigra.install` on a normal clone. Generators belong in [Regenerating the skeleton](#regenerating-the-skeleton-generator-contract) only.

4. **Remove** old Installation steps 4–5 (unconditional generator commands) and **step 7** (redundant `mix run priv/repo/seeds.exs` — note in Base install that `ecto.setup` already runs neutral seeds).

5. Do **not** add Track A/B body yet (Task 2).
  </action>
  <acceptance_criteria>
    - `grep -F '## Choose your path' examples/threadline_phoenix/README.md` matches
    - `grep -F '## Base install (all paths)' examples/threadline_phoenix/README.md` matches
    - `grep -F 'skip generators on a normal clean clone' examples/threadline_phoenix/README.md` matches
    - `grep -F '## Installation (Threadline capture' examples/threadline_phoenix/README.md` returns no matches
    - `grep -F 'mix run priv/repo/seeds.exs' examples/threadline_phoenix/README.md` returns no matches OR appears only as "redundant if `ecto.setup` ran" prose in Base install
  </acceptance_criteria>
  <verify>grep -n 'Choose your path\|Base install\|skip generators' examples/threadline_phoenix/README.md</verify>
</task>

<task type="auto">
  <name>Task 2: Add Track A / Track B forks + compress Demo walkthrough data</name>
  <files>examples/threadline_phoenix/README.md</files>
  <read_first>
    - examples/threadline_phoenix/README.md (## Demo walkthrough data lines 95–113; Audited HTTP path from Plan 01)
    - .planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-02b, D-116-02d–g)
    - .planning/phases/116-example-first-hour-fixes/116-01-PLAN.md (do not modify auth subsection)
  </read_first>
  <action>
1. After `## Base install (all paths)`, add:

**`## Track A — First audited write`**
- Steps: `mix phx.server` → browser login at `/users/log_in` → follow **Authenticate before the audited API call** under Audited HTTP path → optional `GET /api/audit_transactions/:id/changes`
- State explicitly: **does not require `mix demo.seed`**
- Pointer to Audited HTTP path section (do not duplicate curl block)

**`## Track B — Walkthrough fiction`**
- Pointer to [WALKTHROUGH.md](./WALKTHROUGH.md)
- Commands: `mix demo.seed` and `mix demo.reset`
- Credentials: [DEMO_USERS.md](DEMO_USERS.md)

2. Replace body of **`## Demo walkthrough data`** (keep heading **exactly**) with compressed pointer:
   - Track B + WALKTHROUGH owns walkthrough fiction
   - Cross-link `## Mix task reference` (Task 3)
   - Retain existing one-liners if present:
     - `` `ecto.setup` does **not** run `demo.seed` automatically ``
     - `` `mix ecto.reset` is schema/trigger recovery only — use `mix demo.reset` for the daily walkthrough loop ``

3. Terminology rule in Base install or Track B prose:
   - **`priv/repo/seeds.exs` = neutral seeds** (two posts)
   - **`mix demo.seed` = walkthrough fiction** — never use "seed" alone when meaning demo

4. Do **not** edit `### Authenticate before the audited API call` or curl block from Plan 01.
  </action>
  <acceptance_criteria>
    - `grep -F '## Track A — First audited write' examples/threadline_phoenix/README.md` matches
    - `grep -F '## Track B — Walkthrough fiction' examples/threadline_phoenix/README.md` matches
    - `grep -F '## Demo walkthrough data' examples/threadline_phoenix/README.md` matches
    - `grep -F 'does **not** run `demo.seed` automatically' examples/threadline_phoenix/README.md` matches
    - `grep -F 'walkthrough fiction' examples/threadline_phoenix/README.md` matches at least once
    - `grep -F 'neutral' examples/threadline_phoenix/README.md` matches in seeds terminology context
  </acceptance_criteria>
  <verify>grep -n 'Track A\|Track B\|Demo walkthrough data\|walkthrough fiction' examples/threadline_phoenix/README.md</verify>
</task>

<task type="auto">
  <name>Task 3: Mix task reference + Mix task ownership appendix</name>
  <files>examples/threadline_phoenix/README.md</files>
  <read_first>
    - .planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-02d, D-116-03a–e — verbatim table rows)
    - examples/threadline_phoenix/mix.exs (alias definitions)
    - examples/threadline_phoenix/README.md (existing MIX_ENV / app.config note near gen.triggers)
  </read_first>
  <action>
1. Add **`## Mix task reference`** summary table after Track B (or adjacent to Demo section):

| Task | Runs | Demo fiction? |
|------|------|---------------|
| `mix setup` | deps + compile + `ecto.setup` | No |
| `mix ecto.setup` | create + migrate + neutral `priv/repo/seeds.exs` | No |
| `mix ecto.reset` | drop + `ecto.setup` | No |
| `mix demo.seed` | walkthrough fiction | **Yes** |
| `mix demo.reset` | truncate demo tables + re-seed fiction | **Yes** |

2. Add **`## Mix task ownership`** appendix with three-owner paragraph then full table (minimum rows — implement verbatim or tighten wording without losing discriminative "Don't confuse with" column):

Paragraph must name: **Ecto** (DB + apply SQL), **Threadline** (audit schema/trigger generators), **Sigra** (auth generators), **this example** (neutral `seeds.exs` + `demo.*` fiction).

Appendix table minimum rows from D-116-03d:

| Task | Owns | When to run | Don't confuse with |
|------|------|-------------|-------------------|
| `mix ecto.create` | Database object | First machine / missing DB | `threadline.install` (files, not DB) |
| `mix threadline.install` | Migration files: capture function, audit tables | Greenfield / new base migrations | `ecto.migrate`; `ecto.create` |
| `mix threadline.gen.triggers --tables …` | Per-table trigger migrations; reads `app.config` | After install; same `MIX_ENV` as CI | `threadline.install`; auditing `audit_*` tables |
| `mix ecto.migrate` | Applies all pending SQL | After any new migration file | Generator tasks (emit only) |
| `mix sigra.install …` | Sigra auth migrations + modules | Generator-fresh app without committed Sigra migrations | `threadline.*`; **skip on this checkout** |
| `mix ecto.setup` | create + migrate + `priv/repo/seeds.exs` | Convenience bootstrap | `demo.seed`; does **not** install triggers by itself |
| `mix demo.seed` | Walkthrough fiction | After migrate; WALKTHROUGH / `/audit` | `ecto.setup`; not auto-run |
| `mix demo.reset` | Truncate demo tables + re-seed fiction | Walkthrough recovery | `ecto.reset` |
| `mix ecto.reset` | drop + `ecto.setup` | Schema/trigger recovery | `demo.reset` |
| `mix setup` | deps + compile + `ecto.setup` | Quick bootstrap after Postgres up | Starting Postgres; `demo.seed` |

3. Add **greenfield callout** (blockquote or short section) pointing to `guides/getting-started-saas.md` with generator order: `threadline.install` → `threadline.gen.triggers --tables …` → `ecto.migrate` → Sigra when adopting sigra-reference lane.

4. Ensure **`## Regenerating the skeleton (generator contract)`** (or existing equivalent heading) retains:
   - `mix threadline.install`
   - `mix threadline.gen.triggers --tables posts`
   - `mix sigra.install Accounts User users --no-live --no-organizations --no-passkeys --no-admin --yes`
   - Existing **`MIX_ENV` / `app.config`** note adjacent to `gen.triggers` — do not bury only in table
  </action>
  <acceptance_criteria>
    - `grep -F '## Mix task reference' examples/threadline_phoenix/README.md` matches
    - `grep -F '## Mix task ownership' examples/threadline_phoenix/README.md` matches
    - `grep -F 'mix threadline.install' examples/threadline_phoenix/README.md` matches at least twice (ownership table + regenerating skeleton)
    - `grep -F 'mix threadline.gen.triggers' examples/threadline_phoenix/README.md` matches
    - `grep -F 'skip on this checkout' examples/threadline_phoenix/README.md` matches
    - `grep -F 'MIX_ENV' examples/threadline_phoenix/README.md` matches near gen.triggers prose
    - `grep -F 'getting-started-saas.md' examples/threadline_phoenix/README.md` matches in greenfield callout
  </acceptance_criteria>
  <verify>grep -n 'Mix task reference\|Mix task ownership\|threadline.install\|skip on this checkout' examples/threadline_phoenix/README.md</verify>
</task>

<task type="auto">
  <name>Task 4: Extend root doc-contract tests (EXAMPLE-04 remainder)</name>
  <files>test/threadline/readme_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/readme_doc_contract_test.exs (`demo seed and reset`, `REF-01` tests)
    - .planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-04c, D-116-04d)
  </read_first>
  <action>
1. Extend test `"example README documents demo seed and reset tasks"` (or add sibling `"example README documents first-hour path and task ownership"`) with asserts on `examples/threadline_phoenix/README.md`:

```elixir
assert String.contains?(doc, "`mix ecto.reset` is schema/trigger recovery only")
assert String.contains?(doc, "`mix demo.reset` for the daily walkthrough loop")
assert String.contains?(doc, "Mix task ownership")
assert String.contains?(doc, "neutral")
assert String.contains?(doc, "walkthrough fiction")
assert String.contains?(doc, "skip generators on a normal clean clone")
assert String.contains?(doc, "## Choose your path")
assert String.contains?(doc, "## Base install (all paths)")
```

**Retain** existing asserts in that test: `## Demo walkthrough data`, `mix demo.seed`, `mix demo.reset`, `DEMO-MANIFEST.md`, `DEMO_USERS.md`, `does **not** run `demo.seed` automatically`.

2. Verify `"example README carries runbook literals for REF-01"` still passes — if install section moved, ensure `mix threadline.install`, `mix threadline.gen.triggers`, `mix phx.server`, `mix test`, `ecto.migrate` remain somewhere in README.

3. Do **not** lock full curl fences, DEMO passwords, WALKTHROUGH ticket literals, or phx.new flag block per D-116-04d.
  </action>
  <acceptance_criteria>
    - `grep -F 'Mix task ownership' test/threadline/readme_doc_contract_test.exs` matches
    - `grep -F 'skip generators on a normal clean clone' test/threadline/readme_doc_contract_test.exs` matches
    - `grep -F 'walkthrough fiction' test/threadline/readme_doc_contract_test.exs` matches
    - `mix test test/threadline/readme_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/readme_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 5: Phase closeout verification</name>
  <files></files>
  <read_first>
    - .planning/phases/116-example-first-hour-fixes/116-VALIDATION.md (closeout commands)
    - .planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-04e)
  </read_first>
  <action>
From repository root, run canonical verify entrypoints:

```bash
mix verify.doc_contract
mix verify.example
```

If either fails, fix regressions in Plans 01–02 scope (README literals, conn_case, contracts) until both exit 0.

Optional manual Track A golden path (document in commit message if run): migrate → phx.server → browser login → cookie curl → `201` + `audit_transaction_id` without `mix demo.seed`.
  </action>
  <acceptance_criteria>
    - `mix verify.doc_contract` exits 0
    - `mix verify.example` exits 0
    - All four requirement IDs satisfied: EXAMPLE-01 (Plan 01), EXAMPLE-02, EXAMPLE-03, EXAMPLE-04 (combined)
  </acceptance_criteria>
  <verify>mix verify.doc_contract && mix verify.example</verify>
</task>

</tasks>

<verification>
Run after all tasks:

```bash
mix test test/threadline/readme_doc_contract_test.exs
mix test test/threadline/example_phoenix_readme_contract_test.exs
mix verify.doc_contract
mix verify.example
```

All must exit 0 before marking plan and phase complete.
</verification>

<success_criteria>
- EXAMPLE-02: Clean-clone vs `demo.seed` path unambiguous; Track A needs no demo fiction.
- EXAMPLE-03: Generator/migration/demo task responsibilities clear via ownership tables and skip-generators callout.
- EXAMPLE-04: Full doc-contract + example verify green at phase closeout.
</success_criteria>
