---
phase: 115-narrative-doc-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 115-01
files_modified:
  - README.md
  - guides/getting-started-saas.md
  - test/threadline/readme_doc_contract_test.exs
  - mix.exs
autonomous: true
requirements:
  - NARR-02
  - NARR-03
must_haves:
  truths:
    - "README Semantics bullet leads with `Threadline.Audit.transaction/3`; opening API list still enumerates both helper and primitive."
    - "getting-started intro states reciprocal agreement with how-threadline-works on `Audit.transaction/3`."
    - "Cross-doc contract (or readme extension) asserts all three NARR docs contain `Threadline.Audit.transaction/3`."
    - "`mix verify.doc_contract` includes `audit_doc_contract_test.exs` if missing."
  artifacts:
    - path: README.md
      provides: "NARR-02 discovery map and Semantics bullet fix"
    - path: guides/getting-started-saas.md
      provides: "NARR-02 intro cross-link to mental-model guide"
    - path: test/threadline/readme_doc_contract_test.exs
      provides: "optional cross-doc NARR-02 lock"
    - path: mix.exs
      provides: "verify.doc_contract topology includes audit_doc_contract_test"
---

<objective>
Align README and getting-started discovery cross-links with the retargeted how-threadline-works guide (NARR-02), add optional cross-doc doc-contract coverage (NARR-03 extension), and close verify.doc_contract gate drift for `audit_doc_contract_test.exs`.

Purpose: NARR-02 failure mode is conflicting hierarchy across README → guides, not missing links.
Output: Consistent discovery order and `mix verify.doc_contract` green for all narrative contracts.
</objective>

<context>
@.planning/phases/115-narrative-doc-sync/115-CONTEXT.md (D-115-03a through D-115-03g, D-115-04e, D-115-04g)
@.planning/phases/115-narrative-doc-sync/115-01-PLAN.md (how-threadline-works must land first)
@README.md (Start here, What you get → Semantics, L10 API list)
@guides/getting-started-saas.md (intro paragraph)
@test/threadline/readme_doc_contract_test.exs
@test/threadline/audit_doc_contract_test.exs
@mix.exs (`verify.doc_contract` alias list)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-115-04 | readme_doc_contract_test fails after Semantics bullet change | medium | Keep L10 opening API laundry list unchanged; only edit Semantics bullet and optional sentence |
| T-115-05 | verify.doc_contract alias drift hides audit contract | low | Append audit_doc_contract_test to alias per D-115-04g |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fix README Semantics bullet and optional blessed-path sentence</name>
  <files>README.md</files>
  <read_first>
    - README.md (L10 opening paragraph, ## What you get, ## Start here)
    - test/threadline/readme_doc_contract_test.exs (existing positive asserts — do not remove record_action locks)
    - .planning/phases/115-narrative-doc-sync/115-CONTEXT.md (D-115-03c, D-115-03d, D-115-03e)
  </read_first>
  <action>
1. Replace the **Semantics** bullet under `## What you get` (currently line ~44):

From:
`- **Semantics:** actor, intent, correlation, and request context via `Threadline.record_action/2`.`

To:
`- **Semantics:** `Threadline.Audit.transaction/3` as the recommended audited write path (actor, intent, correlation, and request context); `Threadline.record_action/2` is the semantic primitive the helper wraps.`

2. Add **one sentence** immediately after the opening paragraph (after L10, before "Use it when you want"):

`New Phoenix integrations should use `Threadline.Audit.transaction/3`; see [Getting started](guides/getting-started-saas.md) §6.`

3. Under `## Start here`, ensure the **Understanding** and **Adopting** bullets both name `Threadline.Audit.transaction/3` (Adopting bullet already does — tighten Understanding bullet to add: "both paths converge on `Threadline.Audit.transaction/3` for audited writes").

4. Do **not** remove `Threadline.record_action/2` from the L10 opening API enumeration.
  </action>
  <acceptance_criteria>
    - `grep -F 'recommended audited write path' README.md` matches in What you get section
    - `grep -F 'Threadline.Audit.transaction/3' README.md` matches at least 3 times (opening, Start here, What you get or new sentence)
    - `grep -F 'Threadline.record_action/2' README.md` still matches in opening paragraph
    - `grep -F 'semantic primitive' README.md` matches
  </acceptance_criteria>
  <verify>grep -n 'Audit.transaction\|record_action\|recommended audited' README.md</verify>
</task>

<task type="auto">
  <name>Task 2: getting-started intro reciprocal cross-link</name>
  <files>guides/getting-started-saas.md</files>
  <read_first>
    - guides/getting-started-saas.md (intro lines 1–8)
    - guides/how-threadline-works.md (post plan 01 — blessed path phrase)
    - .planning/phases/115-narrative-doc-sync/115-CONTEXT.md (D-115-03g)
  </read_first>
  <action>
After the existing sentence "If you want the architecture/persona/JTBD map first, read [How Threadline works](how-threadline-works.md) before you wire anything.", append a new paragraph:

`This guide and [How Threadline works](how-threadline-works.md) both treat `Threadline.Audit.transaction/3` as the recommended audited write path; §6 below is the canonical runnable snippet.`

Do not change §6 snippet or headings.
  </action>
  <acceptance_criteria>
    - `grep -F 'both treat' guides/getting-started-saas.md` matches
    - `grep -F 'Threadline.Audit.transaction/3' guides/getting-started-saas.md` matches in intro (first 15 lines)
    - `grep -F 'recommended write path' guides/getting-started-saas.md` still matches in §6 (existing phrase preserved)
  </acceptance_criteria>
  <verify>head -12 guides/getting-started-saas.md | grep -E 'both treat|Audit.transaction'</verify>
</task>

<task type="auto">
  <name>Task 3: Cross-doc NARR-02 contract test in readme_doc_contract_test</name>
  <files>test/threadline/readme_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/readme_doc_contract_test.exs
    - .planning/phases/115-narrative-doc-sync/115-CONTEXT.md (D-115-04e)
  </read_first>
  <action>
Add test `"NARR discovery docs agree on Audit.transaction/3 literal"` that reads three files and asserts each contains `Threadline.Audit.transaction/3`:

- `README.md`
- `guides/how-threadline-works.md`
- `guides/getting-started-saas.md`

Do not remove or weaken existing readme contract asserts (especially `record_action/2` in opening API list if locked).

If README optional sentence from Task 1 is present, also assert:

`assert String.contains?(readme, "New Phoenix integrations should use")`
  </action>
  <acceptance_criteria>
    - `grep -F 'NARR discovery docs agree' test/threadline/readme_doc_contract_test.exs` matches
    - `grep -F 'how-threadline-works.md' test/threadline/readme_doc_contract_test.exs` matches in new test
    - `mix test test/threadline/readme_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/readme_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 4: Wire audit_doc_contract_test into verify.doc_contract</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (`verify.doc_contract` alias lines ~80–82)
    - test/threadline/ci_topology_contract_test.exs (if it locks doc_contract file list)
    - test/threadline/audit_doc_contract_test.exs
  </read_first>
  <action>
1. If `test/threadline/audit_doc_contract_test.exs` is **not** already in the `verify.doc_contract` list in `mix.exs`, append:

`test/threadline/audit_doc_contract_test.exs`

to the alias argument list (preserve ordering: group with other threadline doc contract tests).

2. If `test/threadline/ci_topology_contract_test.exs` asserts the exact `verify.doc_contract` file list, update that test to include `audit_doc_contract_test.exs` in the expected topology string.

3. Run `mix verify.doc_contract` and fix any failures from Tasks 1–3 before completing.
  </action>
  <acceptance_criteria>
    - `grep 'audit_doc_contract_test' mix.exs` matches inside `verify.doc_contract` alias
    - `mix verify.doc_contract` exits 0
    - `mix test test/threadline/ci_topology_contract_test.exs` exits 0 (if topology test exists and was updated)
  </acceptance_criteria>
  <verify>mix verify.doc_contract</verify>
</task>

</tasks>

<verification>
Run after all tasks:

```bash
mix verify.doc_contract
mix test test/threadline/readme_doc_contract_test.exs test/threadline/how_threadline_works_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/audit_doc_contract_test.exs
```

Both must exit 0.
</verification>

<success_criteria>
- NARR-02: README, getting-started, and how-threadline-works agree on discovery order and blessed write path.
- NARR-03: Cross-doc literal lock across three guides; verify.doc_contract gate includes audit contract test.
</success_criteria>
