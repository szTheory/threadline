---
phase: 118-pilot-prep-optional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/adoption-pilot-backlog.md
  - test/threadline/adoption_pilot_doc_contract_test.exs
autonomous: true
requirements:
  - PILOT-01
must_haves:
  truths:
    - "Adoption-pilot backlog cites named `mix verify.*` entrypoints matching `mix.exs`; no hardcoded test counts."
    - "Evidence pass and In-repo parity sections stay aligned with `mix ci.all` composition."
    - "Doc contract refutes stale `136 tests` and generic `(N tests` patterns."
  artifacts:
    - path: guides/adoption-pilot-backlog.md
      provides: "PILOT-01 evaluator/pilot host evidence pointers"
    - path: test/threadline/adoption_pilot_doc_contract_test.exs
      provides: "PILOT-01 entrypoint locks without ci.all order duplication"
---

<objective>
Refresh `guides/adoption-pilot-backlog.md` verification pointers (PILOT-01): remove stale test counts, cite canonical named entrypoints from `mix.exs`, sync In-repo parity section, bump evidence-pass date. Extend adoption-pilot doc contract in the same changeset.

Purpose: Evaluators memorize `mix verify.*`, not drifting ExUnit cardinality (OSS DNA §1).
Output: Updated guide + extended contract tests; `mix test test/threadline/adoption_pilot_doc_contract_test.exs` green.
</objective>

<context>
@.planning/phases/118-pilot-prep-optional/118-CONTEXT.md (D-118-01, D-118-04a)
@.planning/phases/118-pilot-prep-optional/118-RESEARCH.md
@mix.exs (L76–96 aliases)
@CONTRIBUTING.md (Setup + `mix ci.all` prose)
@test/threadline/ci_topology_contract_test.exs (owns ci.all ordering — do not duplicate)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-118-01 | Stale test count reintroduced | medium | Contract refutes `136 tests` and `~r/\(\d+ tests/` |
| T-118-02 | In-repo parity drifts from ci.all again | medium | List same eight steps or cross-link Evidence pass + CONTRIBUTING |
| T-118-03 | Duplicating ci.all order in adoption_pilot tests | low | Assert presence only; order owned by ci_topology_contract_test |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refresh adoption-pilot evidence pass paragraph (PILOT-01 prose)</name>
  <files>guides/adoption-pilot-backlog.md</files>
  <read_first>
    - guides/adoption-pilot-backlog.md (L5 evidence pass; L126–134 in-repo parity)
    - mix.exs (L88–96 `ci.all` list)
    - CONTRIBUTING.md (L26–37 local gate commands)
    - .planning/phases/118-pilot-prep-optional/118-CONTEXT.md (D-118-01a–d)
  </read_first>
  <action>
1. Replace the **Evidence pass** paragraph (line 5) with updated maintainer date **`2026-05-27`** and prose that:
   - Cites **`DB_PORT=5433 MIX_ENV=test mix ci.all`** as the canonical local gate.
   - Lists ordered steps matching `mix.exs` exactly: `verify.format` → `verify.credo` → compile strict (`compile --warnings-as-errors`) → `verify.compile_no_optional` → `verify.test` → `verify.threadline` → `verify.example` → `verify.doc_contract`.
   - Points to **`CONTRIBUTING.md`** for env setup and CI job table.
   - **Removes** the stale **`136 tests`** phrase entirely — do **not** substitute another numeric total.
   - Keeps existing PgBouncer / **`verify-pgbouncer-topology`** / **CI-PGBOUNCER-TOPOLOGY-CONTRACT** / **STG-01** host-owned language unchanged.

2. Update **## In-repo parity (library CI)** (L126–134) **Full maintainer CI chain** row to either:
   - List the same eight steps as the evidence pass, **or**
   - Say “see **Evidence pass** above” and link **`CONTRIBUTING.md`** for the full chain.

Do not edit STG templates, ExampleCloud matrix, or checklist walkthrough rows in this task.
  </action>
  <acceptance_criteria>
    - `rg -F '136 tests' guides/adoption-pilot-backlog.md` returns no matches
    - `rg -F 'mix ci.all' guides/adoption-pilot-backlog.md` matches at least twice (evidence pass + in-repo parity or cross-link)
    - `rg -F 'verify.credo' guides/adoption-pilot-backlog.md` matches at least once
    - `rg -F 'verify.compile_no_optional' guides/adoption-pilot-backlog.md` matches at least once
    - `rg -F 'verify.example' guides/adoption-pilot-backlog.md` matches at least once
    - `rg -F '2026-05-27' guides/adoption-pilot-backlog.md` matches at least once in evidence pass
    - `rg -F 'CONTRIBUTING.md' guides/adoption-pilot-backlog.md` matches at least once in evidence pass area (first ~10 lines)
  </acceptance_criteria>
  <verify>rg -F '136 tests' guides/adoption-pilot-backlog.md; rg -F 'verify.example' guides/adoption-pilot-backlog.md</verify>
</task>

<task type="auto">
  <name>Task 2: Extend adoption-pilot doc contract (PILOT-01 contracts)</name>
  <files>test/threadline/adoption_pilot_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/adoption_pilot_doc_contract_test.exs (existing version SSOT tests)
    - guides/adoption-pilot-backlog.md (post Task 1 prose)
    - .planning/phases/118-pilot-prep-optional/118-CONTEXT.md (D-118-04a, D-118-04e)
    - test/threadline/ci_topology_contract_test.exs (do not copy ordering asserts)
  </read_first>
  <action>
Add tests to `Threadline.AdoptionPilotDocContractTest`:

1. `"adoption-pilot refutes stale hardcoded test counts (PILOT-01)"`:
   - Read `@guide`.
   - `refute String.contains?(guide, "136 tests")`
   - `refute Regex.match?(~r/\(\d+ tests/, guide)`

2. `"adoption-pilot evidence pass cites canonical verify entrypoints (PILOT-01)"`:
   - `assert String.contains?(guide, "mix ci.all")`
   - `assert String.contains?(guide, "mix verify.doc_contract")`
   - For each of `"verify.format"`, `"verify.credo"`, `"verify.compile_no_optional"`, `"verify.test"`, `"verify.threadline"`, `"verify.example"`: `assert String.contains?(guide, step)`
   - `assert String.contains?(guide, "CONTRIBUTING.md")`

Keep existing version SSOT and upgrade-path link tests unchanged.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/adoption_pilot_doc_contract_test.exs` exits 0
    - File contains test name with `refutes stale hardcoded test counts`
    - File contains test name with `canonical verify entrypoints`
  </acceptance_criteria>
  <verify>mix test test/threadline/adoption_pilot_doc_contract_test.exs</verify>
</task>

</tasks>

<verification_criteria>
- [ ] `mix test test/threadline/adoption_pilot_doc_contract_test.exs` exit 0
- [ ] No `136 tests` or `(N tests` patterns in adoption-pilot guide
- [ ] Guide lists all eight `ci.all` steps by alias name
</verification_criteria>

## PLANNING COMPLETE
