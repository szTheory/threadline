---
phase: 117-evidence-plane-doc-authority
plan: 02
type: execute
wave: 2
depends_on:
  - 117-01
files_modified:
  - test/threadline/how_threadline_works_doc_contract_test.exs
  - test/threadline/upgrade_path_doc_contract_test.exs
  - test/threadline/semver_adopter_doc_contract_test.exs
  - test/threadline/exploration_routing_doc_contract_test.exs
  - test/threadline/readme_doc_contract_test.exs
  - mix.exs
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "`mix verify.doc_contract` includes exploration_routing and passes after Phase 117 contracts."
    - "Evolution section chronology locked with `0.5.0` before `0.6.0` and Evidence + `Audit.transaction/3` literals."
    - "Adopter-band docs refute `v1.2x` and phantom `guides/evidence-plane.md` paths."
    - "Incident JSON contract asserts `Threadline.Audit.transaction/3` in scoped subsection after COMP-EXAMPLE-INCIDENT-JSON."
  artifacts:
    - path: mix.exs
      provides: "verify.doc_contract topology includes exploration_routing"
    - path: test/threadline/semver_adopter_doc_contract_test.exs
      provides: "DOC-02 semver-not-milestone gate"
    - path: test/threadline/how_threadline_works_doc_contract_test.exs
      provides: "Evolution semver chronology locks"
    - path: test/threadline/upgrade_path_doc_contract_test.exs
      provides: "0.5→0.6 minor bullet locks"
    - path: test/threadline/exploration_routing_doc_contract_test.exs
      provides: "incident blessed-path lock"
---

<objective>
Extend minimal-plus doc-contract coverage (DOC-03), wire orphaned `exploration_routing_doc_contract_test.exs` into `mix verify.doc_contract`, and prove green gate.

Purpose: OSS DNA — doc change + contract change = one changeset; evaluators run `mix verify.doc_contract`.
Output: New/extended tests + mix alias update; `mix verify.doc_contract` exit 0.
</objective>

<context>
@.planning/phases/117-evidence-plane-doc-authority/117-CONTEXT.md (D-117-04)
@.planning/phases/117-evidence-plane-doc-authority/117-01-PLAN.md (prose must land first)
@test/threadline/how_threadline_works_doc_contract_test.exs
@test/threadline/upgrade_path_doc_contract_test.exs
@test/threadline/exploration_routing_doc_contract_test.exs
@test/threadline/readme_doc_contract_test.exs
@mix.exs (`verify.doc_contract` alias ~line 80)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-117-04 | Semver refute false-positives on domain-reference | medium | Exclude `guides/domain-reference.md` from refute file list |
| T-117-05 | Incident assert matches wrong section | low | `:binary.match` scope after COMP-EXAMPLE-INCIDENT-JSON only |
| T-117-06 | verify.doc_contract alias regression | low | Run full alias after mix.exs edit |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Evolution semver chronology contract</name>
  <files>test/threadline/how_threadline_works_doc_contract_test.exs</files>
  <read_first>
    - guides/how-threadline-works.md (`## Evolution so far` section)
    - test/threadline/how_threadline_works_doc_contract_test.exs
    - .planning/phases/117-evidence-plane-doc-authority/117-CONTEXT.md (D-117-04 table)
  </read_first>
  <action>
Add a new test `"mental model guide locks Evolution semver chronology (DOC-02/DOC-03)"` that:

1. Reads `guides/how-threadline-works.md`.
2. Asserts section contains `## Evolution so far`, `` `0.5.0` ``, `` `0.6.0` ``, substring `0.6.0` packaged the Evidence plane`, and `Threadline.Audit.transaction/3`.
3. Uses `:binary.match` scoped to Evolution section only: find `{idx_evolution, _}` for `## Evolution so far`, then `{idx_next, _}` for next `## ` heading after evolution (e.g. `## Natural next work`), set `scope = {idx_evolution, idx_next - idx_evolution}`, assert `{idx_05, _}` and `{idx_06, _}` for `` `0.5.0` `` and `` `0.6.0` `` within scope with `idx_05 < idx_06`.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/how_threadline_works_doc_contract_test.exs` exits 0
    - New test name contains `Evolution semver`
  </acceptance_criteria>
  <verify>mix test test/threadline/how_threadline_works_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 2: Upgrade-path 0.5→0.6 minor bullet contract</name>
  <files>test/threadline/upgrade_path_doc_contract_test.exs</files>
  <read_first>
    - guides/upgrade-path.md (`## Upgrade by Threadline minor`)
    - test/threadline/upgrade_path_doc_contract_test.exs
  </read_first>
  <action>
Add test `"upgrade-path guide locks 0.5.x to 0.6.x minor upgrade bullet"` that reads the guide and, with `:binary.match` scoped between `## Upgrade by Threadline minor` and `## What breaks when Phoenix`, asserts:

- `0.5.x → 0.6.x` or `0.5.x -> 0.6.x`
- `Threadline.Audit.transaction/3` OR `Threadline.Evidence`
- `CHANGELOG.md` and `[0.6.0]`
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/upgrade_path_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/upgrade_path_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 3: Semver-not-milestone + no phantom hub refute test</name>
  <files>test/threadline/semver_adopter_doc_contract_test.exs, test/threadline/readme_doc_contract_test.exs</files>
  <read_first>
    - .planning/phases/117-evidence-plane-doc-authority/117-CONTEXT.md (D-117-04 semver + hub rows)
    - test/threadline/readme_doc_contract_test.exs (evidence-plane strip test pattern)
  </read_first>
  <action>
Create `test/threadline/semver_adopter_doc_contract_test.exs` with:

```elixir
defmodule Threadline.SemverAdopterDocContractTest do
  use ExUnit.Case, async: true

  @adopter_paths [
    "README.md",
    "guides/getting-started-saas.md",
    "guides/how-threadline-works.md",
    "guides/upgrade-path.md",
    "guides/adoption-pilot-backlog.md"
  ]

  @milestone_pattern ~r/v1\.2[0-9]/

  test "adopter-band docs avoid internal v1.2x milestone labels" do
    for path <- @adopter_paths do
      doc = File.read!(path)
      refute Regex.match?(@milestone_pattern, doc), "expected no v1.2x in #{path}"
      refute String.contains?(doc, "guides/evidence-plane.md"),
             "expected no phantom evidence-plane hub in #{path}"
    end
  end
end
```

Optionally add one line to existing readme evidence-plane test docstring noting hub refute is centralized in semver test (no duplicate refute required).
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/semver_adopter_doc_contract_test.exs` exits 0
    - Module file exists at `test/threadline/semver_adopter_doc_contract_test.exs`
  </acceptance_criteria>
  <verify>mix test test/threadline/semver_adopter_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 4: Incident JSON blessed-path + wire verify.doc_contract</name>
  <files>test/threadline/exploration_routing_doc_contract_test.exs, mix.exs</files>
  <read_first>
    - test/threadline/exploration_routing_doc_contract_test.exs
    - guides/domain-reference.md (COMP-EXAMPLE-INCIDENT-JSON block)
    - mix.exs verify.doc_contract alias list (~line 80)
  </read_first>
  <action>
1. Add test to `exploration_routing_doc_contract_test.exs`:

After loading `guides/domain-reference.md`, find `{idx_marker, _}` for `COMP-EXAMPLE-INCIDENT-JSON`, then `{idx_next, _}` for `## Support incident queries` (or next `## ` after incident subsection). Set `scope = {idx_marker, idx_next - idx_marker}`. Assert `:binary.match(doc, "Threadline.Audit.transaction/3", scope: scope)` returns a match.

2. Append to `mix.exs` `verify.doc_contract` alias test list (preserve order style of siblings):

`test/threadline/exploration_routing_doc_contract_test.exs`

and

`test/threadline/semver_adopter_doc_contract_test.exs`

3. Run `mix verify.doc_contract` — must exit 0.
  </action>
  <acceptance_criteria>
    - `rg -F 'exploration_routing_doc_contract_test.exs' mix.exs` matches in verify.doc_contract alias
    - `rg -F 'semver_adopter_doc_contract_test.exs' mix.exs` matches in verify.doc_contract alias
    - `mix verify.doc_contract` exits 0
  </acceptance_criteria>
  <verify>mix verify.doc_contract</verify>
</task>

</tasks>

<verification>
- [ ] `mix verify.doc_contract` — exit 0
- [ ] All five test modules in alias or exercised by alias
</verification>
