---
phase: 45-bench-harness-published-baselines
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - guides/performance.md
  - test/threadline/performance_doc_contract_test.exs
autonomous: true
requirements: [PERF-02, PERF-03, PERF-P1]
must_haves:
  truths:
    - The performance guide presents published numbers for all presets
    - The doc-contract test validates headings and the BENCHMARK-ENV block
  artifacts:
    - path: guides/performance.md
      provides: Published numbers for operators
    - path: test/threadline/performance_doc_contract_test.exs
      provides: Contract enforcement for guide drift
---

<objective>
Ship `guides/performance.md` with published baseline numbers and create an ExUnit doc-contract test to lock the guide's structure over time without asserting brittle numeric values.

Purpose: Provide verifiable expectations for production adopters.
Output: A new ExDoc guide and an ExUnit test asserting its headings and markers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-bench-harness-published-baselines/45-RESEARCH.md
@.planning/phases/45-bench-harness-published-baselines/45-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Performance Guide</name>
  <files>guides/performance.md</files>
  <action>
    Create `guides/performance.md`.
    Include markers `PERF-01`, `PERF-02`, `PERF-03`.
    Include headings: `## Workload Presets`, `## Throughput Baselines`, `## Impact on Primary Transactions`, and `## Capture-time cost knobs`.
    Include a `BENCHMARK-ENV` block for the presets containing realistic environment data (PG version, OTP version, hardware, schema preset, `changed_from` flag).
    Document the presets: `cold_single_table`, `warm_loaded`, `concurrent_purge`.
    Add the required PgBouncer transaction-mode confirmation paragraph.
    Provide placeholder numbers that will be replaced during execution by the outputs of `verify.bench`.
  </action>
  <verify>
    <automated>grep "## Workload Presets" guides/performance.md</automated>
  </verify>
  <done>Markdown file exists and contains all required structure.</done>
</task>

<task type="auto">
  <name>Task 2: Performance Doc Contract Test</name>
  <files>test/threadline/performance_doc_contract_test.exs</files>
  <action>
    Create `test/threadline/performance_doc_contract_test.exs`.
    Follow the `test/threadline/audit_indexing_doc_contract_test.exs` analog from `45-PATTERNS.md`.
    Read `guides/performance.md` and assert the presence of `PERF-01`, `PERF-02`, `PERF-03`, `BENCHMARK-ENV`, and all required headings.
    Assert the presence of the exact section `"Capture-time cost knobs"`.
    Do NOT test actual numbers to avoid drift failures.
  </action>
  <verify>
    <automated>mix test test/threadline/performance_doc_contract_test.exs</automated>
  </verify>
  <done>Doc contract test passes and locks the structure of the guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Information | Documentation is publicly available |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Spoofing | Performance baselines | mitigate | Include BENCHMARK-ENV blocks to provide full context on how numbers were generated. |
</threat_model>

<verification>
- Guide clearly communicates the baseline expectations and variations.
- Contract test prevents regressions in documentation structure.
</verification>

<success_criteria>
- `mix test test/threadline/performance_doc_contract_test.exs` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/45-bench-harness-published-baselines/45-04-SUMMARY.md`
</output>