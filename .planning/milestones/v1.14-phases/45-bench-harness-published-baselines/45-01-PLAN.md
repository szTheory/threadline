---
phase: 45-bench-harness-published-baselines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - bench/mix.exs
  - bench/README.md
  - bench/scripts/seed_audit_changes.exs
  - bench/scripts/teardown.exs
autonomous: true
requirements: [PERF-01, PERF-P2]
must_haves:
  truths:
    - Sibling Mix project exists at `bench/` and builds without errors
    - Bench project has independent dependencies (`benchee` etc.)
    - Seed script can load three data presets
  artifacts:
    - path: bench/mix.exs
      provides: Dependency isolation
    - path: bench/scripts/seed_audit_changes.exs
      provides: Data seeding for benchmarks
  key_links:
    - from: bench/mix.exs
      to: ../
      via: path dependency
---

<objective>
Setup the sibling `bench/` Mix app, its README, and the data seed/teardown scripts that support the three benchmarking presets.

Purpose: Prevent benchmarking dependencies (`benchee`, `benchee_html`, etc.) from bleeding into the root library by enforcing a hard sibling app separation.
Output: An independent `bench/` directory containing `mix.exs`, documentation, and state-management scripts for Ecto.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-bench-harness-published-baselines/45-RESEARCH.md
@.planning/phases/45-bench-harness-published-baselines/45-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup independent bench project structure</name>
  <files>bench/mix.exs, bench/README.md</files>
  <action>
    Create the `bench/mix.exs` using the analog pattern from examples/threadline_phoenix/ but optimized for benchee. 
    It MUST specify `{:threadline, path: ".."}` and include `{:benchee, "~> 1.3"}`, `{:benchee_html, "~> 1.0"}`, `{:benchee_markdown, "~> 0.3"}`, `{:ecto_sql, "~> 3.10"}`, and `{:postgrex, "~> 0.17"}` in deps.
    Define `def application` with `[:logger, :postgrex, :ecto_sql]`.
    Create `bench/README.md` explaining the purpose of the directory and outlining the three presets: `cold_single_table`, `warm_loaded`, `concurrent_purge`.
  </action>
  <verify>
    <automated>cd bench && mix deps.get</automated>
  </verify>
  <done>bench/mix.exs compiles and cleanly fetches dependencies via the path boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement state-management scripts</name>
  <files>bench/scripts/seed_audit_changes.exs, bench/scripts/teardown.exs</files>
  <action>
    Create `bench/scripts/seed_audit_changes.exs` and `bench/scripts/teardown.exs` based on the script bootstrapping pattern in `45-PATTERNS.md`.
    Both scripts must ensure `:postgrex` and `:ecto_sql` are started, and start `Threadline.Test.Repo`.
    The `seed_audit_changes.exs` should accept an arg/ENV indicating which of the three presets to seed (`cold_single_table`, `warm_loaded`, `concurrent_purge`) and execute the required Ecto instructions.
    The `teardown.exs` should cleanly truncate the affected tables.
  </action>
  <verify>
    <automated>cd bench && mix format --check-formatted scripts/*</automated>
  </verify>
  <done>Scripts exist and successfully connect to the target Repo without crashing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB Seed | Connects to local dev DB with administrative privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | `seed_audit_changes.exs` | accept | Script is for local benchmarking and test environments only; never shipped to prod. |
</threat_model>

<verification>
- `bench/` project is fully self-contained.
- Sibling app pattern guarantees no leakage of deps to root app.
</verification>

<success_criteria>
- `cd bench && mix deps.get` succeeds.
- Seed and teardown scripts run against `Threadline.Test.Repo`.
</success_criteria>

<output>
After completion, create `.planning/phases/45-bench-harness-published-baselines/45-01-SUMMARY.md`
</output>