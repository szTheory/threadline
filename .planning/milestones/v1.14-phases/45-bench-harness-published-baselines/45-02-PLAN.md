---
phase: 45-bench-harness-published-baselines
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - bench/audit_capture_bench.exs
  - bench/timeline_query_bench.exs
  - bench/redaction_and_changed_from_bench.exs
autonomous: true
requirements: [PERF-01, PERF-03, PERF-P1, PERF-P3, PERF-P4]
must_haves:
  truths:
    - Benchmarks execute and generate outputs
    - Drift is tracked via git rev-parse HEAD
    - EXPLAIN outputs are written to baselines dir
  artifacts:
    - path: bench/audit_capture_bench.exs
      provides: Core benchmarking workload
    - path: bench/redaction_and_changed_from_bench.exs
      provides: Cost knobs comparison workload
---

<objective>
Implement the benchmarking suites that test capture, timeline querying, and redaction cost knobs, and output environment metadata alongside EXPLAIN plans.

Purpose: Generate statistically rigorous performance numbers grounded in realistic presets.
Output: Three benchee scripts executing within the `bench/` sibling app environment.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-bench-harness-published-baselines/45-RESEARCH.md
@.planning/phases/45-bench-harness-published-baselines/45-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core capture and query benchmarks</name>
  <files>bench/audit_capture_bench.exs, bench/timeline_query_bench.exs</files>
  <action>
    Create `bench/audit_capture_bench.exs` and `bench/timeline_query_bench.exs`.
    Both must start `Threadline.Test.Repo` and pin randomness via `:rand.seed(:exsss, {1, 2, 3})`.
    Use `Benchee.run/2` with formatters (console, html, markdown).
    Ensure the scripts create the `bench/baselines/` directory and output there.
    Ensure a `git rev-parse HEAD` marker is captured and written to a metadata JSON file to track drift (PERF-P4).
    Implement the `Bench.Explain` helper (from RESEARCH.md) to capture `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` and save to `bench/baselines/`.
  </action>
  <verify>
    <automated>cd bench && elixirc audit_capture_bench.exs timeline_query_bench.exs</automated>
  </verify>
  <done>Scripts compile and represent the core workload.</done>
</task>

<task type="auto">
  <name>Task 2: Cost knobs benchmark</name>
  <files>bench/redaction_and_changed_from_bench.exs</files>
  <action>
    Create `bench/redaction_and_changed_from_bench.exs`.
    Test variations of capture overhead: with vs without `RedactionPolicy`, and with vs without `--store-changed-from`.
    Pin randomness using `:rand.seed(:exsss, {1, 2, 3})`.
    Use Benchee's inputs or multiple jobs to compare the cost directly.
    Output results to `bench/baselines/`.
  </action>
  <verify>
    <automated>cd bench && elixirc redaction_and_changed_from_bench.exs</automated>
  </verify>
  <done>Cost knobs script compiles and compares the variations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File I/O | Writes JSON files to the local file system (`bench/baselines/`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02 | Tampering | Bench outputs | accept | Outputs are informational baselines intended to be public. |
</threat_model>

<verification>
- Scripts output Benchee Markdown/HTML and EXPLAIN JSON to `bench/baselines/`.
- Output includes a `git rev-parse HEAD` marker to detect stale numbers.
</verification>

<success_criteria>
- The three Benchee scripts exist and compile successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/45-bench-harness-published-baselines/45-02-SUMMARY.md`
</output>