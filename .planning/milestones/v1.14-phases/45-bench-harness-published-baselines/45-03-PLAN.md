---
phase: 45-bench-harness-published-baselines
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - mix.exs
  - test/threadline/ci_topology_contract_test.exs
autonomous: true
requirements: [PERF-01, PERF-P2, PERF-P5]
must_haves:
  truths:
    - The root mix package does not include bench files in its Hex release
    - The `verify.bench` alias runs the sibling app correctly
    - `ci.all` does not run the benchmark suite
  artifacts:
    - path: mix.exs
      provides: Alias mapping and release exclusion
    - path: test/threadline/ci_topology_contract_test.exs
      provides: Constraint asserting CI doesn't run bench
---

<objective>
Wire the `mix verify.bench` alias to execute the sibling app and assert that these heavy benchmarks never execute in `mix ci.all`. Exclude `bench/` from the final package release.

Purpose: Prevent CI wall-time drag and avoid leaking internal benchmarking logic to adopters via Hex tarballs.
Output: Updated root `mix.exs` and topology test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-bench-harness-published-baselines/45-RESEARCH.md
@.planning/phases/45-bench-harness-published-baselines/45-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root mix.exs integration and constraints</name>
  <files>mix.exs</files>
  <action>
    Add a new `"verify.bench"` alias to the `defp aliases` block that delegates to a private `verify_bench/1` function. The function MUST shell out to `bench/` and run `scripts/setup.exs`, followed by `audit_capture_bench.exs`, `timeline_query_bench.exs`, and `redaction_and_changed_from_bench.exs`.
    Ensure `verify.bench` is NOT added to the `ci.all` alias list.
    Add `"guides/performance.md"` to the `docs` extras.
    Ensure `bench/` is NOT in the `package` `files:` list.
  </action>
  <verify>
    <automated>mix help verify.bench | grep "verify.bench"</automated>
  </verify>
  <done>Alias exists and the package files list excludes bench/.</done>
</task>

<task type="auto">
  <name>Task 2: Assert CI Topology Constraints</name>
  <files>test/threadline/ci_topology_contract_test.exs</files>
  <action>
    Add a test to `test/threadline/ci_topology_contract_test.exs` confirming that `verify.bench` does NOT bleed into `mix ci.all`.
    Read `mix.exs` and assert that the `"ci.all"` list does not contain `"verify.bench"`.
  </action>
  <verify>
    <automated>mix test test/threadline/ci_topology_contract_test.exs</automated>
  </verify>
  <done>Test passes validating the PERF-P5 constraint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build Output | Ensure dev/test deps are not published to Hex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Information Disclosure | `mix.exs` Hex files list | mitigate | Exclude `bench/` directory explicitly from Hex release files. |
</threat_model>

<verification>
- `mix verify.bench` exists and works when run manually.
- `mix ci.all` does not trigger `verify.bench`.
</verification>

<success_criteria>
- Topology test passes.
</success_criteria>

<output>
After completion, create `.planning/phases/45-bench-harness-published-baselines/45-03-SUMMARY.md`
</output>