---
phase: 42-example-readme-contract-repair
plan: 01
type: execute
wave: 1
depends_on:
  - 41
files_modified:
  - examples/threadline_phoenix/README.md
  - examples/README.md
  - test/threadline/readme_doc_contract_test.exs
autonomous: true
requirements:
  - DOC-02
  - DOC-03
must_haves:
  truths:
    - "The Phoenix example README is the canonical runnable docs surface for the reference app."
    - "The examples index clearly points readers at the nested Phoenix example README."
    - "The historical reconstruction walkthrough stays explicit about `ThreadlinePhoenix.Post`, `as_of/4`, `cast: true`, `:deleted_record`, and `:before_audit_horizon`."
    - "Doc-contract coverage must fail if the example README walkthrough or index link drift."
  artifacts:
    - path: "examples/threadline_phoenix/README.md"
      provides: "Updated example-app runbook and historical reconstruction walkthrough"
    - path: "examples/README.md"
      provides: "Stable index link to the nested example README"
    - path: "test/threadline/readme_doc_contract_test.exs"
      provides: "Regression coverage for the example README and index literals"
  key_links:
    - from: "examples/README.md"
      to: "examples/threadline_phoenix/README.md"
      via: "index entry for the canonical Phoenix reference app"
      pattern: "threadline_phoenix/README.md"
    - from: "examples/threadline_phoenix/README.md"
      to: "guides/domain-reference.md"
      via: "incident JSON and time-travel reference anchors"
      pattern: "guides/domain-reference.md"
    - from: "examples/threadline_phoenix/README.md"
      to: "guides/production-checklist.md"
      via: "documentation links section"
      pattern: "../../guides/production-checklist.md"
---

<objective>
Align the Phoenix example README and example index with the runnable reference app contract, and lock the surface with tests, without touching the root README again.
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Example README → integrator behavior | Readers copy the nested README as the runnable example, so stale command literals or walkthrough text become real integration drift. |
| Index README → discoverability | `examples/README.md` is the entry path into the nested docs, so a broken or stale link hides the canonical walkthrough. |
| Contract test → docs surface | Tests must stay focused on the example README and nested index so drift fails CI before release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | I | `examples/threadline_phoenix/README.md` | mitigate | Keep the runbook, walkthrough, and link targets aligned with the runnable example app. |
| T-42-02 | T | `examples/README.md` | mitigate | Preserve the canonical pointer to `threadline_phoenix/README.md` so the example remains discoverable. |
| T-42-03 | R | `test/threadline/readme_doc_contract_test.exs` | mitigate | Assert the example README literals and nested index link so copy drift fails CI immediately. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Repair the example README contract and keep the index/test aligned</name>
  <files>examples/threadline_phoenix/README.md, examples/README.md, test/threadline/readme_doc_contract_test.exs</files>
  <read_first>
  - `.planning/ROADMAP.md`
  - `.planning/REQUIREMENTS.md`
  - `.planning/STATE.md`
  - `.planning/phases/42-example-readme-contract-repair/42-CONTEXT.md`
  - `examples/threadline_phoenix/README.md`
  - `examples/README.md`
  - `test/threadline/readme_doc_contract_test.exs`
  - `guides/domain-reference.md`
  - `guides/production-checklist.md`
  - `guides/adoption-pilot-backlog.md`
  </read_first>
  <action>Update `examples/threadline_phoenix/README.md` so the install/runbook literals, audited HTTP explanation, and historical reconstruction walkthrough stay aligned with the runnable Phoenix reference app. Keep the current walkthrough structure, but make sure the example-specific literals stay explicit enough for a reader to copy and run them. Preserve the `examples/README.md` index pointer if it still matches the canonical path, and tighten `test/threadline/readme_doc_contract_test.exs` so it locks the example README and nested index contract without re-opening the root README slice from Phase 41.</action>
  <verify>
  <automated>mix test test/threadline/readme_doc_contract_test.exs --seed 0</automated>
  </verify>
  <done>The example docs surface is explicit, runnable, and locked by tests, with the nested README discoverable from the examples index.</done>
</task>

</tasks>

<verification>
[Overall phase checks]
- `mix test test/threadline/readme_doc_contract_test.exs --seed 0` passes.
- `examples/threadline_phoenix/README.md` contains the runnable walkthrough and reconstruction literals.
- `examples/README.md` still points at the canonical nested README path.

</verification>

<success_criteria>
- DOC-02 is represented in the roadmap and implemented in the Phoenix example docs surface.
- DOC-03 stays enforced for the example README slice without reopening the root README changes from Phase 41.
- The example docs contract is explicit enough that literal drift fails CI immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/42-example-readme-contract-repair/42-01-SUMMARY.md`
</output>
