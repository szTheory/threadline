---
phase: 41-readme-contract-repair
plan: 01
type: execute
wave: 1
depends_on:
  - 40
files_modified:
  - README.md
  - test/threadline/readme_doc_contract_test.exs
autonomous: true
requirements:
  - DOC-01
  - DOC-03
must_haves:
  truths:
    - "The root README is the canonical first-stop docs entry point for the shipped public API surface."
    - "The README quickstart and feature overview name the shipped API entrypoints explicitly."
    - "Doc-contract coverage fails if the README loses the explicit API names or guide links."
  artifacts:
    - path: "README.md"
      provides: "Updated quickstart, feature overview, and documentation links for the root docs contract"
    - path: "test/threadline/readme_doc_contract_test.exs"
      provides: "Regression coverage for the root README contract literals"
  key_links:
    - from: "README.md"
      to: "guides/domain-reference.md"
      via: "main documentation link in the README"
      pattern: "guides/domain-reference.md"
    - from: "README.md"
      to: "guides/production-checklist.md"
      via: "documentation links section"
      pattern: "guides/production-checklist.md"
    - from: "test/threadline/readme_doc_contract_test.exs"
      to: "README.md"
      via: "literal assertions for the root README contract"
      pattern: "Threadline.history|Threadline.timeline|Threadline.export_json|Threadline.as_of|guides/adoption-pilot-backlog.md"
---

<objective>
Align the root `README.md` with the shipped public API surface and lock that contract with tests, without touching the Phoenix example README yet.
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README prose → integrator behavior | Readers may copy the root README as the first implementation guide, so stale API names or missing links become real integration drift. |
| Contract test → docs surface | Tests must stay narrowly tied to the root README so Phase 42 can repair the example README independently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | I | `README.md` | mitigate | Keep the public entrypoints explicit in the top-level docs and preserve the guide links that route readers to deeper material. |
| T-41-02 | T | `test/threadline/readme_doc_contract_test.exs` | mitigate | Assert the exact README literals for the root docs contract so copy drift fails CI immediately. |
| T-41-03 | R | phase boundary | accept | Phase 41 intentionally excludes the Phoenix example README so the milestone can split the repair into two small slices. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Repair the root README contract and lock it with tests</name>
  <files>README.md, test/threadline/readme_doc_contract_test.exs</files>
  <read_first>
  - `.planning/ROADMAP.md`
  - `.planning/REQUIREMENTS.md`
  - `.planning/STATE.md`
  - `.planning/phases/41-readme-contract-repair/41-CONTEXT.md`
  - `README.md`
  - `test/threadline/readme_doc_contract_test.exs`
  - `test/support/readme_quickstart_fixtures.ex`
  - `lib/threadline.ex`
  - `lib/threadline/query.ex`
  - `guides/domain-reference.md`
  - `guides/production-checklist.md`
  - `guides/adoption-pilot-backlog.md`
  </read_first>
  <action>Update the root `README.md` so the summary, `## What you get`, quick start, and documentation links explicitly match the shipped public API surface. Keep the existing capture / semantics / exploration / operations framing, but make the entrypoints concrete by naming `Threadline.Plug`, `Threadline.record_action/2`, `Threadline.history/3`, `Threadline.timeline/2`, `Threadline.export_json/2`, and `Threadline.as_of/4` in the prose or examples. Preserve the guide links to `guides/domain-reference.md`, `guides/production-checklist.md`, `guides/adoption-pilot-backlog.md`, and `CONTRIBUTING.md`. Then tighten `test/threadline/readme_doc_contract_test.exs` so it locks the root README literals that define DOC-01, while leaving the existing example README assertions untouched for Phase 42.</action>
  <verify>
  <automated>mix test test/threadline/readme_doc_contract_test.exs --seed 0</automated>
  </verify>
  <done>The root README presents the shipped docs contract clearly, and the contract test fails if the core README links or API names drift.</done>
</task>

</tasks>

<verification>
[Overall phase checks]
- `mix test test/threadline/readme_doc_contract_test.exs --seed 0` passes.
- `README.md` contains the explicit public API names and the required guide links.

</verification>

<success_criteria>
- DOC-01 is represented in the roadmap and implemented in the root README.
- DOC-03 stays enforced for the root README slice without absorbing the example README repair.
- The README contract is explicit enough that link or API drift fails CI immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/41-readme-contract-repair/41-01-SUMMARY.md`
</output>
