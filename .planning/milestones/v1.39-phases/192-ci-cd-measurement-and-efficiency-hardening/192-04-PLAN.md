---
phase: 192-ci-cd-measurement-and-efficiency-hardening
plan: 04
type: execute
wave: 3
depends_on: [192-02, 192-03]
files_modified:
  - test/threadline/phase06_nyquist_ci_contract_test.exs
  - test/threadline/dep_floor_guard_test.exs
autonomous: false
requirements: [CI-02, CI-03, CI-04]
must_haves:
  truths:
    - "phase06_nyquist_ci_contract_test.exs asserts, additively and static-parse: 10-key job parity (ci.yml jobs <-> header comment <-> CONTRIBUTING List 1), no :latest in any of the four workflows, ci.yml PR-scoped concurrency, release.yml publish concurrency free of run_id, the verify-test matrix construction (name 'Run test suite' + lane axis min/current) plus the two composed names in CONTRIBUTING List 2, verify-compile-no-optional standalone, and deps/ cache present with no _build cache"
    - "A dep-floor guard test fails loudly at the lock if any locked dep floors above Elixir 1.15 (inspects deps/*/mix.exs elixir: requirements after deps.get)"
    - "The contract test is green-by-construction because it lands AFTER the Wave-2 workflow/doc edits (avoids born-red, honors D-27)"
    - "The human-gated branch-protection reconfig (D-19) and throwaway matrix-resolution run (D-17) are surfaced as an explicit maintainer checklist, never as repo-testable"
  artifacts:
    - "test/threadline/phase06_nyquist_ci_contract_test.exs (D-26 additive assertions)"
    - "test/threadline/dep_floor_guard_test.exs (D-16 dep-floor guard)"
  key_links:
    - "The matrix-name assertion checks ci.yml's CONSTRUCTION (static name 'Run test suite' + base axis lane:[min,current]) not a runtime-composed literal, plus CONTRIBUTING List 2 carrying 'Run test suite (min)'/'(current)'"
    - "Existing phase06 assertions (job-key presence, main-only triggers, ci.all ordering, CONTRIBUTING four-key presence) MUST remain green — extensions are additive"
    - "dep-floor guard uses Version.match?('1.15.0', requirement) per dep so a >1.15 floor surfaces at the lock, not mid-CI"
  prohibitions:
    - "NO editing ci.yml / release.yml / flake-detection.yml / CONTRIBUTING.md / README.md / mix.exs in this plan (Wave 2 owns those; this plan only asserts them)"
    - "NO network or async-unsafe I/O added to phase06 (it stays async static-parse); dep-floor guard is a separate file"
    - "NO breaking of the existing phase06 assertions (additive only)"
    - "NO auto-approval of the human-gated checkpoint (branch protection + throwaway run are maintainer-owned)"
---

<objective>
Lock every Wave-2 construct with static-parse contract assertions and a dep-floor guard, then surface the two human-only steps. Extend `phase06_nyquist_ci_contract_test.exs` with the D-26 assertions, add `dep_floor_guard_test.exs` (D-16), and record the maintainer checklist for the branch-protection reconfiguration (D-19) and the throwaway matrix-resolution run (D-17).

Purpose: Make CI-03/CI-04 alignment durable and testable and give CI-02 a regression lock — green-by-construction after the edits land, so no test is born red (D-27).
Output: Extended contract test, new guard test, maintainer checklist checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-CONTEXT.md
@.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-RESEARCH.md
@test/threadline/phase06_nyquist_ci_contract_test.exs
@.github/workflows/ci.yml
@.github/workflows/release.yml
@CONTRIBUTING.md
</context>

<artifacts_this_phase_produces>
- **Extended** `test/threadline/phase06_nyquist_ci_contract_test.exs` — new `describe` blocks for D-26 (job-key parity; no `:latest`; ci.yml PR concurrency; release publish concurrency free of `run_id`; verify-test matrix construction + composed names in CONTRIBUTING List 2; verify-compile-no-optional standalone; deps/ cache present, `_build` absent).
- **New file** `test/threadline/dep_floor_guard_test.exs` — asserts no locked dep floors above Elixir 1.15 (D-16).
- **Maintainer checklist** (checkpoint) covering branch-protection required-checks reconfiguration (D-19) and the throwaway `elixir 1.15 / otp 26` matrix-resolution run on `ubuntu-22.04` (D-17).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend phase06 contract test with the D-26 alignment assertions</name>
  <files>test/threadline/phase06_nyquist_ci_contract_test.exs</files>
  <read_first>
    - `test/threadline/phase06_nyquist_ci_contract_test.exs` (existing describe blocks 11-124; async static-parse `read_rel!` helper) — extensions must not break these.
    - `192-RESEARCH.md` §"Validation Architecture / Auto-verifiable" table (the exact D-26 assertion set) and §M1 (matrix names are runtime-composed — assert the CONSTRUCTION in ci.yml, not a literal `Run test suite (min)` string in ci.yml).
    - `.github/workflows/ci.yml` (post-Wave-2: 10 job keys, header comment lines 1-2, `concurrency` block, verify-test `lane` axis + static name, `edoburu/pgbouncer:v1.25.2-p0`, 9 `actions/cache@v4` uses) and `.github/workflows/release.yml` (publish-hex `group: release-publish-...`).
  </read_first>
  <behavior>
    - Job-key parity: the 10 keys in ci.yml `jobs:` == the 10 in the header comment (lines 1-2) == the CONTRIBUTING List 1 table.
    - No workflow file (`.github/workflows/{ci,release,flake-detection,hex-publish}.yml`) contains a service `image:` line pinned to the mutable rolling tag (assert none present).
    - ci.yml has a top-level `concurrency` block whose `cancel-in-progress` is gated on `github.event_name == 'pull_request'`.
    - release.yml publish-hex has a `concurrency` group beginning `release-publish-` and NOT containing `run_id`.
    - verify-test declares static `name: Run test suite` AND a matrix base axis `lane` including both `min` and `current`; CONTRIBUTING contains `Run test suite (min)` and `Run test suite (current)`.
    - verify-compile-no-optional is still a standalone `jobs:` key (not folded into the matrix).
    - ci.yml contains `actions/cache@v4` for `deps` and `cache-dependency-path` for the e2e lockfile, and NO cache step whose `path:` is `_build`.
  </behavior>
  <action>
    Add new `describe` blocks to the existing async static-parse test (reuse `read_rel!`; no network). Implement the assertions listed in `<behavior>`. For job-key parity, parse the 10 `jobs:` keys, the header-comment key list (lines 1-2), and the CONTRIBUTING List 1 table, and assert set-equality across all three. For the mutable-tag check, scan all four workflow files and refute any `image:` line pinned to the rolling tag. For the matrix names, assert ci.yml has static `name: Run test suite` plus a `lane` axis containing `min` and `current` (construction A — do NOT grep ci.yml for the runtime-composed `Run test suite (min)` literal, which never appears in the source), and assert CONTRIBUTING List 2 carries both composed names. Keep every existing assertion intact (additive only).
  </action>
  <verify>
    <automated>mix test test/threadline/phase06_nyquist_ci_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - New assertions cover all eight behaviors above; all existing phase06 assertions still pass.
    - Matrix-name assertion checks ci.yml construction + CONTRIBUTING composed names (not a runtime literal in ci.yml).
    - Test remains `async: true`, static-parse, no network.
    - `mix test test/threadline/phase06_nyquist_ci_contract_test.exs` is green.
  </acceptance_criteria>
  <done>phase06 durably locks the D-26 alignment set, green-by-construction over the Wave-2 edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the dep-floor guard test (fail loudly at the lock)</name>
  <files>test/threadline/dep_floor_guard_test.exs</files>
  <read_first>
    - `192-CONTEXT.md` D-16 (shared-mix.lock false-promise guard: assert no locked dep floors above 1.15, fail at the lock not mid-CI).
    - `192-RESEARCH.md` §"Auto-verifiable — new guard test (D-16)" (option (a) recommended: after deps.get, inspect each `deps/*/mix.exs` `elixir:` requirement; a pure mix.lock parse cannot read Elixir floors).
  </read_first>
  <behavior>
    - For every `deps/*/mix.exs` that declares an `elixir:` requirement, `Version.match?("1.15.0", requirement)` is true (i.e. the dep still supports Elixir 1.15).
    - If any dep declares an `elixir:` floor above 1.15 (e.g. `~> 1.16`), the test FAILS naming the offending dep — surfacing the break at the lock rather than mid-CI on the min lane.
    - Deps with no `elixir:` requirement are skipped (no false positive).
  </behavior>
  <action>
    Create `test/threadline/dep_floor_guard_test.exs` (its own file — keeps phase06 pure static-parse; this one reads the fetched `deps/` tree). Enumerate `Path.wildcard("deps/*/mix.exs")`, read each, extract the `elixir:` requirement string from the `project/0` keyword list via regex, and assert `Version.match?("1.15.0", requirement)` for each dep that declares one, failing with the dep name when it does not. Skip deps lacking an `elixir:` key. Assumes `mix deps.get` has run (true in CI and local `mix ci.all`); if `deps/` is empty the test may note it needs deps fetched. Do NOT edit `mix.lock` or `mix.exs`.
  </action>
  <verify>
    <automated>mix deps.get >/dev/null 2>&1; mix test test/threadline/dep_floor_guard_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - New `dep_floor_guard_test.exs` inspects `deps/*/mix.exs` `elixir:` requirements and asserts each supports 1.15.0.
    - Currently green (all current deps support 1.15); a hypothetical >1.15 floor would fail naming the dep.
    - Guard lives in its own file, not folded into async static-parse phase06.
  </acceptance_criteria>
  <done>A locked dep flooring above 1.15 now fails loudly at the lock (D-16 satisfied concretely).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer checklist — throwaway min-lane run (D-17) + branch-protection reconfig (D-19)</name>
  <action>
    This is a human-gated checkpoint (autonomous: false). The two steps below are inherently external — a GitHub live-run resolution check and a repo-settings change — and cannot be asserted by any in-repo test. The executor pauses here and surfaces the checklist to the maintainer; it does not self-approve.
  </action>
  <what-built>
    Wave 2 renamed the required `verify-test` check to two matrix checks (`Run test suite (min)` / `Run test suite (current)`) and added a min lane pinned to `ubuntu-22.04` + `otp 26` + `elixir 1.15`. Two steps are inherently external and cannot be asserted by any in-repo test.
  </what-built>
  <verify>
    <human-check>Maintainer confirms the min lane resolves on ubuntu-22.04 and branch protection requires the two new check names.</human-check>
  </verify>
  <done>Min lane provisions on ubuntu-22.04; `main` branch protection requires `Run test suite (min)` + `Run test suite (current)`; old `Run test suite (verify-test)` requirement removed.</done>
  <how-to-verify>
    1. **Throwaway matrix-resolution run (D-17):** Open the Wave-2 PR (or trigger `ci.yml` on the branch) and confirm the `Run test suite (min)` check actually provisions — i.e. `erlef/setup-beam@v1` resolves `elixir 1.15 / otp 26` on `ubuntu-22.04`. If it fails to resolve, adjust the OTP patch or runner before merge (this is the one runtime-only unknown, RESEARCH A1/M5).
    2. **Branch-protection reconfiguration (D-19):** In GitHub repo settings → Branches → `main` required status checks, once the PR has POSTED `Run test suite (min)` and `Run test suite (current)`: remove the old required check `Run test suite (verify-test)` and add the two new names. Do this just-in-time (or admin-merge) so the PR is not blocked forever — the old check stops posting the moment the matrix lands. `gate-ci-green` keys on workflow-run conclusion, so it is unaffected.
    3. Confirm the two new checks are green on the PR and are the required set on `main`.
  </how-to-verify>
  <resume-signal>Type "approved" once the min lane resolves on ubuntu-22.04 and branch protection requires the two new check names, or describe the resolution failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract test -> live workflow/doc files | The test is the durable guard that docs/pipeline/mix.exs stay aligned |
| matrix rename -> branch protection (GitHub settings) | Renamed required checks block merges until protection is reconfigured |
| committed mix.lock -> min-lane promise | A dep flooring above 1.15 silently breaks the 1.15 support claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-192-04 | Denial of Service | Branch-protection lockout after matrix rename | high | mitigate | Human checkpoint reconfigures required checks to the two new names just-in-time (D-19); contract test asserts the names match CONTRIBUTING List 2 |
| T-192-09 | Tampering | Silent alignment drift (a future edit weakens a gate or reintroduces `:latest`/`run_id` no-op) | high | mitigate | phase06 additive assertions fail CI on any such regression (D-26): no `:latest`, PR concurrency present, publish concurrency free of `run_id`, deps/ cache present with no `_build` cache |
| T-192-10 | Spoofing | False 1.15-support promise from a dep flooring above 1.15 | medium | mitigate | dep-floor guard fails loudly at the lock naming the dep (D-16), not mid-CI |
| T-192-SC | Tampering | Supply-chain: new actions/cache@v4 usage | low | accept | Only added action; GitHub-official; no new npm/hex packages this phase (RESEARCH legitimacy audit clean) |
</threat_model>

<verification>
- `mix test test/threadline/phase06_nyquist_ci_contract_test.exs test/threadline/dep_floor_guard_test.exs` green.
- `mix verify.test` green (full suite) before phase close; `mix ci.all` green.
- Human checkpoint approved: min lane resolves on ubuntu-22.04; branch protection requires the two new check names.
- No edits to ci.yml/release.yml/flake-detection.yml/CONTRIBUTING.md/README.md/mix.exs in this plan.
</verification>

<success_criteria>
CI-02/CI-03/CI-04 alignment is durably asserted (D-26) and the 1.15 floor is guarded at the lock (D-16); the two external steps (branch-protection reconfig D-19, throwaway matrix run D-17) are explicitly maintainer-owned and confirmed. The whole Wave-2+Wave-3 set lands as one coherent, green-by-construction change (D-27).
</success_criteria>

<output>
Create `.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-04-SUMMARY.md` when done.
</output>
