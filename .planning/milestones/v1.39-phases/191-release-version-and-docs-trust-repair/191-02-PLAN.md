---
phase: 191-release-version-and-docs-trust-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - mix.exs
  - test/threadline/release_artifact_contract_test.exs
  - test/threadline/persona_routing_doc_contract_test.exs
autonomous: true
requirements: [ADOPT-03]
must_haves:
  truths:
    - "README ## Start here presents a three-column 'I want to... / Start here / Then read' intent table whose rows are the four intent verbs Evaluate/Adopt/Operate/Contribute, each pointing to its canonical landing."
    - "The flat ## Documentation dump is replaced (not deleted) by a collapsed <details> all-guides index grouped by the four verbs; every previously-listed guide link survives in the README."
    - "mix.exs groups_for_extras exposes the four verb lanes plus Overview and Integrations, with every extra in exactly one lane."
    - "No standalone start-here / where-to-go-next guide exists."
  artifacts:
    - README.md (## Start here intent table + <details> all-guides index)
    - mix.exs groups_for_extras (four verb lanes)
    - test/threadline/persona_routing_doc_contract_test.exs (new)
    - test/threadline/release_artifact_contract_test.exs (groups-key equality updated)
  key_links:
    - "README verb labels == mix.exs groups_for_extras lane keys == persona_routing_doc_contract_test expected labels."
    - "release_artifact_contract_test groups-key exact-equality list matches the mix.exs groups_for_extras key order."
---

<objective>
Give evaluators, first-hour adopters, operators, and maintainers a short next-step path over the EXISTING guides (ADOPT-03), split by medium (D-191-13 Option C): README owns the routing prose, ExDoc owns the sidebar structure. This satisfies ROADMAP success criterion 3 ("README/ExDoc guide routing gives evaluators, first-hour adopters, operators, and maintainers a short next-step path"), using intent VERBS (Evaluate/Adopt/Operate/Contribute), not persona nouns.

Purpose: Reader intent, not a new guide, is the wayfinding fix (Diataxis). Verbs are idiomatic for Elixir libs, match the ExDoc sidebar, and avoid colliding with the locked operator-UI P4 "Audit Operator" persona.
Output: A reshaped README `## Start here` + collapsed all-guides index, four verb lanes in `groups_for_extras`, a new routing doc-contract test, and the updated groups-key equality guard.

## Artifacts this plan produces
- New `test/threadline/persona_routing_doc_contract_test.exs` (module `Threadline.PersonaRoutingDocContractTest`).
- New `mix.exs` `groups_for_extras` lane keys: `:Evaluate`, `:Adopt`, `:Operate`, `:Contribute` (replacing `:Reference` and `:Project`; `:Overview` and `:Integrations` retained).
- New README `## Start here` three-column intent table and a `<details><summary>All guides</summary>` index block replacing the flat `## Documentation` list.
- Updated `groups_for_extras` exact-equality key list in `test/threadline/release_artifact_contract_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/191-release-version-and-docs-trust-repair/191-CONTEXT.md
@.planning/phases/191-release-version-and-docs-trust-repair/191-RESEARCH.md
@.planning/phases/191-release-version-and-docs-trust-repair/191-PATTERNS.md
@README.md
@mix.exs
@brandbook/brand-book.md
@test/threadline/readme_doc_contract_test.exs
@test/threadline/release_artifact_contract_test.exs
@test/threadline/exploration_routing_doc_contract_test.exs
@test/threadline/ia_lock_doc_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace groups_for_extras with four verb lanes and update the equality guard</name>
  <files>mix.exs, test/threadline/release_artifact_contract_test.exs</files>
  <read_first>
    - mix.exs — `extras` list (lines 257-278, 20 entries) and `groups_for_extras` block (lines 279-284); `groups_for_modules` (285+) is unaffected.
    - test/threadline/release_artifact_contract_test.exs (lines 39-51) — the `Keyword.keys(...) == [:Overview, :Integrations, :Reference, :Project]` exact-equality assertion (40-44) that BREAKS on lane replacement; line 51's `groups_for_modules` Integrations check is unaffected.
    - 191-RESEARCH.md "ExDoc Routing Grounding" — the full per-lane guide assignment that sums to all 20 extras.
    - 191-CONTEXT.md D-191-16.
  </read_first>
  <action>
    Per D-191-16, replace the greedy flat `Reference: ~r{^guides/}` and `Project: ~r/(CONTRIBUTING|CHANGELOG)/` buckets in `mix.exs` `groups_for_extras` with four explicit per-lane entries, keeping `Overview` (README) first and `Integrations` (`~r{^guides/integrations/}`) BEFORE any general guide match (ExDoc groups by first match, so Integrations must precede the verb lanes to keep the two integration guides out of a verb lane). Use explicit per-lane file-list regexes (not a greedy `^guides/`), every one of the 20 extras in exactly one lane. Use the RESEARCH lane assignment: Evaluate = evaluating-threadline, how-threadline-works, domain-reference; Adopt = getting-started-saas, production-checklist, brownfield-continuity, integration-contracts, local-docker-dx, upgrade-path; Operate = operator-surface, incident-playbook, performance, audit-indexing, adoption-evidence-playbook; Contribute = CONTRIBUTING, adoption-pilot-backlog, CHANGELOG. Spell the four keys exactly `Evaluate`/`Adopt`/`Operate`/`Contribute`. Confirm the lane lists sum to all 20 extras (README + 15 guides + 2 integrations + CONTRIBUTING + CHANGELOG) with each in exactly one lane before finishing (RESEARCH assumption A2). Keep `groups_for_modules` unchanged.
    In the SAME task, update `test/threadline/release_artifact_contract_test.exs` lines 40-44: change the exact-equality key list from `[:Overview, :Integrations, :Reference, :Project]` to the new key ORDER produced by your `groups_for_extras` (e.g. `[:Overview, :Integrations, :Evaluate, :Adopt, :Operate, :Contribute]`). This test runs only under `mix verify.release` (not per-PR `ci.all`), so a green PR would otherwise still break the release — update it in lockstep. Keep it consistent with the subset check the new persona_routing test uses (Task 3). Do not touch line 51.
  </action>
  <verify>
    <automated>mix test test/threadline/release_artifact_contract_test.exs && mix docs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `groups_for_extras` has keys Overview, Integrations, Evaluate, Adopt, Operate, Contribute; Integrations precedes the verb lanes; every extra lands in exactly one lane (20 total accounted for).
    - `release_artifact_contract_test.exs` groups-key equality matches the new mix.exs key order; `mix test test/threadline/release_artifact_contract_test.exs` passes.
    - `mix docs` builds without an ungrouped-extra warning.
  </acceptance_criteria>
  <done>groups_for_extras exposes the four verb lanes plus Overview/Integrations with every extra in exactly one lane, and release_artifact_contract_test's groups-key equality matches; both `mix docs` and the test pass.</done>
</task>

<task type="auto">
  <name>Task 2: Reshape README ## Start here into an intent table + collapsed all-guides index</name>
  <files>README.md</files>
  <read_first>
    - README.md — `## Start here` (lines 21-30), the install snippet (line 63, leave the pin to plan 03), and `## Documentation` (lines 187-204).
    - test/threadline/readme_doc_contract_test.exs (full) — the ~18 whole-README preservation literals, the refuted `| Lane | Claim type |` header, the refuted `Using Sigra:` and `mix threadline.evidence.show`, and the scoped `## Start here ... ## Evidence plane` slice that must still contain `evaluating-threadline.md`.
    - brandbook/brand-book.md — voice for the row microcopy (active, plainspoken, boundaries explicit, concrete surfaces; no "powerful/seamless", no exclamation marks).
    - 191-CONTEXT.md D-191-14/15/18; 191-RESEARCH.md "README Reshape Preservation Literals"; 191-PATTERNS.md README section.
  </read_first>
  <action>
    Per D-191-15, reshape `## Start here` (README lines 21-30) into a three-column intent table with the header columns "I want to..." / "Start here" / "Then read" (this exact header — never the upgrade-path `| Lane | Claim type |` header, which readme_doc_contract_test refutes README-wide). One row per intent verb, each with its canonical landing and next hop (D-191-14):
    Evaluate -> guides/evaluating-threadline.md -> how-threadline-works.md;
    Adopt -> guides/getting-started-saas.md -> production-checklist.md;
    Operate -> guides/operator-surface.md -> incident-playbook.md;
    Contribute -> CONTRIBUTING.md -> guides/adoption-pilot-backlog.md.
    Row microcopy follows the D-191-18 brand voice and names concrete surfaces (reference copy: Evaluate names in-repo proof vs staging; Adopt names install + first captured write + operator surface in the first hour; Operate names the /audit console, row changes, actor history, evidence; Contribute names repo setup, `mix ci.all`, the contribution gate). The Adopt row says "Wire it into a Phoenix app" — do NOT rename the getting-started-saas.md guide body (out of Phase 191 scope).
    Then REPLACE (not delete) the flat `## Documentation` dump (lines 187-204) with a collapsed `<details><summary>All guides</summary>` index grouped by the four verbs, preserving EVERY guide link currently in that list. A `<details>` index is not a new prose guide, so ADOPT-03 and the D-191-17 refute still hold.
    CRITICAL preservation (readme_doc_contract_test): the current `## Start here` bullets at lines 27-28 carry two literals that do NOT map to the four verb rows — the lane-matrix sentence naming the canonical `capture-only`, `phoenix-surface`, `phx-gen-auth-reference`, `sigra-reference` matrix, and the `Phoenix auth (reference lanes, pick one)` line. RELOCATE both into the `<details>` index or adjacent prose so their exact literals survive somewhere in the README. Keep every other asserted guide link and sentence (how-threadline-works, getting-started-saas, domain-reference, upgrade-path, evaluating-threadline, production-checklist, adoption-pilot-backlog, integrations/phx-gen-auth, integrations/sigra, performance, incident-playbook; the `"which public API first?"` sentence). Keep `evaluating-threadline.md` inside the `## Start here ... ## Evidence plane` slice (the Evaluate row satisfies this). Do NOT introduce the refuted literals. Leave the install snippet / pin edits to plan 03.
  </action>
  <verify>
    <automated>mix test test/threadline/readme_doc_contract_test.exs test/threadline/exploration_routing_doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `## Start here` is a three-column "I want to... / Start here / Then read" table with Evaluate/Adopt/Operate/Contribute rows to their canonical landings; microcopy follows brand voice.
    - The flat `## Documentation` list is replaced by a `<details>` all-guides index; every previously-listed guide link still appears in the README.
    - The lane-matrix sentence and `Phoenix auth (reference lanes, pick one)` literal survive somewhere in the README.
    - `mix test test/threadline/readme_doc_contract_test.exs test/threadline/exploration_routing_doc_contract_test.exs` passes (no dropped literal, no refuted literal introduced).
  </acceptance_criteria>
  <done>README ## Start here is a verb-lane intent table and the flat ## Documentation list is replaced by a <details> all-guides index; every preservation literal survives and the README contracts stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Add persona_routing_doc_contract_test.exs and register it in verify.doc_contract</name>
  <files>test/threadline/persona_routing_doc_contract_test.exs, mix.exs</files>
  <read_first>
    - test/threadline/exploration_routing_doc_contract_test.exs — the `read_rel!/1` + `String.contains?` prose-assertion shape.
    - test/threadline/release_artifact_contract_test.exs (lines 5-7, 40-45) — reading ExDoc config via `Threadline.MixProject.project()[:docs][:groups_for_extras]` and `Keyword.keys/1`.
    - test/threadline/ia_lock_doc_contract_test.exs (lines 8-33) — the `for id <- ~w(...)` label-loop with per-item messages. DO NOT modify ia_lock itself (it locks the P1-P5 operator personas).
    - mix.exs line 82 — the one-line space-joined `verify.doc_contract` alias string (18 test paths today).
    - 191-CONTEXT.md D-191-17; 191-PATTERNS.md persona_routing section.
  </read_first>
  <action>
    Create `test/threadline/persona_routing_doc_contract_test.exs` (module `Threadline.PersonaRoutingDocContractTest`, `use ExUnit.Case, async: true`) with three checks per D-191-17: (a) `groups_for_extras` (read from `Threadline.MixProject.project()[:docs][:groups_for_extras]`) CONTAINS the four verb lane keys `:Evaluate`, `:Adopt`, `:Operate`, `:Contribute` (subset/`in` check, NOT exact equality — release_artifact_contract_test owns the exact-equality assertion; the two must agree on the four verbs); (b) for each verb lane, README `## Start here` contains the lane label AND its canonical landing link (Evaluate/evaluating-threadline.md, Adopt/getting-started-saas.md, Operate/operator-surface.md, Contribute/CONTRIBUTING.md) via a `for {label, landing} <- ...` loop with per-item failure messages; (c) `refute File.exists?` for both `guides/where-to-go-next.md` and `guides/start-here.md` (the anti-new-guide guard). Do NOT touch the P1-P5 operator UI personas or ia_lock. Register the new file: append ` test/threadline/persona_routing_doc_contract_test.exs` to the space-joined `verify.doc_contract` alias string in mix.exs line 82 (plan 03 separately appends version_truth to the same alias — a sequential edit, no conflict).
  </action>
  <verify>
    <automated>mix test test/threadline/persona_routing_doc_contract_test.exs && mix verify.doc_contract</automated>
  </verify>
  <acceptance_criteria>
    - The new test asserts the four verb lane keys present in groups_for_extras, each README lane label + canonical landing, and refutes the two forbidden new-guide files.
    - `test/threadline/persona_routing_doc_contract_test.exs` is listed in the `verify.doc_contract` alias.
    - `mix test test/threadline/persona_routing_doc_contract_test.exs` and `mix verify.doc_contract` both pass.
  </acceptance_criteria>
  <done>persona_routing_doc_contract_test asserts the four verb lanes on both README and ExDoc surfaces and refutes a new start-here/where-to-go-next guide; it is wired into verify.doc_contract and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader -> README/ExDoc routing | An evaluator/adopter/operator/maintainer follows a routing label to a landing guide; a wrong or dead landing wastes the first-hour trust budget. |
| README reshape -> doc contracts | The reshape must preserve discoverability literals; a dropped guide link silently removes a documented adoption promise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-191-06 | Information disclosure | routing labels -> landing guides | low | mitigate | persona_routing_doc_contract_test asserts each verb lane label + its canonical landing on both README and ExDoc surfaces (labels equal on both). |
| T-191-07 | Tampering | README reshape dropping a test-locked literal | medium | mitigate | readme_doc_contract_test preservation-literal set kept green; the lane-matrix + Phoenix-auth lines relocated not deleted; groups-key equality updated in lockstep so verify.release stays green. |
| T-191-SC | Tampering | package installs | low | accept | No external package installs in this plan (docs + config + one test file). |
</threat_model>

<verification>
- `mix test test/threadline/persona_routing_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/exploration_routing_doc_contract_test.exs` — all routing + preservation + groups-key guards pass.
- `mix verify.doc_contract` — full doc-contract suite green with the new test registered.
- `mix docs` — ExDoc builds with every extra in exactly one lane (no ungrouped-extra warning).
</verification>

<success_criteria>
- ROADMAP criterion 3 met: README prose + ExDoc structure route the four intents to canonical landings with a short next hop, no new guide created.
- README verb labels, ExDoc lane keys, and the new test's expected labels all agree.
- Every previously-discoverable guide link survives the reshape; verify.release groups-key guard is consistent with mix.exs.
</success_criteria>

<output>
Create `.planning/phases/191-release-version-and-docs-trust-repair/191-02-SUMMARY.md` when done.
</output>
