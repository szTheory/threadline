---
phase: 190-storage-schema-confidence-and-host-schema-truth
plan: 05
type: execute
wave: 2
depends_on:
  - 190-02
files_modified:
  - lib/threadline/retention.ex
  - lib/threadline/retention/pruner.ex
  - test/threadline/retention_test.exs
  - test/threadline/retention/pruner_test.exs
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
must_haves:
  truths:
    - "D-190-04: retention runs participate in custom storage-schema proof."
    - "D-190-08: retention bulk reads, deletes, inserts, and updates use resolved prefix options."
    - "D-190-09: retention uses the global configured storage schema contract for Phase 190."
  artifacts:
    - path: "lib/threadline/retention.ex"
      provides: "prefix-aware retention run records and destructive purge"
    - path: "lib/threadline/retention/pruner.ex"
      provides: "prefix-aware abandoned retention run pruning"
  key_links:
    - from: "lib/threadline/retention.ex"
      to: "Threadline-owned storage tables"
      via: "StorageSchema.repo_opts() under configured storage"
      pattern: "StorageSchema\\.repo_opts"
---

<objective>
Make retention dry-run, destructive purge, run records, and pruner updates target the configured Threadline storage schema.

Purpose: SCHEMA-01 cannot close if destructive retention can count, delete, or update default `threadline` rows while `audit` is configured.
Output: Prefix-aware retention/pruner code and sentinel tests that prove default rows survive custom-schema retention.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-CONTEXT.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-RESEARCH.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-PATTERNS.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-02-SUMMARY.md
@lib/threadline/retention.ex
@lib/threadline/retention/pruner.ex
@test/threadline/retention_test.exs
@test/threadline/retention/pruner_test.exs
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | phase-190 | Make custom storage_schema trustworthy for retention governance paths | 190-05 | COVERED | Retention destructive paths are isolated here.
REQ | SCHEMA-01 | Custom storage_schema through governance/retention paths | 190-05,190-10 | COVERED | This plan repairs; Plan 10 proves with sentinels.
REQ | SCHEMA-02 | Configurable storage schemas do not silently hit hardcoded threadline | 190-05 | COVERED | Retention Repo operations become explicit.
RESEARCH | retention-prefix | Retention delete/update paths must be prefix-aware | 190-05 | COVERED | Destructive paths get focused tests.
CONTEXT | D-190-04 | Proof matrix includes retention runs | 190-05,190-10 | COVERED | Implementation here, closing proof later.
CONTEXT | D-190-08 | bulk operations use resolved prefix | 190-05 | COVERED | Counts, deletes, inserts, and updates are wired.
CONTEXT | D-190-09 | retention behavior deliberate | 190-05 | COVERED | Selected decision: app configured storage schema is runtime retention contract.
CONTEXT | D-190-10 | runtime contracts fail on omitted prefix plumbing | 190-05 | COVERED | Tests seed plausible old rows in both schemas.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make retention counts and purge storage-prefix safe</name>
  <files>lib/threadline/retention.ex, test/threadline/retention_test.exs</files>
  <behavior>
    - Retention dry-run counts only configured storage rows.
    - Retention purge deletes old rows from configured storage and leaves default `threadline` sentinel rows intact.
    - Retention run records are inserted and updated in the same configured storage schema.
  </behavior>
  <action>Add custom-schema sentinel tests before implementation. Update every retention count, insert, update, delete, and transaction cleanup Repo call to use resolved storage prefix options. Preserve retention policy validation, dry-run semantics, advisory locks, and telemetry behavior.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/threadline/retention_test.exs</automated>
  </verify>
  <done>Retention tests fail if purge or dry-run touches the wrong schema and pass under configured `audit` storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make retention pruner updates storage-prefix safe</name>
  <files>lib/threadline/retention/pruner.ex, test/threadline/retention/pruner_test.exs</files>
  <behavior>
    - Pruner abandoned-run lookup uses configured storage.
    - Pruner update/delete actions target only configured storage.
    - Default-schema retention run sentinels are untouched under configured `audit` storage.
  </behavior>
  <action>Add pruner sentinel tests for both configured and default storage rows. Update pruner Repo operations to use resolved storage prefix options while preserving scheduling, abandoned-run classification, and existing deterministic-test helpers.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/threadline/retention/pruner_test.exs</automated>
  </verify>
  <done>Pruner tests prove abandoned-run updates target configured storage only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retention policy to destructive SQL | Retention deletes Threadline-owned rows in batches. |
| pruner runtime to Repo | Background pruning updates retention run metadata without request context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-11 | Tampering | Retention purge and pruner | mitigate | Prefix all count/delete/update/run-record queries; sentinel tests prove default-schema rows survive custom-schema purge. |
| T-190-12 | Denial of Service | Retention wrong-schema deletes | mitigate | Tests prove configured storage rows are selected and default storage rows are not deleted by custom-schema runs. |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run targeted retention gates:
- `mix compile --warnings-as-errors`
- `mix test test/threadline/retention_test.exs test/threadline/retention/pruner_test.exs`
- `mix format --check-formatted`
</verification>

<success_criteria>
Retention and pruning are prefix-aware and deliberate: dry-run, purge, run records, and abandoned-run updates target configured `audit` storage and leave default `threadline` sentinels intact.
</success_criteria>

<output>
Create `.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-05-SUMMARY.md` when done.
</output>
