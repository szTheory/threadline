---
phase: 190-storage-schema-confidence-and-host-schema-truth
plan: 08
type: execute
wave: 3
depends_on:
  - 190-07
files_modified:
  - lib/mix/tasks/threadline.policy.show.ex
  - lib/threadline/policy/redaction_presenter.ex
  - lib/threadline/operator_surface/live/policy_redaction_live.ex
  - test/threadline/policy/redaction_presenter_test.exs
  - test/threadline/policy/redaction_presenter_catalog_test.exs
  - test/threadline/operator_surface/policy_show_mix_test.exs
  - test/threadline/operator_surface/policy_show_doc_contract_test.exs
  - test/threadline/operator_surface/live/policy_redaction_live_test.exs
autonomous: true
requirements:
  - SCHEMA-04
must_haves:
  truths:
    - "D-190-17: redaction inspection supports selected non-public host schema."
    - "D-190-18: redaction host schema selection remains separate from Threadline storage schema."
    - "D-190-21/D-190-22/D-190-24: CLI/UI copy is operator-focused, pattern-reusing, and fail-loud on bad schema input."
  artifacts:
    - path: "lib/mix/tasks/threadline.policy.show.ex"
      provides: "selected host-schema redaction CLI"
    - path: "lib/threadline/policy/redaction_presenter.ex"
      provides: "selected host-schema redaction drift presenter"
    - path: "lib/threadline/operator_surface/live/policy_redaction_live.ex"
      provides: "selected host-schema redaction UI and Timeline links"
  key_links:
    - from: "PolicyRedactionLive"
      to: "RedactionPresenter.build/1"
      via: "selected schema param validated with CoverageSchemas"
      pattern: "schema:"
---

<objective>
Add selected host-schema redaction support for the CLI, presenter, and PolicyRedactionLive.

Purpose: The resolved research decision selects host-schema redaction support now; SCHEMA-04 cannot claim `support.tickets` truth while redaction inspection remains public-only.
Output: `threadline.policy.show --schema=support`, selected-schema presenter grouping, PolicyRedactionLive schema selection, and tests that preserve read-only/auth behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-CONTEXT.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-RESEARCH.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-PATTERNS.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-07-SUMMARY.md
@lib/threadline/health/coverage_schemas.ex
@lib/mix/tasks/threadline.policy.show.ex
@lib/threadline/policy/redaction_presenter.ex
@lib/threadline/operator_surface/live/policy_redaction_live.ex
@test/threadline/operator_surface/policy_show_mix_test.exs
@test/threadline/operator_surface/live/policy_redaction_live_test.exs
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | phase-190 | Make host-schema assumptions truthful | 190-08 | COVERED | Redaction selected-schema support is isolated here.
REQ | SCHEMA-04 | Non-public host-table support for policy/redaction inspection | 190-08 | COVERED | CLI, presenter, and LiveView support selected host schema.
RESEARCH | redaction-decision | Redaction gets selected host-schema support now, reusing CoverageSchemas and CoverageLive patterns | 190-08 | COVERED | Implements resolved open question.
RESEARCH | qualified-config | Qualified host-table config should match selected-schema deployed rows | 190-08 | COVERED | Presenter normalizes configured keys.
CONTEXT | D-190-17 | Cover redaction inspection for selected host schema | 190-08 | COVERED | CLI/UI presenter paths included.
CONTEXT | D-190-18 | Storage schema separate from captured host table schema | 190-08 | COVERED | `--schema` means host schema only.
CONTEXT | D-190-21 | Operator copy avoids Ecto prefix internals | 190-08 | COVERED | UI/CLI use host-schema language.
CONTEXT | D-190-22 | UI changes reuse existing controls and design-system patterns | 190-08 | COVERED | Model on CoverageLive schema selector.
CONTEXT | D-190-24 | Unsupported or misconfigured behavior fails loudly | 190-08 | COVERED | Invalid schema input gets explicit error.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add selected host-schema redaction CLI and presenter behavior</name>
  <files>lib/mix/tasks/threadline.policy.show.ex, lib/threadline/policy/redaction_presenter.ex, test/threadline/policy/redaction_presenter_test.exs, test/threadline/policy/redaction_presenter_catalog_test.exs, test/threadline/operator_surface/policy_show_mix_test.exs, test/threadline/operator_surface/policy_show_doc_contract_test.exs</files>
  <behavior>
    - `mix threadline.policy.show --schema=support` validates support and renders JSON with `"schema": "support"`.
    - Redaction presenter groups configured and deployed rows by selected host schema.
    - Configured keys may be bare public names or schema-qualified host names such as `support.tickets`.
  </behavior>
  <action>Extend `policy.show` to parse `--schema=NAME` with the same edge-validation semantics as coverage tasks. Update `RedactionPresenter` so selected host schema filters deployed rows and normalized configured table keys. Keep `--schema` as host schema only; do not pass it into Threadline storage repo opts.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/threadline/policy/redaction_presenter_test.exs test/threadline/policy/redaction_presenter_catalog_test.exs test/threadline/operator_surface/policy_show_mix_test.exs test/threadline/operator_surface/policy_show_doc_contract_test.exs</automated>
  </verify>
  <done>CLI and presenter tests prove selected `support` redaction behavior and JSON/schema output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add selected host-schema redaction UI truth</name>
  <files>lib/threadline/operator_surface/live/policy_redaction_live.ex, test/threadline/operator_surface/live/policy_redaction_live_test.exs</files>
  <behavior>
    - `/audit/policy/redaction?schema=support` validates support and displays the selected host schema.
    - Redaction rows link Timeline with `table_schema=support&table=tickets` for non-public host tables.
    - Existing read-only behavior, auth, feature gates, and no-sample-values guardrails remain unchanged.
  </behavior>
  <action>Update `PolicyRedactionLive` with a native selected-schema control modeled on CoverageLive. Preserve existing route, authorization, feature-gate, dark/light/system, and accessibility behavior. Keep operator copy focused on host schema and redaction drift.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/threadline/operator_surface/live/policy_redaction_live_test.exs</automated>
  </verify>
  <done>PolicyRedactionLive tests prove selected host schema, Timeline links, and existing auth/read-only behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/UI schema input to catalog SQL | `--schema` and URL `?schema=` values cross into PostgreSQL catalog queries. |
| redaction config to deployed SQL inspection | Configured table policy is compared with PostgreSQL trigger source by selected host schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-18 | Spoofing | Policy `--schema` and LiveView schema params | mitigate | Validate selected schemas with CoverageSchemas before catalog SQL. |
| T-190-19 | Information Disclosure | Redaction presenter and Timeline links | mitigate | Preserve selected host schema in report rows and Timeline URLs so support data is not confused with public data. |
| T-190-20 | Information Disclosure | Redaction false confidence | mitigate | CLI/UI include selected schema and fail clearly for invalid schema rather than silently inspecting public. |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run targeted redaction gates:
- `mix compile --warnings-as-errors`
- `mix test test/threadline/policy/redaction_presenter_test.exs test/threadline/policy/redaction_presenter_catalog_test.exs test/threadline/operator_surface/policy_show_mix_test.exs test/threadline/operator_surface/policy_show_doc_contract_test.exs`
- `mix test test/threadline/operator_surface/live/policy_redaction_live_test.exs`
- `mix format --check-formatted`
</verification>

<success_criteria>
Selected host-schema redaction is truthful: CLI, presenter, and LiveView inspect `support` when selected, keep storage schema separate, and fail loudly for invalid host-schema input.
</success_criteria>

<output>
Create `.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-08-SUMMARY.md` when done.
</output>
