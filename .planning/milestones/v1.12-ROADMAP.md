# Milestone v1.12: Temporal Truth & Safety

**Status:** ✅ SHIPPED 2026-04-25
**Phases:** 38-40
**Total Plans:** 3

## Overview

Point-in-time row reconstruction landed with snapshot-first `as_of/4`, opt-in struct reification, and operator-facing time travel docs.

## Phases

### Phase 38: Core As-of Reconstruction

**Goal**: Basic historical state retrieval as Maps.
**Depends on**: Phase 37
**Requirements**: ASOF-01, ASOF-02, ASOF-05
**Plans**: 1 plan

Plans:

- [x] 38-01: Single-row as-of lookup with map output, delete detection, and genesis-gap handling

**Details:**

- Added the public `Threadline.as_of/4` delegator and snapshot-first query path.
- Returned stored snapshot maps directly, with explicit `:deleted_record` and `:before_audit_horizon` errors.
- Locked the historical reconstruction contract with regression tests.

### Phase 39: Reification & Schema Safety

**Goal**: Casting historical data to Ecto structs with drift tolerance.
**Depends on**: Phase 38
**Requirements**: ASOF-03, ASOF-04
**Plans**: 1 plan

Plans:

- [x] 39-01: Add opt-in struct reification with loose schema loading and explicit cast errors

**Details:**

- Kept the default return shape map-only and enabled struct reification behind `cast: true`.
- Used `Ecto.embedded_load/3` to ignore unknown historical fields while preserving schema defaults.
- Returned explicit cast errors when historical snapshots could not be loaded into the current schema.

### Phase 40: Temporal Operator Guides

**Goal**: User-facing documentation for time travel features.
**Depends on**: Phase 39
**Requirements**: ASOF-06
**Plans**: 1 plan

Plans:

- [x] 40-01: Document the Time Travel hub and example walkthrough with contract tests

**Details:**

- Added the `## Time Travel (As-of)` hub to `guides/domain-reference.md`.
- Added a runnable reconstruction walkthrough to `examples/threadline_phoenix/README.md`.
- Locked the new anchors and walkthrough literals with doc-contract tests.

---

## Milestone Summary

**Key Decisions:**

- `as_of/4` stays repo-backed and map-first by default.
- Struct reification remains opt-in behind `cast: true`.
- Time Travel lives as a compact hub beside existing exploration docs.

**Issues Resolved:**

- Snapshot reconstruction now handles deleted rows and genesis gaps explicitly.
- Historical loading tolerates schema drift without losing current-schema defaults.
- Documentation and example walkthroughs are contract-locked.

**Issues Deferred:**

- `SEED-001-sigra-integration-adapter` (dormant).

**Technical Debt Incurred:**

- Current worktree still has unrelated root README contract drift noted in the milestone audit.

---

_For current project status, see `.planning/ROADMAP.md`._
