---
phase: 128-readme-phx-gen-auth-mount-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/integrations/phx-gen-auth.md
  - test/threadline/integrations/phx_gen_auth_integration_test.exs
  - test/support/phx_gen_auth_fixtures.ex
  - test/threadline/integrations/phx_gen_auth_doc_contract_test.exs
autonomous: true
requirements:
  - AUTH-MOUNT-01
  - AUTH-MOUNT-02
must_haves:
  truths:
    - "phx-gen-auth guide mount uses `authorize_fn: &MyApp.Audit.authorize_operator/1` with scope-first user lookup and `is_admin: true` gate."
    - "Integration tests prove admin allow via `current_scope.user`, non-admin deny, and optional legacy `current_user` fallback."
    - "Doc-contract test locks canonical mount literals and refutes legacy inline role match."
  artifacts:
    - path: guides/integrations/phx-gen-auth.md
      provides: "MyApp.Audit module + callback-ref mount snippet (AUTH-MOUNT-01)"
    - path: test/threadline/integrations/phx_gen_auth_integration_test.exs
      provides: "PhxGenAuthReference.Audit authorize_operator/1 proof aligned with guide"
    - path: test/support/phx_gen_auth_fixtures.ex
      provides: "admin_scope_user/0 and member_scope_user/0 fixture maps"
    - path: test/threadline/integrations/phx_gen_auth_doc_contract_test.exs
      provides: "Surface section semantic locks + refutes (AUTH-MOUNT-02)"
---

<objective>
Replace legacy phx-gen-auth mount inline `current_user.role` example with canonical scope-first `authorize_fn` callback ref (AUTH-MOUNT-01), align root integration test proof (D-128-12–14), and lock literals via doc-contract test (AUTH-MOUNT-02).

Purpose: phx-gen-auth lane adopters on Phoenix 1.8 scopes copy a broken mount pattern; guide and CI proof must match integration-contracts + getting-started §9 posture.
Output: New `MyApp.Audit` guide module, `PhxGenAuthReference.Audit` test module, updated fixtures, revised contract assertions.
</objective>

<context>
@.planning/phases/128-readme-phx-gen-auth-mount-parity/128-CONTEXT.md (D-128-08 through D-128-14, D-128-17, D-128-18)
@guides/integration-contracts.md (authorize_operator callback shape lines 216–236)
@guides/getting-started-saas.md §9 (callback-ref mount pattern)
@test/threadline/integrations/phx_gen_auth_integration_test.exs (existing AuditActor + legacy guide_authorize)
@test/threadline/integrations/phx_gen_auth_doc_contract_test.exs (legacy role assertion to replace)
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex (sigra lane — do NOT modify)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-128-05 | Guide shows inline role match — adopters skip scope-first lookup | high | Task 1 replaces mount with `&MyApp.Audit.authorize_operator/1`; Task 4 contract refutes legacy pattern |
| T-128-06 | Integration test uses different authorize logic than guide | high | Task 2 extracts shared `PhxGenAuthReference.Audit`; Task 3 tests mirror guide module |
| T-128-07 | String `role: "admin"` as primary gate — schema mismatch | medium | Task 1 uses `is_admin: true` boolean gate inside authorize_operator |
| T-128-08 | Changing sigra example router — out of scope lane confusion | medium | Do not edit examples/threadline_phoenix router; guide targets phx-gen-auth-reference only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update phx-gen-auth guide with MyApp.Audit + callback-ref mount</name>
  <files>guides/integrations/phx-gen-auth.md</files>
  <read_first>
    - guides/integrations/phx-gen-auth.md (## Surface and export auth stay host-owned section)
    - guides/integration-contracts.md (authorize_operator example)
    - .planning/phases/128-readme-phx-gen-auth-mount-parity/128-CONTEXT.md (D-128-08 through D-128-11)
  </read_first>
  <action>
In `guides/integrations/phx-gen-auth.md`, within **## Surface and export auth stay host-owned**:

1. Add **`MyApp.Audit`** module **before** the mount snippet (after the section intro paragraph):

```elixir
defmodule MyApp.Audit do
  @moduledoc false

  def authorize_operator(%{assigns: assigns}) do
    user =
      case assigns[:current_scope] do
        %{user: user} when not is_nil(user) -> user
        _ -> assigns[:current_user]
      end

    case user do
      %{is_admin: true} -> :ok
      _ -> {:error, :unauthorized}
    end
  end
end
```

2. Replace the inline `authorize_fn: fn ... end` mount block with:

```elixir
threadline_operator_surface "/audit",
  authorize_fn: &MyApp.Audit.authorize_operator/1
```

3. Update the prose after the snippet:
   - Note scope-first resolution via `assigns[:current_scope].user` with `current_user` fallback for Phoenix 1.7 hosts
   - One-line pointer to `guides/integration-contracts.md` and getting-started §9 for advanced `{:ok, scope}` returns
   - Keep existing export/evidence pointer to `guides/operator-surface.md`

4. Remove the legacy inline example containing `%{assigns: %{current_user: %{role: "admin"}}}` entirely.

5. Update **Reference semantics** item 3 to mention `is_admin: true` / scope-first lookup (keep items 1–2, 4–6 unchanged).

Do not modify Plug callback wire-up section or `MyApp.AuditActor`.
  </action>
  <acceptance_criteria>
    - `grep -F 'defmodule MyApp.Audit' guides/integrations/phx-gen-auth.md` matches
    - `grep -F 'authorize_fn: &MyApp.Audit.authorize_operator/1' guides/integrations/phx-gen-auth.md` matches
    - `grep -F 'assigns[:current_scope]' guides/integrations/phx-gen-auth.md` matches in Surface section
    - `grep -F 'is_admin: true' guides/integrations/phx-gen-auth.md` matches
    - `grep -F 'role: "admin"' guides/integrations/phx-gen-auth.md` returns no matches
    - `grep -F '_, _ ->' guides/integrations/phx-gen-auth.md` returns no matches
  </acceptance_criteria>
  <verify>grep -n 'MyApp.Audit\|authorize_operator\|current_scope\|is_admin' guides/integrations/phx-gen-auth.md</verify>
</task>

<task type="auto">
  <name>Task 2: Add PhxGenAuthReference.Audit module mirroring guide</name>
  <files>test/threadline/integrations/phx_gen_auth_integration_test.exs</files>
  <read_first>
    - test/threadline/integrations/phx_gen_auth_integration_test.exs
    - guides/integrations/phx-gen-auth.md (Task 1 MyApp.Audit module — mirror logic exactly)
  </read_first>
  <action>
Add new module **`Threadline.Integrations.PhxGenAuthReference.Audit`** in the same file as `AuditActor` (before the test module):

```elixir
defmodule Threadline.Integrations.PhxGenAuthReference.Audit do
  @moduledoc false

  def authorize_operator(%{assigns: assigns}) do
    user =
      case assigns[:current_scope] do
        %{user: user} when not is_nil(user) -> user
        _ -> assigns[:current_user]
      end

    case user do
      %{is_admin: true} -> :ok
      _ -> {:error, :unauthorized}
    end
  end
end
```

Remove private `guide_authorize/1` functions at bottom of test file.

Add alias: `alias Threadline.Integrations.PhxGenAuthReference.Audit`
  </action>
  <acceptance_criteria>
    - `grep -F 'defmodule Threadline.Integrations.PhxGenAuthReference.Audit' test/threadline/integrations/phx_gen_auth_integration_test.exs` matches
    - `grep -F 'defp guide_authorize' test/threadline/integrations/phx_gen_auth_integration_test.exs` returns no matches
    - `grep -F 'assigns[:current_scope]' test/threadline/integrations/phx_gen_auth_integration_test.exs` matches inside Audit module
  </acceptance_criteria>
  <verify>grep -n 'PhxGenAuthReference.Audit\|guide_authorize' test/threadline/integrations/phx_gen_auth_integration_test.exs</verify>
</task>

<task type="auto">
  <name>Task 3: Extend fixtures and rewrite authorize_fn integration tests</name>
  <files>test/support/phx_gen_auth_fixtures.ex, test/threadline/integrations/phx_gen_auth_integration_test.exs</files>
  <read_first>
    - test/support/phx_gen_auth_fixtures.ex
    - test/threadline/integrations/phx_gen_auth_integration_test.exs (describe "guide authorize_fn admin gate")
    - .planning/phases/128-readme-phx-gen-auth-mount-parity/128-CONTEXT.md (D-128-13, D-128-14)
  </read_first>
  <action>
1. In `phx_gen_auth_fixtures.ex`, add:

```elixir
def admin_scope_user, do: %{is_admin: true, id: "admin-1"}
def member_scope_user, do: %{is_admin: false, id: "member-1"}
```

Keep existing `admin_user/0` and `non_admin_user/0` for backward compat if referenced elsewhere.

2. Replace describe `"guide authorize_fn admin gate"` tests:

**Test A — admin via scope:**
```elixir
test "allows admin via current_scope.user with is_admin true" do
  conn =
    PhxGenAuthFixtures.build_phx_scope_conn(
      scope: %{user: PhxGenAuthFixtures.admin_scope_user()}
    )

  conn_out = ExportAuthPlug.call(conn, ExportAuthPlug.init(authorize_fn: &Audit.authorize_operator/1))
  refute conn_out.halted
end
```

**Test B — non-admin via scope deny:**
```elixir
test "denies non-admin via current_scope.user with 403" do
  conn =
    PhxGenAuthFixtures.build_phx_scope_conn(
      scope: %{user: PhxGenAuthFixtures.member_scope_user()}
    )

  conn_out = ExportAuthPlug.call(conn, ExportAuthPlug.init(authorize_fn: &Audit.authorize_operator/1))

  assert conn_out.halted
  assert conn_out.status == 403
  assert conn_out.resp_body == "forbidden"
end
```

**Test C — optional legacy fallback (Phoenix 1.7):**
```elixir
test "allows admin via current_user fallback when scope user absent" do
  conn =
    PhxGenAuthFixtures.build_phx_scope_conn(
      scope: nil,
      current_user: %{is_admin: true, id: "legacy-admin"}
    )

  conn_out = ExportAuthPlug.call(conn, ExportAuthPlug.init(authorize_fn: &Audit.authorize_operator/1))
  refute conn_out.halted
end
```

Use `Audit` alias from Task 2. Keep `"Threadline.Plug composition"` describe unchanged.
  </action>
  <acceptance_criteria>
    - `grep -F 'def admin_scope_user' test/support/phx_gen_auth_fixtures.ex` matches
    - `grep -F 'def member_scope_user' test/support/phx_gen_auth_fixtures.ex` matches
    - `grep -F 'allows admin via current_scope.user' test/threadline/integrations/phx_gen_auth_integration_test.exs` matches
    - `grep -F 'current_user fallback' test/threadline/integrations/phx_gen_auth_integration_test.exs` matches
    - `mix test test/threadline/integrations/phx_gen_auth_integration_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/integrations/phx_gen_auth_integration_test.exs</verify>
</task>

<task type="auto">
  <name>Task 4: Update phx-gen-auth doc-contract test for scope-first mount literals (AUTH-MOUNT-02)</name>
  <files>test/threadline/integrations/phx_gen_auth_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/integrations/phx_gen_auth_doc_contract_test.exs
    - guides/integrations/phx-gen-auth.md (post Task 1)
    - test/threadline/readme_doc_contract_test.exs (section_slice pattern — optional for surface section)
    - .planning/phases/128-readme-phx-gen-auth-mount-parity/128-CONTEXT.md (D-128-17, D-128-18)
  </read_first>
  <action>
Replace test `"phx-gen-auth guide locks host-owned plug and authorize literals"` body:

1. Keep existing plug/AuditActor assertions (`MyApp.AuditActor`, `conn.assigns[:current_scope]`, `actor_fn: &MyApp.AuditActor.actor_ref_from_conn/1`).

2. Extract surface section slice:

```elixir
surface_start = "## Surface and export auth stay host-owned"
surface_end = "## Reference semantics"
{surface_idx, _} = :binary.match(doc, surface_start)
{semantics_idx, _} = :binary.match(doc, surface_end)
surface = binary_part(doc, surface_idx, semantics_idx - surface_idx)
```

3. Assert on `surface` slice:
   - `authorize_fn:`
   - `&MyApp.Audit.authorize_operator/1`
   - `defmodule MyApp.Audit`
   - `%{assigns: assigns}`
   - `assigns[:current_scope]`
   - `is_admin: true`
   - `{:error, :unauthorized}`

4. Refute on `surface` slice:
   - `%{assigns: %{current_user: %{role: "admin"}}}`
   - Keep existing full-doc refute for `_, _ ->`

Do not lock full normalized inline fn block (whitespace brittle per D-128-17).
  </action>
  <acceptance_criteria>
    - `grep -F '&MyApp.Audit.authorize_operator/1' test/threadline/integrations/phx_gen_auth_doc_contract_test.exs` matches
    - `grep -F 'is_admin: true' test/threadline/integrations/phx_gen_auth_doc_contract_test.exs` matches
    - `grep -F 'role: \"admin\"' test/threadline/integrations/phx_gen_auth_doc_contract_test.exs` returns no matches (no legacy assertion)
    - `mix test test/threadline/integrations/phx_gen_auth_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/integrations/phx_gen_auth_doc_contract_test.exs</verify>
</task>

</tasks>

<verification>
```bash
mix test test/threadline/integrations/phx_gen_auth_integration_test.exs
mix test test/threadline/integrations/phx_gen_auth_doc_contract_test.exs
grep -n 'MyApp.Audit\|authorize_operator\|is_admin' guides/integrations/phx-gen-auth.md
```

All must exit 0 before marking plan complete.
</verification>

<success_criteria>
- AUTH-MOUNT-01: phx-gen-auth guide mount snippet is scope-first with callback ref consistent with integration-contracts.
- AUTH-MOUNT-02: Doc-contract test locks mount literals and refutes legacy inline role pattern.
- Integration test literals match guide module (D-128-18).
</success_criteria>
