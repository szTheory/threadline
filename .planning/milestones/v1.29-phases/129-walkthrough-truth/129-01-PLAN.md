---
phase: 129-walkthrough-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/WALKTHROUGH.md
autonomous: true
requirements:
  - WALK-01
  - WALK-02
must_haves:
  truths:
    - "WALKTHROUGH optional verify uses `mix threadline.verify_coverage` from `examples/threadline_phoenix/` cwd — never bare `mix verify.threadline` as a walk step."
    - "§0 documents walk cwd vs contributor root cwd split with CONTRIBUTING cross-link."
    - "Row-history Do steps are navigation-first (History link); operator tables label shorthand vs canonical transaction-scoped path."
    - "§0 includes Row history URLs SSOT callout cross-linking guides/operator-surface.md."
  artifacts:
    - path: examples/threadline_phoenix/WALKTHROUGH.md
      provides: "Maintainer runbook with honest verify cwd and row-history URL presentation (WALK-01, WALK-02)"
---

<objective>
Fix WALKTHROUGH.md maintainer footguns: replace `mix verify.threadline` walk steps with host-local `mix threadline.verify_coverage`, document walk-vs-root cwd split (WALK-01), and adopt hybrid row-history URL pattern — navigation-first Do steps, labeled shorthand in tables, §0 SSOT callout (WALK-02).

Purpose: Evaluators on a clean clone hit wrong-database verify or pasteable `/audit/rows/` URLs that 404.
Output: WALKTHROUGH.md only; README and router unchanged per D-129-14/15.
</objective>

<context>
@.planning/phases/129-walkthrough-truth/129-CONTEXT.md (D-129-01 through D-129-08, D-129-14 through D-129-16)
@guides/operator-surface.md §179–196 (shorthand vs canonical row history)
@guides/production-checklist.md §1 (`mix threadline.verify_coverage` host task)
@CONTRIBUTING.md (root `mix verify.*` / `mix ci.all` ladder)
@examples/threadline_phoenix/WALKTHROUGH.md §5 footnote — `verify.evidence` → `threadline.evidence.show` precedent
@lib/threadline/operator_surface/router.ex — `live("/transactions/:id/history/:table/:record_id", ...)`
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-129-01 | Root `mix verify.threadline` checks Threadline.Test.Repo — false green after example migrate | high | Task 2 replaces verify lines with `mix threadline.verify_coverage`; Task 1 documents cwd split |
| T-129-02 | Pasteable `/audit/rows/…` URLs in Do steps — 404 on shipped router | high | Task 3 rewrites Do steps to History-link navigation; Task 4 relabels table shorthand |
| T-129-03 | Example-app `verify.threadline` wrapper alias — dual semantics | medium | Rejected per D-129-04; not implemented |
| T-129-04 | §0 footnote-only row-history note — maintainers skip it | medium | Task 1 adds dedicated §0 subsection per D-129-08 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: §0 prerequisites — walk vs root cwd split + Row history URLs subsection</name>
  <files>examples/threadline_phoenix/WALKTHROUGH.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (### Prerequisites ~L11–17; ### Sections in this runbook ~L41)
    - .planning/phases/129-walkthrough-truth/129-CONTEXT.md (D-129-02, D-129-08, D-129-16)
    - guides/operator-surface.md (Row History / As-of Sub-view §179–196)
    - CONTRIBUTING.md (contributor verify ladder intro)
  </read_first>
  <action>
In `### Prerequisites` (after the bullet "Repository cloned; all commands run from **`examples/threadline_phoenix/`**"):

Add a new paragraph:

> **Walk vs contributor cwd:** All **walk steps** in this runbook run from **`examples/threadline_phoenix/`**. **Contributor CI gates** (`mix verify.*`, `mix ci.all`) run from the **repository root** — see [`CONTRIBUTING.md`](../../CONTRIBUTING.md). Host/operator tasks use the `threadline.*` namespace (`mix threadline.verify_coverage`, `mix threadline.evidence.show`); root `verify.*` aliases target the library test harness.

Add new subsection **`### Row history URLs`** immediately after `### Prerequisites` (before `### Recovery`):

Include:
- One sentence: guide shorthand `/audit/rows/:table/:pk` describes the **operator question**; the **shipped route** is transaction-scoped: `/audit/transactions/:id/history/:table/:record_id` (slide-over via **History** on a change row).
- Explicit: **Do not paste** `/audit/rows/…` URLs — they are not mounted standalone routes.
- Cross-link: [`guides/operator-surface.md`](../../guides/operator-surface.md) Row History / As-of Sub-view section (use relative path `../../guides/operator-surface.md`).
- Canonical route literal in backticks: `/audit/transactions/:id/history/:table/:record_id`

In `## Further reading (not required for this run)` add bullet:

- [`../../guides/operator-surface.md`](../../guides/operator-surface.md) — row history shorthand vs canonical transaction-scoped path
  </action>
  <acceptance_criteria>
    - `grep -F 'mix threadline.verify_coverage' examples/threadline_phoenix/WALKTHROUGH.md` matches (may be zero until Task 2 — run full plan verify after Task 2)
    - `grep -F 'CONTRIBUTING.md' examples/threadline_phoenix/WALKTHROUGH.md` matches
    - `grep -F 'Walk vs contributor cwd' examples/threadline_phoenix/WALKTHROUGH.md` matches
    - `grep -F '### Row history URLs' examples/threadline_phoenix/WALKTHROUGH.md` matches
    - `grep -F '/audit/transactions/:id/history/:table/:record_id' examples/threadline_phoenix/WALKTHROUGH.md` matches
    - `grep -F 'Do not paste' examples/threadline_phoenix/WALKTHROUGH.md` matches
    - `grep -F 'operator-surface.md' examples/threadline_phoenix/WALKTHROUGH.md` matches at least twice (§0 + Further reading)
  </acceptance_criteria>
  <verify>grep -n 'Row history URLs\|Walk vs contributor\|operator-surface' examples/threadline_phoenix/WALKTHROUGH.md</verify>
</task>

<task type="auto">
  <name>Task 2: Replace verify.threadline with mix threadline.verify_coverage (WALK-01)</name>
  <files>examples/threadline_phoenix/WALKTHROUGH.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (WALK-01-02 Verify ~L120; WALK-04-03 Verify ~L730)
    - .planning/phases/129-walkthrough-truth/129-CONTEXT.md (D-129-01, D-129-03, D-129-04)
    - guides/production-checklist.md §1
    - examples/threadline_phoenix/config/dev.exs (help-desk expected_tables on ThreadlinePhoenix.Repo)
  </read_first>
  <action>
Replace **both** verify lines:

1. WALK-01-02 (~L120): change
   `**Verify:** Optional — \`mix verify.threadline\` coverage check passes.`
   to:

   **Verify:** Optional — from this directory:

   ```bash
   mix threadline.verify_coverage
   ```

   Uses this app's `config :threadline, :verify_coverage` and `ThreadlinePhoenix.Repo` (not root `mix verify.threadline`, which targets the library CI harness).

2. WALK-04-03 (~L730): same replacement pattern with trailing "after fresh migrate."

Remove every occurrence of bare `mix verify.threadline` from WALKTHROUGH.md. Optional one-sentence footnote near §0 or first verify block may mention root `mix verify.threadline` only as the **full maintainer CI ladder** pointer (link CONTRIBUTING.md) — not as a walk-step command.

Do **not** add a `verify.threadline` alias to `examples/threadline_phoenix/mix.exs`.
  </action>
  <acceptance_criteria>
    - `grep -F 'mix threadline.verify_coverage' examples/threadline_phoenix/WALKTHROUGH.md` matches at least 2 times
    - `grep -F 'mix verify.threadline' examples/threadline_phoenix/WALKTHROUGH.md` returns no matches
    - `grep -F 'ThreadlinePhoenix.Repo' examples/threadline_phoenix/WALKTHROUGH.md` matches in verify context
    - `grep -F 'examples/threadline_phoenix/' examples/threadline_phoenix/WALKTHROUGH.md` matches in §0 prerequisites
  </acceptance_criteria>
  <verify>grep -n 'threadline.verify_coverage\|verify.threadline' examples/threadline_phoenix/WALKTHROUGH.md</verify>
</task>

<task type="auto">
  <name>Task 3: Navigation-first row-history Do steps (WALK-02)</name>
  <files>examples/threadline_phoenix/WALKTHROUGH.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (WALK-03-01 step 6 ~L426; WALK-03-04 step 5 ~L531; WALK-04-02 step 5 ~L651)
    - .planning/phases/129-walkthrough-truth/129-CONTEXT.md (D-129-05, D-129-06)
    - guides/operator-surface.md §192–194 (History link pattern)
  </read_first>
  <action>
Rewrite **Do** step prose (not operator-surface table rows yet — Task 4):

**WALK-03-01 step 6** — replace pasteable URL step with:

`6. On the transaction drill-down, click **History** on the `ticket_replies` change row (canonical path: \`/audit/transactions/:id/history/ticket_replies/:record_id\`) and confirm sensitive fields`

**WALK-03-04 step 5** — replace with:

`5. On the delete transaction drill-down, click **History** on the deleted reply change row (canonical path: \`/audit/transactions/:id/history/ticket_replies/:record_id\`) — inspect state before delete`

**WALK-04-02 step 5** — replace pasteable URL with either:
- "Repeat WALK-03-01 step 6 row-history navigation for the #4521 close reply" OR
- inline navigation-first wording matching WALK-03-01 step 6

Ensure **no** `/audit/rows/ticket_replies/` appears inside numbered **Do** lists for these three steps.
  </action>
  <acceptance_criteria>
    - `grep -F 'click **History**' examples/threadline_phoenix/WALKTHROUGH.md` matches at least 2 times
    - `grep -F 'WALK-03-01' examples/threadline_phoenix/WALKTHROUGH.md -A 20 | grep '/audit/rows/ticket_replies/'` returns no matches in Do block
    - `grep -F 'WALK-03-04' examples/threadline_phoenix/WALKTHROUGH.md -A 20 | grep '/audit/rows/ticket_replies/'` returns no matches in Do block
    - `grep -F 'WALK-04-02' examples/threadline_phoenix/WALKTHROUGH.md -A 25 | grep '/audit/rows/ticket_replies/'` returns no matches in Do block
  </acceptance_criteria>
  <verify>grep -n 'History\|/audit/rows/ticket_replies' examples/threadline_phoenix/WALKTHROUGH.md | head -30</verify>
</task>

<task type="auto">
  <name>Task 4: Relabel operator-surface table row-history rows (hybrid shorthand)</name>
  <files>examples/threadline_phoenix/WALKTHROUGH.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (all `/audit/rows/` table rows — WALK-01-06 ~L241, WALK-03-01 ~L441, WALK-03-04 ~L547, WALK-04-02 ~L675; generic ~L391 if present)
    - .planning/phases/129-walkthrough-truth/129-CONTEXT.md (D-129-07)
    - guides/operator-surface.md §217–222 (Mounted workflow parity table)
  </read_first>
  <action>
For every operator-surface table row where **Route** column is `/audit/rows/…`:

1. **Route column:** keep shorthand but append explicit label, e.g.:
   `Row history *(shorthand /audit/rows/ticket_replies/:pk)*` or `Row history *(shorthand /audit/rows/:table/:pk)*`

2. **Drill-down column:** replace bare "prior values / redaction" with canonical navigation hint, e.g.:
   `→ **History** on change row → \`/audit/transactions/:id/history/ticket_replies/:record_id\``

Apply to all 6 table occurrences (WALK-01-06 generic row, WALK-03-01, WALK-03-04, WALK-04-02, plus any `/audit/rows/tickets/:pk` in §3 if present).

Do **not** remove shorthand from Route column entirely — hybrid pattern per D-129-07.
  </action>
  <acceptance_criteria>
    - `grep -F 'shorthand /audit/rows/' examples/threadline_phoenix/WALKTHROUGH.md` matches at least 4 times
    - `grep -F 'History** on change row' examples/threadline_phoenix/WALKTHROUGH.md` matches OR `grep -F '**History** on' examples/threadline_phoenix/WALKTHROUGH.md` matches in Drill-down columns
    - Every line matching `/audit/rows/` in operator-surface tables also contains `shorthand` OR `History`
  </acceptance_criteria>
  <verify>grep -n '/audit/rows/' examples/threadline_phoenix/WALKTHROUGH.md</verify>
</task>

</tasks>

<verification>
```bash
grep -n 'mix verify.threadline' examples/threadline_phoenix/WALKTHROUGH.md  # expect no matches
grep -c 'mix threadline.verify_coverage' examples/threadline_phoenix/WALKTHROUGH.md  # expect >= 2
grep -n 'Row history URLs\|/audit/transactions/:id/history' examples/threadline_phoenix/WALKTHROUGH.md
```
</verification>

<success_criteria>
- WALK-01: Optional verify uses `mix threadline.verify_coverage` from example cwd; §0 documents walk vs root cwd; no bare `mix verify.threadline` walk steps.
- WALK-02: Do steps navigation-first; tables label shorthand vs canonical; §0 Row history URLs callout with operator-surface cross-link.
</success_criteria>
