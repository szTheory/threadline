# Milestone archive: v1.8 — Close the support loop

**Status:** SHIPPED 2026-04-24  
**Phases:** 25–27  
**Total plans:** 5 (execution trees: `.planning/milestones/v1.8-phases/25-correlation-aware-timeline-export/`, `26-support-playbooks-doc-contracts/`, `27-example-app-correlation-path/`)

## Overview

Reduce SaaS support **time-to-answer** with **correlation-aware** **`Threadline.Query` / `Threadline.Export`** filters, **Support incident queries** playbooks in **`guides/domain-reference.md`** and **`guides/production-checklist.md`**, **doc contract** anchors (**LOOP-04**), and a **proven correlation path** in **`examples/threadline_phoenix/`** (HTTP header → `record_action` → timeline/export with **`:correlation_id`**). **`v0.2.0`** / **`threadline` 0.2.0** unchanged unless a separate release decision is made.

## Phase table

| Phase | Name | Requirements |
| ----- | ---- | -------------- |
| 25 | Correlation-aware timeline & export | LOOP-01 |
| 26 | Support playbooks & doc contracts | LOOP-02, LOOP-04 |
| 27 | Example app correlation path | LOOP-03 |

**Living snapshot:** requirements copied to this directory as `v1.8-REQUIREMENTS.md`; canonical close state is git history on `main`.

## Phases (detail)

### Phase 25 — Correlation-aware timeline & export

**Goal:** Optional **`:correlation_id`** on timeline and export entrypoints with strict `audit_actions` join when set; validation, CHANGELOG, integration tests for timeline + export shapes.

**Plans:** 25-01, 25-02 — both complete 2026-04-24.

### Phase 26 — Support playbooks & doc contracts

**Goal:** Five canonical support questions mapped to API vs SQL in guides; **`LOOP-04-SUPPORT-INCIDENT-QUERIES`** marker; **`Threadline.SupportPlaybookDocContractTest`**.

**Plans:** 26-01, 26-02 — both complete 2026-04-24.

### Phase 27 — Example app correlation path

**Goal:** Example **`POST /api/posts`** records **`record_action`** in the audited transaction, links **`audit_transactions.action_id`**, integration test **`ThreadlinePhoenixWeb.PostsCorrelationPathTest`**, README correlation + **`export_json`** / **`jq`**.

**Plans:** 27-01 — complete 2026-04-24.

## Milestone summary

**Key decisions**

- **Strict inner join** on `AuditAction` when **`:correlation_id`** is set; optional left join for JSON `action` metadata when unset (export).
- **`action_id` on `audit_transactions`** set in the same `Repo.transaction` as `record_action` so correlation filter matches capture rows.

**Deferred / follow-up**

- **v1.9 telescope** — ops-at-volume (telemetry, health, indexing, retention-at-scale narrative); not opened at this close.
- Phase directories **25–27** moved to **`.planning/milestones/v1.8-phases/`** post-close.

**Technical debt**

- `gsd-sdk query milestone.complete` still returns `version required for phases archive` in this environment; archives written manually to match **v1.6**/**v1.7** closes.
- **`PROJECT.md` Active** listed LOOP-03 until close; reconciled with **`REQUIREMENTS.md`** (already checked) at archive.

---

_For current project status, see `.planning/ROADMAP.md`._
