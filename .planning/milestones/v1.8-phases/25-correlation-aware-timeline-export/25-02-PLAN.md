---
wave: 2
depends_on: [25-01-PLAN.md]
files_modified:
  - lib/threadline/query.ex
  - lib/threadline/export.ex
  - lib/threadline.ex
  - test/threadline/query_test.exs
  - test/threadline/export_test.exs
  - CHANGELOG.md
autonomous: true
requirements_addressed: [LOOP-01]
---

# Plan 25-02: Export payloads, CSV extended profile, tests, CHANGELOG

**Phase:** 25 — Correlation-aware timeline & export  
**Plan:** 25-02 of 02  
**Status:** Ready to execute  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-25-04 | CSV default column drift breaks integrators | high | Default **`to_csv_iodata/2`** header and column **order unchanged**; extended columns only behind **`include_action_metadata: true`** (or the name locked in **25-01**/**CONTEXT** — use **exactly one** keyword documented in CHANGELOG). |
| T-25-05 | JSON export leaks unexpected PII | low | Add only **`correlation_id`** and **`action`** identifiers per **D-3**; no new free-text **`reason`** fields in this phase. |

---

## Objective

Align **`export_changes_query/1`** `select` / row map with any **`AuditAction`** binding introduced in **25-01**, add **JSON** `"action"` object per **D-3**, implement **opt-in extended CSV**, extend **`Threadline`** timeline `@doc` for **`:correlation_id`**, add **tests** (**D-4**), and update **`CHANGELOG.md`**.

---

## must_haves

- [ ] **`Export.to_json_document/2`** (and NDJSON path) includes nested **`"action"`** (or documented alternate) with **`correlation_id`** and action **`id`** string when the transaction is linked to an action.
- [ ] **JSON-first** integration test: same `filters` including **`:correlation_id`**, **`Threadline.timeline/2`** change id list **sorted** equals ids from **`Export.to_json_document/2`** decoded **`changes`** — mirrors existing parity test style.
- [ ] **`validate_timeline_filters!/1`** test: **`:correlation_id`** accepted when valid; unknown key still raises with **`unknown timeline filter`**.
- [ ] **CSV:** at least one test exercises **`:correlation_id`** with **extended** CSV option; default CSV column count/order unchanged for non-extended calls.
- [ ] **`CHANGELOG.md`**: unreleased section documents filter key, validation rules, strict join semantics, JSON/CSV behavior.

---

## Task 25-02-01 — `export_changes_query/1` + `Export` JSON

<read_first>
- `lib/threadline/query.ex` (post-25-01 state, especially `export_changes_query/1`, `timeline_query/1`)
- `lib/threadline/export.ex` (`export_changes_query` consumer paths, `change_map/1`)
- `.planning/phases/25-correlation-aware-timeline-export/25-CONTEXT.md` (D-3)
</read_first>

<action>
1. Update **`export_changes_query/1`** so its **`select`** matches the **binding list** produced by **`timeline_query/1`** (two or three bindings). Include selected fields needed for **`change_map/1`** to emit **`action`** payload (**`aa.correlation_id`**, **`aa.id`** as strings) when **`aa`** is present; use **`nil`**-safe defaults in Elixir map when left join is required for payload — **planner note:** if **25-01** only uses inner join when filtering, you may need a **`left_join`** to **`AuditAction`** in **`export_changes_query`** only for rows to carry action metadata when filter is off; ensure **row counts** and **filter semantics** stay identical to **`timeline/2`** (no duplicate rows).
2. Extend **`change_map/1`** to add **`"action"`** key (map with **`"id"`**, **`"correlation_id"`**) when action fields are non-nil; omit or set **`null`** per chosen JSON convention — document in **`Export` @moduledoc**.
3. Keep default **`to_csv_iodata/2`** output identical when **`include_action_metadata`** is **`false`**/absent.
4. When **`include_action_metadata: true`** in **opts** (or agreed keyword from CONTEXT): append **trailing** columns **`correlation_id`**, **`action_id`** (document order in moduledoc).
</action>

<acceptance_criteria>
- `grep -q '"action"' lib/threadline/export.ex` exits 0 OR `grep -q "'action'" lib/threadline/export.ex` exits 0.
- `grep -q 'include_action_metadata' lib/threadline/export.ex` exits 0 OR `grep -q 'csv_profile' lib/threadline/export.ex` exits 0.
- `mix compile --warnings-as-errors` exits 0.
</acceptance_criteria>

---

## Task 25-02-02 — Tests + top-level docs + CHANGELOG

<read_first>
- `test/threadline/export_test.exs` (`insert_transaction`, `insert_change`, filter parity describe)
- `test/threadline/query_test.exs` (DX-03 describe)
- `lib/threadline.ex` (`timeline/2` doc)
- `CHANGELOG.md` (Unreleased heading style)
- `.planning/phases/25-correlation-aware-timeline-export/25-CONTEXT.md` (D-4, D-5)
</read_first>

<action>
1. Add **`AuditAction`** insert helper or inline insert in tests; create transactions with **`action_id`** / without; set **`correlation_id`** on actions to match filter string **`"loop01-cid"`** (or similar unique test value).
2. Add **`describe` "LOOP-01:`** or extend existing describes: validation tests, JSON **timeline↔export parity** with **`:correlation_id`**, CSV extended path.
3. Update **`Threadline.timeline/2` @doc** in **`lib/threadline.ex`** to list **`:correlation_id`** with one-line semantics (“strict action link”) pointing to **`Threadline.Query`**.
4. **`CHANGELOG.md`**: under **`[Unreleased]`**, add **Added** bullets for **`:correlation_id`**, validation rules, export JSON/CSV notes per **D-4**.
</action>

<acceptance_criteria>
- `grep -q ':correlation_id' test/threadline/query_test.exs` exits 0.
- `grep -q 'correlation' test/threadline/export_test.exs` exits 0.
- `grep -q ':correlation_id' lib/threadline.ex` exits 0.
- `grep -qi 'correlation' CHANGELOG.md` exits 0.
- `mix test test/threadline/query_test.exs test/threadline/export_test.exs` exits 0.
- `mix test` from repository root exits 0.
</acceptance_criteria>

---

## Verification

1. `mix format --check-formatted`
2. `mix compile --warnings-as-errors`
3. `mix test`

---

## Success criteria

- **LOOP-01** acceptance text satisfied: timeline + export entrypoints share **`:correlation_id`**, validation + CHANGELOG, integration tests prove **timeline + export agree** with filter set.
