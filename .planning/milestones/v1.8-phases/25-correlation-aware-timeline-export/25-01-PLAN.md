---
wave: 1
depends_on: []
files_modified:
  - lib/threadline/query.ex
autonomous: true
requirements_addressed: [LOOP-01]
---

# Plan 25-01: Query — `:correlation_id` validation & timeline SQL

**Phase:** 25 — Correlation-aware timeline & export  
**Plan:** 25-01 of 02  
**Status:** Ready to execute  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-25-01 | Unbounded correlation string → DB/DoS pressure | medium | **256 UTF-8 byte cap** after trim (**D-2**); reject empty after trim. |
| T-25-02 | SQL injection via filter | high | **Parameterized** `where`/`join` only — **no** string interpolation into `fragment` for correlation value. |
| T-25-03 | Silent ignore of bad filter → wrong audit reads | low | **ArgumentError** on wrong type / empty / overlong; extend allowed-key list so unknown keys still fail as today. |

---

## Objective

Extend **`Threadline.Query`** so **`:correlation_id`** is validated (**CONTEXT D-2**), documented with existing keys, and **`timeline_query/1`** applies **strict** `AuditAction` correlation semantics (**CONTEXT D-1**) when the filter is set — **conditional** join only in that case, preserving the default query shape when absent.

---

## must_haves

- [ ] `@allowed_timeline_filter_keys` includes **`:correlation_id`**; unknown keys still raise **`ArgumentError`** with an updated allowed list mentioning **`:correlation_id`**.
- [ ] When `:correlation_id` is set (after normalization), results are limited to changes whose transaction has **`action_id`** set and whose linked **`audit_actions.correlation_id`** equals the filter (inner join semantics).
- [ ] Default path (filter absent or `nil` with key absent): **no** `audit_actions` join in `timeline_query/1`.
- [ ] **`export_changes_query/1`** `select` matches the **binding arity** of **`timeline_query/1`** in every branch (two or three bindings). When the correlation join is **off**, the exported map keys match **today’s** map exactly. When **on**, include the same **`ac`/`at`** keys plus any **`aa`** fields needed so **25-02** can read them (may be **`nil`** until export maps use them).

---

## Task 25-01-01 — `validate_timeline_filters!/1` + `@moduledoc`

<read_first>
- `lib/threadline/query.ex` (full file)
- `.planning/phases/25-correlation-aware-timeline-export/25-CONTEXT.md` (D-2, D-1)
- `.planning/REQUIREMENTS.md` (LOOP-01 bullet)
</read_first>

<action>
1. Add **`:correlation_id`** to **`@allowed_timeline_filter_keys`**.
2. When iterating `filters`, if key is **`:correlation_id`**: if value is **`nil`**, raise **`ArgumentError`** with message body containing **`omit`** or **`omit the key`** (CONTEXT: empty/disabled by omission). If value is not **`binary()`**, raise **`ArgumentError`** mentioning **`binary`**. Otherwise apply **`String.trim/1`**; if result is **`""`**, raise **`ArgumentError`** stating empty is not allowed. If **`byte_size > 256`**, raise **`ArgumentError`** mentioning **`256`**.
3. Update **`@moduledoc`** “Timeline filters” section so the bullet list explicitly includes **`:correlation_id`** and points to **CHANGELOG** for semantics.
4. Update **`timeline/2` `@doc`** allowed-options list if it duplicates filter keys (keep in sync with moduledoc).
</action>

<acceptance_criteria>
- `grep -q ':correlation_id' lib/threadline/query.ex` exits 0.
- `grep -q '256' lib/threadline/query.ex` exits 0.
- `mix compile --warnings-as-errors` from repository root exits 0.
</acceptance_criteria>

---

## Task 25-01-02 — `timeline_query/1` conditional `AuditAction` join

<read_first>
- `lib/threadline/query.ex` (`timeline_query/1`, private filters)
- `lib/threadline/capture/audit_transaction.ex` (`action_id`)
- `lib/threadline/semantics/audit_action.ex` (`correlation_id`)
- `.planning/phases/25-correlation-aware-timeline-export/25-CONTEXT.md` (D-1)
</read_first>

<action>
1. **`alias Threadline.Semantics.AuditAction`** in **`Threadline.Query`** if not already present.
2. Implement **`filter_by_correlation/2`** (or equivalent) applied from **`timeline_query/1`** **after** existing `filter_by_*` calls and **before** `order_by`: when **`Keyword.get(filters, :correlation_id)`** is **`nil`** (key absent), **no-op**. When present, use the **already-validated** normalized binary from filters (store normalized value in a **private** step or re-trim consistently — must match validation).
3. When filter active: pipe **`join(:inner, [ac, at], aa in AuditAction, on: at.action_id == aa.id and aa.correlation_id == ^cid)`** (exact join type/kind may be `inner` per D-1).
4. Ensure **`count_matching`** / **`stream_changes`** callers that use **`timeline_query`** without going through **`export_changes_query`** still see identical filter semantics (no code path skips the new predicate).
5. **Same commit / task:** update **`export_changes_query/1`** so its **`select`** uses **`[ac, at]`** or **`[ac, at, aa]`** consistent with **`timeline_query(filters)`** output; preserve existing **map keys** for **`ac`/`at`** columns when the third binding is absent; when present, add **nullable** keys for **`AuditAction`** columns **`id`** and **`correlation_id`** (names chosen by implementer — **25-02** will consume them in **`change_map/1`**).
</action>

<acceptance_criteria>
- `grep -q 'AuditAction' lib/threadline/query.ex` exits 0.
- `grep -q 'filter_by_correlation' lib/threadline/query.ex` exits 0 OR `grep -q 'correlation_id ==' lib/threadline/query.ex` exits 0.
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/threadline/export_test.exs test/threadline/query_test.exs` exits 0 (existing suite; new tests may land in 25-02).
</acceptance_criteria>

---

## Verification

1. `mix format`
2. `mix compile --warnings-as-errors`
3. `mix test test/threadline/query_test.exs test/threadline/export_test.exs`

---

## Success criteria

- **LOOP-01 (query leg):** Timeline filter vocabulary and SQL semantics for **`:correlation_id`** implemented per **25-CONTEXT.md** D-1/D-2.
