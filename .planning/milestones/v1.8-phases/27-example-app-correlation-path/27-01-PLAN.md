---
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix/blog.ex
  - examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs
  - examples/threadline_phoenix/README.md
autonomous: true
requirements_addressed: [LOOP-03]
---

# Plan 27-01: HTTP correlation slice — `record_action`, integration proof, README

**Phase:** 27 — Example app correlation path  
**Plan:** 27-01 of 01  
**Status:** Ready to execute  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-27-01 | **Semantics/capture desync** — `record_action` outside audited txn → misleading correlation queries | high | **`record_action`** only inside the **same** **`Repo.transaction/1`** as GUC + insert; rollback on **`{:error, _}`** from **`record_action`**. |
| T-27-02 | **Actor spoofing** in example | low | Reuse existing **`audit_context.actor_ref`** from **`Threadline.Plug`** — no new actor extraction surface. |
| T-27-03 | **Doc drift** — README claims without CI | medium | README subsection **cross-links** to **`ThreadlinePhoenixWeb.PostsCorrelationPathTest`** by module string; integration test is primary proof (**27-CONTEXT D-1**). |

---

## Objective

Ship **LOOP-03**: example **`POST /api/posts`** path sets **`x-correlation-id`**, performs audited insert **and** **`Threadline.record_action/2`** in one transaction with correlation metadata, with an integration test proving **`Threadline.timeline/2`** returns **≥1** row when filtering by **`correlation_id:`** and **`table:`**, plus README updates (export snippet + remove stale **`action_id`** disclaimer).

---

## must_haves

- [ ] **`Blog.create_post/2`** calls **`Threadline.record_action/2`** in the **same** transaction as the successful **`Post`** insert, after insert succeeds, with **`repo:`**, **`actor:`** from context, and **`correlation_id:`** / **`request_id:`** from **`%AuditContext{}`** (omit or pass **`nil`** only where **`AuditAction`** changeset allows — match library behavior).
- [ ] New test module **`ThreadlinePhoenixWeb.PostsCorrelationPathTest`** passes under **`mix verify.example`**: HTTP POST with **`x-correlation-id`** → non-empty **`Threadline.timeline([table: "posts", correlation_id: <expected>, repo: Repo], [])`** (or equivalent **`opts`** for **`repo`** per existing **`Query`** API).
- [ ] **`README.md`**: documents operator contract **`:correlation_id`** = strict **`audit_actions`** join; links to **`PostsCorrelationPathTest`**; includes **one** **`Threadline.export_json/2`** example with same **`filters`** and **`json_format: :ndjson`** + one-line **`jq`** hint; **removes** the “future release may tighten **`audit_transactions.action_id`**” / example-does-not-rely paragraph once code populates linkage.
- [ ] **REF-01 literals preserved:** `mix phx.server`, `iex -S mix phx.server`, `mix test`, `ecto.migrate` remain in README as today unless `readme_doc_contract_test.exs` is intentionally extended.

---

## Task 27-01-01 — `Blog.create_post/2`: GUC + insert + `record_action`

<read_first>
- `examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` (full file)
- `lib/threadline.ex` (`record_action/2` @doc — opts list)
- `lib/threadline/job.ex` (`context_opts/1` — pattern for optional correlation keys)
- `.planning/phases/27-example-app-correlation-path/27-CONTEXT.md` (D-2)
</read_first>

<action>
1. Inside the existing **`Repo.transaction`** in **`create_post/2`**, after **`{:ok, post}`** from **`Repo.insert`**, build keyword opts: **`[repo: Repo, actor: actor_ref]`** plus **`correlation_id:`** and **`request_id:`** taken from **`%AuditContext{}`** fields (use **`audit_context.correlation_id`**, **`audit_context.request_id`** — pass only if non-**`nil`** or pass **`nil`** if changeset accepts; align with **`Job.context_opts/1`** style).
2. Call **`Threadline.record_action(:post_created_via_api, opts)`** (or rename atom if you prefer — **must match** test grep in task 27-01-02).
3. On **`{:ok, _}`** from **`record_action`**, return **`post`** from transaction; on **`{:error, changeset}`**, **`Repo.rollback(changeset)`**.
4. Update **`@moduledoc`** on **`create_post/2`** to state semantics **`record_action`** runs in the **same** transaction as capture.
</action>

<acceptance_criteria>
- `grep -q 'record_action' examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 0.
- `grep -q 'post_created_via_api' examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 0.
- `mix compile --warnings-as-errors` from repository root exits 0.
</acceptance_criteria>

---

## Task 27-01-02 — Integration test: HTTP → `timeline` with `:correlation_id`

<read_first>
- `examples/threadline_phoenix/test/threadline_phoenix_web/posts_audit_path_test.exs` (full file)
- `examples/threadline_phoenix/test/support/conn_case.ex` (if exists — ConnCase patterns)
- `lib/threadline/query.ex` (`validate_timeline_filters!/1`, **`timeline/2`** `@doc`)
- `.planning/phases/27-example-app-correlation-path/27-CONTEXT.md` (D-1, D-3)
</read_first>

<action>
1. Add **`examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs`** with **`defmodule ThreadlinePhoenixWeb.PostsCorrelationPathTest`**, **`use ThreadlinePhoenixWeb.ConnCase, async: false`**.
2. Test flow: fixed correlation string (e.g. **`"loop-03-corr-#{System.unique_integer([:positive])}"`**) → **`build_conn()`** with **`content-type`**, **`x-request-id`**, **`x-correlation-id`** → **`post(~p"/api/posts", ...)`** with unique slug → assert **201**.
3. Call **`Threadline.timeline([table: "posts", correlation_id: <same>, repo: ThreadlinePhoenix.Repo], [])`** — **`timeline/2`** invokes **`Threadline.Query.validate_timeline_filters!/1`** internally (do **not** assume **`Threadline.validate_timeline_filters!/1`** exists on the facade). Optional explicit **`Threadline.Query.validate_timeline_filters!/1`** call is allowed for clarity.
4. Assert **non-empty** list and at least one change relates to inserted post (e.g. **`table_name == "posts"`** and operation/title/slug field as appropriate).
5. Optionally assert **`AuditAction`** row exists with same correlation — only if stable with public schemas; otherwise timeline assertion satisfies LOOP-03.
</action>

<acceptance_criteria>
- `test -f examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs` exits 0.
- `grep -q 'ThreadlinePhoenixWeb.PostsCorrelationPathTest' examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs` exits 0.
- `grep -q 'correlation_id' examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs` exits 0.
- `mix verify.example` from repository root exits 0.
</acceptance_criteria>

---

## Task 27-01-03 — README: operator contract, export snippet, cross-link test

<read_first>
- `examples/threadline_phoenix/README.md` (full file)
- `test/threadline/readme_doc_contract_test.exs` (REF-01 substrings)
- `.planning/phases/27-example-app-correlation-path/27-CONTEXT.md` (D-3, D-4)
</read_first>

<action>
1. Add a short subsection (suggested heading: **`## Correlation: HTTP → audit_actions → timeline`**) after or within the audited HTTP / semantics flow: explain **`x-correlation-id`** → **`AuditContext`** → **`record_action`** → **`:correlation_id`** on **`Threadline.timeline/2`** / **`Threadline.export_json/2`** (same filter vocabulary as LOOP-01).
2. Embed **one** **`export_json`** code sample: same **`filters`** list as timeline example, **`json_format: :ndjson`**, **`repo: ThreadlinePhoenix.Repo`** (or module name matching example app), plus **one** line showing **`jq`** reading a field (e.g. **`.table_name`** or **`.verb`** — pick a field export emits).
3. Cross-link: prose must contain the exact substring **`ThreadlinePhoenixWeb.PostsCorrelationPathTest`** pointing maintainers to CI proof.
4. Delete/replace the README paragraph stating the example does not rely on **`audit_transactions.action_id`** / future tightening — replace with accurate **strict filter** wording consistent with Phase 25 (**27-CONTEXT D-4**).
5. Keep existing REF-01 command literals intact unless adding a new contract line to **`readme_doc_contract_test.exs`**.
</action>

<acceptance_criteria>
- `grep -q 'ThreadlinePhoenixWeb.PostsCorrelationPathTest' examples/threadline_phoenix/README.md` exits 0.
- `grep -q 'export_json' examples/threadline_phoenix/README.md` exits 0.
- `grep -q 'jq' examples/threadline_phoenix/README.md` exits 0.
- `grep -q 'mix phx.server' examples/threadline_phoenix/README.md` exits 0 AND `grep -q 'iex -S mix phx.server' examples/threadline_phoenix/README.md` exits 0 AND `grep -q 'mix test' examples/threadline_phoenix/README.md` exits 0 AND `grep -q 'ecto.migrate' examples/threadline_phoenix/README.md` exits 0.
- `mix verify.doc_contract` from repository root exits 0.
</acceptance_criteria>

---

## Verification

1. `mix format`
2. `mix compile --warnings-as-errors`
3. `mix verify.example`
4. `mix verify.doc_contract`

---

## Success criteria

- **LOOP-03:** Example demonstrates end-to-end correlation path with **automated** proof and README aligned to behavior.
