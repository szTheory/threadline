---
wave: 2
depends_on: [26-01-PLAN.md]
files_modified:
  - test/threadline/support_playbook_doc_contract_test.exs
autonomous: true
requirements_addressed: [LOOP-04]
---

# Plan 26-02: LOOP-04 doc contract tests

**Phase:** 26 — Support playbooks & doc contracts  
**Plan:** 26-02 of 02  
**Status:** Ready to execute  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-26-04 | Tests pass while headings drift from operator-visible strings | low | Assert **exact** full heading lines agreed with **26-01**; changing copy requires coordinated test + guide edit. |

---

## Objective

Add **`test/threadline/support_playbook_doc_contract_test.exs`** per **26-CONTEXT D-3**, mirroring **`Threadline.StgDocContractTest`**, locking **LOOP-04** anchors for the Support incident material.

---

## Locked strings (26-01 must match)

These are the **contract headings** — **26-01** implementation must use them **verbatim** (including ASCII hyphen spacing):

- Section: `## Support incident queries`
- Subsections (domain-reference only):
  - `### 1. Row history - PK changes in a time window`
  - `### 2. Actor window - one actor across tables`
  - `### 3. Correlation bundle - shared correlation_id`
  - `### 4. Export parity - timeline and export filters agree`
  - `### 5. Action and capture - link semantic actions to changes`
- Marker: `LOOP-04-SUPPORT-INCIDENT-QUERIES`

---

## must_haves

- [ ] New test module reads guides from repo root via **`read_rel!/1`** helper (same pattern as **`test/threadline/stg_doc_contract_test.exs`**).
- [ ] Asserts section heading in **both** guides; five subsection headings + marker in **domain-reference**; at-a-glance table lists questions **1–5** (e.g. assert body contains **`| 1 |`** … **`| 5 |`** on table rows OR contains all five subsection heading lines — pick one approach and document in test comment).

---

## Task 26-02-01 — `support_playbook_doc_contract_test.exs`

<read_first>
- `test/threadline/stg_doc_contract_test.exs` (structure)
- `guides/domain-reference.md` (post 26-01)
- `guides/production-checklist.md` (post 26-01)
- `.planning/phases/26-support-playbooks-doc-contracts/26-CONTEXT.md` (D-3, D-6)
</read_first>

<action>
1. Create **`test/threadline/support_playbook_doc_contract_test.exs`** defining **`Threadline.SupportPlaybookDocContractTest`** with **`use ExUnit.Case, async: true`**, **`@repo_root File.cwd!()`**, private **`read_rel!/1`** matching STG test.
2. Tests:
   - **`domain-reference.md`** contains **`## Support incident queries`** and all **five** subsection heading lines and **`LOOP-04-SUPPORT-INCIDENT-QUERIES`**.
   - **`production-checklist.md`** contains **`## Support incident queries`**.
   - **At-a-glance invariant:** assert **`domain`** contains **`| 1 |`** and **`| 5 |`** (table row markers) **OR** (alternative) assert each of **`### 1.`** … **`### 5.`** — choose **one** and implement.
3. **`@moduledoc false`** on module.
</action>

<acceptance_criteria>
- `test -f test/threadline/support_playbook_doc_contract_test.exs` exits 0.
- `mix test test/threadline/support_playbook_doc_contract_test.exs` exits 0 **after** 26-01 guide content exists.
- `grep -q 'defmodule Threadline.SupportPlaybookDocContractTest' test/threadline/support_playbook_doc_contract_test.exs` exits 0.
</acceptance_criteria>

---

## Task 26-02-02 — Suite verification

<read_first>
- `mix.exs` (aliases — optional doc_contract extension is **not** required)
</read_first>

<action>
1. Run **`mix format`**
2. Run **`mix compile --warnings-as-errors`**
3. Run **`mix test`** from repository root.
</action>

<acceptance_criteria>
- `mix format --check-formatted` exits 0.
- `mix compile --warnings-as-errors` exits 0.
- `mix test` exits 0.
</acceptance_criteria>

---

## Verification

1. `mix format --check-formatted`
2. `mix compile --warnings-as-errors`
3. `mix test test/threadline/support_playbook_doc_contract_test.exs`
4. `mix test`

---

## Success criteria

- **LOOP-04** satisfied: automated tests lock headings/marker for Support incident sections so they cannot rot silently.
