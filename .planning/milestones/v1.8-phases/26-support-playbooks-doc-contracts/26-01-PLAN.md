---
wave: 1
depends_on: []
files_modified:
  - guides/domain-reference.md
  - guides/production-checklist.md
autonomous: true
requirements_addressed: [LOOP-02]
---

# Plan 26-01: Support incident query playbooks (two guides)

**Phase:** 26 — Support playbooks & doc contracts  
**Plan:** 26-01 of 02  
**Status:** Ready to execute  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-26-01 | Operators paste example SQL against prod without bounds | medium | Document **`LIMIT`**, **`:from`/`:to`**, and **read-only / replica** expectation once in subsection intro; golden queries include bounds where applicable. |
| T-26-02 | Wrong **`:correlation_id`** semantics in prose mislead incident response | medium | Copy **strict inner-join** semantics from **`lib/threadline/query.ex`** moduledoc and **`.planning/phases/25-correlation-aware-timeline-export/25-CONTEXT.md`** — no orphan capture rows in filtered results. |
| T-26-03 | Schema/table names in SQL imply a specific deploy layout | low | Use **`your_schema`** (or single chosen placeholder) consistently + **Replace before run** bullet block above each fence. |

---

## Objective

Implement **LOOP-02**: add **Support incident queries** to **`guides/domain-reference.md`** and **`guides/production-checklist.md`**, mapping the five canonical support questions (see **`.planning/REQUIREMENTS.md`**, “Evidence-driving questions”) to **API / Mix** vs **copy-paste SQL**, per **26-CONTEXT.md** D-1–D-6. **Single source of truth** for long SQL lives in **domain-reference**; checklist carries a **compact three-column** table and **anchor links** only.

---

## must_haves

- [ ] Both guides contain heading line **exactly** `## Support incident queries` (for downstream LOOP-04 tests).
- [ ] **domain-reference.md**: at-a-glance narrow markdown table (question # **1–5** → primary path); body includes marker token **`LOOP-04-SUPPORT-INCIDENT-QUERIES`** once; five subsection headings **exactly** the strings in **Task 26-01-01** `<action>` item 4 (must match **26-02** contract tests).
- [ ] **production-checklist.md**: same section title; compact **Question → API/Mix vs SQL** table with **short** SQL column (anchors / pointers to domain-reference, not duplicated multi-line SQL).
- [ ] Q3 prose states **strict** `:correlation_id` filter semantics (transaction must link to **AuditAction** with matching correlation).

---

## Task 26-01-01 — `domain-reference.md` Support playbook (canonical)

<read_first>
- `guides/domain-reference.md` (full file — pick insertion point after `## Correlation` or integrate without breaking flow)
- `.planning/phases/26-support-playbooks-doc-contracts/26-CONTEXT.md` (D-1, D-2, D-4, D-5, D-6)
- `.planning/REQUIREMENTS.md` (five canonical questions, LOOP-02)
- `.planning/phases/25-correlation-aware-timeline-export/25-CONTEXT.md` (correlation semantics)
- `lib/threadline/query.ex` (`@moduledoc` timeline filters, correlation join behavior)
- `lib/threadline/export.ex` (module doc entrypoints)
- `lib/mix/tasks/threadline.export.ex` (`@moduledoc` for task name)
</read_first>

<action>
1. Add **`## Support incident queries`** with short intro: SQL-native operator playbooks, read-only expectation, placeholder convention (**`your_schema`** + **Replace before run** list).
2. Add **at-a-glance** markdown table: columns **# | Question | Primary path**; rows **1–5** aligned to REQUIREMENTS question titles; **Primary path** cells are pointers only (`Threadline.Query.timeline/2`, `mix threadline.export`, **§** anchor to subsection, etc.) — **no** multi-line SQL in table cells.
3. Insert literal marker line containing **`LOOP-04-SUPPORT-INCIDENT-QUERIES`** once in the section body (visible text, not HTML comment).
4. Add **five** subsections using **exactly** these heading lines (also asserted by **26-02** tests): `### 1. Row history - PK changes in a time window`, `### 2. Actor window - one actor across tables`, `### 3. Correlation bundle - shared correlation_id`, `### 4. Export parity - timeline and export filters agree`, `### 5. Action and capture - link semantic actions to changes`.
5. Under each subsection: small table or bullets for **API/Mix vs SQL**; below that, **fenced** `sql` golden queries or fragments per **D-2**; cite **`audit_changes`**, **`audit_transactions`**, **`audit_actions`** column names where relevant.
6. For **Q3**, explicitly document: with **`:correlation_id`**, timeline/export return only rows whose transaction **joins** an **`audit_actions`** row with that correlation (no implied inclusion of capture rows without that link).
</action>

<acceptance_criteria>
- `grep -nF '## Support incident queries' guides/domain-reference.md` exits 0.
- `grep -q 'LOOP-04-SUPPORT-INCIDENT-QUERIES' guides/domain-reference.md` exits 0.
- `grep -qF '### 1. Row history - PK changes in a time window' guides/domain-reference.md` exits 0.
- `grep -qF '### 2. Actor window - one actor across tables' guides/domain-reference.md` exits 0.
- `grep -qF '### 3. Correlation bundle - shared correlation_id' guides/domain-reference.md` exits 0.
- `grep -qF '### 4. Export parity - timeline and export filters agree' guides/domain-reference.md` exits 0.
- `grep -qF '### 5. Action and capture - link semantic actions to changes' guides/domain-reference.md` exits 0.
- `grep -qi 'audit_changes' guides/domain-reference.md` exits 0.
- `grep -qi 'correlation_id' guides/domain-reference.md` exits 0.
</acceptance_criteria>

---

## Task 26-01-02 — `production-checklist.md` Support playbook (compact)

<read_first>
- `guides/production-checklist.md` (full file)
- `guides/domain-reference.md` (after 26-01-01 — copy anchor fragments for links)
- `.planning/phases/26-support-playbooks-doc-contracts/26-CONTEXT.md` (D-1 checklist half)
</read_first>

<action>
1. Add **`## Support incident queries`** (same exact heading string as domain-reference).
2. Add **compact** three-column markdown table: **Question (1-line) | API / Mix | SQL** where **SQL** column contains **only** short pointers (e.g. `domain-reference.md#3-correlation-bundle---shared-correlation_id` or slug-stable link matching generated anchors) — **not** duplicated fenced SQL blocks.
3. Add checklist bullets (optional per CONTEXT) with **repo-relative** links to each of the five subsection anchors in **domain-reference.md**.
</action>

<acceptance_criteria>
- `grep -nF '## Support incident queries' guides/production-checklist.md` exits 0.
- `grep -q 'domain-reference.md#' guides/production-checklist.md` exits 0.
- `grep -q '|' guides/production-checklist.md` exits 0 (table row present — pipe table).
</acceptance_criteria>

---

## Verification

1. `mix format`
2. Manual: open both guides in preview — anchors for cross-links resolve.

---

## Success criteria

- **LOOP-02** satisfied: both guides answer questions **1–5** with **API/Mix vs SQL** mapping; SQL-native; no LiveView.
