# Plan 06-02: CI discovery (CI-03) and maintainer proof kit (CI-02)

**Phase:** 6 — CI on GitHub  
**Plan:** 06-02 of 02  
**Status:** Ready to execute  
**Depends on:** 06-01 (local parity and contract verified)  
**Blocks:** Phase 7 (Hex publish assumes trustworthy `main`)  
**Requirements covered:** CI-02, CI-03  
**Wave:** 1  
**requirements_addressed:** CI-02, CI-03  
**files_modified:** `README.md` (expected), `.planning/phases/06-ci-on-github/06-VERIFICATION.md` (create), `CONTRIBUTING.md` (optional — only if a grep in Task 2 fails)  
**autonomous:** true  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-06-02-01 | README or verification doc points contributors at wrong repo or fork | high | All new URLs must contain the substring `github.com/szTheory/threadline` and the Actions path segment `actions` (hub or workflow-scoped as specified per task). |
| T-06-02-02 | Maintainer records wrong workflow or job names → false CI-02 sign-off | medium | `06-VERIFICATION.md` template requires literal strings `ci.yml`, `verify-format`, `verify-credo`, `verify-test` in the checklist (`06-CONTEXT` D-12). |

---

## Goal

Close **CI-03** by making README layout match **D-05** (CI discovery immediately adjacent to the badge row). Produce **`06-VERIFICATION.md`** so a maintainer can record **CI-02** proof (SHA-aligned `gh` output + run URL) per **D-10–D-12**, without relying on the badge alone (**D-11**).

---

## Context

- **Authoritative:** `.planning/phases/06-ci-on-github/06-CONTEXT.md` (D-05, D-06, D-10–D-12), `.planning/REQUIREMENTS.md` (CI-02, CI-03), `README.md`, `CONTRIBUTING.md`.

---

## Tasks

### Task 1: CI-03 — README badge row + CI line adjacency (D-05)

**What to do**

1. Open `README.md` at the top: the last workflow/badge line is the HexDocs shield (line beginning `[![HexDocs`).
2. Ensure the **first** non-empty line **after** that badge line is the `**CI:**` paragraph (same wording allowed; no blank line between the HexDocs badge and `**CI:**`). Remove a single intervening blank line if present.
3. Keep the existing CI badge row (first three badges) unchanged in URL semantics.

**Read first**

- `README.md`
- `.planning/phases/06-ci-on-github/06-CONTEXT.md` (D-05, D-06)

**Acceptance criteria**

- `grep -qF '**CI:** Runs on' README.md` exits 0 (CI paragraph still present).
- Python 3 one-liner from repo root exits 0 — **strict D-05:** the line **immediately after** the HexDocs badge line must be `**CI:**` (no intervening blank line):  
  `python3 -c "import pathlib; t=pathlib.Path('README.md').read_text().splitlines(); idx=[i for i,l in enumerate(t) if l.strip().startswith('[![HexDocs')][0]; rest=t[idx+1:]; assert rest and rest[0].startswith('**CI:**'), rest[:3]"`

**Done condition:** Both checks succeed.

---

### Task 2: CI-03 — CONTRIBUTING Actions discovery

**What to do**

1. Confirm `CONTRIBUTING.md` § “CI parity and `act`” still contains a live Actions URL for `main` and names the three job keys.

**Read first**

- `CONTRIBUTING.md`
- `.planning/phases/06-ci-on-github/06-CONTEXT.md` (D-05)

**Acceptance criteria**

- `grep -F 'verify-format' CONTRIBUTING.md` exits 0.
- `grep -F 'verify-credo' CONTRIBUTING.md` exits 0.
- `grep -F 'verify-test' CONTRIBUTING.md` exits 0.
- `grep -F 'https://github.com/szTheory/threadline/actions' CONTRIBUTING.md` exits 0.

**Done condition:** All four greps succeed. If any grep fails, add the missing string(s) in that section only — then re-run greps until all succeed.

---

### Task 3: CI-02 — Add `06-VERIFICATION.md` maintainer checklist

**What to do**

1. Create `.planning/phases/06-ci-on-github/06-VERIFICATION.md` with markdown sections:
   - `# Phase 6 verification (CI-02 / CI-03)`
   - `## origin/main SHA` — placeholder line `SHA: (paste output of: git fetch origin main && git rev-parse origin/main)`
   - `## GitHub Actions run` — placeholder for **workflow file** name `ci.yml` and the three job keys spelled exactly: `verify-format`, `verify-credo`, `verify-test`
   - `## gh audit (preferred)` — fenced `bash` block containing exactly these two commands on separate lines:  
     `gh run list --repo szTheory/threadline --workflow=ci.yml --branch=main --limit=5`  
     `gh run view RUN_ID --repo szTheory/threadline --json conclusion,headSha,url`
   - `## Run URL` — placeholder `https://github.com/...` for maintainer paste
   - Short note: badge alone is insufficient per `06-CONTEXT` D-11.

**Read first**

- `.planning/phases/06-ci-on-github/06-CONTEXT.md` (D-10, D-11, D-12)
- `.planning/phases/05-repository-remote/05-VERIFICATION.md` (tone/structure reference if present)

**Acceptance criteria**

- `test -f .planning/phases/06-ci-on-github/06-VERIFICATION.md` exits 0.
- `grep -F 'ci.yml' .planning/phases/06-ci-on-github/06-VERIFICATION.md` exits 0.
- `grep -F 'verify-format' .planning/phases/06-ci-on-github/06-VERIFICATION.md` exits 0.
- `grep -F 'verify-credo' .planning/phases/06-ci-on-github/06-VERIFICATION.md` exits 0.
- `grep -F 'verify-test' .planning/phases/06-ci-on-github/06-VERIFICATION.md` exits 0.
- `grep -F 'gh run list --repo szTheory/threadline --workflow=ci.yml --branch=main --limit=5' .planning/phases/06-ci-on-github/06-VERIFICATION.md` exits 0.
- `grep -F 'gh run view RUN_ID --repo szTheory/threadline --json conclusion,headSha,url' .planning/phases/06-ci-on-github/06-VERIFICATION.md` exits 0.

**Done condition:** All greps succeed.

---

## Verification

After all tasks:

1. Run `MIX_ENV=test mix ci.all` — must exit 0 (no doc-only regressions that break compile/tests).

---

## Success criteria

- README satisfies Task 1 Python adjacency check.
- CONTRIBUTING satisfies Task 2 greps.
- `06-VERIFICATION.md` exists with required strings for CI-02 maintainer follow-up.
