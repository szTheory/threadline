---
phase: 182-phoenixstorybook-example-dev-lane
plan: 02
type: execute
wave: 2
depends_on:
  - 182-01
files_modified:
  - examples/threadline_phoenix/mix.exs
  - examples/threadline_phoenix/mix.lock
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/storybook.ex
autonomous: true
requirements:
  - STORY-01
must_haves:
  truths:
    - "STORY-01: PhoenixStorybook is installed only in the Phoenix example app and is dev/test-only."
    - "D-182-01: Root `mix.exs`, root `mix.lock`, root `lib/`, and the public operator macro remain free of the Storybook dependency and route surface."
    - "D-182-02, D-182-03, and D-182-04: `/dev/storybook` and `/dev/storybook/assets` are mounted with PhoenixStorybook's root-scope shape and only when example dev/test routes are enabled."
    - "D-182-05: No normal operator login is required for this local maintainer lab unless implementation evidence shows exposure beyond dev/test."
  artifacts:
    - path: "examples/threadline_phoenix/mix.exs"
      provides: "Example-only dev/test PhoenixStorybook dependency"
      contains: "only: [:dev, :test]"
    - path: "examples/threadline_phoenix/mix.lock"
      provides: "Resolved example-app PhoenixStorybook lock entry"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/storybook.ex"
      provides: "PhoenixStorybook backend module"
      exports: ["ThreadlinePhoenixWeb.Storybook"]
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      provides: "Dev/test-only Storybook route and assets mount"
      contains: "/dev/storybook"
  key_links:
    - from: "examples/threadline_phoenix/mix.exs"
      to: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      via: "dev/test dependency available only where dev_routes mounts route"
      pattern: "only: \\[:dev, :test\\]"
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/storybook.ex"
      to: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      via: "backend module passed to live_storybook route macro"
      pattern: "ThreadlinePhoenixWeb.Storybook"
---

<objective>
Install and mount PhoenixStorybook only inside the Phoenix example app.

Purpose: Deliver the maintainer route boundary for STORY-01 while preserving the root package optional-dependency contract and production host surface.
Output: Example-app dependency, lockfile, backend module, and guarded route/assets mount.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-CONTEXT.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-RESEARCH.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-VALIDATION.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-PATTERNS.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-UI-SPEC.md
@CLAUDE.md
@examples/threadline_phoenix/AGENTS.md
@examples/threadline_phoenix/mix.exs
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@examples/threadline_phoenix/config/dev.exs
@examples/threadline_phoenix/config/test.exs
@examples/threadline_phoenix/config/prod.exs
@examples/threadline_phoenix/test/threadline_phoenix_web/storybook_route_test.exs
@test/threadline/operator_surface/storybook_boundary_test.exs
</context>

<!-- planner-discipline-allow: PhoenixStorybook -->
<!-- planner-discipline-allow: phoenix_storybook -->
<!-- planner-discipline-allow: live_storybook -->
<!-- planner-discipline-allow: /dev/storybook -->

## Artifacts this phase produces

- New route paths: `/dev/storybook` and `/dev/storybook/assets`, only in `examples/threadline_phoenix` dev/test route configuration.
- New dependency boundary: `{:phoenix_storybook, "~> 1.2.0", only: [:dev, :test]}` in `examples/threadline_phoenix/mix.exs` only, with example lockfile resolution.
- New module: `ThreadlinePhoenixWeb.Storybook`.
- No root package dependency, root route macro, production route, public component API, schema file, migration, CLI flag, struct field, decorator, or dataclass field is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add example-only Storybook dependency and backend</name>
  <files>examples/threadline_phoenix/mix.exs, examples/threadline_phoenix/mix.lock, examples/threadline_phoenix/lib/threadline_phoenix_web/storybook.ex</files>
  <read_first>
    @examples/threadline_phoenix/mix.exs
    @examples/threadline_phoenix/mix.lock (generated; read if present)
    @examples/threadline_phoenix/lib/threadline_phoenix_web/storybook.ex (create; read if present)
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-RESEARCH.md (Package Legitimacy Audit and Standard Stack)
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-PATTERNS.md
    @examples/threadline_phoenix/AGENTS.md
    @mix.exs
  </read_first>
  <behavior>
    - Test expectation: example dependency resolution locks an approved package version at or above the security floor from 182-RESEARCH.
    - Test expectation: root `mix verify.compile_no_optional` stays green after example-app dependency changes.
    - Test expectation: production compilation does not require the dev/test Storybook package.
  </behavior>
  <action>Add the approved Hex package as an `only: [:dev, :test]` dependency in `examples/threadline_phoenix/mix.exs` per D-182-01 and D-182-19, then run example-app dependency resolution so only `examples/threadline_phoenix/mix.lock` changes. Create `ThreadlinePhoenixWeb.Storybook` as the PhoenixStorybook backend module, guarded so production compilation does not require the dev/test package when unavailable. Configure its title/content path for the curated example-app story tree and avoid adding public-facing docs to private Threadline components per D-182-09.</action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix deps.get && MIX_ENV=test mix compile --warnings-as-errors</automated>
    <automated>mix verify.compile_no_optional</automated>
    <automated>bash -lc 'set +e; rg -n "PhoenixStorybook|phoenix_storybook|live_storybook" mix.exs mix.lock lib/threadline; status=$?; test "$status" -eq 1'</automated>
    <automated>rg -n "phoenix_storybook.*1\\.2|phoenix_storybook" examples/threadline_phoenix/mix.exs examples/threadline_phoenix/mix.lock</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: only `examples/threadline_phoenix/mix.exs` declares the new Storybook dependency, with `only: [:dev, :test]`.
    - Source assertion: root `mix.exs`, root `mix.lock`, and root `lib/threadline` remain free of Storybook package/module/router macro references.
    - Source assertion: `ThreadlinePhoenixWeb.Storybook` exists and points PhoenixStorybook at the curated example-app story content directory.
    - Test-command assertion: example-app test compile and root `mix verify.compile_no_optional` both pass.
    - Security assertion: lockfile resolution keeps the package outside affected versions documented in `182-RESEARCH.md`.
  </acceptance_criteria>
  <done>The example app can compile with the Storybook backend where the dev/test dependency is allowed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount the dev/test route and assets</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</files>
  <read_first>
    @examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
    @examples/threadline_phoenix/lib/threadline_phoenix_web/storybook.ex
    @examples/threadline_phoenix/test/threadline_phoenix_web/storybook_route_test.exs
    @examples/threadline_phoenix/config/dev.exs
    @examples/threadline_phoenix/config/test.exs
    @examples/threadline_phoenix/config/prod.exs
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-CONTEXT.md
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-RESEARCH.md
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-PATTERNS.md
  </read_first>
  <behavior>
    - Test expectation: route table includes `/dev/storybook` and `/dev/storybook/assets` only when the example app enables dev routes.
    - Test expectation: route table excludes Storybook route/assets when dev routes are disabled or production config is used.
    - Test expectation: the route remains outside `/audit` and does not change `/audit/__stress`.
  </behavior>
  <action>Update the example router inside the existing `Application.compile_env(:threadline_phoenix, :dev_routes)` branch. Import/use PhoenixStorybook router macros only inside that guarded branch, mount `storybook_assets` at `/dev/storybook/assets`, and mount `live_storybook` with a root `scope "/"` and full `/dev/storybook` path per D-182-02, D-182-03, and D-182-04. Keep it out of normal operator navigation and do not add operator login unless implementation proves the route is exposed beyond local dev/test, per D-182-05.</action>
  <verify>
    <automated>cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/storybook_route_test.exs</automated>
    <automated>mix test test/threadline/operator_surface/storybook_boundary_test.exs</automated>
    <automated>mix verify.compile_no_optional</automated>
  </verify>
  <acceptance_criteria>
    - Test-command assertion: `storybook_route_test.exs` passes and proves dev/test route presence plus production/no-dev-routes absence.
    - Source assertion: route and assets use `/dev/storybook` paths, not an `/audit` path.
    - Source assertion: PhoenixStorybook router macro imports/usages are inside the dev_routes compile branch.
    - Test-command assertion: root Storybook boundary test and root compile-no-optional gate pass.
    - Regression assertion: `/audit/__stress` route/auth behavior is not edited by this plan.
  </acceptance_criteria>
  <done>The example app owns a dev/test-only Storybook route and assets mount that does not leak into production or root package surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry -> example app | A new package enters the example app dependency graph only. |
| Example router -> browser | `/dev/storybook` serves local maintainer UI and assets. |
| Dev/test config -> production config | Production must not compile or expose the maintainer route. |

## ASVS L1 Baseline

ASVS L1 baseline applies to this plan. Any high-severity finding in route exposure, production absence, dependency leakage, auth/session behavior, unsafe dynamic input handling, or unsafe atom conversion blocks execution and Phase 182 closure until mitigated or explicitly accepted in verification.

| ASVS Area | Plan Mapping |
|-----------|--------------|
| V4 Access Control / V14 Configuration | The router mount and assets must stay inside the example app `dev_routes` boundary, outside `/audit`, and absent from production/no-dev-routes builds. |
| V14 Dependency Management / Configuration | `phoenix_storybook` and its lockfile entries must remain example-app dev/test-only and must not enter root package or public macro surfaces. |
| V2 Authentication / V3 Session Management | This plan must not edit `/audit/__stress` auth/session behavior; if Storybook exposure extends beyond local/dev/test, add a dev/test maintainer guard before closure. |
| V5 Validation, Sanitization, and Encoding | Router/backend code must not derive module names, route names, or atoms from untrusted request input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-04 | Elevation of Privilege | `/dev/storybook` route | mitigate | Mount route/assets only under `dev_routes`; route tests prove production/no-dev-routes absence. |
| T-182-05 | Information Disclosure | Storybook component previews | mitigate | Keep route local/dev/test and outside `/audit`; do not link from production/demo/operator navigation. |
| T-182-06 | Denial of Service | Vulnerable package version | mitigate | Use the approved `~> 1.2.0` package floor from research and assert lockfile resolution stays outside affected ranges. |
| T-182-SC | Tampering | Hex package install | mitigate | Apply the `182-RESEARCH.md` Package Legitimacy Audit; no `[SLOP]` package is allowed and the package remains example dev/test-only. |
</threat_model>

<verification>
Run example dependency resolution and test compile, route contracts, root Storybook boundary contracts, and `mix verify.compile_no_optional`.
</verification>

<success_criteria>
- STORY-01 is satisfied at the package and route boundary level.
- Route/assets exist only in the example app dev/test configuration.
- Root package optional-dependency and public macro boundaries remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/182-phoenixstorybook-example-dev-lane/182-02-SUMMARY.md` when done.
</output>
