---
phase: 188-close-gap-v1-38-export-queue-and-motion-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/style_contract_test.exs
autonomous: true
requirements:
  - MOTION-01
  - CLOSE-01
requirements_addressed:
  - MOTION-01
  - CLOSE-01
must_haves:
  truths:
    - "MOTION-01: .tl-copy keeps its current visible copy affordance while declaring explicit transition properties."
    - "MOTION-01: .tl-copy transition properties are color, border-color, background-color, and box-shadow with var(--tl-motion-fast) and var(--tl-ease-standard)."
    - "MOTION-01: Source contracts reject implicit transition-all, literal transition-property all, token-only transition shorthand without a property list, and layout-affecting transition properties."
    - "CLOSE-01: Motion proof stays source-focused and does not add screenshot matrices, new keyframes, new tokens, animation libraries, or decorative motion."
    - "D-188-12/D-188-13/D-188-14/D-188-15/D-188-16: Copy visual behavior is preserved while the motion guard is strengthened."
  artifacts:
    - path: "lib/threadline/operator_surface/style.ex"
      provides: ".tl-copy explicit transition-property/source CSS"
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "Source contract against implicit transition-all and layout-affecting motion"
  key_links:
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "source parsing of .tl-copy and transition declarations"
      pattern: "tl-copy"
---

<objective>
Close the .tl-copy implicit transition-all validation gap without changing visible copy-button behavior or widening browser/screenshot proof.

Purpose: MOTION-01 is partial because .tl-copy uses a duration/easing shorthand that computes as transition-property all. This plan changes the CSS to explicit governed properties and hardens the source contract per D-188-12 through D-188-16.
Output: Failing-then-passing style contract proof and a .tl-copy CSS declaration that keeps existing hover, copied, focus, and tl-copy-pulse behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.38-MILESTONE-AUDIT.md
@.planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-CONTEXT.md
@.planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-RESEARCH.md
@.planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-VALIDATION.md
@.planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-UI-SPEC.md
@.planning/phases/187-accessibility-motion-docs-and-adversarial-closeout/187-02-SUMMARY.md
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/style_contract_test.exs
@examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts
@.planning/milestones/v1.31-phases/141-motion-micro-animation/141-MOTION-INVENTORY.md
</context>

## Artifacts this phase produces

- Modifies the `.tl-copy` CSS selector in `lib/threadline/operator_surface/style.ex`.
- Adds or updates ExUnit source-contract helpers/tests in `test/threadline/operator_surface/style_contract_test.exs`.
- Does not create new CSS tokens, keyframes, animation names, package dependencies, routes, data-testids, screenshot baselines, masks, or public component APIs.
- Does not modify `examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts` unless a source-test limitation forces a narrow computed-style assertion for `.tl-copy` only.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source contract for implicit-all copy transitions</name>
  <files>test/threadline/operator_surface/style_contract_test.exs</files>
  <read_first>
    - .planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-CONTEXT.md
    - .planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-UI-SPEC.md
    - .planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-VALIDATION.md
    - .planning/v1.38-MILESTONE-AUDIT.md
    - lib/threadline/operator_surface/style.ex
    - test/threadline/operator_surface/style_contract_test.exs
    - .planning/milestones/v1.31-phases/141-motion-micro-animation/141-MOTION-INVENTORY.md
  </read_first>
  <behavior>
    - RED case: .tl-copy must fail while its transition declaration omits an explicit transition-property list.
    - Contract case: .tl-copy must require color, border-color, background-color, and box-shadow as the governed properties.
    - Contract case: source scanning must reject literal all-property transitions, token-only transition shorthand, and layout-affecting transition properties.
    - Contract case: approved keyframes remain exactly tl-drawer-in, tl-rise-in, tl-thread-draw, tl-fade-in, and tl-copy-pulse.
  </behavior>
  <action>Per D-188-13, D-188-14, and D-188-17, extend `StyleContractTest` near the existing Phase 141/MOTION-01 tests. Add a focused assertion that extracts the `.tl-copy` selector block and requires explicit transition properties `color`, `border-color`, `background-color`, and `box-shadow`, duration `var(--tl-motion-fast)`, and timing function `var(--tl-ease-standard)`. Strengthen the existing transition scan so shorthand declarations that provide only duration/easing tokens without a property name cannot pass. Keep the helper local to style_contract_test.exs unless an existing helper already fits. Avoid writing forbidden selector or transition literals into source comments because this file's source-contract tests scan comments too.</action>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/style_contract_test.exs` fails before Task 2 on the current .tl-copy transition declaration.
    - `test/threadline/operator_surface/style_contract_test.exs` contains a .tl-copy-specific assertion for the four required properties and source-wide rejection of implicit all-property transition shorthand.
    - The strengthened test still allows existing governed declarations such as transform-only overlay/control transitions and opacity/transform overlay utilities.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <done>RED source-contract coverage exists for the exact .tl-copy motion gap and does not broaden into screenshot or browser-matrix work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace .tl-copy shorthand with explicit governed properties</name>
  <files>lib/threadline/operator_surface/style.ex, test/threadline/operator_surface/style_contract_test.exs</files>
  <read_first>
    - .planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-CONTEXT.md
    - .planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-RESEARCH.md
    - .planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-UI-SPEC.md
    - lib/threadline/operator_surface/style.ex
    - test/threadline/operator_surface/style_contract_test.exs
  </read_first>
  <behavior>
    - .tl-copy remains inline-flex, compact, hoverable, focusable, and copied-state capable.
    - .tl-copy hover color/border change, copied Signal color, static Copied chip, and tl-copy-pulse animation remain.
    - .tl-copy no longer depends on an implicit all-property transition.
  </behavior>
  <action>Per D-188-12, D-188-13, and D-188-15, update only the `.tl-copy` transition declaration in `style.ex`. Set `transition-property` to `color, border-color, background-color, box-shadow`; set `transition-duration` to `var(--tl-motion-fast)`; set `transition-timing-function` to `var(--tl-ease-standard)`. Preserve min-width, min-height, padding, border, color, hover, `is-copied`, static `Copied` chip, and `tl-copy-pulse` declarations. Do not add keyframes, motion tokens, animation libraries, row/card entrance changes, or per-page motion experiments. Per D-188-16, do not add browser proof unless the source contract cannot validate the property list; if browser proof is added, limit it to one `.tl-copy` computed transitionProperty assertion in the existing operator-motion spec and do not add screenshots or config matrices.</action>
  <acceptance_criteria>
    - `lib/threadline/operator_surface/style.ex` .tl-copy block contains `transition-property: color, border-color, background-color, box-shadow;`.
    - `lib/threadline/operator_surface/style.ex` .tl-copy block contains `transition-duration: var(--tl-motion-fast);` and `transition-timing-function: var(--tl-ease-standard);`.
    - `lib/threadline/operator_surface/style.ex` still contains `.tl-copy:hover`, `.tl-copy.is-copied`, `.tl-copy.is-copied::after`, and `@keyframes tl-copy-pulse`.
    - `mix test test/threadline/operator_surface/style_contract_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <done>.tl-copy visual behavior is preserved and the motion source contract blocks implicit all-property transitions.</done>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Close .tl-copy motion validation gap | 188-02 | COVERED | CSS and source contract updated. |
| REQ | MOTION-01 | No implicit transition-all enters operator surface | 188-02 | COVERED | .tl-copy explicit property list and tests. |
| REQ | CLOSE-01 | Closeout evidence has motion proof without screenshot expansion | 188-02, 188-03 | COVERED | Source proof here; classification in 188-03. |
| RESEARCH | motion-source-contract | Prefer source hardening over new motion tooling | 188-02 | COVERED | No new dependency or broad browser matrix. |
| CONTEXT | D-188-12..D-188-17 | Copy behavior, explicit transition properties, optional browser boundary, red tests | 188-02 | COVERED | Task actions cite locked decisions. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source CSS -> browser rendering | CSS declarations determine interactive affordance behavior in all operator pages. |
| source contract -> future changes | Source tests block future ungoverned motion drift before browser evidence is needed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-188-05 | Tampering | lib/threadline/operator_surface/style.ex | mitigate | Source contract rejects unbounded transition properties and layout-affecting transition lists. |
| T-188-06 | Denial of Service | browser rendering performance | mitigate | Keep .tl-copy transitions limited to color, border-color, background-color, and box-shadow; no layout-affecting properties or new animation families. |
| T-188-07 | Spoofing | copy success feedback | accept | Existing copied Signal color and static Copied chip remain unchanged; this phase does not alter copy logic or accessible names. |
| T-188-SC | Tampering | package installs | mitigate | No npm/pip/cargo package installs are planned; if an executor attempts one, stop for a package legitimacy checkpoint. |
</threat_model>

<verification>
Run `mix test test/threadline/operator_surface/style_contract_test.exs` after each task. Do not run screenshot-update commands. Run `mix verify.example_browser -- operator-motion.spec.ts` only if the executor changes `examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts`.
</verification>

<success_criteria>
MOTION-01 is no longer partial due to .tl-copy implicit all-property transitions. The copy control remains visually and semantically stable while source contracts block future implicit-all and layout-affecting motion regressions.
</success_criteria>

<output>
Create `.planning/phases/188-close-gap-v1-38-export-queue-and-motion-validation/188-02-SUMMARY.md` when done.
</output>
