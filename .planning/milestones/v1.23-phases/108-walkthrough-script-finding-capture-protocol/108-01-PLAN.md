---
phase: 108-walkthrough-script-finding-capture-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
  - examples/threadline_phoenix/DEMO-MANIFEST.md
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex
  - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
autonomous: true
requirements:
  - WALK-04
must_haves:
  truths:
    - "post-demo.seed evidence includes redaction_policy with policy walk-demo-redaction-policy"
    - "DEMO-MANIFEST documents redaction evidence subject_ref"
  artifacts:
    - path: examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex
      provides: "Seeds all four WALK-04 evidence families"
---

<objective>
Close D-108-04e: seed `redaction_policy` evidence row in examples-only code so WALKTHROUGH §5 can document a real post-`mix demo.seed` row. Prerequisite for Plan 05.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/108-walkthrough-script-finding-capture-protocol/108-CONTEXT.md
@.planning/phases/108-walkthrough-script-finding-capture-protocol/108-RESEARCH.md
</context>

<scope_guard>
**ALLOWED:** `examples/threadline_phoenix/**` only  
**FORBIDDEN:** `lib/threadline/**`
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-108-01 | Walkthrough documents fake evidence | high | Contract test asserts row exists after seed |
| T-108-02 | Plaintext internal note in docs | medium | Walkthrough uses `[REDACTED]` only; never `WALKTHROUGH-INTERNAL-SECRET-4521` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Manifest + DEMO-MANIFEST redaction evidence ref</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex, examples/threadline_phoenix/DEMO-MANIFEST.md</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex (existing evidence_subject_ref clauses)
    - examples/threadline_phoenix/DEMO-MANIFEST.md (Correlation and evidence section)
  </read_first>
  <action>
Add module attribute and public accessor:

- `@evidence_redaction_policy_ref %{"policy" => "walk-demo-redaction-policy"}`
- `def evidence_subject_ref(:redaction_policy), do: @evidence_redaction_policy_ref`

Add table row to `DEMO-MANIFEST.md` under evidence:

| Redaction policy snapshot | `walk-demo-redaction-policy` | WALK-04 exercise 2; `claim_assessment.status` expected `inferred_posture` |
  </action>
  <verify>
    <automated>grep -q 'walk-demo-redaction-policy' examples/threadline_phoenix/DEMO-MANIFEST.md && grep -q 'def evidence_subject_ref(:redaction_policy)' examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.ex` contains `@evidence_redaction_policy_ref %{"policy" => "walk-demo-redaction-policy"}`
    - `DEMO-MANIFEST.md` contains string `walk-demo-redaction-policy`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Seed redaction_policy evidence + contract test</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex, examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex (`record_evidence!/2`)
    - lib/threadline/evidence.ex (`record_redaction_policy/3`)
    - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (evidence describe pattern)
  </read_first>
  <action>
In `record_evidence!/2`, after `record_retention_policy`, add:

```elixir
{:ok, _} =
  Evidence.record_redaction_policy(
    Manifest.evidence_subject_ref(:redaction_policy),
    %{
      summary_status: "active",
      detail: %{
        "masked_fields" => ["ticket_replies.internal_note_body"],
        "source" => "trigger_capture"
      }
    },
    repo: Repo
  )
```

Add test in `demo_contract_test.exs` (new describe or extend evidence describe):

- After `Seed.run()` path used by other tests, assert repo has evidence record with `subject == "redaction_policy"` and `subject_ref` matching `%{"policy" => "walk-demo-redaction-policy"}`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `retention_tail.ex` calls `Evidence.record_redaction_policy`
    - `mix test test/threadline_phoenix/demo_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd examples/threadline_phoenix && mix compile --warnings-as-errors`
- [ ] Demo contract suite green
</verification>
