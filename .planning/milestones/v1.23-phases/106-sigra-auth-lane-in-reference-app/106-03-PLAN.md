---
phase: 106-sigra-auth-lane-in-reference-app
plan: 03
type: execute
wave: 3
depends_on:
  - 106-02
files_modified:
  - examples/threadline_phoenix/test/support/conn_case.ex
  - examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_http_test.exs
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/help_desk_dev_controller.ex
autonomous: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - "operator_surface_test uses login_via_sigra, not assign(:current_user"
    - "help_desk_audit_http_test proves actor_ref from real session"
    - "All 23+ pre-existing example tests pass"
    - "mix verify.example passes from repo root"
  artifacts:
    - path: examples/threadline_phoenix/test/support/conn_case.ex
      provides: "login_via_sigra/2"
    - path: examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_http_test.exs
      provides: "HTTP capture proof"
  key_links:
    - from: help_desk_audit_http_test.exs
      to: Threadline.Integrations.Sigra
      via: POST through browser pipeline with Threadline.Plug or explicit actor from conn
---

<objective>
Ship AUTH-04: Replace faked browser auth assigns with `login_via_sigra/2`, add HTTP help-desk audit capture test, keep API `sigra_conn/2` tests and DataCase audit tests unchanged. Full regression green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-sigra-auth-lane-in-reference-app/106-CONTEXT.md
@.planning/phases/106-sigra-auth-lane-in-reference-app/106-RESEARCH.md
@examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
@examples/threadline_phoenix/test/support/conn_case.ex
@examples/threadline_phoenix/test/support/help_desk_fixtures.ex
</context>

<scope_guard>
**ALLOWED:** test/support, test files, minimal dev-only HTTP route for capture proof

**FORBIDDEN:** Changing `posts_*_test.exs` sigra_conn usage, changing `help_desk_audit_test.exs` DataCase tests, production fake-admin plug
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-106-07 | Tests bypass real auth with leftover assign(:current_user) | medium | grep gate: zero matches in test/ |
| T-106-08 | Dev capture route exposed in prod | high | Route only when `Application.get_env(:threadline_phoenix, :dev_routes)` or `Mix.env() == :test` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: login_via_sigra/2 in ConnCase</name>
  <files>examples/threadline_phoenix/test/support/conn_case.ex</files>
  <read_first>
    - examples/threadline_phoenix/test/support/conn_case.ex
    - examples/threadline_phoenix/test/support/conn_case_helpers.ex (after Sigra install)
    - examples/threadline_phoenix/test/support/fixtures/auth_fixtures.ex
    - .planning/phases/106-sigra-auth-lane-in-reference-app/106-CONTEXT.md (D-106-04a)
  </read_first>
  <action>
Add to `ConnCase`:

```elixir
def login_via_sigra(conn, user, opts \\ []) do
  password = Keyword.get(opts, :password, "password123456")
  mode = Keyword.get(opts, :mode, :http)

  case mode do
    :http ->
      conn
      |> Phoenix.ConnTest.init_test_session(%{})
      |> post(~p"/users/log_in", %{
        "user" => %{"email" => user.email, "password" => password}
      })

    :session ->
      import ThreadlinePhoenixWeb.ConnCaseHelpers, only: [log_in_user: 2]
      log_in_user(conn, user)
  end
end
```

Ensure `auth_fixtures` `user_fixture/1` sets `confirmed_at` so login works without mailbox in tests.

Document in moduledoc: default `:http` exercises full plug chain; `:session` for speed if flaky.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix compile && rg 'def login_via_sigra' examples/threadline_phoenix/test/support/conn_case.ex</automated>
  </verify>
  <acceptance_criteria>
    - `conn_case.ex` contains `def login_via_sigra(`
    - Default mode is `:http` (grep `Keyword.get(opts, :mode, :http)`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate operator_surface_test.exs</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs, examples/threadline_phoenix/test/support/help_desk_fixtures.ex</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
    - examples/threadline_phoenix/test/support/help_desk_fixtures.ex
    - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex (scope_operator_query uses UUID org_id)
  </read_first>
  <action>
Replace every `assign(:current_user, %{...})` with real auth flow:

**Admin scenario:**
1. `user = user_fixture(email: "admin@example.com")` (must be in `admin_emails` test config)
2. `org = organization_fixture()` + `membership_fixture(org, user_id: to_string(user.id), role: "agent")` (admin doesn't need support role)
3. `conn = login_via_sigra(conn, user)`
4. Use `to_string(org.id)` for post meta / `create_post_for_org` — NOT slug strings like `"support-org-1"`

**Support scenario:**
1. `user = user_fixture(email: "support@example.com")`
2. `org = organization_fixture()` + `membership_fixture(org, user_id: to_string(user.id), role: "support")`
3. `conn = login_via_sigra(conn, user)`
4. Assert `/audit` returns 200 and scoped results match org UUID

**Agent 403 scenario (optional assertion):**
- Agent membership only, non-admin email → GET `/audit` returns 403

Remove all `assign(:current_user` lines.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix_web/operator_surface_test.exs --trace && ! rg 'assign\(:current_user' test/threadline_phoenix_web/operator_surface_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `operator_surface_test.exs` has zero `assign(:current_user` matches
    - `mix test test/threadline_phoenix_web/operator_surface_test.exs` exits 0
    - Tests use `login_via_sigra`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: help_desk_audit_http_test + minimal capture route</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_http_test.exs, examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/help_desk_dev_controller.ex, examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs (DataCase pattern)
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
    - lib/threadline/integrations/sigra.ex
  </read_first>
  <action>
Add test-only or dev-only route (D-106-04e):

```elixir
# router.ex — inside scope when dev_routes or only in test via compile_env
scope "/dev", ThreadlinePhoenixWeb do
  pipe_through [:browser, :operator_browser]  # needs current_user
  post "/help_desk/ticket_reply", HelpDeskDevController, :ticket_reply
end
```

`HelpDeskDevController.ticket_reply/2`:
- Build `%AuditContext{}` using `Threadline.Integrations.Sigra.actor_ref_from_conn(conn)` OR `ActorRef` from `conn.assigns.current_user.id`
- Call `HelpDesk.ticket_replied_and_closed/6` with fixture org/ticket

Create `help_desk_audit_http_test.exs`:
- `use ThreadlinePhoenixWeb.ConnCase, async: false`
- Register user via fixture + provision + `login_via_sigra`
- POST to dev route
- Assert `audit_transactions` row has `actor_ref` matching `{:user, to_string(user.id)}`

Keep `help_desk_audit_test.exs` unchanged (DataCase).
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/help_desk_audit_http_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `help_desk_audit_http_test.exs` exists
    - Test contains `login_via_sigra`
    - Test asserts actor_ref for logged-in user
    - `mix test test/threadline_phoenix/help_desk_audit_http_test.exs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Full regression + verify.example</name>
  <files>[]
  <read_first>
    - .planning/phases/106-sigra-auth-lane-in-reference-app/106-VALIDATION.md
  </read_first>
  <action>
Run full example-app suite and repo verification gate:

```bash
cd examples/threadline_phoenix && mix test
cd ../.. && mix verify.example
```

Confirm API tests still use `sigra_conn/2` (unchanged):
- `posts_audit_path_test.exs`
- `posts_correlation_path_test.exs`
- `posts_incident_json_path_test.exs`

Grep entire test tree for faked assigns:

```bash
rg 'assign\(:current_user' examples/threadline_phoenix/test/
```

Expected: no matches (or only commented documentation).
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test && cd ../.. && mix verify.example && ! rg 'assign\(:current_user' examples/threadline_phoenix/test/</automated>
  </verify>
  <acceptance_criteria>
    - `mix test` in example app exits 0
    - `mix verify.example` from repo root exits 0
    - No `assign(:current_user` in test directory
    - Test count >= 23 (baseline + new tests)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Phase gate: `cd examples/threadline_phoenix && mix test && cd ../.. && mix verify.example && ! rg 'assign\(:current_user' examples/threadline_phoenix/test/`
</verification>
