---
phase: 109-maintainer-walkthrough-dry-run
plan: 04
type: execute
wave: 4
depends_on:
  - "109-02"
files_modified:
  - .planning/v1.23/findings/*.md
  - .planning/phases/109-maintainer-walkthrough-dry-run/109-WALK-CHECKPOINT.json
  - .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md
autonomous: false
requirements:
  - RUN-03
  - FINDINGS-02
must_haves:
  truths:
    - "Three WALK-04 evidence exercises attempted with canonical mix threadline.evidence.show flags"
    - "FINDINGS-02 reconciliation: every If different in walked sections has a finding"
    - "No finding has empty or TBD classification"
  artifacts:
    - path: .planning/v1.23/findings/
      provides: "§5 gaps plus any §1–§4 reconciliation findings"
---

<objective>
Execute RUN-03: walk WALKTHROUGH §5 three evidence exercises. Reconcile FINDINGS-02 against all walked sections. Update walk status in execution log.
</objective>

<context>
@examples/threadline_phoenix/WALKTHROUGH.md (§5, Appendix B)
@.planning/v1.23/findings/README.md
@.planning/phases/109-maintainer-walkthrough-dry-run/109-CONTEXT.md (D-109-04e, D-109-05c, D-109-07a)
</context>

<scope_guard>
**ALLOWED:** findings; checkpoint/log in clone  
**FORBIDDEN:** running `mix threadline.retention.purge` for proof; fixing CLI docs in §5
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-109-10 | Wrong evidence CLI flags | medium | Use only: `--subject` + `--subject-ref-json '{"run_id":"..."}'` |
| T-109-11 | §5 skipped when §4 partial | low | Attempt §5 if demo.seed succeeded per D-109-04e |
</threat_model>

<tasks>

<task type="manual">
  <name>Task 1: Walk §5 evidence exercises WALK-04-01..03</name>
  <files>.planning/v1.23/findings/</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (§5 full)
    - examples/threadline_phoenix/WALKTHROUGH.md (Appendix B)
  </read_first>
  <action>
**Skip if §1 failed or §5 marked NOT ATTEMPTED.**

Prerequisite: `mix demo.seed` state from §1.

| Step | Subject | CLI |
|------|---------|-----|
| WALK-04-01 | retention_run | `mix threadline.evidence.show --subject retention_run --subject-ref-json '{"run_id":"walk-retention-offboarded-co"}'` |
| WALK-04-02 | redaction_policy | `mix threadline.evidence.show --subject redaction_policy --subject-ref-json '{"policy":"walk-demo-redaction-policy"}'` |
| WALK-04-03 | trigger_coverage | `mix threadline.evidence.show --subject trigger_coverage --subject-ref-json '{"snapshot":"walk-demo-trigger-coverage"}'` |

For each step verify LiveView paths documented in §5 and record `summary_status` / `claim_assessment.status` in finding Evidence when mismatched.

Expected highlights:
- 04-01: `summary_status` completed; org Y timeline empty
- 04-02: `claim_assessment.status` = `inferred_posture`; corroborate #4521 `[REDACTED]`
- 04-03: snapshot `walk-demo-trigger-coverage`; `proven`

MVP finding at each "If different".
  </action>
  <acceptance_criteria>
    - All three WALK-04 step IDs attempted or NOT ATTEMPTED documented in execution log
    - CLI commands use `--subject` and `--subject-ref-json` form (not positional args)
    - §5 checkpoint table reconciled
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: FINDINGS-02 reconciliation audit</name>
  <files>.planning/v1.23/findings/, .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (grep all "If different" lines in walked sections)
    - .planning/v1.23/findings/README.md (4-question tree)
    - .planning/phases/109-maintainer-walkthrough-dry-run/109-CONTEXT.md (D-109-05c)
  </read_first>
  <action>
1. List every `If different` step ID in sections actually walked (§1–§5 subset).

2. For each trigger, confirm a finding exists OR document "expected met — no file" in execution log reconciliation table:

| walkthrough_step | Finding file | classification |
|------------------|--------------|----------------|

3. Audit all `0*.md` in clone findings dir:
   - `classification` in {a,b,c,d} — never `TBD`
   - `walkthrough_step` non-empty
   - `captured` ISO8601 set

4. Update `109-WALK-CHECKPOINT.json`: `"sections_completed"` includes all walked sections; `"findings_filed"` lists all slugs.

5. Update execution log walk status line:
   `RUN-01 ☐/✓ RUN-02 ☐/✓ RUN-03 ☐/✓ | Findings: N (a:X b:Y c:Z d:W) | Not attempted: [...]`
  </action>
  <verify>
    <automated>for f in .planning/v1.23/findings/0*.md; do grep -q '^classification:' "$f" && grep -vq 'TBD' "$f" || exit 1; done 2>/dev/null; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Reconciliation table in execution log covers every walked "If different" step
    - `grep -r 'classification: TBD' .planning/v1.23/findings/` returns empty
    - Finding count by class documented in execution log
    - `109-WALK-CHECKPOINT.json` `findings_filed` array matches filesystem slugs
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] RUN-03 attempted or NOT ATTEMPTED with gate reason
- [ ] FINDINGS-02 auditable via reconciliation table
</verification>
