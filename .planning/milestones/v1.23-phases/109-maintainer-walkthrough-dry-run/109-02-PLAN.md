---
phase: 109-maintainer-walkthrough-dry-run
plan: 02
type: execute
wave: 2
depends_on:
  - "109-01"
files_modified:
  - .planning/v1.23/findings/*.md
  - .planning/phases/109-maintainer-walkthrough-dry-run/109-WALK-CHECKPOINT.json
  - .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md
autonomous: false
requirements:
  - RUN-01
  - FINDINGS-02
must_haves:
  truths:
    - "§1 hard gate passed OR §2–§5 marked NOT ATTEMPTED with blocker cite"
    - "§2 onboarding and §3 daily-use steps walked with MVP findings at each If different"
    - "mix phx.server boots and http://localhost:4000 renders help-desk landing if §1 passed"
  artifacts:
    - path: .planning/v1.23/findings/
      provides: "Numbered findings from §1–§3 triggers"
---

<objective>
Execute RUN-01: walk WALKTHROUGH §1 (clean clone install), §2 (onboarding), and §3 (daily use) verbatim from the isolated clone. Apply hard STOP if §1 fails. File MVP finding at every "If different" before advancing.
</objective>

<context>
@examples/threadline_phoenix/WALKTHROUGH.md (§1–§3)
@.planning/v1.23/findings/README.md
@.planning/v1.23/findings/TEMPLATE.md
@.planning/phases/109-maintainer-walkthrough-dry-run/109-CONTEXT.md (D-109-04a,b, D-109-05a)
</context>

<scope_guard>
**ALLOWED:** `.planning/v1.23/findings/NNNN-*.md` in clone; checkpoint JSON; execution log updates  
**FORBIDDEN:** fixes to WALKTHROUGH, README, lib, seeds, tests; opening DEMO-MANIFEST mid-run (use Appendix A)
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-109-04 | §1 fail but continuing to §4 | high | STOP session; mark §2–§5 NOT ATTEMPTED with `blocked_by: WALK-01-XX` |
| T-109-05 | Batch findings at end of §3 | medium | File at each "If different" trigger with route/ticket literals |
| T-109-06 | Out-of-band docs during RUN-01 | medium | Only WALKTHROUGH.md + Appendix A; no guides/README |
</threat_model>

<tasks>

<task type="manual">
  <name>Task 1: Walk §1 Clean clone install (WALK-01-01..03)</name>
  <files>.planning/v1.23/findings/, examples/threadline_phoenix/WALKTHROUGH.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (§1 steps WALK-01-01 through WALK-01-03)
    - .planning/v1.23/findings/TEMPLATE.md
    - .planning/phases/109-maintainer-walkthrough-dry-run/109-CONTEXT.md (D-109-04a)
  </read_first>
  <action>
From clone `examples/threadline_phoenix/`:

1. **WALK-01-01:** `mix deps.get` && `mix compile` — expect exit 0
2. **WALK-01-02:** `pg_isready -h "${DB_HOST:-localhost}" -p "${DB_PORT:-5432}"` then `mix ecto.create` && `mix ecto.migrate`
3. **WALK-01-03:** `mix demo.seed` (if drift: one `mix demo.reset` then retry)
4. **WALK-01-03b:** `mix phx.server` — verify `http://localhost:4000` help-desk landing renders

At each **If different:** copy TEMPLATE → next `NNNN-slug.md` in clone `.planning/v1.23/findings/` with:
- `classification` set (a/b/c/d) at creation
- `walkthrough_step: WALK-01-NN`
- Actual + Evidence paragraphs with stderr/HTTP status

**If any §1 step hard-fails:** file (a) finding, update execution log:
`§2–§5 NOT ATTEMPTED — blocked_by: WALK-01-XX`, **STOP** — do not run tasks 2–3.

**If §1 passes:** update checkpoint JSON: `"sections_completed":["§1"]`, `"last_completed_step":"WALK-01-03"`, reconcile §1 checkpoint table in WALKTHROUGH.
  </action>
  <verify>
    <automated>grep -q '§1' .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - Execution log states §1 PASS or §1 FAIL with `blocked_by` step ID
    - If PASS: maintainer recorded `mix phx.server` and localhost:4000 landing OK
    - Every §1 "If different" encountered has a matching `0*.md` with `walkthrough_step` matching WALK-01-*
    - If FAIL: plans 109-03 and 109-04 marked NOT ATTEMPTED in execution log
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Walk §2 Onboarding (WALK-01-04..07)</name>
  <files>.planning/v1.23/findings/</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (§2)
    - examples/threadline_phoenix/WALKTHROUGH.md (Appendix A — credentials)
  </read_first>
  <action>
**Skip if §1 hard gate failed.**

Walk §2 verbatim: register org/user flows, login, first ticket reply per WALKTHROUGH step IDs.

Use Appendix A literals only (e.g. `support@acme.example.com` / `password123456`) — do not open DEMO-MANIFEST.

MVP finding at each "If different" before next step.

Update checkpoint: add `"§2"` to `sections_completed`, set `last_completed_step` to final §2 step ID, tick §2 checkpoint table (Expected met? / Findings filed?).
  </action>
  <acceptance_criteria>
    - All §2 steps attempted or documented skip with blocker
    - Checkpoint JSON includes `"§2"` in `sections_completed` when attempted
    - No classification left as TBD in any new finding files
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 3: Walk §3 Daily use (WALK-02-*)</name>
  <files>.planning/v1.23/findings/</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (§3)
    - .planning/phases/109-maintainer-walkthrough-dry-run/109-CONTEXT.md (D-109-04b)
  </read_first>
  <action>
**Skip if §1 hard gate failed.**

Walk §3 agent/admin/support flows via `/audit` and help-desk UI as documented.

Soft gaps → finding + continue.

**Hard gate:** if `/audit` returns 403/500 for all personas everywhere → file (a), STOP before §4 (note in execution log).

Otherwise complete §3, update checkpoint with `"§3"`, reconcile §3 checkpoint table.

Set execution log `RUN-01` checkbox to ✓ only if §1 passed AND §2–§3 attempted per above.
  </action>
  <acceptance_criteria>
    - §3 checkpoint table reconciled in execution log or WALKTHROUGH tick marks copied to log
    - If total /audit failure: execution log says STOP before §4 with finding cite
    - `109-WALK-CHECKPOINT.json` lists `checkpoint_tables_verified` including `"§1"`,`"§2"`,`"§3"` when walked
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] RUN-01 satisfied OR honestly partial with §1 gate documented
- [ ] Finding numbering sequential from 0001 (may include pre-planned 0001/0002 only after §4 — do not consume IDs early unless triggered in §1–§3)
</verification>
