---
phase: 107-realistic-seed-data-demo-mix-tasks
plan: 03
type: execute
wave: 3
depends_on:
  - 107-02
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex
  - examples/threadline_phoenix/lib/mix/tasks/demo.seed.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/temporal.ex
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
  - SEED-05
must_haves:
  truths:
    - "mix demo.seed creates 3 orgs, ~5 agents each, ~50 tickets per org with 14-day spread"
    - "Anchor: #4521 close with masked internal note and correlation walk-acme-4521-close"
    - "Anchor: #4518 reply deleted by distinct deleter actor"
    - "Filler uses per-ticket transactions without record_action"
    - "Temporal backfill sets occurred_at/captured_at from manifest offsets"
  artifacts:
    - path: examples/threadline_phoenix/lib/mix/tasks/demo.seed.ex
      provides: "SEED-01 entrypoint"
    - path: examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex
      provides: "Orchestrator"
---

<objective>
Implement `mix demo.seed` with hybrid activity synthesis (D-107-04): real Sigra users, anchor incidents, PRNG filler, audit timestamp backfill. Wire `Demo.Reset` to call completed `Demo.Seed.run/0`.

Scope: `examples/threadline_phoenix/` only. Retention/evidence tail deferred to Plan 04.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md
@.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-RESEARCH.md
@examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
@examples/threadline_phoenix/test/support/fixtures/auth_fixtures.ex
</context>

<scope_guard>
**ALLOWED:** `examples/threadline_phoenix/**`

**FORBIDDEN:** `lib/threadline/**`, `Retention.purge` (Plan 04), evidence recording (Plan 04)
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-107-01 | demo.seed in production | high | Same prod guard as reset; `Mix.raise` unless `DEMO_ALLOW_RESET=1` |
| T-107-06 | Seeding skips email confirm | medium | Always run `User.confirm_changeset` after register (auth_fixtures pattern) |
| T-107-07 | Plaintext internal notes in DB logs | medium | Use `internal_note_body` on close path; assert masking in contract test (Plan 04) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Mix task + seed orchestrator</name>
  <files>examples/threadline_phoenix/lib/mix/tasks/demo.seed.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/reset.ex
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-07)
  </read_first>
  <action>
`Mix.Tasks.Demo.Seed` delegates to `Demo.Seed.run/0`.

`Demo.Seed.run/0` sequence:
1. `assert_dev_or_allowed!/0`
2. `:rand.seed(:exsss, {1, 2, 3})`
3. `Demo.Seed.Personas.run/0` — orgs + users (see Task 2)
4. `Demo.Seed.Anchors.run/0` — hero incidents (Task 3)
5. `Demo.Seed.Filler.run/0` — volume tickets (Task 4)
6. `Demo.Seed.Temporal.run/0` — backfill timestamps (Task 5)
7. `Mix.shell().info("demo.seed complete")`

Replace Plan 02 stub in `Demo.Reset` to call this orchestrator.

Prod guard on seed task too.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix demo.seed 2>&1 | grep -q "complete"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/demo.seed.ex` exists
    - `mix demo.seed` exits 0 on fresh migrated DB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Personas — Sigra users and orgs</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex</files>
  <read_first>
    - examples/threadline_phoenix/test/support/fixtures/auth_fixtures.ex
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
  </read_first>
  <action>
`Demo.Seed.Personas.run/0`:

For each manifest org (`:acme`, `:globex`, `:offboarded_co`):
- Upsert `Organization` by fixed `id` from `Manifest.org_id/1` and slug from manifest
- Register users via `Accounts.register_user(%{email: ..., password: Manifest.demo_seed_password()})` then confirm
- Store mapping email → `user_id` string in process dictionary or return struct for downstream

For Acme:
- `closer@acme.example.com` — agent, will close #4521
- `deleter@acme.example.com` — agent, will delete reply on #4518
- Additional agents/support to reach ~5 per org

`admin@example.com` — register if missing; ensure `OperatorUser` config lists this email in `config/dev.exs` `config :threadline_phoenix, :admin_emails, ["admin@example.com"]`

First org per user: `HelpDesk.provision_default_workspace_for_user(user_id, slug: ..., name: ...)` when creating primary membership.

Additional org memberships: explicit `OrgMembership` + `Agent` inserts with manifest `user_id`.

Use fixed organization UUIDs from manifest (upsert with `Repo.insert(..., on_conflict: ...)` or get + update).
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix run -e 'ThreadlinePhoenix.Demo.Seed.Personas.run(); o=ThreadlinePhoenix.Repo.get!(ThreadlinePhoenix.HelpDesk.Organization, ThreadlinePhoenix.Demo.Manifest.org_id(:acme)); IO.inspect(o.slug)' | grep acme</automated>
  </verify>
  <acceptance_criteria>
    - After personas run, `Repo.get_by(Organization, slug: "acme")` is non-nil
    - `Repo.get_by(User, email: "closer@acme.example.com")` is non-nil (Sigra user schema)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Anchor incidents (#4521 close, #4518 delete)</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
    - examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-04b, D-107-05)
  </read_first>
  <action>
`Demo.Seed.Anchors.run/0`:

**Acme #4521:**
- Upsert ticket `number: 4521`, status open initially
- Build `%AuditContext{actor_ref: ActorRef.new!(:user, closer_id), correlation_id: "walk-acme-4521-close", request_id: "demo-seed-4521"}`
- `HelpDesk.ticket_replied_and_closed(ctx, acme_org, ticket, %{body: "...", internal_note_body: "WALKTHROUGH-INTERNAL-SECRET-4521"}, %{status: "closed", closed_at: Manifest.epoch()}, [])`
- Record `transaction_id` in manifest offsets map for temporal pass

**Acme #4518 delete:**
- Create ticket `number: 4518`, reply via close or insert reply row
- `HelpDesk.delete_reply(deleter_ctx, acme_org, reply)` with deleter `user_id` ≠ closer
- Record delete transaction id for temporal pass

**Leaving-agent window:** 8–15 additional transactions same `ActorRef` `{:user, leaving_agent_id}` on Acme (simple ticket updates with GUC + meta)

**WALK-02 samples:** one `ticket_replied_and_closed` in non-Acme org (Globex)

Store incident → `{occurred_at_offset_days, transaction_ids}` in module attribute map consumed by Temporal.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix run -e 'alias T.{Demo,Repo,HelpDesk}; import Ecto.Query; Demo.Seed.run(); t=Repo.get_by!(HelpDesk.Ticket, number: 4521); IO.puts(t.status)' | grep closed</automated>
  </verify>
  <acceptance_criteria>
    - Ticket 4521 exists with `status == "closed"`
    - Audit action named `ticket_replied_and_closed` linked to #4521 transaction
    - Delete audit row exists for `ticket_replies` on ticket 4518
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Filler tickets (~50 per org)</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex</files>
  <read_first>
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-04c)
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk/ticket.ex
  </read_first>
  <action>
`Demo.Seed.Filler.run/0`:

Per org (including offboarded-co — audit rows needed before Plan 04 purge):
- Target ~50 tickets per org (hero numbers already exist — skip or upsert)
- For each filler ticket: **separate** `Repo.transaction`:
  - Pick random agent `user_id` from org (PRNG)
  - `set_config` actor GUC
  - `Repo.insert` ticket with `number` from sequence, `status` weighted open/closed
  - Optional light `Repo.update` on ticket
  - `Repo.update_all` audit_transactions for `txid_current()` with org meta
  - **Do not** call `Threadline.record_action/2`

Assign filler `captured_at` offsets using PRNG within 14-day window (store pairs for Temporal).

Total anchor txs ~15–25; filler ~125 txs across 3 orgs.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix run -e 'import Ecto.Query; alias T.{Demo,Repo,HelpDesk}; Demo.Seed.run(); c=Repo.aggregate(from(t in HelpDesk.Ticket, join: o in HelpDesk.Organization, on: o.id==t.organization_id, where: o.slug=="acme"), :count); IO.puts(c)' | awk '$1>=45'</automated>
  </verify>
  <acceptance_criteria>
    - Acme has at least 45 tickets after seed
    - Count distinct `audit_transactions.txid` for filler > 40 (not one mega-tx)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Temporal backfill</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/temporal.ex</files>
  <read_first>
    - examples/threadline_phoenix/priv/repo/migrations/*threadline_triggers*
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-04d)
  </read_first>
  <action>
`Demo.Seed.Temporal.run/0`:

Build map `transaction_id => occurred_at` from manifest offsets relative to `Manifest.epoch()`:
- `#4521` close → `Manifest.last_tuesday()` (or manifest offset 0 for hero)
- `#4518` delete → same Tuesday + 2 hours
- Filler → spread `DateTime.add(epoch, -days, :day)` with PRNG jitter table

Execute:
```elixir
Repo.update_all(from(at in AuditTransaction, where: at.id in ^ids), set: [occurred_at: ^ts])
Repo.update_all(from(ac in AuditChange, where: ac.transaction_id in ^ids), set: [captured_at: ^ts])
```

Use per-transaction timestamps where incidents differ.

Never call `DateTime.utc_now/0`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix run -e 'import Ecto.Query; alias T.{Demo,Repo,Manifest}; alias Threadline.Capture.AuditTransaction; Demo.Seed.run(); at=Repo.one!(from a in AuditTransaction, join: ac in assoc(a,:changes), where: ac.table_name=="tickets", limit: 1); days=DateTime.diff(Manifest.epoch(), at.occurred_at, :day); IO.puts(days>=7)' | grep true</automated>
  </verify>
  <acceptance_criteria>
    - At least one audit transaction `occurred_at` is 7+ days before `Manifest.epoch()`
    - Hero close transaction `occurred_at` equals `Manifest.last_tuesday()` (second precision)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `mix demo.seed` completes on empty DB after migrate
- [ ] `mix demo.reset` runs seed end-to-end
- [ ] No `lib/threadline` edits
</verification>

<success_criteria>
Deterministic demo fiction with walkthrough-critical anchors and realistic volume; timelines operator-filterable to "last Tuesday".
</success_criteria>

<output>
After completion, create `.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-03-SUMMARY.md`
</output>
