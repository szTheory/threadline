---
phase: 107-realistic-seed-data-demo-mix-tasks
plan: 02
type: execute
wave: 2
depends_on:
  - 107-01
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/tables.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/reset.ex
  - examples/threadline_phoenix/lib/mix/tasks/demo.reset.ex
  - examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs
autonomous: true
requirements:
  - SEED-04
  - SEED-05
must_haves:
  truths:
    - "HelpDesk.delete_reply/3 performs hard delete with actor GUC and org meta"
    - "Demo.Tables.truncate_sql/0 shared with help_desk_audit_test"
    - "mix demo.reset truncates demo tables and is blocked in prod without DEMO_ALLOW_RESET=1"
  artifacts:
    - path: examples/threadline_phoenix/lib/mix/tasks/demo.reset.ex
      provides: "Canonical walkthrough recovery command"
    - path: examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
      provides: "delete_reply/3 for SEED-05"
---

<objective>
Ship `HelpDesk.delete_reply/3`, shared `@demo_tables` truncate SQL, and `mix demo.reset` (truncate + invoke seed in Plan 03 — for now call `Demo.Seed.run/0` stub or leave TODO that Plan 03 implements).

Scope: `examples/threadline_phoenix/` only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md
@.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-RESEARCH.md
@examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs
</context>

<scope_guard>
**ALLOWED:** `examples/threadline_phoenix/**`

**FORBIDDEN:** `lib/threadline/**`, full `Demo.Seed` implementation (Plan 03)
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-107-03 | `demo.reset` destroys production data | high | `Mix.raise` when `Mix.env() == :prod` unless `DEMO_ALLOW_RESET=1` |
| T-107-04 | Truncate omits table → stale walkthrough data | medium | Centralize table list in `Demo.Tables`; audit test imports it |
| T-107-05 | Delete without actor GUC | high | `delete_reply/3` requires `%AuditContext{actor_ref: %ActorRef{}}` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: HelpDesk.delete_reply/3</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
    - examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs (lines 79-124)
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-05)
  </read_first>
  <action>
Add `@spec delete_reply(AuditContext.t(), Organization.t(), TicketReply.t()) :: {:ok, :deleted} | {:error, term()}`.

Implementation (mirror audit test):
- Return `{:error, :missing_actor}` when `actor_ref` is nil
- `Repo.transaction`: `set_config('threadline.actor_ref', Jason.encode!(ActorRef.to_map(actor_ref)), true)`
- `Repo.delete!(reply)`
- `Repo.update_all` on `AuditTransaction` where `txid = txid_current()`, set `meta: %{"organization_id" => to_string(org.id)}`
- Return `{:ok, :deleted}`

Do NOT call `Threadline.record_action` for `:ticket_reply_deleted` (D-107-05d).

Refactor `help_desk_audit_test.exs` "hard delete" test to call `HelpDesk.delete_reply/3` instead of inline transaction.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/help_desk_audit_test.exs:79</automated>
  </verify>
  <acceptance_criteria>
    - `help_desk.ex` contains `def delete_reply(`
    - Delete test passes and asserts `ac.op == "delete"` on `ticket_replies`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Demo.Tables shared truncate list</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/tables.ex, examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs (@truncate_sql)
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-03b)
  </read_first>
  <action>
Create `ThreadlinePhoenix.Demo.Tables` with `@tables` list (order matters for FK):

`ticket_replies`, `tickets`, `agents`, `org_memberships`, `organizations`, `audit_changes`, `audit_transactions`, `audit_actions`, `posts`, `oban_jobs`, `audit_events`, `users_tokens`, `users`

Implement `truncate_sql/0` returning `TRUNCATE TABLE ... RESTART IDENTITY CASCADE` string.

Replace `@truncate_sql` in `help_desk_audit_test.exs` with `Demo.Tables.truncate_sql()`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/help_desk_audit_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `demo/tables.ex` defines `truncate_sql/0`
    - `help_desk_audit_test.exs` calls `Demo.Tables.truncate_sql()` (no duplicate hardcoded TRUNCATE)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: mix demo.reset</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/reset.ex, examples/threadline_phoenix/lib/mix/tasks/demo.reset.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/tables.ex
    - lib/mix/tasks/threadline.retention.purge.ex (prod guard pattern reference)
  </read_first>
  <action>
`Mix.Tasks.Demo.Reset`:
- `@shortdoc "Truncate demo tables and re-run demo.seed (dev/test walkthrough recovery)"`
- `run/1`: call `Demo.Reset.run/1`

`Demo.Reset.run/1`:
1. `assert_dev_or_allowed!/0` — raise in `:prod` unless `System.get_env("DEMO_ALLOW_RESET") == "1"`
2. `Repo.query!(Demo.Tables.truncate_sql())`
3. Delegate to `ThreadlinePhoenix.Demo.Seed.run/0` (create stub `Seed.run/0` that logs `:not_implemented` until Plan 03 — **Plan 03 must replace stub**)

Document in moduledoc: canonical recovery is `mix demo.reset`; `mix ecto.reset` is schema escape hatch only (D-107-03c).

Add test: in test env, `Demo.Reset.run/0` after truncate leaves zero rows in `organizations` (use `Repo.aggregate(..., :count)`).
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_reset_test.exs 2>/dev/null || mix test --failed</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/demo.reset.ex` exists
    - `MIX_ENV=prod mix demo.reset` raises without `DEMO_ALLOW_RESET=1`
    - Truncate test file exists and passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `mix test test/threadline_phoenix/help_desk_audit_test.exs` green
- [ ] `delete_reply/3` covered by audit test
</verification>

<success_criteria>
Operators can reset demo fiction via truncate; delete path is reusable for seed incident on ticket #4518.
</success_criteria>

<output>
After completion, create `.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-02-SUMMARY.md`
</output>
