---
phase: 107-realistic-seed-data-demo-mix-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
  - examples/threadline_phoenix/DEMO-MANIFEST.md
  - examples/threadline_phoenix/DEMO_USERS.md
  - examples/threadline_phoenix/config/dev.exs
  - examples/threadline_phoenix/test/threadline_phoenix/demo_manifest_test.exs
autonomous: true
requirements:
  - SEED-03
must_haves:
  truths:
    - "DEMO-MANIFEST.md lists acme, ticket 4521, ticket 4518, offboarded-co, demo_epoch, demo_last_tuesday"
    - "ThreadlinePhoenix.Demo.Manifest exposes epoch/0 and hero UUID accessors"
    - "dev config sets :demo_epoch and :demo_seed_password"
  artifacts:
    - path: examples/threadline_phoenix/DEMO-MANIFEST.md
      provides: "Walkthrough literal contract for Phase 108"
    - path: examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
      provides: "Programmatic manifest accessors"
---

<objective>
Establish the demo manifest contract (D-107-01): frozen timeline anchors, hero entity UUIDs, and documented demo credentials — before any seed writes run.

Scope: `examples/threadline_phoenix/` only. Do not modify repo `lib/`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md
@.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-RESEARCH.md
@.planning/ROADMAP.md
</context>

<scope_guard>
**ALLOWED:** `examples/threadline_phoenix/**`

**FORBIDDEN:** `lib/threadline/**`, `mix demo.seed` implementation (Plan 03), retention purge (Plan 04)
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-107-01 | Demo password treated as production secret | medium | DEMO_USERS.md banner: dev/test only; default `password123456` overridable via `DEMO_SEED_PASSWORD` |
| T-107-02 | Manifest leaks real PII | low | Use `@example.com` emails only; no real names |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ThreadlinePhoenix.Demo.Manifest module</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex</files>
  <read_first>
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-01)
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk/organization.ex
  </read_first>
  <action>
Create `ThreadlinePhoenix.Demo.Manifest` with module attributes (minimum):

- `@demo_epoch` = `~U[2026-05-27 12:00:00Z]`
- `@demo_last_tuesday` = `~U[2026-05-20 14:30:00Z]` (document derivation in moduledoc)
- Org slugs: `acme`, `offboarded-co`, third org slug `globex` (discretion)
- Fixed org UUIDs (binary_id strings) — use deterministic UUID v5 from namespace `"threadline.demo"` + slug, or fixed literals documented in DEMO-MANIFEST.md
- Ticket numbers: `@hero_close_number 4521`, `@hero_delete_number 4518`
- User emails: `closer@acme.example.com`, `deleter@acme.example.com`, `admin@example.com`, plus 3 orgs × support personas
- Correlation id: `"walk-acme-4521-close"`
- Evidence run id placeholder: `"walk-retention-offboarded-co"`

Public functions: `epoch/0`, `last_tuesday/0`, `org_id/1`, `org_slug/1`, `ticket_number/1`, `user_email/1`, `correlation_id/1`, `demo_seed_password/0` (reads `Application.get_env(:threadline_phoenix, :demo_seed_password, "password123456")`).

No `DateTime.utc_now/0` in this module.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_manifest_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/threadline_phoenix/demo/manifest.ex` defines `epoch/0` returning `@demo_epoch`
    - `manifest.ex` contains `@hero_close_number 4521` and `@hero_delete_number 4518`
    - `mix compile --warnings-as-errors` succeeds from example app root
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DEMO-MANIFEST.md and DEMO_USERS.md</name>
  <files>examples/threadline_phoenix/DEMO-MANIFEST.md, examples/threadline_phoenix/DEMO_USERS.md</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-01c, D-107-02b)
  </read_first>
  <action>
Write `DEMO-MANIFEST.md` tables mirroring module attributes: orgs, ticket numbers, user ids, correlation ids, temporal anchors (`demo_epoch`, `demo_last_tuesday`), evidence subject refs for org Y.

Write `DEMO_USERS.md`:
- Banner: **public demo credentials — MIX_ENV dev/test only**
- Table columns: email, password (`password123456` or `DEMO_SEED_PASSWORD`), org slug, role, walkthrough step reference
- Include `admin@example.com` for cross-org operator scenarios

Cross-link both files from each other.
  </action>
  <verify>
    <automated>grep -q "4521" examples/threadline_phoenix/DEMO-MANIFEST.md && grep -q "offboarded-co" examples/threadline_phoenix/DEMO-MANIFEST.md && grep -q "dev/test only" examples/threadline_phoenix/DEMO_USERS.md</automated>
  </verify>
  <acceptance_criteria>
    - `DEMO-MANIFEST.md` contains `4521`, `4518`, `acme`, `offboarded-co`
    - `DEMO_USERS.md` contains `password123456` and dev/test banner text
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Dev config keys + manifest smoke test</name>
  <files>examples/threadline_phoenix/config/dev.exs, examples/threadline_phoenix/test/threadline_phoenix/demo_manifest_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/config/dev.exs
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
  </read_first>
  <action>
In `config/dev.exs` add:

```elixir
config :threadline_phoenix,
  demo_epoch: ~U[2026-05-27 12:00:00Z],
  demo_seed_password: "password123456"
```

Add `demo_manifest_test.exs` asserting `Manifest.epoch()` matches config and `Manifest.org_id(:acme)` is stable binary UUID string.

Do NOT add `demo.seed` to `ecto.setup` aliases.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_manifest_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `config/dev.exs` contains `demo_epoch:` and `demo_seed_password:`
    - `mix test test/threadline_phoenix/demo_manifest_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `mix test test/threadline_phoenix/demo_manifest_test.exs` green
- [ ] No files under `lib/threadline/` modified
</verification>

<success_criteria>
Manifest is the single source of truth for Phase 108 literals; code and docs agree on hero entities.
</success_criteria>

<output>
After completion, create `.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-01-SUMMARY.md`
</output>
