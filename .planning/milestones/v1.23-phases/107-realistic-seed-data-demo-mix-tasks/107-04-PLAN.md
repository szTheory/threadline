---
phase: 107-realistic-seed-data-demo-mix-tasks
plan: 04
type: execute
wave: 4
depends_on:
  - 107-03
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex
  - examples/threadline_phoenix/config/dev.exs
  - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
  - examples/threadline_phoenix/README.md
autonomous: true
requirements:
  - SEED-02
  - SEED-03
  - SEED-04
  - SEED-05
must_haves:
  truths:
    - "org offboarded-co ends purged with retention evidence row"
    - "Contract test proves #4521 close, #4518 delete, redaction, idempotent reset"
    - "README documents mix demo.seed and mix demo.reset"
  artifacts:
    - path: examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
      provides: "SEED-02/03/05 verification"
    - path: examples/threadline_phoenix/README.md
      provides: "Operator install path cites demo tasks"
---

<objective>
Complete org-Y offboard tail (D-107-06), contract tests for all SEED requirements, and README documentation. Close idempotency story for `demo.reset` loop.

Scope: `examples/threadline_phoenix/` only. `lib/` read-only — call existing `Threadline.Retention` and `Threadline.Evidence` APIs only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md
@.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-RESEARCH.md
@lib/threadline/retention.ex
@lib/threadline/evidence.ex
@guides/evidence-plane.md
</context>

<scope_guard>
**ALLOWED:** `examples/threadline_phoenix/**`, `config :threadline` in example `config/dev.exs` only

**FORBIDDEN:** Editing `lib/threadline/**` modules
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-107-08 | Retention purge in test pollutes other tests | medium | Run contract test `async: false` with `Sandbox.unboxed_run` or dedicated tag `@tag :demo_contract` |
| T-107-09 | Evidence detail leaks unrelated org data | low | Fixed `detail` map keys only from manifest |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Org Y retention tail + evidence</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex, examples/threadline_phoenix/config/dev.exs</files>
  <read_first>
    - lib/threadline/retention.ex
    - lib/threadline/evidence.ex
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-06)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
  </read_first>
  <action>
Add step 6 in `Demo.Seed.run/0`: `Demo.Seed.RetentionTail.run/0` **after** Temporal.

`RetentionTail.run/0`:
1. Ensure org Y (`offboarded-co`) has audit txs with `occurred_at` older than retention cutoff (e.g. 90 days before `Manifest.epoch()` — set in `config/dev.exs` under `config :threadline, retention: [enabled: true, max_age_days: 30]` or similar for dev)
2. `Application.put_env(:threadline, :retention, enabled: true, max_age_days: 30)` inside task if needed
3. `Threadline.Retention.purge(repo: ThreadlinePhoenix.Repo)`
4. `Threadline.Evidence.record_retention_run(%{"run_id" => Manifest retention run id, "org_slug" => "offboarded-co", "organization_id" => Manifest.org_id(:offboarded_co)}, %{detail: %{"deleted_changes" => N, "narrative" => "org Y offboard"}}, repo: Repo)`
5. `record_retention_policy/3` and `record_trigger_coverage/3` with manifest-fixed subjects

Verify scoped query for org Y audit changes returns empty (negative check helper in module).

Do NOT extend `Retention` for per-org purge.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix run -e 'ThreadlinePhoenix.Demo.Seed.run(); {:ok,r}=Threadline.Evidence.list_runs(repo: ThreadlinePhoenix.Repo); IO.puts(length(r)>0)' | grep true</automated>
  </verify>
  <acceptance_criteria>
    - `seed/retention_tail.ex` exists
    - After seed, `Evidence.list_runs` includes manifest `run_id`
    - Org Y scoped audit change count is 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: demo_contract_test.exs</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
    - test/threadline/ci_topology_contract_test.exs (doc contract pattern)
  </read_first>
  <action>
Create `demo_contract_test.exs` with `@moduletag :demo_contract` and `async: false`.

In `setup`, `Ecto.Adapters.SQL.Sandbox.unboxed_run(Repo, fn -> Demo.Reset.run() end)` or truncate + seed.

Tests:
1. **SEED-03 heroes** — org `acme` exists; ticket 4521 closed; ticket 4518 exists; org `offboarded-co` exists
2. **SEED-05 delete** — audit change `op=="delete"` on `ticket_replies` for #4518; `actor_ref` user id matches deleter not closer
3. **SEED-03 redaction** — close transaction reply insert has `[REDACTED]` in encoded `data_after`, not plaintext secret
4. **SEED-03 action** — `ticket_replied_and_closed` action on #4521 tx
5. **SEED-02 idempotency** — run `Demo.Reset.run()` twice; ticket 4521 number and org slug unchanged; semantic fingerprint function comparing counts (orgs=3, tickets per acme >= 45)
6. **SEED-04** — after reset, heroes still present

Avoid asserting exact audit UUIDs between runs.

Run inside unboxed sandbox for full test module.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline_phoenix/demo_contract_test.exs` exits 0
    - Test file contains `4521` and `4518` and `ticket_replied_and_closed`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: README + doc contract</name>
  <files>examples/threadline_phoenix/README.md</files>
  <read_first>
    - examples/threadline_phoenix/README.md
    - .planning/phases/107-realistic-seed-data-demo-mix-tasks/107-CONTEXT.md (D-107-07c)
    - prompts/threadline-elixir-oss-dna.md
  </read_first>
  <action>
Add section **Demo walkthrough data** to example README:

```markdown
## Demo walkthrough data

After `mix ecto.setup`, load synthetic help-desk fiction:

    mix demo.seed

Recover a clean walkthrough state (truncate demo tables + re-seed):

    mix demo.reset

Credentials: see [DEMO_USERS.md](DEMO_USERS.md). Literals: [DEMO-MANIFEST.md](DEMO-MANIFEST.md).

`ecto.setup` does **not** run `demo.seed` automatically.
```

State `mix ecto.reset` is schema/trigger recovery only (D-107-03c).

If repo has doc contract test for example README, add assertions for `mix demo.seed` and `mix demo.reset` strings.
  </action>
  <verify>
    <automated>grep -q "mix demo.seed" examples/threadline_phoenix/README.md && grep -q "mix demo.reset" examples/threadline_phoenix/README.md && grep -q "DEMO-MANIFEST" examples/threadline_phoenix/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains `mix demo.seed`, `mix demo.reset`, links to DEMO-MANIFEST.md and DEMO_USERS.md
    - README states demo.seed is not part of ecto.setup
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `mix test test/threadline_phoenix/demo_contract_test.exs` green
- [ ] `mix test` in example app green
- [ ] All SEED-01..05 covered across plans 01–04
</verification>

<success_criteria>
Phase 108 can script against DEMO-MANIFEST literals; operators can reset and re-seed deterministically; org Y retention story is provable on evidence plane.
</success_criteria>

<output>
After completion, create `.planning/phases/107-realistic-seed-data-demo-mix-tasks/107-04-SUMMARY.md`
</output>
