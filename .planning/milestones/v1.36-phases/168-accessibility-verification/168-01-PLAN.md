---
phase: 168-accessibility-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/operator_surface/style_contract_test.exs
  - lib/threadline/operator_surface/style.ex
autonomous: true
requirements: [A11Y-01, A11Y-02]
must_haves:
  truths:
    - "Every text-bearing --tl-color-* token clears WCAG 2.1 AA 4.5:1 on bg/surface/surface-raised/surface-hover in BOTH the light and system lanes, proven by mix test"
    - "Status text (info/warning/success/danger) clears 4.5:1 when composited over its own *-bg tint, in both light and system lanes — a row the hex-only parser previously dropped"
    - "The focus 1px border-focus edge clears non-text 3:1 against surface AND surface-raised in both light and system lanes; the translucent 3px halo is composited-and-reported, never carrying the pass"
    - "Hover/active/disabled/selected interaction states are source-asserted to resolve to a perceptible per-mode delta, including the Phase-167 coverage-hover polarity flip"
    - "The original phase-143 dark contrast test and dark focus guard pass byte-stable and unchanged"
  artifacts:
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "Alpha-aware color_tokens parser + composite helper + light/system AA contrast mirror + focus-ring 3:1 + per-mode interaction-state assertions"
      contains: "rgba"
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "Light + system token lanes (edited only if a D-02 sub-threshold pair is surfaced; dual-branch identical)"
      contains: "data-tl-theme=\"light\""
  key_links:
    - from: "style_contract_test.exs color_tokens/1"
      to: "contrast_ratio/2"
      via: "composite/2 producing opaque #RRGGBB"
      pattern: "composite"
    - from: "style_contract_test.exs light/system mirror"
      to: "lib/threadline/operator_surface/style.ex light + system blocks"
      via: "File.read! + selector_block! (light) + @media split (system)"
      pattern: "data-tl-theme=\\\"(light|system)\\\""
---

<objective>
Prove the Phase 166/167 light token lane is WCAG 2.1 accessible by extending the source-first
style contract (`style_contract_test.exs` reading `style.ex` directly) with: (1) an alpha-aware,
compositing-aware token parser; (2) a light-lane AND system-lane AA contrast mirror of the existing
dark test, including the composited status-text-on-tint rows the current hex-only parser silently
drops; (3) the focus-ring non-text 3:1 contract per mode; and (4) per-mode interaction-state
assertions. This closes A11Y-01 and A11Y-02 part 1 source-side.

Purpose: Turn the accessibility guarantees into measurable, source-first contracts enforced by the
test, not by eyeball — in both dark and light modes. The deliverable is PROOF, not pixels. No new
tokens, colors, type, or layout are invented; the WCAG luminance math already in-repo is REUSED
unchanged. The single genuinely new logic is the rgba parse + per-mode composite step.

Output: An extended `style_contract_test.exs` whose new light/system mirror is green, with the dark
phase-143 test byte-stable. `style.ex` is edited ONLY if the formal math newly surfaces a
sub-threshold pair (D-02 bounded autonomy; D-03 halt for any non-lane-derivable value).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/168-accessibility-verification/168-UI-SPEC.md
@.planning/phases/168-accessibility-verification/168-PATTERNS.md
@.planning/phases/168-accessibility-verification/168-RESEARCH.md

<interfaces>
<!-- Extracted from style_contract_test.exs — the executor reuses these verbatim, no exploration needed. -->

REUSE UNCHANGED (style_contract_test.exs:1064-1091) — WCAG 2.1 math, already correct:
  defp contrast_ratio(foreground, background)         # both args are "#RRGGBB" opaque hex
  defp relative_luminance("#" <> hex)                 # pattern-matches opaque hex ONLY
  defp linear_channel(channel)                        # sRGB threshold 0.03928; coeffs 0.2126/0.7152/0.0722

PARSER CHOKEPOINT to make alpha-aware (style_contract_test.exs:1058-1062) — today HEX-ONLY:
  defp color_tokens(src)
    # regex: ~r/(--tl-color-[a-z-]+):\s*(#[0-9a-fA-F]{6});/   <-- silently drops every rgba(...)

BLOCK-EXTRACTION helpers (style_contract_test.exs:1010-1056):
  defp media_section(src, width)            # :1010 — the @media-split precedent to mirror for the system block
  defp next_media_boundary(width)           # :1018-1019
  defp selector_block!(section, selector)   # :1045 — non-nested [^}]* matcher; works for the top-level light block ONLY
  defp selector_block_pattern(selector, ..) # :1054 — stops at first } (Pitfall 2)

DARK ANALOG to mirror (style_contract_test.exs:653-686) — "phase 143 ... dark-surface contrast baseline"
DARK FOCUS GUARD to keep + extend per mode (style_contract_test.exs:688-735) — "phase 143 focus-visible and non-color status contracts stay locked"
THEME-AWARE + theme-toggle-ban guards (style_contract_test.exs:8-30) — KEEP INTACT, do not duplicate
</interfaces>

<source_anchors>
<!-- style.ex token-lane anchors (verified this session) -->
  Light root block:    style.ex:188-237  (.threadline-ui[data-tl-theme="light"] { ... })
  System branch block: style.ex:240-289  (@media (prefers-color-scheme: light) { .threadline-ui[data-tl-theme="system"] { ... } }) — opening @media at :239
  Per-mode opaque bases:  surface = #FFFFFF (light) / #141B2D (dark);  surface-raised = #EEF3FA (light) / #1B253A (dark);  bg = #F7F9FC (light) / #0B1020 (dark)
  border-focus edge:   style.ex:200 (light) / :252 (system) = #1557C0;  dark :69 = #7FA9FF
  focus-ring token:    style.ex:236 (light) / :288 (system);  dark :157
  muted-soft (disabled): style.ex:203 (light) / :255 (system) = #73819C;  dark :74 = #8F9DB5
  coverage-hover dual-branch override (Phase 167 item B): style.ex:299-315
  :focus-visible restore rule: style.ex:350-356
</source_anchors>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the token parser alpha-aware and add the per-mode composite helper</name>
  <files>test/threadline/operator_surface/style_contract_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/style_contract_test.exs — the FILE being edited; read `color_tokens/1` (:1058-1062), `contrast_ratio/2` + `relative_luminance/1` + `linear_channel/1` (:1064-1091) so you reuse the math unchanged
    - .planning/phases/168-accessibility-verification/168-RESEARCH.md — "Pattern 2: Alpha-aware compositing" + the per-mode base table (:121-128) + the composite/2 shape (:130-142) + Pitfall 3 (wrong base / opaque-text guard)
    - .planning/phases/168-accessibility-verification/168-PATTERNS.md — section C (parser chokepoint, :103-131)
    - .planning/phases/168-accessibility-verification/168-UI-SPEC.md — "The parsing gap to close" (the 3 numbered parsing rules)
  </read_first>
  <behavior>
    - Parsing `--tl-color-danger: #A33434;` returns an opaque hex string unchanged (passes straight to contrast_ratio/2).
    - Parsing `--tl-color-danger-bg: rgba(163, 52, 52, 0.10);` returns a translucent representation (e.g. an {r,g,b,a} tuple), NOT dropped.
    - `composite({163, 52, 52, 0.10}, "#FFFFFF")` returns a 7-char "#RRGGBB" computed as round(src*a + base*(1-a)) per channel, so feeding it to relative_luminance/1 pattern-matches without change.
    - `composite/2` over `#141B2D` (dark base) yields a DIFFERENT result than over `#FFFFFF` (light base) for the same rgba — the base is caller-named per mode, never assumed single.
    - An opaque hex passed to the composite path passes through unchanged (no double-composite).
  </behavior>
  <action>
    Make the parser alpha-aware per Claude's-discretion (extend `color_tokens/1` in place OR add a sibling parser — RESEARCH/UI-SPEC permit either). It MUST additionally match `rgba(\s*(\d+),\s*(\d+),\s*(\d+),\s*([\d.]+)\s*)` and (if any appear — none do today) `#RRGGBBAA`, returning `{r,g,b,a}` for translucent tokens while opaque `#RRGGBB` tokens stay as-is. Add a `composite/2` helper (shape in RESEARCH :130-142) that blends an `{r,g,b,a}` tuple over a caller-named opaque base hex using `round(src*a + base*(1 - a))` per channel and returns `"#RRGGBB"` (zero-padded, lowercase or uppercase consistent with existing hex). Do NOT re-derive `relative_luminance/1`/`contrast_ratio/2`/`linear_channel/1` — they are WCAG-2.1-correct and REUSED verbatim; the composite output simply feeds `contrast_ratio/2` exactly as a dark hex does. Per the per-mode base table: status `*-bg` tints composite over `surface`; `accent-soft`/`accent-wash` over `surface-raised`; page washes over `bg` — and the base hex differs per mode (#FFFFFF light / #141B2D dark; #EEF3FA / #1B253A; #F7F9FC / #0B1020). Guard against compositing an opaque text token (Pitfall 3) — pass opaque hex through unchanged. This is the technical heart of A11Y-01; the parser is the only genuinely new logic this phase.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs && mix format --check-formatted && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/style_contract_test.exs` exits 0 (existing tests still green — the parser change must not regress the dark test, which only feeds it opaque hex)
    - The parser source matches both `#[0-9a-fA-F]{6}` and `rgba(` (grep: `grep -v '^#' test/threadline/operator_surface/style_contract_test.exs | grep -c 'rgba(' ` returns ≥ 1)
    - A `composite`/blend helper exists that produces a `#`-prefixed 6-hex-digit string (grep for `composite` returns ≥ 1 definition)
    - `mix format --check-formatted` exits 0; `mix compile --warnings-as-errors` is clean (no warnings)
    - The phase-143 dark contrast test name still present and passing (it only consumes opaque hex, so behavior is byte-stable)
  </acceptance_criteria>
  <done>Parser accepts both `#RRGGBB` and `rgba(...)`, returns translucent tokens as tuples, and `composite/2` blends them over a caller-named per-mode opaque base into `#RRGGBB`; existing tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the light + system AA contrast mirror including composited-tint and disabled rows</name>
  <files>test/threadline/operator_surface/style_contract_test.exs, lib/threadline/operator_surface/style.ex</files>
  <read_first>
    - test/threadline/operator_surface/style_contract_test.exs — the dark mirror analog (:653-686); `selector_block!` (:1045) and `media_section` (:1010); the parser/composite added in Task 1
    - .planning/phases/168-accessibility-verification/168-UI-SPEC.md — "Token pairs the mirror MUST assert" table (every row, both modes) + the disabled-text exception (D-04)
    - .planning/phases/168-accessibility-verification/168-PATTERNS.md — section A (mirror instructions :33-72), section D (@media-split for the system block :133-163)
    - .planning/phases/168-accessibility-verification/168-RESEARCH.md — Pitfall 2 (system-block extraction), Pitfall 3 (per-mode base), Pitfall 5 (green-on-arrival is expected — do NOT re-litigate Phase 167 item A)
    - lib/threadline/operator_surface/style.ex — light root :188-237 and system branch :240-289 (read the actual token values + the `*-bg` rgba alphas)
  </read_first>
  <behavior>
    - For the light lane: every text token in the UI-SPEC table (`text`, `muted`, `muted-soft`, `info-text`, `warning-text`, `success-text`, `danger`, `accent-strong`, `accent`) clears 4.5:1 against each listed background (`bg`/`surface`/`surface-raised`/`surface-hover` as applicable).
    - The composited-tint rows assert: `info-text` over composite(`info-bg`, surface), `warning-text` over composite(`warning-bg`, surface), `success-text` over composite(`success-bg`, surface), `danger` over composite(`danger-bg`, surface), `accent` over composite(`accent-soft`, surface-raised) — all ≥ 4.5:1, in BOTH the light and system token maps.
    - The system-lane map (extracted through the `@media (prefers-color-scheme: light)` wrapper) is non-empty and asserts the identical row set, since the two branches are byte-identical today.
    - `muted-soft` is asserted at strict 4.5:1 vs `surface` and `surface-raised` in both modes (D-04 strict-by-default).
    - A failing assertion message names the offending token pair and mode.
  </behavior>
  <action>
    Add a light-lane contrast mirror test (or two — light and system can be separate tests or one parameterized over both maps; Claude's discretion on organization) structurally copied from the dark test (:653-686). Extract the light root token map with `selector_block!(src, ~s|.threadline-ui[data-tl-theme="light"]|)` (top-level, extracts cleanly). Extract the system map by first splitting on `"@media (prefers-color-scheme: light) {"` (the `media_section/2` precedent, Pitfall 2) then `selector_block!` the inner `[data-tl-theme="system"]` selector — a bare `selector_block!` through the media wrapper will mis-extract. Run the alpha-aware `color_tokens` over each block. Assert EVERY row of the UI-SPEC "Token pairs the mirror MUST assert" table at 4.5:1 in BOTH maps, INCLUDING the composited-tint rows (`*-text`/`danger`/`accent` over the per-mode-composited `*-bg`/`accent-soft` using `surface`=#FFFFFF / `surface-raised`=#EEF3FA as the light bases). For `muted-soft`, assert strict 4.5:1 vs `surface` and `surface-raised` per D-04. Hold ALL text tokens to the single 4.5:1 bar — no large-text 3:1 carve-out. Green-on-arrival is EXPECTED (Phase 167 already fixed item A danger/warning tints) — do not manufacture a fix or re-tune passing tints (Pitfall 5). IF and ONLY IF a composited pair falls below 4.5:1: apply a D-02 bounded autonomous fix — a uniform lane-root `*-bg`/`*-border` alpha strengthening (the LIGHT-REVIEW item-A pattern) edited IDENTICALLY in BOTH the `style.ex` light root (:188-237) and the system branch (:240-289) in THIS task (dual-branch discipline, same-wave source-first rule), never per-component, never a hue change. For `muted-soft` specifically: if no uniform tune reaches 4.5:1 without breaking the "disabled looks disabled" read, downgrade ONLY the disabled-token rows to "exempt — documented" with a code comment citing WCAG 1.4.3 inactive controls (D-04 bounded fallback) — no other row is exemptable. A genuinely new hue/primitive or any value NOT derivable from the 45-token lane is a D-03 HALT — flag and pause, do not invent.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs && mix format --check-formatted && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/style_contract_test.exs` exits 0
    - The test file contains a light-lane AND a system-lane contrast assertion covering every UI-SPEC token-pair-table row in both modes (grep: file contains both `data-tl-theme="light"` and `data-tl-theme="system"` selector extractions, and a `@media (prefers-color-scheme: light)` split — `grep -c 'data-tl-theme=\\"system\\"' test/threadline/operator_surface/style_contract_test.exs` ≥ 1)
    - The composited-tint rows are present: the test references `info-bg`, `warning-bg`, `success-bg`, `danger-bg`, and `accent-soft` as backgrounds fed through the composite helper (grep for `-bg` token names in the assertion region ≥ 4 distinct)
    - `--tl-color-muted-soft` is asserted at 4.5:1 against `surface` and `surface-raised` (D-04); if exempted, the file contains a comment citing "1.4.3" on the disabled rows only
    - The phase-143 dark contrast test ("phase 143 accessibility tokens meet dark-surface contrast baseline") still passes byte-stable (its source unchanged)
    - `mix format --check-formatted` exits 0; `mix compile --warnings-as-errors` clean
    - If `style.ex` was edited (D-02): `git diff lib/threadline/operator_surface/style.ex` shows edits ONLY within :188-237 and :240-289, the light and system rgba alphas changed identically, no dark token (≤:160) touched
  </acceptance_criteria>
  <done>Both the light and system token maps assert every UI-SPEC text/background pair (incl. composited status-on-tint) at 4.5:1 and `muted-soft` per D-04; the dark test stays byte-stable; any fix is an in-lane dual-branch alpha tune.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focus-ring 3:1 + per-mode interaction-state assertions; assert dark byte-stability</name>
  <files>test/threadline/operator_surface/style_contract_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/style_contract_test.exs — the focus guard analog (:688-735) to keep + extend; the composite helper from Task 1
    - .planning/phases/168-accessibility-verification/168-UI-SPEC.md — "Focus-Ring Non-Text Contrast Contract" (the 3:1 edge, halo composited-and-reported) + the "Interaction-State Contract" table (hover/active/disabled/selected per-mode contract)
    - .planning/phases/168-accessibility-verification/168-PATTERNS.md — section E (focus-ring 3:1 + behavioral guard :165-199), section F (theme-toggle ban KEEP INTACT :201-212)
    - .planning/phases/168-accessibility-verification/168-RESEARCH.md — focus-ring token shapes (:259-266); Anti-Pattern "Masking a focus failure with the halo"
    - lib/threadline/operator_surface/style.ex — border-focus :200/:252, focus-ring :236/:288, coverage-hover override :299-315, :focus-visible restore :350-356
  </read_first>
  <behavior>
    - In the light token map: contrast_ratio(border-focus=#1557C0, surface=#FFFFFF) ≥ 3.0 AND contrast_ratio(#1557C0, surface-raised=#EEF3FA) ≥ 3.0; same assertions run against the system map.
    - The 3px translucent halo (`rgba(21,87,192,0.22)` light) is composited over the control surface and its ratio is computed/reported but does NOT carry the pass — the opaque 1px edge is the load-bearing assertion.
    - The `:focus-visible` behavioral guard (outline:none-forbidden + box-shadow restore on button/[role=button]/input/select/a/summary) still asserts; it is understood to hold per mode (ring token theme-swapped, selector list shared).
    - Interaction states assert per-mode perceptible delta: hover surface differs from rest; the Phase-167 coverage-hover polarity flip (:299-315) holds (light/system tinted-hover, not white-on-white); active/selected uses accent-soft + a non-color cue (border/inset/aria-current); disabled reads not-actionable.
    - The seven theme-aware assertions and the `theme-toggle` ban (:8-30) remain present and passing.
  </behavior>
  <action>
    Extend the focus contract: in both the light and system token maps, assert the opaque 1px `border-focus` edge (#1557C0) clears non-text 3:1 (`>= 3.0`, NOT 4.5) against BOTH `surface` and `surface-raised` (controls sit on both), reusing `contrast_ratio/2`. Composite-and-REPORT the 3px translucent halo (the `rgba(...)` first shadow layer) over the control surface — compute its ratio but never let a low-alpha halo carry the pass (Anti-Pattern: halo masking). Keep the existing `:focus-visible` behavioral guard (:688) intact — do NOT re-author it; it already forbids blanket `outline: none` and asserts box-shadow restore on the shared selector list, now understood to hold per mode. Add per-mode interaction-state source assertions matching the UI-SPEC Interaction-State table: assert hover resolves to a surface/border delta; assert the Phase-167 coverage-hover dual-branch override (style.ex:299-315) is present for both `light` and `system` so the polarity flip holds (do NOT regress to white-on-white); assert active/pressed/selected carries accent-soft + a non-color cue; assert disabled (`muted-soft`) reads not-actionable. Do NOT duplicate or alter the theme-aware / theme-toggle-ban assertions (:8-30) — they are a hard constraint and must stay byte-stable. Finally, add (or confirm) an explicit acceptance that the phase-143 DARK contrast test and dark focus guard are unchanged — name them so a reviewer can confirm byte-stability.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs && mix format --check-formatted && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/style_contract_test.exs` exits 0
    - The test asserts `border-focus` at `>= 3.0` (NOT 4.5) against `surface` and `surface-raised` in both light and system maps (grep for `3.0` or `>= 3` in the focus assertion region ≥ 1, and the literal `4.5` is NOT used for the focus-edge assertion)
    - The translucent halo is composited and referenced but the pass hinges on the opaque edge (the focus rgba halo value appears in a report/compute path, not a sole `assert ... >= 3`)
    - The Phase-167 coverage-hover override (`style.ex:299-315`) is asserted present for both `data-tl-theme="light"` and `data-tl-theme="system"`
    - The seven theme-aware assertions and `refute String.contains?(src, "theme-toggle")` (:8-30) are still present and passing (grep: `grep -c 'theme-toggle' test/threadline/operator_surface/style_contract_test.exs` ≥ 1, in a `refute`)
    - The phase-143 dark contrast test and dark focus guard both still pass with their source bodies unchanged (byte-stable) — confirm via `git diff` shows no edits inside the `test "phase 143 accessibility tokens meet dark-surface contrast baseline"` or the existing portion of `test "phase 143 focus-visible ..."` bodies
    - `mix format --check-formatted` exits 0; `mix compile --warnings-as-errors` clean
  </acceptance_criteria>
  <done>Focus-ring 3:1 edge asserted per mode with halo reported-only; per-mode interaction states asserted incl. the coverage-hover flip; theme-toggle ban + dark phase-143 tests byte-stable and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → in-repo CSS source | `style_contract_test.exs` reads `style.ex` (a trusted, version-controlled file) as a string; no external/user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-01 | Tampering | `style.ex` dark tokens / base rules | mitigate | Dark byte-stability is asserted (Task 3): the phase-143 dark contrast test, dark focus guard, and theme-toggle ban must pass UNCHANGED; any D-02 edit is constrained to the light :188-237 and system :240-289 blocks via the acceptance-criteria git-diff check |
| T-168-02 | Information disclosure | none (no new routes, no runtime dependency) | accept | This plan is pure ExUnit source assertions over a trusted in-repo file; no network, no secrets, no user-input surface. RESEARCH §"Security Domain": ASVS V2–V6 N/A here |
| T-168-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — zero new packages (RESEARCH "Package Legitimacy Audit": not applicable; no runtime axe/contrast dependency added per Hard Constraint 1) |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/style_contract_test.exs` — full contract test green in BOTH light and system lanes
- The phase-143 dark contrast test + dark focus guard pass byte-stable (unchanged source)
- `mix format --check-formatted` and `mix compile --warnings-as-errors` clean
- Per-wave merge: `mix verify.test` — scope-aware of the 3 known nav-lane failures (do NOT conflate; do not stage/edit/revert the nav-overhaul lane)
- If `style.ex` edited (D-02): `git status` shows ONLY `style_contract_test.exs` + `style.ex` newly modified by this plan — no nav-lane file staged
</verification>

<success_criteria>
- A11Y-01: light + system AA contrast mirror with alpha-aware/compositing parsing; every text-bearing token ≥ 4.5:1 in both modes incl. composited status-on-tint rows; dark test byte-stable
- A11Y-02 part 1: the 1px border-focus edge ≥ 3:1 vs surface and surface-raised in both modes; halo composited-and-reported; `:focus-visible` restore guard holds per mode
- A11Y-02 part 1 (interaction-state source side): hover/active/disabled/selected per-mode deltas asserted; coverage-hover polarity flip holds; `muted-soft` per D-04
- Dark byte-stability intact; theme-toggle ban retained; no dark token or base rule edited
</success_criteria>

<output>
Create `.planning/phases/168-accessibility-verification/168-01-SUMMARY.md` when done.
Record: parser placement chosen (extend vs sibling), whether any D-02 tune fired (and which alpha
rows, both branches), any D-04 disabled exemption applied (with the WCAG-1.4.3 comment), and explicit
confirmation that the phase-143 dark test is byte-stable.
</output>
