---
phase: 168-accessibility-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/e2e/playwright.config.ts
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
autonomous: true
requirements: [A11Y-02]
must_haves:
  truths:
    - "The operator-accessibility.spec.ts affordance suite re-runs under the light branch (data-tl-theme=\"system\" resolved via prefers-color-scheme:light emulation) and the same affordance set passes"
    - "The served surface actually enters the light branch — focus-ring/chip computed colors are the LIGHT values, not the dark default (Pitfall 1 reconciled)"
    - "The existing dark e2e runs are unchanged — the default operator mount still renders data-tl-theme=\"dark\" with no behavior change"
    - "Any new served theme path is auth-gated behind the same operator pipeline as /audit (no unauthenticated operator-data leak)"
  artifacts:
    - path: "examples/threadline_phoenix/e2e/playwright.config.ts"
      provides: "A light-lane Playwright project with colorScheme: light"
      contains: "colorScheme"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      provides: "Env-gated theme on the operator mount so the light project resolves :system while default runs stay :dark"
      contains: "theme:"
  key_links:
    - from: "playwright.config.ts light project"
      to: "examples router operator mount"
      via: "colorScheme: light + env-gated theme: :system"
      pattern: "colorScheme"
    - from: "served operator surface"
      to: "[data-tl-theme=\"system\"] light branch"
      via: "prefers-color-scheme: light @media match"
      pattern: "data-tl-theme"
---

<objective>
Close A11Y-02 part 2 (the runtime affordance proof): re-run the existing
`operator-accessibility.spec.ts` affordance suite under the light branch so focus / chip / aria /
dialog / overflow affordances are proven MODE-INDEPENDENT. The same assertions that pass dark must
pass when the focus-ring and chip tokens swap to their light values.

Purpose: Source assertions (Plan 01) prove the token MATH is accessible; this plan proves the
affordances actually RENDER correctly in a browser under the light lane. It is the behavior proof;
Phase 169 owns the appearance/screenshot proof (out of scope here).

Output: A light-lane Playwright project driving `colorScheme: "light"`, plus the minimal example-app
wiring (Wave-0 D-01 reconciliation) that makes the served surface resolve the `[data-tl-theme="system"]`
light branch — WITHOUT changing the existing dark e2e default and WITHOUT bleeding into Phase 169's
`theme: :system` demonstration lane.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/168-accessibility-verification/168-UI-SPEC.md
@.planning/phases/168-accessibility-verification/168-PATTERNS.md
@.planning/phases/168-accessibility-verification/168-RESEARCH.md

<interfaces>
<!-- Extracted this session — the executor reuses these verbatim. -->

PLAYWRIGHT CONFIG (examples/threadline_phoenix/e2e/playwright.config.ts) — existing projects[] to clone:
  use: { baseURL, trace, screenshot, reducedMotion: "reduce" }   // colorScheme is a sibling first-class `use` option
  projects: [
    { name: "chromium", use: { ...devices["Desktop Chrome"] } },
    { name: "desktop-chromium", use: { ...devices["Desktop Chrome"], viewport: { width: 1280, height: 900 } } },
    { name: "mobile-chromium", use: { ...devices["Pixel 5"] } },
  ]

EXAMPLE OPERATOR MOUNT (examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex:171-188):
  scope "/audit" do
    pipe_through([:browser, :operator_browser, :operator_auth])
    threadline_operator_surface("/", actor_fn: ..., authorize_fn: ..., repo: ThreadlinePhoenix.Repo)  # NO theme: key → defaults :dark
  end

THEME DEFAULT (lib/threadline/operator_surface/auth.ex:14, :177-179):
  theme = Keyword.get(opts, :theme, :dark) |> normalize_theme()
  defp normalize_theme(:light), do: "light"
  defp normalize_theme(:system), do: "system"
  defp normalize_theme(_theme), do: "dark"

AFFORDANCE SUITE TO RE-RUN VERBATIM (operator-accessibility.spec.ts):
  expectFocused (:18-24)          → box-shadow != "none"
  expectNoHorizontalOverflow (:26-33), asserted :175,211,245
  nav aria-current="page" (:100,133)
  retention data-confirm (:169)
  dialog aria-modal="true" (:199)
  chip/verdict border non-0px/non-none (:226-231)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Reconcile the D-01 light-branch activation — env-gate the example operator-mount theme</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex — the operator mount (:171-188), the `:operator_browser`/`:operator_auth` pipeline it sits behind
    - lib/threadline/operator_surface/auth.ex — theme default :dark (:14) + normalize_theme/1 (:177-179)
    - .planning/phases/168-accessibility-verification/168-RESEARCH.md — Pitfall 1 (HIGH-IMPACT, the full D-01 reconciliation, options 1-3 + recommendation) + Open Question 1 + Assumption A1 (Playwright colorScheme default)
    - .planning/phases/168-accessibility-verification/168-PATTERNS.md — the router.ex conditional-wiring section (:323-346) + Pitfall-1 decision
    - .planning/phases/168-accessibility-verification/168-CONTEXT.md — D-01 (the locked emulation decision) — note the "zero example-app footprint" premise is NOT literally achievable given the :dark default; the minimal footprint is one env-gated line
  </read_first>
  <action>
    Resolve the D-01 / Pitfall-1 reconciliation by env-gating the example operator mount's theme (RESEARCH-recommended option 3 — lowest friction, zero default-behavior change, no Phase-169 bleed; chosen over option 1 "flip to :system" which bleeds into Phase 169, and over option 2 "dedicated route" which adds a second mount). Rationale (one line, record in SUMMARY): `colorScheme: "light"` alone renders the DARK lane because the mount defaults `theme: :dark` (auth.ex:14) — the `[data-tl-theme="system"]` light branch only activates when served with `theme: :system`; env-gating preserves the existing dark e2e default while letting a second Playwright project opt into the system branch. Add a `theme:` key to the `threadline_operator_surface("/", ...)` mount whose value reads an env var (e.g. `System.get_env("THREADLINE_E2E_THEME")` mapped to `:system` when set, else `:dark`) so that WITHOUT the env var the mount renders `:dark` exactly as today, and WITH the env var set to the system value it renders `:system`. Keep the mount behind the EXISTING `[:browser, :operator_browser, :operator_auth]` pipeline — do NOT add an unauthenticated route (T-168-02 mitigation). Do NOT flip the default to `:system` (Phase-169 bleed). This is a D-01 reconciliation within Claude's discretion ("lowest-friction wiring"), NOT a D-03 new-token halt. Touch ONLY this mount block; do not stage/edit/revert any nav-overhaul-lane file.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The operator mount in `router.ex` now passes a `theme:` key whose value is env-derived (grep: `grep -c 'theme:' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` ≥ 1, and an env read like `THREADLINE_E2E_THEME` or `System.get_env` appears in the mount region)
    - WITHOUT the env var set, the mount resolves `:dark` (the default branch of the env mapping) — existing dark behavior unchanged
    - The mount remains inside the `pipe_through([:browser, :operator_browser, :operator_auth])` scope — no new unauthenticated route added (grep confirms no new top-level `threadline_operator_surface` outside the gated scope)
    - `cd examples/threadline_phoenix && mix compile --warnings-as-errors` is clean
    - `git status` shows `examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` modified and NO nav-overhaul-lane file newly staged
  </acceptance_criteria>
  <done>The example operator mount renders `:dark` by default (existing runs unchanged) and `:system` when the e2e env var is set, behind the existing auth pipeline — the D-01 light-branch can now be driven without altering the dark default.</done>
</task>

<task type="auto">
  <name>Task 2: Add the light-lane Playwright project and re-run the affordance suite under the light branch</name>
  <files>examples/threadline_phoenix/e2e/playwright.config.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/playwright.config.ts — the existing `use` block + `projects[]` array (:13-26) to clone
    - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts — the affordance set to re-run VERBATIM (`expectFocused` :18-24, the full assertion set :100-245) — do NOT author new assertions
    - .planning/phases/168-accessibility-verification/168-PATTERNS.md — the playwright.config.ts clone-a-project section (:269-292) + the verbatim-re-run section (:295-320)
    - .planning/phases/168-accessibility-verification/168-RESEARCH.md — Pitfall 1 warning signs ("light e2e passes but computed colors are the dark values → surface never entered the light branch") + Assumption A1 (introspect Playwright colorScheme default)
    - examples/threadline_phoenix/e2e/run-e2e.sh — the boot+run driver
  </read_first>
  <action>
    Add a light-lane Playwright project by cloning an existing project entry (e.g. `desktop-chromium`) and adding `colorScheme: "light"` to its `use` (colorScheme is a first-class sibling of the existing `reducedMotion`). Wire it so the served surface resolves the `[data-tl-theme="system"]` light branch — pair the `colorScheme: "light"` project with the Task-1 env var set to the system value (set the env when the boot/run path launches the example app for that project, via `run-e2e.sh` or the config's webServer env as appropriate to the existing harness). The light project MUST run the EXISTING `operator-accessibility.spec.ts` VERBATIM — D-01 + UI-SPEC require the SAME affordance set (focus `box-shadow` non-`none`, chip border non-`0px`/non-`none`, `aria-current`, `aria-pressed`, dialog `aria-modal`, no horizontal overflow). Do NOT author new assertions or fork the spec. Guard against the Pitfall-1 false pass: confirm the served root is `data-tl-theme="system"` under the light project (the computed focus-ring/chip colors must be the LIGHT values, not dark) — if the affordances "pass" but the surface is still dark, the env wiring did not take and the re-run is meaningless. Keep `workers: 1` and the existing `reducedMotion: "reduce"` determinism settings. Touch ONLY `playwright.config.ts` (and, if the harness requires it, the env-passing in the existing `run-e2e.sh` invocation — but prefer config `webServer.env` / project env if that is the existing convention). Do not touch the nav-overhaul lane.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && npx tsc --noEmit -p e2e/tsconfig.json 2>/dev/null || node -e "require('./e2e/playwright.config.ts')" 2>/dev/null; node -e "const c=require('fs').readFileSync('e2e/playwright.config.ts','utf8'); if(!/colorScheme/.test(c)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` contains a new project entry with `colorScheme: "light"` (grep: `grep -c 'colorScheme' examples/threadline_phoenix/e2e/playwright.config.ts` ≥ 1)
    - The light project references the existing affordance spec (no new/forked spec file created — `operator-accessibility.spec.ts` is re-run verbatim; `git status` shows NO new `.spec.ts` file added)
    - The config still sets `workers: 1` and `reducedMotion: "reduce"` (determinism preserved)
    - Running the light project (`bash examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-accessibility.spec.ts` with the light project + the Task-1 env set to system) passes the same affordance set, AND the served root is `data-tl-theme="system"` under light emulation (NOT dark — confirm a computed focus-ring/chip color is a light value, defeating the Pitfall-1 false pass)
    - The existing dark project runs are unaffected (no env set → dark surface → same assertions still pass)
    - No nav-overhaul-lane file staged or modified
  </acceptance_criteria>
  <done>A `colorScheme: "light"` Playwright project re-runs `operator-accessibility.spec.ts` verbatim against the `:system` light branch (confirmed not the dark default), the same affordance set passes, and the dark runs are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (Playwright) → served operator surface | The e2e client hits the example app's `/audit` operator surface; it crosses the `:operator_browser`/`:operator_auth` pipeline (existing auth gate) |
| env var → served theme | `THREADLINE_E2E_THEME` (or equivalent) selects the served theme; a trusted local/CI-set env, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-02 | Information disclosure | example-app operator mount (theme wiring) | mitigate | The env-gated theme is added to the EXISTING `/audit` mount behind `[:browser, :operator_browser, :operator_auth]` — no new unauthenticated route serving operator data (RESEARCH §"Security Domain": option 3 chosen specifically to avoid a new route; the e2e re-uses the existing `login()` flow) |
| T-168-03 | Tampering | existing dark e2e default | accept | The env gate defaults to `:dark` when unset, so existing dark runs are byte-behavior-unchanged; the light path is opt-in only — acceptance criteria assert the unset default renders dark |
| T-168-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — `@playwright/test ^1.52.0` is already in `e2e/package.json` and unchanged; `colorScheme` is a built-in `use` option (RESEARCH "Package Legitimacy Audit": not applicable) |
</threat_model>

<verification>
- The light Playwright project re-runs `operator-accessibility.spec.ts` verbatim and the same affordance set passes under the `:system` light branch
- The served root under the light project is `data-tl-theme="system"` (NOT dark) — Pitfall-1 false-pass guard
- `cd examples/threadline_phoenix && mix compile --warnings-as-errors` clean after the router edit
- Existing dark e2e runs unchanged (env unset → dark default)
- `git status` shows only `playwright.config.ts` + `router.ex` (+ possibly `run-e2e.sh`) modified by this plan; NO nav-overhaul-lane file staged
</verification>

<success_criteria>
- A11Y-02 part 2: `operator-accessibility.spec.ts` re-run under the light branch passes the same affordance set (focus box-shadow≠none, chip border≠0px/none, aria-current, aria-pressed, dialog semantics, no horizontal overflow), proving mode-independence
- D-01 reconciled: light branch driven without changing the existing dark e2e default and without bleeding into Phase 169's `theme: :system` demonstration lane
- No new unauthenticated route; no new package; nav-overhaul lane untouched
</success_criteria>

<output>
Create `.planning/phases/168-accessibility-verification/168-02-SUMMARY.md` when done.
Record: the D-01 mechanism chosen (env-gate option 3) with its one-line rationale, the env var name,
the Playwright project name, explicit confirmation the served root was `data-tl-theme="system"` under
the light project (Pitfall-1 guard cleared), and confirmation the dark default runs are unchanged.
</output>
