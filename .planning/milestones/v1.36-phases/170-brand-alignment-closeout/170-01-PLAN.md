---
phase: 170-brand-alignment-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tokens.json
  - brandbook/tokens.css
  - brandbook/brand-book.md
  - brandbook/pressure-test.md
  - test/threadline/brandbook_token_parity_test.exs
autonomous: true
requirements: [BRAND-01, BRAND-02]

must_haves:
  truths:
    - "brandbook/tokens.json and tokens.css curated semantic tokens (dark + light) hold the same name AND value as the shipped style.ex lane for the 18 curated tokens"
    - "The brandbook warning-text values are corrected to style.ex truth (dark #F6C86B, light #8A5512)"
    - "An automated parity test fails CI if brandbook drifts from style.ex in either direction"
    - "brand-book.md carries a settled-truth 'UI theming posture' subsection (dark-primary, light via host config, runtime toggle deferred)"
    - "pressure-test.md dimension #11 carries a dual-mode addendum cross-referencing #5, plus one mechanical-suite parity gate line"
  artifacts:
    - path: "test/threadline/brandbook_token_parity_test.exs"
      provides: "Keystone parity test + posture-note + pressure-test doc-contract locks"
      contains: "defmodule Threadline.BrandbookTokenParityTest"
    - path: "brandbook/tokens.json"
      provides: "Corrected curated semantic dark/light tokens (names + values match style.ex)"
      contains: "semantic"
    - path: "brandbook/tokens.css"
      provides: "Corrected CSS-var emission matching style.ex"
      contains: "--tl-color-warning-text"
    - path: "brandbook/brand-book.md"
      provides: "UI theming posture subsection"
      contains: "UI theming posture"
    - path: "brandbook/pressure-test.md"
      provides: "Dimension #11 dual-mode addendum + mechanical parity gate"
      contains: "brandbook_token_parity_test"
  key_links:
    - from: "test/threadline/brandbook_token_parity_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "File.read! + regex over --tl-color-* declarations (dark base block + light block)"
      pattern: "operator_surface/style.ex"
    - from: "test/threadline/brandbook_token_parity_test.exs"
      to: "brandbook/tokens.json"
      via: "File.read! + Jason.decode! over semantic.dark / semantic.light"
      pattern: "tokens.json"
    - from: "brandbook/pressure-test.md"
      to: "test/threadline/brandbook_token_parity_test.exs"
      via: "mechanical-suite shell command line"
      pattern: "mix test test/threadline/brandbook_token_parity_test.exs"
---

<objective>
Make the brand SSOT state the same token truth as the shipped operator-surface lane,
and lock that alignment with an automated parity test. Then add the settled-truth
UI-theming-posture note to the brand book and the dual-mode addendum to the
pressure-test scorecard.

Purpose: BRAND-01 (token parity + posture note) and BRAND-02 (pressure-test
dual-mode addendum) are the two requirement IDs this milestone-closeout phase must
satisfy. The parity test is the keystone — "correct by default" applied to brand
governance: any future drift between brandbook and `style.ex` fails CI in either
direction.

Output:
- Corrected `brandbook/tokens.json` + `brandbook/tokens.css` (names + values align to `style.ex`)
- New `test/threadline/brandbook_token_parity_test.exs` (parity + doc-contract locks)
- `brandbook/brand-book.md` "UI theming posture" subsection
- `brandbook/pressure-test.md` dimension #11 dual-mode addendum + mechanical gate line
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/170-brand-alignment-closeout/170-CONTEXT.md
@.planning/phases/170-brand-alignment-closeout/170-RESEARCH.md
@.planning/phases/170-brand-alignment-closeout/170-PATTERNS.md

<interfaces>
<!-- Curated parity intersection: 18 tokens, SAME set dark + light. -->
<!-- Names below use style.ex conventions (the brandbook must be renamed to match). -->
<!-- style.ex is the source of truth; brandbook is corrected to it. NEVER edit style.ex. -->

style.ex name      brandbook tokens.json key (CURRENT)   action
  bg               bg                MATCH (no change)
  surface          surface           MATCH
  surface-raised   surface-raised    MATCH
  surface-hover    surface-hover     MATCH
  surface-selected surface-selected  MATCH
  border           border            MATCH
  border-strong    border-strong     MATCH
  text             text              MATCH
  muted            text-muted        RENAME text-muted -> muted (values match: dark #A3AFC2 / light #3B4762)
  muted-soft       text-soft         RENAME text-soft -> muted-soft (values match: dark #8F9DB5 / light #73819C)
  accent           accent            MATCH
  accent-strong    accent-strong     MATCH
  on-accent        accent-text       RENAME accent-text -> on-accent (values match: dark #08101F / light #FFFFFF)
  signal           signal            MATCH
  success-text     success-text      MATCH
  warning-text     warning-text      VALUE FIX: dark #FFD166 -> #F6C86B ; light #7A5400 -> #8A5512
  danger           error-text        RENAME error-text -> danger (values match: dark #FF8585 / light #A33434)
  info-text        info-text         MATCH

<!-- Brand-exclusive (stays in brandbook, intentionally NOT in style.ex): logo-arc (#4781E6 both modes) -->

<!-- Runtime-only exclusion set (in style.ex, MUST be absent from brandbook semantic blocks): -->
  surface-tint surface-tint-strong backdrop border-focus
  accent-soft accent-wash accent-border accent-inset
  signal-bg signal-border ink paper
  danger-bg danger-border warning-bg warning-dot warning-border
  success-bg success-border info-bg info-border
  neutral-bg neutral-text neutral-border
  op-insert-bg op-insert-text op-update-bg op-update-text
  op-delete-bg op-delete-text brand-rail

<!-- tokens.css uses --tl-color-* names: rename --tl-color-text-muted -> --tl-color-muted, -->
<!-- --tl-color-text-soft -> --tl-color-muted-soft, --tl-color-error-text -> --tl-color-danger; -->
<!-- if accent emits --tl-color-accent-text, rename to --tl-color-on-accent (verify against the file). -->
<!-- Fix --tl-color-warning-text in BOTH .tl-theme-dark and .tl-theme-light blocks. Keep --tl-color-logo-arc. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile brandbook tokens.json + tokens.css to style.ex truth</name>
  <files>brandbook/tokens.json, brandbook/tokens.css</files>
  <read_first>
    - brandbook/tokens.json (the file being corrected — read the full semantic.dark + semantic.light blocks)
    - brandbook/tokens.css (the file being corrected — read .tl-theme-dark and .tl-theme-light blocks)
    - lib/threadline/operator_surface/style.ex (SOURCE OF TRUTH — read the `.threadline-ui` dark base block and the `.threadline-ui[data-tl-theme="light"]` override block; style.ex WINS on every conflict, NEVER edit it)
    - 170-RESEARCH.md "Token Reconciliation" table (exact corrections)
    - 170-PATTERNS.md `brandbook/tokens.json` + `brandbook/tokens.css` sections (current JSON shape, value deltas)
  </read_first>
  <action>
    Correct the brandbook so every curated semantic token's NAME and VALUE equals the
    corresponding `style.ex` token in BOTH dark and light blocks (per D-01). Use the
    mapping in the &lt;interfaces&gt; block above. Concretely:

    In `brandbook/tokens.json` `semantic.dark` and `semantic.light`:
    - Rename key `text-muted` -> `muted`; `text-soft` -> `muted-soft`; `accent-text` -> `on-accent`; `error-text` -> `danger`. Preserve each value (they already match style.ex).
    - Fix value `warning-text`: dark `#FFD166` -> `#F6C86B`; light `#7A5400` -> `#8A5512` (style.ex truth).
    - Keep `logo-arc` (`#4781E6` both modes) — brand-exclusive, stays as-is.
    - Do NOT add any runtime-only token (the exclusion set in &lt;interfaces&gt;) to the semantic blocks.
    - Preserve key ORDER and JSON formatting style of the existing file; do not reorder unrelated keys; do not touch `raw`, `typography`, `spacing`, `radius`, `shadow`, `focus`, `code`, `callout`.
    - In the `state` block, update ONLY the two `var()` references to renamed-away CSS vars so they stay valid after the tokens.css rename: `"error": "var(--tl-color-error-text)"` -> `"error": "var(--tl-color-danger)"`; `"muted": "var(--tl-color-text-muted)"` -> `"muted": "var(--tl-color-muted)"`. Leave the rest of `state` untouched. (Without this, the rename leaves dangling brand-SSOT references — the exact drift this phase eliminates.)

    In `brandbook/tokens.css` `.tl-theme-dark` and `.tl-theme-light` blocks:
    - Rename CSS vars to match style.ex: `--tl-color-text-muted` -> `--tl-color-muted`; `--tl-color-text-soft` -> `--tl-color-muted-soft`; `--tl-color-error-text` -> `--tl-color-danger`. If `accent-text` is emitted as `--tl-color-accent-text`, rename to `--tl-color-on-accent` (verify against the live file).
    - Fix `--tl-color-warning-text` in BOTH blocks: dark `#FFD166` -> `#F6C86B`; light `#7A5400` -> `#8A5512`.
    - Keep `--tl-color-logo-arc`. Do not introduce any runtime-only token.

    Document the intentional gap (per D-02): add an `excluded_from_brand_scope` array (or a `_notes` key consistent with the file's existing convention) to `tokens.json` listing the runtime-only token names plus the brand-exclusive `logo-arc`, so the gap reads as intentional, not drift. Keep it minimal and valid JSON.

    Update both files atomically (same task) — they are hand-duplicated with no generated sync (the pre-existing dimension-#11 debt). Do NOT touch `style.ex`.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('brandbook/tokens.json','utf8'))" &amp;&amp; grep -c '#F6C86B' brandbook/tokens.json brandbook/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - `tokens.json` parses as valid JSON.
    - `semantic.dark` / `semantic.light` keys include `muted`, `muted-soft`, `on-accent`, `danger` and NO LONGER include `text-muted`, `text-soft`, `accent-text`, `error-text`.
    - `warning-text` is `#F6C86B` (dark) and `#8A5512` (light) in both tokens.json and tokens.css.
    - `logo-arc` is still present in both files; no runtime-only token (e.g. `accent-soft`, `surface-tint`, `op-insert-bg`) appears in the semantic blocks.
    - `tokens.css` `--tl-color-*` names match style.ex for the 18 curated tokens; warning-text fixed in both `.tl-theme-dark` and `.tl-theme-light`.
    - No `var()` reference in `tokens.json` (incl. the `state` block) points at a renamed-away CSS var: `grep -E "var\(--tl-color-(error-text|text-muted|text-soft|accent-text)\)" brandbook/tokens.json` returns nothing.
    - `git diff` shows no change to `lib/threadline/operator_surface/style.ex`.
  </acceptance_criteria>
  <done>Brandbook token names + values align to style.ex for all 18 curated tokens in both modes; warning-text corrected; runtime-only exclusions documented; style.ex untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add UI theming posture note + pressure-test dual-mode addendum</name>
  <files>brandbook/brand-book.md, brandbook/pressure-test.md</files>
  <read_first>
    - brandbook/brand-book.md lines ~217–248 (Dark/light strategy block + start of ## Color System — insertion target)
    - brandbook/pressure-test.md lines ~9–39 (mechanical suite format), ~66 (scorecard row for #11), ~125–137 (dimension #5 for cross-ref), ~203–213 (dimension #11 body)
    - 170-CONTEXT.md decisions D-04..D-08
    - 170-PATTERNS.md `brand-book.md` + `pressure-test.md` sections (exact placement, surrounding structure, addendum wording)
  </read_first>
  <action>
    Brand book (D-04, D-05, D-06): insert a `### UI theming posture` subsection in
    `brandbook/brand-book.md` immediately after the "Dark/light strategy" block closes
    (after the misuse list, before `## Color System` near line 248). Content states the
    settled truth: dark-primary; light is fully shipped and supported, enabled via host
    config (`theme: :system | :light | :dark`); per-operator runtime toggle is deferred to
    real adopter demand (`THEME-TOGGLE-01`; localStorage remains rejected); frame it as the
    v1.33 lesson (state as settled truth only now that light shipped in v1.36). The literals
    "UI theming posture", "dark-primary", "theme: :system | :light | :dark", and
    "THEME-TOGGLE-01" MUST appear verbatim (these are the Task 3 doc-contract lock targets).

    Pressure-test (D-07, D-08): do NOT add a dimension #16. Augment existing dimension #11
    "Token rigor" body with a "Dual-mode addendum (v1.36)" paragraph that (a) states the
    dual-mode pass condition — value-equality on the curated intersection across dark AND
    light, aligned to the shipped operator-surface lane — and (b) cross-references dimension #5
    "Dark/light versatility" (asset-layer dual-mode). Add ONE mechanical-suite line in the
    suite at the top of the file matching the existing `# description` / command / `# outcome`
    comment style: a comment line, then `mix test test/threadline/brandbook_token_parity_test.exs`,
    then a `# must exit 0` trailing comment. Optionally bump the #11 score from 8/10 to 9/10
    with the justification that drift is now automated, not review-based (discretionary; if
    bumped, update both the scorecard row at ~line 66 and the score line in the #11 body, and
    adjust the scorecard total accordingly).

    Do not alter other dimensions or any brandbook SVG/asset.
  </action>
  <verify>
    <automated>grep -q "UI theming posture" brandbook/brand-book.md &amp;&amp; grep -q "theme: :system | :light | :dark" brandbook/brand-book.md &amp;&amp; grep -q "THEME-TOGGLE-01" brandbook/brand-book.md &amp;&amp; grep -q "brandbook_token_parity_test" brandbook/pressure-test.md &amp;&amp; grep -qi "dual-mode addendum" brandbook/pressure-test.md</automated>
  </verify>
  <acceptance_criteria>
    - `brand-book.md` contains a `### UI theming posture` subsection between the Dark/light strategy block and `## Color System`, with all four required literals verbatim.
    - `pressure-test.md` dimension #11 body contains a "Dual-mode addendum" paragraph that cross-references dimension #5.
    - `pressure-test.md` mechanical suite contains the line `mix test test/threadline/brandbook_token_parity_test.exs`.
    - No new dimension #16 was added; scorecard count of scored dimensions is unchanged (still 15).
  </acceptance_criteria>
  <done>The brand book states the settled UI-theming posture, and the pressure-test carries the dual-mode addendum + mechanical parity gate — both lockable by literal assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the keystone parity test (+ posture-note and pressure-test doc-contract locks)</name>
  <files>test/threadline/brandbook_token_parity_test.exs</files>
  <read_first>
    - test/threadline/operator_surface_doc_contract_test.exs (PRIMARY analog — module header, File.read! + String.contains? / refute house style, custom failure messages, async: true)
    - test/threadline/operator_surface/theme_doc_contract_test.exs (individual-assertion doc-contract pattern, module-attribute paths)
    - lib/threadline/operator_surface/style.ex (the file the test parses — dark base block + [data-tl-theme="light"] block; the @media [data-tl-theme="system"] block must NOT be parsed as the light block)
    - brandbook/tokens.json (semantic.dark / semantic.light maps the test asserts against — read AFTER Task 1 corrections land)
    - brandbook/brand-book.md + brandbook/pressure-test.md (the literals the doc-contract tests lock — read AFTER Task 2 lands)
    - 170-RESEARCH.md "Parity Test Mechanics" + "Code Examples" (parser regex, block-split strategy, skeleton)
    - 170-PATTERNS.md "Shared Patterns" (doc-contract literal-lock, refute exclusion)
  </read_first>
  <behavior>
    - Test: curated dark intersection (18 tokens) — for each name, value parsed from style.ex dark base block == value in tokens.json semantic.dark. Fails loudly with the token name + both values on mismatch.
    - Test: curated light intersection (18 tokens) — same against style.ex [data-tl-theme="light"] block and tokens.json semantic.light. Parser targets the light block specifically, NOT the @media system block (Pitfall 3).
    - Test: brand-exclusive `logo-arc` is ABSENT from style.ex (refute `--tl-color-logo-arc:` present).
    - Test: every runtime-only token (the exclusion set) is ABSENT from tokens.json semantic blocks (refute Map.has_key?).
    - Test (secondary): tokens.css emits the same 18 intersection values as tokens.json for both modes (closes the hand-duplication debt).
    - Test (doc-contract, D-06): brand-book.md contains "UI theming posture", "dark-primary", "theme: :system | :light | :dark", "THEME-TOGGLE-01".
    - Test (doc-contract, BRAND-02): pressure-test.md contains the literal "mix test test/threadline/brandbook_token_parity_test.exs" and the dimension-#11 "Dual-mode addendum" marker + a dimension-#5 cross-reference.
  </behavior>
  <action>
    Create `test/threadline/brandbook_token_parity_test.exs` as
    `defmodule Threadline.BrandbookTokenParityTest`, `use ExUnit.Case, async: true`,
    `@moduledoc false`. Follow the doc-contract house style (File.read! at the top of each
    test; one concern per test block; custom failure messages).

    Store paths as module attributes: `@style_path "lib/threadline/operator_surface/style.ex"`,
    `@tokens_json_path "brandbook/tokens.json"`, `@tokens_css_path "brandbook/tokens.css"`,
    `@brand_book_path "brandbook/brand-book.md"`, `@pressure_test_path "brandbook/pressure-test.md"`.

    Define module attributes for the three token sets exactly as listed in &lt;interfaces&gt;:
    `@parity_intersection` (18 names in style.ex convention), `@brand_exclusive` (`["logo-arc"]`),
    `@runtime_only` (the full exclusion list).

    Parse style.ex per the RESEARCH parser strategy: read raw source, take the dark base block
    as the segment before the first `[data-tl-theme` occurrence; isolate the light block by
    splitting on `[data-tl-theme="light"]` then trimming to the closing brace BEFORE any
    `@media`/system selector; scan with `~r/--tl-color-([^:]+):\s*([^;]+?)\s*;/` into a
    `%{name => value}` map. Decode tokens.json with `Jason.decode!/1`. Implement all behavior
    cases above.

    Do NOT assert any literal token COUNT (the "45" is a planning-doc phrase; the runtime block
    has ~49 declarations). Assert value-equality on named intersection tokens only. Run
    `mix format` on the new file. It must compile under `--warnings-as-errors`.

    Sequencing: this test depends on Task 1's token corrections AND Task 2's brand-book +
    pressure-test edits to go green. Author it last in this plan and run it once Tasks 1–2 land.
  </action>
  <verify>
    <automated>mix test test/threadline/brandbook_token_parity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/brandbook_token_parity_test.exs` exits 0 (all tests green) after Tasks 1 and 2 land.
    - The file defines `Threadline.BrandbookTokenParityTest`, `async: true`, no token-count assertion anywhere.
    - Dark + light intersection tests, brand-exclusive refute, runtime-only refute, tokens.css consistency, posture-note lock, and pressure-test lock are each their own test block.
    - `mix format --check-formatted` passes for the new file; `mix compile --warnings-as-errors` is clean.
  </acceptance_criteria>
  <done>The keystone parity test exists, encodes value-equality + exclusion drift in both directions, locks the posture-note and pressure-test literals, and is green once the brand artifacts are corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan touches only brand JSON/CSS, brand markdown, and a test file. No untrusted input crosses any boundary; no network, auth, session, crypto, or user-facing endpoint is involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-01 | Tampering | brandbook/tokens.json, tokens.css | accept | Brand SSOT edits are dev-authored config; the new parity test detects unintended divergence from `style.ex` in CI (drift-as-tamper detection). No runtime exposure. |
| T-170-02 | Information disclosure | brand markdown / token files | accept | No secrets or PII; all values are public brand colors and documentation. |

No `high`-severity threats exist. This phase modifies test files, brand documentation markdown, and brand JSON/CSS only — no authentication, sessions, input validation, cryptography, network, or user-facing endpoints. No package installs (zero new dependencies), so no supply-chain (T-170-SC) checkpoint is required. Security gate passes.
</threat_model>

<verification>
- `mix test test/threadline/brandbook_token_parity_test.exs` exits 0 (keystone, all assertions green).
- `mix compile --warnings-as-errors` clean.
- `mix format --check-formatted` passes.
- `mix ci.all` green (parity test runs under the default suite; no regressions).
- `git diff lib/threadline/operator_surface/style.ex` is empty (style.ex frozen).
- The uncommitted nav-overhaul lane (~29 files under lib/, examples/, test/) is untouched.
</verification>

<success_criteria>
- BRAND-01: tokens.json + tokens.css reach curated-subset parity with style.ex (18 tokens, both modes); brand book carries the settled-truth "UI theming posture" note.
- BRAND-02: pressure-test.md carries the dual-mode addendum (dimension #11 + mechanical parity gate line).
- The parity test fails CI on any future drift in either direction (brandbook adds an unmirrored token, or style.ex changes a mirrored value).
- style.ex unchanged; nav-overhaul lane untouched; zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/170-brand-alignment-closeout/170-01-SUMMARY.md` when done
</output>
