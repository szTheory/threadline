---
phase: 169-screenshots-example-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts
  - examples/threadline_phoenix/e2e/playwright.config.ts
  - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts
autonomous: true
requirements: [EVID-01]

must_haves:
  truths:
    - "Running the light lane (THREADLINE_E2E_THEME=system, --project=desktop-chromium-light) emits __light__ durable screenshots for all 12 durable screens at viewport 1280"
    - "The dark lane (desktop-chromium / mobile-chromium) still emits __default__ baselines unchanged — no __default__ rename"
    - "The curated 5-screen regression guard (home, timeline-dense, row-history, exports, retention) also runs under the light project with its own auto-namespaced baselines"
    - "The light visual guard stays CI-skipped (test.skip on process.env.CI) — local-only per cf0e8e2"
  artifacts:
    - path: "examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts"
      provides: "Light-suffix durable emit + desktop-chromium-light viewport mapping"
      contains: "__light__"
    - path: "examples/threadline_phoenix/e2e/playwright.config.ts"
      provides: "Widened testMatch for desktop-chromium-light project"
      contains: "operator-screenshots"
    - path: "examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts"
      provides: "Light project admitted to the regression guard viewport gate"
      contains: "desktop-chromium-light"
  key_links:
    - from: "playwright.config.ts desktop-chromium-light project"
      to: "operator-screenshots.spec.ts"
      via: "widened testMatch regex"
      pattern: "operator-(accessibility|screenshots|screenshot-regression)"
    - from: "operator-screenshots.spec.ts screenshotViewport()"
      to: "__light__ durable emit"
      via: "desktop-chromium-light project-name case returns 1280"
      pattern: "desktop-chromium-light"
---

<objective>
Add a `__light__` durable screenshot lane beside the existing dark (`__default__`)
baselines by reusing the Phase-168 light render plumbing (the `desktop-chromium-light`
Playwright project, gated by `THREADLINE_E2E_THEME=system`, rendered under
`colorScheme: light`). Capture all 12 durable screens in light at desktop `1280`,
and mirror the curated 5-screen regression guard under the light project. No new
Playwright project, no new mix alias, no example-app mount change — minimal wiring only.

Purpose: EVID-01 — the appearance evidence that the dual-lane token system renders
correctly in light. Satisfies ROADMAP success-criterion-1 ("screenshot baselines gain
a `__light__` lane alongside dark covering the operator-surface screen set in both modes —
local-only, CI-skipped").
Output: Light-suffix durable PNGs + light regression baselines, produced by
`mix verify.example_browser_light`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-screenshots-example-docs/169-CONTEXT.md
@.planning/phases/169-screenshots-example-docs/169-PATTERNS.md

<interfaces>
<!-- Light lane is already gated; do NOT change these — only widen testMatch + teach the suffix. -->

playwright.config.ts (line 13): `const lightLane = process.env.THREADLINE_E2E_THEME === "system";`
playwright.config.ts (lines 19-31): the `desktop-chromium-light` project, currently
  `testMatch: /operator-accessibility\.spec\.ts/`, `viewport: { width: 1280, height: 900 }`,
  `colorScheme: "light" as const`.
playwright.config.ts (line 37): `snapshotPathTemplate` includes `{projectName}` — light
  regression baselines auto-namespace; do NOT change the template.

operator-screenshots.spec.ts (lines 10-23): `durableScreenshotNames` Set of 12 screens —
  reuse verbatim for the light lane (D-03, full set, desktop-only).
operator-screenshots.spec.ts (lines 41-50): durable emit; `__default__` is hardcoded into
  the PNG path here — this is the single chokepoint to teach `__light__`.
operator-screenshots.spec.ts (lines 53-62): `screenshotViewport(testInfo)` switch on
  `testInfo.project.name`; `desktop-chromium-light` currently falls to `default → null`.

operator-screenshot-regression.spec.ts (lines 78-81): `test.skip(!!process.env.CI, ...)` —
  the CI-skip local-only posture the light lane MUST inherit.
operator-screenshot-regression.spec.ts (lines 83-95): `beforeEach` project-gated viewport
  sizing; the 5 guarded screens are at lines 97, 104, 111, 123, 132.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teach operator-screenshots.spec.ts the light lane (__light__ suffix + desktop-chromium-light viewport)</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts (the file being modified — durable emit lines 41-50, screenshotViewport lines 53-62, durableScreenshotNames lines 10-23, beforeEach lines 65-77)
    - examples/threadline_phoenix/e2e/playwright.config.ts (read-only here: confirm the desktop-chromium-light project name + colorScheme: light + viewport 1280×900, lines 19-31; lightLane gate line 13)
  </read_first>
  <action>
    In `screenshotViewport()` add `case "desktop-chromium-light": return "1280";` so the
    light project produces a durable emit (D-01: teach the project name → 1280). In the
    durable-emit block (lines 41-50) replace the hardcoded `__default__` infix with a
    lane-derived suffix: emit `__light__` when running the light lane, otherwise keep
    `__default__`. Per Claude's-Discretion select the lane via
    `testInfo.project.name === "desktop-chromium-light"` (preferred — it is already the
    viewport key) OR `process.env.THREADLINE_E2E_THEME === "system"`; the dark lane MUST
    keep emitting `__default__` (D-02: NO rename of `__default__`). Reuse the existing
    `durableScreenshotNames` Set verbatim — all 12 screens, desktop-only, NO mobile-light
    (D-03). If the `beforeEach` needs the light project sized, note the config already sets
    `viewport: {1280,900}` for `desktop-chromium-light`, so no setViewportSize branch is
    required there. Do NOT add a `mobile-chromium-light` case. Do NOT touch any
    operator-surface source under `lib/`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx tsc --noEmit -p tsconfig.json 2>/dev/null; grep -c '__light__' tests/operator-screenshots.spec.ts; grep -c '__default__' tests/operator-screenshots.spec.ts; grep -c 'desktop-chromium-light' tests/operator-screenshots.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts | grep -c '__light__'` returns ≥ 1
    - `__default__` literal is still present in the spec (dark lane unchanged — D-02 no rename)
    - `screenshotViewport()` contains a `case "desktop-chromium-light"` returning `"1280"`
    - `durableScreenshotNames` Set still lists exactly the 12 screens (actor, coverage, evidence, exports, home, redaction, retention, row-history, timeline, timeline-dense, timeline-empty, transaction) — unchanged
    - NO `mobile-chromium-light` reference anywhere in the file
    - NO edits to any file under `lib/threadline/operator_surface/`
  </acceptance_criteria>
  <done>The light lane emits `__light__` durable PNGs for all 12 screens at 1280; the dark lane still emits `__default__`.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the desktop-chromium-light testMatch and admit the light project to the regression guard</name>
  <files>examples/threadline_phoenix/e2e/playwright.config.ts, examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/playwright.config.ts (the file being modified — desktop-chromium-light project lines 19-31, testMatch line 23, snapshotPathTemplate line 37, lightLane gate line 13)
    - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts (the file being modified — CI-skip lines 78-81, beforeEach project gate lines 83-95, the 5 guarded screens at lines 97/104/111/123/132)
  </read_first>
  <action>
    In `playwright.config.ts`, widen the `desktop-chromium-light` project's `testMatch`
    regex from `/operator-accessibility\.spec\.ts/` to also match `operator-screenshots.spec.ts`
    and `operator-screenshot-regression.spec.ts` — use an explicit union such as
    `/operator-(accessibility|screenshots|screenshot-regression)\.spec\.ts/` so the gate
    stays readable (D-01). Do NOT change the `lightLane` env gate (line 13),
    `colorScheme: "light"`, the `viewport`, or the `snapshotPathTemplate` (line 37) — the
    `{projectName}` token already auto-namespaces the light regression baselines. In
    `operator-screenshot-regression.spec.ts`, extend the `beforeEach` project-name gate so
    `desktop-chromium-light` is admitted (it inherits the config viewport, so a no-op pass
    is acceptable; ensure the existing `test.skip(testInfo.project.name === "chromium", ...)`
    still excludes the unsized `chromium` project). Guard the SAME 5 screens (home,
    timeline-dense, row-history, exports, retention) — do not add or remove screens. The
    `test.skip(!!process.env.CI, ...)` CI-skip posture MUST remain intact (cf0e8e2 local-only).
    Do NOT touch source under `lib/`, the example-app router mount, or any mix alias.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && grep -E 'operator-(accessibility\|screenshots\|screenshot-regression)' playwright.config.ts; grep -c 'process.env.CI' tests/operator-screenshot-regression.spec.ts; grep -c 'desktop-chromium-light' tests/operator-screenshot-regression.spec.ts; npx tsc --noEmit -p tsconfig.json 2>/dev/null && echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` desktop-chromium-light `testMatch` matches `operator-screenshots.spec.ts` (verify: the regex includes the `screenshots` alternative)
    - `playwright.config.ts` desktop-chromium-light `testMatch` matches `operator-screenshot-regression.spec.ts`
    - `lightLane` gate (line 13), `colorScheme: "light"`, and `snapshotPathTemplate` (line 37) are UNCHANGED
    - `operator-screenshot-regression.spec.ts` still contains `test.skip(!!process.env.CI` (CI-skip preserved)
    - the regression spec still guards exactly the 5 screens: home, timeline-dense, row-history, exports, retention (no additions/removals)
    - `desktop-chromium-light` is admitted by the regression `beforeEach` gate
    - NO edits under `lib/`, NO mix.exs alias change, NO example-app router edit
  </acceptance_criteria>
  <done>`mix verify.example_browser_light` runs the screenshots spec AND the 5-screen regression guard under the light project; both stay CI-skipped/local-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → PNG artifacts on disk | Screenshots written to `OPERATOR_SCREENSHOT_DIR`; never executed in CI (cf0e8e2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-01 | Information Disclosure | light screenshot PNGs | mitigate | Captures only synthetic demo fixture data (admin@example.com / acme.example.com seed); no real secrets or PII enters the frame. Verify by reusing the existing seeded demo, not a real database. |
| T-169-02 | Tampering | Playwright config / e2e specs | accept | Local-only test wiring; no production code path, no package installs, no network secrets. Low-value target. |
| T-169-SC | Tampering | npm installs | accept | No new npm dependencies are added (reuses existing @playwright/test); RESEARCH package-legitimacy gate N/A. |

Surface for this plan is minimal: local-only test/config edits, no source code under `lib/`, no new dependencies.
</threat_model>

<verification>
- `cd examples/threadline_phoenix/e2e && npx tsc --noEmit -p tsconfig.json` compiles clean.
- A local `mix verify.example_browser_light` run produces `*__light__*1280*.png` for all 12 durable screens and light regression baselines (manual/local — the lane is CI-skipped).
- `git diff --name-only` for this plan touches ONLY the 3 listed e2e files — no `lib/`, no `mix.exs`, no example-app router, no nav-overhaul files.
</verification>

<success_criteria>
- Light lane emits `__light__` durable PNGs for all 12 screens at 1280; dark lane unchanged (`__default__`).
- The 5-screen regression guard runs under `desktop-chromium-light` with auto-namespaced baselines.
- Light visual guard stays CI-skipped (local-only).
- EVID-01 / ROADMAP success-criterion-1 satisfied.
</success_criteria>

<output>
Create `.planning/phases/169-screenshots-example-docs/169-01-SUMMARY.md` when done.
</output>
