# Plan 04-02: @moduledoc, ExDoc, package metadata, Hex build gate

**Phase:** 4 — Documentation & Release  
**Plan:** 04-02 of 02  
**Status:** Ready to execute  
**Depends on:** 04-01 complete (README, `guides/domain-reference.md`, LICENSE on disk)  
**Blocks:** None (last plan in phase)  
**Requirements covered:** DOC-05 (and release-file completeness implied by roadmap goal)  
**Wave:** 2  
**requirements_addressed:** DOC-05

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-04-02-01 | `source_ref` points at non-existent git ref → broken “View Source” on HexDocs | medium | Set `source_ref: "main"` in `docs/0` until `v0.1.0` tag exists per **D-06**; add acceptance grep for `source_ref: "main"` **or** document exact alternative if default branch renamed (must not be `v0.1.0-dev` with no tag). |
| T-04-02-02 | `package/0` lists paths missing on disk → supply-chain / tarball failures | high | After edits, `mix hex.build` must exit 0; every path in `files:` must exist. |
| T-04-02-03 | `@moduledoc false` on semver-stable public modules | low | Only add rich docs to `AuditTransaction` and `AuditChange`; do not hide public API modules per **D-15–D-17**. |

---

## Goal

Close **DOC-05** by adding operator-oriented `@moduledoc` to `Threadline.Capture.AuditTransaction` and `Threadline.Capture.AuditChange`. Extend `mix.exs` `docs/0` with `extras`, `groups_for_modules`, `groups_for_extras` per **D-19–D-20** while keeping **`main: "Threadline"`** per **D-07** (not `main: "readme"`). Add `CHANGELOG.md` stub per **D-03**. Align `package/0` `files` with paths that exist (**D-05**) including `guides/` directory. Verify **`mix docs`**, **`mix hex.build`**, and **`mix ci.all`** complete successfully.

---

## Context

- **04-CONTEXT.md** D-01, D-03, D-05–D-07, D-15–D-21.
- **RESEARCH.md** §2–4 informs gaps but **D-07 overrides** `main: "readme"` research suggestion.
- Plans **04-01** must have landed README, guide, LICENSE before this plan’s `mix hex.build` gate.

---

## Tasks

### Task 1: Add `CHANGELOG.md`

**What to do:**

1. Create `CHANGELOG.md` with top-level heading `# Changelog`, an `[Unreleased]` section (can be empty stub), and a `## [0.1.0]` section with bullet “Initial release” (or equivalent per **D-03**).

**Acceptance criteria:**

- `test -f CHANGELOG.md` succeeds.
- `grep -q "## \\[0.1.0\\]" CHANGELOG.md` succeeds.

**Done condition:** Changelog stub exists for first publish story.

---

### Task 2: Add `@moduledoc` to capture schema modules

**What to do:**

1. `lib/threadline/capture/audit_transaction.ex`: insert `@moduledoc """ ... """` immediately after `defmodule` (before `use Ecto.Schema`) describing: role in trigger-backed capture; relationship to `AuditChange` and optional `AuditAction`; meaning of `txid`, `occurred_at`, `actor_ref`; pointer to `Mix.Tasks.Threadline.Install`, `Mix.Tasks.Threadline.Gen.Triggers`, and `guides/domain-reference.md` / HexDocs.
2. `lib/threadline/capture/audit_change.ex`: insert `@moduledoc` describing row-level capture, fields `table_name` / `table_pk` / `op`, link to domain guide and Phase 1 trigger contract.

**Acceptance criteria:**

- `grep -q "@moduledoc" lib/threadline/capture/audit_transaction.ex` succeeds.
- `grep -q "@moduledoc" lib/threadline/capture/audit_change.ex` succeeds.
- `mix compile --warnings-as-errors` exits 0.

**Done condition:** DOC-05 gap for these two modules closed.

---

### Task 3: Update `mix.exs` — `docs/0`

**What to do:**

1. Replace `docs/0` body to include at minimum:
   - `main: "Threadline"`
   - `source_url: @source_url`
   - `source_ref: "main"` (per **D-06** until tagged release — do **not** use `"v#{@version}"` while version is `0.1.0-dev` with no matching tag)
   - `extras:` including `"guides/domain-reference.md"`, `"README.md"`, `"CONTRIBUTING.md"`, `"CHANGELOG.md"` (all must exist on disk)
   - `groups_for_extras:` with sensible group names (e.g. “Overview”, “Reference”, “Project”) — exact labels Claude’s discretion per CONTEXT
   - `groups_for_modules:` with groups **Core API**, **Integration**, **Schemas**, **Mix Tasks** listing modules exactly as in **D-19** (module atoms must match real modules).

**Acceptance criteria:**

- `grep -q 'main: "Threadline"' mix.exs` succeeds.
- `grep -q 'source_ref: "main"' mix.exs` succeeds.
- `grep -q "groups_for_modules:" mix.exs` succeeds.
- `grep -q "guides/domain-reference.md" mix.exs` succeeds.
- `MIX_ENV=dev mix deps.get` and `MIX_ENV=dev mix docs` exit 0.

**Done condition:** ExDoc builds with grouped sidebar and extras.

---

### Task 4: Update `mix.exs` — `package/0`

**What to do:**

1. Ensure `files:` includes `lib`, `guides`, `.formatter.exs`, `mix.exs`, `README.md`, `LICENSE`, `CHANGELOG.md`, `CONTRIBUTING.md` (adjust list if project standard differs — every entry must exist).
2. Remove stale entries that point to missing paths (pre-04-01 state listed README/LICENSE/CHANGELOG as missing — after 04-01+Task1+Task1 of this plan they exist).

**Acceptance criteria:**

- `mix hex.build` exits 0 with no missing-file errors.
- `grep -q "guides" mix.exs` succeeds inside `package` function’s `files` list (or `~w` includes guides).

**Done condition:** Tarball builds cleanly.

---

### Task 5: Optional enrichment — `Threadline.Semantics.AuditAction` @moduledoc

**What to do:**

1. If **RESEARCH.md** “MEDIUM quality” note still applies, expand `lib/threadline/semantics/audit_action.ex` @moduledoc with 2–3 sentences on naming conventions for `name` / category examples — **skip** if already sufficient after re-read.

**Acceptance criteria:**

- `mix compile --warnings-as-errors` exits 0.

**Done condition:** Either improved moduledoc or explicit decision left unchanged with no compile regression.

---

### Task 6: Final verification gate

**What to do:**

1. Run `mix ci.all`.
2. Run `MIX_ENV=dev mix docs` (if not already clean from Task 3).
3. Run `mix hex.build`.

**Acceptance criteria:**

- `mix ci.all` exits 0.
- `MIX_ENV=dev mix docs` exits 0.
- `mix hex.build` exits 0.

**Done condition:** Phase 4 engineering exit per **D-01** (Hex-ready tree; human runs `mix hex.publish` separately).

---

## Verification

- Spot-check `doc/index.html` (local) or `_build/dev/lib/...` output: extras include domain reference.
- Confirm no public `def` in `lib/threadline/**/*.ex` and `lib/mix/**/*.ex` lacks `@doc` except allowed cases (`@doc false`, `@impl Plug`, generated callbacks) — if checker finds gaps, add `@doc` in follow-up task within this plan scope.

---

## Success criteria

- DOC-05 satisfied for identified gaps; `mix ci.all`, `mix docs`, `mix hex.build` all green.
- Package file list matches disk.

---
