# Plan 04-01: README, domain reference, LICENSE

**Phase:** 4 — Documentation & Release  
**Plan:** 04-01 of 02  
**Status:** Ready to execute  
**Depends on:** Phase 3 complete (query API, health, telemetry shipped)  
**Blocks:** 04-02 (ExDoc and Hex metadata assume README + guide paths exist)  
**Requirements covered:** DOC-01, DOC-02, DOC-03  
**Wave:** 1  
**requirements_addressed:** DOC-01, DOC-02, DOC-03

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-04-01-01 | README shows `set_config` / transaction bridge **without** `true` (session-scoped) → unsafe under PgBouncer | high | Copy the **exact** `set_config(..., true)` snippet from `lib/threadline/plug.ex` @moduledoc (lines 40–45 region); acceptance grep enforces `, true)` in README. |
| T-04-01-02 | README implies `repo:` optional on query APIs → runtime errors or wrong repo | medium | State explicitly that `repo: MyApp.Repo` is required; mirror wording from `Threadline.Query` @doc. |
| T-04-01-03 | Domain doc implies `Correlation` is a DB table | low | Open Correlation subsection with “not a database table” per CONTEXT D-13. |

---

## Goal

Add `README.md`, `guides/domain-reference.md`, and MIT `LICENSE` at repo root so a developer can follow the README install path (dependency, `mix threadline.install`, migrate, `mix threadline.gen.triggers`, router `plug Threadline.Plug`, transaction-local GUC + audited write) in line with **DOC-01** (≤15 minutes for a prepared Elixir/Postgres environment). README must **link** to the domain reference (**DOC-02**) and document PgBouncer transaction pooling + Threadline’s safe propagation pattern (**DOC-03**) with a short paragraph plus pointer to `Threadline.Plug` for the full SQL bridge.

---

## Context

- **04-CONTEXT.md** D-02, D-04, D-06–D-14, D-21 are authoritative. **D-07:** keep narrative split — README is thin front door; long SQL stays in module docs.
- **RESEARCH.md** §1–2, §6 (content reuse map): align examples with `MyApp.Repo` / `users` table.
- Canonical capture decision: `.planning/phases/01-capture-foundation/gate-01-01.md`.

---

## Tasks

### Task 1: Add MIT `LICENSE`

**What to do:**

1. Create `LICENSE` at repository root with standard MIT license text. Use copyright line `Copyright (c) 2026 szTheory` (owner per `mix.exs` `@source_url`).
2. Ensure file is plain text, no markdown wrapper.

**Acceptance criteria:**

- `test -f LICENSE` succeeds.
- `grep -q "Permission is hereby granted, free of charge" LICENSE` succeeds.
- `grep -q "THE SOFTWARE IS PROVIDED \"AS IS\"" LICENSE` succeeds.

**Done condition:** LICENSE exists and matches MIT expectations for `mix.exs` `licenses: ["MIT"]`.

---

### Task 2: Create `guides/domain-reference.md`

**What to do:**

1. Create directory `guides/` and file `guides/domain-reference.md`.
2. Structure per **D-12**: short intro → ubiquitous-language table (term → one sentence → tier: persisted row / field on row / concept only) → one ASCII diagram in a fenced `text` code block (spaces only; diagram shows relationship AuditAction ↔ AuditTransaction ↔ AuditChange) → one-sentence invariant below diagram → per-entity sections for **AuditTransaction**, **AuditChange**, **AuditAction**, **AuditContext**, **ActorRef**, **Correlation** (for **Correlation**, start with explicit “not a database table” per **D-13**).
3. Distill meanings from `prompts/audit-lib-domain-model-reference.md` and code (`lib/threadline/capture/*.ex`, `lib/threadline/semantics/*.ex`) — do not paste large excerpts.

**Acceptance criteria:**

- `test -f guides/domain-reference.md` succeeds.
- `grep -q "AuditTransaction" guides/domain-reference.md` succeeds.
- `grep -q "AuditChange" guides/domain-reference.md` succeeds.
- `grep -q "AuditAction" guides/domain-reference.md` succeeds.
- `grep -q "AuditContext" guides/domain-reference.md` succeeds.
- `grep -q "ActorRef" guides/domain-reference.md` succeeds.
- `grep -q "Correlation" guides/domain-reference.md` succeeds.
- `grep -qi "not a database table" guides/domain-reference.md` succeeds (Correlation clarification).

**Done condition:** DOC-02 vocabulary satisfied in one linked document.

---

### Task 3: Create `README.md`

**What to do:**

1. Create `README.md` with: badges row (CI workflow badge linking to `.github/workflows/ci.yml`, Hex version badge `hexpm/v/threadline`, HexDocs badge `hexdocs.pm/threadline`); one paragraph “What is Threadline”; requirements (Elixir ~> 1.15, PostgreSQL, Phoenix/Ecto-friendly); **Installation** with three numbered steps: add `{:threadline, "~> 0.1"}` to `deps`, run `mix threadline.install`, run `mix threadline.gen.triggers` (mention migrate between install and gen if install emits migration — mirror CONTRIBUTING/INSTALL task wording from `Mix.Tasks.Threadline.Install` @moduledoc); **Quick start** with `plug Threadline.Plug` in router/endpoint and a **single** `Ecto.Repo.transaction/1` block showing `Repo.query!("SELECT set_config('threadline.actor_ref', $1::text, true)", [json])` before an audited write — **json** line must use `ActorRef.to_map/1 |> Jason.encode!()` pattern from Plug docs; use `MyApp.Repo` and `users`-style example consistent with `lib/threadline/query.ex`.
2. **PgBouncer / pooling** subsection (3–5 sentences): transaction-mode pooling hazard; Threadline keeps request metadata on `conn.assigns`; GUC is set **inside** the same DB transaction as writes using `set_config(..., true)`; link to `Threadline.Plug` on HexDocs path `https://hexdocs.pm/threadline/Threadline.Plug.html` (or relative `./lib/threadline/plug.ex` for GitHub — include both if practical per **D-14**).
3. Prominent link: “Full API reference on HexDocs” → `https://hexdocs.pm/threadline`.
4. Link to `guides/domain-reference.md` (relative on GitHub: `guides/domain-reference.md`).
5. Link to `CONTRIBUTING.md`.

**Acceptance criteria:**

- `test -f README.md` succeeds.
- `grep -q "{:threadline" README.md` succeeds.
- `grep -q "mix threadline.install" README.md` succeeds.
- `grep -q "mix threadline.gen.triggers" README.md` succeeds.
- `grep -q "Threadline.Plug" README.md` succeeds.
- `grep -q "set_config('threadline.actor_ref'" README.md` succeeds.
- `grep -q ", true)" README.md` succeeds (transaction-local GUC flag visible in README snippet).
- `grep -qi "PgBouncer" README.md` succeeds.
- `grep -q "domain-reference" README.md` succeeds.
- `grep -q "CONTRIBUTING" README.md` succeeds.

**Done condition:** DOC-01, DOC-02 links, and DOC-03 PgBouncer + propagation story are present in README.

---

## Verification

After all tasks:

1. Human read: README install sequence matches actual Mix task names and order used in CI/docs (spot-check against `lib/mix/tasks/threadline.install.ex` and `lib/mix/tasks/threadline.gen.triggers.ex` headings).
2. `mix format` does not apply to markdown — optional: run `mix docs` only after 04-02 updates `mix.exs`; if ExDoc not yet pointing at guide, at least `wc -w guides/domain-reference.md` between 400 and 1200 words (informational, not blocking).

---

## Success criteria

- LICENSE, guides/domain-reference.md, and README.md exist and satisfy acceptance grep checks above.
- No contradiction with `gate-01-01.md` or `Threadline.Plug` @moduledoc on GUC safety.

---
