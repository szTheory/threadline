---
wave: 1
depends_on: []
files_modified:
  - guides/production-checklist.md
autonomous: true
requirements_addressed: [SCALE-01]
---

# Plan 30-01: Production checklist — volume, retention APIs, cross-links

**Phase:** 30 — Retention at scale & discovery  
**Plan:** 30-01 of 02  
**Status:** Ready to execute

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-30-01 | **Unsafe purge instructions:** prose implies deletes without `enabled: true` / `--execute` gates | high | Keep existing §4 bullets intact; new H3 explicitly references `Threadline.Retention.Policy`, `{:error, :disabled}`, and `mix threadline.retention.purge --dry-run` before `--execute`; mirror `lib/threadline/retention.ex` @moduledoc. |
| T-30-02 | **API drift:** wrong option names or defaults for `purge/1` | medium | Cite only documented options: `repo:` (required), `dry_run:`, `batch_size`, `max_batches`, `cutoff:` — from `Threadline.Retention.purge/1` spec line in source. |
| T-30-03 | **False correlation/export story:** text contradicts inner-join semantics | medium | D-3: 2–3 sentences — retention **bounds surviving rows** for timeline/export; defer SQL to `domain-reference.md`; do not assert join shapes here. |

---

## Objective

Implement **SCALE-01**: extend **`guides/production-checklist.md`** with **volume / growth / cadence** guidance tied to **`Threadline.Retention.Policy`**, **`Threadline.Retention.purge/1`**, and **`mix threadline.retention.purge`**, plus thin links to **export**, **timeline**, and **support** narratives per **`30-CONTEXT.md`** D-1–D-3.

---

## must_haves

- [ ] New H3 **`### Volume, growth, and purge cadence`** exists **inside** **`## 4. Retention and purge`**, **after** the bullet that links **`audit-indexing.md`** and **before** **`## 5. Export and investigation`**.
- [ ] H3 body names **`Threadline.Retention.Policy`** and **`Threadline.Retention.purge/1`** at least once each, and **`mix threadline.retention.purge`** with dry-run vs execute, without duplicating Phase 13 retention semantics (pointer to domain-reference).
- [ ] **Cadence + monitoring:** explicit operator guidance for **table growth**, **purge duration** / batch logs, and scheduling **often enough** that `max_batches` is not silently exhausted — aligned with `retention.ex` purge loop behavior.
- [ ] **§5** includes at least one bullet or sentence tying **`:from`/`:to`**, **`max_rows` / streaming**, and **correlation** work to **data surviving purge windows** (see D-3).
- [ ] **Support incident queries** intro (paragraph immediately under the `## Support incident queries` heading, before the table) gains **one sentence** on retention bounding historical data for Q1–Q5 style investigations.

---

## Task 30-01-01 — Add §4 volume H3 (SCALE-01 core)

<read_first>
- `.planning/milestones/v1.9-phases/30-retention-at-scale-discovery/30-CONTEXT.md` (D-1, D-2, D-3)
- `guides/production-checklist.md` (full file)
- `lib/threadline/retention.ex` (through `@spec purge` and first 20 lines of `def purge`)
- `lib/threadline/retention/policy.ex` (module doc + `resolve!/1` validation summary)
- `lib/mix/tasks/threadline.retention.purge.ex` (moduledoc through usage examples)
</read_first>

<action>
1. In **`guides/production-checklist.md`**, inside **`## 4. Retention and purge`**, insert a new markdown heading **exactly** **`### Volume, growth, and purge cadence`** **after** the checklist bullet that references **`audit-indexing.md`** (currently the last bullet of §4) and **before** the **`## 5. Export and investigation`** heading.
2. Under that H3, add **4–8 bullets** of prose that:
   - State operators should plan **purge cadence** (e.g. cron) so each run stays within **`max_batches`** and per-transaction work stays bounded; mention tuning **`--batch-size`** / `batch_size` starting near **500** and adjusting with lock wait and duration metrics.
   - Name **`Threadline.Retention.Policy`** as the validated config surface and **`Threadline.Retention.purge/1`** as the programmatic entry (with required **`repo:`**), and keep **`mix threadline.retention.purge`** as the CLI path including **`--dry-run`** vs **`MIX_ENV=prod … --execute`**.
   - Describe **what to monitor**: monotonic growth of **`audit_changes`** / storage, log lines from the mix task / purge batches (`deleted_changes`, batch indices), and wall-clock **duration per run**.
   - Link forward in prose (markdown link) to **`domain-reference.md`** retention anchor **`#retention-phase-13`** for cutoff / orphan-transaction semantics — **no** second full spec.
3. Do **not** renumber §5–§7; do **not** add a new top-level **`##`** between §4 and §5.
</action>

<acceptance_criteria>
- `grep -qF '### Volume, growth, and purge cadence' guides/production-checklist.md` exits 0.
- `grep -qF 'Threadline.Retention.Policy' guides/production-checklist.md` exits 0.
- `grep -qF 'Threadline.Retention.purge/1' guides/production-checklist.md` exits 0.
- `grep -qF 'mix threadline.retention.purge' guides/production-checklist.md` exits 0.
- `awk '/^## 4\\. Retention/,/^## 5\\. Export/{print}' guides/production-checklist.md | grep -qF '### Volume, growth, and purge cadence'` exits 0 (H3 lies strictly inside §4).
- `grep -qF 'domain-reference.md#retention-phase-13' guides/production-checklist.md` exits 0.
</acceptance_criteria>

---

## Task 30-01-02 — Export §5 retention hook (D-3)

<read_first>
- `guides/production-checklist.md` (`## 5. Export and investigation` section)
- `.planning/milestones/v1.9-phases/30-retention-at-scale-discovery/30-CONTEXT.md` (D-3)
</read_first>

<action>
1. In **`## 5. Export and investigation`**, add **one new bullet** (or extend an existing bullet by one sentence if it already covers filters) stating that **timeline** and **export** only return rows that **still exist** after retention windows — operators must align **`:from` / `:to`**, **`max_rows`**, streaming (`Threadline.Export.stream_changes/2`), and **correlation** investigations with the **purge policy** in §4; link to **`domain-reference.md`** export section **`#export-phase-14`** with relative link from `guides/`: `domain-reference.md#export-phase-14`.
</action>

<acceptance_criteria>
- `grep -qF '## 5. Export and investigation' guides/production-checklist.md` exits 0.
- `grep -qF 'domain-reference.md#export-phase-14' guides/production-checklist.md` exits 0.
- `grep -E 'purge|retention|§ ?4|section 4' guides/production-checklist.md | head -5` matches at least one line in §5 (manual spot-check: line occurs after `## 5` heading in file order — verify with `awk '/^## 5\\./,/^## 6\\./' guides/production-checklist.md | grep -qi retention` exits 0).
</acceptance_criteria>

---

## Task 30-01-03 — Support incident intro sentence (D-3)

<read_first>
- `guides/production-checklist.md` (`## Support incident queries` through table header)
- `30-CONTEXT.md` (D-3 optional intro)
</read_first>

<action>
1. Immediately **after** the heading line **`## Support incident queries`** and **before** the line **`Pre-launch:`**, insert **one sentence** of plain prose: incident queries assume audit rows in the **retained** window — if purges are aggressive, historical answers may be empty; point readers to **`## 4. Retention and purge`** using an in-file markdown link **`[retention and purge](#4-retention-and-purge)`** (slug for `## 4. Retention and purge`; if the repo’s renderer uses a different anchor, match the fragment used by existing internal links to §4 in this file).
</action>

<acceptance_criteria>
- `grep -A8 '^## Support incident queries' guides/production-checklist.md | grep -qiE 'retention|purge|retained'` exits 0.
- `grep -qF '#4-retention-and-purge' guides/production-checklist.md` exits 0.
</acceptance_criteria>

---

## Verification

1. `mix format` (no-op for `.md` but run for consistency)
2. `mix compile --warnings-as-errors`
3. `mix test` (full suite)

---

## Success criteria

- **SCALE-01:** Operators see volume/growth/cadence guidance adjacent to retention gates, with explicit ties to shipped retention APIs and monitoring hooks, without duplicating indexing matrices or retention semantics outside canonical guides.
