---
wave: 2
depends_on: [30-01-PLAN.md]
files_modified:
  - guides/domain-reference.md
  - README.md
autonomous: true
requirements_addressed: [SCALE-02]
---

# Plan 30-02: Discovery hub + README pointer

**Phase:** 30 — Retention at scale & discovery  
**Plan:** 30-02 of 02  
**Status:** Ready to execute

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-30-04 | **Navigation rot / duplicate canon:** hub repeats Phase 28 telemetry tables or Phase 29 DDL | high | Hub body is **links + one short orientation paragraph only**; no new matrices; link to existing anchors by file path + heading text from `guides/domain-reference.md` and `guides/audit-indexing.md`. |
| T-30-05 | **README bloat:** duplicates installation or full retention spec | medium | **One paragraph**; single handoff link to the new hub H2 in `domain-reference.md`; retention details stay in existing README subsection + domain-reference. |

---

## Objective

Implement **SCALE-02**: add **`## Operating at scale (v1.9+)`** to **`guides/domain-reference.md`** (orientation + links) and add **one high-visibility paragraph** in **`README.md`** pointing integrators to that hub per **`30-CONTEXT.md`** D-4.

---

## must_haves

- [ ] **`guides/domain-reference.md`** contains H2 **exactly** **`## Operating at scale (v1.9+)`** whose body links to **Telemetry**, **Trigger coverage**, **`audit-indexing.md`**, and **`production-checklist.md`** (with intent to deep-link §4 volume H3 once it exists from plan 30-01).
- [ ] **`README.md`** contains a **single new paragraph** (not a duplicate of the whole Maintainer section) in the **Maintainer / operator band** (immediately under **`## Maintainer checks`** is preferred) that links to **`guides/domain-reference.md`** using repo-relative path **`guides/domain-reference.md`** and mentions the hub heading text **Operating at scale (v1.9+)** as visible prose or link title.
- [ ] No new standalone `volume-at-scale.md` file.

---

## Task 30-02-01 — Domain reference hub H2

<read_first>
- `.planning/milestones/v1.9-phases/30-retention-at-scale-discovery/30-CONTEXT.md` (D-4, D-5)
- `guides/domain-reference.md` (TOC structure — pick insertion after **`## Audit indexing (integrator-owned)`** or before **`## Brownfield continuity`**, whichever reads more naturally)
- `guides/audit-indexing.md` (title line for correct link text)
- `guides/production-checklist.md` (post plan **30-01**: confirm **`### Volume, growth, and purge cadence`** exists; use GitHub/ExDoc slug for that H3 as sub-fragment, e.g. `#volume-growth-and-purge-cadence`, appended to `production-checklist.md#4-retention-and-purge` only if the renderer supports compound anchors — otherwise link to `production-checklist.md#4-retention-and-purge` only)
</read_first>

<action>
1. Add **`## Operating at scale (v1.9+)`** to **`guides/domain-reference.md`** with:
   - One short intro paragraph: v1.9 adds **telemetry narrative**, **indexing cookbook**, and **checklist volume/retention** guidance — this section is the **map**, not the depth.
   - Bullet list of markdown links:
     - To **`#telemetry-operator-reference`** in the same file (Telemetry).
     - To **`#trigger-coverage-operational`** (Trigger coverage).
     - To **`audit-indexing.md`** (relative: `audit-indexing.md`) with label **Audit table indexing cookbook**.
     - To **`production-checklist.md`** with anchor to the §4 volume subsection — use **`[Production checklist — retention and volume](production-checklist.md#4-retention-and-purge)`** minimum; if volume H3 slug is known, append sub-fragment per GitHub heading rules (e.g. `#volume-growth-and-purge-cadence` after verifying against rendered slug).
2. Do **not** paste IDX matrices or telemetry tables into this section.
</action>

<acceptance_criteria>
- `grep -qF '## Operating at scale (v1.9+)' guides/domain-reference.md` exits 0.
- `grep -qF 'audit-indexing.md' guides/domain-reference.md` exits 0.
- `grep -qF 'production-checklist.md' guides/domain-reference.md` exits 0.
- `grep -qF 'telemetry-operator-reference' guides/domain-reference.md` exits 0.
- `grep -qF 'trigger-coverage-operational' guides/domain-reference.md` exits 0.
</acceptance_criteria>

---

## Task 30-02-02 — README discovery paragraph

<read_first>
- `README.md` (lines 60–100 — Maintainer checks, retention, export)
- `guides/domain-reference.md` (after task 30-02-01 — hub heading exists)
</read_first>

<action>
1. Immediately **after** the markdown line **`## Maintainer checks`** (line ~72), insert **one paragraph** of prose (4–6 sentences max) that:
   - States that **production volume** guidance for **retention cadence**, **telemetry**, **trigger coverage**, and **optional indexing** lives in the **v1.9 operator map** under **`guides/domain-reference.md`**.
   - Includes a markdown link whose URL path is **`guides/domain-reference.md`** and anchor **`#operating-at-scale-v19`** if ExDoc/GitHub slug matches; if uncertain, link to file without fragment plus explicit **“see Operating at scale (v1.9+)”** in the link text.
   - Does **not** remove or shorten the existing **`### Data retention and purge`** subsection — it remains the quick retention summary.
</action>

<acceptance_criteria>
- `grep -n '^## Maintainer checks' README.md` exits 0.
- First paragraph after Maintainer checks header mentions **Operating at scale** OR **v1.9** (case-insensitive): `awk '/^## Maintainer checks/{p=1;next} p&&NF{print;exit}' README.md | grep -qiE 'operating at scale|v1\\.9'` exits 0.
- `grep -qF 'guides/domain-reference.md' README.md` exits 0.
</acceptance_criteria>

---

## Verification

1. `mix compile --warnings-as-errors`
2. `mix test`

---

## Success criteria

- **SCALE-02:** New readers can reach the v1.9 at-scale narrative from **README** in one hop via **domain-reference** hub without spelunking.
