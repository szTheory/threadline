---
wave: 1
depends_on: []
files_modified:
  - guides/domain-reference.md
autonomous: true
requirements_addressed: [OPS-01, OPS-02]
---

# Plan 28-01: Domain reference — telemetry narrative & trigger coverage semantics

**Phase:** 28 — Telemetry & health operators' narrative  
**Plan:** 28-01 of 02  
**Status:** Ready to execute

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-28-01 | **Misleading metrics:** prose implies `table_count` is always accurate when it is often **0** from `record_action` proxy | high | Document **proxy** path explicitly; tell operators to call `Threadline.Telemetry.transaction_committed/2` post-commit for real counts; mirror `lib/threadline/telemetry.ex` wording. |
| T-28-02 | **Wrong CI / Health semantics:** readers think every `{:uncovered, _}` fails `mix threadline.verify_coverage` or that audit tables should appear | medium | State **CoveragePolicy intersection** semantics and **audit table exclusion** verbatim from `Threadline.Health` / `CoveragePolicy` moduledocs. |
| T-28-03 | **Broken navigation:** new anchors break checklist links in plan 28-02 | low | Use stable heading text agreed in **28-CONTEXT.md** (`## Trigger coverage (operational)`); keep existing `## Telemetry (operator reference)` heading unchanged. |

---

## Objective

Extend **`guides/domain-reference.md`** so **OPS-01** (per-event operator narrative + cross-links to **`guides/production-checklist.md`**) and the **domain-reference half of OPS-02** (trigger coverage interpretation, relationship to **`mix threadline.verify_coverage`**, audit exclusion, pointer to **`[:threadline, :health, :checked]`**) are satisfied per **28-CONTEXT.md** D-1 and D-2.

---

## must_haves

- [ ] Existing **`## Telemetry (operator reference)`** heading and summary intro remain; **markdown table** with three event rows is **preserved** (may tighten wording but must not remove rows or change event tuple literals).
- [ ] Exactly **three** new `###` subsections under that section — one each for `[:threadline, :transaction, :committed]`, `[:threadline, :action, :recorded]`, and `[:threadline, :health, :checked]` — each containing the inner pattern: **When it fires**, **What to measure**, **Metadata**, **Misleading or degraded signals**, **Where to look next** (with link to `production-checklist.md` §1 or §6 where appropriate).
- [ ] One **numbered** triage playbook (≥5 and ≤10 steps) for weekly/post-deploy / “metrics look wrong” that points into those subsections and **`guides/production-checklist.md`** (use anchors `production-checklist.md#1-capture-and-triggers` and `production-checklist.md#6-observability`).
- [ ] New section with heading **exactly** `## Trigger coverage (operational)` covering: `{:covered, _}` vs `{:uncovered, _}`; **`mix threadline.verify_coverage`** uses the same catalog and only **expected_tables** gate CI; audit tables excluded by design; link back to the per-event subsection for **`[:threadline, :health, :checked]`** (same-file anchor).
- [ ] No new LOOP-04-class doc contract markers unless optional single marker explicitly added (default: none).

---

## Task 28-01-01 — Per-event telemetry subsections + playbook (OPS-01)

<read_first>
- `guides/domain-reference.md` (full file — `## Telemetry (operator reference)` region through end of that section)
- `.planning/milestones/v1.9-phases/28-telemetry-health-operators-narrative/28-CONTEXT.md` (D-1, D-3)
- `lib/threadline/telemetry.ex`
- `lib/threadline.ex` (lines where `emit_action_recorded` / `emit_transaction_committed_proxy` are called)
- `guides/production-checklist.md` (§1 and §6 headings for link targets)
</read_first>

<action>
1. Under **`## Telemetry (operator reference)`**, **after** the existing four-column table, insert **three** `###` headings whose titles each include the **full event tuple in backticks** as the primary identifier (e.g. ``### `[:threadline, :transaction, :committed]` ``).
2. For **transaction committed**: explain automatic capture path vs **`Threadline.Telemetry.transaction_committed/2`**; document **`table_count: 0`** proxy when **`Threadline.record_action/2`** succeeds without post-commit hook; include **one generic vendor-agnostic “bad signal” example** (e.g. steady zero counts despite known writes).
3. For **action recorded**: document **`status`** `:ok` / `:error` and what operators might chart or alert on.
4. For **health checked**: document **`covered` / `uncovered` as aggregate counts** (not per-table lists in telemetry); point to **`## Trigger coverage (operational)`** for tuple semantics.
5. Add a **numbered list** (5–10 items) titled in prose as a **weekly / post-deploy triage** or **“metrics look wrong”** playbook immediately after the three subsections (still under `## Telemetry (operator reference)`), each step linking to subsection anchors or `production-checklist.md#1-capture-and-triggers` / `#6-observability`.
</action>

<acceptance_criteria>
- `grep -qF '## Telemetry (operator reference)' guides/domain-reference.md` exits 0.
- `grep -qF '[:threadline, :transaction, :committed]' guides/domain-reference.md` exits 0.
- `grep -qF '[:threadline, :action, :recorded]' guides/domain-reference.md` exits 0.
- `grep -qF '[:threadline, :health, :checked]' guides/domain-reference.md` exits 0.
- `grep -q 'production-checklist.md#1-capture-and-triggers' guides/domain-reference.md` exits 0.
- `grep -q 'production-checklist.md#6-observability' guides/domain-reference.md` exits 0.
- `grep -qE '^[0-9]+\. ' guides/domain-reference.md` exits 0 (at least one numbered playbook line).
- `grep -qF 'table_count' guides/domain-reference.md` exits 0.
- `grep -qF 'Threadline.Telemetry.transaction_committed' guides/domain-reference.md` exits 0.
</acceptance_criteria>

---

## Task 28-01-02 — Trigger coverage (operational) section (OPS-02, domain half)

<read_first>
- `guides/domain-reference.md` (post task 28-01-01 state)
- `lib/threadline/health.ex`
- `lib/mix/tasks/threadline.verify_coverage.ex`
- `lib/threadline/verify/coverage_policy.ex`
- `.planning/milestones/v1.9-phases/28-telemetry-health-operators-narrative/28-CONTEXT.md` (D-2)
</read_first>

<action>
1. After the **`## Telemetry (operator reference)`** block (after playbook), insert a new H2 with heading **exactly** `## Trigger coverage (operational)`.
2. Document **`Threadline.Health.trigger_coverage/1`** inputs (`repo:`) and output shape `[{:covered, String.t()} | {:uncovered, String.t()}]`.
3. Document **`audit_transactions`**, **`audit_changes`**, **`audit_actions`** excluded from per-table results and why (CAP-10 / not expected to carry capture triggers) — align with **`Threadline.Health`** `@moduledoc`.
4. Document **`mix threadline.verify_coverage`**: requires **`config :threadline, :verify_coverage, expected_tables:`**; uses **`CoveragePolicy.violations/2`** — only configured expected names can fail CI; **`{:uncovered, t}`** on a non-expected table is not a Mix failure.
5. End the section with a same-file markdown link to the `###` heading for **`[:threadline, :health, :checked]`** (health telemetry subsection).
</action>

<acceptance_criteria>
- `grep -nF '## Trigger coverage (operational)' guides/domain-reference.md` exits 0.
- `grep -qF '{:covered' guides/domain-reference.md` exits 0.
- `grep -qF '{:uncovered' guides/domain-reference.md` exits 0.
- `grep -qF 'mix threadline.verify_coverage' guides/domain-reference.md` exits 0.
- `grep -qF 'CoveragePolicy' guides/domain-reference.md` exits 0.
- `grep -qi 'audit_transactions' guides/domain-reference.md` exits 0.
- `grep -qi 'expected_tables' guides/domain-reference.md` exits 0.
</acceptance_criteria>

---

## Verification

1. `mix format`
2. `mix compile --warnings-as-errors`
3. `mix test`

---

## Success criteria

- **OPS-01:** Operator can read per-event narrative and playbook without opening Elixir sources first.
- **OPS-02 (domain):** Trigger coverage and Mix task semantics documented accurately and cross-linked to telemetry health event.
