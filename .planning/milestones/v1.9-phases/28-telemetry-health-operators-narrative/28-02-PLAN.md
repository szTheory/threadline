---
wave: 2
depends_on: [28-01-PLAN.md]
files_modified:
  - guides/production-checklist.md
  - README.md
autonomous: true
requirements_addressed: [OPS-01, OPS-02]
---

# Plan 28-02: Production checklist + README pointers (OPS-02 checklist half, OPS-01 cross-links)

**Phase:** 28 — Telemetry & health operators' narrative  
**Plan:** 28-02 of 02  
**Status:** Ready to execute

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-28-02 | Checklist contradicts domain-reference on Health / Mix semantics | high | **Read** updated `guides/domain-reference.md` before editing checklist; copy same phrases for exclusion + expected_tables policy. |
| T-28-04 | README becomes third full duplicate of semantics | low | At most **one** added sentence + link to `guides/domain-reference.md#trigger-coverage-operational` if not already present. |

---

## Objective

Update **`guides/production-checklist.md`** so **OPS-02** checklist half (when to run coverage checks, how to interpret tuples vs Mix, audit exclusion, links) and bidirectional links to **`guides/domain-reference.md`** match **28-CONTEXT.md** D-2. Optionally tighten **README** discovery per D-2 without duplicating full semantics.

---

## must_haves

- [ ] §**1. Capture and triggers** contains a **short** bullet row for **`Threadline.Health.trigger_coverage/1`** cadence (after deploy / schema change / periodic) plus **one line** stating audit catalog tables are **excluded by design** with **link** to `domain-reference.md#trigger-coverage-operational` (not a second full essay).
- [ ] §**6. Observability** links to **`domain-reference.md#telemetry-operator-reference`** (preserve or refresh) **and** includes at least one pointer to **`#trigger-coverage-operational`** or an anchor inside the telemetry section for per-event narrative.
- [ ] Relative paths stay repo-root-relative from `guides/` (same style as existing `domain-reference.md` links).

---

## Task 28-02-01 — Expand checklist §1 and §6 (OPS-02)

<read_first>
- `guides/production-checklist.md` (full file)
- `guides/domain-reference.md` (sections `## Telemetry (operator reference)` and `## Trigger coverage (operational)` after plan 28-01)
- `.planning/milestones/v1.9-phases/28-telemetry-health-operators-narrative/28-CONTEXT.md` (D-2)
</read_first>

<action>
1. In **`## 1. Capture and triggers`**, after existing `verify_coverage` / `mix threadline.verify_coverage` bullet, add **≤3 short bullets** total that cover: when operators should call **`Threadline.Health.trigger_coverage/1`**; that **`{:covered,_}`** / **`{:uncovered,_}`** are per public user table; that **`mix threadline.verify_coverage`** only gates **expected_tables**; single **markdown link** `domain-reference.md#trigger-coverage-operational` for full semantics.
2. Add **one explicit short line** in §1 that **`audit_transactions`**, **`audit_changes`**, **`audit_actions`** are not expected in Health’s per-table list — link to same domain anchor.
3. In **`## 6. Observability`**, extend the Threadline telemetry bullet to also link **`domain-reference.md#trigger-coverage-operational`** (or subsection anchor under Telemetry) so operators reach per-event narrative from checklist.
</action>

<acceptance_criteria>
- `grep -q 'domain-reference.md#trigger-coverage-operational' guides/production-checklist.md` exits 0.
- `grep -qi 'audit_transactions' guides/production-checklist.md` exits 0.
- `grep -qF 'Threadline.Health.trigger_coverage' guides/production-checklist.md` exits 0.
- `grep -q 'domain-reference.md#telemetry-operator-reference' guides/production-checklist.md` exits 0.
- `grep -qF 'expected_tables' guides/production-checklist.md` exits 0.
</acceptance_criteria>

---

## Task 28-02-02 — README discovery line (optional, README only if gap)

<read_first>
- `README.md` (region around `verify_coverage` / Health)
- `guides/domain-reference.md` (`## Trigger coverage (operational)` anchor)
</read_first>

<action>
1. If **`README.md`** already contains substring **`domain-reference.md#trigger-coverage-operational`**, make **no** change and note in commit message “README already linked”.
2. Else add **one sentence** near the existing **`mix threadline.verify_coverage`** / **`Threadline.Health.trigger_coverage/1`** prose that links operators to **`guides/domain-reference.md#trigger-coverage-operational`** for full interpretation.
</action>

<acceptance_criteria>
- `grep -q 'domain-reference.md#trigger-coverage-operational' README.md` exits 0 **after** this task (either pre-existing or newly added).
</acceptance_criteria>

---

## Verification

1. `mix format`
2. `mix compile --warnings-as-errors`
3. `mix test`

---

## Success criteria

- **OPS-02:** Checklist and domain reference stay aligned on cadence, tuple meaning, Mix policy, and audit exclusions.
- **OPS-01:** Checklist points operators at expanded telemetry narrative anchors.
