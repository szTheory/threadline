---
wave: 1
depends_on: []
files_modified:
  - guides/audit-indexing.md
  - mix.exs
  - guides/domain-reference.md
  - guides/production-checklist.md
autonomous: true
requirements_addressed: [IDX-01]
---

# Plan 29-01: Audit indexing cookbook + ExDoc + navigation

**Phase:** 29 — Audit table indexing cookbook  
**Plan:** 29-01 of 02  
**Status:** Ready to execute

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-29-01 | **Wrong join story:** prose says INNER for export when code uses LEFT JOIN without `correlation_id` | high | Copy join rules verbatim from `Threadline.Query.export_changes_query/1` and `filter_by_correlation/2` moduledocs; include “Tables & modules” box citing `lib/threadline/query.ex`. |
| T-29-02 | **Mandatory DDL misread:** operators paste indexes that fight shipped baselines | medium | Label every additive DDL **non-mandatory**; duplicate shipped index **names** only from `Threadline.Capture.Migration` / `Threadline.Semantics.Migration` — never invent names. |
| T-29-03 | **Navigation rot:** checklist points to wrong path | low | Use full path `guides/audit-indexing.md` in link targets; grep-verifiable in acceptance criteria. |

---

## Objective

Ship **`guides/audit-indexing.md`** as the canonical indexing cookbook (**IDX-01**), register it in **`mix.exs`** ExDoc extras, and add **thin** navigation from **`guides/domain-reference.md`** and **`guides/production-checklist.md`** per **29-CONTEXT.md** D-1–D-3.

---

## must_haves

- [ ] New file **`guides/audit-indexing.md`** contains HTML comment **`<!-- IDX-02-AUDIT-INDEXING -->`** (exact substring for contracts).
- [ ] **Installed defaults** subsection lists every shipped index **name** matching migrations (txid, transaction_id, table_name, captured_at on changes; GIN + inserted_at + name on actions).
- [ ] Access-pattern sections distinguish **timeline** vs **export** join shape for `correlation_id` present vs absent.
- [ ] **Retention** section references `captured_at` cutoff deletes and orphan `audit_transactions` `NOT EXISTS` — aligned with `Threadline.Retention`.
- [ ] **`mix.exs`** includes `"guides/audit-indexing.md"` in `extras:`.
- [ ] **domain-reference** and **production-checklist** link to the new guide with path `guides/audit-indexing.md` (or relative `audit-indexing.md` from within `guides/` if that is the repo’s established pattern — **must match** existing cross-guide links in those files).

---

## Task 29-01-01 — Author `guides/audit-indexing.md`

<read_first>
- `.planning/milestones/v1.9-phases/29-audit-table-indexing-cookbook/29-CONTEXT.md` (D-1 through D-5, canonical_refs)
- `.planning/milestones/v1.9-phases/29-audit-table-indexing-cookbook/29-RESEARCH.md`
- `lib/threadline/capture/migration.ex`
- `lib/threadline/semantics/migration.ex`
- `lib/threadline/query.ex` (`timeline_query/1`, `export_changes_query/1`, `filter_by_correlation/2`, `timeline_base_query/1`, `timeline_order/1`)
- `lib/threadline/retention.ex` (`delete_change_batch/3`, `drain_orphans/3`)
- `guides/domain-reference.md` (first 80 lines — tone / heading style)
</read_first>

<action>
1. Create **`guides/audit-indexing.md`** with an H1 title **`# Audit table indexing cookbook`**.
2. On line 3 (or immediately after H1), insert the literal HTML comment: **`<!-- IDX-02-AUDIT-INDEXING -->`**.
3. Add H2 **`## Installed defaults`** describing tables and listing **exact index names** from migrations: `audit_transactions_txid_idx`, `audit_changes_transaction_id_idx`, `audit_changes_table_name_idx`, `audit_changes_captured_at_idx`, `audit_actions_actor_ref_idx`, `audit_actions_inserted_at_idx`, `audit_actions_name_idx`; mention PK/FK columns in prose; cite **`Threadline.Capture.Migration`** and **`Threadline.Semantics.Migration`** as source of truth.
4. Add H2 **`## Table primers`** with three H3 headings **exactly** `### audit_transactions`, `### audit_changes`, `### audit_actions` — each ≤1 screen of grain, keys, and invariants (include: retention touches **changes and transactions**, not changes alone).
5. Add H2 **`## Access patterns`** then these H2-level sections (each is its own `##` for doc-contract spine): **`## Timeline and Threadline.Query`**, **`## Export and Threadline.Export`**, **`## Correlation filtering`**, **`## Retention and Threadline.Retention`**. In each, include a short **“Tables & modules”** bullet list naming **`Threadline.Query`**, **`Threadline.Export`**, or **`Threadline.Retention`** as appropriate and the join graph in prose (correlation path: **inner** `audit_actions` on `action_id` + `correlation_id`; export without correlation: **LEFT** join actions).
6. Add a **tradeoffs** subsection (H2 **`## Tradeoffs and evidence`**) covering write amplification, redundant prefixes, `CREATE INDEX CONCURRENTLY`, and `EXPLAIN (ANALYZE, BUFFERS)` / `pg_stat_user_indexes`.
7. Add H2 **`## Optional additive indexes`** with at least one fenced `sql` example prefixed by explicit **non-mandatory** language; example must use placeholder table/index names or generic pattern, not contradict shipped names.
8. End with H2 **`## See also`** containing markdown links to **`guides/domain-reference.md`** and **`guides/production-checklist.md`** (relative links from `guides/` directory: `domain-reference.md` and `production-checklist.md`).
</action>

<acceptance_criteria>
- `test -f guides/audit-indexing.md` exits 0.
- `grep -qF '<!-- IDX-02-AUDIT-INDEXING -->' guides/audit-indexing.md` exits 0.
- `grep -qF '# Audit table indexing cookbook' guides/audit-indexing.md` exits 0.
- `grep -qF '## Installed defaults' guides/audit-indexing.md` exits 0.
- `grep -qF '### audit_transactions' guides/audit-indexing.md` exits 0.
- `grep -qF '### audit_changes' guides/audit-indexing.md` exits 0.
- `grep -qF '### audit_actions' guides/audit-indexing.md` exits 0.
- `grep -qF '## Timeline and Threadline.Query' guides/audit-indexing.md` exits 0.
- `grep -qF '## Export and Threadline.Export' guides/audit-indexing.md` exits 0.
- `grep -qF '## Correlation filtering' guides/audit-indexing.md` exits 0.
- `grep -qF '## Retention and Threadline.Retention' guides/audit-indexing.md` exits 0.
- `grep -qF 'audit_transactions_txid_idx' guides/audit-indexing.md` exits 0.
- `grep -qi 'left join' guides/audit-indexing.md` exits 0.
- `grep -qi 'inner join' guides/audit-indexing.md` exits 0.
- `grep -qF 'Threadline.Retention' guides/audit-indexing.md` exits 0.
- `grep -qF 'domain-reference.md' guides/audit-indexing.md` exits 0.
- `grep -qF 'production-checklist.md' guides/audit-indexing.md` exits 0.
</acceptance_criteria>

---

## Task 29-01-02 — Register ExDoc extra

<read_first>
- `mix.exs` (entire `docs/0` function)
- `guides/audit-indexing.md` (exists from task 29-01-01)
</read_first>

<action>
1. In **`mix.exs`**, inside `docs/0` → `extras: [ ... ]`, add the string **`"guides/audit-indexing.md"`** immediately after **`"guides/adoption-pilot-backlog.md"`** (alphabetical order within `guides/` block: adoption-pilot → **audit-indexing** → brownfield).
</action>

<acceptance_criteria>
- `grep -qF '"guides/audit-indexing.md"' mix.exs` exits 0.
- `mix format mix.exs` exits 0 (run after edit).
</acceptance_criteria>

---

## Task 29-01-03 — Cross-links from existing guides

<read_first>
- `guides/domain-reference.md` (full file — pick insertion point near audit / performance / adoption intro)
- `guides/production-checklist.md` (search for `## Observability` or indexing-adjacent section per file structure)
- `guides/audit-indexing.md`
</read_first>

<action>
1. **`guides/domain-reference.md`:** Add a short subsection (suggested H2 or H3 title: **`## Audit indexing (integrator-owned)`** or match document’s heading depth conventions) stating indexing is **integrator-owned** and linking to **`audit-indexing.md`** with prose anchor text “Audit table indexing cookbook”. Do **not** paste full DDL tables from the new guide.
2. **`guides/production-checklist.md`:** Add at least **one** durable link (markdown) to **`audit-indexing.md`** in a section operators already read for DB health (e.g. near retention, export, or observability); include the filename string **`audit-indexing.md`** so grep finds it.
</action>

<acceptance_criteria>
- `grep -qF 'audit-indexing.md' guides/domain-reference.md` exits 0.
- `grep -qF 'audit-indexing.md' guides/production-checklist.md` exits 0.
</acceptance_criteria>

---

## Verification

1. `mix format`
2. `mix compile --warnings-as-errors`
3. `mix test` (may still pass before plan 29-02 adds new test file)

---

## Success criteria

- **IDX-01:** Operators have a standalone cookbook with baseline inventory, access-pattern alignment to shipped code, tradeoffs, and navigation from existing guides + HexDocs extras list.
