---
wave: 2
depends_on: [29-01-PLAN.md]
files_modified:
  - test/threadline/audit_indexing_doc_contract_test.exs
autonomous: true
requirements_addressed: [IDX-02]
---

# Plan 29-02: Doc contract for audit indexing guide

**Phase:** 29 — Audit table indexing cookbook  
**Plan:** 29-02 of 02  
**Status:** Ready to execute

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-29-04 | **False confidence:** test passes while guide is empty | medium | Assert marker comment **and** ≥8 distinct spine headings from plan 29-01 **and** cross-link paths. |
| T-29-05 | **Brittle over-matching:** every paragraph literal | low | Follow **29-CONTEXT** D-4 medium strictness — no table-row matrices; headings + marker + link paths only. |

---

## Objective

Add **`test/threadline/audit_indexing_doc_contract_test.exs`** locking **IDX-02** anchors for `guides/audit-indexing.md` so navigation spine and marker cannot drift silently.

---

## must_haves

- [ ] New test module follows **`Threadline.SupportPlaybookDocContractTest`** pattern (`@repo_root`, `read_rel!/1`).
- [ ] Tests fail if **`<!-- IDX-02-AUDIT-INDEXING -->`** or agreed H2/H3 spine strings are removed.
- [ ] Tests assert cross-link targets to **`domain-reference.md`** and **`production-checklist.md`** exist in the indexing guide body (bidirectional doc integrity for D-4).

---

## Task 29-02-01 — Add `audit_indexing_doc_contract_test.exs`

<read_first>
- `test/threadline/support_playbook_doc_contract_test.exs`
- `guides/audit-indexing.md` (post plan 29-01)
- `.planning/milestones/v1.9-phases/29-audit-table-indexing-cookbook/29-CONTEXT.md` (D-4)
</read_first>

<action>
1. Create **`test/threadline/audit_indexing_doc_contract_test.exs`** with `use ExUnit.Case, async: true` and `@moduledoc false`.
2. Copy the private helper **`read_rel!/1`** pattern from **`Threadline.SupportPlaybookDocContractTest`** (same `@repo_root` + `Path.join`).
3. Add **`test "audit-indexing guide retains IDX-02 marker and operator spine"`** that reads **`["guides", "audit-indexing.md"]`** and asserts:
   - `String.contains?(doc, "<!-- IDX-02-AUDIT-INDEXING -->")`
   - For each string in this list (exact): `## Installed defaults`, `### audit_transactions`, `### audit_changes`, `### audit_actions`, `## Timeline and Threadline.Query`, `## Export and Threadline.Export`, `## Correlation filtering`, `## Retention and Threadline.Retention`, `## Tradeoffs and evidence` — `assert String.contains?(doc, heading)` inside a comprehension or loop.
4. Add **`test "audit-indexing guide links to domain-reference and production-checklist"`** asserting `String.contains?(doc, "domain-reference.md")` and `String.contains?(doc, "production-checklist.md")`.
</action>

<acceptance_criteria>
- `mix test test/threadline/audit_indexing_doc_contract_test.exs` exits 0.
- `grep -qF 'defmodule Threadline.AuditIndexingDocContractTest' test/threadline/audit_indexing_doc_contract_test.exs` exits 0.
- `grep -qF 'IDX-02-AUDIT-INDEXING' test/threadline/audit_indexing_doc_contract_test.exs` exits 0.
- `grep -qF '## Timeline and Threadline.Query' test/threadline/audit_indexing_doc_contract_test.exs` exits 0.
</acceptance_criteria>

---

## Verification

1. `mix format`
2. `mix compile --warnings-as-errors`
3. `mix test test/threadline/audit_indexing_doc_contract_test.exs`
4. `mix test`

---

## Success criteria

- **IDX-02:** CI fails if the cookbook marker or operator spine headings are removed or renamed without updating the test.
