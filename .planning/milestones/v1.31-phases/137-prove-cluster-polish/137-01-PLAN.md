---
phase: 137-prove-cluster-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/presentation.ex
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/presentation_test.exs
  - test/threadline/operator_surface/style_contract_test.exs
autonomous: true
requirements: [POLISH-PROVE]

must_haves:
  truths:
    - "Shared Prove helpers derive export readiness and secondary ref display without changing persisted export or proof semantics (D-04/D-18/D-19)."
    - "Reusable `.tl-*` primitives support grouped readiness sections, mono secondary refs, and targetable failed-run rows using the Phase 136 token system only (D-18/D-20/D-21)."
    - "The shared layer supports one status owner, explicit muted placeholders, and dense scan order across the Prove cluster (D-22/D-23/D-24/D-25; F-603/F-605/F-608)."
  artifacts:
    - path: "lib/threadline/operator_surface/presentation.ex"
      provides: "Pure readiness/ref helpers consumed by Exports, Retention, and Evidence"
      contains: "Ready to hand off"
    - path: "test/threadline/operator_surface/presentation_test.exs"
      provides: "Focused helper coverage for readiness buckets, action labels, and ref display"
      contains: "Ready to hand off"
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "Token-backed `.tl-*` group/ref/target helpers for the Prove cluster"
      contains: ".tl-job-group"
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "Contract coverage for the new shared Prove primitives"
      contains: "tl-job-group"
  key_links:
    - from: "lib/threadline/operator_surface/presentation.ex"
      to: "lib/threadline/operator_surface/live/export_status_live.ex"
      via: "shared readiness bucket/rank/action-label helpers"
      pattern: "Presentation\\."
    - from: "lib/threadline/operator_surface/presentation.ex"
      to: "lib/threadline/operator_surface/live/evidence_live.ex"
      via: "shared ref-display helpers"
      pattern: "Presentation\\."
    - from: "lib/threadline/operator_surface/style.ex"
      to: "lib/threadline/operator_surface/live/retention_history_live.ex"
      via: "targetable row hook for failed-run anchors"
      pattern: ":target|tl-target-row"
---

<objective>
Create the shared Prove-cluster presentation layer that Wave 2 plans consume: pure readiness/ref helpers in `Presentation`, plus token-backed `.tl-*` primitives in `Style`.

Purpose: land the common scan-order, ref-treatment, and grouped-readiness seams once so Exports, Retention, and Evidence can polish independently in Wave 2 without re-litigating primitives.
Output: updated `presentation.ex`, updated `style.ex`, and a style contract that locks the new reusable surface.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/137-prove-cluster-polish/137-CONTEXT.md
@.planning/phases/137-prove-cluster-polish/137-UI-SPEC.md
@.planning/phases/137-prove-cluster-polish/137-RESEARCH.md
@.planning/phases/137-prove-cluster-polish/137-VALIDATION.md
@.planning/phases/137-prove-cluster-polish/137-PATTERNS.md
@.planning/phases/136-design-system-hardening/136-CONTEXT.md
@.planning/phases/136-design-system-hardening/136-01-SUMMARY.md
@.planning/milestones/v1.31-UI-AUDIT.md
@.planning/milestones/v1.31-PERSONAS-IA.md
@lib/threadline/operator_surface/presentation.ex
@lib/threadline/operator_surface/style.ex

<interfaces>
From `lib/threadline/operator_surface/presentation.ex` (current):
- `status_modifier/1` maps semantic states to `tl-chip--*`.
- `status_label/1` owns operator-facing status text.
- `truncate_middle/2`, `query_pairs/1`, and `export_summary/1` are the current reusable display helpers.

From `lib/threadline/operator_surface/style.ex` (current):
- `.tl-alert`, `.tl-empty`, `.tl-summary-grid`, `.tl-job`, and `.tl-record-card` are the existing Prove primitives.
- `.tl-button--primary` is accent-only; `.tl-button--danger` is destructive styling; `tl-button--primary tl-button--danger` is the exact combination Retention must stop using per D-10 / F-601.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared readiness and ref helpers in Presentation (D-04/D-18/D-19)</name>
  <read_first>
    - lib/threadline/operator_surface/presentation.ex (the file being modified; reuse its helper style and keep the module pure)
    - lib/threadline/operator_surface/live/export_status_live.ex (current readiness/action logic to extract into helpers)
    - lib/threadline/operator_surface/live/evidence_live.ex (current subject-ref rendering and action order)
    - lib/threadline/operator_surface/live/retention_history_live.ex (current explicit placeholder and time-label usage)
    - .planning/phases/137-prove-cluster-polish/137-CONTEXT.md (D-04 through D-08, D-18 through D-19, D-22 through D-25)
    - .planning/phases/137-prove-cluster-polish/137-UI-SPEC.md (readiness titles/actions, ref treatment, placeholder contract)
    - .planning/phases/137-prove-cluster-polish/137-RESEARCH.md (Recommended Plan 01 and the stream-grouping risk notes)
  </read_first>
  <files>lib/threadline/operator_surface/presentation.ex, test/threadline/operator_surface/presentation_test.exs</files>
  <behavior>
    - Export helpers derive `ready`, `preparing`, `needs_attention`, or `unavailable` from `status`, `file_path`, and `expires_at`; completed+unexpired+file_path is the only ready state (D-04/D-05/D-06/D-07).
    - Ref-display helpers return truncated visible text plus the full value for `title`, so actor refs, query params, and subject refs stay readable at 375px (D-19/D-23; F-603).
    - Helper code remains pure presentation logic: no Repo calls, no LiveView events, no route generation, and no `Threadline.Evidence.Proof` vocabulary changes (D-17/D-19).
  </behavior>
  <action>Extend `Threadline.OperatorSurface.Presentation` with pure helpers for export readiness bucket/rank/group title/action label, ready-vs-unavailable state checks, and secondary ref display metadata. Reuse `status_label/1`, `status_modifier/1`, `truncate_middle/2`, and `human_time/2` instead of duplicating local helpers. Keep the API operator-facing: strings such as `Ready to hand off`, `Preparing`, `Needs attention`, `Unavailable`, `Download export`, `Preparing download`, `Export expired`, and `File unavailable` belong here; route construction, card layout, and per-screen copy still live in the LiveViews. Add `test/threadline/operator_surface/presentation_test.exs` or extend it if it already exists so readiness buckets, readiness rank/order, ready-only action labels, expired/file-missing labels, and secondary ref display full-title/truncated-visible behavior are tested directly before Wave 2 consumes the helpers. Do not add DB access, alter `ExportJob.status`, or widen `Threadline.Evidence.Proof` semantics.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/presentation_test.exs test/threadline/operator_surface/live/export_status_live_test.exs test/threadline/operator_surface/live/evidence_live_test.exs test/threadline/operator_surface/live/retention_history_live_test.exs --max-failures 1 && rg -n "Ready to hand off|Preparing download|Export expired|File unavailable" lib/threadline/operator_surface/presentation.ex test/threadline/operator_surface/presentation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/threadline/operator_surface/presentation.ex` contains pure helper logic for readiness grouping/ranking/action labels derived from `status`, `file_path`, and `expires_at` without repo or route calls (D-04/D-05/D-06/D-19)
    - `lib/threadline/operator_surface/presentation.ex` exposes shared ref-display logic that keeps full values discoverable while truncating the visible actor/subject/query string (D-19/D-23; F-603)
    - `test/threadline/operator_surface/presentation_test.exs` proves ready/preparing/needs-attention/unavailable grouping, ready-first ranking, ready-only `Download export`, expired/file-missing unavailable labels, and full-value ref metadata (D-04/D-05/D-06/D-19)
    - No changes are made to `lib/threadline/evidence/proof.ex`, export persistence semantics, or any LiveView route strings in this task (D-17/D-19)
  </acceptance_criteria>
  <done>Wave 2 plans can consume shared readiness/ref helpers from `Presentation` instead of re-implementing export-state or truncated-ref logic locally.</done>
</task>

<task type="auto">
  <name>Task 2: Add token-backed `.tl-*` helpers and contract coverage for downstream Prove screens (D-18/D-20/D-21/D-25)</name>
  <read_first>
    - lib/threadline/operator_surface/style.ex (the file being modified; extend existing `.tl-*` primitives, do not fork the CSS architecture)
    - test/threadline/operator_surface/style_contract_test.exs (the contract suite to extend)
    - lib/threadline/operator_surface/live/export_status_live.ex (needs grouped readiness wrappers and inset alert treatment)
    - lib/threadline/operator_surface/live/retention_history_live.ex (needs a targetable failed-run row hook)
    - lib/threadline/operator_surface/live/evidence_live.ex (needs secondary mono ref treatment)
    - .planning/phases/137-prove-cluster-polish/137-UI-SPEC.md (Primitive Contract, Color, Typography, and Audit Finding Closure sections)
    - .planning/phases/136-design-system-hardening/136-CONTEXT.md (dark-only, token-backed, status-border rules)
  </read_first>
  <files>lib/threadline/operator_surface/style.ex, test/threadline/operator_surface/style_contract_test.exs</files>
  <action>Extend `Threadline.OperatorSurface.Style` with reusable, token-backed helpers consumed by at least the Wave 2 Prove screens: grouped export section wrappers/headers, a generic mono secondary-ref treatment for actor/subject/query values, and a reusable target-row highlight/scroll-margin hook for retention failed-run anchors. Reuse `.tl-alert`, `.tl-job`, `.tl-record-card`, `.tl-summary-grid`, and the existing `--tl-*` tokens; do not add Prove-only color tokens, Tailwind-like utilities, icons, or a second button/alert system. Update `style_contract_test.exs` so the new `.tl-*` helpers are asserted directly while preserving the existing dark-only and semantic-border checks from Phase 136.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `style.ex` defines only `.tl-*` additions for grouped export sections, secondary ref treatment, and targetable rows, and those additions reuse existing `--tl-*` tokens rather than introducing ad-hoc colors or spacing (D-18/D-20/D-21)
    - `style_contract_test.exs` asserts the new class names and still refutes light mode / `prefers-color-scheme` while keeping the semantic-border contract green (Phase 136 carry-forward)
    - The shared styles preserve one status owner and secondary ref hierarchy rather than adding competing strong accents or duplicate badge systems (D-25; F-605)
  </acceptance_criteria>
  <done>`style.ex` contains the reusable `.tl-*` seams that Wave 2 plans consume, and the style contract locks them to the Phase 136 dark token system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presentation helpers -> operator actions | Pure helper logic decides which export states read as ready, unavailable, or dangerous; incorrect mapping can mislead operator behavior. |
| shared CSS primitives -> live surfaces | Reusable `.tl-*` classes shape status prominence and destructive affordance weight across multiple screens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Tampering | `presentation.ex` readiness helpers | mitigate | Keep helpers pure and operator-facing; downstream LiveView tests in Plans 02-04 must prove only completed+unexpired+file-path exports render ready affordances. |
| T-137-02 | Spoofing | shared ref-display helpers | accept | Full values are already visible to authorized operators on these server-rendered surfaces; the shared helper changes hierarchy, not data scope. |
| T-137-03 | Tampering | `style.ex` shared primitives | mitigate | Constrain additions to `.tl-*` token-backed classes and lock them with `style_contract_test.exs`, preventing a second status/alert/button vocabulary from creeping in. |
| T-137-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this plan. |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/style_contract_test.exs`
- `mix test test/threadline/operator_surface/presentation_test.exs test/threadline/operator_surface/live/export_status_live_test.exs test/threadline/operator_surface/live/evidence_live_test.exs test/threadline/operator_surface/live/retention_history_live_test.exs --max-failures 1`
- `rg -n "Ready to hand off|Preparing download|Export expired|File unavailable|tl-job-group|tl-target-row" lib/threadline/operator_surface/presentation.ex lib/threadline/operator_surface/style.ex`
</verification>

<success_criteria>
- Wave 2 plans can consume shared readiness/ref helpers from `Presentation` and shared `.tl-*` primitives from `Style` without re-implementing those contracts.
- The shared layer stays inside the Phase 136 dark token system and does not introduce backend behavior, routes, or a second CSS architecture.
- Decision coverage for D-04, D-18, D-19, D-20, D-21, D-22, D-23, D-24, and D-25 is explicit in code/test assertions before surface-specific polish begins.
</success_criteria>

<output>
Create `.planning/phases/137-prove-cluster-polish/137-01-SUMMARY.md` when done.
</output>
