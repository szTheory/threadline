---
phase: 137-prove-cluster-polish
plan: 02
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - lib/threadline/operator_surface/live/export_status_live.ex
  - test/threadline/operator_surface/live/export_status_live_test.exs
autonomous: true
requirements: [POLISH-PROVE]

must_haves:
  truths:
    - "Exports derives handoff readiness from `status`, `file_path`, and `expires_at` without changing persisted `ExportJob` state (D-04)."
    - "Jobs render in operator-facing readiness groups with ready first, newest first within each group, and exactly one primary action slot per card (D-05/D-06/D-07/D-24/D-25; F-602/F-604)."
    - "Failure recovery, actor/query ref treatment, and empty/unavailable copy are explicit and secondary to the ready download signal (D-08/D-22/D-23; F-603/F-605/F-607/F-608)."
  artifacts:
    - path: "lib/threadline/operator_surface/live/export_status_live.ex"
      provides: "Readiness-grouped exports monitor with one primary action slot"
      contains: "What's ready to hand off?"
    - path: "test/threadline/operator_surface/live/export_status_live_test.exs"
      provides: "Contract tests for grouped readiness, titles, and recovery actions"
      contains: "Ready to hand off"
  key_links:
    - from: "lib/threadline/operator_surface/live/export_status_live.ex"
      to: "lib/threadline/operator_surface/presentation.ex"
      via: "shared readiness bucket/rank/action-label helpers"
      pattern: "Presentation\\."
    - from: "lib/threadline/operator_surface/live/export_status_live.ex"
      to: "/audit/timeline"
      via: "secondary `Reopen source search` recovery path"
      pattern: "timeline"
---

<objective>
Polish Exports into a clear handoff monitor: ready exports are visually obvious, non-ready exports are explicit about why they are unavailable, and recovery paths stay secondary.

Purpose: close the Exports-owned Prove findings without changing export persistence, background job behavior, or the Phase 140 closed-loop flow.
Output: a readiness-grouped `ExportStatusLive` and a test suite that proves ready-only primary download semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/137-prove-cluster-polish/137-CONTEXT.md
@.planning/phases/137-prove-cluster-polish/137-UI-SPEC.md
@.planning/phases/137-prove-cluster-polish/137-RESEARCH.md
@.planning/phases/137-prove-cluster-polish/137-VALIDATION.md
@.planning/phases/137-prove-cluster-polish/137-PATTERNS.md
@.planning/milestones/v1.31-UI-AUDIT.md
@lib/threadline/operator_surface/live/export_status_live.ex
@lib/threadline/operator_surface/presentation.ex
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/live/export_status_live_test.exs

<interfaces>
Consume the Wave 1 shared layer instead of re-implementing it locally:
- `Presentation` now owns readiness bucket/rank/title/action-label semantics.
- `Presentation` now owns secondary ref display metadata for actor/query values.
- `style.ex` now provides grouped readiness wrappers plus reusable secondary-ref styling.

Current runtime constraints in `ExportStatusLive`:
- actor scope stays `where: j.actor_ref == ^actor_ref`
- refresh is timer-driven (`:refresh`)
- route surface stays `/audit/exports` and `/audit/timeline`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild Exports around readiness groups and a single ready signal (D-04 through D-08)</name>
  <read_first>
    - lib/threadline/operator_surface/live/export_status_live.ex (the file being modified; note the current `stream(:jobs, ...)` flat list, action slot, and actor filtering)
    - test/threadline/operator_surface/live/export_status_live_test.exs (current coverage and fixture patterns)
    - lib/threadline/operator_surface/presentation.ex (consume the Wave 1 readiness/ref helpers)
    - lib/threadline/operator_surface/style.ex (consume the Wave 1 grouped-section and ref primitives)
    - lib/threadline/operator_surface/live/policy_redaction_live.ex (baseline grouped-section/summary-before-detail pattern per D-02)
    - .planning/phases/137-prove-cluster-polish/137-CONTEXT.md (D-04 through D-08, D-22 through D-25)
    - .planning/phases/137-prove-cluster-polish/137-UI-SPEC.md (Exports title, empty/error copy, action-slot contract, actor-ref treatment)
    - .planning/phases/137-prove-cluster-polish/137-RESEARCH.md (Plan 02 and the grouped-list-vs-stream risk note)
  </read_first>
  <files>lib/threadline/operator_surface/live/export_status_live.ex, test/threadline/operator_surface/live/export_status_live_test.exs</files>
  <behavior>
    - Ready group: completed + file_path + future expires_at => `Ready to hand off` section with the only solid `Download export` action (D-05/D-06/D-07; F-602/F-604).
    - Preparing/needs-attention/unavailable groups: pending/running => `Preparing download`; failed => danger owner + inset `.tl-alert--error` + `Reopen source search`; completed but expired or missing file => explicit `Export expired` or `File unavailable` (D-06/D-07/D-08; F-605/F-608).
    - Actor refs and query params stay secondary mono text with truncated visible values and full `title`, so they never dominate the export summary at 375px (D-19/D-23/D-24; F-603).
  </behavior>
  <action>Replace the flat `phx-update="stream"` newest-first rendering in `ExportStatusLive` with readiness-grouped rendering backed by the shared Presentation helpers. If the existing stream shape makes grouped rendering awkward, switch to assigned grouped lists and replace the whole grouped assign on refresh; raw newest-first is not acceptable. Use the exact page title `What's ready to hand off?`, render `Ready to hand off`, `Preparing`, `Needs attention`, and `Unavailable` in readiness order, and keep newest-first ordering inside each group. Reserve `tl-button--primary` for `Download export` only when the job is completed, unexpired, and has a file path. Keep `Reopen source search` secondary and only render it when query params exist. Render failed-job messaging with the shared alert primitive inside the card padding. Do not add routes, change export creation flows, alter actor scoping, or touch backend job semantics.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/export_status_live_test.exs</automated>
    <manual>At 375px, inspect the dense Exports state with ready, preparing, failed, expired, and file-missing jobs. Confirm actor refs/query params stay secondary and truncated, readiness group headings are visible, and only ready downloads read as the primary action.</manual>
  </verify>
  <acceptance_criteria>
    - Rendered Exports HTML contains `What's ready to hand off?`, `No export jobs queued`, and the readiness section headings `Ready to hand off`, `Preparing`, `Needs attention`, and `Unavailable` (D-05/D-22; F-602/F-607)
    - Only completed+unexpired+file_path jobs render `Download export` with the primary button styling; pending/running render `Preparing download`; completed but unavailable jobs render `Export expired` or `File unavailable`; failed jobs render `Reopen source search` plus `.tl-alert--error` content (D-06/D-07/D-08; F-604/F-605/F-608)
    - Actor refs and query values render with truncated visible text plus full `title`, and the source-search affordance stays secondary to the ready download action (D-19/D-23/D-24; F-603)
    - No new routes, new export flows, or raw `ExportJob.status` persistence changes appear in the diff (phase non-goals; D-04)
  </acceptance_criteria>
  <done>`ExportStatusLive` clearly distinguishes ready vs. non-ready jobs, and `export_status_live_test.exs` proves the exact readiness, title, recovery, and truncation contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ExportJob` state -> operator affordance | The UI decides whether a job looks ready to download, still preparing, failed, or unavailable. |
| actor-scoped jobs -> displayed refs and filters | Full actor/query values are shown only within the current actor's authorized export list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-04 | Tampering | `export_status_live.ex` readiness mapping | mitigate | Derive readiness only from `status`, `file_path`, and `expires_at`; add test coverage so non-ready jobs cannot render the primary download affordance. |
| T-137-05 | Spoofing | failed/expired export cards | mitigate | Use explicit labels (`Preparing download`, `Export expired`, `File unavailable`) and the danger alert/recovery path so failed or stale jobs cannot visually spoof ready downloads. |
| T-137-06 | Information Disclosure | actor/query value titles | accept | The surface already scopes jobs to `threadline_actor_ref`; this plan only changes display hierarchy and discoverability of already-authorized values. |
| T-137-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this plan. |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/live/export_status_live_test.exs`
- `rg -n "What's ready to hand off\\?|Ready to hand off|Preparing|Needs attention|Unavailable|Download export|Reopen source search" lib/threadline/operator_surface/live/export_status_live.ex`
- Manual viewport check: inspect `/audit/exports` at 375px in a dense state and confirm refs do not become the visual lead and ready-only primary actions remain obvious.
</verification>

<success_criteria>
- Exports renders as a readiness-first handoff surface rather than a flat background-job list.
- Ready downloads are unmistakable, non-ready states are explicit, and recovery stays secondary.
- Decision coverage for D-04 through D-08 and audit coverage for F-602, F-603, F-604, F-605, F-607, and F-608 is locked in code/tests.
</success_criteria>

<output>
Create `.planning/phases/137-prove-cluster-polish/137-02-SUMMARY.md` when done.
</output>
