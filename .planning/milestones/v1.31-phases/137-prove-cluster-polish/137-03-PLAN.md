---
phase: 137-prove-cluster-polish
plan: 03
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - lib/threadline/operator_surface/live/retention_history_live.ex
  - test/threadline/operator_surface/live/retention_history_live_test.exs
autonomous: true
requirements: [POLISH-PROVE]

must_haves:
  truths:
    - "Retention is context-first: the question-form title, summary metrics, latest-completed context, and warning copy appear before the destructive action on desktop and mobile (D-09/D-13; F-601/F-607)."
    - "The prune trigger remains thin and authorized, uses outline-danger styling with the locked confirmation copy, and the failure metric links to the first failed run when one exists (D-10/D-11/D-12; F-601/F-606)."
    - "Pending/unavailable placeholders are explicit muted labels instead of bare dashes or ambiguous `Not started` gaps (D-22/D-23/D-24; F-608)."
  artifacts:
    - path: "lib/threadline/operator_surface/live/retention_history_live.ex"
      provides: "Context-first retention screen with safe destructive affordance"
      contains: "What was purged, and did it succeed?"
    - path: "test/threadline/operator_surface/live/retention_history_live_test.exs"
      provides: "Contract tests for confirmation copy, ordering, failed-run anchors, and thin runtime behavior"
      contains: "Run retention prune"
  key_links:
    - from: "lib/threadline/operator_surface/live/retention_history_live.ex"
      to: "Threadline.Retention.Pruner.trigger/0"
      via: "unchanged `handle_event(\"prune_now\", ...)` runtime path"
      pattern: "Pruner\\.trigger\\("
    - from: "lib/threadline/operator_surface/live/retention_history_live.ex"
      to: "lib/threadline/operator_surface/style.ex"
      via: "targetable failed-run row hook and outline-danger button primitives"
      pattern: "tl-target-row|tl-button--secondary"
---

<objective>
Make Retention safer and clearer without changing retention semantics: lead with completed-run evidence and failure context, then expose the destructive prune action with the exact confirmation contract.

Purpose: close the Phase 137 retention safety findings while leaving the heavier safety-flow work to Phase 140.
Output: a context-first `RetentionHistoryLive` and test coverage that proves the event path stays thin.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/137-prove-cluster-polish/137-CONTEXT.md
@.planning/phases/137-prove-cluster-polish/137-UI-SPEC.md
@.planning/phases/137-prove-cluster-polish/137-RESEARCH.md
@.planning/phases/137-prove-cluster-polish/137-VALIDATION.md
@.planning/phases/137-prove-cluster-polish/137-PATTERNS.md
@.planning/milestones/v1.31-UI-AUDIT.md
@lib/threadline/operator_surface/live/retention_history_live.ex
@lib/threadline/operator_surface/presentation.ex
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/live/retention_history_live_test.exs

<interfaces>
Consume the Wave 1 shared layer instead of inventing a retention-only primitive:
- shared secondary-ref/placeholder styling lives in `style.ex`
- targetable row styling for failed-run anchors lives in `style.ex`
- `Presentation.human_time/2` and related helpers remain the canonical time-label path

Current runtime contract in `RetentionHistoryLive`:
- `handle_event("prune_now", ...)` must keep the policy-enabled check and call `Pruner.trigger/0`
- refresh remains timer-driven (`:refresh`)
- data source stays `RetentionRun` ordered by `started_at`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorder Retention around latest-completed context and safe prune affordance (D-09 through D-13)</name>
  <read_first>
    - lib/threadline/operator_surface/live/retention_history_live.ex (the file being modified; note current title, alert order, placeholder text, and `prune_now` event)
    - test/threadline/operator_surface/live/retention_history_live_test.exs (current runtime-path and empty-state assertions)
    - lib/threadline/operator_surface/presentation.ex (shared time/placeholder helpers)
    - lib/threadline/operator_surface/style.ex (shared button, summary-grid, and target-row primitives from Wave 1)
    - lib/threadline/operator_surface/live/policy_redaction_live.ex (cluster baseline for summary-before-detail)
    - .planning/phases/137-prove-cluster-polish/137-CONTEXT.md (D-09 through D-13, D-22 through D-25)
    - .planning/phases/137-prove-cluster-polish/137-UI-SPEC.md (Retention title, empty/error copy, destructive button contract)
    - .planning/phases/137-prove-cluster-polish/137-RESEARCH.md (Plan 03 and the latest-run vs latest-completed risk note)
  </read_first>
  <files>lib/threadline/operator_surface/live/retention_history_live.ex, test/threadline/operator_surface/live/retention_history_live_test.exs</files>
  <behavior>
    - The page leads with `What was purged, and did it succeed?`, then summary metrics, latest completed/latest status context, and warning copy before the prune button (D-09/D-13; F-601/F-607).
    - `Run retention prune` is `tl-button--secondary tl-button--danger` with the exact confirmation copy; `handle_event("prune_now", ...)` still re-checks access and calls `Pruner.trigger/0` with no modal, typed confirm, or new backend behavior (D-10/D-11; F-601).
    - Failure count appears once as the metric owner and deep-links to the first failed run when present; deleted-count/duration/date placeholders use explicit muted labels such as `Pending`, `Not started`, `No duration yet`, or `No rows deleted` (D-12/D-23; F-606/F-608).
  </behavior>
  <action>Refactor `RetentionHistoryLive` so the header/action stack becomes context-first: use the exact title `What was purged, and did it succeed?`, move the prune action below summary/latest-completed context on both mobile and desktop, and demote the button to `tl-button--secondary tl-button--danger` with the exact `data-confirm` copy from the UI-SPEC. Update the summary model so it computes latest run and latest completed run separately; a queued or failed newest row must not stand in for successful last completion. Render the failure metric as a single owner that links to the first failed row via a native fragment anchor and the shared target-row hook. Replace `-` and ambiguous nil output with explicit muted labels. Keep the `Pruner.trigger/0` event path thin, keep timer refresh behavior, and do not add URL-backed safety flows, JS modals, or retention backend semantics.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/retention_history_live_test.exs && rg -n "Pruner\\.trigger\\(|What was purged, and did it succeed\\?|Run retention prune|Confirm retention prune" lib/threadline/operator_surface/live/retention_history_live.ex</automated>
    <manual>At 375px, inspect Retention with completed, queued, and failed runs. Confirm summary/latest-completed/failure context visually precedes `Run retention prune`, the failure-count anchor target is discoverable, and status chips remain max-content rather than stretched full-width.</manual>
  </verify>
  <acceptance_criteria>
    - `RetentionHistoryLive` renders `What was purged, and did it succeed?`, `No retention runs yet`, and the runtime-recovery copy mentioning `mix threadline.retention.purge --dry-run` (D-09/D-22; F-607)
    - The prune button uses outline-danger styling, contains the exact confirmation sentence `Confirm retention prune. This permanently deletes older audit records; review the latest completed run and failure count first.`, and no longer uses `tl-button--primary tl-button--danger` (D-10; F-601)
    - Summary/latest-completed context precedes the destructive action in the rendered source order, `Pruner.trigger/0` remains the event target, and the newest failed/queued row does not replace the latest completed summary value (D-09/D-11/D-13)
    - Failure count links to the first failed row when one exists, and placeholder values render explicit muted labels rather than bare `-` output (D-12/D-23; F-606/F-608)
  </acceptance_criteria>
  <done>`RetentionHistoryLive` is safer and clearer without backend creep, and `retention_history_live_test.exs` proves the ordering, copy, anchor, and event-path contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| destructive UI -> retention runtime | The operator-facing control can trigger a permanent prune; visual weight and context must not encourage accidental activation. |
| retention summary -> operator trust | The UI chooses which run to summarize; conflating latest queued/failed with latest completed can mislead operators about purge health. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-07 | Tampering | `retention_history_live.ex` prune affordance | mitigate | Demote the destructive action to outline-danger with the exact confirmation copy and visible latest-completed/failure context before the control. |
| T-137-08 | Repudiation | latest-run summary | mitigate | Summarize latest completed separately from latest run so a queued/failed newest row cannot falsely imply recent successful pruning. |
| T-137-09 | Elevation of Privilege | `prune_now` event | mitigate | Preserve the existing policy-enabled gate and `Pruner.trigger/0` runtime path; this plan does not weaken authorization or add side channels. |
| T-137-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this plan. |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/live/retention_history_live_test.exs`
- `rg -n "What was purged, and did it succeed\\?|Run retention prune|Confirm retention prune|Pruner\\.trigger\\(" lib/threadline/operator_surface/live/retention_history_live.ex`
- Manual viewport check: inspect `/audit/policy/retention` at 375px and confirm latest-completed/failure context appears before the destructive prune action.
</verification>

<success_criteria>
- Retention presents proof of the last successful purge before the destructive control.
- The prune trigger keeps the existing backend/authorization path while gaining clearer copy, ordering, and failure navigation.
- Decision coverage for D-09 through D-13 and audit coverage for F-601, F-606, F-607, and F-608 is explicit in code/tests.
</success_criteria>

<output>
Create `.planning/phases/137-prove-cluster-polish/137-03-SUMMARY.md` when done.
</output>
