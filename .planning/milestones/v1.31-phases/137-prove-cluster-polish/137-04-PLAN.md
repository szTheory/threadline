---
phase: 137-prove-cluster-polish
plan: 04
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - lib/threadline/operator_surface/live/evidence_live.ex
  - lib/threadline/operator_surface/live/policy_redaction_live.ex
  - test/threadline/operator_surface/live/evidence_live_test.exs
  - test/threadline/operator_surface/live/policy_redaction_live_test.exs
autonomous: true
requirements: [POLISH-PROVE]

must_haves:
  truths:
    - "Evidence cards are status-led: verdict owner first, subject label next, secondary outcome/status after that, then muted mono subject ref, recorded time, and actions (D-14/D-15/D-16; F-503)."
    - "Failed export evidence is labeled as evidence-of-failure through presentation logic only; `Threadline.Evidence.Proof` semantics do not widen or change (D-17; F-506)."
    - "Redaction remains the baseline Prove pattern: one grouped assurance surface with details disclosure, remediation links, and assurance framing aligned to the Phase 136 primitives (D-01/D-02/D-03/D-18/D-20/D-21)."
  artifacts:
    - path: "lib/threadline/operator_surface/live/evidence_live.ex"
      provides: "Status-led evidence cards with presentation-only failed-export labeling"
      contains: "Open proof history"
    - path: "lib/threadline/operator_surface/live/policy_redaction_live.ex"
      provides: "Assurance-framed Redaction baseline with grouped sections intact"
      contains: "Redaction assurance"
    - path: "test/threadline/operator_surface/live/evidence_live_test.exs"
      provides: "Evidence hierarchy and failed-export label tests"
      contains: "Failed export evidence"
    - path: "test/threadline/operator_surface/live/policy_redaction_live_test.exs"
      provides: "Redaction title/section/remediation contract tests"
      contains: "Redaction assurance"
  key_links:
    - from: "lib/threadline/operator_surface/live/evidence_live.ex"
      to: "lib/threadline/evidence/proof.ex"
      via: "consume `Proof.present_record/1` output without modifying proof semantics"
      pattern: "Proof\\.present_record"
    - from: "lib/threadline/operator_surface/live/policy_redaction_live.ex"
      to: "Threadline.Policy.RedactionPresenter"
      via: "preserve grouped section order and remediation rows"
      pattern: "RedactionPresenter\\.build"
---

<objective>
Align Evidence and Redaction to the shared Prove-cluster narrative: Evidence leads with proof verdicts and a clear history action, while Redaction remains the grouped assurance baseline the rest of the cluster borrows from.

Purpose: close the Evidence-owned audit findings and keep Redaction stable as the cluster's reference pattern without widening backend proof or policy semantics.
Output: polished `EvidenceLive` and `PolicyRedactionLive` plus tests that lock their hierarchy and copy.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/137-prove-cluster-polish/137-CONTEXT.md
@.planning/phases/137-prove-cluster-polish/137-UI-SPEC.md
@.planning/phases/137-prove-cluster-polish/137-RESEARCH.md
@.planning/phases/137-prove-cluster-polish/137-VALIDATION.md
@.planning/phases/137-prove-cluster-polish/137-PATTERNS.md
@.planning/milestones/v1.31-UI-AUDIT.md
@lib/threadline/operator_surface/live/evidence_live.ex
@lib/threadline/operator_surface/live/policy_redaction_live.ex
@lib/threadline/operator_surface/presentation.ex
@lib/threadline/operator_surface/style.ex
@lib/threadline/evidence/proof.ex
@test/threadline/operator_surface/live/evidence_live_test.exs
@test/threadline/operator_surface/live/policy_redaction_live_test.exs

<interfaces>
Evidence inputs:
- `Proof.present_record/1` returns `subject`, `subject_ref`, `summary_status`, `recorded_at`, `verdict_status`, `verdict_kind`, and `verdict_reason`.
- `EvidenceLive.build_row/1` currently collapses that into the rendered card assigns; this is the correct presentation seam for F-506.

Redaction baseline:
- `RedactionPresenter.build/1` remains the only report source.
- `PolicyRedactionLive` already owns grouped sections, `details` disclosure, and remediation links; preserve that structure and only align copy/primitive usage.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorder Evidence around proof verdicts and preserve Redaction as the assurance baseline (D-01 through D-03, D-14 through D-17)</name>
  <read_first>
    - lib/threadline/operator_surface/live/evidence_live.ex (the file being modified; note current title, card order, subject-ref rendering, and support action order)
    - lib/threadline/operator_surface/live/policy_redaction_live.ex (the file being modified; preserve grouped sections/details/remediation links)
    - test/threadline/operator_surface/live/evidence_live_test.exs (current Evidence coverage and insert helper)
    - test/threadline/operator_surface/live/policy_redaction_live_test.exs (current Redaction section-order coverage)
    - lib/threadline/evidence/proof.ex (consume `present_record/1`; do not widen or edit proof semantics)
    - lib/threadline/operator_surface/presentation.ex (shared status/ref helpers from Wave 1)
    - lib/threadline/operator_surface/style.ex (shared card/ref primitives from Wave 1)
    - .planning/phases/137-prove-cluster-polish/137-CONTEXT.md (D-01 through D-03, D-14 through D-17, D-22 through D-25)
    - .planning/phases/137-prove-cluster-polish/137-UI-SPEC.md (Evidence/Redaction title, hierarchy, and copy contracts)
  </read_first>
  <files>lib/threadline/operator_surface/live/evidence_live.ex, lib/threadline/operator_surface/live/policy_redaction_live.ex, test/threadline/operator_surface/live/evidence_live_test.exs, test/threadline/operator_surface/live/policy_redaction_live_test.exs</files>
  <behavior>
    - Evidence card order is verdict/status owner -> subject label -> secondary outcome/status -> muted mono subject ref -> recorded time -> actions, with `Open proof history` before any support navigation (D-14/D-15/D-16; F-503).
    - `export_delivery` failure records render a presentation-only label such as `Failed export evidence` or `Evidence of failed export`, not a generic `Unsupported`, and `lib/threadline/evidence/proof.ex` remains unchanged (D-17; F-506).
    - Redaction keeps the grouped assurance structure, `details` disclosure, and remediation links; only align title/copy/primitives to the locked assurance framing (`Redaction assurance`) and diagnostic tone (D-01/D-02/D-03; F-607).
  </behavior>
  <action>Refactor `EvidenceLive` so each card is status-led and operator-scannable: keep `What can Threadline prove right now?`, move the verdict owner to the front, surface the subject label before raw subject-ref JSON, render subject refs with the shared secondary mono treatment, and ensure `Open proof history` is the first action on each card. For failed export proof rows, add presentation-only display-label logic inside `EvidenceLive`/its row builder so `export_delivery` failures read as failed-export evidence without editing `Threadline.Evidence.Proof`. In `PolicyRedactionLive`, keep the section order, `details` disclosure, and remediation links intact, but align the page title/copy to assurance framing (`Redaction assurance`) and make the no-drift success state/empty guidance explicitly diagnostic rather than generic. Do not change `RedactionPresenter`, proof persistence, routes, or policy semantics.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/evidence_live_test.exs test/threadline/operator_surface/live/policy_redaction_live_test.exs && rg -n "What can Threadline prove right now\\?|Open proof history|Redaction assurance" lib/threadline/operator_surface/live/evidence_live.ex lib/threadline/operator_surface/live/policy_redaction_live.ex</automated>
    <manual>At 375px, inspect dense Evidence and Redaction states. Confirm Evidence verdict/subject lead before subject refs, `Open proof history` remains the first action, and Redaction details/status chips remain readable without orphaned full-width badges.</manual>
  </verify>
  <acceptance_criteria>
    - Evidence still renders `What can Threadline prove right now?`, but card markup/order makes the verdict owner and subject lead while subject refs move to secondary mono detail with full-value discoverability (D-14/D-15/D-24; F-503/F-603)
    - `Open proof history` renders before support navigation such as `Open exports`, `Review retention`, or `Check redaction` (D-16; F-503)
    - Failed `export_delivery` evidence renders a failure-specific label and the diff shows no change to `lib/threadline/evidence/proof.ex` (D-17; F-506)
    - Redaction renders the assurance-framed title `Redaction assurance`, keeps grouped section order/details/remediation links, and avoids generic empty/error copy (D-01/D-02/D-03/D-22/D-23; F-607)
  </acceptance_criteria>
  <done>Evidence and Redaction read as one coherent Prove workflow: verdict-first evidence cards plus a stable assurance baseline, all locked by LiveView tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof document -> evidence card label | Presentation decides whether a proof row reads as proven, inferred, unsupported, or evidence-of-failure. |
| redaction report -> assurance UI | The UI must not hide or dilute drift/introspection risk while polishing the grouped surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-10 | Tampering | `evidence_live.ex` display labels | mitigate | Keep F-506 handling inside `EvidenceLive` presentation only and prove via tests that `proof.ex` is untouched while failed export evidence stops rendering as generic `Unsupported`. |
| T-137-11 | Information Disclosure | subject-ref display | accept | Subject refs are already rendered for authorized operators; this plan changes hierarchy/truncation, not scope or routing. |
| T-137-12 | Tampering | `policy_redaction_live.ex` grouped assurance surface | mitigate | Preserve `RedactionPresenter` as the only data source, keep section order/details/remediation links under test, and limit edits to copy/primitive alignment. |
| T-137-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this plan. |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/live/evidence_live_test.exs test/threadline/operator_surface/live/policy_redaction_live_test.exs`
- `rg -n "Open proof history|Failed export evidence|Evidence of failed export|Redaction assurance" lib/threadline/operator_surface/live/evidence_live.ex lib/threadline/operator_surface/live/policy_redaction_live.ex`
- Manual viewport check: inspect `/audit/evidence` and `/audit/policy/redaction` at 375px in dense states and confirm proof/status owners remain first-scan elements.
</verification>

<success_criteria>
- Evidence cards lead with proof status and history navigation instead of raw payload weight.
- Failed export evidence is distinguished in presentation only, without backend proof-vocabulary creep.
- Redaction remains the grouped assurance baseline, now aligned to the locked assurance framing and diagnostic copy.
</success_criteria>

<output>
Create `.planning/phases/137-prove-cluster-polish/137-04-SUMMARY.md` when done.
</output>
