---
phase: 135-seed-enrichment-ia-lock-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
  - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
autonomous: true
requirements: [POLISH-SEED]

must_haves:
  truths:
    - "Support.set_actor_guc!/2 accepts a kind arg defaulting to :user; existing /1 callers compile unchanged"
    - "Support.set_anonymous_actor_guc!/0 sets an :anonymous actor GUC with no id"
    - "Support.audit_context/2 accepts a :kind keyword option defaulting to :user"
    - "Persona/setup org+membership+agent rows carry a real :admin actor_ref (not null) and are backdated far outside the default 24h window"
    - "Named non-human actor literals exist in Manifest (:zendesk_sync, :oban_retention_purge, :trigger_backfill)"
  artifacts:
    - path: "examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex"
      provides: "Actor-kind-generalized GUC + audit_context helpers (D-07)"
      contains: "def set_actor_guc!(actor_id, kind"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex"
      provides: "D-05 fix: persona/setup rows get :admin actor + backdated timestamp"
      contains: "Support.set_actor_guc!"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex"
      provides: "Named actor literals + accessor (D-06)"
      contains: "def actor_id("
  key_links:
    - from: "personas.ex seed_memberships/1"
      to: "Support.set_actor_guc!/2 + Support.put_timestamp/3"
      via: "Repo.transaction wrapper per org with :admin kind"
      pattern: "Support\\.set_actor_guc!\\([^,]+, :admin\\)"
    - from: "demo_contract_test.exs"
      to: "audit_transactions.actor_ref for org_memberships rows"
      via: "query asserting non-null actor_ref on setup tx"
      pattern: "actor_ref"
---

<objective>
Generalize the demo seed's actor mechanism to all ActorRef kinds (D-07) and fix the D-05 root cause: `Personas.run` currently inserts trigger-audited org/membership/agent rows with NO actor GUC and NO backdate, so they land null-actor at wall-clock `now()` and bury the actor-populated ticket rows at the top of the default 24h Timeline window. This plan is the prerequisite mechanism layer that Plan 03's non-human actor clusters depend on.

Purpose: "Actor unknown everywhere" (F-202) is an orchestration bug, not a broken GUC path. Fix it at the source and unlock multi-kind actors for the variety pack.
Output: `Support` helpers accept a kind; `Personas` setup rows carry an `:admin` actor and a backdated timestamp; `Manifest` exposes named non-human actor literals; a `demo_contract_test.exs` assertion proves setup rows are no longer null-actor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-seed-enrichment-ia-lock-in/135-CONTEXT.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-RESEARCH.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Verified against live source. Executor should use these directly. -->

From lib/threadline/semantics/actor_ref.ex (DO NOT MODIFY — library code):
  @types ~w(user admin service_account job system anonymous)a   # exactly 6 kinds
  ActorRef.new(kind, id) -> {:ok, %ActorRef{}} | {:error, :unknown_actor_type}
  ActorRef.new(:anonymous) -> {:ok, %ActorRef{}}   # no id form
  ActorRef.to_map/1 -> map for Jason.encode!

From examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex (CURRENT — to be generalized):
  set_actor_guc!(user_id) when is_binary(user_id)   # hardcodes ActorRef.new(:user, user_id) at line 57-66
  audit_context(user_id, opts \\ []) when is_binary(user_id)   # hardcodes :user at line 19-27
  put_timestamp(ctx, transaction_id, %DateTime{})   # registers tx for Temporal backdate
  current_audit_transaction_id!()   # returns tx id for txid_current()
  stamp_org_meta!(%Organization{})

From examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex:
  Manifest.epoch() -> ~U[2026-05-27 12:00:00Z]   # frozen demo_epoch
  Manifest.user_id(:admin) -> deterministic UUID for admin@example.com
  existing constant+accessor idiom at lines 62-73, 113-150

From examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex (D-05 root-cause site):
  seed_memberships/1 (lines 96-124) calls ensure_membership!/2 + ensure_agent!/2
  ensure_membership!/2 (lines 126-136) — bare Repo.insert!, NO actor, NO transaction, NO backdate
  ensure_agent!/2 (lines 138-148) — same
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize Support actor helpers to all ActorRef kinds (D-07)</name>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex (the file being modified — read `set_actor_guc!/1` lines 57-66 and `audit_context/2` lines 19-27)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md (§ support.ex EXTEND — the exact generalized shapes for both functions and the new set_anonymous_actor_guc!/0)
    - lib/threadline/semantics/actor_ref.ex (the 6 @types and new/1, new/2 contracts — DO NOT modify this file)
  </read_first>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex</files>
  <action>
    Change `set_actor_guc!/1` to `set_actor_guc!(actor_id, kind \\ :user)` with guard `when is_binary(actor_id) and kind in [:user, :admin, :service_account, :job, :system]`, building the ref via `ActorRef.new(kind, actor_id)` instead of the hardcoded `ActorRef.new(:user, user_id)`. Keep the JSON-encode + `SELECT set_config('threadline.actor_ref', $1::text, true)` body byte-for-byte. Add a sibling `set_anonymous_actor_guc!/0` that builds `ActorRef.new(:anonymous)` (no id) and runs the identical set_config query — `:anonymous` cannot go through the `/2` path because it takes no id. Change `audit_context/2` to read `kind = Keyword.get(opts, :kind, :user)` and call `ActorRef.new(kind, actor_id)` (rename the positional `user_id` param to `actor_id`, keep the `is_binary` guard and the existing `correlation_id`/`request_id` opt handling). All existing single-arg callers in anchors.ex/filler.ex/personas.ex continue to compile via the `:user` default — do NOT change those call sites in this task.
    Do NOT introduce a `:integration` kind (D-08 — ActorRef validates only the 6 kinds).
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd examples/threadline_phoenix && mix compile --warnings-as-errors` exits 0 (no arity-break warnings in anchors.ex/filler.ex/personas.ex)
    - `grep -c "def set_actor_guc!(actor_id, kind" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex` returns 1
    - `grep -c "def set_anonymous_actor_guc!" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex` returns 1
    - `grep -c ":integration" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex` returns 0
  </acceptance_criteria>
  <done>Support.set_actor_guc!/2 (kind default :user), set_anonymous_actor_guc!/0, and audit_context/2 (:kind opt) exist; all existing 1-arg callers compile unchanged; no :integration kind introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Add named non-human actor literals to Manifest (D-06)</name>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex (the file being modified — the constant + accessor idiom at lines 62-73 and 113-150)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md (§ manifest.ex EXTEND — the module-attribute + actor_id/1 accessor pattern and the suggested literal strings)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-CONTEXT.md (D-06 table + Claude's Discretion paragraph finalizing actor-id literal strings)
  </read_first>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex</files>
  <action>
    Add three module attributes and an `actor_id/1` accessor following the existing constant+accessor idiom: `@actor_zendesk_sync "zendesk-sync"`, `@actor_oban_retention_purge "oban-retention-purge"`, `@actor_trigger_backfill "trigger-backfill"`. Add `def actor_id(:zendesk_sync), do: @actor_zendesk_sync`, `def actor_id(:oban_retention_purge), do: @actor_oban_retention_purge`, `def actor_id(:trigger_backfill), do: @actor_trigger_backfill` with a `@doc` line matching the file's documentation style. These literal strings are the bare actor IDs — the kind (`:service_account` / `:job` / `:system`) is supplied at the `Support.set_actor_guc!(Manifest.actor_id(:zendesk_sync), :service_account)` call site in Plan 03, not stored here. Do NOT seed any rows in this task — Manifest is pure SSOT constants.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix compile --warnings-as-errors && mix test test/threadline_phoenix/demo_manifest_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def actor_id(" examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex` returns 3
    - `mix test test/threadline_phoenix/demo_manifest_test.exs` (from examples/threadline_phoenix) exits 0
    - The three literal strings "zendesk-sync", "oban-retention-purge", "trigger-backfill" are each present (`grep -F` each returns ≥1)
  </acceptance_criteria>
  <done>Manifest exposes actor_id/1 for :zendesk_sync, :oban_retention_purge, :trigger_backfill returning the named literals; compiles; existing demo_manifest_test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix D-05 — give persona/setup rows a real :admin actor + backdate, with assertion</name>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex (the file being modified — seed_memberships/1 lines 96-124, ensure_membership!/2 lines 126-136, ensure_agent!/2 lines 138-148)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex (analog — seed_leaving_agent_window lines 131-158 shows the canonical Repo.transaction + set_actor_guc! + current_audit_transaction_id! + put_timestamp four-step idiom)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md (§ personas.ex EXTEND — D-05 fix shape for seed_memberships, the "ONE Repo.transaction per org with :admin kind" pattern, and the "backdate far outside 24h window" constraint; § Pitfall 2)
    - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (the file being extended — setup/unboxed_run pattern lines 1-20, the AuditChange+AuditTransaction join query idiom, @moduletag :demo_contract)
  </read_first>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex, examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs</files>
  <behavior>
    - After seed, every `org_memberships` and `agents` audit_change produced by persona setup has a non-null `actor_ref` on its transaction (the :admin actor)
    - Those setup transactions have `occurred_at` at least ~14 days before `Manifest.epoch()` — i.e. NOT in the default 24h window (so they no longer bury actor-populated ticket rows above the fold)
  </behavior>
  <action>
    Rewrite `seed_memberships/1` so each org's membership+agent inserts run inside ONE `Repo.transaction` that first calls `Support.set_actor_guc!(admin_id, :admin)` (admin_id = `to_string(Map.fetch!(ctx.users, :admin).id)`), performs the existing `ensure_membership!`/`ensure_agent!` work for the admin AND the org's persona keys, then calls `Support.current_audit_transaction_id!()` to capture the tx id; outside the transaction call `Support.put_timestamp(acc, tx_id, setup_ts)` where `setup_ts = DateTime.add(Manifest.epoch(), -21, :day)` (far outside the 24h window per D-05/Pitfall 2). Convert `seed_memberships/1` from `Enum.each` to an `Enum.reduce` over the orgs that threads and returns the updated `ctx` (so Temporal.run sees the new `ctx.timestamps` entries). `ensure_membership!/2` and `ensure_agent!/2` stay idempotent (existing get_by guards) — they now run inside the actored transaction. Do NOT use `DateTime.utc_now()` for setup rows (that would re-create the above-the-fold symptom). `tickets`/`org_memberships`/`agents` use the default trigger (changed_from NULL) — this is honest after-only data, do NOT fake before values (D-09).
    Add a `describe "D-05 persona setup actor attribution"` block to demo_contract_test.exs (NEW test in the same plan so it lands green together) inside `Ecto.Adapters.SQL.Sandbox.unboxed_run(Repo, fn -> ... end)`: assert that the count of `org_memberships` AuditChanges joined to a transaction whose `actor_ref` IS NOT NULL is ≥ 1; and assert no `org_memberships` setup change falls inside `DateTime.utc_now() |> DateTime.add(-24, :hour)` window (occurred_at strictly older). Use the `from(ac in AuditChange, join: at in assoc(ac, :transaction), ...)` join idiom and `is_nil(at.actor_ref) == false` / `not is_nil(at.actor_ref)`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs` exits 0 (the new D-05 describe block passes)
    - `grep -c "Support.set_actor_guc!(admin_id, :admin)" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex` returns ≥ 1
    - `grep -c "DateTime.add(Manifest.epoch(), -21, :day)" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex` returns ≥ 1
    - `grep "DateTime.utc_now" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex` returns no matches (setup rows are epoch-backdated, never wall-clock)
    - Post-seed query: ≥1 `org_memberships` AuditChange has a non-null `transaction.actor_ref`
  </acceptance_criteria>
  <done>Persona/setup org+membership+agent rows carry the :admin actor_ref and are backdated 21 days before epoch (outside the 24h window); the demo_contract_test D-05 block proves both; seed remains deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed code → audit triggers | Demo seed sets the `threadline.actor_ref` GUC; triggers persist it into `audit_transactions.actor_ref`. All values are deterministic demo fiction, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Information Disclosure | Manifest actor literals / personas seed | accept | No real PII or secrets — actor ids are synthetic strings ("zendesk-sync") and the only credential is the existing `Manifest.demo_seed_password()` / example.com fixtures. No new secret introduced. |
| T-135-02 | Spoofing | actor_ref GUC kind generalization | accept | `set_actor_guc!/2` is guarded to the 6 valid kinds via ActorRef.new validation; demo-only code path (Reset.assert_dev_or_allowed!), never runs in production. No auth/authz decision depends on these values. |
| T-135-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this phase (pure Elixir source edits against existing deps). No new dependency added. |

No high or medium threats apply: this plan adds deterministic dev/demo seed code and one test, with no new runtime code paths, routes, auth logic, or production attack surface.
</threat_model>

<verification>
- `cd examples/threadline_phoenix && mix verify.format` (per-commit gate)
- `cd examples/threadline_phoenix && mix compile --warnings-as-errors`
- `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs test/threadline_phoenix/demo_manifest_test.exs`
</verification>

<success_criteria>
- Support helpers accept all ActorRef kinds with `:user` default; no existing caller breaks (criterion: F-202 mechanism ready).
- Persona/setup rows are actor-attributed and backdated outside the 24h window (D-05 fixed; criterion #1 — Actor screen no longer "unknown everywhere").
- Manifest exposes the three named non-human actor literals (D-06 prerequisite for Plan 03 clusters).
- Determinism preserved: PRNG seed, UUID v5 namespace, frozen epoch untouched.
</success_criteria>

<output>
Create `.planning/phases/135-seed-enrichment-ia-lock-in/135-01-SUMMARY.md` when done.
</output>
