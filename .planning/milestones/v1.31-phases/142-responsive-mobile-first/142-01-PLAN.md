---
phase: 142-responsive-mobile-first
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/style_contract_test.exs
autonomous: true
requirements: [POLISH-RESPONSIVE]
must_haves:
  truths:
    - "The operator surface has a named breakpoint scale tied to the 375 / 768 / 1280 acceptance matrix."
    - "Media query literals are standards-compliant but source-governed by tests, with no remaining ad-hoc 481px or 721px responsive layers."
    - "Future responsive CSS changes have a source-contract failure mode instead of relying on visual inspection alone."
  artifacts:
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "Named responsive scale and standards-compliant media-query layers"
      contains: "--tl-breakpoint-tablet: 768px;"
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "Breakpoint token and media literal source contracts"
      contains: "phase 142 breakpoint"
  key_links:
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "File.read! source assertions"
      pattern: "@media \\(min-width: (768|1280)px\\)"
    - from: "lib/threadline/operator_surface/style.ex"
      to: ".planning/phases/142-responsive-mobile-first/142-UI-SPEC.md"
      via: "phone/tablet/desktop token names and acceptance widths"
      pattern: "--tl-breakpoint"
---

<objective>
Create the responsive foundation for Phase 142: named breakpoint tokens in the operator stylesheet and source-contract tests that lock the accepted 375 / 768 / 1280 scale to standards-compliant media query literals.

Purpose: CSS custom properties cannot be used inside standard media queries, so this plan makes breakpoint governance explicit in source and tests before layout remediation begins.
Output: breakpoint token declarations in `style.ex` and passing ExUnit contracts in `style_contract_test.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/142-responsive-mobile-first/142-CONTEXT.md
@.planning/phases/142-responsive-mobile-first/142-RESEARCH.md
@.planning/phases/142-responsive-mobile-first/142-PATTERNS.md
@.planning/phases/142-responsive-mobile-first/142-UI-SPEC.md
@.planning/phases/141-motion-micro-animation/141-03-SUMMARY.md
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/style_contract_test.exs

<interfaces>
Existing source-contract pattern:
- `test/threadline/operator_surface/style_contract_test.exs` uses `@style_path "lib/threadline/operator_surface/style.ex"` and `File.read!(@style_path)` in async ExUnit tests.
- Existing tests use `String.contains?/2`, scoped `String.split/2`, selector loops, `Regex.scan/3`, and `Regex.match?/2`; stay in that style.

Current responsive anchors in `style.ex`:
- Sizing tokens live in `.threadline-ui` near `--tl-table-min-width: 720px;`, `--tl-drawer-width: 760px;`, and `--tl-viewport-max-height: 600px;`.
- Current comments say `Tablet (min-width: 481px)` and `desktop (min-width: 721px)`.
- Current media layers are `@media (min-width: 481px)` and `@media (min-width: 721px)`.
- CSS custom properties are allowed as documentation/source tokens, but `var(--tl-breakpoint-*)` must not appear inside `@media`.

Phase 142 locked decisions:
- D-01: acceptance viewports are 375 / 768 / 1280.
- D-02: preserve the current operator-console visual language and IA.
- D-03: tokenize breakpoints in source while keeping `@media` standards-compliant; use tests to tie literals to tokens.
- D-09: do not build Phase 143 screenshot-diff or broad accessibility infrastructure.
</interfaces>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-142 | Tokenize breakpoints, mobile-first layouts for tables/filters/drawers/nav, every screen correct at 375/768/1280, no horizontal-scroll regressions | 142-01, 142-02, 142-03 | COVERED | Plan 01 creates the breakpoint contract; Plan 02 aligns shared CSS primitives; Plan 03 proves the route matrix in a browser.
REQ | POLISH-RESPONSIVE | Every operator-surface screen usable and correct at 375 / 768 / 1280; breakpoint scale tokenized; tables, filters, drawers, nav mobile-first; no horizontal-scroll regressions | 142-01, 142-02, 142-03 | COVERED | Requirement appears in all plans because each plan closes a distinct part of the same requirement.
RESEARCH | breakpoint-source-contract | Define readable breakpoint tokens and lock literal media queries with ExUnit because custom properties cannot be used in media queries | 142-01 | COVERED | Tasks 1-2 implement this.
RESEARCH | shared-responsive-primitives | Use shared `.tl-table--responsive`, `.tl-toolbar__form`, `.tl-subview`, and `.tl-topbar__nav` patterns rather than bespoke per-screen overflow | 142-02 | COVERED | Plan 02 owns source contracts and CSS alignment.
RESEARCH | browser-matrix-overflow-proof | Add one Playwright matrix spec over the operator routes and assert root overflow <= 1 at 375/768/1280 | 142-03 | COVERED | Plan 03 owns this.
RESEARCH | no-new-packages | Use existing ExUnit and Playwright stack; no new dependencies | 142-01, 142-02, 142-03 | COVERED | Threat models include supply-chain gate.
CONTEXT | D-01 | Treat 375 / 768 / 1280 as the acceptance viewport matrix | 142-01, 142-03 | COVERED | Token names and browser matrix use these exact widths.
CONTEXT | D-02 | Preserve existing visual language and IA | 142-01, 142-02, 142-03 | COVERED | Quality gates forbid redesign and route/workflow changes.
CONTEXT | D-03 | Tokenize breakpoints while keeping media queries standards-compliant | 142-01 | COVERED | This plan implements it directly.
CONTEXT | D-04 | Topbar nav remains reachable; `.tl-topbar__nav` may own internal scroll but root may not | 142-02, 142-03 | COVERED | Plan 02 preserves CSS scroll owner; Plan 03 proves reachability.
CONTEXT | D-05 | Tables are mobile-first labelled cards and shared responsive pattern restores desktop semantics | 142-02, 142-03 | COVERED | Plan 02 aligns CSS; Plan 03 checks behavior.
CONTEXT | D-06 | Filters/toolbars stack on phones, wrap on tablet, compact on desktop | 142-02, 142-03 | COVERED | Plan 02 aligns CSS; Plan 03 checks controls.
CONTEXT | D-07 | Drawers/subviews fit 375px without clipped controls/values | 142-02, 142-03 | COVERED | Plan 02 aligns CSS; Plan 03 checks bounding boxes.
CONTEXT | D-08 | Browser verification covers representative routes with computed overflow and reachability checks, not screenshots | 142-03 | COVERED | Plan 03 implements the matrix.
CONTEXT | D-09 | Keep Phase 142 independent of Phase 143 screenshot/accessibility sweep | 142-01, 142-02, 142-03 | COVERED | Quality gates explicitly forbid Phase 143 infrastructure.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add breakpoint source-contract tests</name>
  <files>test/threadline/operator_surface/style_contract_test.exs</files>
  <behavior>
    - Tests read `lib/threadline/operator_surface/style.ex` through the existing `@style_path`.
    - Tests assert named breakpoint token declarations exist: `--tl-breakpoint-phone-proof: 375px;`, `--tl-breakpoint-tablet: 768px;`, and `--tl-breakpoint-desktop: 1280px;`.
    - Tests assert source comments name the phone, tablet, and desktop layers and explain that `@media` must use literals because CSS variables are not valid there.
    - Tests collect every `@media (min-width: Npx)` literal in `style.ex`; after this plan the only responsive min-width literals are `768` and `1280`, while `prefers-reduced-motion` remains allowed as a non-width media query.
    - Tests refute `@media (min-width: 481px)`, `@media (min-width: 721px)`, and any `@media` containing `var(--tl-breakpoint`.
  </behavior>
  <action>Extend `style_contract_test.exs` with a Phase 142 breakpoint contract using the existing string/regex style. Keep the test focused on source governance only; do not add browser assertions, CSS parser dependencies, or screenshot logic. Make assertion messages name the missing token or invalid literal clearly so Task 2 has a deterministic correction target.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <done>The contract fails before `style.ex` is updated and passes only when the source exposes the 375/768/1280 token scale and no longer uses the old 481/721 layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tokenize breakpoint scale and replace ad-hoc media layers</name>
  <files>lib/threadline/operator_surface/style.ex, test/threadline/operator_surface/style_contract_test.exs</files>
  <behavior>
    - Add breakpoint token declarations near the existing sizing tokens in `.threadline-ui`.
    - Use token names `phone-proof`, `tablet`, and `desktop` with literal values `375px`, `768px`, and `1280px`.
    - Update the mobile-first comments so the default layer is phone, the tablet enhancement layer starts at `768px`, and the desktop dense/operator layer starts at `1280px`.
    - Replace `@media (min-width: 481px)` with `@media (min-width: 768px)`.
    - Replace `@media (min-width: 721px)` with `@media (min-width: 1280px)`.
    - Do not use `var(--tl-breakpoint-*)` inside `@media`.
    - Preserve all Phase 141 `prefers-reduced-motion` behavior.
  </behavior>
  <action>Update `style.ex` per D-01 and D-03. Keep the change mechanical and source-contract driven: named breakpoint declarations plus media layer literals/comments. Do not redesign the UI, add new selectors beyond the breakpoint token/comment block, change routes or LiveView markup, hide nav destinations, introduce `overflow-x: hidden` on root/body, add packages, or touch unrelated Phase 136/137 artifacts.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
    <automated>rg -n "@media \\(min-width: (481|721)px\\)|@media \\(min-width: var\\(--tl-breakpoint" lib/threadline/operator_surface/style.ex && exit 1 || exit 0</automated>
  </verify>
  <done>`style.ex` has named 375/768/1280 breakpoint tokens, only `768px` and `1280px` responsive min-width layers, no media-query CSS variables, and passing style source contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roadmap/UI spec -> CSS source | Planning acceptance widths become source-level CSS contracts. |
| CSS source -> ExUnit contract | Tests parse source text and fail on breakpoint drift. |
| source contract -> future layout work | Later plans depend on these token names and literals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Tampering | `style.ex` media layers | mitigate | Source-contract tests assert exact token declarations, allowed min-width literals, and refute the retired 481/721 widths. |
| T-142-02 | Repudiation | breakpoint rationale | mitigate | Source comments explain token names and why media queries keep literals; summary records exact test results. |
| T-142-03 | Denial of Service | browser layout at intermediate widths | mitigate | Tablet/desktop thresholds are introduced before CSS remediation and browser matrix plans, keeping failures localized. |
| T-142-04 | Information Disclosure | CSS source tokens | accept | Breakpoint values and selector names contain no secrets. |
| T-142-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. If an executor attempts to add a CSS parser or browser package, stop for a package legitimacy checkpoint. |
</threat_model>

<quality_gates>
- No production behavior changes beyond breakpoint token declarations and media layer literal/comment updates.
- No root/body `overflow-x: hidden` rule.
- No Tailwind, CSS-in-JS, shadcn, CSS parser, or browser dependency addition.
- No Phase 143 screenshot-diff or broad accessibility work.
- Existing untracked Phase 136/137 files are not touched.
</quality_gates>

<verification>
- `mix test test/threadline/operator_surface/style_contract_test.exs`
- `rg -n "--tl-breakpoint|@media \\(min-width:" lib/threadline/operator_surface/style.ex`
- `rg -n "@media \\(min-width: (481|721)px\\)|@media \\(min-width: var\\(--tl-breakpoint" lib/threadline/operator_surface/style.ex && exit 1 || exit 0`
</verification>

<success_criteria>
- Breakpoint scale is tokenized and source-contract tested.
- Standards-compliant `@media` literals align to the accepted tablet and desktop thresholds.
- Old 481/721 ad-hoc layers are retired before shared responsive layout work proceeds.
</success_criteria>

<output>
Create `.planning/phases/142-responsive-mobile-first/142-01-SUMMARY.md` when done.
</output>
