---
phase: 143-accessibility-consistency-sweep-regression
plan: "04"
type: execute
wave: 4
depends_on: ["143-03"]
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots/
  - examples/threadline_phoenix/e2e/package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [POLISH-A11Y]
must_haves:
  truths:
    - "A lightweight screenshot regression guard runs through the existing Playwright browser lane."
    - "The final browser gate is green before Phase 143 completes."
  artifacts:
    - path: "examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts"
      provides: "Automated screenshot regression guard"
      contains: "toMatchSnapshot"
    - path: "examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots/"
      provides: "Committed guard snapshot baseline"
      contains: ".png"
---

# Plan 143-04: Screenshot Regression Guard

## Objective

Wire a lightweight screenshot-diff guard into the existing Playwright/CI browser lane so future PRs cannot silently degrade the v1.31 polished baseline.

## Tasks

1. Add a narrow Playwright screenshot-regression guard using existing Playwright snapshot support.
2. Cover a representative subset that catches global chrome, dense Timeline, row-history drawer, Prove surfaces, and mobile responsiveness.
3. Commit initial guard snapshots or an equivalent deterministic baseline artifact.
4. Ensure the guard runs under `npm test` and therefore under `mix verify.example_browser` / CI. Only edit `package.json` or CI if a focused script or explicit job label is needed.

## Required Checks

- `cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-screenshot-regression.spec.ts`
- `mix verify.example_browser`
- `rg -n "operator-screenshot-regression|toMatchSnapshot|toHaveScreenshot|verify.example_browser|playwright" examples/threadline_phoenix/e2e .github/workflows/ci.yml mix.exs`

## Success Criteria

- Screenshot regression guard is automated in the existing browser lane.
- Guard scope is lightweight and deterministic.
- Final `mix verify.example_browser` is green.
