# Milestone archive: v1.7 — Reference integration for SaaS

**Status:** SHIPPED 2026-04-24  
**Phases:** 22–24  
**Total plans:** 5 (execution trees: `.planning/phases/22-example-app-layout-runbook/`, `23-http-audited-path/`, `24-job-path-actions-adoption-pointers/`)

## Overview

Ship a **runnable in-repo Phoenix example** under `examples/threadline_phoenix/` that demonstrates **HTTP** and **Oban** audited writes, **`record_action/2`**, and **links** to `guides/production-checklist.md` and `guides/adoption-pilot-backlog.md` (STG rubric) — integrator-owned host evidence unchanged from v1.6. **Hex `0.2.0`** unchanged unless a separate release decision is made.

## Phase table

| Phase | Name | Requirements |
| ----- | ---- | -------------- |
| 22 | Example app layout & runbook | REF-01, REF-02 |
| 23 | HTTP audited path | REF-03 |
| 24 | Job path, actions, adoption pointers | REF-04, REF-05, REF-06 |

**Living snapshot:** requirements copied to this directory as `v1.7-REQUIREMENTS.md`; canonical close state is git history on `main`.

## Phases (detail)

### Phase 22 — Example app layout & runbook

**Goal:** Contributor can `cd examples/threadline_phoenix`, follow README, migrate, and run with **path** dependency on `threadline`; migrations reflect install + `gen.triggers` for ≥1 table; README documents **`MIX_ENV`** parity for trigger regeneration.

**Plans:** 22-01 (scaffold), 22-02 (verify.example / CI gate) — both complete 2026-04-24.

### Phase 23 — HTTP audited path

**Goal:** `Threadline.Plug` on the API pipeline; audited insert/update; automated test proves `audit_changes` + transaction/actor linkage.

**Plans:** 23-01 — complete 2026-04-24.

### Phase 24 — Job path, actions, adoption pointers

**Goal:** Oban worker + `Threadline.Job` audited write with test coverage; `record_action/2` with operator-facing note; README links to production checklist and adoption backlog STG blocks.

**Plans:** 24-01 (Oban + worker + test), 24-02 (README semantics + adoption links) — both complete 2026-04-24.

## Milestone summary

**Key decisions**

- **Example-only** integration surface under `examples/` — no published `threadline_web` companion.
- **Sandbox `unboxed_run`** for the Oban worker audit assertion where nested savepoints hid trigger-visible GUC (documented in 24-01 summary).

**Deferred / follow-up**

- Optional **`/gsd-cleanup`** to move phase directories to `milestones/v1.7-phases/`; trees remain under `.planning/phases/` by default.

**Technical debt**

- `gsd-sdk query milestone.complete` still returns `version required for phases archive` in this environment; archives written manually to match prior milestones.
- Living `REQUIREMENTS.md` / `ROADMAP.md` checkboxes lagged `PROJECT.md` until milestone close; reconciled in this archive and roadmap rewrite.

---

_For current project status, see `.planning/ROADMAP.md`._
