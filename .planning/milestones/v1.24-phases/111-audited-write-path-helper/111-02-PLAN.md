---
phase: 111-audited-write-path-helper
plan: 02
type: execute
wave: 2
depends_on:
  - 111-01
files_modified:
  - test/threadline/audit_transaction_test.exs
autonomous: true
requirements:
  - AUDIT-TXN-03
must_haves:
  truths:
    - "PostgreSQL integration tests prove row capture under Threadline.Audit.transaction/3"
    - "action_id linkage enables strict :correlation_id timeline filter"
    - "Missing actor_ref returns {:error, :missing_actor} by default"
    - "allow_missing_actor: true permits capture-only without action when actor nil"
  artifacts:
    - path: test/threadline/audit_transaction_test.exs
      provides: "AUDIT-TXN-03 integration proof"
---

<objective>
Wave 2 — PostgreSQL integration tests for `Threadline.Audit.transaction/3` proving capture, correlation linkage, and actor policy (AUDIT-TXN-03).
</objective>

<context>
@.planning/phases/111-audited-write-path-helper/111-CONTEXT.md (D-111-03, D-111-04, D-111-05)
@.planning/phases/111-audited-write-path-helper/111-RESEARCH.md
@lib/threadline/audit.ex
@test/threadline/capture/trigger_context_test.exs
@test/threadline/query_test.exs (LOOP-01 describe)
@test/support/data_case.ex
</context>

<scope_guard>
**ALLOWED:** `test/threadline/audit_transaction_test.exs`  
**FORBIDDEN:** `examples/**`, `guides/**`, modifying `lib/` except bugfixes discovered during testing (commit separately with message prefix `fix(threadline):`)
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-111-03 | Tests pass without real trigger capture | high | Use TriggerSQL.create_trigger on temp table like TriggerContextTest |
| T-111-05 | Correlation test false positive (manual insert) | medium | Full path through Audit.transaction with :action + correlation_id |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold audit_transaction_test with trigger table</name>
  <files>test/threadline/audit_transaction_test.exs</files>
  <read_first>
    - test/threadline/capture/trigger_context_test.exs
    - test/support/data_case.ex
    - lib/threadline/capture/trigger_sql.ex
  </read_first>
  <action>
Create `test/threadline/audit_transaction_test.exs`:

```elixir
defmodule Threadline.AuditTransactionTest do
  use Threadline.DataCase
  @moduletag :integration
```

In `setup_all`:
- Create table `test_audit_helper_target` (uuid id, name text NOT NULL) — unique name to avoid collision
- `Repo.query!(Threadline.Capture.TriggerSQL.create_trigger("test_audit_helper_target"))`
- `on_exit` drop trigger + table

In `setup`: `TRUNCATE test_audit_helper_target CASCADE`

Add helper `insert_row!/1` that runs inside Audit.transaction or raw SQL as needed.
  </action>
  <verify>
    <automated>mix test test/threadline/audit_transaction_test.exs --trace 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `test/threadline/audit_transaction_test.exs` exists
    - File contains `use Threadline.DataCase`
    - File contains `test_audit_helper_target`
    - `mix test test/threadline/audit_transaction_test.exs` exits 0 (even if only placeholder test initially)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Capture + correlation linkage tests</name>
  <files>test/threadline/audit_transaction_test.exs</files>
  <read_first>
    - lib/threadline/audit.ex
    - test/threadline/audit_transaction_test.exs
    - test/threadline/query_test.exs (LOOP-01 timeline assertion)
    - lib/threadline/semantics/actor_ref.ex
  </read_first>
  <action>
Add tests:

**Test A — capture under helper with action (AUDIT-TXN-03a):**
- `actor = ActorRef.new(:user, "audit-helper-1")`
- `Threadline.Audit.transaction(Repo, [actor_ref: actor, action: :audit_helper_test_write], fn -> Repo.query!("INSERT INTO test_audit_helper_target (name) VALUES ('a')"); :ok end)`
- Assert `{:ok, %{result: :ok, audit_transaction_id: id}}` when callback returns atom (or adjust callback to return map)
- Assert `Repo.get!(AuditTransaction, id).actor_ref.type == :user`

**Test B — correlation_id timeline (AUDIT-TXN-03b):**
- Use `correlation_id: "audit-helper-corr-1"` in opts with `:action: :audit_helper_correlation_test`
- Insert row in callback
- `Threadline.timeline(repo: Repo, table: "test_audit_helper_target", correlation_id: "audit-helper-corr-1")`
- Assert `length(results) == 1`

**Test C — capture-only no correlation match:**
- Same insert without `:action`
- Timeline with correlation_id returns empty list

**Test D — missing actor (AUDIT-TXN-03c):**
- `Threadline.Audit.transaction(Repo, [action: :should_fail], fn -> :ok end)` → `{:error, :missing_actor}`
- No `:action`, `allow_missing_actor: true`, nil actor → `{:ok, _}` and captured txn has nil actor_ref

Tag missing-actor test with `@tag :missing_actor` for targeted runs.
  </action>
  <verify>
    <automated>mix test test/threadline/audit_transaction_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/audit_transaction_test.exs` exits 0
    - Test file contains `"audit-helper-corr-1"`
    - Test file contains `:missing_actor`
    - Test file contains `Threadline.timeline`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Linkage failure and return envelope tests</name>
  <files>test/threadline/audit_transaction_test.exs</files>
  <read_first>
    - lib/threadline/audit.ex
    - examples/threadline_phoenix/lib/threadline_phoenix/blog.ex (return shape)
  </read_first>
  <action>
Add tests:

**Test E — map merge return:**
- Callback returns `%{name: "merged"}`
- Assert `{:ok, %{name: "merged", audit_transaction_id: uuid}}` when `:action` present and insert occurred

**Test F — domain rollback propagates:**
- Callback calls `Repo.rollback(:domain_fail)`
- Assert `{:error, :domain_fail}`

**Test G — transaction_meta on linkage:**
- Pass `transaction_meta: %{"organization_id" => "org-1"}` with action
- Assert linked `AuditTransaction.meta == %{"organization_id" => "org-1"}`

Run `mix verify.test` before marking plan complete.
  </action>
  <verify>
    <automated>mix verify.test</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/audit_transaction_test.exs` exits 0
    - `mix verify.test` exits 0
    - Test file contains `audit_transaction_id`
    - Test file contains `transaction_meta`
  </acceptance_criteria>
</task>

</tasks>

<verification_criteria>
- All AUDIT-TXN-03 sub-clauses (a/b/c) covered by named tests
- `mix verify.test` green at repo root
</verification_criteria>
