---
phase: 113-adopter-truth-doc-sync
plan: 04
type: execute
wave: 2
depends_on:
  - 113-01-PLAN.md
  - 113-02-PLAN.md
  - 113-03-PLAN.md
files_modified:
  - test/threadline/evidence_cli_doc_contract_test.exs
  - examples/threadline_phoenix/WALKTHROUGH.md
  - mix.exs
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/milestones/v1.23-REQUIREMENTS.md
  - .planning/milestones/v1.23-ROADMAP.md
autonomous: true
requirements:
  - TRUTH-03
  - TRUTH-05
must_haves:
  truths:
    - "Single canonical evidence CLI: mix threadline.evidence.show — no verify.evidence alias"
    - "evidence_cli_doc_contract_test locks guides, WALKTHROUGH, mix.exs, README"
    - "WALKTHROUGH §5 footnote states verify.evidence was never shipped"
    - "PROJECT.md and MILESTONES.md use canonical CLI + global footnote"
    - "v1.23 milestone files carry errata blocks without rewriting closed checkboxes"
    - "mix verify.doc_contract and mix verify.example exit 0"
  artifacts:
    - path: test/threadline/evidence_cli_doc_contract_test.exs
      provides: TRUTH-03 doc contract
    - path: mix.exs
      provides: verify.doc_contract includes evidence_cli + adoption tests
---

<objective>
Lock canonical evidence CLI naming in doc-contract tests, tighten WALKTHROUGH §5 footnote, update living planning index + v1.23 errata, and run TRUTH-05 verification gates. Closes TRUTH-03, TRUTH-05, D-113-02, D-113-05.
</objective>

<context>
@.planning/phases/113-adopter-truth-doc-sync/113-CONTEXT.md (D-113-02, D-113-05)
@.planning/phases/113-adopter-truth-doc-sync/113-RESEARCH.md (§3, §5)
@.planning/phases/113-adopter-truth-doc-sync/113-PATTERNS.md (errata pattern)
@.planning/phases/108-walkthrough-script-finding-capture-protocol/108-CONTEXT.md (D-108-03f footnote pattern)
</context>

<scope_guard>
**ALLOWED:** evidence CLI test, WALKTHROUGH §5 footnote only, mix.exs alias, PROJECT/MILESTONES, v1.23 errata  
**FORBIDDEN:** mix verify.evidence alias; lib/ changes; v1.22-phases archive edits; RETROSPECTIVE.md body; silent checkbox rewrites
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| — | verify.evidence alias implies CI gate semantics | medium | Refute alias in mix.exs + contract tests |
| — | Planning archive silent rewrite | low | Errata blocks only on v1.23 files |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create evidence_cli_doc_contract_test.exs</name>
  <files>test/threadline/evidence_cli_doc_contract_test.exs</files>
  <read_first>
    - guides/domain-reference.md (~:77 canonical CLI)
    - guides/operator-surface.md (~:130)
    - examples/threadline_phoenix/WALKTHROUGH.md (command blocks + §5 footnote ~570)
    - mix.exs (aliases — refute verify.evidence)
    - test/threadline/readme_doc_contract_test.exs (:63 refute threadline.evidence.show in compact strip)
    - .planning/phases/113-adopter-truth-doc-sync/113-CONTEXT.md (D-113-02e)
  </read_first>
  <action>
Create `test/threadline/evidence_cli_doc_contract_test.exs`:

```elixir
defmodule Threadline.EvidenceCliDocContractTest do
  @moduledoc false
  use ExUnit.Case, async: true

  @canonical "mix threadline.evidence.show"
  @legacy "mix verify.evidence"

  for path <- ["guides/domain-reference.md", "guides/operator-surface.md"] do
    test "#{path} documents canonical evidence CLI only" do
      doc = File.read!(unquote(path))
      assert String.contains?(doc, @canonical)
      refute String.contains?(doc, @legacy)
    end
  end

  test "WALKTHROUGH command blocks use canonical evidence CLI" do
    doc = File.read!("examples/threadline_phoenix/WALKTHROUGH.md")
    assert String.contains?(doc, @canonical)
    assert String.contains?(doc, "--subject retention_run")
  end

  test "WALKTHROUGH allows at most one verify.evidence footnote mention" do
    doc = File.read!("examples/threadline_phoenix/WALKTHROUGH.md")
    count = doc |> String.split(@legacy) |> length() |> Kernel.-(1)
    assert count <= 1
  end

  test "mix.exs has no verify.evidence alias" do
    mix = File.read!("mix.exs")
    refute String.contains?(mix, "\"verify.evidence\":")
  end

  test "README compact evidence strip refutes runnable CLI strings" do
    readme = File.read!("README.md")
    refute String.contains?(readme, @canonical)
    refute String.contains?(readme, @legacy)
  end
end
```
  </action>
  <acceptance_criteria>
    - File exists at test/threadline/evidence_cli_doc_contract_test.exs
    - `mix test test/threadline/evidence_cli_doc_contract_test.exs` exits 0 after WALKTHROUGH footnote update (Task 2)
  </acceptance_criteria>
  <verify>mix test test/threadline/evidence_cli_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 2: Tighten WALKTHROUGH §5 CLI footnote</name>
  <files>examples/threadline_phoenix/WALKTHROUGH.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (§5 footnote ~570)
    - .planning/phases/113-adopter-truth-doc-sync/113-CONTEXT.md (D-113-02c)
  </read_first>
  <action>
Replace the §5 CLI footnote blockquote (~line 570) with:

```markdown
> **CLI footnote:** Earlier milestones and planning prose referenced **`mix verify.evidence`**. That alias was **never shipped**. The canonical viewer in this repo is **`mix threadline.evidence.show`** — use it in body steps and Appendix B.
```

Ensure all command blocks in §5 and Appendix B use `mix threadline.evidence.show` (already mostly true — verify no `verify.evidence` in fenced bash blocks).
  </action>
  <acceptance_criteria>
    - §5 footnote contains substring `never shipped`
    - `grep "verify.evidence" examples/threadline_phoenix/WALKTHROUGH.md` — zero matches inside ```bash code fences (footnote mention allowed once)
    - All fenced command blocks containing evidence use `threadline.evidence.show`
  </acceptance_criteria>
  <verify>mix test test/threadline/evidence_cli_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 3: Update living planning index (PROJECT + MILESTONES)</name>
  <files>.planning/PROJECT.md, .planning/MILESTONES.md</files>
  <read_first>
    - .planning/PROJECT.md (lines ~38, 64, 284 dual CLI phrasing)
    - .planning/MILESTONES.md (line ~50)
    - .planning/phases/113-adopter-truth-doc-sync/113-CONTEXT.md (D-113-02f, D-113-05b)
  </read_first>
  <action>
In **PROJECT.md** and **MILESTONES.md**, replace dual phrasing like `` `mix verify.evidence`, `mix threadline.evidence.show` `` or `` (`mix verify.evidence` family, `mix threadline.evidence.show`) `` with:

**Canonical:** `mix threadline.evidence.show`

Add one global footnote (PROJECT.md near v1.24 section or MILESTONES v1.22 summary):

> **Evidence CLI errata:** `mix verify.evidence` was planned for early milestones but never shipped; runnable viewer is `mix threadline.evidence.show` only.

Do **not** edit `.planning/RETROSPECTIVE.md` body (immutable per D-113-05d).
  </action>
  <acceptance_criteria>
    - `grep "verify.evidence" .planning/PROJECT.md` — at most one match (footnote/errata only, not as runnable command)
    - `grep "threadline.evidence.show" .planning/PROJECT.md` matches
    - Same pattern for `.planning/MILESTONES.md`
  </acceptance_criteria>
  <verify>grep -n "verify.evidence\|threadline.evidence" .planning/PROJECT.md .planning/MILESTONES.md</verify>
</task>

<task type="auto">
  <name>Task 4: Add v1.23 milestone errata blocks</name>
  <files>.planning/milestones/v1.23-REQUIREMENTS.md, .planning/milestones/v1.23-ROADMAP.md</files>
  <read_first>
    - .planning/milestones/v1.23-REQUIREMENTS.md (WALK-04 line ~59)
    - .planning/milestones/v1.23-ROADMAP.md (WALK-04 success criteria if present)
    - .planning/phases/113-adopter-truth-doc-sync/113-PATTERNS.md (errata block pattern)
  </read_first>
  <action>
Add **2-line errata blockquote** immediately **above** the WALK-04 requirement/checkbox section in each file (do not change `[x]` checkbox text):

```markdown
> **Errata (2026-05-27, Phase 113):** Runnable evidence viewer is `mix threadline.evidence.show` only. `mix verify.evidence` was planned but never shipped — see `guides/domain-reference.md`.
```

Leave closed checkbox prose intact below the errata.
  </action>
  <acceptance_criteria>
    - Both v1.23 files contain `Errata (2026-05-27, Phase 113)`
    - WALK-04 checkbox lines still present unchanged (grep `WALK-04` shows original `[x]` line)
  </acceptance_criteria>
  <verify>grep -B2 "WALK-04" .planning/milestones/v1.23-REQUIREMENTS.md</verify>
</task>

<task type="auto">
  <name>Task 5: Wire evidence_cli test into verify.doc_contract and run TRUTH-05 gates</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (verify.doc_contract alias)
    - .planning/ROADMAP.md (Phase 113 success criteria #4)
  </read_first>
  <action>
Add `test/threadline/evidence_cli_doc_contract_test.exs` to `verify.doc_contract` alias if not already present (Plan 03 may have added adoption test — append evidence_cli in same alias line).

Run from repo root:

```bash
mix verify.doc_contract
mix verify.example
```

Both must exit 0 for TRUTH-05.
  </action>
  <acceptance_criteria>
    - `grep "evidence_cli_doc_contract_test" mix.exs` matches
    - `mix verify.doc_contract` exits 0
    - `mix verify.example` exits 0
  </acceptance_criteria>
  <verify>mix verify.doc_contract && mix verify.example</verify>
</task>

</tasks>

<verification>
1. `mix test test/threadline/evidence_cli_doc_contract_test.exs`
2. `mix verify.doc_contract`
3. `mix verify.example`
</verification>

<success_criteria>
- TRUTH-03: single canonical evidence CLI locked in tests and living docs
- TRUTH-05: mix verify.doc_contract and mix verify.example green
- D-113-05: planning errata without archive rewrite
</success_criteria>
