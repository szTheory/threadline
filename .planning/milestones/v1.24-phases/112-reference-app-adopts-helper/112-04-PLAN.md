---
phase: 112-reference-app-adopts-helper
plan: 04
type: execute
wave: 3
depends_on:
  - 112-02
  - 112-03
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix/blog.ex
  - examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs
  - examples/threadline_phoenix/README.md
  - README.md
  - test/threadline/readme_doc_contract_test.exs
autonomous: true
requirements:
  - ADOPT-HELPER-01
  - ADOPT-HELPER-02
  - ADOPT-HELPER-03
must_haves:
  truths:
    - "Blog.touch_post_for_job/2 uses Threadline.Audit.transaction/3 with action_id linkage (SEED-001 fix)"
    - "touch_post_for_job/2 returns {:ok, %{post: %Post{}, audit_transaction_id: uuid}} envelope (D-112-04a)"
    - "post_touch_worker_test asserts at.action_id == action.id"
    - "PostTouchWorker perform/1 unchanged {:ok, _post} collapse pattern"
    - "Root and example README cross-link Threadline.Audit.transaction/3 as first audited write path (D-112-05)"
    - "mix verify.example and mix verify.doc_contract green at phase closeout"
  artifacts:
    - path: examples/threadline_phoenix/lib/threadline_phoenix/blog.ex
      provides: "Oban correlation-ready touch_post_for_job via helper"
    - path: examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs
      provides: "action_id linkage proof (D-112-01d)"
    - path: README.md
      provides: "ADOPT-HELPER-03 quickstart helper cross-link"
    - path: examples/threadline_phoenix/README.md
      provides: "Semantics in jobs helper description (D-112-05a)"
---

<objective>
Wave 3 — migrate `Blog.touch_post_for_job/2` to helper with map-merge return envelope, strengthen worker test for action_id linkage (SEED-001), update root and example README cross-links (ADOPT-HELPER-03, D-112-04, D-112-05). Phase closeout verification.
</objective>

<context>
@.planning/phases/112-reference-app-adopts-helper/112-CONTEXT.md (D-112-04, D-112-05, SEED-001)
@.planning/phases/112-reference-app-adopts-helper/112-RESEARCH.md (§1 touch_post_for_job, §5 README)
@.planning/phases/112-reference-app-adopts-helper/112-PATTERNS.md (Oban helper, README cross-links)
@examples/threadline_phoenix/lib/threadline_phoenix/blog.ex
@examples/threadline_phoenix/lib/threadline_phoenix/workers/post_touch_worker.ex
@examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs
@README.md
@examples/threadline_phoenix/README.md
@lib/threadline/job.ex
</context>

<scope_guard>
**ALLOWED:** `blog.ex` (`touch_post_for_job/2` only), `post_touch_worker_test.exs`, `README.md`, `examples/threadline_phoenix/README.md`, `readme_doc_contract_test.exs`  
**FORBIDDEN:** `post_touch_worker.ex` logic changes beyond compile verification, demo seeds, `incident_replay.exs`, `return_audit:` flag, struct-only return from touch_post_for_job, migrating remaining manual paths outside the four primary write paths
</scope_guard>

<threat_model>
| ID | ASVS L1 / Threat | Severity | Mitigation |
|----|------------------|----------|------------|
| V4.1.1 | Orphaned job actions (SEED-001) | high | Helper links action_id; test asserts `at.action_id == action.id` |
| V14.2.1 | README/doc drift from implementation | medium | Update readme_doc_contract_test with helper literal; run mix verify.doc_contract |
| V14.4.1 | Evaluator copies manual recipe from README | medium | Replace step 4 manual set_config block with helper pointer to guides/getting-started-saas.md §6 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Migrate touch_post_for_job/2 to helper with audit_transaction_id envelope</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/blog.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/blog.ex (:99-137 manual Oban path)
    - .planning/phases/112-reference-app-adopts-helper/112-CONTEXT.md (:160-174 canonical Oban call site)
    - lib/threadline/job.ex (actor_ref_from_args/1, context_opts/1)
    - examples/threadline_phoenix/lib/threadline_phoenix/workers/post_touch_worker.ex (:15-18)
  </read_first>
  <action>
Refactor `touch_post_for_job/2` in `blog.ex`:

1. Keep pre-helper validation: `Job.actor_ref_from_args/1`, post_id/title presence check → `{:error, :missing_post_attrs}`.

2. Replace manual `Repo.transaction` + `set_config` + `record_action` with:

```elixir
Threadline.Audit.transaction(
  Repo,
  [
    actor_ref: actor_ref,
    action: {:post_title_refreshed_from_queue, Job.context_opts(args)}
  ],
  fn ->
    post = Repo.get!(Post, post_id)

    case Repo.update(Post.changeset(post, %{title: title})) do
      {:error, cs} -> Repo.rollback(cs)
      {:ok, updated} -> %{post: updated}
    end
  end
)
```

3. Return helper result directly: `{:ok, %{post: %Post{}, audit_transaction_id: uuid}}` (D-112-04a). Reject struct-only or opt-in flags (D-112-04d).

4. Remove all `set_config`, `record_action`, and `AuditTransaction`/`AuditAction` usage from `touch_post_for_job/2`.

5. After this task, `grep set_config lib/threadline_phoenix/blog.ex` should exit 1 (no manual GUC left in blog.ex).

Do NOT change `PostTouchWorker.perform/1`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix compile --warnings-as-errors && ! grep -q 'set_config' lib/threadline_phoenix/blog.ex && grep -q 'post_title_refreshed_from_queue' lib/threadline_phoenix/blog.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Threadline.Audit.transaction' examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 0
    - `grep -q 'set_config' examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 1
    - `grep -q 'audit_transaction_id' examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 0
    - `grep -q 'Job.context_opts(args)' examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 0
    - `cd examples/threadline_phoenix && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Strengthen post_touch_worker_test action_id linkage assertion</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs (:49-58 action lookup)
    - .planning/phases/112-reference-app-adopts-helper/112-CONTEXT.md (D-112-01d)
    - .planning/v1.24-seeds/SEED-001-audited-write-transaction.md (linkage footgun)
  </read_first>
  <action>
In `post_touch_worker_test.exs`, **after** `refute is_nil(action)` (~line 58), add:

```elixir
assert at.action_id == action.id
```

This proves SEED-001 linkage fix — action exists AND is linked to capture transaction.

Run worker test:
```bash
cd examples/threadline_phoenix && mix test test/threadline_phoenix/workers/post_touch_worker_test.exs
```
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/workers/post_touch_worker_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'at.action_id == action.id' examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs` exits 0
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix/workers/post_touch_worker_test.exs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update README cross-links for ADOPT-HELPER-03</name>
  <files>README.md, examples/threadline_phoenix/README.md, test/threadline/readme_doc_contract_test.exs</files>
  <read_first>
    - README.md (:10 public API, :14-22 Start here, :74-97 Quick Start step 4)
    - examples/threadline_phoenix/README.md (:319-323 Semantics in jobs, :332 POST /api/posts)
    - test/threadline/readme_doc_contract_test.exs (:15-24, :150-158)
    - guides/getting-started-saas.md §6 (helper path from Plan 02)
  </read_first>
  <action>
**Root `README.md`:**

1. In public API blurb (~line 10), add `Threadline.Audit.transaction/3` alongside Plug/record_action/timeline.

2. In "Start here" section, mention `Threadline.Audit.transaction/3` as the recommended audited write path with link to `guides/getting-started-saas.md` §6.

3. Replace Quick Start step 4 manual block (`set_config` + separate `record_action/2` at :87-96) with concise pointer:

```markdown
4. Wrap audited writes with `Threadline.Audit.transaction/3` — see [Getting started with Threadline in a Phoenix SaaS app](guides/getting-started-saas.md) §6 for the canonical helper snippet (actor GUC + domain writes + optional action linkage in one transaction).
```

Keep step 5 query examples unchanged.

**Example `examples/threadline_phoenix/README.md`:**

1. Replace "Semantics in jobs" paragraph (:319-323) to describe helper-based flow:

`PostTouchWorker` → `Blog.touch_post_for_job/2` uses `Threadline.Audit.transaction/3` with `actor_ref:` and `action: {:post_title_refreshed_from_queue, Job.context_opts(args)}`. See `Threadline.Job` for `actor_ref_from_args/1` and `context_opts/1`.

2. Update POST /api/posts note (:332) from manual action_id wiring to "via `Threadline.Audit.transaction/3`".

**`test/threadline/readme_doc_contract_test.exs`:**

Add to appropriate test(s):
```elixir
assert String.contains?(readme, "Threadline.Audit.transaction")
assert String.contains?(doc, "Threadline.Audit.transaction")
```

Keep existing `Threadline.record_action/2` assertions unless step 4 removal breaks them — if so, replace with helper assertion only where step 4 manual block was removed.
  </action>
  <verify>
    <automated>grep -q 'Threadline.Audit.transaction' README.md && grep -q 'Threadline.Audit.transaction' examples/threadline_phoenix/README.md && mix test test/threadline/readme_doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Threadline.Audit.transaction' README.md` exits 0
    - `grep -q 'getting-started-saas.md' README.md` exits 0
    - `grep -q 'Threadline.Audit.transaction' examples/threadline_phoenix/README.md` exits 0
    - `grep -q 'Threadline.Audit.transaction' test/threadline/readme_doc_contract_test.exs` exits 0
    - `mix test test/threadline/readme_doc_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Phase closeout — full verify.example and verify.doc_contract</name>
  <files>[verification only]</files>
  <read_first>
    - .planning/phases/112-reference-app-adopts-helper/112-VALIDATION.md (closeout row)
    - .planning/REQUIREMENTS.md (ADOPT-HELPER-02, ADOPT-HELPER-03)
  </read_first>
  <action>
Run phase closeout gates from repo root:

```bash
mix verify.example
mix verify.doc_contract
mix test test/threadline/audit_transaction_test.exs
```

If any failure, fix in-scope regressions before marking plan complete. Do NOT weaken assertions.
  </action>
  <verify>
    <automated>mix verify.example && mix verify.doc_contract</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.example` exits 0
    - `mix verify.doc_contract` exits 0
    - All four primary write paths in blog.ex + help_desk.ex use `Threadline.Audit.transaction` (grep count >= 4 across both files)
    - No `set_config.*threadline.actor_ref` in blog.ex or help_desk.ex migrated functions
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd examples/threadline_phoenix && mix test test/threadline_phoenix/workers/post_touch_worker_test.exs
mix test test/threadline/readme_doc_contract_test.exs
mix verify.example
mix verify.doc_contract
```
</verification>

<success_criteria>
- Oban path uses helper; action_id linked; worker test proves linkage
- README quickstart cross-links helper (ADOPT-HELPER-03)
- Full example suite and doc contracts green — Phase 112 complete
</success_criteria>
