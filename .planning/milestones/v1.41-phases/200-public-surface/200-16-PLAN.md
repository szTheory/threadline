---
phase: 200-public-surface
plan: 16
type: execute
wave: 8
depends_on: [200-08]
files_modified:
  - lib/threadline/operator_surface/components/logo.ex
  - lib/threadline/operator_surface/mechanical_checker.ex
  - lib/threadline/operator_surface/stress_fixtures.ex
  - lib/threadline/operator_surface/live/stress_live.ex
  - test/threadline/operator_surface/mechanical_checker_test.exs
  - test/threadline/operator_surface/stress_fixtures_test.exs
  - test/threadline/operator_surface/stress_ledger_test.exs
  - test/threadline/operator_surface/stress_router_test.exs
  - test/threadline/operator_surface/ui_stress_test.exs
  - test/fixtures/operator_surface/design-system-ledger.json
autonomous: true
requirements: [SURFACE-01, SURFACE-02]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-14/D-22: operator infrastructure and stress tooling describe current invariants and named evaluation cohorts, never planning chronology."
    - "Rendered stress-copy cleanup changes text only; DOM structure, classes, routes, fixture cardinality, mechanics, and visual behavior remain unchanged for the Phase 201 boundary."
  artifacts:
    - path: lib/threadline/operator_surface/mechanical_checker.ex
      provides: Durable mechanical-gate and auto-fix rationale
    - path: lib/threadline/operator_surface/stress_fixtures.ex
      provides: Planning-independent stress provenance schema
    - path: lib/threadline/operator_surface/live/stress_live.ex
      provides: Stable stress labels and source vocabulary without layout redesign
  key_links:
    - from: lib/threadline/operator_surface/stress_fixtures.ex
      to: lib/threadline/operator_surface/live/stress_live.ex
      via: stable cohort/provenance keys consumed by the stress route
      pattern: "cohort|provenance"
---

<objective>
Clean operator infrastructure and the coupled stress corpus without changing mechanical judgments or interface structure.

Purpose: Remove the densest shipped planning provenance at one coherent owner while protecting the operator UI boundary.
Output: Current-invariant infrastructure comments and a planning-independent stress data/rendering vocabulary.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-public-surface/200-CONTEXT.md
@.planning/phases/200-public-surface/200-RESEARCH.md
@.planning/phases/200-public-surface/200-08-SUMMARY.md
@test/threadline/release_artifact_contract_test.exs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rewrite logo and mechanical-checker provenance as current invariants</name>
  <read_first>
    - lib/threadline/operator_surface/components/logo.ex: compact lockup history comment
    - lib/threadline/operator_surface/mechanical_checker.ex: gate origin, auto-fix set, capture source, and remediation text
    - test/threadline/release_artifact_contract_test.exs: source_vocab_operator_infrastructure owner tag
  </read_first>
  <files>lib/threadline/operator_surface/components/logo.ex, lib/threadline/operator_surface/mechanical_checker.ex, test/threadline/operator_surface/mechanical_checker_test.exs</files>
  <action>Per D-14/D-22, replace Logo's historical winner/release label with the durable fact that its geometry is copied from the brandbook authority and remains theme-driven. Treat the `C13` asset name, SVG `d` attributes, and path coordinate data as legitimate brand/SVG tokens: if the initial matcher collides with those shapes, make the lexical planning-token matcher semantically precise and prove it still catches injected offenders; never rewrite geometry or add a file/token exception. In MechanicalChecker, replace the phase/decision/requirement origin banner with the actual deterministic-gate contract, rename the phase-specific auto-apply whitelist description as the exact approved fix set, describe route captures by runtime/source rather than chronology, and replace remediation text that names release phases with the required structural-and-human-review condition. Preserve SVG path data, component output, metric formulas, thresholds, fix eligibility, capture selection, and message meaning. Update only exact source-text assertions.</action>
  <acceptance_criteria>
    - Logo geometry/rendered output and every mechanical result are unchanged.
    - The bounded infrastructure tag owns exactly both packaged source files and reports an injected planning offender.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/operator_surface/mechanical_checker_test.exs -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/release_artifact_contract_test.exs --only source_vocab_operator_infrastructure -x</automated>
    <fails_when>Either command exits non-zero or selects zero tests, SVG geometry or checker behavior changes, the bounded set is empty or not exact, planning provenance remains, or the injected control is missed.</fails_when>
  </verify>
  <done>Logo and mechanical infrastructure are clean under their exact owner tag with behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stress chronology with stable evaluation cohorts</name>
  <read_first>
    - lib/threadline/operator_surface/stress_fixtures.ex: planning-labeled comments, strings, owner_phase/reserved_for_phase fields, and generated notes
    - lib/threadline/operator_surface/live/stress_live.ex: phase-labeled headings, Owner phase label, field readers, and decision-ID comments
    - test/fixtures/operator_surface/design-system-ledger.json: coupled provenance fields and notes
    - test/threadline/release_artifact_contract_test.exs: source_vocab_operator_stress owner tag
  </read_first>
  <files>lib/threadline/operator_surface/stress_fixtures.ex, lib/threadline/operator_surface/live/stress_live.ex, test/threadline/operator_surface/stress_fixtures_test.exs, test/threadline/operator_surface/stress_ledger_test.exs, test/threadline/operator_surface/stress_router_test.exs, test/threadline/operator_surface/ui_stress_test.exs, test/fixtures/operator_surface/design-system-ledger.json</files>
  <action>Replace genuine planning provenance throughout the coupled stress source and ledger contract. Rename `owner_phase`/`reserved_for_phase` and local `phase` variables to stable cohort/provenance concepts in source, consumer, tests, and ledger JSON; replace numeric chronology in generated notes with named baseline, page-state, data-display, refute-twin, or graded-ladder cohorts while preserving IDs, fixture keys, status, cardinality, ordering, score inputs, and reservation meaning. Rewrite decision/requirement comments as the invariant they protect: structural cartesian coverage, partitioning, low-correlation ladder, canonical reference assembly, and one-intended-flaw rules. Change only text inside existing stress markup: `Owner phase` to `Origin cohort`, remove numeric prefixes from Refute Twin, Primitives Matrix, Data Display, and Data States headings, and retain the same tags, classes, nesting, styles, and routes. This limited copy hygiene is required by D-14 and must not redesign UI or absorb broader Phase 201 DOM/CSS work. Use the same zero-allowlist matcher with no path or token exclusions.</action>
  <acceptance_criteria>
    - Stress story counts, IDs, fixture keys, statuses, score inputs, routes, tags/classes/nesting, and mechanical outcomes are unchanged.
    - All planning-shaped source comments, identifiers, generated copy, and visible headings in the two packaged files are replaced by stable cohort/domain language.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/operator_surface/stress_fixtures_test.exs test/threadline/operator_surface/stress_ledger_test.exs test/threadline/operator_surface/stress_router_test.exs test/threadline/operator_surface/ui_stress_test.exs -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/release_artifact_contract_test.exs --only source_vocab_operator_stress -x</automated>
    <fails_when>Either command exits non-zero or selects zero tests, fixture cardinality/identity/order or mechanical behavior changes, markup structure/classes/routes drift, either owner subject remains dirty, the owner set is empty, or the injected control is missed.</fails_when>
  </verify>
  <done>The stress system uses stable cohort provenance and clean rendered labels with structural and mechanical behavior unchanged.</done>
</task>

</tasks>

<threat_model>
**ASVS level:** L1

## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| fixture corpus → mechanical checker | Provenance renaming must not change evaluation inputs or results. |
| stress source → rendered operator route | Required copy cleanup must not alter structure, layout, or navigation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-02 | Information disclosure | packaged operator/stress source | medium | mitigate | Two exact owner tags with the shared matcher and injected controls. |
| T-200-10 | Tampering | stress fixtures and mechanical outcomes | high | mitigate | Coupled source/ledger/test update with cardinality, identity, route, and mechanics regression suites. |
</threat_model>

<verification>Run both bounded owner tags and all coupled mechanical/stress suites; do not run full source_module_vocabulary or archive_vocabulary.</verification>
<success_criteria>
- The four matched packaged source files appear exactly once in this plan's tasks.
- Stable cohort vocabulary replaces planning chronology without hiding it from the scanner.
- UI structure and mechanical outcomes remain unchanged.
</success_criteria>

## Artifacts this phase produces

- Planning-independent operator infrastructure and stress provenance.
- Green `source_vocab_operator_infrastructure` and `source_vocab_operator_stress` owner projections.

<output>Create .planning/phases/200-public-surface/200-16-SUMMARY.md when done.</output>
