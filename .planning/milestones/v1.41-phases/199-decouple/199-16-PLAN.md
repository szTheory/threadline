---
phase: 199-decouple
plan: 16
type: execute
wave: 5
depends_on: [199-15]
files_modified:
  - test/fixtures/dialyzer/query-storage.json
  - lib/threadline/change_diff.ex
  - lib/threadline/continuity.ex
  - lib/threadline/query.ex
  - lib/threadline/query/actor_history_page.ex
  - lib/threadline/storage/local.ex
autonomous: true
requirements: [DECOUPLE-07, DECOUPLE-08]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All ten sealed warnings W03, W04, and W33-W40 are explicitly dispositioned within five authorized origins."
    - "Remote Ecto-schema and ActorRef contracts use concrete struct types that exist in this source tree."
    - "Continuity validates the schema it actually returns and local storage handles `:code.priv_dir/1`'s error tuple explicitly."
  artifacts:
    - path: test/fixtures/dialyzer/query-storage.json
      provides: "Exact authority, disposition, and proof for the 10-warning query/storage slice"
    - path: lib/threadline/query.ex
      provides: "Truthful source-local result contracts without nonexistent remote type references"
    - path: lib/threadline/storage/local.ex
      provides: "Explicit OTP priv-dir success/error handling"
  key_links:
    - from: test/fixtures/dialyzer/query-storage.json
      to: bin/verify-dialyzer-slice
      via: "shared schema from Plan 199-15"
    - from: lib/threadline/continuity.ex
      to: Threadline.SchemaValidation.validate_schema!/1
      via: "validated schema value continues into the operation"
---

<objective>
Remediate the ten sealed query, continuity, page-contract, and local-storage warnings as one dependency-coherent bounded slice.

Purpose: Make source contracts name types that actually exist and fix two real discarded/error-path defects, following D-23 while retaining the strict evidence requirements of D-26 and D-27.
Output: A sealed 10-warning slice fixture and narrow changes to exactly five warning-origin files.

Warning authority: exactly `lib/threadline/change_diff.ex` (W03), `lib/threadline/continuity.ex` (W04), `lib/threadline/query.ex` (W33-W38), `lib/threadline/query/actor_history_page.ex` (W39), and `lib/threadline/storage/local.ex` (W40). No other warning-origin source file may be edited by this plan.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
@.planning/phases/199-decouple/199-13-SUMMARY.md
@.planning/phases/199-decouple/199-15-SUMMARY.md
@bin/verify-dialyzer-slice
</context>

## Artifacts this phase produces

- `test/fixtures/dialyzer/query-storage.json`: complete W03/W04/W33-W40 sealed-to-current evidence.
- Correct concrete struct types for the query/change-diff/page contracts.
- Explicit value threading in continuity and OTP error handling in local storage.

<dependencies>
Needs: Plan 199-15's fixture schema and executable verifier.
Creates: One disjoint remediation result consumed by Plan 199-20.
Parallel safety: Shares no modified file with Plans 199-17, 199-18, or 199-19 and may run alongside them in Wave 5.
</dependencies>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace nonexistent remote type contracts with real struct contracts</name>
  <files>test/fixtures/dialyzer/query-storage.json, lib/threadline/change_diff.ex, lib/threadline/query.ex, lib/threadline/query/actor_history_page.ex</files>
  <read_first>
    - .planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
    - test/fixtures/dialyzer/critic-tooling.json
    - lib/threadline/change_diff.ex
    - lib/threadline/query.ex
    - lib/threadline/query/actor_history_page.ex
    - lib/threadline/capture/audit_change.ex
    - lib/threadline/capture/audit_transaction.ex
    - lib/threadline/semantics/audit_action.ex
    - lib/threadline/semantics/actor_ref.ex
    - test/threadline/change_diff_test.exs
    - test/threadline/query_test.exs
    - test/threadline/operator_surface/live/actor_live_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/change_diff.ex lib/threadline/query.ex lib/threadline/query/actor_history_page.ex` succeeds.</precondition>
  <behavior>
    - RED: W03 and W33-W39 remain when the slice fixture first declares them fixed.
    - GREEN: Contracts use `%AuditChange{}`, `%AuditTransaction{}`, `%AuditAction{}`, and `%ActorRef{}` shapes instead of remote `.t()` types those modules do not export.
    - Regression: Query filtering, change diffing, and actor-history pagination retain their existing results and ordering.
  </behavior>
  <action>Create the query-storage fixture with exact authority and sealed raw evidence, mark W03 and W33-W39 fixed, and capture RED. Replace only the invalid remote type references with the corresponding concrete struct types already accepted/returned at runtime; do not add broad `term()` types and do not edit the schema or ActorRef modules because they are outside this slice's sealed origins. Preserve query and pagination behavior. Reach GREEN with targeted tests plus the slice verifier, and record post-analysis evidence per D-23 and D-26.</action>
  <verify>
    <automated>mix test test/threadline/change_diff_test.exs test/threadline/query_test.exs test/threadline/operator_surface/live/actor_live_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/query-storage.json</automated>
    <fails_when>Any of W03/W33-W39 survives, a new warning appears in the three origins, a referenced test path is absent, an out-of-authority type provider is edited, or observable query/page behavior changes.</fails_when>
  </verify>
  <acceptance_criteria>
    - W03 and W33-W39 are absent and recorded as fixed with evidence.
    - Every replacement type is a concrete existing struct and no broad type weakens the contract.
    - The fixture contains no warning or source origin owned by another Wave 5 plan.
  </acceptance_criteria>
  <done>The query-facing contracts are truthful, tested, and clean under the bounded analyzer slice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread validated continuity values and handle priv-dir errors</name>
  <files>test/fixtures/dialyzer/query-storage.json, lib/threadline/continuity.ex, lib/threadline/storage/local.ex</files>
  <read_first>
    - test/fixtures/dialyzer/query-storage.json
    - lib/threadline/continuity.ex
    - lib/threadline/storage/local.ex
    - test/threadline/continuity_brownfield_test.exs
    - test/threadline/storage/local_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/continuity.ex lib/threadline/storage/local.ex` succeeds.</precondition>
  <behavior>
    - RED: W04 proves the validated schema return is discarded and W40 proves the current fallback cannot receive `nil`.
    - GREEN: Continuity carries the return of `validate_schema!/1` forward as the schema used by the operation.
    - GREEN: Local storage distinguishes the charlist success from `{:error, :bad_name}` and uses the same documented fallback only on error.
    - Regression: Brownfield continuity and configured/default local storage paths behave as before on successful inputs.
  </behavior>
  <action>Add W04 and W40 to the fixture as fixed and capture RED. In continuity, bind the validating function's returned schema into the subsequent flow so the checked value is the used value. In local storage, replace truthiness fallback with an explicit `:code.priv_dir(:threadline)` result match, converting the success charlist as before and choosing `priv` only for the OTP error tuple. Do not suppress either warning and do not alter configured storage-path precedence. Reach GREEN with both targeted suites and seal the complete 10-warning fixture.</action>
  <verify>
    <automated>mix test test/threadline/continuity_brownfield_test.exs test/threadline/storage/local_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/query-storage.json</automated>
    <fails_when>W04 or W40 survives, schema validation is bypassed, the OTP error tuple can escape as a path, configured path precedence changes, or the fixture does not total exactly ten sealed warnings.</fails_when>
  </verify>
  <acceptance_criteria>
    - The fixture owns exactly W03, W04, and W33-W40 across the five declared origins.
    - All ten warnings are absent or, only if proven irreducible, carry a complete exact residue record for Plan 199-20.
    - Continuity and storage regression tests pass without an ignore-file change.
  </acceptance_criteria>
  <done>The entire 10-warning query/storage slice is dispositioned and certified within its five-file authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database structs → specs | Persisted audit records cross into static contracts used throughout query and operator code. |
| OTP application lookup → filesystem path | `:code.priv_dir/1` returns either a charlist path or an error tuple. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-199-16-01 | Tampering | query/change specs | medium | mitigate | Use exact concrete structs and preserve existing query regression tests; reject broad type substitutions. |
| T-199-16-02 | Elevation of privilege | local storage path | high | mitigate | Pattern-match OTP success/error results so an error tuple cannot be coerced into a filesystem location. |
| T-199-16-03 | Repudiation | 10-warning fixture | medium | mitigate | Require unique sealed IDs, exact authorized origins, and live post-analysis evidence through the shared verifier. |
| T-199-16-04 | Information disclosure | fixture raw lines | low | accept | Raw warnings contain repository paths and type shapes only; no runtime audit records or credentials are captured. |
</threat_model>

<verification>
Run both task commands and confirm the verifier reports exactly ten sealed warnings across exactly five authorized origins with no extra current warnings. Confirm `git diff --name-only ecc1c0a1 -- lib` for this plan is a subset of its warning authority.
</verification>

<success_criteria>
- W03, W04, and W33-W40 are exhaustively triaged and certified.
- Real continuity/storage defects are fixed, and nonexistent type references are replaced by exact existing types.
- No source or fixture overlaps another Wave 5 plan.
</success_criteria>

<output>
Create `.planning/phases/199-decouple/199-16-SUMMARY.md` when done.
</output>
