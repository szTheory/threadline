---
phase: 199-decouple
plan: "14"
type: execute
wave: 7
depends_on: [199-09, 199-11, 199-12, 199-20]
files_modified:
  - .github/workflows/ci.yml
  - mix.exs
  - CONTRIBUTING.md
  - test/threadline/ci_topology_contract_test.exs
  - test/threadline/ci_coverage_doc_contract_test.exs
  - test/threadline/dialyzer_ignore_contract_test.exs
autonomous: false
requirements: [DECOUPLE-07, DECOUPLE-08]
estimate: {tokens: 34000, raw_tokens: 34000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Dialyzer is unconditional locally and in protected CI with exact-toolchain cache lifecycle and measured cold/hit costs."
  artifacts:
    - {path: .github/workflows/ci.yml, provides: "Blocking current-lane Dialyzer job and aggregate dependency"}
  key_links:
    - {from: .github/workflows/ci.yml, to: test/threadline/ci_topology_contract_test.exs, via: "derived bidirectional roster/cache contract"}
  prohibitions: ["No advisory analyzer, optional aggregate edge, imprecise cache key, estimated timing, or missing authenticated provenance."]
---
<objective>
Wire and measure the final Dialyzer CI gate per D-28 through D-30.
Purpose: Local contributors and protected CI share one truthful, secure quality contract.
Output: Blocking Dialyzer topology/cache plus authenticated cold/hit measurements and documentation.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-RESEARCH.md
@.planning/phases/199-decouple/199-PATTERNS.md
@.planning/phases/199-decouple/199-VALIDATION.md
@.github/workflows/ci.yml
@bin/safe-temp-tree
</context>
## Artifacts this phase produces
- Blocking CI/local Dialyzer topology and authenticated cold/hit measurements.
<dependencies>
Needs: Completed CI prerequisites 199-09, 199-11, and 199-12 plus Plan 199-20's green source-owned Dialyzer ratchet. Halted Plan 199-13 remains immutable evidence and is not an execution dependency.
Creates: Final current-lane blocking CI gate, OTP/toolchain-keyed PLT cache, and measured cold/hit evidence consumed by Plan 199-21.
Execution order: Runs alone in Wave 7 after the bounded remediation chain; its maintainer checkpoint remains the only non-autonomous step.
</dependencies>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Wire one analyzer path from local alias through protected aggregate</name>
  <reversibility rating="reversible">CI topology and aliases can be reverted without data migration.</reversibility>
  <files>.github/workflows/ci.yml, mix.exs, CONTRIBUTING.md, test/threadline/ci_topology_contract_test.exs, test/threadline/ci_coverage_doc_contract_test.exs, test/threadline/dialyzer_ignore_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-CONTEXT.md — D-28, D-29
    - .planning/phases/199-decouple/199-PATTERNS.md — exact aggregate/cache analog
    - .github/workflows/ci.yml — current-lane constants and ci-required
  </read_first>
  <behavior>
    - `mix ci.all` blocks on Dialyzer and `ci-required.needs` unconditionally contains stable `verify-dialyzer`.
    - Cache key includes runner/OS, exact OTP/Elixir, mix.lock, mix.exs; build saves before analysis.
    - A cache miss measures the conditional PLT build and first analysis separately; every analysis run emits stable wall-time and peak-RSS fields for later authenticated collection.
  </behavior>
  <action>Add local analyzer command and current-lane job per D-28. Separate restore, conditional PLT build, save, and no-check analysis per D-29. On a cache miss, wrap `mix dialyzer --plt` with `/usr/bin/time -v -o` and emit normalized `THREADLINE_DIALYZER_PLT_WALL_SECONDS` and `THREADLINE_DIALYZER_PLT_MAX_RSS_KB` log fields before the cache-save step. On both miss and hit paths, wrap `mix dialyzer --no-check` independently and emit `THREADLINE_DIALYZER_ANALYSIS_WALL_SECONDS` and `THREADLINE_DIALYZER_ANALYSIS_MAX_RSS_KB`; also emit `THREADLINE_DIALYZER_PLT_CACHE=miss|hit`. Keep dependency-fetch/compile time outside both measurements, fail if GNU time output cannot be parsed, and do not synthesize PLT-build values on a hit. Update header/docs/coverage and derive topology assertions bidirectionally, including exact stable measurement marker names, PLT timing before save, analysis timing after save, near-miss restore restricted to same runner/OTP/Elixir, and no analyzer in the no-optional lane.</action>
  <acceptance_criteria>
    - Local/CI analyzer commands and aggregate edges agree.
    - Cache lifecycle/order and key dimensions have positive controls.
    - The cold path emits cache=miss plus separate numeric PLT-build and analysis wall-seconds/max-RSS markers; the hit path emits cache=hit plus numeric analysis wall-seconds/max-RSS markers and never fabricates PLT-build measurements.
    - Topology mutation controls fail if either timed command, any required marker, the fail-on-unparseable rule, or the build-before-save/analyze-after-save ordering is removed.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/ci_topology_contract_test.exs test/threadline/ci_coverage_doc_contract_test.exs test/threadline/dialyzer_ignore_contract_test.exs --max-failures 1 &amp;&amp; mix dialyzer --no-check</automated><fails_when>non-zero exit, zero tests, roster drift, cache-order defect, missing/unparseable stable PLT or analysis measurement field, dependency time folded into analyzer timing, fabricated hit-path PLT value, or warning</fails_when></verify>
  <done>One blocking analyzer path reaches both local and protected CI entrypoints.</done>
</task>
<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Maintainer publishes and dispatches the prepared measurement commit</name>
  <read_first>
    - .planning/phases/199-decouple/199-20-SUMMARY.md — green ratchet and full local analyzer evidence
    - .github/workflows/ci.yml — exact current-lane workflow to publish and dispatch
    - CONTRIBUTING.md — measurement evidence location and field contract
  </read_first>
  <precondition>`git push` from an agent session is blocked by the repository's local classifier; the analyzer wiring from Task 1 is committed on a prepared branch at a recorded local head SHA.</precondition>
  <action>Record the prepared branch name and local head SHA, then halt. The maintainer pushes that exact prepared commit/branch, lets the push-triggered current-lane run provide the cold-cache run, dispatches the identical commit once for the exact-cache-key hit, and supplies or records both resulting CI run URLs/IDs. Do not attempt the classifier-blocked push, ask for credentials, or ask for secrets.</action>
  <instructions>Push the prepared commit/branch, dispatch the identical commit once for the cache-hit run, and supply or record both resulting CI run URLs/IDs. Do not provide secrets.</instructions>
  <verification>The supplied cold and hit run URLs/IDs both resolve to the recorded local head SHA, and the remote branch head equals that SHA.</verification>
  <resume-signal>Reply with the two CI run URLs/IDs, or record them in the prepared evidence location and reply “ready”.</resume-signal>
  <reversibility rating="reversible">The prepared branch and measurement dispatch create no merge, release, or data migration.</reversibility>
  <verify><human-check>The maintainer confirms the exact prepared commit/branch was pushed and supplies or records the cold and exact-key-hit CI run URLs/IDs.</human-check><fails_when>the remote SHA differs, either run ID is missing, either run targets a different commit, or secrets are requested</fails_when></verify>
  <acceptance_criteria>
    - The remote branch head and both CI runs resolve to the exact Task-1 commit SHA.
    - One supplied run is the push-triggered cold-cache run and one is an explicit identical-commit dispatch for the exact-key hit.
    - The checkpoint records only branch/SHA/run identifiers and never credentials or secrets.
  </acceptance_criteria>
  <done>The maintainer-published identical commit has explicit cold and hit CI run identifiers ready for authenticated read-only inspection.</done>
</task>
<task type="auto">
  <name>Task 3: Retrieve and record authenticated cache-miss and exact-key-hit evidence</name>
  <reversibility rating="reversible">Measurement updates docs and execution-derived timeout only.</reversibility>
  <files>.github/workflows/ci.yml, CONTRIBUTING.md, test/threadline/ci_topology_contract_test.exs, test/threadline/ci_coverage_doc_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-RESEARCH.md — Open Questions (RESOLVED), authoritative CI fallback
  </read_first>
  <precondition>Task 2 completed with two immutable CI run URLs/IDs for the same recorded remote commit: the push-triggered cache miss and the identical-commit cache-hit dispatch.</precondition>
  <action>Resume after Task 2 and use authenticated read-only `gh` inspection of the supplied runs; do not push or dispatch from this task. Confirm both run heads equal the checkpoint SHA and capture immutable run URLs/IDs, commit SHA, runner image, OTP/Elixir, mix.lock/mix.exs hashes, separate PLT/analysis wall time, and peak RSS. Prove the first run's miss predicate and the second run's exact-key hit predicate, then derive timeout from cold total with explicit documented headroom. Docs cannot pass with estimates, placeholders, mismatched commits, or unauthenticated/unlinked evidence.</action>
  <acceptance_criteria>
    - Two authenticated runs reference the identical commit and prove miss then hit predicates.
    - CONTRIBUTING records all provenance/cost fields and CI timeout matches documented formula.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/ci_topology_contract_test.exs test/threadline/ci_coverage_doc_contract_test.exs --max-failures 1</automated><fails_when>non-zero exit, zero tests, unauthenticated/unlinked evidence, or formula drift</fails_when></verify>
  <done>Dialyzer cost, memory, cache benefit, and timeout are measured rather than guessed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CI cache → analyzer | Persisted PLT state can be stale or poisoned. |
| GitHub evidence → docs | External measurements become durable claims. |
| Maintainer push/dispatch → authenticated inspection | Classifier-blocked remote mutation must complete before read-only evidence retrieval. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-199-27 | Tampering | PLT cache | high | mitigate | Exact toolchain/config key, ordered build/save/analyze, topology controls. |
| T-199-28 | Repudiation | measurements | high | mitigate | Authenticated immutable run evidence and exact SHA/hashes. |
| T-199-28A | Spoofing | push/dispatch handoff | high | mitigate | Blocking human-action checkpoint plus remote-head and two-run SHA equality before inspection. |
| T-199-30 | Denial of service | aggregate | medium | mitigate | Measured timeout with explicit cold-run headroom. |
</threat_model>
<verification>Run topology/coverage/ignore tests and complete the authenticated cache-miss/cache-hit measurement pair for one exact commit.</verification>
<success_criteria>Dialyzer is a blocking current-lane local/CI gate whose exact cache lifecycle and cold/hit costs are proven and documented.</success_criteria>
<output>Create `.planning/phases/199-decouple/199-14-SUMMARY.md` when done.</output>
