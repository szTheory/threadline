---
phase: 199-decouple
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/e2e/package.json
  - examples/threadline_phoenix/e2e/support/operator-surface-paths.ts
  - examples/threadline_phoenix/e2e/support/operator-surface-paths.test.ts
  - examples/threadline_phoenix/e2e/critic/run.ts
autonomous: true
requirements: [DECOUPLE-01]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths: ["One ESM adapter owns stable TypeScript evidence paths and symlink-safe read/write containment."]
  artifacts:
    - {path: examples/threadline_phoenix/e2e/support/operator-surface-paths.ts, provides: "Shared ESM path authority"}
  key_links:
    - {from: examples/threadline_phoenix/e2e/critic/run.ts, to: examples/threadline_phoenix/e2e/support/operator-surface-paths.ts, via: "imported roots"}
  prohibitions: ["No process.cwd authority, string-prefix containment, input/output alias, or divergent writer helper."]
---
<objective>
Create the shared TypeScript path/containment tracer per D-06 and D-08 through D-12.
Purpose: Every critic/capture consumer can share one least-surprising ESM contract.
Output: Tested source-anchored adapter and one wired consumer.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-RESEARCH.md
@.planning/phases/199-decouple/199-PATTERNS.md
@examples/threadline_phoenix/e2e/critic/run.ts
</context>
## Artifacts this phase produces
- Shared `operator-surface-paths.ts` plus executable node:test coverage.
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Resolve one reader from import.meta.url under arbitrary cwd</name>
  <reversibility rating="reversible">Internal E2E exports can evolve before the atomic root flip.</reversibility>
  <files>examples/threadline_phoenix/e2e/package.json, examples/threadline_phoenix/e2e/support/operator-surface-paths.ts, examples/threadline_phoenix/e2e/support/operator-surface-paths.test.ts, examples/threadline_phoenix/e2e/critic/run.ts</files>
  <read_first>
    - .planning/phases/199-decouple/199-PATTERNS.md — exact ESM anchor analog
    - examples/threadline_phoenix/e2e/critic/run.ts — current independent root
  </read_first>
  <behavior>
    - Import-meta anchored read works from root, nested cwd, and worktree.
    - Missing/malformed required data fails with D-12 diagnostics.
  </behavior>
  <action>Build the adapter from `fileURLToPath(import.meta.url)` and export named immutable/generated roots, initially pointing at the tracked source corpus. Add grounded package scripts `test:paths`=`tsx --test support/operator-surface-paths.test.ts`, `typecheck`=`tsc --noEmit`, and `critic:check`=`tsx critic/run.ts validate`, then wire `run.ts` as the real tracer. Flags override deterministic defaults without env service location per D-09/D-10.</action>
  <acceptance_criteria>
    - Tests change cwd and still resolve identical evidence.
    - run.ts owns no independent repository-root calculation.
  </acceptance_criteria>
  <verify><automated>npm --prefix examples/threadline_phoenix/e2e run test:paths</automated><fails_when>non-zero exit or zero node tests</fails_when></verify>
  <done>One real critic reader uses the shared stable adapter.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Seal canonical containment and atomic-write primitives</name>
  <reversibility rating="reversible">Internal helpers carry no stored schema.</reversibility>
  <files>examples/threadline_phoenix/e2e/support/operator-surface-paths.ts, examples/threadline_phoenix/e2e/support/operator-surface-paths.test.ts</files>
  <read_first>
    - .planning/phases/199-decouple/199-RESEARCH.md — path confinement and atomic replacement requirements
    - .planning/phases/199-decouple/199-PATTERNS.md — alias/symlink positive-control pattern
  </read_first>
  <behavior>
    - Relative/canonical containment rejects traversal, absolute escape, prefix confusion, symlinks, and immutable/output aliasing.
    - Atomic write preserves original and removes temp on forced failure.
  </behavior>
  <action>Implement one shared `path.relative` plus realpath-existing-parent boundary and one sibling-temp/fsync/close/rename primitive per D-04/D-06/D-11. Test every hostile path and forced-failure cleanup; later plans consume these exact exports without duplicating logic.</action>
  <acceptance_criteria>
    - All path mutation controls fail and valid nested target succeeds.
    - Failure leaves no temp and no partial target.
  </acceptance_criteria>
  <verify><automated>npm --prefix examples/threadline_phoenix/e2e run test:paths</automated><fails_when>non-zero exit, escape accepted, or temp leak</fails_when></verify>
  <done>The shared adapter owns the complete safe read/write boundary.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CLI/ID path → Node filesystem | Inputs select evidence and generated outputs. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-199-07 | Tampering | path adapter | high | mitigate | Relative+realpath containment, alias separation, mutation controls. |
| T-199-08 | Tampering | atomic writer | high | mitigate | Sibling temp, fsync, close, rename, forced-failure test. |
</threat_model>
<verification>Run the adapter test script from root and nested cwd.</verification>
<success_criteria>TypeScript has one safe, stable path authority.</success_criteria>
<output>Create `.planning/phases/199-decouple/199-04-SUMMARY.md` when done.</output>
