---
phase: 199-decouple
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/198-green-bringup/198-41-PLAN.md
  - .planning/phases/199-decouple/199-REMOVAL-INVENTORY.md
  - test/threadline/removed_artifact_contract_test.exs
  - test/threadline/readme_doc_contract_test.exs
files_deleted:
  - .planning/ROADMAP.md.bak
  - .planning/HANDOFF.json
  - update_roadmap.rb
  - fix_tests.exs
autonomous: true
requirements: [DECOUPLE-03, DECOUPLE-04]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths: ["Exactly four dead artifacts are removed with provenance/citation repair, and a live scanner plus README assertion prevents regression."]
  artifacts:
    - {path: .planning/phases/199-decouple/199-REMOVAL-INVENTORY.md, provides: "Purpose, supersession, and recovery SHA"}
    - {path: test/threadline/removed_artifact_contract_test.exs, provides: "Synthetic-first then live deletion/citation scanner"}
  key_links:
    - {from: test/threadline/removed_artifact_contract_test.exs, to: git ls-files, via: "derived tracked/executable/document set"}
  prohibitions: ["No broad deletion, archive junk drawer, stale live citation, disabled README assertion, or one-pass stale-path proof."]
---
<objective>
Remove dead tracked artifacts and restore documentation truth per D-13 through D-16 and D-21.
Purpose: Active HEAD stays honest while git/planning history preserves provenance.
Output: Four exact removals, repaired citation, removal inventory, and live contracts.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-RESEARCH.md
@.planning/phases/199-decouple/199-PATTERNS.md
@.planning/ARCHIVE-REGISTER.md
@test/threadline/readme_doc_contract_test.exs
</context>
## Artifacts this phase produces
- Exact removal inventory, historical addendum, restored README contract, and live scanner.
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove scanner core synthetically and restore README wording contract</name>
  <reversibility rating="reversible">Only test helpers and a currently disabled assertion change.</reversibility>
  <files>test/threadline/removed_artifact_contract_test.exs, test/threadline/readme_doc_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-PATTERNS.md — durable derivation/mutation analogs
    - .planning/ARCHIVE-REGISTER.md — durable recovery identity format
    - test/threadline/readme_doc_contract_test.exs — disabled assertion helper
  </read_first>
  <behavior>
    - Scanner core rejects a temporary tracked/executable/doc tree containing a removed-path citation and accepts a clean tree.
    - README contract asserts current canonical wording and a mutated temporary string fails.
  </behavior>
  <action>D-15: implement scanner logic against injected/synthetic file sets only; do not assert the live repository yet because four targets remain tracked. Derive target/consumer classes and report file+line. Restore the commented README assertion against current wording and prove its teeth with temporary mutated content, never by editing README. Use `199-PATTERNS.md` and `ARCHIVE-REGISTER.md` as durable analogs, not a file retired by Plan 199-12.</action>
  <acceptance_criteria>
    - Synthetic stale path fails and benign diagnostic prose without live dependency passes.
    - Current README assertion passes and mutation control fails.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/removed_artifact_contract_test.exs test/threadline/readme_doc_contract_test.exs -x</automated><fails_when>non-zero exit, zero tests, or synthetic stale path passes</fails_when></verify>
  <done>Scanner mechanics and README truth are proven without contradicting current tracked state.</done>
</task>
<task type="auto">
  <name>Task 2: Repair provenance, delete four literals, and activate the live invariant</name>
  <reversibility rating="reversible">Exact files remain recoverable at recorded full SHAs.</reversibility>
  <files>.planning/ROADMAP.md.bak, .planning/HANDOFF.json, update_roadmap.rb, fix_tests.exs, .planning/phases/198-green-bringup/198-41-PLAN.md, .planning/phases/199-decouple/199-REMOVAL-INVENTORY.md, test/threadline/removed_artifact_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-CONTEXT.md — D-13 through D-16, D-21
    - .planning/phases/198-green-bringup/198-41-PLAN.md — citation requiring minimal addendum
  </read_first>
  <action>D-14: preflight each target with `git ls-files`, consumer search, and `git log --follow`; record purpose, last use, superseding evidence, and full recovery SHA. Add the minimal historical handoff addendum, then `git rm --` the four literal paths in the same task and activate the live scanner. Run its actual stale-path scan once; snapshot `git status --porcelain=v1 --untracked-files=all` and `git diff --binary`. Run the exact scan a second time, then prove both snapshots are byte-identical to establish idempotence. Do not delete untracked scratch or rewrite history.</action>
  <acceptance_criteria>
    - Exactly four targets are untracked/absent and inventoried.
    - No active executable/doc dependency remains.
    - The actual scanner runs twice; second run leaves status and diff byte-identical.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/removed_artifact_contract_test.exs test/threadline/readme_doc_contract_test.exs -x &amp;&amp; mix verify.format</automated><fails_when>non-zero exit, zero tests, missing target, stale citation, or second-run snapshot drift</fails_when></verify>
  <done>Deletion, provenance, citation repair, live assertion, and idempotence land atomically.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Inventory → git removal | Derived evidence authorizes exact destructive operations. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-199-17 | Tampering | deletion scope | high | mitigate | Literal operands, tracked/consumer/history preflight, live scanner. |
| T-199-18 | Repudiation | historical citation | high | mitigate | Minimal addendum and full recovery SHAs. |
</threat_model>
<verification>Focused contracts pass and actual scan is demonstrably idempotent.</verification>
<success_criteria>Dead artifacts disappear without losing history or weakening live documentation truth.</success_criteria>
<output>Create `.planning/phases/199-decouple/199-09-SUMMARY.md` when done.</output>
