---
phase: 199-decouple
plan: 17
type: execute
wave: 5
depends_on: [199-15]
files_modified:
  - test/fixtures/dialyzer/export-investigation.json
  - lib/threadline/export.ex
  - lib/threadline/export/orchestrator.ex
  - lib/threadline/integrations/sigra.ex
  - lib/threadline/investigation/incident_bundle.ex
  - lib/threadline/investigation/linked_change.ex
autonomous: true
requirements: [DECOUPLE-07, DECOUPLE-08]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All fifteen sealed warnings W06-W20 are explicitly dispositioned within five authorized origins."
    - "Export byte contracts, file-close cleanup, and file-removal cleanup describe and handle their real return values."
    - "Sigra and investigation contracts use reachable clauses and concrete source-tree struct types without changing integration behavior."
  artifacts:
    - path: test/fixtures/dialyzer/export-investigation.json
      provides: "Exact authority, disposition, and proof for the 15-warning export/investigation slice"
    - path: lib/threadline/export/orchestrator.ex
      provides: "Explicit close/remove result handling on export lifecycle paths"
    - path: lib/threadline/integrations/sigra.ex
      provides: "Reachable scalar identity handling and existing ActorRef struct contracts"
  key_links:
    - from: lib/threadline/export.ex
      to: NimbleCSV.RFC4180
      via: "concrete iodata normalization before public binary conversion"
    - from: lib/threadline/export/orchestrator.ex
      to: File
      via: "explicit close/remove result handling without masking the primary export result"
    - from: test/fixtures/dialyzer/export-investigation.json
      to: bin/verify-dialyzer-slice
      via: "shared bounded-remediation contract"
---

<objective>
Remediate the fifteen sealed export, Sigra, and investigation warnings as one data-shape and resource-lifecycle slice.

Purpose: Correct real cleanup/return handling and align integration/investigation specs with reachable source-tree shapes per D-23, while preserving exact residue evidence per D-26 and D-27.
Output: A sealed 15-warning slice fixture and narrow changes to exactly five warning-origin files.

Warning authority: exactly `lib/threadline/export.ex` (W06-W07), `lib/threadline/export/orchestrator.ex` (W08-W10), `lib/threadline/integrations/sigra.ex` (W11-W13), `lib/threadline/investigation/incident_bundle.ex` (W14-W15), and `lib/threadline/investigation/linked_change.ex` (W16-W20). No other warning-origin source file may be edited by this plan.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
@.planning/phases/199-decouple/199-13-SUMMARY.md
@.planning/phases/199-decouple/199-15-SUMMARY.md
@bin/verify-dialyzer-slice
</context>

## Artifacts this phase produces

- `test/fixtures/dialyzer/export-investigation.json`: complete W06-W20 sealed-to-current evidence.
- Concrete export serialization and explicit orchestrator cleanup-result handling.
- Reachable Sigra identity clauses and exact investigation struct contracts.

<dependencies>
Needs: Plan 199-15's fixture schema and executable verifier.
Creates: One disjoint remediation result consumed by Plan 199-20.
Parallel safety: Shares no modified file with Plans 199-16, 199-18, or 199-19 and may run alongside them in Wave 5.
</dependencies>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make export bytes and cleanup returns explicit</name>
  <files>test/fixtures/dialyzer/export-investigation.json, lib/threadline/export.ex, lib/threadline/export/orchestrator.ex</files>
  <read_first>
    - .planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
    - test/fixtures/dialyzer/critic-tooling.json
    - lib/threadline/export.ex
    - lib/threadline/export/orchestrator.ex
    - test/threadline/export_test.exs
    - test/threadline/export/orchestrator_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/export.ex lib/threadline/export/orchestrator.ex` succeeds.</precondition>
  <behavior>
    - RED: W06-W10 remain after the fixture first marks them fixed.
    - GREEN: Export formatting preserves exact CSV bytes while narrowing the internal NimbleCSV result to the concrete iodata shape actually converted to binary.
    - GREEN: File close and cleanup removal outcomes are explicitly matched; cleanup failure cannot silently replace a primary export error or success.
    - Regression: Header, change-row, successful export, failed export, and cleanup behaviors retain their public results.
  </behavior>
  <action>Create the slice fixture with W06-W10 exact sealed evidence, mark them fixed, and capture RED. In `Threadline.Export`, normalize the NimbleCSV return at the internal boundary to a concrete iodata/list shape while preserving the public binary specs and exact output bytes; do not widen public return types to include streams. In the orchestrator, handle each `File.close/1` and `File.rm/1` result deliberately, preserving the established primary result and cleanup ordering and surfacing/logging cleanup failure according to existing error conventions. Reach GREEN with export/orchestrator tests and the live slice verifier. This implements D-23 without changing export semantics to appease analysis.</action>
  <verify>
    <automated>mix test test/threadline/export_test.exs test/threadline/export/orchestrator_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/export-investigation.json</automated>
    <fails_when>Any of W06-W10 survives, CSV bytes change, cleanup outcomes remain discarded, primary result precedence changes, or a warning outside the two origins appears.</fails_when>
  </verify>
  <acceptance_criteria>
    - W06-W10 are absent and recorded as fixed with post-analysis evidence.
    - CSV public contracts remain binary and byte-for-byte tests pass.
    - Close/removal results have explicit behavior on success and failure paths.
  </acceptance_criteria>
  <done>The export lifecycle is analyzer-clean within its two origins and retains its tested bytes and result semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align Sigra and investigation contracts with reachable structs</name>
  <files>test/fixtures/dialyzer/export-investigation.json, lib/threadline/integrations/sigra.ex, lib/threadline/investigation/incident_bundle.ex, lib/threadline/investigation/linked_change.ex</files>
  <read_first>
    - test/fixtures/dialyzer/export-investigation.json
    - lib/threadline/integrations/sigra.ex
    - lib/threadline/investigation/incident_bundle.ex
    - lib/threadline/investigation/linked_change.ex
    - lib/threadline/semantics/actor_ref.ex
    - lib/threadline/capture/audit_change.ex
    - lib/threadline/capture/audit_transaction.ex
    - lib/threadline/semantics/audit_action.ex
    - test/threadline/integrations/sigra_test.exs
    - test/threadline/investigation_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/integrations/sigra.ex lib/threadline/investigation/incident_bundle.ex lib/threadline/investigation/linked_change.ex` succeeds.</precondition>
  <behavior>
    - RED: W11-W20 remain after their fixed dispositions are added.
    - GREEN: Sigra uses the existing `%ActorRef{}` contract and removes only the unreachable scalar-id fallback after callers are proven map-shaped.
    - GREEN: Incident bundle and linked change use concrete audit struct contracts instead of nonexistent remote `.t()` types.
    - Regression: Sigra payloads, incident bundles, and linked-change output retain their existing tested shapes.
  </behavior>
  <action>Add W11-W20 to the fixture as fixed and capture RED. Replace Sigra's invalid `ActorRef.t()` references with `%ActorRef{}` and remove the scalar-id fallback only after confirming every in-module call passes the already-supported map shape; retain the reachable clauses and output. Replace the investigation modules' invalid audit remote types with `%AuditChange{}`, `%AuditTransaction{}`, and `%AuditAction{}`. Do not edit those type-provider modules, add broad types, or expand integration scope. Reach GREEN with the Sigra and investigation suites, then seal exactly fifteen warnings across the five authorized origins per D-23 and D-26.</action>
  <verify>
    <automated>mix test test/threadline/integrations/sigra_test.exs test/threadline/investigation_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/export-investigation.json</automated>
    <fails_when>Any of W11-W20 survives, a reachable Sigra input loses support, payload/bundle output changes, an out-of-authority type provider is edited, or the fixture total is not fifteen.</fails_when>
  </verify>
  <acceptance_criteria>
    - The fixture owns exactly W06-W20 across exactly five origins.
    - Sigra's removed branch is proven unreachable by current callers and its output tests pass.
    - Investigation contracts use exact concrete structs and no ignore entry is introduced without irreducibility evidence.
  </acceptance_criteria>
  <done>The 15-warning export/investigation slice is exhaustively dispositioned and certified inside its five-file authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structured audit rows → CSV bytes | Potentially sensitive records are serialized across a format boundary. |
| Export process → filesystem | File descriptors and temporary paths require reliable cleanup. |
| Threadline actors/audit structs → Sigra/investigation | Internal typed records cross into integration and incident representations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-199-17-01 | Information disclosure | export cleanup | high | mitigate | Preserve cleanup ordering, explicitly handle close/remove failures, and exercise failed-export cleanup tests. |
| T-199-17-02 | Tampering | CSV normalization | medium | mitigate | Retain byte-for-byte export tests and public binary contracts while narrowing only the internal library return. |
| T-199-17-03 | Spoofing | Sigra scalar identity | medium | mitigate | Remove only the statically unreachable fallback and retain tested map-shaped identity extraction. |
| T-199-17-04 | Repudiation | 15-warning fixture | medium | mitigate | Bind every disposition to its original ID/raw line, authorized origin, test evidence, and live analyzer result. |
</threat_model>

<verification>
Run both task commands and confirm the verifier reports exactly fifteen sealed warnings across exactly five authorized origins with no extra current warnings. Confirm no warning-origin source outside the declared authority changed.
</verification>

<success_criteria>
- W06-W20 are exhaustively triaged and certified.
- Export resource handling and byte contracts are explicit and tested.
- Sigra/investigation contracts are reachable and source-tree truthful without behavior drift.
</success_criteria>

<output>
Create `.planning/phases/199-decouple/199-17-SUMMARY.md` when done.
</output>
