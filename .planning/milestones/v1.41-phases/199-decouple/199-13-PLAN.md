---
phase: 199-decouple
plan: "13"
type: execute
wave: 4
depends_on: [199-08]
files_modified:
  - mix.exs
  - mix.lock
  - .dialyzer_ignore.exs
  - test/threadline/dialyzer_ignore_contract_test.exs
  - .planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
autonomous: true
requirements: [DECOUPLE-07, DECOUPLE-08]
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low}
must_haves:
  truths: ["Full optional-app Dialyzer blocks immediately; warning fixes/ignores are individually triaged and the exact ignore ceiling only tightens."]
  artifacts:
    - {path: .planning/phases/199-decouple/199-DIALYZER-TRIAGE.md, provides: "Complete first-run warning disposition"}
    - {path: test/threadline/dialyzer_ignore_contract_test.exs, provides: "Strict shape/rationale/ceiling/unused-filter ratchet"}
  key_links:
    - {from: mix.exs, to: .dialyzer_ignore.exs, via: "dialyzer ignore_warnings setting"}
  prohibitions: ["No advisory period, broad snapshot, regex/file/class ignore, global unknown suppression, or estimated ceiling."]
---
<objective>
Bootstrap a blocking full-build Dialyzer ratchet per D-22 through D-27.
Purpose: Later refactors begin behind a trustworthy static-analysis gate.
Output: Dialyxir config, warning triage, narrow fixes, strict ignores, and executable ratchet.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-RESEARCH.md
@.planning/phases/199-decouple/199-PATTERNS.md
@mix.exs
</context>
## Artifacts this phase produces
- Dev/test Dialyxir dependency, full-app PLT config, triage ledger, and strict ratchet.
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Capture and triage the first full optional-app analysis</name>
  <reversibility rating="reversible">Static-analysis configuration and at most 14 pre-counted, distinct discovered warning-origin source files can be reverted; discovering 15 requires re-planning before any such source edit.</reversibility>
  <files>mix.exs, mix.lock, .dialyzer_ignore.exs, test/threadline/dialyzer_ignore_contract_test.exs, .planning/phases/199-decouple/199-DIALYZER-TRIAGE.md; only exact warning-origin source paths recorded in the triage before modification</files>
  <read_first>
    - .planning/phases/199-decouple/199-CONTEXT.md — D-22 through D-27
    - .planning/phases/199-decouple/199-RESEARCH.md — resolved warning triage and Dialyxir configuration
    - .planning/phases/199-decouple/199-PATTERNS.md — MODE-B ratchet analog
    - mix.exs — optional apps and placeholder dialyzer config
  </read_first>
  <behavior>
    - PLT includes mix, ex_unit, and all nine named optional apps; analysis retains unknown and adds unmatched_returns/extra_return.
    - First raw warning set is sealed before edits; every warning gets exact origin and disposition.
  </behavior>
  <action>D-23: add official Dialyxir as dev/test runtime:false and configure the full app list per D-22/D-24/D-25. Run first full build/analysis and record raw command/toolchain/output digest before changing warning-origin code. Before any warning-origin source edit, derive and seal the complete set of distinct source files discovered as warning origins by that raw analysis, record every exact path in the triage, and count the set. If discovery reaches 15 files or more, stop with zero warning-origin source edits and require Phase 199 re-planning/re-slicing; at most 14 distinct discovered warning-origin source files may enter this plan. Add an executable triage-contract assertion and a 15-path positive control for this cap. Only after a count of 14 or fewer may the executor fix every small clearly correct finding, with each edited path already present in the sealed discovered set and task commit scope. Only Phase-203 architectural warnings may become exact suppressions with reason, upstream/version where applicable, and removal trigger.</action>
  <acceptance_criteria>
    - Triage accounts for every initial warning exactly once.
    - Before the first warning-origin source edit, the sealed count of distinct source files discovered as warning origins is at most 14; discovery of 15 or more halts with no warning-origin source edits and a re-planning/re-slicing requirement.
    - Full analysis is green after narrow fixes and approved exact ignores.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/dialyzer_ignore_contract_test.exs -x &amp;&amp; mix dialyzer</automated><fails_when>non-zero exit, zero tests, untriaged warning, omitted optional app, unsealed discovered warning-origin path, 15 or more distinct discovered warning-origin source files, a 15-path mutation being accepted, or any warning-origin edit occurring before the count</fails_when></verify>
  <done>The actual first warning set is fully triaged and the analyzer blocks cleanly.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Seal the execution-derived strict ignore ceiling</name>
  <reversibility rating="reversible">The ceiling can only decrease as warnings are fixed.</reversibility>
  <files>.dialyzer_ignore.exs, test/threadline/dialyzer_ignore_contract_test.exs, mix.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-RESEARCH.md — resolved execution-derived ceiling
    - .planning/phases/199-decouple/199-PATTERNS.md — AST/comments parser and positive controls
  </read_first>
  <behavior>
    - Only literal `{file, warning_description}` entries with immediately preceding rationale are accepted.
    - Ceiling equals approved entry count, cannot increase, and unused filters fail.
  </behavior>
  <action>Parse source with comments, reject regex/file-wide/class/wildcard/duplicates, and set the committed ceiling to the actual approved count per D-26/D-27. Add positive controls for appended strict entry and broadened entry; invoke `mix dialyzer --no-check --list-unused-filters`. Do not evaluate arbitrary ignore code.</action>
  <acceptance_criteria>
    - Empty ignore file is valid if triage needs none; otherwise every entry is exact and justified.
    - Both ceiling and shape mutations fail and unused-filter check passes.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/dialyzer_ignore_contract_test.exs -x &amp;&amp; mix dialyzer --no-check --list-unused-filters</automated><fails_when>non-zero exit, zero tests, mutation accepted, or unused filter</fails_when></verify>
  <done>The ignore budget is exact, reviewable, and tighten-only.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Dependency/type metadata → PLT | Optional app coverage controls soundness. |
| Warning output → suppressions | Broad filters could conceal defects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-199-25 | Tampering | ignore file | high | mitigate | AST/comment shape, exact ceiling, broadened-entry tests. |
| T-199-26 | Repudiation | warning triage | high | mitigate | Sealed raw digest and complete per-warning ledger. |
| T-199-26A | Tampering | warning-fix scope | high | mitigate | Seal and count every distinct discovered warning-origin path before editing; hard stop and re-plan at 15, with a positive control. |
</threat_model>
<verification>Run full analysis, strict contract, and unused-filter mode.</verification>
<success_criteria>Dialyzer is green, full-build, blocking, and ratcheting before CI wiring.</success_criteria>
<output>Create `.planning/phases/199-decouple/199-13-SUMMARY.md` when done.</output>
