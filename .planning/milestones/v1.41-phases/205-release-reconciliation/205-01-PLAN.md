---
phase: 205-release-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .github/workflows/release.yml
  - .release-please-manifest.json
  - CHANGELOG-GENERATED.md
  - CHANGELOG.md
  - CONTRIBUTING.md
  - README.md
  - bin/verify-bump-rehearsal
  - guides/adoption-evidence-playbook.md
  - guides/adoption-pilot-backlog.md
  - guides/configuration-and-commands.md
  - guides/evaluating-threadline.md
  - guides/getting-started-saas.md
  - guides/operator-surface.md
  - lib/mix/tasks/threadline.install.ex
  - mix.exs
  - test/mix/tasks/threadline/install_test.exs
  - test/threadline/changelog_contract_test.exs
  - test/threadline/storage_schema_test.exs
  - test/threadline/release_control_plane_contract_test.exs
autonomous: true
requirements: [RELEASE-02, RELEASE-05]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "HEAD holds exactly one new merge commit whose parents are the recorded pre-merge SHA and origin/main 471ebf6e67365e2ad16a1b8dbac991e5da1ec3b4. `git merge-base --is-ancestor v0.10.1 HEAD` and `git merge-base --is-ancestor v0.10.0 HEAD` both exit 0, and `git rev-list --count HEAD..origin/main` prints 0 (RELEASE-02, audit F1)."
    - "Every conflicted path was resolved as a whole file by the 205-RESEARCH rule table. The 10 Group A files are byte-identical to the pre-merge HEAD. The 4 Group B files are byte-identical to origin/main. mix.exs is the pre-merge HEAD file with only `@version \"0.9.0\"` changed to `\"0.10.1\"` (blob 6404f91ae5e0e999ac713db839cea32f708835f0). install.ex is origin's #46 file with 204-07's `repo_migrations_path/1` flatten re-applied. bin/verify-bump-rehearsal keeps #44's stand-ins and simulation and 204-02's `derived_doc_contract_tests`."
    - "The merge commit changes exactly these 17 tracked paths relative to the pre-merge SHA: .github/workflows/release.yml, .release-please-manifest.json, CHANGELOG-GENERATED.md, CHANGELOG.md, README.md, bin/verify-bump-rehearsal, the six guides (adoption-evidence-playbook, adoption-pilot-backlog, configuration-and-commands, evaluating-threadline, getting-started-saas, operator-surface), lib/mix/tasks/threadline.install.ex, mix.exs, test/mix/tasks/threadline/install_test.exs, test/threadline/changelog_contract_test.exs, test/threadline/storage_schema_test.exs. No lib/threadline/** file and no .planning/** file changes."
    - "Version metadata is 0.10.1 on the branch and owned by automation: mix.exs `@version \"0.10.1\"`, .release-please-manifest.json `\".\": \"0.10.1\"`, and `MIX_ENV=dev mix release.pins --check` reports 0 differing pin sites (RELEASE-02)."
    - "The release flow is whole on the branch: release.yml has the `sync-release-pr-pins` job (needs release-please, runs `mix release.pins`, checkout `persist-credentials: false`, `PUSH_TOKEN` only in the push step env, job concurrency group `sync-release-pr-pins`), and `bootstrap-release-pr-ci` has `needs: [release-please, sync-release-pr-pins]` with `always()`. A new contract test in release_control_plane_contract_test.exs pins all of that and was proven to fail against the pre-merge release.yml (RELEASE-02, research Open Question 1 decided yes)."
    - "The installer carries #46's CR-01 fix and not the old advice: `grep -c 'recommend_dedicated_storage_schema' lib/mix/tasks/threadline.install.ex` prints 3 and no zero-arity `recommend_dedicated_storage_schema()` call exists."
    - "The retired `verify.doc_contract` alias did not come back: `git grep -n 'verify\\.doc_contract' -- ':!.planning' ':!CHANGELOG.md' ':!CHANGELOG-GENERATED.md'` prints nothing."
    - "Phase gates are green on the committed HEAD: format, `MIX_ENV=test mix verify.credo`, `compile --warnings-as-errors`, `verify.xref_cycles`, `verify.compile_no_optional`, `verify.threadline`, `MIX_ENV=dev mix verify.dialyzer` (0 errors), full `mix test` with 0 failures, `release.pins --check`, `verify.example`, `mix verify.bump_rehearsal` printing `0.10.1 -> 0.11.0` and `Bump rehearsal OK`, and `mix verify.release` exiting 0 in a fresh `git clone --no-local` under /Users/jon/.claude/jobs/77cf1bdd/tmp (RELEASE-05)."
    - "Rendered output is unchanged: no lib/threadline/**, test/fixtures/** or examples/** file differs from the pre-merge SHA, and the 201/204 byte-lock and render contracts pass inside the full suite with no repin."
  artifacts:
    - lib/mix/tasks/threadline.install.ex
    - bin/verify-bump-rehearsal
    - mix.exs
    - .github/workflows/release.yml
    - test/threadline/release_control_plane_contract_test.exs
    - test/mix/tasks/threadline/install_test.exs
  key_links:
    - "mix.exs `@version` → `bin/verify-bump-rehearsal` computes CURRENT from it and NEXT as the next minor `.0` → the rehearsal runs 0.10.1 -> 0.11.0 instead of the already-published 0.9.0 -> 0.10.0 (audit F2)."
    - "release.yml `release-please` → `sync-release-pr-pins` (`mix release.pins` then `--check`) → `bootstrap-release-pr-ci` `needs: [release-please, sync-release-pr-pins]` with `always()` → the release PR is green by construction."
    - "`Mix.Tasks.Threadline.Install.run/1` → `generate/4` collects written files → `recommend_dedicated_storage_schema/1` prints advice only after generation, and prints nothing when nothing was written → pinned by test/mix/tasks/threadline/install_test.exs."
    - "release_control_plane_contract_test.exs `job_block!/2` over release.yml → the new sync-pins test runs in `verify.test`, which `ci.all` and `ci-required` include."
  prohibitions:
    - statement: "Exactly one merge commit, made with `git merge --no-ff --no-commit origin/main`. No rebase, no cherry-pick, no history rewrite, no squash, no `-X ours` or `-X theirs` strategy option, no `git reset --hard`."
      status: unverified
      flagged: true
    - statement: "Conflicts are resolved as whole files per the rule table, never by editing only the marker hunks."
      status: unverified
      flagged: true
    - statement: "Staging is by explicit path only. `git add .planning/`, `git add -A`, `git add .` and `git commit -a` are forbidden."
      status: unverified
      flagged: true
    - statement: ".planning/WINDOWS.md, .planning/config.json, the untracked .tool-versions and .gsd/ are not modified, staged, moved, stashed, replaced or restored. If git refuses the merge because of them, the executor halts."
      status: unverified
      flagged: true
    - statement: "No `--no-verify` and no `core.hooksPath` override."
      status: unverified
      flagged: true
    - statement: "Nothing is pushed, tagged or published: no `git push`, no `git tag`, no `git fetch`, no `mix hex.publish`, no gh release or PR operation."
      status: unverified
      flagged: true
    - statement: "Any precondition failure halts the plan. The executor never works around one."
      status: unverified
      flagged: true
    - statement: "Rendered output does not change. No byte-lock, render-contract, screenshot baseline or scorecard fixture is repinned or regenerated."
      status: unverified
      flagged: true
    - statement: "`mix verify.release` runs only in a fresh `git clone --no-local` under /Users/jon/.claude/jobs/77cf1bdd/tmp, never by cleaning the real working tree."
      status: unverified
      flagged: true
---

<objective>
Close v1.41 audit finding F1 on the milestone branch. Merge `origin/main` (471ebf6e, which carries the v0.10.0 and v0.10.1 tags) into branch `fix/branch-protection-actions-capability` as one real merge commit, resolving the 17 conflicted paths as whole files per the 205-RESEARCH rule table. Then land three small follow-up commits: the credo alias the merged installer test needs, a contract test that pins the `sync-release-pr-pins` job, and a one-row CONTRIBUTING wording truth fix. Finally, prove every release, changelog, version-truth and 203/204 gate green on the committed HEAD, including the bump rehearsal (now 0.10.1 -> 0.11.0) and `verify.release` in a clean clone.

Purpose: RELEASE-02 and RELEASE-05 were verified only on origin/main. After this plan they hold on the branch itself, and the next release PR cut from this tree is green by construction.

Output: one merge commit, three follow-up commits, and a gate table in 205-01-SUMMARY.md.

Out of scope: the ai-integration, assumption-delta and schema-gate contributions are detection-based and do not apply here (no external API integration, no identity-model transition, no ORM schema file).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-release-reconciliation/205-RESEARCH.md
@.planning/phases/205-release-reconciliation/205-VALIDATION.md
@.planning/v1.41-MILESTONE-AUDIT.md
@CLAUDE.md
</context>

## Rules for this plan (HALT clauses, carried verbatim from project rules)

- Execute sequentially on the MAIN working tree (isolation none). Do not use a worktree: it has no `deps/`, `_build` or `.dialyzer`, and the merge must land on this branch's HEAD. The orchestrator dispatches this plan with isolation `none` and re-records `.gsd/dispatch-isolation-sentinel.json` as `none` before dispatch; the executor itself never edits `.gsd/`.
- Shell note: the tool shell is zsh. Anything that relies on bash semantics (`PIPESTATUS`, `set -o pipefail`, arrays, process substitution) runs inside one `bash -c '...'`. zsh does not word-split `$(...)`, so test file lists are always written out explicitly.
- Dirty-tree rule (used by Task 1 Step 0(e), Step 8 and Task 3 gate 14): with `BASE` = `git merge-base HEAD origin/main` computed once in Task 1 Step 0 before the merge (expected 5808f14046923ec47785f424849cf1eff91e574d) and recorded in `"$(git rev-parse --git-dir)/gsd-205-base"` (after the merge the live merge-base equals origin/main, so later checks read the recorded value), the tree is acceptable when (1) `git diff --cached --quiet` exits 0 before the merge starts; (2) `git diff --name-only HEAD -- . ':(exclude).planning'` prints nothing, i.e. no dirty tracked path outside `.planning/`; and (3) `bash -c 'git diff --name-only HEAD | grep -Fxf <(git diff --name-only "$(cat "$(git rev-parse --git-dir)/gsd-205-base")" origin/main)'` prints nothing, i.e. no dirty tracked path that origin touches. Dirty `.planning/STATE.md` and `.planning/ROADMAP.md` (left by `state.begin-phase`) and the two protected files are expected and allowed; they are never staged.
- Record the pre-merge SHA first: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-205-premerge"`. Every later check that says `$PRE` means `$(cat "$(git rev-parse --git-dir)/gsd-205-premerge")`. Write the SHA into the SUMMARY too.
- Record the protected-path fingerprint first: `shasum -a 256 .planning/WINDOWS.md .planning/config.json .tool-versions > "$(git rev-parse --git-dir)/gsd-205-protected.sha256"`, and the tag count: `git tag -l | wc -l > "$(git rev-parse --git-dir)/gsd-205-tagcount"`.
- Commits use `{type}(205-01): ...` and end with the line `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`. Use only non-releasable types (`chore`, `test`, `docs`). release-please treats `feat`, `fix`, `perf` and `deps` as releasable, and none of this plan's commits is an adopter-facing change of its own.
- NEVER `git add .planning/`, `git add -A`, `git add .` or `git commit -a`. Stage by explicit path only.
- Protected paths the executor must NOT modify, stage, move, stash, replace or restore: `.planning/WINDOWS.md`, `.planning/config.json`, `.tool-versions` (untracked), `.gsd/`. Reading `.tool-versions` and copying it INTO a scratch clone is allowed.
- No `--no-verify`, no `core.hooksPath` override, no `git stash`, no `git reset --hard`, no `git checkout -- .`.
- No `git push`, `git tag`, `git fetch`, `mix hex.publish`, `gh release` or `gh pr`. Milestone tags stay local.
- On a blocked precondition, HALT and report. Never work around it.
- Never run Playwright directly. The browser lane is carried by the delta argument in Task 3 and is not run in this plan.
- A red Dialyzer usually means a PLT cache miss. Rebuild with `MIX_ENV=dev mix dialyzer --plt` before concluding anything.
- Every mix command in this plan is prefixed with `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27`, and every command that touches the database also sets `DB_PORT=5433`.
- Fix-forward limit: if a gate in Task 3 fails, a fix may land only as a separate `test(205-01)` or `docs(205-01)` commit that is additive (a new assertion, a new alias, new prose) or a non-assertion fix (formatting, an alias, a comment, doc wording) in `test/**`, `bin/verify-bump-rehearsal` or docs. Each of these is a HALT, reported with the failing output quoted: removing or relaxing any existing assertion; editing a `~> 0.10.0` install-pin line by hand (RELEASE-02: only `mix release.pins` writes pins); any change to `lib/**`, `mix.exs`, a byte-lock or render pin, `test/fixtures/**` or a screenshot baseline; any change to the protected contract files test/threadline/version_truth_doc_contract_test.exs, test/threadline/changelog_contract_test.exs, test/threadline/release_artifact_contract_test.exs, test/threadline/release_control_plane_contract_test.exs (beyond Task 2's one added test), test/threadline/ci_topology_contract_test.exs, test/threadline/ci_workflow_parity_contract_test.exs, test/threadline/ci_all_dedup_contract_test.exs, test/threadline/release_ci_gate_contract_test.exs; and any change to the 10 Group A files.
- Rollback, if the maintainer asks for it: before the merge commit exists, `git merge --abort`. After it exists, `git reset --keep "$PRE"` (keeps the dirty protected files). The executor does not roll back on its own initiative.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: merge origin/main as one commit, resolve the 17 conflicts as whole files, and prove it compiles and passes the release quick run</name>
  <reversibility rating="costly">A local, unpushed merge commit. `git reset --keep` to the recorded pre-merge SHA undoes it completely and nothing leaves the machine, so it is not one-way. It is rated costly because a later commit built on top would have to be replayed, so no commit may precede the tracer's green verify.</reversibility>
  <files>.github/workflows/release.yml, .release-please-manifest.json, CHANGELOG-GENERATED.md, CHANGELOG.md, README.md, bin/verify-bump-rehearsal, guides/adoption-evidence-playbook.md, guides/adoption-pilot-backlog.md, guides/configuration-and-commands.md, guides/evaluating-threadline.md, guides/getting-started-saas.md, guides/operator-surface.md, lib/mix/tasks/threadline.install.ex, mix.exs, test/mix/tasks/threadline/install_test.exs, test/threadline/changelog_contract_test.exs, test/threadline/storage_schema_test.exs</files>
  <read_first>
    - .planning/phases/205-release-reconciliation/205-RESEARCH.md (whole file; "Per-file resolution rules", "Reference blob hashes", "Scripted resolution", Pitfalls 1, 2, 7 and 9)
    - lib/mix/tasks/threadline.install.ex (HEAD version: `repo_migrations_path/1` at :111-:124 with its comment at :111; `existing_capture_migration?/1`, `existing_semantics_migration?/1`, `existing_governance_migration?/1` at :125-:145; the zero-arity `recommend_dedicated_storage_schema/0` at :71)
    - `git show origin/main:lib/mix/tasks/threadline.install.ex` (#46: `generate/4` at :60, `recommend_dedicated_storage_schema/1` clauses at :80 and :82, the inline `migrations_path/0` at :115, `existing_migration?/2` at :139)
    - bin/verify-bump-rehearsal (HEAD: `derived_doc_contract_tests` at :381, the gate chain at :404-:405, the OK message at :444)
    - `git show origin/main:bin/verify-bump-rehearsal` (#44: header stand-in notes :43-:66, upgrade-path stand-in :333-:349, CHANGELOG-GENERATED simulation :351-:370, gate chain :436-:438, OK message :477)
    - mix.exs (:1-:10 for `@version`)
  </read_first>
  <action>
    Step 0, preconditions. Each one that fails is a HALT, not something to fix.
    (0) `git rev-parse --show-toplevel` prints /Users/jon/projects/threadline and `git branch --show-current` prints fix/branch-protection-actions-capability. Otherwise HALT with the message "205-01 must run in the main working tree on fix/branch-protection-actions-capability (isolation none); got <toplevel> on <branch>".
    (a) `git rev-parse origin/main` prints 471ebf6e67365e2ad16a1b8dbac991e5da1ec3b4. Do not fetch to change it.
    (b) `git diff --quiet 90b58794b0a89f3d6f90075ac073cb6c044f61d0 HEAD -- . ':(exclude).planning'` exits 0. The research candidate was built on 90b58794; later commits (998f69bf and the plan commit) may touch only `.planning/`.
    (c) `git merge-base HEAD origin/main` prints 5808f14046923ec47785f424849cf1eff91e574d. Record it now: `git merge-base HEAD origin/main > "$(git rev-parse --git-dir)/gsd-205-base"`.
    (d) `git merge-tree --write-tree --name-only --no-messages HEAD origin/main | tail -n +2 | LC_ALL=C sort` prints exactly this 17-path list (also sorted with `LC_ALL=C sort` before comparing) and nothing else: .github/workflows/ci.yml, CHANGELOG-GENERATED.md, CHANGELOG.md, CONTRIBUTING.md, bin/verify-bump-rehearsal, lib/mix/tasks/threadline.install.ex, lib/threadline/operator_surface/live/timeline_live.ex, mix.exs, test/threadline/adoption_pilot_doc_contract_test.exs, test/threadline/changelog_contract_test.exs, test/threadline/community_health_render_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs, test/threadline/operator_surface/rendered_output_contract_test.exs, test/threadline/operator_surface_doc_contract_test.exs, test/threadline/readme_doc_contract_test.exs, test/threadline/release_artifact_contract_test.exs, test/threadline/storage_schema_test.exs. (The merge-tree exit code is 1 when conflicts exist; that is expected. Write the expected list to a file under /Users/jon/.claude/jobs/77cf1bdd/tmp and compare with `diff`, inside `bash -c`.)
    (e) The dirty-tree rule from the Rules section holds: nothing staged, no dirty tracked path outside `.planning/`, and no dirty tracked path that origin touches. Dirty `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/WINDOWS.md` and `.planning/config.json` are allowed.
    (f) `git tag -l v0.10.0 v0.10.1` prints both tags.
    Then record `$PRE`, the protected-path fingerprint and the tag count as the Rules section says.

    Step 1, start the merge. Run `git merge --no-ff --no-commit origin/main`. Exit code 1 with conflicts is expected. If git refuses to start because of a local change to a protected path, HALT (do not stash). Confirm `git diff --name-only --diff-filter=U | wc -l` prints 17.

    Step 2, Group A, take ours as whole files (10 paths): `git checkout --ours --` .github/workflows/ci.yml, CONTRIBUTING.md, lib/threadline/operator_surface/live/timeline_live.ex, test/threadline/adoption_pilot_doc_contract_test.exs, test/threadline/community_health_render_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs, test/threadline/operator_surface/rendered_output_contract_test.exs, test/threadline/operator_surface_doc_contract_test.exs, test/threadline/readme_doc_contract_test.exs, test/threadline/release_artifact_contract_test.exs. Origin's blob for each is already in HEAD's history, and HEAD evolved each file further in 201/203/204.

    Step 3, Group B, take theirs as whole files (4 paths): `git checkout --theirs --` CHANGELOG-GENERATED.md, CHANGELOG.md, test/threadline/changelog_contract_test.exs, test/threadline/storage_schema_test.exs.

    Step 4, Group C, mix.exs. Write `git show HEAD:mix.exs` with the single line `  @version "0.9.0"` replaced by `  @version "0.10.1"` (the research uses sed on that exact line). This keeps HEAD's `verify.xref_cycles`, the ci.all composition, the `critic_trust/` handling and the 204-04/204-09 `stress_live(\.ex$|/)` and `mechanical_checker(\.ex$|/)` package excludes. It drops origin's resurrected `"verify.doc_contract"` alias. `git hash-object mix.exs` must print 6404f91ae5e0e999ac713db839cea32f708835f0.

    Step 5, Group C, lib/mix/tasks/threadline.install.ex. Start from `git show origin/main:lib/mix/tasks/threadline.install.ex` (the #46 file) as the whole file, then re-apply 204-07's flatten (commit d00dd4f6). In `migrations_path/0`, replace the inline `priv = case repo.config()[:priv] ...` block and its `Path.join(priv, "migrations")` with a call to `repo_migrations_path(repo)`. Add `defp repo_migrations_path/1`, with HEAD's body verbatim and HEAD's comment line `# Called inside migrations_path/0, so its rescue still covers a raising repo.` directly above it, placed immediately above `defp existing_migration?(path, suffix)`. Keep #46's `generate/4`, `existing_migration?/2` and both `recommend_dedicated_storage_schema/1` clauses. The result must NOT contain `existing_capture_migration?`, `existing_semantics_migration?`, `existing_governance_migration?`, a zero-arity `recommend_dedicated_storage_schema()` call, or a `recommend_dedicated_storage_schema do` head. That combination is the CR-01 trap from research Pitfall 1: marker-only resolution compiles but prints the old advice. The reference blob is 16a90922e809cc4b5c2638436d29ff10c1071668. Placement may differ, so check it with the structural greps in acceptance_criteria, not the hash.

    Step 6, Group C, bin/verify-bump-rehearsal. Start from `git merge-file -p` with HEAD's file as ours, `git show 23f0505a:bin/verify-bump-rehearsal` as the base and origin's file as theirs, written to scratch files under /Users/jon/.claude/jobs/77cf1bdd/tmp (not /tmp/ours). Write the output to bin/verify-bump-rehearsal. That leaves 2 conflict hunks. Resolve them so the file keeps all of #44's additions (the two-stand-ins header, the richer CHANGELOG stand-in with `### Breaking changes` and `### Added`, the upgrade-path stand-in, and the release-please CHANGELOG-GENERATED simulation) and HEAD's `derived_doc_contract_tests` function. The gate chain must read, in order: `run_gate "doc-contract tests (derived by filename) at $NEXT" derived_doc_contract_tests`, then `run_gate "changelog contract at $NEXT" mix test test/threadline/changelog_contract_test.exs`, then `run_gate "mix verify.release at $NEXT" mix verify.release || true`, joined by `&&`. Rewrite the final `Bump rehearsal OK:` printf so it names all three gates (derived doc-contract tests, the changelog contract and mix verify.release) and does not name `verify.doc_contract`. Also rewrite the header comment (around line 66) that says `mix verify.doc_contract` still enforces the real assertion, so it names `mix test` (verify.test) instead. Wording is at the executor's discretion; the gate chain is not. Then `chmod +x bin/verify-bump-rehearsal` and confirm `bash -n bin/verify-bump-rehearsal` exits 0. The reference blob is 506baf66147a76e554cb98631b3b3e6a7e93297c.

    Step 7, check before staging. No conflict markers remain: `grep -n '^<<<<<<<\|^=======$\|^>>>>>>>' mix.exs lib/mix/tasks/threadline.install.ex bin/verify-bump-rehearsal` prints nothing. Compare index-to-be blobs with `git hash-object`: each Group A file equals `git rev-parse "$PRE:<path>"` and each Group B file equals `git rev-parse "origin/main:<path>"`.

    Step 8, stage and commit. `git add --` the 17 conflicted paths, named explicitly, and nothing else. The auto-merged paths (release.yml, the manifest, README.md, the six guides, install_test.exs) are already staged by `git merge`. Confirm `git diff --cached --name-only` contains no `.planning/` path and no `.tool-versions` (in particular not .planning/STATE.md, .planning/ROADMAP.md, .planning/WINDOWS.md or .planning/config.json), and that parts (2) and (3) of the dirty-tree rule still hold for the unstaged side: `git diff --name-only -- . ':(exclude).planning'` prints nothing. Write the message to a file under /Users/jon/.claude/jobs/77cf1bdd/tmp and run `git commit -F <file>` (plain git, no `-a`, no gsd commit tooling for a merge). Subject: `chore(205-01): merge origin/main (v0.10.1) into the milestone branch`. The body lists the three rule groups with their file counts, names the install.ex and rehearsal combinations, and ends with the Co-Authored-By line.

    Step 9, tracer verify. Run the verify commands below. Credo is expected to be red on test/mix/tasks/threadline/install_test.exs until Task 2, so it is not part of this task's verify. If compile or the quick run fails, HALT and report. Do not start Task 2 on a red tracer.
  </action>
  <verify>
    <automated>test "$(git rev-list --merges --count "$(cat "$(git rev-parse --git-dir)/gsd-205-premerge")"..HEAD)" = 1 && test "$(git rev-list --parents -n1 HEAD | cut -d' ' -f2-)" = "$(cat "$(git rev-parse --git-dir)/gsd-205-premerge") 471ebf6e67365e2ad16a1b8dbac991e5da1ec3b4" && git merge-base --is-ancestor v0.10.1 HEAD && git merge-base --is-ancestor v0.10.0 HEAD && test "$(git rev-list --count HEAD..origin/main)" = 0</automated>
    <fails_when>non-zero exit: more or fewer than 1 merge commit since the pre-merge SHA, merge parents other than (pre-merge SHA, 471ebf6e...), v0.10.1 or v0.10.0 not an ancestor of HEAD, or `git rev-list --count HEAD..origin/main` printing anything other than 0</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors</automated>
    <fails_when>non-zero exit or any "warning:" line, for example an unused `repo_migrations_path/1` or an undefined `existing_migration?/2`</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/mix/tasks/threadline/install_test.exs test/threadline/changelog_contract_test.exs test/threadline/storage_schema_test.exs test/threadline/version_truth_doc_contract_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/release_control_plane_contract_test.exs test/threadline/ci_topology_contract_test.exs test/threadline/ci_all_dedup_contract_test.exs test/threadline/public_surface_contract_test.exs test/threadline/release_distribution_doc_contract_test.exs</automated>
    <fails_when>non-zero exit or an ExUnit summary with "1 failure" or more, for example the changelog contract refuting the dated 0.10.0 entry, version_truth reporting a 0.9.0 site, or ci_all_dedup rejecting a `verify.doc_contract` alias in mix.exs</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only "$PRE" HEAD | sort` prints exactly these 17 lines: .github/workflows/release.yml, .release-please-manifest.json, CHANGELOG-GENERATED.md, CHANGELOG.md, README.md, bin/verify-bump-rehearsal, guides/adoption-evidence-playbook.md, guides/adoption-pilot-backlog.md, guides/configuration-and-commands.md, guides/evaluating-threadline.md, guides/getting-started-saas.md, guides/operator-surface.md, lib/mix/tasks/threadline.install.ex, mix.exs, test/mix/tasks/threadline/install_test.exs, test/threadline/changelog_contract_test.exs, test/threadline/storage_schema_test.exs
    - `git rev-parse HEAD:mix.exs` prints 6404f91ae5e0e999ac713db839cea32f708835f0
    - `git rev-parse HEAD:.github/workflows/release.yml` prints 064e7ee98a2f062e1041d6cb8c40b75a2b8374bd
    - `git rev-parse HEAD:.release-please-manifest.json` prints 30b6d45ad63f1643640935f530bb71e316f5bafb
    - `git rev-parse HEAD:CHANGELOG-GENERATED.md` prints b47569b5e64c68a932ac0a2d607d797b49c767a0, and `git rev-parse HEAD:test/threadline/changelog_contract_test.exs` prints 4a4ca794556148699b7d6d16b05a3c63d5a30edb
    - `git rev-parse HEAD:test/mix/tasks/threadline/install_test.exs` prints 2c7ca7061cd90e43c84cbc664205cb094c86c877
    - For each of the 10 Group A paths, `git diff --quiet "$PRE" HEAD -- <path>` exits 0
    - `grep -c 'recommend_dedicated_storage_schema' lib/mix/tasks/threadline.install.ex` prints 3, and `grep -c 'recommend_dedicated_storage_schema()' lib/mix/tasks/threadline.install.ex` prints 0
    - `grep -c 'defp repo_migrations_path(repo) do' lib/mix/tasks/threadline.install.ex` prints 1, and `grep -c 'Called inside migrations_path/0, so its rescue still covers a raising repo.' lib/mix/tasks/threadline.install.ex` prints 1
    - `grep -cE 'existing_(capture|semantics|governance)_migration\?' lib/mix/tasks/threadline.install.ex` prints 0
    - `grep -c 'derived_doc_contract_tests' bin/verify-bump-rehearsal` prints at least 2 (definition and gate call), `grep -c 'run_gate "changelog contract at \$NEXT" mix test test/threadline/changelog_contract_test.exs' bin/verify-bump-rehearsal` prints 1, and `grep -c 'UPGRADE_GUIDE=' bin/verify-bump-rehearsal` prints 1
    - `git grep -n 'verify\.doc_contract' -- ':!.planning' ':!CHANGELOG.md' ':!CHANGELOG-GENERATED.md'` prints nothing
    - `git log -1 --format=%s "$(git rev-list --merges -n1 HEAD)"` prints `chore(205-01): merge origin/main (v0.10.1) into the milestone branch`
    - `shasum -a 256 -c "$(git rev-parse --git-dir)/gsd-205-protected.sha256"` reports OK for all three protected files
  </acceptance_criteria>
  <done>The branch holds one merge commit of origin/main with v0.10.0 and v0.10.1 as ancestors. The 17 conflicted paths match the rule table, the tree compiles without warnings, and the release/changelog/version-truth quick run is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Follow-up commits: credo alias in the merged installer test, a sync-pins contract test, and the CONTRIBUTING rehearsal row</name>
  <reversibility rating="reversible">Three ordinary commits on top of the merge touching one test file each plus one CONTRIBUTING table row. Each reverts with `git revert`.</reversibility>
  <files>test/mix/tasks/threadline/install_test.exs, test/threadline/release_control_plane_contract_test.exs, CONTRIBUTING.md</files>
  <read_first>
    - test/mix/tasks/threadline/install_test.exs (whole file as merged; the `use ExUnit.Case, async: false` line and the `Mix.Tasks.Threadline.Install.run([])` call near :37)
    - test/threadline/release_control_plane_contract_test.exs (whole file, 130 lines: `@root` at :3, the `@smoke_job` tests at :40-:75 as the style to copy, `release_workflow/0` at :115, `job_block!/2` at :119-:129)
    - .github/workflows/release.yml (as merged: `sync-release-pr-pins` job at about :110-:169, `bootstrap-release-pr-ci` at about :171-:189)
    - CONTRIBUTING.md (:505-:512, the CI job table row for `verify-bump-rehearsal` at :510)
    - test/threadline/ci_topology_contract_test.exs and test/threadline/ci_workflow_parity_contract_test.exs (grep for `CONTRIBUTING` to see which CONTRIBUTING text they pin)
  </read_first>
  <behavior>
    - A new test in release_control_plane_contract_test.exs, named "the release PR pin-sync job exists, is scoped, and gates the CI bootstrap", passes on the merged release.yml.
    - The same test fails when run against the pre-merge release.yml (`git show "$PRE":.github/workflows/release.yml`), with the `job_block!/2` flunk message `could not find a "  sync-release-pr-pins:" job`.
    - `mix credo --strict test/mix/tasks/threadline/install_test.exs` goes from rc=2 with "Nested modules could be aliased at the top of the invoking module" to "found no issues".
  </behavior>
  <action>
    Commit A, the credo alias. In test/mix/tasks/threadline/install_test.exs, add `alias Mix.Tasks.Threadline.Install` directly after the `use ExUnit.Case, async: false` line, separated by one blank line, and change every fully qualified `Mix.Tasks.Threadline.Install.run(...)` call to `Install.run(...)`. Change nothing else. `git hash-object test/mix/tasks/threadline/install_test.exs` should print e105efeb57fa516e62e92d3608e58d83043e1a03, the research candidate's blob. If it differs only because of extra call sites, record that in the SUMMARY. Stage that one path and commit as `test(205-01): alias the install task in the merged installer test for credo --strict`. The type is `test`, not `fix`, so release-please does not list it.

    Commit B, the sync-pins contract (research Open Question 1, decided yes). release_control_plane_contract_test.exs already parses release.yml and has the `job_block!/2` helper, and the merge did not touch it (identical on both sides), so it is the right home. Add one test named "the release PR pin-sync job exists, is scoped, and gates the CI bootstrap". Let `sync = job_block!(release_workflow(), "sync-release-pr-pins")` and `bootstrap = job_block!(release_workflow(), "bootstrap-release-pr-ci")`. Assert on `sync`: a line matching `~r/^    needs: release-please$/m`; the text `run: mix release.pins` and the text `mix release.pins --check`; `persist-credentials: false`; a `concurrency:` block whose `group:` line reads `sync-release-pr-pins`; and exactly one `PUSH_TOKEN:` env binding (count the regex `~r/^\s+PUSH_TOKEN:/m`, expect 1), so the token is supplied to the push step alone. Assert on `bootstrap`: a line matching `~r/^    needs: \[release-please, sync-release-pr-pins\]$/m`, and an `if:` line containing `always()`. Every assertion carries a message in the file's style that names the RELEASE-02 consequence. For example, without the sync job the release PR is born red, because release-please cannot own the install pins. Without `always()` a failed pin sync silently suppresses the CI bootstrap (202-REVIEW WR-03). Prove the failing direction before committing. Save the merged file with `cp .github/workflows/release.yml /Users/jon/.claude/jobs/77cf1bdd/tmp/release.yml.merged`. Write `git show "$PRE":.github/workflows/release.yml` over .github/workflows/release.yml. Run only the new test and confirm it FAILS with the `could not find a "  sync-release-pr-pins:" job` flunk. Then restore with `git checkout HEAD -- .github/workflows/release.yml`, and confirm `git diff --quiet HEAD -- .github/workflows/release.yml` exits 0. Paste the failure line into the SUMMARY as the RED evidence. Stage only the test file and commit as `test(205-01): pin the sync-release-pr-pins job and the bootstrap gate in release.yml`.

    Commit C, CONTRIBUTING truth. After the merge the rehearsal also runs the changelog contract, so CONTRIBUTING.md:510 is no longer the whole truth. Change only the description cell of the `verify-bump-rehearsal` row so it reads that the rehearsal runs every doc-contract test file it finds by filename (at least 30, or the gate fails), the changelog contract and `mix verify.release` against the simulated release commit. Keep the rest of the sentence ("so a born-red release cause fails the pull request that introduces it rather than the publish gate") and the table shape. Do not edit the ci.yml job comment: it already says "plus verify.release and the release contract tests", which covers the changelog contract. Re-run `ci_workflow_parity_contract_test.exs` and `ci_topology_contract_test.exs`. If either pins the old wording, HALT and report, because 205 does not repin 204 contracts. Stage only CONTRIBUTING.md and commit as `docs(205-01): name the changelog contract in the bump-rehearsal CI row`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/release_control_plane_contract_test.exs test/mix/tasks/threadline/install_test.exs test/threadline/ci_workflow_parity_contract_test.exs test/threadline/ci_topology_contract_test.exs</automated>
    <fails_when>non-zero exit or "1 failure" or more in the ExUnit summary, for example the new test flunking with `could not find a "  sync-release-pr-pins:" job`, a PUSH_TOKEN count other than 1, or a parity contract rejecting the CONTRIBUTING row</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict test/mix/tasks/threadline/install_test.exs test/threadline/release_control_plane_contract_test.exs</automated>
    <fails_when>non-zero exit (credo rc=2) or any issue line such as "Nested modules could be aliased at the top of the invoking module"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  alias Mix.Tasks.Threadline.Install$' test/mix/tasks/threadline/install_test.exs` prints 1, and `grep -c 'Mix.Tasks.Threadline.Install.run' test/mix/tasks/threadline/install_test.exs` prints 0
    - `grep -c 'the release PR pin-sync job exists, is scoped, and gates the CI bootstrap' test/threadline/release_control_plane_contract_test.exs` prints 1
    - `grep -c 'sync-release-pr-pins' test/threadline/release_control_plane_contract_test.exs` prints at least 2
    - the SUMMARY quotes the RED run of the new test against the pre-merge release.yml, containing `could not find a "  sync-release-pr-pins:" job`
    - `git diff --quiet HEAD -- .github/workflows/release.yml` exits 0 after the RED proof, and `git rev-parse HEAD:.github/workflows/release.yml` still prints 064e7ee98a2f062e1041d6cb8c40b75a2b8374bd
    - `grep -n 'verify-bump-rehearsal' CONTRIBUTING.md | grep -c 'changelog contract'` prints 1
    - `git log --format=%s "$(git rev-list --merges -n1 HEAD)"..HEAD` lists exactly the three subjects `test(205-01): alias the install task in the merged installer test for credo --strict`, `test(205-01): pin the sync-release-pr-pins job and the bootstrap gate in release.yml` and `docs(205-01): name the changelog contract in the bump-rehearsal CI row`
    - `git show --name-only --format= HEAD~2 HEAD~1 HEAD | sort -u` prints only test/mix/tasks/threadline/install_test.exs, test/threadline/release_control_plane_contract_test.exs and CONTRIBUTING.md
  </acceptance_criteria>
  <done>Credo is clean on the merged installer test. A contract test now fails if a future merge drops the sync-pins job, its token scoping or the bootstrap gate, and it was shown RED against the pre-merge workflow. CONTRIBUTING describes the rehearsal truthfully.</done>
</task>

<task type="auto">
  <name>Task 3: Full gate run on the committed HEAD: 203/204 gates, full suite, pins, bump rehearsal 0.10.1 -> 0.11.0, and verify.release in a clean clone</name>
  <reversibility rating="reversible">Read-only gate runs plus scratch clones under the job tmp dir. The only possible commits are fix-forward commits within the Rules limit, each revertible.</reversibility>
  <files>No file is expected to change. Fix-forward commits, if any, stay within the Rules limit (test/**, bin/verify-bump-rehearsal, doc wording).</files>
  <read_first>
    - .planning/phases/205-release-reconciliation/205-RESEARCH.md ("Validation Architecture", Pitfalls 3, 4 and 8)
    - mix.exs (:222-:230 `verify_bump_rehearsal/1`, :396-:406 `ensure_clean_tree!/0`, and the `ci.all` alias list)
    - bin/verify-bump-rehearsal (:150-:215, CURRENT/NEXT derivation and the clone of HEAD)
  </read_first>
  <action>
    Run each gate below on the committed HEAD, in this order, and record the command, exit code and key output line for each in a gate table in the SUMMARY.
    1. `mix format --check-formatted` (verify.format).
    2. `MIX_ENV=test mix verify.credo`. Expect "found no issues".
    3. `mix compile --force --warnings-as-errors`.
    4. `mix verify.xref_cycles`. Expect "No cycles found".
    5. `mix verify.compile_no_optional`.
    6. `mix verify.threadline`.
    7. `MIX_ENV=dev mix verify.dialyzer`. Expect "Total errors: 0". On a PLT failure, run `MIX_ENV=dev mix dialyzer --plt` once and re-run.
    8. `DB_PORT=5433 mix test`, the full suite. Expect 0 failures. The research candidate measured 1786 tests, 0 failures, 1 excluded; this plan adds 1 test. A failure in `CriticTrustTest` or the critic.measure bidirectional-overlap test is the known intermittent flake recorded in 202 deferred-items. Re-run that one file once. It is a real failure only if it fails twice.
    9. `MIX_ENV=dev mix release.pins --check`. Expect 0 pin sites differing, derived `~> 0.10.0`.
    10. `DB_PORT=5433 mix verify.example`.
    11. `DB_PORT=5433 mix verify.bump_rehearsal`. This clones the committed HEAD, so it runs after every commit above. Expect a line containing `0.10.1 -> 0.11.0` and a line starting `Bump rehearsal OK:`. A dirty `.planning/` in the real tree does not affect it.
    12. `verify.release` in a clean clone (research Pitfall 4). The real tree has modified protected files that must not be cleaned. Set `D=/Users/jon/.claude/jobs/77cf1bdd/tmp/p205-release`, remove any previous `$D`, then run `git clone --quiet --no-local /Users/jon/projects/threadline "$D"`. Confirm `git -C "$D" rev-parse HEAD` equals the real tree's `git rev-parse HEAD`. Copy the real tree's .tool-versions into `$D` (read-only use of the protected file). In `$D`, run `mix deps.get` then `DB_PORT=5433 mix verify.release`. Expect exit 0.
    13. Browser-lane delta argument, recorded in place of a run. Confirm `git diff --name-only "$PRE" HEAD -- lib/threadline examples test/fixtures priv` prints nothing and `grep -rnE 'vsn|:version\]' lib/threadline/operator_surface` prints nothing. The operator surface therefore has no rendered delta, and the Playwright lane (healthy baseline 326 passed / 8 failed / 16 skipped) is carried rather than run. Do not run Playwright.
    14. The no-publish and protected-path proofs: `git rev-parse origin/main` still prints 471ebf6e67365e2ad16a1b8dbac991e5da1ec3b4. `git tag -l | wc -l` equals the recorded tag count. `shasum -a 256 -c` on the recorded fingerprint reports OK. Parts (2) and (3) of the dirty-tree rule hold: `git diff --name-only HEAD -- . ':(exclude).planning'` prints nothing, and no dirty tracked path intersects `git diff --name-only "$BASE" origin/main`. Dirty .planning/STATE.md and .planning/ROADMAP.md are expected.
    If any gate fails, apply the fix-forward limit from the Rules section. Allowed fixes land as separate `test(205-01)` or `docs(205-01)` commits, after which you re-run gates 1-11 on the new HEAD and gate 12 in a fresh clone. Anything outside the limit is a HALT with the failing output quoted.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test</automated>
    <fails_when>non-zero exit or an ExUnit summary line with "1 failure" or more (after the single allowed re-run of a known-flaky critic file)</fails_when>
    <automated>bash -c 'set -o pipefail; L=/Users/jon/.claude/jobs/77cf1bdd/tmp/p205-rehearsal.log; DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.bump_rehearsal 2>&1 | tee "$L" && sed "s/\x1b\[[0-9;]*m//g" "$L" | grep -q "0\.10\.1 -> 0\.11\.0" && sed "s/\x1b\[[0-9;]*m//g" "$L" | grep -q "^Bump rehearsal OK:"'</automated>
    <fails_when>non-zero mix exit ("verify.bump_rehearsal failed (N)"), a rehearsal that names `0.9.0 -> 0.10.0` instead of `0.10.1 -> 0.11.0`, or no line starting `Bump rehearsal OK:`</fails_when>
    <automated>bash -c 'D=/Users/jon/.claude/jobs/77cf1bdd/tmp/p205-release; rm -rf "$D" && git clone --quiet --no-local /Users/jon/projects/threadline "$D" && test "$(git -C "$D" rev-parse HEAD)" = "$(git -C /Users/jon/projects/threadline rev-parse HEAD)" && cp /Users/jon/projects/threadline/.tool-versions "$D/" && cd "$D" && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix deps.get && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.release'</automated>
    <fails_when>non-zero exit: clone HEAD differing from the real HEAD, "verify.release requires a clean working tree", or "verify.release failed while running ..."</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix release.pins --check && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix verify.credo && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix verify.dialyzer && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.xref_cycles</automated>
    <fails_when>non-zero exit from any of the four: a differing pin site, any credo issue, a Dialyzer "Total errors:" count above 0, or a reported xref cycle</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY gate table has one row for each of gates 1-14, each with exit code 0 (gate 13 records the two empty command outputs)
    - the full-suite row shows "0 failures" and a test count of at least 1787
    - the rehearsal row quotes the `0.10.1 -> 0.11.0` line and the `Bump rehearsal OK:` line
    - the verify.release row names the clone path /Users/jon/.claude/jobs/77cf1bdd/tmp/p205-release and the cloned HEAD SHA
    - `git diff --name-only "$PRE" HEAD -- lib/threadline examples test/fixtures priv` prints nothing, and before the SUMMARY commit `git diff --name-only "$PRE" HEAD -- .planning` also prints nothing
    - `git rev-parse origin/main` prints 471ebf6e67365e2ad16a1b8dbac991e5da1ec3b4, and `git tag -l | wc -l` equals the value in "$(git rev-parse --git-dir)/gsd-205-tagcount"
    - `shasum -a 256 -c "$(git rev-parse --git-dir)/gsd-205-protected.sha256"` reports OK for all three files
  </acceptance_criteria>
  <done>Every 203/204 gate, the full suite, the pin check, the example app, the 0.10.1 -> 0.11.0 bump rehearsal and a clean-clone verify.release are green on the committed HEAD. Nothing was pushed, tagged or published, and the protected paths are byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| origin/main → milestone branch | Remote-authored release commits (#41..#48) enter the local branch through the merge |
| local branch → GitHub / hex.pm | Any push, tag or publish would expose the full `.planning/` history and could cut an unintended release |
| release.yml → repository secrets | The merged workflow references `RELEASE_PLEASE_TOKEN` / `GITHUB_TOKEN` and compiles dependencies in the same job |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-205-01 | Information disclosure | release.yml `sync-release-pr-pins` | high | mitigate | #46's hardening is taken byte-exact (release.yml blob 064e7ee9 asserted in Task 1). Task 2's contract test pins `persist-credentials: false`, exactly one `PUSH_TOKEN:` binding (push step only) and the job concurrency group, and it is proven RED against the pre-merge workflow. |
| T-205-02 | Tampering / Repudiation | local git state | high | mitigate | No `git push`, `git tag`, `git fetch`, `mix hex.publish` or gh operation. Task 3 gate 14 proves origin/main is unchanged at 471ebf6e and the tag count is unchanged. Tags stay local (project memory rule). |
| T-205-03 | Tampering | conflict resolution (install.ex) | high | mitigate | Whole-file resolution from #46 plus the flatten re-apply. Structural greps assert 3 `recommend_dedicated_storage_schema` references and 0 zero-arity calls, so the CR-01 split-brain advice cannot return through marker-only resolution. install_test.exs (4 tests from #46) runs in the tracer. |
| T-205-04 | Tampering | protected working-tree files | medium | mitigate | A SHA-256 fingerprint of .planning/WINDOWS.md, .planning/config.json and .tool-versions is recorded before the merge and checked after Tasks 1 and 3. Explicit-path staging only. No stash or reset --hard. |
| T-205-05 | Elevation of privilege | executor workarounds | medium | mitigate | HALT clauses in the Rules section. No `--no-verify` or `core.hooksPath`. The fix-forward limit forbids lib/mix.exs/pin changes. |
| T-205-06 | Denial of service | future release PR | medium | mitigate | The bump rehearsal on the committed HEAD proves the 0.10.1 -> 0.11.0 release commit is green. The sync-pins contract test keeps the job from silently disappearing. |
| T-205-SC | Tampering | npm/pip/cargo installs | low | accept | No new package is installed. `mix deps.get` in the scratch clone resolves the committed, unchanged mix.lock. |
</threat_model>

<verification>
- Task 1 tracer: one merge commit, v0.10.0/v0.10.1 ancestry, compile clean, release quick run green.
- Task 2: credo clean on the installer test; the sync-pins contract is green on HEAD and was shown RED on the pre-merge workflow.
- Task 3: full gate table green, including the 0.10.1 -> 0.11.0 rehearsal and verify.release in a clean clone; the protected paths and remote state are unchanged.
</verification>

<success_criteria>
- `git merge-base --is-ancestor v0.10.1 HEAD` exits 0 and `git rev-list --count HEAD..origin/main` prints 0 (RELEASE-02, audit F1).
- `MIX_ENV=dev mix release.pins --check` is clean at 0.10.1 and `mix verify.bump_rehearsal` rehearses 0.10.1 -> 0.11.0 green (RELEASE-02, audit F2).
- The changelog contract accepts release-please's dated entries, and `mix verify.release` passes in a clean clone (RELEASE-05).
- The full suite has 0 failures and every 203/204 gate is green, with no rendered-output delta.
</success_criteria>

## Artifacts this phase produces (this plan)

- One merge commit `chore(205-01): merge origin/main (v0.10.1) into the milestone branch`, parents = pre-merge SHA and 471ebf6e, changing the 17 paths listed in must_haves
- `lib/mix/tasks/threadline.install.ex`: #46's CR-01 fix with 204-07's `repo_migrations_path/1` flatten
- `bin/verify-bump-rehearsal`: #44's stand-ins and simulation, 204-02's derived doc-contract gate, and the changelog contract gate
- `mix.exs` at `@version "0.10.1"`; `.release-please-manifest.json` at 0.10.1; synced guides and README
- `test/mix/tasks/threadline/install_test.exs` (new from origin, then the credo alias commit)
- `test/threadline/release_control_plane_contract_test.exs`: the new sync-pins/bootstrap contract test
- `CONTRIBUTING.md`: the corrected `verify-bump-rehearsal` row
- `.planning/phases/205-release-reconciliation/205-01-SUMMARY.md`: pre-merge SHA, merge SHA, the RED evidence of the new contract test, and the 14-row gate table
- Scratch only (never staged): `/Users/jon/.claude/jobs/77cf1bdd/tmp/p205-release` clone, `p205-rehearsal.log`, and the merge-file scratch inputs

<output>
Create `.planning/phases/205-release-reconciliation/205-01-SUMMARY.md` when done. Include `requirements-completed: [RELEASE-02, RELEASE-05]` in its frontmatter, the pre-merge SHA, the merge commit SHA and the three follow-up commit SHAs, the RED evidence line from Task 2, and the 14-row gate table from Task 3. Stage the SUMMARY by its explicit path only.
</output>
