---
phase: 205-release-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [205-01]
gap_closure: true
files_modified:
  - .planning/phases/202-release-0-10-0/202-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/202-release-0-10-0/deferred-items.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [RELEASE-02, RELEASE-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "202-VERIFICATION.md carries a dated `## Addendum (2026-09-24, Phase 205): verified on the milestone branch` section, appended after the existing body. It records the merge commit SHA from 205-01-SUMMARY, the `git merge-base --is-ancestor v0.10.1 HEAD` exit 0 proof, `git rev-list --count HEAD..origin/main` = 0, the bump rehearsal `0.10.1 -> 0.11.0` OK line, `release.pins --check` clean, clean-clone `verify.release` exit 0, and the full-suite count. The original frontmatter and evidence are not rewritten, including the `NOTE: shipped code was verified at origin/main 53b5d71a` comment (RELEASE-02, RELEASE-05)."
    - "REQUIREMENTS.md traceability rows for RELEASE-02 and RELEASE-05 read `| RELEASE-02 | Phase 202, Phase 205 | Complete |` and `| RELEASE-05 | Phase 202, Phase 205 | Complete |`. The `[x]` checkboxes at the requirement lines stay checked, and no other requirement row changes."
    - "202's review disposition is recorded, not rewritten. A dated section appended to 202 deferred-items.md marks CR-01, WR-01, WR-02, WR-03 and WR-05 as `resolved` by #46 (45532778), arriving on the milestone branch in the 205-01 merge commit. It marks WR-04, WR-06 and WR-07 as `open`, and IN-01..IN-07 as `open (info)`. 202-REVIEW.md and v1.41-MILESTONE-AUDIT.md are byte-unchanged."
    - "STATE.md and ROADMAP.md show Phase 205 complete with 2/2 plans. The progress block reads total_phases 8, completed_phases 8, total_plans 148, completed_plans 148, percent 100. This is hand-checked after any gsd-tools state handler call, because the handlers miscompute this repo's progress block."
    - "Every bookkeeping commit stages only the named .planning files by explicit path. .planning/WINDOWS.md and .planning/config.json keep their pre-phase SHA-256."
  artifacts:
    - .planning/phases/202-release-0-10-0/202-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/202-release-0-10-0/deferred-items.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
  key_links:
    - "205-01-SUMMARY.md (merge SHA, gate table) → the 202-VERIFICATION addendum and the REQUIREMENTS traceability rows cite the same SHA, so a re-audit (`/gsd-audit-milestone`) finds RELEASE-02/05 satisfied on the branch."
    - "202 deferred-items.md review-disposition section → the re-audit's tech_debt list for 202 shrinks to WR-04, WR-06 and WR-07 plus the info items."
  prohibitions:
    - statement: "v1.41-MILESTONE-AUDIT.md and 202-REVIEW.md are not edited. The audit is re-run later, not amended."
      status: unverified
      flagged: true
    - statement: "Staging is by explicit path only. `git add .planning/`, `git add -A`, `git add .` and `git commit -a` are forbidden."
      status: unverified
      flagged: true
    - statement: ".planning/WINDOWS.md, .planning/config.json, the untracked .tool-versions and .gsd/ are not modified, staged, moved, stashed, replaced or restored."
      status: unverified
      flagged: true
    - statement: "Nothing is pushed, tagged or published."
      status: unverified
      flagged: true
    - statement: "`state.*` and `roadmap` gsd-tools calls use flag-style arguments (gsd-core v1.14.0). Positional arguments are rejected."
      status: unverified
      flagged: true
---

<objective>
Record, truthfully and without rewriting history, that RELEASE-02 and RELEASE-05 now hold on the milestone branch. Add an addendum to 202-VERIFICATION.md that cites 205-01's merge and gate evidence. Update the two REQUIREMENTS traceability rows. Record 202-REVIEW's disposition after #46 in 202's deferred-items register. Mark Phase 205 complete in STATE.md and ROADMAP.md with a hand-checked progress block.

Purpose: the audit found RELEASE-02/05 verified only on origin/main. The code fix is 205-01. This plan makes the planning record match it so the re-audit can close F1 and F2 on evidence.

Output: four edited .planning files plus STATE/ROADMAP, committed as `docs(205-02)` commits.

The ai-integration, assumption-delta and schema-gate contributions are detection-based and do not apply (no external API integration, no identity-model transition, no ORM schema file).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-release-reconciliation/205-01-SUMMARY.md
@.planning/phases/205-release-reconciliation/205-RESEARCH.md
@.planning/v1.41-MILESTONE-AUDIT.md
@CLAUDE.md
</context>

## Rules for this plan (HALT clauses, carried verbatim from project rules)

- Precondition: .planning/phases/205-release-reconciliation/205-01-SUMMARY.md exists, records a merge commit SHA, and its gate table shows every gate at exit 0. Also, `git merge-base --is-ancestor v0.10.1 HEAD` exits 0. If either fails, HALT: there is nothing true to record.
- Execute sequentially on the main working tree. The orchestrator dispatches this plan with isolation `none` and re-records `.gsd/dispatch-isolation-sentinel.json` as `none` before dispatch; the executor never edits `.gsd/`.
- Routing precondition: `git rev-parse --show-toplevel` prints /Users/jon/projects/threadline and `git branch --show-current` prints fix/branch-protection-actions-capability. Otherwise HALT with the message "205-02 must run in the main working tree on fix/branch-protection-actions-capability (isolation none); got <toplevel> on <branch>".
- Shell note: the tool shell is zsh; bash-only constructs run inside one `bash -c '...'`.
- Commits use `docs(205-02): ...` and end with the line `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.
- NEVER `git add .planning/`, `git add -A`, `git add .` or `git commit -a`. Stage each file by its explicit path.
- Protected paths the executor must NOT modify, stage, move, stash, replace or restore: `.planning/WINDOWS.md`, `.planning/config.json`, `.tool-versions` (untracked), `.gsd/`.
- Do not edit `.planning/v1.41-MILESTONE-AUDIT.md` or `.planning/phases/202-release-0-10-0/202-REVIEW.md`.
- No `--no-verify`, no `core.hooksPath` override, no push, no tag, no publish.
- gsd-tools is `node ~/.claude/gsd-core/bin/gsd-tools.cjs` (gsd-core v1.14.0), with flag-style arguments only. Hand-check STATE.md and ROADMAP.md after every call.

<tasks>

<task type="auto">
  <name>Task 1: 202-VERIFICATION addendum and REQUIREMENTS traceability for RELEASE-02/05</name>
  <reversibility rating="reversible">Appended documentation and two table-cell edits in .planning files, reverted by `git revert`.</reversibility>
  <files>.planning/phases/202-release-0-10-0/202-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/202-release-0-10-0/202-VERIFICATION.md (frontmatter :1-:59 including the `# NOTE: shipped code was verified at origin/main 53b5d71a` comment at :28-:29; the last section before end of file at :202-:205)
    - .planning/REQUIREMENTS.md (:69 RELEASE-02 and :72 RELEASE-05 checkboxes; traceability rows at :182 and :185)
    - .planning/phases/205-release-reconciliation/205-01-SUMMARY.md (merge SHA, follow-up SHAs, gate table)
    - .planning/v1.41-MILESTONE-AUDIT.md (frontmatter `gaps.requirements` RELEASE-02/05 evidence text and `integration` F1)
  </read_first>
  <action>
    In 202-VERIFICATION.md, append at end of file (after all existing content) a section headed `## Addendum (2026-09-24, Phase 205): verified on the milestone branch`. State that the v1.41 audit (F1) found this report's evidence came from origin/main 53b5d71a, not the milestone branch, and that Phase 205 merged origin/main 471ebf6e (v0.10.1) into `fix/branch-protection-actions-capability`. Then give a short evidence table, with values copied from 205-01-SUMMARY.md and not re-derived: merge commit SHA and its two parents; `git merge-base --is-ancestor v0.10.0 HEAD` and `v0.10.1 HEAD` both exit 0; `git rev-list --count HEAD..origin/main` = 0; mix.exs `@version "0.10.1"` and `MIX_ENV=dev mix release.pins --check` clean; `release.yml` `sync-release-pr-pins` present and pinned by the new test in release_control_plane_contract_test.exs; changelog contract green; `mix verify.bump_rehearsal` 0.10.1 -> 0.11.0 OK (closes audit F2); `mix verify.release` exit 0 in a clean clone; full `mix test` count with 0 failures. End with one sentence saying RELEASE-02 and RELEASE-05 now hold on the milestone branch, and that the frontmatter above is historical and deliberately unchanged. Do not change the frontmatter, the NOTE comment, or any earlier section.

    In REQUIREMENTS.md, change only the two traceability rows. `| RELEASE-02 | Phase 202 | Complete |` becomes `| RELEASE-02 | Phase 202, Phase 205 | Complete |`, and `| RELEASE-05 | Phase 202 | Complete |` becomes `| RELEASE-05 | Phase 202, Phase 205 | Complete |`. Leave both `[x]` checkboxes and every other row as they are. If the file carries a phase-to-requirements summary table with a 205 row or a `Last updated` footer, update those in the same edit to name 205 with RELEASE-02 and RELEASE-05. If no such row exists, do not add a table.

    Stage exactly those two paths by explicit path and commit as `docs(205-02): record RELEASE-02/05 as verified on the milestone branch`.
  </action>
  <verify>
    <automated>bash -c 'grep -c "^## Addendum (2026-09-24, Phase 205): verified on the milestone branch$" .planning/phases/202-release-0-10-0/202-VERIFICATION.md | grep -qx 1 && grep -c "^| RELEASE-02 | Phase 202, Phase 205 | Complete |$" .planning/REQUIREMENTS.md | grep -qx 1 && grep -c "^| RELEASE-05 | Phase 202, Phase 205 | Complete |$" .planning/REQUIREMENTS.md | grep -qx 1 && git diff --quiet "$(cat "$(git rev-parse --git-dir)/gsd-205-premerge")" HEAD -- .planning/v1.41-MILESTONE-AUDIT.md .planning/phases/202-release-0-10-0/202-REVIEW.md'</automated>
    <fails_when>non-zero exit: the addendum heading is missing or duplicated, either traceability row does not read `Phase 202, Phase 205 | Complete`, or the audit or 202-REVIEW.md differs from the pre-merge SHA</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat "$(cat "$(git rev-parse --git-dir)/gsd-205-premerge")" HEAD -- .planning/phases/202-release-0-10-0/202-VERIFICATION.md | cut -f2` prints exactly `0` (a failed git diff prints nothing and fails this check) (append only; no existing line removed or changed)
    - `grep -c '53b5d71a' .planning/phases/202-release-0-10-0/202-VERIFICATION.md` is at least 2 (the original NOTE plus the addendum's reference)
    - the addendum contains the merge commit SHA recorded in 205-01-SUMMARY.md: `M=$(git rev-parse --short=8 "$(git rev-list --merges -n1 HEAD)") && test -n "$M" && grep -c "$M" .planning/phases/202-release-0-10-0/202-VERIFICATION.md` exits 0 and prints at least 1 (the addendum cites the merge SHA with at least 8 hex characters)
    - the addendum contains the literal `0.10.1 -> 0.11.0`
    - `grep -c '^- \[x\] \*\*RELEASE-02\*\*' .planning/REQUIREMENTS.md` prints 1 and `grep -c '^- \[x\] \*\*RELEASE-05\*\*' .planning/REQUIREMENTS.md` prints 1
    - `git show --name-only --format= HEAD | sort` prints exactly `.planning/REQUIREMENTS.md` and `.planning/phases/202-release-0-10-0/202-VERIFICATION.md`
    - `git log -1 --format=%s` prints `docs(205-02): record RELEASE-02/05 as verified on the milestone branch`
  </acceptance_criteria>
  <done>202-VERIFICATION carries an append-only addendum proving RELEASE-02/05 on the milestone branch, and REQUIREMENTS traceability names Phase 205 for both. The audit and 202-REVIEW are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Record 202-REVIEW's post-#46 disposition, then mark Phase 205 complete in STATE.md and ROADMAP.md with a hand-checked progress block</name>
  <reversibility rating="reversible">Appended register section and state/roadmap edits in .planning files, reverted by `git revert`.</reversibility>
  <files>.planning/phases/202-release-0-10-0/deferred-items.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/202-release-0-10-0/deferred-items.md (whole file; section heading style and the last section)
    - .planning/phases/202-release-0-10-0/202-REVIEW.md (headings :67-:230: CR-01, WR-01..WR-07, IN-01..IN-07; read only, do not edit)
    - `git show 45532778 --stat` and `git log -1 --format=%B 45532778` (the #46 commit message naming what it fixed)
    - .planning/STATE.md (frontmatter :1-:20 progress block; `## Current Position` at about :30; the `Progress:` line at about :163; the decisions list tail at about :830)
    - .planning/ROADMAP.md (:75 the Phase 205 checklist line; `### Phase 205` details at about :882-:895; the progress table row at about :929)
    - .planning/phases/205-release-reconciliation/205-01-SUMMARY.md
  </read_first>
  <action>
    Step 1, review disposition. Append to 202 deferred-items.md a section headed `## 202-REVIEW disposition after #46 (recorded 2026-09-24, Phase 205)`. List each review item on its own line in the form `- **<ID>** <status>: <one-line reason>` (for example `- **WR-04** open: ...`). CR-01 is `resolved` by #46 (45532778): the advice now prints after generation, names the files, says delete then re-run, and is withheld when nothing was written, with 4 tests in test/mix/tasks/threadline/install_test.exs. WR-01 is `resolved` (persist-credentials false, PUSH_TOKEN only on the push step, now pinned by the 205-01 contract test). WR-02 is `resolved` (job concurrency group `sync-release-pr-pins`, pinned by the same test). WR-03 is `resolved` (bootstrap `always()`, pinned by the same test). WR-05 is `resolved` (configuration guide default `"public"` plus the storage_schema_test tie). WR-04 (stale rehearsal tarball), WR-06 (vacuous legacy-schema read-back test) and WR-07 (environment-protection property) are `open`. IN-01..IN-07 are `open (info)`. Before writing each `resolved` line, confirm it against `git show 45532778` and the merged files. If any item the research calls closed is not visibly addressed in the diff, record it as `open` and say why in the SUMMARY. Add one closing line saying all resolved items reached the milestone branch in the 205-01 merge commit (give its short SHA). Do not edit 202-REVIEW.md.

    Step 2, STATE.md and ROADMAP.md. Use `node ~/.claude/gsd-core/bin/gsd-tools.cjs` with flag-style arguments for any state or roadmap handler you call (for example the phase-complete / plan-complete handlers the execute-plan workflow uses). Then hand-check and correct both files. ROADMAP.md: the :75 line becomes `- [x] **Phase 205: Release Reconciliation** ...` with the rest of its text unchanged. The Phase 205 details block reads `**Plans:** 2 plans` and lists `- [x] 205-01-PLAN.md — ...` and `- [x] 205-02-PLAN.md — ...` with the objectives the planner wrote there. The progress table row reads `| 205. Release Reconciliation | v1.41 | 2/2 | Complete    | 2026-09-24 |`. STATE.md frontmatter: `completed_phases: 8`, `total_plans: 148`, `completed_plans: 148`, `percent: 100`, `status` reflecting phase complete, and `stopped_at` naming Phase 205 complete. The `Progress:` line reads 148/148 plans and 100% of phases (8 of 8 complete: 198–205). The `## Current Position` block says Phase 205 is complete and the next step is re-running `/gsd-audit-milestone` for v1.41. Append one decisions-list entry `- [Phase 205]: 205-01: merged origin/main 471ebf6e (v0.10.1) as one merge commit <short SHA>; 17 conflicts resolved whole-file (10 ours / 4 theirs / 3 combined); rehearsal 0.10.1 -> 0.11.0 OK; sync-pins job pinned by contract test`. Record the exact before/after lines of both files in the 205-02 SUMMARY.

    Step 3, commit. Stage exactly `.planning/phases/202-release-0-10-0/deferred-items.md`, `.planning/STATE.md` and `.planning/ROADMAP.md` by explicit path and commit as `docs(205-02): record 202 review disposition and mark phase 205 complete`. Confirm `git show --name-only --format= HEAD` lists only those three paths, and that the protected-path fingerprint from 205-01 still checks OK.
  </action>
  <verify>
    <automated>bash -c 'grep -q "^## 202-REVIEW disposition after #46 (recorded 2026-09-24, Phase 205)$" .planning/phases/202-release-0-10-0/deferred-items.md && grep -q "^  completed_plans: 148$" .planning/STATE.md && grep -q "^  percent: 100$" .planning/STATE.md && grep -q "^  completed_phases: 8$" .planning/STATE.md && grep -q "^- \[x\] \*\*Phase 205: Release Reconciliation\*\*" .planning/ROADMAP.md && grep -q "^| 205. Release Reconciliation | v1.41 | 2/2 | Complete" .planning/ROADMAP.md && shasum -a 256 -c "$(git rev-parse --git-dir)/gsd-205-protected.sha256"'</automated>
    <fails_when>non-zero exit: the disposition heading is missing, the STATE progress block is not 148/148 plans, 8 phases and 100 percent (for example a handler-miscomputed 147 or 94), the ROADMAP 205 line is unchecked or its table row is not 2/2 Complete, or a protected file's SHA-256 changed ("FAILED")</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \*\*(CR-01|WR-01|WR-02|WR-03|WR-05)\*\*.*resolved' .planning/phases/202-release-0-10-0/deferred-items.md` prints 5, unless the SUMMARY records a justified downgrade to `open`
    - `grep -cE '^- \*\*(WR-04|WR-06|WR-07)\*\*.*open' .planning/phases/202-release-0-10-0/deferred-items.md` prints 3
    - `git diff --numstat "$(cat "$(git rev-parse --git-dir)/gsd-205-premerge")" HEAD -- .planning/phases/202-release-0-10-0/deferred-items.md | cut -f2` prints exactly `0` (a failed git diff prints nothing and fails this check) (append only)
    - `git diff --quiet "$(cat "$(git rev-parse --git-dir)/gsd-205-premerge")" HEAD -- .planning/phases/202-release-0-10-0/202-REVIEW.md .planning/v1.41-MILESTONE-AUDIT.md` exits 0
    - `grep -c '^\*\*Plans:\*\* 2 plans$' .planning/ROADMAP.md` is at least 1, and `grep -c '205-0[12]-PLAN.md' .planning/ROADMAP.md` prints 2
    - `grep -c '\[Phase 205\]: 205-01: merged origin/main 471ebf6e' .planning/STATE.md` prints 1
    - `git show --name-only --format= HEAD | sort` prints exactly `.planning/ROADMAP.md`, `.planning/STATE.md` and `.planning/phases/202-release-0-10-0/deferred-items.md`
    - `git rev-parse origin/main` prints 471ebf6e67365e2ad16a1b8dbac991e5da1ec3b4 (nothing fetched or pushed)
  </acceptance_criteria>
  <done>202's review disposition is recorded without rewriting the review. Phase 205 is complete in STATE.md and ROADMAP.md with a correct 148/148, 8/8, 100% progress block, and the next action (re-run the milestone audit) is named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local .planning/ → public repository | .planning/ history must never be pushed; a stray `git add .planning/` would stage ~1066 machine-local critic files |
| gsd-tools state handlers → STATE.md | Handlers are known to miscompute the bespoke progress block |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-205-07 | Information disclosure | .planning/ staging | high | mitigate | Explicit-path staging only. Each commit's `git show --name-only` is asserted to list exactly the named files. No push (tags and .planning history stay local). |
| T-205-08 | Repudiation | 202-VERIFICATION / audit record | medium | mitigate | Append-only addendum, asserted by zero removed lines in the diff against the pre-merge SHA. The audit and 202-REVIEW are asserted byte-unchanged. |
| T-205-09 | Tampering | STATE.md progress block | low | mitigate | Hand-check after every handler call, with exact expected values (148/148, 8, 100) asserted in verify. |
| T-205-10 | Tampering | protected .planning/WINDOWS.md, config.json | medium | mitigate | The SHA-256 fingerprint from 205-01 is re-checked in Task 2's verify. |
</threat_model>

<verification>
- Task 1: the addendum and the two traceability rows are present; the audit and 202-REVIEW are unchanged.
- Task 2: the review disposition is appended; STATE/ROADMAP show 205 complete with a hand-checked 148/148, 8/8, 100% progress block; the protected-path fingerprint is intact.
</verification>

<success_criteria>
- The planning record states, with the 205-01 merge SHA, that RELEASE-02 and RELEASE-05 hold on the milestone branch.
- 202-REVIEW's post-#46 status is discoverable in 202 deferred-items.md: 5 resolved, 3 warnings open, info items open.
- Phase 205 is complete in STATE.md and ROADMAP.md, and the next step is a re-audit.
</success_criteria>

## Artifacts this phase produces (this plan)

- `.planning/phases/202-release-0-10-0/202-VERIFICATION.md`: the appended `## Addendum (2026-09-24, Phase 205)` evidence section
- `.planning/REQUIREMENTS.md`: RELEASE-02 and RELEASE-05 traceability rows naming Phase 205
- `.planning/phases/202-release-0-10-0/deferred-items.md`: the appended 202-REVIEW disposition section
- `.planning/STATE.md` and `.planning/ROADMAP.md`: Phase 205 complete, progress 148/148, 8/8, 100%
- `.planning/phases/205-release-reconciliation/205-02-SUMMARY.md`: before/after lines of STATE.md and ROADMAP.md and any justified disposition downgrade

<output>
Create `.planning/phases/205-release-reconciliation/205-02-SUMMARY.md` when done, with `requirements-completed: [RELEASE-02, RELEASE-05]` in its frontmatter and the exact before/after STATE.md and ROADMAP.md lines. Stage it by its explicit path only.
</output>