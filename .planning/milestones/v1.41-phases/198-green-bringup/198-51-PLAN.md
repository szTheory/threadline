---
phase: 198-green-bringup
plan: 51
type: execute
wave: 2
depends_on: [198-48, 198-49, 198-50]
files_modified:
  - .planning/audits/198-round10-ref-disposition.md
  - .planning/audits/198-round10-ref-disposition.json
  - bin/verify-phase198-ref-disposition
  - test/threadline/phase198_ref_disposition_contract_test.exs
autonomous: false
gap_closure: true
requirements: [GREEN-12]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The three surviving Phase-198 local/remote branches and PRs #29/#32/#33 have a timestamped, schema-validated, full-SHA ancestry/diff/open-state inventory before any ref changes."
    - "The maintainer explicitly chooses retire, preserve, or abort; silence and auto-advance cannot authorize remote ref deletion or PR closure."
    - "GREEN-07's exact-main/ancestry gap and D-39 accepted-Pending disposition are recorded separately and are not treated as authority to merge PR #34 or mutate main."
  artifacts:
    - "198-round10-ref-disposition.md — immutable pre-mutation inventory and verbatim maintainer selection"
    - "198-round10-ref-disposition.json — structured target inventory, stable external-control digests, and decision subject binding"
    - "bin/verify-phase198-ref-disposition — fail-closed inventory/authority/post-mutation validator consumed by Plans 51-52"
    - "test/threadline/phase198_ref_disposition_contract_test.exs — missing-field, malformed-subject, drift, and no-mutation fixtures"
  key_links:
    - "local/remote ref SHA + ancestry/diff -> per-ref recommendation -> blocking-human decision -> Plan 52 precondition"
  prohibitions:
    - {statement: "No branch, tag, pull request, ruleset, protection setting, or main ref may be changed before explicit selection is recorded.", status: unverified, flagged: true}
    - {statement: "PR #34 and origin/main must not be merged, pushed, rebased, or bypassed by this branch-hygiene decision.", status: unverified, flagged: true}
---

<objective>
Create the evidence and obtain the authority required to disposition the three stale Phase-198 measurement branches.

Purpose: GREEN-12 requires intentional branch retirement, while current decisions explicitly forbid inferring permission for remote mutation.

Output: a current ref/PR inventory and a non-auto-approvable maintainer decision consumed by Plan 52.

<assumption_delta_decision>Primary noun: immutable archived ref. Decision: no-change — mutable measurement branches may be retired only after their exact objects and recommendations are durable.</assumption_delta_decision>

### Artifacts this phase produces

- New paths: `.planning/audits/198-round10-ref-disposition.md`, `.planning/audits/198-round10-ref-disposition.json`, `bin/verify-phase198-ref-disposition`, and `test/threadline/phase198_ref_disposition_contract_test.exs`.
- New decision field: one of `retire`, `preserve`, or `abort`, recorded verbatim with timestamp and exact targets.
- No new code, dependency, schema, UI, tag, branch, PR change, or other external mutation in this plan.

### Spec-less probe accounting

- GREEN-12 adjacency/empty/ordering rows are explicit: local branch, remote ref, PR, archive object, and register row are treated as distinct adjacent states; empty/missing state fails; inventory and decision precede mutation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-39-DECISION.md
@.planning/ARCHIVE-REGISTER.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Pin the three stale branch/remote/PR subjects and recommendations</name>
  <files>.planning/audits/198-round10-ref-disposition.md, .planning/audits/198-round10-ref-disposition.json, bin/verify-phase198-ref-disposition, test/threadline/phase198_ref_disposition_contract_test.exs</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-VERIFICATION.md — Round-10 GREEN-12 branch list and no-mutation constraint
    - .planning/ARCHIVE-REGISTER.md — D-31 message fields and existing restore-command convention
    - .planning/phases/198-green-bringup/198-39-DECISION.md — no inferred push/merge/protection authority and GREEN-07 separation
    - .planning/phases/198-green-bringup/198-47-SUMMARY.md — current PR #34/main boundary
    - bin/verify-branch-protection — established read-only ruleset/protection and required-context observation contract
    - .planning/audits/198-round10-ref-disposition.json — create the structured target inventory and stable external-control snapshot consumed by the validator
    - bin/verify-phase198-ref-disposition — create the fail-closed schema/live-state validator used before and after Plan-52 mutations
    - test/threadline/phase198_ref_disposition_contract_test.exs — create mutation fixtures for every required field and invariant
  </read_first>
  <action>After Plans 48-50 complete, use read-only `git`, `gh pr view`, and the established read-only protection verifier to write both the human-readable Markdown packet and a strict JSON inventory for exactly `ci/198-gap-closure`/PR #29, `ci/198-round5`/PR #32, and `ci/198-round6`/PR #33. Each target object must contain its literal branch and PR number; full local and remote SHAs; merge bases with current HEAD and local main; ancestry booleans against both; ahead/behind and unique-commit identities; machine-readable diffstat; PR state/draft/merge-state/head/base/full head SHA; evidence purpose; recommendation with rationale; restore command; and observation timestamp. Record the complete derived local/remote `ci/198-*` sets as the exact mutable target universe, not as an invariant during authorized retirement. Capture stable external controls separately: origin/main full SHA, PR #34 state/draft/head/base/full head SHA, ruleset/protection canonical digest, required-context set/digest, and worktree list digest. Record the observed active branch/upstream for provenance only, explicitly outside the decision authority and cross-plan equality controls because Plan 51 must commit this decision packet and summary before Plan 52 starts. Create `bin/verify-phase198-ref-disposition` with `inventory`, `decision`, `authority`, `controls`, `post-target`, and `final` stages; this task implements and invokes `inventory`, which validates exact keys/types/full-SHA formats, exactly three targets, target-specific invariants, Markdown/JSON agreement, live target equality, exact target-universe equality, and stable external-control equality. Create ExUnit mutation fixtures that delete or corrupt every required field, swap head/base or PR mappings, introduce a fourth target, change a stable external control, wrongly promote the observed active tip into cross-plan authority, and simulate a write-side command receipt; each must fail. Recommend retire only if both tips are reachable and no unique unpreserved work exists; otherwise recommend preserve and name the work. Perform no fetch or external mutation.</action>
  <reversibility rating="reversible">This plan records read-only evidence before any ref lifecycle choice.</reversibility>
  <verify>
    <automated>mix test test/threadline/phase198_ref_disposition_contract_test.exs &amp;&amp; bin/verify-phase198-ref-disposition inventory --inventory .planning/audits/198-round10-ref-disposition.json --decision .planning/audits/198-round10-ref-disposition.md</automated>
    <fails_when>Any required key/type/full SHA, ancestry, unique-commit set, diffstat, head/base, evidence purpose, recommendation, restore command, timestamp, stable external-control digest, Markdown/JSON join, exact target mapping, live-target equality, or no-mutation invariant is absent or false; the check also fails if an active-branch tip is treated as cross-plan authority.</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly three target objects and Markdown rows are complete, timestamped, and joined by literal branch plus PR number.
    - The validator and its test file are executable repository-owned controls; every required inventory field and target-specific invariant has a failing mutation fixture.
    - PR #34, origin/main, protection/ruleset, required contexts, and worktrees are pinned as stable external controls and unchanged after observation; complete local/remote `ci/198-*` sets are the mutable target universe, while active branch/upstream are provenance only and cannot stale Plan 52 authority when Plan 51 commits.
    - Recommendation follows evidence and names any unique work rather than silently discarding it.
    - The entry records no decision before the checkpoint.
  </acceptance_criteria>
  <done>The maintainer can decide against immutable, current, target-specific evidence.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Choose the disposition for stale Phase-198 measurement refs and their open pull requests.</decision>
  <read_first>
    - .planning/audits/198-round10-ref-disposition.md — validated readable inventory and recommendations
    - .planning/audits/198-round10-ref-disposition.json — exact pinned target subjects and stable external controls the selection will bind
    - bin/verify-phase198-ref-disposition — `authority` stage contract Plan 52 must satisfy
    - .planning/phases/198-green-bringup/198-39-DECISION.md — GREEN-07 accepted-Pending decision that grants no mutation authority
  </read_first>
  <context>Remote branch deletion and PR closure are external state changes. D-31/D-32 require preservation and durability; 198-39/47 forbid inferring authority. This decision applies only to ci/198-gap-closure (#29), ci/198-round5 (#32), and ci/198-round6 (#33). It does not authorize any change to PR #34, origin/main, CI required, rulesets, protection, or D-39.</context>
  <options>
    <option id="retire">
      <name>Archive then retire</name>
      <pros>Create/push annotated archive tags, update the register, close the three PRs, delete their remote refs, then delete their local branches; satisfies the actionable GREEN-12 hygiene gap while preserving exact objects.</pros>
      <cons>Changes hosted ref/PR state and adds permanent archive tags.</cons>
    </option>
    <option id="preserve">
      <name>Preserve explicitly</name>
      <pros>Leaves all refs and PRs unchanged and records a maintainer-approved reason/owner/review date.</pros>
      <cons>GREEN-12 remains unmet and security/verification must retain the open hygiene finding.</cons>
    </option>
    <option id="abort">
      <name>Abort mutation</name>
      <pros>Stops with no external change if the inventory is disputed or authorization is premature.</pros>
      <cons>No branch-hygiene gap closes.</cons>
    </option>
  </options>
  <resume-signal>Reply `retire`, `preserve` with a reason/owner/review date, or `abort`. The executor must append the selection verbatim to 198-round10-ref-disposition.md before Plan 51 completes.</resume-signal>
  <verify>
    <automated>bin/verify-phase198-ref-disposition decision --inventory .planning/audits/198-round10-ref-disposition.json --decision .planning/audits/198-round10-ref-disposition.md</automated>
    <fails_when>The recorded response is absent, normalized instead of verbatim, not one of the three options, lacks its timestamp/inventory digest/exact pinned target subjects, grants mutation under preserve/abort, any live target or stable external control drifted before recording, or authority depends on equality with the pre-commit active branch tip.</fails_when>
  </verify>
  <acceptance_criteria>
    - The verbatim response, timestamp, exact three `(branch, PR, local SHA, remote SHA, head, base)` subjects, and SHA-256 digest of the validated JSON inventory are appended to the Markdown decision section.
    - `retire` grants only archive/tag/register/close/delete authority for those unchanged subjects; `preserve` and `abort` grant no mutation authority.
    - Silence, auto-advance, a recommendation, D-39, or a response with mismatched subjects cannot satisfy the checkpoint.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| mutable refs/PRs -> disposition evidence | Branch tips and hosted PR state can move between observation and action. |
| planning -> external mutation authority | A remediation recommendation is not maintainer authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-51-01 | Spoofing | ref identity | high | mitigate | Strict JSON inventory pins local/remote full SHAs, PR/head/base mappings, timestamp, merge bases, and live-state joins before decision. |
| T-198-51-02 | Repudiation | authorization | high | mitigate | Blocking-human decision with verbatim selection and exact target list. |
| T-198-51-03 | Tampering | unrelated refs | high | mitigate | Pin and validate stable external digests for PR #34, main, worktrees, rulesets/protection, and required contexts; bind the exact Phase-198 ref set as the authorized mutable target universe and leave the active branch tip outside cross-plan authority. |
| T-198-47-06 | Denial of service | unavailable observation | low | mitigate | Fail Task 1 without writing a partial current-state verdict; do not accept unavailable evidence as risk. |
</threat_model>

<verification>
- Compare the three target SHAs immediately before the checkpoint and record any drift as a new inventory rather than overwriting observations.
- Confirm `git status --short --branch`, `git branch`, remote refs, and PR states are unchanged by Task 1 before its normal artifact commit; Plan 52 must establish its own active-tip baseline after the Plan-51 commit.
</verification>

<success_criteria>
- A non-auto-approvable decision grants or withholds authority for exactly the three stale measurement refs.
- No external state changes in this plan.
- GREEN-07 remains governed by D-39 and is neither promoted nor re-litigated.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-51-SUMMARY.md` when done, recording the selected option without claiming Plan 52 effects.</output>
