---
phase: 198-green-bringup
plan: 53
type: execute
wave: 3
depends_on: [198-51]
files_modified:
  - .planning/audits/198-round11-ref-disposition.md
  - .planning/audits/198-round11-ref-disposition.json
  - bin/verify-phase198-ref-disposition
  - test/threadline/phase198_ref_disposition_contract_test.exs
autonomous: true
gap_closure: true
requirements: [GREEN-12]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The complete live local and remote `ci/198-*` namespaces, including refs omitted from Plan 51's decision packet, are enumerated from Git and joined to full-SHA ancestry, unique-commit, diffstat, PR, archive, and restore evidence before any new authority is requested."
    - "Plan 52 is preserved but explicitly superseded as inapplicable after the Plan-51 `abort`; the new inventory never treats that abort, an ancestry recommendation, silence, or auto-advance as mutation authority."
    - "Every local-only, remote-only, and divergent local/remote tip is modeled as a distinct preservation subject, so a branch name cannot hide a second object that would be discarded."
    - "The round-11 validator positively proves a fixture-only valid-retire path through decision, authority, controls, post-target, and final before Plans 54-55 may consume those stages, while the round-10 three-target record remains compatible."
  artifacts:
    - ".planning/audits/198-round11-ref-disposition.md — readable six-ref scope, distinct-tip preservation packets, Plan-52 supersession note, and undecided authority section"
    - ".planning/audits/198-round11-ref-disposition.json — machine-readable complete namespace, subject packets, stable controls, null decision, null execution, and empty receipts"
    - "bin/verify-phase198-ref-disposition — backward-compatible round-10 checks plus complete-universe round-11 validation"
    - "test/threadline/phase198_ref_disposition_contract_test.exs — positive full-lifecycle and mutation fixtures for omitted refs, divergent tips, absent PRs, scope swaps, false authority, stable-control drift, receipt ordering, and live-derived final emptiness"
  key_links:
    - "live ref enumeration -> distinct object inventory -> Markdown/JSON digest join -> blocking decision packet"
    - "fixture decision -> exact authority -> stable controls -> preserve-before-delete receipts -> per-target absence -> live-derived final namespace emptiness"
  assumptions:
    - {statement: "Spec-less GREEN-12 adjacency rows remain unresolved until the inventory proves local-only, remote-only, and divergent-tip cases explicitly; this plan supplies that evidence rather than assuming branch-name equality.", status: unverified, flagged: true}
    - {statement: "Spec-less GREEN-12 empty-input and ordering rows are validator responsibilities: Plan 53 must reject an empty initial universe and out-of-order retirement receipts before Plan 55 performs a live mutation.", status: unverified, flagged: true}
    - {statement: "The other 30 unresolved spec-less rows across GREEN-01..GREEN-11 remain accounted for by executed Plans 01-51 and are not reopened by this GREEN-12-only closure.", status: unverified, flagged: true}
  prohibitions:
    - {statement: "A stale-ref recommendation, historical maintainer response, or complete ancestry proof must never be converted into branch, pull-request, tag, main, ruleset, or protection mutation authority.", status: unverified, flagged: true}
    - {statement: "No preservation subject may be collapsed merely because its local and remote refs share a branch name.", status: unverified, flagged: true}
---

<objective>
Replace Plan 52's incomplete three-target premise with a complete, read-only, decision-ready stale-ref inventory for GREEN-12.

Purpose: Plan 51 correctly halted when six remote refs existed outside its exact-three authority. This plan preserves that abort, supersedes only the inapplicable Plan 52 execution contract, and makes every live object visible before the maintainer is asked for fresh authority.

Output: a round-11 Markdown/JSON inventory and a backward-compatible fail-closed validator/test suite.

<assumption_delta_decision>Primary noun: preservation subject. Decision: promote — the object identity (branch side plus full SHA), not the branch name alone, is primary because one branch can have divergent local and remote tips.</assumption_delta_decision>

### Artifacts this phase produces

- `.planning/audits/198-round11-ref-disposition.md` and `.json` with a complete live `ci/198-*` universe and one preservation packet per distinct tip.
- Extended round-aware symbols/stages in `bin/verify-phase198-ref-disposition` for round-11 `inventory`, `decision`, `authority`, `controls`, `post-target`, and `final` validation without breaking round 10.
- New mutation fixtures in `test/threadline/phase198_ref_disposition_contract_test.exs` for omitted remote refs, divergent tips, no-PR refs, subject swaps, stale digests, and unauthorized decisions.
- No branch, pull-request, tag, `main`, ruleset, protection, workflow, product, schema, or dependency mutation.

### Multi-source coverage audit

- GOAL: GREEN-12's remaining repository-hygiene clause is covered here and by Plans 54-55; main landing/CI remains GREEN-07 Pending under D-39 and is not claimed.
- REQ: GREEN-12 is the only reopened requirement. GREEN-01..GREEN-06 and GREEN-08..GREEN-11 remain covered by completed plans; GREEN-07 remains explicitly Pending.
- RESEARCH: preservation-first archive tags, exact GitHub state, one-worktree evidence, and no silent discard remain controlling.
- CONTEXT: D-31 archive metadata and restoreability are retained; D-39/D-42 protected gates and evidence are untouched; deferred ideas remain excluded.
- VERIFICATION/UAT/SECURITY: 192/192 automated UAT remains evidence coverage rather than requirement promotion; the current stale-ref scope conflict and Plan-51 abort are the actionable gap; no open high security threat is waived.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-51-SUMMARY.md
@.planning/phases/198-green-bringup/198-52-PLAN.md
@.planning/audits/198-round10-ref-disposition.md
@.planning/audits/198-round10-ref-disposition.json
@.planning/ARCHIVE-REGISTER.md
@.planning/phases/198-green-bringup/198-39-DECISION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Inventory one divergent branch through both object-preservation paths</name>
  <files>.planning/audits/198-round11-ref-disposition.md, .planning/audits/198-round11-ref-disposition.json, bin/verify-phase198-ref-disposition, test/threadline/phase198_ref_disposition_contract_test.exs</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-51-SUMMARY.md — verbatim abort, six-ref conflict, and unchanged-control boundary
    - .planning/audits/198-round10-ref-disposition.md — local/remote divergence and exact authority joins already established
    - .planning/audits/198-round10-ref-disposition.json — round-10 schema that must remain valid
    - bin/verify-phase198-ref-disposition — existing exact-three assumptions and live comparison stages
    - test/threadline/phase198_ref_disposition_contract_test.exs — fixture mutation idiom and current compatibility contract
    - .planning/ARCHIVE-REGISTER.md — D-31 archive metadata and restore-command convention
  </read_first>
  <behavior>
    - Test 1: a fixture with the same branch name but different local and remote SHAs yields two distinct preservation subjects and two non-colliding archive tag names.
    - Test 2: removing either side, substituting one SHA for the other, or collapsing them into one subject fails inventory validation.
    - Test 3: the canonical round-10 inventory still passes its existing validation unchanged.
  </behavior>
  <action>Start RED with fixtures derived from the observed divergent `ci/198-gap-closure` shape. Extend the validator schema using an explicit round/schema discriminator so round 10 retains its exact-three contract while round 11 derives preservation subjects from side-plus-full-SHA identity. Build the round-11 Markdown/JSON record for that one branch end-to-end: live local and remote refs, ancestry against current HEAD/local main, unique commits, machine-readable numstat, PR head/base/state when present, recommendation rationale, collision-free local-side and remote-side archive names, and restore commands. Bind readable rows to canonical JSON with a digest and exact join markers. Keep `decision` and `execution` null and receipts empty. Record that Plan 52 is preserved but superseded because Plan 51's verbatim abort and incomplete scope make its precondition false; do not edit Plan 52.</action>
  <reversibility rating="reversible">This task adds read-only evidence and compatible validation; corrections are appendable without changing external state.</reversibility>
  <verify>
    <automated>mix test test/threadline/phase198_ref_disposition_contract_test.exs &amp;&amp; bin/verify-phase198-ref-disposition inventory --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md</automated>
    <fails_when>The focused test fails; the round-10 fixture regresses; the live divergent branch is collapsed; either full SHA, ancestry, diffstat, PR join, archive name, restore command, digest, or Plan-52 supersession marker is absent/stale; or any decision, execution, or mutation receipt exists.</fails_when>
  </verify>
  <acceptance_criteria>
    - The divergent branch is represented by both exact objects with distinct archive/restore paths.
    - Round-10 validation remains green, proving completed Plan-51 evidence was not rewritten or invalidated.
    - No external mutation occurs and no authority is inferred.
  </acceptance_criteria>
  <done>One real branch proves the complete read-only path from live refs to two independently recoverable decision subjects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the complete namespace and prove every round-11 validator transition in fixtures</name>
  <files>.planning/audits/198-round11-ref-disposition.md, .planning/audits/198-round11-ref-disposition.json, bin/verify-phase198-ref-disposition, test/threadline/phase198_ref_disposition_contract_test.exs</files>
  <read_first>
    - .planning/audits/198-round11-ref-disposition.md — Task-1 divergent-tip representation to expand
    - .planning/audits/198-round11-ref-disposition.json — Task-1 schema/digest subject
    - bin/verify-phase198-ref-disposition — implement the existing `inventory|decision|authority|controls|post-target|final` CLI surface rather than adding a second validator
    - test/threadline/phase198_ref_disposition_contract_test.exs — extend the existing temp-file mutation helpers and explicit `PHASE198_REF_DISPOSITION_LIVE_FIXTURE` injection boundary
    - .planning/audits/198-round10-ref-disposition.json — retained schema-v1 exact-three compatibility fixture
    - .planning/audits/198-round10-ref-disposition.md — retained round-10 Markdown joins and inventory digest fixture
    - .planning/ARCHIVE-REGISTER.md — exact D-31 register-row fields that preservation receipts must join before retirement receipts
    - .planning/phases/198-green-bringup/198-SECURITY.md — exact-scope, no-weakening, and no-secret evidence constraints
  </read_first>
  <behavior>
    - Test 1: a canonical round-11 fixture with three local refs, six remote refs, a divergent same-name local/remote branch, distinct side-plus-full-SHA preservation subjects, and at least one remote-only subject whose pull-request value is null passes `inventory` with decision/execution null and no receipts.
    - Test 2: the same fixture with a digest-bound verbatim `retire` decision passes `decision`, then passes `authority --target` for each exact target; `preserve`, `abort`, silence, a stale digest, a missing subject, or a target outside the complete subject set fails authority.
    - Test 3: a staged valid-retire fixture passes `controls` after each receipt, passes `post-target` only after every distinct tip for that target has local annotated-tag, exact single-tag push/remote peeled-object, and D-31 register receipts before its PR/remote-ref/local-ref retirement receipts, and handles a null PR without inventing a close receipt.
    - Test 4: changing origin/main, PR #34 identity/head SHA, effective-rules/protection digest, required contexts, worktree identities, or the task-local active branch/upstream baseline makes `controls`, `post-target`, and `final` fail; the fixture-injection path cannot bypass these comparisons.
    - Test 5: moving any PR-close or branch-delete receipt before the complete preservation receipt set for that target fails `post-target` and `final`, including the divergent same-name case where both object-preservation paths are required.
    - Test 6: `final` passes only when live-fixture enumeration derives both complete `refs/heads/ci/198-*` namespaces as empty, every initial subject joins to local/origin peeled tags and one register row, all non-null associated PRs are closed, controls remain stable, and GREEN-07 remains Pending; leaving an unremembered extra ref in either live namespace fails.
    - Test 7: the canonical round-10 exact-three inventory and Markdown continue to pass their existing inventory contract unchanged after round-11 dispatch is added.
  </behavior>
  <action>Start RED by constructing temporary round-11 inventory, Markdown decision, live-state, and archive-register fixtures through the existing ExUnit temp-file helpers. The canonical positive fixture must model the observed topology without copying only remembered target names: three local refs, six remote refs, a divergent same-name local/remote branch as two subjects, remote-only subjects, and a null pull-request association. Extend `bin/verify-phase198-ref-disposition` behind an explicit round/schema discriminator: preserve schema-v1 round-10 exact-three behavior, and implement the complete round-11 `inventory`, `decision`, `authority`, `controls`, `post-target`, and `final` stages on the existing CLI. Round-11 inventory derives one subject per side-plus-full-SHA, validates ancestry/unique-commit/diffstat/archive/restore evidence, binds Markdown and canonical JSON digests, rejects an empty initial universe, and compares complete live local/remote sets. Decision accepts only digest-bound verbatim `retire`, `preserve ...`, or `abort` over the exact subject array. Authority re-runs live subject and stable-control equality and grants only exact-target `retire`. Controls validates immutable origin/main, PR #34, effective rules plus classic protection, required contexts, worktrees, and task-local active branch/head/upstream identities while permitting only the receipt-declared target transition. Post-target requires, for every distinct target object, ordered local annotated-tag, exact single-tag push/remote peeled-object equality, and D-31 register-row receipts before any optional PR-close and exact remote/local branch-delete receipts; a null PR requires no close receipt, and all handles for that target must be absent from the stage-specific live fixture. Final derives both current local and remote `ci/198-*` sets from the live adapter and requires them empty, proves every initial subject against local/origin peeled tags plus its register row, requires all non-null associated PRs closed, rechecks controls, and refuses any GREEN-07 promotion. Keep fixture injection explicit and data-only so it exercises the same validation functions as production but cannot authorize live mutation. After all positive and negative stage tests pass, expand the tracked round-11 evidence to every currently observed ref/subject, snapshot the stable controls, leave decision/execution null and receipts empty, then run the live `inventory` stage only; Plan 53 must not run a live mutation-oriented stage.</action>
  <reversibility rating="reversible">The complete inventory is read-only evidence; a later observation can supersede it without erasing this snapshot.</reversibility>
  <verify>
    <automated>mix test test/threadline/phase198_ref_disposition_contract_test.exs &amp;&amp; bin/verify-phase198-ref-disposition inventory --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md</automated>
    <fails_when>The focused suite lacks a passing round-11 retire lifecycle through all six stages; the complete local or remote inventory differs from live enumeration; divergent tips collapse; a nullable PR is rejected or fabricated; any distinct tip lacks a preservation packet; stable-control drift passes; a retirement receipt can precede complete tag/register preservation; final uses a remembered-name list or accepts a non-empty live-derived namespace; fixture mode bypasses production validation functions; a mutation-oriented stage passes while decision is null; the live command invokes any stage other than inventory; or round-10 compatibility fails.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every live local and remote Phase-198 CI ref is included exactly once in its namespace and every distinct object has exactly one preservation subject.
    - The decision packet offers evidence without recording or implying a choice.
    - One fixture-only round-11 `retire` lifecycle passes decision, per-target authority, controls, post-target, and final before Plan 54 or Plan 55 consumes those stages.
    - Divergent same-name tips, nullable PRs, stable-control drift, preserve-before-delete receipt ordering, and an unremembered live ref each have an explicit discriminating fixture.
    - Final emptiness is derived from the complete live-fixture namespaces, every initial subject remains archive/register joined, and the existing round-10 inventory remains green.
  </acceptance_criteria>
  <done>The complete current namespace is decision-ready and exact-SHA joined, and every round-11 stage needed by Plans 54-55 has already passed a positive valid-retire fixture plus its fail-closed mutation cases without live mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git/GitHub live state -> tracked inventory | Mutable remote refs and PR metadata cross into durable evidence. |
| Markdown decision packet -> JSON authority | Human-readable subjects must join exactly to machine-enforced identities. |
| Fixture/live modes -> validator verdict | Test injection must not weaken production live comparisons. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-53-01 | Spoofing | decision authority | high | mitigate | Inventory stage requires null decision/false authority; later authority requires a digest-bound verbatim selection over every exact subject. |
| T-198-53-02 | Tampering | namespace inventory | high | mitigate | Compare complete live sets, distinct side/SHA subjects, canonical digest, and Markdown/JSON joins; mutation fixtures remove/swap/add each identity. |
| T-198-53-03 | Repudiation | Plan-52 status | medium | mitigate | Append an explicit supersession record naming Plan-51 abort and scope mismatch while preserving both prior artifacts. |
| T-198-53-04 | Information Disclosure | evidence records | low | mitigate | Persist ref/PR/control metadata only; exclude credentials, environment values, headers, and command authentication data. |
| T-198-53-05 | Denial of Service | live GitHub reads | low | accept | Read-only bounded calls may fail and halt inventory validation; no availability guarantee is claimed. |
| T-198-53-06 | Elevation of Privilege | fixture injection | high | mitigate | Fixture mode is explicit and cannot satisfy live authority; production authority re-reads Git and GitHub. |
</threat_model>

<verification>
- Focused ExUnit contract passes with round-10 compatibility and round-11 mutation fixtures.
- Live inventory validation proves exact local/remote set equality, stable controls, null decision, null execution, and zero receipts.
- `git diff --check` passes and the diff contains no external mutation record.
</verification>

<success_criteria>
- Plan 52 remains on disk but is explicitly superseded and cannot execute.
- All six currently observed remote refs, all local refs, and all distinct full-SHA objects are accounted for without inferred authority.
- The resulting packet can be consumed by Plan 54 without another scope guess.
</success_criteria>

<output>
Create `.planning/phases/198-green-bringup/198-53-SUMMARY.md` when done.
</output>
