---
phase: 198-green-bringup
plan: 48
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audits/198-summary-coverage-manifest.json
  - .planning/audits/198-plan46-coverage-delta.json
  - test/threadline/phase198_zero_human_uat_contract_test.exs
  - test/threadline/phase198_nyquist_contract_test.exs
  - test/threadline/ci_topology_contract_test.exs
  - .github/workflows/ci.yml
  - examples/threadline_phoenix/e2e/run-e2e.sh
  - test/threadline/e2e_preflight_contract_test.exs
autonomous: true
gap_closure: true
requirements: [GREEN-04, GREEN-06]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The required `mix test` lane proves an immutable 01-47 Phase-198 baseline, independently validates each repository-owned 48-52 closeout summary, and has an exact 01-52 final-state mode without reading a developer-global GSD installation; the general project-owned classifier remains Phase 199's responsibility."
    - "An executable exact-15-entry allowlist compares pre-Plan-46 and post-Plan-46 coverage states and rejects every changed entry outside that allowlist."
    - "The hosted required-test checkout contains every annotated archive tag object and history needed by the GREEN-12 Nyquist contract, including an empty/missing-tag failure path."
    - "The browser mount preflight accepts only a relative local login target or an absolute login target whose scheme, host, and effective port equal BASE_URL."
    - "A 2xx `/audit` response still requires the operator shell and a rejected redirect fails before Playwright starts."
    - {statement: "Clean-runner portability is verified on the same checkout and command shape used by the required test job.", verification: backstop}
  artifacts:
    - ".planning/audits/198-summary-coverage-manifest.json — exact immutable 01-47 Phase-198 baseline plus the allowed 48-52 closeout namespace and final-state contract"
    - ".planning/audits/198-plan46-coverage-delta.json — pinned pre/post trees and exact 15-entry Plan-46 change allowlist"
    - "test/threadline/phase198_zero_human_uat_contract_test.exs — repository-only manifest verifier with no PATH, home-directory, or global-runtime fallback"
    - "test/threadline/ci_topology_contract_test.exs — checkout history/tag and required-lane portability guard"
    - "test/threadline/e2e_preflight_contract_test.exs — executable redirect-origin cases rather than substring-only source assertions"
  key_links:
    - "immutable summaries 01-47 -> exact committed baseline; summaries 48-52 -> repository-owned validation; final mode -> exact 01-52 set"
    - "pinned pre-Plan-46 tree + post-Plan-46 baseline -> exact 15-entry allowlist -> unchanged-entry rejection"
    - "actions/checkout configuration -> annotated archive tags -> phase198_nyquist_contract_test"
    - "BASE_URL origin -> Location parser -> local login target verdict -> browser launch"
  prohibitions:
    - {statement: "The Phase-198 compatibility backstop must not become the general reusable coverage classifier assigned to Phase 199.", status: unverified, flagged: true}
    - {statement: "The preflight must not follow redirects or accept a login-looking URL on another origin.", status: unverified, flagged: true}
---

<objective>
Restore clean-runner portability and close the cross-origin browser-preflight hole without absorbing Phase 199's classifier scope.

Purpose: GREEN-04 and GREEN-06 must fail or pass from repository-owned inputs available in a default hosted job, not from a maintainer workstation's home directory or git object cache.

Output: an exact Phase-198 coverage manifest backstop, full-history/tag checkout enforcement, and a same-origin login redirect validator with behavioral fixtures.

<assumption_delta_decision>Primary noun: Phase-198 evidence compatibility. Decision: no-change — this phase freezes and verifies its already-classified evidence set; Phase 199 still owns the generalized project classifier.</assumption_delta_decision>

### Artifacts this phase produces

- New paths: `.planning/audits/198-summary-coverage-manifest.json` and `.planning/audits/198-plan46-coverage-delta.json`.
- New invariant: summaries 01-47 are an immutable exact baseline; any present closeout summary must be one of 48-52 and pass the repository-owned coverage checks; the final-state mode requires exactly 01-52.
- New invariant: the pre/post Plan-46 coverage comparison changes exactly the 15 named `(summary, coverage-id)` entries and no others.
- New helper symbols inside `run-e2e.sh`: URL/origin normalization and login-target validation local to the preflight.
- No new dependency, product API, database schema, UI, or external mutation.

### Spec-less probe accounting

- GREEN-04 and GREEN-06 boundary/adjacency/empty/encoding/ordering/precision rows are lifted into the truths and negative fixtures in this plan.
- GREEN-01, GREEN-05, GREEN-09, and GREEN-10 remain unresolved/unclassified assumptions outside the Round-10 corrective files; their prior evidence must remain unchanged and is not silently treated as new work.
- GREEN-02/03/07/08/11/12 probe rows are covered by existing Phase-198 artifacts or Plans 49-52; no row is dismissed.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-46-SUMMARY.md
@.planning/phases/198-green-bringup/198-47-SUMMARY.md
@.planning/phases/198-green-bringup/COVERAGE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace the developer-global classifier dependency with a Phase-198 exact-manifest backstop</name>
  <files>.planning/audits/198-summary-coverage-manifest.json, .planning/audits/198-plan46-coverage-delta.json, test/threadline/phase198_zero_human_uat_contract_test.exs</files>
  <read_first>
    - test/threadline/phase198_zero_human_uat_contract_test.exs — remove both `System.find_executable("gsd-tools")` and `$HOME/.codex` resolution paths
    - .planning/phases/198-green-bringup/198-UAT.md — authoritative sorted 47-summary, 192-entry, zero-present result set
    - .planning/phases/198-green-bringup/198-46-PLAN.md — exact 15 converted coverage entries and historical-preservation contract
    - .planning/phases/198-green-bringup/198-46-SUMMARY.md — completed Plan-46 boundary used to pin the pre/post tree objects
    - .planning/phases/198-green-bringup/198-47-SUMMARY.md — report-only `coverage: []` final input
    - .planning/phases/198-green-bringup/198-SECURITY.md — T-198-46-01 exact-delta and T-198-46-05 disclosure-boundary controls
  </read_first>
  <behavior>
    - Test 1: a temporary HOME with the project toolchain PATH preserved verifies the exact immutable 01-47 baseline without any global GSD lookup
    - Test 2: a missing, extra, renamed, or byte-modified 01-47 baseline summary fails; a present 48-52 summary is validated independently and any other Phase-198 summary number fails
    - Test 3: final-state mode rejects every set other than exact summaries 01-52; baseline totals other than 47 summaries, 192 coverage entries, 192 automated passes, zero present, and zero errors fail non-vacuously
    - Test 4: comparing coverage at the pinned pre-Plan-46 tree with the pinned post-Plan-46 baseline yields exactly the 15 literal allowlisted `(summary, coverage-id)` entries and byte-equivalent normalized coverage for every other entry
    - Test 5: generated UAT/manifest fields reject absolute paths, environment values, credential-shaped keys, authorization headers, tokens, cookies, and non-repository evidence refs
  </behavior>
  <action>Generate a committed, stable JSON manifest from the authoritative 01-47 baseline. Record schema version, exact sorted repository-relative baseline paths, the allowed closeout namespace 48-52, each baseline file digest, sorted coverage IDs and verification-state digest, and aggregate baseline counts 47/192/192/0/0. Add a separate delta artifact containing immutable pre-Plan-46 and post-Plan-46 tree identifiers plus the literal exact allowlist `198-01 D4; 198-03 D7; 198-04 D8; 198-05 D2/D5/D7; 198-06 D3/D4; 198-07 D3; 198-12 D5; 198-18 D3; 198-27 D3; 198-29 D3; 198-31 D4; 198-37 D4`. Rewrite the Phase-198 ExUnit contract using only Elixir/Jason/crypto/git already in the project. It must independently discover summaries, require exact 01-47 baseline equality, validate every present 48-52 summary with the same repository-owned structural/automated-evidence rules, reject any other number, and support a final-state mode requiring exact 01-52 after Plan 52 closes. For T-198-46-01, load the pre-tree coverage through `git show`, compare normalized coverage keyed by `(summary, id)` against the post-tree/current baseline, hard-code the same 15-key set in the test, and fail on a missing allowlisted change or any changed key outside it. For T-198-46-05, validate that copied manifest/UAT evidence is repository-relative and excludes secret-shaped fields or values; record this as a mitigation, not inferred acceptance. Remove every executable lookup, home-directory fallback, and invocation of the general GSD classifier. Keep both artifacts explicitly Phase-198-specific because Phase 199 owns generalization.</action>
  <reversibility rating="reversible">The manifest is a frozen compatibility attestation and Phase 199 can replace it with the generalized repository classifier.</reversibility>
  <verify>
    <automated>bash -c 'set -eu; tmp_home=$(mktemp -d); trap '\''rm -rf "$tmp_home"'\'' EXIT; env HOME="$tmp_home" MIX_ENV=test mix test test/threadline/phase198_zero_human_uat_contract_test.exs'</automated>
    <fails_when>The preserved project toolchain PATH cannot resolve Mix, the test reads any global GSD path, baseline discovery differs from exact 01-47, an unexpected closeout summary passes, the Plan-46 delta differs from the exact 15 keys, a non-allowlisted entry changed, disclosure-boundary fixtures pass, or any digest/count drifts.</fails_when>
  </verify>
  <acceptance_criteria>
    - The source contains no `gsd-tools`, `.codex`, `System.user_home!`, or external classifier process call.
    - The baseline manifest represents exactly summaries 01-47 and the established 192/192/0/0 outcome; present summaries 48-52 are checked separately and `PHASE198_SUMMARY_SET=final` requires exactly 01-52.
    - The delta artifact and test carry the same literal 15-entry allowlist, resolve both pinned trees, and prove every non-allowlisted normalized coverage entry unchanged.
    - T-198-46-05 is re-auditable as a mitigation through executable repository-relative and secret-pattern rejection, without maintainer acceptance.
    - Phase 199's general classifier boundary is named in the manifest metadata and not implemented here.
  </acceptance_criteria>
  <done>The default test lane has a repository-only, non-vacuous Phase-198 coverage attestation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make annotated archive-tag history part of the required CI checkout contract</name>
  <files>.github/workflows/ci.yml, test/threadline/ci_topology_contract_test.exs, test/threadline/phase198_nyquist_contract_test.exs</files>
  <read_first>
    - .github/workflows/ci.yml — `verify-test` checkout step and matrix lane setup
    - test/threadline/phase198_nyquist_contract_test.exs — GREEN-12 annotated-tag resolution loop
    - test/threadline/ci_topology_contract_test.exs — established derive-from-workflow job/config assertions
    - .planning/ARCHIVE-REGISTER.md — exact registered refs and annotated-tag contract
  </read_first>
  <behavior>
    - Test 1: verify-test checkout uses `fetch-depth: 0` and does not disable tags
    - Test 2: every register row resolves to an annotated tag in a fresh full-history checkout
    - Test 3: empty register rows, missing tag objects, lightweight tags, or SHA mismatch fail with the exact ref
  </behavior>
  <action>Set `fetch-depth: 0` on the checkout used by every `verify-test` matrix leg so annotated archive tag objects and their reachable history exist before `mix verify.test`. Add a topology contract that derives the required job's checkout configuration from `ci.yml` and rejects missing/positive shallow depth or `fetch-tags: false`. Strengthen the Nyquist archive test to report missing-object and lightweight-tag failures without crashing on pattern-match, while preserving exact SHA equality. Do not add a network fetch inside ExUnit; checkout preparation belongs to CI configuration.</action>
  <reversibility rating="reversible">Checkout depth is a local CI cost/configuration choice and can later be replaced by an explicit bounded tag fetch if measured.</reversibility>
  <verify>
    <automated>mix test test/threadline/ci_topology_contract_test.exs test/threadline/phase198_nyquist_contract_test.exs</automated>
    <fails_when>The workflow remains shallow, disables tag fetch, the tests fetch the network themselves, an absent/lightweight tag raises without a ref-specific assertion, or any registered tag resolves to the wrong SHA.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both min/current required-test legs inherit one full-history checkout definition.
    - The contract detects a one-line regression back to default shallow checkout.
    - No workflow job id, name, `ci-required` membership, or D-39/D-42 gate is changed.
  </acceptance_criteria>
  <done>The clean hosted checkout contains and verifies the annotated archive evidence expected by `mix test`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate login redirects against BASE_URL before browser launch</name>
  <files>examples/threadline_phoenix/e2e/run-e2e.sh, test/threadline/e2e_preflight_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/e2e/run-e2e.sh — current `operator_surface_ready` response split and permissive `*/users/log_in*` case
    - test/threadline/e2e_preflight_contract_test.exs — current source-presence assertions to replace with behavioral cases
    - .planning/phases/198-green-bringup/198-CONTEXT.md — D-18 fail-fast/no-follow contract
  </read_first>
  <behavior>
    - Test 1: `/users/log_in` and same-origin absolute login URLs pass, including matching default ports
    - Test 2: protocol-relative, foreign host, foreign port, userinfo-confused, backslash, control-character, and suffix/prefix lookalike targets fail
    - Test 3: missing/multiple malformed Location headers and empty BASE_URL fail; 2xx still requires both shell markers
    - Test 4: rejection exits before the Playwright command is invoked
  </behavior>
  <action>Extract the preflight into a sourceable/testable shell mode with curl injection or a pure redirect-target helper. Parse BASE_URL and Location without following redirects. Accept a path only when its decoded/normalized path is exactly `/users/log_in` with optional query/fragment, or when an absolute `http(s)` URL has the same normalized scheme, hostname, and effective port as BASE_URL and that exact path. Reject ambiguous headers and URL forms rather than substring matching. Replace the static substring test with table-driven process tests that feed synthetic headers/status/body and use a sentinel Playwright command to prove rejected preflight state aborts before suite launch. Preserve D-18 log-tail diagnostics.</action>
  <reversibility rating="reversible">The stricter parser is isolated to the test runner preflight.</reversibility>
  <verify>
    <automated>mix test test/threadline/e2e_preflight_contract_test.exs &amp;&amp; bash -n examples/threadline_phoenix/e2e/run-e2e.sh</automated>
    <fails_when>Any cross-origin or ambiguous target passes, a valid relative/same-origin login redirect fails, 2xx shell checks disappear, redirects are followed, or Playwright runs after a rejected preflight.</fails_when>
  </verify>
  <acceptance_criteria>
    - The previous cross-origin counterexample is a named red-then-green fixture.
    - URL comparison includes scheme, host, and effective port and rejects parser ambiguity.
    - Failure remains one bounded preflight error with boot-log tail, not per-test browser timeouts.
  </acceptance_criteria>
  <done>GREEN-06's early-abort mount proof cannot be spoofed by an unrelated login-looking redirect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tracked summaries -> required ExUnit lane | Frozen evidence must be verified without executing workstation-global tools. |
| GitHub checkout -> git-object assertions | Shallow clones omit annotated tag objects that local clones silently provide. |
| HTTP Location -> browser readiness | An untrusted redirect target is interpreted as proof that the local operator mount exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-48-01 | Spoofing | coverage manifest | high | mitigate | Exact 01-47 baseline discovery, independently checked 48-52 closeout namespace, exact 01-52 final mode, fixed non-vacuous totals, and isolated-HOME test. |
| T-198-46-01 | Tampering | Plan-46 coverage metadata | high | mitigate | Pinned pre/post trees, literal exact-15 allowlist in artifact and test, normalized keyed diff, and rejection of every outside change. |
| T-198-46-05 | Information disclosure | generated UAT and manifests | low | mitigate | Repository-relative field allowlist plus credential/header/token/cookie rejection fixtures; no risk acceptance inferred. |
| T-198-48-02 | Tampering | required checkout | high | mitigate | Full-history checkout plus derive-from-YAML contract and exact annotated-tag/SHA assertions. |
| T-198-48-03 | Spoofing | preflight redirect | high | mitigate | Normalize and compare scheme/host/effective-port/path; reject ambiguous encodings and multiple headers. |
| T-198-48-04 | Denial of service | browser startup | medium | mitigate | Preserve bounded curl and fail-before-Playwright behavior with sentinel test. |
| T-198-48-05 | Information disclosure | manifest | low | mitigate | Store repository paths and digests only; no environment, auth, or runtime-home data. |
</threat_model>

<verification>
- Run all three focused ExUnit commands and `git diff --check`.
- After `198-52-SUMMARY.md` exists, run `PHASE198_SUMMARY_SET=final mix test test/threadline/phase198_zero_human_uat_contract_test.exs`. Fails when the final discovered set is not exactly summaries 01-52 or any closeout summary violates the repository-owned coverage contract.
- Confirm `git grep -n 'gsd-tools\|System.user_home!\|\.codex' -- test/threadline/phase198_zero_human_uat_contract_test.exs` returns no matches.
- Confirm no files under `lib/`, no general coverage-classifier command, no scorecard, and no screenshot changed.
</verification>

<success_criteria>
- GREEN-04's required test lane runs in a clean checkout without developer-global GSD state and with the archive objects its tests require.
- GREEN-06's preflight rejects the Round-10 cross-origin counterexample before launching Playwright.
- Phase 199 retains ownership of the generalized repository classifier.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-48-SUMMARY.md` when done.</output>
