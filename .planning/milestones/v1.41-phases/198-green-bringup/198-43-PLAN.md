---
phase: 198-green-bringup
plan: 43
type: execute
wave: 1
gap_closure: true
gap_ids: [G-198-4, G-198-5, G-198-7, G-198-8]
depends_on: []
files_modified:
  - bin/upsert-ci-issue
  - bin/verify-playwright-fail-fast
  - .github/workflows/browser-full.yml
  - .github/workflows/flake-detection.yml
  - test/threadline/ci_issue_upsert_contract_test.exs
  - test/threadline/playwright_fail_fast_contract_test.exs
  - test/threadline/release_control_plane_contract_test.exs
autonomous: true
requirements: [GREEN-06, GREEN-07, GREEN-10, GREEN-11]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 3, confidence: medium}
must_haves:
  truths:
    - "Browser-full and flake-detection use one fixture-testable create-or-update issue boundary; a second report updates the original issue instead of creating another."
    - "A hermetic browser-only Playwright smoke run contains more than five deterministic failures, stops at exactly the configured CI cap, and leaves trace evidence for the attempted failures."
    - "Release verification proves from source that the only Hex publish command is in the production-hex job, that the job depends on the CI-green gate, and that authentication remains one replaceable step."
    - "No test publishes a package, mutates a GitHub Environment, creates a live issue, or waits for a nightly dispatch."
  artifacts:
    - "bin/upsert-ci-issue — shared issue create-or-update adapter with injectable gh boundary"
    - "bin/verify-playwright-fail-fast — hermetic behavioral smoke for maxFailures and trace retention"
    - "test/threadline/ci_issue_upsert_contract_test.exs — create, update, empty, malformed, and injection fixtures"
    - "test/threadline/release_control_plane_contract_test.exs — side-effect-free release gate and auth-boundary proof"
  key_links:
    - ".github/workflows/browser-full.yml and flake-detection.yml -> bin/upsert-ci-issue -> fake-gh integration tests"
    - "playwright.config.ts CI cap -> synthetic failing project -> machine-checked failure/trace counts"
    - "release.yml publish-hex job -> production-hex environment + gate-ci-green needs + single auth step"
  prohibitions:
    - {statement: "Do not make browser-full or flake-detection required checks.", status: unverified, flagged: true}
    - {statement: "Do not exercise release verification by publishing or weakening the required-reviewer environment.", status: unverified, flagged: true}
---

<objective>
Move the four event-dependent CI controls left into hermetic integration and smoke tests.

Purpose: nightly timing, two live failures, and an actual package release must not be prerequisites for knowing that fail-fast, issue deduplication, and release gating work.

Output: shared CI issue automation, a real Playwright fail-fast smoke, and a side-effect-free release control-plane contract.

**Assumption delta:** `decision: no-change`. Existing job ids, trigger topology, required-check membership, production environment, and publish semantics remain unchanged; only their verification seam changes from human observation to executable evidence.

### Artifacts this phase produces

- New symbols: `bin/upsert-ci-issue`, `bin/verify-playwright-fail-fast`.
- New flags: `GH_BIN` test adapter for the issue helper; no product/runtime flag.
- New files: three contract-test modules/scripts listed in `must_haves.artifacts`.
- Existing artifacts amended: browser-full and flake-detection workflows delegate their duplicate issue logic.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-UAT.md
@.planning/phases/198-green-bringup/198-05-SUMMARY.md
@.planning/phases/198-green-bringup/198-06-SUMMARY.md
@.github/workflows/browser-full.yml
@.github/workflows/flake-detection.yml
@.github/workflows/release.yml
@examples/threadline_phoenix/e2e/playwright.config.ts
@test/threadline/flake_classifier_contract_test.exs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extract and prove one idempotent CI issue-upsert path</name>
  <files>bin/upsert-ci-issue, .github/workflows/browser-full.yml, .github/workflows/flake-detection.yml, test/threadline/ci_issue_upsert_contract_test.exs</files>
  <read_first>
    - .github/workflows/browser-full.yml — first inline issue create/update implementation and trigger/label invariants
    - .github/workflows/flake-detection.yml — second inline implementation and classifier outputs
    - test/threadline/flake_classifier_contract_test.exs — fake-process integration-test idiom and reachability assertions
  </read_first>
  <action>Create executable `bin/upsert-ci-issue` accepting a stable marker, title, body-file, and label. Use `GH_BIN` when set and default to `gh`; pass body by file rather than shell interpolation. Query open issues for the exact marker, fail closed on malformed/ambiguous JSON or more than one match, create when zero match, and comment on the one existing issue when one match. Emit `action=create|update` and `issue_number` as stable output. Replace both workflows' inline scripts with this helper while preserving distinct labels, `if:` reachability, permissions, triggers, job ids, and non-required topology.

Build ExUnit fixtures around a fake `gh` executable for zero→create, one→comment, two→fail, API failure, marker metacharacters, multiline body, and a sequential create-then-update replay. Assert the second replay targets the first issue and performs no second create.</action>
  <reversibility rating="reversible">The helper and workflow delegation can be reverted; tests use only fake executables and temporary files.</reversibility>
  <verify>
    <automated>mix test test/threadline/ci_issue_upsert_contract_test.exs test/threadline/flake_classifier_contract_test.exs</automated>
    <fails_when>Any fixture makes a live network call, duplicate matches are accepted, the second replay creates another issue, either workflow retains an inline `gh issue create`, or existing classifier reachability regresses.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both workflows invoke the same executable helper and retain their original label/marker distinction.
    - Create-then-update behavior is proven with process-level fixtures, including ambiguous and hostile inputs.
    - No live issue or workflow dispatch is needed by the test suite.
  </acceptance_criteria>
  <done>Issue deduplication is an executable integration contract rather than an unobserved nightly side effect.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise Playwright's CI failure cap and trace retention hermetically</name>
  <files>bin/verify-playwright-fail-fast, test/threadline/playwright_fail_fast_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/e2e/playwright.config.ts — production maxFailures and trace policy to import rather than duplicate
    - examples/threadline_phoenix/e2e/package.json — installed Playwright entrypoint
    - .github/workflows/browser-full.yml — boot-log and artifact-retention contract
  </read_first>
  <action>Create a smoke script that builds a temporary Playwright config by importing the repository's default config and overriding only `testDir`, reporter, output directory, workers, retries, and a single Chromium project. Generate seven deterministic failing tests that open a browser page with `page.setContent` so Playwright produces real traces without using `baseURL` or starting Phoenix. Run with `CI=1` and assert non-zero exit, exactly five final failed tests, at least two tests not run/interrupted because of the cap, and retained trace archives for the attempted failures. Keep every fixture under a trapped `mktemp -d`; install nothing and never start the example app. Add an ExUnit contract that invokes the script and also mutates a copied config to prove the verifier goes red when the cap or trace policy is removed.</action>
  <reversibility rating="reversible">Only a local smoke script and test are added; all generated runs live in a trapped temporary directory.</reversibility>
  <verify>
    <automated>mix test test/threadline/playwright_fail_fast_contract_test.exs</automated>
    <fails_when>The smoke executes fewer than six available cases, does not stop at five, passes unexpectedly, leaves no traces, needs the Phoenix server, or its negative controls stay green.</fails_when>
  </verify>
  <acceptance_criteria>
    - The smoke proves behavior, not just the presence of `maxFailures` text.
    - The default E2E test inventory is unchanged.
    - Temporary output is removed on success, failure, and interruption.
  </acceptance_criteria>
  <done>A broken browser suite's fail-fast and forensic-retention behavior is reproducible locally and in CI.</done>
</task>

<task type="auto">
  <name>Task 3: Turn release approval/auth shape into a side-effect-free control-plane test</name>
  <files>test/threadline/release_control_plane_contract_test.exs</files>
  <read_first>
    - .github/workflows/release.yml — authoritative job graph, environment, auth step, and only publish command
    - test/threadline/release_ci_gate_contract_test.exs — existing newest-main-run selection contract
    - test/threadline/ci_topology_contract_test.exs — existing single-publisher resurrection guard
    - .github/rulesets/main.json — source-controlled protection idiom; read only
  </read_first>
  <action>Add a source-derived contract that isolates `publish-hex`, asserts `environment: production-hex`, hard `needs:` on the CI-green gate and build/shape gates, exactly one `mix hex.publish` in all workflows, and one contiguous auth step whose secret/input names can be swapped without changing publish/gate steps. Assert no `continue-on-error`, conditional bypass, alternate publisher, or workflow-dispatch input can skip the environment boundary. Reuse the existing live environment snapshot/API evidence as historical configuration evidence; do not add a release dispatch, dry-run publish, environment mutation, or fake approval.</action>
  <reversibility rating="reversible">This adds an offline contract only.</reversibility>
  <verify>
    <automated>mix test test/threadline/release_control_plane_contract_test.exs test/threadline/release_ci_gate_contract_test.exs test/threadline/ci_topology_contract_test.exs</automated>
    <fails_when>The publish command is reachable outside the gated job, the environment/needs/auth assertions are vacuous, a bypass condition exists, or any command attempts publication or GitHub mutation.</fails_when>
  </verify>
  <acceptance_criteria>
    - The irreversible path is proven structurally without invoking it.
    - Required-reviewer operation remains a release control, not a UAT verification step.
    - Negative assertions catch a second publisher and a removed environment boundary.
  </acceptance_criteria>
  <done>Release gating is continuously verified with zero human UAT and zero publication risk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workflow data -> shell helper | Titles, markers, bodies, and issue numbers cross from CI into a command process. |
| test fixtures -> external CLIs | Hermetic tests must never fall through to real `gh` or remote state. |
| release workflow -> Hex | A workflow-shape mistake could bypass the environment before an irreversible publish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-43-01 | Tampering | issue marker/body | high | mitigate | Exact marker match, body files, quoted argv, and hostile-input fixtures. |
| T-198-43-02 | Spoofing | fake/live gh boundary | high | mitigate | Explicit `GH_BIN`; tests fail if the fake executable is not called. |
| T-198-43-03 | Denial of service | duplicate issues | medium | mitigate | Fail on more than one match and prove create-then-update idempotency. |
| T-198-43-04 | Elevation of privilege | publish path | critical | mitigate | Offline source contract; no dispatch, publish, environment edit, or bypass. |
| T-198-43-05 | Repudiation | fail-fast evidence | medium | mitigate | Count machine reporter events and trace archives; include negative controls. |
</threat_model>

<verification>
- Run the three task-level suites, then `mix test test/threadline/ci_*contract_test.exs test/threadline/release_*contract_test.exs`.
- Confirm `git diff -- .github/workflows` changes only the two issue-upsert bodies and retains triggers, job ids, permissions, timeouts, and aggregate membership.
</verification>

<success_criteria>
- G-198-4, G-198-5, G-198-7, and G-198-8 have process-level automated evidence.
- No check depends on a human-observed scheduled run, a live failure, or a package release.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-43-SUMMARY.md` when done.</output>
