---
phase: 198-green-bringup
plan: 45
type: execute
wave: 1
gap_closure: true
gap_ids: [G-198-12]
depends_on: []
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts
  - lib/threadline/operator_surface/ui.ex
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/live/row_history_component.ex
  - .planning/audits/198-row-history-focus-regression.md
autonomous: true
requirements: [GREEN-07]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: medium}
must_haves:
  truths:
    - "The mobile row-history drawer date input is visible, keyboard-focusable, non-obscured, and causes no horizontal overflow in repeated focused E2E runs."
    - "The fix changes the product or synchronization at the established root cause; it does not weaken/remove accessibility assertions, add sleeps, retries, force clicks, or broaden tolerances."
    - "A focused red control demonstrates the regression test fails when the diagnosed fix is absent, and the same test passes repeatedly with the fix."
  artifacts:
    - "198-row-history-focus-regression.md — symptom, trace/screenshot/geometry observations, root cause, red control, and repeated green evidence"
    - "operator-accessibility.spec.ts — focused regression remains in the normal mobile project"
  key_links:
    - "row-history route -> drawer dialog -> snapshot field -> focus/non-obscured/overflow assertions"
    - "measured browser geometry or readiness race -> minimal product/synchronization fix -> repeated mobile E2E"
  prohibitions:
    - {statement: "Do not delete or soften `expectNonObscuredFocused`, visibility, aria-modal, labelledby, or overflow assertions.", status: unverified, flagged: true}
    - {statement: "Do not use waitForTimeout, retries, force, or screenshot-baseline regeneration as the fix.", status: unverified, flagged: true}
---

<objective>
Diagnose and retire the one unresolved operator-accessibility row-history failure with a focused E2E regression.

Purpose: this is the sole gap whose product/test root cause is genuinely unknown; it must become a normal automated browser contract rather than a human deferral judgment.

Output: root-cause evidence, minimal fix, and repeated desktop/mobile regression proof.

**Assumption delta:** `decision: no-change`. The drawer remains a modal dialog, the date field remains keyboard accessible, and existing visual/accessibility acceptance criteria stay authoritative.

### Artifacts this phase produces

- New symbols/flags: None expected.
- Existing code amended only at the demonstrated root cause; untouched candidate files must remain out of the final diff.
- New evidence artifact: `198-row-history-focus-regression.md`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-UAT.md
@.planning/phases/198-green-bringup/198-27-SUMMARY.md
@examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts
@lib/threadline/operator_surface/ui.ex
@lib/threadline/operator_surface/style.ex
@lib/threadline/operator_surface/live/row_history_component.ex
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reproduce the mobile focus failure and establish one falsifiable root cause</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts, .planning/audits/198-row-history-focus-regression.md</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts — failure near the snapshot focus assertion and shared non-obscured helper
    - examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts — independent row-history mobile geometry expectations
    - lib/threadline/operator_surface/ui.ex — drawer DOM/focus behavior
    - lib/threadline/operator_surface/style.ex — drawer viewport, overflow, sticky, and motion rules
    - lib/threadline/operator_surface/live/row_history_component.ex — labelled snapshot field and drawer lifecycle
  </read_first>
  <action>Run only the named row-history accessibility test on `mobile-chromium` with trace/video/screenshot on first retry disabled as a masking device, repeating enough times to expose the reported flake. Record viewport, activeElement, input/dialog bounding boxes, scroll offsets, animation state, drawer readiness, and the first failing assertion. Form one root-cause hypothesis at a time and falsify it with a temporary diagnostic assertion or CSS/DOM toggle. Commit no speculative product change. Preserve a focused regression row in the spec that asserts semantic dialog attributes, field visibility, keyboard focus, non-obscuration, and zero horizontal overflow.</action>
  <reversibility rating="reversible">Diagnostics and test instrumentation are local; speculative changes are not committed.</reversibility>
  <verify>
    <automated>mix verify.example_browser operator-accessibility.spec.ts --project=mobile-chromium --grep "row history"</automated>
    <fails_when>The symptom cannot be tied to recorded DOM/geometry/readiness evidence, multiple hypotheses remain indistinguishable, or reproduction requires broad-suite noise.</fails_when>
  </verify>
  <acceptance_criteria>
    - The audit names one established cause and cites concrete trace/geometry evidence.
    - A red control fails at the original assertion without weakening it.
    - The focused command is documented and repeatable.
  </acceptance_criteria>
  <done>The former cause-in-progress note is replaced by a falsifiable, evidenced diagnosis.</done>
</task>

<task type="auto">
  <name>Task 2: Fix at cause and prove the row-history interaction repeatedly</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts, examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts, lib/threadline/operator_surface/ui.ex, lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/live/row_history_component.ex, .planning/audits/198-row-history-focus-regression.md</files>
  <read_first>
    - .planning/audits/198-row-history-focus-regression.md — Task 1 root cause and red control
    - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts — exact regression row
    - the single product file implicated by Task 1 — only that candidate may receive a behavior change
  </read_first>
  <action>Apply the smallest change at the established cause. If the cause is readiness/synchronization, wait on a semantic state the product exposes; if it is DOM or CSS, repair the component/style and leave test thresholds intact. Remove diagnostic-only logging. Run the focused mobile test 10 consecutive times, its desktop counterpart once, and the responsive row-history test on both projects. Record commands, attempt counts, and outcomes in the audit. Demonstrate teeth by temporarily reverting only the causal line in a disposable diff/worktree, capturing the expected focused failure, then restore and re-run green. Do not update screenshots unless the causal fix intentionally changes the approved visual contract and an existing screenshot test itself proves that change is required.</action>
  <reversibility rating="reversible">The minimal source/test fix is locally revertible; red-control manipulation occurs only in disposable state.</reversibility>
  <verify>
    <automated>bash -c 'set -eu; i=1; while [ "$i" -le 10 ]; do mix verify.example_browser operator-accessibility.spec.ts --project=mobile-chromium --grep "row history"; i=$((i+1)); done; mix verify.example_browser operator-accessibility.spec.ts operator-responsive-mobile-first.spec.ts --project=desktop-chromium --project=mobile-chromium --grep "row history"'</automated>
    <fails_when>Any repeat fails, desktop/regression coverage regresses, the diff contains assertion weakening or forbidden waits/retries/force, or the red control does not fail.</fails_when>
  </verify>
  <acceptance_criteria>
    - Focused mobile passes 10/10 and the cross-project row-history set passes.
    - The final diff touches only the diagnosed source plus retained regression/evidence files.
    - The audit includes both red-control and green-repeat evidence.
  </acceptance_criteria>
  <done>The unknown E2E discovery is closed by an ordinary automated regression, with no human judgment remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser timing -> accessibility verdict | Animation/readiness races can look like semantic or layout defects. |
| test change -> product acceptance | A weakened locator/assertion can create a false green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-45-01 | Repudiation | flaky reproduction | high | mitigate | Record traces, geometry, active element, commands, and repeat counts. |
| T-198-45-02 | Tampering | assertions | high | mitigate | Explicit prohibition scan plus red-control proof. |
| T-198-45-03 | Denial of service | animation/readiness | medium | mitigate | Synchronize on semantic state; forbid sleeps/retry inflation. |
| T-198-45-04 | Spoofing | wrong viewport/project | medium | mitigate | Pin mobile-chromium and run desktop as an adjacency check. |
</threat_model>

<verification>
- Execute the Task 2 repeated command and `git diff --check`.
- Search the final diff for removed accessibility assertions, `waitForTimeout`, `force: true`, and retry changes; all must be absent.
</verification>

<success_criteria>
- G-198-12 has an established cause, red control, minimal fix, and repeatable green E2E evidence.
- No user or maintainer inspection is part of acceptance.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-45-SUMMARY.md` when done.</output>
