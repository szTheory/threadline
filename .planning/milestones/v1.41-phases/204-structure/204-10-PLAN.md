---
phase: 204-structure
plan: 10
type: execute
wave: 10
depends_on: ["204-09"]
files_modified:
  - lib/threadline/operator_surface/live/retention_history_live.ex
  - lib/threadline/operator_surface/live/export_status_live.ex
  - lib/threadline/operator_surface/live/export_status_live/components.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
  - test/threadline/release_artifact_contract_test.exs
  - test/threadline/source_size_contract_test.exs
  - test/threadline/credo_config_contract_test.exs
autonomous: true
requirements: [STRUCT-03, STRUCT-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`render/1` in retention_history_live, export_status_live, and transaction_live is at most 120 lines, carved into private `defp` function components in the same LiveView called as `<.name …/>` with `attr` declarations (D-08, D-14)."
    - "A sibling `@moduledoc false` module is used only when the LiveView would otherwise exceed 800 lines (expected: `Live.ExportStatusLive.Components`, because export_status_live.ex starts at 796); any such sibling joins `@source_owners` in release_artifact_contract_test (D-08)."
    - "The register site in export_status_live.ex is drained, with the credo register lowered in the same commit (D-13)."
    - "The size gate carries no function exception for these three LiveViews after this plan; 204-11 carves the other three."
    - "Rendered output is unchanged at every commit: byte lock, rendered_output_contract, copy_contract, each LiveView's tests, and the browser lane at exactly the 8 known failures (D-00b)."
  artifacts:
    - lib/threadline/operator_surface/live/retention_history_live.ex
    - lib/threadline/operator_surface/live/export_status_live.ex
    - lib/threadline/operator_surface/live/transaction_live.ex
  key_links:
    - "component_contract_test's per-page shell and reconnect scans read each page through `Threadline.Test.SourceFamily.read!/1` (204-08), so an `export_status_live/components.ex` sibling stays inside the `class=\"threadline-ui\"` / `id=\"tl-main\"` refutations."
    - "release_artifact_contract_test `@source_owners.source_vocab_operator_live_forms` → the export-status sibling if created, so the archive vocabulary scan covers its copy."
  prohibitions:
    - statement: "No operator-facing copy, heading, class, or attribute changes while carving render bodies."
      status: resolved
      verification: test
---

<objective>
Carve the retention-history, export-status, and transaction `render/1` functions into in-module function components and drain the export-status register site (STRUCT-03, STRUCT-07; D-08, D-13, D-14). 204-11 does the same for actor, start, and coverage.

Purpose: after 204-03..09 no file is over 800 lines; what remains of STRUCT-03 is function length in the page LiveViews. In-module `defp` components keep the markup next to its page while making each piece reviewable. Split across two plans to stay inside the per-plan context budget.

Output: three LiveViews with no function over 120 lines, possibly one component sibling for export status, and three fewer function exceptions in the size gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-PATTERNS.md
@.planning/phases/204-structure/204-08-SUMMARY.md
@CLAUDE.md
</context>

## Zero-output-change proof (ZOC) — after EVERY commit (D-00b)

1. `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors`
2. `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live test/threadline/operator_surface/transaction_live_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/coverage_doc_contract_test.exs test/threadline/operator_surface/ui_form_policy_contract_test.exs test/threadline/operator_surface/style_byte_lock_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs`
3. Browser lane: `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-10-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-10-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-10-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-10-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"`
4. `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict <touched LiveView>`

Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-10-base"`. Before each commit: `grep -rn '<liveview>.ex' test examples/threadline_phoenix/test` and repoint pins whose text moved.

**HALT clause:** any ZOC failure means output changed: revert and retry. Never edit a pinned string or contract assertion, never regenerate baselines, never add a size exception, never use `embed_templates`/`.heex`/`defdelegate`, never bypass hooks, never `git add .planning/`.

Carving recipe (D-08): pick cohesive regions of the template (header, each card/section, table, empty state); move each verbatim into a `defp name(assigns)` with `attr(...)` declarations for every assign it reads and `~H` holding the moved markup; call it as `<.name a={@a} …/>`. Assigns computed in `render/1` before the template move into the component that uses them or stay as passed attrs. One LiveView per commit: `refactor(204-10): carve <page> render into function components`.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — carve retention_history_live render/1 end-to-end</name>
  <reversibility rating="reversible">Private function components inside one LiveView; no API change (D-08).</reversibility>
  <files>lib/threadline/operator_surface/live/retention_history_live.ex, test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/live/retention_history_live.ex (`render/1` body only)
    - lib/threadline/operator_surface/live/timeline_live.ex (the carved `render/1` from 204-08 as the in-repo pattern)
  </read_first>
  <action>Per D-08 and D-14, apply the carving recipe to retention_history_live `render/1` until every clause is at most 120 lines; delete its function exception in the same commit; run full ZOC.</action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live/retention_history_live_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/copy_contract_test.exs test/threadline/source_size_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-10-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-10-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-10-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-10-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"</automated>
    <fails_when>non-zero exit: not exactly 82 passed / 8 failed, a failure outside operator-screenshot-regression.spec.ts, or a baseline PNG changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'retention_history_live.ex' test/threadline/source_size_contract_test.exs` prints 0
    - `grep -c '^    attr(' lib/threadline/operator_surface/live/retention_history_live.ex` is at least 1
  </acceptance_criteria>
  <done>One page render carved with identical output, proving the recipe for the rest.</done>
</task>

<task type="auto">
  <name>Task 2: Carve export_status_live and drain its register site</name>
  <files>lib/threadline/operator_surface/live/export_status_live.ex, lib/threadline/operator_surface/live/export_status_live/components.ex, test/threadline/release_artifact_contract_test.exs, test/threadline/source_size_contract_test.exs, test/threadline/credo_config_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/live/export_status_live.ex (`render/1` and the enclosing function of its `Structural debt:` line)
    - test/threadline/release_artifact_contract_test.exs (`@source_owners`)
  </read_first>
  <action>
    Per D-08 and D-13, one commit for the carve and one for the register drain (or one commit if they touch the same function). export_status_live.ex starts at 796 lines: if carving pushes it over 800, create `lib/threadline/operator_surface/live/export_status_live/components.ex` (`Threadline.OperatorSurface.Live.ExportStatusLive.Components`, `@moduledoc false`, public components with `attr`) for the carved components, call them as `<Components.name …/>`, and add the path to `@source_owners.source_vocab_operator_live_forms`. Delete the function exception in the commit that makes it stale; lower the credo register by one for the drained site.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live/export_status_live_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/component_contract_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>test "$(wc -l < lib/threadline/operator_surface/live/export_status_live.ex)" -le 800 && test "$(grep -c 'Structural debt:' lib/threadline/operator_surface/live/export_status_live.ex)" = "0"</automated>
    <fails_when>non-zero exit: export_status_live.ex exceeds 800 lines or its register site remains</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export_status_live.ex' test/threadline/source_size_contract_test.exs` prints 0
    - the ZOC browser chain passed after every commit (outcomes in the SUMMARY)
  </acceptance_criteria>
  <done>Export status render carved and its register site drained with identical output.</done>
</task>

<task type="auto">
  <name>Task 3: Carve transaction_live render/1</name>
  <files>lib/threadline/operator_surface/live/transaction_live.ex, test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/live/transaction_live.ex (`render/1` body only)
  </read_first>
  <action>Per D-08 and D-14, apply the carving recipe to transaction_live `render/1` until every clause is at most 120 lines; delete its function exception in the same commit; run full ZOC.</action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/transaction_live_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/component_contract_test.exs test/threadline/source_size_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-10-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-10-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-10-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-10-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"</automated>
    <fails_when>non-zero exit: not exactly 82 passed / 8 failed, a failure outside operator-screenshot-regression.spec.ts, or a baseline PNG changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'retention_history_live.ex|export_status_live.ex|transaction_live.ex' test/threadline/source_size_contract_test.exs` prints 0
    - `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix verify.dialyzer` exits 0 after the last commit
  </acceptance_criteria>
  <done>Three page renders carved with identical output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser ← page HTML | pages render audit data, actor identities, and export status to authorized operators |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-22 | Information disclosure | transaction_live rendering | medium | mitigate | Markup moves verbatim; scope-filtered assigns are passed unchanged as attrs; LiveView tests with scoped routers stay green |
| T-204-23 | Tampering | export_status_live controls | low | mitigate | Rendered-output and export-status tests pin buttons, links, and download anchors |
</threat_model>

<verification>
- ZOC green after every commit; `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures; `MIX_ENV=dev mix verify.dialyzer` → exit 0.
</verification>

<success_criteria>
No `def`/`defp` clause in retention_history_live, export_status_live, or transaction_live exceeds 120 lines, with byte-identical output.
</success_criteria>

## Artifacts this phase produces (this plan)

- Private function components in retention_history_live, export_status_live, transaction_live (names recorded in the SUMMARY)
- Optional `Threadline.OperatorSurface.Live.ExportStatusLive.Components` (`lib/threadline/operator_surface/live/export_status_live/components.ex`)

<output>
Create `.planning/phases/204-structure/204-10-SUMMARY.md` when done, with before/after render lengths for each page.
</output>
