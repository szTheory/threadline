---
phase: 204-structure
plan: 13
type: execute
wave: 13
depends_on: ["204-12"]
files_modified:
  - test/support/operator_surface_case.ex
  - test/threadline/operator_surface/live/actor_live_test.exs
  - test/threadline/operator_surface/live/coverage_live_test.exs
  - test/threadline/operator_surface/live/evidence_live_test.exs
  - test/threadline/operator_surface/live/export_status_live_test.exs
  - test/threadline/operator_surface/live/policy_redaction_live_test.exs
  - test/threadline/operator_surface/live/retention_history_live_test.exs
  - test/threadline/operator_surface/live/row_history_live_test.exs
  - test/threadline/operator_surface/live/start_live_test.exs
  - test/threadline/operator_surface/live/timeline_live_test.exs
autonomous: true
requirements: [STRUCT-05]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`test/support/operator_surface_case.ex` (whole file inside `if Code.ensure_loaded?(Phoenix.LiveView) do`) defines four modules, each with a real `@moduledoc`: `Threadline.OperatorSurfaceTest.Layouts` (`root/1` titled \"Test\", `render(\"500.html\", _)`), `Threadline.OperatorSurfaceTest.Router` (`__using__(opts)`: `use Phoenix.Router`, LiveView router import, `require Threadline.OperatorSurface.Router`, `pipeline :browser` with `accepts: opts[:accepts] || [\"html\"]`, fetch_session, fetch_live_flash, shared root layout, plus `opts[:browser_plugs]`), `Threadline.OperatorSurfaceTest.Endpoint` (`__using__(router:, parsers: …)`: Session, fetch_session, optional Parsers, MethodOverride, Head, Router; cookie key from `Macro.underscore(__MODULE__)`; constant salt), and `Threadline.OperatorSurfaceCase` (plain `__using__(endpoint:)`, imports ConnTest/LiveViewTest, sets `@endpoint`, and `start_endpoint!(endpoint, extra_env \\\\ [])` that calls `Application.put_env` before `start_supervised!` with `on_exit` cleanup) (D-16)."
    - "The Endpoint template supports the three real variants: default (`parsers: [:json]`), export (`parsers: [:urlencoded, :json]`), and none (`parsers: false`, no `Plug.Parsers` at all) (RESEARCH: three endpoint variants)."
    - "Each migrated file keeps its own unique Endpoint/Router module names, its own `scope` + `threadline_operator_surface(...)` mount with its own opts, its own router helper functions (`auth/1`, `scope_operator_query/3`, `put_test_actor/2`, …) defined in the router module, and its own `use Threadline.DataCase`/`ExUnit.Case` line with the same `async:` value (D-15, D-16)."
    - "Concurrency edge (STRUCT-05): no migrated file starts another file's endpoint, every `async:` flag is unchanged, and `start_endpoint!/2` sets the endpoint's env before `start_supervised!` and restores it in `on_exit`."
    - "The nine `live/*_live_test.exs` files no longer contain `use Phoenix.Endpoint`, and contain `use Phoenix.Router` only through `use Threadline.OperatorSurfaceTest.Router` (D-17)."
    - "Each migrated file's test count is unchanged and its suite passes (recorded before/after per file)."
  artifacts:
    - test/support/operator_surface_case.ex
  key_links:
    - "`verify.compile_no_optional` compiles test/support in :test with optional deps off → the LiveView guard around the whole file keeps it compiling."
    - "Router helpers captured as `&__MODULE__.fun/n` → must stay defined inside each file's router module after `use Threadline.OperatorSurfaceTest.Router`."
  prohibitions:
    - statement: "No migrated test loses an authorization, scope, or actor assertion; each file keeps its own authorize/scope/actor functions and every test it had."
      status: resolved
      verification: test
---

<objective>
Create the shared operator-surface test templates and migrate the nine LiveView test files onto them (STRUCT-05, D-15..D-17). The remaining eight migrations and the structure contract follow in 204-14.

Purpose: 31 hand-rolled Endpoint modules in 18 files normalize to three variants. Shared templates remove the copy-paste while keeping each file's deliberate differences (mount path, opts, router helpers) visible in that file.

Output: `test/support/operator_surface_case.ex` and nine migrated LiveView test files with unchanged test counts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-RESEARCH.md
@.planning/phases/204-structure/204-PATTERNS.md
@CLAUDE.md
</context>

## Rules for this plan

- Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-13-base"`. Before migrating each file, record its test count: `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test <file> 2>&1 | grep -E '[0-9]+ tests?,'`; after migration the count must be identical with 0 failures.
- Commit type `test(204-13)`, one test file per commit (the support file lands with the tracer). `quote` blocks stay under 150 lines (LongQuoteBlocks); aliases used inside `quote` are declared inside it (AliasUsage). No planning vocabulary in test/support.
- Keep each file's `async:` flag, module names, mount path/opts, router helper functions, and `setup`/`setup_all` semantics. The only intended difference is the removed boilerplate.
- HALT clause: if a file cannot be migrated without changing a test's assertions or losing a test, leave that file hand-rolled, record why, and continue; 204-14's structure contract will then need an allowlist entry with that reason (a deliberate, documented per-file difference). Never delete or skip a test, never loosen an assertion, never bypass hooks, never `git add .planning/`.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — shared templates plus actor_live_test migrated end-to-end</name>
  <reversibility rating="reversible">Test-only support module; files can return to hand-rolled modules independently (D-16).</reversibility>
  <files>test/support/operator_surface_case.ex, test/threadline/operator_surface/live/actor_live_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/live/actor_live_test.exs (lines 1-135: Layouts, Router, ScopedRouter, Endpoint, ScopedEndpoint, setup_all)
    - test/support/data_case.ex (the `__using__` + `quote` shape)
    - test/threadline/operator_surface/stress_router_test.exs (~:138-150: env save/put/on_exit restore)
    - test/threadline/operator_surface/gating_test.exs (~:58-72: the endpoint with no Plug.Parsers)
    - test/threadline/operator_surface/controllers/export_controller_test.exs (~:85-99: the urlencoded+json endpoint)
  </read_first>
  <action>
    Per D-16. Create `test/support/operator_surface_case.ex` with the four modules in the must_haves. Endpoint template options: `router:` (required), `parsers:` (default `[:json]`; `false` omits the `Plug.Parsers` plug entirely), `json_decoder:` (default `Phoenix.json_library()`), and `signing_salt:` (default a constant 8-char string). Router template options: `accepts:` (default `["html"]`), `browser_plugs:` (list of atoms plugged after the root layout, in order). `Threadline.OperatorSurfaceCase.__using__(endpoint: mod)` imports `Phoenix.ConnTest` and `Phoenix.LiveViewTest`, sets `@endpoint mod`, and imports `start_endpoint!/2`. `start_endpoint!(endpoint, extra_env \\ [])` saves the prior env, puts `secret_key_base` (64 chars), `live_view: [signing_salt: …]`, `render_errors: [view: Threadline.OperatorSurfaceTest.Layouts]` merged with `extra_env`, then `ExUnit.Callbacks.start_supervised!(endpoint)`, and registers `on_exit` to restore or delete the env. Migrate actor_live_test: delete its Layouts module; its two routers become `use Threadline.OperatorSurfaceTest.Router` plus their own `scope` blocks and helper `def`s (and `import Ecto.Query` where used); its two endpoints become `use Threadline.OperatorSurfaceTest.Endpoint, router: …`; `setup_all` calls `start_endpoint!/2` for each endpoint; keep `use Threadline.DataCase, async: false`. Commit.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live/actor_live_test.exs</automated>
    <fails_when>non-zero exit, any failure in the ExUnit summary, or a test count different from the count recorded before migration</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix verify.compile_no_optional && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict test/support/operator_surface_case.ex test/threadline/operator_surface/live/actor_live_test.exs</automated>
    <fails_when>non-zero exit from either command, a compiler warning, or any credo issue</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'use Phoenix.Endpoint' test/threadline/operator_surface/live/actor_live_test.exs` prints 0
    - `grep -c '@moduledoc """' test/support/operator_surface_case.ex` is at least 4
    - `head -1 test/support/operator_surface_case.ex` is `if Code.ensure_loaded?(Phoenix.LiveView) do`
    - `grep -c 'def scope_operator_query' test/threadline/operator_surface/live/actor_live_test.exs` is at least 1 (router helper stayed in the file)
  </acceptance_criteria>
  <done>The templates exist and one real file runs on them with the same tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate coverage, evidence, export_status, and policy_redaction LiveView tests</name>
  <files>test/threadline/operator_surface/live/coverage_live_test.exs, test/threadline/operator_surface/live/evidence_live_test.exs, test/threadline/operator_surface/live/export_status_live_test.exs, test/threadline/operator_surface/live/policy_redaction_live_test.exs</files>
  <read_first>
    - the Layouts/Router/Endpoint/setup section at the top of each of the four files (only that section)
    - test/support/operator_surface_case.ex
  </read_first>
  <action>Per D-16/D-17, migrate each file exactly as actor_live_test was, one commit per file, recording before/after test counts. Keep each file's mount path, opts, and helper functions in its router module.</action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live/coverage_live_test.exs test/threadline/operator_surface/live/evidence_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs test/threadline/operator_surface/live/policy_redaction_live_test.exs</automated>
    <fails_when>non-zero exit, any failure in the ExUnit summary, or a total test count different from the sum recorded before migration</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -l 'use Phoenix.Endpoint' test/threadline/operator_surface/live/coverage_live_test.exs test/threadline/operator_surface/live/evidence_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs test/threadline/operator_surface/live/policy_redaction_live_test.exs` prints nothing
    - `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict test/threadline/operator_surface/live/coverage_live_test.exs test/threadline/operator_surface/live/evidence_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs test/threadline/operator_surface/live/policy_redaction_live_test.exs` exits 0
  </acceptance_criteria>
  <done>Four more LiveView tests run on the shared templates with unchanged counts.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate retention_history, row_history, start, and timeline LiveView tests</name>
  <files>test/threadline/operator_surface/live/retention_history_live_test.exs, test/threadline/operator_surface/live/row_history_live_test.exs, test/threadline/operator_surface/live/start_live_test.exs, test/threadline/operator_surface/live/timeline_live_test.exs</files>
  <read_first>
    - the Layouts/Router/Endpoint/setup section at the top of each file (timeline_live_test has 5 endpoints and an ActorRouter with `:put_test_actor` near ~:95-117; start_live_test has 3)
  </read_first>
  <action>Per D-16/D-17, migrate each file, one commit per file. timeline_live_test's ActorRouter uses `use Threadline.OperatorSurfaceTest.Router, browser_plugs: [:put_test_actor]` and keeps `def put_test_actor/2` in its own module. Every endpoint keeps a unique module name. Record before/after test counts.</action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live</automated>
    <fails_when>non-zero exit, any failure in the ExUnit summary, or a total test count for test/threadline/operator_surface/live different from the plan-base count</fails_when>
    <automated>test "$(grep -l 'use Phoenix.Endpoint' test/threadline/operator_surface/live/*_test.exs | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>non-zero exit: a LiveView test file still defines its own Phoenix.Endpoint</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'browser_plugs: \[:put_test_actor\]' test/threadline/operator_surface/live/timeline_live_test.exs` prints 1
    - `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` full suite reports 0 failures and the same total test count as at plan base
  </acceptance_criteria>
  <done>All nine LiveView tests run on the shared templates; the full suite's test count is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → operator surface | tests exercise authorization and scoping through per-file routers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-28 | Elevation of privilege | test coverage of authorize/scope fns | medium | mitigate | Per-file router helpers stay in each file; per-file test counts compared before/after |
| T-204-29 | Tampering | shared endpoint env | low | mitigate | `start_endpoint!/2` puts env before start and restores in `on_exit`; unique endpoint names per file |
</threat_model>

<verification>
- `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures with the plan-base test count; `MIX_ENV=test mix verify.compile_no_optional` → exit 0; `mix credo --strict` → exit 0.
</verification>

<success_criteria>
The LiveView test files share endpoint and router templates from test/support with every per-file difference kept visible in the file.
</success_criteria>

## Artifacts this phase produces (this plan)

- `test/support/operator_surface_case.ex` — modules `Threadline.OperatorSurfaceTest.Layouts`, `Threadline.OperatorSurfaceTest.Router` (`__using__/1`: `accepts`, `browser_plugs`), `Threadline.OperatorSurfaceTest.Endpoint` (`__using__/1`: `router`, `parsers`, `json_decoder`, `signing_salt`), `Threadline.OperatorSurfaceCase` (`__using__/1`: `endpoint`; `start_endpoint!/2`)

<output>
Create `.planning/phases/204-structure/204-13-SUMMARY.md` when done, with the before/after test count per migrated file.
</output>
