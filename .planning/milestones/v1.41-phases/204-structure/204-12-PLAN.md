---
phase: 204-structure
plan: 12
type: execute
wave: 12
depends_on: ["204-11"]
files_modified:
  - lib/threadline/operator_surface/controllers/export_controller.ex
  - lib/threadline/operator_surface/controllers/export_controller/encoding.ex
  - lib/threadline/semantics/actor_ref.ex
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/presentation.ex
  - lib/threadline/operator_surface/live/row_history_component.ex
  - lib/threadline/operator_surface/auth.ex
  - test/threadline/operator_surface/controllers/export_controller_test.exs
  - test/threadline/operator_surface/exports_doc_contract_test.exs
  - test/threadline/release_artifact_contract_test.exs
  - test/threadline/source_size_contract_test.exs
  - test/threadline/credo_config_contract_test.exs
autonomous: true
requirements: [STRUCT-04, STRUCT-07]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The 9 banner lines in export_controller.ex and 3 in semantics/actor_ref.ex are gone; each is replaced by a real module boundary (for example the per-format prefix/row/suffix emission moving to `ExportController.Encoding`) or deleted because its section is one cohesive clause group, with any informative text kept as a plain comment without rule characters (D-11)."
    - "`@banner_exceptions` in source_size_contract_test is `%{}` after this plan, so the banner scan is zero-tolerance over `lib/**/*.ex` (STRUCT-04)."
    - "Register sites in export_controller.ex, router.ex, presentation.ex, row_history_component.ex, and auth.ex are drained; after this plan no `# Structural debt:` line remains anywhere in `lib/` (D-13)."
    - "`Threadline.Semantics.ActorRef` keeps every public function, type, and callback; only comment lines change there (public module; no API change)."
    - "auth.ex's actor-reconciliation branch keeps the same condition, the same `emit_actor_mismatch/2` call, and the same return values; gating and export-controller tests stay green (D-13 fallback applies if not)."
    - "Export responses (CSV/JSON bytes, headers, chunking threshold 5_000) are unchanged: export_controller_test and exports_doc_contract_test stay green with their pins reading the controller family."
  artifacts:
    - lib/threadline/operator_surface/controllers/export_controller.ex
    - test/threadline/source_size_contract_test.exs
  key_links:
    - "exports_doc_contract_test (~:90-173) and export_controller_test (~:572) read export_controller.ex source → `Threadline.Test.SourceFamily.read!/1` if pinned text moves to `export_controller/`."
    - "`threadline_operator_surface/2` macro in router.ex → every host router and test router; router_test compiles throwaway routers against it."
  prohibitions:
    - statement: "Operator authorization and actor attribution never change: `authorize_fn`, scope, and actor-mismatch handling produce the same outcomes for every session."
      status: resolved
      verification: test
    - statement: "Export payloads, headers, and streaming behavior are byte-identical for the same request."
      status: resolved
      verification: test
---

<objective>
Finish STRUCT-04 in lib (export controller and ActorRef banners) and drain the last five lib register sites in the operator surface: router macro, presentation, row-history component, auth, export controller (STRUCT-04, STRUCT-07; D-11, D-13).

Purpose: after this plan the banner gate is zero-tolerance and lib carries no structural debt, leaving only test-side sites for 204-15.

Output: banner-free lib, an empty banner map, and five fewer register sites.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-RESEARCH.md
@CLAUDE.md
</context>

## Rules for this plan

- Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-12-base"`.
- `refactor(204-12)`, one lib file per commit (a new sibling plus its parent is one commit). New modules `@moduledoc false`, prose as plain comments, no banners, no planning vocabulary, no `file:line` citations. A new `export_controller/` sibling carries user-visible CSV/JSON output, so add it to the appropriate `@source_owners` group in release_artifact_contract_test.
- Per commit: `mix compile --force --warnings-as-errors`; `mix credo --strict <file>`; the file's tests (`grep -rln '<path or Module>' test`) plus `test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs`. For presentation.ex and row_history_component.ex (rendered), also the byte lock, rendered_output_contract, and the browser lane: `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-12-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-12-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-12-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-12-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"` (D-00b).
- Register procedure: remove the `# Structural debt:` line and its disable together; restructure (extract `defp`, `with`, multi-clause, early return); lower `@register`/`@ceiling` in the same commit (D-13).
- HALT clause: if auth.ex or router.ex cannot be restructured without a behavior change, apply the D-13 per-site fallback for that one site (re-register with exact count and a named post-v1.41 successor) and record it; never weaken a gate, edit a pinned string, add a size exception, or bypass hooks; never `git add .planning/`.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — export controller banners to real boundaries, pins repointed, register site drained</name>
  <reversibility rating="reversible">Internal controller structure; routes and payloads unchanged (D-11).</reversibility>
  <files>lib/threadline/operator_surface/controllers/export_controller.ex, lib/threadline/operator_surface/controllers/export_controller/encoding.ex, test/threadline/operator_surface/controllers/export_controller_test.exs, test/threadline/operator_surface/exports_doc_contract_test.exs, test/threadline/release_artifact_contract_test.exs, test/threadline/source_size_contract_test.exs, test/threadline/credo_config_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/controllers/export_controller.ex (whole file, 366 lines)
    - test/threadline/operator_surface/exports_doc_contract_test.exs (~:85-175) and test/threadline/operator_surface/controllers/export_controller_test.exs (~:565-580)
  </read_first>
  <action>
    Per D-11 and D-13. For each of the 9 banners decide: (a) the per-format prefix / per-batch row formatting / per-format suffix / headers sections form a cohesive encoding concern → move them to `lib/threadline/operator_surface/controllers/export_controller/encoding.ex` (`Threadline.OperatorSurface.Controllers.ExportController.Encoding`, `@moduledoc false`), called from the controller; or (b) the section is one clause group (the three thin actions, the iodata path, the chunked path, filter validation) → delete the rule characters and keep the title text as a plain one-line comment only if it adds information (the 5_000 threshold is worth keeping). Record the decision per banner in the SUMMARY decision table, including a D-11 criterion column: for a boundary, the module or function created; for an outright deletion, the name/arity clause group(s) the section contains and why they form one cohesive clause group (a section that is several unrelated function groups does not qualify and must become a boundary). Repoint pins whose text moved to `Threadline.Test.SourceFamily.read!("lib/threadline/operator_surface/controllers/export_controller.ex")`. Drain the register site. Delete the export_controller banner entry in source_size_contract_test and lower the credo register. Add a new sibling to `@source_owners` if created.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/controllers/export_controller_test.exs test/threadline/operator_surface/exports_doc_contract_test.exs test/threadline/operator_surface/exports_mix_parity_test.exs test/threadline/operator_surface/gating_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>test "$(grep -rcE '^\s*#\s*(-{3,}|={3,}|─{3,})' lib/threadline/operator_surface/controllers | awk -F: '{s+=$2} END {print s}')" = "0" && test "$(grep -c 'Structural debt:' lib/threadline/operator_surface/controllers/export_controller.ex)" = "0"</automated>
    <fails_when>non-zero exit: a banner line or register site remains under controllers/</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export_controller.ex' test/threadline/source_size_contract_test.exs` prints 0
    - `grep -c '5_000' lib/threadline/operator_surface/controllers/export_controller.ex` is at least 1 (the threshold is still in the controller)
  </acceptance_criteria>
  <done>The export controller has real boundaries instead of banners and no register site, with identical export behavior.</done>
</task>

<task type="auto">
  <name>Task 2: ActorRef banners and the router, presentation, row-history, and auth register sites; banner map to empty</name>
  <files>lib/threadline/semantics/actor_ref.ex, lib/threadline/operator_surface/router.ex, lib/threadline/operator_surface/presentation.ex, lib/threadline/operator_surface/live/row_history_component.ex, lib/threadline/operator_surface/auth.ex, test/threadline/source_size_contract_test.exs, test/threadline/credo_config_contract_test.exs</files>
  <read_first>
    - lib/threadline/semantics/actor_ref.ex (lines 25-110: the three banner sections)
    - the enclosing function of each `Structural debt:` line in router.ex (the `threadline_operator_surface/2` macro head ~:56), presentation.ex, row_history_component.ex, auth.ex (~:120-140)
  </read_first>
  <action>
    Per D-11: actor_ref.ex's sections (constructor, map serialization, `Ecto.ParameterizedType` callbacks) are cohesive clause groups in a public module: delete the three banners; no function moves. Add each to the SUMMARY decision table with its D-11 criterion entry: the name/arity groups the section holds (constructor `new/2` + `identifiable?/1`; serialization `to_map/1` + `from_map/1` + private `type_from_string/1`; the `Ecto.ParameterizedType` callback set `init/1`, `type/1`, `cast/2`, `load/3`, `dump/3`) and why each is one cohesive group whose public functions cannot leave the public module. Then `@banner_exceptions %{}` in source_size_contract_test (commit `refactor(204-12): remove separator banners from ActorRef`).
    Per D-13, one commit per file: router.ex — extract the macro's option validation (complexity 12) into private helper functions evaluated at expansion time, keeping every raised error message and the generated quoted code identical; presentation.ex and row_history_component.ex — flatten with `with`/multi-clause/extracted `defp` and run the browser lane; auth.ex — extract the session-vs-scope actor reconciliation into a `defp` that keeps the `!=` comparison, the `emit_actor_mismatch/2` call, and the return values exactly. Lower the register each commit.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/semantics test/threadline/operator_surface/router_test.exs test/threadline/operator_surface/gating_test.exs test/threadline/operator_surface/live/row_history_live_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/style_byte_lock_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs $(grep -rlE 'OperatorSurface\.Auth|Presentation\.' test | sort)</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary; a missing-path error means a listed path does not exist — substitute from `ls test/threadline` and record it</fails_when>
    <automated>test -z "$(grep -rln 'Structural debt:' lib)" && test "$(grep -rcE '^\s*#\s*(-{3,}|={3,}|─{3,}|\*{3,})' lib --include='*.ex' | awk -F: '{s+=$2} END {print s}')" = "0" && grep -q '@banner_exceptions %{}' test/threadline/source_size_contract_test.exs</automated>
    <fails_when>non-zero exit: a lib register site or banner remains, or the banner map is not empty</fails_when>
  </verify>
  <acceptance_criteria>
    - `D=$(git diff "$(cat "$(git rev-parse --git-dir)/gsd-204-12-base")" HEAD -- lib/threadline/semantics/actor_ref.ex) && ! printf '%s\n' "$D" | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)' | grep -qvE '^[-+][[:space:]]*(#.*)?$'` exits 0 (only comment or blank lines changed in ActorRef)
    - `grep -c 'emit_actor_mismatch' lib/threadline/operator_surface/auth.ex` equals its count at plan base (record both)
    - the browser lane chain passed after the presentation.ex and row_history_component.ex commits
  </acceptance_criteria>
  <done>lib has zero banners and zero register sites; the banner gate is zero-tolerance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → export controller | operator-authorized export downloads of audit data |
| session → auth | session actor vs scope actor reconciliation decides attribution |
| host router → operator surface macro | macro options configure authorization hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-24 | Spoofing | auth.ex actor reconciliation | high | mitigate | Extraction keeps comparison, telemetry emission, and returns; gating tests green; per-site fallback if any doubt |
| T-204-25 | Elevation of privilege | router.ex macro option validation | high | mitigate | Error messages and generated code unchanged; router_test compiles throwaway routers covering the options |
| T-204-26 | Information disclosure | export_controller encoding | medium | mitigate | export_controller_test, exports_mix_parity_test, and exports doc contracts pin payloads and headers |
| T-204-27 | Repudiation | ActorRef serialization | medium | mitigate | Only comment lines change (acceptance diff check) |
</threat_model>

<verification>
- `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures; `mix credo --strict` → exit 0; `MIX_ENV=dev mix verify.dialyzer` → exit 0.
</verification>

<success_criteria>
Separator comments no longer stand in for boundaries anywhere in lib (ROADMAP SC 2, second half), and lib carries no structural-register site.
</success_criteria>

## Artifacts this phase produces (this plan)

- Optional `Threadline.OperatorSurface.Controllers.ExportController.Encoding` (`lib/threadline/operator_surface/controllers/export_controller/encoding.ex`)
- Extracted private helpers in router.ex (macro option validation), auth.ex (actor reconciliation), presentation.ex, row_history_component.ex
- `@banner_exceptions %{}` in test/threadline/source_size_contract_test.exs

<output>
Create `.planning/phases/204-structure/204-12-SUMMARY.md` when done, with the per-banner decision table (columns: file, banner line, section, disposition, D-11 criterion) and the register sum after the plan.
</output>
