---
phase: 204-structure
plan: 15
type: execute
wave: 15
depends_on: ["204-14"]
files_modified:
  - test/threadline/credo_config_contract_test.exs
  - test/threadline/dialyzer_ignore_contract_test.exs
  - test/threadline/guide_graph_contract_test.exs
  - test/threadline/release_artifact_contract_test.exs
  - test/support/getting_started_fixtures.ex
  - test/threadline/operator_surface/card_nesting_regression_test.exs
  - test/threadline/operator_surface/rendered_output_contract_test.exs
  - test/threadline/operator_surface/operator_surface_fixture_contract_test.exs
  - test/threadline/source_size_contract_test.exs
autonomous: true
requirements: [STRUCT-07, STRUCT-03, STRUCT-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The 12 test-side register sites (rendered_output_contract_test 3, operator_surface_fixture_contract_test 3, support/getting_started_fixtures 2, release_artifact_contract_test 1, card_nesting_regression_test 1, guide_graph_contract_test 1, dialyzer_ignore_contract_test 1; re-measure) are fixed, each `# Structural debt:` line removed with its disable (D-13)."
    - "credo_config_contract_test ends at `@register %{}`, `@ceiling 0`, `@historical_max 46`; the shape check accepts any subset of the two registered checks (a missing key counts as 0), the self-tests use a local synthetic register instead of `Map.fetch!(@register, …)`, `@successor` stays used, and the moduledoc states the register is drained and any new disable must be registered in review (D-13)."
    - "Boundary edge (STRUCT-07): the real tree passes with the register at exactly 0, and a synthetic tree with one annotated Nesting disable fails with `scanned 1`; Credo `max_nesting 2` and `max_complexity 9` are unchanged."
    - "Adjacency edge (STRUCT-07): no orphan `# Structural debt:` comment and no orphan per-site Refactor disable remains anywhere in `lib/` or `test/` (both greps return nothing)."
    - "Empty edge (STRUCT-07): an empty register is a valid register, and the empty-scan-set guard still fails an empty file list."
    - "source_size_contract_test gains an at-rest test: `Map.keys(@file_exceptions) == [\"lib/threadline/operator_surface/stress_fixtures.ex\"]`, `@function_exceptions == %{}`, `@banner_exceptions == %{}` — exactly one named exception at phase end (D-12, STRUCT-03, STRUCT-04)."
    - "Phase-end proof: `MIX_ENV=test mix ci.all` exits 0; `mix verify.bump_rehearsal` prints \"Bump rehearsal OK\"; the local browser lane is exactly 82 passed / 8 failed with the known eight; `MIX_ENV=dev mix verify.dialyzer`, `mix verify.xref_cycles`, and `MIX_ENV=test mix verify.compile_no_optional` exit 0 (D-00, D-00b)."
  artifacts:
    - test/threadline/credo_config_contract_test.exs
    - test/threadline/source_size_contract_test.exs
  key_links:
    - "`mix credo --strict` (verify.credo in ci.all) + credo_config_contract_test exact register → any new structural disable fails until registered in review."
  prohibitions:
    - statement: "The register reaches zero only by real fixes: no bulk re-registration, no `disable-for-this-file`/`disable-for-lines`, no threshold raise, no `disabled:` entry."
      status: resolved
      verification: test
    - statement: "No contract test's assertion strength drops while its own structure is flattened (same assertions, same failure messages, same scanned sets)."
      status: resolved
      verification: judgment
---

<objective>
Drain the last twelve structural-register sites (all in test code), ratchet the Credo structural ceiling from its remaining value to 0 with an empty register, lock the size gate's at-rest state to exactly one named exception, and run the phase-end proof (STRUCT-07, closing STRUCT-03/04; D-12, D-13).

Purpose: ROADMAP SC 5 requires the ceiling at 0 (or named post-v1.41 successors), and SC 2 requires exactly one named exception. This plan makes both executable and proves the whole phase green.

Output: an empty credo register, an at-rest size assertion, and recorded phase-end gate results.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-RESEARCH.md
@.planning/phases/204-structure/204-PATTERNS.md
@CLAUDE.md
</context>

## Rules for this plan

- Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-15-base"`.
- `refactor(204-15)` for flattening test code, `test(204-15)` for gate changes; one file per commit. Per commit: `mix credo --strict <file>`, the file itself under `mix test`, plus `test/threadline/credo_config_contract_test.exs`; lower `@register`/`@ceiling` in the same commit.
- These are contract tests: a flattened function must assert exactly what it asserted before (same checks, same messages, same scanned sets). Prefer extracting a `defp` that returns offenders over rewriting the assertion.
- HALT clause: a site that cannot be flattened without weakening its test is re-registered alone under the D-13 fallback (exact count, named post-v1.41 successor, and then `@ceiling` equals that count rather than 0 — record it prominently in the SUMMARY). Never bulk re-register, never delete an assertion, never raise a threshold, never regenerate baselines, never bypass hooks, never `git add .planning/`. If any phase-end gate is red, stop and report the failing gate; do not patch around it.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — make an empty register valid, then drain the dialyzer_ignore_contract_test site through it</name>
  <reversibility rating="reversible">Test-gate shape change; the register can be repopulated by a reviewed diff (D-13 rating).</reversibility>
  <files>test/threadline/credo_config_contract_test.exs, test/threadline/dialyzer_ignore_contract_test.exs</files>
  <read_first>
    - test/threadline/credo_config_contract_test.exs (lines 1-130, 175-190, 360-420, 480-510)
    - test/threadline/dialyzer_ignore_contract_test.exs (the enclosing function of its `Structural debt:` line)
  </read_first>
  <action>
    Per D-13. Commit 1 (`test(204-15)`): change `validate_register_shape!/2` to demand `register` is a map whose keys are a subset of `@registered_checks` (keep rejecting any other key, which the existing extra-check test at ~:184 proves); in `validate_register/3` read `Map.get(register, check, {0, @successor})`; rewrite the "one extra or one missing disable" self-test to build its own `register_with(2, 1)` and synthetic sources instead of `Map.fetch!(@register, …)`; add a self-test "an empty register accepts a tree with no disables and rejects one annotated Nesting disable with `scanned 1`". Keep the real-tree assertions (`@ceiling == sum`, `@ceiling <= @historical_max`, `@historical_max == 46`). Commit 2 (`refactor(204-15)`): drain the dialyzer_ignore_contract_test site and lower the register/ceiling by one.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/credo_config_contract_test.exs test/threadline/dialyzer_ignore_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict test/threadline/credo_config_contract_test.exs test/threadline/dialyzer_ignore_contract_test.exs</automated>
    <fails_when>non-zero exit or any credo issue listed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Map.fetch!(@register' test/threadline/credo_config_contract_test.exs` prints 0
    - `grep -c 'Structural debt:' test/threadline/dialyzer_ignore_contract_test.exs` prints 0
  </acceptance_criteria>
  <done>The gate can represent a drained register, proven on the first test-side site.</done>
</task>

<task type="auto">
  <name>Task 2: Drain the remaining eleven test sites; set the register to %{} and the ceiling to 0; lock the size gate at rest</name>
  <files>test/threadline/guide_graph_contract_test.exs, test/threadline/release_artifact_contract_test.exs, test/support/getting_started_fixtures.ex, test/threadline/operator_surface/card_nesting_regression_test.exs, test/threadline/operator_surface/rendered_output_contract_test.exs, test/threadline/operator_surface/operator_surface_fixture_contract_test.exs, test/threadline/credo_config_contract_test.exs, test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - `grep -rn 'Structural debt:' test` output, then the enclosing function of each site only
    - test/threadline/source_size_contract_test.exs (the three exception maps)
  </read_first>
  <action>
    Per D-13, one commit per file (getting_started_fixtures.ex is test/support: run `test/threadline/getting_started_fixtures_test.exs` and every test that uses it). After the last site: `@register %{}` and `@ceiling 0` (commit `test(204-15): pin the structural register at zero`), and update the moduledoc paragraph that said the register "ratchets toward 0" to say it is drained and a new disable must be registered in review. Per D-12, add the at-rest test to source_size_contract_test asserting `Map.keys(@file_exceptions) == ["lib/threadline/operator_surface/stress_fixtures.ex"]`, `@function_exceptions == %{}`, and `@banner_exceptions == %{}`, with a failure message saying a new exception needs a named reason in review.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/credo_config_contract_test.exs test/threadline/source_size_contract_test.exs test/threadline/guide_graph_contract_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/getting_started_fixtures_test.exs test/threadline/operator_surface/card_nesting_regression_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/operator_surface_fixture_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>test -z "$(grep -rln 'Structural debt:' lib test --include='*.ex' --include='*.exs' | grep -v 'test/threadline/credo_config_contract_test.exs')" && grep -q '@register %{}' test/threadline/credo_config_contract_test.exs && grep -q '@ceiling 0' test/threadline/credo_config_contract_test.exs && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict</automated>
    <fails_when>non-zero exit: a `Structural debt:` comment survives outside the contract test's own string literals, the register/ceiling are not zero, or `mix credo --strict` reports any issue</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@historical_max 46' test/threadline/credo_config_contract_test.exs` prints 1
    - `grep -c 'stress_fixtures.ex' test/threadline/source_size_contract_test.exs` is at least 2 (the exception entry and the at-rest assertion)
    - `grep -rnE 'credo:disable-for-next-line Credo\.Check\.Refactor\.(Nesting|CyclomaticComplexity)' lib test` prints only lines inside string literals of credo_config_contract_test (inspect and record)
  </acceptance_criteria>
  <done>The structural register is empty and pinned at zero, and the size gate's at-rest state is exactly one named exception.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-end proof across every gate</name>
  <files>test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - .planning/phases/204-structure/204-VALIDATION.md (sampling rate and phase gate)
  </read_first>
  <action>
    Per D-00 and D-00b, run and record (command, exit code, summary line, duration) in the SUMMARY: `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format`; `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix ci.all`; `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.bump_rehearsal`; the local browser lane chain; `MIX_ENV=dev mix verify.dialyzer`. Also record the before/after line counts of every file that had an exception at 204-01 and the byte-lock hashes (unchanged from 204-01). No code change is expected; the file listed is only touched if a recorded measurement reveals a stale pin, which is a HALT condition to report rather than patch. The Elixir 1.15 minimum lane runs in CI only: note it as pending CI.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix ci.all</automated>
    <fails_when>non-zero exit from any ci.all step (the failing alias is printed last)</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-15-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-15-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-15-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-15-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"</automated>
    <fails_when>non-zero exit: not exactly 82 passed / 8 failed, a failure outside operator-screenshot-regression.spec.ts, or a baseline PNG changed</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.bump_rehearsal > /tmp/p204-15-rehearsal.log 2>&1; grep -q 'Bump rehearsal OK' /tmp/p204-15-rehearsal.log</automated>
    <fails_when>the log lacks "Bump rehearsal OK"</fails_when>
  </verify>
  <acceptance_criteria>
    - `L=$(git log --format=%h -- test/fixtures/style/operator_surface.css) && test "$(printf '%s\n' "$L" | grep -c .)" = "1"` exits 0 (the golden was created in 204-01 and never modified afterwards)
    - the SUMMARY contains a gate table with exit 0 for verify.format, ci.all, verify.bump_rehearsal, verify.dialyzer, and the browser lane at 82/8
  </acceptance_criteria>
  <done>Every phase gate is green and recorded; the CSS golden was never touched after it was created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor diff → Credo/size gates | gates decide whether new structural debt can land |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-32 | Tampering | credo_config_contract_test register | medium | mitigate | Empty-register semantics proven by synthetic self-tests; exact equality still fails any unregistered disable |
| T-204-33 | Tampering | contract tests being flattened | medium | mitigate | Same assertions/messages/scans required; each test file runs green after its own commit |
| T-204-34 | Repudiation | size exceptions | low | mitigate | At-rest assertion pins exactly one named exception |
</threat_model>

<verification>
- Task 3 gate table all green; the golden CSS file has exactly one commit in its history.
</verification>

<success_criteria>
The credo_config_contract_test structural ceiling is 0 with an empty register (ROADMAP SC 5), exactly one named size exception remains (SC 2), and every gate in ci.all plus the bump rehearsal and local browser lane is green.
</success_criteria>

## Artifacts this phase produces (this plan)

- `test/threadline/credo_config_contract_test.exs` — `@register %{}`, `@ceiling 0`; subset-tolerant `validate_register_shape!/2`; new empty-register self-test
- `test/threadline/source_size_contract_test.exs` — new at-rest exceptions test
- Extracted private helpers in the seven flattened test files (names recorded in the SUMMARY)

<output>
Create `.planning/phases/204-structure/204-15-SUMMARY.md` when done, with the phase-end gate table.
</output>
