---
phase: 204-structure
plan: 08
type: execute
wave: 8
depends_on: ["204-07"]
files_modified:
  - lib/threadline/operator_surface/live/timeline_live.ex
  - lib/threadline/operator_surface/live/timeline_live/helpers.ex
  - lib/threadline/operator_surface/live/timeline_live/filters.ex
  - test/threadline/operator_surface/timeline_browse_doc_contract_test.exs
  - test/threadline/operator_surface/exports_doc_contract_test.exs
  - test/threadline/operator_surface/live/timeline_live_test.exs
  - test/threadline/operator_surface/ui_form_policy_contract_test.exs
  - test/threadline/operator_surface/component_contract_test.exs
  - test/threadline/release_artifact_contract_test.exs
  - test/threadline/source_size_contract_test.exs
  - test/threadline/credo_config_contract_test.exs
autonomous: true
requirements: [STRUCT-03, STRUCT-04, STRUCT-07]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`timeline_live.ex` is under 800 lines with two `@moduledoc false` siblings: `TimelineLive.Helpers` (presentation helpers from the helper section) and `TimelineLive.Filters` (the `timeline_command` and `timeline_filter_drawer` function components with `attr` declarations) — the case D-08 anticipates when one sibling is not enough (resolved open question 2)."
    - "`scope_aware_opts` and `defp preload_visible_context(%{entries: entries} = page, repo, opts)` stay in timeline_live.ex, where timeline_live_test pins them (RESEARCH extraction math)."
    - "Every function clause in the timeline family is at most 120 lines: `render/1`, `timeline_command/1`, and `timeline_filter_drawer/1` are carved into function components called as `<.name …/>` or `<Filters.name …/>` with `attr` declarations; no `embed_templates`, no `.heex`, no `defdelegate` (D-08)."
    - "The 10 banner lines in timeline_live.ex are gone: the helper banner by the module boundary, the callback rule/title/rule triplets deleted because each section is one cohesive callback clause group (D-11)."
    - "Both register sites in timeline_live.ex are drained, with the credo register lowered in the same commit (D-13)."
    - "Doc-contract pins on the filter form, drawer, and download anchors read the timeline family through `Threadline.Test.SourceFamily.read!/1`; the line-1 `if Code.ensure_loaded?(Phoenix.LiveView) do` guard check stays a single-file read (RESEARCH Pattern 3)."
    - "Rendered output is unchanged at every commit: byte lock, rendered_output_contract, timeline tests, and the browser lane at exactly the 8 known failures (D-00b)."
  artifacts:
    - lib/threadline/operator_surface/live/timeline_live/helpers.ex
    - lib/threadline/operator_surface/live/timeline_live/filters.ex
  key_links:
    - "component_contract_test per-page shell scan (`@page_live_views` loop: the `UI.Page.shell` assert and the `class=\"threadline-ui\"` / `id=\"tl-main\"` refutations, plus the reconnect/`.phx-disconnected` scan over the same file list) → `Threadline.Test.SourceFamily.read!/1` of `live/<file>` plus its `live/<page>/` siblings, repointed in this plan's first commit, which is the first commit in the phase to create a LiveView sibling; markup moved to `TimelineLive.Filters` or a later sibling therefore stays inside the refutation scope."
    - "release_artifact_contract_test `@source_owners.source_vocab_operator_live_records` → both new siblings, so the archive vocabulary scan keeps covering the timeline's user-visible copy."
    - "rendered_output_contract_test `owned_static_sources/0` uses the git pathspec `lib/threadline/operator_surface/live/*.ex`, whose `*` also matches `timeline_live/*.ex`; confirm the siblings appear in its scanned set."
  prohibitions:
    - statement: "No timeline filter, cursor, or scope behavior changes: the same URL params yield the same query and the same rendered page."
      status: resolved
      verification: test
---

<objective>
Make the 1,399-line Timeline LiveView legible: two siblings for helpers and filter components, a carved `render/1`, no banners, and no register sites (STRUCT-03, STRUCT-04, STRUCT-07; D-08, D-11, D-13).

Purpose: Timeline is the product's primary screen, so its LiveView is the file contributors touch most. It is also the densest source of doc-contract pins, which is why it has its own plan.

Output: timeline_live.ex under 800 lines, `TimelineLive.Helpers`, `TimelineLive.Filters`, repointed pins, and three fewer function exceptions.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-RESEARCH.md
@.planning/phases/204-structure/204-PATTERNS.md
@CLAUDE.md
</context>

## Zero-output-change proof (ZOC) — after EVERY commit (D-00b)

1. `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors`
2. `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/timeline_browse_doc_contract_test.exs test/threadline/operator_surface/exports_doc_contract_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/style_byte_lock_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/operator_surface/ui_form_policy_contract_test.exs test/threadline/operator_surface/component_contract_test.exs`
3. Browser lane: `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-08-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-08-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-08-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-08-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"`
4. `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict lib/threadline/operator_surface/live/timeline_live.ex lib/threadline/operator_surface/live/timeline_live/`

Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-08-base"`. Before each commit, run `grep -rn 'timeline_live.ex' test examples/threadline_phoenix/test` and repoint any pin whose text moved.

**HALT clause:** a failing doc-contract pin means pinned text moved or changed: repoint the read to the source family (text unchanged) or move the code back; never edit the pinned string. A red byte lock, rendered-output contract, 9th browser failure, or newly passing member of the known 8 is a regression: revert and retry. Never add a size exception, never regenerate baselines, never bypass hooks, never `git add .planning/`.

Conventions (D-00): `refactor(204-08)`; new modules inside `if Code.ensure_loaded?(Phoenix.LiveView) do`, `@moduledoc false`, `use Phoenix.Component`, only the aliases they use; `attr(...)` paren style from components/surface_header.ex; no banners, no `file:line` comments, no planning vocabulary.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — extract TimelineLive.Helpers with pins, packaging owners, and full ZOC</name>
  <reversibility rating="reversible">Internal LiveView helper module; no public API or rendered change (D-08).</reversibility>
  <files>lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/operator_surface/live/timeline_live/helpers.ex, test/threadline/operator_surface/ui_form_policy_contract_test.exs, test/threadline/operator_surface/component_contract_test.exs, test/threadline/operator_surface/timeline_browse_doc_contract_test.exs, test/threadline/operator_surface/exports_doc_contract_test.exs, test/threadline/release_artifact_contract_test.exs, test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/live/timeline_live.ex (lines 1-30, then 880-930, then skim function heads after 930 via `grep -n '  defp\? ' `)
    - test/threadline/operator_surface/timeline_browse_doc_contract_test.exs (every `File.read!` of timeline_live.ex, including the line-1 guard check ~:106-110)
    - test/threadline/operator_surface/exports_doc_contract_test.exs (~:15-35, ~:150-160)
    - test/threadline/operator_surface/live/timeline_live_test.exs (~:1345-1360)
    - test/threadline/release_artifact_contract_test.exs (lines 16-47 `@source_owners`)
    - test/threadline/operator_surface/ui_form_policy_contract_test.exs (lines 40-100: the `:formless` branch scans `File.read!(path)` of the LiveView file only)
    - test/threadline/operator_surface/component_contract_test.exs (~:455-530: `@page_live_views` and every `File.read!` over it — the shared-shell test and the reconnect-anchor scan)
  </read_first>
  <action>
    Per D-08 (sibling because the file stays over 800) and D-11. Create `lib/threadline/operator_surface/live/timeline_live/helpers.ex`, `Threadline.OperatorSurface.Live.TimelineLive.Helpers`, holding the presentation helpers after `preload_visible_context` (~:920-end, re-measure). Functions called from the parent or its templates become `def` (`@doc false`); private helpers used only inside the sibling stay `defp`. The parent aliases `Helpers` and calls `Helpers.fun(...)` (templates included). Keep `scope_aware_opts` and `preload_visible_context` in the parent. Remove the helper-section banner. Add the sibling path to `@source_owners.source_vocab_operator_live_records`. Repoint timeline_browse and exports doc-contract reads of timeline_live.ex to `Threadline.Test.SourceFamily.read!/1` except the line-1 guard check, which stays single-file. Lower the timeline_live.ex file pin and banner count in source_size_contract_test. In the same commit, harden ui_form_policy_contract_test so moving markup into a sibling can never hide a form control from the `:formless` scan: replace `File.read!(path)` in that branch with `Threadline.Test.SourceFamily.read!(relative)` (page file plus its `<page>/` siblings), and update the failure message's `UI.field / UI.field_group` to `UI.Form.field / UI.Form.field_group`. This strengthens the guard; never weaken it. In the same commit, apply the same hardening to component_contract_test's per-page source scans (this is the first commit in the phase that creates a LiveView sibling, so it must land here): in the shared-shell test (`for file <- @page_live_views`) and the reconnect-anchor scan that maps `@page_live_views` to paths, replace `File.read!(Path.join("lib/threadline/operator_surface/live", file))` / `File.read!(file)` with `Threadline.Test.SourceFamily.read!(...)` of the same path, so the `UI.Page.shell` assert and the `class="threadline-ui"` / `id="tl-main"` / `.phx-disconnected` refutations cover `live/<page>/*.ex` siblings created now or by 204-09/204-10/204-11. Assertions and messages are otherwise unchanged. Commit and run ZOC.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/timeline_browse_doc_contract_test.exs test/threadline/operator_surface/exports_doc_contract_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/style_byte_lock_test.exs test/threadline/source_size_contract_test.exs test/threadline/release_artifact_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-08-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-08-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-08-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-08-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"</automated>
    <fails_when>non-zero exit: not exactly 82 passed / 8 failed, a failure outside operator-screenshot-regression.spec.ts, or a baseline PNG changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'defp preload_visible_context(%{entries: entries} = page, repo, opts)' lib/threadline/operator_surface/live/timeline_live.ex` prints 1
    - `grep -c 'timeline_live/helpers.ex' test/threadline/release_artifact_contract_test.exs` prints 1
    - `grep -c 'SourceFamily' test/threadline/operator_surface/timeline_browse_doc_contract_test.exs` is at least 1
    - `grep -c 'SourceFamily.read!' test/threadline/operator_surface/ui_form_policy_contract_test.exs` is at least 1
    - `awk '/every operator page routes its chrome/,/^    end$/' test/threadline/operator_surface/component_contract_test.exs | grep -c 'File.read!'` prints 0 and `awk '/neither the shell nor any page anchors reconnect/,/^    end$/' test/threadline/operator_surface/component_contract_test.exs | grep -c 'File.read!'` prints 0 (both per-page scans read the source family; 204-05 may already have switched the `@ui_module` entry of the reconnect scan)
  </acceptance_criteria>
  <done>The helper section is a real module, every pin still sees its text, and output is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extract TimelineLive.Filters and carve render/timeline_command/timeline_filter_drawer to ≤120 lines</name>
  <files>lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/operator_surface/live/timeline_live/filters.ex, test/threadline/release_artifact_contract_test.exs, test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/live/timeline_live.ex (the `render/1`, `timeline_command/1`, `timeline_filter_drawer/1` bodies only)
    - lib/threadline/operator_surface/components/surface_header.ex (`attr` declaration style)
  </read_first>
  <action>
    Per D-08. Commit 1: create `lib/threadline/operator_surface/live/timeline_live/filters.ex`, `TimelineLive.Filters`, moving `timeline_command` and `timeline_filter_drawer` as public function components with `attr` declarations for every assign they read, plus their private helpers; the parent calls `<Filters.timeline_command …/>` / `<Filters.timeline_filter_drawer …/>` with the same assigns. Add the path to `@source_owners`. Commit 2: carve each of `timeline_command/1` and `timeline_filter_drawer/1` into sub-components (for example the window summary, active-filter chips, the advanced disclosure, each fieldset) until every clause is at most 120 lines. Commit 3: carve `render/1` in the parent into private `defp` function components (`<.name …/>`) with `attr` declarations, at most 120 lines each. Markup moves verbatim: same elements, attributes, classes, ids, and whitespace-significant text; HEEx whitespace between tags may be normalised only if the byte lock and rendered-output contract stay green. Delete the matching function exceptions and lower the file pin in the commit that makes each stale.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/timeline_browse_doc_contract_test.exs test/threadline/operator_surface/exports_doc_contract_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/copy_contract_test.exs test/threadline/source_size_contract_test.exs test/threadline/release_artifact_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>test "$(grep -c 'live/timeline_live' test/threadline/source_size_contract_test.exs)" -le 1 && ! grep -q 'embed_templates' lib/threadline/operator_surface/live/timeline_live.ex lib/threadline/operator_surface/live/timeline_live/filters.ex</automated>
    <fails_when>non-zero exit: more than the file-pin entry for timeline_live remains in the size gate (a function exception survived), or embed_templates appeared</fails_when>
  </verify>
  <acceptance_criteria>
    - the source_size_contract_test real-tree test passes with no `timeline_live` function exception (grep for `:timeline_filter_drawer`, `:timeline_command`, and `timeline_live.ex", :render` in the file prints 0 matches)
    - `grep -c '^ *attr(' lib/threadline/operator_surface/live/timeline_live/filters.ex` is at least 4
    - the ZOC browser chain passed after each of the three commits (log outcomes in the SUMMARY)
  </acceptance_criteria>
  <done>No timeline function exceeds 120 lines and the filter UI lives in its own module with typed attrs.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the remaining callback banners and drain both timeline register sites</name>
  <files>lib/threadline/operator_surface/live/timeline_live.ex, test/threadline/source_size_contract_test.exs, test/threadline/credo_config_contract_test.exs</files>
  <read_first>
    - `grep -nE '^\s*#\s*(-{3,}|={3,}|─{3,})' lib/threadline/operator_surface/live/timeline_live.ex` output with 2 lines of context
    - the enclosing function of each `Structural debt:` line in timeline_live.ex
  </read_first>
  <action>
    Per D-11: delete each remaining rule/title/rule triplet (mount, handle_params, handle_event, render): each section is one cohesive callback clause group, so no boundary is lost; a one-line plain comment may remain only if it says something the code does not. Per D-13: drain both register sites (commit per site or one commit for the file), lowering `@register`/`@ceiling`. Delete the timeline_live banner entry and, if the file is now under 800, its file pin. If `handle_params/3` (116 lines at research time) crosses 120 after edits, split it rather than pinning it.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>test "$(grep -cE '^\s*#\s*(-{3,}|={3,}|─{3,})' lib/threadline/operator_surface/live/timeline_live.ex)" = "0" && test "$(grep -c 'Structural debt:' lib/threadline/operator_surface/live/timeline_live.ex)" = "0" && test "$(wc -l < lib/threadline/operator_surface/live/timeline_live.ex)" -le 800</automated>
    <fails_when>non-zero exit: a banner or register site remains, or the file is still over 800 lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'live/timeline_live' test/threadline/source_size_contract_test.exs` prints 0
    - ZOC browser chain passed after the final commit
  </acceptance_criteria>
  <done>The timeline family has no size exception, no banners, and no register sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → timeline filters | operator-supplied filter/cursor params drive queries |
| operator browser ← timeline HTML | scope-aware rendering must not show rows outside the operator's scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-18 | Information disclosure | `scope_aware_opts` / `preload_visible_context` | high | mitigate | Both stay in the LiveView untouched; timeline_live_test pins the head; scope tests green |
| T-204-19 | Tampering | filter form markup | medium | mitigate | Doc-contract pins on form/drawer/download anchors keep reading the family; rendered-output contract green |
</threat_model>

<verification>
- ZOC green after every commit; `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures; `MIX_ENV=dev mix verify.dialyzer` → exit 0.
</verification>

<success_criteria>
Timeline LiveView under 800 lines, every function ≤120, no banners, no register sites, byte-identical output.
</success_criteria>

## Artifacts this phase produces (this plan)

- `Threadline.OperatorSurface.Live.TimelineLive.Helpers` (`lib/threadline/operator_surface/live/timeline_live/helpers.ex`)
- `Threadline.OperatorSurface.Live.TimelineLive.Filters` (`lib/threadline/operator_surface/live/timeline_live/filters.ex`) with `timeline_command/1`, `timeline_filter_drawer/1` and their sub-components
- Private function components inside timeline_live.ex carved from `render/1` (names recorded in the SUMMARY)

<output>
Create `.planning/phases/204-structure/204-08-SUMMARY.md` when done, with before/after line counts and the carved component list.
</output>
