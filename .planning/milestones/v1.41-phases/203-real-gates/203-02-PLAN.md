---
phase: 203-real-gates
plan: 02
type: execute
wave: 2
depends_on: ["203-01"]
files_modified:
  - lib/threadline/capture/audit_transaction.ex
  - test/threadline/no_warn_undefined_contract_test.exs
  - mix.exs
  - .github/workflows/ci.yml
  - test/threadline/ci_topology_contract_test.exs
  - CONTRIBUTING.md
  - guides/configuration-and-commands.md
  - guides/evaluating-threadline.md
  - guides/adoption-pilot-backlog.md
autonomous: true
requirements: [GATE-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`lib/threadline/capture/audit_transaction.ex` no longer contains `@compile {:no_warn_undefined, Threadline.Semantics.AuditAction}`, while `belongs_to(:action, Threadline.Semantics.AuditAction)` and AuditAction's `has_many :transactions` are unchanged (D-17)."
    - "GATE-04 'cycle resolved' means zero compile-connected xref cycles plus zero `no_warn_undefined` papering. The runtime AuditTransaction<->AuditAction association edge remains by design, because it is the ordinary bidirectional Ecto association shape and `Repo.preload(tx, :action)` is public API (D-18)."
    - "`mix verify.xref_cycles` runs `xref graph --format cycles --label compile-connected --fail-above 0`, exits 0, and is wired into `ci.all` immediately after `compile --warnings-as-errors` and as a step inside the existing `verify-test` CI job with no lane guard, so the Elixir 1.15 min lane runs it (D-19a)."
    - "A contract test fails on any `no_warn_undefined` in `lib/**/*.ex` or in `Mix.Project.config()[:elixirc_options]`, so the suppression cannot be relocated (D-19b)."
    - "`mix compile --force --warnings-as-errors` is clean after the deletion, and `Repo.preload(tx, :action)` still works: investigation tests are green (D-17)."
    - "No CI job `id:` changes; `ci_workflow_parity_contract_test.exs` and `ci_topology_contract_test.exs` stay green, and the topology test pins the new alias and its position."
    - "Every guide that enumerates the `ci.all` chain step by step (`guides/evaluating-threadline.md` 'The chain runs', `guides/adoption-pilot-backlog.md` 'Evidence pass' and the 'Full maintainer CI chain' row) lists `mix verify.xref_cycles` immediately after `mix compile --warnings-as-errors`, so no human-facing chain enumeration goes stale (D-19a)."
    - statement: "The Elixir 1.15 / OTP 26 min lane compiles with --warnings-as-errors after the @compile deletion (verified locally only on 1.17.3; the verify-test (min) CI lane is the proof) (D-19)."
      verification: backstop
  artifacts:
    - test/threadline/no_warn_undefined_contract_test.exs
    - mix.exs
    - .github/workflows/ci.yml
  key_links:
    - "`mix ci.all` → `verify.xref_cycles` → `mix xref graph ... --fail-above 0`. The CI `verify-test` job runs the same alias after `Compile (warnings as errors)`."
    - "`Threadline.Query` preload of `:action` → `AuditTransaction.belongs_to :action`. This is load-bearing for investigation and the transaction/timeline LiveViews, and it must keep working after the suppression is deleted."
  prohibitions:
    - "The suppression is deleted, never moved into `mix.exs` `elixirc_options`, a config file, or another module."
    - "The `:action` association is not removed or altered to make a cycle count drop, because that would break public API (D-18)."
    - "No CI job key is renamed and no existing step is reordered."
---

<objective>
Delete the Capture↔Semantics compiler suppression and pin, with two independent gates, that it stays gone and that no compile-connected cycle appears.

Purpose: GATE-04. The `@compile {:no_warn_undefined, ...}` arrived alongside AuditAction and suppresses nothing, which research proved with a scratch compile that emitted 0 warnings. Removing it without a pin would let it quietly return in `elixirc_options`.

Output: a one-line lib deletion, `test/threadline/no_warn_undefined_contract_test.exs`, the `verify.xref_cycles` alias in `ci.all`, and a new step in the `verify-test` job.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-real-gates/203-CONTEXT.md
@.planning/phases/203-real-gates/203-RESEARCH.md
@.planning/phases/203-real-gates/203-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces (Plan 02)

| Symbol | Kind | Path |
|---|---|---|
| `Threadline.NoWarnUndefinedContractTest` | NEW contract test | `test/threadline/no_warn_undefined_contract_test.exs` |
| `"verify.xref_cycles"` | NEW mix alias | `mix.exs` |
| "Verify no compile-connected xref cycles" | NEW CI step inside the existing `verify-test` job | `.github/workflows/ci.yml` |

## Flagged assumptions (spec-less probe, GATE-04 `unclassified`: unresolved, surfaced rather than dropped)

- **A-GATE-04:** `mix xref graph --format cycles --label compile-connected --fail-above 0` behaves the same on Elixir 1.15.x as on 1.17.3. The flags are documented in the 1.15.0 docs, but they are not executed locally, because 1.15 is not installed. The CI min lane is the first real proof. If it goes red there, record the failure and halt. Do not add a lane guard to hide it.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the no_warn_undefined suppression and pin its absence</name>
  <files>lib/threadline/capture/audit_transaction.ex, test/threadline/no_warn_undefined_contract_test.exs</files>
  <read_first>
    - lib/threadline/capture/audit_transaction.ex (lines 50-69: the `@compile` line and the `belongs_to :action` it sits above)
    - lib/threadline/semantics/audit_action.ex (near line 47, `has_many :transactions`, which stays)
    - test/threadline/zero_skips_contract_test.exs (source-scan template and self-match avoidance at lines 26-39)
    - test/threadline/dialyzer_ignore_contract_test.exs (lines 60-75: `Threadline.MixProject.project()` introspection)
    - mix.exs `project/0` (confirm there is no `elixirc_options` key today)
  </read_first>
  <behavior>
    - Test "lib has no no_warn_undefined": the offenders are the files in `Path.wildcard("lib/**/*.ex")` whose source contains the needle. The assertion is `offenders == []`, and it also asserts that the scanned set is non-empty.
    - Test "mix project does not relocate it": `Keyword.get(Threadline.MixProject.project()[:elixirc_options] || [], :no_warn_undefined)` is `nil`. So is `Mix.Project.config()[:elixirc_options]`.
    - RED: before the lib deletion, the first test fails and names `lib/threadline/capture/audit_transaction.ex`.
  </behavior>
  <action>
    Per D-17 and D-19(b):

    (0) Before any edit, record the plan base outside the work tree so later fail-closed criteria can use it: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-203-02-base"`. Also copy the SHA into the SUMMARY. The file lives in the git dir, so it is never staged.

    (a) Create `test/threadline/no_warn_undefined_contract_test.exs` as `Threadline.NoWarnUndefinedContractTest` (`async: true`). Its prose `@moduledoc` explains that GATE-04 requires the Capture↔Semantics suppression to be removed, not relocated, and that both the lib tree and the project compiler options are scanned for that reason. Assemble the needle at runtime by joining the fragments "no_warn_" and "undefined", following the zero_skips pattern. The test file is not in the lib glob, but the runtime assembly keeps a literal out of this file for grep-based audits. Include the non-empty guard and the planning-independence self-guard. Run it and confirm RED naming audit_transaction.ex.

    (b) Delete exactly the one `@compile` line in `lib/threadline/capture/audit_transaction.ex`. Keep the `belongs_to(:action, Threadline.Semantics.AuditAction)` line and everything else byte-identical. Run `mix compile --force --warnings-as-errors` and confirm 0 warnings.

    (c) Commit the lib file alone as `refactor(203-02): delete no_warn_undefined papering in AuditTransaction`. The body states: "GATE-04 (D-17): the suppression covered nothing — scratch and real compile with --warnings-as-errors emit 0 warnings. belongs_to :action kept (public API, D-18). No behavior change." Then commit the contract test alone as `refactor(203-02): add no_warn_undefined contract test (GATE-04)`.
  </action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors</automated>
    <fails_when>non-zero exit or any `warning:` line mentioning `Threadline.Semantics.AuditAction` or an undefined module</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/no_warn_undefined_contract_test.exs test/threadline/investigation_test.exs test/threadline/audit_transaction_test.exs test/threadline/query_test.exs</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`. An investigation failure means the `:action` preload broke.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "no_warn_undefined" lib mix.exs config` prints nothing
    - `grep -c "belongs_to(:action, Threadline.Semantics.AuditAction)" lib/threadline/capture/audit_transaction.ex` prints `1`
    - `git show --stat <deletion-sha>` (SHA recorded in SUMMARY) shows exactly `lib/threadline/capture/audit_transaction.ex`, 1 deletion
    - `git show --stat <contract-test-sha>` shows exactly the new contract test file
  </acceptance_criteria>
  <done>The suppression is gone. A committed contract test would fail if it reappeared in lib or in the compiler options. Preload behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the compile-connected cycle gate into ci.all and the verify-test job</name>
  <files>mix.exs, .github/workflows/ci.yml, test/threadline/ci_topology_contract_test.exs, CONTRIBUTING.md, guides/configuration-and-commands.md, guides/evaluating-threadline.md, guides/adoption-pilot-backlog.md</files>
  <read_first>
    - mix.exs `aliases/0` (the `"verify.compile_no_optional"` line and the `"ci.all"` list)
    - .github/workflows/ci.yml `verify-test:` job (the `Compile (warnings as errors)` and `Run tests` steps near lines 348-352)
    - test/threadline/ci_topology_contract_test.exs (the "mix aliases expose the named support-lane proof entrypoints" test and the "ci.all keeps capture-only and phoenix-surface proof steps in order" `:binary.match` block)
    - test/threadline/ci_workflow_parity_contract_test.exs (lines 1-60: stable job keys and ci.all ordering, which must stay green)
    - CONTRIBUTING.md (the "CI parity and `act`" job table row for `verify-test`)
    - guides/configuration-and-commands.md (the verify-alias table near lines 120-131)
    - guides/evaluating-threadline.md (the "Full contributor gate" line that says "The chain runs:", near line 57) and guides/adoption-pilot-backlog.md (the "Evidence pass" paragraph near line 5 and the "Full maintainer CI chain" row near line 133 that says "Same eight steps"); locate by grep for `verify.compile_no_optional`
    - test/threadline/adoption_pilot_doc_contract_test.exs (the "cites canonical verify entrypoints (PILOT-01)" test: membership-only `String.contains?` over step names, so inserting a step keeps it green) and test/threadline/evaluating_threadline_doc_contract_test.exs (membership asserts only)
  </read_first>
  <action>
    Per D-19(a):

    (a) In `mix.exs` `aliases/0`, add `"verify.xref_cycles": ["xref graph --format cycles --label compile-connected --fail-above 0"]` directly after the `"verify.compile_no_optional"` entry. Put a one-line why-comment above it in the style of its neighbours, stating that GATE-04 is pinned to zero compile-connected cycles and that the runtime association edge is allowed. In the `"ci.all"` list, insert `"verify.xref_cycles",` immediately after `"compile --warnings-as-errors",` and before `"verify.compile_no_optional",`.

    (b) In `.github/workflows/ci.yml`, inside the existing `verify-test:` job, insert a step between `Compile (warnings as errors)` and `Run tests`: name `Verify no compile-connected xref cycles`, run `mix verify.xref_cycles`, with no `if:` lane guard, so both the min (1.15) and current lanes run it. Change no job key and no other step.

    (c) In `test/threadline/ci_topology_contract_test.exs`, extend the support-lane alias test with two `String.contains?` assertions: one for `"verify.xref_cycles":` and one for the exact command string. Extend the ordering test with a `pos_xref_cycles` match and assert `pos_compile_strict < pos_xref_cycles` and `pos_xref_cycles < pos_compile_no_optional`.

    (d) Documentation parity. In `guides/configuration-and-commands.md`, add a table row for `mix verify.xref_cycles` ("Fail if any compile-connected cycle exists between modules."). In `CONTRIBUTING.md`, update the `verify-test` row to "compile `--warnings-as-errors` + `mix verify.xref_cycles` + `mix verify.test` (Postgres service)". Then update every guide that enumerates the `ci.all` chain: in `guides/evaluating-threadline.md` ("The chain runs: ...") and in both enumerations in `guides/adoption-pilot-backlog.md` (the "Evidence pass" paragraph and the "Full maintainer CI chain" row), insert `` → `mix verify.xref_cycles` `` immediately after `` `mix compile --warnings-as-errors` ``, and change the row's "Same eight steps" to "Same nine steps". Insert only; do not otherwise rewrite those enumerations (their pins are membership-only, and re-listing the later dialyzer/critic/browser steps is outside this plan). Then run the doc contract suite. If any doc contract test pins the exact old wording of a changed line, update the doc so the test's intent holds. Do not weaken the test.

    (e) Commits (D-28, one file per commit where contract tests are involved): mix.exs alone; ci.yml alone; ci_topology_contract_test.exs alone; the four docs together as `docs(203-02): document verify.xref_cycles`. Everything else uses the `refactor(203-02): ...` type.
  </action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.xref_cycles</automated>
    <fails_when>non-zero exit, or output listing any cycle under the compile-connected label</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/ci_topology_contract_test.exs test/threadline/ci_workflow_parity_contract_test.exs test/threadline/optional_deps_contract_test.exs</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`, for example "expected mix.exs to declare a multiline ci.all list" after a malformed insertion</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.doc_contract</automated>
    <fails_when>non-zero exit or a summary other than `0 failures` after the CONTRIBUTING and guide edits</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format</automated>
    <fails_when>non-zero exit: the new contract test, mix.exs, or the topology test is not `mix format`-clean. Format it and fold the fix into the commit that introduced the file (or a `style(203-02)` commit if that commit is already made) before closing the plan.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '"verify.xref_cycles": \["xref graph --format cycles --label compile-connected --fail-above 0"\]' mix.exs` prints `1`
    - In the `ci.all` block, the line numbers satisfy `"compile --warnings-as-errors"` < `"verify.xref_cycles"` < `"verify.compile_no_optional"` (check with `grep -n`)
    - `grep -n "run: mix verify.xref_cycles" .github/workflows/ci.yml` prints exactly one line, located between the `verify-test:` job header and that job's `Run tests` step
    - `BASE=$(cat "$(git rev-parse --git-dir)/gsd-203-02-base") && git rev-parse --verify --quiet "$BASE^{commit}" >/dev/null && D=$(git diff "$BASE" HEAD -- .github/workflows/ci.yml) && test -n "$D" && ! printf '%s\n' "$D" | grep -qE '^[-+]  [a-z-]+:$'` exits 0 (fails closed: a missing or invalid base, or a failing diff command, makes the `&&` chain exit non-zero before the grep; `test -n` proves the diff saw this plan's step insertion; a job key added, removed, or renamed also exits non-zero)
    - `grep -c "verify.xref_cycles" guides/evaluating-threadline.md` prints at least `1`, and `grep -c "verify.xref_cycles" guides/adoption-pilot-backlog.md` prints at least `2`; `grep -c "Same eight steps" guides/adoption-pilot-backlog.md` prints `0`
    - `grep -c "verify.xref_cycles" guides/configuration-and-commands.md CONTRIBUTING.md` reports at least 1 in each file
  </acceptance_criteria>
  <done>The cycle gate is a named, canonical entrypoint. It runs locally in `ci.all` and in both CI test lanes, and its presence and position are test-pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor change → CI gate set | A change can weaken gates by relocating suppressions or dropping steps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-05 | Tampering (of the gate) | compiler suppression | medium | mitigate | no_warn_undefined contract scans lib and `elixirc_options`, so relocation fails the suite |
| T-203-06 | Tampering (of the gate) | `verify.xref_cycles` wiring | medium | mitigate | ci_topology contract pins alias text and ci.all position; CI step has no lane guard |
| T-203-07 | Denial of service | public `:action` preload | medium | mitigate | association retained (D-18); investigation tests in verify |
</threat_model>

<verification>
- `mix compile --force --warnings-as-errors` is clean, `mix verify.xref_cycles` exits 0, and both new or extended contract tests are green.
- Credo full-default count is unchanged at 484 (`... --config-file deps/credo/.credo.exs --format json | jq '.issues | length'`). Input note: the 484 also counts findings in the new contract test file this plan creates, so that file must be Credo-clean under `deps/credo/.credo.exs --strict` (0 findings of any check) before the count is read; fix the test file rather than accept a shifted total.
- `mix verify.format` exits 0.
</verification>

<success_criteria>
GATE-04 holds: there are 0 compile-connected cycles and 0 `no_warn_undefined`. Each fact is pinned by an executable gate that runs in CI on both lanes.
</success_criteria>

<output>
Create `.planning/phases/203-real-gates/203-02-SUMMARY.md` when done. Record the `mix xref graph --format cycles` runtime-cycle list, informational only, so that D-18's accepted runtime edge is documented.
</output>
