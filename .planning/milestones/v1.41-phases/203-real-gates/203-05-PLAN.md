---
phase: 203-real-gates
plan: 05
type: execute
wave: 5
depends_on: ["203-04"]
files_modified:
  - test/threadline/operator_surface/live/actor_live_test.exs
  - test/threadline/operator_surface/live/coverage_live_test.exs
  - test/threadline/operator_surface/live/evidence_live_test.exs
  - test/threadline/operator_surface/live/export_status_live_test.exs
  - test/threadline/operator_surface/live/policy_redaction_live_test.exs
  - test/threadline/operator_surface/live/retention_history_live_test.exs
  - test/threadline/operator_surface/live/row_history_live_test.exs
  - test/threadline/operator_surface/live/start_live_test.exs
  - test/threadline/operator_surface/live/timeline_live_test.exs
  - test/threadline/operator_surface/breadcrumb_test.exs
  - test/threadline/operator_surface/controllers/export_controller_test.exs
  - test/threadline/operator_surface/coverage_mix_test.exs
  - test/threadline/operator_surface/critic_trust_test.exs
  - test/threadline/operator_surface/exports_mix_parity_test.exs
  - test/threadline/operator_surface/policy_show_mix_test.exs
  - test/threadline/operator_surface/row_history_component_test.exs
  - test/threadline/operator_surface/skip_link_test.exs
  - test/threadline/operator_surface/stress_router_test.exs
  - test/threadline/operator_surface/transaction_live_test.exs
  - test/threadline/operator_surface/ui_test.exs
autonomous: true
requirements: [GATE-01, GATE-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Under Credo's full defaults (`--config-file deps/credo/.credo.exs --strict`), `Credo.Check.Design.AliasUsage` reports 0 findings anywhere in `lib/` and `test/`. All 356 are fixed across Plans 03-05 with no threshold delta and no path scoping (D-01, D-03)."
    - "No new `Readability.AliasOrder` finding exists: every remaining AliasOrder site is in a pre-existing file owned by Plan 06 (lib) or Plan 07 (test) (D-10)."
    - "Short-name collisions use `as:` (for example a test-local `StartLiveTest.Auth` against `Threadline.OperatorSurface.Auth`), and `mix compile --warnings-as-errors` stays clean (D-10)."
    - "Commits follow D-28: non-contract test files batched by directory, type `refactor`, pure mechanical, blame-ignorable."
    - "The full test suite passes after the whole alias sweep; aliasing changes no behavior."
  artifacts:
    - test/threadline/operator_surface/live/start_live_test.exs
    - test/threadline/operator_surface/live/timeline_live_test.exs
  key_links:
    - "Aliases are lexically scoped. In multi-module test files (such as start_live_test.exs with its Auth, Router, Endpoint and Live modules), each module body gets its own alias block. A missing one shows up as a compile error, not a silent change."
  prohibitions:
    - "AliasUsage is never disabled, re-parameterized, or path-scoped (D-03)."
    - "No semantic edit rides in an alias commit."
    - "No `# credo:disable` or `# credo:enable` comment is added."
---

<objective>
Finish the `Design.AliasUsage` backlog in the non-contract operator_surface tests (third of three alias plans), then prove the whole sweep complete.

Purpose: GATE-01/GATE-02 via D-01 and D-03. After this plan AliasUsage is 0 across the tree.

Output: 0 AliasUsage findings in these 20 files (187 findings on 2026-09-22), committed per directory, plus a full-suite run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-real-gates/203-CONTEXT.md
@.planning/phases/203-real-gates/203-03-PLAN.md
@CLAUDE.md
</context>

## Procedure

Follow the **Shared procedure (Plans 03, 04, 05)** section of `203-03-PLAN.md` exactly: measurement command, read scope (alias blocks and flagged lines only), per-pair aliasing rules, per-file compile and test, AliasOrder handling, and the D-28 commit template with `203-05` in place of `203-NN`. The base file for this plan is `"$(git rev-parse --git-dir)/gsd-203-05-base"`.

<tasks>

<task type="auto">
  <name>Task 1: Alias sweep in test/threadline/operator_surface/live/</name>
  <files>test/threadline/operator_surface/live/actor_live_test.exs, test/threadline/operator_surface/live/coverage_live_test.exs, test/threadline/operator_surface/live/evidence_live_test.exs, test/threadline/operator_surface/live/export_status_live_test.exs, test/threadline/operator_surface/live/policy_redaction_live_test.exs, test/threadline/operator_surface/live/retention_history_live_test.exs, test/threadline/operator_surface/live/row_history_live_test.exs, test/threadline/operator_surface/live/start_live_test.exs, test/threadline/operator_surface/live/timeline_live_test.exs</files>
  <read_first>
    - per the shared read scope: each file's alias block(s) and each flagged line with context (timeline_live_test.exs is about 1,900 lines: never read it whole)
    - test/threadline/operator_surface/live/start_live_test.exs: every `defmodule` line (grep), each module's alias block, and each flagged line. It is a multi-module file (Auth/Router/Endpoint/Live) with 20 findings and is the collision case D-10 names.
  </read_first>
  <action>
    Per D-03, D-10, and D-28:

    (0) Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-203-05-base"`, and copy the SHA into the SUMMARY.

    (a) Apply the shared procedure to the nine files. Because they share one directory, D-28 batches them into one commit: `refactor(203-05): alias repeated module references in test/threadline/operator_surface/live/`. Compile and run each file's tests as you go; commit once the directory is done.

    (b) In start_live_test.exs, alias the test-local Auth module with `as:`, for example `alias Threadline.OperatorSurface.StartLiveTest.Auth, as: StartAuth`, wherever `Auth` would collide with `Threadline.OperatorSurface.Auth`. Record the chosen name in the SUMMARY.
  </action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit or any `warning:` line</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`</fails_when>
  </verify>
  <acceptance_criteria>
    - `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix compile >/dev/null 2>&1; mix credo --strict --config-file deps/credo/.credo.exs --format json' | jq -e '[.issues[] | select(.check=="Credo.Check.Design.AliasUsage" and (.filename | startswith("test/threadline/operator_surface/live/")))] | length == 0'` prints `true`
  </acceptance_criteria>
  <done>AliasUsage is 0 in the operator_surface LiveView tests.</done>
</task>

<task type="auto">
  <name>Task 2: Alias sweep in the remaining operator_surface tests, then prove the sweep complete</name>
  <files>test/threadline/operator_surface/breadcrumb_test.exs, test/threadline/operator_surface/controllers/export_controller_test.exs, test/threadline/operator_surface/coverage_mix_test.exs, test/threadline/operator_surface/critic_trust_test.exs, test/threadline/operator_surface/exports_mix_parity_test.exs, test/threadline/operator_surface/policy_show_mix_test.exs, test/threadline/operator_surface/row_history_component_test.exs, test/threadline/operator_surface/skip_link_test.exs, test/threadline/operator_surface/stress_router_test.exs, test/threadline/operator_surface/transaction_live_test.exs, test/threadline/operator_surface/ui_test.exs</files>
  <read_first>
    - per the shared read scope: each file's alias block and each flagged line with context (ui_test.exs and critic_trust_test.exs are about 1,300 lines each: never read them whole)
  </read_first>
  <action>
    Per D-03, D-10, and D-28, apply the shared procedure. Commit in two directory batches: `test/threadline/operator_surface/` (top level) and `test/threadline/operator_surface/controllers/`.

    After the last batch, run the full measurement and the whole suite. Pre-existing AliasOrder sites in `test/support/data_case.ex`, `test/support/storage_schema_case.ex`, `test/threadline/export_queue/task_adapter_test.exs`, `test/threadline/storage_schema_integration_test.exs`, and (if Plan 03 did not absorb it) `copy_contract_test.exs` stay for Plan 07; the lib ones stay for Plan 06.
  </action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix compile >/dev/null 2>&1; mix credo --strict --config-file deps/credo/.credo.exs --format json' | jq -e '([.issues[] | select(.check=="Credo.Check.Design.AliasUsage")] | length == 0) and ([.issues[] | select(.check=="Credo.Check.Readability.AliasOrder") | .filename] - ["lib/threadline.ex","lib/threadline/operator_surface/live/coverage_live.ex","lib/threadline/operator_surface/live/export_status_live.ex","lib/threadline/operator_surface/live/retention_history_live.ex","lib/threadline/operator_surface/live/start_live.ex","test/support/data_case.ex","test/support/storage_schema_case.ex","test/threadline/export_queue/task_adapter_test.exs","test/threadline/operator_surface/copy_contract_test.exs","test/threadline/storage_schema_integration_test.exs"] == [])'</automated>
    <fails_when>jq exits 1 because any AliasUsage finding remains anywhere, or an AliasOrder finding appears in a file outside the pre-existing set (an inserted alias created a new ordering finding)</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format</automated>
    <fails_when>non-zero exit: an edited file is not `mix format`-clean; format it and fold the fix into that batch's commit (or a `style(203-05)` commit if already made)</fails_when>
  </verify>
  <acceptance_criteria>
    - All verify commands succeed
    - `BASE=$(cat "$(git rev-parse --git-dir)/gsd-203-05-base") && git rev-parse --verify --quiet "$BASE^{commit}" >/dev/null && L=$(git log --format=%s "$BASE"..HEAD) && printf '%s\n' "$L" | grep -c '^refactor(203-05): alias repeated module references in '` prints `3` (live/, top-level operator_surface/, controllers/); a missing or invalid base exits non-zero before counting
    - `grep -rnE "credo:(disable|enable)" lib test --include='*.ex' --include='*.exs'` prints nothing (no disables added)
  </acceptance_criteria>
  <done>AliasUsage is 0 across the tree and no new AliasOrder exists. The suite is green, and every commit is pure and blame-ignorable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mechanical edit → test behavior | A mis-scoped alias in a multi-module test file can change which module a test exercises. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-08c | Tampering (of test intent) | start_live_test.exs collision aliasing | medium | mitigate | `as:` for colliding short names; `--warnings-as-errors` compile; LiveView tests run before commit. |
| T-203-09c | Tampering (of the gate) | AliasUsage check | medium | mitigate | No delta or disable; the tree-wide jq gate measures 0 against upstream defaults. |
</threat_model>

<verification>
- Under full defaults, AliasUsage is 0 tree-wide, and AliasOrder appears only in pre-existing files. The total drops from 484 to at most 128.
- `mix test` is green, `mix compile --force --warnings-as-errors` is clean, and `mix verify.format` exits 0.
</verification>

<success_criteria>
All 356 AliasUsage findings are fixed (Plans 03-05) as pure `refactor` commits with no config change and no suppression.
</success_criteria>

<output>
Create `.planning/phases/203-real-gates/203-05-SUMMARY.md` when done. Record the plan base SHA, the before and after per-check histogram for the whole tree, the commit SHA list (a later `.git-blame-ignore-revs` candidate set that is not committed here), every `as:` name chosen, and the AliasOrder sites that remain for Plans 06/07.
</output>
