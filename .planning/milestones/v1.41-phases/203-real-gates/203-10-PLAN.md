---
phase: 203-real-gates
plan: 10
type: execute
wave: 10
depends_on: ["203-09"]
files_modified:
  - .credo.exs
  - test/threadline/credo_config_contract_test.exs
autonomous: true
requirements: [GATE-01, GATE-02, GATE-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.credo.exs` is `deps/credo/.credo.exs` scaffolding copied verbatim (top of file through the `checks:` key, comments kept). The only scaffolding change is `strict: true`, and the checks are `checks: %{extra: [...], disabled: []}`. No `enabled:` key appears anywhere (GATE-01; D-02, D-22, D-25)."
    - "The header comment reads `# Scaffolding copied from credo 1.7.18 deps/credo/.credo.exs; checks are deltas over Credo's embedded defaults — do not add enabled:` and the cited version equals the loaded Credo version (D-22, D-26)."
    - "`extra:` is exactly three entries, in this order: `{Credo.Check.Design.TagTODO, [exit_status: 0]}`; `{Credo.Check.Readability.ModuleDoc, [ignore_names: [], ignore_modules_using: []]}`; `{Credo.Check.Warning.MissedMetadataKeyInLoggerConfig, [metadata_keys: <the keys re-verified from retention.ex>]}` (D-04, D-24)."
    - "`Design.TagFIXME` stays blocking at its upstream default; only TagTODO is advisory (D-04)."
    - "`Design.AliasUsage` has no delta, so upstream's `priority: :low, if_nested_deeper_than: 2` params stay intact (D-03, D-24)."
    - "`disabled: []` is empty, not a copy of upstream's opt-in list, and no opt-in check such as `Readability.Specs` is enabled (D-06, D-23)."
    - "`mix credo --strict` (the project config) exits 0 with 0 issues, and `mix verify.credo` (still `credo --strict`) passes in `ci.all` (GATE-01, GATE-02; D-25)."
    - "`mix credo info --strict --verbose --format json` reports 69 effective checks, the same as upstream's embedded `enabled:` length. Every `extra:` entry re-parameterizes an existing default check (GATE-01)."
    - "The contract test asserts: no `:enabled` key anywhere in the `.credo.exs` AST; `checks` keys ⊆ `[:extra, :disabled]`; `disabled == []`, with a message citing GATE-01; `extra` modules equal the pinned `@deltas` exactly; every extra module ∈ upstream `enabled:` and ∉ upstream `disabled:`, read from `deps/credo/.credo.exs` at test time; every extra param key ∈ `Mod.param_names()` ∪ the pinned Credo builtin params (D-26 interpretation); the header version equals the loaded Credo version; upstream `enabled:` count ≥ `@min_default_checks 69`; `strict: true`; the `verify.credo` alias string is pinned (D-26)."
    - "The params assertion follows the recorded D-26 interpretation: every `extra` param key must be in `Mod.param_names() ++ @credo_builtin_params`, where `@credo_builtin_params [:category, :exit_status, :files, :priority, :tags]` is pinned and asserted to be a subset of `Credo.Check.Params.builtin_param_names()`. TagTODO's `exit_status` (a builtin param; `TagTODO.param_names() == [:include_doc]`) therefore passes, and an unknown or misspelled key still fails (D-04, D-24, D-26 as annotated in CONTEXT.md)."
    - "An `extra:` entry for a check already in the defaults merges by module key and replaces that check's params rather than adding a second instance. The contract's membership assertion, backed by the 69-check info count, proves an extra never grows or duplicates the set (GATE-01 adjacency)."
    - "An empty or shrunken base cannot pass: `disabled` must equal `[]` exactly, `extra` must equal the three pinned entries, and upstream `enabled:` must hold at least 69 entries (GATE-01 empty)."
    - "The `@deltas` comparison is exact list equality including order, so a reorder or insertion shows up as a reviewed diff (GATE-01 ordering)."
    - "Every lib module has a deliberate `@moduledoc` or `@moduledoc false`, and this is enforced by Credo ModuleDoc with its ignore lists cleared: LiveViews, controllers, routers and Ecto schemas are no longer skipped (GATE-05; D-24)."
    - "Dialyzer is re-confirmed at `Total errors: 0` and `mix verify.xref_cycles` exits 0 after all Phase 203 refactors (D-00b, D-19)."
    - "The phase ran the D-21 plan shape in strict serial order (split into ten plans for context budget, which D-21 allows): inversions (01-02) before the alias sweep (03-05), then mechanical (06-07), then structural filing against re-measured lines (08-09), with the config going live last (10). This plan flips the gate only after the full-default residue was exactly the one config-owned Logger finding (D-21, D-16)."
    - "`mix verify.format` passes before Task 2 (Task 1 verify), so the no-code-change proof task never meets a formatting failure it may not fix."
    - "The full local gate `mix ci.all` passes through every step before the browser lane, and the browser lane is at its known healthy baseline of exactly 8 pre-existing screenshot failures, with no ninth and no changed names."
  artifacts:
    - .credo.exs
    - test/threadline/credo_config_contract_test.exs
  key_links:
    - "`mix verify.credo` → `credo --strict` → Credo embedded defaults (69) ⊕ `.credo.exs` `extra:` ⊕ per-site register disables (Plans 08-09) → exit 0. The CI `verify-credo` job (MIX_ENV=dev) and `ci.all` (MIX_ENV=test) evaluate the same env-independent config."
    - "`credo_config_contract_test.exs` → `deps/credo/.credo.exs` at test time. A Credo bump that changes the scaffolding or the defaults fails the header-version assertion with 're-diff the scaffolding and bump the header'."
  prohibitions:
    - "`.credo.exs` never contains an `enabled:` key and never lists a check in `disabled:`."
    - "No threshold (`max_nesting`, `max_complexity`) or check priority is changed through `extra:`."
    - "The gate is not made green by any means other than the pinned three deltas and the Plan 09 register; if `mix verify.credo` is red, halt and report."
    - "Executors do not move protected files, bypass hooks, or edit tests' intent to pass a blocking gate — they halt and report."
---

<objective>
Make the gate live. Rebuild `.credo.exs` as verbatim upstream scaffolding plus three pinned deltas, lock its shape with the config half of the contract test, then prove the whole phase with the full local gate.

Purpose: GATE-01. Today's `checks: %{enabled: [2 checks]}` replaces Credo's defaults, so the "gate" lints almost nothing. Plans 01-09 drained or registered every finding under full defaults, so switching the config on now yields exit 0 honestly.

Output: the rebuilt `.credo.exs`, the config-shape tests in `test/threadline/credo_config_contract_test.exs`, and phase-end evidence (credo 0 issues and 69 checks, dialyzer 0, xref 0, ci.all).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-real-gates/203-CONTEXT.md
@.planning/phases/203-real-gates/203-RESEARCH.md
@.planning/phases/203-real-gates/203-PATTERNS.md
@.planning/phases/203-real-gates/203-09-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces (Plan 10)

| Symbol | Kind | Path |
|---|---|---|
| Rebuilt Credo config (verbatim scaffolding + `extra:` deltas, `disabled: []`) | config | `.credo.exs` |
| `@deltas`, `@min_default_checks 69`, `@credo_builtin_params`, `describe "config shape (GATE-01)"` | contract-test additions | `test/threadline/credo_config_contract_test.exs` |

## Reversibility

Rebuilding `.credo.exs` is **reversible**: a single-file revert restores the old config, and no data or public API depends on it (D-02/D-22). No `checkpoint:decision` is warranted.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the config shape (RED), then rebuild .credo.exs from verbatim scaffolding plus three deltas (GREEN)</name>
  <files>test/threadline/credo_config_contract_test.exs, .credo.exs</files>
  <read_first>
    - .credo.exs (current 20-line file; the `enabled:` list is the defect)
    - deps/credo/.credo.exs in full (lines 1-66 are the scaffolding to copy; `strict: false` near line 49; `checks: %{enabled: [...]` from about line 67; `disabled:` near 166)
    - deps/credo/lib/credo/config_file.ex lines 370-400 (`merge_checks/2`: `extra` merges by key, `disabled` forces false)
    - deps/credo/lib/credo/check/readability/module_doc.ex lines 1-10 (default `ignore_names`/`ignore_modules_using`)
    - deps/credo/lib/credo/check/warning/missed_metadata_key_in_logger_config.ex (the `metadata_keys` param name and semantics)
    - lib/threadline/retention.ex around `Logger.info("threadline retention purge batch"` (re-verify the metadata keys; research lists deleted_changes, deleted_transactions, batch, total_changes, total_transactions)
    - test/threadline/credo_config_contract_test.exs (register half from Plan 09)
    - deps/credo/lib/credo/check/params.ex `builtin_param_names/0` (near line 80: the params every check accepts, including `exit_status`)
    - mix.exs `"verify.credo"` alias line
  </read_first>
  <behavior>
    - Shape test: `Code.string_to_quoted!(File.read!(".credo.exs"))` contains no keyword pair with key `:enabled` anywhere in the AST, checked by walking with `Macro.prewalk`.
    - Evaluated test: `{config, _} = Code.eval_file(".credo.exs")`. There is exactly one entry in `configs`. Its `strict` is `true`, `Map.keys(checks)` is a subset of `[:extra, :disabled]`, and `checks.disabled == []`, with a failure message citing GATE-01 and the silent-drop hazard. `checks.extra == @deltas`, compared exactly and in order.
    - Upstream test: evaluate `deps/credo/.credo.exs`. `length(upstream_enabled) >= @min_default_checks` (69). Every module in `@deltas` is in the upstream enabled modules and not in the upstream disabled modules.
    - Params test (D-26 as interpreted in CONTEXT.md): for every `{mod, params}` in `@deltas`, `Code.ensure_loaded!(mod)` succeeds and `Keyword.keys(params) -- (mod.param_names() ++ @credo_builtin_params) == []`. A second assertion checks `@credo_builtin_params -- Credo.Check.Params.builtin_param_names() == []`, so a Credo release that drops a builtin fails loudly instead of widening the allowance. A synthetic negative (`{Credo.Check.Design.TagTODO, [exit_statuss: 0]}` passed to the same private validator) must fail, proving unknown keys are still rejected. Measured 2026-09-22: `TagTODO.param_names() == [:include_doc]`, `ModuleDoc.param_names() == [:ignore_names, :ignore_modules_using]`, `MissedMetadataKeyInLoggerConfig.param_names() == [:metadata_keys]`.
    - Version test: the header comment's cited version (captured by regex from the first lines of `.credo.exs`) equals `to_string(Application.spec(:credo, :vsn))`, after calling `Application.load(:credo)` and tolerating an already-loaded result. On mismatch, the message says "re-diff the scaffolding and bump the header".
    - Alias test: `mix.exs` contains the literal `"verify.credo": ["credo --strict"]`.
    - RED: against the current `.credo.exs`, the shape, evaluated, and version tests fail.
  </behavior>
  <action>
    Per D-02, D-03, D-04, D-05, D-06, D-22, D-23, D-24, D-25, and D-26:

    (a) Add a `describe "config shape (GATE-01)"` block to the existing contract test with the behavior tests. Set the module attribute `@deltas` to the three entries, in order: TagTODO `[exit_status: 0]`; ModuleDoc `[ignore_names: [], ignore_modules_using: []]`; MissedMetadataKeyInLoggerConfig `[metadata_keys: [...]]`, with the keys re-verified from retention.ex. Also set `@min_default_checks 69` and `@credo_builtin_params [:category, :exit_status, :files, :priority, :tags]`. D-26's literal "every extra param key ∈ `Mod.param_names()`" cannot pass together with D-04's `exit_status: 0`, because `exit_status` is a Credo builtin param that no check lists in `param_names/0`; the recorded interpretation (CONTEXT.md, note under D-26) accepts builtin params, pinned, and still rejects unknown keys. Put a one-line comment above `@credo_builtin_params` citing that interpretation. Run the test and confirm RED.

    (b) Rebuild `.credo.exs`:
    - Start with the header comment line exactly as the must_haves truth states, citing the version you read from `deps/credo/mix.exs` (1.7.18 expected).
    - Copy `deps/credo/.credo.exs` from its first line through the line before `checks: %{`, verbatim, comments included.
    - Change only `strict: false` to `strict: true`, and add a one-line comment explaining why: bare `mix credo` equals the CI gate, and AliasUsage's low priority is otherwise hidden (D-25).
    - Write `checks: %{extra: [...], disabled: []}`. Give each extra entry a one-line why-comment: TagTODO advisory while FIXME stays blocking; ModuleDoc ignore lists cleared because LiveViews and controllers appear in hexdocs (GATE-05); Logger metadata keys declared here and not in host config (D-09), env-independent.
    - Close the structure as upstream does.
    - Do not copy upstream's `enabled:` or `disabled:` lists.

    (c) Run `mix credo --strict`. It must report 0 issues and exit 0. If anything is reported, do not add a delta or disable to hide it. A structural finding means a Plan 08/09 register gap. A ModuleDoc finding means a module needs a deliberate `@moduledoc` or `@moduledoc false`: add it in a separate `refactor(203-10)` commit for that file and record it. Anything else: halt and report.

    (d) Commits: `.credo.exs` alone as `refactor(203-10): rebuild .credo.exs as upstream scaffolding plus deltas (GATE-01)`, with a body listing the three deltas and why `disabled: []`. Then the contract test alone as `refactor(203-10): pin Credo config shape in contract test (GATE-01)`.
  </action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.credo</automated>
    <fails_when>non-zero exit, or any issue line in the output; the summary must show 0 issues</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix compile >/dev/null 2>&1; mix credo info --strict --verbose --format json' | jq -e '.config.checks | length == 69'</automated>
    <fails_when>jq exits 1: the effective check count is not 69 (an extra added or duplicated a check, or the defaults were replaced)</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/credo_config_contract_test.exs test/threadline/ci_workflow_parity_contract_test.exs test/threadline/removed_artifact_contract_test.exs</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`; a params-test failure naming `exit_status` means the builtin-param allowance from the D-26 interpretation is missing</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format</automated>
    <fails_when>non-zero exit: `.credo.exs` or the contract test is not `mix format`-clean. Format it and fold the fix into that file's commit (or a `style(203-10)` commit if already made) now, because Task 2 forbids code changes</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "enabled:" .credo.exs` prints `0`, where the only allowed matches would be in upstream comment text. If upstream comments contain the word, use `grep -v '^\s*#' .credo.exs | grep -c "enabled:"` → `0`.
    - `grep -v '^\s*#' .credo.exs | grep -c "disabled: \[\]"` prints `1`
    - `grep -c "Scaffolding copied from credo 1.7.18 deps/credo/.credo.exs" .credo.exs` prints `1`
    - `grep -c "TagFIXME" .credo.exs` prints `0` (not re-parameterized)
    - `grep -c "AliasUsage" .credo.exs` prints `0` (no delta)
    - `mix verify.credo` exits 0 and `mix credo info` reports 69 checks
  </acceptance_criteria>
  <done>The gate is live with teeth. It runs the full default set as deltas, reports 0 issues, and has a config shape future edits cannot quietly regress.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-end proof — full local gate, dialyzer, xref, and the deferred follow-up record</name>
  <files>.credo.exs</files>
  <read_first>
    - mix.exs `"ci.all"` list (step order; browser lane last)
    - .planning/phases/203-real-gates/203-01-SUMMARY.md through 203-09-SUMMARY.md (commit SHA lists for the deferred blame-ignore record)
  </read_first>
  <action>
    Per D-00b, D-19, and D-21 (verification only; `.credo.exs` is listed only because this task re-reads it, and it must not change here):

    (a) Make sure Postgres is up on 5433. Run `DB_PORT=5433 mix ci.all` and capture the log. Every step before the browser lane must pass: format, credo, compile, xref_cycles, compile_no_optional, test, threadline, example, doc_contract, dialyzer, critic_trust, mechanical. The browser lane's healthy result is exactly the 8 pre-existing screenshot failures (baselines from 180-04). Compare the failing test names with that known set. A ninth failure or a changed name is a real regression: halt and report. Never run Playwright directly and never regenerate baselines. If Dialyzer is red, first rule out a PLT cache miss (`MIX_ENV=dev mix dialyzer --plt`) before calling it real.

    (b) Separately record `MIX_ENV=dev mix dialyzer --no-check` (must report `Total errors: 0`) and `mix verify.xref_cycles` (exit 0).

    (c) In the SUMMARY, record: the final Credo numbers (484 at the start, 0 issues now, 69 checks, register n/m); dialyzer 0; xref 0; the ci.all log excerpt; and a **Deferred follow-up** entry. The entry: add `.git-blame-ignore-revs` entries for the pure-mechanical `refactor(203-03)` through `refactor(203-09)` commits, but only after their SHAs are final on origin/main, because a squash-merge would invalidate local SHAs. List the candidate local SHAs as provisional. Record that the Elixir 1.15 min-lane proof of the `@compile` deletion and the xref step (A-GATE-04) is pending the first CI run.

    No code change in this task. If any gate is red, halt and report instead of adjusting config or tests.
  </action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix dialyzer --no-check</automated>
    <fails_when>output lacks `Total errors: 0` or exit status non-zero</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.xref_cycles</automated>
    <fails_when>non-zero exit or any compile-connected cycle printed</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix ci.all > /tmp/p203-10-ci-all.log 2>&1; grep -q "Total errors: 0" /tmp/p203-10-ci-all.log && grep -Eq "82 passed|8 failed" /tmp/p203-10-ci-all.log</automated>
    <fails_when>exit status non-zero: Dialyzer inside ci.all did not report `Total errors: 0`, or the run stopped before the browser lane (no Playwright pass/fail summary in the log). Separately, if the browser summary shows a failure count other than the known 8, or a failing name outside the pre-existing screenshot set, that is a regression even when this grep passes. Compare names manually against the SUMMARY baseline list.</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains the lines `credo: 0 issues, 69 checks`, `dialyzer: Total errors: 0`, and `xref compile-connected cycles: 0`
    - SUMMARY has a `Deferred follow-up` section naming `.git-blame-ignore-revs` and the squash-merge SHA caveat
    - `git status --porcelain -- .credo.exs lib test mix.exs` prints nothing after this task (no code change)
  </acceptance_criteria>
  <done>Phase 203 is proven end to end locally. The deferred blame-ignore follow-up and the CI min-lane confirmation are recorded, not dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Credo release → project gate | A Credo upgrade can change defaults. The config must neither silently drop new checks nor silently keep promoted opt-ins off. |
| CI dev env ↔ local test env | The same config must mean the same thing in both. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-19 | Tampering (of the gate) | `.credo.exs` `enabled:` | high | mitigate | AST walk rejects any `:enabled` key; 69-check info count in verify |
| T-203-20 | Tampering (of the gate) | `.credo.exs` `disabled:` list | high | mitigate | `disabled == []` exact, so a promoted default cannot be held off silently. The opt-in exclusion is asserted via upstream membership instead. |
| T-203-21 | Tampering (of the gate) | Credo version drift | medium | mitigate | Header-version equals loaded-version assertion, plus the upstream enabled ≥ 69 lower bound |
| T-203-22 | Repudiation | env-dependent Logger check | medium | mitigate | `metadata_keys:` in config makes the result env-independent, so the dev CI job and test ci.all agree |
| T-203-23 | Tampering | executor routing around a red gate | high | mitigate | The prohibitions and action text require a halt-and-report; no gate relaxation is allowed |
</threat_model>

<verification>
- `mix verify.credo` exits 0 with 0 issues, `mix credo info` reports 69 checks, and the config-shape contract is green.
- Dialyzer reports `Total errors: 0`, xref reports 0 compile-connected cycles, and `ci.all` is green through the mechanical gate with the browser lane at its known baseline.
</verification>

<success_criteria>
GATE-01 is live: `mix credo --strict` runs Credo's full default set through deltas. Every finding is fixed or registered (GATE-02), and GATE-05's moduledoc half is Credo-enforced. The whole phase is verified by the canonical `mix ci.all`.
</success_criteria>

<output>
Create `.planning/phases/203-real-gates/203-10-SUMMARY.md` when done, including the Deferred follow-up section.
</output>
