---
phase: 207-gen-triggers-rerun-name-collision-storage-schema-default-doc
plan: 02
type: execute
wave: 2
depends_on: ["207-01"]
files_modified:
  - lib/threadline/mix/migration_version.ex
  - lib/threadline/mix/trigger_migration.ex
  - lib/mix/tasks/threadline.gen.triggers.ex
  - test/threadline/mix/trigger_migration_test.exs
  - test/mix/tasks/threadline/gen_triggers_test.exs
autonomous: true
requirements: [W1]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `mix threadline.gen.triggers --tables posts` into an empty migrations dir still writes `<version>_threadline_triggers_posts.exs` with `defmodule ThreadlineTriggersPosts`; `posts,org_memberships` still yields `ThreadlineTriggersPostsOrgMemberships` and `AuditLog` still yields `ThreadlineTriggersAuditLog` (first-run name and module byte-identical, D-01, D-02 parts refinement)"
    - "A second run of `--tables posts` writes a migration whose Ecto name (Integer.parse of the basename rootname → {_, \"_\" <> name}) is `threadline_triggers_posts_2` and whose module is `ThreadlineTriggersPosts2`, with a strictly greater 14-digit version; a third run yields `_3` (D-01, D-02, D-08 cases 1-2)"
    - "The name and module are derived by one function from one parts list (table suffixes plus an optional ordinal); a candidate is skipped when EITHER its name or its module already exists among the migrations in the dir (recursive **/*.exs, Ecto's discovery rule shared with MigrationVersion); no ordinal is ever parsed back out of an existing name (D-01, D-02)"
    - "Lookalikes never collide: table `posts_2` then a `posts` rerun, and `a_b` then `a,b`, all produce pairwise-distinct Ecto names and modules; a legacy `20260424080642_threadline_triggers_posts.exs` already in the dir makes the next posts run take `threadline_triggers_posts_2` (D-02, D-08 cases 3-4)"
    - "Host migration files are only read as text and regex-scanned for names, `defmodule` aliases and trigger references; they are never compiled or evaluated, and an unparseable host file does not break generation (D-02, security)"
    - "In `up`, every default-mode table's `CREATE OR REPLACE TRIGGER` is followed by the non-cascading `DROP FUNCTION IF EXISTS \"<storage_schema>\".\"threadline_capture_changes_<suffix>\"()`; per-table tables get no orphan drop and still have their `CREATE OR REPLACE FUNCTION` before the triggers (D-03, D-05)"
    - "A table is a rerun table when any existing migration file matches `threadline_audit_<suffix>` not followed by [A-Za-z0-9_]; in `down`, rerun tables get no DROP TRIGGER/DROP FUNCTION, first-run tables keep today's drops, and mixed table sets (`posts,users` then `posts,comments`) split per table (D-06, D-08 case 5)"
    - "When any table is a rerun table, the generated `down` carries a comment stating that rolling back does not restore the earlier capture policy, that capture stays on, how to remove capture, that a rolled-back rerun which had removed redaction leaves capture unredacted, and that `mix threadline.policy.show` flags the mismatch; the task prints one info line naming the rerun tables (D-06, D-07)"
    - "The gen.triggers moduledoc no longer says the migration contains bare trigger statements; it describes OR REPLACE, what a rerun writes, the first-run NOTICE from the idempotent drop, and what rolling a rerun back does, without naming hidden modules in backticks (D-04, D-12)"
    - "Every new regression case was run RED against unmodified lib code (except the first-run pin, which is green by design) and the RED output is recorded in the SUMMARY; no test runs the Ecto migrator (D-08)"
    - "Code commits use releasable `fix(gen.triggers): ...` subjects with no phase or finding IDs (D-13)"
  artifacts:
    - path: lib/threadline/mix/trigger_migration.ex
      provides: "Threadline.Mix.TriggerMigration (@moduledoc false): scan/1, resolve_name/2, rerun?/2"
      contains: "@moduledoc false"
    - path: lib/threadline/mix/migration_version.ex
      provides: "shared discovery existing/1 → [{version, name, path}] reused by next/3 and the resolver"
      contains: "def existing("
    - path: lib/mix/tasks/threadline.gen.triggers.ex
      provides: "thin assembler: resolved name/module, orphan drop in up, per-table down + rollback comment, updated moduledoc"
      contains: "TriggerMigration.resolve_name"
    - path: test/mix/tasks/threadline/gen_triggers_test.exs
      provides: "file-tier regression cases (first-run pin, rerun name/module, legacy, lookalikes, orphan drop order, per-table down)"
      contains: "defmodule Mix.Tasks.Threadline.GenTriggersTest"
    - path: test/threadline/mix/trigger_migration_test.exs
      provides: "pure unit tests for resolve_name/2, rerun?/2, scan/1"
      contains: "defmodule Threadline.Mix.TriggerMigrationTest"
  key_links:
    - from: lib/mix/tasks/threadline.gen.triggers.ex
      to: Threadline.Mix.TriggerMigration.resolve_name/2
      via: "write branch, after MigrationVersion.next(path, 1)"
      pattern: "TriggerMigration\\.resolve_name\\("
    - from: lib/threadline/mix/trigger_migration.ex
      to: Threadline.Mix.MigrationVersion.existing/1
      via: "one discovery function, Ecto's rule"
      pattern: "MigrationVersion\\.existing\\("
    - from: "migration_content up body"
      to: "TriggerSQL.drop_orphan_function_for_table/2 (plan 01)"
      via: "emitted after each default-mode table's trigger statement"
      pattern: "drop_orphan_function_for_table"
---

<objective>
Make a rerun of `mix threadline.gen.triggers` write a migration Ecto accepts (audit item W1):
a distinct name and module on collision only (D-01, D-02), the orphan-function cleanup wired into
`up` (D-05), a per-table `down` that keeps capture on for rerun tables with an explanatory comment
(D-06, D-07), and an honest moduledoc (D-04, D-12). Prove it with a new file-tier regression file
and pure unit tests, RED first (D-08).

Purpose: today two runs of `--tables posts` write two files both named `threadline_triggers_posts`
with `defmodule ThreadlineTriggersPosts`. On a database where both are pending, Ecto raises
`(Ecto.MigrationError) migrations can't be executed, migration name threadline_triggers_posts is duplicated`.
The drift guides prescribe exactly this rerun.

Output: one new hidden helper module, a shared discovery function in `MigrationVersion`, the
rewired generator, and two new test files. Depends on plan 01 for `drop_orphan_function_for_table/2`
and the OR REPLACE SQL.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-CONTEXT.md
@.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-RESEARCH.md
@.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-PATTERNS.md
@.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-01-SUMMARY.md
@lib/mix/tasks/threadline.gen.triggers.ex
@lib/threadline/mix/migration_version.ex
@test/mix/tasks/threadline/install_test.exs
@test/threadline/mix/migration_version_test.exs

<interfaces>
Existing (lib/threadline/mix/migration_version.ex, @moduledoc false):
  @spec next(Path.t(), non_neg_integer(), NaiveDateTime.t()) :: [String.t()]
  defp existing_versions(path)     # :60-70, recursive [path, "**", "*.exs"], Integer.parse(Path.basename(file, ".exs")) -> {v, "_" <> _}

New in MigrationVersion (Task 1):
  @doc false
  @spec existing(Path.t()) :: [{non_neg_integer(), String.t(), Path.t()}]
  def existing(path)               # same glob + parse, {version, "_" <> name} -> {version, name, file}
  existing_versions/1 becomes Enum.map(existing(path), &elem(&1, 0))

New module (Task 1), lib/threadline/mix/trigger_migration.ex:
  defmodule Threadline.Mix.TriggerMigration   # @moduledoc false
    @type scan :: %{names: MapSet.t(String.t()), modules: MapSet.t(String.t()), sources: [String.t()]}
    @spec scan(Path.t()) :: scan()
      # names  = the name element of MigrationVersion.existing(path)
      # sources = File.read/1 contents of those files ({:error, _} skipped)
      # modules = full aliases captured by ~r/^\s*defmodule\s+([A-Z][A-Za-z0-9_.]*)\s+do\b/m over sources
    @spec resolve_name([String.t()], scan() | %{names: MapSet.t(), modules: MapSet.t()}) :: {String.t(), String.t()}
      # ordinal 1: parts = suffixes; ordinal n>=2: parts = suffixes ++ [Integer.to_string(n)]
      # name   = "threadline_triggers_" <> Enum.join(parts, "_")
      # module = "ThreadlineTriggers" <> Enum.map_join(parts, "", &Macro.camelize/1)
      # first n where name not in names AND module not in modules
    @spec rerun?(String.t(), [String.t()]) :: boolean()      (Task 3)
      # Regex.compile!("threadline_audit_" <> Regex.escape(suffix) <> "(?![A-Za-z0-9_])") matched against any source

From plan 01 (lib/threadline/capture/trigger_sql.ex):
  TriggerSQL.create_trigger/1,2 -> "CREATE OR REPLACE TRIGGER \"threadline_audit_<suffix>\" ..."
  TriggerSQL.drop_orphan_function_for_table/1 -> "DROP FUNCTION IF EXISTS \"<schema>\".\"threadline_capture_changes_<suffix>\"()"
  TriggerSQL.drop_trigger/1, TriggerSQL.drop_function_for_table/1 (cascading), TriggerSQL.install_function_for_table/2

Generator today (lib/mix/tasks/threadline.gen.triggers.ex):
  write branch :130-145 (path "priv/repo/migrations" hardcoded — leave it, custom :priv is deferred)
  migration_content/1 :201-262 (module_name :236-237 to be replaced)
  StorageSchema.threadline_table? call at :91 is a doc-contract anchor (code_walkthrough_doc_contract_test.exs:12-14) — keep it.

Test env: config/test.exs:50 sets storage_schema "threadline"; config/test.exs:62-69 makes
`test_redaction_users` a per-table table (exclude/mask/store_changed_from). Mix.Task "app.config"
loads it, so `--tables test_redaction_users` exercises per-table mode with no flags.
Legacy fixture (tracked): priv/ci/hex_evaluator/priv/repo/migrations/20260424080642_threadline_triggers_posts.exs
  defines `HexEvaluator.Repo.Migrations.ThreadlineTriggersPosts` (full alias, so its NAME collides, not its module)
  and contains the unquoted `CREATE TRIGGER threadline_audit_posts\nAFTER ...` inside an execute string.
</interfaces>
</context>

## Recorded choices (visible, not silent)

- **Spec-less probe fallback skipped (visible skip):** no requirement IDs to probe; the D-01..D-13
  decisions are the contract (cited in `must_haves.truths`). `requirements: [W1]` names the audit item.
- **API-coverage detector:** `detected: false`. No COVERAGE.md.
- **D-02 refinement (not a reopen):** the module is built from the same parts list as the name
  (`"ThreadlineTriggers" <> Enum.map_join(parts, "", &Macro.camelize/1)`), not by camelizing the
  whole final name, because camelizing `threadline_triggers_AuditLog` gives
  `ThreadlineTriggers_AuditLog` and would break D-01's byte-identical first run. CONTEXT D-02 already
  records this refinement. Both still come from one function over one list, so they cannot diverge.
- **Helper names (Claude's discretion):** `Threadline.Mix.TriggerMigration.scan/1`, `resolve_name/2`,
  `rerun?/2`; `Threadline.Mix.MigrationVersion.existing/1`. Kept outside `Mix.Tasks.*` because
  `public_surface_contract_test.exs` classifies every `Mix.Tasks.*` module as a task.
- **Module scan by regex, never compile/eval:** host dirs hold arbitrary files. A regex false
  positive only skips to the next ordinal, which is harmless.
- **Rerun info line (discretion, RESEARCH open Q4):** one `Mix.shell().info` line naming the rerun
  tables and saying that rolling the new migration back keeps capture on.
- **Optional unit file adopted:** `test/threadline/mix/trigger_migration_test.exs` covers the
  lookahead boundary (`posts` vs `posts_archive`) and the no-eval guarantee, which the file tier
  cannot isolate.
- **Green-per-commit staging:** each task writes only the cases its own fix turns green, proves
  them RED, then implements and commits. No holding file is needed.
- **Pre-existing aliasing, out of scope:** `support.tickets` and a public `support_tickets` share the
  suffix `support_tickets` (and so the per-table function name). Detection errs toward "rerun"
  (capture stays on) and the non-cascading drop fails loudly. Note it in the SUMMARY; do not fix.

<assumption_delta_decision>
Detector: `detected: true`, signal `pluralization` on "second" (second-run test).
Noun now primary: the trigger migration FILE identity (Ecto version + resolved name + module), with
uniqueness decided by what is already in the migrations dir. The table set is no longer a unique
key for a trigger migration; one table set can own several migrations over time.
Decision: `promote`. The dir scan (existing names and modules) becomes the authority for identity,
and the table-set-derived name is demoted to the first candidate in a sequence (D-01/D-02). This is
not add-alongside: the old "one migration per table set" assumption is removed everywhere it was
load-bearing (name, module, and `down`, which now decides per table from the dir, D-06).
Invariant test (adopted, Task 1): for any sequence of runs over any table sets, the Ecto names and
the modules in the dir are pairwise distinct (the lookalike and repeated-run cases in
gen_triggers_test.exs assert exactly this over the whole dir, not just the last file).
</assumption_delta_decision>

## Guardrails for every task (executor MUST follow)

- **Execution location:** main checkout `/Users/jon/projects/threadline`, isolation `none`, never a
  worktree. If `git rev-parse --show-toplevel` is not exactly that path, HALT and report. Never
  copy/move/sync files between trees; never edit `.planning/config.json` or `.gsd/dispatch-isolation-sentinel.json`.
- **Halt clause:** if a gate, hook or verify cannot pass honestly, STOP and report the exact output.
  Do NOT move, delete, rename, skip (`@tag :skip`) or weaken any existing test or assertion
  (including `install_test.exs` and `migration_version_test.exs`), contract tests, `test/test_helper.exs`,
  `.credo.exs` or `mix.exs`. No `--no-verify`, no hook edits.
- Do NOT run `Ecto.Migrator` or `mix ecto.migrate` against any generated dir (shared test DB, no
  sandbox; `ensure_no_duplication!/1` is private, so the file tier asserts Ecto's rule directly).
- Do NOT regenerate or edit the committed example/evaluator trigger migrations under `examples/` or
  `priv/ci/`; the legacy file is only copied into a tmp dir as a fixture.
- Do NOT change the hardcoded `priv/repo/migrations` path in gen.triggers (custom `:priv` is deferred).
- Stage explicit paths only; never `git add .planning/`, `git add .`, `git add -A`. Leave the
  pre-existing untracked/modified `.tool-versions`, `.planning/WINDOWS.md`, `.planning/config.json`,
  `.planning/phases/*/.gitkeep` alone. Do not push, branch, or switch branches.
- `lib/` (including the generated-migration comment text inside the heredoc) is packaged and scanned
  by `release_artifact_contract_test.exs:7-14`: no decision IDs, finding IDs, `Phase N`, `phase207`,
  or milestone versions. Do not name `Threadline.Capture.TriggerSQL` or `Threadline.Mix.*` in
  backticks in the moduledoc (ExDoc autolink warnings fail `mix docs --warnings-as-errors`).
- Temp files under `/Users/jon/.claude/jobs/77cf1bdd/tmp`; strip ANSI before grepping test output,
  under `bash -c`. Every command uses `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27`.
- Commit messages end with `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.

<!-- planner-discipline-allow: Ecto.Migrator -->
<!-- planner-discipline-allow: Code.eval_file -->
<!-- planner-discipline-allow: Code.compile_file -->
<!-- planner-discipline-allow: CASCADE -->
<!-- planner-discipline-allow: CREATE TRIGGER -->
<!-- planner-discipline-allow: Threadline.Capture.TriggerSQL -->
<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — a second `--tables posts` run gets a distinct Ecto name and module end to end</name>
  <files>lib/threadline/mix/migration_version.ex, lib/threadline/mix/trigger_migration.ex, lib/mix/tasks/threadline.gen.triggers.ex, test/mix/tasks/threadline/gen_triggers_test.exs, test/threadline/mix/trigger_migration_test.exs</files>
  <precondition>Plan 01 is committed: `grep -c 'def drop_orphan_function_for_table' lib/threadline/capture/trigger_sql.ex` >= 1 and `git rev-parse --show-toplevel` prints /Users/jon/projects/threadline; otherwise HALT.</precondition>
  <read_first>
    - lib/mix/tasks/threadline.gen.triggers.ex (whole file; write branch :130-145, migration_content/1 :201-262)
    - lib/threadline/mix/migration_version.ex (whole file; existing_versions/1 :57-70)
    - test/mix/tasks/threadline/install_test.exs:1-90 (setup, drain_shell/1, @migrations, seed/2, assert_valid_increasing!/1 — copy, do not import)
    - test/threadline/mix/migration_version_test.exs:1-30 (pure-helper test header and tmp-dir setup)
    - deps/ecto_sql/lib/ecto/migrator.ex:660-720 (discovery rule and ensure_no_duplication!/1 messages)
    - .planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-RESEARCH.md "Pattern 1", "Pattern 2", "Pattern 3", "D-08 file tier skeleton", "Pitfall 2", "Pitfall 7"
  </read_first>
  <behavior>
    - gen_triggers_test "first run keeps today's name and module" (pin, green before and after): empty dir; `--tables posts` → exactly one file ending `_threadline_triggers_posts.exs`, ecto_name "threadline_triggers_posts", module ThreadlineTriggersPosts; separate tmp subtests (fresh tmp per test) for `posts,org_memberships` → ThreadlineTriggersPostsOrgMemberships and `AuditLog` → ThreadlineTriggersAuditLog.
    - gen_triggers_test "a rerun gets a distinct Ecto name" (RED): run `--tables posts` twice; ecto names ["threadline_triggers_posts", "threadline_triggers_posts_2"] in version order; versions pass assert_valid_increasing!/1; a third run adds "threadline_triggers_posts_3". Failure message quotes `migration name <name> is duplicated`.
    - gen_triggers_test "a rerun gets a distinct module" (RED): two runs → modules ThreadlineTriggersPosts and ThreadlineTriggersPosts2 via Code.string_to_quoted!/1 on the generated file.
    - gen_triggers_test "a legacy trigger migration already present" (RED): File.cp! the tracked legacy fixture priv/ci/hex_evaluator/priv/repo/migrations/20260424080642_threadline_triggers_posts.exs into tmp/priv/repo/migrations/ BEFORE File.cd! (the source path is repo-relative); run `--tables posts`; the new file's ecto_name is "threadline_triggers_posts_2" and module ThreadlineTriggersPosts2; all Ecto names in the dir are distinct.
    - gen_triggers_test "lookalike table names never collide" (RED): runs `posts`, `posts_2`, `posts` in one tmp → three files whose Ecto names and modules are pairwise distinct (expected names threadline_triggers_posts, threadline_triggers_posts_2, threadline_triggers_posts_3; modules ThreadlineTriggersPosts, ThreadlineTriggersPosts2, ThreadlineTriggersPosts3); in a second tmp dir (make a subdir of the test tmp), runs `a_b` then `a,b` → names threadline_triggers_a_b and threadline_triggers_a_b_2, modules ThreadlineTriggersAB and ThreadlineTriggersAB2. Assertions cover ALL files in the dir (the pairwise-distinct invariant), not only the newest.
    - trigger_migration_test resolve_name/2: free base → {"threadline_triggers_posts", "ThreadlineTriggersPosts"}; name taken → _2/Posts2; only module taken → _2; both base and _2 taken → _3; ["a", "b"] with name "threadline_triggers_a_b" taken → {"threadline_triggers_a_b_2", "ThreadlineTriggersAB2"}; ["AuditLog"] free → module "ThreadlineTriggersAuditLog".
    - trigger_migration_test scan/1: recursive (a file in a subdirectory counts); a non-migration file (no integer prefix) is ignored; captures a full alias `MyApp.Repo.Migrations.AddThings`; a file whose top level is `raise "must not run"` plus a syntax error is scanned without raising and without executing.
    - MigrationVersion.existing/1: returns {integer, name, path} triples; existing migration_version_test.exs stays green unchanged.
  </behavior>
  <action>
    Step A (RED, D-08 cases 1-2, D-01, D-02): create test/mix/tasks/threadline/gen_triggers_test.exs defining Mix.Tasks.Threadline.GenTriggersTest with a comment explaining async: false (cwd and :storage_schema app env are VM-wide), `use ExUnit.Case, async: false`, alias Mix.Tasks.Threadline.Gen.Triggers. Copy install_test.exs's setup verbatim except the tmp prefix "threadline-gen-triggers-". Add private helpers: drain_shell/1 (copy), run_triggers(tmp, args) (File.cd!(tmp, fn -> Triggers.run(args) end) then drain_shell([])), trigger_files(tmp) (recursive wildcard of "*_threadline_triggers_*.exs" under tmp/priv/repo/migrations, sorted by basename so version order holds), ecto_name(file) (Path.basename |> Path.rootname |> Integer.parse → {_, "_" <> name}), version(file), module_of(file) (Code.string_to_quoted! → {:defmodule, _, [{:__aliases__, _, parts}, _]} → Module.concat(parts)), and assert_valid_increasing!/1 copied from install_test.exs:66-90 (keep the Ecto-quoting duplicate message). Each test calls Application.delete_env(:threadline, :storage_schema) first. Add the five gen_triggers_test cases from <behavior> in `describe "rerun naming"`. Create test/threadline/mix/trigger_migration_test.exs (Threadline.Mix.TriggerMigrationTest, @moduledoc false, async: true, tmp-dir setup copied from migration_version_test.exs with prefix "threadline-trigger-migration-") with `describe "resolve_name/2"` and `describe "scan/1"` from <behavior>. Do not edit migration_version_test.exs; put the existing/1 triple assertion in trigger_migration_test.exs under `describe "MigrationVersion.existing/1"`.

    Step B (prove RED): before any lib/ edit run the quick command and save ANSI-stripped output to /Users/jon/.claude/jobs/77cf1bdd/tmp/207-02-t1-red.txt. Expected: the first-run pin passes; "a rerun gets a distinct Ecto name" fails (two files both named threadline_triggers_posts); "a rerun gets a distinct module" fails (both ThreadlineTriggersPosts); the legacy case fails (new file named threadline_triggers_posts); the lookalike case fails (duplicate names); the unit tests fail with UndefinedFunctionError / module not available. A new rerun case passing here means it is vacuous: fix the test. Record the summary in the SUMMARY.

    Step C (GREEN, D-01): in lib/threadline/mix/migration_version.ex add `@doc false` + `@spec existing(Path.t()) :: [{non_neg_integer(), String.t(), Path.t()}]` + `def existing(path)` using the same recursive glob and parse, matching {version, "_" <> name} → [{version, name, file}]; move the Ecto-discovery comment onto it; make existing_versions/1 map elem 0 of existing/1 (or inline that into next/3). Behaviour of next/3 must not change.

    Step D (GREEN, D-01, D-02): create lib/threadline/mix/trigger_migration.ex, `defmodule Threadline.Mix.TriggerMigration` with `@moduledoc false` and a plain `#` comment block (no planning IDs) explaining: Ecto names a migration by the text after the version, refuses two pending files with the same name, and a module defined twice clashes when both load; so a generated name/module pair is chosen by trying the table-derived base first and then numbered candidates until both are unused in the directory. Implement scan/1 and resolve_name/2 exactly as in <interfaces> (module attribute for the defmodule regex; File.read/1 with {:error, _} skipped; MapSet for names and modules; Stream.iterate(1, &(&1 + 1)) |> Enum.find_value/2 for the candidate loop). Never call Code.string_to_quoted, Code.compile_file, Code.eval_file or Code.require_file on host files. Add @spec for every public function and a @type for the scan map (dialyzer runs at the phase gate).

    Step E (wire, D-01/D-02): in lib/mix/tasks/threadline.gen.triggers.ex add `alias Threadline.Mix.{MigrationVersion, TriggerMigration}` (replacing the single MigrationVersion alias). In the write branch keep `[version] = MigrationVersion.next(path, 1)`, then `scan = TriggerMigration.scan(path)`, `suffixes = Enum.map(tables, &StorageSchema.host_table_suffix/1)`, `{name, module} = TriggerMigration.resolve_name(suffixes, scan)`, `file = Path.join(path, "#{version}_#{name}.exs")`, and call migration_content(table_specs, module). Change migration_content/1 to migration_content/2 taking the module string; delete the old module_name derivation at :234-237. Keep the existing comment above MigrationVersion.next and the StorageSchema.threadline_table? call at :91. Leave up/down bodies unchanged in this task.

    Commit with explicit paths only: `git add lib/threadline/mix/migration_version.ex lib/threadline/mix/trigger_migration.ex lib/mix/tasks/threadline.gen.triggers.ex test/mix/tasks/threadline/gen_triggers_test.exs test/threadline/mix/trigger_migration_test.exs`; subject `fix(gen.triggers): give a rerun migration a distinct name and module` (D-13); body quotes the Ecto duplicate-name error and says a first run keeps today's name; Co-Authored-By trailer.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/mix/tasks/threadline/gen_triggers_test.exs test/threadline/mix/ test/mix/tasks/threadline/install_test.exs'</automated>
    <fails_when>non-zero exit, any failure count other than "0 failures", or an ", N excluded" suffix</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix format --check-formatted && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix credo --strict lib/threadline/mix/migration_version.ex lib/threadline/mix/trigger_migration.ex lib/mix/tasks/threadline.gen.triggers.ex test/mix/tasks/threadline/gen_triggers_test.exs test/threadline/mix/trigger_migration_test.exs'</automated>
    <fails_when>non-zero exit, a "warning:" line, unformatted files, or any credo issue</fails_when>
  </verify>
  <acceptance_criteria>
    - /Users/jon/.claude/jobs/77cf1bdd/tmp/207-02-t1-red.txt exists, captured before lib/ edits, and shows the four rerun/legacy/lookalike cases failing while "first run keeps today's name and module" passes.
    - `grep -c '20260424080642_threadline_triggers_posts.exs' test/mix/tasks/threadline/gen_triggers_test.exs` >= 1 and `grep -c '"posts_2"' test/mix/tasks/threadline/gen_triggers_test.exs` >= 1 and `grep -c '"a,b"' test/mix/tasks/threadline/gen_triggers_test.exs` >= 1.
    - `grep -c '@moduledoc false' lib/threadline/mix/trigger_migration.ex` >= 1; `grep -c 'def scan(' lib/threadline/mix/trigger_migration.ex` >= 1; `grep -c 'def resolve_name(' lib/threadline/mix/trigger_migration.ex` >= 1; `grep -c 'MigrationVersion.existing(' lib/threadline/mix/trigger_migration.ex` >= 1.
    - `grep -cE 'Code\.(string_to_quoted|compile_file|eval_file|require_file|eval_string)' lib/threadline/mix/trigger_migration.ex` = 0.
    - `grep -c 'def existing(path)' lib/threadline/mix/migration_version.ex` >= 1.
    - `grep -c 'TriggerMigration.resolve_name(' lib/mix/tasks/threadline.gen.triggers.ex` >= 1 and `grep -c 'StorageSchema.threadline_table?' lib/mix/tasks/threadline.gen.triggers.ex` >= 1 and `grep -c 'priv/repo/migrations' lib/mix/tasks/threadline.gen.triggers.ex` >= 1.
    - `grep -c 'Ecto.Migrator' test/mix/tasks/threadline/gen_triggers_test.exs` = 0.
    - `bash -c 'if ! H=$(git log --reverse --format=%H --grep="replace the audit trigger in place so a rerun migration applies"); then exit 1; fi; read -r S <<< "$H"; if [ -z "$S" ]; then exit 1; fi; test -z "$(git diff --stat "$S~1" -- test/threadline/mix/migration_version_test.exs test/mix/tasks/threadline/install_test.exs)"'` exits 0 (existing tests untouched; exits 1 if the first plan-01 commit cannot be found, so the check cannot pass vacuously).
    - `grep -nE '\bD-[0-9]{2,}\b|\bPhase\s+[0-9]+|\bphase[_-]?[0-9]+|\bv1\.4[01]\b' lib/threadline/mix/trigger_migration.ex lib/threadline/mix/migration_version.ex lib/mix/tasks/threadline.gen.triggers.ex` prints nothing.
    - Both automated commands exit 0; `git log -1 --format=%s` prints `fix(gen.triggers): give a rerun migration a distinct name and module`.
  </acceptance_criteria>
  <done>First run output name/module unchanged; the second and third runs get _2/_3 names and Posts2/Posts3 modules; RED recorded; one releasable commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orphan per-table function dropped right after each default-mode trigger in up</name>
  <files>lib/mix/tasks/threadline.gen.triggers.ex, test/mix/tasks/threadline/gen_triggers_test.exs</files>
  <read_first>
    - lib/mix/tasks/threadline.gen.triggers.ex (as left by Task 1; trigger_ups assembly)
    - test/mix/tasks/threadline/gen_triggers_test.exs (as left by Task 1)
    - priv/ci/hex_evaluator/priv/repo/migrations/20260424080642_threadline_triggers_posts.exs (legacy fixture; note its module is HexEvaluator.Repo.Migrations.ThreadlineTriggersPosts)
    - lib/threadline/capture/trigger_sql.ex (drop_orphan_function_for_table/2 from plan 01)
    - config/test.exs:50-69 (storage schema and the per-table test_redaction_users fixture)
    - .planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-RESEARCH.md "Pitfall 3", "Pattern 5"
  </read_first>
  <behavior>
    - "default-mode up drops the orphan function after the trigger" (RED): storage_schema deleted (→ public); `--tables posts`; parse the file with Code.string_to_quoted!/1 and collect the string argument of every `execute` call inside `def up` (helper executes(file, :up)); the list is exactly [create_trigger SQL for posts, "DROP FUNCTION IF EXISTS \"public\".\"threadline_capture_changes_posts\"()"] in that order; no element contains "CASCADE".
    - "per-table up keeps its function and gets no orphan drop" (RED: today's list lacks the posts orphan drop): storage_schema deleted; `--tables test_redaction_users,posts`; executes(file, :up) is [install_function_for_table SQL for test_redaction_users, per-table create_trigger for test_redaction_users, default create_trigger for posts, orphan drop for posts]; no orphan drop names threadline_capture_changes_test_redaction_users.
  </behavior>
  <action>
    Step A (RED, D-08 cases 3-4, D-05): add helper executes(file, fun_name) to gen_triggers_test.exs that walks the Code.string_to_quoted!/1 AST, finds the `def` whose head is fun_name, and returns the list of string arguments of `execute` calls in its body in source order (Macro.prewalk/3 accumulating {:execute, _, [sql]} when is_binary(sql)). Add the two cases from <behavior> in a new `describe "up body"`. Build expected SQL strings by calling Threadline.Capture.TriggerSQL functions in the test (after delete_env, so they resolve "public"), never by hand-copying SQL. Run the quick command and save ANSI-stripped output to /Users/jon/.claude/jobs/77cf1bdd/tmp/207-02-t2-red.txt. Expected RED: both up-body cases fail because the orphan drop is not emitted yet; every Task 1 case stays green. Record in the SUMMARY.

    Step B (GREEN, D-05): in migration_content/2's trigger_ups, for a table with needs_per_table false emit two lines joined by "\n\n": `    execute #{inspect(TriggerSQL.create_trigger(t))}` then `    execute #{inspect(TriggerSQL.drop_orphan_function_for_table(t))}`; per-table tables emit only their per-table trigger line. Pass no opts to the helper (the app-env storage schema is resolved at generation time, same as create_trigger/1). Keep function_ups before trigger_ups. Keep the `"    execute #{inspect(sql)}"` line format.

    Commit with explicit paths: `git add lib/mix/tasks/threadline.gen.triggers.ex test/mix/tasks/threadline/gen_triggers_test.exs`; subject `fix(gen.triggers): drop a leftover per-table capture function when a table returns to the default trigger` (D-13); Co-Authored-By trailer.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/mix/tasks/threadline/gen_triggers_test.exs test/threadline/mix/ test/mix/tasks/threadline/install_test.exs'</automated>
    <fails_when>non-zero exit or any failure count other than "0 failures"</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix format --check-formatted && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix credo --strict lib/mix/tasks/threadline.gen.triggers.ex test/mix/tasks/threadline/gen_triggers_test.exs'</automated>
    <fails_when>non-zero exit, a "warning:" line, unformatted files, or any credo issue</fails_when>
  </verify>
  <acceptance_criteria>
    - /Users/jon/.claude/jobs/77cf1bdd/tmp/207-02-t2-red.txt exists and shows the two `describe "up body"` cases failing before the lib edit.
    - `grep -c 'drop_orphan_function_for_table' lib/mix/tasks/threadline.gen.triggers.ex` >= 1.
    - `grep -c 'describe "up body"' test/mix/tasks/threadline/gen_triggers_test.exs` >= 1 and `grep -c 'defp executes(' test/mix/tasks/threadline/gen_triggers_test.exs` >= 1.
    - `bash -c 'if ! H=$(git log --reverse --format=%H --grep="replace the audit trigger in place so a rerun migration applies"); then exit 1; fi; read -r S <<< "$H"; if [ -z "$S" ]; then exit 1; fi; test -z "$(git diff --stat "$S~1" -- examples/ priv/ci/)"'` exits 0 (fixtures and example migrations untouched; exits 1 if the first plan-01 commit cannot be found, so the check cannot pass vacuously).
    - Both automated commands exit 0; `git log -1 --format=%s` starts with `fix(gen.triggers): drop a leftover per-table capture function`.
  </acceptance_criteria>
  <done>Default-mode up emits the non-cascading orphan drop right after that table's trigger; per-table tables are unchanged; one releasable commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-table rollback — rerun tables keep capture on with an explanatory comment; moduledoc tells the truth</name>
  <files>lib/threadline/mix/trigger_migration.ex, lib/mix/tasks/threadline.gen.triggers.ex, test/mix/tasks/threadline/gen_triggers_test.exs, test/threadline/mix/trigger_migration_test.exs</files>
  <read_first>
    - lib/mix/tasks/threadline.gen.triggers.ex (as left by Task 2; moduledoc :4-52, trigger_downs/function_downs, down_parts)
    - lib/threadline/mix/trigger_migration.ex (as left by Task 1)
    - test/mix/tasks/threadline/gen_triggers_test.exs and test/threadline/mix/trigger_migration_test.exs (as left by Task 2)
    - test/threadline/code_walkthrough_doc_contract_test.exs:10-20 and :72-83 (anchor to keep; hidden-module names to avoid)
    - test/threadline/public_surface_contract_test.exs (search "Gen.Triggers": must stay in the visible Mix Tasks group)
    - .planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-RESEARCH.md "Pattern 4", "Pattern 6", "Open Questions" 3-4
  </read_first>
  <behavior>
    - trigger_migration_test rerun?/2: true for a source containing `execute "CREATE OR REPLACE TRIGGER \"threadline_audit_posts\"\nAFTER ...` (escaped-quote form as written by the generator) and for the legacy form `CREATE TRIGGER threadline_audit_posts\nAFTER` (backslash-n text); false for a source mentioning only `threadline_audit_posts_archive`; false for empty sources.
    - gen_triggers_test "a first-run table keeps today's rollback": empty dir, `--tables posts` → executes(file, :down) == [TriggerSQL.drop_trigger("posts")]; `--tables test_redaction_users` in a fresh tmp → [drop_trigger, drop_function_for_table] for it; no rerun comment in the file text.
    - "a rerun table's rollback leaves capture on" (RED): two runs of `--tables posts`; the second file's executes(file, :down) == []; its raw text contains the phrases "does not restore the earlier capture policy", "Capture stays on", "continues unredacted", and "mix threadline.policy.show"; the first file is unchanged (still drops).
    - "mixed table sets split the rollback per table" (RED): `--tables posts,users` then `--tables posts,comments`; second file's down executes == [TriggerSQL.drop_trigger("comments")] (no drop for posts); the comment names posts.
    - "the task names rerun tables" (RED): the second run's drained shell output contains "already have a Threadline trigger migration" and "posts".
  </behavior>
  <action>
    Step A (RED, D-06, D-07, D-08 case 5): add the rerun?/2 unit tests in `describe "rerun?/2"` and the four gen_triggers_test cases in a new `describe "down body"`. Run the quick command and save ANSI-stripped output to /Users/jon/.claude/jobs/77cf1bdd/tmp/207-02-t3-red.txt; expected RED: rerun?/2 undefined, and the three rerun cases fail because down still drops. The first-run case passes (pin). Record in the SUMMARY.

    Step B (GREEN, D-06): add `@spec rerun?(String.t(), [String.t()]) :: boolean()` `def rerun?(suffix, sources)` to TriggerMigration per <interfaces> (Regex.escape on the suffix; negative lookahead `(?![A-Za-z0-9_])`; Enum.any?). In the generator write branch compute `rerun_tables = Enum.filter(tables, &TriggerMigration.rerun?(StorageSchema.host_table_suffix(&1), scan.sources))` from the scan taken BEFORE the new file is written, pass it to migration_content/3 (table_specs, module, rerun_tables). In migration_content/3 filter trigger_downs and function_downs to tables not in rerun_tables. When rerun_tables is non-empty, prepend to the down body a comment block (each line prefixed "    # ") in plain words, naming the rerun tables, that says: this migration replaced the audit trigger of <tables> in place; rolling it back does not restore the earlier capture policy; Capture stays on with the policy this migration installed; to stop capturing a table, write an explicit migration that drops its trigger or roll back the migration that first installed it; if this migration removed redaction rules, rolling it back leaves capture running with them removed, so changes continue unredacted until you regenerate; `mix threadline.policy.show` and the redaction drift view flag that mismatch. The block must contain the exact phrases "does not restore the earlier capture policy", "Capture stays on", "continues unredacted", "mix threadline.policy.show". If every table is a rerun table, `def down do` contains only the comment block (valid Elixir). After create_file, when rerun_tables is non-empty print one info line: "These tables already have a Threadline trigger migration: <comma list>. Rolling back the new migration keeps their capture on." No planning vocabulary in any of this text (it is packaged in lib/).

    Step C (D-04, D-12 moduledoc): rewrite the moduledoc sentence at :13-14 so it says each invocation writes one migration whose statements create or replace the audit trigger on each listed table (CREATE OR REPLACE TRIGGER, PostgreSQL 14 or later). Add a `## Rerunning` section: rerunning for tables that already have a trigger migration (for example after changing `:trigger_capture` redaction, or to clear a `Drift detected` status) writes a new migration with a numbered name such as `threadline_triggers_posts_2` that replaces the trigger in place with no capture gap; a table returning to the default trigger also drops its leftover per-table function, and on a first run that drop prints a harmless PostgreSQL NOTICE that the function does not exist; rolling back a rerun migration keeps capture on and does not restore the earlier policy, and a rolled-back rerun that had removed redaction leaves capture unredacted, which `mix threadline.policy.show` flags. The `## Rerunning` section must contain the exact phrases "replaces the trigger in place", "does not restore the earlier capture policy", "unredacted" and "mix threadline.policy.show" (the same wording as the generated down comment; plan 03 pins the guides to these phrases with a default-suite test that reads this moduledoc via Code.fetch_docs; keep each phrase unbroken on one source line, since the test matches substrings and a line wrap inside a phrase breaks the match). Do not name hidden modules in backticks; keep every other section and the Guards section intact.

    Commit with explicit paths: `git add lib/threadline/mix/trigger_migration.ex lib/mix/tasks/threadline.gen.triggers.ex test/mix/tasks/threadline/gen_triggers_test.exs test/threadline/mix/trigger_migration_test.exs`; subject `fix(gen.triggers): keep capture on when a rerun migration is rolled back` (D-13); body summarises the per-table rollback rule and the unredacted-rollback caveat; Co-Authored-By trailer.

    Step D (end-to-end file probe, no DB): run the probe command in <verify> and paste its ls output into the SUMMARY: two runs of `--tables posts` into /Users/jon/.claude/jobs/77cf1bdd/tmp/207-02-probe must list `..._threadline_triggers_posts.exs` and `..._threadline_triggers_posts_2.exs`, and `grep -h '^defmodule' ` over them must print ThreadlineTriggersPosts and ThreadlineTriggersPosts2.
  </action>
  <reversibility rating="costly">The rollback behaviour and comment are written into adopters' migration files (D-06); once shipped, a change only affects newly generated migrations.</reversibility>
  <verify>
    <automated>bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/mix/tasks/threadline/gen_triggers_test.exs test/threadline/mix/ test/mix/tasks/threadline/install_test.exs test/threadline/capture/ test/threadline/code_walkthrough_doc_contract_test.exs test/threadline/public_surface_contract_test.exs test/threadline/release_artifact_contract_test.exs'</automated>
    <fails_when>non-zero exit or any failure count other than "0 failures"</fails_when>
    <automated>bash -c 'P=/Users/jon/.claude/jobs/77cf1bdd/tmp/207-02-probe; rm -rf "$P" && mkdir -p "$P" && cd /Users/jon/projects/threadline && MIX_ENV=test DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix run --no-start -e "Application.delete_env(:threadline, :storage_schema); File.cd!(\"$P\", fn -> Mix.Tasks.Threadline.Gen.Triggers.run([\"--tables\", \"posts\"]); Mix.Tasks.Threadline.Gen.Triggers.run([\"--tables\", \"posts\"]) end)" && ls "$P/priv/repo/migrations" | grep -c "_threadline_triggers_posts_2.exs" && grep -h "^defmodule" "$P"/priv/repo/migrations/*.exs'</automated>
    <fails_when>non-zero exit, the count printed is 0 (no `_2` file), or the defmodule lines are not exactly ThreadlineTriggersPosts and ThreadlineTriggersPosts2</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix format --check-formatted && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix credo --strict lib/threadline/mix/trigger_migration.ex lib/mix/tasks/threadline.gen.triggers.ex test/mix/tasks/threadline/gen_triggers_test.exs test/threadline/mix/trigger_migration_test.exs && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix docs --warnings-as-errors'</automated>
    <fails_when>non-zero exit, a "warning:" line (compile or ExDoc autolink), unformatted files, or any credo issue</fails_when>
  </verify>
  <acceptance_criteria>
    - /Users/jon/.claude/jobs/77cf1bdd/tmp/207-02-t3-red.txt exists and shows the three rerun `describe "down body"` cases failing before the lib edit.
    - `grep -c 'def rerun?(suffix, sources)' lib/threadline/mix/trigger_migration.ex` >= 1 and `grep -c 'Regex.escape' lib/threadline/mix/trigger_migration.ex` >= 1 and `grep -cF '(?![A-Za-z0-9_])' lib/threadline/mix/trigger_migration.ex` >= 1.
    - `grep -c 'does not restore the earlier capture policy' lib/mix/tasks/threadline.gen.triggers.ex` >= 1 and `grep -c 'continues unredacted' lib/mix/tasks/threadline.gen.triggers.ex` >= 1 and `grep -c 'already have a Threadline trigger migration' lib/mix/tasks/threadline.gen.triggers.ex` >= 1.
    - `grep -c '## Rerunning' lib/mix/tasks/threadline.gen.triggers.ex` >= 1 and `grep -c 'replaces the trigger in place' lib/mix/tasks/threadline.gen.triggers.ex` >= 1 and `grep -c 'does not restore the earlier capture policy' lib/mix/tasks/threadline.gen.triggers.ex` >= 2 (down comment + moduledoc).
    - `grep -c 'CREATE OR REPLACE TRIGGER' lib/mix/tasks/threadline.gen.triggers.ex` >= 1.
    - `grep -c 'Threadline.Capture.TriggerSQL' lib/mix/tasks/threadline.gen.triggers.ex` = 0 (the alias line uses the brace form `Threadline.Capture.{`, and the moduledoc names no hidden module).
    - `grep -nE '\bD-[0-9]{2,}\b|\bPhase\s+[0-9]+|\bphase[_-]?[0-9]+|\bv1\.4[01]\b' lib/threadline/mix/trigger_migration.ex lib/mix/tasks/threadline.gen.triggers.ex` prints nothing.
    - All three automated commands exit 0; `git log -1 --format=%s` prints `fix(gen.triggers): keep capture on when a rerun migration is rolled back`; the probe output is pasted in the SUMMARY.
  </acceptance_criteria>
  <done>Rerun tables' rollback keeps capture on with a comment naming the unredacted-rollback case; first-run tables keep today's drops; mixed sets split per table; moduledoc describes OR REPLACE and reruns; docs build clean; one releasable commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host migrations directory → generator | Arbitrary host-authored files are read at generation time |
| Developer CLI (`--tables`) → generated file name, module and SQL | Table names flow into a file path, an Elixir module name and DDL |
| Generated migration `down` → production rollback | An operator's `mix ecto.rollback` decides whether audit capture continues |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-05 | Elevation of privilege | TriggerMigration.scan/1 reading host files | high | mitigate | Files are only File.read and regex-scanned; no Code.* evaluation or compilation (acceptance grep + unit test with a `raise` + syntax-error file) |
| T-207-06 | Tampering | name/module/SQL built from `--tables` | medium | mitigate | Tables already pass StorageSchema validation (`^[A-Za-z_][A-Za-z0-9_]*$`, ≤63 bytes) before host_table_suffix; SQL comes only from TriggerSQL with quote_ident; detector uses Regex.escape on the suffix |
| T-207-07 | Repudiation (audit gap) | rerun `down` | high | mitigate | Rerun tables' rollback no longer drops the trigger, so rolling back a rerun cannot silently stop capture while the original migration is still recorded as applied (tests in describe "down body") |
| T-207-08 | Information disclosure | rolled-back rerun that removed redaction | medium | accept | Accepted fail-open edge case (D-07): named in the generated comment and moduledoc; `mix threadline.policy.show` and the redaction drift view flag the mismatch |
| T-207-09 | Denial of service | ordinal loop | low | accept | Loop terminates: the dir holds finitely many names/modules, so some ordinal is free |
</threat_model>

<verification>
- Quick: `bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/mix/tasks/threadline/ test/threadline/mix/ test/threadline/capture/'` → 0 failures.
- Contract batch: public_surface, code_walkthrough_doc, release_artifact contract tests green (Task 3 verify).
- `MIX_ENV=dev mix docs --warnings-as-errors` green.
- RED proofs for all three tasks recorded; probe output recorded.
</verification>

<success_criteria>
- Rerun of gen.triggers writes a uniquely named, uniquely moduled migration; first-run name/module unchanged.
- Up re-points triggers with OR REPLACE and removes orphan per-table functions without cascading.
- Down is per table: first-run tables drop, rerun tables keep capture on, with the explanatory comment.
- Three local `fix(gen.triggers): ...` commits; no example or fixture migration changed.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- NEW `lib/threadline/mix/trigger_migration.ex` — module `Threadline.Mix.TriggerMigration` (@moduledoc false): `@type scan`, `scan/1`, `resolve_name/2`, `rerun?/2`.
- MODIFIED `lib/threadline/mix/migration_version.ex` — NEW public `@doc false` `Threadline.Mix.MigrationVersion.existing/1` returning `[{version, name, path}]`.
- MODIFIED `lib/mix/tasks/threadline.gen.triggers.ex` — `migration_content/3` (table_specs, module, rerun_tables), orphan drop in `up`, per-table `down` + rollback comment, rerun info line, `## Rerunning` moduledoc section.
- NEW test file `test/mix/tasks/threadline/gen_triggers_test.exs` — `Mix.Tasks.Threadline.GenTriggersTest`, helpers `run_triggers/2`, `trigger_files/1`, `ecto_name/1`, `version/1`, `module_of/1`, `executes/2`, `assert_valid_increasing!/1`; describes "rerun naming", "up body", "down body".
- NEW test file `test/threadline/mix/trigger_migration_test.exs` — `Threadline.Mix.TriggerMigrationTest`; describes "resolve_name/2", "scan/1", "rerun?/2", "MigrationVersion.existing/1".

<output>
Create `.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-02-SUMMARY.md` when done, with a "RED proof" section per task, the probe output, and the deferred note on `support.tickets` / `support_tickets` suffix aliasing.
</output>
