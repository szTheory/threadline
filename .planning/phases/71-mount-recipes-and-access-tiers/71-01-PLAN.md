---
phase: 71
plan: "71-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/operator-surface.md
  - guides/integration-contracts.md
autonomous: true
requirements:
  - INTEG-03
  - ADOPT-08
must_haves:
  truths:
    - "Adopters can copy one canonical `/audit` mount recipe for admins and one support-read-only variation without Threadline owning auth or roles."
    - "The docs teach router auth plus `authorize_fn` together, and clearly explain why export routes have a separate HTTP auth boundary."
    - "The support recipe promises narrower effective visibility and stricter export posture without overclaiming page-level blocking or universal scope enforcement."
  artifacts:
    - path: "guides/operator-surface.md"
      provides: "Canonical admin-first mount/runbook with support-read-only variation, first verification steps, and parity table"
    - path: "guides/integration-contracts.md"
      provides: "Shared assigns-shaped `authorize_fn` contract, opaque `{:ok, scope}` language, and advanced `export_authorize_fn` override guidance"
  key_links:
    - from: "guides/operator-surface.md"
      to: "lib/threadline/operator_surface/router.ex"
      via: "documented mount and export-boundary behavior"
      pattern: "live_session :threadline"
    - from: "guides/integration-contracts.md"
      to: "lib/threadline/operator_surface/export_auth_plug.ex"
      via: "shared-callback fallback and HTTP-native 403 behavior"
      pattern: "mirror = %{assigns: conn.assigns}"
---

<objective>
Define the canonical mount and auth contract for Phase 71 in the primary operator-surface docs.

Purpose: satisfy the documentation half of `INTEG-03` and `ADOPT-08` with one obvious admin-first path that also teaches the support-read-only variant safely.
Output: refreshed mount/auth guides that explain the secure topology, shared callback shape, support export defaults, and inline fallback parity.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/71-mount-recipes-and-access-tiers/71-CONTEXT.md
@.planning/phases/71-mount-recipes-and-access-tiers/71-RESEARCH.md
@.planning/phases/71-mount-recipes-and-access-tiers/71-PATTERNS.md
@guides/operator-surface.md
@guides/integration-contracts.md
@lib/threadline/operator_surface/router.ex
@lib/threadline/operator_surface/auth.ex
@lib/threadline/operator_surface/export_auth_plug.ex
@lib/threadline/operator_surface/live/timeline_live.ex

<interfaces>
From `lib/threadline/operator_surface/router.ex`:
```elixir
threadline_operator_surface(path, opts \\ [])
# secure mount requires scope pipe_through, :authorize_fn, or explicit ack
# exports: false suppresses the sibling /exports controller scope
```

From `lib/threadline/operator_surface/auth.ex`:
```elixir
case authorize_fn.(socket) do
  :ok | true -> {:cont, socket}
  {:ok, scope} -> {:cont, assign(socket, :threadline_scope, scope)}
  _ -> {:halt, redirect(socket, to: "/")}
end
```

From `lib/threadline/operator_surface/export_auth_plug.ex`:
```elixir
mirror = %{assigns: conn.assigns}
authorize_fn.(mirror)
# denial/error -> 403 forbidden plain text + halt
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the operator-surface guide around the canonical admin and support-read-only recipes</name>
  <read_first>guides/operator-surface.md; .planning/phases/71-mount-recipes-and-access-tiers/71-CONTEXT.md; lib/threadline/operator_surface/router.ex; lib/threadline/operator_surface/export_auth_plug.ex; lib/threadline/operator_surface/live/timeline_live.ex</read_first>
  <files>guides/operator-surface.md</files>
  <action>Update the guide so the primary mount story implements D-123 through D-147 exactly: teach one `/audit` topology, show the admin recipe first, show the support-read-only variation second, keep both behind `pipe_through [:browser, :admin_auth]` plus `authorize_fn` per D-129, and make the support variation set `exports: false` by default per D-127. State explicitly that `live_session`/`on_mount` does not protect export controller routes and that export denials stay HTTP-native `403` via `ExportAuthPlug` per D-133/D-134. Add first verification steps for the mounted surface, and add one compact parity table mapping mounted workflow -> operator question -> fallback transport -> guarantee level per D-139 through D-143. Keep the scope promise narrow per D-136/D-138: support scope is carried host-side and applied where implemented/tested today; do not claim page-level blocking or universal scope narrowing.</action>
  <acceptance_criteria>`rg -n "support-read-only|exports: false|live_session|403|mix threadline\\.incident|mix threadline\\.export|mix threadline\\.health\\.coverage|mix threadline\\.policy\\.show" guides/operator-surface.md` returns all required anchors, and the guide does not introduce a second support route tree or a Threadline-owned role vocabulary.</acceptance_criteria>
  <verify>
    <automated>rg -n "support-read-only|exports: false|live_session|403|mix threadline\\.(incident|export|health\\.coverage|policy\\.show)" guides/operator-surface.md</automated>
  </verify>
  <done>The operator-surface guide is the single canonical runbook: admin first, support variation second, export transport split explicit, parity table present, and support-scope claims limited to what the code/test surface proves.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the integration contract around the shared assigns-shaped authorizer and opaque scope</name>
  <read_first>guides/integration-contracts.md; .planning/phases/71-mount-recipes-and-access-tiers/71-CONTEXT.md; lib/threadline/operator_surface/auth.ex; lib/threadline/operator_surface/export_auth_plug.ex; examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</read_first>
  <files>guides/integration-contracts.md</files>
  <action>Extend the operator-surface contract section so the canonical callback shape implements D-130 through D-135: recommend one shared `%{assigns: assigns}` `authorize_fn` as the default teaching shape, keep `export_authorize_fn` as an advanced override only, and preserve `{:ok, scope}` as opaque host-owned scope with no Threadline role or tenancy DSL per D-126/D-132. Explain that the shared callback must work for both LiveView sockets and the export mirror fallback, and call out the current example app's split `%Plug.Conn{}`/`%Phoenix.LiveView.Socket{}` callback style as non-canonical by replacing it in the docs with the shared callback story. Keep the contract transport-specific but not feature-expanding: no page-level authorization DSL, no second auth vocabulary, no broader support-scope promise than the currently tested query paths.</action>
  <acceptance_criteria>`rg -n "%\\{assigns: assigns\\}|export_authorize_fn|opaque|host-owned|support_read_only|organization_id" guides/integration-contracts.md` shows the new callback and scope anchors, and the guide does not define any Threadline role enum or permissions DSL.</acceptance_criteria>
  <verify>
    <automated>rg -n "%\\{assigns: assigns\\}|export_authorize_fn|host-owned|support_read_only|organization_id" guides/integration-contracts.md</automated>
  </verify>
  <done>The integration-contract guide now teaches one shared assigns-shaped `authorize_fn`, keeps `export_authorize_fn` advanced, and describes `{:ok, scope}` as an opaque host-owned carrier rather than a Threadline auth model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> adopters | If the canonical recipe teaches the wrong auth topology, adopters can deploy `/audit` or exports with a weaker-than-intended boundary. |
| support recipe -> data egress | If support access is documented without stricter export defaults, read-only users may gain unnecessary download capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | E | canonical mount docs | mitigate | Teach `pipe_through [:browser, :admin_auth]` plus `authorize_fn` together per D-129, anchored to `threadline_operator_surface/2` compile-time guard behavior. |
| T-71-02 | I | support-read-only recipe | mitigate | Document `exports: false` as the default support posture per D-127 and keep `export_authorize_fn` opt-in only. |
| T-71-03 | T | scope language | mitigate | Keep support visibility promises narrow per D-136/D-138 and explicitly avoid page-level blocking or universal narrowing claims. |
</threat_model>

<verification>
Run:
- `rg -n "support-read-only|exports: false|live_session|403|mix threadline\\.(incident|export|health\\.coverage|policy\\.show)" guides/operator-surface.md`
- `rg -n "%\\{assigns: assigns\\}|export_authorize_fn|host-owned|support_read_only|organization_id" guides/integration-contracts.md`
</verification>

<success_criteria>
- One canonical admin-first mount/runbook exists in `guides/operator-surface.md`.
- The support-read-only variation reuses the same surface and defaults to `exports: false`.
- The integration contract teaches one shared assigns-shaped `authorize_fn` and keeps scope semantics host-owned and opaque.
</success_criteria>

<output>
After completion, create `.planning/phases/71-mount-recipes-and-access-tiers/71-01-SUMMARY.md`
</output>
