---
phase: 71
plan: "71-03"
type: execute
wave: 2
depends_on:
  - "71-01"
  - "71-02"
files_modified:
  - test/threadline/operator_surface_doc_contract_test.exs
  - test/threadline/integration_contracts_doc_contract_test.exs
  - test/threadline/integrations/sigra_doc_contract_test.exs
  - test/threadline/getting_started_saas_doc_contract_test.exs
  - test/threadline/example_phoenix_readme_contract_test.exs
  - test/threadline/operator_surface/auth_test.exs
  - test/threadline/operator_surface/export_auth_plug_test.exs
autonomous: true
requirements:
  - INTEG-03
  - ADOPT-08
must_haves:
  truths:
    - "Phase 71’s admin/support access-tier story is enforced by focused doc-contract tests instead of soft prose."
    - "The shared assigns-shaped authorization callback works across LiveView auth and export fallback without requiring transport-specific callback heads."
    - "The test suite prevents future docs from promising broader support-scope behavior than the current implementation proves."
  artifacts:
    - path: "test/threadline/operator_surface_doc_contract_test.exs"
      provides: "Guide-level contract lock for admin/support recipes, export split, and parity table wording"
    - path: "test/threadline/integration_contracts_doc_contract_test.exs"
      provides: "Contract lock for shared assigns-shaped authorizer and opaque scope semantics"
    - path: "test/threadline/integrations/sigra_doc_contract_test.exs"
      provides: "Reference-lane contract lock for host-owned `/audit` access-tier wording and shared callback shape"
    - path: "test/threadline/getting_started_saas_doc_contract_test.exs"
      provides: "Quickstart parity and mount-recipe contract lock"
    - path: "test/threadline/example_phoenix_readme_contract_test.exs"
      provides: "Runnable reference-path contract lock for the shared authorizer and support recipe wording"
    - path: "test/threadline/operator_surface/auth_test.exs"
      provides: "LiveView auth behavior proof for the shared callback shape and scope assignment"
    - path: "test/threadline/operator_surface/export_auth_plug_test.exs"
      provides: "HTTP export auth proof for mirror fallback and support-scope carriage"
  key_links:
    - from: "test/threadline/operator_surface/auth_test.exs"
      to: "lib/threadline/operator_surface/auth.ex"
      via: "shared callback result vocabulary and `:threadline_scope` assignment"
      pattern: "authorize_fn"
    - from: "test/threadline/operator_surface/export_auth_plug_test.exs"
      to: "lib/threadline/operator_surface/export_auth_plug.ex"
      via: "synthetic mirror fallback and 403 halt behavior"
      pattern: "mirror = %{assigns: conn.assigns}"
---

<objective>
Lock the Phase 71 access-tier contract in tests.

Purpose: satisfy the “tested” half of `INTEG-03` and keep `ADOPT-08` honest by failing fast on drift in docs, example code, or auth behavior.
Output: refreshed doc-contract tests and auth tests that prove the canonical admin/support recipes, shared authorizer contract, export split, and honest scope language.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-mount-recipes-and-access-tiers/71-CONTEXT.md
@.planning/phases/71-mount-recipes-and-access-tiers/71-RESEARCH.md
@guides/operator-surface.md
@guides/integration-contracts.md
@guides/getting-started-saas.md
@guides/integrations/sigra.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@examples/threadline_phoenix/README.md
@lib/threadline/operator_surface/auth.ex
@lib/threadline/operator_surface/export_auth_plug.ex
@test/threadline/operator_surface_doc_contract_test.exs
@test/threadline/integration_contracts_doc_contract_test.exs
@test/threadline/integrations/sigra_doc_contract_test.exs
@test/threadline/getting_started_saas_doc_contract_test.exs
@test/threadline/example_phoenix_readme_contract_test.exs
@test/threadline/operator_surface/auth_test.exs
@test/threadline/operator_surface/export_auth_plug_test.exs

<interfaces>
Public auth result vocabulary to preserve:
```elixir
:ok | true | {:ok, scope} -> grant
other | raise -> deny
```

Export fallback contract to preserve:
```elixir
mirror = %{assigns: conn.assigns}
authorize_fn.(mirror)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the doc-contract suite to lock the Phase 71 recipe and parity wording</name>
  <read_first>test/threadline/operator_surface_doc_contract_test.exs; test/threadline/integration_contracts_doc_contract_test.exs; test/threadline/integrations/sigra_doc_contract_test.exs; test/threadline/getting_started_saas_doc_contract_test.exs; test/threadline/example_phoenix_readme_contract_test.exs; guides/operator-surface.md; guides/integration-contracts.md; guides/integrations/sigra.md; guides/getting-started-saas.md; examples/threadline_phoenix/README.md</read_first>
  <files>test/threadline/operator_surface_doc_contract_test.exs, test/threadline/integration_contracts_doc_contract_test.exs, test/threadline/integrations/sigra_doc_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs, test/threadline/example_phoenix_readme_contract_test.exs</files>
  <action>Update the focused doc-contract tests so they lock the exact Phase 71 promises per D-123 through D-146: one canonical `/audit` mount story, admin first, support-read-only as the same surface with `exports: false`, router `pipe_through [:browser, :admin_auth]` plus `authorize_fn`, shared `%{assigns: assigns}` callback wording, explicit `live_session` versus export-controller transport split, inline fallback mappings for `mix threadline.incident`, `mix threadline.export`, `mix threadline.health.coverage`, and `mix threadline.policy.show`, plus API fallbacks where no dedicated Mix task exists. Extend the Sigra doc-contract file so the reference-lane guide is also locked to the same host-owned `/audit` access-tier wording and shared callback shape. Add negative assertions that reject a second support route tree, Threadline-owned roles/permissions vocabulary, or doc language claiming universal support-scope narrowing. Keep the existing source-reading assertion style; do not introduce snapshots.</action>
  <acceptance_criteria>`mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/integration_contracts_doc_contract_test.exs test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1` passes, and the assertions explicitly cover `exports: false`, `%{assigns: assigns}`, the export transport split, and the Sigra guide’s host-owned `/audit` wording.</acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/integration_contracts_doc_contract_test.exs test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>Doc drift now fails fast if the canonical recipe, parity map, or scope-honesty wording regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Extend auth tests for the shared callback contract across LiveView and export transports</name>
  <read_first>test/threadline/operator_surface/auth_test.exs; test/threadline/operator_surface/export_auth_plug_test.exs; lib/threadline/operator_surface/auth.ex; lib/threadline/operator_surface/export_auth_plug.ex; examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</read_first>
  <files>test/threadline/operator_surface/auth_test.exs, test/threadline/operator_surface/export_auth_plug_test.exs</files>
  <action>Extend the auth tests so the tested access pattern matches the Phase 71 docs. Add/adjust cases proving that a shared `%{assigns: assigns}` callback can authorize the LiveView surface and the export fallback mirror without transport-specific function heads per D-130/D-135. Keep existing result-vocabulary coverage (`:ok`, `true`, `{:ok, scope}`, deny, raise) and explicitly preserve `:threadline_scope` assignment and HTTP-native `403` export denial per D-134. Add at least one assertion that support-style scope data (for example `%{access: :support_read_only, organization_id: "org_123"}`) survives the grant path as an opaque host-owned map rather than being normalized into a Threadline role model. Add one focused `export_authorize_fn` support-export opt-in case that proves the documented advanced override path can deliberately grant export access under stricter host policy than the default `exports: false` support recipe.</action>
  <acceptance_criteria>`mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/export_auth_plug_test.exs --max-failures 1` passes, at least one case in each file exercises the shared `%{assigns: assigns}` contract plus an opaque support-scope map, and `test/threadline/operator_surface/export_auth_plug_test.exs` contains a named `export_authorize_fn` support-export opt-in case.</acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/export_auth_plug_test.exs --max-failures 1</automated>
  </verify>
  <done>The auth tests now prove the documented shared-callback contract across both transports while preserving opaque scope semantics and HTTP-native export denial behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prose/example -> test suite | If the docs or example drift without contract coverage, the repo can over-teach support scope or under-teach export boundaries. |
| auth hooks -> export routes | If the shared-callback contract is not tested across both transports, future changes can accidentally widen or split the auth boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-07 | I | doc-contract suite | mitigate | Assert `exports: false`, `%{assigns: assigns}`, inline fallbacks, and anti-overclaim wording directly in focused tests. |
| T-71-08 | E | auth tests | mitigate | Add shared-callback cases in both LiveView and export tests so transport drift becomes a red test immediately. |
| T-71-09 | T | scope semantics | mitigate | Assert support-style scope maps remain opaque host-owned data and reject Threadline-owned role vocabulary in docs/tests. |
</threat_model>

<verification>
Run:
- `mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/integration_contracts_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1`
- `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/export_auth_plug_test.exs --max-failures 1`
- `mix ci.all`
</verification>

<success_criteria>
- Focused doc-contract tests lock the Phase 71 recipe and prevent overclaiming support-scope behavior.
- Auth tests prove the shared assigns-shaped callback contract across LiveView and export transports.
- Future regressions in export posture, callback shape, or parity wording fail in the Phase 71 test slice.
</success_criteria>

<output>
After completion, create `.planning/phases/71-mount-recipes-and-access-tiers/71-03-SUMMARY.md`
</output>
