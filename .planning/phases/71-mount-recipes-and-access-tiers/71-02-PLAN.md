---
phase: 71
plan: "71-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/getting-started-saas.md
  - guides/integrations/sigra.md
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - examples/threadline_phoenix/README.md
autonomous: true
requirements:
  - INTEG-03
  - ADOPT-08
must_haves:
  truths:
    - "The runnable Phoenix/Sigra reference path and the SaaS quickstart both use the same canonical access-tier recipe."
    - "The example router demonstrates the shared assigns-shaped authorizer instead of split LiveView-versus-Conn authorization callbacks."
    - "Every mounted workflow named in the onboarding/reference docs points to an equivalent Mix task or API fallback without overclaiming full UI/CLI symmetry."
  artifacts:
    - path: "guides/getting-started-saas.md"
      provides: "Canonical first-hour walkthrough aligned to the Phase 71 mount recipes"
    - path: "guides/integrations/sigra.md"
      provides: "Sigra reference-lane auth wording aligned to host-owned `/audit` access tiers"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      provides: "Runnable shared-callback admin mount example"
    - path: "examples/threadline_phoenix/README.md"
      provides: "Reference-lane proof for admin mount plus support-read-only variation and inline parity notes"
  key_links:
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      to: "lib/threadline/operator_surface/export_auth_plug.ex"
      via: "shared assigns-shaped authorizer matches the export mirror fallback"
      pattern: "%{assigns: assigns}"
    - from: "guides/getting-started-saas.md"
      to: "guides/operator-surface.md"
      via: "surface-first walkthrough points to the canonical runbook instead of inventing a second recipe"
      pattern: "guides/operator-surface.md"
---

<objective>
Align the onboarding and runnable reference path to the canonical Phase 71 mount recipe.

Purpose: make `ADOPT-08` copy-pasteable in the quickstart and example app while reinforcing the same host-owned access story required by `INTEG-03`.
Output: refreshed quickstart/reference docs plus an example router that demonstrates the shared authorizer shape and honest fallback parity.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-mount-recipes-and-access-tiers/71-CONTEXT.md
@.planning/phases/71-mount-recipes-and-access-tiers/71-RESEARCH.md
@.planning/phases/71-mount-recipes-and-access-tiers/71-PATTERNS.md
@guides/getting-started-saas.md
@guides/integrations/sigra.md
@guides/operator-surface.md
@guides/integration-contracts.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@examples/threadline_phoenix/README.md
@lib/threadline/operator_surface/router.ex
@lib/threadline/operator_surface/export_auth_plug.ex

<interfaces>
From `examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex`:
```elixir
scope "/audit" do
  pipe_through [:browser, :admin_auth]

  threadline_operator_surface "/",
    actor_fn: &ThreadlinePhoenixWeb.Router.my_actor_fn/1,
    authorize_fn: &ThreadlinePhoenixWeb.Router.my_authorize_fn/1,
    repo: ThreadlinePhoenix.Repo
end
```

From `lib/threadline/operator_surface/export_auth_plug.ex`:
```elixir
mirror = %{assigns: conn.assigns}
authorize_fn.(mirror)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the SaaS quickstart and Sigra guide around the canonical access-tier story</name>
  <read_first>guides/getting-started-saas.md; guides/integrations/sigra.md; guides/operator-surface.md; guides/integration-contracts.md; .planning/phases/71-mount-recipes-and-access-tiers/71-CONTEXT.md</read_first>
  <files>guides/getting-started-saas.md, guides/integrations/sigra.md</files>
  <action>Update the quickstart and Sigra reference guide so they implement D-123 through D-146 consistently: preserve the surface-first narrative per D-145, point readers to one canonical mount/runbook path per D-146, and add the admin-first plus support-read-only variation language without turning these docs into a second compatibility matrix. In the quickstart, keep the mounted workflow canonical but add inline fallback equivalents exactly where they answer the same operator question per D-139 through D-143: `mix threadline.incident`, `mix threadline.export`, `mix threadline.health.coverage`, `mix threadline.policy.show`, plus API fallbacks for timeline/actor/row-history flows where no dedicated task exists per D-141/D-142. In the Sigra guide, keep Sigra request-capture-only and explicitly state that `/audit` auth remains host-owned, with the shared `%{assigns: assigns}` callback shape and support `exports: false` default aligned to the Phase 71 recipe.</action>
  <acceptance_criteria>`rg -n "support-read-only|exports: false|mix threadline\\.(incident|export|health\\.coverage|policy\\.show)|Threadline\\.timeline_page/2|Threadline\\.actor_history/2|Threadline\\.history/3|Threadline\\.as_of/4|%\\{assigns: assigns\\}" guides/getting-started-saas.md guides/integrations/sigra.md` returns the required parity and callback anchors.</acceptance_criteria>
  <verify>
    <automated>rg -n "support-read-only|exports: false|mix threadline\\.(incident|export|health\\.coverage|policy\\.show)|Threadline\\.timeline_page/2|Threadline\\.actor_history/2|Threadline\\.history/3|Threadline\\.as_of/4|%\\{assigns: assigns\\}" guides/getting-started-saas.md guides/integrations/sigra.md</automated>
  </verify>
  <done>The quickstart and Sigra guide reinforce one canonical access-tier story, keep Sigra request-capture-only, and name honest inline fallback equivalents for every mounted workflow they introduce.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the example’s split authorizer with the shared callback and refresh the proof README</name>
  <read_first>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex; examples/threadline_phoenix/README.md; .planning/phases/71-mount-recipes-and-access-tiers/71-CONTEXT.md; lib/threadline/operator_surface/export_auth_plug.ex; guides/getting-started-saas.md</read_first>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex, examples/threadline_phoenix/README.md</files>
  <action>Update the runnable Phoenix example so the code and README implement D-130/D-135 directly. Replace the split `%Plug.Conn{}`/`%Phoenix.LiveView.Socket{}` `my_authorize_fn/1` pattern with one shared assigns-shaped callback that reads `%{assigns: assigns}` and returns `:ok`, `{:ok, scope}`, or deny based on host-owned policy. Keep the mounted example behind `pipe_through [:browser, :admin_auth]` plus `authorize_fn` per D-129. Refresh the README so it stays a narrow reference-lane proof artifact, shows the admin recipe as the canonical mount, names the support-read-only variation with `exports: false` and host-owned scope per D-125 through D-127, and points back to the canonical guide while keeping inline parity reminders honest per D-139/D-140. Do not add a second support route tree, a Threadline role vocabulary, or broader scope claims than the current implementation proves.</action>
  <acceptance_criteria>`rg -n "%\\{assigns: assigns\\}|exports: false|support-read-only|pipe_through \\[:browser, :admin_auth\\]|mix threadline\\.(incident|export|health\\.coverage|policy\\.show)" examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex examples/threadline_phoenix/README.md` returns the new shared-callback and parity anchors, and the router no longer defines separate `%Plug.Conn{}` and `%Phoenix.LiveView.Socket{}` authorizer heads.</acceptance_criteria>
  <verify>
    <automated>rg -n "%\\{assigns: assigns\\}|exports: false|support-read-only|pipe_through \\[:browser, :admin_auth\\]|mix threadline\\.(incident|export|health\\.coverage|policy\\.show)" examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex examples/threadline_phoenix/README.md</automated>
  </verify>
  <done>The example router demonstrates the shared authorizer contract, and the example README proves the same access-tier story as the canonical docs without becoming a second narrative owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example code/docs -> adopters | If the runnable example still shows a split authorizer, adopters may diverge LiveView and export auth in ways the canonical contract is trying to prevent. |
| quickstart/reference docs -> support operators | If fallback parity is omitted or overstated, capture-only users either lose operator paths or are misled about feature equivalence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04 | E | example router | mitigate | Replace the split authorizer with one `%{assigns: assigns}` callback per D-130/D-135 so LiveView and export fallback can share host-owned policy safely. |
| T-71-05 | I | support-read-only docs | mitigate | Keep `exports: false` as the default support recipe and clearly mark broader export access as deliberate `export_authorize_fn` opt-in per D-127/D-131. |
| T-71-06 | R | parity wording | mitigate | Name only existing Mix-task/API fallbacks inline per D-141/D-144 and avoid invented symmetry claims. |
</threat_model>

<verification>
Run:
- `rg -n "support-read-only|exports: false|mix threadline\\.(incident|export|health\\.coverage|policy\\.show)|Threadline\\.timeline_page/2|Threadline\\.actor_history/2|Threadline\\.history/3|Threadline\\.as_of/4|%\\{assigns: assigns\\}" guides/getting-started-saas.md guides/integrations/sigra.md`
- `rg -n "%\\{assigns: assigns\\}|exports: false|support-read-only|pipe_through \\[:browser, :admin_auth\\]|mix threadline\\.(incident|export|health\\.coverage|policy\\.show)" examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex examples/threadline_phoenix/README.md`
</verification>

<success_criteria>
- The quickstart, Sigra guide, example router, and example README all teach the same Phase 71 access-tier recipe.
- The runnable example uses one shared assigns-shaped authorizer.
- Mounted workflows in onboarding/reference docs point to existing non-surface fallback paths without overclaiming full parity.
</success_criteria>

<output>
After completion, create `.planning/phases/71-mount-recipes-and-access-tiers/71-02-SUMMARY.md`
</output>
