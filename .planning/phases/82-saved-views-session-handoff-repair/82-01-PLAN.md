---
phase: 82-saved-views-session-handoff-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/session_plug.ex
  - lib/threadline/operator_surface/auth.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - guides/operator-surface.md
  - guides/integration-contracts.md
  - guides/getting-started-saas.md
  - examples/threadline_phoenix/README.md
  - test/threadline/operator_surface/router_test.exs
  - test/threadline/operator_surface/session_plug_test.exs
  - test/threadline/operator_surface/auth_test.exs
  - test/threadline/operator_surface/live/timeline_live_test.exs
  - test/threadline/operator_surface_doc_contract_test.exs
  - test/threadline/integration_contracts_doc_contract_test.exs
  - test/threadline/example_phoenix_readme_contract_test.exs
  - test/threadline/getting_started_saas_doc_contract_test.exs
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
must_haves:
  truths:
    - "The default `threadline_operator_surface/2` mount path automatically wires actor session handoff whenever `:actor_fn` is present."
    - "Session actor data remains the canonical ownership source; scope-derived actor data is compatibility-only and cannot silently override it."
    - "Saved-view behavior is proven on a normal `actor_fn` mount path, not only through isolated module tests or scope-only fallbacks."
    - "The canonical example and guides use a real `ActorRef`-returning `actor_fn` and describe the repaired default path truthfully."
  artifacts:
    - path: lib/threadline/operator_surface/router.ex
      provides: "Macro-owned actor/session handoff on the default operator-surface mount path"
      contains: "SessionPlug"
    - path: lib/threadline/operator_surface/auth.ex
      provides: "Session-first actor precedence and mismatch signaling"
      contains: "threadline_actor_ref"
    - path: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      provides: "Reference `ActorRef`-returning actor callback on the canonical mount path"
      contains: "ActorRef"
  key_links:
    - from: lib/threadline/operator_surface/router.ex
      to: lib/threadline/operator_surface/session_plug.ex
      via: "generated mount path installs the existing session handoff plug"
      pattern: "actor_fn|SessionPlug|threadline_actor_ref"
    - from: lib/threadline/operator_surface/auth.ex
      to: lib/threadline/operator_surface/live/timeline_live.ex
      via: "saved-view ownership still keys off session-first `threadline_actor_ref`"
      pattern: "threadline_actor_ref|saved_views|scope"
    - from: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      to: guides/operator-surface.md
      via: "example and docs teach the same default actor-handoff story"
      pattern: "threadline_operator_surface|actor_fn|authorize_fn"
---

<objective>
Repair the actual saved-view runtime gap by making the standard operator-surface mount path deliver actor identity into LiveView automatically and by aligning the canonical example/docs with that repaired behavior.

Purpose: satisfy the runtime portion of VIEW-01 and VIEW-02 before the evidence-only closeout work in Plan 02.
Output: a macro-owned actor/session handoff on normal mounts, explicit session-vs-scope precedence proof, and a truthful reference example/docs surface.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.20-MILESTONE-AUDIT.md
@.planning/phases/82-saved-views-session-handoff-repair/82-CONTEXT.md
@.planning/phases/82-saved-views-session-handoff-repair/82-RESEARCH.md
@.planning/phases/77-saved-views-ergonomics/77-DISCUSSION.md
@.planning/phases/77-saved-views-ergonomics/77-01-SUMMARY.md
@.planning/phases/77-saved-views-ergonomics/77-02-SUMMARY.md
@lib/threadline/operator_surface/router.ex
@lib/threadline/operator_surface/session_plug.ex
@lib/threadline/operator_surface/auth.ex
@lib/threadline/operator_surface/live/timeline_live.ex
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@guides/operator-surface.md
@guides/integration-contracts.md
@guides/getting-started-saas.md
@examples/threadline_phoenix/README.md
@test/threadline/operator_surface/router_test.exs
@test/threadline/operator_surface/session_plug_test.exs
@test/threadline/operator_surface/auth_test.exs
@test/threadline/operator_surface/live/timeline_live_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto-wire actor session handoff in the default mount macro</name>
  <files>lib/threadline/operator_surface/router.ex, lib/threadline/operator_surface/session_plug.ex, test/threadline/operator_surface/router_test.exs, test/threadline/operator_surface/session_plug_test.exs</files>
  <action>Update `threadline_operator_surface/2` so the generated router path installs `Threadline.OperatorSurface.SessionPlug` automatically when `:actor_fn` is present. Reuse the existing plug instead of inventing a second transfer mechanism. Preserve the secure-mount compile checks, keep mounts without `:actor_fn` valid, and make sure the actor-handoff wiring composes with the existing `live_session` and export scope generation rather than replacing them. If the current `SessionPlug` implementation needs a small compatibility refinement for the macro-owned path, make that change here without broadening the plug's public contract.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/router_test.exs test/threadline/operator_surface/session_plug_test.exs --max-failures 1</automated>
  </verify>
  <done>The normal operator-surface mount path hands actor identity into LiveView automatically whenever the adopter provides `:actor_fn`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove session-first actor ownership on the repaired path</name>
  <files>lib/threadline/operator_surface/auth.ex, test/threadline/operator_surface/auth_test.exs, test/threadline/operator_surface/live/timeline_live_test.exs</files>
  <action>Keep `Threadline.OperatorSurface.Auth` session-first and preserve scope fallback only for compatibility, but add explicit proof for the conflict case where session actor data and fallback scope actor data disagree. Prefer a low-noise warning or telemetry signal over silent ambiguity. Extend the LiveView and auth tests so they cover: a standard `actor_fn` mount path with actor-owned saved views, a no-actor mount path that does not expose actor-owned behavior, the legacy scope-only fallback, and mismatch precedence where session actor wins. Only touch `TimelineLive` if a small repair is needed to make the saved-view proof executable on the default mount path.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/live/timeline_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Saved-view ownership is proven on the repaired default path and session actor data is observably authoritative over scope fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Align the canonical example, guides, and doc contracts</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex, guides/operator-surface.md, guides/integration-contracts.md, guides/getting-started-saas.md, examples/threadline_phoenix/README.md, test/threadline/operator_surface_doc_contract_test.exs, test/threadline/integration_contracts_doc_contract_test.exs, test/threadline/example_phoenix_readme_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs</files>
  <action>Change the example router's `my_actor_fn/1` to return a real `%Threadline.Semantics.ActorRef{}` so the reference app exercises the repaired contract honestly. Rewrite the relevant guides and README sections so the default actor-owned saved-view story is: authenticated browser pipeline, `threadline_operator_surface` with `actor_fn` and `authorize_fn`, and no extra manual session plug for standard mounts. Keep any mention of manual `SessionPlug` composition as an advanced escape hatch rather than the primary recipe. Update doc-contract tests so the public contract locks this repaired story.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/integration_contracts_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The public example and docs teach the same repaired mount contract that the runtime now enforces and the tests now prove.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host request auth -> LiveView session | Actor identity moves from host-owned `conn` auth state into LiveView session state. |
| Session actor -> scope fallback | Conflicting identity-like data may exist in both places and must not silently invert ownership. |
| Public docs/examples -> adopter integrations | Canonical snippets drive how adopters mount the operator surface in real applications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Spoofing | `router.ex` / `session_plug.ex` | mitigate | Reuse signed session transport and only auto-wire the existing `ActorRef`-based handoff path when `:actor_fn` is present. |
| T-82-02 | Elevation of privilege | `auth.ex` | mitigate | Keep session actor authoritative, preserve fallback only when session actor is absent, and emit an explicit mismatch signal when both disagree. |
| T-82-03 | Repudiation | docs/example contract | mitigate | Update the example router and doc-contract tests so the public recipe matches the repaired runtime truth. |
| T-82-04 | Availability | generated router composition | mitigate | Preserve existing secure-mount and export-scope behavior while inserting the actor-handoff wiring conservatively. |
</threat_model>

<verification>
Run the targeted router, session plug, auth, LiveView, and doc-contract tests, then confirm the repaired code path is the default one: `threadline_operator_surface/2` installs the handoff automatically when `:actor_fn` is present, the example router returns an `ActorRef`, and saved-view tests pass on the standard mount path.
</verification>

<success_criteria>
Saved views work as actor-owned features on the normal `threadline_operator_surface/2` mount path without extra adopter wiring, and the canonical example/docs describe that repaired path truthfully.
</success_criteria>

<output>
After completion, create `.planning/phases/82-saved-views-session-handoff-repair/82-01-SUMMARY.md`
</output>
