# Plan 21-01: Adoption backlog — STG topology template + audited-path rubric

**Phase:** 21 — Host staging & pooler parity  
**Plan:** 21-01 of 02  
**Status:** Ready to execute  
**Depends on:** None (first wave)  
**Blocks:** 21-02 (CONTRIBUTING and checklist assume backlog anchors exist)  
**Requirements covered:** STG-01, STG-03  
**Wave:** 1  
**requirements_addressed:** STG-01, STG-03  
**files_modified:** `guides/adoption-pilot-backlog.md`, `test/threadline/ci_topology_contract_test.exs`  
**autonomous:** true  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-21-01-01 | Guide implies pasting **live** connection strings or tokens into `main` | high | Rubric text must say **redacted** / **link out** only; acceptance greps forbid example strings like `postgresql://user:pass@`. |
| T-21-01-02 | **OK** used without evidence pointer (STG-03) | medium | Table headers and copy must pair **OK \| Issue \| N/A \| Not run** with **Evidence / pointer** column; grep enforces marker `STG-AUDITED-PATH-RUBRIC`. |
| T-21-01-03 | Reader confuses CI proof with host HTTP/Oban proof | medium | Topology template must include explicit **class: CI \| host** or equivalent wording in the field block; grep enforces `STG-HOST-TOPOLOGY-TEMPLATE`. |

---

## Goal

Extend **`guides/adoption-pilot-backlog.md`** so integrators can fill **STG-01** (fixed-field topology narrative) and **STG-02** (HTTP + job paths) in-repo as an **index**, with **STG-03**-honest **OK / Issue / N/A / Not run** semantics and evidence pointers — aligned with **21-CONTEXT.md** D-01–D-15. Lock key phrases via **`test/threadline/ci_topology_contract_test.exs`**.

---

## Context

- **21-CONTEXT.md** D-08–D-15, D-01–D-03 (evidence shape B+C).
- **`.planning/milestones/v1.6-REQUIREMENTS.md`** (archived at milestone close) STG-01–STG-03.
- Existing **CI-PGBOUNCER-TOPOLOGY-CONTRACT** block must remain intact.

---

## Tasks

### Task 1: Add STG host topology template block (STG-01)

**What to do**

1. In **`guides/adoption-pilot-backlog.md`**, immediately **after** the existing **Connection topology (host / maintainer)** heading (before or integrated with the existing CI paragraph — keep the **CI-PGBOUNCER-TOPOLOGY-CONTRACT** paragraph and table **unchanged** in meaning).
2. Insert a new subsection heading: `## STG host topology template (STG-01)`.
3. Under it, include a **visible marker line** (plain text, not HTML comment): `STG-HOST-TOPOLOGY-TEMPLATE` on its own line in a short “How to use” paragraph.
4. Add a **Markdown table** (integrator copy-paste scaffold) whose **header row** includes these column titles **verbatim** (pipe-separated, no nested backticks inside cells): `Environment`, `Class (CI or host)`, `Chain (app to pooler to postgres)`, `Pooler product`, `Pool mode`, `Ecto pools`, `prepare policy`, `Sandbox note`, `Matches prod`, `Evidence link`. Use `to` instead of arrow unicode if you prefer ASCII-only; then grep for `Chain (app to`.
5. Below the table, add one short paragraph requiring the **one-paragraph rationale** when **partial** is selected (per STG-01).

**Read first**

- `guides/adoption-pilot-backlog.md`
- `.planning/phases/21-host-staging-pooler-parity/21-CONTEXT.md` (D-13–D-15)

**Acceptance criteria**

- `grep -nF 'STG-HOST-TOPOLOGY-TEMPLATE' guides/adoption-pilot-backlog.md` exits 0.
- `grep -nF '## STG host topology template (STG-01)' guides/adoption-pilot-backlog.md` exits 0.
- `grep -nE 'Chain \(app (to|→)' guides/adoption-pilot-backlog.md` exits 0.
- `grep -nF 'Matches prod' guides/adoption-pilot-backlog.md` exits 0.
- `grep -nF 'CI-PGBOUNCER-TOPOLOGY-CONTRACT' guides/adoption-pilot-backlog.md` exits 0 (preserve existing marker).

**Done condition:** All greps succeed.

---

### Task 2: Add STG audited-path rubric (STG-02 / STG-03)

**What to do**

1. In **`guides/adoption-pilot-backlog.md`**, after the STG-01 template section, add subsection heading: `## STG audited write paths (STG-02)`.
2. Include marker line: `STG-AUDITED-PATH-RUBRIC` on its own line in introductory prose.
3. Add a table with columns: `Path` | `Kind (HTTP \| job)` | `Status (OK \| Issue \| N/A \| Not run)` | `Evidence / pointer` | `Notes (N/A justification if N/A)`.
4. Add a **bullet list** of normative rules (from CONTEXT D-08–D-11): **Not run** vs **N/A**; **N/A** requires objective one-line justification; **OK** requires reproducible pointer; label **CI-class** vs **host-class** where confusion is likely.

**Read first**

- `guides/adoption-pilot-backlog.md` (post Task 1)
- `21-CONTEXT.md` (D-08–D-12)

**Acceptance criteria**

- `grep -nF 'STG-AUDITED-PATH-RUBRIC' guides/adoption-pilot-backlog.md` exits 0.
- `grep -nF '## STG audited write paths (STG-02)' guides/adoption-pilot-backlog.md` exits 0.
- `grep -nF 'Status (OK' guides/adoption-pilot-backlog.md` exits 0.

**Done condition:** All greps succeed.

---

### Task 3: Doc contract — lock STG markers in CI topology test

**What to do**

1. In **`test/threadline/ci_topology_contract_test.exs`**, add **two** new tests (or extend existing backlog test): assert `File.read!("guides/adoption-pilot-backlog.md")` contains **`STG-HOST-TOPOLOGY-TEMPLATE`** and **`STG-AUDITED-PATH-RUBRIC`**.
2. Do not remove existing assertions.

**Read first**

- `test/threadline/ci_topology_contract_test.exs`
- `guides/adoption-pilot-backlog.md`

**Acceptance criteria**

- `MIX_ENV=test mix test test/threadline/ci_topology_contract_test.exs` exits 0.

**Done condition:** Command exits 0.

---

## Verification

1. `MIX_ENV=test mix test test/threadline/ci_topology_contract_test.exs`
2. `MIX_ENV=test mix ci.all`

---

## Success criteria

- Backlog contains stable STG markers and scaffolds for **STG-01** and **STG-02**.
- Doc contract prevents silent removal of those markers.
- **STG-03** alignment: tables encode “no OK without pointer” in rubric text (verified by grep for `Evidence / pointer` header).
