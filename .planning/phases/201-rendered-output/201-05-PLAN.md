---
phase: 201-rendered-output
plan: 05
type: execute
wave: 5
depends_on: [201-04]
files_modified:
  - test/threadline/forward_only_gate_doc_contract_test.exs
  - test/threadline/critic_iteration_runbook_doc_contract_test.exs
  - test/threadline/phase06_nyquist_ci_contract_test.exs
  - test/threadline/ci_workflow_parity_contract_test.exs
  - mix.exs
  - test/threadline/operator_surface/rendered_output_contract_test.exs
  - .planning/audits/201-rendered-output-evidence.md
autonomous: true
requirements: [RENDER-01, RENDER-02, RENDER-03, RENDER-04, RENDER-05, RENDER-06]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The owned-source and representative-render guard reports exactly zero phase-number, decision-ID, and internal-taxonomy offenders while positive controls prove each matcher fails on a seeded offender."
    - "The guarded source inventory and representative render inventory must both be non-empty before a zero-offender result can pass."
    - "Pre/post corpus verification compares the same canonical tracked path set one-to-one; added, removed, duplicated, or renamed reference paths fail before byte hashes are compared."
    - "A zero-entry exception registry is the expected successful result and is recorded explicitly; any non-zero entry requires a named measured node, bounded delta, rationale, and expiry."
    - "Exception matching uses exact stable node identifiers and numeric before/after measurements, never display-text substring matching."
  artifacts:
    - path: test/threadline/critic_iteration_runbook_doc_contract_test.exs
      provides: History-preserved durable maintainer-runbook contract
    - path: test/threadline/ci_workflow_parity_contract_test.exs
      provides: History-preserved durable CI workflow parity contract
    - path: .planning/audits/201-rendered-output-evidence.md
      provides: Final accepted-blob, structural, corpus-integrity, exception-registry, test, and Phase 202 handoff evidence
    - path: test/threadline/operator_surface/rendered_output_contract_test.exs
      provides: Final non-vacuous owned-source/render/CSS and structural-integrity contract
  key_links:
    - from: mix.exs
      to: test/threadline/critic_iteration_runbook_doc_contract_test.exs
      via: Exact verify.doc_contract test path moves atomically with git mv
      pattern: "critic_iteration_runbook_doc_contract_test"
    - from: .planning/audits/201-rendered-output-evidence.md
      to: test/fixtures/operator_surface/manifest.sha256
      via: Same sorted normalized tracked path set and one-to-one SHA-256 equality before/after
      pattern: "entry count|path set|sha256"
    - from: .planning/audits/201-rendered-output-evidence.md
      to: test/threadline/operator_surface/rendered_output_contract_test.exs
      via: Exact seven-node pre/post canonical structure receipt and zero-offender output
      pattern: "seven|zero offenders|exception registry"
  prohibitions:
    - {statement: "Cleanup MUST NOT regenerate captures, scorecards, fixtures, screenshots, or baselines, nor invoke the paid/local critic to make the phase green; immutable reference bytes and deterministic checks are the evidence boundary.", status: unverified, flagged: true}
---

<objective>
Seal the residual cleanup with durable test names, one-to-one immutable evidence, and the full deterministic release gate.

Purpose: D-08, D-09, and D-14 through D-17 require the already-landed copy/CSS blobs and the Phase 201 attribute-only delta to be proven—not recreated—before Phase 202. This plan removes the last roadmap chronology from root test identities, finalizes the pre/post receipts, and runs all existing quality gates without updating reference artifacts.

Output: two history-preserved root test renames, complete final evidence, and a deterministic Phase 202 handoff.

### Source audit coverage

| Source | Items | Coverage |
|---|---|---|
| GOAL | Zero roadmap taxonomy reaches rendered output with unchanged structure/layout/appearance | Task 3 ties source/render/CSS guards, structural equality, immutable bytes, browser lanes, and CI into the handoff. |
| REQ | RENDER-01..RENDER-06 | All six IDs are in frontmatter and are independently evidenced by the final guard, receipt, mechanical gate, and browser/CI commands. |
| RESEARCH | Two residual root test renames and exact consumers | Tasks 1-2 use `git mv`; Task 1 updates the sole explicit Mix consumer and Task 2 retains convention discovery. |
| RESEARCH | Same-path corpus hashes, seven-node canonical structure, zero exception registry | Task 3 fails path-set drift before hash comparison and records exact stable IDs and measurements. |
| CONTEXT | D-08..D-17 | Tasks accept landed blobs, forbid replay/recapture, preserve baselines and browser coverage, and emit a clean Phase 202 handoff. |

Deferred ideas and external API/schema work are excluded. The two unclassified specless assumptions remain visibly flagged in Plan 01; successful requirement tests provide evidence without silently changing their probe classification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/201-rendered-output/201-CONTEXT.md
@.planning/phases/201-rendered-output/201-RESEARCH.md
@.planning/phases/201-rendered-output/201-PATTERNS.md
@.planning/phases/201-rendered-output/201-VALIDATION.md
@.planning/phases/201-rendered-output/201-01-SUMMARY.md
@.planning/phases/201-rendered-output/201-04-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rename the critic iteration runbook contract and its exact Mix consumer</name>
  <files>test/threadline/forward_only_gate_doc_contract_test.exs, test/threadline/critic_iteration_runbook_doc_contract_test.exs, mix.exs</files>
  <read_first>
    - .planning/phases/201-rendered-output/201-CONTEXT.md
    - .planning/phases/201-rendered-output/201-RESEARCH.md
    - test/threadline/forward_only_gate_doc_contract_test.exs
    - mix.exs
  </read_first>
  <action>Use `git mv` to rename `test/threadline/forward_only_gate_doc_contract_test.exs` to `test/threadline/critic_iteration_runbook_doc_contract_test.exs` per D-16, preserving blame. Rename `Threadline.ForwardOnlyGateDocContractTest` to `Threadline.CriticIterationRunbookDocContractTest` and remove roadmap chronology from test descriptions/moduledoc only where it names the test's provenance; retain the actual maintainer runbook assertions and protected grading vocabulary. Atomically replace the old explicit path in the `verify.doc_contract` alias in `mix.exs`; use `rg` over tracked source to prove no other discovery/reference consumer was missed. Do not rename the underlying product/maintainer runbook heading or weaken its contract.</action>
  <reversibility rating="reversible">The history-preserving file/module/discovery rename can be reverted without data or interface migration.</reversibility>
  <acceptance_criteria>
    - Git records one rename, not delete-and-recreate, and the durable module name matches the new file.
    - `mix.exs` invokes the new path and no tracked source refers to the old path/module.
    - All original runbook behavior assertions remain present and passing.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/critic_iteration_runbook_doc_contract_test.exs</automated>
    <fails_when>The command exits non-zero, reports zero tests, or any preserved runbook contract fails.</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.doc_contract</automated>
    <fails_when>The command exits non-zero, skips the renamed file because discovery still points at the old path, or any documentation contract fails.</fails_when>
  </verify>
  <done>The critic iteration runbook contract has a durable history-preserved identity and every exact discovery consumer resolves it.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the CI workflow parity contract without weakening discovery</name>
  <files>test/threadline/phase06_nyquist_ci_contract_test.exs, test/threadline/ci_workflow_parity_contract_test.exs</files>
  <read_first>
    - .planning/phases/201-rendered-output/201-CONTEXT.md
    - .planning/phases/201-rendered-output/201-RESEARCH.md
    - test/threadline/phase06_nyquist_ci_contract_test.exs
    - mix.exs
  </read_first>
  <action>Use `git mv` to rename `test/threadline/phase06_nyquist_ci_contract_test.exs` to `test/threadline/ci_workflow_parity_contract_test.exs` per D-16. Rename `Threadline.Phase06NyquistCIContractTest` to `Threadline.CIWorkflowParityContractTest` and replace roadmap-plan prefixes in test descriptions with the durable CI behaviors they pin, without changing assertions, workflow keys, triggers, job dependency checks, or helper semantics. Confirm tracked-source search finds no explicit old-path/module consumer; keep ordinary ExUnit convention discovery intact.</action>
  <reversibility rating="reversible">The git-preserved test identity rename has no production or data migration effect.</reversibility>
  <acceptance_criteria>
    - Git records one history-preserving rename and the new path/module contain no phase-number identity.
    - The assertion set remains behaviorally identical and ExUnit convention discovery executes it.
    - No tracked source references the old path or module.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/ci_workflow_parity_contract_test.exs</automated>
    <fails_when>The command exits non-zero, reports zero tests, or any preserved CI workflow assertion fails.</fails_when>
  </verify>
  <done>The root CI workflow contract has a durable history-preserved identity and remains convention-discoverable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize immutable structural/mechanical evidence and Phase 202 handoff</name>
  <files>test/threadline/operator_surface/rendered_output_contract_test.exs, .planning/audits/201-rendered-output-evidence.md</files>
  <read_first>
    - .planning/phases/201-rendered-output/201-CONTEXT.md
    - .planning/phases/201-rendered-output/201-RESEARCH.md
    - .planning/phases/201-rendered-output/201-VALIDATION.md
    - test/threadline/operator_surface/rendered_output_contract_test.exs
    - .planning/audits/201-rendered-output-evidence.md
    - test/fixtures/operator_surface/manifest.sha256
    - test/threadline/operator_surface/operator_surface_fixture_contract_test.exs
    - test/threadline/operator_surface/mechanical_checker_test.exs
    - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts
    - examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts
    - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts
  </read_first>
  <behavior>
    - Owned source, representative renders, and emitted CSS report zero offenders only after non-empty inventory checks; each seeded matcher offender fails.
    - The exact seven stable node IDs have equal pre/post canonical structural hashes.
    - The normalized tracked reference path set and each byte hash match one-to-one, with an explicit zero-entry exception registry.
    - Existing LiveView, accessibility, desktop/mobile/responsive/overflow, screenshots, example precommit, mechanical, and root CI gates pass without regenerating evidence.
  </behavior>
  <action>Complete the deterministic Plan 01 contract and evidence file per D-08 through D-17. For source/render/CSS vocabulary and provenance matching, assert both source and render inventories are non-empty, emit actionable path-or-stable-node offenders, and require seeded positive controls for phase-number, decision-ID, internal taxonomy, each provenance attribute, and CSS provenance. Keep token-bounded phase matching restricted to Threadline-owned static UTF-8 content; never feed host/user runtime values. Recompute post-edit canonical receipts for exactly the seven stable node IDs and compare to the recorded pre-edit receipts using LazyHTML element/tag hierarchy and behaviorally meaningful attributes after removing only the three named provenance attributes; fail empty/incomplete inventories. Normalize and sort the tracked paths from `manifest.sha256`, require the manifest non-empty with its recorded exact count, fail added/removed/duplicated/renamed paths before comparing every byte hash. Record zero exceptions explicitly. If a real measured exception exists, stop automatic completion and require an exact stable node ID, numeric before/after/delta within the researched bound, rationale, and expiry—never display-text substring identity. Attribute CSS to `5752e357`, copy to `1a5fbef2`, and their integrated identity to `18fe87f5`; verify identical accepted blobs through current HEAD and do not replay those edits per D-08/D-09. Append command results and a deterministic Phase 202 clean-handoff statement to the tracked evidence file. Never update snapshots, captures, fixtures, scorecards, baselines, or invoke any critic command.</action>
  <acceptance_criteria>
    - All matcher lanes report zero real offenders and each positive-control fixture fails its lane; empty inventories cannot pass.
    - Exactly seven pre/post canonical node receipts match and the same non-empty normalized manifest path set has byte-for-byte equal hashes.
    - The exception registry is explicitly zero entries; any non-zero measured delta blocks autonomous completion pending a compliant stable-ID record.
    - All targeted and aggregate gates pass without modifying fixture, scorecard, capture, screenshot, or baseline bytes.
    - The evidence names the accepted landed commits and states the exact clean preconditions handed to Phase 202.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/operator_surface_fixture_contract_test.exs test/threadline/operator_surface/mechanical_checker_test.exs</automated>
    <fails_when>The command exits non-zero, reports zero tests, accepts an empty inventory, misses a positive control, detects an offender, finds structural/path/hash drift, or accepts a malformed exception.</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.mechanical</automated>
    <fails_when>The command exits non-zero or reports a fixture, scorecard, ratchet, integrity, or measured-floor regression.</fails_when>
    <automated>npm --prefix examples/threadline_phoenix/e2e exec playwright test -- tests/operator-earned-flows.spec.ts tests/operator-shell-home.spec.ts tests/operator-home-nav-mobile.spec.ts tests/operator-responsive-mobile-first.spec.ts tests/operator-accessibility.spec.ts tests/operator-screenshot-regression.spec.ts</automated>
    <fails_when>The command exits non-zero, reports no tests, detects behavior/accessibility/responsive/overflow/visual drift, or updates a snapshot instead of comparing it.</fails_when>
    <automated>env -C examples/threadline_phoenix ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix precommit</automated>
    <fails_when>The command exits non-zero or compile, dependency hygiene, formatting, or example tests fail.</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix ci.all</automated>
    <fails_when>The command exits non-zero or any root compile, format, static analysis, test, documentation, security, or mechanical-integrity lane fails.</fails_when>
    <automated>git diff --exit-code 5752e357 18fe87f5 -- lib/threadline/operator_surface/style.ex</automated>
    <fails_when>The command exits non-zero because the accepted Style blob does not match its attributed integrated version.</fails_when>
    <automated>git diff --exit-code 1a5fbef2 18fe87f5 -- lib/threadline/operator_surface/live/stress_live.ex lib/threadline/operator_surface/stress_fixtures.ex</automated>
    <fails_when>The command exits non-zero because the accepted StressLive or StressFixtures blob does not match its attributed integrated version.</fails_when>
    <automated>git diff --exit-code 18fe87f5 HEAD -- lib/threadline/operator_surface/style.ex lib/threadline/operator_surface/live/stress_live.ex lib/threadline/operator_surface/stress_fixtures.ex</automated>
    <fails_when>The command exits non-zero because Style, StressLive, or StressFixtures differs between the accepted integrated baseline and the phase HEAD.</fails_when>
  </verify>
  <done>The tracked evidence proves zero offenders, exact seven-node structural identity, exact immutable corpus identity, zero exceptions, unchanged browser output, passing aggregate gates, accepted landed-blob attribution, and a clean deterministic Phase 202 handoff.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1

## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracked evidence to release decision | Repository-owned receipts and deterministic commands authorize the Phase 202 handoff; untracked local critic/scorecard state is untrusted and excluded. |
| Browser to LiveView | Existing Auth/Coverage hooks remain the authorization boundary while the final browser suite exercises operator routes. |
| Test harness to stress route | `/audit/__stress` remains a dev/test-only synthetic surface and must fail closed in production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-201-05-01 | Tampering | Corpus/structure receipts | high | mitigate | Require non-empty exact path/node sets, path equality before hash equality, and deterministic canonical hashes. |
| T-201-05-02 | Repudiation | Landed copy/CSS attribution | medium | mitigate | Record commit IDs, blob equality commands, and current deterministic gate outputs in the tracked evidence. |
| T-201-05-03 | Information Disclosure | Guard inputs | medium | mitigate | Scan only explicit Threadline-owned static sources/assets/renders; exclude host/user runtime data and untracked local reports. |
| T-201-05-04 | Elevation of Privilege | `/audit/__stress` and operator routes | high | mitigate | Preserve production fail-close plus Auth/Coverage hooks and run aggregate LiveView/browser gates. |
| T-201-05-05 | Tampering | Exception registry | high | mitigate | Default to explicit zero entries; require exact stable node IDs, numeric measurements, bounded delta, rationale, and expiry for any deviation. |
| T-201-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install occurs; use existing lockfiles and the already-audited toolchain. |
</threat_model>

<verification>
Run the targeted contract/mechanical tests, existing affected browser lanes, example `mix precommit`, and root `mix ci.all`. Confirm accepted blob identity and a clean tracked diff for all immutable corpus, scorecard, fixture, capture, screenshot, and baseline paths.
</verification>

<success_criteria>
- Both residual root test identities are history-preserved, durable, and correctly discovered.
- Source/render/CSS guards are non-vacuous, exact-boundary, zero-offender, and positive-controlled.
- Seven-node structure, canonical reference paths, and immutable bytes match one-to-one with zero exceptions.
- Existing browser, accessibility, responsive, screenshot, mechanical, precommit, and CI gates pass without regeneration.
- The evidence artifact records accepted landed commits and the deterministic Phase 202 handoff.
</success_criteria>

## Artifacts this phase produces

- New durable tracked path `test/threadline/critic_iteration_runbook_doc_contract_test.exs` with module `Threadline.CriticIterationRunbookDocContractTest`, created by history-preserving rename.
- New durable tracked path `test/threadline/ci_workflow_parity_contract_test.exs` with module `Threadline.CIWorkflowParityContractTest`, created by history-preserving rename.
- Final content in existing phase artifact `.planning/audits/201-rendered-output-evidence.md`; no fixture, scorecard, capture, screenshot, baseline, production symbol, route, DOM hook, or dependency is created.

<output>Create .planning/phases/201-rendered-output/201-05-SUMMARY.md when done.</output>
