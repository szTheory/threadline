---
phase: 201-rendered-output
plan: 02
type: execute
wave: 2
depends_on: [201-01]
files_modified:
  - lib/threadline/operator_surface/live/start_live.ex
  - test/threadline/operator_surface/live/start_live_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts
autonomous: true
requirements: [RENDER-01, RENDER-02, RENDER-06]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Matching is defined over UTF-8 Elixir strings using explicit case-insensitive patterns for the fixed ASCII planning vocabulary; arbitrary host- or user-supplied values are outside the scan boundary."
  artifacts:
    - path: lib/threadline/operator_surface/live/start_live.ex
      provides: Clean record and correlation lookup nodes without replacement metadata
    - path: test/threadline/operator_surface/live/start_live_test.exs
      provides: Behavior-first Start LiveView coverage independent of deleted provenance attributes
    - path: examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts
      provides: Semantic Start-page browser coverage shared with later cohorts
  key_links:
    - from: test/threadline/operator_surface/live/start_live_test.exs
      to: lib/threadline/operator_surface/live/start_live.ex
      via: Phoenix.LiveViewTest semantic queries prove both Start lookup behaviors after deletion
      pattern: "record lookup|correlation lookup"
    - from: examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
      to: lib/threadline/operator_surface/live/start_live.ex
      via: Roles, labels, visible text, and stable product anchors replace planning-provenance selectors
      pattern: "getByRole|getByText|getByLabel"
  prohibitions:
    - {statement: "Cleanup MUST NOT become a redesign or erase durable product/maintainer domain vocabulary (`Ledger item`, `Origin cohort`, `Refute Twin`, grading/rung language, stable story/fixture/ledger/cohort identifiers); only roadmap/process taxonomy is removed.", status: unverified, flagged: true}
---

<objective>
Remove the six Start-page provenance attributes atomically with every directly coupled semantic consumer migration.

Purpose: D-01 through D-07 require `start.record_lookup` and `start.correlation_lookup` to lose their provenance triples without replacement metadata, selector regressions, vocabulary loss, or visual/behavioral change.

D-06 specifically removes or permanently rejects planning chronology and opaque roadmap taxonomy from browser-owned copy/metadata while preserving stable non-rendered fixture keys and legitimate engineering/domain terms.

Output: a clean Start LiveView plus one ExUnit and three Playwright consumers that target user-observable behavior.

### Source audit coverage

| Source | Items | Coverage |
|---|---|---|
| GOAL | Browser output contains no internal planning provenance and behaves identically | The tracer migrates every Start consumer before deleting only six inventoried attributes. |
| REQ | RENDER-01, RENDER-02, RENDER-06 | Durable copy, complete Start attribute removal, and unchanged behavior/layout are asserted. |
| RESEARCH | Two Start nodes, six attributes, one ExUnit and three Playwright consumers | This plan owns exactly that source/direct-consumer cohort; later plans own the remaining 15 attributes. |
| CONTEXT | D-01..D-07, D-10, D-12, D-15 | The action enforces deletion without replacement hooks, semantic selectors, static-owned scope, and existing browser behavior. |

Deferred ideas are excluded. RENDER-02 remains explicitly flagged as unclassified in Plan 01 until concrete execution proves its requirement behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/201-rendered-output/201-CONTEXT.md
@.planning/phases/201-rendered-output/201-RESEARCH.md
@.planning/phases/201-rendered-output/201-PATTERNS.md
@.planning/phases/201-rendered-output/201-VALIDATION.md
@.planning/phases/201-rendered-output/201-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Remove the six Start attributes after semantic consumer migration</name>
  <files>lib/threadline/operator_surface/live/start_live.ex, test/threadline/operator_surface/live/start_live_test.exs, examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts, examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts, examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts</files>
  <read_first>
    - .planning/phases/201-rendered-output/201-CONTEXT.md
    - .planning/phases/201-rendered-output/201-RESEARCH.md
    - .planning/phases/201-rendered-output/201-PATTERNS.md
    - lib/threadline/operator_surface/live/start_live.ex
    - test/threadline/operator_surface/live/start_live_test.exs
    - examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts
    - examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
    - examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts
  </read_first>
  <behavior>
    - Start record lookup remains reachable and operable through its existing role, label, visible text, and stable semantic anchors.
    - Start correlation lookup remains reachable and operable through its existing role, label, visible text, and stable semantic anchors.
    - Neither node emits any of the three planning-provenance attributes.
  </behavior>
  <action>First migrate every Start-page assertion in the three named Playwright files and `start_live_test.exs` away from `data-earned-flow`, `data-persona`, and `data-jtbd`, using existing roles, labels, visible text, form controls, links, headings, or stable product-domain identifiers per D-03. Exercise those assertions while the old attributes still exist so the selector migration is proven independently. Then remove exactly the EF1/P2/J4 triple from canonical node `start.record_lookup` and the EF4/P1/J1 triple from `start.correlation_lookup` in `start_live.ex` per D-01. Add no replacement metadata or `data-testid` per D-02. Do not alter copy, event names, form submission, Auth/Coverage hooks, classes, responsive behavior, or the durable identifiers protected by D-05 and D-07.</action>
  <acceptance_criteria>
    - Exactly six attributes are deleted from exactly the two Start nodes, with no other template edit.
    - The Start ExUnit test and all affected Playwright assertions use behavioral semantics and contain no dependency on the deleted attributes.
    - Record lookup, correlation lookup, Home navigation, and responsive behavior still pass under the existing authenticated example application.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/live/start_live_test.exs</automated>
    <fails_when>The command exits non-zero, reports zero tests, or any Start behavior assertion fails.</fails_when>
    <automated>npm --prefix examples/threadline_phoenix/e2e exec playwright test -- tests/operator-earned-flows.spec.ts tests/operator-home-nav-mobile.spec.ts tests/operator-responsive-mobile-first.spec.ts</automated>
    <fails_when>The command exits non-zero, reports no tests, or any desktop/mobile semantic behavior assertion fails.</fails_when>
  </verify>
  <done>Both Start nodes are provenance-free and all Start-facing automated consumers pass without replacement test hooks.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1

## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser to Start LiveView | Authenticated browser events and lookup parameters cross the existing Auth/Coverage mount boundary. |
| Owned Start output to guard | Only Threadline-owned static template output enters the planning-vocabulary guard; host/user runtime values remain excluded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-201-02-01 | Spoofing | Start LiveView mount/events | high | mitigate | Retain Auth/Coverage hooks and run Start LiveView plus authenticated browser flows after selector migration. |
| T-201-02-02 | Information Disclosure | Static guard boundary | medium | mitigate | Scan only explicit owned static content and never host/user runtime values. |
| T-201-02-03 | Tampering | Semantic selector migration | medium | mitigate | Prove behavior-first assertions before deleting exactly the two source triples. |
| T-201-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install occurs; use the existing locked ExUnit and Playwright toolchain only. |
</threat_model>

<verification>
Run the targeted Start LiveView test and all three Start-coupled browser specs using the exact ASDF runtime; inspect the task diff for exactly six provenance-attribute deletions and no replacement metadata, copy, class, hierarchy, route, or event change.
</verification>

<success_criteria>
- Exactly two Start nodes lose exactly six provenance attributes.
- One ExUnit and three Playwright consumers use behavior-first semantics and pass.
- Existing copy, durable identifiers, Auth/Coverage hooks, structure, layout, and responsive behavior remain unchanged.
</success_criteria>

## Artifacts this phase produces

- No new production symbol, route, DOM hook, dependency, fixture, screenshot, scorecard, or baseline is created; existing Start source/tests are narrowed to semantic behavior.

<output>Create .planning/phases/201-rendered-output/201-02-SUMMARY.md when done.</output>
