---
phase: 62-mix-task-and-example-app-wiring
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - examples/threadline_phoenix/mix.exs
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
autonomous: true
requirements:
  - SURF-04
must_haves:
  truths:
    - The example app mounts the operator surface behind an auth pipeline
    - The auth pipeline has an actor_fn and authorize_fn that satisfy the AUTH contract
    - Anonymous users are rejected from the surface
    - Authenticated admins can reach the surface
  artifacts:
    - path: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      provides: Router mounting the surface
      contains: threadline_operator_surface
    - path: examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
      provides: Integration test proving access controls
      min_lines: 30
  key_links:
    - from: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      to: lib/threadline/operator_surface/router.ex
      via: threadline_operator_surface macro
---

<objective>
Wire the operator surface end-to-end in `examples/threadline_phoenix` behind a `phx.gen.auth`-style admin pipeline (SURF-04).

Purpose: Prove the canonical adopter wiring works, verifying the AUTH-02 / AUTH-04 contracts in a real Phoenix host application, and setting up the example that DOC-03 will reference.
Output: A fully wired operator surface in the example app with an integration test validating authenticated vs anonymous access.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-mix-task-and-example-app-wiring/62-RESEARCH.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UI dependencies and mock admin auth</name>
  <files>
    examples/threadline_phoenix/mix.exs
    examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  </files>
  <behavior>
    - Test 1 (implied): `mix deps.get` succeeds and project compiles with `phoenix_live_view` present.
    - Test 2 (implied): The router defines an admin pipeline that halts unauthenticated requests.
  </behavior>
  <action>
    1. In `examples/threadline_phoenix/mix.exs`, add `{:phoenix_live_view, "~> 1.0"}` and `{:phoenix_html, "~> 4.0"}` to `deps()`.
    2. In `router.ex`, add a `browser` pipeline with basic session/flash/html plugs.
    3. In `router.ex`, add an `admin_auth` pipeline (or `plug :require_authenticated_admin`) that checks for `conn.assigns[:current_user].is_admin == true` and halts with 403 or redirects if missing.
    4. Implement mock `actor_fn` and `authorize_fn` callbacks in the router matching the `Threadline.OperatorSurface` contract.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix deps.get && mix compile</automated>
  </verify>
  <done>Dependencies added and router compiles with the new pipelines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount operator surface and prove access</name>
  <files>
    examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
    examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
  </files>
  <behavior>
    - Test 1: Anonymous request to `/audit/transactions/:id` is rejected (e.g. 403 or 302).
    - Test 2: Authenticated admin request to `/audit/transactions/:id` reaches the surface (renders successfully or returns expected HTML).
  </behavior>
  <action>
    1. In `router.ex`, `import Threadline.OperatorSurface.Router`.
    2. Create a new `scope "/audit", ... do` using the `browser` and `admin_auth` pipelines.
    3. Inside the scope, call `threadline_operator_surface "/", actor_fn: &my_actor_fn/1, authorize_fn: &my_authorize_fn/1`.
    4. Create `operator_surface_test.exs` using `Phoenix.ConnTest`.
    5. Write tests asserting that requests to the surface without the `current_user` assign fail, and requests with the mock `current_user` pass.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix_web/operator_surface_test.exs</automated>
  </verify>
  <done>Operator surface is mounted and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web -> Operator Surface | Ensures that the `threadline_operator_surface` macro relies securely on the host app's authentication pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02 | Elevation of Privilege | Example App Router | mitigate | Explicitly requires the `:authorize_fn` and uses `pipe_through` for an admin plug. |
</threat_model>

<verification>
mix verify.example passes.
</verification>

<success_criteria>
The example application cleanly hosts the LiveView operator surface, strictly gated behind an admin pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/62-mix-task-and-example-app-wiring/62-02-SUMMARY.md`
</output>