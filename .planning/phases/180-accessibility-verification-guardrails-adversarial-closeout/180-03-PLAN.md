---
phase: 180-accessibility-verification-guardrails-adversarial-closeout
plan: 03
type: execute
wave: 3
depends_on:
  - 180-02
files_modified:
  - test/threadline/operator_surface/style_contract_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/ui.ex
autonomous: true
requirements:
  - MOTION-01
must_haves:
  truths:
    - "Motion is meaningful, compositor-friendly, origin-aware, token-governed, and avoids zero-scale collapse per D-08 and D-10."
    - "Reduced-motion mode collapses non-essential motion while preserving usable state changes per MOTION-01."
    - "Press feedback exists only on enabled interactive controls, while disabled controls look and behave disabled per D-09."
  artifacts:
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "Motion token, reduced-motion, transition, and transform contract checks"
      contains: "motion"
    - path: "examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts"
      provides: "Rendered computed-style motion and reduced-motion browser evidence"
      contains: "reducedMotion"
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "Operator motion tokens, keyframes, transition utilities, active states, and reduced-motion CSS"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts"
      to: "lib/threadline/operator_surface/style.ex"
      via: "computed CSS verifies generated motion and reduced-motion behavior"
      pattern: "getComputedStyle"
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "source contract scans enforce tokenized motion and disallowed patterns"
      pattern: "operator_css"
---

<objective>
Create the MOTION-01 rendered and source-contract motion plan for Phase 180.

Purpose: Make motion compliance measurable through existing style contracts and Playwright computed-style checks, per D-08, D-09, and D-10.
Output: Tightened motion/reduced-motion checks and only narrow CSS/UI fixes required to satisfy the contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/180-accessibility-verification-guardrails-adversarial-closeout/180-CONTEXT.md
@.planning/phases/180-accessibility-verification-guardrails-adversarial-closeout/180-RESEARCH.md
@.planning/phases/180-accessibility-verification-guardrails-adversarial-closeout/180-VALIDATION.md
@examples/threadline_phoenix/AGENTS.md
@test/threadline/operator_surface/style_contract_test.exs
@examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts
@examples/threadline_phoenix/e2e/playwright.config.ts
@lib/threadline/operator_surface/style.ex
@lib/threadline/operator_surface/ui.ex
</context>

<source_audit>
## Phase 180 Coverage

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Verify WCAG 2.2 AA rendered accessibility, motion compliance, guardrails, and adversarial closeout | Plans 180-01 through 180-04 |
| REQ | A11Y-01 rendered state, keyboard, visible focus, restored focus, screen-reader-ready evidence | Plan 180-01 and final manual evidence in 180-04 |
| REQ | A11Y-02 APG semantics, non-color cues, touch target checks | Plan 180-02 |
| REQ | MOTION-01 motion token and reduced-motion contract | Plan 180-03 |
| REQ | MOTION-02 guardrail matrix and adversarial review | Plan 180-04 |
| RESEARCH | Motion must use transform/opacity, tokens, reduced-motion, no zero-scale collapse, no high-frequency row animation | Plan 180-03 per D-08 and D-10 |
| RESEARCH | Press feedback only for enabled controls and disabled controls remain disabled | Plan 180-03 per D-09 |
| CONTEXT | D-08 motion scope, D-09 interactive press feedback, D-10 measurable checks | Plan 180-03 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten source motion contracts</name>
  <files>test/threadline/operator_surface/style_contract_test.exs, lib/threadline/operator_surface/style.ex</files>
  <behavior>
    - Source contracts enforce token-governed durations/easing, no broad transition-all usage, compositor-friendly transform/opacity motion, and no zero-scale collapse.
    - Source contracts prevent high-frequency row/list/table animations and require reduced-motion overrides for relevant active, overlay, details, toast, and transition utility states.
    - Source contracts distinguish enabled press feedback from disabled control styling.
  </behavior>
  <action>Add failing or tightened checks in the existing style contract before CSS fixes. Keep scans selector-bounded and comment-safe, following the existing style contract helper patterns. Do not ban legitimate small press feedback transforms; ban collapse-to-nothing behavior and layout-affecting or ungoverned motion per D-08. Add disabled-control checks that prove press feedback is only applied to enabled interactive controls per D-09. If the contract fails, change only `style.ex` selectors or tokens needed to satisfy the measured behavior.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <done>Style contracts enforce the MOTION-01 source-level motion rules without blocking legitimate existing active feedback or reduced-motion fallbacks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add rendered computed-style motion checks</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts, lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/ui.ex</files>
  <behavior>
    - Default-motion browser checks verify tokenized duration/easing and transform/opacity properties on opened modal, drawer, dropdown/popover, details/accordion, toast/status, and enabled press interactions.
    - Reduced-motion browser checks verify shortened durations, disabled non-essential transforms, no delayed animation, and retained visible state changes.
    - Disabled controls do not animate or visually press when activated through mouse or keyboard attempts.
  </behavior>
  <action>Extend the existing Playwright motion spec and its helper style, using `test.use` or browser media emulation already supported by the current Playwright configuration. Assert computed style on real rendered operator and stress surfaces rather than source strings alone per D-10. If source fixes are required, update only existing `style.ex` or shared UI transition attributes. Keep motion scope to state changes, continuity, and feedback; do not add decorative animation or high-frequency row animations per D-08.</action>
  <verify>
    <automated>./examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-motion.spec.ts</automated>
    <automated>THREADLINE_E2E_THEME=system ./examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-motion.spec.ts</automated>
  </verify>
  <done>Browser computed-style checks prove MOTION-01 in default and reduced-motion lanes, including press feedback and disabled-control behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS motion to user perception | CSS state changes affect operator orientation and must not create misleading or inaccessible feedback. |
| Browser media preference to rendered UI | Reduced-motion preferences cross from browser settings into generated CSS behavior. |
| Test harness to production style contract | Motion checks must guard current UI behavior without adding runtime dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-07 | Spoofing | Press feedback on disabled controls | mitigate | Add checks proving disabled controls do not respond as enabled controls. |
| T-180-08 | Denial of Service | High-frequency row/list/table motion | mitigate | Add source and rendered checks that prevent repeated row animations from consuming attention or browser work. |
| T-180-09 | Tampering | Reduced-motion overrides | mitigate | Verify computed style under reduced-motion media settings, not source comments alone. |
| T-180-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are allowed; if a dependency is proposed, stop for human verification before changing the supply chain. |
</threat_model>

<verification>
Run `mix test test/threadline/operator_surface/style_contract_test.exs`, `./examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-motion.spec.ts`, and `THREADLINE_E2E_THEME=system ./examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-motion.spec.ts`.
</verification>

<rollback>
Revert only this plan's style contract, motion spec, and narrow CSS/UI motion changes. If a rendered motion assertion proves too brittle, replace it with a computed property that still measures the D-08 through D-10 contract before rolling forward.
</rollback>

<success_criteria>
MOTION-01 is enforced by source and rendered checks, default motion remains meaningful and tokenized, reduced motion behaves correctly, disabled controls do not present enabled press feedback, and no new dependency or framework is introduced.
</success_criteria>

<output>
Create `.planning/phases/180-accessibility-verification-guardrails-adversarial-closeout/180-03-SUMMARY.md` when done.
</output>
