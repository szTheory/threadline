---
phase: 59
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
  - lib/threadline/operator_surface/style.ex
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - User can visit /audit/transactions/:id to see a specific transaction
    - Visiting an invalid ID explicitly shows a not-found state
    - Valid transaction renders the bundle header (actor, context)
    - Valid transaction with no changes shows a 'No Changes Recorded' empty state
    - Valid transaction renders row-level changes with virtualization (preventing browser lockup)
    - UI strictly uses .threadline-ui scoped CSS without breaking host application styling
  artifacts:
    - path: lib/threadline/operator_surface/router.ex
      provides: URL routing to TransactionLive
      exports: []
    - path: lib/threadline/operator_surface/live/transaction_live.ex
      provides: Drill-down LiveView and state management
      exports: []
    - path: lib/threadline/operator_surface/style.ex
      provides: Scoped CSS string or component for .threadline-ui
      exports: []
  key_links:
    - from: lib/threadline/operator_surface/router.ex
      to: lib/threadline/operator_surface/live/transaction_live.ex
      via: live_session route
    - from: lib/threadline/operator_surface/live/transaction_live.ex
      to: lib/threadline/investigation.ex
      via: Threadline.incident_bundle/2
---

<objective>
Implement the Incident Drill-down Screen LiveView at `/audit/transactions/:id`.
Purpose: Provide operators with a stable deep-link to view the full context and ordered changes of a specific transaction, using DOM virtualization for massive timelines and scoped CSS for isolated styling.
Output: Route, TransactionLive, and Scoped CSS implementation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-incident-drill-down-screen/59-CONTEXT.md
@.planning/phases/59-incident-drill-down-screen/UI-SPEC.md
@.planning/phases/59-incident-drill-down-screen/DECISIONS.md
@.planning/phases/59-incident-drill-down-screen/PATTERNS.md
@.planning/phases/59-incident-drill-down-screen/59-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Setup and LiveView State Management</name>
  <files>lib/threadline/operator_surface/router.ex, lib/threadline/operator_surface/live/transaction_live.ex, test/threadline/operator_surface/transaction_live_test.exs</files>
  <behavior>
    - Test 1: Renders explicit not-found state for missing transaction ID
    - Test 2: Renders bundle header details (actor, action) for valid transaction ID
    - Test 3: Renders 'No Changes Recorded' empty state when bundle.changes is empty
  </behavior>
  <action>
    1. In `lib/threadline/operator_surface/router.ex`, add `live "/transactions/:id", TransactionLive, :show` inside the `live_session :threadline`.
    2. Create `TransactionLive` with a `mount/3` function matching `{"id" => id}`. Wrap the module definition in `if Code.ensure_loaded?(Phoenix.LiveView) do ... end` to preserve the Phase 57 optional-dependency contract.
    3. Call `Threadline.incident_bundle(id, preload: :action)`. 
    4. On `{:error, :not_found}`, assign a not-found state and render the specific empty state defined in UI-SPEC ("Transaction Not Found - The requested transaction ID does not exist or has been purged by the retention policy.").
    5. On `{:ok, bundle}`, assign the bundle, and use `stream(:changes, bundle.changes)` to initialize the stream. Render the incident bundle header (actor, context).
    6. If `bundle.changes` is empty, render the 'No Changes Recorded' empty state from UI-SPEC.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/transaction_live_test.exs</automated>
  </verify>
  <done>LiveView correctly routes, handles `not_found`, handles empty changes, mounts valid bundle streams, and preserves module gating.</done>
</task>

<task type="auto">
  <name>Task 2: DOM Virtualization and Change Row Rendering</name>
  <files>lib/threadline/operator_surface/live/transaction_live.ex</files>
  <action>
    Implement the continuous-scroll timeline using `phx-viewport` (DOM Virtualization).
    1. Create a scrollable container in the HEEx template with `phx-viewport-top` and `phx-viewport-bottom` handling.
    2. Render the `stream` of `changes` inside the container using `phx-update="stream"`.
    3. For each `change`, display the `change.change_diff` visually: showing `op` ("INSERT", "UPDATE", "DELETE"), `table_name`, `captured_at`, and the `field_changes` (prior state vs after).
    4. Handle the dummy event handlers for viewport limits (to satisfy `phx-viewport` events, e.g., `def handle_event("prev-page", _, socket), do: {:noreply, socket}`).
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/transaction_live_test.exs</automated>
  </verify>
  <done>Virtualization attributes present and row-level diffs render `op`, `table_name`, and `field_changes`.</done>
</task>

<task type="auto">
  <name>Task 3: Scoped CSS Integration</name>
  <files>lib/threadline/operator_surface/live/transaction_live.ex, lib/threadline/operator_surface/style.ex</files>
  <action>
    Implement custom scoped CSS to ensure resilient out-of-the-box UI styling, bypassing `core_components.ex` dependency.
    1. Create `lib/threadline/operator_surface/style.ex`. Wrap the module definition in `if Code.ensure_loaded?(Phoenix.LiveView) do ... end` to preserve the Phase 57 optional-dependency contract. Inside the module, create a function (e.g., `css/0`) that returns the scoped `.threadline-ui` CSS string (or use HEEx `<style>` block). Follow UI-SPEC spacing (4px xs, 8px sm, 16px md), typography (14px body, 12px label), and colors (#FFFFFF main, #F3F4F6 secondary, #3B82F6 accent, #EF4444 destructive).
    2. Inject this style inside the `TransactionLive` template.
    3. Wrap the main render content in a `<div class="threadline-ui">` container.
    4. Ensure all styling (padding, gaps, background colors) in the HEEx template relies on these scoped classes instead of Tailwind classes from the host.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/transaction_live_test.exs</automated>
  </verify>
  <done>Scoped CSS string is generated and injected, the UI container is wrapped in `.threadline-ui`, and module gating is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> UI | URL parameter `:id` directly fetches database records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Information Disclosure | TransactionLive | mitigate | Ensure URL param is safely passed to `incident_bundle/2` (which handles secure lookup). Route remains gated by the existing `Auth` on_mount hook. |
</threat_model>

<verification>
mix test test/threadline/operator_surface/transaction_live_test.exs
</verification>

<success_criteria>
LiveView mounts on `/audit/transactions/:id`, handles not found and zero-changes empty states, streams changes with DOM virtualization, and styles itself independently using scoped CSS while preserving optional dependencies via module gating.
</success_criteria>

<output>
After completion, create `.planning/phases/59-incident-drill-down-screen/59-01-SUMMARY.md`
</output>