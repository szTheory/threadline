---
phase: 79-scale-adapters
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - lib/threadline/export_queue/oban.ex
  - test/threadline/export_queue/oban_test.exs
autonomous: true
requirements:
  - ADAPT-01

must_haves:
  truths:
    - "System can enqueue background exports using Oban when configured"
    - "Oban adapter fails fast with a clear error message if :oban dependency is missing"
    - "Oban Worker compiles conditionally without crashing when :oban is absent"
  artifacts:
    - path: "lib/threadline/export_queue/oban.ex"
      provides: "Oban ExportQueue adapter and worker"
      contains: "Code.ensure_loaded?(Oban)"
    - path: "test/threadline/export_queue/oban_test.exs"
      provides: "Tests for Oban adapter enqueue and safeguards"
  key_links:
    - from: "lib/threadline/export_queue/oban.ex"
      to: "Oban"
      via: "enqueue calls Oban.insert()"
---

<objective>
Implement the Oban Queue Adapter for background export tasks.

Purpose: Provide an enterprise-grade scaling option for multi-node setups by allowing adopters to use Oban for running export jobs. We must ensure that the worker is conditionally compiled so that non-Oban users do not encounter compile-time errors.
Output: A new `Threadline.ExportQueue.Oban` adapter and corresponding tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-scale-adapters/79-RESEARCH.md
@.planning/phases/79-scale-adapters/79-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Oban Queue Adapter</name>
  <files>
    lib/threadline/export_queue/oban.ex
    test/threadline/export_queue/oban_test.exs
  </files>
  <behavior>
    - Test 1: `init/1` returns `:ok` when `:oban` is loaded (mock this or test in an environment where it's loaded).
    - Test 2: `enqueue/2` delegates to `Oban.insert/1` with a properly structured job.
  </behavior>
  <action>
    Create `Threadline.ExportQueue.Oban`.
    - Implement `init/1` using `Code.ensure_loaded?(Oban)`. If missing, raise a clear error string containing `{:oban, "~> 2.15"}`.
    - Implement `enqueue/2` behavior that builds an `ObanWorker` struct and inserts it via `Oban.insert/1`.
    
    In the same file (or a properly placed sibling file), conditionally define `Threadline.ExportQueue.ObanWorker` wrapped in `if Code.ensure_loaded?(Oban) do`. 
    - The worker should `use Oban.Worker, queue: :threadline_exports, max_attempts: 3`
    - Its `perform/1` should call `Threadline.Export.Orchestrator.run(job_id)`

    Write ExUnit tests for `enqueue/2` to ensure proper Oban job arguments are passed.
  </action>
  <verify>
    <automated>mix test test/threadline/export_queue/oban_test.exs</automated>
  </verify>
  <done>Adapter properly enqueues Oban jobs and raises on missing dependency; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Job queue | Export jobs are submitted to the internal database-backed queue (Oban). They only contain standard `job_id` references, no PII directly in queue payload. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02 | Denial of Service | `ObanWorker` | mitigate | `max_attempts: 3` prevents infinite retry loops on failing export payloads. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test test/threadline/export_queue/oban_test.exs
</verification>

<success_criteria>
- Adapter compiles safely without Oban loaded.
- `init/1` correctly raises if required deps missing.
- Enqueue creates a valid Oban job record with `job_id`.
</success_criteria>

<output>
After completion, create `.planning/phases/79-scale-adapters/79-02-SUMMARY.md`
</output>
