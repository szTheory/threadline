---
phase: 79-scale-adapters
plan: 03
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - lib/threadline/storage/s3.ex
  - test/threadline/storage/s3_test.exs
autonomous: true
requirements:
  - ADAPT-02

must_haves:
  truths:
    - "System can store exports to S3 when configured"
    - "S3 adapter fails fast with a clear error message if :ex_aws_s3 or related dependencies are missing"
    - "System can generate short-lived presigned URLs for secure S3 downloads"
  artifacts:
    - path: "lib/threadline/storage/s3.ex"
      provides: "S3 Storage adapter"
      contains: "Code.ensure_loaded?(ExAws.S3)"
    - path: "test/threadline/storage/s3_test.exs"
      provides: "Tests for S3 adapter put, get, and presigned url generation"
  key_links:
    - from: "lib/threadline/storage/s3.ex"
      to: "ExAws.S3"
      via: "calls to ExAws.S3.put_object and presigned_url"
---

<objective>
Implement the S3 Storage Adapter for managing distributed exports.

Purpose: Allow multi-node deployments to push background exports to shared cloud storage (AWS S3 or S3-compatible APIs) so that generated files are accessible across the cluster via presigned URLs.
Output: A new `Threadline.Storage.S3` adapter that satisfies `Threadline.Storage` and provides initialization safeguards.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-scale-adapters/79-RESEARCH.md
@.planning/phases/79-scale-adapters/79-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement S3 Storage Adapter</name>
  <files>
    <file analog="lib/threadline/storage/local.ex">lib/threadline/storage/s3.ex</file>
    test/threadline/storage/s3_test.exs
  </files>
  <behavior>
    - Test 1: `init/1` returns `:ok` when `:ex_aws_s3` is available, raises clear error otherwise.
    - Test 2: `put/2` calls `ExAws.S3.put_object/3` (mocked) and returns `{:ok, file_id}`.
    - Test 3: `download_url/2` successfully returns a presigned URL using `ExAws.S3.presigned_url`.
  </behavior>
  <action>
    Create `Threadline.Storage.S3`.
    - Implement `init/1` to ensure `Code.ensure_loaded?(ExAws.S3)`. If not, raise an error specifying that `ex_aws`, `ex_aws_s3`, `hackney`, and `sweet_xml` must be in `mix.exs`.
    - Implement `put/2` to push CSV contents or files to S3 via `ExAws.S3.put_object/3`. Bucket should be configurable via `opts`. File ID should default to `Ecto.UUID.generate() <> ".csv"`.
    - Implement `get/1` via `ExAws.S3.get_object`.
    - Implement `path/1` to return `{:error, :not_local}` since S3 files do not have local paths.
    - Implement `download_url/2` to generate a presigned URL using `ExAws.S3.presigned_url`. Ensure short-lived defaults (e.g. 15 minutes / 900 seconds) if not provided in `opts`.
    - Implement `delete/1` via `ExAws.S3.delete_object`.

    Write tests covering the adapter callbacks. Use Mox or a mock module for ExAws if needed, or stick to unit testing the struct building if possible.
  </action>
  <verify>
    <automated>mix test test/threadline/storage/s3_test.exs</automated>
  </verify>
  <done>S3 adapter correctly interacts with ExAws and passes tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Cloud Storage API | S3 adapter accesses cloud object storage using ExAws. Uses SigV4 authentication. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03 | Information Disclosure | `S3.download_url` | mitigate | Generate short-lived presigned URLs (15 min default) instead of public ACLs or open buckets. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test test/threadline/storage/s3_test.exs
</verification>

<success_criteria>
- `init/1` raises appropriately if `ExAws.S3` is not loaded.
- Adapter supports `put`, `get`, `download_url`, `delete`.
- Download URLs are presigned to prevent unauthenticated access.
</success_criteria>

<output>
After completion, create `.planning/phases/79-scale-adapters/79-03-SUMMARY.md`
</output>
