---
wave: 1
depends_on: []
files_modified:
  - lib/threadline/query.ex
  - lib/threadline.ex
  - test/threadline/query_test.exs
autonomous: true
requirements_addressed: [XPLO-02]
---

# Plan 32-01: Transaction-scoped `AuditChange` listing

**Phase:** 32 — Transaction-scoped change listing  
**Plan:** 32-01 of 01  
**Status:** Ready to execute

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-32-01 | **Injection / type confusion:** non-UUID `transaction_id` passed to Postgrex produces opaque driver errors | medium | Validate with `Ecto.UUID.cast/1` before query; raise `ArgumentError` with message containing **`invalid audit transaction id`** on `:error`. |
| T-32-02 | **Information disclosure:** implied “transaction exists” signal via different empty vs error shapes | low | Return **`[]`** for well-formed UUID with zero rows (per **32-CONTEXT** D-4); never return `{:error, :not_found}` tuples from this API. |

---

## Objective

Ship **XPLO-02**: `Threadline.Query.audit_changes_for_transaction/2` and **`Threadline.audit_changes_for_transaction/2`** with required **`opts[:repo]`**, stable ordering **identical to `timeline/2`** tie-break (`captured_at` DESC, `id` DESC), documented in `@moduledoc`; integration-style tests with **multiple** changes per transaction and delegator parity.

---

## must_haves

- [ ] `@spec` + `@doc` on `Threadline.Query.audit_changes_for_transaction/2` stating: argument is **`audit_transactions.id`** (same as `AuditChange.transaction_id`); ordering is **total order on `(captured_at, id)`** descending; **`binary_id` is not a time sequence`**; **`[]`** when no rows; **`ArgumentError`** for invalid UUID shape.
- [ ] `Threadline` module exposes the **same** function name delegating to `Threadline.Query` (mirror **`history/3`** style, not `defdelegate` required).
- [ ] `mix test test/threadline/query_test.exs` exits **0**.
- [ ] `mix compile --warnings-as-errors` exits **0**.

---

## Task 32-01-01 — `Threadline.Query.audit_changes_for_transaction/2`

<read_first>
- `.planning/phases/32-transaction-scoped-change-listing/32-CONTEXT.md` (D-1 through D-5)
- `lib/threadline/query.ex` (`timeline_order/1`, `history/3`, moduledoc “See also” area)
- `lib/threadline/capture/audit_change.ex` (`transaction_id`, `@primary_key`)
</read_first>

<action>
1. Add **`@doc`** / **`@spec`** for **`audit_changes_for_transaction(transaction_id, opts)`** where **`transaction_id`** is **`Ecto.UUID`** raw type (binary or UTF-8 string per what `AuditChange` stores — match **`cast`** acceptance: use `Ecto.UUID.cast/1` which accepts both string and `<<>>` **or** document binary-only; prefer **string OR binary** if `cast` supports both for integrator convenience).
2. If **`Ecto.UUID.cast(transaction_id) == :error`**, raise **`ArgumentError`** with substring **`invalid audit transaction id`**.
3. **`repo = Keyword.fetch!(opts, :repo)`** — same error semantics as **`history/3`** when `:repo` missing.
4. Build **`from(ac in AuditChange)`** (or **`AuditChange`** pipe), **`where(ac.transaction_id == ^validated_uuid)`** using the **cast** output form `Ecto.UUID.dump!/1` or the canonical binary form Ecto expects — use the same type the schema column uses (**`:binary_id`**); typically **`^Ecto.UUID.cast!(transaction_id)`** after cast check.
5. Pipe query through existing **`timeline_order/1`** private function so ordering cannot drift from **`timeline/2`**.
6. **`results = repo.all(query)`**.
7. If **`opts[:preload]`** is **`nil`** or **`[]`**: return **`results`** unchanged. If **non-empty list**: return **`repo.preload(results, opts[:preload])`** (document in `@doc` that only **`[:transaction]`** is the intended use in Phase 32).
8. Add **`@moduledoc`** subsection **“See also”** bullet for **`audit_changes_for_transaction/2`** (one line: transaction drill-down vs **`timeline/2`**).
</action>

<acceptance_criteria>
- `grep -q 'def audit_changes_for_transaction' lib/threadline/query.ex` exits 0.
- `grep -q 'timeline_order' lib/threadline/query.ex` exits 0 (still present; caller uses it for this query path).
- `grep -q 'invalid audit transaction id' lib/threadline/query.ex` exits 0.
- `grep -q 'audit_changes_for_transaction' lib/threadline/query.ex` exits 0.
- `mix compile --warnings-as-errors` exits 0.
</acceptance_criteria>

---

## Task 32-01-02 — `Threadline` delegator + `QueryTest`

<read_first>
- `lib/threadline.ex` (`history/3` delegator lines)
- `test/threadline/query_test.exs` (`insert_transaction/1`, `insert_change/2`, first describe block style)
- `lib/threadline/query.ex` (new function body from task 32-01-01)
</read_first>

<action>
1. In **`lib/threadline.ex`**, add **`def audit_changes_for_transaction(transaction_id, opts), do: Threadline.Query.audit_changes_for_transaction(transaction_id, opts)`** immediately after **`timeline/2`** (or adjacent to other **`Query`** delegators) with **`@doc`** paragraph referencing **`Threadline.Query.audit_changes_for_transaction/2`** for options and ordering (at least **`:repo`**, optional **`:preload`**).
2. In **`test/threadline/query_test.exs`**, add **`describe "audit_changes_for_transaction/2 — XPLO-02"`** with tests:
   - **Multi-change ordering:** insert **one** `AuditTransaction`, then **two** `AuditChange` rows with same **`transaction_id`**, **`captured_at`** values **`t_old`** and **`t_new`** where **`DateTime.compare(t_new, t_old) == :gt`**; call **`Threadline.Query.audit_changes_for_transaction(txn.id, repo: @repo)`**; assert **length == 2**; assert **`hd(results).captured_at`** equals **`t_new`** (newer first).
   - **Delegator parity:** assert **`Threadline.audit_changes_for_transaction(txn.id, repo: @repo)`** **`==`** **`Threadline.Query.audit_changes_for_transaction(txn.id, repo: @repo)`** for the same fixture.
   - **Empty set:** well-formed UUID with **no** rows — `Ecto.UUID.generate()` — assert **`Threadline.audit_changes_for_transaction(uuid, repo: @repo) == []`**.
   - **Malformed id:** **`assert_raise ArgumentError, ~r/invalid audit transaction id/, fn -> Threadline.audit_changes_for_transaction("not-a-uuid", repo: @repo) end`** (adjust regex if message casing differs; must match substring from task 32-01-01).
3. Optional fourth test: **`[preload: [:transaction]]`** merged into opts on the multi-change fixture — assert **`hd(results).transaction.__struct__`** is **`Threadline.Capture.AuditTransaction`** (only if preload implementation exists from 32-01-01).
</action>

<acceptance_criteria>
- `grep -q 'audit_changes_for_transaction' lib/threadline.ex` exits 0.
- `grep -q 'XPLO-02' test/threadline/query_test.exs` exits 0.
- `mix test test/threadline/query_test.exs` exits 0.
- `mix format --check-formatted` exits 0 (run **`mix format`** if needed).
</acceptance_criteria>

---

## Verification

1. `mix format`
2. `mix compile --warnings-as-errors`
3. `mix test test/threadline/query_test.exs`

---

## Success criteria

- Roadmap item **4–5** for Phase 32 satisfied: public **`Threadline.Query`** + **`Threadline`** entrypoints, documented stable order, integration/repo test for multi-change ordering.

## PLANNING COMPLETE
