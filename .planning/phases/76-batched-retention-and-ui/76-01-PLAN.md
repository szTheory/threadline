---
phase: 76
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/retention.ex
  - lib/threadline/retention/pruner.ex
  - test/threadline/retention_test.exs
  - test/threadline/retention/pruner_test.exs
autonomous: true
requirements:
  - RET-01
  - RET-02
must_haves:
  truths:
    - Retention pruner deletes expired rows in chunked batches and sleeps to yield DB locks.
    - Start time, completion time, duration, and deleted row counts are tracked in the database.
    - A PostgreSQL advisory lock ensures only one node executes pruning concurrently.
  artifacts:
    - path: lib/threadline/retention.ex
      provides: Batched deletion loop with run tracking and sleep.
    - path: lib/threadline/retention/pruner.ex
      provides: GenServer scheduler that maintains a DB lock and calls purge.
      analog: lib/threadline/retention.ex
  key_links:
    - from: lib/threadline/retention/pruner.ex
      to: lib/threadline/retention.ex
      via: Threadline.Retention.purge/1 call
    - from: lib/threadline/retention.ex
      to: threadline_retention_runs
      via: Repo.insert! and Repo.update!
---

<objective>
Implement autovacuum-aware retention pruning and a background scheduler to safely delete old records and track execution.

Purpose: Protect database availability by yielding locks during massive deletions, and provide an audit trail of deleted rows via `threadline_retention_runs`.
Output: Updated `Threadline.Retention` module with run tracking, and a new `Threadline.Retention.Pruner` GenServer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/76-batched-retention-and-ui/76-RESEARCH.md
@.planning/phases/76-batched-retention-and-ui/76-PATTERNS.md
@lib/threadline/governance/retention_run.ex

<interfaces>
From lib/threadline/governance/retention_run.ex:
```elixir
defmodule Threadline.Governance.RetentionRun do
  schema "threadline_retention_runs" do
    field(:status, :string)
    field(:deleted_count, :integer)
    field(:duration_ms, :integer)
    field(:error_message, :string)
    field(:started_at, :utc_datetime_usec)
    field(:completed_at, :utc_datetime_usec)
  end
  def changeset(run \\ %__MODULE__{}, attrs)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core Batched Pruning and Run Tracking</name>
  <files>lib/threadline/retention.ex, test/threadline/retention_test.exs</files>
  <behavior>
    - Test 1: Given a `sleep_ms` opt > 0, the `purge_loop` correctly sleeps between batches without blocking.
    - Test 2: Purging records inserts a new `Threadline.Governance.RetentionRun` on start with status "running", and updates it to "completed" with `deleted_count` and `duration_ms` when finished.
  </behavior>
  <action>
    Update `Threadline.Retention.purge/1` and `purge_loop/5`.
    1. Add a `:sleep_ms` option (default `50`).
    2. Wrap the chunked loops in an insert to `Threadline.Governance.RetentionRun` (status: "running", started_at).
    3. Update `purge_loop` to conditionally call `Process.sleep(sleep_ms)` if `sleep_ms > 0` and the batch deleted rows.
    4. Upon completion, update the run record with status "completed", total `deleted_count` (changes + txns), `duration_ms`, and `completed_at`.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/threadline/retention_test.exs</automated>
  </verify>
  <done>Retention deletions correctly insert/update tracking records and conditionally yield locks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Background Scheduler GenServer</name>
  <files>lib/threadline/retention/pruner.ex, test/threadline/retention/pruner_test.exs</files>
  <behavior>
    - Test 1: GenServer schedules itself and attempts to acquire an advisory lock via Ecto before running `Retention.purge/1`.
    - Test 2: If the lock is held elsewhere, it skips purging and reschedules.
    - Test 3: On `init`, marks any "running" RetentionRuns older than 24 hours as "failed" (abandoned).
  </behavior>
  <action>
    Create `Threadline.Retention.Pruner`. Follow the pattern mapped in PATTERNS.md for analog `lib/threadline/retention.ex` regarding batch looping and DB operations.
    1. Implement a GenServer that accepts `repo`, `interval_ms`, and `sleep_ms` in init.
    2. On mount/init, query `RetentionRun` where `status == "running"` and `started_at` < 24h ago, updating them to `status: "failed", error_message: "Abandoned"`.
    3. Periodically cast a message to itself. When handled, acquire lock `pg_try_advisory_lock` using a hash of "threadline_retention_pruner".
    4. If acquired, run `Threadline.Retention.purge(repo: repo, sleep_ms: sleep_ms)`. Use `after` block for `pg_advisory_unlock`.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/threadline/retention/pruner_test.exs</automated>
  </verify>
  <done>Pruner schedules executions and guards against concurrent multi-node runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB -> Pruner | DB locks and table changes execute with elevated background privileges |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Denial of Service | `Threadline.Retention` | mitigate | Implement default `sleep_ms: 50` and chunk size limits to prevent DB lock exhaustion during purge. |
| T-76-02 | Elevation of Priv | `Threadline.Retention.Pruner` | accept | GenServer operates under application's Ecto credentials which are authorized to DELETE. |
</threat_model>

<verification>
- Retention runs are correctly captured in the database with accurate counts.
- Heavy deletions don't cause DB lockouts.
- Only one node runs the pruner at any given time.
</verification>

<success_criteria>
Pruning loop successfully sleeps between batches and run history is permanently stored in `threadline_retention_runs`.
</success_criteria>

<output>
After completion, create `.planning/phases/76-batched-retention-and-ui/76-01-SUMMARY.md`
</output>