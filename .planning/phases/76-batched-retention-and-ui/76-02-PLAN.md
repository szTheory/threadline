---
phase: 76
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/components/surface_header.ex
  - lib/threadline/operator_surface/live/retention_history_live.ex
  - test/threadline/operator_surface/live/retention_history_live_test.exs
autonomous: true
requirements:
  - RET-03
must_haves:
  truths:
    - Operator can view a history of past and active retention runs.
    - Operator can manually trigger a pruning batch.
    - Empty state displayed when no retention history exists.
  artifacts:
    - path: lib/threadline/operator_surface/live/retention_history_live.ex
      provides: LiveView monitoring interface for retention runs.
    - path: lib/threadline/operator_surface/router.ex
      provides: Route to the retention LiveView.
  key_links:
    - from: lib/threadline/operator_surface/router.ex
      to: lib/threadline/operator_surface/live/retention_history_live.ex
      via: live "/policy/retention" mapping
    - from: lib/threadline/operator_surface/components/surface_header.ex
      to: /policy/retention
      via: Header navigation badge link
    - from: lib/threadline/operator_surface/live/retention_history_live.ex
      to: lib/threadline/retention/pruner.ex
      via: GenServer.cast/2
---

<objective>
Provide a "Retention History" LiveView page inside the operator surface to monitor active pruning and past runs, and allow operators to trigger a manual batch.

Purpose: Operators need visibility into when and how much data is being pruned to ensure the retention policy is running effectively.
Output: A new LiveView page and updated router/header navigation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/76-batched-retention-and-ui/76-RESEARCH.md
@.planning/phases/76-batched-retention-and-ui/76-PATTERNS.md
@.planning/phases/76-batched-retention-and-ui/76-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Navigation and Routing</name>
  <files>lib/threadline/operator_surface/router.ex, lib/threadline/operator_surface/components/surface_header.ex</files>
  <behavior>
    - The `/policy/retention` route is properly mounted in `live_session :threadline`.
    - The surface header renders a link to the retention page.
  </behavior>
  <action>
    1. In `router.ex`, add `live("/policy/retention", RetentionHistoryLive, :index)` inside the `live_session :threadline` scope block.
    2. In `surface_header.ex`, add a navigation badge for Retention History next to the coverage badge: `<a class="surface-badge surface-badge--ok" href={"#{@base_path}/policy/retention"}>Retention</a>`.
  </action>
  <verify>
    <automated>MIX_ENV=test mix compile</automated>
  </verify>
  <done>Retention route exists and is linked from the main surface header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retention History LiveView</name>
  <files>lib/threadline/operator_surface/live/retention_history_live.ex, test/threadline/operator_surface/live/retention_history_live_test.exs</files>
  <behavior>
    - Test 1: Mount queries the latest `RetentionRun` records (limit 100) and displays them in a table.
    - Test 2: When 0 runs exist, the empty state displays "No Retention History" per UI-SPEC.
    - Test 3: The "Run Pruning Batch" primary CTA triggers a manual `GenServer.cast(Threadline.Retention.Pruner, :prune)` asynchronously.
    - Test 4: Page auto-refreshes periodically using `Process.send_after` to update run status (following coverage_live.ex pattern).
  </behavior>
  <action>
    Create `RetentionHistoryLive`.
    1. Implement mount: fetch `Threadline.Governance.RetentionRun` records, order by `started_at` descending. Use LiveView `stream` to assign the list of runs to the socket, ensuring no flickering on refresh.
    2. Render a table with Status, Deleted Rows, Duration, and Date. Follow the exact copywriting from `76-UI-SPEC.md` for empty states and CTAs.
    3. Implement `handle_event("prune_now", ...)` which asynchronously calls `GenServer.cast(Threadline.Retention.Pruner, :prune)` and redirects/updates the UI. Use `<button class="primary" phx-click="prune_now" data-confirm="Prune: Are you sure you want to run a pruning batch? This permanently deletes older records.">Run Pruning Batch</button>`.
    4. Implement auto-refresh via `handle_info(:refresh, ...)` sending `Process.send_after` on interval, similar to `coverage_live.ex`. Ensure you use `stream_insert` to update records progressively.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/threadline/operator_surface/live/retention_history_live_test.exs</automated>
  </verify>
  <done>Operator can view the list of retention runs and manually trigger pruning with the new UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Operator -> HTTP | Requests cross into the live_session which requires authorization |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03 | Elevation of Priv | `RetentionHistoryLive` | mitigate | Relies on the host `authorize_fn` enforced by `threadline_operator_surface` router macro. |
| T-76-04 | Spoofing | DB queries | accept | Only reads `threadline_retention_runs` metadata; no PII exposure. |
</threat_model>

<verification>
- Navigate to `/policy/retention` successfully.
- Triggering "Run Pruning Batch" successfully spawns a run and updates the table.
</verification>

<success_criteria>
Operators have full visibility into past and active pruning processes via the LiveView interface.
</success_criteria>

<output>
After completion, create `.planning/phases/76-batched-retention-and-ui/76-02-SUMMARY.md`
</output>