---
phase: 172
plan: 02
type: execute
wave: 2
depends_on: [172-01]
files_modified:
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
  - lib/threadline/operator_surface/controllers/theme_controller.ex
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/auth.ex
  - lib/threadline/operator_surface/components/surface_header.ex
autonomous: true
requirements: [DS-05]
must_haves:
  truths:
    - Transaction page is max-width constrained and centered on wide monitors.
    - Coverage card grouping is flattened without nested borders.
    - Users can toggle between Dark, Light, and System themes without JS FOUC.
    - Selected theme is persisted across sessions via a cookie.
  artifacts:
    - path: lib/threadline/operator_surface/controllers/theme_controller.ex
      provides: Endpoint to set tl_theme cookie
    - path: lib/threadline/operator_surface/router.ex
      provides: Route for theme toggle endpoint
  key_links:
    - from: lib/threadline/operator_surface/components/surface_header.ex
      to: /theme
      via: Form submission on segmented control change
---

<objective>
Fix operator surface layout bugs and implement a zero-JS persistent theme picker.
Purpose: Fix the transaction page left-push, declutter coverage cards, and allow users to select dark/light/system theme per DS-05.
Output: Max-width layout container, flattened coverage card styles, new ThemeController, and header theme picker UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-foundations-audit-hardening/172-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Layout Declutter & Centering</name>
  <read_first>
    - lib/threadline/operator_surface/style.ex
    - lib/threadline/operator_surface/live/transaction_live.ex
  </read_first>
  <files>
    - lib/threadline/operator_surface/style.ex
    - lib/threadline/operator_surface/live/transaction_live.ex
  </files>
  <action>
    - In `style.ex`, add a `.tl-container` CSS class that enforces centering on wide monitors: `max-width: 1000px; margin: 0 auto;`.
    - Also in `style.ex`, update the `.tl-coverage-command` class to remove its nested card styling (remove `border`, `background`, `border-radius`, `padding`, and `box-shadow`) so it sits directly on the page background to reduce visual clutter.
    - In `transaction_live.ex`, append the `tl-container` class to the main `<main id="tl-main" class="tl-page">` container (so it becomes `class="tl-page tl-container"`) to ensure content is centered on ultra-wide screens.
  </action>
  <verify>
    <automated>grep -q "max-width: 1000px" lib/threadline/operator_surface/style.ex</automated>
  </verify>
  <done>
    - `grep -q "max-width: 1000px" lib/threadline/operator_surface/style.ex`
    - `grep -q "tl-container" lib/threadline/operator_surface/live/transaction_live.ex`
    - `.tl-coverage-command` block in `style.ex` no longer contains `border:`, `background:`, or `box-shadow:` declarations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Theme Persistence Endpoint & Auth</name>
  <read_first>
    - lib/threadline/operator_surface/router.ex
    - lib/threadline/operator_surface/auth.ex
  </read_first>
  <files>
    - lib/threadline/operator_surface/controllers/theme_controller.ex
    - lib/threadline/operator_surface/router.ex
    - lib/threadline/operator_surface/auth.ex
  </files>
  <action>
    - Create `lib/threadline/operator_surface/controllers/theme_controller.ex` defining `Threadline.OperatorSurface.Controllers.ThemeController`. Add an `update(conn, %{"theme" => theme})` action. Validate `theme` is in `["light", "dark", "system"]`, save it to the session (`put_session(conn, :tl_theme, theme)`), write it to a cookie for SSR consistency (`put_resp_cookie(conn, "tl_theme", theme, path: "/")`), and redirect back to the referer (`get_req_header(conn, "referer") |> List.first() || "/"`).
    - In `router.ex`, inside the `scope unquote(path)` macro block (alongside `live_session` or the `exports` endpoints block), add `post("/theme", Threadline.OperatorSurface.Controllers.ThemeController, :update)`.
    - In `auth.ex` (`on_mount/4`), check for `session_theme = session["tl_theme"] || session[:tl_theme]`. Use this `session_theme` over the default `Keyword.get(opts, :theme, :dark)` when determining the final theme.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>
    - `ThemeController` exists and implements `update/2`.
    - `router.ex` exposes `POST /theme`.
    - `Auth.on_mount/4` extracts `:tl_theme` from the session map.
  </done>
</task>

<task type="auto">
  <name>Task 3: Theme Toggle UI</name>
  <read_first>
    - lib/threadline/operator_surface/components/surface_header.ex
    - lib/threadline/operator_surface/style.ex
  </read_first>
  <files>
    - lib/threadline/operator_surface/components/surface_header.ex
    - lib/threadline/operator_surface/style.ex
  </files>
  <action>
    - In `surface_header.ex`, add an idiomatic theme picker (System / Light / Dark radio group or select) to the topbar or nav shell area.
    - Implement the toggle as a standard HTML form: `<form action={"#{@base_path}/theme"} method="post" class="tl-theme-picker">`. Include the CSRF token (e.g., using `<input type="hidden" name="_csrf_token" value={Phoenix.HTML.Tag.csrf_token_value()} />`).
    - Add a `<noscript>` submit button (or a styled visible submit button if preferred for JS-less interaction) so it submits without inline JS, respecting strict CSP rules. 
    - Read the current theme from socket assigns (`@threadline_theme` must be passed down if not available, or assume a default; since it's a LiveView, the layout or parent component may need to pass `@threadline_theme` to the header. Check assigns available to `surface_header`).
    - In `style.ex`, add basic CSS for `.tl-theme-picker` so it sits flush in the header without breaking the brand aesthetics.
  </action>
  <verify>
    <automated>grep -q "/theme" lib/threadline/operator_surface/components/surface_header.ex</automated>
  </verify>
  <done>
    - `surface_header.ex` outputs a form pointing to `#{@base_path}/theme`.
    - The form uses standard HTTP `POST` submission.
    - Select/radio inputs auto-submit on change.
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThemeController | Handles user-provided string input for the theme cookie. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-02 | Tampering | ThemeController | mitigate | Validate that the `theme` param strictly equals "light", "dark", or "system" before putting it in the session/cookie. |
| T-172-03 | Elevation of Privilege | ThemeController route | accept | Protected by the same pipeline/macro as the rest of the operator surface. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test
</verification>

<success_criteria>
Transaction layout is centered, coverage layout is flattened, and operators can persist their theme selection securely via HTTP POST.
</success_criteria>

<output>
Create `.planning/phases/172-foundations-audit-hardening/172-02-SUMMARY.md` when done
</output>