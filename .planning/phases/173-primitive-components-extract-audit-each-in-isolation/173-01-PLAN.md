---
phase: 173
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/ui.ex
  - test/threadline/operator_surface/ui_test.exs
autonomous: true
requirements:
  - COMP-01
  - COMP-03
must_haves:
  truths:
    - "Internal private function components exist for the atom primitive set"
    - "Primitives are documented with attrs/slots and expose no public API"
  artifacts:
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "Unified UI components module"
    - path: "test/threadline/operator_surface/ui_test.exs"
      provides: "Unit tests for UI atom primitives"
  key_links: []
---

<objective>
Create the unified `Threadline.OperatorSurface.UI` module and implement the foundational atom primitives (button, link, badge, alert, divider, spinner, avatar).

Purpose: Extract hard-coded HTML into a single private component module matching Phoenix's `core_components.ex` idiom with strict validation.
Output: `ui.ex` with basic primitives and `ui_test.exs` test suite.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/173-primitive-components-extract-audit-each-in-isolation/173-CONTEXT.md
@.planning/phases/173-primitive-components-extract-audit-each-in-isolation/173-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold UI module and implement button/link atoms</name>
  <files>lib/threadline/operator_surface/ui.ex, test/threadline/operator_surface/ui_test.exs</files>
  <behavior>
    - Test 1: ui.ex is a private module (`@moduledoc false`)
    - Test 2: button component renders with correct `tl-button` class and explicit typing
    - Test 3: link component renders with correct interactive classes
    - Test 4: icon-button component renders correctly
  </behavior>
  <action>Create the unified `Threadline.OperatorSurface.UI` module using `Phoenix.Component`. Define explicit `attr` and `slot` contracts for `button`, `icon_button`, and `link` primitives. Ensure `@doc false` on all components to prevent public API exposure per D-01 and D-02.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <done>Button and link atoms implemented with strict validation and passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement badge, alert, divider, spinner, and avatar</name>
  <files>lib/threadline/operator_surface/ui.ex, test/threadline/operator_surface/ui_test.exs</files>
  <behavior>
    - Test 1: badge/chip/tag/pill renders with static classes and no misleading affordances (no cursor-pointer if unlinked)
    - Test 2: alert/banner/callout renders with appropriate state variants
    - Test 3: divider, spinner/progress/skeleton, and avatar components render properly
  </behavior>
  <action>Add the remaining atom primitives to `Threadline.OperatorSurface.UI`: `badge`, `alert`, `divider`, `spinner`, `avatar`. Use typed `attr` validations. Apply strictly pure CSS handling for `:hover`, `:focus-visible`, `:active`, and `:disabled` interaction states.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <done>All atom primitives from COMP-01 exist in `ui.ex` and pass rendering unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component APIs | Internal use only; no untrusted input crosses a security boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-01 | Information Disclosure | `Threadline.OperatorSurface.UI` | accept | Components are private and only mounted inside authenticated `/audit` operator views. |
</threat_model>

<verification>
- `ui.ex` exists and contains `@moduledoc false` and `@doc false` on all functions
- `ui_test.exs` runs and passes
</verification>

<success_criteria>
Core UI atom primitives exist as strictly validated internal functions without a public API.
</success_criteria>

<output>
Create `.planning/phases/173-primitive-components-extract-audit-each-in-isolation/173-01-SUMMARY.md` when done
</output>