---
phase: 121-adopter-doc-neutrality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/getting-started-saas.md
  - guides/upgrade-path.md
  - test/threadline/getting_started_saas_doc_contract_test.exs
autonomous: true
requirements:
  - ADOPT-AUTH-01
must_haves:
  truths:
    - "Getting-started §5 opens with host-owned Threadline.Plug wiring, not Sigra as default."
    - "Sigra router excerpt lives only in optional sigra-reference subsection."
    - "§6 teaches generic auth-before-plug contract; runnable curl is labeled sigra-reference example only."
    - "Next reads links phx-gen-auth guide alongside sigra."
    - "getting_started_saas_doc_contract_test.exs passes without whole-guide router_block assert."
  artifacts:
    - path: guides/getting-started-saas.md
      provides: "auth-neutral §5/§6 and lane pointers"
    - path: test/threadline/getting_started_saas_doc_contract_test.exs
      provides: "neutrality doc-contract locks"
---

<objective>
Rewrite `guides/getting-started-saas.md` §5 and §6 for auth-neutral first-hour adoption (ADOPT-AUTH-01, D-121-04 through D-121-12, D-121-23, D-121-24).

Purpose: Adopters wire host callbacks first; Sigra and phx are peer optional reference lanes.
Output: Neutral hero plug fence, optional Sigra subsection, collapsed sigra-reference curl, updated doc contract.
</objective>

<context>
@.planning/phases/121-adopter-doc-neutrality/121-CONTEXT.md (D-121-01 through D-121-12, D-121-23, D-121-24)
@.planning/phases/121-adopter-doc-neutrality/121-RESEARCH.md
@guides/integration-contracts.md (§ Request path via Threadline.Plug)
@guides/integrations/phx-gen-auth.md
@guides/integrations/sigra.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex (router-pipeline-actor-fn anchor)
@test/support/getting_started_fixtures.ex
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-121-01 | §5 still implies Sigra required | high | refute Sigra adapter in §5 body; generic plug fence first |
| T-121-02 | §6 curl read as universal login path | medium | `<details>` + sigra-reference label + example README link |
| T-121-05 | Doc contract still asserts Sigra router on whole guide | medium | scope router_block to optional subsection marker only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite §5 for generic plug + lane pointers</name>
  <files>guides/getting-started-saas.md</files>
  <read_first>
    - guides/getting-started-saas.md (## 5. Wire `Threadline.Plug`...)
    - guides/integration-contracts.md (lines 18–47)
    - .planning/phases/121-adopter-doc-neutrality/121-CONTEXT.md (D-121-04 through D-121-09)
    - guides/integrations/phx-gen-auth.md (MyApp.AuditActor naming)
  </read_first>
  <action>
Edit `guides/getting-started-saas.md` **§5** (`## 5. Wire \`Threadline.Plug\`...`):

1. **Remove** opening: "The Phoenix example keeps request capture small and explicit by wiring both Sigra callbacks directly into `Threadline.Plug`:"

2. **Replace** with two-tier opening: host establishes identity on `conn`, then wires `Threadline.Plug` with host callbacks (Threadline does not own auth).

3. **Primary code fence** (hero) — match integration-contracts verbatim module names:

   ```elixir
   plug Threadline.Plug,
     actor_fn: &MyApp.Audit.actor_ref_from_conn/1,
     context_overrides_fn: &MyApp.Audit.audit_context_overrides_from_conn/1
   ```

4. Add one sentence: `guides/integrations/phx-gen-auth.md` uses `MyApp.AuditActor` for the same two callbacks — rename the module freely in your app.

5. **Retain** existing contract teaching paragraphs: plug order after auth; `actor_fn` sole authority; additive overrides; `ArgumentError` on bad overrides (do not delete these literals).

6. **Add lane pointer bullets** (short, link only):
   - `phx-gen-auth-reference` → `guides/integrations/phx-gen-auth.md`
   - `sigra-reference` (optional) → `guides/integrations/sigra.md`
   - lane matrix → `guides/upgrade-path.md`

7. **Optional subsection** after pointers — heading `### Sigra reference wiring (optional)` and HTML comment `<!-- getting-started-sigra-reference-fence -->` immediately before the Sigra router excerpt. Insert excerpt via same content as today (accepts/json + Threadline.Plug with Sigra callbacks). Add summary line: **sigra-reference example app only** — not required for capture.

8. **Before** optional subsection body, add explicit sentence: Threadline does not require Sigra; do not use `Threadline.Integrations.Sigra` unless you adopt the optional sigra-reference lane.

Do **not** duplicate phx cookbook content into §5.
  </action>
  <acceptance_criteria>
    - `grep -F 'actor_fn: &MyApp.Audit.actor_ref_from_conn/1' guides/getting-started-saas.md` exits 0
    - `grep -F 'guides/integrations/phx-gen-auth.md' guides/getting-started-saas.md` exits 0
    - `grep -F 'The Phoenix example keeps' guides/getting-started-saas.md` exits 1
    - `grep -F 'Threadline.Integrations.Sigra' guides/getting-started-saas.md` matches only inside optional subsection (after `getting-started-sigra-reference-fence` marker) — manual read OK if grep counts >0 only below marker
    - `grep -F 'getting-started-sigra-reference-fence' guides/getting-started-saas.md` exits 0
  </acceptance_criteria>
  <verify>grep -F 'actor_fn: &MyApp.Audit.actor_ref_from_conn/1' guides/getting-started-saas.md</verify>
</task>

<task type="auto">
  <name>Task 2: Neutralize §6 authenticate + Next reads + upgrade-path intro</name>
  <files>guides/getting-started-saas.md, guides/upgrade-path.md</files>
  <read_first>
    - guides/getting-started-saas.md (### Authenticate before the audited API call through curl block)
    - examples/threadline_phoenix/README.md (cookie / curl SSOT)
    - .planning/phases/121-adopter-doc-neutrality/121-CONTEXT.md (D-121-10 through D-121-12, D-121-23, D-121-24)
    - guides/upgrade-path.md (## Who this guide is for)
  </read_first>
  <action>
**§6 `### Authenticate before the audited API call`:**

1. Rewrite opening to generic contract:
   - Identity on `conn` before `Threadline.Plug` on audited pipelines
   - Prefer `401`/`403` at host auth boundary; `500` / `missing actor` = semantics rejected after capture (keep existing teaching sentences)

2. Add small **lane table** (markdown table or bullet list) with three columns/links:
   - phx.gen.auth lane → `guides/integrations/phx-gen-auth.md`
   - Sigra lane → `guides/integrations/sigra.md`
   - choose lane → `guides/upgrade-path.md`

3. Move existing curl block into HTML `<details>`:
   - `<summary>Runnable curl — sigra-reference example app only</summary>`
   - Keep curl body with `_threadline_phoenix_key`, `localhost:4000`, headers unchanged inside details
   - Below details: link `examples/threadline_phoenix/README.md` as SSOT for cookie staging

4. If `<details>` is undesirable for hexdocs, use `### Runnable curl (sigra-reference example app only)` instead (document choice in commit message).

**`## Next reads`:** add line before or after sigra link:
`- [guides/integrations/phx-gen-auth.md](integrations/phx-gen-auth.md)`

**`guides/upgrade-path.md` `## Who this guide is for`:** ensure first bullet list explicitly names all four lanes: `capture-only`, `phoenix-surface`, `phx-gen-auth-reference`, `sigra-reference` (merge line 12 phx mention into consistent four-lane list if needed). Do **not** edit matrix table body.
  </action>
  <acceptance_criteria>
    - `grep -F 'Runnable curl' guides/getting-started-saas.md` exits 0
    - `grep -F 'sigra-reference example app only' guides/getting-started-saas.md` exits 0
    - `grep -F 'examples/threadline_phoenix/README.md' guides/getting-started-saas.md` exits 0
    - `grep -F 'phx-gen-auth.md' guides/getting-started-saas.md` exits 0 (§6 table or §5 pointers count)
    - `grep -F 'phx-gen-auth-reference' guides/upgrade-path.md` exits 0 in "Who this guide is for" section
  </acceptance_criteria>
  <verify>grep -F 'sigra-reference example app only' guides/getting-started-saas.md</verify>
</task>

<task type="auto">
  <name>Task 3: Update getting_started doc contract for neutrality</name>
  <files>test/threadline/getting_started_saas_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/getting_started_saas_doc_contract_test.exs (full file)
    - guides/getting-started-saas.md (after Tasks 1–2)
    - test/support/getting_started_fixtures.ex
    - .planning/phases/121-adopter-doc-neutrality/121-CONTEXT.md (D-121-20)
  </read_first>
  <action>
Edit `test/threadline/getting_started_saas_doc_contract_test.exs`:

1. **Remove** `assert String.contains?(doc, router_block())` from the main walkthrough test (line ~37).

2. **Add** neutrality asserts to main test:
   - `assert String.contains?(doc, "guides/integrations/phx-gen-auth.md")`
   - `assert String.contains?(doc, "actor_fn: &MyApp.Audit.actor_ref_from_conn/1")`
   - `refute String.contains?(doc, "The Phoenix example keeps")`

3. **Add** new test `"getting-started optional sigra-reference fence is scoped"`:
   - `assert String.contains?(doc, "getting-started-sigra-reference-fence")`
   - Extract subsection: split doc on `"getting-started-sigra-reference-fence"` and take tail
   - `assert String.contains?(subsection, router_block())` where `router_block/0` unchanged
   - `refute String.contains?(subsection, "MyApp.Audit.actor_ref_from_conn")` OR ensure hero section before marker contains MyApp.Audit — prefer: assert generic plug appears **before** marker index and Sigra plug appears **after** marker using `:binary.match/2` indices

4. Keep existing contract literals that still apply (`blog_block`, mount, Audit.transaction, operator surface, etc.) unless they conflict with rewritten §5.

5. Add assert for Next reads phx link if not covered: `guides/integrations/phx-gen-auth.md` in closing pointers test or main test.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/getting_started_saas_doc_contract_test.exs` exits 0
    - `grep 'assert String.contains?(doc, router_block())' test/threadline/getting_started_saas_doc_contract_test.exs` in main test body exits 1 (scoped test only)
    - `grep 'phx-gen-auth.md' test/threadline/getting_started_saas_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/getting_started_saas_doc_contract_test.exs</verify>
</task>

</tasks>

<verification>
After all tasks:
- `mix test test/threadline/getting_started_saas_doc_contract_test.exs`
- Read §5/§6 manually for cognitive order: contract → lane choice → optional runnable reference
</verification>
