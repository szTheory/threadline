# Plan 09-02: `AuditChange.changed_from`, `history/3`, tests, and install docs (BVAL-01, BVAL-02)

**Phase:** 9 — Before-values capture  
**Plan:** 09-02 of 02  
**Status:** Ready to execute  
**Depends on:** Plan 09-01 complete (DDL + trigger builders + gen flags)  
**Blocks:** —  
**Requirements covered:** BVAL-01, BVAL-02  
**Wave:** 2  
**requirements_addressed:** BVAL-01, BVAL-02  
**files_modified:** `lib/threadline/capture/audit_change.ex`, `lib/threadline/query.ex`, `lib/threadline.ex`, `test/threadline/capture/trigger_test.exs`, `test/threadline/capture/trigger_changed_from_test.exs`, `test/threadline/query_test.exs`, `README.md`  
**autonomous:** true  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-09-02-01 | `history/3` narrows `select` omitting `changed_from` → silent nil for populated rows | high | Acceptance: `rg 'select\\(' lib/threadline/query.ex` on `history/3` path must show **no** custom `select` that excludes `changed_from`; preferred: **no** `select` merge on `AuditChange` query in `history/3`. |
| T-09-02-02 | doctest or examples leak real secrets | low | Use synthetic schema names (`MyApp.User`-style) only. |

---

## Goal

Close **BVAL-02** (`Threadline.history/3` returns `changed_from` when populated, `nil` when off/null without breaking callers) and finish **BVAL-01** verification (PostgreSQL integration: UPDATE on/off, INSERT/DELETE null) per roadmap success criteria #1–#3.

---

## Context

- **Authoritative:** `09-CONTEXT.md` (D-08–D-10), `09-RESEARCH.md`, `.planning/ROADMAP.md` success criteria 1–3, `09-01-PLAN.md` deliverables.

---

## Tasks

### Task 1: Ecto schema — `field(:changed_from, :map)`

**What to do**

1. In `lib/threadline/capture/audit_change.ex`, add `field(:changed_from, :map)` after `field(:changed_fields, {:array, :string})`.
2. Add `:changed_from` to `cast/2` field list in `changeset/2`.
3. Extend `@moduledoc` with one bullet describing sparse prior-value JSON on UPDATE when enabled.

**Read first**

- `lib/threadline/capture/audit_change.ex`
- `lib/threadline/capture/trigger_sql.ex` (column name spelling)

**Acceptance criteria**

- `grep -n 'field(:changed_from' lib/threadline/capture/audit_change.ex` exits 0.
- `grep -n ':changed_from' lib/threadline/capture/audit_change.ex` shows occurrence inside `cast(attrs, [` list.
- `MIX_ENV=test mix compile` exits 0.

**Done condition:** All acceptance criteria pass.

---

### Task 2: Confirm `Query.history/3` loads `changed_from` by default

**What to do**

1. Read `lib/threadline/query.ex` `history/3`. If any `select` is applied to `AuditChange`, remove or extend it so **`changed_from`** is loaded from the database for all code paths.
2. If no changes needed, add an **end-of-line comment** is **not** acceptable per project style — instead add a **one-sentence** `@doc` note under `history/3` that `changed_from` is loaded when present.

**Read first**

- `lib/threadline/query.ex`
- `lib/threadline.ex` (`history/3` passthrough)

**Acceptance criteria**

- `rg 'def history' -n lib/threadline/query.ex -A25` shows **no** `|> select\\(` fragment between `AuditChange` and `repo.all`.

**Done condition:** Criterion passes.

---

### Task 3: Integration tests — `trigger_changed_from_test.exs`

**What to do**

1. Add `test/threadline/capture/trigger_changed_from_test.exs` using `Threadline.DataCase` pattern from `trigger_test.exs`.
2. **Fixture A:** Create table `test_audit_changed_from`, install **global** `threadline_capture_changes()` via `Repo.query!(TriggerSQL.install_function([]))` and `create_trigger("test_audit_changed_from")` — assert UPDATE produces `changed_from: nil` (feature off).
3. **Fixture B:** `Repo.query!(TriggerSQL.drop_trigger(...))`, `Repo.query!(TriggerSQL.install_function_for_table("test_audit_changed_from", store_changed_from: true, except_columns: []))` — or the **exact** API name chosen in 09-01 — reinstall trigger pointing at per-table function name; run UPDATE changing one column; assert `changed_from` map has **only** changed keys with prior values; assert INSERT and DELETE rows have **`changed_from == nil`** on loaded `AuditChange`.
4. Optional subcase: `--except-columns` equivalent — if generator list plumbing exists, assert omitted column not in `changed_fields` / `changed_from`.

**Read first**

- `test/threadline/capture/trigger_test.exs`
- `test/support/data_case.ex`
- `lib/threadline/capture/trigger_sql.ex` (final function names)

**Acceptance criteria**

- `MIX_ENV=test mix test test/threadline/capture/trigger_changed_from_test.exs` exits 0.
- File contains substring `changed_from` at least **5** times (`grep -c changed_from test/threadline/capture/trigger_changed_from_test.exs` returns ≥ 5).

**Done condition:** All acceptance criteria pass.

---

### Task 4: `Threadline.history/3` doctest or integration example (BVAL-02)

**What to do**

1. Add a `@doc` example block on `Threadline.history/3` in `lib/threadline.ex` **or** a doctest line that documents `%AuditChange{}` includes `:changed_from` key (may be `nil`). Use **synthetic** module name; do not assert DB contents if doctest not runnable — prefer **integration** in `test/threadline/query_test.exs` inserting audited row via trigger then `Threadline.history(Schema, id, repo: Repo)` asserting struct field.

**Read first**

- `lib/threadline.ex`
- `test/threadline/query_test.exs`

**Acceptance criteria**

- `grep -n 'changed_from' lib/threadline.ex` exits 0 **or** `grep -n 'changed_from' test/threadline/query_test.exs` exits 0 (at least one file proves BVAL-02 surface).
- `MIX_ENV=test mix test test/threadline/query_test.exs` exits 0.

**Done condition:** All acceptance criteria pass.

---

### Task 5: README — fresh install + upgrade note

**What to do**

1. In `README.md` near existing `mix threadline.install` / `mix threadline.gen.triggers` section, add a short subsection **“Before-values (`changed_from`)”** with three bullets:
   - Fresh installs get `changed_from` from `mix threadline.install` migration template.
   - Existing databases: run `ALTER TABLE audit_changes ADD COLUMN IF NOT EXISTS changed_from jsonb;` (exact SQL) then regenerate triggers with documented flags.
   - Link or mention `mix threadline.gen.triggers --tables ... --store-changed-from` and `--except-columns` **exact** spelling matching code.

**Read first**

- `README.md`
- `lib/mix/tasks/threadline.gen.triggers.ex` (final flag names)

**Acceptance criteria**

- `grep -n 'changed_from' README.md` exits 0.
- `grep -n 'ADD COLUMN IF NOT EXISTS changed_from' README.md` exits 0.

**Done condition:** All acceptance criteria pass.

---

## Verification

1. `MIX_ENV=test mix ci.all` exits 0.
2. Roadmap SC #1–#3 satisfied by tests + README evidence above.

---

## Success criteria

- **BVAL-01:** Integration tests on PostgreSQL prove UPDATE on/off and INSERT/DELETE null for `changed_from`.
- **BVAL-02:** `Threadline.history/3` returns `changed_from` on structs when populated; `nil` when column null / feature off, without narrowing selects.
