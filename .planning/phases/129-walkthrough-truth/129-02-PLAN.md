---
phase: 129-walkthrough-truth
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs
autonomous: true
requirements:
  - WALK-03
must_haves:
  truths:
    - "walkthrough_doc_contract_test.exs locks `mix threadline.verify_coverage` and refutes bare `mix verify.threadline`."
    - "Contract asserts canonical row-history route literal and refutes `/audit/rows/ticket_replies/` in WALK-03-01 and WALK-03-04 Do slices."
    - "Tests run under existing `mix verify.example` gate — no root verify.doc_contract addition."
  artifacts:
    - path: examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs
      provides: "Two-tier WALK-01/02 doc contracts with section_slice and step_do_slice helpers (WALK-03)"
---

<objective>
Extend `walkthrough_doc_contract_test.exs` with WALK-01/02 truth locks: footgun literals + semantic refutes for verify cwd and row-history URL presentation (WALK-03).

Purpose: Doc fixes regress without contract tests; Phase 128 two-tier pattern applies.
Output: Extended contract test only; gated by `mix verify.example`.
</objective>

<context>
@.planning/phases/129-walkthrough-truth/129-CONTEXT.md (D-129-09 through D-129-13)
@examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs (existing RUN-01 test — preserve unchanged)
@test/threadline/readme_doc_contract_test.exs (section_slice/3 pattern ~L272)
@lib/threadline/operator_surface/router.ex (canonical history route)
@examples/threadline_phoenix/WALKTHROUGH.md (post 129-01 edits)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-129-05 | Reintroducing `mix verify.threadline` in walkthrough | high | Task 2 refutes bare pattern globally |
| T-129-06 | Do-step `/audit/rows/` regression | high | Task 3 step_do_slice refutes in WALK-03-01/04 |
| T-129-07 | Contract scans full doc — false positives outside target sections | medium | section_slice and step_do_slice scoping per D-129-10 |
| T-129-08 | Adding walkthrough to root verify.doc_contract — unnecessary duplication | low | Do not modify root mix.exs aliases per D-129-13 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add section_slice and step_do_slice helpers</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs (full file)
    - test/threadline/readme_doc_contract_test.exs (section_slice/3 ~L272–285)
    - .planning/phases/129-walkthrough-truth/129-CONTEXT.md (D-129-10)
  </read_first>
  <action>
Add private helpers at bottom of module (copy section_slice from readme_doc_contract_test.exs):

```elixir
defp section_slice(doc, start_heading, end_heading) do
  doc
  |> String.split(start_heading, parts: 2)
  |> case do
    [_before, rest] ->
      rest
      |> String.split(end_heading, parts: 2)
      |> List.first()

    _ ->
      flunk("start heading #{inspect(start_heading)} not found")
  end
end

defp step_do_slice(doc, step_marker, next_marker) do
  doc
  |> String.split(step_marker, parts: 2)
  |> case do
    [_before, rest] ->
      rest
      |> String.split("**Do:**", parts: 2)
      |> case do
        [_pre, after_do] ->
          after_do
          |> String.split(next_marker, parts: 2)
          |> List.first()
          |> String.split("**Expected outcome:**", parts: 2)
          |> List.first()

        _ ->
          flunk("Do block not found after #{inspect(step_marker)}")
      end

    _ ->
      flunk("step marker #{inspect(step_marker)} not found")
  end
end
```

Preserve existing RUN-01 test unchanged.
  </action>
  <acceptance_criteria>
    - `grep -F 'defp section_slice' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches
    - `grep -F 'defp step_do_slice' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches
    - `grep -F 'WALKTHROUGH.md carries walk-critical literals' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches (existing test preserved)
  </acceptance_criteria>
  <verify>grep -n 'section_slice\|step_do_slice' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 2: WALK-01 doc-contract locks (verify cwd truth)</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (§0, §1 WALK-01-02, §5 WALK-04-03 — post 129-01)
    - .planning/phases/129-walkthrough-truth/129-CONTEXT.md (D-129-11)
  </read_first>
  <action>
Add `describe "WALK-01 verify cwd truth"` with tests:

**Test A — threadline.verify_coverage in verify contexts:**
```elixir
test "WALKTHROUGH optional verify uses mix threadline.verify_coverage" do
  doc = File.read!(@walkthrough)
  section_1 = section_slice(doc, "#### Step WALK-01-02", "#### Step WALK-01-03")
  section_5 = section_slice(doc, "#### Step WALK-04-03", "### §5 Checkpoint")

  assert String.contains?(section_1, "mix threadline.verify_coverage")
  assert String.contains?(section_5, "mix threadline.verify_coverage")
end
```

**Test B — refute bare verify.threadline:**
```elixir
test "WALKTHROUGH refutes bare mix verify.threadline walk steps" do
  doc = File.read!(@walkthrough)
  refute String.contains?(doc, "mix verify.threadline")
end
```

**Test C — walk cwd documented:**
```elixir
test "WALKTHROUGH §0 locks examples/threadline_phoenix walk cwd" do
  doc = File.read!(@walkthrough)
  prereq = section_slice(doc, "### Prerequisites", "### Recovery")

  assert String.contains?(prereq, "examples/threadline_phoenix/")
  assert String.contains?(doc, "Walk vs contributor cwd")
end
```
  </action>
  <acceptance_criteria>
    - `grep -F 'describe "WALK-01 verify cwd truth"' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches
    - `grep -F 'refute String.contains?(doc, "mix verify.threadline")' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches
    - `grep -F 'mix threadline.verify_coverage' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches in test body
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix/walkthrough_doc_contract_test.exs` exits 0 (requires 129-01 complete)
  </acceptance_criteria>
  <verify>cd examples/threadline_phoenix && mix test test/threadline_phoenix/walkthrough_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 3: WALK-02 doc-contract locks (row-history URL truth)</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (WALK-03-01, WALK-03-04, §0 Row history URLs)
    - .planning/phases/129-walkthrough-truth/129-CONTEXT.md (D-129-12)
    - lib/threadline/operator_surface/router.ex
  </read_first>
  <action>
Add `describe "WALK-02 row-history URL truth"` with tests:

**Test A — global canonical route literal:**
```elixir
test "WALKTHROUGH documents canonical transaction-scoped row history route" do
  doc = File.read!(@walkthrough)

  assert String.contains?(
           doc,
           "/audit/transactions/:id/history/:table/:record_id"
         )
end
```

**Test B — Do slices refute pasteable /audit/rows/ in incident steps:**
```elixir
test "WALK-03-01 and WALK-03-04 Do steps refute pasteable /audit/rows/ URLs" do
  doc = File.read!(@walkthrough)

  walk_0301_do =
    step_do_slice(doc, "#### Step WALK-03-01", "#### Step WALK-03-02")

  walk_0304_do =
    step_do_slice(doc, "#### Step WALK-03-04", "### §4 Checkpoint")

  assert String.contains?(walk_0301_do, "History")
  assert String.contains?(walk_0304_do, "History")

  refute String.contains?(walk_0301_do, "/audit/rows/ticket_replies/")
  refute String.contains?(walk_0304_do, "/audit/rows/ticket_replies/")
end
```

**Test C — WALK-04-02 corroboration step:**
```elixir
test "WALK-04-02 corroboration avoids bare /audit/rows/ in Do prose" do
  doc = File.read!(@walkthrough)
  walk_0402_do = step_do_slice(doc, "#### Step WALK-04-02", "#### Step WALK-04-03")

  refute String.match?(walk_0402_do, ~r`/audit/rows/ticket_replies/`)
end
```

**Test D — optional router cross-check (recommended):**
```elixir
test "shipped operator surface mounts transaction-scoped history route" do
  router =
    File.read!(
      Path.expand("../../../../lib/threadline/operator_surface/router.ex", __DIR__)
    )

  assert String.contains?(
           router,
           ~s/live("\\/transactions\\/:id\\/history\\/:table\\/:record_id"/)
         ) or
           String.contains?(
             router,
             ~s/live("/transactions/:id/history/:table/:record_id"/)
           )
end
```

Adjust router path if expand differs — use same pattern as other example-app tests referencing lib/.
  </action>
  <acceptance_criteria>
    - `grep -F 'describe "WALK-02 row-history URL truth"' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches
    - `grep -F 'refute String.contains?(walk_0301_do, "/audit/rows/ticket_replies/")' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches
    - `grep -F 'shipped operator surface mounts transaction-scoped history route' examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix/walkthrough_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>cd examples/threadline_phoenix && mix test test/threadline_phoenix/walkthrough_doc_contract_test.exs</verify>
</task>

</tasks>

<verification>
```bash
cd examples/threadline_phoenix && mix test test/threadline_phoenix/walkthrough_doc_contract_test.exs
# From repo root — confirm verify.example gate includes example tests:
mix verify.example 2>&1 | tail -5
```

All must exit 0 before marking plan complete.
</verification>

<success_criteria>
- WALK-03: Doc-contract test locks verify cwd (`mix threadline.verify_coverage`, refute `mix verify.threadline`) and row-history URL truth (canonical literal, Do-step refutes).
- Existing RUN-01 literal-presence test unchanged.
- Tests pass under `mix verify.example`; no root verify.doc_contract alias changes.
</success_criteria>
