---
phase: 200-public-surface
plan: 15
type: execute
wave: 8
depends_on: [200-08]
files_modified:
  - lib/mix/tasks/threadline.health.coverage.ex
  - lib/threadline/capture/audit_transaction.ex
  - lib/threadline/plug.ex
  - lib/threadline/query.ex
  - lib/threadline/retention/policy.ex
  - test/threadline/operator_surface/coverage_doc_contract_test.exs
  - test/threadline/audit_transaction_test.exs
  - test/threadline/plug_test.exs
  - test/threadline/query_test.exs
  - test/threadline/retention/policy_test.exs
autonomous: true
requirements: [SURFACE-01, SURFACE-02, SURFACE-07]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-14/D-22: core Mix, capture, Plug, query, and retention source explains durable behavior without decision, requirement, phase, milestone, or stale release provenance."
    - "D-06/D-14: both bounded core source-owner tags are nonempty and clean under the same zero-allowlist matcher used by the final archive aggregate."
  artifacts:
    - path: lib/threadline/plug.ex
      provides: Public request-context documentation with a real public bridge reference
    - path: lib/threadline/query.ex
      provides: Durable index-tradeoff and anonymous-actor rationale
  key_links:
    - from: test/threadline/release_artifact_contract_test.exs
      to: the ten files owned by this plan
      via: source_vocab_core_runtime and source_vocab_core_query_policy
      pattern: "source_vocab_core_(runtime|query_policy)"
---

<objective>
Remove planning provenance from the remaining core packaged source before any full source/archive vocabulary aggregate runs.

Purpose: Make public and implementation rationale useful to adopters and maintainers without leaking the planning system.
Output: Two independently green, behavior-preserving core source cohorts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-public-surface/200-CONTEXT.md
@.planning/phases/200-public-surface/200-RESEARCH.md
@.planning/phases/200-public-surface/200-01-SUMMARY.md
@.planning/phases/200-public-surface/200-08-SUMMARY.md
@test/threadline/release_artifact_contract_test.exs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Clean Mix, capture, and Plug provenance without changing their contracts</name>
  <read_first>
    - lib/mix/tasks/threadline.health.coverage.ex: viewer exit semantics comment
    - lib/threadline/capture/audit_transaction.ex: transaction grouping and nullable context comments
    - lib/threadline/plug.ex: PostgreSQL bridge section and obsolete gate document link
    - test/threadline/release_artifact_contract_test.exs: source_vocab_core_runtime owner tag
  </read_first>
  <files>lib/mix/tasks/threadline.health.coverage.ex, lib/threadline/capture/audit_transaction.ex, lib/threadline/plug.ex, test/threadline/operator_surface/coverage_doc_contract_test.exs, test/threadline/audit_transaction_test.exs, test/threadline/plug_test.exs</files>
  <action>Apply D-14/D-22 to the exact live offenders. Replace the coverage task's decision-ID comment with the durable fact that this viewer reports findings without acting as a CI gate. Replace AuditTransaction's decision/requirement labels with the PostgreSQL transaction-grouping reason and the nullable actor/request-context guarantee. In Threadline.Plug, retain the public PostgreSQL bridge heading but replace the internal requirement label and `gate-01-01.md` planning-file reference with the supported public module/guide destination that explains callers which manage `set_config` themselves. Update only assertions coupled to these source strings. Preserve callback names, output, exit behavior, transaction metadata, context propagation, and Plug behavior. Run the unchanged zero-allowlist owner matcher; do not encode a token or path exception.</action>
  <acceptance_criteria>
    - The three packaged sources contain no planning-shaped provenance and their user-facing behavior/contracts are unchanged.
    - The task-local owner tag selects exactly these three nonempty source subjects and detects an injected offender.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/operator_surface/coverage_doc_contract_test.exs test/threadline/audit_transaction_test.exs test/threadline/plug_test.exs -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/release_artifact_contract_test.exs --only source_vocab_core_runtime -x</automated>
    <fails_when>Either command exits non-zero or selects zero tests, the public bridge points at an internal/nonexistent planning file, behavior changes, the owner set is empty or differs from the three declared sources, a planning shape remains, or the injected control is missed.</fails_when>
  </verify>
  <done>The Mix/capture/Plug cohort is behavior-preserving and clean under the final matcher.</done>
</task>

<task type="auto">
  <name>Task 2: Replace query and retention chronology with stable contract language</name>
  <read_first>
    - lib/threadline/query.ex: ordering-index debt and anonymous-transaction comments
    - lib/threadline/retention/policy.ex: version-shaped retention contract prose
    - test/threadline/release_artifact_contract_test.exs: source_vocab_core_query_policy owner tag
  </read_first>
  <files>lib/threadline/query.ex, lib/threadline/retention/policy.ex, test/threadline/query_test.exs, test/threadline/retention/policy_test.exs</files>
  <action>Rewrite Query's decision/deferred/milestone annotations as durable engineering rationale: the current ordering index tradeoff, the capture-layer ownership boundary, and why anonymous transactions are intentionally equivalent. Rewrite Retention.Policy's stale version-shaped contract prose as a present-tense global-policy contract while preserving the exact enabled/window/delete-empty semantics and explicit absence of per-table/per-tenant overrides. Update only source-text-coupled tests. Per D-14, remove genuine planning provenance rather than disguising it, and keep the shared matcher zero-allowlist.</action>
  <acceptance_criteria>
    - Query retains ordering and actor-equivalence behavior with durable rationale.
    - Retention policy documents current behavior without a stale release milestone and all validation behavior remains green.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/query_test.exs test/threadline/retention/policy_test.exs -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/release_artifact_contract_test.exs --only source_vocab_core_query_policy -x</automated>
    <fails_when>Either command exits non-zero or selects zero tests, query/retention behavior changes, the owner tag is empty or owns a different path, planning provenance remains, or an injected offender is not reported.</fails_when>
  </verify>
  <done>Both core policy sources are vocabulary-clean, behavior-preserving, and explicitly owned before the full aggregate.</done>
</task>

</tasks>

<threat_model>
**ASVS level:** L1

## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| packaged core source → adopter | Public docs/comments can create false operational expectations. |
| source cleanup → runtime contract | Prose cleanup must not alter query, retention, capture, Plug, or Mix-task behavior. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-02 | Information disclosure | packaged core source | medium | mitigate | Two exact nonempty owner tags using the shared zero-allowlist matcher and injected controls. |
| T-200-05 | Tampering | public Plug/retention/query contracts | medium | mitigate | Existing focused test suites plus source-resolved public guide/module references. |
</threat_model>

<verification>Run both owner tags and all five focused behavior suites; do not run full source_module_vocabulary or archive_vocabulary.</verification>
<success_criteria>
- Each of the five matched packaged source files appears exactly once in this plan's task ownership.
- Both bounded tags are green with injected controls and no allowlist.
- Runtime behavior and public interfaces are unchanged.
</success_criteria>

## Artifacts this phase produces

- Durable source rationale across the remaining core matched files.
- Green `source_vocab_core_runtime` and `source_vocab_core_query_policy` owner projections.

<output>Create .planning/phases/200-public-surface/200-15-SUMMARY.md when done.</output>
