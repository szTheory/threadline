---
phase: 200-public-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/public_surface_contract_test.exs
  - test/threadline/release_artifact_contract_test.exs
  - test/threadline/guide_graph_contract_test.exs
  - test/threadline/community_health_contract_test.exs
  - test/threadline/operator_surface/controllers/export_controller_test.exs
autonomous: true
requirements:
  - SURFACE-01
  - SURFACE-02
  - SURFACE-03
  - SURFACE-04
  - SURFACE-05
  - SURFACE-06
  - SURFACE-07
  - SURFACE-08
  - SURFACE-09
  - SURFACE-10
  - SURFACE-11
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-06: source-derived runtime-key, Mix-task, alias, and module discoveries are nonempty, contain stable sentinels, and are compared with exact disjoint classifications."
    - "D-12/D-14: visible-module grouping and unpacked-archive vocabulary contracts fail on omissions, duplicates, hidden members, and an injected archive offender."
    - "D-18/D-29: guide and community contracts expose broken routes and unsafe intake before prose or hosted files are changed."
    - "D-03: a behavior-conforming storage adapter without optional path/1 has a failing delivery regression before the runtime repair."
  artifacts:
    - path: test/threadline/public_surface_contract_test.exs
      provides: AST/config/task/alias and compiled-doc/module-group contracts
    - path: test/threadline/guide_graph_contract_test.exs
      provides: intent-lane, link, anchor, canonical-owner, and public-reference contracts
    - path: test/threadline/community_health_contract_test.exs
      provides: contributor and GitHub community-health structural contracts
    - path: test/threadline/release_artifact_contract_test.exs
      provides: authoritative unpacked-Hex vocabulary and URL-extra contracts
    - path: test/threadline/operator_surface/controllers/export_controller_test.exs
      provides: optional storage callback regression
  key_links:
    - from: test/threadline/public_surface_contract_test.exs
      to: mix.exs
      via: Threadline.MixProject.project/0 plus AST traversal
      pattern: "Code\\.string_to_quoted!|groups_for_modules"
    - from: test/threadline/release_artifact_contract_test.exs
      to: mix hex.build --unpack
      via: fresh temporary archive traversal
      pattern: "hex.build.*--unpack"
    - from: test/threadline/guide_graph_contract_test.exs
      to: guides/**/*.md
      via: explicit lane map and resolved Markdown paths/anchors
      pattern: "Next steps"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Contracts must not create a public component API, a new start-here guide, public mount options, or an alternate docs taxonomy."
    - "[FLAGGED-UNVERIFIED] A zero-result discovery, count floor, or scanner exclusion for a legitimate packaged document must never satisfy a public-surface gate."
---

<objective>
Create the executable red baseline that defines Phase 200's public surface before changing that surface.

Purpose: Make every later documentation, package, runtime, and community edit answer to source-derived, non-vacuous contracts instead of remembered counts.
Output: Three new contract-test modules plus extensions to the archive and export-delivery tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-public-surface/200-CONTEXT.md
@.planning/phases/200-public-surface/200-RESEARCH.md
@.planning/phases/200-public-surface/200-VALIDATION.md
@.planning/phases/200-public-surface/200-PATTERNS.md
@test/threadline/release_artifact_contract_test.exs
@test/threadline/persona_routing_doc_contract_test.exs
@test/threadline/version_truth_doc_contract_test.exs
@test/threadline/operator_surface/controllers/export_controller_test.exs
@mix.exs

<interfaces>
- `Threadline.MixProject.project()[:docs]` exposes `:extras`, `:groups_for_extras`, and `:groups_for_modules`; `project()[:package][:files]` is the package allowlist.
- `Threadline.Storage.path/1` is optional; `download_url/2` is required.
- The established archive test creates a unique `System.tmp_dir!/0` directory, registers `on_exit/1` cleanup, and invokes `mix hex.build --unpack --output` with captured stderr.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace source facts through docs grouping and the built archive</name>
  <read_first>
    - mix.exs: aliases/0, package/0, docs/0, doc_source_ref/0
    - test/threadline/release_artifact_contract_test.exs
    - test/threadline/version_truth_doc_contract_test.exs
    - .planning/phases/200-public-surface/200-RESEARCH.md: Pattern 1, Pattern 2, Pattern 5
  </read_first>
  <files>test/threadline/public_surface_contract_test.exs, test/threadline/release_artifact_contract_test.exs</files>
  <action>Create `Threadline.PublicSurfaceContractTest` and extend the existing archive contract per D-01, D-06, D-12, and D-14. Parse every tracked `lib/**/*.ex` file and `mix.exs` with `Code.string_to_quoted!/2`; collect literal `Application.get_env/2`, `get_env/3`, and `fetch_env*` reads for `:threadline`, and report dynamic key expressions separately. Discover Mix tasks from `Mix.Tasks.*` modules and repository aliases from `aliases/0`. Assert every discovered set is nonempty and contains a stable sentinel before comparing it to explicit adopter-public, repository-only, maintainer-only, and internal sets; assert exact union, pairwise disjointness, and no duplicates. Discover compiled application modules and use `Code.fetch_docs/1` to distinguish visible, hidden, and absent docs; compare visible modules with flattened explicit group lists, require each member once, and assert the D-09 hidden set. Add focused tags `:public_inventory`, `:module_visibility_tracer`, and `:module_visibility` so later plans can turn slices green independently. Extend the archive harness to return sorted entry paths plus readable UTF-8 contents, treat URL extras separately from local extras, assert nonempty entries/readable files and stable `lib/threadline.ex`/`mix.exs` sentinels, scan path names and content for planning-vocabulary shapes and source-derived current requirement/milestone literals, and prove the same scanner reports a temporary injected offender. Keep binary skipping content-derived; add no path exception for README, CONTRIBUTING, CHANGELOG, guides, or source. Commit the synthetic extractor/positive-control tests green while the tagged live assertions remain red only for measured Phase 200 violations.</action>
  <acceptance_criteria>
    - Synthetic multiline config calls, dynamic module-key reads, duplicate classification, hidden-group membership, and injected archive text each exercise the intended failure path.
    - Live failures name the unclassified key/task/alias/module or archive path and never report only a count.
    - URL extras are not interpreted as package paths, while DESIGN-SYSTEM.md and the example README are asserted absent from the archive.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs test/threadline/release_artifact_contract_test.exs --exclude phase200_red -x</automated>
    <fails_when>The test files do not compile, a synthetic control fails, discovery is empty, a stable sentinel is absent, or the real archive harness cannot build and unpack the package.</fails_when>
  </verify>
  <done>The source-to-classification-to-docs/archive tracer is executable, its controls are green, and its tagged live contract records only the known public-surface debt.</done>
</task>

<task type="auto">
  <name>Task 2: Define guide-graph and community-health red contracts</name>
  <read_first>
    - test/threadline/persona_routing_doc_contract_test.exs
    - README.md: Start here routing
    - guides/getting-started-saas.md: opening and terminal links
    - .planning/phases/200-public-surface/200-VALIDATION.md: Wave 0 Requirements
  </read_first>
  <files>test/threadline/guide_graph_contract_test.exs, test/threadline/community_health_contract_test.exs</files>
  <action>Create `Threadline.GuideGraphContractTest` per D-18 and `Threadline.CommunityHealthContractTest` per D-19/D-23/D-29. The guide contract owns one explicit intent-lane-to-guide map for all tracked `guides/**/*.md`, asserts exact one-lane membership and nonempty/stable sentinels, parses descriptive Markdown links, resolves paths relative to each source, normalizes heading anchors, and requires every guide to have a non-README inbound edge plus an outbound edge. Require leaf guides to end with `Next steps`, link to their lane landing, and link to a distinct task-adjacent successor; add negative fixtures for missing targets and anchors. Add tagged canonical-owner checks that installation/first-hour, operator mount/auth/config, and Docker lifecycle sequences occur only in their D-16 owners, with allowed D-17 orientation text treated separately. The community contract checks the intended issue/PR/security/conduct paths are nonempty, the three form names and chooser policy are exact, labels stay within the known `bug`/`enhancement`/`question` set, sensitive-data warnings exist, security/support/conduct destinations remain distinct, the exact D-19 database error is searchable and links to the Docker repair anchor, and CONTRIBUTING has no planning dependency. Use narrow structural YAML assertions without adding a parser dependency or pretending to render GitHub.</action>
  <acceptance_criteria>
    - Every one of the 18 tracked guides is assigned exactly once and graph discovery cannot pass empty.
    - Missing-path and missing-anchor fixtures fail through the same resolver used for live docs.
    - Community assertions reject a public vulnerability route, missing privacy warning, stale version choice, nonexistent Discussions destination, or maintainer credential requirement.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/guide_graph_contract_test.exs test/threadline/community_health_contract_test.exs --exclude phase200_red -x</automated>
    <fails_when>Either contract does not compile, a synthetic graph/control passes incorrectly, the guide discovery is empty, or the current missing community files are accidentally treated as success.</fails_when>
  </verify>
  <done>Graph, canonical-owner, contributor, and community-health invariants exist with green controls and tagged live red assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the optional-storage-callback regression red</name>
  <read_first>
    - lib/threadline/storage.ex
    - lib/threadline/operator_surface/controllers/export_controller.ex: resolve_delivery/2
    - test/threadline/operator_surface/controllers/export_controller_test.exs: RemoteStorageStub and download tests
  </read_first>
  <files>test/threadline/operator_surface/controllers/export_controller_test.exs</files>
  <behavior>
    - A storage adapter implementing every required Threadline.Storage callback but omitting optional path/1 delivers a completed export through download_url/2.
    - The existing adapter returning `{:error, :not_local}` still redirects through download_url/2.
    - Other path/1 errors retain the existing unavailable response.
  </behavior>
  <action>Add a second behavior-conforming stub that deliberately omits optional `path/1`; do not alter `RemoteStorageStub`, because its `{:error, :not_local}` branch is a separate regression. Follow the existing Application-environment isolation, completed-job fixture, request, actor assignment, and exact 302 `location` assertion. Tag only the new test `:phase200_red` so the test module remains usable while Plan 200-02 implements D-03.</action>
  <acceptance_criteria>
    - The new test fails against the current unconditional callback invocation for the specific missing optional callback.
    - Existing local, remote, expiry, 404, and 410 tests remain unchanged and green when the new red tag is excluded.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/operator_surface/controllers/export_controller_test.exs --exclude phase200_red -x</automated>
    <fails_when>The existing controller suite regresses or global application environment is not restored after the test.</fails_when>
  </verify>
  <done>The missing-optional-callback behavior is captured by a precise red regression without weakening the existing remote-adapter test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source/docs config → public contract | Repository implementation facts become compatibility promises. |
| working tree → Hex archive | Only the unpacked consumer artifact proves shipped content. |
| public issue fields → repository maintainers | Sensitive information can cross into a public queue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-01 | Information disclosure | module/config/task inventory | medium | mitigate | AST and compiled-doc exact-set contracts with nonempty sentinels and hidden-member rejection. |
| T-200-02 | Information disclosure | unpacked Hex archive | high | mitigate | Fresh unpack, content/path vocabulary scan, stable sentinels, and injected-positive control in release_artifact_contract_test.exs. |
| T-200-03 | Information disclosure | issue/PR intake | high | mitigate | Community contract rejects public vulnerability routing and missing sensitive-data warnings. |
| T-200-04 | Denial of service | export delivery callback dispatch | medium | mitigate | Behavior-conforming adapter regression for the absent optional callback. |
</threat_model>

## Spec-less probe assumptions

- `[FLAGGED-UNVERIFIED]` SURFACE-04's adjacency/empty/ordering probes are interpreted as exact visible-module set equality, unique membership, six-key order, and nonempty sentinels.
- `[FLAGGED-UNVERIFIED]` SURFACE-06/07/09 boundary, adjacency, empty, ordering, and precision probes are interpreted as exact lane/config/task maps plus shared negative fixtures, not generic count checks.
- `[FLAGGED-UNVERIFIED]` SURFACE-08's empty/encoding probe requires the exact UTF-8 database error string in tracked newcomer docs.
- `[FLAGGED-UNVERIFIED]` SURFACE-11's concurrency probe is hosted default-branch state and remains manual until Plan 200-05; local parsing is not a substitute.
- The unclassified probes for SURFACE-01/02/03/05/10 are covered directly by their requirement assertions. Generic routine-engineering and canonical OWASP/compliance probe noise is not promoted into a prohibition; the retained safety boundaries are the project-specific entries in `must_haves.prohibitions`.

<verification>
Run the three task commands, then confirm the tagged live-red cases fail for named current violations rather than compile errors, empty discovery, or unavailable tooling.
</verification>

<success_criteria>
- All five test paths compile.
- Synthetic positive/negative controls are green and non-vacuity sentinels are asserted before equality checks.
- Live Phase 200 debt is represented by scoped red tags consumed by Plans 200-02 through 200-05.
</success_criteria>

## Artifacts this phase produces

- `Threadline.PublicSurfaceContractTest` at `test/threadline/public_surface_contract_test.exs`.
- `Threadline.GuideGraphContractTest` at `test/threadline/guide_graph_contract_test.exs`.
- `Threadline.CommunityHealthContractTest` at `test/threadline/community_health_contract_test.exs`.
- Extended archive scanner and URL-extra assertions in `test/threadline/release_artifact_contract_test.exs`.
- An optional-`path/1` regression stub and tagged test in `test/threadline/operator_surface/controllers/export_controller_test.exs`.

<output>
Create `.planning/phases/200-public-surface/200-01-SUMMARY.md` when done.
</output>
