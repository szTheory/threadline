---
phase: 200-public-surface
plan: 02
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - guides/configuration-and-commands.md
autonomous: true
requirements: [SURFACE-07]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-05/D-06: one canonical reference classifies every discovered Threadline runtime key, Mix task, and repository alias with exact, disjoint, non-vacuous sets."
    - "D-04/D-07: supported polling controls are precise without inventing mount options, aliases, callbacks, or a new configuration namespace."
  artifacts:
    - path: guides/configuration-and-commands.md
      provides: Canonical compatibility-grade configuration and command reference
  key_links:
    - from: guides/configuration-and-commands.md
      to: mix.exs
      via: source-derived task/alias inventory
      pattern: "verify\\.|threadline\\."
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Do not present repository-only aliases as host-project aliases or promote socket assigns into public mount options."
---

<objective>
Publish one exact configuration and command reference before the documentation graph links to it.

Purpose: Give adopters and contributors a source-checked vocabulary without turning every implementation detail into API.
Output: `guides/configuration-and-commands.md` with the complete D-01/D-04/D-05 classification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/200-public-surface/200-CONTEXT.md
@.planning/phases/200-public-surface/200-RESEARCH.md
@.planning/phases/200-public-surface/200-PATTERNS.md
@.planning/phases/200-public-surface/200-01-SUMMARY.md
@test/threadline/public_surface_contract_test.exs
@mix.exs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace every supported runtime key into the canonical reference</name>
  <read_first>
    - .planning/phases/200-public-surface/200-CONTEXT.md: D-01, D-04, D-05, D-06, D-07
    - .planning/phases/200-public-surface/200-RESEARCH.md: Pattern 1 runtime-key inventory
    - test/threadline/public_surface_contract_test.exs: runtime_key_reference contract
    - lib/threadline/application.ex: adapter-module dynamic-key lookup
  </read_first>
  <reversibility rating="costly">D-01 makes documented adopter configuration a compatibility promise whose removal needs deprecation and release notes.</reversibility>
  <files>guides/configuration-and-commands.md</files>
  <action>Create the D-05 reference with the fourteen literal runtime keys enumerated by 200-RESEARCH plus the separate dynamic adapter-module key class. Per D-01/D-06, document each adopter-public key's purpose, shape/default or absence semantics, safe example, and owning public module/guide; keep the runtime-key classification exact, disjoint, duplicate-free, nonempty, and sentinel-backed through `:runtime_key_reference`. Per D-04, group `:coverage_poll_ms` (`30_000`), `:export_status_poll_ms` (`5_000`), and `:retention_poll_ms` (`5_000`) under advanced operator tuning, require positive integer milliseconds, distinguish UI polling from worker/cleanup schedules, and do not describe internal socket assigns as mount options.</action>
  <acceptance_criteria>
    - All fourteen literal keys and the dynamic adapter-module key class are classified and documented with source-accurate semantics.
    - The runtime-key-only contract is green without requiring Task 2's task and alias sections.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only runtime_key_reference -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only public_doc_refs_config -x</automated>
    <fails_when>Either command exits non-zero or selects zero tests, discovers no runtime keys/config-reference subjects, misses a stable sentinel, or reports an unclassified, overlapping, duplicate, nonexistent, or undocumented runtime key.</fails_when>
  </verify>
  <done>Every discovered runtime key and dynamic-key seam is classified, and every supported key is documented without namespace redesign.</done>
</task>

<task type="auto">
  <name>Task 2: Classify adopter tasks and repository commands without alias leakage</name>
  <read_first>
    - .planning/phases/200-public-surface/200-CONTEXT.md: D-05 and D-06
    - .planning/phases/200-public-surface/200-RESEARCH.md: Task/alias classification
    - test/threadline/public_surface_contract_test.exs: command_reference and public_inventory contracts
    - mix.exs: aliases/0
  </read_first>
  <files>guides/configuration-and-commands.md</files>
  <action>Complete the same reference per D-05/D-06. Classify all `Mix.Tasks.Threadline.*` modules and every `aliases/0` key: the adopter set is install, trigger generation, coverage verification/viewing, continuity, retention purge, export, incident, evidence show, and policy show; `Mix.Tasks.Threadline.VerifyTopology` and `verify.topology` are repository-only; critic tasks and credentialed/phase-shaped critic aliases are maintainer-only; every other `verify.*`, `test.*`, and `ci.all` alias is a repository command, never a dependency alias available in a host project. End with `Next steps` links that later Adopt and Operate slices can wire, without duplicating an end-to-end tutorial.</action>
  <acceptance_criteria>
    - Every source-discovered Mix task and alias belongs to exactly one class and the reference distinguishes host commands from repository aliases.
    - Both the command-only projection and complete public inventory are green with nonempty sentinels.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only command_reference -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only public_inventory -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only public_doc_refs_config -x</automated>
    <fails_when>Any command exits non-zero or selects zero tests, or a task/alias/config reference is missing, duplicated, overlapping, stale, misclassified, nonexistent, or absent from the canonical reference.</fails_when>
  </verify>
  <done>Every source-discovered task and alias has exactly one truthful classification and every supported item is present in the public reference.</done>
</task>

</tasks>

<threat_model>
**ASVS level:** L1

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config → Threadline runtime | Adopter-controlled values select runtime and operator behavior. |
| repository aliases → contributor docs | Repository commands can be mistaken for dependency-provided host commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-05 | Tampering | config/task reference | medium | mitigate | AST/task/alias discovery, exact disjoint classification, stable sentinels, and public-reference equality. |
| T-200-06 | Information disclosure | config examples | low | accept | Examples use fictional modules and only supported option shapes. |
</threat_model>

<verification>
Run both task checks and confirm `:public_inventory` names any missing, duplicate, overlapping, stale, or undocumented member.
</verification>

<success_criteria>
- Every runtime key, task, and alias is exactly classified.
- Every supported item is documented in one canonical reference.
- No new configuration/API surface or host-alias fiction appears.
</success_criteria>

## Artifacts this phase produces

- Canonical compatibility-grade configuration and command reference at `guides/configuration-and-commands.md`.
- Green runtime-key, command, public-inventory, and config-reference contract projections.

<output>
Create `.planning/phases/200-public-surface/200-02-SUMMARY.md` when done.
</output>
