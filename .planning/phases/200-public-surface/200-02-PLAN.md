---
phase: 200-public-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 200-01
files_modified:
  - guides/configuration-and-commands.md
  - lib/threadline/storage.ex
  - lib/threadline/storage/local.ex
  - lib/threadline/storage/s3.ex
  - lib/threadline/export_queue.ex
  - lib/threadline/export_queue/task_adapter.ex
  - lib/threadline/export_queue/oban.ex
  - lib/threadline/operator_surface/controllers/export_controller.ex
autonomous: true
requirements:
  - SURFACE-07
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-05/D-06: one canonical reference classifies every discovered Threadline runtime key, task, and repository alias, and documents every supported item without presenting dependency aliases as host aliases."
    - "D-02: storage_adapter is a supported extension point with binary-content put/2 semantics, documented Local/S3 behavior, and module-keyed adapter configuration."
    - "D-03/D-07: an adapter omitting optional path/1 falls back to download_url/2 without adding or renaming a callback or redesigning configuration."
    - "D-04: the three polling keys are documented together with exact defaults, positive-integer millisecond validation language, and no promotion of socket assigns into mount options."
  artifacts:
    - path: guides/configuration-and-commands.md
      provides: Canonical compatibility-grade configuration and command reference
    - path: lib/threadline/storage.ex
      provides: Public adapter behavior and portable content contract
    - path: lib/threadline/operator_surface/controllers/export_controller.ex
      provides: Optional callback capability dispatch
  key_links:
    - from: guides/configuration-and-commands.md
      to: mix.exs
      via: exact source-derived alias and task classification
      pattern: "verify\\.|threadline\\."
    - from: lib/threadline/operator_surface/controllers/export_controller.ex
      to: lib/threadline/storage.ex
      via: function_exported?/3 before optional path/1 dispatch
      pattern: "function_exported\\?.*:path.*1"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Do not add a nested operator-surface namespace, compatibility aliases, a façade-only API, public mount options, or any new storage callback."
    - "[FLAGGED-UNVERIFIED] Do not describe Local's filesystem-path convenience as portable cross-adapter behavior."
---

<objective>
Publish the deliberate configuration, command, storage, and queue extension contract and repair the one runtime contradiction that prevents conforming adapters from working.

Purpose: Give adopters an exact compatibility surface while keeping repository commands and implementation conveniences from becoming accidental API.
Output: A canonical configuration/command guide, clarified behavior and adapter docs, and capability-aware export delivery.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/200-public-surface/200-CONTEXT.md
@.planning/phases/200-public-surface/200-RESEARCH.md
@.planning/phases/200-public-surface/200-PATTERNS.md
@.planning/phases/200-public-surface/200-01-SUMMARY.md
@test/threadline/public_surface_contract_test.exs
@test/threadline/operator_surface/controllers/export_controller_test.exs
@mix.exs
@lib/threadline/storage.ex
@lib/threadline/operator_surface/controllers/export_controller.ex

<interfaces>
- `Threadline.Storage` required callbacks: `init/1`, `put/2`, `get/1`, `download_url/2`, `delete/1`; optional callback: `path/1`.
- `resolve_delivery/2` currently maps local paths to `{:local, absolute_path}` and remote URLs to `{:remote, url}`; only callback capability selection changes.
- Runtime keys discovered by research: `:ecto_repos`, `:retention`, `:exports`, `:trigger_capture`, `:verify_coverage`, `:storage_adapter`, `:health`, `:coverage_poll_ms`, `:operator_surface_embed_fonts`, `:export_status_poll_ms`, `:export_queue_adapter`, `:retention_poll_ms`, `:operator_surface_embed_scripts`, `:storage_schema`, plus dynamic adapter-module keys.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace every supported key and command into one public reference</name>
  <reversibility rating="costly">D-01 makes documented adopter-public configuration a compatibility promise whose removal needs deprecation and release notes.</reversibility>
  <read_first>
    - test/threadline/public_surface_contract_test.exs: public_inventory classification
    - mix.exs: aliases/0 and preferred_cli_env/0
    - lib/threadline/application.ex
    - .planning/phases/200-public-surface/200-RESEARCH.md: Recommended config inventory and task/alias classification
  </read_first>
  <files>guides/configuration-and-commands.md</files>
  <action>Create the canonical D-05 reference using stable domain language. Document the fourteen literal runtime keys listed in `<interfaces>`, their current shapes/default sources and owning capability, and the dynamic module-key configuration seam without enumerating test doubles. Classify all `Mix.Tasks.Threadline.*` modules and every `aliases/0` key: the adopter task set is exactly install, trigger generation, coverage verification/viewing, continuity, retention purge, export, incident, evidence show, and policy show; `Mix.Tasks.Threadline.VerifyTopology` and its alias are repository-only; critic tasks and credentialed/phase-specific critic aliases are maintainer-only; all other `verify.*`, `test.*`, and `ci.all` aliases are repository commands and never host-project aliases. Per D-04, group `:coverage_poll_ms` default `30_000`, `:export_status_poll_ms` default `5_000`, and `:retention_poll_ms` default `5_000` under advanced operator tuning, require positive integer milliseconds, distinguish worker/cleanup schedules, and omit internal socket assigns/mount-option fiction. Include purpose, safe example, default/absence semantics, and owning public module or guide for each adopter key. End with `Next steps`; Plan 200-04 will wire this reference from Adopt and Operate. Do not add a second tutorial or duplicate an end-to-end setup sequence.</action>
  <acceptance_criteria>
    - The public-inventory tag passes exact set equality, disjointness, duplicate detection, and documented-public-member checks.
    - The reference clearly labels host-adopter tasks versus commands available only in Threadline's repository.
    - Dynamic adapter-module configuration is explicit and no test-only module becomes public documentation.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only public_inventory -x</automated>
    <fails_when>A discovered key/task/alias is unclassified, classifications overlap, discovery is empty, a public item is undocumented, or a documented identifier does not exist.</fails_when>
  </verify>
  <done>Every current runtime key, task, and alias has one explicit classification and every supported item is described in the canonical reference.</done>
</task>

<task type="auto">
  <name>Task 2: Make storage and queue extension docs compatibility-grade</name>
  <reversibility rating="one-way">D-02 publishes the storage behavior for adopter implementations; changing it later breaks external adapters or requires deliberate deprecation.</reversibility>
  <read_first>
    - lib/threadline/storage.ex
    - lib/threadline/storage/local.ex
    - lib/threadline/storage/s3.ex
    - lib/threadline/export_queue.ex
    - lib/threadline/export_queue/task_adapter.ex
    - lib/threadline/export_queue/oban.ex
    - guides/configuration-and-commands.md
  </read_first>
  <files>lib/threadline/storage.ex, lib/threadline/storage/local.ex, lib/threadline/storage/s3.ex, lib/threadline/export_queue.ex, lib/threadline/export_queue/task_adapter.ex, lib/threadline/export_queue/oban.ex</files>
  <action>Implement the already locked D-02 public-contract decision without re-asking it. Rename only the misleading public type/docs around `put/2` so the portable input is binary content; do not rename callback functions or alter behavior arities. Document `config :threadline, storage_adapter: MyApp.AuditStorage`, behavior implementation expectations, Local and optional S3 selection, S3 module-keyed options, initialization/failure semantics, Local's regular-file-path detection as adapter-specific convenience, and the optional nature of `path/1`. Apply the same consumer-first standard to `Threadline.ExportQueue`, `TaskAdapter`, and optional `Oban`: document the supported `:export_queue_adapter` seam, adapter-module options, and why custom queue adapters call `Threadline.Export.Orchestrator.run/2`. Keep optional dependency guards and current runtime behavior intact.</action>
  <acceptance_criteria>
    - Public storage docs show the required custom-adapter configuration and both built-in adapters.
    - Cross-adapter `put/2` text promises binary content only; Local's extra path handling is clearly local.
    - Queue docs expose the existing behavior/adapter seam without turning repository internals into supported API.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only public_inventory -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --warnings-as-errors</automated>
    <fails_when>Public identifiers/config examples drift from source, a callback signature changes, optional dependencies become required, or compilation emits a warning.</fails_when>
  </verify>
  <done>Storage and queue extension contracts are explicit, portable, and source-accurate with no new public callback family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Honor the optional storage path callback during export delivery</name>
  <read_first>
    - lib/threadline/operator_surface/controllers/export_controller.ex: resolve_delivery/2 and download_url_opts/2
    - test/threadline/operator_surface/controllers/export_controller_test.exs: Phase 200 red adapter and existing RemoteStorageStub cases
    - lib/threadline/storage.ex: optional_callbacks declaration
  </read_first>
  <files>lib/threadline/operator_surface/controllers/export_controller.ex</files>
  <behavior>
    - Missing optional path/1 selects download_url/2 and preserves adapter-module options/expiry.
    - `{:error, :not_local}` selects the same remote path.
    - `{:ok, absolute_path}` remains local delivery; any other path/1 error remains the established unavailable response.
  </behavior>
  <action>In `resolve_delivery/2`, test `function_exported?(storage_adapter, :path, 1)` before invoking the optional callback. When absent, run exactly the existing remote `download_url/2` path with `download_url_opts/2`; when present, preserve the current local success, `:not_local` fallback, and other-error behavior. Remove the Phase 200 red tag after the regression passes. Do not change authorization, expiration, redirect, send-file, cache headers, status codes, callback names/arities, or storage configuration.</action>
  <acceptance_criteria>
    - The behavior-conforming no-path adapter returns the exact expected 302 location.
    - Existing local and RemoteStorageStub branches remain green.
    - No new callback or compatibility alias appears in Threadline.Storage.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/operator_surface/controllers/export_controller_test.exs -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format</automated>
    <fails_when>The no-path adapter crashes or fails to redirect, an existing delivery outcome changes, the red tag remains, or formatting fails.</fails_when>
  </verify>
  <done>Operator export delivery supports every conforming Threadline.Storage adapter, including those that omit optional path/1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config → Threadline runtime | Adopter-controlled modules and values select storage, queue, and operator behavior. |
| storage adapter → export controller | A third-party adapter returns a local path, remote URL, or error. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-04 | Denial of service | optional path/1 dispatch | medium | mitigate | Capability check plus no-path integration regression in export_controller_test.exs. |
| T-200-05 | Tampering | config/task reference | medium | mitigate | Source-derived exact classifications and documented shape/default checks. |
| T-200-06 | Information disclosure | adapter configuration docs | low | accept | Examples use fictional application modules and option names already supported; no credentials or real endpoints are embedded. |
</threat_model>

<verification>
Run all task checks, then run `ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs -x`.
</verification>

<success_criteria>
- The inventory contract's public configuration/command lane is green.
- Public storage and queue docs match existing callbacks, adapters, and application configuration.
- The optional-callback regression and all prior export-controller behaviors are green.
</success_criteria>

## Artifacts this phase produces

- `guides/configuration-and-commands.md`, the single public configuration and command reference.
- Updated public contracts on `Threadline.Storage`, `Threadline.Storage.Local`, `Threadline.Storage.S3`, `Threadline.ExportQueue`, `Threadline.ExportQueue.TaskAdapter`, and `Threadline.ExportQueue.Oban`.
- Capability-aware optional-`path/1` dispatch inside `Threadline.OperatorSurface.Controllers.ExportController`; no new public symbol.

<output>
Create `.planning/phases/200-public-surface/200-02-SUMMARY.md` when done.
</output>
