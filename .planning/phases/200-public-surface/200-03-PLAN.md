---
phase: 200-public-surface
plan: 03
type: execute
wave: 3
depends_on:
  - 200-01
  - 200-02
files_modified:
  - mix.exs
  - CHANGELOG.md
  - lib/threadline/critic_trust/measure.ex
  - lib/threadline/critic_trust/rank_metrics.ex
  - lib/threadline/critic_trust/ledger_splice.ex
  - lib/threadline/critic_trust/krippendorff_alpha.ex
  - lib/mix/tasks/critic.measure.ex
  - lib/mix/tasks/critic.synth.ex
  - lib/threadline/capture/migration.ex
  - lib/threadline/capture/redaction_policy.ex
  - lib/threadline/capture/trigger_capture_config.ex
  - lib/threadline/capture/trigger_sql.ex
  - lib/threadline/export/cleanup_task.ex
  - lib/threadline/governance/export_job.ex
  - lib/threadline/governance/migration.ex
  - lib/threadline/governance/retention_run.ex
  - lib/threadline/governance/saved_view.ex
  - lib/threadline/health/coverage_schemas.ex
  - lib/threadline/policy/redaction_presenter.ex
  - lib/threadline/retention/pruner.ex
  - lib/threadline/semantics/migration.ex
  - lib/threadline/operator_surface/controllers/export_controller.ex
  - lib/threadline/operator_surface/controllers/theme_controller.ex
  - lib/threadline/operator_surface/coverage/on_mount.ex
  - lib/threadline/operator_surface/coverage/snapshot.ex
  - lib/threadline/operator_surface/export_auth_plug.ex
  - lib/threadline/operator_surface/session_plug.ex
  - lib/threadline/operator_surface/theme_auth_plug.ex
  - lib/threadline/operator_surface/exports/filename.ex
  - lib/threadline/operator_surface/exports/filter_params.ex
  - lib/threadline/operator_surface/fonts.ex
  - lib/threadline/operator_surface/presentation.ex
  - lib/threadline/operator_surface/scope.ex
  - lib/threadline/operator_surface/script.ex
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/ui.ex
autonomous: true
requirements:
  - SURFACE-01
  - SURFACE-02
  - SURFACE-03
  - SURFACE-04
  - SURFACE-05
  - SURFACE-07
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-08/D-09/D-10: ExDoc visibility follows supported call paths, return types, and extension roles; six critic pages are always hidden and every conditional candidate is audit-backed."
    - "D-11/D-12: every visible module appears exactly once in the six fixed groups and no hidden module is grouped."
    - "D-13/D-14/D-22: newly hidden 0.9 pages are recorded as documentation-surface clarification, while shipped source/module docs use durable public language with no planning chronology."
    - "D-15/D-20/D-21: README is the native HexDocs main page, the existing six intent lanes remain ordered, and both version-pinned repository resources are reachable without entering the package."
  artifacts:
    - path: mix.exs
      provides: README main, Guides label, explicit six module groups, URL extras, package contract, and fatal release docs gate
    - path: CHANGELOG.md
      provides: Unreleased documentation-surface clarification
    - path: lib/threadline/critic_trust/measure.ex
      provides: Hidden maintainer module representative of the unconditional set
  key_links:
    - from: mix.exs
      to: test/threadline/public_surface_contract_test.exs
      via: exact six-group visible-module set
      pattern: "groups_for_modules"
    - from: mix.exs
      to: DESIGN-SYSTEM.md
      via: URL extra interpolating doc_source_ref/0
      pattern: "DESIGN-SYSTEM"
    - from: mix.exs
      to: examples/threadline_phoenix/README.md
      via: URL extra interpolating doc_source_ref/0
      pattern: "threadline_phoenix"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Documentation hiding must not be described as runtime privacy or used to remove a supported return/extension module merely to reduce the index."
    - "[FLAGGED-UNVERIFIED] DESIGN-SYSTEM.md and the reference-app README must remain external links, never package files."
    - "[FLAGGED-UNVERIFIED] Preserve native ExDoc, the existing Mermaid hook, theme-aware README asset, favicon, and six intent lanes; no custom site or skin."
---

<objective>
Make the generated API index and Hex package configuration express one deliberate, exhaustive public module surface.

Purpose: Prevent runtime plumbing and maintainer tools from becoming accidental semver promises while keeping every useful adopter-facing type and extension point discoverable.
Output: Audited visibility annotations, six exact module groups, README-led ExDoc configuration, external version-pinned resources, changelog clarification, and fatal docs warnings in the release lane.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/200-public-surface/200-CONTEXT.md
@.planning/phases/200-public-surface/200-RESEARCH.md
@.planning/phases/200-public-surface/200-PATTERNS.md
@.planning/phases/200-public-surface/200-01-SUMMARY.md
@.planning/phases/200-public-surface/200-02-SUMMARY.md
@test/threadline/public_surface_contract_test.exs
@test/threadline/release_artifact_contract_test.exs
@mix.exs
@CHANGELOG.md

<interfaces>
- `Code.fetch_docs/1` returns module-doc visibility independently from runtime callability.
- The public visible inventory and six group memberships are enumerated in Phase 200 RESEARCH.md Pattern 2.
- `doc_source_ref/0` returns `v<package-version>` for releases and `main` only for prereleases; every external resource URL must use it.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one hidden maintainer module and one public façade through ExDoc</name>
  <read_first>
    - test/threadline/public_surface_contract_test.exs: module_visibility_tracer and module_visibility tags
    - mix.exs: docs/0 groups_for_modules
    - lib/threadline/critic_trust/measure.ex
    - lib/threadline.ex
    - .planning/phases/200-public-surface/200-RESEARCH.md: recommended visible inventory
  </read_first>
  <files>mix.exs, lib/threadline/critic_trust/measure.ex, lib/threadline/critic_trust/rank_metrics.ex, lib/threadline/critic_trust/ledger_splice.ex, lib/threadline/critic_trust/krippendorff_alpha.ex, lib/mix/tasks/critic.measure.ex, lib/mix/tasks/critic.synth.ex</files>
  <action>Add `@moduledoc false` to all six D-09 maintainer-only modules. Replace `groups_for_modules` with the six fixed keys in this exact order: `Core API`, `Data Types`, `Configuration & Extension Points`, `Integrations`, `Operator Surface`, `Mix Tasks`. Seed the explicit lists from the research inventory, preserving `Threadline` in Core API, `Threadline.Storage` in Configuration & Extension Points, `Threadline.Integrations.Sigra` in Integrations, `Threadline.OperatorSurface.Router`/`Auth` in Operator Surface, public returned structs in Data Types, and only the ten adopter task modules in Mix Tasks. Exclude `Mix.Tasks.Threadline.VerifyTopology`. Make the tracer tag prove a representative public façade is grouped and a representative critic module is hidden before expanding the full audit.</action>
  <acceptance_criteria>
    - All six unconditional critic modules return hidden module docs.
    - The group key list is exactly the locked six-key order.
    - The tracer tag proves both inclusion and exclusion through compiled docs metadata.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only module_visibility_tracer -x</automated>
    <fails_when>A critic page remains visible, the stable public façade is absent/ungrouped, the six group keys or order differ, or discovery is empty.</fails_when>
  </verify>
  <done>A real public façade and hidden maintainer module traverse compilation, docs metadata, and ExDoc grouping successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the audit-backed visibility and public-source vocabulary sweep</name>
  <read_first>
    - .planning/phases/200-public-surface/200-CONTEXT.md: D-08 through D-14
    - .planning/phases/200-public-surface/200-RESEARCH.md: full visible/hidden inventories and current archive offenders
    - test/threadline/public_surface_contract_test.exs: complete visibility and vocabulary assertions
    - Every file listed in this task's files field before changing its module docs
  </read_first>
  <files>CHANGELOG.md, lib/threadline/capture/migration.ex, lib/threadline/capture/redaction_policy.ex, lib/threadline/capture/trigger_capture_config.ex, lib/threadline/capture/trigger_sql.ex, lib/threadline/export/cleanup_task.ex, lib/threadline/governance/export_job.ex, lib/threadline/governance/migration.ex, lib/threadline/governance/retention_run.ex, lib/threadline/governance/saved_view.ex, lib/threadline/health/coverage_schemas.ex, lib/threadline/policy/redaction_presenter.ex, lib/threadline/retention/pruner.ex, lib/threadline/semantics/migration.ex, lib/threadline/operator_surface/controllers/export_controller.ex, lib/threadline/operator_surface/controllers/theme_controller.ex, lib/threadline/operator_surface/coverage/on_mount.ex, lib/threadline/operator_surface/coverage/snapshot.ex, lib/threadline/operator_surface/export_auth_plug.ex, lib/threadline/operator_surface/session_plug.ex, lib/threadline/operator_surface/theme_auth_plug.ex, lib/threadline/operator_surface/exports/filename.ex, lib/threadline/operator_surface/exports/filter_params.ex, lib/threadline/operator_surface/fonts.ex, lib/threadline/operator_surface/presentation.ex, lib/threadline/operator_surface/scope.ex, lib/threadline/operator_surface/script.ex, lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/ui.ex</files>
  <action>For every D-10 candidate, use the contract's source/docs/test call-site audit to determine whether a supported adopter calls it, receives it, configures it, or must implement it. Hide the research-confirmed plumbing with `@moduledoc false`; preserve the exact research visible inventory, including `Threadline.Export.Orchestrator`, all public result structs, behavior/adapter modules, and operator Router/Auth. Do not treat unknown external callers as evidence to shrink a supported role. Rewrite any published `@moduledoc`/`@doc`, source comment, or identifier in this bounded source cohort that contains planning chronology into the durable invariant or domain reason it represented; keep runtime semantics unchanged. Shorten visible module first paragraphs for ExDoc listings and replace references to hidden implementation modules with façade/task/domain language. Update the complete six group lists to exact set equality after the audit. In CHANGELOG's Unreleased section, list every newly hidden previously documented 0.9 module as documentation-surface clarification and state that callable code was not made runtime-private. Do not use hiding as a compatibility substitute.</action>
  <acceptance_criteria>
    - Visible compiled modules equal flattened group members; every member occurs once; hidden and visible sets are disjoint.
    - D-09 modules and every audit-confirmed D-10 internal are hidden, while supported façades, return structs, behaviors, adapters, integrations, Router/Auth, Orchestrator, and adopter Mix tasks remain visible.
    - The bounded source cohort contains no public planning chronology and no runtime behavior changes.
    - CHANGELOG names the clarification honestly.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only module_visibility -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --warnings-as-errors</automated>
    <fails_when>A visible module is missing/duplicated/ungrouped, a hidden module is grouped, a required public role disappears, a source-vocabulary offender remains, or compilation warns.</fails_when>
  </verify>
  <done>The entire compiled public module surface is explicit, audit-backed, grouped once, and described without internal chronology.</done>
</task>

<task type="auto">
  <name>Task 3: Make README-led HexDocs and external resources release-shaped</name>
  <read_first>
    - mix.exs: doc_source_ref/0, package/0, docs/0, verify_release/1, Mermaid hooks
    - test/threadline/release_artifact_contract_test.exs: URL extra normalization and package absence tests
    - brandbook/brand-book.md
    - brandbook/README.md
  </read_first>
  <files>mix.exs</files>
  <action>Per D-15, set `main: "readme"` and `extra_section: "Guides"`; preserve exact extra-group order `Overview`, `Integrations`, `Evaluate`, `Adopt`, `Operate`, `Contribute` and the README's four intent lanes. Add `guides/configuration-and-commands.md` to the appropriate existing lane. Per D-20, add first-class external URL extras for `examples/threadline_phoenix/README.md` under Adopt and `DESIGN-SYSTEM.md` under Contribute, interpolating the existing `doc_source_ref/0`; never add either file to `package.files`. Use the installed ExDoc URL-extra shape and keep local-versus-URL normalization explicit. Per D-21, leave native responsive/light-dark behavior, existing theme-aware Mermaid hook, README picture asset, and favicon intact; add no custom docs site or skin. Change only the existing `verify.release` docs step to run warnings-as-errors so release and direct docs gates cannot drift. Plan 200-04 resolves prose/link warnings before the final gate.</action>
  <acceptance_criteria>
    - README is the ExDoc main page and Guides is the extra-section label.
    - Both external resources carry the version-derived ref, appear in the correct existing lanes, and remain absent from package files/archive.
    - `verify.release` has one warnings-as-errors docs step and no duplicate release alias.
    - Existing Mermaid/theme/favicon hooks are byte-semantically intact.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/release_artifact_contract_test.exs --only url_extras -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix format --check-formatted</automated>
    <fails_when>A resource URL is unversioned/misgrouped, either repository resource enters the package, main/extra_section/group order drifts, the release docs command is non-fatal, or formatting fails.</fails_when>
  </verify>
  <done>HexDocs has one README-led navigation authority, six explicit module groups, and two version-pinned link-only project resources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled modules → ExDoc index | Documentation visibility creates perceived compatibility commitments. |
| repository paths → generated HexDocs/Hex archive | External references must remain links rather than shipped files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-01 | Information disclosure | module visibility/grouping | medium | mitigate | Code.fetch_docs exact-set contract, unconditional hidden set, audit-backed candidate set, and duplicate rejection. |
| T-200-02 | Information disclosure | package/external extras | high | mitigate | Explicit package allowlist plus archive absence and versioned URL-extra assertions. |
| T-200-07 | Tampering | release documentation gate | medium | mitigate | Modify the sole verify.release docs step and assert warnings-as-errors in project config. |
</threat_model>

<verification>
Run all task checks. The full docs warnings gate is intentionally sampled after Plan 200-04 repairs guide references; this plan must not add warning skips or accepted-warning baselines.
</verification>

<success_criteria>
- Exact compiled-visible/grouped set equality is green.
- All unconditional and audit-confirmed internals are hidden without hiding supported roles.
- README main, Guides label, six intent lanes, version-pinned external resources, package absence, and release command shape are contract-green.
</success_criteria>

## Artifacts this phase produces

- Six explicit `groups_for_modules` lists and README-led ExDoc settings in `mix.exs`.
- Version-pinned URL extras for `DESIGN-SYSTEM.md` and `examples/threadline_phoenix/README.md` without package inclusion.
- `@moduledoc false` annotations for the six D-09 modules and each audit-confirmed D-10 internal module.
- An Unreleased `CHANGELOG.md` clarification covering newly hidden 0.9 module pages.
- A warnings-as-errors docs step inside the existing `verify.release` alias.

<output>
Create `.planning/phases/200-public-surface/200-03-SUMMARY.md` when done.
</output>
