---
phase: 200-public-surface
plan: 03
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - lib/threadline/storage.ex
  - lib/threadline/storage/local.ex
  - lib/threadline/storage/s3.ex
  - lib/threadline/export_queue.ex
  - lib/threadline/export_queue/task_adapter.ex
  - lib/threadline/export_queue/oban.ex
  - lib/threadline/operator_surface/controllers/export_controller.ex
  - test/threadline/operator_surface/controllers/export_controller_test.exs
autonomous: false
requirements: [SURFACE-07]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-02: the already-confirmed storage adapter compatibility commitment is acknowledged before publication and implemented exactly as locked."
    - "D-03/D-07: a conforming adapter without optional path/1 falls back to download_url/2 without a new callback, alias, or configuration redesign."
  artifacts:
    - path: lib/threadline/storage.ex
      provides: Public behavior and portable binary-content contract
    - path: lib/threadline/operator_surface/controllers/export_controller.ex
      provides: Capability-aware optional callback dispatch
  key_links:
    - from: lib/threadline/operator_surface/controllers/export_controller.ex
      to: lib/threadline/storage.ex
      via: function_exported?/3 before path/1
      pattern: "function_exported\\?.*:path.*1"
---

<objective>
Make the locked storage/queue extension contract compatibility-grade and repair its one implementation contradiction.

Purpose: Let custom adapters implement the published behavior without depending on Local-only conveniences.
Output: Clarified storage/queue docs and capability-aware export delivery.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/200-public-surface/200-CONTEXT.md
@.planning/phases/200-public-surface/200-RESEARCH.md
@.planning/phases/200-public-surface/200-01-SUMMARY.md
@test/threadline/public_surface_contract_test.exs
@test/threadline/operator_surface/controllers/export_controller_test.exs
@lib/threadline/storage.ex
@lib/threadline/operator_surface/controllers/export_controller.ex
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm execution of the locked storage compatibility commitment</name>
  <read_first>
    - .planning/phases/200-public-surface/200-CONTEXT.md: D-02 and D-03
    - .planning/phases/200-public-surface/200-RESEARCH.md: Resolved Questions and Pattern 4
  </read_first>
  <decision>Confirm execution of the already-locked D-02 storage compatibility commitment</decision>
  <context>D-02 is already the developer-approved architecture. This checkpoint does not reopen storage design; it records acknowledgement that publishing the behavior invites external implementations and later changes require deprecation.</context>
  <action>Present only the confirmation-or-halt gate below. Accept `confirm D-02` as authorization to publish the already-locked contract; accept `halt` only as stopping execution. Do not offer or infer an alternate adapter architecture.</action>
  <options>
    <option id="confirm-d02">
      <name>Confirm locked D-02</name>
      <pros>Execution proceeds with the exact binary-content behavior, Local/S3 documentation, and module-keyed configuration already chosen.</pros>
      <cons>Future incompatible changes require a deliberate deprecation path.</cons>
    </option>
    <option id="halt">
      <name>Halt phase execution</name>
      <pros>No public compatibility commitment is published in this run.</pros>
      <cons>Phase 200 cannot satisfy SURFACE-07 or reach release readiness.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The recorded response is exactly `confirm D-02` or `halt`; no product choice is reopened.
    - Only `confirm D-02` permits Task 2 to begin.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/200-public-surface/200-CONTEXT.md &amp;&amp; rg -q '\*\*D-02:\*\* Treat `:storage_adapter` as a supported global extension point' .planning/phases/200-public-surface/200-CONTEXT.md</automated>
    <fails_when>The command exits non-zero, the context artifact is missing or empty, or the locked D-02 commitment cannot be found verbatim before confirmation.</fails_when>
  </verify>
  <resume-signal>Reply "confirm D-02" to execute the locked decision, or "halt" to stop without substituting another architecture.</resume-signal>
  <done>The locked one-way commitment is explicitly acknowledged without reopening its architecture.</done>
</task>

<task type="tracer">
  <name>Task 2: Publish the confirmed storage and queue extension contract</name>
  <read_first>
    - .planning/phases/200-public-surface/200-CONTEXT.md: D-02 and D-07
    - lib/threadline/storage.ex
    - lib/threadline/storage/local.ex
    - lib/threadline/storage/s3.ex
    - lib/threadline/export_queue.ex
  </read_first>
  <reversibility rating="one-way">D-02 publishes a behavior for adopter implementations; incompatible change later breaks external adapters or requires deprecation.</reversibility>
  <files>lib/threadline/storage.ex, lib/threadline/storage/local.ex, lib/threadline/storage/s3.ex, lib/threadline/export_queue.ex, lib/threadline/export_queue/task_adapter.ex, lib/threadline/export_queue/oban.ex</files>
  <action>Implement D-02 exactly as confirmed. Rename only misleading public type/docs around put/2 so portable input is binary content; do not rename callbacks or change arities. Document config :threadline, storage_adapter: MyApp.AuditStorage, behavior expectations, Local and optional S3 selection, S3 module-keyed options, initialization/failure semantics, Local regular-file-path detection as adapter-specific convenience, and optional path/1. Apply the same consumer-first standard to Threadline.ExportQueue, TaskAdapter, and optional Oban: document the supported :export_queue_adapter seam, adapter-module options, and why custom queues call Threadline.Export.Orchestrator.run/2. Preserve optional dependency guards and runtime semantics.</action>
  <acceptance_criteria>
    - Storage and queue behaviors document their supported callbacks, built-ins, configuration, failure semantics, and optional dependency boundaries.
    - The portable storage input is binary content while Local path detection remains explicitly adapter-specific.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only public_inventory -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/public_surface_contract_test.exs --only public_doc_refs_extensions -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --warnings-as-errors</automated>
    <fails_when>Any command exits non-zero or selects zero tests, public inventory no longer matches source, an extension-doc module/task/config reference is nonexistent, compilation emits a warning, or documented callbacks/configuration diverge from the implementation.</fails_when>
  </verify>
  <done>Storage and queue extension contracts are explicit, portable, and source-accurate with no new callback family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Honor the optional storage path callback during export delivery</name>
  <read_first>
    - .planning/phases/200-public-surface/200-CONTEXT.md: D-03
    - lib/threadline/operator_surface/controllers/export_controller.ex: resolve_delivery/2
    - test/threadline/operator_surface/controllers/export_controller_test.exs: no-path and remote-adapter regressions
  </read_first>
  <files>lib/threadline/operator_surface/controllers/export_controller.ex, test/threadline/operator_surface/controllers/export_controller_test.exs</files>
  <behavior>
    - Missing optional path/1 selects download_url/2 and preserves adapter options and expiry.
    - {:error, :not_local} selects the same remote path.
    - Local success and other established errors remain unchanged.
  </behavior>
  <action>Per D-03, test function_exported?(storage_adapter, :path, 1) before invoking the optional callback. When absent, use the existing remote download_url/2 path and download_url_opts/2; when present, preserve local success, :not_local fallback, and other-error behavior. Remove the Phase 200 red tag after the regression passes. Do not change authorization, expiry, cache headers, status codes, callback names/arities, or configuration.</action>
  <acceptance_criteria>
    - An adapter omitting optional path/1 redirects through download_url/2 with the established options and expiry.
    - Local delivery, :not_local fallback, authorization, and all established error responses remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix test test/threadline/operator_surface/controllers/export_controller_test.exs -x &amp;&amp; ASDF_ERLANG_VERSION=27.3.4.15 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format</automated>
    <fails_when>Either command exits non-zero, the controller suite selects zero tests, the no-path adapter does not redirect, any existing delivery status/header changes, or formatting fails.</fails_when>
  </verify>
  <done>Every conforming storage adapter, including one omitting optional path/1, can deliver completed exports.</done>
</task>

</tasks>

<threat_model>
**ASVS level:** L1

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config → Threadline runtime | Adopter-selected modules control storage and queue behavior. |
| storage adapter → export controller | Third-party results choose local, remote, or unavailable delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-04 | Denial of service | optional path/1 dispatch | medium | mitigate | Capability check and no-path adapter regression. |
| T-200-06 | Information disclosure | adapter docs | low | accept | Examples use fictional modules and supported option shapes only. |
</threat_model>

<verification>After D-02 confirmation, run both automated task checks and the combined public-inventory/controller suite.</verification>

<success_criteria>
- The locked D-02 commitment is confirmed through a workflow-valid gate.
- Public storage/queue docs match existing behavior and adapters.
- Optional path/1 absence falls back without changing any other delivery result.
</success_criteria>

## Artifacts this phase produces

- Compatibility-grade storage/queue behavior and adapter documentation.
- Capability-aware optional `path/1` export delivery with a green regression.

<output>Create .planning/phases/200-public-surface/200-03-SUMMARY.md when done.</output>
