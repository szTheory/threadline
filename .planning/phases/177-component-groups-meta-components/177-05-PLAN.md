---
phase: 177-component-groups-meta-components
plan: 05
type: execute
wave: 5
depends_on: ["177-04"]
files_modified:
  - lib/threadline/operator_surface/stress_fixtures.ex
  - .planning/design-system-ledger.json
  - DESIGN-SYSTEM.md
  - test/threadline/operator_surface/stress_fixtures_test.exs
  - test/threadline/operator_surface/stress_ledger_test.exs
autonomous: true
requirements: [GROUP-01, GROUP-02]

must_haves:
  truths:
    - "All 12 GROUP-01 configurations are present as group stress stories in stress_fixtures.ex (the 6 reserved baselines remapped/absorbed + the missing 6 added)."
    - "Each group story is tagged `live` vs `reference-only` so Phase 178 knows which groups ship on a real page (D-07)."
    - "Every group story exists in BOTH stress_fixtures.ex AND design-system-ledger.json AND DESIGN-SYSTEM.md (ledger↔fixtures↔projection parity holds)."
    - "Each of the 12 stories renders without error on /audit/__stress across the viewport × theme matrix."
  prohibitions:
    - "MUST NOT add a story to fixtures without the matching ledger row + DESIGN-SYSTEM projection — it would silently not render and break parity (Runtime State Inventory parity caution)."
    - "MUST NOT extract drawer+form or tabs+subviews into shipped pages — they are reference-only stress assemblies this phase (D-07, Deferred Ideas)."
    - "MUST NOT touch the capture or semantics layers (v1.37 invariant) — this is presentational verification scaffold only."
  artifacts:
    - path: "lib/threadline/operator_surface/stress_fixtures.ex"
      provides: "12 group stories with a live/reference surface tag"
      contains: "surface"
    - path: ".planning/design-system-ledger.json"
      provides: "12 group ledger rows matching the fixtures"
      contains: "group."
    - path: "DESIGN-SYSTEM.md"
      provides: "Groups section projection rows for the 12 configurations"
      contains: "group"
  key_links:
    - from: "lib/threadline/operator_surface/stress_fixtures.ex (@group_stories)"
      to: ".planning/design-system-ledger.json (entries)"
      via: "ledger↔fixtures parity (stress_ledger_test)"
      pattern: "group\\."
    - from: ".planning/design-system-ledger.json"
      to: "DESIGN-SYSTEM.md"
      via: "deterministic projection freshness (stress_ledger_test)"
      pattern: "Groups"
---

<objective>
Wave 5: map all 12 GROUP-01 configurations onto group stress stories, tag each `live` vs `reference-only` (D-07), and keep the ledger↔fixtures↔DESIGN-SYSTEM projection in lockstep — so every group is auditable as a unit on `/audit/__stress` at 320/375/768/1024/1440 × dark/light/system.

Purpose: GROUP-01's literal acceptance list is the 12 configurations "audited as units." The 6 reserved baselines (`action-bar`, `filter-bar`, `kv-list`, `pagination`, `status-strip`, `timeline-list`) get remapped/absorbed and the missing 6 added; the audit surface is the verification scaffold the whole phase is checked against. Depends on Plan 04 (all components + motion + offline must exist before the group stories render them).
Output: 12 group stories + matching ledger rows + DESIGN-SYSTEM projection; updated parity tests; the audit pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-component-groups-meta-components/177-RESEARCH.md
@.planning/phases/177-component-groups-meta-components/177-UI-SPEC.md
@lib/threadline/operator_surface/stress_fixtures.ex
@lib/threadline/operator_surface/live/stress_live.ex
@test/threadline/operator_surface/stress_ledger_test.exs
@DESIGN-SYSTEM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remap @group_stories to the 12 configurations with a live/reference surface tag</name>
  <files>lib/threadline/operator_surface/stress_fixtures.ex, test/threadline/operator_surface/stress_fixtures_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/stress_fixtures.ex (L65-78 `@group_stories` 6 baselines; L243 `reserved_story_maps(@group_stories, "group", 177)`; L416-433 `reserved_story/5`; L435-458 `story/1` map shape incl. `metadata`)
    - lib/threadline/operator_surface/live/stress_live.ex (L9 `@ledger_path`; the ledger↔fixtures rendering — a story renders only if it's in BOTH ledger and StressFixtures.by_id)
    - test/threadline/operator_surface/stress_fixtures_test.exs (existing assertions on group stories)
    - .planning/phases/177-component-groups-meta-components/177-RESEARCH.md (Code Examples: stress-story map with live/reference tag; the 12-config → story-id mapping table; Runtime State Inventory parity caution)
  </read_first>
  <action>
    Replace the 6 reserved `@group_stories` with the 12 GROUP-01 configurations as `status: "current"`, `owner_phase: 177` stories carrying a `surface` tag (`:live` | `:reference`) in both `data` and `metadata` (so the tag flows to the ledger projection). Use a dedicated builder (e.g. `group_story/4` taking `surface in [:live, :reference]`) rather than `reserved_story/5`, mirroring `data_display_story_maps`. Wire it into `stories/0` in place of `reserved_story_maps(@group_stories, "group", 177)`.

    The 12 configurations (GROUP-01 list) → suggested ids + surface (D-14 discretion on exact ids; match the dotted-hyphen idiom):
    - page-header+actions+breadcrumbs → `group.page-header.current` (live)
    - toolbar+search+filters+sort → `group.toolbar.current` (live; absorbs `filter-bar`)
    - table+empty+loading+pagination → `group.data-panel.current` (live; absorbs `pagination`, `timeline-list`)
    - stat-cards+chart+table → `group.stats-chart-table.current` (live; absorbs `status-strip`)
    - detail-header+metadata+actions → `group.detail-header.current` (live; absorbs `kv-list`)
    - modal-confirm+destructive → `group.modal-destructive.current` (live)
    - drawer+form → `group.drawer-form.reference` (reference-only — no live page, D-07)
    - toast+state-update → `group.toast-update.current` (live)
    - tabs+subviews → `group.tabs-subviews.reference` (reference-only)
    - empty+CTA → `group.empty-cta.current` (live; **absorbs `action-bar`** — the action-bar baseline's action-cluster semantics are now carried across the action-bearing group configs (page-header+actions, detail-header+actions, empty+CTA), so `action-bar` is fully absorbed and NO standalone `group.action-bar.reserved` id is retained)
    - permission-denied → `group.permission-denied.current` (live)
    - reconnect/offline-banner+disabled-actions → `group.offline.current` (live)

    Build each story's `data` from real-page markup intent where a live page exists; for reference-only configs the story IS the canonical reference assembly (D-06b). Keep `cases`/`themes`/`viewports` per the existing `story/1` defaults. Update `stress_fixtures_test.exs` to assert: exactly 12 group stories, each carries a `surface` tag in `[:live, :reference]`, and the live/reference split matches the mapping.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_fixtures_test.exs 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 12 group stories exist; each has a `surface` tag in `[:live, :reference]`.
    - All 6 prior reserved baselines (`action-bar`, `filter-bar`, `kv-list`, `pagination`, `status-strip`, `timeline-list`) are remapped/absorbed into the 12 configurations — zero orphaned `*.reserved` group ids remain.
    - `stress_fixtures_test.exs` asserts the count, the tag presence, and the live/reference split; GREEN.
  </acceptance_criteria>
  <done>All 12 GROUP-01 configurations are group stress stories tagged live vs reference-only.</done>
</task>

<task type="auto">
  <name>Task 2: Sync design-system-ledger.json + DESIGN-SYSTEM.md to the 12 stories (parity)</name>
  <files>.planning/design-system-ledger.json, DESIGN-SYSTEM.md, test/threadline/operator_surface/stress_ledger_test.exs</files>
  <read_first>
    - .planning/design-system-ledger.json (L300-390 the 6 `group.*.reserved` entries; top-level `ratchet.locked_ids`, `ratchet.minimum_scores`, `required_inventory.groups`; `@entry_keys`/`@optional_entry_keys` enforced by the ledger test)
    - DESIGN-SYSTEM.md (the Groups section — projection rows the ledger test checks for freshness)
    - test/threadline/operator_surface/stress_ledger_test.exs (full — `@top_level_keys`, `@entry_keys`, `@allowed_kinds`, `@design_sections`, the "projection is fresh for every ledger row" + "DESIGN-SYSTEM projection contains deterministic inventory sections" tests)
    - .planning/phases/177-component-groups-meta-components/177-RESEARCH.md (Runtime State Inventory: ledger↔fixtures↔projection parity; a story in fixtures but not the ledger silently does not render)
  </read_first>
  <action>
    Update `design-system-ledger.json` so its `entries` carry one row per group story, matching the 12 fixture ids exactly (same `id`/`story_id`/`fixture_key`/`stress_path` shape as the existing group rows, `category: "group"`, `kind: "group"`, `owner_phase: 177`, `status: "current"`, scores per the ratchet rule — current_score/ratchet_score/target_score). Carry the `surface` (live/reference) signal in `notes` (the `@entry_keys`/`@optional_entry_keys` set is enforced — do NOT add an unlisted key; if a dedicated key is needed, update the test's allowlist in the SAME commit). Reconcile `required_inventory.groups` (currently the 6 baseline names) to the 12 configuration names, and update `ratchet.locked_ids` + `ratchet.minimum_scores` to reference the new group ids (remove the absorbed `*.reserved` ids). Keep top-level `@top_level_keys` intact.

    Regenerate/update the DESIGN-SYSTEM.md "Groups" section so the projection has a deterministic row for every ledger group entry (the ledger test asserts projection freshness for every row + the presence of the `@design_sections` headings including "Groups"). Run the ledger parity test to confirm fixtures↔ledger↔projection are in lockstep.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_ledger_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the 12 group fixture ids has a matching `design-system-ledger.json` entry and a DESIGN-SYSTEM.md Groups row.
    - No ledger entry uses a key outside the enforced `@entry_keys`/`@optional_entry_keys` (or the test allowlist is updated in the same commit).
    - `required_inventory.groups`, `ratchet.locked_ids`, `ratchet.minimum_scores` reference the new ids; no orphaned absorbed ids.
    - `mix test test/threadline/operator_surface/stress_ledger_test.exs` GREEN (parity + projection freshness).
  </acceptance_criteria>
  <done>Ledger and DESIGN-SYSTEM projection are in lockstep with the 12 group stories; parity tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all 12 group stories render across the matrix + full-suite gate</name>
  <files>lib/threadline/operator_surface/live/stress_live.ex, test/threadline/operator_surface/stress_router_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/live/stress_live.ex (story rendering across viewport × theme; how a group story is dispatched to its assembly)
    - test/threadline/operator_surface/stress_router_test.exs + any stress_live live test (the render-without-error pattern)
    - .planning/phases/177-component-groups-meta-components/177-VALIDATION.md (Manual-Only Verifications — the visual audit instructions)
  </read_first>
  <action>
    Add/extend a stress live render assertion that loads `/audit/__stress?story=<id>` for each of the 12 group story ids and asserts the page renders without error (the integration check from the RESEARCH Test Map: "each of the 12 stories renders without error across the viewport/theme matrix"). Ensure `stress_live.ex` dispatches each group story id to its assembly (live ids → real-page markup composing the Plan 02/03/04 components; reference-only ids → the canonical reference assembly). Then run the full gate.

    Record the manual audit checklist in the SUMMARY (the actual pixel "holds together at every viewport" judgment is a `/gsd-verify-work` human pass per VALIDATION.md): each of the 12 stories at 320/375/768/1024/1440 × dark/light/system; toggle each data_panel state (empty/loading/error/stale/no-data/permission/unavailable) confirming toolbar disables on loading/error, stale banner sits above live data, permission/unavailable collapses the body with distinct icon shape; observe overlay enter/exit + data-region cross-fade and re-check under reduced-motion; drop the LiveView socket to confirm the reconnect banner + action-disable.
  </action>
  <verify>
    <automated>mix ci.all 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - A render assertion covers all 12 group story ids loading without error on `/audit/__stress`.
    - `mix ci.all` (verify.format + verify.credo + verify.test) is fully green.
    - The manual audit checklist (viewport × theme × state × motion × reconnect) is recorded in the SUMMARY for the `/gsd-verify-work` pass.
  </acceptance_criteria>
  <done>All 12 group stories render across the matrix; the full suite is green; the manual audit checklist is staged for human verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stress scaffold → capture/semantics | the audit surface is pure presentation; it must not reach into or mutate capture/semantics state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-10 | Tampering | ledger↔fixtures drift | mitigate | parity enforced by stress_ledger_test; a fixture without a ledger row silently fails to render — both updated together |
| T-177-11 | Information disclosure / trust | permission-denied story | mitigate | the permission-denied group story audits the forensic distinction (lock vs funnel vs cloud_off) survives composition (D-176-16, ASVS V4) |
| T-177-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs (v1.37 zero-new-dep invariant) |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/stress_fixtures_test.exs test/threadline/operator_surface/stress_ledger_test.exs` — count + tags + parity GREEN.
- `mix ci.all` — full phase gate green.
- Manual (`/gsd-verify-work`): the 12-story visual audit across 320/375/768/1024/1440 × dark/light/system × state matrix × motion × reconnect.
</verification>

<success_criteria>
All 12 GROUP-01 configurations are auditable as units on `/audit/__stress`, tagged live vs reference-only, with ledger↔fixtures↔projection parity green and the full suite passing.
</success_criteria>

<output>
Create `.planning/phases/177-component-groups-meta-components/177-05-SUMMARY.md` when done, including the staged manual-audit checklist.
</output>
