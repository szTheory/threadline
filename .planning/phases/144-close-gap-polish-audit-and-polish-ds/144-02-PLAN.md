---
phase: 144-close-gap-polish-audit-and-polish-ds
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/presentation.ex
  - test/threadline/operator_surface/presentation_test.exs
  - lib/threadline/operator_surface/live/transaction_live.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
autonomous: true
requirements: [POLISH-DS]
must_haves:
  truths:
    - "Operation chip semantics are consolidated through shared presentation helpers instead of duplicate LiveView-local modifier functions."
    - "No product routes, queries, schemas, demo business logic, theme mode, Tailwind, or public component API is introduced."
  artifacts:
    - path: "lib/threadline/operator_surface/presentation.ex"
      provides: "Shared operation presentation helper contract"
      exports: ["operation_modifier/1", "operation_label/1"]
    - path: "test/threadline/operator_surface/presentation_test.exs"
      provides: "Pure helper tests for operation presentation"
      contains: "operation"
  key_links:
    - from: "lib/threadline/operator_surface/live/transaction_live.ex"
      to: "Threadline.OperatorSurface.Presentation.operation_modifier/1"
      via: "operation badge class assignment"
      pattern: "Presentation\\.operation_modifier"
    - from: "lib/threadline/operator_surface/live/timeline_live.ex"
      to: "Threadline.OperatorSurface.Presentation.operation_modifier/1"
      via: "operation badge class assignment"
      pattern: "Presentation\\.operation_modifier"
---

<objective>
Perform the narrow source-first design-system consolidation needed before the final catalog/freeze.

Purpose: POLISH-DS must close from source truth, not documentation-only acceptance of drift. This plan consolidates the remaining duplicated operation badge helper seam while preserving the Phoenix-optional mounted operator surface.
Output: Shared operation helper functions, updated call sites, and focused helper tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-close-gap-polish-audit-and-polish-ds/144-CONTEXT.md
@.planning/phases/144-close-gap-polish-audit-and-polish-ds/144-RESEARCH.md
@.planning/phases/144-close-gap-polish-audit-and-polish-ds/144-PATTERNS.md
@.planning/phases/144-close-gap-polish-audit-and-polish-ds/144-VALIDATION.md
@.planning/phases/136-design-system-hardening/136-VERIFICATION.md
@lib/threadline/operator_surface/presentation.ex
@test/threadline/operator_surface/presentation_test.exs
@lib/threadline/operator_surface/live/transaction_live.ex
@lib/threadline/operator_surface/live/timeline_live.ex

<interfaces>
Existing presentation helpers:
- `status_modifier/1` returns `tl-chip--success`, `tl-chip--danger`, `tl-chip--warning`, `tl-chip--info`, or `tl-chip--neutral`.
- `status_label/1` returns human labels such as `Completed`, `Failed`, `Queued`, `Needs capture`, and `Expected gap`.
- `value_token/1` and `change_value_token/2` return maps with `:text`, `:modifier`, and optional `:title`.
- Current duplicate private functions exist in `transaction_live.ex` and `timeline_live.ex` as `op_chip_modifier/1`; consolidate only this operation modifier seam unless a test failure proves a directly related call-site adjustment is required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared operation presentation helpers</name>
  <files>lib/threadline/operator_surface/presentation.ex, test/threadline/operator_surface/presentation_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/presentation.ex
    - test/threadline/operator_surface/presentation_test.exs
    - .planning/phases/144-close-gap-polish-audit-and-polish-ds/144-CONTEXT.md
    - .planning/phases/144-close-gap-polish-audit-and-polish-ds/144-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `Presentation.operation_modifier("INSERT")` and atom/string lowercase equivalents return `tl-change__op--insert`.
    - Test 2: `Presentation.operation_modifier("UPDATE")` returns `tl-change__op--update`.
    - Test 3: `Presentation.operation_modifier("DELETE")` returns `tl-change__op--delete`.
    - Test 4: nil or unknown operation values return an empty string or neutral safe value without raising and without atom creation.
    - Test 5: `Presentation.operation_label/1` returns uppercase display labels for known operations and a safe string label for unknown values.
  </behavior>
  <action>Extend `Threadline.OperatorSurface.Presentation` with `operation_modifier/1` and `operation_label/1` near `status_modifier/1` / `status_label/1`. Implement with string normalization only; do not use `String.to_atom/1`. Add focused tests under `test/threadline/operator_surface/presentation_test.exs` in a `describe "operation presentation"` block. Per D-07 and D-10, keep this as pure presentation logic and do not add Phoenix components, routes, queries, schemas, or dependencies.</action>
  <acceptance_criteria>
    - Behavior assertion: the new tests fail before implementation and pass after implementation when run with `DB_PORT=5433 mix test test/threadline/operator_surface/presentation_test.exs`.
    - Source assertion: `rg -n "def operation_modifier|def operation_label|operation presentation" lib/threadline/operator_surface/presentation.ex test/threadline/operator_surface/presentation_test.exs` returns matches.
    - Source assertion: `rg -n "String\\.to_atom" lib/threadline/operator_surface/presentation.ex` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>DB_PORT=5433 mix test test/threadline/operator_surface/presentation_test.exs</automated>
  </verify>
  <done>Shared operation helpers are tested as pure functions and are safe for arbitrary string/atom/nil inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Replace duplicate operation modifier call sites</name>
  <files>lib/threadline/operator_surface/live/transaction_live.ex, lib/threadline/operator_surface/live/timeline_live.ex</files>
  <read_first>
    - lib/threadline/operator_surface/live/transaction_live.ex
    - lib/threadline/operator_surface/live/timeline_live.ex
    - lib/threadline/operator_surface/presentation.ex
    - test/threadline/operator_surface/transaction_live_test.exs
    - test/threadline/operator_surface/live/timeline_live_test.exs
    - .planning/phases/144-close-gap-polish-audit-and-polish-ds/144-CONTEXT.md
  </read_first>
  <action>Update operation badge rendering in `transaction_live.ex` and `timeline_live.ex` to call `Presentation.operation_modifier/1` and `Presentation.operation_label/1`. Remove the duplicate private `op_chip_modifier/1` functions from both LiveViews after the shared helper is used. Preserve existing `tl-change__op` markup, data flow, routes, filters, queries, and business behavior. Do not touch `examples/threadline_phoenix`, Tailwind/build tooling, theme logic, or any public component API.</action>
  <acceptance_criteria>
    - Source assertion: `rg -n "Presentation\\.operation_modifier|Presentation\\.operation_label" lib/threadline/operator_surface/live/transaction_live.ex lib/threadline/operator_surface/live/timeline_live.ex` returns matches in both files.
    - Source assertion: `rg -n "defp op_chip_modifier" lib/threadline/operator_surface/live/transaction_live.ex lib/threadline/operator_surface/live/timeline_live.ex` returns no matches.
    - Behavior assertion: Timeline and Transaction tests still pass without route/query/schema changes.
    - CLI assertion: `gsd-sdk query verify.schema-drift 144 --raw` reports `drift_detected: false` or an equivalent non-blocking false result.
  </acceptance_criteria>
  <verify>
    <automated>DB_PORT=5433 mix test test/threadline/operator_surface/presentation_test.exs test/threadline/operator_surface/transaction_live_test.exs test/threadline/operator_surface/live/timeline_live_test.exs && gsd-sdk query verify.schema-drift 144 --raw</automated>
  </verify>
  <done>Transaction and Timeline operation badges consume the shared helper contract, duplicate operation modifier functions are gone, and schema drift remains false.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit row data -> LiveView class strings | Operation values from persisted audit data influence CSS class modifiers. |
| library source -> adopter mounted UI | Source changes ship in the Phoenix-optional operator surface used by host applications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-02-01 | Tampering | `Presentation.operation_modifier/1` | mitigate | Normalize operation values with strings only, never `String.to_atom/1`, and return known `.tl-change__op--*` classes only. |
| T-144-02-02 | Denial of Service | operation helper input handling | mitigate | nil/unknown values must not raise; presentation tests cover arbitrary unknown values. |
| T-144-02-03 | Elevation of Privilege | LiveView route/query/schema surface | accept | This plan must not edit auth, routes, queries, schemas, or business logic; schema drift command verifies no migration/schema changes. |
| T-144-02-SC | Tampering | package installs | accept | No npm/pip/cargo install tasks exist in this plan; package legitimacy gate is N/A. |
</threat_model>

<verification>
Run the task verification commands. If browser-impacting markup changes appear larger than the operation badge helper replacement, stop and shrink the diff back to the helper seam before proceeding.
</verification>

<success_criteria>
POLISH-DS source consolidation advances through a compatibility-preserving helper seam: operation semantics are shared, tested, and consumed by existing LiveViews without product expansion.
</success_criteria>

<output>
Create `.planning/phases/144-close-gap-polish-audit-and-polish-ds/144-02-SUMMARY.md` when done. Its frontmatter must use `requirements-completed: [POLISH-DS]`, not `requirements_completed`.
</output>
