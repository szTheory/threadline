---
phase: 69
plan: "69-03"
type: execute
wave: 3
depends_on:
  - "69-01"
  - "69-02"
files_modified:
  - test/threadline/integration_contracts_doc_contract_test.exs
  - test/threadline/operator_surface_doc_contract_test.exs
  - test/threadline/ci_topology_contract_test.exs
autonomous: true
requirements:
  - COMPAT-02
must_haves:
  truths:
    - "The named breadth story is locked by focused source-reading tests and tied to concrete proof anchors already present in the repo."
    - "`capture-only` proof is explicitly bound to `mix verify.compile_no_optional`, `phoenix-surface` proof to the mounted library test/doc path, and `sigra-reference` proof to the current example-app verification path."
    - "Phase 69 does not invent new CI topology unless a concrete mismatch is discovered; the default is to test and document the existing anchors honestly."
  artifacts:
    - path: "test/threadline/integration_contracts_doc_contract_test.exs"
      provides: "Contract guard for the new canonical integration-contract guide"
    - path: "test/threadline/operator_surface_doc_contract_test.exs"
      provides: "Operator-surface scope guard plus callback-shape/export-fallback wording guard"
    - path: "test/threadline/ci_topology_contract_test.exs"
      provides: "Stable proof-anchor guard for the CI job IDs the docs rely on"
  key_links:
    - from: "guides/integration-contracts.md"
      to: "test/threadline/integration_contracts_doc_contract_test.exs"
      via: "heading and contract literal assertions"
      pattern: "Threadline.Job"
    - from: ".github/workflows/ci.yml"
      to: "test/threadline/ci_topology_contract_test.exs"
      via: "stable job IDs cited by the support matrix"
      pattern: "verify-compile-no-optional|verify-test|verify-docs"
---

<objective>
Lock the breadth contract and support-lane proof story to the repo's actual verification surface.

Purpose: satisfy `COMPAT-02` by making docs, tests, and named verification entrypoints agree on what the repo proves today.
Output: focused doc/source contract tests and, only if needed, narrow CI-proof contract updates.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-integration-contracts-and-support-matrix/69-CONTEXT.md
@.planning/phases/69-integration-contracts-and-support-matrix/69-RESEARCH.md
@.planning/phases/69-integration-contracts-and-support-matrix/69-PATTERNS.md
@guides/integration-contracts.md
@guides/upgrade-path.md
@guides/integrations/sigra.md
@guides/operator-surface.md
@README.md
@examples/threadline_phoenix/README.md
@mix.exs
@.github/workflows/ci.yml
@test/threadline/upgrade_path_doc_contract_test.exs
@test/threadline/integrations/sigra_doc_contract_test.exs
@test/threadline/operator_surface_doc_contract_test.exs
@test/threadline/readme_doc_contract_test.exs
@test/threadline/example_phoenix_readme_contract_test.exs
@test/threadline/ci_topology_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused contract tests for the new breadth guide and support-lane wording</name>
  <files>test/threadline/integration_contracts_doc_contract_test.exs, test/threadline/operator_surface_doc_contract_test.exs</files>
  <action>
    Follow Pattern 3 from `69-PATTERNS.md` for the new guide and operator-surface scope guard: section/literal assertions, not snapshots. This slice is the Phase 68 `68-03` analog for proof-locking after the docs settle.

    Add or extend source-reading contract tests so the Phase 69 story cannot drift:

    - create `test/threadline/integration_contracts_doc_contract_test.exs` for the new guide
    - extend `operator_surface_doc_contract_test.exs` so the operator-surface guide links out to the contract/support guides instead of duplicating them
    - add assertions that the operator-surface docs no longer teach stale tuple-style callback examples and do explain the export fallback behavior accurately

    Keep the test style narrow: `String.contains?/2`, section-heading checks, marker/order checks, and direct alias/job-ID literal assertions. Do not add snapshots or full-file matching.
  </action>
  <verify>
    <automated>mix test test/threadline/integration_contracts_doc_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - The new integration-contract guide has a focused contract test.
    - The operator-surface contract test locks its new scope, corrected callback-shape examples, and export fallback wording without over-broad snapshots.
    - This plan does not re-edit test files already owned by `69-02`.
  </acceptance_criteria>
  <done>The Phase 69 breadth docs are protected by focused contract tests that will fail on claim drift.</done>
</task>

<task type="auto">
  <name>Task 2: Tie the support lanes to the actual proof anchors already present in Mix and CI</name>
  <files>test/threadline/ci_topology_contract_test.exs</files>
  <action>
    Follow Pattern 5 from `69-PATTERNS.md`: assert the existing alias/job-ID surface the docs rely on rather than redesigning CI unless a concrete mismatch is found.

    Extend the existing CI/topology contract test only as needed so the named proof anchors in docs remain grounded in the repo:

    - `capture-only` must map to `verify.compile_no_optional` / `verify-compile-no-optional`
    - `phoenix-surface` must map to the mounted library path already covered by `verify.test` and CI `verify-test` / `verify-docs`
    - `sigra-reference` must map to `verify.example` and the current example-app proof path

    Prefer asserting the existing alias/job-ID presence and order rather than redesigning them. Only edit `.github/workflows/ci.yml` or `mix.exs` if you discover a concrete mismatch between the documented proof anchors and the real repo surface.
  </action>
  <verify>
    <automated>mix test test/threadline/ci_topology_contract_test.exs test/threadline/phase06_nyquist_ci_contract_test.exs --max-failures 1</automated>
    <automated>mix verify.compile_no_optional</automated>
    <automated>mix verify.example</automated>
  </verify>
  <acceptance_criteria>
    - Contract tests make the documented proof anchors fail loudly if the repo stops exposing them.
    - The plan does not add CI topology unless a real mismatch forces it.
    - The verification commands for the phase exercise compile-without-optional and the current example-app reference path explicitly.
  </acceptance_criteria>
  <done>The support-matrix claims are tied to actual repo proof anchors instead of free-floating wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| support docs -> verification entrypoints | If docs cite proof anchors that are not tested, claims can silently outrun evidence. |
| test contracts -> CI topology | If CI job IDs or alias names drift, the support story can become false without obvious failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-07 | R | proof-anchor wording | mitigate | Lock proof-anchor literals in focused contract tests tied to Mix aliases and CI job IDs. |
| T-69-08 | T | CI topology drift | mitigate | Extend the existing topology contract only where Phase 69 docs rely on it; avoid speculative workflow redesign. |
| T-69-09 | I | example-path support claims | mitigate | Tie `sigra-reference` wording to `verify.example` and example README assertions explicitly. |
</threat_model>

<verification>
Run:
- `mix test test/threadline/integration_contracts_doc_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs --max-failures 1`
- `mix test test/threadline/ci_topology_contract_test.exs test/threadline/phase06_nyquist_ci_contract_test.exs --max-failures 1`
- `mix verify.compile_no_optional`
- `mix verify.example`
- `mix ci.all`
</verification>

<success_criteria>
- The new canonical breadth guide and operator-surface scope/callback corrections are covered by focused contract tests.
- The documented proof story maps directly to existing Mix aliases and CI jobs.
- Phase 69 tightens evidence alignment without inventing unnecessary CI topology.
</success_criteria>

<output>
After completion, create `.planning/phases/69-integration-contracts-and-support-matrix/69-03-SUMMARY.md`
</output>
