---
phase: 69
plan: "69-02"
type: execute
wave: 2
depends_on:
  - "69-01"
files_modified:
  - guides/upgrade-path.md
  - guides/integrations/sigra.md
  - README.md
  - examples/threadline_phoenix/README.md
  - test/threadline/upgrade_path_doc_contract_test.exs
  - test/threadline/integrations/sigra_doc_contract_test.exs
  - test/threadline/readme_doc_contract_test.exs
  - test/threadline/example_phoenix_readme_contract_test.exs
autonomous: true
requirements:
  - COMPAT-01
must_haves:
  truths:
    - "The support story is reduced to three named lanes: `capture-only`, `phoenix-surface`, and `sigra-reference`."
    - "Public wording distinguishes `supported`, `reference`, and `unclaimed` instead of implying broad ecosystem compatibility."
    - "Sigra is described as the current first-party reference path for an already-Sigra Phoenix host, not as generic Sigra compatibility."
  artifacts:
    - path: "guides/upgrade-path.md"
      provides: "Lane-oriented support matrix and proof-language source of truth"
    - path: "guides/integrations/sigra.md"
      provides: "Reference-path wording aligned to the narrowed support lanes"
    - path: "README.md"
      provides: "Top-level support/discovery wording that does not overclaim host breadth"
    - path: "examples/threadline_phoenix/README.md"
      provides: "Runnable reference-path wording that matches the named `sigra-reference` lane"
    - path: "test/threadline/upgrade_path_doc_contract_test.exs"
      provides: "Support-lane wording guard updated in the same slice as the matrix rewrite"
    - path: "test/threadline/integrations/sigra_doc_contract_test.exs"
      provides: "Sigra reference-path wording guard updated with the guide rewrite"
  key_links:
    - from: "guides/upgrade-path.md"
      to: "guides/integration-contracts.md"
      via: "lane vocabulary built on the canonical contract"
      pattern: "capture-only"
    - from: "guides/integrations/sigra.md"
      to: "examples/threadline_phoenix/README.md"
      via: "reference-path wording consistency"
      pattern: "reference"
    - from: "README.md"
      to: "guides/upgrade-path.md"
      via: "support-matrix discovery pointer"
      pattern: "upgrade-path.md"
    - from: "guides/upgrade-path.md"
      to: "test/threadline/upgrade_path_doc_contract_test.exs"
      via: "same-slice contract lock for lane vocabulary"
      pattern: "phoenix-surface"
---

<objective>
Narrow the public support story to the three Phase 69 lanes and align the reference-path wording around them.

Purpose: satisfy `COMPAT-01` by making Threadline's compatibility claims honest, narrow, and consistent across the public doc surfaces.
Output: lane-oriented support wording in the upgrade-path guide, Sigra guide, README, and example README.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/69-integration-contracts-and-support-matrix/69-CONTEXT.md
@.planning/phases/69-integration-contracts-and-support-matrix/69-RESEARCH.md
@.planning/phases/69-integration-contracts-and-support-matrix/69-PATTERNS.md
@guides/integration-contracts.md
@guides/upgrade-path.md
@guides/integrations/sigra.md
@README.md
@examples/threadline_phoenix/README.md
@mix.exs
@mix.lock
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the support matrix around named support lanes</name>
  <files>guides/upgrade-path.md, README.md</files>
  <action>
    Follow Pattern 2 from `69-PATTERNS.md` and the Phase 68 `68-02` support-matrix analog: lane-level wording first, exact proof anchors second.

    Update `guides/upgrade-path.md` so the matrix and surrounding prose speak in the Phase 69 lane vocabulary:

    - `capture-only`
    - `phoenix-surface`
    - `sigra-reference`

    Keep the current narrow-evidence posture, but make the matrix answer "what kind of install is this?" before it answers "what dependency rows are resolved?". The guide should explicitly distinguish:

    - `supported` = documented and backed by current repo proof
    - `reference` = current first-party composition path inside a narrower host story
    - `unclaimed` = plausible but not currently proven by the repo

    Keep declared ranges, current lock resolution, and CI anchors where they are useful, but stop sounding like the dependency rows alone are the support story. Update `README.md` only enough to point readers at the narrowed support-matrix source of truth.
  </action>
  <verify>
    <automated>rg -n "capture-only|phoenix-surface|sigra-reference|supported|reference|unclaimed|not claimed" guides/upgrade-path.md README.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/upgrade-path.md` explicitly names `capture-only`, `phoenix-surface`, and `sigra-reference`.
    - The guide distinguishes `supported`, `reference`, and `unclaimed`.
    - `README.md` points to the support-matrix guide without implying broader compatibility in its own voice.
  </acceptance_criteria>
  <done>The public support matrix is lane-oriented and honest instead of sounding like a broad version-compatibility promise.</done>
</task>

<task type="auto">
  <name>Task 2: Align Sigra and example-app wording to the narrowed reference-path claim</name>
  <files>guides/integrations/sigra.md, examples/threadline_phoenix/README.md</files>
  <action>
    Follow the existing Sigra guide pattern mapped in `69-PATTERNS.md`: keep the direct callback contract and soft-dep posture intact while narrowing the support claim around it.

    Update the Sigra guide and example README so they describe Sigra as the current `sigra-reference` lane:

    - maintained first-party reference path
    - soft-loaded and host-owned
    - proven only through the current example app, docs, and repo verification
    - not a claim about arbitrary Sigra versions, arbitrary auth layouts, or non-Phoenix hosts

    Keep the direct callback wiring and additive-only semantics intact. If these docs mention tested versions or current resolved stacks, distinguish the root library's optional-dependency lock from the example app's narrower proven Sigra/Phoenix path instead of flattening them into one vague claim. Do not turn this into the Phase 70 refresh; stay at the level of claim language and support boundaries.
  </action>
  <verify>
    <automated>rg -n "reference|soft-dep|soft-loaded|host-owned|supported|unclaimed|Sigra" guides/integrations/sigra.md examples/threadline_phoenix/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/integrations/sigra.md` describes Sigra as a reference path, not a blanket compatibility promise.
    - `examples/threadline_phoenix/README.md` uses the same narrowed reference-path wording.
    - Any version/resolution wording makes clear whether it is talking about the root library support ranges or the narrower example-app proof path.
    - Neither file re-opens dependency/version refresh work that belongs to Phase 70.
  </acceptance_criteria>
  <done>The Sigra surfaces now reinforce the honest reference-path claim instead of implying broad Sigra support.</done>
</task>

<task type="auto">
  <name>Task 3: Update the affected doc-contract tests in the same slice as the wording rewrite</name>
  <files>test/threadline/upgrade_path_doc_contract_test.exs, test/threadline/integrations/sigra_doc_contract_test.exs, test/threadline/readme_doc_contract_test.exs, test/threadline/example_phoenix_readme_contract_test.exs</files>
  <action>
    Follow Pattern 3 from `69-PATTERNS.md`: use the repo's existing narrow `String.contains?/2` doc-contract style rather than snapshots or broad exact-match locking.

    Update the doc-contract tests that are directly affected by the support-lane rewrite so `69-02` can verify on its own final tree:

    - extend `upgrade_path_doc_contract_test.exs` for `capture-only`, `phoenix-surface`, and `sigra-reference`
    - extend `sigra_doc_contract_test.exs` for the narrowed reference-path wording
    - update README/example README contract tests only where they need to lock the new support/discovery language

    Keep these test changes scoped to the documents rewritten in this plan. Leave the new canonical contract-guide test and operator-surface scope test for `69-03`.
  </action>
  <verify>
    <automated>mix test test/threadline/upgrade_path_doc_contract_test.exs test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `69-02` no longer depends on a later plan to make its rewritten wording testable.
    - The upgrade-path contract test asserts the new lane vocabulary.
    - The Sigra, README, and example README tests reflect the narrowed support/reference wording where applicable.
  </acceptance_criteria>
  <done>The support-lane rewrite and its affected doc-contract locks land together and verify on the same tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| support docs -> adopters | Overbroad wording causes adopters to infer compatibility the repo does not prove. |
| reference-path docs -> future phases | If Sigra wording stays broad now, Phase 70 will inherit ambiguous scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | I | support matrix wording | mitigate | Name the three lanes explicitly and define `supported`, `reference`, and `unclaimed` in docs. |
| T-69-05 | T | Sigra guide/example README | mitigate | Rephrase Sigra as the current first-party reference path only, preserving soft-dep and host-owned language. |
| T-69-06 | E | phase scope | mitigate | Keep version refresh and reference-integration behavior changes out of this slice; those belong to Phase 70. |
</threat_model>

<verification>
Run:
- `rg -n "capture-only|phoenix-surface|sigra-reference|supported|reference|unclaimed|not claimed" guides/upgrade-path.md README.md`
- `rg -n "reference|soft-dep|soft-loaded|host-owned|supported|unclaimed|Sigra" guides/integrations/sigra.md examples/threadline_phoenix/README.md`
- `mix test test/threadline/upgrade_path_doc_contract_test.exs test/threadline/integrations/sigra_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1`
</verification>

<success_criteria>
- The support matrix uses the three Phase 69 lanes consistently.
- Public docs distinguish `supported`, `reference`, and `unclaimed`.
- Sigra is described narrowly as the current reference path only.
</success_criteria>

<output>
After completion, create `.planning/phases/69-integration-contracts-and-support-matrix/69-02-SUMMARY.md`
</output>
