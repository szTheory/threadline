---
phase: 69
plan: "69-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/integration-contracts.md
  - README.md
  - guides/operator-surface.md
  - mix.exs
autonomous: true
requirements:
  - INTEG-01
must_haves:
  truths:
    - "Phase 69 publishes one canonical integration-contract document that explains the existing concrete seams instead of inventing a new adapter abstraction."
    - "`Threadline.Plug`, `Threadline.Job`, `Threadline.Integrations.*`, and operator-surface auth/export auth are documented as one breadth contract with transport-specific shapes."
    - "The contract keeps actor identity single-authority, additive context narrowly scoped, host-owned auth intact, and soft dependencies inside integration modules."
  artifacts:
    - path: "guides/integration-contracts.md"
      provides: "Canonical breadth-contract guide for request, job, reference-adapter, and operator-surface composition seams"
    - path: "README.md"
      provides: "Top-level discovery link to the canonical contract guide without turning the README into a second contract spec"
    - path: "guides/operator-surface.md"
      provides: "Operator-surface guide that links to the canonical contract guide for breadth semantics"
    - path: "mix.exs"
      provides: "ExDoc discovery wiring for the new canonical guide"
  key_links:
    - from: "guides/integration-contracts.md"
      to: "lib/threadline/plug.ex"
      via: "request-path contract"
      pattern: "context_overrides_fn"
    - from: "guides/integration-contracts.md"
      to: "lib/threadline/job.ex"
      via: "job-path serialized context contract"
      pattern: "\"actor_ref\""
    - from: "guides/integration-contracts.md"
      to: "lib/threadline/operator_surface/export_auth_plug.ex"
      via: "HTTP export auth parity and synthetic mirror delegation"
      pattern: "mirror = %{assigns: conn.assigns}"
    - from: "README.md"
      to: "guides/integration-contracts.md"
      via: "short discovery pointer"
      pattern: "integration-contracts.md"
---

<objective>
Publish the canonical Phase 69 breadth-contract guide and make it discoverable from the public entry points.

Purpose: satisfy `INTEG-01` by documenting the existing Threadline seams as an intentional contract before later v1.19 phases build on them.
Output: one new canonical guide plus narrow cross-links from README, operator-surface docs, and ExDoc extras.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/69-integration-contracts-and-support-matrix/69-CONTEXT.md
@.planning/phases/69-integration-contracts-and-support-matrix/69-RESEARCH.md
@.planning/phases/69-integration-contracts-and-support-matrix/69-PATTERNS.md
@README.md
@guides/operator-surface.md
@guides/upgrade-path.md
@guides/integrations/sigra.md
@mix.exs
@lib/threadline/plug.ex
@lib/threadline/job.ex
@lib/threadline/integrations/sigra.ex
@lib/threadline/operator_surface/router.ex
@lib/threadline/operator_surface/auth.ex
@lib/threadline/operator_surface/export_auth_plug.ex
@test/threadline/readme_doc_contract_test.exs
@test/threadline/operator_surface_doc_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical integration-contract guide from the current code seams</name>
  <files>guides/integration-contracts.md</files>
  <action>
    Follow Pattern 1 from `69-PATTERNS.md` and the Phase 68 `68-01` docs-first analog: one canonical guide owns the contract, secondary surfaces link to it.

    Create `guides/integration-contracts.md` as the Phase 69 source of truth. Document the contract in four sections:

    1. request path via `Threadline.Plug`
    2. job path via `Threadline.Job`
    3. reference integrations via `Threadline.Integrations.*`
    4. operator-surface composition via `authorize_fn` and `export_authorize_fn`

    Implement the locked decisions directly:

    - document the current concrete seams; do not introduce a new behaviour/protocol or speculative umbrella API
    - state that `actor_fn` is the only actor-authority callback
    - state that `context_overrides_fn` is additive-only for `request_id` and `correlation_id`, and unknown keys / non-map returns fail closed
    - document `Threadline.Job` as explicit serialized data, not a callback mini-framework
    - document `Threadline.Integrations.Sigra` as the model for soft-gated reference adapters
    - document operator-surface auth as one contract across LiveView and HTTP export transport faces, with the host still owning auth semantics
    - document that `export_authorize_fn` is optional and, when absent, export auth deliberately falls back to `authorize_fn` through the synthetic `%{assigns: conn.assigns}` mirror
    - state the secure-by-default mount boundary from `threadline_operator_surface/2`

    Keep the guide concrete and code-shaped. This phase should explain what exists, not generalize beyond repo evidence.
  </action>
  <verify>
    <automated>rg -n "Threadline\\.Plug|Threadline\\.Job|Threadline\\.Integrations|authorize_fn|export_authorize_fn|capture-only|host-owned" guides/integration-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/integration-contracts.md` exists and clearly covers Plug, Job, Integrations, and operator-surface composition.
    - The guide explicitly states that `actor_fn` owns identity and `context_overrides_fn` is additive-only for `request_id` and `correlation_id`.
    - The guide describes `Threadline.Integrations.*` as reference adapters with soft-dependency gating, not framework ownership claims.
    - The guide explains `authorize_fn` and `export_authorize_fn` as one breadth contract, including the export fallback behavior, without inventing a Threadline-owned auth model.
  </acceptance_criteria>
  <done>The repo has one canonical integration-contract document that downstream phases can link to instead of re-explaining the seams ad hoc.</done>
</task>

<task type="auto">
  <name>Task 2: Wire discovery pointers without turning secondary docs into competing specs</name>
  <files>README.md, guides/operator-surface.md, mix.exs</files>
  <action>
    Follow the scoped-discovery pattern mapped in `69-PATTERNS.md`: keep secondary docs short and link-driven instead of creating competing specs.

    Add narrow discovery links to the new guide:

    - `README.md` should point readers to `guides/integration-contracts.md` when they need the host/framework breadth contract
    - `guides/operator-surface.md` should keep its focus on mount/auth/screens, but link to the new guide for the broader composition contract
    - `mix.exs` should expose the guide in ExDoc extras and the appropriate extras group

    While touching `README.md` and `guides/operator-surface.md`, correct any contract examples that still imply tuple-style callback options for `actor_fn`, `authorize_fn`, or similar seams. The implementation invokes 1-arity functions directly, so the docs in this slice should reflect the real callable shape instead of preserving stale MFA-style examples.

    Keep the README concise and the operator-surface guide scope-bounded. The new guide is the canonical contract surface.
  </action>
  <verify>
    <automated>rg -n "integration-contracts\\.md" README.md guides/operator-surface.md mix.exs</automated>
    <automated>mix docs</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` links to `guides/integration-contracts.md`.
    - `guides/operator-surface.md` links to `guides/integration-contracts.md` without taking on support-matrix sections.
    - `mix.exs` includes the new guide in ExDoc extras.
    - Any touched callback examples in `README.md` or `guides/operator-surface.md` use the callable shapes the code actually accepts, not tuple-style placeholders that drift from implementation.
  </acceptance_criteria>
  <done>The canonical guide is discoverable from the public entry points and HexDocs without duplicating its full content elsewhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code seams -> public docs | If the canonical guide over-abstracts, adopters will be taught a contract the code does not actually expose. |
| canonical guide -> secondary docs | If README or operator-surface docs restate the contract independently, they will drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | T | integration-contract guide | mitigate | Document only the current concrete seams and cite existing code paths directly. |
| T-69-02 | I | auth boundary wording | mitigate | Preserve the host-owned auth boundary explicitly in the new guide and linked operator-surface docs. |
| T-69-03 | R | secondary discovery docs | mitigate | Keep README/operator-surface wording short and link-driven so the canonical guide remains the only full breadth spec. |
</threat_model>

<verification>
Run:
- `rg -n "Threadline\\.Plug|Threadline\\.Job|Threadline\\.Integrations|authorize_fn|export_authorize_fn|capture-only|host-owned" guides/integration-contracts.md`
- `rg -n "integration-contracts\\.md" README.md guides/operator-surface.md mix.exs`
- `mix docs`
</verification>

<success_criteria>
- Threadline has one canonical integration-contract guide.
- The guide explains the existing seams without inventing a new abstraction layer.
- README, operator-surface docs, and ExDoc all point to the new guide cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/69-integration-contracts-and-support-matrix/69-01-SUMMARY.md`
</output>
