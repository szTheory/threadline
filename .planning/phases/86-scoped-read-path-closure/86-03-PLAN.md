---
phase: "86-scoped-read-path-closure"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/query.ex
  - test/threadline/query_test.exs
  - lib/threadline/operator_surface/live/row_history_component.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
autonomous: true
requirements:
  - SCOPE-02
must_haves:
  truths:
    - "Row history queries apply the host-owned scope_query_fn."
    - "Cross-tenant data leakage is prevented on the As-Of UI."
  artifacts:
    - path: "lib/threadline/query.ex"
      provides: "Scoped history/3, as_of/4, row_history/4, row_history_page/4"
    - path: "lib/threadline/operator_surface/live/row_history_component.ex"
      provides: "Scope configuration parameter unpacking"
  key_links:
    - from: "lib/threadline/operator_surface/live/transaction_live.ex"
      to: "lib/threadline/operator_surface/live/row_history_component.ex"
      via: "scope={@threadline_scope} scope_query_fn={@threadline_scope_query_fn}"
---

<objective>
Apply explicit scope enforcement to the row history and As-Of features.

Purpose: Guarantee that tenant-scoped support staff cannot see row histories (As-Of views) for records outside their host-owned boundaries.
Output: Query module pipelines that invoke `maybe_apply_scope/2` and LiveView threading of these parameters down to components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/86-scoped-read-path-closure/86-RESEARCH.md
@.planning/phases/86-scoped-read-path-closure/86-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Query module to apply row history scopes</name>
  <files>lib/threadline/query.ex, test/threadline/query_test.exs</files>
  <behavior>
    - `history/3`, `as_of/4`, `row_history/4`, and `row_history_page/4` queries run through `maybe_apply_scope/2`.
  </behavior>
  <action>Add a `row_history_scope_opts/3` private helper following the established `transaction_scope_opts/2` pattern. Pipe the Ecto.Query through `maybe_apply_scope` before execution in all 4 row-history related functions. Add scoping tests to query_test.exs.</action>
  <verify>
    <automated>mix test test/threadline/query_test.exs</automated>
  </verify>
  <done>Row-level queries enforce scoping when `scope_query_fn` is provided.</done>
</task>

<task type="auto">
  <name>Task 2: Thread scope to RowHistoryComponent</name>
  <files>
    lib/threadline/operator_surface/live/transaction_live.ex,
    lib/threadline/operator_surface/live/row_history_component.ex
  </files>
  <action>In `TransactionLive`, update the `<.live_component module={RowHistoryComponent} />` render to pass `scope={@threadline_scope}` and `scope_query_fn={@threadline_scope_query_fn}`, explicitly following the analog established in `TimelineLive` (as mapped in 86-PATTERNS.md). In `RowHistoryComponent`, ensure these are unpacked into the `opts` keyword list when calling the underlying Query functions (e.g. `Threadline.history` and `Threadline.as_of`).</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/transaction_live_test.exs</automated>
  </verify>
  <done>LiveView renders safely pass tenant scoping down to the data access layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → `RowHistoryComponent` | Tenant-scoped operators must not access records of other tenants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04 | Elevation of Privilege | `Threadline.Query` | mitigate | Ensure `scope_query_fn` is mandatorily appended via `maybe_apply_scope/2` to all row history endpoints. |
| T-86-05 | Information Disclosure | `RowHistoryComponent` | mitigate | Ensure the UI wrapper components accurately thread down the auth context. |
</threat_model>

<verification>
mix test test/threadline/query_test.exs
</verification>

<success_criteria>
Row History component safely queries data limited to the host's provided `scope_query_fn`.
</success_criteria>

<output>
After completion, create `.planning/phases/86-scoped-read-path-closure/86-03-SUMMARY.md`
</output>
n, create `.planning/phases/86-scoped-read-path-closure/86-03-SUMMARY.md`
</output>
