---
phase: "86-scoped-read-path-closure"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/auth.ex
  - test/threadline/operator_surface/auth_test.exs
  - lib/threadline/operator_surface/coverage/on_mount.ex
  - test/threadline/operator_surface/coverage/on_mount_test.exs
  - lib/threadline/operator_surface/live/coverage_live.ex
  - test/threadline/operator_surface/live/coverage_live_test.exs
autonomous: true
requirements:
  - SCOPE-01
must_haves:
  truths:
    - "Coverage dashboard access is gated by coverage_authorize_fn."
    - "Unauthorized users are redirected when visiting the Coverage view."
    - "Unauthorized LiveView sessions do not poll for telemetry."
  artifacts:
    - path: "lib/threadline/operator_surface/auth.ex"
      provides: "coverage_enabled_for_socket?/2 logic"
    - path: "lib/threadline/operator_surface/coverage/on_mount.ex"
      provides: "Polling authorization gate"
  key_links:
    - from: "lib/threadline/operator_surface/auth.ex"
      to: "lib/threadline/operator_surface/live/coverage_live.ex"
      via: ":threadline_coverage_enabled assign"
---

<objective>
Explicitly gate the Coverage dashboard backend using the `coverage_authorize_fn` configuration hook.

Purpose: Prevent system-global database schema topologies from leaking to tenant-scoped support staff by denying access to the Coverage view and halting background telemetry loops.
Output: Working authentication check for the `/coverage` URL and stopped background loops when unauthorized.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/86-scoped-read-path-closure/86-RESEARCH.md
@.planning/phases/86-scoped-read-path-closure/86-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add assign_coverage_enabled/2 to Auth</name>
  <files>lib/threadline/operator_surface/auth.ex, test/threadline/operator_surface/auth_test.exs</files>
  <behavior>
    - Extracts :coverage_authorize_fn from opts (defaulting to fn _ -> false end).
    - Assigns :threadline_coverage_enabled to the socket after evaluation.
  </behavior>
  <action>Extend `Auth.on_mount/4` with `assign_coverage_enabled(socket, opts)` following the exact pattern used by `assign_exports_enabled`. Implement the helper `coverage_enabled_for_socket?`. Add corresponding assertions to auth_test.exs.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/auth_test.exs</automated>
  </verify>
  <done>Socket assigns `:threadline_coverage_enabled` on mount correctly according to `coverage_authorize_fn`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Halt unauthenticated telemetry polling</name>
  <files>lib/threadline/operator_surface/coverage/on_mount.ex, test/threadline/operator_surface/coverage/on_mount_test.exs</files>
  <behavior>
    - Polling interval loop does not start if :threadline_coverage_enabled is false.
    - Initial coverage fetch is skipped if :threadline_coverage_enabled is false.
  </behavior>
  <action>Update `Coverage.OnMount.on_mount/4` to check `socket.assigns.threadline_coverage_enabled`. Only run `assign_initial_coverage` and start the `Process.send_after` background refresh loop if `threadline_coverage_enabled` is true. Create/update tests asserting the gate works.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/coverage/on_mount_test.exs</automated>
  </verify>
  <done>Telemetry does not poll when coverage is disabled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface-level halt on CoverageLive</name>
  <files>lib/threadline/operator_surface/live/coverage_live.ex, test/threadline/operator_surface/live/coverage_live_test.exs</files>
  <behavior>
    - Direct navigation to /coverage redirects to / (base_path) if unauthorized.
  </behavior>
  <action>In `CoverageLive.mount/3`, check `socket.assigns[:threadline_coverage_enabled]`. If true, allow normal mount behavior. If false, utilize the exact pattern from `Auth.halt_unauthorized/2` (as mapped in 86-PATTERNS.md) to redirect the user to `/` (base_path). Assert redirect in coverage_live_test.exs.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/coverage_live_test.exs</automated>
  </verify>
  <done>Manual visits to /coverage by unauthorized users are immediately bounced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → `/coverage` | Tenant-scoped operators must not view system-wide db topologies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Information Disclosure | `CoverageLive` | mitigate | Ensure `coverage_authorize_fn` correctly restricts LiveView rendering. |
| T-86-02 | Information Disclosure | `Coverage.OnMount` | mitigate | Ensure background telemetry polling doesn't run and leak details on unauthorized sockets. |
</threat_model>

<verification>
mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/coverage/on_mount_test.exs test/threadline/operator_surface/live/coverage_live_test.exs
</verification>

<success_criteria>
`coverage_authorize_fn` successfully gates both the HTTP UI for `/coverage` and the background telemetry processing.
</success_criteria>

<output>
After completion, create `.planning/phases/86-scoped-read-path-closure/86-01-SUMMARY.md`
</output>
