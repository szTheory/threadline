---
phase: "86-scoped-read-path-closure"
plan: "02"
type: execute
wave: 2
depends_on:
  - "86-01"
files_modified:
  - lib/threadline/operator_surface/components/surface_header.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - lib/threadline/operator_surface/live/retention_history_live.ex
  - lib/threadline/operator_surface/live/export_status_live.ex
  - lib/threadline/operator_surface/live/actor_live.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
autonomous: true
requirements:
  - SCOPE-01
must_haves:
  truths:
    - "Coverage badge UI is hidden for unauthorized users across all surfaces."
  artifacts:
    - path: "lib/threadline/operator_surface/components/surface_header.ex"
      provides: "coverage_enabled attr logic wrapper"
  key_links:
    - from: "lib/threadline/operator_surface/live/timeline_live.ex"
      to: "lib/threadline/operator_surface/components/surface_header.ex"
      via: "coverage_enabled={@threadline_coverage_enabled}"
---

<objective>
Ensure the Coverage badge UI in the header is explicitly gated across all LiveViews calling it.

Purpose: Prevent leaking existence or state of the coverage dashboard to unauthorized operators.
Output: Conditional rendering of the surface badge based on the `coverage_enabled` attribute.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/86-scoped-read-path-closure/86-RESEARCH.md
@.planning/phases/86-scoped-read-path-closure/86-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coverage_enabled attribute to SurfaceHeader</name>
  <files>lib/threadline/operator_surface/components/surface_header.ex</files>
  <action>Add `attr :coverage_enabled, :boolean, default: false` to the `surface_header` component. Wrap the coverage badge markup (the `<a>` tags with `surface-badge--warn` or `surface-badge--ok`) in a `<%= if @coverage_enabled do %>...<% end %>` block.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/components/surface_header_test.exs</automated>
  </verify>
  <done>SurfaceHeader accepts the boolean attribute and only renders badges when true.</done>
</task>

<task type="auto">
  <name>Task 2: Pass assignment to surface_header across views</name>
  <files>
    lib/threadline/operator_surface/live/timeline_live.ex,
    lib/threadline/operator_surface/live/retention_history_live.ex,
    lib/threadline/operator_surface/live/export_status_live.ex,
    lib/threadline/operator_surface/live/actor_live.ex,
    lib/threadline/operator_surface/live/transaction_live.ex
  </files>
  <action>Update every invocation of `<.surface_header>` in these LiveView template renders to explicitly pass `coverage_enabled={@threadline_coverage_enabled}`. Do not skip any.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/ test/threadline/operator_surface/transaction_live_test.exs</automated>
  </verify>
  <done>All callers correctly thread down the auth assignment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | Visual UI badges revealing schema topology states |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03 | Information Disclosure | `SurfaceHeader` | mitigate | Conditionally hide the coverage badge entirely when `coverage_enabled` is false. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test test/threadline/operator_surface/live/
</verification>

<success_criteria>
`surface_header` is securely gated and templates compile with no errors.
</success_criteria>

<output>
After completion, create `.planning/phases/86-scoped-read-path-closure/86-02-SUMMARY.md`
</output>
6-scoped-read-path-closure/86-02-SUMMARY.md`
</output>
reate `.planning/phases/86-scoped-read-path-closure/86-02-SUMMARY.md`
</output>
</output>
