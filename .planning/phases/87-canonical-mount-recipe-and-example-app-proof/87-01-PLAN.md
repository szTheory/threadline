---
phase: 87-canonical-mount-recipe-and-example-app-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
autonomous: true
requirements:
  - ADOPT-01
  - ADOPT-03
must_haves:
  truths:
    - "Only one canonical /audit mount is present in the example router"
    - "No alternative /support mount is suggested or commented out"
  artifacts:
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      provides: "Canonical router configuration"
      min_lines: 50
  key_links:
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      to: "/audit"
      via: "threadline_operator_surface mount"
      pattern: "threadline_operator_surface \"/\""
---

<objective>
Remove the secondary "separate tree" concept from the example app's router, establishing a single canonical `/audit` tree that leverages `scope_query_fn` and `export_authorize_fn` for persona degradation.

Purpose: Shifts the mental model from "configure multiple UI instances" to "mount once and use standard Elixir functions to control behavior," improving developer ergonomics.
Output: A cleaner router example with a single, clear path for operator surface integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/87-canonical-mount-recipe-and-example-app-proof/87-DISCUSSION.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove secondary mount from router</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</files>
  <action>
    Remove the commented-out `threadline_operator_surface "/support"` block entirely, including the comment block that starts with `# Support-read-only variation on a separate tree:`. Keep the primary `threadline_operator_surface "/"` inside the `/audit` scope untouched.
  </action>
  <verify>
    <automated>grep -q "/support" examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex && exit 1 || exit 0</automated>
  </verify>
  <done>The file contains exactly one `threadline_operator_surface` mount, and no references to a separate `/support` tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> /audit | Internal support operators accessing the operator surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Elevation of Privilege | router.ex / support access | mitigate | Authorization remains enforced via `authorize_fn` and `export_authorize_fn` on the single canonical route. |
</threat_model>

<verification>
- `cd examples/threadline_phoenix && mix compile` succeeds.
- `grep "/support" examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` returns no results.
</verification>

<success_criteria>
The router example is focused on a single canonical mount without confusing secondary variations.
</success_criteria>

<output>
After completion, create `.planning/phases/87-canonical-mount-recipe-and-example-app-proof/87-01-SUMMARY.md`
</output>