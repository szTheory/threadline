---
phase: 87-canonical-mount-recipe-and-example-app-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - examples/threadline_phoenix/README.md
  - guides/getting-started-saas.md
autonomous: true
requirements:
  - ADOPT-02
must_haves:
  truths:
    - "Documentation no longer mentions a separate /support tree"
    - "Docs reinforce that export_authorize_fn and scope_query_fn are the primary tools for persona degradation on a single mount"
  artifacts:
    - path: "examples/threadline_phoenix/README.md"
      provides: "Runnable proof artifact documentation"
      min_lines: 100
    - path: "guides/getting-started-saas.md"
      provides: "Canonical first-hour path documentation"
      min_lines: 100
  key_links: []
---

<objective>
Align documentation (example app README and the getting-started guide) with the unified router approach, removing all references to the separate `/support` tree variation.

Purpose: Provide adopters with a single, clear adoption narrative, emphasizing how a single `/audit` tree handles both Admins and Support Operators securely.
Output: Updated documentation artifacts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/87-canonical-mount-recipe-and-example-app-proof/87-DISCUSSION.md
@examples/threadline_phoenix/README.md
@guides/getting-started-saas.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README documentation</name>
  <files>examples/threadline_phoenix/README.md</files>
  <action>
    Find the `scope "/audit" do` code block under the "Operator Surface" section and remove the commented-out `threadline_operator_surface "/support"` variation. Remove the subsequent paragraphs that describe "If your host prefers a separate support-read-only variation...". Rewrite the guidance to strongly advocate for the single mount using `export_authorize_fn` for degrading export capability, noting it provides a seamless UX for support operators without needing multiple router scopes.
  </action>
  <verify>
    <automated>grep -q "Support-read-only variation on a separate tree" examples/threadline_phoenix/README.md && exit 1 || exit 0</automated>
  </verify>
  <done>README.md contains no reference to the `/support` tree and promotes the single canonical mount.</done>
</task>

<task type="auto">
  <name>Task 2: Update getting started guide</name>
  <files>guides/getting-started-saas.md</files>
  <action>
    Find the `scope "/audit" do` code block under the "9. Mount the operator surface and open /audit" section. Remove the commented-out `/support` mount. Update the prose that follows ("If your host prefers a separate support-only mount...") to remove that option and instead clarify that the single mount natively supports both admin and support roles via `authorize_fn`, `export_authorize_fn`, and `scope_query_fn`.
  </action>
  <verify>
    <automated>grep -q "Support-read-only variation on a separate tree" guides/getting-started-saas.md && exit 1 || exit 0</automated>
  </verify>
  <done>getting-started-saas.md contains no reference to the separate `/support` tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> Implementation | Assuring developers apply the correct security posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02 | Information Disclosure | Documentation | mitigate | The guides strictly recommend `scope_query_fn` and `export_authorize_fn` for security degradation, ensuring adopters build secure defaults. |
</threat_model>

<verification>
- `grep "Support-read-only variation" examples/threadline_phoenix/README.md` returns no matches.
- `grep "Support-read-only variation" guides/getting-started-saas.md` returns no matches.
</verification>

<success_criteria>
All documentation points users to exactly one copy-pasteable canonical `/audit` mount that supports role degradation.
</success_criteria>

<output>
After completion, create `.planning/phases/87-canonical-mount-recipe-and-example-app-proof/87-02-SUMMARY.md`
</output>