---
phase: 197-coverage-growth-adversarial-closeout-design-debt-register
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/e2e/critic/cache.ts
  - examples/threadline_phoenix/e2e/critic/gate.ts
autonomous: true
requirements: [PROOF-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A re-captured screenshot for the same cell+lens+rubric+model produces a verdict-cache MISS (cache key now includes a screenshot sha8), so a stale verdict can no longer be silently returned after re-capture"
    - "Running critic:score for a cell that already has a stamped before pole refuses to clobber the existing before-pole score files and prints an instruction naming the gate as the only post-edit scoring command"
    - "critic:gate run with no before-pole snapshot still returns VOID with the run-score-before-editing instruction (existing behavior preserved)"
    - "npm run critic:gate -- --page route.actor --lens density --dry-run walks the full 7-step pipeline without ANTHROPIC_API_KEY and exits 0 with the hardened cache/pole code in place"
    - "mix verify.mechanical (18 tests) and mix verify.critic_trust (22 tests) pass unchanged — hardening touches only maintainer-local e2e tooling"
  artifacts:
    - "examples/threadline_phoenix/e2e/critic/cache.ts — cacheKey extended with screenshotHash component"
    - "examples/threadline_phoenix/e2e/critic/gate.ts — before-pole overwrite guard"
  key_links:
    - "cache.ts cacheKey → lookupCache/writeCache call sites → gate.ts scoring path (all callers pass the new screenshotHash)"
  prohibitions: []
---

<objective>
Wave-1 loop-measurement hardening (RESEARCH OQ-3 recommendation): fix the two defects Phase 196 proved can silently corrupt PROOF-02 measurement — (1) the verdict cache is keyed on cell+rubric+model but NOT the screenshot, so re-captures return stale verdicts; (2) `critic:score` run after an edit silently overwrites the gate's before pole. Both fixes are small, deterministic, executor-runnable (no API key needed), and de-risk every paid gate run in plans 02–04.

Purpose: PROOF-02 requires genuine before/after evidence; these defects fake it. Fixing them first means the maintainer's paid iterations in later waves cannot be invalidated by tooling.
Output: hardened `cache.ts` + `gate.ts`, proven end-to-end by a dry-run of the gate pipeline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-PATTERNS.md
@examples/threadline_phoenix/e2e/critic/cache.ts
@examples/threadline_phoenix/e2e/critic/gate.ts
</context>

## Artifacts this phase produces

Plan-01 scope: no new symbols exported to product code. New/changed tooling symbols (maintainer-local e2e, TypeScript):
- `cache.ts`: `screenshotHash` parameter added to `cacheKey(...)`, `lookupCache(...)`, `writeCache(...)`; `CachedVerdict` interface gains a `screenshot_hash` field; new helper `sha8OfFile(path)` (or equivalent) computing an 8-char sha of a PNG.
- `gate.ts` / score path: before-pole stamp file (`.planning/critic-scores/<cell>/<lens>/.pole-stamp` or equivalent single sentinel per cell) + refusal path in the score command when a stamped before pole exists.

<tasks>

<task type="tracer">
  <name>Task 1: Screenshot-keyed verdict cache + before-pole overwrite guard, proven by a full gate dry-run</name>
  <files>examples/threadline_phoenix/e2e/critic/cache.ts, examples/threadline_phoenix/e2e/critic/gate.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/critic/cache.ts (current cacheKey at ~lines 43-52, CachedVerdict interface ~lines 22-37, lookupCache/writeCache ~lines 43-90, header comment documenting the rubric_hash auto-invalidation idiom)
    - examples/threadline_phoenix/e2e/critic/gate.ts (beforeLensScore ~lines 307-329, missing-before VOID path ~lines 388-397, dry-run / missing-key branch ~lines 360-372, ACCEPT_REJECT_RULE ~lines 294-298)
    - examples/threadline_phoenix/e2e/critic/run.ts (score/gate subcommand dispatch — find where critic:score writes into .planning/critic-scores/)
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-PATTERNS.md (sections "cache.ts (OQ-3 debt #1)" and "gate.ts (OQ-3 debt #2)" — the exact patterns to replicate)
  </read_first>
  <action>
    Two surgical fixes replicating the file's own idioms (per RESEARCH OQ-3 and 196-06 debt seeds; loop constraints per [196-D9] — this tooling stays maintainer-local, out of CI):
    1. Screenshot-keyed cache (debt #1): extend the cache key tuple exactly the way rubric_hash already works — add a screenshotHash component (sha8 of the cell's PNG bytes) to cacheKey, thread it through the CachedVerdict interface and both lookupCache/writeCache call sites, and update every caller (run.ts / gate.ts scoring path) to compute the sha8 from the screenshot file it is about to score. Old-format cache entries must simply MISS (the existing corrupt/missing-entry degrade-to-miss path already handles this — no migration code). Update the file header comment to document screenshot-hash auto-invalidation alongside the rubric_hash sentence.
    2. Before-pole overwrite guard (debt #2): make the score command refuse to overwrite an existing before pole. Implementation: when critic:score is about to write score JSONs for a cell/lens that already has score files present, abort with a VOID-style self-documenting error in the exact house style of the existing missing-before message in gate.ts (~line 388): name the cell, state that a before pole already exists, and instruct that `npm run critic:gate -- --page route.<x> --lens <lens>` is the ONLY post-edit scoring command; provide an explicit escape hatch flag (e.g. `--fresh-before`) that deletes the old pole deliberately (for starting a brand-new iteration on the same cell). Do NOT reimplement scoring or aggregation — compose the existing primitives (scoreCellLens, runNSamples, median aggregation).
    Then prove the slice end-to-end: run the gate dry-run (no API key needed) so every touched code path executes.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npm run critic:gate -- --page route.actor --lens density --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "screenshotHash\|screenshot_hash" examples/threadline_phoenix/e2e/critic/cache.ts` shows the parameter in cacheKey AND the CachedVerdict interface field AND both lookup/write paths.
    - Source assertion: cacheKey's returned string contains the screenshot hash component (a 5-part `__`-joined key).
    - Behavior assertion: the dry-run command above exits 0 and prints the 7-step pipeline with the hardened code loaded (no TypeScript/tsx load errors).
    - Source assertion: the score path contains a guard that checks for existing before-pole score files and an error message naming `critic:gate` as the only post-edit scoring command, plus a `--fresh-before` (or equivalently named, documented) escape flag.
    - The gate's existing missing-before VOID path is unchanged in behavior (still returns void:true with the run-score-BEFORE-editing instruction).
  </acceptance_criteria>
  <done>Verdict cache misses on any screenshot change; critic:score cannot silently clobber a before pole; gate dry-run pipeline exits 0 end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Regression sweep — deterministic guards and repo hygiene unaffected</name>
  <files>examples/threadline_phoenix/e2e/critic/cache.ts, examples/threadline_phoenix/e2e/critic/gate.ts</files>
  <read_first>
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md (§Sampling Rate, §Anti-Patterns)
    - CONTRIBUTING.md ("Forward-only gate — run one iteration" runbook, ~lines 312-383) — confirm whether the runbook text mentions the cache-bust `rm` step or the score/gate sequencing in a way the new guard changes
  </read_first>
  <action>
    Confirm the hardening is invisible to CI and the committed floor. Run the deterministic guard suite. If (and only if) the CONTRIBUTING runbook's iteration steps now differ materially (e.g. the manual `rm .planning/critic-verdict-cache/<cell>__*.json` cache-bust is still documented as mandatory even though the cache is now screenshot-keyed), update the runbook wording minimally AND update `test/threadline/forward_only_gate_doc_contract_test.exs` in the same commit (doc-contract lockstep, RESEARCH Pitfall 6). Prefer keeping the manual cache-bust documented as belt-and-braces for one more phase rather than deleting it — note the choice in the SUMMARY. No committed `page.*` scorecard, ledger, floor, or `.planning/CRITIQUE.md` change of any kind in this plan.
  </action>
  <verify>
    <automated>mix verify.mechanical && mix verify.critic_trust && mix verify.doc_contract && mix compile --warnings-as-errors && mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.mechanical` passes (18 tests, 0 failures) and `mix verify.critic_trust` passes (22 tests, 0 failures).
    - `mix verify.doc_contract` passes.
    - `git status --porcelain .planning/scorecards .planning/design-system-ledger.json` is empty (no floor/ledger/scorecard drift).
    - CLI assertion: `git diff --name-only HEAD~1` (after commit) lists only e2e critic TS files and, at most, CONTRIBUTING.md + forward_only_gate_doc_contract_test.exs as a lockstep pair.
  </acceptance_criteria>
  <done>All deterministic gates green; hardening confined to maintainer-local tooling; doc-contract lockstep honored if the runbook changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local screenshots → paid LLM verdict | untrusted/stale image bytes previously reused via cache; now hash-bound |
| maintainer CLI → .planning/critic-scores pole files | post-edit score runs could previously falsify the before pole |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-197-01 | Tampering | verdict cache (cache.ts) | high | mitigate | screenshot sha8 in cache key — stale verdict cannot masquerade as a fresh score (this plan, Task 1) |
| T-197-02 | Tampering | before pole (.planning/critic-scores) | high | mitigate | overwrite guard + explicit --fresh-before escape (this plan, Task 1) |
| T-197-03 | Tampering | supply chain (npm/hex installs) | low | accept | zero packages added this phase (RESEARCH: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `cd examples/threadline_phoenix/e2e && npm run critic:gate -- --page route.actor --lens density --dry-run` exits 0.
- `mix verify.mechanical && mix verify.critic_trust && mix verify.doc_contract` green.
</verification>

<success_criteria>
Both 196-06 measurement defects (stale cache, before-pole overwrite) are closed in code; gate dry-run proves the pipeline end-to-end; deterministic CI guards unchanged and green. Plans 02-04's paid iterations can no longer be invalidated by these two defects.
</success_criteria>

<output>
Create `.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-01-SUMMARY.md` when done.
</output>
