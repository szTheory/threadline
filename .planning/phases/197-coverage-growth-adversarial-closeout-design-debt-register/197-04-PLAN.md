---
status: superseded
superseded_reason: "WAIVED 2026-08-27 — maintainer-ratified PROOF-02 shortfall (197-02-SUMMARY); paid loop parked, plan did not run"
phase: 197-coverage-growth-adversarial-closeout-design-debt-register
plan: 04
type: execute
wave: 4
depends_on: [197-03]
files_modified:
  - .planning/design-system-ledger.json
  - test/threadline/operator_surface/critic_trust_test.exs
  - lib/threadline/operator_surface/live/coverage_live.ex
  - test/threadline/operator_surface/live/coverage_live_test.exs
autonomous: false
requirements: [PROOF-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Phase 197 closes PROOF-02 with at least 2 NEW gate-ACCEPTED, maintainer-ratified page improvements (retention's 196 accept counts as bonus only, per OQ-2); each ACCEPT has its atomic signoff+pin pair committed"
    - "If the first two candidates did not yield 2 accepts, the pivot loop ran further ratified candidates (from the ≥4 list) until 2 accepts landed or the maintainer explicitly ratified stopping with a recorded rationale — a silent shortfall is not a valid close"
    - "Every REJECT/VOID along the way is recorded as such — no rejected edit is counted toward the 2-3 pages"
    - "Final deterministic sweep is green: verify.mechanical, verify.critic_trust (with all new pins), all touched pages' LiveView tests, compile --warnings-as-errors, format, and mix ci.all green except the pre-existing 3-module doc-contract baseline (V123Charter/FormlessPages/Phase06Nyquist, tracked since 195-10)"
  artifacts:
    - ".planning/design-system-ledger.json — ratchet.signoffs containing >=2 new forward_only_accept entries dated this phase"
    - "test/threadline/operator_surface/critic_trust_test.exs — @known_signoffs pins matching every new entry"
  key_links:
    - "each ACCEPT verdict → signoff entry → ==-equal pin (atomic commit per accept)"
    - "count of new signoffs → PROOF-02 satisfaction (2-3 NEW pages)"
  prohibitions:
    - statement: "A REJECTed or VOID gate verdict must never be recorded as a ratified improvement — no ratchet.signoffs entry, no @known_signoffs pin, no PROOF-02 credit"
      status: flagged
    - statement: "No ledger floor, target_score, critic-panel, or structural_whitelist change may ride along with any signoff commit in this plan"
      status: flagged
---

<objective>
Close PROOF-02: book the iteration-2 verdict from Plan 03, run the pivot/stretch loop until 2–3 NEW accepted pages are landed (or the maintainer explicitly ratifies a shortfall with rationale), and run the final deterministic sweep. Honest REJECT is a valid iteration outcome but PROOF-02 requires landed improvement — the ≥4-candidate ranking from Plan 02 is the fuel for pivots (stretch candidates: coverage×density, timeline×typography/rhythm with --n 7).

Purpose: convert the loop from "proven once" (196) to "drives real improvement across the lowest-scoring pages" (PROOF-02).
Output: ≥2 new signoff+pin pairs (target 2–3), all gates green, PROOF-02 evidence trail complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-PATTERNS.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-02-SUMMARY.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-03-SUMMARY.md
@.planning/design-system-ledger.json
</context>

## Artifacts this phase produces

Plan-04 scope symbols:
- 1+ additional `ratchet.signoffs` JSON entries (kind `forward_only_accept`) + matching `@known_signoffs` maps (same field set as Plan 03).
- Possible pivot/stretch edits: HEEx render changes + flipped tests in `coverage_live.ex` / `timeline_live.ex` (or remaining ratified candidates) with `197-04`-prefixed rationale comments.
- PROOF-02 closure record in the SUMMARY: table of every iteration (cell, lens, verdict, delta, noise, signoff commit or pivot note).

<tasks>

<task type="auto">
  <name>Task 1: Book the iteration-2 verdict (atomic signoff+pin on ACCEPT, or explicit pivot on REJECT)</name>
  <files>.planning/design-system-ledger.json, test/threadline/operator_surface/critic_trust_test.exs</files>
  <read_first>
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-03-SUMMARY.md (verbatim iteration-2 verdict + before-panel numbers)
    - .planning/design-system-ledger.json (current ratchet.signoffs tail — the shape to replicate)
    - test/threadline/operator_surface/critic_trust_test.exs (current @known_signoffs tail + enforcement loop)
  </read_first>
  <reversibility rating="costly">Append-only ledger — removal later trips GATE-04; ratification already happened at the Plan-03 blocking checkpoint.</reversibility>
  <action>
    Identical booking procedure to Plan 197-03 Task 1, applied to the iteration-2 verdict: on ACCEPT append the forward_only_accept entry (target, lens, numeric delta/noise_floor, before.panel with all four blocking lenses, ratified_by citing 197-03 Task 3, commit sha of the iteration-2 edit) plus its ==-equal string-keyed @known_signoffs pin, in ONE `chore: forward-only gate — ...` commit touching exactly those two files. On REJECT/VOID: no ledger change; record the pivot target; keep the edit as unratified cleanup (green gates) or revert per maintainer instruction. Never edit existing entries, floors, targets, or the panel.
  </action>
  <verify>
    <automated>mix verify.critic_trust && mix verify.mechanical</automated>
  </verify>
  <acceptance_criteria>
    - On ACCEPT: the signoff commit's `git show --name-only` lists exactly the ledger and critic_trust_test.exs; verify.critic_trust green with the new pin.
    - On REJECT: ledger byte-identical; pivot target named in SUMMARY.
  </acceptance_criteria>
  <done>Iteration-2 outcome durably booked; guards green.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Accept-count decision — continue the pivot loop or ratify closure</name>
  <files>varies (next candidate's _live.ex + _live_test.exs; ledger + critic_trust_test.exs per additional ACCEPT)</files>
  <read_first>
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-02-SUMMARY.md (the ratified >=4 candidate ranking)
    - CONTRIBUTING.md runbook (~lines 337-367)
  </read_first>
  <action>
    Count NEW 197 accepts booked so far (0, 1, or 2). Present the maintainer the state and the decision:
    - If accepts >= 2: maintainer may ratify PROOF-02 closure now (2 NEW pages) or authorize one stretch iteration toward 3 (next ratified candidate, e.g. coverage×density).
    - If accepts < 2: continue the pivot loop on the next ratified candidate. Each additional iteration follows the exact 3-step shape already executed twice: (a) executor authors the edit + flips tests + deterministic gates green (c6f9355e pattern, `feat(197-04): ...`, rationale comment prefixed `197-04`); (b) maintainer runs after-capture + cache-bust `rm .planning/critic-verdict-cache/route.<x>__dark-1280__*.json` + `npm run critic:gate -- --page route.<x> --lens <lens>` (with `--n 7` on instability) + `mix verify.mechanical`, and ratifies the verdict; (c) executor books it (atomic signoff+pin on ACCEPT, pivot note on REJECT). Repeat until 2 accepts land, candidates are exhausted, or the maintainer explicitly ratifies stopping short — a shortfall requires a recorded maintainer rationale in the SUMMARY (it becomes a PROOF-02 gap for the phase verifier, never silently papered over).
    Per-iteration acceptance tracking: each `continue:` (or `stretch:`) decision re-enters the full 3-step shape above (a: edit → b: maintainer gate run with cache-bust → c: verdict booking) and produces its OWN recorded verification block in the SUMMARY, mirroring 197-03 Tasks 2-3 exactly — the verbatim gate verdict, delta vs noise, cache-bust command used, and the booking commit sha (signoff+pin on ACCEPT, pivot note on REJECT). One recorded round per maintainer reply; no batching multiple candidates into a single reply, and no round closes without its verification block written before the next candidate starts.
    Rhythm-lens candidates always use `--n 7`; VOID counts as REJECT. New before poles for not-yet-scored cells were already captured in Plan 02 Task 1 and are overwrite-protected by 197-01.
  </action>
  <decision>With N NEW 197 accepts booked so far, how does PROOF-02 close?</decision>
  <options>
    - continue: run the next ratified candidate through the edit → gate → book cycle (name the cell + lens); repeatable until 2-3 accepts land or candidates are exhausted
    - stretch: 2 accepts already booked — run one more iteration toward 3 (name the cell + lens)
    - close: 2+ accepts booked — ratify PROOF-02 closure now
    - shortfall: fewer than 2 accepts and no viable candidates remain — ratify stopping short WITH a recorded rationale (becomes an explicit PROOF-02 gap for the phase verifier)
  </options>
  <resume-signal>Maintainer replies with one of: "continue: route.<x>/<lens>", "stretch: route.<x>/<lens>", "close", or "shortfall: <rationale>".</resume-signal>
  <verify>Maintainer has issued an explicit decision at each round (continue with named candidate / ratify closure / ratify shortfall with rationale), and every additional verdict is recorded verbatim with its booking commit.</verify>
  <acceptance_criteria>
    - Final state is one of: >=2 NEW accepts booked, or a maintainer-ratified shortfall rationale recorded in the SUMMARY.
    - Every additional ACCEPT has its atomic signoff+pin commit; every REJECT is listed with no ledger change.
    - No iteration used a page.*/story.* cell or an advisory lens.
  </acceptance_criteria>
  <done>PROOF-02 accept count resolved by explicit maintainer decision, with complete per-iteration evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Final PROOF-02 deterministic sweep + closure record</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md (§Sampling Rate — the phase-gate command set and the allowed 3-module baseline exception)
  </read_first>
  <action>
    Run the full phase-gate sweep and write the PROOF-02 closure table into the SUMMARY: one row per iteration (cell, lens, verdict, delta vs noise, signoff commit sha or pivot note), plus the count of NEW accepted pages. Confirm `route.*` scorecards and `.planning/CRITIQUE.md` remain uncommitted (`git status` check — they are gitignored/uncommitted by design) and that no committed `page.*` Tier-A scorecard changed during the plan. `mix ci.all` must be green except the pre-existing 3-module doc-contract baseline (V123Charter/FormlessPages/Phase06Nyquist); confirm that failure set is exactly those 3 modules and no more.
  </action>
  <verify>
    <automated>mix verify.mechanical && mix verify.critic_trust && mix compile --warnings-as-errors && mix format --check-formatted && mix verify.doc_contract</automated>
  </verify>
  <acceptance_criteria>
    - The verify command chain exits 0.
    - `mix ci.all` output shows failures only in the 3 known baseline modules (or none).
    - `git status --porcelain .planning/scorecards` empty; no CRITIQUE.md or route.* scorecard staged/committed.
    - SUMMARY contains the per-iteration closure table with >=2 ACCEPT rows (or the ratified shortfall rationale).
  </acceptance_criteria>
  <done>PROOF-02 closed with green gates and a complete, honest evidence trail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM verdict → ratchet ledger | unearned signoffs corrupt the forward-only guarantee |
| pivot loop → requirement claim | counting rejects or the 196 retention accept toward PROOF-02 would fake coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-197-10 | Tampering | ratchet.signoffs appends | high | mitigate | atomic entry+pin pairs, verify.critic_trust enforcement, no ride-along floor changes |
| T-197-11 | Repudiation | closure claim (2-3 pages) | medium | mitigate | per-iteration closure table with verdict numbers + commit shas in SUMMARY; shortfall requires recorded maintainer rationale |
</threat_model>

<verification>
- >=2 NEW forward_only_accept entries (or ratified shortfall), all gates green, ci.all at the known 3-module baseline.
</verification>

<success_criteria>
PROOF-02 satisfied: 2-3 lowest-scoring operator pages carry real, maintainer-ratified, gate-ACCEPTED improvements with append-only evidence — or an explicit ratified shortfall is on record for the phase verifier.
</success_criteria>

<output>
Create `.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-04-SUMMARY.md` when done (include the per-iteration closure table).
</output>
