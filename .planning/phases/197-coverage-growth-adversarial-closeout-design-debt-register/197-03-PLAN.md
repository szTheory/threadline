---
phase: 197-coverage-growth-adversarial-closeout-design-debt-register
plan: 03
type: execute
wave: 3
depends_on: [197-02]
files_modified:
  - .planning/design-system-ledger.json
  - test/threadline/operator_surface/critic_trust_test.exs
  - lib/threadline/operator_surface/live/evidence_live.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - test/threadline/operator_surface/live/evidence_live_test.exs
  - test/threadline/operator_surface/live/timeline_live_test.exs
autonomous: false
requirements: [PROOF-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Iteration-1's outcome is booked honestly: an ACCEPT produced exactly one new ratchet.signoffs entry (kind forward_only_accept, with delta, noise_floor, before-panel, commit sha, ratified_by) plus its ==-equal @known_signoffs pin in the SAME commit; a REJECT/VOID produced NO signoff and an explicit recorded pivot to the next ratified candidate"
    - "Iteration-2 edit is landed on the next ratified candidate (evidence as an IA restructure, or timeline) with its LiveView tests flipped and all deterministic gates green before the paid gate run"
    - "The maintainer's iteration-2 gate verdict is recorded verbatim with delta and noise floor"
    - "mix verify.critic_trust stays 22+/0 after any signoff append — existing pins are never edited, only a sibling map appended"
  artifacts:
    - ".planning/design-system-ledger.json — 0 or 1 new ratchet.signoffs entries (exactly matching ratified ACCEPTs)"
    - "test/threadline/operator_surface/critic_trust_test.exs — matching @known_signoffs pin per new signoff"
    - "iteration-2 target's _live.ex + _live_test.exs edits"
  key_links:
    - "ledger ratchet.signoffs entry ↔ @known_signoffs pin (==-equal string-keyed map, same commit — the GATE-04 atomic pair, f1610d87 precedent)"
    - "iteration-2 edit → after-capture → cache-busted critic:gate against its protected before pole"
  prohibitions:
    - statement: "A REJECTed or VOID gate verdict must never be recorded as a ratified improvement — no ratchet.signoffs entry, no @known_signoffs pin, no PROOF-02 credit; rejected edits may land only as ordinary unratified cleanup or be reverted"
      status: flagged
    - statement: "No ledger floor, target_score, critic-panel, or structural_whitelist change may ride along with an iteration edit or a signoff commit — each such change requires its own recorded signoff"
      status: flagged
---

<objective>
Book iteration 1's verdict (signoff+pin on ACCEPT, or explicit pivot on REJECT), then run PROOF-02 iteration 2 on the next ratified candidate — expected `route.evidence` × `density` as an IA restructure (merge the six one-row sections' scaffolding; chrome removal already failed 3 gate runs in 196) or `route.timeline` × `typography`, per the Plan-02 ratified ranking.

Purpose: PROOF-02 needs 2–3 NEW accepted pages (OQ-2); this plan converts verdict #1 into durable append-only evidence and produces verdict #2.
Output: 0–1 new signoff+pin pairs, iteration-2 edit + tests, iteration-2 verdict recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-PATTERNS.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-02-SUMMARY.md
@.planning/design-system-ledger.json
@test/threadline/operator_surface/critic_trust_test.exs
</context>

## Artifacts this phase produces

Plan-03 scope symbols:
- New JSON object appended to `ratchet.signoffs` in `.planning/design-system-ledger.json` (kind `forward_only_accept`; fields: target, lens, delta, noise_floor, before.panel, verdict, date, ratified_by, commit, notes) — one per ratified ACCEPT.
- Sibling string-keyed Elixir map appended to `@known_signoffs` in `critic_trust_test.exs` — one per ACCEPT, ==-equal to the JSON entry.
- Edited HEEx render blocks + flipped tests for the iteration-2 page (rationale comment prefix `Density (197-03, ...)` or the targeted lens).

<tasks>

<task type="auto">
  <name>Task 1: Book iteration-1 outcome — atomic signoff+pin on ACCEPT, or explicit pivot on REJECT</name>
  <files>.planning/design-system-ledger.json, test/threadline/operator_surface/critic_trust_test.exs</files>
  <read_first>
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-02-SUMMARY.md (the verbatim iteration-1 verdict + before-panel numbers)
    - .planning/design-system-ledger.json (existing ratchet.signoffs[0] — the exact entry shape to replicate; touch nothing else in the file)
    - test/threadline/operator_surface/critic_trust_test.exs (existing pin ~lines 172-200 and the enforcement loop ~lines 464-468 — pinned map must be ==-equal to the JSON, integers stay integers, floats stay floats)
  </read_first>
  <reversibility rating="costly">Signoff entries are append-only by GATE-04 convention — removing one later trips verify.critic_trust; ratification already happened at the Plan-02 blocking checkpoint.</reversibility>
  <action>
    Branch on the ratified iteration-1 verdict from 197-02:
    - ACCEPT: append one sibling object to `ratchet.signoffs` mirroring the f1610d87 entry shape — kind `forward_only_accept`, target `route.<x>__dark-1280`, lens, numeric delta, numeric noise_floor, `before.panel` carrying all four blocking-lens before numbers from the Plan-02 Task-1 pole, verdict ACCEPT, date, `ratified_by` citing the 197-02 Task-3 checkpoint, `commit` = the iteration-1 source-edit sha, notes describing the edit. Append the ==-equal string-keyed map to `@known_signoffs` in critic_trust_test.exs. Ledger append + pin land in ONE commit (`chore: forward-only gate — <page> <lens> advanced (+N > noise M), zero regressions`, f1610d87 precedent). Never edit existing entries; never change floors/targets/panel in this commit.
    - REJECT/VOID: no ledger change of any kind. Record the pivot explicitly (which candidate is next per the ratified ranking) and either keep the iteration-1 edit as ordinary unratified cleanup (tests + floor green, 2eca4208 precedent — commit message must NOT claim ratification) or revert it per the maintainer's checkpoint instruction.
    route.* scorecards and .planning/CRITIQUE.md stay uncommitted by design either way.
  </action>
  <verify>
    <automated>mix verify.critic_trust && mix verify.mechanical</automated>
  </verify>
  <acceptance_criteria>
    - On ACCEPT: `git show --name-only HEAD` for the signoff commit lists exactly .planning/design-system-ledger.json and test/threadline/operator_surface/critic_trust_test.exs; `mix verify.critic_trust` passes with the new pin (23+ signoff-related assertions all green).
    - On REJECT: `git log --oneline -3` contains no chore signoff commit and the ledger is byte-identical to its pre-plan state; the pivot target is named in the SUMMARY.
    - In both cases `mix verify.mechanical` 18/0.
  </acceptance_criteria>
  <done>Iteration-1 outcome durably and honestly booked; guards green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author iteration-2 edit on the next ratified candidate + flip its LiveView tests</name>
  <files>lib/threadline/operator_surface/live/evidence_live.ex, test/threadline/operator_surface/live/evidence_live_test.exs</files>
  <read_first>
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-02-SUMMARY.md (ratified ranking — confirms whether iteration-2 is evidence, timeline, or the pivot target; substitute the matching _live.ex/_live_test.exs pair throughout)
    - lib/threadline/operator_surface/live/evidence_live.ex (current render — the six one-row sections paying full section scaffolding, per 196-06)
    - test/threadline/operator_surface/live/evidence_live_test.exs (assertions to flip; note the file already carries unratified 196 cleanup edits — build on them, do not revert)
    - Output of `git show c6f9355e` (edit + test-flip pattern) and `git show 2eca4208` (the evidence cleanup already landed unratified)
  </read_first>
  <behavior>
    - Test 1: every load-bearing evidence signal (record identifiers, export/download affordances, statuses) survives the restructure — assert semantic attributes and surviving copy.
    - Test 2: merged/removed scaffolding headings no longer render — refute, with comments naming the surviving carrier.
    - Test 3: page renders correctly in the empty/no-records state after restructure.
  </behavior>
  <action>
    If iteration-2 target is evidence×density: perform the IA restructure the 196 gate demanded — merge the six one-row sections' repeated section scaffolding (headers, framing prose, per-section chrome) into a consolidated structure so each datum pays for its container once; this is a structural regrouping, not another round of chrome deletion (three chrome-removal variants were already gate-REJECTED in 196). If the ratified target is timeline×typography (or another pivot), apply the c6f9355e-shape edit for that lens instead on timeline_live.ex/timeline_live_test.exs. Either way: HEEx rationale comment prefixed `<lens> (197-03, ...)`, presentation-only (capture/query/auth untouched), `Code.ensure_loaded?` wrapper respected, tests flipped in the assert-to-refute pattern, no ledger/floor/scorecard change, commit as `feat(197-03): ...`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/evidence_live_test.exs && mix verify.mechanical && mix verify.critic_trust && mix compile --warnings-as-errors && mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - Target page's LiveView test file passes 0 failures including an empty-state test.
    - `mix verify.mechanical` 18/0, `mix verify.critic_trust` green.
    - `git diff HEAD~1 --name-only` for the edit commit lists only the target page's _live.ex and _live_test.exs.
  </acceptance_criteria>
  <done>Iteration-2 edit committed with green deterministic gates, shaped as the gate demanded (IA restructure for evidence).</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Maintainer runs the iteration-2 gate and ratifies the verdict</name>
  <files>none (maintainer-local)</files>
  <read_first>
    - CONTRIBUTING.md runbook (~lines 337-367)
  </read_first>
  <action>
    Present paste-ready commands for the iteration-2 cell (route.<x> / <lens> from the ratified ranking):
    1. `cd examples/threadline_phoenix/e2e && npm run capture:pages`
    2. `rm .planning/critic-verdict-cache/route.<x>__dark-1280__*.json`
    3. `npm run critic:gate -- --page route.<x> --lens <lens>` (only post-edit scoring command; `--n 7` on instability)
    4. `mix verify.mechanical`
    Maintainer pastes the full gate output and ratifies ACCEPT / REJECT / VOID (VOID = REJECT-equivalent). Record verbatim. Signoff/pivot booking for THIS verdict is Plan 197-04 Task 1.
  </action>
  <instructions>
    Run these locally against the iteration-2 cell (route.<x> / <lens> from the ratified ranking):
    1. `cd examples/threadline_phoenix/e2e && npm run capture:pages`
    2. `rm .planning/critic-verdict-cache/route.<x>__dark-1280__*.json`
    3. `npm run critic:gate -- --page route.<x> --lens <lens>` (add --n 7 on instability)
    4. `mix verify.mechanical`
    5. Paste the full gate output and state your verdict: ACCEPT / REJECT / VOID.
  </instructions>
  <verification>The verbatim iteration-2 gate output is recorded with numeric delta and noise floor; the ledger is untouched pending Plan 197-04 Task 1.</verification>
  <resume-signal>Maintainer replies with the gate output and the word ACCEPT, REJECT, or VOID for the iteration-2 cell.</resume-signal>
  <verify>Checkpoint record contains the verbatim iteration-2 gate output and an explicit maintainer verdict with numeric delta and noise floor.</verify>
  <acceptance_criteria>
    - Verdict word + delta + noise floor recorded.
    - `git status --porcelain .planning/design-system-ledger.json` empty at checkpoint close (booking deferred to 197-04).
  </acceptance_criteria>
  <done>Iteration-2 verdict ratified and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM verdict → ratchet ledger | unearned signoff = corrupted forward-only guarantee |
| signoff JSON ↔ test pin | drift between the pair silently weakens GATE-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-197-07 | Tampering | ratchet.signoffs append | high | mitigate | append-only sibling entry, ==-equal pin in same commit, verify.critic_trust enforcement loop |
| T-197-08 | Repudiation | ACCEPT booking | medium | mitigate | ratified_by cites the 197-02/197-03 checkpoint; commit sha embedded in the entry |
| T-197-09 | Elevation of Privilege | floor/target ride-along | high | mitigate | prohibition + AC that signoff commits touch exactly the ledger entry + pin; any floor change needs its own signoff or critic_trust fails |
</threat_model>

<verification>
- verify.critic_trust and verify.mechanical green after booking; iteration-2 edit committed and its verdict recorded verbatim.
</verification>

<success_criteria>
Iteration 1 is durably booked (signoff+pin or honest pivot), iteration 2 is landed and gated — PROOF-02 stands at 1–2 NEW verdicts with full evidence trail.
</success_criteria>

<output>
Create `.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-03-SUMMARY.md` when done.
</output>
