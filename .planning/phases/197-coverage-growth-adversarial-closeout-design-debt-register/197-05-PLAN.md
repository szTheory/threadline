---
phase: 197-coverage-growth-adversarial-closeout-design-debt-register
plan: 05
type: execute
wave: 5
depends_on: [197-01, 197-04]
files_modified:
  - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-ADVERSARIAL-REVIEW.md
  - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-DESIGN-DEBT-REGISTER.md
autonomous: true
requirements: [PROOF-03, PROOF-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "197-ADVERSARIAL-REVIEW.md exists in the 180-ADVERSARIAL-REVIEW.md shape (frontmatter with phase/artifact/created/status/requirement, lens-review table, Findings, Follow-Through), with one row per loop invariant: no root runtime dep, no public component API, dev/test-only, LLM out of CI, capture/query/auth untouched, loop cannot regress the deterministic floor"
    - "Every lens row cites adversarial evidence (a tamper probe that made verify.critic_trust FAIL then pass after revert, or a pinned doc-contract/source test), never bare assertion"
    - "All four tamper probes were executed and reverted: floor drop without signoff, oracle fixture removal, pinned-signoff deletion, structural_whitelist add without signoff — each observed to FAIL verify.critic_trust while applied"
    - "If any tamper probe fails to trip its guard, it is recorded as a blocking Finding in the review (status not passed) — never silently passed"
    - "The working tree is clean after the probe sequence (git status --porcelain empty) — every probe fully reverted before the next"
    - "Every invariant lens has exactly one row and its Result column is Pass or Pass-with-bounded-caveat; Findings explicitly says none when no blocking issue remains"
    - "197-DESIGN-DEBT-REGISTER.md exists in the 193-RISK-REGISTER.md shape (frontmatter with clause/ranking_lens/source_precedence/seed/status; ranked rows; adoption/ops/maintainer ranking lens, NOT severity-x-likelihood; Verdict section)"
    - "All 9 seed debt items from RESEARCH (#1 cache key, #2 before-pole, #3 Tier-A drift, #4 evidence IA, #5 GATE-02 auto-write, #6 color_contrast rho 0.698, #7 hierarchy rho 0.42, #8 3-module doc-contract baseline, #9 search_path local failures) are accounted for as rows — items consumed this phase (#1/#2 fixed in 197-01; #4 if landed via PROOF-02) appear as distinct rows with a closed/consumed disposition rather than being silently dropped"
    - "Every open register row carries an explicit Owner and a concrete reopen-trigger; GATE-02 auto-write (196-D3 first-escalation deferral) is registered as debt per OQ-1 with a trigger like N accepted iterations where the surfaced diff was applied verbatim with zero human modification"
    - "The rank column is strictly ordered 1..N with ties broken explicitly in the row text"
    - { statement: "Debt row #9's ~81 local-failure count is re-verified with one fresh mix test run before the row is written (RESEARCH Assumption A1); the row frames it as maintainer friction, not regression", verification: backstop }
    - { statement: "Re-running the register-writing task on the same seed produces the same rows in the same file (single committed artifact, overwrite-in-place, no duplicate rows)", verification: backstop }
    - { statement: "The two closeout artifacts are written by this single sequential plan — no concurrent writer exists for either file (plan-level file ownership)", verification: backstop }
  artifacts:
    - ".planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-ADVERSARIAL-REVIEW.md"
    - ".planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-DESIGN-DEBT-REGISTER.md"
  key_links:
    - "tamper-probe transcript evidence → lens-row Review cells (probe output pasted/summarized with commands)"
    - "196-06-SUMMARY 'Deferred to Phase 197' + RESEARCH debt table → register seed rows"
  prohibitions:
    - statement: "The debt register must not contain a vague polish-later bucket — no row may lack an explicit Owner or a concrete reopen-trigger"
      status: flagged
---

<objective>
Close the phase's audit lanes: PROOF-03 — a v1.37-style multi-lens adversarial closeout proving (by executed tamper probes, not assertion) that the loop cannot regress the deterministic floor and every standing invariant holds; PROOF-04 — a residual design-debt register in the v1.39 risk-register shape, seeded from the 196-06 debt list, every row with Owner + concrete reopen-trigger. Both are planning artifacts mirroring literal precedent files (180-ADVERSARIAL-REVIEW.md, 193-RISK-REGISTER.md).

Purpose: PROOF-03/PROOF-04 are the milestone's honesty backstops — they make the loop's guarantees demonstrated and its remaining gaps owned.
Output: 197-ADVERSARIAL-REVIEW.md (status: passed) + 197-DESIGN-DEBT-REGISTER.md (status: complete).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md
@.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-PATTERNS.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-06-SUMMARY.md
@.planning/milestones/v1.37-phases/180-accessibility-verification-guardrails-adversarial-closeout/180-ADVERSARIAL-REVIEW.md
@.planning/milestones/v1.39-phases/193-quality-closeout-and-next-step-decision/193-RISK-REGISTER.md
@.planning/design-system-ledger.json
</context>

## Artifacts this phase produces

Plan-05 scope symbols (planning artifacts only — no product code, no schema, no mix.exs change):
- `197-ADVERSARIAL-REVIEW.md` — frontmatter keys `phase`, `artifact: adversarial-review`, `created`, `status`, `requirement: PROOF-03`; sections `## Invariant Lens Review` (table: Lens | Review | Result), `## Findings`, `## Follow-Through`.
- `197-DESIGN-DEBT-REGISTER.md` — frontmatter keys `phase`, `artifact`, `milestone: v1.40`, `clause: PROOF-04`, `generated`, `ranking_lens`, `source_precedence`, `seed`, `status`; ranked-row table (Rank | ID | Residual | Layer/Area | Owner | Adoption/Ops/Maintainer classification | Severity note (secondary) | Reopen trigger (concrete)); `## Verdict`; `## Boundary Check`.

<tasks>

<task type="auto">
  <name>Task 1: Execute the four tamper probes (probe → observe FAIL → revert) and capture evidence</name>
  <files>none committed (ephemeral local edits to .planning/design-system-ledger.json and test/threadline/operator_surface/critic_trust_test.exs, fully reverted)</files>
  <read_first>
    - .planning/design-system-ledger.json (trust_floors / ratchet.minimum_scores / synthetic oracle keys / mechanical_auto_apply.structural_whitelist — the probe targets; whitelist is currently [])
    - test/threadline/operator_surface/critic_trust_test.exs (@known_signoffs pins and the enforcement assertions the probes should trip)
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md (§PROOF-03 probe list)
  </read_first>
  <action>
    Run each probe as a temporary local edit, observe `mix verify.critic_trust` FAIL with the guard's message, revert via `git checkout -- <file>`, and confirm green before the next probe. The four probes ([196-D5] GATE-04 territory):
    1. Lower one `trust_floors` / `ratchet.minimum_scores` value in the ledger without a signoff → expect the no-silent-target-drop guard to fail.
    2. Remove one synthetic-oracle fixture reference / shrink `synthetic_item_count` → expect the no-fixture-removal guard to fail.
    3. Delete one pinned `ratchet.signoffs` entry from the ledger → expect the append-only pin assertion (`pinned in signoffs`) to fail.
    4. Add an entry to `mechanical_auto_apply.structural_whitelist` (currently empty) without a `structural_whitelist_add` signoff → expect the whitelist guard to fail.
    Capture for each: the exact edit made, the failing test name + message excerpt, and the green re-run after revert. Additionally record the CI-posture evidence: `mix verify.doc_contract` green (forward_only_gate_doc_contract_test pins ci.all ordering, the LLM's exclusion from CI, and critic non-leakage into guides/), and a `mix.exs` read confirming `ci.all` includes verify.critic_trust and verify.mechanical and excludes verify.ui_critique. If any probe does NOT trip its guard, stop treating it as evidence and record it as a blocking Finding for Task 2 (the review cannot be status: passed with an untripped probe).
  </action>
  <verify>
    <automated>git status --porcelain && mix verify.critic_trust && mix verify.mechanical && mix verify.doc_contract</automated>
  </verify>
  <acceptance_criteria>
    - Probe transcript contains 4 probes, each with: the edit description, a FAILING verify.critic_trust run (non-zero exit, guard message quoted), and a passing run after revert.
    - `git status --porcelain` is empty after the sequence (all probes reverted).
    - `mix verify.doc_contract` exits 0; the recorded mix.exs evidence shows verify.ui_critique absent from the ci.all alias.
  </acceptance_criteria>
  <done>All four guards demonstrated adversarially; working tree clean; evidence transcript ready for the review artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Write 197-ADVERSARIAL-REVIEW.md in the 180 shape (PROOF-03)</name>
  <files>.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-ADVERSARIAL-REVIEW.md</files>
  <read_first>
    - .planning/milestones/v1.37-phases/180-accessibility-verification-guardrails-adversarial-closeout/180-ADVERSARIAL-REVIEW.md (the literal shape: frontmatter, lens table, Findings, Follow-Through)
    - Task-1 probe transcript (this plan)
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-04-SUMMARY.md (PROOF-02 closure state — the diff scope of the phase for the invariants rows)
  </read_first>
  <action>
    Create the review with frontmatter `phase: 197-coverage-growth-adversarial-closeout-design-debt-register`, `artifact: adversarial-review`, `created: <today>`, `requirement: PROOF-03`, `status: passed` only if every Result cell is Pass / Pass-with-bounded-caveat. Lens table rows (one each): (1) loop cannot regress the deterministic floor — cite probes 1-4 with failing-guard messages + mechanical 18/0; (2) no root runtime dependency — cite the phase git diff scope (e2e TS + LiveView presentation + planning artifacts only; mix.exs deps untouched) and RESEARCH's zero-package finding; (3) no public component API — cite the diff scope + standing invariant tests; (4) dev/test-only surfaces — operator surface + e2e harness only; (5) LLM out of CI — cite forward_only_gate_doc_contract_test + the mix.exs ci.all evidence; (6) capture/query/auth untouched — cite `git diff <phase-start>..HEAD --name-only` showing no capture/semantics-layer file. Findings: blocking issues or an explicit none, plus honest proof-boundary caveats (e.g. Tier-A recapture drift means the floor gates committed cells, not live recaptures; the pre-existing 3-module doc-contract baseline is inherited, exactly as 180 carried its Phase-179 inherited failures). Follow-Through: imperative Keep-bullets (keep GATE-04 probes as the tamper evidence pattern; keep the 3-module baseline in the residual bucket until a doc-contract phase owns it; keep route.*-only gating per 196-D8).
  </action>
  <verify>
    <automated>test -f .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-ADVERSARIAL-REVIEW.md && grep -c "| Pass" .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-ADVERSARIAL-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with frontmatter keys phase/artifact/created/status/requirement and requirement value PROOF-03.
    - Lens table has exactly 6 invariant rows; every Review cell names a concrete probe, test file, or diff-scope command (no bare assertion cells).
    - Findings section present (blocking issues or explicit none + proof boundaries); Follow-Through has >=3 imperative bullets.
    - status is passed ONLY if all 6 Result cells are Pass or Pass-with-bounded-caveat.
  </acceptance_criteria>
  <done>PROOF-03 closeout artifact committed, evidence-backed in the v1.37 shape.</done>
</task>

<task type="auto">
  <name>Task 3: Write 197-DESIGN-DEBT-REGISTER.md in the 193 shape (PROOF-04)</name>
  <files>.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-DESIGN-DEBT-REGISTER.md</files>
  <read_first>
    - .planning/milestones/v1.39-phases/193-quality-closeout-and-next-step-decision/193-RISK-REGISTER.md (the literal shape: frontmatter, ranked-row table with Owner + reopen-trigger, Verdict, closing rules, Boundary Check)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-06-SUMMARY.md ("Deferred to Phase 197" — the seed)
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-RESEARCH.md (§PROOF-04 register shape — the 9-item verified debt table with root-cause file locations)
    - .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-04-SUMMARY.md (which debt items PROOF-02 consumed, e.g. evidence IA if it landed)
  </read_first>
  <action>
    First re-verify debt item #9 (Assumption A1): run `mix test` once locally and count failures with the `(undefined_table)` signature; write the row with the measured count (framed as maintainer friction with the confirmed `ALTER DATABASE ... SET search_path` fix, explicitly NOT a regression — the 193 R-D stance). Then create the register with frontmatter `clause: PROOF-04`, `milestone: v1.40`, `ranking_lens: adoption / operations / maintainer-risk (D-12) — NOT severity×likelihood`, `seed:` citing 196-06-SUMMARY "Deferred to Phase 197", `source_precedence` mirroring 193, `status: complete`. Rows: account for all 9 seed items — #1 (verdict cache screenshot key) and #2 (before-pole overwrite) as CLOSED-in-197 rows citing the 197-01 commit; #4 (evidence IA) as consumed-by-PROOF-02 if it landed, else an open row; open rows for #3 Tier-A recapture drift, #5 GATE-02 true auto-write (registered per OQ-1/[196-D3] with reopen-trigger "N consecutive accepted iterations where the surfaced mechanical diff was applied verbatim with zero human modification" — pick and state a concrete N), #6 color_contrast rho 0.698 (reopen: fresh oracle re-validation clears 0.70 AND a GATE-04 panel-change signoff), #7 hierarchy rho 0.42 (reopen: rubric redesign + re-validation clears 0.70), #8 the 3-module doc-contract baseline (reopen: any NEW module joins the failure set, or a doc-contract phase is opened), #9 search_path friction (reopen: the same undefined_table signature appears in CI, not locally). Every open row: explicit Owner (maintainer / e2e tooling / doc-contract phase / maintainer-env) + concrete reopen-trigger; ranks strictly 1..N on the adoption/ops/maintainer lens with ties broken in row text; severity notes secondary color only. Close with the 193 no-polish-later-bucket statement and a Boundary Check confirming this plan changed only `.planning/phases/197-*` files.
  </action>
  <verify>
    <automated>test -f .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-DESIGN-DEBT-REGISTER.md && grep -c "Reopen trigger" .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-DESIGN-DEBT-REGISTER.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with frontmatter keys clause/ranking_lens/source_precedence/seed/status; clause value is PROOF-04 and ranking_lens names adoption / operations / maintainer-risk.
    - The ranked table accounts for all 9 seed items (rows may be CLOSED-in-197 or consumed dispositions, but every seed item ID appears exactly once).
    - Every OPEN row's Owner cell is non-empty and its Reopen-trigger cell states an observable condition (a command result, a count threshold, a CI signature, or a signoff event) — no "revisit later"-style triggers.
    - Rank column values are strictly increasing 1..N.
    - Row #9 cites a freshly measured failure count from a mix test run performed in this task.
    - Register commit touches only files under .planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/.
  </acceptance_criteria>
  <done>PROOF-04 register committed in the v1.39 shape: all residual debt owned, ranked, and reopen-triggered; no polish-later bucket.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tamper probes → committed guard state | probes must be fully reverted; a leaked probe edit would weaken the ledger |
| debt register claims → future phase scoping | over/under-claiming debt misdirects future work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-197-12 | Tampering | ledger/floors (probe targets) | high | mitigate | probes run locally, each verified reverted (git status clean AC) before commit; verify.critic_trust green at close |
| T-197-13 | Repudiation | closeout claims | medium | mitigate | every lens row cites a runnable probe/test/diff command, not assertion; untripped probes become blocking Findings |
| T-197-14 | Information Disclosure | planning artifacts | low | accept | artifacts live in .planning/ which is never pushed to public origin (milestone-tags-stay-local policy) |
</threat_model>

<verification>
- Both artifacts exist in their precedent shapes; probe transcript shows 4 FAIL-then-revert cycles; git tree clean; verify.critic_trust / verify.mechanical / verify.doc_contract green.
</verification>

<success_criteria>
PROOF-03: the loop's guards are adversarially demonstrated (four tamper probes tripped, CI posture pinned) and all six standing invariants reviewed with evidence in the v1.37 shape. PROOF-04: all residual design-debt registered with Owner + concrete reopen-trigger in the v1.39 shape, all 9 seed items accounted for.
</success_criteria>

<output>
Create `.planning/phases/197-coverage-growth-adversarial-closeout-design-debt-register/197-05-SUMMARY.md` when done (include the probe transcript excerpt and both artifact paths).
</output>