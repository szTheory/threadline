# Phase 5 — Pattern map (repository / URLs)

## Files touched or read

| Path | Role | Closest analog in repo |
|------|------|------------------------|
| `mix.exs` | `@source_url`, `package/0`, `docs/0` | Self — v1.0 milestone already aligned URLs |
| `.github/workflows/ci.yml` | `on.*.branches` | Self — stable job keys `verify-format`, `verify-credo`, `verify-test` |
| `README.md` | Badges / links to GitHub Actions | `.planning/milestones/v1.0-phases/04-documentation-release/04-CONTEXT.md` for badge URL policy |

No new application modules; planning is **read-heavy** with optional **no-op** doc edits if traceability gaps appear.

## PATTERN MAPPING COMPLETE
