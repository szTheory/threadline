---
phase: 192-ci-cd-measurement-and-efficiency-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-BASELINE.md
  - .planning/phases/192-ci-cd-measurement-and-efficiency-hardening/scripts/aggregate-ci-baseline.sh
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "A durable read-only artifact 192-BASELINE.md records the cache-absent, deps.get-xN, browser-lane-dominated state of ci.yml before any edit"
    - "p50/p95 per-job timing is aggregated from ~15 GREEN push-on-main ci.yml runs (paged past the red-heavy recent window)"
    - "Static-analysis findings (10 parallel jobs, zero needs:, zero actions/cache, browser lane ~98% of ~5m52s critical path, mix deps.get cold x8) are recorded"
    - "Genuinely unavailable metrics (billed minutes, cache-hit rate) are honest rows carrying owner/date/superseding-evidence-pointer/reopen-trigger, not fabricated numbers"
  artifacts:
    - ".planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-BASELINE.md"
    - ".planning/phases/192-ci-cd-measurement-and-efficiency-hardening/scripts/aggregate-ci-baseline.sh"
  key_links:
    - "192-BASELINE.md frontmatter mirrors 189-QUALITY-AUDIT.md shape so Phase 193 (CLOSE-01) can do a pure in-repo before/after diff"
    - "Job display name -> job key mapping (Run test suite -> verify-test, Example app browser E2E (Playwright) -> verify-example-browser) is applied when building the ranked table"
  prohibitions:
    - "NO edits to any .github/workflows/*.yml file in this plan (D-05 observer-effect ordering — baseline is read-only)"
    - "NO edits to ci.yml, release.yml, flake-detection.yml, CONTRIBUTING.md, README.md, mix.exs, or any test file"
    - "NO fabricated numbers for unavailable metrics — record honest unavailable rows with reopen triggers"
    - "The aggregation script lives under .planning/ only and is NEVER wired into CI"
---

<objective>
Produce the CI-01 read-only baseline. Statically analyze `.github/workflows/ci.yml` and aggregate per-job p50/p95 wall-clock from the last ~15 GREEN `push`-on-`main` runs via `gh api`, then write a durable `192-BASELINE.md` in the Phase-189 audit shape so Phase 193 has a clean before/after anchor.

Purpose: "Measure before optimize" is a hard ordering (D-05). This artifact captures the cache-absent, deps.get-xN, browser-lane-dominated state BEFORE Wave 2 touches any workflow, avoiding an observer effect.
Output: `192-BASELINE.md` (frontmatter + ranked table + honest-unavailable rows) plus the throwaway aggregation script under `.planning/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-CONTEXT.md
@.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-RESEARCH.md
@.planning/phases/189-quality-baseline-and-authority-surface-audit/189-QUALITY-AUDIT.md
@.github/workflows/ci.yml
</context>

<artifacts_this_phase_produces>
- **New file** `.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-BASELINE.md` — read-only baseline artifact (YAML frontmatter `phase/artifact/audited/scope/requirements/status/source_precedence` + one ranked table; columns per D-03).
- **New file** `.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/scripts/aggregate-ci-baseline.sh` — throwaway `gh`/`jq` run-history aggregation script (lives in `.planning`, never in CI).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate GREEN ci.yml run-history timing via gh api</name>
  <files>.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/scripts/aggregate-ci-baseline.sh</files>
  <read_first>
    - `.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-RESEARCH.md` §"Baseline Feasibility" and §"Common Pitfalls" item 7 (recent window is red-heavy; page to ~15 GREEN runs of one event type).
    - `192-RESEARCH.md` §"Environment Availability" (gh 2.95.0 authed as szTheory; jq available; billing API empty for public repo).
  </read_first>
  <action>
    Write `scripts/aggregate-ci-baseline.sh` (throwaway, lives under `.planning/`, never referenced by any workflow). It MUST:
    1. Query `gh api "repos/szTheory/threadline/actions/workflows/ci.yml/runs?status=success&event=push&branch=main&per_page=100"` and page (via `?page=N`) until it collects ~15 GREEN runs — do NOT sample "last 15 total runs" (the recent window near 2026-06-26 is almost all failure; successes cluster earlier). Resolve D-02 open question by filtering `event=push` on `main` (most stable; honors the path-filter+main invariant that main runs the full set).
    2. For each collected run id, call `gh api repos/szTheory/threadline/actions/runs/{id}/jobs` and read per-job and per-step `started_at`/`completed_at`; compute each job's duration in seconds.
    3. Map the API display `name` back to the stable job key when tabulating (e.g. `Run test suite` -> `verify-test`, `Example app browser E2E (Playwright)` -> `verify-example-browser`) — the jobs endpoint returns display names, not keys.
    4. Aggregate p50 and p95 per job across the ~15 GREEN runs; also emit, per job: on-critical-path? (parallel fan-out means critical path == the single longest job per run), repeated setup cost (`mix deps.get` + `setup-beam` presence), cache state (none today), and `run_attempt>1` rerun count.
    5. Print a markdown table plus the aggregate critical-path p50/p95 to stdout for Task 2 to transcribe.
    Use `jq` for parsing. Record `run_attempt=1`-only windows honestly as "0 reruns observed in sampled window" (not a bug). Do NOT invent billed-minute or cache-hit numbers — those are Task 2 honest-unavailable rows. Do NOT edit any workflow file.
  </action>
  <verify>
    <automated>bash .planning/phases/192-ci-cd-measurement-and-efficiency-hardening/scripts/aggregate-ci-baseline.sh | grep -Eiq 'verify-test|Run test suite' && echo AGG_OK</automated>
  </verify>
  <acceptance_criteria>
    - Script runs against live `gh api`, collects ~15 GREEN `push`-on-`main` runs (paging past the red-heavy recent window), and prints per-job p50/p95 plus an aggregate critical-path figure.
    - Job display names are mapped to stable job keys in the printed table.
    - No `.github/workflows/*.yml` file is read-modified-written; the script exists only under `.planning/`.
  </acceptance_criteria>
  <done>Aggregation script produces per-job p50/p95 and critical-path numbers from GREEN history with name->key mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Author 192-BASELINE.md in the Phase-189 audit shape</name>
  <files>.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-BASELINE.md</files>
  <read_first>
    - `.planning/phases/189-quality-baseline-and-authority-surface-audit/189-QUALITY-AUDIT.md` lines 1-40 (frontmatter keys `phase/artifact/audited/scope/requirements/status/source_precedence` + table shape to mirror).
    - `192-CONTEXT.md` D-01/D-02/D-03/D-04 (artifact shape, depth, table columns, honest-unavailable rows with Nyquist-debt metadata).
    - `192-RESEARCH.md` §"Baseline Feasibility" (measured facts: 96 total runs; green run 27082048346 total ~5m49s, browser lane ~344s, Run test suite 256s; billing `{"billable":{}}` empty; no actions/cache today).
  </read_first>
  <action>
    Create `192-BASELINE.md` mirroring the 189 frontmatter convention: `phase: 192`, `artifact: ci-cd-baseline`, `audited: 2026-07-02`, `scope: v1.39-ci-efficiency-baseline`, `requirements: [CI-01]`, `status: complete`, `source_precedence` list (runtime/gh-api evidence > static ci.yml analysis > planning history). Body sections:
    1. **Executive summary** stating the headline pre-justifying CI-02 (D-05): all 10 jobs run in parallel (zero `needs:`), zero `actions/cache`, browser lane ~= 98% of the ~5m52s critical path, `mix deps.get` runs cold across the 8 root-deps jobs.
    2. **Ranked table** with the exact D-03 columns: `job | p50 | p95 | on critical path? | repeated setup cost (deps.get + setup-beam) | cache state | flaky/rerun (run_attempt>1 count) | evidence source`. Populate from Task 1 output; use the measured green-run spot-check as corroboration.
    3. **Static-analysis findings** row-set: 10 jobs / zero `needs:` / single pinned lane 1.17.3+OTP27 / `edoburu/pgbouncer:latest` at ci.yml:225 / no `concurrency:` block.
    4. **Honest-unavailable rows** for billed-minute cost (public-repo billing API returns empty `{"billable":{}}`) and cache-hit rate (N/A — no `actions/cache` configured today). Each such row MUST carry the DNA Nyquist-debt shape: owner, date, pointer to superseding evidence, reopen trigger (D-04, `threadline-elixir-oss-dna.md:16`) — never a bare "unavailable". Reopen trigger for cache-hit rate = "Phase 193 after Wave 2 caches land".
    Do NOT edit any workflow, doc, or test file — this artifact is read-only baseline evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-BASELINE.md && grep -q 'requirements: \[CI-01\]' .planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-BASELINE.md && grep -q 'on critical path' .planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-BASELINE.md && grep -Eiq 'reopen trigger' .planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-BASELINE.md && echo BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `192-BASELINE.md` exists with Phase-189-shaped frontmatter including `requirements: [CI-01]` and `status: complete`.
    - Ranked table carries all eight D-03 columns and per-job p50/p95 from Task 1.
    - Executive summary records the parallel-fan-out / zero-cache / browser-dominated headline.
    - Billed-minute and cache-hit-rate rows are honest-unavailable with owner/date/superseding-pointer/reopen-trigger metadata.
    - Zero `.github/workflows/` or source edits made in this plan.
  </acceptance_criteria>
  <done>Durable read-only baseline artifact exists in the 189 shape, ready for a Phase-193 diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local executor -> GitHub Actions API | Read-only `gh api` calls using existing keyring auth; no writes to the repo or workflows |
| baseline step -> live pipeline | The measurement step must not perturb the thing it measures (observer effect) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-192-B01 | Tampering | Baseline step editing a workflow (observer effect) | high | mitigate | Plan is read-only by contract (D-05); prohibitions forbid any `.github/workflows/*` edit; verify greps only the `.planning/` artifact |
| T-192-B02 | Repudiation | Fabricated/unsourced baseline numbers | medium | mitigate | Every row cites an evidence source column; unavailable metrics recorded honestly with reopen triggers (D-04), not invented |
| T-192-B03 | Information Disclosure | Leaking `gh` auth token into the committed artifact | low | accept | Script uses `gh api` (token stays in gh keyring); only aggregated timings are transcribed; no secrets written |
</threat_model>

<verification>
- `192-BASELINE.md` frontmatter validates and mirrors the 189 shape.
- Ranked table columns match D-03 exactly; numbers trace to the Task 1 aggregation.
- Unavailable rows carry the four-field Nyquist-debt metadata.
- `git diff --name-only` shows changes confined to `.planning/phases/192-.../` (no workflow/source/test files touched).
</verification>

<success_criteria>
CI-01 baseline exists as a durable read-only in-repo artifact capturing critical path, repeated setup cost, browser-lane cost, cache state (none), and flaky/rerun signal — with genuinely unavailable metrics marked honestly — so Phase 193 can diff before/after.
</success_criteria>

<output>
Create `.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-01-SUMMARY.md` when done.
</output>
