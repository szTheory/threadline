---
phase: 192-ci-cd-measurement-and-efficiency-hardening
plan: 03
type: execute
wave: 2
depends_on: [192-01]
files_modified:
  - .github/workflows/release.yml
  - CONTRIBUTING.md
  - README.md
  - mix.exs
autonomous: true
requirements: [CI-03, CI-04]
must_haves:
  truths:
    - "release.yml serializes only the publish job (group release-publish-${{ github.ref }}, cancel-in-progress: false) so release-please bookkeeping stays independent"
    - "The publish concurrency group is free of ${{ github.run_id }}"
    - "CONTRIBUTING List 1 'Stable job keys' table lists all 10 ci.yml job keys (adds verify-hex-evaluator, verify-example-browser)"
    - "CONTRIBUTING List 2 branch-protection required-checks renames 'Run test suite (verify-test)' to the two matrix check names and stays an intentional subset"
    - "README and a mix.exs comment state the explicit support contract: Elixir 1.15 floor / 1.17.3 current, OTP 26 min / 27 current, PostgreSQL 14 min / 16 current"
  artifacts:
    - ".github/workflows/release.yml (publish-job concurrency)"
    - "CONTRIBUTING.md (List 1 8->10; List 2 rename)"
    - "README.md (compatibility contract paragraph)"
    - "mix.exs (compatibility contract comment)"
  key_links:
    - "List 1 (job keys, ~120-129) and List 2 (branch-protection required checks, ~173-185, line 179) are DELIBERATELY different lists; List 2 is a subset and only gets the verify-test rename, not the 8->10 fix"
    - "mix.exs elixir: ~> 1.15 (line 28) stays unchanged; the contract comment sits beside it explaining floor vs current"
  prohibitions:
    - "NO raising mix.exs elixir: ~> 1.15 — add a comment only (D-14)"
    - "NO collapsing release.yml to a workflow-level concurrency group (serializes bookkeeping behind 30-min publishes — D-24 regression)"
    - "NO ${{ github.run_id }} in the publish concurrency group (would make it a no-op)"
    - "NO adding verify-compile-no-optional / verify-hex-evaluator / verify-example-browser rows to List 2 (it is intentionally a subset — D-25/pitfall 6)"
    - "NO :latest tag introduced in release.yml"
---

<objective>
Land the release-concurrency fix and the doc/version alignment: publish-job-level `concurrency` on `release.yml` (D-24), the CONTRIBUTING two-list repair (List 1 8→10, List 2 verify-test rename — D-25/D-19), and the explicit compatibility contract in README + a `mix.exs` comment (D-13).

Purpose: CI-03 alignment/hygiene (release concurrency, branch-protection docs, job-key parity) and CI-04 explicit compatibility policy — without raising the Elixir floor (D-14).
Output: Edited `release.yml`, `CONTRIBUTING.md`, `README.md`, `mix.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-CONTEXT.md
@.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-RESEARCH.md
@.github/workflows/release.yml
@CONTRIBUTING.md
@README.md
@mix.exs
</context>

<artifacts_this_phase_produces>
- **release.yml new construct:** publish-job-level `concurrency: { group: release-publish-${{ github.ref }}, cancel-in-progress: false }` on `publish-hex` (D-24).
- **CONTRIBUTING.md edits:** List 1 "Stable job keys" table gains `verify-hex-evaluator` + `verify-example-browser` rows (8→10, D-25); List 2 branch-protection required-checks line 179 `Run test suite (verify-test)` → the two matrix check names (D-19).
- **README.md edit:** explicit min/current support-contract paragraph (D-13).
- **mix.exs edit:** compatibility-contract comment beside `elixir: "~> 1.15"` (D-13); the requirement string itself is unchanged.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scope release.yml concurrency to the publish job</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - `192-CONTEXT.md` D-24 (publish-level scope; workflow-level collapse is a regression).
    - `192-RESEARCH.md` §".github/workflows/release.yml" (workflow-level no-op group at line 40; publish-hex job at line 276; gate-ci-green keys on run conclusion; publish-hex idempotency skip guard is the real race guard).
    - `.github/workflows/release.yml` lines 39-41 (existing no-op `concurrency` group) and 276-284 (publish-hex job header, `needs`, `if`).
  </read_first>
  <action>
    Add a job-level `concurrency` block to the `publish-hex` job (at line 276, alongside `runs-on`/`needs`/`if`): `group: release-publish-${{ github.ref }}` and `cancel-in-progress: false`, so concurrent publishes on the same ref serialize while the frequent `release-please` bookkeeping job stays independent. Leave the workflow-level `concurrency` group (line 40) as-is OR remove it — but the publish-job group MUST NOT contain `${{ github.run_id }}` (that made the old group a no-op). Do not change `gate-ci-green` or the `publish-hex` idempotency skip (they remain the real publish-race guards). Introduce no `:latest` tag.
  </action>
  <verify>
    <automated>grep -q 'group: release-publish-${{ github.ref }}' .github/workflows/release.yml && awk '/^  publish-hex:/{f=1} f&&/group: release-publish/{print; found=1} END{exit !found}' .github/workflows/release.yml && echo REL_CONC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `publish-hex` has a job-level `concurrency` with `group: release-publish-${{ github.ref }}` and `cancel-in-progress: false`.
    - The publish concurrency group contains no `${{ github.run_id }}`.
    - `release-please` bookkeeping is not placed behind a workflow-level serial group; `gate-ci-green` and the idempotency skip are untouched.
  </acceptance_criteria>
  <done>Publishes serialize per-ref; release-please bookkeeping stays independent.</done>
</task>

<task type="auto">
  <name>Task 2: Repair CONTRIBUTING two lists (List 1 8->10, List 2 verify-test rename)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - `192-RESEARCH.md` §"CONTRIBUTING.md — TWO lists" (List 1 at 120-129 = 8 rows missing verify-hex-evaluator + verify-example-browser; List 2 at 173-185 with line 179 `Run test suite (verify-test)`; List 2 is a deliberate subset).
    - `192-CONTEXT.md` D-25 (List 1 8→10), D-19 (List 2 rename to matrix names).
    - `.github/workflows/ci.yml` header comment lines 1-2 (authoritative 10 job keys) — reconcile List 1 to this.
  </read_first>
  <action>
    **List 1 (Stable job keys table, ~120-129):** add two rows so the table lists all 10 `jobs:` keys matching the ci.yml header comment (lines 1-2): `verify-hex-evaluator` ("`mix verify.hex_evaluator` — threadline resolved from hex.pm in a nested project") and `verify-example-browser` ("`mix verify.example_browser` — operator-surface Playwright e2e on the example app"). Keep the existing 8 rows.
    **List 2 (Branch protection required checks, ~173-185):** replace ONLY line 179 `Run test suite (\`verify-test\`)` with the two matrix check names that ci.yml now posts: `Run test suite (min) (\`verify-test\` min lane)` and `Run test suite (current) (\`verify-test\` current lane)`. Do NOT add any other rows to List 2 — it is intentionally a subset (it still omits verify-compile-no-optional / verify-hex-evaluator / verify-example-browser). Do not touch the other List 2 entries.
  </action>
  <verify>
    <automated>grep -q '`verify-hex-evaluator`' CONTRIBUTING.md && grep -q '`verify-example-browser`' CONTRIBUTING.md && grep -q 'Run test suite (min)' CONTRIBUTING.md && grep -q 'Run test suite (current)' CONTRIBUTING.md && echo CONTRIB_OK</automated>
  </verify>
  <acceptance_criteria>
    - List 1 table lists all 10 job keys (adds verify-hex-evaluator + verify-example-browser), matching the ci.yml header comment.
    - List 2 line 179 is replaced by `Run test suite (min)` and `Run test suite (current)`; no other List 2 rows added.
    - List 2 remains a subset (verify-compile-no-optional / verify-hex-evaluator / verify-example-browser still absent from List 2).
  </acceptance_criteria>
  <done>Both CONTRIBUTING lists reconciled without conflating them.</done>
</task>

<task type="auto">
  <name>Task 3: Make the compatibility contract explicit in README and mix.exs</name>
  <files>README.md, mix.exs</files>
  <read_first>
    - `192-CONTEXT.md` D-13 (Elixir 1.15 floor / 1.17.3 current; OTP 26 min / 27 current; PostgreSQL 14 min / 16 current; make explicit in README + a mix.exs comment).
    - `README.md` line 11 (`**CI:** Runs on [GitHub Actions]...`) — a good anchor to place/extend the support-contract line near.
    - `mix.exs` line 28 (`elixir: "~> 1.15"`) — the comment sits beside it.
    - `CONTRIBUTING.md` lines 5-9 already state 1.15+/OTP26+/PG14+ (partly present) — keep README/mix.exs consistent with it.
  </read_first>
  <action>
    In `README.md`, add an explicit **Supported versions** statement (near the `**CI:**` line at 11) spelling out the min/current split: Elixir **1.15 floor / 1.17.3 current**, OTP **26 min / 27 current**, PostgreSQL **14 min / 16 current**, and note the CI min lane proves the 1.15 floor. In `mix.exs`, add a comment on the line directly above `elixir: "~> 1.15"` (line 28) recording the same contract and that the floor is honored by the CI min lane, NOT by raising this requirement (D-14). Do NOT change the `elixir: "~> 1.15"` string itself.
  </action>
  <verify>
    <automated>grep -Eiq '1\.15.*floor|floor.*1\.15|Supported versions' README.md && grep -q 'elixir: "~> 1.15"' mix.exs && awk '/elixir: "~> 1.15"/{if(prev ~ /#/) ok=1} {prev=$0} END{exit !ok}' mix.exs && echo COMPAT_OK</automated>
  </verify>
  <acceptance_criteria>
    - README states the explicit Elixir/OTP/PostgreSQL min/current support contract.
    - mix.exs has a comment directly above the `elixir: "~> 1.15"` line recording the contract and floor-honoring rationale.
    - The `elixir: "~> 1.15"` requirement string is unchanged.
  </acceptance_criteria>
  <done>Compatibility policy is explicit in README + mix.exs without moving the floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release publish concurrency -> hex.pm | Concurrency scope must not serialize bookkeeping nor allow overlapping publishes |
| docs (CONTRIBUTING/README/mix.exs) -> adopters & branch protection | Drift between docs and the live pipeline misleads adopters and maintainers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-192-06 | Denial of Service | Release-workflow serialization regression | medium | mitigate | Scope concurrency to the publish job only (D-24); bookkeeping stays independent; idempotency skip + gate-ci-green remain the real race guards |
| T-192-07 | Tampering | Doc/pipeline drift (job-key list, required checks) | medium | mitigate | List 1 reconciled to the ci.yml header comment; List 2 renamed in lockstep with the matrix; Plan 04 contract test locks both |
| T-192-08 | Elevation of Privilege | Silently raising the Elixir floor and stranding 1.15 adopters | high | mitigate | `elixir: "~> 1.15"` string unchanged; contract added as comment/README only (D-14); prohibitions forbid raising it |
| T-192-04 | Denial of Service | Branch-protection lockout (List 2 rename ahead of reconfig) | high | transfer | List 2 documents the new names; the actual GitHub reconfiguration is the human step in Plan 04 (D-19) |
</threat_model>

<verification>
- `git diff mix.exs` shows only an added comment; `elixir: "~> 1.15"` untouched.
- CONTRIBUTING List 1 has 10 keys; List 2 has the two renamed test-suite checks and no added subset rows.
- release.yml publish-hex carries the per-ref publish concurrency; no `run_id`.
- Plan 04's contract test asserts publish concurrency free of run_id, job-key parity, and the matrix check names in List 2 (durable lock).
</verification>

<success_criteria>
CI-03: pgbouncer pin (Plan 02) + release concurrency + branch-protection docs + job names align and are documented. CI-04: min/current Elixir/OTP/PostgreSQL policy is explicit in README + mix.exs without raising the floor.
</success_criteria>

<output>
Create `.planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-03-SUMMARY.md` when done.
</output>
