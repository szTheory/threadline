---
phase: 96-evidence-persistence-and-public-api
plan: 02
type: execute
wave: 2
depends_on:
  - "96-01"
files_modified:
  - lib/threadline/evidence.ex
  - test/threadline/evidence_test.exs
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "Evidence reads are Phoenix-optional and use the same public context as writes."
    - "History is the canonical truth surface; latest helpers are explicit projections over append-only rows."
    - "List helpers return lists and singular helpers return one record or `nil`."
    - "Phase 96 does not add a mutable current-state table, cache, or broad root-module evidence sprawl."
  artifacts:
    - path: lib/threadline/evidence.ex
      provides: "Public create/read evidence API"
      contains: "list_latest_subject_refs"
    - path: test/threadline/evidence_test.exs
      provides: "Executable proof for history/latest read semantics and stable return shapes"
      contains: "get_latest_subject_ref"
  key_links:
    - from: lib/threadline/evidence.ex
      to: lib/threadline/query.ex
      via: "read helpers should follow explicit validation and stable return-shape conventions"
      pattern: "history|as_of|timeline"
    - from: lib/threadline/evidence.ex
      to: lib/threadline/investigation.ex
      via: "higher-level helper families should compose query logic without Phoenix coupling"
      pattern: "row_history|linked_changes"
---

<objective>
Finish Phase 96 by extending the public `Threadline.Evidence` context with the
read-side contract: generic history helpers, explicit latest projections, and
stable return shapes that later Mix-task and mounted `/audit` work can reuse.

Purpose: land the public create/read half of `PROOF-01`.
Output: Phoenix-optional evidence read helpers and tests proving history-first,
append-only semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/96-evidence-persistence-and-public-api/96-CONTEXT.md
@.planning/phases/96-evidence-persistence-and-public-api/96-RESEARCH.md
@.planning/phases/96-evidence-persistence-and-public-api/96-PATTERNS.md
@lib/threadline/evidence.ex
@lib/threadline/query.ex
@lib/threadline/investigation.ex
@lib/threadline/governance/evidence_record.ex
@test/threadline/evidence_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generic history reads with explicit filters and stable return shapes</name>
  <files>lib/threadline/evidence.ex, test/threadline/evidence_test.exs</files>
  <read_first>
    <file>lib/threadline/evidence.ex</file>
    <file>lib/threadline/query.ex</file>
    <file>lib/threadline/investigation.ex</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-RESEARCH.md</file>
  </read_first>
  <action>Extend `Threadline.Evidence` with generic read helpers for the append-only ledger. Add a broad history helper plus a focused helper for one subject family and subject reference. Filter vocabulary may include `subject`, `subject_ref`, `from`, `to`, and `limit`, but the surface should stay small and explicit. Validate option keys instead of silently ignoring unknown filters. Lists must always return lists, including `[]`, and the helpers must remain Phoenix-optional with explicit `repo:` handling.</action>
  <acceptance_criteria>
    <criterion>`Threadline.Evidence` exposes explicit history/list helpers for the evidence ledger without leaking schema-query details into callers.</criterion>
    <criterion>Unknown filter keys are rejected loudly rather than silently ignored.</criterion>
    <criterion>`test/threadline/evidence_test.exs` proves list helpers return stable list shapes for hit and miss cases.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix verify.test</automated>
    <manual>Use `mix test test/threadline/evidence_test.exs --max-failures 1` only as the focused iteration loop for the evidence read contract before rerunning the named verification entrypoint.</manual>
  </verify>
  <done>Threadline has a reusable, Phoenix-optional history read surface for evidence rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit overview and singular latest helpers as append-only query projections</name>
  <files>lib/threadline/evidence.ex, test/threadline/evidence_test.exs</files>
  <read_first>
    <file>lib/threadline/evidence.ex</file>
    <file>lib/threadline/query.ex</file>
    <file>lib/threadline/governance/evidence_record.ex</file>
    <file>.planning/phases/96-evidence-persistence-and-public-api/96-CONTEXT.md</file>
  </read_first>
  <action>Add two explicit latest helper shapes that project over history rather than introducing mutable state. First, add an overview helper that returns a list-shaped latest-per-subject-ref projection for one subject family so Phase 97/98 summary consumers can read the newest row for each subject reference without changing list semantics. Second, add a singular helper for one subject plus one subject reference that returns one `%Threadline.Governance.EvidenceRecord{}` or `nil`. The helper naming should use `latest_*`, not `current_*`. Order rows by `recorded_at` descending and then `id` descending so ties stay deterministic. Add tests proving the overview helper stays list-shaped, the singular helper stays singular, the newest row wins for one subject/reference pair, older history remains accessible through the list helper, and no helper changes return shape through options.</action>
  <acceptance_criteria>
    <criterion>The latest helper family uses explicit `latest_*` naming and includes both a list-shaped latest-per-subject-ref overview helper and a singular latest helper for one subject/reference pair.</criterion>
    <criterion>`test/threadline/evidence_test.exs` locks the list return shape for the overview helper separately from the singular return shape of the one-subject-reference helper.</criterion>
    <criterion>The read path does not add a second mutable state model, cache, or update-in-place evidence abstraction.</criterion>
    <criterion>`mix verify.test` exits 0 with coverage for latest-vs-history semantics and list-vs-singular return-shape stability.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix verify.test</automated>
    <manual>Use `mix test test/threadline/evidence_test.exs --max-failures 1` as the narrow repro loop while iterating on latest-helper semantics, then rerun `mix verify.test` as the named proof band.</manual>
  </verify>
  <done>The public evidence API now offers both canonical history and convenient latest projections without breaking append-only semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public reader -> evidence ledger | Read helpers must stay truthful to append-only history and not imply mutable current-state semantics. |
| Filter input -> query execution | The API should validate allowed filters explicitly to avoid accidental broad or misleading query shapes. |
| Latest helper -> historical truth | Convenience projections must not replace or obscure the canonical ledger history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04 | Tampering / semantic drift | latest projection | mitigate | Implement latest as ordered history query (`recorded_at`, `id`) instead of any mutable cache or upserted current-state row. |
| T-96-05 | Information disclosure / misuse | query surface | mitigate | Keep filter vocabulary explicit and small; reject unknown keys loudly. |
| T-96-06 | Repudiation | public evidence reads | mitigate | Preserve history-first helpers so later phases and users can inspect the full append-only chain behind any latest projection. |
</threat_model>

<verification>
Run `mix verify.test`, then read the evidence public API to confirm that
history helpers return lists, overview latest-per-subject-ref helpers also
return lists, singular latest helpers return one record or `nil`, and latest is
implemented as a projection over append-only rows rather than as mutable
current state. Use the targeted evidence test file only as a focused debugging
loop before rerunning `mix verify.test`.
</verification>

<success_criteria>
Threadline exposes a coherent public evidence API for create/read flows:
subject-focused writes, generic history reads, and explicit latest helpers that
stay honest about append-only semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/96-evidence-persistence-and-public-api/96-02-SUMMARY.md`
</output>
