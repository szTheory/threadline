---
phase: 92-phase-87-verification-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/getting-started-saas.md
  - guides/operator-surface.md
  - guides/upgrade-path.md
  - examples/threadline_phoenix/README.md
  - test/threadline/getting_started_saas_doc_contract_test.exs
  - test/threadline/example_phoenix_readme_contract_test.exs
  - test/threadline/operator_surface_doc_contract_test.exs
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
autonomous: true
requirements:
  - ADOPT-01
  - ADOPT-02
must_haves:
  truths:
    - "Adopters can copy one canonical `/audit` mount recipe that keeps admin and support personas on the same host-owned route tree."
    - "The public contract surfaces and the runnable example prove the same host-owned `authorize_fn`, `scope_query_fn`, and `export_authorize_fn` story."
    - "The proof can be rerun through named repo entrypoints instead of one-off shell commands."
  artifacts:
    - path: "guides/getting-started-saas.md"
      provides: "Canonical `/audit` adoption recipe"
      min_lines: 200
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      provides: "Shared `/audit` router proof with host-owned auth, scope, and export seams"
      min_lines: 120
    - path: "mix.exs"
      provides: "Named verification entrypoints for doc and example proof"
      contains: "\"verify.doc_contract\""
  key_links:
    - from: "guides/getting-started-saas.md"
      to: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      via: "shared `/audit` code snippet and seam naming"
      pattern: "scope \"/audit\""
    - from: "mix.exs"
      to: "examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs"
      via: "`mix verify.example` nested example rerun surface"
      pattern: "verify\\.example"
    - from: "test/threadline/getting_started_saas_doc_contract_test.exs"
      to: "guides/getting-started-saas.md"
      via: "doc-contract assertions for support-lane wording"
      pattern: "scope_query_fn|export_authorize_fn|/audit"
---

<objective>
Re-verify the current-tree public contract and example-host proof for the canonical shared `/audit` mount, repairing only local, fully provable drift in the same pass.

Purpose: Close the missing proof band for `ADOPT-01` and `ADOPT-02` using the repo's named rerun surfaces instead of artifact-only claims.
Output: Aligned public docs, example proof surfaces, and named verification entrypoints for the canonical `/audit` recipe.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/92-phase-87-verification-backfill/92-CONTEXT.md
@.planning/phases/92-phase-87-verification-backfill/92-RESEARCH.md
@.planning/phases/92-phase-87-verification-backfill/92-PATTERNS.md
@.planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md
@.planning/phases/89-contract-lock-final-verification/89-VALIDATION.md

<interfaces>
From mix.exs:
```elixir
preferred_envs: [
  "ci.all": :test,
  "verify.doc_contract": :test,
  "verify.example": :test
]
```

```elixir
"verify.doc_contract": [...]
"verify.example": &verify_example/1,
"ci.all": [
  ...,
  "verify.example",
  "verify.doc_contract"
]
```

From examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex:
```elixir
scope "/audit" do
  pipe_through([:browser, :operator_auth])

  threadline_operator_surface("/",
    authorize_fn: &ThreadlinePhoenixWeb.Router.my_authorize_fn/1,
    export_authorize_fn: &ThreadlinePhoenixWeb.Router.my_export_authorize_fn/1,
    scope_query_fn: &ThreadlinePhoenixWeb.Router.scope_operator_query/3,
    repo: ThreadlinePhoenix.Repo
  )
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-verify and, if needed, repair the public contract surfaces for the canonical `/audit` recipe</name>
  <files>guides/getting-started-saas.md, guides/operator-surface.md, guides/upgrade-path.md, examples/threadline_phoenix/README.md, test/threadline/getting_started_saas_doc_contract_test.exs, test/threadline/example_phoenix_readme_contract_test.exs, test/threadline/operator_surface_doc_contract_test.exs</files>
  <read_first>.planning/phases/92-phase-87-verification-backfill/92-CONTEXT.md, .planning/phases/92-phase-87-verification-backfill/92-RESEARCH.md, guides/getting-started-saas.md, guides/operator-surface.md, guides/upgrade-path.md, examples/threadline_phoenix/README.md, test/threadline/getting_started_saas_doc_contract_test.exs, test/threadline/example_phoenix_readme_contract_test.exs, test/threadline/operator_surface_doc_contract_test.exs, test/threadline/upgrade_path_doc_contract_test.exs</read_first>
  <behavior>
    - Test 1: The docs and example README keep one shared `/audit` mount and mention host-owned `authorize_fn`, `scope_query_fn`, and `export_authorize_fn` on that same tree.
    - Test 2: If any surface overclaims beyond what the current tree proves, the wording is narrowed immediately instead of preserving broader aspiration text.
    - Test 3: No surface introduces a separate `/support` tree, Threadline-owned RBAC, or Phase 93 fallback/unsupported UX copy.
  </behavior>
  <action>Per D-01, D-02, D-06, D-07, D-09, D-10, D-15, D-16, D-17, and D-18, rerun the doc-contract proof first and treat the four public contract files as product authority. If `mix verify.doc_contract` exposes local, non-controversial drift, repair the specific guide/README text and matching doc-contract assertions in the same pass so the public contract surfaces agree on one canonical shared `/audit` recipe, the host-owned `authorize_fn` + `scope_query_fn` + `export_authorize_fn` seam contract, the example-host `sigra-reference` proof, and the current-tree claim boundary. Treat `test/threadline/upgrade_path_doc_contract_test.exs` as a verification input rather than a default edit target in this plan; only expand into that assertion file if a later split is needed because the current tree proves broader upgrade-path drift than the local repair band allows. Narrow any wording that out-runs proof; do not add a second support tree, a Threadline-owned role/tenant abstraction, or broader export fallback UX from Phase 93.</action>
  <acceptance_criteria>
    - `mix verify.doc_contract` exits 0.
    - `rg -n 'scope \"/audit\"|authorize_fn|scope_query_fn|export_authorize_fn' guides/getting-started-saas.md guides/operator-surface.md guides/upgrade-path.md examples/threadline_phoenix/README.md` returns matches in the canonical contract files.
    - `rg -n '/support|Threadline-owned RBAC|tenancy DSL|policy engine' guides/getting-started-saas.md guides/operator-surface.md guides/upgrade-path.md examples/threadline_phoenix/README.md` returns no matches.
    - `rg -n 'mix verify\\.example|sigra-reference' guides/upgrade-path.md examples/threadline_phoenix/README.md test/threadline/upgrade_path_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs` returns matches proving the example-host rerun surface remains part of the claim.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.doc_contract</automated>
  </verify>
  <done>The public contract files and their doc-contract tests state one truthful, rerunnable canonical `/audit` recipe and no longer contain broader or conflicting support-lane wording.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-verify and, if needed, repair the runnable example-host proof and named rerun surfaces</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex, examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs</files>
  <read_first>.planning/phases/92-phase-87-verification-backfill/92-CONTEXT.md, .planning/phases/92-phase-87-verification-backfill/92-RESEARCH.md, examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex, examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs, mix.exs, .github/workflows/ci.yml</read_first>
  <behavior>
    - Test 1: The example router keeps one shared `/audit` scope with `my_authorize_fn/1`, `my_export_authorize_fn/1`, and `scope_operator_query/3`.
    - Test 2: The nested example project proves support-scope narrowing and admin-only export posture through `mix verify.example`, not through a root-run-only test path.
    - Test 3: CI and Mix aliases still expose `mix verify.example` and `mix verify.doc_contract` as discoverable named rerun surfaces.
  </behavior>
  <action>Per D-01, D-03, D-04, D-05, D-10, D-15, D-16, D-17, and D-18, rerun the example-host proof through `mix verify.example` and inspect the router, nested example tests, `mix.exs`, and `.github/workflows/ci.yml` as a single proof chain. If the shared `/audit` router snippet or support/export assertions drifted, repair those implementation proof surfaces inline in this plan. Treat `mix.exs` and `.github/workflows/ci.yml` as verification inputs rather than default edit targets; only carry alias or CI edits forward in a later authority-surface pass if the named rerun surfaces are actually broken on the current tree. Keep the proof adopter-facing: one shared route tree, host-owned auth/scope semantics, support narrowing on the same mount, and admin-only export posture. Do not replace the named rerun surfaces with ad-hoc artifact-only commands.</action>
  <acceptance_criteria>
    - `mix verify.example` exits 0.
    - `rg -n 'scope \"/audit\"|threadline_operator_surface\\(\"/\"|authorize_fn:|export_authorize_fn:|scope_query_fn:' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` returns the canonical shared-tree mount.
    - `rg -n 'verify\\.example|verify\\.doc_contract|ci\\.all' mix.exs` returns the named aliases and preferred env entries.
    - `rg -n 'run: mix verify\\.example|run: mix verify\\.doc_contract|verify-test|verify-docs' .github/workflows/ci.yml` returns the CI rerun surfaces tied to the example/doc proof.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.example</automated>
  </verify>
  <done>The current tree has a green named example-host proof path and the router, nested tests, Mix aliases, and CI job references all point to the same canonical `/audit` story.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter docs -> shared `/audit` route configuration | Untrusted implementer interpretation crosses from public guidance into a privileged operator mount. |
| Support-scoped operator -> export endpoint | Support users can browse scoped data but must not inherit export capability on the same tree. |
| Maintainer verification run -> proof verdict | Verification evidence crosses from ephemeral command output into durable milestone truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Information Disclosure | `guides/getting-started-saas.md`, `guides/operator-surface.md`, `guides/upgrade-path.md`, `examples/threadline_phoenix/README.md` | mitigate | Keep D-02 and D-07 enforced: docs may claim only the current-tree support lane, and doc-contract tests must pass before closure. |
| T-92-02 | Elevation of Privilege | `examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex`, `examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs` | mitigate | Keep one shared `/audit` mount with explicit `export_authorize_fn` and prove admin-only export posture through `mix verify.example` per D-03 and D-15. |
| T-92-03 | Repudiation | `mix.exs`, `.github/workflows/ci.yml` | mitigate | Use named rerun surfaces (`mix verify.doc_contract`, `mix verify.example`) and CI discoverability per D-04 so the proof can be reproduced after the run. |
| T-92-04 | False Closure / Tampering | plan execution narrative | mitigate | Reject one-off shell-only closure paths; if proof cannot be rerun through the named entrypoints, repair the entrypoints or leave the claim narrower. |
</threat_model>

<verification>
- `mix verify.doc_contract`
- `mix verify.example`
- `rg -n 'scope "/audit"|authorize_fn|scope_query_fn|export_authorize_fn' guides/getting-started-saas.md guides/operator-surface.md guides/upgrade-path.md examples/threadline_phoenix/README.md examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex`
- `rg -n 'verify\\.example|verify\\.doc_contract|ci\\.all' mix.exs .github/workflows/ci.yml`
</verification>

<success_criteria>
The current tree proves one adopter-facing canonical `/audit` recipe and one rerunnable example-host proof path for the same support-lane claim, with no broader unsupported wording left in the public contract surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/92-phase-87-verification-backfill/92-01-SUMMARY.md`
</output>
