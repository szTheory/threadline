---
phase: 162-brand-book-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/logo-primary.svg
  - brandbook/logo-primary-light.svg
  - brandbook/logo-mark.svg
  - brandbook/logo-monochrome.svg
  - brandbook/favicon.svg
  - brandbook/logo-primary-subtitle.svg
  - brandbook/logo-wordmark.svg
  - brandbook/social-card.svg
  - brandbook/examples/readme-header.svg
  - brandbook/examples/docs-page.svg
  - brandbook/examples/components.svg
  - brandbook/examples/landing-hero.svg
  - brandbook/examples/palette.svg
  - brandbook/examples/terminal.svg
  - brandbook/examples/typography.svg
  - .planning/phases/162-brand-book-v2/tools/brand-gate.mjs
autonomous: true
requirements: [BOOK-01, BOOK-02]
tags: [brand, svg, logo, c13, graduation]

must_haves:
  truths:
    - "Opening brandbook/logo-primary.svg in a browser shows the C13 topstitch wordmark — Geist 600 outlines, d/l ascenders cut at y=444, one blue stitch arc — geometrically identical to the tournament winner"
    - "Every SVG in brandbook/ (including examples/) renders correctly with zero installed fonts — pure paths, no <text> elements anywhere"
    - "logo-monochrome.svg survives literal one-color reproduction: exactly one paint value, no gradients, no opacity"
    - "favicon.svg is identifiable at a literal 16px render, carries no container chip, and every stroke is >= 1.0px at 16px canvas"
    - "The string FOLLOW WHAT HAPPENED appears in exactly two brandbook SVGs: logo-primary-subtitle.svg and social-card.svg"
    - "logo-primary-light.svg is an explicitly designed light rendition, not a recolor of the dark asset"
  artifacts:
    - path: "brandbook/logo-primary.svg"
      provides: "Dark-surface primary lockup, pure paths, C13 geometry"
      contains: "data-role=\"mark\""
    - path: "brandbook/logo-primary-light.svg"
      provides: "Light-surface primary lockup, explicitly designed inks"
    - path: "brandbook/logo-mark.svg"
      provides: "Extractable stitch mark (display-scale form of the favicon idiom)"
    - path: "brandbook/logo-monochrome.svg"
      provides: "True one-color master"
    - path: "brandbook/favicon.svg"
      provides: "16px-designed chipless favicon (C13 favicon cut)"
    - path: "brandbook/logo-primary-subtitle.svg"
      provides: "The ONLY lockup carrying FOLLOW WHAT HAPPENED, tagline as outlined IBM Plex Mono paths"
    - path: "brandbook/logo-wordmark.svg"
      provides: "Geist-600 pure-path wordmark with intact (uncut) d/l ascenders, no arc"
    - path: "brandbook/social-card.svg"
      provides: "1280x640 social card, dark, lockup + tagline, pure paths"
    - path: ".planning/phases/162-brand-book-v2/tools/brand-gate.mjs"
      provides: "Mechanical HC-1..6 gate adapted from Phase 161 hc-gate.mjs for brandbook asset roles"
  key_links:
    - from: "brandbook/logo-primary.svg"
      to: "C13 winner geometry"
      via: "arc path copied verbatim from c13-topstitch-geist.svg"
      pattern: "M 3164 449 V 260 A 130.5 130.5 0 0 1 3425 260 V 449"
    - from: ".planning/phases/162-brand-book-v2/tools/brand-gate.mjs"
      to: "brandbook/*.svg"
      via: "node brand-gate.mjs brandbook — exit 0, zero FAIL"
      pattern: "data-role"
    - from: "brandbook/logo-primary-subtitle.svg"
      to: "priv/fonts/ibm-plex-mono-500.woff2"
      via: "tagline outlined through brandbook/tools/text-to-paths.mjs"
      pattern: "FOLLOW WHAT HAPPENED"
---

<objective>
Graduate tournament winner C13 topstitch-geist into brandbook/ as the complete regenerated asset family — the first phase allowed to write brandbook/. Regenerate faithfully, do not redesign: the user's verbatim bar is "elegantly good" (their words selecting C13). Every asset is pure paths (HC-6), chipless (HC-2), and mechanically gated.

Purpose: BOOK-01 (full asset family from the winner) and BOOK-02 (tagline isolated to -subtitle + social card). Everything downstream — index.html (plan 02), pressure-test rerun (plan 03) — builds on these files.
Output: 8 logo-family SVGs in brandbook/, refreshed/pruned examples/, and a committed phase-dir brand-gate that proves HC-1..6 mechanically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-brand-book-v2/162-CONTEXT.md
@.planning/phases/161-logo-tournament/candidates/round-2/c13-topstitch-geist.svg
@.planning/phases/161-logo-tournament/candidates/round-2/c13-topstitch-geist-mono.svg
@.planning/phases/161-logo-tournament/candidates/round-2/c13-topstitch-geist-favicon.svg
@.planning/phases/161-logo-tournament/tools/hc-gate.mjs
@brandbook/tools/README.md
@.planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md

<interfaces>
<!-- C13 winner geometry facts — use directly, do not re-derive. -->

From c13-topstitch-geist.svg (the graduation source):
- viewBox: "-80 60 5194 1140"
- Glyph translate x-offsets (Geist 600, shaped kerning): T=0, h=562, r=1147, e=1516, a=2105, d=2677, l=3291, i=3580, n=3855, e=4443
- d/l ascenders CUT at y=444 (just above Geist x-height top 471); d stem spans x 3100-3228, l stem x 3361-3489
- The stitch arc (the mark): `<path data-role="mark" fill="none" stroke="#4781E6" stroke-width="128" d="M 3164 449 V 260 A 130.5 130.5 0 0 1 3425 260 V 449"/>` — stroke 128 = measured Geist 600 stem width, rise 319.5 above the cut
- Mono variant: identical structure, every fill/stroke = currentColor
- Favicon (designed at 16px, viewBox "0 0 16 16", chipless): arc `M 5.2 13.6 V 6 A 2.8 2.8 0 0 1 10.8 6 V 13.6` + fabric line `M 0.8 10.9 H 15.2`, both stroke-width 1.7, currentColor

Uncut (original) d and l Geist 600 outlines for logo-wordmark.svg: read the Phase 160 glyph kit in `.planning/phases/160-glyph-outline-pipeline/` (glyph-kit-geist-600 .svg/.json), or regenerate deterministically via the committed pipeline:
  FONTKIT_DIR=$(mktemp -d)
  npm install --prefix "$FONTKIT_DIR" --no-fund --no-audit --silent fontkit@2.0.4
  NODE_PATH="$FONTKIT_DIR/node_modules" node brandbook/tools/text-to-paths.mjs --font priv/fonts/geist-600.woff2 --out /tmp/kit-geist-600
Same pipeline outlines the tagline: `--font priv/fonts/ibm-plex-mono-500.woff2 --text "FOLLOW WHAT HAPPENED"` (fontkit@2.0.4 is the pinned, Phase-160-vetted canonical invocation from brandbook/tools/README.md — ephemeral install, never commit node_modules/package.json).

Token blues (brandbook/tokens.json + tokens.css): `--tl-color-thread-blue: #4F8CFF`, `--tl-color-accent: #4F8CFF`. The winner arc is #4781E6 — reconciliation decision belongs to this plan (Task 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build brand-gate.mjs — the mechanical verification contract for brandbook assets</name>
  <files>.planning/phases/162-brand-book-v2/tools/brand-gate.mjs</files>
  <action>Adapt .planning/phases/161-logo-tournament/tools/hc-gate.mjs (plain Node, no deps, exit 0 only on zero FAIL) into a brandbook-role gate. Invocation: `node .planning/phases/162-brand-book-v2/tools/brand-gate.mjs brandbook`. Drop the TOUR-01 quota section (tournament-only). Replace the `c\d+-` filename filter with role classification by filename: logo-primary.svg / logo-primary-light.svg / logo-wordmark.svg = role "primary" (HC-1 glyph inventory == letters of "Threadline" exactly, via data-glyph attributes); logo-mark.svg = role "mark" (inventory subset, mark-only allowed); logo-monochrome.svg = role "mono" (HC-5: no gradient, no opacity, exactly one paint value across all fill/stroke != none); favicon.svg = role "favicon" (HC-4: <= 4 painted elements, every stroke >= 1.0px scaled to 16px canvas with WARN below 1.5px target, inventory subset; ignore an internal `<style>` block when counting paints — a prefers-color-scheme flip is sanctioned for favicons); logo-primary-subtitle.svg / social-card.svg = role "tagline-bearing" (must contain the literal string "FOLLOW WHAT HAPPENED" in an aria-label, `<title>`, or comment — pure-path tagline glyphs do not carry the string, so the gate enforces its presence as metadata; "Threadline" inventory must be present as a subset; extra tagged paths allowed); examples/*.svg = role "example" (HC-6 + hygiene only). Apply to ALL roles: HC-6 zero `<text>`; hygiene (no script/image/foreignObject, no http refs in href/src/url()); HC-2 zero `<rect>` and the >=85%-of-both-viewBox-dimensions filled-plate heuristic — with ONE sanctioned exemption: social-card.svg may have exactly one full-bleed path tagged data-role="background" (a social card has a canvas; a mark must not have a chip — HC-2 protects the mark, the literal `<rect>` ban stays absolute). TAGGING: every painted path carries data-glyph or data-role in {mark, tagline, background, art}. Also gate BOOK-02 corpus-wide: scan every brandbook SVG and FAIL if "FOLLOW WHAT HAPPENED" appears in any file other than logo-primary-subtitle.svg and social-card.svg, or is missing from either of those two.</action>
  <verify>
    <automated>node .planning/phases/162-brand-book-v2/tools/brand-gate.mjs brandbook; test $? -le 1</automated>
  </verify>
  <done>brand-gate.mjs exists, runs against the current (old) brandbook and reports FAILs for the known old-asset defects (text elements, favicon chip, two-ink monochrome) — proving the gate detects what it must. Exit-0 is NOT expected until Tasks 2-3 land.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the core family — primary dark/light, wordmark, monochrome, mark, favicon</name>
  <files>brandbook/logo-primary.svg, brandbook/logo-primary-light.svg, brandbook/logo-wordmark.svg, brandbook/logo-monochrome.svg, brandbook/logo-mark.svg, brandbook/favicon.svg</files>
  <action>Replace the old files in place (same filenames — downstream references survive, per CONTEXT "Asset family" decision). Regenerate faithfully from C13, do not redesign. (1) logo-primary.svg: C13 geometry verbatim (glyph paths, offsets, cut d/l, arc) with literal colors for dark surfaces — glyph ink from the existing dark-surface ink family in tokens.css (light neutral, e.g. the ink the old dark primary used) and the arc in the reconciled blue. BLUE RECONCILIATION (record in SUMMARY, applied in docs by plan 02): keep #4781E6 — the user approved this exact rendering at the tournament checkpoint and CONTEXT mandates faithful regeneration; #4F8CFF stays the UI/tokens accent; #4781E6 becomes an additive "stitch blue" raw token documented in plan 02. Operator-surface tokens untouched. (2) logo-primary-light.svg: explicitly DESIGNED for white/light surfaces (tournament lesson — never a recolor): choose dark glyph ink from the light-mode token lane, then verify the arc blue on white — #4781E6 on #FFFFFF is ~3.9:1 (passes the 3:1 graphics floor) but confirm visually at render; if the arc reads weak, darken the light-surface arc deliberately and record the chosen hex. (3) logo-wordmark.svg: the Geist-600 word with INTACT d/l ascenders and no arc — restore the uncut d and l outlines from the Phase 160 glyph kit (or regenerate via the pinned pipeline); fills currentColor (adaptive, inline-use asset). (4) logo-monochrome.svg: C13 mono structure verbatim — every paint currentColor, exactly one paint value, zero gradients/opacity (HC-5). (5) logo-mark.svg: the extractable stitch mark — display-scale form of the favicon idiom (one stitch arc diving through the fabric line), proportions derived from the C13 favicon cut scaled up; stroke geometry as paths with stroke-width, fabric/stem geometry currentColor, arc in the stitch blue; must read as the same mark as favicon.svg. (6) favicon.svg: the C13 favicon cut verbatim (viewBox 0 0 16 16, two 1.7-width strokes, NO container chip — replaces the old rounded-square); color treatment must be legible on both light and dark browser chrome WITHOUT relying on currentColor inheritance: default dark-ink strokes with an internal `<style>@media (prefers-color-scheme: dark)</style>` flip to light ink, or stitch-blue strokes if they hold on both — pick one, record why. Tag every painted path data-glyph or data-role="mark". Preserve C13's explanatory comment style (one short geometry comment per file is fine; no process history).</action>
  <verify>
    <automated>sh -c 'for f in brandbook/logo-primary.svg brandbook/logo-primary-light.svg brandbook/logo-wordmark.svg brandbook/logo-monochrome.svg brandbook/logo-mark.svg brandbook/favicon.svg; do xmllint --noout "$f" || exit 1; done && ! grep -l "<text" brandbook/logo-*.svg brandbook/favicon.svg && grep -q "M 3164 449 V 260 A 130.5 130.5 0 0 1 3425 260 V 449" brandbook/logo-primary.svg && grep -q "M 3164 449 V 260 A 130.5 130.5 0 0 1 3425 260 V 449" brandbook/logo-primary-light.svg'</automated>
  </verify>
  <done>All six files replaced, xmllint-clean, zero text elements, arc geometry byte-identical to the winner in both primaries, monochrome has exactly one paint value, favicon has no rect and no chip, wordmark d/l ascenders reach the full Geist ascender height (h-glyph top y=295, not 444).</done>
</task>

<task type="auto">
  <name>Task 3: Tagline-bearing assets + examples refresh/prune, then run the full gate suite</name>
  <files>brandbook/logo-primary-subtitle.svg, brandbook/social-card.svg, brandbook/examples/readme-header.svg, brandbook/examples/docs-page.svg, brandbook/examples/components.svg, brandbook/examples/landing-hero.svg, brandbook/examples/palette.svg, brandbook/examples/terminal.svg, brandbook/examples/typography.svg</files>
  <action>(1) Outline the tagline once: run the pinned pipeline with --font priv/fonts/ibm-plex-mono-500.woff2 --text "FOLLOW WHAT HAPPENED" (canonical ephemeral-fontkit invocation from brandbook/tools/README.md). (2) logo-primary-subtitle.svg: the ONLY lockup carrying the tagline (BOOK-02) — primary dark lockup with the outlined tagline beneath, letter-spacing applied by adjusting per-glyph translate offsets (mono advance + a consistent tracking increment; derive optically, record the number); tagline paths grouped with data-role="tagline"; include aria-label="Threadline — FOLLOW WHAT HAPPENED" on the svg root so the literal string greps here (the gate requires it). (3) social-card.svg: 1280x640 (og:image aspect), dark night-infrastructure canvas as ONE full-bleed path tagged data-role="background" (no rect — the gate exempts exactly this one path), C13 lockup + outlined tagline, optionally the positioning line as outlined paths tagged data-role="copy" only if it survives legibility at 1280x640 preview scale — all pure paths, aria-label carrying the tagline string. (4) Examples — all killer no filler (CONTEXT discretion, exercised here): regenerate readme-header.svg (dark GitHub-header specimen with the new lockup INLINED as paths — no image href, per GAP-07/UP-09) and docs-page.svg (docs chrome specimen with inlined mark) as the minimum set; PRUNE landing-hero.svg (landing deferred to LANDING-01 — no consuming surface), and evaluate components.svg / palette.svg / terminal.svg / typography.svg: keep only those a real surface needs AND that can be re-cut to pure paths cheaply; delete the rest with `git rm`, recording each removal + reason in the SUMMARY (plan 02 aligns doc references to the surviving set). Any kept example must be pure paths, zero text, no http refs. (5) Run the complete mechanical suite and fix until green: brand-gate exit 0 with zero FAIL; `grep -rln '<text' brandbook/ --include='*.svg'` empty; tagline grep returns exactly the two sanctioned files; `grep -rln '<rect' brandbook/ --include='*.svg'` empty; xmllint clean across all brandbook SVGs; size budget `find brandbook -type f ! -name '.DS_Store' -print0 | xargs -0 du -ck | tail -1` <= 300KB. Git hygiene: stage ONLY brandbook/ and .planning/phases/162-brand-book-v2/ paths explicitly — never `git add -A`; pre-existing uncommitted lib/, examples/threadline_phoenix/, test/ changes belong to another lane and must not be staged.</action>
  <verify>
    <automated>sh -c 'node .planning/phases/162-brand-book-v2/tools/brand-gate.mjs brandbook && test -z "$(grep -rln "<text" brandbook/ --include="*.svg")" && test "$(grep -rl "FOLLOW WHAT HAPPENED" brandbook/ --include="*.svg" | sort | tr "\n" " ")" = "brandbook/logo-primary-subtitle.svg brandbook/social-card.svg " && test -z "$(grep -rln "<rect" brandbook/ --include="*.svg")"'</automated>
  </verify>
  <done>brand-gate exits 0 with zero FAIL across all brandbook SVGs; tagline isolated to exactly the two sanctioned files; examples are pure-path and pruned with recorded reasons; brandbook total <= 300KB; no out-of-lane files staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG → GitHub/browser render | Brand SVGs render under GitHub's CSP sandbox (`default-src 'none'`) and arbitrary browsers — no scripts/external refs may exist |
| npm → build host | Ephemeral fontkit install for glyph outlining |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-01 | Tampering/Elevation | brandbook/*.svg | mitigate | brand-gate hygiene check FAILs on `<script>`, `<image>`, `<foreignObject>`, or any http(s) ref in href/src/url() |
| T-162-SC | Tampering | npm install fontkit | mitigate | Pinned fontkit@2.0.4 per the committed Phase-160-vetted canonical invocation in brandbook/tools/README.md; ephemeral prefix dir; node_modules/package.json never committed |
</threat_model>

<verification>
- `node .planning/phases/162-brand-book-v2/tools/brand-gate.mjs brandbook` → exit 0, zero FAIL
- `grep -rln '<text' brandbook/ --include='*.svg'` → empty (HC-6, recursive incl. examples)
- `grep -rl 'FOLLOW WHAT HAPPENED' brandbook/ --include='*.svg' | sort` → exactly logo-primary-subtitle.svg + social-card.svg (BOOK-02)
- `grep -rln '<rect' brandbook/ --include='*.svg'` → empty (HC-2)
- `for f in brandbook/*.svg brandbook/examples/*.svg; do xmllint --noout "$f"; done` → exit 0
- `find brandbook -type f ! -name '.DS_Store' -print0 | xargs -0 du -ck | tail -1` → <= 300KB
- `git status --porcelain lib/ test/ examples/threadline_phoenix/ | grep -c '^[MADRC]'` → 0 staged (pre-existing worktree changes untouched)
</verification>

<success_criteria>
- BOOK-01: all 8 family files exist in brandbook/, pure paths, regenerated from C13 with byte-identical arc geometry in the primaries
- BOOK-02: tagline grep gate green
- Blue reconciliation decision recorded in SUMMARY with rationale (default: keep #4781E6 as stitch blue, additive token in plan 02)
- Light rendition + favicon color treatment decisions recorded
- Example prune list with per-file reasons recorded for plan 02 doc alignment
</success_criteria>

<output>
Create `.planning/phases/162-brand-book-v2/162-01-SUMMARY.md` when done. It MUST record: the blue reconciliation decision + hex used per surface, the favicon color mechanism chosen, the subtitle tracking number, and the example keep/prune table with reasons.
</output>
