---
phase: 162-brand-book-v2
plan: 02
type: execute
wave: 2
depends_on: [162-01]
files_modified:
  - brandbook/index.html
  - brandbook/brand-book.md
  - brandbook/README.md
  - brandbook/tokens.json
  - brandbook/tokens.css
autonomous: true
requirements: [BOOK-03, BOOK-05]
tags: [brand, brandbook, html, misuse-gallery, docs, tokens]

must_haves:
  truths:
    - "User can open brandbook/index.html directly from disk (file://) and the entire brand book renders with networking disabled — zero external requests"
    - "index.html covers identity story, logo system (full family, clear-space rule, minimum sizes, dark/light usage with the GitHub <picture> snippet), color, typography, voice/microcopy, and application examples"
    - "A visual misuse gallery renders the killed antipatterns inline: background chip, icon-bolted-beside-plain-text, subtitle-in-primary, gradient-dependence, stretched/recolored abuse — as rendered don'ts, not prose"
    - "brand-book.md describes the stitch logo system as settled brand truth with no tournament/process-history framing"
    - "The stitch blue is reconciled in tokens: additive entry only, operator-surface token values unchanged"
  artifacts:
    - path: "brandbook/index.html"
      provides: "Standalone professional brand book, self-contained CSS, misuse gallery inline"
      contains: "prefers-color-scheme"
    - path: "brandbook/brand-book.md"
      provides: "Source-of-truth guide updated: logo system, clear-space/min-size numbers, misuse rules with numeric thresholds, asset roster"
    - path: "brandbook/README.md"
      provides: "Accurate asset inventory matching the new family and surviving examples"
    - path: "brandbook/tokens.json"
      provides: "Additive stitch-blue raw token (if plan 01 kept #4781E6)"
  key_links:
    - from: "brandbook/index.html"
      to: "brandbook/logo-primary.svg"
      via: "relative same-directory img/inline references only"
      pattern: "logo-primary"
    - from: "brandbook/index.html"
      to: "GitHub README dark/light handling"
      via: "copy-pasteable <picture> snippet (GAP-06/UP-06)"
      pattern: "<picture>"
    - from: "brandbook/tokens.css"
      to: "brandbook/index.html color section"
      via: "documented hex values match token values"
      pattern: "4781E6|4F8CFF"
---

<objective>
Rebuild brandbook/index.html as a standalone professional HTML brand book around the C13 stitch identity, with a rendered misuse gallery, and update the durable docs (brand-book.md, README.md, tokens) to describe the new system as settled truth. The user's bar, verbatim: "very professional, stands on its own."

Purpose: BOOK-03 (standalone brand book, zero network) and BOOK-05 (misuse gallery documenting the killed antipatterns). Closes GAP-06/UP-06 (<picture> documentation), GAP-12/UP-10 (visual misuse gallery + numeric thresholds).
Output: rebuilt index.html, updated brand-book.md / README.md / tokens.{json,css}.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-brand-book-v2/162-CONTEXT.md
@.planning/phases/162-brand-book-v2/162-01-SUMMARY.md
@brandbook/brand-book.md
@brandbook/tokens.css
@brandbook/tokens.json
@.planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md

<interfaces>
<!-- Contracts this plan consumes. Plan 01's SUMMARY carries the blue decision,
     favicon mechanism, and example keep/prune table — read it first. -->

GitHub README dark/light snippet to document (RESEARCH §3b / GAP-06 / UP-06 — documented HERE; applying to the root README is optional Phase 163 ROLL-03):
```html
<picture>
  <source media="(prefers-color-scheme: dark)" srcset="brandbook/logo-primary.svg">
  <img alt="Threadline" src="brandbook/logo-primary-light.svg" width="420">
</picture>
```

HC-4 numeric thresholds (the misuse-gallery/min-size numbers — restate, do not invent):
- Stroke >= 1.5px at 16px canvas (target), >= 1.0px absolute floor
- Gap/counter >= 1.0px at 16px; >= 1.5px around modifier elements
- <= 4 distinct strokes/elements at 16px; silhouette-first identification required
- Design at 16px, never shrink — favicon.svg IS the 16px artifact

C13 geometry for clear-space/min-size derivation (CONTEXT discretion — derive, document, done):
- Primary viewBox 5194x1140 (aspect ~4.56:1), cap height 710 units (y 295→1005), arc stroke 128 units, arc rise 319.5 above the y=444 cut
- Arc stroke renders at height_px * (128/1140) — e.g. 32px-tall render gives ~3.6px arc; wordmark legibility, not the arc, governs the minimum

KEEP verdicts (AUDIT §10, §7 — port, don't rewrite): voice/microcopy say-this/not-this pairs, banned vocabulary, palette philosophy, token architecture. Naming and tagline are settled language.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild index.html as the standalone professional brand book</name>
  <files>brandbook/index.html</files>
  <action>Replace brandbook/index.html wholesale. Design (CONTEXT discretion): calm, dark-first, professional — the page itself must embody the brand it documents; use the token palette via embedded CSS (copy values in, or inline tokens.css content — NO external stylesheet links, no @import, no web fonts; system font stacks with Geist/IBM Plex Mono named first so locally-installed fonts upgrade gracefully; logo renders never depend on fonts because they are paths). Self-containment rules: CSS embedded in `<style>`; asset references are relative same-directory paths only (`<img src="logo-primary.svg">` works under file:// and is not a network request); zero http(s) in src/href/url(); no `<script src>`, prefer zero JS entirely. Sections, in order: (1) Identity story — essence ("Threadline makes system history followable"), the metaphor now genuinely embodied: the stitch arc literally rises out of the word's fabric and dives back in — tell it without overclaiming (CONTEXT wording); anti-traits list. (2) Logo system — full family table (each asset rendered, role, surface assignment per plan 01's decisions incl. dark/light/adaptive usage), clear-space rule and minimum sizes (derive concrete numbers from C13 geometry + HC-4 and state them as numbers), dark/light strategy including the GitHub `<picture>` snippet rendered as a copy-pasteable HTML-ESCAPED code block (&lt;picture&gt;) showing root-README-relative paths; if a LIVE <picture> demo is also embedded, it must use same-directory paths (logo-primary.svg, not brandbook/logo-primary.svg) so it renders under file://, favicon story (designed at 16px, never shrunk). (3) MISUSE GALLERY (BOOK-05) — each killed antipattern rendered visually as an inline SVG "don't" specimen with a one-line rule beneath: background chip behind the mark, icon-bolted-beside-plain-text, subtitle-in-primary, gradient-dependence, stretched/squashed distortion, off-palette recolor. Build the abuse specimens as INLINE SVG inside index.html only — never as committed .svg files (the repo must not ship antipattern assets; the brand-gate would also fail them). Include the HC-4 numeric thresholds as the testable small-size rules. (4) Color — palette philosophy (night-infrastructure, KEEP), token swatches with hex values, and the stitch-blue reconciliation story exactly as plan 01's SUMMARY recorded it. (5) Typography — Geist + IBM Plex Mono roles, now with the pure-path wordmark story (why logo text is outlines, one sentence on portability). (6) Voice & microcopy — PORT the say-this/not-this pairs and banned vocabulary from brand-book.md ## Voice verbatim-faithful (KEEP verdict: port, don't rewrite). (7) Application examples — render the surviving examples/ specimens and the social card. Tagline appears in prose as "Follow what happened." (sentence case); the all-caps form stays exclusive to the two sanctioned SVGs. No process history anywhere: no tournament, no phase numbers, no audit/process references (the WORD audit stays — it is the product domain; banned is referencing the brand-audit process) — this is settled brand truth.</action>
  <verify>
    <automated>sh -c 'xmllint --html --noout brandbook/index.html 2>/dev/null; grep -cE "(src|href)[[:space:]]*=[[:space:]]*[\"'"'"']https?://" brandbook/index.html | grep -qx 0 && ! grep -q "url(http" brandbook/index.html && ! grep -q "@import" brandbook/index.html && grep -qE "(&lt;|<)picture" brandbook/index.html && grep -qi "misuse" brandbook/index.html'</automated>
  </verify>
  <done>index.html opens from disk fully rendered with networking disabled; all seven sections present; misuse gallery shows >= 6 rendered inline-SVG don'ts; <picture> snippet present as copyable code; zero external requests; no process-history framing.</done>
</task>

<task type="auto">
  <name>Task 2: Settle the durable docs — brand-book.md, README.md, tokens</name>
  <files>brandbook/brand-book.md, brandbook/README.md, brandbook/tokens.json, brandbook/tokens.css</files>
  <action>(1) brand-book.md: rewrite the ## Logo System section to describe the C13 stitch system as settled brand truth (family roster with per-surface assignments, clear-space + minimum-size numbers matching index.html exactly, the stitch metaphor, dark/light strategy + <picture> snippet, misuse rules updated to include the numeric HC-4 thresholds and the killed antipatterns); update ## Color System with the stitch-blue entry and the reconciliation rationale (one paragraph, present tense, no process history — v1.33 lesson per CONTEXT); update ## Repo Artifact Rules / asset references to the new family and the surviving examples (plan 01 SUMMARY's keep/prune table is authoritative); leave ## Voice, ## Messaging Framework, ## Brand DNA untouched (KEEP). Remove or rewrite any sentence describing the OLD marks (continuous-line squiggle, favicon chip) as current. (2) README.md: update the asset list ("Best Current Defaults" logo-system line now describes the integrated stitch typemark; file inventory matches what exists; usage line for dark vs light primaries stays accurate). (3) tokens.json + tokens.css: ADDITIVE only — if plan 01 kept #4781E6, add a raw token (e.g. "stitch-blue": "#4781E6") and a semantic logo/arc alias in both formats, mirroring the existing raw/semantic layering and dark/light lane structure; change NO existing values (operator-surface contract untouched — verify by diff that every pre-existing key still maps to its pre-existing value). If plan 01 snapped to #4F8CFF instead, tokens need no change — document that in brand-book.md. Consistency check: every hex mentioned in index.html and brand-book.md exists in tokens (or is explicitly noted as a literal-asset ink).</action>
  <verify>
    <automated>sh -c 'git diff -U0 brandbook/tokens.css brandbook/tokens.json | grep "^-" | grep -vE "^---" | grep -cE "#[0-9A-Fa-f]{6}" | grep -qx 0 && ! grep -niE "tournament|phase 16[0-9]|candidate|round-[0-9]|c13|topstitch-geist" brandbook/brand-book.md brandbook/index.html'</automated>
  </verify>
  <done>brand-book.md logo/color sections describe the new system in present tense with numbers matching index.html; README.md inventory accurate; tokens changed additively only (zero removed/modified hex lines in the diff); no process-history vocabulary in brandbook/ docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.html → browser via file:// | Opened directly from disk by users; must make zero network requests and carry no script payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-03 | Information disclosure | brandbook/index.html | mitigate | Zero external requests gate (no http(s) in src/href/url()/@import) — file:// open leaks nothing to any host |
| T-162-04 | Tampering | brandbook/index.html | accept | Static HTML with no JS preferred / no remote script possible given the no-http gate; low-value target |
</threat_model>

<verification>
- `grep -nE '(src|href)[[:space:]]*=[[:space:]]*["'"'"']https?://' brandbook/index.html` → empty; `grep -n 'url(http\|@import' brandbook/index.html` → empty
- index.html contains: identity story, logo system w/ clear-space + min sizes, `<picture>` snippet, misuse gallery (>= 6 inline don'ts), color, typography, voice, application examples
- No new .svg files added for misuse specimens (`git status --porcelain brandbook/ | grep -c 'misuse'` → 0)
- tokens diff shows additions only — no modified or removed lines carrying hex values
- `find brandbook -type f ! -name '.DS_Store' -print0 | xargs -0 du -ck | tail -1` → still <= 300KB
- Staged paths limited to brandbook/ + phase dir; pre-existing lib//examples//test/ worktree changes untouched
</verification>

<success_criteria>
- BOOK-03: brandbook/index.html stands alone from disk as a professional brand book with zero network requests
- BOOK-05: misuse gallery renders the killed antipatterns (chips, icon-beside-text, subtitle-in-primary) plus gradient/distortion/recolor abuses, with HC-4 numbers as the thresholds
- GAP-06/UP-06 closed: <picture> snippet documented; GAP-12/UP-10 closed: visual gallery + numeric thresholds
- brand-book.md / README.md / tokens are settled truth, additive-only token change, operator-surface values untouched
</success_criteria>

<output>
Create `.planning/phases/162-brand-book-v2/162-02-SUMMARY.md` when done. Record: section inventory of index.html, the misuse specimens rendered, the token additions (exact keys/values), and the clear-space/min-size numbers chosen.
</output>
