---
phase: 162-brand-book-v2
plan: 03
type: execute
wave: 3
depends_on: [162-01, 162-02]
files_modified:
  - brandbook/pressure-test.md
  - .planning/phases/162-brand-book-v2/162-BACKLOG-CLOSURE.md
  - .planning/phases/162-brand-book-v2/162-EVIDENCE.md
  - .planning/phases/162-brand-book-v2/evidence/
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [BOOK-04, BOOK-06, BOOK-07]
tags: [brand, pressure-test, backlog, verification, evidence]

must_haves:
  truths:
    - "Every ROUTED BOOK-* row of the 159-DESIGN-BRIEF traceability table is resolved with named evidence or explicitly descoped with a recorded reason"
    - "pressure-test.md is rebuilt on the audit's 15 adversarial dimensions and the rerun scorecard totals >= 79/150 with no dimension below its baseline row"
    - "Phase-dir evidence exists: desktop + mobile direct-open screenshots of index.html, favicon at literal 16px and 32px plus 4x magnification, size-budget output, and the full grep gate suite output"
    - "brandbook/ holds only text/SVG/HTML/CSS/JSON/Markdown files at <= 300KB with no committed binaries"
    - "REQUIREMENTS.md marks BOOK-01..07 complete and the traceability table reflects Phase 162"
  artifacts:
    - path: "brandbook/pressure-test.md"
      provides: "Durable adversarial QA guide: 15 dimensions, testable pass conditions, current honest scores"
      contains: "150"
    - path: ".planning/phases/162-brand-book-v2/162-BACKLOG-CLOSURE.md"
      provides: "BOOK-04 ledger: every ROUTED item -> evidence; every DESCOPED item -> reason"
    - path: ".planning/phases/162-brand-book-v2/162-EVIDENCE.md"
      provides: "Baseline-vs-rerun scorecard comparison + evidence index for UAT"
    - path: ".planning/phases/162-brand-book-v2/evidence/"
      provides: "Screenshots (desktop, mobile, favicon 16/32/4x) and gate outputs — phase dir, never brandbook/"
  key_links:
    - from: "brandbook/pressure-test.md"
      to: "159-AUDIT.md section 3 scorecard"
      via: "same 15 dimension names, row-for-row comparable"
      pattern: "Distinctiveness"
    - from: ".planning/phases/162-brand-book-v2/162-BACKLOG-CLOSURE.md"
      to: "159-DESIGN-BRIEF.md traceability table"
      via: "one row per GAP/UP/AUDIT-S item routed to BOOK-*"
      pattern: "GAP-04|UP-11"
    - from: ".planning/phases/162-brand-book-v2/evidence/"
      to: "brandbook/index.html + favicon.svg"
      via: "Playwright file:// screenshots"
      pattern: "png"
---

<objective>
Close the audit backlog (BOOK-04), enforce the budget/hygiene contract (BOOK-06), and rerun the adversarial pressure test to meet-or-beat the 79/150 baseline with no dimension regressing (BOOK-07). Capture all evidence in the phase dir so the user's gsd-verify-work UAT — the phase's hard human gate — has everything in one place.

Purpose: this is the proof layer. Plans 01-02 built the system; this plan demonstrates, mechanically and visually, that it beats the old one.
Output: rebuilt brandbook/pressure-test.md, 162-BACKLOG-CLOSURE.md, 162-EVIDENCE.md + evidence/ screenshots, updated REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-brand-book-v2/162-CONTEXT.md
@.planning/phases/162-brand-book-v2/162-01-SUMMARY.md
@.planning/phases/162-brand-book-v2/162-02-SUMMARY.md
@.planning/phases/159-brand-audit-and-research/159-AUDIT.md
@.planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md

<interfaces>
<!-- The BOOK-07 baseline — row-for-row, from 159-AUDIT.md §3. The rerun must meet
     or beat EVERY row and the total. Scoring a row below baseline = STOP, that is
     a gap, not a rounding choice. -->

| # | Dimension | Baseline |
|---|---|---|
| 1 | Distinctiveness | 4 |
| 2 | Mark/type integration | 2 |
| 3 | 16px survival | 3 |
| 4 | Monochrome survival | 3 |
| 5 | Dark/light versatility | 5 |
| 6 | Portability (no font dependency) | 2 |
| 7 | Scalability | 4 |
| 8 | Voice | 9 |
| 9 | Palette | 8 |
| 10 | Typography | 8 |
| 11 | Token rigor | 8 |
| 12 | Application coverage | 7 |
| 13 | Misuse guidance | 6 |
| 14 | Consistency | 6 |
| 15 | Craft | 4 |
| | **Total** | **79/150** |

BOOK-04 scope — the ROUTED items targeting BOOK-01/02/03/05/07 (from the DESIGN-BRIEF traceability table; resolve each with evidence):
GAP-04 (16px favicon geometry), GAP-05 (true one-color master), GAP-06 (<picture> dark/light documentation), GAP-07 + UP-09 (examples regenerated with inlined geometry), GAP-08 + UP-07 (light/adaptive icon mark), GAP-10 (chipless favicon), GAP-12 + UP-10 (visual misuse gallery + numeric thresholds), UP-04 (size-specific favicon cut), UP-05 (one-color master, flatten-verified), UP-06 (<picture> snippet committed), UP-11 (pressure-test rebuilt on 15 adversarial dimensions), plus section verdicts AUDIT-S03/S04 (-> BOOK-07 rerun), AUDIT-S05/S06/S13 (-> this ledger itself).
DESCOPED, stays descoped with reason restated: GAP-09 + UP-08 (raster/og:image export pipeline -> SOCIAL-PNG-01, future).
Items routed to LOGO-*/TOUR-* (GAP-01/02/03/11, UP-01/02/03) were closed by Phase 161 — record them as "closed upstream, re-verified on final assets by brand-gate".

Screenshot tooling (Playwright is installed under examples/threadline_phoenix/e2e/node_modules):
  cd examples/threadline_phoenix/e2e
  npx playwright screenshot --viewport-size=1440,900 --full-page "file:///Users/jon/projects/threadline/brandbook/index.html" ../../../.planning/phases/162-brand-book-v2/evidence/index-desktop.png
  npx playwright screenshot --viewport-size=390,844 --full-page "file:///Users/jon/projects/threadline/brandbook/index.html" ../../../.planning/phases/162-brand-book-v2/evidence/index-mobile.png
For the favicon: write a throwaway harness at /tmp/favicon-harness.html (never committed) embedding favicon.svg via <img> at 16x16, 32x32, and 64x64 (= 4x of 16) on side-by-side light (#FFFFFF) and dark (#0B1020) panels, then screenshot it the same way. Screenshots live ONLY in the phase dir (CONTEXT: never brandbook/).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk the traceability table — write 162-BACKLOG-CLOSURE.md</name>
  <files>.planning/phases/162-brand-book-v2/162-BACKLOG-CLOSURE.md</files>
  <action>Create the BOOK-04 ledger: one row per traceability item listed in the interfaces block (every GAP-nn, UP-nn, and AUDIT-Snn touching BOOK-01/02/03/05/07, plus the upstream-closed and descoped items). Columns: Item | Audit finding (one line, copied) | Resolution | Evidence. Resolution is exactly one of RESOLVED (with the file + mechanical check proving it, e.g. "GAP-10: brandbook/favicon.svg contains no rect and no chip — brand-gate HC-2 PASS line quoted"), CLOSED-UPSTREAM (Phase 161 item, re-verified by brand-gate on the final assets), or DESCOPED (reason restated from the DESIGN-BRIEF, consistent with REQUIREMENTS Out of Scope/Future). Evidence must be checkable: a file path plus a quoted gate line, grep output, or screenshot filename — never "done" without a pointer. Zero empty cells (the DESIGN-BRIEF's own rule). If ANY item cannot be marked RESOLVED with real evidence, STOP and fix the asset/doc first (or, if genuinely out of reach, surface it — do not write a soft resolution).</action>
  <verify>
    <automated>sh -c 'f=.planning/phases/162-brand-book-v2/162-BACKLOG-CLOSURE.md; for id in GAP-01 GAP-02 GAP-03 GAP-04 GAP-05 GAP-06 GAP-07 GAP-08 GAP-09 GAP-10 GAP-11 GAP-12 UP-01 UP-02 UP-03 UP-04 UP-05 UP-06 UP-07 UP-08 UP-09 UP-10 UP-11 AUDIT-S03 AUDIT-S04 AUDIT-S05 AUDIT-S06 AUDIT-S08 AUDIT-S13; do grep -q "$id" "$f" || { echo "missing $id"; exit 1; }; done && ! grep -E "\|\s*\|\s*\|" "$f" | grep -q "GAP\|UP-"'</automated>
  </verify>
  <done>Every listed item has a row with a non-empty resolution and pointer-style evidence; descoped rows carry the recorded reason; the ledger is self-sufficient for UAT review.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild pressure-test.md and rerun the 15-dimension scorecard</name>
  <files>brandbook/pressure-test.md, .planning/phases/162-brand-book-v2/162-EVIDENCE.md</files>
  <action>(1) Replace brandbook/pressure-test.md wholesale with the durable adversarial QA guide (UP-11, AUDIT §14 discipline): the 15 dimensions from the baseline table by their exact names, each with a TESTABLE pass condition (the AUDIT §14 conversions: "survives small sizes" = renders identifiably at a literal 16px canvas with strokes >= the HC-4 floors, verifiable from geometry; "portability" = zero text elements / renders correctly in a fontless sandbox; "monochrome" = flatten-to-one-color leaves structure identical; etc.), the mechanical command where one exists (brand-gate invocation path, grep gates, xmllint, size budget), and the CURRENT honest score with a one-line rationale citing evidence. Self-assessment is banned: every score must point at a mechanical output, a screenshot, or a concrete property of the committed files. The doc is settled QA truth — present tense, no tournament/process history, no baseline-comparison narrative inside brandbook/ (the comparison lives in the phase dir). (2) Score honestly. The seven mark-survival dimensions (1-7) should now be strong (pure paths, designed 16px favicon, true mono, explicit light renditions); the five verbal/system rows (8-11, 13) were already 6-9 and are KEEP/ported — they must not regress. If ANY dimension lands below its baseline row, STOP: that is a real gap — fix the underlying asset/doc in place if it is within this phase's files, otherwise report it as a verification gap; never inflate the number. (3) Write .planning/phases/162-brand-book-v2/162-EVIDENCE.md: the row-for-row baseline-vs-rerun comparison table (baseline | rerun | delta), total vs 79/150, and an index of every evidence artifact (gate outputs, grep outputs, screenshot filenames from Task 3 — leave the screenshot index section with explicit TODO markers that Task 3 fills, or write Task 3 first locally and backfill; final committed file has zero TODOs).</action>
  <verify>
    <automated>sh -c 'grep -c "^| [0-9]" brandbook/pressure-test.md | awk "{exit (\$1>=15)?0:1}" && grep -qE "Distinctiveness|Craft" brandbook/pressure-test.md && ! grep -niE "tournament|phase 16[0-9]|candidate|baseline" brandbook/pressure-test.md && grep -q "79" .planning/phases/162-brand-book-v2/162-EVIDENCE.md'</automated>
  </verify>
  <done>pressure-test.md carries all 15 dimensions with testable pass conditions and honest current scores; 162-EVIDENCE.md shows total >= 79/150 and zero rows below baseline; any sub-baseline row was fixed or surfaced, never papered over.</done>
</task>

<task type="auto">
  <name>Task 3: Capture evidence, enforce the budget, settle requirements metadata</name>
  <files>.planning/phases/162-brand-book-v2/evidence/, .planning/phases/162-brand-book-v2/162-EVIDENCE.md, .planning/REQUIREMENTS.md</files>
  <action>(1) Screenshots via the Playwright invocations in the interfaces block: index-desktop.png (1440x900 full-page, file://), index-mobile.png (390x844 full-page), favicon-contexts.png (the /tmp harness: 16px, 32px, 64px=4x on light and dark panels). Eyeball each capture: index.html must look professional at both widths (no broken images, no overflow); the favicon must be identifiable at the literal 16px cell. If anything looks broken, fix the source file (brandbook/ is in this phase's blast radius) and re-capture. (2) Budget + hygiene gates (BOOK-06), outputs saved into evidence/gates.txt: `find brandbook -type f ! -name '.DS_Store' -print0 | xargs -0 du -ck | tail -1` <= 300KB; `git ls-files brandbook/ | grep -vE "[.](svg|html|css|json|md|mjs)$"` empty (no binaries, text-only formats); full grep suite (`grep -rln '<text' brandbook/ --include='*.svg'` empty; tagline grep returns exactly the two sanctioned files; `grep -rln '<rect' brandbook/ --include='*.svg'` empty; index.html zero http src/href); brand-gate rerun exit 0. (3) Backfill the screenshot index in 162-EVIDENCE.md (zero TODOs remain). (4) Metadata: in .planning/REQUIREMENTS.md check the BOOK-01..07 boxes and set their traceability rows to Complete; confirm .planning/ROADMAP.md Phase 162 plan checkboxes reflect completed plans and STATE.md points at Phase 162 (fix only stale phase-162 entries — do not touch other lanes' content). (5) Git hygiene: stage only brandbook/pressure-test.md, the phase dir, and the three planning metadata files by explicit path; the pre-existing uncommitted lib//examples//test/ worktree changes must remain unstaged and unmodified. Note in the SUMMARY: the phase closes with the user's gsd-verify-work UAT (user opens index.html and approves) — that human gate happens after this plan, not inside it.</action>
  <verify>
    <automated>sh -c 'test -s .planning/phases/162-brand-book-v2/evidence/index-desktop.png && test -s .planning/phases/162-brand-book-v2/evidence/index-mobile.png && test -s .planning/phases/162-brand-book-v2/evidence/favicon-contexts.png && ! grep -q "TODO" .planning/phases/162-brand-book-v2/162-EVIDENCE.md && grep -c "\[x\] \*\*BOOK-0[1-7]\*\*" .planning/REQUIREMENTS.md | grep -qx 7 && node .planning/phases/162-brand-book-v2/tools/brand-gate.mjs brandbook && find brandbook -type f ! -name ".DS_Store" -print0 | xargs -0 du -ck | tail -1 | awk "\$1<=300{exit 0}{exit 1}"'</automated>
  </verify>
  <done>All three screenshots exist and look right; gates.txt records the full green suite incl. budget <= 300KB; 162-EVIDENCE.md is complete; BOOK-01..07 checked in REQUIREMENTS.md; no out-of-lane files staged; SUMMARY flags the pending gsd-verify-work UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file:// → headless browser | Playwright renders local HTML for evidence; no remote content reachable (plan 02's no-http gate already holds) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-05 | Repudiation | pressure-test scorecard | mitigate | Every score cites mechanical evidence (gate line, grep output, screenshot); baseline comparison committed in phase dir for UAT |
| T-162-06 | Tampering | evidence/ PNGs in .planning | accept | Planning-dir evidence, not shipped artifacts; brandbook/ itself stays binary-free (gated) |
</threat_model>

<verification>
- 162-BACKLOG-CLOSURE.md: every ROUTED BOOK-* traceability item RESOLVED with pointer evidence or DESCOPED with reason; zero empty cells
- brandbook/pressure-test.md: 15 dimensions, testable pass conditions, no process history
- 162-EVIDENCE.md: total >= 79/150, no dimension below its baseline row, complete evidence index
- evidence/: index-desktop.png, index-mobile.png, favicon-contexts.png (16/32/4x, light+dark), gates.txt
- `find brandbook -type f ! -name '.DS_Store' -print0 | xargs -0 du -ck | tail -1` <= 300KB; `git ls-files brandbook/` shows only svg/html/css/json/md/mjs
- brand-gate exit 0 on final state; REQUIREMENTS.md BOOK rows complete
- Pre-existing lib//examples//test/ worktree changes untouched and unstaged
</verification>

<success_criteria>
- BOOK-04: backlog ledger complete — every item resolved-with-evidence, closed-upstream-and-reverified, or descoped-with-reason
- BOOK-06: budget, format, and no-binaries gates green with outputs captured
- BOOK-07: rerun scorecard meets or beats 79/150 row-for-row, honestly scored
- Phase ready for the gsd-verify-work UAT human gate (user opens index.html and approves)
</success_criteria>

<output>
Create `.planning/phases/162-brand-book-v2/162-03-SUMMARY.md` when done. Record: final scorecard total + per-row deltas, any dimension that required a fix mid-plan, and the explicit note that gsd-verify-work UAT is the remaining phase gate.
</output>
