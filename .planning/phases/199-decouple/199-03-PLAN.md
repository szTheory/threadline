---
phase: 199-decouple
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/critic.measure.ex
  - lib/mix/tasks/critic.synth.ex
  - lib/threadline/critic_trust/measure.ex
  - test/threadline/operator_surface/critic_trust_test.exs
autonomous: true
requirements: [DECOUPLE-01]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
must_haves:
  truths: ["Mix readers use project-file anchors and writers are contained, root-separated, atomic, and review-explicit."]
  artifacts:
    - {path: lib/mix/tasks/critic.measure.ex, provides: "Safe repository-only measurement edge"}
    - {path: lib/mix/tasks/critic.synth.ex, provides: "Safe named regeneration edge"}
  key_links:
    - {from: lib/mix/tasks/critic.measure.ex, to: lib/threadline/critic_trust/measure.ex, via: "validated decoded data"}
  prohibitions: ["No cwd/env root, immutable/output alias, prefix containment, direct overwrite, or public resolver API."]
---
<objective>
Harden Mix evidence edges per D-03 through D-12.
Purpose: Maintainers get deterministic flags, safe writes, and actionable failures.
Output: Private anchored adapters and crash-safe canonical writes.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-RESEARCH.md
@.planning/phases/199-decouple/199-PATTERNS.md
@lib/mix/tasks/critic.measure.ex
@lib/mix/tasks/critic.synth.ex
@test/threadline/operator_surface/critic_trust_test.exs
</context>
## Artifacts this phase produces
- Private `--fixture-root`/`--output-root` contracts and sibling-temp replacement.
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Run one anchored measurement through validated roots</name>
  <reversibility rating="reversible">Private task flags can change without runtime API migration.</reversibility>
  <files>lib/mix/tasks/critic.measure.ex, lib/threadline/critic_trust/measure.ex, test/threadline/operator_surface/critic_trust_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-CONTEXT.md — D-03 through D-12
    - .planning/phases/199-decouple/199-PATTERNS.md — maintainer writer and containment analogs
    - lib/mix/tasks/critic.measure.ex — current parser/read/write path
  </read_first>
  <behavior>
    - Flags override a `Mix.Project.project_file/0` anchored default.
    - Missing/malformed input and traversal/symlink/alias targets fail before IO.
  </behavior>
  <action>Add private OptionParser keys and deterministic precedence per D-09/D-10. Validate normalized relative containment, canonical existing parents, symlinks, and immutable/generated separation per D-06/D-11. Keep `Measure` pure and make every failure satisfy D-12.</action>
  <acceptance_criteria>
    - Nested-directory invocation and explicit override succeed.
    - Traversal, absolute escape, prefix confusion, symlink escape, and alias controls fail.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/operator_surface/critic_trust_test.exs -x</automated><fails_when>non-zero exit, zero tests, or containment control passes</fails_when></verify>
  <done>One measurement path is anchored, explicit, and contained end to end.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Make both canonical writers atomic and review-explicit</name>
  <reversibility rating="reversible">Writer implementation is private and byte output remains review-controlled.</reversibility>
  <files>lib/mix/tasks/critic.measure.ex, lib/mix/tasks/critic.synth.ex, test/threadline/operator_surface/critic_trust_test.exs</files>
  <read_first>
    - lib/mix/tasks/critic.synth.ex — current direct write
    - .planning/phases/199-decouple/199-PATTERNS.md — sibling-temp/sync/rename composition
  </read_first>
  <behavior>
    - Unique same-directory temp is exclusively created, synced, closed, renamed, and cleaned on failure.
    - Success prints exact `git diff -- &lt;target&gt;`.
  </behavior>
  <action>Implement D-04 for both tasks with exclusive sibling temps, `:file.sync/1`, close-before-rename, and after-cleanup. Validate roots before creation and keep immutable/generated roots disjoint. Add forced-interruption and atomic replacement controls; ordinary tests remain read-only.</action>
  <acceptance_criteria>
    - No direct canonical File.write remains.
    - Failure leaves original intact and no temp; success prints exact review command.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/operator_surface/critic_trust_test.exs -x &amp;&amp; mix verify.format</automated><fails_when>non-zero exit, temp leak, byte drift, or formatter drift</fails_when></verify>
  <done>Named regeneration is contained, crash-safe, and explicitly reviewed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CLI path → filesystem | Maintainer-controlled paths select reads/writes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-199-05 | Tampering | path containment | high | mitigate | Canonical-parent and relative-path controls including symlinks. |
| T-199-06 | Tampering | writer | high | mitigate | Exclusive sibling temp, sync, rename, cleanup, mutation tests. |
</threat_model>
<verification>Focused tests and formatter pass; Plan 199-08 flips defaults atomically.</verification>
<success_criteria>Mix evidence IO is deterministic, contained, atomic, and ergonomic.</success_criteria>
<output>Create `.planning/phases/199-decouple/199-03-SUMMARY.md` when done.</output>
