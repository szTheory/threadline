---
phase: 199-decouple
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/operator_surface/operator_surface_fixture_contract_test.exs
autonomous: true
requirements: [DECOUPLE-02]
estimate: {tokens: 16000, raw_tokens: 16000, tasks: 2, confidence: low}
must_haves:
  truths: ["Fixture manifest logic hashes tracked corpus entries only and ignores mutable generated scores."]
  artifacts:
    - {path: test/threadline/operator_surface/operator_surface_fixture_contract_test.exs, provides: "Synthetic-first tracked-corpus contract"}
  key_links:
    - {from: test/threadline/operator_surface/operator_surface_fixture_contract_test.exs, to: git ls-files, via: "tracked entry derivation"}
  prohibitions: ["No whole-tree digest, ignored output in manifest, hardcoded 427 count, or live destination assertion before migration."]
---
<objective>
Define the corpus integrity contract before the move per D-01 through D-06.
Purpose: The atomic migration has an executable, non-vacuous tracked-data oracle.
Output: Synthetic-first manifest/ownership tests with mutation teeth.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-RESEARCH.md
@.planning/phases/199-decouple/199-PATTERNS.md
</context>
## Artifacts this phase produces
- Fixture contract core that Plan 199-08 binds to the live repository.
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove a tracked-only manifest over a synthetic corpus</name>
  <reversibility rating="reversible">The contract is isolated test code; migration remains untouched.</reversibility>
  <files>test/threadline/operator_surface/operator_surface_fixture_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-CONTEXT.md — D-01, D-02, D-03
    - .planning/phases/199-decouple/199-PATTERNS.md — non-vacuity, manifest, and positive-control analogs
  </read_first>
  <behavior>
    - Manifest entries are derived from an explicit tracked set matching `git ls-files`, sorted by relative path, and SHA-256 each byte stream.
    - Skeleton is included; ignored generated critic scores are excluded.
  </behavior>
  <action>Build pure test helpers against a temporary Git repository containing representative ledger, scorecard, golden, refute, tracked critic-scores skeleton, and ignored generated score. Derive the manifest from `git ls-files --` or an exactly equivalent injected tracked set, never recursive filesystem enumeration. Assert path and byte digest shape without pinning the corpus count.</action>
  <acceptance_criteria>
    - Synthetic manifest includes every tracked evidence entry and only those entries.
    - Manifest order and byte digests are deterministic.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/operator_surface/operator_surface_fixture_contract_test.exs -x</automated><fails_when>non-zero exit, zero tests, or ignored output is hashed</fails_when></verify>
  <done>A real Git-backed synthetic corpus proves tracked-only manifest semantics.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add positive controls for bytes, joins, and ignored output independence</name>
  <reversibility rating="reversible">Mutation controls affect temporary data only.</reversibility>
  <files>test/threadline/operator_surface/operator_surface_fixture_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-RESEARCH.md — fixture failure modes and required joins
  </read_first>
  <behavior>
    - Tracked byte mutation, missing required root, malformed structure, or broken cross-reference changes/fails the contract.
    - Adding/changing/removing an ignored generated score leaves the tracked manifest byte-identical.
  </behavior>
  <action>Add positive controls for tracked byte drift and cross-dataset joins. Crucially, snapshot the manifest, create and mutate ignored generated scores, recompute, and assert exact equality; also prove promoting that file to the tracked set changes the manifest. Leave live-root activation to the indivisible Plan 199-08 migration.</action>
  <acceptance_criteria>
    - Ignored mutable output cannot perturb the manifest.
    - A tracked mutation and broken join are detected.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/operator_surface/operator_surface_fixture_contract_test.exs -x</automated><fails_when>non-zero exit, zero tests, missing teeth, or ignored-score digest drift</fails_when></verify>
  <done>The contract distinguishes immutable tracked evidence from mutable ignored outputs.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Git index → integrity manifest | Tracked membership defines review authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-199-13 | Tampering | manifest membership | high | mitigate | Git-index derivation and ignored-output positive control. |
| T-199-14 | Repudiation | byte preservation | high | mitigate | Deterministic per-path SHA-256 and mutation teeth. |
</threat_model>
<verification>Run the synthetic contract; no live destination is required yet.</verification>
<success_criteria>Manifest semantics are correct before any corpus path changes.</success_criteria>
<output>Create `.planning/phases/199-decouple/199-07-SUMMARY.md` when done.</output>
