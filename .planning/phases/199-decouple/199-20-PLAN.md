---
phase: 199-decouple
plan: 20
type: execute
wave: 6
depends_on: [199-16, 199-17, 199-18, 199-19]
files_modified:
  - .dialyzer_ignore.exs
  - test/threadline/dialyzer_ignore_contract_test.exs
  - test/fixtures/dialyzer/README.md
autonomous: true
requirements: [DECOUPLE-07, DECOUPLE-08]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The five source-owned fixtures form an exact, disjoint partition of all 40 sealed warnings and all 22 sealed warning-origin files."
    - "Every fixed warning is absent; every irreducible warning has one exact `{file, warning_description}` entry, one adjacent specific rationale, and a removal trigger."
    - "The ignore contract reads no planning artifact, rejects broad/duplicate/malformed/unused filters, and commits a warning ceiling equal to the approved residue count that policy permits only to decrease."
    - "A full local `mix dialyzer` run is green with all nine optional applications still present in the PLT configuration and strict warning classes still enabled."
  artifacts:
    - path: .dialyzer_ignore.exs
      provides: "Exact individually justified irreducible residue only"
    - path: test/threadline/dialyzer_ignore_contract_test.exs
      provides: "Planning-independent partition, AST shape, comment, ceiling, positive-control, and unused-filter enforcement"
    - path: test/fixtures/dialyzer/README.md
      provides: "Schema, provenance, update, and ceiling-decrease policy for the five slice fixtures"
  key_links:
    - from: test/fixtures/dialyzer/*.json
      to: test/threadline/dialyzer_ignore_contract_test.exs
      via: "exact disjoint union of W01-W40 and the 22 sealed origins"
    - from: .dialyzer_ignore.exs
      to: mix dialyzer
      via: "Dialyxir exact filters plus `--list-unused-filters`"
    - from: mix.exs
      to: mix dialyzer
      via: "D-22 through D-25 configuration: full PLT app set and strict warning classes"
  assumptions:
    - statement: "DECOUPLE-07 adjacency probe remains unresolved by specification; this plan treats exact warning-to-filter adjacency as an explicit unverified assumption and enforces the strongest deterministic join available from the sealed IDs and AST/comments."
      status: unverified
      flagged: true
    - statement: "DECOUPLE-07 empty-input probe remains unresolved by specification; this plan explicitly tests zero-residue and empty-ignore behavior without claiming product-level coverage."
      status: unverified
      flagged: true
    - statement: "DECOUPLE-07 ordering probe remains unresolved by specification; this plan requires deterministic fixture and filter ordering as an explicit unverified assumption."
      status: unverified
      flagged: true
    - statement: "DECOUPLE-08 concurrency probe remains unresolved by specification; this plan assumes CI serialization/caching does not alter analyzer semantics and leaves that assumption flagged for execution evidence."
      status: unverified
      flagged: true
    - statement: "The other 11 unresolved generic probe rows belong to completed DECOUPLE-01 through DECOUPLE-06; this recovery tail neither resolves nor reopens them."
      status: unverified
      flagged: true
  prohibitions:
    - statement: "Remediation must not relabel a real warning as architectural residue or change capture, query, authentication, or operator semantics merely to obtain a green analyzer."
      status: unverified
      flagged: true
    - statement: "The strict ignore contract must not turn broad or untriaged suppressions into accepted type safety."
      status: unverified
      flagged: true
    - statement: "CI evidence must not present estimated or mixed timing, or a partial/advisory analyzer run, as the measured full blocking gate."
      status: unverified
      flagged: true
---

<objective>
Consolidate the five bounded remediation slices into one exact source-owned ratchet and prove the full analyzer green before CI wiring.

Purpose: Complete local enforcement of DECOUPLE-07/08 while honoring D-22 through D-27: full PLT scope, strict warning classes, real-defect fixes first, exact residue only, and a count ceiling that never increases.
Output: The final exact ignore file, a planning-independent contract test, fixture maintenance documentation, and green full-Dialyzer evidence for Plan 199-14.

Authority: this plan may edit no warning-origin source file. Any surviving fixed warning, new warning, origin drift, or need for an unsealed source edit is a designed halt requiring another bounded remediation plan.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-RESEARCH.md
@.planning/phases/199-decouple/199-PATTERNS.md
@.planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
@.planning/phases/199-decouple/199-13-SUMMARY.md
@.planning/phases/199-decouple/199-16-SUMMARY.md
@.planning/phases/199-decouple/199-17-SUMMARY.md
@.planning/phases/199-decouple/199-18-SUMMARY.md
@.planning/phases/199-decouple/199-19-SUMMARY.md
@mix.exs
@.dialyzer_ignore.exs
</context>

## Artifacts this phase produces

- `.dialyzer_ignore.exs`: zero or more exact two-field filters, each individually justified; empty is preferred when all defects are fixed.
- `test/threadline/dialyzer_ignore_contract_test.exs`: final source-only ratchet and exact 40-warning/22-origin partition proof.
- `test/fixtures/dialyzer/README.md`: executor-facing fixture schema, update rules, evidence requirements, and ceiling policy.

<dependencies>
Needs: All four Wave 5 plans complete, yielding five fixtures total when combined with Plan 199-15.
Creates: The green local analyzer and strict ratchet prerequisite consumed by Plan 199-14.
Parallel safety: Runs alone in Wave 6 because it reads every slice result and is the sole writer of the shared ignore contract.
</dependencies>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build the source-only exact partition and ignore ratchet</name>
  <files>.dialyzer_ignore.exs, test/threadline/dialyzer_ignore_contract_test.exs, test/fixtures/dialyzer/README.md</files>
  <read_first>
    - .planning/phases/199-decouple/199-CONTEXT.md
    - .planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
    - test/threadline/dialyzer_ignore_contract_test.exs
    - test/threadline/dialyzer_slice_contract_test.exs
    - test/fixtures/dialyzer/critic-tooling.json
    - test/fixtures/dialyzer/query-storage.json
    - test/fixtures/dialyzer/export-investigation.json
    - test/fixtures/dialyzer/operator-boundaries.json
    - test/fixtures/dialyzer/operator-liveviews.json
    - .dialyzer_ignore.exs
  </read_first>
  <behavior>
    - RED: The old contract fails because it depends on `.planning`, enforces the obsolete 14-origin edit cap, and does not join all five source fixtures to exact approved residue.
    - GREEN: The five fixtures contain each of W01-W40 once, cover each of the 22 sealed origins once, and have no cross-slice origin overlap.
    - GREEN: Each irreducible fixture record joins one-to-one to one literal `{file, warning_description}` tuple and one adjacent specific comment; fixed records join to no filter.
    - Contract: Wildcards, regexes, message-only/file-only entries, helper calls, duplicate tuples, duplicate comments, missing rationale/removal trigger, count above the committed ceiling, and unused filters fail.
    - Positive controls: Synthetic broad, duplicate, uncommented, ceiling-exceeding, fixed-warning, unknown-warning, missing-origin, and unused-filter mutations are each rejected.
    - Empty case: If every warning is fixed, an empty list and ceiling zero pass while all residue mutations fail.
  </behavior>
  <action>Replace the planning-dependent contract with a source-only test that reads the five JSON fixtures and parses `.dialyzer_ignore.exs` with `Code.string_to_quoted_with_comments/2`. First add the union/join and mutation controls so the current incomplete contract is RED. Enforce the exact sealed set W01-W40, exact disjoint 22-origin union, fixture schema/provenance fields, deterministic ordering, and disposition completeness. Generate no filters for fixed warnings. For each proven irreducible warning only, add exactly one literal `{file, warning_description}` tuple with an immediately adjacent specific comment naming its rationale, upstream/tool version where relevant, and executable removal trigger. Set the committed ceiling to the actual irreducible execution count after triage, assert exact count and `count &lt;= ceiling`, document that future changes may lower but never raise it, and keep zero when no residue remains. Retain Dialyxir's unused-filter check and positive controls. Remove the obsolete 14-origin cap and all runtime reads of `.planning`; historical authority now comes solely from the sealed source fixtures. This implements D-23, D-26, and D-27.</action>
  <verify>
    <automated>mix test test/threadline/dialyzer_slice_contract_test.exs test/threadline/dialyzer_ignore_contract_test.exs --max-failures 1 &amp;&amp; mix dialyzer --no-check --list-unused-filters</automated>
    <fails_when>The fixture union is not exactly 40 warnings/22 origins, any warning is duplicated or silently dropped, a fixed warning has a filter, an irreducible warning lacks one exact commented filter, a broad/malformed/unused filter passes, the ceiling exceeds approved residue, or any test reads `.planning` at runtime.</fails_when>
  </verify>
  <acceptance_criteria>
    - The contract deterministically proves the exact disjoint partition 3 + 10 + 15 + 7 + 5 = 40 warnings across 22 origins.
    - Every filter, if any, is an exact tuple with its own adjacent rationale/removal comment and exact fixture join.
    - The ceiling equals the approved irreducible count and README states it may only decrease.
    - All positive controls fail for the intended reason and the real contract passes without planning files.
  </acceptance_criteria>
  <done>The final source-only ignore ratchet exactly accounts for all sealed evidence and accepts only irreducible, individually justified residue.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the complete strict analyzer green for CI handoff</name>
  <files>.dialyzer_ignore.exs, test/threadline/dialyzer_ignore_contract_test.exs, test/fixtures/dialyzer/README.md</files>
  <read_first>
    - mix.exs
    - .dialyzer_ignore.exs
    - test/threadline/dialyzer_ignore_contract_test.exs
    - test/fixtures/dialyzer/README.md
    - .planning/phases/199-decouple/199-13-SUMMARY.md
  </read_first>
  <precondition>All five `bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/{slice}.json` invocations succeed for `critic-tooling`, `query-storage`, `export-investigation`, `operator-boundaries`, and `operator-liveviews`.</precondition>
  <action>Run one full local analyzer using the committed configuration and final ignore file. Confirm per D-22/D-24 that Dialyxir remains dev/test-only with `runtime: false` and that `plt_add_apps` still includes `:mix`, `:ex_unit`, and all nine optional applications: `:phoenix`, `:phoenix_live_view`, `:phoenix_html`, `:phoenix_pubsub`, `:oban`, `:ex_aws`, `:ex_aws_s3`, `:hackney`, and `:sweet_xml`. Confirm per D-25 that `:unknown`, `:unmatched_returns`, and `:extra_return` remain enabled. Do not reinterpret any new or surviving warning: halt with its exact origin/raw output because this plan has zero origin-edit authority. Record command, toolchain, PLT identity, elapsed time, peak memory if locally available, final warning/filter counts, and output hash in the summary; this is local proof, not the CI cold-build measurement reserved for D-30 in Plan 199-14.</action>
  <verify>
    <automated>mix test --max-failures 1 &amp;&amp; mix dialyzer --list-unused-filters</automated>
    <fails_when>The suite fails, full Dialyzer exits nonzero, any unused filter exists, the PLT app set differs from the required eleven total additions, a strict warning class is disabled, or execution reveals an unsealed warning.</fails_when>
  </verify>
  <acceptance_criteria>
    - The full test suite and full strict Dialyzer run exit zero from the source tree.
    - Exactly all nine optional applications plus Mix and ExUnit remain in the PLT configuration.
    - The summary separates local warm/available-PLT evidence from the later true CI cold-build measurement.
    - No warning-origin source file changes in this plan.
  </acceptance_criteria>
  <done>Plan 199-14 receives a green, strict, planning-independent Dialyzer ratchet with all 40 sealed warnings accounted for.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slice fixtures → global ignore policy | Independently produced evidence determines which warnings may be suppressed. |
| Elixir AST/comments → contract test | Executable ignore configuration and human rationale must remain in one-to-one correspondence. |
| Full analyzer → CI handoff | Local output is evidence for gate readiness but not a substitute for CI cold-cache measurement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-199-20-01 | Tampering | ignore filters | high | mitigate | Parse literal AST/comments, require exact two-field tuples, reject broad forms/duplicates, and join every filter to one irreducible record. |
| T-199-20-02 | Repudiation | sealed partition | high | mitigate | Assert exact W01-W40 and 22-origin disjoint union with original and post-analysis hashes. |
| T-199-20-03 | Elevation of privilege | warning ceiling | high | mitigate | Commit the exact residue ceiling, fail above it, forbid increases in the maintenance contract, and retain positive controls. |
| T-199-20-04 | Information disclosure | analyzer evidence | low | accept | Store only paths, warning descriptions, toolchain, timing, and hashes; no runtime audit payloads or secrets. |
| T-199-20-05 | Denial of service | full PLT analysis | medium | transfer | Plan 199-14 measures the true cold cost and sets current-lane timeout/headroom using observed CI data per D-30. |
</threat_model>

## Multi-source coverage audit

| Source | Item | Coverage | Status |
|--------|------|----------|--------|
| GOAL | Self-contained source-tree tests and CI | Completed Plans 199-01 through 199-12 plus Plan 199-14's planning-independent certification | COVERED |
| GOAL | Clean checkout/dependency fetch | Completed Plans 199-01 through 199-12 | COVERED |
| GOAL | Measured ratcheting Dialyzer gate before refactors | Halted evidence in 199-13, bounded remediation in 199-15 through 199-20, CI measurement/certification in 199-14 | COVERED |
| REQ | DECOUPLE-01 through DECOUPLE-06 | Immutable completed Plans 199-01 through 199-12 | COVERED |
| REQ | DECOUPLE-07 | 199-13 sealed evidence; 199-15 through 199-20 exact triage and source-only ratchet; 199-14 final certification | COVERED |
| REQ | DECOUPLE-08 | 199-15 through 199-20 warning elimination/ratchet; 199-14 blocking aggregate and CI measurement | COVERED |
| RESEARCH | Dialyxir stack, full optional-app PLT, strict ignores, cache, measurement | 199-13 implements config; 199-15 through 199-20 remediate/ratchet; 199-14 owns cache and measurements | COVERED |
| CONTEXT | D-01 through D-21 | Immutable completed Plans 199-01 through 199-12 | COVERED |
| CONTEXT | D-22 through D-25 | 199-13 implemented; this plan re-verifies; 199-14 invokes | COVERED |
| CONTEXT | D-23, D-26, D-27 | 199-15 through 199-20 bounded fixes, exact residue, executable ceiling | COVERED |
| CONTEXT | D-28 through D-30 | 199-14 current-lane gate, cache key/restore, and true cold/hit measurement | COVERED |
| CONTEXT | Deferred public vocabulary/architecture work | Explicitly Phase 200/203 and excluded from Phase 199 | EXCLUDED |

<verification>
Run every slice verifier, the two contract tests, the full test suite, and full `mix dialyzer --list-unused-filters`. Inspect the changed-file list and halt if it contains a warning-origin source file. Confirm the multi-source audit has no missing row and the four remaining-requirement edge cases plus eleven completed-requirement rows remain explicitly flagged rather than fabricated as covered.
</verification>

<success_criteria>
- All 40 sealed warnings and all 22 origins are accounted for exactly once.
- Real defects are fixed; only proven irreducible residue is exact, individually commented, and ceiling-bound.
- Full local Dialyzer is green with all optional applications and strict classes before CI wiring begins.
- The enforcement path has no runtime dependency on `.planning`.
</success_criteria>

<output>
Create `.planning/phases/199-decouple/199-20-SUMMARY.md` when done.
</output>
