---
phase: 199-decouple
plan: "11"
type: execute
wave: 2
depends_on: [199-10]
files_modified:
  - bin/safe-temp-tree
  - bin/verify-clean-checkout
  - test/threadline/clean_checkout_contract_test.exs
autonomous: true
requirements: [DECOUPLE-05]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths: ["A committed-HEAD disposable clone stays exactly clean after dependency fetch and generated-output probes, with hardened cleanup that rejects every registered Git worktree root."]
  artifacts:
    - {path: bin/safe-temp-tree, provides: "Single hardened temp-child cleanup primitive"}
    - {path: bin/verify-clean-checkout, provides: "Committed-state clean-checkout proof"}
  key_links:
    - {from: bin/verify-clean-checkout, to: bin/safe-temp-tree, via: "sourced cleanup contract"}
  prohibitions: ["No current-worktree clone, uncommitted-policy assumption, recursive cleanup without canonical/lstat checks, symlink target, or broad ignore laundering."]
---
<objective>
Prove clean dependency fetch from committed state with one reusable hardened cleanup contract per D-19.
Purpose: Contributors can trust a fresh checkout and verification scripts cannot delete caller data.
Output: Safe temp primitive, clone verifier, and adversarial controls.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-RESEARCH.md
@.planning/phases/199-decouple/199-PATTERNS.md
@test/threadline/clean_checkout_contract_test.exs
</context>
## Artifacts this phase produces
- Private shared cleanup primitive and committed-HEAD clean checkout verifier.
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove hardened temp-child cleanup against hostile mutation</name>
  <reversibility rating="reversible">Private shell contract has no persisted state.</reversibility>
  <files>bin/safe-temp-tree, test/threadline/clean_checkout_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-RESEARCH.md — cleanup/trap threat boundary
    - .planning/phases/199-decouple/199-PATTERNS.md — disposable clone analog
  </read_first>
  <behavior>
    - Immediately before removal, canonical trusted temp parent and literal child are revalidated with realpath and lstat.
    - Empty, root, workspace/repository, outside-parent, symlink, and replaced-root targets are rejected.
    - A cleanup target whose canonical path equals any main or linked worktree root reported by `git worktree list --porcelain` is rejected.
  </behavior>
  <action>Create a sourceable private primitive that receives explicit trusted-parent and literal-child paths. Directly before quoted recursive removal, require non-empty absolute canonical parent, child strictly one descendant under it, lstat non-symlink identity matching creation, target not `/`, repository/workspace, parent, or outside parent, and no replaced root. Parse every `worktree ` record from `git worktree list --porcelain`, canonicalize each registered main or linked worktree root and the proposed cleanup target, and reject exact canonical equality with any registered root; do not limit the comparison to the current repository root. Refuse cleanup on any failed predicate. Add controls for symlink replacement and attempts to target the caller worktree/root/outside paths. Add an adversarial control that registers a linked worktree as the literal child beneath a trusted temp parent, plants a sentinel inside it, invokes cleanup, and proves rejection plus sentinel and worktree registration survival; unregister and remove that test-owned worktree only through explicit test teardown.</action>
  <acceptance_criteria>
    - Valid literal child is removed.
    - Every hostile target is rejected and sentinel caller files survive.
    - A canonical target equal to any porcelain-listed main or linked worktree root is rejected, its sentinel is byte-identical, and the worktree remains registered.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/clean_checkout_contract_test.exs -x</automated><fails_when>non-zero exit, zero tests, any sentinel changes, a registered worktree target is removed or mutated, or the linked-worktree positive control does not remain listed after rejected cleanup</fails_when></verify>
  <done>The one cleanup primitive is safe under path and post-creation mutation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Verify committed HEAD stays clean after dependencies and producers</name>
  <reversibility rating="reversible">Verifier operates only on disposable clones of committed history.</reversibility>
  <files>bin/verify-clean-checkout, test/threadline/clean_checkout_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-CONTEXT.md — D-17 through D-19
    - bin/safe-temp-tree — sole cleanup implementation from Task 1
  </read_first>
  <behavior>
    - `git clone --no-local` of committed HEAD receives `mix deps.get --check-locked` and exact clean status assertion.
    - Crash/tar/PLT/e2e generated probes stay ignored while snapshots/source stay trackable.
  </behavior>
  <action>Source the Task-1 primitive rather than duplicate cleanup. Clone the exact committed HEAD into its registered child; explicitly exclude current index/untracked state. Run dependency fetch and generated probes, require empty `git status --porcelain=v1 --untracked-files=all`, then prove reviewed snapshot/source controls appear when created. Trap every exit through safe cleanup and add forced-command-failure/restoration controls. The dependency on committed Plan 199-10 is load-bearing.</action>
  <acceptance_criteria>
    - Clone reports exact source SHA and empty status after deps/probes.
    - Forced failure cleans only clone child and preserves caller worktree.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/clean_checkout_contract_test.exs -x &amp;&amp; bin/verify-clean-checkout</automated><fails_when>non-zero exit, non-empty clone status, wrong SHA, or unsafe cleanup</fails_when></verify>
  <done>DECOUPLE-05 is proven against committed policy, not uncommitted or operator-local state.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Temp paths → recursive cleanup | Mutable filesystem names could redirect deletion. |
| Committed HEAD → clone | Only committed shared policy is authoritative. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-199-21 | Tampering | cleanup target | high | mitigate | Immediate realpath/lstat containment, replacement controls, and canonical rejection of every root from `git worktree list --porcelain`. |
| T-199-22 | Repudiation | clean proof | high | mitigate | Exact SHA and porcelain status from no-local committed clone. |
</threat_model>
<verification>Run adversarial contract then the real committed clone verifier.</verification>
<success_criteria>Fresh dependencies and producer outputs leave committed checkout policy clean, with safe cleanup.</success_criteria>
<output>Create `.planning/phases/199-decouple/199-11-SUMMARY.md` when done.</output>
