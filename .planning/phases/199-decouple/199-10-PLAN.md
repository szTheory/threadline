---
phase: 199-decouple
plan: "10"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .formatter.exs
  - bench/.formatter.exs
  - examples/threadline_phoenix/.formatter.exs
  - test/threadline/formatter_topology_contract_test.exs
  - test/threadline/clean_checkout_contract_test.exs
autonomous: true
requirements: [DECOUPLE-05, DECOUPLE-06]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths: ["Generated ignores are exact and each Elixir file has exactly one formatter owner."]
  artifacts:
    - {path: test/threadline/formatter_topology_contract_test.exs, provides: "Derived exactly-one formatter ownership"}
    - {path: test/threadline/clean_checkout_contract_test.exs, provides: "Ignore-policy mutation contract"}
  key_links:
    - {from: .formatter.exs, to: bench/.formatter.exs, via: "subdirectories ownership"}
  prohibitions: ["No broad extension/directory ignore, tool-version scratch ignore, snapshot ignore, or overlapping formatter input."]
---
<objective>
Define narrow generated-output policy and child-aware formatting per D-17 through D-20.
Purpose: Shared repository policy stays predictable without hiding reviewed source.
Output: Exact ignores and executable formatter/ignore contracts.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-RESEARCH.md
@.planning/phases/199-decouple/199-PATTERNS.md
@.gitignore
@.formatter.exs
</context>
## Artifacts this phase produces
- Exact producer ignores and child-aware formatter topology tests.
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Encode producer-owned ignores with trackability controls</name>
  <reversibility rating="reversible">Anchored ignore rules can be narrowed without data migration.</reversibility>
  <files>.gitignore, test/threadline/clean_checkout_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-CONTEXT.md — D-17 through D-19
    - .planning/phases/199-decouple/199-PATTERNS.md — exact ignore destinations
  </read_first>
  <behavior>
    - Crash, tarball, PLT, Playwright report/artifact, and critic-score outputs are ignored at exact producer roots.
    - Reviewed snapshots, source, `.tool-versions`, and unrelated similarly named paths stay trackable.
  </behavior>
  <action>Add anchored producer rules and `.gitkeep` exception per D-17/D-18. Build `git check-ignore -v` positive and negative controls, including prefix-confusion paths. Do not tune policy to current untracked files.</action>
  <acceptance_criteria>
    - Every generated probe maps to its intended rule.
    - Every reviewed/source/operator-local probe remains trackable.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/clean_checkout_contract_test.exs -x</automated><fails_when>non-zero exit, zero tests, or trackability control fails</fails_when></verify>
  <done>Shared ignores cover only universal producer output.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Give every Elixir source exactly one formatter owner</name>
  <reversibility rating="reversible">Formatter globs are configuration-only and tested bidirectionally.</reversibility>
  <files>.formatter.exs, bench/.formatter.exs, examples/threadline_phoenix/.formatter.exs, test/threadline/formatter_topology_contract_test.exs</files>
  <read_first>
    - .planning/phases/199-decouple/199-PATTERNS.md — exact three formatter analogs
  </read_first>
  <behavior>
    - Root delegates bench/example and owns scripts; child configs retain imports/migrations and extend their own scripts.
    - Representative planned files have exactly one owner; injected overlap fails.
  </behavior>
  <action>Add root subdirectories and scripts inputs per D-20, preserve child rules, and derive ownership from configs rather than a parallel hardcoded roster. Add uncovered and overlap positive controls.</action>
  <acceptance_criteria>
    - Root, bench, and example paths have exactly one owner.
    - `mix format --check-formatted` traverses all configured subprojects.
  </acceptance_criteria>
  <verify><automated>mix test test/threadline/formatter_topology_contract_test.exs -x &amp;&amp; mix format --check-formatted</automated><fails_when>non-zero exit, zero tests, overlap, uncovered file, or drift</fails_when></verify>
  <done>Formatting is child-aware, non-overlapping, and contributor-friendly.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Ignore/formatter globs → repository files | Broad patterns can hide or double-own reviewed content. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-199-19 | Tampering | ignore policy | high | mitigate | Anchored rules plus positive/negative check-ignore controls. |
| T-199-20 | Denial of service | formatter topology | medium | mitigate | Derived exactly-one ownership contract. |
</threat_model>
<verification>Run ignore contract, formatter topology, and actual formatter.</verification>
<success_criteria>Generated outputs are precise and every Elixir file is formatted once.</success_criteria>
<output>Create `.planning/phases/199-decouple/199-10-SUMMARY.md` when done.</output>
