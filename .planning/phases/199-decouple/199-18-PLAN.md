---
phase: 199-decouple
plan: 18
type: execute
wave: 5
depends_on: [199-15]
files_modified:
  - test/fixtures/dialyzer/operator-boundaries.json
  - lib/threadline/operator_surface/auth.ex
  - lib/threadline/operator_surface/presentation.ex
  - lib/threadline/plug.ex
  - lib/threadline/policy/redaction_presenter.ex
autonomous: true
requirements: [DECOUPLE-07, DECOUPLE-08]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All seven sealed warnings W21 and W27-W32 are explicitly dispositioned within four authorized origins."
    - "Operator authentication and plug input contracts contain only reachable fallback/address shapes."
    - "Presentation and redaction helpers retain output behavior while unreachable guards and catchalls are removed or narrowed."
  artifacts:
    - path: test/fixtures/dialyzer/operator-boundaries.json
      provides: "Exact authority, disposition, and proof for the 7-warning operator-boundary slice"
    - path: lib/threadline/plug.ex
      provides: "Reachable Plug.Conn remote-address handling"
    - path: lib/threadline/policy/redaction_presenter.ex
      provides: "Exhaustive redaction-reason presentation contract"
  key_links:
    - from: lib/threadline/plug.ex
      to: Plug.Conn
      via: "remote_ip tuple contract"
    - from: test/fixtures/dialyzer/operator-boundaries.json
      to: bin/verify-dialyzer-slice
      via: "shared bounded-remediation contract"
---

<objective>
Remediate the seven sealed operator authentication, connection, presentation, and redaction boundary warnings.

Purpose: Remove only statically unreachable branches and align helper contracts with actual caller shapes, preserving product semantics per D-23 and exact residue handling per D-26/D-27.
Output: A sealed 7-warning slice fixture and narrow changes to exactly four warning-origin files.

Warning authority: exactly `lib/threadline/operator_surface/auth.ex` (W21), `lib/threadline/operator_surface/presentation.ex` (W27-W29), `lib/threadline/plug.ex` (W30-W31), and `lib/threadline/policy/redaction_presenter.ex` (W32). No other warning-origin source file may be edited by this plan.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
@.planning/phases/199-decouple/199-13-SUMMARY.md
@.planning/phases/199-decouple/199-15-SUMMARY.md
@bin/verify-dialyzer-slice
</context>

## Artifacts this phase produces

- `test/fixtures/dialyzer/operator-boundaries.json`: complete W21/W27-W32 sealed-to-current evidence.
- Reachable operator auth and Plug.Conn address handling.
- Presentation/redaction helpers whose clauses match the established caller and reason domains.

<dependencies>
Needs: Plan 199-15's fixture schema and executable verifier.
Creates: One disjoint remediation result consumed by Plan 199-20.
Parallel safety: Shares no modified file with Plans 199-16, 199-17, or 199-19 and may run alongside them in Wave 5.
</dependencies>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Narrow operator auth and connection fallbacks to reachable inputs</name>
  <files>test/fixtures/dialyzer/operator-boundaries.json, lib/threadline/operator_surface/auth.ex, lib/threadline/plug.ex</files>
  <read_first>
    - .planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
    - test/fixtures/dialyzer/critic-tooling.json
    - lib/threadline/operator_surface/auth.ex
    - lib/threadline/plug.ex
    - test/threadline/operator_surface/auth_test.exs
    - test/threadline/plug_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/operator_surface/auth.ex lib/threadline/plug.ex` succeeds.</precondition>
  <behavior>
    - RED: W21, W30, and W31 remain after the fixture declares them fixed.
    - GREEN: Auth retains its map-shaped fallback actor behavior without an unreachable catchall.
    - GREEN: Plug address formatting follows the Plug.Conn remote-ip tuple type and retains tested IPv4/IPv6 output.
    - Regression: Authenticated and fallback operator identities remain unchanged for supported calls.
  </behavior>
  <action>Create the operator-boundaries fixture with exact W21/W30/W31 evidence, mark them fixed, and capture RED. In auth, remove or narrow only the fallback clause proven unreachable from every caller while preserving map-scope behavior. In the plug, express remote address handling for Plug.Conn's tuple domain and eliminate branches that the connection contract cannot supply; retain IPv4/IPv6 formatting and existing fallback identity semantics. Do not broaden call inputs or change authentication decisions. Reach GREEN with auth/plug tests and the live verifier per D-23.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/auth_test.exs test/threadline/plug_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/operator-boundaries.json</automated>
    <fails_when>W21/W30/W31 survives, a supported auth scope or IP tuple changes result, authentication behavior changes, or a warning appears outside the two authorized origins.</fails_when>
  </verify>
  <acceptance_criteria>
    - W21, W30, and W31 are absent and recorded as fixed with evidence.
    - Auth and remote-address tests cover all retained input shapes.
    - No new catchall or broad type masks an unsupported input.
  </acceptance_criteria>
  <done>The auth/connection boundary is analyzer-clean and preserves the established operator identity contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove unreachable presentation guards and redaction catchall</name>
  <files>test/fixtures/dialyzer/operator-boundaries.json, lib/threadline/operator_surface/presentation.ex, lib/threadline/policy/redaction_presenter.ex</files>
  <read_first>
    - test/fixtures/dialyzer/operator-boundaries.json
    - lib/threadline/operator_surface/presentation.ex
    - lib/threadline/policy/redaction_presenter.ex
    - test/threadline/operator_surface/presentation_test.exs
    - test/threadline/policy/redaction_presenter_test.exs
    - test/threadline/policy/redaction_presenter_catalog_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/operator_surface/presentation.ex lib/threadline/policy/redaction_presenter.ex` succeeds.</precondition>
  <behavior>
    - RED: W27-W29 and W32 remain after fixed dispositions are added.
    - GREEN: Presentation helpers operate on the binary value their callers already provide, without impossible binary-versus-nil guards.
    - GREEN: Redaction reason formatting retains an explicit clause for every member of the established reason union and no unreachable final catchall.
    - Regression: Existing presentation strings and the complete redaction catalog are unchanged.
  </behavior>
  <action>Add W27-W29 and W32 to the fixture as fixed and capture RED. Remove the nil coalescing/guards that are impossible after the existing binary-producing expression, retaining the same output for empty and populated strings. Remove the redaction reason catchall only after the catalog test proves the declared reason domain is exhaustively handled; do not silently accept new reasons. Reach GREEN with presentation and both redaction suites, then seal the fixture at exactly seven warnings across four origins per D-23 and D-26.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/presentation_test.exs test/threadline/policy/redaction_presenter_test.exs test/threadline/policy/redaction_presenter_catalog_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/operator-boundaries.json</automated>
    <fails_when>Any of W27-W29/W32 survives, a presentation string changes, the catalog is no longer exhaustive, or the fixture total/authority differs from seven warnings/four origins.</fails_when>
  </verify>
  <acceptance_criteria>
    - The fixture owns exactly W21 and W27-W32 across four origins.
    - Every removed branch is proven impossible by current contracts and regression tests.
    - New redaction reasons still require an explicit presentation clause.
  </acceptance_criteria>
  <done>The 7-warning operator-boundary slice is exhaustively dispositioned and certified inside its four-file authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plug.Conn → operator identity | Network metadata participates in fallback actor presentation. |
| Redaction reason → operator display | Policy decisions cross into human-readable output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-199-18-01 | Spoofing | fallback actor identity | high | mitigate | Preserve authenticated/fallback behavior tests and alter only clauses proven unreachable from typed callers. |
| T-199-18-02 | Information disclosure | redaction presentation | high | mitigate | Keep the reason catalog exhaustive and preserve explicit formatting tests for every supported reason. |
| T-199-18-03 | Tampering | presentation helpers | medium | mitigate | Assert existing exact output strings before removing impossible guards. |
| T-199-18-04 | Repudiation | 7-warning fixture | medium | mitigate | Require sealed IDs/raw lines and live analyzer evidence for all four origins. |
</threat_model>

<verification>
Run both task commands and confirm the verifier reports exactly seven sealed warnings across exactly four authorized origins with no extra current warnings. Confirm no authentication, presentation, or redaction output regression.
</verification>

<success_criteria>
- W21 and W27-W32 are exhaustively triaged and certified.
- Only unreachable clauses/guards are removed; supported operator and policy behavior remains intact.
- The plan writes no source or fixture owned by another Wave 5 plan.
</success_criteria>

<output>
Create `.planning/phases/199-decouple/199-18-SUMMARY.md` when done.
</output>
