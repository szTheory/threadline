---
phase: 199-decouple
plan: 19
type: execute
wave: 5
depends_on: [199-15]
files_modified:
  - test/fixtures/dialyzer/operator-liveviews.json
  - lib/threadline/operator_surface/live/coverage_live.ex
  - lib/threadline/operator_surface/live/export_status_live.ex
  - lib/threadline/operator_surface/live/retention_history_live.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
autonomous: true
requirements: [DECOUPLE-07, DECOUPLE-08]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All five sealed warnings W22-W26 are explicitly dispositioned within five authorized LiveView origins."
    - "Timer creation/cancellation results are deliberately consumed and retain current refresh behavior."
    - "Timeline and transaction result matching contains only branches reachable from their callees' contracts."
  artifacts:
    - path: test/fixtures/dialyzer/operator-liveviews.json
      provides: "Exact authority, disposition, and proof for the 5-warning LiveView slice"
    - path: lib/threadline/operator_surface/live/coverage_live.ex
      provides: "Explicit timer cancellation-result handling"
    - path: lib/threadline/operator_surface/live/timeline_live.ex
      provides: "Reachable export-count result matching"
  key_links:
    - from: lib/threadline/operator_surface/live/export_status_live.ex
      to: Process.send_after/3
      via: "refresh scheduling whose returned timer reference is explicitly consumed"
    - from: test/fixtures/dialyzer/operator-liveviews.json
      to: bin/verify-dialyzer-slice
      via: "shared bounded-remediation contract"
---

<objective>
Remediate the five sealed operator LiveView warnings as a disjoint timer/result-contract slice.

Purpose: Make timer side effects deliberate and remove only impossible result branches, preserving operator behavior per D-23 and exact residue handling per D-26/D-27.
Output: A sealed 5-warning slice fixture and narrow changes to exactly five warning-origin LiveViews.

Warning authority: exactly `lib/threadline/operator_surface/live/coverage_live.ex` (W22), `lib/threadline/operator_surface/live/export_status_live.ex` (W23), `lib/threadline/operator_surface/live/retention_history_live.ex` (W24), `lib/threadline/operator_surface/live/timeline_live.ex` (W25), and `lib/threadline/operator_surface/live/transaction_live.ex` (W26). No other warning-origin source file may be edited by this plan.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
@.planning/phases/199-decouple/199-13-SUMMARY.md
@.planning/phases/199-decouple/199-15-SUMMARY.md
@bin/verify-dialyzer-slice
</context>

## Artifacts this phase produces

- `test/fixtures/dialyzer/operator-liveviews.json`: complete W22-W26 sealed-to-current evidence.
- Deliberate timer return handling for coverage, export status, and retention history.
- Reachable result matching for timeline and transaction LiveViews.

<dependencies>
Needs: Plan 199-15's fixture schema and executable verifier.
Creates: One disjoint remediation result consumed by Plan 199-20.
Parallel safety: Shares no modified file with Plans 199-16, 199-17, or 199-18 and may run alongside them in Wave 5.
</dependencies>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make coverage and export-status timer effects deliberate</name>
  <files>test/fixtures/dialyzer/operator-liveviews.json, lib/threadline/operator_surface/live/coverage_live.ex, lib/threadline/operator_surface/live/export_status_live.ex</files>
  <read_first>
    - .planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
    - test/fixtures/dialyzer/critic-tooling.json
    - lib/threadline/operator_surface/live/coverage_live.ex
    - lib/threadline/operator_surface/live/export_status_live.ex
    - test/threadline/operator_surface/live/coverage_live_test.exs
    - test/threadline/operator_surface/live/export_status_live_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/operator_surface/live/coverage_live.ex lib/threadline/operator_surface/live/export_status_live.ex` succeeds.</precondition>
  <behavior>
    - RED: W22 and W23 remain after their fixed dispositions are added.
    - GREEN: Coverage cancellation deliberately consumes `Process.cancel_timer/1`'s boolean result while retaining cleanup timing.
    - GREEN: Export-status scheduling deliberately consumes or stores the timer reference without adding duplicate refreshes.
    - Regression: Mount, refresh, and termination flows retain existing messages and rendered state.
  </behavior>
  <action>Create the LiveView fixture with exact W22/W23 evidence, mark both fixed, and capture RED. Make the coverage cancellation result explicitly consumed because the side effect, not its boolean, is the behavior. Make export-status scheduling explicitly consume or retain the returned reference according to its lifecycle, without changing intervals or adding timers. Reach GREEN using both LiveView suites and the live slice verifier. Do not suppress side-effect return warnings or alter operator-visible refresh semantics; this is the tracer for D-23.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/coverage_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/operator-liveviews.json</automated>
    <fails_when>W22 or W23 survives, timer cadence/count changes, mount/termination tests regress, or a warning outside the two origins appears.</fails_when>
  </verify>
  <acceptance_criteria>
    - W22 and W23 are absent and recorded as fixed with evidence.
    - Timer calls have explicit result intent and no duplicate timer is introduced.
    - The fixture contains only the declared LiveView authority.
  </acceptance_criteria>
  <done>One timer-based LiveView path is certified end-to-end and the second follows the same proven pattern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close retention timer and timeline result warnings</name>
  <files>test/fixtures/dialyzer/operator-liveviews.json, lib/threadline/operator_surface/live/retention_history_live.ex, lib/threadline/operator_surface/live/timeline_live.ex</files>
  <read_first>
    - test/fixtures/dialyzer/operator-liveviews.json
    - lib/threadline/operator_surface/live/retention_history_live.ex
    - lib/threadline/operator_surface/live/timeline_live.ex
    - lib/threadline/export.ex
    - test/threadline/operator_surface/live/retention_history_live_test.exs
    - test/threadline/operator_surface/live/timeline_live_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/operator_surface/live/retention_history_live.ex lib/threadline/operator_surface/live/timeline_live.ex` succeeds.</precondition>
  <behavior>
    - RED: W24 and W25 remain after fixed dispositions are added.
    - GREEN: Retention-history refresh scheduling deliberately consumes or stores the timer reference with unchanged cadence.
    - GREEN: Timeline matches only the reachable `Export.count_matching/1` success contract and preserves count display.
    - Regression: Retention refresh and timeline export-count interactions render the same states.
  </behavior>
  <action>Add W24/W25 to the fixture as fixed and capture RED. Apply the proven timer-return pattern to retention history without changing refresh cadence. In timeline, remove only the impossible catchall after confirming `Export.count_matching/1`'s contract and all local call results; preserve the success value and rendering. Do not invent a new error behavior outside the callee contract. Reach GREEN with both LiveView suites and record exact post-analysis evidence per D-23 and D-26.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/retention_history_live_test.exs test/threadline/operator_surface/live/timeline_live_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/operator-liveviews.json</automated>
    <fails_when>W24 or W25 survives, timer cadence changes, timeline count behavior changes, or the verifier sees a warning outside the two added origins.</fails_when>
  </verify>
  <acceptance_criteria>
    - W24/W25 are absent and recorded as fixed with evidence.
    - Retention scheduling follows the same explicit timer-result contract as the tracer.
    - Timeline retains every reachable result branch and its existing count behavior.
  </acceptance_criteria>
  <done>Retention and timeline are analyzer-clean inside their two-origin authority with operator behavior preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove the unreachable transaction fallback and seal the slice</name>
  <files>test/fixtures/dialyzer/operator-liveviews.json, lib/threadline/operator_surface/live/transaction_live.ex</files>
  <read_first>
    - test/fixtures/dialyzer/operator-liveviews.json
    - lib/threadline/operator_surface/live/transaction_live.ex
    - test/threadline/operator_surface/transaction_live_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/operator_surface/live/transaction_live.ex` succeeds.</precondition>
  <behavior>
    - RED: W26 remains after its fixed disposition is added.
    - GREEN: Transaction matching retains every result shape the called function can return and removes only the unreachable fallback.
    - Regression: Existing transaction success/failure rendering and navigation remain unchanged.
  </behavior>
  <action>Add W26 to the fixture as fixed and capture RED. Trace the called function's exact return contract and remove/narrow only the transaction fallback that cannot match; retain all reachable success and error paths and their operator-visible messages. Reach GREEN with the transaction LiveView suite, then seal the fixture at exactly five warnings across five origins. If any of W22-W26 is genuinely irreducible rather than fixed, retain its exact live raw line plus specific tuple, rationale, upstream/version note where applicable, and removal trigger for Plan 199-20; never relabel a real defect as residue.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/transaction_live_test.exs test/threadline/dialyzer_slice_contract_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/operator-liveviews.json</automated>
    <fails_when>W26 survives under a fixed disposition, a reachable transaction result loses handling, the UI behavior changes, or the fixture total/authority differs from five warnings/five origins.</fails_when>
  </verify>
  <acceptance_criteria>
    - The fixture owns exactly W22-W26 across five origins.
    - Every warning has a fixed or fully evidenced irreducible disposition.
    - All five targeted LiveView suites pass and no ignore entry is added in this slice plan.
  </acceptance_criteria>
  <done>The complete 5-warning operator-LiveView slice is dispositioned and certified inside its five-file authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BEAM timer service → LiveView process | Timer references and messages cross an asynchronous process boundary. |
| Export/query result → rendered operator state | Typed backend results determine human-visible counts and transaction details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-199-19-01 | Denial of service | refresh timers | high | mitigate | Preserve intervals, prove no duplicate scheduling, and exercise mount/refresh/termination tests. |
| T-199-19-02 | Tampering | unreachable result cleanup | medium | mitigate | Remove only branches excluded by concrete callee contracts and retain exact rendered-state tests. |
| T-199-19-03 | Repudiation | 5-warning fixture | medium | mitigate | Bind W22-W26 to exact origins, raw evidence, and live verifier results. |
| T-199-19-04 | Information disclosure | LiveView tests | low | accept | Tests operate on fixtures and rendered state already present in the repository; no new runtime logging is added. |
</threat_model>

<verification>
Run all three task commands and confirm the verifier reports exactly five sealed warnings across exactly five authorized origins with no extra current warnings. Confirm timer cadence and rendered-state assertions remain unchanged.
</verification>

<success_criteria>
- W22-W26 are exhaustively triaged and certified.
- Timer effects are deliberate without duplicate scheduling or lifecycle drift.
- Only unreachable result branches are removed and operator-visible behavior remains stable.
</success_criteria>

<output>
Create `.planning/phases/199-decouple/199-19-SUMMARY.md` when done.
</output>
