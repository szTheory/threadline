---
phase: 199-decouple
plan: 15
type: execute
wave: 4
depends_on: [199-08]
files_modified:
  - bin/verify-dialyzer-slice
  - test/threadline/dialyzer_slice_contract_test.exs
  - test/fixtures/dialyzer/critic-tooling.json
  - lib/mix/tasks/critic.measure.ex
  - lib/mix/tasks/critic.synth.ex
  - lib/threadline/critic_trust/measure.ex
autonomous: true
requirements: [DECOUPLE-07, DECOUPLE-08]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The halted 40-warning run is consumed as sealed evidence, never rerun as an unbounded edit mandate."
    - "Warnings W01, W02, and W05 are absent because the three critic-tooling origins have narrow, behavior-preserving fixes."
    - "A source-owned verifier proves each later remediation slice against current raw Dialyzer output without reading .planning."
  artifacts:
    - path: bin/verify-dialyzer-slice
      provides: "Executable per-slice authority and warning-disposition verifier"
    - path: test/threadline/dialyzer_slice_contract_test.exs
      provides: "Synthetic positive and negative controls for the slice contract"
    - path: test/fixtures/dialyzer/critic-tooling.json
      provides: "Sealed W01/W02/W05 provenance and post-remediation evidence"
  key_links:
    - from: test/fixtures/dialyzer/critic-tooling.json
      to: bin/verify-dialyzer-slice
      via: "schema-versioned fixture validation and exact origin filtering"
    - from: bin/verify-dialyzer-slice
      to: mix dialyzer --format raw
      via: "live raw warning comparison"
---

<objective>
Establish the bounded-remediation tracer and eliminate the three critic-tooling warnings from the sealed run.

Purpose: Recover from the designed Plan 199-13 halt without granting any executor authority over the other 19 warning origins. Per D-23, real defects receive narrow fixes; per D-26 and D-27, residue and ceilings remain exact and machine checked.
Output: A reusable planning-independent slice verifier, its executable contract tests, one sealed slice fixture, and behavior-preserving fixes for W01, W02, and W05.

Warning authority: exactly `lib/mix/tasks/critic.measure.ex` (W01), `lib/mix/tasks/critic.synth.ex` (W02), and `lib/threadline/critic_trust/measure.ex` (W05). No other warning-origin source file may be edited by this plan.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-decouple/199-CONTEXT.md
@.planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
@.planning/phases/199-decouple/199-13-SUMMARY.md
@mix.exs
@.dialyzer_ignore.exs
</context>

## Artifacts this phase produces

- `bin/verify-dialyzer-slice`: repository-owned, executable slice verifier with no `.planning` dependency.
- `test/threadline/dialyzer_slice_contract_test.exs`: RED/GREEN controls for malformed authority, surviving fixed warnings, unrecorded warnings, and incomplete residue metadata.
- `test/fixtures/dialyzer/critic-tooling.json`: W01/W02/W05 ownership, disposition, evidence, and raw-output provenance.

<dependencies>
Needs: Plan 199-08's source-tree test/CI independence and Plan 199-13's immutable sealed triage evidence. Plan 199-13 is context, not an execution dependency, because its terminal state is `halted`.
Creates: The slice-fixture schema and `bin/verify-dialyzer-slice` command required by Plans 199-16 through 199-20.
Parallel safety: This plan is the sole writer of the verifier and critic-tooling origins and therefore runs before every remediation expansion slice.
</dependencies>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one sealed warning can move from RED to fixed</name>
  <files>bin/verify-dialyzer-slice, test/threadline/dialyzer_slice_contract_test.exs, test/fixtures/dialyzer/critic-tooling.json, lib/threadline/critic_trust/measure.ex</files>
  <read_first>
    - .planning/phases/199-decouple/199-DIALYZER-TRIAGE.md
    - .planning/phases/199-decouple/199-13-SUMMARY.md
    - lib/threadline/critic_trust/measure.ex
    - test/threadline/critic_trust/measure_test.exs
    - mix.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/threadline/critic_trust/measure.ex` succeeds, proving this authorized origin has not drifted since the sealed run.</precondition>
  <behavior>
    - RED: A schema-versioned fixture declaring W05 fixed fails while the raw `unmatched_return` at line 94 remains.
    - GREEN: Explicitly consuming the return from `:rand.seed/2` preserves deterministic seeding and removes W05.
    - Contract: Duplicate warning IDs, origins outside `authorized_origins`, a surviving fixed warning, an unrecorded live warning, or irreducible residue without exact tuple/rationale/removal trigger is rejected.
    - Contract: `--raw-output PATH` permits deterministic synthetic tests; omitting it invokes the repository's raw Dialyzer command and checks only the fixture's authorized origins.
  </behavior>
  <action>First add the fixture schema, executable verifier, and ExUnit controls, marking W05 as `fixed`; run the verifier against current raw output and record the expected RED failure. The fixture must carry schema version 1, sealed-run hash/count, slice name, the exact authorized origins, unique original warning IDs/classes/raw lines, disposition, evidence, and post-analysis command/hash. Implement the verifier so it normalizes project-relative paths, rejects origins outside authority, compares every live warning for the authorized origins, and never reads `.planning`. Then make the narrow W05 fix by explicitly binding/consuming the seed return without changing seed inputs or generated values, and reach GREEN. Keep warning parsing exact enough to distinguish warning class and origin; do not create suppression entries in this task. This is the production tracer for D-23, D-26, and D-27.</action>
  <verify>
    <automated>mix test test/threadline/dialyzer_slice_contract_test.exs test/threadline/critic_trust/measure_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/critic-tooling.json</automated>
    <fails_when>The synthetic negative controls are accepted, the verifier depends on `.planning`, W05 remains in live raw output, a new warning appears in the authorized origin, or critic measurement tests change behavior.</fails_when>
  </verify>
  <acceptance_criteria>
    - The RED failure and GREEN success are both recorded in the plan summary with commands and exit status.
    - The verifier is executable, source-owned, and rejects every malformed/surviving-warning case in the behavior contract.
    - W05 is recorded as fixed and absent from current raw output with no ignore entry.
  </acceptance_criteria>
  <done>One sealed warning has completed a reproducible RED-to-GREEN path through the reusable bounded-remediation verifier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the two no-return critic task warnings</name>
  <files>test/fixtures/dialyzer/critic-tooling.json, lib/mix/tasks/critic.measure.ex, lib/mix/tasks/critic.synth.ex</files>
  <read_first>
    - test/fixtures/dialyzer/critic-tooling.json
    - lib/mix/tasks/critic.measure.ex
    - lib/mix/tasks/critic.synth.ex
    - test/threadline/operator_surface/critic_trust_test.exs
  </read_first>
  <precondition>`git diff --quiet ecc1c0a1 -- lib/mix/tasks/critic.measure.ex lib/mix/tasks/critic.synth.ex` succeeds.</precondition>
  <behavior>
    - RED: Adding W01 and W02 as fixed dispositions makes the slice verifier fail on both surviving `warn_return_no_exit` warnings.
    - GREEN: Each private `task_error!/1` has a truthful `no_return()` contract matching its unconditional `Mix.raise/1` behavior.
    - Regression: Measure and synth failure paths still raise the same operator-facing errors.
  </behavior>
  <action>Extend the slice fixture with W01 and W02, declare them fixed, and capture the expected RED verifier result. Add the narrow private function specifications that describe the existing unconditional exception behavior; do not change error text, exit behavior, or command flow. Run the relevant critic-trust tests and the live slice verifier to GREEN, then seal the post-analysis hash and evidence for all three warnings. Per D-23, type facts must describe real runtime behavior rather than weakening specifications to silence analysis.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/critic_trust_test.exs test/threadline/dialyzer_slice_contract_test.exs --max-failures 1 &amp;&amp; bin/verify-dialyzer-slice --fixture test/fixtures/dialyzer/critic-tooling.json</automated>
    <fails_when>W01 or W02 survives, task failure behavior changes, the fixture omits any of W01/W02/W05, or the verifier reports a warning outside the three-origin authority set.</fails_when>
  </verify>
  <acceptance_criteria>
    - The fixture contains exactly W01, W02, and W05 and exactly three authorized origins.
    - All three dispositions are `fixed`, all three original warnings are absent, and no current warning exists in those origins.
    - No `.dialyzer_ignore.exs` edit is made.
  </acceptance_criteria>
  <done>The critic-tooling slice is fully green and provides the proven contract used by all later slices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sealed triage → editable source fixture | Historical warning evidence is transcribed into an executable repository artifact. |
| Raw Dialyzer text → verifier | Tool output is untrusted structured input whose paths and classes govern remediation claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-199-15-01 | Tampering | slice fixture | high | mitigate | Preserve W01/W02/W05 raw evidence and sealed-run hash, enforce unique IDs and exact authorized origins, and fail on origin drift. |
| T-199-15-02 | Spoofing | raw-output parser | medium | mitigate | Normalize only repository-relative warning origins and reject output that cannot be attributed to an authorized source file. |
| T-199-15-03 | Repudiation | RED/GREEN evidence | medium | mitigate | Record both commands, exit states, and post-analysis hash in the fixture and summary. |
| T-199-15-04 | Denial of service | live Dialyzer invocation | low | accept | The slice command reuses the already-built full PLT and bounds evaluation to three origins; CI timeout policy is owned by Plan 199-14. |
</threat_model>

<verification>
Run the two task commands, then confirm `git diff --name-only ecc1c0a1 -- lib | sort` contains only the three authorized warning origins for this plan. The fixture must report 3/40 sealed warnings and zero live warnings in its origins.
</verification>

<success_criteria>
- A planning-independent executable can certify a bounded warning slice.
- W01, W02, and W05 are fixed without suppressions or semantic drift.
- Later remediation plans have one stable fixture schema and verifier interface to reuse.
</success_criteria>

<output>
Create `.planning/phases/199-decouple/199-15-SUMMARY.md` when done.
</output>
