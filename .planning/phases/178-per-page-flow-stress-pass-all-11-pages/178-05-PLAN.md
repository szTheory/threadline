---
phase: 178-per-page-flow-stress-pass-all-11-pages
plan: 05
type: execute
wave: 4
depends_on: [178-01, 178-02, 178-03, 178-04]
files_modified:
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/style_contract_test.exs
  - test/threadline/operator_surface/component_contract_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts
autonomous: true
requirements: [PAGE-02]
tags: [operator-surface, page-02, footguns, contrast, focus, overlay, pagination, tier-a, tier-b, fix-to-green]

must_haves:
  truths:
    - "Per D-05/D-06/D-07, all 11 named footgun classes have a permanent detector and the detector is GREEN on the composed pages (write-fail-fix-green ratchet)"
    - "Per footgun #10/#11, every text/bg role pairing meets WCAG 2.2 AA (>=4.5) in both dark and light lanes via contrast_ratio/2 + composite/2"
    - "Per footgun #1, scroll traps are eliminated (scroll-margin/scroll-padding reconciled, overscroll-behavior:contain, 100svh) and the Tier B sweep confirms no nested-scroll trap / sticky occlusion"
    - "Per footgun #2/#3/#4, overlays sit above scrim (z-token ascending + elementFromPoint hit-test), focus enters on open and returns to trigger on close, Esc and scrim-click both dismiss"
    - "Per footgun #5/#7/#8/#9, no cursor:pointer on non-interactive; disabled controls carry real disabled/aria-disabled + not-allowed; active tab/nav state has aria-current/aria-selected distinct bg + a non-color cue; pager disables-at-edge/hides-at-zero with honest caption"
    - "Per D-02, the Tier B representative ~66-cell sample (320+1440, happy+worst, dark+light, +1 keyboard +1 reduced-motion +1 reconnect) is GREEN over /audit/*"
  prohibitions:
    - "MUST NOT remove the PAGE-03 `justify-self: center` rules added in Plan 03 (style.ex contention — preserve them)"
    - "MUST NOT add a new --tl-* token; footgun fixes compose existing tokens/classes (v1.37 invariant; brand-token parity must stay green)"
    - "MUST NOT introduce an inline `on*=` handler or new JS/runtime dep (CSP-proof, zero-new-dep invariant)"
    - "MUST NOT key any reconnect/offline assertion off `<body>` or `.phx-disconnected` (D-11)"
    - "MUST NOT pre-empt the Phase 179 microcopy/IA sweep — copy touched during a fix follows brand voice but no systematic copy rewrite"
    - "MUST NOT add pixel-diff / screenshot baselines (deterministic, baseline-free)"
    - "MUST NOT have a negative-grep acceptance literal appear verbatim in any source comment (comment-text discipline)"
  artifacts:
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "GREEN per-role contrast + scroll-trap + cursor/disabled source guards"
    - path: "examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts"
      provides: "GREEN Tier B footgun sweep over /audit/* + the ~66-cell sample"
  key_links:
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "contrast/cursor/scroll source guards assert against the fixed CSS"
      pattern: "contrast_ratio|overscroll-behavior|not-allowed"
    - from: "examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts"
      to: "lib/threadline/operator_surface/live"
      via: "Tier B sweep iterates /audit/* routes asserting overlay/focus/pager/geometry"
      pattern: "/audit"
---

<objective>
Turn the Wave-0 footgun detectors GREEN by fixing the 11 named footgun classes on the composed pages — the guard-first ratchet (D-05): the detector failed on today's surface, now the fix makes it pass and the detector stays a permanent CI assertion. Layered global (style.ex source scans + Tier B /audit/* sweep) plus per-page/per-component detectors on the stress story matrix (D-06). Then finalize the Tier B ~66-cell representative sample.

Purpose: A footgun can be absent per-component yet present once composed onto a page (PAGE-02's whole point); this plan proves and fixes them at the assembled-page level. Reuse the existing infra — contrast_ratio/2, z-order guard, elementFromPoint, focus/Esc helpers, pager disabled-edge — do NOT reinvent (D-07).
Output: Footgun fixes in style.ex (and any composing class wiring), all 11 detectors GREEN (Tier A + Tier B), the Tier B sample finalized.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the surface-wide footguns (contrast #10/#11, scroll-traps #1, spacing #6 source half, cursor #5, disabled #7) — Tier A green</name>
  <files>lib/threadline/operator_surface/style.ex, test/threadline/operator_surface/style_contract_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/style_contract_test.exs:697-854 (contrast_ratio/2 + composite/2 engine; the Wave-0 per-role contrast guards) and the Wave-0 scroll-trap source guard (#1: reconciled scroll-padding-top↔scroll-margin-top + overscroll-behavior:contain + 100svh, authored in Plan 01 Task 1), the Wave-0 spacing-token source guard (#6: --tl-space-*/gap, no raw child margin, authored in Plan 01 Task 1), and the Wave-0 cursor/disabled source guards (#5/#7)
    - lib/threadline/operator_surface/style.ex (the source-of-truth for all footgun CSS; scroll-padding/scroll-margin tokens, overscroll-behavior, 100svh; the `not-allowed`/disabled patterns at the ui.ex:710 precedent; the `--tl-color-*` text/bg tokens in both lanes)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md § Color (the role pairings that must hit AA in both lanes) and § "Interaction & Footgun Contract" (#1/#5/#7/#10/#11 rows)
  </read_first>
  <behavior>
    - GREEN: every text/bg role pairing >= 4.5 (AA) in both lanes, alpha-aware via composite (#10/#11) — fix any failing token pairing by adjusting the existing token value (parity-updated same-commit) or the role usage, never by adding a new token
    - GREEN: scroll-trap guard (#1, the Wave-0 source-scan detector from Plan 01 Task 1) — sticky never occludes (scroll-padding-top reconciled to scroll-margin-top), overscroll-behavior:contain present, 100svh used where the Wave-0 detector requires; the Plan 01 Task 3 Tier B scroll-trap/sticky-occlusion cell also turns green
    - GREEN: spacing-token guard (#6, the Wave-0 source-scan detector from Plan 01 Task 1) — stressed-page child spacing resolves through --tl-space-*/gap tokens with no raw child margin
    - GREEN: no `cursor: pointer` on non-interactive elements (#5); disabled controls carry `not-allowed` + reduced-opacity token + real disabled/aria-disabled (#7)
  </behavior>
  <action>
    For each surface-wide footgun whose Wave-0 detector is RED, fix the CSS in style.ex: resolve any sub-AA text/bg pairing (#10/#11) by correcting the offending `--tl-color-*` value — if a token value changes, update brandbook/tokens.{json,css} in the SAME commit so brand-token parity stays green (NEVER add a new token); reconcile scroll-padding-top to the per-row scroll-margin-top token + ensure `overscroll-behavior: contain` and `100svh` where the scroll-trap guard requires (#1); route any raw child margin on a stressed-page selector through `--tl-space-*`/gap tokens so the Wave-0 #6 spacing-token source guard goes green (the boundingBox/within-viewport #6 half is finalized in Task 3's Tier B sweep); remove any `cursor: pointer`/hover affordance on non-interactive selectors (#5); ensure disabled affordance is the `not-allowed` cursor + reduced-opacity token (#7). Reuse the existing `contrast_ratio/2`/`composite/2` engine for the assertions — do NOT build a new calculator (D-07). Preserve the PAGE-03 `justify-self: center` rules from Plan 03. When a negative-grep acceptance literal would otherwise appear in a CSS comment, rephrase by concept (comment-text discipline). If any pairing is already AA today, document it as GREEN-confirming.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs 2>&1 | tail -20</automated>
  </verify>
  <done>All surface-wide Tier A footgun source guards (#1/#5/#6 source half/#7/#10/#11) GREEN; PAGE-03 justify-self rules intact; brand-token parity green (any token value change mirrored in brandbook same-commit); no new token; format + credo + warnings-as-errors clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the overlay/active-state/pagination footguns (#2/#3/#4/#6/#8/#9) — structural Tier A green</name>
  <files>lib/threadline/operator_surface/style.ex, test/threadline/operator_surface/component_contract_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/component_contract_test.exs:243-275 (z-order ascending guard #2; reconnect/focus structural) and the Wave-0 per-footgun structural detectors from Plan 01 Task 2 — including the explicit #4 Esc + click-outside dismiss-marker detector (asserted independently of #3's focus-entry hooks) and #5/#7/#8/#9
    - lib/threadline/operator_surface/ui.ex (the overlay shells modal/drawer/toast, JS.focus_first/phx-mounted focus hooks, UI.pager disabled-at-edge/hide-at-zero, the page_header active-state with aria-current + non-color cue from 175)
    - lib/threadline/operator_surface/style.ex (z-layer tokens, the active-state distinct-bg + non-color cue, the spacing tokens for #6 alignment)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md § "Interaction & Footgun Contract" (#2/#3/#4/#6/#8/#9 rows) and § Typography "Heading hierarchy contract"
  </read_first>
  <behavior>
    - GREEN: z-token ascending order holds (#2); overlays expose JS.focus_first/phx-mounted focus-entry hooks (#3 structural half) AND — turning the independent Wave-0 #4 detector green — each overlay carries an Escape dismiss binding plus a scrim/click-outside dismiss marker (#4 structural half ratchets against its own Plan 01 Task 2 detector, not #3's hooks)
    - GREEN: the #6 boundingBox/within-viewport structural half holds on the composed pages (the #6 source-scan half is already green from Task 1)
    - GREEN: active tab/nav state carries aria-current="page"/aria-selected with distinct bg PLUS a non-color cue (#8); UI.pager disables-at-edge/hides-at-zero with honest cap caption (#9)
  </behavior>
  <action>
    For each structural footgun whose Wave-0 detector is RED, fix the composing markup/CSS without adding a new token or public API: ensure z-layer tokens stay strictly ascending (#2 — reuse the ascending guard, do not reorder regressively); ensure every overlay (modal/drawer) carries the `JS.focus_first`/`phx-mounted` focus-entry hook (#3) AND, to turn the independent Wave-0 #4 detector green, the Escape-key dismiss binding plus the scrim/click-outside dismiss marker — all wired via Phoenix.LiveView.JS (CSP-safe, no inline on*=) (#4 ratchets against its own detector, not #3's); ensure stressed pages route spacing through `--tl-space-*`/gap tokens / UI.stack/UI.cluster, no raw child margins (#6); ensure active tab/nav state pairs `aria-current="page"`/`aria-selected` distinct background with a non-color cue (weight/underline/shape) per the no-color-alone rule (#8); ensure `UI.pager` disables (not hides) at boundaries, hides at zero, and renders the honest "Showing latest N"/"10,000+" cap caption (#9). Reuse the existing UI components from 175/176/177 — do NOT reinvent (D-07). These are per-page/per-component detectors hosted on the `__stress` story matrix (D-06) — mount the relevant stress story (the Plan-04 fixtures) where an overlay/pager must be present. Do not pre-empt the Phase 179 copy sweep.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/component_contract_test.exs 2>&1 | tail -20</automated>
  </verify>
  <done>All structural footgun detectors (#2/#3/#4/#6/#8/#9) GREEN on the composed pages/stories; no new token; no public API; no inline on*= handler; format + credo + warnings-as-errors clean.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the Tier B footgun sweep + the representative ~66-cell sample (D-02)</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts (the Wave-0 footgun-sweep + sample scaffolds to finalize) and operator-phase-177-uat.spec.ts:54,83 (boundingBox/within-viewport) + operator-accessibility.spec.ts:18,88 (expectFocused/Esc)
    - examples/threadline_phoenix/e2e/.../playwright.config.ts (dark + desktop-chromium-light + reducedMotion:"reduce" lanes — the sampling lanes)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md § "Pitfall 3/4" (socket-drop ladder, centering measurement) and § Validation Architecture (the ~66-cell rule)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VALIDATION.md § "The Two-Tier Honesty Contract"
  </read_first>
  <action>
    Finalize `operator-phase-178-uat.spec.ts` so the Tier B layer is GREEN and honest: the global footgun SWEEP iterates all `/audit/*` routes asserting overlay-above-scrim (elementFromPoint hit-test #2), focus enters on open + returns to trigger on close + Esc/scrim-click dismiss (#3/#4 via expectFocused/Esc), within-viewport / no-h-scroll at 320–1440 (#6 boundingBox), disabled affordance + cursor (#5/#7), active-state distinct (#8), pager edges (#9), and AA contrast spot-checks where a real engine is needed (#10/#11). Wire the representative ~66-cell sample per D-02: per page 320 floor + 1440 ceiling, happy + worst-case (error/empty), dark + light lanes, plus one keyboard-only, one reduced-motion (reducedMotion project), and one reconnect spec (the real socket-drop already authored in Wave 0). Keep the honesty framing in a spec-file header comment: Tier A proved the full structural cartesian; Tier B proves this representative sample in a real engine — a green suite does NOT mean all ~3,465 cells were eyeballed. NO pixel-diff. Un-`.fixme` the scaffolds now that the fixes (Plans 02/03/05 Tasks 1-2) and fixtures (Plan 04) are in.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && npx playwright test e2e/tests/operator-phase-178-uat.spec.ts 2>&1 | tail -25</automated>
  </verify>
  <done>The Tier B footgun sweep + the ~66-cell representative sample (incl. the real socket-drop and the keyboard/reduced-motion/light cells) are GREEN across `/audit/*` on dark + light lanes; no scaffold markers remain; no pixel-diff; the honesty framing comment is present.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
NEW symbols created by this plan (excluded from drift verification):
- GREEN-turned Tier A footgun detectors in style_contract_test.exs (#1/#5/#7/#10/#11) and component_contract_test.exs (#2/#3/#4/#6/#8/#9)
- Finalized Tier B sweep + representative-sample specs in operator-phase-178-uat.spec.ts
- Any new CSS rules added to style.ex to fix footguns (e.g. overscroll-behavior/scroll-padding reconciliation, cursor removals, active-state non-color cue) — composing existing tokens only
</artifacts_this_phase_produces>

<verification>
- `mix test test/threadline/operator_surface/` — all 11 footgun detectors GREEN; PAGE-03 justify-self guards still GREEN (not regressed).
- `mix ci.all` Tier A green; `examples/threadline_phoenix/e2e/run-e2e.sh` (or `mix verify.example_browser`) Tier B representative sample green on dark + light.
- `mix compile --warnings-as-errors`, `mix format --check-formatted`, `mix verify.credo`, brand-token parity all green; zero new token; no inline on*= handler.
- Capture & semantics layers: `git diff --stat` shows no change under those layers.
</verification>

<success_criteria>
All 11 named footgun classes are eliminated and each has a permanent CI assertion (Tier A + Tier B) that fails if the footgun silently returns; the Tier B representative ~66-cell sample is green and the two-tier honesty framing is explicit. PAGE-02 complete.
</success_criteria>

<output>
Create `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-05-SUMMARY.md` when done, listing each footgun class with its detector(s), whether the fix was a real change or GREEN-confirming, and confirming PAGE-03 guards were not regressed and no new token was added.
</output>
