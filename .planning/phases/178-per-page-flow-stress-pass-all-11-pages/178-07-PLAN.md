---
phase: 178-per-page-flow-stress-pass-all-11-pages
plan: 07
type: execute
wave: 6
depends_on: [178-06]
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts
autonomous: true
gap_closure: true
requirements: [PAGE-01, PAGE-02, PAGE-03]
requirements_addressed: [PAGE-01, PAGE-02, PAGE-03]
tags: [operator-surface, gap-closure, e2e, liveview, reconnect, d-13]

must_haves:
  truths:
    - "PAGE-01 / D-13: the documented real socket-drop command passes 3/3 Playwright projects, including default chromium."
    - "PAGE-01 / D-13: the default chromium lane opens the real retention prune modal before disconnect assertions run."
    - "PAGE-01 / D-13: the positive socket-drop proof still asserts [data-phx-main] lifecycle flip, visible .tl-reconnect-banner, opacity 0.55, pointer-events none, and reconnect restoration."
    - "PAGE-02: existing named-footgun Tier A and Tier B assertions remain present and are not weakened while the socket-drop setup is fixed."
    - "PAGE-03: existing desktop centering guards for .tl-container and .tl-home remain green and are not replanned."
  artifacts:
    - path: "examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts"
      provides: "Deterministic real LiveView modal setup for the D-13 socket-drop proof."
      contains: "openPruneModal"
    - path: "examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts"
      provides: "Positive D-13 reconnect assertions against [data-phx-main], .tl-reconnect-banner, and #prune-confirm-content [data-tl-mutating]."
      contains: "real dropped live socket"
    - path: ".planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-07-SUMMARY.md"
      provides: "Execution evidence for the final gap-closure run."
  key_links:
    - from: "examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts"
      to: "lib/threadline/operator_surface/live/retention_history_live.ex"
      via: "openPruneModal clicks the real phx-click=open_prune_modal button and waits for the real UI.modal content."
      pattern: "openPruneModal"
    - from: "examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts"
      to: "lib/threadline/operator_surface/style.ex"
      via: "socket-drop assertions observe computed reconnect CSS from [data-phx-main].phx-* selectors."
      pattern: "opacity.*0\\.55|pointer-events.*none"
---

<objective>
Close the single remaining Phase 178 verification gap: the documented Tier B command for the D-13 real socket-drop proof fails in the default `chromium` project before the disconnect assertions because `#prune-confirm-content` remains hidden after clicking the real "Run retention prune" button.

Purpose: PAGE-01 includes the LiveView reconnect stress dimension, and 178-VERIFICATION is still `gaps_found` until the official command passes all three projects. The plan makes the e2e setup deterministic in default Chromium while preserving the already-correct selector source, Tier A guards, PAGE-02 footgun evidence, PAGE-03 centering evidence, and positive reconnect behavior assertions.

Output: A hardened D-13 e2e setup in `operator-phase-178-uat.spec.ts`, a documented 3/3 targeted Tier B pass, and a 178-07 execution summary. No runtime dependencies, production JavaScript, capture-layer edits, semantics-layer edits, or server-side prune authorization changes are expected.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VALIDATION.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VERIFICATION.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-06-PLAN.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-06-SUMMARY.md
@examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts
@examples/threadline_phoenix/e2e/test-results/operator-phase-178-uat-Pha-a7793-ims-mutating-controls-D-13--chromium/error-context.md
@lib/threadline/operator_surface/live/retention_history_live.ex
@lib/threadline/operator_surface/ui.ex
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/component_contract_test.exs
@test/threadline/operator_surface/style_contract_test.exs
</context>

<!-- planner-discipline-allow: .phx-disconnected -->
<!-- planner-discipline-allow: body.phx- -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the prune modal setup deterministic before the socket drop</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts (current `openPruneModal`, D-13 socket-drop test, and overlay modal users)
    - examples/threadline_phoenix/e2e/test-results/operator-phase-178-uat-Pha-a7793-ims-mutating-controls-D-13--chromium/error-context.md (default chromium failure: content exists but remains hidden)
    - lib/threadline/operator_surface/live/retention_history_live.ex (real `phx-click="open_prune_modal"`, `@prune_modal_open`, `UI.modal id="prune-confirm"`, and server prune enforcement to preserve)
    - lib/threadline/operator_surface/ui.ex (UI.modal show/hide contract: `phx-mounted={@show && show_modal(@id)}`, `show_modal/2`, and target ids)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VERIFICATION.md (authoritative remaining blocker)
  </read_first>
  <action>
    Update `openPruneModal(page)` in `operator-phase-178-uat.spec.ts` so it never clicks the LiveView `phx-click` button before the real page is connected in default Chromium. After navigating to `/audit/policy/retention`, locate `[data-phx-main]` and wait for the class to include `phx-connected` before clicking. Locate the real button with an exact "Run retention prune" accessible-name match, assert it is visible and enabled, then click it. After the click, wait for the real modal container `#prune-confirm` to become visible or lose its hidden state, then wait for `#prune-confirm-content` to be visible and for at least one focusable control inside it to be visible. Use Playwright auto-retrying assertions, not fixed sleeps. Keep the helper driving the production button and production LiveView event; do not manually set assigns, remove classes, call `show_modal` from the browser, dispatch synthetic Phoenix events, or force the modal open. If the helper still leaves the content hidden after a proven `phx-connected` click, collect the class/display state for `#prune-confirm`, `#prune-confirm-content`, and `[data-phx-main]` in assertion messages, then treat it as an actual UI event wiring defect for a follow-up production fix rather than weakening this e2e proof.
  </action>
  <acceptance_criteria>
    - Source: `openPruneModal(page)` waits for `[data-phx-main]` to be connected before clicking the prune button.
    - Source: the helper still clicks the real "Run retention prune" button and waits for the real `#prune-confirm-content`; it does not open the modal by DOM surgery or test-only event injection.
    - Behavior: default chromium reaches the disconnect assertions instead of timing out while `#prune-confirm-content` is hidden.
    - Preservation: overlay sample tests continue using the same helper and still exercise the real `UI.modal id="prune-confirm"` path.
    - Scope: `retention_history_live.ex`, `ui.ex`, server prune authorization, typed confirmation, secure compare, fail-closed scope, and audit-before-action are not edited by this task.
  </acceptance_criteria>
  <verify>
    <automated>./examples/threadline_phoenix/e2e/run-e2e.sh e2e/tests/operator-phase-178-uat.spec.ts -g "real dropped live socket"</automated>
  </verify>
  <done>The e2e helper opens the real prune modal deterministically in default Chromium and the documented D-13 command no longer fails before disconnect assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the positive D-13 reconnect assertions and regression guards</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts (D-13 socket-drop test and `openPruneModal` after Task 1)
    - lib/threadline/operator_surface/style.ex (correct `[data-phx-main].phx-* .threadline-ui` selectors and `opacity: 0.55` / `pointer-events: none`)
    - test/threadline/operator_surface/component_contract_test.exs (Tier A reconnect selector-correctness guard)
    - test/threadline/operator_surface/style_contract_test.exs (PAGE-02 and PAGE-03 preservation guards)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-06-SUMMARY.md (what already changed and must remain intact)
  </read_first>
  <action>
    Audit the D-13 test after the setup fix and keep its behavior assertions substantive. The test must still install the sustained websocket drop, prove `[data-phx-main]` changes from connected to a disconnected lifecycle class, assert `.tl-reconnect-banner` becomes visible, assert the real `#prune-confirm-content [data-tl-mutating]` control computes `opacity` as `0.55`, assert that same control computes `pointer-events` as `none`, then reconnect and assert the lifecycle class, banner visibility, opacity, and pointer-events all restore. Do not replace these checks with lifecycle-class detection alone. Do not add `test.skip`, `test.fixme`, browser-project conditionals, default-chromium skips, retries that hide a deterministic failure, or weaker assertions that allow the command to pass without proving banner visibility and mutating-control dimming. Keep the existing PAGE-02 overlay/active-state/pager tests and PAGE-03 centering tests in this file; do not delete or narrow them to make the grep target pass faster.
  </action>
  <acceptance_criteria>
    - Source: the D-13 test still targets `[data-phx-main]`, `.tl-reconnect-banner`, and `#prune-confirm-content [data-tl-mutating]`.
    - Source: the D-13 test still asserts visible banner, `opacity: 0.55`, `pointer-events: none`, and reconnect restoration.
    - Source: the spec contains no skip/fixme/only marker for the Phase 178 socket-drop test or the default `chromium` project.
    - Behavior: the required command passes 3/3 projects: `chromium`, `desktop-chromium`, and `mobile-chromium`.
    - Preservation: `mix test test/threadline/operator_surface/` remains green so PAGE-02 footgun and PAGE-03 centering guards are preserved.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n "test\\.(skip|fixme|only)|describe\\.only|\\.skip\\(" examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts</automated>
    <automated>./examples/threadline_phoenix/e2e/run-e2e.sh e2e/tests/operator-phase-178-uat.spec.ts -g "real dropped live socket"</automated>
    <automated>mix test test/threadline/operator_surface/</automated>
  </verify>
  <done>The D-13 proof still proves real reconnect UI behavior, the official command passes all three projects including default chromium, and existing PAGE-02/PAGE-03 source guards remain green.</done>
</task>

</tasks>

<source_audit>
| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 178 | Stress all operator pages including LiveView reconnect; eliminate footguns; preserve centering fix | 178-07 | COVERED | Gap closure targets the one failed reconnect proof while preserving already-green PAGE-02/PAGE-03 evidence. |
| REQ | PAGE-01 | 11-page stress matrix includes a real LiveView reconnect proof | 178-07 | COVERED | New work centers on the D-13 command passing 3/3. |
| REQ | PAGE-02 | Named footgun classes remain eliminated | 178-07 | COVERED | Regression guard only via existing e2e cells and operator-surface tests; no footgun scope reopened. |
| REQ | PAGE-03 | Transaction desktop centering remains fixed | 178-07 | COVERED | Regression guard only via existing style/e2e centering tests; no centering scope reopened. |
| RESEARCH | Pitfall 3 | Socket-drop tests must use auto-retrying assertions and sustained disconnects | 178-07 | COVERED | D-13 assertions stay positive; setup waits for LiveView connection before modal click. |
| RESEARCH | zero new deps | No packages or production JS | 178-07 | COVERED | E2E spec-only expected edit; existing wrapper and Playwright harness used. |
| RESEARCH | prune enforcement boundary | Client affordance is not authorization | 178-07 | COVERED | Plan explicitly forbids server prune enforcement edits for the helper race. |
| CONTEXT | D-01 | Tier A proves structure; Tier B proves representative real-engine sample | 178-07 | COVERED | Official Tier B command is the closure proof. |
| CONTEXT | D-02 | Tier B sampling rule includes one reconnect spec | 178-07 | COVERED | The existing reconnect spec is repaired, not expanded into a broader matrix. |
| CONTEXT | D-03 | Real LiveViews own reconnect/socket-drop proof | 178-07 | COVERED | Helper drives `/audit/policy/retention`, not a static fixture. |
| CONTEXT | D-04 | Ledger ratchet for pages | prior plans | COVERED | Existing PAGE-01 ledger work is preserved and not replanned. |
| CONTEXT | D-05 | Guard-first detectors | prior plans, 178-07 | COVERED | Existing guards preserved; no weakening of D-13 assertions. |
| CONTEXT | D-06 | Layered global/per-page detectors | prior plans, 178-07 | COVERED | Operator-surface suite remains required. |
| CONTEXT | D-07 | Reuse existing infra | 178-07 | COVERED | Uses existing `run-e2e.sh`, Playwright spec, and ExUnit guards. |
| CONTEXT | D-08 | Transaction centering root cause | prior plans, 178-07 | COVERED | Preservation only; no centering refactor. |
| CONTEXT | D-09 | `justify-self: center` centering fix | prior plans, 178-07 | COVERED | Preservation via style/e2e tests. |
| CONTEXT | D-09b | Inner short-content alignment optional | none | EXCLUDED | Deferred/opportunistic idea; not part of this reconnect blocker. |
| CONTEXT | D-10 | Shared shell and one banner mount | prior plans, 178-07 | COVERED | Source read and regression suite preserve mount. |
| CONTEXT | D-11 | Corrected lifecycle anchor is `[data-phx-main]` with `.threadline-ui` descendant scoping | 178-07 | COVERED | Positive assertions depend on the corrected selector source from 178-06. |
| CONTEXT | D-12 | Real mutating controls carry `data-tl-mutating` | prior plans, 178-07 | COVERED | D-13 targets the real prune submit inside the modal. |
| CONTEXT | D-13 | Real socket-drop Playwright proof | 178-07 | COVERED | Primary blocker and primary closure command. |
| CONTEXT | D-14 | Match 171-177 conventions; do not pre-empt Phase 179/180 | 178-07 | COVERED | Existing file/test organization preserved; no copy/IA or formal accessibility sweep. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright -> example LiveView app | The e2e test clicks real operator UI and observes real browser-computed state; it must not inject a fake modal state. |
| Browser affordance -> server prune event | `data-tl-mutating` dimming is a client affordance only; server-side prune authorization and typed confirmation remain the security boundary. |
| LiveView socket lifecycle -> CSS UI state | Phoenix client lifecycle classes on `[data-phx-main]` drive banner visibility and disabled affordance styling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-07-01 | Tampering | `openPruneModal` helper | mitigate | The helper must wait for real LiveView connection and click the real button; it must not remove classes, set assigns, or inject modal state. |
| T-178-07-02 | Spoofing | socket-drop proof | mitigate | Preserve assertions against real `[data-phx-main]` lifecycle classes, visible `.tl-reconnect-banner`, and computed `[data-tl-mutating]` styles. |
| T-178-07-03 | Elevation of Privilege | retention prune flow | mitigate | Do not change `authorize_prune`, typed confirmation, `Plug.Crypto.secure_compare`, fail-closed handling, or audit-before-action; client dimming remains UX only. |
| T-178-07-SC | Tampering | package installs | mitigate | No package-manager installs; use existing project e2e wrapper and dependencies. |
</threat_model>

<verification>
Primary closure proof:
- `./examples/threadline_phoenix/e2e/run-e2e.sh e2e/tests/operator-phase-178-uat.spec.ts -g "real dropped live socket"` must pass all 3 projects, including default `chromium`.

Regression preservation:
- `mix test test/threadline/operator_surface/` must pass.
- `rg -n "\\[data-phx-main\\]\\.phx-(loading|error|client-error).*\\.threadline-ui .*tl-reconnect-banner|\\[data-phx-main\\]\\.phx-(loading|error|client-error).*\\[data-tl-mutating\\]" lib/threadline/operator_surface/style.ex` must still find the corrected selector family from 178-06.
- `! rg -n "\\.threadline-ui\\.phx-(loading|error|client-error)|body\\.phx-|\\.phx-disconnected" lib/threadline/operator_surface/style.ex lib/threadline/operator_surface/ui.ex lib/threadline/operator_surface/live` must remain clean.
</verification>

<success_criteria>
Phase 178 is closure-ready when the default `chromium`, `desktop-chromium`, and `mobile-chromium` projects all pass the documented real socket-drop command; the test reaches and preserves the positive D-13 assertions; PAGE-02 footgun and PAGE-03 centering guards remain green; and the summary records that production prune enforcement, capture, semantics, runtime dependencies, production JavaScript, Phase 179 microcopy/IA, and Phase 180 accessibility closeout scope were not changed.
</success_criteria>

## Artifacts this phase produces

- Modified `examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts`:
  - hardened `openPruneModal(page)` or directly equivalent helper logic that waits for `[data-phx-main].phx-connected` before clicking the real "Run retention prune" button;
  - preserved D-13 test assertions for `[data-phx-main]` lifecycle flip, visible `.tl-reconnect-banner`, `#prune-confirm-content [data-tl-mutating]` `opacity: 0.55`, `pointer-events: none`, and reconnect restoration;
  - no project skip/conditional bypass for default `chromium`.
- New `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-07-SUMMARY.md` created by the executor with the required command output and preservation notes.
- No expected production-code artifact. If the executor proves a real production modal event defect after a connected click, the summary must name the exact production path and symbol changed and must explicitly confirm `authorize_prune`, typed confirmation, `secure_compare`, fail-closed scope, and audit-before-action were preserved.

<output>
Create `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-07-SUMMARY.md` when done. Include the exact output summary for `./examples/threadline_phoenix/e2e/run-e2e.sh e2e/tests/operator-phase-178-uat.spec.ts -g "real dropped live socket"`, whether all three projects passed, and confirmation that PAGE-02/PAGE-03 guards and prune enforcement were preserved.
</output>
