---
phase: 178-per-page-flow-stress-pass-all-11-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/operator_surface/style_contract_test.exs
  - test/threadline/operator_surface/component_contract_test.exs
  - test/threadline/operator_surface/stress_fixtures_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03]
tags: [operator-surface, stress, footgun, tier-a, tier-b, wave-0, guard-first]

must_haves:
  truths:
    - "Per D-05, a failing Tier A detector exists for every one of the 11 named footgun classes and fails on today's surface"
    - "Per D-09, a Tier A assertion requires `.tl-container` to carry `justify-self: center` and fails on today's surface (style.ex:675-678 has no justify-self)"
    - "Per RESEARCH Pitfall 1 / Open Q1, a Tier A assertion requires `.tl-home` to carry `justify-self: center` and fails on today's surface (the PAGE-03 latent twin, style.ex:689-692)"
    - "Per D-13, a real socket-drop Tier B spec scaffold (operator-phase-178-uat.spec.ts) exists using the window.liveSocket.disconnect() ladder, replacing the 177 inject-probe"
    - "Per D-10, a Tier A assertion requires reconnect_banner to be mounted exactly once in a shared shell above #tl-main and fails on today's surface (no shared shell, no mounted banner)"
    - "Per D-04, a stress_fixtures_test assertion requires the 11 reserved page.<x>.reserved entries to be fixture-backed current path stories and fails on today's surface (still reserved baselines)"
  prohibitions:
    - "MUST NOT add any production code (no style.ex, ui.ex, or LiveView edits) — Wave 0 is RED scaffolds only"
    - "MUST NOT introduce Floki/LazyHTML tree traversal — use rendered_to_string + =~ substring assertions only (RESEARCH Pitfall 2)"
    - "MUST NOT add a new --tl-* token or any new runtime dependency (v1.37 invariant)"
    - "MUST NOT key any reconnect assertion off `<body>` or the legacy `.phx-disconnected` class (D-11)"
    - "MUST NOT add pixel-diff / screenshot-baseline assertions (baseline-free, deterministic)"
  artifacts:
    - path: "examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts"
      provides: "Tier B spec scaffold: centering geometry, footgun sweep, real socket-drop"
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "Tier A RED guards: justify-self on .tl-container + .tl-home; per-role contrast"
  key_links:
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "reads style.ex source, asserts justify-self:center on .tl-container and .tl-home"
      pattern: "justify-self.*center"
---

<objective>
Wave 0 of Phase 178: lay the guard-first RED detectors and the Tier B spec scaffold that the later waves turn green. This is the 171–177 ratchet ethos (D-05): write the failing detector first, watch it fail on today's surface, then fix to green in a later wave. Zero production code lands in this plan.

Purpose: Make "fixed but silently regressed" structurally impossible — every footgun class, the centering bug (and its latent Home twin), the reconnect-banner mount, and the page-story ledger conversion become permanent CI assertions that are RED today.
Output: Extended Tier A contract tests (style_contract_test, component_contract_test, stress_fixtures_test) plus a new Tier B spec file (operator-phase-178-uat.spec.ts), all failing against the current surface by design.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED Tier A guards — PAGE-03 centering (and latent Home twin) + per-role contrast footguns #10/#11</name>
  <files>test/threadline/operator_surface/style_contract_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/style_contract_test.exs (the contrast_ratio/2 + composite/2 engine at :697-854; the existing source-scan idiom)
    - lib/threadline/operator_surface/style.ex:673-695 (.tl-container at :675-678 and .tl-home at :689-692 — both carry max-width:1000px; margin:0 auto, NEITHER has justify-self today)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md § "Pitfall 1" (the .tl-home latent twin) and § "Code Examples" (contrast detector reuse)
  </read_first>
  <behavior>
    - RED: asserting `.tl-container` block source contains `justify-self: center` fails (no such rule at style.ex:675-678 today) — per D-09
    - RED: asserting `.tl-home` block source contains `justify-self: center` fails (no such rule at style.ex:689-692 today) — per RESEARCH Pitfall 1 / Open Q1; this is the explicit latent-twin guard
    - RED-or-GREEN-documented: per-text/bg-role contrast assertions extended to cover any role pairing not yet asserted; any pairing below 4.5 (AA) on either lane fails — per footgun #10/#11. If all current pairings already pass, document that the new coverage is GREEN-confirming (the detector still becomes a permanent guard)
  </behavior>
  <action>
    Add a `style_contract_test` assertion that reads style.ex source and requires the `.tl-container` selector block to contain `justify-self: center` while retaining `max-width: 1000px` (D-09). Add a SEPARATE assertion requiring the `.tl-home` selector block to contain `justify-self: center` (RESEARCH Pitfall 1 — the latent twin; this is the visible, deliberate guard the latent-Home-twin decision requires). Both fail today (style.ex:675-678 and :689-692 have only margin:0 auto). Extend the existing `contrast_ratio/2`/`composite/2` coverage so every operator-surface text-token-on-background-token pairing named in 178-UI-SPEC § Color (muted/meta/label/status text on bg/surface/surface-raised/surface-hover, both dark and light lanes via composite over the per-mode opaque base) is asserted >= 4.5 — reuse the engine at :723,751-773, do NOT build a new calculator (footgun #10/#11). Scope each negative source-scan with `String.split` on the selector so a sibling rule cannot false-pass. Do NOT edit style.ex.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs 2>&1 | grep -E "justify-self|failure|test" | tail -20</automated>
  </verify>
  <done>The justify-self assertions for `.tl-container` AND `.tl-home` both FAIL against current style.ex; the per-role contrast assertions are present (failing only if a real AA violation exists today, otherwise GREEN-confirming and documented as such in the SUMMARY). No style.ex edit. `mix format --check-formatted` clean for the test file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED Tier A guards — footgun structural detectors + reconnect-banner-mounted-once shell contract</name>
  <files>test/threadline/operator_surface/component_contract_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/component_contract_test.exs (rendered_to_string + =~ idiom at :30-55; z-order ascending guard at :243-260; reconnect structural at :267-275)
    - lib/threadline/operator_surface/ui.ex:1063-1075 (reconnect_banner/1 + Pitfall-6 mutating-link aria-disabled/tabindex contract)
    - lib/threadline/operator_surface/live/transaction_live.ex:100 (a representative `<main id="tl-main" class="tl-page tl-container" tabindex="-1">` wrapper — note there is NO shared shell and NO reconnect_banner above #tl-main today)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md § "Interaction & Footgun Contract" (the 11 classes table) and § Color "Reconnect / offline state"
  </read_first>
  <behavior>
    - RED: rendering each of the 11 `/audit/*` page render functions (or their stress story preview) and asserting the reconnect_banner marker (`tl-reconnect-banner`, role=status) appears exactly once above `#tl-main` inside `.threadline-ui` fails today (no shared shell mounts it) — per D-10/D-11
    - RED: per-footgun structural detectors that today's surface fails — e.g. tab/nav active-state carries `aria-current="page"`/`aria-selected` PLUS a non-color cue (footgun #8); non-interactive elements expose no `cursor:pointer` (footgun #5); disabled controls carry real `disabled`/`aria-disabled`+`not-allowed` (footgun #7); pager disabled-at-edge/hide-at-zero (footgun #9) — write the ones that genuinely fail; for any that already pass today, register the permanent guard and note GREEN-confirming
    - The z-order ascending guard (#2) and existing reconnect structural (#3 hooks) are extended, not duplicated
  </behavior>
  <action>
    In `component_contract_test`, add a "reconnect banner mounted once in shared shell" assertion: render the operator pages (iterate the page render functions or render each via the stress `page.<x>` story preview) and require `tl-reconnect-banner` with `role="status"` to appear exactly once, positioned above the `id="tl-main"` element and inside `class="threadline-ui"` (D-10/D-11). Assert via `rendered_to_string(...) =~` substring + ordering checks (parser-agnostic, RESEARCH Pitfall 2); NEVER reference `<body>` or `.phx-disconnected` (D-11). Add per-footgun structural detectors for the classes whose structural half is Tier-A-assertable (#5 no cursor:pointer on non-interactive, #7 disabled affordance, #8 active-state aria-current + non-color cue, #9 pager edges) using the existing z-order/reconnect idioms — extend, do not reinvent (D-07). Each detector that genuinely fails today is the RED deliverable; document any GREEN-confirming guard explicitly. Use `grep -v '^#'`-style scoping if scanning source so comment text cannot self-invalidate. Do NOT add production code.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/component_contract_test.exs 2>&1 | tail -25</automated>
  </verify>
  <done>The "reconnect banner mounted once above #tl-main" assertion FAILS against today's surface (no shared shell). Footgun structural detectors are present, each genuinely-failing one RED, each GREEN-confirming one documented. No `<body>`/`.phx-disconnected` reference. No production code. Format clean.</done>
</task>

<task type="auto">
  <name>Task 3: RED page-story ledger contract + Tier B spec scaffold (centering geometry, footgun sweep, real socket-drop)</name>
  <files>test/threadline/operator_surface/stress_fixtures_test.exs, examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts</files>
  <read_first>
    - test/threadline/operator_surface/stress_fixtures_test.exs (the reserved-story assertions)
    - lib/threadline/operator_surface/stress_fixtures.ex:98-118 (@page_stories 11 reserved tuples + @current_page_stories home.happy/timeline.empty) and :405-414 (current_page_story_maps builder)
    - .planning/design-system-ledger.json:495-720 (the 11 page.<x>.reserved entries, score 35, status reserved) and :1091-1210 (ratchet locked_ids/minimum_scores/resets)
    - examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts (boundingBox/within-viewport/focus helpers at :54,83 to extend); examples/threadline_phoenix/priv/static/assets/app.js:12 (window.liveSocket exposed); examples/threadline_phoenix/e2e/.../playwright.config.ts (dark + desktop-chromium-light + reducedMotion lanes)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md § "Pattern 3" (socket-drop ladder) and § "Pitfall 4" (centering tolerance / nav-column offset)
  </read_first>
  <action>
    In `stress_fixtures_test`, add an assertion that each of the 11 `page.<x>.reserved` stories is expected to become a fixture-backed CURRENT path story carrying the 7 paths (happy/empty/loading/error/permission/boundary/advanced) — RED today because the registry still holds them as reserved baselines (D-04). Write the assertion against the eventual shape (story status `current`/`baseline`, path-keyed cases) so it is the binding RED target Plan 04 turns green; do NOT mutate stress_fixtures.ex or the ledger here. Create `examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts` as a Tier B scaffold (tests marked failing/`.fixme` is acceptable for scaffolding, but the socket-drop and centering specs must be authored, not stubbed) covering: (a) PAGE-03 centering geometry — load `/audit/transactions/:id` at 1024 and 1440, measure `.tl-container` bbox, assert horizontally centered within grid column 2 (measure symmetric gutters within the content column, NOT raw viewport/2 — RESEARCH Pitfall 4), tolerance left to D-14 discretion (document the chosen value); ALSO author a Home (`/audit`) centering cell at 1024+1440 for the latent twin; (b) the footgun sweep iterating `/audit/*` routes using the extended 177 boundingBox/within-viewport + accessibility expectFocused/Esc helpers (D-06); (c) the real socket-drop spec (D-13): `window.liveSocket.disconnect()` → assert real `.tl-reconnect-banner` visible (allow `.phx-loading` OR `.phx-error`, RESEARCH Pitfall 3) and a `[data-tl-mutating]` prune control computes `pointer-events:none`/`opacity:0.55`, then `window.liveSocket.connect()` → banner hidden, control re-enabled; drive on `/audit/policy/retention` after opening the prune modal; document the `setOffline(true)` / `routeWebSocket(..., ws => ws.close())` fallback ladder in a comment. NO pixel-diff. The ~66-cell sampling rule (D-02): 320 floor + 1440 ceiling, happy + worst-case, dark + light, one keyboard, one reduced-motion, one reconnect — wire the lanes via the existing playwright projects.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_fixtures_test.exs 2>&1 | tail -15 && test -f examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts && echo "SPEC_PRESENT"</automated>
  </verify>
  <done>The page-story conversion assertion FAILS against the current reserved registry (Plan 04 turns it green). The Tier B spec file exists with authored (not empty) centering, footgun-sweep, and socket-drop specs honoring the D-02 sampling lanes, the column-2 centering measurement (Pitfall 4), and the disconnect ladder (Pitfall 3). No pixel-diff, no new dep.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
NEW symbols/files created by this plan (excluded from drift verification):
- `examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts` (new Tier B spec file)
- New test assertions in `style_contract_test.exs` (justify-self guards for `.tl-container` and `.tl-home`; extended per-role contrast coverage)
- New test assertions in `component_contract_test.exs` (reconnect-banner-mounted-once contract; footgun structural detectors #5/#7/#8/#9)
- New test assertion in `stress_fixtures_test.exs` (11 page-story reserved→current conversion target)
</artifacts_this_phase_produces>

<verification>
- `mix test test/threadline/operator_surface/` shows the new Phase-178 RED assertions failing by design; no PRE-EXISTING test regresses (compare failing-test list to the new assertions only).
- `mix format --check-formatted` clean for all edited test files.
- The Tier B spec file parses (TypeScript) and references `window.liveSocket`, column-2 centering, and the disconnect ladder.
</verification>

<success_criteria>
Every later-wave deliverable has a binding RED target authored here: PAGE-03 centering (+ Home twin), the 11 footgun classes (Tier A structural halves + Tier B sweep), the page-story ledger conversion, and the SEED-005 reconnect-banner-mounted-once contract + real socket-drop. RED is the deliverable. Zero production code, zero new dep, zero new token.
</success_criteria>

<output>
Create `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-01-SUMMARY.md` when done, listing exactly which assertions are RED-by-design vs GREEN-confirming, and which wave/plan turns each green.
</output>
