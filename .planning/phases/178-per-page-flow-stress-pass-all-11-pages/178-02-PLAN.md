---
phase: 178-per-page-flow-stress-pass-all-11-pages
plan: 02
type: execute
wave: 2
depends_on: [178-01]
files_modified:
  - lib/threadline/operator_surface/ui.ex
  - lib/threadline/operator_surface/live/start_live.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
  - lib/threadline/operator_surface/live/row_history_live.ex
  - lib/threadline/operator_surface/live/actor_live.ex
  - lib/threadline/operator_surface/live/coverage_live.ex
  - lib/threadline/operator_surface/live/evidence_live.ex
  - lib/threadline/operator_surface/live/policy_redaction_live.ex
  - lib/threadline/operator_surface/live/retention_history_live.ex
  - lib/threadline/operator_surface/live/export_status_live.ex
  - lib/threadline/operator_surface/live/stress_live.ex
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03]
tags: [operator-surface, seed-005, shared-shell, reconnect-banner, data-tl-mutating, integration]

must_haves:
  truths:
    - "Per D-10, ONE internal `@doc false` shell/chrome function component exists in ui.ex and all 11 LiveViews render their chrome through it (no 11-way duplication of `<div class=threadline-ui>…surface_header…<main id=tl-main>`)"
    - "Per D-10/D-11, `reconnect_banner/1` is mounted exactly once inside the shared shell, above `#tl-main`, inside `.threadline-ui` — keyed off `.threadline-ui.phx-loading`/`.phx-error`"
    - "Per D-12, the genuine state-changers carry `data-tl-mutating`: the prune_now submit (retention_history_live.ex:299), the save-view submit (timeline_live.ex:703), and the delete-view button (timeline_live.ex:720)"
    - "Per D-12 + Pitfall-6, download/export LINKS (timeline CSV/JSON/NDJSON, export download) carry `data-tl-mutating` PLUS `aria-disabled=\"true\" tabindex=\"-1\"` (links cannot take HTML disabled)"
    - "Per RESEARCH Security note, the server-side prune enforcement in retention_history_live.ex handle_event(\"prune_now\", …) is byte-for-byte unchanged — the shell wraps chrome only"
  prohibitions:
    - "MUST NOT add `data-tl-mutating` to the no-op export 'queue' stubs (timeline_live.ex:652, export_status_live.ex:174) — no real mutation (D-12)"
    - "MUST NOT wire any control in policy_redaction_live.ex — it is read-only/config-driven (D-12)"
    - "MUST NOT key the reconnect banner off `<body>` or the legacy `.phx-disconnected` class (D-11)"
    - "MUST NOT add a public/host-facing component API — the shell stays `@doc false` private in ui.ex (v1.31 freeze)"
    - "MUST NOT route the prune mutating event around the existing server-side secure_compare + authz re-check (Security: affordance is not enforcement)"
    - "MUST NOT add a new --tl-* token or new runtime dependency (v1.37 invariant)"
  artifacts:
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "the new shared shell/chrome function component + reconnect_banner mounted once"
      contains: "def shell"
  key_links:
    - from: "lib/threadline/operator_surface/live/start_live.ex"
      to: "lib/threadline/operator_surface/ui.ex"
      via: "renders chrome via the shared shell component instead of inline `<div class=threadline-ui>`"
      pattern: "UI.shell|<\\.shell"
    - from: "lib/threadline/operator_surface/ui.ex"
      to: "lib/threadline/operator_surface/style.ex"
      via: "reconnect_banner CSS keyed off .threadline-ui.phx-loading/.phx-error (style.ex:3405-3424, already correct)"
      pattern: "tl-reconnect-banner"
---

<objective>
Land SEED-005: extract the duplicated `threadline-ui` + `#tl-main` chrome wrapper into ONE internal shell/chrome function component, route all 11 LiveViews through it, mount `reconnect_banner/1` once above `#tl-main`, and wire `data-tl-mutating` onto the genuine state-changers (plus `aria-disabled`+`tabindex=-1` on download links). This turns the Wave-0 "reconnect banner mounted once in shared shell" RED contract green and makes the 177 reconnect work real (the banner+CSS were proven to compute but nothing mounted them).

Purpose: Give the reconnect banner a single structural home and kill 11-way drift (D-10); make connection-lifecycle dimming actually fire on real controls (D-12).
Output: A shared shell component in ui.ex; 11 LiveViews converged onto it; the prune/save-view/delete-view controls and download links carrying the mutating affordance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md
@.planning/phases/177-component-groups-meta-components/177-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared shell/chrome component and mount reconnect_banner once</name>
  <files>lib/threadline/operator_surface/ui.ex, test/threadline/operator_surface/component_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/ui.ex:1063-1075 (reconnect_banner/1 + the Pitfall-6 mutating-link contract docs) and the surrounding `@doc false` component idiom (page_header/1, surface_header composition)
    - lib/threadline/operator_surface/live/transaction_live.ex (the `<div class="threadline-ui">…<Style.css/>…surface_header…<main id="tl-main" …>` wrapper — representative of all 11)
    - lib/threadline/operator_surface/live/start_live.ex and coverage_live.ex (two more wrapper instances — confirm the shared slots: the surface_header attrs current/base_path/scoped/*_enabled, the Style.css mount, the #tl-main inner_block)
    - lib/threadline/operator_surface/style.ex:3405-3424 (reconnect/offline CSS — already correct, do not edit) and :3891-3936 (grid shell; #tl-main is grid-column:2)
    - test/threadline/operator_surface/component_contract_test.exs (the Wave-0 "reconnect banner mounted once" RED assertion this task turns green)
  </read_first>
  <behavior>
    - GREEN after this task: the Wave-0 reconnect-banner-mounted-once assertion passes — `tl-reconnect-banner` with role=status appears exactly once, above `#tl-main`, inside `.threadline-ui`
    - Each of the 11 pages still renders its existing surface_header (current page highlight, enabled destinations) and its existing `#tl-main` body content unchanged
    - The shell is a single `@doc false` private function component; no public API
  </behavior>
  <action>
    Add an `@doc false` shell/chrome function component to ui.ex (name at D-14 discretion, e.g. `shell/1`) that renders, in order: the `<div class="threadline-ui">` root, the `Style.css` mount, the existing `surface_header` (taking the same attrs the 11 pages pass today — `current`, `base_path`, `scoped`, `coverage`/`policy`/`evidence`/`exports`_enabled, `error`), `reconnect_banner/1` mounted ONCE directly above `#tl-main` and inside `.threadline-ui` (D-10/D-11), then `<main id="tl-main" …>` wrapping an `inner_block` slot. Preserve the per-page `<main>` class list (e.g. transaction passes `tl-page tl-container`; home passes `tl-page tl-home`) via a `:main_class` attr so the centering wrappers survive. Reconnect CSS already exists at style.ex:3405-3424 keyed off `.threadline-ui.phx-loading/.phx-error` — do NOT edit style.ex; do NOT reference `<body>` or `.phx-disconnected` (D-11). Keep the component `@doc false` (no public API, v1.31 freeze). Extend the component_contract reconnect test only if needed to assert the single mount point.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/component_contract_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>The shared shell component exists in ui.ex (`@doc false`); the Wave-0 reconnect-banner-mounted-once assertion is GREEN; the component carries no public API; style.ex untouched. `mix compile --warnings-as-errors` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route all 11 LiveViews through the shared shell</name>
  <files>lib/threadline/operator_surface/live/start_live.ex, lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/operator_surface/live/transaction_live.ex, lib/threadline/operator_surface/live/row_history_live.ex, lib/threadline/operator_surface/live/actor_live.ex, lib/threadline/operator_surface/live/coverage_live.ex, lib/threadline/operator_surface/live/evidence_live.ex, lib/threadline/operator_surface/live/policy_redaction_live.ex, lib/threadline/operator_surface/live/retention_history_live.ex, lib/threadline/operator_surface/live/export_status_live.ex, lib/threadline/operator_surface/live/stress_live.ex</files>
  <read_first>
    - The shell component built in Task 1 (its slot/attr API)
    - All 11 wrappers: lib/threadline/operator_surface/live/{start,timeline,transaction,row_history,actor,coverage,evidence,policy_redaction,retention_history,export_status,stress}_live.ex — each has exactly one `class="threadline-ui"` and one `id="tl-main"` (confirmed: grep shows 11/11)
    - 177-CONTEXT.md (the surface_header/page_header composition the pages already use)
  </read_first>
  <behavior>
    - GREEN: every page's existing rendered output is structurally equivalent (same surface_header state, same `#tl-main` body, same per-page main class) PLUS the single reconnect banner now present
    - No page loses its current-nav highlight, its breadcrumbs, or its `#tl-main` content
  </behavior>
  <action>
    Replace the inline `<div class="threadline-ui">…<Style.css/>…surface_header…<main id="tl-main" class="…">…</main></div>` wrapper in each of the 11 LiveViews with a call to the shared shell component, passing the page's existing surface_header attrs and its existing `<main>` class via `:main_class`, with the former `#tl-main` body moved into the shell's `inner_block` slot. For transaction_live.ex keep `tl-page tl-container` and for start_live.ex keep `tl-page tl-home` so the PAGE-03/Home centering wrappers are preserved (Plan 03 adds the centering fix to the CSS). The stress_live.ex shell may stay bespoke if its chrome differs materially — document the decision; otherwise route it too. Do NOT alter any `handle_event`, mount, or assign logic; this is a render-tree refactor only. Verify each page still compiles and renders by running the full operator_surface suite.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 LiveViews render via the shared shell; the operator_surface suite passes except known cross-plan RED scaffolds (PAGE-03 centering, footgun fixes, page-story ledger — owned by Plans 03/04/05); no `handle_event`/mount logic changed; `mix compile --warnings-as-errors` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire data-tl-mutating onto genuine state-changers + aria-disabled on download links</name>
  <files>lib/threadline/operator_surface/live/retention_history_live.ex, lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/operator_surface/live/export_status_live.ex</files>
  <read_first>
    - lib/threadline/operator_surface/live/retention_history_live.ex:290-303 (the `<form phx-submit="prune_now">` and its `<button type="submit" class="tl-button tl-button--danger">Prune now…</button>` at ~:299) and :67 (the server-side handle_event — leave untouched)
    - lib/threadline/operator_surface/live/timeline_live.ex:694-730 (the `<form id="save-view-form" phx-submit="save-view">` save submit at ~:703; the `phx-click="delete-view"` button at ~:720; the CSV/JSON/NDJSON download links at ~:709-733) and :652 (the no-op export queue stub — SKIP)
    - lib/threadline/operator_surface/live/export_status_live.ex:296 (export download link) and :174 (no-op queue stub — SKIP)
    - lib/threadline/operator_surface/ui.ex:1063-1071 (the Pitfall-6 contract: links get data-tl-mutating + aria-disabled="true" tabindex="-1")
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md § "Pitfall 5" (the exact mutating control set; what to skip)
  </read_first>
  <behavior>
    - GREEN: the real socket-drop Tier B spec (authored in Wave 0) can find `[data-tl-mutating]` on the prune control and observe `pointer-events:none`/`opacity:0.55` while disconnected
    - Buttons (prune_now submit, save-view submit, delete-view) carry `data-tl-mutating`
    - Download/export links carry `data-tl-mutating` AND `aria-disabled="true" tabindex="-1"`
    - No-op stubs and read-only redaction controls carry nothing
  </behavior>
  <action>
    Add `data-tl-mutating` to: the prune_now submit button (retention_history_live.ex ~:299), the save-view submit button (timeline_live.ex ~:703), and the delete-view button (timeline_live.ex ~:720) — these are genuine DB mutations (D-12). Add `data-tl-mutating` PLUS `aria-disabled="true" tabindex="-1"` to the timeline CSV/JSON/NDJSON download links (~:709-733) and the export download link (export_status_live.ex ~:296) — links cannot take HTML `disabled` (Pitfall-6, ui.ex:1066). Do NOT touch the no-op export "queue" stubs (timeline_live.ex:652, export_status_live.ex:174) or anything in policy_redaction_live.ex (read-only, D-12). Do NOT modify the server-side prune enforcement at retention_history_live.ex:67 — `data-tl-mutating` is affordance only, never the gate (Security note). The reconnect/dimming CSS already keys off these attributes at style.ex:3405-3424 (no CSS change needed).
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ 2>&1 | tail -15 && grep -c "data-tl-mutating" lib/threadline/operator_surface/live/retention_history_live.ex lib/threadline/operator_surface/live/timeline_live.ex lib/threadline/operator_surface/live/export_status_live.ex</automated>
  </verify>
  <done>`data-tl-mutating` present on exactly the D-12 control set (3 buttons + the download links); download links also carry `aria-disabled="true" tabindex="-1"`; no-op stubs and redaction untouched; prune server-side enforcement byte-for-byte unchanged; operator_surface suite green except cross-plan RED scaffolds; format + credo + warnings-as-errors clean.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
NEW symbols created by this plan (excluded from drift verification):
- The shared shell/chrome function component in `ui.ex` (name + `:main_class`/`inner_block` slot API per D-14)
- `data-tl-mutating` attribute occurrences on the prune_now/save-view/delete-view controls and the download links (new attribute wiring, not new selectors)
</artifacts_this_phase_produces>

<verification>
- `mix test test/threadline/operator_surface/component_contract_test.exs` — reconnect-banner-mounted-once GREEN.
- `mix test test/threadline/operator_surface/` — green except the documented cross-plan RED scaffolds (PAGE-03 centering, footgun fixes, page-story ledger).
- `mix compile --warnings-as-errors`, `mix format --check-formatted`, `mix credo --strict` (or `mix verify.credo`) clean.
- `mix test test/threadline/brandbook_token_parity_test.exs` (or the parity entrypoint) green — zero new tokens.
- Capture & semantics layers: `git diff --stat` shows no change under those layers.
</verification>

<success_criteria>
One shared shell, banner mounted once, real controls dimmable on disconnect — SEED-005 closed structurally and the Wave-0 reconnect contract GREEN. Server-side prune enforcement untouched; no public API; no new token/dep.
</success_criteria>

<output>
Create `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-02-SUMMARY.md` when done, naming the shell component, its slot API, the exact controls wired, and confirming the prune handler diff is empty.
</output>
