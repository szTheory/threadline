---
phase: 178-per-page-flow-stress-pass-all-11-pages
plan: 04
type: execute
wave: 3
depends_on: [178-01, 178-02]
files_modified:
  - lib/threadline/operator_surface/stress_fixtures.ex
  - .planning/design-system-ledger.json
  - DESIGN-SYSTEM.md
  - test/threadline/operator_surface/stress_fixtures_test.exs
  - test/threadline/operator_surface/stress_ledger_test.exs
autonomous: true
requirements: [PAGE-01]
tags: [operator-surface, page-01, ledger, ratchet, fixtures, stress-stories, tier-a]

must_haves:
  truths:
    - "Per D-04, each of the 11 reserved `page.<x>.reserved` entries becomes a fixture-backed CURRENT path story carrying the 7 paths (happy/empty/loading/error/permission/boundary/advanced)"
    - "Per D-04, each of the 11 page entries' `current_score` is raised toward `target_score: 90` via the upward-only ratchet (>= ratchet_score, >= the minimum_scores floor of 35)"
    - "Per D-04, the two existing baselines `page.home.happy` and `page.timeline.empty` (score 62) are also raised toward 90"
    - "Per D-04, `story_id`/`fixture_key` parity is maintained between stress_fixtures.ex and design-system-ledger.json, and the `DESIGN-SYSTEM.md` projection is refreshed and asserted fresh"
    - "Per D-01, the Wave-0 page-story conversion assertion in stress_fixtures_test is now GREEN; the full structural cartesian (page × path × theme × viewport) renders without loud-fail and carries the right data-state"
  prohibitions:
    - "MUST NOT lower any `current_score` below its `ratchet_score` without an explicit `ratchet.resets` entry + `reset_rationale` (stress_ledger_test:115-120)"
    - "MUST NOT orphan any `page.<x>.reserved` id — every reserved page entry is converted, none left dangling (the 177-05 group precedent)"
    - "MUST NOT add a new --tl-* token or runtime dependency (v1.37 invariant)"
    - "MUST NOT add pixel-diff / screenshot baselines — keep `screenshot_baseline_refs` empty or as-is; assertions are structural (baseline-free)"
    - "MUST NOT touch capture or semantics layers (fixtures are DB-free static data)"
  artifacts:
    - path: "lib/threadline/operator_surface/stress_fixtures.ex"
      provides: "fixture-backed page.<x>.{7 paths} stories for all 11 pages + raised baselines"
    - path: ".planning/design-system-ledger.json"
      provides: "11 page entries converted reserved->current, scores ratcheted toward 90"
    - path: "DESIGN-SYSTEM.md"
      provides: "refreshed Pages projection in parity with the ledger"
  key_links:
    - from: "lib/threadline/operator_surface/stress_fixtures.ex"
      to: ".planning/design-system-ledger.json"
      via: "story_id/fixture_key parity enforced by stress_ledger_test + stress_fixtures_test"
      pattern: "page\\.[a-z-]+\\."
    - from: ".planning/design-system-ledger.json"
      to: "DESIGN-SYSTEM.md"
      via: "projection freshness asserted by the ledger parity test"
      pattern: "page\\."
---

<objective>
Land PAGE-01's ledger worklist: convert the 11 reserved `page.<x>.reserved` entries into fixture-backed per-page path stories (happy/empty/loading/error/permission/boundary/advanced) on `/audit/__stress`, raise their scores toward 90 via the upward-only ratchet, raise the two existing baselines too, maintain story_id/fixture_key parity, and refresh the `DESIGN-SYSTEM.md` projection. This turns the Wave-0 page-story conversion RED target green and proves the full Tier A structural cartesian.

Purpose: PAGE-01 requires every page audited across the path × theme × viewport matrix recorded in the ledger; this is the worklist (D-04). The honesty contract: Tier A proves the FULL structural cartesian here; Tier B (Plan 05 sweep) proves the representative sample.
Output: 11 fixture-backed page stories (7 paths each), ratcheted ledger entries, refreshed projection, GREEN ledger + fixtures tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build fixture-backed per-page path stories for all 11 pages</name>
  <files>lib/threadline/operator_surface/stress_fixtures.ex, test/threadline/operator_surface/stress_fixtures_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/stress_fixtures.ex:4-25 (@required_cases ugly-data library, @theme_modes, @viewports), :98-118 (@page_stories 11 reserved tuples + @current_page_stories home.happy/timeline.empty), :255-271 (stories/0 assembly), :405-414 (current_page_story_maps builder), :195-253 (assigns_for clauses)
    - lib/threadline/operator_surface/live/stress_live.ex:458-595 (ledger_stories, selected_story, assigns_for dispatch, preview_copy — how a story renders on /audit/__stress)
    - .planning/phases/176.../176-CONTEXT.md state taxonomy (the 7 paths map onto empty/loading/error/permission/no-data/boundary; "advanced" = power-user dense path)
    - test/threadline/operator_surface/stress_fixtures_test.exs (the Wave-0 page-story conversion RED target)
  </read_first>
  <behavior>
    - GREEN: each of the 11 pages has fixture-backed stories for the 7 paths (happy/empty/loading/error/permission/boundary/advanced), each resolving to renderable assigns via assigns_for
    - The full structural cartesian (page × path × theme × viewport) renders on /audit/__stress without loud-fail, carrying the correct `data-state`
    - The two existing baselines (page.home.happy, page.timeline.empty) remain valid and are joined by the full path set for those pages
  </behavior>
  <action>
    Convert the 11 `@page_stories` reserved tuples into fixture-backed CURRENT path stories. For each page (actor, coverage, evidence, exports, home, redaction, retention, row-history, shell, timeline, transaction) define the 7 path stories using D-14-discretion IDs/shapes that match the `@required_cases` idiom and the existing `page.home.happy`/`page.timeline.empty` convention — e.g. `page.<x>.happy`, `page.<x>.empty`, `page.<x>.loading`, `page.<x>.error`, `page.<x>.permission`, `page.<x>.boundary`, `page.<x>.advanced`, each mapping to ugly-data cases drawn from @required_cases and a renderable `assigns_for` clause that drives the corresponding data-state (reuse the 176 state taxonomy: empty/loading/error/permission/no-data, plus boundary=pagination/timezone boundary cases and advanced=dense/many). Wire them into `stories/0` (replacing the `reserved_story_maps(@page_stories, "page", 178)` reserved path with a current builder, or extend `current_page_story_maps`), keeping `page.home.happy`/`page.timeline.empty` absorbed (no orphaned ids — the 177-05 group precedent). Ensure `assigns_for` covers every new story id. Do NOT add a new token; fixtures are DB-free static data.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_fixtures_test.exs 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 pages have 7 fixture-backed path stories resolving to renderable assigns; no orphaned `*.reserved` page id; the Wave-0 conversion assertion GREEN; `mix compile --warnings-as-errors` + format clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ratchet the ledger entries and refresh the DESIGN-SYSTEM.md projection</name>
  <files>.planning/design-system-ledger.json, DESIGN-SYSTEM.md, test/threadline/operator_surface/stress_ledger_test.exs</files>
  <read_first>
    - .planning/design-system-ledger.json:495-720 (the 11 page.<x>.reserved entries: status reserved, current_score 35, ratchet_score 35, owner_phase 178; the two baselines page.home.happy/page.timeline.empty at 62) and :1091-1210 (ratchet block: locked_ids includes all 13 page ids, minimum_scores page.*.reserved=35 + baselines=62, resets={})
    - test/threadline/operator_surface/stress_ledger_test.exs:101-126 (upward-only ratchet rule), :128+ (locked IDs + minimum scores enforcement)
    - DESIGN-SYSTEM.md (the Pages projection section that must stay parity-fresh with the ledger)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md § "Pitfall 6" (raising below ratchet_score requires a reset)
  </read_first>
  <behavior>
    - GREEN: every page entry's `current_score` is raised toward `target_score:90`, staying >= `ratchet_score` and >= its `minimum_scores` floor (no reset needed since we only raise)
    - GREEN: `status` flips reserved->current (or baseline) for the converted entries; story_id/fixture_key parity holds with stress_fixtures.ex
    - GREEN: DESIGN-SYSTEM.md Pages projection matches the ledger (freshness assertion passes)
  </behavior>
  <action>
    For each of the 11 page ledger entries, flip `status` from `reserved` to `current` (or `baseline`, matching the home.happy/timeline.empty convention), raise `current_score` monotonically toward `target_score: 90` (upward-only — never below `ratchet_score` 35 or the `minimum_scores` floor; since we only raise, no `ratchet.resets` entry is needed, Pitfall 6), and update `ratchet_score` to the new locked floor as appropriate. Raise `page.home.happy` and `page.timeline.empty` from 62 toward 90 as well (D-04). If multiple path stories per page each need a ledger row, add them with matching `story_id`/`fixture_key`/`stress_path` parity to the new fixtures and register them in `locked_ids`/`minimum_scores`/`required_inventory.groups`(pages) so the locked-IDs test passes — mirror the 177-05 group-entry precedent. Refresh the `DESIGN-SYSTEM.md` Pages projection so the ledger<->projection freshness assertion stays green. Keep `screenshot_baseline_refs` baseline-free (no pixel-diff). Do NOT lower any score.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_ledger_test.exs 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 page entries converted to current/baseline with scores ratcheted upward toward 90; baselines raised; story_id/fixture_key parity GREEN; DESIGN-SYSTEM.md projection fresh; no score lowered; no reset entry required; ledger ratchet + locked-IDs + parity tests all GREEN.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
NEW symbols created by this plan (excluded from drift verification):
- 77 new fixture-backed page path story IDs (11 pages × 7 paths, `page.<x>.{happy,empty,loading,error,permission,boundary,advanced}`) in stress_fixtures.ex with matching `assigns_for` clauses
- Converted/added ledger entries in design-system-ledger.json (status current/baseline, scores raised) + their `locked_ids`/`minimum_scores` registrations
- Refreshed Pages projection rows in DESIGN-SYSTEM.md
</artifacts_this_phase_produces>

<verification>
- `mix test test/threadline/operator_surface/stress_fixtures_test.exs` + `stress_ledger_test.exs` GREEN (conversion + ratchet + locked-IDs + parity).
- `mix verify.test` GREEN — full Tier A structural cartesian renders without loud-fail.
- `mix compile --warnings-as-errors`, `mix format --check-formatted`, brand-token parity all green; zero new token.
- No `page.<x>.reserved` id remains orphaned (grep the registry).
</verification>

<success_criteria>
All 11 pages are fixture-backed across the 7 paths and recorded in the ledger at ratchet target; the DESIGN-SYSTEM.md projection is fresh; Tier A proves the full structural cartesian. PAGE-01's ledger worklist is complete (Tier B sample lands in Plan 05).
</success_criteria>

<output>
Create `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-04-SUMMARY.md` when done, listing the new story IDs per page, the score raises, and confirming no orphaned reserved id and no score lowered.
</output>
