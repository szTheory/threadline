---
phase: 178-per-page-flow-stress-pass-all-11-pages
plan: 06
type: execute
wave: 5
depends_on: [178-05]
files_modified:
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/ui.ex
  - test/threadline/operator_surface/component_contract_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts
  - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [PAGE-01, PAGE-02, PAGE-03]
requirements_addressed: [PAGE-01, PAGE-02, PAGE-03]
tags: [operator-surface, gap-closure, reconnect, liveview, tier-a, tier-b]

must_haves:
  truths:
    - "PAGE-01 reconnect dimension closes: a genuine LiveView socket drop reveals the reconnect banner and disables data-tl-mutating affordances."
    - "D-11 is corrected: lifecycle classes are treated as belonging to [data-phx-main], with .threadline-ui as the scoped descendant, not as the class-bearing element."
    - "D-10 shell mount remains intact: the reconnect banner is still mounted exactly once above #tl-main inside the shared shell."
    - "D-12 control scope remains intact: prune/save/delete and download controls keep their existing data-tl-mutating wiring; server prune enforcement remains server-side."
    - "D-13 real-engine proof is positive: banner visible and mutating control computed opacity 0.55 plus pointer-events none during disconnect, then restored after reconnect."
    - "PAGE-02 footgun and PAGE-03 centering truths stay green while the reconnect selector changes."
  artifacts:
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "Correct reconnect reveal selectors anchored on [data-phx-main].phx-* with .threadline-ui descendant scoping."
      contains: "[data-phx-main].phx-loading .threadline-ui .tl-reconnect-banner"
    - path: "test/threadline/operator_surface/component_contract_test.exs"
      provides: "Tier A selector-correctness guard for the real class-bearing container and scoped descendant selectors."
    - path: "examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts"
      provides: "Tier B real socket-drop positive assertion for banner visibility and data-tl-mutating computed style."
    - path: ".planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md"
      provides: "Corrected D-11 planning decision replacing the contradicted anchor claim."
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement status closeout for PAGE-01/PAGE-03 after reconnect proof passes, while preserving PAGE-02 complete."
  key_links:
    - from: "lib/threadline/operator_surface/style.ex"
      to: "examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts"
      via: "real Chromium socket-drop computes banner display and mutating-control style from the corrected [data-phx-main].phx-* selectors"
      pattern: "\\[data-phx-main\\]\\.phx-(loading|error|client-error).*\\.tl-reconnect-banner"
    - from: "test/threadline/operator_surface/component_contract_test.exs"
      to: "lib/threadline/operator_surface/ui.ex"
      via: "source-order guard preserves one banner mount above #tl-main inside .threadline-ui while selector guard moves lifecycle anchoring to [data-phx-main]"
      pattern: "data-phx-main"
    - from: ".planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md"
      to: "lib/threadline/operator_surface/style.ex"
      via: "corrected D-11 decision names the actual selector anchor implemented in CSS"
      pattern: "D-11"
---

<objective>
Close the single blocking Phase 178 verification gap: reconnect banner visibility and data-tl-mutating dimming do not engage on a genuine LiveView socket drop because the reveal selector is anchored to the wrong DOM node.

Purpose: PAGE-01 explicitly includes the LiveView reconnect stress dimension. Plans 178-01 through 178-05 shipped the shell, mutating-control wiring, page stories, PAGE-02 footgun fixes, and PAGE-03 centering, but verification proved D-11's selector premise false in the real app. This closure corrects D-11, re-anchors CSS and tests to the class-bearing [data-phx-main] container, and requires the real-engine positive proof D-13 originally intended.
Output: Corrected CSS selector family, stronger Tier A selector-correctness guard, positive Tier B socket-drop proof, corrected D-11 context, and requirement status closeout without touching capture or semantics layers.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-RESEARCH.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-UI-SPEC.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VALIDATION.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VERIFICATION.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-02-SUMMARY.md
@.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-05-SUMMARY.md
@lib/threadline/operator_surface/style.ex
@lib/threadline/operator_surface/ui.ex
@test/threadline/operator_surface/component_contract_test.exs
@examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts
</context>

<!-- planner-discipline-allow: .phx-disconnected -->
<!-- planner-discipline-allow: <body> -->
<!-- planner-discipline-allow: body.phx- -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-anchor reconnect CSS to the real LiveView container</name>
  <files>lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/ui.ex</files>
  <read_first>
    - lib/threadline/operator_surface/style.ex:3388-3432 (current reconnect banner + data-tl-mutating CSS, currently anchored to the wrong element)
    - lib/threadline/operator_surface/ui.ex:1048-1112 and :1128-1172 (reconnect_banner/1 docs and UI.shell/1 docs, currently repeating the contradicted D-11 premise)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VERIFICATION.md (gap reason: lifecycle classes attach to [data-phx-main], not .threadline-ui)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-05-SUMMARY.md (threat_flag: reconnect-anchor-gap and real-engine finding)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md (D-10, D-11, D-12, D-13; D-11 must be corrected)
  </read_first>
  <action>
    Update the reconnect/offline selector family in `style.ex` so the lifecycle-class anchor is `[data-phx-main]`, with `.threadline-ui` retained only as the scoped descendant that contains the Threadline shell. Replace the banner reveal selector with `[data-phx-main].phx-loading .threadline-ui .tl-reconnect-banner`, `[data-phx-main].phx-error .threadline-ui .tl-reconnect-banner`, and `[data-phx-main].phx-client-error .threadline-ui .tl-reconnect-banner`. Replace the mutating-control selector with the same three `[data-phx-main].phx-* .threadline-ui [data-tl-mutating]` anchors. Preserve the existing hidden default, `display: flex`, warning colors, `pointer-events: none`, and `opacity: 0.55` values exactly unless a test proves a value is already different in source. Update the reconnect comments in `style.ex` and the `reconnect_banner/1` plus `shell/1` comments in `ui.ex` to say that Phoenix LiveView applies lifecycle classes to the `[data-phx-main]` container in this app and that `.threadline-ui` is the scoped descendant shell. Keep D-10's single mount point unchanged. Keep D-12 control wiring unchanged. Do not add JavaScript, dependencies, tokens, body-level selectors, or the legacy pre-1.0 disconnected-class anchor. Do not edit prune authorization, `secure_compare`, fail-closed scope checks, or audit-the-action logic.
  </action>
  <acceptance_criteria>
    - Source assertion: `style.ex` contains the three banner selectors anchored on `[data-phx-main].phx-loading`, `[data-phx-main].phx-error`, and `[data-phx-main].phx-client-error`, each scoped through `.threadline-ui .tl-reconnect-banner`.
    - Source assertion: `style.ex` contains the matching three `[data-phx-main].phx-* .threadline-ui [data-tl-mutating]` selectors with `pointer-events: none` and `opacity: 0.55`.
    - Source assertion: the old same-element `.threadline-ui` lifecycle selector family is absent from active reconnect CSS.
    - Documentation assertion: `style.ex` and `ui.ex` comments no longer claim `.threadline-ui` is the lifecycle class-bearing render root; they name `[data-phx-main]` as the class-bearing container.
    - Preservation assertion: `UI.shell/1` still mounts `<.reconnect_banner />` exactly once above `#tl-main` and the D-12 controls remain wired; no capture, semantics, or prune enforcement file is changed.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "\\[data-phx-main\\]\\.phx-(loading|error|client-error).*\\.threadline-ui .*tl-reconnect-banner|\\[data-phx-main\\]\\.phx-(loading|error|client-error).*\\[data-tl-mutating\\]|opacity: 0\\.55|pointer-events: none" lib/threadline/operator_surface/style.ex lib/threadline/operator_surface/ui.ex</automated>
  </verify>
  <done>The reconnect CSS is anchored to `[data-phx-main]` and scoped to `.threadline-ui`; old same-element lifecycle selectors are gone from active CSS; comments reflect the corrected D-11 premise; D-10/D-12 behavior is preserved; no security enforcement code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the Tier A string-presence guard with selector-correctness coverage</name>
  <files>test/threadline/operator_surface/component_contract_test.exs, lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/ui.ex</files>
  <read_first>
    - test/threadline/operator_surface/component_contract_test.exs:260-392 (current reconnect guard that asserts the wrong selector string and the shared-shell guard)
    - lib/threadline/operator_surface/style.ex:3388-3432 (corrected selector target from Task 1)
    - lib/threadline/operator_surface/ui.ex:1128-1172 (UI.shell order and single banner mount)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VERIFICATION.md (the exact Tier A weakness: selector-presence was not selector-correctness)
  </read_first>
  <action>
    Update `component_contract_test.exs` so the reconnect stylesheet test asserts selector correctness against the rendered DOM relationship instead of asserting the old wrong selector string. The test must require the active stylesheet to anchor on `[data-phx-main].phx-loading`, `[data-phx-main].phx-error`, and `[data-phx-main].phx-client-error`, then descend through `.threadline-ui` to `.tl-reconnect-banner` and `[data-tl-mutating]`. Add a negative assertion that the active reconnect CSS does not use the old same-element lifecycle selector family. Keep the existing no document-body and no legacy disconnected-class invariant. Keep the shell-order test that proves `UI.shell/1` mounts the banner exactly once above `#tl-main`; adjust its explanatory text only where it still repeats the contradicted D-11 premise. If useful, add a small source helper that extracts the reconnect CSS block before matching so comments or unrelated selector text cannot make the test pass. Do not broaden this task into PAGE-02/PAGE-03 refactors.
  </action>
  <acceptance_criteria>
    - Tier A fails if banner reveal selectors are changed back to lifecycle classes on `.threadline-ui` itself.
    - Tier A fails if `[data-tl-mutating]` dimming is not anchored on `[data-phx-main]` with `.threadline-ui` descendant scoping.
    - Tier A fails if the forbidden body-level or legacy disconnected anchors appear in the shell/page reconnect source.
    - Tier A still proves one banner mount above `#tl-main` in `UI.shell/1` and every operator page still routes chrome through `UI.shell`.
    - PAGE-02 footgun guards and PAGE-03 centering guards still run as part of the operator-surface suite.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/component_contract_test.exs</automated>
  </verify>
  <done>`component_contract_test.exs` guards the actual `[data-phx-main]` ancestor selector shape and the shell mount order; the previous string-presence false positive cannot pass; no body-level or legacy disconnected anchors are introduced; the component contract suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Require positive real-socket-drop proof and correct D-11 closeout docs</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts, .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts:260-356 (current real socket-drop test asserts lifecycle-class detection only and explicitly does not assert banner visibility)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-VERIFICATION.md (missing: positive banner visibility and `[data-tl-mutating]` computed style during genuine disconnect)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md (D-11 must be corrected; D-10/D-12/D-13 must remain intact)
    - .planning/REQUIREMENTS.md (PAGE-01/PAGE-02/PAGE-03 statuses and traceability table)
    - .planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-02-SUMMARY.md and 178-05-SUMMARY.md (what already shipped and must not be replanned)
  </read_first>
  <action>
    Rewrite the `operator-phase-178-uat.spec.ts` socket-drop cell so it proves the positive D-13 behavior after the CSS fix. Keep the routeWebSocket block so the dropped state is sustained. Drive `/audit/policy/retention`, open the real prune confirmation modal, locate the real `.tl-reconnect-banner`, the first `[data-tl-mutating]` prune control, and the `[data-phx-main]` container. On sustained disconnect, assert the container flips to a disconnected lifecycle class, assert the banner is visible, and assert the mutating control's computed styles are exactly `opacity: 0.55` and `pointer-events: none`. On reconnect, assert the container returns to connected, the banner is hidden, and the mutating control's computed styles are restored to an enabled visual state (`pointer-events` no longer `none`, opacity no longer `0.55`). Remove or rewrite comments that say the cell intentionally does not assert banner visibility. Correct D-11 in `178-CONTEXT.md`: mark the prior `.threadline-ui` lifecycle-anchor claim as superseded by verification evidence, state that `[data-phx-main]` is the lifecycle class-bearing container and `.threadline-ui` is the scoped descendant, and preserve the explicit no document-body and no legacy disconnected-class invariant. After Tier A and Tier B pass, update `REQUIREMENTS.md` to reflect PAGE-01 complete for the reconnect dimension and PAGE-03 complete based on the already-verified centering evidence; leave PAGE-02 complete. Do not edit Phase 179 or Phase 180 scope and do not change server-side prune enforcement.
  </action>
  <acceptance_criteria>
    - Tier B assertion: during a genuine socket drop, `.tl-reconnect-banner` is visible in real Chromium.
    - Tier B assertion: during the same drop, the real `[data-tl-mutating]` control computes `opacity` as `0.55` and `pointer-events` as `none`.
    - Tier B assertion: after reconnect, the banner is hidden and the mutating control no longer computes the disconnected affordance styles.
    - Planning-doc assertion: D-11 in `178-CONTEXT.md` is corrected rather than left as a locked false premise; D-10, D-12, and D-13 remain traceable.
    - Requirement assertion: `REQUIREMENTS.md` no longer leaves PAGE-01/PAGE-03 pending after the closure evidence passes; PAGE-02 remains complete.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/threadline_phoenix && npx playwright test e2e/tests/operator-phase-178-uat.spec.ts -g "real dropped live socket"</automated>
    <automated>mix test test/threadline/operator_surface/</automated>
  </verify>
  <done>The real-engine socket-drop spec asserts and passes the visible banner plus computed-style dimming/restoration behavior; D-11 is corrected in CONTEXT; requirement status reflects the closed PAGE-01 reconnect gap and already-satisfied PAGE-03 centering; PAGE-02 remains complete; no Phase 179/180 scope is added.</done>
</task>

</tasks>

<source_audit>
| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 178 | Stress all operator pages including LiveView reconnect; close the remaining reconnect affordance gap | 178-06 | COVERED | This closure targets the sole failed truth in 178-VERIFICATION. |
| REQ | PAGE-01 | Reconnect stress dimension proves banner and mutating-control affordance during a real LiveView socket drop | 178-06 | COVERED | Mandatory blocker; positive Tier B proof required. |
| REQ | PAGE-02 | Named footgun classes remain eliminated while selector changes | 178-06 | COVERED | Preservation via `mix test test/threadline/operator_surface/`; no new footgun work added. |
| REQ | PAGE-03 | Transaction and Home centering remain green while reconnect changes close requirements status | 178-06 | COVERED | Existing 178-03 evidence preserved; closeout status updated after tests. |
| RESEARCH | reconnect selector finding | Lifecycle classes are on `[data-phx-main]`, not `.threadline-ui`; real-engine proof needed | 178-06 | COVERED | Corrects the researched/verified mismatch from 178-05. |
| RESEARCH | zero new deps | No packages, no new JS, inline CSS only | 178-06 | COVERED | CSS/test/doc-only closure. |
| RESEARCH | prune enforcement boundary | `data-tl-mutating` is affordance only; server authz and secure compare are enforcement | 178-06 | COVERED | Explicit no server prune logic edit. |
| CONTEXT | D-01 | Tier A/Tier B honesty split | 178-01..05, 178-06 | COVERED | Closure keeps Tier A source guard plus Tier B real-engine proof. |
| CONTEXT | D-02 | Tier B representative sample | 178-05, 178-06 | COVERED | Closure updates the existing socket-drop cell inside the Tier B spec. |
| CONTEXT | D-03 | Hybrid static fixtures plus real LiveView flows | 178-04..06 | COVERED | Reconnect remains a real `/audit/*` LiveView flow. |
| CONTEXT | D-04 | Ledger ratchet for pages | 178-04, 178-06 | COVERED | Not reopened; requirements status closeout only. |
| CONTEXT | D-05 | Guard-first detectors | 178-01..05, 178-06 | COVERED | Tier A guard strengthened from string presence to selector correctness. |
| CONTEXT | D-06 | Layered global and per-page detectors | 178-01..06 | COVERED | Closure keeps source guard plus real browser test. |
| CONTEXT | D-07 | Reuse existing infra | 178-01..06 | COVERED | Uses existing ExUnit and Playwright harnesses. |
| CONTEXT | D-08 | Grid centering root cause | 178-03, 178-06 | COVERED | Preserved by operator-surface suite. |
| CONTEXT | D-09 | `justify-self: center` fix | 178-03, 178-06 | COVERED | Preservation guard remains part of suite. |
| CONTEXT | D-09b | Inner short-content alignment optional | existing scope | EXCLUDED | Deferred/opportunistic item not part of reconnect closure. |
| CONTEXT | D-10 | Shared shell component and one banner mount | 178-02, 178-06 | COVERED | Closure preserves mount, changes only selector anchor. |
| CONTEXT | D-11 | Correct connection-class anchor | 178-06 | COVERED | Prior locked decision is corrected per verification evidence. |
| CONTEXT | D-12 | `data-tl-mutating` scope | 178-02, 178-06 | COVERED | Existing control set preserved; computed style proof added. |
| CONTEXT | D-13 | Real socket-drop Playwright proof | 178-06 | COVERED | Cell now asserts positive banner/dimming behavior. |
| CONTEXT | D-14 | Executor discretion for exact test organization | 178-06 | COVERED | Uses existing files and narrow selector/test updates. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser LiveView client -> CSS affordance | Phoenix lifecycle classes are client-applied to `[data-phx-main]`; CSS visibility/dimming is UX state, not authorization. |
| Browser control affordance -> server prune event | `[data-tl-mutating]` and pointer-events are client affordances; prune remains enforced server-side by existing authz, typed confirmation, and audit logic. |
| Test harness -> example app | Playwright simulates a real socket drop against the example LiveView; assertions must observe computed browser behavior, not injected classes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-06-01 | Elevation of Privilege | retention prune controls | mitigate | Do not treat `[data-tl-mutating]` as enforcement; preserve existing server-side `authorize_prune`, typed confirmation, fail-closed scope, and audit-the-action code untouched. |
| T-178-06-02 | Spoofing | reconnect banner state | mitigate | Anchor selectors to Phoenix's real `[data-phx-main].phx-*` lifecycle classes and prove behavior with a genuine socket drop instead of manually adding classes. |
| T-178-06-03 | Tampering | reconnect selector tests | mitigate | Replace string-presence guard with selector-correctness assertions and positive computed-style Tier B proof. |
| T-178-06-04 | Denial of Service | disconnected mutating controls | accept | Pointer-events disabling is UX affordance only; genuine reconnect restores controls and server remains source of truth. |
| T-178-06-SC | Tampering | package installs | mitigate | No package-manager installs in this closure; zero-new-dependency invariant remains. |
</threat_model>

<verification>
Exact closure checks:
- `rg -n "\\[data-phx-main\\]\\.phx-(loading|error|client-error).*\\.threadline-ui .*tl-reconnect-banner|\\[data-phx-main\\]\\.phx-(loading|error|client-error).*\\[data-tl-mutating\\]" lib/threadline/operator_surface/style.ex`
- `! rg -n "\\.threadline-ui\\.phx-(loading|error|client-error)" lib/threadline/operator_surface/style.ex lib/threadline/operator_surface/ui.ex`
- `! rg -n "body\\.phx-|\\.phx-disconnected" lib/threadline/operator_surface/style.ex lib/threadline/operator_surface/ui.ex lib/threadline/operator_surface/live`
- `mix test test/threadline/operator_surface/component_contract_test.exs`
- `mix test test/threadline/operator_surface/`
- `cd examples/threadline_phoenix && npx playwright test e2e/tests/operator-phase-178-uat.spec.ts -g "real dropped live socket"`
- Preferred full Tier B rerun if runtime allows: `mix verify.example_browser`

Real-engine proof requirement: the Tier B socket-drop test must assert banner visibility and the real mutating control's computed `opacity: 0.55` plus `pointer-events: none` during genuine disconnect, then assert banner hidden and controls restored after reconnect. Lifecycle-class detection alone is not sufficient.
</verification>

<success_criteria>
Phase 178 is closure-ready when the corrected `[data-phx-main]` selector reveals the reconnect banner and dims `[data-tl-mutating]` controls in a genuine socket-drop test, the Tier A guard would fail on the old wrong anchor, D-11 is corrected in the phase context, and PAGE-01/PAGE-02/PAGE-03 requirement status is coherent with the verified evidence. No server-side prune enforcement, capture layer, semantics layer, public component API, new dependency, new token, pixel baseline, Phase 179 scope, or Phase 180 scope is introduced.
</success_criteria>

## Artifacts this phase produces

- Changed selector family in `lib/threadline/operator_surface/style.ex`: `[data-phx-main].phx-loading .threadline-ui .tl-reconnect-banner`, `[data-phx-main].phx-error .threadline-ui .tl-reconnect-banner`, `[data-phx-main].phx-client-error .threadline-ui .tl-reconnect-banner`.
- Changed selector family in `lib/threadline/operator_surface/style.ex`: `[data-phx-main].phx-loading .threadline-ui [data-tl-mutating]`, `[data-phx-main].phx-error .threadline-ui [data-tl-mutating]`, `[data-phx-main].phx-client-error .threadline-ui [data-tl-mutating]`.
- Updated reconnect lifecycle comments in `lib/threadline/operator_surface/style.ex` and `lib/threadline/operator_surface/ui.ex` to identify `[data-phx-main]` as the class-bearing LiveView container.
- Updated Tier A reconnect selector test in `test/threadline/operator_surface/component_contract_test.exs`; if a helper is added, it is source-scoped to the reconnect CSS block and must not parse CSS with a new dependency.
- Updated Tier B socket-drop test in `examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts` to assert positive banner visibility plus computed mutating-control dimming/restoration.
- Corrected planning decision D-11 in `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-CONTEXT.md`.
- Updated `.planning/REQUIREMENTS.md` statuses for PAGE-01/PAGE-03 after positive closure evidence; PAGE-02 remains complete.
- New execution summary `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-06-SUMMARY.md`.

<output>
Create `.planning/phases/178-per-page-flow-stress-pass-all-11-pages/178-06-SUMMARY.md` when done, including the corrected selector family, the Tier A/Tier B command output, the D-11 correction, the requirement-status updates, and confirmation that prune enforcement, PAGE-02 footgun guards, and PAGE-03 centering did not regress.
</output>
