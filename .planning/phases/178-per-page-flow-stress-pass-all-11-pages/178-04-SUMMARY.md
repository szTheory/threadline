---
phase: 178-per-page-flow-stress-pass-all-11-pages
plan: 04
subsystem: operator-surface
tags: [operator-surface, page-01, ledger, ratchet, fixtures, stress-stories, tier-a]
requires:
  - "test/threadline/operator_surface/stress_fixtures_test.exs (Wave-0 page-story conversion RED guard, D-04)"
  - "lib/threadline/operator_surface/stress_fixtures.ex (@required_cases ugly-data library, story/1 builder, assigns_for/1 generic clause)"
  - ".planning/design-system-ledger.json (11 page.<x>.reserved entries, ratchet block, required_inventory.pages)"
provides:
  - "77 fixture-backed page path stories (11 subjects x 7 paths) on /audit/__stress"
  - "11 page ledger entries converted reserved->current, scores ratcheted 35->62; two baselines raised 62->72"
  - "refreshed DESIGN-SYSTEM.md Pages projection in parity with the ledger"
  - "GREEN Wave-0 page-story conversion guard (PAGE-01 D-04)"
affects:
  - "lib/threadline/operator_surface/live/stress_live.ex (renders the new page stories via the generic assigns_for clause — NOT edited here)"
tech-stack:
  added: []
  patterns:
    - "Per-page 7-path fixture cartesian generated by comprehension (subject x path)"
    - "Reserved->current ledger conversion with upward-only ratchet (no reset needed)"
    - "Pre-existing baseline absorption (no orphaned id — 177-05 group precedent)"
key-files:
  created: []
  modified:
    - "lib/threadline/operator_surface/stress_fixtures.ex"
    - "test/threadline/operator_surface/stress_fixtures_test.exs"
    - ".planning/design-system-ledger.json"
    - "DESIGN-SYSTEM.md"
decisions:
  - "Converted-page-cell score = 62 (the established fixture-backed-current Tier-A floor used by group/data-display stories); two baselines raised 62->72 (current-with-adapter tier, matching primitive.surface-header.current). Neither claims 90 — full done requires Tier B sample (Plan 05), keeping the score honest per D-01."
  - "One ledger entry + one fixture story per (subject, path) cell = 77 entries (the page.home.happy / page.timeline.empty one-story-per-path convention), not a single multi-path entry per page."
  - "New page stories resolve through the existing generic assigns_for(%{} = story) clause (title/body/fallback) — no per-story clause needed; DB-free static data, no new token, capture/semantics untouched."
metrics:
  duration: "~12 min"
  completed: "2026-06-18"
  tasks: 2
  files_changed: 4
status: complete
---

# Phase 178 Plan 04: PAGE-01 Per-Page Path-Story Ledger Worklist Summary

Landed PAGE-01's ledger worklist: converted the 11 reserved `page.<x>.reserved`
entries into 77 fixture-backed per-page path stories (11 subjects × the 7 audit
paths happy/empty/loading/error/permission/boundary/advanced) on `/audit/__stress`,
ratcheted their scores upward-only, raised the two existing baselines, kept
`story_id`/`fixture_key` parity, and refreshed the `DESIGN-SYSTEM.md` Pages
projection. This flipped the Wave-0 page-story conversion RED guard GREEN and proves
the full Tier A structural cartesian. The genuinely-live loading/reconnect flows
remain deferred to the real-LiveView Tier B specs (Plans 01/05), not these static
fixtures (D-01/D-03 honesty contract).

## What was built

**Task 1 — `stress_fixtures.ex` + `stress_fixtures_test.exs` (commit 7677be0):**
- Replaced the `@page_stories` (11 reserved tuples) and `@current_page_stories`
  (2 baselines) with a per-page 7-path generator: `@page_subjects` (11) ×
  `@page_paths` (7) comprehension in `page_story_maps/0`.
- Each cell's `cases` map onto the `@required_cases` vocabulary to satisfy the
  conversion contract: happy→`[one,many,mixed_severity]`, empty→`[empty,zero_count]`,
  loading→`[reconnecting,stale]`, error→`[error]`, permission→`[permission_denied]`,
  boundary→`[pagination_boundary,timezone_boundary]`, advanced→`[non_ascii,null_fields]`.
- Absorbed the two Phase 171 baselines verbatim (`page.home.happy`,
  `page.timeline.empty`) as the home/happy and timeline/empty cells via
  `@page_baseline_cells` — `baseline` status, owner 171, no orphaned id.
- Fixture keys underscore multi-word subjects (`row-history` → `page.row_history.<path>`).
- New stories resolve through the existing generic `assigns_for/1` clause.
- Updated the test `@required_inventory_story_ids` from the 11 `.reserved` ids to
  the 77 page path ids.

**Task 2 — `design-system-ledger.json` + `DESIGN-SYSTEM.md` (commit 4a07288):**
- Replaced the 11 `page.<x>.reserved` ledger entries with 77 page entries (75
  `current` owner-178 + the 2 raised `baseline` owner-171), sorted by id, full
  `story_id`/`fixture_key`/`stress_path` parity with the fixtures.
- `ratchet.locked_ids`: removed the old page ids, added all 77 new page ids.
- `ratchet.minimum_scores`: removed old page floors, added the 77 new floors
  (= each entry's `ratchet_score`).
- `screenshot_baseline_refs` kept baseline-free for the new cells (no pixel-diff);
  the two baselines keep their existing refs.
- Refreshed the `DESIGN-SYSTEM.md` Pages projection table (13 rows → 77 rows) so
  the ledger↔projection freshness assertion stays GREEN.

## Per-page score ratchet summary

| Page subject | Before | After | Status before → after | Cells |
|---|---:|---:|---|---:|
| actor | 35 (reserved) | 62 | reserved → current | 7 |
| coverage | 35 (reserved) | 62 | reserved → current | 7 |
| evidence | 35 (reserved) | 62 | reserved → current | 7 |
| exports | 35 (reserved) | 62 | reserved → current | 7 |
| home | 35 reserved / 62 happy baseline | 62 (6 cells) / 72 (happy) | reserved → current; baseline raised | 7 |
| redaction | 35 (reserved) | 62 | reserved → current | 7 |
| retention | 35 (reserved) | 62 | reserved → current | 7 |
| row-history | 35 (reserved) | 62 | reserved → current | 7 |
| shell | 35 (reserved) | 62 | reserved → current | 7 |
| timeline | 35 reserved / 62 empty baseline | 62 (6 cells) / 72 (empty) | reserved → current; baseline raised | 7 |
| transaction | 35 (reserved) | 62 | reserved → current | 7 |

- **All scores moved upward only** — no `current_score` lowered, so no
  `ratchet.resets` / `reset_rationale` entry was required (RESEARCH Pitfall 6).
- **No orphaned `page.<x>.reserved` id** remains in fixtures, ledger, or projection
  (verified by grep — NONE).
- Scores intentionally stop at 62/72 (not 90): per D-01, Tier A structural-green is
  proven here; reaching `target_score: 90` ("done per page") additionally requires
  the Tier B representative sample, which lands in Plan 05. The ratchet stays honest.

## RED → GREEN delta

| Guard | Before | After | Turned green by |
|---|---|---|---|
| `stress_fixtures_test` PAGE-01 (D-04) page-story conversion | **RED** (all 11 subjects still `reserved`, single-path) | **GREEN** | this plan, Task 1 |
| `stress_fixtures_test` planned-inventory presence (77 page ids) | n/a (was 11 reserved ids) | **GREEN** | this plan, Task 1 |
| `stress_ledger_test` ratchet / locked-ids / parity / projection-freshness | would break on conversion | **GREEN** | this plan, Task 2 |

## Deviations from Plan

None — plan executed exactly as written. `stress_ledger_test.exs` was listed in the
plan's `files_modified`, but the existing ratchet/locked-ids/parity/freshness
assertions already enforce the post-conversion contract correctly, so no test edit
was required (the data change alone satisfies them). This is a no-op narrowing within
plan latitude, not a behavior deviation.

## Verification

- `mix test test/threadline/operator_surface/stress_fixtures_test.exs` →
  **12 tests, 0 failures**.
- `mix test test/threadline/operator_surface/stress_ledger_test.exs` →
  **10 tests, 0 failures**.
- `mix test test/threadline/operator_surface/` → **591 tests, 3 failures**, and all 3
  are pre-existing Wave-1 PAGE-02 footgun RED scaffolds (#1 desktop scroll
  reconciliation, #4 scrim click-outside, #6 child spacing) owned by Plans 03/05 —
  in `style_contract_test.exs` / `component_contract_test.exs`, which this plan does
  not touch. Left RED by design per the plan's critical notes.
- `mix compile --warnings-as-errors` clean; `mix format --check-formatted` clean on
  both edited code files.
- `mix test test/threadline/brandbook_token_parity_test.exs` → **4 tests, 0 failures**
  (zero new `--tl-*` token; no style.ex / CSS touched).
- `grep page.<x>.reserved` across ledger + projection + fixtures → **NONE** (no
  orphaned reserved id).
- JSON validity confirmed (`json.load`).

## Self-Check: PASSED

- `lib/threadline/operator_surface/stress_fixtures.ex` — FOUND (modified)
- `test/threadline/operator_surface/stress_fixtures_test.exs` — FOUND (modified)
- `.planning/design-system-ledger.json` — FOUND (modified, 77 page entries)
- `DESIGN-SYSTEM.md` — FOUND (modified, 77 Pages rows)
- Commit 7677be0 (Task 1) — FOUND
- Commit 4a07288 (Task 2) — FOUND
