---
phase: 193-quality-closeout-and-next-step-decision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/193-quality-closeout-and-next-step-decision/193-RISK-REGISTER.md
  - .planning/phases/193-quality-closeout-and-next-step-decision/193-NEXT-STEP.md
autonomous: true
requirements: [CLOSE-01]

must_haves:
  truths:
    - "193-RISK-REGISTER.md is a verify-and-refresh of the 189-QUALITY-AUDIT.md ranked ledger: rows 1-3 verified CLOSED by phases 190/191/192, rows 6-12 preserved unchanged, ranked on the adoption/operations/maintainer-risk lens per D-10/D-11/D-12 (not severity x likelihood)"
    - "Four new post-189 residuals are folded in, each with an Owner and a concrete reopen-trigger: R-A ship-gated D-17/D-19, R-B WR-01 alt-schema FK gap, R-C v1_23_charter_doc_contract_test drift, R-D ~81 local mix test failures"
    - "R-D is framed as maintainer-friction (identical count pre/post-192, CI green), explicitly NOT a v1.39 quality regression, and is NOT ranked above the ship-gated or storage-schema residuals"
    - "Every register row carries an Owner and a concrete follow-up/reopen-trigger — there is no bare 'polish later' bucket"
    - "193-NEXT-STEP.md states exactly one primary recommendation, HOLD / thin-polish, backed by the three converging gates (done-band ~92-95% >= 90, no external-adopter signal, 189 adjudicated candidates 2-4), and maps all four armed flip-triggers to Future Requirements (EXT-PILOT-01, CI-depth, OBS-01, UI-REG-01, RECONNECT-01) per D-05/D-06/D-07"
  artifacts:
    - .planning/phases/193-quality-closeout-and-next-step-decision/193-RISK-REGISTER.md
    - .planning/phases/193-quality-closeout-and-next-step-decision/193-NEXT-STEP.md
  key_links:
    - "Each flip-trigger row names its observed signal AND the Future Requirement (or CI-depth track) it flips to"
    - "Each risk row's Owner + reopen-trigger is concrete and actionable, not a placeholder"
  prohibitions:
    - "Does NOT run /gsd-audit-milestone inline and does NOT create v1.39-MILESTONE-AUDIT.md — hand-synthesize in the v1.38 audit FORMAT only (D-01/D-03/D-10)"
    - "Does NOT open v1.40, and does NOT recommend anything other than HOLD without a named trigger firing (D-05, no_auto_new_milestone)"
    - "Does NOT re-litigate or promote 189 rows 6-12 (e.g. no auto-promoting OBS-01) without fresh evidence (D-05/D-07)"
---

<objective>
Deliver CLOSE-01 clauses 3 and 4 for milestone v1.39: a ranked residual-risk register
(`193-RISK-REGISTER.md`) and a v1.40 next-step recommendation (`193-NEXT-STEP.md`). Both synthesize
existing verified evidence; the register is a verify-and-refresh of the pre-ranked 189 ledger, not a
fresh discovery pass.

Purpose: Give the milestone an honest, owner-tagged residual-risk picture ranked on the
project-native adoption/operations/maintainer lens, and a single clear next-step recommendation
(HOLD / thin-polish) with armed flip-triggers — so the follow-on `/gsd-audit-milestone` and
`/gsd-complete-milestone` runs have a decision to act on.

Output:
- `193-RISK-REGISTER.md` — refreshed 189 ledger + four folded-in post-189 residuals, all with Owner +
  reopen-trigger, ranked on adoption/ops/maintainer risk.
- `193-NEXT-STEP.md` — HOLD / thin-polish recommendation, three gates, four armed flip-triggers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/config.json
@.planning/phases/193-quality-closeout-and-next-step-decision/193-CONTEXT.md
@.planning/phases/193-quality-closeout-and-next-step-decision/193-RESEARCH.md
@.planning/phases/189-quality-baseline-and-authority-surface-audit/189-QUALITY-AUDIT.md
@.planning/milestones/v1.38-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the ranked residual-risk register (193-RISK-REGISTER.md)</name>
  <files>.planning/phases/193-quality-closeout-and-next-step-decision/193-RISK-REGISTER.md</files>
  <read_first>
    - .planning/phases/189-quality-baseline-and-authority-surface-audit/189-QUALITY-AUDIT.md (the 12-row ranked ledger + QUAL-03 residuals — the register seed)
    - .planning/phases/193-quality-closeout-and-next-step-decision/193-RESEARCH.md ("Risk Register Synthesis" — rows 1-3 close-out status + the four new R-A..R-D residuals + critical framing)
    - .planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-VERIFICATION.md (WR-01 residual)
    - .planning/phases/191-release-version-and-docs-trust-repair/deferred-items.md (charter-test drift residual)
    - .planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-SHIP-CHECKLIST.md (ship-gated D-17/D-19)
    - .planning/phases/192-ci-cd-measurement-and-efficiency-hardening/192-VERIFICATION.md (~81 local-env test-failure observation)
    - .planning/milestones/v1.38-MILESTONE-AUDIT.md (Residual Tech Debt table format to mirror)
  </read_first>
  <action>
    Create 193-RISK-REGISTER.md realizing CLOSE-01 clause 3 (per D-01), hand-synthesized in the v1.38
    audit "Residual Tech Debt" FORMAT — do NOT run /gsd-audit-milestone and do NOT create
    v1.39-MILESTONE-AUDIT.md (D-03/D-10; the formal skill run is a follow-on after 193). Method per
    D-10/D-11/D-12: (1) seed from the 189 ranked ledger; (2) verify rows 1-3 as CLOSED by phases
    190/191/192, each leaving exactly one carried residual; (3) preserve 189 rows 6-12
    (screenshot-regression, host staging, external pilot, Hex/dep notes, SEED-005, optional-Phoenix
    boundary, compliance/API/WAL/redaction N/A) unchanged — do not re-litigate; row 4 IS this phase
    (193), row 5 is 192-owned. Then fold in the four new post-189 residuals with Owner + reopen-trigger:
    R-A ship-gated D-17/D-19 (Ops, inherently external; owner maintainer szTheory); R-B WR-01 alt-schema
    fixture FK gap (Maintainer-confidence, capture/storage-schema fixture-fidelity; owner future
    schema-fixture hardening); R-C v1_23_charter_doc_contract_test.exs milestone-literal drift v1.38->
    v1.39 (Maintainer doc-contract greenness; owner charter/SSOT-truth owner); R-D ~81 local mix test
    failures (Maintainer-friction ONLY). Rank the whole register on the project-native adoption /
    operations / maintainer-risk lens, NOT severity x likelihood; a one-line severity note per row is
    allowed as secondary color. CRITICAL framing: R-D must be recorded as maintainer-friction with the
    "identical count at pre-192 commit and HEAD, CI provisions the DB correctly" evidence inline, and it
    must NOT be ranked above the ship-gated (R-A) or storage-schema (R-B) residuals — over-claiming this
    as a regression is as dishonest as hiding it. R-C is a milestone-literal drift, not a version-truth
    defect (ADOPT-01's 0.9.0 reconciliation is proven). Characterize each residual against the
    three-layer architecture where relevant (e.g. WR-01 is a capture/storage-schema fixture residual,
    not a semantics/exploration defect) per CLAUDE.md. Every row MUST carry an Owner and a concrete
    reopen-trigger — this structurally forbids a bare "polish later" bucket. Include the "Artifacts this
    phase produces" list.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/threadline && F=.planning/phases/193-quality-closeout-and-next-step-decision/193-RISK-REGISTER.md; test -f "$F" || { echo "MISSING FILE"; exit 1; }; MISS=""; for r in R-A R-B R-C R-D WR-01; do grep -q "$r" "$F" || MISS="$MISS $r"; done; grep -qi 'owner' "$F" || MISS="$MISS OWNER-COL"; grep -qiE 'reopen|trigger' "$F" || MISS="$MISS REOPEN-TRIGGER"; grep -qi 'maintainer-friction' "$F" || MISS="$MISS R-D-FRAMING"; if grep -qiE 'polish later' "$F"; then echo "ERROR: bare 'polish later' bucket present"; exit 1; fi; if [ -n "$MISS" ]; then echo "MISSING:$MISS"; exit 1; else echo "register complete: R-A..R-D + WR-01, owner + reopen-trigger, R-D maintainer-friction, no polish-later bucket"; fi</automated>
  </verify>
  <acceptance_criteria>
    193-RISK-REGISTER.md exists; verified-CLOSED rows 1-3 and preserved rows 6-12 are present; the four
    new residuals R-A/R-B/R-C/R-D are folded in; every row has an Owner and a concrete reopen-trigger;
    R-D is framed as maintainer-friction and not ranked above R-A/R-B; ranking is on the
    adoption/ops/maintainer lens; no bare "polish later" bucket exists; no /gsd-audit-milestone run and
    no v1.39-MILESTONE-AUDIT.md created.
  </acceptance_criteria>
  <done>193-RISK-REGISTER.md refreshes the 189 ledger (rows 1-3 CLOSED, rows 6-12 preserved), folds in R-A/R-B/R-C/R-D each with Owner + reopen-trigger on the adoption/ops/maintainer lens, frames R-D as maintainer-friction (not ranked above R-A/R-B), has no "polish later" bucket, and creates no milestone-audit artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Write the v1.40 next-step recommendation (193-NEXT-STEP.md)</name>
  <files>.planning/phases/193-quality-closeout-and-next-step-decision/193-NEXT-STEP.md</files>
  <read_first>
    - .planning/phases/193-quality-closeout-and-next-step-decision/193-RESEARCH.md ("Next-Step Recommendation" — three gates table + armed flip-triggers table)
    - .planning/config.json (workflow.milestone_next_step + workflow.milestone_assessment: default_no_signal_path, done_band_threshold, no_auto_new_milestone, adopter_lens)
    - .planning/REQUIREMENTS.md (Future Requirements: EXT-PILOT-01, OBS-01, UI-REG-01, RECONNECT-01; Out of Scope)
    - .planning/STATE.md (done-band ~92-95%; Deferred Items external-pilot posture)
  </read_first>
  <action>
    Create 193-NEXT-STEP.md realizing CLOSE-01 clause 4 (per D-01/D-05/D-06/D-07). State exactly ONE
    primary recommendation: HOLD / thin-polish for v1.40 — the project's own default_no_signal_path.
    Back it with the three converging gates (reproduce the RESEARCH.md gates table): done-band scope
    completion ~92-95% vs done_band_threshold 90; no sustained real external-adopter signal (STATE.md
    Deferred Items + REQUIREMENTS Out-of-Scope forbid an external pilot without real signal); and Phase
    189 already adjudicated every remaining candidate as score-2-4 with all score-1 must-fixes closed
    inside v1.39. Cite the config policy explicitly: default_no_signal_path hold_or_thin_polish,
    no_auto_new_milestone true (193 recommends, it does not open v1.40), adopter_lens true. Then record
    the armed flip-triggers table (reproduce RESEARCH.md, one row per trigger): named external
    host/integrator -> External adopter proof (EXT-PILOT-01, highest-leverage, simultaneously arms
    observability); measured sustained CI bottleneck the Phase-192 changes did NOT resolve -> CI/CD depth
    (a track, not a Future Req ID; requires the post-ship measurement first); real operator
    production-debugging gap / audit re-scores debuggability score-1 -> Observability (OBS-01 — note
    Phase 189 did NOT flag debuggability, so do NOT auto-promote without fresh evidence); visual
    regression escapes a release -> UI regression confidence (UI-REG-01); socket-drop spec failure /
    operator field report -> Reconnect UX (RECONNECT-01). Describe thin-polish content if pursued (D-06):
    close ship-gated 192-04-03 when it can legitimately go public, plus Nyquist/charter/frontmatter
    backlog cleanup (R-C is a natural candidate) — nothing here needs a version bump. Frame the whole
    document as a recommendation awaiting human sign-off, NOT a milestone open. Include the "Artifacts
    this phase produces" list.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/threadline && F=.planning/phases/193-quality-closeout-and-next-step-decision/193-NEXT-STEP.md; test -f "$F" || { echo "MISSING FILE"; exit 1; }; MISS=""; grep -qiE 'HOLD|thin.?polish' "$F" || MISS="$MISS RECOMMENDATION"; for t in EXT-PILOT-01 OBS-01 UI-REG-01 RECONNECT-01; do grep -q "$t" "$F" || MISS="$MISS $t"; done; grep -qi 'no_auto_new_milestone' "$F" || MISS="$MISS CONFIG-POLICY"; grep -qi 'done.?band' "$F" || MISS="$MISS DONE-BAND-GATE"; if [ -n "$MISS" ]; then echo "MISSING:$MISS"; exit 1; else echo "next-step: single HOLD recommendation + three gates + four future-req triggers present"; fi</automated>
  </verify>
  <acceptance_criteria>
    193-NEXT-STEP.md exists; states exactly one primary recommendation (HOLD / thin-polish) backed by
    the three converging gates and the config policy (no_auto_new_milestone); all four Future
    Requirements (EXT-PILOT-01, OBS-01, UI-REG-01, RECONNECT-01) plus the CI-depth track appear as armed
    flip-triggers each with its observed signal; thin-polish content is described; framed as a
    recommendation for human sign-off, not a milestone open.
  </acceptance_criteria>
  <done>193-NEXT-STEP.md states exactly one HOLD / thin-polish recommendation backed by the three converging gates and config policy (no_auto_new_milestone), with all four Future-Requirement flip-triggers (EXT-PILOT-01, OBS-01, UI-REG-01, RECONNECT-01) plus the CI-depth track armed, framed as a recommendation awaiting human sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `.planning/` -> public `origin/main` | The only security-relevant boundary; this phase never pushes, tags, or bumps version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-193-01 | Information Disclosure | git push / tag to public origin | low | mitigate | Boundary check in 193-VERIFICATION (plan 03) asserts only .planning/phases/193-* changed; no tag/push (D-02, milestone-tags-stay-local). |
| T-193-03 | Elevation of Privilege | scope creep into ship/archive/audit territory | low | mitigate | Tasks emit only 193-* docs; register hand-synthesizes format, never invokes /gsd-audit-milestone (D-03). |

No new runtime code, dependency, network path, or attack surface is introduced — decision docs only.
No package-manager installs occur, so no T-193-SC supply-chain row applies.
</threat_model>

<verification>
- Both artifacts exist under `.planning/phases/193-quality-closeout-and-next-step-decision/`.
- 193-RISK-REGISTER.md: every row has Owner + reopen-trigger; R-A..R-D folded in; R-D framed as
  maintainer-friction; no "polish later" bucket; no v1.39-MILESTONE-AUDIT.md created.
- 193-NEXT-STEP.md: exactly one HOLD recommendation; three gates; four Future-Req flip-triggers.
- `git diff --name-only` shows only `.planning/phases/193-*` files.
</verification>

<success_criteria>
CLOSE-01 clauses 3 and 4 are satisfied: residual risks are ranked with owner + follow-up on the
adoption/ops/maintainer lens with no vague bucket, and the next milestone recommendation is a single
clear HOLD / thin-polish with armed, requirement-mapped flip-triggers — all within the 193-* docs
boundary, without opening v1.40 or running the downstream audit skill.
</success_criteria>

<output>
Create `.planning/phases/193-quality-closeout-and-next-step-decision/193-02-SUMMARY.md` when done.
</output>
