---
phase: 109-maintainer-walkthrough-dry-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md
  - .planning/phases/109-maintainer-walkthrough-dry-run/109-WALK-CHECKPOINT.json
autonomous: false
requirements:
  - FINDINGS-02
must_haves:
  truths:
    - "WALK_BASELINE_SHA and clone path recorded before any walk steps"
    - "WR-001 and WR-002 pre-registered in execution log with expected classifications"
    - "Isolated clone exists at pinned SHA with examples/threadline_phoenix as cwd"
  artifacts:
    - path: .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md
      provides: "Auditable walk metadata and pre-registered review expectations"
    - path: .planning/phases/109-maintainer-walkthrough-dry-run/109-WALK-CHECKPOINT.json
      provides: "Resume state updated at section boundaries"
---

<objective>
Prepare Phase 109 dry-run instrumentation: record baseline SHA, create isolated clean clone (or worktree), bootstrap Postgres per WALKTHROUGH §0 Path A or B, and initialize checkpoint JSON. No WALKTHROUGH §1+ steps yet.
</objective>

<context>
@.planning/phases/109-maintainer-walkthrough-dry-run/109-CONTEXT.md
@.planning/phases/109-maintainer-walkthrough-dry-run/109-RESEARCH.md
@examples/threadline_phoenix/WALKTHROUGH.md (§0 only)
@.planning/phases/108-walkthrough-script-finding-capture-protocol/108-REVIEW.md
</context>

<scope_guard>
**ALLOWED:** `.planning/phases/109-maintainer-walkthrough-dry-run/*` in main repo; findings path in clone only until import plan  
**FORBIDDEN:** `lib/`, `guides/`, `examples/threadline_phoenix/` edits on main repo; any in-flight doc/code fixes
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-109-01 | Walk on dirty dev tree invalidates RUN-01 | high | Fresh clone at `WALK_BASELINE_SHA`; reject cwd = main dirty tree |
| T-109-02 | Compose Postgres on 5433 without DB_PORT export | medium | Path B: `export DB_HOST=localhost DB_PORT=5433`; file (c) if ambiguous §0 |
| T-109-03 | Pre-fixing WR-001/WR-002 before walk | high | Execution log lists expected 0001/0002 only — no WALKTHROUGH edits |
</threat_model>

<tasks>

<task type="manual">
  <name>Task 1: Create 109-EXECUTION-LOG.md with baseline metadata</name>
  <files>.planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md</files>
  <read_first>
    - .planning/phases/109-maintainer-walkthrough-dry-run/109-CONTEXT.md (D-109-06b, D-109-03b, D-109-07a)
    - .planning/phases/108-walkthrough-script-finding-capture-protocol/108-REVIEW.md (WR-001, WR-002)
  </read_first>
  <action>
In the **main repo** (not clone), create `109-EXECUTION-LOG.md` with these sections and literal fields:

1. **Baseline:** `WALK_BASELINE_SHA=$(git rev-parse HEAD)` recorded verbatim
2. **Timing:** `WALK_STARTED_AT` UTC ISO8601, `PHASE_109_START_SHA` same as baseline (for later `git log` window)
3. **Environment:** `elixir -v`, `erl -version`, chosen Postgres path (`A` local :5432 or `B` compose :5433), `DB_HOST`/`DB_PORT` values
4. **Pre-registered expectations table:**

| Review ID | Step | Expected class | Planned finding # |
|-----------|------|----------------|-------------------|
| WR-001 | WALK-03-02 | a | 0001 |
| WR-002 | WALK-03-03 | c | 0002 |

5. **Clone plan:** `CLONE_DIR=${TMPDIR:-/tmp}/threadline-walk-109-$(git rev-parse --short HEAD)` (or worktree path)
6. **Walk status checklist** (empty checkboxes): RUN-01, RUN-02, RUN-03, findings count a/b/c/d
  </action>
  <verify>
    <automated>grep -q 'WALK_BASELINE_SHA' .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md && grep -q 'WR-001' .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md && grep -q 'WR-002' .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `109-EXECUTION-LOG.md` contains literal `WALK_BASELINE_SHA` line with 40-char hex SHA
    - WR-001 mapped to finding 0001 classification **a**; WR-002 to 0002 classification **c**
    - `PHASE_109_START_SHA` documented for scope-guard git audit in plan 109-05
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Create isolated clone and §0 Postgres bootstrap</name>
  <files>.planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (§0 Prerequisites, Recovery, Phase 109 discipline)
    - docker-compose.yml (repo root — postgres port 5433)
    - .planning/phases/109-maintainer-walkthrough-dry-run/109-CONTEXT.md (D-109-01a, D-109-02b)
  </read_first>
  <action>
1. From main repo: `git clone "$(git rev-parse --show-toplevel)" "$CLONE_DIR"` then `cd "$CLONE_DIR" && git checkout "$WALK_BASELINE_SHA"`

   Alternative: `git worktree add "$CLONE_DIR" "$WALK_BASELINE_SHA"` — note "clean sibling checkout" in execution log.

2. **Path B (recommended if local :5432 busy):** from clone repo root:
   ```bash
   docker compose up -d postgres
   export DB_HOST=localhost DB_PORT=5433
   ```
   **Path A:** ensure `pg_isready -h localhost -p 5432` succeeds; no DB_PORT override.

3. `cd examples/threadline_phoenix` — all subsequent walk commands use this cwd.

4. Update execution log with actual `CLONE_DIR`, path A/B choice, and `pg_isready` output snippet.

5. Initialize `109-WALK-CHECKPOINT.json` in clone at `.planning/phases/109-maintainer-walkthrough-dry-run/`:
   ```json
   {"phase":"109","last_completed_step":"§0","sections_completed":[],"findings_filed":[],"blockers":[],"checkpoint_tables_verified":[]}
   ```
  </action>
  <verify>
    <automated>test -d "${CLONE_DIR:-/nonexistent}" && test -f "${CLONE_DIR}/examples/threadline_phoenix/mix.exs"</automated>
  </verify>
  <acceptance_criteria>
    - Clone directory exists and `git -C "$CLONE_DIR" rev-parse HEAD` equals `WALK_BASELINE_SHA`
    - Execution log records `CLONE_DIR` absolute path
    - `pg_isready` succeeds for chosen host/port before plan 109-02 starts
    - `109-WALK-CHECKPOINT.json` exists in clone with `"last_completed_step":"§0"`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] No edits under `lib/`, `guides/`, `examples/`, `test/` on main repo during this plan
- [ ] Clone is not the dirty dev tree (separate directory or worktree)
</verification>
