---
phase: 116-example-first-hour-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - examples/threadline_phoenix/test/support/conn_case.ex
  - examples/threadline_phoenix/test/threadline_phoenix_web/posts_audit_path_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs
  - examples/threadline_phoenix/README.md
  - guides/getting-started-saas.md
  - test/threadline/example_phoenix_readme_contract_test.exs
  - test/threadline/getting_started_saas_doc_contract_test.exs
autonomous: true
requirements:
  - EXAMPLE-01
  - EXAMPLE-04
must_haves:
  truths:
    - "`:api` pipeline runs `fetch_session` and `fetch_current_scope` before `Threadline.Plug`, outside the `router-pipeline-actor-fn` doc fence."
    - "Browser Sigra login populates `current_scope` on `POST /api/posts`; cookie curl returns `201` + `audit_transaction_id`."
    - "README and getting-started §6 document host-owned auth staging (`_threadline_phoenix_key`, no bearer tokens, `missing actor` without session)."
    - "`sigra_conn/2` uses session-token staging compatible with API session plugs; `mix verify.example` green."
  artifacts:
    - path: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      provides: "API session plugs before Threadline.Plug (EXAMPLE-01 code fix)"
    - path: examples/threadline_phoenix/test/support/conn_case.ex
      provides: "Session-token sigra_conn/2 compatible with fetch_current_scope"
    - path: examples/threadline_phoenix/README.md
      provides: "Authenticate before subsection + cookie curl under Audited HTTP path"
    - path: guides/getting-started-saas.md
      provides: "§6 dual-contract auth staging for greenfield integrators"
    - path: test/threadline/example_phoenix_readme_contract_test.exs
      provides: "EXAMPLE-01 auth staging literal locks"
    - path: test/threadline/getting_started_saas_doc_contract_test.exs
      provides: "§6 auth staging dual-contract lock"
---

<objective>
Wire session plugs on the example `:api` pipeline so browser Sigra login reaches `Threadline.Plug`, fix `sigra_conn/2` for plug compatibility, and document API auth staging in the example README and getting-started §6 (EXAMPLE-01) with doc-contract locks (EXAMPLE-04 partial).

Purpose: Evaluators currently hit `500 missing actor` on bare curl and blame Threadline; the fix is minimal router truth plus runbook auth staging, not a new auth product.
Output: ~4 LOC router change, conn_case/test helper fix, auth subsection + curl sync, two contract test extensions.
</objective>

<context>
@.planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-01, D-116-04a–b, D-116-04e)
@.planning/phases/116-example-first-hour-fixes/116-RESEARCH.md (§2.1 router, §2.5 sigra_conn breakage, R-116-01, R-116-08)
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex (pipeline :api lines 115–125)
@examples/threadline_phoenix/lib/threadline_phoenix_web/user_auth.ex (`fetch_current_scope/2`)
@examples/threadline_phoenix/test/support/conn_case.ex (`sigra_conn/2`)
@examples/threadline_phoenix/test/support/conn_case_helpers.ex (`log_in_user/2` — `init_test_session` + `put_session(:user_token, token)`)
@examples/threadline_phoenix/test/support/fixtures/auth_fixtures.ex (`log_in_user_with_org/3` session org staging pattern)
@guides/integrations/sigra.md (host-owned auth cross-link target)
@test/support/getting_started_fixtures.ex (`router-pipeline-actor-fn` fence extraction — session plugs must stay outside fence)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-116-01 | Session plugs missing or after Threadline.Plug — API routes never load login cookie | high | Task 1 inserts `plug(:fetch_session)` and `plug(:fetch_current_scope)` immediately after `pipeline :api do` and **before** `# doc: start: router-pipeline-actor-fn`; verify with grep |
| T-116-02 | `fetch_current_scope` on `:api` breaks `sigra_conn/2` direct assigns — CI red, false regression signal | high | Task 2 refactors `sigra_conn/2` to session-token path; run all `posts_*_test.exs` and `mix verify.example` before plan close |
| T-116-03 | Dual-contract drift between example README and getting-started §6 curl | medium | Tasks 3–4 same commit; Task 5 locks shared literals in both contract tests |
| T-116-04 | Doc fence change breaks `getting_started_saas_doc_contract_test` router_block extract | medium | Keep `router-pipeline-actor-fn` markers unchanged; session plugs outside fence per R-116-08 |
| T-116-05 | README implies Threadline ships API bearer tokens | medium | Literal lock `does not ship API bearer`; reject Bearer plug per D-116-01f |
| T-116-06 | Passwords embedded in curl section | medium | Contract test refutes password-shaped literals in curl fence vicinity |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add session plugs to `:api` pipeline (outside doc fence)</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex (pipeline :api lines 115–125; pipeline :browser lines 9–18 for plug order reference)
    - .planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-01b)
    - .planning/phases/116-example-first-hour-fixes/116-RESEARCH.md (R-116-08)
  </read_first>
  <action>
In `examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex`, replace the `:api` pipeline block with **exactly** this shape (session plugs before the doc fence; fence markers unchanged):

```elixir
  pipeline :api do
    plug(:fetch_session)
    plug(:fetch_current_scope)

    # doc: start: router-pipeline-actor-fn
    plug(:accepts, ["json"])

    plug(Threadline.Plug,
      actor_fn: &Threadline.Integrations.Sigra.actor_ref_from_conn/1,
      context_overrides_fn: &Threadline.Integrations.Sigra.audit_context_overrides_from_conn/1
    )

    # doc: end: router-pipeline-actor-fn
  end
```

Do **not** move or rename `# doc: start: router-pipeline-actor-fn` / `# doc: end: router-pipeline-actor-fn`.
Do **not** add session plugs inside the fenced block.
`import ThreadlinePhoenixWeb.UserAuth` is already present at line 4 — no new import.
  </action>
  <acceptance_criteria>
    - `grep -n 'pipeline :api' -A8 examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` shows `fetch_session` and `fetch_current_scope` before `# doc: start: router-pipeline-actor-fn`
    - `grep -F '# doc: start: router-pipeline-actor-fn' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` matches once
    - `mix test test/threadline/getting_started_saas_doc_contract_test.exs` exits 0 (router_block extract unchanged)
  </acceptance_criteria>
  <verify>grep -n 'fetch_session\|fetch_current_scope\|router-pipeline-actor-fn' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</verify>
</task>

<task type="auto">
  <name>Task 2: Fix `sigra_conn/2` for session-plug compatibility</name>
  <files>examples/threadline_phoenix/test/support/conn_case.ex, examples/threadline_phoenix/test/threadline_phoenix_web/posts_audit_path_test.exs, examples/threadline_phoenix/test/threadline_phoenix_web/posts_correlation_path_test.exs, examples/threadline_phoenix/test/threadline_phoenix_web/posts_incident_json_path_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/test/support/conn_case.ex (`sigra_conn/2` lines 49–76)
    - examples/threadline_phoenix/test/support/conn_case_helpers.ex (`log_in_user/2` lines 49–56)
    - examples/threadline_phoenix/test/support/fixtures/auth_fixtures.ex (`log_in_user_with_org/3` lines 138–163)
    - examples/threadline_phoenix/lib/threadline_phoenix_web/user_auth.ex (`fetch_current_scope/2`, `ensure_user_token/1`)
    - .planning/phases/116-example-first-hour-fixes/116-RESEARCH.md (§2.5, R-116-01)
  </read_first>
  <action>
1. Refactor `sigra_conn/2` in `conn_case.ex` — **remove** direct `Plug.Conn.assign(:current_scope, …)` and private `:sigra_session` staging as the primary path. Replace with session-token staging:

   - Resolve user: `user = Map.get(attrs, :user) || ThreadlinePhoenix.AccountsFixtures.user_fixture()`
   - Generate token: `token = ThreadlinePhoenix.Accounts.generate_user_session_token(user)`
   - When `active_organization_id` present in attrs: after token generation, load session via `Accounts.get_user_and_session_by_token(token)` and `Repo.update!` the session row's `active_organization_id` (mirror `AccountsFixtures.log_in_user_with_org/3` lines 145–151; org id may be a string UUID from `create_organization/1` when test passes org attrs)
   - Return conn via:

```elixir
conn
|> Phoenix.ConnTest.init_test_session(%{})
|> Plug.Conn.put_session(:user_token, token)
```

   - Update `@moduledoc` for `sigra_conn/2` to state it stages auth through `:user_token` session key (compatible with `fetch_current_scope/2` on `:api`).

2. Update `posts_audit_path_test.exs`:
   - Bind `user = ThreadlinePhoenix.AccountsFixtures.user_fixture()` before `sigra_conn`
   - Call `sigra_conn(%{user: user})` (drop hardcoded `user_id` / `session_id` attrs)
   - Change actor assertion from `id: "phase-44-user"` to `id: to_string(user.id)`

3. Update `posts_correlation_path_test.exs`:
   - First test: use `sigra_conn(%{user: user_fixture()})` — correlation still from `x-correlation-id` header
   - Second test (Sigra-derived correlation): create org via `create_organization/1`, pass `active_organization_id: org.id` in attrs; after `POST`, load the persisted Sigra session from DB and compute expected correlation as `"sigra-session:#{session.id}:org:#{org.id}"` instead of hardcoded `session_id` string

4. Update `posts_incident_json_path_test.exs`: replace hardcoded `sigra_conn` user_id attrs with `user = user_fixture(); sigra_conn(%{user: user})` pattern (preserve test intent; actor must be non-nil after dispatch)

5. Do **not** change production `user_auth.ex` or `post_controller.ex`.
  </action>
  <acceptance_criteria>
    - `grep -F 'init_test_session' examples/threadline_phoenix/test/support/conn_case.ex` matches inside `sigra_conn`
    - `grep -F 'put_session(:user_token' examples/threadline_phoenix/test/support/conn_case.ex` matches inside `sigra_conn`
    - `grep -F 'assign(:current_scope' examples/threadline_phoenix/test/support/conn_case.ex` returns no matches inside `def sigra_conn` body (direct assign removed)
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix_web/posts_audit_path_test.exs test/threadline_phoenix_web/posts_correlation_path_test.exs test/threadline_phoenix_web/posts_incident_json_path_test.exs` exits 0
    - `mix verify.example` exits 0
  </acceptance_criteria>
  <verify>cd examples/threadline_phoenix && mix test test/threadline_phoenix_web/posts_audit_path_test.exs test/threadline_phoenix_web/posts_correlation_path_test.exs test/threadline_phoenix_web/posts_incident_json_path_test.exs</verify>
</task>

<task type="auto">
  <name>Task 3: Example README auth subsection + cookie curl</name>
  <files>examples/threadline_phoenix/README.md</files>
  <read_first>
    - examples/threadline_phoenix/README.md (## Audited HTTP path section; curl block ~lines 336–346)
    - .planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-01c, D-116-01d)
    - examples/threadline_phoenix/WALKTHROUGH.md (cookie `-b` precedent)
    - guides/integrations/sigra.md (host-owned auth pointer)
  </read_first>
  <action>
Under `## Audited HTTP path (\`POST /api/posts\`)`, insert **`### Authenticate before the audited API call`** immediately **before** the existing curl block (before the line `Example request (include **`x-request-id`**…` or replace that lead-in so auth subsection precedes curl).

Subsection must include these literals (exact phrases unless noted):

- `Threadline.Integrations.Sigra.actor_ref_from_conn/1` reads Sigra state from `current_scope`
- `This example does not ship API bearer tokens` (or `does not ship API bearer` — pick one phrase and use consistently in README + contract)
- Steps: `mix phx.server` → optional `mix demo.seed` + link `[DEMO_USERS.md](DEMO_USERS.md)` → sign in at `/users/log_in` → copy `_threadline_phoenix_key` cookie → curl with `-b '_threadline_phoenix_key=PASTE_FROM_BROWSER'`
- Mention `plug(:fetch_session)` and `plug(:fetch_current_scope)` on the `:api` pipeline (use exact plug names including `fetch_current_scope`)
- **Expected with session:** `201` + `audit_transaction_id`
- **Without session:** `500` + `missing actor` is intentional for this reference lane
- **CI without browser:** `mix test test/threadline_phoenix_web/posts_audit_path_test.exs` with note that tests stage scope via `sigra_conn/2` in tests only
- Cross-link `../../guides/integrations/sigra.md` for host-owned auth

Replace the canonical curl block with **one** block (remove any bare unauthenticated duplicate):

```bash
curl -sS -X POST "http://localhost:4000/api/posts" \
  -H "content-type: application/json" \
  -H "x-request-id: $(uuidgen)" \
  -H "x-correlation-id: demo-corr" \
  -b '_threadline_phoenix_key=PASTE_FROM_BROWSER' \
  -d '{"post":{"title":"Hello","slug":"hello-demo-slug"}}'
```

Do **not** add passwords or DEMO user credentials inside the curl fence.
Limit edits to the Audited HTTP / curl area — defer README install restructure to Plan 02.
  </action>
  <acceptance_criteria>
    - `grep -F '### Authenticate before the audited API call' examples/threadline_phoenix/README.md` matches
    - `grep -F 'fetch_current_scope' examples/threadline_phoenix/README.md` matches
    - `grep -F '_threadline_phoenix_key=PASTE_FROM_BROWSER' examples/threadline_phoenix/README.md` matches
    - `grep -F 'missing actor' examples/threadline_phoenix/README.md` matches
    - `grep -F 'does not ship API bearer' examples/threadline_phoenix/README.md` matches
    - `grep -F 'password123456' examples/threadline_phoenix/README.md` returns no matches
  </acceptance_criteria>
  <verify>grep -n 'Authenticate before\|fetch_current_scope\|_threadline_phoenix_key\|missing actor' examples/threadline_phoenix/README.md</verify>
</task>

<task type="auto">
  <name>Task 4: Sync getting-started §6 auth staging + curl</name>
  <files>guides/getting-started-saas.md</files>
  <read_first>
    - guides/getting-started-saas.md (## 6. Exercise the first audited write, curl block lines 117–125)
    - examples/threadline_phoenix/README.md (Task 3 auth subsection — mirror story)
    - .planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-01e)
  </read_first>
  <action>
In `guides/getting-started-saas.md`, within **`## 6. Exercise the first audited write`**, insert **`### Authenticate before the audited API call`** before the sentence `Start your Phoenix app, then send the first audited request:`.

Content (adapt greenfield wording while preserving shared literals):

- Host establishes identity on the conn before `Threadline.Plug` runs
- Reference app path: sign in on your app's login route (for this repo: `/users/log_in`), copy `_threadline_phoenix_key`, pass `-b '_threadline_phoenix_key=PASTE_FROM_BROWSER'`
- Include `does not ship API bearer` boundary phrase (match README Task 3)
- Without session on reference lane: `500` + `missing actor` is intentional
- Link `integrations/sigra.md`

Update the §6 curl block to match README (add `-b '_threadline_phoenix_key=PASTE_FROM_BROWSER'` line; keep existing headers and JSON body).

Do **not** change §6 Elixir `Audit.transaction/3` snippet or other section headings.
  </action>
  <acceptance_criteria>
    - `grep -F '### Authenticate before the audited API call' guides/getting-started-saas.md` matches
    - `grep -F '_threadline_phoenix_key=PASTE_FROM_BROWSER' guides/getting-started-saas.md` matches
    - `grep -F 'does not ship API bearer' guides/getting-started-saas.md` matches
    - `grep -F '## 6. Exercise the first audited write' guides/getting-started-saas.md` still matches (heading unchanged)
  </acceptance_criteria>
  <verify>grep -n 'Authenticate before\|_threadline_phoenix_key\|does not ship API bearer' guides/getting-started-saas.md</verify>
</task>

<task type="auto">
  <name>Task 5: Doc-contract locks for API auth staging (EXAMPLE-04 partial)</name>
  <files>test/threadline/example_phoenix_readme_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/example_phoenix_readme_contract_test.exs (existing 6 tests — do not weaken)
    - test/threadline/getting_started_saas_doc_contract_test.exs (router_block via GettingStartedFixtures)
    - .planning/phases/116-example-first-hour-fixes/116-CONTEXT.md (D-116-04b, D-116-04d)
  </read_first>
  <action>
1. In `test/threadline/example_phoenix_readme_contract_test.exs`, add test `"example README documents API auth staging for POST /api/posts"` with minimum asserts on `examples/threadline_phoenix/README.md`:

```elixir
assert String.contains?(doc, "Authenticate before")
assert String.contains?(doc, "fetch_current_scope")
assert String.contains?(doc, "missing actor")
assert String.contains?(doc, "DEMO_USERS.md")
assert String.contains?(doc, "_threadline_phoenix_key")
assert String.contains?(doc, "does not ship API bearer")
```

Add refute for password in curl vicinity — scan lines between `### Authenticate before` and the closing ``` of the curl fence; refute `password123456` and `@example.com` credential patterns if present.

**Retain** all 6 existing tests unchanged.

2. In `test/threadline/getting_started_saas_doc_contract_test.exs`, extend `"quickstart guide locks the adopter walkthrough"` (or add sibling test) with:

```elixir
assert String.contains?(doc, "Authenticate before")
assert String.contains?(doc, "_threadline_phoenix_key")
assert String.contains?(doc, "does not ship API bearer")
```

Do **not** lock full curl fences or DEMO passwords per D-116-04d.
  </action>
  <acceptance_criteria>
    - `grep -F 'API auth staging for POST /api/posts' test/threadline/example_phoenix_readme_contract_test.exs` matches
    - `grep -F 'does not ship API bearer' test/threadline/example_phoenix_readme_contract_test.exs` matches
    - `grep -F 'Authenticate before' test/threadline/getting_started_saas_doc_contract_test.exs` matches
    - `mix test test/threadline/example_phoenix_readme_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/example_phoenix_readme_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs</verify>
</task>

</tasks>

<verification>
Run after all tasks:

```bash
cd examples/threadline_phoenix && mix test test/threadline_phoenix_web/posts_audit_path_test.exs test/threadline_phoenix_web/posts_correlation_path_test.exs test/threadline_phoenix_web/posts_incident_json_path_test.exs
mix verify.example
mix test test/threadline/example_phoenix_readme_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs
```

All must exit 0 before marking plan complete.
</verification>

<success_criteria>
- EXAMPLE-01: API auth staging documented; router loads session on `:api`; cookie curl is canonical happy path.
- EXAMPLE-04 (partial): Example + getting-started auth contract tests green; `mix verify.example` green.
</success_criteria>
