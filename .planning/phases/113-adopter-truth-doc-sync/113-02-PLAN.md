---
phase: 113-adopter-truth-doc-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/WALKTHROUGH.md
  - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs
autonomous: true
requirements:
  - TRUTH-04
must_haves:
  truths:
    - "WALK-03-02 operator question uses demo_last_tuesday through demo_epoch anchors (not last 24 hours)"
    - "Expected outcome describes tickets table status changes only (no ticket/reply overclaim)"
    - "demo_contract_test asserts leaving-agent count == 12 with tickets audit_changes join"
    - "walkthrough_doc_contract_test locks demo_last_tuesday, demo_epoch, agent2 user id literals"
  artifacts:
    - path: examples/threadline_phoenix/WALKTHROUGH.md
      provides: WR-110-001 prose fix at WALK-03-02
    - path: examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
      provides: strengthened SEED-03 leaving agent window contract
---

<objective>
Fix WALK-03-02 operator fiction to match seeded time anchors and ticket-only leaving-agent activity. Extend walkthrough and demo contract tests. Closes TRUTH-04 / WR-110-001 / D-113-04.
</objective>

<context>
@.planning/phases/113-adopter-truth-doc-sync/113-CONTEXT.md (D-113-04)
@.planning/phases/113-adopter-truth-doc-sync/113-RESEARCH.md (§4)
@.planning/phases/113-adopter-truth-doc-sync/113-PATTERNS.md (walkthrough literal + demo contract patterns)
@examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex (@leaving_agent_tx_count 12)
@.planning/phases/110-triage-narrow-fixes/110-REVIEW.md (WR-110-001)
</context>

<scope_guard>
**ALLOWED:** WALKTHROUGH.md WALK-03-02 section only (not §5 footnote — Plan 04); demo_contract_test.exs; walkthrough_doc_contract_test.exs  
**FORBIDDEN:** LiveView filter UI tests; locking full operator-question prose in contracts; seed data changes
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| — | Doc fiction misleads evaluators on time window | medium | Frozen anchor prose + literal contract tests |
| — | Overclaimed ticket_replies in walkthrough | low | Align expected outcome with seed (tickets only) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace WALK-03-02 operator question and expected outcome</name>
  <files>examples/threadline_phoenix/WALKTHROUGH.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (WALK-03-02 at :449-477)
    - .planning/phases/113-adopter-truth-doc-sync/113-CONTEXT.md (D-113-04a/b exact wording)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex (seed_leaving_agent_window — tickets only)
  </read_first>
  <action>
In **Step WALK-03-02**, replace the operator question line (~451) with exactly:

```markdown
**Operator question:** Agent **`agent2@acme.example.com`** is leaving — what did they touch from **`demo_last_tuesday`** through **`demo_epoch`**?
```

Update **Do** step 4 (~460): change "Scan the actor history list for Acme help-desk tables (`tickets`, `ticket_replies`)" to scan for **`tickets`** table activity (status changes) — remove `ticket_replies` from this step.

Replace **Expected outcome** bullets (~462-465):

```markdown
- Actor history lists seeded leaving-agent window activity (**ticket status changes on `tickets` only** — 12 transactions in seed)
- Rows span Acme org only for this persona — not cross-org admin noise
- Each entry drill-downs to a transaction with **`agent2@acme.example.com`** as actor
```

Remove any "ticket/reply" or "last 24 hours" phrasing from WALK-03-02 section.
  </action>
  <acceptance_criteria>
    - `grep -n "last 24 hours" examples/threadline_phoenix/WALKTHROUGH.md` returns no matches inside lines 449-477 (WALK-03-02 section)
    - `grep -n "demo_last_tuesday" examples/threadline_phoenix/WALKTHROUGH.md` includes a match on the operator question line in WALK-03-02
    - WALK-03-02 expected outcome contains substring `tickets` and does not contain `ticket/reply`
  </acceptance_criteria>
  <verify>grep -A2 "WALK-03-02" examples/threadline_phoenix/WALKTHROUGH.md | head -20</verify>
</task>

<task type="auto">
  <name>Task 2: Extend walkthrough_doc_contract_test literals</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs
    - .planning/phases/113-adopter-truth-doc-sync/113-CONTEXT.md (D-113-04c)
  </read_first>
  <action>
Add to the `for literal <- [...]` list:

```elixir
"demo_last_tuesday",
"demo_epoch",
"33123cc4-da21-5674-b030-e168cee90521"
```

Do **not** add full operator-question prose or `"last 24 hours"` refute assertions.
  </action>
  <acceptance_criteria>
    - `grep "demo_last_tuesday" examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches
    - `grep "33123cc4-da21-5674-b030-e168cee90521" examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs` matches
  </acceptance_criteria>
  <verify>cd examples/threadline_phoenix && mix test test/threadline_phoenix/walkthrough_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 3: Strengthen demo_contract_test leaving-agent window</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (:105-126)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex (last_tuesday/0, epoch/0)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex (@leaving_agent_tx_count 12)
  </read_first>
  <action>
In `"SEED-03 leaving agent window"` test, replace hard-coded timestamps with:

```elixir
from_ts = Manifest.last_tuesday()
to_ts = Manifest.epoch()
```

Change `assert count >= 1` to `assert count == 12`.

Add after count assertion:

```elixir
ticket_change_count =
  Repo.aggregate(
    from(ac in AuditChange,
      join: at in assoc(ac, :transaction),
      where: ac.table_name == "tickets",
      where:
        fragment("? @> ?::jsonb", at.actor_ref, ^ActorRef.to_map(agent2_ref)),
      where: at.occurred_at >= ^from_ts,
      where: at.occurred_at <= ^to_ts
    ),
    :count
  )

assert ticket_change_count >= 1
```

Ensure `alias Threadline.Capture.AuditChange` is present at module top (add if missing).
  </action>
  <acceptance_criteria>
    - Test contains `assert count == 12`
    - Test contains `ac.table_name == "tickets"`
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs --only demo_contract` exits 0 (or full file if no only tag)
  </acceptance_criteria>
  <verify>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs:105</verify>
</task>

</tasks>

<verification>
`cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs test/threadline_phoenix/walkthrough_doc_contract_test.exs`
</verification>

<success_criteria>
- WR-110-001 closed: WALK-03-02 prose matches seed fiction
- Contract tests prevent regression on anchors and leaving-agent count
</success_criteria>
