---
phase: 63
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - CHANGELOG.md
  - test/threadline/operator_surface_doc_contract_test.exs
autonomous: true
requirements:
  - DOC-01
  - DOC-04
must_haves:
  truths:
    - "Changelog documents v1.17 operator surface features"
    - "Doc contract test locks README and guide literals against drift"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release notes"
      contains: "OperatorSurface.Router"
    - path: "test/threadline/operator_surface_doc_contract_test.exs"
      provides: "ExUnit drift prevention"
      contains: "File.read!"
  key_links:
    - from: "test/threadline/operator_surface_doc_contract_test.exs"
      to: "README.md"
      via: "File.read!/1"
      pattern: "File\\.read!\\(.*README\\.md.*\\)"
---

<objective>
Update the CHANGELOG for the next release to communicate the new optional dependencies and mount macro, and write a strict ExUnit doc-contract test to lock the new surface's route literals and macro signatures.

Purpose: Prevent silent documentation drift. When the implementation changes in the future, CI must fail if the documentation isn't updated.
Output: Updated CHANGELOG.md and a new doc-contract test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-docs-contracts-and-changelog/63-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>Under the `## [Unreleased]` heading, create a new `### Added` entry detailing the v1.17 Operator Surface. Clearly document that `phoenix`, `phoenix_live_view`, `phoenix_html`, and `phoenix_pubsub` are now `optional: true` dependencies (meaning zero bloat for capture-only adopters). Document the new `Threadline.OperatorSurface.Router` mount macro and the `mix.exs` adjustments hosts need if they want the surface.</action>
  <verify>
    <automated>grep "optional: true" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md contains the v1.17 operator surface release notes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Operator Surface Doc Contract Test</name>
  <files>test/threadline/operator_surface_doc_contract_test.exs</files>
  <behavior>
    - The test reads `README.md` and asserts it contains the `threadline_operator_surface` macro signature.
    - The test asserts that the default route literals `/audit/transactions/:id`, `/audit/actors/:kind/:id`, and `/audit/rows/:table/:pk` are present in either `README.md` or `guides/operator-surface.md`.
    - The test asserts that the auth section strings or fail-closed explanations are present.
  </behavior>
  <action>Create a new ExUnit test file `test/threadline/operator_surface_doc_contract_test.exs`. Use `File.read!("README.md")` and `File.read!("guides/operator-surface.md")` to assert the presence of critical substrings (like `threadline_operator_surface`, `/audit/transactions/:id`, `/audit/actors/:kind/:id`, and `/audit/rows/:table/:pk`). Model the assertions on `test/threadline/readme_doc_contract_test.exs`.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface_doc_contract_test.exs</automated>
  </verify>
  <done>Doc-contract test passes, locking the new UI surface documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test to Source | Tests must assert against the actual documentation files to be effective. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-02 | Tampering | `test/threadline/operator_surface_doc_contract_test.exs` | mitigate | Assert exact strings rather than vague regexes to ensure instructions copy-pasted by adopters are valid. |
</threat_model>

<verification>
- `CHANGELOG.md` properly communicates the optional dependency posture.
- `test/threadline/operator_surface_doc_contract_test.exs` passes and fails correctly if a string is changed.
</verification>

<success_criteria>
CI will fail if the core operator surface documentation drifts from the implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/63-docs-contracts-and-changelog/63-02-SUMMARY.md`
</output>
