---
phase: 63
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/operator-surface.md
  - README.md
  - guides/production-checklist.md
  - examples/threadline_phoenix/README.md
autonomous: true
requirements:
  - DOC-02
  - DOC-03
must_haves:
  truths:
    - "Operator surface guide exists and documents mount, auth, screens, and Mix task"
    - "README contains an Operator Surface section with a 1-minute mount example pointing to the guide"
    - "Production checklist requires operators to wire the surface behind an authenticated admin pipeline"
    - "Example app README explains the end-to-end surface usage"
  artifacts:
    - path: "guides/operator-surface.md"
      provides: "Definitive operator manual"
      min_lines: 50
    - path: "README.md"
      provides: "Entrypoint discovery"
      contains: "Operator Surface"
    - path: "guides/production-checklist.md"
      provides: "Release safety requirements"
      contains: "operator surface"
    - path: "examples/threadline_phoenix/README.md"
      provides: "End-to-end operator usage guide"
      contains: "operator surface"
  key_links:
    - from: "README.md"
      to: "guides/operator-surface.md"
      via: "Markdown link"
      pattern: "\\[.*\\]\\(guides/operator-surface\\.md\\)"
    - from: "examples/threadline_phoenix/README.md"
      to: "guides/operator-surface.md"
      via: "Markdown link"
      pattern: "\\[.*\\]\\(\\.\\./guides/operator-surface\\.md\\)|\\[.*\\]\\(guides/operator-surface\\.md\\)"
---

<objective>
Create the definitive Operator Surface guide and update all high-level documentation hubs (README, production checklist, example app README) to make the new LiveView surface discoverable.

Purpose: To ensure the v1.17 operator surface is fully documented without bloating the core library README, keeping the "host owns auth" boundary clear for adopters.
Output: A new comprehensive guide and updated documentation hubs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-docs-contracts-and-changelog/63-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Operator Surface Guide</name>
  <files>guides/operator-surface.md</files>
  <action>Create a new comprehensive guide for the operator surface. Document the following:
1. The 1-minute mount (`threadline_operator_surface "/audit", opts`).
2. The `:actor_fn` and `:authorize_fn` configuration options (explicitly explaining the "fail-closed" default and how the host provides policy).
3. The screens shipped in v1.17: Incident Drill-down (`/audit/transactions/:id`), Actor Window (`/audit/actors/:kind/:id`), and Row History / As-of Sub-view (`/audit/rows/:table/:pk`). Document the support questions each screen answers.
4. The `mix threadline.incident` companion task for SSH/CLI-only operators.
Do not embed literal code examples that drift easily unless necessary, but clearly provide the `Threadline.OperatorSurface.Router` macro signature so it can be locked by a doc-contract test.</action>
  <verify>
    <automated>test -f guides/operator-surface.md</automated>
  </verify>
  <done>The guide is created with all required sections.</done>
</task>

<task type="auto">
  <name>Task 2: Update README and Example App README</name>
  <files>README.md, examples/threadline_phoenix/README.md</files>
  <action>In `README.md`, add a top-level `## Operator Surface` section. Keep it scannable: provide a "1-minute mount" code snippet using `import Threadline.OperatorSurface.Router`, and then link directly to `guides/operator-surface.md` for policy and auth details. Do not duplicate the guide's content.
In `examples/threadline_phoenix/README.md`, add documentation explaining the wired example end-to-end, pointing out the authenticated admin pipeline that protects the mounted operator surface routes.</action>
  <verify>
    <automated>grep "## Operator Surface" README.md</automated>
  </verify>
  <done>Both README files point adopters to the operator surface and the new guide.</done>
</task>

<task type="auto">
  <name>Task 3: Update Production Checklist</name>
  <files>guides/production-checklist.md</files>
  <action>Add a new row or item to the production checklist reminding adopters to mount the operator surface behind an authenticated admin pipeline, and providing a link to the `guides/operator-surface.md`.</action>
  <verify>
    <automated>grep -i "operator surface" guides/production-checklist.md</automated>
  </verify>
  <done>The production checklist includes the operator surface security reminder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation to Adopter | Inaccurate documentation can lead to an adopter opening a security hole (e.g., misconfiguring `:authorize_fn`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Information Disclosure | `guides/operator-surface.md` | mitigate | Ensure the guide strongly emphasizes the "fail-closed" auth macro default and instructs the operator to implement rigorous access control. |
</threat_model>

<verification>
- `guides/operator-surface.md` exists and contains required sections.
- `README.md` includes the `## Operator Surface` heading and points to the guide.
- `guides/production-checklist.md` references the operator surface.
- `examples/threadline_phoenix/README.md` references the operator surface.
</verification>

<success_criteria>
Documentation hubs provide a clear path to discovering and safely mounting the operator surface.
</success_criteria>

<output>
After completion, create `.planning/phases/63-docs-contracts-and-changelog/63-01-SUMMARY.md`
</output>
