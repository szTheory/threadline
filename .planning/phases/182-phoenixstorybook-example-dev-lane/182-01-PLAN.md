---
phase: 182-phoenixstorybook-example-dev-lane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/operator_surface/storybook_boundary_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix_web/storybook_route_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix_web/storybook_stories_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts
  - test/threadline/example_phoenix_readme_contract_test.exs
  - test/threadline/operator_surface_doc_contract_test.exs
autonomous: true
requirements:
  - STORY-01
  - STORY-02
  - STORY-03
must_haves:
  truths:
    - "STORY-01: The Phoenix example app has tests proving the maintainer component-lab lane is example-only, dev/test-only, and not a root dependency."
    - "STORY-02: Story coverage tests require the category spine, Threadline theme wrapper, and representative ugly-data vocabulary before implementation."
    - "STORY-03: Browser and docs contracts preserve /audit/__stress as the authenticated operator-flow stress harness."
    - "D-182-01, D-182-02, D-182-03, D-182-04, D-182-05, D-182-19, D-182-20, D-182-21, and D-182-22 are locked by executable contracts before implementation."
  artifacts:
    - path: "test/threadline/operator_surface/storybook_boundary_test.exs"
      provides: "Root/package/source boundary tests for Storybook absence"
    - path: "examples/threadline_phoenix/test/threadline_phoenix_web/storybook_route_test.exs"
      provides: "Example-app route and production-absence contracts"
    - path: "examples/threadline_phoenix/test/threadline_phoenix_web/storybook_stories_test.exs"
      provides: "Storybook backend, category, wrapper, theme, and fixture contracts"
    - path: "examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts"
      provides: "Bounded browser smoke for index and representative stories"
    - path: "test/threadline/example_phoenix_readme_contract_test.exs"
      provides: "Example README Storybook-vs-stress docs contract"
    - path: "test/threadline/operator_surface_doc_contract_test.exs"
      provides: "Operator guide Storybook-vs-stress docs contract"
  key_links:
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      to: "examples/threadline_phoenix/test/threadline_phoenix_web/storybook_route_test.exs"
      via: "compiled route table and dev_routes source contract"
      pattern: "dev_routes"
    - from: "examples/threadline_phoenix/storybook"
      to: "examples/threadline_phoenix/test/threadline_phoenix_web/storybook_stories_test.exs"
      via: "curated category and wrapper assertions"
      pattern: "threadline-ui"
    - from: "examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts"
      to: "examples/threadline_phoenix/e2e/tests/operator-stress.spec.ts"
      via: "bounded smoke pattern without screenshot matrix expansion"
      pattern: "expectNoHorizontalOverflow"
---

<objective>
Create the RED contract layer for Phase 182 before implementation starts.

Purpose: Lock the example-app-only package boundary, route gating, story taxonomy, theme wrapper, browser smoke, and docs posture so later plans can turn the contracts green without interpretation.
Output: New or extended ExUnit and Playwright contract files.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-CONTEXT.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-RESEARCH.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-VALIDATION.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-PATTERNS.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-UI-SPEC.md
@CLAUDE.md
@examples/threadline_phoenix/AGENTS.md
@test/threadline/operator_surface/stress_router_test.exs
@test/threadline/operator_surface/stress_fixtures_test.exs
@test/threadline/example_phoenix_readme_contract_test.exs
@test/threadline/operator_surface_doc_contract_test.exs
@examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
@examples/threadline_phoenix/e2e/tests/operator-stress.spec.ts
@examples/threadline_phoenix/e2e/playwright.config.ts
</context>

<!-- planner-discipline-allow: PhoenixStorybook -->
<!-- planner-discipline-allow: phoenix_storybook -->
<!-- planner-discipline-allow: live_storybook -->
<!-- planner-discipline-allow: /dev/storybook -->

## Artifacts this phase produces

- New route paths: `/dev/storybook` and `/dev/storybook/assets`, only in `examples/threadline_phoenix` dev/test route configuration.
- New dependency boundary: `{:phoenix_storybook, "~> 1.2.0", only: [:dev, :test]}` in `examples/threadline_phoenix/mix.exs` only, with example lockfile resolution.
- New modules: `ThreadlinePhoenixWeb.Storybook`, `ThreadlinePhoenixWeb.Storybook.Wrapper`, and `ThreadlinePhoenixWeb.Storybook.Fixtures`.
- New wrapper/helper functions: `threadline_preview/1`, `preview_section/1`, `theme_modes/0`, `ugly_cases/0`, `sample/1`, and `component_assigns/1`.
- New story files under `examples/threadline_phoenix/storybook/` for Foundations, Primitives, Forms, States, Overlays, Data Display, Groups, and Patterns.
- New tests: `Threadline.OperatorSurface.StorybookBoundaryTest`, `ThreadlinePhoenixWeb.StorybookRouteTest`, `ThreadlinePhoenixWeb.StorybookStoriesTest`, and Playwright `operator-storybook.spec.ts`.
- New docs/evidence paths: updated `examples/threadline_phoenix/README.md`, optionally updated `guides/operator-surface.md`, and `.planning/phases/182-phoenixstorybook-example-dev-lane/182-VERIFICATION.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add root boundary and docs RED contracts</name>
  <files>test/threadline/operator_surface/storybook_boundary_test.exs, test/threadline/example_phoenix_readme_contract_test.exs, test/threadline/operator_surface_doc_contract_test.exs</files>
  <read_first>
    @test/threadline/operator_surface/storybook_boundary_test.exs (create; read if present)
    @test/threadline/operator_surface/stress_router_test.exs
    @test/threadline/example_phoenix_readme_contract_test.exs
    @test/threadline/operator_surface_doc_contract_test.exs
    @mix.exs
    @lib/threadline/operator_surface/router.ex
    @lib/threadline/operator_surface/stress_router.ex
    @examples/threadline_phoenix/README.md
    @guides/operator-surface.md
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-CONTEXT.md
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-PATTERNS.md
  </read_first>
  <behavior>
    - RED expectation: root source/package files still pass absence checks, but docs assertions fail until Plan 05 adds maintainer-only Storybook-vs-stress wording.
    - RED expectation: doc contracts require D-182-21 and D-182-22 language without telling adopters to install the example-app component lab.
    - GREEN target for later plans: root compile without optional dependencies remains green after the example app gains its dev/test tooling.
  </behavior>
  <action>Create `Threadline.OperatorSurface.StorybookBoundaryTest` using the source-contract patterns from `stress_router_test.exs`. Assert the root package, root lockfile, root public router macro source, and root `lib/threadline` source do not gain the component-lab package/module/router macro surface while the example app is allowed to own it per D-182-01 and D-182-19. Extend the existing example README and operator-guide doc contract tests with assertions for D-182-21 and D-182-22: component documentation/design review belongs to maintainers in the example app, `/audit/__stress` remains authenticated operator-flow stress testing, neither lane is a production route, and adopters are not instructed to install the component-lab dependency in host apps.</action>
  <verify>
    <automated>bash -lc 'set +e; mix test test/threadline/operator_surface/storybook_boundary_test.exs test/threadline/example_phoenix_readme_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs; status=$?; test "$status" -ne 0'</automated>
    <automated>mix verify.compile_no_optional</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/threadline/operator_surface/storybook_boundary_test.exs` exists and scans root package/source surfaces plus the allowed example-app boundary.
    - Source assertion: README and operator-guide doc contracts include Storybook-vs-stress expectations from D-182-21 and D-182-22.
    - Test-command assertion: the combined docs/source contract command fails before implementation because expected documentation is not present yet.
    - Test-command assertion: `mix verify.compile_no_optional` still passes before implementation.
  </acceptance_criteria>
  <done>Root boundary and docs posture contracts exist before any package or route change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add example route and story RED contracts</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix_web/storybook_route_test.exs, examples/threadline_phoenix/test/threadline_phoenix_web/storybook_stories_test.exs</files>
  <read_first>
    @examples/threadline_phoenix/test/threadline_phoenix_web/storybook_route_test.exs (create; read if present)
    @examples/threadline_phoenix/test/threadline_phoenix_web/storybook_stories_test.exs (create; read if present)
    @examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
    @examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
    @examples/threadline_phoenix/config/dev.exs
    @examples/threadline_phoenix/config/test.exs
    @examples/threadline_phoenix/config/prod.exs
    @lib/threadline/operator_surface/ui.ex
    @lib/threadline/operator_surface/style.ex
    @lib/threadline/operator_surface/stress_fixtures.ex
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-PATTERNS.md
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-UI-SPEC.md
  </read_first>
  <behavior>
    - RED expectation: compiled route tests fail until `/dev/storybook` and its asset route are mounted behind the example `dev_routes` branch.
    - RED expectation: story tests fail until backend discovery, category files, wrapper rendering, themes, and representative fixture helpers exist.
    - GREEN target for later plans: the route lives outside `/audit`, uses the documented root-scope router shape, and is absent when dev routes are disabled per D-182-02, D-182-03, and D-182-04.
  </behavior>
  <action>Create `ThreadlinePhoenixWeb.StorybookRouteTest` and `ThreadlinePhoenixWeb.StorybookStoriesTest`. Route tests must inspect compiled routes for the maintainer route and asset path when the example app enables dev/test routes, and must compile or inspect a no-dev-routes router/config path proving production absence per D-182-02 through D-182-05. Story tests must require the D-182-10 category spine, the small D-182-11 Patterns branch, story metadata for D-182-13, the real Threadline wrapper from D-182-14, representative ugly data from D-182-16, and explicit evidence that stories are curated documentation rather than page-flow tests per D-182-06, D-182-07, D-182-08, and D-182-12.</action>
  <verify>
    <automated>bash -lc 'set +e; cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/storybook_route_test.exs test/threadline_phoenix_web/storybook_stories_test.exs; status=$?; test "$status" -ne 0'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: route tests assert dev/test route presence and production/no-dev-routes absence for route and assets.
    - Source assertion: story tests assert Foundations, Primitives, Forms, States, Overlays, Data Display, Groups, and Patterns coverage.
    - Source assertion: story tests require `.threadline-ui`, `data-tl-theme`, and use of `Threadline.OperatorSurface.Style.css`.
    - Test-command assertion: the route/story contract command fails before implementation for the missing route, backend, wrapper, and story files.
  </acceptance_criteria>
  <done>Example route and story contracts are present and RED for the missing implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add bounded Storybook browser smoke scaffold</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts</files>
  <read_first>
    @examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts (create; read if present)
    @examples/threadline_phoenix/e2e/tests/operator-stress.spec.ts
    @examples/threadline_phoenix/e2e/playwright.config.ts
    @examples/threadline_phoenix/e2e/run-e2e.sh
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-VALIDATION.md
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-UI-SPEC.md
  </read_first>
  <behavior>
    - RED expectation: the smoke spec fails until the Storybook index and representative category story URLs render.
    - GREEN target for later plans: the browser verifies index, representative primitive/form/state/overlay/data-display/group stories, assets, theme wrapper, and no horizontal overflow without a screenshot matrix.
  </behavior>
  <action>Create `operator-storybook.spec.ts` by following the bounded patterns in `operator-stress.spec.ts`: helper for horizontal overflow, index smoke, representative story URL smoke, theme wrapper assertions, asset/render assertions, and a small viewport loop. Cover one representative story from primitive, form, state, overlay, data-display, and group categories per D-182-20. Keep page-flow, auth behavior, nav flows, and stress footguns in the existing stress route spec per D-182-12 and STORY-03.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx playwright test --list tests/operator-storybook.spec.ts</automated>
    <automated>test -s examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the spec names `/dev/storybook` and representative story URLs.
    - Source assertion: the spec asserts `.threadline-ui`, a valid `data-tl-theme`, visible preview content, and no horizontal overflow.
    - Source assertion: the spec does not add screenshot expectations or external visual-regression services.
    - Test-command assertion: Playwright can list the new spec without TypeScript discovery errors.
  </acceptance_criteria>
  <done>Bounded browser smoke exists and is ready to turn green after Storybook implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root package -> example app | Tests must prevent maintainer tooling from leaking into the root package dependency or public macro surface. |
| Browser -> dev/test route | Browser smoke will exercise a local maintainer route that must stay out of production. |
| Docs -> adopter behavior | Documentation must not instruct host-app adopters to install example-app maintainer tooling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-01 | Information Disclosure | Dev/test component lab route | mitigate | RED route contracts require production/no-dev-routes absence before Plan 02 implementation is accepted. |
| T-182-02 | Elevation of Privilege | Root package/public router macro | mitigate | Source contracts scan root dependency and route surfaces while allowing example-app ownership only. |
| T-182-03 | Spoofing | Storybook-vs-stress docs | mitigate | Doc contracts require maintainer-only component-doc wording and authenticated stress-harness wording. |
| T-182-SC | Tampering | Package install legitimacy | mitigate | Later dependency work must use the 182-RESEARCH Package Legitimacy Audit and keep the approved Hex package/version inside the example app only. |
</threat_model>

<verification>
Plan 01 completes when all RED contract files exist, their targeted commands fail for the expected missing implementation or docs, and root compile without optional dependencies remains green.
</verification>

<success_criteria>
- STORY-01, STORY-02, and STORY-03 have executable RED contracts.
- All tests are scoped to the example-app maintainer lane, root optional-dependency boundary, curated story coverage, bounded browser smoke, and docs posture.
- No production code or runtime route is implemented by this plan.
</success_criteria>

<output>
Create `.planning/phases/182-phoenixstorybook-example-dev-lane/182-01-SUMMARY.md` when done.
</output>
