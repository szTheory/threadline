---
phase: 182-phoenixstorybook-example-dev-lane
plan: 04
type: execute
wave: 4
depends_on:
  - 182-03
files_modified:
  - examples/threadline_phoenix/storybook/overlays/modal.story.exs
  - examples/threadline_phoenix/storybook/data_display/data_table.story.exs
  - examples/threadline_phoenix/storybook/groups/operator_groups.story.exs
  - examples/threadline_phoenix/storybook/patterns/operator_patterns.story.exs
  - examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts
  - examples/threadline_phoenix/e2e/playwright.config.ts
autonomous: true
requirements:
  - STORY-02
  - STORY-03
must_haves:
  truths:
    - "STORY-02: Overlay, data-display, group, and pattern stories complete the required Storybook coverage."
    - "STORY-03: Browser smoke proves Storybook renders as component documentation while /audit/__stress remains the flow-stress harness."
    - "D-182-10, D-182-11, D-182-12, D-182-15, D-182-16, D-182-17, and D-182-20 are covered without a full screenshot matrix."
  artifacts:
    - path: "examples/threadline_phoenix/storybook/overlays/modal.story.exs"
      provides: "Overlay story coverage"
    - path: "examples/threadline_phoenix/storybook/data_display/data_table.story.exs"
      provides: "Data Display story coverage"
    - path: "examples/threadline_phoenix/storybook/groups/operator_groups.story.exs"
      provides: "Recurring group story coverage"
    - path: "examples/threadline_phoenix/storybook/patterns/operator_patterns.story.exs"
      provides: "Small Patterns branch"
    - path: "examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts"
      provides: "Bounded browser smoke"
    - path: "examples/threadline_phoenix/e2e/playwright.config.ts"
      provides: "Light-lane project inclusion for bounded Storybook smoke"
  key_links:
    - from: "examples/threadline_phoenix/storybook/groups/operator_groups.story.exs"
      to: "lib/threadline/operator_surface/stress_fixtures.ex"
      via: "representative group samples by explicit helper"
      pattern: "group."
    - from: "examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts"
      to: "examples/threadline_phoenix/storybook"
      via: "representative story URL smoke"
      pattern: "/dev/storybook"
---

<objective>
Complete story coverage for overlays, data display, recurring groups, and patterns, then turn the bounded browser smoke green.

Purpose: Give maintainers useful component documentation across the full required catalog without moving flow testing out of `/audit/__stress`.
Output: Remaining curated story files, Playwright smoke coverage, and light-lane config inclusion.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-CONTEXT.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-RESEARCH.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-VALIDATION.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-PATTERNS.md
@.planning/phases/182-phoenixstorybook-example-dev-lane/182-UI-SPEC.md
@examples/threadline_phoenix/AGENTS.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/storybook/wrapper.ex
@examples/threadline_phoenix/lib/threadline_phoenix_web/storybook/fixtures.ex
@examples/threadline_phoenix/test/threadline_phoenix_web/storybook_stories_test.exs
@examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-stress.spec.ts
@examples/threadline_phoenix/e2e/playwright.config.ts
@examples/threadline_phoenix/e2e/run-e2e.sh
@lib/threadline/operator_surface/ui.ex
@lib/threadline/operator_surface/stress_fixtures.ex
</context>

## Artifacts this phase produces

- New story files for Overlays, Data Display, Groups, and Patterns.
- New browser smoke coverage for `/dev/storybook` index plus representative primitive, form, state, overlay, data-display, and group stories.
- Optional Playwright config inclusion of `operator-storybook.spec.ts` in the existing light-lane project testMatch.
- No screenshot baseline matrix, external visual-regression service, new browser framework, auth-flow migration, navigation-flow migration, or stress footgun migration is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add overlay and data-display stories</name>
  <files>examples/threadline_phoenix/storybook/overlays/modal.story.exs, examples/threadline_phoenix/storybook/data_display/data_table.story.exs</files>
  <read_first>
    @examples/threadline_phoenix/storybook/overlays/modal.story.exs (create; read if present)
    @examples/threadline_phoenix/storybook/data_display/data_table.story.exs (create; read if present)
    @examples/threadline_phoenix/lib/threadline_phoenix_web/storybook/wrapper.ex
    @examples/threadline_phoenix/lib/threadline_phoenix_web/storybook/fixtures.ex
    @examples/threadline_phoenix/test/threadline_phoenix_web/storybook_stories_test.exs
    @lib/threadline/operator_surface/ui.ex
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-UI-SPEC.md
  </read_first>
  <behavior>
    - Test expectation: overlay story coverage includes modal, drawer, toast, tooltip, popover, dropdown, accordion, tabs, and segmented-control contracts where supported by `UI`.
    - Test expectation: data-display story coverage includes ref, kv, data table, data panel, code block, detail header, and toolbar contracts where supported by `UI`.
    - Test expectation: static overlay examples document visual/focus contracts without requiring live destructive behavior.
  </behavior>
  <action>Create Overlays and Data Display stories using the existing `UI` components and the wrapper/fixtures from Plan 03. Overlay stories must include static or templated open-state examples for modal/drawer/toast/tooltip/popover/dropdown/accordion/tabs/segmented-control, with notes for keyboard and focus expectations from D-182-18. Data Display stories must cover refs, key/value, table, data panel, code block, detail header, and toolbar with long IDs, null fields, mixed severity, disabled/error/empty, pagination boundary, and timezone boundary examples per D-182-16. Keep destructive examples inert and component-focused per D-182-08 and D-182-12.</action>
  <verify>
    <automated>cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/storybook_stories_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test-command assertion: Storybook story tests pass for Overlays and Data Display categories.
    - Source assertion: overlay examples include accessibility/focus notes and do not trigger live destructive operations.
    - Source assertion: data-display examples use representative ugly data and Threadline wrapper helpers.
    - Source assertion: no new motion token, decorative animation, keyframe, or `transition: all` is introduced.
  </acceptance_criteria>
  <done>Overlay and data-display stories complete their category coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recurring group and pattern stories</name>
  <files>examples/threadline_phoenix/storybook/groups/operator_groups.story.exs, examples/threadline_phoenix/storybook/patterns/operator_patterns.story.exs</files>
  <read_first>
    @examples/threadline_phoenix/storybook/groups/operator_groups.story.exs (create; read if present)
    @examples/threadline_phoenix/storybook/patterns/operator_patterns.story.exs (create; read if present)
    @examples/threadline_phoenix/lib/threadline_phoenix_web/storybook/wrapper.ex
    @examples/threadline_phoenix/lib/threadline_phoenix_web/storybook/fixtures.ex
    @examples/threadline_phoenix/test/threadline_phoenix_web/storybook_stories_test.exs
    @lib/threadline/operator_surface/stress_fixtures.ex
    @lib/threadline/operator_surface/ui.ex
    @DESIGN-SYSTEM.md
  </read_first>
  <behavior>
    - Test expectation: Groups story documents recurring assemblies with explicit story IDs or labels traceable to existing component-group vocabulary.
    - Test expectation: Patterns branch stays small and contains toolbar+filters, detail header+metadata, data panel+state/pager, destructive modal, offline/reconnect, and permission-denied assemblies.
    - Test expectation: the ledger remains a ratchet/projection source, not generated Storybook navigation.
  </behavior>
  <action>Create Groups and Patterns stories for the recurring assemblies named in D-182-11 and the UI-SPEC: toolbar plus filters, detail header plus metadata, data panel plus loading/empty/pager, inert destructive modal, offline/reconnect group, and permission-denied group. Sample existing group/stress data through the Plan 03 helper allowlist per D-182-07, but do not generate the Storybook tree from `.planning/design-system-ledger.json` or the full stress registry per D-182-06 and D-182-12. Use story notes to record fixture provenance, maturity, theme support, and a11y notes per D-182-13.</action>
  <verify>
    <automated>cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/storybook_stories_test.exs</automated>
    <automated>mix test test/threadline/operator_surface/stress_fixtures_test.exs test/threadline/operator_surface/stress_ledger_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test-command assertion: Storybook story tests pass for Groups and Patterns coverage.
    - Test-command assertion: stress fixture and ledger tests still pass, proving the canonical stress harness data remains intact.
    - Source assertion: Patterns branch is small and limited to recurring assemblies named in D-182-11.
    - Source assertion: no full page-flow matrix, auth behavior, navigation flow, or stress footgun moves into Storybook.
  </acceptance_criteria>
  <done>Groups and Patterns stories document recurring assemblies without replacing stress-route responsibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Turn bounded browser smoke green</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts, examples/threadline_phoenix/e2e/playwright.config.ts</files>
  <read_first>
    @examples/threadline_phoenix/e2e/tests/operator-storybook.spec.ts
    @examples/threadline_phoenix/e2e/playwright.config.ts
    @examples/threadline_phoenix/e2e/tests/operator-stress.spec.ts
    @examples/threadline_phoenix/e2e/run-e2e.sh
    @examples/threadline_phoenix/storybook/index.exs
    @examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
    @examples/threadline_phoenix/lib/threadline_phoenix_web/storybook/wrapper.ex
    @.planning/phases/182-phoenixstorybook-example-dev-lane/182-VALIDATION.md
  </read_first>
  <behavior>
    - Test expectation: Playwright renders the Storybook index and representative category stories with visible preview content.
    - Test expectation: every representative preview has `.threadline-ui` and `data-tl-theme`.
    - Test expectation: Storybook smoke remains bounded and avoids snapshot baselines or external visual-regression services.
  </behavior>
  <action>Update `operator-storybook.spec.ts` to the final URLs generated by the story tree and ensure each smoke assertion targets stable rendered preview selectors from the wrapper. If the existing light-lane Playwright project uses `testMatch`, add `operator-storybook.spec.ts` to that bounded match list so light/system wrapper behavior can be sampled without broadening all browser suites. Keep browser coverage to the index plus representative primitive/form/state/overlay/data-display/group stories per D-182-20, and keep `/audit/__stress` browser semantics in `operator-stress.spec.ts` per STORY-03.</action>
  <verify>
    <automated>./examples/threadline_phoenix/e2e/run-e2e.sh tests/operator-storybook.spec.ts</automated>
    <automated>mix verify.operator_stress</automated>
  </verify>
  <acceptance_criteria>
    - Test-command assertion: `operator-storybook.spec.ts` passes through the existing example E2E runner.
    - Test-command assertion: `mix verify.operator_stress` still passes, proving the stress harness remains canonical and unaffected.
    - Source assertion: browser smoke has no screenshot baseline calls and no external visual-regression service dependency.
    - Source assertion: light-lane project inclusion is bounded to the Storybook spec if config changes are needed.
  </acceptance_criteria>
  <done>Bounded browser smoke proves representative Storybook rendering without expanding the stress screenshot matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storybook preview -> browser | Browser smoke exercises rendered component docs with real assets/styles. |
| Stress harness -> Storybook | Flow stress remains in `/audit/__stress`; Storybook consumes only component examples. |
| Playwright config -> CI scope | Config changes can accidentally broaden expensive or flaky browser coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-10 | Information Disclosure | Storybook browser previews | mitigate | Keep route dev/test-only from Plan 02 and smoke only component examples. |
| T-182-11 | Denial of Service | Browser suite expansion | mitigate | Use bounded Storybook smoke; no screenshot matrix or external visual-regression service. |
| T-182-12 | Tampering | Stress fixture responsibility | mitigate | Keep stress fixture/ledger tests green and do not generate stories from the ledger. |
| T-182-SC | Tampering | Package install legitimacy | mitigate | This plan installs no packages and preserves the package boundary established in Plan 02. |
</threat_model>

<verification>
Run Storybook story tests, stress fixture/ledger tests, bounded Storybook Playwright smoke, and `mix verify.operator_stress`.
</verification>

<success_criteria>
- STORY-02 category coverage is complete for required component families.
- STORY-03 remains true because stress route browser semantics continue to pass separately.
- Browser coverage is representative and bounded.
</success_criteria>

<output>
Create `.planning/phases/182-phoenixstorybook-example-dev-lane/182-04-SUMMARY.md` when done.
</output>
