---
phase: 176-data-display-operator-patterns
plan: 04
type: execute
wave: 4
depends_on: ["176-03"]
files_modified:
  - lib/threadline/operator_surface/live/coverage_live.ex
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/style_contract_test.exs
  - test/threadline/operator_surface/card_nesting_regression_test.exs
  - test/threadline/operator_surface/live/coverage_live_test.exs
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "Coverage uses UI.page_header in ALL three branches — the hand-rolled header inside the synthetic shell is gone"
    - "No card-family class is nested under another card-family class on any of the 11 rendered pages"
    - "The dead tl-coverage-command__* CSS is deleted and the deletion is locked by a contract assertion"
  artifacts:
    - path: "lib/threadline/operator_surface/live/coverage_live.ex"
      provides: "page_header in all 3 branches; tl-coverage-command shell demoted to page-stack siblings"
      contains: "UI.page_header"
    - path: "test/threadline/operator_surface/card_nesting_regression_test.exs"
      provides: "GREEN: refutes card-under-card across 11 pages"
      contains: "tl-card"
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "tl-coverage-command__* CSS deleted"
      contains: "tl-summary-grid"
  key_links:
    - from: "coverage success branch"
      to: "UI.page_header (title/:lede/:actions/:meta)"
      via: "replace hand-rolled tl-page__title/__lede/__meta"
      pattern: "UI.page_header"
    - from: "style_contract_test"
      to: "deleted tl-coverage-command__* selectors"
      via: "refute presence (locks the deletion)"
      pattern: "tl-coverage-command"
---

<objective>
Flatten the coverage "schema" section card-in-card nesting (the named `coverage-schema-card-declutter` todo) and remove accidental nesting / table-overuse system-wide, enforced by a card-nesting regression test across all 11 pages. This is the DATA-05 deliverable. It turns the Plan-01 card-nesting RED test GREEN and pairs every CSS deletion with a contract-test update.

Purpose: the real coverage defect is not literal `card > card` — it is the success branch hand-rolling a page header (`tl-page__title/__lede/__meta`) inside a synthetic `tl-coverage-command` shell while the error/empty branches already use `UI.page_header`. The fix is "one card boundary per logical unit" (brand-book.md:348): page sections (header, summary band, command bar) become plain `<section>`/`<header>` spaced by `--tl-gap-stack`, and only genuinely repeated items keep a card boundary. A regression test then refutes card-family-under-card-family across every page so the declutter cannot silently regress.
Output: `coverage_live.ex` uses `UI.page_header` in all three branches with the `tl-coverage-command` shell demoted to page-stack siblings; the dead `tl-coverage-command__*` CSS deleted with a paired `style_contract_test.exs` assertion; the card-nesting regression test (from Plan 01) GREEN after a system-wide sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/176-data-display-operator-patterns/176-CONTEXT.md
@.planning/phases/176-data-display-operator-patterns/176-PATTERNS.md
@.planning/phases/176-data-display-operator-patterns/176-UI-SPEC.md
@.planning/phases/176-data-display-operator-patterns/176-03-SUMMARY.md

<interfaces>
<!-- Verified coverage_live.ex anchors: -->
- L112-122 — the form-error branch already using `UI.page_header` (the canonical pattern to replicate, with :lede + :actions Refresh).
- L132-142 — the empty branch already using `UI.page_header`.
- L150-171 — the SUCCESS branch: hand-rolled `<section class="tl-coverage-command">` with `tl-coverage-command__header/__heading/__actions` wrapping `<h1 class="tl-page__title">`/`tl-page__lede`/`tl-page__meta` — DELETE this, use page_header.
- L187-198 — `tl-card--metric` tiles (LEGIT repeated items — KEEP).
- L212-224 — the coverage `<table>` with `data-label` cells (may adopt `UI.data_table` from Plan 02, optional; keep as table per D-10).
- L234 — copy command already present.
- UI.page_header/1 (ui.ex:202) — title attr + `:lede`/`:actions`/`:meta` slots (D-12 adds `:meta` for checked_label).
- style.ex: `tl-coverage-command__*` CSS ~L3336 (delete after flatten).
- card-nesting regression test from Plan 01 (currently RED).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten coverage — page_header in all 3 branches, demote the command shell, delete dead CSS</name>
  <read_first>
    - lib/threadline/operator_surface/live/coverage_live.ex (the file being modified — L110-200; compare the page_header branches L112-122 / L132-142 against the hand-rolled success branch L150-171)
    - .planning/phases/176-data-display-operator-patterns/176-PATTERNS.md §"coverage_live.ex" (the exact hand-rolled header to delete + the canonical page_header to use; line anchors)
    - .planning/phases/176-data-display-operator-patterns/176-UI-SPEC.md §"Flatten Rule" (one card boundary per logical unit; coverage concrete fix) + §"Spacing Scale" (`--tl-gap-stack` = `--tl-space-6`)
    - lib/threadline/operator_surface/style.ex (`tl-coverage-command__*` block ~L3336)
    - test/threadline/operator_surface/style_contract_test.exs + test/threadline/operator_surface/live/coverage_live_test.exs
  </read_first>
  <behavior>
    - All three coverage branches (form-error, empty, success) render the header via `UI.page_header` (title "Coverage — schema: <schema>", `:lede`, `:actions` Refresh, `:meta` last-checked via `Presentation.checked_label/1`). No `tl-page__title`/`tl-coverage-command__*` markup remains in the success branch.
    - The former `tl-coverage-command` shell children (trust-rail, metric `tl-summary-grid`, remediation, table) become direct page-stack siblings spaced by `--tl-gap-stack`. `tl-card--metric` tiles are kept (legit repeated items).
    - The `tl-coverage-command__*` CSS is deleted from style.ex; `style_contract_test.exs` is updated in the same task to refute those selectors (locks the deletion).
    - coverage still renders correctly in all three branches (no broken layout, no orphaned class).
  </behavior>
  <action>
    In `coverage_live.ex` delete the hand-rolled `<section class="tl-coverage-command">…<h1 class="tl-page__title">…` header (L150-171) and render `<UI.page_header title={"Coverage — schema: #{@schema_param}"}>` with `:lede`, `:actions` (Refresh button + `:refresh` icon), and a `:meta` slot carrying `Presentation.checked_label(@coverage_for_schema.last_checked_at)` — matching the L112-122 branch (D-12). Demote the `tl-coverage-command` shell so its children become direct page-stack siblings (plain `<section>`) spaced by `--tl-gap-stack`; KEEP the `tl-card--metric` tiles (L187-198). Delete the `tl-coverage-command__*` CSS block in `style.ex` (~L3336) and add a paired `refute String.contains?(css, "tl-coverage-command")` assertion to `style_contract_test.exs` IN THE SAME TASK (Pitfall 2). Extend `coverage_live_test.exs` to assert page_header renders in all three branches and no `tl-coverage-command` class remains. Add NO new `--tl-*` token.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/coverage_live_test.exs test/threadline/operator_surface/style_contract_test.exs test/threadline/brandbook_token_parity_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The coverage live test + style-contract + parity tests pass.
    - Source assertion: `grep -c 'tl-coverage-command' lib/threadline/operator_surface/live/coverage_live.ex lib/threadline/operator_surface/style.ex` returns 0 (shell + CSS fully removed); `UI.page_header` appears in all three branches.
    - The style-contract test now refutes `tl-coverage-command` selectors (deletion locked).
    - Behavior assertion: each of the three coverage branches renders exactly one `<h1>` via page_header; `tl-card--metric` tiles still present.
    - No new `--tl-*` token; parity green; `mix verify.format` clean.
  </acceptance_criteria>
  <done>Coverage uses page_header in all 3 branches; command shell demoted; tl-coverage-command__* CSS deleted with paired contract assertion; metric tiles kept; tests green; format clean.</done>
</task>

<task type="auto">
  <name>Task 2: System-wide accidental-nesting sweep + make the card-nesting regression test GREEN</name>
  <read_first>
    - test/threadline/operator_surface/card_nesting_regression_test.exs (the Plan-01 RED test — the binding contract; renders all 11 pages, refutes card-family-under-card-family)
    - .planning/phases/176-data-display-operator-patterns/176-CONTEXT.md D-11 (one card boundary per logical unit) + D-12 (the regression test)
    - .planning/phases/176-data-display-operator-patterns/176-UI-SPEC.md §"Flatten Rule" (per-surface verdict: which surfaces keep cards vs sections)
    - lib/threadline/operator_surface/live/*.ex (grep each page for `tl-card` nested under `tl-card`/`tl-record-card`/`tl-job` — fix any accidental nesting found beyond coverage)
  </read_first>
  <behavior>
    - The card-nesting regression test (renders all 11 pages, refutes any `tl-card*` class nested inside another `tl-card*` element) passes GREEN — no page nests a card-family boundary inside another.
    - Page sections that were wrongly wrapped in a card become plain `<section>`/`<header>` with spacing; genuinely repeated items (timeline change cards, evidence `tl-record-card`, export `tl-job`, coverage metric tiles) keep their single card boundary per the per-surface verdict (D-10).
  </behavior>
  <action>
    Run the card-nesting regression test; for every page it flags (beyond coverage, already fixed in Task 1), demote the offending page-section card to a plain `<section>`/`<header>` spaced by `--tl-gap-stack`, applying the "one card boundary per logical unit" rule (D-11) and respecting the per-surface verdict (D-10 — do NOT strip the legitimate repeated-item cards: timeline change cards, evidence record cards, export job cards, coverage metric tiles). Iterate until the regression test is fully GREEN across all 11 pages. Add NO new `--tl-*` token; reuse existing section/spacing classes.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/card_nesting_regression_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/card_nesting_regression_test.exs` passes GREEN for all 11 pages.
    - Behavior assertion: no rendered page contains a `tl-card*` element nested inside another `tl-card*` element.
    - Legit repeated-item cards preserved: the test still confirms timeline change cards / evidence record cards / export job cards / coverage metric tiles render (they are NOT page sections).
    - `mix verify.test` green (no collateral regression from the sweep); `mix verify.format` clean.
  </acceptance_criteria>
  <done>Card-nesting regression test GREEN across 11 pages; accidental page-section cards demoted to sections; legit repeated-item cards preserved; full suite + format clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS source string ↔ contract test | style-contract test asserts literal source strings; a CSS deletion must be paired with the assertion update or the deletion silently regresses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-08 | Tampering (silent regression) | `tl-coverage-command__*` CSS deletion | mitigate | Deletion paired in the same task with a `refute` assertion in style_contract_test (Task 1); re-introduction fails CI. |
| T-176-09 | (defense-in-depth) | accidental card nesting across pages | mitigate | Card-nesting regression test renders all 11 pages and refutes card-under-card (Task 2); locks the declutter against future regression. |
| T-176-SC | Tampering | npm/pip/cargo installs | accept | No installs (zero new runtime deps invariant); not applicable per RESEARCH.md. |
</threat_model>

<verification>
- `mix test .../coverage_live_test.exs .../style_contract_test.exs .../brandbook_token_parity_test.exs` green (Task 1).
- `mix test .../card_nesting_regression_test.exs` GREEN across 11 pages (Task 2).
- `mix verify.test` green at wave merge.
- Capture & semantics untouched (only coverage_live.ex, style.ex coverage region, and tests edited).
</verification>

<success_criteria>
- Coverage uses `UI.page_header` in all three branches; the synthetic `tl-coverage-command` shell + its dead CSS are gone; metric tiles kept.
- Every CSS deletion is paired with a contract-test assertion update.
- No card-family class is nested under another card-family class on any of the 11 pages (regression test GREEN).
- No new `--tl-*` token; parity green; capture/semantics untouched.
</success_criteria>

<output>
Create `.planning/phases/176-data-display-operator-patterns/176-04-SUMMARY.md` when done.
</output>
