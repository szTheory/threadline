---
phase: 176-data-display-operator-patterns
plan: 02
type: execute
wave: 2
depends_on: ["176-01"]
files_modified:
  - lib/threadline/operator_surface/ui.ex
  - test/threadline/operator_surface/ui_test.exs
  - lib/threadline/operator_surface/stress_fixtures.ex
  - lib/threadline/operator_surface/stress_live.ex
  - test/threadline/operator_surface/stress_ledger_test.exs
  - test/threadline/operator_surface/stress_router_test.exs
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "ref/1 always binds data-tl-copy to ref.full (never .title, never .visible) on both the <code> and the copy button"
    - "data_table/1 :col label feeds BOTH the <th> and every <td data-label> from one source"
    - "loading/stale/no_data/permission/unavailable states are visually distinct, each with its own role + icon shape + heading"
    - "every new/extended unit renders in isolation on /audit/__stress across the matrix"
  artifacts:
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "ref/1, kv/1, data_table/1, loading_state/1, stale_banner/1, empty_state variant extension"
      contains: "def ref(assigns)"
    - path: "test/threadline/operator_surface/ui_test.exs"
      provides: "contract tests locking attrs/slots/states/a11y per new component"
      contains: "describe \"ref/1\""
    - path: "lib/threadline/operator_surface/stress_fixtures.ex"
      provides: "stories for ref/kv/data_table + each data-state"
      contains: "assigns_for"
  key_links:
    - from: "UI.ref/1"
      to: "Presentation.ref/2"
      via: "core helper returns the three faces; component binds full"
      pattern: "data-tl-copy=.*full"
    - from: "UI.data_table/1 :col label"
      to: "<th> and <td data-label>"
      via: "single label source"
      pattern: "data-label"
    - from: "stress_live.ex stories"
      to: "stress_ledger_test.exs expectations"
      via: "ledger coverage enforcement"
      pattern: "ref|kv|data_table|loading|stale|permission|unavailable"
---

<objective>
Build the internal `Threadline.OperatorSurface.UI` data-display + data-state component set that converges the five drifting render paths and locks the DATA-03 taxonomy, with per-component contract tests and stress-route registration. This is the component layer: pure render functions that downstream consumer plans (03, 04, 05) adopt. No page is migrated here.

Purpose: a single `ref/1` is the call-site API that retires the 3 ad-hoc copy wirings; `kv/1` + `data_table/1` make the correct structure the path of least resistance; the named data-state family (`loading_state`, `stale_banner`, three `empty_state` variants) makes the three load-bearing forensic distinctions (permission ≠ no-data ≠ unavailable) impossible to collapse. Building and stress-registering them in isolation (the 173/174/175 pattern) means each is audited before any page depends on it.
Output: `ref/1`, `kv/1`, `data_table/1`, `loading_state/1`, `stale_banner/1` added to `ui.ex`; `empty_state/1` variant enum extended; `ui_test.exs` contract describes; stress stories + ledger/router test updates turning the Plan-01 data-state RED test partially GREEN at the component level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/176-data-display-operator-patterns/176-CONTEXT.md
@.planning/phases/176-data-display-operator-patterns/176-PATTERNS.md
@.planning/phases/176-data-display-operator-patterns/176-UI-SPEC.md
@.planning/phases/176-data-display-operator-patterns/176-01-SUMMARY.md

<interfaces>
<!-- Verified anchors in lib/threadline/operator_surface/ui.ex (1005 lines). -->
- `stat_tile/1` (L341) — simplest attr-only component to mirror; `attr(:label, :string, required: true)` at L337; `data-status` stripe at L343.
- `empty_state/1` (L358) — block shape; `attr(:variant, :string, default: nil, values: [nil, "error", "never", "unsupported"])` at L352 (EXTEND this list).
- `error_state/1` (L377) — thin `<.empty_state variant="error">` wrapper; DO NOT change its delegation.
- `spinner/1` (L131), `alert/1` (L109, `tl-alert tl-alert--warning` shell + `role`), `card/1` (L166, slotted), `tabs/1` (L653, slot-with-required-attr idiom: `slot :tab, required: true do attr(:active, :boolean) end`).
- From Plan 01: `Presentation.ref/2 → %{visible, title, full}`; icons `:eye_off`, `:funnel`, `:lock`, source-down glyph, `:archive`, `:shield`, `:history`, `:refresh`.
- Copy mechanism: `Threadline.OperatorSurface.Script.enabled?/0` gates copy buttons; delegated `[data-tl-copy]` listener (script.ex) survives patches.
- row_history_component.ex:173-184 — the canonical `<dl class="tl-kv">` body to lift for `kv/1`.
- stress_fixtures.ex:95-108 `@state_stories` tuple shape; `assigns_for/1` L148-206 dispatch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ref/1, kv/1, data_table/1 to UI + contract tests</name>
  <read_first>
    - lib/threadline/operator_surface/ui.ex (the file being modified — stat_tile/1 L329-348 attr style, card/1 L152-179 slots, tabs/1 L653-655 slot-attr idiom, empty_state/1 L358)
    - .planning/phases/176-data-display-operator-patterns/176-PATTERNS.md §"UI.ref/1", §"UI.kv/1", §"UI.data_table/1" (copy-ready markup + the D-02 footgun to avoid; anchors transaction_live.ex:119-124, row_history_component.ex:173-184, retention_history_live.ex:177-189, coverage_live.ex:221-224)
    - .planning/phases/176-data-display-operator-patterns/176-UI-SPEC.md §"Component Inventory" + §"Truncation Rules" (exact attr contracts, copy contract)
    - lib/threadline/operator_surface/live/row_history_component.ex (the tl-kv <dl> source, L170-208)
    - test/threadline/operator_surface/ui_test.exs (existing describe-block style to extend)
  </read_first>
  <behavior>
    - ref/1: renders `<code class="tl-secondary-ref" title={ref.full} data-tl-copy={ref.full}>{ref.visible}</code>` + a `:if={Script.enabled?()}` gated copy button that ALSO carries `data-tl-copy={ref.full}`. For a long (truncated) value, the rendered `data-tl-copy` == full and != visible (the Plan-01 ref-copy-equals-full assertion goes GREEN against this component). When `Script.enabled?()` is false, the `<code>` renders ref.full (not ref.visible) for zero-JS select-all (D-06). `copy_label` is required with no default — omitting it is a compile-time error (D-07); visible button text is "Copy", specificity in aria-label.
    - kv/1: renders `<dl class="tl-kv">` with an `:item` slot carrying a REQUIRED `key` attr; `<dt>` shows key, `<dd>` shows `render_slot(item)` so callers drop a `ref/1`/value span inside.
    - data_table/1: `:col` slot with required `label` → emits `<th>{label}` AND every `<td data-label={label}>` from the SAME source; supports `rows` OR `stream` (truthy → `phx-update="stream"` on `<tbody>`), `row_id` → `<tr id=...>`, `row_status` → `data-status` stripe (zero new CSS), pass-through `class`, always emits `tl-table tl-table--responsive`; `:action` slot hosts the kebab. NO ARIA `role="table"/"row"/"cell"` (D-09).
  </behavior>
  <action>
    Add `ref/1`, `kv/1`, `data_table/1` to `ui.ex` as `@doc false` function components mirroring `stat_tile/1` (attr-only) and `card/1` (slotted) idioms. `ref/1` attrs: `value` (required), `kind` (default nil), `copy_label` (required, NO default — D-07), `class`, `rest`; bind `Presentation.ref(@value, kind: @kind)` to the three faces and put `data-tl-copy={r.full}` on BOTH the `<code>` and the gated copy button (NEVER `.title`/`.visible` — D-02). `kv/1`: `slot :item, required: true do attr(:key, :string, required: true) end` (mirror tabs/1 L653-655). `data_table/1`: `attr :rows`/`attr :stream`, `attr :row_id`, `attr :row_status`, `attr :class`, `slot :col, required: true do attr(:label, :string, required: true) end`, `slot :action`; single label source feeds th + td-data-label; `phx-update="stream"` only when stream truthy; `data-status` stripe per stat_tile (L343); emit `tl-table tl-table--responsive` always; add NO ARIA table roles. In `ui_test.exs` add `describe "ref/1"`, `describe "kv/1"`, `describe "data_table/1"` asserting: ref data-tl-copy == full != visible for a long value; copy_label required (raises without it); kv `:item key` renders dt/dd; data_table `:col label` produces matching `<th>` and `<td data-label>`, stream toggles `phx-update`, no `role="table"`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/ui_test.exs` passes including the three new describes.
    - Source assertion: `ui.ex` contains `def ref(assigns)`, `def kv(assigns)`, `def data_table(assigns)`; `grep -n 'data-tl-copy' ui.ex` shows the ref binding uses `.full` (never `.title`/`.visible`).
    - Behavior assertion: a rendered `ref` of a >40-char value has `data-tl-copy` equal to the full value and `!=` the truncated visible text.
    - Behavior assertion: a rendered `data_table` contains NO `role="table"`/`role="row"`/`role="cell"`; each `<td>`'s `data-label` matches its column `<th>`.
    - `mix verify.format` clean.
  </acceptance_criteria>
  <done>ref/1, kv/1, data_table/1 added with required-attr contracts; ref binds full on code+button; data_table label feeds th+td with no ARIA roles; ui_test green; format clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add loading_state/1 + stale_banner/1 + extend empty_state variants + contract tests</name>
  <read_first>
    - lib/threadline/operator_surface/ui.ex (the file being modified — empty_state/1 L358 + variant enum L352, error_state/1 L377, spinner/1 L131, alert/1 L109)
    - .planning/phases/176-data-display-operator-patterns/176-PATTERNS.md §"UI.loading_state/1", §"UI.stale_banner/1", §"UI.empty_state/1 variant extension" (analogs: empty_state shape, coverage stale strip coverage_live.ex:125-129, alert/1)
    - .planning/phases/176-data-display-operator-patterns/176-UI-SPEC.md §"Data-State Taxonomy" (the role/icon-shape/heading/body table — the locked spec for all 7 states) + §"Copywriting Contract"
    - test/threadline/operator_surface/ui_test.exs
  </read_first>
  <behavior>
    - loading_state/1: `role="status"` + `aria-busy="true"`, renders `<.spinner/>` + a literal text node (default "Loading audit changes…"); structurally distinct from empty_state (its own function, NOT a variant).
    - stale_banner/1: `role="status"` strip (the `tl-alert tl-alert--warning` shell), refresh/warning icon shape, an `as_of` timestamp attr, copy "Couldn't refresh — showing last known data from <timestamp>. Retry." It is NOT in the async switch (renders ABOVE data).
    - empty_state variant enum gains `no_data`, `permission`, `unavailable` (existing nil/error/never/unsupported kept). permission renders `role="alert"` + lock/shield icon + "You don't have access to this audit data"; no_data renders `role="status"` + funnel + "No changes match these filters"; unavailable renders the cause-split icon (plug/cloud-off down, eye-off redacted, archive pruned) each stating "This is not a permissions issue." error_state/1 delegation UNCHANGED.
    - Focus management (D-15): `error_state/1` and the `permission` variant render their heading with `tabindex="-1"` and move focus to it on mount (the D-15 "focus rescue"). We build to this AA behavior here; the formal a11y audit lands in Phase 180.
  </behavior>
  <action>
    Add `loading_state/1` (mirror empty_state block shape, `role="status"` + `aria-busy="true"`, reuse `spinner/1` verbatim, default text node from UI-SPEC). Add `stale_banner/1` (mirror `alert/1` `tl-alert tl-alert--warning` shell, `role="status"`, `as_of` attr, refresh/warning icon, copy from Copywriting Contract). Extend the `empty_state/1` variant `values:` list at L352 to add `"no_data"`, `"permission"`, `"unavailable"` — do NOT touch `error_state/1`'s delegation. Wire each new variant's role/icon/heading per the Data-State Taxonomy table (permission=alert+lock/shield; no_data=status+funnel; unavailable=cause-split icon, "not a permissions issue"). Render the `error_state` and `permission`-variant heading with `tabindex="-1"` and move focus to it on mount (D-15 focus rescue — JS.focus / phx-mounted, CSP-clean, no inline handler). Add `describe "loading_state/1"`, `describe "stale_banner/1"`, and `empty_state` variant assertions to `ui_test.exs` checking each state's role, distinct icon name, heading text, and the `tabindex="-1"` focusable heading on error_state + permission. Use only existing `--tl-*` tokens / existing `tl-empty--*` + `tl-alert--*` CSS (no style.ex change in this plan — compose existing classes).
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/ui_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/ui_test.exs` passes including loading_state, stale_banner, and the three new empty_state variants.
    - Source assertion: `ui.ex` contains `def loading_state(assigns)` and `def stale_banner(assigns)`; the variant enum line contains `"no_data"`, `"permission"`, `"unavailable"`.
    - Behavior assertion: loading_state output contains `role="status"` and `aria-busy="true"`; permission variant contains `role="alert"` + a lock/shield icon; the three unavailable sub-cases each contain the literal "not a permissions issue".
    - Behavior assertion: each state uses a DISTINCT icon name (history/spinner/alert/refresh/lock/funnel/plug-or-cloud-off/eye-off/archive) — no two content-replacing states share a glyph (D-16, no-color-alone via shape).
    - Behavior assertion (D-15 focus management): `error_state/1` and the `permission` variant render a heading carrying `tabindex="-1"` and move focus to it on mount (the "focus rescue"); `ui_test.exs` asserts the `tabindex="-1"` heading is present on both. Built to AA here; formal a11y audit is Phase 180.
    - `mix verify.format` clean.
  </acceptance_criteria>
  <done>loading_state + stale_banner added as named siblings; empty_state gains no_data/permission/unavailable; each state has distinct role+icon+heading per UI-SPEC; error_state + permission render a focusable `tabindex="-1"` heading with focus moved on mount (D-15); ui_test green; error_state delegation untouched; format clean.</done>
</task>

<task type="auto">
  <name>Task 3: Register every new unit + data-state as a /audit/__stress story; update ledger/router tests</name>
  <read_first>
    - lib/threadline/operator_surface/stress_fixtures.ex (the file being modified — @state_stories L95-108 tuple shape, assigns_for/1 L148-206 dispatch, existing ugly-data fixtures long_id/permission_denied/stale/non_ascii L8-18,415-420)
    - lib/threadline/operator_surface/stress_live.ex (story registration surface)
    - test/threadline/operator_surface/stress_ledger_test.exs + stress_router_test.exs (the expectation lists to extend)
    - .planning/phases/176-data-display-operator-patterns/176-PATTERNS.md §"Stress story registration" (the exact tuple + assigns_for clause shapes) and §"Research Open Q3"
  </read_first>
  <action>
    Add a `/audit/__stress` story (a `@state_stories` tuple + matching `assigns_for/1` clause in `stress_fixtures.ex`, surfaced via `stress_live.ex`) for each new component (`ref`, `kv`, `data_table`) and each data-state (`loading`, `stale`, `no_data`, `permission`, `unavailable-down`, `unavailable-redacted`, `unavailable-pruned`), reusing the existing ugly-data fixtures (`long_id`, `permission_denied`, `stale`, `non_ascii`). Update `stress_ledger_test.exs` and `stress_router_test.exs` expected-story lists IN THE SAME task so coverage enforcement stays green (do not let the ledger assert a story that is not registered, or vice versa). Match the existing `{id, fixture_key, scenario, cases}` shape verbatim.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_ledger_test.exs test/threadline/operator_surface/stress_router_test.exs test/threadline/operator_surface/stress_fixtures_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The three stress tests pass with the new stories registered.
    - Source assertion: `stress_fixtures.ex` contains `assigns_for/1` clauses (or story tuples) for `ref`, `kv`, `data_table` and the seven data-states; `stress_ledger_test.exs` expectations include the same identifiers (ledger ↔ registry parity).
    - `mix verify.format` clean.
  </acceptance_criteria>
  <done>Every new component + each data-state registered as a stress story; ledger/router/fixtures tests green with matching expectations; format clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored value → rendered copy target | ref/1 must bind ref.full to data-tl-copy on every render path (code + button) — the forensic integrity boundary |
| typed reason (server) → rendered state | the data-state component must preserve the distinct meaning; collapsing two states to one glyph/heading destroys the forensic distinction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-03 | Tampering (evidence) | `UI.ref/1` | mitigate | data-tl-copy bound to `r.full` on both `<code>` and button; zero-JS fallback renders `r.full`; ui_test asserts copy==full!=visible for a long value (Task 1). |
| T-176-04 | Information disclosure / Repudiation | data-state components | mitigate | permission/no_data/unavailable are structurally distinct (own variant, own role, own icon shape, own heading stating "not a permissions issue"); ui_test asserts distinct glyphs (Task 2). Closes the "collapse to something went wrong" failure mode (D-16). |
| T-176-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero new runtime deps invariant); not applicable per RESEARCH.md. |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/ui_test.exs` green (Tasks 1-2).
- `mix test test/threadline/operator_surface/stress_ledger_test.exs stress_router_test.exs stress_fixtures_test.exs` green (Task 3).
- `mix verify.test` + `mix test .../brandbook_token_parity_test.exs` green at wave merge (no new `--tl-*` token introduced; compose existing classes).
- `mix verify.format` clean.
- Capture & semantics untouched (only ui.ex + stress fixtures/tests edited).
</verification>

<success_criteria>
- `ref/1`, `kv/1`, `data_table/1`, `loading_state/1`, `stale_banner/1` exist in `ui.ex` with required-attr contracts; `empty_state` gains three variants; `error_state` unchanged.
- `ref/1` binds `data-tl-copy={full}` on code + button; `data_table` `:col label` feeds th + td-data-label with no ARIA table roles; each data-state has a distinct role + icon shape + heading.
- Every new unit + each data-state is a stress story; ledger/router/fixtures tests green.
- No new `--tl-*` token; brand-token parity stays green; capture/semantics untouched.
</success_criteria>

<output>
Create `.planning/phases/176-data-display-operator-patterns/176-02-SUMMARY.md` when done.
</output>
