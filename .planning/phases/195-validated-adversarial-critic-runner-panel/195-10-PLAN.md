---
phase: 195-validated-adversarial-critic-runner-panel
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - test/threadline/operator_surface/stress_ledger_test.exs
  - test/threadline/critic_trust/ledger_splice_test.exs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "mix test test/threadline/operator_surface/stress_ledger_test.exs test/threadline/critic_trust/ledger_splice_test.exs exits 0 with 0 failures (was 8 failures at HEAD)"
    - "verify.test inside mix ci.all shows only the ~11 pre-existing search_path baseline failures — zero StressLedgerTest and zero LedgerSpliceTest failures (restores plan 195-01 truth 3 / 195-04 'ci.all green')"
    - "Contracts are EXTENDED, never weakened: @top_level_keys remains an exact-match sorted assertion (now covering the ratified critic_trust_provenance plus 196-era critic_panel and mechanical_auto_apply keys); kind 'refute' gets an explicit sub-contract (current_score must be null per D-04 null-never-0, never exempt-and-forget); graded-twin stories removed from the ledger fixture-registry assertion gain a REPLACEMENT registry guard against .planning/golden/synthetic-set.json"
    - "REQUIREMENTS.md traceability shows CRITIC-01, CRITIC-05, RUNNER-01, RUNNER-02 as Complete/checked (verifier-confirmed satisfied)"
  artifacts:
    - "test/threadline/operator_surface/stress_ledger_test.exs (extended contracts: refute kind, provenance/196 keys, graded-story synthetic-set guard)"
    - "test/threadline/critic_trust/ledger_splice_test.exs (error atom reconciled with generalized LedgerSplice)"
    - ".planning/REQUIREMENTS.md (4 traceability rows flipped)"
  key_links:
    - "StressFixtures.graded_stories/0 ids -> .planning/golden/synthetic-set.json items[].cell_id prefixes ({story_id}__{theme}-{breakpoint}) — the graded ladder's registry is the synthetic oracle set, not the ledger"
    - "stress_ledger_test.exs contracts -> .planning/design-system-ledger.json at HEAD (14 refute-twin entries with current_score null; 13 top-level keys)"
    - "ledger_splice_test.exs absent-block expectation -> LedgerSplice.replace/2 generalized {:error, :object_key_not_found} (aef9e655)"
---

<objective>
Close the single failed truth from 195-VERIFICATION.md: **`mix ci.all` stays green.** Phase 195's own commits (099afbaa refute-twin ledger entries; aef9e655 D-12 pivot: `critic_trust_provenance` key, graded-twin fixtures, generalized LedgerSplice error atom) left two frozen deterministic guards un-reconciled with the ratified ledger shape — 7 StressLedgerTest failures + 1 LedgerSpliceTest failure in the `verify.test` lane, distinct from the ~11 pre-existing search_path baseline failures.

Purpose: restore the "CI stays honest" contract while PRESERVING guard integrity — the fix contracts the ratified D-12 shapes IN (explicit refute sub-contract, exact-match key list, replacement registry guard for graded stories), it does not delete or loosen assertions. Also flips 4 stale REQUIREMENTS.md traceability rows the verifier confirmed satisfied.

Output: two reconciled test files green, traceability current, `verify.test` back to baseline-only failures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-VERIFICATION.md
@test/threadline/operator_surface/stress_ledger_test.exs
@test/threadline/critic_trust/ledger_splice_test.exs
@lib/threadline/operator_surface/stress_fixtures.ex
@lib/threadline/critic_trust/ledger_splice.ex
@.planning/design-system-ledger.json
</context>

## Artifacts this phase produces

| Artifact / symbol | Change |
|---|---|
| `test/threadline/operator_surface/stress_ledger_test.exs` `@top_level_keys` | + `critic_panel`, `critic_trust_provenance`, `mechanical_auto_apply` (sorted; exact-match assertion retained) |
| same file, `@allowed_kinds` | + `refute` |
| same file, refute sub-contract | new explicit branch: refute entries must carry `current_score == nil`, `legacy_score == nil`, `ratchet_score == 0`, integer `target_score`; excluded from ratchet-monotonicity / rollup / evidence_ref / DESIGN-SYSTEM inventory-row assertions via `kind` guard clauses |
| same file, new test `graded-ladder stories are registered in the synthetic oracle set` | replacement registry guard: every `StressFixtures.graded_stories/0` id has ≥1 `synthetic-set.json` item whose `cell_id` starts with `"#{story.id}__"` |
| same file, fixture-registry test | iterates `StressFixtures.all()` minus graded stories (graded ladder registered in synthetic set, binary twins stay ledger-registered) |
| same file, evidence_ref comprehension | nil-safe: score-increase filter requires `is_integer(current_score)` (nil > 0 is true under Elixir term ordering — latent bug) |
| `test/threadline/critic_trust/ledger_splice_test.exs` absent-block test | expects `{:error, :object_key_not_found}` |
| `.planning/REQUIREMENTS.md` | CRITIC-01/CRITIC-05/RUNNER-01/RUNNER-02 checkboxes → `[x]`; traceability table rows → Complete |

<tasks>

<task type="auto">
  <name>Task 1: Extend stress_ledger_test contracts to the ratified D-12 / 196-era ledger shape</name>
  <files>test/threadline/operator_surface/stress_ledger_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/stress_ledger_test.exs (the frozen guard being extended — all 15 tests)
    - .planning/design-system-ledger.json (HEAD state: 13 top-level keys incl. critic_panel/critic_trust_provenance/mechanical_auto_apply; 14 kind:"refute" entries, all with current_score/legacy_score null, ratchet_score 0, target_score 90, full 14-cell scores map, no evidence_ref)
    - lib/threadline/operator_surface/stress_fixtures.ex (lines 182-300: @refute_twin_stories, @graded_ladder, @graded_twin_stories; public graded_stories/0 and graded_rungs/0 at ~line 939-951)
    - .planning/golden/synthetic-set.json (144 items; items[].cell_id = "{graded_story_id}__dark-1280")
    - test/threadline/operator_surface/critic_trust_test.exs lines 385-430 and 510-560 (existing GATE-04 inventory + D-12 provenance guards — do NOT duplicate them here)
  </read_first>
  <action>
    Reconcile the guard with the ratified ledger shape by EXTENDING contracts (per 195-VERIFICATION.md gap "missing" items 1-2). Six coordinated edits:

    1. `@top_level_keys`: add `critic_panel`, `critic_trust_provenance`, `mechanical_auto_apply` keeping the list alphabetically sorted (the assertion is exact-match `sorted_keys(ledger) == @top_level_keys` — keep it exact-match; the extension is the fix, not a subset relaxation).
    2. `@allowed_kinds`: add `refute` (sorted).
    3. New refute-entry sub-contract test (deliberate, not a blanket exemption): for every entry with `kind == "refute"`, assert `current_score == nil` and `legacy_score == nil` (D-04: an unscored/vetoed fixture is null, NEVER 0 — 0 would poison min() and read as "bad"), `ratchet_score == 0`, `is_integer(target_score)`, `status == "current"`, `owner_phase == 195`. Also assert no refute id appears in `ratchet.locked_ids`, `ratchet.minimum_scores` keys, or any `ratchet.signoffs` cell_key prefix — the null-score allowance can never leak into the ratchet.
    4. Guard the four score-shape tests with a kind filter so refute entries are covered by the sub-contract instead: "scores can only ratchet upward" (integer assertions), "current_score is the min of rated cells" (rollup), "a score increase carries a File.exists?-true evidence_ref" (score-increase comprehension), and "DESIGN-SYSTEM projection is fresh for every ledger row" (inventory rows — refute twins are capture substrate, not design-system inventory; DESIGN-SYSTEM.md deliberately carries no refute rows). Implement as `entry["kind"] != "refute"` filters in the comprehensions/loops.
    5. Fix the latent nil-ordering bug while there: the evidence_ref comprehension's score-increase filter must require `is_integer(entry["current_score"])` before comparing against `ratchet_score` (Elixir term ordering makes `nil > 0` true, which is how null refute scores masqueraded as score increases).
    6. Replacement registry guard for graded stories (per the verifier's "contract them out of the fixture-registry assertion" option, done as a positive contract): change the "fixture registry stories are present in the ledger" test to iterate `StressFixtures.all()` excluding the graded-ladder stories (identify via `StressFixtures.graded_stories/0` id membership — build a MapSet of graded ids), and ADD a new test asserting every `StressFixtures.graded_stories/0` id is registered in the synthetic oracle: at least one item in `.planning/golden/synthetic-set.json` `items` has a `cell_id` that starts with `"#{story.id}__"`. The 14 binary refute twins remain ledger-registered and keep the existing story_id/fixture_key round-trip assertions unchanged.

    Run `mix format` after editing. Do not touch `.planning/design-system-ledger.json` or `DESIGN-SYSTEM.md` — the ledger at HEAD is the ratified shape; only the guard moves.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_ledger_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/stress_ledger_test.exs` exits 0 (0 failures; test count >= 16 — the original 15 plus the new graded-story synthetic-set guard and refute sub-contract)
    - `grep -c 'critic_trust_provenance' test/threadline/operator_surface/stress_ledger_test.exs` >= 1; same for `critic_panel` and `mechanical_auto_apply`
    - `grep -n 'refute' test/threadline/operator_surface/stress_ledger_test.exs` shows `refute` present in `@allowed_kinds`
    - `grep -c 'synthetic-set.json' test/threadline/operator_surface/stress_ledger_test.exs` >= 1 (replacement registry guard exists)
    - `grep -c 'graded_stories' test/threadline/operator_surface/stress_ledger_test.exs` >= 1
    - The exact-match key assertion survives: `grep -c 'sorted_keys(ledger) == @top_level_keys' test/threadline/operator_surface/stress_ledger_test.exs` == 1
    - `git diff -- .planning/design-system-ledger.json DESIGN-SYSTEM.md` is empty (guard reconciled, artifacts untouched)
    - `mix format --check-formatted` exits 0
  </acceptance_criteria>
  <done>All 7 StressLedgerTest failures resolved by contract extension; ledger and DESIGN-SYSTEM.md unmodified; refute-kind null scores explicitly contracted (never exempted silently); graded stories carry a positive synthetic-set registry guard.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ledger_splice_test with the generalized LedgerSplice error atom</name>
  <files>test/threadline/critic_trust/ledger_splice_test.exs</files>
  <read_first>
    - test/threadline/critic_trust/ledger_splice_test.exs (5 tests; the absent-block test at line 76-78)
    - lib/threadline/critic_trust/ledger_splice.ex (replace/2 and replace_provenance/2 both route through splice_object/3, which returns {:error, :object_key_not_found} on :nomatch since aef9e655)
  </read_first>
  <action>
    Update the "returns an error when the block is absent" test to expect `{:error, :object_key_not_found}` instead of `{:error, :critic_trust_not_found}` — LedgerSplice was generalized in aef9e655 to splice any object key (it now also handles `critic_trust_provenance` via `replace_provenance/2`), so the error atom is key-agnostic by design. Keep the test name and the `~s({"a": 1})` input unchanged. Optionally strengthen (cheap, in the spirit of extend-not-weaken): add a sibling assertion that `LedgerSplice.replace_provenance(~s({"a": 1}), %{})` also returns `{:error, :object_key_not_found}` — only if `replace_provenance/2` accepts that arity/shape per the source read; skip if its contract differs.
  </action>
  <verify>
    <automated>mix test test/threadline/critic_trust/ledger_splice_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/critic_trust/ledger_splice_test.exs` exits 0 (5 tests, 0 failures)
    - `grep -c ':object_key_not_found' test/threadline/critic_trust/ledger_splice_test.exs` >= 1
    - `git diff --stat -- lib/threadline/critic_trust/ledger_splice.ex` is empty (the test moves to the generalized module, not the reverse)
  </acceptance_criteria>
  <done>LedgerSpliceTest green; expected atom matches the generalized splice implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Flip stale REQUIREMENTS.md traceability rows and prove the suite-green truth</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (checkbox lines 29, 33, 37, 38; traceability table lines 96, 100, 101, 102)
    - .planning/phases/195-validated-adversarial-critic-runner-panel/195-VERIFICATION.md ("Requirements Coverage" table — all 4 IDs SATISFIED with evidence)
  </read_first>
  <action>
    Verifier-confirmed satisfied but never flipped (195-VERIFICATION.md "Tracking drift" note): change the checkboxes for **CRITIC-01**, **CRITIC-05**, **RUNNER-01**, **RUNNER-02** from `- [ ]` to `- [x]`, and their traceability-table rows (`| CRITIC-01 | Phase 195 | Pending |` etc.) from `Pending` to `Complete`. Touch ONLY those 8 lines — CRITIC-02/03/04, RUNNER-03/04/05 are already Complete; GATE-*/PROOF-* rows belong to phases 196/197 and stay Pending. Use Edit (scoped), never a whole-file rewrite.

    Then run the phase-truth proof: the two reconciled test files together, and the full `mix test` lane to confirm the only remaining failures are the ~11 pre-existing search_path baseline group (FormlessPagesTest, Phase06NyquistCIContractTest, V123CharterDocContractTest, and the audit-table/search_path env failures) with ZERO failures in StressLedgerTest or LedgerSpliceTest. Note: the local test DB may need the documented non-destructive `search_path` fix; the baseline failures are out of scope for this plan — the acceptance bar is "no failures in the two reconciled files", not "full suite 0 failures locally".
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_ledger_test.exs test/threadline/critic_trust/ledger_splice_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/stress_ledger_test.exs test/threadline/critic_trust/ledger_splice_test.exs` exits 0 (0 failures)
    - `mix test 2>&1 | tail -5` failure list contains no occurrence of `StressLedgerTest` and no occurrence of `LedgerSpliceTest` (grep the full output for those module names among failures: `mix test 2>&1 | grep -E "StressLedgerTest|LedgerSpliceTest" | grep -cv "0 failures"` reports no failing entries)
    - `grep -c '\- \[x\] \*\*CRITIC-01\*\*' .planning/REQUIREMENTS.md` == 1; likewise for CRITIC-05, RUNNER-01, RUNNER-02
    - `grep -c '| CRITIC-01 | Phase 195 | Complete |' .planning/REQUIREMENTS.md` == 1; likewise for CRITIC-05, RUNNER-01, RUNNER-02
    - `grep -c '| GATE-01 | Phase 196 | Pending |' .planning/REQUIREMENTS.md` == 1 (no over-flip)
    - `mix verify.format` exits 0; `mix verify.credo` exits 0
  </acceptance_criteria>
  <done>Traceability current for all 10 Phase-195 requirement IDs; the failed truth "`mix ci.all` stays green" is restored to its pre-195 meaning — verify.test carries only the documented pre-existing baseline, and both deterministic guards run green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Test-only + docs reconciliation; no untrusted input path, no dependency installs, no runtime code change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-10-01 | Tampering | stress_ledger_test.exs guard weakening | medium | mitigate | Contracts are extended, never deleted: exact-match key assertion retained, refute kind gets a POSITIVE sub-contract (null-score + ratchet-exclusion assertions), graded stories get a replacement synthetic-set registry guard; acceptance criteria grep for the surviving exact-match assertion |
| T-195-10-02 | Repudiation | REQUIREMENTS.md traceability | low | accept | Flips are verifier-backed (195-VERIFICATION.md Requirements Coverage table cites evidence per ID); scoped Edit prevents accidental over-flip of 196/197 rows |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/stress_ledger_test.exs test/threadline/critic_trust/ledger_splice_test.exs` — 0 failures (was 8 at HEAD).
- Full `mix test` — no StressLedgerTest / LedgerSpliceTest failures; remaining failures are exactly the pre-existing search_path baseline group documented in 196's deferred-items.md and the memory note (local env; non-blocking).
- `git diff` for `.planning/design-system-ledger.json` and `DESIGN-SYSTEM.md` empty — the ratified ledger shape is the fixed point; only the guards and REQUIREMENTS.md moved.
- `mix verify.format` + `mix verify.credo` clean (ci.all's other lanes untouched).
</verification>

<success_criteria>
The single failed truth from 195-VERIFICATION.md ("`mix ci.all` stays green") is closed: the 8 phase-195-introduced failures in the verify.test lane are resolved by deliberate contract extension, guard integrity is preserved (exact-match keys, explicit refute sub-contract, positive graded-story registry), and REQUIREMENTS.md traceability matches the verifier's SATISFIED findings for all 10 Phase-195 IDs.
</success_criteria>

<output>
Create `.planning/phases/195-validated-adversarial-critic-runner-panel/195-10-SUMMARY.md` when done.
</output>
