---
phase: 195-validated-adversarial-critic-runner-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/e2e/critic/rubrics/hierarchy.md
  - examples/threadline_phoenix/e2e/critic/rubrics/density.md
  - examples/threadline_phoenix/e2e/critic/rubrics/rhythm.md
  - examples/threadline_phoenix/e2e/critic/rubrics/typography.md
  - examples/threadline_phoenix/e2e/critic/rubrics/color_contrast.md
  - examples/threadline_phoenix/e2e/critic/rubrics/brand_fidelity.md
autonomous: true
requirements: [CRITIC-04]

must_haves:
  truths:
    - "One committed markdown rubric exists per frozen lens (6 files), each with a version header, a Dimensions block, a Reference bar block, and an Anchors block."
    - "The 13 gestalt-only dimensions are distributed exactly per D-05 (hierarchy 3; density/rhythm/typography/color_contrast/brand_fidelity 2 each)."
    - "Every dimension is phrased as an adversarial pass/fail with a WRITTEN pass condition and requires citing a region/selector/mechanical line (CRITIC-05 wording)."
    - "Each rubric names one pass-pole and one fail-pole golden cell-id in its Anchors block (primitive/v1.38 pole vs footgun pole)."
  artifacts:
    - "examples/threadline_phoenix/e2e/critic/rubrics/<lens>.md for all 6 lenses"
  key_links:
    - "rubrics/<lens>.md version header sha8 -> prompt-cache key + critic_trust.golden_rubric_version (validated in Plan 04)"
    - "rubric Anchors pole cell-ids -> committed .planning/scorecards/*.json (must resolve)"
  prohibitions:
    - "NO dimension that re-measures px/ratios/counts the mechanical layer already covers (gestalt only, D-05)."
    - "NO scraped Linear/Vercel/Stripe pixel values or numbers — prose reference descriptors only (no-network + Goodhart)."
    - "NO mid-range golden cell or held_out_id referenced as a pole (only the two poles; disjointness guarded in Plan 04)."
    - "NO external SaaS visual-diff product names in committed copy."
---

<objective>
Author the six versioned, anchored, adversarial per-lens rubrics — the judgment contract every critic call is scored against. Each rubric decomposes its lens into the locked gestalt-only dimensions (D-05), phrases each as a pass/fail the critic tries to FAIL, anchors to the reference bar in prose, and names its two pole exemplars.

Purpose: CRITIC-04 — versioned anchored rubrics, one per lens, each dimension an adversarial pass/fail with a written pass condition and a reference-bar anchor.
Output: six `rubrics/<lens>.md` files with version headers, Dimensions, Reference bar, and Anchors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-CONTEXT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-PATTERNS.md
@brandbook/brand-book.md
@brandbook/pressure-test.md
@.planning/milestones/v1.31-PERSONAS-IA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: hierarchy, density, rhythm rubrics</name>
  <read_first>195-CONTEXT.md D-05 (dimension decomposition + example pass conditions), D-11 (adversarial framing / cite-before-score), D-06 (persona-is-a-weight; hierarchy is critic-only highest-stakes); 195-PATTERNS.md "rubrics/*.md" (version header format + section layout); brandbook/pressure-test.md (no-praise/cite-evidence doctrine); brandbook/brand-book.md (color-as-signal, cards-for-repeated-items); .planning/scorecards/ (to pick real pole cell-ids)</read_first>
  <files>examples/threadline_phoenix/e2e/critic/rubrics/hierarchy.md, examples/threadline_phoenix/e2e/critic/rubrics/density.md, examples/threadline_phoenix/e2e/critic/rubrics/rhythm.md</files>
  <action>Author three rubrics. Each file opens with the version header format from PATTERNS (lens + version 1.0.0 + a sha8 placeholder the Plan-04 rubric-hash guard recomputes from disk), then ## Dimensions, ## Reference bar, ## Anchors. hierarchy (3 dimensions, critic-only/highest-stakes): entry-point clarity, scan-path/reading-order, emphasis discipline — e.g. entry-point PASSES iff, shown cold, you can name the single element the eye lands on first AND it is what this persona's JTBD needs first; FAIL if two or more compete or the primary is not task-primary. density (2 dimensions): signal-to-chrome, task-primary prominence — FAIL if you can name a control/chrome block whose removal costs the primary task nothing (the anti-verbosity lever). rhythm (2 dimensions): grouping-by-proximity, vertical-cadence coherence. Every dimension states a written pass condition, is framed as a failure the critic hunts for, and requires a cited region/selector/mechanical line (CRITIC-05) or the finding is discarded. Reference bar: Linear (primary) as prose descriptor for hierarchy/density/rhythm ("Linear packs primary + secondary metadata per row with one accent doing one job — match that signal-to-chrome"); Grafana as the cautionary high-density anchor. Anchors: name one pass-pole (a polished primitive/v1.38 page cell-id from committed scorecards) and one fail-pole (a footgun.* cell-id). Use ONLY committed scorecard cell-ids that resolve. No scraped numbers; no external SaaS tool names.</action>
  <verify>
    <automated>test $(ls examples/threadline_phoenix/e2e/critic/rubrics/{hierarchy,density,rhythm}.md 2>/dev/null | wc -l) -eq 3 && grep -lq "Reference bar" examples/threadline_phoenix/e2e/critic/rubrics/hierarchy.md && grep -lq "Anchors" examples/threadline_phoenix/e2e/critic/rubrics/hierarchy.md</automated>
  </verify>
  <acceptance_criteria>Three files exist; hierarchy has 3 dimensions, density and rhythm have 2 each; every dimension has a written pass condition + cite requirement; Reference bar + Anchors present; pole cell-ids resolve to committed scorecards.</acceptance_criteria>
  <done>hierarchy/density/rhythm rubrics authored with version header, adversarial dimensions, reference bar, and resolving pole anchors.</done>
</task>

<task type="auto">
  <name>Task 2: typography, color_contrast, brand_fidelity rubrics</name>
  <read_first>Same as Task 1, plus 195-CONTEXT.md D-06 brand-veto ordering (brand_fidelity is post-veto) and brandbook/brand-book.md accent palette ("two blues two jobs": Thread Blue = interface action, Signal Cyan = correlation, Ember = diff-emphasis not warning; radius <=8px)</read_first>
  <files>examples/threadline_phoenix/e2e/critic/rubrics/typography.md, examples/threadline_phoenix/e2e/critic/rubrics/color_contrast.md, examples/threadline_phoenix/e2e/critic/rubrics/brand_fidelity.md</files>
  <action>Author three rubrics in the same structure/header format. typography (2 dimensions): role differentiation, scale-expresses-hierarchy — judge ROLE not size-count (size-count is mechanical). color_contrast (2 dimensions): color-as-signal, accent-job discipline — every hue maps to a documented job (Thread Blue = interface action, Signal Cyan = correlation, Ember = diff-emphasis not warning). brand_fidelity (2 dimensions, post-veto): designed-not-recolored, register/voice fit — FAIL if it reads as a mechanical recolor or drifts to generic/marketing chrome. Reference bar routing: Vercel/Stripe (secondary) for typographic restraint + accent discipline; Linear primary throughout. Each dimension: written pass condition, adversarial framing, cite requirement. Anchors: pass-pole + fail-pole committed cell-ids. Prose descriptors only; no scraped numbers; no external SaaS tool names.</action>
  <verify>
    <automated>test $(ls examples/threadline_phoenix/e2e/critic/rubrics/{typography,color_contrast,brand_fidelity}.md 2>/dev/null | wc -l) -eq 3 && grep -lq "Anchors" examples/threadline_phoenix/e2e/critic/rubrics/brand_fidelity.md</automated>
  </verify>
  <acceptance_criteria>Three files exist; each has exactly 2 dimensions; total across all six rubrics = 13 dimensions; brand_fidelity documented as post-veto; pole anchors resolve.</acceptance_criteria>
  <done>typography/color_contrast/brand_fidelity rubrics authored; the 6-file / 13-dimension set is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rubric bytes -> prompt-cache key + trust version stamp | rubric content is the judgment contract; drift silently invalidates trust |
| screenshot content -> critic score | rendered UI text could attempt prompt-injection to inflate scores |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-04 | Tampering | rubric content vs stamped version | high | mitigate | Version header sha8 is recomputed from disk and asserted == stamped by Plan 04's rubric-hash guard; any edit auto-invalidates that lens's trust |
| T-195-05 | Tampering | prompt-injection via rendered UI in screenshots | medium | mitigate | Rubrics frame the system role as adversarial ("default verdict FAIL", judge gestalt only, cite mechanical evidence); the frozen system stratum (Plan 05) instructs the model to treat screenshot text as content-under-review, never instructions |
| T-195-06 | Information Disclosure | committed rubric copy | low | accept | Rubrics contain only prose reference descriptors and public brand doctrine; no secrets, no scraped third-party data |
</threat_model>

<verification>
- All 6 rubric files present under `e2e/critic/rubrics/`.
- Dimension count is 13 total (3+2+2+2+2+2).
- Every rubric has version header, Dimensions, Reference bar, Anchors; pole cell-ids resolve to committed scorecards.
- No scraped numbers, no external SaaS tool names.
</verification>

<success_criteria>
Six versioned, anchored, adversarial per-lens rubrics exist, decomposed exactly per D-05, each dimension a written adversarial pass/fail with a reference-bar anchor and cite requirement (CRITIC-04).
</success_criteria>

## Artifacts this plan produces
- `examples/threadline_phoenix/e2e/critic/rubrics/hierarchy.md` (3 dimensions)
- `examples/threadline_phoenix/e2e/critic/rubrics/density.md` (2)
- `examples/threadline_phoenix/e2e/critic/rubrics/rhythm.md` (2)
- `examples/threadline_phoenix/e2e/critic/rubrics/typography.md` (2)
- `examples/threadline_phoenix/e2e/critic/rubrics/color_contrast.md` (2)
- `examples/threadline_phoenix/e2e/critic/rubrics/brand_fidelity.md` (2, post-veto)

<output>
Create `.planning/phases/195-validated-adversarial-critic-runner-panel/195-02-SUMMARY.md` when done.
</output>
