---
phase: 195-validated-adversarial-critic-runner-panel
plan: 06
type: execute
wave: 3
depends_on: [195-05, 195-03]
files_modified:
  - examples/threadline_phoenix/e2e/critic/panel.ts
  - examples/threadline_phoenix/e2e/critic/refute.ts
autonomous: true
requirements: [RUNNER-03, CRITIC-02]

must_haves:
  truths:
    - "The panel runs one critic per persona (P1-P5) writing p{n}.hierarchy + p{n}.density, a graphic-design critic writing all.rhythm/typography/color_contrast, and a brand-veto critic writing all.brand_fidelity = 14 lens-cells/page."
    - "Veto pipeline order per cell: (1) verify.mechanical, (2) --tl-* token-parity / dark-light parity brand veto, (3) on veto write all.brand_fidelity={current:null,vetoed:true,evidence} and SKIP ALL aesthetic vision calls, (4) no veto -> vision critics run."
    - "Precedence vetoed(null) > unstable(null) > scored; veto and unstable both write null (distinguished by the vetoed flag) so neither poisons min()."
    - "Each critic is a blind pure cell-writer — no critic sees another's output; persona disagreement (P1-high/P3-low) is PRESERVED, never averaged; the min()-losing (persona,lens) cell is named in rollup evidence."
    - "The refute battery scores every twin and enforces two-tier pass bars: binary directional (correct rank/sign AND correct lens with located evidence) + margin gate (delta exceeds measured noise floor) + metamorphic invariance (verdict void if it flips under A/B swap or paraphrase)."
  artifacts:
    - "e2e/critic/panel.ts (critic->lens map, veto ordering, blind fan-out)"
    - "e2e/critic/refute.ts (directional + margin + metamorphic gates)"
  key_links:
    - "panel.ts token-parity veto reads scorecard tokens -> skips vision calls on the off-token refute twin (RUNNER-03 confirmation)"
    - "refute.ts -> reads .planning/refute/refute-set.json + committed scorecards -> asserts polished>flawed per lens"
  prohibitions:
    - "NO critic reads a sibling critic's output (blindness preserves independent judgments)."
    - "NO averaging of persona scores to resolve disagreement (min() rollup + named losing cell)."
    - "NO aesthetic vision call on a vetoed cell (persona AND graphic-design skipped)."
    - "NO drift-brittle absolute margin threshold — margin is measured against the critic's own noise floor."
---

<objective>
Assemble the 7-critic panel and its brand-veto ordering (RUNNER-03) on top of the Plan-05 scoring engine, and build the refute battery (CRITIC-02) that runs the critic against the Plan-03 twins and enforces directional + margin + metamorphic pass bars. This is where "the panel runs one critic per persona + graphic-design + brand-veto, and a token/parity violation vetoes a change before aesthetic scoring" becomes real, and where the critic must PASS its refute-tests before it may drive the ratchet.

Purpose: RUNNER-03 (panel + veto ordering) and CRITIC-02 (refute-test pass bars).
Output: `panel.ts` and `refute.ts`, wired into `run.ts` score/validate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-CONTEXT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-RESEARCH.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-PATTERNS.md
@.planning/refute/refute-set.json
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-05-SUMMARY.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: panel.ts — critic->lens map, veto ordering, blind fan-out</name>
  <read_first>195-CONTEXT.md D-06 (critic->cell mapping table, veto pipeline steps 1-4, precedence vetoed>unstable>scored, disagreement preserved, fan-out per cell x lens-key with warm cache); 195-RESEARCH.md Anti-Patterns (averaging persona scores, persona-in-prefix); 195-PATTERNS.md "panel.ts" (cell iteration + cellId)</read_first>
  <files>examples/threadline_phoenix/e2e/critic/panel.ts</files>
  <action>Implement the panel: the critic->lens map (P1-P5 each write p{n}.hierarchy + p{n}.density with persona=p1..p5; graphic-design writes all.rhythm/all.typography/all.color_contrast; brand-veto writes all.brand_fidelity) = 14 lens-cells/page. Veto pipeline per capture cell, in order: (1) run verify.mechanical (deterministic, $0) via the committed scorecard mechanical fields; (2) --tl-* token-parity + dark<->light parity brand veto reading scorecard tokens (MODE A, $0); (3) on veto write all.brand_fidelity={current:null, vetoed:true, evidence:<mechanical line>} and SKIP ALL aesthetic vision calls for that cell (persona AND graphic-design); (4) no veto -> fan out one vision call per (cell x lens-key x dimension) through the Plan-05 client, each blind (no sibling output in context), the persona pass-condition clause supplied only in the uncached suffix so all five persona critics reuse one cached lens prefix. Apply precedence vetoed(null) > unstable(null) > scored — both write null distinguished by the vetoed flag. Preserve disagreement: no averaging across personas; when computing a min() rollup, name the losing (persona,lens) cell in the rollup evidence string. Order calls to keep the cached prefix warm inside the 5-min TTL (pre-warm before the sweep).</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>Type-checks; 14 lens-cells/page mapping; veto fires before any aesthetic call and writes null+vetoed; vetoed/unstable both null (flag-distinguished); no persona averaging; losing cell named in rollup evidence.</acceptance_criteria>
  <done>The 7-critic blind panel with correct brand-veto ordering is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: refute.ts — directional + margin + metamorphic gates</name>
  <read_first>195-CONTEXT.md D-03 (two-tier pass bars: binary directional + margin gate vs noise floor + metamorphic invariance; refute!=golden) + D-11 (position/order bias: prefer two independent blind single-cell scores compared by delta; void verdicts that flip under swap/paraphrase); 195-PATTERNS.md "refute.ts"; .planning/refute/refute-set.json (twin manifest)</read_first>
  <files>examples/threadline_phoenix/e2e/critic/refute.ts</files>
  <action>Implement the refute battery invoked by run.ts validate / score --refute-only. Read .planning/refute/refute-set.json; for each gestalt twin, score polished_cell_id and flawed_cell_id as two independent blind single-cell scores (position-bias-free) via the panel/client, then assert: binary directional gate (polished band/score > flawed AND the failure is attributed to the correct target_lens with located evidence) — ALL must pass; margin gate (the polished-vs-flawed delta and footgun-vs-polished gap exceed the critic's measured noise floor, not a fixed absolute); metamorphic invariance (re-score under A/B order-swap and a microcopy paraphrase — void any verdict that flips). For the veto_ordering twin, assert the panel emits NO aesthetic score (brand_fidelity null+vetoed) confirming RUNNER-03 ordering. A refute failure bars the critic from writing any ledger bump. Emit a committed last-known transcript per fixture for the deterministic residue asserted in Plan 03.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx tsc --noEmit -p tsconfig.json && node --import tsx critic/run.ts validate --dry-run; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>Type-checks; directional + margin (noise-floor-relative) + metamorphic gates implemented; veto-ordering twin yields no aesthetic score; failure bars ledger bumps; validate --dry-run exits 0.</acceptance_criteria>
  <done>The refute battery enforces sign/attribution/margin/metamorphic pass bars gating the critic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| token-parity veto -> aesthetic scoring | a broken brand envelope must earn no aesthetic credit |
| refute pass/fail -> ratchet permission | the critic must prove sign/attribution before driving anything |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-19 | Elevation of Privilege | vetoed change earning aesthetic credit | high | mitigate | Veto pipeline skips ALL aesthetic vision calls on veto; refute veto-ordering twin proves no aesthetic score is emitted |
| T-195-20 | Tampering | critic passing refute via position/verbosity bias | medium | mitigate | Two independent blind single-cell scores compared by delta; metamorphic invariance voids order/paraphrase flips; density anti-verbosity dimension |
| T-195-21 | Spoofing | drift-brittle margin threshold masking regressions | medium | mitigate | Margin gate is relative to the critic's measured noise floor, not a fixed absolute |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `node --import tsx critic/run.ts validate --dry-run` exits 0.
- Panel maps to 14 lens-cells/page; veto ordering correct; refute gates enforced.
</verification>

<success_criteria>
The 7-critic panel with brand-veto ordering (RUNNER-03) and the refute battery with directional/margin/metamorphic pass bars (CRITIC-02) are implemented; a token/parity violation vetoes before aesthetic scoring and refute failure bars ratcheting.
</success_criteria>

## Artifacts this plan produces
- `examples/threadline_phoenix/e2e/critic/panel.ts` (critic→lens map, veto ordering, blind fan-out)
- `examples/threadline_phoenix/e2e/critic/refute.ts` (directional + margin + metamorphic refute gates)

<output>
Create `.planning/phases/195-validated-adversarial-critic-runner-panel/195-06-SUMMARY.md` when done.
</output>
