---
phase: 195-validated-adversarial-critic-runner-panel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/stress_fixtures.ex
  - lib/threadline/operator_surface/live/stress_live.ex
  - examples/threadline_phoenix/e2e/tests/operator-tier-a-capture.spec.ts
  - .planning/refute/refute-set.json
  - .planning/scorecards/
  - test/threadline/operator_surface/refute_partition_test.exs
autonomous: true
requirements: [CRITIC-02]

must_haves:
  truths:
    - "A committed refute manifest (.planning/refute/refute-set.json) lists ~8 hand-authored A/B twins, each = one polished pole + one injected flaw, tagged with its target lens, expected direction, and class (gestalt vs veto-ordering)."
    - "Every gestalt twin PASSES all mechanical gates (MODE A + MODE B) — the D-03 partition rule — so it isolates the gestalt the mechanical layer cannot catch."
    - "The one off-token raw-hex accent twin is authored to trip the --tl-* token-parity brand veto (exercised at the panel layer in Plan 06), confirming veto-ordering."
    - "mix verify.mechanical stays green over the committed refute scorecards; the refute set is a separate manifest from the golden set (never folded in)."
  artifacts:
    - "refute-twin fixtures extending the footgun.*/reserved_for_phase idiom in stress_fixtures.ex"
    - ".planning/refute/refute-set.json manifest"
    - "committed .planning/scorecards/<refute cell>.json for the gestalt twins"
    - "test/threadline/operator_surface/refute_partition_test.exs (partition-rule guard)"
  key_links:
    - "refute-set.json twin -> stress_fixtures.ex fixture id -> committed scorecard -> Plan 06 refute.ts directional/margin/metamorphic gates"
    - "off-token twin -> Plan 06 panel.ts token-parity veto (no aesthetic score emitted)"
  prohibitions:
    - "NO runtime CSS-injection mutation path — twins are hand-authored committed fixtures (D-03)."
    - "NO refute fixture folded into golden-set.json (partition; no teaching-to-the-test)."
    - "NO flaw that trips a mechanical gate for the gestalt twins (if it trips mechanics it is a mechanical test, not a critic one)."
---

<objective>
Build the refute-test battery's deterministic substrate: ~8 hand-authored A/B twin fixtures (polished pole + one injected gestalt flaw) extending the existing `footgun.*` idiom, a committed refute manifest mapping each twin to its target lens and expected direction, committed scorecards for the gestalt twins, and a pure-Elixir partition-rule guard proving the gestalt flaws all PASS the mechanical gates (so they isolate exactly what the critic must catch). The LLM directional/margin/metamorphic scoring rides on top in Plan 06.

Purpose: CRITIC-02 — the fixtures and partition proof the critic will later be required to pass before it may drive the ratchet.
Output: refute fixtures, `.planning/refute/refute-set.json`, committed refute scorecards, and `refute_partition_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-CONTEXT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-PATTERNS.md
@lib/threadline/operator_surface/stress_fixtures.ex
@lib/threadline/operator_surface/live/stress_live.ex
@examples/threadline_phoenix/e2e/tests/operator-tier-a-capture.spec.ts
@brandbook/brand-book.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: refute-twin fixtures + capture wiring</name>
  <read_first>lib/threadline/operator_surface/stress_fixtures.ex (footgun.* + reserved_for_phase idiom, story/2 shape, assigns_for/1, by_id/1); lib/threadline/operator_surface/live/stress_live.ex (registration); examples/threadline_phoenix/e2e/tests/operator-tier-a-capture.spec.ts (BAND lists, cellId, captureCell); 195-CONTEXT.md D-03 (twin catalog + partition rule); brandbook/brand-book.md (real --tl-* accents + radius <=8px + card doctrine)</read_first>
  <files>lib/threadline/operator_surface/stress_fixtures.ex, lib/threadline/operator_surface/live/stress_live.ex, examples/threadline_phoenix/e2e/tests/operator-tier-a-capture.spec.ts</files>
  <action>Author the D-03 twin catalog as committed fixtures extending the footgun.*/reserved_for_phase idiom (DB-free, byte-stable). Six gestalt twins (each = polished + one injected flaw that stays within all mechanical gates): doubled section padding (token-step to token-step, still on-grid) -> rhythm; card-wrapping-a-section (under the depth-3 nesting ceiling) -> density (+brand card-rule); flattened hierarchy (equal weights/sizes) -> hierarchy; type-scale collapse (two sizes 1px apart, count still above the floor) -> typography; mis-jobbed accent (a real --tl-* token used in the wrong job, e.g. Ember as default action) -> brand_fidelity; chrome bloat (redundant help copy) -> density. Plus ONE veto-ordering twin: an off-token raw-hex accent authored so the critic-runner's --tl-* token-parity brand veto (Plan 06) fires and NO aesthetic score is emitted; author it so it is NOT committed as a mechanical MODE-A hard-block scorecard (keep verify.mechanical green — exercise it at the panel layer). Register the fixtures in stress_live and add the gestalt twin cell-ids to a refute capture band in the capture spec so verify.capture regenerates their scorecards byte-stably.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -c 'refute' lib/threadline/operator_surface/stress_fixtures.ex | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>~7-8 twin fixtures compile; registered in stress_live; gestalt twins added to the capture band; the veto-ordering twin does not create a mechanical hard-block scorecard.</acceptance_criteria>
  <done>Refute twins authored and wired for capture; codebase compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: refute manifest + committed scorecards + partition guard</name>
  <read_first>195-CONTEXT.md D-03 (partition rule, binary directional + margin + metamorphic gates, refute != golden); 195-PATTERNS.md "critic_trust_test.exs" (pure-Elixir guard + File.exists? evidence idiom); test/threadline/operator_surface/stress_ledger_test.exs (assertion helpers); .planning/scorecards/*.json (committed shape)</read_first>
  <files>.planning/refute/refute-set.json, .planning/scorecards/, test/threadline/operator_surface/refute_partition_test.exs</files>
  <action>Run verify.capture to produce byte-stable committed scorecards for the gestalt twins under .planning/scorecards/. Write .planning/refute/refute-set.json (two-space + trailing newline) as the separate refute manifest: version + an items array where each twin records {twin_id, polished_cell_id, flawed_cell_id, target_lens, expected_direction (polished>flawed), class ("gestalt"|"veto_ordering"), evidence_note}. Author test/threadline/operator_surface/refute_partition_test.exs (pure Elixir, async: true, Phoenix.LiveView guard): assert every gestalt twin's polished_cell_id and flawed_cell_id resolve to committed .planning/scorecards/*.json; assert each gestalt twin's flawed scorecard PASSES the mechanical gates (reuse/mirror the mechanical_checker assertions — MODE A no hard-block, MODE B within floor) proving the partition rule; assert the veto_ordering twin's flawed scorecard carries the off-token accent the panel veto will trip; assert no refute twin_id or cell_id appears in .planning/golden/golden-set.json (partition — refute never folded into golden). Do NOT add a new mix alias; this runs under verify.mechanical/verify.test and folds into ci.all via the test suite.</action>
  <verify>
    <automated>mix verify.mechanical && mix test test/threadline/operator_surface/refute_partition_test.exs</automated>
  </verify>
  <acceptance_criteria>All gestalt twin scorecards committed + pass mechanical; refute-set.json lists ~8 twins with lens+direction+class; partition test proves gestalt flaws pass mechanics and refute is disjoint from golden; verify.mechanical green.</acceptance_criteria>
  <done>Refute manifest + committed scorecards + partition guard exist; the deterministic residue of CRITIC-02 is proven in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed refute fixtures/scorecards -> critic validation gate | refute integrity gates whether the critic may drive the ratchet |
| refute set vs golden set | partition must hold — mixing teaches-to-the-test |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-07 | Tampering | refute fixtures silently tripping mechanics | high | mitigate | Partition guard asserts gestalt flaws PASS all mechanical gates; a flaw that trips mechanics is reclassified as a mechanical test, never a critic one |
| T-195-08 | Tampering | refute leaking into golden set | high | mitigate | refute_partition_test asserts refute twin_ids/cell_ids are disjoint from golden-set.json (no teaching-to-the-test) |
| T-195-09 | Denial of Service | verify.mechanical broken by veto-ordering twin | medium | mitigate | Off-token veto twin is exercised at the panel layer (Plan 06), not committed as a MODE-A hard-block scorecard, so ci.all stays green |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix verify.mechanical` green over committed refute scorecards.
- `mix test test/threadline/operator_surface/refute_partition_test.exs` green.
- refute-set.json disjoint from golden-set.json.
</verification>

<success_criteria>
The refute battery's deterministic substrate exists and is proven: ~8 committed A/B twins, the partition rule holds (gestalt flaws pass mechanics), the veto-ordering twin is set up, and refute is disjoint from golden (CRITIC-02 deterministic residue).
</success_criteria>

## Artifacts this plan produces
- Refute-twin fixtures in `lib/threadline/operator_surface/stress_fixtures.ex` (~8 twins) + `stress_live.ex` registration
- Refute cell-ids added to the capture band in `operator-tier-a-capture.spec.ts`
- `.planning/refute/refute-set.json` manifest
- Committed `.planning/scorecards/<refute cell>.json` for gestalt twins
- `test/threadline/operator_surface/refute_partition_test.exs`

<output>
Create `.planning/phases/195-validated-adversarial-critic-runner-panel/195-03-SUMMARY.md` when done.
</output>
