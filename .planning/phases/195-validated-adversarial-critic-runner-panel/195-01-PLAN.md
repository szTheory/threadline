---
phase: 195-validated-adversarial-critic-runner-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - examples/threadline_phoenix/e2e/package.json
  - examples/threadline_phoenix/e2e/tsconfig.json
  - .gitignore
  - CONTRIBUTING.md
  - .planning/design-system-ledger.json
  - test/threadline/operator_surface/stress_ledger_test.exs
  - test/threadline/operator_surface/critic_trust_test.exs
  - .planning/golden/golden-set.json
  - .planning/golden/rounds/.gitkeep
  - .planning/critic-scores/.gitkeep
autonomous: true
requirements: [RUNNER-04, RUNNER-05]
user_setup:
  - service: anthropic
    why: "Local-only critic runner (mix verify.ui_critique) calls the Claude API; the alias no-ops cleanly without the key so CI and contributors without a key are unaffected."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (maintainer-local only; never committed, never in CI)"

must_haves:
  truths:
    - "ANTHROPIC_API_KEY=\"\" mix verify.ui_critique exits 0 with a skip message (RUNNER-04 no-op)."
    - "mix verify.compile_no_optional stays green — root threadline gains no runtime dependency (RUNNER-05)."
    - "mix ci.all runs verify.critic_trust (pure-Elixir, no network) and stays green with the empty critic_trust skeleton."
    - "@anthropic-ai/sdk, zod, typescript, and tsx appear ONLY under devDependencies of examples/threadline_phoenix/e2e/package.json."
    - "CONTRIBUTING.md documents verify.ui_critique as local-only / excluded-from-ci.all under a doc-contract lock mirroring verify.flake."
  artifacts:
    - "mix.exs aliases verify.ui_critique (local-only) and verify.critic_trust (in ci.all)"
    - ".planning/design-system-ledger.json critic_trust block (6 lenses, all validated:false)"
    - "test/threadline/operator_surface/critic_trust_test.exs (green pure-Elixir stub)"
    - ".planning/golden/golden-set.json empty skeleton + .planning/golden/rounds/ + .planning/critic-scores/"
    - "examples/threadline_phoenix/e2e/tsconfig.json"
  key_links:
    - "mix.exs ci.all -> verify.critic_trust -> reads design-system-ledger.json critic_trust block"
    - "verify.ui_critique -> System.cmd npm run critic:score (only when ANTHROPIC_API_KEY present)"
  prohibitions:
    - "NO @anthropic-ai/sdk / zod in root mix.exs deps (devDependency of e2e only)."
    - "NO verify.ui_critique inside ci.all (local-only, LLM, excluded — the verify.flake precedent)."
    - "NO ANTHROPIC_API_KEY value committed anywhere; the key is read from env only."
    - "NO LiveView route, dashboard, HTML report, or public design-eval API (D-08)."
---

<objective>
Lay the build spine and named entrypoints for the whole phase: add the critic runner's Node toolchain as devDependencies of the e2e package (never root), define the two mix aliases (`verify.ui_critique` local-only, `verify.critic_trust` in `ci.all`), wire `verify.critic_trust` into `ci.all`, and seed the committed skeletons (empty golden set, empty `critic_trust` block, gitignored critic-scores tree) so every later plan lands on a green, self-consistent foundation.

Purpose: RUNNER-04 (the local-only no-op entrypoint under a doc-contract lock) and RUNNER-05 (Anthropic SDK stays an e2e devDependency; root stays Phoenix-optional). Also scaffolds the CRITIC-03 trust gate so `ci.all` is green before the α logic lands in Plan 04.
Output: mix aliases, e2e toolchain, committed skeleton manifests, and a passing pure-Elixir `critic_trust_test.exs` stub.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-CONTEXT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-RESEARCH.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-PATTERNS.md
@mix.exs
@examples/threadline_phoenix/e2e/package.json
@test/threadline/operator_surface/stress_ledger_test.exs
@.planning/design-system-ledger.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e Node toolchain + gitignore + tsconfig</name>
  <read_first>examples/threadline_phoenix/e2e/package.json (current shape), 195-PATTERNS.md "e2e/package.json" section (exact additive change + scripts), 195-RESEARCH.md Standard Stack + Pattern 4 (ESM), .gitignore</read_first>
  <files>examples/threadline_phoenix/e2e/package.json, examples/threadline_phoenix/e2e/tsconfig.json, .gitignore</files>
  <action>Add to e2e/package.json devDependencies (per D-07/RUNNER-05, additive only): @anthropic-ai/sdk ^0.110.0, zod ^3.x, typescript ^5.x, and tsx (the TS runner used by the npm scripts). Add scripts critic:score, critic:validate, critic:label, critic:rubric each invoking tsx against critic/run.ts with the matching subcommand (run.ts lands in Plan 05; the script names must exist now so verify.ui_critique can reference them). Create examples/threadline_phoenix/e2e/tsconfig.json for an ESM Node project (module/target modern ES, moduleResolution bundler/nodenext, strict true) scoped to critic/**. Add .gitignore entries for the regenerable critic verdict cache and the nondeterministic .planning/critic-scores/ tree (keep .planning/critic-scores/.gitkeep tracked); do NOT gitignore .planning/golden/ (the oracle is committed). Run npm install in the e2e dir to refresh the lockfile.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && node -e "const p=require('./package.json'); const d=p.devDependencies||{}; if(!(d['@anthropic-ai/sdk']&&d.zod&&d.typescript&&d.tsx)) process.exit(1); if((p.dependencies&&Object.keys(p.dependencies).length)) process.exit(2); process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>@anthropic-ai/sdk, zod, typescript, tsx present in e2e devDependencies; no runtime `dependencies` block; tsconfig.json parses; .planning/critic-scores/ is gitignored except .gitkeep.</acceptance_criteria>
  <done>e2e package declares the critic toolchain as devDependencies only; tsconfig + gitignore in place.</done>
</task>

<task type="auto">
  <name>Task 2: mix aliases + ci.all wiring + doc-contract lock</name>
  <read_first>mix.exs lines 10-23 (preferred_envs), 83-130 (aliases + ci.all), 162-178 (verify_example_browser System.cmd), 108-109 (verify.flake local-only comment); 195-PATTERNS.md "mix.exs" + "Local-only mix alias" + "mix.exs preferred_envs" sections; 195-RESEARCH.md Pattern 5; CONTRIBUTING.md "Deterministic tests" section</read_first>
  <files>mix.exs, CONTRIBUTING.md</files>
  <action>Add alias verify.critic_trust running the single file test test/threadline/operator_surface/critic_trust_test.exs (mirror verify.mechanical's single-file form) and add "verify.critic_trust": :test to preferred_envs. Insert "verify.critic_trust" into the ci.all list immediately BEFORE "verify.mechanical" (pure-Elixir, no LLM, fail-fast before browser cost). Define private verify_ui_critique/1 mirroring verify_example_browser/1 but: first read System.get_env("ANTHROPIC_API_KEY"); when nil or empty, IO.puts a "skipping (local-only)" message and return :ok (clean exit 0 — RUNNER-04 no-op); otherwise System.cmd("npm", ["run", "critic:score" | args], cd: the e2e dir, env: passing the key through, into: IO.stream(:stdio,:line)). Register "verify.ui_critique": &verify_ui_critique/1 in aliases and do NOT add it to ci.all. Add a load-bearing comment above the alias stating it is local-only / requires ANTHROPIC_API_KEY / excluded from ci.all (mirror the verify.flake comment). In CONTRIBUTING.md add a "Local-only critic (verify.ui_critique)" subsection paralleling the "Deterministic tests" verify.flake note so the doc-contract lock has a target; if a doc-contract test enumerates aliases, add verify.ui_critique/verify.critic_trust there. Per the milestone invariant, describe the local-only nature by concept — do NOT list any external SaaS visual-diff product names in committed copy.</action>
  <verify>
    <automated>ANTHROPIC_API_KEY="" mix verify.ui_critique && mix verify.compile_no_optional</automated>
  </verify>
  <acceptance_criteria>verify.ui_critique exits 0 with skip message when key absent; verify.critic_trust is in ci.all before verify.mechanical and NOT verify.ui_critique; compile_no_optional green (RUNNER-05); CONTRIBUTING documents the local-only lock without naming external SaaS tools.</acceptance_criteria>
  <done>Both aliases defined; ci.all wired; local-only no-op proven; doc-contract lock in place.</done>
</task>

<task type="auto">
  <name>Task 3: critic_trust skeleton + green pure-Elixir guard stub + committed manifests</name>
  <read_first>test/threadline/operator_surface/stress_ledger_test.exs (module/async pattern, @top_level_keys, helper defps, File.exists? evidence idiom); 195-PATTERNS.md "critic_trust_test.exs", "design-system-ledger.json", "golden-set.json" sections; 195-RESEARCH.md Code Examples (critic_trust block + golden-set item shapes); .planning/design-system-ledger.json top-level keys</read_first>
  <files>.planning/design-system-ledger.json, test/threadline/operator_surface/stress_ledger_test.exs, test/threadline/operator_surface/critic_trust_test.exs, .planning/golden/golden-set.json, .planning/golden/rounds/.gitkeep, .planning/critic-scores/.gitkeep</files>
  <action>Add a critic_trust top-level block to design-system-ledger.json with all 6 frozen lenses (hierarchy, density, rhythm, typography, color_contrast, brand_fidelity), each {alpha:null, raw_agreement:null, pairwise_acc:null, n:0, ci95:null, golden_rubric_version:null, model_id:"claude-opus-4-8", validated:false}; use the two-space-indent + trailing-newline byte-stable convention. Add "critic_trust" to the @top_level_keys assertion in stress_ledger_test.exs so its key-set test stays green. Create .planning/golden/golden-set.json skeleton with top-level {version, model_pin, rubric_rev, held_out_ids:[], items:[]} (per D-01; exact field spelling is Claude's discretion, consistent with the ledger convention). Create test/threadline/operator_surface/critic_trust_test.exs as a pure-Elixir ExUnit module (async: true, guarded by Code.ensure_loaded?(Phoenix.LiveView) like stress_ledger_test.exs; File.read!/Jason.decode! helpers). The stub asserts NOW (must be green against the skeleton): (a) critic_trust block exists with exactly the 6 lens keys; (b) every lens has the required field set; (c) for any lens with validated==true → alpha>=0.67 AND n>=20 AND raw_agreement>=0.80 AND model_id=="claude-opus-4-8" (vacuously true while all false); (d) golden-set.json parses and every item in items (empty now) resolves cell_id to an existing .planning/scorecards/<cell_id>.json and has non-empty r1/r2 evidence with r1.verdict==r2.verdict; (e) the critic never wrote under .planning/scorecards/ (assert no scorecard file references .planning/critic-scores). Leave the Krippendorff α gate, rubric-hash freshness guard, and CRITIQUE.md freshness guard as TODO markers for Plan 04/07 (those files do not exist yet — guard with File.exists? so absence is a vacuous pass, never a failure). Create .gitkeep files for golden/rounds and critic-scores.</action>
  <verify>
    <automated>mix verify.critic_trust && mix test test/threadline/operator_surface/stress_ledger_test.exs</automated>
  </verify>
  <acceptance_criteria>verify.critic_trust green against the empty skeleton; stress_ledger_test still green with critic_trust added to @top_level_keys; golden-set.json + rounds/ + critic-scores/ committed; no network/LLM in the test.</acceptance_criteria>
  <done>Ledger, golden skeleton, and a green pure-Elixir trust guard exist; ci.all is green end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer shell -> Anthropic API | ANTHROPIC_API_KEY crosses here; local-only, never CI |
| committed ledger/golden JSON -> ci.all gate | trust-gate integrity depends on these files not silently loosening the ratchet |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-01 | Information Disclosure | ANTHROPIC_API_KEY | high | mitigate | Key read from env only; alias no-ops cleanly when absent; .gitignore excludes verdict cache; guard/doc-contract asserts the key is never written to committed files |
| T-195-02 | Tampering | critic_trust block / golden-set.json | high | mitigate | Pure-Elixir verify.critic_trust in ci.all asserts validated lenses meet the bar; all lenses seed validated:false so nothing can ratchet before Plan 04 lands the real α gate |
| T-195-03 | Tampering | root dependency surface | high | mitigate | SDK is e2e devDependency only; verify.compile_no_optional in ci.all proves threadline stays Phoenix-optional (RUNNER-05) |
| T-195-SC | Tampering | npm devDependency install (@anthropic-ai/sdk, zod, tsx) | high | mitigate | RESEARCH Package Legitimacy Audit approved @anthropic-ai/sdk (official Anthropic org, 22.6M/wk, no postinstall); zod/tsx/typescript are ubiquitous OSS; no [ASSUMED]/[SUS] blockers remain |
</threat_model>

<verification>
- `mix ci.all` green (includes verify.critic_trust before verify.mechanical; excludes verify.ui_critique).
- `ANTHROPIC_API_KEY="" mix verify.ui_critique` exits 0.
- `mix verify.compile_no_optional` green.
- No root `deps` change in mix.exs; SDK only in e2e devDependencies.
</verification>

<success_criteria>
The build spine, named entrypoints, and committed skeletons exist; ci.all is green; the local-only no-op contract and no-root-dependency invariant are proven.
</success_criteria>

## Artifacts this plan produces
- mix.exs aliases: `verify.ui_critique` (local-only, no-op without key), `verify.critic_trust` (in ci.all)
- `examples/threadline_phoenix/e2e/package.json` devDependencies: @anthropic-ai/sdk, zod, typescript, tsx + critic:* npm scripts
- `examples/threadline_phoenix/e2e/tsconfig.json`
- `.planning/design-system-ledger.json` `critic_trust` block (6 lenses, validated:false)
- `test/threadline/operator_surface/critic_trust_test.exs` (green pure-Elixir stub)
- `.planning/golden/golden-set.json` skeleton, `.planning/golden/rounds/`, `.planning/critic-scores/`
- CONTRIBUTING.md local-only critic doc-contract lock

<output>
Create `.planning/phases/195-validated-adversarial-critic-runner-panel/195-01-SUMMARY.md` when done.
</output>
