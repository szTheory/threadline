---
phase: 195-validated-adversarial-critic-runner-panel
plan: 05
type: execute
wave: 2
depends_on: [195-01, 195-02]
files_modified:
  - examples/threadline_phoenix/e2e/critic/schema.ts
  - examples/threadline_phoenix/e2e/critic/prompt.ts
  - examples/threadline_phoenix/e2e/critic/bundle.ts
  - examples/threadline_phoenix/e2e/critic/client.ts
  - examples/threadline_phoenix/e2e/critic/scorecard.ts
  - examples/threadline_phoenix/e2e/critic/cache.ts
  - examples/threadline_phoenix/e2e/critic/run.ts
autonomous: true
requirements: [RUNNER-01, RUNNER-02, CRITIC-05]

must_haves:
  truths:
    - "The runner calls Claude vision via client.messages.parse() with output_config.format = zodOutputFormat(schema), one lens-dimension per call, model claude-opus-4-8, adaptive thinking display:summarized, and NEVER sends temperature/top_p/top_k/budget_tokens."
    - "The Zod schema makes evidence{kind,locator,observation} REQUIRED so a score without a located citation fails parse() and is discarded, not scored (CRITIC-05)."
    - "The cached prefix (system frozen -> rubric + reference-bar + 2 pole IMAGES with cache_control:ephemeral) exceeds 4096 tokens; the persona clause is LAST in the uncached suffix; the target screenshot is downsampled to ~1092px long edge and sits after the cache boundary."
    - "N-sample self-consistency: N=3 default escalating to N=7 on no-majority-band / within +-2pts of a band cut / within <=5pts of floor-target; point score = median(raw), band = band(median); unstable (band-mode < ceil(N/2) OR IQR>10 OR range>15) => current:null NEVER 0."
    - "Every critic-score file stamps model_id + rubric_version + n + band_mode + iqr + range + stable, is written under .planning/critic-scores/ ONLY, and never under .planning/scorecards/."
  artifacts:
    - "e2e/critic/{schema,prompt,bundle,client,scorecard,cache,run}.ts"
    - ".planning/critic-scores/<cell>/<lens>/<dim>.json output shape"
  key_links:
    - "run.ts score -> bundle.ts (reads scorecard+screenshot+aria) -> prompt.ts (cached prefix) -> client.ts (messages.parse N-sample) -> scorecard.ts (writes critic-scores)"
    - "cache.ts verdict cache keyed {cell,dim,rubric_hash,model_id} -> free resume/replay"
  prohibitions:
    - "NO writes under .planning/scorecards/ (deterministic bundle; guard enforced in critic_trust_test.exs)."
    - "NO temperature/top_p/top_k/budget_tokens sent to Opus 4.8 (400 error)."
    - "NO current:0 on unstable cells (poisons min() rollup) — must be null."
    - "NO hi-res 2576px screenshots (VLM must not re-measure pixels; downsample ~1092px)."
    - "NO persona clause inside the cached prefix (5x cache misses)."
---

<objective>
Build the runner's core call path — the module chain that turns one capture cell + one lens-dimension into a stamped, evidence-cited, self-consistency-averaged critic score. This is RUNNER-01 (SDK structured-output call with cached prefix, one dimension per call), RUNNER-02 (N-sample median + variance + unstable→null + stamping), and CRITIC-05 (evidence-or-discard enforced at the schema layer).

Purpose: the trustworthy scoring engine every other runner surface (panel, refute, report) rides on.
Output: `schema/prompt/bundle/client/scorecard/cache/run.ts` and the `.planning/critic-scores/` output tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-CONTEXT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-RESEARCH.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-PATTERNS.md
@examples/threadline_phoenix/e2e/tests/operator-tier-a-capture.spec.ts
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-01-SUMMARY.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: schema.ts (CRITIC-05) + prompt.ts (3-strata cached prefix) + bundle.ts</name>
  <read_first>195-RESEARCH.md Pattern 1 (messages.parse + zodOutputFormat), Pattern 2 (cache verification >4096), Pattern 3 (adaptive thinking), Pattern 4 (ESM path resolution), Pitfalls 1/2/8 (cache floor, persona-in-prefix, poles-only); 195-PATTERNS.md "schema.ts", "prompt.ts", "bundle.ts", "repoRoot convention"; 195-CONTEXT.md D-11 (three strata + cite-before-score field order) + D-05 (poles-only few-shot)</read_first>
  <files>examples/threadline_phoenix/e2e/critic/schema.ts, examples/threadline_phoenix/e2e/critic/prompt.ts, examples/threadline_phoenix/e2e/critic/bundle.ts</files>
  <action>schema.ts: define the Zod CriticDimensionSchema per RESEARCH Pattern 1 with field order evidence{kind(enum region|selector|mechanical_line), locator(string, REQUIRED), observation} -> pass(bool) -> band(enum) -> score(int) and lens(enum) — evidence and its kind+locator are REQUIRED (not optional) so parse() discards any uncited score (CRITIC-05); export zodOutputFormat(schema). bundle.ts: read the deterministic Tier-B input for a cell — scorecardPath(cellId) JSON (tokens, mode_b mechanical lines, artifacts.screenshot, artifacts.aria) — using ESM import.meta.url path resolution (repoRoot = resolve(here,"../../../..")); load + downsample the screenshot PNG to ~1092px long edge as base64; load the aria snapshot when present. prompt.ts: build the three byte-stable strata — system (frozen adversarial framing: role = adversarial critic, default verdict FAIL, evidence law/CRITIC-05, judge gestalt only never re-measure, no-praise) shared across lenses; cached prefix (rubrics/<lens>.md text + reference-bar + the two pole exemplar IMAGES) with cache_control:{type:"ephemeral"} on the LAST prefix block so it clears the 4096-token floor; uncached suffix (target screenshot, mechanical evidence lines, aria snapshot, then the persona pass-condition clause LAST). Pin MODEL_ID and SCHEMA_VERSION constants; no Date.now() in committed output.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>Type-checks clean; schema requires evidence.kind+locator; prompt builds system/cached-prefix/uncached-suffix with cache_control on the last prefix block and persona clause last; bundle downsamples screenshots to ~1092px.</acceptance_criteria>
  <done>Schema (CRITIC-05), cached-prefix prompt builder, and Tier-B bundle reader compile and enforce the locked prompt architecture.</done>
</task>

<task type="auto">
  <name>Task 2: client.ts (N-sample self-consistency + variance + stamping) + scorecard.ts + cache.ts</name>
  <read_first>195-RESEARCH.md Pattern 1/2/3, Pitfalls 3/4/5 (no temp/top_p, adaptive display, unstable->null); Anti-Patterns list; 195-CONTEXT.md D-04 (N=3->7 escalation triggers, variance rule) + D-10 (band-of-median, band-mode stability, raw IQR/range gates, band cuts {0,35,55,70,85}); 195-PATTERNS.md "client.ts", "scorecard.ts", "cache.ts" (writeJson two-space convention)</read_first>
  <files>examples/threadline_phoenix/e2e/critic/client.ts, examples/threadline_phoenix/e2e/critic/scorecard.ts, examples/threadline_phoenix/e2e/critic/cache.ts</files>
  <action>client.ts: wrap the Anthropic SDK; call messages.parse({model:"claude-opus-4-8", thinking:{type:"adaptive",display:"summarized"}, output_config:{format:zodOutputFormat(...)}}) — NEVER send temperature/top_p/top_k/budget_tokens (400 on Opus 4.8); clamp score client-side to 0-100 after parse (SDK strips numeric bounds); rely on SDK auto-retry for 429/5xx. N-sample loop: run N=3, compute point score = median(raw), band = band(median) using the 5-band cuts, band-mode for stability (unstable if count(modal band) < ceil(N/2)); escalate to N=7 when no strict majority band OR median within +-2pts of a band cut OR within <=5pts of floor/target; at N=7 re-test with ceil(7/2)=4; variance gates on RAW: IQR>10 OR range>15 => unstable. Assert usage.cache_creation_input_tokens>4096 on the first call of a lens sweep and warn if cache_read_input_tokens==0 on subsequent identical-prefix calls. scorecard.ts: write one file per (cell,lens,dimension) under .planning/critic-scores/<cell>/<lens>/<dim>.json (mkdir recursive) stamping model_id, rubric_version, n, scores_raw, score, band, band_mode, iqr, range, stable, pass, evidence, rationale, scored_at; unstable cells set score/current to null (NEVER 0); use the two-space + trailing-newline writeJson helper; NEVER write under .planning/scorecards/. cache.ts: verdict cache keyed {cell}__{dim}__{rubric_hash}__{model_id} -> fs JSON for free resume/replay (skip billed calls on hit).</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>Type-checks clean; no sampling params sent; median/band-of-median + band-mode + IQR/range unstable rule implemented; unstable→null; every write stamps model_id+rubric_version+variance; writes only under critic-scores/; verdict cache keyed on rubric_hash+model_id.</acceptance_criteria>
  <done>N-sample self-consistency engine + stamped critic-score writer + verdict cache, all Opus-4.8-correct.</done>
</task>

<task type="auto">
  <name>Task 3: run.ts CLI dispatcher (score path + no-op/empty-state exits)</name>
  <read_first>195-CONTEXT.md D-07 (CLI surface: critic score flags, --dry-run budget bands, empty-golden clean exit 0) + D-09 (first-run empty state guided path); 195-PATTERNS.md "run.ts" (repoRoot/ESM, pinned constants); RESEARCH Validation Architecture (Wave-0 gaps)</read_first>
  <files>examples/threadline_phoenix/e2e/critic/run.ts</files>
  <action>Implement run.ts as the CLI entry + subcommand dispatcher: score (fully wired through bundle->prompt->client->cache->scorecard), and validate|label|rubric routed via lazy dynamic import to their modules (panel/refute in Plan 06, report/label in Plan 07) so run.ts is the sole owner and missing modules only error when that subcommand is invoked. score flags: --page, --lens, --theme, --changed, --refute-only, --dry-run, --force (--dry-run prints the ~$0.45/$12/$23 budget bands via countTokens, no billed calls). First-run empty state: score against an empty golden set prints a guided path ("run critic label --bootstrap") and exits 0 (mirrors the no-key no-op); never dead-ends or reads "broken". The Node side reads ANTHROPIC_API_KEY from env (the Elixir alias owns the no-key skip). Use ESM path resolution + pinned MODEL_ID/SCHEMA_VERSION.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx tsc --noEmit -p tsconfig.json && node --import tsx critic/run.ts score --dry-run; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>run.ts type-checks; score --dry-run exits 0 and prints budget bands; empty-golden score exits 0 with guided path; validate/label/rubric routed via lazy import (no crash on import of run.ts).</acceptance_criteria>
  <done>The runner CLI dispatches the full score path and handles empty/dry-run states cleanly; downstream subcommands wired for Plans 06/07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screenshot + DOM/aria content -> Anthropic API | maintainer-local dev tooling sends captured operator-UI pixels/markup to a third-party API |
| LLM output -> committed critic-scores | nondeterministic output must be schema-validated and isolated from deterministic bundles |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-14 | Information Disclosure | screenshot/DOM sent to third-party API | medium | accept | Documented boundary: dev/maintainer-only local tooling, opt-in via ANTHROPIC_API_KEY, excluded from CI; captured content is synthetic stress fixtures + committed scorecards, no production/customer data |
| T-195-15 | Tampering | LLM output injection into critic-scores | high | mitigate | Zod messages.parse() validates every response; evidence.kind+locator required; non-conforming responses discarded, never written |
| T-195-16 | Tampering | critic writing to .planning/scorecards/ | high | mitigate | scorecard.ts writes only under .planning/critic-scores/; critic_trust_test.exs guard asserts no scorecard mutation |
| T-195-17 | Tampering | path traversal via untrusted cell_id | medium | mitigate | Validate cell_id against committed ledger/scorecard entries before constructing fs paths; never interpolate raw input into paths |
| T-195-18 | Information Disclosure | ANTHROPIC_API_KEY | high | mitigate | Read from env only via SDK; never logged, never written to critic-scores or cache files |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across critic/*.ts.
- `node --import tsx critic/run.ts score --dry-run` exits 0 with budget bands.
- Empty-golden score exits 0 with guided path.
- No sampling params in client.ts; no writes under .planning/scorecards/.
</verification>

<success_criteria>
The runner core calls Claude vision with schema-enforced evidence (CRITIC-05), one dimension per call over a >4096-token cached prefix (RUNNER-01), performs N-sample self-consistency with unstable→null and full stamping (RUNNER-02), and isolates output in `.planning/critic-scores/`.
</success_criteria>

## Artifacts this plan produces
- `examples/threadline_phoenix/e2e/critic/schema.ts` (Zod, required evidence — CRITIC-05)
- `examples/threadline_phoenix/e2e/critic/prompt.ts` (3-strata cached prefix)
- `examples/threadline_phoenix/e2e/critic/bundle.ts` (Tier-B reader + screenshot downsample)
- `examples/threadline_phoenix/e2e/critic/client.ts` (messages.parse + N-sample + variance + stamping)
- `examples/threadline_phoenix/e2e/critic/scorecard.ts` (writes .planning/critic-scores/)
- `examples/threadline_phoenix/e2e/critic/cache.ts` (verdict cache)
- `examples/threadline_phoenix/e2e/critic/run.ts` (CLI dispatcher + score path)

<output>
Create `.planning/phases/195-validated-adversarial-critic-runner-panel/195-05-SUMMARY.md` when done.
</output>
