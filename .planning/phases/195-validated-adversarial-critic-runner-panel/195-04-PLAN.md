---
phase: 195-validated-adversarial-critic-runner-panel
plan: 04
type: execute
wave: 2
depends_on: [195-01, 195-02]
files_modified:
  - lib/threadline/critic_trust/krippendorff_alpha.ex
  - test/threadline/critic_trust/krippendorff_alpha_test.exs
  - test/threadline/operator_surface/critic_trust_test.exs
autonomous: true
requirements: [CRITIC-03, CRITIC-04]

must_haves:
  truths:
    - "A pure-Elixir Krippendorff's α (ordinal) module computes chance-corrected agreement for two raters, handling De=0 (return 1.0), α<0 (not clamped), and n<2 (insufficient data)."
    - "mix verify.critic_trust asserts, for any lens with validated:true: alpha>=0.67 AND n>=20 AND raw_agreement>=0.80 AND golden_rubric_version matches the sha8 recomputed from rubrics/<lens>.md on disk AND model_id=='claude-opus-4-8'."
    - "A rubric-hash freshness guard recomputes each rubric's sha8 from disk bytes and asserts it equals the stamped version; a bump auto-invalidates that lens's trust."
    - "The golden-set guard asserts every item's cell_id resolves to a committed scorecard, r1==r2, and evidence is non-empty; the prefix-exemplar disjointness guard asserts pole cell-ids are disjoint from mid-range + held_out cells."
    - "The guard is pure Elixir, async:true, no LLM, no network — safe in ci.all (mirrors stress_ledger_test.exs)."
  artifacts:
    - "lib/threadline/critic_trust/krippendorff_alpha.ex (+ bootstrap CI helper)"
    - "test/threadline/critic_trust/krippendorff_alpha_test.exs (edge-case unit tests)"
    - "test/threadline/operator_surface/critic_trust_test.exs (expanded full gate)"
  key_links:
    - "rubrics/<lens>.md bytes -> sha8 -> critic_trust.golden_rubric_version match (auto-invalidation)"
    - "golden-set.json labels + critic-scores medians -> Krippendorff α -> critic_trust.alpha gate -> blocks Phase-196 ratchet"
  prohibitions:
    - "NO external process / Python / network in the α computation (pure Elixir, dependency-light)."
    - "NO clamping of negative α (systematic-worse-than-chance is a valid signal)."
    - "NO global aggregate pass — every critic-bearing lens clears its own per-lens bar (min() discipline)."
    - "NO raw_agreement used as the gate (it is a recorded companion; α is the gate)."
---

<objective>
Implement the trust gate that decides whether the critic may drive the ratchet: a pure-Elixir ordinal Krippendorff's α, and the expanded `verify.critic_trust` guard that enforces the per-lens bar (α ≥ 0.67 ∧ n ≥ 20 ∧ raw ≥ 80% ∧ version+model match) for any lens flagged `validated`, plus the rubric-hash freshness guard (auto-invalidation on bump) and the full golden-set + prefix-disjointness guards. This is the linchpin: below-threshold blocks automated ratcheting.

Purpose: CRITIC-03 (chance-corrected per-lens trust bar, pure-Elixir CI gate) and CRITIC-04 (rubric versioning enforced by hash freshness).
Output: the α module + edge-case tests + the fully expanded `critic_trust_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-CONTEXT.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-RESEARCH.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-PATTERNS.md
@test/threadline/operator_surface/stress_ledger_test.exs
@.planning/design-system-ledger.json
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-01-SUMMARY.md
@.planning/phases/195-validated-adversarial-critic-runner-panel/195-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Krippendorff's α (ordinal) pure-Elixir module</name>
  <read_first>195-RESEARCH.md Pattern 6 (full α implementation path + coincidence-matrix formula + ordinal distance + bootstrap CI) and Pitfalls 6/7 (imbalanced-set inflation, De=0); Assumptions A1 (no Hex library) and A6 (bootstrap vs jackknife); check hex.pm for an existing ordinal-α package before hand-rolling</read_first>
  <behavior>
    - compute/1 on perfect agreement pairs → {:ok, 1.0}
    - compute/1 when all values identical (De=0) → {:ok, 1.0} (no disagreement possible)
    - compute/1 on systematic worse-than-chance → {:ok, alpha} with alpha < 0 (NOT clamped)
    - compute/1 with < 2 pairable values → {:error, :insufficient_data}
    - Known reference vector from RESEARCH → alpha within tolerance of the documented value
    - bootstrap_ci/2 returns a {lo, hi} tuple deterministically (seeded) for n>=20
  </behavior>
  <files>lib/threadline/critic_trust/krippendorff_alpha.ex, test/threadline/critic_trust/krippendorff_alpha_test.exs</files>
  <action>First confirm no maintained Hex library provides ordinal Krippendorff's α (A1); if one exists and is dependency-light, prefer it — otherwise hand-roll per RESEARCH Pattern 6. Implement Threadline.CriticTrust.KrippendorffAlpha.compute/1 taking a list of {r1_value, r2_value} ordinal pairs, building the coincidence matrix, computing Do/De with the ordinal distance function, returning {:ok, alpha} or {:error, :insufficient_data}. Handle De=0 → {:ok, 1.0}; do NOT clamp negative α. Add bootstrap_ci/2 (seeded/deterministic) for the ci95 field. Write RED-first edge-case unit tests (perfect, De=0, negative, insufficient, reference vector, bootstrap determinism) then implement to green. Keep it pure (no network, no external process).</action>
  <verify>
    <automated>mix test test/threadline/critic_trust/krippendorff_alpha_test.exs</automated>
  </verify>
  <acceptance_criteria>All edge-case tests green; De=0→1.0; negative α not clamped; insufficient-data path returns error; bootstrap CI deterministic.</acceptance_criteria>
  <done>Pure-Elixir ordinal Krippendorff's α + bootstrap CI, fully unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: expand verify.critic_trust — per-lens gate + rubric-hash + golden + disjointness guards</name>
  <read_first>test/threadline/operator_surface/critic_trust_test.exs (the Plan-01 stub + its TODO markers); 195-CONTEXT.md D-02 (per-lens gate exact thresholds), D-05 (per-lens semver+sha8 versioning), D-01 (golden guard: cell refs resolve, r1==r2, evidence non-empty, held_out frozen); 195-PATTERNS.md "critic_trust_test.exs" (assertion + File.exists? idioms); 195-RESEARCH.md Code Examples (critic_trust + golden-set shapes)</read_first>
  <files>test/threadline/operator_surface/critic_trust_test.exs</files>
  <action>Expand the Plan-01 stub into the full gate. Per-lens trust gate: for every lens with validated==true, assert alpha (via the ledger value, cross-checked recomputable from golden labels + committed critic-scores medians when present) >=0.67 AND n>=20 AND raw_agreement>=0.80 AND model_id=="claude-opus-4-8" AND golden_rubric_version equals "<lens>@<semver>+<sha8>" where sha8 is recomputed from the bytes of examples/threadline_phoenix/e2e/critic/rubrics/<lens>.md on disk (rubric-hash freshness — a bump auto-invalidates until re-validated). Golden-set guard: every item cell_id resolves to a committed .planning/scorecards/<cell_id>.json; every kept item has non-empty r1.evidence and r2.evidence and r1.verdict==r2.verdict; held_out_ids never appear in items. Prefix-exemplar disjointness guard: the pole cell-ids named in each rubric's Anchors block are disjoint from the golden mid-range items and held_out_ids (no teaching-to-the-test). CRITIQUE.md freshness + forbidden-terms guard: gated behind File.exists?(".planning/CRITIQUE.md") (Plan 07 creates it) — assert one row per (cell_id x persona) is a substring and reuse the existing @forbidden_terms list convention so no external SaaS visual-diff product name appears. Keep async:true, no LLM, no network.</action>
  <verify>
    <automated>mix verify.critic_trust && mix ci.all</automated>
  </verify>
  <acceptance_criteria>verify.critic_trust enforces the full per-lens bar + rubric-hash + golden + disjointness guards; passes against current committed state (all lenses still validated:false → gate vacuous, other guards active); ci.all green.</acceptance_criteria>
  <done>The trust gate is fully implemented and green; below-threshold or version-drift blocks ratcheting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden labels + critic medians -> α -> ratchet permission | the integrity of the whole forward-only gate rides here |
| rubric bytes on disk -> stamped version | drift must auto-invalidate, never silently loosen |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-195-10 | Tampering | critic_trust block silently loosening the ratchet | high | mitigate | Pure-Elixir gate asserts per-lens α>=0.67 ∧ n>=20 ∧ raw>=80 ∧ version+model match; no global aggregate pass; raw% is companion-only |
| T-195-11 | Tampering | rubric edited without re-validation | high | mitigate | sha8 recomputed from disk == stamped version; a bump auto-invalidates that lens (validated must return to false until re-scored) |
| T-195-12 | Tampering | golden set weakened (fabricated labels / unresolved cells) | high | mitigate | Golden guard: cell refs resolve, r1==r2, evidence non-empty; disjointness guard prevents poles/mid-range/held_out overlap (Goodhart-on-validator) |
| T-195-13 | Spoofing | imbalanced golden set inflating raw agreement | medium | mitigate | α is the chance-corrected gate (exposes always-bad critic); raw% is recorded but never the gate |
</threat_model>

<verification>
- `mix test test/threadline/critic_trust/krippendorff_alpha_test.exs` green (edge cases).
- `mix verify.critic_trust` green (full gate over current committed state).
- `mix ci.all` green.
</verification>

<success_criteria>
The chance-corrected per-lens trust gate exists in pure Elixir and blocks ratcheting below threshold or on version drift; rubric-hash, golden, and disjointness guards are enforced in ci.all (CRITIC-03, CRITIC-04).
</success_criteria>

## Artifacts this plan produces
- `lib/threadline/critic_trust/krippendorff_alpha.ex` (+ bootstrap CI helper)
- `test/threadline/critic_trust/krippendorff_alpha_test.exs`
- Expanded `test/threadline/operator_surface/critic_trust_test.exs` (per-lens gate + rubric-hash + golden + disjointness + conditional CRITIQUE freshness guards)

<output>
Create `.planning/phases/195-validated-adversarial-critic-runner-panel/195-04-SUMMARY.md` when done.
</output>
