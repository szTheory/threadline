---
phase: 195-validated-adversarial-critic-runner-panel
plan: 08
type: execute
wave: 5
depends_on: [195-04, 195-05, 195-06, 195-07]
gap_closure: true
files_modified:
  - lib/threadline/critic_trust/measure.ex
  - lib/threadline/critic_trust/ledger_splice.ex
  - lib/mix/tasks/critic.measure.ex
  - test/threadline/critic_trust/measure_test.exs
  - test/threadline/critic_trust/ledger_splice_test.exs
  - mix.exs
  - examples/threadline_phoenix/e2e/critic/run.ts
  - CONTRIBUTING.md
  - .planning/phases/195-validated-adversarial-critic-runner-panel/195-07-PLAN.md
  - .planning/phases/195-validated-adversarial-critic-runner-panel/195-VALIDATION.md
autonomous: true
requirements: [CRITIC-03, CRITIC-01]
---

<objective>
Gap-closure discovered while preparing the 195-07 human-verify validation: the
phase's designed "trust-measurement recompute" (golden labels + critic scores →
measured per-lens `critic_trust` block → `validated:true` promotion) was never
implemented by any plan, and three runbook commands did not match the shipped CLI.
Build the missing writer and reconcile the CLI/runbook so the maintainer validation
loop (195-07 Task 3) is actually executable.
</objective>

<tasks>
<task>Task 1: Trust-measurement writer — `mix critic.measure` (`Mix.Tasks.Critic.Measure`)
+ pure engine `Threadline.CriticTrust.Measure` + byte-stable idempotent
`Threadline.CriticTrust.LedgerSplice`, reusing `KrippendorffAlpha`. Emits the guard's
exact 8-field key set; promotes iff α≥0.67 ∧ n≥20 ∧ raw≥0.80 ∧ model pin ∧ fresh rubric.
Local-only, not in ci.all, never auto-commits (T-195-24). Unit tests for both modules.</task>
<task>Task 2: CLI wiring — mix.exs `--` separator so verify.ui_critique forwards flags;
run.ts `--golden` to scope scoring to labeled (cell,lens) pairs (T-195-17 guard).</task>
<task>Task 3: Reconcile the runbook across CONTRIBUTING.md + 195-07-PLAN.md + 195-VALIDATION.md
(source .env; refute battery = npm run critic:validate; scoped scoring = critic:score
-- --golden; trust write = mix critic.measure).</task>
</tasks>

<verification>
- mix compile --warnings-as-errors; mix format --check-formatted
- mix test measure_test + ledger_splice_test + critic_trust_test + refute_partition_test (36 tests)
- cd e2e && npx tsc --noEmit; node run.ts score --golden --dry-run exits 0
- mix critic.measure on empty golden = byte-identical no-op (idempotent)
- doc-contract greps: no stale `verify.ui_critique --refute-only`; all three docs carry the real commands
- ci.all green through verify.critic_trust + verify.mechanical (verify.example fails only on the
  pre-existing demo_contract search_path env issue, unrelated)
</verification>

<success_criteria>
`mix critic.measure` measures and writes the per-lens critic_trust block from the golden
set + critic scores (validated or provisional), the CLI/runbook match reality, and the
195-07 maintainer validation loop is executable end-to-end.
</success_criteria>
