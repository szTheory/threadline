# Plan 11-01: Continuity public API, Mix task, brownfield integration test (TOOL-02 core)

**Phase:** 11 — Backfill / continuity  
**Plan:** 11-01 of 02  
**Status:** Ready to execute  
**Depends on:** —  
**Blocks:** 11-02  
**Requirements covered:** TOOL-02  
**Wave:** 1  
**requirements_addressed:** TOOL-02  
**files_modified:** `lib/threadline/continuity.ex`, `lib/mix/tasks/threadline.continuity.ex`, `test/threadline/continuity_brownfield_test.exs`, `mix.exs`  
**autonomous:** true  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-11-01-01 | Application code inserts `AuditChange` rows that look like trigger captures | high | Module and task **never** call `Repo.insert` on `AuditChange`; `rg 'AuditChange' lib/threadline/continuity.ex lib/mix/tasks/threadline.continuity.ex` must show **no** insert/build of schema struct for inserts (only allowed: documentation strings mentioning the schema). |
| T-11-01-02 | User-supplied table name strings passed into SQL without quoting → injection | high | Any dynamic identifier uses `Ecto.Adapters.SQL.quote_table/2` or pre-approved allowlist from `Threadline.Capture.TriggerSQL` patterns; document in moduledoc that only validated public table names are accepted. |
| T-11-01-03 | Mix task implies history exists before T₀ | medium | `@moduledoc` and runtime banner include explicit sentence containing substring **`no pre-trigger history`** (grep-verifiable). |

---

## Goal

Ship the **executable** slice of **TOOL-02**: a **public** `Threadline.Continuity` module and **`mix threadline.continuity`** task that document and enforce **honest T₀** semantics (empty `audit_changes` until first post-install mutation), plus a **PostgreSQL integration test** proving invariants on `audit_changes` / `audit_transactions` for a brownfield table.

---

## Context

- **Authoritative:** `11-CONTEXT.md` (D-01–D-16), `11-RESEARCH.md`, `.planning/REQUIREMENTS.md` (TOOL-02), `11-PATTERNS.md`.

---

## Tasks

### Task 1: `Threadline.Continuity` module

**What to do**

1. Add `lib/threadline/continuity.ex` with `@moduledoc` that states: (a) **T₀** = no `AuditChange` rows until first real trigger-fired mutation after capture is installed; (b) `Threadline.history/3` returns **`[]`** for a PK until that first mutation; (c) link to `guides/brownfield-continuity.md` (file may not exist until Plan 11-02 — use relative path as **future** anchor matching plan 11-02 filename exactly **`guides/brownfield-continuity.md`**).
2. Implement at minimum:
   - `explain_cutover(repo, opts)` **or** equivalently named function returning `{:ok, iodata}` / `{:ok, map}` describing steps (no writes) — satisfies dry-run semantics for programmatic callers.
   - `assert_capture_ready!(repo, table_name)` **or** similar: validates table exists in public schema, trigger installed (reuse or delegate to `Threadline.Health.trigger_coverage/1` query semantics — **do not** fork catalog SQL; call `trigger_coverage(repo: repo)` and check tuple list contains `{:covered, table_name}`).
3. All public functions take **`repo:`** as first-class option (keyword).

**Read first**

- `lib/threadline/capture/audit_change.ex`
- `lib/threadline/health.ex`
- `lib/mix/tasks/threadline.verify_coverage.ex`

**Acceptance criteria**

- `grep -n 'defmodule Threadline.Continuity' lib/threadline/continuity.ex` exits **0**.
- `grep -F 'guides/brownfield-continuity.md' lib/threadline/continuity.ex` exits **0**.
- `grep -F 'Threadline.Health.trigger_coverage' lib/threadline/continuity.ex` exits **0** if implementation uses Health for coverage check; if not used, file must document why in moduledoc and still prove no duplicated `pg_trigger` SQL via `rg 'pg_trigger|pg_tables' lib/threadline/continuity.ex` exit code **1**.

**Done condition:** All acceptance criteria pass.

---

### Task 2: `mix threadline.continuity`

**What to do**

1. Add `lib/mix/tasks/threadline.continuity.ex` with `@shortdoc` **exactly**: `Brownfield capture cutover helper — honest T₀ semantics (see guides/brownfield-continuity)` (adjust length if needed — max ~100 chars; must contain **`brownfield`** and **`T₀`** or the substring **`T0`** for grep stability — use **`T0`** ASCII if `₀` is problematic).
2. **Concrete `@shortdoc`:** `Brownfield capture cutover — honest T0; see guides/brownfield-continuity.md`
3. Delegate to `Threadline.Continuity`; support `--dry-run` flag via `OptionParser` that prints explain output and exits 0 without writes.
4. Idempotent messaging: second run when trigger already present prints skip/info containing substring **`already`** (case-insensitive).

**Read first**

- `lib/mix/tasks/threadline.install.ex`
- `lib/threadline/continuity.ex`

**Acceptance criteria**

- `grep -F 'Mix.Tasks.Threadline.Continuity' lib/mix/tasks/threadline.continuity.ex` exits **0**.
- `grep -F 'Threadline.Continuity' lib/mix/tasks/threadline.continuity.ex` exits **0**.
- `MIX_ENV=test mix compile --warnings-as-errors` exits **0**.

**Done condition:** All acceptance criteria pass.

---

### Task 3: Brownfield integration test

**What to do**

1. Add `test/threadline/continuity_brownfield_test.exs` using `Threadline.DataCase`.
2. **Fixture flow:** In `setup_all`, create table `threadline_continuity_brownfield` (or similar unique name), `INSERT` at least one row **without** trigger; install `Threadline.Capture.TriggerSQL.create_trigger/1` for that table; `on_exit` drop trigger and table.
3. In test: define an inline **`Ecto.Schema`** module mapping `schema "threadline_continuity_brownfield"` with the same PK field as the SQL table (pattern: `test/threadline/query_test.exs` `FakeUser` modules). Resolve stable PK from inserted row; call **`Threadline.history(SchemaModule, id, repo: Repo)`** per `lib/threadline.ex` and assert **`[] ==`** result before any post-trigger mutation.
4. Perform one `UPDATE` or `INSERT` after trigger is live; assert **at least one** `AuditChange` for that PK and `AuditTransaction` FK integrity (`Repo.all` + assertions similar to `trigger_test.exs`).
5. Truncate/cleanup per `DataCase` `setup`.

**Read first**

- `test/threadline/capture/trigger_test.exs`
- `lib/threadline.ex` (for `history/3` signature)
- `lib/threadline/capture/trigger_sql.ex`

**Acceptance criteria**

- `MIX_ENV=test mix test test/threadline/continuity_brownfield_test.exs` exits **0** when PostgreSQL is available (same skip policy as other DataCase tests if any — if project uses `@tag :postgres`, mirror that).
- Test file contains substring **`threadline_continuity_brownfield`** OR table name constant documented in module attribute used consistently.

**Done condition:** Test passes in CI matrix that includes PostgreSQL.

---

### Task 4: Register Mix task in `mix.exs`

**What to do**

1. Under `groups_for_modules` → `"Mix Tasks"`, add `Mix.Tasks.Threadline.Continuity` adjacent to existing Threadline tasks.

**Read first**

- `mix.exs`

**Acceptance criteria**

- `grep -F 'Mix.Tasks.Threadline.Continuity' mix.exs` exits **0**.

**Done condition:** Criterion passes.

---

## Verification

1. `MIX_ENV=test mix test test/threadline/continuity_brownfield_test.exs` exits **0**.
2. `MIX_ENV=test mix compile --warnings-as-errors` exits **0**.
3. `rg 'Repo\.(insert|insert_all).*AuditChange' lib/threadline/continuity.ex lib/mix/tasks/threadline.continuity.ex`; exit code **1** (no matches).

---

## Success criteria

- **TOOL-02 (partial):** Runnable API + Mix task + brownfield proof; documentation and Hex extras complete in **11-02**.
- Roadmap SC2 (tests on `audit_changes` / `audit_transactions`) satisfied for brownfield path.
- Roadmap SC3: no default misleading `AuditChange` inserts from library code.
