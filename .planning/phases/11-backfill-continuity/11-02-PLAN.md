# Plan 11-02: Brownfield guide, README, HexDocs extras, cross-links (TOOL-02 docs)

**Phase:** 11 — Backfill / continuity  
**Plan:** 11-02 of 02  
**Status:** Ready to execute  
**Depends on:** 11-01  
**Blocks:** —  
**Requirements covered:** TOOL-02  
**Wave:** 2  
**requirements_addressed:** TOOL-02  
**files_modified:** `guides/brownfield-continuity.md`, `README.md`, `guides/domain-reference.md`, `mix.exs`  
**autonomous:** true  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-11-02-01 | Operators copy-paste dangerous SQL from README | medium | README contains **no** full migration DDL blocks for brownfield — only bullets + **one** link to `guides/brownfield-continuity.md`; verify `grep -c 'CREATE TABLE' README.md` for new section remains **0** additions (baseline-aware: if README already has CREATE TABLE elsewhere, instead assert new brownfield subsection does not add `CREATE TABLE` lines — **concrete:** new subsection title contains **`Brownfield`** and body under it has **no** lines starting with `CREATE `). |
| T-11-02-02 | HexDocs omits the guide | low | `mix.exs` `extras:` includes **`guides/brownfield-continuity.md`** exactly once. |

---

## Goal

Complete **TOOL-02** documentation contracts: canonical **`guides/brownfield-continuity.md`**, README discovery bullets, **`mix.exs` HexDocs extras**, and **`guides/domain-reference.md`** cross-links — aligned with roadmap SC1 and CONTEXT D-12–D-14.

---

## Context

- **Authoritative:** `11-CONTEXT.md`, `11-RESEARCH.md`, `11-01-PLAN.md` (implementation must exist first).

---

## Tasks

### Task 1: Author `guides/brownfield-continuity.md`

**What to do**

1. Create `guides/brownfield-continuity.md` with sections at minimum:
   - **H1** title containing **`Brownfield`** and **`continuity`** (both substrings).
   - **Semantics:** explain **T0** / empty `audit_changes` until first post-install mutation; explicitly state **`Threadline.history/3`** may return **`[]`** for existing PKs until then.
   - **Operator checklist:** numbered steps: install audit schema → generate triggers → run `mix threadline.verify_coverage` (cross-link) → run `mix threadline.continuity` (or `--dry-run`).
   - **Compliance snapshot:** paragraph referencing CONTEXT D-03 (export / app-owned table, not retroactive audit) with substring **`not`** and **`retroactive`** in same sentence.
   - **PgBouncer / transactions:** short note on single `BEGIN…COMMIT` for operator SQL bundles (D-07).

**Read first**

- `11-CONTEXT.md`
- `guides/domain-reference.md`

**Acceptance criteria**

- `test -f guides/brownfield-continuity.md` exits **0**.
- `grep -F 'Threadline.history' guides/brownfield-continuity.md` exits **0**.
- `grep -F 'mix threadline.continuity' guides/brownfield-continuity.md` exits **0**.

**Done condition:** All acceptance criteria pass.

---

### Task 2: `mix.exs` — HexDocs extras

**What to do**

1. Append `"guides/brownfield-continuity.md"` to the `extras:` list (preserve ordering style of existing entries).

**Read first**

- `mix.exs`

**Acceptance criteria**

- `grep -F '"guides/brownfield-continuity.md"' mix.exs` exits **0**.

**Done condition:** Criterion passes.

---

### Task 3: README brownfield subsection

**What to do**

1. Add a short subsection (under a heading containing **`Brownfield`**) with 3–6 bullets: honest gap before capture; link to `guides/brownfield-continuity.md`; mention `mix threadline.continuity` and **`--dry-run`**.
2. Do not duplicate the full guide.

**Read first**

- `README.md`
- `guides/brownfield-continuity.md` (after Task 1)

**Acceptance criteria**

- `grep -F 'guides/brownfield-continuity.md' README.md` exits **0**.
- `grep -F 'threadline.continuity' README.md` exits **0**.

**Done condition:** Both criteria pass.

---

### Task 4: `guides/domain-reference.md` cross-link

**What to do**

1. Add a bullet or paragraph under an appropriate section (capture / operations) linking to **`guides/brownfield-continuity.md`** with one-sentence description of T0 semantics.

**Read first**

- `guides/domain-reference.md`

**Acceptance criteria**

- `grep -F 'brownfield-continuity.md' guides/domain-reference.md` exits **0**.

**Done condition:** Criterion passes.

---

## Verification

1. `MIX_ENV=test mix docs` exits **0** (or `mix docs` per project convention).
2. `grep -F 'brownfield-continuity' mix.exs README.md guides/domain-reference.md` — all three files match (single shell: `test $(grep -l brownfield-continuity mix.exs README.md guides/domain-reference.md | wc -l) -eq 3`).

---

## Success criteria

- **TOOL-02** documentation items from roadmap SC1 satisfied (public module + Mix task already from 11-01; this plan completes narrative in versioned guide + README + HexDocs).
- SC3 preserved: guide explicitly warns against fabricating `AuditChange` pre-history.
