---
status: testing
phase: 194-deterministic-scorecard-cube-ledger-mechanical-capture-found
source: [194-VERIFICATION.md]
started: 2026-07-03T00:00:00Z
updated: 2026-07-03T00:00:00Z
---

## Current Test

number: 1
name: Tier A capture lane emits 120 byte-stable evidence bundles in CI
expected: |
  In CI (or a local env with a migrated example-app DB), `mix verify.capture` produces
  exactly 120 committed `.planning/scorecards/*.json` (66 Band-1 + 54 Band-2) plus 54
  `.aria.yml` deep-band files. A second run leaves `git diff` on `.planning/scorecards/`
  empty (byte-stable regeneration).
awaiting: user response

## Tests

### 1. Tier A capture lane emits 120 byte-stable evidence bundles in CI
expected: Run `mix verify.capture` in CI (migrated example DB + browser), then re-run and confirm empty `git diff` on `.planning/scorecards/`. 120 scorecard JSON + 54 aria.yml generated; regeneration byte-stable.
result: [pending]

### 2. Capture→check E2E clean over real evidence; mechanical_floors seeded
expected: After scorecards are captured, run `mix verify.mechanical` (and full `mix ci.all`) over the real committed `.planning/scorecards/*.json`; seed `mechanical_floors` from that first capture and confirm the Plan-01 monotonicity guard stays green. `MechanicalChecker.run/1` returns `{:ok, []}` over real evidence (any MODE-A finding snapped to nearest token); `ci.all` green end-to-end.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
