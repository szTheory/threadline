---
status: complete
phase: 194-deterministic-scorecard-cube-ledger-mechanical-capture-found
source: [194-VERIFICATION.md]
started: 2026-07-03T00:00:00Z
updated: 2026-07-03T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Tier A capture lane emits 120 byte-stable evidence bundles
expected: Run `mix verify.capture` against a migrated example DB + browser, then re-run and confirm empty `git diff` on `.planning/scorecards/`. 120 scorecard JSON (66 Band-1 + 54 Band-2) + 54 aria.yml generated; regeneration byte-stable.
result: pass
source: automated
covered_by: >
  Executed locally against the migrated example-app test DB — produced exactly
  120 `.planning/scorecards/*.json` (66 Band-1 + 54 Band-2) + 54 `.aria.yml`;
  re-run left `git diff` byte-identical (proven twice). Now committed evidence.
  Recurring gate: CI job `verify-capture` (.github/workflows/ci.yml) regenerates
  the lane on a fresh DB and asserts the 120+54 count AND byte-stable
  regeneration (`git status --porcelain .planning/scorecards/` empty) on every run.
  Local command: `mix verify.capture`.

### 2. Capture→check E2E clean over real evidence; mechanical_floors seeded
expected: Run the mechanical checker over the real committed `.planning/scorecards/*.json`; `MechanicalChecker.run/1` returns `{:ok, []}`; seed `mechanical_floors` from the first capture; Plan-01 monotonicity guard stays green.
result: pass
source: automated
covered_by: >
  `MechanicalChecker.run/1` returns `{:ok, []}` over the 120 committed scorecards.
  (One capture-scoping defect was found and fixed: the `/audit/__stress` harness
  chrome leaked into evidence and produced 120 spurious 1:1 WCAG findings — the
  real product surface is fully conformant; see fix ab2fb7f7.) `mechanical_floors`
  seeded with 600 values (20 ledger-ids × 5 MODE-B metrics × 6 theme-breakpoints)
  via `MechanicalChecker.measure_mode_b/1`; ledger diff is a single hunk (floors
  block only). stress_ledger_test (16/16) + mechanical_checker_test (18/18) green.
  Recurring gate: the real-evidence assertion in mechanical_checker_test.exs runs
  under `mix verify.mechanical` (CI job `verify-mechanical`, every PR) and asserts
  run/1 clean over the committed evidence. Local command: `mix verify.mechanical`.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — both items shifted left into automated CI gates; 0 human verification]
