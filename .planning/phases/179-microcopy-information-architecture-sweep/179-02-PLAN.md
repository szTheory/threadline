---
phase: 179-microcopy-information-architecture-sweep
plan: 02
type: execute
wave: 2
depends_on:
  - "179-01"
files_modified:
  - lib/threadline/operator_surface/ui.ex
  - lib/threadline/operator_surface/unsupported.ex
  - lib/threadline/operator_surface/components/unsupported_view.ex
  - lib/threadline/operator_surface/presentation.ex
  - test/threadline/operator_surface/ui_test.exs
  - test/threadline/operator_surface/data_state_mapping_wave0_test.exs
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
requirements_addressed:
  - COPY-01
  - COPY-02
  - COPY-03
must_haves:
  truths:
    - "D-04/D-05: Shared helper labels use plain operator nouns while precise evidence verdicts and code-ish tokens remain allowed only in scoped contexts."
    - "D-06/D-08: Error, empty, stale, permission, unavailable, redacted, pruned, warning, success, and destructive-support copy uses sentence-case controlled templates."
    - "D-09: Permission/unavailable states are alerts, neutral empty/progress/stale states are status-oriented, and validation summaries are focusable with field links."
    - "D-10: Existing reference copy helpers keep full-value `data-tl-copy` targets."
    - "D-15: Shared helper edits do not introduce dependencies, public API churn, LiveComponent extraction, route changes, or new capabilities."
  artifacts:
    - path: "lib/threadline/operator_surface/ui.ex"
      provides: "Shared state grammar, stale banner, error summary, and reference copy affordance"
      exports:
        - "Threadline.OperatorSurface.UI.data_state/1"
        - "Threadline.OperatorSurface.UI.empty_state/1"
        - "Threadline.OperatorSurface.UI.error_state/1"
        - "Threadline.OperatorSurface.UI.stale_banner/1"
        - "Threadline.OperatorSurface.UI.error_summary/1"
        - "Threadline.OperatorSurface.UI.ref/1"
    - path: "lib/threadline/operator_surface/unsupported.ex"
      provides: "Unsupported, unavailable, and export-denied descriptor copy"
      exports:
        - "Threadline.OperatorSurface.Unsupported.descriptor/1"
        - "Threadline.OperatorSurface.Unsupported.export_denied_descriptor/1"
    - path: "lib/threadline/operator_surface/components/unsupported_view.ex"
      provides: "Rendered unsupported descriptor surface"
      contains: "Threadline.OperatorSurface.Components.UnsupportedView.unsupported_view/1"
    - path: "lib/threadline/operator_surface/presentation.ex"
      provides: "Shared status labels and allowed evidence verdict labels"
      exports:
        - "Threadline.OperatorSurface.Presentation.status_label/1"
  key_links:
    - from: "lib/threadline/operator_surface/ui.ex"
      to: "test/threadline/operator_surface/copy_contract_test.exs"
      via: "state/error/copy affordance rendered assertions created by 179-01"
      pattern: "data_state|stale_banner|error_summary|data-tl-copy"
    - from: "lib/threadline/operator_surface/unsupported.ex"
      to: "lib/threadline/operator_surface/components/unsupported_view.ex"
      via: "descriptor struct rendered by unsupported_view/1"
      pattern: "Unsupported\\.descriptor"
    - from: "lib/threadline/operator_surface/presentation.ex"
      to: "test/threadline/operator_surface/copy_contract_test.exs"
      via: "status/evidence verdict label assertions"
      pattern: "status_label"
---

<objective>
Normalize shared state grammar, unsupported descriptors, validation summary behavior, and helper-level domain labels after the Phase 179 guard tests exist.

Purpose: COPY-01 and COPY-02 require repeated state patterns to be precise before page-specific copy is swept, and COPY-03 requires copy helpers to preserve current workflows and full-value copy affordances.
Output: updated shared helper copy, unsupported/unavailable descriptors, status-label handling, and targeted ExUnit assertions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/179-microcopy-information-architecture-sweep/179-CONTEXT.md
@.planning/phases/179-microcopy-information-architecture-sweep/179-RESEARCH.md
@.planning/phases/179-microcopy-information-architecture-sweep/179-VALIDATION.md
@brandbook/brand-book.md
@prompts/audit-lib-domain-model-reference.md
@.planning/phases/179-microcopy-information-architecture-sweep/179-01-SUMMARY.md
</context>

## Artifacts this phase produces

- No new runtime module is created in this plan.
- Existing helper surfaces expected to carry the controlled state templates: `UI.data_state/1`, `UI.empty_state/1`, `UI.error_state/1`, `UI.stale_banner/1`, `UI.error_summary/1`, `Unsupported.descriptor/1`, `Unsupported.export_denied_descriptor/1`, and `Presentation.status_label/1`.
- `UI.stale_banner/1` may add an internal optional object label attr if needed to render D-08 object-specific stale copy while preserving existing call sites.
- No new dependency, public API, route, LiveComponent, CSS class contract, CLI flag, copy DSL, CMS, or i18n layer is created.

<source_audit>
## Multi-Source Coverage Audit

| Source item | Plan coverage |
|-------------|---------------|
| GOAL: brand voice and domain language standards | Shared helpers receive controlled templates and canonical helper-level terms. |
| REQ COPY-01 | D-06, D-08, and D-09 state templates are implemented in shared helpers and unsupported views. |
| REQ COPY-02 | D-04 and D-05 terminology is applied to status labels and helper-rendered state copy. |
| REQ COPY-03 | D-10 and D-15 preserve full-value copy targets and existing helper boundaries. |
| RESEARCH: use existing helpers/components and resolve `UI.error_summary/1` focus behavior | This plan edits `UI.*`, `Unsupported.*`, and `Presentation.*` rather than adding a copy registry or new component layer, and it verifies focusable linked error summaries. |
| CONTEXT decisions | D-04, D-05, D-06, D-07, D-08, D-09, D-10, and D-15 are implemented here; D-01 through D-03 are covered by 179-01; D-11 through D-17 are covered by 179-04 through 179-06. |
| Deferred ideas | Not planned: i18n/copy DSL, new capabilities, new filters, new export flows, novice/expert mode, or coverage-schema card declutter. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize shared state and unsupported templates</name>
  <files>lib/threadline/operator_surface/ui.ex, lib/threadline/operator_surface/unsupported.ex, lib/threadline/operator_surface/components/unsupported_view.ex, lib/threadline/operator_surface/presentation.ex, test/threadline/operator_surface/ui_test.exs, test/threadline/operator_surface/data_state_mapping_wave0_test.exs</files>
  <behavior>
    - Shared state helpers render sentence-case, object-specific copy for no-data, permission, unavailable, redacted, pruned, stale, warning, success, and generic error states.
    - ARIA roles match D-09 severity rules and validation summaries are focusable with links to invalid fields.
    - Reference rendering preserves full exact `data-tl-copy` values while visible IDs may remain truncated.
  </behavior>
  <read_first>
    - .planning/phases/179-microcopy-information-architecture-sweep/179-CONTEXT.md
    - .planning/phases/179-microcopy-information-architecture-sweep/179-RESEARCH.md
    - .planning/phases/179-microcopy-information-architecture-sweep/179-VALIDATION.md
    - brandbook/brand-book.md
    - prompts/audit-lib-domain-model-reference.md
    - lib/threadline/operator_surface/ui.ex
    - lib/threadline/operator_surface/unsupported.ex
    - lib/threadline/operator_surface/components/unsupported_view.ex
    - lib/threadline/operator_surface/presentation.ex
    - test/threadline/operator_surface/ui_test.exs
    - test/threadline/operator_surface/data_state_mapping_wave0_test.exs
    - test/threadline/operator_surface/copy_contract_test.exs
  </read_first>
  <action>Implement the controlled state grammar in existing helpers per D-04, D-06, D-07, D-08, D-09, D-10, and D-15. Keep `UI.ref/1` copy targets unchanged. Make `UI.stale_banner/1` render object-specific copy with the D-08 stale template shape and a default object label for existing callers. Make `UI.data_state/1`, `UI.empty_state/1`, `UI.error_state/1`, and `UI.error_summary/1` state what happened, name the object, provide the next action, and avoid blame; permission and unavailable variants must remain distinct from no-data, redacted, and pruned variants. Add focusability and field-link verification to validation summaries without changing their public assign names. Update `Unsupported.descriptor/1`, `Unsupported.export_denied_descriptor/1`, and `UnsupportedView.unsupported_view/1` to sentence-case unavailable/permission copy and keep existing descriptor shapes. Keep `Presentation.status_label/1` evidence verdict labels allowed by D-05, but avoid arbitrary title-case fallback text in visible UI. Update tests to assert roles, copy, focus target attributes, and unchanged full-value copy affordances.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/ui_test.exs test/threadline/operator_surface/data_state_mapping_wave0_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `UI.data_state/1` distinguishes no-data, permission, unavailable, redacted, pruned, stale, and generic error states with sentence-case copy and next actions.
    - Permission and unavailable rendered states use alert semantics; routine empty/no-data/stale/success/progress states use status semantics unless an existing stricter role already applies.
    - `UI.error_summary/1` renders a focusable validation summary with links to invalid fields.
    - Unsupported descriptors render sentence-case titles and bodies, including export access denial, without changing descriptor structs or route behavior.
    - `UI.ref/1` and related reference rendering still expose full exact values in `data-tl-copy`.
  </acceptance_criteria>
  <done>Shared state, unsupported, and presentation helpers enforce Phase 179 copy templates while preserving existing helper boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit data to rendered state copy | Database, coverage, redaction, retention, and permission states are interpreted by operators through UI wording. |
| Operator input to validation summary | Invalid params and form fields cross into validation/error copy. |
| Rendered reference to clipboard | Full IDs cross from server-rendered attrs into copy affordances. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-04 | Information Disclosure | Permission/unavailable/redacted/pruned states | mitigate | Keep states distinct and avoid implying missing data when access, source availability, policy, or retention is the cause. |
| T-179-05 | Denial of Service | Validation summaries | mitigate | Focusable summaries with links reduce repeated invalid submissions while preserving existing forms. |
| T-179-06 | Tampering | Copy affordances | mitigate | Tests assert `data-tl-copy` full values remain unchanged when visible refs are truncated. |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks exist; RESEARCH marks package legitimacy audit not applicable. |
</threat_model>

<verification>
Plan-local verification:
- `mix test test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/ui_test.exs test/threadline/operator_surface/data_state_mapping_wave0_test.exs`

Phase validation commands carried forward from `179-VALIDATION.md`:
- `mix test test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/live`
- `mix ci.all` before verify-work.
</verification>

<success_criteria>
Shared state helpers follow the Phase 179 state grammar, domain language, ARIA severity rules, and focusable validation summary contract; copy affordances retain full values; no new runtime dependencies, public APIs, routes, LiveComponents, or capabilities are introduced.
</success_criteria>

<output>
Create `.planning/phases/179-microcopy-information-architecture-sweep/179-02-SUMMARY.md` when done.
</output>
