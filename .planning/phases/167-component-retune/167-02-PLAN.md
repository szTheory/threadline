---
phase: 167-component-retune
plan: 02
type: execute
wave: 2
depends_on: ["167-01"]
files_modified:
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/style_contract_test.exs
autonomous: false
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "Every family the live review proved override-needed has an additive light override authored in BOTH the [data-tl-theme=\"light\"] lane AND the mirrored @media (prefers-color-scheme: light) [data-tl-theme=\"system\"] branch"
    - "No family that passed the review has an override (confirm-strict, D-03)"
    - "No tint-rider (chips/alerts/timeline-facts/op-badges/redaction/policy/job) has a per-component [data-tl-theme=\"light\"] override (TOKEN-02 invariant)"
    - "Dark token values and base .tl-* rules are byte-stable (frozen-hex assertions stay green)"
    - "The contract test asserts D-07(a) authored-override presence (dual-branch) and D-07(b) tint-rider class-qualified absence"
  artifacts:
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "Additive dual-branch light overrides for the proven-fail families only (or no new override if fail-list empty)"
      contains: "data-tl-theme=\"light\""
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "D-07(a) authored-override-present + D-07(b) tint-rider absence assertions"
      contains: "data-tl-theme"
  key_links:
    - from: "test/threadline/operator_surface/style_contract_test.exs (D-07b)"
      to: "tint-rider classes (tl-chip, tl-alert, tl-timeline-fact, tl-change__op, tl-redaction, tl-policy, tl-job)"
      via: "Regex.match? class-qualified [data-tl-theme=\"light\"] absence (NOT bare absence)"
      pattern: "\\\\.tl-chip[^{]*\\\\[data-tl-theme"
    - from: "style.ex authored override selector"
      to: "both light lane and system @media branch"
      via: "dual-branch presence assertion (D-07a)"
      pattern: "prefers-color-scheme: light"
---

<objective>
Author the additive light overrides for ONLY the families the Plan 01 review proved fail (confirm-strict, D-03), each edited in both the `[data-tl-theme="light"]` lane and the mirrored `@media (prefers-color-scheme: light) [data-tl-theme="system"]` branch in the same task (166 D-03), AND extend `style_contract_test.exs` in the SAME wave (source-first amendment, 166 D-05) to prove D-07(a) authored-override presence and D-07(b) tint-rider class-qualified absence.

Purpose: Closes COMP-01 (dark-effect families render correctly on white via designed treatments) and the COMP-01/TOKEN-02 invariant (tint-riders carry no per-component override) with a CI guard. The dark base stays byte-stable (Hard Constraint 3).
Output: Additive override blocks in `style.ex` (only for proven fails — possibly none) + two new contract-test assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-component-retune/167-UI-SPEC.md
@.planning/phases/167-component-retune/167-RESEARCH.md
@.planning/phases/167-component-retune/167-VALIDATION.md
@.planning/phases/167-component-retune/LIGHT-REVIEW.md
@.planning/phases/167-component-retune/167-01-SUMMARY.md

<interfaces>
<!-- The existing light lane + system branch already define every token (style.ex:187-235 / :237-287). -->
<!-- Overrides reference tokens, never literals. New blocks are additive; never touch the base .threadline-ui block (style.ex:19-185). -->

Override mechanism (dual-branch, both edited in the same task — 166 D-03):
  Branch A:  .threadline-ui[data-tl-theme="light"] { --tl-...: <value>; }
  Branch B:  @media (prefers-color-scheme: light) { .threadline-ui[data-tl-theme="system"] { --tl-...: <value>; } }

Bounded alpha-tuning targets (AUTONOMOUS per D-04 — adjust existing token values, still additive):
  glass:  --tl-color-surface-tint (currently rgba(255,255,255,0.92))  → lower alpha if #1/#2 glass collapses to flat white
  scrim:  --tl-color-backdrop (currently rgba(15,23,40,0.42))         → bump alpha if #5 scrim too weak
  divider promotion: --tl-color-border (#C9D3E2) → --tl-color-border-strong (#A7B4C8) on a surface, per data-viz override path

NEW-TOKEN halt (D-05): a new hue, a non-blur primitive, or any value not derivable from the 45-token lane (style.ex:187-235) → DO NOT author. HALT and FLAG for a user decision. Blur radii may reuse --tl-blur-panel (8px) / --tl-blur-veil (1px) at style.ex:168-169.

Tint-rider classes for D-07(b) absence assertion (167-RESEARCH §Code Examples):
  tl-chip, tl-alert, tl-timeline-fact, tl-change__op, tl-redaction, tl-policy, tl-job

Existing contract-test helpers (test/threadline/operator_surface/style_contract_test.exs):
  @style_path "lib/threadline/operator_surface/style.ex"   File.read!(@style_path)
  selector_block!(section, selector)  — extracts a selector's declaration block
  String.contains?/2, refute, Regex.match?/2  — used at lines 103/134/180
  Existing dark byte-stability: frozen-hex assertions (lines 787-812); theme-toggle ban (lines 28, 837)
  Baseline: grep -c 'data-tl-theme="light"' style.ex == 1 (the lane root at :187) — D-07(b) must match the attribute QUALIFIED by a rider class, not bare absence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author additive dual-branch overrides for the proven-fail families only</name>
  <read_first>
    - .planning/phases/167-component-retune/LIGHT-REVIEW.md (the fail-list — the ONLY input; author overrides only for rows marked `override-needed`)
    - .planning/phases/167-component-retune/167-UI-SPEC.md (per-family "Additive light-lane treatment" column for the exact token/value to override; §Data-Viz override path for divider promotion)
    - .planning/phases/167-component-retune/167-RESEARCH.md (§Pattern 1 dual-branch shape; §Anti-Patterns; Pitfall 2 mirror-omission, Pitfall 5 byte-stability)
    - lib/threadline/operator_surface/style.ex:187-287 (the light lane + system branch — append additive overrides here, never edit the base block :19-185)
  </read_first>
  <action>
    For EACH family/data-viz surface marked `override-needed` in `LIGHT-REVIEW.md` (and ONLY those — confirm-strict D-03; if the fail-list is empty, author NOTHING in style.ex and proceed to Task 2):

    Author an additive override that adjusts the existing token value per the UI-SPEC "Additive light-lane treatment" column for that family. Write it TWICE in the same task — once under `.threadline-ui[data-tl-theme="light"]` (near style.ex:187-235) and once under the mirrored `@media (prefers-color-scheme: light) { .threadline-ui[data-tl-theme="system"] { ... } }` (near :237-287). The two edits are atomic (166 D-03; RESEARCH Pitfall 2). Reference tokens/values from the 45-token lane only — e.g. lower `--tl-color-surface-tint` alpha for #1/#2 glass, bump `--tl-color-backdrop` alpha for #5 scrim, or promote a divider to `--tl-color-border-strong` for a data-viz surface. These alpha tunings are AUTONOMOUS (D-04).

    DO NOT touch the base `.threadline-ui {` block (:19-185), any base `.tl-*` rule, or any dark token value (Hard Constraint 3 — frozen-hex tests guard this). DO NOT pre-author overrides for families that passed.

    If any required value is a genuinely NEW token (new hue, non-blur primitive, or not derivable from the existing lane) → HALT and FLAG for a user decision (D-05); do not invent it. (This is why the plan is `autonomous: false`.)

    Run `mix format` on style.ex after editing.
  </action>
  <verify>
    <automated>mix format --check-formatted lib/threadline/operator_surface/style.ex && grep -v '^#' lib/threadline/operator_surface/style.ex | grep -c 'data-tl-theme="light"'</automated>
  </verify>
  <acceptance_criteria>
    - Every `override-needed` family from `LIGHT-REVIEW.md` has an override present in BOTH branches (grep the overridden token name returns it in the light lane and again inside the `@media (prefers-color-scheme: light)` block).
    - No family marked `pass` has a new override.
    - No diff line falls inside the base `.threadline-ui {` block (:19-185) or changes a frozen dark hex.
    - `mix format --check-formatted` passes on style.ex.
    - If fail-list was empty: style.ex is unchanged and the SUMMARY says so.
  </acceptance_criteria>
  <done>Additive dual-branch overrides exist for exactly the proven-fail set (or none); dark base untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend style_contract_test.exs with D-07(a) presence + D-07(b) tint-rider absence</name>
  <read_first>
    - test/threadline/operator_surface/style_contract_test.exs:1-30 (header, @style_path, existing theme-aware lane test + theme-toggle ban) and :971-1006 (selector_block! and helper vocabulary) and :787-812 (frozen-hex assertions — do not alter)
    - .planning/phases/167-component-retune/167-RESEARCH.md (§Code Examples — the exact D-07a and D-07b assertion shapes; Pitfall 3 — class-qualified regex, NOT bare absence, because the lane root occurs once legitimately)
    - .planning/phases/167-component-retune/LIGHT-REVIEW.md (to know which override selectors/tokens to assert present in D-07a)
    - lib/threadline/operator_surface/style.ex (the source the test reads as a string)
  </read_first>
  <behavior>
    - D-07(b): for each rider class in `tl-chip, tl-alert, tl-timeline-fact, tl-change__op, tl-redaction, tl-policy, tl-job`, assert NO `[data-tl-theme="light"]` selector QUALIFIED by that class exists (Regex `~r/\.#{klass}[A-Za-z0-9_-]*[^{]*\[data-tl-theme="light"\]/` must NOT match). Starts green on current source (baseline: only the lane root carries the attribute). Validate the assertion by mentally/scratch-adding a per-component override — it must go red.
    - D-07(a): for each token actually overridden in Task 1 (driven by the fail-list), assert it appears in BOTH the light lane block AND the system `@media (prefers-color-scheme: light)` block. If the fail-list was empty, this assertion is a guarded no-op (skip-with-comment) so it cannot false-fail.
    - Existing frozen-hex + theme-toggle assertions remain unchanged and green.
  </behavior>
  <action>
    Add two new tests to `style_contract_test.exs` reusing the existing pattern (`File.read!(@style_path)`, `selector_block!`, `String.contains?`, `refute`, `Regex.match?`). For D-07(a), split the source on the light-lane selector and the `@media (prefers-color-scheme: light)` boundary (mirror the existing lane-test approach at lines 8-29) and assert each Task-1-overridden token name appears in both halves — gate the body on the fail-list so an empty fail-list is a documented no-op, not a failure. For D-07(b), loop the seven rider classes and `refute Regex.match?` the class-qualified attribute pattern (RESEARCH §Code Examples verbatim shape). Add a descriptive message per refute. Do not touch the frozen-hex block or the theme-toggle refute. Run `mix format` on the test.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/style_contract_test.exs` exits 0.
    - The D-07(b) test refutes a class-qualified `[data-tl-theme="light"]` for all seven rider classes (NOT bare absence of the attribute).
    - The D-07(a) test asserts each overridden token in both branches (or is a documented no-op if fail-list empty).
    - Existing frozen-hex and theme-toggle assertions still pass.
    - `mix format --check-formatted` passes on the test file.
  </acceptance_criteria>
  <done>The contract test enforces COMP-01 authored-override presence and the TOKEN-02 tint-rider invariant; full file green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite verification + scoped commit</name>
  <read_first>
    - CLAUDE.md (§CI & Verification Conventions — cite `mix verify.test` / `mix verify.format` / `mix ci.all`; §GSD positional-args)
    - .planning/phases/167-component-retune/167-RESEARCH.md (Pitfall 4 — scoped commit only)
  </read_first>
  <action>
    Run `mix verify.format` and `mix verify.test` (full suite — confirms no regression and dark byte-stability holds). Then `mix ci.all` as the phase gate signal. Commit ONLY the two changed source files with explicit paths: `lib/threadline/operator_surface/style.ex` and `test/threadline/operator_surface/style_contract_test.exs` (never `git add -A` — protects the uncommitted nav-overhaul lane). Commit message: `feat(167): additive light overrides for proven-fail families + contract guard`. If the fail-list was empty, commit only the contract-test file with message `test(167): tint-rider light-override absence guard`.
  </action>
  <verify>
    <automated>mix verify.format && mix verify.test</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.format` and `mix verify.test` both green.
    - `mix ci.all` green (phase gate).
    - Commit touches only style.ex and/or the contract test (`git log` confirms; no example-app/nav files).
  </acceptance_criteria>
  <done>Source changes verified and committed in scope; phase gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS custom-property value edits + string-assertion tests. No input handling, auth, crypto, data flow, or network surface (167-RESEARCH §Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-03 | Tampering | frozen dark catalog / base `.tl-*` rules | mitigate | Overrides are additive `[data-tl-theme]` blocks only; base block (:19-185) never edited. Existing frozen-hex assertions (style_contract_test.exs:787-812) run in Task 3 `mix verify.test` and go red on any dark mutation. |
| T-167-04 | Tampering | uncommitted nav-overhaul lane (~29 files) | mitigate | Scoped commit with explicit paths only (style.ex + contract test); never `git add -A` (RESEARCH Pitfall 4). Task 3 acceptance asserts the commit touches only the two files. |
| T-167-05 | Tampering | TOKEN-02 invariant (tint-riders ride shared tokens) | mitigate | D-07(b) class-qualified absence assertion is the CI guard; a future per-component tint-rider override turns the test red. |

No high-severity threats. ASVS L1 input/auth/crypto categories do not apply; the only invariant at risk is regression of the frozen catalog, guarded by existing + new contract tests.
</threat_model>

<verification>
- `mix verify.test` full suite green (dark byte-stability + theme-toggle ban intact).
- `mix verify.format` green.
- `mix ci.all` green (phase gate).
- D-07(a) presence + D-07(b) class-qualified absence assertions pass.
- Overrides exist in BOTH branches for every proven-fail family and NONE for passing families.
</verification>

<success_criteria>
- COMP-01: proven-fail dark-effect families have additive, dual-branch, token-anchored light overrides; passing families have none (confirm-strict).
- COMP-01/TOKEN-02: no tint-rider carries a per-component light override; the contract test enforces it.
- Dark mode visually unchanged (additive lane only; frozen-hex tests green) — ROADMAP success criterion 4.
</success_criteria>

<output>
Create `.planning/phases/167-component-retune/167-02-SUMMARY.md` when done. Record which families got overrides (or "none — all confirm-only"), the exact tokens tuned, and confirmation that both branches were edited.
</output>
