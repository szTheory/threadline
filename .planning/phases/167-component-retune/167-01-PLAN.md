---
phase: 167-component-retune
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/167-component-retune/LIGHT-REVIEW.md
autonomous: false
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "A reviewer has eyeballed the operator surface live in :dark, :light, AND :system modes"
    - "Each of the 9 dark-effect families has a recorded pass | override-needed disposition"
    - "Each of the 3 data-viz surfaces (coverage/timeline/diff) has a recorded pass | override-needed outcome per named UI-SPEC criterion"
    - "The flagged trio (#1 glass topbar, #5 drawer scrim, #8 home-card signal-line) received explicit recorded attention"
    - "The proven fail-list (the set of override-needed families) is the committed input to override authoring"
  artifacts:
    - path: ".planning/phases/167-component-retune/LIGHT-REVIEW.md"
      provides: "Per-family disposition table (9 families) + per-data-viz-surface named-criteria outcomes (3 surfaces); the committed human-judgment fail-list (D-08)"
      contains: "override-needed"
  key_links:
    - from: "LIGHT-REVIEW.md disposition rows"
      to: "167-02-PLAN.md override tasks"
      via: "fail-list drives which overrides are authored (D-03 confirm-strict)"
      pattern: "override-needed|pass"
---

<objective>
Run the review-first gate (D-01/D-02): render the Phase 166 light/system token-recolor live and produce the committed disposition fail-list that determines which override tasks exist in Plan 02. This is an EARLY GATE, not an end check — its output is the input to all override authoring.

Purpose: Satisfies the STATE human gate ("eyeball the rendered light surface before retune effort is spent") and the UI-SPEC confirm-first default. You cannot know which of the 9 families fail (most likely #1/#5/#8) without looking. Also delivers the COMP-02 data-viz design review as recorded human judgment ("not just a passing test").
Output: `LIGHT-REVIEW.md` in the phase dir with explicit `pass | override-needed` dispositions for all 9 dark-effect families and named-criteria outcomes for the 3 data-viz surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-component-retune/167-UI-SPEC.md
@.planning/phases/167-component-retune/167-RESEARCH.md
@.planning/phases/167-component-retune/167-VALIDATION.md

<interfaces>
<!-- Source-of-truth anchors the reviewer needs. All file:line VERIFIED in 167-RESEARCH.md. -->
<!-- The light lane and system branch already exist (Phase 166); this gate does NOT edit style.ex. -->

The 9 dark-effect families (COMP-01) — anchors in lib/threadline/operator_surface/style.ex:
  #1 Glass topbar           — .tl-topbar (style.ex:384), rides --tl-color-surface-tint + --tl-blur-panel + border   [FLAGGED]
  #2 Glass shell-nav        — .tl-shell-nav (style.ex:470), shares --tl-color-surface-tint with #1
  #3 Glass toolbar          — .tl-toolbar (style.ex:966), rides --tl-color-surface-tint-strong + shadow-subtle + blur
  #4 Glass coverage-command + subview header — .tl-coverage-command (style.ex:3047), .tl-subview__header (style.ex:2748)
  #5 Drawer scrim           — .tl-subview-backdrop (style.ex:2722), rides --tl-color-backdrop + --tl-blur-veil       [FLAGGED — most likely to fail]
  #6 Drawer shadow          — .tl-subview box-shadow var(--tl-shadow-raised) (style.ex:2740)
  #7 Focus glow             — --tl-focus-ring (style.ex:156) + :focus-visible (style.ex:322); chip dot glow (style.ex:1539, currentColor)
  #8 Home-card signature     — .tl-home__card--primary (style.ex:681) + ::before thread line (style.ex:692)          [FLAGGED — designed inversion]
  #9 Shell-nav active inset  — .tl-shell-nav__item--active (style.ex:588), rides --tl-color-accent-inset (un-stray-ed in 166)

Light-lane token values (already present, style.ex:187-235) the families resolve through:
  --tl-color-surface-tint: rgba(255,255,255,0.92)   --tl-color-surface-tint-strong: rgba(247,249,252,0.96)
  --tl-color-backdrop: rgba(15,23,40,0.42)          --tl-shadow-raised: 0 18px 48px rgba(15,23,40,0.24)
  --tl-shadow-subtle: 0 1px 2px rgba(15,23,40,0.08), 0 1px 3px rgba(15,23,40,0.06)
  --tl-color-accent-wash: rgba(21,87,192,0.06)      --tl-color-signal: #0F8F85
  --tl-color-accent-inset: rgba(21,87,192,0.16)     --tl-focus-ring: 0 0 0 3px rgba(21,87,192,0.22), 0 0 0 1px #1557C0

The 3 data-viz surfaces (COMP-02) — anchors + named criteria are in 167-UI-SPEC.md §Data-Viz and 167-RESEARCH.md §Data-Viz Review Criteria:
  Coverage matrix/command — .tl-coverage-command (style.ex:3047)
  Timeline                — .tl-timeline-command (style.ex:980), .tl-timeline-fact (style.ex:1022)
  Diff                    — .tl-diff (style.ex:2262), .tl-diff__arrow (style.ex:2277)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold LIGHT-REVIEW.md with the disposition tables (Wave 0 artifact)</name>
  <files>.planning/phases/167-component-retune/LIGHT-REVIEW.md</files>
  <read_first>
    - .planning/phases/167-component-retune/167-UI-SPEC.md (§Component Retune Contract table for the 9 families; §Data-Viz Design-Review Contract for the 3 surfaces and their named criteria)
    - .planning/phases/167-component-retune/167-RESEARCH.md (§Family Source-Anchor Inventory; §Data-Viz Review Criteria; Open Question 1 recommends a table with explicit `pass | override-needed` cells)
    - .planning/phases/167-component-retune/167-VALIDATION.md (§Wave 0 Requirements — this artifact IS the proven fail-list)
  </read_first>
  <action>
    Create `.planning/phases/167-component-retune/LIGHT-REVIEW.md` as the committed human-judgment artifact (D-08). Include frontmatter recording phase 167 and a `reviewed_at` date placeholder. Build two empty-but-fully-rowed tables:

    (a) A "Dark-Effect Families (COMP-01)" table with one row per family #1–#9 using the exact names/anchors from the §interfaces block, columns: `#`, `Family`, `Source anchor`, `Designed light intent (from UI-SPEC)`, `Disposition (pass | override-needed)`, `Notes`. Pre-fill the intent column from the UI-SPEC per-family table. Leave Disposition cells blank (filled by the reviewer in Task 2). Mark families #1, #5, #8 with a `FLAGGED` tag in Notes so they get explicit attention.

    (b) A "Data-Viz Surfaces (COMP-02)" table with one ROW PER NAMED CRITERION for each of the 3 surfaces (coverage / timeline / diff), columns: `Surface`, `Criterion (from UI-SPEC)`, `Outcome (pass | override-needed)`, `Notes`. Pre-fill the Criterion column verbatim from the UI-SPEC §Data-Viz named criteria so no criterion is forgotten. Leave Outcome cells blank.

    Add a short header note stating: dispositions are filled by the live 3-mode review in Task 2; the set of `override-needed` rows is the fail-list that drives Plan 02; any value not derivable from the 45-token lane must be FLAGGED (D-05), not invented. Do NOT pre-judge any disposition. Do NOT edit style.ex in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/167-component-retune/LIGHT-REVIEW.md && grep -c 'override-needed' .planning/phases/167-component-retune/LIGHT-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `LIGHT-REVIEW.md` exists in the phase dir.
    - The families table has exactly 9 family rows (#1–#9) with anchors matching the §interfaces block.
    - The data-viz table has one row per named criterion for each of coverage/timeline/diff (criteria copied verbatim from UI-SPEC).
    - Disposition/Outcome cells are present (blank) for every row; the string `override-needed` appears at least in the column headers / legend.
    - `git status` shows only `LIGHT-REVIEW.md` added (no style.ex, no example-app, no nav-overhaul files).
  </acceptance_criteria>
  <done>Fully-rowed disposition tables exist and are committed-ready; reviewer can fill them without re-deriving the family/criterion list.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live 3-mode review — fill the disposition fail-list</name>
  <files>.planning/phases/167-component-retune/LIGHT-REVIEW.md</files>
  <read_first>
    - .planning/phases/167-component-retune/167-UI-SPEC.md (per-family light intent; data-viz named criteria)
    - .planning/phases/167-component-retune/167-RESEARCH.md (§Common Pitfalls — esp. Pitfall 4: do NOT disturb the uncommitted nav-overhaul lane; §Environment Availability for the render path)
    - .planning/phases/167-component-retune/LIGHT-REVIEW.md (the tables to fill)
  </read_first>
  <action>
    Blocking human-verify checkpoint — the reviewer eyeballs the live render and supplies the `pass | override-needed` verdicts. Claude's role: (a) start the example-app render path, (b) walk the reviewer through `<how-to-verify>`, (c) record the reviewer's verdicts plus a one-line note into each Disposition/Outcome cell of `LIGHT-REVIEW.md`. The visual judgment itself cannot be automated (pixel/contrast checks defer to Phases 169/168). Do NOT edit style.ex; do NOT stage any example-app or nav-overhaul file (RESEARCH Pitfall 4).
  </action>
  <what-built>
    Task 1 produced the empty disposition tables. The operator surface itself already renders all three theme lanes (Phase 166 mechanism: base dark, `[data-tl-theme="light"]`, and `@media (prefers-color-scheme: light) [data-tl-theme="system"]`). No new render code was written. This checkpoint is the eyeball review that produces human judgment — it cannot be automated (pixel/contrast checks defer to Phases 169/168).
  </what-built>
  <how-to-verify>
    1. Start the example app: `cd examples/threadline_phoenix && mix phx.server`, then open `http://localhost:4000/audit`. This renders the default `:dark` lane — confirm dark is unchanged (baseline).
    2. Render `:light`: use the non-committed path. Primary path = browser DevTools "Rendering → Emulate prefers-color-scheme: light" exercises the `:system` `@media` branch with ZERO source edits. If a direct `:light` mount is needed, make a LOCAL, un-staged, reverted-after edit to the example router `theme:` opt and stage nothing — see RESEARCH Pitfall 4. Never stage example-app or nav-overhaul files.
    3. Walk all 9 families on white (topbar, shell-nav, toolbar, coverage-command + subview header, drawer scrim, drawer shadow, focus glow, home-card signature, shell-nav active inset). For each, judge against the UI-SPEC "Designed light intent" column.
    4. Give EXPLICIT attention to the flagged trio: #5 drawer scrim (does `rgba(15,23,40,0.42)` dim the page enough to read as "behind/inactive"?), #1 glass topbar (is the frosted glass legibly distinct from the white page on scroll, or does it collapse to flat white?), #8 home-card signal-line (does the `#0F8F85` teal thread line read as a deliberate signal on white?).
    5. Walk the 3 data-viz surfaces (coverage / timeline / diff) against each named criterion in the data-viz table.
    6. Verify `:system` path explicitly via DevTools prefers-color-scheme emulation — it mirrors `:light` values but is a distinct render branch (UI-SPEC §Specifics requires all three reviewed).
    7. Fill every Disposition/Outcome cell in `LIGHT-REVIEW.md` with `pass` or `override-needed` plus a one-line note. For any `override-needed`, note whether the fix is alpha-tuning an existing token (autonomous in Plan 02 per D-04) or appears to need a NEW token (must be FLAGGED per D-05).
    8. Confirm `git status` shows no example-app or nav-overhaul files staged.
  </how-to-verify>
  <verify>
    <human-check>Every family (#1–#9) and every data-viz criterion has a recorded `pass | override-needed` disposition; flagged trio has explicit notes; all three modes (:dark/:light/:system) reviewed; no example-app/nav files staged.</human-check>
  </verify>
  <acceptance_criteria>
    - Every family row (#1–#9) has a `pass` or `override-needed` disposition.
    - Every data-viz criterion row has a `pass` or `override-needed` outcome.
    - The flagged trio has explicit notes recording the judgment.
    - `:dark`, `:light`, and `:system` were all reviewed (the review note states this).
    - No example-app / nav-overhaul files staged (`git status` clean of them).
  </acceptance_criteria>
  <done>Every disposition cell recorded with a verdict and note; the fail-list is determined.</done>
  <resume-signal>Type "approved" once all dispositions are recorded, or describe what still needs review.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit LIGHT-REVIEW.md and emit the fail-list for Plan 02</name>
  <files>.planning/phases/167-component-retune/LIGHT-REVIEW.md</files>
  <read_first>
    - .planning/phases/167-component-retune/LIGHT-REVIEW.md (now fully filled)
    - CLAUDE.md (§GSD / local planning; verification entrypoints)
  </read_first>
  <action>
    Commit ONLY `.planning/phases/167-component-retune/LIGHT-REVIEW.md` (explicit path; never `git add -A` — protects the uncommitted nav-overhaul lane per RESEARCH Pitfall 4). Use commit message `docs(167): record light-lane review dispositions`. In the plan SUMMARY, list the exact set of `override-needed` families and data-viz surfaces — this is the proven fail-list that Plan 02 consumes to decide which override tasks to author. If the fail-list is empty (all confirm-only), state that explicitly: Plan 02 then authors NO style.ex overrides and only extends the contract test with the D-07(b) tint-rider absence guard.
  </action>
  <verify>
    <automated>git log --oneline -1 -- .planning/phases/167-component-retune/LIGHT-REVIEW.md | grep -q . && echo committed</automated>
  </verify>
  <acceptance_criteria>
    - `LIGHT-REVIEW.md` is committed (git log shows the commit touching only that file).
    - The SUMMARY enumerates the override-needed fail-list (or explicitly states "empty — all confirm-only").
    - No other files committed alongside it.
  </acceptance_criteria>
  <done>The committed review checklist exists with all dispositions; Plan 02 has its driving input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a Markdown artifact and runs a manual review. No input handling, auth, data flow, or network surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-01 | Tampering | uncommitted nav-overhaul lane (~29 files) | mitigate | Commit only the explicit `LIGHT-REVIEW.md` path; render `:light`/`:system` via DevTools emulation or a reverted local edit; never `git add -A`. Task 2/3 acceptance asserts `git status` is clean of example-app/nav files (RESEARCH Pitfall 4). |
| T-167-02 | Tampering | frozen dark catalog / base `.tl-*` rules | accept | This plan does not edit style.ex at all; no risk path. Recorded explicitly per planning guidance (CSS-only phase, no new attack surface). |

No high-severity threats. This is a Markdown-artifact + manual-review plan; ASVS L1 input/auth/crypto categories do not apply (167-RESEARCH §Security Domain).
</threat_model>

<verification>
- `mix verify.test` still green (no source changed; dark byte-stability intact).
- `LIGHT-REVIEW.md` committed with all 9 family dispositions and all 3 data-viz surface criteria recorded.
- `git status` shows no staged example-app or nav-overhaul files.
</verification>

<success_criteria>
- The review-first gate has run across `:dark` / `:light` / `:system` (D-01/D-02).
- Every family and every data-viz criterion has an explicit `pass | override-needed` disposition (D-08).
- The fail-list is committed and enumerated for Plan 02 to consume (confirm-strict input, D-03).
</success_criteria>

<output>
Create `.planning/phases/167-component-retune/167-01-SUMMARY.md` when done. The SUMMARY MUST enumerate the override-needed fail-list (families + data-viz surfaces), or state it is empty.
</output>
