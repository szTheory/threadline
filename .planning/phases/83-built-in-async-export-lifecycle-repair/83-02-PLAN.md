---
phase: 83-built-in-async-export-lifecycle-repair
plan: 02
type: execute
wave: 2
depends_on:
  - "83-01"
files_modified:
  - .planning/phases/78-async-exports-ui/78-VERIFICATION.md
  - .planning/phases/78-async-exports-ui/78-VALIDATION.md
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-04
must_haves:
  truths:
    - "Phase 78 has a current-tree verification artifact that proves the repaired built-in runtime path instead of relying on historical summary prose."
    - "Phase 78's validation artifact uses the current Nyquist closeout shape and maps EXP-01, EXP-02, and EXP-04 to concrete repaired-path checks."
    - "The closeout language stays within the built-in export runtime boundary and does not claim the Phase 84 adapter-backed delivery or S3/Oban runtime closure early."
  artifacts:
    - path: .planning/phases/78-async-exports-ui/78-VERIFICATION.md
      provides: "Current-tree verification report for built-in export runtime closure"
      contains: "EXP-01"
    - path: .planning/phases/78-async-exports-ui/78-VALIDATION.md
      provides: "Nyquist-shaped validation artifact for repaired built-in export lifecycle behavior"
      contains: "nyquist_compliant: true"
  key_links:
    - from: .planning/phases/78-async-exports-ui/78-VERIFICATION.md
      to: lib/threadline/application.ex
      via: "proof that the library starts the built-in export runtime by default"
      pattern: "TaskSupervisor|CleanupTask"
    - from: .planning/phases/78-async-exports-ui/78-VERIFICATION.md
      to: test/threadline/operator_surface/live/timeline_live_test.exs
      via: "proof that enqueue failures are surfaced and jobs do not stall in pending"
      pattern: "Background Export|failed|pending"
    - from: .planning/phases/78-async-exports-ui/78-VERIFICATION.md
      to: test/threadline/export/cleanup_test.exs
      via: "proof that terminal expiry cleanup is operational on the repaired tree"
      pattern: "expires_at|CleanupTask|running|completed|failed"
---

<objective>
Backfill the missing closeout evidence for Phase 78 using the repaired built-in export runtime truth produced by Plan 01.

Purpose: satisfy the evidence portion of EXP-01, EXP-02, and EXP-04 in the milestone-closeout format expected after the v1.20 audit.
Output: a real `78-VERIFICATION.md` plus a normalized Nyquist-shaped `78-VALIDATION.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.20-MILESTONE-AUDIT.md
@.planning/phases/83-built-in-async-export-lifecycle-repair/83-CONTEXT.md
@.planning/phases/83-built-in-async-export-lifecycle-repair/83-RESEARCH.md
@.planning/phases/83-built-in-async-export-lifecycle-repair/83-01-PLAN.md
@.planning/phases/78-async-exports-ui/78-01-SUMMARY.md
@.planning/phases/78-async-exports-ui/78-02-SUMMARY.md
@.planning/phases/78-async-exports-ui/78-03-SUMMARY.md
@.planning/phases/78-async-exports-ui/78-VALIDATION.md
@lib/threadline/application.ex
@lib/threadline/export_queue/task_adapter.ex
@lib/threadline/export/orchestrator.ex
@lib/threadline/export/cleanup_task.ex
@lib/threadline/operator_surface/live/timeline_live.ex
@lib/threadline/operator_surface/live/export_status_live.ex
@lib/threadline/operator_surface/controllers/export_controller.ex
@test/threadline/export_queue/task_adapter_test.exs
@test/threadline/export/orchestrator_test.exs
@test/threadline/export/cleanup_test.exs
@test/threadline/operator_surface/live/timeline_live_test.exs
@test/threadline/operator_surface/live/export_status_live_test.exs
@test/threadline/operator_surface/controllers/export_controller_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author a current-tree Phase 78 verification report for the repaired built-in runtime</name>
  <files>.planning/phases/78-async-exports-ui/78-VERIFICATION.md</files>
  <read_first>
    <file>.planning/phases/83-built-in-async-export-lifecycle-repair/83-01-PLAN.md</file>
    <file>.planning/phases/78-async-exports-ui/78-01-SUMMARY.md</file>
    <file>.planning/phases/78-async-exports-ui/78-02-SUMMARY.md</file>
    <file>.planning/phases/78-async-exports-ui/78-03-SUMMARY.md</file>
    <file>lib/threadline/application.ex</file>
    <file>lib/threadline/export_queue/task_adapter.ex</file>
    <file>lib/threadline/export/orchestrator.ex</file>
    <file>lib/threadline/export/cleanup_task.ex</file>
    <file>lib/threadline/operator_surface/live/timeline_live.ex</file>
    <file>test/threadline/export_queue/task_adapter_test.exs</file>
    <file>test/threadline/export/orchestrator_test.exs</file>
    <file>test/threadline/export/cleanup_test.exs</file>
    <file>test/threadline/operator_surface/live/timeline_live_test.exs</file>
    <file>test/threadline/operator_surface/live/export_status_live_test.exs</file>
  </read_first>
  <action>Create `78-VERIFICATION.md` in the repaired closeout style used by later audit-repair phases. The report must prove, on the current tree, that the library now starts the built-in export runtime by default, the background export request path surfaces enqueue failure truthfully, the orchestrator records `started_at` plus terminal expiry metadata, and `CleanupTask` removes expired terminal artifacts/jobs end to end. Keep the report scoped to EXP-01, EXP-02, and EXP-04 on the built-in path. Explicitly note that adapter-backed downloads, S3 compatibility, and full Oban runtime closure remain Phase 84 work so this artifact does not overclaim the milestone state.</action>
  <acceptance_criteria>
    <criterion>`.planning/phases/78-async-exports-ui/78-VERIFICATION.md` contains `EXP-01`, `EXP-02`, and `EXP-04` in its requirements coverage section.</criterion>
    <criterion>`.planning/phases/78-async-exports-ui/78-VERIFICATION.md` contains an `Observable Truths` section naming `TaskSupervisor`, enqueue failure truth, lifecycle timestamps, and `CleanupTask`.</criterion>
    <criterion>`.planning/phases/78-async-exports-ui/78-VERIFICATION.md` explicitly states that Phase 84 still owns adapter-backed delivery and runtime closure.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'EXP-01|EXP-02|EXP-04|Observable Truths|TaskSupervisor|CleanupTask|Phase 84' .planning/phases/78-async-exports-ui/78-VERIFICATION.md</automated>
  </verify>
  <done>Phase 78 has a first-class verification artifact that closes the audit gap on the repaired built-in export runtime path.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize `78-VALIDATION.md` into Nyquist closeout shape for the repaired lifecycle</name>
  <files>.planning/phases/78-async-exports-ui/78-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/78-async-exports-ui/78-VALIDATION.md</file>
    <file>.planning/phases/83-built-in-async-export-lifecycle-repair/83-VALIDATION.md</file>
    <file>.planning/phases/76-batched-retention-and-ui/76-VALIDATION.md</file>
    <file>.planning/phases/77-saved-views-ergonomics/77-VALIDATION.md</file>
    <file>test/threadline/export_queue/task_adapter_test.exs</file>
    <file>test/threadline/export/orchestrator_test.exs</file>
    <file>test/threadline/export/cleanup_test.exs</file>
    <file>test/threadline/operator_surface/live/timeline_live_test.exs</file>
    <file>test/threadline/operator_surface/live/export_status_live_test.exs</file>
    <file>test/threadline/operator_surface/controllers/export_controller_test.exs</file>
  </read_first>
  <action>Rewrite `78-VALIDATION.md` into the current Nyquist format used by repaired closeout phases: frontmatter with `nyquist_compliant: true`, explicit sampling guidance, a per-task verification map, Wave 0 completion list, and manual-only checks. Map EXP-01, EXP-02, and EXP-04 to the repaired built-in export runtime commands from Phase 83 execution, including queue runtime startup, truthful enqueue failure behavior, lifecycle timestamping, cleanup-worker proof, and actor-scoped export-status visibility. Keep EXP-03, ADAPT-01, and ADAPT-02 out of the satisfied set here unless the repaired current tree actually proves them; instead, note that full adapter-backed delivery remains future work owned by Phase 84.</action>
  <acceptance_criteria>
    <criterion>`.planning/phases/78-async-exports-ui/78-VALIDATION.md` frontmatter contains `nyquist_compliant: true`.</criterion>
    <criterion>`.planning/phases/78-async-exports-ui/78-VALIDATION.md` contains a `Per-Task Verification Map` with entries for EXP-01, EXP-02, and EXP-04.</criterion>
    <criterion>`.planning/phases/78-async-exports-ui/78-VALIDATION.md` references `orchestrator_test`, `cleanup_test`, `timeline_live_test`, and `export_status_live_test` in automated commands.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'nyquist_compliant: true|Per-Task Verification Map|EXP-01|EXP-02|EXP-04|orchestrator_test|cleanup_test|timeline_live_test|export_status_live_test' .planning/phases/78-async-exports-ui/78-VALIDATION.md</automated>
  </verify>
  <done>The Phase 78 validation artifact matches the current closeout contract and can be consumed by future milestone audits without extra interpretation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Current tree runtime proof -> planning artifacts | Verification docs directly influence requirement closure and milestone readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-05 | Repudiation | `78-VERIFICATION.md` | mitigate | Require current-tree proof for runtime startup, enqueue truth, lifecycle timestamps, and cleanup instead of summary-derived claims. |
| T-83-06 | Tampering | `78-VALIDATION.md` | mitigate | Normalize to the Nyquist structure so requirement/test mappings are explicit, executable, and auditable. |
| T-83-07 | Information disclosure | verification wording | accept | Evidence is limited to code paths and tests already present in the repository; no production data is introduced. |
</threat_model>

<verification>
Confirm both Phase 78 artifacts exist and contain the repaired built-in-runtime proof strings, then re-run the targeted export commands cited in the validation map so the new evidence matches executable reality.
</verification>

<success_criteria>
Phase 78 has the verification and validation artifacts the v1.20 audit expects, and those artifacts accurately describe the repaired built-in export runtime now owned by Phase 83 without claiming Phase 84 closure early.
</success_criteria>

<output>
After completion, create `.planning/phases/83-built-in-async-export-lifecycle-repair/83-02-SUMMARY.md`
</output>
