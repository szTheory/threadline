---
phase: 83-built-in-async-export-lifecycle-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - config/config.exs
  - config/test.exs
  - lib/threadline/application.ex
  - lib/threadline/export_queue/task_adapter.ex
  - lib/threadline/export/orchestrator.ex
  - lib/threadline/export/cleanup_task.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - test/threadline/export_queue/task_adapter_test.exs
  - test/threadline/export/orchestrator_test.exs
  - test/threadline/export/cleanup_test.exs
  - test/threadline/operator_surface/live/timeline_live_test.exs
  - test/threadline/operator_surface/live/export_status_live_test.exs
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-04
must_haves:
  truths:
    - "The built-in export `Task.Supervisor` path is started by the library on the default runtime instead of dangling behind optional host wiring."
    - "Background export requests preserve the governance row and fail it truthfully if enqueue/startup fails, instead of leaving a dead `pending` job."
    - "Export lifecycle timestamps are semantically correct: `started_at` on real execution start, `completed_at` on completion, and `expires_at` only on terminal states."
    - "Cleanup reconciles stale `running` rows and deletes only expired terminal jobs/artifacts through DB-driven lifecycle truth."
    - "The built-in runtime closure does not broaden into S3/Oban download semantics owned by Phase 84."
  artifacts:
    - path: lib/threadline/application.ex
      provides: "Built-in export runtime startup under the library-owned OTP tree"
      contains: "Threadline.Export.TaskSupervisor"
    - path: lib/threadline/operator_surface/live/timeline_live.ex
      provides: "Truthful background-export request path with enqueue failure handling"
      contains: "request_background_export"
    - path: lib/threadline/export/orchestrator.ex
      provides: "Lifecycle timestamping and terminal retention semantics"
      contains: "started_at"
    - path: lib/threadline/export/cleanup_task.ex
      provides: "Terminal-state-aware cleanup and abandoned-running reconciliation"
      contains: "expires_at"
  key_links:
    - from: lib/threadline/application.ex
      to: lib/threadline/export_queue/task_adapter.ex
      via: "application-owned startup of the named built-in queue supervisor"
      pattern: "TaskSupervisor|supervisor_not_started|export"
    - from: lib/threadline/operator_surface/live/timeline_live.ex
      to: lib/threadline/governance/export_job.ex
      via: "insert pending row, then fail or enqueue truthfully"
      pattern: "pending|failed|error_message"
    - from: lib/threadline/export/orchestrator.ex
      to: lib/threadline/export/cleanup_task.ex
      via: "terminal timestamp + expiry semantics consumed by cleanup"
      pattern: "started_at|completed_at|expires_at|running|failed|completed"
---

<objective>
Repair the actual built-in async export runtime so Threadline's default export lane can enqueue, execute, track, and clean up jobs truthfully without Oban, S3, or extra host supervision code.

Purpose: satisfy the runtime portion of EXP-01, EXP-02, and EXP-04 before the Phase 78 evidence-only closeout work in Plan 02.
Output: library-owned export runtime startup, truthful enqueue failure handling, corrected lifecycle timestamps/expiry semantics, and targeted tests proving the repaired path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.20-MILESTONE-AUDIT.md
@.planning/phases/83-built-in-async-export-lifecycle-repair/83-CONTEXT.md
@.planning/phases/83-built-in-async-export-lifecycle-repair/83-RESEARCH.md
@.planning/phases/81-retention-runtime-closure/81-01-PLAN.md
@.planning/phases/78-async-exports-ui/78-01-PLAN.md
@.planning/phases/78-async-exports-ui/78-02-PLAN.md
@.planning/phases/78-async-exports-ui/78-03-PLAN.md
@mix.exs
@config/config.exs
@config/test.exs
@lib/threadline/application.ex
@lib/threadline/export_queue/task_adapter.ex
@lib/threadline/export/orchestrator.ex
@lib/threadline/export/cleanup_task.ex
@lib/threadline/governance/export_job.ex
@lib/threadline/operator_surface/live/timeline_live.ex
@lib/threadline/operator_surface/live/export_status_live.ex
@test/threadline/export_queue/task_adapter_test.exs
@test/threadline/export/orchestrator_test.exs
@test/threadline/export/cleanup_test.exs
@test/threadline/operator_surface/live/timeline_live_test.exs
@test/threadline/operator_surface/live/export_status_live_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Start the built-in export runtime from `Threadline.Application`</name>
  <files>mix.exs, config/config.exs, config/test.exs, lib/threadline/application.ex, lib/threadline/export_queue/task_adapter.ex, test/threadline/export_queue/task_adapter_test.exs, test/threadline/export/cleanup_test.exs</files>
  <read_first>
    <file>mix.exs</file>
    <file>config/config.exs</file>
    <file>config/test.exs</file>
    <file>lib/threadline/application.ex</file>
    <file>lib/threadline/export_queue/task_adapter.ex</file>
    <file>lib/threadline/export/cleanup_task.ex</file>
    <file>test/threadline/export_queue/task_adapter_test.exs</file>
    <file>test/threadline/export/cleanup_test.exs</file>
    <file>.planning/phases/81-retention-runtime-closure/81-01-PLAN.md</file>
  </read_first>
  <action>Extend the existing library-owned application startup pattern used for retention so the default export runtime is real. Keep `Threadline.Application` as the single OTP owner and add two export children when a repo is configured: a named `Task.Supervisor` for the built-in queue path (`name: Threadline.Export.TaskSupervisor`) and `Threadline.Export.CleanupTask` with `repo:` plus runtime options read from `config :threadline, :exports`. Preserve the current retention child and avoid introducing a new false-by-default `exports.enabled` gate. Add or normalize explicit export runtime config keys in `config/config.exs` and `config/test.exs` for cleanup interval, stale-running cutoff, and terminal retention TTL so the cleanup worker and orchestrator share the same contract. Update the built-in queue adapter tests so they prove the named queue path is application-owned, while still keeping a direct supervisor override seam for isolated tests.</action>
  <acceptance_criteria>
    <criterion>`lib/threadline/application.ex` contains `Threadline.Export.TaskSupervisor` and `Threadline.Export.CleanupTask` child wiring that is conditional on repo presence.</criterion>
    <criterion>`lib/threadline/export_queue/task_adapter.ex` still defaults to `Threadline.Export.TaskSupervisor` and no longer relies on an unattached runtime contract.</criterion>
    <criterion>`config/config.exs` or `config/test.exs` contains export runtime settings for cleanup interval and retention TTL.</criterion>
    <criterion>`mix test test/threadline/export_queue/task_adapter_test.exs test/threadline/export/cleanup_test.exs --max-failures 1` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/export_queue/task_adapter_test.exs test/threadline/export/cleanup_test.exs --max-failures 1</automated>
  </verify>
  <done>The default built-in export queue and cleanup workers are started by the library runtime instead of dangling behind optional host wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make enqueue failures and lifecycle timestamps truthful</name>
  <files>lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/export/orchestrator.ex, lib/threadline/governance/export_job.ex, test/threadline/operator_surface/live/timeline_live_test.exs, test/threadline/export/orchestrator_test.exs, test/threadline/operator_surface/live/export_status_live_test.exs</files>
  <read_first>
    <file>lib/threadline/operator_surface/live/timeline_live.ex</file>
    <file>lib/threadline/export/orchestrator.ex</file>
    <file>lib/threadline/governance/export_job.ex</file>
    <file>lib/threadline/operator_surface/live/export_status_live.ex</file>
    <file>test/threadline/operator_surface/live/timeline_live_test.exs</file>
    <file>test/threadline/export/orchestrator_test.exs</file>
    <file>test/threadline/operator_surface/live/export_status_live_test.exs</file>
    <file>.planning/phases/83-built-in-async-export-lifecycle-repair/83-CONTEXT.md</file>
  </read_first>
  <action>Refine the request and execution path around durable lifecycle truth. In `TimelineLive`, keep inserting a `pending` `ExportJob`, but inspect the configured queue adapter result immediately afterward. If `enqueue/2` returns `:ok`, preserve the success flash and redirect to `#{base_path}/exports`. If it returns `{:error, reason}`, update that same row to `status: "failed"` with a stable human-readable `error_message`, surface an error flash, and avoid redirecting with false success. In `Orchestrator`, set `started_at` on the real `pending -> running` transition, keep `completed_at` for successful terminal state, and assign `expires_at` only when the job reaches `completed` or `failed` using a terminal retention TTL from export config. Preserve EXP-02's streaming behavior and keep failed rows durable instead of deleting them. Extend the existing Timeline, Orchestrator, and Export Status tests so they prove failed enqueue semantics, `started_at` on actual execution start, and visible terminal metadata on the actor-scoped status surface.</action>
  <acceptance_criteria>
    <criterion>`lib/threadline/operator_surface/live/timeline_live.ex` checks the result of `enqueue(` and contains a `failed` update path with `error_message` persistence.</criterion>
    <criterion>`lib/threadline/export/orchestrator.ex` sets `started_at` on the `running` transition and writes `expires_at` only from terminal `completed` or `failed` branches.</criterion>
    <criterion>`test/threadline/operator_surface/live/timeline_live_test.exs` contains an enqueue-failure case that asserts the job row becomes `failed` instead of staying `pending`.</criterion>
    <criterion>`mix test test/threadline/export/orchestrator_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs --max-failures 1` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/export/orchestrator_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Operators either get a real queued export or an immediate durable failure, and lifecycle timestamps now describe actual runtime state instead of intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tighten cleanup to terminal-state retention semantics and re-prove the built-in path</name>
  <files>lib/threadline/export/cleanup_task.ex, lib/threadline/export/orchestrator.ex, test/threadline/export/cleanup_test.exs, test/threadline/export/orchestrator_test.exs, test/threadline/operator_surface/live/timeline_live_test.exs</files>
  <read_first>
    <file>lib/threadline/export/cleanup_task.ex</file>
    <file>lib/threadline/export/orchestrator.ex</file>
    <file>lib/threadline/storage.ex</file>
    <file>lib/threadline/storage/local.ex</file>
    <file>test/threadline/export/cleanup_test.exs</file>
    <file>test/threadline/export/orchestrator_test.exs</file>
    <file>.planning/phases/78-async-exports-ui/78-02-PLAN.md</file>
  </read_first>
  <action>Keep `CleanupTask` as the built-in owner of stale-running reconciliation and expired-artifact cleanup, but narrow it to the intended lifecycle model. Update startup reconciliation so stale `running` rows become `failed` with a stable message and receive terminal expiry metadata if that is not already set. Update periodic cleanup so it only selects terminal rows (`completed` and `failed`) with non-null `expires_at < now`, deletes artifacts through the configured storage adapter when `file_path` exists, and then deletes the DB rows. Preserve the advisory-lock singleton pattern already used here. Extend cleanup/orchestrator tests so they cover failed-row expiry, completed-row cleanup, and the absence of accidental deletion for non-terminal or future-expiry rows. Finish by re-running the built-in export test set that proves queue startup, truthful enqueue behavior, lifecycle fields, and cleanup still compose end to end on the default path.</action>
  <acceptance_criteria>
    <criterion>`lib/threadline/export/cleanup_task.ex` filters cleanup to terminal statuses with non-null `expires_at` rather than sweeping every expired row.</criterion>
    <criterion>`test/threadline/export/cleanup_test.exs` covers stale `running` reconciliation plus expired `completed` or `failed` row cleanup.</criterion>
    <criterion>`mix test test/threadline/export_queue/task_adapter_test.exs test/threadline/export/orchestrator_test.exs test/threadline/export/cleanup_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs --max-failures 1` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/export_queue/task_adapter_test.exs test/threadline/export/orchestrator_test.exs test/threadline/export/cleanup_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs --max-failures 1</automated>
  </verify>
  <done>The built-in export lifecycle is self-consistent: runtime startup, enqueue truth, execution timestamps, and cleanup all operate from durable DB state on the default path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application config -> background runtime | Repo/config state now determines whether export workers start automatically. |
| Operator action -> queued background work | The UI must not misrepresent whether a job actually entered the runtime path. |
| Durable job state -> artifact deletion | Cleanup deletes files and rows based on DB lifecycle truth and must not target active work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Availability | `Threadline.Application` export children | mitigate | Start the built-in queue and cleanup workers conditionally on repo presence and keep config defaults bounded. |
| T-83-02 | Repudiation | `TimelineLive` enqueue path | mitigate | Preserve the export job row and convert enqueue failures into durable `failed` rows with explicit `error_message`. |
| T-83-03 | Tampering | `Export.Orchestrator` lifecycle fields | mitigate | Set `started_at`, `completed_at`, and `expires_at` only at semantically correct lifecycle transitions. |
| T-83-04 | Information disclosure / integrity | `CleanupTask` artifact deletion | mitigate | Delete only terminal expired jobs through the storage behaviour; do not sweep active rows. |
| T-83-05 | Repudiation | milestone evidence | mitigate | Keep Phase 83 verification scoped to built-in runtime closure and defer adapter-backed delivery claims to Phase 84. |
</threat_model>

<verification>
Run the targeted export runtime, LiveView, and cleanup tests, then confirm the current tree now has one truthful default path: `Threadline.Application` starts the built-in export runtime, `TimelineLive` surfaces enqueue failure immediately, `Orchestrator` records lifecycle timestamps/expiry correctly, and `CleanupTask` removes only terminal expired artifacts.
</verification>

<success_criteria>
Starting `:threadline` with a configured repo makes the built-in export runtime operational end to end, and operators no longer see dead `pending` rows or misleading success flows when the default runtime cannot accept work.
</success_criteria>

<output>
After completion, create `.planning/phases/83-built-in-async-export-lifecycle-repair/83-01-SUMMARY.md`
</output>
