---
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/mix.exs
  - examples/threadline_phoenix/mix.lock
  - examples/threadline_phoenix/config/config.exs
  - examples/threadline_phoenix/config/test.exs
  - examples/threadline_phoenix/lib/threadline_phoenix/application.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/blog.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/workers/post_touch_worker.ex
  - examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs
autonomous: true
requirements_addressed: [REF-04, REF-05]
---

# Plan 24-01: Oban worker — audited `posts` update + `record_action/2` (job path)

**Phase:** 24 — Job path, actions, adoption pointers  
**Plan:** 24-01 of 02  
**Status:** Ready to execute  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-24-01 | `set_config('threadline.actor_ref', …)` runs outside `Repo.transaction/1` or on a pooled connection without `true` (session-scoped) | high | Mirror **Phase 23** `Blog.create_post/2`: **only** `Repo.query!("SELECT set_config('threadline.actor_ref', $1::text, true)", [json])` as **first** statement inside the **same** `Repo.transaction/1` as DML + `record_action`. |
| T-24-02 | Worker uses process-laden actor instead of serialized args | medium | **`Threadline.Job.actor_ref_from_args/1`** only; reject missing `"actor_ref"` with `{:error, :missing_actor_ref}`. |
| T-24-03 | Mass assignment updates arbitrary columns on `posts` | medium | **`Post.changeset/2`** with **`cast`** limited to **`:title`** and **`:slug`** only (same as Phase 23 schema). |

---

## Objective

Add **Oban** to **`examples/threadline_phoenix`**, start it under **`Application`**, implement **`ThreadlinePhoenix.Workers.PostTouchWorker`** whose **`perform/1`** merges **`"job_id" => to_string(job.id)`** into args, delegates to **`ThreadlinePhoenix.Blog`** for a **`Repo.transaction`** that sets the GUC, **updates one `posts` row**, then calls **`Threadline.record_action/2`** with **`Threadline.Job.context_opts/1`** — satisfying **REF-04** and the **code** portion of **REF-05** per **24-CONTEXT** D-01–D-12.

---

## must_haves

- [ ] Example **`mix.exs`** lists **`{:oban, "~> 2.19"}`** (or compatible 2.x) and application supervises **Oban** after **Repo**.
- [ ] **`config/config.exs`** (and **`config/test.exs`** as needed) define **`Oban`** with **`repo: ThreadlinePhoenix.Repo`**, a dedicated queue (e.g. **`threadline_audit`**), and test-friendly Oban settings so **`Oban.Testing.perform_job/1`** works.
- [ ] **`Blog`** exposes a function used only from the worker path that runs **GUC → update post → `record_action(:post_title_refreshed_from_queue, …)`** in **one** transaction.
- [ ] Test inserts a **post**, **`Oban.insert!`** a job with **`%{"actor_ref" => …}`** (from **`ActorRef.to_map/1`**), **`perform_job`**, asserts **`audit_changes`** for **`posts`**, **`audit_transactions`** linkage, and **`audit_actions`** row with **`job_id`** matching the job.

---

## Task 24-01-01 — Dependencies + Oban configuration + supervision

<read_first>
- `examples/threadline_phoenix/mix.exs`
- `examples/threadline_phoenix/lib/threadline_phoenix/application.ex`
- `examples/threadline_phoenix/config/config.exs`
- `examples/threadline_phoenix/config/test.exs`
- `.planning/phases/24-job-path-actions-adoption-pointers/24-CONTEXT.md` (D-01, D-04)
</read_first>

<action>
1. In **`examples/threadline_phoenix/mix.exs`** **`deps/0`**, add **`{:oban, "~> 2.19"}`** after **`{:bandit, …}`** (or the last runtime dep), preserving alphabetical or existing ordering style in that file.
2. In **`config/config.exs`**, append **`config :threadline_phoenix, Oban, repo: ThreadlinePhoenix.Repo, queues: [threadline_audit: 10], plugins: []`** (adjust concurrency **10** only if file style prefers a different literal — document in commit if changed).
3. In **`config/test.exs`**, add Oban **testing** configuration required for **`Oban.Testing`** in current Oban 2.x (e.g. **`testing: :manual`** or **`testing: :inline`** per Oban docs for your locked version) so jobs can be **`perform_job`**’d without a full queue drain.
4. In **`ThreadlinePhoenix.Application.start/2`**, insert **`{Oban, Application.fetch_env!(:threadline_phoenix, Oban)}`** in **`children`** **immediately after** **`ThreadlinePhoenix.Repo`** and before PubSub/Endpoint.
5. Run **`cd examples/threadline_phoenix && mix deps.get && mix compile --warnings-as-errors`** and fix any compile warnings.
</action>

<acceptance_criteria>
- `grep -q ':oban' examples/threadline_phoenix/mix.exs` exits 0.
- `grep -q 'threadline_audit' examples/threadline_phoenix/config/config.exs` exits 0.
- `grep -q 'Oban' examples/threadline_phoenix/lib/threadline_phoenix/application.ex` exits 0.
- `grep -q ':oban' examples/threadline_phoenix/config/test.exs` **or** `grep -q 'Oban' examples/threadline_phoenix/config/test.exs` exits 0 (Oban test mode configured).
- `cd examples/threadline_phoenix && mix compile --warnings-as-errors` exits 0.
</acceptance_criteria>

---

## Task 24-01-02 — `Blog.touch_post_for_job/2` (GUC + update + `record_action`)

<read_first>
- `examples/threadline_phoenix/lib/threadline_phoenix/blog.ex`
- `examples/threadline_phoenix/lib/threadline_phoenix/post.ex`
- `lib/threadline/job.ex`
- `lib/threadline.ex` (`record_action/2` options)
- `.planning/phases/24-job-path-actions-adoption-pointers/24-CONTEXT.md` (D-07, D-11, D-12)
</read_first>

<action>
1. Add **`ThreadlinePhoenix.Blog.touch_post_for_job/2`** with arity **`(args :: map, attrs :: map)`** where **`attrs`** contains **`post_id`** as **integer** ( **`posts` migration uses default `bigserial` primary key** ) and **`title`** string for the new title.
2. **`touch_post_for_job/2`** MUST **`case Threadline.Job.actor_ref_from_args(args)`** — on **`{:error, _}`** return that error **without** opening a transaction; on **`{:ok, actor_ref}`** proceed.
3. Inside **`Repo.transaction(fn -> … end)`**, first line **`Repo.query!("SELECT set_config('threadline.actor_ref', $1::text, true)", [Jason.encode!(Threadline.Semantics.ActorRef.to_map(actor_ref))])`**.
4. Load **`%Post{}`** with **`Repo.get!/2`**, apply **`Post.changeset(post, %{title: attrs["title"]})`** (or atom keys if you normalize attrs — stay consistent with string keys from job args), **`Repo.update!/2`** or **`case Repo.update`** with **`rollback`** on error.
5. Still **inside the same transaction fn**, call **`Threadline.record_action(:post_title_refreshed_from_queue, [repo: Repo, actor: actor_ref] ++ Threadline.Job.context_opts(args))`** — use this **exact** intent atom so tests can grep it.
6. Return **`{:ok, post}`** / **`{:error, …}`** from the public function matching transaction semantics.
</action>

<acceptance_criteria>
- `grep -q 'touch_post_for_job' examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 0.
- `grep -q "set_config('threadline.actor_ref'" examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 0.
- `grep -q 'record_action(:post_title_refreshed_from_queue' examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 0.
- `grep -q 'actor_ref_from_args' examples/threadline_phoenix/lib/threadline_phoenix/blog.ex` exits 0.
</acceptance_criteria>

---

## Task 24-01-03 — `PostTouchWorker` + `Oban.Testing` integration test

<read_first>
- `examples/threadline_phoenix/test/support/data_case.ex`
- `examples/threadline_phoenix/test/test_helper.exs`
- `examples/threadline_phoenix/lib/threadline_phoenix/repo.ex`
- `test/threadline/job_test.exs` (args shapes)
- Hexdocs **Oban.Testing** for **`use Oban.Testing`** / **`perform_job`** API for locked version
</read_first>

<action>
1. Create **`examples/threadline_phoenix/lib/threadline_phoenix/workers/post_touch_worker.ex`** as **`Oban.Worker`** with **`queue: :threadline_audit`**, **`unique` disabled** for simplicity.
2. **`perform(%Oban.Job{} = job)`** pattern-matches **`job.args`**, builds **`args_with_job_id = Map.put(job.args, "job_id", to_string(job.id))`**, then calls **`ThreadlinePhoenix.Blog.touch_post_for_job(args_with_job_id, %{"post_id" => …, "title" => …})`** — return **`{:ok, _}`** or **`{:error, reason}`** from **`perform/1`** consistent with Oban expectations.
3. Create **`examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs`**: **`use ThreadlinePhoenix.DataCase, async: false`**, **`use Oban.Testing, repo: ThreadlinePhoenix.Repo`**, alias **`Threadline.Capture.{AuditChange, AuditTransaction}`**, **`Threadline.Semantics.AuditAction`**, **`ThreadlinePhoenix.Post`**, **`Repo`**.
4. Test: insert **`%Post{title: "before", slug: "job-touch-#{System.unique_integer([:positive])}"}`** via **`Repo.insert!`**, unwrap **`{:ok, ref} = ActorRef.new(:service_account, "threadline-phoenix-example-job")`**, set **`actor_ref_map = ActorRef.to_map(ref)`**, build job with **`PostTouchWorker.new(%{"actor_ref" => actor_ref_map, "correlation_id" => "phase-24-corr", "post_id" => post.id, "title" => "after"})`** (or current **`Oban.Worker.new/2`** shape for your Oban version), **`Oban.insert!(…)`**, then call **`Oban.Testing.perform_job/1`** or **`perform_job/2`** exactly as Oban 2.x documents for a sandboxed **`Repo`**.
5. Assertions: at least one **`AuditChange`** for **`table_name == "posts"`**; joined **`AuditTransaction`** with **`actor_ref.type == :service_account`** (or matching test actor); **`AuditAction`** row where **`name == "post_title_refreshed_from_queue"`** and **`job_id == to_string(job.id)`** and **`correlation_id == "phase-24-corr"`**.
</action>

<acceptance_criteria>
- `test -f examples/threadline_phoenix/lib/threadline_phoenix/workers/post_touch_worker.ex` exits 0.
- `grep -q 'defmodule ThreadlinePhoenix.Workers.PostTouchWorker' examples/threadline_phoenix/lib/threadline_phoenix/workers/post_touch_worker.ex` exits 0.
- `grep -q 'queue: :threadline_audit' examples/threadline_phoenix/lib/threadline_phoenix/workers/post_touch_worker.ex` exits 0.
- `test -f examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs` exits 0.
- `grep -q 'perform_job' examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs` exits 0.
- `grep -q 'post_title_refreshed_from_queue' examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs` exits 0.
- `cd examples/threadline_phoenix && mix test test/threadline_phoenix/workers/post_touch_worker_test.exs` exits 0.
- From repo root: **`MIX_ENV=test mix verify.example`** exits 0 (executor verifies full gate after all tasks).
</acceptance_criteria>

---

## Verification

1. `cd examples/threadline_phoenix && mix format`
2. `cd examples/threadline_phoenix && mix compile --warnings-as-errors`
3. `MIX_ENV=test mix verify.example` from **repository root**

---

## Success criteria

- **REF-04:** Oban **`perform_job`** path mutates **`posts`** with trigger capture and **`Threadline.Job`**-derived actor + job id in semantics row.
- **REF-05 (code):** **`record_action/2`** invoked once with domain intent atom **`post_title_refreshed_from_queue`** in the job transaction.
