---
wave: 2
depends_on: [24-01-PLAN.md]
files_modified:
  - examples/threadline_phoenix/README.md
autonomous: true
requirements_addressed: [REF-05, REF-06]
---

# Plan 24-02: README — semantics in jobs + adoption doc links

**Phase:** 24 — Job path, actions, adoption pointers  
**Plan:** 24-02 of 02  
**Status:** Ready to execute  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-24-04 | README implies library certifies third-party staging URLs | medium | **Explicit integrator-owned sentence** per **D-16** — CI proves patterns; host fills STG matrix. |
| T-24-05 | Broken relative links to guides | low | Use exact paths **`../../guides/production-checklist.md`** and **`../../guides/adoption-pilot-backlog.md`**; verify with **`grep`** in acceptance criteria. |

---

## Objective

Extend **`examples/threadline_phoenix/README.md`** with a **“Semantics in jobs”** subsection (**REF-05** prose) and a **“Documentation & production adoption”** block linking the two guides plus the integrator-owned STG line (**REF-06**), without duplicating guide bodies (**D-15**–**D-17**).

---

## must_haves

- [ ] README contains heading **`## Semantics in jobs`** (or anchor-equivalent **`Semantics in jobs`** as markdown `##` title).
- [ ] README contains relative links **`../../guides/production-checklist.md`** and **`../../guides/adoption-pilot-backlog.md`**.
- [ ] README states integrator owns host-class STG evidence (library CI does not certify external staging).
- [ ] Optional **D-14** sentence: **`action_id`** FK between capture and **`audit_actions`** may be tightened in a later version.

---

## Task 24-02-01 — README sections (semantics + adoption links)

<read_first>
- `examples/threadline_phoenix/README.md`
- `.planning/phases/24-job-path-actions-adoption-pointers/24-CONTEXT.md` (D-13–D-17, D-14)
- `lib/threadline/job.ex` (moduledoc pointers)
- `guides/production-checklist.md` (first heading — do not copy body)
- `guides/adoption-pilot-backlog.md` (intro only — do not copy matrix)
</read_first>

<action>
1. Add markdown section **`## Semantics in jobs`** with **3–6 sentences** explaining: trigger-backed rows record **what changed**; **`Threadline.record_action/2`** records **why / operator intent** when diffs are insufficient; reference **`ThreadlinePhoenix.Workers.PostTouchWorker`** + **`Blog.touch_post_for_job/2`** as the concrete example added in **24-01**; link **`Threadline.Job`** as **`../../lib/threadline/job.ex`** **or** “see Hexdocs for **`Threadline.Job`**” — pick **one** style consistent with rest of README.
2. Add section **`## Documentation & production adoption`** (or merge into one block if readability is better) containing bullet or short paragraph with **markdown links** **`[Production checklist](../../guides/production-checklist.md)`** and **`[Adoption pilot / STG backlog](../../guides/adoption-pilot-backlog.md)`** (paths **must** resolve from **`examples/threadline_phoenix/README.md`** → **`../../guides/...`**).
3. Add **one explicit sentence**: integrator maintains **host-class** staging topology matrix and evidence (fork/PR per **`CONTRIBUTING.md`**); **Threadline CI** demonstrates reference patterns, not the integrator’s production URLs.
4. If **D-14** applies: one sentence that **`audit_transactions.action_id`** optional linkage is a future tightening — do **not** promise FK is set in the example unless **24-01** already wired it.
</action>

<acceptance_criteria>
- `grep -q 'Semantics in jobs' examples/threadline_phoenix/README.md` exits 0.
- `grep -q '../../guides/production-checklist.md' examples/threadline_phoenix/README.md` exits 0.
- `grep -q '../../guides/adoption-pilot-backlog.md' examples/threadline_phoenix/README.md` exits 0.
- `grep -qi 'integrator' examples/threadline_phoenix/README.md` exits 0.
- `grep -q 'Threadline.Job' examples/threadline_phoenix/README.md` exits 0.
- `cd examples/threadline_phoenix && mix format` exits 0.
- `MIX_ENV=test mix verify.example` from repo root exits 0.
</acceptance_criteria>

---

## Verification

1. `cd examples/threadline_phoenix && mix format`
2. `MIX_ENV=test mix verify.example` from repository root
3. If README literals are covered elsewhere: `mix verify.doc_contract` from repo root (when part of local habit; CI already runs it).

---

## Success criteria

- **REF-05:** Operator-facing note for **`record_action/2`** vs capture-only path.
- **REF-06:** Example README links production checklist + adoption/STG guide with integrator-owned positioning.
