---
phase: 89-contract-lock-final-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/operator-surface.md
  - guides/upgrade-path.md
  - guides/getting-started-saas.md
  - guides/integration-contracts.md
  - examples/threadline_phoenix/README.md
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - test/threadline/operator_surface_doc_contract_test.exs
  - test/threadline/upgrade_path_doc_contract_test.exs
  - test/threadline/getting_started_saas_doc_contract_test.exs
  - test/threadline/integration_contracts_doc_contract_test.exs
  - test/threadline/example_phoenix_readme_contract_test.exs
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "Public guides name one support-lane contract without letting the example app silently become broader `phoenix-surface` authority."
    - "The example Phoenix host remains the narrower `sigra-reference` proof path and says so explicitly in both prose and router-backed behavior."
    - "If row history and as-of remain in the support-lane claim, the example host actually scopes `:row_history` queries instead of implying that proof."
    - "Root doc-contract tests lock the authority split across upgrade-path, operator-surface, getting-started, integration-contracts, and the example README."
  artifacts:
    - path: guides/upgrade-path.md
      provides: "Lane authority and `supported` vs `reference` vs `unclaimed` contract"
      contains: "sigra-reference"
    - path: guides/operator-surface.md
      provides: "Screen-level `/audit` support-lane behavior contract"
      contains: "export_authorize_fn"
    - path: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      provides: "Runnable shared `/audit` mount proof for admin and support personas"
      contains: "scope_operator_query"
    - path: test/threadline/example_phoenix_readme_contract_test.exs
      provides: "Doc-contract proof that the example stays a narrow reference lane"
      contains: "root library's broader `phoenix-surface` lane"
  key_links:
    - from: guides/upgrade-path.md
      to: examples/threadline_phoenix/README.md
      via: "lane taxonomy keeps `sigra-reference` narrower than `phoenix-surface`"
      pattern: "sigra-reference|phoenix-surface|reference"
    - from: guides/operator-surface.md
      to: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      via: "screen-level support claim matches the host-owned scope and export seams in the example"
      pattern: "scope_query_fn|export_authorize_fn|support_read_only"
    - from: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      to: guides/getting-started-saas.md
      via: "canonical `/audit` recipe and scoped support posture point to the same shared mount"
      pattern: "threadline_operator_surface|scope_query_fn"
---

<objective>
Lock the public support-lane contract and the example-host contract around one
truthful current-tree story: layered authority by concern, host-owned auth and
scope semantics, and a narrow `sigra-reference` proof that does not overclaim
for the broader root `phoenix-surface` lane.

Purpose: satisfy `DOC-01` by making the public contract text and example proof
say the same thing before final closeout verification begins.
Output: reconciled public guides, an example router/README that prove the
claimed support lane, and doc-contract tests that pin the hierarchy.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/89-contract-lock-final-verification/89-CONTEXT.md
@.planning/phases/89-contract-lock-final-verification/89-RESEARCH.md
@.planning/phases/89-contract-lock-final-verification/89-PATTERNS.md
@.planning/phases/85-support-lane-surface-audit/85-CONTEXT.md
@.planning/phases/88-denial-fallback-ux-closure/88-CONTEXT.md
@guides/operator-surface.md
@guides/upgrade-path.md
@guides/getting-started-saas.md
@guides/integration-contracts.md
@examples/threadline_phoenix/README.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@test/threadline/operator_surface_doc_contract_test.exs
@test/threadline/upgrade_path_doc_contract_test.exs
@test/threadline/getting_started_saas_doc_contract_test.exs
@test/threadline/integration_contracts_doc_contract_test.exs
@test/threadline/example_phoenix_readme_contract_test.exs

<interfaces>
From examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex:
```elixir
def my_authorize_fn(%{assigns: assigns}) do
  case assigns[:current_user] do
    %{is_admin: true} -> :ok
    %{role: :support, organization_id: org_id} when is_binary(org_id) and org_id != "" ->
      {:ok, %{access: :support_read_only, organization_id: org_id}}
    _ -> {:error, :unauthorized}
  end
end

def my_export_authorize_fn(%{assigns: assigns}) do
  case assigns[:current_user] do
    %{is_admin: true} -> :ok
    _ -> {:error, :unauthorized}
  end
end

def scope_operator_query(query, %{organization_id: org_id}, %{surface: surface})
    when surface in [:timeline, :transaction, :export] and is_binary(org_id) and org_id != "" do
  where(query, [_ac, at], fragment("?->>'organization_id' = ?", at.meta, ^org_id))
end
```

From guides/upgrade-path.md:
```text
`supported` means the lane is documented and backed by current repo proof.
`reference` means the repo maintains a first-party composition path inside a narrower host story.
`unclaimed` means the combination may be plausible locally, but this repo does not currently prove it.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile the layered public support-lane contract</name>
  <files>guides/operator-surface.md, guides/upgrade-path.md, guides/getting-started-saas.md, guides/integration-contracts.md</files>
  <read_first>
    <file>guides/operator-surface.md</file>
    <file>guides/upgrade-path.md</file>
    <file>guides/getting-started-saas.md</file>
    <file>guides/integration-contracts.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-CONTEXT.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-RESEARCH.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-PATTERNS.md</file>
    <file>.planning/phases/85-support-lane-surface-audit/85-CONTEXT.md</file>
  </read_first>
  <action>Rewrite only the four public guide surfaces so each one owns its concern per D-01 and D-06, following the existing literal-lock and section-shape patterns from `89-PATTERNS.md`: `upgrade-path` stays the lane taxonomy and proof matrix, `operator-surface` stays the screen-level `/audit` behavior contract, `getting-started-saas` stays the first-hour adopter recipe, and `integration-contracts` stays the host seam contract. Preserve the host-owned `authorize_fn`, `scope_query_fn`, and `export_authorize_fn` posture per D-20 and D-21. This task owns guide truth only: do not edit the example README, example router, or any test files here. Because the current example router appears not to scope `:row_history`, default to narrowing the public support-lane claim for row history / as-of per D-11 and D-12 unless the executor can prove in the same pass that the current tree safely scopes and tests that path; do not leave ambiguous prose that implies proof exists.</action>
  <acceptance_criteria>
    <criterion>`guides/upgrade-path.md` still contains ``phoenix-surface``, ``sigra-reference``, ``supported``, ``reference``, and ``unclaimed`` while clearly separating root-lane proof from example-lane proof.</criterion>
    <criterion>`guides/operator-surface.md`, `guides/getting-started-saas.md`, and `guides/integration-contracts.md` all mention `scope_query_fn` and `export_authorize_fn` as host-owned seams instead of introducing any Threadline-owned role or policy DSL.</criterion>
    <criterion>If row history / as-of remains claimed for support-scoped sessions, the updated guide text points to scoped proof rather than implying it from generic screen lists.</criterion>
    <criterion>This task leaves `examples/threadline_phoenix/README.md`, `examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex`, and the doc-contract test files untouched so example proof and test enforcement remain owned by Task 2.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/upgrade_path_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/integration_contracts_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The public contract layer names one truthful support lane with explicit authority boundaries and no overclaiming.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the example host to the narrow Sigra reference claim</name>
  <files>examples/threadline_phoenix/README.md, examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex, test/threadline/operator_surface_doc_contract_test.exs, test/threadline/upgrade_path_doc_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs, test/threadline/integration_contracts_doc_contract_test.exs, test/threadline/example_phoenix_readme_contract_test.exs</files>
  <read_first>
    <file>examples/threadline_phoenix/README.md</file>
    <file>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</file>
    <file>test/threadline/operator_surface_doc_contract_test.exs</file>
    <file>test/threadline/upgrade_path_doc_contract_test.exs</file>
    <file>test/threadline/getting_started_saas_doc_contract_test.exs</file>
    <file>test/threadline/integration_contracts_doc_contract_test.exs</file>
    <file>test/threadline/example_phoenix_readme_contract_test.exs</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-CONTEXT.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-PATTERNS.md</file>
    <file>.planning/phases/88-denial-fallback-ux-closure/88-CONTEXT.md</file>
  </read_first>
  <action>Keep the example app plus enforcement artifacts as proof of the narrower `sigra-reference` lane per D-02 and D-09, using the router-snippet and literal doc-contract patterns in `89-PATTERNS.md`. This task owns every non-guide change in the plan: `examples/threadline_phoenix/README.md`, the example router, and the root doc-contract tests that lock the authority split. Treat the missing `:row_history` example scope branch as a truth risk, not assumed repair work. Preferred path: if no already-provable support-scoped row-history behavior exists on the current tree, narrow the example README and root doc-contract wording so the example does not imply row-history/as-of proof. Only if the executor can make a bounded same-pass repair and add matching proof without widening scope may the router gain an explicit `:row_history` branch. In all cases, preserve the hierarchy that the example is runnable proof for the reference lane, not top-level authority for the broader `phoenix-surface` lane per D-02 and D-03.</action>
  <acceptance_criteria>
    <criterion>If any public support-lane text still claims row history / as-of for support-scoped sessions, `examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` contains an explicit scoped support path for `:row_history` and the docs point to that proof.</criterion>
    <criterion>If the current tree cannot prove support-scoped row history / as-of safely, the updated docs and example README explicitly narrow the public claim instead of implying hidden proof.</criterion>
    <criterion>`examples/threadline_phoenix/README.md` still contains `sigra-reference` and explicitly distinguishes that lane from the root library's broader `phoenix-surface` lane.</criterion>
    <criterion>The root doc-contract suite continues to pin the layered authority split after the example wording and router proof are updated.</criterion>
    <criterion>Task 2 is the only task in this plan that edits the example README, example router, and doc-contract tests, keeping example proof and enforcement ownership separate from Task 1's guide-only contract work.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/upgrade_path_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/integration_contracts_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs --max-failures 1</automated>
  </verify>
  <done>The example host, public docs, and contract tests now prove the same narrow lane without accidentally promoting the example to broader product authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public contract text -> adopter expectations | Overstated docs can cause adopters to rely on unsupported support-lane behavior. |
| Example-host router -> scoped support data | A missing `:row_history` scope branch would let the example imply proof it does not actually enforce. |
| Root docs -> example docs | The example must remain a narrower reference lane and must not override the root support matrix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Information disclosure | example support-scoped row-history path | mitigate | Prefer narrowing the public claim immediately when `:row_history` proof is absent; only keep the claim if an explicit scoped router branch and matching proof land in the same pass per D-11/D-12. |
| T-89-02 | Spoofing | example README posing as broader product authority | mitigate | Keep `sigra-reference` wording explicit and pin the hierarchy in doc-contract tests per D-02/D-03. |
| T-89-03 | Repudiation | conflicting guide authority by concern | mitigate | Reconcile all four guide surfaces so each owns one concern and contract tests assert the lane vocabulary and proof boundaries. |
</threat_model>

<verification>
Run the doc-contract suite plus the focused example operator-surface suite. The
result should show that the public guides, the example README, and the example
router all describe one truthful support lane with the example kept deliberately
narrow.
</verification>

<success_criteria>
Threadline's public docs and example-host contract agree on the current support
lane, including row-history/as-of only where the current tree truly proves it,
and the example app remains the `sigra-reference` proof path rather than
broader product authority.
</success_criteria>

<output>
After completion, create `.planning/phases/89-contract-lock-final-verification/89-01-SUMMARY.md`
</output>
