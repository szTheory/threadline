---
phase: 89-contract-lock-final-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - 89-01
files_modified:
  - test/threadline/operator_surface/transaction_live_test.exs
  - test/threadline/operator_surface/live/row_history_component_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
  - mix.exs
  - .github/workflows/ci.yml
  - .planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md
  - .planning/phases/89-contract-lock-final-verification/89-VALIDATION.md
autonomous: true
requirements:
  - DOC-02
must_haves:
  truths:
    - "If row-history/as-of remains in the support-lane claim after `89-01`, root and example behavioral proof cover that path with the same seriousness as timeline, actor, transaction, and export denial."
    - "If `89-01` narrows row-history/as-of out of the support-lane claim, verification artifacts explicitly record that narrowed truth instead of inventing proof."
    - "Named verification entrypoints and CI jobs expose the full support-lane contract lock, not just the README doc contract."
    - "Nyquist closeout artifacts record the four evidence bands and explicitly state whether authoritative-surface drift exists."
  artifacts:
    - path: test/threadline/operator_surface/live/row_history_component_test.exs
      provides: "Focused root proof for support-scoped row history and as-of behavior when that path remains in the claim"
      contains: "surface: :row_history"
    - path: mix.exs
      provides: "Named verification aliases for support-lane doc and example proof"
      contains: "verify.doc_contract"
    - path: .planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md
      provides: "Current-tree closeout evidence for the four contract bands"
      contains: "authoritative-surface drift"
  key_links:
    - from: test/threadline/operator_surface/live/row_history_component_test.exs
      to: lib/threadline/operator_surface/live/row_history_component.ex
      via: "support-scoped history and as-of are proven through the real row-history component path only if that path remains in the public claim"
      pattern: "Threadline.history|Threadline.as_of|surface: :row_history"
    - from: examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
      to: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      via: "example-host row-history proof is required only if the reference lane still claims that surface"
      pattern: "history|support user"
    - from: mix.exs
      to: .github/workflows/ci.yml
      via: "named doc/example verification aliases stay discoverable through stable CI job ids"
      pattern: "verify.doc_contract|verify.example|verify-test|verify-docs"
    - from: .planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md
      to: .planning/phases/89-contract-lock-final-verification/89-VALIDATION.md
      via: "evidence log and Nyquist sampling record the same serialized commands and drift outcome"
      pattern: "public contract text|root behavioral proof|example-host proof|named verification"
---

<objective>
Verify the final support-lane contract end to end on the current tree, widen the
named proof entrypoints to cover that contract honestly, and write the Nyquist
closeout artifacts that record whether Phase 89 is done or whether a separate
authoritative-surface reconciliation plan is required.

Purpose: satisfy `DOC-02` with rerunnable proof and current-tree closeout
evidence instead of relying on prose continuity.
Output: focused proof for the surviving support-lane claim, aligned
`mix verify.*` / CI coverage, and Phase 89 verification and validation
artifacts.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/89-contract-lock-final-verification/89-CONTEXT.md
@.planning/phases/89-contract-lock-final-verification/89-RESEARCH.md
@.planning/phases/89-contract-lock-final-verification/89-PATTERNS.md
@.planning/phases/89-contract-lock-final-verification/89-VALIDATION.md
@.planning/phases/89-contract-lock-final-verification/89-01-PLAN.md
@lib/threadline/operator_surface/live/row_history_component.ex
@lib/threadline/operator_surface/router.ex
@test/threadline/operator_surface/transaction_live_test.exs
@examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
@mix.exs
@.github/workflows/ci.yml

<interfaces>
From lib/threadline/operator_surface/live/row_history_component.ex:
```elixir
opts = [
  repo: assigns.repo,
  scope: assigns[:scope],
  scope_query_fn: assigns[:scope_query_fn]
]

history = Threadline.history(schema_module, assigns.record_id, opts)
snapshot = Threadline.as_of(schema_module, assigns.record_id, as_of_dt, opts)
```

From lib/threadline/operator_surface/router.ex:
```elixir
live("/transactions/:id/history/:table/:record_id", TransactionLive, :history)
```

From mix.exs:
```elixir
"verify.doc_contract": ["test test/threadline/readme_doc_contract_test.exs"],
"verify.example": &verify_example/1,
"ci.all": [
  "compile --warnings-as-errors",
  "format --check-formatted",
  "verify.compile_no_optional",
  "verify.test",
  "verify.example",
  "verify.doc_contract"
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add first-class root and example proof for support-scoped row history</name>
  <files>test/threadline/operator_surface/transaction_live_test.exs, test/threadline/operator_surface/live/row_history_component_test.exs, examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs</files>
  <read_first>
    <file>lib/threadline/operator_surface/live/row_history_component.ex</file>
    <file>lib/threadline/operator_surface/router.ex</file>
    <file>test/threadline/operator_surface/transaction_live_test.exs</file>
    <file>examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-CONTEXT.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-RESEARCH.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-PATTERNS.md</file>
  </read_first>
  <action>Verify the row-history decision from `89-01` using the existing scoped-router and request-path patterns in `89-PATTERNS.md`. If `89-01` kept row-history/as-of inside the public support-lane claim, create focused proof for the real `/transactions/:id/history/:table/:record_id` path per D-05, D-07, and D-08: add `test/threadline/operator_surface/live/row_history_component_test.exs`, cover both history and as-of, and extend the example app test so support users can open only in-scope row history. If `89-01` narrowed row-history/as-of out of the support-lane claim, do not invent replacement proof here; instead keep behavioral verification focused on the still-claimed surfaces and record the narrowed contract explicitly in the closeout artifacts.</action>
  <acceptance_criteria>
    <criterion>If row-history/as-of remains in the public claim, `test/threadline/operator_surface/live/row_history_component_test.exs` exists and asserts support-scoped behavior on the real row-history route, including `Threadline.history/3` and `Threadline.as_of/4` through `scope_query_fn`.</criterion>
    <criterion>If row-history/as-of remains in the public claim, `examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs` contains a support-scoped row-history case for one visible org record and one hidden org record.</criterion>
    <criterion>If `89-01` narrowed row-history/as-of out of the support-lane claim, the Phase 89 verification artifacts say so explicitly and Task 1 does not add speculative row-history proof files.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/transaction_live_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/live/actor_live_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs --max-failures 1</automated>
  </verify>
  <done>The row-history decision is verified honestly: either it is explicitly proven with root and example coverage, or it has been narrowed out of the public support-lane contract and recorded as such.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align named verification entrypoints and CI with the contract lock</name>
  <files>mix.exs, .github/workflows/ci.yml</files>
  <read_first>
    <file>mix.exs</file>
    <file>.github/workflows/ci.yml</file>
    <file>test/threadline/operator_surface_doc_contract_test.exs</file>
    <file>test/threadline/upgrade_path_doc_contract_test.exs</file>
    <file>test/threadline/getting_started_saas_doc_contract_test.exs</file>
    <file>test/threadline/integration_contracts_doc_contract_test.exs</file>
    <file>test/threadline/example_phoenix_readme_contract_test.exs</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-CONTEXT.md</file>
  </read_first>
  <action>Make the named proof surface honest per D-10. Expand `mix verify.doc_contract` so it runs the support-lane guide and example contract suite, not just `readme_doc_contract_test.exs`. Keep the stable CI job ids unchanged per `CLAUDE.md`, but ensure the existing workflow keeps calling the widened alias alongside `mix verify.test` and `mix verify.example`. Do not add parallel DB-backed test fan-out here; keep the closeout commands serial and discoverable.</action>
  <acceptance_criteria>
    <criterion>`mix.exs` maps `verify.doc_contract` to the README contract plus the support-lane guide/example contract test files.</criterion>
    <criterion>`.github/workflows/ci.yml` still contains the stable job ids `verify-test` and `verify-docs` while invoking the named aliases rather than ad-hoc replacement commands.</criterion>
    <criterion>`mix verify.doc_contract` and `mix verify.example` both remain grep-discoverable in `mix.exs` and `.github/workflows/ci.yml`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix verify.doc_contract && mix verify.example && rg -n 'verify\\.doc_contract|verify\\.example|verify\\.test|ci\\.all' mix.exs && rg -n 'verify-test|verify-docs|verify-compile-no-optional' .github/workflows/ci.yml</automated>
  </verify>
  <done>The rerunnable named proof entrypoints and CI surface now match the actual support-lane contract lock.</done>
</task>

<task type="auto">
  <name>Task 3: Write current-tree verification artifacts and gate authoritative-surface drift</name>
  <files>.planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md, .planning/phases/89-contract-lock-final-verification/89-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/89-contract-lock-final-verification/89-VALIDATION.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/PROJECT.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-CONTEXT.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-PATTERNS.md</file>
    <file>.planning/phases/80-governance-verification-and-milestone-surface-repair/80-CONTEXT.md</file>
  </read_first>
  <action>Create `89-VERIFICATION.md` as the final current-tree evidence log with four named sections per D-05: public contract text, root behavioral proof, example-host proof, and named verification/CI proof. Follow the closeout structure patterns from `89-PATTERNS.md`, `84-VERIFICATION.md`, and `80-VALIDATION.md`. Update `89-VALIDATION.md` to set `nyquist_compliant: true`, record the actual serialized commands used, remove the draft `89-03-01` Wave 3 row so the artifact does not predeclare a third plan, and add an explicit authoritative-surface drift verdict. Mention `89-03` only as a contingent follow-up when the drift verdict is positive. The artifacts must also state the row-history/as-of outcome plainly: either proven on the current tree or narrowed out of the support-lane claim. Per D-17 through D-19, do not edit `ROADMAP.md`, `STATE.md`, or `PROJECT.md` inside this plan; if those surfaces still contradict the verified contract, record the exact contradiction in `89-VERIFICATION.md` and stop closeout so a separate `89-03` plan can be opened intentionally.</action>
  <acceptance_criteria>
    <criterion>`.planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md` contains the phrases `public contract text`, `root behavioral proof`, `example-host proof`, `named verification/CI proof`, and `authoritative-surface drift`.</criterion>
    <criterion>`.planning/phases/89-contract-lock-final-verification/89-VALIDATION.md` sets `nyquist_compliant: true` and records the task-level commands actually used for Phase 89 closeout.</criterion>
    <criterion>If authoritative-surface drift exists, the verification artifact names the contradictory files and states that `89-03` is required instead of silently editing those files here.</criterion>
    <criterion>`.planning/phases/89-contract-lock-final-verification/89-VALIDATION.md` no longer contains a predeclared `89-03-01` row or any other routine Wave 3 task entry; `89-03` appears only in contingent drift language.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'public contract text|root behavioral proof|example-host proof|named verification/CI proof|authoritative-surface drift' .planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md && rg -n 'nyquist_compliant: true|authoritative-surface drift|mix verify\\.doc_contract|mix verify\\.example|mix ci\\.all' .planning/phases/89-contract-lock-final-verification/89-VALIDATION.md && ! rg -n '89-03-01|\\| 89-03 \\|' .planning/phases/89-contract-lock-final-verification/89-VALIDATION.md</automated>
  </verify>
  <done>Phase 89 has current-tree closeout evidence and a hard gate against silently mutating milestone authority surfaces without an explicit reconciliation plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Support-scoped route -> row-history/as-of data | A missing or weak proof path could leak out-of-scope record contents. |
| Named Mix aliases -> maintainer trust | Misleading verification aliases create false confidence about what the repo actually proves. |
| Closeout artifacts -> milestone authority surfaces | Verification notes must not silently overwrite or hide contradictions in `ROADMAP.md`, `STATE.md`, or `PROJECT.md`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04 | Information disclosure | row-history/as-of support path | mitigate | Treat row-history/as-of as unproven by default; keep it in the public claim only if explicit root and example tests land in the same pass, otherwise narrow the claim and record that outcome. |
| T-89-05 | Repudiation | `verify.doc_contract` alias vs actual proof surface | mitigate | Expand the alias to include the support-lane guide/example contract suite and keep CI invoking the named alias. |
| T-89-06 | Tampering | milestone truth during closeout | mitigate | Record drift in `89-VERIFICATION.md` and block direct authoritative-surface edits inside this plan unless a separate `89-03` reconciliation plan is opened. |
</threat_model>

<verification>
Run the focused row-history proof tests serially, run the widened named
doc/example verification entrypoints, then write the final evidence artifacts.
Phase 89 only closes if those artifacts show all four proof bands aligned and no
unreconciled authority-surface contradiction.
</verification>

<success_criteria>
The current tree has first-class row-history support proof, honest named
verification entrypoints, and Nyquist-compliant closeout artifacts that either
finish Phase 89 cleanly or explicitly trigger a separate 89-03 reconciliation
plan.
</success_criteria>

<output>
After completion, create `.planning/phases/89-contract-lock-final-verification/89-02-SUMMARY.md`
</output>
