---
phase: 73
plan: "73-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/auth.ex
  - lib/threadline/operator_surface/export_auth_plug.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - lib/threadline/operator_surface/live/actor_live.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
  - lib/threadline/operator_surface/controllers/export_controller.ex
  - lib/threadline/operator_surface/scope.ex
  - lib/threadline/query.ex
  - lib/threadline/investigation.ex
  - test/threadline/operator_surface/auth_test.exs
  - test/threadline/operator_surface/export_auth_plug_test.exs
  - test/threadline/operator_surface/live/timeline_live_test.exs
  - test/threadline/operator_surface/live/actor_live_test.exs
  - test/threadline/operator_surface/transaction_live_test.exs
  - test/threadline/operator_surface/controllers/export_controller_test.exs
autonomous: true
requirements:
  - INTEG-03
  - ADOPT-08
must_haves:
  truths:
    - "The operator surface has one host-owned scope-enforcement seam that applies across timeline, actor, transaction, and export flows."
    - "Threadline still treats `{:ok, scope}` as opaque data and does not define built-in role or organization semantics."
    - "Support-read-only enforcement is proven by focused tests instead of a mount-only smoke check."
  artifacts:
    - path: "lib/threadline/operator_surface/scope.ex"
      provides: "Shared host callback dispatch for scoped operator-surface queries"
    - path: "lib/threadline/operator_surface/live/timeline_live.ex"
      provides: "Timeline scope narrowing wired to the host callback"
    - path: "lib/threadline/operator_surface/live/actor_live.ex"
      provides: "Actor flow scope enforcement"
    - path: "lib/threadline/operator_surface/live/transaction_live.ex"
      provides: "Transaction drill-down scope enforcement"
    - path: "lib/threadline/operator_surface/controllers/export_controller.ex"
      provides: "Export scope enforcement"
    - path: "test/threadline/operator_surface/live/actor_live_test.exs"
      provides: "Focused proof that out-of-scope actor paths are denied or hidden"
    - path: "test/threadline/operator_surface/transaction_live_test.exs"
      provides: "Focused proof that out-of-scope transaction paths are denied or hidden"
  key_links:
    - from: "lib/threadline/operator_surface/router.ex"
      to: "lib/threadline/operator_surface/scope.ex"
      via: "new `scope_query_fn` option"
      pattern: "scope_query_fn"
    - from: "lib/threadline/operator_surface/live/timeline_live.ex"
      to: "lib/threadline/query.ex"
      via: "scope-aware query transform for timeline"
      pattern: "surface: :timeline"
    - from: "lib/threadline/operator_surface/live/transaction_live.ex"
      to: "lib/threadline/investigation.ex"
      via: "scope-aware incident lookup"
      pattern: "surface: :transaction"
---

<objective>
Add the real scoped-enforcement seam for support-read-only operator access.

Purpose: satisfy the implementation half of `INTEG-03` and make the Phase 71 support-read-only story materially true across all operator flows instead of only assigning `:threadline_scope`.
Output: code-path wiring plus focused tests for timeline, actor, transaction, and export scope enforcement.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.19-MILESTONE-AUDIT.md
@.planning/phases/71-mount-recipes-and-access-tiers/71-CONTEXT.md
@.planning/phases/73-authorization-contract-repair-and-scoped-access-enforcement/73-RESEARCH.md
@.planning/phases/73-authorization-contract-repair-and-scoped-access-enforcement/73-PATTERNS.md
@lib/threadline/operator_surface/router.ex
@lib/threadline/operator_surface/auth.ex
@lib/threadline/operator_surface/export_auth_plug.ex
@lib/threadline/operator_surface/live/timeline_live.ex
@lib/threadline/operator_surface/live/actor_live.ex
@lib/threadline/operator_surface/live/transaction_live.ex
@lib/threadline/operator_surface/controllers/export_controller.ex
@lib/threadline/query.ex
@lib/threadline/investigation.ex
@test/threadline/operator_surface/live/timeline_live_test.exs
@test/threadline/operator_surface/live/actor_live_test.exs
@test/threadline/operator_surface/transaction_live_test.exs
@test/threadline/operator_surface/controllers/export_controller_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the host-owned scope callback seam and thread it through the operator surface</name>
  <read_first>lib/threadline/operator_surface/router.ex; lib/threadline/operator_surface/auth.ex; lib/threadline/operator_surface/export_auth_plug.ex; lib/threadline/operator_surface/live/timeline_live.ex; lib/threadline/query.ex; lib/threadline/investigation.ex</read_first>
  <files>lib/threadline/operator_surface/router.ex, lib/threadline/operator_surface/auth.ex, lib/threadline/operator_surface/export_auth_plug.ex, lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/operator_surface/scope.ex, lib/threadline/query.ex, lib/threadline/investigation.ex</files>
  <action>Add a new optional operator-surface mount option `scope_query_fn: &MyApp.Audit.scope_operator_query/3` to `threadline_operator_surface/2`. Preserve the existing auth result vocabulary (`:ok | true | {:ok, scope}`), but make the new callback available anywhere `:threadline_scope` is present. Implement a helper module such as `Threadline.OperatorSurface.Scope` that calls `scope_query_fn.(query, scope, %{surface: surface, params: params})` and returns the transformed Ecto query. Use this helper in timeline queries immediately, with `surface: :timeline` and `params: %{filters: filters}`. Keep `scope` opaque: no built-in `organization_id`, no built-in role checks, and no Threadline-owned permission enum. If `scope_query_fn` is absent or `scope` is nil, the helper must return the original query unchanged.</action>
  <acceptance_criteria>`lib/threadline/operator_surface/router.ex` documents the literal option name `scope_query_fn`; `lib/threadline/operator_surface/scope.ex` contains a public helper that dispatches on `surface: :timeline`; `lib/threadline/query.ex` or `lib/threadline/investigation.ex` contains the literal `scope_query_fn` call site; `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/export_auth_plug_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs --max-failures 1` exits 0.</acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/export_auth_plug_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs --max-failures 1</automated>
  </verify>
  <done>The timeline/export surfaces have a real host-owned scope callback seam instead of a no-op `scope_to_query_opts/1` stub.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the same scope seam for actor and transaction drill-down flows</name>
  <read_first>lib/threadline/operator_surface/live/actor_live.ex; lib/threadline/operator_surface/live/transaction_live.ex; lib/threadline/investigation.ex; lib/threadline/query.ex; test/threadline/operator_surface/live/actor_live_test.exs; test/threadline/operator_surface/transaction_live_test.exs</read_first>
  <files>lib/threadline/operator_surface/live/actor_live.ex, lib/threadline/operator_surface/live/transaction_live.ex, lib/threadline/query.ex, lib/threadline/investigation.ex, test/threadline/operator_surface/live/actor_live_test.exs, test/threadline/operator_surface/transaction_live_test.exs</files>
  <action>Extend the new `scope_query_fn` seam to actor-history and incident-bundle drill-downs. Add internal query-builder helpers or operator-surface-private wrappers so actor and transaction screens can apply `scope_query_fn` with `surface: :actor_history` and `surface: :transaction` before repo execution. For out-of-scope actor or transaction requests, render the existing not-found/empty affordance rather than leaking records. In tests, add scoped endpoints mirroring the existing `TimelineLiveScopedTest` pattern. Use generic Threadline-owned fields that already exist in fixtures, such as `actor_ref` or `source`, to prove that support-read-only users see only allowed actor/transaction data without introducing app-specific organization tables.</action>
  <acceptance_criteria>`test/threadline/operator_surface/live/actor_live_test.exs` and `test/threadline/operator_surface/transaction_live_test.exs` each contain a scoped test case name mentioning `scope` or `scoped`; those tests prove that an out-of-scope actor or transaction does not render the protected data; `mix test test/threadline/operator_surface/live/actor_live_test.exs test/threadline/operator_surface/transaction_live_test.exs --max-failures 1` exits 0.</acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/actor_live_test.exs test/threadline/operator_surface/transaction_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Actor and transaction drill-downs honor the same host-owned scope contract as timeline and export flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host auth scope -> Threadline queries | Host-owned scope enters Threadline as opaque data and must not be silently ignored. |
| export/page auth -> row visibility | Allowing page access must not imply unrestricted row visibility for support-read-only users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | E | operator-surface query paths | mitigate | Add one shared `scope_query_fn` seam and prove it on every operator flow. |
| T-73-02 | T | timeline/export scope bridge | mitigate | Replace the current timeline scope stub with a real host callback and focused tests. |
| T-73-03 | I | actor/transaction drill-down | mitigate | Reject or hide out-of-scope records via scoped query builders instead of mount-only checks. |
</threat_model>

<verification>
Run:
- `mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/export_auth_plug_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs --max-failures 1`
- `mix test test/threadline/operator_surface/live/actor_live_test.exs test/threadline/operator_surface/transaction_live_test.exs --max-failures 1`
- `mix verify.compile_no_optional`
</verification>

<success_criteria>
- The operator surface has one host-owned scope callback seam that applies across timeline, actor, transaction, and export flows.
- Support-read-only proof is behavioral, not just an auth-assign smoke test.
- Threadline still does not interpret host roles or organization semantics itself.
</success_criteria>

<output>
After completion, create `.planning/phases/73-authorization-contract-repair-and-scoped-access-enforcement/73-01-SUMMARY.md`
</output>
