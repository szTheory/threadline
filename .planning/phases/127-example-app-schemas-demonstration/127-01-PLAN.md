---
phase: 127-example-app-schemas-demonstration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - guides/getting-started-saas.md
  - examples/threadline_phoenix/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Example router `threadline_operator_surface/2` mount includes `:schemas` for `tickets` and `ticket_replies` help-desk tables."
    - "`scope_operator_query/3` handles `%{surface: :row_history}` with the same org meta filter as timeline/transaction surfaces."
    - "getting-started §9 and example README mount snippets normalize-match the router `operator-surface-mount` doc block."
  artifacts:
    - path: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      provides: "Runnable :schemas mount (D-14 closure)"
      contains: "schemas:"
    - path: guides/getting-started-saas.md
      provides: "§9 snippet parity with router SSOT"
    - path: examples/threadline_phoenix/README.md
      provides: "Track A mount parity with router SSOT"
  key_links:
    - from: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      to: guides/getting-started-saas.md
      via: "GettingStartedFixtures operator-surface-mount extraction"
      pattern: "operator-surface-mount"
    - from: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      to: examples/threadline_phoenix/README.md
      via: "example_phoenix_readme_contract_test router_mount_block"
      pattern: "ticket_replies"
---

<objective>
Wire `:schemas` on the example app operator mount and sync getting-started §9 + README mount blocks to the router SSOT.

Purpose: Close v1.27 milestone blocker — documented row-history reification path becomes runnable (Phase 124 D-14 deferred).
Output: Router mount with help-desk schema map, org-scoped `:row_history` query clause, doc snippet parity, green `mix verify.doc_contract`.
</objective>

<context>
@.planning/phases/127-example-app-schemas-demonstration/127-RESEARCH.md
@.planning/milestones/v1.27-MILESTONE-AUDIT.md (Integration gaps)
@.planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-10–D-14)
@guides/operator-surface.md (Row history reification subsection)
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@guides/getting-started-saas.md (§9 lines 300–323)
@examples/threadline_phoenix/README.md (mount block ~287–299)
@test/threadline/getting_started_saas_doc_contract_test.exs (mount_block/0)
@test/threadline/example_phoenix_readme_contract_test.exs (router_mount_block/0)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-127-01 | Over-broad schema map exposes unrelated tables | low | Limit map to walkthrough help-desk tables only (`tickets`, `ticket_replies`) |
| T-127-02 | Doc snippets drift from router after mount edit | medium | Edit router first; copy exact block to §9 + README; run `mix verify.doc_contract` |
| T-127-03 | Support row history leaks cross-org without `:row_history` scope | medium | Add `:row_history` to org-scoped `scope_operator_query/3` clause |

**Surface:** Example app router + docs. Auth boundary unchanged — mount remains behind `:operator_auth`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add :schemas and :row_history scope to example router</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex (operator-surface-mount block L144–158; scope_operator_query/3 L98–113)
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk/ticket.ex
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk/ticket_reply.ex
    - guides/operator-surface.md (Row history reification subsection)
    - lib/threadline/operator_surface/live/row_history_component.ex (error copy)
  </read_first>
  <action>
Inside the `# doc: start: operator-surface-mount` / `# doc: end: operator-surface-mount` block, add **before** `repo:`:

```elixir
schemas: %{
  "tickets" => ThreadlinePhoenix.HelpDesk.Ticket,
  "ticket_replies" => ThreadlinePhoenix.HelpDesk.TicketReply
},
```

Update `scope_operator_query/3` third clause surface guard from:

`when surface in [:timeline, :transaction, :export]`

to:

`when surface in [:timeline, :transaction, :export, :row_history]`

Do **not** add `posts` or other tables. Do **not** move doc markers.
  </action>
  <acceptance_criteria>
    - `rg -n 'schemas: %' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` exits 0 with at least 1 match inside operator-surface-mount markers.
    - `rg -n 'ticket_replies.*ThreadlinePhoenix.HelpDesk.TicketReply' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` exits 0.
    - `rg -n ':row_history' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` exits 0.
    - `cd examples/threadline_phoenix && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rg -q 'schemas: %' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex && rg -q 'TicketReply' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex && cd examples/threadline_phoenix && mix compile --warnings-as-errors</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Sync getting-started §9 and README mount blocks to router SSOT</name>
  <files>guides/getting-started-saas.md, examples/threadline_phoenix/README.md</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex (updated mount block)
    - guides/getting-started-saas.md (§9 elixir fence lines 305–317)
    - examples/threadline_phoenix/README.md (elixir mount fence ~287–299)
    - test/threadline/getting_started_saas_doc_contract_test.exs (contains_normalized? mount_block)
    - test/threadline/example_phoenix_readme_contract_test.exs
  </read_first>
  <action>
Replace the `threadline_operator_surface("/", ...)` elixir fence bodies in:

1. `guides/getting-started-saas.md` §9 (between `## 9.` and the `:schemas` prose sentence)
2. `examples/threadline_phoenix/README.md` (mounted operator-surface story section)

with the **exact** mount option list from router doc markers (including new `schemas:` map). Preserve surrounding prose — only update the fenced elixir blocks.

Keep the existing §9 sentence after the fence:
"Map captured table names to Ecto modules with the `:schemas` option..." — do not duplicate the full map in prose.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/getting_started_saas_doc_contract_test.exs` exits 0.
    - `mix test test/threadline/example_phoenix_readme_contract_test.exs` exits 0.
    - `mix verify.doc_contract` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/getting_started_saas_doc_contract_test.exs && mix test test/threadline/example_phoenix_readme_contract_test.exs && mix verify.doc_contract</automated>
  </verify>
</task>

</tasks>

<verification_criteria>
- [ ] Router mount contains `schemas:` for tickets + ticket_replies
- [ ] `:row_history` included in org-scoped query clause
- [ ] getting-started §9 and README mount blocks match router extraction
- [ ] `mix verify.doc_contract` green
</verification_criteria>
