---
phase: 127-example-app-schemas-demonstration
plan: "02"
type: execute
wave: 2
depends_on:
  - "127-01"
files_modified:
  - examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
  - test/threadline/example_phoenix_schemas_mount_contract_test.exs
  - .planning/phases/127-example-app-schemas-demonstration/127-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Integration test proves `/audit/transactions/:id/history/ticket_replies/:pk` renders row history (not schema-mapping error)."
    - "Contract test locks router mount contains help-desk `:schemas` entries (D-14 runnable evidence)."
    - "127-VERIFICATION.md records passed status for all three ROADMAP success criteria."
  artifacts:
    - path: examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
      provides: "Row-history reification integration proof"
    - path: test/threadline/example_phoenix_schemas_mount_contract_test.exs
      provides: "D-14 mount map contract"
    - path: .planning/phases/127-example-app-schemas-demonstration/127-VERIFICATION.md
      provides: "Phase closeout evidence"
  key_links:
    - from: examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
      to: examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
      via: "HTTP GET history sub-route"
      pattern: "/history/ticket_replies/"
---

<objective>
Prove row-history reification works on the example app mount and record phase verification for v1.27 gap closure.

Purpose: Satisfy ROADMAP SC #3 — contract/walkthrough evidence for reification path (shipped history sub-route).
Output: New/extended tests green, `127-VERIFICATION.md` with `status: passed`.
</objective>

<context>
@.planning/phases/127-example-app-schemas-demonstration/127-RESEARCH.md
@.planning/phases/127-example-app-schemas-demonstration/127-01-PLAN.md
@examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs (capture fixture pattern)
@examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
@test/threadline/operator_surface/transaction_live_test.exs (scoped history precedent)
@guides/operator-surface.md (shorthand vs `/transactions/:id/history/...` slide-over)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-127-03 | Test passes without real reification (false green) | medium | Assert `Row History: ticket_replies /` heading; refute `not mapped to an Ecto schema` |
| T-127-04 | Test uses non-existent `/audit/rows/...` route | low | Use shipped path `/audit/transactions/:id/history/:table/:record_id` per operator-surface guide |

**Surface:** Example app tests only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Integration test — history sub-route reifies ticket_replies</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs
    - examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs
    - test/support/help_desk_fixtures.ex (or examples equivalent)
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
    - lib/threadline/operator_surface/live/row_history_component.ex
  </read_first>
  <action>
Add test `"admin reaches ticket_replies row history via :schemas mount"`:

1. In `Ecto.Adapters.SQL.Sandbox.unboxed_run/2`, create org + agent + ticket via help-desk fixtures.
2. Call `HelpDesk.ticket_replied_and_closed/6` with audit context (copy pattern from `help_desk_audit_test.exs`).
3. Query `AuditChange` for `table_name == "ticket_replies"` and `op == "insert"`; extract pk from `table_pk["id"]`.
4. Create admin user with org membership (`role: "agent"`, admin email pattern from existing tests).
5. `login_via_sigra(conn, user)` then:
   `get(conn, "/audit/transactions/#{tx_id}/history/ticket_replies/#{reply_pk}")`
6. Assert `html_response(conn, 200)` contains `Row History: ticket_replies / #{reply_pk}`.
7. Refute response contains `not mapped to an Ecto schema`.

Use existing conn-case helpers; no LiveView test required (HTTP GET sufficient for RowHistoryComponent render).
  </action>
  <acceptance_criteria>
    - `rg -n 'Row History: ticket_replies' examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs` exits 0.
    - `rg -n 'not mapped to an Ecto schema' examples/threadline_phoenix/test/threadline_phoenix_web/operator_surface_test.exs` exits 0 (refute assertion).
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix_web/operator_surface_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix_web/operator_surface_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Contract test — router mount locks help-desk :schemas (D-14)</name>
  <files>test/threadline/example_phoenix_schemas_mount_contract_test.exs</files>
  <read_first>
    - test/support/getting_started_fixtures.ex
    - test/threadline/getting_started_saas_doc_contract_test.exs
    - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
    - .planning/milestones/v1.27-MILESTONE-AUDIT.md (D-14 gap)
  </read_first>
  <action>
Create `test/threadline/example_phoenix_schemas_mount_contract_test.exs`:

```elixir
defmodule Threadline.ExamplePhoenixSchemasMountContractTest do
  use ExUnit.Case, async: true

  alias Threadline.GettingStartedFixtures

  test "example router operator mount maps help-desk tables for row history (D-14)" do
    mount =
      GettingStartedFixtures.extract!(
        "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex",
        "operator-surface-mount"
      )

    assert mount =~ "schemas:"
    assert mount =~ "\"tickets\" => ThreadlinePhoenix.HelpDesk.Ticket"
    assert mount =~ "\"ticket_replies\" => ThreadlinePhoenix.HelpDesk.TicketReply"
  end
end
```
  </action>
  <acceptance_criteria>
    - `test -f test/threadline/example_phoenix_schemas_mount_contract_test.exs` exits 0.
    - `mix test test/threadline/example_phoenix_schemas_mount_contract_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/example_phoenix_schemas_mount_contract_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Create 127-VERIFICATION.md phase closeout artifact</name>
  <files>.planning/phases/127-example-app-schemas-demonstration/127-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/126-nyquist-validation-signoff-122-124/126-VERIFICATION.md (format precedent)
    - .planning/ROADMAP.md (Phase 127 success criteria)
    - .planning/phases/127-example-app-schemas-demonstration/127-VALIDATION.md
  </read_first>
  <action>
Create `127-VERIFICATION.md` with:

Frontmatter: `status: passed`, `verified: 2026-05-28`

Sections:
1. **Success Criteria** — table mapping ROADMAP SC #1–3 to evidence (router grep, doc contract commands, test file paths)
2. **Commands Actually Used** — list with PASS results:
   - `cd examples/threadline_phoenix && mix test test/threadline_phoenix_web/operator_surface_test.exs`
   - `mix test test/threadline/example_phoenix_schemas_mount_contract_test.exs`
   - `mix verify.doc_contract`
3. **Notes** — WALKTHROUGH shorthand `/audit/rows/...` vs shipped history sub-route (no new route added)

Update `127-VALIDATION.md` per-task rows to ✅ green and set `nyquist_compliant: true`, `status: finalized` after commands pass.
  </action>
  <acceptance_criteria>
    - `rg -n 'status: passed' .planning/phases/127-example-app-schemas-demonstration/127-VERIFICATION.md` exits 0.
    - `rg -n 'nyquist_compliant: true' .planning/phases/127-example-app-schemas-demonstration/127-VALIDATION.md` exits 0 after Task 3 edits.
    - `mix verify.doc_contract` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/127-example-app-schemas-demonstration/127-VERIFICATION.md && mix verify.doc_contract</automated>
  </verify>
</task>

</tasks>

<verification_criteria>
- [ ] Row history integration test green on example app
- [ ] D-14 contract test locks mount schema map
- [ ] 127-VERIFICATION.md passed; 127-VALIDATION.md finalized
- [ ] `mix verify.doc_contract` green
</verification_criteria>
