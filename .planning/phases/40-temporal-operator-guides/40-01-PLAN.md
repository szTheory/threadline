---
phase: 40-temporal-operator-guides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/domain-reference.md
  - examples/threadline_phoenix/README.md
  - test/threadline/exploration_routing_doc_contract_test.exs
  - test/threadline/readme_doc_contract_test.exs
autonomous: true
requirements:
  - ASOF-06
must_haves:
  truths:
    - "Readers can find one Time Travel (As-of) hub section in the operator guide."
    - "Readers can copy one runnable historical reconstruction walkthrough from the Phoenix example README."
    - "Doc contract tests fail if the guide anchor or README walkthrough drifts."
  artifacts:
    - path: "guides/domain-reference.md"
      provides: "Time Travel hub section, behavior table, and cross-link to the example walkthrough"
    - path: "examples/threadline_phoenix/README.md"
      provides: "Short historical reconstruction walkthrough using ThreadlinePhoenix.Post"
    - path: "test/threadline/exploration_routing_doc_contract_test.exs"
      provides: "Regression coverage for the new guide section and anchor"
    - path: "test/threadline/readme_doc_contract_test.exs"
      provides: "Regression coverage for the example README walkthrough"
  key_links:
    - from: "guides/domain-reference.md"
      to: "examples/threadline_phoenix/README.md"
      via: "cross-link from the new Time Travel hub to the runnable example"
      pattern: "Time Travel \\(As-of\\).*examples/threadline_phoenix/README.md"
    - from: "test/threadline/exploration_routing_doc_contract_test.exs"
      to: "guides/domain-reference.md"
      via: "anchor and section assertions"
      pattern: "Time Travel \\(As-of\\)|ASOF-06"
    - from: "test/threadline/readme_doc_contract_test.exs"
      to: "examples/threadline_phoenix/README.md"
      via: "walkthrough assertions"
      pattern: "ThreadlinePhoenix.Post|as_of|cast: true"
---

<objective>
Document the existing single-row `as_of/4` contract where operators already look for guidance, then prove the docs stay in sync with contract tests.

Purpose: keep Phase 40 narrowly focused on discoverable, copy-pasteable time-travel guidance rather than new reconstruction behavior.
Output: updated operator guide, updated Phoenix example README, and refreshed doc-contract tests.
</objective>

<execution_context>
@/Users/jon/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jon/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-temporal-operator-guides/40-CONTEXT.md
@.planning/phases/40-temporal-operator-guides/40-RESEARCH.md
@guides/domain-reference.md
@examples/threadline_phoenix/README.md
@test/threadline/exploration_routing_doc_contract_test.exs
@test/threadline/readme_doc_contract_test.exs

<interfaces>
From `test/threadline/exploration_routing_doc_contract_test.exs`:
```elixir
defmodule Threadline.ExplorationRoutingDocContractTest do
  @moduledoc false
  use ExUnit.Case, async: true
```

From `test/threadline/readme_doc_contract_test.exs`:
```elixir
defmodule Threadline.ReadmeDocContractTest do
  @moduledoc false
  use Threadline.DataCase
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Time Travel hub to the operator guide</name>
  <files>guides/domain-reference.md, test/threadline/exploration_routing_doc_contract_test.exs</files>
  <action>Add a single `## Time Travel (As-of)` hub section beside the existing exploration/support material per D-01 and D-02. Keep it compact per D-03, D-07, D-08, and D-09: define `as_of/4`, show the default map result, and call out deleted records, genesis gaps, and `cast: true` in a small behavior table or equivalent block. Update the doc-contract test so it locks the new section, the anchor, and the required behavior phrases; use the existing style from the exploration routing test rather than inventing a new pattern.</action>
  <verify><automated>mix test test/threadline/exploration_routing_doc_contract_test.exs --seed 0</automated></verify>
  <done>The guide exposes the Time Travel hub in the right neighborhood and the test fails if the section, anchor, or edge-case wording disappears.</done>
</task>

<task type="auto">
  <name>Task 2: Add the runnable walkthrough to the Phoenix example README</name>
  <files>examples/threadline_phoenix/README.md, test/threadline/readme_doc_contract_test.exs</files>
  <action>Extend the main README flow with one short, runnable historical reconstruction walkthrough per D-04 and D-05. Use `ThreadlinePhoenix.Post` as the concrete example per D-10, and keep the content teaching usage/ergonomics rather than repeating the full API per D-06 and D-12. Explicitly mention the deleted-record note and the `cast: true` example per D-07 and D-09, then strengthen the README doc-contract test so the walkthrough and required literals stay present.</action>
  <verify><automated>mix test test/threadline/readme_doc_contract_test.exs --seed 0</automated></verify>
  <done>The Phoenix example README shows one copy-pasteable time-travel walkthrough in the main usage path and the test fails if the walkthrough or required literals drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown docs → operator behavior | Readers may copy the example into production without the right auth/tenancy wrapper. |
| Doc source → contract tests | Tests must keep the guide/README aligned with the locked semantics from Phase 39. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | I | examples/threadline_phoenix/README.md | mitigate | Keep the production warning that auth and tenancy are required before exposing drill-down or time-travel examples. |
| T-40-02 | T | test/threadline/*doc_contract_test.exs | mitigate | Assert the new anchor, section title, and required literals so doc drift fails CI immediately. |
| T-40-03 | R | markdown docs | accept | No audit log is introduced by this docs-only phase; the risk is bounded to reader misunderstanding. |
</threat_model>

<verification>
[Overall phase checks]
- `mix test test/threadline/exploration_routing_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs --seed 0` passes.
- `guides/domain-reference.md` contains a Time Travel hub near the exploration/support sections.
- `examples/threadline_phoenix/README.md` contains one runnable historical reconstruction walkthrough in the main flow.
</verification>

<success_criteria>
- ASOF-06 is represented in the roadmap and implemented in docs.
- The domain guide and example README both document `as_of/4` without changing runtime behavior.
- The doc-contract tests fail if either docs surface loses the locked anchor or required walkthrough text.
</success_criteria>

<output>
After completion, create `.planning/phases/40-temporal-operator-guides/40-01-SUMMARY.md`
</output>
