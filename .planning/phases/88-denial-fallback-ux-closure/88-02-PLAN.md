---
phase: 88-denial-fallback-ux-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - "88-01"
files_modified:
  - lib/threadline/operator_surface/unsupported.ex
  - lib/threadline/operator_surface/live/export_status_live.ex
  - guides/operator-surface.md
  - examples/threadline_phoenix/README.md
  - guides/getting-started-saas.md
  - test/threadline/operator_surface/live/export_status_live_test.exs
  - test/threadline/operator_surface_doc_contract_test.exs
  - test/threadline/example_phoenix_readme_contract_test.exs
  - test/threadline/getting_started_saas_doc_contract_test.exs
autonomous: true
requirements:
  - UX-01
  - UX-02
must_haves:
  truths:
    - "Export denial prefers an exact dry-run fallback command only when the current filter state can be represented safely and truthfully."
    - "When exact export fallback generation is not safe or not possible, the denial shell degrades to a generic but truthful export transport hint instead of a fake example command."
    - "Operator docs and example guidance teach one coherent posture: hidden unavailable actions, explicit direct-route messaging, hard HTTP `403` export denial, and named fallback transports."
  artifacts:
    - path: lib/threadline/operator_surface/live/export_status_live.ex
      provides: "Denied export route with exact-or-generic fallback command selection"
      contains: "mix threadline.export --dry-run"
    - path: guides/operator-surface.md
      provides: "Canonical support-lane denial and fallback contract"
      contains: "Action Denied"
    - path: test/threadline/operator_surface_doc_contract_test.exs
      provides: "Locked docs proof for denial/fallback posture"
      contains: "plain-text `403`"
  key_links:
    - from: lib/threadline/operator_surface/live/export_status_live.ex
      to: lib/mix/tasks/threadline.export.ex
      via: "exact fallback generation only emits CLI flags the export task actually supports"
      pattern: "from|to|table|actor_json"
    - from: guides/operator-surface.md
      to: test/threadline/operator_surface_doc_contract_test.exs
      via: "support-lane denial and fallback wording is locked as public contract"
      pattern: "Action Denied|plain-text `403`|mix threadline.export --dry-run"
    - from: examples/threadline_phoenix/README.md
      to: test/threadline/example_phoenix_readme_contract_test.exs
      via: "example app teaches the same canonical fallback posture as the main operator guide"
      pattern: "export_authorize_fn|403|mix threadline.export --dry-run"
    - from: guides/getting-started-saas.md
      to: test/threadline/getting_started_saas_doc_contract_test.exs
      via: "first-hour guide names the same denial and fallback transports on the canonical `/audit` path"
      pattern: "mix threadline.export --dry-run|mix threadline.health.coverage|mix threadline.policy.show"
---

<objective>
Finish the support-lane denial UX by making export fallback guidance exact when
truthfully derivable, generic when not, and by locking the same posture into
the public docs and example app.

Purpose: satisfy the remaining user-facing part of `UX-01` and the doc/example
alignment required by `UX-02`.
Output: exact-or-generic export fallback selection on the denied route plus
doc-contract coverage for the named fallback story.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-denial-fallback-ux-closure/88-CONTEXT.md
@.planning/phases/88-denial-fallback-ux-closure/88-RESEARCH.md
@.planning/phases/88-denial-fallback-ux-closure/88-UI-SPEC.md
@lib/threadline/operator_surface/unsupported.ex
@lib/threadline/operator_surface/live/export_status_live.ex
@lib/mix/tasks/threadline.export.ex
@guides/operator-surface.md
@examples/threadline_phoenix/README.md
@guides/getting-started-saas.md
@test/threadline/operator_surface/live/export_status_live_test.exs
@test/threadline/operator_surface_doc_contract_test.exs
@test/threadline/example_phoenix_readme_contract_test.exs
@test/threadline/getting_started_saas_doc_contract_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive exact export fallback commands only from safe current-state filters</name>
  <files>lib/threadline/operator_surface/unsupported.ex, lib/threadline/operator_surface/live/export_status_live.ex, test/threadline/operator_surface/live/export_status_live_test.exs</files>
  <read_first>
    <file>lib/threadline/operator_surface/unsupported.ex</file>
    <file>lib/threadline/operator_surface/live/export_status_live.ex</file>
    <file>lib/mix/tasks/threadline.export.ex</file>
    <file>test/threadline/operator_surface/live/export_status_live_test.exs</file>
    <file>.planning/phases/88-denial-fallback-ux-closure/88-CONTEXT.md</file>
  </read_first>
  <action>Add a helper-backed export fallback path that inspects the current denied route state and emits an exact `mix threadline.export --dry-run ...` command only when the active filter state can be represented safely with the CLI's currently supported flags. On the current tree, treat `table`, `from`, and `to` as the exact subset; if actor-scoped or correlation-scoped filters are present, degrade to a generic truthful export fallback instead of inventing lossy example arguments. Keep this logic out of the HEEx template and make the generic/exact choice explicit in tests.</action>
  <acceptance_criteria>
    <criterion>The denied export route can render an exact dry-run command from the currently supported CLI subset: `table`, `from`, and `to`.</criterion>
    <criterion>Actor-scoped, correlation-scoped, unsupported, or partial state falls back to a generic truthful export hint rather than a fake exact command.</criterion>
    <criterion>`test/threadline/operator_surface/live/export_status_live_test.exs` proves both the exact and generic denial-fallback paths.</criterion>
    <criterion>`mix test test/threadline/operator_surface/live/export_status_live_test.exs --max-failures 1` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/export_status_live_test.exs --max-failures 1</automated>
  </verify>
  <done>The export denial shell now prefers exact fallback guidance when it can do so truthfully and safely.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the denial and fallback posture in guides and example docs</name>
  <files>guides/operator-surface.md, examples/threadline_phoenix/README.md, guides/getting-started-saas.md, test/threadline/operator_surface_doc_contract_test.exs, test/threadline/example_phoenix_readme_contract_test.exs, test/threadline/getting_started_saas_doc_contract_test.exs</files>
  <read_first>
    <file>guides/operator-surface.md</file>
    <file>examples/threadline_phoenix/README.md</file>
    <file>guides/getting-started-saas.md</file>
    <file>test/threadline/operator_surface_doc_contract_test.exs</file>
    <file>test/threadline/example_phoenix_readme_contract_test.exs</file>
    <file>test/threadline/getting_started_saas_doc_contract_test.exs</file>
    <file>.planning/phases/88-denial-fallback-ux-closure/88-UI-SPEC.md</file>
  </read_first>
  <action>Update the operator guide, example README, and first-hour getting-started guide so they all teach the same support-lane story: keep one canonical `/audit` tree, hide unavailable actions in normal navigation, show explicit direct-route denial or unsupported messaging, keep export HTTP requests on plain-text `403`, and name fallback transports honestly. Extend the existing doc-contract tests so the repo treats this wording as a public contract rather than incidental prose.</action>
  <acceptance_criteria>
    <criterion>`guides/operator-surface.md` explicitly names `Action Denied`, hard HTTP `403`, and truthful fallback transport guidance.</criterion>
    <criterion>The example README and getting-started guide teach the same canonical support-lane denial and fallback posture.</criterion>
    <criterion>Doc-contract tests lock the repaired wording and reject drift back toward fake fallback examples or route ambiguity.</criterion>
    <criterion>`mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs --max-failures 1` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The public docs and example path now teach the same denial and fallback truth the code enforces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filter state -> suggested CLI command | The denied export page must not emit commands that imply unsupported parity or omit active constraints. |
| Public docs -> adopter implementation | Canonical docs directly shape how hosts explain and validate the support lane. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-04 | Repudiation | fallback command generation | mitigate | Emit exact export commands only from supported, lossless filter state; otherwise degrade to a generic truthful hint. |
| T-88-05 | Information disclosure | overclaimed fallback parity | mitigate | Keep coverage, policy, and retention fallback wording constrained to what the underlying transport actually supports. |
| T-88-06 | Spoofing / documentation drift | guides and example README | mitigate | Lock the support-lane denial and fallback wording with doc-contract tests. |
</threat_model>

<verification>
Run the export-status and doc-contract tests, then confirm the repaired tree now
teaches one stable rule: exact export fallback only when safely derivable,
generic fallback otherwise, with docs and example guidance matching the code.
</verification>

<success_criteria>
Denied export UX is truthful about exact-vs-generic fallback guidance, and the
canonical docs, example app, and tests all teach the same support-lane
behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/88-denial-fallback-ux-closure/88-02-SUMMARY.md`
</output>
