---
phase: 114-release-0-6-0-packaging
plan: 03
type: execute
wave: 3
depends_on:
  - 114-01-PLAN.md
  - 114-02-PLAN.md
files_modified:
  - CONTRIBUTING.md
  - test/threadline/release_artifact_contract_test.exs
autonomous: true
requirements:
  - REL-03
must_haves:
  truths:
    - "CONTRIBUTING maintainer runbook references v0.6.0 and matches @version 0.6.0."
    - "`mix verify.release` passes on a clean working tree."
  artifacts:
    - path: CONTRIBUTING.md
      provides: "Post-merge tag/publish checklist for 0.6.0"
---

<objective>
Finish REL-03 publish-ready boundary: refresh CONTRIBUTING maintainer literals and run full `mix verify.release` pre-flight on a clean tree.

Purpose: Maintainers have accurate tag examples; packaging drift is caught before human Hex publish.
Output: Updated CONTRIBUTING + green `mix verify.release`.
</objective>

<context>
@.planning/phases/114-release-0-6-0-packaging/114-CONTEXT.md (D-114-05)
@.planning/phases/114-release-0-6-0-packaging/114-RESEARCH.md (§5)
@CONTRIBUTING.md (lines ~120–145)
@test/threadline/release_artifact_contract_test.exs (CONTRIBUTING test)
@.github/workflows/hex-publish.yml
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-114-04 | Tag/version mismatch on publish | medium | verify.release + hex-publish.yml @version check; CONTRIBUTING names v0.6.0 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refresh CONTRIBUTING release runbook to v0.6.0</name>
  <files>CONTRIBUTING.md, test/threadline/release_artifact_contract_test.exs</files>
  <read_first>
    - CONTRIBUTING.md (Tag-triggered publish section)
    - test/threadline/release_artifact_contract_test.exs (`CONTRIBUTING carries` test)
    - .planning/milestones/v1.14-phases/48-threadline-0.3.0-release/48-CONTEXT.md (D-06–D-11 publish model)
  </read_first>
  <action>
In **CONTRIBUTING.md**, replace every maintainer-facing **`v0.3.0`** and **`0.3.0`** release-example literal with **`v0.6.0`** / **`0.6.0`**:

- Example tag line: `git tag -a v0.6.0 -m "Release v0.6.0"`
- Push example: `git push origin v0.6.0`
- Checklist title referencing preparing the `v0.6.0` release
- Step 5: Tag `v0.6.0` matching `@version "0.6.0"` in `mix.exs`
- Workflow explanation example may keep generic `v0.3.0` **only** in the GITHUB_REF_NAME explanatory parenthetical OR update to `v0.6.0` for consistency — prefer **all examples use 0.6.0**.

Preserve `mix verify.release` and `wait for green CI on main before tagging` prose unchanged.

In **release_artifact_contract_test.exs** CONTRIBUTING test: change `v0.3.0` assert to `v0.6.0`.
  </action>
  <acceptance_criteria>
    - `grep 'v0.6.0' CONTRIBUTING.md` returns ≥2 matches
    - `grep 'v0.3.0' CONTRIBUTING.md` returns no matches
    - `grep 'v0.6.0' test/threadline/release_artifact_contract_test.exs` matches
    - `mix test test/threadline/release_artifact_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/release_artifact_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 2: Run mix verify.release on clean tree</name>
  <files></files>
  <read_first>
    - mix.exs `verify_release/1` helper
    - .planning/phases/114-release-0-6-0-packaging/114-CONTEXT.md (D-114-05f scope)
  </read_first>
  <action>
1. Ensure working tree is clean: `git status --porcelain` must be empty. If dirty, commit phase changes first (all plans 01–03 files).

2. Run: `mix verify.release`

3. If `mix hex.build` or `mix docs` fails, fix only release-surface issues (do not expand scope into feature work).

**Out of scope for this task:** `git tag`, `git push`, `mix hex.publish`, hex.pm verification.
  </action>
  <acceptance_criteria>
    - `git status --porcelain` output is empty before verify.release
    - `mix verify.release` exits 0
  </acceptance_criteria>
  <verify>git diff --quiet HEAD -- && mix verify.release</verify>
</task>

<task type="auto">
  <name>Task 3: Phase closeout — ci.all</name>
  <files></files>
  <read_first>
    - mix.exs `ci.all` alias list
    - test/threadline/ci_topology_contract_test.exs
  </read_first>
  <action>
Run `mix ci.all` to confirm contributor CI topology still excludes `verify.release` and all gates pass after release-surface edits.

If Postgres unavailable locally, document skip — but attempt run in normal dev environment.
  </action>
  <acceptance_criteria>
    - `mix ci.all` exits 0
    - `grep 'verify.release' mix.exs` does not appear inside the `ci.all` list body
  </acceptance_criteria>
  <verify>mix ci.all</verify>
</task>

</tasks>

<verification>
- [ ] `mix test test/threadline/release_artifact_contract_test.exs test/threadline/ci_topology_contract_test.exs`
- [ ] `mix verify.release` (clean tree)
- [ ] `mix ci.all`
</verification>

<manual_followup>
Post-merge maintainer gate (not phase verification):

```bash
git status --porcelain
mix verify.release
mix ci.all
git tag -a v0.6.0 -m "Release v0.6.0"
git push origin v0.6.0
mix hex.info threadline
```
</manual_followup>
