---
phase: 198-green-bringup
plan: 06
type: execute
wave: 4
depends_on: ["198-04", "198-05"]
files_modified:
  - .github/workflows/ui-critic.yml
  - .github/workflows/hex-publish.yml
  - .github/workflows/flake-detection.yml
  - .github/workflows/release.yml
  - test/threadline/ci_topology_contract_test.exs
  - .planning/ARCHIVE-REGISTER.md
  - .planning/audits/198-phase166-diff.md
autonomous: false
requirements: [GREEN-09, GREEN-10, GREEN-11, GREEN-12]

estimate:
  tokens: 108000
  raw_tokens: 54000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "No workflow in `.github/workflows/` references the paid critic API key, and the workflow that carried the scoring input no longer exists — the input and the billing trigger are absent, not defaulted off (GREEN-09, D-24)."
    - "Exactly one workflow invokes the Hex publish command, and it is the one already carrying the CI-green poll, the hard dependency, the release-shape and build verification, the already-published idempotency skip, and post-publish verification (GREEN-10, D-26)."
    - "Both facts above are asserted by tests in `test/threadline/ci_topology_contract_test.exs`, which is already wired into the release verification alias — a test, not a comment (D-25)."
    - "The surviving publish job runs inside a GitHub Environment named `production-hex` carrying a required-reviewer rule, so the irreversible step needs a human click, and its authentication is expressed as one swappable block so trusted-publishing migration is a single-block replacement when the registry ships it (D-27)."
    - "Flake Detection names a run-1 failure as broken and a later-iteration failure as flaky, carries a time bound, and creates-or-updates a single tracking issue rather than opening one per run (GREEN-11, D-35)."
    - "`git worktree list` shows exactly one entry and no non-main, non-archive local branches remain (GREEN-12)."
    - "Every removed branch exists as an annotated tag named `archive/<original-branch-name>` whose message carries SHA, base, ancestry verification, diffstat, archive date and requirement, the merge-or-archive recommendation, rationale, restore command, and a register pointer (D-31)."
    - "`.planning/ARCHIVE-REGISTER.md` has one row per archived ref with ref, SHA, date, reason, recommendation, and restore command (D-31)."
    - "The phase-166 diff, summary and recommendation exist and are committed BEFORE anything is removed (D-33, D-34 step 1)."
    - "GREEN-11 empty edge: the broken-vs-flaky classifier states its behaviour when the suite produces no failure at all, and no issue is created in that case."
    - "GREEN-12 adjacency edge: a branch whose tip is exactly equal to `main`'s tip, or is a strict ancestor of main, is classified as merged and needs no archive tag; only a non-ancestor tip earns one."
    - "GREEN-12 empty edge: if the repository has no stale branches at the time of execution, the register records that explicitly rather than being left absent."
    - statement: "GREEN-11 encoding edge: the classifier's parse of the repeat-until-failure output is anchored on a stable marker, and if the output format is not what the parser expects the workflow reports unknown rather than silently classifying as flaky."
      verification: backstop
    - statement: "GREEN-12 ordering edge: archive tags are created and verified before any branch deletion or worktree removal, and the register row is written before the delete — the ordering is the safety property, not a formality."
      verification: backstop
  artifacts:
    - .planning/ARCHIVE-REGISTER.md
    - .planning/audits/198-phase166-diff.md
    - test/threadline/ci_topology_contract_test.exs
  key_links:
    - "The actual paid API client lives in the example app's e2e critic directory and a shell script, neither of which enters the Hex tarball. Deleting the workflow makes them unreachable from CI — that is what satisfies 'the billing code path is absent'."
    - "`mix verify.ui_critique` is maintainer-local, requires a key in the operator's own shell, and is excluded from the aggregate verification alias — leave it alone."
    - "The two critic mix tasks under `lib/mix/tasks/` do ship in the tarball but make no API calls; suppressing them from the docs index is Phase 200's job, not this phase's."
    - "Deleting `ui-critic.yml` MUST precede the push to main: 587 commits will match its operator-namespace path filter and fire the parked paid lane."
  prohibitions:
    - statement: "MUST NOT keep a stripped skeleton of the paid critic workflow. A workflow with the input removed IS the defaulted-off state the requirement explicitly rejects, and it invites a one-line resurrection."
      status: flagged-unverified
      verification: manual
    - statement: "MUST NOT delete a branch, remove a worktree, or discard unmerged work before its archive tag has been created AND its existence verified by resolving the tag object. Losing the only copy of real unmerged work is the failure this requirement exists to prevent."
      status: flagged-unverified
      verification: manual
  flagged_assumptions:
    - "GREEN-09's edge-probe row returned `unclassified — review manually` and stays unresolved. Planner assumption: the operative edge is a partial removal that leaves the trigger reachable, which the delete-outright decision and the zero-reference assertion cover. Surfaced, not dismissed."
    - "GREEN-10's edge-probe row returned `unclassified — review manually` and stays unresolved. Planner assumption: the operative edge is two publish paths racing, which the exactly-one assertion covers. Surfaced, not dismissed."
    - "RESEARCH A5: the repeat-until-failure output format was not verified, so the classifier's parse is unproven. Task 4 must run the command locally once and anchor the parser on what it actually prints, and report unknown rather than guessing when the format does not match."
    - "Revoking the paid API key from the repository's secret store is a GitHub Settings action outside any file this phase edits. Task 3 surfaces it as an explicit maintainer decision rather than assuming the workflow deletion removed the secret."
---

<objective>
Close the three irreversible hazards before anything reaches `main`: make the paid critic lane structurally untriggerable, collapse two Hex publish paths to the one that is gated, and preserve every piece of real unmerged work under a findable archive tag before removing it. Then make Flake Detection tell the truth about what it found.

Purpose: pushing 587 commits will match the paid critic workflow's path filter and fire a lane that is parked on ratified spend grounds. A legacy tag-triggered publish workflow wins the publish race by construction against the properly gated one. A branch carrying verified-unmerged work exists only on this laptop. All three must be settled before the push, in the order D-34 fixes.
Output: two workflows deleted with executable resurrection guards, a hardened Flake Detection lane, archive tags plus a register, and a single worktree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch and worktree triage — diff and archive before anything is removed (D-31, D-33, D-34 step 1)</name>
  <files>.planning/audits/198-phase166-diff.md, .planning/ARCHIVE-REGISTER.md</files>
  <read_first>
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` — D-31 (the annotated-tag message contract and the register columns), D-33 (the decision rule: archive by default, cherry-pick only for unique unshipped value), D-34 step 1 (the gate and the abort branch)
    - `.planning/ROADMAP.md` §"Phase 198" Notes — the statement that the phase-166 branch is verified not an ancestor of main and is therefore real unmerged work
    - the current output of `git worktree list` and `git branch -vv` — establish the live state rather than trusting this plan's snapshot
  </read_first>
  <action>
    Establish state first. Record verbatim the output of `git worktree list`, `git branch -vv`, and for each non-main local branch `git merge-base --is-ancestor <branch> main; echo $?`. The expected state at planning time was two worktrees (the repository root and a phase-166 worktree) and three branches (main, the phase-166 branch, and a branch named for a pre-release cleanup backup); verify rather than assume.

    Produce the artifact BEFORE any removal. Write `.planning/audits/198-phase166-diff.md` containing, for the phase-166 branch: the tip SHA, the merge base against main, the explicit ancestry verification output, `git diff --stat main...<branch>`, the full `git diff main...<branch>` as a fenced block or an explicit file-by-file summary if it is too large to inline, and a `## Recommendation` section applying D-33's rule — archive by default; cherry-pick only if the diff carries unique unshipped value. Weigh two questions in writing: does it duplicate work already on main, and does it touch files this milestone is about to rewrite. The branch is operator-surface routing and style-contract work, and Phases 201 and 204 rewrite exactly those areas — that weighs toward archive, but state the reasoning rather than asserting the conclusion.

    Then create the archive tags. For each branch being retired, create an ANNOTATED tag named `archive/<original-branch-name>` whose message carries all nine elements D-31 requires: SHA, base, ancestry verification result, diffstat, archive date, the requirement id GREEN-12, the merge-or-archive recommendation, the rationale, the restore command, and a pointer to `.planning/ARCHIVE-REGISTER.md`. Apply this to BOTH the phase-166 branch and the pre-release-cleanup backup branch — a branch named backup is an archive tag wearing the wrong hat.

    Verify each tag resolves before deleting anything: `git cat-file -p archive/<name>` must print the annotated tag object with the message intact. If ANY tag fails to resolve, STOP and delete nothing — that is D-34 step 1's abort branch.

    Create `.planning/ARCHIVE-REGISTER.md` with one row per archived ref and the six columns ref, SHA, date, reason, recommendation, restore command. If there are no stale branches at execution time, the register still exists and says so explicitly.

    Only after all tags are verified and the register is committed: remove the phase-166 worktree with `git worktree remove`, then delete the retired branches with `git branch -D`. Confirm `git worktree list` shows one entry.

    Do not push the tags in this plan — pushing archive tags is a separate recorded decision taken in Plan 07 alongside the main push.
  </action>
  <verify>
    <automated>bash -c 'test "$(git worktree list | wc -l | tr -d " ")" = "1" && test -f .planning/ARCHIVE-REGISTER.md && for t in $(git tag -l "archive/*"); do git cat-file -p "$t" > /dev/null || exit 1; done && test -n "$(git tag -l "archive/*")"'</automated>
  </verify>
  <acceptance_criteria>
    - `git worktree list | wc -l` equals 1.
    - `git branch --format='%(refname:short)' | grep -v '^main$'` returns no output.
    - `git tag -l 'archive/*'` returns at least the two expected tags, and each is an annotated tag (`git cat-file -t` reports `tag`, not `commit`).
    - Each archive tag's message contains all nine D-31 elements; verify by grepping the message for the literals SHA, base, diffstat, GREEN-12, recommendation, restore, and ARCHIVE-REGISTER.
    - `.planning/audits/198-phase166-diff.md` exists with a `## Recommendation` section that names either archive or cherry-pick and gives the reasoning against both D-33 questions.
    - `git log --format=%H --diff-filter=A -- .planning/audits/198-phase166-diff.md` resolves to a commit that is an ancestor of the commit deleting the branches — the artifact provably predates the removal.
    - `.planning/ARCHIVE-REGISTER.md` has a header row with the six columns and one row per archived ref.
  </acceptance_criteria>
  <done>Every piece of unmerged work is preserved under a verified annotated tag with a recorded recommendation, the register is findable, and the repository has one worktree and one branch.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: One-way decision gate — collapse to a single Hex publish path</name>
  <files>.planning/phases/198-green-bringup/198-TRIAGE.md</files>
  <read_first>
    - `.github/workflows/hex-publish.yml` — read in full: its own header describing itself as a legacy fallback, its tag trigger, and its ungated publish step
    - `.github/workflows/release.yml` — lines 221-234 (the CI-green poll), 276-281 (the hard dependency), 308 (version-to-tag agreement), 319-323 (release-shape verification and build), 326-333 (already-published idempotency skip), 362 (post-publish verification), and 13-27 (the manual dispatch that becomes the recovery path)
  </read_first>
  <reversibility rating="one-way">Publishing to Hex is irreversible in effect — the registry offers roughly a one-hour revert window and then it is a support request. Deleting the ungated tag-triggered path removes a fallback; keeping it leaves a path that publishes within a minute while the gated path is still polling. The gates protecting the surviving path must never be weakened.</reversibility>
  <decision>Delete the legacy tag-triggered publish workflow, leaving exactly one gated publish path — and decide whether to revoke the paid critic API key from the repository secret store.</decision>
  <context>
    D-26 locks the outcome; this checkpoint exists because the consequence is irreversible in effect and carries a stated abort branch that must be exercised as a judgement, not assumed away. The legacy workflow fires on any version tag push and publishes with no CI gate, while the gated path polls for CI-green for up to thirty minutes — so the ungated path wins the race by construction. Separately, deleting a workflow does not revoke a repository secret, and that residual is easy to leave behind silently.
  </context>
  <options>
    <option id="delete">
      <name>Delete the legacy path (D-26 as locked)</name>
      <pros>Exactly one publish path, and it is the one carrying all five pre-publish gates; the race disappears; recovery is the gated workflow's manual dispatch</pros>
      <cons>Loses the tag-triggered fallback entirely</cons>
    </option>
    <option id="keep-both">
      <name>Abort — keep both paths</name>
      <pros>Correct answer if the surviving workflow cannot be shown green end to end; a broken single path is worse than a racy dual path</pros>
      <cons>The ungated path remains able to publish an unverified release within a minute of any tag push</cons>
    </option>
  </options>
  <resume-signal>Reply "delete" or "keep both", plus "revoke key" or "keep key" for the secret-store question. Both answers are recorded with timestamps; neither is inferred from silence.</resume-signal>
  <action>
    Present to the maintainer, in one message:
    1. Why the legacy path wins the race by construction: it fires on any version tag push and publishes with no CI gate, while the gated path sits in a poll of up to thirty minutes; the release automation's token is a fine-grained personal access token, and tags created with such a token do trigger workflows.
    2. What is lost by deleting it: the tag-triggered fallback. The recovery path becomes the gated workflow's manual dispatch.
    3. What is kept: all five pre-publish gates on the surviving path, none of which may be weakened.
    4. The abort branch, stated in advance: if the surviving workflow cannot be shown green end to end, keep BOTH and stop. A broken single path is worse than a racy dual path.
    5. The open question to decide now: whether to also revoke the paid critic API key from the repository secret store, which deleting a workflow does not do.

    Ask for an explicit confirm-or-abort on the deletion, and a yes-or-no on the secret revocation. Do not proceed on silence.

    Record both answers, with the date, in a `## Publish-path and secret-store decisions` section appended to `.planning/phases/198-green-bringup/198-TRIAGE.md`. If the answer is abort, stop the phase here — Plan 07 must not push.
  </action>
  <verify>
    <automated>grep -q '^## Publish-path and secret-store decisions' .planning/phases/198-green-bringup/198-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/198-green-bringup/198-TRIAGE.md` contains a `## Publish-path and secret-store decisions` section.
    - That section records an explicit confirm or abort on deleting the legacy publish workflow, with a date.
    - That section records an explicit yes or no on revoking the paid critic key from the repository secret store, with a date and, if yes, the completion timestamp.
    - The section restates the abort branch: if the surviving workflow cannot be shown green end to end, both are kept.
  </acceptance_criteria>
  <done>The one-way decision to collapse to a single publish path is taken and recorded with a human confirmation, and the secret-store question is answered rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Delete both hazard workflows and guard resurrection with tests (D-24, D-25, D-26, D-34 steps 2-3)</name>
  <files>.github/workflows/ui-critic.yml, .github/workflows/hex-publish.yml, test/threadline/ci_topology_contract_test.exs</files>
  <read_first>
    - `.github/workflows/ui-critic.yml` — read in full before deleting, so the SUMMARY can record exactly what was removed: its push trigger and operator-namespace path filter, its scoring input, its API key binding, and its paid-runner branch
    - `.github/workflows/hex-publish.yml` — read in full before deleting
    - `test/threadline/ci_topology_contract_test.exs` — read in full; lines 5-9 give the repository-root file-read helper and lines 11-21 and 91-97 give the string-containment and regex assertion idioms to copy
    - `mix.exs` line 158 — confirms this test file is already wired into the release verification alias, so the new assertions need no new harness
    - `.planning/phases/198-green-bringup/198-PATTERNS.md` §"D-25" — the two assertion shapes, including the reason both must assert over a glob of all workflow files rather than a hardcoded filename
  </read_first>
  <behavior>
    - The zero-reference assertion fails if any file under the workflows directory names the paid critic API key.
    - The single-publish-path assertion fails if zero workflows or more than one workflow invokes the publish command — an empty result must fail, not pass.
    - Both assertions glob all workflow files, so a future workflow addition is covered without editing the test.
  </behavior>
  <action>
    Order matters and is gated. Do step 2 before step 3, and do not start either until Task 2 recorded a confirm.

    Step 2 — delete `.github/workflows/ui-critic.yml` outright with `git rm`. Do not leave a stripped skeleton: a workflow with the input removed is exactly the defaulted-off state the requirement rejects, and it invites a one-line resurrection. Gate: after deletion, `grep -rl ANTHROPIC .github/` must return nothing. Leave the maintainer-local critique alias in `mix.exs` alone — it requires a key in the operator's own shell and is excluded from the aggregate verification alias.

    Step 3 — delete `.github/workflows/hex-publish.yml` with `git rm`, then add the two guard assertions to `test/threadline/ci_topology_contract_test.exs`:
    - One test globs every workflow file and refutes that any contains the paid critic API key name, with a failure message naming the file and stating that the paid lane must stay structurally unreachable from CI.
    - One test collects every workflow file that contains the publish command and asserts the collection equals exactly the single-element list naming the release workflow. Assert equality against that exact list, not merely a length or a refutation, so an accidentally empty glob cannot launder a false pass.

    Gate for step 3: `mix verify.release` must be green, and `grep -rl "hex.publish" .github/workflows/` must return exactly the release workflow. ABORT branch: if the release workflow cannot be shown green end to end, restore both files and stop the phase — a broken single path is worse than a racy dual path.

    Step 3b — put a human click in front of the irreversible step (D-27). The registry's trusted-publishing / OIDC flow is announced but not generally available, so the long-lived API key stays for now; mitigate it rather than pretend otherwise. Create a GitHub Environment named `production-hex` with a required-reviewer rule naming the maintainer, via the environments API. Bind the publish job in `.github/workflows/release.yml` to that environment with an `environment:` key so the job pauses for approval before publishing. Confirm the key's scope is publish-only and record the scope in the SUMMARY. Restructure the job's authentication into ONE contiguous, commented block so the future migration to trusted publishing is a single-block replacement rather than a diff spread across the job — add a comment on that block naming it as the swap point. Do not weaken any of the five existing pre-publish gates while doing this; the environment gate is added in front of them, not in place of any of them.

    Prove both new assertions have teeth before committing: temporarily reintroduce the key name into a scratch workflow file and confirm the first test goes red; temporarily add the publish command to a second workflow and confirm the second test goes red naming both files. Remove both and confirm green. Record all four outcomes in the SUMMARY.

    Record in the SUMMARY that the two critic mix tasks under `lib/mix/tasks/` still ship in the tarball, make no API calls, and are therefore Phase 200's documentation-suppression work rather than a billing path.
  </action>
  <verify>
    <automated>mix test test/threadline/ci_topology_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ui-critic.yml` and `.github/workflows/hex-publish.yml` no longer exist, and both appear in `git log --diff-filter=D --name-only` for this plan.
    - `grep -rl ANTHROPIC .github/` returns no output.
    - `grep -rl 'hex.publish' .github/workflows/` returns exactly `.github/workflows/release.yml`.
    - `mix test test/threadline/ci_topology_contract_test.exs` exits 0 and the file contains two new tests, one per guard.
    - The single-publish-path test asserts list equality against a one-element list, not a count or a bare refutation.
    - `mix verify.release` exits 0.
    - `grep -c 'verify.ui_critique' mix.exs` is unchanged from before this task — the maintainer-local alias was not removed.
    - `yq '.jobs["publish-hex"].environment' .github/workflows/release.yml` resolves to `production-hex` (use the job's real id if it differs; verify from the file rather than assuming the name).
    - `gh api repos/:owner/:repo/environments/production-hex --jq '.protection_rules[] | .type'` includes `required_reviewers`.
    - `git diff HEAD~1 -- .github/workflows/release.yml` shows no deletion of any of the five pre-publish gate steps: the CI-green poll, the hard dependency, release-shape verification, the already-published idempotency skip, and post-publish verification.
    - The release workflow's authentication block is one contiguous region carrying a comment naming it as the trusted-publishing swap point.
    - The SUMMARY records all four teeth demonstrations, red and green, for both guards.
  </acceptance_criteria>
  <done>The paid lane cannot be triggered from CI and exactly one gated publish path exists, both facts asserted by tests already wired into the release verification alias.</done>
</task>

<task type="auto">
  <name>Task 4: Flake Detection tells the truth — broken versus flaky, bounded, deduplicated (D-35, GREEN-11)</name>
  <files>.github/workflows/flake-detection.yml</files>
  <read_first>
    - `.github/workflows/flake-detection.yml` — read in full, including the header comment stating the job id is stable and the name may evolve, and line 20's job name
    - `mix.exs` — the flake verification alias and the repeat count it passes, so the classifier keys on the real command
    - `.planning/phases/198-green-bringup/198-RESEARCH.md` §"Code Examples" — the deduplicated issue create-or-update idiom and the explicit warning that the repeat-until-failure output format was NOT verified
  </read_first>
  <action>
    Run the flake verification alias locally ONCE first and capture its real output for both a passing run and a deliberately failing one. Anchor the classifier on what it actually prints. Do not commit a parser built from an assumed format — a fragile regex against exact wording is precisely the kind of guard this phase exists to eliminate, and the research explicitly flagged this format as unverified.

    Then change the workflow to classify by name:
    - A failure on the FIRST iteration means the suite is broken, not flaky, and must be reported with the word broken in the issue title and body.
    - A failure on a later iteration means flaky.
    - If the output does not match the expected shape at all, report unknown and attach the raw log — never silently default to flaky, which would let a broken suite be filed as an intermittent annoyance.
    - If there is no failure, create and update nothing.

    Add a `timeout-minutes` bound to every job in this workflow, sized above the observed duration of a full repeat run.

    Replace any create-an-issue-per-run behaviour with a create-or-update: search open issues by a fixed title prefix and the flake-detection label, comment on the existing issue if one is found, create it only if none exists. Use a label distinct from the nightly-browser label added in Plan 05 so the two dedup streams do not merge into one issue. Include in the body the run id, the classification, and the first failing iteration number.

    Keep the job id unchanged — the header comment states it is relied upon. Evolve the name freely if the classification makes a better one available.

    Verify by dispatching the workflow manually and observing: a run that classifies correctly, and a second failing run that comments on the existing issue rather than opening a second one. Record both run ids and the issue number in the SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'yq "[.jobs[] | select(has(\"timeout-minutes\"))] | length" .github/workflows/flake-detection.yml | grep -qv "^0$" && grep -qi "broken" .github/workflows/flake-detection.yml'</automated>
  </verify>
  <acceptance_criteria>
    - Every job in `.github/workflows/flake-detection.yml` carries a `timeout-minutes` key.
    - The workflow contains a classification branch producing at least the three values broken, flaky, and unknown.
    - The workflow's issue step searches for an existing open issue by title prefix and label before creating one, and comments when found.
    - The label used differs from the nightly-browser label introduced in Plan 05.
    - `git diff HEAD~1 -- .github/workflows/flake-detection.yml | grep -E '^-[a-z-]+:$'` shows no job id was renamed.
    - The SUMMARY records the real captured output of the flake verification alias that the parser was anchored on, plus two dispatched run ids and the single issue number that both runs wrote to.
  </acceptance_criteria>
  <done>A suite that fails on the first iteration is reported as broken rather than flaky, the lane is time-bounded, and repeated failures accumulate on one tracking issue instead of spawning a new one per run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version tag push → package registry | The legacy workflow turns any tag push into an immediate, ungated public publish |
| push to main → paid third-party API | The parked critic workflow's path filter plus its API key binding turn a push into billable spend |
| local-only git objects → durability | Unmerged work exists on one laptop until an archive tag is created and later pushed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-06-01 | Elevation of Privilege | ungated tag-triggered publish workflow | critical | mitigate | It publishes within a minute with no CI gate while the gated path is still polling, and a publish is irreversible past roughly an hour. Deleted outright, with a test asserting exactly one publish path remains, and an abort branch if the surviving path cannot be shown green. |
| T-198-06-02 | Denial of Service | paid critic workflow fired by 587 commits | high | mitigate | The path filter matches the operator namespace and the workflow binds the paid API key. Deleted outright before any push to main, guarded by a zero-reference assertion over all workflow files. |
| T-198-06-03 | Information Disclosure | paid API key remaining in the repository secret store | medium | mitigate | Deleting a workflow does not revoke a secret. Surfaced as an explicit decision in the checkpoint and recorded with an answer, rather than assumed resolved. |
| T-198-06-07 | Elevation of Privilege | long-lived registry API key on the surviving publish path | high | mitigate | Trusted publishing is not generally available, so the key stays. Mitigated by binding the publish job to a `production-hex` Environment with a required-reviewer rule, so a compromised workflow change still cannot publish without a human click, and by scoping the key to publish-only. The authentication block is isolated so the migration is a single swap. |
| T-198-06-04 | Repudiation | branch deletion without an archive | high | mitigate | Tags are annotated and their existence is verified by resolving the tag object before any deletion; the register row is written first; a tag that fails to resolve aborts the step. |
| T-198-06-05 | Spoofing | a broken suite filed as merely flaky | medium | mitigate | The classifier reports unknown and attaches the raw log when the output does not match the expected shape, rather than defaulting to flaky. |
| T-198-06-06 | Tampering | resurrection of either deleted workflow | high | mitigate | Both guards are tests in a file already wired into the release verification alias, and both are demonstrated failing against a deliberate reintroduction before commit. |
</threat_model>

<verification>
- `grep -rl ANTHROPIC .github/` returns nothing
- `grep -rl 'hex.publish' .github/workflows/` returns exactly the release workflow
- `mix test test/threadline/ci_topology_contract_test.exs` exits 0
- `mix verify.release` exits 0
- `git worktree list | wc -l` equals 1 and no non-main local branch remains
- Every archive tag resolves via `git cat-file -p` and carries the nine-element message
- `.planning/ARCHIVE-REGISTER.md` exists with the six columns
</verification>

<success_criteria>
The paid lane is structurally untriggerable, exactly one gated publish path exists, both facts are asserted by tests rather than promised in comments, Flake Detection distinguishes broken from flaky on one deduplicated issue, and every piece of unmerged work is preserved under a verified annotated tag before the repository was reduced to one worktree and one branch.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan (new symbols — exclude from drift verification):
- `.planning/ARCHIVE-REGISTER.md`
- `.planning/audits/198-phase166-diff.md`
- annotated tags `archive/gsd/phase-166-unfreeze-token-lane-mechanism` and `archive/backup/pre-release-cleanup-2026-05-08`
- two new tests inside `test/threadline/ci_topology_contract_test.exs` (zero paid-key references; exactly one publish path)
- a `## Publish-path and secret-store decisions` section in `.planning/phases/198-green-bringup/198-TRIAGE.md`
- GitHub Environment `production-hex` with a required-reviewer protection rule (live GitHub state), and an `environment:` binding on the publish job in `.github/workflows/release.yml`
Deleted by this plan:
- `.github/workflows/ui-critic.yml`
- `.github/workflows/hex-publish.yml`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/198-green-bringup/198-06-SUMMARY.md` when done
</output>
