---
phase: 198-green-bringup
plan: 07
type: execute
wave: 5
depends_on: ["198-01", "198-06"]
files_modified:
  - bin/verify-branch-protection
  - .github/rulesets/main.json
  - .github/workflows/branch-protection.yml
  - .planning/audits/198-branch-protection-migration.md
autonomous: false
requirements: [GREEN-07, GREEN-08, GREEN-12]

estimate:
  tokens: 98000
  raw_tokens: 49000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`git log origin/main..main` is empty — origin carries every local commit (GREEN-07)."
    - "The latest run on `main` concludes success in twenty minutes or less, measured from the run's own start and update timestamps (GREEN-07)."
    - "Branch protection on `main` requires exactly one check context, the string `CI required`, and that name has provably been emitted on a real run — both halves asserted by a committed script, not a runbook (GREEN-08, D-12)."
    - "The protection configuration is exported and committed as `.github/rulesets/main.json`, so the rule set is git-tracked and auditable rather than living only on GitHub's servers (D-13)."
    - "The up-to-date-branch requirement is OFF and administrator enforcement is ON — one legible gate, genuinely enforced (D-14)."
    - "Whether classic branch protection remains alongside the ruleset is an explicitly recorded decision with a rationale, not an accident of migration — the two stack rather than replace, and the union is what actually applies."
    - "Archive tags exist on origin, so a laptop loss cannot destroy the only copy of real unmerged work (GREEN-12, D-32)."
    - "GREEN-08 empty edge: the protection script fails if the live required-context list is empty, not only if it differs — an unprotected branch must not read as passing."
    - "GREEN-08 encoding edge: the live context list is compared to the expected singleton by exact sorted string equality, so a differing case or an extra space fails rather than fuzzily matching."
    - "GREEN-07 empty edge: the script fails when zero check runs named `CI required` exist on the main head, which is the state that would otherwise let a configured-but-never-emitted name pass as verified."
    - statement: "GREEN-07 ordering edge: when several runs exist for the same head commit, the wall-clock measurement names which run id it measured rather than silently taking an arbitrary one."
      verification: backstop
    - statement: "GREEN-07 adjacency edge: a run concluding at exactly twenty minutes is treated as within budget, and the boundary convention is stated in the artifact rather than left to the reader."
      verification: backstop
    - statement: "GREEN-12 ordering edge: archive tags are pushed only after the credential verdict is PROCEED and after the tags have been verified locally, never as a convenience alongside an unrelated push."
      verification: backstop
  artifacts:
    - bin/verify-branch-protection
    - .github/rulesets/main.json
    - .github/workflows/branch-protection.yml
    - .planning/audits/198-branch-protection-migration.md
  key_links:
    - "The protection script must do BOTH halves: diff the live required contexts against the expected singleton, AND assert the name has actually been emitted as a check run. The second half is the executable form of 'verified after the matrix has reported once'. A runbook is an assertion; a script is proof."
    - "The protection job lives in its own workflow rather than inside the main CI workflow, so it never becomes a conditionally-skipped member of the aggregate's needs list — a skipped required check scores as passing."
    - "The ruleset's single required context is `CI required`, whose name carries no matrix axis and therefore cannot rot as Phases 199, 203 and 204 add lanes."
  prohibitions:
    - statement: "MUST NOT weaken any pre-publish gate on the surviving release path in order to make this phase green. Publishing is irreversible in effect, and those gates are the only thing standing in front of it."
      status: flagged-unverified
      verification: manual
    - statement: "MUST NOT bypass push protection. A blocked push is a credential-class signal and returns to the disposition rule, never to the allow-secret control."
      status: flagged-unverified
      verification: manual
  flagged_assumptions:
    - "RESEARCH A3 / Open Question 1: the release automation's token is self-documented as a fine-grained personal access token, not a GitHub App installation. Whether an integration-typed bypass actor is even the right primitive is unresolved. The plan tests empirically WITHOUT a bypass actor first and adds one only if a real release-please pull request is observed blocked."
    - "RESEARCH A4: jq availability on the runner image is assumed. The script guards for it explicitly rather than relying on the assumption."
---

<objective>
Land every local commit on `origin/main` with a green run inside the feedback budget, and replace a protection configuration that lives only on GitHub's servers with one that is git-tracked, enforced, requires exactly the single check CI actually emits, and is verified by a committed script.

Purpose: this is D-34 step 5 and the completion of GREEN-07, GREEN-08 and GREEN-12. Every gate that precedes it is already green: the branch triage is archived, the paid lane and the ungated publish path are deleted, the credential audit returned PROCEED, and push protection is live.
Output: origin carrying 587 commits, one required check, a committed ruleset, a protection verification script running on main, and archive tags on origin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-PATTERNS.md
@.planning/phases/198-green-bringup/198-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Final pre-push gate — confirm D-34 steps 1 through 4 are green and authorize the archive-tag push</name>
  <files>.planning/audits/198-branch-protection-migration.md</files>
  <read_first>
    - `.planning/audits/198-credential-audit.md` — the verdict line, the Class A rotation timestamps, and the push-protection confirmation
    - `.planning/ARCHIVE-REGISTER.md` and the output of `git tag -l 'archive/*'` — what is about to become permanently public
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` — D-34's five ordered steps with their gates and abort branches, D-32 (push archive tags, and why the local-only convention's rationale is void once the planning directory is public)
  </read_first>
  <reversibility rating="one-way">Merging 587 commits to `main` makes the planning history permanently public and mirrorable, and the milestone's accepted posture forbids a history rewrite as the remedy. Pushed archive tags are likewise public and, per D-31, are the durable copy of unmerged work.</reversibility>
  <decision>Confirm D-34 steps 1 through 4 are green, then authorize (a) merging the staging pull request into `main` and (b) pushing the archive tags to origin.</decision>
  <context>
    This is D-34 step 5, the phase's last irreversible action. Steps 1 through 4 each carried their own abort branch and each produced an artifact; this checkpoint verifies those artifacts rather than trusting a recollection. D-32 already settles that archive tags are pushed — the local-only convention existed solely because pushing would publish planning history, and that rationale is void once the planning directory is public on main in this very phase — but the tag push is still an irreversible publication and is confirmed here alongside the merge.
  </context>
  <options>
    <option id="authorize-both">
      <name>Authorize the merge and the archive-tag push (D-32 as locked)</name>
      <pros>Completes GREEN-07 and GREEN-12; unmerged work becomes durable off this laptop, which is what the requirement exists to guarantee</pros>
      <cons>Both actions are permanently public; a laptop-local copy is no longer the only exposure boundary</cons>
    </option>
    <option id="merge-only">
      <name>Merge, but keep archive tags local</name>
      <pros>Slightly smaller public surface</pros>
      <cons>Contradicts D-32; a laptop loss silently destroys the only copy of verified-unmerged work — the exact failure GREEN-12 forbids. Choosing this requires overriding a locked decision and must be recorded as such.</cons>
    </option>
    <option id="abort">
      <name>Abort — a preceding gate is not green</name>
      <pros>Honors the stated abort branch of whichever of steps 1 through 4 failed</pros>
      <cons>The phase stops short of GREEN-07, GREEN-08 and GREEN-12</cons>
    </option>
  </options>
  <resume-signal>Reply "authorize both", "merge only" (with an explicit acknowledgement that this overrides D-32), or "abort" naming the failing gate.</resume-signal>
  <action>
    Present a D-34 gate checklist with the evidence for each of the first four steps, each as a pass or fail with the artifact that proves it:
    1. Branch and worktree triage — diff artifact committed, tags created and verified by resolving each tag object, register written, one worktree, no stale branches.
    2. Paid critic workflow deleted — a recursive grep of the workflows directory for the paid key name returns nothing.
    3. Legacy publish workflow deleted and topology assertions added — exactly one workflow invokes the publish command, and the release verification alias is green.
    4. Credential audit — verdict PROCEED, every Class A finding rotated with a timestamp preceding this moment, zero Class B findings, push protection enabled.

    If ANY of the four is not green, stop. Each has a stated abort branch and none of them is a judgement call at this point.

    Then ask two explicit questions:
    - Confirm the merge of the staging pull request into `main`, making 587 commits of planning history permanently public.
    - Confirm pushing the archive tags to origin. The convention that milestone tags stay local existed solely because pushing would publish planning history; that rationale is void the moment the planning directory is public on main in this very phase. Local-only archive tags mean a laptop loss silently destroys the only copy of real unmerged work.

    Record both answers with timestamps in a new `.planning/audits/198-branch-protection-migration.md` under `## D-34 step 5 authorization`, together with the four-step checklist and its evidence. Whether milestone tags stay local is a separate question, deferred to milestone close — note it, do not decide it.
  </action>
  <verify>
    <automated>bash -c 'grep -q "^## D-34 step 5 authorization" .planning/audits/198-branch-protection-migration.md && grep -q "VERDICT: PROCEED" .planning/audits/198-credential-audit.md'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audits/198-branch-protection-migration.md` contains `## D-34 step 5 authorization` with a four-row checklist, each row naming its evidence artifact and marked pass.
    - The section records an explicit confirmation of the merge to main, with a timestamp.
    - The section records an explicit confirmation of the archive-tag push, with a timestamp.
    - The section notes that the milestone-tag convention question is deferred to milestone close and is not decided here.
    - `.planning/audits/198-credential-audit.md` still reads `## VERDICT: PROCEED` and shows push protection enabled.
    - `grep -rl ANTHROPIC .github/` returns no output at the moment of the check.
  </acceptance_criteria>
  <done>All four preceding gates are confirmed green against their artifacts, and the two one-way actions are explicitly authorized and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate protection to a committed ruleset and verify it with a committed script (D-12, D-13, D-14)</name>
  <files>bin/verify-branch-protection, .github/rulesets/main.json, .github/workflows/branch-protection.yml, .planning/audits/198-branch-protection-migration.md</files>
  <read_first>
    - `bin/verify-release-shape` — read the whole file; it is the explicit sibling pattern. Copy its shell dialect, its strict-mode header, its repository-root resolution, its one-if-block-per-invariant compute-then-assert style, its error messages that state both what was expected and what triggered the failure, and its single-line success message.
    - `.github/workflows/ci.yml` lines 566-573 — how the sibling script is wired as a one-step job
    - `.github/workflows/flake-detection.yml` — a complete standalone workflow shape with a stable job id comment
    - `.planning/phases/198-green-bringup/198-RESEARCH.md` §"Pattern 3" (the ruleset write payload and the bypass-actor open question) and §"Pattern 4" (the two API halves and the exact endpoints)
    - `.planning/audits/198-matrix-name-observation.md` — the emitted names observed by the tracer, which is what protection must agree with
  </read_first>
  <action>
    Write `bin/verify-branch-protection` as a sibling of the release-shape script. Strict shell mode, resolve the repository root from the script's own location, guard explicitly that the JSON processor is available with a message and a non-zero exit, and resolve the repository slug from the environment with a CLI fallback. Then assert two invariants, each in its own block:

    Half (a): read the effective rules for the `main` branch from the rules-for-a-branch endpoint, extract every required status-check context, and compare the sorted list to the sorted single-element list containing exactly the string `CI required`. Fail on any difference, including an empty list — an unprotected branch must not read as passing. The error message names the live list and the expected list.

    Half (b): read the check runs for `main`'s head from the check-runs-for-a-reference endpoint and assert at least one is named exactly `CI required`. Fail with a message stating the name has never been emitted on a real run. This half is the executable form of the requirement's "verified after the matrix has reported once" — a name can be configured in protection without CI ever emitting it, which is the deadlock the requirement exists to prevent.

    End with a single-line success message naming both facts.

    Author `.github/rulesets/main.json` targeting the branch `main` with enforcement active, containing: a required-status-checks rule listing the single context `CI required` with the strict up-to-date policy set to false; non-fast-forward; deletion; required linear history; and a pull-request rule with zero required approving reviews and required review-thread resolution. Do NOT add signed commits, which breaks bot pull requests and deters contributors, and do NOT add a merge queue, which is overkill at solo throughput.

    On the bypass actor, follow the research recommendation: attempt the ruleset with NO bypass actor first. The release automation's token is a fine-grained personal access token, not a GitHub App installation, and its commits already flow through a normal pull request and merge — so the bypass may be solving a problem that does not exist. Only if a real release automation pull request is observed being blocked, add a bypass actor, and record in the artifact which actor type was needed and why. Note that the read-only ruleset CLI cannot write; create it via the raw API with the committed JSON as the input.

    Apply the D-14 trade explicitly: turn the up-to-date-branch requirement OFF and administrator enforcement ON. Record before and after values.

    Close the stacking question rather than leaving it implicit: classic protection and rulesets both evaluate and the union applies, so a stale classic rule can silently re-arm the up-to-date requirement the ruleset just relaxed. Decide, and record the decision with its rationale in `.planning/audits/198-branch-protection-migration.md` under `## Classic protection disposition`: either delete the classic rule for `main` once the ruleset is confirmed active and enforcing, or state why both are deliberately kept. Verify the outcome by re-reading the effective rules endpoint after the change and pasting the response into the artifact.

    Create `.github/workflows/branch-protection.yml` with job id `verify-branch-protection`, triggered on push to main, on a schedule, and on manual dispatch, running the script with a token in the environment and carrying a `timeout-minutes` bound. Put it in its OWN workflow, not inside the main CI workflow: a job restricted to main inside that workflow would be a conditionally-skipped member of the aggregate's needs list, and a skipped required check scores as passing. Add a header comment stating the job id is stable and that this job is deliberately not part of the required aggregate.

    Prove the script has teeth before relying on it: run it against the live configuration and confirm success, then temporarily add a second required context to the ruleset and confirm half (a) fails naming both lists, then remove it. Record both outcomes.
  </action>
  <verify>
    <automated>bash bin/verify-branch-protection</automated>
  </verify>
  <acceptance_criteria>
    - `bin/verify-branch-protection` exists, is executable (`test -x`), and exits 0 against the live configuration.
    - The script's first lines set strict shell mode and resolve the repository root from the script's own location, matching `bin/verify-release-shape`.
    - The script guards for the JSON processor with its own error message and non-zero exit.
    - The script contains two distinct assertion blocks: one reading the rules-for-a-branch endpoint, one reading the check-runs-for-a-reference endpoint.
    - Half (a) compares sorted lists for exact equality against a one-element list containing `CI required`, and fails on an empty live list.
    - Temporarily adding a second required context makes the script exit non-zero with a message naming both the live and the expected lists; this is demonstrated and recorded.
    - `.github/rulesets/main.json` exists, is valid JSON (`jq -e . ` exits 0), targets `main`, has enforcement active, and lists exactly one required status check context, the string `CI required`.
    - The ruleset JSON contains no signed-commits rule and no merge-queue rule.
    - `gh api repos/:owner/:repo/rules/branches/main` returns a required-status-checks rule whose context list is exactly `CI required`, and that response is pasted into the migration artifact.
    - The live protection reports the up-to-date requirement off and administrator enforcement on, with before and after values recorded in the artifact.
    - `.planning/audits/198-branch-protection-migration.md` contains a `## Classic protection disposition` section naming the decision and its rationale, plus the re-read effective-rules response proving the outcome.
    - `.github/workflows/branch-protection.yml` exists with job id `verify-branch-protection`, a `timeout-minutes` key, and triggers covering push to main, schedule, and manual dispatch.
    - `verify-branch-protection` does NOT appear in the aggregate gate's needs list in `.github/workflows/ci.yml`.
  </acceptance_criteria>
  <done>Protection requires exactly the one check CI emits, that requirement is git-tracked as committed JSON, the stacking question is decided in writing, and a committed script proves both the configuration and the emission on every run against main.</done>
</task>

<task type="auto">
  <name>Task 3: Land every commit on origin/main, measure the wall clock, and push the archive tags (GREEN-07, GREEN-12)</name>
  <files>.planning/audits/198-branch-protection-migration.md</files>
  <read_first>
    - `.planning/audits/198-branch-protection-migration.md` — the step 5 authorization recorded in Task 1 and the classic-protection disposition from Task 2
    - `.planning/ROADMAP.md` §"Phase 198" Success Criteria 3 — the twenty-minute ceiling with a twelve-minute target
    - `.planning/audits/198-matrix-name-observation.md` §"Min-lane rehearsal" — the min lane's rehearsed result and any drift classification, since the min lane has never executed on origin and is the milestone's highest-variance risk
  </read_first>
  <precondition>`.planning/audits/198-branch-protection-migration.md` contains a `## D-34 step 5 authorization` section with both confirmations recorded, and `bin/verify-branch-protection` exits 0. This task performs the merge that makes 587 commits of planning history permanently public.</precondition>
  <action>
    Merge the staging pull request into `main` through the pull request, not by a direct push — the ruleset's pull-request rule now applies and the merge path is the one contributors will use, so it should be exercised.

    Watch the resulting run on `main` to completion. Deleting the paid critic workflow in Plan 06 is what makes this safe: 587 commits would otherwise have matched its operator-namespace path filter and fired the parked paid lane.

    Measure the wall clock from the run's own timestamps via `gh run view <id> --json conclusion,startedAt,updatedAt,jobs` and compute the elapsed minutes. Record the run id, the conclusion, the elapsed time, and a per-job duration table in `.planning/audits/198-branch-protection-migration.md` under `## Post-merge main run`. State the boundary convention explicitly: exactly twenty minutes is within budget.

    If the run is red, triage it rather than relaxing anything. Do not weaken a gate, do not downgrade a lane to non-blocking, and do not bypass protection. If the min lane is the cause and the diagnosis is runner or Playwright version drift, apply the roadmap's stated fallback — move the browser lanes off the pull-request trigger onto nightly — and record that as a scope change with its rationale rather than an inline fix.

    If the run exceeds twenty minutes, evaluate the sharding fallback against the measured post-surgery wall clock. Sharding is deliberately last because it pays a forced-recompile and demo-seed preamble per runner. Record the judgement either way: if sharding is not needed, say so with the number that settles it.

    Confirm `git log origin/main..main` is empty.

    Then push the archive tags, per the authorization recorded in Task 1: push each tag matching the archive prefix explicitly by name rather than pushing all tags, so no unrelated milestone tag is published as a side effect — whether milestone tags stay local is a separate deferred question. Verify each tag is present on origin by resolving it remotely, and record the verification in the artifact under `## Archive tags on origin`.

    Finally, run `bin/verify-branch-protection` once against `main` now that a real run has reported there, and paste its success line into the artifact — that is the executable proof that the required name has actually been emitted.
  </action>
  <verify>
    <automated>bash -c 'test -z "$(git log origin/main..main)" && bash bin/verify-branch-protection && test -n "$(git ls-remote --tags origin "refs/tags/archive/*")"'</automated>
  </verify>
  <acceptance_criteria>
    - `git log origin/main..main` produces no output.
    - `git ls-remote --tags origin 'refs/tags/archive/*'` lists every archive tag created in Plan 06.
    - `git ls-remote --tags origin` shows no milestone tag that was not previously on origin — only archive tags were pushed.
    - The latest run on `main` has conclusion `success`, and its elapsed time computed from its own start and update timestamps is twenty minutes or less.
    - `.planning/audits/198-branch-protection-migration.md` contains `## Post-merge main run` with the run id, conclusion, elapsed minutes, the stated boundary convention, and a per-job duration table.
    - That section states whether the sharding fallback is needed, with the measured number that settles it.
    - The artifact contains `## Archive tags on origin` with the remote verification output.
    - `bash bin/verify-branch-protection` exits 0 against `main` and its success line is pasted into the artifact.
    - No gate was weakened: `git diff` across this plan shows no change to `.github/workflows/release.yml`'s pre-publish gate steps and no `continue-on-error` added anywhere.
  </acceptance_criteria>
  <done>Origin carries every local commit, the main run concludes green inside the feedback budget, the archive tags are durable off this laptop, and the protection contract is proven by a script rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → public origin/main | The permanent publication of 587 commits of internal planning history |
| GitHub platform config → merge gate | Protection is the only thing between an arbitrary pull request and the default branch |
| release automation token → protected branch | A fine-grained personal access token operates against a branch this plan is re-protecting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-07-01 | Elevation of Privilege | single-context protection with the up-to-date requirement off | high | mitigate | Collapsing twelve required contexts to one narrows the gate's surface, and relaxing the up-to-date requirement admits a merge tested against a slightly older base. Traded deliberately against turning administrator enforcement ON, which closes the far larger hole of a protection rule the owner can simply walk past. Both halves land together, and the committed script asserts the resulting configuration on every run against main. |
| T-198-07-02 | Spoofing | a required check name that CI never emits | high | mitigate | Half (b) of the script asserts the name has actually been emitted as a check run on the main head, so a configured-but-impossible context cannot masquerade as protection. |
| T-198-07-03 | Tampering | classic protection stacking with the ruleset | medium | mitigate | The two evaluate as a union, so a stale classic rule can re-arm what the ruleset relaxed. Forced to an explicit recorded decision with a re-read of the effective rules endpoint as proof. |
| T-198-07-04 | Information Disclosure | 587 commits of planning history on a public default branch | high | mitigate | Gated by the credential audit's PROCEED verdict, all Class A rotations completed beforehand, push protection enabled, and an explicit recorded human authorization. Content disclosure is an accepted posture, not an oversight — the record is published deliberately. |
| T-198-07-05 | Elevation of Privilege | a ruleset bypass actor | medium | mitigate | No bypass actor is configured initially. One is added only if a real release automation pull request is observed blocked, and the actor type and reason are recorded. An unnecessary standing bypass is a permanent hole. |
| T-198-07-06 | Denial of Service | protection deadlocking the release automation | medium | mitigate | The up-to-date requirement is off precisely because it forces rebase churn against a long CI and deadlocks release pull requests; the aggregate's name carries no matrix axis and cannot rot as later phases add lanes. |
</threat_model>

<verification>
- `git log origin/main..main` is empty
- `bash bin/verify-branch-protection` exits 0
- `gh api repos/:owner/:repo/rules/branches/main` shows exactly one required context, `CI required`
- The latest main run concludes success in twenty minutes or less
- `git ls-remote --tags origin 'refs/tags/archive/*'` lists every archive tag
- `git worktree list | wc -l` equals 1
</verification>

<success_criteria>
Origin carries every local commit with a green run inside the feedback budget; protection requires exactly the one check CI emits, is committed as JSON, is genuinely enforced against administrators, and is proven on every main run by a committed script; and every archived branch is durable off this laptop.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan (new symbols — exclude from drift verification):
- `bin/verify-branch-protection`
- `.github/rulesets/main.json`
- `.github/workflows/branch-protection.yml` with new CI job id `verify-branch-protection`
- `.planning/audits/198-branch-protection-migration.md`
- Live GitHub state: a repository ruleset targeting `main`; classic protection disposition applied
- Archive tags pushed to origin
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/198-green-bringup/198-07-SUMMARY.md` when done
</output>
