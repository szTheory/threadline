---
phase: 198-green-bringup
plan: 40
type: execute
wave: 3
gap_closure: true
depends_on:
  - 198-38
  - 198-39
files_modified:
  - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - GREEN-01
  - GREEN-02
  - GREEN-03
  - GREEN-04
  - GREEN-05
  - GREEN-06
  - GREEN-07
  - GREEN-08
  - GREEN-09
  - GREEN-10
  - GREEN-11
  - GREEN-12

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Round 6 section is appended to 198-CI-MEASUREMENT.md containing a per-lane prediction written and committed BEFORE the push, and that prediction is scored afterwards without being edited. No prior round's section is modified by a single character."
    - "A real GitHub Actions run exists whose head SHA contains every commit from plans 198-38 and 198-39, and its run id, head SHA, attempt number, per-job conclusions and wall-clock duration are recorded on disk verbatim."
    - "The push is performed by the maintainer at a blocking checkpoint using an exact command stated in this plan. `git push` is a human-authorized gate in this project and an agent session must not perform it; a plan that assumes otherwise halts mid-measurement."
    - "`Run test suite (current)`'s conclusion is recorded verbatim, and GREEN-04's status is confirmed or revised strictly from that conclusion — never from local `mix test`, `mix ci.all`, `mix verify.example` or `mix verify.example_browser` output (D-01). Plan 198-38 changed example-app source, so GREEN-04 is re-proved on this run rather than assumed to have survived."
    - "GREEN-01, GREEN-02, GREEN-03, GREEN-05, GREEN-06, GREEN-09, GREEN-10, GREEN-11 and GREEN-12 are carried forward unchanged on their round-5 evidence, with no new work planned for them and no status altered, and that carry is stated explicitly so a coverage reader sees a decision rather than an omission."
    - "GREEN-07's status after this run is whatever the disposition recorded in 198-39-DECISION.md says it is; a measured `failure` is recorded as failure and never as an acceptable-therefore-satisfied outcome."
    - statement: "Adjacency edge (GREEN-07): a `needs:` member counts as met only on the exact conclusion string `success`; `neutral`, `skipped` and `cancelled` are each recorded as not-success, because the alls-green aggregator scores a skipped required check as passing and that is exactly the laundering D-09 rejects."
      verification: explicit
    - statement: "Empty edge (GREEN-07): if the run reports zero `needs:`-member conclusions, that is recorded as failed collection rather than as a pass, so a broken query cannot manufacture a green record."
      verification: explicit
    - statement: "Ordering edge (GREEN-07): if more than one workflow run exists for the pushed head SHA, the most recent completed run is authoritative and every competing run id is listed, so the selection is auditable rather than implicit."
      verification: explicit
  artifacts:
    - ".planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — an appended Round 6 section: pre-push prediction, push record, run id, head SHA, attempt, per-job table, verbatim alls-green output, prediction scorecard, red-count comparison against round 5's 2, wall clock, `CI required` conclusion, `mergeStateStatus`, and a root cause for every remaining red check"
    - ".planning/REQUIREMENTS.md — GREEN-04 re-confirmed and GREEN-07 recorded from the measured run, with the carried-forward statuses of the other ten left untouched and their carry stated"
    - ".planning/STATE.md — current position updated to the measured round-6 outcome"
  key_links:
    - "plan 198-38's connection-pinned demo lock → `mix verify.example` still passes on CI → `Run test suite (current)` stays `success` → GREEN-04's only admissible evidence is re-proved rather than assumed"
    - "plan 198-39's recorded disposition → GREEN-07's status line after this run → the measured `CI required` conclusion is reported as-is and interpreted through the disposition, never softened by it"
    - "D-39's regeneration prohibition → `verify-capture` and the three `operator-stress.spec.ts` rows stay red → `CI required` cannot conclude `success` → the round-6 prediction's ceiling is stated before the push and is therefore scoreable"
  prohibitions:
    - statement: "A prior round's prediction or measurement section must not be retro-edited. The Round 6 section is appended; rounds 1 through 5 are read-only."
      status: kept
      verification: flagged-unverified
    - statement: "A requirement must not be closed on local evidence. Local `mix test`, `mix ci.all`, `mix verify.example` and `mix verify.example_browser` results are readiness signals only (D-01)."
      status: kept
      verification: flagged-unverified
    - statement: "Tier-A `page.*` scorecard, screenshot ledger baseline and `*.png` regeneration are forbidden for milestone v1.41 (D-39), including as a response to a red lane observed on this run."
      status: kept
      verification: flagged-unverified
    - statement: "`ci-required`'s `needs:` list, `.github/` gates, `CONTRIBUTING.md`, `playwright.config.ts` and `.planning/scorecards/` must not be edited or narrowed (D-42); `git diff --stat` over exactly that path set must be empty for the round-6 range."
      status: kept
      verification: flagged-unverified
    - statement: "No test may be skipped, tagged out, weakened, or excluded to improve this run's result, and no check may be re-run, re-dispatched, or selectively retried; the run's attempt number is recorded and any value above 1 is explained verbatim."
      status: kept
      verification: flagged-unverified
    - statement: "`git push` must not be performed by the agent. It is a human-authorized gate; the executor stages the branch and halts."
      status: kept
      verification: flagged-unverified
---

<objective>
The closing plan of gap-closure round 6: state a falsifiable prediction, have the maintainer push by
hand, measure one real CI run to completion, and record the outcome precisely enough that the next
verifier can re-derive every verdict from the run id alone. Then record the carried requirement set
honestly.

**Why round 6 needs a measured run at all.** Plan 198-38 changed shipped example-app source
(`demo/reset.ex`, `demo/seed.ex`) and added a test module that manipulates the repo's sandbox mode.
`Run test suite (current)` — the job whose `success` conclusion is GREEN-04's *only* admissible
evidence under D-01 — exercises exactly that code through `mix verify.example`. A local green is a
readiness signal, not a verdict. GREEN-04 must be re-proved on CI, not assumed to have survived a
change to the code its evidence covers.

**What this round can close and what it cannot, stated before the run so the prediction is
falsifiable:**

- `Run test suite (current)` — **expected to stay green.** 198-38's change is confined to the demo
  lock guard and one new `async: false` test module. If it goes red, that is 198-38 regressing
  GREEN-04, and the honest response is to fix the cause, not to re-run the check.
- `Example app browser E2E (Playwright)` — **cannot close.** Three `operator-stress.spec.ts` rows
  (`page.home.happy`, `page.timeline.empty`, `footgun.transaction-page-left-push-desktop`) are
  ledger-baseline screenshot diffs whose only remedy is `page.*` regeneration, forbidden by D-39 for
  this entire milestone.
- `Tier A capture lane (byte-stable evidence)` — **cannot close.** Same D-39 class, 198 drifted
  scorecard files, no plan in this round touched it.

**Therefore `CI required` cannot conclude `success` this round, and GREEN-07 cannot reach Complete on
any basis.** The honest target is that the red `needs:` count stays at round 5's 2 and does not grow.
Stating the ceiling before the run is what makes the result scoreable rather than rationalizable
afterwards.

**The push is a human-authorized gate.** `git push` from an agent session is blocked in this project
and a relayed in-session approval does not unblock it; prior measured-CI plans in this phase halted
at exactly this point. Task 2 is therefore an explicit blocking human-action checkpoint carrying the
exact command, not an autonomous task.

**Edge lift, spec-less probe fallback §A — full enumeration, no silent drops.** The deterministic
edge probe (`coverage.json`) surfaced **26** applicable items, every one `unresolved` — none was
auto-resolved with a backstop and none was auto-dismissed.

- **6 authored into `must_haves.truths`:** GREEN-04's `adjacency`, `empty` and `ordering` in plan
  198-38, and GREEN-07's `adjacency`, `empty` and `ordering` above.
- **20 surfaced here as flagged assumptions, not authored as new predicates.** They belong to the ten
  requirements that are Complete and independently re-verified in `198-VERIFICATION.md`'s round-5
  section, whose satisfying files round 6 does not touch, and their edges are carried by the
  already-closed plans 198-01 through 198-12. Enumerated so the decision is visible rather than
  silent: GREEN-01 `unclassified`; GREEN-02 `concurrency`; GREEN-03 `empty`, `encoding`; GREEN-05
  `unclassified`; GREEN-06 `boundary`, `adjacency`, `empty`, `ordering`, `precision`; GREEN-08
  `empty`, `encoding`, `concurrency`; GREEN-09 `unclassified`; GREEN-10 `unclassified`; GREEN-11
  `empty`, `encoding`; GREEN-12 `adjacency`, `empty`, `ordering`.
- Of those 20, the four `unclassified` rows (GREEN-01, GREEN-05, GREEN-09, GREEN-10) **remain
  `unresolved`** and are never auto-dismissed.

6 + 20 = 26. Section equality holds.

**Carried requirements, stated as a decision.** GREEN-01, GREEN-02, GREEN-03, GREEN-05, GREEN-06,
GREEN-09, GREEN-10, GREEN-11 and GREEN-12 are Complete and were independently re-verified at round 5.
Round 6 plans **no new work** for them; they are carried forward unchanged on their round-5 evidence,
and Task 3 records that carry explicitly with each requirement's evidence citation so a coverage
reader sees a deliberate carry rather than an omission. GREEN-04 is re-proved on this run because
198-38 touched its covered code. GREEN-07's status follows 198-39-DECISION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
@.planning/phases/198-green-bringup/198-39-DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and commit a falsifiable per-lane prediction before anything is pushed</name>
  <files>.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md, the Round 5 prediction section (line ~1206) and the Round 5 measured section (line ~1248) in full — the per-job table, the prediction scorecard, the six-column baseline comparison, and the honestly-recorded composition partial-miss where a third, un-cited failing Playwright row appeared
    - .planning/phases/198-green-bringup/198-38-SUMMARY.md — what actually changed at tree level in round 6, including the red-then-green teeth proof, and which lanes those files feed
    - .planning/phases/198-green-bringup/198-39-SUMMARY.md and 198-39-DECISION.md — the recorded GREEN-07 disposition, which frames but must not soften this round's measured result
    - .github/workflows/ci.yml, the `ci-required` job and its 12 `needs:` members plus the emitted check name — read only, never edited
    - examples/threadline_phoenix/e2e/playwright.config.ts — `maxFailures` behavior under CI, i.e. whether this lane's failure count is a census or a floor this round — read only, never edited
    - .planning/phases/198-green-bringup/198-CONTEXT.md — D-01, D-09, D-39, D-42
  </read_first>
  <action>
Append a **Round 6** section to `198-CI-MEASUREMENT.md`. Do not edit any prior round's section; a
superseded record is appended to, never rewritten.

Open the section with a prediction, written and committed **before** the push, stating for each of
`ci-required`'s 12 `needs:` members its expected conclusion, plus the expected red count as an
integer and the expected `CI required` conclusion. Ground the prediction in round-6 tree-level
evidence — 198-38's actual file list and its local verification results — rather than in plan
promises, and name the source you used for each lane.

Carry round 5's own methodological lesson forward explicitly: an unbounded local Playwright run and a
CI run sample different, non-nested populations, so a per-test composition prediction for the browser
lane is weakly grounded and must be labelled as such rather than stated with false confidence. Round
5 also recorded a third, un-predicted failing row of the same D-39-forbidden class; predict the
`operator-stress.spec.ts` set as the three now-known rows and say plainly that additional rows of the
same class would not be a surprise.

State the ceiling plainly in the same section: `CI required` cannot conclude `success` this round
because the Tier A capture lane and the `operator-stress.spec.ts` ledger rows are red by construction
under D-39, and GREEN-07 therefore cannot reach Complete on any basis. State the specific new risk
this round carries: 198-38 modified code exercised by `mix verify.example`, so `Run test suite
(current)` is the lane most likely to move, and predict its conclusion explicitly.

Commit this section before Task 2 raises the push checkpoint. A prediction that lands in the same
commit as its result is not a prediction.
  </action>
  <reversibility rating="reversible">An additive documentation section.</reversibility>
  <verify>
    <automated>bash -c 'grep -q "^## Round 6" .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md &amp;&amp; git log -1 --pretty=format:%H -- .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` contains a `## Round 6` prediction section listing all 12 `needs:` members with a predicted conclusion each, a predicted red count as an integer, and a predicted `CI required` conclusion.
    - The section states the D-39 ceiling, states that GREEN-07 cannot reach Complete this round, and predicts `Run test suite (current)` explicitly with 198-38's file list as its stated basis.
    - `git diff HEAD~1 -- .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md | grep -c '^-[^-]'` returns `0` — no prior round's line was removed or altered.
    - `git log --oneline -1 -- .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` shows the prediction committed, and that commit precedes the push performed in Task 2.
    - `git status --porcelain .github/ .planning/scorecards/ CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts` is empty.
  </acceptance_criteria>
  <done>A committed, falsifiable, per-lane prediction with its ceiling and its new-risk lane stated, appended without touching any prior round's record.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Maintainer pushes the round-6 measurement branch by hand and opens a draft DO NOT MERGE pull request</name>
  <read_first>
    - .planning/phases/198-green-bringup/198-37-PLAN.md Task 2 — the round-5 push procedure this mirrors, including its branch naming, its three-ref SHA resolution, and its draft DO NOT MERGE pull-request body
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md Round 6 prediction section, committed in Task 1 — it must be present on the pushed branch, not added afterwards
    - .planning/phases/198-green-bringup/198-39-DECISION.md, the branch and pull-request disposition section — specifically whether PR #32 is to be left open or closed, so the round-6 PR does not silently replace an evidence anchor
    - .planning/phases/198-green-bringup/198-CONTEXT.md — D-01 (measured evidence only) and the standing project rule that `git push` is human-authorized
  </read_first>
  <precondition>`git push` from this agent session is a human-authorized gate and an in-session or relayed approval does not unblock it. This task cannot be completed by the agent and must not be attempted; the executor stages the branch locally, prints the exact commands, and halts for the maintainer.</precondition>
  <action>
Prepare locally, then halt and ask the maintainer to run the push themselves.

Create the round-6 measurement branch from the current head — name it consistently with the round-5
branch so the ledger reads in sequence — and verify locally, before halting, that its tip contains
every commit from plans 198-38 and 198-39 and the Task 1 prediction commit. Resolve and print the tip
SHA, the branch name, and the exact `git push` command plus the exact `gh pr create` command with a
draft flag and a body that states DO NOT MERGE and names the pull request as a measurement vehicle
rather than a merge request.

Then halt. State to the maintainer, in the halt message, the four things they need: the branch name,
the tip SHA, the push command verbatim, and the pull-request command verbatim. Do not run either.
Do not attempt the push to see whether it works; a blocked attempt leaves the session in a worse
state than a clean halt and prior plans in this phase have already established that it fails.

After the maintainer confirms the push, resolve the run: list workflow runs for the pushed head SHA,
record every run id found, and select the most recent completed run as authoritative, listing any
competing ids. Wait for it to complete without re-running, re-dispatching, or selectively retrying
any check. Record the run's `attempt` number; if it is above 1, explain verbatim why, and note that
this plan did not cause it.
  </action>
  <instructions>
    Everything automatable is already done: the round-6 measurement branch is created at this round's
    HEAD, its name is confirmed distinct from every existing `ci/198-*` ref, and the tip SHA, the
    `origin/main..HEAD` commit count, and the presence of the 198-38, 198-39 and prediction commits on
    that tip are all recorded.

    `git push` from this agent session is a human-authorized gate, and publishing local history — which
    includes `.planning/` — to a public remote is your decision, not mine. Please run the two commands
    printed in the halt message yourself: the `git push -u origin &lt;branch&gt;`, then the
    `gh pr create --draft --base main --head &lt;branch&gt;` with a DO NOT MERGE title and body.

    Then tell me the pull request number. I will resolve the run for the pushed head SHA and observe
    exactly one run to completion without re-running or re-dispatching anything.
  </instructions>
  <verification>`git ls-remote --heads origin &lt;branch&gt;` resolves to the recorded local tip SHA, and `gh pr list --head &lt;branch&gt; --json number,isDraft,body` shows exactly one draft pull request whose body contains DO NOT MERGE.</verification>
  <resume-signal>Type "pushed" once both commands have completed, or paste the error if either failed.</resume-signal>
  <reversibility rating="costly">A push to a remote is not locally revertible and publishes commit content; the branch is a draft measurement vehicle and is never merged, but the push itself is why this is a human-authorized gate rather than an agent action.</reversibility>
  <verify>
    <human-check>The maintainer confirms they ran the push and the pull-request creation themselves, and the resulting run id and head SHA are reported back into the session.</human-check>
    <automated>bash -c 'gh run list --limit 5 --json databaseId,headSha,status,conclusion'</automated>
  </verify>
  <acceptance_criteria>
    - The executor performed no `git push`; the halt message contains the branch name, the tip SHA, the verbatim `git push` command and the verbatim `gh pr create --draft` command.
    - `git log <branch> --oneline` locally shows the 198-38, 198-39 and Task 1 prediction commits present on the pushed tip before the halt.
    - A run id and head SHA are recorded, and the head SHA matches the locally resolved tip SHA character for character.
    - If more than one workflow run exists for that head SHA, every run id is listed and the authoritative selection rule (most recent completed) is stated.
    - The run's `attempt` is recorded; a value above 1 carries a verbatim explanation.
    - The pull request is a draft and its body contains DO NOT MERGE.
  </acceptance_criteria>
  <done>One real CI run exists for a head SHA containing round 6's full commit set, pushed by the maintainer, with its run id, head SHA and attempt recorded and no check re-run.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measured outcome, score the prediction, and state the carried requirement set</name>
  <files>
    .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md Round 6 prediction section from Task 1, and the Round 5 measured section for the exact subsection layout to mirror: the run, per-job table, verbatim alls-green output, prediction scorecard, baseline comparison, red count, root cause for every still-red check, wall clock, `CI required` conclusion, `mergeStateStatus`, headline claim, re-run discipline, and the D-42 invariants subsection
    - .planning/REQUIREMENTS.md — GREEN-04's and GREEN-07's status lines and their mapping-table rows, plus the nine carried requirements' rows whose text must be left byte-unchanged
    - .planning/phases/198-green-bringup/198-VERIFICATION.md — the round-5 evidence citation for each of the nine carried requirements, which Task 3 restates rather than re-derives
    - .planning/phases/198-green-bringup/198-39-DECISION.md — the recorded GREEN-07 disposition
    - .planning/STATE.md — the current-position and progress blocks, which this project maintains by hand because the `state.*` SDK handlers miscompute the bespoke progress block
  </read_first>
  <action>
Append the measured half of the Round 6 section, mirroring Round 5's subsection layout exactly.

Record verbatim: the run id, head SHA, attempt, `createdAt`/`updatedAt` and the derived wall clock; a
per-job table for all 12 `needs:` members with each conclusion as the literal string GitHub returned;
the `re-actors/alls-green` step's own output; and `CI required`'s conclusion. Treat only the exact
string `success` as met — record `neutral`, `skipped` and `cancelled` each as not-success, because
the aggregator scores a skipped required check as passing and that is exactly the laundering D-09
rejects. If zero `needs:`-member conclusions come back, record that as failed collection and stop;
a broken query is not a green run.

Score the Task 1 prediction without editing it. State the hit count, name every miss, and state
whether the red `needs:` count moved from round 5's 2. Give every still-red check a root-cause
citation to a specific defect or a specific recorded decision — never a bare statement that it is
still red.

Then set statuses strictly from this run. GREEN-04: confirm or revise from `Run test suite
(current)`'s conclusion alone; if 198-38 regressed it, record the regression plainly and do not
soften it. GREEN-07: record the measured `CI required` conclusion and apply the disposition from
`198-39-DECISION.md` — the disposition explains the status, it does not satisfy the requirement, and
GREEN-07 is not marked Complete without a measured `success`.

Then write the carried-requirements statement, as a decision rather than an omission: GREEN-01,
GREEN-02, GREEN-03, GREEN-05, GREEN-06, GREEN-09, GREEN-10, GREEN-11 and GREEN-12 are Complete,
independently re-verified in `198-VERIFICATION.md`'s round-5 section, and are carried forward
unchanged. For each, restate its round-5 evidence citation in one line. Round 6 planned no work for
them, touched none of their satisfying files, and changed none of their statuses — say so explicitly.

Finally update `STATE.md`'s current position and progress blocks by hand, then re-read them to
confirm the numbers are internally consistent, since this project's `state.*` handlers are known to
miscompute that block.

Close the section with the D-42 invariants subsection: run `git diff --stat` for the full round-6
commit range over `.github/`, `CONTRIBUTING.md`, `examples/threadline_phoenix/e2e/playwright.config.ts`,
`.planning/scorecards/` and every `*.png`, and record the output. It must be empty. If it is not,
that is a laundering finding against round 6 itself and must be reported as one, not corrected
quietly.
  </action>
  <reversibility rating="reversible">A documentation append and status lines derived mechanically from a measured run.</reversibility>
  <verify>
    <automated>bash -c 'grep -q "Measured CI run" .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md &amp;&amp; git diff --stat $(git merge-base HEAD origin/main)..HEAD -- .github/ CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts .planning/scorecards/ "*.png" | wc -l | grep -qx 0'</automated>
  </verify>
  <acceptance_criteria>
    - The Round 6 measured subsection records the run id, head SHA, attempt, wall clock, a 12-row per-job table with literal conclusion strings, the verbatim alls-green output, and `CI required`'s conclusion.
    - The prediction scorecard states a hit count and names every miss, and `git diff` shows zero removed lines in the Round 6 prediction subsection and zero changed lines in Rounds 1 through 5.
    - Every still-red check has a root-cause line citing either a specific defect or a specific recorded decision (D-39 for the Tier A and ledger-baseline lanes).
    - GREEN-04's status in `REQUIREMENTS.md` cites this run's `Run test suite (current)` conclusion and no local command output.
    - GREEN-07 is not marked Complete, and its status line cites both the measured `CI required` conclusion and `198-39-DECISION.md`.
    - `REQUIREMENTS.md` contains an explicit carried-forward statement naming GREEN-01, GREEN-02, GREEN-03, GREEN-05, GREEN-06, GREEN-09, GREEN-10, GREEN-11 and GREEN-12, each with its round-5 evidence citation, and `git diff -- .planning/REQUIREMENTS.md | grep '^-[^-]' | grep -c 'GREEN-0[12356]\|GREEN-1[012]'` returns `0` — none of their existing rows was rewritten.
    - `git diff --stat` for the round-6 range over `.github/`, `CONTRIBUTING.md`, `playwright.config.ts`, `.planning/scorecards/` and `*.png` produces no output, and that empty result is recorded in the D-42 invariants subsection.
    - `.planning/STATE.md`'s progress block is internally consistent after a manual re-read, and its current position names the round-6 run id.
  </acceptance_criteria>
  <done>The round-6 measured outcome, its scored prediction, its per-check root causes, its D-42 invariants and its carried-requirement statement are all on disk, with every status set from the measured run alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → GitHub REST API (`gh`, read-only measurement) | `gh run list`, `gh run view`, `gh pr view` read CI state; no write operation is authorized to the agent |
| local repository → public `origin` (push) | The push publishes commit content including `.planning/` history; `origin/main` is far behind local head, so a push is consequential and human-gated |
| measured run output → requirement status | The only admissible evidence path for GREEN-04 and GREEN-07 (D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-40-01 | Information Disclosure | `git push` publishing `.planning/` history to a public remote | high | mitigate | The agent performs no push; Task 2 is a blocking human-action checkpoint that prints the exact command and halts, and its acceptance criteria assert no push was attempted |
| T-198-40-02 | Repudiation | the pre-push prediction | high | mitigate | The prediction is committed in Task 1 before the push and scored in Task 3 without edit; criteria assert zero removed lines across the prediction and all prior round sections |
| T-198-40-03 | Tampering | prior rounds' measurement sections | high | mitigate | Append-only discipline, asserted by a diff criterion that counts removed lines |
| T-198-40-04 | Spoofing | a local green presented as a CI verdict | high | mitigate | D-01 is restated in the objective and in each status criterion; GREEN-04's criterion requires the job conclusion and forbids local command output |
| T-198-40-05 | Tampering | re-running or selectively retrying a check to obtain a better conclusion | high | mitigate | The run's `attempt` is recorded and any value above 1 requires a verbatim explanation; the plan forbids re-dispatch outright |
| T-198-40-06 | Elevation of Privilege | narrowing `ci-required` or regenerating a baseline in response to a red lane observed on this run | high | mitigate | D-39 and D-42 prohibitions are authored into `must_haves.prohibitions`; Task 3 closes with a mandatory D-42 invariants diff that must be empty and must be reported as a finding if it is not |
| T-198-40-SC | Tampering | package-manager installs | low | accept | No dependency is added, removed, or upgraded; this plan modifies planning records only |

Canon-referral breadcrumb: generic credential-handling, transport-security and supply-chain
obligations (OWASP ASVS L1) are canon and are not restated as plan-local prohibitions.
</threat_model>

<artifacts_this_phase_produces>
**New files**
- `.planning/phases/198-green-bringup/198-40-SUMMARY.md`
- `.planning/phases/198-green-bringup/198-40-PLAN.md` (this file)

**Modified files**
- `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` — new `## Round 6` prediction section and `## Round 6 — Measured CI run` section, appended only
- `.planning/REQUIREMENTS.md` — GREEN-04 and GREEN-07 set from the measured run; a carried-forward statement naming the other nine
- `.planning/STATE.md` — current position and progress blocks

**Named artifacts created outside the repository**
- one git branch (round-6 measurement branch, pushed by the maintainer)
- one draft DO NOT MERGE pull request
- one GitHub Actions workflow run id, recorded on disk

**Commands used as instruments (none created)**
- `git push` (maintainer only), `gh pr create --draft`, `gh run list`, `gh run view`, `gh pr view`, `git diff --stat`, `git rev-list`

**Config keys / CLI tasks / source symbols**: none created or changed. No source file is modified by this plan.
</artifacts_this_phase_produces>

<verification>
- `## Round 6` prediction and measured sections both exist in `198-CI-MEASUREMENT.md`, with rounds 1 through 5 byte-unchanged.
- A run id and head SHA are recorded; the head SHA matches the locally resolved branch tip.
- All 12 `needs:` members have a recorded literal conclusion string; `CI required`'s conclusion is recorded verbatim.
- Every still-red check carries a root-cause citation.
- GREEN-04's status cites this run's job conclusion; GREEN-07 is not Complete and cites both the run and `198-39-DECISION.md`.
- The carried-forward statement names all nine carried requirements with their round-5 citations.
- `git diff --stat` over `.github/`, `CONTRIBUTING.md`, `playwright.config.ts`, `.planning/scorecards/` and `*.png` for the round-6 range is empty and that result is recorded.
- No push was performed by the agent; no check was re-run.
</verification>

<success_criteria>
- One real, measured CI run anchors every status this round sets.
- The prediction was committed before the push and scored without edit.
- Every requirement ID GREEN-01 through GREEN-12 has an explicit, cited status or an explicit carry.
- No gate narrowed, no baseline regenerated, no test weakened, no prior record retro-edited, no autonomous push.
</success_criteria>

<output>
Create `.planning/phases/198-green-bringup/198-40-SUMMARY.md` when done, including the run id, the
per-job table, the prediction scorecard, and the D-42 invariants result verbatim.
</output>
