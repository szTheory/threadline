---
phase: 198-green-bringup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audits/198-ci-run-28214113903-logs.md
  - .planning/audits/198-credo-full-default.json
  - .planning/audits/198-credo-histogram.md
  - .planning/audits/198-mechanical-sensitivity.md
autonomous: true
requirements: [GREEN-01, GREEN-02, GREEN-03]

estimate:
  tokens: 84000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The failing logs of GitHub Actions run 28214113903 are readable from a file committed in this repository, and that file states in its own header that the run predates the local matrix work and therefore describes the older ci.yml on origin/main (D-36)."
    - "A per-check Credo finding histogram AND a per-file concentration table are readable from committed files, and `git diff --exit-code .credo.exs` exits 0 for the whole plan (D-37)."
    - "The Credo histogram was produced with `--config-file` pointing at a config held outside the repo's `.credo.exs`, and its total issue count is strictly greater than the count produced by the repo's own `.credo.exs` (proving the flag was honored, RESEARCH Pitfall 4)."
    - "A committed finding states, with an executed before/after result attached, whether MechanicalChecker is sensitive to rendered text content and to text width, or only to tokens, contrast, and element geometry (GREEN-03, D-38)."
    - "`git status --porcelain .planning/scorecards/` is empty after the GREEN-03 probe runs — no committed scorecard was read-modified-written (D-38)."
    - statement: "If run 28214113903's logs have already been purged by GitHub at execution time, the plan records the purge and its date in the artifact rather than emitting an empty or fabricated log file."
      verification: backstop
    - statement: "Re-running the measurement sweep concurrently or after interruption does not corrupt a partially-written audit artifact — each artifact is written whole or not at all (GREEN-02 concurrency edge)."
      verification: backstop
    - "GREEN-03 empty edge: the probe explicitly records what MechanicalChecker.run/1 returns for an empty scratch scorecard directory, and that result is stated in the finding."
    - statement: "GREEN-03 encoding edge: the text-content variant used in the probe differs by grapheme content, and the finding states which notion of text difference (bytes vs code points vs rendered width) the checker actually reacted to."
      verification: backstop
  artifacts:
    - .planning/audits/198-ci-run-28214113903-logs.md
    - .planning/audits/198-credo-full-default.json
    - .planning/audits/198-credo-histogram.md
    - .planning/audits/198-mechanical-sensitivity.md
  key_links:
    - "The mechanical-sensitivity finding is the sizing input for Phase 201 Tier 2 — it must state a decision-grade answer, not a description of the code."
    - "The Credo histogram + per-file concentration table are the sizing input for Phase 203 (GATE-01/GATE-02)."
    - "No artifact produced here may ever be read by a gate (Phase 199 / DECOUPLE-01)."
  prohibitions:
    - statement: "MUST NOT modify `.credo.exs` in any way while producing the Credo histogram, including a temporary edit that is reverted."
      status: flagged-unverified
      verification: manual
    - statement: "MUST NOT write to, copy over, or restore any file under `.planning/scorecards/` while running the mechanical-sensitivity probe."
      status: flagged-unverified
      verification: manual
  flagged_assumptions:
    - "GREEN-01 edge probe returned `unclassified — review manually` and stays unresolved. Planner assumption: the only edge that matters for GREEN-01 is log availability at execution time, which is covered by the backstop truth above. Surfaced, not dismissed."
---

<objective>
Produce the read-only measurement sweep that sizes Phases 201 and 203, before any mutation of this repository happens.

Purpose: D-36 locks this plan as read-only and first. Preserving run `28214113903` is a ~4-week window against GitHub's 90-day log purge. The Credo histogram sizes Phase 203; the mechanical-sensitivity finding sizes Phase 201 Tier 2. Neither downstream phase may be planned before these artifacts exist on disk.
Output: four committed artifacts under `.planning/audits/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-PATTERNS.md
</context>

<assumption_delta_decision>
**Detector signal:** one weak `pluralization` hit on the noun `fallback`, fired on the roadmap's contingency note ("the fallback is moving the browser lanes off the PR trigger").

**Noun now primary:** none. `fallback` here names a contingency plan for a risk, not a new first-class entity in the domain model. No identity model changed: `AuditTransaction`, `AuditChange`, `AuditAction`, `AuditContext`, `ActorRef`, and `Correlation` are untouched by this phase (it is a bringup/gating phase with no capture, semantics, or query change).

**Decision:** `no-change`.

**Rationale:** The signal is a contingency noun in prose, not a pluralized domain concept requiring promotion to a first-class type.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Preserve run 28214113903's failing logs end-to-end</name>
  <files>.planning/audits/198-ci-run-28214113903-logs.md</files>
  <read_first>
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` — D-36 (read-only, first; the staleness caveat requirement)
    - `.planning/ROADMAP.md` §"Phase 198" Notes — the ~62-days-old / 90-day-purge framing
  </read_first>
  <action>
    This plan is read-only with respect to every layer the phase modifies (D-36) — it touches the GitHub-platform tier only as a reader, and writes only to `.planning/audits/`. The phase's tracer slice is Plan 02 Task 1, which runs in wave 2 before any expansion plan.

    Create the directory `.planning/audits/` if absent. Run `gh run view 28214113903 --log-failed` and capture stdout into `.planning/audits/198-ci-run-28214113903-logs.md`, wrapped in a fenced block, preceded by a header block that records verbatim: the run id `28214113903`, the run's `conclusion`, `createdAt`, `headSha`, and `workflowName` obtained from `gh run view 28214113903 --json conclusion,createdAt,headSha,workflowName`, the capture date, and the D-36 caveat stated as its own line — that this run predates the local matrix commits `cce7f409`/`8fe32c6b` and therefore describes the OLDER `ci.yml` on `origin/main` (`67998e0b`), not the current pipeline.

    If `gh run view 28214113903 --log-failed` fails because the logs have already been purged or the run is not found, do NOT emit an empty or reconstructed log file. Instead write the same header block plus an explicit `## Status: LOGS UNAVAILABLE` section recording the exact `gh` error text and the date the purge was observed. GREEN-01 is then satisfied by an honest record of loss, not a fabrication.

    Also run `gh run view 28214113903 --json jobs --jq '[.jobs[] | {name, conclusion}]'` and record the per-job conclusion table so a future reader can tell which lanes were red without re-fetching.

    Commit with message `docs(198): preserve run 28214113903 failing logs (GREEN-01)`.
  </action>
  <verify>
    <automated>test -f .planning/audits/198-ci-run-28214113903-logs.md && grep -q '28214113903' .planning/audits/198-ci-run-28214113903-logs.md && grep -qi 'predates' .planning/audits/198-ci-run-28214113903-logs.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audits/198-ci-run-28214113903-logs.md` exists and is tracked by git (`git ls-files --error-unmatch` on it exits 0).
    - The file contains the literal run id `28214113903`.
    - The file contains a caveat line stating the run predates the local matrix work and describes the older `ci.yml` on `origin/main`.
    - The file contains either a fenced log capture OR a `## Status: LOGS UNAVAILABLE` section with the verbatim `gh` error — never both empty.
    - The file contains a per-job name/conclusion table.
    - `git status --porcelain` shows no modification to any file outside `.planning/audits/`.
  </acceptance_criteria>
  <done>The last red run's failing logs (or an honest record of their loss) are readable from the repository with the staleness caveat attached.</done>
</task>

<task type="auto">
  <name>Task 2: Credo full-default histogram and per-file concentration, with `.credo.exs` untouched</name>
  <files>.planning/audits/198-credo-full-default.json, .planning/audits/198-credo-histogram.md</files>
  <read_first>
    - `.credo.exs` — the current config (enables only `Readability.ModuleDoc` and `Design.TagTODO`); read it to establish the baseline issue count, do NOT edit it
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` — D-37 (external config via `--config-file`, per-check histogram AND per-file concentration table)
    - `.planning/phases/198-green-bringup/198-RESEARCH.md` §"Pattern 5" and §"Pitfall 4" — `--config-file` replaces rather than merges; the positional-vs-flag trap
  </read_first>
  <action>
    Generate a stock full-default Credo config OUTSIDE the repository working tree — write it to `/tmp/198-full-default.credo.exs`. Produce it with `mix credo.gen.config` and redirect/move the output out of the repo (never let it land at `.credo.exs`); if `credo.gen.config` insists on writing `.credo.exs`, generate into an empty scratch directory instead and move the file to `/tmp/`. Set `strict: true` in it and keep `included`/`excluded` matching this repo's `lib/`, `test/`, `config/` scope.

    First record the BASELINE: `mix credo --strict --format json` (repo config) and store its `.issues | length` as `baseline_count`.

    Then run the full-default measurement: `mix credo --strict --config-file /tmp/198-full-default.credo.exs --format json > .planning/audits/198-credo-full-default.json`. Pass `--config-file` as a NAMED FLAG — a bare positional path means "analyze only this path" and silently ignores the intended config (RESEARCH Pitfall 4).

    Derive two tables into `.planning/audits/198-credo-histogram.md`:
    - Per-check histogram: `jq '.issues | group_by(.check) | map({check: .[0].check, count: length}) | sort_by(-.count)'`
    - Per-file concentration: same shape keyed on `.filename`, sorted descending, with a total row.
    Include in the header: `baseline_count`, the full-default total, the Credo version (`mix credo --version`), the config path used, and the capture date. Assert in prose and in the numbers that full-default total > `baseline_count`; if it is not, the flag was not honored — stop and re-run rather than committing a false measurement.

    Confirm and record that `git diff --exit-code .credo.exs` exits 0 at the end.

    Commit with message `docs(198): credo full-default histogram and per-file concentration (GREEN-02)`.
  </action>
  <verify>
    <automated>git diff --exit-code .credo.exs && test -f .planning/audits/198-credo-full-default.json && test -f .planning/audits/198-credo-histogram.md && jq -e '.issues | length > 0' .planning/audits/198-credo-full-default.json</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code .credo.exs` exits 0 and `git log -1 --name-only` does not list `.credo.exs`.
    - `.planning/audits/198-credo-full-default.json` parses as JSON and has a non-empty `.issues` array.
    - `.planning/audits/198-credo-histogram.md` contains a per-check table and a separately-headed per-file concentration table.
    - The histogram header records the baseline issue count from the repo's own `.credo.exs` and the full-default total, and the full-default total is strictly greater.
    - The histogram header names `/tmp/198-full-default.credo.exs` (or the actual out-of-repo path used) as the config source.
    - `git ls-files .credo.exs` still resolves and its content hash is unchanged from `HEAD~` at commit time.
  </acceptance_criteria>
  <done>A maintainer can read a measured per-check histogram and per-file concentration table produced from a full-default config, with `.credo.exs` provably unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: Scorecard-free mechanical-sensitivity probe (GREEN-03)</name>
  <files>.planning/audits/198-mechanical-sensitivity.md</files>
  <read_first>
    - `lib/threadline/operator_surface/mechanical_checker.ex` — read `run/1` (approx. line 92) and every private check function it dispatches to, so the enumerated signal list is derived from source, not memory
    - one file under `.planning/scorecards/*.json` — READ ONLY, to learn the scorecard schema and which fields are actually populated
    - `.planning/phases/198-green-bringup/198-PATTERNS.md` §"D-38" — confirms `run/1` is path-parameterized via `:scorecard_dir` defaulting to `.planning/scorecards`
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` — D-38 (evidence, not inference; no scorecard touched)
  </read_first>
  <action>
    Answer GREEN-03 from an executed experiment, not from reading the implementation.

    Build synthetic scorecards in a scratch directory `/tmp/198-mechanical-probe/` — never inside `.planning/`. Start from a copy of one committed scorecard's STRUCTURE (schema shape), then create these variants, each differing from the control in exactly one dimension:
    - `control.json` — the unmodified baseline shape
    - `text-content.json` — identical tokens, identical contrast values, identical geometry; only rendered text content strings changed (use a grapheme-distinct replacement, e.g. a string whose byte length, code-point count, and rendered width all differ, and record which of those three actually changed)
    - `text-width.json` — identical text content and tokens; only the width/bounding-box value(s) changed
    - `token.json` — a deliberate token/contrast violation, as the positive control proving the probe harness itself has teeth
    - an empty directory case, to record `run/1`'s behavior on zero scorecards

    Invoke `Threadline.OperatorSurface.MechanicalChecker.run(scorecard_dir: "/tmp/198-mechanical-probe/<variant>")` directly — from `mix run` with a one-off script under `/tmp` or `iex -S mix`. Do NOT invoke `mix verify.mechanical`, which is hard-wired to the committed set. Do NOT copy, move, restore, or write anything under `.planning/scorecards/`.

    Write `.planning/audits/198-mechanical-sensitivity.md` containing: the enumerated list of signals `run/1` actually extracts (with source line references); a results table with one row per variant giving the exact `{:ok, []}` / `{:error, violations}` return; the positive-control result proving the harness detects a real violation; the empty-directory result; and a single-sentence **Finding** that states the decision-grade answer — whether the checker is sensitive to rendered text content and to text width, or only to tokens, contrast, and element geometry — plus a **Sizing implication for Phase 201 Tier 2** paragraph.

    End by asserting `git status --porcelain .planning/scorecards/` is empty and record that in the artifact.

    Commit with message `docs(198): mechanical-checker sensitivity probe finding (GREEN-03)`.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain .planning/scorecards/)" && test -f .planning/audits/198-mechanical-sensitivity.md && grep -q '^## Finding' .planning/audits/198-mechanical-sensitivity.md</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain .planning/scorecards/` produces empty output.
    - `git log -1 --name-only` lists no path under `.planning/scorecards/`.
    - `.planning/audits/198-mechanical-sensitivity.md` contains a `## Finding` heading whose body names either "sensitive to text content" or "not sensitive to text content" explicitly, and likewise for text width — no hedged or absent verdict.
    - The artifact contains a results table with at least the rows: control, text-content variant, text-width variant, token positive control, empty directory.
    - The token positive-control row shows an `{:error, ...}` result, proving the harness can detect a violation.
    - The artifact cites at least one `lib/threadline/operator_surface/mechanical_checker.ex:<line>` reference for the enumerated signal list.
    - The artifact contains a `Sizing implication for Phase 201 Tier 2` section.
  </acceptance_criteria>
  <done>The maintainer can state from executed evidence whether `verify.mechanical` reacts to text content and width, with no scorecard touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → GitHub API (`gh`) | Maintainer credential is used to read run logs; log content may itself contain secrets echoed by an older workflow |
| repo working tree → `.planning/audits/` commit | Anything written here becomes public in Plan 06's push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-01-01 | Information Disclosure | `.planning/audits/198-ci-run-28214113903-logs.md` | high | mitigate | CI logs can contain masked-but-leaked env output. Plan 06's `gitleaks`/`trufflehog` sweep (D-28) runs over the tree INCLUDING this artifact before any push; the artifact is committed here but published only after that gate. |
| T-198-01-02 | Tampering | `.credo.exs` | medium | mitigate | `git diff --exit-code .credo.exs` is the task's own automated verify; the external config lives at `/tmp/`, outside the tree. |
| T-198-01-03 | Tampering | `.planning/scorecards/*` | high | mitigate | Probe runs against `/tmp/198-mechanical-probe/` via `run(scorecard_dir:)`; `git status --porcelain .planning/scorecards/` asserted empty. Regenerating a scorecard would manufacture rather than measure the Phase 201 floor. |
| T-198-01-04 | Repudiation | run-log artifact | low | accept | If GitHub has purged the run, the artifact records the loss with the verbatim error and date; no reconstruction is attempted. |
</threat_model>

<verification>
- `git diff --exit-code .credo.exs` — clean
- `git status --porcelain .planning/scorecards/` — empty
- All four artifacts exist under `.planning/audits/`
- `git diff --name-only HEAD~3..HEAD` lists only paths under `.planning/audits/` — this plan mutates nothing else
</verification>

<success_criteria>
GREEN-01, GREEN-02, and GREEN-03 each have a committed, readable artifact carrying a decision-grade answer, produced without modifying `.credo.exs` or any scorecard.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan (new symbols — exclude from drift verification):
- `.planning/audits/` (new directory)
- `.planning/audits/198-ci-run-28214113903-logs.md`
- `.planning/audits/198-credo-full-default.json`
- `.planning/audits/198-credo-histogram.md`
- `.planning/audits/198-mechanical-sensitivity.md`
- `/tmp/198-full-default.credo.exs`, `/tmp/198-mechanical-probe/` (scratch, not committed)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/198-green-bringup/198-01-SUMMARY.md` when done
</output>
