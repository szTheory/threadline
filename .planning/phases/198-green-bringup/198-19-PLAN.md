---
phase: 198-green-bringup
plan: 19
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - test/threadline/storage_schema_call_site_contract_test.exs
autonomous: true
requirements:
  - GREEN-04

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `verify-test` job's `current` lane prepares `threadline_phoenix_test` with `threadline` on the database search_path exactly as its two sibling jobs do, so `mix verify.example`'s `ThreadlinePhoenixWeb.WalkthroughHappyPathTest` no longer hits `(undefined_table) relation \"audit_transactions\" does not exist`."
    - "`.github/workflows/ci.yml` contains exactly three `ALTER DATABASE` statements and all three of them name `threadline_phoenix_test` — the example-app database — so the count of `ALTER DATABASE` occurrences equals the count of `ALTER DATABASE threadline_phoenix_test SET search_path TO` occurrences."
    - "The static call-site sweep detects `Threadline.Test.Repo.delete_all(AuditChange)` (a fully-qualified receiver chain) as `in_scope: true, offense: true`, closing CR-01's blind spot over 73 real call sites."
    - "The static call-site sweep detects `repo.insert_all(AuditChange, entries)` with no trailing opts as `in_scope: true, offense: true`, closing CR-02's blind spot."
    - "The sweep still rejects `MyRepo.delete_all(AuditChange)` and `some_repo.delete_all(AuditChange)` — widening the receiver pattern did not widen it into mid-identifier matching."
    - "The real-tree sweep over `test/**/*.exs` reports zero offenses after the widening — no live offense is introduced or newly revealed — while `in_scope_count` strictly increases versus the pre-fix detector, proving the widening admits real call sites rather than nothing."
    - statement: "Boundary/adjacency edge (GREEN-04): a receiver glued to a preceding word character (`MyRepo`, `some_repo`) does not match, while a receiver preceded only by a `.` in a CamelCase chain (`Test.Repo`, `Threadline.Test.Repo`) does — the two cases are one character apart and both are asserted."
      verification: explicit
    - statement: "Empty edge (GREEN-04): the real-tree sweep asserts a non-empty file glob and `in_scope_count > 0`, so a detector that silently matches nothing cannot pass vacuously."
      verification: explicit
    - statement: "Ordering edge (GREEN-04): `@ecto_functions` alternation ordering does not affect results — `insert_all(` resolves to `insert_all` and not to a partial `insert` match, and `get_by(` resolves to `get_by` and not `get`, because the pattern anchors on a literal open paren."
      verification: explicit
    - statement: "Idempotency edge (GREEN-04): the `current`-lane db-prep step is re-runnable on the same runner — `createdb` is guarded to tolerate an existing database and the `ALTER DATABASE ... SET search_path` restates the same value, so a second execution changes nothing and exits 0."
      verification: explicit
    - statement: "Concurrency edge (GREEN-04): the sweep opens no database connection and runs `async: true` under ExUnit, so parallel execution with the rest of the suite is safe by construction."
      verification: backstop
  artifacts:
    - ".github/workflows/ci.yml — `verify-test` current-lane db-prep step carrying the search_path statement"
    - "test/threadline/storage_schema_call_site_contract_test.exs — widened `@call_regex`, extended `@ecto_functions`, and the CR-01/CR-02 regression fixtures"
  key_links:
    - "verify-test (current lane) db-prep step → `mix verify.example` → `ThreadlinePhoenixWeb.WalkthroughHappyPathTest` → the `threadline` schema's `audit_transactions` table"
    - "`Threadline.StorageSchemaCase.owned_schema_modules/0` → the sweep's derived owned-name roster (never re-typed, cannot drift)"
  prohibitions:
    - statement: "Tier-A `page.*` evidence regeneration is forbidden for this milestone (maintainer-ratified twice, most recently 2026-08-28). No task in this plan may run a capture regeneration or modify a Tier-A scorecard."
      status: recalled
      verification: flagged-unverified
    - statement: "`ALTER DATABASE ... SET search_path` must never be applied to the `threadline_test` database. A database-level search_path there would mask the unprefixed-call-site defect class GREEN-04 exists to close. It is valid for the example-app database only."
      status: recalled
      verification: flagged-unverified
    - statement: "No test may be skipped, tagged out, deleted, or loosened to make this plan pass. Every former failure is fixed on its merits (GREEN-04's own wording)."
      status: recalled
      verification: flagged-unverified
    - statement: "The sweep must gain no escape hatch — no permitted-file collection, no opt-out source marker, no skipped-path constant (198-CONTEXT.md D-05 anti-laundering)."
      status: recalled
      verification: flagged-unverified
---

<objective>
Close the two remaining engineering causes behind GREEN-04, measured on CI run
`33197493051`:

1. `.github/workflows/ci.yml:235-240` — the `verify-test` job's `current`-lane
   db-prep step omits the `ALTER DATABASE threadline_phoenix_test SET search_path`
   statement that its sibling jobs at `:346-355` (`verify-example-browser`) and
   `:500-509` (`verify-capture`) both carry. Consequence: `mix verify.example`'s
   `ThreadlinePhoenixWeb.WalkthroughHappyPathTest` fails with
   `(undefined_table) relation "audit_transactions" does not exist` (9 failures).
2. `test/threadline/storage_schema_call_site_contract_test.exs` — CR-01 and CR-02
   from `198-REVIEW.md`, two reproduced regex blind spots that make 75 real call
   sites invisible to a sweep whose stated must-have is exhaustiveness.

Purpose: (1) is the single highest-leverage fix in round 3 — it is the last known
cause standing between `Run test suite (current)` and green. (2) is not urgent (no
live offense exists today) but GREEN-04's bar is "each former failure fixed on its
merits", and a sweep that cannot see 75 real call sites is a false completeness
guarantee, not that bar met.

Output: a corrected `ci.yml` db-prep step and a sweep with teeth, both proven
locally, ready for the push-and-measure in plan 198-21. **Local proof is a
precondition for pushing, never proof that GREEN-04 is met** — that verdict belongs
to a real CI run (198-CONTEXT.md D-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-REVIEW.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — the current lane prepares the example-app database with `threadline` on its search_path</name>
  <read_first>
    - `.github/workflows/ci.yml` lines 157-249 — the whole `verify-test` job: its `name: Run test suite`, the `lane: [min, current]` matrix, and every `if: matrix.lane == 'current'` step. Read this first so the placement of the change is grounded in the job's real step order, not assumed.
    - `.github/workflows/ci.yml` lines 346-355 — `verify-example-browser`'s step `Ensure threadline_phoenix_test database exists`, the sibling being matched, including its explanatory comment about `demo.seed` issuing unqualified queries.
    - `.github/workflows/ci.yml` lines 500-509 — `verify-capture`'s step `Ensure threadline_phoenix_test database exists (threadline on search_path)`, the second sibling.
    - `.planning/phases/198-green-bringup/198-VERIFICATION.md` — the `gaps:` frontmatter entry for GREEN-04 and the "What Round 3 must do" section item 1.
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` — decision D-01 (what "green" means) and the deferred-items note at line 218 describing the example-app `ALTER DATABASE` as a known wart whose *removal* is what would force Tier-A regeneration.
  </read_first>
    <files>.github/workflows/ci.yml</files>
  <precondition>A local PostgreSQL reachable at `DB_HOST`/`DB_PORT` with a `postgres` superuser, and `examples/threadline_phoenix` deps fetchable — `mix verify.example` cannot run without both.</precondition>
  <reversibility rating="reversible">Adding a database-level search_path to the example-app test database in one more CI job mirrors two existing jobs exactly; reverting is deleting the two added lines.</reversibility>
  <action>
    In `.github/workflows/ci.yml`, extend the `verify-test` job's step named
    `Ensure threadline_phoenix_test database exists` (the one carrying
    `if: matrix.lane == 'current'`, currently a single `createdb` line) so that,
    after the `createdb` call, it invokes `psql` against the host in `$DB_HOST` as
    user `postgres` on database `threadline_phoenix_test` with the statement
    `ALTER DATABASE threadline_phoenix_test SET search_path TO "$user", public, threadline;`.
    Reproduce the sibling steps' exact escaping of the `$user` role placeholder and
    their exact `PGPASSWORD: postgres` env block; the two siblings are the
    authoritative shape and this step must end up textually equivalent to them
    apart from the `if: matrix.lane == 'current'` guard it already carries.

    Carry over the siblings' explanatory comment (the audit tables live in the
    dedicated `threadline` schema and the demo seed queries them unqualified, so
    the schema must be on the database search_path because nothing in app config or
    a migration sets it), and additionally record in that comment that this
    statement targets the example-app database and is deliberately confined to it.

    Do not touch the root library's own test database in any way — this plan makes
    no schema, config, or migration change to it, and the root suite's freedom from
    a database-level search_path is exactly the property the static sweep in Task 2
    exists to protect.

    Also rename the step so it matches the `verify-capture` sibling's more precise
    wording, keeping the job `id:` `verify-test` and the job `name:`
    `Run test suite` byte-identical — the CLAUDE.md stable-job-id convention and the
    GREEN-08 exact-match contract both key on those two, never on step names.
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'ALTER DATABASE'` outputs `3`.
    - `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'ALTER DATABASE threadline_phoenix_test SET search_path TO'` also outputs `3` — the two counts being equal proves every `ALTER DATABASE` in the workflow names the example-app database and no other.
    - `grep -c 'name: Run test suite$' .github/workflows/ci.yml` outputs `1` and `grep -c '^  verify-test:' .github/workflows/ci.yml` outputs `1` — the job id and required-check-visible name are unchanged.
    - `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (the workflow still parses).
    - Local end-to-end reproduction: with the example-app database dropped and recreated from scratch, `DB_HOST=localhost mix verify.example` completes with `0 failures`, and `psql -d threadline_phoenix_test -Atc "SHOW search_path"` contains `threadline`.
    - Idempotency: executing the step's shell body twice in a row exits 0 both times and the second `SHOW search_path` output is byte-identical to the first.
    - No file outside `.github/workflows/ci.yml` is modified by this task: `git diff --name-only` lists exactly that one path.
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'ALTER DATABASE' | grep -qx 3 &amp;&amp; grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'ALTER DATABASE threadline_phoenix_test SET search_path TO' | grep -qx 3 &amp;&amp; python3 -c "import yaml;yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <done>The `current` lane's db-prep step carries the search_path statement, all three `ALTER DATABASE` statements in the workflow name the example-app database, the workflow parses, and `mix verify.example` runs green locally against a freshly created example-app database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close CR-01 and CR-02 — the sweep sees fully-qualified receivers and bulk inserts</name>
  <read_first>
    - `test/threadline/storage_schema_call_site_contract_test.exs` in full — the moduledoc's "no escape hatch" claim, `@ecto_functions` (lines 38-52), `@call_regex` (lines 56-66), `scan_call_sites/2`, `balanced_call_expression/3`, `@trailing_var_regex`, `contains_unclosed?/3`, and every existing behaviour case, so the new fixtures follow the file's own assertion style rather than inventing one.
    - `.planning/phases/198-green-bringup/198-REVIEW.md` — CR-01 and CR-02 in full, including the already-specified regex replacement and the already-specified `@ecto_functions` roster. These are the target state; do not re-derive them.
    - `test/support/storage_schema_case.ex` — `owned_schema_modules/0` and `repo_opts/0,1`, the roster source the sweep derives from.
    - `test/threadline/operator_surface/live/timeline_live_test.exs` around line 458 and `test/threadline/operator_surface/controllers/export_controller_test.exs` around line 431 — the two real `insert_all` call sites that must become visible.
    - `.planning/phases/198-green-bringup/198-14-SUMMARY.md` — the exhaustiveness claim this task makes true.
  </read_first>
  <files>test/threadline/storage_schema_call_site_contract_test.exs</files>
  <behavior>
    - `Threadline.Test.Repo.delete_all(AuditChange)` → `in_scope: true, offense: true`
    - `Threadline.Test.Repo.delete_all(AuditChange, repo_opts())` → `in_scope: true, offense: false`
    - `repo.insert_all(AuditChange, entries)` → `in_scope: true, offense: true`
    - `@repo.insert_all(AuditChange, chunk, repo_opts())` → `in_scope: true, offense: false`
    - `MyRepo.delete_all(AuditChange)` → no match at all (not in scope, not an offense)
    - `some_repo.delete_all(AuditChange)` → no match at all
    - `Repo.delete_all(AuditChange)` → still `in_scope: true, offense: true` (unchanged behaviour)
    - Real-tree sweep over `test/**/*.exs` → zero offenses, non-empty glob, `in_scope_count` strictly greater than the pre-fix value
  </behavior>
  <action>
    Write the failing fixtures FIRST and observe them red against the current
    detector, then apply the two fixes and observe them green. Record both
    observations in the plan SUMMARY — a fixture that was never red proves nothing.

    Fix CR-01 by replacing `@call_regex`'s receiver clause with the review's
    specified form: drop the `.` from the negative lookbehind class so it becomes a
    word-character-only lookbehind, and permit an arbitrary chain of capitalised
    dot-terminated module segments in front of the literal `Repo`, while leaving the
    `@repo` and lowercase `repo` alternatives untouched. The result must admit
    `Test.Repo.` and `Threadline.Test.Repo.` while continuing to reject `MyRepo.`
    and `some_repo.`, which is exactly the pair of cases the behaviour block above
    pins.

    Fix CR-02 by extending `@ecto_functions` with the Ecto.Repo callback names the
    review lists as absent: `insert_all`, `insert_or_update!`, `insert_or_update`,
    `update_all` if not already present, and `stream`. Keep the file's existing
    convention of listing the longer, more specific variant before its prefix, and
    keep the moduledoc's note that the trailing literal open-paren anchor makes
    ordering immaterial — then prove that note with the ordering fixtures in the
    behaviour block.

    Update the moduledoc so its exhaustiveness language describes what the detector
    now actually does, naming the two receiver-shape families it recognises. Do not
    soften the no-escape-hatch paragraph — nothing in this task adds an exemption
    surface, and D-05 forbids one.

    Strengthen the real-tree test's non-vacuity assertion: alongside the existing
    `in_scope_count > 0` check, assert that at least one scanned call site uses the
    fully-qualified receiver form, so a future regression that re-narrows the regex
    fails here rather than passing silently on the aliased-receiver sites alone.

    Leave `contains_unclosed?/3` alone — IN-02 is explicitly out of scope for round 3.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/storage_schema_call_site_contract_test.exs` passes with `0 failures`.
    - Red-then-green teeth proof: with the `@call_regex` change reverted (`git stash` of that hunk alone), the new fully-qualified-receiver fixture fails; with `@ecto_functions`' new entries reverted, the new `insert_all` fixture fails. Both observed and recorded in the SUMMARY with their failure output.
    - The test module contains a case asserting `Threadline.Test.Repo.delete_all(AuditChange)` yields `offense: true` and a case asserting `repo.insert_all(AuditChange, [...])` with no opts yields `offense: true`.
    - The test module contains cases asserting `MyRepo.delete_all(` and `some_repo.delete_all(` yield an empty scan result.
    - `grep -c 'insert_all' test/threadline/storage_schema_call_site_contract_test.exs` is at least `3` (roster entry plus at least two fixtures).
    - The real-tree sweep reports zero offenses; `mix test` over the whole suite reports the same failure count as before this task (currently `0 failures`, 1 excluded) — the widening reveals no live offense.
    - `mix format --check-formatted` exits 0 and `mix credo --strict` reports no new issues for this file.
    - No file outside `test/threadline/storage_schema_call_site_contract_test.exs` is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/storage_schema_call_site_contract_test.exs &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>The sweep detects both previously-invisible call-site shapes as offenses when unprefixed, still rejects mid-identifier receivers, reports zero offenses against the real tree, and each fix was observed red before green.</done>
</task>

<task type="auto">
  <name>Task 3: Full local suite plus a fresh-clone-shaped `mix ci.all` reproduction, recorded honestly</name>
  <read_first>
    - `mix.exs` lines 87-160 — the `verify.*` alias definitions and the `ci.all` composition order, so the run is the project's own canonical entrypoint rather than an ad-hoc command (CLAUDE.md CI conventions).
    - `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` — the round-2 measurement artifact whose format this task appends to, including its local-run baseline of 1412 tests / 0 failures.
    - `.planning/phases/198-green-bringup/deferred-items.md` — plan 198-12's acknowledged example-app `DemoContractTest` demo-seed deferral, which is expected to still fail `mix ci.all` at the `verify.example` step locally and is explicitly out of round 3's scope.
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` decision D-01 — the definition of "green" this task must not overstate.
  </read_first>
  <files>.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</files>
  <action>
    Run `mix test` twice consecutively and record the exact test/failure/excluded
    counts from both runs, asserting they are identical. Then run `mix ci.all` and
    record, step by step, which aliases pass and which fail, with the failing step's
    name and the first failure's message.

    Append a section to `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md`
    titled for round 3 and dated, recording: the two `mix test` results, the
    `mix ci.all` step-by-step result, and an explicit statement that this local
    evidence is NOT admissible as proof for GREEN-04 or GREEN-07 and is being
    recorded only as a pre-push readiness signal, with the measured CI run in plan
    198-21 named as the authority.

    If `mix ci.all` fails only at `verify.example` on the already-deferred
    example-app `DemoContractTest` demo-seed assertions, record that as the known,
    previously-acknowledged deferral and cite `deferred-items.md`. If it fails
    anywhere else, do not paper over it — record the new failure verbatim and halt
    the plan rather than proceeding to push readiness, because a new local failure
    at this point is a fresh defect in the exact class GREEN-04 covers.
  </action>
  <acceptance_criteria>
    - `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` gains a round-3 section containing both `mix test` result lines verbatim and the `mix ci.all` step ledger.
    - The two recorded `mix test` result lines are byte-identical to each other.
    - The recorded `mix test` failure count is `0`.
    - The appended section contains an explicit sentence stating local evidence is inadmissible for GREEN-04 and GREEN-07 and naming plan 198-21's measured CI run as the authority.
    - Any `mix ci.all` failure is recorded with its step name and first failure message; if the failing step is anything other than the deferred example-app demo-seed contract, the plan halts with that failure quoted in the SUMMARY.
    - `git diff --name-only` for this task lists exactly `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md`.
  </acceptance_criteria>
  <verify>
    <automated>mix test 2>&amp;1 | tail -3 &amp;&amp; grep -q 'Round 3' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</automated>
  </verify>
  <done>Two byte-identical zero-failure local `mix test` runs and a step-by-step `mix ci.all` ledger are recorded in the measurement artifact, with the inadmissibility of local evidence stated in the artifact itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → PostgreSQL service container | CI shell steps issue superuser SQL (`createdb`, `ALTER DATABASE`) against an ephemeral service database |
| Workflow definition → merge gate | A change to `ci.yml` can alter what the required `CI required` aggregate actually proves |
| Repository source tree → static sweep detector | Contributor-authored test source is the untrusted input the regex sweep parses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-19-01 | Tampering | `ci.yml` verify-test db-prep step | high | mitigate | The added `ALTER DATABASE` is constrained to `threadline_phoenix_test`; acceptance criteria assert the count of `ALTER DATABASE` occurrences equals the count naming that database, so no statement can silently target the library's own test database |
| T-198-19-02 | Elevation of Privilege | PostgreSQL service container | low | accept | The `postgres` superuser and its literal password are the standard, already-established shape for the ephemeral CI service container in all three sibling jobs; nothing durable or externally reachable is created |
| T-198-19-03 | Information Disclosure | CI step logs | low | mitigate | The step logs only DDL against a throwaway database; `PGPASSWORD` is supplied via the step `env:` block exactly as the siblings do and is never echoed into the command line |
| T-198-19-04 | Repudiation | merge gate guarantee | medium | mitigate | No `ci-required` `needs:` entry, job `id:`, or job `name:` is changed by this plan; acceptance criteria assert `verify-test` / `Run test suite` remain byte-identical, so the gate's published guarantee is unchanged |
| T-198-19-05 | Spoofing | static sweep detector | medium | mitigate | Widening `@call_regex` risks admitting mid-identifier receivers; the `MyRepo` / `some_repo` negative fixtures pin the boundary, and the real-tree zero-offense assertion proves no false positive was introduced |
| T-198-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan (no dependency is added to `mix.exs`, `package.json`, or any lockfile), so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'ALTER DATABASE'` equals `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'ALTER DATABASE threadline_phoenix_test SET search_path TO'` and both equal 3.
- `mix test test/threadline/storage_schema_call_site_contract_test.exs` → 0 failures, with the four new fixtures present.
- `mix test` → 0 failures, twice, byte-identical.
- `git diff --stat` touches exactly `.github/workflows/ci.yml`, `test/threadline/storage_schema_call_site_contract_test.exs`, and `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md`.
</verification>

<success_criteria>
Both measured GREEN-04 causes are fixed on their merits, each proven red-then-green
locally, with no test skipped, weakened, or deleted, no Tier-A evidence regenerated,
and no search_path applied to the library's own test database. GREEN-04 is NOT
marked Complete by this plan — that awaits the measured CI run in 198-21.
</success_criteria>

## Artifacts this phase produces

**Changed CI symbols**
- `.github/workflows/ci.yml` — `verify-test` job, `current`-lane step renamed to
  `Ensure threadline_phoenix_test database exists (threadline on search_path)`,
  gaining a `psql ... ALTER DATABASE threadline_phoenix_test SET search_path TO "$user", public, threadline;`
  invocation. Job `id:` `verify-test` and job `name:` `Run test suite` unchanged.

**Changed test symbols** (`test/threadline/storage_schema_call_site_contract_test.exs`)
- `@ecto_functions` — gains `insert_all`, `insert_or_update!`, `insert_or_update`, `stream`
- `@call_regex` — receiver clause widened to admit CamelCase module chains
- New behaviour fixtures: fully-qualified `Threadline.Test.Repo.delete_all/1` offense
  and non-offense pair; `repo.insert_all/2` offense and `@repo.insert_all/3`
  non-offense pair; `MyRepo.`/`some_repo.` non-match pair
- New real-tree assertion: at least one scanned site uses the fully-qualified receiver form

**Changed planning artifacts**
- `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` — new "Round 3" section

**No new files, no new mix aliases, no new CI job ids.**

<assumption_delta_decision>
`assumption-delta scan 198 --json` returned `detected: true` on two weak prose
signals inside already-executed plans: a "fallback" mention (moving browser lanes
off the PR trigger) and an "optional" mention (the optional-Phoenix module guards).

**Decision: add alongside, do not promote.** Neither signal names a new assumption
that round 3 should elevate into a requirement. The "fallback" phrasing describes
exactly the disposition round 3 puts to the maintainer at 198-20's checkpoint — it
belongs in that decision brief's option set, where it now appears, rather than being
promoted into a locked assumption ahead of the maintainer's answer. The "optional"
signal refers to `verify.compile_no_optional`, which is Complete and re-confirmed
(GREEN-04 support work from plan 198-09) and is not in this round's scope.

Advisory only; no plan content changed on account of this scan.
</assumption_delta_decision>

<edge_coverage_note>
The spec-less probe fallback produced 28 applicable edge items across all twelve
GREEN requirements, all initially `unresolved`
(`{"applicable":28,"resolved":0,"unresolved":28}`).

**GREEN-04 (5 edges) and GREEN-07 (3 edges) — the two open requirements — are lifted
into `must_haves.truths` across plans 198-19, 198-20, 198-21, and 198-22**, each as
an auto-`resolved` item: `verification: explicit` where a defensible acceptance
criterion exists (11 of the 12 lifted statements), and `verification: backstop` as a
structured flat-scalar marker where the property holds by construction rather than by
assertion (2 statements: the sweep's `async: true`, no-database property in 198-19,
and the same property for the two new contract tests in 198-21).

**The remaining edges belong to the ten requirements already marked Complete and
re-confirmed by `198-VERIFICATION.md` (GREEN-01, 02, 03, 05, 06, 08, 09, 10, 11, 12).
They are carried by the already-executed plans 198-01 through 198-18 and are NOT
re-authored here** — this round is scoped to GREEN-04 and GREEN-07 by the round-2
verification's own gap list. This is stated explicitly rather than dropped silently:
if a later verification pass finds one of those ten regressed, its edges are
re-derivable from this same probe report. GREEN-08's `encoding` edge is the one
exception — it is re-authored in plan 198-21 because that plan makes the
exact-name-match contract executable for the first time.

Nothing was auto-dismissed. Three rows are `unclassified` in the probe output
(GREEN-01, GREEN-05, GREEN-09) and remain `unresolved`; they are flagged here as
planner assumptions rather than resolved. All three requirements are Complete and
re-confirmed, so none blocks this round.
</edge_coverage_note>

<output>
Create `.planning/phases/198-green-bringup/198-19-SUMMARY.md` when done.
</output>
