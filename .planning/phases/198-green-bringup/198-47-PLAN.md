---
phase: 198-green-bringup
plan: 47
type: execute
wave: 3
gap_closure: true
gap_ids: [G-198-R9-STALE-VERIFICATION, G-198-R9-GREEN-07]
depends_on: [198-46]
files_modified:
  - .planning/phases/198-green-bringup/198-VERIFICATION.md
  - .planning/phases/198-green-bringup/198-UAT.md
  - .planning/phases/198-green-bringup/198-47-SUMMARY.md
autonomous: true
requirements: [GREEN-08]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The authoritative verification section is newer than Round 6 and labels 192/192 over summaries 198-01..46 as the immutable Plan 47 entry snapshot, then separately proves the final 198-01..47 classifier/UAT state after the required 198-47-SUMMARY.md exists; an evaluator pass is not treated as requirement success."
    - "GREEN-07 remains Pending because live origin/main does not contain local main and the newest canonical exact-origin/main-SHA CI run reports failure; PR #34's successful branch run is recorded as branch evidence only."
    - "GREEN-08 remains Complete for its exact emitted-check contract while roadmap success criterion 4 remains partial because PR #26 is still BLOCKED downstream of GREEN-07, preserving the existing distinction instead of forcing the two artifacts to the same score."
    - "The final ancestry predicate is reported from the immutable entry snapshot plus the mandatory Plan 47 task and summary commit categories; the pre-task numeric ahead count is never presented as the closeout count."
    - "Round 1 through Round 8 evidence, 198-39-DECISION.md, REQUIREMENTS.md, ROADMAP.md, and STATE.md remain unchanged; 198-UAT.md changes only to add 198-47-SUMMARY.md to its exhaustive source list while its 192/192 zero-human totals remain derived and unchanged."
  artifacts:
    - ".planning/phases/198-green-bringup/198-VERIFICATION.md — authoritative current-state verification section plus preserved archived Round 6 and earlier evidence"
    - ".planning/phases/198-green-bringup/198-UAT.md — exhaustive 198-01..47 source list with unchanged derived 192/192 coverage totals"
    - ".planning/phases/198-green-bringup/198-47-SUMMARY.md — report-only closeout summary with intentional `coverage: []`, GREEN-07 pending, and no invented deliverable"
  key_links:
    - "git ls-remote origin main -> exact remote SHA -> bin/observe-main-ci -> canonical main-run state -> GREEN-07 disposition"
    - "198-01..46 SUMMARY coverage plus 198-47-SUMMARY.md `coverage: []` -> canonical classifier over the final set -> 198-UAT.md 192/192 automated -> verification-mechanism status, not requirement completion"
    - "Plan 47 entry HEAD/counts -> Task 1 evidence commit -> Task 2 report/UAT commit -> required summary commit -> final origin/main..HEAD is non-empty without relabeling an entry count as final"
    - "ruleset 21702804 sole byte-exact context CI required -> GREEN-08 Complete -> PR #26 BLOCKED remains a downstream roadmap outcome"
  flagged_assumptions:
    - "GREEN-01 unclassified remains unresolved. It is not reopened because the preserved-log artifact and its automated coverage row are unchanged and passing."
    - "GREEN-02 concurrency remains unresolved. It is not reopened because this plan neither regenerates nor concurrently writes the Credo measurement artifacts."
    - "GREEN-03 empty and encoding remain unresolved. They are surfaced here but outside the current gap because the scorecard-free probe and automated evidence contract remain unchanged."
    - "GREEN-04 boundary, adjacency, empty, encoding, ordering, and precision remain unresolved. They are surfaced here but outside the current gap because the fresh CI attestation and zero-skip contracts remain unchanged."
    - "GREEN-05 unclassified remains unresolved. It is not reopened because the derived form-policy roster and its tests are unchanged."
    - "GREEN-06 boundary, adjacency, empty, ordering, and precision remain unresolved. They are surfaced here but outside the current gap because timeout and fail-fast contracts are unchanged."
    - "GREEN-07 boundary, adjacency, empty, encoding, ordering, precision, and concurrency remain unresolved and are in scope: exact 20-minute comparison, success-only conclusions, non-empty job/check sets, byte-exact SHA/check names, newest-run ordering, elapsed-second precision, and no stale-run substitution must all be reported explicitly."
    - "GREEN-08 empty, encoding, and concurrency remain unresolved and are in scope: zero contexts fails, CI required is byte-exact, and ruleset/PR observations are read-only snapshots rather than a temporary exception."
    - "GREEN-09 unclassified and GREEN-10 unclassified remain unresolved. They are not reopened because no workflow or release-control file changes in this plan."
    - "GREEN-11 empty and encoding remain unresolved. They are not reopened because the issue-upsert classifier and event-path automation remain unchanged."
    - "GREEN-12 adjacency, empty, and ordering remain unresolved. They are not reopened because this plan performs no branch, tag, or worktree mutation."
  prohibitions:
    - statement: "Do not convert a passing automated UAT evaluator into a claim that GREEN-07 or the phase goal succeeded."
      status: unverified
      flagged: true
    - statement: "Do not rewrite or silently reverse the maintainer's recorded option-a disposition in 198-39-DECISION.md."
      status: unverified
      flagged: true
    - statement: "Do not weaken required checks, alter ruleset enforcement, mutate a remote ref, or substitute branch CI for exact-main CI to improve the reported verdict."
      status: unverified
      flagged: true
---

<objective>
Reconcile Phase 198's stale Round 6 verification report with the current Round 8 repository and live read-only GitHub evidence, without inventing implementation work or changing the recorded GREEN-07 disposition.

Purpose: At Plan 47 entry, `198-UAT.md` is complete with 192/192 automated coverage rows from summaries 198-01..46, but it explicitly does not own requirement completion. The product gap remains exact ancestry plus exact-main CI: live `origin/main` is behind local `main`, its canonical run is red, PR #34's green run is on a branch, and PR #26 remains blocked. The verification report must describe that current state rather than the superseded Round 6 red-lane cause, and closeout must incorporate the required Plan 47 summary without making the entry snapshot stale.

Output: an authoritative current-state section in `198-VERIFICATION.md` that distinguishes immutable entry evidence from the final lifecycle predicate; an exhaustive `198-UAT.md` source list through 198-47; and a report-only `198-47-SUMMARY.md` whose intentional zero-deliverable coverage shape preserves 192/192 automated rows, 11/12 requirement completion, GREEN-07 Pending, GREEN-08 Complete with roadmap SC4 partial, and no remote mutation.

**Discovery:** Level 0 for implementation and Level 1 for evidence. No new dependency, product API, UI, schema, or workflow integration is introduced. The deterministic API detector fires only on existing GitHub REST measurement prose; the existing reasoned `COVERAGE.md` declaration remains valid and no coverage matrix is created. Schema-push detection found no schema path.

**Assumption delta:** `decision: no-change`; primary noun: `exact origin/main state`. The detector's `fallback` and `optional` signals come from older phase text, not this gap. This plan preserves the singular required context and derived evidence model and introduces no identity generalization.

**Spec-less edge audit — no silent drops:** all 33 unresolved probe rows are surfaced in `must_haves.flagged_assumptions`. The ten GREEN-07/GREEN-08 rows are exercised by this reconciliation. The other 23 remain explicitly unresolved but are not reopened because this plan changes none of their satisfying artifacts or automated evidence. Equality: 10 in-scope + 23 unchanged-surface = 33 surfaced.

**Descriptor-less prohibition recall:** the adversarial recall produced three bespoke transparency/safety constraints after routine engineering and canon security items were dropped: do not promote evaluator success into requirement success; do not reverse the recorded maintainer disposition; and do not weaken or substitute evidence to manufacture a green verdict. All three are descriptor-less, flagged, and unverified as required by the spec-less fallback.

**Multi-source coverage audit:**

| Source | IDs | Coverage | Disposition |
|---|---|---|---|
| GOAL | Phase goal | 198-01..46 plus 198-47 | Measurement, baseline retirement, protection, and sizing are preserved from executed plans; 198-47 reports the sole remaining exact-main gap without claiming closure. |
| REQ | GREEN-01..06, GREEN-09..12 | 198-01..46 | Complete and not reopened; current plan changes no satisfying artifact. |
| REQ | GREEN-07 | 198-47 | Current live ancestry and exact-main CI evidence are re-derived; Pending is preserved under the recorded maintainer disposition. |
| REQ | GREEN-08 | 198-47 | Exact required-context/ruleset evidence is re-observed while the downstream PR #26 outcome remains partial in the roadmap. |
| RESEARCH | exact GitHub evidence, newest-run selection, no gate weakening, append-only history | 198-47 | Task 1 uses the committed observer and read-only API queries; Task 2 archives rather than erases Round 6. |
| CONTEXT | D-01, D-08..D-14, D-39, D-42 | 198-47 | Exact-main evidence, the sole aggregate context, active ruleset, accepted-Pending authority, and same-diff interlock are preserved. |
| CONTEXT | D-02..D-07, D-15..D-38, D-40..D-41 | 198-01..46 | Already implemented and verified; no current gap touches these decisions. |

Deferred ideas in `198-CONTEXT.md` and the no-target option-a disposition are excluded by explicit source authority, not silently omitted. No source item is missing from the combined existing-plus-new plan set.

### Artifacts this phase produces

- New symbols: None.
- New path: `.planning/phases/198-green-bringup/198-47-SUMMARY.md`, created by the normal executor closeout with an intentional `coverage: []` contract.
- New flags: None.
- New fields: None; existing verification frontmatter fields are refreshed for the authoritative current round.
- Existing path amended: `.planning/phases/198-green-bringup/198-VERIFICATION.md` receives the current reconciliation while prior rounds remain preserved.
- Existing path amended: `.planning/phases/198-green-bringup/198-UAT.md` adds `198-47-SUMMARY.md` to the exhaustive source list without adding a coverage row or changing the derived 192/192 totals.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-UAT.md
@.planning/phases/198-green-bringup/198-39-DECISION.md
@.planning/phases/198-green-bringup/198-41-SUMMARY.md
@.planning/phases/198-green-bringup/198-42-SUMMARY.md
@.planning/phases/198-green-bringup/198-46-SUMMARY.md
@bin/observe-main-ci
@test/threadline/phase198_zero_human_uat_contract_test.exs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace current local ancestry through exact-main CI, ruleset, PR, and UAT evidence</name>
  <precondition>`gh auth status`, `git ls-remote --heads origin main`, and `bin/observe-main-ci --sha &lt;live-main-sha&gt; --format json` all succeed read-only.</precondition>
  <files>.planning/phases/198-green-bringup/198-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-VERIFICATION.md — stale Round 6 report to preserve below the new authoritative section
    - .planning/phases/198-green-bringup/198-UAT.md — current complete 192/192 automated ledger and its non-promotion disclaimer
    - .planning/phases/198-green-bringup/198-39-DECISION.md — maintainer-selected option-a authority; do not reverse it
    - .planning/phases/198-green-bringup/198-42-SUMMARY.md — last exact-main/ruleset observation before Round 8
    - .planning/phases/198-green-bringup/198-46-SUMMARY.md — zero-human UAT cutover and evaluator-versus-requirement distinction
    - .planning/REQUIREMENTS.md — current 11 complete and GREEN-07 Pending statuses
    - .planning/ROADMAP.md — literal phase goal and success criteria
    - bin/observe-main-ci — canonical exact-SHA main-run selector and state vocabulary
  </read_first>
  <action>Collect a timestamped, read-only Plan 47 entry snapshot without fetching or changing any ref: `git rev-parse HEAD`, `git rev-parse main`, the live SHA from `git ls-remote --heads origin main`, both directional ancestry counts for live-main versus local `main` and live-main versus entry `HEAD`, `bin/observe-main-ci --sha &lt;live-sha&gt; --format json`, PR #34's state/draft/base/head/headRefOid/mergeStateStatus and latest cited run, PR #26's state/mergeStateStatus, and ruleset 21702804's enforcement, bypass actors, ref condition, and required-status contexts. Run the canonical Phase 198 zero-human contract and independently aggregate classifier totals over the 46 summaries that exist at entry; require 192 total, 192 auto-passed, zero present, and zero errors before writing. Label these SHA/count/classifier values `immutable Plan 47 entry snapshot`, not current-at-closeout or final.

Replace only the current authoritative frontmatter and top report section of `198-VERIFICATION.md`; move the former Round 6 report intact under an explicit archived heading. Record the live remote SHA separately from the local tracking ref and do not update either. The new evidence table must state entry local-main and entry-HEAD ahead counts, observer state/id/status/workflow conclusion/job count/byte-exact aggregate count and conclusion, PR #34's branch-only green evidence, ruleset active/empty-bypass/sole-context state, PR #26's downstream BLOCKED state, and the entry UAT classifier totals. Add a lifecycle table with separate rows for the already-committed plan/entry HEAD, the mandatory Task 1 evidence commit, the mandatory Task 2 report/UAT commit, and the required `198-47-SUMMARY.md` closeout commit. State that the latter three do not exist at entry and therefore cannot be included in its numeric ahead count; because this plan performs no remote mutation, the final ancestry predicate remains `origin/main..HEAD non-empty` even though its exact final numeric count is deliberately not guessed before closeout. If live evidence differs from the expected classes, record the observed values and derive status from them; never substitute the older Round 6 or PR #34 branch result for the exact-main observer.</action>
  <reversibility rating="reversible">The task changes one tracked report and performs only read-only Git/GitHub observations.</reversibility>
  <verify>
    <automated>bash -c 'set -eu; f=.planning/phases/198-green-bringup/198-VERIFICATION.md; remote_sha=$(git ls-remote --heads origin main | awk '\''{print $1}'\''); test -n "$remote_sha"; obs=$(bin/observe-main-ci --sha "$remote_sha" --format json); state=$(printf "%s" "$obs" | jq -er .state); mix test test/threadline/main_ci_observer_contract_test.exs test/threadline/phase198_zero_human_uat_contract_test.exs; grep -Fq "$remote_sha" "$f"; grep -Fq "Main CI observer state: \`$state\`" "$f"; grep -Fq "immutable Plan 47 entry snapshot" "$f"; grep -Fq "198-47-SUMMARY.md" "$f"; grep -Fq "final ancestry predicate: \`origin/main..HEAD non-empty\`" "$f"; grep -Fq "192/192" "$f"; grep -q "^status: gaps_found$" "$f"'</automated>
    <fails_when>Remote-main discovery or the canonical observer fails, either focused contract fails, the report omits the live SHA/current observer state/entry 192-of-192 evidence, the entry snapshot is presented as final, any mandatory Plan 47 commit category is absent, the final ancestry predicate is not explicitly non-empty, or the authoritative status is promoted above `gaps_found` while exact ancestry or exact-main CI remains unmet.</fails_when>
  </verify>
  <acceptance_criteria>
    - The authoritative section is timestamped after Round 8 and records live remote main, local main, entry HEAD, both entry ahead counts, the exact-main observer record, PR #34, PR #26, ruleset 21702804, and the entry classifier totals.
    - The lifecycle table includes the plan/entry commit, Task 1 commit, Task 2 commit, and required summary commit; it reports only the entry count numerically and derives the final non-empty predicate without inventing a final count.
    - PR #34's successful pull-request run is labeled branch-only and is never used as the exact-main run.
    - UAT automation is reported complete with 192/192 passing rows and zero pending, while GREEN-07 remains derived solely from ancestry and exact-main CI.
    - No fetch, push, merge, PR mutation, ruleset mutation, workflow edit, requirement edit, roadmap edit, or state edit occurs; the later UAT edit is limited to its exhaustive source metadata.
  </acceptance_criteria>
  <done>The current repository, GitHub, and UAT state is connected end-to-end in the authoritative verification report without changing external state.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile dispositions and stage the final summary/UAT closeout contract</name>
  <files>.planning/phases/198-green-bringup/198-VERIFICATION.md, .planning/phases/198-green-bringup/198-UAT.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-VERIFICATION.md — Task 1 snapshot and archived prior report
    - .planning/REQUIREMENTS.md — authoritative checkboxes and the current GREEN-07/GREEN-08 notes
    - .planning/ROADMAP.md — six success criteria and the existing terminal-disposition annotation
    - .planning/phases/198-green-bringup/198-39-DECISION.md — option-a decision that remains recorded history
    - .planning/phases/198-green-bringup/198-UAT.md — generated automation ledger whose exhaustive source metadata currently ends at 198-46
    - /Users/jon/.codex/gsd-core/bin/lib/coverage.cjs — canonical proof that explicit `coverage: []` is coverage mode with zero entries, unlike omitted legacy coverage
  </read_first>
  <action>Complete the authoritative current section with a six-row roadmap truth table and a twelve-row requirement table. Count exactly 11 requirements Complete from REQUIREMENTS.md; keep GREEN-07 Pending. Keep GREEN-08 Complete for the exact-check contract while labeling roadmap success criterion 4 partial because PR #26 remains BLOCKED downstream of GREEN-07. Describe the Round 6 cause as superseded in substance by Phase 199/PR #34's branch run, not erased, and name the current first failing predicate as live origin/main ancestry before evaluating main CI. Preserve the option-a decision as the terminal v1.41 disposition; do not silently convert it into authorization for a merge, push, temporary bypass, or ruleset edit.

Add explicit sections for: zero remaining UAT automation gaps at entry; the still-unmet product gap; 33/33 edge-probe assumption accounting using the same 10-in-scope plus 23-unchanged grouping from this plan; all three descriptor-less prohibitions; the multi-source coverage audit; API detector/COVERAGE declaration status; assumption-delta no-change; schema-push not applicable; the four-category Plan 47 lifecycle; and a no-mutation integrity statement. Describe the numeric ancestry/classifier observations as entry values. Report the closeout ancestry only as the mechanically derived predicate `origin/main..HEAD non-empty`, because the Task 1, Task 2, and summary commits are guaranteed later local commits and no remote mutation occurs. End with `Overall verdict: gaps_found` and state that the next state-changing step, if ever desired, requires a separate maintainer decision because planning and reconciliation do not authorize it.

Update only `198-UAT.md` frontmatter/source metadata: append `198-47-SUMMARY.md` after `198-46-SUMMARY.md` in the exhaustive source list. Keep the 192 total/passed and zero pending/issues/blocked/skipped counts, ledger rows, requirement meanings, and non-promotion disclaimer unchanged because the required Plan 47 summary is a report-only closeout with intentional `coverage: []` and contributes zero classifier entries. Mark this as a staged closeout contract that becomes final only after the normal executor summary step creates `198-47-SUMMARY.md` and the post-summary command below reruns the canonical classifier over all 47 summaries.</action>
  <reversibility rating="reversible">This is a report-only reconciliation that retains all earlier evidence and decisions.</reversibility>
  <verify>
    <automated>bash -c 'set -eu; f=.planning/phases/198-green-bringup/198-VERIFICATION.md; u=.planning/phases/198-green-bringup/198-UAT.md; test "$(grep -c "^| GREEN-[0-9][0-9] |" "$f")" -ge 12; grep -Fq "11/12 requirements Complete" "$f"; grep -Fq "GREEN-07" "$f"; grep -Fq "Pending" "$f"; grep -Fq "GREEN-08" "$f"; grep -Fq "immutable Plan 47 entry snapshot" "$f"; grep -Fq "final ancestry predicate: \`origin/main..HEAD non-empty\`" "$f"; grep -Fq "10 in-scope + 23 unchanged-surface = 33" "$f"; grep -Fq "Overall verdict: \`gaps_found\`" "$f"; grep -q "^source: .*198-46-SUMMARY.md, 198-47-SUMMARY.md$" "$u"; grep -q "^total: 192$" "$u"; grep -q "^passed: 192$" "$u"; grep -q "^pending: 0$" "$u"; git diff --check -- "$f" "$u"'</automated>
    <fails_when>The requirement table is incomplete, the score differs from the authoritative checkboxes, GREEN-07 is promoted, GREEN-08's requirement/outcome distinction is erased, an entry observation is presented as the final closeout count, any lifecycle category or edge row is silently dropped, the UAT source list does not anticipate the required Plan 47 summary while retaining its derived 192/192 zero-human totals, the final verdict is not `gaps_found`, or markdown whitespace is invalid.</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly 11/12 requirements are reported Complete, with GREEN-07 Pending and no unrelated status changed.
    - All six roadmap criteria and all twelve requirement IDs have current evidence or an explicit carried-forward basis.
    - The report accounts for 33/33 unresolved edge items and all three descriptor-less prohibitions without inventing checks or dismissals.
    - The source audit covers GOAL, all REQ IDs, active RESEARCH constraints, and all CONTEXT decisions through grouped existing-plan coverage.
    - The former Round 6 report and recorded maintainer decision remain readable and are not rewritten.
    - The UAT source list is exhaustive through 198-47, and the planned report-only summary's zero-entry coverage shape leaves the generated ledger at 192/192 with no human row.
    - Task verification identifies the UAT metadata as staged; only the post-summary classifier rerun may call the 47-summary UAT state final.
  </acceptance_criteria>
  <done>Phase 198 has a coherent staged closeout: immutable entry evidence, lifecycle-derived final ancestry, an exhaustive future-complete UAT source list, GREEN-07 still honestly Pending, and no spurious implementation gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local git graph -> verification report | Mutable local refs and later plan commits can invalidate an earlier ancestry statement. |
| GitHub API -> verification report | Remote main, runs, PRs, and rulesets are mutable external evidence and must be timestamped and pinned. |
| automated UAT -> product requirements | Evaluator correctness crosses into requirement status only if the requirement's own predicate is satisfied. |
| executor summary closeout -> generated UAT | The final summary appears after task verification, so source completeness and classifier totals require a post-summary check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-47-01 | Spoofing | branch CI represented as main CI | high | mitigate | Select the canonical run from the exact live origin/main SHA with `bin/observe-main-ci`; label PR #34 evidence branch-only. |
| T-198-47-02 | Tampering | stale local tracking ref or rewritten verification history | high | mitigate | Read remote main with `git ls-remote`, record it separately, and archive Round 6 intact below the current section. |
| T-198-47-03 | Elevation of privilege | push, merge, or ruleset bypass implied by planning | high | mitigate | Perform read-only queries only and state that any future mutation needs a separate explicit maintainer decision. |
| T-198-47-04 | Repudiation | evaluator pass promoted into requirement success | high | mitigate | Report classifier totals separately from the 12-row requirement table and preserve GREEN-07 Pending. |
| T-198-47-05 | Information disclosure | captured GitHub responses | low | mitigate | Record public repository metadata only; exclude auth headers, environment values, tokens, and log bodies. |
| T-198-47-06 | Denial of service | remote observation unavailable | low | accept | Fail the task precondition rather than writing a partial or fabricated current-state report. |
| T-198-47-07 | Tampering | summary/UAT self-coverage | high | mitigate | Require the normal closeout summary to use explicit `coverage: []`, include it in UAT source metadata, and rerun the canonical classifier after the file exists. |
</threat_model>

<verification>
- Run both focused observer/UAT contract tests and re-run the exact live-main observer for the immutable entry snapshot.
- Validate that the authoritative verification frontmatter remains `status: gaps_found`, the body reports 11/12, and GREEN-07 is Pending.
- Confirm the lifecycle table accounts for the plan/entry, Task 1, Task 2, and summary commit categories and derives a non-empty final ancestry predicate without claiming the entry count is final.
- After the normal summary step, rerun the canonical classifier over all 47 summary files and reconcile its 192/192 zero-human totals with the UAT source list.
- Confirm `git diff --name-only` for plan execution is limited to `198-VERIFICATION.md`, `198-UAT.md`, and the normal `198-47-SUMMARY.md`; all remote operations are read-only.
</verification>

<success_criteria>
- A fresh reader can distinguish complete automated UAT from incomplete product requirements without consulting older commentary.
- The current first failing GREEN-07 predicate is exact ancestry, followed by the exact-main observer result; branch CI is never substituted.
- The old Round 6 cause remains historical evidence while the authoritative section reflects Round 8/current reality.
- The final summary is coverage mode with zero deliverables, lists GREEN-07 only as pending, and is included in the UAT source list; the final canonical aggregate remains 192 total, 192 auto-passed, zero present, and zero errors.
- No remote state, protection rule, workflow, requirement, roadmap, state, prior plan, or UAT ledger row is changed; UAT changes only by adding the required summary to source metadata.
</success_criteria>

<output>
Create `.planning/phases/198-green-bringup/198-47-SUMMARY.md` in the normal executor closeout step. This report-only plan creates no independently classifiable product deliverable, so the summary frontmatter MUST contain explicit `coverage: []` (coverage mode with zero entries), `requirements-completed: [GREEN-08]`, and `requirements-pending: [GREEN-07]`; GREEN-07 MUST NOT appear under `requirements-completed`. GREEN-07 remains in `gap_ids`, must-haves, tasks, and the pending summary disposition, but is intentionally excluded from the plan's `requirements` array so the canonical executor cannot auto-mark it complete. The summary body must retain the entry-versus-final lifecycle distinction and state that the final ancestry predicate remains non-empty with no remote mutation.

After the summary exists, run this final closeout check before declaring execution complete:

<post_summary_verify>
  <automated>bash -c 'set -eu; d=.planning/phases/198-green-bringup; s="$d/198-47-SUMMARY.md"; u="$d/198-UAT.md"; r=.planning/REQUIREMENTS.md; tool=/Users/jon/.codex/gsd-core/bin/gsd-tools.cjs; test -f "$s"; test -f "$tool"; grep -q "^coverage: \[\]$" "$s"; grep -q "^requirements-pending: \[GREEN-07\]$" "$s"; grep -q "^requirements-completed: \[GREEN-08\]$" "$s"; ! sed -n "/^requirements-completed:/p" "$s" | grep -q "GREEN-07"; test "$(grep -Ec "^- \[ \] \*\*GREEN-07\*\*([[:space:]]|$)" "$r")" -eq 1; test "$(grep -Ec "^- \[[xX]\] \*\*GREEN-07\*\*([[:space:]]|$)" "$r")" -eq 0; total=0; passed=0; present=0; errors=0; summaries=0; for f in "$d"/198-*-SUMMARY.md; do summaries=$((summaries + 1)); j=$(node "$tool" uat classify-coverage --summary "$f" --raw); test "$(printf "%s" "$j" | jq -r .mode)" = coverage; total=$((total + $(printf "%s" "$j" | jq ".total"))); passed=$((passed + $(printf "%s" "$j" | jq ".auto_passed | length"))); present=$((present + $(printf "%s" "$j" | jq ".present | length"))); errors=$((errors + $(printf "%s" "$j" | jq ".errors | length"))); done; test "$summaries" -eq 47; test "$total" -eq 192; test "$passed" -eq 192; test "$present" -eq 0; test "$errors" -eq 0; grep -q "^source: .*198-46-SUMMARY.md, 198-47-SUMMARY.md$" "$u"; test "$(sed -n "s/^total: //p" "$u")" -eq "$total"; test "$(sed -n "s/^passed: //p" "$u")" -eq "$passed"; test "$(sed -n "s/^pending: //p" "$u")" -eq "$present"'</automated>
  <fails_when>The required summary is absent or legacy/malformed, it adds a deliverable row, GREEN-07 is listed completed rather than pending, REQUIREMENTS.md does not contain exactly one unchecked GREEN-07 checkbox or contains any checked GREEN-07 checkbox, any of the 47 summaries is outside canonical coverage mode, the final aggregate differs from 192 total/192 auto-passed/zero present/zero errors, or the UAT source list/counts do not match that final summary set.</fails_when>
</post_summary_verify>
</output>
