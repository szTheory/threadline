---
phase: 198-green-bringup
plan: 25
type: execute
wave: 3
gap_closure: true
depends_on:
  - 198-24
files_modified:
  - examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs
  - examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/exports.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_runs.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex
  - .planning/audits/198-round4-demo-seed.md
autonomous: true
requirements:
  - GREEN-04

estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every failing test in `walkthrough_happy_path_test.exs` and `walkthrough_evidence_test.exs` is fixed at its cause, following plan 198-23's recorded fix protocol."
    - "The single `ExUnit.TimeoutError` observed in the round-3 CI job is diagnosed to a named cause — a genuinely slow seeded query, a deadlock, or a wait on data that is never produced — and fixed at that cause, never by raising a timeout to hide it."
    - "A red-then-green teeth proof is captured for at least one failure per test file."
    - "`mix verify.example` is measured after this plan and its failure count is recorded with an integer delta against plan 198-24's closing count; the target state is `0 failures`."
    - "If `mix verify.example` reaches 0 failures, that is recorded as a local readiness signal only — GREEN-04's verdict still belongs to plan 198-29's measured CI run (D-01)."
    - "Any residual failure is enumerated by module, test name and cause, with a dated `deferred-items.md` entry — never a bare 'still red'."
    - statement: "Empty edge (GREEN-04): the `WALK-04-01 retention_run evidence and empty offboarded-co timeline` test pairs its empty-timeline assertion with a positive retention_run evidence assertion, so a database that seeded nothing cannot satisfy it."
      verification: explicit
    - statement: "Ordering edge (GREEN-04): `admin export status shows seeded job states` and `retention history shows seeded completed purge run` assert over the set of seeded states present, not over a positionally-indexed ordered list whose sort key can tie."
      verification: explicit
    - statement: "Adjacency edge (GREEN-04): where a walkthrough assertion depends on a transaction falling inside the default audit window, the boundary side is stated explicitly rather than left to a strict-versus-inclusive comparison."
      verification: explicit
  artifacts:
    - "examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs — its §2/§3/§4 failures fixed at cause"
    - "examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs — its §5 evidence-plane failures fixed at cause"
    - ".planning/audits/198-round4-demo-seed.md — extended with this plan's timeout diagnosis, teeth proofs, and the closing measured count"
  key_links:
    - "`mix demo.seed` content → the walkthrough LiveView/controller assertions → `mix verify.example` exit status → `Run test suite (current)` → GREEN-04"
    - "plans 198-23 and 198-24's seed changes → this plan's walkthrough expectations → re-inventory before assuming the round-3 failure list still holds"
    - "the `ExUnit.TimeoutError` → a specific awaited condition that never becomes true → the seed producer that should have produced it"
  prohibitions:
    - statement: "A failing example-app test must not be made to pass by deleting, weakening, `@tag :skip`-ing, `git rm`-ing, or conditionally guarding its assertion."
      status: kept
      verification: flagged-unverified
    - statement: "The `ExUnit.TimeoutError` must not be resolved by raising a test timeout, adding a sleep, or adding a retry. It is fixed at the cause of the wait."
      status: kept
      verification: flagged-unverified
    - statement: "GREEN-04 must not be marked Complete on this plan's local output; the verdict belongs to plan 198-29's measured CI run (D-01)."
      status: kept
      verification: flagged-unverified
---

<objective>
Close the last demo-seed cluster: the walkthrough tests. In scope are the failures in
`examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs` (§1-§4,
`WALK-01-06` through `WALK-03-04` plus the two non-WALK-numbered tests `admin export status shows
seeded job states` and `retention history shows seeded completed purge run`) and
`examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs` (§5,
`WALK-04-01` through `WALK-04-03`), including the single `ExUnit.TimeoutError` recorded among the
round-3 CI job's 9 failures.

Purpose: `mix verify.example` cannot exit 0 while these fail, and `Run test suite (current)` cannot
go green while `mix verify.example` exits non-zero. This is the final local piece of GREEN-04.

Depends on plan 198-24 because both plans may change demo-seed producer modules, and because this
plan's inventory must be taken *after* the seed changes 198-23 and 198-24 land — the round-3 failure
list is a stale starting point, not a work order.

Output: both walkthrough files passing, the timeout diagnosed and fixed at cause, and a measured
`mix verify.example` count recorded with its delta.

**Ceiling:** this plan cannot close GREEN-04. Local output is inadmissible per D-01; the verdict is
plan 198-29's measured CI run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/deferred-items.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-inventory after the seed changes, then fix the happy-path walkthrough failures at cause</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/exports.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex, .planning/audits/198-round4-demo-seed.md</files>
  <read_first>
    - .planning/audits/198-round4-demo-seed.md (plan 198-23's `## Fix protocol for the remaining clusters`, plus the `## Measured after-count (198-24)` figure this plan's delta is measured against, plus every seed module 198-23 and 198-24 recorded changing — those changes may have already resolved or already moved some of these failures)
    - examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs (all 249 lines — `describe "§1 clean clone / landing (WALK-01-04)"` through `describe "§4 operator incidents (WALK-03-01..04)"`, specifically `test "WALK-01-06 support user reaches org-scoped audit timeline"`, `test "WALK-01-07 support ticket reply via dev route returns audit_transaction_id"`, `test "WALK-02-01 closer ticket reply captured; admin views transaction semantics"`, `test "WALK-02-02 admin reaches cross-org audit timeline"`, `test "WALK-02-03 support cannot export from operator surface"`, `test "admin export status shows seeded job states"`, `test "WALK-03-01 correlation filter surfaces hero #4521 close transaction"`, `test "WALK-03-02 admin actor history for agent2 persona"`, `test "WALK-03-03 retention evidence row present for offboarded-co purge proof"`, `test "retention history shows seeded completed purge run"`, `test "WALK-03-04 deleter hard-delete on #4518 visible to admin"`)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/exports.ex (the seeded export job states `admin export status shows seeded job states` reads)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex and seed/filler.ex (the support-user and cross-org timeline content §2 and §3 assert against)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex (hero ticket 4521 / 4518 and the correlation and retention evidence contracts)
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md (`### verify.example failure — known, previously-acknowledged deferral` — the round-3 record that a second run surfaced two `WalkthroughHappyPathTest` content mismatches absent from the first; expect non-determinism)
  </read_first>
  <behavior>
    - `admin export status shows seeded job states`: the export-status surface renders every distinct job state the seed produces. Asserted as set containment over the distinct states present, not by positional index into a rendered list.
    - `WALK-03-01 correlation filter surfaces hero #4521 close transaction`: filtering by the hero correlation returns a non-empty result set containing the close transaction for ticket 4521, with the ticket number read from `Demo.Manifest` rather than as an inline literal.
    - `WALK-03-03 retention evidence row present for offboarded-co purge proof` and `retention history shows seeded completed purge run`: a completed purge run row exists and renders. The non-emptiness is asserted before any property over the set.
    - `WALK-03-04 deleter hard-delete on #4518 visible to admin`: the hard-delete change on ticket 4518 is visible to the admin persona and attributed to the deleter, asserted by actor identity and change operation.
  </behavior>
  <action>
    Re-inventory first. Run `mix verify.example` twice from the repository root and record, under
    `## Re-inventory after 198-23/198-24 (198-25)` in `.planning/audits/198-round4-demo-seed.md`, the
    union of failures across both runs with module, test name, line, failure class and verbatim
    message. Do not start from the round-3 CI list — the seed has changed underneath it since.

    Then fix the `walkthrough_happy_path_test.exs` failures from that fresh inventory, using plan
    198-23's attribution vocabulary (`seed-content-wrong` versus `assertion-rotted`) and its fix
    protocol. Read hero identifiers and declared evidence shapes from
    `ThreadlinePhoenix.Demo.Manifest` rather than restating them inline. Pair every emptiness or
    absence assertion with a positive existence assertion on the same surface.

    Capture a red-then-green transcript for at least one fixed failure and append it under
    `## Red-then-green teeth proof (198-25, happy path)`.

    Forbidden: `@tag :skip`, `git rm`, deleted assertion, `assert true`, added sleep or retry,
    narrowing `mix verify.example`, editing `.github/workflows/ci.yml` or `mix.exs`.
  </action>
  <acceptance_criteria>
    - `.planning/audits/198-round4-demo-seed.md` contains `## Re-inventory after 198-23/198-24 (198-25)` with a two-run union table whose every row carries a verbatim message.
    - `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/walkthrough_happy_path_test.exs` reports `0 failures` on two consecutive runs.
    - `.planning/audits/198-round4-demo-seed.md` contains `## Red-then-green teeth proof (198-25, happy path)` with a captured pre-fix failure and matching post-fix pass.
    - `grep -c '@tag :skip' examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs` returns `0`.
    - `grep -c ':timer.sleep' examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs` returns `0` — no failure is waited out.
    - `grep -c '^\s*test "' examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs` is greater than or equal to its pre-task value.
    - `git diff -- .github/workflows/ci.yml mix.exs` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/threadline_phoenix &amp;&amp; MIX_ENV=test mix test test/threadline_phoenix_web/walkthrough_happy_path_test.exs</automated>
  </verify>
  <done>The happy-path walkthrough passes for cause-attributed reasons against a freshly-taken inventory, with a captured teeth proof and no waited-out or skipped assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the evidence-plane failures and the ExUnit.TimeoutError at its cause, then close the local measurement</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_runs.ex, .planning/audits/198-round4-demo-seed.md</files>
  <read_first>
    - .planning/audits/198-round4-demo-seed.md (this plan's Task 1 re-inventory — the row whose failure class is `ExUnit.TimeoutError` is this task's headline work item; plus plan 198-23's fix protocol)
    - examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs (all 129 lines — `describe "§5 evidence plane (WALK-04-01..03)"` with `test "WALK-04-01 retention_run evidence and empty offboarded-co timeline"`, `test "WALK-04-02 redaction_policy evidence and #4521 row history shows [REDACTED]"`, `test "WALK-04-03 trigger coverage dashboard and evidence snapshot"`)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_runs.ex (the retention_run evidence rows `WALK-04-01` reads)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex (the redaction `subject_ref` and the coverage/evidence-snapshot contract)
    - .planning/phases/198-green-bringup/198-VERIFICATION.md (the round-3 record that exactly one of the 9 failures is an `ExUnit.TimeoutError` — read it so the timeout is not confused with an ordinary assertion mismatch)
  </read_first>
  <behavior>
    - `WALK-04-01 retention_run evidence and empty offboarded-co timeline`: the offboarded-co timeline renders empty AND a retention_run evidence row exists explaining why. Both halves are asserted; the empty half alone is not sufficient.
    - `WALK-04-02 redaction_policy evidence and #4521 row history shows [REDACTED]`: the row history for ticket 4521 renders the redaction sentinel for the redacted field, with the sentinel and the `subject_ref` read from `Demo.Manifest`.
    - `WALK-04-03 trigger coverage dashboard and evidence snapshot`: the coverage dashboard renders a non-empty covered set and a non-empty uncovered set, so an all-covered or all-empty database fails rather than passing vacuously.
    - The timing-out test: whatever condition it awaits becomes true within the default ExUnit timeout because the data it waits on is actually produced, not because the timeout was raised.
  </behavior>
  <action>
    Diagnose the `ExUnit.TimeoutError` first and record the diagnosis under
    `## ExUnit.TimeoutError diagnosis (198-25)` in `.planning/audits/198-round4-demo-seed.md`: name
    the test, the exact operation that did not complete, and the cause class — a genuinely slow
    seeded query, a lock/deadlock against the seeded transaction, or a wait on data the seed never
    produces. Fix at that cause. Raising `@moduletag timeout:`, inserting `:timer.sleep/1`, or adding
    a retry are all forbidden: they convert a real defect into a slower green.

    Then fix the remaining `walkthrough_evidence_test.exs` failures from Task 1's re-inventory using
    the same attribution vocabulary and protocol. Pair every emptiness assertion with a positive
    existence assertion — `WALK-04-01`'s "empty offboarded-co timeline" is the exact shape that
    passes vacuously against a database that seeded nothing.

    Capture a red-then-green transcript under `## Red-then-green teeth proof (198-25, evidence)`.

    Finally, close the local measurement: run `mix verify.example` twice from the repository root and
    append `## Measured after-count (198-25, closing)` with both verbatim `N tests, M failures` lines,
    the integer delta against plan 198-24's closing count, and the integer delta against plan 198-23's
    original baseline. State plainly whether the count reached 0.

    If it did not, enumerate every residual failure by module, test name and cause, and add a dated
    entry to `.planning/phases/198-green-bringup/deferred-items.md`. Add, in the same section, the
    single line that local output is inadmissible for GREEN-04 per D-01 and that the verdict belongs
    to plan 198-29.
  </action>
  <acceptance_criteria>
    - `.planning/audits/198-round4-demo-seed.md` contains `## ExUnit.TimeoutError diagnosis (198-25)` naming the test, the operation that did not complete, and exactly one cause class.
    - `grep -c 'timeout:' examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs` shows no timeout override was added relative to the pre-task value.
    - `grep -c ':timer.sleep' examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs` returns `0`.
    - `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/walkthrough_evidence_test.exs` reports `0 failures` on two consecutive runs.
    - `WALK-04-01`'s test body contains both an empty-timeline assertion and a positive retention_run existence assertion; `WALK-04-03`'s body asserts both a non-empty covered set and a non-empty uncovered set.
    - `.planning/audits/198-round4-demo-seed.md` contains `## Measured after-count (198-25, closing)` with two verbatim `N tests, M failures` lines and two integer deltas, and an explicit statement of whether the count reached 0.
    - `mix verify.example` exits 0 — or every residual failure is named with a cause in the audit file and carries a dated `deferred-items.md` entry.
    - `git diff -- .github/workflows/ci.yml mix.exs playwright.config.ts` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/threadline_phoenix &amp;&amp; MIX_ENV=test mix test test/threadline_phoenix_web/walkthrough_evidence_test.exs</automated>
  </verify>
  <done>The evidence plane passes, the timeout is fixed at its cause rather than waited out, and the local `mix verify.example` measurement is closed with deltas against both prior plans and an explicit inadmissibility note.</done>
</task>

</tasks>

<reversibility>
  <decision rating="reversible">Rewriting a walkthrough assertion into manifest-read shape form is reversible — an ordinary source change with recorded reasoning.</decision>
  <decision rating="costly">Fixing the `ExUnit.TimeoutError` by changing a seed producer or a query path is costly: it can move the Playwright lane's behaviour in plans 198-26/27/28, which run against the same seeded database. Mitigated by recording the change in the audit file and by 198-26 taking its own fresh browser-lane inventory.</decision>
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mix demo.seed` output → walkthrough LiveView assertions | Seeded content is the untrusted input judged by the walkthrough surfaces |
| A timing-out test → the green/red verdict | A timeout is a defect signal; converting it to a longer wait launders the signal |
| Local run output → requirement status | Local output must never cross into a Complete verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-25-01 | Tampering | the `ExUnit.TimeoutError` | high | mitigate | Timeout-override and `:timer.sleep` counts are acceptance criteria; a written diagnosis naming the operation and cause class is required before any fix |
| T-198-25-02 | Repudiation | the claim "fixed on its merits" | high | mitigate | Captured red-then-green transcripts per test file, plus a freshly-taken re-inventory rather than a stale round-3 list |
| T-198-25-03 | Information Disclosure | vacuous emptiness assertions | high | mitigate | `WALK-04-01` and `WALK-04-03` must pair emptiness with positive existence, checked in source |
| T-198-25-04 | Elevation of Privilege | requirement status | high | mitigate | Recorded D-01 inadmissibility statement; verdict deferred to plan 198-29 |
| T-198-25-SC | Tampering | npm/pip/cargo/hex installs | high | mitigate | No dependency added or upgraded; `mix.exs` and lockfiles untouched (asserted), so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix_web/walkthrough_happy_path_test.exs test/threadline_phoenix_web/walkthrough_evidence_test.exs` passes on two consecutive runs.
- `mix verify.example` exits 0, or residuals are enumerated with causes and deferred with a dated entry.
- `.planning/audits/198-round4-demo-seed.md` carries the re-inventory, the timeout diagnosis, both teeth proofs, and the closing measured counts with two deltas.
- `git diff -- .github/workflows/ci.yml mix.exs playwright.config.ts` is empty.
</verification>

<success_criteria>
The demo-seed failure class that has been deferred since Phase 177 is resolved locally: both
walkthrough files pass, the timeout is fixed at its cause, and `mix verify.example`'s measured
failure count is recorded — with the honest note that this is a readiness signal, not GREEN-04's
verdict.
</success_criteria>

## Artifacts this phase produces

**Changed files**
- `examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs` — §1-§4 failures fixed at cause, manifest-read identifiers, set-based ordering assertions.
- `examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs` — §5 failures fixed at cause, emptiness assertions paired with existence assertions.
- Zero or more of `demo/seed/exports.ex`, `demo/seed/retention_runs.ex`, `demo/seed/support.ex`, `demo/seed/filler.ex` — changed only where seeded content is the attributed cause.
- `.planning/audits/198-round4-demo-seed.md` — extended with `## Re-inventory after 198-23/198-24 (198-25)`, `## ExUnit.TimeoutError diagnosis (198-25)`, two teeth-proof sections, and `## Measured after-count (198-25, closing)`.
- `.planning/phases/198-green-bringup/deferred-items.md` — a dated entry only if a residual failure is honestly deferred.

**Explicitly not produced**
- No change to `.github/workflows/ci.yml`, `mix.exs`, `playwright.config.ts`, `.github/rulesets/main.json`, `CONTRIBUTING.md`, or `.planning/REQUIREMENTS.md`.

<output>
Create `.planning/phases/198-green-bringup/198-25-SUMMARY.md` when done.
</output>
