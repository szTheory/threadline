---
phase: 198-green-bringup
plan: 28
type: execute
wave: 4
gap_closure: true
depends_on:
  - 198-24
  - 198-25
  - 198-26
  - 198-27
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts
  - .planning/audits/198-round4-playwright.md
autonomous: false
requirements:
  - GREEN-07

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "For every attribution row assigned to cluster `198-28`, the audit record states whether that test actually executes on CI or is locally-only — `operator-screenshot-regression.spec.ts` carries `test.skip(!!process.env.CI, ...)` at its describe level, so its failures are part of the 28 local failures but are NOT part of what makes the CI lane red."
    - "The CI-contributing subset of this cluster is separated from the local-only subset, with counts, so this round's effect on the CI lane is not over-claimed."
    - "This plan is the first consumer of both attribution tables that executes AFTER every demo-seed-writing plan (198-23, 198-24, 198-25) has merged, so it runs a post-merge re-validation gate: the lane's failure set is re-derived from a fresh unbounded run, and every row that plans 198-26 and 198-27 marked `seed-sensitive? = yes` is re-attributed against the merged seed before any fix is made here or any prediction is written by 198-29."
    - "The re-validation is a blocking gate with a checkable outcome, not a caveat: if any re-derived row's cause differs from its pre-merge attribution, or if plans 198-26/198-27 declared a fixed test that the post-merge run reports failing, the divergence is recorded row by row and re-attributed before Task 3 changes anything."
    - "Every fix in the CI-contributing subset is made at its named cause with a red-then-green teeth proof."
    - "Any proposal to regenerate a committed PNG baseline under `operator-screenshot-regression.spec.ts-snapshots/` is escalated to a blocking human decision checkpoint before it happens — evidence regeneration is exactly the remedy D-39 forbids for the Tier A lane, and the same reasoning is presented before applying it here."
    - "The lane's measured failure count is recorded before and after with the integer delta, taken with the same unbounded local command plan 198-26 used."
    - "`playwright.config.ts`, the `--project` set, `ci.yml`, `.github/rulesets/main.json` and `CONTRIBUTING.md` are byte-unchanged; no new `test.skip`/`test.fixme` is introduced and the pre-existing CI guard is neither widened nor narrowed."
    - "The plan restates the round's ceiling: `CI required` cannot conclude `success` this round because the Tier A capture lane's only remedy is forbidden under D-39."
    - statement: "Adjacency edge (GREEN-07): a pixel-diff assertion has a tolerance threshold; the audit records the configured tolerance and states which side of it a given diff falls on, so a near-threshold diff is dispositioned by a number rather than by impression."
      verification: explicit
    - statement: "Empty edge (GREEN-07): if a screenshot test produces zero captured artifacts — a boot or navigation failure rather than a visual difference — that is recorded as failed collection, never as a passing or improved result."
      verification: explicit
    - statement: "Ordering edge (GREEN-07): the CI-contributing and local-only subsets are counted separately and both counts are stated, so a reduction concentrated entirely in the local-only subset cannot be presented as progress on the CI lane."
      verification: explicit
  artifacts:
    - "examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts — CI-versus-local execution status established; fixed at cause where it fails"
    - "examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts — fixed at cause"
    - ".planning/audits/198-round4-playwright.md — the CI-contributing versus local-only split with counts, the baseline-regeneration decision record, teeth proofs, and the after-count"
  key_links:
    - "`operator-screenshot-regression.spec.ts`'s `test.skip(!!process.env.CI, ...)` → its failures do not make the CI lane red → fixing them does not move `Example app browser E2E` toward success, and claiming otherwise would over-report this round"
    - "`operator-screenshots.spec.ts`'s `OPERATOR_SCREENSHOT_DIR` guard in `beforeEach` → whether its capture path runs at all under the CI lane's env → establish before attributing"
    - "committed PNG baselines under `operator-screenshot-regression.spec.ts-snapshots/` → regenerating them is evidence regeneration → the same category D-39 forbids for Tier A → a human decides, not the executor"
    - "plans 198-26 and 198-27 inventoried and fixed in isolated worktrees, pre-merge relative to the seed changes in 198-24 and 198-25 → their `seed-sensitive? = yes` rows are provisional → this plan is the first point at which every seed change is merged, so the re-derivation happens here or it never happens before 198-29 predicts from it"
  prohibitions:
    - statement: "A lane must not be made green by hiding failures. `maxFailures` must not be raised or removed, the `--project` set must not be narrowed, and no spec file or test may be newly skipped, `.fixme`-d, `.only`-d, deleted, or `git rm`-ed. The pre-existing CI guard on the regression spec must not be widened to hide a failure or narrowed to manufacture one."
      status: kept
      verification: flagged-unverified
    - statement: "A committed PNG baseline must not be regenerated to make a test pass without an explicit human decision recorded at a blocking checkpoint. Regenerating evidence to match current output is the precise remedy D-39 forbids for the Tier A lane."
      status: kept
      verification: flagged-unverified
    - statement: "Progress on locally-skipped tests must not be reported as progress on the CI lane. The CI-contributing and local-only subsets are counted and reported separately."
      status: kept
      verification: flagged-unverified
    - statement: "A pre-merge attribution row marked `seed-sensitive? = yes` must not be carried into a fix or into plan 198-29's prediction without being re-derived against the merged demo-seed pipeline. A stale inventory is not evidence."
      status: kept
      verification: flagged-unverified
---

<objective>
**Gap 2, part 3 of 3.** Fix the screenshot cluster of the masked Playwright failures:
`operator-screenshot-regression.spec.ts` and `operator-screenshots.spec.ts` — the rows plan 198-26's
attribution table assigned to cluster `198-28`.

This cluster carries a complication the other two do not, and it must be established before any fix:
`operator-screenshot-regression.spec.ts` begins its describe block with
`test.skip(!!process.env.CI, "visual screenshot baselines are platform-sensitive; run this guard
locally before updating PNG snapshots")`. **That guard means its failures appear in the unbounded
local run — and therefore in the 28 — but do not contribute to the CI lane's red conclusion.**
Fixing them is still worth doing (they are real local red, and the guard exists to be run locally
before baselines change), but reporting that work as progress on `Example app browser E2E` would be
an over-claim, and this plan forbids it explicitly.

The cluster also carries the round's one genuinely one-way decision: whether to regenerate a
committed PNG baseline under `operator-screenshot-regression.spec.ts-snapshots/`. Regenerating a
baseline to match current output is exactly the remedy D-39 forbids for the Tier A capture lane. A
human decides that, at a blocking checkpoint, with the reasoning on the table — not an executor
mid-task.

**Ceiling, restated:** `CI required` cannot conclude `success` this round. `Tier A capture lane
(byte-stable evidence)` stays red under D-39 and must not be touched. The honest target is 3 red
`needs:` lanes down to 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/deferred-items.md
@.planning/WINDOWS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-validate both attribution tables post-merge, then establish which rows actually run on CI</name>
  <files>.planning/audits/198-round4-playwright.md</files>
  <precondition>Every demo-seed-writing plan of this round (198-23, 198-24, 198-25) and both Playwright inventory/fix plans (198-26, 198-27) are merged into this worktree — assert with `git log --oneline` that each plan's commits are present before running anything, and halt if any is absent, because the whole point of this task is to measure against the merged seed pipeline rather than a pre-merge snapshot.</precondition>
  <read_first>
    - .planning/audits/198-round4-playwright.md (plan 198-26's `## Cluster assignment` rows for `198-28`, their causes, the `## Forbidden remedies` list, and — critically — the `## Pre-merge status` and `## Pre-merge status (198-27)` sections with their `seed-sensitive?` verdicts and recorded HEAD SHAs; those flagged rows are this task's re-validation work order)
    - .planning/audits/198-round4-demo-seed.md (the complete record of every demo-seed producer module plans 198-23, 198-24 and 198-25 changed, and their closing measured `mix verify.example` counts — this is what the pre-merge Playwright inventories could not see)
    - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts (lines 76-142 — the describe-level `test.skip(!!process.env.CI, ...)`, the `beforeEach` that additionally skips `testInfo.project.name === "chromium"` and sets 1280x900 for `desktop-chromium` and 375x812 for `mobile-chromium`, the `expectOperatorScreenshot` helper, and the five tests `global chrome and Home workflow launchers stay stable`, `dense Timeline keeps row-first evidence stable`, `row-history drawer keeps as-of evidence stable`, `Exports readiness hierarchy stays stable`, `Retention safety hierarchy stays stable`)
    - examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts (lines 70-196 — the `beforeEach` that returns early unless `OPERATOR_SCREENSHOT_DIR` is set, and the two tests `admin investigation and governance surfaces` and `empty and denied states`; establish whether the capture path executes at all under the CI lane's environment)
    - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots (the committed baselines: `home-`, `timeline-dense-`, `row-history-`, `exports-`, `retention-` for both `desktop-chromium` and `mobile-chromium` — these are the artifacts a regeneration decision would rewrite)
    - examples/threadline_phoenix/e2e/playwright.config.ts (`snapshotPathTemplate` and the project definitions — how baselines are namespaced per project, and the pixel-diff tolerance in effect)
    - .github/workflows/ci.yml (the `verify-example-browser` job — the env it sets, which decides whether `OPERATOR_SCREENSHOT_DIR` and `CI` are present; read, do not edit)
  </read_first>
  <action>
    Change no spec, no baseline, and no config in this task. Re-validate, then establish and record.

    **First, the post-merge re-validation gate.** Plans 198-26 and 198-27 took their inventories and
    made their fixes in isolated worktrees, in the same waves as plans 198-24 and 198-25 respectively,
    which rewrite demo-seed producer modules. Neither could observe its sibling's seed changes. This
    plan is the first point at which all of them are merged, and its output feeds 198-29's prediction
    scorecard — so a stale attribution propagates straight into the measured-CI record unless it is
    re-derived here.

    Run `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` unbounded
    against the merged tree. Append `## Post-merge re-validation (198-28)` to
    `.planning/audits/198-round4-playwright.md` containing:

    - the current `git rev-parse HEAD`, and the two pre-merge HEAD SHAs recorded by 198-26 and 198-27,
      so the measurement points are distinguishable;
    - the verbatim Playwright summary line from this post-merge run, and its total failure count;
    - a re-derivation table with one row per attribution row that 198-26 or 198-27 marked
      `seed-sensitive? = yes`, columns: spec file, test title, project, pre-merge cause, post-merge
      observed state (`still failing with the same cause` / `failing with a different cause` /
      `now passing` / `newly failing`), and `re-attributed? (yes/no)`;
    - a `## Divergences` subsection listing every row whose post-merge state is anything other than
      `still failing with the same cause`, plus every test that 198-26 or 198-27 recorded as fixed
      which this run reports failing. Each divergence gets a fresh cause, and its attribution row is
      rewritten in place with the post-merge cause. State the divergence count as an integer — `0` is
      a legitimate and welcome result, but it must be a measured `0`, not an unexamined one.

    A regression introduced by a sibling's seed change is fixed at cause under the same rules as the
    rest of the round — never by reverting the seed fix, and never by weakening the spec.

    **Then**, for every cluster-`198-28` attribution row, determine from source and from the CI job definition
    whether that test executes on CI at all. `operator-screenshot-regression.spec.ts` skips entirely
    when `process.env.CI` is set; confirm that from the source line rather than assuming it, and
    confirm what `.github/workflows/ci.yml`'s `verify-example-browser` job sets for `CI` and for
    `OPERATOR_SCREENSHOT_DIR`.

    Append `## CI-contributing versus local-only split (198-28)` to
    `.planning/audits/198-round4-playwright.md` with a table of one row per cluster-`198-28`
    attribution row, columns: spec file, test title, project, `runs on CI? (yes/no)`, the source line
    that decides it, cause. Then state two integers: the CI-contributing count and the local-only
    count, and confirm they sum to the cluster's assigned row count.

    Also record the pixel-diff tolerance in effect (from `playwright.config.ts` and any
    `expectOperatorScreenshot` helper argument) and, for every row whose cause is a visual diff, the
    measured diff figure against that tolerance. A diff dispositioned as "close enough" without a
    number is not accepted.

    Record explicitly that fixing a local-only row does not move `Example app browser E2E` toward
    `success`, so the round's reporting cannot conflate the two.
  </action>
  <acceptance_criteria>
    - `git log --oneline` confirms commits from plans 198-23, 198-24, 198-25, 198-26 and 198-27 are all present in this worktree before any measurement is taken.
    - `.planning/audits/198-round4-playwright.md` contains `## Post-merge re-validation (198-28)` recording a 40-character post-merge `git rev-parse HEAD` plus the two pre-merge SHAs from 198-26 and 198-27, and the verbatim post-merge Playwright summary line.
    - The re-derivation table has exactly one row for every attribution row marked `seed-sensitive? = yes` by 198-26 or 198-27; the row count equals the sum of those two plans' stated `yes` counts, shown as an arithmetic line.
    - Every re-derivation row carries a post-merge observed state from the four permitted values and a `re-attributed?` verdict of exactly `yes` or `no` — no blank cells.
    - A `## Divergences` subsection exists with the divergence count stated as an integer; every listed divergence has a fresh post-merge cause, and its row in `## Full failure attribution` has been rewritten to that cause.
    - No fix is made in this task and no fix is made in Task 3 against a `seed-sensitive? = yes` row whose `re-attributed?` cell is blank.
    - `.planning/audits/198-round4-playwright.md` contains `## CI-contributing versus local-only split (198-28)` with one row per cluster-`198-28` attribution row and a non-empty `runs on CI?` and deciding-source-line cell for each.
    - Two integers are stated — CI-contributing count and local-only count — with an arithmetic line showing they sum to the cluster's assigned row count.
    - The recorded `runs on CI?` value for every `operator-screenshot-regression.spec.ts` row is `no`, justified by citing the describe-level `test.skip(!!process.env.CI, ...)` line.
    - The pixel-diff tolerance is recorded as a number, and every visual-diff row records its measured diff against that number.
    - The file states that local-only fixes do not move the CI lane.
    - `git diff --name-only` after this task lists only `.planning/audits/198-round4-playwright.md`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^## Post-merge re-validation (198-28)' .planning/audits/198-round4-playwright.md &amp;&amp; grep -q '^## Divergences' .planning/audits/198-round4-playwright.md &amp;&amp; grep -q '^## CI-contributing versus local-only split (198-28)' .planning/audits/198-round4-playwright.md &amp;&amp; grep -q 'test.skip(' examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts &amp;&amp; test -z "$(git diff --name-only | grep -v '^\.planning/audits/198-round4-playwright\.md$')"</automated>
  </verify>
  <done>Every pre-merge seed-sensitive attribution row has been re-derived against the merged seed pipeline with its divergences recorded and re-attributed, and every row in this cluster is labelled CI-contributing or local-only with its deciding source line, the two counts summing correctly and every visual diff carrying a number against a recorded tolerance.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide whether any committed PNG baseline may be regenerated</name>
  <decision>May any committed PNG baseline under `examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots/` be regenerated to resolve a cluster-`198-28` failure?</decision>
  <read_first>
    - .planning/audits/198-round4-playwright.md (Task 1's CI-contributing/local-only split table and the measured diffs against the recorded tolerance — the evidence this decision rests on)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-39 in full — the maintainer's A1 decision, and the milestone-level prohibition on Tier-A `page.*` regeneration that makes evidence regeneration a decision rather than a technique)
    - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots (the ten committed baselines a regeneration would rewrite: `home-`, `timeline-dense-`, `row-history-`, `exports-`, `retention-`, each for `desktop-chromium` and `mobile-chromium`)
  </read_first>
  <context>
    Task 1 established which baselines diverge, by how much against the recorded pixel-diff tolerance,
    and what caused each divergence — a deliberate already-shipped product change, an unintentional
    visual regression, or platform/renderer variance (the exact reason this spec carries its own
    `test.skip(!!process.env.CI, ...)` guard).

    Regenerating a baseline to match current output is evidence regeneration: it erases the record of
    what the surface previously looked like, which is the sole thing this guard exists to hold. That is
    the same category of remedy D-39 forbids for the Tier A capture lane this entire milestone. The
    parallel is presented rather than assumed away, and the maintainer decides.

    Note the stakes are bounded: every `operator-screenshot-regression.spec.ts` row is local-only, so
    none of these options changes the `Example app browser E2E (Playwright)` CI conclusion either way.
  </context>
  <options>
    <option id="option-a">
      <name>A — Fix at the product or spec cause; regenerate nothing</name>
      <pros>Preserves every baseline as evidence; correct when the divergence is an unintentional regression; extending the spec's masking locator list handles newly-volatile seeded fields without touching a baseline</pros>
      <cons>Costs more diagnosis time; cannot resolve a divergence that is a legitimate, already-shipped visual change</cons>
    </option>
    <option id="option-b">
      <name>B — Regenerate the specific named baselines</name>
      <pros>Correct when the divergence reflects a deliberate product change that already shipped; restores the guard to a useful state instead of a permanently-red one</pros>
      <cons>Evidence regeneration — the same category of remedy D-39 forbids for Tier A; if taken, each regenerated file and the product change justifying it must be named in the record</cons>
    </option>
    <option id="option-c">
      <name>C — Leave this cluster's local-only rows red, recorded with causes</name>
      <pros>Costs nothing on the CI lane since these rows do not run there; keeps every baseline intact; honest about an unresolved local red</pros>
      <cons>Leaves a known local failure standing; the guard stays noisy until a later round resolves it</cons>
    </option>
  </options>
  <action>
    Present the divergences with their measured diffs and diagnoses, then present options A, B and C as
    stated above. Record the maintainer's verbatim answer, the date, and the chosen option letter under
    `## Baseline regeneration decision (198-28)` in `.planning/audits/198-round4-playwright.md`. If the
    answer is A or C, Task 3 may not write any baseline file. If it is B, the decision record must name
    each baseline to be regenerated and the product change that justifies it.
  </action>
  <acceptance_criteria>
    - `.planning/audits/198-round4-playwright.md` contains `## Baseline regeneration decision (198-28)` with the maintainer's verbatim answer, the date, and the chosen option letter.
    - If the recorded option is A or C, `git status --porcelain examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots` is empty at the end of this plan.
    - If the recorded option is B, every regenerated baseline file is named in the decision record together with the product change that justifies it.
    - No baseline file is written before this checkpoint records an answer.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^## Baseline regeneration decision (198-28)' .planning/audits/198-round4-playwright.md</automated>
  </verify>
  <resume-signal>Reply with `A`, `B`, or `C`. If `B`, name the specific baseline files that may be regenerated and the product change justifying each.</resume-signal>
  <done>The maintainer has decided, in writing, whether any committed baseline may be regenerated, with the D-39 parallel presented rather than assumed away.</done>
</task>

<task type="auto">
  <name>Task 3: Fix this cluster at its causes within the recorded decision, and re-measure</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts, examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts, .planning/audits/198-round4-playwright.md</files>
  <read_first>
    - .planning/audits/198-round4-playwright.md (Task 1's split table and Task 2's recorded decision — the decision bounds what this task may do; also plan 198-26's `## Measured after-count (198-26)` and plan 198-27's `## Measured after-count (198-27)`, whichever is later, for the delta baseline)
    - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts (the five tests and the `expectOperatorScreenshot` helper plus the masking locator list at lines 55-74 — volatile values like `id`, `inserted_at`, `ticket_id`, `updated_at` are already masked, so a diff in those fields means the mask list, not the baseline, is what is wrong)
    - examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts (the two tests and the `capture` helper — establish whether its failures are capture-path failures or assertion failures)
    - .planning/audits/198-round4-demo-seed.md (plans 198-23/24/25's seed changes — seeded content appears in these screenshots, so a seed change is a plausible and legitimate cause of a diff; if so, the mask list or the fixture, not the baseline, is usually the right fix)
  </read_first>
  <action>
    Fix the cluster-`198-28` rows at the causes Task 1 recorded, staying strictly inside Task 2's
    decision.

    Where a diff sits in a field that should be volatile (`id`, `inserted_at`, `ticket_id`,
    `updated_at`, or any newly-volatile seeded value introduced by plans 198-23/24/25), the correct
    fix is to extend the spec's masking locator list — not to regenerate a baseline. Prefer that fix
    wherever it applies, and say so in the audit record.

    Where a diff reflects a real, unintentional visual regression, fix the product and record it.

    Regenerate a baseline only if Task 2 recorded option B, and only the specific files that decision
    named.

    Capture a red-then-green teeth proof under `## Red-then-green teeth proof (198-28)` for at least
    one fixed failure per spec file.

    Then re-measure: run `mix verify.example_browser --project=desktop-chromium
    --project=mobile-chromium` unbounded and append `## Measured after-count (198-28)` with the
    verbatim summary line and the integer delta against the most recent prior after-count. **Report
    the delta split two ways: total local failures, and CI-contributing failures.** A total that drops
    while the CI-contributing count does not must be reported as exactly that.

    Reconcile every cluster-`198-28` row as `closed` (with the fix) or `open` (with the reason), and
    show the arithmetic that the two sum to the cluster's assigned count. Add a dated
    `deferred-items.md` entry for anything left open.

    Close with the restated ceiling: `CI required` cannot conclude `success` this round because the
    Tier A capture lane's only remedy is forbidden under D-39; the target is 3 red `needs:` lanes down
    to 1.

    Forbidden: new `test.skip`/`test.fixme`/`.only`, deleted assertion or spec, `git rm`, widening or
    narrowing the pre-existing CI guard, any edit to `playwright.config.ts`, `--project` narrowing,
    any edit to `.github/workflows/ci.yml`, `.github/rulesets/main.json` or `CONTRIBUTING.md`, and any
    Tier-A `page.*` regeneration.
  </action>
  <acceptance_criteria>
    - `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` reports `0 failed` for `operator-screenshots.spec.ts` under both projects, and for `operator-screenshot-regression.spec.ts` in the local (non-`CI`) environment where it executes — or every remaining failure is dispositioned `open` with a named reason.
    - `.planning/audits/198-round4-playwright.md` contains `## Red-then-green teeth proof (198-28)` with verbatim pre-fix and post-fix transcripts for at least one failure per spec file fixed.
    - `## Measured after-count (198-28)` states the verbatim summary line, the total-failure delta, and the CI-contributing-failure delta as two separate integers.
    - Every cluster-`198-28` row carries `closed` or `open`; an arithmetic line shows the two sum to the cluster's assigned count; every `open` row has a named reason.
    - If Task 2 recorded option A or C, `git status --porcelain examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots` is empty. If option B, the changed baseline files match exactly the set named in the decision record.
    - The count of `test.skip(` occurrences in `operator-screenshot-regression.spec.ts` equals its pre-task value — the CI guard is neither widened nor narrowed, and no new skip is added.
    - `git diff -- examples/threadline_phoenix/e2e/playwright.config.ts .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md` is empty.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.example_browser --project=desktop-chromium --project=mobile-chromium</automated>
  </verify>
  <done>The screenshot cluster is fixed within the maintainer's recorded decision, masking is preferred over baseline rewriting, and the after-count is reported with the CI-contributing delta separated from the total so no local-only win is presented as CI progress.</done>
</task>

</tasks>

<reversibility>
  <decision rating="one-way">Regenerating a committed PNG baseline under `operator-screenshot-regression.spec.ts-snapshots/` destroys the prior evidence of what the surface looked like — the very thing the guard exists to preserve, and the same class of remedy D-39 forbids for the Tier A lane. Gated behind the `checkpoint:decision` in Task 2.</decision>
  <decision rating="reversible">Extending the spec's masking locator list to cover a newly-volatile seeded field is reversible — an ordinary spec change, and it preserves rather than rewrites the baseline evidence.</decision>
  <decision rating="costly">A product-side visual fix is costly: the Tier A capture lane observes the same surface and cannot be regenerated this milestone, so a visual change could deepen its drift with no permitted remedy. Recorded in the attribution table so plan 198-29 can attribute any Tier A movement.</decision>
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed PNG baselines → the regression guard's evidentiary value | Regenerating a baseline to match current output erases the evidence the guard exists to hold |
| Pre-merge worktree inventory → post-merge fix and prediction | Attribution taken blind to a same-wave sibling's seed rewrite is provisional until re-derived against the merged tree |
| Local-only test results → CI lane reporting | The regression spec skips on CI; conflating its results with the lane's is an over-claim |
| Product visual change → Tier A capture lane | The Tier A lane observes the same surface and is under a regeneration prohibition (D-39) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-28-01 | Tampering | committed PNG baselines | high | mitigate | Regeneration is gated behind a blocking `checkpoint:decision`; if the decision is A or C, an empty `git status` on the snapshots directory is an acceptance criterion; if B, the changed set must match the named set exactly |
| T-198-28-02 | Repudiation | CI-lane progress reporting | high | mitigate | Task 1's CI-contributing versus local-only split with summing counts, and Task 3's two-integer delta, are acceptance criteria |
| T-198-28-03 | Tampering | the pre-existing CI skip guard | high | mitigate | The `test.skip(` occurrence count must equal its pre-task value — the guard may be neither widened to hide a failure nor narrowed to manufacture one |
| T-198-28-04 | Elevation of Privilege | `ci-required`'s `needs:` and the ruleset | high | mitigate | Empty diffs on `ci.yml`, `.github/rulesets/main.json` and `CONTRIBUTING.md` are acceptance criteria; D-42 restated in the prohibitions |
| T-198-28-06 | Repudiation | pre-merge attribution tables | high | mitigate | Plans 198-26/198-27 inventoried in isolated worktrees blind to their same-wave siblings' seed rewrites; Task 1's post-merge re-validation gate re-derives every `seed-sensitive? = yes` row against the merged pipeline, with a divergence count and a `re-attributed?` verdict per row as acceptance criteria, and forbids Task 3 fixing any row left un-re-attributed |
| T-198-28-05 | Information Disclosure | unnumbered visual dispositions | medium | mitigate | Every visual diff must carry a measured figure against a recorded tolerance; "close enough" without a number is rejected |
| T-198-28-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed, added, or upgraded; `package.json` and its lockfile are untouched, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `.planning/audits/198-round4-playwright.md` carries `## Post-merge re-validation (198-28)` with the three HEAD SHAs, the re-derivation table covering every `seed-sensitive? = yes` row, and a `## Divergences` subsection with an integer count; it also carries the CI-contributing/local-only split with summing counts, the recorded baseline-regeneration decision, the teeth proof, `## Measured after-count (198-28)` with two deltas, and the arithmetic cluster reconciliation.
- `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` shows this cluster's failures resolved or dispositioned `open` with reasons.
- `git status --porcelain examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots` matches the recorded decision.
- `git diff -- .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts` is empty.
</verification>

<success_criteria>
The screenshot cluster is resolved without a single baseline being rewritten on an executor's
judgment, the CI lane's real gain is reported separately from the local-only gain, and the evidentiary
value the regression guard exists to hold is intact.
</success_criteria>

## Artifacts this phase produces

**Changed files**
- `examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts` — masking locator list extended where a seeded field became volatile; fixed at cause elsewhere.
- `examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts` — fixed at cause.
- `.planning/audits/198-round4-playwright.md` — `## Post-merge re-validation (198-28)` with the re-derivation table and `## Divergences`, `## CI-contributing versus local-only split (198-28)`, `## Baseline regeneration decision (198-28)`, `## Red-then-green teeth proof (198-28)`, `## Measured after-count (198-28)`, and the cluster reconciliation.
- `examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots/*.png` — **only** if the Task 2 checkpoint recorded option B, and only the files that decision named.
- `.planning/phases/198-green-bringup/deferred-items.md` — a dated entry for anything left `open`.

**Explicitly not produced**
- No change to `examples/threadline_phoenix/e2e/playwright.config.ts`, `.github/workflows/ci.yml`, `.github/rulesets/main.json`, `CONTRIBUTING.md`, or `.planning/REQUIREMENTS.md`.
- No Tier A capture-lane work of any kind (D-39).

<output>
Create `.planning/phases/198-green-bringup/198-28-SUMMARY.md` when done.
</output>
