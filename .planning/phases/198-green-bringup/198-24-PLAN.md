---
phase: 198-green-bringup
plan: 24
type: execute
wave: 2
gap_closure: true
depends_on:
  - 198-23
files_modified:
  - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_runs.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/temporal.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/reset.ex
  - .planning/audits/198-round4-demo-seed.md
autonomous: true
requirements:
  - GREEN-04

estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every remaining failing test in `demo_contract_test.exs` — the clusters the tracer did not take — is fixed at its cause, with the seed-vs-assertion disposition recorded per failure."
    - "The fix protocol written by plan 198-23 is followed literally, not re-derived: same inventory step, same attribution vocabulary, same red-then-green proof requirement, same re-measure step."
    - "A red-then-green teeth proof is captured for at least one failure per describe block fixed here."
    - "`mix verify.example`'s measured failure count is recorded before and after this plan's changes, and the delta is stated as an integer."
    - "Any failure this plan cannot fix is recorded with a named cause and a dated `deferred-items.md` entry — never left as a bare 'still red' and never silently absorbed."
    - "The whole of `demo_contract_test.exs` passes on two consecutive runs, or the residual failures are enumerated by name with causes."
    - statement: "Ordering edge (GREEN-04): the `D-13 in-window variety guarantee` test asserts the presence of at least one INSERT, one UPDATE and one DELETE as a set-membership property, not as an ordered sequence, so a change in seed emission order cannot flip it."
      verification: explicit
    - statement: "Empty edge (GREEN-04): the `SEED-04 org Y retention end state` and `WALK-04 redaction policy evidence` assertions fail loudly when their query returns zero rows, rather than passing vacuously on an empty purged footprint."
      verification: explicit
    - statement: "Adjacency edge (GREEN-04): the `org_memberships setup rows are backdated outside the default 24h window` and `untracked null-actor setup rows are backdated outside the default 24h window` tests state explicitly whether a row landing exactly on the 24h boundary counts as inside or outside."
      verification: explicit
  artifacts:
    - "examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs — the SEED-05, SEED-02/04, WALK-04, D-05 and D-13 describe blocks, fixed at cause"
    - "The demo-seed module(s) named by this plan's attribution — changed only where seeded content, not the assertion, is wrong"
    - ".planning/audits/198-round4-demo-seed.md — extended with this plan's inventory rows, dispositions, teeth proofs, and re-measured delta"
  key_links:
    - "plan 198-23's `## Fix protocol for the remaining clusters` → this plan's method (read it, do not invent a second method)"
    - "`mix demo.reset` idempotency (`SEED-02`/`SEED-04` describe blocks) → `Demo.Reset` and `Demo.Seed` → a double-reset must keep heroes and the semantic fingerprint stable"
    - "persona setup-row actor attribution (`D-05`) → `Demo.Seed.Personas` → `org_memberships` rows must carry a non-null `actor_ref` on their transaction"
  prohibitions:
    - statement: "A failing example-app test must not be made to pass by deleting, weakening, `@tag :skip`-ing, `git rm`-ing, or conditionally guarding its assertion."
      status: kept
      verification: flagged-unverified
    - statement: "`mix verify.example` must not be removed from `mix ci.all`, narrowed in scope, or detached from `ci.yml`'s `current` lane."
      status: kept
      verification: flagged-unverified
    - statement: "GREEN-04 must not be marked Complete on this plan's local output; the verdict belongs to plan 198-29's measured CI run (D-01)."
      status: kept
      verification: flagged-unverified
---

<objective>
Expansion of the tracer: fix the remaining failing clusters in
`examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs` — everything plan
198-23 did not take. In scope: `describe "SEED-05 delete incident"`, `describe "SEED-02 idempotency
and SEED-04 reset recovery"`, `describe "SEED-04 org Y retention end state"`, `describe "WALK-04
redaction policy evidence"`, `describe "D-05 persona setup actor attribution"`, and `describe "D-13
in-window variety guarantee"`.

Purpose: `Run test suite (current)` stays red until `mix verify.example` exits 0. The tracer proved
the loop on the SEED-03 clusters; this plan applies that exact loop to the rest of the same file,
where the prior characterisation in `deferred-items.md` names `org_memberships` actor attribution and
the SEED-05 delete incident specifically.

Output: `demo_contract_test.exs` passing on two consecutive runs (or its residual failures
enumerated by name with causes), a lower measured `mix verify.example` failure count, and the audit
file extended with this plan's evidence.

**Ceiling:** this plan cannot close GREEN-04 either. Only the measured CI run in plan 198-29 can.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/deferred-items.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the SEED-05, SEED-02/04 and WALK-04 clusters at their causes</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_runs.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/reset.ex, .planning/audits/198-round4-demo-seed.md</files>
  <read_first>
    - .planning/audits/198-round4-demo-seed.md (plan 198-23's output — read `## Fix protocol for the remaining clusters` first and follow it literally; read `## Two-run failure inventory` and `## Cause attribution` for the rows belonging to this task's describe blocks)
    - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (lines 155-268 — `describe "SEED-05 delete incident"` / `test "hard delete on ticket_replies for #4518 by deleter not closer"`; `describe "SEED-02 idempotency and SEED-04 reset recovery"` / `test "double demo.reset keeps heroes and semantic fingerprint stable"` and `test "Seed.run/0 twice in sequence (reset-then-seed) does not raise"`; `describe "SEED-04 org Y retention end state"` / `test "offboarded-co audit footprint purged with manifest retention evidence"`; `describe "WALK-04 redaction policy evidence"` / `test "post-demo.seed redaction_policy row matches manifest subject_ref"`)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex (the declared retention evidence and `subject_ref` contract these assertions read against)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/reset.ex and lib/threadline_phoenix/demo/seed.ex (the reset/seed idempotency path — the `double demo.reset` and `Seed.run/0 twice` tests judge exactly this)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex and seed/retention_runs.ex (where the offboarded-co purge footprint and its retention-run evidence are produced)
    - .planning/phases/198-green-bringup/deferred-items.md (the `## Plan 198-12` entry names the SEED-05 delete incident explicitly as part of this class)
  </read_first>
  <behavior>
    - `hard delete on ticket_replies for #4518 by deleter not closer`: a DELETE change row exists on `ticket_replies` for ticket 4518, and the actor on its audit transaction is the deleter persona and not the closer persona. Asserted by actor identity and change operation, not by a pinned row id or count.
    - `double demo.reset keeps heroes and semantic fingerprint stable`: running reset twice leaves the hero tickets present and the semantic fingerprint byte-equal to the single-reset fingerprint. Asserted as equality between two computed fingerprints, never against a pinned literal fingerprint that a future milestone would rot.
    - `offboarded-co audit footprint purged with manifest retention evidence`: the offboarded-co org's audit footprint is empty AND a retention-evidence row exists proving why. The emptiness assertion must be paired with a non-empty evidence assertion, so an all-empty database cannot pass this test.
    - `post-demo.seed redaction_policy row matches manifest subject_ref`: exactly one redaction_policy row exists whose `subject_ref` equals the manifest's declared value, read from `Demo.Manifest` rather than from an inline literal.
  </behavior>
  <action>
    Follow plan 198-23's fix protocol step for step. For each failing test in this task's four
    describe blocks, take the cause attribution from `.planning/audits/198-round4-demo-seed.md` (or
    extend the inventory if a failure appeared in this plan's runs that the tracer's two runs did not
    surface — the failure set is non-deterministic and a new row is expected, not an anomaly).

    Fix `seed-content-wrong` failures in the named seed module; fix `assertion-rotted` failures by
    rewriting the assertion into shape form. Where a test reads a value the manifest already declares
    (`subject_ref`, the hero ticket numbers 4521 and 4518, the retention evidence shape), read it from
    `ThreadlinePhoenix.Demo.Manifest` rather than restating it as an inline literal — that is exactly
    the version-pinned-literal rot GREEN-04's own text calls out.

    Pair every emptiness assertion with a positive existence assertion, so the offboarded-co "purged"
    test cannot pass against a database that seeded nothing at all.

    Capture a red-then-green transcript for at least one failure per describe block and append it to
    `.planning/audits/198-round4-demo-seed.md` under `## Red-then-green teeth proof (198-24)`.

    Forbidden: `@tag :skip`, `git rm`, deleted assertion, `assert true`, narrowing `mix
    verify.example`, editing `.github/workflows/ci.yml`, editing `mix.exs`.
  </action>
  <acceptance_criteria>
    - `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix/demo_contract_test.exs` reports `0 failures` for all tests in the four describe blocks named in this task, on two consecutive runs.
    - `.planning/audits/198-round4-demo-seed.md` contains `## Red-then-green teeth proof (198-24)` with at least one captured pre-fix failure transcript and matching post-fix pass transcript per describe block fixed.
    - The `offboarded-co audit footprint purged` test contains both an emptiness assertion and a positive retention-evidence existence assertion; a source read of the test shows both present.
    - The `redaction_policy` test reads `subject_ref` from `ThreadlinePhoenix.Demo.Manifest` rather than from an inline string literal.
    - `grep -c '@tag :skip' examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs` returns `0`.
    - `git diff -- .github/workflows/ci.yml mix.exs` is empty.
    - `grep -c '^\s*test "' examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs` is greater than or equal to its pre-task value, proving no test was deleted.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/threadline_phoenix &amp;&amp; MIX_ENV=test mix test test/threadline_phoenix/demo_contract_test.exs</automated>
  </verify>
  <done>The SEED-05, SEED-02/04 and WALK-04 clusters pass for cause-attributed reasons with captured teeth proofs, and manifest-declared values are read from the manifest rather than pinned inline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the D-05 persona-attribution and D-13 variety clusters, then re-measure the lane</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/temporal.ex, .planning/audits/198-round4-demo-seed.md</files>
  <read_first>
    - .planning/audits/198-round4-demo-seed.md (the fix protocol, and this plan's Task 1 output — the running inventory and delta)
    - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (lines 268-367 — `describe "D-05 persona setup actor attribution"` / `test "org_memberships setup rows have non-null actor_ref on transaction"`, `test "org_memberships setup rows are backdated outside the default 24h window"`, `test "untracked null-actor setup rows are backdated outside the default 24h window"`; and `describe "D-13 in-window variety guarantee"` / `test "default 24h window contains ≥1 INSERT, ≥1 UPDATE, and ≥1 DELETE"`)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex (where `org_memberships` setup rows and their actor attribution are produced — `deferred-items.md`'s Plan 198-12 entry names `org_memberships actor attribution` as one of the three original failure families)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/temporal.ex (where backdating relative to the default 24h window is computed — the boundary semantics live here)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-05 and D-13 in the decisions section — the contracts these tests encode)
  </read_first>
  <behavior>
    - `org_memberships setup rows have non-null actor_ref on transaction`: every `org_memberships` setup row's audit transaction carries a non-null `actor_ref`. The assertion first asserts the row set is non-empty, then asserts the property over it — an empty set must fail, not pass.
    - `org_memberships setup rows are backdated outside the default 24h window` and `untracked null-actor setup rows are backdated outside the default 24h window`: every such row's timestamp is strictly older than `now - 24h`. The comparison is stated explicitly as strict, and a row landing exactly on the boundary is documented as inside the window (i.e. it fails the "outside" assertion) rather than left to `<` versus `<=` chance.
    - `default 24h window contains ≥1 INSERT, ≥1 UPDATE, and ≥1 DELETE`: the set of operations present in the window contains all three, asserted as set membership over the distinct operations, independent of emission order.
  </behavior>
  <action>
    Apply plan 198-23's fix protocol to the `D-05` and `D-13` describe blocks, with the same
    seed-vs-assertion attribution discipline and the same red-then-green requirement.

    Make the 24h-boundary semantics explicit in the test source: assert with a stated strict
    comparison and add a one-line comment naming which side an exactly-on-the-boundary row falls on.
    Make the D-13 variety assertion a set-membership check over the distinct operations found in the
    window, so a change in seed emission order cannot flip it.

    Pair each of the three D-05 property assertions with a preceding non-emptiness assertion on its
    row set.

    Then re-measure: run `mix verify.example` from the repository root twice and append a
    `## Measured after-count (198-24)` section to `.planning/audits/198-round4-demo-seed.md` with both
    verbatim `N tests, M failures` lines and the integer delta against the count recorded at the end
    of plan 198-23.

    If any failure in `demo_contract_test.exs` remains after this plan, enumerate it by module, test
    name and cause in the audit file, and add a dated entry to
    `.planning/phases/198-green-bringup/deferred-items.md`. A bare "still red" is not acceptable.

    Forbidden: `@tag :skip`, `git rm`, deleted assertion, narrowing `mix verify.example`, editing
    `.github/workflows/ci.yml` or `mix.exs`.
  </action>
  <acceptance_criteria>
    - `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix/demo_contract_test.exs` reports `0 failures` across the whole file on two consecutive runs — or, if not, every residual failure is enumerated by name and cause in `.planning/audits/198-round4-demo-seed.md` and carries a dated `deferred-items.md` entry.
    - The three D-05 tests each contain a non-emptiness assertion on the row set that precedes the property assertion.
    - The two backdating tests state the boundary side explicitly in a source comment naming the strict comparison used.
    - The D-13 test asserts over the distinct operation set, not over a positional index into an ordered list.
    - `.planning/audits/198-round4-demo-seed.md` contains `## Measured after-count (198-24)` with two verbatim `N tests, M failures` lines and an explicit integer delta.
    - `grep -c '@tag :skip' examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs` returns `0` and `git diff -- .github/workflows/ci.yml mix.exs` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/threadline_phoenix &amp;&amp; MIX_ENV=test mix test test/threadline_phoenix/demo_contract_test.exs</automated>
  </verify>
  <done>Every cluster in `demo_contract_test.exs` is resolved or honestly deferred with a named cause, the boundary and ordering semantics are explicit in source, and the lane's measured failure count is recorded with its delta.</done>
</task>

</tasks>

<reversibility>
  <decision rating="reversible">Rewriting a pinned literal into a manifest read or a shape assertion is reversible — an ordinary source change with the reasoning recorded in the audit file.</decision>
  <decision rating="costly">Changing `Demo.Seed.Personas` or `Demo.Seed.Temporal` is costly: persona attribution and backdating feed the walkthrough tests (plan 198-25) and the Playwright specs (plans 198-26/27/28). Mitigated by recording each seed change in the audit file so downstream plans re-inventory rather than assume.</decision>
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mix demo.seed` output → contract assertions | Seeded content is the untrusted input; a wrong disposition launders a data defect into a weakened assertion |
| Manifest declaration → test literal | A literal restated inline diverges silently from the manifest it is supposed to encode |
| Local run output → requirement status | Local output must never cross into a Complete verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-24-01 | Repudiation | the claim "fixed on its merits" | high | mitigate | Per-describe-block captured red-then-green transcripts are an acceptance criterion |
| T-198-24-02 | Tampering | test assertions | high | mitigate | Test-count-non-decreasing, `@tag :skip` count 0, empty `ci.yml`/`mix.exs` diffs, all as acceptance criteria |
| T-198-24-03 | Information Disclosure | vacuous emptiness assertions | high | mitigate | Every emptiness assertion must be paired with a positive existence assertion, checked in source |
| T-198-24-04 | Elevation of Privilege | requirement status | high | mitigate | Recorded statement that local output is inadmissible for GREEN-04 per D-01 |
| T-198-24-SC | Tampering | npm/pip/cargo/hex installs | high | mitigate | No dependency added or upgraded; `mix.exs` and lockfiles untouched (asserted), so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix/demo_contract_test.exs` passes on two consecutive runs, or residuals are enumerated with causes.
- `mix verify.example`'s failure count after this plan is recorded with an integer delta against plan 198-23's closing count.
- `.planning/audits/198-round4-demo-seed.md` carries this plan's teeth proofs and after-count sections.
- `git diff -- .github/workflows/ci.yml mix.exs playwright.config.ts` is empty.
</verification>

<success_criteria>
`demo_contract_test.exs` no longer contributes failures to `mix verify.example` — every one of its
clusters is fixed at a named cause with proof of teeth, or honestly deferred with a named blocker.
</success_criteria>

## Artifacts this phase produces

**Changed files**
- `examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs` — SEED-05, SEED-02/04, WALK-04, D-05, D-13 describe blocks fixed at cause, boundary and ordering semantics made explicit, manifest-declared values read from `Demo.Manifest`.
- Zero or more of `demo/seed.ex`, `demo/seed/retention_tail.ex`, `demo/seed/retention_runs.ex`, `demo/seed/temporal.ex`, `demo/seed/personas.ex`, `demo/reset.ex` — changed only where seeded content is the attributed cause.
- `.planning/audits/198-round4-demo-seed.md` — extended with `## Red-then-green teeth proof (198-24)` and `## Measured after-count (198-24)`.
- `.planning/phases/198-green-bringup/deferred-items.md` — a dated entry only if a residual failure is honestly deferred.

**Explicitly not produced**
- No change to `.github/workflows/ci.yml`, `mix.exs`, `playwright.config.ts`, `.github/rulesets/main.json`, `CONTRIBUTING.md`, or `.planning/REQUIREMENTS.md`.

<output>
Create `.planning/phases/198-green-bringup/198-24-SUMMARY.md` when done.
</output>
