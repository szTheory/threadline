---
phase: 198-green-bringup
plan: 49
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audits/198-automation-policy.json
  - bin/verify-phase198-evidence
  - test/threadline/phase198_automation_policy_test.exs
  - bin/observe-main-ci
  - test/threadline/main_ci_observer_contract_test.exs
autonomous: true
gap_closure: true
requirements: [GREEN-03, GREEN-04, GREEN-07]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The evidence evaluator rejects unknown/missing keys, duplicate IDs, broken evidence joins, malformed values, and non-exhaustive prediction sets before emitting a passing result."
    - "Prediction output contains predicted, observed, intersection, extra, and missing sets in stable order, with target and composition scored separately."
    - "The exact-main observer executes only the declared `gh run list` and `gh run view ID --json jobs` read forms; every other fake or real command shape is rejected before invocation."
    - "Evaluator or observer correctness never promotes GREEN-07 while ancestry or exact-main success remains false per D-39."
  artifacts:
    - "bin/verify-phase198-evidence — strict schema/evidence-join evaluator"
    - "test/threadline/phase198_automation_policy_test.exs — mutation matrix for every load-bearing policy field"
    - "bin/observe-main-ci — declared read-only command dispatcher"
    - "test/threadline/main_ci_observer_contract_test.exs — argv-capturing allowlist and negative command fixtures"
  key_links:
    - "policy schema -> evidence paths/IDs -> derived values -> stable complete result sets"
    - "validated observer arguments -> exact allowed gh argv -> selected run/jobs -> non-success/success state"
  prohibitions:
    - {statement: "A successful evaluator run must not be represented as successful exact-main CI.", status: unverified, flagged: true}
    - {statement: "The observer must not expose dispatch, rerun, cancel, workflow-run, API write, or arbitrary pass-through verbs.", status: unverified, flagged: true}
---

<objective>
Close the two high-severity Plan-44 evidence-automation threats and its medium prediction-accounting gap.

Purpose: the Round-10 verifier must be able to trust the new evaluator and observer as falsifiers, not merely as happy-path scripts.

Output: strict policy validation/evidence joins, complete prediction set output, and an executable read-only `gh` argv allowlist.

<assumption_delta_decision>Primary noun: evidence subject identity. Decision: promote — evidence IDs and exact run SHA are the validated primary keys; copied prose remains descriptive and cannot select a subject.</assumption_delta_decision>

### Artifacts this phase produces

- New policy fields: explicit evidence IDs/paths and per-collection unique IDs where absent.
- New evaluator result field: `prediction.observed`, `prediction.predicted`, and `prediction.intersection` alongside existing `extra` and `missing`.
- New observer-internal allowlist: two exact read-only command templates; no public product API.
- No dependency, schema, UI, or external mutation.

### Spec-less probe accounting

- GREEN-03 empty/encoding edges and GREEN-04/07 boundary/adjacency/empty/encoding/ordering/precision/concurrency edges are resolved with strict parsing, duplicate/tie fixtures, stable sorting, and explicit non-success states.
- GREEN-07 remains Pending by D-39; the plan verifies observation integrity only and does not dismiss its unresolved outcome.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-44-PLAN.md
@.planning/phases/198-green-bringup/198-44-SUMMARY.md
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-39-DECISION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the Phase-198 policy evaluator strict and evidence-joined</name>
  <files>.planning/audits/198-automation-policy.json, bin/verify-phase198-evidence, test/threadline/phase198_automation_policy_test.exs</files>
  <read_first>
    - .planning/audits/198-automation-policy.json — current schema and evidence-bearing values
    - bin/verify-phase198-evidence — current selected-field jq predicate and incomplete prediction result
    - test/threadline/phase198_automation_policy_test.exs — two existing negative mutations
    - .planning/phases/198-green-bringup/198-SECURITY.md — T-198-44-02 and T-198-44-03 required controls
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — source IDs/sets the policy must join rather than narratively copy
  </read_first>
  <behavior>
    - Test 1: unknown/missing top-level or nested keys, wrong types, empty evidence, duplicate collection IDs, and dangling evidence references fail
    - Test 2: equality-at-timeout boundary passes and one-second overrun fails; malformed/negative/non-integral values fail
    - Test 3: predicted/observed duplicates fail and stable predicted/observed/intersection/extra/missing sets are all emitted
    - Test 4: non-comparable populations abstain while equal population IDs compute the defined comparison
  </behavior>
  <action>Version the policy schema if its shape changes and validate exact allowed keys recursively before any derivation. Give every measurement, timeout, lane fingerprint, and attestation source a non-empty unique ID plus repository-relative evidence path; require referenced paths to exist and join the policy subject/run/population identifiers to the cited evidence content. Reject duplicates, dangling joins, null/NaN-like strings, negative values, and unexpected keys. Compute predictions with de-duplicated stable arrays and emit all five sets: predicted, observed, intersection, extra, missing; retain separate target hit/miss and composition exact/partial/miss fields. Expand mutation fixtures to flip every load-bearing field and prove each class fails. Do not parse narrative adjectives as policy.</action>
  <reversibility rating="reversible">The internal policy schema is versioned and has only this evaluator/test as consumers.</reversibility>
  <verify>
    <automated>mix test test/threadline/phase198_automation_policy_test.exs &amp;&amp; bin/verify-phase198-evidence --policy .planning/audits/198-automation-policy.json --format json | jq -e '.checks.failed == 0 and (.prediction | has("predicted") and has("observed") and has("intersection") and has("extra") and has("missing"))'</automated>
    <fails_when>Any unknown/duplicate/dangling/malformed mutation passes, evidence values can be changed independently of their cited subject, a timeout overrun passes, or any required prediction set is absent/unstable.</fails_when>
  </verify>
  <acceptance_criteria>
    - T-198-44-02's strict schema/evidence-join controls have one red fixture per failure class.
    - T-198-44-03's five named sets are emitted and asserted exactly.
    - Stable output still reports evaluator success separately from GREEN-07 disposition.
  </acceptance_criteria>
  <done>The Phase-198 evaluator is strict, joined to its evidence subjects, and complete enough to audit prediction claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce an exact read-only gh command allowlist</name>
  <files>bin/observe-main-ci, test/threadline/main_ci_observer_contract_test.exs</files>
  <read_first>
    - bin/observe-main-ci — current two direct GH_BIN calls
    - test/threadline/main_ci_observer_contract_test.exs — current fake-gh that accepts every non-list command as a jobs read
    - .planning/phases/198-green-bringup/198-SECURITY.md — T-198-44-05 required declaration/rejection contract
    - test/threadline/release_ci_gate_contract_test.exs — exact argv and authoritative-run ordering precedent
  </read_first>
  <behavior>
    - Test 1: only the exact run-list query and a numeric-ID run-view jobs query reach GH_BIN
    - Test 2: dispatch, workflow run, rerun, cancel, delete, api POST/PATCH/PUT/DELETE, unknown flags, and shell-hostile IDs are rejected
    - Test 3: fake-gh records argv as NUL-safe entries and proves no hidden third invocation occurred
    - Test 4: API errors/malformed JSON remain non-success and never fall through to another verb
  </behavior>
  <action>Centralize GH_BIN execution behind a local dispatcher that accepts exactly two symbolic operations and constructs fixed argv arrays: the existing canonical `run list` query and `run view <validated decimal databaseId> --json jobs`. Reject unknown operation names and validate database IDs before the subprocess. Keep SHA/workflow/branch/event filters fixed by source, not caller-provided flags. Rewrite the fake-gh fixture to record and compare each argument boundary, fail on any unrecognized argv, and provide explicit red cases for all write-side verbs named above. Do not add a generic `gh api` seam or arbitrary extra-argument forwarding.</action>
  <reversibility rating="reversible">The allowlist is an internal hardening of a read-only observer.</reversibility>
  <verify>
    <automated>mix test test/threadline/main_ci_observer_contract_test.exs</automated>
    <fails_when>The fake accepts an unrecognized command, a write-side verb is reachable, argv boundaries are inferred from a joined string, an invalid run ID reaches GH_BIN, or API failure can yield success.</fails_when>
  </verify>
  <acceptance_criteria>
    - T-198-44-05 has an explicit allowlist in source and rejection tests with teeth.
    - The existing exact-SHA/newest-before-status behavior remains unchanged.
    - No remote call is made during tests.
  </acceptance_criteria>
  <done>The exact-main observer is mechanically confined to its two necessary read operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| policy JSON/evidence files -> derived verdict | Versioned but mutable inputs could be partially validated or refer to a different subject. |
| observer -> GitHub CLI | A configurable executable boundary could gain unintended write-capable argv. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-44-02 | Tampering | policy evidence | high | mitigate | Exact recursive schema, unique IDs, evidence joins, and exhaustive mutation fixtures. |
| T-198-44-03 | Repudiation | prediction scoring | medium | mitigate | Emit and assert predicted/observed/intersection/extra/missing stable sets. |
| T-198-44-05 | Elevation of privilege | observer | high | mitigate | Two-operation allowlist, fixed argv construction, numeric ID validation, and rejecting fake-gh. |
| T-198-49-01 | Spoofing | evidence subject | high | mitigate | Join SHA/run/population identifiers to cited repository evidence before derivation. |
| T-198-49-02 | Denial of service | malformed API/policy input | medium | mitigate | Bounded inputs, fail-closed parsing, and no retry/write fallback. |
</threat_model>

<verification>
- Run both focused test files plus `git diff --check`.
- Run the real observer read-only against the exact live `origin/main` SHA and confirm its state remains an observation, not a requirement promotion.
</verification>

<success_criteria>
- Security re-audit can close T-198-44-02, T-198-44-03, and T-198-44-05 from executable controls.
- GREEN-07 remains Pending unless its own D-39-governed predicates change independently.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-49-SUMMARY.md` when done.</output>
