---
phase: 198-green-bringup
plan: 54
type: execute
wave: 5
depends_on: [198-53]
files_modified:
  - .planning/audits/198-round11-ref-disposition.md
  - .planning/audits/198-round11-ref-disposition.json
autonomous: false
gap_closure: true
requirements: [GREEN-12]
estimate:
  tokens: 5000
  raw_tokens: 5000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A human maintainer sees the complete immutable round-11 subject set and explicitly chooses retire, preserve, or abort before any branch, pull-request, or tag mutation can occur."
    - "The verbatim response, timestamp, inventory digest, and exact preservation subjects are stored in both joined artifacts and revalidated against live state."
    - "Only `retire` grants Plan 55 authority; `preserve`, `abort`, silence, auto-advance, and any other response leave every external object untouched and GREEN-12 Pending."
  artifacts:
    - ".planning/audits/198-round11-ref-disposition.md — verbatim maintainer decision and human-readable consequence"
    - ".planning/audits/198-round11-ref-disposition.json — digest-bound decision payload over the exact complete subject set"
  key_links:
    - "complete round-11 inventory digest -> blocking-human decision -> validator authority verdict"
  assumptions:
    - {statement: "The complete ref universe may drift after Plan 53; the decision validator must re-read it and refuse to bind a stale inventory rather than treating the checkpoint as timeless authority.", status: unverified, flagged: true}
  prohibitions:
    - {statement: "Auto-advance, a recommendation, or the earlier Plan-51 response must never select an option at this checkpoint.", status: unverified, flagged: true}
    - {statement: "This checkpoint must not mutate any branch, pull request, tag, main ref, ruleset, or protection setting.", status: unverified, flagged: true}
---

<objective>
Obtain or decline exact human authority over the complete round-11 stale-ref subject set.

Purpose: branch/PR retirement changes hosted state and cannot be inferred. The complete inventory is presented as a one-way trust decision under a blocking-human gate.

Output: a verbatim, digest-bound `retire`, `preserve`, or `abort` record and no external mutation.

<assumption_delta_decision>Primary noun: preservation subject. Decision: no-change — Plan 53 already promoted distinct side/SHA objects, and this checkpoint binds exactly that representation.</assumption_delta_decision>

### Artifacts this phase produces

- A `decision` object in `.planning/audits/198-round11-ref-disposition.json` containing option, verbatim response, UTC timestamp, inventory digest, and exact subjects.
- A matching Markdown decision section and join marker in `.planning/audits/198-round11-ref-disposition.md`.
- No branch, pull-request, tag, main, ruleset, protection, workflow, schema, or dependency mutation.

### Spec-less probe accounting

- GREEN-12's unresolved authority/ordering edge is explicit: no write-side task becomes reachable until this human decision is recorded and live-revalidated.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-53-SUMMARY.md
@.planning/audits/198-round11-ref-disposition.md
@.planning/audits/198-round11-ref-disposition.json
@.planning/phases/198-green-bringup/198-51-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide the complete Phase-198 ref disposition</name>
  <files>.planning/audits/198-round11-ref-disposition.md, .planning/audits/198-round11-ref-disposition.json</files>
  <read_first>
    - .planning/audits/198-round11-ref-disposition.md — complete ref/object/PR table, recommendation rationale, restore paths, stable controls, and Plan-52 supersession
    - .planning/audits/198-round11-ref-disposition.json — exact machine identities and inventory digest to bind
    - .planning/phases/198-green-bringup/198-53-SUMMARY.md — passing live inventory proof and any execution deviations
    - .planning/phases/198-green-bringup/198-51-SUMMARY.md — prior abort that grants no current authority
    - bin/verify-phase198-ref-disposition — decision and authority validation semantics
  </read_first>
  <decision>Choose whether the exact complete round-11 Phase-198 preservation subjects may be archived and their stale branch/PR handles retired.</decision>
  <context>The live namespace exceeded Plan 51's exact-three scope. `retire` authorizes only the newly displayed digest-bound subjects and Plan 55's preservation-first sequence. `preserve` leaves them intentionally present and records why. `abort` grants nothing. Any namespace/control drift requires a fresh inventory and decision.</context>
  <options>
    <option id="retire">
      <name>Retire all exact subjects</name>
      <pros>Allows GREEN-12 closure after every distinct tip has verified local and remote annotated archive tags and register rows.</pros>
      <cons>Closes associated stale PRs and deletes the exact listed local/remote branch refs; restoration is possible but coordinated.</cons>
    </option>
    <option id="preserve">
      <name>Preserve selected or all subjects</name>
      <pros>Keeps named branch/PR handles live and records the maintainer rationale without discarding anything.</pros>
      <cons>GREEN-12 stays Pending; the response must name the subjects to preserve and why, and Plan 55 remains blocked.</cons>
    </option>
    <option id="abort">
      <name>Abort without authority</name>
      <pros>Makes no external change and records a clear stop.</pros>
      <cons>The stale-ref gap remains open and Plan 55 cannot execute.</cons>
    </option>
  </options>
  <action>Immediately before presenting the options, run the live `inventory` stage and stop if any namespace subject or stable control drifted. Present the complete Markdown table, distinct-tip archive names, PR associations, recommendations, restore commands, and consequences. Accept only the literal `retire`, `abort`, or a `preserve ...` response that names its subjects/rationale. Record the response verbatim in joined Markdown/JSON with UTC timestamp, current inventory digest, and the exact complete subject array. Run `decision` validation afterward. Do not execute any write-side Git, GitHub, ruleset, or protection command.</action>
  <reversibility rating="one-way">The decision authorizes later hosted branch/PR retirement; undo requires reconstructing refs and workflow state from archives.</reversibility>
  <verify>
    <automated>bin/verify-phase198-ref-disposition decision --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md</automated>
    <fails_when>The live namespace or stable controls drifted; the response is absent, normalized rather than verbatim, not one of the allowed forms, bound to a stale digest, missing any exact subject, or accompanied by an execution field or mutation receipt.</fails_when>
  </verify>
  <acceptance_criteria>
    - The human sees and decides the complete current scope under a gate that auto-mode cannot bypass.
    - The selected option and exact consequence are durable and machine-joined.
    - No external state changes during this plan.
  </acceptance_criteria>
  <resume-signal>Reply `retire`, `abort`, or `preserve &lt;subjects and rationale&gt;`.</resume-signal>
  <done>A fresh, verbatim human disposition is recorded over the exact round-11 subject set, with external state unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer response -> tracked authority | Free text becomes a narrowly scoped machine decision. |
| Mutable live state -> immutable decision digest | Current subjects and controls must still equal the reviewed inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-54-01 | Spoofing | maintainer decision | high | mitigate | `blocking-human` gate plus exact literal response; auto-advance and prior responses are rejected. |
| T-198-54-02 | Tampering | decision subject set | high | mitigate | Bind the verbatim response to the canonical inventory digest and exact complete subject array after a fresh live comparison. |
| T-198-54-03 | Repudiation | preserve/abort consequence | medium | mitigate | Persist UTC timestamp, verbatim response, subject identities, and explicit no-authority semantics. |
| T-198-54-04 | Elevation of Privilege | checkpoint execution | high | mitigate | Decision stage forbids execution fields/receipts and performs no external mutation. |
</threat_model>

<verification>
- The live inventory still matches before prompting.
- The decision validator passes and execution remains null with zero receipts.
- Git/GitHub controls are unchanged.
</verification>

<success_criteria>
- Exactly one allowed verbatim decision is bound to the complete subject set.
- Only `retire` can satisfy Plan 55's precondition.
- No mutation occurs in this decision plan.
</success_criteria>

<output>
Create `.planning/phases/198-green-bringup/198-54-SUMMARY.md` when done.
</output>
