---
status: diagnosed
phase: 198-green-bringup
source: 198-01-SUMMARY.md, 198-02-SUMMARY.md, 198-03-SUMMARY.md, 198-04-SUMMARY.md, 198-05-SUMMARY.md, 198-06-SUMMARY.md, 198-07-SUMMARY.md, 198-08-SUMMARY.md, 198-09-SUMMARY.md, 198-10-SUMMARY.md, 198-11-SUMMARY.md, 198-12-SUMMARY.md, 198-13-SUMMARY.md, 198-14-SUMMARY.md, 198-15-SUMMARY.md, 198-16-SUMMARY.md, 198-17-SUMMARY.md, 198-18-SUMMARY.md, 198-19-SUMMARY.md, 198-20-SUMMARY.md, 198-21-SUMMARY.md, 198-22-SUMMARY.md, 198-23-SUMMARY.md, 198-24-SUMMARY.md, 198-25-SUMMARY.md, 198-26-SUMMARY.md, 198-27-SUMMARY.md, 198-28-SUMMARY.md, 198-29-SUMMARY.md, 198-30-SUMMARY.md, 198-31-SUMMARY.md, 198-32-SUMMARY.md, 198-33-SUMMARY.md, 198-34-SUMMARY.md, 198-35-SUMMARY.md, 198-36-SUMMARY.md, 198-37-SUMMARY.md, 198-38-SUMMARY.md, 198-39-SUMMARY.md, 198-40-SUMMARY.md, 198-41-SUMMARY.md, 198-42-SUMMARY.md
started: 2026-09-09T00:08:29.899Z
updated: 2026-09-09T00:24:00.000Z
---

## Current Test

[testing complete — remaining checkpoints converted to automation gaps]

## Tests

### 1. [198-01 D4] The sizing implication drawn for Phase 201 Tier 2 — that copy work is cheap to gate but unprotected against layout consequences
expected: |
  The sizing implication drawn for Phase 201 Tier 2 — that copy work is cheap to gate but unprotected against layout consequences
rationale: |
  The measurement is proven; the estimate drawn from it is a planning judgment the maintainer must ratify when Phase 201 is planned.
reason: human_judgment
requirement: GREEN-03
result: issue
reported: |
  integration/e2e/smoke.... shift left automate the world devops mindset... goal 0 s human verifciation/uat required
severity: major

### 2. [198-03 D7] Classification of the min-lane failure as a genuine code/test failure rather than runner-image or Playwright version drift
expected: |
  Classification of the min-lane failure as a genuine code/test failure rather than runner-image or Playwright version drift
rationale: |
  The differential evidence (min and current failing identically) is strong and mechanical, and it is what rules out toolchain drift. But 'is the Elixir 1.15 floor promise sound?' is ultimately a maintainer's judgment about what the 83 shared failures mean, and those failures are the milestone's pre-existing red baseline that Plans 04-06 have not yet triaged. A verifier should read the differential rather than accept the headline.
reason: human_judgment
verification: |
  - [pass] current lane (job 98661003731) reported the IDENTICAL '1382 tests, 83 failures, 1 excluded' — the min lane contributes zero additional failures
result: issue
reported: |
  Replace maintainer classification with deterministic differential evidence.
severity: major

### 3. [198-04 D8] The corrected root-cause diagnosis of the red baseline: 79 test-side defects assuming an unprefixed search_path, not a stale database
expected: |
  The corrected root-cause diagnosis of the red baseline: 79 test-side defects assuming an unprefixed search_path, not a stale database
rationale: |
  The mechanism is proven mechanically and the arithmetic reconciles exactly (79+1+1+1 = 82 local, 83 CI). What needs a maintainer's judgment is the remediation choice for the 79 — per-call-site prefix, StorageSchemaCase.repo_opts/1, or a prefixed repo in DataCase — since each trades explicitness against blast radius, and one of them (DataCase) cannot fix the raw Repo.query! SQL those same tests use. A verifier should read the evidence rather than accept the headline.
reason: human_judgment
verification: |
  - [pass] Fresh DB 4 -> 82 failures after recreate; psql proves `select count(*) from audit_changes` errors by default and succeeds under `set search_path to public,threadline`; no role-level pg_db_role_setting entry exists
result: issue
reported: |
  Replace maintainer diagnosis with a reproducible integration test and machine-readable classification.
severity: major

### 4. [198-05 D2] A systemically broken browser suite aborts after 5 failures on CI while keeping trace retention and the boot log
expected: |
  A systemically broken browser suite aborts after 5 failures on CI while keeping trace retention and the boot log
rationale: |
  The abort-after-5 path itself has NOT been exercised on CI. The browser lane currently dies in `mix demo.seed` with `relation \"audit_transactions\" does not exist` — the pre-existing red baseline that plans 04/06 own — which is BEFORE Playwright starts. The mechanism is wired and its ordering is proven; its actual firing is unobserved and a verifier should not read the upload step's execution as proof that maxFailures works.
reason: human_judgment
requirement: GREEN-06
verification: |
  - [pass] playwright.config.ts `maxFailures: process.env.CI ? 5 : 0`; grep -c 'retain-on-failure' == 1, unchanged from before the task
  - [pass] run 33115615482 job 98669374595: the `if: failure()` upload step EXECUTED after the Playwright step (log line 20:56:53)
result: issue
reported: |
  Exercise the failure path in an automated smoke test; configuration inspection alone is insufficient.
severity: major

### 5. [198-05 D5] A new non-required job id `verify-example-browser-full` runs the whole project set on push-to-main and nightly
expected: |
  A new non-required job id `verify-example-browser-full` runs the whole project set on push-to-main and nightly
rationale: |
  The workflow has never executed — it triggers on push-to-main and a nightly schedule, and this phase deliberately has not pushed main. Its YAML shape is asserted; its runtime behaviour (including the gh issue create-or-update step, which needs a real failure and a real token) is entirely unobserved.
reason: human_judgment
requirement: GREEN-07
verification: |
  - [pass] yq over browser-full.yml: job id verify-example-browser-full, on.push.branches == [main], on.schedule cron '0 5 * * *', workflow_dispatch present, no per-project flags on the run step
  - [pass] yq '.jobs[\"ci-required\"].needs | contains([\"verify-example-browser-full\"])' == false
result: issue
reported: |
  Prove scheduling, full-project execution, and issue deduplication without waiting for a human-observed nightly run.
severity: major

### 6. [198-05 D7] Every job in ci.yml, release.yml and browser-full.yml carries a timeout-minutes bound
expected: |
  Every job in ci.yml, release.yml and browser-full.yml carries a timeout-minutes bound
rationale: |
  The VALUES are a judgment call a maintainer should review, and one of them is weaker than the others: 18 for the browser lane is a D-16 budget, not a multiple of an observed green p95, because that lane has no observed green run (its only two recorded durations are 1h33m38s broken and ~2m fast-failing). No bound has been observed actually firing on a hang.
reason: human_judgment
requirement: GREEN-06
verification: |
  - [pass] yq job-count vs bounded-count: ci.yml 13/13, release.yml 7/7, browser-full.yml 1/1; grep -c 'timeout-minutes' ci.yml == 14 (13 job-level + 1 step-level)
  - [pass] verify-example-browser job bound 18, its Playwright step bound 14 (strictly less); verify-capture 35, verify-test 20, ci-required 5, browser-full 50; release.yml gate-ci-green 45 > its 60x30s = 30-minute poll window
  - [pass] run 33115615482 executed with the bounds in place — no workflow parse error, all 14 jobs scheduled
result: issue
reported: |
  Derive and enforce timeout budgets from recorded runtime evidence rather than maintainer taste.
severity: major

### 7. [198-06 D3] The irreversible publish step sits behind a production-hex Environment carrying a required-reviewer rule, and Hex auth is one swappable block
expected: |
  The irreversible publish step sits behind a production-hex Environment carrying a required-reviewer rule, and Hex auth is one swappable block
rationale: |
  The gate's real behaviour — a release actually pausing for a human click — can only be observed on a live release run, which requires the push (198-07) and a real 0.10.0 cut (Phase 202). The configuration is proven; the runtime pause is not.
reason: human_judgment
verification: |
  - [pass] yq '.jobs[\"publish-hex\"].environment' .github/workflows/release.yml -> production-hex
  - [pass] gh api repos/szTheory/threadline/environments/production-hex --jq '.protection_rules[].type' -> required_reviewers
result: issue
reported: |
  Verify the publish gate and swappable authentication boundary with a non-publishing release smoke path.
severity: major

### 8. [198-06 D4] Flake Detection classifies broken vs flaky vs unknown, carries timeout-minutes, and creates-or-updates a single deduplicated issue
expected: |
  Flake Detection classifies broken vs flaky vs unknown, carries timeout-minutes, and creates-or-updates a single deduplicated issue
rationale: |
  The live two-dispatch dedup demonstration (two failing runs writing to ONE issue) could not be run: workflow_dispatch requires the workflow to exist on the remote default branch, and this plan is forbidden to push. The classifier is proven; the gh issue create-or-update path is unexercised. Recorded in WINDOWS.md as unrun-verify.
reason: human_judgment
requirement: GREEN-11
verification: |
  - [pass] Classifier logic replayed against four real captured logs: pass(exit0,4 iters), broken(exit2,1 iter), flaky(exit2,3 iters), unknown(exit2,0 iters) — all correct
  - [pass] yq: every job carries timeout-minutes; label ci-flake distinct from ci-browser-full; job id verify-flake unrenamed
result: issue
reported: |
  Exercise create-then-update deduplication deterministically without requiring two live dispatches.
severity: major

### 9. [198-07 D3] The post-push run on main concludes success
expected: |
  The post-push run on main concludes success
rationale: |
  NOT MET and deliberately not manufactured. The cause is the 79 test-side search_path defects 198-04 filed as real bugs outside its declared scope, plus 1 unowned CONTRIBUTING.md List 1 drift. Nothing was skipped, excluded, tagged out, downgraded to continue-on-error, or removed from the required-check definition to change this number. A maintainer must decide who owns the 79, since retiring them is also the condition that unlocks the merge gate.
reason: human_judgment
requirement: GREEN-07
verification: |
  - [fail] gh run view 33138291361 --json conclusion -> failure; 8 of 14 jobs red including both verify-test lanes
result: issue
reported: |
  Replace human confirmation with an exact-SHA main-run attestation that reports pass, fail, incomplete, or not observed without substituting evidence.
severity: major

### 10. [198-12 D5] mix ci.all's remaining verify.example failure is a documented, out-of-scope pre-existing gap, not silently ignored
expected: |
  mix ci.all's remaining verify.example failure is a documented, out-of-scope pre-existing gap, not silently ignored
rationale: |
  This is a judgment call that the examples/threadline_phoenix demo-seed content-drift failure is genuinely unrelated to GREEN-04's storage_schema defect class and out of this plan's declared files_modified scope -- a human/future-plan should confirm that framing before closing the phase.
reason: human_judgment
result: issue
reported: |
  Reconcile this stale judgment against the now-green CI evidence and enforce the classification mechanically.
severity: major

### 11. [198-18 D3] GREEN-04 and GREEN-07 set strictly from the measured run, both REQUIREMENTS.md views agreeing row for row, run ID cited in both
expected: |
  GREEN-04 and GREEN-07 set strictly from the measured run, both REQUIREMENTS.md views agreeing row for row, run ID cited in both
rationale: |
  The plan's Task 3 is itself a checkpoint:human-verify with gate=blocking-human — the maintainer must confirm the recorded statuses match the measured run and that neither requirement was marked Complete on local-only evidence, per this run's checkpoint protocol (blocking-human is never auto-approved).
reason: human_judgment
requirement: GREEN-07
result: issue
reported: |
  Replace the blocking human checkpoint with a consistency contract over attestations and both requirements views.
severity: major

### 12. [198-27 D3] "New discovery (operator-accessibility.spec.ts:655:3,
expected: |
  "New discovery (operator-accessibility.spec.ts:655:3,
rationale: |
  "Root cause of the new discovery is not established — a human
reason: human_judgment
requirement: GREEN-07
result: issue
reported: |
  Establish and fix the E2E root cause, then lock it with a focused regression test.
severity: major

### 13. [198-29 D3] Honest verdict that this round MISSED its stated target (red needs: 3→1) and FALSIFIED its own pre-push prediction (2), recorded without softening or retro-editing
expected: |
  Honest verdict that this round MISSED its stated target (red needs: 3→1) and FALSIFIED its own pre-push prediction (2), recorded without softening or retro-editing
rationale: |
  Whether the record is genuinely unsoftened — rather than technically accurate but rhetorically hedged — is a judgment about candour that no automated check can make.
reason: human_judgment
result: issue
reported: |
  Compute prediction outcomes and target deltas from machine-readable inputs; do not require subjective candour review.
severity: major

### 14. [198-31 D4] Local unbounded Playwright measurement recorded beside round 4's CI-capped figures, with the non-nested-population caveat stated
expected: |
  Local unbounded Playwright measurement recorded beside round 4's CI-capped figures, with the non-nested-population caveat stated
rationale: |
  This is a measurement/documentation deliverable, not a pass/fail assertion — recorded below and in the audit doc for human review, not machine-verifiable as pass/fail.
reason: human_judgment
result: issue
reported: |
  Emit and validate population metadata so capped and unbounded measurements cannot be compared incorrectly.
severity: major

### 15. [198-37 D4] Honest scoring of the one composition-level partial miss (third un-predicted Playwright failure row), not softened into a clean hit
expected: |
  Honest scoring of the one composition-level partial miss (third un-predicted Playwright failure row), not softened into a clean hit
rationale: |
  Whether the record genuinely distinguishes conclusion-level accuracy from composition-level accuracy — rather than technically noting the miss while rhetorically minimizing it — is a judgment about candour no automated check can make.
reason: human_judgment
result: issue
reported: |
  Score conclusion-level and composition-level accuracy separately with a deterministic rubric.
severity: major

### 16. [198-01 D1] Run 28214113903's failing logs are readable from the repository, with run metadata, a per-job conclusion table, and the D-36 staleness caveat attached
expected: |
  Run 28214113903's failing logs are readable from the repository, with run metadata, a per-job conclusion table, and the D-36 staleness caveat attached
requirement: GREEN-01
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test -f .planning/audits/198-ci-run-28214113903-logs.md && grep -q '28214113903' && grep -qi 'predates'
  - [pass] gh run view 28214113903 --log-failed (exit 0, 8856 lines, 1218469 bytes captured verbatim)

### 17. [198-01 D2] A per-check Credo histogram and a per-file concentration table, produced from a full-default config held outside the repo, with .credo.exs provably unmodified
expected: |
  A per-check Credo histogram and a per-file concentration table, produced from a full-default config held outside the repo, with .credo.exs provably unmodified
requirement: GREEN-02
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] git diff --exit-code .credo.exs (exit 0) + .credo.exs content hash a5f0e0df... identical before and after
  - [pass] jq -e '.issues | length > 0' .planning/audits/198-credo-full-default.json (377 issues vs baseline 0 — proves --config-file was honored)

### 18. [198-01 D3] An executed answer to whether MechanicalChecker is sensitive to rendered text content and text width, with no scorecard touched
expected: |
  An executed answer to whether MechanicalChecker is sensitive to rendered text content and text width, with no scorecard touched
requirement: GREEN-03
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] mix run /tmp/198-work/probe.exs — control == text-content : true; control == text-width : true; token control {:error, 2}; MODE-B control {:error, 1}
  - [pass] git status --porcelain .planning/scorecards/ (empty output)

### 19. [198-02 D1] Full-history credential scan across 2381 commits with two independent engines plus an untracked-tree pass and an ever-added-file sweep, raw reports committed
expected: |
  Full-history credential scan across 2381 commits with two independent engines plus an untracked-tree pass and an ever-added-file sweep, raw reports committed
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test -f .planning/audits/198-gitleaks-history.json && test -f .planning/audits/198-gitleaks-worktree.json && test -f .planning/audits/198-trufflehog-verified.json && grep -q 'Scans run' .planning/audits/198-credential-audit.md

### 20. [198-02 D2] Every scanner finding carries a Class A/B/C disposition row with a stated reason, and the phase carries a machine-readable push verdict
expected: |
  Every scanner finding carries a Class A/B/C disposition row with a stated reason, and the phase carries a machine-readable push verdict
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] grep -qE '^## VERDICT: (PROCEED|ABORT)' .planning/audits/198-credential-audit.md && grep -q '^## Finding register' .planning/audits/198-credential-audit.md
  - [pass] grep -riE 'sk-ant-|ghp_|github_pat_|AKIA|BEGIN [A-Z ]*PRIVATE KEY' .planning/audits/198-credential-audit.md returns no match

### 21. [198-02 D3] Classification of F-002/F-003 (example app dev/test secret_key_base) as Class C rather than Class A
expected: |
  Classification of F-002/F-003 (example app dev/test secret_key_base) as Class C rather than Class A
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/phase198_decision_attestation_test.exs

### 22. [198-02 D4] The one-way publication of `.planning/` history authorized by the maintainer at a recorded checkpoint (D-30)
expected: |
  The one-way publication of `.planning/` history authorized by the maintainer at a recorded checkpoint (D-30)
requirement: GREEN-07
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] test/threadline/phase198_decision_attestation_test.exs

### 23. [198-02 D5] GitHub secret scanning and push protection live on szTheory/threadline before any push of these commits
expected: |
  GitHub secret scanning and push protection live on szTheory/threadline before any push of these commits
requirement: GREEN-07
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] gh api repos/:owner/:repo --jq .security_and_analysis reports secret_scanning=enabled and secret_scanning_push_protection=enabled
  - [pass] grep -q '^## Push protection enabled' .planning/audits/198-credential-audit.md && grep -q '\"secret_scanning_push_protection\"' .planning/audits/198-credential-audit.md

### 24. [198-02 D6] This plan pushed nothing — the gate is recorded but no commit reached origin on any ref
expected: |
  This plan pushed nothing — the gate is recorded but no commit reached origin on any ref
requirement: GREEN-07
result: pass
source: automated
coverage_id: D6
verification: |
  - [pass] git log origin/main..main is non-empty (606 commits); git rev-parse origin/main unchanged at 67998e0b

### 25. [198-03 D1] `ci-required` job wired into ci.yml with if: always(), timeout-minutes, alls-green, and needs: exactly the twelve existing verify-* job ids
expected: |
  `ci-required` job wired into ci.yml with if: always(), timeout-minutes, alls-green, and needs: exactly the twelve existing verify-* job ids
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] yq '.jobs[\"ci-required\"].needs | length' .github/workflows/ci.yml == 12; .name == 'CI required'; .if == 'always()'; .[\"timeout-minutes\"] == 5
  - [pass] grep -c 'contains(needs' .github/workflows/ci.yml == 0 (no hand-rolled result-string gate)
  - [pass] yq '.on.push | has(\"paths\")' and '.on.pull_request | has(\"paths\")' both false (D-10/D-20)

### 26. [198-03 D2] No existing ci.yml job id renamed or removed — the stated CLAUDE.md id contract held
expected: |
  No existing ci.yml job id renamed or removed — the stated CLAUDE.md id contract held
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] git diff HEAD~1 -- .github/workflows/ci.yml | grep -E '^-  [a-z-]+:$' returns empty; yq '.jobs | keys | length' == 13 (12 + ci-required)

### 27. [198-03 D3] A real pull request against main, from a real push of the local commit backlog, carrying a real CI run
expected: |
  A real pull request against main, from a real push of the local commit backlog, carrying a real CI run
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] gh pr view 27 --json number,baseRefName,headRefName,state => {27, main, ci/v1_41-green-bringup, OPEN}
  - [pass] git push origin HEAD:refs/heads/ci/v1_41-green-bringup succeeded; push protection did not block (no D-29 Class A/B signal)

### 28. [198-03 D4] `CI required` emitted by a real run on the pushed HEAD, and correctly RED on failing lanes
expected: |
  `CI required` emitted by a real run on the pushed HEAD, and correctly RED on failing lanes
requirement: GREEN-07
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] gh api repos/:owner/:repo/commits/009edbd9/check-runs => 2 check runs named exactly 'CI required', both conclusion=failure, check-run id 98663112338

### 29. [198-03 D5] GREEN-08's 'verified after the matrix has reported once' satisfied by a committed observation of the emitted names
expected: |
  GREEN-08's 'verified after the matrix has reported once' satisfied by a committed observation of the emitted names
requirement: GREEN-08
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] check-runs API on 009edbd9 emitted 'Run test suite (min)' and 'Run test suite (current)' verbatim; recorded in .planning/audits/198-matrix-name-observation.md with a stated ## Verdict

### 30. [198-03 D6] Min lane rehearsed against origin before it can block a contributor PR (D-21), with its conclusion and cause classification recorded
expected: |
  Min lane rehearsed against origin before it can block a contributor PR (D-21), with its conclusion and cause classification recorded
requirement: GREEN-08
result: pass
source: automated
coverage_id: D6
verification: |
  - [pass] workflow_dispatch run 33113172280; job 98661003718 'Run test suite (min)' conclusion=failure, 1382 tests / 83 failures; grep -c 'continue-on-error' ci.yml == 0

### 31. [198-03 D8] Whether the aggregate gate goes GREEN when all twelve needed jobs succeed
expected: |
  Whether the aggregate gate goes GREEN when all twelve needed jobs succeed
result: pass
source: automated
coverage_id: D8
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33353447804.json

### 32. [198-04 D1] Stale-schema tripwire in test/test_helper.exs raises once, naming the migration and `mix test.reset`, and is silent on a correctly migrated database
expected: |
  Stale-schema tripwire in test/test_helper.exs raises once, naming the migration and `mix test.reset`, and is silent on a correctly migrated database
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] Created scratch DB threadline_tripwire_probe with a deliberate public.audit_transactions; suite raised the expected message before any test ran (quoted in this SUMMARY)
  - [pass] mix test.reset on a correctly migrated DB — tripwire silent; the legitimate `audit` schema from prepare_dual_storage!/1 did not false-positive
  - [pass] grep -c 'information_schema.tables' test/test_helper.exs == 1; query contains table_schema = 'public'; message contains 20260607000000_threadline_storage_schema_default and `mix test.reset`

### 33. [198-04 D2] mix test.reset and mix test.setup exist and recreate the test database before running the suite
expected: |
  mix test.reset and mix test.setup exist and recreate the test database before running the suite
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] mix test.reset executes ecto.drop -r Threadline.Test.Repo then the suite; both aliases registered in cli/0 preferred_envs (:test)

### 34. [198-04 D3] D-02 not violated — no repo-level search_path, no default repo prefix
expected: |
  D-02 not violated — no repo-level search_path, no default repo prefix
requirement: GREEN-04
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] grep -c search_path config/test.exs == 0; git diff --exit-code config/test.exs clean; mix test test/threadline/storage_schema_prefix_contract_test.exs => 5 tests, 0 failures

### 35. [198-04 D4] Zero-skips cap asserted mechanically, and not self-invalidating
expected: |
  Zero-skips cap asserted mechanically, and not self-invalidating
requirement: GREEN-04
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] test/threadline/zero_skips_contract_test.exs — 2 tests, 0 failures; both halves demonstrated red (scratch skip tag; scratch exclude tag) then green
  - [pass] grep -c 'moduletag' test/threadline/zero_skips_contract_test.exs == 0 — needles built by concatenation

### 36. [198-04 D5] 198-TRIAGE.md dispositions every former failure with the four binding columns
expected: |
  198-TRIAGE.md dispositions every former failure with the four binding columns
requirement: GREEN-04
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] .planning/phases/198-green-bringup/198-TRIAGE.md — 19 file rows / 82 baseline failures, columns test|category|disposition|evidence, ordered by test file path

### 37. [198-04 D6] Obsolete charter test deleted with a recorded admission that coverage was dropped with no successor
expected: |
  Obsolete charter test deleted with a recorded admission that coverage was dropped with no successor
requirement: GREEN-04
result: pass
source: automated
coverage_id: D6
verification: |
  - [pass] git rm test/threadline/v1_23_charter_doc_contract_test.exs (commit ba7a7140); removed from verify.doc_contract alias; ci_topology_contract_test.exs assert -> refute for that path

### 38. [198-04 D7] @ui_form_policy is exhaustive and self-declaring — a page gaining a form, or a new page declaring nothing, fails in the same diff
expected: |
  @ui_form_policy is exhaustive and self-declaring — a page gaining a form, or a new page declaring nothing, fails in the same diff
requirement: GREEN-05
result: pass
source: automated
coverage_id: D7
verification: |
  - [pass] test/threadline/operator_surface/ui_form_policy_contract_test.exs — 1 test, 0 failures; glob non-emptiness asserted
  - [pass] Red demo 1: form control appended to actor_live.ex (:formless) => guard red naming file and [\"<input\"]. Red demo 2: declaration stripped from evidence_live.ex => guard red naming the missing declaration. Both restored, green.
  - [pass] grep -L ui_form_policy lib/threadline/operator_surface/live/*_live.ex returns nothing (all 11 declare); row_history_component.ex excluded by construction

### 39. [198-04 D9] Whether the suite reaches zero failures on a fresh database
expected: |
  Whether the suite reaches zero failures on a fresh database
result: pass
source: automated
coverage_id: D9
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33344382035.json

### 40. [198-05 D1] The bare `chromium` Playwright project is deleted, provably with zero orphaned baselines and zero coverage loss
expected: |
  The bare `chromium` Playwright project is deleted, provably with zero orphaned baselines and zero coverage loss
requirement: GREEN-06
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] find examples/threadline_phoenix/e2e/tests -name '*-chromium.png' | grep -v -e '-desktop-chromium.png' -e '-mobile-chromium.png' returned NOTHING, run BEFORE the deletion
  - [pass] grep -c 'name: \"chromium\"' playwright.config.ts == 0; desktop-chromium == 1; mobile-chromium == 1
  - [pass] npx playwright test --list: 549 tests before, 368 after (-181, -33%)

### 41. [198-05 D3] The e2e boot preflight refuses to start if the operator surface is not mounted
expected: |
  The e2e boot preflight refuses to start if the operator surface is not mounted
requirement: GREEN-06
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/e2e_preflight_contract_test.exs + examples/threadline_phoenix/e2e/run-e2e.sh
  - [pass] bash -n run-e2e.sh passes; `operator_surface_ready()` requests ${BASE_URL}/audit and `fail_with_log` exits 1 with tail -80 of the boot log

### 42. [198-05 D4] The pull-request browser job keeps its job id AND its human-readable name byte-identical, and stays inside the aggregate gate
expected: |
  The pull-request browser job keeps its job id AND its human-readable name byte-identical, and stays inside the aggregate gate
requirement: GREEN-07
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] git diff HEAD~1 -- .github/workflows/ci.yml | grep '^-.*Example app browser E2E' returned empty; yq '.jobs[\"ci-required\"].needs' still contains verify-example-browser and has length 12
  - [pass] run 33115615482 emitted a job named exactly `Example app browser E2E (Playwright)`

### 43. [198-05 D6] CONTRIBUTING.md states verbatim which Playwright projects run where, guarded by a contract test with demonstrated teeth
expected: |
  CONTRIBUTING.md states verbatim which Playwright projects run where, guarded by a contract test with demonstrated teeth
requirement: GREEN-07
result: pass
source: automated
coverage_id: D6
verification: |
  - [pass] mix test test/threadline/ci_coverage_doc_contract_test.exs — 2 tests, 0 failures
  - [pass] Teeth demo: removing the `desktop-chromium` row turned the test RED naming `desktop-chromium` exactly; row restored, green again
  - [pass] grep -c 'ci_coverage_doc_contract' mix.exs == 0 — not wired into any verify.* alias

### 44. [198-05 D8] The two matrix lanes no longer share one cache entry
expected: |
  The two matrix lanes no longer share one cache entry
requirement: GREEN-06
result: pass
source: automated
coverage_id: D8
verification: |
  - [pass] run 33115615482 job 98669374716 (min) logged key `ubuntu-22.04-otp26-elixir1.15-mix-deps-e0c08af6...`; job 98669374524 (current) logged `ubuntu-24.04-otp27-elixir1.17.3-mix-deps-e0c08af6...` — distinct prefixes, same lockfile hash
  - [pass] grep -c 'runner.os' .github/workflows/ci.yml == 0 (also 0 case-insensitively)
  - [pass] yq '.on.push | has(\"paths\")' and '.on.pull_request | has(\"paths\")' both false

### 45. [198-06 D1] The paid critic lane is structurally untriggerable from CI: ui-critic.yml deleted outright, no workflow references the API key
expected: |
  The paid critic lane is structurally untriggerable from CI: ui-critic.yml deleted outright, no workflow references the API key
requirement: GREEN-09
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test/threadline/ci_topology_contract_test.exs#no workflow references the paid critic API key (GREEN-09 resurrection guard)
  - [pass] grep -rl ANTHROPIC .github/ returns no output

### 46. [198-06 D2] Exactly one Hex publish path exists and it is the gated one; the legacy tag-triggered workflow is deleted
expected: |
  Exactly one Hex publish path exists and it is the gated one; the legacy tag-triggered workflow is deleted
requirement: GREEN-10
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] test/threadline/ci_topology_contract_test.exs#exactly one workflow invokes the Hex publish command (GREEN-10 resurrection guard)
  - [pass] mix verify.release (exit 0; 19 tests, 0 failures)

### 47. [198-06 D5] Every piece of unmerged work is preserved under a verified annotated archive tag with a recorded recommendation, before the repository was reduced to one worktree and one branch
expected: |
  Every piece of unmerged work is preserved under a verified annotated archive tag with a recorded recommendation, before the repository was reduced to one worktree and one branch
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] git worktree list -> 1 entry; git branch -> main only; both archive/* tags are annotated (cat-file -t = tag) and resolve to dd5b48be / 50374eb7 post-deletion
  - [pass] Both tag messages grep-verified to carry all ten D-31 elements (SHA, base, ancestry verification, diffstat, archive date, GREEN-12, recommendation, rationale, restore, ARCHIVE-REGISTER)

### 48. [198-07 D1] origin/main contains every local commit, with full history intact
expected: |
  origin/main contains every local commit, with full history intact
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] git rev-list --count origin/main..main -> 0; push reported 67998e0b..a97f527e (fast-forward, not squash or rebase)

### 49. [198-07 D2] The post-push run on main completes inside the 20-minute feedback budget
expected: |
  The post-push run on main completes inside the 20-minute feedback budget
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] gh run view 33138291361 --json startedAt,updatedAt -> 03:13:58Z..03:20:05Z = 6m07s, against a 20-min ceiling and 12-min target

### 50. [198-07 D4] Branch protection on main requires exactly one context, the string `CI required`, and that name has provably been emitted on a real run — both asserted by a committed script
expected: |
  Branch protection on main requires exactly one context, the string `CI required`, and that name has provably been emitted on a real run — both asserted by a committed script
requirement: GREEN-08
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] bash bin/verify-branch-protection exits 0 on live state: 'required contexts are exactly [CI required], that name has been emitted 1 time(s) on head a97f527e…, and no classic protection is stacking'
  - [pass] gh api /repos/szTheory/threadline/rules/branches/main -> single required_status_checks rule, context list [CI required]

### 51. [198-07 D5] The protection configuration is git-tracked and auditable as committed JSON rather than living only on GitHub's servers
expected: |
  The protection configuration is git-tracked and auditable as committed JSON rather than living only on GitHub's servers
requirement: GREEN-08
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] .github/rulesets/main.json committed (e34d19d9); applied live as ruleset 21702804, enforcement active, bypass_actors []

### 52. [198-07 D6] The up-to-date-branch requirement is OFF and administrator enforcement is ON (D-14)
expected: |
  The up-to-date-branch requirement is OFF and administrator enforcement is ON (D-14)
requirement: GREEN-08
result: pass
source: automated
coverage_id: D6
verification: |
  - [pass] strict_required_status_checks_policy false on the live ruleset; classic protection (which carried strict:true and enforce_admins:false) DELETED -> gh api branches/main/protection returns 'Branch not protected'; bypass_actors [] means no actor can walk past

### 53. [198-07 D7] Whether classic protection remains alongside the ruleset is an explicitly recorded decision, not an accident of migration
expected: |
  Whether classic protection remains alongside the ruleset is an explicitly recorded decision, not an accident of migration
requirement: GREEN-08
result: pass
source: automated
coverage_id: D7
verification: |
  - [pass] '## Classic protection disposition' in the migration artifact: DELETE, with the union-evaluation rationale; the verifier's third assertion block fails if classic protection exists at all, so the decision cannot rot silently

### 54. [198-07 D8] Archive tags exist on origin, so a laptop loss cannot destroy the only copy of real unmerged work
expected: |
  Archive tags exist on origin, so a laptop loss cannot destroy the only copy of real unmerged work
requirement: GREEN-12
result: pass
source: automated
coverage_id: D8
verification: |
  - [pass] git ls-remote --tags origin 'refs/tags/archive/*' lists both tags, dereferencing to dd5b48be / 50374eb7; both pushed by name, never --tags; no milestone tag published as a side effect

### 55. [198-07 D9] The verifier has teeth — it fails on a wrong live configuration, not only on a staged one
expected: |
  The verifier has teeth — it fails on a wrong live configuration, not only on a staged one
result: pass
source: automated
coverage_id: D9
verification: |
  - [pass] test/threadline/branch_protection_comparison_contract_test.exs + bin/compare-required-contexts
  - [pass] Edge 1 (empty required-context list) RED on real live state; edge 3 (name never emitted on head) RED on real live state; edge 2 (singleton match) PASS

### 56. [198-08 D1] transaction_live_test.exs (14 tests, 2 modules) passes against a threadline_test database with no schema search parameter, using only StorageSchemaCase.repo_opts()
expected: |
  transaction_live_test.exs (14 tests, 2 modules) passes against a threadline_test database with no schema search parameter, using only StorageSchemaCase.repo_opts()
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] mix test test/threadline/operator_surface/transaction_live_test.exs

### 57. [198-08 D2] Threadline.StorageSchemaMaskContractTest — D-02 mask-regression guard with a demonstrated red-then-green non-vacuity proof
expected: |
  Threadline.StorageSchemaMaskContractTest — D-02 mask-regression guard with a demonstrated red-then-green non-vacuity proof
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] mix test test/threadline/storage_schema_prefix_contract_test.exs

### 58. [198-08 D3] Port recipe for the remaining 14 defective files is proven end-to-end and written down for 198-12
expected: |
  Port recipe for the remaining 14 defective files is proven end-to-end and written down for 198-12
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/storage_schema_call_site_contract_test.exs

### 59. [198-09 D1] mix verify.compile_no_optional exits 0 with ui.ex and theme_controller.ex guarded
expected: |
  mix verify.compile_no_optional exits 0 with ui.ex and theme_controller.ex guarded
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] mix verify.compile_no_optional (run locally after removing _build/dev/lib/phoenix* to force a true no-optional-deps compile)
  - [pass] mix compile --warnings-as-errors (deps present)
  - [pass] mix verify.format

### 60. [198-09 D2] Derived-roster contract test fails a future unguarded lib/ module in its own diff
expected: |
  Derived-roster contract test fails a future unguarded lib/ module in its own diff
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] test/threadline/optional_deps_contract_test.exs — 3 tests, 0 failures

### 61. [198-10 D1] verify-mechanical job gains env+services shape matching verify-capture; mix verify.mechanical reaches a live database instead of dying on connection refused
expected: |
  verify-mechanical job gains env+services shape matching verify-capture; mix verify.mechanical reaches a live database instead of dying on connection refused
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] mix verify.mechanical (18 tests, 0 failures) — run locally against a live postgres, both before the DB existed (would fail) and after the job shape was added
  - [pass] test/threadline/ci_topology_contract_test.exs (12 tests, 0 failures) + test/threadline/phase06_nyquist_ci_contract_test.exs (1 pre-existing failure, confirmed present before this plan's edits via git-diff-free re-run, owned by plan 198-12)

### 62. [198-10 D2] Tier A evidence-bundle assertion replaced with a non-vacuity + completeness shape check; proven to fail on an empty bundle and an orphan aria.yml
expected: |
  Tier A evidence-bundle assertion replaced with a non-vacuity + completeness shape check; proven to fail on an empty bundle and an orphan aria.yml
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] new step body run verbatim against .planning/scorecards/ (366 json, 54 aria.yml) — exits 0
  - [pass] same body run against an empty temp directory — exits 1, names the zero json count
  - [pass] same body run against a temp directory with one orphan *.aria.yml and no *.json sibling — exits 1, names the orphan stem

### 63. [198-10 D3] verify-example-browser's DB-prep step gains the same ALTER DATABASE threadline_phoenix_test SET search_path statement verify-capture already carries
expected: |
  verify-example-browser's DB-prep step gains the same ALTER DATABASE threadline_phoenix_test SET search_path statement verify-capture already carries
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33353447804.json
  - [pass] the exact psql ALTER DATABASE statement run manually against a freshly created threadline_phoenix_test_verify database — exits 0 (ALTER DATABASE)
  - [pass] Example app browser E2E (Playwright) concluded success on measured CI run 33353447804 — the reduced lane this ref names, run in the environment it could not be run in locally

### 64. [198-11 D1] bin/classify-flake-run: a committed, directly executable, unit-tested classifier implementing the exact six-row behavior table (pass/broken/flaky/unknown), with `flaky` reachable only via an explicit `>= 2` branch and `unknown` as the true default (CR-02 fixed).
expected: |
  bin/classify-flake-run: a committed, directly executable, unit-tested classifier implementing the exact six-row behavior table (pass/broken/flaky/unknown), with `flaky` reachable only via an explicit `>= 2` branch and `unknown` as the true default (CR-02 fixed).
requirement: GREEN-11
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test/threadline/flake_classifier_contract_test.exs#Test 1: six-row classification behavior table
  - [pass] direct invocation of bin/classify-flake-run against 6 fixture logs — verbatim output recorded below

### 65. [198-11 D2] flake-detection.yml wired so the classify + issue steps are actually reached on a failing repeat step (CR-01 fixed): repeat step uses explicit set +e, classify step carries if: always(), issue and final-fail steps gated on if: always() && classification-value.
expected: |
  flake-detection.yml wired so the classify + issue steps are actually reached on a failing repeat step (CR-01 fixed): repeat step uses explicit set +e, classify step carries if: always(), issue and final-fail steps gated on if: always() && classification-value.
requirement: GREEN-11
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] test/threadline/flake_classifier_contract_test.exs#Test 2/3/4
  - [pass] local execution of the repeat step's exact shell body against a failing command under bash -e {0}, with GITHUB_OUTPUT pointed at a temp file — transcript recorded below

### 66. [198-11 D3] Threadline.FlakeClassifierContractTest exercises the classifier and its workflow reachability under plain `mix test`, not only via the 120-minute nightly Flake Detection workflow.
expected: |
  Threadline.FlakeClassifierContractTest exercises the classifier and its workflow reachability under plain `mix test`, not only via the 120-minute nightly Flake Detection workflow.
requirement: GREEN-11
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/flake_classifier_contract_test.exs (10 tests, 0 failures)

### 67. [198-12 D1] 9 remaining use Threadline.DataCase files ported to repo_opts() (34 failing -> 0)
expected: |
  9 remaining use Threadline.DataCase files ported to repo_opts() (34 failing -> 0)
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] mix test test/mix/tasks/ test/threadline/capture/ test/threadline/evidence/ test/threadline/governance/

### 68. [198-12 D2] 5 remaining non-DataCase files ported to repo_opts() with import Threadline.StorageSchemaCase added where needed (31 failing -> 0)
expected: |
  5 remaining non-DataCase files ported to repo_opts() with import Threadline.StorageSchemaCase added where needed (31 failing -> 0)
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] mix test test/threadline/operator_surface/breadcrumb_test.exs test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/exports_mix_parity_test.exs test/threadline/operator_surface/live/row_history_live_test.exs test/threadline/operator_surface/row_history_component_test.exs

### 69. [198-12 D3] CONTRIBUTING.md List 1 gains verify-mechanical and verify-capture rows; three-way parity test green
expected: |
  CONTRIBUTING.md List 1 gains verify-mechanical and verify-capture rows; three-way parity test green
requirement: GREEN-04
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] mix test test/threadline/phase06_nyquist_ci_contract_test.exs

### 70. [198-12 D4] Full mix test suite passes at 0 failures on two consecutive runs, anti-laundering caps stay green
expected: |
  Full mix test suite passes at 0 failures on two consecutive runs, anti-laundering caps stay green
requirement: GREEN-04
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] mix test (1397 tests, 0 failures, run twice) + mix test test/threadline/zero_skips_contract_test.exs test/threadline/storage_schema_prefix_contract_test.exs

### 71. [198-13 D1] GREEN-04 (mix test has no deterministically-failing tests) re-verified independently: two consecutive local `mix test` runs this session both report 1397 tests, 0 failures (1 excluded) — the first run's single failure (stress_router_test.exs) was traced to this fresh worktree never having run `mix deps.get` inside examples/threadline_phoenix, not a real regression; fixed and the suite re-run clean.
expected: |
  GREEN-04 (mix test has no deterministically-failing tests) re-verified independently: two consecutive local `mix test` runs this session both report 1397 tests, 0 failures (1 excluded) — the first run's single failure (stress_router_test.exs) was traced to this fresh worktree never having run `mix deps.get` inside examples/threadline_phoenix, not a real regression; fixed and the suite re-run clean.
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] mix test (full suite) — 1397 tests, 0 failures (1 excluded), run twice this session

### 72. [198-13 D2] GREEN-11 (Flake Detection classifies broken vs flaky, time-bounded, dedup issue) re-verified: Threadline.FlakeClassifierContractTest passes 10/10 locally; 198-11-SUMMARY.md documents the red-then-green non-vacuity pairs for both the classifier script and the workflow's if: always() wiring.
expected: |
  GREEN-11 (Flake Detection classifies broken vs flaky, time-bounded, dedup issue) re-verified: Threadline.FlakeClassifierContractTest passes 10/10 locally; 198-11-SUMMARY.md documents the red-then-green non-vacuity pairs for both the classifier script and the workflow's if: always() wiring.
requirement: GREEN-11
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] mix test test/threadline/flake_classifier_contract_test.exs — 10 tests, 0 failures

### 73. [198-13 D3] GREEN-07 (origin/main carries every local commit, CI concludes success) deliberately NOT claimed complete — commits are unpushed (35 ahead of origin/main), origin/main's last observed CI run (33138291361) still concludes failure, and PR #26 reports mergeStateStatus BLOCKED not CLEAN. This is a human-judgment item pending an actual push+landing step outside this run's authorized scope.
expected: |
  GREEN-07 (origin/main carries every local commit, CI concludes success) deliberately NOT claimed complete — commits are unpushed (35 ahead of origin/main), origin/main's last observed CI run (33138291361) still concludes failure, and PR #26 reports mergeStateStatus BLOCKED not CLEAN. This is a human-judgment item pending an actual push+landing step outside this run's authorized scope.
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33344382035.json

### 74. [198-13 D4] CR-03, CR-04, CR-05 (WARNING-severity findings from 198-REVIEW.md, explicitly out of this plan's fix scope) recorded as carried-forward debt in .planning/WINDOWS.md with severity and one-line consequence each, so they are visible at ship time rather than evaporating when this phase's SUMMARY scrolls out of context.
expected: |
  CR-03, CR-04, CR-05 (WARNING-severity findings from 198-REVIEW.md, explicitly out of this plan's fix scope) recorded as carried-forward debt in .planning/WINDOWS.md with severity and one-line consequence each, so they are visible at ship time rather than evaporating when this phase's SUMMARY scrolls out of context.
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] .planning/WINDOWS.md entries #5, #6, #7

### 75. [198-14 D1] pgbouncer_topology_test.exs passes against threadline_test with no schema search parameter, using only StorageSchemaCase.repo_opts() at every owned-schema call site, verified through a real PgBouncer transaction pool
expected: |
  pgbouncer_topology_test.exs passes against threadline_test with no schema search parameter, using only StorageSchemaCase.repo_opts() at every owned-schema call site, verified through a real PgBouncer transaction pool
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] DB_HOST=localhost DB_PORT=6432 THREADLINE_PGBOUNCER_TOPOLOGY=1 mix test test/threadline/pgbouncer_topology_test.exs --include pgbouncer_topology

### 76. [198-14 D2] Threadline.StorageSchemaCallSiteContractTest — static source sweep of test/**/*.exs proving zero unprefixed owned-schema Ecto call sites, with demonstrated teeth (5/14 tests go RED when the detector is neutered) and no escape hatch
expected: |
  Threadline.StorageSchemaCallSiteContractTest — static source sweep of test/**/*.exs proving zero unprefixed owned-schema Ecto call sites, with demonstrated teeth (5/14 tests go RED when the detector is neutered) and no escape hatch
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] mix test test/threadline/storage_schema_call_site_contract_test.exs

### 77. [198-14 D3] Full default suite green twice, plus the three anti-laundering caps (zero-skips, storage-schema-prefix, and the new call-site sweep) all green
expected: |
  Full default suite green twice, plus the three anti-laundering caps (zero-skips, storage-schema-prefix, and the new call-site sweep) all green
requirement: GREEN-04
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] mix test (two consecutive runs)
  - [pass] mix test test/threadline/storage_schema_call_site_contract_test.exs test/threadline/zero_skips_contract_test.exs test/threadline/storage_schema_prefix_contract_test.exs

### 78. [198-14 D4] The pooled topology test result closes GREEN-04 only once CI's verify-pgbouncer-topology job (not this local run) reports green
expected: |
  The pooled topology test result closes GREEN-04 only once CI's verify-pgbouncer-topology job (not this local run) reports green
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33344382035.json

### 79. [198-15 D1] stress_router_test.exs no longer depends on ambient examples/threadline_phoenix dependency state
expected: |
  stress_router_test.exs no longer depends on ambient examples/threadline_phoenix dependency state
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test/threadline/operator_surface/stress_router_test.exs (17 tests)

### 80. [198-15 D2] Runtime route-mount proof for /audit and /audit/__stress relocated to a named successor guard proven to have teeth (RED against a broken router, GREEN against the restored one)
expected: |
  Runtime route-mount proof for /audit and /audit/__stress relocated to a named successor guard proven to have teeth (RED against a broken router, GREEN against the restored one)
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33344382035.json
  - [pass] examples/threadline_phoenix/e2e/tests/operator-stress.spec.ts via mix verify.operator_stress

### 81. [198-16 D1] Diagnosis artifact identifying the measured root cause of the scroll_cost byte-stability drift, written and committed before any fix
expected: |
  Diagnosis artifact identifying the measured root cause of the scroll_cost byte-stability drift, written and committed before any fix
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test -s .planning/audits/198-tier-a-byte-stability.md && grep -qi reproduction ... && grep -qi 'ruled out' ...
  - [pass] DB_HOST=localhost DB_PORT=5432 mix verify.capture (run twice, byte-identical drift both times)

### 82. [198-16 D2] The byte-stability gate's strength is unchanged (no code, config, or ci.yml modification of any kind)
expected: |
  The byte-stability gate's strength is unchanged (no code, config, or ci.yml modification of any kind)
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] git diff --exit-code .github/workflows/ci.yml examples/threadline_phoenix/e2e/tests/operator-page-capture.spec.ts examples/threadline_phoenix/e2e/playwright.config.ts .github/rulesets/ .github/workflows/branch-protection.yml

### 83. [198-16 D3] GREEN-04 Gap 3's actual resolution (whether to authorize regeneration, and which remedy shape) requires a maintainer decision this executor run could not obtain synchronously
expected: |
  GREEN-04 Gap 3's actual resolution (whether to authorize regeneration, and which remedy shape) requires a maintainer decision this executor run could not obtain synchronously
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/phase198_decision_attestation_test.exs + .planning/audits/198-tier-a-byte-stability.md

### 84. [198-17 D1] Diagnosis artifact attributing all 5 CI-observed failures to a single, evidence-backed cause, and settling the 348-did-not-run figure as the configured maxFailures:5 abort, written and committed before any fix
expected: |
  Diagnosis artifact attributing all 5 CI-observed failures to a single, evidence-backed cause, and settling the 348-did-not-run figure as the configured maxFailures:5 abort, written and committed before any fix
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test -s .planning/audits/198-example-browser-e2e.md && grep -qi 'ruled out' ... && git diff --exit-code examples/ .github/ (at Task 1 commit)

### 85. [198-17 D2] The two rotting Playwright assertions rewritten at the identified cause, with a red-then-green teeth proof for each, and the browser job's identifier/check-name/aggregate-membership/timeout bounds verified unchanged
expected: |
  The two rotting Playwright assertions rewritten at the identified cause, with a red-then-green teeth proof for each, and the browser job's identifier/check-name/aggregate-membership/timeout bounds verified unchanged
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] mix verify.example_browser operator-coverage-readiness.spec.ts --project=desktop-chromium --grep desktop-1024 (RED then GREEN)
  - [pass] mix verify.example_browser operator-accessibility.spec.ts --project=desktop-chromium --grep 'keyboard reachable' (RED then GREEN)
  - [pass] git diff .github/workflows/ci.yml examples/threadline_phoenix/e2e/playwright.config.ts .github/rulesets/ .github/workflows/branch-protection.yml — all empty

### 86. [198-17 D3] The must-have backstop truth ('Example app browser E2E (Playwright) concludes success on the next CI run') requires an actual next CI run to verify, and this plan's own full local reproduction found 28 additional, unrelated, pre-existing failures that will very likely keep the job red on that next run
expected: |
  The must-have backstop truth ('Example app browser E2E (Playwright) concludes success on the next CI run') requires an actual next CI run to verify, and this plan's own full local reproduction found 28 additional, unrelated, pre-existing failures that will very likely keep the job red on that next run
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33353447804.json

### 87. [198-18 D1] Local pre-flight baseline established and an explicit prediction written before the push (mix test x2 green, anti-laundering caps green, pooled PgBouncer topology sequence green, mix ci.all stops at the documented verify.example deferred item)
expected: |
  Local pre-flight baseline established and an explicit prediction written before the push (mix test x2 green, anti-laundering caps green, pooled PgBouncer topology sequence green, mix ci.all stops at the documented verify.example deferred item)
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — Task 1 section

### 88. [198-18 D2] A real CI run on PR #29 observed to completion and recorded with run ID, head SHA, conclusion, wall-clock, per-check table, prediction scorecard, three-way job comparison, and verbatim failure output for every still-red check
expected: |
  A real CI run on PR #29 observed to completion and recorded with run ID, head SHA, conclusion, wall-clock, per-check table, prediction scorecard, three-way job comparison, and verbatim failure output for every still-red check
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] gh run watch 33197493051 --exit-status; gh api repos/szTheory/threadline/actions/runs/33197493051/jobs

### 89. [198-19 D1] verify-test job's current lane sets threadline on the example-app database search_path, matching its two sibling jobs exactly
expected: |
  verify-test job's current lane sets threadline on the example-app database search_path, matching its two sibling jobs exactly
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] grep -v '^\\s*#' .github/workflows/ci.yml | grep -c 'ALTER DATABASE' == 3, and grep -c '...threadline_phoenix_test SET search_path TO' == 3
  - [pass] local end-to-end: DB_HOST=localhost mix verify.example against a freshly recreated threadline_phoenix_test — 0 undefined_table occurrences across 3 independent runs (vs. the prior 9-failure undefined_table class)
  - [pass] idempotency: db-prep step body run twice consecutively, both exit 0, SHOW search_path output byte-identical

### 90. [198-19 D2] Static call-site sweep closes CR-01 (fully-qualified Threadline.Test.Repo.* receiver blind spot) and CR-02 (insert_all/insert_or_update/stream blind spot), still rejecting mid-identifier receivers (MyRepo., some_repo.)
expected: |
  Static call-site sweep closes CR-01 (fully-qualified Threadline.Test.Repo.* receiver blind spot) and CR-02 (insert_all/insert_or_update/stream blind spot), still rejecting mid-identifier receivers (MyRepo., some_repo.)
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] test/threadline/storage_schema_call_site_contract_test.exs (22 tests, includes 8 new CR-01/CR-02 fixtures)
  - [pass] red-then-green teeth proof: reverting @call_regex alone fails 3 CR-01 fixtures; reverting @ecto_functions alone fails 3 CR-02 fixtures; both applied together, 0 failures
  - [pass] real-tree sweep: in_scope_count 208 (pre-fix) -> 270 (post-fix), offense_count 0 in both

### 91. [198-19 D3] Round-3 local pre-push readiness signal recorded in 198-CI-MEASUREMENT.md, explicitly marked inadmissible as GREEN-04/GREEN-07 proof
expected: |
  Round-3 local pre-push readiness signal recorded in 198-CI-MEASUREMENT.md, explicitly marked inadmissible as GREEN-04/GREEN-07 proof
requirement: GREEN-04
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md Round 3 section

### 92. [198-20 D1] Decision brief exists on disk enumerating every disposition for verify-capture and verify-example-browser (plus an appendix third lane), each with all four required consequence fields, no recommendation, ranking, or default
expected: |
  Decision brief exists on disk enumerating every disposition for verify-capture and verify-example-browser (plus an appendix third lane), each with all four required consequence fields, no recommendation, ranking, or default
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test -s .planning/audits/198-ci-required-aggregate-decision.md && grep -q verify-capture ... && grep -q verify-example-browser ... && grep -q rulesets/main.json ...

### 93. [198-20 D2] Maintainer decision on Lane A/B/C dispositions obtained and recorded verbatim, attributed, dated — not selected by the executor
expected: |
  Maintainer decision on Lane A/B/C dispositions obtained and recorded verbatim, attributed, dated — not selected by the executor
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] test/threadline/phase198_decision_attestation_test.exs

### 94. [198-20 D3] Three (expanded from two, per the appendix) new numbered locked decisions appended to 198-CONTEXT.md, additions-only, each carrying a Reversibility clause, citing CI run 33197493051 where applicable, with no existing decision modified and REQUIREMENTS.md left untouched
expected: |
  Three (expanded from two, per the appendix) new numbered locked decisions appended to 198-CONTEXT.md, additions-only, each carrying a Reversibility clause, citing CI run 33197493051 where applicable, with no existing decision modified and REQUIREMENTS.md left untouched
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] git diff --numstat .planning/phases/198-green-bringup/198-CONTEXT.md (7 insertions, 0 deletions); grep -c 'Reversibility:' (10, up from 6 pre-existing); git diff --quiet .planning/REQUIREMENTS.md (clean)

### 95. [198-21 D1] ci-required's needs: list is confirmed in exactly its 198-20-decided (all-keep) state; ci.yml, rulesets/main.json, and playwright.config.ts are untouched
expected: |
  ci-required's needs: list is confirmed in exactly its 198-20-decided (all-keep) state; ci.yml, rulesets/main.json, and playwright.config.ts are untouched
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] git diff --quiet .github/rulesets/main.json && git diff --quiet examples/threadline_phoenix/e2e/playwright.config.ts; python3 needs-count check == 12 >= 10; grep -c 'ci-required:' == 1; grep -c 'name: CI required' == 1

### 96. [198-21 D2] Roster contract test derives ci-required's needs: from ci.yml, compares against a new CONTRIBUTING.md roster subheading, fails distinctly on each drift direction, on non-vacuity (<10 entries), and on undocumented allowed-skips/allowed-failures; observed red (three separate mutations) then green
expected: |
  Roster contract test derives ci-required's needs: from ci.yml, compares against a new CONTRIBUTING.md roster subheading, fails distinctly on each drift direction, on non-vacuity (<10 entries), and on undocumented allowed-skips/allowed-failures; observed red (three separate mutations) then green
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] mix test test/threadline/ci_topology_contract_test.exs:255 (roster test) — 0 failures after mutation-and-revert

### 97. [198-21 D3] Singleton contract test asserts .github/rulesets/main.json names exactly one required status check context, byte-exact-equal to the literal \"CI required\", matching ci-required's emitted name:; observed red (two separate mutations) then green
expected: |
  Singleton contract test asserts .github/rulesets/main.json names exactly one required status check context, byte-exact-equal to the literal \"CI required\", matching ci-required's emitted name:; observed red (two separate mutations) then green
requirement: GREEN-08
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] mix test test/threadline/ci_topology_contract_test.exs:295 — 0 failures after mutation-and-revert

### 98. [198-21 D4] No regression: full mix test suite, mix format --check-formatted, and mix credo --strict all clean after the plan's changes
expected: |
  No regression: full mix test suite, mix format --check-formatted, and mix credo --strict all clean after the plan's changes
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] mix test — 1422 tests, 0 failures (1 excluded); mix format --check-formatted — clean; mix credo --strict — 2726 mods/funs, found no issues

### 99. [198-22 D1] A real GitHub Actions run exists on fresh branch ci/198-round3 whose head commit contains the 198-19 and 198-21 changes; run id, per-job conclusions, and wall-clock duration recorded on disk
expected: |
  A real GitHub Actions run exists on fresh branch ci/198-round3 whose head commit contains the 198-19 and 198-21 changes; run id, per-job conclusions, and wall-clock duration recorded on disk
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] gh run view 33204829086 --json status,conclusion,createdAt,updatedAt,attempt,headSha -> completed/failure/80bf701e.../attempt 1; git ls-remote --heads origin ci/198-round3 matches local head

### 100. [198-22 D2] Run test suite (current) conclusion recorded verbatim; search_path cause (round 2) confirmed closed via 0 undefined_table occurrences; new cause (demo-seed content mismatches) identified and cited to D-41
expected: |
  Run test suite (current) conclusion recorded verbatim; search_path cause (round 2) confirmed closed via 0 undefined_table occurrences; new cause (demo-seed content mismatches) identified and cited to D-41
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] gh run view --job 98963106051 --log | grep -c undefined_table == 0; 9 named test failures all Ecto.NoResultsError/assertion-mismatch/ExUnit.TimeoutError, none undefined_table

### 101. [198-22 D3] CI required's conclusion recorded verbatim alongside all 12 needs: member conclusions, so the aggregate's verdict traces to its constituents; zero skipped/cancelled
expected: |
  CI required's conclusion recorded verbatim alongside all 12 needs: member conclusions, so the aggregate's verdict traces to its constituents; zero skipped/cancelled
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] gh run view --job 98966369580 --log alls-green summary: 9 success, 3 failure (verify-test, verify-example-browser, verify-capture), 0 skipped/cancelled

### 102. [198-22 D4] GREEN-04 and GREEN-07 marked in REQUIREMENTS.md strictly per the measured run; wall clock recorded as exact figure and compared to the 20-minute clause
expected: |
  GREEN-04 and GREEN-07 marked in REQUIREMENTS.md strictly per the measured run; wall clock recorded as exact figure and compared to the 20-minute clause
requirement: GREEN-04, GREEN-07
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] REQUIREMENTS.md both entries cite run 33204829086 as current evidence, cite 33197493051 only as superseded; wall clock 13m29s recorded and compared (<=20min)

### 103. [198-23 D1] Two-run failure inventory for mix verify.example with per-failure module/describe/test/line/class/verbatim-message, union=9, intersection=8, non-determinism marked explicitly
expected: |
  Two-run failure inventory for mix verify.example with per-failure module/describe/test/line/class/verbatim-message, union=9, intersection=8, non-determinism marked explicitly
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] .planning/audits/198-round4-demo-seed.md#two-run-failure-inventory (mix verify.example, run 1 and run 2 captured verbatim)

### 104. [198-23 D2] Cause attribution: 7 of 9 union failures share one root cause (RetentionTail.run/1's unscoped Retention.purge/1); 2 undiagnosed, out of scope
expected: |
  Cause attribution: 7 of 9 union failures share one root cause (RetentionTail.run/1's unscoped Retention.purge/1); 2 undiagnosed, out of scope
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] .planning/audits/198-round4-demo-seed.md#cause-attribution + #root-cause-confirmation (manual mix run reproduction against real DB state)

### 105. [198-23 D3] SEED-03 manifest heroes and SEED-03 leaving agent window clusters fixed at cause, with red-then-green teeth proof, no test assertion changed
expected: |
  SEED-03 manifest heroes and SEED-03 leaving agent window clusters fixed at cause, with red-then-green teeth proof, no test assertion changed
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (13 tests, 0 failures, two consecutive runs)

### 106. [198-23 D4] Measured mix verify.example failure count strictly lower after the fix (9 -> 1, both after-runs consistent) with a reusable fix protocol on disk for plans 198-24..28
expected: |
  Measured mix verify.example failure count strictly lower after the fix (9 -> 1, both after-runs consistent) with a reusable fix protocol on disk for plans 198-24..28
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] .planning/audits/198-round4-demo-seed.md#measured-after-count + #fix-protocol-for-the-remaining-clusters

### 107. [198-23 D5] GREEN-04 not marked Complete — only a measured CI run (plan 198-29, D-01) is admissible evidence; this plan is a local readiness signal only
expected: |
  GREEN-04 not marked Complete — only a measured CI run (plan 198-29, D-01) is admissible evidence; this plan is a local readiness signal only
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33344382035.json

### 108. [198-24 D1] SEED-05, SEED-02/04, SEED-04, WALK-04, D-05, D-13 clusters confirmed passing (13 tests, 0 failures) before and after this plan's edits, two consecutive runs
expected: |
  SEED-05, SEED-02/04, SEED-04, WALK-04, D-05, D-13 clusters confirmed passing (13 tests, 0 failures) before and after this plan's edits, two consecutive runs
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (13 tests, 0 failures, two consecutive runs, both pre- and post-edit)

### 109. [198-24 D2] WALK-04 redaction_policy assertion reads subject_ref from the manifest-sourced local variable instead of a duplicate inline literal
expected: |
  WALK-04 redaction_policy assertion reads subject_ref from the manifest-sourced local variable instead of a duplicate inline literal
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] .planning/audits/198-round4-demo-seed.md#plan-198-24 (static/structural teeth proof)

### 110. [198-24 D3] D-05 backdating tests pair a non-emptiness assertion with the outside-window property assertion, with explicit boundary-comparison semantics stated in source
expected: |
  D-05 backdating tests pair a non-emptiness assertion with the outside-window property assertion, with explicit boundary-comparison semantics stated in source
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] reproducible mix run script (/tmp/teeth_proof_198_24.exs) demonstrating the old assertion form's vacuous pass and the new guard's correct failure under a simulated seed regression

### 111. [198-24 D4] mix verify.example re-measured twice after this plan's changes: 109 tests, 1 failure both runs, delta 0 vs plan 198-23's closing count, residual failure already dated and unrelated
expected: |
  mix verify.example re-measured twice after this plan's changes: 109 tests, 1 failure both runs, delta 0 vs plan 198-23's closing count, residual failure already dated and unrelated
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] .planning/audits/198-round4-demo-seed.md#measured-after-count-198-24

### 112. [198-24 D5] GREEN-04 not marked Complete -- only the measured CI run in plan 198-29 (D-01) is admissible evidence
expected: |
  GREEN-04 not marked Complete -- only the measured CI run in plan 198-29 (D-01) is admissible evidence
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33344382035.json

### 113. [198-25 D1] Fresh two-run re-inventory of both target files after 198-23/198-24's seed changes landed, before any 198-25 edit
expected: |
  Fresh two-run re-inventory of both target files after 198-23/198-24's seed changes landed, before any 198-25 edit
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] .planning/audits/198-round4-demo-seed.md#re-inventory-after-198-23-198-24-198-25

### 114. [198-25 D2] Root-cause fix for 'admin export status shows seeded job states' (assert html =~ Export expired): a production-code label divergence, not a seed defect
expected: |
  Root-cause fix for 'admin export status shows seeded job states' (assert html =~ Export expired): a production-code label divergence, not a seed defect
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_happy_path_test.exs (12 tests, 0 failures, two consecutive runs)
  - [pass] test/threadline/operator_surface/live/export_status_live_test.exs + copy_contract_test.exs + presentation_test.exs (68 tests, 0 failures) and root mix test (1422 tests, 0 failures)

### 115. [198-25 D3] WALK-04-01 and WALK-04-03 vacuous-pass assertions hardened (wrong route + string never rendered anywhere; assertion matching static CSS instead of data)
expected: |
  WALK-04-01 and WALK-04-03 vacuous-pass assertions hardened (wrong route + string never rendered anywhere; assertion matching static CSS instead of data)
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs (3 tests, 0 failures, two consecutive runs)

### 116. [198-25 D4] ExUnit.TimeoutError diagnosed via direct pg_stat_activity capture: an orphaned idle-in-transaction session blocking a deterministic-UUID upsert (lock/deadlock cause class), with an in-scope defensive advisory-lock hardening applied
expected: |
  ExUnit.TimeoutError diagnosed via direct pg_stat_activity capture: an orphaned idle-in-transaction session blocking a deterministic-UUID upsert (lock/deadlock cause class), with an in-scope defensive advisory-lock hardening applied
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] test/threadline/idle_transaction_reaper_contract_test.exs + config/test.exs
  - [pass] .planning/audits/198-round4-demo-seed.md#exuniontimeouterror-diagnosis-198-25 (live pg_stat_activity transcript)

### 117. [198-25 D5] mix verify.example measured at 109 tests, 0 failures on two consecutive runs — the target state named by this plan's must-haves
expected: |
  mix verify.example measured at 109 tests, 0 failures on two consecutive runs — the target state named by this plan's must-haves
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] .planning/audits/198-round4-demo-seed.md#measured-after-count-198-25-closing

### 118. [198-25 D6] GREEN-04 not marked Complete -- only the measured CI run in plan 198-29 (D-01) is admissible evidence
expected: |
  GREEN-04 not marked Complete -- only the measured CI run in plan 198-29 (D-01) is admissible evidence
result: pass
source: automated
coverage_id: D6
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33344382035.json

### 119. [198-26 D1] "Full attribution table for all 29 masked Playwright failures, with
expected: |
  "Full attribution table for all 29 masked Playwright failures, with
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] .planning/audits/198-round4-playwright.md#full-failure-attribution (produced from a real unbounded local run, /tmp/198-26-before.log)

### 120. [198-26 D2] "register.spec.ts fixed at cause (scoped signed-in assertion to
expected: |
  "register.spec.ts fixed at cause (scoped signed-in assertion to
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] mix verify.example_browser register.spec.ts --project=desktop-chromium --project=mobile-chromium

### 121. [198-26 D3] "operator-find-mobile.spec.ts fixed at cause (expand collapsed
expected: |
  "operator-find-mobile.spec.ts fixed at cause (expand collapsed
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] mix verify.example_browser operator-find-mobile.spec.ts --project=desktop-chromium --project=mobile-chromium

### 122. [198-26 D4] "Lane re-measured with an integer delta (29 -> 18) and the GREEN-07
expected: |
  "Lane re-measured with an integer delta (29 -> 18) and the GREEN-07
requirement: GREEN-07
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] .planning/audits/198-round4-playwright.md#measured-after-count-198-26 (mix verify.example_browser --project=desktop-chromium --project=mobile-chromium, /tmp/198-26-after.log)

### 123. [198-27 D1] "Cluster 198-27's 12 rows (Phase 135/173/175/177 UAT specs)
expected: |
  "Cluster 198-27's 12 rows (Phase 135/173/175/177 UAT specs)
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] mix verify.example_browser operator-phase-135-uat.spec.ts operator-phase-173-uat.spec.ts operator-phase-175-uat.spec.ts operator-phase-177-uat.spec.ts --project=desktop-chromium --project=mobile-chromium

### 124. [198-27 D2] "Lane re-measured unbounded with an integer delta (18 -> 11)
expected: |
  "Lane re-measured unbounded with an integer delta (18 -> 11)
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] .planning/audits/198-round4-playwright.md#measured-after-count-198-27 (mix verify.example_browser --project=desktop-chromium --project=mobile-chromium)

### 125. [198-28 D1] "Post-merge re-validation of all 7 seed-sensitive=yes attribution rows
expected: |
  "Post-merge re-validation of all 7 seed-sensitive=yes attribution rows
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] .planning/audits/198-round4-playwright.md#post-merge-re-validation-198-28 (mix verify.example_browser --project=desktop-chromium --project=mobile-chromium, /tmp/198-28-postmerge.log)

### 126. [198-28 D2] "CI-contributing (2) vs local-only (8) split established for cluster
expected: |
  "CI-contributing (2) vs local-only (8) split established for cluster
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] .planning/audits/198-round4-playwright.md#ci-contributing-versus-local-only-split-198-28

### 127. [198-28 D3] "Blocking checkpoint:decision (Task 2, baseline regeneration) answered
expected: |
  "Blocking checkpoint:decision (Task 2, baseline regeneration) answered
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/phase198_decision_attestation_test.exs

### 128. [198-28 D4] "Both CI-contributing rows (operator-screenshots.spec.ts admin
expected: |
  "Both CI-contributing rows (operator-screenshots.spec.ts admin
requirement: GREEN-07
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] mix verify.example_browser operator-screenshots.spec.ts:90 --project=desktop-chromium --project=mobile-chromium

### 129. [198-28 D5] "8 local-only rows reconciled as open with a named cause each, and no
expected: |
  "8 local-only rows reconciled as open with a named cause each, and no
requirement: GREEN-07
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] .planning/audits/198-round4-playwright.md#cluster-198-28-reconciliation

### 130. [198-29 D1] Round 4 measured-CI record on disk: run id, head SHA, attempt, per-job table, verbatim alls-green output, prediction scorecard, five-column baseline comparison, wall clock, CI required conclusion, mergeStateStatus, root cause per red lane
expected: |
  Round 4 measured-CI record on disk: run id, head SHA, attempt, per-job table, verbatim alls-green output, prediction scorecard, five-column baseline comparison, wall clock, CI required conclusion, mergeStateStatus, root cause per red lane
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] gh run view 33253587315 --json status,conclusion,createdAt,updatedAt,attempt,headSha,event
  - [pass] grep -q '^## Round 4 (2026-08-29)' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md

### 131. [198-29 D2] GREEN-04 and GREEN-07 statuses set strictly from the measured run, prose and status table in agreement, no gate narrowed
expected: |
  GREEN-04 and GREEN-07 statuses set strictly from the measured run, prose and status table in agreement, no gate narrowed
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] git diff f18ed150 --name-only (only .planning/ documentation files changed; .github/, CONTRIBUTING.md, playwright.config.ts untouched)

### 132. [198-30 D1] Cold MIX_ENV=prod compile moved out of the per-test ExUnit timeout budget in demo_reset_test.exs via setup_all; both original assertions kept verbatim
expected: |
  Cold MIX_ENV=prod compile moved out of the per-test ExUnit timeout budget in demo_reset_test.exs via setup_all; both original assertions kept verbatim
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] cd examples/threadline_phoenix && rm -rf _build/prod && MIX_ENV=test mix test test/threadline_phoenix/demo_reset_test.exs

### 133. [198-30 D2] Demo seed/reset advisory lock moved to Reset.run/1 and Seed.run/0 (every entry point), namespaced with a fixed classid, bounded, abnormal-exit-safe; WalkthroughCase's disproven comment corrected
expected: |
  Demo seed/reset advisory lock moved to Reset.run/1 and Seed.run/0 (every entry point), namespaced with a fixed classid, bounded, abnormal-exit-safe; WalkthroughCase's disproven comment corrected
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix/demo_reset_test.exs test/threadline_phoenix/demo_contract_test.exs test/threadline_phoenix_web/walkthrough_happy_path_test.exs test/threadline_phoenix_web/walkthrough_evidence_test.exs test/mix/tasks/threadline_evidence_show_example_test.exs
  - [pass] manual psql pg_advisory_lock(84672301,1) hold + demo_reset_test.exs:43 run — failed with named lock-timeout error at 45.7s, not a hang

### 134. [198-30 D3] mix verify.example run twice consecutively from repo root, both 109 tests / 0 failures — readiness signal only, explicitly not GREEN-04 evidence per D-01
expected: |
  mix verify.example run twice consecutively from repo root, both 109 tests / 0 failures — readiness signal only, explicitly not GREEN-04 evidence per D-01
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline/ci_attestation_contract_test.exs + .planning/audits/ci-attestation-33344382035.json
  - [pass] mix verify.example (run 1 and run 2)

### 135. [198-31 D1] Both self-caused /Expired/ regression rows (operator-accessibility.spec.ts:565:3, operator-prove-mobile.spec.ts:38:3) pass by name, anchored to the canonical 'Export expired' literal
expected: |
  Both self-caused /Expired/ regression rows (operator-accessibility.spec.ts:565:3, operator-prove-mobile.spec.ts:38:3) pass by name, anchored to the canonical 'Export expired' literal
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] operator-accessibility.spec.ts:565:3 (desktop-chromium + mobile-chromium)
  - [pass] operator-prove-mobile.spec.ts:38:3 (desktop-chromium + mobile-chromium)

### 136. [198-31 D2] operator-responsive-mobile-first.spec.ts:577:5 diagnosed at an established, cited cause and fixed; phone-viewport row-history route passes by name
expected: |
  operator-responsive-mobile-first.spec.ts:577:5 diagnosed at an established, cited cause and fixed; phone-viewport row-history route passes by name
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] operator-responsive-mobile-first.spec.ts:584:5 'operator responsive matrix: phone' (desktop-chromium + mobile-chromium)

### 137. [198-31 D3] WR-08, WR-09, WR-11 closed by strictly-added assertions, no expect() removed
expected: |
  WR-08, WR-09, WR-11 closed by strictly-added assertions, no expect() removed
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] operator-phase-175-uat.spec.ts:84:3, operator-screenshots.spec.ts:90:3, operator-find-mobile.spec.ts:103:3 (desktop-chromium + mobile-chromium)

### 138. [198-32 D1] Coverage-snapshot assertions in walkthrough_evidence_test.exs discriminate on count, failing when covered_count and uncovered_count are both zero (CR-02)
expected: |
  Coverage-snapshot assertions in walkthrough_evidence_test.exs discriminate on count, failing when covered_count and uncovered_count are both zero (CR-02)
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test/threadline_phoenix_web/walkthrough_evidence_test.exs#WALK-04-03 trigger coverage dashboard and evidence snapshot

### 139. [198-32 D2] Negative timeline assertion refute timeline_html =~ \"View Incident\" restored alongside the positive empty-state assertion on /audit/timeline (WR-10)
expected: |
  Negative timeline assertion refute timeline_html =~ \"View Incident\" restored alongside the positive empty-state assertion on /audit/timeline (WR-10)
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] test/threadline_phoenix_web/walkthrough_evidence_test.exs#WALK-04-01 retention_run evidence and empty offboarded-co timeline

### 140. [198-32 D3] Manifest's declared redaction-policy subject_ref literal pinned in demo_contract_test.exs, alongside the retained (now honestly-labelled) tautological round-trip assertion (CR-03)
expected: |
  Manifest's declared redaction-policy subject_ref literal pinned in demo_contract_test.exs, alongside the retained (now honestly-labelled) tautological round-trip assertion (CR-03)
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline_phoenix/demo_contract_test.exs#WALK-04 redaction policy evidence post-demo.seed redaction_policy row matches manifest subject_ref

### 141. [198-33 D1] Phase-177 group-story catalog assertion restores a >=12 cardinality floor, 4 pinned identities, and a group. prefix filter-applied proof (CR-04, WR-07)
expected: |
  Phase-177 group-story catalog assertion restores a >=12 cardinality floor, 4 pinned identities, and a group. prefix filter-applied proof (CR-04, WR-07)
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts#Phase 177 UAT #1 — group catalog holds together at every viewport

### 142. [198-33 D2] Every group story reports its own PASS/FAIL verdict in one run, and the time budget is traced to a measured per-story cost with a stated headroom multiplier (WR-06)
expected: |
  Every group story reports its own PASS/FAIL verdict in one run, and the time budget is traced to a measured per-story cost with a stated headroom multiplier (WR-06)
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts#Phase 177 UAT #1 — group catalog holds together at every viewport

### 143. [198-33 D3] Phase-135 Coverage test discriminates role from lane: fails if the admin half is removed (product regressed to deny admin) and fails if the support half is removed (product regressed to allow support) (CR-05)
expected: |
  Phase-135 Coverage test discriminates role from lane: fails if the admin half is removed (product regressed to deny admin) and fails if the support half is removed (product regressed to allow support) (CR-05)
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] examples/threadline_phoenix/e2e/tests/operator-phase-135-uat.spec.ts#Coverage is role-discriminating: support gets the unavailable lane, admin gets the table

### 144. [198-34 D1] One public, :now-aware, normalization-consistent function (Presentation.export_status_label/2) owns the export status copy contract; the private duplicate in export_status_live.ex is deleted, not realigned.
expected: |
  One public, :now-aware, normalization-consistent function (Presentation.export_status_label/2) owns the export status copy contract; the private duplicate in export_status_live.ex is deleted, not realigned.
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test/threadline/operator_surface/presentation_test.exs#export status label (10 tests: Queued/Processing/Failed/expired/unavailable branches, frozen-clock exact-equality boundary, nil/absent expires_at fallback, nil/unrecognised status fallback, atom/string parity)
  - [pass] grep -c \"defp export_job_status_label\" lib/threadline/operator_surface/live/export_status_live.ex == 0

### 145. [198-34 D2] The Phase-186 copy lock in copy_contract_test.exs guards the function that actually renders (re-anchored from export_status_live.ex to presentation.ex), at full five-literal strength.
expected: |
  The Phase-186 copy lock in copy_contract_test.exs guards the function that actually renders (re-anchored from export_status_live.ex to presentation.ex), at full five-literal strength.
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] test/threadline/operator_surface/copy_contract_test.exs#Phase 186 export source locks real completed downloads and non-ready status text

### 146. [198-34 D3] Every downstream e2e assertion depending on the rendered export status vocabulary is reconciled — under Option B the reconciliation is verified-empty (byte-identical copy), confirmed by re-deriving the grep against live source rather than trusting the plan's snapshot.
expected: |
  Every downstream e2e assertion depending on the rendered export status vocabulary is reconciled — under Option B the reconciliation is verified-empty (byte-identical copy), confirmed by re-deriving the grep against live source rather than trusting the plan's snapshot.
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] examples/threadline_phoenix/e2e/tests/operator-prove-mobile.spec.ts, operator-accessibility.spec.ts, operator-screenshots.spec.ts, operator-features.spec.ts — full run via run-e2e.sh, --project=desktop-chromium --project=mobile-chromium

### 147. [198-34 D4] The recorded maintainer decision (198-34-DECISION.md) names the selected option, its rationale, and an explicit disposition for export_action_label/2, and no source file was modified before the answer was recorded.
expected: |
  The recorded maintainer decision (198-34-DECISION.md) names the selected option, its rationale, and an explicit disposition for export_action_label/2, and no source file was modified before the answer was recorded.
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] .planning/phases/198-green-bringup/198-34-DECISION.md — recorded 2026-08-30; git diff --stat -- lib/ test/ examples/threadline_phoenix/e2e/ was empty at checkpoint time (verified before any implementation task began)

### 148. [198-35 D1] Compile-time invariant guard fails the build when the retention cutoff is outside — or exactly at — either bound, naming the invariant and both bounds
expected: |
  Compile-time invariant guard fails the build when the retention cutoff is outside — or exactly at — either bound, naming the invariant and both bounds
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] MIX_ENV=test mix compile --force with @org_y_backdate_days, @earliest_other_org_epoch_offset_days, and @retention_purge_cutoff_days_before_epoch perturbed to violate/equal each bound

### 149. [198-35 D2] Safety comment states the re-derived earliest other-org epoch offset (21 days) and cites personas.ex:99 and temporal.ex:37
expected: |
  Safety comment states the re-derived earliest other-org epoch offset (21 days) and cites personas.ex:99 and temporal.ex:37
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] grep -rn 'Manifest.epoch()' examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/ cross-checked against every seeder's timestamp arithmetic

### 150. [198-35 D3] Retention.Policy's latent resolve_cutoff/2 ArgumentError is wrapped with a legible clock-skew diagnostic naming both cutoffs and the approximate clock threshold
expected: |
  Retention.Policy's latent resolve_cutoff/2 ArgumentError is wrapped with a legible clock-skew diagnostic naming both cutoffs and the approximate clock threshold
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] test/threadline_phoenix/demo_contract_test.exs (13 tests, exercises RetentionTail.run/1 end-to-end)

### 151. [198-35 D4] The seed raises with a named cause (naming the cutoff) when the purge deletes every non-org-Y audit change, instead of surfacing as a downstream test failure
expected: |
  The seed raises with a named cause (naming the cutoff) when the purge deletes every non-org-Y audit change, instead of surfacing as a downstream test failure
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] Isolated test invoking the real assert_other_orgs_survived!/1 after simulating a full non-org-Y sweep via direct SQL delete — raised the exact intended message naming the cutoff

### 152. [198-35 D5] Application retention env is restored to its prior value on both the success and failure paths of the purge
expected: |
  Application retention env is restored to its prior value on both the success and failure paths of the purge
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] Isolated test comparing Application.get_env(:threadline, :retention) before and after a full mix demo.seed run — equal

### 153. [198-35 D6] No existing assertion weakened; library retention module unchanged; demo-seed defect fixed at cause per D-41
expected: |
  No existing assertion weakened; library retention module unchanged; demo-seed defect fixed at cause per D-41
result: pass
source: automated
coverage_id: D6
verification: |
  - [pass] test/threadline_phoenix/demo_contract_test.exs, test/threadline_phoenix_web/walkthrough_happy_path_test.exs, test/threadline_phoenix_web/walkthrough_evidence_test.exs (28 tests); mix verify.example (109 tests); git diff --stat -- lib/threadline/retention.ex .github/ CONTRIBUTING.md .planning/scorecards/ '*.png' (empty)

### 154. [198-36 D1] 20-row ledger whose finding-id set is provably identical to REVIEW.md's, with 18 fixed / 1 deferred / 1 no-action-required / 0 unaddressed, every fixed row citing a command+output or commit sha re-run against the tree
expected: |
  20-row ledger whose finding-id set is provably identical to REVIEW.md's, with 18 fixed / 1 deferred / 1 no-action-required / 0 unaddressed, every fixed row citing a command+output or commit sha re-run against the tree
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] grep -oE id-set diff between REVIEW.md and 198-round5-review-triage.md (empty diff, exit 0); grep -c row-count = 20; per-disposition grep counts = 18/1/1

### 155. [198-36 D2] Round-5's four actionable causes recorded with closing evidence, including the correction that operator-responsive-mobile-first.spec.ts:577:5 was fixed at cause and did not honestly halt
expected: |
  Round-5's four actionable causes recorded with closing evidence, including the correction that operator-responsive-mobile-first.spec.ts:577:5 was fixed at cause and did not honestly halt
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] 198-round5-review-triage.md 'Round-5 actionable causes' table, cross-checked against .planning/audits/198-round5-playwright.md

### 156. [198-36 D3] Both structurally-uncloseable items (Tier A capture lane, operator-stress.spec.ts page.* diffs) recorded with D-39 citation and no closure attempted
expected: |
  Both structurally-uncloseable items (Tier A capture lane, operator-stress.spec.ts page.* diffs) recorded with D-39 citation and no closure attempted
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] 198-round5-review-triage.md 'Structurally uncloseable' section citing REQUIREMENTS.md's Out of Scope table and 198-31-SUMMARY.md's Next Phase Readiness

### 157. [198-36 D4] Deferrals and carried-debt updates appended with owners, dates, and citations; no prior record edited in place, proven mechanically via git diff -U0 zero-removed-lines
expected: |
  Deferrals and carried-debt updates appended with owners, dates, and citations; no prior record edited in place, proven mechanically via git diff -U0 zero-removed-lines
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] bash -c 'test \"$(git diff -U0 -- .planning/phases/198-green-bringup/deferred-items.md .planning/WINDOWS.md | grep -c \"^-[^-]\")\" = \"0\"'

### 158. [198-37 D1] Round 5 measured-CI record on disk: run id, head SHA, attempt, per-job table, verbatim alls-green output, prediction scorecard, six-column baseline comparison, wall clock, CI required conclusion, mergeStateStatus, root cause per red lane
expected: |
  Round 5 measured-CI record on disk: run id, head SHA, attempt, per-job table, verbatim alls-green output, prediction scorecard, six-column baseline comparison, wall clock, CI required conclusion, mergeStateStatus, root cause per red lane
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] gh run view 33336651956 --json status,conclusion,createdAt,updatedAt,attempt,headSha,event
  - [pass] grep -q '^## Round 5 (2026-08-30) — Measured CI run' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md

### 159. [198-37 D2] GREEN-04 and GREEN-07 statuses set strictly from the measured run, prose and status table in agreement, no gate narrowed
expected: |
  GREEN-04 and GREEN-07 statuses set strictly from the measured run, prose and status table in agreement, no gate narrowed
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] git diff --stat ab412fdd..14f923a7 -- .github/ CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts .planning/scorecards/ '*.png' (empty)
  - [pass] gh run view --job 99324669885 --log | grep '1434 tests, 0 failures, 1 excluded'

### 160. [198-37 D3] Falsifiable prediction committed before the push (14f923a7), scored without retro-editing after the run
expected: |
  Falsifiable prediction committed before the push (14f923a7), scored without retro-editing after the run
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] git diff -U0 -- .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md | grep -c '^-[^-]' (returns 0 across both commits)

### 161. [198-38 D1] Demo.Reset.with_demo_lock/1 pins the entire guarded region (SET lock_timeout, retry loop, fun.(), pg_advisory_unlock) inside one Repo.checkout/2 call; Demo.Seed delegates to it instead of maintaining a second copy
expected: |
  Demo.Reset.with_demo_lock/1 pins the entire guarded region (SET lock_timeout, retry loop, fun.(), pg_advisory_unlock) inside one Repo.checkout/2 call; Demo.Seed delegates to it instead of maintaining a second copy
requirement: GREEN-04
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] examples/threadline_phoenix/test/threadline_phoenix/demo_reset_test.exs
  - [pass] examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
  - [pass] MIX_ENV=dev mix demo.reset (real pooled connection, pool_size: 10)

### 162. [198-38 D2] A connection-identity regression test observed red on the pre-fix tree and green on the post-fix tree, falsifying CR-01 rather than re-asserting the sandbox-masked happy path
expected: |
  A connection-identity regression test observed red on the pre-fix tree and green on the post-fix tree, falsifying CR-01 rather than re-asserting the sandbox-masked happy path
requirement: GREEN-04
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] examples/threadline_phoenix/test/threadline_phoenix/demo/advisory_lock_pinning_test.exs — ThreadlinePhoenix.Demo.AdvisoryLockPinningTest (2 tests)

### 163. [198-38 D3] CR-01, WR-01, IN-01 each carry a terminal, cited disposition; IN-01's cosmetic comment defect is corrected
expected: |
  CR-01, WR-01, IN-01 each carry a terminal, cited disposition; IN-01's cosmetic comment defect is corrected
requirement: GREEN-04
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] .planning/phases/198-green-bringup/198-round6-review-triage.md (all three rows disposition=fixed, with file/function/command citations)
  - [pass] examples/threadline_phoenix/test/threadline_phoenix_web/walkthrough_evidence_test.exs

### 164. [198-39 D1] GREEN-07 and roadmap SC3 carry a terminal, maintainer-selected disposition (option-a, accepted-Pending for v1.41) recorded verbatim in 198-39-DECISION.md, naming both D-39-forced red lanes and each lane's unblock condition
expected: |
  GREEN-07 and roadmap SC3 carry a terminal, maintainer-selected disposition (option-a, accepted-Pending for v1.41) recorded verbatim in 198-39-DECISION.md, naming both D-39-forced red lanes and each lane's unblock condition
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] test/threadline/phase198_decision_attestation_test.exs
  - [pass] bash -c 'grep -Eq \"option-(a|b|c)\" .planning/phases/198-green-bringup/198-39-DECISION.md'

### 165. [198-39 D2] The disposition reads identically from REQUIREMENTS.md, ROADMAP.md, deferred-items.md, and STATE.md — no reader finds two different answers about GREEN-07
expected: |
  The disposition reads identically from REQUIREMENTS.md, ROADMAP.md, deferred-items.md, and STATE.md — no reader finds two different answers about GREEN-07
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] bash -c 'for f in .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/198-green-bringup/deferred-items.md .planning/STATE.md; do grep -q 198-39-DECISION \"$f\" || exit 1; done'

### 166. [198-39 D3] GREEN-07 remains unchecked/Pending and the round-5 measured evidence (run 33336651956) was appended to, not overwritten
expected: |
  GREEN-07 remains unchecked/Pending and the round-5 measured evidence (run 33336651956) was appended to, not overwritten
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] grep -n 'GREEN-07' .planning/REQUIREMENTS.md (still `- [ ]`, still Pending); git diff -- .planning/REQUIREMENTS.md | grep '^-[^-]' | grep -c 33336651956 (returns 0)

### 167. [198-39 D4] origin/main gap and PR #32/#26 dispositions recorded from live observation, with an explicit written refusal to edit branch protection, and no push/merge performed
expected: |
  origin/main gap and PR #32/#26 dispositions recorded from live observation, with an explicit written refusal to edit branch protection, and no push/merge performed
requirement: GREEN-08
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] git rev-list --count origin/main..HEAD (202, 2026-08-30T22:50:23Z); gh pr view 32/26 --json number,state,isDraft,mergeStateStatus (both BLOCKED); git status --porcelain .github/ (empty)

### 168. [198-40 D1] A falsifiable Round 6 prediction, covering all 12 ci-required needs: members plus the aggregate, is committed to disk before the push and never edited afterward
expected: |
  A falsifiable Round 6 prediction, covering all 12 ci-required needs: members plus the aggregate, is committed to disk before the push and never edited afterward
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] bash -c 'git diff HEAD~2 -- .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md | grep -c \"^-[^-]\"' returns 0 across both Task 1 and Task 3 commits

### 169. [198-40 D2] One real GitHub Actions run (33344382035) exists whose head SHA matches the sealed prediction's SHA character-for-character, with run id, attempt, per-job conclusions, and wall clock recorded verbatim
expected: |
  One real GitHub Actions run (33344382035) exists whose head SHA matches the sealed prediction's SHA character-for-character, with run id, attempt, per-job conclusions, and wall clock recorded verbatim
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] gh run view 33344382035 --json status,conclusion,createdAt,updatedAt,attempt,headSha,event (attempt:1, conclusion:failure, headSha matches 23c16267 exactly)

### 170. [198-40 D3] GREEN-04 confirmed strictly from this run's Run test suite (current) success conclusion, re-proved (not inherited) because 198-38 changed its covered example-app source
expected: |
  GREEN-04 confirmed strictly from this run's Run test suite (current) success conclusion, re-proved (not inherited) because 198-38 changed its covered example-app source
requirement: GREEN-04
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] gh run view 33344382035 --json jobs — Run test suite (current) conclusion:success; job log: 1434 tests/0 failures/1 excluded, mix verify.example 111/0, mix verify.doc_contract 128/0

### 171. [198-40 D4] GREEN-07 remains Pending, cited to both this run's measured CI required: failure and 198-39-DECISION.md's standing option-a disposition; the disposition is not revisited
expected: |
  GREEN-07 remains Pending, cited to both this run's measured CI required: failure and 198-39-DECISION.md's standing option-a disposition; the disposition is not revisited
requirement: GREEN-07
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] grep -n 'GREEN-07' .planning/REQUIREMENTS.md (still `- [ ]`, still Pending); gh api /repos/szTheory/threadline/actions/jobs/99347003929 alls-green step output (2 of 12 failure)

### 172. [198-40 D5] The nine carried requirements (GREEN-01, 02, 03, 05, 06, 09, 10, 11, 12) are stated as an explicit decision citing round-5 evidence, with no existing row rewritten
expected: |
  The nine carried requirements (GREEN-01, 02, 03, 05, 06, 09, 10, 11, 12) are stated as an explicit decision citing round-5 evidence, with no existing row rewritten
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] git diff -- .planning/REQUIREMENTS.md | grep '^-[^-]' | grep -c 'GREEN-0[12356]\\|GREEN-1[012]' returns 0

### 173. [198-40 D6] D-42 invariants held over the round-6-specific commit range: no gate, config, or scorecard/PNG evidence was narrowed, weakened, or regenerated by this round's own commits
expected: |
  D-42 invariants held over the round-6-specific commit range: no gate, config, or scorecard/PNG evidence was narrowed, weakened, or regenerated by this round's own commits
result: pass
source: automated
coverage_id: D6
verification: |
  - [pass] git diff --stat 14f923a7..23c16267 -- .github/ CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts .planning/scorecards/ '*.png' — empty, exit 0

### 174. [198-41 D1] Round 7 pins the live local graph, PR #34, successful run 33354216172, ruleset 21702804, required context, and PR #26 in one timestamped packet.
expected: |
  Round 7 pins the live local graph, PR #34, successful run 33354216172, ruleset 21702804, required context, and PR #26 in one timestamped packet.
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] gh pr view 34 + gh run view 33354216172 + gh api repos/szTheory/threadline/rulesets/21702804 at 2026-09-08T19:20:03Z

### 175. [198-41 D2] The packet proves merge, squash, rebase, and pre-closeout fast-forward each fail at least one locked final-state predicate.
expected: |
  The packet proves merge, squash, rebase, and pre-closeout fast-forward each fail at least one locked final-state predicate.
requirement: GREEN-07
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] grep and ancestry checks over the Round 7 Merge-method and executor-lifecycle proof

### 176. [198-41 D3] All 33 deterministic edge rows are explicitly accounted for as 10 lifted plus 23 disposed.
expected: |
  All 33 deterministic edge rows are explicitly accounted for as 10 lifted plus 23 disposed.
requirement: GREEN-08
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] grep '10 lifted + 23 disposed = 33' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md

### 177. [198-41 D4] The lifecycle table counts planning, task-evidence, and summary commits and preserves GREEN-07 Pending with no remote or ruleset mutation.
expected: |
  The lifecycle table counts planning, task-evidence, and summary commits and preserves GREEN-07 Pending with no remote or ruleset mutation.
requirement: GREEN-07
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] Plan 198-41 Task 2 automated lifecycle-section verification

### 178. [198-41 D5] Rounds 1–6 remain byte-identical and the 36 pending UAT judgments remain classified as judgment rather than implementation gaps.
expected: |
  Rounds 1–6 remain byte-identical and the 36 pending UAT judgments remain classified as judgment rather than implementation gaps.
requirement: GREEN-08
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] SHA-256 of first 2026 lines = 25b4b4658b44b61636bebbc4c570904bd0fda07ce73a7d28196f42b56f474ba6; 198-UAT.md pending: 36

### 179. [198-42 D1] Plan 42 records its entry SHA separately from the immutable PR/run subject and includes the committed Plan 41 evidence and summary.
expected: |
  Plan 42 records its entry SHA separately from the immutable PR/run subject and includes the committed Plan 41 evidence and summary.
requirement: GREEN-07
result: pass
source: automated
coverage_id: D1
verification: |
  - [pass] Task 1 graph verification at b7140a0491fb0bcb8c2a81b85e0a7b7e796fc000

### 180. [198-42 D2] Two read-only ruleset snapshots have matching complete editable-field digests, active enforcement, no bypass actors, and sole context CI required.
expected: |
  Two read-only ruleset snapshots have matching complete editable-field digests, active enforcement, no bypass actors, and sole context CI required.
requirement: GREEN-08
result: pass
source: automated
coverage_id: D2
verification: |
  - [pass] Task 1 canonical ruleset verification; digest d65ef5955bd63595aaee3372f8bae2b940e448617a9dde0974e6aeee95861372

### 181. [198-42 D3] The newest canonical-CI exact-origin/main-SHA run is selected deterministically before status evaluation and its unique aggregate job conclusion is recorded.
expected: |
  The newest canonical-CI exact-origin/main-SHA run is selected deterministically before status evaluation and its unique aggregate job conclusion is recorded.
requirement: GREEN-07
result: pass
source: automated
coverage_id: D3
verification: |
  - [pass] Task 2 live selector replay for run 33138291361 at 906902f131a72558d0ded1d0fd60f84fb3860b82

### 182. [198-42 D4] GREEN-07 remains Pending on exact ancestry while Task 1, Task 2, and summary commit categories remain inside every local commit.
expected: |
  GREEN-07 remains Pending on exact ancestry while Task 1, Task 2, and summary commit categories remain inside every local commit.
requirement: GREEN-07
result: pass
source: automated
coverage_id: D4
verification: |
  - [pass] Plan-level origin/main..HEAD count and lifecycle-category verification

### 183. [198-42 D5] Rounds 1–6 and 198-UAT.md remain byte-identical; no machine or human-judgment UAT row changes.
expected: |
  Rounds 1–6 and 198-UAT.md remain byte-identical; no machine or human-judgment UAT row changes.
requirement: GREEN-08
result: pass
source: automated
coverage_id: D5
verification: |
  - [pass] sealed prefix SHA-256 25b4b4658b44b61636bebbc4c570904bd0fda07ce73a7d28196f42b56f474ba6; UAT SHA-256 1383aa92e587ac9006ed45d6f7d411dffa31cec169584bde67b185ef631793f6

## Summary

total: 183
passed: 168
issues: 15
pending: 0
skipped: 0
blocked: 0

## Gaps

- id: G-198-1
  truth: "Phase 201 sizing implications are derived from machine-readable probe results and explicit policy, with no human ratification step."
  status: failed
  reason: "The probe is automated, but its planning implication exists only as subjective prose."
  artifacts:
    - path: ".planning/phases/198-green-bringup/198-01-SUMMARY.md"
      issue: "D4 is classified human_judgment."
  missing:
    - "A deterministic sizing-policy contract tied to the recorded probe output."
- id: G-198-2
  truth: "Minimum-lane failures are classified mechanically as code/test defects or toolchain drift."
  status: failed
  reason: "Identical lane evidence exists, but D7 still delegates the conclusion to a maintainer."
  artifacts:
    - path: ".planning/phases/198-green-bringup/198-03-SUMMARY.md"
      issue: "D7 is classified human_judgment despite differential evidence."
  missing:
    - "An executable differential-classification assertion."
- id: G-198-3
  truth: "The search_path root cause is reproduced and classified by an integration contract."
  status: failed
  reason: "The evidence is reproducible but the summary still requires human diagnosis."
  artifacts:
    - path: ".planning/phases/198-green-bringup/198-04-SUMMARY.md"
      issue: "D8 is classified human_judgment."
  missing:
    - "A fresh-database integration assertion distinguishing test-side search_path defects from stale state."
- id: G-198-4
  truth: "CI fail-fast stops a synthetic broken Playwright run after five failures and retains its trace and boot evidence."
  status: failed
  reason: "Static wiring is covered; the failure path has not been executed."
  artifacts:
    - path: "examples/threadline_phoenix/e2e/playwright.config.ts"
      issue: "maxFailures is configured but lacks an isolated behavioral smoke test."
  missing:
    - "A deterministic synthetic fail-fast smoke fixture and trace-retention assertion."
- id: G-198-5
  truth: "The browser-full lane's trigger, full-project command, and create-or-update issue behavior are executable in integration tests."
  status: failed
  reason: "Workflow shape is checked, but runtime issue behavior depends on an unobserved live event."
  artifacts:
    - path: ".github/workflows/browser-full.yml"
      issue: "Issue upsert logic is embedded in the workflow and cannot be fixture-tested."
  missing:
    - "A reusable issue-upsert boundary with create-then-update integration fixtures."
- id: G-198-6
  truth: "Every CI timeout is validated against a committed measurement and explicit budget policy."
  status: failed
  reason: "Timeout presence is automated, but adequacy is subjective."
  artifacts:
    - path: "test/threadline/phase198_nyquist_contract_test.exs"
      issue: "It asserts bounds exist, not how each bound is derived."
  missing:
    - "Machine-readable timeout budgets and a policy check against runtime attestations."
- id: G-198-7
  truth: "A non-publishing release smoke test proves the production environment gate and swappable Hex authentication boundary."
  status: failed
  reason: "Static and API configuration checks do not exercise the release job boundary."
  artifacts:
    - path: ".github/workflows/release.yml"
      issue: "No safe dry-run path verifies gating without publishing."
  missing:
    - "A side-effect-free release-gate smoke mode and contract test."
- id: G-198-8
  truth: "Flake issue create-or-update deduplication is verified without live workflow dispatches."
  status: failed
  reason: "Classifier behavior is covered; issue upsert behavior is not."
  artifacts:
    - path: ".github/workflows/flake-detection.yml"
      issue: "Deduplication is inline and live-event dependent."
  missing:
    - "Fixture-driven create-then-update coverage for the shared issue-upsert boundary."
- id: G-198-9
  truth: "The newest canonical exact-SHA main CI run is selected and reported automatically, including explicit not-observed and incomplete states."
  status: failed
  reason: "The current summary asks for human confirmation and cites stale failure evidence."
  artifacts:
    - path: ".planning/phases/198-green-bringup/198-07-SUMMARY.md"
      issue: "D3 is human_judgment rather than a durable attestation contract."
  missing:
    - "A read-only exact-SHA main-run verifier with deterministic state output."
- id: G-198-10
  truth: "The verify.example disposition follows current CI evidence and cannot be silently ignored."
  status: failed
  reason: "The old out-of-scope framing is stale now that a green required run exists."
  artifacts:
    - path: ".planning/phases/198-green-bringup/198-12-SUMMARY.md"
      issue: "D5 remains a human judgment based on superseded evidence."
  missing:
    - "A CI-attestation assertion that reconciles verify.example outcome and disposition."
- id: G-198-11
  truth: "GREEN-04 and GREEN-07 statuses are derived consistently from the same attestation in every requirements view."
  status: failed
  reason: "Consistency is automatable but remains a blocking human checkpoint."
  artifacts:
    - path: ".planning/phases/198-green-bringup/198-18-SUMMARY.md"
      issue: "D3 is classified human_judgment."
  missing:
    - "A cross-view requirements/attestation consistency contract."
- id: G-198-12
  truth: "The operator-accessibility row-history failure has an established root cause and focused E2E regression."
  status: failed
  reason: "The discovery is recorded without a confirmed root cause."
  artifacts:
    - path: "examples/threadline_phoenix/e2e/operator-accessibility.spec.ts"
      issue: "Failure near the row-history scenario remains unexplained."
  missing:
    - "A focused reproduction, root-cause fix, and passing regression test."
- id: G-198-13
  truth: "Round predictions and targets are scored from machine-readable observations."
  status: failed
  reason: "Accuracy and miss classification depend on prose-level candour review."
  artifacts:
    - path: ".planning/phases/198-green-bringup/198-29-SUMMARY.md"
      issue: "D3 is classified human_judgment."
  missing:
    - "A deterministic target/prediction scoring schema and checker."
- id: G-198-14
  truth: "Capped and unbounded Playwright measurements carry validated population metadata."
  status: failed
  reason: "The non-nested-population caveat is prose only."
  artifacts:
    - path: ".planning/phases/198-green-bringup/198-31-SUMMARY.md"
      issue: "D4 cannot currently be checked mechanically."
  missing:
    - "Machine-readable measurement population fields and comparison rules."
- id: G-198-15
  truth: "Conclusion accuracy and composition accuracy are computed separately."
  status: failed
  reason: "Composition-level misses still require subjective review."
  artifacts:
    - path: ".planning/phases/198-green-bringup/198-37-SUMMARY.md"
      issue: "D4 is classified human_judgment."
  missing:
    - "A deterministic two-level scoring rubric with regression fixtures."
