---
phase: 198-green-bringup
plan: 58
type: execute
wave: 7
depends_on: [198-57]
files_modified:
  - .planning/audits/198-round12-prohibition-resolution.json
  - .planning/audits/198-round12-prohibition-resolution.md
  - test/threadline/phase198_prohibition_resolution_contract_test.exs
autonomous: true
gap_closure: true
requirements: [GREEN-12]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All five Plan-53/55 prohibitions have an explicit verification tier and named evidence; mechanically knowable outcomes use test, while the unreconstructable historical command-method negative remains judgment-pending."
    - "No retrospective argv, refspec, force-state, timestamp, or before/after value is invented for the completed round-11 execution."
    - "The new round-12 record is append-only evidence that cites, but does not mutate, the completed round-11 artifacts."
    - "T-198-55-03 remains an irrecoverable below-threshold open finding unless a distinct truthful attestation supplies every missing receipt field; it is not accepted and does not count as a blocking `threats_open` item."
  artifacts:
    - ".planning/audits/198-round12-prohibition-resolution.json — strict five-row test/judgment ledger plus threat mapping"
    - ".planning/audits/198-round12-prohibition-resolution.md — readable evidence and explicit historical limitation"
    - "test/threadline/phase198_prohibition_resolution_contract_test.exs — exact row, tier, evidence, and anti-fabrication contract"
  key_links:
    - "Plan prohibition -> tier -> named evidence -> passing test or pending human attestation"
    - "round-11 immutable digest -> round-12 citation -> no invented receipt fields"
  prohibitions:
    - {statement: "Historical command evidence must not be reconstructed from final Git state or executor narration.", status: pending, verification: test}
---

<objective>
Resolve the Plan-53/55 prohibition accounting honestly and prepare the sole irreducible human attestation.

Purpose: verification found five untyped/flagged prohibitions; four are mechanically decidable, while the exact commands historically typed cannot be recovered from Git state.
Output: a new round-12 evidence schema with four passing test-tier rows and one explicitly pending judgment-tier row consumed by Plan 59.

<assumption_delta_decision>Primary noun: prohibition evidence. Decision: promote — every prohibition is a typed claim with a verification reference, not an unstructured flag.</assumption_delta_decision>

### Multi-source coverage audit

- GOAL/REQ: GREEN-12 governance evidence is covered without altering its verified final state; GREEN-04 is covered by Plan 56; GREEN-07 remains Pending.
- CONTEXT: D-31 restoration evidence and D-39/D-42 non-promotion/protected-gate boundaries are preserved.
- RESEARCH/PATTERNS: test executable state, escalate irreducible judgment, and keep audit history append-only.
- VERIFICATION/SECURITY: all five prohibition gaps and the retrospective-receipt limitation are represented; none is silently accepted.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-53-PLAN.md
@.planning/phases/198-green-bringup/198-55-PLAN.md
@.planning/phases/198-green-bringup/198-55-SUMMARY.md
@.planning/audits/198-round11-ref-disposition.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build the exact five-prohibition typed ledger</name>
  <files>.planning/audits/198-round12-prohibition-resolution.json, .planning/audits/198-round12-prohibition-resolution.md, test/threadline/phase198_prohibition_resolution_contract_test.exs</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-53-PLAN.md — two exact prohibition statements
    - .planning/phases/198-green-bringup/198-55-PLAN.md — three exact prohibition statements
    - .planning/phases/198-green-bringup/198-VERIFICATION.md — tier/evidence gap and historical-command limitation
    - .planning/phases/198-green-bringup/198-SECURITY.md — open receipt/authority/control findings
    - .planning/audits/198-round11-ref-disposition.json — immutable source digest and mechanically visible outcomes
  </read_first>
  <behavior>
    - Test 1: exactly five rows exist and each copies one Plan-53/55 prohibition byte-for-byte with source plan and stable ID
    - Test 2: recommendation/history/silence authority rejection is tier test with passing validator mutation references
    - Test 3: distinct same-name preservation subjects are tier test with passing inventory/fixture references
    - Test 4: preserve-before-retire and GREEN-07 non-promotion are tier test with passing ordering/final-state references
    - Test 5: historical command-method negative is tier judgment, status pending, with no argv/refspec/force/timestamp/before/after evidence claim before Plan 59
  </behavior>
  <action>Before creating either ledger artifact, write the exact-five-row schema/anti-fabrication fixture and run the focused test to an observed RED caused by the absent round-12 ledger or its deliberately malformed fixture; record the RED command and observed cause in the eventual Plan-58 summary, then commit that failing test before implementation. Create a strict JSON ledger and matching Markdown rendering for exactly the two Plan-53 and three Plan-55 prohibitions. Assign four mechanically observable statements to `test` and cite exact passing test names/commands from the hardened ref-disposition suite. Assign the broad historical command-method statement to `judgment` with pending status and a precise explanation that final refs, abstract receipts, and summary prose cannot establish which command operands were typed. Include SHA-256 digests of both completed round-11 artifacts and reject later mutation of those inputs. Map every row to the relevant round-11 threat IDs without marking the pending judgment closed. Add T-198-55-03 as an irrecoverable, below-threshold open finding: its missing argv, per-operation timestamps, exit status, and before/after identities cannot be truthfully reconstructed; it is neither closed nor accepted and is excluded only from the canonical blocking `threats_open` count by its documented severity/threshold classification.</action>
  <verify>
    <automated>mix test test/threadline/phase198_prohibition_resolution_contract_test.exs test/threadline/phase198_ref_disposition_contract_test.exs</automated>
    <fails_when>The row set differs from exactly five, a tier/evidence field is absent, a test-tier ref does not name a passing executable check, the historical-method row claims mechanical proof, or a completed round-11 digest changes.</fails_when>
  </verify>
  <done>Every prohibition is typed and evidence-linked, with the single irreducible judgment still visibly pending.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove append-only and anti-fabrication boundaries</name>
  <files>test/threadline/phase198_prohibition_resolution_contract_test.exs, .planning/audits/198-round12-prohibition-resolution.json, .planning/audits/198-round12-prohibition-resolution.md</files>
  <read_first>
    - test/threadline/phase198_prohibition_resolution_contract_test.exs — Task-1 schema and source digest checks
    - .planning/audits/198-round11-ref-disposition.md — readable historical receipts lacking exact argv
    - .planning/phases/198-green-bringup/198-55-SUMMARY.md — narration boundary and documented failed first push
  </read_first>
  <behavior>
    - Test 1: adding retrospective command fields before a signed Plan-59 attestation fails
    - Test 2: changing a source prohibition, source digest, tier, evidence result, or threat mapping fails
    - Test 3: judgment may become resolved only through one exact Plan-59 attestation object with verbatim text, timestamp, signer, and explicit cannot-attest alternative
  </behavior>
  <action>Extend the already-observed RED test before changing the schema implementation: add a malformed attestation/receipt fixture whose invented argv or incomplete receipt fields fail for the anti-fabrication reason, run it RED, and preserve the command and observed cause in the Plan-58 summary. Then add mutation fixtures for every schema field and an explicit anti-fabrication rule: the round-12 ledger may record the absence of historical command proof, but may not synthesize command arrays or convert the Plan-55 summary into such proof. Define the only permitted judgment transition for Plan 59 as a separately recorded verbatim maintainer attestation or a `cannot-attest` outcome. Neither outcome is risk acceptance. A cannot-attest outcome keeps the judgment row open; T-198-55-03 remains separately open below threshold unless an attestation truthfully supplies every missing receipt field, not merely the broad prohibited-method statement.</action>
  <verify>
    <automated>mix test test/threadline/phase198_prohibition_resolution_contract_test.exs</automated>
    <fails_when>Fabricated command fields pass, source history can be rewritten unnoticed, unsigned judgment resolution passes, or cannot-attest is converted into closure.</fails_when>
  </verify>
  <done>The new ledger can be completed only by evidence or explicit human judgment, never retrospective invention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| historical narration -> audit claim | Final state and summaries cannot prove exact historical command operands. |
| plan flags -> verification verdict | Untyped prohibitions must not disappear from closeout accounting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-58-01 | Repudiation | prohibition ledger | high | mitigate | Exact five-row source join, explicit tier, named evidence, and mutation fixtures. |
| T-198-58-02 | Tampering | historical evidence | high | mitigate | Immutable source digests and rejection of synthesized argv/refspec/force/timing fields. |
| T-198-58-03 | Spoofing | judgment resolution | high | mitigate | Only verbatim signed attestation or cannot-attest transition accepted. |
</threat_model>

<verification>
- Run both task commands and `git diff --check`.
- Confirm `git diff -- .planning/audits/198-round11-ref-disposition.json .planning/audits/198-round11-ref-disposition.md` is empty.
- The Plan-59 checkpoint must remain blocking-human even under auto mode.
</verification>

<success_criteria>
- Five of five prohibitions have a tier and evidence status.
- Four mechanically decidable rows pass; one historical-method row remains pending judgment.
- T-198-55-03 is explicitly open below threshold and is never described as closed or accepted from incomplete retrospective evidence.
- No historical command evidence is fabricated and no risk is accepted.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-58-SUMMARY.md` when done.</output>
