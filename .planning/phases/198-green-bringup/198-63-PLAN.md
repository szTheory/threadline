---
phase: 198-green-bringup
plan: 63
type: execute
wave: 12
depends_on: [198-62]
files_modified:
  - .planning/audits/198-round14-security-disposition.json
  - test/threadline/phase198_prohibition_resolution_contract_test.exs
autonomous: false
gap_closure: true
gap_ids: [T-198-55-02]
requirements: [GREEN-12]
estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "T-198-55-02 is never closed from final ref state, abstract receipts, Plan-55 summary prose, reconstructed argv, or the truthful `cannot-attest by szTheory` response."
    - "The maintainer chooses one of three explicit outcomes: identify contemporaneous immutable historical evidence for independent validation, explicitly accept the narrowly stated security risk, or decline acceptance and stop with Phase 198 still blocked."
    - "Risk acceptance is never the default and is never inferred from silence, non-attestation, plan approval, execution approval, or a request to finish the phase."
    - "If the maintainer identifies historical evidence, the record binds the exact pre-existing tracked path, commit, blob, SHA-256, signer, timestamp, and verbatim response without copying absent command fields into a new artifact."
    - "If the maintainer explicitly accepts the risk, the record says that exact historical argv/non-force proof remains unavailable, scopes acceptance only to T-198-55-02, and leaves T-198-55-03 open/not accepted and GREEN-07 accepted-Pending."
    - "If the maintainer declines acceptance, execution writes only the standard 198-63 summary with `status: halted`, writes no disposition artifact, invokes no canonical audit, and leaves 198-SECURITY.md blocked."
    - "Only the canonical security workflow may add an Accepted Risks Log row or change T-198-55-02, and phase verification runs only after that security result."
  artifacts:
    - ".planning/audits/198-round14-security-disposition.json — conditional, attributed evidence-pointer or explicit-risk-acceptance record; absent on the decline-and-stop branch"
    - "test/threadline/phase198_prohibition_resolution_contract_test.exs — deterministic branch schema, immutable-source, anti-reconstruction, scope, and non-promotion checks"
  key_links:
    - "maintainer checkpoint response -> exact disposition enum and verbatim attribution -> deterministic contract -> Plan-63 summary -> canonical security re-audit -> canonical phase re-verification"
    - "historical-evidence branch -> pre-existing tracked source at an immutable Git object -> independent security-auditor validation; the new disposition record is only a pointer, never replacement evidence"
    - "explicit-risk-acceptance branch -> T-198-55-02 only -> SECURITY.md Accepted Risks Log via `$gsd-secure-phase 198`; no evidence claim and no propagation to T-198-55-03 or GREEN-07"
  assumptions:
    - {statement: "[GREEN-01/unclassified] Manual review remains required; historical coverage is in the completed measurement plans and is not re-opened by this security-disposition plan.", status: unverified, flagged: true}
    - {statement: "[GREEN-02/concurrency] Interrupted or parallel measurement guarantees remain governed by the completed measurement artifacts; this plan adds no measurement execution.", status: unverified, flagged: true}
    - {statement: "[GREEN-03/empty] Empty, singleton, and null probe inputs remain an unresolved spec-less edge assumption already outside this decision-only gap.", status: unverified, flagged: true}
    - {statement: "[GREEN-03/encoding] Byte/code-point/grapheme/normalization semantics remain an unresolved spec-less edge assumption already outside this decision-only gap.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/boundary] Exact threshold and one-step-either-side behavior remains an unresolved spec-less edge assumption historically exercised by completed test plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/adjacency] Equality/touching behavior remains an unresolved spec-less edge assumption historically exercised by completed test plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/empty] Empty, singleton, and null test-input behavior remains an unresolved spec-less edge assumption historically exercised by completed test plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/encoding] Byte/code-point/grapheme/normalization semantics remain an unresolved spec-less edge assumption historically exercised by completed test plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/ordering] Stable ordering for equal elements remains an unresolved spec-less edge assumption historically exercised by completed test plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/precision] Precision/overflow/rounding/tie-breaking behavior remains an unresolved spec-less edge assumption historically exercised by completed test plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-05/unclassified] Manual review remains required; the self-declaring form-policy work is historically complete and is not re-opened here.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/boundary] Timeout/failure thresholds and one-step-either-side behavior remain an unresolved spec-less edge assumption historically covered by the completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/adjacency] Equality/touching behavior remains an unresolved spec-less edge assumption historically covered by the completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/empty] Empty, singleton, and null job-set behavior remains an unresolved spec-less edge assumption historically covered by the completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/ordering] Stable ordering for equal job results remains an unresolved spec-less edge assumption historically covered by the completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/precision] Duration precision and threshold rounding remain an unresolved spec-less edge assumption historically covered by the completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/boundary] The exact 20-minute boundary and one-step-either-side behavior remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/adjacency] Equal/touching CI-duration or ancestry states remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/empty] Empty, singleton, and null run/result behavior remains unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/encoding] Identity/name normalization semantics remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/ordering] Equal-result ordering remains unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/precision] CI duration precision and tie-breaking remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/concurrency] Interrupted or parallel main-run guarantees remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-08/empty] Empty, singleton, and null emitted-check-set behavior remains an unresolved spec-less edge assumption historically covered by branch-protection contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-08/encoding] Check-name equality and normalization semantics remain an unresolved spec-less edge assumption historically covered by branch-protection contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-08/concurrency] Interrupted or parallel check-emission guarantees remain an unresolved spec-less edge assumption historically covered by branch-protection contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-09/unclassified] Manual review remains required; the paid-trigger removal is historically complete and is not re-opened here.", status: unverified, flagged: true}
    - {statement: "[GREEN-10/unclassified] Manual review remains required; the single publish path is historically complete and is not re-opened here.", status: unverified, flagged: true}
    - {statement: "[GREEN-11/empty] Empty, singleton, and null flake-run behavior remains an unresolved spec-less edge assumption historically covered by completed classifier plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-11/encoding] Flake-name equality and normalization semantics remain an unresolved spec-less edge assumption historically covered by completed classifier plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-12/adjacency] Equal/touching ref identities remain an unresolved spec-less edge assumption historically covered by the completed preservation contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-12/empty] Empty, singleton, and null ref-set behavior remains an unresolved spec-less edge assumption historically covered by the completed preservation contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-12/ordering] Equal-ref ordering remains an unresolved spec-less edge assumption historically covered by the completed preservation contracts.", status: unverified, flagged: true}
  prohibitions:
    - {statement: "Historical uncertainty must not be presented as evidence, mitigation, or implicit consent; `cannot-attest by szTheory` remains truthful non-evidence unless the maintainer separately chooses explicit risk acceptance.", status: unverified, flagged: true}
    - {statement: "The executor must not choose security-risk acceptance for the maintainer or conceal the decline-and-stop branch behind an implementation task.", status: unverified, flagged: true}
    - {statement: "GREEN-07 must not be promoted from accepted-Pending, and repository-hygiene/security disposition must not be described as satisfying its exact-main predicate.", status: unverified, flagged: true}
---

<objective>
Provide the only truthful executable path for the remaining Phase-198 blocker without inventing historical evidence or preselecting risk acceptance.

Purpose: canonical Round-14 verification says every fixable engineering gap is closed and T-198-55-02 remains high/blocking/open/not accepted solely because the completed Plan-55 session did not retain exact argv/non-force method evidence. This plan therefore gates the one-way security disposition on an explicit maintainer choice and preserves a real non-acceptance stop branch.

Output: on a positive evidence or acceptance choice only, one attributed machine-validated disposition record and a Plan-63 summary that hands control to canonical security first and canonical verification second. On a decline, no artifact is written and the phase remains blocked.

Tracer-first is non-applicable: the gap is solely an irreducible human security decision after all implementation paths were verified closed. A manufactured code tracer would create unrelated work and could not establish a historical fact or accept risk.

<assumption_delta_decision>Typed scan returned detected=true for the historical words `another`, `fallback`, and `optional`. Decision: no-change. Those signals occur in already-completed phase history and do not create a second disposition mechanism; Plan 63 exposes exactly the three explicit branches below.</assumption_delta_decision>

### Active contribution checks

- API coverage detector returned `detected: true` only because the historical phase scope mentions GitHub API measurement. The existing tracked `COVERAGE.md` reasoned no-external-product-API declaration remains authoritative and unchanged; this plan adds no API capability matrix.
- ORM/schema-push scan found historical Ecto/schema/migration language but no schema push in this gap. No schema-push task is injected.
- Security enforcement is ASVS L1 and blocks on high; T-198-55-02 is therefore the sole blocking target.

### Artifacts this phase produces

- Conditional new file `.planning/audits/198-round14-security-disposition.json`. Both positive branches have exactly the common keys `schema_version`, `phase`, `threat_id`, `decision`, `verbatim`, `decided_by`, `decided_at`, `historical_evidence_reconstructed`, `excluded_threats`, and `green_07`. The `historical-evidence` branch adds exactly `evidence_source.{path,commit,blob,sha256}` and forbids `rationale` and `accepted_scope`. The `accept-risk` branch adds exactly `rationale` and `accepted_scope` and forbids `evidence_source`.
- New test cases and helper validation in `Threadline.Phase198ProhibitionResolutionContractTest`; no production symbol, endpoint, flag, dependency, schema, workflow, ruleset, CI, scorecard, tag, or non-current branch is created or changed. The current branch advances only through the scoped disposition/test and summary commits, and the plan invokes no external-state command or mutation.
- On the decline-and-stop branch: only `.planning/phases/198-green-bringup/198-63-SUMMARY.md` is created with `status: halted`; no disposition file, production symbol, field, flag, canonical verdict, or automated-coverage claim is created.

### Multi-source coverage audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Green bringup goal and honest terminal state | 01-62, 63 | COVERED | Completed engineering evidence is preserved; this plan addresses only its blocking security gate. |
| REQ | GREEN-01..GREEN-06 | Measurement, test, form-policy, and bounded-CI outcomes | 01-62 | COVERED | Historical plans and Round-14 verification cover these; Plan 63 changes none. |
| REQ | GREEN-07 | Exact-main CI outcome | 39, 61-63 | COVERED | Explicitly remains accepted-Pending and is never restated as met. |
| REQ | GREEN-08..GREEN-11 | Protection, critic, publish, and flake outcomes | 01-62 | COVERED | Historical plans and Round-14 verification cover these; Plan 63 changes none. |
| REQ | GREEN-12 | Preservation and truthful repository-hygiene evidence | 53-63 | COVERED | T-198-55-02 originated in Plan 55's GREEN-12 mutation evidence; Plan 63 supplies only the decision path. |
| RESEARCH | — | Fail-closed, attributable, durable evidence and bounded authority | 63 | COVERED | Exact enum, verbatim signer/time, immutable evidence pointer, no reconstruction, and canonical ownership. |
| CONTEXT | D-01..D-30 | Locked implementation decisions | 01-62 | COVERED | Already delivered and independently verified; no gap-plan task reopens them. |
| CONTEXT | D-31, D-34 | Preservation metadata and irreversible ordering | 53-63 | COVERED | Immutable preservation history remains unchanged; checkpoint precedes the conditional one-way disposition. |
| CONTEXT | D-32..D-38 | Locked archive/measurement decisions | 01-62 | COVERED | Already delivered and independently verified; no gap-plan task reopens them. |
| CONTEXT | D-39, D-42 | GREEN-07 disposition and aggregate-gate interlock | 63 | COVERED | No CI/protection mutation; accepted-Pending is preserved literally. |
| CONTEXT | D-40, D-41 | Completed lane dispositions | 20-62 | COVERED | Historical coverage retained; unrelated to the remaining security choice. |
| VERIFICATION | T-198-55-02 | Truthful historical evidence or explicit maintainer risk disposition | 63 | COVERED | Exact three-branch checkpoint with non-acceptance stop path. |
| UAT | — | Zero remaining UAT gaps | 46-62 | COVERED | No UAT task is invented. |

All 33 deterministic spec-less edge rows are surfaced as flagged assumptions in frontmatter. Existing plans provide historical coverage where noted, but none is silently promoted from unresolved. The two-stage prohibition recall dropped routine engineering candidates and canon security/compliance candidates; the latter remain owned by `$gsd-secure-phase`. The three kept bespoke transparency/safety prohibitions are descriptor-less and flagged-unverified above.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-55-SUMMARY.md
@.planning/phases/198-green-bringup/198-59-SUMMARY.md
@.planning/phases/198-green-bringup/198-62-SUMMARY.md
@.planning/audits/198-round11-ref-disposition.json
@.planning/audits/198-round11-ref-disposition.md
@.planning/audits/198-round12-prohibition-resolution.json
@.planning/audits/198-round13-terminal-certification.md
@test/threadline/phase198_prohibition_resolution_contract_test.exs
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Choose evidence, narrow risk acceptance, or an honest stop</name>
  <files>.planning/phases/198-green-bringup/198-63-SUMMARY.md (decline branch only); `/tmp/threadline-gsd-198-63-$(id -u)/protected-baseline.sha256`, `continuation.lock`, and `continuation.lock/owner` are executor-local scratch metadata outside the repository, not tracked deliverables.</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-VERIFICATION.md — authoritative Round-14 gap, exact closure condition, and GREEN-07 accepted-Pending state
    - .planning/phases/198-green-bringup/198-SECURITY.md — ASVS L1/high blocking threshold and T-198-55-02 high/open/not-accepted row
    - .planning/audits/198-round12-prohibition-resolution.json — exact `cannot-attest by szTheory` non-evidence and `risk_accepted: false`
    - .planning/audits/198-round13-terminal-certification.md — immutable two-finding state and non-reconstruction declaration
    - .planning/phases/198-green-bringup/198-55-SUMMARY.md — completed mutation commits `ce5bd1ff` and `1cedb8e2`; outcome evidence that does not reveal argv
  </read_first>
  <decision>Choose how T-198-55-02 proceeds: identify genuinely pre-existing historical command-method evidence for independent audit, explicitly accept only this historical-proof risk, or decline acceptance and stop with the security gate blocked.</decision>
  <context>All fixable engineering gaps are closed. Final refs, archive joins, abstract receipts, later strict validators, summary prose, and `cannot-attest by szTheory` cannot prove which argv or force mode was used during completed Plan 55. Risk acceptance does not make that evidence exist; it only records the maintainer's informed disposition of the residual uncertainty.</context>
  <action>Before presenting the checkpoint, require the protected repository paths named by the automated command to match HEAD. Set `umask 077`, derive the exact handoff directory as `/tmp/threadline-gsd-198-63-$(id -u)`, and create it with one non-`-p` `mkdir -m 0700`; an existing path is a collision from another or stale Plan-63 execution and must halt without reading, deleting, or replacing anything. Fail closed unless `stat -f` proves the new path is a real directory rather than a symlink, is owned by the current uid, and has mode 0700. Create the baseline with `mktemp` inside that validated directory, prove it is a regular non-symlink file owned by the current uid with mode 0600 and one link, write the protected worktree-byte identities plus all tag and non-current-branch ref identities, validate it again, then use no-clobber `mv -n` to atomically rename it to the exact continuation handoff path `/tmp/threadline-gsd-198-63-$(id -u)/protected-baseline.sha256`; require the unpredictable temporary name to disappear and validate the renamed file before presenting the checkpoint. Never use a predictable temporary filename, shell redirection to the final handoff path, overwrite an existing handoff, or reuse another run's scratch directory. This absolute uid-scoped path is outside both the main checkout and harness worktree, so it survives the checkpoint's fresh continuation and cannot be staged or committed as a repository deliverable. Present the three options and their consequences exactly as written, then halt for the maintainer's response.

Immediately after the checkpoint resumes, before parsing or otherwise consuming the selected response, reading the baseline, writing a decline summary, or entering Task 2, establish the sole continuation owner. Set `umask 077`; bind `uid=$(id -u)`, `scratch=/tmp/threadline-gsd-198-63-$uid`, `baseline=$scratch/protected-baseline.sha256`, and `claim=$scratch/continuation.lock`; revalidate the scratch directory without reading its contents; create `"$claim"` exactly once with non-`-p` `mkdir -m 0700 "$claim"`; and halt on any pre-existing claim, because it represents a concurrent or stale continuation. Generate a fresh 64-lowercase-hex `THREADLINE_198_63_CLAIM_TOKEN` from `/dev/urandom` and retain that exact value in the fresh continuation context; never recover or derive it from disk. Create `continuation.lock/owner` through a `mktemp` mode-0600 one-link regular file and atomic no-clobber rename. Use the macOS-compatible sequence `claim_token=$(dd if=/dev/urandom bs=32 count=1 2>/dev/null | shasum -a 256 | cut -d " " -f 1)`, `THREADLINE_198_63_CLAIM_TOKEN=$claim_token`, `baseline_sha=$(shasum -a 256 "$baseline" | cut -d " " -f 1)`, `owner_tmp=$(mktemp "$claim/.owner.XXXXXXXX")`, `printf "schema=threadline-gsd-198-63-claim-v1\\nuid=%s\\ntoken=%s\\nbaseline_sha256=%s\\n" "$uid" "$THREADLINE_198_63_CLAIM_TOKEN" "$baseline_sha" > "$owner_tmp"`, validate the temporary owner with `stat -f "%HT:%u:%Lp:%l"`, then require `test ! -e "$claim/owner"` and `test ! -L "$claim/owner"`, publish it with `mv -n "$owner_tmp" "$claim/owner"`, and require the temporary path to be absent. Its exact four-line format is `schema=threadline-gsd-198-63-claim-v1`, `uid=UID`, `token=TOKEN`, and `baseline_sha256=SHA256`, where SHA256 is the 64-lowercase-hex digest of `protected-baseline.sha256`. Validate the claim directory and owner file with macOS `stat -f`, then require every subsequent shell/tool invocation in either decline or positive processing to receive the retained token as `THREADLINE_198_63_CLAIM_TOKEN` and fail closed unless the already-existing claim directory and owner file have the exact type, owner, mode, link count, schema, uid, token, and current baseline digest. No later invocation may acquire, replace, repair, or remove the claim before a terminal commit.

Only under that validated existing claim, parse the complete response and validate its signer before any branch writes anything: the signer must be 1..200 printable UTF-8 characters, with control characters, PEM blocks, and case-insensitive secret-bearing assignment or header forms for authorization, cookie, set-cookie, api-key, secret, token, and password rejected. Do not select a branch from prior prose, plan approval, execution approval, auto mode, or silence. The exact decline and both positive forms use this same claim protocol. If the response, signer, or branch-specific payload is malformed or unknown, stop without a summary or disposition and retain the claim, owner token, baseline, and scratch directory so the response can be corrected by the owning continuation; an interrupted owner likewise retains all scratch state. A continuation without the retained token, or one encountering a pre-existing claim it did not create, fails closed and does not delete or replace the baseline. If the response exactly matches `do-not-accept by SIGNER` and the signer passes those rules, create and commit only the normal `198-63-SUMMARY.md` with `status: halted`, `coverage: []`, zero completed automated deliverables, the verbatim attributed decline in narrative, no disposition artifact, no security/verification invocation, and no claim of GREEN-12 or threat closure. After that status-halted summary commit, perform explicit post-commit cleanup under the still-valid owner token: remove only `continuation.lock/owner`, remove the now-empty `continuation.lock`, revalidate the baseline and scratch directory, remove the baseline, and remove the now-empty scratch directory; then stop before Task 2. Only an exact `historical-evidence` or `accept-risk` response whose signer passes the same rules may enter Task 2 while retaining the same claim and token.</action>
  <options>
    <option id="historical-evidence">
      <name>Identify historical evidence</name>
      <pros>May let the canonical security auditor close T-198-55-02 as mitigated if a contemporaneous immutable source really contains the exact one-object argv and non-force facts.</pros>
      <cons>The source must already exist in tracked history at or before completed mutation commit `1cedb8e2`; a new recollection, rewritten receipt, or derived final-state narrative is rejected.</cons>
    </option>
    <option id="accept-risk">
      <name>Explicitly accept risk</name>
      <pros>Records an attributable, narrowly scoped maintainer decision that permits the security workflow to close the high finding as accepted risk without pretending historical proof exists.</pros>
      <cons>The missing historical evidence remains missing forever; the accepted-risk record becomes durable milestone history and must name a concrete rationale.</cons>
    </option>
    <option id="do-not-accept">
      <name>Do not accept; stop</name>
      <pros>Preserves the current evidence standard and makes no assertion beyond what is known.</pros>
      <cons>Phase 198 remains security-blocked and cannot advance through canonical verification.</cons>
    </option>
  </options>
  <resume-signal>Reply exactly `historical-evidence PATH at COMMIT by SIGNER`, `accept-risk by SIGNER: RATIONALE`, or `do-not-accept by SIGNER`. On `do-not-accept`, create only the standard `198-63-SUMMARY.md` with `status: halted`, then halt before Task 2. Any ambiguous or differently scoped answer remains blocked, writes no tracked file, and must not be rounded to acceptance.</resume-signal>
  <reversibility rating="one-way">The accept-risk branch becomes durable milestone history for a completed destructive-operation session whose missing command record cannot later be recreated; the checkpoint must precede any recorded acceptance.</reversibility>
  <verify>
    <automated>bash -lc 'set -euo pipefail; umask 077; uid=$(id -u); scratch=/tmp/threadline-gsd-198-63-$uid; baseline=$scratch/protected-baseline.sha256; protected=(.planning/phases/198-green-bringup/198-SECURITY.md .planning/phases/198-green-bringup/198-VERIFICATION.md .planning/phases/198-green-bringup/198-VALIDATION.md .planning/REQUIREMENTS.md .planning/audits/198-round11-ref-disposition.json .planning/audits/198-round11-ref-disposition.md .planning/audits/198-round12-prohibition-resolution.json .planning/audits/198-round13-terminal-certification.md .github/workflows .github/rulesets .planning/scorecards); git diff --quiet HEAD -- "${protected[@]}"; test ! -e "$scratch" &amp;&amp; test ! -L "$scratch"; mkdir -m 0700 "$scratch"; tmp=; cleanup() { if [ -n "$tmp" ] &amp;&amp; [ -e "$tmp" ]; then rm -f "$tmp"; fi; if [ ! -e "$baseline" ]; then rmdir "$scratch" 2&gt;/dev/null || true; fi; }; trap cleanup EXIT HUP INT TERM; test ! -L "$scratch"; test "$(stat -f "%HT:%u:%Lp" "$scratch")" = "Directory:$uid:700"; tmp=$(mktemp "$scratch/.protected-baseline.XXXXXXXX"); test ! -L "$tmp"; test "$(stat -f "%HT:%u:%Lp:%l" "$tmp")" = "Regular File:$uid:600:1"; current=$(git symbolic-ref --quiet --short HEAD); { git ls-files --cached --others --exclude-standard -- "${protected[@]}" | while IFS= read -r path; do printf "%s  %s\n" "$(git hash-object --no-filters -- "$path")" "$path"; done; git for-each-ref --format="%(refname) %(objectname)" refs/heads refs/tags | awk -v current="refs/heads/$current" "\$1 != current"; } | shasum -a 256 &gt; "$tmp"; test -s "$tmp"; test ! -L "$tmp"; test "$(stat -f "%HT:%u:%Lp:%l" "$tmp")" = "Regular File:$uid:600:1"; test ! -e "$baseline" &amp;&amp; test ! -L "$baseline"; mv -n "$tmp" "$baseline"; test ! -e "$tmp"; tmp=; test -s "$baseline"; test ! -L "$baseline"; test "$(stat -f "%HT:%u:%Lp:%l" "$baseline")" = "Regular File:$uid:600:1"; trap - EXIT HUP INT TERM'</automated>
    <fails_when>Any protected path differs from HEAD before the checkpoint; the uid-scoped scratch path already exists; the directory is a symlink, has the wrong owner, or is not mode 0700; `umask 077` is not in force before creation; `mktemp` fails or yields a symlink, non-regular file, wrong owner, mode other than 0600, or link count other than one; the no-clobber atomic rename collides, overwrites a handoff, leaves the temporary file behind, or yields an invalid final file; the baseline is written under `.git`; after resume the continuation parses or consumes the response, reads the baseline, or writes any tracked file before exclusively creating and validating the four-line claim owner; a pre-existing claim is consumed, replaced, repaired, or removed; any later invocation lacks the retained owner token or accepts a mismatched claim; an ambiguous response produces a summary; any branch writes a summary or disposition when its signer is empty, over 200 characters, non-printable UTF-8, contains control characters or a PEM block, or matches a secret-bearing authorization/cookie/set-cookie/api-key/secret/token/password assignment or header form; the exact valid decline produces anything other than a status-halted summary; claim or scratch state is removed before the terminal summary commit; or execution continues to Task 2 after decline.</fails_when>
  </verify>
  <acceptance_criteria>
    - On fresh continuation and before the response or baseline is consumed, Task 1 exclusively creates and validates one durable mode-0700 continuation claim whose mode-0600 one-link owner file binds the current uid, a retained 64-lowercase-hex owner token, and the baseline digest; decline and both positive branches require that same already-existing owner identity on every later invocation.
    - Before every branch may write anything, the maintainer's response exactly matches one allowed form and its signer is 1..200 printable UTF-8 characters; control characters, PEM blocks, and case-insensitive secret-bearing authorization/cookie/set-cookie/api-key/secret/token/password assignment or header forms are rejected.
    - No answer is inferred from plan approval, execution approval, silence, prior `cannot-attest`, or auto mode.
    - `historical-evidence` names a repository-relative tracked path and a full commit; Task 1 makes no claim that the source is sufficient.
    - `accept-risk` includes a non-empty maintainer rationale and scopes acceptance only to T-198-55-02's unavailable historical argv/non-force proof.
    - Task 1 exclusively creates a uid-owned mode-0700 non-symlink scratch directory, writes through a uid-owned mode-0600 one-link regular file returned by `mktemp`, and atomically no-clobber renames it to the exact handoff path; any existing path or validation failure halts instead of overwriting or consuming another execution's baseline.
    - `do-not-accept` with a valid signer halts before Task 2, creates only a standard summary with `status: halted` and `coverage: []`, creates no disposition artifact, leaves canonical security blocked, and releases the claim then scratch only in explicit cleanup after the summary commit; a malformed answer, invalid signer, or interrupted owning continuation creates neither summary nor disposition and retains the claim and baseline for correction, while a stale or concurrent continuation without the retained token fails closed.
    - GREEN-07 remains accepted-Pending under D-39, and no D-42-governed CI/protection path changes.
  </acceptance_criteria>
  <done>The fresh continuation owns one durable validated claim before it consumes any response or baseline; a valid explicit evidence-pointer or risk-acceptance response proceeds to Task 2 under that same owner token, an exact non-acceptance is durably indexed by a status-halted summary only and then explicitly releases the claim and scratch, and an ambiguous or interrupted owning continuation stops with no tracked mutation while preserving the claim and baseline for correction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and validate only the selected positive disposition</name>
  <reversibility rating="one-way">On the accept-risk branch, the attributed acceptance is append-only evidence for canonical security and cannot be erased without rewriting the milestone audit trail; later mitigation may supersede but not delete it.</reversibility>
  <files>.planning/audits/198-round14-security-disposition.json, test/threadline/phase198_prohibition_resolution_contract_test.exs</files>
  <read_first>
    - test/threadline/phase198_prohibition_resolution_contract_test.exs — existing exact-literal, digest-binding, anti-fabrication, and non-attestation validation patterns
    - .planning/audits/198-round12-prohibition-resolution.json — stable source identities, pending P-198-55-01 row, and T-198-55-03 state
    - .planning/audits/198-round13-terminal-certification.md — exact open-finding set and GREEN-07 terminal state
    - .planning/phases/198-green-bringup/198-SECURITY.md — canonical accepted-risk log ownership and the precise high finding text
  </read_first>
  <behavior>
    - "The artifact accepts exactly decision `historical-evidence` or `accept-risk`; it never accepts `do-not-accept`, silence, unknown values, or an implicit default."
    - "Both positive branches require exact threat ID T-198-55-02, verbatim checkpoint response, non-empty signer, executor-recorded RFC3339 UTC timestamp, `historical_evidence_reconstructed: false`, GREEN-07 `accepted-Pending`, and excluded threat T-198-55-03."
    - "The evidence branch has exactly the ten common keys plus `evidence_source`; it requires one repository-relative git-tracked path, a full commit at or before and ancestral to Plan-55 completion commit `1cedb8e2`, exact `commit:path` blob identity, and SHA-256 of those immutable bytes; it forbids `rationale`, `accepted_scope`, argv, and method claims."
    - "The risk branch has exactly the ten common keys plus `rationale` and `accepted_scope`; it requires a non-empty bounded maintainer rationale, `accepted_scope` equal to the historical exact-argv/non-force uncertainty for completed Plan 55, forbids `evidence_source`, and makes no assertion that the threat was mitigated or that missing evidence exists."
    - "Signer is 1..200 printable UTF-8 characters and rationale is 1..500 printable UTF-8 characters; both reject control characters, PEM blocks, and case-insensitive secret-bearing assignments or header forms for authorization, cookie, set-cookie, api-key, secret, token, and password."
    - "Mutating either exact branch key set, decision, threat, signer, timestamp, verbatim text, scope, exclusion, GREEN-07 state, evidence temporal boundary, blob, digest, reconstruction flag, or sensitive-content bounds fails closed."
  </behavior>
  <action>Task 2 inherits the continuation claim and retained `THREADLINE_198_63_CLAIM_TOKEN` established by Task 1; it must not acquire, regenerate, repair, rename, replace, or remove the claim, owner record, baseline, or scratch directory. Before reading the handoff or mutating a tracked file, set `umask 077`, derive `/tmp/threadline-gsd-198-63-$(id -u)`, and fail closed unless the already-existing scratch directory, `protected-baseline.sha256`, `continuation.lock`, and `continuation.lock/owner` match the exact type, uid, modes, link count, four-line schema, retained token, and current baseline digest established in Task 1. Repeat that owner validation at the start of every separate shell/tool invocation, passing the retained token explicitly rather than reading it from the owner file. A missing or mismatched owner is stale/concurrent state and must halt without tracked mutation or cleanup. Write the test first and observe it fail because the selected positive disposition artifact does not exist. Then create `.planning/audits/198-round14-security-disposition.json` from the exact checkpoint response. Enforce the exact per-branch key sets from the behavior block, rejecting both missing and additional keys. For `historical-evidence`, store only the immutable source pointer and identities, prove the path is tracked at the supplied commit, prove the commit is an ancestor of or equal to `1cedb8e2`, resolve `COMMIT:PATH` to a blob, hash its bytes, forbid `rationale` and `accepted_scope`, and do not transcribe, normalize, reconstruct, or infer any historical argv field; the canonical auditor decides sufficiency. For `accept-risk`, store the exact response, parsed signer, rationale, exact narrow scope, and explicit `historical_evidence_reconstructed: false`; forbid `evidence_source`, and do not mark mitigation, accept T-198-55-03, close P-198-55-01 as attested, or change GREEN-07. Validate signer/rationale bounds and sensitive-content exclusions exactly as specified in behavior. Extend the existing contract with positive and mutation fixtures for both branches, including missing/extra/wrong-branch keys, a post-Plan-55 evidence commit, a working-tree-only path, a non-blob, digest substitution, missing/overlong/control-character rationale, secret/header-shaped rationale examples, broadened scope, and every implicit/decline outcome. Preserve the captured paths and ref identities byte-for-byte; the current branch may advance only through the disposition/test and summary commits. Do not invoke a network command or any external-state mutation; external state is intentionally not attested by this local plan. After the focused test and baseline comparison pass, commit the two Task-2 files and create and commit the Plan-63 summary with `status: complete` and `human_judgment: false` coverage limited to mechanical persistence/schema/integrity. Keep the same claim present through that terminal summary commit. Only afterward perform explicit post-commit cleanup under the still-valid owner token: remove `continuation.lock/owner`, remove the now-empty `continuation.lock`, revalidate the baseline and scratch directory, remove the baseline, and remove the now-empty scratch directory. Quote the human choice only in narrative and the dedicated disposition record. Return control to the execute-phase orchestrator: canonical `$gsd-secure-phase 198` must evaluate or log the disposition first, and canonical phase verification may run only after security completes.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; umask 077; : "${THREADLINE_198_63_CLAIM_TOKEN:?retained Task-1 claim token is required}"; test "${#THREADLINE_198_63_CLAIM_TOKEN}" -eq 64; case "$THREADLINE_198_63_CLAIM_TOKEN" in (*[!0-9a-f]*) exit 1;; esac; uid=$(id -u); scratch=/tmp/threadline-gsd-198-63-$uid; baseline=$scratch/protected-baseline.sha256; claim=$scratch/continuation.lock; owner=$claim/owner; test ! -L "$scratch"; test "$(stat -f "%HT:%u:%Lp" "$scratch")" = "Directory:$uid:700"; test -s "$baseline"; test ! -L "$baseline"; test "$(stat -f "%HT:%u:%Lp:%l" "$baseline")" = "Regular File:$uid:600:1"; test ! -L "$claim"; test "$(stat -f "%HT:%u:%Lp" "$claim")" = "Directory:$uid:700"; test -s "$owner"; test ! -L "$owner"; test "$(stat -f "%HT:%u:%Lp:%l" "$owner")" = "Regular File:$uid:600:1"; test "$(wc -l &lt; "$owner" | tr -d " ")" = 4; test "$(sed -n "1p" "$owner")" = schema=threadline-gsd-198-63-claim-v1; test "$(sed -n "2p" "$owner")" = uid=$uid; test "$(sed -n "3p" "$owner")" = token=$THREADLINE_198_63_CLAIM_TOKEN; baseline_sha=$(shasum -a 256 "$baseline" | awk "{print \$1}"); test "$(sed -n "4p" "$owner")" = baseline_sha256=$baseline_sha; mix test test/threadline/phase198_prohibition_resolution_contract_test.exs | tee /tmp/threadline-198-63-test.log; grep -Eq "[1-9][0-9]* tests?, 0 failures" /tmp/threadline-198-63-test.log; protected=(.planning/phases/198-green-bringup/198-SECURITY.md .planning/phases/198-green-bringup/198-VERIFICATION.md .planning/phases/198-green-bringup/198-VALIDATION.md .planning/REQUIREMENTS.md .planning/audits/198-round11-ref-disposition.json .planning/audits/198-round11-ref-disposition.md .planning/audits/198-round12-prohibition-resolution.json .planning/audits/198-round13-terminal-certification.md .github/workflows .github/rulesets .planning/scorecards); current=$(git symbolic-ref --quiet --short HEAD); { git ls-files --cached --others --exclude-standard -- "${protected[@]}" | while IFS= read -r path; do printf "%s  %s\\n" "$(git hash-object --no-filters -- "$path")" "$path"; done; git for-each-ref --format="%(refname) %(objectname)" refs/heads refs/tags | awk -v current="refs/heads/$current" "\$1 != current"; } | shasum -a 256 | cmp -s "$baseline" -; test -d "$claim"; test -f "$owner"; test "$(sed -n "3p" "$owner")" = token=$THREADLINE_198_63_CLAIM_TOKEN'</automated>
    <fails_when>The retained claim token is missing or malformed; the already-existing scratch directory, baseline, claim, or owner file is missing, symlinked, empty, has the wrong type/owner/mode/link count/schema/uid/token/baseline digest, or would need acquisition, regeneration, repair, replacement, or early removal; another execution can act without the retained owner token; the focused test exits nonzero or reports zero tests; either branch accepts the wrong key set; a decline/unknown/implicit answer validates; attribution, timestamp, sensitive-content bounds, scope, exclusions, GREEN-07, temporal Git identity, blob/digest, or anti-reconstruction checks are absent or loose; the independent protected-byte/ref comparison differs; claim or scratch is removed before a terminal summary commit; or the executor invokes or claims a canonical audit or external-state mutation.</fails_when>
  </verify>
  <acceptance_criteria>
    - The test is observed red before the artifact exists and green only after one exact positive branch is represented.
    - The artifact contains every common field and exactly the branch-specific fields listed in `Artifacts this phase produces`; no unlisted historical command field appears.
    - Evidence mode points to pre-existing immutable tracked bytes at or before `1cedb8e2`, forbids `rationale` and `accepted_scope`, and leaves sufficiency to canonical security audit.
    - Risk mode carries explicit signer/verbatim/rationale, forbids `evidence_source`, and accepts only T-198-55-02 without asserting mitigation or evidence.
    - Signer and rationale satisfy the length/printability limits and reject the enumerated credential/header forms; exact-key-set and sensitive-content mutation fixtures fail closed.
    - The post-task protected-byte/ref digest equals the pre-checkpoint baseline loaded from the exact uid-scoped scratch handoff path independent of staged or committed state; Task 2 runs only under Task 1's already-existing claim and retained token, validates the directory, baseline, claim, and owner at every invocation, never independently acquires or removes the claim, retains all scratch state after interruption, and releases the claim and scratch only in explicit cleanup after the positive summary commit; no external-state command was invoked or attested.
    - T-198-55-03 remains medium/nonblocking/open/not accepted; GREEN-07 remains accepted-Pending; `cannot-attest by szTheory` remains unchanged.
    - The Plan-63 summary claims only automated artifact integrity with `human_judgment: false` and routes security before verification.
  </acceptance_criteria>
  <done>One explicit positive maintainer disposition is durably and mechanically recorded under the same Task-1 continuation owner through the status-complete summary commit, then the claim and scratch are explicitly released; canonical security now has the sole authority to close or keep the threat open before phase re-verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| maintainer response -> disposition record | A human choice crosses into machine-readable security evidence and must remain exact, attributable, and non-defaulted. |
| historical source pointer -> security finding | A newly named source may be genuine only if immutable tracked bytes predate or coincide with completed Plan-55 execution. |
| explicit risk acceptance -> canonical SECURITY.md | Acceptance can close a high finding only through the security workflow's accepted-risk log; a plan artifact cannot self-approve its consequence. |
| plan executor -> canonical post-hooks | The executor may persist selected input and test integrity but cannot author SECURITY/VERIFICATION verdicts. |
| shared temporary namespace -> checkpoint continuation | Predictable `/tmp` names can be substituted or raced unless directory ownership, permissions, unpredictable file creation, atomic no-clobber publication, and one durable token-bound continuation claim all fail closed. |

## STRIDE Threat Register — ASVS L1, block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-63-01 | Repudiation | maintainer disposition | high | mitigate | Require exact verbatim response, signer, rationale where applicable, and RFC3339 UTC recording time under a blocking-human checkpoint. |
| T-198-63-02 | Spoofing | historical evidence source | high | mitigate | Require tracked repository-relative path, pre-/at-Plan-55 ancestral commit, exact blob, and SHA-256; canonical security independently judges sufficiency. |
| T-198-63-03 | Tampering | historical method record | high | mitigate | Forbid reconstructed argv/method fields and mutation-test every identity, scope, state, and temporal boundary. |
| T-198-63-04 | Elevation of Privilege | auto/implicit acceptance | high | mitigate | Provide no default, preserve a stop branch, reject decline/unknown values in the artifact schema, and halt on ambiguity. |
| T-198-63-05 | Information Disclosure | disposition rationale | medium | mitigate | Enforce 1..200 signer and 1..500 rationale printable-UTF-8 bounds; reject control characters, PEM blocks, and secret-bearing authorization/cookie/set-cookie/api-key/secret/token/password forms with explicit mutation fixtures. |
| T-198-63-06 | Tampering | canonical verdict ordering | high | mitigate | Capture pre-checkpoint worktree-byte plus tag/non-current-branch identities for every enumerated protected repository path and compare after Task 2 independently of index/commit state; orchestrator runs security first and verification second after the summary. |
| T-198-63-07 | Tampering | checkpoint scratch handoff | high | mitigate | Exclusively create the uid-owned scratch directory and baseline before the checkpoint; immediately after resume and before consuming the response or baseline, create one mode-0700 claim whose mode-0600 owner record binds uid, retained random token, and baseline digest; require that same already-existing owner for decline and positive work through the terminal summary commit, then clean up explicitly. |
</threat_model>

<verification>
- At Task 1, set `umask 077`, exclusively create and validate the uid-scoped mode-0700 scratch directory, and publish a validated mode-0600 `mktemp` baseline through atomic no-clobber rename. Immediately after resume and before consuming the response or baseline, create and validate one token-bound persistent claim; require that owner for all three branches, preserve it with the baseline for malformed or interrupted owner continuations, and keep it through an exact decline's status-halted summary commit before explicit cleanup.
- At Task 2, observe the missing-artifact RED, then run the complete prohibition-resolution contract with nonzero test count and all branch/mutation cases passing.
- Before Task 2 reads the baseline, assert the retained token owns the already-existing claim and validate the scratch directory, baseline, claim directory, and owner record without acquiring, replacing, or removing them. Require the after-state digest for canonical reports, sealed Round-11/12/13 evidence, REQUIREMENTS, workflows, rulesets, scorecards, tags, and non-current branches to match the pre-checkpoint baseline independently of index/commit state; keep the claim through the status-complete summary commit, then clean it and the scratch state explicitly; make no external-state attestation.
- A positive Plan-63 summary has `status: complete` and records mechanical integrity only; a decline summary has `status: halted` and `coverage: []`; neither claims security closure.
- After the summary exists, the execute-phase orchestrator runs canonical security first. Only if that workflow returns a non-blocked ASVS-L1 result may canonical phase verification run.
</verification>

<success_criteria>
- No historical command fact is inferred, reconstructed, or laundered from final state, summary prose, strict future validators, or `cannot-attest`.
- The maintainer can explicitly identify genuine historical evidence, explicitly accept only T-198-55-02, or decline and stop without hidden mutation.
- A positive record is exact, attributed, immutable-source-aware, narrowly scoped, adversarially tested, and leaves T-198-55-03 and GREEN-07 unchanged.
- A decline produces only the standard status-halted Plan-63 summary under the same continuation owner, produces no disposition artifact, leaves the existing high security blocker intact without being re-offered as an incomplete runnable plan, and releases claim plus scratch only after that commit.
- Canonical security remains the only writer of accepted-risk/verdict state and runs before canonical verification.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-63-SUMMARY.md` on either designed terminal branch while holding the same token-bound continuation claim established immediately after checkpoint resume. After Task 2 completes on a valid positive branch, use `status: complete`; coverage is mechanical-only (`human_judgment: false`) and states that canonical security and phase verification have not yet run. On exact `do-not-accept`, create only the normal summary with `status: halted`, `coverage: []`, no completed automated deliverable or closure claim, and no disposition artifact/security/verification invocation. Release the claim and scratch only in explicit post-commit cleanup. Malformed or ambiguous input creates no summary, retains the owner state and baseline, and remains resumable only by the owning continuation; stale or concurrent continuations fail closed.</output>
