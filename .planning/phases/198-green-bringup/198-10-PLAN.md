---
phase: 198-green-bringup
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [GREEN-07]
estimate:
  tokens: 65000
  raw_tokens: 33000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`mix verify.mechanical` reaches a live database in CI instead of dying on connection refused"
    - "The Tier A evidence-bundle assertion states the real bundle size and cannot rot at the next capture change"
    - "The example-app browser E2E job resolves the audit tables the demo seed queries unqualified"
    - "No job id and no job name that feeds the `CI required` aggregate changes"
  artifacts:
    - ".github/workflows/ci.yml — postgres service on verify-mechanical; rot-proof evidence assertion on verify-capture; schema resolution step on verify-example-browser"
  key_links:
    - "verify-mechanical -> mix verify.mechanical -> MechanicalChecker.run/1 over .planning/scorecards/*.json -> requires a reachable Repo"
    - "verify-example-browser -> mix verify.example_browser -> examples/threadline_phoenix demo seed -> unqualified audit table reads -> needs the example DB's schema resolution, exactly as verify-capture already sets up"
    - "every verify-* job -> ci-required (`CI required`) -> the single required status context on main -> PR #26 mergeability"
---

<objective>
Close the two remaining newly diagnosed red CI jobs plus the example-app schema-resolution
failure. All three live in `.github/workflows/ci.yml` and none is a test-suite defect.

Purpose: three of the eight red jobs on run `33138291361` fail for reasons that have nothing to
do with GREEN-04's 79 test-side defects. Verification stopped at "then diagnose the 6 remaining
red jobs"; they are now diagnosed and each gets a concrete task here. Until all three are green
the `CI required` aggregate cannot pass, so GREEN-07 and PR #26 stay blocked.

Output: a `ci.yml` in which `Mechanical checker`, `Tier A capture lane`, and
`Example app browser E2E (Playwright)` can pass.

**Hard constraint that governs every task in this plan.** Keep every job `id:` and every job
`name:` in `ci.yml` byte-identical to its current value. The live ruleset requires the single
context `CI required`, byte-identical to the aggregate job's `name:`, with `bypass_actors: []`
— renaming any job that feeds a required context hard-locks `main` with no actor able to walk
past it. Job `id:`s are additionally the durable `needs:` contract per CLAUDE.md. This plan
edits only `services:`, `steps:`, and step `run:` bodies.

**Also out of scope by maintainer confirmation:** no change to branch protection, to
`.github/rulesets/main.json`, or to `bypass_actors`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give `verify-mechanical` the database it needs</name>
  <read_first>
    - .github/workflows/ci.yml lines 388-418 (the `verify-mechanical` job as it stands: `runs-on`, `timeout-minutes: 10`, checkout, setup-beam, deps cache, `mix deps.get`, `mix verify.mechanical` — and NO `services:` block and no DB env)
    - .github/workflows/ci.yml lines 425-460 (the `verify-capture` job — the in-repo reference implementation: `env: DB_HOST/DB_PORT` plus `services: postgres: image: postgres:16` with the `pg_isready` health options at :431-437)
    - config/test.exs (the `DB_HOST` / `DB_PORT` env vars `Threadline.Test.Repo` reads, and its defaults)
    - mix.exs (the `verify.mechanical` alias definition — confirm exactly which test files it runs before assuming what it needs)
  </read_first>
  <precondition>Docker-backed GitHub-hosted `services:` are available on the `ubuntu-24.04` runner this job uses — already true for `verify-capture` and `verify-test` in the same workflow.</precondition>
  <action>
    `mix verify.mechanical` currently dies with
    `(DBConnection.ConnectionError) tcp connect (localhost:5432): connection refused -
    :econnrefused`. The job header comment claims the gate is "Pure Elixir — no DB, no browser",
    which is false: the alias runs `mechanical_checker_test.exs`, whose real-evidence gate starts
    the application and therefore the repo.

    Add to the `verify-mechanical` job, copying the shape from `verify-capture` verbatim:
    a job-level `env:` block setting `DB_HOST: localhost` and `DB_PORT: 5432`, and a `services:`
    block with a `postgres` service on `image: postgres:16`, `POSTGRES_USER: postgres`,
    `POSTGRES_PASSWORD: postgres`, `POSTGRES_DB: postgres`, port mapping `5432:5432`, and the
    same `--health-cmd pg_isready` options.

    Add a `mix test.setup`-equivalent database preparation step only if a run without one still
    fails — determine this by running the job's exact command sequence locally against a clean
    database first, and record which of the two shapes was actually required. Do not guess.

    Also correct the job's header comment so it no longer asserts "no DB" — a comment that
    contradicts the job is the exact anti-pattern verification flagged as a Blocker elsewhere in
    this phase.

    Do not change the job `id:` (`verify-mechanical`) or `name:`
    (`Mechanical checker (committed scorecards)`). Raise `timeout-minutes` from 10 only if the
    measured local runtime with the service exceeds 6 minutes; state the measured number.
  </action>
  <verify>
    <automated>mix verify.mechanical</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.mechanical` exits 0 locally against a database prepared exactly as the job's step sequence prepares it.
    - `.github/workflows/ci.yml` `verify-mechanical` job contains a `services:` key with a `postgres` entry — verified by parsing the workflow, not by eyeballing.
    - `grep -c 'id: verify-mechanical' .github/workflows/ci.yml` and the job's `name:` line are unchanged from `git show HEAD:.github/workflows/ci.yml`.
    - The job header comment no longer claims the gate needs no database.
    - `mix test test/threadline/ci_topology_contract_test.exs test/threadline/phase06_nyquist_ci_contract_test.exs` shows no NEW failures caused by this edit (the List 1 parity failure owned by plan 198-12 may still be present; record which failures are pre-existing).
  </acceptance_criteria>
  <done>The `Mechanical checker (committed scorecards)` job can reach a database, and the job's own comment matches what it does.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the rotted Tier A evidence-count literal with a shape assertion</name>
  <read_first>
    - .github/workflows/ci.yml lines 480-500 (the "Assert complete evidence bundle" step and the `ALTER DATABASE ... SET search_path` step immediately above it, whose comment explains why the example DB needs it)
    - .planning/scorecards/ (run `find .planning/scorecards -maxdepth 1 -name '*.json' | wc -l` and the same for `*.aria.yml` — the measured truth is 366 JSON and 54 aria.yml, against a hardcoded expectation of 120 and 54)
    - test/threadline/operator_surface/ui_form_policy_contract_test.exs (the derived-roster + explicit non-empty assertion idiom this step should imitate in shell form)
    - .planning/REQUIREMENTS.md (GREEN-04's own text: "version-pinned milestone literals replaced by shape assertions that cannot rot at the next milestone" — the rule this task applies)
  </read_first>
  <action>
    The `Tier A capture lane (byte-stable evidence)` job fails at its
    "Assert complete evidence bundle" step with
    `::error::expected 120 scorecard JSON + 54 aria.yml, got 366 + 54`. The aria count is
    correct; the `120` rotted when the capture set grew.

    **Do not bump 120 to 366.** That reproduces the exact failure class GREEN-04 names. Replace
    the count comparison with a shape assertion that cannot rot:

    - assert the JSON count is greater than zero, and that its failure message says a zero count
      means the capture produced nothing (this is the non-vacuity floor — the current form would
      also catch a broken glob only by accident);
    - assert the aria.yml count is greater than zero on the same footing;
    - assert the RELATIONSHIP that actually encodes bundle completeness rather than either
      absolute size — every `*.aria.yml` has a same-stem `*.json` sibling, and report the
      set difference by name when it does not hold. This is what "complete bundle" means, and it
      stays true at any bundle size.

    Keep the existing `echo` line that prints both counts, so the observed numbers stay visible
    in the log for future sizing without being asserted against.

    Update the step `name:` and the job header comment, which both currently state "120 + 54", so
    no reader is left with a number the step no longer checks. The job `id:` (`verify-capture`)
    and `name:` (`Tier A capture lane (byte-stable evidence)`) stay byte-identical.

    Verify by running the new shell body directly against the working tree's real
    `.planning/scorecards/` directory, and separately against a temporary empty directory to
    confirm the zero case fails.
  </action>
  <verify>
    <automated>bash -c 'json=$(find .planning/scorecards -maxdepth 1 -name "*.json" | wc -l | tr -d " "); aria=$(find .planning/scorecards -maxdepth 1 -name "*.aria.yml" | wc -l | tr -d " "); test "$json" -gt 0 && test "$aria" -gt 0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '120' .github/workflows/ci.yml` returns 0 occurrences within the `verify-capture` job's evidence-bundle step, step name, and header comment (state the remaining repo-wide occurrences and why each is unrelated).
    - The new step body, executed verbatim against the real `.planning/scorecards/` directory, exits 0.
    - The same body, executed against an empty temporary directory, exits non-zero with a message naming a zero count — recorded verbatim in the SUMMARY as the non-vacuity proof.
    - The same body, executed against a temporary directory holding one orphan `*.aria.yml` with no `*.json` sibling, exits non-zero and names the orphan stem.
    - `git diff .github/workflows/ci.yml` shows no change to any line beginning with `  verify-` at job-key indentation, and no change to any `name:` line belonging to a job.
  </acceptance_criteria>
  <done>The Tier A bundle assertion states a truth that survives the next capture-set change, and it has been shown to fail on both an empty bundle and an incomplete one.</done>
</task>

<task type="auto">
  <name>Task 3: Make the example-app browser lane resolve the audit tables the demo seed reads</name>
  <read_first>
    - .github/workflows/ci.yml lines 294-350 (the `verify-example-browser` job: its `services: postgres` block, and the "Ensure threadline_phoenix_test database exists" step at ~:344-348 which runs `createdb` and NOTHING else)
    - .github/workflows/ci.yml lines 474-486 (the `verify-capture` job's equivalent step — it runs the same `createdb` AND an `ALTER DATABASE threadline_phoenix_test SET search_path ...` with a comment explaining the demo seed queries the audit tables unqualified)
    - examples/threadline_phoenix/e2e/run-e2e.sh (the boot preflight; understand where in the sequence the seed runs and where the failure surfaces)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-02 — confirm the prohibition is scoped to the LIBRARY test database `threadline_test`, not to the example app's `threadline_phoenix_test`)
  </read_first>
  <action>
    The `Example app browser E2E (Playwright)` job fails with
    `relation "audit_transactions" does not exist`, then
    `** (Mix) verify.example_browser failed (1)`.

    Root cause, already diagnosed — do not re-investigate: the `ALTER DATABASE
    threadline_phoenix_test SET search_path ...` statement exists ONLY in the `verify-capture`
    job's database-preparation step. The `verify-example-browser` job's own preparation step runs
    `createdb` alone, so the demo seed's unqualified audit-table reads have nothing to resolve
    against. The statement is not "not reaching" this job; it was never in it.

    Add the identical `psql` statement to `verify-example-browser`'s "Ensure
    threadline_phoenix_test database exists" step, immediately after the `createdb`, carrying the
    same explanatory comment and the same `PGPASSWORD: postgres` env.

    This is the one place in the repository where that statement is legitimate: it targets the
    EXAMPLE APP's database, not `threadline_test`. D-02's prohibition covers the library's own
    test database and `Threadline.Test.Repo`, and neither is touched here. Do not remove it from
    `verify-capture`, and do not add it to any job that runs `mix test` for the library.

    Preferring a duplicated two-line statement over a shared composite action is deliberate at
    two call sites; note in the SUMMARY that a third call site should be the trigger to extract
    one.

    Do not change the job `id:` (`verify-example-browser`) or `name:`
    (`Example app browser E2E (Playwright)`) — D-17 made both byte-identical to their pre-split
    values precisely because this job stays inside `CI required` and votes.
  </action>
  <verify>
    <automated>mix verify.example_browser --project=desktop-chromium</automated>
  </verify>
  <acceptance_criteria>
    - The `verify-example-browser` job's database-preparation step contains an `ALTER DATABASE threadline_phoenix_test` statement, and the `verify-capture` job's still does — both confirmed by parsing the workflow.
    - No job whose steps invoke `mix verify.test`, `mix test`, or `mix ci.all` for the LIBRARY contains any `ALTER DATABASE threadline_test` statement — assert this by listing the jobs that reference `threadline_test` and showing the list is empty.
    - `mix verify.example_browser --project=desktop-chromium` exits 0 locally against a `threadline_phoenix_test` database prepared exactly as the amended step prepares it, OR the SUMMARY records verbatim why local execution is not possible and names the CI run that will be the proof.
    - `git diff .github/workflows/ci.yml` shows no change to the `verify-example-browser` job's `id:` or `name:` lines.
  </acceptance_criteria>
  <done>The example-app browser lane prepares its database the same way the capture lane already does, and the library's own test database is provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job -> required status context `CI required` | A renamed job silently detaches from the required context and hard-locks `main` |
| CI assertion -> maintainer trust | An assertion pinned to a rotting literal fails for the wrong reason and trains reviewers to ignore it |
| example-app DB setup -> library test DB | Ambient schema resolution must not leak from the example app into the library's own suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-10-01 | Denial of Service | job `name:` feeding `CI required` with `bypass_actors: []` | critical | mitigate | Every task forbids `id:`/`name:` edits, and acceptance criteria diff those lines explicitly against `git show HEAD` |
| T-198-10-02 | Tampering | Tier A evidence-bundle assertion | high | mitigate | Replace the rotted literal with a shape assertion proven to fail on both an empty and an incomplete bundle |
| T-198-10-03 | Elevation of Privilege | ambient schema resolution bleeding into `threadline_test` | high | mitigate | Acceptance criterion asserts no library-suite job carries an `ALTER DATABASE threadline_test` statement; plan 198-08's mask guard is the durable backstop |
| T-198-10-04 | Spoofing | none — no credential or identity path changes | low | accept | This plan adds only a local postgres service and a database-preparation statement; no secret is read or written |
</threat_model>

<verification>
- `mix verify.mechanical` exits 0.
- The Tier A bundle assertion body exits 0 against the real scorecard directory and non-zero against an empty one and an orphan-file one.
- `mix verify.example_browser --project=desktop-chromium` exits 0, or the SUMMARY names the CI run that proves it.
- Diff of `.github/workflows/ci.yml` touches no job key, no job `id:`, and no job `name:`.
- `mix test test/threadline/ci_topology_contract_test.exs` exits 0.
</verification>

<success_criteria>
Three of the eight red jobs on run `33138291361` have their diagnosed root causes fixed in
`ci.yml`, with the required-context surface provably unchanged.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol | Kind | Path |
|---|---|---|
| `services.postgres` on the `verify-mechanical` job | new job-level config | `.github/workflows/ci.yml` |
| `env.DB_HOST` / `env.DB_PORT` on the `verify-mechanical` job | new job-level config | `.github/workflows/ci.yml` |
| rewritten "Assert complete evidence bundle" step body | changed step | `.github/workflows/ci.yml` (`verify-capture` job) |
| `ALTER DATABASE threadline_phoenix_test` statement in "Ensure threadline_phoenix_test database exists" | changed step | `.github/workflows/ci.yml` (`verify-example-browser` job) |

**No new job keys. No new step ids. No changed job `id:` or `name:`. No new mix aliases.**
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/198-green-bringup/198-10-SUMMARY.md` when done
</output>
