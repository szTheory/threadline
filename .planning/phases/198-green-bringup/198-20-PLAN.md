---
phase: 198-green-bringup
plan: 20
type: execute
wave: 2
gap_closure: true
depends_on:
  - 198-19
files_modified:
  - .planning/audits/198-ci-required-aggregate-decision.md
  - .planning/phases/198-green-bringup/198-CONTEXT.md
autonomous: false
requirements:
  - GREEN-07

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A written decision brief exists on disk enumerating every available disposition for the two structurally-blocking lanes (`Tier A capture lane` / `verify-capture`, and `Example app browser E2E (Playwright)` / `verify-example-browser`), each with its honest consequence stated as: what `CI required` would still guarantee if that option is taken."
    - "The brief states plainly, for each option, whether GREEN-07 can close under it and whether the closure is genuine or definitional."
    - "The maintainer — not the planner and not the executor — records the chosen disposition for each of the two lanes, and the choice is written into `198-CONTEXT.md` as a numbered locked decision with a reversibility rating."
    - "The brief does not re-open the Tier-A `page.*` regeneration prohibition, which is maintainer-ratified twice; the only question it puts is the narrower one about the aggregate's `needs:` membership."
    - "The brief does not re-open GREEN-07's wording; the round-1 amendment rested on a false premise (`.planning/` was already public — 2191 tracked files) and was correctly retracted."
    - statement: "Empty edge (GREEN-07): the brief states explicitly what `CI required` would still guarantee at each proposed `needs:` size, including the degenerate reading — an aggregate that needs nothing asserts nothing — so no option can be chosen without its guarantee being named."
      verification: explicit
    - statement: "Adjacency edge (GREEN-07): the brief distinguishes a lane that is red from a lane that is skipped or absent, because under `re-actors/alls-green` a skipped `needs:` member fails the gate unless allow-listed, while an absent member is simply not asserted — one character of YAML apart, materially different guarantees."
      verification: explicit
    - statement: "Ordering edge (GREEN-07): the brief records that the two lane decisions are independent and may be answered differently, and that the answers are applied in a single diff so no intermediate state publishes a guarantee nobody chose."
      verification: explicit
  artifacts:
    - ".planning/audits/198-ci-required-aggregate-decision.md — the decision brief with per-option consequences"
    - ".planning/phases/198-green-bringup/198-CONTEXT.md — new locked decisions recording the maintainer's answers"
  key_links:
    - "`ci-required`'s `needs:` list → what the single required status check `CI required` actually proves → `.github/rulesets/main.json`'s `required_status_checks` singleton"
    - "198-16's diagnosis (`scroll_cost` coupled to stress-lab catalog size) → the Tier-A regeneration prohibition → the lane's permanent red"
    - "`playwright.config.ts:141` `maxFailures: 5` → the 28 previously-masked failures → `deferred-items.md` / WINDOWS.md #8"
  prohibitions:
    - statement: "Tier-A `page.*` evidence regeneration is forbidden for this milestone (maintainer-ratified twice, most recently 2026-08-28). This plan may describe regeneration as an option's precondition but may not perform it."
      status: recalled
      verification: flagged-unverified
    - statement: "`ALTER DATABASE ... SET search_path` must never be applied to the `threadline_test` database; it is valid for the example-app database only."
      status: recalled
      verification: flagged-unverified
    - statement: "GREEN-04 and GREEN-07 must not be marked Complete on local-only evidence."
      status: recalled
      verification: flagged-unverified
    - statement: "The executor must not narrow `CI required`'s `needs:` list on its own judgment. Removing a lane changes the merge gate's published guarantee and is a maintainer decision."
      status: recalled
      verification: flagged-unverified
    - statement: "No option may be implemented in this plan. This plan produces a brief and a recorded answer only; implementation is plan 198-21."
      status: recalled
      verification: flagged-unverified
---

<objective>
Surface — and get a maintainer answer to — the one question round 3 cannot answer
by engineering: whether `CI required` should keep `needs:`-ing the two lanes that
are structurally red for reasons outside this phase's authority.

- **`verify-capture` / `Tier A capture lane`** — cause fully diagnosed in 198-16
  (a document-wide `scrollHeight` read couples `scroll_cost` to the stress-lab
  catalog size). Every remedy requires Tier-A `page.*` regeneration, forbidden this
  milestone and re-ratified by the maintainer this session. While this lane is in
  the aggregate, `CI required` cannot conclude `success`, so GREEN-07 cannot close.
- **`verify-example-browser` / `Example app browser E2E (Playwright)`** — 198-17
  fixed its 5 diagnosed failures (confirmed held on run `33197493051`) and thereby
  revealed 28 further pre-existing failures that `playwright.config.ts:141`'s
  `maxFailures: 5` had always masked. Deliberately deferred by maintainer decision.
  Same structural effect.

Purpose: `198-VERIFICATION.md` names both as maintainer decisions, and
REVERSIBILITY_GATES rates "remove a lane from the merge gate" as one-way — it
changes what the published gate proves. The failure mode this plan exists to
prevent is either horn: an executor silently narrowing the gate to manufacture a
green, or an executor silently leaving GREEN-07 permanently uncloseable and calling
that "blocked".

Output: a decision brief with honest per-option consequences, and the maintainer's
recorded answer as a locked decision. **No implementation** — that is 198-21.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/deferred-items.md
@.planning/phases/198-green-bringup/198-16-SUMMARY.md
@.planning/phases/198-green-bringup/198-17-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the decision brief — every option, with what the gate would still guarantee under it</name>
  <read_first>
    - `.github/workflows/ci.yml` lines 1-30 — the header comment declaring `ci-required` the single required check and its `needs:` list the extension point, plus the D-10/D-20 no-path-filters rationale and the `allowed-skips` note.
    - `.github/workflows/ci.yml` lines 744-775 — the `ci-required` job itself: `if: always()`, the twelve `needs:` entries, the `re-actors/alls-green@release/v1` step, and the comment stating `allowed-skips`/`allowed-failures` are deliberately absent today.
    - `.github/rulesets/main.json` in full — note that `required_status_checks` names exactly one context, `CI required`, so a `needs:` change does not by itself change any protection context. The brief must say this explicitly rather than implying a ruleset edit is required.
    - `.planning/audits/198-tier-a-byte-stability.md` — 198-16's measured diagnosis and its two documented remedy shapes with their blast radii.
    - `.planning/phases/198-green-bringup/deferred-items.md` — plan 198-17's entry listing the 8 spec files and 28 failures, and the `maxFailures: 5` masking mechanism.
    - `CONTRIBUTING.md` lines 384-425 — the `## CI Coverage` table, which currently marks `verify-example-browser` **required** and states plainly what stopped running on pull requests in v1.41. Any option that changes required-ness makes a claim in this table false.
    - `test/threadline/ci_coverage_doc_contract_test.exs` — the existing derive-from-workflow guard that keeps that table honest, and the shape a stronger guard would follow.
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` decisions D-08, D-09, D-17, D-23 — the aggregate-gate rationale, the alls-green skipped-check reasoning, the "PR lane stays inside CI required and votes" commitment, and the honesty-mechanism requirements for anything that moves out of the PR set.
  </read_first>
  <files>.planning/audits/198-ci-required-aggregate-decision.md</files>
  <action>
    Create `.planning/audits/198-ci-required-aggregate-decision.md`. Structure it as
    two independent sections, one per lane, because the two questions may be
    answered differently.

    For each lane, state first the measured facts already on disk (cause, evidence
    reference, who ratified what and when) with no new analysis invented. Then
    enumerate the dispositions. For `verify-capture` these are at minimum: keep it in
    `needs:` and accept GREEN-07 stays open until the regeneration prohibition lifts;
    remove it from `needs:` with a documented rationale while the job continues to
    run and report non-blocking; or lift the prohibition (explicitly listed and
    explicitly marked as already-refused this milestone, present only so the option
    set is complete). For `verify-example-browser` these are at minimum: diagnose and
    fix the 28 masked failures in a dedicated successor plan in the same
    no-weakening manner 198-17 used; keep it in `needs:` and accept GREEN-07 stays
    open until that plan lands; or remove it from `needs:` with a documented
    rationale while the job continues to run and report non-blocking.

    For every option write four fields, each a sentence, none of them optional:
    what `CI required` would still guarantee; what it would stop guaranteeing;
    whether GREEN-07 can close under this option and whether that closure is genuine
    or definitional; and the reversibility rating with its one-line rationale.

    State explicitly that removing a lane from `needs:` requires no edit to
    `.github/rulesets/main.json`, because protection names the single context
    `CI required` and nothing else — and state the consequence that follows: the
    protection contract stays byte-identical while the guarantee behind it shrinks,
    which is precisely why this needs a recorded decision and a doc-level guard
    rather than a quiet YAML edit.

    Record the `re-actors/alls-green` semantics as a hard constraint on any option:
    a `needs:` member that becomes conditionally skipped fails the gate unless it is
    named in `allowed-skips`, whereas a member removed from `needs:` is simply not
    asserted at all. Name which mechanism each option would use.

    Name, for every option that reduces coverage, the honesty obligations D-23
    already established: the `## CI Coverage` table in `CONTRIBUTING.md` must state
    the new reality, and a derive-from-source contract test must make that statement
    non-vacuous.

    Do not recommend a winner. Do not re-litigate the Tier-A regeneration
    prohibition beyond listing it as an already-refused option. Do not re-litigate
    GREEN-07's wording.
  </action>
  <acceptance_criteria>
    - `.planning/audits/198-ci-required-aggregate-decision.md` exists and is non-empty.
    - It contains exactly two lane sections, one naming `verify-capture` and one naming `verify-example-browser`, each also naming the job's emitted `name:` string.
    - Every enumerated option carries all four required fields; a mechanical check confirms the count of "what it would stop guaranteeing" fields equals the count of enumerated options.
    - It contains a sentence stating that `.github/rulesets/main.json` names exactly one required context and that a `needs:` change requires no ruleset edit.
    - It contains a sentence distinguishing an `allowed-skips`-listed skipped member from a member removed from `needs:`.
    - It contains no recommendation, ranking, or default — verified by the absence of any option marked preferred and by the SUMMARY recording that the executor made no selection.
    - `git diff --name-only` for this task lists exactly `.planning/audits/198-ci-required-aggregate-decision.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/audits/198-ci-required-aggregate-decision.md &amp;&amp; grep -q 'verify-capture' .planning/audits/198-ci-required-aggregate-decision.md &amp;&amp; grep -q 'verify-example-browser' .planning/audits/198-ci-required-aggregate-decision.md &amp;&amp; grep -q 'rulesets/main.json' .planning/audits/198-ci-required-aggregate-decision.md</automated>
  </verify>
  <done>A two-section brief exists in which every option for both lanes carries its guarantee, its lost guarantee, its GREEN-07 consequence, and its reversibility rating — with no recommendation.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: MAINTAINER DECISION — the `CI required` aggregate's membership for the two structurally-red lanes</name>
  <read_first>
    - `.planning/audits/198-ci-required-aggregate-decision.md` — the brief produced by Task 1, in full. Present its contents to the maintainer; do not summarise away any option's "what it would stop guaranteeing" field.
    - `.planning/phases/198-green-bringup/198-VERIFICATION.md` — the `gaps:` entry for GREEN-07 and item 4 of "What Round 3 must do", which is the origin of this checkpoint.
  </read_first>
  <decision>Whether `CI required`'s `needs:` list keeps `verify-capture` (`Tier A capture lane`) and `verify-example-browser` (`Example app browser E2E (Playwright)`) — two lanes that are structurally red for reasons outside this phase's authority — and, for the browser lane, whether the 28 masked failures get a dedicated fix round.</decision>
  <context>
    While either lane remains in the aggregate and red, `CI required` cannot conclude
    `success`, so GREEN-07 cannot close — no amount of further engineering inside this
    phase changes that. Removing a lane makes the gate close but shrinks what it
    proves, and because `.github/rulesets/main.json` names only the single context
    `CI required`, that shrinkage is invisible at the protection layer. Both horns are
    real; neither is the planner's or the executor's to pick.
  </context>
  <options>
    <option id="capture-keep">
      <name>Lane A (`verify-capture`) — keep it in `needs:`</name>
      <pros>The merge gate keeps proving Tier-A byte-stable evidence on every pull request; nothing about what a green PR means changes; no documentation becomes false.</pros>
      <cons>`CI required` stays red and GREEN-07 stays open indefinitely — until the Tier-A `page.*` regeneration prohibition lifts, which is a milestone-level policy decision, not a Phase 198 one.</cons>
    </option>
    <option id="capture-remove">
      <name>Lane A (`verify-capture`) — remove it from `needs:`, job still runs and reports</name>
      <pros>GREEN-07 becomes closeable on engineering merit; the job keeps running and its status stays visible, so the signal is not lost, only made non-blocking.</pros>
      <cons>A green pull request stops proving Tier-A byte-stability; the reduction is invisible in branch protection and only legible in `CONTRIBUTING.md` and the new contract test; rated one-way.</cons>
    </option>
    <option id="capture-lift-prohibition">
      <name>Lane A (`verify-capture`) — lift the Tier-A regeneration prohibition (already refused twice)</name>
      <pros>Fixes the lane on its actual merits rather than routing around it.</pros>
      <cons>Explicitly refused by the maintainer twice, most recently this session; listed only so the option set is complete. Selecting it re-opens a settled milestone policy.</cons>
    </option>
    <option id="browser-fix">
      <name>Lane B (`verify-example-browser`) — fix the 28 masked failures in a dedicated successor round</name>
      <pros>The lane goes genuinely green with no weakening, in the same red-then-green manner 198-17 used; the gate's guarantee is unchanged and strengthened.</pros>
      <cons>28 unrelated diagnoses exceed a single executor's context budget, so this becomes its own planning round; GREEN-07 stays open until it lands.</cons>
    </option>
    <option id="browser-keep">
      <name>Lane B (`verify-example-browser`) — keep it in `needs:` and accept GREEN-07 stays open</name>
      <pros>No coverage claim changes; the debt stays visible as a red gate rather than being absorbed.</pros>
      <cons>Same structural effect as Lane A's keep option — GREEN-07 is capped at "cannot close" for as long as the debt stands.</cons>
    </option>
    <option id="browser-remove">
      <name>Lane B (`verify-example-browser`) — remove it from `needs:`, job still runs and reports</name>
      <pros>GREEN-07 becomes closeable; the browser signal remains visible, only non-blocking.</pros>
      <cons>A green pull request stops proving the example app's interactive Chromium flows; contradicts D-17's explicit "stays inside `CI required` and votes" commitment, which would need restating; rated one-way.</cons>
    </option>
  </options>
  <resume-signal>Answer both questions explicitly — one option id for Lane A and one for Lane B (e.g. "capture-remove" and "browser-fix"). The executor records both verbatim with attribution and selects nothing itself.</resume-signal>
  <reversibility rating="one-way">Removing a lane from `CI required`'s `needs:` changes what the published merge gate proves for every future pull request, while the protection contract's single context name stays byte-identical — the change is invisible at the protection layer and therefore not self-announcing on reversal.</reversibility>
  <action>
    HALT. Present the maintainer with the brief and ask two separate questions,
    one per lane, each requiring an explicit answer. Auto-advance does not apply:
    this checkpoint is `blocking-human` and must not be auto-selected, defaulted,
    or answered by the executor under any circumstance, including yolo mode. The
    precedent is 198-16, which correctly refused to auto-select a checkpoint whose
    auto-choice would have misrepresented measured evidence.

    Question 1 — `verify-capture` (`Tier A capture lane`): keep it in
    `CI required`'s `needs:`, or remove it with a documented rationale?

    Question 2 — `verify-example-browser` (`Example app browser E2E (Playwright)`):
    fix the 28 masked failures in a dedicated successor plan, keep it in `needs:`
    and accept GREEN-07 stays open until that lands, or remove it from `needs:`
    with a documented rationale?

    With each question, restate in one line the honest consequence of each answer
    for GREEN-07 and for what the merge gate proves. Do not editorialise toward an
    answer and do not present one option as the obvious choice.

    If the maintainer chooses to fix the 28 failures, record that answer and note
    that plan 198-21 will halt with a recommendation to plan a dedicated diagnosis
    round rather than attempting 28 unrelated diagnoses inside this plan — that is a
    scope reality, not a scope reduction, and it must be surfaced rather than
    silently absorbed.
  </action>
  <acceptance_criteria>
    - The executor records, verbatim, the maintainer's answer to each of the two questions, attributed and dated.
    - The SUMMARY states explicitly that the executor selected nothing and that both answers came from the maintainer.
    - No file under `.github/` is modified by this task: `git diff --name-only -- .github/` is empty.
    - If the checkpoint is reached in a non-interactive context, the plan halts with the reason recorded rather than proceeding on a default.
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-only -- .github/ | wc -l | grep -qx 0</automated>
  </verify>
  <done>Both questions carry a recorded, attributed maintainer answer, and no workflow file was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Record both answers as numbered locked decisions with reversibility ratings</name>
  <read_first>
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` — the `## Decisions` block, specifically the highest existing decision number (currently D-38) and the exact formatting of entries that carry a `— **Reversibility:**` clause (D-02, D-08, D-24, D-26, D-30 are the models).
    - The Task 2 record of the maintainer's two answers.
    - `.planning/phases/198-green-bringup/198-VERIFICATION.md` — so the decision text can cite the measured run `33197493051` as the evidence the decision responds to.
  </read_first>
  <files>.planning/phases/198-green-bringup/198-CONTEXT.md</files>
  <action>
    Append two new numbered decisions to `198-CONTEXT.md`'s `## Decisions` block,
    continuing from the highest existing number, one per lane. Each must state: the
    lane's job id and emitted name; the chosen disposition in the maintainer's own
    terms; the evidence it responds to, citing CI run `33197493051`; what
    `CI required` guarantees under the choice and what it no longer guarantees; the
    consequence for GREEN-07 stated plainly, including whether GREEN-07 remains
    uncloseable; and a `— **Reversibility:**` clause rated one-way, costly, or
    reversible with a one-line rationale.

    Add a third decision recording the interlock finding from Task 1 as a standing
    constraint: any future change to `ci-required`'s `needs:` list must be
    reconciled in the same diff against `.github/rulesets/main.json` and the
    `## CI Coverage` table in `CONTRIBUTING.md`, because protection names only the
    aggregate and therefore cannot itself detect the narrowing.

    Do not modify, renumber, or reword any existing decision. Do not modify
    `REQUIREMENTS.md` — the requirement status verdict belongs to plan 198-22 and
    only after a measured CI run.
  </action>
  <acceptance_criteria>
    - `198-CONTEXT.md` gains exactly three new decision entries, each with a number strictly greater than every pre-existing decision number in the file.
    - Each of the three entries contains the literal substring `Reversibility:`.
    - Two of the three name a job id (`verify-capture`, `verify-example-browser` respectively) and cite the run id `33197493051`.
    - `git diff .planning/phases/198-green-bringup/198-CONTEXT.md` shows additions only — the diff contains no removed lines outside the decision block's trailing context.
    - `git diff --name-only` for this task lists exactly `.planning/phases/198-green-bringup/198-CONTEXT.md`.
    - `.planning/REQUIREMENTS.md` is unmodified across this entire plan.
  </acceptance_criteria>
  <verify>
    <automated>git diff --numstat .planning/phases/198-green-bringup/198-CONTEXT.md | awk '{exit ($3=="" || $2!="0") ? ($2>0?0:1) : 1}' &amp;&amp; grep -c 'Reversibility:' .planning/phases/198-green-bringup/198-CONTEXT.md &amp;&amp; git diff --quiet .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Three new locked decisions record both lane dispositions and the `needs:`/ruleset/CONTRIBUTING interlock, each with a reversibility rating, with no existing decision touched and no requirement status changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor judgment → merge gate guarantee | An automated agent could narrow what the single required check proves without any human seeing the change |
| Planning record → future maintainer | The decision record is the only durable statement of what the gate is meant to guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-20-01 | Elevation of Privilege | `ci-required` `needs:` list | high | mitigate | The narrowing decision is gated behind a `blocking-human` `checkpoint:decision`; this plan modifies no file under `.github/` and its acceptance criteria assert that emptiness mechanically |
| T-198-20-02 | Repudiation | merge gate guarantee | high | mitigate | Both answers are recorded as numbered locked decisions in `198-CONTEXT.md` with attribution, evidence citation, and an explicit statement of what stops being guaranteed — so a future reader cannot mistake a narrowed gate for an unchanged one |
| T-198-20-03 | Tampering | `198-CONTEXT.md` decision history | medium | mitigate | Acceptance criteria require additions-only and forbid renumbering or rewording any existing decision |
| T-198-20-04 | Information Disclosure | decision brief content | low | accept | The brief contains only CI topology and already-public planning content; `.planning/` is already published on `origin/main` per D-30 |
| T-198-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan; no dependency or lockfile is touched, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `.planning/audits/198-ci-required-aggregate-decision.md` exists, names both job ids, and enumerates options with all four required fields each.
- `git diff --name-only -- .github/` is empty for the whole plan.
- `git diff --quiet .planning/REQUIREMENTS.md` exits 0 for the whole plan.
- `198-CONTEXT.md` carries three new numbered decisions, each containing `Reversibility:`.
</verification>

<success_criteria>
The question `198-VERIFICATION.md` identified as a maintainer decision has been put
to the maintainer with honest consequences attached, answered by the maintainer, and
recorded durably — with nothing implemented, no gate narrowed, and no requirement
status changed.
</success_criteria>

## Artifacts this phase produces

**New files**
- `.planning/audits/198-ci-required-aggregate-decision.md` — the two-lane decision
  brief; every option carries what the gate still guarantees, what it stops
  guaranteeing, the GREEN-07 consequence, and a reversibility rating.

**Changed planning artifacts**
- `.planning/phases/198-green-bringup/198-CONTEXT.md` — three new numbered
  decisions: the `verify-capture` disposition, the `verify-example-browser`
  disposition, and the standing `needs:` / `rulesets/main.json` / `## CI Coverage`
  reconciliation interlock.

**No CI job ids, job names, test functions, file paths under `.github/`, or mix
aliases are created or changed by this plan.**

<output>
Create `.planning/phases/198-green-bringup/198-20-SUMMARY.md` when done.
</output>
