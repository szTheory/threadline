---
phase: 198-green-bringup
plan: 27
type: execute
wave: 3
gap_closure: true
depends_on:
  - 198-26
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-phase-135-uat.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-phase-173-uat.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts
  - .planning/audits/198-round4-playwright.md
autonomous: true
requirements:
  - GREEN-07

estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every attribution row assigned to cluster `198-27` in `.planning/audits/198-round4-playwright.md` is fixed at its named cause, and rows sharing a cause are closed by one fix that is credited once."
    - "A red-then-green teeth proof is captured for at least one failure per spec file fixed."
    - "The lane's measured failure count is recorded before and after this plan, taken with the same unbounded local command plan 198-26 used, with the integer delta stated."
    - "`playwright.config.ts` (including `maxFailures`), the `--project` set, the spec roster, `ci.yml`, `.github/rulesets/main.json` and `CONTRIBUTING.md` are all byte-unchanged."
    - "Any newly-surfaced failure not in plan 198-26's inventory is added as an attribution row with a cause and a cluster assignment — never silently absorbed and never silently dropped."
    - "This plan's fixes are explicitly labelled PRE-MERGE with respect to same-wave sibling plan 198-25, which rewrites `demo/seed/exports.ex`, `demo/seed/retention_runs.ex`, `demo/seed/support.ex` and `demo/seed/filler.ex` in an isolated worktree this plan cannot observe. Every row this plan touches carries a `seed-sensitive?` verdict, and every `yes` row is marked provisional pending 198-28's post-merge re-validation gate."
    - "The plan restates that closing this cluster cannot make `CI required` conclude `success`, because the Tier A capture lane's only remedy is forbidden this milestone under D-39."
    - statement: "Adjacency edge (GREEN-07): where a UAT spec asserts a threshold — a transition duration that must be non-collapsed versus collapsed under reduced motion, or a sticky topbar that must sit above content without covering it — the assertion states which side of the threshold passes, so a value landing exactly on it is not left to chance."
      verification: explicit
    - statement: "Empty edge (GREEN-07): the `pager hides entirely at zero matches (hide-at-zero, D-16)` and `offboarded support user gets an honest empty scoped Timeline` tests assert the empty case as a positive, named outcome, and the non-empty pager test asserts non-emptiness first so neither can pass vacuously against a database that seeded nothing."
      verification: explicit
    - statement: "Ordering edge (GREEN-07): where a UAT spec asserts over a set of rendered elements whose order is not contractually fixed (the Phase 177 story catalog loop, the timeline's op-variety labels), the assertion is set-based rather than positional."
      verification: explicit
  artifacts:
    - "examples/threadline_phoenix/e2e/tests/operator-phase-135-uat.spec.ts — fixed at cause"
    - "examples/threadline_phoenix/e2e/tests/operator-phase-173-uat.spec.ts — fixed at cause"
    - "examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts — fixed at cause"
    - "examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts — fixed at cause"
    - ".planning/audits/198-round4-playwright.md — extended with this plan's teeth proofs, after-count and any new discoveries"
  key_links:
    - "plan 198-26's `## Cluster assignment` → this plan's work list (read it; do not re-derive the failure set from `deferred-items.md`)"
    - "`mix demo.seed` content changed by plans 198-23/24/25 → the timeline/coverage fixtures these UAT specs read → a seed change can be the cause or the fix"
    - "same-wave sibling 198-25 rewrites the export/retention/support/filler seed producers in an isolated worktree → the Phase 135 op-variety, Phase 175 pager, and export/retention surfaces this plan asserts on are exactly what those producers feed → seed-sensitive rows are flagged here and re-derived post-merge by 198-28's gate"
    - "197-era product changes → UAT specs pinned to removed literals → 198-17's precedent for how such a rot is legitimately fixed"
  prohibitions:
    - statement: "A lane must not be made green by hiding failures. `maxFailures` must not be raised or removed, the `--project` set must not be narrowed, and no spec file or test may be skipped, `.fixme`-d, `.skip`-ped, deleted, or `git rm`-ed."
      status: kept
      verification: flagged-unverified
    - statement: "`ci-required`'s `needs:` list must not be changed, and `CI required`'s guarantee must not be reduced. Any `needs:` change would require reconciling `.github/rulesets/main.json` and `CONTRIBUTING.md`'s `## CI Coverage` table in the same diff (D-42)."
      status: kept
      verification: flagged-unverified
    - statement: "Tier-A `page.*` evidence regeneration is forbidden for this milestone (D-39). No task in this plan touches the Tier A capture lane, its specs, or its scorecards."
      status: kept
      verification: flagged-unverified
---

<objective>
**Gap 2, part 2 of 3.** Fix the UAT-spec cluster of the masked Playwright failures:
`operator-phase-135-uat.spec.ts`, `operator-phase-173-uat.spec.ts`, `operator-phase-175-uat.spec.ts`
and `operator-phase-177-uat.spec.ts` — the rows plan 198-26's attribution table assigned to cluster
`198-27`.

Purpose: these four files carry the bulk of the 28 masked failures across the two projects, and
`operator-phase-135-uat.spec.ts:76:3` and `operator-phase-173-uat.spec.ts:74:3` are among the five
CI actually reported before `maxFailures` truncated the run. D-40 (maintainer decision B1) directs
this round to fix them in the same disciplined, no-weakening manner 198-17 used.

Output: the four UAT specs passing under both projects for cause-attributed reasons, teeth proofs on
disk, and a lower measured lane failure count.

**Ceiling, restated:** closing this cluster does not make `CI required` conclude `success`. The
`Tier A capture lane (byte-stable evidence)` stays red because its only remedy — Tier-A `page.*`
scorecard regeneration — is forbidden for this entire milestone under D-39, and no task here may
attempt it or route around it. This round's honest target is 3 red `needs:` lanes down to 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/deferred-items.md
@.planning/WINDOWS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the Phase 135 and Phase 173 UAT specs at their causes</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-phase-135-uat.spec.ts, examples/threadline_phoenix/e2e/tests/operator-phase-173-uat.spec.ts, .planning/audits/198-round4-playwright.md</files>
  <read_first>
    - .planning/audits/198-round4-playwright.md (plan 198-26's `## Cluster assignment` rows for these two files, their `cause` and `shares cause with` columns, and the `## Forbidden remedies` list — this is the work order)
    - examples/threadline_phoenix/e2e/tests/operator-phase-135-uat.spec.ts (all 85 lines — `test.describe("operator surface - Phase 135 automated UAT")` with `test("default Timeline renders in-window op variety and non-human actor labels")` at 18, `test("reply-edit transaction renders before/after diff with redacted internal note")` at 33, `test("offboarded support user gets an honest empty scoped Timeline")` at 65, `test("support user is denied admin-only Coverage")` at 76 — the last is one of the five CI reported)
    - examples/threadline_phoenix/e2e/tests/operator-phase-173-uat.spec.ts (all 146 lines — `test.describe("Phase 173 UAT #1 — primitive interaction states + honest affordances")` with the cursor and disabled-button tests at 46 and 61; `test.describe("Phase 173 UAT #2 — overlay dismissal, focus, stacking")` with the dropdown `aria-expanded` test at 74 (one of the five CI reported), the modal stacking test at 88, and the retention-prune dialog Escape/outside-click test at 109)
    - .planning/audits/198-round4-demo-seed.md (plans 198-23's and 198-24's seed changes, already merged and therefore visible here — `in-window op variety`, the reply-edit diff, and the offboarded empty timeline all read seeded content)
    - .planning/phases/198-green-bringup/198-25-PLAN.md (its `files_modified` list — `demo/seed/exports.ex`, `demo/seed/retention_runs.ex`, `demo/seed/support.ex`, `demo/seed/filler.ex`. **198-25 executes in the same wave as this plan, in an isolated worktree, so its seed changes are NOT visible here.** Read its plan and its `<behavior>` blocks to know which seeded surfaces are about to move, and flag every row that reads them.)
    - .planning/audits/198-example-browser-e2e.md (198-17's precedent for distinguishing a product regression from a legitimately-rotted spec literal)
  </read_first>
  <action>
    Fix the cluster-`198-27` rows for these two spec files at the causes plan 198-26 attributed. Where
    rows share a cause, apply one fix and record which rows it closed.

    For `operator-phase-135-uat.spec.ts`: `default Timeline renders in-window op variety and non-human
    actor labels` reads the same in-window INSERT/UPDATE/DELETE variety the Elixir `D-13` contract
    test asserts — if plans 198-23/24 changed the seed's variety guarantee, that is the likely shared
    cause and the fix belongs in the spec's expectation or in the seed, not in both. `offboarded
    support user gets an honest empty scoped Timeline` must assert emptiness as a named positive
    outcome (an explicit empty-state affordance is rendered), not as the absence of rows, so it cannot
    pass against a database that seeded nothing.

    For `operator-phase-173-uat.spec.ts`: cursor, `aria-expanded`, stacking and dismissal assertions
    are product-contract assertions. If the product legitimately changed a selector or an attribute,
    fix the spec to the new contract and prefer a role/attribute assertion over a pinned class name.
    If the product regressed a documented affordance, fix the product and say so — the whole point of
    a UAT spec is that a real regression is not silently renamed into an outdated expectation.

    Capture red-then-green transcripts under `## Red-then-green teeth proof (198-27, phase-135/173)`.

    Forbidden: `test.skip`, `test.fixme`, `.only`, deleted assertion or spec, `git rm`, any edit to
    `playwright.config.ts`, `--project` narrowing, any edit to `.github/workflows/ci.yml`,
    `.github/rulesets/main.json` or `CONTRIBUTING.md`, and any Tier-A `page.*` regeneration.
  </action>
  <acceptance_criteria>
    - `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` reports `0 failed` for every test in `operator-phase-135-uat.spec.ts` and `operator-phase-173-uat.spec.ts` under both projects.
    - `.planning/audits/198-round4-playwright.md` contains `## Red-then-green teeth proof (198-27, phase-135/173)` with verbatim pre-fix failure and post-fix pass transcripts for at least one failure per file.
    - The `offboarded support user gets an honest empty scoped Timeline` test asserts a named empty-state affordance, not merely a zero row count; a source read shows the positive assertion present.
    - `grep -cE 'test\.(skip|fixme|only)' examples/threadline_phoenix/e2e/tests/operator-phase-135-uat.spec.ts examples/threadline_phoenix/e2e/tests/operator-phase-173-uat.spec.ts` returns `0` for both files.
    - `grep -c '^\s*test(' ` on each file is greater than or equal to its pre-task value.
    - `git diff -- examples/threadline_phoenix/e2e/playwright.config.ts .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md` is empty.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.example_browser --project=desktop-chromium --project=mobile-chromium</automated>
  </verify>
  <done>The Phase 135 and Phase 173 UAT specs pass under both projects for cause-attributed reasons, with teeth proofs, and no product regression was renamed into an updated expectation without saying so.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the Phase 175 and Phase 177 UAT specs at their causes</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts, examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts, .planning/audits/198-round4-playwright.md</files>
  <read_first>
    - .planning/audits/198-round4-playwright.md (plan 198-26's cluster-`198-27` rows for these two files, plus this plan's Task 1 output)
    - examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts (all 154 lines — `test.describe("Phase 175 UAT — runtime theme picker (real user flow)")` with `test("theme is server-decided and the picker is wired to switch it (form contract)")` at 35 and `test("shell nav is a native <details> that toggles open and closed")` at 84; `test.describe("Phase 175 UAT — sticky topbar + timeline pager")` with `test("sticky topbar stays above content without covering it at rest")` at 115, `test("pager renders the timeline's next-only control when there is data")` at 134, and `test("pager hides entirely at zero matches (hide-at-zero, D-16)")` at 150)
    - examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts (all 267 lines — `test.describe("Phase 177 UAT #1 — group catalog holds together at every viewport")` and its per-story loop at 83; `test.describe("Phase 177 UAT #3 — overlay motion (default motion)")` with the modal and drawer non-collapsed-duration tests at 137 and 151; `test.describe("Phase 177 UAT #3 — overlay motion collapses under reduced motion")` with the ~instant tests at 180 and 192; `test.describe("Phase 177 UAT #4 — reconnect/offline CSS contract")` with the `phx-error` banner test at 221)
    - examples/threadline_phoenix/e2e/playwright.config.ts (the `use:` block's reduced-motion neutralization note — Phase 177's motion tests deliberately fight that default, so read how before judging a motion failure)
    - .planning/audits/198-round4-demo-seed.md (the pager tests read seeded timeline volume — a seed change can flip `there is data` and `zero matches` both ways)
  </read_first>
  <action>
    Fix the cluster-`198-27` rows for these two spec files at their attributed causes, crediting
    shared-cause fixes once.

    For the Phase 175 pager pair: `pager renders the timeline's next-only control when there is data`
    must first assert that data is present, then assert the control renders; `pager hides entirely at
    zero matches` must reach a genuinely zero-match filter state and assert the control's absence
    together with a named zero-state affordance. Those two tests are the empty-edge pair — if a seed
    volume change flipped one, it very likely flipped the other, and they share a cause.

    For the Phase 175 sticky-topbar test and the Phase 177 motion tests: these assert thresholds. State
    which side of the threshold passes — a transition duration counts as "real" above a named
    millisecond floor and as "collapsed" below a named ceiling; the topbar sits above content at a
    named stacking relationship without overlapping it at rest. Do not leave a boundary value to an
    implicit `>` versus `>=`.

    For the Phase 177 per-story viewport loop: assert over the set of stories the catalog actually
    exposes, resolved at runtime, rather than a positionally-indexed or hard-coded story list that
    rots when a story is added.

    Capture red-then-green transcripts under `## Red-then-green teeth proof (198-27, phase-175/177)`.

    Forbidden: the same list as Task 1, verbatim.
  </action>
  <acceptance_criteria>
    - `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` reports `0 failed` for every test in `operator-phase-175-uat.spec.ts` and `operator-phase-177-uat.spec.ts` under both projects.
    - `.planning/audits/198-round4-playwright.md` contains `## Red-then-green teeth proof (198-27, phase-175/177)` with verbatim pre-fix and post-fix transcripts for at least one failure per file.
    - The two pager tests each contain their paired precondition assertion (data present / zero matches reached) before the presence-or-absence assertion; a source read shows both.
    - Each motion and stacking threshold assertion names its numeric boundary and states the passing side in a source comment.
    - The Phase 177 story loop resolves its story list at runtime rather than from a hard-coded array; a source read confirms it.
    - `grep -cE 'test\.(skip|fixme|only)' examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts` returns `0` for both files.
    - `git diff -- examples/threadline_phoenix/e2e/playwright.config.ts .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md` is empty.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.example_browser --project=desktop-chromium --project=mobile-chromium</automated>
  </verify>
  <done>The Phase 175 and Phase 177 UAT specs pass under both projects, with thresholds stated explicitly, the empty-edge pager pair fixed together, and the story loop rot-proofed.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure the lane and reconcile this cluster against the attribution table</name>
  <files>.planning/audits/198-round4-playwright.md</files>
  <read_first>
    - .planning/audits/198-round4-playwright.md (this plan's own Tasks 1 and 2 output, plan 198-26's `## Measured after-count (198-26)` figure this plan's delta is measured against, and the `## Cluster assignment` rows owned by `198-27`)
    - .planning/phases/198-green-bringup/deferred-items.md (the format an honestly-recorded discovery takes — 198-17's entry is the model)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-39, D-40, D-42 — the constraints this reconciliation must restate)
  </read_first>
  <action>
    Run `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` unbounded
    and append `## Measured after-count (198-27)` to `.planning/audits/198-round4-playwright.md` with
    the verbatim Playwright summary line and the integer delta against plan 198-26's closing count.

    Reconcile: mark every cluster-`198-27` attribution row as `closed` with the fix that closed it, or
    as `open` with the reason. The count of `closed` plus `open` rows must equal the cluster's assigned
    row count — no row may be missing from the reconciliation.

    Add a `## Pre-merge status (198-27)` section: state that this plan's fixes were made in an isolated
    worktree BEFORE same-wave sibling plan 198-25's demo-seed changes merged, record the current
    `git rev-parse HEAD`, and give every cluster-`198-27` row a `seed-sensitive?` verdict of exactly
    `yes` or `no` — `yes` if the assertion reads `mix demo.seed`-produced content (timeline op variety,
    pager data volume and zero-match state, coverage rows, export job states, retention runs, the
    offboarded empty timeline), `no` if it depends only on static markup, CSS, motion, cursors, ARIA
    state, or routing. State the `yes` count as an integer and mark every `yes` row **provisional
    pending 198-28's post-merge re-validation**.

    Record any newly-surfaced failure as a new attribution row with a cause and a cluster assignment,
    and add a dated entry to `.planning/phases/198-green-bringup/deferred-items.md` if it will not be
    fixed in this round. 198-17's own record is that fixing a diagnosed cause reveals the next layer;
    that outcome is recorded, not hidden, and it is not a reason to reach for a forbidden remedy.

    Close with a restated ceiling paragraph: even with this cluster and 198-28's closed, `CI required`
    cannot conclude `success` this round because `Tier A capture lane (byte-stable evidence)` stays
    red under D-39. State the target as 3 red `needs:` lanes down to 1.
  </action>
  <acceptance_criteria>
    - `.planning/audits/198-round4-playwright.md` contains `## Measured after-count (198-27)` with the verbatim summary line and an explicit integer delta against `## Measured after-count (198-26)`.
    - Every cluster-`198-27` attribution row carries a `closed` or `open` disposition; the sum of the two equals the cluster's assigned row count, and that equality is stated as an arithmetic line.
    - Every `open` row carries a named reason — no row reads "still red" without attribution.
    - `.planning/audits/198-round4-playwright.md` contains `## Pre-merge status (198-27)` naming plan 198-25 as the unobserved same-wave sibling and recording a 40-character `git rev-parse HEAD` value.
    - Every cluster-`198-27` row carries a `seed-sensitive?` cell whose value is exactly `yes` or `no` — no blank cells — the `yes` count is stated as an integer, and every `yes` row is marked provisional pending 198-28's post-merge re-validation.
    - Any newly-surfaced failure has an attribution row with a cause and a cluster, and a dated `deferred-items.md` entry if unfixed this round.
    - The file contains a ceiling paragraph naming `Tier A capture lane (byte-stable evidence)`, D-39, and the "3 red lanes down to 1" target.
    - `git diff -- examples/threadline_phoenix/e2e/playwright.config.ts .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md` is empty.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^## Measured after-count (198-27)' .planning/audits/198-round4-playwright.md &amp;&amp; grep -q 'D-39' .planning/audits/198-round4-playwright.md &amp;&amp; test -z "$(git diff --name-only -- examples/threadline_phoenix/e2e/playwright.config.ts .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md)"</automated>
  </verify>
  <done>Every row this cluster owns is dispositioned, the lane's measured count is recorded with a delta, new discoveries are logged rather than absorbed, and the round's ceiling is restated in writing.</done>
</task>

</tasks>

<reversibility>
  <decision rating="reversible">Rewriting a pinned class-name or copy-string assertion into a role/attribute or shape assertion is reversible — an ordinary spec change with recorded reasoning.</decision>
  <decision rating="costly">A product-side fix to an operator-surface affordance (cursor, `aria-expanded`, stacking, motion duration) is costly: the Tier A capture lane observes the same surface and is under a regeneration prohibition (D-39), so a visual-affecting product change could deepen that lane's drift without any permitted remedy. Mitigated by preferring the spec-side fix whenever the evidence shows a legitimately-changed contract, and by recording any product change explicitly in the attribution table so plan 198-29's measurement can attribute movement in the Tier A lane.</decision>
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser lane result → merge gate | The lane is a `needs:` member of `CI required`; weakening it silently weakens the merge guarantee |
| Product regression → spec expectation | Renaming a real regression into an updated expectation launders a defect into a passing test |
| Product visual change → Tier A capture lane | The Tier A lane observes the same surface and cannot be regenerated this milestone (D-39) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-27-01 | Tampering | the spec roster and `maxFailures` | high | mitigate | `test.skip`/`test.fixme`/`.only` counts of 0, non-decreasing test counts, and an empty `playwright.config.ts` diff are acceptance criteria |
| T-198-27-02 | Spoofing | a product regression disguised as a rotted literal | high | mitigate | Each fix records whether the product or the spec was wrong and cites the evidence; 198-17's precedent is required reading |
| T-198-27-03 | Repudiation | cluster completeness | medium | mitigate | Task 3's arithmetic reconciliation requires `closed + open` to equal the assigned row count |
| T-198-27-04 | Elevation of Privilege | `ci-required`'s `needs:` and the ruleset | high | mitigate | Empty diffs on `ci.yml`, `.github/rulesets/main.json` and `CONTRIBUTING.md` are acceptance criteria; D-42 restated in the prohibitions |
| T-198-27-05 | Tampering | Tier A evidence | high | mitigate | D-39 restated as a prohibition; no task touches the Tier A specs or scorecards; product visual changes are recorded so 198-29 can attribute any Tier A movement |
| T-198-27-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed, added, or upgraded; `package.json` and its lockfile are untouched, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` reports 0 failures across all four Phase-UAT spec files under both projects.
- `.planning/audits/198-round4-playwright.md` carries both teeth-proof sections, `## Measured after-count (198-27)` with its delta, the arithmetic cluster reconciliation, and the restated D-39 ceiling.
- `grep -n 'maxFailures' examples/threadline_phoenix/e2e/playwright.config.ts` still returns `maxFailures: process.env.CI ? 5 : 0`.
- `git diff -- .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts` is empty.
</verification>

<success_criteria>
The four Phase-UAT specs pass under both projects for causes that are named and evidenced, every row
this cluster owns is dispositioned with arithmetic that closes, and the lane's guarantee is exactly
what it was before — narrower nowhere.
</success_criteria>

## Artifacts this phase produces

**Changed files**
- `examples/threadline_phoenix/e2e/tests/operator-phase-135-uat.spec.ts` — fixed at cause; empty-timeline assertion made a named positive outcome.
- `examples/threadline_phoenix/e2e/tests/operator-phase-173-uat.spec.ts` — fixed at cause; affordance assertions preferred over pinned class names.
- `examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts` — fixed at cause; pager empty-edge pair fixed together, stacking threshold stated.
- `examples/threadline_phoenix/e2e/tests/operator-phase-177-uat.spec.ts` — fixed at cause; motion thresholds stated with their passing side, story loop resolved at runtime.
- `.planning/audits/198-round4-playwright.md` — two teeth-proof sections, `## Measured after-count (198-27)`, the cluster reconciliation, new discovery rows, and the restated ceiling.
- Possibly operator-surface product source, if and only if the attribution shows a product regression; any such file is named in the audit file.
- `.planning/phases/198-green-bringup/deferred-items.md` — a dated entry only if a discovery is honestly deferred.

**Explicitly not produced**
- No change to `examples/threadline_phoenix/e2e/playwright.config.ts`, `.github/workflows/ci.yml`, `.github/rulesets/main.json`, `CONTRIBUTING.md`, or `.planning/REQUIREMENTS.md`.
- No Tier A capture-lane work of any kind (D-39).

<output>
Create `.planning/phases/198-green-bringup/198-27-SUMMARY.md` when done.
</output>
