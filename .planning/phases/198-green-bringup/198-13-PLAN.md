---
phase: 198-green-bringup
plan: 13
type: execute
wave: 3
depends_on: ["198-09", "198-10", "198-11", "198-12"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - GREEN-01
  - GREEN-02
  - GREEN-03
  - GREEN-04
  - GREEN-05
  - GREEN-06
  - GREEN-07
  - GREEN-08
  - GREEN-09
  - GREEN-10
  - GREEN-11
  - GREEN-12
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The latest `ci.yml` run on `main` concludes `success`"
    - "PR #26 reports `mergeStateStatus: CLEAN`, reached with no change to branch protection, the ruleset, or bypass_actors"
    - "`.planning/REQUIREMENTS.md` reflects the verified status of all 12 GREEN requirements, neither over- nor under-claiming"
    - "CR-03, CR-04, and CR-05 are recorded as carried-forward debt with their severity and consequence stated"
  artifacts:
    - ".planning/REQUIREMENTS.md — GREEN-04, GREEN-07, GREEN-11 status corrected against measured evidence"
    - ".planning/STATE.md — phase position and last activity updated"
  key_links:
    - "every verify-* job green -> `ci-required` (`CI required`) concludes success -> the single required context on main is satisfied -> PR #26 unblocks"
    - "198-09 (compile-no-optional) + 198-10 (mechanical/capture/example-browser) + 198-12 (test lanes + pgbouncer topology) = the 7 non-aggregate red jobs on run 33138291361"
---

<objective>
Land the four gap-closure plans on `main`, observe CI conclude `success`, confirm PR #26 is
`CLEAN`, and correct the requirement traceability against measured evidence.

Purpose: the phase goal is "`origin/main` carries every local commit and its CI concludes green
well inside a usable feedback loop." Plans 198-09 through 198-12 fix the causes; this plan
produces the observation that closes GREEN-07 and, as a consequence, SC4. It is also where the
phase's record is made honest: `.planning/REQUIREMENTS.md` must state what is true after these
plans, not what was hoped.

Output: a green `main`, an unblocked PR #26, and a requirements file that matches reality.

**The mergeability clause is closed by greening `CI required`, not by touching protection.**
GREEN-08 is met and proven non-vacuous by a red→green pair on an identical SHA (runs
`33138291397` and `33140020321` @ `a97f527e`). The maintainer accepted the hard merge lock
explicitly before the ruleset was applied. **No task in this plan may edit branch protection,
`.github/rulesets/main.json`, `enforcement`, or `bypass_actors`, or add a bypass actor.**

**Landing route.** The live ruleset carries a `pull_request` rule with `bypass_actors: []`, so a
direct push to `main` is refused. The gap-closure commits land via a branch and a pull request,
whose own `CI required` must be green before merge. This is the gate working as designed, not an
obstacle to route around.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-REVIEW.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local green before anything is pushed</name>
  <read_first>
    - mix.exs (the `ci.all` alias at ~:140-160 — the exact ordered list of verification steps CI mirrors, and the canonical entrypoints CLAUDE.md requires be cited verbatim)
    - .planning/phases/198-green-bringup/198-09-SUMMARY.md, 198-10-SUMMARY.md, 198-11-SUMMARY.md, 198-12-SUMMARY.md (what each plan actually changed and what each measured — the input to this task's reconciliation)
    - .github/workflows/ci.yml (the `ci-required` job's `needs:` list — the definitive roster of jobs that must be green)
  </read_first>
  <precondition>Plans 198-09, 198-10, 198-11, and 198-12 are complete and their SUMMARY files exist. `git status --porcelain` shows no uncommitted work from those plans.</precondition>
  <action>
    Run the full local gate before touching the remote, so a red push does not burn a CI cycle
    and a cancelled-in-progress iteration.

    Run, in this order, and record each exit code verbatim:
    `mix verify.format`, `mix verify.credo`, `mix compile --warnings-as-errors`,
    `mix verify.compile_no_optional`, `mix test`, `mix verify.mechanical`, and finally
    `mix ci.all`. These are the project's named entrypoints; do not substitute ad-hoc commands.

    Then reconcile, in a table, each of the 8 red jobs from run `33138291361` against the plan
    that fixed it and the local command that now proves it:
    `Run test suite (min)`, `Run test suite (current)`, and `PgBouncer transaction topology` →
    198-12; `Compile without optional deps` → 198-09; `Mechanical checker (committed
    scorecards)`, `Tier A capture lane (byte-stable evidence)`, and `Example app browser E2E
    (Playwright)` → 198-10; `CI required` → the aggregate of the other seven.

    Any row you cannot prove locally must be named as such, with the specific reason and the CI
    job that will be its only proof. Do not write "should pass."

    If anything is red, stop and fix it on its merits. Do not skip, exclude, tag out, or weaken.
  </action>
  <verify>
    <automated>mix ci.all</automated>
  </verify>
  <acceptance_criteria>
    - `mix ci.all` exits 0.
    - `mix test` exits 0 with 0 failures.
    - `mix verify.compile_no_optional` exits 0.
    - `mix verify.mechanical` exits 0.
    - `mix verify.format` and `mix verify.credo` exit 0.
    - The SUMMARY carries the 8-row reconciliation table mapping each formerly-red job to its closing plan and its local proof command, with every locally-unprovable row explicitly named.
  </acceptance_criteria>
  <done>Every gate CI will run has been run locally and passed, and the mapping from each red job to its fix is written down.</done>
</task>

<task type="auto">
  <name>Task 2: Land on main and observe CI conclude success</name>
  <read_first>
    - .github/rulesets/main.json (the live ruleset's committed snapshot: `required_status_checks: ["CI required"]`, `enforcement: active`, `bypass_actors: []`, and the `pull_request` rule that makes a direct push to main impossible)
    - .planning/phases/198-green-bringup/198-07-SUMMARY.md (how the previous landing was done, the measured 6m07s wall clock, and what the migration to the ruleset changed)
    - bin/verify-branch-protection (what the drift guard asserts — so you can confirm it still passes without changing anything it checks)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-30/D-32 — the publication posture already authorized by 198-02's credential audit; nothing here re-opens it)
  </read_first>
  <reversibility rating="costly">Publishing commits to a public `origin/main` cannot be un-published; the no-history-rewrite constraint applies. The content posture was already decided (D-30) and gated by 198-02's credential audit, so the new commits are in-scope of an existing authorization — but the act itself is not undoable.</reversibility>
  <action>
    Push the gap-closure work to a branch and open a pull request against `main`. A direct push to
    `main` is refused by the live ruleset's `pull_request` rule with `bypass_actors: []` — that is
    the gate working, and it must not be bypassed, weakened, or worked around.

    Name the branch in the existing convention (`ci/198-gaps` matches the `ci/198-05-verify` and
    `ci/v1_41-green-bringup` branches already on origin).

    Watch `ci.yml` on the PR. Record, for the run: its id, its conclusion, its wall-clock duration,
    and the per-job conclusion for all 14 jobs. GREEN-07's wall-clock clause is already met at
    6m07s against a ≤20min bound and a ≤12min target — confirm the new run stays inside it and
    report the number; do not treat the clause as needing re-establishment.

    When `CI required` is green, merge the PR. Then delete the merged branch, and delete the two
    stale remote branches `ci/198-05-verify` and `ci/v1_41-green-bringup` if they are confirmed
    merged or superseded — GREEN-12's local clause is met, and leaving stale remote refs behind
    contradicts its intent. If either is NOT an ancestor of `main`, apply D-31: archive tag plus a
    row in `.planning/ARCHIVE-REGISTER.md` before removal. Never silently discard.

    Then observe the `ci.yml` run triggered on `main` by the merge, and record its id, conclusion,
    and duration. That run is GREEN-07's proof.

    Finally, re-run or re-request checks on PR #26 (`release-please--branches--main`, `chore(main):
    release 0.10.0`) so its `CI required` is evaluated against the fixed code, and record
    `gh pr view 26 --json mergeStateStatus`.

    **Forbidden in this task:** any edit to branch protection, `.github/rulesets/main.json`,
    `enforcement`, or `bypass_actors`; adding a bypass actor; renaming any job `id:` or `name:`;
    merging PR #26 itself (that is a release decision, not a gap-closure step).
  </action>
  <verify>
    <automated>gh run list --workflow=ci.yml --branch main --limit 1 --json conclusion,databaseId,createdAt,updatedAt</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --workflow=ci.yml --branch main --limit 1 --json conclusion` reports `"conclusion":"success"`.
    - That run's wall clock is ≤ 20 minutes, and the measured value is recorded (the prior measurement was 6m07s).
    - `git log origin/main..main` is empty at task end.
    - `gh pr view 26 --json mergeStateStatus` reports `CLEAN`.
    - `git diff origin/main -- .github/rulesets/main.json` is empty, and `gh api repos/:owner/:repo/rulesets/21702804` still reports `enforcement: active` with `bypass_actors: []` and the single required context `CI required` — verbatim output recorded in the SUMMARY.
    - `bin/verify-branch-protection` exits 0.
    - Every stale remote branch is either deleted after confirming it is an ancestor of `main`, or archived per D-31 with a new `.planning/ARCHIVE-REGISTER.md` row. State which, per branch.
    - PR #26 is NOT merged by this task.
  </acceptance_criteria>
  <done>`main` CI concludes success, PR #26 is CLEAN, and the protection surface is byte-identical to what the maintainer approved.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the requirement record and register the carried-forward debt</name>
  <read_first>
    - .planning/REQUIREMENTS.md lines 11-22 (the `[x]`/`[ ]` checkboxes for GREEN-01..GREEN-12) and lines 130-141 (the status table). **Note: a prior commit (`f25987a5`) already corrected GREEN-05, GREEN-06, and GREEN-11. Read the current state before editing — GREEN-11 is already `[ ]`/Pending, and GREEN-04/07 are already honestly pending. Verify, then change only what measurement now makes false.**
    - .planning/phases/198-green-bringup/198-VERIFICATION.md (the verified status of all 12 requirements, and the traceability-defect note)
    - .planning/phases/198-green-bringup/198-REVIEW.md sections CR-03, CR-04, and CR-05 (the three WARNING-severity findings carried forward)
    - .planning/STATE.md (the frontmatter and the "Current Position" block; note the CLAUDE.md warning that `gsd-sdk state.*` handlers miscompute this file's bespoke progress block and must be hand-corrected)
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md` so every GREEN row states what Task 1 and Task 2 measured:

    - GREEN-04 → complete, on the evidence of two consecutive `mix test` runs at 0 failures.
    - GREEN-07 → complete, on the evidence of the named `main` run id concluding `success` inside
      the recorded wall clock.
    - GREEN-11 → complete, on the evidence of `Threadline.FlakeClassifierContractTest` passing and
      the recorded red-then-green non-vacuity pairs.
    - Every other row: confirm it already matches verification and leave it alone. Do not flip a
      row you did not measure.

    Update BOTH representations — the checkbox list near the top and the status table below it —
    and confirm they agree; the traceability defect verification found was precisely a
    disagreement between those two.

    Update `.planning/STATE.md`: `stopped_at`, `last_activity`, `last_activity_desc`, and the
    "Current Position" block, naming the green `main` run id and PR #26's CLEAN status. Correct the
    progress block by hand — per CLAUDE.md the `gsd-sdk state.*` handlers miscompute it.

    Record the carried-forward debt explicitly in this plan's SUMMARY, with severity and
    consequence, so it survives into the next milestone's backlog rather than evaporating:

    - **CR-03 (WARNING)** — `.github/workflows/branch-protection.yml:27-28` declares
      `permissions: contents: read`, which sets every unlisted scope to none. Block (c) of
      `bin/verify-branch-protection:107-108` queries an endpoint needing administration scope and
      maps a 403 to the passing branch, so its "no classic protection is stacking" assertion is
      unfalsifiable in CI. It passed locally only because the operator's token carries admin.
    - **CR-04 (WARNING)** — `bin/verify-branch-protection:95-104` launders an API failure into
      "zero check runs emitted" in half (b): the `check-runs` endpoint needs `checks: read`, which
      the same `permissions: contents: read` block withholds, and on any API failure the pipeline
      yields an empty `EMITTED_COUNT`. It fails closed today, so it is benign now — but it is the
      same file and severity class as CR-03 and rests on the same missing-scope root cause.
    - **CR-05 (WARNING)** — `.github/rulesets/main.json` is a checked-in snapshot that nothing
      diffs against live state, so `enforcement` and `bypass_actors` are never asserted. A bypass
      actor added through the UI, or enforcement flipped to `evaluate`, passes all three blocks.
      This matters specifically because the accepted merge lock is predicated on
      `bypass_actors: []`.

    Do NOT fix either one in this run — maintainer-confirmed scope excludes them. Record them.
  </action>
  <verify>
    <automated>bash -c 'test "$(grep -c "^| GREEN-.* | Phase 198 | Complete |$" .planning/REQUIREMENTS.md)" -eq 12 && test "$(grep -c "^- \[ \] \*\*GREEN-" .planning/REQUIREMENTS.md)" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` shows all 12 GREEN rows as `[x]` in the checkbox list and `Complete` in the status table, and the two representations agree row for row.
    - Every flipped row cites the measured evidence for the flip in the SUMMARY (run id, command, or test module) — no row is flipped on inference.
    - `.planning/STATE.md` names the green `main` run id and PR #26's `CLEAN` status in `last_activity_desc`, and its progress block is internally consistent.
    - The SUMMARY contains a "Carried-forward debt" section with CR-03, CR-04, and CR-05, each with severity and one-line consequence.
    - `git diff .github/` is empty for this task — no workflow, ruleset, or protection file is touched.
    - `mix test` still exits 0 after the documentation edits.
  </acceptance_criteria>
  <done>The project's own record states what is true, and the two guard weaknesses this run deliberately did not fix are written down rather than forgotten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree -> public `origin/main` | Published commits cannot be un-published; the no-history-rewrite constraint applies |
| PR -> `main` via the `CI required` gate | The gate must be satisfied, never bypassed or weakened, to reach green |
| project record -> future maintainer | A requirements file that over-claims is a false audit trail in a project whose thesis is honest record-keeping |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-13-01 | Elevation of Privilege | branch protection / ruleset / `bypass_actors` | critical | mitigate | Explicitly forbidden in Task 2's action; acceptance criteria assert the ruleset is byte-identical and re-read live `enforcement` and `bypass_actors` |
| T-198-13-02 | Information Disclosure | commits pushed to a public repository | medium | accept | Posture decided at D-30 and gated by 198-02's full-history credential audit; these commits are code and planning docs inside that existing authorization |
| T-198-13-03 | Repudiation | `.planning/REQUIREMENTS.md` status flips | high | mitigate | Every flip must cite a run id, command, or test module; both representations asserted to agree |
| T-198-13-04 | Tampering | stale remote branches deleted without record | medium | mitigate | D-31 applies: ancestry confirmed, or archive tag plus an `ARCHIVE-REGISTER.md` row before removal |
| T-198-13-05 | Spoofing | drift guard blind to a re-armed classic protection (CR-03) | medium | accept | Out of maintainer-confirmed scope this run; recorded as carried-forward debt with its consequence stated |
</threat_model>

<verification>
- `mix ci.all` exits 0.
- `gh run list --workflow=ci.yml --branch main --limit 1` reports `conclusion: success` in ≤ 20 minutes.
- `gh pr view 26 --json mergeStateStatus` reports `CLEAN`.
- `git log origin/main..main` is empty.
- `bin/verify-branch-protection` exits 0 and the live ruleset still reports `enforcement: active`, `bypass_actors: []`, single required context `CI required`.
- `git diff origin/main -- .github/rulesets/main.json .github/workflows/branch-protection.yml` is empty.
- `.planning/REQUIREMENTS.md` shows 12/12 GREEN complete, in both representations, each backed by cited evidence.
</verification>

<success_criteria>
Phase 198's goal statement is true as written: `origin/main` carries every local commit, its CI
concludes green in 6-ish minutes against a 20-minute bound, the red-test baseline is retired with
every former failure fixed on its merits, branch protection requires exactly the check CI emits
and was not touched to get here, and the measurement sweep is on disk.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol | Kind | Path |
|---|---|---|
| GREEN-04 / GREEN-07 / GREEN-11 status flips | changed rows in both the checkbox list and the status table | `.planning/REQUIREMENTS.md` |
| updated position, `stopped_at`, `last_activity_desc`, progress block | changed frontmatter and body | `.planning/STATE.md` |
| `ci/198-gaps` | temporary branch, deleted after merge | git ref only |
| CR-03 / CR-04 / CR-05 carried-forward debt entries | new SUMMARY section | `.planning/phases/198-green-bringup/198-13-SUMMARY.md` |
| `.planning/ARCHIVE-REGISTER.md` row(s) | conditional — only if a stale remote branch proves unmerged | `.planning/ARCHIVE-REGISTER.md` |

No source, workflow, ruleset, mix alias, CI job key, or step id changes.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/198-green-bringup/198-13-SUMMARY.md` when done
</output>
