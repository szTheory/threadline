---
phase: 198-green-bringup
plan: 22
type: execute
wave: 4
gap_closure: true
depends_on:
  - 198-21
files_modified:
  - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - GREEN-04
  - GREEN-07

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real GitHub Actions run exists on a fresh `ci/198-*` branch whose head commit contains the 198-19 and 198-21 changes, and its run id, per-job conclusions, and wall-clock duration are recorded on disk."
    - "`Run test suite (current)` is observed on that measured run and its conclusion is recorded verbatim — if `success`, the GREEN-04 search_path cause is closed against admissible evidence; if not, the new failure is recorded verbatim without interpretation."
    - "`CI required`'s conclusion on that measured run is recorded verbatim, alongside the conclusion of every one of its `needs:` members, so the aggregate's verdict can be traced to its constituents."
    - "GREEN-04 and GREEN-07 are marked Complete in `.planning/REQUIREMENTS.md` if and only if the measured CI run supports it — never on local `mix test` output, and never on a prior run's result."
    - "The measured wall-clock duration is compared against the ≤20-minute clause and the comparison is recorded with the actual figure, not a qualitative claim."
    - "If the run is not green, the report says so plainly, names each still-red job with its first failure, and leaves the affected requirements Pending with an evidence-backed inline note citing the new run id — the same discipline round 2's record demonstrated."
    - statement: "Adjacency edge (GREEN-07): only a conclusion of exactly `success` counts. `neutral`, `skipped`, and `cancelled` are recorded as not-success, because under `re-actors/alls-green` a skipped required check is scored by GitHub as passing and that is precisely the laundering D-09 rejects."
      verification: explicit
    - statement: "Boundary edge (GREEN-07): the ≤20-minute clause is evaluated against the measured wall clock with the exact seconds recorded; a duration at or below 20m00s satisfies it and anything above does not, stated as a figure rather than a judgment."
      verification: explicit
    - statement: "Ordering edge (GREEN-07): when more than one workflow run exists for the pushed head SHA, the most recent completed run is authoritative and the earlier ones are listed with their ids so the selection is auditable rather than implicit."
      verification: explicit
    - statement: "Empty edge (GREEN-07): if the run reports zero `needs:`-member check conclusions, the measurement is treated as failed collection rather than as a pass, so a broken `gh` query cannot manufacture a green record."
      verification: explicit
    - statement: "Idempotency edge (GREEN-04): re-running the measurement query against the same run id yields the same recorded conclusions; the recorded evidence is a run id plus its conclusions, not a transient console read."
      verification: explicit
    - statement: "Concurrency edge (GREEN-04): the pushed branch is fresh and named for this round, so a concurrent run on the older `ci/198-gap-closure` branch cannot be mistaken for this round's evidence."
      verification: explicit
  artifacts:
    - ".planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — a round-3 measured section with run id, per-job conclusions, and wall clock"
    - ".planning/REQUIREMENTS.md — GREEN-04 and GREEN-07 statuses set strictly from the measured run"
    - ".planning/STATE.md — current position updated to the measured outcome"
  key_links:
    - "fresh `ci/198-*` branch head SHA → GitHub Actions run → `CI required` conclusion → GREEN-07's success clause"
    - "`Run test suite (current)` conclusion → GREEN-04's admissible evidence"
    - "recorded run id → `198-VERIFICATION.md`'s next round, which must be able to re-query the same evidence"
  prohibitions:
    - statement: "GREEN-04 and GREEN-07 must not be marked Complete on local-only evidence. Local `mix test` and local `mix ci.all` results are recorded as readiness signals only."
      status: recalled
      verification: flagged-unverified
    - statement: "Tier-A `page.*` evidence regeneration is forbidden for this milestone."
      status: recalled
      verification: flagged-unverified
    - statement: "`ALTER DATABASE ... SET search_path` must never be applied to the `threadline_test` database."
      status: recalled
      verification: flagged-unverified
    - statement: "No test may be skipped, deleted, retried-until-green, or re-run selectively to produce a greener measured result. The first completed run on the pushed head SHA is the evidence."
      status: recalled
      verification: flagged-unverified
    - statement: "Direct pushes to `main` are refused by the repository ruleset by design (GREEN-08 working correctly) and must not be worked around, bypassed, or force-pushed."
      status: recalled
      verification: flagged-unverified
    - statement: "A requirement's status may not be marked Complete because the remaining failure is 'known', 'deferred', or 'accepted'. Those dispositions are recorded; they do not change a status."
      status: recalled
      verification: flagged-unverified
---

<objective>
Produce the only evidence this phase accepts for GREEN-04 and GREEN-07: a real,
measured CI run.

This phase's own history is the argument for the discipline. Round 1 marked GREEN-04
Complete on local `mix test` output and a real CI run refuted it the same day,
exposing two live defects that no local run could ever have shown. Round 2's record
then correctly kept GREEN-04 and GREEN-07 Pending with the exact run id and cause —
and `198-VERIFICATION.md` singled that out as the traceability discipline working on
itself.

Purpose: close, or honestly re-open, the two remaining requirements against
admissible evidence.
Output: a measured round-3 CI record, and requirement statuses that follow it
rather than lead it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push a fresh `ci/198-round3` branch and observe one real CI run to completion</name>
  <read_first>
    - `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` — the round-2 measured section, so this round's record uses the same field set (run id, per-job conclusion, per-job duration, wall clock) and is directly comparable.
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` decision D-01 — the definition of green, and D-34's ordering constraints on irreversible steps.
    - `.github/rulesets/main.json` — confirm that direct pushes to `main` are refused and that a pull request is the only merge path, so the branch-and-PR route is the designed one rather than a workaround.
    - `.planning/phases/198-green-bringup/198-19-SUMMARY.md` and `198-21-SUMMARY.md` — confirm both landed and are in the commit range about to be pushed.
  </read_first>
  <files>(no repository files modified; pushes branch `ci/198-round3` and opens a pull request)</files>
  <precondition>`gh` is authenticated against this repository with permission to push a branch and open a pull request, and the local working tree is clean with the 198-19 and 198-21 commits present on the current branch.</precondition>
  <reversibility rating="reversible">Pushing a topic branch and opening a pull request creates no irreversible state; `.planning/` is already public on `origin/main` per D-30, so no new disclosure occurs.</reversibility>
  <action>
    Confirm the working tree is clean and that the head commit contains both the
    `ci.yml` search_path change from 198-19 and the merge-gate contract tests from
    198-21. Record the head SHA.

    Push the current head to a NEW remote branch named for this round —
    `ci/198-round3` — rather than reusing `ci/198-gap-closure`, so this round's
    evidence cannot be confused with round 2's. Open a pull request against `main`
    whose body names the two round-3 changes and cites the prior measured run
    `33197493051` as what this run supersedes.

    Do not push to `main` directly; the ruleset refuses it and that refusal is
    GREEN-08 working as intended.

    Watch the run to completion. Record the run id, the head SHA it ran against, the
    total wall-clock duration in minutes and seconds, and the conclusion of every
    check the run reports — not only the failures. If more than one run exists for
    the head SHA, list every run id and state which one is authoritative and why.

    Do not re-run, re-dispatch, or selectively retry any job. If a job fails on
    infrastructure grounds rather than on the commit under test, record that
    verbatim and say so, but do not replace the failed run's record with a rerun's.
  </action>
  <acceptance_criteria>
    - `gh run view <run_id> --json conclusion,headSha,createdAt,updatedAt` returns a completed run whose `headSha` equals the recorded local head SHA.
    - `gh run view <run_id> --json jobs` yields a conclusion for every job; the number of job conclusions collected is greater than zero and is recorded — a zero-length result is treated as failed collection, not as a pass.
    - The conclusions of the checks named `Run test suite (current)`, `Run test suite (min)`, and `CI required` are each recorded verbatim as the literal string GitHub returned.
    - The wall-clock duration is recorded as an exact figure in minutes and seconds, computed from `createdAt` and `updatedAt`.
    - The branch pushed is `ci/198-round3` and `git ls-remote --heads origin ci/198-round3` returns a SHA equal to the recorded head SHA.
    - `git log origin/main..HEAD --oneline | wc -l` is recorded, establishing what `origin/main` does and does not yet carry.
    - No job was re-run: `gh run view <run_id> --json attempt` reports attempt 1, or any higher attempt is explained verbatim in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>gh run list --branch ci/198-round3 --limit 1 --json databaseId,conclusion,headSha</automated>
  </verify>
  <done>One completed CI run on a fresh `ci/198-round3` branch is identified by run id and head SHA, with every job's conclusion and the exact wall clock captured, and no rerun substituted for it.</done>
</task>

<task type="auto">
  <name>Task 2: Write the round-3 measured record, with a three-way comparison against rounds 1 and 2</name>
  <read_first>
    - `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` in full — rounds 1 and 2 sections and the existing three-way comparison table format this round extends to four columns.
    - The Task 1 measurement output.
    - `.planning/phases/198-green-bringup/198-VERIFICATION.md` — the seven originally-red baseline jobs and the running count of how many are now green, so this round's count continues that series rather than restarting it.
    - `.planning/audits/198-ci-required-aggregate-decision.md` — so any lane made non-blocking in 198-21 is reported as non-blocking-by-decision rather than as silently absent.
  </read_first>
  <files>.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</files>
  <action>
    Append a round-3 measured section to `198-CI-MEASUREMENT.md` containing: the run
    id, head SHA, branch, pull request number, wall clock, and a per-job table of
    conclusions. Extend the existing baseline comparison to a four-column form
    (baseline, round 1, round 2, round 3) so the trajectory of the seven originally-
    red jobs is legible at a glance and the count of jobs now genuinely green is
    stated as a number.

    For every job still red, record its first failure message verbatim from the run
    logs and classify it as one of: a newly discovered cause, a previously known and
    deferred cause with a pointer to `deferred-items.md`, or a lane made
    non-blocking by the 198-20 decision. Do not classify anything as "expected red"
    without one of those three citations.

    State the ≤20-minute evaluation as an explicit comparison of the measured figure
    against the threshold. State the `CI required` conclusion as the literal string
    returned, and state whether it is exactly `success`.

    Close the section with a plain sentence on whether the phase goal's headline
    claim — `origin/main` carries every local commit and its CI concludes green — is
    now true, false, or true-subject-to-a-merge, with the pull request's
    `mergeStateStatus` recorded to support it.
  </action>
  <acceptance_criteria>
    - `198-CI-MEASUREMENT.md` contains a round-3 section naming the new run id and the branch `ci/198-round3`.
    - The four-column baseline comparison table is present and its round-3 column has one entry per originally-red baseline job.
    - Every still-red job in the round-3 column carries a verbatim first-failure line and exactly one of the three permitted classifications with its citation.
    - The section contains the measured wall clock as a figure and an explicit statement of whether it is at or below 20 minutes.
    - The section contains the literal `CI required` conclusion string and a yes/no statement of whether it equals `success`.
    - The section records `gh pr view <n> --json mergeStateStatus` output verbatim.
    - `git diff --name-only` for this task lists exactly `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'ci/198-round3' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md &amp;&amp; grep -q 'mergeStateStatus' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</automated>
  </verify>
  <done>A round-3 measured section records the run, extends the baseline trajectory to four columns, classifies every remaining red job with a citation, and states the goal's headline claim as true or false with evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Set GREEN-04 and GREEN-07 statuses strictly from the measured run, and update STATE</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 10-25 and the status table around line 130 — GREEN-04's and GREEN-07's current entries, including the inline evidence notes round 2 wrote, whose format and honesty this task must match or exceed.
    - The Task 2 round-3 measured section.
    - `.planning/STATE.md` frontmatter (`status`, `stopped_at`, `last_activity_desc`) and the `## Current Position` block — the fields to update, noting the project convention that the bespoke progress block is hand-corrected after any state handler call.
    - `.planning/phases/198-green-bringup/198-CONTEXT.md` decision D-01 — the admissibility rule this task enforces.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Update GREEN-04's entry. Mark it Complete only if the measured run's
    `Run test suite (current)` and `Run test suite (min)` both concluded `success`
    and the round-3 record shows no remaining failure attributable to an unprefixed
    owned-schema call site or a missing example-app search_path. Otherwise leave it
    Pending and rewrite its inline note to cite the new run id, the exact failing
    check, and the exact first failure message — replacing the now-superseded
    reference to run `33197493051` while preserving the history of what was fixed.

    Update GREEN-07's entry. Mark it Complete only if `CI required` on the measured
    run concluded exactly `success` AND the measured wall clock is at or below 20
    minutes AND `git log origin/main..HEAD` is empty or the pull request is merged so
    that it becomes empty. If any of those three fails, leave it Pending with an
    inline note stating which of the three clauses failed and by how much, citing the
    new run id.

    Do not mark either requirement Complete because a remaining failure is known,
    deferred, accepted, or made non-blocking by the 198-20 decision. Those
    dispositions belong in the note, not in the status. If the 198-20 decision moved
    a lane out of `CI required` and that is what allowed the aggregate to conclude
    `success`, say so explicitly in GREEN-07's note — a closure that depended on
    narrowing the gate must be legible as such to anyone reading the requirement
    later.

    Update the requirement status table at the bottom of `REQUIREMENTS.md` to agree
    with the entries above; the two must not disagree.

    Update `.planning/STATE.md`: `status`, `stopped_at`, `last_activity`,
    `last_activity_desc`, and the `## Current Position` block, naming the new run id
    and the resulting requirement statuses. Hand-correct the bespoke progress block
    afterwards rather than trusting a state handler to compute it.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s GREEN-04 and GREEN-07 entries each cite the new run id from Task 1.
    - Neither entry cites `33197493051` as its current evidence, though it may appear as superseded history.
    - The checkbox state of GREEN-04 and GREEN-07 in the prose entries matches their row in the status table — verified by reading both and stating the match in the SUMMARY.
    - If either is marked Complete, the SUMMARY quotes the exact measured conclusion string that justifies it; if either is Pending, the SUMMARY quotes the exact failing check and its first failure message.
    - `.planning/STATE.md` frontmatter `last_activity_desc` names the new run id and the `CI required` conclusion.
    - `grep -c 'GREEN-04' .planning/REQUIREMENTS.md` and `grep -c 'GREEN-07' .planning/REQUIREMENTS.md` are each unchanged or greater than their pre-task values — no requirement entry was deleted.
    - `git diff --name-only` for this task lists exactly `.planning/REQUIREMENTS.md` and `.planning/STATE.md`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'GREEN-04' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'GREEN-07' .planning/REQUIREMENTS.md &amp;&amp; grep -qE 'run [0-9]{8,}' .planning/STATE.md</automated>
  </verify>
  <done>GREEN-04 and GREEN-07 carry statuses that follow the measured run rather than lead it, with the new run id cited in both the requirement entries and STATE, and any gate-narrowing dependency stated explicitly in GREEN-07's note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell → GitHub API (`gh`) | The maintainer's credential pushes a branch, opens a pull request, and reads run logs; log content may contain incidental output |
| Local commits → public `origin` | Pushed content becomes public; `.planning/` is already public per D-30 |
| Measured run → requirement status | The run's conclusions are the sole admissible basis for two requirement verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-22-01 | Repudiation | requirement status verdict | high | mitigate | Statuses are derived from a recorded run id and verbatim conclusion strings, re-queryable by any later reader; prohibitions forbid Complete-on-local-evidence and Complete-because-deferred |
| T-198-22-02 | Tampering | measured evidence | high | mitigate | No rerun, re-dispatch, or selective retry is permitted; the run attempt number is recorded and any attempt above 1 must be explained verbatim |
| T-198-22-03 | Elevation of Privilege | branch protection | high | mitigate | Direct pushes to `main` are refused by the ruleset and the plan forbids working around, bypassing, or force-pushing; the pull request is the only merge path |
| T-198-22-04 | Information Disclosure | pushed content and CI logs | medium | accept | `.planning/` is already tracked and public on `origin/main` (2191 files at round-1 retraction time); D-30 ratified publish-as-is, and the D-28 credential audit already gated the original push |
| T-198-22-05 | Spoofing | run selection | medium | mitigate | The run's `headSha` must equal the recorded local head SHA, and a fresh branch name prevents round-2 evidence being mistaken for round 3 |
| T-198-22-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs; no dependency or lockfile is touched, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `gh run list --branch ci/198-round3 --limit 1 --json databaseId,conclusion,headSha` returns a completed run whose `headSha` matches the local head.
- `198-CI-MEASUREMENT.md` contains a round-3 section with the run id, the four-column baseline comparison, the exact wall clock, the `CI required` conclusion string, and the pull request `mergeStateStatus`.
- `.planning/REQUIREMENTS.md` GREEN-04 and GREEN-07 cite the new run id and their prose entries agree with the status table.
- `.planning/STATE.md` names the new run id in `last_activity_desc`.
</verification>

<success_criteria>
The phase's two open requirements are resolved against a real, re-queryable CI run —
closed if the run supports it, honestly re-opened with the exact new cause if it does
not. In either case the record is precise enough that the next verifier can re-derive
the verdict from the run id alone.
</success_criteria>

## Artifacts this phase produces

**New remote refs**
- Branch `ci/198-round3` on `origin`, and its pull request against `main`.

**Changed planning artifacts**
- `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` — round-3 measured
  section: run id, head SHA, per-job conclusion table, four-column baseline
  trajectory, exact wall clock, `CI required` conclusion, `mergeStateStatus`.
- `.planning/REQUIREMENTS.md` — GREEN-04 and GREEN-07 statuses and inline evidence
  notes, citing the new run id.
- `.planning/STATE.md` — `status`, `stopped_at`, `last_activity`,
  `last_activity_desc`, `## Current Position`, and the hand-corrected progress block.

**No source files, no CI job ids, no test functions, and no mix aliases are created
or changed by this plan.**

<output>
Create `.planning/phases/198-green-bringup/198-22-SUMMARY.md` when done.
</output>
