---
phase: 198-green-bringup
plan: 56
type: execute
wave: 6
depends_on: [198-55]
files_modified:
  - .planning/audits/198-summary-coverage-manifest.json
  - test/threadline/phase198_zero_human_uat_contract_test.exs
autonomous: true
gap_closure: true
requirements: [GREEN-04]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The repository-owned Phase-198 summary contract accepts and validates completed summaries 53-55 instead of deterministically failing the required test lane."
    - "The audited closeout namespace is exactly 48-59, and final-state mode requires exactly summaries 01-59 only after the last audited summary exists."
    - "Plan 60 is a separately declared terminal certification summary: it is outside the audited 01-59 set, may appear only at number 60, and remains subject to the same mechanical coverage-shape rules without making the final-set gate recursive."
    - "Missing, extra, renamed, malformed, human-only, or non-passing closeout coverage remains a loud failure."
  artifacts:
    - ".planning/audits/198-summary-coverage-manifest.json — exact Phase-198 baseline, closeout namespace, and final plan number"
    - "test/threadline/phase198_zero_human_uat_contract_test.exs — manifest-driven exact discovery and closeout validation"
  key_links:
    - "manifest final_plan_number -> generated exact closeout set -> discovery assertion -> per-summary coverage validation"
  prohibitions:
    - {statement: "The closeout fix must not weaken exact-set discovery or automated-evidence requirements.", status: pending, verification: test}
---

<objective>
Repair GREEN-04's summary-coverage regression while keeping the gate exact and non-self-invalidating.

Purpose: Plans 53-55 created legitimate summaries outside the 48-52 closeout namespace, making the current contributor test lane deterministically red.
Output: one manifest-owned exact 01-59 audited contract plus an explicit, non-recursive Plan-60 certification-summary rule consumed by the terminal certification plan.

<assumption_delta_decision>Primary noun: Phase-198 closeout namespace. Decision: promote — the manifest's final plan number is the single exact namespace authority consumed by the test.</assumption_delta_decision>

### Multi-source coverage audit

- GOAL/REQ: GREEN-04 is directly covered; GREEN-07 remains accepted-Pending and unchanged.
- RESEARCH/CONTEXT: D-01's honest `mix test` contract is restored without using local narration as CI evidence; D-39 and D-42 protected paths are untouched.
- VERIFICATION/SECURITY: the round-11 GREEN-04 regression is fully covered here; security gaps are assigned to Plans 57-59.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-48-SUMMARY.md
@.planning/phases/198-green-bringup/198-53-SUMMARY.md
@.planning/phases/198-green-bringup/198-54-SUMMARY.md
@.planning/phases/198-green-bringup/198-55-SUMMARY.md
@.planning/audits/198-summary-coverage-manifest.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extend the exact closeout path through Plan 59</name>
  <files>.planning/audits/198-summary-coverage-manifest.json, test/threadline/phase198_zero_human_uat_contract_test.exs</files>
  <read_first>
    - test/threadline/phase198_zero_human_uat_contract_test.exs — current duplicate 48-52 range and final assertion
    - .planning/audits/198-summary-coverage-manifest.json — repository-owned namespace authority
    - .planning/phases/198-green-bringup/198-53-SUMMARY.md — first rejected closeout summary
    - .planning/phases/198-green-bringup/198-54-SUMMARY.md — blocking-decision summary shape
    - .planning/phases/198-green-bringup/198-55-SUMMARY.md — final rejected round-11 summary
  </read_first>
  <behavior>
    - Test 1: summaries 53, 54, and 55 are discovered inside the allowed closeout namespace and independently pass the existing structural evidence rules
    - Test 2: an inserted summary number below 01, between the permitted sets, or above 60 fails as an unexpected number; 60 is accepted only as the declared terminal certification summary
    - Test 3: final-state mode requires exact audited summaries 01-59 and therefore fails before 198-59-SUMMARY.md exists, while normal mode accepts only the exact present subset of 48-59 plus optional terminal summary 60
    - Test 4: any closeout coverage entry with missing ref, non-pass status, or human_judgment other than false fails
  </behavior>
  <action>Start RED by asserting the current 53-55 summaries are legitimate closeout inputs and that the last audited plan number is 59. Add explicit `audited_final_plan_number: 59` and `terminal_certification_plan_number: 60` fields to the manifest, extend `allowed_closeout_numbers` and `final_state_numbers` through 59, and make the test derive its closeout and final sets from those fields after validating contiguity, uniqueness, string shape, agreement with 48 as the closeout start, and that the certification number is exactly audited-final plus one. Preserve the immutable 01-47 digest baseline. Normal mode must reject every discovered number outside baseline, the exact closeout set, and the single optional certification number; it validates every present closeout or certification summary with the existing mechanical coverage rules. Final mode is an explicit real-tree audit of exactly 01-59 and must reject an early/missing/extra audited summary. Plan 60 owns that executable final-mode run after 198-59-SUMMARY.md exists and before its own deliberately excluded certification summary is emitted.</action>
  <verify>
    <automated>mix test test/threadline/phase198_zero_human_uat_contract_test.exs</automated>
    <fails_when>Summaries 53-55 remain rejected; the manifest/test disagree; the allowed range is non-contiguous or does not end at 59; an outside summary passes; a malformed closeout coverage entry passes; or final mode can pass before the exact 01-59 set exists.</fails_when>
  </verify>
  <done>The focused repository-owned contract passes now without weakening its exact terminal check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove strict discovery and final-mode timing with isolated fixtures</name>
  <files>test/threadline/phase198_zero_human_uat_contract_test.exs</files>
  <read_first>
    - test/threadline/phase198_zero_human_uat_contract_test.exs — Task-1 manifest-driven helpers and disclosure checks
    - .planning/phases/198-green-bringup/198-52-SUMMARY.md — empty coverage closeout shape that remains valid
  </read_first>
  <behavior>
    - Test 1: a temporary copied phase directory with exact audited summaries 01-59 passes final discovery
    - Test 2: removing one audited file, duplicating a number under a malformed name, or adding 61 fails with the offending path or number
    - Test 3: adding the sole 198-60-SUMMARY.md certification fixture leaves the audited final set 01-59 unchanged but independently fails unless its coverage is `human_judgment: false` with passing automated refs
    - Test 4: empty coverage is accepted only as a structurally explicit empty list; malformed YAML-like text is rejected
  </behavior>
  <action>Add fixture-level tests around the discovery and closeout validators so the current tree's partial execution state is distinct from terminal exact-set validation. Exercise Plan 60 as a named certification exception rather than expanding the audited range: its presence must not change the exact 01-59 assertion, no other exception number is legal, and any Plan-60 coverage entry is still mechanically validated. Keep negative fixtures in temporary directories and make diagnostics identify the missing or extra number. Do not alter any completed summary or its coverage claims.</action>
  <verify>
    <automated>mix test test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; mix test test/threadline/phase198_nyquist_contract_test.exs</automated>
    <fails_when>Fixture mutations do not fail, diagnostics omit the offending number/path, any completed summary changes, or the Nyquist backstop regresses.</fails_when>
  </verify>
  <done>Exact namespace detection has positive and negative evidence independent of the current partial closeout state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| summary files -> required ExUnit lane | Planning artifacts influence whether the contributor test contract is green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-56-01 | Tampering | closeout namespace | high | mitigate | Single manifest authority, contiguous-range validation, exact final set, and outside-number fixtures. |
| T-198-56-02 | Spoofing | closeout coverage | high | mitigate | Independently require explicit automated passing refs for every present non-empty closeout entry. |
| T-198-56-03 | Denial of Service | self-invalidating final gate | medium | mitigate | Separate normal partial-closeout mode from explicit post-summary final mode. |
</threat_model>

<verification>
- Run both task commands and `git diff --check`.
- Plan 60, not this plan's executor, runs `PHASE198_SUMMARY_SET=final mix test test/threadline/phase198_zero_human_uat_contract_test.exs` after 198-59-SUMMARY.md exists; it must require exactly audited summaries 01-59.
- Run `mix test` after all four plans; GREEN-04 cannot close from the focused test alone under D-01.
</verification>

<success_criteria>
- Summaries 53-55 no longer make `mix test` deterministically red.
- Exact detection remains strict through audited Plan 59, with only the tested, non-recursive Plan-60 certification-summary exception.
- GREEN-07 remains Pending and no protected CI topology changes.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-56-SUMMARY.md` when done.</output>
