---
phase: 198-green-bringup
plan: 55
type: execute
wave: 6
depends_on: [198-54]
files_modified:
  - .planning/ARCHIVE-REGISTER.md
  - .planning/audits/198-round11-ref-disposition.md
  - .planning/audits/198-round11-ref-disposition.json
autonomous: true
gap_closure: true
requirements: [GREEN-12]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Plan 55 cannot begin unless a live-revalidated round-11 decision says verbatim `retire` over the unchanged complete subject set."
    - "Every distinct local or remote tip is recoverable from matching local and origin annotated archive tags plus a tracked D-31 register row before its branch or PR handle is retired."
    - "Retirement uses exact refspecs and exact PR identities one at a time; the complete derived local and remote `ci/198-*` branch sets are empty only after every preservation proof passes."
    - "PR #34, `origin/main`, required contexts, ruleset/protection, active branch/upstream within each task, and the one-worktree invariant remain unchanged."
    - "GREEN-12 closes without claiming GREEN-07, merging any PR, landing main, or modifying a required gate."
  artifacts:
    - ".planning/ARCHIVE-REGISTER.md — one D-31 row per distinct retired object with archive tag and restore command"
    - ".planning/audits/198-round11-ref-disposition.md — ordered preservation/mutation receipts and final GREEN-12 verdict"
    - ".planning/audits/198-round11-ref-disposition.json — execution baselines, per-step receipts, final live state, and exact restoration data"
  key_links:
    - "retire authority -> annotated tag locally -> exact tag push -> peeled-object/register equality -> PR/branch retirement -> full-namespace empty proof"
  assumptions:
    - {statement: "The number of archive tags is derived from distinct side/SHA subjects, not assumed to equal the branch count; the currently divergent branch therefore requires two preservation paths.", status: unverified, flagged: true}
    - {statement: "Spec-less ordering and empty-set edges close only if the validator proves preserve-before-delete receipts and derives final emptiness from live namespaces rather than a literal known-name list.", status: unverified, flagged: true}
  prohibitions:
    - {statement: "Do not use wildcard, mirror, all-tags, force, rebase, squash, branch-switch, merge, ruleset, or protection mutations.", status: unverified, flagged: true}
    - {statement: "Do not retire any handle until every distinct object reachable only through that handle has verified local and remote annotated-tag recovery plus a tracked register row.", status: unverified, flagged: true}
    - {statement: "Do not relabel GREEN-07 or roadmap criterion 3 as met from repository-hygiene work.", status: unverified, flagged: true}
---

<objective>
Execute the exact human-authorized, preservation-first retirement and prove GREEN-12 from the complete live namespace.

Purpose: remove stale Phase-198 CI handles without losing any distinct object or changing main, CI guarantees, branch protection, or Phase-199 work.

Output: verified annotated archives on both local and origin, immutable register rows, closed stale PRs, empty Phase-198 CI branch namespaces, and ordered receipts.

<assumption_delta_decision>Primary noun: preservation subject. Decision: no-change — execution follows the distinct side/SHA identity ratified in Plans 53-54, including multiple objects under one branch name.</assumption_delta_decision>

### Artifacts this phase produces

- Collision-free `archive/ci/198-*` annotated tags derived from every distinct round-11 preservation subject, locally and on origin.
- One `.planning/ARCHIVE-REGISTER.md` row per distinct object with SHA, base, ancestry, diffstat, date, GREEN-12, recommendation, rationale, restore command, and evidence pointer per D-31.
- Ordered `authority`, preservation, PR-close, remote-delete, local-delete, control, post-target, and final receipts in the round-11 Markdown/JSON evidence.
- No new code, dependency, schema, UI, main commit landing, ruleset, protection, required-check, or workflow change.

### Multi-source coverage audit

- GOAL/REQ: this plan closes only GREEN-12's stale-ref clause. The measurement sweep, test baseline, emitted-check protection, critic, publish, and flake requirements stay covered by Plans 01-51.
- CONTEXT/RESEARCH: D-31 preservation metadata and exact restoreability govern every object; D-39 and D-42 forbid laundering CI status or narrowing gates.
- VERIFICATION/UAT: automated evidence remains 192/192; this plan changes repository hygiene, not evaluator coverage or GREEN-07 status.
- SECURITY: exact authority, non-batched writes, stable-control checks, and preserve-before-delete ordering mitigate every high threat; no blocking threat is accepted.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-54-SUMMARY.md
@.planning/audits/198-round11-ref-disposition.md
@.planning/audits/198-round11-ref-disposition.json
@.planning/ARCHIVE-REGISTER.md
@.planning/phases/198-green-bringup/198-39-DECISION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Preserve and retire the divergent branch end-to-end</name>
  <precondition>`bin/verify-phase198-ref-disposition authority --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md --target ci/198-gap-closure` exits zero against live state and proves a verbatim `retire` decision over the unchanged complete round-11 subject set.</precondition>
  <files>.planning/ARCHIVE-REGISTER.md, .planning/audits/198-round11-ref-disposition.md, .planning/audits/198-round11-ref-disposition.json</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-54-SUMMARY.md — exact human choice and any decision-stage deviations
    - .planning/audits/198-round11-ref-disposition.md — divergent branch subjects, archive names, PR identity, and authority record
    - .planning/audits/198-round11-ref-disposition.json — exact local/remote SHAs, stable controls, and decision digest
    - .planning/ARCHIVE-REGISTER.md — D-31 immutable row convention
    - bin/verify-phase198-ref-disposition — authority, controls, post-target, and receipt-order enforcement
    - .planning/phases/198-green-bringup/198-39-DECISION.md — GREEN-07 non-promotion and protected-control boundary
  </read_first>
  <action>Run authority immediately before the first external mutation and halt on any subject/control drift or non-retire decision. Snapshot active branch, active full SHA, upstream, and upstream full SHA as a within-task no-mutation baseline. For each distinct local and remote tip of the divergent target, create its exact collision-free annotated archive tag with all D-31 fields, append the exact-SHA register row, push only that one tag ref, and verify the local peeled object, remote peeled object, and parsed register SHA are identical. Only after both object paths validate, close the exact associated PR if it is still open, delete only the exact remote branch ref, and delete only the exact local branch without force. After every tag, register, PR, and branch operation, append a structured receipt and run `controls`; then run `post-target`. Never switch branches or batch writes.</action>
  <reversibility rating="costly">Deleted branch and PR handles can be reconstructed from verified archives, but restoration requires coordinated local and hosted operations.</reversibility>
  <verify>
    <automated>bin/verify-phase198-ref-disposition post-target --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md --register .planning/ARCHIVE-REGISTER.md --target ci/198-gap-closure</automated>
    <fails_when>Authority is absent/stale; either distinct tip lacks matching local/origin annotated tags and a register row before retirement; receipt order is incomplete; the PR or exact branch handles remain; the active task baseline changes; or PR #34, origin/main, worktree, required contexts, ruleset, or protection drifts.</fails_when>
  </verify>
  <acceptance_criteria>
    - The highest-risk divergent case proves two-object preservation before one branch/PR retirement.
    - Every command targets one exact tag, ref, or PR identity and is followed by a control check.
    - Main, PR #34, Phase-199 provenance, ruleset/protection, and required checks remain outside authority and unchanged.
  </acceptance_criteria>
  <done>The divergent branch is retired without collapsing or losing either preserved object.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the remaining authorized refs and prove complete GREEN-12 hygiene</name>
  <precondition>The Task-1 `post-target` stage passes, and `authority` passes separately for every remaining round-11 target against the same unchanged verbatim `retire` decision, complete namespace packet, and stable controls.</precondition>
  <files>.planning/ARCHIVE-REGISTER.md, .planning/audits/198-round11-ref-disposition.md, .planning/audits/198-round11-ref-disposition.json</files>
  <read_first>
    - .planning/audits/198-round11-ref-disposition.md — remaining targets and completed divergent-target receipts
    - .planning/audits/198-round11-ref-disposition.json — remaining exact objects, PR/no-PR associations, and stable controls
    - .planning/ARCHIVE-REGISTER.md — verified Task-1 rows and exact tag convention
    - bin/verify-phase198-ref-disposition — per-target transitions and live-derived final-set checks
    - .planning/REQUIREMENTS.md — GREEN-12 predicate and GREEN-07 status boundary
  </read_first>
  <action>Snapshot a fresh within-task active branch/upstream baseline after the Task-1 artifact commit. For each remaining target independently, run `authority`; preserve every distinct object with its exact annotated tag, D-31 register row, single-tag push, and peeled-object equality proof; then close only its associated stale PR when one exists and delete only its exact remote/local branch handles. Run `controls` after every mutation and `post-target` after every branch target; append ordered receipts throughout. Finish with `final` before the Task-2 artifact commit: derive complete live local and remote `ci/198-*` sets and require both empty; require every associated stale PR closed; parse every round-11 preservation subject and prove exact equality with local/origin peeled archive tags and its register row; require exactly one worktree; and prove PR #34, origin/main, required contexts, ruleset/protection, and the within-task active branch/upstream baseline unchanged. Append GREEN-12's completed evidence without changing GREEN-07 or claiming roadmap criterion 3.</action>
  <reversibility rating="costly">All retired objects remain recoverable, but reconstructing multiple hosted/local handles and PR workflow state is coordinated work.</reversibility>
  <verify>
    <automated>bin/verify-phase198-ref-disposition final --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md --register .planning/ARCHIVE-REGISTER.md</automated>
    <fails_when>Either complete live Phase-198 CI branch set is non-empty; an associated stale PR remains open; any subject lacks matching local/origin peeled tag and register SHA; receipts are missing/out of order; worktree count differs from one; the active task baseline changes; a stable control drifts; or GREEN-07 is promoted.</fails_when>
  </verify>
  <acceptance_criteria>
    - Complete live local and remote `ci/198-*` branch namespaces are empty, derived rather than name-hardcoded.
    - Every distinct former tip has two verified tag copies, an immutable register row, and a tested restore command.
    - Associated stale PRs are closed; a no-PR ref is handled without fabricating a PR identity.
    - GREEN-12 is Complete on exact evidence while GREEN-07 remains Pending per D-39.
  </acceptance_criteria>
  <done>The Round-11 stale-ref gap is closed without silent discard or unrelated remote mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tracked authority -> Git/GitHub writes | A narrow human decision permits exact hosted/local mutations. |
| Mutable branch handles -> immutable archives | Objects must become independently recoverable before handles disappear. |
| External command receipts -> completion claim | Ordered receipts must prove what changed and what stayed fixed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-55-01 | Spoofing | retire authority | high | mitigate | Revalidate verbatim decision, digest, complete subjects, and live identities immediately before every target. |
| T-198-55-02 | Tampering | tag/ref/PR operations | high | mitigate | Exact one-object commands, peeled-tag equality, register joins, per-step receipts, and post-target checks; no batching or force. |
| T-198-55-03 | Repudiation | external mutations | medium | mitigate | Persist ordered argv-safe receipts, timestamps, before/after identities, and restoration commands. |
| T-198-55-04 | Information Disclosure | receipts | low | mitigate | Record ref/object/control outputs only; exclude tokens, headers, environment, and authentication material. |
| T-198-55-05 | Denial of Service | partial retirement | high | mitigate | Preserve first, operate one target at a time, validate controls/post-target after each step, and halt with already-created recovery paths intact. |
| T-198-55-06 | Elevation of Privilege | unrelated GitHub controls | high | mitigate | Pin PR #34, origin/main, required contexts, ruleset/protection, worktree, and task-local active provenance as immutable controls. |
</threat_model>

<verification>
- Every per-target authority and post-target stage passes in order.
- Final validation proves complete namespace emptiness, closed associated stale PRs, archive/register equality for every distinct object, one worktree, and unchanged controls.
- `git diff --check` passes; GREEN-07 remains Pending and protected workflow/ruleset paths are untouched.
</verification>

<success_criteria>
- No stale local or remote Phase-198 CI branch remains.
- No distinct object was discarded; all are recoverable locally, from origin tags, and from the tracked register.
- No unauthorized main, PR #34, ruleset, protection, required-check, workflow, or Phase-199 mutation occurred.
- GREEN-12 is truthfully closed without falsely claiming GREEN-07.
</success_criteria>

<output>
Create `.planning/phases/198-green-bringup/198-55-SUMMARY.md` when done.
</output>
