---
phase: 198-green-bringup
plan: 50
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts
  - bin/verify-row-history-focus-red-control
  - .planning/audits/198-row-history-focus-regression.md
  - test/threadline/row_history_focus_evidence_contract_test.exs
autonomous: true
gap_closure: true
requirements: [GREEN-06]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The row-history accessibility evidence records the exact command, project, viewport, repeat count, active element, dialog/input geometry, scroll offsets, and trace attachment for the measured run."
    - "A controlled known-bad obscuration makes the existing non-obscured-focus assertion fail within the existing bounded browser timeout, while the identical normal scenario passes without retry, force, timeout inflation, or assertion weakening."
    - "The red-control path is explicitly opt-in, test-only, and cannot affect a normal CI/browser execution."
    - "The evidence contract rejects removal of protected semantic/focus/overflow assertions and forbidden masking primitives."
  artifacts:
    - "bin/verify-row-history-focus-red-control — fail-first prover for the named Playwright assertion"
    - "test/threadline/row_history_focus_evidence_contract_test.exs — fast source/evidence integrity contract"
    - "198-row-history-focus-regression.md — complete measured evidence and red/clean control transcript"
  key_links:
    - "row-history scenario -> structured geometry/focus attachment -> evidence artifact"
    - "opt-in known-bad overlay -> existing `expectNonObscuredFocused` -> expected red -> normal clean green"
  prohibitions:
    - {statement: "The red control must not change screenshot baselines, product source, assertion thresholds, retries, or CI defaults.", status: unverified, flagged: true}
    - {statement: "A current green repetition must not be described as proof of the historical failure's original cause.", status: unverified, flagged: true}
---

<objective>
Supply the missing fail-first and measured-geometry evidence for the Plan-45 row-history accessibility control.

Purpose: close T-198-45-01 and T-198-45-02 without inventing a historical root cause or weakening the test that already passes on the current tree.

Output: structured trace/focus/geometry evidence, an opt-in known-bad control, and a fast contract preserving the prohibitions.

<assumption_delta_decision>Primary noun: current accessibility assertion integrity. Decision: no-change — the plan proves the assertion has teeth; it does not reclassify the non-reproduced historical symptom.</assumption_delta_decision>

### Artifacts this phase produces

- New executable path: `bin/verify-row-history-focus-red-control`.
- New test module: `Threadline.RowHistoryFocusEvidenceContractTest`.
- New test-only flag: `THREADLINE_ROW_HISTORY_RED_CONTROL=obscure-date-input`, accepted only by the named row-history scenario.
- New Playwright attachment schema for active-element and geometry evidence.
- No product source, UI styling, screenshot, database schema, dependency, or external mutation.

### Spec-less probe accounting

- This plan materially addresses GREEN-06 only: the red-control prover inherits and asserts the existing browser timeout/retry policy, fails the known-bad mount within that bound, and proves the clean scenario cannot inherit mutable failure state. GREEN-06's systemic mount-preflight path remains in Plan 48.
- GREEN-11 is not claimed here; broken-vs-flaky classification and issue deduplication remain owned by their existing Phase-198 artifacts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-45-PLAN.md
@.planning/phases/198-green-bringup/198-45-SUMMARY.md
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/audits/198-row-history-focus-regression.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add a narrowly-scoped fail-first control to the existing row-history scenario</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts, bin/verify-row-history-focus-red-control, test/threadline/row_history_focus_evidence_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts — named row-history scenario and `expectNonObscuredFocused` helper
    - .planning/phases/198-green-bringup/198-45-SUMMARY.md — current 10/10 and cross-project evidence with no product change
    - .planning/phases/198-green-bringup/198-SECURITY.md — T-198-45-02 required red-control proof
    - examples/threadline_phoenix/e2e/playwright.config.ts — project, retry, trace, and worker defaults that must remain unchanged
    - test/threadline/row_history_focus_evidence_contract_test.exs — create the fast source/argv contract before running the browser proof
  </read_first>
  <behavior>
    - Test 1: normal named mobile row-history scenario passes with the existing semantic/focus/non-obscured/overflow assertions
    - Test 2: exact opt-in known-bad value injects an obscuring element over the focused date input and fails at `expectNonObscuredFocused`
    - Test 3: missing/unknown flag values take the normal path or fail setup explicitly; no other spec or scenario reads the flag
    - Test 4: fast source/argv fixtures prove the prover uses identical bounded commands, rejects unexpected flags, and requires named-assertion RED before clean GREEN
  </behavior>
  <action>Within the named row-history test only, recognize the exact red-control value and inject a deterministic fixed-position test element over the already-focused date input after focus and before `expectNonObscuredFocused`. Mark it with a unique test-only attribute and remove it in cleanup. Do not branch around, delete, or alter any assertion. Create a defensive shell prover that first runs only this scenario on `mobile-chromium` with the flag, requires non-zero exit and output identifying the non-obscured assertion within the existing GREEN-06 browser timeout, then runs the byte-equivalent command without the flag and requires success. Preserve and assert Playwright retries/workers/timeouts and forbid screenshot updates. Create the fast ExUnit contract in this task with fixture-driven source and captured-argv checks so per-task acceptance does not require launching a browser; reserve the actual fail-first/clean browser pair for the plan-level gate.</action>
  <reversibility rating="reversible">The known-bad subject is opt-in test instrumentation isolated to one scenario.</reversibility>
  <verify>
    <automated>mix test test/threadline/row_history_focus_evidence_contract_test.exs --only red_control_contract</automated>
    <fails_when>The fast contract cannot prove exact flag scope, identical bounded argv, named-assertion RED-before-GREEN handling, unchanged retry/timeout policy, or screenshot/product-file prohibitions.</fails_when>
  </verify>
  <acceptance_criteria>
    - The fast contract establishes the prover's fail-closed RED-then-GREEN command contract; the plan-level browser gate machine-establishes the actual RED and clean GREEN outcomes.
    - The original assertion source is byte-identical except for surrounding instrumentation/evidence attachment.
    - No generic environment-driven assertion bypass exists.
  </acceptance_criteria>
  <done>T-198-45-02 has a repeatable fail-first proof tied to the actual protected assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist structured focus/geometry evidence and prohibit masking regressions</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts, .planning/audits/198-row-history-focus-regression.md, test/threadline/row_history_focus_evidence_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts — final Task-1 scenario and existing helper assertions
    - .planning/audits/198-row-history-focus-regression.md — incomplete commands/counts-only evidence
    - .planning/phases/198-green-bringup/198-SECURITY.md — T-198-45-01 trace/focus/geometry requirements
    - test/threadline/e2e_preflight_contract_test.exs — repository-style source/evidence contract precedent
  </read_first>
  <behavior>
    - Test 1: structured attachment contains viewport, active element, dialog/input rectangles, scroll offsets, project, attempt, and trace status
    - Test 2: audit records the exact negative/clean commands and observed counts without claiming an unreproduced historical cause
    - Test 3: removal of semantics/focus/non-obscured/overflow assertions or addition of force/retry/sleep masking fails the fast ExUnit contract
    - Test 4: red-control metadata is absent from a normal run's DOM after cleanup
  </behavior>
  <action>Capture one JSON attachment immediately before the protected focus assertion with project/viewport/repeat identity, `document.activeElement` identity, dialog and input bounding boxes, viewport dimensions, document/dialog scroll offsets, visibility, and the Playwright trace setting/path indicator available from test info. Update the audit with exact commands and results for the known-bad run, clean run, 10-repeat mobile run, and desktop adjacency run; attach or cite durable text/JSON evidence without committing transient trace archives. Explicitly state that this proves current assertion sensitivity, not the historical failure's cause. Expand the Task-1 fast ExUnit contract to check required fields/commands, exact protected assertion tokens, absence of masking constructs, unique flag scope, and the recorded result schema. Keep repeated Playwright execution in the plan-level gate.</action>
  <reversibility rating="reversible">Structured test attachments and evidence contracts are additive.</reversibility>
  <verify>
    <automated>mix test test/threadline/row_history_focus_evidence_contract_test.exs</automated>
    <fails_when>Required evidence fields/counts are absent, the audit overclaims historical causation, the red-control marker can leak, protected assertions/masking prohibitions fail, or the exact browser commands/results cannot be reconciled from the durable evidence.</fails_when>
  </verify>
  <acceptance_criteria>
    - T-198-45-01's trace, geometry, active-element, command, and repeat evidence is explicit and durable.
    - T-198-45-02's prohibition scan and red-control proof are both executable.
    - Normal mobile passes 10/10 and desktop adjacency remains green with no product diff.
  </acceptance_criteria>
  <done>The Plan-45 accessibility claim has complete measured evidence and a non-vacuous negative control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test instrumentation -> accessibility verdict | A red-control seam could accidentally bypass or contaminate the normal assertion path. |
| transient browser state -> durable evidence | Narrative pass counts can omit the geometry/focus state that the claim depends on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-45-01 | Repudiation | flaky reproduction | high | mitigate | Structured attachment plus exact commands, project, active element, geometry, trace status, and repeat counts. |
| T-198-45-02 | Tampering | assertions | high | mitigate | Opt-in known-bad overlay, fail-first prover, clean control, and fast prohibition scan. |
| T-198-50-01 | Elevation of privilege | red-control flag | high | mitigate | Exact value, named-test-local read, unique marker, cleanup, and source-scope contract. |
| T-198-50-02 | Denial of service | repeated browser proof | low | mitigate | Focused one-spec/project commands and existing bounded Playwright configuration. |
| T-198-50-03 | Information disclosure | browser attachment | low | mitigate | Record synthetic DOM geometry/identifiers only; exclude cookies, headers, form values, and environment data. |
</threat_model>

<verification>
- Per-task acceptance uses the fast ExUnit contract. At the plan gate, run `bash bin/verify-row-history-focus-red-control`; it fails when the known-bad run does not fail at the named assertion within the bound or the identical clean run does not pass. Then run the 10-repeat mobile command and one desktop adjacency run under the existing GREEN-06 timeout/retry policy; they fail when any repeat fails, exceeds the bound, or inherits red-control state.
- Confirm `git diff --name-only` contains no product, screenshot, scorecard, workflow, or Playwright-config file.
</verification>

<success_criteria>
- Security re-audit can close T-198-45-01 and T-198-45-02 without inferred risk acceptance.
- The historical non-reproduction remains honest while current assertion sensitivity is mechanically proven.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-50-SUMMARY.md` when done.</output>
