---
phase: 198-green-bringup
plan: 12
type: execute
wave: 2
depends_on: ["198-08"]
gap_closure: true
files_modified:
  - test/mix/tasks/threadline.evidence_show_test.exs
  - test/mix/tasks/threadline.incident_test.exs
  - test/mix/tasks/threadline/export_test.exs
  - test/threadline/capture/trigger_changed_from_test.exs
  - test/threadline/capture/trigger_context_test.exs
  - test/threadline/capture/trigger_redaction_test.exs
  - test/threadline/capture/trigger_test.exs
  - test/threadline/evidence/proof_test.exs
  - test/threadline/governance/evidence_record_test.exs
  - test/threadline/operator_surface/breadcrumb_test.exs
  - test/threadline/operator_surface/copy_contract_test.exs
  - test/threadline/operator_surface/exports_mix_parity_test.exs
  - test/threadline/operator_surface/live/row_history_live_test.exs
  - test/threadline/operator_surface/row_history_component_test.exs
  - CONTRIBUTING.md
autonomous: true
requirements: [GREEN-04]
estimate:
  tokens: 110000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`mix test` reports 0 failures and exits 0, against a database with no schema search parameter"
    - "Every former failure was fixed on its merits — no skip tag, no new exclusion, no deleted test, no assertion weakened"
    - "CONTRIBUTING.md List 1 names every verify-* job ci.yml runs, and the nyquist parity test is green for that reason"
  artifacts:
    - "14 test files ported to Threadline.StorageSchemaCase.repo_opts/0"
    - "CONTRIBUTING.md — two added rows in the `| Job key | Purpose |` table"
  key_links:
    - "198-08's port recipe -> the 14 remaining files -> 79 formerly-failing tests"
    - "CONTRIBUTING.md List 1 <-> ci.yml verify-* job keys <-> test/threadline/phase06_nyquist_ci_contract_test.exs three-way parity assertion"
    - "test/threadline/zero_skips_contract_test.exs — the anti-laundering cap that must stay green throughout"
---

<objective>
Drive `mix test` to zero failures by porting the remaining 14 defective files and landing the
one CONTRIBUTING.md parity failure that `198-TRIAGE.md:170-178` records as owned by no plan and
no later phase.

Purpose: GREEN-04's headline — "`mix test` passes with no deterministically-failing tests, each
former failure fixed rather than skipped" — is the load-bearing gap. It is also the upstream
cause of GREEN-07 (two red test lanes plus the PgBouncer topology job, which fails with the
same `relation "audit_changes" does not exist`) and therefore of PR #26's BLOCKED state.

Output: 1380 tests, 0 failures, exit 0.

**Measured baseline — do not re-derive.** 80 failures = 79 unprefixed-audit-table defects
across 15 files, plus 1 CONTRIBUTING List 1 parity failure. Plan 198-08 ports one of the 15
(`transaction_live_test.exs`, 14 tests) and leaves 66. This plan closes those 66 and the
parity row.

**The port recipe is 198-08's output, not a decision to re-open.** Route every defective call
site through `Threadline.StorageSchemaCase.repo_opts/0` as the trailing opts argument. Read
`198-08-SUMMARY.md` first; it records the exact recipe and the call shapes that do and do not
take it.

**No laundering, at any point.** Every failure is fixed on its merits. Never `@tag :skip`,
never add to the ExUnit exclude list, never delete a test, never weaken an assertion to make it
pass. `test/threadline/zero_skips_contract_test.exs` is the mechanical cap and it must stay
green on every run. Per CLAUDE.md, no suite may be excluded from `mix test` without updating
`test/test_helper.exs` and the docs together — and nothing here warrants that.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-08-SUMMARY.md
@.planning/phases/198-green-bringup/198-TRIAGE.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the 9 remaining `use Threadline.DataCase` files</name>
  <read_first>
    - .planning/phases/198-green-bringup/198-08-SUMMARY.md (the port recipe this task executes)
    - test/support/data_case.ex (line 21 — `import Threadline.StorageSchemaCase` is ALREADY present, so `repo_opts/0` is in scope in every file in this task with no new import; line 19 aliases the bare `Threadline.Test.Repo`; line 24's setup already calls the correctly-prefixed `clean_storage_schemas!/0`)
    - test/support/storage_schema_case.ex (lines 37-45 — `repo_opts/1` and its zero-arity default)
    - test/threadline/export_test.exs (lines 10, 19 — a `DataCase` file where this idiom is already green; the closest analog for this task)
    - test/threadline/capture/trigger_test.exs (line 110 — the ONE audit-table raw SQL in the repo, already schema-qualified; confirm it needs no change)
  </read_first>
  <precondition>Plan 198-08 is complete and `198-08-SUMMARY.md` records the port recipe. `mix test test/threadline/operator_surface/transaction_live_test.exs` exits 0 before this task starts.</precondition>
  <action>
    Port these 9 files, each of which already has `repo_opts/0` in scope via `Threadline.DataCase`
    (failing-test counts from `198-TRIAGE.md`, grep-visible call-site counts measured):

    - `test/mix/tasks/threadline.evidence_show_test.exs` (7 failing, 2 sites)
    - `test/mix/tasks/threadline.incident_test.exs` (4 failing, 2 sites)
    - `test/mix/tasks/threadline/export_test.exs` (1 failing, 2 sites)
    - `test/threadline/capture/trigger_changed_from_test.exs` (4 failing, 13 sites)
    - `test/threadline/capture/trigger_context_test.exs` (2 failing, 2 sites)
    - `test/threadline/capture/trigger_redaction_test.exs` (3 failing, 8 sites)
    - `test/threadline/capture/trigger_test.exs` (5 failing, 12 sites)
    - `test/threadline/evidence/proof_test.exs` (5 failing, 2 sites)
    - `test/threadline/governance/evidence_record_test.exs` (3 failing, 3 sites)

    **Two gotchas that will cost you the whole task if missed.**

    1. **Module-attribute call sites evade a naive `Repo\.` grep.** Both mix-task files under
       `test/mix/tasks/` bind a module attribute (`@repo Threadline.Test.Repo`) and then call
       `@repo.insert!(...)`. Any grep used to enumerate call sites must match the attribute form
       as well as the module form, or these files will silently keep two defects each.
    2. **Most raw SQL in the trigger tests is NOT defective and must not be touched.** The ~60
       raw-SQL sites in `trigger_test.exs`, `trigger_changed_from_test.exs`,
       `trigger_context_test.exs`, and `trigger_redaction_test.exs` target `public` fixture
       tables (`test_audit_target`, the `@table` attribute) and the `TriggerSQL` DDL, which
       resolves the storage schema internally via `Threadline.StorageSchema.function/2`. The one
       audit-table raw SQL, `trigger_test.exs:110`, is already schema-qualified. Change only
       Ecto calls that name a Threadline-owned schema module.

    Do not add a `prefix: "threadline"` literal anywhere. Do not touch `config/test.exs`,
    `test/support/repo.ex`, or any `lib/` schema module.

    Work file by file, running that file's tests to green before moving to the next, and record a
    per-file before/after failure count so the arithmetic to zero is auditable rather than
    asserted.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/ test/threadline/capture/ test/threadline/evidence/ test/threadline/governance/</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 9 named files exits 0 with 0 failures when run individually.
    - `grep -c 'prefix: "threadline"' <each of the 9 files>` returns 0.
    - `git diff --exit-code config/ test/support/ lib/` reports no changes.
    - `git diff --stat` shows no test was deleted and no `@tag :skip` was added: `mix test test/threadline/zero_skips_contract_test.exs` exits 0.
    - `git diff test/threadline/capture/trigger_test.exs` shows line 110's schema-qualified raw SQL unchanged.
    - The SUMMARY carries a per-file before/after failure table summing to the claimed total delta.
    - `mix verify.format` exits 0.
  </acceptance_criteria>
  <done>Nine files that already had the helper in scope now use it, and the untouched raw-SQL sites are documented as deliberate non-targets.</done>
</task>

<task type="auto">
  <name>Task 2: Port the 5 remaining files that need an added import</name>
  <read_first>
    - .planning/phases/198-green-bringup/198-08-SUMMARY.md (the port recipe, including the import line 198-08 added to a `use ExUnit.Case` module)
    - test/threadline/operator_surface/transaction_live_test.exs (198-08's finished work — the exact precedent for a non-`DataCase` module in this same directory)
    - test/support/storage_schema_case.ex (lines 37-45)
    - test/threadline/operator_surface/live/actor_live_test.exs (lines 139-176 — the parameterized variant, for the one case where a file genuinely exercises a second schema)
  </read_first>
  <action>
    These files use bare `use ExUnit.Case, async: false` and do NOT import
    `Threadline.StorageSchemaCase`. Each needs `import Threadline.StorageSchemaCase` added at the
    top of every module it declares — several declare more than one — before the call sites can
    be ported:

    - `test/threadline/operator_surface/breadcrumb_test.exs` (2 failing, 2 sites; module at :74)
    - `test/threadline/operator_surface/copy_contract_test.exs` (13 failing, 5 sites) — note this
      one uses `use Threadline.DataCase`, so it needs NO import; verify before adding one. Its
      known clusters are the `delete_all(SavedView | ExportJob | RetentionRun)` calls at
      `:147-149` and the seeds at `:565` and `:574`.
    - `test/threadline/operator_surface/exports_mix_parity_test.exs` (3 failing, 5 sites; module at :33)
    - `test/threadline/operator_surface/live/row_history_live_test.exs` (8 failing, 10 sites; modules at :142 and :356)
    - `test/threadline/operator_surface/row_history_component_test.exs` (5 failing, 5 sites; module at :15)

    Confirm each file's case module from its own source before deciding whether to add the import
    — the line numbers above are from a measured survey, but the `use` line is the authority.

    Then apply the same port as Task 1: append `repo_opts()` to every Ecto call naming a
    Threadline-owned schema module. Use the parameterized `repo_opts(storage_schema)` form ONLY
    where a test genuinely exercises a non-default schema.

    Do not add a `prefix: "threadline"` literal. Do not touch `config/test.exs`,
    `test/support/repo.ex`, or `lib/`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 named files exits 0 with 0 failures when run individually.
    - Every module in those files that calls `repo_opts` either declares `import Threadline.StorageSchemaCase` directly or is a `use Threadline.DataCase` module — state which is which per file in the SUMMARY.
    - `grep -c 'prefix: "threadline"' <each of the 5 files>` returns 0.
    - `mix test test/threadline/zero_skips_contract_test.exs` exits 0.
    - `git diff --exit-code config/ test/support/ lib/` reports no changes.
    - `mix verify.format` exits 0.
  </acceptance_criteria>
  <done>All 15 formerly-defective files are ported, and the 79 unprefixed-audit-table defects are retired.</done>
</task>

<task type="auto">
  <name>Task 3: Land the unowned CONTRIBUTING List 1 parity rows and drive the suite to zero</name>
  <read_first>
    - CONTRIBUTING.md lines 430-450 (the `| Job key | Purpose |` table — List 1. It currently ends at `verify-release-shape` and omits `verify-capture` and `verify-mechanical`. Line 447 additionally links `.github/workflows/hex-publish.yml`, which GREEN-10 deleted)
    - test/threadline/phase06_nyquist_ci_contract_test.exs (the three-way parity assertion; its current failure message reads `only-in-jobs=["verify-capture", "verify-mechanical"]` — the test is red for a REAL reason, exactly as 198-04 intended)
    - .github/workflows/ci.yml lines 388-440 (the `verify-mechanical` and `verify-capture` job definitions and their header comments — the source of the Purpose text)
    - .planning/phases/198-green-bringup/198-TRIAGE.md lines 170-178 (the "Deferred: CONTRIBUTING List 1" section recording this row as owned by no plan and no later phase)
  </read_first>
  <action>
    Add exactly two rows to CONTRIBUTING.md's `| Job key | Purpose |` table, in the same one-line
    `| \`job-key\` | command + one-clause purpose |` style as the ten rows already there, placed
    to match the order the jobs appear in `ci.yml`:

    - `verify-capture` — `mix verify.capture`; regenerates the Tier A evidence from scratch
      against a migrated example DB and a real browser, and asserts byte-stable regeneration
      against the committed evidence.
    - `verify-mechanical` — `mix verify.mechanical`; deterministic MODE-A / MODE-B gate over the
      committed `.planning/scorecards/*.json`.

    Do not restructure the table, do not rename any existing row, and do not touch the `## CI
    Coverage` table plan 198-05 added — that is a different table with a different contract test.

    Also correct line 447's dangling link to the deleted `.github/workflows/hex-publish.yml`.
    GREEN-10 deleted that workflow, and `198-REVIEW.md` flags the sibling stale reference in
    `release.yml:5` as inviting a future maintainer to recreate the ungated second publish path.
    Leaving a live link to it in the contributor-facing doc is the same invitation. Delete the
    "Legacy tag-only fallback" sentence; do not replace it with a note describing a path that no
    longer exists.

    Then run the FULL suite and drive it to zero. If anything is still red after Tasks 1 and 2,
    diagnose and fix it on its merits — do not skip, exclude, tag out, delete, or weaken. If a
    failure turns out to be genuinely environmental, it must be fixed in the SETUP PATH (the D-05
    binding rule), and the SUMMARY must name the setup file that changed.

    Run the suite at least twice and report both numbers. `198-TRIAGE.md` records one intermediate
    run that reported `175 failures, 8 invalid` and did not reproduce, attributed to contention on
    the shared `threadline_test` database from a concurrently executing sibling plan (this suite
    deliberately does not use the Ecto SQL Sandbox because triggers fire below sandbox awareness).
    A single green run is therefore not sufficient evidence.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <acceptance_criteria>
    - `mix test` exits 0 with 0 failures, on two consecutive runs, and both totals are recorded verbatim in the SUMMARY.
    - `mix test test/threadline/phase06_nyquist_ci_contract_test.exs` exits 0 — the three-way parity between `ci.yml` job keys, CONTRIBUTING List 1, and the test's roster holds.
    - `mix test test/threadline/zero_skips_contract_test.exs` exits 0 — zero skip tags, and the ExUnit exclude list still carries nothing beyond `:pgbouncer_topology`.
    - `git diff test/test_helper.exs` is empty — no suite was excluded to reach green.
    - `grep -c 'hex-publish.yml' CONTRIBUTING.md` returns 0.
    - `mix ci.all` exits 0.
    - `git diff --exit-code config/ test/support/repo.ex` reports no changes.
  </acceptance_criteria>
  <done>`mix test` passes with no deterministically-failing tests, twice, with every former failure fixed on its merits and the anti-laundering cap green — GREEN-04 met verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> `threadline_test` database | Ambient schema resolution must never be what makes a test pass |
| red suite -> green suite | The transition must be by repair, not by suppression |
| CONTRIBUTING.md -> `ci.yml` | A contributor-facing doc that names the wrong job set is a false statement in the project's own contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-12-01 | Tampering | the path from 80 failures to 0 | critical | mitigate | `zero_skips_contract_test.exs` asserted green in every task; `git diff test/test_helper.exs` asserted empty; per-file before/after counts required in the SUMMARY |
| T-198-12-02 | Tampering | `config/test.exs`, `test/support/repo.ex` | high | mitigate | Asserted unchanged in all three tasks; plan 198-08's mask guard is the durable backstop |
| T-198-12-03 | Repudiation | a single lucky green run | medium | mitigate | Two consecutive full runs required, both reported — the recorded non-reproducing 175-failure run is why |
| T-198-12-04 | Information Disclosure | CONTRIBUTING.md's dangling link to a deleted publish workflow | low | mitigate | Sentence deleted; asserted by a zero-occurrence grep |
</threat_model>

<verification>
- `mix test` exits 0 with 0 failures, twice.
- `mix ci.all` exits 0.
- `mix test test/threadline/zero_skips_contract_test.exs test/threadline/phase06_nyquist_ci_contract_test.exs test/threadline/storage_schema_prefix_contract_test.exs` exits 0.
- `git diff --exit-code config/ test/support/repo.ex test/test_helper.exs lib/` is clean.
- The SUMMARY carries a per-file before/after failure table summing from 66 to 0.
</verification>

<success_criteria>
GREEN-04 is met on its own terms: `mix test` passes, no test was skipped, excluded, deleted, or
weakened, and the two CONTRIBUTING List 1 rows that no plan owned are landed.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol | Kind | Path |
|---|---|---|
| ported `insert_transaction`/`insert_change`/`delete_all`/`aggregate` call sites across 14 files | changed test-support functions | the 14 test paths in `files_modified` |
| `import Threadline.StorageSchemaCase` | new import in up to 4 non-`DataCase` modules | `breadcrumb_test.exs`, `exports_mix_parity_test.exs`, `row_history_live_test.exs`, `row_history_component_test.exs` |
| `verify-capture` and `verify-mechanical` rows | new rows in the List 1 `\| Job key \| Purpose \|` table | `CONTRIBUTING.md` |

No new test modules, mix aliases, CI job keys, or step ids. No new file paths.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/198-green-bringup/198-12-SUMMARY.md` when done
</output>
