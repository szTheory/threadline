---
phase: 198-green-bringup
plan: 44
type: execute
wave: 1
gap_closure: true
gap_ids: [G-198-1, G-198-2, G-198-3, G-198-6, G-198-9, G-198-10, G-198-11, G-198-13, G-198-14, G-198-15]
depends_on: []
files_modified:
  - bin/verify-phase198-evidence
  - bin/observe-main-ci
  - .planning/audits/198-automation-policy.json
  - test/threadline/phase198_automation_policy_test.exs
  - test/threadline/main_ci_observer_contract_test.exs
  - test/threadline/ci_attestation_contract_test.exs
  - test/threadline/phase198_nyquist_contract_test.exs
autonomous: true
requirements: [GREEN-03, GREEN-04, GREEN-06, GREEN-07]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: medium}
must_haves:
  truths:
    - "Planning implications, lane/root-cause classifications, timeout budgets, measurement populations, and prediction scores are explicit data with executable pass/fail rules."
    - "The main-CI observer selects the newest canonical `.github/workflows/ci.yml` push run for the exact requested SHA before evaluating status and emits success, failure, incomplete, or not_observed without substitution."
    - "GREEN-04/GREEN-07 and verify.example prose cannot disagree with the committed attestation selected by policy."
    - "A successful automation check means the recorded disposition matches evidence; it never means a currently unmet requirement was silently changed to Complete."
  artifacts:
    - "198-automation-policy.json — versioned policy inputs, evidence links, thresholds, populations, and score formulas"
    - "bin/verify-phase198-evidence — offline policy/evidence evaluator with stable JSON output"
    - "bin/observe-main-ci — read-only exact-SHA canonical-run selector with injectable gh boundary"
  key_links:
    - "historical probe/CI attestations -> automation-policy fields -> offline evaluator -> requirements/prose consistency tests"
    - "origin/main or explicit SHA -> canonical workflow run list -> deterministic newest selection -> explicit state token"
    - "prediction rows + observed rows -> conclusion score and composition score as separate outputs"
  prohibitions:
    - {statement: "Do not convert GREEN-07 to Complete unless exact ancestry and the selected exact-SHA main aggregate are both successful.", status: unverified, flagged: true}
    - {statement: "Do not encode subjective adjectives as a substitute for numerical thresholds or enumerated policy.", status: unverified, flagged: true}
---

<objective>
Replace the ten remaining evidence-review judgments with versioned policy and deterministic evaluators.

Purpose: shift verification to committed, replayable contracts while preserving honest fail/incomplete/not-observed outcomes.

Output: one offline evidence policy/evaluator, one read-only main-CI observer, and contracts tying current requirements prose to attestations.

**Assumption delta:** `decision: no-change`. Requirement meanings and thresholds remain fixed. The plan formalizes how evidence is interpreted; it does not loosen GREEN-07, relabel branch CI as main CI, or turn prediction misses into hits.

**Spec-less edge audit:** empty evidence, ambiguous/duplicate runs, equality-at-boundary timeouts, malformed timestamps, non-nested measurement populations, zero predicted/observed rows, partial composition matches, and stale prose are all explicit negative fixtures below. No edge is deferred to human review.

### Artifacts this phase produces

- New symbols: `bin/verify-phase198-evidence`, `bin/observe-main-ci`.
- New schema: `threadline.phase198-automation-policy/1`.
- New output states: `success`, `failure`, `incomplete`, `not_observed` for main CI; `hit`, `miss`, `partial` for predictions.
- No product API, database schema, UI, dependency, or external write is introduced.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-UAT.md
@.planning/audits/198-mechanical-sensitivity.md
@.planning/audits/198-round4-playwright.md
@.planning/audits/ci-attestation-33353447804.json
@.planning/audits/ci-attestation-33354216172.json
@.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
@.planning/REQUIREMENTS.md
@bin/record-ci-attestation
@test/threadline/ci_attestation_contract_test.exs
@test/threadline/phase198_nyquist_contract_test.exs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Define one machine-readable policy and prove its evaluator has teeth</name>
  <files>.planning/audits/198-automation-policy.json, bin/verify-phase198-evidence, test/threadline/phase198_automation_policy_test.exs</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-01-SUMMARY.md — GREEN-03 probe and former sizing judgment
    - .planning/phases/198-green-bringup/198-03-SUMMARY.md — min/current differential evidence
    - .planning/phases/198-green-bringup/198-04-SUMMARY.md — fresh-database search_path evidence
    - .planning/phases/198-green-bringup/198-29-SUMMARY.md — prediction target and observed result
    - .planning/phases/198-green-bringup/198-31-SUMMARY.md — capped/unbounded population caveat
    - .planning/phases/198-green-bringup/198-37-SUMMARY.md — conclusion-vs-composition scoring case
  </read_first>
  <action>Add schema-versioned JSON containing: (a) Phase-201 implication rule keyed to the mechanical probe result; (b) min/current lane fingerprints and classification table (`same failures` => code/test, tool/version-only delta => runner/toolchain, insufficient data => unknown); (c) fresh-DB/search_path observations and arithmetic; (d) each workflow timeout with observed duration, ceiling/target rule, and provenance; (e) Playwright measurement `population_id`, cap, projects, filters, and comparability relation; and (f) prediction target, predicted rows, observed rows, conclusion result, and set-based composition score.

Create a strict offline evaluator that validates schema/types/non-empty evidence, rejects unknown keys and duplicate ids, computes every derived value, and writes stable JSON. Equality at a ceiling passes; above it fails. Non-comparable populations produce `not_comparable`, never a percentage. Prediction output must separately report target hit/miss and exact/intersection/extra/missing row sets. Add fixture tests mutating every load-bearing field so stale, empty, ambiguous, malformed, and rhetorically relabeled evidence fails.</action>
  <reversibility rating="reversible">The policy, evaluator, and fixtures are additive and offline.</reversibility>
  <verify>
    <automated>mix test test/threadline/phase198_automation_policy_test.exs &amp;&amp; bin/verify-phase198-evidence --policy .planning/audits/198-automation-policy.json --format json | jq -e '.schema == "threadline.phase198-evidence-result/1" and .checks.failed == 0'</automated>
    <fails_when>The evaluator accepts empty evidence, compares non-comparable populations, merges conclusion/composition scores, tolerates a threshold overrun, or a negative fixture remains green.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every derived assertion named in G-198-1/2/3/6/13/14/15 is data-backed and replayable.
    - Policy contains evidence paths/ids, not copied narrative as its only input.
    - The stable JSON result distinguishes policy-check success from underlying requirement disposition.
  </acceptance_criteria>
  <done>Seven former judgment calls are executable evidence transformations with explicit edge behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Build a deterministic, read-only exact-SHA main-CI observer</name>
  <files>bin/observe-main-ci, test/threadline/main_ci_observer_contract_test.exs</files>
  <read_first>
    - bin/record-ci-attestation — strict input, atomic output, and no-normalization precedent
    - .planning/phases/198-green-bringup/198-42-PLAN.md — canonical selector contract and failure directions
    - test/threadline/release_ci_gate_contract_test.exs — authoritative-run ordering guard
    - .github/workflows/ci.yml — canonical workflow path and byte-exact aggregate name
  </read_first>
  <action>Create `bin/observe-main-ci --sha SHA [--format json]` using `GH_BIN` injection. Validate SHA as 40 lowercase hex. List only `push` runs for branch `main` and workflow `.github/workflows/ci.yml`; filter exact byte-equal head SHA; order all matches by `[createdAt, databaseId]`; choose the newest before inspecting status. Emit schema, selected id, status, conclusion, job count, byte-exact `CI required` count/conclusion, and one state: `not_observed`, `incomplete`, `success`, or `failure`. Empty jobs, absent/duplicate aggregate, skipped/neutral/cancelled, malformed timestamps, or API errors must not yield success. Never poll, dispatch, rerun, or mutate GitHub.

Add fake-gh process fixtures for unordered records, timestamp ties, newer incomplete over older success, wrong workflow, wrong SHA, zero jobs, duplicate aggregate, every non-success conclusion, malformed JSON, and shell-hostile values.</action>
  <reversibility rating="reversible">The observer is read-only and the tests use a fake CLI.</reversibility>
  <verify>
    <automated>mix test test/threadline/main_ci_observer_contract_test.exs</automated>
    <fails_when>An older green run substitutes for a newer incomplete/failing run, a branch run is admitted, ambiguity yields success, a fixture touches the network, or any mutation command is reachable.</fails_when>
  </verify>
  <acceptance_criteria>
    - Selection precedes status evaluation and uses a deterministic tie-break.
    - All absence and non-success states are machine-readable, not prompts.
    - The test suite proves the observer contains no write-side GitHub operation.
  </acceptance_criteria>
  <done>GREEN-07 main-run evidence can be observed automatically and honestly in every state.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce requirements and verify.example consistency against attestations</name>
  <files>test/threadline/ci_attestation_contract_test.exs, test/threadline/phase198_nyquist_contract_test.exs</files>
  <read_first>
    - .planning/REQUIREMENTS.md — both GREEN-04/GREEN-07 views and current status markers
    - .planning/audits/ci-attestation-33353447804.json — green required-CI evidence
    - .planning/audits/ci-attestation-33354216172.json — second green evidence packet
    - .planning/phases/198-green-bringup/198-12-SUMMARY.md — stale verify.example disposition
    - .planning/phases/198-green-bringup/198-18-SUMMARY.md — former cross-view human checkpoint
    - test/threadline/ci_attestation_contract_test.exs — existing prose/evidence join
  </read_first>
  <action>Extend the attestation contract with explicit helpers for selecting the cited authoritative attestation and deriving aggregate success. Assert both requirements views use the same status and run id for GREEN-04/GREEN-07, and that no prose calls verify.example an outstanding ignored failure when the selected attestation records its job success. Keep exact ancestry independent: a green branch/required run can close a lane defect without closing GREEN-07's main/exact-ancestry clause. Extend the Nyquist contract to call the policy evaluator and assert all policy checks pass. Add negative in-memory/temp-file fixtures for mismatched run ids, status drift, a stale out-of-scope sentence, and a green aggregate with a red verify.example job.</action>
  <reversibility rating="reversible">Only offline contract tests are amended.</reversibility>
  <verify>
    <automated>mix test test/threadline/ci_attestation_contract_test.exs test/threadline/phase198_nyquist_contract_test.exs test/threadline/phase198_automation_policy_test.exs</automated>
    <fails_when>Requirements views may disagree, prose can contradict the chosen attestation, GREEN-07 ancestry is inferred from branch CI, or any negative fixture passes.</fails_when>
  </verify>
  <acceptance_criteria>
    - G-198-9/10/11 are evaluated without human confirmation.
    - Current unmet exact-main predicates stay explicitly unmet/pending rather than being laundered.
    - All checks run offline after evidence capture.
  </acceptance_criteria>
  <done>Evidence/status drift is a CI failure, not a future UAT question.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub JSON -> main-run status | Mutable/unordered API data is interpreted as authoritative evidence. |
| prose/policy -> requirement status | A passing checker could accidentally be confused with a passing requirement. |
| historical measurements -> derived scores | Different populations and stale values can create false comparisons. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-44-01 | Spoofing | run selection | high | mitigate | Exact workflow/event/branch/SHA filters and deterministic `[createdAt,databaseId]` ordering. |
| T-198-44-02 | Tampering | policy evidence | high | mitigate | Strict schema, evidence joins, duplicate rejection, and negative mutation fixtures. |
| T-198-44-03 | Repudiation | prediction scoring | medium | mitigate | Emit exact predicted/observed/intersection/extra/missing sets. |
| T-198-44-04 | Information disclosure | gh responses | low | mitigate | Persist no headers/tokens; tests use fake data. |
| T-198-44-05 | Elevation of privilege | observer | high | mitigate | Read-only command allowlist and tests rejecting dispatch/rerun/write verbs. |
</threat_model>

<verification>
- Run all three task suites and `git diff --check`.
- Inspect stable evaluator JSON to confirm `checks.failed == 0` while unmet requirement states remain separately represented.
</verification>

<success_criteria>
- Ten former judgments have deterministic inputs, edge behavior, and failing negative controls.
- Main CI produces explicit machine states and never triggers a human UAT prompt.
- No evidence checker manufactures GREEN-07 completion.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-44-SUMMARY.md` when done.</output>
