---
phase: 198-green-bringup
plan: 42
type: execute
wave: 2
gap_closure: true
depends_on:
  - 198-41
files_modified:
  - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - GREEN-07
  - GREEN-08

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Plan 42 begins from the committed Plan 41 result, including `198-41-SUMMARY.md`, and records that entry SHA separately from the immutable PR #34/run 33354216172 evidence subject."
    - "Every Plan 42 evidence task and required `198-42-SUMMARY.md` closeout commit remains inside GREEN-07's phrase `every local commit`; none is hidden behind an earlier candidate SHA."
    - "No push, ruleset PUT, bypass actor, merge, squash, rebase, or force update occurs in Round 7; any future remote mutation requires a new explicit blocking maintainer checkpoint (D-13, D-39)."
    - "Ruleset 21702804 is observed twice without mutation; canonical digests of name, target, enforcement, conditions, and rules match, enforcement is active, bypass actors are empty, and the sole required context is byte-exact `CI required` (GREEN-08)."
    - "The newest exact-SHA `main` push run is selected before status is evaluated; its status, workflow conclusion, non-empty job count, unique byte-exact `CI required` count, and that job's own conclusion are recorded without substituting an older completed run."
    - "GREEN-07 remains Pending because the final local graph necessarily contains Round-7 planning/evidence/summary commits absent from the measured origin/main; a green branch run or older main run cannot close that ancestry clause (D-01)."
    - "No UAT row changes: branch-run evidence does not satisfy the two main-run machine rows, and all human-judgment rows remain pending for maintainer ratification."
  artifacts:
    - ".planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — appended Round 7 no-mutation closeout, canonical ruleset digests, deterministic newest-main-run observation, and atomic-commit remainder"
    - ".planning/REQUIREMENTS.md — appended Round 7 GREEN-07 Pending rationale and GREEN-08 live re-observation without changing unrelated requirements"
  key_links:
    - "Plan 41 summary commit -> Plan 42 entry SHA -> Plan 42 task/summary commits -> non-empty final origin/main..HEAD remainder"
    - "origin/main exact SHA -> newest matching main push run selected before status -> byte-exact aggregate job conclusion recorded"
    - "two canonical ruleset snapshots -> unchanged editable-field digest plus active enforcement/empty bypass -> GREEN-08 re-observation"
  prohibitions:
    - statement: "Do not report GREEN-07 Complete from a branch run, an older completed main run, or a SHA that excludes Round-7 local commits."
      status: verified
      flagged: false
    - statement: "Do not create a broad, unpinned, or lingering branch-protection bypass."
      status: verified
      flagged: false
    - statement: "Do not rewrite sealed round-1 through round-6 measurement or D-39 evidence; append supersession evidence."
      status: unverified
      flagged: true
---

<objective>
Close Round 7 honestly: measure the post-Plan-41 local/remote graph, re-observe main CI and branch protection without mutation, and preserve GREEN-07 as Pending because the GSD closeout commits themselves are not on origin/main.

Purpose: The former plan's in-task fast-forward could not include commits created after that push. This plan removes that self-invalidating privilege path, treats the PR/run SHA as an immutable evidence subject rather than mutable HEAD, and records only predicates supported by the final reachable evidence.

Output: An append-only no-mutation closeout in `198-CI-MEASUREMENT.md` and a narrow Round-7 status note in `REQUIREMENTS.md`; `198-UAT.md` remains byte-identical.

**Assumption delta:** `decision: no-change`. GREEN-07 still quantifies every local commit, the authoritative CI run still must be the newest exact-SHA main push run, and `CI required` remains the sole required context. The only change is removal of an execution route that could not satisfy those existing predicates.

### Artifacts this phase produces

- New symbols: None.
- New flags: None.
- New fields: None.
- New files: None.
- Existing artifacts amended: `198-CI-MEASUREMENT.md` and `REQUIREMENTS.md`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
@.planning/phases/198-green-bringup/198-UAT.md
@.planning/phases/198-green-bringup/198-41-SUMMARY.md
@.planning/phases/198-green-bringup/199-RATIFICATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the post-Plan-41 graph through an unchanged live ruleset</name>
  <files>.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-41-SUMMARY.md — committed Plan 41 result that must already be inside Plan 42 entry HEAD
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — Round 7 packet and append target; rounds 1-6 are sealed
    - .planning/phases/198-green-bringup/198-CONTEXT.md — D-01 evidence rule, D-13 protection posture, D-39 Pending authority, and D-42 aggregate interlock
    - .planning/REQUIREMENTS.md — exact GREEN-07 and GREEN-08 predicates
    - .github/workflows/ci.yml — emitted `CI required` check and main push trigger, read only
  </read_first>
  <action>At Plan 42 entry, fetch `origin/main` and append the exact entry HEAD, origin/main SHA, both directional rev-list counts, and the immutable PR/run evidence subject `46213f9bc0ecbff356058d317c882d5a643ae86f`. Explicitly identify the entry HEAD as including the committed Plan 41 task evidence and `198-41-SUMMARY.md`; do not require it to equal the older evidence subject.

Create a temporary directory with `mktemp -d` and clean it on EXIT, INT, TERM, and HUP. Read ruleset 21702804 twice without issuing any PUT. For each snapshot, record a SHA-256 digest of the canonical JSON object containing every editable non-bypass field: `name`, `target`, `enforcement`, `conditions`, and `rules`. Separately record bypass actors, enforcement, required-status contexts, response timestamps, and HTTP success. Require both canonical digests to match, enforcement to equal `active`, the bypass list to be empty, and the required-context array to contain exactly one byte-exact `CI required`. If any predicate fails, record GREEN-08 drift and stop; do not attempt repair or mutation.

Append a closeout projection naming the Task 1 evidence commit, Task 2 status commit, and required `198-42-SUMMARY.md` commit as later local commits that cannot exist in the already-observed origin/main SHA. Record `remote mutation: none`, `ruleset mutation: none`, and `bypass window: not opened`. State that any future remote mutation is out of these plans and requires a new `blocking-human` maintainer checkpoint before the first mutation.</action>
  <reversibility rating="reversible">This task performs read-only GitHub queries and appends evidence locally.</reversibility>
  <verify>
    <automated>bash -c 'set -eu; d=$(mktemp -d); trap '\''rm -rf "$d"'\'' EXIT INT TERM HUP; gh api repos/szTheory/threadline/rulesets/21702804 &gt; "$d/a.json"; gh api repos/szTheory/threadline/rulesets/21702804 &gt; "$d/b.json"; da=$(jq -S -c '\''{name,target,enforcement,conditions,rules}'\'' "$d/a.json" | shasum -a 256 | awk '\''{print $1}'\''); db=$(jq -S -c '\''{name,target,enforcement,conditions,rules}'\'' "$d/b.json" | shasum -a 256 | awk '\''{print $1}'\''); test "$da" = "$db"; test "$(jq -r .enforcement "$d/b.json")" = active; test "$(jq '\''.bypass_actors | length'\'' "$d/b.json")" -eq 0; test "$(jq -r '\''[.rules[] | select(.type == "required_status_checks") | .parameters.required_status_checks[].context] | length'\'' "$d/b.json")" -eq 1; test "$(jq -r '\''[.rules[] | select(.type == "required_status_checks") | .parameters.required_status_checks[].context][0]'\'' "$d/b.json")" = "CI required"; grep -q "198-42-SUMMARY.md" .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md; grep -q "remote mutation: none" .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md'</automated>
    <fails_when>The two canonical editable-field digests differ, enforcement is not active, bypass actors are non-empty, the required context is absent/ambiguous/not byte-exact, the summary commit category is omitted, or the ledger claims a remote mutation.</fails_when>
  </verify>
  <acceptance_criteria>
    - The Plan 42 entry SHA is recorded separately from the immutable PR/run evidence subject and explicitly includes Plan 41's committed evidence and summary.
    - Two successful read-only ruleset snapshots have identical canonical digests across name, target, enforcement, conditions, and rules; enforcement is active, bypass actors are empty, and the sole context is byte-exact `CI required`.
    - The lifecycle projection includes both Plan 42 task commits and `198-42-SUMMARY.md`, with no claim that an earlier SHA contains them.
    - No push, ruleset PUT, bypass actor, merge, squash, rebase, force update, or PR mutation occurs.
  </acceptance_criteria>
  <done>The post-Plan-41 graph and full editable ruleset state are connected end-to-end without opening a privilege window.</done>
</task>

<task type="auto">
  <name>Task 2: Select the newest exact-SHA main run before status and derive honest requirement dispositions</name>
  <files>.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — Task 1 graph/ruleset proof and prior duration methodology
    - .planning/REQUIREMENTS.md — append only the Round-7 GREEN-07/GREEN-08 note; preserve every other status
    - .planning/phases/198-green-bringup/198-UAT.md — read-only proof that no main-run machine row or human-judgment row may change
    - .github/workflows/ci.yml — push trigger, exact `CI required` name, and timeout bounds; read only
    - .planning/phases/198-green-bringup/199-RATIFICATION.md — nine human judgments remain a maintainer boundary
  </read_first>
  <action>List `push`-event runs for branch `main`, filter only by exact `headSha == origin/main`, sort all matches by `createdAt`, and choose the newest match before inspecting its status. Never filter to completed runs before ordering. If no exact-SHA match exists, record that observable condition. If the newest match is incomplete, record its id/status and keep it authoritative; do not substitute an older completed run. If it is completed, fetch a non-empty job set and record the workflow conclusion, the count of jobs named byte-exact `CI required`, and that unique job's own conclusion. The only successful aggregate observation is exactly one such job with conclusion `success`; skipped, neutral, cancelled, absent, duplicate, or any other value is not success. Emit the fixed-format line `main run observation: id=&lt;id-or-none&gt;; status=&lt;status-or-none&gt;; conclusion=&lt;conclusion-or-none&gt;; ci_required_count=&lt;integer&gt;; ci_required_conclusion=&lt;value-or-none&gt;` so verification can compare the recorded values with the API.

Append the final live origin/main SHA, Plan 42 pre-Task-2 HEAD, both ancestry counts, and the explicit later Task 2 plus `198-42-SUMMARY.md` commit categories. Preserve GREEN-07 as Pending regardless of the observed main run because that run's SHA excludes the Round-7 local commits; state the first failing predicate as exact ancestry. Do not describe PR #34's successful branch run as a main run.

Append a Round-7 note to `REQUIREMENTS.md`: GREEN-07 remains Pending because origin/main does not contain the Round-7 plan/evidence/summary commits; GREEN-08 is re-proved only if Task 1's two canonical ruleset snapshots pass. Preserve D-39, GREEN-01..06, GREEN-09..12, and all prior evidence verbatim. Do not edit `198-UAT.md`: neither `198-07 D3` nor `198-18 D3` has a qualifying landed-main run, and the human-judgment rows remain pending.</action>
  <reversibility rating="reversible">The task performs read-only remote observation and append-only/narrow local status recording.</reversibility>
  <verify>
    <automated>bash -c 'set -eu; sha=$(git rev-parse origin/main); runs=$(gh run list --branch main --event push --limit 100 --json databaseId,headSha,status,conclusion,createdAt); row=$(printf "%s" "$runs" | jq -c --arg sha "$sha" '\''[.[] | select(.headSha == $sha)] | sort_by(.createdAt) | last // {}'\''); id=$(printf "%s" "$row" | jq -r '\''.databaseId // "none"'\''); status=$(printf "%s" "$row" | jq -r '\''.status // "none"'\''); conclusion=$(printf "%s" "$row" | jq -r '\''.conclusion // "none"'\''); count=0; job_conclusion=none; if test "$id" != none &amp;&amp; test "$status" = completed; then jobs=$(gh run view "$id" --json jobs); test "$(printf "%s" "$jobs" | jq '\''.jobs | length'\'')" -gt 0; count=$(printf "%s" "$jobs" | jq '\''[.jobs[] | select(.name == "CI required")] | length'\''); if test "$count" -eq 1; then job_conclusion=$(printf "%s" "$jobs" | jq -r '\''[.jobs[] | select(.name == "CI required")][0].conclusion'\''); fi; fi; expected="main run observation: id=$id; status=$status; conclusion=$conclusion; ci_required_count=$count; ci_required_conclusion=$job_conclusion"; grep -Fq "$expected" .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md; grep -q "GREEN-07: Pending" .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md; grep -q "198-42-SUMMARY.md" .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md'</automated>
    <fails_when>The recorded run is not the newest exact-SHA match selected before status evaluation, an older completed run substitutes for an incomplete newest run, completed-run jobs are empty, the recorded aggregate count/conclusion differs from the byte-exact live job, GREEN-07 is not Pending, or the required summary commit category is omitted.</fails_when>
  </verify>
  <acceptance_criteria>
    - The newest exact-origin/main-SHA push run is selected before its status is evaluated; no older completed run can replace a newer incomplete run.
    - A completed run records a non-empty job set, the exact aggregate count, and the `CI required` job's own conclusion; only unique plus `success` is described as successful.
    - GREEN-07 stays Pending with exact ancestry named first because Round-7 local commits, including the required Plan 42 summary, are not on the measured main SHA.
    - GREEN-08 is re-proved from matching full editable-field digests, active enforcement, empty bypass actors, and the sole byte-exact context.
    - `198-UAT.md`, `.planning/STATE.md`, `.github/`, prior measurement rounds, and unrelated requirement statuses are unchanged.
  </acceptance_criteria>
  <done>Round 7 closes with deterministic run evidence, full ruleset integrity evidence, and an honest GREEN-07 Pending disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutable local git graph -> tracked evidence | Task and summary commits created during execution change HEAD after earlier observations. |
| GitHub API -> tracked evidence | Mutable run and ruleset responses are converted into durable requirement status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-42-01 | Elevation of privilege | ruleset 21702804/main ref | high | mitigate | Eliminate the mutation path entirely: no PUT, bypass actor, push, merge, squash, rebase, or force update; require a future blocking maintainer checkpoint for any new mutation plan. |
| T-198-42-02 | Tampering | mutable HEAD lifecycle | high | mitigate | Record Plan 41 summary, both Plan 42 task commits, and Plan 42 summary as quantified local commits; preserve Pending rather than pinning mutable HEAD to the older PR SHA. |
| T-198-42-03 | Tampering | ruleset integrity | high | mitigate | Compare two canonical SHA-256 digests covering every editable non-bypass field and separately assert active enforcement, empty bypass actors, and the exact required context. |
| T-198-42-04 | Spoofing | main run selection | high | mitigate | Select the newest exact-SHA main push run before evaluating status; record the unique byte-exact aggregate job's own conclusion and preserve incomplete as authoritative. |
| T-198-42-05 | Repudiation | no-mutation closeout | medium | mitigate | Append SHAs, counts, run fields, canonical digests, timestamps, and explicit no-mutation statements to the sealed evidence ledger. |
| T-198-42-06 | Information disclosure | GitHub API captures | low | mitigate | Capture repository metadata only and exclude tokens, headers, environment output, and credentials. |
</threat_model>

<verification>
- Task 1 proves the full editable ruleset payload is stable across two read-only snapshots and that no privilege window opens.
- Task 2 deterministically selects the newest exact-SHA main push run before status, records the exact aggregate job conclusion, and preserves GREEN-07 Pending.
- Git diff is limited to the two tracked evidence/status files; `198-UAT.md`, STATE.md, GitHub configuration, and sealed prior rounds remain unchanged.
</verification>

<success_criteria>
- No Round-7 action changes origin/main, PR state, ruleset 21702804, or any bypass actor.
- The final evidence counts every planning, task, and summary commit category and never equates immutable PR evidence with mutable local HEAD.
- GREEN-07 remains Pending for the exact ancestry failure; GREEN-08 is re-observed from full ruleset integrity plus explicit enforcement.
</success_criteria>

<output>
Create `.planning/phases/198-green-bringup/198-42-SUMMARY.md` when done.
</output>
