---
phase: 198-green-bringup
plan: 66
type: execute
wave: 14
depends_on: [198-65]
files_modified:
  - .planning/audits/198-summary-coverage-manifest.json
  - test/threadline/phase198_zero_human_uat_contract_test.exs
autonomous: true
gap_closure: true
gap_ids: [GREEN-04-summary-boundary]
requirements: [GREEN-04]
estimate:
  tokens: 8500
  raw_tokens: 8500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The immutable content audit remains exactly summaries 01-61, and Plan 62 remains the sole terminal-certification summary outside that audited final-state set."
    - "Plans 63, 64, and 65 are accepted only as the explicit content-bound post-terminal set: exact number, path, SHA-256, order, normal-summary identity, status, and mechanical coverage are all enforced."
    - "Plan 66's execution summary is handled explicitly as the policy-repair summary, never as an audited-final or terminal-certification member; it is optional while tasks execute, required in final mode, and mechanically validated when present."
    - "Missing, renamed, modified, malformed, and extra post-terminal summaries fail closed in isolated mutation fixtures rather than being blessed from whatever filenames happen to exist."
    - "The focused summary contract and broader repository suite pass after the repair, restoring GREEN-04 on the current tree without claiming new CI or cross-environment evidence."
    - "GREEN-07 remains accepted-Pending; T-198-55-02 remains the sole accepted security risk by szTheory; T-198-55-03 and T-198-62-SC remain open, nonblocking, and unaccepted."
  artifacts:
    - ".planning/audits/198-summary-coverage-manifest.json — immutable 01-61 namespace plus an explicit content-bound 63-65 post-terminal policy and a non-terminal Plan-66 repair-summary rule"
    - "test/threadline/phase198_zero_human_uat_contract_test.exs — fail-closed real-directory and isolated mutation enforcement for audited, terminal, post-terminal, and policy-repair summary roles"
  key_links:
    - "manifest audited set 01-61 -> exact existing digest and coverage checks -> unchanged historical audit"
    - "Plan 62 -> sole terminal-certification exception -> existing strict terminal coverage contract"
    - "manifest post-terminal records 63-65 -> discovery -> exact path/content/identity/status/coverage validation -> mutation fixtures"
    - "explicit Plan-66 repair-summary rule -> normal execution-time optionality -> final-mode required presence -> no recursive namespace regression"
  assumptions:
    - {statement: "[GREEN-01/unclassified] Manual review remains required; historical coverage is in the completed measurement plans and is not re-opened by this GREEN-04 summary-boundary repair.", status: unverified, flagged: true}
    - {statement: "[GREEN-02/concurrency] Interrupted or parallel measurement guarantees remain governed by the completed measurement artifacts; this plan adds no measurement execution.", status: unverified, flagged: true}
    - {statement: "[GREEN-03/empty] Empty, singleton, and null probe inputs remain an unresolved spec-less edge assumption outside this summary-boundary gap.", status: unverified, flagged: true}
    - {statement: "[GREEN-03/encoding] Byte/code-point/grapheme/normalization semantics remain an unresolved spec-less edge assumption outside this summary-boundary gap.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/boundary] Exact threshold and one-step-either-side behavior remains an inherited unresolved spec-less edge assumption; this plan tests only the exact summary-role boundary named by Round 15.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/adjacency] Equality/touching behavior remains an inherited unresolved spec-less edge assumption; this plan tests adjacent summary numbers only as namespace mutations.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/empty] Empty, singleton, and null test-input behavior remains an inherited unresolved spec-less edge assumption; this plan covers missing summary members but claims no generic input proof.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/encoding] Byte/code-point/grapheme/normalization semantics remain an inherited unresolved spec-less edge assumption; this plan binds summary bytes by SHA-256 without generalizing across encodings.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/ordering] Stable ordering for equal elements remains an inherited unresolved spec-less edge assumption; this plan enforces only the explicit ordered summary records.", status: unverified, flagged: true}
    - {statement: "[GREEN-04/precision] Precision/overflow/rounding/tie-breaking behavior remains an inherited unresolved spec-less edge assumption outside this exact integer-summary namespace.", status: unverified, flagged: true}
    - {statement: "[GREEN-05/unclassified] Manual review remains required; the self-declaring form-policy work is historically complete and is not re-opened here.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/boundary] Timeout/failure thresholds and one-step-either-side behavior remain inherited unresolved spec-less assumptions covered only by completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/adjacency] Equality/touching behavior remains an inherited unresolved spec-less assumption covered only by completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/empty] Empty, singleton, and null job-set behavior remains an inherited unresolved spec-less assumption covered only by completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/ordering] Stable ordering for equal job results remains an inherited unresolved spec-less assumption covered only by completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-06/precision] Duration precision and threshold rounding remain inherited unresolved spec-less assumptions covered only by completed CI plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/boundary] The exact 20-minute boundary and one-step-either-side behavior remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/adjacency] Equal/touching CI-duration or ancestry states remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/empty] Empty, singleton, and null run/result behavior remains unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/encoding] Identity/name normalization semantics remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/ordering] Equal-result ordering remains unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/precision] CI duration precision and tie-breaking remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-07/concurrency] Interrupted or parallel main-run guarantees remain unresolved by the spec-less probe; GREEN-07 remains accepted-Pending.", status: unverified, flagged: true}
    - {statement: "[GREEN-08/empty] Empty, singleton, and null emitted-check-set behavior remains an inherited unresolved spec-less assumption covered by completed branch-protection contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-08/encoding] Check-name equality and normalization semantics remain an inherited unresolved spec-less assumption covered by completed branch-protection contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-08/concurrency] Interrupted or parallel check-emission guarantees remain an inherited unresolved spec-less assumption covered by completed branch-protection contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-09/unclassified] Manual review remains required; paid-trigger removal is historically complete and is not re-opened here.", status: unverified, flagged: true}
    - {statement: "[GREEN-10/unclassified] Manual review remains required; the single publish path is historically complete and is not re-opened here.", status: unverified, flagged: true}
    - {statement: "[GREEN-11/empty] Empty, singleton, and null flake-run behavior remains an inherited unresolved spec-less assumption covered by completed classifier plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-11/encoding] Flake-name equality and normalization semantics remain an inherited unresolved spec-less assumption covered by completed classifier plans.", status: unverified, flagged: true}
    - {statement: "[GREEN-12/adjacency] Equal/touching ref identities remain an inherited unresolved spec-less assumption covered by completed preservation contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-12/empty] Empty, singleton, and null ref-set behavior remains an inherited unresolved spec-less assumption covered by completed preservation contracts.", status: unverified, flagged: true}
    - {statement: "[GREEN-12/ordering] Equal-ref ordering remains an inherited unresolved spec-less assumption covered by completed preservation contracts.", status: unverified, flagged: true}
  prohibitions:
    - {statement: "The summary repair must not rewrite, re-hash from mutable expectations, or enlarge the immutable 01-61 audited-final set, and must not convert Plan 62 into an ordinary or repeatable exception.", status: unverified, flagged: true}
    - {statement: "The repair must not derive an allowed namespace from current wildcard results; only the explicit manifest roles and content identities may authorize a summary.", status: unverified, flagged: true}
    - {statement: "Local deterministic test evidence must not be described as cross-environment reproducibility, and neither GREEN-07 nor any security disposition may change through this GREEN-04 repair.", status: unverified, flagged: true}
---

<objective>
Restore GREEN-04 by defining and enforcing the exact post-terminal summary boundary created by Plans 63-65.

Purpose: authoritative Round-15 verification found one deterministic failure: the real Phase-198 directory now contains valid normal summaries 63-65, while the repository-owned summary contract still permits only audited summaries 01-61 and the special Plan-62 terminal certificate. This plan adds an explicit, content-bound post-terminal role instead of widening the audited final state or blessing wildcard discoveries. It also accounts for its own subsequently generated Plan-66 summary without recursively breaking the suite or making Plan 66 another terminal certificate.

Output: a manifest-owned exact 63-65 set, a narrow Plan-66 repair-summary rule, adversarial mutation coverage, a green focused summary contract, and a green broader repository suite.

### Normative post-terminal policy

Keep every existing manifest field and every existing `summaries` record unchanged. Add one exact `post_terminal_policy` object with no additional keys:

- `policy`: `explicit-content-bound-repair-summaries`
- `audited_final_state_unchanged`: `true`
- `terminal_certification_unchanged`: `true`
- `numbers`: exactly `["63", "64", "65"]`
- `summaries`: exactly three ordered records, each with the key set `{number,path,sha256}`:
  - `63`, `.planning/phases/198-green-bringup/198-63-SUMMARY.md`, `12fc1b775f7822b74bbc5d8648e15eb37ae5387dff726dded9ca6bc7e2e2c23c`
  - `64`, `.planning/phases/198-green-bringup/198-64-SUMMARY.md`, `76e9ed1735d3885114c7de09cdb721bac2ce34a5f9cc7d3696eb46d2ef0fe4da`
  - `65`, `.planning/phases/198-green-bringup/198-65-SUMMARY.md`, `9542156d7a2aaf0209a7e48920fed1b581163e5fd2e07b8ce4248eb2ed5d87b0`
- `policy_repair_summary`: exactly `{number,path,role,required_in_final_mode,excluded_from_audited_final_state,terminal_certification}`, with values `"66"`, `.planning/phases/198-green-bringup/198-66-SUMMARY.md`, `post-terminal-policy-repair-execution-summary`, `true`, `true`, and `false` respectively.

The 63-65 records authorize only those exact tracked bytes and normal-summary semantics. Plan 66 is not content-hashable before execution creates its summary, so it is authorized by one explicit number/path/role plus strict semantic checks: frontmatter phase `198-green-bringup`, plan `66`, status `complete`, and either an explicit empty coverage list or structurally valid non-human coverage with nonempty passing references. It may be absent during ordinary task execution, must be present in `PHASE198_SUMMARY_SET=final` mode, and fails validation whenever present but malformed. No summary numbered 67 or above is authorized.

### Artifacts this phase produces

- A tracked manifest extension that preserves every byte-identity record for summaries 01-47 and every existing namespace field for 01-61 and Plan 62, while adding only the exact policy above.
- Focused ExUnit behavior covering the real Phase-198 directory and isolated missing, renamed, modified, malformed, and extra post-terminal cases.
- No production module, dependency, schema, workflow, ruleset, branch-protection setting, scorecard, ref, tag, release, security record, accepted-risk row, or remote state.
- The standard executor-generated `198-66-SUMMARY.md`, governed by the explicit repair-summary rule and required by final-mode verification after it exists.

### Spec-less fallback and prohibition recall

No Phase-198 SPEC exists. The deterministic fallback yielded 33 generic edge probes across GREEN-01..12; all 33 remain flagged-unverified in frontmatter. This plan narrows its proof to the exact Round-15 GREEN-04 summary boundary and does not claim those generic probes resolved. Prohibition recall retained only three project-value constraints: immutable role boundaries, no wildcard-derived allowlist, and no promotion of local evidence/security/remote state. Canonical security findings remain in the threat model and existing SECURITY artifact, not duplicated as frontmatter prohibitions.

### Multi-source coverage audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Green bringup with a genuinely green deterministic repository suite and honest terminal state | 01-66 | COVERED | Plan 66 repairs the sole Round-15 deterministic regression without changing remote-state truth. |
| REQ | GREEN-01..GREEN-03 | Durable measurement and mechanical-sensitivity evidence | 01-65 | COVERED | Previously verified; Plan 66 does not reopen or restate it. |
| REQ | GREEN-04 | `mix test` passes without deterministic failures | 56, 60-62, 66 | COVERED | Exact summary roles and mutation teeth restore the current-tree suite. |
| REQ | GREEN-05..GREEN-06 | Self-declaring form policy and bounded CI | 01-65 | COVERED | Previously verified; unchanged. |
| REQ | GREEN-07 | Exact-main ancestry and successful bounded CI | 39, 61-66 | COVERED | Remains accepted-Pending under D-39; local tests are not substituted. |
| REQ | GREEN-08..GREEN-12 | Protection, critic, publish, flake, and preservation outcomes | 01-65 | COVERED | Previously verified; security remains exactly as Round 15 recorded. |
| RESEARCH | — | Exact, fail-closed, repository-owned evidence contracts | 66 | COVERED | Explicit records and digests replace ambient wildcard trust; no new dependency or architecture choice. |
| CONTEXT | D-01..D-38 | Locked measurement, test, CI, release, and preservation decisions | 01-65 | COVERED | Already delivered; Plan 66 changes only the stale summary boundary. |
| CONTEXT | D-39 | GREEN-07 terminal disposition | 66 | COVERED | Preserved literally as accepted-Pending. |
| CONTEXT | D-40..D-42 | Lane remedies and aggregate-gate interlock | 01-65 | COVERED | No workflow, evidence baseline, protection, or external-state mutation occurs. |
| VERIFICATION | GREEN-04 summary boundary | Explicit 63-65 post-terminal policy with mutation teeth | 66 | COVERED | Directly implements every `missing` item in Round 15. |
| SECURITY | T-198-55-02 | Narrow accepted risk by szTheory only | 65 | COVERED | Preserved, not re-decided. |
| SECURITY | T-198-55-03, T-198-62-SC | Open/nonblocking/unaccepted findings | 65-66 | COVERED | Explicit non-mutation invariant; no disposition changes. |

All 12 phase requirements and all 42 locked decisions remain covered across Plans 01-66. No deferred item or generic spec-less probe is silently absorbed.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-VALIDATION.md
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-65-SUMMARY.md
@.planning/audits/198-summary-coverage-manifest.json
@test/threadline/phase198_zero_human_uat_contract_test.exs
@test/threadline/phase198_prohibition_resolution_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind the exact post-terminal set and close the real-directory regression</name>
  <files>.planning/audits/198-summary-coverage-manifest.json, test/threadline/phase198_zero_human_uat_contract_test.exs</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-VERIFICATION.md — authoritative Round-15 failure, exact missing repair, and evidence boundaries
    - .planning/audits/198-summary-coverage-manifest.json — existing immutable 01-61 namespace, Plan-62 terminal boundary, and 01-47 content records
    - test/threadline/phase198_zero_human_uat_contract_test.exs — current discovery, manifest, coverage, digest, and fixture helpers
    - .planning/phases/198-green-bringup/198-63-SUMMARY.md — halted but valid normal summary whose exact bytes are post-terminal member 63
    - .planning/phases/198-green-bringup/198-64-SUMMARY.md — valid normal summary whose exact bytes are post-terminal member 64
    - .planning/phases/198-green-bringup/198-65-SUMMARY.md — valid normal summary whose exact bytes are post-terminal member 65
  </read_first>
  <behavior>
    - "The pre-change final-mode focused test is RED specifically because real summaries 63-65 are unexpected."
    - "After the repair, real summaries 01-61, terminal Plan 62, and exact post-terminal summaries 63-65 pass in ordinary and final modes before Plan 66's summary exists only when final-mode expectation is intentionally fixture-supplied; ordinary execution never invents the file."
    - "Existing audited-final and terminal fields are unchanged and structurally disjoint from the new post-terminal policy."
    - "Each 63-65 member matches its normative path and SHA-256 and satisfies normal summary identity/status/coverage semantics."
  </behavior>
  <action>Begin with a test-only RED commit. Extend the contract to require the exact `post_terminal_policy` schema and values above while leaving the manifest unchanged; run the focused suite and retain the expected failure direction: the manifest lacks the policy and the real directory still reports 63-65 as unexpected. Then add exactly the normative policy object to the tracked manifest without editing, reordering, regenerating, or re-hashing any existing manifest field or existing `summaries` record. Update discovery validation to construct allowed roles from the existing audited-final list, the sole terminal number, the explicit 63-65 records, and the one literal Plan-66 repair-summary rule; never construct an allowlist from discovered wildcard results or from the largest present number. Validate every present post-terminal record against its exact path, digest, phase/plan identity, status, and coverage semantics. Preserve the current execution behavior by allowing Plan 66 to be absent in ordinary mode; when present it must satisfy its literal path/identity/status/coverage contract and remain excluded from both the audited-final set and terminal role. Run the ordinary focused suite GREEN against the real directory. Do not modify any SUMMARY, SECURITY, VALIDATION, VERIFICATION, REQUIREMENTS, workflow, ruleset, scorecard, ref, tag, branch, release, or remote state. Local passing evidence restores only the deterministic current-tree GREEN-04 contract; it is not cross-environment byte reproducibility or GREEN-07 evidence.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin /opt/homebrew/bin/mix test test/threadline/phase198_zero_human_uat_contract_test.exs</automated>
    <fails_when>The pre-change RED failure is not observed; any existing audited record or 01-61/62 boundary changes; the policy is inferred from current filenames; 63-65 are not content-bound; Plan 66 is treated as terminal/audited-final; a malformed present Plan-66 summary passes; the ordinary focused suite remains red; zero tests run; or any protected non-task file changes.</fails_when>
  </verify>
  <acceptance_criteria>
    - A committed RED test fails on the missing normative policy and the stale real-directory namespace before the manifest repair is written.
    - The manifest retains schema version, scope, classifier owner, audited-final number 61, terminal-certification number 62, baseline/closeout/final-state arrays, baseline counts, and every existing summary record unchanged.
    - `post_terminal_policy` has exactly the normative keys and values; records 63-65 are ordered, path-bound, SHA-256-bound, and semantically valid normal summaries.
    - Plan 66 has one explicit non-terminal, non-audited-final repair-summary rule, is optional only during ordinary execution before its summary exists, and fails ordinary validation whenever present but malformed.
    - The ordinary focused command exits zero with a nonzero ExUnit test count on the real current directory.
    - GREEN-07 and the three named security findings retain their Round-15 dispositions exactly; no local result is labeled cross-environment reproducibility.
  </acceptance_criteria>
  <done>The real Phase-198 directory passes through an explicit content-bound 63-65 policy while the 01-61 audit and sole Plan-62 terminal boundary remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the post-terminal boundary fails closed under adversarial mutations</name>
  <files>test/threadline/phase198_zero_human_uat_contract_test.exs</files>
  <read_first>
    - .planning/audits/198-summary-coverage-manifest.json — Task-1 exact post-terminal policy and immutable historical boundary
    - test/threadline/phase198_zero_human_uat_contract_test.exs — Task-1 validator and existing isolated summary fixtures
    - .planning/phases/198-green-bringup/198-SECURITY.md — passed security state whose accepted/open dispositions must remain untouched
    - test/threadline/phase198_prohibition_resolution_contract_test.exs — existing mechanical projection for exact accepted/open threat state
  </read_first>
  <behavior>
    - "For each of 63, 64, and 65 independently, deleting the file, renaming it to a zero-padded or suffixed variant, changing one byte, replacing it with malformed frontmatter/coverage, or adding an unauthorized neighbor fails."
    - "Changing policy number/path/digest/order, omitting or adding a record/key, overlapping audited/terminal/post-terminal roles, or deriving the policy from discovered files fails."
    - "An isolated exact Plan-66 summary passes in final mode; missing Plan 66 in final mode, wrong plan/phase/status, malformed or human/pending coverage, renamed Plan 66, and extra Plan 67 fail."
    - "The focused contract, the existing security-disposition contract, and the broader root test suite all pass with nonzero test counts."
  </behavior>
  <action>Add isolated copied-directory fixtures that start from the exact 01-65 repository state plus a mechanically valid Plan-66 fixture. Exercise missing, renamed, modified, malformed, and extra cases separately for every content-bound member 63-65; a renamed member must leave its authorized number missing and surface the unauthorized filename/number rather than silently normalizing it. Add manifest-policy mutation fixtures for wrong number, path, digest, record order, role overlap, missing/extra keys, and added members; validate a supplied manifest value or helper input so these mutations test the actual policy parser rather than editing the repository manifest during the test. Add final-mode Plan-66 fixtures for exact valid identity/status/coverage, absence, wrong identity, non-complete status, malformed coverage, human judgment, non-pass references, renamed path, and extra 67. Ensure test-generated expected values come from the normative manifest records, not from wildcard discovery or hashes computed from mutated inputs. Run the focused suite, then the existing Plan-65 security-disposition suite, then the broader root suite using the known Homebrew toolchain path. The broad suite must not be filtered to hide the formerly failing summary test. After the executor writes the standard `198-66-SUMMARY.md`, the orchestrator must rerun the focused contract with `PHASE198_SUMMARY_SET=final`; that post-summary gate must require the real Plan-66 file and reject all later numbers. Do not update canonical security or verification in this task; those workflows remain the orchestrator's post-execution owners.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin /opt/homebrew/bin/mix test test/threadline/phase198_zero_human_uat_contract_test.exs test/threadline/phase198_prohibition_resolution_contract_test.exs &amp;&amp; PATH=/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin /opt/homebrew/bin/mix test</automated>
    <fails_when>Any named mutation passes; a mutation oracle recomputes expected members from the mutated/current directory; the ordinary suite omits the real summary contract; the security projection changes; the broader suite has any deterministic failure; zero tests run; Plan-66 final-mode presence is not left as an explicit post-summary gate; or results are promoted to GREEN-07/cross-environment proof.</fails_when>
  </verify>
  <acceptance_criteria>
    - Missing, renamed, modified, malformed, and extra post-terminal mutations are separately exercised and fail for the expected role violation.
    - The manifest schema itself rejects changed identities, order, membership, role overlap, missing keys, and extra keys.
    - Plan 66 is required only in final mode, is never classified as audited-final or terminal-certification, and must satisfy exact normal-summary identity/status/coverage semantics when present.
    - The combined focused command and unfiltered root `mix test` both exit zero and report nonzero test counts.
    - After the standard Plan-66 summary exists, `PHASE198_SUMMARY_SET=final PATH=/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin /opt/homebrew/bin/mix test test/threadline/phase198_zero_human_uat_contract_test.exs` exits zero; any Plan-67-or-later summary would make it fail.
    - Existing SECURITY content and dispositions are unchanged: only T-198-55-02 is accepted by szTheory, while T-198-55-03 and T-198-62-SC remain open/nonblocking/unaccepted; GREEN-07 remains accepted-Pending.
  </acceptance_criteria>
  <done>The post-terminal boundary has independent mutation teeth, the root suite is green, and the standard Plan-66 summary can be verified afterward without creating another recursive namespace gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| filesystem discovery -> authorized summary roles | Ambient filenames are untrusted and cannot define their own allowlist. |
| manifest record -> summary bytes | Path, number, order, digest, identity, status, and coverage must agree before a post-terminal summary is accepted. |
| executor lifecycle -> Plan-66 summary | The summary does not exist during task execution but becomes mandatory in final verification without becoming a second terminal certificate. |
| local tests -> phase claims | Deterministic local success can restore the current-tree GREEN-04 contract but cannot prove remote exact-main CI or cross-environment byte stability. |

## STRIDE Threat Register — ASVS L1, block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-66-01 | Tampering | summary discovery namespace | high | mitigate | Construct authorization only from exact manifest roles; reject every discovered number/path outside audited 01-61, terminal 62, content-bound 63-65, and explicit repair summary 66. |
| T-198-66-02 | Repudiation | post-terminal summary content | high | mitigate | Bind 63-65 to exact ordered path/SHA-256 records and normal-summary identity/status/coverage, with per-member modified and malformed fixtures. |
| T-198-66-03 | Elevation of Privilege | audited/terminal role separation | high | mitigate | Assert disjoint roles, audited-final 61 unchanged, terminal 62 unique, and Plan 66 explicitly false for terminal certification and excluded from audited final state. |
| T-198-66-04 | Denial of Service | recursive plan-summary lifecycle | high | mitigate | Permit only absent Plan 66 during ordinary pre-summary execution, require it in final mode, validate it whenever present, and reject Plan 67+ so the repair closes rather than shifts the failure. |
| T-198-66-05 | Spoofing | mutation oracle | medium | mitigate | Expected members and digests come from fixed normative manifest records, never wildcard discoveries or hashes recomputed from mutated inputs. |
| T-198-66-06 | Information Disclosure | manifest paths and fixtures | low | mitigate | Retain repository-relative tracked paths and existing disclosure checks; introduce no absolute path, secret field, credential, remote payload, or copied sensitive content. |
</threat_model>

<verification>
1. Observe and commit the focused RED failure before changing the manifest.
2. Pass the ordinary focused summary contract against the real directory before Plan 66's summary exists.
3. Pass the full adversarial fixture matrix and the existing security-disposition projection.
4. Pass an unfiltered root `mix test` with a nonzero count.
5. After the executor creates `198-66-SUMMARY.md`, run the focused summary contract in final mode before canonical phase verification.
6. Confirm `git diff` contains only the two task-owned tracked files plus the standard Plan-66 summary; security, validation, requirements, workflows, rulesets, scorecards, and remote state are unchanged.
</verification>

<success_criteria>
- GREEN-04's current deterministic failure is gone from both focused and broad root testing.
- Summaries 01-61 retain their immutable audited-final contract byte-for-byte at the manifest-record level.
- Plan 62 remains the sole terminal-certification exception and is not generalized.
- Plans 63-65 are authorized by an exact content-bound post-terminal policy, not by wildcard presence or numeric widening.
- Plan 66's own summary is explicitly lifecycle-safe, semantically checked, required in final mode, and not a new terminal certificate.
- Every requested missing/renamed/modified/malformed/extra mutation fails closed.
- GREEN-07 and all three named security dispositions remain exactly unchanged, and no local result is represented as cross-environment reproducibility.
</success_criteria>

<output>
Create `.planning/phases/198-green-bringup/198-66-SUMMARY.md` when done.
</output>
