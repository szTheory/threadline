---
phase: 198-green-bringup
plan: 23
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - .planning/audits/198-round4-demo-seed.md
  - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex
autonomous: true
requirements:
  - GREEN-04

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A measured, on-disk failure inventory exists for `mix verify.example` before any test or seed file is edited: every failure named by module, test name, failure class (`Ecto.NoResultsError` / assertion mismatch / `ExUnit.TimeoutError`), and its verbatim message."
    - "Because the failing set is non-deterministic run-to-run (198-19-SUMMARY, `198-CI-MEASUREMENT.md` Round 3), the inventory is taken over at least two independent `mix verify.example` runs and records the union of failures plus which ones appeared in only one run."
    - "The `SEED-03 manifest heroes` cluster in `demo_contract_test.exs` is fixed at its cause — either `mix demo.seed` produces the wrong content (seed fixed) or the assertion is rotted (assertion fixed to a shape assertion) — with the choice recorded per failure and justified from evidence, never by preference."
    - "A red-then-green teeth proof exists for the tracer cluster: the fixed assertion is shown failing against the pre-fix state and passing after, both captured verbatim."
    - "The measured `mix verify.example` failure count is recorded before and after the tracer fix, and the after-count is strictly lower than the before-count."
    - "A reusable fix protocol is written to disk that plans 198-24 through 198-28 follow, so the remaining clusters expand from a proven loop rather than re-inventing one."
    - "No failing test is skipped, tagged out, deleted, or narrowed; `mix verify.example`'s scope and its wiring into `ci.yml`'s `current` lane are untouched."
    - statement: "Adjacency edge (GREEN-04): where a demo-seed row sits exactly on a window boundary (the `default 24h window` and the `demo_last_tuesday`..`demo_epoch` bounds in `demo_contract_test.exs`), the assertion states inclusively or exclusively which side the boundary row falls on, rather than leaving strict-vs-inclusive comparison to be guessed."
      verification: explicit
    - statement: "Empty edge (GREEN-04): a seeded-row query returning zero rows fails its assertion with a message naming the expected shape, never passing vacuously through an `Enum.all?/2` or `Enum.any?/2` over an empty list."
      verification: explicit
    - statement: "Ordering edge (GREEN-04): where a test asserts over a list of seeded rows whose sort key can tie, the rewritten assertion is order-independent (set/shape based) rather than depending on an unspecified stable order — this is exactly the rot GREEN-04's own text targets."
      verification: explicit
  artifacts:
    - ".planning/audits/198-round4-demo-seed.md — the two-run failure inventory, the per-failure cause attribution, the seed-vs-assertion disposition, the red-then-green transcript, and the reusable fix protocol"
    - "examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs — the SEED-03 manifest-heroes cluster, fixed at its cause"
    - "The demo-seed module(s) named by the diagnosis — changed only where the diagnosis shows the seeded content, not the assertion, is what is wrong"
  key_links:
    - "`mix demo.seed` output → `ThreadlinePhoenix.DemoContractTest` assertions → `mix verify.example` exit status → `ci.yml`'s `current`-lane step → the `Run test suite (current)` check → GREEN-04"
    - "non-deterministic failure set → a single-run inventory would under-count → the two-run union is what makes the after-count comparison meaningful"
    - "the fix protocol written here → plans 198-24/25/26/27/28 read it as `read_first` → the remaining clusters are fixed the same disciplined way"
  prohibitions:
    - statement: "A failing example-app test must not be made to pass by deleting, weakening, `@tag :skip`-ing, `git rm`-ing, or conditionally guarding its assertion. Each failure is fixed at its cause."
      status: kept
      verification: flagged-unverified
    - statement: "`mix verify.example` must not be removed from `mix ci.all`, narrowed in scope, or detached from `ci.yml`'s `current` lane in order to make `Run test suite (current)` green."
      status: kept
      verification: flagged-unverified
    - statement: "GREEN-04 must not be marked Complete on local `mix verify.example` output. Only a measured CI run is admissible evidence (D-01); this plan produces a readiness signal, not a verdict."
      status: kept
      verification: flagged-unverified
---

<objective>
**Tracer for gap-closure round 4.** Prove the whole "diagnose one demo-seed failure → fix it on
its merits → prove red-then-green → watch the lane's failure count drop" loop end-to-end, for real,
on one cluster — before six expansion plans repeat it across the rest.

Purpose: `Run test suite (current)` is red because `mix verify.example` reports 9 failures / 109
tests against `mix demo.seed`-generated content. D-41 (maintainer decision C1) directs this round to
fix them on their merits. That class has been acknowledged and deferred since Phases 177/179/180/182
— nobody has yet demonstrated a working fix loop for even one of them. This plan demonstrates it on
the `SEED-03 manifest heroes` cluster in `examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs`,
and writes down the protocol the rest of the round follows.

Output: a measured two-run failure inventory, one cluster fixed at its cause with a red-then-green
transcript, a lower measured `mix verify.example` failure count, and a reusable fix protocol on disk.

**What this plan cannot do:** it cannot close GREEN-04. Per D-01, local `mix verify.example` output
is not admissible evidence for GREEN-04 — only the measured CI run in plan 198-29 is. This plan
produces a readiness signal.

**Flagged assumption (spec-less probe fallback, no-silent-drop):** the edge-coverage probe report
(`27 applicable items`) contains 20 items belonging to GREEN-01, 02, 03, 05, 06, 08, 09, 10, 11 and
12 — including four `unclassified` rows (GREEN-01, GREEN-05, GREEN-09, GREEN-10) that remain
`unresolved`. All ten of those requirements are **Complete and independently re-verified** in
`198-VERIFICATION.md` round 3, and none of this round's plans touches the files that satisfy them, so
no predicate is minted for them here. They are surfaced, not dropped. The 7 items belonging to the
two open requirements (GREEN-04: adjacency/empty/ordering; GREEN-07: adjacency/empty/ordering/
concurrency) are authored into `must_haves.truths` across this round's plans.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/deferred-items.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Measure the real failure inventory over two independent runs, before changing anything</name>
  <files>.planning/audits/198-round4-demo-seed.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md (the `Round 3 (2026-08-28) — Local pre-push readiness signal (Plan 198-19)` section, specifically `### verify.example failure — known, previously-acknowledged deferral`, which records that a second independent run produced a *different* 9-test failure set including two `ThreadlinePhoenixWeb.WalkthroughHappyPathTest` mismatches absent from the first — this is why one run is not an inventory)
    - .planning/phases/198-green-bringup/deferred-items.md (the `## Plan 198-12` entry — the exact prior characterisation of this failure class: `org_memberships` actor attribution, SEED-03 manifest hero transactions, SEED-05 delete incident; `Ecto.NoResultsError` and assertion mismatches, never `Postgrex.Error 42P01`)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-41 in full — the maintainer's C1 decision and its explicit statement that this fix is a dedicated successor round's job)
    - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (all 367 lines — the eight `describe` blocks and their 13 tests are the whole surface being inventoried)
    - mix.exs (the `verify_example/1` private helper, ~line 182 — the exact command `mix verify.example` runs: `cd examples/threadline_phoenix && mix deps.get && mix compile --warnings-as-errors && mix ecto.create --quiet -r ThreadlinePhoenix.Repo && mix test` under `MIX_ENV=test`)
  </read_first>
  <action>
    Change no test file and no seed module in this task. Measure only.

    Run `mix verify.example` from the repository root twice, back to back, as two independent runs.
    Capture the full output of each. For each run record: the trailing `N tests, M failures` line
    verbatim, and for every failure the module (`ThreadlinePhoenix.DemoContractTest`,
    `ThreadlinePhoenixWeb.WalkthroughHappyPathTest`, `ThreadlinePhoenixWeb.WalkthroughEvidenceTest`),
    the `describe` block, the test name, the source line, the failure class, and the verbatim
    assertion/exception message.

    Write `.planning/audits/198-round4-demo-seed.md` with:

    (a) A `## Two-run failure inventory` table, one row per distinct failure across both runs,
    columns: module, describe, test name, line, failure class, verbatim message, `run 1?`, `run 2?`.
    Compute and state the union size and the intersection size explicitly. Any failure appearing in
    only one run is marked `non-deterministic` in its own column — do not average it away, and do not
    assume the union equals 9.

    (b) A `## Measured before-count` line stating both runs' `N tests, M failures` figures verbatim,
    and which figure this round treats as the baseline (use the union size, and say so).

    (c) A `## Cause attribution` table with one row per inventory row and a `cause` column, filled in
    by reading the failing assertion against what `mix demo.seed` actually produces. For each row,
    classify the cause as exactly one of: `seed-content-wrong` (the seeded data does not match a
    contract the manifest or the demo narrative genuinely asserts), `assertion-rotted` (the assertion
    pins a literal or an ordering that the product legitimately changed), or `undiagnosed`. Mark rows
    that share a cause as sharing it, so a single seed fix is not credited as N separate fixes.

    (d) A `## Explicitly not the cause` line confirming, with a `grep -c` figure over both runs'
    output, that `undefined_table` / `42P01` occurrences are `0` — the search_path defect closed by
    198-19 must not be re-opened or re-blamed here.

    Do not attempt any fix in this task. The inventory is the deliverable.
  </action>
  <acceptance_criteria>
    - `.planning/audits/198-round4-demo-seed.md` exists and contains the literal headings `## Two-run failure inventory`, `## Measured before-count`, `## Cause attribution`, and `## Explicitly not the cause`.
    - The inventory table has at least one row per failure observed in either run, and every row carries a non-empty verbatim message column — no row reads only "assertion failed".
    - The union size and intersection size are both stated as integers, and any row present in only one run carries the `non-deterministic` mark.
    - Every `## Cause attribution` row carries exactly one of `seed-content-wrong`, `assertion-rotted`, or `undiagnosed` — no blank cells.
    - The `grep -c` figure for `undefined_table` over both captured run outputs is recorded and is `0`; if it is not `0`, the task stops and reports rather than proceeding.
    - `git diff --name-only` after this task lists only `.planning/audits/198-round4-demo-seed.md` — no test file and no seed module changed.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/audits/198-round4-demo-seed.md &amp;&amp; grep -q '^## Two-run failure inventory' .planning/audits/198-round4-demo-seed.md &amp;&amp; grep -q '^## Cause attribution' .planning/audits/198-round4-demo-seed.md &amp;&amp; test "$(git diff --name-only | grep -vc '^\.planning/audits/198-round4-demo-seed\.md$')" = "0"</automated>
  </verify>
  <done>A measured, two-run failure inventory with per-failure cause attribution exists on disk, taken before any code changed, with the non-determinism made explicit rather than averaged away.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the SEED-03 manifest-heroes cluster at its cause, with a red-then-green teeth proof</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/support.ex, examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex, .planning/audits/198-round4-demo-seed.md</files>
  <read_first>
    - .planning/audits/198-round4-demo-seed.md (Task 1's own output — the cause attribution rows for the `SEED-03 manifest heroes` and `SEED-03 leaving agent window` describe blocks are this task's work list)
    - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (lines 24-154 — `describe "SEED-03 manifest heroes"` with `test "acme, offboarded-co orgs and hero tickets 4521 closed and 4518 present"`, `test "ticket_replied_and_closed action on #4521 close transaction"`, `test "close reply insert is redacted on #4521"`; and `describe "SEED-03 leaving agent window"` with `test "agent2 audit transactions fall within demo_last_tuesday through demo_epoch"`)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/manifest.ex (the manifest is the declared contract these assertions are supposed to be reading; a divergence between manifest and seed is a seed bug, a divergence between manifest and assertion is a rotted assertion)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex and lib/threadline_phoenix/demo/seed/anchors.ex (where ticket 4521's close transaction and 4518's presence are actually produced, and where `demo_last_tuesday` / `demo_epoch` are anchored)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/personas.ex and support.ex (persona/actor attribution for the `ticket_replied_and_closed` action and the redaction of the close reply insert)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-41 — the fix mandate; and the standing rule that nothing here narrows a check)
  </read_first>
  <action>
    Fix only the failures Task 1 attributed to the `SEED-03 manifest heroes` and `SEED-03 leaving
    agent window` describe blocks. Leave every other failing cluster alone — plans 198-24 and 198-25
    own those.

    For each such failure, apply the disposition Task 1 recorded:

    - `seed-content-wrong` → change the demo-seed producer so `mix demo.seed` emits content matching
      the manifest's declared contract. Name the module and function changed in the audit file.
    - `assertion-rotted` → rewrite the assertion. GREEN-04's own text requires replacing
      version-pinned milestone literals with shape assertions that cannot rot at the next milestone,
      so prefer asserting on structure (an action name of `ticket_replied_and_closed` exists on the
      close transaction for ticket 4521; the redacted field is present and its value is the redaction
      sentinel; agent2's transactions all fall between the two anchors) over asserting on a pinned
      count or an exact string that a future milestone will legitimately change.

    Honor the three edge contracts while rewriting:
    - `demo_last_tuesday`..`demo_epoch` window membership: state inclusively or exclusively which
      side a boundary-timestamped transaction falls on, so the assertion is not silently dependent on
      `<` versus `<=`.
    - Any query whose result set can be empty must fail loudly on empty rather than pass through an
      `Enum.all?/2` vacuous truth; assert a non-zero count first, then assert the shape.
    - Any assertion over a list whose sort key can tie must compare as a set or by shape, not by
      positional index.

    Prove teeth for at least one fixed failure per describe block. The proof is: run the target test
    against the pre-fix state (e.g. `git stash` the fix, or check out the prior revision of the file
    into a scratch path) and capture the verbatim failure; then run it against the post-fix state and
    capture the verbatim pass. Append both transcripts to `.planning/audits/198-round4-demo-seed.md`
    under a `## Red-then-green teeth proof (tracer cluster)` heading. A fix without a captured red
    is not accepted — that is precisely how a vacuous fix hides.

    Forbidden, and this is the whole point of the plan: no `@tag :skip`, no `git rm`, no deleted
    assertion, no `assert true`, no narrowing of `mix verify.example`, and no edit to
    `.github/workflows/ci.yml`.
  </action>
  <acceptance_criteria>
    - `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix/demo_contract_test.exs` reports `0 failures` for every test inside `describe "SEED-03 manifest heroes"` and `describe "SEED-03 leaving agent window"`, on two consecutive runs.
    - `.planning/audits/198-round4-demo-seed.md` contains the heading `## Red-then-green teeth proof (tracer cluster)` with at least one captured pre-fix failure transcript and its matching post-fix pass transcript per fixed describe block.
    - `grep -c '@tag :skip' examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs` returns `0`.
    - `git diff -- .github/workflows/ci.yml` is empty, and `git diff -- mix.exs` is empty — neither the lane nor the alias was touched.
    - The number of `test ` declarations in `demo_contract_test.exs` after this task is greater than or equal to the number before it (`grep -c '^\s*test "'`), proving no test was deleted to reach green.
    - Every assertion changed in this task is recorded in the audit file with its disposition (`seed-content-wrong` or `assertion-rotted`) and a one-line justification citing the manifest or the seed module.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/threadline_phoenix &amp;&amp; MIX_ENV=test mix test test/threadline_phoenix/demo_contract_test.exs</automated>
  </verify>
  <done>The SEED-03 clusters pass for cause-attributed reasons, with a captured red-then-green proof per cluster, and no test, assertion, alias, or CI lane was weakened to get there.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure the lane, record the drop, and write the fix protocol the expansion plans follow</name>
  <files>.planning/audits/198-round4-demo-seed.md</files>
  <read_first>
    - .planning/audits/198-round4-demo-seed.md (Tasks 1 and 2's own output — the before-count and the teeth proof)
    - .planning/phases/198-green-bringup/198-17-SUMMARY.md (the prior round's disciplined fix loop, and its honest record that fixing the diagnosed cause revealed the next layer of red rather than turning the lane green — the expansion plans must expect the same and must not treat it as failure)
    - .planning/phases/198-green-bringup/deferred-items.md (the format an honestly-deferred discovery takes, if this task surfaces one)
  </read_first>
  <action>
    Run `mix verify.example` twice more from the repository root. Record both trailing
    `N tests, M failures` lines verbatim under a `## Measured after-count` heading in
    `.planning/audits/198-round4-demo-seed.md`, alongside the Task 1 before-count, and state the
    delta as an integer.

    If the after-count is not strictly lower than the before-count, do not paper over it: record the
    figures as measured, state plainly that the tracer did not reduce the lane's failure count, and
    name what the remaining failures are. A truthful non-improvement is a valid outcome of this task;
    a claimed improvement that the numbers do not support is not.

    Then write a `## Fix protocol for the remaining clusters` section — the reusable loop that plans
    198-24, 198-25, 198-26, 198-27 and 198-28 will read as `read_first`. It must state, concretely
    and in order: take the two-run inventory for your cluster; attribute each failure to
    `seed-content-wrong` or `assertion-rotted` from the manifest or the product source, never from
    preference; fix at the cause; capture a red-then-green transcript per fixed cluster; re-measure
    the lane's failure count and record the delta; record any newly-surfaced failure as a discovery
    in `deferred-items.md` rather than silently absorbing or silently dropping it. It must also list
    the forbidden remedies verbatim (`@tag :skip`, `git rm`, deleted assertion, narrowed scope,
    raised `maxFailures`, reduced `--project` set, `needs:` edit) so no expansion executor has to
    infer them.

    Finally, record a one-line honest ceiling statement: this plan produces a local readiness signal
    only; GREEN-04's verdict belongs to the measured CI run in plan 198-29 (D-01).
  </action>
  <acceptance_criteria>
    - `.planning/audits/198-round4-demo-seed.md` contains `## Measured after-count` with two verbatim `N tests, M failures` lines and an explicit integer delta against the Task 1 before-count.
    - The file contains `## Fix protocol for the remaining clusters` listing the six ordered steps and the verbatim forbidden-remedy list.
    - The file contains a line stating that local `mix verify.example` output is not admissible evidence for GREEN-04 and citing D-01.
    - `grep -c 'still red' .planning/audits/198-round4-demo-seed.md | grep -v '^#'` — any remaining failure recorded in this file carries a named cause, never a bare "still red" with no attribution.
    - If the after-count is not lower than the before-count, the file says so in those terms and `.planning/phases/198-green-bringup/deferred-items.md` carries a new dated entry naming the blocker.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^## Measured after-count' .planning/audits/198-round4-demo-seed.md &amp;&amp; grep -q '^## Fix protocol for the remaining clusters' .planning/audits/198-round4-demo-seed.md &amp;&amp; grep -q 'D-01' .planning/audits/198-round4-demo-seed.md</automated>
  </verify>
  <done>The tracer loop is closed end to end — measured before, fixed at cause, proven red-then-green, measured after — and the protocol the rest of the round follows is on disk.</done>
</task>

</tasks>

<reversibility>
  <decision rating="reversible">Choosing `seed-content-wrong` versus `assertion-rotted` per failure is reversible — both edits are ordinary source changes under version control, and the audit file records the reasoning so a later reader can re-decide with the same evidence.</decision>
  <decision rating="costly">Changing a demo-seed producer module is costly, not one-way: `mix demo.seed` output feeds the Playwright specs in plans 198-26/27/28, so a seed change can move the browser lane's failure set. Mitigated by recording every seed change in the audit file and by 198-26 taking its own fresh inventory after this plan lands.</decision>
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mix demo.seed` output → test assertions | Seeded content is the untrusted input the contract tests judge; a wrong fix here launders a data defect into a weakened assertion |
| Local run output → the audit record | Transcripts are the evidence; a paraphrased transcript is unfalsifiable |
| This plan's readiness signal → requirement status | Local output must never cross into a Complete verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-23-01 | Repudiation | the claim "fixed on its merits" | high | mitigate | Every fix carries a captured pre-fix red transcript and a post-fix green transcript in `198-round4-demo-seed.md`; a fix with no captured red is rejected by the acceptance criteria |
| T-198-23-02 | Tampering | test assertions | high | mitigate | Test-count-non-decreasing check, `@tag :skip` count of 0, and empty diffs on `ci.yml` and `mix.exs` are acceptance criteria, not advice |
| T-198-23-03 | Repudiation | non-deterministic failure set | medium | mitigate | Two independent runs; union and intersection both recorded; single-run-only failures explicitly marked rather than averaged |
| T-198-23-04 | Elevation of Privilege | requirement status | high | mitigate | An explicit recorded statement that local output is inadmissible for GREEN-04 per D-01; the verdict is deferred to plan 198-29's measured CI run |
| T-198-23-SC | Tampering | npm/pip/cargo/hex installs | high | mitigate | No dependency is added or upgraded; `mix.exs` and lockfiles are untouched and their emptiness is an acceptance criterion, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `mix verify.example` reports a strictly lower failure count than the Task 1 baseline, with both figures recorded verbatim.
- `cd examples/threadline_phoenix && MIX_ENV=test mix test test/threadline_phoenix/demo_contract_test.exs` shows the SEED-03 describe blocks passing on two consecutive runs.
- `.planning/audits/198-round4-demo-seed.md` carries the inventory, the attribution, the teeth proof, the after-count, and the fix protocol.
- `git diff -- .github/workflows/ci.yml mix.exs` is empty.
</verification>

<success_criteria>
One demo-seed failure cluster is fixed at its cause with proof of teeth, the lane's measured failure
count is lower than it was, and the loop that achieved it is written down precisely enough that six
expansion plans can repeat it without re-deriving it.
</success_criteria>

## Artifacts this phase produces

**New files**
- `.planning/audits/198-round4-demo-seed.md` — two-run failure inventory, cause attribution table, red-then-green teeth proof, measured before/after counts, and the reusable fix protocol for plans 198-24 … 198-28.

**Changed files**
- `examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs` — the `SEED-03 manifest heroes` and `SEED-03 leaving agent window` describe blocks, fixed at cause.
- Zero or more of `examples/threadline_phoenix/lib/threadline_phoenix/demo/seed.ex`, `demo/seed/anchors.ex`, `demo/seed/personas.ex`, `demo/seed/support.ex`, `demo/manifest.ex` — changed only where the diagnosis attributes the failure to seeded content.

**Explicitly not produced**
- No change to `.github/workflows/ci.yml`, `mix.exs`, `playwright.config.ts`, `.github/rulesets/main.json`, or `CONTRIBUTING.md`.
- No requirement status change in `.planning/REQUIREMENTS.md` — that belongs to plan 198-29.

<output>
Create `.planning/phases/198-green-bringup/198-23-SUMMARY.md` when done.
</output>
