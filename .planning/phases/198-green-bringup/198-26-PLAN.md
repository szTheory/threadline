---
phase: 198-green-bringup
plan: 26
type: execute
wave: 2
gap_closure: true
depends_on:
  - 198-23
files_modified:
  - .planning/audits/198-round4-playwright.md
  - examples/threadline_phoenix/e2e/tests/register.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts
autonomous: true
requirements:
  - GREEN-07

estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A measured, on-disk attribution table exists for all 28 masked Playwright failures before any spec is edited: one row per failure with spec file, line, test title, project (`desktop-chromium` or `mobile-chromium`), verbatim assertion message, and a named cause — with rows sharing a cause explicitly marked as sharing it."
    - "The inventory is taken with the full unbounded local run (`mix verify.example_browser --project=desktop-chromium --project=mobile-chromium`), because `playwright.config.ts`'s `maxFailures` is `0` locally and `5` on CI — the CI number is a truncation, not a count."
    - "`maxFailures`, the `--project` set, the spec roster, and `ci-required`'s `needs:` are all byte-unchanged by this plan; the lane's guarantee is not narrowed to reach a lower failure count."
    - "The `register.spec.ts` and `operator-find-mobile.spec.ts` cluster is fixed at cause, with a red-then-green teeth proof captured per fixed spec file."
    - "The lane's measured failure count is recorded before and after, with the integer delta stated."
    - "The inventory is explicitly labelled PRE-MERGE with respect to same-wave sibling plan 198-24, which rewrites demo-seed producer modules in an isolated worktree this plan cannot observe. Every attribution row carries a `seed-sensitive?` verdict, so the rows that 198-28's post-merge re-validation must re-derive are identified here rather than guessed later."
    - "The plan states explicitly that fixing this cluster cannot make `CI required` conclude `success`, because the `Tier A capture lane` is forbidden a remedy this milestone under D-39."
    - statement: "Adjacency edge (GREEN-07): a lane conclusion counts as met only on the exact string `success`; `neutral`, `skipped` and `cancelled` are recorded as not-success, because `re-actors/alls-green` scores a skipped required check as passing and that is precisely the laundering D-09 rejects."
      verification: explicit
    - statement: "Empty edge (GREEN-07): if the local run reports zero test results — a harness or boot failure rather than a test outcome — that is recorded as failed collection, never as an improvement, and the boot log is captured."
      verification: explicit
    - statement: "Ordering edge (GREEN-07): the attribution table is keyed by spec file, line and project, so the same test failing under two projects is two rows rather than one; the union size is stated and compared against the 28 recorded in `deferred-items.md`, and any divergence is explained rather than assumed."
      verification: explicit
  artifacts:
    - ".planning/audits/198-round4-playwright.md — the full 28-failure attribution table, the shared-cause marking, the before-count, the teeth proof for this plan's cluster, and the cluster assignment plans 198-27 and 198-28 execute"
    - "examples/threadline_phoenix/e2e/tests/register.spec.ts — fixed at cause"
    - "examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts — fixed at cause"
  key_links:
    - "`playwright.config.ts:141` `maxFailures: process.env.CI ? 5 : 0` → CI truncates at 5 → the 28-failure figure exists only from an unbounded local run → any before/after comparison must use the same unbounded local command"
    - "`mix demo.seed` content (changed by plan 198-23) → the browser lane's fixtures → re-inventory rather than reuse the 198-17-era failure list"
    - "same-wave sibling 198-24 rewrites demo-seed producers in an isolated worktree → this plan's inventory is pre-merge with respect to it → seed-sensitive rows are flagged here and re-derived post-merge by 198-28's gate, never assumed still valid"
    - "this plan's attribution table → plans 198-27 and 198-28's work lists → clusters are assigned here, not re-derived there"
  prohibitions:
    - statement: "A lane must not be made green by hiding failures. `maxFailures` must not be raised or removed, the `--project` set must not be narrowed, and no spec file or test may be skipped, `.fixme`-d, `.skip`-ped, deleted, or `git rm`-ed."
      status: kept
      verification: flagged-unverified
    - statement: "`ci-required`'s `needs:` list must not be changed. If it ever were, D-42 requires reconciling `.github/rulesets/main.json` and `CONTRIBUTING.md`'s `## CI Coverage` table in the same diff — nothing in this round should need to touch it."
      status: kept
      verification: flagged-unverified
    - statement: "Tier-A `page.*` evidence regeneration is forbidden for this milestone (D-39). No task in this plan touches the Tier A capture lane or its scorecards."
      status: kept
      verification: flagged-unverified
---

<objective>
**Gap 2, part 1 of 3.** `Example app browser E2E (Playwright)` is red. CI reports only 5 failures
because `playwright.config.ts:141` sets `maxFailures: process.env.CI ? 5 : 0` — the real, unbounded
local figure recorded by plan 198-17 is **28 failures across 14 tests in 8 spec files**. D-40
(maintainer decision B1) directs this round to fix them, in the same disciplined, no-weakening,
red-then-green manner 198-17 used.

This plan does two things: it produces the attribution table for **all 28** so plans 198-27 and
198-28 have real work lists instead of guesses, and it fixes the first, smallest cluster —
`register.spec.ts` (1 test) and `operator-find-mobile.spec.ts` (5 tests) — end to end with proof.

**Stated plainly, and this is not a hedge: GREEN-07 cannot reach Complete this round even if every
task in every plan succeeds.** `CI required` has 12 `needs:` members and 3 are red. This round can
close two of them — `Run test suite (current)` (plans 198-23/24/25) and `Example app browser E2E`
(this plan and 198-27/198-28). The third, `Tier A capture lane (byte-stable evidence)`, has exactly
one available remedy — Tier-A `page.*` scorecard regeneration — and that remedy is **forbidden for
this entire milestone** under D-39. It is out of scope and must not be attempted. **The honest target
for this round is: reduce the red `needs:` count from 3 to 1, where the sole remaining red lane is
the one D-39 forbids fixing.** No `must_haves` truth in this round claims `CI required` will conclude
`success`, and no task may make the aggregate green by removing a lane.

Output: the 28-failure attribution table, cluster 1 fixed, and a measured local failure-count delta.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/deferred-items.md
@.planning/WINDOWS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attribute all 28 masked failures to named causes, before changing any spec</name>
  <files>.planning/audits/198-round4-playwright.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/deferred-items.md (the `## Plan 198-17` entry in full — it names the 8 spec files, the 28-failure figure, the 14 affected tests, and the `maxFailures: 5` masking mechanism; this is the starting hypothesis, not the answer)
    - .planning/audits/198-example-browser-e2e.md (198-17's own diagnosis and its fix — the model for the attribution table's shape and for what "fixed at cause" looked like when it worked)
    - examples/threadline_phoenix/e2e/playwright.config.ts (lines 130-150 — `expect.timeout` 15s, `retries: process.env.CI ? 1 : 0`, `maxFailures: process.env.CI ? 5 : 0`, `workers: 1`, and the project definitions; confirm the local run is unbounded so the count is a count and not a truncation)
    - examples/threadline_phoenix/e2e/run-e2e.sh (the boot path: `mix deps.get --only test`, `ecto.create`, `ecto.migrate`, `demo.reset`, `demo.seed`, and the boot preflight — the fixtures every spec depends on)
    - mix.exs (the `verify_example_browser/1` private helper — the exact script and env the verify command runs, including `THREADLINE_E2E=1`)
    - .planning/audits/198-round4-demo-seed.md (plan 198-23's seed changes — the browser lane runs against the same `mix demo.seed` output, so a seed change may have already moved this failure set)
    - .planning/phases/198-green-bringup/198-24-PLAN.md (its `files_modified` list — `demo/seed.ex`, `demo/seed/personas.ex`, `demo/seed/temporal.ex`, `demo/seed/retention_tail.ex`, `demo/seed/retention_runs.ex`, `demo/reset.ex`. **198-24 executes in the same wave as this plan, in an isolated worktree, so its seed changes are NOT visible here.** Read its plan to know which seeded surfaces are about to move, and flag the rows that read them.)
    - .planning/WINDOWS.md (entry #8 — the record of this masked-failure window)
  </read_first>
  <action>
    Change no spec file and no config in this task. Measure and attribute only.

    Run `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` from the
    repository root, unbounded (local, so `maxFailures` is `0`). Capture the full output and the
    `playwright-report` / `test-results` directories. If the run aborts on a boot failure rather than
    producing test outcomes, record that as failed collection with the captured
    `/tmp/threadline_phoenix_e2e.log`, and stop — a boot failure is not a test failure and must not be
    attributed as one.

    Write `.planning/audits/198-round4-playwright.md` with:

    (a) `## Measured before-count` — the verbatim Playwright summary line (`N failed`, `N passed`,
    `N did not run`) plus the total test count, and an explicit note that this is the unbounded local
    figure and therefore differs from CI's truncated 5 by construction, citing
    `playwright.config.ts:141`.

    (b) `## Full failure attribution` — one row per failure, columns: spec file, line, test title,
    project (`desktop-chromium` / `mobile-chromium`), verbatim assertion or error message, cause,
    `shares cause with`. Cause values are free text but must name a mechanism (a removed product
    literal, a changed selector, a missing seeded row, a viewport/overflow regression, a snapshot
    baseline divergence, a timing/wait defect) — never "unknown" without a follow-up note.
    The same test failing under both projects is two rows.

    State the union size explicitly and compare it against the 28 recorded in `deferred-items.md`.
    If it is not 28, explain the divergence — plan 198-23's seed changes and 197-era product changes
    are both plausible causes, and an unexplained divergence is itself a finding.

    (c) `## Cluster assignment` — assign every row to exactly one of three clusters, so no failure is
    orphaned and none is double-owned:
      - `198-26` (this plan): `register.spec.ts`, `operator-find-mobile.spec.ts`
      - `198-27`: `operator-phase-135-uat.spec.ts`, `operator-phase-173-uat.spec.ts`, `operator-phase-175-uat.spec.ts`, `operator-phase-177-uat.spec.ts`
      - `198-28`: `operator-screenshot-regression.spec.ts`, `operator-screenshots.spec.ts`
    Any failure in a spec file outside those eight is a new discovery: give it a row, assign it to a
    cluster, and record it as a discovery in the audit file — do not silently drop it.

    (d) `## Pre-merge status` — state, in one sentence, that this inventory was taken in an isolated
    worktree BEFORE same-wave sibling plan 198-24's demo-seed changes merged, and record the current
    `git rev-parse HEAD` so the point of measurement is pinned. Then add a `seed-sensitive?` column to
    the `## Full failure attribution` table with a `yes`/`no` verdict per row: `yes` if the failing
    assertion reads `mix demo.seed`-produced content (timeline rows, transaction/row-history values,
    coverage rows, export job states, retention runs, hero tickets 4521/4518, persona attribution),
    `no` if it depends only on static markup, CSS, motion, or routing. Read 198-24's `files_modified`
    and its task bodies to decide, and state the count of `yes` rows explicitly — that count is the
    work order for 198-28's post-merge re-validation gate.

    (e) `## Forbidden remedies` — restate verbatim, for the executors of 198-27 and 198-28: no
    `test.skip`, `test.fixme`, `.only`, deleted assertion, deleted spec, `git rm`, raised or removed
    `maxFailures`, narrowed `--project` set, `needs:` edit, and no Tier-A `page.*` regeneration
    (D-39).
  </action>
  <acceptance_criteria>
    - `.planning/audits/198-round4-playwright.md` exists and contains the literal headings `## Measured before-count`, `## Full failure attribution`, `## Pre-merge status`, `## Cluster assignment`, and `## Forbidden remedies`.
    - `## Pre-merge status` names plan 198-24 as the unobserved same-wave sibling and records a 40-character `git rev-parse HEAD` value.
    - Every row of `## Full failure attribution` carries a `seed-sensitive?` cell whose value is exactly `yes` or `no` — no blank cells — and the count of `yes` rows is stated as an integer.
    - The attribution table has one row per (spec file, line, project) failure, every row has a non-empty verbatim message and a non-empty cause naming a mechanism, and no cause cell reads `unknown` without an accompanying follow-up note.
    - The union size is stated as an integer and compared explicitly against 28, with a written explanation if it differs.
    - Every attribution row is assigned to exactly one of `198-26`, `198-27`, `198-28` in `## Cluster assignment`; the assigned row count equals the union size.
    - `git diff -- examples/threadline_phoenix/e2e/ .github/workflows/ci.yml` is empty after this task — nothing but the audit file changed.
    - `grep -n 'maxFailures' examples/threadline_phoenix/e2e/playwright.config.ts` still returns the line `maxFailures: process.env.CI ? 5 : 0`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/audits/198-round4-playwright.md &amp;&amp; grep -q '^## Full failure attribution' .planning/audits/198-round4-playwright.md &amp;&amp; grep -q '^## Cluster assignment' .planning/audits/198-round4-playwright.md &amp;&amp; grep -q 'maxFailures: process.env.CI ? 5 : 0' examples/threadline_phoenix/e2e/playwright.config.ts &amp;&amp; test -z "$(git diff --name-only -- examples/threadline_phoenix/e2e .github/workflows/ci.yml)"</automated>
  </verify>
  <done>Every masked Playwright failure has a row, a verbatim message, a named cause, and exactly one owning cluster — produced from an unbounded local run, with nothing edited.</done>
</task>

<task type="auto">
  <name>Task 2: Fix register.spec.ts at its cause, with a red-then-green teeth proof</name>
  <files>examples/threadline_phoenix/e2e/tests/register.spec.ts, .planning/audits/198-round4-playwright.md</files>
  <read_first>
    - .planning/audits/198-round4-playwright.md (Task 1's attribution rows assigned to cluster `198-26` for `register.spec.ts`)
    - examples/threadline_phoenix/e2e/tests/register.spec.ts (all 18 lines — `test.describe("register UX")` / `test("register lands on home signed in")`; the whole spec is one flow, so the cause is either the registration path itself or the post-registration home assertion)
    - .planning/audits/198-example-browser-e2e.md (198-17's precedent: two specs asserted a `"selected schema"` literal that commit `842bd737` intentionally removed from the product a day earlier — a product change that legitimately invalidated a spec literal is fixed by updating the spec, and a product regression is fixed in the product; decide which this is from evidence)
    - examples/threadline_phoenix/e2e/run-e2e.sh (the boot/seed path — if registration depends on seeded state, the fixture is part of the cause)
  </read_first>
  <action>
    Fix the `register lands on home signed in` failure at the cause Task 1 attributed to it.

    If the cause is a product regression on the registration or post-registration-redirect path, fix
    the product. If the cause is a spec assertion pinned to a literal the product legitimately changed
    (198-17's precedent), fix the assertion — and prefer a shape assertion (the signed-in home
    surface is reachable and exposes its signed-in affordance) over a pinned copy string that will rot
    again at the next milestone.

    Capture a red-then-green teeth proof: run the single spec against the pre-fix state and capture
    the verbatim failure, then against the post-fix state and capture the verbatim pass. Append both
    under `## Red-then-green teeth proof (198-26, register)` in
    `.planning/audits/198-round4-playwright.md`.

    Forbidden: `test.skip`, `test.fixme`, `.only`, deleted assertion, deleted spec, `git rm`, any
    edit to `playwright.config.ts`, any change to the `--project` set, any edit to
    `.github/workflows/ci.yml`.
  </action>
  <acceptance_criteria>
    - `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` reports `0 failed` for every test in `register.spec.ts` under both projects.
    - `.planning/audits/198-round4-playwright.md` contains `## Red-then-green teeth proof (198-26, register)` with a captured pre-fix failure and a matching post-fix pass, both verbatim.
    - `grep -cE 'test\.(skip|fixme|only)' examples/threadline_phoenix/e2e/tests/register.spec.ts` returns `0`.
    - `grep -c '^\s*test(' examples/threadline_phoenix/e2e/tests/register.spec.ts` is greater than or equal to its pre-task value.
    - `git diff -- examples/threadline_phoenix/e2e/playwright.config.ts .github/workflows/ci.yml` is empty.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.example_browser --project=desktop-chromium --project=mobile-chromium</automated>
  </verify>
  <done>`register.spec.ts` passes under both projects for a cause-attributed reason, with a captured red-then-green proof and no weakened assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the operator-find-mobile cluster at its cause and re-measure the lane</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts, .planning/audits/198-round4-playwright.md</files>
  <read_first>
    - .planning/audits/198-round4-playwright.md (Task 1's attribution rows assigned to cluster `198-26` for `operator-find-mobile.spec.ts`, and the `shares cause with` column — several of these five tests likely share one cause and must not be fixed five separate ways)
    - examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts (all 113 lines — `test.describe("operator Find cluster mobile UAT")` with `test("timeline dense mobile keeps filters and rows visible without a journey legend")` at line 32, `test("transaction mobile opens from Timeline with semantic values and copy controls")` at 48, `test("row-history mobile opens from a Transaction row with formatted values")` at 66, `test("actor mobile exposes selected window state and transaction pivots")` at 88, `test("coverage mobile shows Add capture remediation without horizontal overflow")` at 103)
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md (the Round 3 per-job section — three of the five failures CI actually reported before truncating are `operator-find-mobile.spec.ts:48:3`, `:66:3` and `:103:3`, so this file is the single largest contributor to the visible red)
    - .planning/audits/198-round4-demo-seed.md (plan 198-23's seed changes — the mobile Find flows read seeded timeline/transaction/row-history content)
    - .planning/phases/198-green-bringup/198-24-PLAN.md (same-wave sibling, not visible in this worktree; the mobile Find flows read exactly the seeded content 198-24 may rewrite, so these fixes are provisional until 198-28's post-merge gate re-derives them)
  </read_first>
  <action>
    Fix the five `operator-find-mobile.spec.ts` failures at the causes Task 1 attributed to them.
    Where the attribution marks rows as sharing a cause, apply one fix and record that it closed N
    rows — do not credit one fix as five.

    Three of these — `:48:3`, `:66:3`, `:103:3` — are the tests CI itself reported before truncating,
    so this file is the highest-leverage target in the lane. Treat a per-viewport failure (overflow,
    responsive collapse) and a shared data failure (a missing seeded transaction or row-history entry)
    as distinguishable hypotheses and say which the evidence supports, rather than fixing both
    speculatively.

    Where an assertion pins a product copy string or an exact pixel figure, prefer a shape assertion
    (the control is present and reachable; the surface has no horizontal overflow at the tested
    viewport) so the spec cannot rot at the next milestone — the same discipline GREEN-04's text
    demands of the Elixir suite.

    Capture a red-then-green teeth proof under `## Red-then-green teeth proof (198-26, find-mobile)`.

    Then re-measure: run `mix verify.example_browser --project=desktop-chromium
    --project=mobile-chromium` unbounded and append `## Measured after-count (198-26)` with the
    verbatim summary line and the integer delta against Task 1's before-count. If new failures
    surfaced that Task 1's inventory did not contain, add them as rows with causes and assign them to
    a cluster — 198-17's own record is that fixing a diagnosed cause reveals the next layer, and that
    is an expected outcome to be recorded, not a failure to be hidden.

    Close with an explicit written statement, in the audit file, that this cluster's closure does not
    and cannot make `CI required` conclude `success`, naming the `Tier A capture lane` and D-39 as the
    reason. In the same section, state that every `seed-sensitive? = yes` row in this plan's
    attribution and every fix made against one is **provisional pending 198-28's post-merge
    re-validation**, because same-wave sibling 198-24's demo-seed changes were not observable here.

    Forbidden: `test.skip`, `test.fixme`, `.only`, deleted assertion or spec, `git rm`, any edit to
    `playwright.config.ts`, `--project` narrowing, any edit to `.github/workflows/ci.yml`, and any
    Tier-A `page.*` regeneration.
  </action>
  <acceptance_criteria>
    - `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` reports `0 failed` for every test in `operator-find-mobile.spec.ts` under both projects.
    - `.planning/audits/198-round4-playwright.md` contains `## Red-then-green teeth proof (198-26, find-mobile)` with verbatim pre-fix failure and post-fix pass transcripts.
    - The audit file contains `## Measured after-count (198-26)` with the verbatim Playwright summary line and an explicit integer delta against `## Measured before-count`.
    - Where the attribution marked shared causes, the audit file records which single fix closed which set of rows.
    - The audit file contains a statement naming `Tier A capture lane (byte-stable evidence)` and D-39 as the reason `CI required` cannot conclude `success` this round.
    - The audit file states that every `seed-sensitive? = yes` row and every fix made against one is provisional pending plan 198-28's post-merge re-validation, naming 198-24 as the unobserved sibling.
    - `grep -cE 'test\.(skip|fixme|only)' examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts` returns `0`.
    - `git diff -- examples/threadline_phoenix/e2e/playwright.config.ts .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md` is empty.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.example_browser --project=desktop-chromium --project=mobile-chromium</automated>
  </verify>
  <done>The find-mobile cluster passes under both projects for cause-attributed reasons, the lane's measured failure count is recorded with a delta, and the round's honest GREEN-07 ceiling is stated in writing.</done>
</task>

</tasks>

<reversibility>
  <decision rating="reversible">Rewriting a pinned copy-string assertion into a shape assertion is reversible — an ordinary spec change with the reasoning recorded in the attribution table.</decision>
  <decision rating="costly">A product-side fix on the registration or mobile Find path is costly: it changes operator-surface behaviour that other specs and the Tier A capture lane observe. Mitigated by the unbounded re-measure in Task 3, which surfaces any lane-wide movement before the plan closes.</decision>
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser lane result → merge gate | The lane is a `needs:` member of `CI required`; weakening it silently weakens the merge guarantee |
| `maxFailures` truncation → the failure count | CI's 5 is a truncation of a larger real number; treating it as the count under-scopes the work |
| `mix demo.seed` fixtures → spec assertions | Seeded content changed by plans 198-23/24/25 can move this lane's failure set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-26-01 | Tampering | `playwright.config.ts` `maxFailures` | high | mitigate | The literal `maxFailures: process.env.CI ? 5 : 0` is asserted present by an acceptance criterion, and `git diff` on the config must be empty |
| T-198-26-02 | Tampering | the spec roster | high | mitigate | `test.skip`/`test.fixme`/`.only` counts of 0 and non-decreasing test counts are acceptance criteria |
| T-198-26-03 | Repudiation | the "28 failures" figure | medium | mitigate | The union size is stated and reconciled against `deferred-items.md`'s 28, with divergence explained rather than assumed |
| T-198-26-04 | Elevation of Privilege | `ci-required`'s `needs:` and the ruleset | high | mitigate | Empty `git diff` on `ci.yml`, `.github/rulesets/main.json` and `CONTRIBUTING.md` is an acceptance criterion; D-42's same-diff interlock is restated in the prohibitions |
| T-198-26-05 | Repudiation | the GREEN-07 ceiling | high | mitigate | A written statement in the audit file that this round cannot make `CI required` `success`, naming the Tier A lane and D-39 |
| T-198-26-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed, added, or upgraded; `package.json` and its lockfile are untouched, so the package-legitimacy gate does not apply. `.planning/phases/198-green-bringup/198-RESEARCH.md` carries no new package requiring audit for this plan |
</threat_model>

<verification>
- `.planning/audits/198-round4-playwright.md` contains the before-count, the full 28-row attribution with causes, the cluster assignment covering every row exactly once, both teeth proofs, the after-count with delta, and the D-39 ceiling statement.
- `mix verify.example_browser --project=desktop-chromium --project=mobile-chromium` reports 0 failures for `register.spec.ts` and `operator-find-mobile.spec.ts` under both projects.
- `grep -n 'maxFailures' examples/threadline_phoenix/e2e/playwright.config.ts` is unchanged.
- `git diff -- .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts` is empty.
</verification>

<success_criteria>
Every one of the lane's masked failures has a named cause and an owning cluster, the first cluster is
fixed with proof of teeth, the lane's measured failure count is lower, and nothing about the lane's
guarantee — its `maxFailures`, its projects, its spec roster, or its membership in `needs:` — was
weakened to achieve it.
</success_criteria>

## Artifacts this phase produces

**New files**
- `.planning/audits/198-round4-playwright.md` — measured before-count, full per-failure attribution table for all masked failures, cluster assignment for plans 198-26/27/28, forbidden-remedy list, two red-then-green teeth proofs, measured after-count with delta, and the written D-39 ceiling statement.

**Changed files**
- `examples/threadline_phoenix/e2e/tests/register.spec.ts` — fixed at cause.
- `examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts` — fixed at cause.
- Possibly operator-surface product source, if and only if the attribution shows a product regression rather than a rotted assertion; any such file is named in the audit file.

**Explicitly not produced**
- No change to `examples/threadline_phoenix/e2e/playwright.config.ts`, `.github/workflows/ci.yml`, `.github/rulesets/main.json`, `CONTRIBUTING.md`, or `.planning/REQUIREMENTS.md`.
- No Tier A capture-lane work of any kind (D-39).

<output>
Create `.planning/phases/198-green-bringup/198-26-SUMMARY.md` when done.
</output>
