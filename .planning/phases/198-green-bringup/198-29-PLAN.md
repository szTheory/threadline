---
phase: 198-green-bringup
plan: 29
type: execute
wave: 5
gap_closure: true
depends_on:
  - 198-23
  - 198-24
  - 198-25
  - 198-26
  - 198-27
  - 198-28
files_modified:
  - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/198-green-bringup/deferred-items.md
autonomous: true
requirements:
  - GREEN-04
  - GREEN-07

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real GitHub Actions run exists on a fresh `ci/198-round4` branch whose head SHA contains every commit from plans 198-23 through 198-28, and its run id, head SHA, per-job conclusions and wall-clock duration are recorded on disk."
    - "`Run test suite (current)`'s conclusion is recorded verbatim; GREEN-04 is marked Complete if and only if that check concludes `success` on this measured run, never on local `mix verify.example` output (D-01)."
    - "`CI required`'s conclusion is recorded verbatim alongside every one of its 12 `needs:` members' conclusions, so the aggregate's verdict is traceable to its constituents."
    - "The count of red `needs:` members is stated as an integer and compared against round 3's 3; the honest target of this round is 3 down to 1."
    - "GREEN-07 stays Pending with an evidence-backed note if any `needs:` member is red — and `Tier A capture lane (byte-stable evidence)` is expected to be red because D-39 forbids its only remedy for this milestone. GREEN-07 is NOT marked Complete this round on any basis."
    - "Every remaining red check carries a root-cause citation to a specific defect or decision — never a bare 'still red'."
    - "The pre-push prediction is based on plan 198-28's POST-MERGE re-validated attribution, not on plans 198-26/198-27's pre-merge tables, and the prediction record says which it used. If 198-28 recorded a non-zero divergence count, the prediction accounts for it explicitly."
    - "The pull request is a draft, explicitly marked DO NOT MERGE in its body, and is a measurement vehicle rather than a merge request."
    - "No check is re-run, re-dispatched, or selectively retried; the run's `attempt` number is recorded, and any value above 1 is explained verbatim."
    - statement: "Adjacency edge (GREEN-07): a lane counts as met only on the exact conclusion string `success`; `neutral`, `skipped` and `cancelled` are each recorded as not-success, because `re-actors/alls-green` scores a skipped required check as passing and that is exactly the laundering D-09 rejects."
      verification: explicit
    - statement: "Empty edge (GREEN-07): if the run reports zero `needs:`-member conclusions, that is recorded as failed collection rather than as a pass, so a broken `gh` query cannot manufacture a green record."
      verification: explicit
    - statement: "Ordering edge (GREEN-07): if more than one workflow run exists for the pushed head SHA, the most recent completed run is authoritative and every competing run id is listed, so the selection is auditable rather than implicit."
      verification: explicit
    - statement: "Concurrency edge (GREEN-07): the branch is freshly named `ci/198-round4`, distinct from `ci/198-round3` and `ci/198-gap-closure`, so a concurrent or stale run on an earlier branch cannot be mistaken for this round's evidence; the resolved SHAs of all three refs are recorded."
      verification: explicit
    - statement: "Adjacency edge (GREEN-04): `Run test suite (current)` counts as met only on the exact conclusion `success`; a job that fails after `mix verify.test` passes but `mix verify.example` fails is recorded as not-met with the failing step named."
      verification: explicit
    - statement: "Empty edge (GREEN-04): if the `verify-test` job reports no test-summary line at all, that is failed collection, not a pass; the job log excerpt is captured verbatim."
      verification: explicit
    - statement: "Ordering edge (GREEN-04): where the round-3 record and this round's record disagree on a failure count, both figures are shown side by side with their run ids rather than the newer silently replacing the older."
      verification: explicit
  artifacts:
    - ".planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — a Round 4 section with the push, run id, head SHA, per-job table, prediction scorecard, five-column baseline comparison, wall clock, `CI required` conclusion, `mergeStateStatus`, and a root cause for every remaining red check"
    - ".planning/REQUIREMENTS.md — GREEN-04 and GREEN-07 statuses set strictly from the measured run"
    - ".planning/STATE.md — current position updated to the measured outcome"
  key_links:
    - "plans 198-23/24/25's demo-seed fixes → `mix verify.example` exit 0 → `Run test suite (current)` `success` → GREEN-04's only admissible evidence"
    - "plans 198-26/27/28's Playwright fixes → `Example app browser E2E (Playwright)` `success` → one fewer red `needs:` member"
    - "`Tier A capture lane (byte-stable evidence)` → D-39's milestone-level regeneration prohibition → `CI required` stays `failure` → GREEN-07 stays Pending by construction, not by defect"
  prohibitions:
    - statement: "GREEN-04 and GREEN-07 must not be marked Complete on local evidence. Local `mix test`, `mix ci.all`, `mix verify.example` and `mix verify.example_browser` results are readiness signals only (D-01)."
      status: kept
      verification: flagged-unverified
    - statement: "`ci-required`'s `needs:` list must not be narrowed and `CI required`'s guarantee must not be reduced to manufacture a green aggregate. Any `needs:` change would require reconciling `.github/rulesets/main.json` and `CONTRIBUTING.md`'s `## CI Coverage` table in the same diff (D-42) — and nothing in this round should need to touch it."
      status: kept
      verification: flagged-unverified
    - statement: "Tier-A `page.*` evidence regeneration is forbidden for this milestone (D-39). The Tier A capture lane must not be fixed, worked around, or removed from `needs:`; its red conclusion is recorded with its citation."
      status: kept
      verification: flagged-unverified
    - statement: "No check may be re-run, re-dispatched, or selectively retried to obtain a better result. The run's `attempt` number is recorded and any value above 1 is explained verbatim."
      status: kept
      verification: flagged-unverified
---

<objective>
The closing plan of gap-closure round 4: push a fresh branch, measure one real CI run to completion,
and record the outcome with enough precision that the next verifier can re-derive every verdict from
the run id alone.

Per D-01, local `mix test`, `mix ci.all`, `mix verify.example` and `mix verify.example_browser` are
**not admissible evidence** for GREEN-04 or GREEN-07. Only a measured CI run is. Plans 198-23 through
198-28 produced readiness signals; this plan produces the verdict.

**What this round can close and what it cannot, stated before the run so the prediction is
falsifiable:**
- `Run test suite (current)` — can close (plans 198-23/24/25).
- `Example app browser E2E (Playwright)` — can close (plans 198-26/27/28).
- `Tier A capture lane (byte-stable evidence)` — **cannot close.** Its `scroll_cost` drift has exactly
  one available remedy, Tier-A `page.*` scorecard regeneration, and that remedy is forbidden for this
  entire milestone under D-39. No plan in this round touched it.

**Therefore `CI required` cannot conclude `success` this round, and GREEN-07 cannot reach Complete.**
The honest target is: reduce the red `needs:` count from 3 to 1, where the sole remaining red lane is
the one D-39 forbids fixing. GREEN-04, by contrast, *can* close this round — it depends only on
`Run test suite (current)`.

**Flagged assumption (spec-less probe fallback, no-silent-drop):** of the edge-coverage probe's 27
applicable items, 20 belong to GREEN-01, 02, 03, 05, 06, 08, 09, 10, 11 and 12 — including four
`unclassified` rows (GREEN-01, GREEN-05, GREEN-09, GREEN-10) that remain `unresolved`. Those ten
requirements are Complete and independently re-verified in `198-VERIFICATION.md` round 3, and this
round touches none of the files that satisfy them, so no predicate is minted for them. They are
surfaced here rather than dropped. The 7 items belonging to GREEN-04 and GREEN-07 are authored into
`must_haves.truths` across plans 198-23 … 198-29.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/deferred-items.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: State a falsifiable prediction, push `ci/198-round4`, and observe one real CI run to completion</name>
  <files>.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md (the `## Round 3 (2026-08-28) — Measured CI run (Plan 198-22, gap-closure round 3)` section in full — the push format, the run-selection discipline, the per-job table shape, the four-column baseline comparison, and the `attempt: 1` / re-run-discipline record this round must match and extend)
    - .planning/audits/198-round4-demo-seed.md (plans 198-23/24/25's closing measured `mix verify.example` count — the basis for predicting `Run test suite (current)`)
    - .planning/audits/198-round4-playwright.md (**read `## Post-merge re-validation (198-28)` and its `## Divergences` subsection first** — plans 198-26 and 198-27 inventoried pre-merge, blind to their same-wave siblings' seed rewrites, so 198-28's re-derived rows supersede theirs; then the closing counts, the CI-contributing versus local-only split, and any rows left `open` — the basis for predicting `Example app browser E2E`)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-01 admissibility, D-39 Tier A, D-40 browser lane, D-41 demo-seed, D-42 the `needs:` interlock)
    - .github/workflows/ci.yml (the `ci-required` job's 12-member `needs:` list and each job's `name:` — read only, to enumerate the members the per-job table must account for; do not edit)
  </read_first>
  <action>
    Write the prediction **before** pushing, under `## Round 4 (2026-08-28) — Prediction stated
    before the push` in `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md`. One row per
    `needs:` member with a predicted conclusion and a one-line basis. Predict
    `Tier A capture lane (byte-stable evidence)` as `failure` with basis `D-39 — only remedy
    forbidden this milestone; untouched this round`. Predict the red-lane count explicitly as an
    integer. A prediction written after the fact is worthless; this one is a scorecard input.

    Then push. Use a **fresh** branch name `ci/198-round4`, distinct from `ci/198-round3` and
    `ci/198-gap-closure`. Record verbatim: the `git push` output, `git ls-remote --heads origin
    ci/198-round4`, and the resolved SHAs of `ci/198-round3` and `ci/198-gap-closure` at the same
    moment, so the three refs are provably distinct and this round's evidence cannot be confused with
    an earlier round's. Record `git log origin/main..HEAD --oneline | wc -l` at push time.

    Open a **draft** pull request against `main` whose body explicitly says DO NOT MERGE and states
    that it is a measurement vehicle. Record its number, state and `mergeStateStatus`.

    Observe exactly one run to natural completion. Record `gh run list --branch ci/198-round4 --json
    databaseId,conclusion,headSha,status,createdAt` verbatim; if it returns more than one run for the
    head SHA, list every run id and state which is authoritative (the most recent completed) and why.
    Then record `gh run view <id> --json status,conclusion,createdAt,updatedAt,attempt,headSha`
    verbatim. Confirm the run's `headSha` equals the recorded local head SHA.

    Do not re-run, re-dispatch, or selectively retry any check for any reason. Record the `attempt`
    number; if it is above 1, explain verbatim why.
  </action>
  <acceptance_criteria>
    - `198-CI-MEASUREMENT.md` contains `## Round 4 (2026-08-28) — Prediction stated before the push` with one row per `needs:` member, each carrying a predicted conclusion and a one-line basis, plus a predicted red-lane count as an integer.
    - The Tier A row's predicted conclusion is `failure` and its basis cites D-39.
    - The `Example app browser E2E (Playwright)` row's basis cites `## Post-merge re-validation (198-28)` by name and states 198-28's recorded divergence count as an integer.
    - The file records the verbatim `git push` output for `ci/198-round4`, the `git ls-remote` resolution of all three `ci/198-*` refs with distinct SHAs, and the `origin/main..HEAD` commit count at push time.
    - The pull request is recorded as `isDraft: true` with a body containing the literal string DO NOT MERGE, and its `mergeStateStatus` is recorded.
    - The run's `headSha` recorded from `gh run view` equals the recorded local head SHA, character for character.
    - The run's `attempt` value is recorded; if greater than 1, a verbatim explanation is present.
    - If `gh run list` returns more than one run for the head SHA, every run id is listed and the authoritative one is named with its reason.
  </acceptance_criteria>
  <verify>
    <automated>gh run list --branch ci/198-round4 --limit 5 --json databaseId,conclusion,headSha,status &amp;&amp; grep -q 'Round 4' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</automated>
  </verify>
  <done>A falsifiable per-lane prediction is on disk, written before the push; one real run on a provably fresh branch has been observed to natural completion with no retry; and its identity is pinned to the local head SHA.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Round 4 measured record — per-job table, prediction scorecard, and a root cause for every red check</name>
  <files>.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md, .planning/phases/198-green-bringup/deferred-items.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md (Task 1's prediction and run record, plus the Round 3 section's per-job table, `### Prediction scorecard`, `### Four-column baseline comparison`, `### Wall-clock ≤20-minute evaluation`, `### CI required conclusion`, `### mergeStateStatus`, `### Headline claim`, and `### Re-run discipline` — Round 4 extends every one of these)
    - .planning/audits/198-round4-demo-seed.md and .planning/audits/198-round4-playwright.md (this round's local evidence — used to root-cause any lane that came back red despite the fixes, never to substitute for the run's own conclusions)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-39, D-40, D-41 — the citations every expected-red lane must carry)
  </read_first>
  <action>
    Write `## Round 4 (2026-08-28) — Measured CI run (Plan 198-29, gap-closure round 4)` into
    `198-CI-MEASUREMENT.md`, containing:

    (a) `### Per-job table` — every job the run reported plus `CI required` called out separately,
    columns: check name, conclusion, duration (started → completed). Record the number of conclusions
    collected; if that number is zero, record failed collection rather than a pass.

    (b) The verbatim `re-actors/alls-green` step output enumerating all 12 `needs:` members by name
    with their status. State the counts: `success`, `failure`, `skipped`, `cancelled`. Any `skipped`
    or `cancelled` member is recorded as not-success with an explicit note that GitHub would score it
    as passing and D-09 rejects that laundering.

    (c) `### Prediction scorecard` — one row per `needs:` member: predicted, actual, hit or miss. State
    the hit rate as a fraction. A missed prediction is written as a miss with its explanation, never
    retroactively re-described.

    (d) `### Five-column baseline comparison` — extend Round 3's four-column table (baseline
    `33138291361`, round 1 `33183920952`, round 2 `33197493051`, round 3 `33204829086`) with round 4's
    run id. State "N of the 7 originally-red baseline jobs are now green" as an integer and compare it
    against round 3's 4.

    (e) `### Red `needs:` count` — the integer count of red members, compared against round 3's 3, and
    an explicit statement of whether this round hit its stated target of 1.

    (f) `### Root cause for every still-red check` — one subsection per red check with a named cause
    and a citation to a specific defect, audit file, or decision. `Tier A capture lane` cites D-39 and
    `.planning/audits/198-tier-a-byte-stability.md`. Any lane that came back red despite this round's
    fixes gets a fresh diagnosis from the job log — the local audit files are context, not an excuse.
    **A bare "still red" without attribution is not acceptable in any row.**

    (g) `### Wall-clock ≤20-minute evaluation` — the measured duration in `NmNNs` form and the
    explicit comparison against 20m00s, as a figure rather than a judgment.

    (h) `### CI required conclusion` — the literal conclusion string, and the answer to "is it exactly
    `success`?".

    (i) `### mergeStateStatus` — the verbatim `gh pr view` output.

    (j) `### Headline claim` — is "`origin/main` carries every local commit and its CI concludes
    green" true, false, or true-subject-to-a-merge? Answer with the `origin/main..HEAD` count and the
    PR's mergeability, not with a narrative.

    (k) `### Re-run discipline` — the `attempt` number and a statement that no check was re-run.

    Add a dated entry to `.planning/phases/198-green-bringup/deferred-items.md` for every lane that
    remains red, naming its cause and the decision or successor work that owns it.
  </action>
  <acceptance_criteria>
    - `198-CI-MEASUREMENT.md` contains a `## Round 4 (2026-08-28) — Measured CI run` section with all eleven subsections (a) through (k) present under recognisable headings.
    - The per-job table accounts for every job the run reported plus `CI required`, and the number of conclusions collected is stated as an integer; a zero is recorded as failed collection.
    - All 12 `needs:` members appear by name in the recorded `alls-green` output, with `success`/`failure`/`skipped`/`cancelled` counts stated and summing to 12.
    - The prediction scorecard has one row per `needs:` member with predicted, actual and hit/miss, and a stated hit-rate fraction.
    - The five-column comparison includes round 4's run id and states the "N of 7" integer alongside round 3's 4.
    - The red-`needs:` count is stated as an integer, compared against 3, and the target-of-1 outcome is stated as hit or missed.
    - Every still-red check has its own root-cause subsection with a citation; `grep -c` for a bare "still red" with no adjacent citation returns 0.
    - The wall clock is stated in `NmNNs` form with an explicit comparison to 20m00s.
    - `deferred-items.md` carries a dated entry for every still-red lane.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^## Round 4' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md &amp;&amp; grep -q 'Prediction scorecard' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md &amp;&amp; grep -q 'D-39' .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</automated>
  </verify>
  <done>The Round 4 record is complete enough that a later verifier can re-derive every verdict from the run id, with a scored prediction and a cited root cause behind every red check.</done>
</task>

<task type="auto">
  <name>Task 3: Set GREEN-04 and GREEN-07 statuses strictly from the measured run, and update STATE</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md (Task 2's Round 4 record — the sole admissible basis for both statuses)
    - .planning/REQUIREMENTS.md (lines 14 and 17 — GREEN-04's and GREEN-07's current Pending entries and their round-3 inline notes, plus the status table rows at lines 133 and 136 that must agree with the prose)
    - .planning/STATE.md (the current-position block and `last_activity_desc` format)
    - .planning/phases/198-green-bringup/198-CONTEXT.md (D-01 admissibility; D-39/D-40/D-41 for the citations the notes must carry)
  </read_first>
  <action>
    Set both statuses from the run, never ahead of it.

    GREEN-04: mark Complete if and only if `Run test suite (current)` concluded exactly `success` on
    the Round 4 run. Cite the run id and the check's conclusion string in the inline note. If it did
    not, leave it Pending and rewrite the inline note to name the *current* cause with the new run id,
    explicitly distinguishing it from the round-3 demo-seed cause and from the already-closed
    search_path cause — the same three-way precision round 3's note achieved.

    GREEN-07: leave Pending. Rewrite its inline note to record the measured `CI required` conclusion,
    the red-`needs:` count with the members named, and the reason closure is structurally unavailable
    this round: `Tier A capture lane (byte-stable evidence)` is red and D-39 forbids its only remedy
    for this milestone. State explicitly that closure was NOT pursued by narrowing `needs:` and that
    `CI required`'s guarantee is unchanged. Record which of its clauses is met — the ≤20-minute clause
    with its measured figure — and which is not.

    Make the status-table rows at the bottom of `REQUIREMENTS.md` agree with the prose entries; a
    table row that disagrees with its prose is the exact traceability defect this phase exists to
    prevent.

    Update `.planning/STATE.md`'s current position and `last_activity_desc` to name the Round 4 run id
    and the measured outcome, including the red-`needs:` count.

    Do not mark GREEN-07 Complete on any basis. Do not mark either requirement Complete because a
    lane was deferred, decided, or declared out of scope — a decision to defer is not evidence of
    satisfaction.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s GREEN-04 entry cites the Round 4 run id, and its checkbox state matches the recorded `Run test suite (current)` conclusion exactly — `[x]` only if that conclusion is the literal string `success`.
    - `.planning/REQUIREMENTS.md`'s GREEN-07 entry is `[ ]` Pending, cites the Round 4 run id, names every red `needs:` member, states the measured wall clock against the ≤20-minute clause, and states that closure was not pursued by narrowing `needs:`.
    - GREEN-07's note names `Tier A capture lane (byte-stable evidence)` and D-39 as the structural blocker.
    - The status-table rows for GREEN-04 and GREEN-07 agree with their prose entries; a diff-free read of both locations shows the same verdict.
    - `.planning/STATE.md` names the Round 4 run id and the red-`needs:` count in `last_activity_desc`.
    - `git diff -- .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts` is empty — no gate was narrowed anywhere in this round.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'D-39' .planning/REQUIREMENTS.md &amp;&amp; test -z "$(git diff --name-only -- .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts)"</automated>
  </verify>
  <done>Both requirement statuses follow the measured run rather than lead it, GREEN-07's structural blocker is named with its decision citation, and the prose and the status table agree.</done>
</task>

</tasks>

<reversibility>
  <decision rating="reversible">Pushing a fresh measurement branch and opening a draft pull request is reversible — the branch can be deleted and the draft closed; nothing merges, and `mergeStateStatus` is recorded rather than acted on.</decision>
  <decision rating="one-way">Marking a requirement Complete is effectively one-way in the project record: downstream phases and the milestone audit read it as settled. Gated by the rule that the checkbox may change only on an exact `success` conclusion string from the measured run, never on local output and never on a deferral decision.</decision>
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell → GitHub API (`gh`) | The maintainer's credential pushes a branch, opens a pull request, and reads run logs |
| Local commits → public `origin` | Pushed content becomes public; `.planning/` is already public per D-30 |
| Measured run → requirement status | The run's conclusions are the sole admissible basis for two requirement verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-29-01 | Repudiation | requirement status verdict | high | mitigate | Statuses derive from a recorded run id and verbatim conclusion strings, re-queryable by any later reader; prohibitions forbid Complete-on-local-evidence and Complete-because-deferred |
| T-198-29-02 | Tampering | measured evidence | high | mitigate | No rerun, re-dispatch or selective retry is permitted; the `attempt` number is recorded and any value above 1 must be explained verbatim |
| T-198-29-03 | Spoofing | run selection | medium | mitigate | The run's `headSha` must equal the recorded local head SHA, and a fresh `ci/198-round4` branch with all three refs' SHAs recorded prevents an earlier round's evidence being mistaken for this one's |
| T-198-29-04 | Elevation of Privilege | branch protection and `needs:` | high | mitigate | Direct pushes to `main` are refused by the ruleset; the plan forbids bypass and force-push; empty diffs on `ci.yml`, the ruleset and `CONTRIBUTING.md` are acceptance criteria (D-42) |
| T-198-29-05 | Information Disclosure | pushed content and CI logs | medium | accept | `.planning/` is already tracked and public on `origin/main`; D-30 ratified publish-as-is and the D-28 credential audit gated the original push |
| T-198-29-06 | Repudiation | a green aggregate manufactured by a skipped lane | high | mitigate | `skipped` and `cancelled` members are counted separately and recorded as not-success, with the D-09 laundering hazard named explicitly |
| T-198-29-SC | Tampering | npm/pip/cargo/hex installs | high | mitigate | No package-manager install occurs; no dependency or lockfile is touched, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `gh run list --branch ci/198-round4 --limit 1 --json databaseId,conclusion,headSha` returns a completed run whose `headSha` matches the recorded local head.
- `198-CI-MEASUREMENT.md` contains a Round 4 section with the prediction, the per-job table, the scored prediction, the five-column comparison, the red-`needs:` count, a cited root cause per red check, the wall clock, the `CI required` conclusion string, and `mergeStateStatus`.
- `.planning/REQUIREMENTS.md` GREEN-04 and GREEN-07 cite the Round 4 run id and their prose agrees with the status table.
- `.planning/STATE.md` names the Round 4 run id and the red-`needs:` count.
- `git diff -- .github/workflows/ci.yml .github/rulesets/main.json CONTRIBUTING.md examples/threadline_phoenix/e2e/playwright.config.ts` is empty.
</verification>

<success_criteria>
Round 4's outcome is measured, scored against a prediction written beforehand, and recorded precisely
enough to be re-derived from the run id — with GREEN-04 closed if and only if the run says so, and
GREEN-07 left honestly Pending with its structural blocker named rather than routed around.
</success_criteria>

## Artifacts this phase produces

**Changed files**
- `.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md` — a Round 4 section: pre-push prediction, push record with all three `ci/198-*` ref SHAs, run id and head SHA, per-job table, verbatim `alls-green` output with member counts, prediction scorecard with hit rate, five-column baseline comparison, red-`needs:` count against the target of 1, a cited root cause per red check, wall-clock evaluation, `CI required` conclusion string, `mergeStateStatus`, headline-claim verdict, and re-run discipline.
- `.planning/REQUIREMENTS.md` — GREEN-04 and GREEN-07 statuses and inline notes set strictly from the measured run; status-table rows reconciled with the prose.
- `.planning/STATE.md` — current position and `last_activity_desc` naming the Round 4 run id and the red-`needs:` count.
- `.planning/phases/198-green-bringup/deferred-items.md` — a dated entry per still-red lane with its cause and owning decision.

**New remote refs**
- Branch `ci/198-round4` on `origin`, and its draft pull request against `main`, marked DO NOT MERGE.

**Explicitly not produced**
- No change to `.github/workflows/ci.yml`, `.github/rulesets/main.json`, `CONTRIBUTING.md`, `examples/threadline_phoenix/e2e/playwright.config.ts`, or `mix.exs`.
- No Tier A capture-lane work of any kind (D-39).
- No merge. The pull request is a measurement vehicle and stays a draft.

<output>
Create `.planning/phases/198-green-bringup/198-29-SUMMARY.md` when done.
</output>
