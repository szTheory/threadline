---
phase: 198-green-bringup
plan: 62
type: execute
wave: 11
depends_on: [198-61]
files_modified:
  - bin/verify-phase198-ref-disposition
  - test/threadline/phase198_ref_disposition_contract_test.exs
  - .planning/audits/198-summary-coverage-manifest.json
  - test/threadline/phase198_zero_human_uat_contract_test.exs
  - test/threadline/phase198_terminal_certification_contract_test.exs
  - .planning/audits/198-round13-terminal-certification.md
autonomous: true
gap_closure: true
gap_ids: [T-198-57-04]
requirements: [GREEN-04, GREEN-12]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Weak legacy receipt compatibility is available only to the exact immutable, completed canonical Round-11 JSON/Markdown pair; copied, aliased, mutated, incomplete, or future production inputs cannot select it."
    - "Every non-canonical production schema-v2 receipt is validated with the strict argv-array, force=false, ordered RFC3339 timestamps, zero exit status, before/after identity, and operation-specific one-object contract."
    - "Explicit fixture stages remain test-only, exercise the same strict receipt semantics, and cannot authorize or satisfy a production decision, authority, controls, post-target, or final stage."
    - "The exact canonical Round-11 production final command and branch-protection verifier pass against unchanged live state using read-only operations only."
    - "Phase-198 summary discovery audits exactly summaries 01-61 and recognizes Plan 62 as the sole mechanically validated non-recursive certification-summary exception."
    - "The terminal certificate records exactly six ordered, zero-exit receipts and uniquely binds every source to certified_head, certified_head:path blob identity, and SHA-256."
    - "Post-fix terminal state keeps exactly T-198-55-02 high/blocking/open/not accepted and T-198-55-03 medium/nonblocking/open/not accepted; T-198-57-04 is absent from open findings, receipt evidence is not reconstructed, and GREEN-07 remains accepted-Pending."
  artifacts:
    - "bin/verify-phase198-ref-disposition — canonical-identity-bound legacy compatibility and strict-default receipt validation"
    - "test/threadline/phase198_ref_disposition_contract_test.exs — adversarial production-path compatibility and strict-receipt tests"
    - ".planning/audits/198-summary-coverage-manifest.json — exact audited summaries 01-61 with Plan 62 as the sole terminal exception"
    - "test/threadline/phase198_zero_human_uat_contract_test.exs — exact summary boundary and mechanical coverage contract"
    - "test/threadline/phase198_terminal_certification_contract_test.exs — exact command, source, finding, and requirement-state contract"
    - ".planning/audits/198-round13-terminal-certification.md — immutable Plan-62 terminal reseal"
    - ".planning/phases/198-green-bringup/198-62-SUMMARY.md — sole non-recursive certification summary with machine-verifiable coverage"
  key_links:
    - "production receipt input -> canonical realpath pair + exact Git blob/SHA-256 + completed Round-11 state -> legacy validator; every failed predicate -> strict validator"
    - "strict receipt -> exact operation type -> literal argv array -> one authorized object/refspec -> force=false -> timestamps/exit -> before/after identity"
    - "summary manifest 01-61 -> exact-summary lane -> terminal source seal -> Plan-62 summary exception -> orchestrator-owned security and verification hooks"
  prohibitions:
    - {statement: "Plan 62 must not infer historical argv, close or accept T-198-55-02 or T-198-55-03, or promote GREEN-07.", status: pending, verification: test}
    - {statement: "Plan 62 performs no branch, tag, pull-request, ruleset, protection, workflow, release, or other external mutation.", status: pending, verification: test}
---

<objective>
Close T-198-57-04 by reducing the Round-11 legacy-receipt exception to its one immutable completed artifact and reseal Phase 198 at the next exact terminal boundary.

Purpose: preserve truthful readability of the completed Round-11 evidence without turning its missing historical fields into a schema-v2 production bypass, while keeping every future production receipt strict by default.
Output: a TDD-proven canonical compatibility predicate, adversarial production-path coverage, exact audited summaries 01-61 with Plan 62 as the sole non-recursive exception, and a six-command immutable terminal certificate ready for orchestrator-owned canonical hooks.

<assumption_delta_decision>Primary noun: legacy receipt compatibility. Decision: constrain — compatibility belongs to one canonical completed evidence identity, never to a mode bit, a caller-selected path, schema version alone, or the general production namespace.</assumption_delta_decision>

### Multi-source coverage audit

| Source | ID | Coverage | Plan | Status |
|---|---|---|---|---|
| GOAL | Phase 198 | Deterministic test and repository-hygiene controls remain green; the separately accepted origin/main shortfall is not restated as success. | 198-01..61, 198-62 | COVERED |
| REQ | GREEN-01..GREEN-06 | Existing completed plans retain their evidence; Plan 62 re-runs GREEN-04's exact-summary/focused/full lanes. | 198-01..61, 198-62 | COVERED |
| REQ | GREEN-07 | Preserve the maintainer-selected accepted-Pending disposition; no merge, push, CI narrowing, or promotion is planned. | 198-39, 198-62 | COVERED |
| REQ | GREEN-08..GREEN-11 | Existing completed plans remain authoritative; the live protection verifier is rerun without mutation. | 198-01..61, 198-62 | COVERED |
| REQ | GREEN-12 | Canonical Round-11 evidence remains readable while all other production receipts fail closed unless strict. | 198-53..61, 198-62 | COVERED |
| RESEARCH | Executable evidence and fail-closed boundaries | Real tests, immutable Git identities, exact-set validation, bounded live reads, and no inferred success are explicit. | 198-62 | COVERED |
| CONTEXT | D-01 | The fix is proved by real focused and full tests, not prose or local inference. | 198-62 Tasks 1-3 | COVERED |
| CONTEXT | D-39 | GREEN-07 and its protected lane membership remain accepted-Pending and unchanged. | 198-62 Tasks 1-3 | COVERED |
| CONTEXT | D-42 | No `ci-required` membership, ruleset, or CI Coverage contract change occurs; branch protection is read only. | 198-62 Tasks 1-3 | COVERED |

Deferred CONTEXT items remain excluded. No new dependency or external integration is introduced; discovery level is 0 because the work extends established repository-owned shell/ExUnit/certification patterns.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-61-SUMMARY.md
@.planning/audits/198-round11-ref-disposition.json
@.planning/audits/198-round11-ref-disposition.md
@.planning/audits/198-summary-coverage-manifest.json
@.planning/audits/198-round12-terminal-certification.md
@.planning/audits/198-round12-prohibition-resolution.json
@.planning/audits/198-round12-prohibition-resolution.md
@bin/verify-phase198-ref-disposition
@bin/verify-branch-protection
@test/threadline/phase198_ref_disposition_contract_test.exs
@test/threadline/phase198_zero_human_uat_contract_test.exs
@test/threadline/phase198_terminal_certification_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind legacy compatibility to the exact completed Round-11 identity</name>
  <files>bin/verify-phase198-ref-disposition, test/threadline/phase198_ref_disposition_contract_test.exs</files>
  <behavior>
    - "RED: a PATH-isolated invocation of an actual production lifecycle stage currently accepts a copied schema-v2 record carrying abstract legacy receipts because non-fixture mode alone selects the weak branch."
    - "The canonical production final accepts legacy receipts only when inventory realpath is exactly `.planning/audits/198-round11-ref-disposition.json`, decision realpath is exactly `.planning/audits/198-round11-ref-disposition.md`, the JSON/Markdown Git blob identities are respectively `a4fc711dca3b2e0779bd0a2abf4ed551e61e8c93` and `5b97c7873bd51a7aa43be1686b71a001f3c31225`, and their SHA-256 values are respectively `6b39204b95dcf1dbd42c6885bd4d0d0f6c861ef4d7286b18822edf6c721c6591` and `70589513acd68de861d74f928ec302a6b455ad0e5c548b3383fa723c50b0ef19`."
    - "The same exception also requires schema_version 2, round 11, the exact JSON/Markdown inventory-decision join, decision option/verbatim retire, execution.status complete, the exact completed target set and final live-state shape, and the exact 41-row legacy sequence/subject/target/tag/SHA/PR join."
    - "A byte-identical copy, a symlink/relative-path alias, one-byte mutation, substituted digest/blob, incomplete or altered execution state, changed receipt count/order/join, or spoofed non-fixture mode cannot select legacy validation."
    - "Every other production schema-v2 document, including a future/copy document with abstract receipts, must satisfy the strict validator's argv array, literal no-glob operands, force=false, ordered RFC3339 timestamps, exit_status=0, before/after identity, and operation-specific one-object semantics."
    - "A future production document with complete strict receipts passes the receipt boundary; force, bulk, mirror, wildcard, extra object/refspec, wrong command, malformed timestamp, nonzero exit, or invalid identity transition fails."
    - "Explicit fixture lifecycle stages use strict validation and remain incapable of satisfying any production stage."
  </behavior>
  <action>Start by adding adversarial tests that traverse the actual production decision/post-target/final dispatch with PATH-isolated read-only Git/GitHub observers; do not test a duplicate Elixir classifier. Capture the current copied-legacy acceptance as the required RED. Refactor `validate_receipt_shape_v2` into an exact canonical-completed-Round-11 predicate, a legacy validator reachable only when every predicate above passes, and a strict validator used for every other input in both production and explicit fixture stages. Resolve both supplied paths with a non-aliasing canonical-path check rooted at the repository; require the committed canonical path's exact Git blob plus exact byte digest for both JSON and Markdown, and then validate the completed state and all 41 legacy joins before taking the exception. A matching schema version, non-fixture stage, filename, copied bytes, caller-controlled environment, or mutable JSON field alone is never sufficient. If any identity/completion predicate fails, do not partially accept the legacy shape: apply the strict schema and reject missing fields. Preserve existing live-observation deadlines, explicit fixture boundaries, preservation-before-deletion ordering, and Markdown/JSON decision joins. Add production-path cases for canonical success, byte-identical copy rejection, alias rejection, one-byte mutation, digest/blob substitution, altered completion state, future abstract-record rejection, strict future-record success, and every strict destructive-operation mutation named above. Per D-01 the tests must show the pre-fix RED and post-fix GREEN; per D-39/D-42 they must not edit CI membership, scorecards, rulesets, protection, or documentation coverage.</action>
  <verify>
    <automated>mix test test/threadline/phase198_ref_disposition_contract_test.exs --only receipt_identity &amp;&amp; mix test test/threadline/phase198_ref_disposition_contract_test.exs</automated>
    <fails_when>The pre-fix copied-record RED is not observed; canonical completed Round-11 final stops reading its immutable legacy receipts; any copied/aliased/mutated/incomplete/future abstract production record selects compatibility; a strict future receipt fails; any malformed/bulk/force/nonzero/unverifiable strict receipt passes; fixtures weaken their receipt contract or cross into production authority; or the full ref-disposition contract regresses.</fails_when>
  </verify>
  <done>T-198-57-04's cause is closed: one immutable completed artifact may be read historically, while strict one-object receipts are mandatory for every other production input and every synthetic lifecycle receipt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Advance the exact terminal contract through Plan 61</name>
  <files>.planning/audits/198-summary-coverage-manifest.json, test/threadline/phase198_zero_human_uat_contract_test.exs, test/threadline/phase198_terminal_certification_contract_test.exs</files>
  <behavior>
    - "RED: the checked-in manifest and tests still audit summaries 01-60 and name Plan 61 as the terminal exception; assertions for audited 01-61 and sole exception 62 fail first."
    - "Final summary discovery requires every summary 01-61 exactly once, rejects missing/duplicate/malformed summary numbers, and permits only mechanically covered Plan 62 outside the recursive audited set."
    - "The terminal record requires exactly the six ordered command texts for exact-summary, focused, production final, branch protection, terminal-focused, and full-suite lanes, each with ordered RFC3339 timestamps, zero exit, and pass result."
    - "Every terminal source entry has exactly path, commit, blob, and sha256; commit equals the one certified_head, certified_head:path resolves to that blob, and Git bytes hash to sha256. Duplicate aliases, missing paths/objects, non-blobs, alternate ancestors, or coordinated commit/blob/digest substitution fail."
    - "The exact source set includes the summary manifest, summaries 56-61, Round-11 JSON/Markdown, Round-12 prohibition JSON/Markdown, and the pre-hook canonical SECURITY/VERIFICATION reports."
    - "Open findings are exactly T-198-55-02 high/blocking/open/not accepted/no reconstruction and T-198-55-03 medium/irrecoverable-below-threshold/nonblocking/open/not accepted/no reconstruction with the five missing receipt fields. T-198-57-04 is not open, and GREEN-07 is exactly accepted-Pending."
  </behavior>
  <action>First update the summary and terminal tests so they fail against the old 60/61 boundary and old Round-12 record path. Advance the manifest's audited final plan number to 61, terminal certification plan number to 62, and exact final-state number list through 61. Point the terminal contract at the net-new `.planning/audits/198-round13-terminal-certification.md`; never rewrite or delete the completed Round-12 certificate. Require the exact source set stated above and validate it as a mathematical set, including unique source paths and exact certified_head equality before resolving each `certified_head:path` blob and digest. Update the exact schema to `audited_summaries` 01-61 and `certification_summary` number 62 with `recursive: false`. Retain the same six command categories and exact command text, with the focused lane including the ref-disposition, prohibition-resolution, and zero-human-summary contracts; terminal-focused includes the terminal contract plus those three. Update open-state mutations to require the two residual findings only and explicitly reject an open T-198-57-04, any close/accept/reclassification/reconstruction of T-198-55-02 or T-198-55-03, any additional finding, or any GREEN-07 promotion. Ensure Plan 62's eventual summary can qualify only with `human_judgment: false` and named passing automated refs. Do not edit canonical SECURITY, VERIFICATION, VALIDATION, REQUIREMENTS, or external repository state.</action>
  <verify>
    <automated>PHASE198_SUMMARY_SET=final mix test test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; mix test test/threadline/phase198_terminal_certification_contract_test.exs</automated>
    <fails_when>The RED boundary is not observed; audited summaries differ from exactly 01-61; an exception other than mechanically covered Plan 62 is allowed; source keys are missing/extra/aliased/duplicated or not uniquely bound to certified_head; command text/order/result is loose; open findings differ from the exact two residual IDs/states; T-198-57-04 remains open in the terminal handoff; GREEN-07 changes; or tests depend on editing an orchestrator-owned report.</fails_when>
  </verify>
  <done>The repository mechanically defines one exact Plan-62 terminal boundary: summaries 01-61 audited, one non-recursive exception, one source authority, six command receipts, two residual open findings, and GREEN-07 accepted-Pending.</done>
</task>

<task type="auto">
  <name>Task 3: Reseal the six exact lanes without external mutation</name>
  <files>.planning/audits/198-round13-terminal-certification.md</files>
  <action>After Tasks 1 and 2 are committed, record that exact commit as certified_head and derive every source blob and SHA-256 from `certified_head:path`; no source may use another commit even when its bytes or blob match. Use the established temporary uncommitted bootstrap record only to break terminal-test recursion and only from commands already observed passing, then replace it with a strict final record. Run and record exactly six commands in this order: `PHASE198_SUMMARY_SET=final mix test test/threadline/phase198_zero_human_uat_contract_test.exs`; `mix test test/threadline/phase198_ref_disposition_contract_test.exs test/threadline/phase198_prohibition_resolution_contract_test.exs test/threadline/phase198_zero_human_uat_contract_test.exs`; `bin/verify-phase198-ref-disposition final --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md --register .planning/ARCHIVE-REGISTER.md`; `bin/verify-branch-protection`; `mix test test/threadline/phase198_terminal_certification_contract_test.exs test/threadline/phase198_ref_disposition_contract_test.exs test/threadline/phase198_prohibition_resolution_contract_test.exs test/threadline/phase198_zero_human_uat_contract_test.exs`; `mix test`. Persist actual RFC3339 UTC start/end times, exit status zero, and pass result for each observed command, then rerun the terminal-focused and full-suite commands against the strict final record. Record exactly the two residual open findings and exact GREEN-07 state from Task 2; the certificate may hand off T-198-57-04 as fixed by the named passing test but must not claim the canonical security or verification hooks ran. Leave SECURITY, VERIFICATION, VALIDATION, and REQUIREMENTS unchanged for the execute-phase orchestrator to update in canonical security-first/verification-second order. Do not run any write-side Git or GitHub operation.</action>
  <verify>
    <automated>PHASE198_SUMMARY_SET=final mix test test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; mix test test/threadline/phase198_ref_disposition_contract_test.exs test/threadline/phase198_prohibition_resolution_contract_test.exs test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; bin/verify-phase198-ref-disposition final --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md --register .planning/ARCHIVE-REGISTER.md &amp;&amp; bin/verify-branch-protection &amp;&amp; mix test test/threadline/phase198_terminal_certification_contract_test.exs test/threadline/phase198_ref_disposition_contract_test.exs test/threadline/phase198_prohibition_resolution_contract_test.exs test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; mix test &amp;&amp; mix test test/threadline/phase198_terminal_certification_contract_test.exs test/threadline/phase198_ref_disposition_contract_test.exs test/threadline/phase198_prohibition_resolution_contract_test.exs test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; mix test &amp;&amp; git diff --exit-code -- .planning/phases/198-green-bringup/198-SECURITY.md .planning/phases/198-green-bringup/198-VERIFICATION.md .planning/phases/198-green-bringup/198-VALIDATION.md .planning/REQUIREMENTS.md</automated>
    <fails_when>Any exact-summary, focused, production-final, branch-protection, terminal-focused, strict-final rerun, or full-suite command fails; a receipt is missing, extra, reordered, predicted, or fabricated; a source is not uniquely resolved from certified_head; canonical Round-11 legacy validation fails; the two residual open findings or GREEN-07 state change; an orchestrator-owned canonical report changes; or any external mutation occurs.</fails_when>
  </verify>
  <done>The strict Plan-62 terminal artifact validates from immutable Git objects, all six exact lanes and strict-final reruns are green, live repository controls pass read-only, and the remaining security/requirement state is exact and honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller-controlled production paths -> legacy compatibility | A path, mode, filename, or schema claim must not impersonate the canonical completed Round-11 artifact. |
| historical abstract receipts -> production mutation proof | Legacy readability cannot be promoted into proof of argv, force, time, exit, or identity fields that were never captured. |
| explicit fixtures -> production lifecycle | Synthetic state may falsify validation but cannot grant live production authority. |
| Git/GitHub observation -> terminal evidence | Read-only external state and command results must remain bounded, exact, and distinct from mutation authority. |
| plan executor -> canonical audit hooks | The executor certifies fix evidence; only orchestrator-owned hooks may rewrite canonical SECURITY/VERIFICATION verdicts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-62-01 | Spoofing | canonical legacy-artifact selector | high | mitigate | Require exact canonical realpaths, Git blobs, byte digests, JSON/Markdown join, and completed state before legacy validation. |
| T-198-62-02 | Tampering | schema-v2 receipts | high | mitigate | Strict validation is the default for every non-canonical input and enforces exact fields and one-object operation semantics. |
| T-198-62-03 | Repudiation | future production commands | high | mitigate | Require argv arrays, force=false, ordered timestamps, zero exit status, and before/after identities with adversarial production-path tests. |
| T-198-62-04 | Elevation of Privilege | fixture lifecycle | high | mitigate | Preserve explicit fixture-only stages and reject fixture adapters at every production authority stage. |
| T-198-62-05 | Tampering | terminal certificate sources | high | mitigate | Bind the exact unique source set to certified_head:path blobs and SHA-256 and reject alternate ancestors or coordinated substitutions. |
| T-198-62-06 | Repudiation | terminal finding state | high | mitigate | Exact-set tests retain the two residual open findings, close T-198-57-04 only from passing controls, and preserve GREEN-07 accepted-Pending. |
| T-198-62-07 | Denial of Service | live observation | medium | mitigate | Reuse bounded production observers and fail closed on deadline, transport, authentication, rate-limit, or schema errors. |
| T-198-62-SC | Tampering | package supply chain | low | accept | No dependency installation, manifest change, or package-manager operation is in scope. |
</threat_model>

<verification>
- Observe Task 1 RED on a copied legacy production record, then GREEN for the exact canonical identity and all adversarial/strict cases.
- Run the complete ref-disposition contract and require zero failures.
- Observe Task 2 RED on the old 60/61 summary boundary, then require exact audited summaries 01-61 and sole exception 62.
- Run the production final ref-disposition command against the canonical Round-11 JSON/Markdown pair; any live drift or compatibility failure stops execution with no fallback.
- Run `bin/verify-branch-protection`; any non-exact required context, classic-protection ambiguity, or observation error stops execution.
- Validate the strict final terminal record's exact sources, six commands, two residual findings, T-198-57-04 closure handoff, and GREEN-07 disposition.
- Run the terminal-inclusive focused lane and full `mix test` after final record persistence.
- Require empty scoped diffs for SECURITY, VERIFICATION, VALIDATION, and REQUIREMENTS so canonical hooks remain orchestrator-owned.
</verification>

<success_criteria>
- T-198-57-04 has a named passing control proving no general production legacy-receipt bypass remains.
- The immutable completed Round-11 JSON/Markdown remains readable at its exact canonical identity without reconstructing absent receipt fields.
- Every copied, mutated, incomplete, aliased, future-abstract, or spoofed production input fails strict receipt validation; a complete strict future receipt passes.
- Fixture stages remain isolated from production authority.
- Summary discovery audits exactly 01-61 and Plan 62 is the sole non-recursive mechanically covered exception.
- The new Round-13 certificate validates exactly six passing commands and uniquely sealed sources.
- Production final, branch protection, focused tests, terminal tests, and the full suite pass with no external mutation.
- T-198-55-02 and T-198-55-03 remain open and not accepted; T-198-57-04 is handed off closed; GREEN-07 remains accepted-Pending.
</success_criteria>

<output>
Create `.planning/phases/198-green-bringup/198-62-SUMMARY.md` when done. Its frontmatter must use mechanical coverage only (`human_judgment: false`) with named passing automated verification refs for the receipt-identity, exact-summary, production-final, branch-protection, terminal, and full-suite lanes. Plan 62 is the sole non-recursive terminal certification exception and must not claim the later canonical security or phase-verification hooks ran.
</output>
