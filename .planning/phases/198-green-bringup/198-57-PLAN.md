---
phase: 198-green-bringup
plan: 57
type: execute
wave: 6
depends_on: [198-55]
files_modified:
  - bin/verify-phase198-ref-disposition
  - test/threadline/phase198_ref_disposition_contract_test.exs
autonomous: true
gap_closure: true
requirements: [GREEN-12]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Fixture/data-only inputs can exercise dedicated test stages but can never satisfy production decision, authority, controls, post-target, or final stages."
    - "Every production authority or mutation-control stage independently re-reads target identities and protected controls from live Git/GitHub state within a bounded observation window."
    - "Decision validation rejects any non-null execution object or any receipt before authority can be granted."
    - "Every mutation receipt records an argv array, exact single-object target/refspec, force=false, timestamps, exit status, and before/after identities; operation-specific semantics reject batching and ambiguous operands."
  artifacts:
    - "bin/verify-phase198-ref-disposition — separated fixture and production trust paths with bounded live reads and strict receipts"
    - "test/threadline/phase198_ref_disposition_contract_test.exs — RED/GREEN bypass and argv mutation fixtures"
  key_links:
    - "production stage -> bounded live adapter -> independently observed identities/controls -> authority or mutation verdict"
    - "operation type -> exact argv schema -> one object/refspec -> before/after identity -> receipt ordering"
  prohibitions:
    - {statement: "No fixture environment variable or data-only adapter may make a production authority or mutation stage exit zero.", status: pending, verification: test}
---

<objective>
Close the round-11 ref-disposition trust-boundary vulnerabilities with fail-closed production semantics.

Purpose: the current validator lets fixture state stand in for live authority/controls and accepts abstract receipts that cannot prove exact one-object, non-force commands.
Output: a TDD-hardened validator and exhaustive contract tests covering T-198-52-01/02/04, T-198-53-05/06, T-198-54-02/04, and T-198-55-01/02/03/06.

<assumption_delta_decision>Primary noun: live authorization observation. Decision: promote — production stages trust only fresh subprocess observations; fixtures are a separate test interface.</assumption_delta_decision>

### Multi-source coverage audit

- GOAL/REQ: GREEN-12's preservation result remains intact while its enforcement is repaired; GREEN-07 remains Pending.
- CONTEXT: D-31/D-32 exact restoration identities remain authoritative; D-39/D-42 protected controls cannot be mutated or restated.
- RESEARCH/PATTERNS: fail closed, preserve immutable evidence, use exact argv arrays, and keep external observation bounded.
- VERIFICATION/SECURITY: every named blocking and non-blocking ref-disposition threat is implemented here or assigned to the historical-evidence Plans 58-59; no risk is accepted by default.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-53-PLAN.md
@.planning/phases/198-green-bringup/198-55-PLAN.md
@.planning/audits/198-round11-ref-disposition.json
@bin/verify-phase198-ref-disposition
@test/threadline/phase198_ref_disposition_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Separate fixture lifecycle proof from production live authority</name>
  <files>bin/verify-phase198-ref-disposition, test/threadline/phase198_ref_disposition_contract_test.exs</files>
  <read_first>
    - bin/verify-phase198-ref-disposition — `live_compare_v2`, decision, authority, controls, post-target, and final dispatch
    - test/threadline/phase198_ref_disposition_contract_test.exs — positive fixture lifecycle currently routed through production stage names
    - .planning/phases/198-green-bringup/198-SECURITY.md — exact audit results for T-198-52-01/04, T-198-53-05/06, T-198-54-02/04, T-198-55-01/06
    - .planning/audits/198-round11-ref-disposition.json — immutable completed evidence whose historical values must not be rewritten
  </read_first>
  <behavior>
    - Test 1: each production decision/authority/controls/post-target/final invocation fails if the fixture adapter variable is present or live reads are disabled
    - Test 2: dedicated fixture-only stage names still prove the full synthetic lifecycle without contacting GitHub
    - Test 3: decision fails when execution is any object or receipts are non-empty, including otherwise valid authorized content
    - Test 4: authority independently detects target SHA, PR state/head/base/head SHA, origin/main, PR 34, worktrees, required contexts, ruleset, and classic protection drift
    - Test 5: controls, post-target, and final each perform their own live protected-control observation instead of trusting a prior stage's fixture or cached result
    - Test 6: an unavailable or over-deadline GitHub observation fails closed with stage, operation, elapsed/deadline, and retryability fields and never falls back to artifact data
  </behavior>
  <action>Begin with failing bypass tests. Introduce an explicit test-only stage family for synthetic lifecycle validation and make every production decision/authority/controls/post-target/final stage reject the fixture adapter before any verdict. Tighten decision shape so it requires execution null and zero receipts. Factor live observation into a bounded adapter invoked independently by every production stage; enumerate exact allowed read-only Git and GitHub operations, capture structured failure diagnostics, apply a finite total deadline with bounded attempts, and return nonzero on timeout, authentication, rate-limit, schema, or transport failure. Production authority must freshly compare the target identity and all protected controls; later mutation stages must freshly compare protected controls and the relevant before/after live target state. Do not mutate the completed round-11 JSON/Markdown and do not convert availability failure into accepted risk.</action>
  <verify>
    <automated>mix test test/threadline/phase198_ref_disposition_contract_test.exs</automated>
    <fails_when>A production stage passes with fixture/data-only input, decision accepts execution/receipts, a protected-control mutation escapes any live stage, an observation can hang or use unbounded retries, or unavailable live state falls back to committed evidence.</fails_when>
  </verify>
  <done>Fixture-backed success is confined to an explicit test interface and production authority is freshly live-bound.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact argv-safe one-object receipts</name>
  <files>bin/verify-phase198-ref-disposition, test/threadline/phase198_ref_disposition_contract_test.exs</files>
  <read_first>
    - bin/verify-phase198-ref-disposition — current abstract `validate_receipt_shape_v2` and preservation ordering
    - test/threadline/phase198_ref_disposition_contract_test.exs — receipt generators and ordering mutations
    - .planning/audits/198-round11-ref-disposition.md — historical 41 receipts that lack argv and must remain immutable
    - .planning/phases/198-green-bringup/198-55-SUMMARY.md — documented rejected first push and exact-refspec retry; narration is context, not fabricated command proof
  </read_first>
  <behavior>
    - Test 1: every synthetic mutation receipt requires seq, type, target/subject, argv array, exact object/refspec, force=false, started_at, completed_at, exit_status, before, and after
    - Test 2: tag creation permits one annotated tag and one full SHA; tag push permits one source:destination tag refspec; branch deletion and PR close permit one exact identity
    - Test 3: force flags, wildcard operands, multiple refspecs/PRs, shell command strings, ambiguous shorthand, option injection, missing timestamps, nonzero exits, or mismatched before/after identities fail
    - Test 4: preserve-before-retire ordering and live peeled-object/register joins remain mandatory
  </behavior>
  <action>Define a strict operation-specific receipt schema for the dedicated fixture lifecycle and every future production mutation validation. Store argv as an array of literal operands rather than a shell string; require exactly one authorized object for tag creation, one exact tag source/destination refspec for push, one exact PR number for close, and one exact branch ref for deletion. Require an explicit false force field, RFC3339 start/end timestamps in order, zero exit status, and operation-specific before/after identities. Reject batch/all-tag/mirror/force semantics structurally, including disguised option ordering. Keep round-11 receipts readable only as historical legacy evidence and make the new round-12 resolution artifact state that they cannot be upgraded into argv proof retrospectively.</action>
  <verify>
    <automated>mix test test/threadline/phase198_ref_disposition_contract_test.exs &amp;&amp; bin/verify-phase198-ref-disposition inventory --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md</automated>
    <fails_when>Any malformed/multi-object/force receipt passes, receipt timing or identity transitions are optional, preservation ordering weakens, or backward-compatible read-only round-11 inventory validation fails.</fails_when>
  </verify>
  <done>Exact one-object argv semantics are mechanically enforceable without rewriting historical receipts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixture data -> production verdict | Test-controlled JSON must never establish live mutation authority. |
| Git/GitHub observations -> authority | Mutable hosted/local identity and controls must be fresh and bounded. |
| receipt data -> mutation claim | Abstract labels must not stand in for the exact command performed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-57-01 | Spoofing | production mode | critical | mitigate | Production stages reject fixture adapters and independently invoke bounded live observation. |
| T-198-57-02 | Tampering | decision state | high | mitigate | Decision requires null execution and empty receipts before authority. |
| T-198-57-03 | Elevation of Privilege | protected controls | critical | mitigate | Every live mutation stage re-reads main, PR34, contexts, ruleset, classic protection, and worktrees. |
| T-198-57-04 | Repudiation | mutation receipts | high | mitigate | Operation-specific argv arrays, exact one-object identities, timestamps, exit status, and before/after state. |
| T-198-57-05 | Denial of Service | live GitHub observation | medium | mitigate | Finite deadline/attempt budget, structured failure, nonzero exit, and no evidence fallback. |
| T-198-57-SC | Tampering | dependencies | high | mitigate | No package install or dependency/lockfile mutation occurs. |
</threat_model>

<verification>
- Run both task commands and `git diff --check`.
- The focused suite must contain a positive fixture lifecycle only under test-only stages and a negative case for every production stage with fixture mode.
- Security re-audit must map the implemented controls to the nine blocking findings and T-198-53-05 availability mitigation; T-198-55-03 remains separately open below threshold unless complete receipt evidence exists, and no manual verdict edit is allowed.
</verification>

<success_criteria>
- All nine blocking and two non-blocking validator findings have implemented controls or an explicit historical-evidence disposition path in Plans 58-59.
- Round-11 artifacts remain immutable.
- GREEN-07 and protected repository state remain unchanged.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-57-SUMMARY.md` when done.</output>
