---
phase: 198-green-bringup
plan: 65
type: execute
wave: 13
depends_on: [198-64]
files_modified:
  - .planning/audits/198-round15-security-authorization.txt
  - .planning/audits/198-round15-security-disposition.json
  - test/threadline/phase198_prohibition_resolution_contract_test.exs
autonomous: true
gap_closure: true
gap_ids: [T-198-55-02, T-198-64-01, T-198-64-02]
requirements: [GREEN-12]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The exact new authority names the attributable maintainer identity szTheory and supersedes only the T-198-55-02 disposition; Plan 63's decline and Plan 64's invalid placeholder attempt remain immutable history."
    - "The superseding JSON is decoded through a recursive duplicate-member rejection boundary before any object becomes a map; duplicate root and nested names fail in either member order."
    - "The decision time is derived from and bounded by immutable plan-origin and authorization-capture commit times, not trusted from free-form input or wall-clock prose."
    - "Complete Plan-63 history and the relevant Plan-64 plan, invalid record, summary, and rejecting security audit are pinned by exact path, commit, blob, and SHA-256."
    - "No historical argv/non-force evidence or mitigation is claimed; T-198-55-03 and T-198-62-SC remain open/not accepted and GREEN-07 remains accepted-Pending."
    - "Canonical security evaluates the superseding record before canonical phase verification."
  artifacts:
    - ".planning/audits/198-round15-security-authorization.txt — one-line exact attributable maintainer response"
    - ".planning/audits/198-round15-security-disposition.json — append-only superseding disposition with immutable authorization and history pins"
    - "test/threadline/phase198_prohibition_resolution_contract_test.exs — raw duplicate-aware decoder, exact-schema, time-bound, history-pin, and anti-fabrication contract"
  key_links:
    - "exact szTheory response -> authorization commit -> derived time/identity bounds -> superseding disposition -> duplicate-aware contract -> Plan-65 summary -> canonical security -> phase verification"
    - "supersedes history pins -> Plan63 plan+halted summary and Plan64 plan+invalid artifact+summary+security rejection -> no rewrite of prior artifacts"
---

<objective>
Replace the invalid placeholder attribution with a new append-only, attributable and duplicate-safe T-198-55-02 risk disposition.

Purpose: canonical security rejected Plan 64 because literal `YOUR_NAME` is not attributable and `Jason.decode!/1` collapses duplicate object members before exact-key validation. The maintainer has now explicitly supplied `accept-risk by szTheory: I accept the residual uncertainty that Plan 198-55’s exact historical argv and non-force method evidence was not retained.` Plan 65 captures that exact authority in new artifacts, closes the two implementation gaps, and preserves every prior decision record as history.

Output: one exact text authorization, one v2 superseding disposition, recursive duplicate-member rejection before map conversion, immutable history/time pins, and a summary routing canonical security before phase verification.

### Canonical authorization text contract

`.planning/audits/198-round15-security-authorization.txt` contains exactly one UTF-8 line plus one LF byte and no BOM:

`accept-risk by szTheory: I accept the residual uncertainty that Plan 198-55’s exact historical argv and non-force method evidence was not retained.`

### Fixed immutable history pins

The v2 disposition's `supersedes` array contains these six entries in this exact order and with no extra keys:

| kind | path | commit | blob | sha256 |
|---|---|---|---|---|
| plan63-plan | `.planning/phases/198-green-bringup/198-63-PLAN.md` | `a7f24af6ed19bfecd4942c624ef777b104974805` | `acc7506bb2e993c0935de6e7318b8443fd674acb` | `49280cb866ad36975bad5259731d38a1aad169df533fd6999e9d31ef8e246d18` |
| plan63-decline | `.planning/phases/198-green-bringup/198-63-SUMMARY.md` | `59d3c6985ef08b34f519b5d7587a60fc0ee76158` | `0c529f821e4880296bdabad4ab691fa6b1cc1eda` | `12fc1b775f7822b74bbc5d8648e15eb37ae5387dff726dded9ca6bc7e2e2c23c` |
| plan64-plan | `.planning/phases/198-green-bringup/198-64-PLAN.md` | `16afcfa17d873c9017e6b13197d909026174a00d` | `b720f6630fb97658d1e8cb46760c35535f94a893` | `c2e43295b36d2a34391768bd42fe74fbc1f1d95d71ebb9a7a7fd521a35a8e2aa` |
| plan64-invalid-disposition | `.planning/audits/198-round14-security-disposition.json` | `1760066a529e3028d9515cdf5a37ed43bbc07403` | `51fb65b2d2caaf34123b4b4090369129aed1ede6` | `6b48fd906c38a20e08e29c072a33e576d567ce61a55b853f10e3f9d4259898a9` |
| plan64-summary | `.planning/phases/198-green-bringup/198-64-SUMMARY.md` | `6f8d13c7f8d4f59782a9f6879b4ccc00dc21cc6c` | `3ffb89060ff753bd5555d80cb08143a92b22e2fc` | `76e9ed1735d3885114c7de09cdb721bac2ce34a5f9cc7d3696eb46d2ef0fe4da` |
| plan64-security-rejection | `.planning/phases/198-green-bringup/198-SECURITY.md` | `a25f90ad7a41097e638f495ee73aa904d4e50001` | `e5e89fedb1f948fa59b5eb5c05c5c795489d26e2` | `3e82abcfa4a5363c4fecd903c0b924db66ff11407a798c0e1c7615f48569fcb8` |

Each entry's JSON shape is exactly `{kind, path, commit, blob, sha256}`, all strings. Validation resolves `commit:path`, requires a blob, compares the blob object ID, hashes those Git object bytes, and compares SHA-256; current working-tree bytes are not a substitute.

### Canonical v2 disposition JSON contract

The executor writes exactly this object shape. Literal values, JSON types, array order, nested shape, and Unicode punctuation are normative. The four `AUTHORIZATION_*` tokens are not executor-chosen schema: Task 1 replaces them mechanically from the first commit containing the exact authorization text. `decided_at` and `decision_time_bounds.authorization_committed_at` both equal that commit's committer timestamp normalized to real UTC seconds. `decision_time_bounds.plan_origin_committed_at` is fixed below from the immutable Plan-65 origin commit.

```json
{
  "schema_version": "threadline.phase198.security-disposition.v2",
  "phase": "198",
  "threat_id": "T-198-55-02",
  "decision": "accept-risk",
  "verbatim": "accept-risk by szTheory: I accept the residual uncertainty that Plan 198-55’s exact historical argv and non-force method evidence was not retained.",
  "decided_by": "szTheory",
  "decided_at": "AUTHORIZATION_COMMITTED_AT_UTC_SECONDS",
  "decided_at_basis": "authorization-commit-committer-time",
  "historical_evidence_reconstructed": false,
  "rationale": "I accept the residual uncertainty that Plan 198-55’s exact historical argv and non-force method evidence was not retained.",
  "accepted_scope": "The residual uncertainty for T-198-55-02 caused solely by the unavailable exact historical argv and non-force method evidence for completed Plan 198-55 mutations.",
  "excluded_threats": ["T-198-55-03", "T-198-62-SC"],
  "green_07": {"status": "accepted-Pending", "changed": false},
  "authorization_source": {
    "path": ".planning/audits/198-round15-security-authorization.txt",
    "commit": "AUTHORIZATION_COMMIT",
    "blob": "AUTHORIZATION_BLOB",
    "sha256": "AUTHORIZATION_SHA256"
  },
  "decision_time_bounds": {
    "plan_origin_commit": "bbdcebf2e9266115326df0b852345fd1fcf170bb",
    "plan_origin_committed_at": "2026-09-10T21:22:56Z",
    "authorization_commit": "AUTHORIZATION_COMMIT",
    "authorization_committed_at": "AUTHORIZATION_COMMITTED_AT_UTC_SECONDS"
  },
  "supersedes": [
    {"kind": "plan63-plan", "path": ".planning/phases/198-green-bringup/198-63-PLAN.md", "commit": "a7f24af6ed19bfecd4942c624ef777b104974805", "blob": "acc7506bb2e993c0935de6e7318b8443fd674acb", "sha256": "49280cb866ad36975bad5259731d38a1aad169df533fd6999e9d31ef8e246d18"},
    {"kind": "plan63-decline", "path": ".planning/phases/198-green-bringup/198-63-SUMMARY.md", "commit": "59d3c6985ef08b34f519b5d7587a60fc0ee76158", "blob": "0c529f821e4880296bdabad4ab691fa6b1cc1eda", "sha256": "12fc1b775f7822b74bbc5d8648e15eb37ae5387dff726dded9ca6bc7e2e2c23c"},
    {"kind": "plan64-plan", "path": ".planning/phases/198-green-bringup/198-64-PLAN.md", "commit": "16afcfa17d873c9017e6b13197d909026174a00d", "blob": "b720f6630fb97658d1e8cb46760c35535f94a893", "sha256": "c2e43295b36d2a34391768bd42fe74fbc1f1d95d71ebb9a7a7fd521a35a8e2aa"},
    {"kind": "plan64-invalid-disposition", "path": ".planning/audits/198-round14-security-disposition.json", "commit": "1760066a529e3028d9515cdf5a37ed43bbc07403", "blob": "51fb65b2d2caaf34123b4b4090369129aed1ede6", "sha256": "6b48fd906c38a20e08e29c072a33e576d567ce61a55b853f10e3f9d4259898a9"},
    {"kind": "plan64-summary", "path": ".planning/phases/198-green-bringup/198-64-SUMMARY.md", "commit": "6f8d13c7f8d4f59782a9f6879b4ccc00dc21cc6c", "blob": "3ffb89060ff753bd5555d80cb08143a92b22e2fc", "sha256": "76e9ed1735d3885114c7de09cdb721bac2ce34a5f9cc7d3696eb46d2ef0fe4da"},
    {"kind": "plan64-security-rejection", "path": ".planning/phases/198-green-bringup/198-SECURITY.md", "commit": "a25f90ad7a41097e638f495ee73aa904d4e50001", "blob": "e5e89fedb1f948fa59b5eb5c05c5c795489d26e2", "sha256": "3e82abcfa4a5363c4fecd903c0b924db66ff11407a798c0e1c7615f48569fcb8"}
  ]
}
```

The exact root key set is `{schema_version, phase, threat_id, decision, verbatim, decided_by, decided_at, decided_at_basis, historical_evidence_reconstructed, rationale, accepted_scope, excluded_threats, green_07, authorization_source, decision_time_bounds, supersedes}`. Nested key sets are exactly those shown. No additional key is permitted at any depth.

### Multi-source coverage audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Green bringup goal and honest terminal state | 01-65 | COVERED | Engineering outcomes stay fixed; Plan 65 repairs only the canonical security blockers. |
| REQ | GREEN-01..GREEN-06 | Measurement, test, form-policy, and bounded-CI outcomes | 01-62 | COVERED | Already verified; unchanged. |
| REQ | GREEN-07 | Exact-main CI outcome | 39, 61-65 | COVERED | Remains accepted-Pending. |
| REQ | GREEN-08..GREEN-11 | Protection, critic, publish, and flake outcomes | 01-62 | COVERED | Already verified; unchanged. |
| REQ | GREEN-12 | Preservation and truthful repository-hygiene evidence | 53-65 | COVERED | Plan 65 repairs disposition attribution and parsing only. |
| RESEARCH | — | Research skipped in gap mode | — | EXCLUDED | No external dependency or architecture choice. |
| CONTEXT | D-01..D-30 | Locked implementation decisions | 01-62 | COVERED | Already shipped and verified. |
| CONTEXT | D-31..D-38 | Archive, preservation, and measurement decisions | 01-65 | COVERED | Immutable history is pinned rather than rewritten. |
| CONTEXT | D-39..D-42 | Terminal lane and GREEN-07 decisions | 39-65 | COVERED | No promotion or external-state mutation. |
| SECURITY | T-198-55-02 | Attributable narrow risk acceptance | 65 | COVERED | Exact szTheory authority; no evidence/mitigation claim. |
| SECURITY | T-198-64-01 | Placeholder attribution | 65 | COVERED | New append-only record names szTheory and pins invalid history. |
| SECURITY | T-198-64-02 | Duplicate-member ambiguity | 65 | COVERED | Recursive ordered-object validation precedes map conversion. |
| SECURITY | T-198-55-03, T-198-62-SC | Nonblocking open findings | 65 | COVERED | Explicitly excluded and remain open/not accepted. |
| UAT | — | Zero UAT gaps | 46-62 | COVERED | No UAT task is invented. |

All 12 phase requirements and all 42 decisions remain covered across Plans 01-65. No deferred item is absorbed.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-63-PLAN.md
@.planning/phases/198-green-bringup/198-63-SUMMARY.md
@.planning/phases/198-green-bringup/198-64-PLAN.md
@.planning/phases/198-green-bringup/198-64-SUMMARY.md
@.planning/audits/198-round14-security-disposition.json
@test/threadline/phase198_prohibition_resolution_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Capture szTheory authority and produce the duplicate-safe superseding record</name>
  <files>.planning/audits/198-round15-security-authorization.txt, .planning/audits/198-round15-security-disposition.json, test/threadline/phase198_prohibition_resolution_contract_test.exs</files>
  <behavior>
    - "The exact one-line authorization text is committed unchanged and resolves through its first containing commit to a blob and SHA-256."
    - "The v2 disposition matches the complete canonical schema above after mechanical token substitution only."
    - "Raw JSON is decoded with `Jason.decode(raw, objects: :ordered_objects)`; a recursive walk rejects repeated member names in every `Jason.OrderedObject` before any conversion to ordinary maps."
    - "The exact response is attributable to szTheory; the accepted scope remains T-198-55-02 residual uncertainty only."
    - "Plan63 and Plan64 artifacts remain byte-unchanged and are pinned through immutable Git objects."
  </behavior>
  <reversibility rating="one-way">This append-only record durably captures an attributed security-risk decision. The exact szTheory authorization in this invocation already approves that one-way audit entry, so no duplicate checkpoint is inserted.</reversibility>
  <action>First extend the focused test so it expects the new authorization and v2 disposition, requires the fixed history pins, and calls a raw duplicate-aware decoder; run it and observe RED because the new artifacts and decoder do not exist. Create the authorization text with exactly the bytes specified above and commit it alone. Resolve its first containing commit, `commit:path` blob, SHA-256, and normalized UTC-seconds committer time. Replace only the four AUTHORIZATION tokens in the normative specimen with those mechanically derived values; the Plan-65 origin is exactly commit `bbdcebf2e9266115326df0b852345fd1fcf170bb` at normalized committer time `2026-09-10T21:22:56Z`, and both must resolve from Git rather than being accepted as prose. Implement the test-only decoder by asking Jason for ordered objects, recursively checking each ordered member list for duplicates before conversion, then converting only the validated ordered tree to maps/lists/scalars. Do not use `Jason.decode!/1` or any map-returning decode before duplicate validation. Write the v2 JSON and make the exact end-to-end test green. Verify every fixed history pin from Git object bytes. Do not modify or delete the Plan63/64 records, Round-14 disposition, SECURITY, VERIFICATION, VALIDATION, REQUIREMENTS, workflows, rulesets, refs, tags, branches, or external state.</action>
  <verify>
    <automated>mix test test/threadline/phase198_prohibition_resolution_contract_test.exs</automated>
    <fails_when>The RED observation is absent; authorization bytes differ; a placeholder signer survives; the JSON differs from the canonical contract after mechanical substitution; any duplicate is map-normalized before rejection; any immutable history/time pin fails; or protected history/canonical state changes.</fails_when>
  </verify>
  <done>The attributable szTheory decision flows from exact text through an immutable authorization commit into one exact, duplicate-safe, history-pinned v2 disposition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust duplicate, time, history, scope, and anti-fabrication boundaries</name>
  <files>test/threadline/phase198_prohibition_resolution_contract_test.exs</files>
  <behavior>
    - "For every root and nested object, duplicate names fail before map conversion when the malicious member appears first or last."
    - "Root `decision`, `decided_by`, and `historical_evidence_reconstructed` duplicates and nested `green_07.status`, authorization_source.commit, decision_time_bounds.authorization_commit, and supersedes-entry commit duplicates are explicit fixtures in both orders."
    - "`decided_at` equals the normalized authorization-commit committer time; plan-origin time is not after authorization time; authorization commit/path/blob/digest and both bound commits resolve exactly."
    - "Every fixed history entry rejects path, commit, blob, digest, kind, order, omission, addition, and current-working-tree substitution."
    - "T-198-55-03 and T-198-62-SC remain excluded/open/not accepted; GREEN-07 stays accepted-Pending; evidence, mitigation, attestation, and canonical verdict fields fail."
  </behavior>
  <action>Add raw-string fixtures that insert every named duplicate at the root and nested locations in both value orders, including one duplicate hidden in each object inside the `supersedes` array. Assert the duplicate-specific error is returned before canonical map/schema validation. Add invalid UTF-8, malformed JSON, wrong-type, missing/extra key, reordered/duplicated exclusion, widened scope, placeholder signer, ASCII-apostrophe substitution, timestamp mismatch, reversed bounds, authorization/history Git identity mutation, and evidence/mitigation/attestation/verdict fixtures. Require the full exact supersedes array as an ordered list and validate every historical blob/digest from `commit:path`. Re-run the focused suite with a nonzero test count. Commit only the test expansion. The executor then writes a status-complete Plan-65 summary claiming mechanical coverage only and returns to the orchestrator; canonical `$gsd-secure-phase 198` runs first, and canonical phase verification runs only afterward.</action>
  <verify>
    <automated>mix test test/threadline/phase198_prohibition_resolution_contract_test.exs</automated>
    <fails_when>Either duplicate order reaches map/schema validation; any recursive object is skipped; a time or Git identity is trusted from prose; prior history can be rewritten/substituted; scope broadens; an excluded finding or GREEN-07 changes; zero tests run; or canonical workflows execute out of order.</fails_when>
  </verify>
  <done>The superseding disposition has a recursive pre-map duplicate barrier, trustworthy commit-derived time and source bounds, immutable superseded-history pins, and no authority beyond T-198-55-02 risk acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| raw maintainer response -> authorization commit | Exact attributable authority must become immutable without normalization. |
| raw JSON -> decoded object | Duplicate names must be rejected recursively before lossy map conversion. |
| Git object database -> time/history pins | Commit, blob, bytes, digest, and time bounds must be derived rather than asserted. |
| local disposition -> canonical security | The record supplies decision input but cannot self-author a security verdict. |

## STRIDE Threat Register — ASVS L1, block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-65-01 | Spoofing | maintainer attribution | high | mitigate | Exact szTheory text, immutable authorization commit, and exact disposition attribution. |
| T-198-65-02 | Tampering | JSON decoding | high | mitigate | Ordered-object raw decode and recursive duplicate rejection before maps, with both-order root/nested fixtures. |
| T-198-65-03 | Repudiation | decision time | high | mitigate | Derive decision time from immutable authorization commit and bound it after the fixed plan-origin commit. |
| T-198-65-04 | Tampering | superseded history | high | mitigate | Pin complete Plan63 and relevant Plan64 history by exact commit:path blob and SHA-256. |
| T-198-65-05 | Elevation of Privilege | acceptance scope | high | mitigate | Exact schema, fixed scope/exclusions, anti-verdict fields, and canonical-security-first ordering. |
| T-198-65-06 | Information Disclosure | authorization/rationale | medium | mitigate | Persist only the exact supplied non-secret response and no environment or credential material. |
</threat_model>

<verification>
- Observe a focused RED before either new artifact/decoder exists and GREEN after the exact v2 path is implemented.
- Require raw ordered-object decoding and recursive duplicate rejection before map conversion; test both member orders at root, nested objects, and objects inside arrays.
- Resolve the exact Plan65 origin, authorization source, and six fixed history pins through Git; verify blob types, IDs, SHA-256, ordering, and commit-derived UTC seconds.
- Confirm Plan63 and Plan64 history plus SECURITY/VERIFICATION/VALIDATION/REQUIREMENTS remain unchanged during execution.
- Create `198-65-SUMMARY.md` with `status: complete` and automated `human_judgment: false` coverage only; do not claim canonical closure.
- Route canonical security first. Run canonical phase verification only after the resulting security verdict.
</verification>

<success_criteria>
- `szTheory` replaces the rejected placeholder through a new append-only record, without rewriting prior history.
- Duplicate JSON members cannot be collapsed into an apparently valid map at any depth or member order.
- Decision time and authorization identity are mechanically bound to immutable commits, and all prior decision history is pinned by Git identity/digest.
- Acceptance remains limited to T-198-55-02 residual uncertainty and claims no reconstructed historical evidence or mitigation.
- T-198-55-03 and T-198-62-SC remain open/not accepted; GREEN-07 remains accepted-Pending.
- Canonical security owns verdict changes and precedes canonical phase verification.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-65-SUMMARY.md` after both tasks pass. The summary records exact commits/test counts and mechanical coverage only; it must not rewrite Plans 63/64, claim a security verdict, or imply historical argv/non-force evidence exists. Route `$gsd-secure-phase 198`, then canonical phase verification.</output>
