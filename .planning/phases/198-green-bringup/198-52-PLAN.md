---
phase: 198-green-bringup
plan: 52
type: execute
wave: 3
depends_on: [198-51]
files_modified:
  - .planning/ARCHIVE-REGISTER.md
  - .planning/audits/198-round10-ref-disposition.md
  - .planning/audits/198-round10-ref-disposition.json
autonomous: true
gap_closure: true
requirements: [GREEN-12]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Only a verbatim recorded `retire` selection whose inventory digest and exact pinned branch/PR subjects pass the repository-owned authority validator authorizes changes to the three named Phase-198 measurement refs and PRs."
    - "Before deletion, each exact retired tip is preserved by an annotated `archive/<branch>` tag locally, on origin, and in the tracked archive register with a restore command."
    - "After retirement, none of the three local branches, remote branch refs, or open PR states remains; Plan 52 snapshots the current Phase-199 branch/upstream tip at execution start as a within-plan no-mutation baseline, while PR #34 remains a stable external control."
    - "One worktree remains and no unique unpreserved commit is discarded."
  artifacts:
    - ".planning/ARCHIVE-REGISTER.md — three new immutable Phase-198 measurement-ref rows"
    - "198-round10-ref-disposition.md — post-action receipts and final GREEN-12 predicate"
    - "198-round10-ref-disposition.json — machine-readable per-target receipts, stable external-control post-state, and execution-local active-tip baselines"
    - "bin/verify-phase198-ref-disposition — Plan-51 repository-owned authority, row/tag, stable-control, execution-baseline, and final stale-set validator"
  key_links:
    - "recorded retire decision + unchanged pinned SHA -> annotated tag -> remote tag -> register -> branch/PR retirement"
  prohibitions:
    - {statement: "Do not use `git push --mirror`, `git push --tags`, force update, rebase, squash, branch switch, or ruleset/protection mutation.", status: unverified, flagged: true}
    - {statement: "Do not delete a branch until its exact tip is recoverable from the verified local and remote annotated archive tag.", status: unverified, flagged: true}
---

<objective>
Execute the explicitly authorized, preservation-first retirement of the three stale Phase-198 measurement refs.

Purpose: close GREEN-12's current branch-hygiene gap without touching the active Phase-199 work or silently discarding any object.

Output: durable archive tags/register rows, closed stale PRs, retired local/remote branch refs, and a post-action receipt.

<assumption_delta_decision>Primary noun: immutable archive tag. Decision: promote — after retirement the annotated archive tag and tracked register become the restoration authority; the mutable branch is removed.</assumption_delta_decision>

### Artifacts this phase produces

- New tag paths: `archive/ci/198-gap-closure`, `archive/ci/198-round5`, `archive/ci/198-round6`.
- Three new `.planning/ARCHIVE-REGISTER.md` rows plus Markdown/JSON post-action receipt fields validated by `bin/verify-phase198-ref-disposition`.
- No new code, dependency, schema, UI, main commit landing, or ruleset/protection change.

### Spec-less probe accounting

- GREEN-12 adjacency/empty/ordering rows are enforced by exact subject comparison, create/verify-before-delete ordering, and final absence checks.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-51-SUMMARY.md
@.planning/audits/198-round10-ref-disposition.md
@.planning/ARCHIVE-REGISTER.md
@.planning/phases/198-green-bringup/198-39-DECISION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Preserve one authorized branch end-to-end before retiring it</name>
  <precondition>`bin/verify-phase198-ref-disposition authority --inventory .planning/audits/198-round10-ref-disposition.json --decision .planning/audits/198-round10-ref-disposition.md --target ci/198-gap-closure` exits zero, proving a verbatim `retire` decision bound to the unchanged inventory digest, exact three-target set, exact pinned local/remote/PR subjects, and stable external controls; it does not compare the active branch tip with Plan 51's pre-commit observation.</precondition>
  <files>.planning/ARCHIVE-REGISTER.md, .planning/audits/198-round10-ref-disposition.md, .planning/audits/198-round10-ref-disposition.json</files>
  <read_first>
    - .planning/audits/198-round10-ref-disposition.md — authorization and pinned subject for ci/198-gap-closure / PR #29
    - .planning/ARCHIVE-REGISTER.md — D-31 exact tag-message and row fields
    - .planning/phases/198-green-bringup/198-51-SUMMARY.md — selected option and exclusions
    - .planning/phases/198-green-bringup/198-39-DECISION.md — main/ruleset/PR #34 non-authority boundary
    - bin/verify-phase198-ref-disposition — run `authority` immediately before mutation and `post-target` after every mutation step
  </read_first>
  <action>Run the repository-owned `authority` preflight immediately before the first mutation. It must parse the verbatim decision, verify its inventory SHA-256, require the exact three pinned subjects and `retire` verb, compare ci/198-gap-closure local/remote full SHAs plus PR #29 state/head/base/head SHA to the inventory, require the live Phase-198 ref universe to equal the three authorized targets at Plan 52 start, and compare PR #34, origin/main, worktree, ruleset/protection, and required-context digests to the stable external controls; halt closed on any mismatch. Immediately after authority passes, snapshot the active branch name, full tip SHA, upstream name, and upstream full SHA as Plan 52's within-task no-mutation baseline; do not compare these values to Plan 51's provenance snapshot and do not treat them as mutation authority. Create `archive/ci/198-gap-closure` as an annotated tag at the exact preserved object using the D-31 message fields, append its exact-SHA register row, push only that tag ref, and use the validator to parse the register row and compare its SHA with both the local peeled object and the remote peeled tag object. After preservation validates, close PR #29, delete only its exact remote branch ref, and delete only its exact local branch with non-force deletion. After each of tag creation, tag push/register update, PR close, remote deletion, and local deletion, run the `controls` stage so stable external controls and the Plan-52 active-tip baseline remain equal and append the completed-step receipt; after the target sequence, run `post-target` to require the exact authorized target transition and complete preservation/retirement validation. Never switch branches.</action>
  <reversibility rating="costly">Branch and PR state can be recreated from the durable tag, but restoration requires coordinated local/remote ref and PR work.</reversibility>
  <verify>
    <automated>bin/verify-phase198-ref-disposition post-target --inventory .planning/audits/198-round10-ref-disposition.json --decision .planning/audits/198-round10-ref-disposition.md --register .planning/ARCHIVE-REGISTER.md --target ci/198-gap-closure</automated>
    <fails_when>The verbatim decision, inventory digest, exact subject set, pinned head/base/full SHAs, or stable external controls do not authorize retirement; a receipt step is absent or out of order; local tag, remote peeled tag, and parsed register-row SHA differ; target ref/PR retirement is incomplete; the Plan-52 active branch/upstream changes during the task's external mutation sequence; or any PR #34, origin/main, ruleset/protection, required-context, or worktree control drifts.</fails_when>
  </verify>
  <acceptance_criteria>
    - One complete preservation-to-retirement path is proven before expanding to the other targets.
    - The tag message and register carry SHA/base/ancestry/diffstat/date/requirement/recommendation/rationale/restore/pointer fields.
    - The validator binds authority to the exact target packet and stable external controls, snapshots current branch/upstream at Plan 52 start solely as a within-task no-mutation baseline, and proves that baseline plus PR #34, origin/main, worktree, ruleset/protection, and required-context digests unchanged after every external mutation step.
  </acceptance_criteria>
  <done>The first stale measurement branch is retired with two-copy immutable recovery and a tracked receipt.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the remaining two authorized refs and prove final repository hygiene</name>
  <precondition>`bin/verify-phase198-ref-disposition post-target --inventory .planning/audits/198-round10-ref-disposition.json --decision .planning/audits/198-round10-ref-disposition.md --register .planning/ARCHIVE-REGISTER.md --target ci/198-gap-closure` passes, then separate `authority` checks for ci/198-round5 and ci/198-round6 bind the same verbatim `retire` decision and unchanged stable external controls to each still-pinned local/remote/PR subject.</precondition>
  <files>.planning/ARCHIVE-REGISTER.md, .planning/audits/198-round10-ref-disposition.md, .planning/audits/198-round10-ref-disposition.json</files>
  <read_first>
    - .planning/audits/198-round10-ref-disposition.md — pinned subjects for ci/198-round5/#32 and ci/198-round6/#33 plus Task-1 receipt
    - .planning/ARCHIVE-REGISTER.md — verified Task-1 row and tag convention
    - .planning/phases/198-green-bringup/198-51-SUMMARY.md — exact decision scope
    - bin/verify-phase198-ref-disposition — per-target authority/post-target checks and complete final-set derivation
  </read_first>
  <action>At Task 2 start, account for the executor's expected Task-1 artifact commit by snapshotting the then-current active branch/upstream tip as a fresh within-task no-mutation baseline; never require equality with the Plan-51 observation or pre-Task-1 Plan-52 tip. For each remaining target independently, run `authority`, then repeat the tag/create-register/push/verify-before-close-and-delete sequence from Task 1, invoking `controls` after each mutation, `post-target` after each completed target, and appending structured receipts. Never batch tag pushes or deletions. Finish with the validator's `final` stage before the Task-2 artifact commit: parse all three register rows and compare each exact row SHA to its local and remote peeled annotated-tag object; derive the complete local and remote `ci/198-*` branch sets from live ref enumeration and require both sets empty rather than checking only the three known literals; require target PRs closed and exactly one worktree; compare PR #34 including full head SHA, origin/main SHA, ruleset/protection canonical digest, and required-context digest against the original stable external controls; and compare active branch/upstream only against Task 2's execution-start baseline.</action>
  <reversibility rating="costly">All deleted refs are recoverable, but restoring three hosted/local branches and PR workflow state is coordinated work.</reversibility>
  <verify>
    <automated>bin/verify-phase198-ref-disposition final --inventory .planning/audits/198-round10-ref-disposition.json --decision .planning/audits/198-round10-ref-disposition.md --register .planning/ARCHIVE-REGISTER.md</automated>
    <fails_when>Any complete derived local or remote `ci/198-*` set is non-empty, a target PR remains open, any parsed register SHA differs from its local or remote peeled annotated tag, receipt ordering is incomplete, worktree count differs from one, active branch/upstream changes during Task 2's external mutation sequence, or any pinned PR #34, origin/main, ruleset/protection, or required-context stable control drifts.</fails_when>
  </verify>
  <acceptance_criteria>
    - The complete derived local and remote `ci/198-*` sets are empty and the three pinned PRs are closed; no wildcard/bulk destructive command is used.
    - Exactly three verified archive tags/rows preserve their former tips and restoration instructions.
    - GREEN-12's one-worktree/no-stale-Phase-198-branch predicate is true.
    - GREEN-07 remains Pending per D-39; PR #34 and origin/main are unchanged.
  </acceptance_criteria>
  <done>The actionable Round-10 repository-hygiene gap is closed without broadening authority to main or active Phase-199 work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| recorded decision -> external ref mutation | Authorization must bind exact targets and unchanged subjects. |
| mutable branch -> immutable recovery object | Deletion is safe only after local/remote annotated tags and register agree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-52-01 | Spoofing | stale authorization | high | mitigate | Repository-owned preflight parses verbatim `retire`, binds the inventory digest and exact targets, and re-reads full SHAs plus PR head/base/state before each mutation. |
| T-198-52-02 | Tampering | wrong ref deletion | critical | mitigate | Exact literal ref names, no globs/bulk flags/force, per-target verification before next target. |
| T-198-52-03 | Repudiation | archive provenance | high | mitigate | Annotated D-31 messages, tracked register rows, remote peeled-object checks, and receipts. |
| T-198-52-04 | Elevation of privilege | main/protection mutation | critical | mitigate | Validator binds authority to stable external controls, then compares the Plan-52-start active branch/upstream baseline plus PR #34/full head SHA, origin/main, worktree, ruleset/protection, and required-context digests after every external mutation. |
| T-198-52-05 | Denial of service | lost branch work | high | mitigate | Verify two-copy archive recovery before branch deletion; use non-force local deletion. |
</threat_model>

<verification>
- Run each task's exact checks plus `git diff --check`.
- Run `bin/verify-branch-protection` read-only; it fails when its result differs from the pinned protection/ruleset and required-context controls, and it does not authorize editing protection.
- Run the final ref-disposition validator; it fails when any parsed register-row SHA differs from either peeled tag object or either complete derived local/remote `ci/198-*` set is non-empty.
- Confirm git diff contains only the register, disposition receipt, and normal Plan-52 summary; expected executor artifact commits are outside the active-tip comparison windows and never become mutation authority.
</verification>

<success_criteria>
- The three Round-10 stale measurement branches have durable archive objects and no remaining local/remote/PR mutable refs.
- One worktree remains.
- Active Phase-199 and exact-main/protection state are untouched.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-52-SUMMARY.md` when done. If Plan 51 selected `preserve` or `abort`, do not execute this plan; report its precondition unmet without mutation.</output>
