---
phase: 198-green-bringup
plan: 31
type: execute
wave: 2
gap_closure: true
depends_on:
  - 198-30
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-prove-mobile.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts
  - .planning/audits/198-round5-playwright.md
autonomous: true
requirements: []

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The two `/Expired|File unavailable/` locator rows that plan 198-25's own copy fix broke resolve again, and they resolve against the canonical product literal `\"Export expired\"` rather than against a restored stale literal — the fix moves the test toward the product, never the product back toward the test."
    - "The cause of `operator-responsive-mobile-first.spec.ts:577:5` is **established from the run-33253587315 artifacts and from product source before any fix is written**, and the written diagnosis names the specific product element and the specific reason it does not resolve at phone width. A speculative fix is not written."
    - "If the cause of `operator-responsive-mobile-first.spec.ts:577:5` cannot be established from the available evidence, the plan halts honestly with the evidence gap named, rather than shipping a guess or loosening the locator."
    - "The `operator-nav-shell` landmark contract the phase-175 comment already claims is actually asserted, not merely described (WR-08)."
    - "The actor-page assertion asserts both the actor id and the actor type, is strict-mode safe, and does not interpolate a seeded value raw into a CSS attribute selector (WR-09)."
    - "The `<details>` expansion in the mobile find spec is idempotent and asserts the resulting open state, so a future `<details open>` default fails with a legible message rather than an opaque visibility timeout (WR-11)."
    - "No Playwright assertion is deleted, no `test.skip` / `.skip(` / `xit(` / `it.todo` is added, no `maxDiffPixelRatio` is loosened, no `maxFailures` or project list is changed, and `playwright.config.ts` is byte-unchanged."
    - statement: "Ordering edge: where this plan's local unbounded Playwright measurement disagrees with CI's `maxFailures`-capped run on which tests fail, both populations are recorded side by side with their identities, because the two sample different populations and the newer figure does not silently replace the older."
      verification: explicit
    - statement: "Empty edge: if `discoverMatrixRoutes` returns zero routes, or a locator resolves zero elements where the diagnosis expects at least one, that is recorded as failed collection rather than as a pass."
      verification: backstop
  artifacts:
    - ".planning/audits/198-round5-playwright.md — a written diagnosis for `operator-responsive-mobile-first.spec.ts:577:5` citing the run-33253587315 artifact and the product source line, plus a red-then-green teeth proof for each row fixed"
    - "examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts and operator-prove-mobile.spec.ts — `/Expired|File unavailable/` re-anchored to the canonical `\"Export expired\"` literal"
    - "examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts — the phone-viewport row-history assertion fixed at its established cause"
  key_links:
    - "plan 198-25's `\"Expired\"` → `\"Export expired\"` product copy fix → the capital-E `/Expired/` regex no longer matches → two CI-contributing Playwright rows red → this plan re-anchors the locators to the canonical literal → `Example app browser E2E (Playwright)` loses two of its five red rows"
    - "`operator-responsive-mobile-first.spec.ts:577:5` cause established → fix at cause → a third red row removed → the lane's remaining red is exactly the two D-39-forbidden `operator-stress.spec.ts` `page.*` baseline diffs"
  prohibitions:
    - statement: "A broken locator must not be repaired by restoring a stale product literal or by loosening the matcher until it passes. The canonical, spec-tested product copy is the source of truth and the test moves toward it."
      status: kept
      verification: flagged-unverified
    - statement: "A failure whose cause is not established must not receive a speculative fix presented as a cause fix. Establish the cause from evidence, or halt and record the evidence gap."
      status: kept
      verification: flagged-unverified
    - statement: "The two `operator-stress.spec.ts` `page.*` ledger-baseline screenshot rows must not be touched. Their only remedy is `page.*` scorecard regeneration, which D-39 forbids for this entire milestone; they are red by construction, not by defect."
      status: kept
      verification: flagged-unverified
---

<objective>
Repair the Playwright lane's *fixable* red rows from CI run `33253587315`, at their causes.

Round 4 left three actionable Playwright rows. Two of them this phase caused itself — plan 198-25's
correct product-copy fix (`"Expired"` → the canonical `"Export expired"`) broke two locators whose
regex is capital-`E` `/Expired|File unavailable/` at `operator-accessibility.spec.ts:611` and
`operator-prove-mobile.spec.ts:60`. The third,
`operator-responsive-mobile-first.spec.ts:577:5`, is an un-inventoried CI-only discovery whose cause
`198-CI-MEASUREMENT.md` records, correctly, as **not established**. This plan establishes it before
fixing it.

Two further rows on that run — `operator-stress.spec.ts:277:5` `page.home.happy` and
`page.timeline.empty`, dark 1024px — are explicitly **out of scope and untouchable**. Their only
remedy is regenerating committed `page.*` ledger baselines, which D-39 forbids for the whole of
milestone v1.41. They are red by construction. Nothing in this plan may narrow, retarget, retolerance,
or regenerate them.

The plan also closes three REVIEW.md warnings that live in these same files and are cheap to fix while
they are open: **WR-08** (a dropped landmark assertion whose comment still claims it), **WR-09** (an
actor assertion that lost its type check and can trip strict mode), and **WR-11** (an order-dependent
`<details>` toggle with no state assertion).

Purpose: reduce `Example app browser E2E (Playwright)`'s red rows to exactly the D-39-forbidden set.
Output: three fixed rows, three closed warnings, and a written diagnosis on disk.

**Flagged assumptions (spec-less probe fallback §A).** This plan claims no requirement id — it repairs
round-4 code-review and regression findings, the established convention for plans 198-13…198-29. The
edge-probe lift for GREEN-04 and GREEN-07 is authored in plans 198-30 and 198-37; the 20 items
belonging to the ten already-Complete requirements (including the four `unclassified` rows for
GREEN-01, GREEN-05, GREEN-09, GREEN-10, which remain `unresolved`) are carried by the closed plans
198-01…198-12 and enumerated in plan 198-37. Recorded, not dropped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/REVIEW.md
@.planning/audits/198-round4-playwright.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor the two /Expired/ locators to the canonical "Export expired" literal (self-caused regression from 198-25)</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts, examples/threadline_phoenix/e2e/tests/operator-prove-mobile.spec.ts, .planning/audits/198-round5-playwright.md</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts lines 595-620 (the `exportJobs` block; the failing locator is at :611, the assertion at :612)
    - examples/threadline_phoenix/e2e/tests/operator-prove-mobile.spec.ts lines 50-70 (the failing locator at :60)
    - lib/threadline/operator_surface/presentation.ex around line 340 (`export_action_label/2`, canonical since Phase 137)
    - lib/threadline/operator_surface/live/export_status_live.ex lines 476-493 (`export_job_status_label/1` — the function that actually renders, returning `"Export expired"` and `"File unavailable"`)
    - test/threadline/operator_surface/copy_contract_test.exs lines 443-463 (the Phase-186 lock listing `["Queued", "Processing", "Failed", "Export expired", "File unavailable"]`)
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md Round 4, per-failure causes 1 and 2
  </read_first>
  <precondition>The example app's Playwright browsers are installed and the demo seed is runnable locally (`mix verify.example_browser` has been run at least once on this machine). If not, install per CONTRIBUTING.md before starting; do not change `playwright.config.ts` to work around a missing browser.</precondition>
  <action>
<!-- planner-discipline-allow: Expired|File unavailable -->
Change the regex at `operator-accessibility.spec.ts:611` and at `operator-prove-mobile.spec.ts:60`
from `/Expired|File unavailable/` to match the literals the product actually renders:
`"Export expired"` and `"File unavailable"`. Prefer an explicit alternation of the two full literals
over a case-insensitive or partial-word match — a partial match like `/expired/i` would have
tolerated the very drift plan 198-25 fixed, and this phase's whole point is that a locator loose
enough to survive a copy change is a locator that cannot detect one.

Before editing, confirm from `export_status_live.ex:476-493` which literal is genuinely reachable on
each surface, and state that in the audit entry. Do **not** restore `"Expired"` anywhere, and do not
edit `presentation.ex`, `export_status_live.ex`, or `copy_contract_test.exs` from this plan — the
product copy is canonical and its ownership belongs to plan 198-34.

Note in the audit entry that the rendered vocabulary on the `:preparing` and `:needs_attention`
branches is itself under review as CR-01 and is owned by plan 198-34 (wave 3), which runs after this
plan and may change `"Queued"` / `"Processing"` / `"Failed"`. Do not pre-empt that decision here; fix
only the two `/Expired/` rows.

Record a red-then-green teeth proof for each row in `.planning/audits/198-round5-playwright.md`:
the verbatim failing locator and error from run `33253587315`, the product line that supplies the
literal, and the passing run afterwards.
  </action>
  <reversibility rating="reversible">Two locator literals in e2e specs; no product change, revertible in one commit.</reversibility>
  <verify>
    <automated>cd examples/threadline_phoenix &amp;&amp; npx playwright test e2e/tests/operator-accessibility.spec.ts e2e/tests/operator-prove-mobile.spec.ts --project=desktop-chromium</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Expired|File unavailable" examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts examples/threadline_phoenix/e2e/tests/operator-prove-mobile.spec.ts` returns `0` for both files — the stale capital-`E` alternation is gone.
    - `grep -c "Export expired" examples/threadline_phoenix/e2e/tests/operator-accessibility.spec.ts` returns at least `1`, and the same for `operator-prove-mobile.spec.ts`.
    - `git diff --stat -- lib/threadline/operator_surface/ test/threadline/operator_surface/` is empty — the product and its copy contract were not moved toward the test.
    - The two named tests (`operator-accessibility.spec.ts:565:3` and `operator-prove-mobile.spec.ts:38:3`) pass by name in the verify command's output.
    - `.planning/audits/198-round5-playwright.md` contains the verbatim run-33253587315 error text for both rows and a cited product source line for the literal each now matches.
  </acceptance_criteria>
  <done>Both self-caused regression rows pass by name, anchored to the canonical product literal, with a red-then-green proof on disk and no product file touched.</done>
</task>

<task type="auto">
  <name>Task 2: Establish the cause of operator-responsive-mobile-first.spec.ts:577:5 from evidence, then fix at cause — or halt honestly</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts, .planning/audits/198-round5-playwright.md</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts lines 460-482 (`assertRowHistory`, the failing exact-heading assertion at :475) and lines 570-591 (the matrix test at :577, `discoverMatrixRoutes` at :581, the `test.step` at :584-589)
    - the `viewports` array and `discoverMatrixRoutes` definitions in the same file (find with `grep -n "const viewports\|function discoverMatrixRoutes" examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts`)
    - lib/threadline/operator_surface/live/row_history_live.ex lines 55-70 (`title="Row history"` — the standalone route's heading)
    - lib/threadline/operator_surface/live/row_history_component.ex lines 70-90 (`Row history: <table> / <short_id>` — the drawer panel's heading, which is NOT an exact `"Row history"` match)
    - lib/threadline/operator_surface/live/timeline_live.ex around line 467 (the `Row history` link/affordance)
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md Round 4, per-failure cause 3 (the verbatim locator and the honest "cause not established" record)
  </read_first>
  <action>
**Diagnosis before fix. This task is a diagnosis task that may produce a fix; it is not a fix task.**

Establish, from evidence, why `getByRole('heading', { name: 'Row history', exact: true })` does not
resolve at phone width within 15s on the `phone` viewport of the responsive matrix, while the same
assertion passes on wider viewports. Concrete leads to check, each of which must be confirmed or
eliminated against product source rather than assumed:

1. Which surface the phone matrix actually lands on for the row-history route — the standalone
   `row_history_live.ex` page (whose `title="Row history"` would satisfy an exact match) or the
   drawer panel from `row_history_component.ex:83` (whose text is `Row history: <table> / <id>`,
   which an `exact: true` name match cannot satisfy). If `discoverMatrixRoutes` resolves a different
   path at phone width than at desktop width, that difference is the cause and the diagnosis must
   name the resolving line.
2. Whether the heading exists in the DOM but is not exposed as a `heading` role at phone width
   (a responsive template branch rendering a different element).
3. Whether the row-history affordance the matrix clicks to reach the surface is hidden or collapsed
   at phone width, so the surface is never opened at all.

Download or read the run-`33253587315` Playwright artifact for this row (`gh run view 33253587315`
and the uploaded failure artifact) and quote its verbatim error, DOM snapshot excerpt, and the
resolved route path into `.planning/audits/198-round5-playwright.md`.

Then, and only then, fix at the established cause. The fix must move the test toward the product
contract the product genuinely offers at phone width. Do **not** loosen `exact: true` to a substring
match as a first move — if the correct assertion is against the drawer panel's `Row history: ...`
heading, assert that heading's full shape (prefix plus the table name), not a bare substring, so the
assertion still detects a copy change.

**Halt clause.** If the cause cannot be established from the run artifact plus product source, do not
write a fix. Record the evidence gap in `.planning/audits/198-round5-playwright.md` and in the
SUMMARY, naming exactly what evidence would settle it, and leave the row red. A speculative fix
presented as a cause fix is the unmeasured attribution this phase exists to prevent.
  </action>
  <reversibility rating="reversible">A locator or assertion in one e2e spec; revertible in one commit. The diagnosis artifact is additive.</reversibility>
  <verify>
    <automated>cd examples/threadline_phoenix &amp;&amp; npx playwright test e2e/tests/operator-responsive-mobile-first.spec.ts --project=desktop-chromium --grep "keeps every operator route usable"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audits/198-round5-playwright.md` contains a section for `operator-responsive-mobile-first.spec.ts:577:5` holding: the verbatim run-33253587315 locator and error, the resolved route path at phone width, and a named product source file:line that explains the non-resolution — or, in the halt case, an explicit "cause not established" record naming the missing evidence.
    - If a fix was written: the `phone` viewport variant of `keeps every operator route usable without root horizontal overflow` passes by name in the verify command's output.
    - `git diff -- examples/threadline_phoenix/e2e/playwright.config.ts` is empty; `grep -c "test.skip\|\.skip(\|xit(\|it.todo" examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts` returns `0`.
    - The diff for this file removes no `expect(` call: `git diff -U0 -- examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts | grep -c '^-.*expect('` returns `0` unless each removed line is replaced by a strictly stronger assertion named in the SUMMARY.
  </acceptance_criteria>
  <done>The row's cause is on disk with citations, and either a cause fix lands and the phone-viewport test passes by name, or the plan halts with the evidence gap named and the row left honestly red.</done>
</task>

<task type="auto">
  <name>Task 3: Close WR-08, WR-09 and WR-11 — restore the landmark assertion, the actor type check, and an idempotent details expansion</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts, examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts, examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts</files>
  <read_first>
    - .planning/phases/198-green-bringup/REVIEW.md sections WR-08, WR-09, WR-11 (each carries a concrete target state)
    - examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts lines 85-100 (the `operator-nav-shell` block whose comment claims a `<nav>` landmark that nothing asserts)
    - lib/threadline/operator_surface/live/components/surface_header.ex lines 50-65 (the actual element and its `aria-label`)
    - examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts lines 130-150 (the `.tl-secondary-ref[title=...]` actor assertion)
    - examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts lines 105-120 (the `<details>` toggle)
    - lib/threadline/operator_surface/live/coverage_live.ex around line 197 (the `<details>` element and its default open state)
  </read_first>
  <action>
**WR-08** — `operator-phase-175-uat.spec.ts`: the shell's tag assertion was correctly moved onto the
inner `.tl-shell-nav__disclosure`, but nothing now asserts anything about `operator-nav-shell` itself
beyond visibility, while the in-file comment still claims it "is now a `<nav>` landmark". Assert the
contract the comment already describes: the element's `tagName` and its `aria-label`. Read the exact
`aria-label` string from `surface_header.ex` rather than trusting the review's paraphrase, and cite
the source line in a comment. Keep the existing `.tl-shell-nav__disclosure` `DETAILS` assertion.

**WR-09** — `operator-screenshots.spec.ts`: three defects in one replacement. (a) The actor **type**
assertion was lost — the previous `Actor: user / ${leavingAgentId}` text check covered id *and* type;
restore a type assertion alongside the id. (b) Add `.first()` to the `.tl-secondary-ref[title=...]`
locator: `UI.ref` renders in multiple places on the actor detail surface, so a duplicate render throws
a strict-mode violation rather than passing, and the surrounding file already uses `.first()` for
exactly this reason. (c) `leavingAgentId` is interpolated raw into a CSS attribute selector — quote it
with `JSON.stringify` so a value containing a quote or backslash fails legibly instead of producing an
invalid selector. Do not weaken the id assertion while restoring the type assertion; keep both.

**WR-11** — `operator-find-mobile.spec.ts`: the `<details>` expansion is order-dependent. Make it
idempotent — read the element's `open` property, click the `<summary>` only when it is closed, then
assert the resulting open state before asserting the revealed content. Also assert the `<summary>`'s
text so a renamed affordance fails with a named cause rather than an opaque visibility timeout. Cite
`coverage_live.ex`'s element line in a comment so a future default change is traceable.

All three are strict additions or strict strengthenings. Do not remove any existing `expect(` call in
these three files.
  </action>
  <reversibility rating="reversible">Assertion additions in three e2e specs; no product change.</reversibility>
  <verify>
    <automated>cd examples/threadline_phoenix &amp;&amp; npx playwright test e2e/tests/operator-phase-175-uat.spec.ts e2e/tests/operator-screenshots.spec.ts e2e/tests/operator-find-mobile.spec.ts --project=desktop-chromium --project=mobile-chromium</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'toBe("NAV")' examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts` returns at least `1`, and the file also asserts the `aria-label` value read from `surface_header.ex`.
    - `grep -c 'toBe("DETAILS")' examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts` returns at least `1` — the pre-existing disclosure assertion survives.
    - `grep -c "JSON.stringify(leavingAgentId)" examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts` returns at least `1`, and the same block contains both a `.first()` call and an assertion covering the actor **type**.
    - `grep -c 'toHaveAttribute("open"' examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts` returns at least `1`.
    - `git diff -U0 -- examples/threadline_phoenix/e2e/tests/operator-phase-175-uat.spec.ts examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts | grep -c '^-.*expect('` returns `0` — no assertion removed.
    - The verify command exits 0 on both projects.
  </acceptance_criteria>
  <done>All three warnings are closed by strictly-added assertions, no existing assertion is removed, and the three specs pass on both CI projects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser → example app HTTP surface | The e2e suite drives a real browser against the demo app; the demo seed supplies all data, none of it attacker-controlled. |
| Seeded data → CSS attribute selector | `leavingAgentId` flows from seeded data into a `[title="..."]` selector string (WR-09c). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-31-01 | Tampering | Locator string built by raw interpolation of a seeded id | low | mitigate | Task 3 quotes the value with `JSON.stringify` before embedding it in the CSS attribute selector, so a quote or backslash produces a legible failure rather than a silently different selector. This is test-harness robustness, not a product injection path — the value never reaches the application. |
| T-198-31-02 | Repudiation | A fix landed without an established cause | medium | mitigate | Task 2 requires a written, citation-backed diagnosis in `.planning/audits/198-round5-playwright.md` before any fix, and carries an explicit halt clause. An unattributed fix would leave no record of *why* the row went green — precisely the laundering vector this phase exists to close. |
| T-198-31-03 | Information Disclosure | Playwright failure artifacts uploaded from CI | low | accept | Artifacts contain demo-fiction data only (fictional orgs `acme` / `globex` / `offboarded-co`); the demo seed carries no real credentials or PII. Accepted with that rationale rather than mitigated. |
| T-198-31-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package is installed, added, or upgraded by this plan; `package.json` and lockfiles are outside `files_modified`. |

**Proportionality statement (ASVS L1, block on `high`).** This plan edits browser-test assertions and
writes a diagnosis document. It introduces no product code, no new endpoint, no authentication or
authorization change, and no dependency. There is **no `high` threat in this plan's change surface**,
and none is invented to populate the table. The one genuinely load-bearing row is T-198-31-02, which
is a process-integrity threat rather than a technical one, and it is mitigated by the diagnosis-first
structure of Task 2.
</threat_model>

<verification>
- `cd examples/threadline_phoenix && npx playwright test --project=desktop-chromium --project=mobile-chromium` run unbounded locally (`maxFailures: 0`, `process.env.CI` unset); the full pass/fail/skip counts recorded verbatim in the SUMMARY, beside round 4's CI-capped figures, with an explicit note that the two populations are not nested and that a delta between them is not by itself evidence of progress.
- `git diff --stat -- examples/threadline_phoenix/e2e/playwright.config.ts .github/ CONTRIBUTING.md .planning/rulesets/ '*.png' .planning/scorecards/` is empty.
- `git diff --stat -- examples/threadline_phoenix/e2e/tests/operator-stress.spec.ts` is empty — the two D-39-forbidden `page.*` baseline rows were not touched.
- `.planning/audits/198-round5-playwright.md` exists and carries one section per row addressed, each with a verbatim run-33253587315 error and a cited product source line.
</verification>

<success_criteria>
1. Both `/Expired/` rows pass by name against the canonical `"Export expired"` literal, with no product file edited.
2. `operator-responsive-mobile-first.spec.ts:577:5` has an established, cited cause on disk — and either a cause fix that passes by name, or an honest halt naming the missing evidence.
3. WR-08, WR-09 and WR-11 are closed by strictly-added assertions.
4. No assertion is removed, no skip is added, `playwright.config.ts` is byte-unchanged, and `operator-stress.spec.ts` is untouched.
5. The local unbounded measurement is recorded beside CI's capped figures with the non-nested-population caveat stated.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location |
|---|---|---|
| `.planning/audits/198-round5-playwright.md` | new file — round-5 Playwright diagnosis ledger | `.planning/audits/` |
| `/Export expired|File unavailable/` export-status locator | changed locator literal | `operator-accessibility.spec.ts`, `operator-prove-mobile.spec.ts` |
| `operator-nav-shell` `NAV` tagName + `aria-label` assertions | new assertions | `operator-phase-175-uat.spec.ts` |
| strict-mode-safe, `JSON.stringify`-quoted actor-ref locator + actor-type assertion | changed/new assertions | `operator-screenshots.spec.ts` |
| idempotent `<details>` open helper + `toHaveAttribute("open")` assertion | new assertions | `operator-find-mobile.spec.ts` |
| row-history phone-viewport assertion | changed assertion (conditional on Task 2's diagnosis) | `operator-responsive-mobile-first.spec.ts` |

No new CLI flags, config keys, product routes, or library functions.

<output>
Create `.planning/phases/198-green-bringup/198-31-SUMMARY.md` when done.
</output>
