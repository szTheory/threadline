---
phase: 198-green-bringup
plan: 61
type: execute
wave: 10
depends_on: [198-60]
files_modified:
  - .planning/audits/198-summary-coverage-manifest.json
  - .planning/audits/198-round12-terminal-certification.md
  - bin/verify-phase198-ref-disposition
  - test/threadline/phase198_zero_human_uat_contract_test.exs
  - test/threadline/phase198_terminal_certification_contract_test.exs
  - test/threadline/phase198_ref_disposition_contract_test.exs
autonomous: true
gap_closure: true
requirements: [GREEN-04, GREEN-12]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The exact Phase-198 summary scheme audits summaries 01-60 and recognizes Plan 61 as the sole mechanically validated, non-recursive certification-summary exception."
    - "Terminal certification resolves every sealed pre-audit source from a recorded ancestor commit and blob identity, so later orchestrator-owned SECURITY/VERIFICATION edits do not invalidate the seal while any recorded commit, blob, path, or digest substitution fails closed."
    - "The production classic-protection observer communicates explicit absent, present, and error outcomes: HTTP 404 is absent, a successful response is present, and HTTP 403/429/5xx or transport failure cannot be decoded as either state."
    - "The real production final ref-disposition command and bin/verify-branch-protection both pass against the current unchanged live state, with no external mutation."
    - "GREEN-04's exact-summary, focused, and full test lanes pass after the final certificate is resealed."
    - "Security remains blocked by T-198-55-02 because cannot-attest supplied no historical command-method proof; T-198-55-03 remains open below threshold; neither finding is accepted, and GREEN-07 remains accepted-Pending."
  artifacts:
    - ".planning/audits/198-summary-coverage-manifest.json — exact audited 01-60 namespace and sole Plan-61 certification exception"
    - "test/threadline/phase198_terminal_certification_contract_test.exs — immutable historical-blob identity validator and adversarial fixtures"
    - "bin/verify-phase198-ref-disposition — explicit classic-protection observation contract consumed fail-closed"
    - "test/threadline/phase198_ref_disposition_contract_test.exs — production-adapter fixtures for absence, presence, and observation errors"
    - ".planning/audits/198-round12-terminal-certification.md — resealed command receipts and immutable pre-audit source identities"
    - ".planning/phases/198-green-bringup/198-61-SUMMARY.md — sole non-recursive certification summary with mechanical coverage"
  key_links:
    - "certificate source entry -> recorded ancestor commit:path -> git blob identity -> git show bytes -> SHA-256 comparison"
    - "gh classic-protection response -> bounded observer explicit state -> protected-control digest consumer"
    - "audited summaries 01-60 -> focused/live/full certification -> Plan-61 summary -> security hook -> phase-verification hook"
  prohibitions:
    - {statement: "Plan 61 must not close or accept T-198-55-02 or T-198-55-03, reconstruct missing historical receipts, or promote GREEN-07.", status: pending, verification: test}
    - {statement: "Plan 61 performs no branch, tag, pull-request, ruleset, protection, workflow, or other external mutation.", status: pending, verification: test}
---

<objective>
Close the two fixable Round-12 regressions and reseal Phase 198 at a stable terminal boundary.

Purpose: restore GREEN-04's required test lane and make the production ref-disposition validator correctly recognize the unchanged live protection state without laundering either unresolved historical security finding.
Output: immutable commit/blob-backed terminal source identities, an explicit classic-protection adapter contract, exact audited summaries 01-60 with Plan 61 as the sole non-recursive exception, and a fresh terminal certificate ready for orchestrator-owned canonical hooks.

<assumption_delta_decision>Primary noun: Phase-198 terminal evidence identity. Decision: promote — a pre-audit source is identified by an immutable Git commit/blob pair and content digest, not by mutable current working-tree bytes.</assumption_delta_decision>

### Multi-source coverage audit

- GOAL: the deterministic test and repository-hygiene control portions of the Phase-198 goal are covered; origin/main CI remains outside this plan under the recorded GREEN-07 disposition.
- REQ: GREEN-04 is covered by the summary/certificate/full-suite repair; GREEN-12 is covered by the corrected read-only live-control verifier. GREEN-07 is explicitly excluded from implementation and remains accepted-Pending.
- RESEARCH/PATTERNS: executable evidence, immutable Git identities, bounded live observation, fail-closed parsing, and no weakening of required checks are covered.
- CONTEXT: D-01's real test requirement, D-39's GREEN-07 disposition, and D-42's no-unreconciled-protection-change interlock are preserved; no deferred item is implemented.
- VERIFICATION/SECURITY: both fixable Round-12 gaps are covered. The separate historical-proof gap T-198-55-02 and the below-threshold receipt gap T-198-55-03 deliberately remain open and not accepted under the user's explicit scope.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-SECURITY.md
@.planning/phases/198-green-bringup/198-VALIDATION.md
@.planning/phases/198-green-bringup/198-60-SUMMARY.md
@.planning/audits/198-summary-coverage-manifest.json
@.planning/audits/198-round12-terminal-certification.md
@.planning/audits/198-round12-prohibition-resolution.json
@bin/verify-phase198-ref-disposition
@bin/verify-branch-protection
@test/threadline/phase198_zero_human_uat_contract_test.exs
@test/threadline/phase198_terminal_certification_contract_test.exs
@test/threadline/phase198_ref_disposition_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Move the exact terminal boundary to immutable Git source identities</name>
  <files>.planning/audits/198-summary-coverage-manifest.json, test/threadline/phase198_zero_human_uat_contract_test.exs, test/threadline/phase198_terminal_certification_contract_test.exs</files>
  <behavior>
    - "RED: the checked-in manifest/test still name audited summaries 01-59 and Plan 60 as the exception, so new assertions for audited 01-60 and sole exception 61 fail before implementation."
    - "A certificate source entry is valid only when its recorded commit is a real ancestor, commit:path resolves to the recorded blob object, and SHA-256 of git show commit:path equals the recorded digest."
    - "Changing current SECURITY.md or VERIFICATION.md bytes after the recorded commit does not affect validation of the sealed historical blobs."
    - "Substituting the commit, blob object, path, or digest; naming a missing object; or resolving a non-blob object fails closed in adversarial fixtures."
  </behavior>
  <action>First add and run failing summary-boundary and tagged immutable-source-identity tests. Update the summary manifest and exact-set contract to audit 01 through 60 and treat 61 as the one non-recursive certification-summary exception, including fixture assertions that reject any additional, missing, malformed, or human-judgment exception. Refactor the terminal validator's source contract so each sealed source carries its repository-relative path, ancestor commit, blob object ID, and SHA-256; validate object type and identity with Git and hash bytes read from the recorded commit rather than the current filesystem. Build isolated temporary-repository fixtures that commit a source, seal it, then alter the working-tree copy to prove post-hook replacement is permitted; add independent mutations for every identity component and ensure each fails. Do not alter the canonical SECURITY, VERIFICATION, or VALIDATION reports.</action>
  <verify>
    <automated>PHASE198_SUMMARY_SET=final mix test test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; mix test test/threadline/phase198_terminal_certification_contract_test.exs --only immutable_source_identity</automated>
    <fails_when>The exact set is not 01-60, any exception other than mechanically valid summary 61 is accepted, validation reads current report bytes, a sealed-identity mutation passes, or the test does not demonstrate its pre-fix RED.</fails_when>
  </verify>
  <done>The summary boundary is exact and the independently exercised source validator proves immutable historical blob identity without coupling the seal to later canonical-report edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make classic-protection observation explicitly tri-state and fail-closed</name>
  <files>bin/verify-phase198-ref-disposition, test/threadline/phase198_ref_disposition_contract_test.exs</files>
  <behavior>
    - "RED: a fake production gh adapter returning HTTP 404 reaches the current consumer as exit zero and is misclassified as present."
    - "HTTP 404 produces the explicit absent outcome; a successful protection response produces the explicit present outcome."
    - "HTTP 403, HTTP 429, every tested 5xx response, timeout/transport failure, malformed status output, and unknown adapter state exit nonzero and never contribute a protection digest."
    - "The production final stage uses the same adapter/consumer path exercised by the fixtures; production stages continue to reject the existing live-fixture bypass environment variable."
  </behavior>
  <action>First create a PATH-isolated fake-gh seam test that runs the actual bounded classic-protection adapter and demonstrates the 404 misclassification. Change the observer boundary to return one unambiguous state token for absence or presence and a nonzero observation error for every other response; change its consumer to accept only those two exact tokens and reject empty, mixed, or unknown output. Exercise success, 404, 403, 429, representative 5xx statuses, and transport failure through the real shell adapter rather than by directly calling a duplicate classifier in test code. Retain the bounded deadline/retry behavior, stderr-safe error reporting, and all existing fixture-versus-production separation. Do not invoke any write-side Git or GitHub command.</action>
  <verify>
    <automated>mix test test/threadline/phase198_ref_disposition_contract_test.exs --only classic_adapter &amp;&amp; mix test test/threadline/phase198_ref_disposition_contract_test.exs</automated>
    <fails_when>404 maps to present, any authorization/rate/server/transport error maps to absent or present, the consumer infers state from exit zero alone, the fixture duplicates rather than traverses the production adapter, or any existing ref-disposition contract regresses.</fails_when>
  </verify>
  <done>The production observer-to-consumer boundary preserves absent/present/error semantics and its complete focused contract passes.</done>
</task>

<task type="auto">
  <name>Task 3: Reseal exact-summary, live-control, focused, and full-suite evidence</name>
  <files>.planning/audits/198-round12-terminal-certification.md</files>
  <action>After Tasks 1 and 2 are committed, record that commit as the certification source head and derive every source commit:path blob identity and digest with Git. Reuse the established temporary, uncommitted bootstrap stage only to break command-evidence recursion: populate it solely from commands already observed passing, run the terminal-inclusive focused and full lanes, then replace it with the strict final record containing the complete ordered, timestamped, zero-exit receipts. The receipt sequence must include the exact-summary lane, the ref/prohibition/summary focused lane, the real read-only production final command against round-11 inventory/decision/register, bin/verify-branch-protection, the terminal-inclusive focused lane, and full mix test. After writing the final record, rerun the terminal contract and full suite against that strict record. Ensure the final source entries resolve exclusively through recorded Git objects, so later orchestrator SECURITY/VERIFICATION commits cannot invalidate them. Preserve the exact cannot-attest result: T-198-55-02 remains high, blocking, open, and not accepted; T-198-55-03 remains medium, nonblocking, open, and not accepted; GREEN-07 remains accepted-Pending. Do not edit canonical SECURITY, VERIFICATION, or VALIDATION reports, and do not perform any external mutation.</action>
  <verify>
    <automated>PHASE198_SUMMARY_SET=final mix test test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; mix test test/threadline/phase198_ref_disposition_contract_test.exs test/threadline/phase198_prohibition_resolution_contract_test.exs test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; bin/verify-phase198-ref-disposition final --inventory .planning/audits/198-round11-ref-disposition.json --decision .planning/audits/198-round11-ref-disposition.md --register .planning/ARCHIVE-REGISTER.md &amp;&amp; bin/verify-branch-protection &amp;&amp; mix test test/threadline/phase198_terminal_certification_contract_test.exs test/threadline/phase198_ref_disposition_contract_test.exs test/threadline/phase198_prohibition_resolution_contract_test.exs test/threadline/phase198_zero_human_uat_contract_test.exs &amp;&amp; mix test &amp;&amp; mix test test/threadline/phase198_terminal_certification_contract_test.exs &amp;&amp; mix test &amp;&amp; git diff --exit-code -- .planning/phases/198-green-bringup/198-SECURITY.md .planning/phases/198-green-bringup/198-VERIFICATION.md .planning/phases/198-green-bringup/198-VALIDATION.md</automated>
    <fails_when>Any exact-summary, adapter, production-live, branch-protection, focused, terminal, or full-suite command fails; a receipt is predicted or fabricated; current canonical-report bytes are compared to pre-audit digests; either open finding is closed/accepted/omitted; GREEN-07 changes; or the executor edits an orchestrator-owned report.</fails_when>
  </verify>
  <done>The strict final certificate validates from immutable Git objects, production live controls pass read-only, focused and full lanes are green, and unresolved security/requirement state remains exact and honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| mutable canonical reports -> immutable pre-audit evidence | Security and verification hooks replace current report bytes after certification; the seal must identify historical Git objects rather than mutable paths. |
| GitHub HTTP/transport -> classic-protection state | CLI status, HTTP headers, and response bodies cross into a binary protection-state digest. |
| plan executor -> canonical audit hooks | The executor may certify tests and live read-only controls but cannot author the final security or phase verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-198-61-01 | Tampering | terminal source identities | high | mitigate | Bind each source to an ancestor commit, exact path, blob object ID, and git-show content digest; adversarially mutate every field. |
| T-198-61-02 | Spoofing | classic-protection adapter | high | mitigate | Emit only explicit absent/present tokens and preserve all other HTTP/transport outcomes as errors. |
| T-198-61-03 | Repudiation | terminal command evidence | high | mitigate | Persist exact ordered argv text, UTC intervals, zero exits, results, certified head, and immutable source identities only after observation. |
| T-198-61-04 | Elevation of Privilege | canonical verdict ownership | high | mitigate | Scoped-diff guard canonical reports and return control to ordered security then verification hooks after the Plan-61 summary. |
| T-198-61-05 | Tampering | unresolved historical findings | high | mitigate | Contractually preserve T-198-55-02 and T-198-55-03 as open and not accepted, without reconstructing absent evidence. |
</threat_model>

<verification>
- Capture the required RED evidence before each implementation commit, then run each task's automated gate from the repository root.
- Run `git diff --check` and confirm no external state changed before the Plan-61 summary is generated.
- The Plan-61 summary must carry only passing automated coverage with `human_judgment: false`, name audited summaries 01-60 and itself as the sole non-recursive exception, and explicitly state that T-198-55-02 remains blocking due `cannot-attest`, T-198-55-03 remains open below threshold, neither risk is accepted, and GREEN-07 remains accepted-Pending.
- After 198-61-SUMMARY.md exists, the execute-phase orchestrator runs canonical security first and phase verification second. Those hooks may update their reports without invalidating the immutable pre-audit seal and must derive their own truthful verdicts.
</verification>

<success_criteria>
- Exact-summary mode accepts audited summaries 01-60 plus only a mechanically valid Plan-61 exception.
- Immutable-source fixtures accept post-hook working-tree replacement and reject every sealed historical identity mutation.
- The classic adapter fixtures prove 404 absence, success presence, and fail-closed authorization/rate/server/transport outcomes through production code.
- The real production final validator and branch-protection verifier pass without mutation.
- Terminal-focused and full repository test lanes pass against the strict resealed certificate.
- T-198-55-02 remains the blocking cannot-attest finding; T-198-55-03 remains open below threshold; neither is accepted; GREEN-07 remains accepted-Pending.
</success_criteria>

<output>Create `.planning/phases/198-green-bringup/198-61-SUMMARY.md` with mechanical-only coverage and the required unresolved-state statement. Then return control to the execute-phase orchestrator for canonical security and phase-verification hooks, in that order.</output>
