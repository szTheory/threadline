---
phase: 198-green-bringup
plan: 41
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
autonomous: true
requirements:
  - GREEN-07
  - GREEN-08

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The live closure packet pins PR #34, head SHA 46213f9bc0ecbff356058d317c882d5a643ae86f, successful CI run 33354216172, origin/main a97f527e375f4c1909236b7dbdd5fa3fd9b7d2f2, and the measured origin/main..HEAD count rather than relying on the stale round-6 verification snapshot (D-01, D-39)."
    - "The packet proves that merge-commit is incompatible with required linear history, while squash and rebase do not make every original local commit an ancestor of origin/main; the exact-ancestry GREEN-07 clause therefore has no silent ordinary-merge route."
    - "The immutable evidence subject is the pre-plan PR head, not mutable local HEAD; every Plan 41 task commit and `198-41-SUMMARY.md` is counted as a local commit for GREEN-07."
    - "Because a GSD evidence or summary commit necessarily occurs after any in-task push, this plan performs no remote mutation and preserves the accepted-Pending disposition (D-13, D-39)."
    - "GREEN-07 boundary is exact: elapsed time equal to 20 minutes passes, any value above 20 minutes fails, and the target remains 12 minutes or less."
    - "GREEN-07 adjacency and empty edges are explicit: only conclusion `success` passes, and a run with zero discovered jobs or zero `CI required` check-runs fails evidence collection."
    - "GREEN-07 encoding, ordering, precision, and concurrency edges are explicit: check names and SHAs compare byte-for-byte; matching main runs are ordered before status is evaluated; timestamps are UTC and elapsed seconds are retained; no older completed run substitutes for a newer incomplete run."
    - "GREEN-08 empty, encoding, and concurrency edges are explicit: zero required contexts fails; the sole context is byte-exact `CI required`; and ruleset plus PR #26 state are observed read-only with no temporary exception."
  artifacts:
    - ".planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — appended Round 7 live-readiness and atomic-commit lifecycle packet; rounds 1-6 remain byte-identical"
  key_links:
    - "PR #34 head SHA -> successful CI run 33354216172 -> immutable evidence subject recorded separately from mutable HEAD"
    - "GREEN-07 exact ancestry -> GSD task/summary commit lifecycle -> honest Pending disposition"
    - "immutable PR/run subject SHA -> Plan 41 task and summary commits -> measured `origin/main..HEAD` remainder"
  prohibitions:
    - statement: "Do not report GREEN-07 Complete from a branch run, squash, or rebase when origin/main does not contain every original local commit."
      status: unverified
      flagged: true
    - statement: "Do not create a broad, unpinned, or lingering branch-protection bypass."
      status: unverified
      flagged: true
    - statement: "Do not rewrite sealed round-1 through round-6 measurement or D-39 evidence; append supersession evidence."
      status: unverified
      flagged: true
---

<objective>
Produce a current, independently checkable GREEN-07/GREEN-08 closure packet and prove why the phase's own atomic evidence/summary commits prevent a finite in-plan landing from satisfying the literal local-commit predicate.

Purpose: Live evidence supersedes the terminal round-6 CI-failure cause on its merits: Phase 199 repaired both formerly red lanes and run 33354216172 is green, but `origin/main` still lacks the local graph. This plan separates the immutable PR/run evidence subject from mutable local HEAD, counts planning/evidence/summary commits honestly, and retains D-39's accepted-Pending result.

Output: One appended Round 7 section in `198-CI-MEASUREMENT.md`, including the pinned live snapshot, merge-method proof, atomic-commit lifecycle proof, edge dispositions, and explicit no-mutation/Pending conclusion.

**Discovery:** Level 1 complete against the live repository/ruleset and GitHub's official repository-rules API. No package install, schema-push framework, UI surface, or product external-API surface is introduced. `COVERAGE.md` therefore remains unchanged.

**Assumption delta:** `decision: no-change`. This work does not change singular to plural, required to optional, or derived to chosen architecture: `CI required` remains the single required context and GREEN-07 remains derived from exact ancestry plus the latest completed main run. No ruleset exception or public ref update is attempted because the plan cannot truthfully include its own later atomic commits in an earlier pushed SHA.

**Spec-less edge audit — no silent drops:** the deterministic probe emitted 33 unresolved rows. Ten are lifted verbatim into this plan's truths: GREEN-07 boundary, adjacency, empty, encoding, ordering, precision, concurrency and GREEN-08 empty, encoding, concurrency. The remaining 23 are explicitly disposed as already covered and unaffected: GREEN-01 unclassified; GREEN-02 concurrency; GREEN-03 empty/encoding; GREEN-04 boundary/adjacency/empty/encoding/ordering/precision; GREEN-05 unclassified; GREEN-06 boundary/adjacency/empty/ordering/precision; GREEN-09 unclassified; GREEN-10 unclassified; GREEN-11 empty/encoding; GREEN-12 adjacency/empty/ordering. Their satisfying artifacts were re-verified before Phase 199, the current live diff changes none of those requirement surfaces, and plans 198-01 through 198-40 remain their implementation backstop. Equality: 10 lifted + 23 disposed = 33 emitted.

**Multi-source coverage audit:** GOAL's final unsatisfied main-landing clause is covered by plans 198-41/42 with an evidence-backed Pending disposition; all measurement, red-baseline, branch-protection, and sizing clauses remain covered by executed plans 198-01 through 198-40. REQ GREEN-07 and its downstream GREEN-08 are covered here; GREEN-01..06 and GREEN-09..12 remain covered by executed plans and are not reopened. RESEARCH constraints retain one emitted aggregate check, measured GitHub evidence, no weakening, and append-only records. CONTEXT D-01..D-38 remain embodied by executed plans; Phase 199 supersedes D-39's former CI-failure cause but not its accepted-Pending authority because the local-commit predicate still fails; D-40..D-42 remain unchanged. Deferred ideas and human-only UAT judgments are excluded, not missing.

### Artifacts this phase produces

- New symbols: None.
- New flags: None.
- New fields: None.
- New files: None.
- Existing artifact amended: `198-CI-MEASUREMENT.md` receives a Round 7 readiness-and-atomic-lifecycle section.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/198-green-bringup/198-CONTEXT.md
@.planning/phases/198-green-bringup/198-VERIFICATION.md
@.planning/phases/198-green-bringup/198-UAT.md
@.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md
@.planning/phases/198-green-bringup/198-39-DECISION.md
@.planning/phases/198-green-bringup/198-40-SUMMARY.md
@.planning/phases/198-green-bringup/199-RATIFICATION.md
@.planning/HANDOFF.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Append a live end-to-end closure packet from local ancestry through GitHub CI and ruleset state</name>
  <files>.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — append after Round 6; do not edit prior evidence
    - .planning/phases/198-green-bringup/198-VERIFICATION.md — stale `gaps_found` source whose terminal GREEN-07 evidence must be compared with the live tree
    - .planning/phases/198-green-bringup/198-40-SUMMARY.md — last executed Phase 198 state
    - .planning/phases/198-green-bringup/199-RATIFICATION.md — Phase 199 authorization and the human-judgment boundary
    - .planning/HANDOFF.json — current PR and task disposition
    - .planning/REQUIREMENTS.md — exact GREEN-07 and GREEN-08 predicates
    - .planning/phases/198-green-bringup/198-CONTEXT.md — D-01, D-08, D-09, D-12, D-13, D-14, D-39, D-42
  </read_first>
  <action>
Append `## Round 7 — live landing readiness and atomic-commit constraint` to `198-CI-MEASUREMENT.md`. Capture fresh command output for `git rev-parse HEAD`, `git rev-parse origin/main`, both directional `git rev-list --count` queries, PR #34's number/draft/base/head/headRefOid/mergeability fields, run 33354216172's event/headSha/status/conclusion/timestamps/job names and conclusions, ruleset 21702804's enforcement/bypass actors/ref condition/rules/required contexts, and PR #26's state and mergeStateStatus. Treat `46213f9bc0ecbff356058d317c882d5a643ae86f` as the immutable evidence subject, never as an alias for mutable local HEAD; if PR or run state has moved, record the new values and stop without remote mutation.

Write a three-row method proof: a merge commit preserves original commit identities but violates `required_linear_history`; squash produces one replacement commit; rebase produces replacement commit identities. Demonstrate each claim with Git object/ancestry semantics and the live ruleset. Then add the controlling fourth row: an exact fast-forward can land only the SHA that exists at push time, while the executor's later evidence-task and `198-41-SUMMARY.md` commits create new local commits, so `git log origin/main..HEAD` becomes non-empty again. Record that the ruleset currently has `bypass_actors: []`, the only emitted required context is byte-exact `CI required`, and no schema-push or COVERAGE change is implicated.

Add the full ten-row GREEN-07/GREEN-08 edge matrix from `must_haves.truths`, with observable pass and fail values, plus the 23-row grouped disposition and the equality `10 + 23 = 33`. Distinguish the nine Phase 199 human-ratification rows and all other pending UAT judgment rows from implementation gaps; do not convert them into tasks. Record the three retained prohibitions, state that no ruleset or ref mutation is authorized by this plan, and preserve GREEN-07 as Pending under D-39.
  </action>
  <reversibility rating="reversible">The task appends a factual packet to an existing evidence ledger and changes no remote state.</reversibility>
  <verify>
    <automated>bash -c 'subject=46213f9bc0ecbff356058d317c882d5a643ae86f &amp;&amp; test "$(gh pr view 34 --json headRefOid --jq .headRefOid)" = "$subject" &amp;&amp; test "$(gh run view 33354216172 --json headSha --jq .headSha)" = "$subject" &amp;&amp; test "$(gh run view 33354216172 --json conclusion --jq .conclusion)" = success &amp;&amp; grep -q "^## Round 7 — live landing readiness and atomic-commit constraint" .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md'</automated>
    <fails_when>PR #34 or run 33354216172 differs from the immutable evidence subject, the run is not `success`, live queries fail, or the Round 7 heading is absent.</fails_when>
  </verify>
  <acceptance_criteria>
    - The Round 7 packet contains every pinned SHA, count, PR, run, job, ruleset, required-context, and PR #26 field named in the action with capture timestamps.
    - The method proof explains why merge, squash, and rebase each fail at least one locked predicate and why an in-plan exact fast-forward is self-invalidated by later atomic evidence/summary commits.
    - All 33 edge-probe rows are accounted for with equality 10 lifted + 23 disposed = 33; no row is silently dismissed.
    - The packet states that the 36 pending human UAT rows are not implementation gaps and does not manufacture tasks for them.
    - A diff of the measurement file shows additions only below the Round 7 heading; rounds 1-6 are byte-identical.
  </acceptance_criteria>
  <done>A current evidence packet connects local ancestry, PR #34, its green run, ruleset 21702804, and the atomic-commit constraint end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Record the immutable-candidate lifecycle and preserve GREEN-07 Pending</name>
  <files>.planning/phases/198-green-bringup/198-CI-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md — Round 7 packet produced by Task 1
    - .planning/phases/198-green-bringup/198-39-DECISION.md — existing terminal GREEN-07 disposition that remains controlling unless expressly superseded
    - .planning/phases/198-green-bringup/198-CONTEXT.md — D-13 branch-protection posture and D-39 terminal-disposition authority
    - .planning/REQUIREMENTS.md — literal exact-ancestry and main-CI predicates
  </read_first>
  <action>Append an immutable-candidate lifecycle table that names the pre-plan HEAD, the immutable PR/run evidence subject, the HEAD observed when Plan 41 begins, the commit expected after Task 1, and the required `198-41-SUMMARY.md` closeout commit. For every row, state whether it can already be contained by origin/main and whether it exists early enough to be included in an in-task push. Conclude from the last two rows that no SHA available before Task 1 can contain the later task/summary commits, and any push performed before closeout would leave `git log origin/main..HEAD` non-empty again. Do not omit planning, evidence, task, or summary commits from "every local commit." Record `remote mutation: none`, `ruleset mutation: none`, and `GREEN-07: Pending`; retain D-39 without asking the maintainer to authorize an operation that cannot close the literal requirement.</action>
  <reversibility rating="reversible">This task appends an evidence-derived lifecycle proof and performs no remote mutation.</reversibility>
  <verify>
    <automated>bash -c 'section=$(sed -n "/^### Atomic-commit lifecycle proof/,/^### /p" .planning/phases/198-green-bringup/198-CI-MEASUREMENT.md) &amp;&amp; printf "%s" "$section" | grep -q "198-41-SUMMARY.md" &amp;&amp; printf "%s" "$section" | grep -q "remote mutation: none" &amp;&amp; printf "%s" "$section" | grep -q "ruleset mutation: none" &amp;&amp; printf "%s" "$section" | grep -q "GREEN-07: Pending"'</automated>
    <fails_when>The lifecycle proof omits the required summary commit, claims a remote/ruleset mutation, or records GREEN-07 as Complete.</fails_when>
  </verify>
  <acceptance_criteria>
    - The lifecycle table includes every Plan 41 planning, task-evidence, and summary commit category rather than treating the pre-plan SHA as mutable HEAD.
    - The proof states why a push before closeout cannot contain a later closeout commit and therefore cannot make the final local remainder empty.
    - The ledger explicitly records no remote mutation, no ruleset mutation, and GREEN-07 Pending under D-39.
  </acceptance_criteria>
  <done>The evidence ledger honestly proves the self-reference constraint and preserves Pending without exposing branch protection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git/evidence -> GitHub API | Local SHAs and ledger claims are compared with untrusted, mutable remote PR/run/ruleset state. |
| GSD atomic commits -> GREEN-07 evidence | Task and summary commits created after observation change mutable local HEAD and remain inside the quantified local graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-41-01 | Tampering | PR #34 head and ruleset 21702804 | high | mitigate | Pin the immutable evidence-subject SHA and ids, re-query live state, and stop on drift without equating that subject to mutable local HEAD. |
| T-198-41-02 | Tampering | mutable HEAD represented as immutable candidate | high | mitigate | Enumerate every later task/summary commit category and preserve Pending when the final local graph cannot equal the observed remote graph. |
| T-198-41-03 | Elevation of privilege | ruleset/main mutation | high | mitigate | Perform no remote or ruleset mutation in either Round-7 plan; any future mutation requires a new explicit blocking maintainer checkpoint. |
| T-198-41-04 | Information disclosure | GitHub API captures | low | accept | Capture public repository metadata only; exclude tokens, headers, local environment, and credential output. |
</threat_model>

<verification>
- The tracer command passes against current PR/run state.
- The Round 7 packet accounts for 33/33 edge rows and identifies no new schema, UI, package, or product API work.
- The atomic-commit lifecycle proof includes the required summary and concludes Pending; remote state remains unchanged during this plan.
</verification>

<success_criteria>
- A fresh reader can reproduce the remaining constraint from recorded SHAs, run id, and ruleset id.
- No human-only UAT judgment is mislabeled as an implementation gap.
- Plan 198-42 is read-only with respect to GitHub and cannot convert branch evidence into a main-landing claim.
</success_criteria>

<output>
Create `.planning/phases/198-green-bringup/198-41-SUMMARY.md` when done.
</output>
