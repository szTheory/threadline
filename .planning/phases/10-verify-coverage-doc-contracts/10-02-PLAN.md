# Plan 10-02: Doc contract tests, `ci.all`, Actions, Nyquist (TOOL-03 + CI)

**Phase:** 10 — Verify coverage & doc contracts  
**Plan:** 10-02 of 02  
**Status:** Ready to execute  
**Depends on:** 10-01  
**Blocks:** —  
**Requirements covered:** TOOL-03, TOOL-01  
**Wave:** 2  
**requirements_addressed:** TOOL-03, TOOL-01  
**files_modified:** `mix.exs`, `.github/workflows/ci.yml`, `test/threadline/phase06_nyquist_ci_contract_test.exs`, `test/threadline/readme_doc_contract_test.exs`, `test/support/readme_quickstart_fixtures.ex` (or equivalent path), `README.md`, `CONTRIBUTING.md`  
**autonomous:** true  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-10-02-01 | Doc contract modules import **private** modules → brittle false greens | medium | Only `alias` / call modules documented as public in README quickstart; list them in test `@moduledoc`. |
| T-10-02-02 | CI runs different command graph than local `ci.all` | high | Extend **`ci.all`** literal and **CI-02** test same commit; add **identical** `mix` invocations as new steps after `mix verify.test` in `verify-test` job. |
| T-10-02-03 | README **CI:** paragraph drifts from Actions | low | Update README **CI:** line to mention `mix threadline.verify_coverage` and doc-contract step names exactly as in `ci.yml`. |

---

## Goal

Complete **TOOL-03**: compile-checked mirrors of README / quickstart public API usage. Complete **TOOL-01** CI success criteria: **`mix ci.all`** runs **`verify.threadline`** (runs `mix threadline.verify_coverage`) and **`verify.doc_contract`** after **`verify.test`**; **GitHub Actions** `verify-test` job runs the same; **Nyquist CI-02** literal updated; **CONTRIBUTING** documents full gate.

---

## Context

- **Authoritative:** `10-CONTEXT.md` (D-11–D-18), `10-01-PLAN.md` (Mix task shipped), `.planning/REQUIREMENTS.md` (TOOL-01, TOOL-03).

---

## Tasks

### Task 1: Compile-checked README / quickstart API mirrors

**What to do**

1. Add `test/support/readme_quickstart_fixtures.ex` (module name e.g. `Threadline.ReadmeQuickstartFixtures`) containing **only** expressions that compile and mirror critical README paths:
   - `Threadline.Semantics.ActorRef` — `anonymous/0`, `user/1`, `to_map/1`
   - `Jason.encode!/1` on actor map
   - **Plug line:** reference `Threadline.Plug` as `plug Threadline.Plug, actor_fn: &Fake.actor/1` inside a **quoted** `defmodule` that is not loaded — **prefer:** a real `defmodule Threadline.ReadmeDocContractRouter` in test support that **uses** `plug Threadline.Plug, actor_fn: &Threadline.ReadmeDocContractAuth.to_actor_ref/1` with minimal stub `to_actor_ref/1` returning `ActorRef.anonymous()` so it **compiles**.
2. Include **`Threadline.record_action/2`** call shape (may noop in test with stub repo if needed — use **`Code.ensure_loaded?`** pattern **not** preferred; use valid arity call matching public API).
3. Include **`Threadline.Health.trigger_coverage(repo: Threadline.Test.Repo)`** call.
4. Add `test/threadline/readme_doc_contract_test.exs` with at least one test that **`assert Code.ensure_loaded?(Threadline.ReadmeQuickstartFixtures)`** (adjust module name to match the support module you add).

**Read first**

- `README.md` (Quick Start sections 1–3)
- `lib/threadline.ex` (delegates)
- `lib/threadline/plug.ex` (plug options)
- `test/support/data_case.ex`

**Acceptance criteria**

- `MIX_ENV=test mix test test/threadline/readme_doc_contract_test.exs` exits **0**.
- `grep -F 'Threadline.Plug' test/support/readme_quickstart_fixtures.ex` exits **0** (adjust filename if module placed elsewhere — grep the **canonical** path you created).

**Done condition:** Both criteria pass.

---

### Task 2: Mix aliases `verify.threadline`, `verify.doc_contract`, extend `ci.all`

**What to do**

1. In `mix.exs` `aliases/0`, add:
   - `"verify.threadline": ["threadline.verify_coverage"]`
   - **`"verify.doc_contract": "test test/threadline/readme_doc_contract_test.exs"`**  
     (Single string so Mix runs **`mix test test/threadline/readme_doc_contract_test.exs`**, not two separate tasks. Do **not** use `["test", "path"]` — that runs `mix test` then tries `mix path` as a second task.)

2. Change **`ci.all`** to:

   ```elixir
   "ci.all": [
     "verify.format",
     "verify.credo",
     "compile --warnings-as-errors",
     "verify.test",
     "verify.threadline",
     "verify.doc_contract"
   ]
   ```

**Read first**

- `mix.exs`
- `10-CONTEXT.md` (D-15)

**Acceptance criteria**

- `MIX_ENV=test mix verify.threadline` exits **0** when DB + config valid.
- `MIX_ENV=test mix verify.doc_contract` exits **0**.

**Done condition:** Both criteria pass.

---

### Task 3: GitHub Actions `verify-test` job

**What to do**

1. In `.github/workflows/ci.yml`, under **`verify-test`** job steps, **after** the step **`Run tests`** (`run: mix verify.test`), add two steps:

   - **name:** `Verify Threadline trigger coverage` — **`run: mix verify.threadline`**
   - **name:** `Doc contract tests` — **`run: mix verify.doc_contract`**

2. Preserve **`env:`** `DB_HOST`, `MIX_ENV: test` inherited from job.

**Read first**

- `.github/workflows/ci.yml`
- `10-CONTEXT.md` (D-16)

**Acceptance criteria**

- `grep -F 'mix verify.threadline' .github/workflows/ci.yml` exits **0**.
- `grep -F 'mix verify.doc_contract' .github/workflows/ci.yml` exits **0**.

**Done condition:** Both criteria pass.

---

### Task 4: Nyquist CI-02 literal

**What to do**

1. Update `test/threadline/phase06_nyquist_ci_contract_test.exs` assertion string for **`ci.all`** to **exactly** match the new `mix.exs` list including **`verify.threadline`** and **`verify.doc_contract`** in order after **`verify.test`**.

**Read first**

- `mix.exs`
- `test/threadline/phase06_nyquist_ci_contract_test.exs`

**Acceptance criteria**

- `MIX_ENV=test mix test test/threadline/phase06_nyquist_ci_contract_test.exs` exits **0**.

**Done condition:** Criterion passes.

---

### Task 5: README **CI:** paragraph and CONTRIBUTING

**What to do**

1. Update `README.md` line starting **`**CI:**`** to include that **`verify-test`** runs **`mix verify.test`**, then **`mix verify.threadline`**, then **`mix verify.doc_contract`** (exact command names).
2. In `CONTRIBUTING.md`, in CI / parity section, state full gate **`MIX_ENV=test mix ci.all`** requires Postgres and includes Threadline coverage + doc contract steps.

**Read first**

- `README.md`
- `CONTRIBUTING.md`

**Acceptance criteria**

- `grep -F 'verify.threadline' README.md` exits **0**.
- `grep -F 'ci.all' CONTRIBUTING.md` exits **0**.

**Done condition:** Both criteria pass.

---

## Verification

1. `MIX_ENV=test mix ci.all` exits **0** (Postgres running).
2. `MIX_ENV=test mix test test/threadline/phase06_nyquist_ci_contract_test.exs` exits **0**.

---

## Success criteria

- **TOOL-03:** Doc contract tests in default CI path; compile break on API rename.
- **TOOL-01:** CI and local `ci.all` run `verify_coverage` after tests; README/CONTRIBUTING aligned.
