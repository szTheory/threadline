---
phase: 115-narrative-doc-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/how-threadline-works.md
  - test/threadline/how_threadline_works_doc_contract_test.exs
autonomous: true
requirements:
  - NARR-01
  - NARR-03
must_haves:
  truths:
    - "First screenful of how-threadline-works names `Threadline.Audit.transaction/3` as the recommended audited write path before teaching `record_action/2` as a primitive."
    - "Flow steps and first executable example use `Audit.transaction/3` with Plug context + optional `:action`, not standalone `record_action/2`."
    - "Doc-contract test locks `Threadline.Audit.transaction/3`, `recommended audited write path`, and write-side ordering before `record_action/2`."
  artifacts:
    - path: guides/how-threadline-works.md
      provides: "NARR-01 mental-model guide centered on blessed write path"
    - path: test/threadline/how_threadline_works_doc_contract_test.exs
      provides: "NARR-03 narrative literals and write-side ordering locks"
---

<objective>
Surgically retarget `guides/how-threadline-works.md` so `Threadline.Audit.transaction/3` is the primary audited write path (NARR-01), and extend the how-threadline-works doc-contract test with blessed-path literals and write-side ordering (NARR-03 core).

Purpose: Fix the outlier guide that still teaches `record_action/2`-first while README and getting-started already converge on the helper.
Output: Updated guide (~30–40 lines touched) + second contract test with grep-verifiable locks.
</objective>

<context>
@.planning/phases/115-narrative-doc-sync/115-CONTEXT.md (D-115-01, D-115-02, D-115-04)
@guides/getting-started-saas.md §6 (canonical `Audit.transaction/3` snippet shape)
@guides/integration-contracts.md § Audited write path (link target for manual recipe — do not rewrite)
@CHANGELOG.md 0.6.0 Deprecated bullet (manual GUC + record_action recipe)
@test/threadline/how_threadline_works_doc_contract_test.exs
@test/threadline/exploration_routing_doc_contract_test.exs (`:binary.match` section scoping pattern)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-115-01 | Docs imply semantics-only writes suffice without triggers | medium | Keep Architecture §1 capture substrate and flow step for trigger capture unchanged |
| T-115-02 | Full guide rewrite churns JTBD Jobs 1/3/4 | low | D-115-01a surgical scope — only formula, flow, one code block, Job 2, Public API write-side, Evolution bullet, Where to go next |
| T-115-03 | Contract test breaks on intentional `record_action/2` vocabulary removal | low | Retain existing positive `record_action/2` asserts in structural test; add new blessed-path test separately |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Retarget formula and short-version bullets</name>
  <files>guides/how-threadline-works.md</files>
  <read_first>
    - guides/how-threadline-works.md (lines 9–15 formula block)
    - .planning/phases/115-narrative-doc-sync/115-CONTEXT.md (D-115-01c, D-115-02c, unified formula shape in specifics)
  </read_first>
  <action>
In `guides/how-threadline-works.md`, replace the three formula bullets under "The memorable formula is:" with:

```markdown
- `DB truth` = trigger-captured `AuditTransaction` + `AuditChange`
- `app intent` = semantic `AuditAction` records — normally via `Threadline.Audit.transaction/3`
  with `:action` (implemented by `Threadline.record_action/2` inside the helper)
- `operator tooling` = timelines, actor windows, incident bundles, exports, and the optional `/audit` surface
```

Do not change the headline formula line `` `DB truth` + `app intent` + `operator tooling` `` or any Architecture layers / JTBD Jobs 1/3/4 sections in this task.
  </action>
  <acceptance_criteria>
    - `grep -F 'Threadline.Audit.transaction/3' guides/how-threadline-works.md` matches at least once
    - `grep -F 'normally via' guides/how-threadline-works.md` matches
    - `grep -F 'app intent` = `Threadline.record_action/2' guides/how-threadline-works.md` returns no matches (old single-line primitive-only bullet removed)
  </acceptance_criteria>
  <verify>grep -F 'Threadline.Audit.transaction/3' guides/how-threadline-works.md | head -1</verify>
</task>

<task type="auto">
  <name>Task 2: Reorder flow, replace code sample, fix JTBD Job 2</name>
  <files>guides/how-threadline-works.md</files>
  <read_first>
    - guides/how-threadline-works.md (## The flow, flow code fence, Job 2 paragraph)
    - guides/getting-started-saas.md (lines 91–105 `Audit.transaction` fenced block)
    - .planning/phases/115-narrative-doc-sync/115-CONTEXT.md (D-115-01d, D-115-01e, D-115-02d, D-115-02e)
  </read_first>
  <action>
1. Replace "## The flow" numbered list (steps 1–6) with:

```markdown
1. A request enters the host app.
2. `Threadline.Plug` attaches request-scoped audit context and the host decides the actor.
3. Domain writes run inside `Threadline.Audit.transaction/3` — the **recommended audited write path** — so capture and optional semantics share one database transaction.
4. PostgreSQL triggers capture the physical row changes from that transaction.
5. When you pass `:action`, the helper records semantic intent via `Threadline.record_action/2` and links `audit_transactions.action_id` for correlation filters.
6. Operators inspect the result through the query APIs, Mix tasks, or the mounted `/audit` surface.
```

Add one sentence after the list (before "That makes Threadline useful"):

`Request headers can populate audit context at the edge, but queryable correlation requires an audit_actions row linked in the same transaction — use Audit.transaction/3 with :action when filters must match intent.`

2. Replace the flow code fence (currently standalone `record_action`) with a minimal example aligned to getting-started §6:

```elixir
plug Threadline.Plug, actor_fn: &MyApp.Audit.current_actor/1

{:ok, %{post: post}} =
  Threadline.Audit.transaction(
    MyApp.Repo,
    [audit_context: audit_context, action: :post_created],
    fn ->
      case MyApp.Repo.insert(Post.changeset(%Post{}, attrs)) do
        {:ok, post} -> %{post: post}
        {:error, changeset} -> MyApp.Repo.rollback(changeset)
      end
    end
  )
```

3. In JTBD Job 2 ("Who Did This?"), replace the sentence ending with standalone `Threadline.record_action(:refund_issued)` with prose that says: use `plug Threadline.Plug` for actor tagging, and wrap business writes in `Threadline.Audit.transaction/3` with `:action` when intent and correlation matter (no standalone `record_action` call in Job 2 flow text).

4. Do not restructure Architecture layers, Diminishing Returns, or Natural next work.
  </action>
  <acceptance_criteria>
    - `grep -F 'recommended audited write path' guides/how-threadline-works.md` matches in flow section
    - `grep -F 'Threadline.Audit.transaction' guides/how-threadline-works.md` matches in a ```elixir fence
    - `grep -F 'Threadline.record_action(:refund_issued)' guides/how-threadline-works.md` returns no matches
    - `grep -F 'audit_transactions.action_id' guides/how-threadline-works.md` matches
    - `grep -F 'capture_only' guides/how-threadline-works.md` matches OR `grep -F 'Omit :action' guides/how-threadline-works.md` matches (capture-only note present)
  </acceptance_criteria>
  <verify>grep -c 'Threadline.Audit.transaction' guides/how-threadline-works.md | awk '$1>=2'</verify>
</task>

<task type="auto">
  <name>Task 3: Public API write-side, Evolution bullet, Where to go next</name>
  <files>guides/how-threadline-works.md</files>
  <read_first>
    - guides/how-threadline-works.md (### Write-side, ## Evolution so far, ## Where to go next)
    - .planning/phases/115-narrative-doc-sync/115-CONTEXT.md (D-115-02f, D-115-03h, D-115-04f)
    - guides/integration-contracts.md (grep Audited write path heading for link anchor text)
  </read_first>
  <action>
1. Replace `### Write-side` bullet list with:

```markdown
### Write-side

- `Threadline.Audit.transaction/3` — **recommended audited write path** (capture + optional semantics in one transaction)
- `Threadline.Plug` for request capture context
- `Threadline.Job` for serialized job-path context
- `Threadline.record_action/2` — semantic primitive; prefer `Audit.transaction/3` for new code

Manual `set_config` + `record_action/2` linkage recipes are deprecated — see [Integration contracts](integration-contracts.md) § Audited write path via `Threadline.Audit`.
```

2. Under `## Evolution so far`, add as the **last** bullet before "That evolution matters":

`- `0.6.0` packaged the Evidence plane and `Threadline.Audit.transaction/3` as the recommended audited write path.`

(use backticks exactly as shown for version and module)

3. Replace `## Where to go next` bullet list with numbered discovery order:

```markdown
## Where to go next

Adoption discovery order:

1. [README](../README.md) — top-level map and version quick start
2. This guide — mental model, formula, and flow
3. [Getting started with Phoenix SaaS](getting-started-saas.md) §6 — canonical runnable `Audit.transaction/3` snippet
4. [Domain reference](domain-reference.md) — vocabulary and API routing
5. [Integration contracts](integration-contracts.md) — host seams and audited write path contracts
6. [Operator surface](operator-surface.md) — mount, auth, and screens
7. [Support lanes and upgrade path](upgrade-path.md) — support matrix
```
  </action>
  <acceptance_criteria>
    - `grep -F '### Write-side' -A6 guides/how-threadline-works.md` shows `Audit.transaction/3` before `record_action/2`
    - `grep -F '0.6.0' guides/how-threadline-works.md` matches in Evolution section
    - `grep -F 'getting-started-saas.md' guides/how-threadline-works.md` matches in Where to go next
    - `grep -F 'integration-contracts.md' guides/how-threadline-works.md` matches with Audited write path reference in Write-side or body
  </acceptance_criteria>
  <verify>grep -n 'Write-side\|Audit.transaction\|record_action' guides/how-threadline-works.md | head -8</verify>
</task>

<task type="auto">
  <name>Task 4: Extend how-threadline-works doc-contract test (NARR-03)</name>
  <files>test/threadline/how_threadline_works_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/how_threadline_works_doc_contract_test.exs (existing structural test)
    - test/threadline/exploration_routing_doc_contract_test.exs (`:binary.match` between headings)
    - .planning/phases/115-narrative-doc-sync/115-CONTEXT.md (D-115-04a through D-115-04d)
  </read_first>
  <action>
Add a **second** test function `test "mental model guide locks recommended audited write path (NARR-03)"` — do not create a new test file.

Minimum asserts on `guides/how-threadline-works.md`:

```elixir
assert String.contains?(doc, "Threadline.Audit.transaction/3")
assert String.contains?(doc, "recommended audited write path")
```

Add write-side ordering check using `:binary.match`:

```elixir
{idx_write, _} = :binary.match(doc, "### Write-side")
{idx_tx, _} = :binary.match(doc, "Threadline.Audit.transaction/3", idx_write)
{idx_ra, _} = :binary.match(doc, "Threadline.record_action/2", idx_write)
assert idx_tx < idx_ra
```

Also assert:

```elixir
assert String.contains?(doc, "getting-started-saas.md")
assert String.contains?(doc, "§6")
```

**Retain** all existing asserts in the first test unchanged (including `Threadline.record_action/2` in structural vocabulary list).
  </action>
  <acceptance_criteria>
    - `grep -F 'recommended audited write path (NARR-03)' test/threadline/how_threadline_works_doc_contract_test.exs` matches
    - `grep -F ':binary.match' test/threadline/how_threadline_works_doc_contract_test.exs` matches
    - `mix test test/threadline/how_threadline_works_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/how_threadline_works_doc_contract_test.exs</verify>
</task>

</tasks>

<verification>
Run after all tasks:

```bash
mix test test/threadline/how_threadline_works_doc_contract_test.exs
mix verify.doc_contract
```

Both must exit 0 before marking plan complete.
</verification>

<success_criteria>
- NARR-01: how-threadline-works centers `Audit.transaction/3` in formula, flow, example, Job 2, and Public API write-side.
- NARR-03: contract test locks blessed-path literals and write-side ordering.
</success_criteria>
