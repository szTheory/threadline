---
phase: 81-retention-runtime-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/threadline/application.ex
  - lib/threadline/retention/pruner.ex
  - lib/threadline/operator_surface/live/retention_history_live.ex
  - config/config.exs
  - config/test.exs
  - test/threadline/retention/pruner_test.exs
  - test/threadline/operator_surface/live/retention_history_live_test.exs
autonomous: true
requirements:
  - RET-01
  - RET-02
  - RET-03
must_haves:
  truths:
    - "Retention pruning starts from a built-in supervised library runtime instead of requiring ad hoc host/test startup."
    - "The manual retention-history trigger uses the same named supervised runtime path as scheduled pruning."
    - "Run tracking in `threadline_retention_runs` still reflects real purge execution on the repaired runtime path."
    - "The runtime remains on the built-in OTP stack and preserves the advisory-lock singleton model."
  artifacts:
    - path: mix.exs
      provides: "OTP application startup wiring for Threadline"
      contains: "mod:"
    - path: lib/threadline/application.ex
      provides: "Built-in supervision tree for retention runtime"
      contains: "Threadline.Retention.Pruner"
    - path: lib/threadline/operator_surface/live/retention_history_live.ex
      provides: "Manual UI trigger routed through the supervised runtime API"
      contains: "Retention.Pruner"
  key_links:
    - from: lib/threadline/application.ex
      to: lib/threadline/retention/pruner.ex
      via: "child-spec startup using the named pruner process"
      pattern: "start_link|repo:|interval_ms|sleep_ms"
    - from: lib/threadline/operator_surface/live/retention_history_live.ex
      to: lib/threadline/retention/pruner.ex
      via: "manual trigger through runtime-owned API instead of bypass purge"
      pattern: "trigger|cast|run_purge"
---

<objective>
Close the actual runtime gap for Phase 76 by starting the retention pruner from Threadline's built-in application tree and proving the operator surface talks to that same supervised process.

Purpose: satisfy the real runtime portion of RET-01 through RET-03 before the evidence-only closeout work in Plan 02.
Output: an OTP application module, supervised retention runtime startup, and tests proving scheduled + manual flows work on the repaired path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.20-MILESTONE-AUDIT.md
@.planning/phases/81-retention-runtime-closure/81-CONTEXT.md
@.planning/phases/81-retention-runtime-closure/81-RESEARCH.md
@.planning/phases/76-batched-retention-and-ui/76-RESEARCH.md
@mix.exs
@config/config.exs
@config/test.exs
@lib/threadline/retention.ex
@lib/threadline/retention/pruner.ex
@lib/threadline/operator_surface/live/retention_history_live.ex
@test/threadline/retention/pruner_test.exs
@test/threadline/operator_surface/live/retention_history_live_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a built-in application supervisor for retention runtime</name>
  <files>mix.exs, lib/threadline/application.ex, config/config.exs, config/test.exs, test/threadline/retention/pruner_test.exs</files>
  <action>Introduce `Threadline.Application` and wire it via `mix.exs` `mod:` so the library can own runtime children. Build the child list conservatively: resolve the configured repo from `:ecto_repos`, resolve retention runtime options from `config :threadline, :retention`, and start `Threadline.Retention.Pruner` only when retention is enabled and a repo exists. Keep the pruner named and reuse its existing scheduling/advisory-lock behavior. Add or update tests so the runtime startup is proven from the application-owned supervision path, not only from manual `start_supervised` calls. Handle test isolation explicitly now that the named pruner may be auto-started in app lifecycle flows.</action>
  <verify>
    <automated>mix test test/threadline/retention/pruner_test.exs --max-failures 1</automated>
  </verify>
  <done>`:threadline` owns a real application supervisor that can start the retention pruner on the default library path without manual host wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route the retention UI through an explicit supervised-runtime API</name>
  <files>lib/threadline/retention/pruner.ex, lib/threadline/operator_surface/live/retention_history_live.ex, test/threadline/operator_surface/live/retention_history_live_test.exs</files>
  <action>Refine `Threadline.Retention.Pruner` with a small public API such as `trigger/0` and, if needed, a runtime-availability helper so the LiveView no longer depends on a raw named cast with no observable contract. Keep the implementation routed to the same named supervised process. Update `RetentionHistoryLive` to trigger the pruner through that API and to surface the repaired runtime path cleanly in tests. Preserve the existing page shape and polling behavior; this plan is runtime closure, not a UX redesign.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/retention_history_live_test.exs --max-failures 1</automated>
  </verify>
  <done>The retention page's "Run Pruning Batch" flow talks to the same supervised runtime path that scheduled pruning uses, and tests prove a run is recorded on that path.</done>
</task>

<task type="auto">
  <name>Task 3: Reconfirm runtime tracking invariants on the repaired path</name>
  <files>lib/threadline/retention.ex, test/threadline/retention/pruner_test.exs, test/threadline/retention_test.exs</files>
  <action>Re-run and extend the existing retention tests as needed so the repaired supervision path does not regress run tracking, abandoned-run cleanup, or advisory-lock singleton behavior. If application startup or trigger API changes expose missing seams in `Threadline.Retention.purge/1` or `Pruner`, tighten them here, but do not widen scope into new scheduling features or alternate runtimes.</action>
  <verify>
    <automated>mix test test/threadline/retention/pruner_test.exs test/threadline/retention_test.exs test/threadline/operator_surface/live/retention_history_live_test.exs --max-failures 1</automated>
  </verify>
  <done>The repaired runtime path preserves the existing retention data contract while making the default supervised story operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application config -> runtime startup | `:retention` and `:ecto_repos` now influence whether a destructive background worker starts. |
| Operator surface -> retention runtime | Manual UI actions must trigger the same supervised runtime process, not an unsupervised bypass path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01 | Denial of service | `Threadline.Application` startup | mitigate | Start the pruner conditionally and preserve batch/sleep defaults so background deletes remain bounded. |
| T-81-02 | Tampering | `RetentionHistoryLive` trigger path | mitigate | Route UI actions through an explicit pruner API bound to the named supervised process; do not add a direct `Retention.purge/1` bypass. |
| T-81-03 | Repudiation | retention run evidence | mitigate | Keep DB-backed `threadline_retention_runs` tracking as the runtime source of truth and verify it on the repaired path. |
| T-81-04 | Availability | tests and named process ownership | mitigate | Update test setup to avoid duplicate named children and prove behavior from the application-owned runtime where appropriate. |
</threat_model>

<verification>
Run the targeted retention runtime and LiveView tests, then confirm the resulting code path is application-owned: `mix.exs` declares `mod:`, `Threadline.Application` starts the named pruner when enabled, and the retention page triggers that runtime without direct `Retention.purge/1` bypasses.
</verification>

<success_criteria>
Starting `:threadline` with retention enabled and a configured repo makes retention pruning operational through a built-in supervised runtime, and the retention history page uses that same runtime path successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/81-retention-runtime-closure/81-01-SUMMARY.md`
</output>
