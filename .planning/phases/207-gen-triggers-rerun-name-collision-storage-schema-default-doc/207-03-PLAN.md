---
phase: 207-gen-triggers-rerun-name-collision-storage-schema-default-doc
plan: 03
type: execute
wave: 3
depends_on: ["207-02"]
files_modified:
  - test/threadline/storage_schema_test.exs
  - guides/audit-indexing.md
  - guides/production-checklist.md
  - guides/how-threadline-works.md
  - guides/domain-reference.md
  - CHANGELOG.md
  - test/mix/tasks/threadline/gen_triggers_test.exs
autonomous: true
requirements: [W1, W2]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No guide under guides/ and not README.md claims a storage_schema default other than StorageSchema.get([]) (\"public\"): audit-indexing.md:7, production-checklist.md:14, how-threadline-works.md:94 and domain-reference.md:311 now say `public` by default, with a dedicated schema such as `\"threadline\"` as an opt-in chosen before install, and the \"explicit `public` for the historical footprint\" phrasing is gone (D-10)"
    - "A widened guard in test/threadline/storage_schema_test.exs scans guides/**/*.md plus README.md for default claims on storage-schema lines, fails listing path:line for any value other than StorageSchema.get([]), was proven RED against the four wrong sites before they were fixed, flags each of the four known-bad sentences in a positive-control test, ignores `storage_schema: \"threadline\"` opt-in examples, and finds at least 3 correct claims on the real tree so it cannot pass vacuously (D-11)"
    - "guides/production-checklist.md (rerun item near :45) and guides/domain-reference.md (drift section near :52-53) say that a rerun writes a new migration that replaces the trigger in place, that rolling that migration back keeps capture on without restoring the earlier policy, and that a rolled-back rerun which removed redaction continues unredacted and is flagged by the drift view / `mix threadline.policy.show` (D-06, D-07, D-12)"
    - "A default-suite test in test/mix/tasks/threadline/gen_triggers_test.exs pins the rerun/rollback wording: guides/production-checklist.md, guides/domain-reference.md and the gen.triggers moduledoc (via Code.fetch_docs) each contain \"replaces the trigger in place\", \"does not restore the earlier capture policy\", \"unredacted\" and \"mix threadline.policy.show\", and a generated rerun migration's down comment contains the three of those it shares, from one module attribute, so guide and generator text cannot drift apart (D-12)"
    - "CHANGELOG.md `## Unreleased — highlights` gains a second prose paragraph and appended Required action / Fixed items (no second set of ### headings): Breaking changes stays None; both exact errors are quoted (`(Ecto.MigrationError) migrations can't be executed, migration name threadline_triggers_<tables> is duplicated` and `trigger \"threadline_audit_<table>\" for relation \"<table>\" already exists`) with when each appears; affected releases 0.1.0 through 0.10.1; the four corrected guides are named; no hidden module in backticks and no planning vocabulary (D-13)"
    - "A throwaway-database probe applies the up statements of three consecutive generated posts migrations (default, --store-changed-from, default) in order with ON_ERROR_STOP and ends with exactly one threadline_audit_posts trigger on the global function and no leftover per-table function (D-03, D-05; phase goal end to end)"
    - "Phase gate green: full default suite, format, credo, dialyzer, xref cycles, release pins, docs --warnings-as-errors, and the doc/contract batch; no example-app or evaluator migration changed (D-04, D-08, D-09, D-13)"
  artifacts:
    - path: test/threadline/storage_schema_test.exs
      provides: "default-claim guard over guides + README with positive controls and a non-vacuity floor"
      contains: "README.md"
    - path: guides/how-threadline-works.md
      provides: "correct default sentence"
      contains: "defaults to `public`"
    - path: guides/production-checklist.md
      provides: "correct default + rerun truth"
      contains: "replaces the trigger in place"
    - path: test/mix/tasks/threadline/gen_triggers_test.exs
      provides: "rerun documentation contract tying both guides and the moduledoc to the generated down comment"
      contains: "does not restore the earlier capture policy"
    - path: CHANGELOG.md
      provides: "Unreleased entry for the rerun fix and guide corrections"
      contains: "migration name threadline_triggers_"
  key_links:
    - from: test/threadline/storage_schema_test.exs
      to: "Threadline.StorageSchema.get([])"
      via: "every captured default claim must equal the code default"
      pattern: "StorageSchema\\.get\\(\\[\\]\\)"
    - from: "guides rerun text"
      to: "generated migration down comment (plan 02)"
      via: "shared exact phrases asserted by one default-suite test over both guides, the moduledoc and a generated rerun down"
      pattern: "does not restore the earlier capture policy"
---

<objective>
Close audit item W2 and finish W1's documentation and release story: correct all four guides that
state the wrong `storage_schema` default (D-10), add a guard that makes the next straggler
impossible (D-11), tell the truth about reruns and their rollback in the drift guides (D-12, D-06,
D-07), write the CHANGELOG entry that ships with 0.10.2 (D-13), and run the phase gate including an
end-to-end apply probe on a throwaway database.

Purpose: three stragglers survived 0.10.1 because the existing doc/default tie checked only one
guide; a fourth was found in research. The drift guides prescribe a rerun whose behaviour changed
in plans 01-02, so they must describe it.

Output: one widened test file, four edited guides, the CHANGELOG entry, and a green phase gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-CONTEXT.md
@.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-RESEARCH.md
@.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-PATTERNS.md
@.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-02-SUMMARY.md
@test/threadline/storage_schema_test.exs
@CHANGELOG.md

<interfaces>
Code default: lib/threadline/storage_schema.ex:22 `@default "public"`; StorageSchema.get([]) returns it
when :storage_schema is unset (the describe "default storage schema (D-01)" setup in
storage_schema_test.exs already deletes the env and restores it).

Current wrong text (exact; these are the guard's positive controls):
  guides/audit-indexing.md:7         (`threadline` by default, explicit `public` for the historical footprint)
  guides/production-checklist.md:14  (default `threadline`, explicit `public` for the historical footprint)
  guides/how-threadline-works.md:94  The storage schema defaults to `threadline`.
  guides/domain-reference.md:311     usually `threadline` unless you configured `storage_schema: "public"` or another name

Voice to match:
  guides/getting-started-saas.md:63           `storage_schema` **defaults to `"public"`**, your host's default schema.
  guides/configuration-and-commands.md:32     `"public"`, your host's default schema ... A new install can opt into a dedicated schema such as `"threadline"`; set it before `mix threadline.install`, because the generated migrations freeze the choice.

Guard regexes (RESEARCH "D-11 guard", prototyped on the live tree):
  scope line filter:  ~r/storage[ _]schema/i
  id fragment:        "`\"?([A-Za-z_][A-Za-z0-9_]*)\"?`"
  claims: "\\bdefaults?\\s+(?:to\\s+)?(?:the host.s\\s+)?" <> id (i); "\\(default\\s+" <> id; id <> "\\s+(?:\\(the default\\)|by default)" (i); "\\busually\\s+" <> id (i)
  Prototype on today's tree: 4 files WRONG (production-checklist matched twice), correct claims at
  getting-started-saas.md:63, upgrade-path.md:89, :121, :125, :129.

Rerun prescription sites (D-12): guides/production-checklist.md:45 (checklist item);
guides/domain-reference.md:51-53 (three status bullets) followed by the "This is a viewer, not a mutator." paragraph.
</interfaces>
</context>

## Recorded choices (visible, not silent)

- **Spec-less probe fallback skipped (visible skip):** no requirement IDs to probe; D-01..D-13 are the
  contract. `requirements: [W1, W2]` names the audit items.
- **API-coverage detector:** `detected: false`. Assumption-delta decision lives in plan 02.
- **Guard placement:** inside the existing `describe "default storage schema (D-01)"` block, next to
  the configuration-reference tie it widens (RESEARCH recommendation). Two tests: a pure matcher
  positive-control test and a real-tree scan with a floor of 3 correct claims.
- **D-12 edits keep existing sentences:** the three drift-status bullets in domain-reference.md are
  left verbatim (their wording mirrors the presenter hint that `policy_show_doc_contract_test.exs:94`
  pins in lib); the rerun truth is added as a new short paragraph after the bullets. The
  production-checklist item is extended in place. `domain-reference.md:240` is not touched
  (policy_show_doc_contract_test.exs:76-88 pins it) and the `## 1. Capture and triggers` heading stays.
- **Operator-surface and README rerun mentions** (`guides/operator-surface.md:423-424`, `README.md:105`)
  stay as they are: their "rerun gen.triggers" advice is true after the fix and no contract requires
  parity. README is scanned by the guard but not edited (README contract refutes gen.triggers fences).
- **CHANGELOG release range:** gen.triggers has used the `threadline_triggers_<tables>` name and bare
  trigger DDL since its first commit (27553946, before v0.1.0) through v0.10.1 (RESEARCH Q8, git
  verified), so every release 0.1.0 through 0.10.1 is affected.
- **Commit split:** Task 1 `docs(guides): state the public storage_schema default in every guide`;
  Task 2 `docs(gen.triggers): explain rerun migrations and their rollback` (guides + CHANGELOG).
  Both are docs commits that ride with plans 01-02's `fix(gen.triggers)` commits into 0.10.2 (D-13).

## Guardrails for every task (executor MUST follow)

- **Execution location:** main checkout `/Users/jon/projects/threadline`, isolation `none`. If
  `git rev-parse --show-toplevel` is not exactly that path, HALT and report. Never copy/move/sync
  between trees; never edit `.planning/config.json` or `.gsd/dispatch-isolation-sentinel.json`.
- **Halt clause:** if any gate or hook cannot pass honestly, STOP and report the exact output. Do NOT
  move, delete, rename, skip or weaken any existing test/assertion or contract test (including the
  vocabulary regexes in `test/threadline/release_artifact_contract_test.exs:7-14` and every
  `*_doc_contract_test.exs`), `test/test_helper.exs`, `.credo.exs`, or `mix.exs`. No `--no-verify`.
  Do not regenerate screenshot baselines or touch unrelated failing lanes.
- Never `git add .planning/`, `git add .`, `git add -A`; stage explicit paths. Leave the pre-existing
  untracked/modified `.tool-versions`, `.planning/WINDOWS.md`, `.planning/config.json`,
  `.planning/phases/*/.gitkeep` alone. Do not push, branch or switch branches.
- Guides, README and CHANGELOG are packaged/scanned: no `Phase N`, `D-NN`, `phase207`, `v1.41`, or
  audit item IDs in them. Do not name `Threadline.Capture.TriggerSQL` or `Threadline.Mix.*` anywhere in
  guides (code_walkthrough_doc_contract_test.exs:72-83) or in backticks in CHANGELOG (ExDoc autolink).
- No `Ecto.Migrator` / `mix ecto.migrate`. The Task 3 probe applies SQL with `psql` to a throwaway
  database `threadline_probe_207` only, and drops it afterwards; never touch `threadline_test` or
  `threadline_dev` with the probe.
- Temp files under `/Users/jon/.claude/jobs/77cf1bdd/tmp`; strip ANSI before grepping, under `bash -c`.
  Every mix command uses `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27` (plus `DB_PORT=5433` for tests).
- Commit messages end with `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.

<!-- planner-discipline-allow: historical footprint -->
<!-- planner-discipline-allow: Threadline.Capture.TriggerSQL -->
<!-- planner-discipline-allow: Ecto.Migrator -->
<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — the default-claim guard goes RED on the four wrong guides, then every guide says `public`</name>
  <files>test/threadline/storage_schema_test.exs, guides/audit-indexing.md, guides/production-checklist.md, guides/how-threadline-works.md, guides/domain-reference.md</files>
  <precondition>Plan 02 is committed (`git log -F --grep='fix(gen.triggers): keep capture on when a rerun migration is rolled back' --format=%H` prints at least one hash) and `git rev-parse --show-toplevel` prints /Users/jon/projects/threadline; otherwise HALT.</precondition>
  <read_first>
    - test/threadline/storage_schema_test.exs (whole describe "default storage schema (D-01)", :10-66; failure-message style at :51-54)
    - test/threadline/release_artifact_contract_test.exs:249-267 (positive-control matcher pattern)
    - guides/audit-indexing.md:1-12, guides/production-checklist.md:1-20, guides/how-threadline-works.md:88-100, guides/domain-reference.md:305-315
    - guides/getting-started-saas.md:55-66 and guides/configuration-and-commands.md:30-34 (voice)
    - test/threadline/audit_indexing_doc_contract_test.exs, test/threadline/production_checklist_doc_contract_test.exs, test/threadline/how_threadline_works_doc_contract_test.exs, test/threadline/code_walkthrough_doc_contract_test.exs:72-83 (confirm no assertion on the edited sentences)
    - .planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-RESEARCH.md "D-11 guard" and "Q7 Detail"
  </read_first>
  <behavior>
    - "every documented storage_schema default matches the code default" (real tree): files = Path.wildcard("guides/**/*.md") ++ ["README.md"]; for each line matching the scope regex, collect captured claim values; offenders = claims whose value != StorageSchema.get([]); assert offenders == [] with a message listing "path:line -> value" and naming the resolved default; assert the number of correct claims found is >= 3 (non-vacuity). RED today: offenders list audit-indexing.md:7, domain-reference.md:311, how-threadline-works.md:94, production-checklist.md:14.
    - "the default-claim matcher flags every known offender" (pure, green from the start by design — it is the positive control): the four exact bad sentences from <interfaces> each yield a claim value "threadline"; the lines `config :threadline, storage_schema: "threadline"`, the getting-started-saas.md:63 sentence and "a dedicated schema such as `\"threadline\"` is an opt-in" yield no non-public claim.
  </behavior>
  <action>
    Step A (RED, D-11): in test/threadline/storage_schema_test.exs, inside `describe "default storage schema (D-01)"`, add module attributes for the scope regex and claim regexes from <interfaces> (compile with Regex.compile!/2 at module level), a private default_claims(text) :: [{line_number, value}] that splits text into lines, keeps only scope-matching lines, and collects capture group 1 of every claim regex match (Regex.scan) with its 1-based line number, and the two tests from <behavior>. Failure messages follow the existing style: a full sentence built with <> naming the files and StorageSchema.get([]). Do not modify the existing three tests in that describe. Run `mix test test/threadline/storage_schema_test.exs` and save ANSI-stripped output to /Users/jon/.claude/jobs/77cf1bdd/tmp/207-03-t1-red.txt; expected: the real-tree test fails listing the four files (production-checklist possibly twice), the matcher test passes. If the real-tree test passes, the guard is vacuous — fix the regexes, not the guides. Record in the SUMMARY.

    Step B (GREEN, D-10): edit exactly the four sentences, matching the voice in <interfaces>:
    - guides/audit-indexing.md:7 — replace the parenthetical with one stating `public` by default (the host's default schema) and that a dedicated schema such as `"threadline"` is an opt-in chosen before `mix threadline.install`. Remove "explicit `public` for the historical footprint".
    - guides/production-checklist.md:14 — replace the parenthetical with "default `public`; a dedicated schema such as `"threadline"` is an opt-in set before install" (same meaning, checklist voice). Remove the historical-footprint phrase.
    - guides/how-threadline-works.md:94 — "The storage schema defaults to `public`, your host's default schema." then keep the existing point that a host can choose a dedicated schema such as `"threadline"` or another valid PostgreSQL identifier, but must do so before generation because the generated SQL carries that schema name. Do not add any module name.
    - guides/domain-reference.md:311 — "usually `public` unless you configured a dedicated `storage_schema` such as `"threadline"`".
    Keep every heading, marker comment and link (e.g. `<!-- IDX-02-AUDIT-INDEXING -->`, `## 1. Capture and triggers`). Re-run the storage_schema test file: green.

    Commit with explicit paths: `git add test/threadline/storage_schema_test.exs guides/audit-indexing.md guides/production-checklist.md guides/how-threadline-works.md guides/domain-reference.md`; subject `docs(guides): state the public storage_schema default in every guide`; Co-Authored-By trailer.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/storage_schema_test.exs test/threadline/audit_indexing_doc_contract_test.exs test/threadline/production_checklist_doc_contract_test.exs test/threadline/how_threadline_works_doc_contract_test.exs test/threadline/code_walkthrough_doc_contract_test.exs test/threadline/operator_surface/policy_show_doc_contract_test.exs test/threadline/guide_graph_contract_test.exs test/threadline/release_artifact_contract_test.exs'</automated>
    <fails_when>non-zero exit or any failure count other than "0 failures" (the guard lists an offender, or a doc contract broke on an edited line)</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix format --check-formatted && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix credo --strict test/threadline/storage_schema_test.exs'</automated>
    <fails_when>non-zero exit, unformatted files, or any credo issue</fails_when>
  </verify>
  <acceptance_criteria>
    - /Users/jon/.claude/jobs/77cf1bdd/tmp/207-03-t1-red.txt exists (captured before any guide edit) and names all four of audit-indexing.md, domain-reference.md, how-threadline-works.md, production-checklist.md.
    - `grep -c 'README.md' test/threadline/storage_schema_test.exs` >= 1 and `grep -c 'guides/\*\*/\*.md' test/threadline/storage_schema_test.exs` >= 1.
    - `grep -rn 'historical footprint' guides/ README.md` prints nothing.
    - `grep -rnE '`threadline` by default|default `threadline`|defaults to `threadline`|usually `threadline`' guides/ README.md` prints nothing.
    - `grep -c 'defaults to `public`' guides/how-threadline-works.md` >= 1.
    - `grep -rnE '\bD-[0-9]{2,}\b|\bPhase\s+[0-9]+|\bv1\.4[01]\b' guides/audit-indexing.md guides/production-checklist.md guides/how-threadline-works.md guides/domain-reference.md` prints nothing.
    - Both automated commands exit 0; `git log -1 --format=%s` prints `docs(guides): state the public storage_schema default in every guide`.
  </acceptance_criteria>
  <done>The guard failed on the four wrong sentences, they are fixed, the guard and every related doc contract are green, one docs commit exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rerun truth in the drift guides, pinned to the generator by a doc contract, and the 0.10.2 CHANGELOG entry</name>
  <files>test/mix/tasks/threadline/gen_triggers_test.exs, guides/production-checklist.md, guides/domain-reference.md, CHANGELOG.md</files>
  <read_first>
    - guides/production-checklist.md:38-50 (rerun checklist item at :45, as left by Task 1)
    - guides/domain-reference.md:40-60 (drift section and the "viewer, not a mutator" paragraph)
    - lib/mix/tasks/threadline.gen.triggers.ex (the `## Rerunning` moduledoc and the generated down comment from plan 02 — the guides must say the same thing)
    - CHANGELOG.md:1-70 (Unreleased block: boilerplate, the installer paragraph, ### Breaking changes / ### Required action / ### Fixed)
    - test/threadline/changelog_contract_test.exs and test/threadline/release_artifact_contract_test.exs:7-14 (shape and vocabulary rules)
    - test/threadline/operator_surface/policy_show_doc_contract_test.exs:76-95 (pinned literals near the drift text; do not change them)
    - test/mix/tasks/threadline/gen_triggers_test.exs (plan 02: setup, run_triggers/2, trigger_files/1 and the `describe "down body"` cases; test/mix/tasks/threadline.evidence_show_test.exs:169 for the Code.fetch_docs moduledoc pattern)
  </read_first>
  <behavior>
    - "guides and the task docs describe a rerun the way the generated migration does": for each of guides/production-checklist.md, guides/domain-reference.md and the Mix.Tasks.Threadline.Gen.Triggers moduledoc, after collapsing whitespace runs to one space, every phrase in @rerun_doc_phrases ("replaces the trigger in place", "does not restore the earlier capture policy", "unredacted", "mix threadline.policy.show") is present; the failure message names the source and the missing phrase. RED before the guide edits (both guides lack the first three phrases); the moduledoc part is already green from plan 02.
    - "a generated rerun down comment uses the shared phrases": run gen.triggers twice for posts in a tmp dir; the second file's `def down` source contains every phrase in @generated_down_phrases ("does not restore the earlier capture policy", "unredacted", "mix threadline.policy.show"), and @generated_down_phrases is a subset of @rerun_doc_phrases (asserted), so the guides, the moduledoc and the generator share one wording. Green from plan 02.
  </behavior>
  <action>
    Step A (RED, D-12 doc contract): in test/mix/tasks/threadline/gen_triggers_test.exs add module attributes @repo_root File.cwd!() (evaluated at compile time, because tests File.cd! into tmp dirs), @rerun_doc_phrases and @generated_down_phrases as in <behavior>, and a new `describe "rerun documentation"` with the two tests from <behavior>. Read guides with Path.join(@repo_root, ...) and File.read!/1; read the moduledoc with `{:docs_v1, _, :elixir, _, %{"en" => moduledoc}, _, _} = Code.fetch_docs(Mix.Tasks.Threadline.Gen.Triggers)`; normalise with String.replace(text, ~r/\s+/, " ") before matching. Reuse plan 02's run_triggers/2 and trigger_files/1 helpers and its Application.delete_env(:threadline, :storage_schema) convention for the generated case. Do not modify any existing test in the file. Run `mix test test/mix/tasks/threadline/gen_triggers_test.exs` and save ANSI-stripped output to /Users/jon/.claude/jobs/77cf1bdd/tmp/207-03-t2-red.txt; expected: the guides test fails naming production-checklist.md and domain-reference.md; the generated-down test passes. If the moduledoc part fails, plan 02's moduledoc is missing a phrase — HALT and report rather than weakening the phrase list.

    Step B (GREEN) Guides (D-12, D-06, D-07): extend the production-checklist.md:45 item so that, after "rerun `mix threadline.gen.triggers`, apply the generated migration, and re-check", it adds in the same item: the rerun writes a new migration (for example `..._threadline_triggers_posts_2.exs`) that replaces the trigger in place; rolling that migration back keeps capture on and does not restore the earlier capture policy; if the rerun removed redaction rules, a rollback leaves changes captured unredacted, which `/audit/policy/redaction` and `mix threadline.policy.show` report as `Drift detected`. In guides/domain-reference.md leave the three status bullets verbatim and insert, between them and the "This is a viewer, not a mutator." paragraph, one short paragraph with the same meaning (rerun writes a new migration with a numbered name that replaces the trigger in place with no capture gap; rolling it back keeps capture on and does not restore the earlier capture policy; to stop capture, write an explicit migration or roll back the migration that first installed the trigger; if the rerun removed redaction rules, a rollback leaves changes captured unredacted, which the drift view and `mix threadline.policy.show` flag). Both guides must contain, each unbroken, the exact phrases "replaces the trigger in place", "does not restore the earlier capture policy", "unredacted", "capture on" and "mix threadline.policy.show". No module names. Re-run gen_triggers_test.exs: green.

    CHANGELOG (D-13): under `## Unreleased — highlights`, after the existing installer paragraph and before `### Breaking changes`, add one prose paragraph: rerunning `mix threadline.gen.triggers` for tables that already had a trigger migration (as the drift guides instruct) wrote a migration that could not be applied; every release 0.1.0 through 0.10.1 is affected; the generator reused the migration name and module, and the trigger statement could not replace an existing trigger; a rerun now gets a numbered name and module (a first run is named as before), replaces the trigger in place, drops a per-table capture function left behind when a table returns to the default trigger, and its rollback keeps capture on. Keep `### Breaking changes` as None. Append under `### Required action` a paragraph: none, unless a rerun migration failed with one of the errors below; then delete that migration file (it never applied) and run `mix threadline.gen.triggers` again after upgrading. Append under `### Fixed` bullets: (1) the rerun fix quoting `(Ecto.MigrationError) migrations can't be executed, migration name threadline_triggers_<tables> is duplicated` (appears when both trigger migrations are pending together: a fresh or CI database, `mix ecto.reset`, or a rollback across both) and `trigger "threadline_audit_<table>" for relation "<table>" already exists` (appears on a database that already applied the first trigger migration); (2) rolling back a rerun migration no longer leaves a table uncaptured, and the generated migration explains the rollback, including that a rolled-back rerun which removed redaction continues unredacted until regenerated; (3) guides/audit-indexing.md, guides/production-checklist.md, guides/how-threadline-works.md and guides/domain-reference.md now state the real `storage_schema` default, `public`. Do not add a second set of `###` headings. Do not name the hidden SQL module in backticks (prefer not naming it). No planning vocabulary.

    Commit with explicit paths: `git add test/mix/tasks/threadline/gen_triggers_test.exs guides/production-checklist.md guides/domain-reference.md CHANGELOG.md`; subject `docs(gen.triggers): explain rerun migrations and their rollback`; Co-Authored-By trailer.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/mix/tasks/threadline/gen_triggers_test.exs test/threadline/changelog_contract_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/production_checklist_doc_contract_test.exs test/threadline/operator_surface/policy_show_doc_contract_test.exs test/threadline/guide_graph_contract_test.exs test/threadline/storage_schema_test.exs test/threadline/code_walkthrough_doc_contract_test.exs'</automated>
    <fails_when>non-zero exit or any failure count other than "0 failures" (including the new `describe "rerun documentation"` in gen_triggers_test.exs)</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix docs --warnings-as-errors && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix format --check-formatted && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix credo --strict test/mix/tasks/threadline/gen_triggers_test.exs'</automated>
    <fails_when>non-zero exit, any "warning:" line (e.g. an ExDoc autolink to a hidden module), unformatted files, or any credo issue</fails_when>
  </verify>
  <acceptance_criteria>
    - /Users/jon/.claude/jobs/77cf1bdd/tmp/207-03-t2-red.txt exists (captured before any guide edit) and names both production-checklist.md and domain-reference.md.
    - `grep -c 'describe "rerun documentation"' test/mix/tasks/threadline/gen_triggers_test.exs` >= 1 and `grep -c 'Code.fetch_docs(Mix.Tasks.Threadline.Gen.Triggers)' test/mix/tasks/threadline/gen_triggers_test.exs` >= 1 and `grep -c 'does not restore the earlier capture policy' test/mix/tasks/threadline/gen_triggers_test.exs` >= 1.
    - For each of guides/production-checklist.md and guides/domain-reference.md: `grep -c 'replaces the trigger in place'` >= 1 and `grep -c 'does not restore the earlier capture policy'` >= 1 and `grep -c 'unredacted'` >= 1.
    - `grep -c 'Rerun `mix threadline.gen.triggers` and apply the migration.' guides/domain-reference.md` >= 1 (status bullet kept verbatim).
    - `grep -c "migration name threadline_triggers_" CHANGELOG.md` >= 1 and `grep -c 'already exists' CHANGELOG.md` >= 1 and `grep -c '0.10.1' CHANGELOG.md` >= 2.
    - `awk '/^## Unreleased/{f=1} f&&/^## [0-9]/{exit} f' CHANGELOG.md | grep -c '^### Fixed'` = 1 and the same slice greps to 1 for `^### Breaking changes` and 1 for `^### Required action`.
    - `grep -n '`Threadline.Capture.TriggerSQL`' CHANGELOG.md guides/*.md` prints nothing.
    - `awk '/^## Unreleased/{f=1} f&&/^## [0-9]/{exit} f' CHANGELOG.md | grep -nE '\bD-[0-9]{2,}\b|\bPhase\s+[0-9]+|\bv1\.4[01]\b|\bW[0-9]\b'` prints nothing.
    - Both automated commands exit 0; `git log -1 --format=%s` prints `docs(gen.triggers): explain rerun migrations and their rollback`.
  </acceptance_criteria>
  <done>Both drift guides and the CHANGELOG describe the rerun and its rollback truthfully; contracts and docs build are green; one docs commit.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — end-to-end apply probe on a throwaway database, full suite, static gates</name>
  <files>none (verification only; temp files under /Users/jon/.claude/jobs/77cf1bdd/tmp)</files>
  <read_first>
    - lib/mix/tasks/threadline.gen.triggers.ex and lib/threadline/capture/trigger_sql.ex (final state)
    - config/test.exs:5-11 (DB credentials postgres/postgres; port from DB_PORT)
    - .planning/phases/206-installer-migration-versions/206-02-PLAN.md (gate command shapes that passed)
  </read_first>
  <action>
    Step A (end-to-end apply probe, phase goal): write /Users/jon/.claude/jobs/77cf1bdd/tmp/207-probe.exs (a tooling script, never committed) that: deletes :storage_schema from the app env (so SQL targets "public"); makes a fresh dir /Users/jon/.claude/jobs/77cf1bdd/tmp/207-e2e; inside it (File.cd!) runs Mix.Tasks.Threadline.Gen.Triggers.run with ["--tables", "posts"], then ["--tables", "posts", "--store-changed-from"], then ["--tables", "posts"]; then builds /Users/jon/.claude/jobs/77cf1bdd/tmp/207-e2e.sql containing, each terminated by ";" and a newline: `CREATE TABLE posts (id bigserial PRIMARY KEY, title text)`, Threadline.Capture.TriggerSQL.install_function([]), and then, for each generated file sorted by basename, every string argument of an `execute` call inside `def up` (Code.string_to_quoted! AST walk, the same approach as gen_triggers_test's executes/2 — these are our own generated files). Run it with `MIX_ENV=test ... mix run --no-start <script>`. Then with PGPASSWORD=postgres against 127.0.0.1:5433 user postgres: drop-if-exists and create database threadline_probe_207 (connect to db postgres); `psql -v ON_ERROR_STOP=1 -d threadline_probe_207 -f /Users/jon/.claude/jobs/77cf1bdd/tmp/207-e2e.sql`; then query and record: count of pg_trigger rows with tgname 'threadline_audit_posts' (expect 1); proname of that trigger's function via pg_trigger JOIN pg_proc (expect threadline_capture_changes); count of pg_proc rows named threadline_capture_changes_posts (expect 0); finally drop database threadline_probe_207. Also list the three generated basenames (expect names ending _threadline_triggers_posts.exs, _posts_2.exs, _posts_3.exs). Paste all outputs into the SUMMARY under "End-to-end probe". A NOTICE line from the first-run function drop is expected and harmless.

    Step B (gates): run the contract batch, the full suite via the canonical `mix verify.test` entrypoint, and the static gate commands in <verify>. Dialyzer red with a PLT/cache message means a stale PLT: run `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix dialyzer --plt` once and re-run; any other red is a real failure — HALT and report.

    Step C (hygiene checks, record in SUMMARY): `git diff --stat <plan-01 first commit>~1..HEAD -- examples/ priv/` prints nothing; `git status --porcelain` shows no new tracked-file changes beyond the pre-existing `.planning/WINDOWS.md` and `.planning/config.json` modifications and the pre-existing untracked files; `git log --format=%s <plan-01 first commit>~1..HEAD` (this phase's commits only; the earlier 2d6cf376 gen.triggers fix is outside the range) shows only `fix(gen.triggers): ...`, `docs(guides): ...`, `docs(gen.triggers): ...` subjects with no phase or finding IDs.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/jon/projects/threadline || exit 1; export PGPASSWORD=postgres; MIX_ENV=test DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix run --no-start /Users/jon/.claude/jobs/77cf1bdd/tmp/207-probe.exs && psql -h 127.0.0.1 -p 5433 -U postgres -d postgres -v ON_ERROR_STOP=1 -c "DROP DATABASE IF EXISTS threadline_probe_207" -c "CREATE DATABASE threadline_probe_207" && psql -h 127.0.0.1 -p 5433 -U postgres -d threadline_probe_207 -v ON_ERROR_STOP=1 -f /Users/jon/.claude/jobs/77cf1bdd/tmp/207-e2e.sql && psql -h 127.0.0.1 -p 5433 -U postgres -d threadline_probe_207 -tA -c "SELECT count(*) FROM pg_trigger WHERE tgname = '"'"'threadline_audit_posts'"'"'" -c "SELECT p.proname FROM pg_trigger t JOIN pg_proc p ON p.oid = t.tgfoid WHERE t.tgname = '"'"'threadline_audit_posts'"'"'" -c "SELECT count(*) FROM pg_proc WHERE proname = '"'"'threadline_capture_changes_posts'"'"'"; rc=$?; psql -h 127.0.0.1 -p 5433 -U postgres -d postgres -c "DROP DATABASE IF EXISTS threadline_probe_207"; exit $rc'</automated>
    <fails_when>non-zero exit (a statement in the three generated migrations failed to apply, e.g. "already exists" or "cannot drop function"), or the three query lines are not exactly 1, threadline_capture_changes, 0</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/mix/tasks/threadline/install_test.exs test/mix/tasks/threadline/gen_triggers_test.exs test/threadline/mix/ test/threadline/capture/ test/threadline/storage_schema_test.exs test/threadline/changelog_contract_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/public_surface_contract_test.exs test/threadline/code_walkthrough_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/upgrade_path_doc_contract_test.exs test/threadline/how_threadline_works_doc_contract_test.exs test/threadline/audit_indexing_doc_contract_test.exs test/threadline/production_checklist_doc_contract_test.exs test/threadline/operator_surface/policy_show_doc_contract_test.exs test/threadline/guide_graph_contract_test.exs'</automated>
    <fails_when>non-zero exit or any failure count other than "0 failures"</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.test'</automated>
    <fails_when>non-zero exit, or the summary line reports anything other than "0 failures"</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix verify.credo && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix verify.dialyzer && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.xref_cycles && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix release.pins --check && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix docs --warnings-as-errors'</automated>
    <fails_when>non-zero exit from any gate, a "warning:" line, a credo issue, a dialyzer warning, an xref cycle, or a release-pin mismatch</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY "End-to-end probe" section shows three generated basenames ending `_threadline_triggers_posts.exs`, `_threadline_triggers_posts_2.exs`, `_threadline_triggers_posts_3.exs`, psql exit 0, and query results 1 / threadline_capture_changes / 0.
    - `PGPASSWORD=postgres psql -h 127.0.0.1 -p 5433 -U postgres -d postgres -tA -c "SELECT count(*) FROM pg_database WHERE datname = 'threadline_probe_207'"` prints 0 after the task (probe DB dropped).
    - All four automated commands exit 0; the full-suite summary line shows 0 failures and is pasted into the SUMMARY.
    - `bash -c 'if ! H=$(git log --reverse --format=%H --grep="replace the audit trigger in place so a rerun migration applies"); then exit 1; fi; read -r S <<< "$H"; if [ -z "$S" ]; then exit 1; fi; test -z "$(git diff --stat "$S~1..HEAD" -- examples/ priv/)"'` exits 0 (exits 1 if the first plan-01 commit cannot be found, so it cannot pass vacuously).
    - With the log range scoped to this phase's commits only (the pre-existing `fix(gen.triggers): version the trigger migration after existing migrations`, 2d6cf376, lies before it and must not be counted): `bash -c 'if ! H=$(git log --reverse --format=%H --grep="replace the audit trigger in place so a rerun migration applies"); then exit 1; fi; read -r S <<< "$H"; if [ -z "$S" ]; then exit 1; fi; if ! LOG=$(git log --format=%s "$S~1..HEAD"); then exit 1; fi; L=$(grep -e "^fix(gen\.triggers):" -e "^docs(guides):" -e "^docs(gen\.triggers):" <<< "$LOG"); [ "$(grep -c -e "\b207\b" -e "D-[0-9]" -e "\bW[0-9]\b" -e "\bIN-[0-9]" -e "\bWR-[0-9]" -e "\bCR-[0-9]" <<< "$L")" = 0 ] && [ "$(awk "NF{n++} END{print n+0}" <<< "$L")" -eq 7 ]'` exits 0 (no IDs in subjects; exactly 7 subjects: plan 01: 2, plan 02: 3, plan 03: 2).
  </acceptance_criteria>
  <done>Three consecutive generated migrations apply cleanly on real PostgreSQL and leave exactly one trigger on the global function; full suite and all static gates are green; no fixture or example migration changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published guides/CHANGELOG → adopter operators | Operators act on the documented default and rerun/rollback behaviour in production |
| Phase-gate probe → local PostgreSQL | The probe creates and drops a throwaway database with superuser test credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-10 | Information disclosure | guides stating the wrong storage_schema default | medium | mitigate | Operators querying the wrong schema miss audit data or point retention/redaction checks at the wrong place; four sites fixed and the guard (D-11) blocks regressions across all guides + README |
| T-207-11 | Repudiation (audit gap) | rollback guidance | medium | mitigate | Guides and CHANGELOG state that a rerun's rollback keeps capture on and does not restore the prior policy, and name the unredacted-rollback case and where it is flagged (D-07) |
| T-207-12 | Tampering | probe database | low | mitigate | Probe touches only `threadline_probe_207`, drops it before and after; never runs against `threadline_test`/`threadline_dev`; credentials are the existing local test credentials in config/test.exs |
| T-207-13 | Information disclosure | CHANGELOG/guides leaking internal planning vocabulary | low | mitigate | release_artifact_contract_test.exs vocabulary scan plus the acceptance greps |
</threat_model>

<verification>
- Guard proven RED then green; doc contracts green.
- End-to-end probe: three generated posts migrations apply in order with ON_ERROR_STOP; final state 1 trigger / global function / 0 orphan functions.
- Full suite (`mix verify.test`) + `mix verify.format`, `MIX_ENV=test mix verify.credo`, `MIX_ENV=dev mix verify.dialyzer`, `mix verify.xref_cycles`, `MIX_ENV=dev mix release.pins --check`, `MIX_ENV=dev mix docs --warnings-as-errors` green.
</verification>

<success_criteria>
- Every guide states the `public` default; a guard keeps it that way.
- Drift guides and CHANGELOG describe the rerun fix and its rollback truthfully.
- The phase goal is demonstrated on real PostgreSQL: rerun migrations apply and converge to one trigger.
- Phase gate green; commits are `fix(gen.triggers)` / `docs(...)` only.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- MODIFIED `test/threadline/storage_schema_test.exs` — private `default_claims/1`, module attributes for the scope and claim regexes, tests "every documented storage_schema default matches the code default" and "the default-claim matcher flags every known offender".
- MODIFIED guides: `guides/audit-indexing.md`, `guides/production-checklist.md`, `guides/how-threadline-works.md`, `guides/domain-reference.md`.
- MODIFIED `CHANGELOG.md` — Unreleased paragraph + Required action + Fixed bullets.
- Temp (never committed): `/Users/jon/.claude/jobs/77cf1bdd/tmp/207-probe.exs`, `207-e2e/`, `207-e2e.sql`.

## Artifacts this phase produces (whole phase, for the verifier)

| Artifact | Kind | Plan |
|---|---|---|
| `Threadline.Capture.TriggerSQL.drop_orphan_function_for_table/2` | new public function (hidden module) | 01 |
| `create_trigger/3` emits `CREATE OR REPLACE TRIGGER` | changed SQL | 01 |
| `test/threadline/capture/trigger_rerun_test.exs` (`Threadline.Capture.TriggerRerunTest`) | new DB-tier test | 01 |
| `Threadline.Mix.MigrationVersion.existing/1` | new `@doc false` function | 02 |
| `lib/threadline/mix/trigger_migration.ex` (`Threadline.Mix.TriggerMigration`: `scan/1`, `resolve_name/2`, `rerun?/2`) | new hidden module | 02 |
| gen.triggers `migration_content/3`, rerun info line, `## Rerunning` moduledoc | changed task | 02 |
| `test/mix/tasks/threadline/gen_triggers_test.exs`, `test/threadline/mix/trigger_migration_test.exs` | new tests | 02 |
| storage_schema default-claim guard; 4 guides; CHANGELOG entry | docs + guard | 03 |

<output>
Create `.planning/phases/207-gen-triggers-rerun-name-collision-storage-schema-default-doc/207-03-SUMMARY.md` when done, with "RED proof" (guard), "End-to-end probe", full-suite line, and gate results.
</output>
