---
phase: 124-adopter-doc-finish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/operator-surface.md
  - guides/getting-started-saas.md
  - test/threadline/operator_surface_doc_contract_test.exs
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "Operator-surface 1-Minute Mount includes `schemas:` map with PostgreSQL `table_name` keys."
    - "Row history reification subsection documents `:schemas`, failure UX, route shorthand vs drill-down, and two prerequisites (`scope_query_fn` + `:schemas`)."
    - "Getting-started §9 links to reification SSOT without duplicating full map."
  artifacts:
    - path: guides/operator-surface.md
      provides: "DOC-03 mount + reification SSOT"
    - path: guides/getting-started-saas.md
      provides: "DOC-03 §9 one-liner"
    - path: test/threadline/operator_surface_doc_contract_test.exs
      provides: "DOC-03 regression lock"
---

<objective>
Document `:schemas` on `threadline_operator_surface/2` for row-history reification and link from getting-started §9.

Purpose: Operators learn table→schema mapping before hitting the unmapped-table error panel.
Output: Updated `guides/operator-surface.md`, §9 sentence in getting-started, extended `operator_surface_doc_contract_test.exs`.
</objective>

<context>
@.planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-10–D-14, D-24)
@.planning/phases/124-adopter-doc-finish/124-RESEARCH.md (§2.2, §3.1–3.2 code anchors)
@.planning/phases/124-adopter-doc-finish/124-PATTERNS.md (§3.3)
@lib/threadline/operator_surface/auth.ex (lines 12–20 — `:schemas` assign)
@lib/threadline/operator_surface/live/row_history_component.ex (line 40 — error copy)
@lib/threadline/operator_surface/router.ex (line 106 — drill-down route)
@guides/operator-surface.md (mount blocks ~46–50, ~76–80; Row History ~172–175)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-124-05 | Operator hits row-history error without prior doc | medium | D-10–D-12: mount block + reification subsection + §9 link |
| T-124-06 | Doc says option on wrong plug name | low | D-12: document option on `threadline_operator_surface/2`; note UI copy says "auth plug" for grep parity |
| T-124-07 | Full `:schemas` map duplicated in getting-started §9 | low | D-13: one sentence + link only |

**Surface:** Docs-only — no runtime changes. Prevents support-scoped row-history misconfiguration.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Document :schemas in operator-surface guide (DOC-03 prose)</name>
  <files>guides/operator-surface.md</files>
  <read_first>
    - guides/operator-surface.md (§1-Minute Mount admin ~46–50; support ~76–80; Row History ~172–175)
    - lib/threadline/operator_surface/auth.ex
    - lib/threadline/operator_surface/live/row_history_component.ex
    - lib/threadline/operator_surface/router.ex
    - .planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-10–D-12)
  </read_first>
  <action>
**D-10 — Canonical mount block:** Add to **both** admin and support `threadline_operator_surface` examples:

```elixir
schemas: %{"posts" => MyApp.Post, "users" => MyApp.Accounts.User},
```

One sentence after map: keys are PostgreSQL `table_name` values from capture; required for row-history/as-of reification in transaction drill-down.

**D-11 — New subsection** under `### Row History / As-of Sub-view` (~line 172):

`#### Row history reification (:schemas)`

Cover:
- Purpose: table string → Ecto module for `history/3` + `as_of/4`
- String keys preferred; atom keys optional
- Pair with `scope_query_fn` and `%{surface: :row_history}` for support-scoped row history
- API/IEx parity: schema module passed directly off-mount
- Route shorthand `/audit/rows/:table/:pk` vs actual `live("/transactions/:id/history/:table/:record_id", ...)` slide-over
- **Two host prerequisites** for support-scoped row history: (1) `scope_query_fn`, (2) `:schemas`

**D-12 — Failure UX:** Document exact error copy:

`Table 'X' is not mapped to an Ecto schema. Configure :schemas in the auth plug.`

Note: option lives on `threadline_operator_surface/2` opts; UI copy uses "auth plug" for historical grep parity.

**D-14:** Do **not** wire example app `:schemas` or router `@moduledoc` unless zero additional scope.
  </action>
  <acceptance_criteria>
    - `grep -F 'schemas:' guides/operator-surface.md` exits 0
    - `grep -F '#### Row history reification (:schemas)' guides/operator-surface.md` exits 0
    - `grep -F 'Configure :schemas in the auth plug' guides/operator-surface.md` exits 0
    - `grep -F 'table_name' guides/operator-surface.md` exits 0
    - `grep -F 'scope_query_fn' guides/operator-surface.md` matches reification subsection
  </acceptance_criteria>
  <verify>grep -F 'schemas:' guides/operator-surface.md && grep -F '#### Row history reification (:schemas)' guides/operator-surface.md</verify>
</task>

<task type="auto">
  <name>Task 2: §9 one-liner + operator_surface doc contract (DOC-03)</name>
  <files>guides/getting-started-saas.md, test/threadline/operator_surface_doc_contract_test.exs</files>
  <read_first>
    - guides/getting-started-saas.md (§9 ~256–274)
    - guides/operator-surface.md (after task 1 — anchor slug for link)
    - test/threadline/operator_surface_doc_contract_test.exs (canonical recipes test)
    - .planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-13, D-14)
  </read_first>
  <action>
**D-13 — getting-started §9:** After mount block or in §9 prose, add one sentence + markdown link to operator-surface reification subsection (e.g. `guides/operator-surface.md#row-history-reification-schemas`). Do **not** duplicate full `schemas:` map in §9.

**D-14 — Contract:** Add test `"operator surface guide documents :schemas for row history reification (DOC-03)"`:

```elixir
test "operator surface guide documents :schemas for row history reification (DOC-03)" do
  guide = File.read!("guides/operator-surface.md")

  assert String.contains?(guide, "schemas:")
  assert String.contains?(guide, "#### Row history reification (:schemas)")
  assert String.contains?(guide, "Configure :schemas in the auth plug")
  assert String.contains?(guide, "table_name")
end
```

Extend existing mount recipe test if needed so admin **and** support blocks include `schemas:` (grep both occurrences).

Do not create new test file.
  </action>
  <acceptance_criteria>
    - `grep -F 'operator-surface' guides/getting-started-saas.md` matches §9 link to reification
    - `grep -F ':schemas' guides/getting-started-saas.md` exits 0 in §9 (sentence reference)
    - `mix test test/threadline/operator_surface_doc_contract_test.exs` exits 0
    - `grep -F 'row history reification (DOC-03)' test/threadline/operator_surface_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/operator_surface_doc_contract_test.exs</verify>
</task>

</tasks>

<verification>
```bash
mix test test/threadline/operator_surface_doc_contract_test.exs
```
</verification>

<success_criteria>
- DOC-03: `:schemas` in mount blocks; reification subsection; failure UX; §9 link
- Example app `:schemas` wiring remains deferred per D-14
</success_criteria>
