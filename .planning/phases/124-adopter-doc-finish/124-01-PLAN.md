---
phase: 124-adopter-doc-finish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/getting-started-saas.md
  - test/threadline/getting_started_saas_doc_contract_test.exs
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "Getting-started §6 teaches IEx-first audited write with `demo-corr` handoff to §8; open prose has no Sigra cookie names."
    - "§6 HTTP subsection is `### HTTP requests and host auth` with canonical lane IDs; all cookie/curl prose lives inside `<details>` only."
    - "Dedicated DOC-02 test locks ADOPT-AUTH literals and §5 ordering; monolith no longer requires open `_threadline_phoenix_key`."
  artifacts:
    - path: guides/getting-started-saas.md
      provides: "DOC-01 §6 IEx-first + collapsed HTTP; DOC-02 §5 verify-only"
    - path: test/threadline/getting_started_saas_doc_contract_test.exs
      provides: "DOC-01 contract evolution + DOC-02 dedicated test"
---

<objective>
Restructure getting-started §6 for auth-neutral first-hour exercise (IEx-first) and lock ADOPT-AUTH literals with a dedicated doc contract.

Purpose: Close v1.26 audit INFO-2 (Sigra-only §6 cookie prose) and ADOPT-AUTH soft gap without duplicating example README HTTP SSOT.
Output: Updated `guides/getting-started-saas.md` §5–§6, extended `getting_started_saas_doc_contract_test.exs`, green targeted tests.
</objective>

<context>
@.planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-01–D-09)
@.planning/phases/124-adopter-doc-finish/124-RESEARCH.md (§2.1, §5 DOC-01/DOC-02)
@.planning/phases/124-adopter-doc-finish/124-PATTERNS.md (§3.1–§3.2)
@guides/getting-started-saas.md (§5 lines 73–104, §6 lines 121–189)
@test/threadline/getting_started_saas_doc_contract_test.exs (monolith lines 67–69)
@test/threadline/example_phoenix_readme_contract_test.exs (sigra HTTP SSOT — retain)
@examples/threadline_phoenix/README.md (HTTP depth SSOT per D-03)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-124-01 | phx-gen-auth adopters see Sigra cookie names in open §6 | medium | D-02: delete lines 183–187; collapse cookie prose into `<details>`; contract refutes open `_threadline_phoenix_key` in §6 |
| T-124-02 | Monolith test removed before prose moves sigra literals | medium | Edit guide + test in same commit; run `example_phoenix_readme_contract_test.exs` |
| T-124-03 | IEx snippet implies Plug wiring unnecessary | low | D-05: explicit note IEx builds context Plug attaches on HTTP |
| T-124-04 | ADOPT-AUTH literals drift to §6 only | low | D-08: scope §5 between `## 5.` and `## 6.` for lane ordering |

**Surface:** Docs-only — no runtime security changes. Doc accuracy prevents operator/auth misconfiguration (wrong lane, wrong HTTP staging path).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Restructure getting-started §6 (DOC-01 prose)</name>
  <files>guides/getting-started-saas.md</files>
  <read_first>
    - guides/getting-started-saas.md (§6 lines 121–189; §8 line 215 `demo-corr`)
    - .planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-01, D-02, D-05)
    - examples/threadline_phoenix/README.md (Track A HTTP — do not duplicate depth)
    - lib/threadline/semantics/audit_context.ex (field names for IEx snippet)
  </read_first>
  <action>
**D-01 / D-02:** After existing `### Recommended path (0.6.0+)`, insert open subsection:

`### Run your first audited write in IEx`

Include:
- `iex -S mix` preamble
- `ActorRef.new(:user, "you@example.com")` (or equivalent host identity)
- `%Threadline.Semantics.AuditContext{actor_ref: actor, correlation_id: "demo-corr", ...}` with minimal required fields
- `Threadline.Audit.transaction/3` example returning `audit_transaction_id`
- Explicit handoff: keep `audit_transaction_id` for step 8; `demo-corr` matches §8 `correlation_id: "demo-corr"`
- **D-05:** One sentence — IEx builds the context `Threadline.Plug` would attach on HTTP; does not replace §5 Plug wiring

**Rename** `### Authenticate before the audited API call` → `### HTTP requests and host auth` (D-02).

**Lane table** — replace display labels with canonical IDs (D-02):
| Lane | Guide |
| `phx-gen-auth-reference` | phx-gen-auth guide |
| `sigra-reference` | sigra guide |
| Include `Threadline does not require Sigra` in open prose

**Delete lines 183–187** (cookie staging outside `<details>`).

**Collapse** into existing `<details>` (`Runnable curl — sigra-reference example app only`):
- Move lines 183–187 content inside `<details>` before closing tag
- Keep curl block with `_threadline_phoenix_key`, `/users/log_in`, bearer disclaimer
- Optional HTML comment `getting-started-sigra-http-staging-fence` inside `<details>` (D-04)

**Do not** add four lane-branched §6 subsections or phx-gen-auth curl (D-01 rejects).

Do **not** edit §5 in this task unless lane table typos require one-line fix.
  </action>
  <acceptance_criteria>
    - `grep -F '### Run your first audited write in IEx' guides/getting-started-saas.md` exits 0
    - `grep -F '### HTTP requests and host auth' guides/getting-started-saas.md` exits 0
    - `grep -F '### Authenticate before the audited API call' guides/getting-started-saas.md` exits 1
    - `grep -F '_threadline_phoenix_key' guides/getting-started-saas.md` matches only inside `&lt;details&gt;` block (manual spot-check or awk between details tags)
    - `grep -n 'demo-corr' guides/getting-started-saas.md` shows occurrence in §6 before §8 heading
  </acceptance_criteria>
  <verify>grep -F '### Run your first audited write in IEx' guides/getting-started-saas.md && grep -F '### HTTP requests and host auth' guides/getting-started-saas.md && ! grep -F '### Authenticate before the audited API call' guides/getting-started-saas.md</verify>
</task>

<task type="auto">
  <name>Task 2: Evolve getting_started doc contracts (DOC-01 + DOC-02)</name>
  <files>test/threadline/getting_started_saas_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/getting_started_saas_doc_contract_test.exs (monolith test lines 67–69; CFG-02 ordering pattern)
    - guides/getting-started-saas.md (after task 1)
    - .planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-04, D-06–D-08)
    - test/threadline/example_phoenix_readme_contract_test.exs (retain sigra HTTP asserts)
  </read_first>
  <action>
**D-04 — Monolith cleanup:** In `"quickstart guide locks the adopter walkthrough"`, **remove**:
- `assert String.contains?(doc, "Authenticate before")`
- `assert String.contains?(doc, "_threadline_phoenix_key")`
- `assert String.contains?(doc, "does not ship API bearer")`

Replace with assert for `### HTTP requests and host auth` if walkthrough still needs §6 HTTP anchor.

**D-01 — New dedicated test** `"getting-started §6 locks IEx-first audited write and demo-corr handoff (DOC-01)"`:
- Assert `### Run your first audited write in IEx`
- Assert `### HTTP requests and host auth`
- `refute` `### Authenticate before the audited API call`
- Slice §6 (`## 6.` .. `## 7.`): assert `demo-corr` and `audit_transaction_id` present
- `refute` `_threadline_phoenix_key` in §6 slice (open prose)
- If `getting-started-sigra-http-staging-fence` added: assert sigra literals only after marker inside `<details>`

**D-06–D-08 — New dedicated test** `"getting-started §5 locks auth-neutral ADOPT-AUTH literals (DOC-02)"`:
- Slice §5 (`## 5.` .. `## 6.`) — presence of each D-07 literal:
  - `Threadline does not own auth`
  - `Choose an auth lane when you need a full cookbook:`
  - `phx-gen-auth-reference`
  - `sigra-reference`
  - `Threadline does not require Sigra; do not use \`Threadline.Integrations.Sigra\``
  - `unless you adopt the optional sigra-reference lane.`
- `:binary.match` ordering (full doc):
  - `Threadline does not require Sigra` index &lt; `## 6.` index
  - `phx-gen-auth-reference` &lt; `sigra-reference`
  - `Threadline does not require Sigra` &lt; `getting-started-sigra-reference-fence` &lt; `Threadline.Integrations.Sigra.actor_ref_from_conn/1`

**D-09:** Do not add README four-lane matrix, phx guide plug literals, or evaluator neutrality asserts.

Run `mix test test/threadline/example_phoenix_readme_contract_test.exs` to confirm dual-contract intact.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/getting_started_saas_doc_contract_test.exs` exits 0
    - `mix test test/threadline/example_phoenix_readme_contract_test.exs` exits 0
    - `grep -F 'getting-started §5 locks auth-neutral ADOPT-AUTH literals (DOC-02)' test/threadline/getting_started_saas_doc_contract_test.exs` exits 0
    - `grep -F 'getting-started §6 locks IEx-first' test/threadline/getting_started_saas_doc_contract_test.exs` exits 0
    - `grep '_threadline_phoenix_key' test/threadline/getting_started_saas_doc_contract_test.exs` in monolith test returns no match (or only in comments)
  </acceptance_criteria>
  <verify>mix test test/threadline/getting_started_saas_doc_contract_test.exs && mix test test/threadline/example_phoenix_readme_contract_test.exs</verify>
</task>

</tasks>

<verification>
```bash
mix test test/threadline/getting_started_saas_doc_contract_test.exs
mix test test/threadline/example_phoenix_readme_contract_test.exs
```
</verification>

<success_criteria>
- DOC-01: §6 IEx-first; no open Sigra cookie prose; `demo-corr` handoff to §8
- DOC-02: Dedicated ADOPT-AUTH test with D-07 literals and D-08 ordering
- Dual-contract: sigra HTTP literals locked in example README test only
</success_criteria>
