---
phase: 124-adopter-doc-finish
plan: 03
type: execute
wave: 2
depends_on:
  - 124-01
  - 124-02
files_modified:
  - guides/domain-reference.md
  - guides/how-threadline-works.md
  - guides/integration-contracts.md
  - guides/operator-surface.md
  - test/threadline/how_threadline_works_doc_contract_test.exs
  - test/threadline/integration_contracts_doc_contract_test.exs
autonomous: true
requirements:
  - DOC-04
  - DOC-05
must_haves:
  truths:
    - "domain-reference has `## Evidence write boundary (host-written)` with `EVIDENCE-HOST-WRITE-BOUNDARY` before proof contract."
    - "Mental-model and integration-contracts guides refute auto-populate evidence; operator-surface states `/audit/evidence` is viewer-only."
    - "integration-contracts enumerates four lanes in canonical order with upgrade-path cross-link; no matrix table duplicate."
  artifacts:
    - path: guides/domain-reference.md
      provides: "DOC-04 evidence write boundary SSOT"
    - path: guides/how-threadline-works.md
      provides: "DOC-04 mental model fix"
    - path: guides/integration-contracts.md
      provides: "DOC-04 evidence paragraph + DOC-05 lane section"
    - path: guides/operator-surface.md
      provides: "DOC-04 /audit/evidence viewer sentence"
    - path: test/threadline/how_threadline_works_doc_contract_test.exs
      provides: "DOC-04 contracts"
    - path: test/threadline/integration_contracts_doc_contract_test.exs
      provides: "DOC-05 four-lane contract"
---

<objective>
Establish evidence host-write boundary across adopter guides and add integration-contracts four-lane vocabulary aligned with upgrade-path.

Purpose: Correct stale "Threadline may persist evidence" copy and close integration-contracts lane enumeration gap.
Output: Four guide updates, two contract test extensions, green `mix verify.doc_contract`.
</objective>

<context>
@.planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-15–D-26)
@.planning/phases/124-adopter-doc-finish/124-RESEARCH.md (§2.3–2.5, §5 DOC-04/DOC-05)
@.planning/phases/124-adopter-doc-finish/124-PATTERNS.md (§3.4–§3.6)
@lib/threadline/evidence.ex (six `record_*` entrypoints)
@guides/upgrade-path.md (matrix SSOT — link only)
@test/threadline/upgrade_path_doc_contract_test.exs (matrix stays here)
@guides/operator-surface.md (mounted evidence ~127–131 — edit after 124-02)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-124-08 | Adopters expect retention/health to auto-fill evidence | high | D-16: negative claim + `threadline_retention_runs` vs `threadline_evidence_records` distinction |
| T-124-09 | Compliance/SOC2 over-promise in evidence docs | medium | D-18: align v1.22 non-goals; no `guides/evidence-plane.md` hub |
| T-124-10 | Matrix duplicate in integration-contracts | medium | D-22: refute `\| Lane \| Claim type \|`; link upgrade-path only |
| T-124-11 | Merge conflict on operator-surface.md | low | Wave 2 depends_on 124-02 |

**Surface:** Docs-only. Accurate evidence boundary prevents false compliance expectations and wrong ops debugging (empty evidence ≠ missing job).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Evidence host-write boundary SSOT + mental model fixes (DOC-04)</name>
  <files>guides/domain-reference.md, guides/how-threadline-works.md, guides/integration-contracts.md, guides/operator-surface.md</files>
  <read_first>
    - guides/domain-reference.md (lines 75+ — insert before Evidence proof contract)
    - guides/how-threadline-works.md (lines 31–33 stale copy)
    - guides/integration-contracts.md (lines 151–154 evidence paragraph)
    - guides/operator-surface.md (mounted `/audit/evidence` ~127–131)
    - lib/threadline/evidence.ex (record_* list)
    - .planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-15–D-18)
  </read_first>
  <action>
**D-15 — domain-reference:** Insert **immediately before** `## Evidence proof contract`:

```markdown
## Evidence write boundary (host-written)

<!-- EVIDENCE-HOST-WRITE-BOUNDARY -->
```

**D-16 claim shape (~15–25 lines):**
- Positive: host writes via `Threadline.Evidence` `record_*` for six closed subjects; list all six `record_*` names from `lib/threadline/evidence.ex`
- Read surfaces (`Proof`, `mix threadline.evidence.show`, `/audit/evidence`) interpret rows already written
- Negative: `Threadline.Retention`, `Threadline.Health`, export paths **do not** auto-create evidence rows — use phrase `does not auto-populate` (or equivalent locked in contract)
- Precision: `threadline_retention_runs` (ops metadata) ≠ `threadline_evidence_records` (host attestations)
- Empty evidence view = no host attestation yet, not missing background job

**D-17 — how-threadline-works.md:** Replace lines 31–33 — remove `Threadline may persist evidence`; write host-written attestations + link to `domain-reference.md` anchor (`#evidence-write-boundary-host-written` or rendered slug).

**D-17 — integration-contracts.md:** Replace evidence paragraph at ~151–154 — host-owned writes; retain RBAC non-goals framing.

**D-17 — operator-surface.md:** One sentence at `/audit/evidence` block: viewer only; host writes via `Threadline.Evidence` (coordinate with 124-02 mount section — additive sentence only).

**D-18:** Do **not** create `guides/evidence-plane.md`. Do not promise compliance packs/SOC2 pipeline.
  </action>
  <acceptance_criteria>
    - `grep -F 'EVIDENCE-HOST-WRITE-BOUNDARY' guides/domain-reference.md` exits 0
    - `grep -F '## Evidence write boundary (host-written)' guides/domain-reference.md` exits 0
    - `grep -F 'does not auto-populate' guides/domain-reference.md` exits 0
    - `grep 'Threadline may persist evidence' guides/how-threadline-works.md guides/integration-contracts.md` exits 1
    - `grep -F 'record_redaction_policy' guides/domain-reference.md` exits 0
    - `grep -F 'host-written' guides/how-threadline-works.md` exits 0
  </acceptance_criteria>
  <verify>grep -F 'EVIDENCE-HOST-WRITE-BOUNDARY' guides/domain-reference.md && ! grep -F 'Threadline may persist evidence' guides/how-threadline-works.md</verify>
</task>

<task type="auto">
  <name>Task 2: Lock DOC-04 in how_threadline_works doc contract</name>
  <files>test/threadline/how_threadline_works_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/how_threadline_works_doc_contract_test.exs (scoped `:binary.match` pattern)
    - guides/domain-reference.md (boundary section after task 1)
    - guides/how-threadline-works.md (after task 1)
    - .planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-19)
  </read_first>
  <action>
**D-19 — Add tests in `how_threadline_works_doc_contract_test.exs`:**

1. `"mental model guide locks host-written evidence framing (DOC-04)"`:
   - `refute String.contains?(doc, "Threadline may persist evidence")`
   - `assert String.contains?(doc, "domain-reference.md")`
   - `assert` host-written framing (`host-written` or `host apps write`)

2. `"domain-reference locks evidence host-write boundary before proof contract (DOC-04)"`:
   - Read `guides/domain-reference.md`
   - Assert `EVIDENCE-HOST-WRITE-BOUNDARY` and `## Evidence write boundary (host-written)`
   - Slice between marker and `## Evidence proof contract`
   - Assert `does not auto-populate` (or equivalent) in slice
   - Assert `record_redaction_policy` and `threadline_retention_runs` in slice

Do not create new test file.
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/how_threadline_works_doc_contract_test.exs` exits 0
    - `grep -F 'host-written evidence framing (DOC-04)' test/threadline/how_threadline_works_doc_contract_test.exs` exits 0
    - `grep -F 'evidence host-write boundary before proof contract (DOC-04)' test/threadline/how_threadline_works_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/how_threadline_works_doc_contract_test.exs</verify>
</task>

<task type="auto">
  <name>Task 3: Four-lane vocabulary in integration-contracts + contract (DOC-05)</name>
  <files>guides/integration-contracts.md, test/threadline/integration_contracts_doc_contract_test.exs</files>
  <read_first>
    - guides/integration-contracts.md (intro lines 7–18; Plug section; Integrations section; operator-surface mention)
    - guides/upgrade-path.md (canonical lane order — link target)
    - CHANGELOG.md line 38 (Phase 122 compact four-lane pattern)
    - test/threadline/integration_contracts_doc_contract_test.exs
    - .planning/phases/124-adopter-doc-finish/124-CONTEXT.md (D-20–D-23)
  </read_first>
  <action>
**D-20 — New section** after intro seam list (~line 16), **before** `## Request path via Threadline.Plug`:

`## Adoption lanes and integration seams`

Enumerate four lane IDs in **canonical order** with one-line seam map each:
1. `capture-only`
2. `phoenix-surface`
3. `phx-gen-auth-reference`
4. `sigra-reference`

Cross-link: `guides/upgrade-path.md` for matrix, claim types, proof, lifecycle. **No** full matrix table (D-20 rejects duplicate).

**D-21 — Inline touch-ups:**
- Plug section: `` `capture-only` `` lane — stop after this section
- Integrations section: `` `sigra-reference` `` lane label
- Operator-surface mention: required for `phoenix-surface` + reference lanes

**D-23 (optional):** If upgrade-path "How to tell which lane" prose order drifts from canonical four-lane order, harmonize only if trivial one-line edit.

**D-22 — Contract test** `"integration-contracts guide locks four-lane vocabulary and upgrade-path cross-link"`:
- Assert `## Adoption lanes and integration seams`
- Assert `guides/upgrade-path.md` link
- `:binary.match` indices for `capture-only`, `phoenix-surface`, `phx-gen-auth-reference`, `sigra-reference` — strictly increasing
- `refute String.contains?(guide, "| Lane | Claim type |")`

**D-09:** Do not duplicate README matrix or upgrade_path matrix rows here.
  </action>
  <acceptance_criteria>
    - `grep -F '## Adoption lanes and integration seams' guides/integration-contracts.md` exits 0
    - `grep -F 'guides/upgrade-path.md' guides/integration-contracts.md` exits 0
    - `grep -E 'capture-only|phoenix-surface|phx-gen-auth-reference|sigra-reference' guides/integration-contracts.md` shows all four in lane section
    - `grep -F '| Lane | Claim type |' guides/integration-contracts.md` exits 1
    - `mix test test/threadline/integration_contracts_doc_contract_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/integration_contracts_doc_contract_test.exs</verify>
</task>

</tasks>

<verification>
```bash
mix test test/threadline/how_threadline_works_doc_contract_test.exs
mix test test/threadline/integration_contracts_doc_contract_test.exs
mix verify.doc_contract
mix ci.all
```
</verification>

<success_criteria>
- DOC-04: Host-write boundary SSOT; stale "may persist" removed; contracts green
- DOC-05: Four-lane section + upgrade-path link; matrix refuted in integration-contracts
- Wave closeout: `mix verify.doc_contract` and `mix ci.all` green
- All D-01–D-26 decisions from CONTEXT addressed or explicitly deferred (example `:schemas`, D-23 optional only)
</success_criteria>
