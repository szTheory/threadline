---
phase: 68
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/upgrade-path.md
  - guides/operator-surface.md
  - CHANGELOG.md
  - mix.exs
  - test/threadline/operator_surface_doc_contract_test.exs
  - test/threadline/upgrade_path_doc_contract_test.exs
autonomous: true
requirements:
  - ADOPT-06
user_setup: []
tags:
  - elixir
  - threadline
  - docs
  - compatibility
  - optional-deps
  - doc-contract

must_haves:
  truths:
    - "`guides/upgrade-path.md` is the canonical lifecycle guide per D-64, not `guides/operator-surface.md`."
    - "Compatibility claims stay narrow and honest per D-68 through D-70: capture-only is proven by `mix verify.compile_no_optional`, and surface-mounted claims do not exceed declared optional deps, current lock resolution, and current CI coverage."
    - "The guide clearly tells adopters whether they are capture-only or surface-mounted per D-69."
    - "The guide includes the locked section architecture from D-67, including the surface-only deprecation/removal policy from D-71 and D-72."
    - "`guides/operator-surface.md` stays focused on mount/auth/screens and links out to the upgrade-path guide per D-65."
  artifacts:
    - path: "guides/upgrade-path.md"
      provides: "Canonical optional-deps compatibility and lifecycle guide"
      contains: "## Supported compatibility matrix"
    - path: "guides/operator-surface.md"
      provides: "Mount/auth/screens guide with an explicit link to the upgrade-path guide"
      contains: "guides/upgrade-path.md"
    - path: "CHANGELOG.md"
      provides: "Release-history note for the surface-only compatibility/deprecation policy"
      contains: "upgrade-path"
    - path: "mix.exs"
      provides: "ExDoc extras wiring for the new canonical guide"
      contains: "\"guides/upgrade-path.md\""
    - path: "test/threadline/upgrade_path_doc_contract_test.exs"
      provides: "Focused source-reading contract for matrix headers, track detection, and policy literals"
    - path: "test/threadline/operator_surface_doc_contract_test.exs"
      provides: "Cross-link contract keeping operator-surface docs scoped correctly"
  key_links:
    - from: "guides/upgrade-path.md"
      to: "mix.exs"
      via: "declared optional dependency ranges are the support-floor source of truth"
      pattern: "optional: true"
    - from: "guides/upgrade-path.md"
      to: "mix.lock"
      via: "current tested resolution row"
      pattern: "phoenix"
    - from: "guides/upgrade-path.md"
      to: ".github/workflows/ci.yml"
      via: "CI-covered support claims and stable job topology"
      pattern: "verify-compile-no-optional|verify-test"
    - from: "guides/operator-surface.md"
      to: "guides/upgrade-path.md"
      via: "mount/auth guide delegates lifecycle policy to the canonical upgrade guide"
      pattern: "upgrade-path"
---

<objective>
Close ADOPT-06 by adding one canonical upgrade-path guide for the optional Phoenix/LiveView/HTML/PubSub posture, then lock its support matrix and policy language with focused doc contracts.

Purpose: adopters need an honest answer to three lifecycle questions that the current docs do not settle cleanly: which track they are on, what support is actually claimed, and how surface-only compatibility/deprecations move between Threadline minors. This plan implements D-64 through D-72 without letting lifecycle policy sprawl into the operator-surface guide.

Output: a new `guides/upgrade-path.md`, additive operator-surface/changelog/ExDoc wiring, and a focused contract test for the compatibility matrix and policy literals.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/68-lifecycle-ergonomics/68-CONTEXT.md
@.planning/phases/68-lifecycle-ergonomics/68-RESEARCH.md
@guides/operator-surface.md
@mix.exs
@mix.lock
@.github/workflows/ci.yml
@test/threadline/operator_surface_doc_contract_test.exs

Locked decisions to implement directly:

- D-64: create `guides/upgrade-path.md` as the canonical guide.
- D-65: keep `guides/operator-surface.md` focused on mount/auth/screens.
- D-66: discovery only in README; no compatibility authority there.
- D-67: use the locked section architecture.
- D-68/D-69/D-70: claims must derive only from declared optional deps, current lock resolution, and current CI coverage.
- D-71/D-72: surface-only public contract and deprecation/removal overlap policy are explicit.

Compatibility source of truth already in-repo:

```elixir
{:phoenix, "~> 1.7", optional: true},
{:phoenix_live_view, "~> 1.0", optional: true},
{:phoenix_html, "~> 4.0", optional: true},
{:phoenix_pubsub, "~> 2.1", optional: true}
```

Current lock resolution already in-repo:

- Phoenix `1.8.7`
- Phoenix LiveView `1.1.30`
- Phoenix HTML `4.3.0`
- Phoenix PubSub `2.2.0`

CI support anchors already in-repo:

- stable job IDs in `.github/workflows/ci.yml`
- `mix verify.compile_no_optional`
- `mix ci.all`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical upgrade-path guide and wire the scoped cross-links</name>
  <files>
    guides/upgrade-path.md,
    guides/operator-surface.md,
    CHANGELOG.md,
    mix.exs
  </files>
  <action>
    Implement ADOPT-06 exactly, using the locked structure from D-67 in `guides/upgrade-path.md`:

    1. `Who this guide is for`
    2. `How to tell which track you are on`
    3. `Supported compatibility matrix`
    4. `Upgrade by Threadline minor`
    5. `What breaks when Phoenix/LiveView floors move`
    6. `Surface-only deprecation and removal policy`
    7. `Release checklist for adopters`
    8. `Canonical references`

    Fill the matrix and narrative conservatively:

    - capture-only support is tied to no optional Phoenix deps plus `mix verify.compile_no_optional`, per D-69
    - surface-mounted support is tied only to the declared optional dep ranges in `mix.exs`, the current lock resolution in `mix.lock`, and the currently covered CI surface in `.github/workflows/ci.yml`, per D-68 through D-70
    - anything outside the listed ranges is explicitly "not claimed", even if it may work

    Keep `guides/operator-surface.md` focused on mount/auth/screens per D-65 and link it to `guides/upgrade-path.md` for compatibility/support policy. Add the new guide to `mix.exs` docs extras, and add a changelog note that points readers at the new lifecycle policy guide.

    Do not invent broader support promises, do not add a new CI matrix, and do not move compatibility authority into README or `guides/operator-surface.md`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface_doc_contract_test.exs && MIX_ENV=dev mix docs</automated>
  </verify>
  <done>
    `guides/upgrade-path.md` exists with the locked sections and honest compatibility language, `guides/operator-surface.md` links out instead of absorbing lifecycle policy, `mix.exs` exposes the guide in ExDoc extras, and CHANGELOG points to the new canonical guide.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add focused contract coverage for matrix headers, track detection, and policy literals</name>
  <files>
    test/threadline/operator_surface_doc_contract_test.exs,
    test/threadline/upgrade_path_doc_contract_test.exs
  </files>
  <action>
    Keep the contract surface disciplined:

    - extend `operator_surface_doc_contract_test.exs` only for cross-link and scope assertions that prove `guides/operator-surface.md` stays a mount/auth/screens guide and points to `guides/upgrade-path.md`
    - add a new focused `test/threadline/upgrade_path_doc_contract_test.exs` that source-reads the guide and locks:
      - the section headings required by D-67
      - the compatibility matrix headers
      - the "capture-only" vs "surface-mounted" track-detection language
      - the narrow support-claim wording from D-68 through D-70
      - the surface-only deprecation/removal overlap policy from D-71 and D-72
      - the `mix verify.compile_no_optional` proof point
      - the ExDoc extras entry in `mix.exs`

    Prefer pure source-reading assertions. The test should fail if the docs start claiming broader Phoenix/LiveView compatibility than the repo actually declares and covers.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/upgrade_path_doc_contract_test.exs test/threadline/ci_topology_contract_test.exs</automated>
  </verify>
  <done>
    CI will fail if the upgrade-path guide loses its locked sections, broadens its support claims beyond declared/tested ranges, or if the operator-surface guide absorbs lifecycle policy instead of linking to the canonical guide.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared deps / lock / CI evidence -> published support policy | Compatibility guidance must not exceed what the repo actually declares and verifies |
| operator-surface guide -> lifecycle policy guide | Readers need a clean boundary between mount/auth instructions and upgrade/support policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04 | I | compatibility guide | mitigate | Derive every support claim only from `mix.exs`, `mix.lock`, and `.github/workflows/ci.yml`; the focused doc-contract test should fail on broader prose. |
| T-68-05 | T | operator-surface docs | mitigate | Keep `guides/operator-surface.md` scoped to mount/auth/screens and lock the cross-link to `guides/upgrade-path.md` so lifecycle policy does not drift into the wrong document. |
| T-68-06 | R | deprecation/removal policy | mitigate | Lock the overlap-window and exception language in a dedicated source-reading contract test so future edits cannot silently narrow the surface-only contract. |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface_doc_contract_test.exs test/threadline/upgrade_path_doc_contract_test.exs test/threadline/ci_topology_contract_test.exs`
- `mix verify.compile_no_optional`
</verification>

<success_criteria>
- ADOPT-06 is satisfied with one canonical guide that answers track detection, support matrix, upgrade movement, and deprecation/removal policy.
- Compatibility claims do not exceed declared optional deps, current lock resolution, or current CI coverage.
- The operator-surface guide remains focused on mount/auth/screens and links out for lifecycle policy.
</success_criteria>

<output>
After completion, create `.planning/phases/68-lifecycle-ergonomics/68-02-SUMMARY.md`
</output>
