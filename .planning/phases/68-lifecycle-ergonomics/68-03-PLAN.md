---
phase: 68
plan: 03
type: execute
wave: 2
depends_on:
  - 68-01
  - 68-02
files_modified:
  - .planning/phases/68-lifecycle-ergonomics/68-VERIFICATION.md
  - .planning/phases/68-lifecycle-ergonomics/68-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/MILESTONES.md
  - .planning/v1.16-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - ADOPT-07
user_setup: []
tags:
  - elixir
  - threadline
  - verification
  - planning
  - ci
  - formatter

must_haves:
  truths:
    - "ADOPT-07 is treated as evidence capture plus stale artifact cleanup per D-73, not speculative formatter churn."
    - "`mix verify.format` and `mix ci.all` are re-run against the final Phase 68 tree and recorded with the concrete 2026-05-07 evidence trail."
    - "Stable GitHub Actions job IDs and `ci.all` ordering remain unchanged; the phase proves that via existing CI topology contracts rather than changing CI design."
    - "Active planning artifacts stop claiming that repo-wide formatter drift still blocks `mix ci.all`."
    - "Historical stale references that still imply an open blocker are reconciled in the explicitly named planning artifacts, without broadening scope into unrelated CI cleanup."
  artifacts:
    - path: ".planning/phases/68-lifecycle-ergonomics/68-VERIFICATION.md"
      provides: "Execution evidence for the Phase 68 doc and CI-closeout slice"
      contains: "mix ci.all"
    - path: ".planning/phases/68-lifecycle-ergonomics/68-VALIDATION.md"
      provides: "Requirement and evidence traceability for ADOPT-07 closeout"
      contains: "ADOPT-07"
    - path: ".planning/STATE.md"
      provides: "Retired live blocker wording for repo-wide format drift"
      contains: "repo-wide-format-drift"
    - path: ".planning/ROADMAP.md"
      provides: "Active roadmap text no longer implying an unresolved format blocker"
      contains: "Phase 68: Lifecycle Ergonomics"
    - path: ".planning/MILESTONES.md"
      provides: "Milestone summary language reconciled to the now-green formatter/CI state"
      contains: "v1.16"
    - path: ".planning/v1.16-MILESTONE-AUDIT.md"
      provides: "Historical audit note updated so it no longer reads like a current open blocker"
      contains: "format drift"
  key_links:
    - from: ".planning/phases/68-lifecycle-ergonomics/68-VERIFICATION.md"
      to: "mix.exs"
      via: "records canonical verification entrypoints and `ci.all` ordering evidence"
      pattern: "\"ci.all\": \\["
    - from: ".planning/phases/68-lifecycle-ergonomics/68-VERIFICATION.md"
      to: ".github/workflows/ci.yml"
      via: "records stable job-ID evidence without changing topology"
      pattern: "verify-format|verify-credo|verify-compile-no-optional|verify-test|verify-pgbouncer-topology"
    - from: ".planning/STATE.md"
      to: ".planning/v1.16-MILESTONE-AUDIT.md"
      via: "stale blocker wording is retired consistently across active and historical planning artifacts"
      pattern: "format drift"
---

<objective>
Close ADOPT-07 as an auditable blocker-retirement slice: capture fresh formatter/CI evidence on the final Phase 68 tree, prove CI topology stayed stable, and clean up the stale planning language that still claims repo-wide format drift blocks `mix ci.all`.

Purpose: per D-73 through D-75, this plan should not reopen generic CI work. It should document that the blocker is already gone on 2026-05-07, preserve stable job IDs and `ci.all` ordering, and reconcile the planning artifacts that still describe the old problem as live.

Output: Phase 68 verification/validation artifacts plus focused planning-document updates that retire the stale blocker language cleanly.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/v1.16-MILESTONE-AUDIT.md
@.planning/phases/68-lifecycle-ergonomics/68-CONTEXT.md
@.planning/phases/68-lifecycle-ergonomics/68-RESEARCH.md
@mix.exs
@.github/workflows/ci.yml
@test/threadline/ci_topology_contract_test.exs
@test/threadline/phase06_nyquist_ci_contract_test.exs

Locked decisions to implement directly:

- D-73: treat ADOPT-07 as evidence capture + stale artifact cleanup unless drift reappears.
- D-74: do not broaden into generic CI cleanup.
- D-75: make the closeout auditable and update planning/state wording so it no longer implies an unresolved blocker.

Current stale blocker references already identified:

- `.planning/STATE.md` open todo / blocker / tech-debt lines for repo-wide format drift
- `.planning/ROADMAP.md` Phase 68 wording that still frames the blocker as future cleanup
- `.planning/MILESTONES.md` and `.planning/v1.16-MILESTONE-AUDIT.md` notes that still read like a live repo-wide blocker

Existing CI/topology contracts to preserve rather than edit:

- stable job IDs in `.github/workflows/ci.yml`
- `mix verify.format`
- `mix ci.all`
- `test/threadline/ci_topology_contract_test.exs`
- `test/threadline/phase06_nyquist_ci_contract_test.exs`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture fresh ADOPT-07 evidence on the final Phase 68 tree</name>
  <files>
    .planning/phases/68-lifecycle-ergonomics/68-VERIFICATION.md,
    .planning/phases/68-lifecycle-ergonomics/68-VALIDATION.md
  </files>
  <action>
    After Plans 68-01 and 68-02 are complete, run the existing verification surface and write explicit evidence artifacts:

    - `mix verify.format`
    - `mix ci.all`
    - `mix test test/threadline/ci_topology_contract_test.exs test/threadline/phase06_nyquist_ci_contract_test.exs`

    Record the concrete execution date (`2026-05-07` if that remains the run date), the commands, and the outcome in `68-VERIFICATION.md`. In `68-VALIDATION.md`, map ADOPT-07 back to the exact evidence and explain that the requirement was satisfied through blocker retirement and unchanged CI topology, not by adding or redesigning jobs.

    If formatter drift or `ci.all` failure reappears for a fresh reason, stop and document that blocker instead of broadening scope into unrelated fixes. If job IDs or `ci.all` ordering differ from the established contracts, treat that as a blocking regression and do not paper over it in the docs.
  </action>
  <verify>
    <automated>mix verify.format && mix ci.all && mix test test/threadline/ci_topology_contract_test.exs test/threadline/phase06_nyquist_ci_contract_test.exs</automated>
  </verify>
  <done>
    `68-VERIFICATION.md` and `68-VALIDATION.md` exist, cite the exact commands and date, and prove ADOPT-07 on the final Phase 68 tree without changing CI topology or claiming broader cleanup than was actually performed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retire the stale format-blocker language in the planning artifacts only</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/MILESTONES.md,
    .planning/v1.16-MILESTONE-AUDIT.md
  </files>
  <action>
    Reconcile the named planning artifacts so they stop implying repo-wide formatter drift is still an unresolved blocker:

    - remove or rewrite the active `STATE.md` todo/blocker/tech-debt entries so they reflect that Phase 68 retired the blocker
    - update the Phase 68 text in `ROADMAP.md` so it reflects evidence-backed closeout language rather than a presumed future cleanup sweep
    - update `MILESTONES.md` and `.planning/v1.16-MILESTONE-AUDIT.md` where they still read like a current live blocker, preserving the historical fact that drift once existed while making clear it is no longer open

    Keep this scope narrow per D-74: no unrelated CI cleanup, no workflow redesign, no new job IDs, and no opportunistic edits to requirements outside the ADOPT-07 closeout evidence. Preserve the existing CI job IDs and `ci.all` order exactly; prove them via the evidence captured in Task 1 rather than editing CI files.
  </action>
  <verify>
    <automated>mix test test/threadline/ci_topology_contract_test.exs test/threadline/phase06_nyquist_ci_contract_test.exs</automated>
  </verify>
  <done>
    The named planning artifacts no longer describe repo-wide format drift as a current blocker, while the historical record still accurately says the problem existed and was later retired in Phase 68.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final Phase 68 tree -> verification evidence | Evidence must describe the actual final docs/tests tree, not an earlier intermediate state |
| planning artifacts -> future maintainers | Stale blocker wording can mislead maintainers into reopening solved CI debt or hiding a new regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-07 | R | ADOPT-07 evidence | mitigate | Capture exact commands, date, and outcomes in `68-VERIFICATION.md` and `68-VALIDATION.md`; do not claim success without fresh runs on the final tree. |
| T-68-08 | T | planning artifacts | mitigate | Limit edits to the explicitly named stale references and preserve historical context while removing the "still blocked" wording. |
| T-68-09 | D | CI topology | mitigate | Treat any job-ID or `ci.all` ordering drift as blocking; verify via the existing topology contract tests instead of changing CI. |
</threat_model>

<verification>
- `mix verify.format`
- `mix ci.all`
- `mix test test/threadline/ci_topology_contract_test.exs test/threadline/phase06_nyquist_ci_contract_test.exs`
</verification>

<success_criteria>
- ADOPT-07 is satisfied as evidence capture plus stale artifact cleanup, consistent with D-73 through D-75.
- The final Phase 68 tree has fresh recorded `mix verify.format` and `mix ci.all` evidence.
- Planning artifacts no longer claim repo-wide formatter drift is an active blocker, and CI topology remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/68-lifecycle-ergonomics/68-03-SUMMARY.md`
</output>
