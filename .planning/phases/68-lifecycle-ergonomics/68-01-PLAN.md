---
phase: 68
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/getting-started-saas.md
  - README.md
  - examples/threadline_phoenix/README.md
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - test/threadline/getting_started_saas_doc_contract_test.exs
  - test/threadline/readme_doc_contract_test.exs
  - test/threadline/example_phoenix_readme_contract_test.exs
autonomous: true
requirements:
  - ADOPT-05
user_setup: []
tags:
  - elixir
  - threadline
  - docs
  - onboarding
  - operator-surface
  - doc-contract

must_haves:
  truths:
    - "`guides/getting-started-saas.md` becomes the one canonical first-hour path and reaches a mounted operator surface end-to-end instead of stopping at IEx-only investigation."
    - "`README.md` stays short per D-60: it remains the entry map with a compact mount pointer, not a second full walkthrough."
    - "`examples/threadline_phoenix/README.md` stays runnable-contract-first per D-61 while still showing the mounted operator-surface path."
    - "The operator-surface mount snippet is shared verbatim across docs via the example router source, not hand-copied prose, per D-63."
    - "Capture-only remains a subordinate note or branch, not a parallel top-level onboarding architecture, per D-62."
  artifacts:
    - path: "guides/getting-started-saas.md"
      provides: "Canonical install-to-mounted-surface walkthrough per D-59"
      contains: "threadline_operator_surface"
    - path: "README.md"
      provides: "Short entry-map quickstart with 1-minute operator-surface pointer per D-60"
      contains: "## Operator Surface"
    - path: "examples/threadline_phoenix/README.md"
      provides: "Runnable reference-app contract with mounted-surface runbook per D-61"
      contains: "/audit"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      provides: "Verbatim snippet source for the operator-surface mount block"
      contains: "threadline_operator_surface \"/\""
    - path: "test/threadline/getting_started_saas_doc_contract_test.exs"
      provides: "Doc-contract coverage for the canonical first-hour mounted-surface story"
    - path: "test/threadline/readme_doc_contract_test.exs"
      provides: "README entry-map contract preserving the short posture while adding lifecycle pointers"
    - path: "test/threadline/example_phoenix_readme_contract_test.exs"
      provides: "Runnable-example contract coverage for the mounted operator-surface story"
  key_links:
    - from: "guides/getting-started-saas.md"
      to: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      via: "shared extracted mount snippet, using a new router anchor if needed"
      pattern: "threadline_operator_surface"
    - from: "README.md"
      to: "guides/getting-started-saas.md"
      via: "short discovery pointer only, per D-60"
      pattern: "guides/getting-started-saas.md"
    - from: "examples/threadline_phoenix/README.md"
      to: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      via: "runnable-contract mount path stays aligned with the real example router"
      pattern: "/audit"
---

<objective>
Close ADOPT-05 by making the first-hour adoption story actually mount the shipped operator surface, while preserving the locked doc hierarchy: canonical walkthrough in `guides/getting-started-saas.md`, short discovery posture in `README.md`, and runnable-contract posture in `examples/threadline_phoenix/README.md`.

Purpose: Phase 68 should remove the current adoption gap where Threadline documents capture and investigation but stops short of the now-shipped `/audit` surface. This plan implements D-59 through D-63 without creating two equal onboarding flows.

Output: updated onboarding docs, a router-backed mount snippet source, and extended existing doc-contract tests that pin the mounted-surface path.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/68-lifecycle-ergonomics/68-CONTEXT.md
@.planning/phases/68-lifecycle-ergonomics/68-RESEARCH.md
@README.md
@guides/getting-started-saas.md
@examples/threadline_phoenix/README.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@test/threadline/getting_started_saas_doc_contract_test.exs
@test/threadline/readme_doc_contract_test.exs
@test/threadline/example_phoenix_readme_contract_test.exs
@test/support/getting_started_fixtures.ex

Locked decisions to implement directly:

- D-59: `guides/getting-started-saas.md` is canonical.
- D-60: `README.md` stays short and skimmable.
- D-61: example README stays runnable-contract-first.
- D-62: do not create two equal first-hour flows.
- D-63: duplicate only high-signal snippets; extract from real source where possible.

Source-backed mount contract to reuse or anchor in the example router:

```elixir
scope "/audit" do
  pipe_through [:browser, :admin_auth]

  threadline_operator_surface "/",
    actor_fn: &ThreadlinePhoenixWeb.Router.my_actor_fn/1,
    authorize_fn: &ThreadlinePhoenixWeb.Router.my_authorize_fn/1,
    repo: ThreadlinePhoenix.Repo
end
```

Executor rule: if `GettingStartedFixtures.extract!/2` cannot already extract this block cleanly, add one explicit doc anchor around the mount snippet in `examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` and drive all doc reuse from that anchor.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the first-hour docs around the mounted operator-surface path</name>
  <files>
    guides/getting-started-saas.md,
    README.md,
    examples/threadline_phoenix/README.md,
    examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  </files>
  <action>
    Update the docs so ADOPT-05 is satisfied exactly:

    - Extend `guides/getting-started-saas.md` per D-59 into one canonical flow that goes from install through `threadline_operator_surface` mount, visiting the shipped `/audit` surface after the existing capture/investigation steps. Keep capture-only as a secondary note, not a peer flow, per D-62.
    - Keep `README.md` short per D-60. It may keep the high-signal "1-minute mount" snippet and point to the canonical walkthrough plus the operator-surface guide, but it must not become a second full walkthrough.
    - Keep `examples/threadline_phoenix/README.md` runnable-contract-first per D-61: reference the mounted `/audit` path, the admin pipeline posture, and the example router wiring without turning this README into the primary narrative.
    - Reuse real example code for the operator-surface mount snippet per D-63. Add a new router anchor only if required for snippet extraction; make that change explicitly in `examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex`.

    Do not introduce a separate top-level "capture-only quickstart", do not invent new auth architecture beyond the existing `phx.gen.auth`-style admin pipeline, and do not let README prose drift away from the canonical guide.
  </action>
  <verify>
    <automated>mix test test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs && mix verify.example</automated>
  </verify>
  <done>
    The canonical guide reaches a mounted operator surface end-to-end, README remains an entry map with a compact mount pointer, the example README remains runnable-contract-first, and any shared mount snippet comes from the real example router source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend the existing doc-contract tests for the mounted-surface onboarding story</name>
  <files>
    test/threadline/getting_started_saas_doc_contract_test.exs,
    test/threadline/readme_doc_contract_test.exs,
    test/threadline/example_phoenix_readme_contract_test.exs
  </files>
  <action>
    Extend the current contract suites rather than creating a new generic onboarding test:

    - `getting_started_saas_doc_contract_test.exs` must assert the canonical walkthrough now includes the mounted operator-surface path, the mount snippet appears verbatim via fixture extraction, and the flow still points at the shipped first-hour commands and investigation APIs.
    - `readme_doc_contract_test.exs` must preserve the README "entry-map" posture while locking the high-signal operator-surface mount pointer and canonical guide links.
    - `example_phoenix_readme_contract_test.exs` must lock the example README’s mounted `/audit` surface references and the `phx.gen.auth`-style admin pipeline posture without turning it into the main narrative.

    Prefer pure source-reading assertions and fixture extraction over brittle markdown-shape matching. Reuse the existing `GettingStartedFixtures.extract!/2` infrastructure aggressively.
  </action>
  <verify>
    <automated>mix test test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs</automated>
  </verify>
  <done>
    CI fails if the canonical mounted-surface onboarding story, the README’s short posture, or the example README’s runnable-contract posture drifts from the real router-backed snippet and route literals.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example router source -> published docs | Shared mount snippets must stay derived from runnable source rather than drifting into hand-maintained prose |
| docs -> adopter auth posture | Mount instructions must not weaken the host-owned admin/auth boundary while simplifying onboarding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | T | onboarding docs | mitigate | Reuse extracted router-backed snippet per D-63 and lock it with doc-contract tests so prose cannot drift away from the actual mount contract. |
| T-68-02 | E | operator-surface mount guidance | mitigate | Keep the `phx.gen.auth`-style admin pipeline and host-owned `authorize_fn` posture explicit; do not introduce unauthenticated or implied-safe examples. |
| T-68-03 | S | README guidance | mitigate | Preserve README as a short discovery surface per D-60 so adopters are routed to the canonical guide instead of following an incomplete second flow. |
</threat_model>

<verification>
- `mix test test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs`
- `mix verify.example`
</verification>

<success_criteria>
- ADOPT-05 is satisfied with one obvious path: canonical walkthrough in `guides/getting-started-saas.md`, short README pointer, runnable example README.
- The mounted operator-surface snippet is source-derived and contract-tested.
- The docs teach the shipped `/audit` surface without weakening the host-owned auth boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/68-lifecycle-ergonomics/68-01-SUMMARY.md`
</output>
