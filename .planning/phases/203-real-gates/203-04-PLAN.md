---
phase: 203-real-gates
plan: 04
type: execute
wave: 4
depends_on: ["203-03"]
files_modified:
  - test/mix/tasks/threadline.evidence_show_test.exs
  - test/mix/tasks/threadline.incident_test.exs
  - test/mix/tasks/threadline/export_test.exs
  - test/support/stress_router_prod_compile.exs
  - test/threadline/audit_transaction_test.exs
  - test/threadline/continuity_brownfield_test.exs
  - test/threadline/health_test.exs
  - test/threadline/verify_coverage_task_test.exs
  - test/threadline/capture/trigger_context_test.exs
  - test/threadline/capture/trigger_test.exs
  - test/threadline/export/cleanup_test.exs
  - test/threadline/export/orchestrator_test.exs
  - test/threadline/export_queue/oban_test.exs
  - test/threadline/storage/s3_test.exs
autonomous: true
requirements: [GATE-01, GATE-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Under Credo's full defaults, `Design.AliasUsage` reports 0 findings in every test file outside `test/threadline/operator_surface/` (116 findings across 14 non-contract files on 2026-09-22), with no threshold delta and no path scoping (D-01, D-03)."
    - "No AliasOrder finding is created by an inserted alias; any it causes is fixed in the same commit (D-10, D-28)."
    - "Commits follow D-28: non-contract test files batched by directory, type `refactor`, pure mechanical, blame-ignorable."
    - "Executors read only alias blocks and flagged lines, per Plan 03's shared procedure, so the plan fits one context budget."
  artifacts:
    - test/threadline/export/cleanup_test.exs
    - test/threadline/export/orchestrator_test.exs
  key_links:
    - "`test/support/stress_router_prod_compile.exs` is compiled by `test/threadline/operator_surface/stress_router_test.exs` in a prod-like mode, so alias resolution inside router `scope` blocks is proven by that test, which this plan runs."
  prohibitions:
    - "AliasUsage is never disabled, re-parameterized, or path-scoped (D-03)."
    - "No semantic edit rides in an alias commit."
    - "No `# credo:disable` or `# credo:enable` comment is added."
---

<objective>
Pay down the `Design.AliasUsage` findings in the non-contract test files outside `test/threadline/operator_surface/` (second of three alias plans).

Purpose: GATE-01/GATE-02 via D-01 and D-03. Same rationale as Plan 03: cheap mechanical debt is paid, not registered.

Output: 0 AliasUsage findings in these 14 files, committed per directory.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-real-gates/203-CONTEXT.md
@.planning/phases/203-real-gates/203-03-PLAN.md
@CLAUDE.md
</context>

## Procedure

Follow the **Shared procedure (Plans 03, 04, 05)** section of `203-03-PLAN.md` exactly: measurement command, read scope (alias blocks and flagged lines only), per-pair aliasing rules, per-file compile and test, AliasOrder handling, and the D-28 commit template with `203-04` in place of `203-NN`. The base file for this plan is `"$(git rev-parse --git-dir)/gsd-203-04-base"`.

<tasks>

<task type="auto">
  <name>Task 1: Alias sweep in test/mix/tasks, test/support, and top-level test/threadline files</name>
  <files>test/mix/tasks/threadline.evidence_show_test.exs, test/mix/tasks/threadline.incident_test.exs, test/mix/tasks/threadline/export_test.exs, test/support/stress_router_prod_compile.exs, test/threadline/audit_transaction_test.exs, test/threadline/continuity_brownfield_test.exs, test/threadline/health_test.exs, test/threadline/verify_coverage_task_test.exs</files>
  <read_first>
    - per the shared read scope: each file's alias block and each flagged line with context
    - test/support/stress_router_prod_compile.exs in full (16 lines) and only the part of test/threadline/operator_surface/stress_router_test.exs that loads it (grep for `stress_router_prod_compile`)
  </read_first>
  <action>
    Per D-03, D-10, and D-28:

    (0) Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-203-04-base"`, and copy the SHA into the SUMMARY.

    (a) Apply the shared procedure. Commit in three directory batches: `test/mix/tasks/` (including its `threadline/` subdir), `test/support/`, and top-level `test/threadline/` (the four `*_test.exs` files directly under it).

    (b) In `stress_router_prod_compile.exs`, do not alias a module in route-target position inside a `scope "...", Alias do` block if that changes resolution. Prove it with `stress_router_test.exs`.
  </action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --warnings-as-errors</automated>
    <fails_when>non-zero exit or any `warning:` line</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/mix/tasks/threadline.evidence_show_test.exs test/mix/tasks/threadline.incident_test.exs test/mix/tasks/threadline/export_test.exs test/threadline/audit_transaction_test.exs test/threadline/continuity_brownfield_test.exs test/threadline/health_test.exs test/threadline/verify_coverage_task_test.exs test/threadline/operator_surface/stress_router_test.exs</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`; a stress_router failure means a route target was aliased in a way that changed resolution</fails_when>
  </verify>
  <acceptance_criteria>
    - `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix compile >/dev/null 2>&1; mix credo --strict --config-file deps/credo/.credo.exs --format json' | jq -e '[.issues[] | select(.check=="Credo.Check.Design.AliasUsage" and ((.filename | startswith("test/mix/")) or (.filename | startswith("test/support/")) or (.filename | test("^test/threadline/[^/]+_test\\.exs$"))))] | length == 0'` prints `true`
    - Each commit in this task touches files from exactly one of the three directories
  </acceptance_criteria>
  <done>AliasUsage is 0 in test/mix, test/support, and top-level test/threadline files.</done>
</task>

<task type="auto">
  <name>Task 2: Alias sweep in test/threadline/{capture,export,export_queue,storage}</name>
  <files>test/threadline/capture/trigger_context_test.exs, test/threadline/capture/trigger_test.exs, test/threadline/export/cleanup_test.exs, test/threadline/export/orchestrator_test.exs, test/threadline/export_queue/oban_test.exs, test/threadline/storage/s3_test.exs</files>
  <read_first>
    - per the shared read scope: each file's alias block and each flagged line with context (cleanup_test.exs has 29 findings and orchestrator_test.exs 26; most are `Threadline.Test.Repo`, `Threadline.Storage.Local`, or `Ecto.Adapters.SQL`, so one alias line per module fixes many call sites)
  </read_first>
  <action>
    Per D-03, D-10, and D-28, apply the shared procedure. Commit in four directory batches: `test/threadline/capture/`, `test/threadline/export/`, `test/threadline/export_queue/`, `test/threadline/storage/`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/capture test/threadline/export test/threadline/export_queue test/threadline/storage</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix compile >/dev/null 2>&1; mix credo --strict --config-file deps/credo/.credo.exs --format json' | jq -e '([.issues[] | select(.check=="Credo.Check.Design.AliasUsage" and (.filename | startswith("test/threadline/operator_surface/") | not))] | length == 0) and ([.issues[] | select(.check=="Credo.Check.Readability.AliasOrder") | .filename] - ["lib/threadline.ex","lib/threadline/operator_surface/live/coverage_live.ex","lib/threadline/operator_surface/live/export_status_live.ex","lib/threadline/operator_surface/live/retention_history_live.ex","lib/threadline/operator_surface/live/start_live.ex","test/support/data_case.ex","test/support/storage_schema_case.ex","test/threadline/export_queue/task_adapter_test.exs","test/threadline/operator_surface/copy_contract_test.exs","test/threadline/storage_schema_integration_test.exs"] == [])'</automated>
    <fails_when>jq exits 1: an AliasUsage finding remains anywhere outside test/threadline/operator_surface/, or an AliasOrder finding appears in a file outside the pre-existing set</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format</automated>
    <fails_when>non-zero exit: an edited file is not `mix format`-clean; format it and fold the fix into that batch's commit (or a `style(203-04)` commit if already made)</fails_when>
  </verify>
  <acceptance_criteria>
    - All verify commands succeed
    - `BASE=$(cat "$(git rev-parse --git-dir)/gsd-203-04-base") && git rev-parse --verify --quiet "$BASE^{commit}" >/dev/null && L=$(git log --format=%s "$BASE"..HEAD) && printf '%s\n' "$L" | grep -c '^refactor(203-04): alias repeated module references in '` prints `7` (3 batches in Task 1 + 4 in Task 2, or the re-measured batch count stated in SUMMARY); a missing or invalid base exits non-zero before counting
    - `grep -rnE "credo:(disable|enable)" lib test --include='*.ex' --include='*.exs'` prints nothing
  </acceptance_criteria>
  <done>AliasUsage is 0 everywhere except the non-contract operator_surface tests, which Plan 05 owns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mechanical edit → test behavior | A mis-scoped alias can change which module a test exercises. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-08b | Tampering (of test intent) | test alias edits | low | mitigate | `--warnings-as-errors` compile plus each directory's tests run before its commit; no string data is edited. |
| T-203-09b | Tampering (of the gate) | AliasUsage check | medium | mitigate | No delta or disable; the jq gate measures against upstream defaults. |
</threat_model>

<verification>
- Under full defaults, AliasUsage is 0 outside `test/threadline/operator_surface/`, and every AliasOrder finding is in a pre-existing file.
- `mix verify.format` exits 0.
</verification>

<success_criteria>
116 more AliasUsage findings are fixed as pure `refactor` commits, batched by directory.
</success_criteria>

<output>
Create `.planning/phases/203-real-gates/203-04-SUMMARY.md` when done. Record the plan base SHA, the before and after AliasUsage count, the commit SHA list, and every `as:` name chosen.
</output>
