---
phase: 203-real-gates
plan: 05
type: execute
wave: 5
depends_on: ["203-04"]
files_modified:
  - lib/mix/tasks/critic.measure.ex
  - lib/mix/tasks/threadline.export.ex
  - lib/mix/tasks/threadline.install.ex
  - lib/mix/tasks/threadline.verify_coverage.ex
  - lib/threadline/audit.ex
  - lib/threadline/change_diff.ex
  - lib/threadline/critic_trust/krippendorff_alpha.ex
  - lib/threadline/critic_trust/measure.ex
  - lib/threadline/critic_trust/rank_metrics.ex
  - lib/threadline/evidence.ex
  - lib/threadline/export/cleanup_task.ex
  - lib/threadline/export/orchestrator.ex
  - lib/threadline/operator_surface/auth.ex
  - lib/threadline/operator_surface/controllers/export_controller.ex
  - lib/threadline/query/filter_params.ex
  - lib/threadline/operator_surface/live/actor_live.ex
  - lib/threadline/operator_surface/live/export_status_live.ex
  - lib/threadline/operator_surface/live/row_history_component.ex
  - lib/threadline/operator_surface/live/stress_live.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
  - lib/threadline/operator_surface/mechanical_checker.ex
  - lib/threadline/operator_surface/presentation.ex
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/policy/redaction_presenter.ex
  - lib/threadline/query.ex
  - lib/threadline/retention/policy.ex
  - lib/threadline/storage/local.ex
  - test/support/getting_started_fixtures.ex
  - test/threadline/dialyzer_ignore_contract_test.exs
  - test/threadline/guide_graph_contract_test.exs
  - test/threadline/operator_surface/card_nesting_regression_test.exs
  - test/threadline/operator_surface/operator_surface_fixture_contract_test.exs
  - test/threadline/operator_surface/rendered_output_contract_test.exs
  - test/threadline/release_artifact_contract_test.exs
  - test/threadline/credo_config_contract_test.exs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [GATE-02]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every remaining `Refactor.Nesting` and `Refactor.CyclomaticComplexity` finding is filed to Phase 204. Each site carries a per-site `# credo:disable-for-next-line Credo.Check.Refactor.<Check>` directly above the reported line, with nothing after the check name. For Nesting, the reported line is the nested construct, not the `def` (D-07, D-27)."
    - "Directly above each disable is its own comment line, `# Phase 204 (STRUCT-07): <why it needs extraction/splitting>` (D-07, D-27)."
    - "A Nesting site is fixed in place instead of filed only when `with` or an early return flattens it without extracting or splitting a function. The register shrinks accordingly, and nothing is filed wholesale (D-07, D-13)."
    - "Thresholds are untouched: `max_nesting 2` and `max_complexity 9` stay at upstream defaults. No check is disabled (D-05)."
    - "The register lives in source, in `test/threadline/credo_config_contract_test.exs`: `@register %{Nesting => {n, \"Phase 204 / STRUCT-07\"}, CyclomaticComplexity => {m, \"Phase 204 / STRUCT-07\"}}`, `@ceiling == n + m`, and a literal `@historical_max 46` that `@ceiling` may never exceed. No gate reads the planning directory (D-08, D-27; DECOUPLE-01)."
    - "The scanned per-check disable count must EQUAL the register exactly, not just stay within it. One extra disable fails, and one missing disable fails too, so a new site cannot hide in slack left by an in-place fix (GATE-02 boundary; D-27)."
    - "The contract rejects every other `credo:` config-comment form: `disable-for-this-file`, `disable-for-lines:N`, `disable-for-previous-line`, nameless `disable-for-next-line`, any check other than the two registered, trailing text after the check name, and any `enable-*` form (D-08(3))."
    - "A disable whose immediately preceding line is not `# Phase 204 (STRUCT-07): ...` fails the contract, including when a blank line separates them (GATE-02 adjacency; D-08(4))."
    - "The contract fails when its scanned file set is empty. A source set with zero disables and a zero register validates as `:ok`, so Phase 204's drain to 0 stays expressible (GATE-02 empty; D-08(5))."
    - "Contract failures list offenders sorted by `{path, line}` (GATE-02 ordering)."
    - "Counts are compared as exact integers, with no percentage, rounding, or tolerance arithmetic anywhere in the register check (GATE-02 precision)."
    - "The contract is a pure function of source text. Running it twice gives the same result, and a doubled `Phase 204` + disable pair above the same line counts twice and fails equality (GATE-02 idempotency)."
    - "Disable-comment discovery mirrors Credo exactly. It reads only real comments via `Code.string_to_quoted_with_comments/1` and applies Credo's regex, so synthetic negative sources held as string literals in the test are invisible to both Credo and the scan."
    - "REQUIREMENTS.md gains STRUCT-07 (requirement text per D-27, a traceability row `STRUCT-07 | Phase 204 | Pending`, and coverage counts 53 to 54 and STRUCT 6 to 7). ROADMAP Phase 204 lists STRUCT-01..STRUCT-07, adds the ratchet success criterion, and mirrors the register counts for humans (D-27)."
    - statement: "If two branches each add or remove a structural disable in parallel and both update the register, the merged tree fails exact equality until reconciled — the gate cannot silently absorb a concurrent change (proven only on a real concurrent merge)."
      verification: backstop
  artifacts:
    - test/threadline/credo_config_contract_test.exs
  key_links:
    - "`# credo:disable-for-next-line` → Credo's `ConfigComment.ignores_issue?/2`, which suppresses only when `line_no_issue == line_no + 1`. A misplaced disable leaves the finding live, and Task 1's full-default measurement catches it."
    - "`credo_config_contract_test.exs` `@register`/`@ceiling` → Phase 204 STRUCT-07 ratchets it toward 0. The ROADMAP mirror is informational only."
  prohibitions:
    - "No function is extracted or split and no module is created to clear a structural finding in this phase; that is Phase 204's work (hard rule)."
    - "No `disable-for-this-file`, `disable-for-lines`, nameless disable, threshold raise, or `disabled:` entry is used for structural findings."
    - "The register successor text never names a planning-directory path; it names `Phase 204 / STRUCT-07`."
    - "`.planning/` is never staged wholesale. REQUIREMENTS.md and ROADMAP.md are staged by explicit path only."
---

<objective>
File the structural findings to Phase 204 honestly. Each site gets a per-site disable plus a named successor, and every site is counted by a source-resident register that ratchets down only through review.

Purpose: GATE-02's "fix or register with an exact count and a named successor". It also enforces the hard rule that extraction and splitting belong to Phase 204, so 203 does not swallow 204.

Output: about 46 annotated sites (fewer if some flatten in place), the register portion of `test/threadline/credo_config_contract_test.exs`, and the STRUCT-07 requirement with its ROADMAP mirror.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-real-gates/203-CONTEXT.md
@.planning/phases/203-real-gates/203-RESEARCH.md
@.planning/phases/203-real-gates/203-PATTERNS.md
@.planning/phases/203-real-gates/203-04-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces (Plan 05)

| Symbol | Kind | Path |
|---|---|---|
| `Threadline.CredoConfigContractTest` (register + ceiling half) | NEW contract test | `test/threadline/credo_config_contract_test.exs` |
| `@register`, `@ceiling`, `@historical_max 46` | module attributes (the enforced register) | same file |
| ~46 `# Phase 204 (STRUCT-07): ...` + `# credo:disable-for-next-line Credo.Check.Refactor.<Check>` pairs | per-site annotations | lib/ and test/ files listed in frontmatter |
| STRUCT-07 | NEW requirement + traceability row | `.planning/REQUIREMENTS.md` |
| Phase 204 STRUCT-07 criterion + register mirror | roadmap entry | `.planning/ROADMAP.md` |

<tasks>

<task type="auto">
  <name>Task 1: Re-measure, then annotate (or flatten in place) every lib structural site</name>
  <files>lib/mix/tasks/critic.measure.ex, lib/mix/tasks/threadline.export.ex, lib/mix/tasks/threadline.install.ex, lib/mix/tasks/threadline.verify_coverage.ex, lib/threadline/audit.ex, lib/threadline/change_diff.ex, lib/threadline/critic_trust/krippendorff_alpha.ex, lib/threadline/critic_trust/measure.ex, lib/threadline/critic_trust/rank_metrics.ex, lib/threadline/evidence.ex, lib/threadline/export/cleanup_task.ex, lib/threadline/export/orchestrator.ex, lib/threadline/operator_surface/auth.ex, lib/threadline/operator_surface/controllers/export_controller.ex, lib/threadline/query/filter_params.ex, lib/threadline/operator_surface/live/actor_live.ex, lib/threadline/operator_surface/live/export_status_live.ex, lib/threadline/operator_surface/live/row_history_component.ex, lib/threadline/operator_surface/live/stress_live.ex, lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/operator_surface/live/transaction_live.ex, lib/threadline/operator_surface/mechanical_checker.ex, lib/threadline/operator_surface/presentation.ex, lib/threadline/operator_surface/router.ex, lib/threadline/policy/redaction_presenter.ex, lib/threadline/query.ex, lib/threadline/retention/policy.ex, lib/threadline/storage/local.ex</files>
  <read_first>
    - the live structural measurement: `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix compile >/dev/null 2>&1; mix credo --strict --config-file deps/credo/.credo.exs --format json' | jq -r '.issues[] | select(.check=="Credo.Check.Refactor.Nesting" or .check=="Credo.Check.Refactor.CyclomaticComplexity") | "\(.filename):\(.line_no) \(.check) \(.message)"'`. Plans 03-04 shifted every line number, so RESEARCH Appendix A is a cross-check only.
    - each listed file around each reported line, plus the enclosing function in full, to decide between flattening and filing
    - lib/threadline/query/scope.ex (read only; if any structural finding lands in it, file it and do not flatten it, because it is the tenant-scope hook)
  </read_first>
  <action>
    Per D-05, D-07, D-13, D-27, and D-28, work from the live measurement. For each lib site:

    (a) Decide whether to flatten or file. Flatten in place only for a Nesting site where replacing nested `case`/`if` with `with` or an early-return clause removes the finding without creating, extracting, or splitting any function and without changing behavior. Every CyclomaticComplexity site is filed. Reducing complexity below 9 needs a function split, which is Phase 204's work. Do not flatten anything in `query/scope.ex`, `operator_surface/auth.ex`, or `session_plug.ex` (auth/tenant paths); file them.

    (b) To file a site, insert two comment lines directly above the reported line, at that line's indentation. First line: `# Phase 204 (STRUCT-07): ` followed by a short concrete reason, for example "cyclomatic complexity 33 — split resolve!/1 per option group" or "nested case inside reduce — extract the per-row branch". Second line: `# credo:disable-for-next-line Credo.Check.Refactor.Nesting`, or the `CyclomaticComplexity` equivalent, with nothing after the check name. For CyclomaticComplexity, the reported line is the `def`/`defp` head. For Nesting, it is the nested construct.

    (c) After each file, re-run the measurement filtered to that file. Its structural count must be 0. A non-zero count means a disable is on the wrong line. Run `mix compile --warnings-as-errors` and the file's tests. Commit one file per commit: `refactor(203-05): file structural Credo findings to Phase 204 in <path>`, with a body that lists each site as check, line, and reason. If a site was flattened, use `refactor(203-05): flatten nested <construct> in <path>` and keep it separate from the filing commit for that file.

    Keep a running tally of filed Nesting, filed CyclomaticComplexity, and flattened sites for Task 2's register and for the SUMMARY.
  </action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix compile >/dev/null 2>&1; mix credo --strict --config-file deps/credo/.credo.exs --format json' | jq -e '[.issues[] | select((.filename | startswith("lib/")) and (.check=="Credo.Check.Refactor.Nesting" or .check=="Credo.Check.Refactor.CyclomaticComplexity"))] | length == 0'</automated>
    <fails_when>jq exits 1 because a lib structural finding is still live, meaning a disable is misplaced or missing</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`; a flattened site changed behavior</fails_when>
  </verify>
  <acceptance_criteria>
    - The jq gate prints `true`
    - `grep -rn "credo:disable" lib | grep -vE "# credo:disable-for-next-line Credo\.Check\.Refactor\.(Nesting|CyclomaticComplexity)$"` prints nothing
    - For every disable line in lib, the line above matches `# Phase 204 (STRUCT-07): ` (check with `grep -rn -B1 "credo:disable" lib | grep -c "Phase 204 (STRUCT-07): "`, which equals the lib disable count)
    - `git diff <plan-base-sha>..HEAD -- lib/threadline/query/scope.ex | grep -c '^-[^-]'` prints `0` (nothing removed from the `apply/2` body), where `<plan-base-sha>` is `git rev-parse HEAD` recorded in SUMMARY before Task 1
  </acceptance_criteria>
  <done>No lib structural finding is live. Every lib site is filed with its successor line or flattened without extraction, and each file is a separate commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Annotate the test structural sites and create the register contract</name>
  <files>test/support/getting_started_fixtures.ex, test/threadline/dialyzer_ignore_contract_test.exs, test/threadline/guide_graph_contract_test.exs, test/threadline/operator_surface/card_nesting_regression_test.exs, test/threadline/operator_surface/operator_surface_fixture_contract_test.exs, test/threadline/operator_surface/rendered_output_contract_test.exs, test/threadline/release_artifact_contract_test.exs, test/threadline/credo_config_contract_test.exs</files>
  <read_first>
    - the live structural measurement, filtered to `test/`
    - test/threadline/dialyzer_ignore_contract_test.exs in full (the template: `@root`, the throw-based `validate_contract`, `demand!`/`fail!`, the adjacent-comment check near 421-431, the planning-independence guard near 40-41, and synthetic negative tests near 44-58 and 93-116)
    - test/threadline/zero_skips_contract_test.exs lines 44-65 (non-empty glob guard and message style)
    - test/threadline/planning_dependency_contract_test.exs (what the planning-independence guard protects against)
  </read_first>
  <behavior>
    - Positive (real tree): scanning `{lib,test}/**/*.{ex,exs}` returns `:ok` against `@register` and `@ceiling`. Per-check counts equal the register exactly. `@ceiling` equals the register sum. `@ceiling <= @historical_max` and `@historical_max == 46`.
    - Negative, boundary: a synthetic source set with register count n+1 fails, and so does one with n-1. The message contains "adding a disable requires raising the register in review".
    - Negative, forms: `disable-for-this-file`, `disable-for-lines:3`, `disable-for-previous-line`, a nameless `disable-for-next-line`, `disable-for-next-line Credo.Check.Readability.ModuleDoc`, the Nesting disable followed by trailing text, and any `enable-*` instruction each yield `{:error, _}`.
    - Negative, adjacency: a disable whose previous line is blank, or whose previous line lacks the `# Phase 204 (STRUCT-07): ` prefix, or has the prefix with an empty reason, fails.
    - Empty: an empty scanned file set fails with the vacuous-gate message. A non-empty set with zero disables validates `:ok` against a zero register.
    - Ordering: offender lists in error messages are sorted by `{path, line}`.
    - Idempotency: validating the same inputs twice gives identical results. A doubled annotation pair counts as 2.
    - Self-guard: this file's own source does not contain the runtime-assembled planning directory name.
  </behavior>
  <action>
    Per D-07, D-08, D-26 (register portion), D-27, and D-28:

    (a) Annotate each test structural site the same way as Task 1, directly above the reported line. The test files are mostly contract tests, so each file gets its own commit: `refactor(203-05): file structural Credo findings to Phase 204 in <path>`. `test/support/getting_started_fixtures.ex` gets a commit of its own. Re-measure after each file.

    (b) Create `test/threadline/credo_config_contract_test.exs` as `Threadline.CredoConfigContractTest` (`async: true`). Mirror dialyzer_ignore_contract_test's shape: `@root`, `validate_register/3` taking a list of `{path, source}` and returning `:ok | {:error, message}` through throw/catch, and `demand!/2`/`fail!/1` copied verbatim. Discovery parses each source with `Code.string_to_quoted_with_comments/1` and applies Credo's own config-comment regex (`#\s*credo\:([\w\-\:]+)\s*(.*)`, case-insensitive) to each comment's text only. Then enforce every behavior item.

    Set the attributes from Task 1 and 2's actual tallies: `@register` as a map of the two check modules to `{count, "Phase 204 / STRUCT-07"}`, `@ceiling` as a literal integer, and `@historical_max 46`. The main failure message reads: "fix it (prefer `with`/early return); adding a disable requires raising the register in review." Add a prose `@moduledoc` explaining the register, why exact equality is used rather than a ceiling alone, and that Phase 204 (STRUCT-07) ratchets it to 0. Put the register tests in a `describe "structural register (GATE-02)"` block. Do not add any config-shape assertions here. Plan 06 adds them in their own `describe "config shape (GATE-01)"` block once `.credo.exs` is rebuilt.

    (c) Run the test green, then commit the file alone: `refactor(203-05): add Credo structural register contract (GATE-02)`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/credo_config_contract_test.exs</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`, or fewer than 8 tests reported (a missing negative case)</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix compile >/dev/null 2>&1; mix credo --strict --config-file deps/credo/.credo.exs --format json' | jq -e '[.issues[].check] == ["Credo.Check.Warning.MissedMetadataKeyInLoggerConfig"]'</automated>
    <fails_when>jq exits 1: any structural finding is still live anywhere, or anything besides the single config-owned Logger finding remains</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test</automated>
    <fails_when>non-zero exit or a summary other than `0 failures`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "@historical_max 46" test/threadline/credo_config_contract_test.exs` prints `1`
    - `grep -c "Phase 204 / STRUCT-07" test/threadline/credo_config_contract_test.exs` is at least `2`
    - `grep -rn "credo:disable" lib test --include='*.ex' --include='*.exs' | grep -v credo_config_contract_test | wc -l` equals the `@ceiling` literal
    - `grep -c '\.planning' test/threadline/credo_config_contract_test.exs` prints `0`
    - The full-default measurement residue is exactly one `MissedMetadataKeyInLoggerConfig` finding
  </acceptance_criteria>
  <done>Every structural site is filed or flattened. The register is exact, test-pinned, and ratchet-only. The residue under full defaults is the single Logger finding that Plan 06's config owns.</done>
</task>

<task type="auto">
  <name>Task 3: Add STRUCT-07 to REQUIREMENTS and mirror the register into ROADMAP Phase 204</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 82-92 (STRUCT block), 180-200 (traceability), 216-239 (coverage and per-phase table)
    - .planning/ROADMAP.md `### Phase 204: Structure` section (Requirements line, Success Criteria, notes; near lines 740-760)
    - test/threadline/credo_config_contract_test.exs (`@register`/`@ceiling` final values)
  </read_first>
  <action>
    Per D-27:

    (a) REQUIREMENTS.md. After the STRUCT-06 bullet, add exactly: "- [ ] **STRUCT-07**: The Credo structural register (Refactor.Nesting and Refactor.CyclomaticComplexity per-site disables in lib/ and test/) is drained to zero and its ceiling pinned at 0, or each remaining site is re-registered with its exact count and a named successor beyond v1.41, with the ceiling lowered to match." Add the traceability row `| STRUCT-07 | Phase 204 | Pending |` after the STRUCT-06 row. Update coverage: "53 total" becomes "54 total", "STRUCT 6" becomes "STRUCT 7", "Mapped to phases: 53" becomes "54". In the per-phase table, change the 204 row to `STRUCT-01..STRUCT-07 | 7` and the total to `**54**`. Append a dated "Last updated" note: 2026-09-22, STRUCT-07 added by Phase 203 per D-27.

    (b) ROADMAP.md Phase 204 section. Append STRUCT-07 to the Requirements line. Add success criterion 5: "The `credo_config_contract_test` structural ceiling is ratcheted from <final @ceiling> to 0, or every remaining site names a post-v1.41 successor. (STRUCT-07)". Add a notes bullet mirroring the register for humans: "Credo structural register (from Phase 203; enforced in source by `test/threadline/credo_config_contract_test.exs`, this is a mirror only): Refactor.Nesting <n>, Refactor.CyclomaticComplexity <m>, ceiling <n+m>, historical max 46; <k> Nesting sites were flattened in place in 203." Use the actual values. Grep the ROADMAP for any other "53" requirement total tied to v1.41 and update it consistently. Do not touch unrelated numbers.

    (c) Stage both files by explicit path only: `git add .planning/REQUIREMENTS.md .planning/ROADMAP.md`. Never stage `.planning/` as a directory. Commit as `docs(203-05): add STRUCT-07 and mirror the Credo structural register into Phase 204`.
  </action>
  <verify>
    <automated>grep -c "STRUCT-07" /Users/jon/projects/threadline/.planning/REQUIREMENTS.md | awk '$1 >= 3 {ok=1} END {exit !ok}' && grep -q "54 total" /Users/jon/projects/threadline/.planning/REQUIREMENTS.md && grep -A3 "^### Phase 204" /Users/jon/projects/threadline/.planning/ROADMAP.md | grep -q "STRUCT-07"</automated>
    <fails_when>exit status non-zero: STRUCT-07 appears fewer than 3 times in REQUIREMENTS (bullet, traceability, per-phase range), the coverage total is not 54, or the Phase 204 requirements line lacks STRUCT-07</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "| STRUCT-07 | Phase 204 | Pending |" .planning/REQUIREMENTS.md` prints `1`
    - `grep -c "STRUCT-01..STRUCT-07" .planning/REQUIREMENTS.md` prints at least `1`
    - `git show --stat <docs-sha>` (SHA recorded in SUMMARY) lists exactly `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`
    - The ROADMAP mirror's numbers equal the `@register` values in the contract test
  </acceptance_criteria>
  <done>The register's named successor exists as a real, traced requirement. Humans can see the counts in the ROADMAP, while enforcement stays in source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor → lint gate | A suppression comment is the cheapest way to silence a gate. |
| gate → planning directory | DECOUPLE-01 forbids gates depending on planning files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-15 | Tampering (of the gate) | `credo:` config comments | high | mitigate | Only the per-line two-check form is accepted, and it requires an adjacent successor line. Exact per-check equality plus the `@historical_max 46` literal applies. Negative tests cover each broad form. |
| T-203-16 | Tampering (of the gate) | trailing-text disables | high | mitigate | Credo parses trailing text as part of the check name and the suppression silently fails. The contract rejects trailing text, and the full-default measurement residue gate catches any live finding. |
| T-203-17 | Repudiation | register provenance | low | accept | Register changes are reviewed diffs to a contract test. Git history is the audit trail. |
| T-203-18 | Information disclosure | `.planning/` staging | medium | mitigate | Explicit-path staging only. The acceptance criterion checks the commit's file list. |
</threat_model>

<verification>
- Under full defaults the residue is exactly `["Credo.Check.Warning.MissedMetadataKeyInLoggerConfig"]`.
- The register contract is green, and `mix test` is green.
- STRUCT-07 is present in REQUIREMENTS (bullet, traceability, counts) and in ROADMAP Phase 204.
</verification>

<success_criteria>
GATE-02 holds for the structural set. Every finding is either fixed or counted exactly in a source-resident register that names Phase 204 / STRUCT-07, and the register can only ratchet down through review.
</success_criteria>

<output>
Create `.planning/phases/203-real-gates/203-05-SUMMARY.md` when done. Include a per-site table (path, line, check, filed or flattened, reason), the final `@register`/`@ceiling`, and the commit SHA list.
</output>
