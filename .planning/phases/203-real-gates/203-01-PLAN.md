---
phase: 203-real-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/query/scope.ex
  - lib/threadline/query.ex
  - lib/threadline/query/filter_params.ex
  - lib/threadline/export/orchestrator.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - lib/threadline/operator_surface/live/start_live.ex
  - lib/threadline/operator_surface/live/export_status_live.ex
  - lib/threadline/operator_surface/controllers/export_controller.ex
  - test/threadline/query/filter_params_test.exs
  - test/threadline/operator_surface/exports_doc_contract_test.exs
  - test/threadline/operator_surface/controllers/export_controller_test.exs
  - test/threadline/code_walkthrough_doc_contract_test.exs
  - test/threadline/how_threadline_works_doc_contract_test.exs
  - test/threadline/public_surface_contract_test.exs
  - test/threadline/layer_boundary_contract_test.exs
autonomous: true
requirements: [GATE-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No `lib/**/*.ex` file outside `lib/threadline/operator_surface.ex`, `lib/threadline/operator_surface/**`, and the existence-checked allowlist entry `lib/mix/tasks/critic.synth.ex` contains the case-sensitive substring `OperatorSurface` (GATE-03; D-14, D-15)."
    - "`Threadline.OperatorSurface.Scope` is now `Threadline.Query.Scope` at `lib/threadline/query/scope.ex`, moved with `git mv`, `@moduledoc false` kept, and the `apply/2` body byte-identical to the pre-move body (D-11, D-13)."
    - "`Threadline.OperatorSurface.Exports.FilterParams` is now `Threadline.Query.FilterParams` at `lib/threadline/query/filter_params.ex`, moved with `git mv`; its body is byte-identical apart from the `defmodule` line (D-12, D-13)."
    - "Every pin that named the old module or path is retargeted in the same commit as its rename, including the D-30 pins (`public_surface_contract_test.exs`, `filter_params_test.exs` moved to `test/threadline/query/`, `export_controller_test.exs`), so no refute list passes vacuously (D-11, D-12, D-30)."
    - "The `exports_doc_contract_test.exs` guard that forbids `String.to_atom` in FilterParams still runs, now against `lib/threadline/query/filter_params.ex` through the `@filter_params_path` attribute (D-30)."
    - "`lib/mix/tasks/critic.synth.ex` is untouched and is the only allowlisted non-surface file; the contract test fails if that allowlisted path stops existing (D-14)."
    - "The layer-boundary contract test fails when its scanned file set is empty (vacuous-gate guard) and lists offenders sorted by path (D-15)."
    - "This plan runs before the AliasUsage sweep (D-16), and it adds no `boundary` dependency and no custom Credo check (D-15)."
    - "The FilterParams alias lines in the five lib callers are renamed in place and never re-sorted, so the full-default Credo total stays 484 and the pre-existing AliasOrder set stays exactly 11 sites; their fixes belong to Plan 03 (timeline_live.ex) and Plan 06 (start_live.ex, export_status_live.ex) (D-10, D-13)."
  artifacts:
    - lib/threadline/query/scope.ex
    - lib/threadline/query/filter_params.ex
    - test/threadline/query/filter_params_test.exs
    - test/threadline/layer_boundary_contract_test.exs
  key_links:
    - "`Threadline.Query.maybe_apply_scope/2` calls `Threadline.Query.Scope.apply/2`, which calls the host's `:scope_query_fn`. This is the tenant-scoping enforcement point. The scoped LiveView tests (TimelineLiveScoped, TransactionLiveScoped, StartLiveScoped) prove the path still applies after the move."
    - "`Threadline.Export.Orchestrator` calls `FilterParams.parse`, and the timeline, start, export-status and export-controller surfaces call `FilterParams`. All callers compile against the new name in one commit."
    - "The `@filter_params_path` attribute in `exports_doc_contract_test.exs` feeds the atom-safety guard. A stale path makes `File.read!` crash; it cannot pass vacuously."
  prohibitions:
    - "The body of `Scope.apply/2` is never edited, reformatted, or restructured during the move. Any Credo finding inside it waits for the mechanical plan."
    - "No FilterParams or Scope logic is inlined, split, or dependency-inverted. A whole-module rename is the only change (D-13)."
    - "No pin test is deleted or weakened to make the rename pass. Every refute or guard is repointed to the new name."
---

<objective>
Move the two misfiled query concepts out of the operator-surface namespace and pin the boundary with an executable source-scan contract.

Purpose: GATE-03. `query.ex` and `export/orchestrator.ex` reference `Threadline.OperatorSurface.*`, which inverts the capture/semantics/exploration layering. Scope is already the dependency-injection point (`:scope_query_fn`), and FilterParams is a URL-to-Query filter codec. Both are query concepts that were filed in the wrong place. This plan leads with the tracer: one inversion moved, plus the contract test proving end to end that it detects the remaining inversion. The second inversion then turns the test green.

Output: `Threadline.Query.Scope`, `Threadline.Query.FilterParams`, the moved FilterParams test, and `test/threadline/layer_boundary_contract_test.exs`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-real-gates/203-CONTEXT.md
@.planning/phases/203-real-gates/203-RESEARCH.md
@.planning/phases/203-real-gates/203-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces (Plan 01)

| Symbol | Kind | Path |
|---|---|---|
| `Threadline.Query.Scope` | module (renamed from `Threadline.OperatorSurface.Scope`, `@moduledoc false`) | `lib/threadline/query/scope.ex` |
| `Threadline.Query.FilterParams` | module (renamed from `Threadline.OperatorSurface.Exports.FilterParams`, `@moduledoc false`) | `lib/threadline/query/filter_params.ex` |
| `Threadline.Query.FilterParamsTest` | test module (moved) | `test/threadline/query/filter_params_test.exs` |
| `Threadline.LayerBoundaryContractTest` | NEW contract test | `test/threadline/layer_boundary_contract_test.exs` |

## Reversibility

Both renamed modules are `@moduledoc false`, so they are hidden from HexDocs and are not public API. `public_surface_contract_test.exs` lists them only as hidden owner-tag subjects. The rename is **reversible**: a later `git mv` back restores the old names, and no adopter can depend on either module. No `checkpoint:decision` is warranted.

## Flagged assumptions (spec-less probe, GATE-03 `unclassified`: unresolved, surfaced rather than dropped)

- **A-GATE-03:** The case-sensitive `OperatorSurface` substring is treated as a complete detector for references from lib into the operator surface. A reference assembled at runtime from string fragments (for example `Module.concat` over split literals) would evade it. This is accepted for Phase 203 because the codebase has no such construct today, and compile-time `boundary` enforcement is deferred. It is flagged here and not verified.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — move Scope into the query layer and prove the boundary scan end-to-end</name>
  <files>lib/threadline/query/scope.ex, lib/threadline/query.ex, test/threadline/code_walkthrough_doc_contract_test.exs, test/threadline/how_threadline_works_doc_contract_test.exs, test/threadline/public_surface_contract_test.exs, test/threadline/layer_boundary_contract_test.exs</files>
  <precondition>Postgres for the threadline compose stack answers on port 5433 (`docker compose up -d` from the repo root). Never point tests at the foreign 5432 listener.</precondition>
  <read_first>
    - lib/threadline/operator_surface/scope.ex (whole file, 25 lines; this is the body that must stay byte-identical)
    - lib/threadline/query.ex (alias block near line 30-38 and `maybe_apply_scope/2` near line 729-732; re-locate by grep because lines move)
    - test/threadline/code_walkthrough_doc_contract_test.exs (the refute list containing "Threadline.OperatorSurface.Scope")
    - test/threadline/how_threadline_works_doc_contract_test.exs (the `refute String.contains?(doc, "Threadline.OperatorSurface.Scope")` line)
    - test/threadline/public_surface_contract_test.exs (`modules_for_visibility_tag(:module_visibility_domain_tail)` and `(:module_visibility_operator_helpers)`, near lines 613-644)
    - test/threadline/zero_skips_contract_test.exs (whole file; the house template for a source-scan contract: prose moduledoc, non-empty glob guard, offenders comprehension)
    - test/threadline/public_surface_contract_test.exs `read_public!/1` (the allowlist existence-check precedent near line 529)
  </read_first>
  <behavior>
    - Test "scan set is non-empty": the filtered `lib/**/*.ex` set (excluding the surface tree and the allowlist) is not empty. The failure message explains that an empty glob would pass vacuously.
    - Test "allowlisted paths exist": every entry of `@allowlist` (exactly `lib/mix/tasks/critic.synth.ex`) and the surface root `lib/threadline/operator_surface.ex` satisfy `File.regular?/1`.
    - Test "no lib module outside the operator surface references it": the offenders are those scanned files whose contents contain the case-sensitive `OperatorSurface` substring. They are reported sorted by path as relative paths, and the assertion is `offenders == []`.
    - RED expectation after the Scope move alone: exactly one test fails, and it names exactly one offender, `lib/threadline/export/orchestrator.ex`. The name `lib/threadline/query.ex` must be absent from the output.
  </behavior>
  <action>
    Per D-11, D-13, D-30, and D-15:

    (a) Run `git mv lib/threadline/operator_surface/scope.ex lib/threadline/query/scope.ex`. Change only line 1 to `defmodule Threadline.Query.Scope do`. Keep `@moduledoc false`. Do not touch lines 2 onward: `apply/2` is the tenant-scoping hook, and it contains a `cond` that must not be restructured here even though Credo may flag it later.

    (b) In `lib/threadline/query.ex`, replace `alias Threadline.OperatorSurface.Scope, as: OperatorScope` with `alias Threadline.Query.Scope`. Place it so the alias block stays alphabetical: after the `Threadline.Capture.*` aliases, before `Threadline.Semantics.ActorRef`. Change the call in `maybe_apply_scope/2` from `OperatorScope.apply(query, opts)` to `Scope.apply(query, opts)`. First confirm by grep that no other `Scope` short name already exists in query.ex. If one does, use `as: QueryScope` per D-10 and record why.

    (c) Retarget the pins in the same commit. In `code_walkthrough_doc_contract_test.exs`, the refute-list string becomes "Threadline.Query.Scope". In `how_threadline_works_doc_contract_test.exs`, the refute string becomes "Threadline.Query.Scope". In `public_surface_contract_test.exs`, remove `Threadline.OperatorSurface.Scope` from the `:module_visibility_operator_helpers` list and add `Threadline.Query.Scope` to the `:module_visibility_domain_tail` list. This is Claude's-discretion placement: the module is no longer an operator helper, and both tag lists stay non-empty.

    (d) Run the pin tests (verify command 1). Then commit only these five files plus the rename as `refactor(203-01): move OperatorSurface.Scope to Threadline.Query.Scope`. The body states "Whole-module git mv + rename (D-11/D-13); pins retargeted in the same commit. No behavior change." plus the verify commands run. Never use the `fix` type (D-28).

    (e) RED step of the tracer: create `test/threadline/layer_boundary_contract_test.exs` as `Threadline.LayerBoundaryContractTest` (`use ExUnit.Case, async: true`). Give it a prose `@moduledoc` stating the GATE-03 invariant and why a case-sensitive substring is used. Mirror the zero_skips structure: `@allowlist ["lib/mix/tasks/critic.synth.ex"]`, a surface-root constant, and a `scanned_files/0` that is `Path.wildcard("lib/**/*.ex")` minus the surface root, minus anything under `lib/threadline/operator_surface/`, minus the allowlist, then `Enum.sort/1`. Add the three tests from the behavior block. Use actionable `<>`-concatenated failure messages naming the fix ("move the module into its own layer or route through an injected function; do not widen the allowlist without review"). Copy the planning-independence self-guard from `dialyzer_ignore_contract_test.exs` (build the directory name at runtime and refute that this file's own source contains it). Run it (verify command 2) and confirm the RED shape described in behavior. Do NOT commit the test file in this task. It commits green in Task 2.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/code_walkthrough_doc_contract_test.exs test/threadline/how_threadline_works_doc_contract_test.exs test/threadline/public_surface_contract_test.exs test/threadline/query_test.exs</automated>
    <fails_when>non-zero exit, or the summary line reports anything other than `0 failures`, for example the public-surface test reporting "is absent from the compiled app" for a stale module name</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/layer_boundary_contract_test.exs > /tmp/p203-01-tracer.txt 2>&1; grep -q '1 failure' /tmp/p203-01-tracer.txt && grep -q 'lib/threadline/export/orchestrator.ex' /tmp/p203-01-tracer.txt && ! grep -q 'lib/threadline/query.ex' /tmp/p203-01-tracer.txt</automated>
    <fails_when>exit status non-zero: the run did not report exactly `1 failure`, or the output lacks `lib/threadline/export/orchestrator.ex`, or `lib/threadline/query.ex` still appears as an offender (Scope move incomplete)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format=%s <scope-sha>` equals `refactor(203-01): move OperatorSurface.Scope to Threadline.Query.Scope`, where `<scope-sha>` is the rename commit's SHA recorded in SUMMARY
    - `git show --stat <scope-sha>` lists a rename `lib/threadline/{operator_surface => query}/scope.ex`
    - `git diff -M <scope-sha>^..<scope-sha> -- lib/threadline/operator_surface/scope.ex lib/threadline/query/scope.ex` shows only line 1 changed
    - `grep -c "OperatorSurface" lib/threadline/query.ex` prints `0`
    - `grep -c "Threadline.Query.Scope" test/threadline/public_surface_contract_test.exs` prints `1`
    - `test -f lib/threadline/operator_surface/scope.ex` exits 1
    - The tracer run reports exactly `1 failure` naming only `lib/threadline/export/orchestrator.ex`
  </acceptance_criteria>
  <done>Scope lives in the query layer with its pins retargeted and committed. The new boundary test demonstrably catches the one remaining inversion. The enforcement path is proven end to end before the second move.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move FilterParams into the query layer and commit the boundary contract green</name>
  <files>lib/threadline/query/filter_params.ex, lib/threadline/export/orchestrator.ex, lib/threadline/operator_surface/live/timeline_live.ex, lib/threadline/operator_surface/live/start_live.ex, lib/threadline/operator_surface/live/export_status_live.ex, lib/threadline/operator_surface/controllers/export_controller.ex, test/threadline/query/filter_params_test.exs, test/threadline/operator_surface/exports_doc_contract_test.exs, test/threadline/operator_surface/controllers/export_controller_test.exs, test/threadline/public_surface_contract_test.exs, test/threadline/layer_boundary_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/exports/filter_params.ex (header lines 1-6; body must stay byte-identical)
    - lib/threadline/export/orchestrator.ex (alias block near lines 8-13)
    - the alias line of each surface caller, found by `grep -n "Exports.FilterParams"` over lib/threadline/operator_surface/live/timeline_live.ex, start_live.ex, export_status_live.ex, and lib/threadline/operator_surface/controllers/export_controller.ex
    - test/threadline/operator_surface/exports/filter_params_test.exs (lines 1, 5, 146)
    - test/threadline/operator_surface/exports_doc_contract_test.exs (`@filter_params_path` line 10; its read sites near 140, 192, 266; the module-name assertion near 157-158; the `String.to_atom` guard near 262-270)
    - test/threadline/operator_surface/controllers/export_controller_test.exs (the fully-qualified `canonical_query` call near line 436)
    - test/threadline/public_surface_contract_test.exs (`:module_visibility_operator_helpers` list)
    - test/threadline/layer_boundary_contract_test.exs (uncommitted from Task 1)
  </read_first>
  <behavior>
    - After this task the layer-boundary contract test passes: 3 tests, 0 failures.
    - The FilterParams unit tests, the exports doc contract (including the `String.to_atom` guard), the export controller tests, and the public-surface contract all pass against the new name and path.
  </behavior>
  <action>
    Per D-12, D-13, and D-30:

    (a) Run `git mv lib/threadline/operator_surface/exports/filter_params.ex lib/threadline/query/filter_params.ex`. Change only line 1 to `defmodule Threadline.Query.FilterParams do`.

    (b) Rename each lib alias **in place**: replace the text `alias Threadline.OperatorSurface.Exports.FilterParams` with `alias Threadline.Query.FilterParams` on the same line, in orchestrator.ex, timeline_live.ex, start_live.ex, export_status_live.ex, and export_controller.ex. Every call site already uses the short `FilterParams.` name. Do NOT move or re-sort any alias line, and change nothing else in those files. Probe-proven on 2026-09-22 (in-place edit, `mix credo --strict --config-file deps/credo/.credo.exs` on the six files, then reverted): the in-place rename leaves the AliasOrder picture unchanged in count and file set. orchestrator.ex and export_controller.ex stay clean, and the three pre-existing AliasOrder sites stay pre-existing, with only their reported lines shifting: timeline_live.ex 15→16, start_live.ex 18→17, export_status_live.ex stays at 15. Those three sites are NOT fixed here. timeline_live.ex's site is absorbed by Plan 03 (its alias block is rewritten there for AliasUsage), and start_live.ex and export_status_live.ex are owned by Plan 06 (mechanical lib sweep). This keeps the rename commit a pure rename and the phase total at 484.

    (c) Run `mkdir -p test/threadline/query`, then `git mv test/threadline/operator_surface/exports/filter_params_test.exs test/threadline/query/filter_params_test.exs`. In the moved file, line 1 becomes `defmodule Threadline.Query.FilterParamsTest do`, line 5 becomes `alias Threadline.Query.FilterParams`, and the `File.read!` path at about line 146 becomes `lib/threadline/query/filter_params.ex`.

    (d) In `exports_doc_contract_test.exs`, set `@filter_params_path` to `lib/threadline/query/filter_params.ex`. Retarget both strings in the module-name assertion to `Threadline.Query.FilterParams` and `alias Threadline.Query.FilterParams`. The `String.to_atom` guard must keep reading through `@filter_params_path`. It is repointed, never removed or weakened. In `export_controller_test.exs`, change the fully-qualified `Threadline.OperatorSurface.Exports.FilterParams.canonical_query` to `Threadline.Query.FilterParams.canonical_query`. In `public_surface_contract_test.exs`, remove `Threadline.OperatorSurface.Exports.FilterParams` from `:module_visibility_operator_helpers` and add `Threadline.Query.FilterParams` to `:module_visibility_domain_tail`, next to the Scope entry from Task 1.

    (e) Run `mix compile --force --warnings-as-errors` (this clears stale beams for the old module names), then verify command 1. Commit the rename with all its pins as `refactor(203-01): move Exports.FilterParams to Threadline.Query.FilterParams`, with a body in the D-28 template.

    (f) Run the boundary test (verify command 2). It must be fully green. Commit that one file alone as `refactor(203-01): add layer-boundary contract test (GATE-03)`. The body explains the allowlist choice: critic.synth.ex is a maintainer-only Mix task outside the four layers and is excluded from the Hex package (D-14).
  </action>
  <verify>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors</automated>
    <fails_when>non-zero exit, or any `warning:` line, for example an undefined `Threadline.OperatorSurface.Exports.FilterParams` reference left in a caller</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/query/filter_params_test.exs test/threadline/operator_surface/exports_doc_contract_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs test/threadline/public_surface_contract_test.exs test/threadline/export/orchestrator_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/transaction_live_test.exs test/threadline/layer_boundary_contract_test.exs</automated>
    <fails_when>non-zero exit, or the summary reports anything other than `0 failures`. A `File.read!` enoent in exports_doc_contract_test means `@filter_params_path` was not repointed.</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format</automated>
    <fails_when>non-zero exit: a file created or edited in this plan is not `mix format`-clean. Run `mix format` on that file and fold the result into the commit that introduced it before closing the plan (a `style` commit only if that commit is already made).</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "OperatorSurface" lib --include='*.ex' | grep -v '^lib/threadline/operator_surface' | grep -v '^lib/mix/tasks/critic.synth.ex'` prints nothing
    - `grep -rn "Exports.FilterParams\|operator_surface/exports/filter_params" lib test` prints nothing
    - `grep -c 'lib/threadline/query/filter_params.ex' test/threadline/operator_surface/exports_doc_contract_test.exs` prints at least `1`
    - `grep -c 'String.to_atom' test/threadline/operator_surface/exports_doc_contract_test.exs` equals its pre-task count (the guard still exists)
    - The SUMMARY records the FilterParams rename SHA and the `add layer-boundary contract test (GATE-03)` SHA. `git show --stat <boundary-test-sha>` shows exactly 1 file changed.
    - `test -f test/threadline/operator_surface/exports/filter_params_test.exs` exits 1
    - `D=$(git diff <filterparams-sha>^..<filterparams-sha> -- lib/threadline/export/orchestrator.ex lib/threadline/operator_surface/live/timeline_live.ex lib/threadline/operator_surface/live/start_live.ex lib/threadline/operator_surface/live/export_status_live.ex lib/threadline/operator_surface/controllers/export_controller.ex) && test -n "$D" && ! printf '%s\n' "$D" | grep -E '^[-+][^-+]' | grep -qv 'FilterParams'` exits 0 (the git status is captured first, so a bad SHA fails closed; the diff is non-empty and every changed line in the five callers is a FilterParams alias line: rename in place, no re-sort)
  </acceptance_criteria>
  <done>Both inversions are gone. GATE-03 is enforced by a committed, green, non-vacuous contract test with an existence-checked allowlist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host `:scope_query_fn` → `Threadline.Query.Scope.apply/2` | Tenant scoping crosses here. A host-supplied function narrows every operator query to the caller's tenant. |
| URL params → `Threadline.Query.FilterParams` | Untrusted query-string input is decoded into filter atoms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-01 | Elevation of privilege | `Threadline.Query.Scope.apply/2` | high | mitigate | Pure `git mv` plus a line-1 rename only. Acceptance checks that the rename commit changes only line 1. The scoped LiveView tests (timeline/transaction) run in Task 2 verify. |
| T-203-02 | Denial of service (atom exhaustion) | `Threadline.Query.FilterParams` | high | mitigate | The `String.to_atom` guard in exports_doc_contract_test is repointed through `@filter_params_path` and never dropped. A stale path crashes instead of passing, and acceptance checks the guard count is unchanged. |
| T-203-03 | Tampering (of the gate) | layer_boundary_contract_test allowlist | medium | mitigate | The allowlist is one explicit path, existence-checked. The scan set has a non-empty guard. Widening the allowlist is a reviewed diff to a contract test. |
| T-203-04 | Information disclosure | commits | low | mitigate | Stage by explicit path only. Never `git add .planning/` (machine-local critic files). |
</threat_model>

<verification>
- `mix compile --force --warnings-as-errors` is clean.
- The layer-boundary contract test is green with 3 tests. Its tracer run proved it red on a real inversion.
- `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix compile >/dev/null 2>&1; mix credo --strict --config-file deps/credo/.credo.exs --format json' | jq -e '(.issues | length == 484) and ([.issues[] | select(.check=="Credo.Check.Readability.AliasOrder")] | length == 11)'` prints `true`. The FilterParams findings now report under `lib/threadline/query/filter_params.ex`. A pure in-place rename changes no count: the 11 pre-existing AliasOrder sites are still 11 (three of them at shifted line numbers, see Task 2 (b)). Input note: this exact-count gate also counts findings in the new contract test file this plan creates, so that file must be Credo-clean under `deps/credo/.credo.exs --strict` (0 findings of any check, including AliasUsage and AliasOrder) before the gate is read; a non-zero count there is fixed in the test file, never absorbed by the 484/11 numbers.
- `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format` exits 0 (the new contract test and edited files are formatted before the plan closes).
</verification>

<success_criteria>
GATE-03 holds and is test-pinned. The tree is `mix verify.format`-clean. Both renames landed as single commits carrying their pins, and the boundary test landed as its own commit. There is no change to tenant scoping or filter decoding behavior.
</success_criteria>

<output>
Create `.planning/phases/203-real-gates/203-01-SUMMARY.md` when done. Record the tracer's RED output excerpt, the three commit SHAs, and the public-surface tag placement choice (domain_tail).
</output>
