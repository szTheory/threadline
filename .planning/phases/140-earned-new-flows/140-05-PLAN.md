---
phase: 140-earned-new-flows
plan: "05"
type: execute
wave: 3
depends_on: ["140-02", "140-04"]
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts
autonomous: true
requirements: [POLISH-FLOWS]
must_haves:
  truths:
    - "Browser UAT proves Home record-first lookup reaches first-class row history."
    - "Browser UAT proves direct first-class row-history entry works without opening a transaction first."
    - "Browser UAT proves Home correlation paste/deep-link lands on the existing Timeline correlation filter."
    - "Browser UAT proves filtered Timeline context carries into Exports."
    - "Browser UAT proves filtered Evidence proof context carries into Exports."
    - "Browser UAT stays focused on earned flows and does not assert Phase 141 motion or Phase 142 responsive redesign behavior."
  artifacts:
    - path: "examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts"
      provides: "Focused cross-surface Playwright UAT for EF1, EF2, EF3, and EF4"
      contains: "walk-acme-4521-close"
  key_links:
    - from: "examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts"
      to: "/audit"
      via: "Home record-first and correlation forms"
      pattern: "audit"
    - from: "examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts"
      to: "/audit/rows"
      via: "first-class row-history direct route"
      pattern: "rows"
    - from: "examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts"
      to: "/audit/exports"
      via: "Timeline and Evidence carry-forward handoffs"
      pattern: "exports"
---

<objective>
Add focused browser UAT proving the four Phase 140 earned flows work across surfaces.

Purpose: the phase is complete only when a real browser can exercise the record-first lookup, first-class row-history entry, correlation paste/deep-link, and Timeline/Evidence export handoffs using seeded example data.
Output: one Playwright spec for the earned-flow cross-surface paths.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/140-earned-new-flows/140-CONTEXT.md
@.planning/phases/140-earned-new-flows/140-RESEARCH.md
@.planning/phases/140-earned-new-flows/140-PATTERNS.md
@.planning/phases/139-orientation-hub-home-nav/139-VERIFICATION.md
@.planning/phases/140-earned-new-flows/140-01-SUMMARY.md
@.planning/phases/140-earned-new-flows/140-02-SUMMARY.md
@.planning/phases/140-earned-new-flows/140-03-SUMMARY.md
@.planning/phases/140-earned-new-flows/140-04-SUMMARY.md
@.planning/milestones/v1.31-PERSONAS-IA.md
@examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-prove-mobile.spec.ts
@examples/threadline_phoenix/e2e/tests/operator.spec.ts
@examples/threadline_phoenix/DEMO-MANIFEST.md

<interfaces>
Browser setup:
- Existing specs log in through `/users/log_in` using `admin@example.com` and `process.env.DEMO_SEED_PASSWORD ?? "password123456"`.
- `run-e2e.sh` resets/migrates/seeds, starts Phoenix, installs Playwright chromium, and runs `npm test`.
- Stable correlation literal is `walk-acme-4521-close`.

Seed navigation:
- Use Timeline correlation results to discover a real `ticket_replies` row-history href when a concrete record id is needed.
- Use the discovered row id for Home record-first and direct `/audit/rows/ticket_replies/:id` checks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused earned-flow Playwright spec</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts</files>
  <behavior>
    - The spec imports only from `@playwright/test` and adds no dependency.
    - The spec logs in using the existing helper shape from `operator-home-nav-mobile.spec.ts`.
    - Test setup uses the seeded `walk-acme-4521-close` correlation id and discovers a stable `ticket_replies` record id by opening Timeline and parsing an existing row-history link href.
    - EF1 test starts at `/audit`, uses the Home record-first section, selects `ticket_replies`, enters the discovered record id, submits, and expects `/audit/rows/ticket_replies/:id` plus `data-testid="row-history-drawer"`.
    - EF2 test opens `/audit/rows/ticket_replies/:id` directly and expects row-history drawer content without visiting a transaction route first.
    - EF4 test starts at `/audit`, uses the Home correlation section, submits `walk-acme-4521-close`, and expects `/audit/timeline?correlation_id=...`, `#filter-correlation-id` value, and visible Timeline rows.
    - EF3 Timeline test visits filtered Timeline, clicks `Carry to Exports`, and expects `/audit/exports?...`, the Timeline context banner, canonical correlation/table filter display, and no unsupported filter keys.
    - EF3 Evidence test visits an Evidence filtered/history context, clicks `Carry to Exports`, and expects Exports proof-context banner with Evidence subject/mode and a return link to Evidence.
    - Each test asserts the relevant `data-earned-flow` trace attribute when the section/banner is visible.
    - The spec includes only a root no-horizontal-overflow smoke check copied from Phase 139; it does not assert table/drawer/filter responsive layouts, screenshots, or motion.
  </behavior>
  <action>Create `operator-earned-flows.spec.ts` by reusing login, viewport, route, and overflow helper patterns from the existing operator E2E specs. Keep the browser proof cross-surface and narrow per D-01 and D-10: EF1 record-first Home to row history, EF2 direct row-history route, EF3 Timeline and Evidence carry-forward to Exports, and EF4 Home correlation to Timeline. Use seeded data; do not mutate demo schema/routes/business logic. Avoid screenshot baselines, animation assertions, breakpoint matrix assertions, and broad responsive checks owned by Phases 141 and 142.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-earned-flows.spec.ts</automated>
  </verify>
  <done>The earned-flow Playwright spec proves all four flows and trace attributes in a browser against seeded example data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run focused phase gate commands and record execution summary</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts</files>
  <behavior>
    - Focused ExUnit command from Plans 01-04 passes before browser UAT is considered complete.
    - Browser UAT command passes against a seeded example app server.
    - The summary records the server setup command or script used, exact E2E base URL, test counts, and any skipped browser prerequisites.
  </behavior>
  <action>Run the focused phase gate after the spec exists: row-history tests, Home tests, Timeline/Exports tests, Evidence/Exports tests, export controller guard tests, then the Playwright spec. If no server is running, use the existing example-app E2E setup path (`examples/threadline_phoenix/e2e/run-e2e.sh`) or start the example server with the same test env used by Phase 139 summaries, then run the focused spec command. Do not broaden the browser suite beyond `operator-earned-flows.spec.ts` unless failures show shared setup is broken.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/row_history_live_test.exs test/threadline/operator_surface/row_history_component_test.exs test/threadline/operator_surface/transaction_live_test.exs test/threadline/operator_surface/live/start_live_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs test/threadline/operator_surface/live/evidence_live_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs</automated>
    <automated>cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-earned-flows.spec.ts</automated>
  </verify>
  <done>Focused ExUnit and browser earned-flow UAT pass, and the plan summary contains exact verification evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded browser session -> operator surface | E2E logs in as seeded admin and exercises mounted LiveViews. |
| browser-discovered row id -> Home form | A real authorized row id from Timeline is reused in Home record-first lookup. |
| cross-surface query params -> assertions | Browser verifies carried params without adding new parser behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-19 | Repudiation | browser proof | mitigate | Assert `data-earned-flow` attributes and record exact commands/base URL in the summary. |
| T-140-20 | Information disclosure | browser-discovered record id | accept | Test uses an admin seeded session and a row already visible in Timeline; scoped ExUnit tests own least-privilege checks. |
| T-140-21 | Tampering | E2E setup | mitigate | Use existing example seed/reset/server script or Phase 139 server setup; do not alter demo business logic. |
| T-140-22 | Denial of service | browser scope creep | mitigate | Keep to one focused spec and one seeded correlation; avoid screenshot/matrix/motion assertions. |
| T-140-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; existing Playwright dependency is already present. |
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/live/row_history_live_test.exs test/threadline/operator_surface/row_history_component_test.exs test/threadline/operator_surface/transaction_live_test.exs test/threadline/operator_surface/live/start_live_test.exs test/threadline/operator_surface/live/timeline_live_test.exs test/threadline/operator_surface/live/export_status_live_test.exs test/threadline/operator_surface/live/evidence_live_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs`
- `cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-earned-flows.spec.ts`
- `rg -n "data-earned-flow|walk-acme-4521-close|/audit/rows|Carry to Exports" examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts`
</verification>

<success_criteria>
- Focused browser UAT covers EF1, EF2, EF3, and EF4 end to end.
- The spec uses existing seeded data and E2E helper patterns.
- The proof remains scoped to earned flows and does not enter Phase 141 motion, Phase 142 responsive matrix, broad screenshot diffing, new dependencies, or demo-app redesign.
</success_criteria>

<output>
Create `.planning/phases/140-earned-new-flows/140-05-SUMMARY.md` when done.
</output>
