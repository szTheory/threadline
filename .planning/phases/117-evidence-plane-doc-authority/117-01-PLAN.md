---
phase: 117-evidence-plane-doc-authority
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - guides/getting-started-saas.md
  - guides/upgrade-path.md
  - guides/domain-reference.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "Maintainer PROJECT.md no longer references phantom `guides/evidence-plane.md`; lists real split-guide paths."
    - "Adopter guides getting-started and upgrade-path use Hex semver (or timeless headings), not `v1.2x` milestone labels."
    - "`guides/upgrade-path.md` documents `0.5.x → 0.6.x` minor upgrade with Evidence + `Audit.transaction/3` pointers."
    - "domain-reference incident JSON step 1 describes semantics via `Threadline.Audit.transaction/3` with `:action`, not standalone `record_action/2` as the HTTP write-path face."
  artifacts:
    - path: .planning/PROJECT.md
      provides: "DOC-01 maintainer truth — split evidence guide map"
    - path: guides/upgrade-path.md
      provides: "DOC-02 semver minor-history + lane prose"
    - path: guides/getting-started-saas.md
      provides: "DOC-02 recommended-path heading without v1.24+"
    - path: guides/domain-reference.md
      provides: "DOC-03 narrative prerequisite — incident blessed path"
---

<objective>
Apply locked doc-authority prose: fix maintainer dead evidence-plane hub reference (DOC-01), semver-not-milestone sweep in adopter guides plus 0.5→0.6 upgrade bullet (DOC-02), and surgical domain-reference incident JSON step 1 (deferred Phase 115 narrative).

Purpose: Prose must land before plan 117-02 doc-contract locks.
Output: Four markdown files updated; no new hub file; domain-reference API-era headers untouched.
</objective>

<context>
@.planning/phases/117-evidence-plane-doc-authority/117-CONTEXT.md (D-117-01, D-117-02, D-117-03)
@.planning/phases/117-evidence-plane-doc-authority/117-RESEARCH.md
@guides/how-threadline-works.md (Evolution bullets — do not rewrite structure)
@CHANGELOG.md (`[0.6.0]` section for upgrade bullet pointer)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-117-01 | New hub file duplicates non-goals across guides | medium | D-117-01a forbids `guides/evidence-plane.md` |
| T-117-02 | Mass-scrubbing domain-reference breaks API-era contracts | low | Allowlist v1.10+ section titles; only incident step 1 |
| T-117-03 | Incident prose implies record_action alone suffices | medium | Name `Audit.transaction/3` with `:action` and action_id linkage |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fix PROJECT.md evidence-plane guide map (DOC-01)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (line ~33 shipped-milestone bullet listing guides)
    - .planning/phases/117-evidence-plane-doc-authority/117-CONTEXT.md (D-117-01c)
    - README.md (`## Evidence plane` link targets)
  </read_first>
  <action>
In `.planning/PROJECT.md`, replace the bullet that lists `guides/evidence-plane.md` with maintainer-accurate split-guide paths. The bullet should read equivalent to:

`CHANGELOG`, `guides/operator-surface.md`, `guides/how-threadline-works.md`, `guides/upgrade-path.md`, `guides/domain-reference.md`, README, production checklist, and the example app are aligned end-to-end.

Do **not** change adopter-facing README content in this task. If you touch any `.planning/milestones/**` file that still contains `@guides/evidence-plane.md`, replace with the same four guide paths (batch only files you touch for this fix).
  </action>
  <acceptance_criteria>
    - `rg -F 'guides/evidence-plane.md' .planning/PROJECT.md` returns no matches
    - `rg -F 'guides/how-threadline-works.md' .planning/PROJECT.md` matches at least once in the evidence/doc alignment bullet
    - `rg -F 'guides/domain-reference.md' .planning/PROJECT.md` matches at least once in that bullet
  </acceptance_criteria>
  <verify>rg -F 'guides/evidence-plane.md' .planning/PROJECT.md; echo exit:$?</verify>
</task>

<task type="auto">
  <name>Task 2: Semver sweep getting-started + upgrade-path (DOC-02)</name>
  <files>guides/getting-started-saas.md, guides/upgrade-path.md</files>
  <read_first>
    - guides/getting-started-saas.md (line ~87 `### Recommended path (v1.24+)`)
    - guides/upgrade-path.md (lines 23–26 v1.21 prose; lines 62–76 minor upgrades; line ~92 v1.19 scorecard)
    - .planning/phases/117-evidence-plane-doc-authority/117-CONTEXT.md (D-117-02b, D-117-02d, D-117-02g)
  </read_first>
  <action>
1. In `guides/getting-started-saas.md`, rename `### Recommended path (v1.24+)` to **`### Recommended path (0.6.0+)`** (keep following paragraph about `Audit.transaction/3` unchanged except if it still says `v1.24` — remove any `v1.2x` in that subsection).

2. In `guides/upgrade-path.md`:
   - Replace `For v1.21's support-lane wording` with **`For 0.5.0 support-lane wording`** (same following sentences).
   - Under `## Upgrade by Threadline minor` → `Current guidance by minor:`, add as the **first** bullet (before `0.3.x -> 0.4.x`):

   `- **0.5.x → 0.6.x**: Evidence plane (\`Threadline.Evidence\`, \`mix threadline.evidence.show\`, \`/audit/evidence\`), recommended audited write path (\`Threadline.Audit.transaction/3\`); see \`CHANGELOG.md\` \`[0.6.0]\` for deprecated manual GUC + \`record_action/2\` recipe.`

   - Replace `Threadline closes v1.19 with` with **`Threadline's 0.5.0 integration-breadth era closed with`** (keep stay-in-tree decision prose).

3. Optionally add D-117-02g evaluator sentence to upgrade-path intro (after first paragraph) if it fits without bloat:

   `Threadline **0.6.0** packages Evidence, \`Audit.transaction/3\`, and aligned operator surfaces that landed in-repo after **0.5.0**; upgrade steps are semver-scoped in \`CHANGELOG.md\` and this guide.`
  </action>
  <acceptance_criteria>
    - `rg 'v1\.2[0-9]' guides/getting-started-saas.md guides/upgrade-path.md` returns no matches
    - `rg -F '0.5.x → 0.6.x' guides/upgrade-path.md` matches
    - `rg -F 'Threadline.Audit.transaction/3' guides/upgrade-path.md` matches under minor upgrades section
    - `rg -F 'Recommended path (0.6.0+)' guides/getting-started-saas.md` matches
  </acceptance_criteria>
  <verify>rg 'v1\.2[0-9]' guides/getting-started-saas.md guides/upgrade-path.md; rg -F '0.5.x → 0.6.x' guides/upgrade-path.md</verify>
</task>

<task type="auto">
  <name>Task 3: Fix domain-reference incident JSON step 1 (DOC-03 prose)</name>
  <files>guides/domain-reference.md</files>
  <read_first>
    - guides/domain-reference.md (lines 267–276, anchor COMP-EXAMPLE-INCIDENT-JSON)
    - guides/getting-started-saas.md (Audit.transaction/3 snippet for wording consistency)
    - .planning/phases/117-evidence-plane-doc-authority/117-CONTEXT.md (D-117-03a, D-117-03b)
  </read_first>
  <action>
Replace only numbered step 1 under `### Reference example: incident JSON (v1.11+)` with:

`1. **`POST /api/posts`** returns **`audit_transaction_id`** — the UUID of the **`audit_transactions`** row for that HTTP request's database transaction (after **`Threadline.Audit.transaction/3`** with **`:action`** records semantics and links **`audit_transactions.action_id`** in the same transaction).`

Leave steps 2–3, anchor id, test name, and auth notes unchanged. Do not edit other `record_action/2` primitive documentation elsewhere in the file.
  </action>
  <acceptance_criteria>
    - `rg -F 'COMP-EXAMPLE-INCIDENT-JSON' guides/domain-reference.md` matches
    - `rg -F 'Threadline.Audit.transaction/3' guides/domain-reference.md` matches
    - Step 1 scoped check: `rg -n 'after \`Threadline.record_action/2\` links semantics' guides/domain-reference.md` returns no matches
  </acceptance_criteria>
  <verify>rg -n 'Threadline.Audit.transaction/3' guides/domain-reference.md | head -3</verify>
</task>

</tasks>

<verification>
- [ ] `rg 'v1\.2[0-9]' guides/getting-started-saas.md guides/upgrade-path.md` — empty
- [ ] `rg -F 'guides/evidence-plane.md' .planning/PROJECT.md` — empty
- [ ] Incident step 1 names `Audit.transaction/3` with `:action`
</verification>
