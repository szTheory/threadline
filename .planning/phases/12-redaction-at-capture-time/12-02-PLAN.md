# Plan 12-02: Mix task, integration tests, operator docs (REDN-01, REDN-02)

**Phase:** 12 — Redaction at capture time  
**Plan:** 12-02 of 02  
**Status:** Ready to execute  
**Depends on:** 12-01 complete  
**Blocks:** Nothing  
**Requirements covered:** REDN-01, REDN-02  
**Wave:** 2  
**requirements_addressed:** REDN-01, REDN-02  
**files_modified:** `lib/mix/tasks/threadline.gen.triggers.ex`, `config/config.exs`, `config/test.exs`, `README.md`, `guides/domain-reference.md`, `test/threadline/capture/trigger_redaction_test.exs` (new)  
**autonomous:** true  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-12-02-01 | Wrong `MIX_ENV` loads empty policy → silent no-op migrations | medium | Docs + test use explicit `Application.put_env` in setup; README states `MIX_ENV` parity (D-02). |
| T-12-02-02 | Integration tests skip on CI without Postgres | medium | Tests use `DataCase` same as sibling files; `mix ci.all` is gate; tag documented if any `@tag :skip`. |
| T-12-02-03 | Docs promise columns not actually redacted | medium | README bullets mirror exact config keys implemented in code; grep keys in `threadline.gen.triggers.ex` moduledoc. |

---

## Goal

Wire **`mix threadline.gen.triggers`** to read **canonical config** (`Application.get_env(:threadline, :trigger_capture)` or the atom name chosen in 12-01 — use **one** atom consistently; this plan references `:trigger_capture`), generate correct per-table vs global functions per D-13, add **PostgreSQL integration tests** proving persisted JSONB excludes/masks correctly, and update **README** + **`guides/domain-reference.md`**.

---

## Context

- `.planning/phases/12-redaction-at-capture-time/12-CONTEXT.md` (D-01–D-03, D-16)
- `lib/mix/tasks/threadline.verify_coverage.ex` (`Mix.Task.run("app.config", [])` pattern)

---

## Tasks

### Task 1: Config shape + `test.exs` fixture

**What to do**

1. Add `config :threadline, :trigger_capture, tables: %{...}` example under `config/config.exs` **or** document-only in `config/test.exs` if dev default should stay empty — **minimum:** `config/test.exs` must define at least one table key used by new tests (e.g. `"test_redaction_users"` → `[exclude: [...], mask: [...]]` structure matching implementation from 12-01).
2. Structure keys: **table name string** → keyword list with `:exclude`, `:mask`, optional `:mask_placeholder`, and pass-through for `:store_changed_from` / `:except_columns` if present.

**Read first**

- `config/test.exs`
- `config/config.exs`
- `lib/mix/tasks/threadline.verify_coverage.ex`

**Acceptance criteria**

- `grep -E ':trigger_capture|trigger_capture' config/test.exs` exits 0.
- `MIX_ENV=test mix run -e 'Application.ensure_all_started(:threadline); IO.inspect(Application.get_env(:threadline, :trigger_capture))'` prints a non-nil keyword or map.

**Done condition:** Test env can resolve policy for integration tests.

---

### Task 2: `Mix.Tasks.Threadline.Gen.Triggers` — load policy + optional `--dry-run`

**What to do**

1. At start of `run/1`, call `Mix.Task.run("app.config", [])` then read `Application.get_env(:threadline, :trigger_capture, %{})`.
2. For each table in `--tables`, resolve its redaction entry; call `RedactionPolicy.validate!/1` before writing migration.
3. Emit **per-table** `install_function_for_table` + `:per_table` triggers when `store_changed_from` **or** non-empty exclude/mask; else keep current global-only migration shape for that table.
4. Optional: implement `--dry-run` flag (strict) that prints **one line per table** to stdout: `table=<name> exclude=[...] mask=[...]` and **does not** write a migration file — if omitted, skip flag in moduledoc until implemented.

**Read first**

- `lib/mix/tasks/threadline.gen.triggers.ex`
- `lib/threadline/capture/redaction_policy.ex`
- `lib/threadline/capture/trigger_sql.ex`

**Acceptance criteria**

- `grep -F "app.config" lib/mix/tasks/threadline.gen.triggers.ex` exits 0.
- `MIX_ENV=test mix threadline.gen.triggers --tables nonexistent_table_xyz` exits non-zero **or** creates migration only for valid tables — **pick:** `Mix.raise` when table missing from `:trigger_capture` if policy required; document in moduledoc. Minimum: gen task runs with `MIX_ENV=test` for tables used in test without `Mix.raise` unrelated to argv.

**Done condition:** Migration content reflects resolved exclude/mask for fixture tables.

---

### Task 3: Integration tests `trigger_redaction_test.exs`

**What to do**

1. Add `test/threadline/capture/trigger_redaction_test.exs` following **`trigger_changed_from_test.exs`** structure: temp table with columns `password` (text), `email` (text), `public_bio` (text), `id` uuid PK.
2. Cases (minimum):
   - **INSERT:** excluded column absent from `data_after` keys; masked column value equals exactly `"[REDACTED]"` string in decoded JSON (unless tests set custom placeholder — then match that literal).
   - **UPDATE:** same + when `store_changed_from: true` with mask on a changed column, `changed_from` for that key is placeholder or absent per D-10 (assert concrete map shape).
   - **DELETE:** no raw leak in any JSON column on audited row (may be null `data_after`).
3. Install SQL via `TriggerSQL` functions produced with the **same options** the Mix task would generate (call Elixir API directly in test to avoid migration file I/O coupling).

**Read first**

- `test/threadline/capture/trigger_changed_from_test.exs`
- `test/support/data_case.ex`

**Acceptance criteria**

- `MIX_ENV=test mix test test/threadline/capture/trigger_redaction_test.exs` exits 0.
- Test file contains the literal substring `REDN` in a comment **or** `@tag` module attribute documenting requirement traceability.

**Done condition:** Integration tests green locally/CI.

---

### Task 4: README + `guides/domain-reference.md`

**What to do**

1. Document `:trigger_capture` (or final atom), per-table `exclude` vs `mask`, overlap error, `MIX_ENV` note, json/jsonb whole-value behavior, link to `mix threadline.gen.triggers`.
2. `guides/domain-reference.md`: add subsection **Redaction at capture** with same semantics in operator-oriented language.

**Read first**

- `README.md` (existing gen.triggers section)
- `guides/domain-reference.md`

**Acceptance criteria**

- `grep -F "exclude" README.md` exits 0.
- `grep -F "mask" guides/domain-reference.md` exits 0.
- `grep -F "trigger_capture" README.md` exits 0 **or** grep matches the actual atom implemented — adjust grep to chosen atom.

**Done condition:** Operator can configure redaction without reading Elixir source.

---

### Task 5: CI + regression sweep

**What to do**

1. Run `MIX_ENV=test mix ci.all` from repo root.

**Read first**

- `mix.exs` aliases

**Acceptance criteria**

- Command exits 0.

**Done condition:** CI contract green.

---

## Verification

1. `MIX_ENV=test mix test test/threadline/capture/trigger_redaction_test.exs test/threadline/capture/trigger_changed_from_test.exs test/threadline/capture/trigger_test.exs`
2. `MIX_ENV=test mix ci.all`
3. Record results in `12-02-SUMMARY.md`.

---

## Success criteria

- REDN-01 and REDN-02 verifiable by automated integration tests and docs.
- Roadmap success criteria 1–3 satisfied by test + doc artifacts; SC4 satisfied by explicit threat model + review note in SUMMARY.
