---
phase: 104-reference-walkthrough-charter-override-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONE-ARC.md
autonomous: true
requirements:
  - CHARTER-01
  - CHARTER-02
  - CHARTER-03
must_haves:
  truths:
    - "A maintainer opening .planning/PROJECT.md can identify v1.23 as the synthetic-first-adopter milestone within <2 minutes"
    - "A maintainer can identify why v1.22's real-adopter-first rule was set aside (Rationale cell of new Key Decisions row)"
    - "A maintainer can identify what would re-engage the v1.22 rule (bolded clause in same row)"
    - "v1.23 non-goals are locked in PROJECT.md Current Milestone block (no new evidence subjects, no Threadline-owned RBAC/tenancy DSLs, no `lib/` auth/domain code, no 'demo product' rebrand, no Sigra extension absent contract gap)"
    - ".planning/MILESTONE-ARC.md identifies v1.23 as active in both the strategic-thesis paragraph and the arc-order table"
    - ".planning/MILESTONE-ARC.md no longer actively lies about v1.22 being active (header lines 3-5 + arc-order row + option-record row are all flipped to past tense / shipped)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "v1.23 override Key Decision row + v1.23 non-goals subsection + updated footnote"
      contains: "v1.23 deliberately overrides v1.22"
    - path: ".planning/MILESTONE-ARC.md"
      provides: "v1.23-aware header + strategic-thesis sentence + v1.22 shipped status + new v1.23 arc-order row"
      contains: "v1.23 — Realistic-Demo Walkthrough"
  key_links:
    - from: ".planning/PROJECT.md (Key Decisions row)"
      to: ".planning/PROJECT.md (Current Milestone block)"
      via: "shared override narrative; reread cohesion at milestone-arc time"
      pattern: "Re-engages the v1.22 rule"
    - from: ".planning/MILESTONE-ARC.md (strategic thesis)"
      to: ".planning/PROJECT.md (Key Decisions row)"
      via: "MILESTONE-ARC.md strategic-thesis sentence references 'see PROJECT.md Key Decisions'"
      pattern: "see PROJECT.md Key Decisions"
---

<objective>
Phase 104 records — in two specific planning files — the deliberate v1.22 → v1.23 framing override and locks v1.23-scoped non-goals so the next milestone-arc reread does not re-litigate the decision.

This plan executes the locked CONTEXT.md decisions D-02, D-03, and D-04 across `.planning/PROJECT.md` and `.planning/MILESTONE-ARC.md`. Single coherent doc-edit set delivered in one plan, one commit. No code, no tests, no `guides/` edits, no example-app edits.

Purpose: After this plan ships, a maintainer reopening the planning surface in six months can identify in <2 minutes that v1.23 is the synthetic-first-adopter milestone, why the v1.22 rule was set aside, and what would re-engage it.

Output: Two edited `.planning/` files committed in one coherent change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/MILESTONE-ARC.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/104-reference-walkthrough-charter-override-decision/104-CONTEXT.md
@.planning/phases/104-reference-walkthrough-charter-override-decision/104-RESEARCH.md
@.planning/phases/104-reference-walkthrough-charter-override-decision/104-VALIDATION.md
</context>

<scope_guard>
**ALLOWED files:** `.planning/PROJECT.md`, `.planning/MILESTONE-ARC.md` ONLY.

**FORBIDDEN:** Any change under `lib/`, `test/`, `guides/`, `examples/`, `config/`, `mix.exs`. Any change to `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` (those are upstream sources — Phase 104 is downstream of them). No new files in `.planning/phases/104-*/` other than the SUMMARY.md the executor writes at end-of-phase. No `104-SUMMARY.md` content gets created during this plan — D-05's precedent rule lives in CONTEXT.md and is surfaced in SUMMARY.md by execute-phase/verify-phase, not by this PLAN.

If any edit you are about to make falls outside the two ALLOWED files, STOP. This is a scope-locked docs phase.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: PROJECT.md — insert v1.23 non-goals subsection, Key Decisions row, and refresh stale footnote</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 1-50 for Current Milestone block, lines 320-373 for Key Decisions table and footnote — read current state in full before editing)
    - .planning/phases/104-reference-walkthrough-charter-override-decision/104-CONTEXT.md (D-02 supplies the EXACT verbatim Key Decisions row text; D-03 supplies the EXACT verbatim non-goals bullets)
    - .planning/phases/104-reference-walkthrough-charter-override-decision/104-RESEARCH.md (Line-Number / Current-Text Verification section confirms insertion points; Landmines 1, 2, 7 are directly relevant)
  </read_first>
  <action>
Perform THREE discrete edits to `.planning/PROJECT.md` in this single task. All three are independent string-level inserts/replacements — no edit depends on another.

**Edit 1A — Insert `**v1.23 non-goals:**` subsection between line 30 (blank line closing the Key context block) and line 31 (`**Current State:**`).** Per D-03, insert verbatim (preserve markdown exactly, including the blank line after the heading and one blank line at the end before `**Current State:**`):

```
**v1.23 non-goals:**

- No new `Threadline.Evidence` subjects beyond the six shipped in v1.22 — walkthrough exercises shipped subjects only; new-subject pressure routes to v1.24 seeds, not in-milestone scope creep.
- No Threadline-owned RBAC / tenancy DSLs and no `lib/` auth or domain-model code — the host-owned auth/tenancy boundary that has held since v1.15 is not relitigated; all auth and help-desk domain work lives in `examples/threadline_phoenix/`.
- No rebrand of `examples/threadline_phoenix/` from "reference app" to "demo product" — seed data + clickable UI enables the walkthrough; it does not reposition the artifact. Sigra integration is not extended unless real signup/login surfaces a contract gap (handled via sub-phase 106b escape valve).
```

Resulting layout: `**Key context:**` bullets (lines 24-29) → blank line (line 30) → `**v1.23 non-goals:**` block → blank line → `**Current State:** v1.22 — Policy / Evidence Plane (closed 2026-05-27). v1.23 opened 2026-05-27.`

Landmine guard: verify `**v1.23 non-goals:**` appears BEFORE `**Current State:**` in the resulting file. Inserting it after `**Current State:**` is a known mechanical-edit failure mode.

**Edit 1B — Append a new Key Decisions row immediately after the current last row of the table (line 350 — the D-02 archive/closeout-gate separation row).** Per D-02, insert verbatim — single dense row with bolded re-engagement clause:

```
| v1.23 deliberately overrides v1.22's "real-adopter feedback first" closeout rule and ships a synthetic-first-adopter walkthrough instead | No real adopter exists at v1.22 close, and the alternative is shipping nothing — a maintainer walking the reference app on a clean clone is the strongest available signal until external pressure arrives. Override scoped to v1.23 only and does not extend evidence subjects, Threadline-owned RBAC/tenancy DSLs, or `lib/` auth code. **Re-engages the v1.22 rule on first sustained real-adopter signal** (live-integration issue, maintainer-confirmed pilot host, or named procurement/security-review/evaluation conversation); drive-by interest routes to `.planning/v1.24-seeds/` rather than re-engaging the rule. | — Active (recorded Phase 104, v1.23, 2026-05-27) |
```

The row goes between the existing line 350 (last data row) and the existing line 351 (blank line preceding `## Evolution`). The table has no footer row — append the row as the new last data row. Confirm the row begins with `| v1.23 deliberately overrides` and ends with ` |` followed by a newline.

**Edit 1C — Flip the stale footnote tense on the last line of the file (currently line 372–373).** Current text:

```
*Last updated: 2026-05-27 — v1.23 Realistic-Demo Walkthrough opened. Current Milestone section added, Active requirements seeded with Phase 104–110 placeholders; Phase 104 will record the v1.22-override Key Decision and update MILESTONE-ARC.md.*
```

Replace `Phase 104 will record the v1.22-override Key Decision and update MILESTONE-ARC.md` with `Phase 104 recorded the v1.22-override Key Decision and updated MILESTONE-ARC.md`. This is RESEARCH.md Landmine 7 — keep the doc honest post-commit. (Per CONTEXT.md "Claude's Discretion" + RESEARCH.md Open Question 1 recommendation; <5-second edit; prevents the footnote from actively lying after this phase lands.)

**Do NOT touch any other line in `.planning/PROJECT.md`.** No reformatting, no whitespace cleanup, no markdown "improvements." Three surgical edits, nothing else.
  </action>
  <verify>
    <automated>grep -Fq '**v1.23 non-goals:**' .planning/PROJECT.md &amp;&amp; grep -Fq 'No new `Threadline.Evidence` subjects beyond the six shipped in v1.22' .planning/PROJECT.md &amp;&amp; grep -Fq 'No Threadline-owned RBAC / tenancy DSLs and no `lib/` auth or domain-model code' .planning/PROJECT.md &amp;&amp; grep -Fq 'No rebrand of `examples/threadline_phoenix/` from "reference app" to "demo product"' .planning/PROJECT.md &amp;&amp; grep -Fq 'v1.23 deliberately overrides v1.22' .planning/PROJECT.md &amp;&amp; grep -Fq '**Re-engages the v1.22 rule on first sustained real-adopter signal**' .planning/PROJECT.md &amp;&amp; grep -Fq '— Active (recorded Phase 104, v1.23, 2026-05-27)' .planning/PROJECT.md &amp;&amp; grep -Fq 'Phase 104 recorded the v1.22-override Key Decision and updated MILESTONE-ARC.md' .planning/PROJECT.md &amp;&amp; ! grep -Fq 'Phase 104 will record the v1.22-override' .planning/PROJECT.md</automated>
    <ordering-check>awk '/\*\*v1.23 non-goals:\*\*/{a=NR} /\*\*Current State:\*\*/{b=NR} END{exit (a&amp;&amp;b&amp;&amp;a&lt;b)?0:1}' .planning/PROJECT.md</ordering-check>
  </verify>
  <done>
- `**v1.23 non-goals:**` subsection present in PROJECT.md Current Milestone block, positioned BEFORE `**Current State:**` (ordering-check passes).
- Three non-goal bullets present verbatim (Evidence subjects, RBAC/tenancy/`lib/`, "demo product" rebrand + Sigra carve-out).
- New Key Decisions row present with the verbatim opening clause `v1.23 deliberately overrides v1.22's "real-adopter feedback first" closeout rule` and the bolded re-engagement clause `**Re-engages the v1.22 rule on first sustained real-adopter signal**`.
- Status cell uses the verbatim shape `— Active (recorded Phase 104, v1.23, 2026-05-27)`.
- Footnote on the last line of the file uses past tense (`recorded` / `updated`), not future tense (`will record`).
- No other lines in PROJECT.md modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: MILESTONE-ARC.md — refresh header (lines 3-5), append strategic-thesis sentence, rewrite option-record row 6 to past tense</name>
  <files>.planning/MILESTONE-ARC.md</files>
  <read_first>
    - .planning/MILESTONE-ARC.md (full file — only 41 lines; read all of it for surrounding context before editing)
    - .planning/phases/104-reference-walkthrough-charter-override-decision/104-CONTEXT.md (D-04 items 1-5 supply EXACT target text for header lines, strategic-thesis append, and option-record row 6 status field)
    - .planning/phases/104-reference-walkthrough-charter-override-decision/104-RESEARCH.md (Line-Number / Current-Text Verification confirms lines 3, 4, 5, 9, 22 are accurate; Landmines 3, 4, 6 directly relevant — past-tense rewording, plain ASCII, correct table targeting)
  </read_first>
  <action>
Perform FIVE discrete edits to `.planning/MILESTONE-ARC.md` in this single task. All five edits target the upper portion of the file (header through option-record table). Plain ASCII throughout — no smart quotes, no Unicode curly apostrophes (Landmine 4).

**Edit 2A — Line 3 header `Updated` date.** Replace the exact string `**Updated:** 2026-05-25` with `**Updated:** 2026-05-27`.

**Edit 2B — Line 4 header `Active milestone`.** Replace the exact string `**Active milestone:** v1.22 — Policy / Evidence Plane` with `**Active milestone:** v1.23 — Realistic-Demo Walkthrough`. (Em-dash `—` is preserved — same character that already appears on the line.)

**Edit 2C — Line 5 header `Next ranked candidate`.** Replace the exact string `**Next ranked candidate:** TBD after v1.22 closeout` with `**Next ranked candidate:** TBD after v1.23 closeout`.

**Edit 2D — Line 9 strategic-thesis paragraph — APPEND a sentence to the existing paragraph (do not replace the paragraph).** Current line 9 text ends with `...without widening Threadline into a Threadline-owned auth, tenancy, or compliance platform.`. Append a single space and then the verbatim D-04 item 4 sentence:

```
v1.23 deliberately overrides v1.22's 'real-adopter-first' closeout rule because no real adopter has surfaced yet and the alternative is shipping nothing; the override re-engages on the first sustained real-adopter signal (see PROJECT.md Key Decisions).
```

Result: line 9 becomes a three-sentence paragraph. Use plain ASCII straight apostrophes (`'`) — not curly `'` — around `'real-adopter-first'`. The two existing sentences on line 9 are preserved verbatim.

**Edit 2E — Option-record row 6 (line 22) — status field flip + Why cell past-tense rewrite.** Current line 22:

```
| 6 | Policy / compliance depth | **Active (v1.22)** | After the support-safe adopter lane is proven, strengthen durable evidence records and audit-of-audit posture without broadening into a Threadline-owned platform expansion. |
```

Replace with:

```
| 6 | Policy / compliance depth | **Shipped (v1.22)** | Shipped durable evidence records and audit-of-audit posture after the support-safe adopter lane proved out — without broadening into a Threadline-owned platform expansion. |
```

Rationale for the rewrite (planner's discretion per CONTEXT.md "Claude's Discretion" + RESEARCH.md Landmine 3): flips `is proven` → `proved out`, `strengthen` → `Shipped … records and posture`, keeps the boundary clause unchanged ("without broadening into a Threadline-owned platform expansion"). Reads as a done-thing, not a goal. Does not restate v1.22 facts already captured in PROJECT.md.

**Do NOT touch any other line in MILESTONE-ARC.md in this task.** Task 3 handles the arc-order table (lines 26-34 and the append after line 34) separately so the table edits are isolated from the upper-portion edits.

Verify after each edit: file is still 41 lines (no lines added or removed by Task 2 — Task 2 is purely in-place replacements + one same-line append).
  </action>
  <verify>
    <automated>grep -Fq '**Updated:** 2026-05-27' .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq '**Active milestone:** v1.23 — Realistic-Demo Walkthrough' .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq '**Next ranked candidate:** TBD after v1.23 closeout' .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq "v1.23 deliberately overrides v1.22's 'real-adopter-first' closeout rule because no real adopter has surfaced yet and the alternative is shipping nothing" .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq 'the override re-engages on the first sustained real-adopter signal (see PROJECT.md Key Decisions).' .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq '| 6 | Policy / compliance depth | **Shipped (v1.22)** |' .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq 'Shipped durable evidence records and audit-of-audit posture after the support-safe adopter lane proved out' .planning/MILESTONE-ARC.md &amp;&amp; ! grep -Fq '**Updated:** 2026-05-25' .planning/MILESTONE-ARC.md &amp;&amp; ! grep -Fq '**Active milestone:** v1.22 — Policy / Evidence Plane' .planning/MILESTONE-ARC.md &amp;&amp; ! grep -Fq 'TBD after v1.22 closeout' .planning/MILESTONE-ARC.md &amp;&amp; ! grep -Fq '| 6 | Policy / compliance depth | **Active (v1.22)** |' .planning/MILESTONE-ARC.md</automated>
    <line-count-check>[ "$(wc -l &lt; .planning/MILESTONE-ARC.md)" = "41" ]</line-count-check>
  </verify>
  <done>
- Line 3 reads `**Updated:** 2026-05-27`.
- Line 4 reads `**Active milestone:** v1.23 — Realistic-Demo Walkthrough`.
- Line 5 reads `**Next ranked candidate:** TBD after v1.23 closeout`.
- Line 9 strategic-thesis paragraph contains the appended sentence verbatim, including the `(see PROJECT.md Key Decisions)` parenthetical at the end.
- Line 22 option-record row 6 status field is `**Shipped (v1.22)**` (was `**Active (v1.22)**`), Why cell reads as past-tense done-thing language.
- Stale strings (`Updated: 2026-05-25`, `Active milestone: v1.22 — Policy / Evidence Plane`, `TBD after v1.22 closeout`, `Active (v1.22)` in row 6) are GONE.
- File line count unchanged at 41 (no lines added or removed by this task).
- No edits to lines 26-34 or anywhere else (Task 3 owns the arc-order table).
  </done>
</task>

<task type="auto">
  <name>Task 3: MILESTONE-ARC.md — flip v1.22 arc-order row status to shipped and append new v1.23 arc-order row</name>
  <files>.planning/MILESTONE-ARC.md</files>
  <read_first>
    - .planning/MILESTONE-ARC.md (lines 24-35 — the arc-order table starting at `## Arc order` on line 24, header at 26, data rows 28-34, blank line at 35)
    - .planning/phases/104-reference-walkthrough-charter-override-decision/104-CONTEXT.md (D-04 items 6 and 7 supply EXACT target text — D-04 item 7 is the verbatim v1.23 row)
    - .planning/phases/104-reference-walkthrough-charter-override-decision/104-RESEARCH.md (Edits 6 and 7 sections confirm line 34 is the v1.22 row; Landmines 5 and 6 directly relevant — backtick spans valid in tables, target is arc-order table NOT option-record table)
  </read_first>
  <action>
Perform TWO discrete edits to the arc-order table in `.planning/MILESTONE-ARC.md`. Both edits target the table under `## Arc order` (line 24) — NOT the table under `## Option record` (line 11). Landmine 6 guard: confirm visually before editing that you are below line 24 (`## Arc order`) and operating on the 6-column table (Version, Status, Theme, Why now, Unlocks, Non-goals), not the 4-column option-record table.

**Edit 3A — Line 34 v1.22 arc-order row status field flip.** Current line 34 begins:

```
| v1.22 | **active** | Policy / Evidence Plane | ...
```

Replace ONLY the status field `**active**` with `**shipped**`. All other cells (Theme, Why now, Unlocks, Non-goals) on this row are preserved EXACTLY as-is. Resulting line 34 begins:

```
| v1.22 | **shipped** | Policy / Evidence Plane | ...
```

This is a single-cell, in-place replacement on the existing line. Do not touch any other cell of the v1.22 row.

**Edit 3B — Append new v1.23 arc-order row after the current line 34 (the v1.22 row).** Per D-04 item 7, insert the row verbatim (single line, six cells, backticks preserved as inline-code spans which are valid in markdown tables — Landmine 5):

```
| v1.23 | **active** | Realistic-Demo Walkthrough | Synthetic-first-adopter override of v1.22's real-adopter-first rule — no real adopter exists and the alternative is shipping nothing. | Walkthrough-surfaced design gaps as v1.24 seeds; first concrete fix-vs-defer rule in practice; durable boundary against scope creep tested under synthetic pressure. | No new Evidence subjects, no Threadline-owned RBAC/tenancy DSLs, no `lib/` auth or domain code, no "demo product" rebrand, no Sigra integration extension absent contract gap. Re-engages v1.22 rule on first sustained real-adopter signal. |
```

The new row sits between the existing v1.22 row (was line 34, now the line above the v1.23 row) and the existing blank line that precedes `## Activation rules`. Result: the arc-order table grows by one data row; the file grows by one line (41 → 42 total lines). The v1.23 row is the new last data row of the arc-order table.

Plain ASCII em-dash (`—`) and straight quotes (`"`) used throughout — copy verbatim from this prompt, do not paraphrase or "improve" the punctuation.

**Do NOT touch any other line in MILESTONE-ARC.md in this task.** Tasks 1 and 2 are complete; do not re-touch Task 2's edits to lines 3-5, 9, or 22.
  </action>
  <verify>
    <automated>grep -Fq '| v1.22 | **shipped** | Policy / Evidence Plane |' .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq '| v1.23 | **active** | Realistic-Demo Walkthrough |' .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq "Synthetic-first-adopter override of v1.22's real-adopter-first rule — no real adopter exists and the alternative is shipping nothing." .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq 'Walkthrough-surfaced design gaps as v1.24 seeds; first concrete fix-vs-defer rule in practice' .planning/MILESTONE-ARC.md &amp;&amp; grep -Fq 'Re-engages v1.22 rule on first sustained real-adopter signal.' .planning/MILESTONE-ARC.md &amp;&amp; ! grep -Fq '| v1.22 | **active** | Policy / Evidence Plane |' .planning/MILESTONE-ARC.md</automated>
    <table-ordering-check>awk '/^\| v1.22 \| \*\*shipped\*\*/{a=NR} /^\| v1.23 \| \*\*active\*\*/{b=NR} END{exit (a&amp;&amp;b&amp;&amp;b==a+1)?0:1}' .planning/MILESTONE-ARC.md</table-ordering-check>
    <line-count-check>[ "$(wc -l &lt; .planning/MILESTONE-ARC.md)" = "42" ]</line-count-check>
  </verify>
  <done>
- v1.22 arc-order row status field is `**shipped**` (was `**active**`); all other cells on the v1.22 row unchanged.
- New v1.23 arc-order row present immediately after the v1.22 row (table-ordering-check passes: v1.23 row is line v1.22 row + 1).
- v1.23 row contains all six cells verbatim from D-04 item 7, including the `**active**` status, the synthetic-first-adopter override theme phrase, the v1.24 seeds Unlocks framing, and the re-engagement-trigger phrase in the Non-goals cell.
- Stale string `| v1.22 | **active** | Policy / Evidence Plane |` is GONE.
- File line count is now 42 (one line added by this task; was 41 before).
- No edits to lines 3-5, 9, 22, or anything outside the arc-order table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this phase) | Phase 104 is `.planning/`-only docs work. No code, no input handling, no external integrations, no package installs. No new trust boundaries introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Tampering | `.planning/PROJECT.md` Key Decisions table | accept | Doc-only edit; git history is the integrity surface; the new row passes grep-verifiable substring checks. No runtime trust boundary. |
| T-104-02 | Tampering | `.planning/MILESTONE-ARC.md` arc-order table | accept | Doc-only edit; git history is the integrity surface; both edits pass grep-verifiable substring checks. No runtime trust boundary. |
| T-104-03 | Information disclosure | None | accept | No secrets, no PII, no credentials introduced. The two edited files are already public in the repository. |

**Package legitimacy gate:** N/A — no package installs in this phase (no `mix.exs` edits, no `mix deps.get`, no npm/pip/cargo). Audit table not required.

**Rationale for the all-accept disposition:** This is a documentation-only planning phase with no code, no inputs, no execution surface, and no new attack surface. STRIDE applies thinly; all threats reduce to "someone tampers with the planning docs," which is governed by repository-level git/branch protection, not phase-level mitigations.
</threat_model>

<verification>
## Plan-level verification (run after all three tasks complete)

**Automated (grep-based, runs in <5 seconds):**

```bash
# PROJECT.md edits (Task 1)
grep -Fq '**v1.23 non-goals:**' .planning/PROJECT.md
grep -Fq 'v1.23 deliberately overrides v1.22' .planning/PROJECT.md
grep -Fq '**Re-engages the v1.22 rule on first sustained real-adopter signal**' .planning/PROJECT.md
grep -Fq '— Active (recorded Phase 104, v1.23, 2026-05-27)' .planning/PROJECT.md
grep -Fq 'Phase 104 recorded the v1.22-override Key Decision' .planning/PROJECT.md
! grep -Fq 'Phase 104 will record the v1.22-override' .planning/PROJECT.md

# MILESTONE-ARC.md edits (Tasks 2 and 3)
grep -Fq '**Updated:** 2026-05-27' .planning/MILESTONE-ARC.md
grep -Fq '**Active milestone:** v1.23 — Realistic-Demo Walkthrough' .planning/MILESTONE-ARC.md
grep -Fq '**Next ranked candidate:** TBD after v1.23 closeout' .planning/MILESTONE-ARC.md
grep -Fq "v1.23 deliberately overrides v1.22's 'real-adopter-first' closeout rule" .planning/MILESTONE-ARC.md
grep -Fq '| 6 | Policy / compliance depth | **Shipped (v1.22)** |' .planning/MILESTONE-ARC.md
grep -Fq '| v1.22 | **shipped** | Policy / Evidence Plane |' .planning/MILESTONE-ARC.md
grep -Fq '| v1.23 | **active** | Realistic-Demo Walkthrough |' .planning/MILESTONE-ARC.md
! grep -Fq '**Updated:** 2026-05-25' .planning/MILESTONE-ARC.md
! grep -Fq '| v1.22 | **active** | Policy / Evidence Plane |' .planning/MILESTONE-ARC.md

# Scope guard (no out-of-scope files touched)
git diff --name-only HEAD | grep -v -E '^\.planning/(PROJECT\.md|MILESTONE-ARC\.md|phases/104-)' | wc -l
# expect: 0 (no files outside .planning/PROJECT.md, .planning/MILESTONE-ARC.md, and the phase dir itself were modified)
```

**Manual (cold-read — surfaces in verify-phase, not in execute-phase grep gates):**

Maintainer opens `.planning/PROJECT.md` and `.planning/MILESTONE-ARC.md` from scratch and confirms within 2 minutes that:
1. v1.23 is identifiable as the synthetic-first-adopter milestone (PROJECT.md Current Milestone Goal + MILESTONE-ARC.md arc-order v1.23 row Theme cell).
2. The reason v1.22's rule was set aside is identifiable (PROJECT.md Key Decisions row Rationale cell + MILESTONE-ARC.md strategic-thesis third sentence).
3. The re-engagement trigger is identifiable (bolded `**Re-engages the v1.22 rule…**` clause in PROJECT.md Key Decisions row).

This <2-minute cold-read criterion is human-validated only. It is recorded as a manual check in `104-VERIFICATION.md` during verify-phase, not asserted by this plan.
</verification>

<success_criteria>
Plan complete when all of:

1. **CHARTER-01** (PROJECT.md Key Decisions row) — verified by `grep -Fq 'v1.23 deliberately overrides v1.22' .planning/PROJECT.md` AND `grep -Fq '**Re-engages the v1.22 rule on first sustained real-adopter signal**' .planning/PROJECT.md`.
2. **CHARTER-02** (MILESTONE-ARC.md v1.23 arc-order row + strategic-thesis sentence) — verified by `grep -Fq '| v1.23 | **active** | Realistic-Demo Walkthrough |' .planning/MILESTONE-ARC.md` AND `grep -Fq "v1.23 deliberately overrides v1.22's 'real-adopter-first' closeout rule" .planning/MILESTONE-ARC.md`.
3. **CHARTER-03** (v1.23 non-goals locked in PROJECT.md) — verified by `grep -Fq '**v1.23 non-goals:**' .planning/PROJECT.md` AND ordering-check (`**v1.23 non-goals:**` precedes `**Current State:**`).
4. **Scope guard** — git diff for the phase commit shows ONLY `.planning/PROJECT.md` and `.planning/MILESTONE-ARC.md` modified (plus phase-dir artifacts the executor naturally produces — SUMMARY.md, etc.). No `lib/`, `test/`, `guides/`, `examples/`, `config/`, or `mix.exs` changes.
5. **No stale-doc regressions** — `! grep -Fq 'Phase 104 will record the v1.22-override' .planning/PROJECT.md` AND `! grep -Fq '**Updated:** 2026-05-25' .planning/MILESTONE-ARC.md` AND `! grep -Fq '| v1.22 | **active** | Policy / Evidence Plane |' .planning/MILESTONE-ARC.md`.

The <2-minute cold-read criterion (ROADMAP success criterion 4) is a verify-phase concern, not an execute-phase plan gate.
</success_criteria>

<output>
Create `.planning/phases/104-reference-walkthrough-charter-override-decision/104-01-SUMMARY.md` when done.

The SUMMARY.md should include:
- Standard GSD summary header
- One-line precedent rule per D-05 (CONTEXT.md): *"Closeout-gate and first-milestone-phase work may correct demonstrably-stale status fields in archive-owned docs, but never restructure narrative sections or option/rank ordering — that remains `/gsd-complete-milestone`'s job."*
- Pass/fail record for the 5 success-criteria checks above
- Note that the <2-minute cold-read criterion deferred to verify-phase / VERIFICATION.md

The SUMMARY.md is written by execute-phase as it lands tasks; this PLAN does NOT include a task to author SUMMARY.md content.
</output>
