---
phase: 119-phx-gen-auth-integration-guide-lane
plan: 02
type: execute
wave: 2
depends_on:
  - 119-01
files_modified:
  - guides/upgrade-path.md
autonomous: true
requirements:
  - AUTH-LANE-01
  - AUTH-LANE-02
must_haves:
  truths:
    - "upgrade-path.md names phx-gen-auth-reference as a reference lane with guide-based proof vocabulary."
    - "sigra-reference matrix row and semantics remain unchanged."
    - "phx-gen-auth-reference does not claim Sigra compatibility."
    - "No compatibility matrix row added in Phase 119 (deferred Phase 120)."
  artifacts:
    - path: guides/upgrade-path.md
      provides: "Lane detection prose for phx-gen-auth-reference"
---

<objective>
Extend `guides/upgrade-path.md` with **prose-only** `phx-gen-auth-reference` lane vocabulary (AUTH-LANE-01/02) without adding a matrix row or implying root CI already proves the lane.

Purpose: Let adopters self-identify their auth lane before Phase 120 ships matrix + integration tests.
Output: Targeted edits to Who / How to tell / vocabulary / checklist sections only.
</objective>

<context>
@.planning/phases/119-phx-gen-auth-integration-guide-lane/119-CONTEXT.md (D-14, D-15, D-16, D-17)
@.planning/phases/119-phx-gen-auth-integration-guide-lane/119-RESEARCH.md
@guides/integrations/phx-gen-auth.md (must exist from 119-01)
@guides/upgrade-path.md (current three-lane structure)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-119-04 | Premature matrix row claims CI proof | medium | Do not edit Supported compatibility matrix table |
| T-119-05 | phx lane implies Sigra interchangeability | medium | Explicit "not Sigra-compatible" sentence in lane paragraph |
| T-119-06 | Accidental sigra-reference regression | low | Grep count on sigra-reference unchanged in matrix row literal |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend Who / How to tell / vocabulary for phx-gen-auth-reference</name>
  <files>guides/upgrade-path.md</files>
  <read_first>
    - guides/upgrade-path.md (## Who this guide is for, ## How to tell, support vocabulary bullets)
    - guides/integrations/phx-gen-auth.md (proof wording to mirror)
    - .planning/phases/119-phx-gen-auth-integration-guide-lane/119-CONTEXT.md (D-14, D-15)
  </read_first>
  <action>
Edit `guides/upgrade-path.md`:

1. **## Who this guide is for** — add bullet after sigra bullet:
   `- deciding whether you are running phx.gen.auth-style session auth and should follow phx-gen-auth-reference`

2. **## How to tell which lane you are on** — add new paragraph after `sigra-reference` paragraph (before "For 0.5.0 support-lane wording"):
   ```
   You are on the `phx-gen-auth-reference` lane when your Phoenix host uses `mix phx.gen.auth` (or equivalent generated session auth) and wires `Threadline.Plug` with a host-owned actor module derived from `conn.assigns[:current_scope]` per `guides/integrations/phx-gen-auth.md`. Proof for this lane is the maintained guide plus forthcoming root integration tests in this repo — not a second example application. This lane is not Sigra-compatible and is narrower than generic `phoenix-surface`.
   ```

3. Under "That distinction matters more than dependency rows alone" (after `sigra-reference` bullet, before unclaimed bullet), add:
   `- phx-gen-auth-reference is a reference lane for Phoenix hosts on generated session auth; it is proven by guides/integrations/phx-gen-auth.md and focused root verification, not by examples/threadline_phoenix/ or Sigra.`

4. **Do not** add a row to the `## Supported compatibility matrix` table (Phase 120).

Preserve existing `sigra-reference` matrix row text exactly.
  </action>
  <acceptance_criteria>
    - `grep -F 'phx-gen-auth-reference' guides/upgrade-path.md` exits 0
    - `grep -F 'guides/integrations/phx-gen-auth.md' guides/upgrade-path.md` exits 0
    - `grep -F 'not Sigra-compatible' guides/upgrade-path.md` exits 0
    - `grep -F '| `phx-gen-auth-reference` |' guides/upgrade-path.md` exits 1 (no matrix row)
    - `grep -F '| `sigra-reference` | `reference` |' guides/upgrade-path.md` exits 0 (unchanged row)
  </acceptance_criteria>
  <verify>grep -F 'phx-gen-auth-reference' guides/upgrade-path.md</verify>
</task>

<task type="auto">
  <name>Task 2: Release checklist + canonical references cross-link</name>
  <files>guides/upgrade-path.md</files>
  <read_first>
    - guides/upgrade-path.md (## Release checklist, ## Canonical references)
    - .planning/phases/119-phx-gen-auth-integration-guide-lane/119-CONTEXT.md (D-15, D-17)
  </read_first>
  <action>
1. **## Release checklist for adopters** — update first bullet from three-lane to four-lane wording:
   `Decide whether you are capture-only, phoenix-surface, sigra-reference, or phx-gen-auth-reference.`
   Add checklist item after sigra-reference item:
   `- If you are on phx-gen-auth-reference, read guides/integrations/phx-gen-auth.md and compare your host actor_fn and authorize_fn wiring before deploy.`

2. **## Canonical references** — add bullet:
   `- guides/integrations/phx-gen-auth.md`

Do not cite `test/threadline/integrations/phx_gen_auth_integration_test.exs` anywhere in upgrade-path.md in this phase.
  </action>
  <acceptance_criteria>
    - `grep -F 'phx-gen-auth-reference' guides/upgrade-path.md` | wc -l` is at least 4
    - `grep -F 'phx_gen_auth_integration_test' guides/upgrade-path.md` exits 1
    - `grep -F 'guides/integrations/phx-gen-auth.md' guides/upgrade-path.md` exits 0
    - `grep -F 'guides/integrations/sigra.md' guides/upgrade-path.md` exits 0 (sigra link preserved)
  </acceptance_criteria>
  <verify>grep -c 'phx-gen-auth-reference' guides/upgrade-path.md</verify>
</task>

</tasks>

<verification>
- `mix format --check-formatted guides/upgrade-path.md`
- Confirm matrix table still has exactly 3 data rows (capture-only, phoenix-surface, sigra-reference)
</verification>

## PLANNING COMPLETE
