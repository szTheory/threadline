---
phase: 119-phx-gen-auth-integration-guide-lane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/integrations/phx-gen-auth.md
autonomous: true
requirements:
  - AUTH-GUIDE-01
  - AUTH-GUIDE-02
  - AUTH-GUIDE-03
must_haves:
  truths:
    - "Integrators on stock phx.gen.auth can copy Plug + operator mount snippets without opening guides/integrations/sigra.md."
    - "Guide uses host-owned MyApp.AuditActor (or equivalent) — not Threadline.Integrations.Sigra."
    - "Non-goals state Threadline does not run mix phx.gen.auth, own user tables, or secure routes without host pipelines."
  artifacts:
    - path: guides/integrations/phx-gen-auth.md
      provides: "phx-gen-auth-reference cookbook (~70–90 lines)"
---

<objective>
Create `guides/integrations/phx-gen-auth.md` — the phx.gen.auth integration cookbook with Plug wiring, admin operator mount, reference semantics, optional correlation, non-goals, and lane honesty (AUTH-GUIDE-01/02/03).

Purpose: Close the largest Phoenix SaaS reach gap with a host-owned template module story parallel to sigra.md but without Install/Soft-dep/Sigra correlation formats.
Output: New guide file with HTML marker and grep-verifiable literals.
</objective>

<context>
@.planning/phases/119-phx-gen-auth-integration-guide-lane/119-CONTEXT.md (D-01 through D-13, D-18 through D-21)
@.planning/phases/119-phx-gen-auth-integration-guide-lane/119-RESEARCH.md
@guides/integrations/sigra.md (structure template — omit Install, Soft-dep, correlation_id formats)
@guides/integration-contracts.md
@guides/operator-surface.md (link targets for deferred callbacks)
@lib/threadline/plug.ex
@examples/threadline_phoenix/lib/threadline_phoenix_web/user_auth.ex (fetch_current_scope pattern)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-119-01 | Guide implies Threadline ships phx.gen.auth or owns auth tables | high | AUTH-GUIDE-03 non-goals section with explicit `mix phx.gen.auth` / user table / pipeline bullets |
| T-119-02 | Missing plug order causes nil actor on audited writes | medium | Hard-requirement paragraph: fetch_session → fetch_current_scope → Threadline.Plug |
| T-119-03 | Guide prescribes Sigra correlation prefixes | low | No phx-session:/phx-user: table; link header precedence only |
| T-119-04 | Export routes secured only by LiveView on_mount | medium | Footgun paragraph on export_authorize_fn fallback |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create guide skeleton, marker, intro, and prerequisites</name>
  <files>guides/integrations/phx-gen-auth.md</files>
  <read_first>
    - guides/integrations/sigra.md (lines 1–10 intro + honesty framing)
    - .planning/phases/119-phx-gen-auth-integration-guide-lane/119-CONTEXT.md (D-08 section order, D-10 marker)
  </read_first>
  <action>
Create `guides/integrations/phx-gen-auth.md` with:

1. HTML comment on line 2: `<!-- PHX-GEN-AUTH-03-INTEGRATION-GUIDE -->`
2. H1: `# Threadline ↔ phx.gen.auth integration`
3. Intro paragraph stating this documents the current `phx-gen-auth-reference` lane: a maintained composition path for Phoenix hosts using `mix phx.gen.auth` (or equivalent generated session auth). Include exact phrase: `reference claim, not a blanket support promise` and that it is narrower than generic `phoenix-surface` and not Sigra-compatible.
4. `## Prerequisites` — bullets: host already ran `mix phx.gen.auth` (or equivalent); Threadline does not add an auth Hex dependency; host owns user schema and session tables.
  </action>
  <acceptance_criteria>
    - `test -f guides/integrations/phx-gen-auth.md`
    - `grep -F 'PHX-GEN-AUTH-03-INTEGRATION-GUIDE' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'phx-gen-auth-reference' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'reference claim, not a blanket support promise' guides/integrations/phx-gen-auth.md` exits 0
  </acceptance_criteria>
  <verify>grep -F 'PHX-GEN-AUTH-03-INTEGRATION-GUIDE' guides/integrations/phx-gen-auth.md</verify>
</task>

<task type="auto">
  <name>Task 2: Plug callback wire-up with host AuditActor template</name>
  <files>guides/integrations/phx-gen-auth.md</files>
  <read_first>
    - guides/integrations/sigra.md (## Plug callback wire-up)
    - lib/threadline/plug.ex (actor_fn, context_overrides_fn)
    - lib/threadline/integrations/sigra.ex (Map.get current_scope pattern)
    - .planning/phases/119-phx-gen-auth-integration-guide-lane/119-CONTEXT.md (D-04, D-06, D-07)
  </read_first>
  <action>
Add sections in order:

`## Plug callback wire-up` containing:
- Paragraph: wire `Threadline.Plug` after session + scope assigns exist; link `guides/integration-contracts.md`.
- **Plug order** bullet list: `plug :fetch_session` → `plug :fetch_current_scope` (or host equivalent from generated UserAuth) → `plug Threadline.Plug, ...` on pipelines with audited writes.
- Host module `MyApp.AuditActor` fenced Elixir with `actor_ref_from_conn/1` using:
  ```elixir
  case conn.assigns[:current_scope] do
    %{user: %{id: id}} -> ActorRef.new(:user, to_string(id))
    _ -> nil
  end
  ```
  and `audit_context_overrides_from_conn/1` returning `%{}`.
- Router pipeline fence with:
  ```elixir
  plug Threadline.Plug,
    actor_fn: &MyApp.AuditActor.actor_ref_from_conn/1,
    context_overrides_fn: &MyApp.AuditActor.audit_context_overrides_from_conn/1
  ```
- Short **Phoenix 1.7 legacy** sub-bullet: if only `current_user` exists, host may fall back inside `actor_fn` with a 3-line `case conn.assigns[:current_user]` — scope-first, not dual-primary.
- Sentences locking header precedence: `x-request-id` first, `x-correlation-id` first; overrides additive only; unknown override keys raise `ArgumentError`.
  </action>
  <acceptance_criteria>
    - `grep -F 'MyApp.AuditActor' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'fetch_current_scope' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'actor_fn: &MyApp.AuditActor.actor_ref_from_conn/1' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'current_scope' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'Threadline.Integrations.Sigra' guides/integrations/phx-gen-auth.md` exits 1 (must not appear)
  </acceptance_criteria>
  <verify>grep -F 'actor_fn: &MyApp.AuditActor.actor_ref_from_conn/1' guides/integrations/phx-gen-auth.md</verify>
</task>

<task type="auto">
  <name>Task 3: Operator surface, reference semantics, correlation, non-goals, lane proof</name>
  <files>guides/integrations/phx-gen-auth.md</files>
  <read_first>
    - guides/integrations/sigra.md (## Surface and export auth — abbreviate)
    - guides/operator-surface.md (mount macro)
    - .planning/phases/119-phx-gen-auth-integration-guide-lane/119-CONTEXT.md (D-11–D-13, D-18–D-21, D-15)
  </read_first>
  <action>
Add remaining sections in this order:

1. `## Surface and export auth stay host-owned` — abbreviated split: capture vs operator; admin-only `threadline_operator_surface/2` fence:
   ```elixir
   threadline_operator_surface "/audit",
     authorize_fn: fn
       %{assigns: %{current_user: %{role: "admin"}}}, _ -> :ok
       _, _ -> {:error, :unauthorized}
     end
   ```
   Note host may use `is_admin` instead of `role`. Link `guides/operator-surface.md` for export/evidence/coverage/policy callbacks (deferred depth).
   Footgun paragraph: LiveView `on_mount` does not secure export HTTP routes; export uses `export_authorize_fn` or falls back to `authorize_fn` with `%{assigns: conn.assigns}`.

2. `## Reference semantics` — numbered list (5–7 bullets) of what Phase 120 root tests will prove, e.g.: scope-shaped user id → user actor; logged-out scope → nil actor; admin authorize_fn allows/denies; header correlation wins; overrides additive. Do **not** title this "Behaviors locked by SPEC".

3. `## Optional correlation strategy` — default `%{}`; headers win; optional non-normative W3C traceparent / BFF / `Audit.transaction/3` business ids; remind strict timeline correlation needs `:action` / `Audit.transaction/3`.

4. `## Non-goals` (AUTH-GUIDE-03) — bullets: Threadline does not run `mix phx.gen.auth`; does not own user tables or sessions; does not secure routes without host `require_authenticated_user` / pipeline plugs.

5. `## Lane and proof` — maintained composition path is this guide; root integration tests forthcoming (Phase 120); **do not** cite `phx_gen_auth_integration_test.exs`; not a second example app.

Keep total file between **70 and 95 lines** (`wc -l`).
  </action>
  <acceptance_criteria>
    - `grep -F 'threadline_operator_surface' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'authorize_fn' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'export_authorize_fn' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F '## Non-goals' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'does not run' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'mix phx.gen.auth' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -F 'phx_gen_auth_integration_test' guides/integrations/phx-gen-auth.md` exits 1
    - `wc -l guides/integrations/phx-gen-auth.md | awk '{print $1}'` outputs a number between 70 and 95
  </acceptance_criteria>
  <verify>wc -l guides/integrations/phx-gen-auth.md</verify>
</task>

</tasks>

<verification>
- `mix format --check-formatted guides/integrations/phx-gen-auth.md` (or `mix format` then recheck)
- Section order grep: Prerequisites before Plug before Surface before Reference semantics before Non-goals
</verification>

## PLANNING COMPLETE
