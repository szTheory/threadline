---
phase: 142-responsive-mobile-first
plan: "03"
type: execute
wave: 3
depends_on: ["142-02"]
files_modified:
  - lib/threadline/operator_surface/style.ex
  - examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts
autonomous: true
requirements: [POLISH-RESPONSIVE]
must_haves:
  truths:
    - "Every operator-surface route in the Phase 142 matrix loads and remains usable at 375, 768, and 1280."
    - "The document root has no horizontal overflow regression at any acceptance viewport."
    - "Tables, filters/toolbars, drawers/subviews, and nav are verified in a real browser without screenshot baselines."
  artifacts:
    - path: "examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts"
      provides: "Responsive browser matrix UAT for all operator-surface screens"
      contains: "responsive matrix"
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "Any narrowly scoped CSS remediation needed for matrix failures"
      contains: "--tl-breakpoint-tablet"
  key_links:
    - from: "examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts"
      to: "lib/threadline/operator_surface/style.ex"
      via: "runtime computed styles and overflow checks for `.tl-*` selectors"
      pattern: "scrollWidth - clientWidth"
    - from: "examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts"
      to: "examples/threadline_phoenix/e2e/playwright.config.ts"
      via: "existing Playwright runner and baseURL"
      pattern: "npm test"
---

<objective>
Prove Phase 142 in a browser with a 375 / 768 / 1280 responsive matrix across the operator surface.

Purpose: source contracts prove the CSS vocabulary, but the requirement closes only when real screens load, controls remain reachable, and the root document avoids horizontal overflow at every acceptance viewport.
Output: one focused Playwright matrix spec plus any targeted CSS fixes required by its failures.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/142-responsive-mobile-first/142-CONTEXT.md
@.planning/phases/142-responsive-mobile-first/142-RESEARCH.md
@.planning/phases/142-responsive-mobile-first/142-PATTERNS.md
@.planning/phases/142-responsive-mobile-first/142-UI-SPEC.md
@.planning/phases/142-responsive-mobile-first/142-01-SUMMARY.md
@.planning/phases/142-responsive-mobile-first/142-02-SUMMARY.md
@examples/threadline_phoenix/e2e/package.json
@examples/threadline_phoenix/e2e/playwright.config.ts
@examples/threadline_phoenix/e2e/run-e2e.sh
@examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-prove-mobile.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts
@lib/threadline/operator_surface/style.ex

<interfaces>
Existing E2E setup:
- Specs import `{ expect, Locator, Page, test }` from `@playwright/test`.
- Login helper uses `/users/log_in`, `admin@example.com`, and `process.env.DEMO_SEED_PASSWORD ?? "password123456"`.
- Existing no-overflow helper computes `document.documentElement.scrollWidth - document.documentElement.clientWidth` and requires `<= 1`.
- `examples/threadline_phoenix/e2e/package.json` exposes `npm test` as `playwright test`.
- `examples/threadline_phoenix/e2e/run-e2e.sh` can set up the test database, seed demo data, start the Phoenix example app, and run the full browser suite.

Seeded dynamic route patterns:
- Use `walk-acme-4521-close` as the correlation id.
- Discover a transaction route by visiting `/audit/timeline?correlation_id=walk-acme-4521-close` and clicking the first `transaction-link`.
- Discover a row-history route by filtering `transaction-change-row` for `ticket_replies`, reading/clicking `row-history-link`, or extracting the href.
- Use `/audit/actors/service_account/zendesk-sync` as the stable actor route from existing mobile UAT.

Route matrix:
- `/audit`
- `/audit/timeline`
- `/audit/coverage`
- seeded `/audit/transactions/:id`
- seeded `/audit/rows/:table/:record_id`
- `/audit/actors/service_account/zendesk-sync`
- `/audit/evidence`
- `/audit/policy/redaction`
- `/audit/policy/retention`
- `/audit/exports`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add responsive route matrix browser UAT</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts</files>
  <behavior>
    - Define viewports `{ name: "phone", width: 375, height: 812, isMobile: true }`, `{ name: "tablet", width: 768, height: 900, isMobile: false }`, and `{ name: "desktop", width: 1280, height: 900, isMobile: false }`.
    - Log in once per test using the existing admin helper pattern.
    - Before route assertions, discover the seeded transaction path and row-history path from the Timeline/Transaction UI instead of hardcoding opaque ids.
    - For every viewport and every matrix route, set viewport size, navigate, assert `#tl-main` and `operator-header` are visible, assert root horizontal overflow is `<= 1`, and assert key route-specific controls/content are visible.
    - On every route, prove `.tl-topbar__nav` is visible and `operator-nav-timeline`, `operator-nav-coverage`, `operator-nav-evidence`, `operator-nav-policy`, `operator-nav-retention`, and `operator-nav-exports` can be scrolled into view with expected hrefs.
    - For Timeline at 375 and 768, assert `.tl-toolbar__form` controls stack/wrap without root overflow and `#filter-correlation-id` is usable.
    - For Coverage and Retention, assert `.tl-table--responsive td::before` has non-empty computed `content` at 375/768 and desktop table headers are visible at 1280.
    - For Transaction and Row History, assert row/action controls remain visible, `.tl-value` or `.tl-secondary-ref` content fits inside viewport bounds, and the row-history drawer bounding box width is `<= viewport width`.
    - For Evidence, Redaction, Retention, and Exports, assert the existing dense/prove controls from prior specs remain visible without root overflow.
    - Do not create screenshots, baseline images, Playwright projects, new dependencies, or test-only routes.
  </behavior>
  <action>Create `operator-responsive-mobile-first.spec.ts` by reusing helpers from the existing operator E2E specs, but keep this file self-contained to avoid shared-helper churn. Use loops for the viewport and route matrix so failures report the route and viewport name. Prefer computed overflow, bounding boxes, computed display/content checks, and reachability assertions over screenshots. Keep allowed internal scroll owners explicit in helper names: `.tl-topbar__nav`, `.tl-table-wrap`, `.tl-subview`, and code/value containers. This task implements D-01, D-04, D-05, D-06, D-07, and D-08 without Phase 143 screenshot/accessibility work.</action>
  <verify>
    <automated>test -f examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts</automated>
    <automated>rg -n "375|768|1280|scrollWidth|clientWidth|tl-table--responsive|tl-toolbar__form|tl-subview|operator-nav" examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts</automated>
  </verify>
  <done>`operator-responsive-mobile-first.spec.ts` contains a full route × viewport matrix with root overflow, nav reachability, table/card, toolbar/filter, and drawer/subview checks ready for focused execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run matrix and fix only responsive CSS regressions</name>
  <files>lib/threadline/operator_surface/style.ex, examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts</files>
  <behavior>
    - Run `mix test test/threadline/operator_surface/style_contract_test.exs` before browser verification; source contracts must be green first.
    - If the example app is not already available at `127.0.0.1:4002`, use the existing setup path: `PORT=4002 MIX_ENV=test THREADLINE_E2E=1 mix ecto.create --quiet -r ThreadlinePhoenix.Repo || true`, `PORT=4002 MIX_ENV=test THREADLINE_E2E=1 mix ecto.migrate --quiet`, `PORT=4002 MIX_ENV=test THREADLINE_E2E=1 mix demo.reset`, then start `PORT=4002 MIX_ENV=test THREADLINE_E2E=1 mix phx.server`.
    - Run the focused Playwright spec from `examples/threadline_phoenix/e2e` against `E2E_BASE_URL=http://127.0.0.1:4002`.
    - If the focused matrix fails because of real layout overflow, fix the responsible CSS selector in `style.ex` using existing tokens, wrapping, `min-width: 0`, `max-width: 100%`, internal scroll ownership, or grid/flex constraints.
    - If the focused matrix fails because of an over-broad or flaky selector, refine the spec to assert the same Phase 142 behavior through a deterministic selector; do not loosen the root overflow rule.
    - Do not fix unrelated old E2E failures outside this focused spec. If `mix verify.example_browser` remains red due known unrelated specs, record it in the summary and include the focused spec result.
  </behavior>
  <action>Use the matrix as the acceptance gate for D-08. Keep any remediation to responsive CSS and this one spec. Do not change LiveView markup, data queries, auth, routes, seed data, exports, row-history semantics, motion inventory, Playwright config, package dependencies, screenshots, or Phase 143 accessibility/screenshot infrastructure. Do not mask failures with root/body `overflow-x: hidden`; the root overflow rule must continue to catch regressions.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
    <automated>cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-responsive-mobile-first.spec.ts</automated>
    <automated>rg -n "body[^}]*overflow-x:\\s*hidden|html[^}]*overflow-x:\\s*hidden|\\.threadline-ui[^}]*overflow-x:\\s*hidden" lib/threadline/operator_surface/style.ex && exit 1 || exit 0</automated>
  </verify>
  <done>The source contract and focused responsive matrix pass; every matrix route is usable at 375, 768, and 1280 with root horizontal overflow <= 1 and no masking fix.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase-level browser gate and record exceptions</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-responsive-mobile-first.spec.ts</files>
  <action>Run `mix verify.example_browser` after the focused matrix passes. If it passes, record the full-suite result in the summary. If it fails on unrelated pre-existing specs, record the failing spec names and exact failure classes, then prove the Phase 142 acceptance with the focused matrix command and source-contract command. Do not edit unrelated screenshot specs or old operator specs in this phase unless the failure is caused by the responsive changes from this plan.</action>
  <verify>
    <automated>mix verify.example_browser</automated>
  </verify>
  <done>Phase summary includes either a green full browser gate or a precise exception log plus green focused Phase 142 source/browser acceptance evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser -> authenticated example app | Browser tests log in as seeded admin and navigate real operator routes. |
| route matrix -> responsive acceptance | Runtime checks determine whether CSS changes satisfy the user-visible requirement. |
| internal scroll owner -> root overflow | The test distinguishes approved internal scroll containers from document-level overflow regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-09 | Spoofing | viewport acceptance | mitigate | Spec explicitly sets and names 375, 768, and 1280 viewport sizes in each matrix run. |
| T-142-10 | Tampering | browser proof | mitigate | Assertions use route-specific selectors, computed overflow, and bounding boxes rather than manual screenshots. |
| T-142-11 | Repudiation | full-suite exceptions | mitigate | Summary must record exact commands, base URL, pass counts, and any unrelated failing spec names/classes. |
| T-142-12 | Denial of Service | mobile operator workflows | mitigate | Root overflow and control reachability checks run across all operator-surface screens, including dynamic transaction/row/actor routes. |
| T-142-13 | Information Disclosure | seeded admin session | accept | Tests use existing local seeded demo credentials and do not add logging of secrets or private data. |
| T-142-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; use existing Playwright dependency and E2E runner only. |
</threat_model>

<quality_gates>
- Focused matrix must cover all routes from the Phase 142 context and all three widths: 375, 768, 1280.
- Root document overflow must stay `<= 1`; allowed internal horizontal/vertical scroll owners remain explicit.
- No screenshots, screenshot-diff baselines, Playwright config changes, package installs, new routes, seed changes, or Phase 143 accessibility sweep.
- No edits to unrelated Phase 136/137 artifacts.
- If full `mix verify.example_browser` is red for unrelated existing specs, do not disguise it as green; record the exact residual risk and focused acceptance proof.
</quality_gates>

<verification>
- `mix test test/threadline/operator_surface/style_contract_test.exs`
- `cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-responsive-mobile-first.spec.ts`
- `mix verify.example_browser`
- `rg -n "body[^}]*overflow-x:\\s*hidden|html[^}]*overflow-x:\\s*hidden|\\.threadline-ui[^}]*overflow-x:\\s*hidden" lib/threadline/operator_surface/style.ex && exit 1 || exit 0`
</verification>

<success_criteria>
- Every operator-surface screen in the matrix is usable and correct at 375, 768, and 1280.
- Topbar nav, tables, filters/toolbars, drawers/subviews, and dense prove/find/home surfaces are browser-verified without screenshot baselines.
- No horizontal-scroll regressions exist at the document root, and any full-suite exceptions are separated from Phase 142 acceptance evidence.
</success_criteria>

<output>
Create `.planning/phases/142-responsive-mobile-first/142-03-SUMMARY.md` when done.
</output>
