---
phase: 85-support-lane-surface-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/MILESTONES.md
  - .planning/phases/85-support-lane-surface-audit/85-CONTEXT.md
autonomous: true
requirements:
  - "Lock the milestone claim that all read paths, including Row History/As-Of, will be safely scoped for support operators."
must_haves:
  truths:
    - "The milestone explicitly claims support-lane safety for Timeline, Actor, Transaction, and Row History views."
    - "No existing UI components will be disabled for support agents; instead, `scope_query_fn` will be pushed into core query APIs."
  artifacts:
    - path: .planning/phases/85-support-lane-surface-audit/85-CONTEXT.md
      provides: "The definitive surface audit and requirement lock for the support lane."
      contains: "RowHistoryComponent"
  key_links: []
---

<objective>
Audit the operator surface and explicitly lock the requirement that all read paths—including the `RowHistoryComponent`—must be scoped rather than disabled, ensuring the support lane remains fully functional for operators.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/MILESTONES.md
@.planning/phases/85-support-lane-surface-audit/85-RECOMMENDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate 85-CONTEXT.md and lock the surface audit</name>
  <files>.planning/phases/85-support-lane-surface-audit/85-CONTEXT.md, .planning/ROADMAP.md</files>
  <read_first>
    <file>.planning/phases/85-support-lane-surface-audit/85-RECOMMENDATION.md</file>
  </read_first>
  <action>Create `85-CONTEXT.md` translating the approved recommendation into explicit locked requirements. Explicitly list the supported surfaces (Timeline, Actor, Transaction, Row History) and declare that `RowHistoryComponent` will be scoped by extending `Threadline.history/3` and `Threadline.as_of/4` with `scope_query_fn`. Update `.planning/ROADMAP.md` and `.planning/MILESTONES.md` if necessary to reflect this clarified claim.</action>
  <acceptance_criteria>
    <criterion>`85-CONTEXT.md` exists and lists `RowHistoryComponent` as a scoped (not disabled) surface.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cat .planning/phases/85-support-lane-surface-audit/85-CONTEXT.md | grep -q "RowHistoryComponent"</automated>
  </verify>
  <done>The surface audit is documented and the row history requirement is locked.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Information disclosure | Row History view | mitigate | Explicitly define the requirement to push `scope_query_fn` into `history/3` and `as_of/4` to prevent cross-tenant data leaks. |
</threat_model>

<verification>
Ensure `85-CONTEXT.md` clearly states that all read paths will be scoped.
</verification>

<success_criteria>
The project team and future phases have a clear, locked requirement that `RowHistoryComponent` must be scoped, preserving the operator DX while maintaining security.
</success_criteria>

<output>
After completion, create `.planning/phases/85-support-lane-surface-audit/85-01-SUMMARY.md`
</output>
