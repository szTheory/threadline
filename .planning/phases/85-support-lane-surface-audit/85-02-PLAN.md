---
phase: 85-support-lane-surface-audit
plan: 02
type: execute
wave: 1
depends_on: [85-01]
files_modified:
  - .planning/phases/85-support-lane-surface-audit/85-CONTEXT.md
autonomous: true
requirements:
  - "Lock the wording for the support-matrix specifically separating UI scope from export scope."
must_haves:
  truths:
    - "Exports are explicitly locked as an admin-tier privilege by default."
    - "The support-matrix wording teaches adopters that `exports: false` is the support default, and explicitly opting in is required for bulk data."
  artifacts:
    - path: .planning/phases/85-support-lane-surface-audit/85-CONTEXT.md
      provides: "The definitive wording and packaging guard for the export support matrix."
      contains: "export_authorize_fn"
  key_links: []
---

<objective>
Document and lock the "packaging and scope guard" requirements, explicitly separating the safe UI-exploration lane (`scope_query_fn`) from the dangerous bulk-export lane (`export_authorize_fn`), ensuring the milestone claim does not accidentally promise universal support-tier exports.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-support-lane-surface-audit/85-RECOMMENDATION.md
@.planning/phases/85-support-lane-surface-audit/85-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lock the export packaging and wording</name>
  <files>.planning/phases/85-support-lane-surface-audit/85-CONTEXT.md</files>
  <read_first>
    <file>.planning/phases/85-support-lane-surface-audit/85-RECOMMENDATION.md</file>
  </read_first>
  <action>Append to `85-CONTEXT.md` the explicit support-matrix wording rules. Define the "scope guard": the documentation and recipes must teach that `exports: false` is the default posture for support-scoped users, and that `export_authorize_fn` is required to override it. This locks the claim that Threadline provides safe UI exploration out of the box, but treats bulk data exfiltration as a separate, opt-in privilege.</action>
  <acceptance_criteria>
    <criterion>`85-CONTEXT.md` contains the wording lock for `exports: false` and `export_authorize_fn`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cat .planning/phases/85-support-lane-surface-audit/85-CONTEXT.md | grep -q "export_authorize_fn"</automated>
  </verify>
  <done>The export support matrix and packaging guard requirements are locked.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02 | Elevation of privilege | Export affordances | mitigate | Explicitly lock the requirement that support-lane docs default to `exports: false` to prevent implicit bulk data exfiltration. |
</threat_model>

<verification>
Ensure `85-CONTEXT.md` explicitly separates the UI scope from the export scope.
</verification>

<success_criteria>
Future phases implementing the support matrix and documentation have clear, locked wording rules that treat exports as a separate tier.
</success_criteria>

<output>
After completion, create `.planning/phases/85-support-lane-surface-audit/85-02-SUMMARY.md`
</output>
