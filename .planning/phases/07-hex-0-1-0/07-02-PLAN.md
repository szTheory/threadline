# Plan 07-02: Tag, push, and publish (HEX-03, HEX-04)

**Phase:** 7 — Hex 0.1.0  
**Plan:** 07-02 of 02  
**Status:** Ready to execute  
**Depends on:** 07-01 complete (release tree on the commit to tag); Phase 8 evidence optional but **recommended**: `main` pushed to `origin` and CI green per project policy  
**Blocks:** _(none — milestone close-out can follow)_  
**Requirements covered:** HEX-03, HEX-04  
**Wave:** 2  
**requirements_addressed:** HEX-03, HEX-04  
**files_modified:** _(none expected — git and Hex registry only)_  
**autonomous:** false  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-07-02-01 | Tag on wrong commit → source links / audit trail wrong | high | Before tagging: `git rev-parse HEAD` equals the commit recorded in `07-01-SUMMARY.md` as release-ready; document both SHAs in `07-02-SUMMARY.md`. |
| T-07-02-02 | Force-push or tag move after fetch | high | Use **only** `git tag -a …`, `git push origin main` (if needed), `git push origin v0.1.0` — **no** `--force` on refs. |
| T-07-02-03 | Publish before tag on GitHub → broken “View source” / changelog links | high | Order: tag locally → push `main` (if ahead) → push `v0.1.0` → then `mix hex.publish` per `07-CONTEXT` D-11, D-15. |
| T-07-02-04 | Hex credentials in shell history / logs | high | Do not echo `HEX_API_KEY` or password; use documented Hex auth only. |

---

## Goal

Satisfy **HEX-03** (annotated tag `v0.1.0` on the release commit, pushed to `origin`) and **HEX-04** (`threadline` **0.1.0** published and visible via `mix hex.info threadline`).

---

## Context

- **Authoritative:** `.planning/phases/07-hex-0-1-0/07-CONTEXT.md` (D-05–D-11, D-15–D-18), `.planning/REQUIREMENTS.md` (HEX-03, HEX-04), `07-01-SUMMARY.md`.

---

## Tasks

### Task 1: Pre-flight — clean tree and release-ready tree

**What to do**

1. `git status --porcelain` must be empty (or only intentional untracked artifacts documented in SUMMARY — **prefer** fully clean before tag).
2. Confirm this commit is release-ready: `mix.exs` already has `@version "0.1.0"` and `CHANGELOG.md` already has the dated `## [0.1.0] - YYYY-MM-DD` section from plan 07-01.

**Read first**

- `mix.exs`
- `CHANGELOG.md`
- `.planning/phases/07-hex-0-1-0/07-CONTEXT.md` (D-07)

**Acceptance criteria**

- `[ -z "$(git status --porcelain)" ]` exits 0.
- `grep -F '@version "0.1.0"' mix.exs` exits 0.
- `grep -E '^## \[0\.1\.0\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md` exits 0.

**Done condition:** Document in `07-02-SUMMARY.md` the output of `git rev-parse HEAD` and one line stating “release-ready tree verified (version + changelog greps)”.

---

### Task 2: HEX-03 — Create annotated tag `v0.1.0`

**What to do**

1. If tag already exists locally, **stop** and document — do not move tag (D-16). Otherwise:
2. `git tag -a v0.1.0 -m "Release v0.1.0"`

**Read first**

- `.planning/phases/07-hex-0-1-0/07-CONTEXT.md` (D-05, D-06)

**Acceptance criteria**

- `git cat-file -t v0.1.0` outputs `tag` (annotated).

**Done condition:** Annotated tag exists locally.

---

### Task 3: HEX-03 — Push branch and tag to `origin`

**What to do**

1. If `main` is ahead of `origin/main`, run `git push origin main`.
2. Always run `git push origin v0.1.0` explicitly (D-08).

**Read first**

- `.planning/phases/07-hex-0-1-0/07-CONTEXT.md` (D-08)
- `mix.exs` (`@source_url` for canonical repo path check)

**Acceptance criteria**

- After push: `git ls-remote --tags origin 'refs/tags/v0.1.0'` shows the tag ref (non-empty output).
- `git remote get-url origin` output contains the GitHub path consistent with `@source_url` in `mix.exs` (e.g. `szTheory/threadline`).

**Done condition:** Tag visible on `origin`.

---

### Task 4: HEX-04 — Publish to Hex

**What to do**

1. Re-run `mix hex.publish --dry-run` once on this commit if any doubt; then run **`mix hex.publish`** for real (accept prompts per local Hex; do not commit secrets).
2. If your Hex CLI publishes docs separately, complete both so HexDocs shows 0.1.0.

**Read first**

- `.planning/phases/07-hex-0-1-0/07-CONTEXT.md` (D-10, D-11, D-17)

**Acceptance criteria**

- Real publish command(s) exit 0; SUMMARY records “published” with timestamp (no secrets).

**Done condition:** Hex accepted the package.

---

### Task 5: HEX-04 — Registry proof

**What to do**

1. Run `mix hex.info threadline` and capture output showing version **0.1.0** in the listed releases.

**Read first**

- _(none beyond prior context)_

**Acceptance criteria**

- `mix hex.info threadline | grep -F '0.1.0'` exits 0.

**Done condition:** Grep succeeds.

---

## Verification

1. `git ls-remote origin refs/tags/v0.1.0` — non-empty.
2. `mix hex.info threadline | grep -F '0.1.0'` — exit 0.
3. `07-02-SUMMARY.md` contains: HEAD SHA, tag type confirmation, push and publish notes, **no** credential material.

---

## Success criteria

- HEX-03: annotated `v0.1.0` on release commit, pushed to `origin`.
- HEX-04: `threadline` 0.1.0 installable from Hex; `mix hex.info` proves it.
