---
phase: 161-logo-tournament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/161-logo-tournament/candidates/round-1/c1-<slug>.svg through c8-<slug>.svg (8 primary lockups; slugs chosen at design time)
  - .planning/phases/161-logo-tournament/candidates/round-1/cN-<slug>-mono.svg (8 monochrome renditions)
  - .planning/phases/161-logo-tournament/candidates/round-1/cN-<slug>-favicon.svg (8 16px-designed favicon forms)
  - .planning/phases/161-logo-tournament/candidates/round-1/gallery.html
  - .planning/phases/161-logo-tournament/tools/hc-gate.mjs
  - .planning/phases/161-logo-tournament/ROUNDS.md
autonomous: true
requirements: [TOUR-01, TOUR-02, LOGO-01, LOGO-02, LOGO-03, LOGO-04, LOGO-05]

must_haves:
  truths:
    - "Exactly 8 round-1 candidates exist: 3 integrated typemarks, 3 unified mark+type lockups, 1 monogram/mark-led, 1 wordmark-only — each declaring a distinct (technique, letterform-hook) pair per DESIGN-BRIEF §5"
    - "Every candidate ships three forms: primary lockup (no subtitle, no chip), monochrome single-flat-color rendition, and a dedicated 16px-designed favicon form"
    - "Every candidate SVG is pure paths with zero <text> elements, built from the Phase 160 glyph kits or the same pipeline run against an OFL alternate font"
    - "Every candidate passes the mechanical HC-1..6 gate before user review — failures fixed or replaced, never shown"
    - "User can open candidates/round-1/gallery.html over file:// with zero network requests and see all 8 candidates in six contexts each, neutrally ordered, with no recommendation styling"
    - "ROUNDS.md exists with the round-1 roster: per candidate ID, lane, declared (technique, hook) strategy, typeface, 2-3 sentence design rationale, HC gate results — feedback section empty for the checkpoint"
  artifacts:
    - path: ".planning/phases/161-logo-tournament/candidates/round-1/gallery.html"
      provides: "Self-contained six-context review gallery for all 8 candidates"
      min_lines: 200
    - path: ".planning/phases/161-logo-tournament/tools/hc-gate.mjs"
      provides: "Mechanical HC-1..6 gate script runnable against any round directory"
      min_lines: 60
    - path: ".planning/phases/161-logo-tournament/ROUNDS.md"
      provides: "Round-1 roster with declared strategies and rationale; empty feedback slots"
      contains: "ADVANCE/KILL/MUTATE"
  key_links:
    - from: ".planning/phases/161-logo-tournament/candidates/round-1/*.svg"
      to: ".planning/phases/160-glyph-outline-pipeline/glyph-kit-geist-*.svg"
      via: "letterform path data sourced from the kits (or pipeline rerun with --font)"
      pattern: "<path"
    - from: ".planning/phases/161-logo-tournament/candidates/round-1/gallery.html"
      to: "candidates/round-1/cN-<slug>.svg"
      via: "inlined pure-path SVG per candidate per context"
      pattern: "<svg"
---

<objective>
Design and ship the 8 round-1 logo candidates for the Threadline tournament, gate every one of them through the mechanical HC-1..6 checks from `159-DESIGN-BRIEF.md`, and package them in a self-contained file:// gallery plus a seeded ROUNDS.md roster — everything the orchestrator needs to run the round-1 human checkpoint in plan 161-02.

Purpose: This is the user-facing deliverable of the whole v1.35 milestone. Candidate craft quality matters enormously — these 8 marks are what the user judges, and a weak round 1 poisons every subsequent round. Each candidate must be a genuine, defensible design with a recorded rationale, not a quota-filler.

Output: 24 candidate SVGs (8 × primary/mono/favicon), `candidates/round-1/gallery.html`, `tools/hc-gate.mjs`, and `ROUNDS.md` seeded with the roster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-logo-tournament/161-CONTEXT.md
@.planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md
@.planning/phases/160-glyph-outline-pipeline/160-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md

The DESIGN-BRIEF is the binding generation contract — MR-1..5 motif rules, HC-1..6 hard constraints, §4 degrees of freedom, §5 lane quota and distinct-named-motif rule, and the "Definition of done for round 1". Cite it; do not restate-and-drift.

Key facts (verified at planning time):

- **Glyph kits:** `.planning/phases/160-glyph-outline-pipeline/glyph-kit-geist-{500,600}.{svg,json}`. One `<path>` per glyph, glyph-local `d` + `translate(x 0)` transform exposing the kerned x-offset. JSON exposes per-glyph name, advance, `x_offset_kerned`, baseline, units-per-em (1000), and measured stem widths — Geist 500: l=106 / T=108; Geist 600: l=128 / T=130 font units — for matching motif stroke weight to letterform stems.
- **OFL alternates:** `brandbook/tools/text-to-paths.mjs --font <path> --out <dir>` reruns the pipeline against any woff2/ttf. fontkit is installed ephemerally: `FONTKIT_DIR=$(mktemp -d); npm install --prefix "$FONTKIT_DIR" fontkit@2.0.4; NODE_PATH="$FONTKIT_DIR/node_modules" node ...` (pattern documented in `brandbook/tools/README.md`). Alternate font binaries are downloaded to a temp dir from the official upstream repo (e.g., google/fonts or the vendor's GitHub releases), used, and NEVER committed — record the source URL + version in ROUNDS.md for reproducibility.
- **Raster self-checks:** Playwright is reusable from `examples/threadline_phoenix/e2e/node_modules` via `module.createRequire` anchored at the e2e dir (Phase 160 pattern, see `tools/capture-overlay.mjs` there). PNG checks go to /tmp and are never committed — SVG-only in git.
- **xmllint** available at `/usr/bin/xmllint`.
- Brand dark is `#0B1020`; light surface is `#FFFFFF`/Paper.

Locked decisions from 161-CONTEXT.md (NON-NEGOTIABLE):
- Exactly 8 candidates: 3 integrated typemarks, 3 unified mark+type lockups, 1 monogram/mark-led, 1 wordmark-only. All 8 declare distinct (technique, hook) pairs; no technique name repeats within a lane; at most two technique names appear twice across lanes and only with different hooks (BRIEF §5).
- Zero `<text>` anywhere (HC-6). Letterforms from the Phase 160 kits or the pipeline run against an OFL candidate font from BRIEF §4's verified list.
- Display-size optical kerning pass is owned HERE: after any letterform surgery, adjust per-glyph translate offsets and overlay against the kit baseline for evidence.
- Per-candidate deliverables: primary lockup (pure paths, no subtitle, no chip), monochrome rendition, 16px-designed favicon form (the silhouette-first survivor — may be the mark/monogram element alone).
- Candidate SVG sources committed beside the gallery as `cN-<slug>.svg` (+ `-mono`, `-favicon`).
- Gallery: self-contained, file://, zero network. Chrome MAY use system fonts/inline CSS; candidate renders are inlined pure-path SVG only. Neutral order, labels C1..C8 with lane + declared strategy, NO recommendation styling, no pre-ranking.

Claude's discretion (per 161-CONTEXT.md): which (technique, hook) pairs go to which lanes; palette/typeface per candidate within BRIEF §4 (a mix of Geist-based and 1-3 OFL-alternate candidates is healthy; do not make all 8 non-Geist); exact gallery layout (calm, dark-first, professional — the gallery is itself a brand artifact); uncommitted PNG self-checks.

User's strongest stated wants (judging signal — design toward these): fully integrated custom type treatment ("motif worked in, not icon left of text"); marks that "break the boundaries" (no containment); logotype optically close to the mark; no subtitle in primary. The anti-trait list is a judging criterion: not flashy, not cute, not cyberpunk, not militarized, not compliance-bureaucratic, not generic SaaS. Named letterform hooks (MR-3): Th ligature pair, double-l verticals, i dot, e/a/d counters, the descender-free lowercase run of "threadline" (a horizontal thread can pass through the whole word at one consistent height).

HARD RULES (this plan and every round):
- Nothing is written under `brandbook/` or `lib/` — all artifacts live in `.planning/phases/161-logo-tournament/`. Losing candidates never leave the phase dir.
- The working tree has pre-existing uncommitted changes under `lib/`, `examples/`, and `test/` belonging to other lanes — never touch or stage them. Stage only phase-dir paths, explicitly by path.
- Every verify gate is mechanical (command + expected output), no judgment-call gates.
</context>

<interfaces>
From `.planning/phases/160-glyph-outline-pipeline/glyph-kit-geist-500.json` (same shape for 600):

- Top level: `font`, `weight`, `units_per_em` (1000), `ascent`, `descent`, `text` ("Threadline"), `stem_widths` ({ l, T } in font units), `glyphs` (array).
- Per glyph: `name`, `char`, `advance`, `x_offset_kerned` (cumulative kerned pen position in font units), path `d` data is in the sibling SVG, one `<path>` per glyph with `transform="translate(x 0)"` matching `x_offset_kerned`.

CLI contract of `brandbook/tools/text-to-paths.mjs`: `--font <woff2/ttf path> --out <dir> [--text Threadline]` → emits `<kit>.svg` + `<kit>.json` in the same schema. Coordinates 2-decimal, y-flipped, deterministic.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Design and author the 8 round-1 candidate SVGs (primary + mono + favicon each)</name>
  <files>.planning/phases/161-logo-tournament/candidates/round-1/c1-<slug>.svg … c8-<slug>.svg, plus cN-<slug>-mono.svg and cN-<slug>-favicon.svg for each</files>
  <action>
  First, on paper (in working notes, not committed prose): assign 8 distinct (technique, hook) strategies across the four lanes per BRIEF §5 — 3 integrated typemarks, 3 unified mark+type lockups, 1 monogram/mark-led, 1 wordmark-only. Techniques verbatim from BRIEF §2: Negative space, Pattern-through-letterforms, Continuous-line mark, Ligature wordmark, Stroke continuation, Counter replacement. Hooks from MR-3. Verify distinctness before drawing: no two candidates share a pair, no technique repeats within a lane, at most two techniques appear twice across lanes (different hooks). Continuous-line is the strongest conceptual fit (a thread IS a continuous line) — use it on at least one strong candidate.

  Then author each candidate as pure-path SVG. Letterform sourcing: copy glyph `d` data + translate offsets from the Geist 500/600 kits for Geist-based candidates; for 1-3 OFL-alternate candidates (pick from BRIEF §4's named list — Space Grotesk and Sora are the strongest motif-friendly options for the double-l hooks), download the font to a temp dir from its official upstream repo, run `brandbook/tools/text-to-paths.mjs --font` with the ephemeral-fontkit pattern, and build from that kit. Never commit font binaries or node_modules; record font source URL + version per candidate for ROUNDS.md.

  Design constraints, every candidate (MR + HC, cite BRIEF): motif encoded structurally, not decoratively — the MR-2 removability test must fail to strip it; no gradient-dependence, no gimmick-dependence, no container chips (MR-4); the device assists reading (MR-5); no subtitle glyphs in the primary (HC-1); no rect/rounded-rect background plates (HC-2); mark and type one drawing — shared geometry or designed optical spacing, never icon-beside-plain-set-type (HC-3). Match motif stroke weight to the kit's measured stem widths (l/T values in the JSON) so thread lines read as kin to the letterforms. Letterform surgery (slicing, fusing, extending) is expected — the OFL permits it.

  Per candidate produce three files: (1) primary lockup `cN-<slug>.svg`; (2) monochrome `cN-<slug>-mono.svg` — every fill/stroke one flat color, no opacity tiers, no gradients, structure identical to the primary (HC-5); (3) favicon `cN-<slug>-favicon.svg` — DESIGNED at 16px, never the wordmark scaled: strokes >= 1.5px at 16px canvas (absolute floor 1.0px), gaps/counters >= 1.0px, <= 4 distinct strokes/elements, silhouette-first (HC-4). For the monogram lane the favicon IS the monogram. Tag every letterform path with a `data-glyph` attribute naming its letter(s) (e.g., data-glyph="T", data-glyph="Th" for a fused ligature) so the gate can verify glyph inventory mechanically; pure mark geometry gets `data-role="mark"`.

  Optical kerning pass (locked decision): after any motif surgery, re-check per-glyph translate offsets at display size; where surgery changed apparent sidebearings, adjust offsets and verify by overlaying the adjusted lockup against the unmodified kit baseline (Playwright screenshot to /tmp at 2x, Phase 160 capture pattern). Record adjusted-vs-kit offsets per modified glyph in working notes for the ROUNDS.md roster.

  Craft bar: each candidate gets a written 2-3 sentence design rationale (what the motif is, why this hook, how it embodies "follow what happened") — drafted now, recorded in ROUNDS.md in Task 4. If a candidate's rationale reads as "fills the quota", redesign it. Anti-trait list is a kill criterion at design time, not just at judging.
  </action>
  <verify>
    <automated>ls .planning/phases/161-logo-tournament/candidates/round-1/c*.svg | wc -l → 24 (8 primary + 8 mono + 8 favicon); for f in candidates/round-1/c*.svg; do xmllint --noout "$f"; done → exit 0</automated>
  </verify>
  <done>24 SVG files exist, well-formed, 8 distinct (technique, hook) strategies spanning the exact lane quota, every letterform path carrying data-glyph, every favicon designed at 16px, optical-kerning offsets recorded in working notes.</done>
</task>

<task type="auto">
  <name>Task 2: Build tools/hc-gate.mjs and run the mechanical HC-1..6 gate over every candidate</name>
  <files>.planning/phases/161-logo-tournament/tools/hc-gate.mjs</files>
  <action>
  Write `tools/hc-gate.mjs` (plain Node, no dependencies) taking a round directory argument and checking every `c*.svg` in it. Checks, each mapped to its HC and printed PASS/FAIL per file with a non-zero exit on any failure:
  - HC-6: zero `<text>` elements (string scan of the parsed source).
  - HC-2: zero `<rect>` elements anywhere; additionally fail any `<path>` whose `data-role` is not "mark"/letterform and whose fill spans the full viewBox (background-plate detection via a bounding-box heuristic on the first painted element is acceptable; at minimum the literal `<rect>` ban must hold).
  - HC-1 + glyph inventory: concatenate all `data-glyph` values in the primary; the resulting letter multiset must equal exactly the letters of "Threadline" — no more (subtitle glyphs), no fewer (missing letters). Wordmark-only and typemark candidates: same rule. Favicon/monogram forms: multiset must be a subset of "Threadline" letters.
  - HC-5 (mono files): zero `gradient`, zero `opacity` attributes/elements, exactly one distinct fill/stroke color value across all painted elements.
  - HC-4 (favicon files): for stroke-based paths, assert stroke-width scaled to a 16px canvas (16 / viewBox height × stroke-width) >= 1.0, warn below 1.5; assert total painted elements <= 4; emit the scaled numbers so they can be transcribed into the roster.
  - Hygiene: zero `<script>`, `<image>`, `<foreignObject>`, and zero `http://`/`https://` references in any candidate SVG.
  Run the gate over `candidates/round-1/`. Separately run the two render checks (uncommitted, /tmp only, Playwright via the e2e createRequire pattern): (a) silhouette test — each favicon with every path forced to one solid fill, rendered at literal 16px plus a 4x-scaled copy; (b) flatten check — each mono beside its primary at 64px. Inspect the renders; any candidate failing silhouette recognition or flatten-structure-identity is FIXED OR REPLACED now (maintaining lane quota and strategy distinctness), then the gate is re-run. Nothing reaches the gallery with a failing HC. Record per-candidate gate output (including the HC-4 measured numbers and silhouette PASS) in working notes for Task 4.
  </action>
  <verify>
    <automated>node .planning/phases/161-logo-tournament/tools/hc-gate.mjs .planning/phases/161-logo-tournament/candidates/round-1 → exit 0, 24 files reported, zero FAIL lines</automated>
  </verify>
  <done>hc-gate.mjs committed and green over all 24 round-1 SVGs; silhouette + flatten render checks performed (artifacts in /tmp only); any failing candidate fixed or replaced before review.</done>
</task>

<task type="auto">
  <name>Task 3: Build candidates/round-1/gallery.html (six contexts, neutral, self-contained)</name>
  <files>.planning/phases/161-logo-tournament/candidates/round-1/gallery.html</files>
  <action>
  Single self-contained HTML file opening over file:// with ZERO network requests: all CSS inline, gallery chrome on system fonts only (font-family: system-ui stack), every candidate render an INLINED copy of the committed pure-path SVG (no img src, no fetch, no external href of any kind).

  Per candidate, one section labeled `C1`..`C8` (order: neutral — do not order by preference; use the order strategies were assigned, c1..c8) showing the lane name and the declared (technique, hook) strategy verbatim, followed by six context panels per TOUR-02:
  1. Dark — primary on #0B1020.
  2. Light — primary (or its light-adaptive form) on #FFFFFF.
  3. Monochrome — the mono rendition, one flat color on a neutral ground.
  4. Literal 16px — the favicon form rendered at exactly 16x16 CSS px, beside a 4x magnified copy (64px) of the same artifact.
  5. 32px — the favicon (or mark) at 32x32.
  6. Simulated GitHub README header strip — a light-chrome strip mimicking a README top (repo-title typography around it acceptable as plain system-font text), with the candidate placed as the header logo; a dark-mode sub-variant of the strip may share the panel.

  NO recommendation styling: identical card chrome for all 8, no stars, no ordering cues, no "Claude's pick", no differential sizing. Keep the gallery itself calm, dark-first, professional — it is a brand artifact the user experiences. Include a small header noting the round number, date, and the verdict vocabulary the user will use (ADVANCE / KILL / MUTATE + reasons), so the checkpoint conversation has shared language.
  </action>
  <verify>
    <automated>xmllint --html --noout .planning/phases/161-logo-tournament/candidates/round-1/gallery.html 2>/dev/null || node -e "require('fs').readFileSync('.planning/phases/161-logo-tournament/candidates/round-1/gallery.html','utf8')"; grep -cE 'https?://' .planning/phases/161-logo-tournament/candidates/round-1/gallery.html → 0; grep -c '<text' .planning/phases/161-logo-tournament/candidates/round-1/gallery.html → 0; for n in 1 2 3 4 5 6 7 8; do grep -c "C$n" .planning/phases/161-logo-tournament/candidates/round-1/gallery.html; done → each >= 1; grep -c '<svg' gallery.html → >= 48 (8 candidates x 6 contexts)</automated>
  </verify>
  <done>gallery.html opens from disk with zero network requests, shows all 8 candidates in all six contexts with lane + strategy labels, neutral presentation, no recommendation styling.</done>
</task>

<task type="auto">
  <name>Task 4: Seed ROUNDS.md with the round-1 roster and commit the round-1 package</name>
  <files>.planning/phases/161-logo-tournament/ROUNDS.md</files>
  <action>
  Create `ROUNDS.md` at the phase root. Structure: a header explaining the tournament protocol in brief (verdict vocabulary ADVANCE/KILL/MUTATE, round cap 4 with explicit decision, winner = explicit user statement — cite 161-CONTEXT.md and BRIEF §5), then a `## Round 1` section containing:
  - **Roster table**, one row per candidate: ID (C1..C8), file slug, lane, declared (technique, hook) strategy verbatim, typeface + weight (and OFL source URL + version for non-Geist), and HC gate result (PASS, with the measured HC-4 favicon numbers and silhouette PASS from Task 2).
  - **Design rationale**, per candidate: the 2-3 sentence rationale drafted in Task 1 — what the motif is, why this hook, how it embodies the thread/"follow what happened" metaphor. This is part of the user-facing deliverable; write it at presentation quality.
  - **Optical kerning notes**: per candidate with letterform surgery, which glyph offsets were adjusted from the kit values and why.
  - **Feedback** subsection: an empty per-candidate verdict skeleton (C1: verdict —, reasons —, …) explicitly marked "to be filled verbatim at the round-1 checkpoint (plan 161-02); Claude does not pre-fill verdicts."
  Then commit the complete round-1 package. Stage ONLY phase-dir paths explicitly: `git add .planning/phases/161-logo-tournament/` — verify with `git status --porcelain` that no `lib/`, `examples/`, `test/`, or `brandbook/` path is staged (pre-existing uncommitted changes there belong to other lanes). Commit message: `feat(161): round-1 logo candidates, HC gate, gallery, ROUNDS roster`.
  </action>
  <verify>
    <automated>grep -c 'ADVANCE/KILL/MUTATE' .planning/phases/161-logo-tournament/ROUNDS.md → >= 1; for n in 1 2 3 4 5 6 7 8; do grep -c "C$n" .planning/phases/161-logo-tournament/ROUNDS.md; done → each >= 2 (roster row + feedback skeleton); git status --porcelain | grep -cE '^(A|M).*(brandbook/|lib/|examples/|test/)' → 0; git log -1 --stat -- .planning/phases/161-logo-tournament/ shows the round-1 files</automated>
  </verify>
  <done>ROUNDS.md seeded with roster, rationales, kerning notes, and empty feedback skeleton; round-1 package committed touching only the phase dir; working-tree changes outside the phase dir untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local toolchain | Ephemeral `fontkit` install (OFL-alternate kit generation) executes third-party code at generation time |
| Upstream font repos → local temp dir | OFL alternate woff2/ttf downloaded at generation time, never committed |
| Candidate SVGs / gallery.html → local browser + future GitHub render | Output is destined for user review over file:// and eventually public surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-SC | Tampering (supply chain) | `npm install fontkit` | mitigate | Reuse the Phase 160 verified pin `fontkit@2.0.4` (npmjs.com/package/fontkit — foliojs, MIT, verified in Phase 160 planning); ephemeral mktemp install, nothing from node_modules committed |
| T-161-01 | Tampering | OFL font downloads | mitigate | Download only from official upstream (google/fonts repo or vendor GitHub releases); record source URL + version in ROUNDS.md; binaries never committed |
| T-161-02 | Tampering/Injection | candidate SVGs + gallery.html | mitigate | hc-gate.mjs enforces zero `<script>`/`<image>`/`<foreignObject>`/`<text>` and zero http(s) references; gallery network-free by grep gate; all candidate renders inlined static paths |
</threat_model>

<verification>
- `node tools/hc-gate.mjs candidates/round-1` exits 0 across all 24 SVGs (HC-1..6 mechanical gate).
- `grep -rcE 'https?://' candidates/round-1/gallery.html` returns 0; gallery opens from disk.
- Lane quota + strategy distinctness verifiable from the ROUNDS.md roster table (8 rows, 8 distinct pairs, lanes 3/3/1/1).
- `git status --porcelain` shows no staged or new paths under brandbook/, lib/, examples/, or test/.
</verification>

<success_criteria>
- TOUR-01: exactly 8 candidates meeting the archetype quota, each with a distinct named motif strategy recorded in the gallery and ROUNDS.md.
- TOUR-02 (round-1 portion): self-contained file:// gallery with six contexts per candidate, zero network.
- LOGO-01..05: enforced mechanically by hc-gate.mjs on every committed candidate (no icon-beside-type, no chips, no subtitle, mono rendition shipped, zero `<text>`).
- Phase dir contains everything plan 161-02 needs to open the round-1 checkpoint; nothing written outside the phase dir.
</success_criteria>

<output>
Create `.planning/phases/161-logo-tournament/161-01-SUMMARY.md` when done.
</output>
