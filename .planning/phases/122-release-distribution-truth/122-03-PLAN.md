---
phase: 122-release-distribution-truth
plan: 03
type: execute
wave: 3
depends_on:
  - 122-01-PLAN.md
  - 122-02-PLAN.md
files_modified:
  - guides/adoption-pilot-backlog.md
  - guides/evaluating-threadline.md
  - .planning/phases/122-release-distribution-truth/122-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
must_haves:
  truths:
    - "`122-VERIFICATION.md` records tag `v0.6.0`, successful hex-publish workflow URL, and redacted hex.info excerpt with **0.6.0** (DIST-01 maintainer record)."
    - "Adoption-pilot Distribution preflight Hex row is **OK** with `Verified YYYY-MM-DD:`, hex.pm latest **0.6.0**, workflow URL, CONTRIBUTING pointer — no stale 0.5.0-as-latest lag prose."
    - "Evaluating guide removes `may still list **0.5.0** as latest` caveat; points evaluators at Hex 0.6.0 + adoption-pilot preflight."
    - "Conditional doc contracts from Wave 1 now enforce anti-stale refutes with OK row present."
    - "`.planning/REQUIREMENTS.md` DIST-01–03 checked; STATE and ROADMAP reflect Phase 122 complete."
  artifacts:
    - path: .planning/phases/122-release-distribution-truth/122-VERIFICATION.md
      provides: "DIST-01 GSD audit trail"
    - path: guides/adoption-pilot-backlog.md
      provides: "DIST-02 adopter-facing distribution SSOT"
    - path: guides/evaluating-threadline.md
      provides: "DIST-02 evaluator packaging anchor post-publish"
---

<objective>
Sync adopter and maintainer surfaces with hex.pm 0.6.0 reality: flip adoption-pilot Hex row to OK, trim evaluating guide lag caveat, write verification record, and close planning requirements.

Purpose: DIST-01 record + DIST-02 distribution truth; phase complete only after registry attestation lands in docs (D-12).
Output: Post-publish PR with honest OK row, `122-VERIFICATION.md`, planning closeout, green `mix verify.doc_contract` + `mix ci.all`.
</objective>

<context>
@.planning/phases/122-release-distribution-truth/122-CONTEXT.md (D-01, D-05, D-06, D-08, D-12, D-14)
@.planning/phases/122-release-distribution-truth/122-RESEARCH.md (§3 DIST-01 Wave 3, §3 DIST-02)
@.planning/phases/122-release-distribution-truth/122-PATTERNS.md (§3.6–3.9, §4.5)
@122-02-PLAN.md manual_outputs (workflow URL, hex.info excerpt, verified date — **required inputs**)
@guides/adoption-pilot-backlog.md (line 13 — Hex row to flip Pending → OK)
@guides/evaluating-threadline.md (line 11 — lag caveat removal)
@test/threadline/adoption_pilot_doc_contract_test.exs (conditional anti-stale — now must pass with OK row)
@test/threadline/evaluating_threadline_doc_contract_test.exs (SSOT-chain conditional — now must pass)
@.planning/milestones/v1.25-phases/114-release-0-6-0-packaging/114-VERIFICATION.md (format analog)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-122-09 | OK row merged before hex.pm shows 0.6.0 | **high** | Maintainer runs `mix hex.info threadline` immediately before Wave 3 PR; 122-02 must be complete |
| T-122-10 | Full mix hex.info pasted in backlog (noisy churn) | low | D-05: pointer to CONTRIBUTING `#hex-publish-maintainers` only |
| T-122-11 | Evaluating guide loses 0.5.0 upgrade context | medium | Preserve existing semver/historical 0.5.0 mentions; remove only "may still list **0.5.0** as latest" caveat |
| T-122-12 | Phase marked complete at Wave 1 merge | **high** | D-12/D-14: tick ROADMAP/REQUIREMENTS only in this plan after Waves 2+3 |

**Block on high severity:** Confirm `mix hex.info threadline` shows 0.6.0 before editing Hex row to OK (T-122-09).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create 122-VERIFICATION.md (DIST-01 record)</name>
  <files>.planning/phases/122-release-distribution-truth/122-VERIFICATION.md</files>
  <read_first>
    - 122-02-PLAN.md manual_outputs (workflow URL, hex.info excerpt, verified date)
    - .planning/milestones/v1.25-phases/114-release-0-6-0-packaging/114-VERIFICATION.md (format analog)
    - .planning/phases/122-release-distribution-truth/122-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
Create `.planning/phases/122-release-distribution-truth/122-VERIFICATION.md` with frontmatter and sections:

```markdown
---
status: passed
phase: 122-release-distribution-truth
verified: YYYY-MM-DD
requirements: DIST-01, DIST-02, DIST-03
---

# Phase 122 Verification Report

## Registry publish (DIST-01)

- Tag: `v0.6.0`
- Workflow: <paste successful hex-publish.yml run URL from 122-02>
- Registry excerpt (`mix hex.info threadline`, redacted):
  ```
  <paste line(s) containing 0.6.0 from Recent releases>
  ```

## Adopter surfaces (DIST-02)

- adoption-pilot Hex row: OK with dated evidence (see Task 2)
- evaluating-threadline: no `may still list **0.5.0** as latest` caveat

## CHANGELOG (DIST-03)

- `[0.6.0]` four-lane upgrade bullet lists `phx-gen-auth-reference`
- `release_distribution_doc_contract_test.exs` green via `mix verify.doc_contract`
```

Replace `YYYY-MM-DD` with the maintainer verification date from 122-02.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/122-release-distribution-truth/122-VERIFICATION.md` exits 0
    - `grep 'v0.6.0' .planning/phases/122-release-distribution-truth/122-VERIFICATION.md` matches at least twice (tag + registry)
    - `grep 'hex-publish' .planning/phases/122-release-distribution-truth/122-VERIFICATION.md` matches (workflow reference)
    - `grep '0.6.0' .planning/phases/122-release-distribution-truth/122-VERIFICATION.md` matches in registry excerpt section
  </acceptance_criteria>
  <verify>grep -q 'v0.6.0' .planning/phases/122-release-distribution-truth/122-VERIFICATION.md && grep -q 'hex-publish' .planning/phases/122-release-distribution-truth/122-VERIFICATION.md</verify>
</task>

<task type="auto">
  <name>Task 2: Flip adoption-pilot Hex row Pending → OK (DIST-02)</name>
  <files>guides/adoption-pilot-backlog.md</files>
  <read_first>
    - guides/adoption-pilot-backlog.md (line 13 — current Pending row)
    - 122-02-PLAN.md manual_outputs (workflow URL, verified date)
    - CONTRIBUTING.md (#hex-publish-maintainers — pointer target)
    - .planning/phases/122-release-distribution-truth/122-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
In `guides/adoption-pilot-backlog.md` Distribution preflight table, replace the Hex row (line 13):

**Status:** change `Pending` → `OK`

**Evidence cell** — use this shape (substitute real date and workflow URL from 122-02):

```
Verified YYYY-MM-DD: [hex.pm](https://hex.pm/packages/threadline) latest is **0.6.0** (tag **`v0.6.0`**). Publish workflow: <GitHub Actions hex-publish run URL>. Confirm locally: `mix hex.info threadline` ([CONTRIBUTING.md](../CONTRIBUTING.md#hex-publish-maintainers)). Doc contract: `test/threadline/adoption_pilot_doc_contract_test.exs`
```

Requirements:
- Include `Verified YYYY-MM-DD:` prefix (ISO date)
- State hex.pm latest is **0.6.0**
- Link to **successful** `hex-publish.yml` run for tag **`v0.6.0`**
- Pointer to `CONTRIBUTING.md#hex-publish-maintainers` for `mix hex.info threadline`
- Do **not** paste full `mix hex.info` output
- Remove prose: `latest is **0.5.0**` and `Unblock: push tag`
  </action>
  <acceptance_criteria>
    - `grep '| OK |' guides/adoption-pilot-backlog.md` matches on the Hex distribution row
    - `grep 'latest is **0.6.0**' guides/adoption-pilot-backlog.md` matches
    - `grep 'latest is **0.5.0**' guides/adoption-pilot-backlog.md` returns no matches
    - `grep 'Unblock: push tag' guides/adoption-pilot-backlog.md` returns no matches
    - `grep 'Verified 20' guides/adoption-pilot-backlog.md` matches (dated evidence)
  </acceptance_criteria>
  <verify>grep 'latest is \*\*0.6.0\*\*' guides/adoption-pilot-backlog.md && ! grep 'latest is \*\*0.5.0\*\*' guides/adoption-pilot-backlog.md</verify>
</task>

<task type="auto">
  <name>Task 3: Remove evaluating guide hex lag caveat (DIST-02)</name>
  <files>guides/evaluating-threadline.md</files>
  <read_first>
    - guides/evaluating-threadline.md (line 11 — current lag paragraph)
    - guides/adoption-pilot-backlog.md (updated OK row — cross-link target)
    - test/threadline/evaluating_threadline_doc_contract_test.exs (preserve 0.5.0 upgrade context assert)
  </read_first>
  <action>
In `guides/evaluating-threadline.md` § **What Threadline 0.6.0 packages**, replace paragraph on line 11:

**From:**
```
Threadline **0.6.0** is the in-repo and doc SSOT (`mix.exs` `@version`). As of 2026-05-27, [hex.pm](https://hex.pm/packages/threadline) may still list **0.5.0** as latest until maintainers push tag **`v0.6.0`** (see [`guides/adoption-pilot-backlog.md`](adoption-pilot-backlog.md) Distribution preflight).
```

**To:**
```
Threadline **0.6.0** is the in-repo, doc, and [Hex](https://hex.pm/packages/threadline) SSOT (`mix.exs` `@version`). See [`guides/adoption-pilot-backlog.md`](adoption-pilot-backlog.md) Distribution preflight for maintainer attestation and publish workflow evidence.
```

Keep line 13 and all other **0.5.0** mentions (upgrade/historical context) unchanged.
  </action>
  <acceptance_criteria>
    - `grep 'may still list **0.5.0** as latest' guides/evaluating-threadline.md` returns no matches
    - `grep 'hex.pm/packages/threadline' guides/evaluating-threadline.md` matches
    - `grep 'adoption-pilot-backlog.md' guides/evaluating-threadline.md` matches
    - `grep '0.5.0' guides/evaluating-threadline.md` still matches (upgrade context preserved)
  </acceptance_criteria>
  <verify>! grep -F 'may still list **0.5.0** as latest' guides/evaluating-threadline.md && grep '0.5.0' guides/evaluating-threadline.md</verify>
</task>

<task type="auto">
  <name>Task 4: Optional CONTRIBUTING DIST-01 proof cross-link</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (Hex publish section — after step 5 confirm with mix hex.info)
    - .planning/phases/122-release-distribution-truth/122-CONTEXT.md (D-02, optional one-liner)
  </read_first>
  <action>
In **CONTRIBUTING.md** Hex publish (maintainers) section, after the step confirming with `mix hex.info threadline`, add one line:

```
Post-publish distribution proof for adopters: adoption-pilot Distribution preflight OK row in `guides/adoption-pilot-backlog.md` plus `.planning/phases/122-release-distribution-truth/122-VERIFICATION.md`.
```

Skip only if an equivalent sentence already exists.
  </action>
  <acceptance_criteria>
    - `grep '122-VERIFICATION.md' CONTRIBUTING.md` matches OR `grep 'adoption-pilot-backlog.md' CONTRIBUTING.md` matches in Hex publish section with "proof" or "DIST-01" context
  </acceptance_criteria>
  <verify>grep -E '122-VERIFICATION|adoption-pilot-backlog' CONTRIBUTING.md</verify>
</task>

<task type="auto">
  <name>Task 5: Planning closeout — REQUIREMENTS, STATE, ROADMAP</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DIST-01–03 unchecked lines)
    - .planning/STATE.md (Hex distribution lag line, phase progress)
    - .planning/ROADMAP.md (Phase 122 checkbox and progress table)
  </read_first>
  <action>
1. In `.planning/REQUIREMENTS.md`, change DIST-01, DIST-02, DIST-03 checkboxes from `[ ]` to `[x]`.

2. In `.planning/STATE.md`:
   - Update Performance Metrics Hex line to: in-repo and hex.pm latest **0.6.0**
   - Set phase/plan progress for Phase 122 complete (3/3 plans or equivalent)
   - Update `last_activity` to Phase 122 complete date

3. In `.planning/ROADMAP.md`:
   - Mark Phase 122 checkbox `[x]` in Phases list
   - Update progress table: Phase 122 status **Complete**, plans 3/3

Do not tick Phase 123/124 requirements.
  </action>
  <acceptance_criteria>
    - `grep '\[x\] \*\*DIST-01\*\*' .planning/REQUIREMENTS.md` matches
    - `grep '\[x\] \*\*DIST-02\*\*' .planning/REQUIREMENTS.md` matches
    - `grep '\[x\] \*\*DIST-03\*\*' .planning/REQUIREMENTS.md` matches
    - `grep '0.6.0' .planning/STATE.md` matches in Hex distribution context
    - `grep '\[x\].*Phase 122' .planning/ROADMAP.md` matches
  </acceptance_criteria>
  <verify>grep '\[x\] \*\*DIST-0[123]\*\*' .planning/REQUIREMENTS.md | wc -l | grep -q '^3$'</verify>
</task>

<task type="auto">
  <name>Task 6: Wave 3 verification gate</name>
  <files></files>
  <read_first>
    - test/threadline/adoption_pilot_doc_contract_test.exs (conditional anti-stale — must pass with OK row)
    - test/threadline/evaluating_threadline_doc_contract_test.exs (conditional anti-stale — must pass)
    - mix.exs (`verify.doc_contract`, `ci.all`)
  </read_first>
  <action>
Run verification with OK row present (conditional tests now enforce anti-stale):

```bash
mix test test/threadline/adoption_pilot_doc_contract_test.exs
mix test test/threadline/evaluating_threadline_doc_contract_test.exs
mix test test/threadline/release_distribution_doc_contract_test.exs
mix verify.doc_contract
mix ci.all
```

Manual sanity:
- `mix hex.info threadline` still shows 0.6.0 (registry unchanged since 122-02)
  </action>
  <acceptance_criteria>
    - `mix test test/threadline/adoption_pilot_doc_contract_test.exs` exits 0
    - `mix test test/threadline/evaluating_threadline_doc_contract_test.exs` exits 0
    - `mix verify.doc_contract` exits 0
    - `mix ci.all` exits 0
  </acceptance_criteria>
  <verify>mix verify.doc_contract && mix ci.all</verify>
</task>

</tasks>

<verification>
- [ ] `122-VERIFICATION.md` exists with tag, workflow URL, hex.info excerpt
- [ ] Adoption-pilot Hex row `| OK |` with `Verified YYYY-MM-DD:` and **0.6.0**
- [ ] Evaluating guide has no `may still list **0.5.0** as latest`
- [ ] `mix verify.doc_contract`
- [ ] `mix ci.all`
- [ ] DIST-01–03 checked in REQUIREMENTS.md
</verification>
