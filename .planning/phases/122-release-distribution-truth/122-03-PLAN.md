---
phase: 122-release-distribution-truth
plan: 03
type: execute
wave: 3
depends_on:
  - 122-01-PLAN.md
  - 122-02-PLAN.md
files_modified:
  - guides/adoption-pilot-backlog.md
  - guides/evaluating-threadline.md
  - .planning/phases/122-release-distribution-truth/122-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
must_haves:
  truths:
    - "`122-VERIFICATION.md` records tag `v0.6.0`, successful Release workflow URL, and registry attestation with **0.6.0** (DIST-01 CI record)."
    - "Adoption-pilot Distribution preflight Hex row is **OK** with `Verified YYYY-MM-DD:`, hex.pm latest **0.6.0**, workflow URL, CONTRIBUTING pointer — no stale 0.5.0-as-latest lag prose."
    - "Evaluating guide removes `may still list **0.5.0** as latest` caveat; points evaluators at Hex 0.6.0 + adoption-pilot preflight."
    - "Conditional doc contracts from Wave 1 now enforce anti-stale refutes with OK row present."
    - "`.planning/REQUIREMENTS.md` DIST-01–03 checked; STATE and ROADMAP reflect Phase 122 complete."
  artifacts:
    - path: .planning/phases/122-release-distribution-truth/122-VERIFICATION.md
      provides: "DIST-01 GSD audit trail (CI-generated)"
    - path: guides/adoption-pilot-backlog.md
      provides: "DIST-02 adopter-facing distribution SSOT"
    - path: guides/evaluating-threadline.md
      provides: "DIST-02 evaluator packaging anchor post-publish"
---

<objective>
Sync adopter and maintainer surfaces with hex.pm 0.6.0 reality — **automated by Release workflow `distribution-sync` job** (`bin/post-publish-distribution-sync` opens PR).

Purpose: DIST-01 record + DIST-02 distribution truth; phase complete when registry attestation PR merges (D-12).
Output: Bot PR with OK row, `122-VERIFICATION.md`, planning closeout; green `mix verify.doc_contract` on that PR.
</objective>

<context>
@.planning/phases/122-release-distribution-truth/122-CONTEXT.md (D-01, D-05, D-06, D-08, D-12, D-14)
@.github/workflows/release.yml (distribution-sync job)
@bin/post-publish-distribution-sync (doc sync SSOT)
@guides/adoption-pilot-backlog.md (line 13 — Hex row Pending → OK)
@guides/evaluating-threadline.md (line 11 — lag caveat removal)
@test/threadline/adoption_pilot_doc_contract_test.exs (conditional anti-stale — now must pass with OK row)
@test/threadline/evaluating_threadline_doc_contract_test.exs (SSOT-chain conditional — now must pass)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-122-09 | OK row merged before hex.pm shows 0.6.0 | **high** | `distribution-sync` polls Hex API before edits; D-06 |
| T-122-10 | Full mix hex.info pasted in backlog (noisy churn) | low | D-05: pointer to CONTRIBUTING `#hex-publish-maintainers` only |
| T-122-11 | Evaluating guide loses 0.5.0 upgrade context | medium | Script preserves historical 0.5.0 mentions; removes lag caveat only |
| T-122-12 | Phase marked complete at Wave 1 merge | **high** | D-12/D-14: REQUIREMENTS/ROADMAP ticks in sync PR only |

**Block on high severity:** Merge sync PR only after publish job succeeded (T-122-09 enforced by workflow).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: CI generates 122-VERIFICATION.md (DIST-01 record)</name>
  <files>.planning/phases/122-release-distribution-truth/122-VERIFICATION.md</files>
  <read_first>
    - bin/post-publish-distribution-sync (122-VERIFICATION.md template)
    - .planning/phases/122-release-distribution-truth/122-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
Executed by **`distribution-sync`** job after Hex API confirms **0.6.0**.

`bin/post-publish-distribution-sync` writes `.planning/phases/122-release-distribution-truth/122-VERIFICATION.md` with:
- Tag `v0.6.0`
- Release workflow run URL
- Registry attestation excerpt (version + verified date)

No manual authoring required.
  </action>
  <acceptance_criteria>
    - Sync PR includes `122-VERIFICATION.md` with `v0.6.0` and workflow URL
    - File references Release workflow (not legacy hex-publish-only path)
  </acceptance_criteria>
  <verify>grep -q 'v0.6.0' .planning/phases/122-release-distribution-truth/122-VERIFICATION.md</verify>
</task>

<task type="auto">
  <name>Task 2: CI flips adoption-pilot Hex row Pending → OK (DIST-02)</name>
  <files>guides/adoption-pilot-backlog.md</files>
  <read_first>
    - bin/post-publish-distribution-sync (Hex row replacement)
    - guides/adoption-pilot-backlog.md (Pending row shape)
  </read_first>
  <action>
`bin/post-publish-distribution-sync` replaces the Hex distribution row:
- Status: `Pending` → `OK`
- Evidence: `Verified YYYY-MM-DD:` + hex.pm **0.6.0** + Release workflow URL + CONTRIBUTING pointer

Removes stale `latest is **0.5.0**` and `Unblock: push tag` prose.
  </action>
  <acceptance_criteria>
    - Sync PR has `| OK |` on Hex row with `latest is **0.6.0**`
    - No `latest is **0.5.0**` or `Unblock: push tag` in backlog
  </acceptance_criteria>
  <verify>grep 'latest is \*\*0.6.0\*\*' guides/adoption-pilot-backlog.md</verify>
</task>

<task type="auto">
  <name>Task 3: CI removes evaluating guide hex lag caveat (DIST-02)</name>
  <files>guides/evaluating-threadline.md</files>
  <read_first>
    - bin/post-publish-distribution-sync (evaluating guide replacement)
    - test/threadline/evaluating_threadline_doc_contract_test.exs
  </read_first>
  <action>
`bin/post-publish-distribution-sync` replaces the lag paragraph with Hex SSOT + adoption-pilot cross-link.

Preserves other **0.5.0** upgrade/historical mentions.
  </action>
  <acceptance_criteria>
    - No `may still list **0.5.0** as latest` in evaluating guide
    - `0.5.0` still present for upgrade context
  </acceptance_criteria>
  <verify>! grep -F 'may still list **0.5.0** as latest' guides/evaluating-threadline.md</verify>
</task>

<task type="auto">
  <name>Task 4: CONTRIBUTING DIST-01 proof cross-link (idempotent)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - bin/post-publish-distribution-sync (CONTRIBUTING insert)
    - CONTRIBUTING.md (Release workflow section — already updated in Wave 1 automation PR)
  </read_first>
  <action>
Script adds cross-link if missing; Wave 1 automation PR may already include it.

Post-publish distribution proof: adoption-pilot OK row + `122-VERIFICATION.md`.
  </action>
  <acceptance_criteria>
    - `grep '122-VERIFICATION.md' CONTRIBUTING.md` matches in Hex publish section
  </acceptance_criteria>
  <verify>grep -E '122-VERIFICATION|adoption-pilot-backlog' CONTRIBUTING.md</verify>
</task>

<task type="auto">
  <name>Task 5: Planning closeout — REQUIREMENTS, STATE, ROADMAP</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - bin/post-publish-distribution-sync (planning closeout block)
  </read_first>
  <action>
Script ticks DIST-01–03 in REQUIREMENTS.md, updates STATE Hex line to **0.6.0**, marks Phase 122 complete in ROADMAP.md.
  </action>
  <acceptance_criteria>
    - Sync PR ticks `[x] **DIST-01**`, `[x] **DIST-02**`, `[x] **DIST-03**`
    - Phase 122 marked complete in ROADMAP
  </acceptance_criteria>
  <verify>grep '\[x\] \*\*DIST-0[123]\*\*' .planning/REQUIREMENTS.md</verify>
</task>

<task type="manual">
  <name>Task 6: Merge distribution sync PR + verify</name>
  <files></files>
  <read_first>
    - Sync PR CI checks (verify.doc_contract, ci.all jobs)
  </read_first>
  <action>
1. Review bot PR `chore(release): sync distribution docs for 0.6.0`.

2. Confirm CI green on the PR (conditional anti-stale tests now enforce OK row).

3. Merge PR.

4. Optional local confirmation:
   ```bash
   mix verify.doc_contract
   mix ci.all
   ```
  </action>
  <acceptance_criteria>
    - Distribution sync PR merged
    - `mix verify.doc_contract` exits 0 on `main` after merge
  </acceptance_criteria>
  <verify>mix verify.doc_contract</verify>
</task>

</tasks>

<verification>
- [ ] Distribution sync PR merged
- [ ] `122-VERIFICATION.md` exists with tag and Release workflow URL
- [ ] Adoption-pilot Hex row `| OK |` with `Verified YYYY-MM-DD:` and **0.6.0**
- [ ] Evaluating guide has no `may still list **0.5.0** as latest`
- [ ] `mix verify.doc_contract` green on `main`
- [ ] DIST-01–03 checked in REQUIREMENTS.md
</verification>
