---
phase: 122-release-distribution-truth
plan: 02
type: maintainer-gate
wave: 2
depends_on:
  - 122-01-PLAN.md
files_modified: []
autonomous: false
requirements:
  - DIST-01
must_haves:
  truths:
    - "Annotated tag `v0.6.0` pushed to origin; `hex-publish.yml` job **Publish package and docs** succeeds."
    - "`mix hex.info threadline` shows **0.6.0** in Recent releases; install config reflects `~> 0.6` after registry propagation."
    - "Maintainer captures successful workflow run URL and redacted `mix hex.info` excerpt for Wave 3 (`122-VERIFICATION.md`)."
  artifacts:
    - path: .github/workflows/hex-publish.yml
      provides: "Tag-triggered publish (unchanged — event only)"
    - path: git tag v0.6.0
      provides: "Registry release event trigger"
---

<objective>
Publish `threadline` **0.6.0** to hex.pm via CI **`.github/workflows/release.yml`** (`workflow_dispatch` bootstrap or Release Please merge) — **supersedes manual tag/`hex-publish.yml` checkpoint**.

Purpose: DIST-01 publish half — closes Phase 114 deferral (D-114-05c); evaluators can `mix deps.get` 0.6.0 from Hex.
Output: Green Release workflow (gate-ci-green → publish-hex → Hex API verify); evidence captured automatically for Wave 3 distribution sync PR.
</objective>

<context>
@.planning/phases/122-release-distribution-truth/122-CONTEXT.md (D-01, D-02, D-12, D-13, D-15)
@.planning/phases/122-release-distribution-truth/122-RESEARCH.md (§3 DIST-01 Wave 2)
@.planning/phases/122-release-distribution-truth/122-PATTERNS.md (§4.4 maintainer gate checklist)
@CONTRIBUTING.md (#hex-publish-maintainers — procedure SSOT)
@.github/workflows/hex-publish.yml (tag ↔ @version gate — no edits)
@mix.exs (`@version "0.6.0"` — confirm, do not bump)
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-122-05 | Tag pushed from dirty tree or wrong commit | **high** | `git status --porcelain` empty; tag only green `main` HEAD; `mix verify.release` pre-flight |
| T-122-06 | Tag/version mismatch blocks publish | medium | `hex-publish.yml` Confirm tag matches mix.exs step; CONTRIBUTING documents sequence |
| T-122-07 | `HEX_API_KEY` missing or rotated | medium | Workflow emits error; manual `mix hex.publish` fallback per CONTRIBUTING |
| T-122-08 | Wave 3 OK row before registry propagates | **high** | Run `mix hex.info threadline` and confirm **0.6.0** before starting 122-03 |

**Block on high severity:** Do not proceed to Wave 3 until T-122-05 preconditions pass and T-122-08 registry check confirms 0.6.0.
</threat_model>

<tasks>

<task type="manual">
  <name>Task 1: Pre-tag release verification on clean main</name>
  <files></files>
  <read_first>
    - CONTRIBUTING.md (Hex publish section — steps 1–4)
    - mix.exs (`verify.release` composition)
    - .planning/phases/122-release-distribution-truth/122-CONTEXT.md (D-12 Wave 2)
  </read_first>
  <action>
1. Ensure Wave 1 PR is merged and **`main` CI is green** (all branch-protection jobs).

2. Checkout `main` and pull latest.

3. Confirm clean tree:
   ```bash
   git status --porcelain
   ```
   Output must be empty.

4. Confirm version SSOT:
   ```bash
   grep '@version "0.6.0"' mix.exs
   ```

5. Run maintainer pre-tag gate:
   ```bash
   mix verify.release
   ```

6. Optional contributor gate confirmation:
   ```bash
   mix ci.all
   ```

Record the commit SHA to tag (typically `main` HEAD after Wave 1 merge).
  </action>
  <acceptance_criteria>
    - `git status --porcelain` output is empty
    - `grep '@version "0.6.0"' mix.exs` returns exactly one match
    - `mix verify.release` exits 0
    - GitHub Actions shows green CI on target commit on `main`
  </acceptance_criteria>
  <verify>git diff --quiet HEAD -- && mix verify.release</verify>
</task>

<task type="manual">
  <name>Task 2: Tag v0.6.0 and push to origin</name>
  <files></files>
  <read_first>
    - CONTRIBUTING.md (tag and push examples — `v0.6.0`)
    - .github/workflows/hex-publish.yml (trigger: tag `v[0-9]+.[0-9]+.[0-9]+`)
  </read_first>
  <action>
From the verified clean commit on `main`:

```bash
git tag -a v0.6.0 -m "Release v0.6.0"
git push origin main    # only if local main was behind remote
git push origin v0.6.0
```

Verify tag points at intended commit:
```bash
git show v0.6.0 --no-patch --format='%H %s'
```

Do **not** change `mix.exs`, workflows, or docs in this task — tag push only.
  </action>
  <acceptance_criteria>
    - `git tag -l 'v0.6.0'` lists the tag
    - `git ls-remote --tags origin v0.6.0` returns the tag on remote
    - GitHub Actions workflow **Hex publish** (`.github/workflows/hex-publish.yml`) triggers on tag push
  </acceptance_criteria>
  <verify>git ls-remote --tags origin 'refs/tags/v0.6.0'</verify>
</task>

<task type="manual">
  <name>Task 3: Confirm hex-publish.yml success and registry state</name>
  <files></files>
  <read_first>
    - .github/workflows/hex-publish.yml (job name: Publish package and docs)
    - CONTRIBUTING.md (step 5 — watch workflow, confirm with mix hex.info)
  </read_first>
  <action>
1. Open GitHub Actions → **Hex publish** workflow run for tag **`v0.6.0`**.

2. Confirm job **Publish package and docs** completes successfully (green).

3. After registry propagation (retry if needed):
   ```bash
   mix hex.info threadline
   ```

4. Verify output includes **0.6.0** in Recent releases and install snippet shows `~> 0.6` (or `0.6.0`).

5. **Capture for Wave 3** (paste into executor notes or 122-02-SUMMARY):
   - Successful workflow run URL (full GitHub Actions link)
   - Redacted `mix hex.info threadline` excerpt containing a line with **`0.6.0`**
   - ISO date of verification (`YYYY-MM-DD`)

If publish fails: diagnose per CONTRIBUTING (tag/version mismatch, HEX_API_KEY); do **not** start Wave 3 with Pending registry.
  </action>
  <acceptance_criteria>
    - GitHub Actions hex-publish run for tag `v0.6.0` has conclusion **success**
    - `mix hex.info threadline 2>&1 | grep '0.6.0'` matches at least once
    - Maintainer has saved workflow URL and hex.info excerpt for `122-VERIFICATION.md`
  </acceptance_criteria>
  <verify>mix hex.info threadline 2>&1 | grep -q '0.6.0'</verify>
</task>

</tasks>

<verification>
- [ ] `git status --porcelain` empty before tag
- [ ] `mix verify.release` exits 0
- [ ] Tag `v0.6.0` on origin
- [ ] `hex-publish.yml` run success for `v0.6.0`
- [ ] `mix hex.info threadline` shows 0.6.0
</verification>

<manual_outputs>
Wave 3 executor inputs (required before starting 122-03):

| Field | Example shape |
|-------|---------------|
| Tag | `v0.6.0` |
| Workflow URL | `https://github.com/<org>/threadline/actions/runs/<id>` |
| Verified date | `2026-05-28` |
| hex.info excerpt | One line containing `0.6.0` from Recent releases (secrets redacted) |
</manual_outputs>
