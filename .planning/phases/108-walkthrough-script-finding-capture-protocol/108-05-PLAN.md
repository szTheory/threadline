---
phase: 108-walkthrough-script-finding-capture-protocol
plan: 05
type: execute
wave: 4
depends_on:
  - "108-01"
  - "108-04"
files_modified:
  - examples/threadline_phoenix/WALKTHROUGH.md
  - examples/threadline_phoenix/README.md
  - examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs
autonomous: true
requirements:
  - WALK-04
must_haves:
  truths:
    - "WALKTHROUGH §5 documents three evidence exercises with CLI and LiveView paths"
    - "Appendix A contains walk-critical literals for RUN-01 self-containment"
    - "Example README points maintainers to WALKTHROUGH.md for reference-app walk"
  artifacts:
    - path: examples/threadline_phoenix/WALKTHROUGH.md
      provides: "Complete maintainer runbook with appendices"
---

<objective>
Complete `WALKTHROUGH.md`: §5 evidence exercises (WALK-04), Appendix A/B, optional doc contract test, README pointer. Depends on Plan 01 redaction_policy seed.
</objective>

<context>
@.planning/phases/108-walkthrough-script-finding-capture-protocol/108-CONTEXT.md
@examples/threadline_phoenix/DEMO-MANIFEST.md
@guides/how-threadline-works.md
</context>

<scope_guard>
**ALLOWED:** files listed in frontmatter  
**FORBIDDEN:** new seed logic (Plan 01), findings files
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-108-02 | Secret in walkthrough prose | medium | Grep verify no `WALKTHROUGH-INTERNAL-SECRET` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: §5 evidence exercises WALK-04</name>
  <files>examples/threadline_phoenix/WALKTHROUGH.md</files>
  <read_first>
    - .planning/phases/108-walkthrough-script-finding-capture-protocol/108-CONTEXT.md (D-108-04b table)
    - examples/threadline_phoenix/DEMO-MANIFEST.md
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_tail.ex
  </read_first>
  <action>
Add **§5 Evidence plane exercises** opening with `mix demo.seed` prerequisite and purge-during-seed footnote (D-108-04c).

Three steps per D-108-04b:

| Step ID | Exercise | CLI | LiveView | Expected |
|---------|----------|-----|----------|----------|
| `WALK-04-01` | retention_run | `mix threadline.evidence.show` with run_id `walk-retention-offboarded-co` | `/audit/evidence` | `summary_status` completed; org Y timeline empty |
| `WALK-04-02` | redaction_policy | same task, filter policy `walk-demo-redaction-policy` | `/audit/policy/redaction` | `claim_assessment.status` = `inferred_posture`; corroborate #4521 `[REDACTED]` |
| `WALK-04-03` | trigger_coverage | optional `mix threadline.health.coverage` | `/audit/coverage` | snapshot `walk-demo-trigger-coverage`; `proven` |

Document fields: subject, subject_ref keys, summary_status, claim_assessment.status — not full JSON.

Footnote: REQUIREMENTS may say `mix verify.evidence`; canonical command is `mix threadline.evidence.show`.

**Checkpoint** after §5.
  </action>
  <verify>
    <automated>grep -q 'WALK-04-01' examples/threadline_phoenix/WALKTHROUGH.md && grep -q 'walk-demo-redaction-policy' examples/threadline_phoenix/WALKTHROUGH.md && grep -q 'mix threadline.evidence.show' examples/threadline_phoenix/WALKTHROUGH.md && ! grep -q 'WALKTHROUGH-INTERNAL-SECRET' examples/threadline_phoenix/WALKTHROUGH.md</automated>
  </verify>
  <acceptance_criteria>
    - §5 contains `WALK-04-01`, `WALK-04-02`, `WALK-04-03`
    - Documents `walk-retention-offboarded-co` and `walk-demo-redaction-policy`
    - Uses `mix threadline.evidence.show` as canonical CLI
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Appendix A/B + Further reading</name>
  <files>examples/threadline_phoenix/WALKTHROUGH.md</files>
  <read_first>
    - examples/threadline_phoenix/DEMO-MANIFEST.md (full)
    - examples/threadline_phoenix/DEMO_USERS.md
    - examples/threadline_phoenix/README.md (command cheat sheet)
  </read_first>
  <action>
Add **Appendix A — Demo reference**: copy tables for orgs, hero tickets 4521/4518, users/passwords, correlation ids, evidence refs, `demo_epoch`, `demo_last_tuesday` — synced from manifest (not invented literals).

Add **Appendix B — Command cheat sheet**: `mix setup`, `mix demo.seed`, `mix demo.reset`, `mix phx.server`, `mix threadline.evidence.show`, `mix threadline.policy.show`, `mix threadline.health.coverage`.

Add **Further reading** (optional links): `guides/getting-started-saas.md`, example README — labeled not required for run.
  </action>
  <verify>
    <automated>grep -q 'Appendix A' examples/threadline_phoenix/WALKTHROUGH.md && grep -q '4521' examples/threadline_phoenix/WALKTHROUGH.md && grep -q 'Appendix B' examples/threadline_phoenix/WALKTHROUGH.md</automated>
  </verify>
  <acceptance_criteria>
    - Appendix A includes ticket numbers 4521 and 4518
    - Appendix A includes `2026-05-20T14:30:00Z` or `demo_last_tuesday`
    - Appendix B includes `mix demo.reset`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: README pointer + walkthrough doc contract test</name>
  <files>examples/threadline_phoenix/README.md, examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/README.md (first-hour walkthrough paragraph)
    - test/threadline/readme_doc_contract_test.exs (pattern)
    - prompts/threadline-elixir-oss-dna.md (doc contract tests)
  </read_first>
  <action>
Update README paragraph: for **reference-app maintainer walk**, start with `./WALKTHROUGH.md`; integrators still use `guides/getting-started-saas.md`.

Create `test/threadline_phoenix/walkthrough_doc_contract_test.exs` reading `WALKTHROUGH.md` from priv dir or File.read! relative path asserting substrings: `4521`, `4518`, `walk-retention-offboarded-co`, `2026-05-20T14:30:00Z`, `mix demo.reset`, `WALK-03-04`.
  </action>
  <verify>
    <automated>grep -q 'WALKTHROUGH.md' examples/threadline_phoenix/README.md && cd examples/threadline_phoenix && mix test test/threadline_phoenix/walkthrough_doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - README references `WALKTHROUGH.md` for maintainer walk
    - `walkthrough_doc_contract_test.exs` exists and `mix test` on that file exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Full WALKTHROUGH readable without opening DEMO-MANIFEST mid-run (Appendix A complete)
- [ ] `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs` still passes
</verification>
