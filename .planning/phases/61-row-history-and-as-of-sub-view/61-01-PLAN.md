---
phase: 61-row-history-and-as-of-sub-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/auth.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
  - lib/threadline/operator_surface/live/row_history_component.ex
  - test/threadline/operator_surface/transaction_live_test.exs
  - test/threadline/operator_surface/row_history_component_test.exs
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "Clicking a change row in a transaction drill-down navigates to the row history sub-view URL"
    - "The sub-view shows the row's change history and an as-of snapshot based on the URL params"
    - "Clicking a row in the sub-view history updates the as-of snapshot to that timestamp via URL patch"
    - "The sub-view can handle manual as-of timestamp entry"
  artifacts:
    - path: "lib/threadline/operator_surface/live/row_history_component.ex"
      provides: "Row history slide-over sub-view component"
  key_links:
    - from: "lib/threadline/operator_surface/live/transaction_live.ex"
      to: "lib/threadline/operator_surface/live/row_history_component.ex"
      via: "live_action :history and conditional render"
    - from: "lib/threadline/operator_surface/auth.ex"
      to: "lib/threadline/operator_surface/live/row_history_component.ex"
      via: "assigns.threadline_schemas mapping"
---

<objective>
Render `Threadline.history/3` plus `Threadline.as_of/4` as a sub-view reachable only from incident drill-down change rows, with a click-to-scrub timeline driving the as-of snapshot panel.

Purpose: Enable time-travel forensics directly from a transaction view without losing context, with shareable deep-linked URLs.
Output: Route configured, parent LiveView wired with `handle_params`, and slide-over component created.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
From lib/threadline/operator_surface/router.ex:
```elixir
defmacro threadline_operator_surface(path, opts \\ [])
```

From lib/threadline/operator_surface/live/transaction_live.ex:
```elixir
def mount(%{"id" => id}, _session, socket)
def render(assigns)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route & Schema Mapping Config</name>
  <files>lib/threadline/operator_surface/router.ex, lib/threadline/operator_surface/auth.ex, test/threadline/operator_surface/transaction_live_test.exs</files>
  <action>
    Modify `lib/threadline/operator_surface/router.ex` to add the `:history` live route to the `threadline_operator_surface` macro's scope block: `live "/transactions/:id/history/:table/:record_id", TransactionLive, :history`.
    Modify `lib/threadline/operator_surface/auth.ex` in `on_mount/4` to extract `:schemas` from `opts` (default `%{})` and assign it to the socket as `:threadline_schemas`. This is crucial for resolving string table names to Ecto schema modules later.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/transaction_live_test.exs</automated>
  </verify>
  <done>Router correctly maps the sub-view route and auth pipeline assigns schema mappings.</done>
</task>

<task type="auto">
  <name>Task 2: TransactionLive Sub-view Wiring</name>
  <files>lib/threadline/operator_surface/live/transaction_live.ex, test/threadline/operator_surface/transaction_live_test.exs</files>
  <action>
    Add a `handle_params(params, _uri, socket)` callback to `TransactionLive` to handle both `:show` and `:history` actions. For `:history`, extract `table`, `record_id`, and `as_of` from `params`. If `as_of` is present, attempt to parse it with `DateTime.from_iso8601/1`. Assign these to the socket, along with a `show_history` flag.
    In `render/1`, update the `.change-row` elements to include a link (e.g. `live_patch` or `a` tag with `phx-click`) pointing to the history URL for that record, passing its `captured_at` as the `as_of` param. 
    Also in `render/1`, when `@live_action == :history`, conditionally render `<.live_component module={Threadline.OperatorSurface.Live.RowHistoryComponent} id="row-history" ... />` inside the `.threadline-ui` container, passing down the necessary params and the `threadline_schemas`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/transaction_live_test.exs</automated>
  </verify>
  <done>TransactionLive correctly parses URL params, conditionally renders the component, and wires up links from change rows.</done>
</task>

<task type="auto">
  <name>Task 3: RowHistoryComponent Implementation</name>
  <files>lib/threadline/operator_surface/live/row_history_component.ex, test/threadline/operator_surface/row_history_component_test.exs</files>
  <action>
    Create `RowHistoryComponent`. Wrap the module in `if Code.ensure_loaded?(Phoenix.LiveView) do`.
    Implement `update/2` to resolve `schema_module = assigns.threadline_schemas[assigns.table]`.
    If no schema maps, assign an error state. If valid, fetch data using `Threadline.history(schema_module, assigns.record_id, repo: assigns.repo)`. Also fetch the snapshot via `Threadline.as_of/4` using the parsed `as_of` datetime (or fallback if empty).
    Implement the `render/1` template as a slide-over panel with two sections: a click-to-scrub timeline (list of changes, each pushing a URL patch to update `as_of`) and a snapshot display panel. Include a fallback `datetime-local` input that patches the URL for manual entry. Use `.threadline-ui` class conventions for styling where applicable.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/row_history_component_test.exs</automated>
  </verify>
  <done>RowHistoryComponent successfully fetches its own data using domain APIs, handles missing schemas gracefully, and renders the click-to-scrub UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL -> Parent LiveView | Untrusted URL params (`table`, `record_id`, `as_of`) are parsed to drive database lookup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | `RowHistoryComponent` | mitigate | `threadline_schemas` whitelist assignment prevents arbitrary `table` strings from resolving to malicious/unintended Ecto schemas. Unmapped tables yield safe empty states. |
| T-61-02 | Info Disclosure | `RowHistoryComponent` | accept | Sub-view inherently exposes historical row states. Authorized via parent `TransactionLive` policy scope already validated in `mount`. |
</threat_model>

<verification>
mix test
</verification>

<success_criteria>
- The `/transactions/:id/history/:table/:record_id` route parses securely and resolves state.
- Component fetches `Threadline.history` and `Threadline.as_of` isolated from the parent.
- URL-driven click-to-scrub timeline works as intended.
- Module optionally loaded correctly based on the Phase 57 standard.
</success_criteria>

<output>
After completion, create `.planning/phases/61-row-history-and-as-of-sub-view/61-01-SUMMARY.md`
</output>
