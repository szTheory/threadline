---
phase: 204-structure
plan: 08
type: execute
wave: 6
depends_on: ["204-07"]
files_modified:
  - lib/threadline/operator_surface/live/stress_live.ex
  - lib/threadline/operator_surface/live/stress_live/sections.ex
  - lib/threadline/operator_surface/live/stress_live/paths.ex
  - mix.exs
  - test/threadline/release_artifact_contract_test.exs
  - test/threadline/operator_surface/stress_router_test.exs
  - test/threadline/operator_surface/component_contract_test.exs
  - test/threadline/source_size_contract_test.exs
  - test/threadline/credo_config_contract_test.exs
autonomous: true
requirements: [STRUCT-03, STRUCT-07]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`stress_live.ex` is under 800 lines and its 540-line `render/1` is carved into one function component per story section (each ≤120 lines, with `attr` declarations), hosted in `@moduledoc false` sibling `Threadline.OperatorSurface.Live.StressLive.Sections`; path/link helpers may move to `StressLive.Paths` if still needed to get under 800 (D-08, D-12: stress_live is not an exception)."
    - "mix.exs `exclude_patterns` covers the whole family with `~r{^lib/threadline/operator_surface/live/stress_live(\\.ex$|/)}`, and release_artifact_contract_test lists `lib/threadline/operator_surface/live/stress_live/` in `@maintainer_only_prefixes` plus each new file in `@maintainer_only_paths`, in the commit that creates the first sibling (Pitfall 4)."
    - "Both register sites in stress_live.ex are drained, with the credo register lowered in the same commit (D-13)."
    - "The only file exception left in the size gate after this plan's lib work is `lib/threadline/operator_surface/stress_fixtures.ex` with reason \"declarative fixture data tables; excluded from the Hex package (mix.exs exclude_patterns)\" (D-12)."
    - "Rendered output is unchanged at every commit: byte lock, rendered_output_contract, stress_router_test, and the browser lane at exactly the 8 known failures; `mix verify.operator_stress` has the same outcome as at plan base (D-00b)."
    - "`@ui_form_policy` of StressLive stays declared on the page module, and the `:formless`/`:has_forms` check (hardened in 204-07 to read the page family) still covers markup moved into the siblings."
  artifacts:
    - lib/threadline/operator_surface/live/stress_live/sections.ex
  key_links:
    - "mix.exs exclude pattern → unpacked Hex tarball → `@maintainer_only_prefixes`; the stress lab never ships."
    - "stress_router_test `@stress_live_source` reads → `Threadline.Test.SourceFamily.read!/1` so moved section markup stays pinned."
  prohibitions:
    - statement: "Maintainer-only stress-lab code never enters the published Hex package."
      status: resolved
      verification: test
    - statement: "Historical fixture text (for example the `stress_live.ex:794` citation inside `test/fixtures/operator_surface/design-system-ledger.json`) is never edited: the fixture corpus is sealed by `manifest.sha256`."
      status: resolved
      verification: test
---

<objective>
Make the 1,398-line maintainer-only stress lab LiveView legible: carve its 540-line `render/1` into per-section components in a sibling module, keep the whole family out of the Hex package, and drain its two register sites (STRUCT-03, STRUCT-07; D-08, D-12, D-13).

Purpose: D-12 rules that stress_live gets no size exception, because it is compiled on the lib path and splits cleanly. Its packaging exclusion must widen with it.

Output: stress_live.ex under 800 lines, `StressLive.Sections` (and `StressLive.Paths` if needed), directory-wide Hex exclusion with archive proof.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-RESEARCH.md
@.planning/phases/204-structure/204-PATTERNS.md
@CLAUDE.md
</context>

## Zero-output-change proof (ZOC) — after EVERY commit (D-00b)

1. `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors`
2. `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/stress_router_test.exs test/threadline/operator_surface/component_contract_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/ui_form_policy_contract_test.exs test/threadline/operator_surface/style_byte_lock_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs test/threadline/release_artifact_contract_test.exs`
3. Browser lane: `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-08-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-08-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-08-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-08-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"`
4. `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.operator_stress > /tmp/p204-08-stress.log 2>&1; echo "exit=$?"` — record the exit code and summary line at plan base BEFORE any change; after each commit it must match the base outcome exactly.

Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-08-base"`.

**HALT clause:** if a ZOC step differs from base, revert the commit and retry. Never regenerate baselines, Tier A scorecards, or fixtures; never edit a pinned string; never add a size exception for stress_live; never bypass hooks; never `git add .planning/`.

Conventions (D-00): `refactor(204-08)`; siblings inside `if Code.ensure_loaded?(Phoenix.LiveView) do`, `@moduledoc false`, `use Phoenix.Component`, `attr(...)` declarations, only used aliases; no banners, no planning vocabulary.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — widen the Hex exclusion and move the first story section into StressLive.Sections</name>
  <reversibility rating="reversible">Maintainer-only, unpackaged module; no public API (D-12).</reversibility>
  <files>mix.exs, test/threadline/release_artifact_contract_test.exs, lib/threadline/operator_surface/live/stress_live.ex, lib/threadline/operator_surface/live/stress_live/sections.ex, test/threadline/operator_surface/stress_router_test.exs, test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/live/stress_live.ex (lines 1-120, then the first story section inside `render/1`)
    - mix.exs `package/0` exclude_patterns
    - test/threadline/release_artifact_contract_test.exs (lines 150-200)
    - test/threadline/operator_surface/stress_router_test.exs (~:130-140, ~:520-545)
  </read_first>
  <action>
    Per D-08, D-12, Pitfall 4, in ONE commit: widen the mix.exs pattern to `~r{^lib/threadline/operator_surface/live/stress_live(\.ex$|/)}`; add the prefix `lib/threadline/operator_surface/live/stress_live/` to `@maintainer_only_prefixes` and `lib/threadline/operator_surface/live/stress_live/sections.ex` to `@maintainer_only_paths`; create `Threadline.OperatorSurface.Live.StressLive.Sections` with the first story section of `render/1` as a public function component (`attr` for each assign it reads), called from the parent as `<Sections.name …/>`; repoint stress_router_test `@stress_live_source` reads to `Threadline.Test.SourceFamily.read!/1`; lower the stress_live.ex function and file pins. Run ZOC including the base-compare of `verify.operator_stress`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/release_artifact_contract_test.exs test/threadline/operator_surface/stress_router_test.exs test/threadline/operator_surface/ui_form_policy_contract_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/source_size_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary; a release-artifact failure naming `stress_live/` means the exclusion missed the sibling</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-08-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-08-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-08-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-08-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"</automated>
    <fails_when>non-zero exit: not exactly 82 passed / 8 failed, a failure outside operator-screenshot-regression.spec.ts, or a baseline PNG changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'stress_live(' mix.exs` prints 1
    - `grep -c 'live/stress_live/"' test/threadline/release_artifact_contract_test.exs` is at least 1
    - `D=$(git diff "$(cat "$(git rev-parse --git-dir)/gsd-204-08-base")" HEAD -- test/fixtures) && test -z "$D"` exits 0
  </acceptance_criteria>
  <done>The stress-lab family is excluded as a directory and the first section renders from the sibling with identical output.</done>
</task>

<task type="auto">
  <name>Task 2: Carve the remaining sections, bring stress_live.ex under 800, drain its two register sites</name>
  <files>lib/threadline/operator_surface/live/stress_live.ex, lib/threadline/operator_surface/live/stress_live/sections.ex, lib/threadline/operator_surface/live/stress_live/paths.ex, mix.exs, test/threadline/release_artifact_contract_test.exs, test/threadline/operator_surface/component_contract_test.exs, test/threadline/source_size_contract_test.exs, test/threadline/credo_config_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/live/stress_live.ex (the remaining `render/1` body; the helper heads from `grep -n '    defp ' `)
    - test/threadline/operator_surface/component_contract_test.exs (~:460-470: stress_live listing)
  </read_first>
  <action>
    Per D-08, D-12, D-13. Move each remaining story section into `StressLive.Sections` as its own component (one commit per two or three sections), until `render/1` in the parent is at most 120 lines and every component is at most 120 lines. If the parent is still over 800 lines, move the path/link helpers (`base_path`, `stress_path`, `clear_path`, `filter_path`, `story_path`, `maybe_put`, `*_link_class`) into `lib/threadline/operator_surface/live/stress_live/paths.ex` (`StressLive.Paths`) and add that path to `@maintainer_only_paths` in the same commit. Drain both register sites in stress_live.ex. Remove the stress_live file and `render/1` exceptions in the commits that make them stale; verify the only remaining file exception is stress_fixtures.ex. Update component_contract_test's stress_live listing only if it enumerates file paths.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/stress_router_test.exs test/threadline/operator_surface/component_contract_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs test/threadline/release_artifact_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>test "$(wc -l < lib/threadline/operator_surface/live/stress_live.ex)" -le 800 && test "$(grep -c 'Structural debt:' lib/threadline/operator_surface/live/stress_live.ex)" = "0" && test "$(grep -c 'live/stress_live' test/threadline/source_size_contract_test.exs)" = "0"</automated>
    <fails_when>non-zero exit: stress_live.ex is over 800 lines, a register site remains, or a stress_live size exception survives</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '"lib/threadline/operator_surface/stress_fixtures.ex" =>' test/threadline/source_size_contract_test.exs` prints 1
    - `mix verify.operator_stress` outcome after the last commit equals the base outcome recorded in ZOC step 4
    - ZOC browser chain passed after every commit (outcomes in the SUMMARY)
  </acceptance_criteria>
  <done>The stress lab is legible, excluded from Hex as a family, and has no size exception or register site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → Hex package | only adopter-relevant code may ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-20 | Information disclosure | stress_live siblings | high | mitigate | Directory-wide exclude pattern plus prefix/path assertions against the unpacked tarball, same commit as the first sibling |
| T-204-21 | Tampering | sealed fixture corpus | medium | mitigate | Fixture diff must be empty (Task 1 acceptance); manifest contract test unchanged |
</threat_model>

<verification>
- ZOC green after every commit; `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures; `MIX_ENV=dev mix verify.dialyzer` → exit 0.
</verification>

<success_criteria>
stress_live.ex under 800 lines with every function ≤120, excluded from Hex as a directory family, and the size gate's only file exception is stress_fixtures.ex.
</success_criteria>

## Artifacts this phase produces (this plan)

- `Threadline.OperatorSurface.Live.StressLive.Sections` (`lib/threadline/operator_surface/live/stress_live/sections.ex`) with one component per story section
- Optional `Threadline.OperatorSurface.Live.StressLive.Paths` (`lib/threadline/operator_surface/live/stress_live/paths.ex`)
- mix.exs exclude pattern `~r{^lib/threadline/operator_surface/live/stress_live(\.ex$|/)}`

<output>
Create `.planning/phases/204-structure/204-08-SUMMARY.md` when done, with the section-to-component map and the verify.operator_stress base/after outcomes.
</output>
