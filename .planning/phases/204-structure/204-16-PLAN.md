---
phase: 204-structure
plan: 16
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - test/threadline/source_size_contract_test.exs
  - lib/threadline/critic_trust/krippendorff_alpha.ex
  - lib/mix/tasks/critic.measure.ex
  - lib/threadline/critic_trust/repository_boundary.ex
  - test/threadline/public_surface_contract_test.exs
  - test/threadline/release_artifact_contract_test.exs
  - .planning/REQUIREMENTS.md
  - .planning/phases/204-structure/204-12-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [STRUCT-04, STRUCT-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The five box-drawing section banners found by 204-VERIFICATION are gone from lib/: krippendorff_alpha.ex:107 and critic.measure.ex:44, :122, :172, :431. `grep -rnE '^\\s*#.*(-{3,}|={3,}|─{3,}|\\*{3,})\\s*$' lib --include='*.ex'` prints nothing (STRUCT-04, D-11)."
    - "Each banner went by the D-11 rule. Four sections were already one cohesive clause group, so their banners were deleted: the krippendorff private helpers, the critic.measure option/provenance parsing, the three `read_*!` readers, and the print_summary/fmt output group. The fifth section, the ~258-line repository-only path and decode boundary in critic.measure.ex, became a real module boundary, `Threadline.CriticTrust.RepositoryBoundary` (D-11)."
    - "The size gate's `@banner` regex is `~r/^\\s*#\\s*(?:-{3,}|={3,}|─{2,}|\\*{3,})|(?:-{3,}|={3,}|─{3,}|\\*{3,})\\s*$/u`. A comment counts as a banner if it opens with a rule run (a box-drawing run can be as short as 2) or ends with a rule run of 3 or more. With the banners still present, this regex matched exactly the 5 known lines on the real lib/ comment set. The planner measured that at HEAD f045ce28, and the executor re-measures it in Task 1 while the tree is still RED."
    - "A planted self-test proves the widened gate fails on the titled form (rule, title, trailing rule), on a leading-box-only title, and on a trailing-dash title. The same test proves it still ignores prose that contains rule characters, such as CSS custom properties with a `--tl-` prefix, markdown table rules and arrows."
    - "Rendered output is unchanged (D-00b). No CSS, HEEx or operator-surface file is touched. The CSS byte lock stays green. `mix critic.measure` prints the same messages and error text as before, including the `critic.measure:` prefix and the `repository-only: true` line, and it still reads the atomic-write hook from the Process key `{Mix.Tasks.Critic.Measure, :atomic_write_hook}`."
    - "WR-01 is fixed (STRUCT-03). A keyword-form clause (`def f(...), do: ...`) is measured to the end of its expression, not counted as 1 line. The gate uses `meta[:end_of_expression][:line]`, falls back to the maximum metadata line in the clause subtree, and leaves the `do ... end` path unchanged. Planted self-tests prove that a 121-line `do: ~H` heredoc clause fails and that a keyword clause followed by another def is measured exactly."
    - "With the corrected counter, the real tree still passes the function rule with `@function_exceptions == %{}`. The longest clause stays at or under 120 lines; the verifier measured 114."
    - "STRUCT-04 is reopened in REQUIREMENTS.md (checkbox unchecked, traceability row `In Progress`) before any code change, and is marked Complete again only after the widened gate is green in the phase-end `ci.all`. 204-12-SUMMARY.md gets an appended erratum. Its body is not rewritten."
    - "Phase-end proof: `MIX_ENV=test mix ci.all` exits 0 with 0 test failures, `mix credo --strict` is clean, and `mix verify.example_browser` is exactly 326 passed / 8 failed / 16 skipped. The failures are exactly operator-screenshot-regression.spec.ts :108 :115 :136 :145 on desktop and mobile."
  artifacts:
    - test/threadline/source_size_contract_test.exs
    - lib/threadline/critic_trust/repository_boundary.ex
    - lib/mix/tasks/critic.measure.ex
    - lib/threadline/critic_trust/krippendorff_alpha.ex
  key_links:
    - "source_size_contract_test `@banner` → `count_banners/1` over `Code.string_to_quoted_with_comments` comments → `validate_banners/2` with `@banner_exceptions %{}` → runs in `verify.test`, which `ci.all` lists."
    - "`Mix.Tasks.Critic.Measure.run/1` → `Threadline.CriticTrust.RepositoryBoundary` (path resolution, validated decode, atomic ledger write, `task_error!/3`) → exercised end to end by test/threadline/operator_surface/critic_trust_test.exs (traversal, symlink, overlap, malformed input, adjudication, atomic replace, interrupted write)."
    - "`collect_clause/2` length → `measure_functions/1` → `validate_functions/2` against `@function_limit 120`."
  prohibitions:
    - statement: "The widened banner gate must not be turned green by weakening it: no new `@banner_exceptions` entry, no narrowing of `@source_glob`, no exclusion of the critic files from the scan, and no edit to the 'exceptions at rest' test."
      status: unverified
      flagged: true
    - statement: "Removing a banner must not change what the critic.measure task does or guards: the repository-only path checks (traversal, absolute escape, prefix confusion, symlink, overlap), the validated decode, and the atomic ledger write keep identical behavior and identical error text."
      status: unverified
      flagged: true
    - statement: "The WR-01 counter fix must not be offset by quietly adding a function exception. If the corrected measure puts any real clause over 120 lines, the executor halts and reports it rather than pinning it."
      status: unverified
      flagged: true
---

<objective>
Close the single 204-VERIFICATION gap, STRUCT-04. The size gate's banner regex did not catch the box-drawing banner form that opens with two rule characters, then a title, then a trailing rule. Five such banners survived in lib/ while the gate reported zero tolerance and 204-12-SUMMARY claimed it caught every such line. This plan widens the regex and proves it with a planted self-test, turns the gate RED on the real five lines, and removes them by the D-11 rule: delete a banner whose section is one cohesive clause group, and turn the one real boundary section into a module. It also fixes review WR-01 in the same file, where keyword-form `do:` clauses were measured as 1 line (STRUCT-03). Finally it re-closes STRUCT-04 truthfully and records an erratum on the false claim.

Purpose: SC 2 and STRUCT-04 must be true of the tree and enforced by the gate. Today they are false of the tree, and the gate is weaker than its moduledoc says.

Output: a widened and self-tested banner gate, a keyword-clause-aware function measure, zero banners in lib/, the `Threadline.CriticTrust.RepositoryBoundary` module, and STRUCT-04 marked Complete again once ci.all is green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-VERIFICATION.md
@.planning/phases/204-structure/204-REVIEW.md
@.planning/phases/204-structure/204-12-SUMMARY.md
@.planning/phases/204-structure/204-15-SUMMARY.md
@CLAUDE.md
</context>

## Rules for this plan (HALT clauses, carried verbatim from project rules)

- Record the plan base first: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-16-base"`.
- Execute sequentially on the main working tree. Do not use worktrees, because the Elixir suite cannot run in one.
- Each commit is atomic, uses the message form `{type}(204-16): ...`, and ends with the line `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.
- NEVER `git add .planning/` or `git add -A` / `git add .`. Stage by explicit path only.
- Protected paths the executor must NOT modify, stage, move, replace, or restore: `.planning/WINDOWS.md`, `.planning/config.json`, `.tool-versions` (untracked), `.gsd/`.
- On a blocked precondition, HALT and report. Never work around it by moving protected files or bypassing hooks (`--no-verify` and `core.hooksPath` are forbidden).
- Never run playwright directly. Use `mix verify.example_browser`.
- A red Dialyzer in ci.all usually means a PLT cache miss. Rebuild with `mix dialyzer --plt` before concluding anything.
- No pushing. Milestone tags stay local.
- Never regenerate screenshot baselines. After any browser run, `git checkout -- test/fixtures/operator_surface/scorecards/` and never stage that directory.
- Do not touch the sealed dialyzer fixture `test/fixtures/dialyzer/critic-tooling.json`. Its origin lines are historical records of fixed warnings, not live pins. If `verify.dialyzer` reports a NEW warning in `repository_boundary.ex` or `critic.measure.ex`, HALT and report it.
- Out of scope: review WR-02 and IN-01..IN-05. Do not fix them here.

## Flagged assumptions (spec-less probe; unresolved, not dismissed)

- **STRUCT-04, category `unclassified`, status unresolved.** The probe could not classify the edge, so no probe predicate is authored for it. The plan's own edges are the must_haves truths above: the regex matches exactly 5 real lines while RED, stops at 0 once GREEN, and its planted positives and negatives are named. This row stays **unresolved** for manual review.
- **STRUCT-03, category `concurrency` ("If interrupted or run in parallel, what is guaranteed?"), status unresolved.** The assumed answer is that the size gate is a read-only scan of the working tree inside an `async: true` ExUnit case with no shared mutable state, and that each commit in this plan is individually green, so an interruption between commits leaves a consistent tree. This is **assumed, not proven by a probe**, so the row stays **unresolved**.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer: widen the banner gate, watch it go RED on the real five lines, then remove the five banners by D-11 to turn it GREEN</name>
  <reversibility rating="reversible">A test-regex change plus a code move into one new module. Both revert with a plain `git revert` and change no persisted data or public API (the new module is `@moduledoc false` and excluded from the Hex package).</reversibility>
  <files>.planning/REQUIREMENTS.md, test/threadline/source_size_contract_test.exs, lib/threadline/critic_trust/krippendorff_alpha.ex, lib/mix/tasks/critic.measure.ex, lib/threadline/critic_trust/repository_boundary.ex, test/threadline/public_surface_contract_test.exs, test/threadline/release_artifact_contract_test.exs</files>
  <read_first>
    - test/threadline/source_size_contract_test.exs (whole file, 439 lines: `@banner` at :38, moduledoc banner bullet at :14-16, banner self-test at :156-183, `banner_count!/2` at :281-286)
    - lib/threadline/critic_trust/krippendorff_alpha.ex (:100-115, the private-helpers banner at :107 and the explanatory comment below it)
    - lib/mix/tasks/critic.measure.ex (whole file, 466 lines: banners at :44, :122, :172, :431; the boundary section spans :172-:429; `atomic_write_hook/0` at :412 uses `Process.get({__MODULE__, :atomic_write_hook})`)
    - test/threadline/operator_surface/critic_trust_test.exs (:1-10 alias; :761-1100, the critic.measure behavior tests including `Process.put({Mix.Tasks.Critic.Measure, :atomic_write_hook}, ...)` at :1067)
    - test/threadline/public_surface_contract_test.exs (`@hidden_modules` at :6-13)
    - test/threadline/release_artifact_contract_test.exs (`@maintainer_only_paths` at :166-186)
    - mix.exs (:445-453 `exclude_patterns`, which already covers `^lib/threadline/critic_trust/`)
    - .planning/REQUIREMENTS.md (:87 STRUCT-04 checkbox, :194 traceability row)
  </read_first>
  <action>
    Step 0, reopen STRUCT-04 (per the verifier's gap). In .planning/REQUIREMENTS.md, change line 87 from a checked to an unchecked box (keep the text) and change the traceability row `| STRUCT-04 | Phase 204 | Complete |` to `| STRUCT-04 | Phase 204 | In Progress |`. Commit only that file, by explicit path, as `docs(204-16): reopen STRUCT-04 until the widened banner gate is green`.

    Step 1, widen the gate and watch it go RED. Do not commit this step on its own. In source_size_contract_test.exs, replace `@banner` at :38 with `~r/^\s*#\s*(?:-{3,}|={3,}|─{2,}|\*{3,})|(?:-{3,}|={3,}|─{3,}|\*{3,})\s*$/u`. The regex has two branches. The first catches a comment that opens with a rule run, where a box-drawing run can be as short as 2 characters. The second catches a comment that ends with a rule run of 3 or more. The first branch deliberately does not use a 2-character dash run, so a comment that starts with a CSS custom property such as the `--tl-space-*` comment at stress_live/refute.ex:27 is not a banner. Update the moduledoc "Separator banners" bullet so it states both forms in plain words and gives the titled, trailing-rule example in prose. Add a planted self-test in the "separator banners" describe block, named "a titled banner is counted whether the rule leads or trails, and prose with rule characters is not". Its synthetic module holds exactly 4 banner comments: a title between a 2-character box run and a long box run, a 2-character box run followed by a title with no trailing rule, a title between `===` runs, and a title followed by a `-----` run. It also holds 4 non-banner comments: one starting with a `--tl-` custom property name, a markdown table rule surrounded by pipes, a prose line containing an arrow, and plain prose. Assert `%{"lib/titled.ex" => 4} = count_banners(files)`, assert `validate_banners(files, %{})` returns an error containing "lib/titled.ex has 4 separator banner", and assert `:ok` when the file is pinned at 4. Keep the existing banner self-test unchanged; it must still count 4. Run `mix test test/threadline/source_size_contract_test.exs` and confirm it is RED. The real-tree test "the real tree matches the banner register exactly" must fail with exactly two offenders: "lib/mix/tasks/critic.measure.ex has 4 separator banner" and "lib/threadline/critic_trust/krippendorff_alpha.ex has 1 separator banner". Paste the failure lines into the SUMMARY as the RED evidence. If the RED run names any other file or count, HALT and report. That would mean the regex has false positives the planner did not see.

    Step 2, remove the five banners per D-11 and turn the gate GREEN.
    (a) In krippendorff_alpha.ex, delete the banner line at :107 and its separating blank line, so one blank line remains between `bootstrap_ci/3` and the explanatory comment that opens the defp group. The section is one cohesive group of private helpers (`weighted_sum/5`, `add_coincidence/2`, `expected_term/5`, `ordinal_distance_sq/4`) after the public API, and the `defp` keyword is already the boundary. Keep every other comment.
    (b) In critic.measure.ex, delete the banners at :44 (option and provenance parsing: `parse_options!/1`, `source!/2`, `provenance_for/2`), :122 (the three `read_*!` readers) and :431 (`print_summary/2`, `fmt/1`). Each of those sections is one cohesive clause group.
    (c) Turn the :172 section, "repository-only path and decode boundary" (:172-:429), into a real module boundary. Create lib/threadline/critic_trust/repository_boundary.ex defining `Threadline.CriticTrust.RepositoryBoundary` with `@moduledoc false`, and move every function of that section into it verbatim. The functions are `project_root!`, `resolve_repository_root!`, `canonicalize_root!`, `validate_root_separation!`, `within?`, `require_directory!`, `read_json_object!`, `read_json_text!`, `read_text!`, `validate_score!`, `validate_golden!` with its `valid_*`/`adjudication_*` helpers, `atomic_replace!`, `atomic_write_hook`, `restore_command` and `task_error!` with its `@spec ... :: no_return()`. Make public (`def`) exactly the functions the task still calls after the move: project_root!/0, resolve_repository_root!/3, validate_root_separation!/2, require_directory!/3, read_json_object!/3, read_json_text!/3, read_text!/3, validate_score!/2, validate_golden!/3, atomic_replace!/2, restore_command/1 and task_error!/3. Everything else stays `defp`. Re-grep to confirm the list before finalizing. Two literals must survive the move unchanged, because `__MODULE__` now names a different module and would silently break both. First, `atomic_write_hook` must read `Process.get({Mix.Tasks.Critic.Measure, :atomic_write_hook})` with the task module written out, not `__MODULE__`, because critic_trust_test.exs:1067 plants the hook under that key. Second, every `Mix.raise` message keeps its `critic.measure:` prefix and its `resolved path:` / `repository-only: true` / `next:` lines byte for byte. The new module must carry `alias Threadline.CriticTrust.Measure`, because the moved `validate_golden!` helpers call `Measure.lenses()` (critic.measure.ex :297 and :324). The planner found no other alias or module attribute used inside :172-:429; everything else there is fully qualified (`Mix.Project`, `Jason`, `File`, `Path`, `System`). Before compiling, re-grep the moved code for any unqualified module reference or `@attribute` and carry each one across. In critic.measure.ex, extend the alias to `alias Threadline.CriticTrust.{LedgerSplice, Measure, RepositoryBoundary}` and call the moved functions as `RepositoryBoundary.name(...)`. Do not import them. Do not leave a banner or section-title comment in either file.
    (d) Register the new module wherever the critic modules are enumerated. Add `Threadline.CriticTrust.RepositoryBoundary` to `@hidden_modules` in public_surface_contract_test.exs, and add `"lib/threadline/critic_trust/repository_boundary.ex"` to `@maintainer_only_paths` in release_artifact_contract_test.exs, keeping alphabetical order within the critic_trust entries. `exclude_patterns` in mix.exs already covers the path, so do not edit mix.exs.

    Run the verify commands. Then commit in three green commits, per D-00, staging by explicit path with one lib/ restructuring concern per commit. Commit 1: `refactor(204-16): make the critic.measure repository boundary a real module`, staging critic.measure.ex (the extraction plus its three cohesive-group banner deletions), repository_boundary.ex, and the two enumeration tests that must change in the same commit for it to be green. Commit 2: `refactor(204-16): drop the private-helpers banner in krippendorff_alpha`, staging only krippendorff_alpha.ex. Commit 3: `test(204-16): widen the banner gate to titled box-rule banners`, staging only source_size_contract_test.exs. Each commit is green on its own: commits 1 and 2 are green under the old regex, and commit 3 lands the widened regex only after all five banners are gone. If the three critic.measure.ex banner deletions cannot be separated from the extraction at staging time, they stay in commit 1 as described. If any other combination is unavoidable, a combined commit of lib/ files uses the type `refactor(204-16)`. Test-file changes never go into a `refactor` commit except the two enumeration updates that commit 1 needs. Record the actual commit list in the SUMMARY.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/source_size_contract_test.exs test/threadline/operator_surface/critic_trust_test.exs test/threadline/critic_trust test/threadline/public_surface_contract_test.exs test/threadline/release_artifact_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary, for example "has 4 separator banner", "has 1 separator banner", a critic.measure error-text mismatch, an interrupted-write test that no longer sees its hook, or the hidden-module or maintainer-path assertion failing</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors</automated>
    <fails_when>non-zero exit or any "warning:" line, such as an unused function left behind in critic.measure.ex or an undefined RepositoryBoundary function</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict lib/mix/tasks/critic.measure.ex lib/threadline/critic_trust/repository_boundary.ex lib/threadline/critic_trust/krippendorff_alpha.ex test/threadline/source_size_contract_test.exs test/threadline/public_surface_contract_test.exs test/threadline/release_artifact_contract_test.exs</automated>
    <fails_when>non-zero exit or any credo issue listed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '^\s*#.*(-{3,}|={3,}|─{3,}|\*{3,})\s*$' lib --include='*.ex' | wc -l` prints 0
    - `grep -rnE '^\s*#\s*─' lib --include='*.ex' | wc -l` prints 0
    - `grep -cF '@banner ~r/^\s*#\s*(?:-{3,}|={3,}|─{2,}|\*{3,})|(?:-{3,}|={3,}|─{3,}|\*{3,})\s*$/u' test/threadline/source_size_contract_test.exs` prints 1
    - `grep -c 'a titled banner is counted whether the rule leads or trails' test/threadline/source_size_contract_test.exs` prints 1
    - `grep -c '@banner_exceptions %{}' test/threadline/source_size_contract_test.exs` prints 1 (no pin was added)
    - `grep -c 'defmodule Threadline.CriticTrust.RepositoryBoundary do' lib/threadline/critic_trust/repository_boundary.ex` prints 1, and `grep -c '@moduledoc false' lib/threadline/critic_trust/repository_boundary.ex` prints 1
    - `grep -c 'Process.get({Mix.Tasks.Critic.Measure, :atomic_write_hook})' lib/threadline/critic_trust/repository_boundary.ex` prints 1
    - `grep -c 'atomic_write_hook\|defp canonicalize_root!\|defp within?' lib/mix/tasks/critic.measure.ex` prints 0 (the boundary moved out, not copied)
    - `grep -c 'Threadline.CriticTrust.RepositoryBoundary' test/threadline/public_surface_contract_test.exs` prints 1, and `grep -c 'lib/threadline/critic_trust/repository_boundary.ex' test/threadline/release_artifact_contract_test.exs` prints 1
    - `git diff --stat "$(cat "$(git rev-parse --git-dir)/gsd-204-16-base")" -- lib/threadline/operator_surface test/fixtures mix.exs` prints nothing
    - the SUMMARY quotes the RED run naming exactly critic.measure.ex (4) and krippendorff_alpha.ex (1)
    - `git log -1 --format=%s -- .planning/REQUIREMENTS.md` prints `docs(204-16): reopen STRUCT-04 until the widened banner gate is green`
  </acceptance_criteria>
  <done>The widened, self-tested banner gate went RED on exactly the five known lines and is now GREEN with no exception. The critic.measure repository boundary is a real module with unchanged behavior, and STRUCT-04 is open until the phase-end proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Measure keyword-form clauses to the end of their expression (review WR-01)</name>
  <reversibility rating="reversible">A change to the test-only measuring function, reverted by `git revert`. No lib code changes.</reversibility>
  <files>test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - test/threadline/source_size_contract_test.exs (`collect_clause/2` at :252-262, the function-length describe at :105-149, the `clause/4` and `module_source/1` helpers at :428-434)
    - .planning/phases/204-structure/204-REVIEW.md (the WR-01 section: metadata shape and suggested measure)
  </read_first>
  <behavior>
    - A module whose last expression is `def f(assigns),` followed by `do: ~H` with a 119-line heredoc body and its closing delimiter is measured at 121 lines or more, and `validate_functions/2` returns an error containing "lib/kw.ex f/1 has a" and "-line clause". This is the planted violation, and the old counter reported 1.
    - A keyword clause with a plain `"""` heredoc body of 5 lines, followed by `def g, do: 1`, measures f/1 as exactly 8 (head line, `do:` line, 5 body lines, closing line) and g/0 as exactly 1.
    - The existing do/end measurements are unchanged: the 121-line clause test, the never-summed test (run/1 is 60) and the guarded-head test (f/1 is 5, g/0 is 1) keep passing without edits.
    - The real tree still returns `:ok` from `validate_functions(scan(@source_glob), @function_exceptions)` with `@function_exceptions == %{}`.
  </behavior>
  <action>
    Per review WR-01 and STRUCT-03. RED first. Add the two planted tests from the behavior block to the "function length" describe, named "a keyword do: clause is measured to the end of its expression, not counted as one line" and "a keyword clause followed by another def is measured exactly". Build their sources inline with `String.duplicate`, as the existing helpers do. Run them and confirm the first fails because the old counter reports 1. Paste the failure line into the SUMMARY as the RED evidence. Do not commit the red state; the tests and the fix land together in one green commit.

    GREEN next. In `collect_clause/2`, keep the `meta[:end]` branch exactly as it is (`end_meta[:line] - meta[:line] + 1`). Replace the `nil -> 1` fallback with a last-line computation. Use `meta[:end_of_expression][:line]` when it is present. Otherwise take the maximum `:line` found in the metadata of every node in the clause subtree, looking at each node's own `:line`, `:closing`, `:end` and `:end_of_expression` keys, with `meta[:line]` as the floor. The length is that last line minus `meta[:line]` plus 1. The planner probed Elixir 1.17.3: `Code.string_to_quoted(..., token_metadata: true)` puts `end_of_expression` on a keyword-form def even when it is the module's last expression, and its line is the closing heredoc delimiter's line. That holds for both `~H` sigils and plain strings, and plain heredoc strings carry no metadata of their own, so `end_of_expression` is the primary source. Put the subtree walk in a small private helper, for example `last_meta_line/1`. Update the moduledoc "Function length" bullet to say keyword-form clauses are measured from the head line to the end of their expression.

    Re-measure the real tree. If any real clause now exceeds 120 lines, HALT and report its path, name/arity and length. Do not add a `@function_exceptions` entry. The verifier's independent measure says the longest keyword-form clause is 7 lines and the longest clause overall is 114. Record the new measured maximum in the SUMMARY. Commit as `test(204-16): measure keyword-form clauses to the end of their expression`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/source_size_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary, including "has a 1-line clause"-style under-measurement in the planted test or a real-tree "over the 120-line limit" message</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict test/threadline/source_size_contract_test.exs</automated>
    <fails_when>non-zero exit or any credo issue listed (for example nesting depth in the new helper)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nil -> 1' test/threadline/source_size_contract_test.exs` prints 0
    - `grep -c 'end_of_expression' test/threadline/source_size_contract_test.exs` prints at least 1
    - `grep -c 'a keyword do: clause is measured to the end of its expression' test/threadline/source_size_contract_test.exs` prints 1
    - `grep -c 'a keyword clause followed by another def is measured exactly' test/threadline/source_size_contract_test.exs` prints 1
    - `grep -c '@function_exceptions %{}' test/threadline/source_size_contract_test.exs` prints 1
    - the SUMMARY records the real-tree maximum clause length after the fix, and it is 120 or less
  </acceptance_criteria>
  <done>A long keyword-form clause can no longer evade the 120-line rule, do/end measurements are unchanged, and the real tree still passes with no function exception.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-end proof, then truthfully re-close STRUCT-04 and record the erratum</name>
  <reversibility rating="reversible">Planning-document edits and gate runs only. Every edit is plain text in git.</reversibility>
  <files>.planning/REQUIREMENTS.md, .planning/phases/204-structure/204-12-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (:87 and :194 as reopened by Task 1)
    - .planning/phases/204-structure/204-12-SUMMARY.md (frontmatter line :56 and body line :127, which carry the false claim)
    - .planning/ROADMAP.md (the Phase 204 block from :787: `**Plans**:` count at :800 and the plan list ending at the Wave 15 entry at :861)
    - .planning/STATE.md (the current position and progress block, read before and after any state.* call)
    - .planning/phases/204-structure/204-15-SUMMARY.md (the previous phase-end gate table, for format)
  </read_first>
  <action>
    Per D-00 and D-00b. Run the gates below and record command, exit code, summary line and duration in a gate table in the SUMMARY.
    (1) `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix ci.all`. It must exit 0 with 1778 or more tests and 0 failures. If Dialyzer is red, rebuild the PLT with `mix dialyzer --plt` and re-run once before concluding anything.
    (2) `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict`.
    (3) The browser lane, using the verify command below. It must be exactly 326 passed / 8 failed / 16 skipped, and the failing set must be exactly operator-screenshot-regression.spec.ts :108 :115 :136 :145 on desktop and mobile. A known flake clears with ONE unchanged re-run. A 9th failure or a changed baseline is a real regression: HALT. Immediately after every browser run, run `git checkout -- test/fixtures/operator_surface/scorecards/` and never stage that directory.
    If any gate is red after these allowances, HALT and report the failing gate. Do not patch around it, and do not re-close STRUCT-04.

    Only when all three are green, re-close STRUCT-04. In .planning/REQUIREMENTS.md, set line 87 back to a checked box and the traceability row back to `| STRUCT-04 | Phase 204 | Complete |`.

    Append an erratum to the end of .planning/phases/204-structure/204-12-SUMMARY.md. Do not edit any existing line in that file. Write a new `## Erratum (204-16, 2026-09-24)` section stating three things. First, the claim that the zero-tolerance banner gate failed any rule comment was false: the `@banner` regex required three rule characters immediately after `#`, so five titled box-drawing banners (critic.measure.ex ×4, krippendorff_alpha.ex ×1) escaped it. Second, the "tree-wide banner grep prints 0" claim used a pattern with the same blind spot. Third, 204-16 widened the regex, added a planted self-test, and removed the five banners.

    Update ROADMAP.md. The planner already added a `- [ ] 204-16-PLAN.md` line under a new gap-closure wave heading in the Phase 204 plan list and changed `**Plans**: 15 plans` to 16. Check that line and change `[ ]` to `[x]`. Update STATE.md with exactly these gsd-core v1.14.0 commands, run from the repo root, in this order: (i) `node ~/.claude/gsd-core/bin/gsd-tools.cjs query state.advance-plan`; (ii) `node ~/.claude/gsd-core/bin/gsd-tools.cjs query state.update-progress`; (iii) `node ~/.claude/gsd-core/bin/gsd-tools.cjs query state.record-metric --phase 204 --plan 16 --duration "$DURATION_MIN" --tasks 3 --files "$FILE_COUNT" (both measured by the executor)`; (iv) `node ~/.claude/gsd-core/bin/gsd-tools.cjs query state.record-session --stopped-at "Completed 204-16-PLAN.md" --resume-file None`. Copy STATE.md to `/tmp/p204-16-STATE.before` before (i). Use flag-style arguments only; if a command rejects its arguments, HALT and report rather than retrying with positional arguments. Positional arguments are rejected, and the old `gsd-sdk` wrapper must not be used. Afterwards, hand-check STATE.md's progress block and the ROADMAP progress row, because the state.* handlers miscompute this repo's bespoke progress block. Report in the SUMMARY exactly which lines of STATE.md and ROADMAP.md changed, with a before and after for each.

    Commit, by explicit paths only (.planning/REQUIREMENTS.md, .planning/phases/204-structure/204-12-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md, and this plan's SUMMARY), as `docs(204-16): close STRUCT-04 after the widened banner gate is green`. Never stage `.planning/` as a directory, `.planning/WINDOWS.md` or `.planning/config.json`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix ci.all</automated>
    <fails_when>non-zero exit from any ci.all step (the failing alias is printed last), or a test summary line with 1 or more failures</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict</automated>
    <fails_when>non-zero exit or any credo issue listed</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-16-browser.log 2>&1; grep -Eq '(^|[^0-9])326 passed' /tmp/p204-16-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-16-browser.log && grep -Eq '(^|[^0-9])16 skipped' /tmp/p204-16-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-16-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"; R=$?; git checkout -- test/fixtures/operator_surface/scorecards/ && test "$R" = 0</automated>
    <fails_when>non-zero exit, meaning one of these: not exactly 326 passed / 8 failed / 16 skipped, a failure outside operator-screenshot-regression.spec.ts, or a baseline PNG changed</fails_when>
    <automated>git diff --numstat "$(cat "$(git rev-parse --git-dir)/gsd-204-16-base")" -- .planning/phases/204-structure/204-12-SUMMARY.md > /tmp/p204-16-erratum.numstat && test "$(awk '{print $2}' /tmp/p204-16-erratum.numstat)" = 0</automated>
    <fails_when>non-zero exit: the deleted-lines column of the numstat for 204-12-SUMMARY.md is not 0 (any removed line, including a bullet or a blank line), or the file shows no change at all because the erratum was never appended (empty output)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*STRUCT-04\*\*' .planning/REQUIREMENTS.md` prints 1, and `grep -c '| STRUCT-04 | Phase 204 | Complete |' .planning/REQUIREMENTS.md` prints 1
    - `grep -c '^## Erratum (204-16' .planning/phases/204-structure/204-12-SUMMARY.md` prints 1, and `git diff --numstat "$(cat "$(git rev-parse --git-dir)/gsd-204-16-base")" -- .planning/phases/204-structure/204-12-SUMMARY.md > /tmp/p204-16-erratum.numstat && awk '{print $2}' /tmp/p204-16-erratum.numstat` prints exactly `0`. The file is append-only: the deleted-lines column must be 0, so a removed bullet, a removed blank line or any other deleted line fails the check
    - `grep -c '\[x\] 204-16-PLAN.md' .planning/ROADMAP.md` prints 1
    - `git status --porcelain -- test/fixtures/operator_surface/scorecards/` prints nothing
    - no commit in this plan touches `.planning/WINDOWS.md`, `.planning/config.json` or `.tool-versions` (`git log --name-only --format= "$(cat "$(git rev-parse --git-dir)/gsd-204-16-base")"..HEAD > /tmp/p204-16-touched.txt && grep -cxF -e .planning/WINDOWS.md -e .planning/config.json -e .tool-versions /tmp/p204-16-touched.txt` prints 0)
    - the SUMMARY contains a gate table showing ci.all exit 0 (test count and 0 failures), credo --strict clean, and the browser lane at 326/8/16 with the eight known failures listed
  </acceptance_criteria>
  <done>Every phase gate is green with the widened banner gate and the corrected function measure. STRUCT-04 is Complete again on evidence, the false 204-12 claim carries an appended erratum, and the ROADMAP and STATE changes are reported line by line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor diff → size/banner gate | the gate decides whether a separator banner or an oversized function can land in lib/ |
| CLI argv / fixture files → `mix critic.measure` | maintainer-supplied paths and JSON cross into a task that rewrites a tracked ledger file; the repository-only boundary being moved is the guard |
| agent completion claims → planning record | a SUMMARY or REQUIREMENTS checkbox asserting a gate property the gate does not have |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-35 | Tampering | source_size_contract_test `@banner` | medium | mitigate | Widened two-branch regex plus a planted self-test with 4 positives and 4 negatives. `@banner_exceptions %{}` and the at-rest test stay unchanged, and an acceptance grep checks there is no pin |
| T-204-36 | Elevation of Privilege | critic.measure repository-only path boundary (traversal, symlink, prefix confusion, overlap), moved into `RepositoryBoundary` | high | mitigate | Verbatim move with no logic edits. The Process hook key and the `Mix.raise` text are kept as literals. The existing critic_trust_test traversal, absolute-escape, symlink, overlap, malformed-input and interrupted-write tests must pass unchanged in Task 1 |
| T-204-37 | Tampering | `collect_clause/2` keyword-clause measure | medium | mitigate | Measure keyword clauses by `end_of_expression`, with a subtree-max fallback. The planted 121-line `do: ~H` test fails under the old counter and passes under the new one. The HALT clause forbids adding function exceptions |
| T-204-38 | Repudiation | 204-12-SUMMARY / REQUIREMENTS STRUCT-04 | low | mitigate | STRUCT-04 is reopened before any code change and re-closed only after a green ci.all. An append-only erratum is checked by a diff grep for removed lines |
| T-204-39 | Information Disclosure | new lib module leaking into the Hex package | low | mitigate | The path falls under the existing `^lib/threadline/critic_trust/` exclude pattern, and it is added to `@maintainer_only_paths`, which the release-artifact contract checks against the unpacked tarball |
</threat_model>

<verification>
- Task 1's RED run names exactly critic.measure.ex (4) and krippendorff_alpha.ex (1). After Task 1, the gate is GREEN with `@banner_exceptions %{}`.
- Task 2's planted 121-line keyword clause fails the gate, and the real tree passes with `@function_exceptions %{}`.
- Task 3's gate table: ci.all exits 0 with 0 failures, credo --strict is clean, and the browser lane is at 326/8/16 with the known eight.
- STRUCT-04 goes Complete, then In Progress, then Complete again, and each transition is a separate `docs(204-16)` commit.
</verification>

<success_criteria>
No separator banner remains in lib/ under a gate that would catch the titled box-drawing form (ROADMAP SC 2, STRUCT-04, D-11). The function-length rule measures keyword-form clauses correctly (STRUCT-03, WR-01). Rendered output is unchanged (D-00b). STRUCT-04 is marked Complete only on green evidence, and the earlier false claim is corrected by an appended erratum.
</success_criteria>

## Artifacts this phase produces (this plan)

- `test/threadline/source_size_contract_test.exs`: the widened two-branch `@banner` regex, the titled-banner self-test, the keyword-clause-aware `collect_clause/2` with a last-line helper, two planted keyword-clause tests, and updated moduledoc bullets
- `lib/threadline/critic_trust/repository_boundary.ex` (new): `Threadline.CriticTrust.RepositoryBoundary`, `@moduledoc false`, holding critic.measure's repository-only path, decode and atomic-write boundary
- `lib/mix/tasks/critic.measure.ex`: no banners, calls into `RepositoryBoundary`
- `lib/threadline/critic_trust/krippendorff_alpha.ex`: the private-helpers banner removed
- `test/threadline/public_surface_contract_test.exs` and `test/threadline/release_artifact_contract_test.exs`: the new module registered as hidden and as maintainer-only
- `.planning/REQUIREMENTS.md` (STRUCT-04 reopened, then closed), `.planning/phases/204-structure/204-12-SUMMARY.md` (appended erratum), `.planning/ROADMAP.md` and `.planning/STATE.md` (plan 16 recorded)
- `.planning/phases/204-structure/204-16-SUMMARY.md`: the RED evidence, the new maximum clause length, the phase-end gate table, and the STATE/ROADMAP line diff

<output>
Create `.planning/phases/204-structure/204-16-SUMMARY.md` when done. It holds the RED evidence from Task 1, the post-fix maximum clause length from Task 2, the phase-end gate table from Task 3, and an exact before/after list of the STATE.md and ROADMAP.md lines that changed.
</output>
