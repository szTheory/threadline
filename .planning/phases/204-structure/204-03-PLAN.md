---
phase: 204-structure
plan: 03
type: execute
wave: 2
depends_on: ["204-01"]
files_modified:
  - lib/threadline/operator_surface/style.ex
  - lib/threadline/operator_surface/style/stylesheet.css
  - lib/threadline/operator_surface/style/01_tokens.css
  - lib/threadline/operator_surface/style/02_base_shell.css
  - lib/threadline/operator_surface/style/03_page_home.css
  - lib/threadline/operator_surface/style/04_controls.css
  - lib/threadline/operator_surface/style/05_feedback.css
  - lib/threadline/operator_surface/style/06_layout_primitives.css
  - lib/threadline/operator_surface/style/07_find_detail.css
  - lib/threadline/operator_surface/style/08_overlays_motion.css
  - lib/threadline/operator_surface/style/09_responsive.css
  - test/threadline/operator_surface/style_byte_lock_test.exs
  - test/threadline/source_size_contract_test.exs
  - test/threadline/operator_surface/style_contract_test.exs
  - test/threadline/brandbook_token_parity_test.exs
  - test/threadline/operator_surface/component_contract_test.exs
  - test/threadline/operator_surface/stress_router_test.exs
  - test/threadline/operator_surface/rendered_output_contract_test.exs
  - test/threadline/release_artifact_contract_test.exs
  - examples/threadline_phoenix/e2e/tests/operator-stress.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-component-contracts.spec.ts
  - DESIGN-SYSTEM.md
autonomous: true
requirements: [STRUCT-02, STRUCT-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The CSS byte lock from 204-01 is green at every commit of this plan: the pivot and each of the eight peels (D-04, STRUCT-02)."
    - "`Threadline.OperatorSurface.Style` reads nine ordered `.css` files from `lib/threadline/operator_surface/style/` at compile time: an explicit literal `@segments` list (no `Path.wildcard`), one `@external_resource` per segment, `@stylesheet` built as `\"<style>\" <> … <> \"</style>\"` (or with the tags inside the bytes, whichever the pivot proved byte-identical), rendered through `Phoenix.HTML.raw/1` next to the existing `fonts_html` assign (D-02)."
    - "The segment files and their content follow D-03: 01_tokens, 02_base_shell, 03_page_home, 04_controls, 05_feedback, 06_layout_primitives, 07_find_detail, 08_overlays_motion, 09_responsive; the `/* End Find cluster primitives */` comment moves verbatim into 07 (D-06)."
    - "Peels land tail-first (09 first, 02 last), one per commit; the remainder is renamed with `git mv` to `01_tokens.css` (D-04, D-00)."
    - "Every source-text reader of style.ex is repointed to `Threadline.Test.SourceFamily.read!/1` in the pivot commit, and the planning-vocabulary scan in rendered_output_contract_test covers `lib/threadline/operator_surface/style/*.css` (D-05)."
    - "The Hex archive contains every segment file, asserted against the unpacked tarball in release_artifact_contract_test (D-02, Pitfall 3)."
    - "Adjacency edge (STRUCT-02): concatenating the segments in `@segments` order reproduces the rendered bytes exactly; each cut sits at brace depth 0 after a blank line, and the byte lock proves no newline is gained or lost at any join."
    - "Empty edge (STRUCT-02): `Style.segments/0` is non-empty, every listed file exists and is non-empty, no file is listed twice, and no `.css` file in `style/` is outside the list (no orphan)."
    - "Ordering edge (STRUCT-02): a test asserts `Style.segments/0` equals the literal cascade order 01..09, and `style.ex` contains no `Path.wildcard`."
    - "At plan end the size gate carries no style entry: style.ex and every segment are under 800 lines, and the `css/1` function exception is gone (D-12)."
  artifacts:
    - lib/threadline/operator_surface/style.ex
    - lib/threadline/operator_surface/style/01_tokens.css
    - lib/threadline/operator_surface/style/09_responsive.css
  key_links:
    - "`style.ex` `@external_resource` per segment → recompiles `Style` when a `.css` file changes; without it the lock stays green against stale beams (Pitfall 2)."
    - "`ui.ex` shell → `<Threadline.OperatorSurface.Style.css />` is the single call site and is not edited."
  prohibitions:
    - statement: "No byte of rendered CSS or HTML changes; the golden file and both pins from 204-01 are never regenerated in this plan."
      status: resolved
      verification: test
    - statement: "Screenshot baselines under `operator-screenshot-regression.spec.ts-snapshots/` are never regenerated or modified."
      status: resolved
      verification: test
---

<objective>
Split the 4,509-line `style.ex` into nine ordered, individually legible `.css` segments read at compile time, with the rendered CSS byte-identical at every commit (STRUCT-02, D-02..D-06). This retires the largest size exception.

Purpose: `style.ex` is one `~H` heredoc. Nobody can review it, and CSS in a heredoc is invisible to CSS tooling. The byte lock from 204-01 turns this into a provably inert move.

Output: `style.ex` (~40 lines) plus nine segment files, repointed source readers, a segment-order test, and packaging assertions.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-RESEARCH.md
@.planning/phases/204-structure/204-PATTERNS.md
@.planning/phases/204-structure/204-01-SUMMARY.md
@CLAUDE.md
</context>

## Zero-output-change proof (ZOC) — run after EVERY commit in this plan (D-00b)

1. `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix compile --force --warnings-as-errors`
2. `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/style_byte_lock_test.exs test/threadline/source_size_contract_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/component_contract_test.exs test/threadline/operator_surface/style_contract_test.exs test/threadline/brandbook_token_parity_test.exs test/threadline/operator_surface/stress_router_test.exs`
3. Browser lane: `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-03-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-03-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-03-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-03-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"`. Never run Playwright directly.

Record the plan base first: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-03-base"`.

**HALT clause:** if the byte lock goes red, the change altered bytes: revert that commit's change and retry the cut. Never regenerate `test/fixtures/style/operator_surface.css` or either sha pin, never regenerate screenshot baselines, never raise a size pin to absorb growth, never bypass hooks. A 9th browser failure or a newly passing member of the known 8 is a regression: stop and report. Never `git add .planning/`.

Commit type `refactor(204-03)` for lib moves, `test(204-03)` for test-only changes, `docs(204-03)` for comment/doc wording. No `Phase \d+`/`STRUCT-0x`/`D-NN` text inside any `.css` or `lib/` file.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — pivot the whole stylesheet into one compile-time `.css` file behind the lock</name>
  <reversibility rating="reversible">One module's internals plus a moved resource; `git revert` restores the heredoc (D-02 rating).</reversibility>
  <files>lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/style/stylesheet.css, test/threadline/operator_surface/style_byte_lock_test.exs, test/threadline/source_size_contract_test.exs, test/threadline/operator_surface/style_contract_test.exs, test/threadline/brandbook_token_parity_test.exs, test/threadline/operator_surface/component_contract_test.exs, test/threadline/operator_surface/stress_router_test.exs, test/threadline/operator_surface/rendered_output_contract_test.exs, test/threadline/release_artifact_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/style.ex (lines 1-20 and 4490-4509 only; the middle is CSS)
    - lib/threadline/operator_surface/fonts.ex (the `@external_resource` loop and compile-time `File.read!`)
    - test/support/source_family.ex
    - test/threadline/operator_surface/style_contract_test.exs (line 5 and the literals near :973, :1014, :1020, :1356 that carry source indentation)
    - test/threadline/operator_surface/rendered_output_contract_test.exs (:180-192 and `owned_static_sources/0` near :415)
    - test/threadline/release_artifact_contract_test.exs (:135-145 "built Hex archive excludes repository evidence")
  </read_first>
  <action>
    Per D-02, D-04 (pivot), D-05, and Pitfalls 1-3:

    (1) Extract the style-owned CSS from the RENDERED bytes, not from source lines: reuse the golden `test/fixtures/style/operator_surface.css` (it is exactly `<style>…</style>` after the fonts prefix) and write its bytes minus the leading `<style>` and trailing `</style>` to `lib/threadline/operator_surface/style/stylesheet.css`. The file keeps the 2-space rendered indentation.

    (2) Rewrite `style.ex` inside the existing `if Code.ensure_loaded?(Phoenix.LiveView) do` guard: `@style_dir Path.join(__DIR__, "style")`, `@segments ~w(stylesheet.css)` (explicit literal list), a `for segment <- @segments do @external_resource Path.join(@style_dir, segment) end` loop, `@stylesheet "<style>" <> Enum.map_join(@segments, &File.read!(Path.join(@style_dir, &1))) <> "</style>"`. In `css/1`, assign `:fonts_html` as today and `:stylesheet` as `Phoenix.HTML.raw(@stylesheet)`, and render `~H` containing `{@fonts_html}{@stylesheet}` only. Keep `font_face_style/0`. Add `@doc false def segments, do: @segments`. If the byte lock shows a join newline off by one, settle it here, empirically, by where `<style>`/`</style>` live; do not touch the golden.

    (3) Repoint every style.ex text reader to `Threadline.Test.SourceFamily.read!("lib/threadline/operator_surface/style.ex")` (it returns style.ex plus the `.css` files): style_contract_test `@style_path` reads, brandbook_token_parity_test `style_source/0`, component_contract_test (:21 and the reads near :281, :321, :578, :650), stress_router_test `@style_source` reads. Update indent-sensitive literals from source indentation (8/10 spaces) to rendered indentation (2/4 spaces) where an assertion matches whitespace; re-grep and record each changed literal in the SUMMARY. Assertion meaning must not change.

    (4) rendered_output_contract_test: widen `owned_static_sources/0` and the listing near :187 so `lib/threadline/operator_surface/style/*.css` is scanned for planning vocabulary (the `.ex` filter becomes `.ex` or `.css`), and assert the stylesheet path is in the scanned set.

    (5) style_byte_lock_test: add "segments are the cascade" (for now `Style.segments() == ["stylesheet.css"]`), "every segment exists, is non-empty, and is unique", and "no orphan css" (`Style.segments() |> Enum.sort() == Path.wildcard(style dir <> "/*.css") |> Enum.map(&Path.basename/1) |> Enum.sort()`), plus a refutation that style.ex source contains `Path.wildcard`.

    (6) release_artifact_contract_test: in the archive-contents test assert `"lib/threadline/operator_surface/style/stylesheet.css" in entries`.

    (7) source_size_contract_test (same commit, because the gate fails on stale pins): delete the `style.ex` file exception and the `{style.ex, :css, 1}` function exception; add a file exception for `style/stylesheet.css` at its measured line count with reason "oversized; being split into cohesive modules".

    Commit: `refactor(204-03): render the operator stylesheet from a compile-time css resource`. Run ZOC.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/style_byte_lock_test.exs test/threadline/source_size_contract_test.exs test/threadline/operator_surface/style_contract_test.exs test/threadline/brandbook_token_parity_test.exs test/threadline/operator_surface/component_contract_test.exs test/threadline/operator_surface/stress_router_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/release_artifact_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary; a byte-lock failure message naming a first differing line means `raw` or a join newline is wrong</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.example_browser > /tmp/p204-03-browser.log 2>&1; grep -Eq '(^|[^0-9])82 passed' /tmp/p204-03-browser.log && grep -Eq '(^|[^0-9])8 failed' /tmp/p204-03-browser.log && ! grep -E '^ +[0-9]+\) \[' /tmp/p204-03-browser.log | grep -qv 'operator-screenshot-regression.spec.ts' && test -z "$(git status --porcelain -- examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts-snapshots)"</automated>
    <fails_when>non-zero exit: the summary is not 82 passed / 8 failed, a failure is listed outside operator-screenshot-regression.spec.ts, or a baseline PNG changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `wc -l < lib/threadline/operator_surface/style.ex` prints a number under 80
    - `grep -c 'Phoenix.HTML.raw(@stylesheet)' lib/threadline/operator_surface/style.ex` prints 1 and `grep -c '@external_resource' lib/threadline/operator_surface/style.ex` is at least 1
    - `git diff --quiet "$(cat "$(git rev-parse --git-dir)/gsd-204-03-base")" HEAD -- test/fixtures/style/` exits 0 (golden untouched)
    - `grep -rn '"lib/threadline/operator_surface/style.ex"' test | grep -v SourceFamily` prints only rendered_output_contract listings and the size/lock tests (record each remaining hit in the SUMMARY with why it is intentionally single-file)
  </acceptance_criteria>
  <done>The stylesheet lives in a compile-time resource, the lock is green, and every reader follows it.</done>
</task>

<task type="auto">
  <name>Task 2: Peel the eight segments tail-first, one commit each, then name the remainder 01_tokens.css</name>
  <files>lib/threadline/operator_surface/style.ex, lib/threadline/operator_surface/style/stylesheet.css, lib/threadline/operator_surface/style/01_tokens.css, lib/threadline/operator_surface/style/02_base_shell.css, lib/threadline/operator_surface/style/03_page_home.css, lib/threadline/operator_surface/style/04_controls.css, lib/threadline/operator_surface/style/05_feedback.css, lib/threadline/operator_surface/style/06_layout_primitives.css, lib/threadline/operator_surface/style/07_find_detail.css, lib/threadline/operator_surface/style/08_overlays_motion.css, lib/threadline/operator_surface/style/09_responsive.css, test/threadline/operator_surface/style_byte_lock_test.exs, test/threadline/source_size_contract_test.exs, test/threadline/release_artifact_contract_test.exs</files>
  <read_first>
    - .planning/phases/204-structure/204-CONTEXT.md D-03 table (segment names and content)
    - .planning/phases/204-structure/204-RESEARCH.md §Pattern 2 (source→rendered line mapping: rendered = source − 17; cut sheet lines 318, 674, 1099, 1706, 2125, 2779, 3285, 3970)
    - lib/threadline/operator_surface/style/stylesheet.css (only the ~10 lines around each cut, located by grep of the section comment, not the whole file)
  </read_first>
  <action>
    Per D-03, D-04, D-06. Before each cut, re-verify with a comment-aware brace-depth scan (a short `mix run --no-start -e` or awk script, recorded in the SUMMARY) that the cut line sits at depth 0 and follows a blank line. Then, one commit per segment in this order: 09_responsive, 08_overlays_motion, 07_find_detail (keep `/* End Find cluster primitives */` verbatim), 06_layout_primitives, 05_feedback, 04_controls, 03_page_home, 02_base_shell. Each commit moves the tail bytes from `stylesheet.css` into the new file, inserts the new name into `@segments` immediately after the remainder (so the list stays in cascade order: remainder, 02…09 as they appear), updates the segment-order test expectation, adds the file to the release-artifact `in entries` assertions, and updates the size-gate pin for `stylesheet.css` (delete the pin once it is at or under 800). After the last peel, `git mv style/stylesheet.css style/01_tokens.css`, set `@segments ~w(01_tokens.css 02_base_shell.css 03_page_home.css 04_controls.css 05_feedback.css 06_layout_primitives.css 07_find_detail.css 08_overlays_motion.css 09_responsive.css)`, and make the order test assert that exact list. Commit messages: `refactor(204-03): peel <name> from the operator stylesheet`. Run ZOC after every commit (all three steps).
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/style_byte_lock_test.exs test/threadline/source_size_contract_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/operator_surface/style_contract_test.exs test/threadline/brandbook_token_parity_test.exs test/threadline/operator_surface/component_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
    <automated>test "$(ls lib/threadline/operator_surface/style/*.css | wc -l | tr -d ' ')" = "9" && ! test -e lib/threadline/operator_surface/style/stylesheet.css && test "$(wc -l lib/threadline/operator_surface/style/*.css | awk '$2 != "total" && $1 > 800' | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>non-zero exit: not exactly nine segments, the remainder was not renamed, or a segment exceeds 800 lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `L=$(git log --format=%s "$(cat "$(git rev-parse --git-dir)/gsd-204-03-base")"..HEAD) && printf '%s\n' "$L" | grep -c '^refactor(204-03): peel '` prints 8
    - `F=$(git log --follow --format=%h -- lib/threadline/operator_surface/style/01_tokens.css) && test "$(printf '%s\n' "$F" | grep -c .)" -ge 2` exits 0 (rename tracked)
    - `grep -c 'operator_surface/style' test/threadline/source_size_contract_test.exs` prints 0 (no style entry left in any exception map)
    - the browser-lane grep chain from ZOC step 3 passed after each of the nine commits (log the nine outcomes in the SUMMARY)
  </acceptance_criteria>
  <done>Nine legible segments in cascade order, each under 800 lines, with the lock green at every intermediate commit.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh stale style.ex citations in e2e comments and DESIGN-SYSTEM.md</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-stress.spec.ts, examples/threadline_phoenix/e2e/tests/operator-phase-178-uat.spec.ts, examples/threadline_phoenix/e2e/tests/operator-screenshot-regression.spec.ts, examples/threadline_phoenix/e2e/tests/operator-component-contracts.spec.ts, DESIGN-SYSTEM.md</files>
  <read_first>
    - `grep -rn 'style\.ex' examples/threadline_phoenix/e2e/tests DESIGN-SYSTEM.md` output
    - any doc-contract test that pins DESIGN-SYSTEM.md wording (`grep -rln 'DESIGN-SYSTEM.md' test`)
  </read_first>
  <action>
    Per D-05. Rewrite each `style.ex:NNNN` citation to name the segment file and selector instead of a line number (for example "`09_responsive.css`, the `@media (min-width: 1280px)` block"), because line citations rot. Change only comments and the one assertion-message string at operator-stress.spec.ts ~:498; never an assertion condition, selector, or threshold. DESIGN-SYSTEM.md ~:176: "Map primitives to functional UI tokens in the operator stylesheet (`lib/threadline/operator_surface/style/01_tokens.css`)". If a doc-contract test pins the old wording, update it in the same commit. Commit: `docs(204-03): cite stylesheet segments instead of style.ex line numbers`.
  </action>
  <verify>
    <automated>test "$(grep -rn 'style\.ex:[0-9]' examples/threadline_phoenix/e2e/tests DESIGN-SYSTEM.md | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>non-zero exit: a `style.ex:<line>` citation remains</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test $(grep -rln 'DESIGN-SYSTEM.md' test | sort) test/threadline/operator_surface/style_byte_lock_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `D=$(git diff "$(cat "$(git rev-parse --git-dir)/gsd-204-03-base")" HEAD -- examples/threadline_phoenix/e2e/tests) && printf '%s\n' "$D" | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[-+][[:space:]]*(//|\*)' | grep -v 'must become two-column'` prints nothing and exits via the final grep with status 1 (only comment lines and the one message string changed)
  </acceptance_criteria>
  <done>No comment or doc points at a style.ex line that no longer exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library source → adopter compile | adopters compile the Hex package from source, reading `.css` files at their compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-07 | Denial of service | Hex package contents | high | mitigate | release_artifact_contract_test asserts every segment is in the unpacked archive; a missing file would break adopters' `mix compile` |
| T-204-08 | Tampering | compiled CSS vs source | medium | mitigate | `@external_resource` per segment plus orphan/order tests, so edited `.css` always recompiles and every file is read |
| T-204-09 | Information disclosure | CSS comments shipped in the package | low | mitigate | planning-vocabulary scan widened to `style/*.css` |
</threat_model>

<verification>
- ZOC green after every commit; `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures.
- `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix verify.compile_no_optional` and `MIX_ENV=dev mix verify.dialyzer` → exit 0.
</verification>

<success_criteria>
The emitted CSS hash is unchanged at every commit and the style module is nine ordered, legible segments (ROADMAP SC 1).
</success_criteria>

## Artifacts this phase produces (this plan)

- `lib/threadline/operator_surface/style/01_tokens.css` … `09_responsive.css` (nine compile-time CSS resources; `stylesheet.css` exists only transiently)
- `Threadline.OperatorSurface.Style.segments/0` (`@doc false`), attributes `@segments`, `@style_dir`, `@stylesheet`
- New tests in `style_byte_lock_test.exs`: segment order, existence/uniqueness, no orphan, no wildcard

<output>
Create `.planning/phases/204-structure/204-03-SUMMARY.md` when done, with the nine browser-lane outcomes and every indentation literal changed.
</output>
