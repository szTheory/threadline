---
phase: 204-structure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - bin/verify-bump-rehearsal
  - test/threadline/ci_all_dedup_contract_test.exs
  - test/threadline/ci_topology_contract_test.exs
  - test/threadline/ci_workflow_parity_contract_test.exs
  - test/threadline/adoption_pilot_doc_contract_test.exs
  - test/threadline/evaluating_threadline_doc_contract_test.exs
  - test/threadline/ci_coverage_doc_contract_test.exs
  - CONTRIBUTING.md
  - guides/configuration-and-commands.md
  - guides/evaluating-threadline.md
  - guides/adoption-pilot-backlog.md
  - prompts/ARCHITECTURE-CODE-WALKTHROUGH-DNA.md
autonomous: true
requirements: [STRUCT-06]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`mix.exs` defines no `verify.doc_contract` alias and no `preferred_envs` entry for it; `ci.all` no longer lists `verify.doc_contract`, `verify.critic_trust`, or `verify.mechanical` (D-20, D-21)."
    - "The `verify.critic_trust` and `verify.mechanical` aliases still exist, because the ci.yml `verify-mechanical` and capture jobs and maintainers use them (D-21)."
    - "`test/threadline/ci_all_dedup_contract_test.exs` reads `Threadline.MixProject.project()[:aliases]` at runtime (no regex over mix.exs) and asserts: (a) `:\"verify.doc_contract\"` is absent; (b) recursively expanding string aliases, exactly one `ci.all` step (`verify.test`) expands to a `test…` command, with function-capture aliases treated as opaque; (c) no alias anywhere is a `test` command with two or more explicit `.exs` paths; (d) `ci.all` is non-empty and contains `verify.test` (D-24)."
    - "The ci.yml `Doc contract tests` step is gone from job `verify-test`; every job `id:` is unchanged (D-22, CLAUDE.md stable job IDs)."
    - "`bin/verify-bump-rehearsal` derives its doc-contract file list at run time inside the throwaway clone with `find test \\( -name '*doc_contract_test.exs' -o -name '*readme_contract_test.exs' \\) | sort`, fails when fewer than 30 files are found, and runs `mix test` on that explicit list (D-23)."
    - "Every doc and contract test that named `verify.doc_contract`, or said critic_trust/mechanical run inside ci.all, is updated in the same commit as the change it describes; CHANGELOG history lines stay (D-25)."
    - "Adjacency edge (STRUCT-06): after expansion, no two ci.all steps both run test files; `verify.test` is the single step that does (guard (b))."
    - "Empty edge (STRUCT-06): the rehearsal refuses to run `mix test` with an empty or short list (floor 30); the dedup guard fails when ci.all is empty."
    - "Ordering edge (STRUCT-06): ci.all keeps `verify.test` before `verify.threadline`, `verify.example`, Dialyzer, and the browser lane; the rehearsal file list is `sort`ed so its run order is deterministic."
  artifacts:
    - test/threadline/ci_all_dedup_contract_test.exs
    - bin/verify-bump-rehearsal
    - mix.exs
  key_links:
    - "ci_topology_contract_test and ci_workflow_parity_contract_test use `:binary.match` on the ci.all block, which raises MatchError when a step disappears, so mix.exs and both tests change in one commit (Pitfall 9)."
    - "`mix verify.bump_rehearsal` → `bin/verify-bump-rehearsal` → `mix test <derived list>` at the NEXT version; the 12 formerly-drifted doc-contract files run there for the first time."
  prohibitions:
    - statement: "The set of contract tests that actually run in ci.all is not reduced: only steps that re-ran files `verify.test` already runs are removed."
      status: resolved
      verification: test
    - statement: "No GitHub Actions job `id:` is renamed or removed; branch protection keys on job ids."
      status: resolved
      verification: test
---

<objective>
Remove the redundant second definition of "which contract tests matter" and the ci.all steps that re-run assertions `verify.test` already ran (STRUCT-06, D-20..D-25). Replace the hand-listed alias with a run-time derivation in the bump rehearsal, and guard the result with a runtime alias-tree contract test.

Purpose: the hand-listed `verify.doc_contract` alias had already drifted (21 listed against 33 real doc-contract files). A second list that silently drifts is worse than none.

Output: edited mix.exs aliases, one new guard test, updated CI topology/parity tests, ci.yml step deletion, a derived-list rehearsal script, and truthful docs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-RESEARCH.md
@.planning/phases/204-structure/204-PATTERNS.md
@CLAUDE.md
</context>

<assumption_delta_decision>
noun: the set of contract tests that run
definition: defined solely by `mix test` file discovery (every `*_test.exs` under `test/`), which `verify.test` executes
decision: no-change
rationale: the phase deletes a redundant hand-listed second definition (`verify.doc_contract`) and de-duplicates toward that single primary; no second case is introduced.
</assumption_delta_decision>

## Rules for this plan

- Record the plan base first: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-02-base"`; copy the SHA into the SUMMARY.
- Commit types: `refactor(204-02)` for mix.exs/CI/script changes (not releasable), `test(204-02)` for the guard, `docs(204-02)` for prose. Never `fix`/`feat`. Never `git add .planning/`.
- Re-grep every line number below before editing; they are indicative (D-25).
- HALT clause: if the bump rehearsal is red at NEXT, that is a real born-red defect (D-23). Fix the defect on its merits in a separate commit and record it. Do not shrink the derived list, lower the floor, restore `verify.doc_contract`, or exclude a file to make it pass. If the defect cannot be fixed within this plan, stop and report.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — delete the duplicate alias and ci.all re-runs, guard the alias tree, drop the CI step (one commit)</name>
  <reversibility rating="reversible">`verify.doc_contract` is maintainer/evaluator prose only, not adopter API, so restoring it is a one-commit revert (D-20 rating).</reversibility>
  <files>mix.exs, test/threadline/ci_all_dedup_contract_test.exs, test/threadline/ci_topology_contract_test.exs, test/threadline/ci_workflow_parity_contract_test.exs, .github/workflows/ci.yml</files>
  <read_first>
    - mix.exs (lines 1-30 preferred envs; 122-215 aliases and ci.all)
    - test/threadline/ci_topology_contract_test.exs (lines 50-115 and 205-225)
    - test/threadline/ci_workflow_parity_contract_test.exs (lines 30-65)
    - test/threadline/public_surface_contract_test.exs (lines 395-405: runtime alias reader)
    - .github/workflows/ci.yml (lines 355-385 job verify-test; 860-882 bump rehearsal comments)
  </read_first>
  <action>
    Per D-20, D-21, D-22, D-24 and Pitfall 9 (all in ONE commit, because the topology tests raise on a missing step):

    (1) mix.exs: delete the `"verify.doc_contract"` preferred-env entry (~:13), the alias (~:129-131), and its `ci.all` entry; delete the `verify.critic_trust` and `verify.mechanical` entries (and their comment lines) from `ci.all` only. Keep both aliases and rewrite their comments so they no longer say "folded into ci.all": say they are focused maintainer/CI-job commands whose test files already run in `verify.test`.

    (2) Create `test/threadline/ci_all_dedup_contract_test.exs`, module `Threadline.CiAllDedupContractTest`, `async: true`. Read aliases with `Threadline.MixProject.project()[:aliases]`. Write a pure `expand(step, aliases, seen)` helper: a string step whose first word names an alias key recurses into that alias; `cmd env … mix TASKNAME` steps recurse on TASKNAME; function captures are opaque leaves; a cycle raises. Tests (a)-(d) from D-24, each failure message listing the offending alias chain. Add a self-test that feeds a synthetic alias keyword (ci.all with two test-expanding steps; an alias `"x": ["test a_test.exs b_test.exs"]`) and asserts both are rejected. DECOUPLE-01 self-refute.

    (3) ci_topology_contract_test: remove the `verify.doc_contract` content assertions (~:60-79), the `pos_verify_doc_contract` match and comparisons (keep the rest of the ordering chain: verify.example before the browser step), and the ci.yml `Doc contract tests` / `run: mix verify.doc_contract` assertions (~:218-219); add `refute String.contains?(yaml, "mix verify.doc_contract")`. ci_workflow_parity_contract_test: remove `verify.doc_contract` from the step list and the ordering assert; update the message to "verify.test before verify.threadline before verify.example".

    (4) ci.yml: delete the `Doc contract tests` step (~:379-381) inside job `verify-test`, step only. Rewrite the comments at ~:869/:876 to say the rehearsal runs the doc-contract files it derives by filename plus `verify.release`.

    Commit: `refactor(204-02): delete the hand-listed doc-contract alias and ci.all re-runs`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/ci_all_dedup_contract_test.exs test/threadline/ci_topology_contract_test.exs test/threadline/ci_workflow_parity_contract_test.exs test/threadline/ci_coverage_doc_contract_test.exs</automated>
    <fails_when>non-zero exit, a MatchError from `:binary.match`, or any failure in the ExUnit summary</fails_when>
    <automated>D=$(git diff "$(cat "$(git rev-parse --git-dir)/gsd-204-02-base")" HEAD -- .github/workflows/ci.yml) || exit 1; test -n "$D" && ! printf '%s\n' "$D" | grep -E '^[-+][[:space:]]+[a-z0-9_-]+:[[:space:]]*$' | grep -qvE '^[-+][[:space:]]+(with|env|steps):'</automated>
    <fails_when>non-zero exit: the diff is empty (step not deleted), the git diff failed, or an added/removed bare `key:` line shows a job id or job block changed instead of a single step</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'verify.doc_contract' mix.exs` prints 0
    - `grep -c '"verify.critic_trust": \["test' mix.exs` prints 1 and `grep -c '"verify.mechanical": \["test' mix.exs` prints 1
    - `grep -c 'Doc contract tests' .github/workflows/ci.yml` prints 0
    - `grep -c 'Mix.Project.config\|MixProject.project' test/threadline/ci_all_dedup_contract_test.exs` is at least 1 and `grep -c 'Regex.run(~r/"ci' test/threadline/ci_all_dedup_contract_test.exs` prints 0
  </acceptance_criteria>
  <done>ci.all has one test-running step; the alias tree is guarded at runtime; CI no longer runs a hand-listed subset.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the bump-rehearsal subset by filename with a 30-file floor, and run it</name>
  <files>bin/verify-bump-rehearsal</files>
  <read_first>
    - bin/verify-bump-rehearsal (lines 85-140 for `set -euo pipefail` and `tracked_blast_radius`; 360-425 gates and summary)
    - mix.exs `verify_bump_rehearsal/1` (how the alias shells the script and its preferred env)
  </read_first>
  <action>
    Per D-23 and Pitfall 8. Replace the `run_gate "mix verify.doc_contract at $NEXT" mix verify.doc_contract` line with a gate that, inside `$CLONE` (wrap the listing in a function invoked through `run_gate`, or `cd "$CLONE"` in a subshell as `run_gate` does), builds the list with `find test \( -name '*doc_contract_test.exs' -o -name '*readme_contract_test.exs' \) | sort` read through a `while IFS= read -r` loop into a bash array (no `mapfile`, for bash 3.2), fails with a message naming the count when fewer than 30 files are found, and then runs `mix test "${DC_FILES[@]}"`. Label it "doc-contract tests (derived by filename) at $NEXT". Update the fail-fast comment above the gates and the OK summary line (~:419) to name the derived set instead of `mix verify.doc_contract`. Commit: `refactor(204-02): derive the bump-rehearsal doc-contract set by filename`.

    Then run `mix verify.bump_rehearsal` locally. If a formerly-drifted file (audit_indexing, ci_coverage, incident_playbook, integrations/sigra, operator_surface/{coverage, exports, policy_show, theme, timeline_browse}, performance, stg, support_playbook) is red at NEXT, apply the HALT clause: fix the version-pinned assertion on its merits in its own `test(204-02)` commit, and record each such defect in the SUMMARY.
  </action>
  <verify>
    <automated>bash -n bin/verify-bump-rehearsal && ! grep -q 'mapfile' bin/verify-bump-rehearsal && grep -q "doc_contract_test.exs" bin/verify-bump-rehearsal && grep -qE -- '-lt 30|< 30' bin/verify-bump-rehearsal</automated>
    <fails_when>non-zero exit: a syntax error, `mapfile` present, the find pattern missing, or no 30-file floor</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.bump_rehearsal > /tmp/p204-02-rehearsal.log 2>&1; grep -q 'Bump rehearsal OK' /tmp/p204-02-rehearsal.log</automated>
    <fails_when>the log lacks "Bump rehearsal OK" (it prints "Bump rehearsal FAIL" and the failing gate label instead)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'verify.doc_contract' bin/verify-bump-rehearsal` prints 0
    - `/tmp/p204-02-rehearsal.log` shows the derived gate label and a `mix test` run over at least 30 files (record the count in the SUMMARY)
    - the log contains "tree identity: MATCH"
  </acceptance_criteria>
  <done>The rehearsal runs every doc-contract file that exists at NEXT, cannot run an empty list, and is green locally.</done>
</task>

<task type="auto">
  <name>Task 3: Make every doc and doc-contract statement about ci.all true</name>
  <files>CONTRIBUTING.md, guides/configuration-and-commands.md, guides/evaluating-threadline.md, guides/adoption-pilot-backlog.md, .github/workflows/release.yml, test/threadline/adoption_pilot_doc_contract_test.exs, test/threadline/evaluating_threadline_doc_contract_test.exs, test/threadline/ci_coverage_doc_contract_test.exs, prompts/ARCHITECTURE-CODE-WALKTHROUGH-DNA.md, mix.exs</files>
  <read_first>
    - `grep -rn 'verify.doc_contract\|verify.critic_trust\|verify.mechanical' CONTRIBUTING.md guides README.md .github prompts test --include='*.md' --include='*.yml' --include='*.exs'` output (excluding CHANGELOG.md)
    - test/threadline/adoption_pilot_doc_contract_test.exs (~:92), test/threadline/evaluating_threadline_doc_contract_test.exs (~:42), test/threadline/ci_coverage_doc_contract_test.exs (:15-30)
  </read_first>
  <action>
    Per D-25. Rewrite each reference so it is true after Task 1, keeping every doc and its pinning test in the same commit:
    - CONTRIBUTING.md (~:154-155, :169, :303-304, :345, :397-398): critic_trust/mechanical prose becomes "in `mix verify.test` (and so in ci.all)"; (~:508, :682) remove or replace `mix verify.doc_contract` with `mix test` on the named file or `mix verify.test`.
    - guides/configuration-and-commands.md (~:116), guides/evaluating-threadline.md (~:38, :57, :58), guides/adoption-pilot-backlog.md (~:5, :133, "nine steps" → "eight steps" only if the counted list is ci.all's; recount it).
    - .github/workflows/release.yml (~:578): "Merge after CI (`mix verify.test`) is green."
    - mix.exs comments (~:146-161) that say a gate is "folded into ci.all".
    - adoption_pilot_doc_contract_test / evaluating_threadline_doc_contract_test: update the pinned phrases to the new prose; ci_coverage_doc_contract_test moduledoc (:21-23) to past tense.
    - prompts/ARCHITECTURE-CODE-WALKTHROUGH-DNA.md (~:394, :424): truthfulness only (ungated).
    Guides ship in the Hex package, so use `docs(204-02): …` (not releasable). CHANGELOG.md history is not edited.
  </action>
  <verify>
    <automated>G=$(git grep -n 'verify.doc_contract' -- . ':!CHANGELOG.md' ':!.planning'); test $? -eq 1 && test -z "$G"</automated>
    <fails_when>non-zero exit: git grep found a match (status 0) or errored (status 2); some tracked file outside CHANGELOG.md and .planning still names verify.doc_contract</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test $(find test \( -name '*doc_contract_test.exs' -o -name '*readme_contract_test.exs' \) | sort) test/threadline/ci_all_dedup_contract_test.exs test/threadline/ci_topology_contract_test.exs test/threadline/ci_workflow_parity_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary; fewer than 30 test files loaded (check the file count in the SUMMARY)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'folded into ci.all' mix.exs` prints 0
    - `grep -c 'mix verify.test' .github/workflows/release.yml` is at least 1
    - `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix ci.all` completes with exit 0 through the browser lane (record duration; the `CI=true` browser lane self-skips the 8 screenshot comparisons)
  </acceptance_criteria>
  <done>No tracked text outside CHANGELOG history claims a step that no longer exists, and every doc contract is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → CI gate | ci.yml steps decide what must be green before merge |
| release commit → publish | bump rehearsal is the pre-publish detector for version-pinned doc defects |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-04 | Tampering | ci.all topology | medium | mitigate | Runtime alias-tree guard (D-24) fails if a test-running step is added twice or a multi-path test alias reappears |
| T-204-05 | Denial of service | bin/verify-bump-rehearsal | medium | mitigate | 30-file floor prevents an empty list from making `mix test` run everything and pass vacuously |
| T-204-06 | Elevation of privilege | branch protection | high | mitigate | Only a step is deleted; job ids unchanged, checked by the ci.yml key-line diff in Task 1 |
</threat_model>

<verification>
- `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures.
- `mix verify.bump_rehearsal` → "Bump rehearsal OK".
- `git grep -n 'verify.doc_contract' -- . ':!CHANGELOG.md' ':!.planning'` → no output.
</verification>

<success_criteria>
ci.all contains no step that re-runs assertions another step already ran, and no second, drift-prone definition of which contract tests matter (ROADMAP SC 4).
</success_criteria>

## Artifacts this phase produces (this plan)

- `test/threadline/ci_all_dedup_contract_test.exs` — module `Threadline.CiAllDedupContractTest`; private `expand/3` alias-tree expander
- `bin/verify-bump-rehearsal` — derived-list gate (`DC_FILES` array, 30-file floor)
- Removed: Mix alias `verify.doc_contract`, its preferred-env entry, ci.all entries for `verify.doc_contract`/`verify.critic_trust`/`verify.mechanical`, ci.yml step `Doc contract tests`

<output>
Create `.planning/phases/204-structure/204-02-SUMMARY.md` when done, including the derived doc-contract count and any born-red defects fixed.
</output>
