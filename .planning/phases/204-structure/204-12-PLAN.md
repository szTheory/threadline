---
phase: 204-structure
plan: 12
type: execute
wave: 10
depends_on: ["204-11"]
files_modified:
  - test/support/operator_surface_case.ex
  - test/threadline/operator_surface/controllers/export_controller_test.exs
  - test/threadline/operator_surface/exports_mix_parity_test.exs
  - test/threadline/operator_surface/breadcrumb_test.exs
  - test/threadline/operator_surface/card_nesting_regression_test.exs
  - test/threadline/operator_surface/copy_contract_test.exs
  - test/threadline/operator_surface/gating_test.exs
  - test/threadline/operator_surface/rendered_output_contract_test.exs
  - test/threadline/operator_surface/skip_link_test.exs
  - test/threadline/operator_surface/transaction_live_test.exs
  - test/threadline/test_structure_contract_test.exs
autonomous: true
requirements: [STRUCT-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "export_controller_test's four endpoints use `use Threadline.OperatorSurfaceTest.Endpoint, router: …, parsers: [:urlencoded, :json]` and its routers `accepts: [\"csv\", \"json\"]` (or the exact list the file uses today) (D-17)."
    - "exports_mix_parity_test no longer loads or starts `ExportControllerTest.Endpoint`; it defines its own endpoint/router through the templates and starts it with `start_endpoint!/2` (D-17 cross-file coupling fix)."
    - "gating_test's `ExportsDisabledEndpoint` uses `parsers: false`, keeping it free of `Plug.Parsers` exactly as today (RESEARCH variant 3)."
    - "breadcrumb, card_nesting_regression, copy_contract, gating, rendered_output_contract, skip_link, and transaction_live tests use the shared templates; the three `<title>` variants collapse to the shared \"Test\" title, confirmed by the golden/contract tests staying green (D-17)."
    - "`test/threadline/test_structure_contract_test.exs` (async, scans `test/**/*.{ex,exs}`) asserts: `use Phoenix.Endpoint` appears only in `test/support/operator_surface_case.ex`; `use Phoenix.Router` appears only there or in `@allowlist %{path => reason}`; every allowlisted path exists and still contains `use Phoenix.Router` (stale entries fail); the scan set is non-empty (D-19)."
    - "`@allowlist` holds exactly the documented exceptions with reasons: `test/threadline/operator_surface/router_test.exs` (throwaway `Code.compile_quoted` routers are the subject under test), `test/threadline/operator_surface/stress_router_test.exs` (different macro `threadline_operator_surface_stress` and `live_session :threadline_stress`, compiles throwaway routers), `test/support/stress_router_prod_compile.exs` (runs under `MIX_ENV=prod mix run --no-start`, cannot depend on test/support), plus any file 204-11 recorded under its HALT clause (D-18)."
    - "stress_router_test keeps its own endpoint(s); the Endpoint rule therefore allowlists it too, with the same reason (D-18)."
  artifacts:
    - test/threadline/test_structure_contract_test.exs
  key_links:
    - "test_structure_contract_test → every `test/**/*.{ex,exs}` file; a new hand-rolled Endpoint/Router fails the suite unless allowlisted with a reason."
  prohibitions:
    - statement: "No export-authorization or feature-gating assertion is removed or weakened by the migration; gating and export tests keep every case."
      status: resolved
      verification: test
---

<objective>
Finish STRUCT-05: migrate the export-controller, gating, and page-level test files onto the shared templates, decouple exports_mix_parity from another file's endpoint, and guard the result with a structure contract (D-17..D-19).

Purpose: without the contract, the next contributor copy-pastes a hand-rolled endpoint and the duplication returns.

Output: eight more migrated test files, one decoupled parity test, and `test_structure_contract_test.exs` with a reasoned allowlist.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-PATTERNS.md
@.planning/phases/204-structure/204-11-SUMMARY.md
@CLAUDE.md
</context>

## Rules for this plan

- Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-12-base"`; record each file's test count before migrating it (same command as 204-11); after migration: identical count, 0 failures.
- `test(204-12)`, one test file per commit. Keep module names unique per file, keep `async:` flags, keep router helper functions in their router modules.
- HALT clause: never delete, skip, or loosen a test; never edit a golden or rendered-output fixture to absorb a title change (if the shared title changes a fixture-backed assertion, keep that file's own Layouts and allowlist it with the reason); never bypass hooks; never `git add .planning/`.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — export controller variant end-to-end and exports_mix_parity decoupled</name>
  <reversibility rating="reversible">Test-only restructuring (D-16).</reversibility>
  <files>test/support/operator_surface_case.ex, test/threadline/operator_surface/controllers/export_controller_test.exs, test/threadline/operator_surface/exports_mix_parity_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/controllers/export_controller_test.exs (the module definitions and setup_all at the top, ~:1-140)
    - test/threadline/operator_surface/exports_mix_parity_test.exs (lines 1-60)
    - test/support/operator_surface_case.ex
  </read_first>
  <action>
    Per D-16/D-17. Migrate export_controller_test's four endpoints and routers (`parsers: [:urlencoded, :json]`; `json_decoder: Jason` passed explicitly if the template default differs; per-endpoint salts may become the template constant because no test asserts them — confirm with grep before relying on it; router `accepts:` as the file has it). In exports_mix_parity_test, remove the require/borrow of ExportControllerTest modules and define `Threadline.OperatorSurface.ExportsMixParityTest.{Router, Endpoint}` via the templates with the mount the parity test needs, started with `start_endpoint!/2`; drop the `{:error, {:already_started, _}}` tolerance. If the template needs a small option to support these (for example a `layouts:` override), add it to test/support in this commit. Commit per file.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/controllers/export_controller_test.exs test/threadline/operator_surface/exports_mix_parity_test.exs</automated>
    <fails_when>non-zero exit, any failure in the ExUnit summary, or a test count different from the pre-migration counts</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/exports_mix_parity_test.exs --seed 0</automated>
    <fails_when>non-zero exit or any failure: the parity test must pass when run alone, proving it no longer depends on ExportControllerTest's endpoint</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ExportControllerTest' test/threadline/operator_surface/exports_mix_parity_test.exs` prints 0
    - `grep -c 'use Phoenix.Endpoint' test/threadline/operator_surface/controllers/export_controller_test.exs` prints 0
    - `grep -c 'parsers: \[:urlencoded, :json\]' test/threadline/operator_surface/controllers/export_controller_test.exs` is at least 1
  </acceptance_criteria>
  <done>The export variant runs on the templates and the parity test stands alone.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate breadcrumb, card_nesting_regression, copy_contract, gating, rendered_output_contract, skip_link, transaction_live tests</name>
  <files>test/threadline/operator_surface/breadcrumb_test.exs, test/threadline/operator_surface/card_nesting_regression_test.exs, test/threadline/operator_surface/copy_contract_test.exs, test/threadline/operator_surface/gating_test.exs, test/threadline/operator_surface/rendered_output_contract_test.exs, test/threadline/operator_surface/skip_link_test.exs, test/threadline/operator_surface/transaction_live_test.exs</files>
  <read_first>
    - the module-definition and setup section at the top of each of the seven files (only that section)
  </read_first>
  <action>Per D-16/D-17, migrate each file, one commit per file. gating_test's `ExportsDisabledEndpoint` uses `parsers: false`. rendered_output_contract_test and copy_contract_test drop their custom `<title>` layouts for the shared one; if any assertion or fixture depends on the old title, apply the HALT clause for that file. Do not touch the `# Structural debt:` sites in rendered_output_contract_test or card_nesting_regression_test here (204-13 drains them).</action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/breadcrumb_test.exs test/threadline/operator_surface/card_nesting_regression_test.exs test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/gating_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/skip_link_test.exs test/threadline/operator_surface/transaction_live_test.exs</automated>
    <fails_when>non-zero exit, any failure in the ExUnit summary, or a total test count different from the pre-migration sum</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -l 'use Phoenix.Endpoint' test/threadline/operator_surface/breadcrumb_test.exs test/threadline/operator_surface/card_nesting_regression_test.exs test/threadline/operator_surface/copy_contract_test.exs test/threadline/operator_surface/gating_test.exs test/threadline/operator_surface/rendered_output_contract_test.exs test/threadline/operator_surface/skip_link_test.exs test/threadline/operator_surface/transaction_live_test.exs` prints nothing (unless a HALT-recorded file kept its own, listed in the SUMMARY)
    - `grep -c 'parsers: false' test/threadline/operator_surface/gating_test.exs` prints 1
  </acceptance_criteria>
  <done>Every migratable operator-surface test file uses the shared templates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Structure contract with a reasoned, stale-checked allowlist</name>
  <files>test/threadline/test_structure_contract_test.exs</files>
  <read_first>
    - test/threadline/source_comment_location_contract_test.exs (whole-file shape: moduledoc, glob, non-empty guard, offenders, detector sanity, planning self-refute)
    - test/threadline/release_artifact_contract_test.exs (~:180-186: stale-path assertion message style)
  </read_first>
  <behavior>
    - A synthetic source list with `use Phoenix.Endpoint` in `test/threadline/foo_test.exs` is rejected; the same line in `test/support/operator_surface_case.ex` is accepted.
    - `use Phoenix.Router` in an allowlisted path is accepted; in any other path it is rejected.
    - An allowlist entry whose file lacks `use Phoenix.Router` (or does not exist) is rejected as stale.
    - An empty scan set is rejected.
  </behavior>
  <action>Per D-19, create `Threadline.TestStructureContractTest` (`async: true`). Matchers: `~r/^\s*use Phoenix\.Endpoint\b/m` and `~r/^\s*use Phoenix\.Router\b/m` over sorted `Path.wildcard("test/**/*.{ex,exs}")`. `@endpoint_allowlist` and `@router_allowlist` maps of path → reason, seeded with the D-18 entries (stress_router_test in both; router_test and stress_router_prod_compile.exs in the router map) and any 204-11/204-12 HALT entries. A pure `validate(files, endpoint_allowlist, router_allowlist)` returns `:ok | {:error, msg}` so the behavior list above runs on synthetic input; the real-tree test calls it on the live scan. Include the `"." <> "planning"` self-refute. Commit.</action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/test_structure_contract_test.exs</automated>
    <fails_when>non-zero exit, any failure in the ExUnit summary, or fewer than 5 tests ran</fails_when>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test</automated>
    <fails_when>non-zero exit or any failure in the full-suite ExUnit summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'router_test.exs' test/threadline/test_structure_contract_test.exs` is at least 1 and every allowlist value is a non-empty reason string
    - `grep -rlE '^\s*use Phoenix\.Endpoint' test | sort` lists only `test/support/operator_surface_case.ex`, `test/threadline/operator_surface/stress_router_test.exs`, and any HALT-recorded file
  </acceptance_criteria>
  <done>Hand-rolled test endpoints and routers can only exist with a written reason, and stale reasons fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → export/gating paths | tests prove exports are authorized and feature-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-30 | Elevation of privilege | gating/export test coverage | medium | mitigate | Per-file test counts compared; `parsers: false` keeps the gating endpoint identical |
| T-204-31 | Repudiation | structure allowlist | low | mitigate | Every allowlist entry carries a reason and is stale-checked |
</threat_model>

<verification>
- `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures with the expected total count; `MIX_ENV=test mix verify.compile_no_optional` and `mix credo --strict` → exit 0.
</verification>

<success_criteria>
Test files share endpoint and router templates from test/support except where a per-file difference is deliberate and documented (ROADMAP SC 3).
</success_criteria>

## Artifacts this phase produces (this plan)

- `test/threadline/test_structure_contract_test.exs` — module `Threadline.TestStructureContractTest`; attributes `@endpoint_allowlist`, `@router_allowlist`; pure `validate/3`
- `Threadline.OperatorSurface.ExportsMixParityTest.Router` / `.Endpoint` (per-file modules via templates)
- Any template options added to `test/support/operator_surface_case.ex` (recorded in the SUMMARY)

<output>
Create `.planning/phases/204-structure/204-12-SUMMARY.md` when done, with the final allowlist and per-file test counts.
</output>
