---
phase: 204-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/operator_surface/style_byte_lock_test.exs
  - test/fixtures/style/operator_surface.css
  - test/fixtures/style/README.md
  - test/threadline/source_size_contract_test.exs
  - test/support/source_family.ex
  - test/threadline/source_family_test.exs
autonomous: true
requirements: [STRUCT-01, STRUCT-03, STRUCT-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The rendered operator CSS (`Style.css(%{__changed__: nil})` → `Phoenix.HTML.Safe.to_iodata/1` → binary) is pinned by a committed golden file `test/fixtures/style/operator_surface.css` plus two sha256 pins (`@golden_sha256` over the golden file, `@rendered_sha256` over the full output including the fonts prefix), measured at execution time, not copied from research (D-01)."
    - "The lock lives in `verify.test` (plain `*_test.exs`), so it runs inside `ci.all` on both lanes with no new Mix alias (D-01, STRUCT-06 alignment)."
    - "The lock lands before any refactor commit of this phase, and the size gate lands second, seeded at measured values so it starts green (D-26 ordering constraint, D-07)."
    - "`test/threadline/source_size_contract_test.exs` fails for any `lib/**/*.{ex,css}` file over 800 lines and any `def/defp/defmacro/defmacrop` clause over 120 lines (per clause, max per name/arity, never summed) unless an exact exception entry exists; it also fails on a stale exception (measured value differs from the pin) (D-07, D-14)."
    - "The same gate holds a banner register: every `#` comment matching `^\\s*#\\s*(-{3,}|={3,}|─{3,}|\\*{3,})` in `lib/**/*.ex` is counted per file and must equal `@banner_exceptions` exactly; the map drains to `%{}` by phase end (D-11, STRUCT-04)."
    - "The gate refutes any `lib/**/*.heex` file and any `embed_templates` call in `lib/**/*.ex` (D-08 ban)."
    - "Adjacency edge (STRUCT-01): prefix-strip proves `\"<style>\" <> Fonts.face_css() <> \"</style>\" <> golden == full output` byte for byte, so no newline is merged or dropped at the fonts/style join."
    - "Empty edge (STRUCT-01): a missing or empty golden file fails the lock with a message naming the file; `Application.get_env(:threadline, :operator_surface_embed_fonts, true) == true` is asserted so the lock is never computed against the fonts-off variant."
    - "Ordering edge (STRUCT-01): two renders in the same VM produce identical bytes (asserted), so the pin is deterministic."
    - "Concurrency edge (STRUCT-03): the size/banner gate and the byte lock are read-only `async: true` scans that never write a file and never call `Application.put_env`, so parallel test execution cannot change their verdict."
    - "`Threadline.Test.SourceFamily.read!/1` returns a file's bytes followed by every `*.ex`/`*.css` file under its sibling directory in sorted order, tolerates a missing parent when siblings exist, and raises when nothing is found (Pattern 3 in RESEARCH)."
  artifacts:
    - test/threadline/operator_surface/style_byte_lock_test.exs
    - test/fixtures/style/operator_surface.css
    - test/fixtures/style/README.md
    - test/threadline/source_size_contract_test.exs
    - test/support/source_family.ex
  key_links:
    - "style_byte_lock_test → `Threadline.OperatorSurface.Style.css/1` + `Threadline.OperatorSurface.Fonts.face_css/0`; if either is renamed the lock fails loudly rather than passing vacuously."
    - "source_size_contract_test `@file_exceptions` / `@function_exceptions` / `@banner_exceptions` → every later plan in this phase edits these maps in the same commit as the extraction that changes a measured value."
  prohibitions:
    - statement: "The CSS golden file and both sha256 pins are never regenerated during this phase to match changed output; a lock failure means the refactor changed bytes and must be reverted."
      status: resolved
      verification: test
    - statement: "No size exception is added beyond the measured seed set, and no exception reason names a planning artifact, phase number, or requirement ID."
      status: resolved
      verification: judgment
---

<objective>
Land the two gates every later refactor in Phase 204 is judged by, in the order D-26 requires: first the CSS byte lock (STRUCT-01, D-01), then the source size/banner/heex gate seeded at today's measured values (STRUCT-03/04 enforcement, D-07, D-11, D-14). Also land the shared `Threadline.Test.SourceFamily` reader that later extraction commits use to repoint source-text pins.

Purpose: "zero output change" must be proven, not asserted (D-00b). Without the lock, the style split cannot show byte equality. Without the size gate, STRUCT-03/04 have no executable definition of done.

Output: one byte-lock contract test with its golden fixture and README, one size/banner contract test, one test-support reader with a small unit test. No `lib/` file changes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-RESEARCH.md
@.planning/phases/204-structure/204-PATTERNS.md
@CLAUDE.md
</context>

## Rules for this plan (apply to every task)

- Before the first commit, record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-01-base"` and copy the SHA into the SUMMARY. All range checks below diff against that file, never a relative `HEAD~N`.
- D-00: commit type `test` for new gates (never `feat`/`fix`, which release-please would publish). Never `git add .planning/`; stage explicit paths only. No `Phase \d+`, `STRUCT-0x`, or `D-NN` text in any file this plan creates (test files included, to keep the reason strings adopter-neutral).
- Every new test file includes the DECOUPLE-01 self-check: build `"." <> "planning"` and `refute File.read!(__ENV__.file) =~ planning_directory`.
- HALT clause: if a gate cannot be made green honestly against the current tree (for example the rendered hash is not stable across two renders), stop and report. Do not edit `lib/`, do not weaken an assertion, do not skip a test, do not bypass hooks.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — CSS byte lock end-to-end (render → golden → sha pins → verify.test)</name>
  <reversibility rating="reversible">A test file plus a fixture; deleting them restores the prior state with no other coupling (D-01 rating).</reversibility>
  <files>test/threadline/operator_surface/style_byte_lock_test.exs, test/fixtures/style/operator_surface.css, test/fixtures/style/README.md</files>
  <read_first>
    - lib/threadline/operator_surface/style.ex (lines 1-20 and the last 20 lines only: `css/1` head, `{@fonts_html}<style>`, `font_face_style/0`)
    - lib/threadline/operator_surface/fonts.ex (`face_css/0`, the `operator_surface_embed_fonts` branch)
    - test/threadline/dialyzer_ignore_contract_test.exs (lines 1-45 and the `sha256/1` helper near line 315: pin shape, fixture dir, README path)
    - test/fixtures/dialyzer/README.md (README tone and "Updating" section shape)
    - test/threadline/credo_config_contract_test.exs (lines 355-410 and 478-508: demand!/fail! reporter, synthetic self-test)
  </read_first>
  <action>
    Per D-01 and D-26 (lock lands first):

    (1) Generate the golden at execution time. With `MIX_ENV=test` and the ASDF env prefix, run a `mix run --no-start -e` one-liner that renders `Threadline.OperatorSurface.Style.css(%{__changed__: nil})` through `Phoenix.HTML.Safe.to_iodata/1` and `IO.iodata_to_binary/1`, builds `prefix = "<style>" <> Threadline.OperatorSurface.Fonts.face_css() <> "</style>"`, asserts `String.starts_with?(full, prefix)`, and writes `binary_part(full, byte_size(prefix), byte_size(full) - byte_size(prefix))` to `test/fixtures/style/operator_surface.css`. Print `byte_size(full)`, `sha256(full)`, and `sha256(suffix)` (lowercase hex). Research measured 212,633 bytes / `b10d6a2c…` full and 119,508 bytes / `c7baf51e…` suffix; commit what you measure, and record both measured values in the SUMMARY. If they differ from research, note it; do not stop.

    (2) Create `test/threadline/operator_surface/style_byte_lock_test.exs`, module `Threadline.OperatorSurface.StyleByteLockTest`, wrapped entirely in `if Code.ensure_loaded?(Phoenix.LiveView) do … end`, `use ExUnit.Case, async: true`. Attributes: `@root Path.expand("../../..", __DIR__)`, `@golden_path` (the fixture), `@golden_sha256`, `@rendered_sha256`. Tests:
    - "fonts are embedded": `Application.get_env(:threadline, :operator_surface_embed_fonts, true) == true`.
    - "golden file is present and non-empty": `File.regular?/1` and `byte_size > 0`, message names the path.
    - "golden hash pin": `sha256(File.read!(@golden_path)) == @golden_sha256`.
    - "rendered output equals prefix plus golden": render, prefix-strip (never toggle app env), compare suffix with golden through a private `diff_report(expected, actual)` that returns `:same` or a message with both byte sizes, the 1-based first differing line with expected and actual lines `inspect`ed, the byte offset of the first difference, the new sha256, and the regeneration one-liner from the README. Use `flunk(message)` on mismatch; never `assert a == b` on the ~100 KB binaries.
    - "full rendered hash pin": `sha256(full) == @rendered_sha256`.
    - "render is deterministic": two renders are byte-identical.
    - "diff reporter self-test": feed `diff_report/2` two synthetic multi-line binaries that differ on line 3; assert the message names line 3 and the offset, and that its length is under 2,000 bytes even when the synthetic inputs are 200 KB.
    - DECOUPLE-01 self-refute.
    The test never writes any file.

    (3) Create `test/fixtures/style/README.md`: one provenance paragraph (what the golden is, where the lock test lives, that it is the style-owned suffix after the embedded font-face block) and an "Updating" section: intentional CSS changes only; regenerate with the exact `MIX_ENV=test mix run --no-start -e …` one-liner from step (1); paste both new hashes into the test; commit golden and pins together with a why. No planning vocabulary.

    (4) Confirm the new file is picked up by bare `mix test` (no `test_helper.exs` exclusion applies). Commit: `test(204-01): lock the rendered operator CSS behind a golden file and sha256 pins`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/operator_surface/style_byte_lock_test.exs</automated>
    <fails_when>non-zero exit, or any failure in the ExUnit summary, or fewer than 7 tests ran ("0 tests" means the Phoenix.LiveView guard compiled the module away)</fails_when>
    <automated>test -s test/fixtures/style/operator_surface.css && head -c 40 test/fixtures/style/operator_surface.css | grep -q '^<style>' && tail -c 9 test/fixtures/style/operator_surface.css | grep -q '</style>'</automated>
    <fails_when>non-zero exit: the golden is missing, empty, or does not start with `<style>` / end with `</style>`, meaning the prefix strip cut at the wrong offset</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@golden_sha256 "[0-9a-f]\{64\}"' test/threadline/operator_surface/style_byte_lock_test.exs` prints 1 and `grep -c '@rendered_sha256 "[0-9a-f]\{64\}"'` prints 1
    - `grep -c 'put_env' test/threadline/operator_surface/style_byte_lock_test.exs` prints 0
    - `grep -c 'File.write' test/threadline/operator_surface/style_byte_lock_test.exs` prints 0
    - `grep -cE 'Phase [0-9]|STRUCT-0|D-[0-9]{2}' test/fixtures/style/README.md test/threadline/operator_surface/style_byte_lock_test.exs` prints 0 for each file
    - `git diff --stat "$(cat "$(git rev-parse --git-dir)/gsd-204-01-base")" HEAD -- lib/` prints nothing (no lib change)
  </acceptance_criteria>
  <done>The lock is green on the untouched tree, deterministic, and part of `mix test`; its failure message is bounded and names the regeneration procedure.</done>
</task>

<task type="auto">
  <name>Task 2: Source size, function-length, banner, and heex gate seeded at measured values</name>
  <files>test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - test/threadline/credo_config_contract_test.exs (lines 30-45 register attributes; 91-115 planted self-test; 355-410 scanner and validator; 478-508 helpers)
    - test/threadline/source_comment_location_contract_test.exs (lines 19-71: comment-only scan via `Code.string_to_quoted_with_comments/2`, non-empty guard, detector sanity test)
    - .planning/phases/204-structure/204-RESEARCH.md §"Size Inventory" and §"Banner Inventory" (indicative numbers only; re-measure)
  </read_first>
  <action>
    Per D-07, D-11, D-12, D-14 (and the RESEARCH §Banner Inventory gap: STRUCT-04 needs enforcement):

    Create `test/threadline/source_size_contract_test.exs`, module `Threadline.SourceSizeContractTest`, `use ExUnit.Case, async: true`, `@root Path.expand("../..", __DIR__)`. Structure it like credo_config_contract_test: pure validators returning `:ok | {:error, message}` via `throw({:contract_error, msg})`, `demand!/2`, `fail!/1`, and real-tree tests that call the validators on the live scan.

    (1) File limit. `@file_limit 800`, scan `lib/**/*.{ex,css}` (sorted). Line count = number of `"\n"` bytes (matches `wc -l`). `@file_exceptions %{path => {exact_lines, reason}}`. Validation: scan set non-empty ("would pass vacuously" message); every file over the limit has an entry; every entry's path exists; every entry's measured count equals its pinned count exactly (lower → "stale: lower or delete the pin"; higher → "grew: split it, do not raise the pin"); an entry for a file at or under the limit fails as stale.

    (2) Function limit. `@function_limit 120`, scan `lib/**/*.ex`. Parse with `Code.string_to_quoted(src, token_metadata: true, columns: true, file: path)` and `Macro.prewalk` for `:def/:defp/:defmacro/:defmacrop` nodes; clause length = `meta[:end][:line] - meta[:line] + 1` when `meta[:end]` exists, else 1 (keyword `do:` form). Unwrap `{:when, _, [head | _]}`; a head with `nil` args is arity 0. Group by `{path, name, arity}` and take the max clause length (never sum clauses). `@function_exceptions %{{path, name, arity} => {exact_lines, reason}}` with the same exact/stale/missing rules.

    (3) Banner register. Regex `~r/^\s*#\s*(-{3,}|={3,}|─{3,}|\*{3,})/u` applied to comment text from `Code.string_to_quoted_with_comments/2` (string literals are ignored), over `lib/**/*.ex`. `@banner_exceptions %{path => exact_count}`; files with banners must have an exact entry; stale entries fail.

    (4) heex/embed ban. Refute any `lib/**/*.heex` file; refute `embed_templates` in any `lib/**/*.ex` source (D-08).

    (5) Seed the three maps with values measured by this gate on the current tree (run it once with empty maps, read its offender list, then pin). Expected shape (RESEARCH, re-measure): files style.ex, ui.ex, live/timeline_live.ex, live/stress_live.ex, stress_fixtures.ex, mechanical_checker.ex, query.ex; functions style `css/1`, stress_live `render/1`, export_status_live `render/1`, timeline_live `timeline_filter_drawer/1` `render/1` `timeline_command/1`, start_live, retention_history_live, transaction_live, actor_live, coverage_live `render/1`, governance/migration.ex `migration_content/0`; banners mechanical_checker 10, timeline_live 10, export_controller 9, start_live 6, actor_ref 3, coverage_live 3, query 1. Reason strings: `stress_fixtures.ex` gets "declarative fixture data tables; excluded from the Hex package (mix.exs exclude_patterns)" (D-12); every other entry gets "oversized; being split into cohesive modules" (temporary; each later plan removes its entries). Record the full measured seed in the SUMMARY.

    (6) Self-tests with synthetic `{path, source}` input: an 801-line file with no entry fails; an exact entry passes; a stale entry (pinned 900, measured 850) fails with "stale"; a 121-line `def` fails, a 3-clause function whose clauses total 150 but max 60 passes; a guarded head (`def f(x) when is_integer(x)`) is measured under name `f` arity 1; a `# ----` banner comment is counted but a string literal containing `# ----` is not; a `.heex` path in the synthetic list fails. Plus the DECOUPLE-01 self-refute.

    Commit: `test(204-01): gate lib file size, function length, separator banners, and heex templates`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/source_size_contract_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports any failure, or fewer than 8 tests ran</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict test/threadline/source_size_contract_test.exs</automated>
    <fails_when>non-zero exit, or credo prints any issue for the new file</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '"lib/threadline/operator_surface/stress_fixtures.ex" =>' test/threadline/source_size_contract_test.exs` prints 1 and that entry's reason contains `exclude_patterns`
    - `grep -c 'Path.wildcard' test/threadline/source_size_contract_test.exs` is at least 1 and the file contains a non-empty scan-set demand (grep for `pass vacuously` prints at least 1)
    - `grep -c 'embed_templates' test/threadline/source_size_contract_test.exs` is at least 1 and `grep -c 'heex' test/threadline/source_size_contract_test.exs` is at least 1
    - `git diff --stat "$(cat "$(git rev-parse --git-dir)/gsd-204-01-base")" HEAD -- lib/` prints nothing
  </acceptance_criteria>
  <done>The gate is green on the untouched tree with every oversized file, function, and banner pinned at its exact measured value, and its self-tests prove each failure direction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shared source-family reader for pin repointing</name>
  <files>test/support/source_family.ex, test/threadline/source_family_test.exs</files>
  <read_first>
    - test/support/operator_surface_fixtures.ex (lines 1-30: `Threadline.Test.*` naming, `!` helpers)
    - mix.exs line with `elixirc_paths(:test)` (test/support is compiled in :test and by `verify.compile_no_optional`)
  </read_first>
  <behavior>
    - `read!("lib/threadline/operator_surface/ui.ex")` today returns exactly `File.read!` of ui.ex (no sibling dir yet).
    - With a temp dir holding `a.ex` and `a/z.ex`, `a/b.css`, `a/c.ex`, `files!/1` returns `[a.ex, a/b.css, a/c.ex, a/z.ex]` (parent first, then siblings sorted).
    - With only `a/x.ex` present (parent deleted), `read!` returns the sibling bytes.
    - With neither present, `read!` raises with a message naming the path.
  </behavior>
  <action>
    Per RESEARCH Pattern 3 (generalising D-05's style reader). Create `Threadline.Test.SourceFamily` (`@moduledoc false`, no Phoenix dependency so no optional-dep guard needed) with `files!(path)` (parent if it exists, then `Path.wildcard(Path.rootname(path) <> "/**/*.{ex,css}")` sorted; raise if the list is empty) and `read!(path)` (concatenate `File.read!` of `files!/1` joined with `"\n"`). Paths are relative to the repo root; resolve with `Path.expand("../..", __DIR__)` so tests work regardless of cwd. Wildcards are fine here (the ordered-list rule applies only to `Style` segments in `lib/`). Write `test/threadline/source_family_test.exs` covering the behavior list using `System.tmp_dir!/0` fixtures (`async: true`, unique subdir per test, cleaned in `on_exit`). Commit: `test(204-01): add a source-family reader for split-module pins`.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/source_family_test.exs</automated>
    <fails_when>non-zero exit, or any failure in the ExUnit summary, or "0 tests"</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix verify.compile_no_optional</automated>
    <fails_when>non-zero exit, or any compiler warning printed for test/support/source_family.ex</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'defmodule Threadline.Test.SourceFamily' test/support/source_family.ex` prints 1
    - `grep -c '@moduledoc false' test/support/source_family.ex` prints 1
    - `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` full-suite summary reports 0 failures after all three commits (record the test count in the SUMMARY)
  </acceptance_criteria>
  <done>Later plans can repoint any source-text pin to "file plus extracted siblings" with one call, in the same commit as each extraction.</done>
</task>

</tasks>

## Flagged assumptions (edge-probe fallback, no silent drops)

- **STRUCT-04 edge `unclassified` (unresolved, flagged):** assumed that a "separator comment standing in for a boundary" means any `#` comment line in `lib/**/*.ex` matching `^\s*#\s*(-{3,}|={3,}|─{3,}|\*{3,})`. Prose section comments without rule characters are not banners and are not counted. If the maintainer defines banners more widely, the regex in Task 2 is the single place to widen.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → test gate | Gates read committed source only; no network, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-01 | Tampering | style_byte_lock_test pins | medium | mitigate | Golden + two independent sha256 pins; README bump procedure requires committing golden and pins together with a stated reason; test never writes fixtures |
| T-204-02 | Repudiation | source_size_contract_test exceptions | low | mitigate | Exact-equality pins with stale detection, so an exception cannot silently outlive its reason |
| T-204-03 | Information disclosure | new test files | low | accept | Test files are not in the Hex `files:` list; DECOUPLE-01 self-refute keeps planning paths out |
</threat_model>

<verification>
- `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures.
- `ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format` and `mix credo --strict` → exit 0.
- `git log --oneline "$(cat "$(git rev-parse --git-dir)/gsd-204-01-base")"..HEAD` shows three `test(204-01)` commits and `git diff --stat "$(cat "$(git rev-parse --git-dir)/gsd-204-01-base")" HEAD -- lib/ mix.exs` prints nothing.
</verification>

<success_criteria>
The CSS bytes and every oversized lib file, function, and banner are pinned by executable gates in `verify.test` before any refactor starts.
</success_criteria>

## Artifacts this phase produces (this plan)

- `test/threadline/operator_surface/style_byte_lock_test.exs` — module `Threadline.OperatorSurface.StyleByteLockTest`; attributes `@golden_sha256`, `@rendered_sha256`; private `diff_report/2`, `sha256/1`
- `test/fixtures/style/operator_surface.css` — golden style-owned CSS bytes
- `test/fixtures/style/README.md` — provenance + bump procedure
- `test/threadline/source_size_contract_test.exs` — module `Threadline.SourceSizeContractTest`; attributes `@file_limit 800`, `@function_limit 120`, `@file_exceptions`, `@function_exceptions`, `@banner_exceptions`
- `test/support/source_family.ex` — module `Threadline.Test.SourceFamily`; functions `files!/1`, `read!/1`
- `test/threadline/source_family_test.exs` — unit tests for the reader

<output>
Create `.planning/phases/204-structure/204-01-SUMMARY.md` when done, recording the measured rendered size and both hashes, and the full seeded exception maps.
</output>
