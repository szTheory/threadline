---
phase: 204-structure
plan: 04
type: execute
wave: 4
depends_on: ["204-02", "204-03"]
files_modified:
  - lib/threadline/operator_surface/mechanical_checker.ex
  - lib/threadline/operator_surface/mechanical_checker/scorecards.ex
  - lib/threadline/operator_surface/mechanical_checker/parsing.ex
  - mix.exs
  - test/threadline/release_artifact_contract_test.exs
  - test/threadline/operator_surface/mechanical_checker_test.exs
  - lib/threadline/query.ex
  - lib/threadline/query/cursors.ex
  - lib/threadline/query/filter_params.ex
  - lib/threadline/governance/migration.ex
  - test/threadline/storage_schema_migration_contract_test.exs
  - test/threadline/source_size_contract_test.exs
  - test/threadline/credo_config_contract_test.exs
  - test/threadline/public_surface_contract_test.exs
autonomous: true
requirements: [STRUCT-03, STRUCT-04, STRUCT-07]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`mechanical_checker.ex` is under 800 lines: scorecard loading/validation moves to `Threadline.OperatorSurface.MechanicalChecker.Scorecards` and shared parsing helpers to `MechanicalChecker.Parsing` (both `@moduledoc false`), `run/1` stays in the parent, and the MODE-A/B constants, scale lists, and the `linearize_channel` gamma-2.4 line stay textually in the parent where mechanical_checker_test pins them (D-10, RESEARCH size inventory)."
    - "`exclude_patterns` in mix.exs matches the whole `mechanical_checker` family (`~r{^lib/threadline/operator_surface/mechanical_checker(\\.ex$|/)}`) and release_artifact_contract_test lists `lib/threadline/operator_surface/mechanical_checker/` in `@maintainer_only_prefixes` and each new file in `@maintainer_only_paths`, in the same commit that creates the first sibling (Pitfall 4)."
    - "`query.ex` is under 800 lines by moving only private helpers (actor-history cursor helpers, timeline page-size/cursor validators, next-cursor) into `Threadline.Query.Cursors` (`@moduledoc false`); no public `Threadline.Query` function moves or changes signature; the `filter_by_*` group, the three `repo.preload(… storage_opts([], opts))` lines, and `@allowed_timeline_filter_keys` stay in query.ex (D-10, RESEARCH query.ex notes)."
    - "`Threadline.Governance.Migration.migration_content/0` output is pinned by sha256 for the default and `AuditLog` storage schemas before it is split into ordered helper functions, and the pins stay green after the split (resolved open question 1; D-12 exactly-one-exception rule)."
    - "Banners in mechanical_checker.ex (10) and query.ex (1) are gone: sections became module boundaries, the query `filter_by_*` banner is deleted as a cohesive clause group, and the three constant-frame banners become plain prose comments without rule characters (D-11)."
    - "Structural register sites in mechanical_checker.ex (1), query.ex (1), and query/filter_params.ex (2) are fixed with early return, multi-clause matching, `with`, or an extracted private function; each site's `# Structural debt:` line goes with its disable, and the credo register counts drop in the same commit (D-13)."
    - "The size gate drops the mechanical_checker.ex and query.ex file exceptions, the migration_content/0 function exception, and both banner entries in the commits that make them stale (D-07)."
  artifacts:
    - lib/threadline/operator_surface/mechanical_checker/scorecards.ex
    - lib/threadline/operator_surface/mechanical_checker/parsing.ex
    - lib/threadline/query/cursors.ex
  key_links:
    - "mix.exs `exclude_patterns` → unpacked Hex tarball → release_artifact_contract_test `@maintainer_only_prefixes`; a sibling outside the pattern would publish maintainer tooling."
    - "`mix verify.mechanical` (ci.yml fast job) → mechanical_checker_test → `MechanicalChecker.run/1`, which now calls the siblings."
    - "guides/code-walkthrough.md → code_walkthrough_doc_contract_test pins `filter_by_correlation` text in lib/threadline/query.ex."
  prohibitions:
    - statement: "No public function of `Threadline.Query` or `Threadline.Governance.Migration` changes name, arity, return value, or moves module."
      status: resolved
      verification: test
    - statement: "Maintainer-only mechanical-checker code never enters the published Hex package."
      status: resolved
      verification: test
    - statement: "The generated governance migration an adopter writes with `mix threadline.install` is byte-identical before and after the split."
      status: resolved
      verification: test
---

<objective>
Retire three size problems whose code is not rendered: the maintainer-only `mechanical_checker.ex` (948 lines, 10 banners), the public `query.ex` (895 lines, 1 banner), and the 138-line `Governance.Migration.migration_content/0`. Drain the four register sites that live in those files (STRUCT-03, STRUCT-04, STRUCT-07; D-10, D-11, D-13).

Purpose: these extractions carry the phase's packaging risk (a sibling leaking into Hex) and its public-API risk (`Threadline.Query`). Doing them before the rendered-code work isolates those risks.

Output: three new internal modules, widened Hex exclusion with archive assertions, a migration-output pin, a flattened register, and a size gate with five fewer entries.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-structure/204-CONTEXT.md
@.planning/phases/204-structure/204-RESEARCH.md
@.planning/phases/204-structure/204-PATTERNS.md
@CLAUDE.md
</context>

## Rules for this plan

- Record the plan base: `git rev-parse HEAD > "$(git rev-parse --git-dir)/gsd-204-04-base"`.
- D-00: `refactor(204-04)` commits, one lib file per commit where practical (a new sibling plus the parent it was carved from is one commit). New modules: deliberate `@moduledoc false`, prose as plain `#` comments, no banners, no `file:line` citations, no `Phase \d+`/`STRUCT-0x`/`D-NN`. New call sites alias (AliasUsage stays at defaults).
- Every commit: `mix compile --force --warnings-as-errors`; `mix test test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs` plus every test that reads the touched file (`grep -rln '<path>' test examples/threadline_phoenix/test`); `mix credo --strict <touched files>`.
- Register procedure (D-13): locate each site with `grep -n 'Structural debt:' <file>`, read only the enclosing function, remove the `# Structural debt:` line and its disable together, restructure until `mix credo --strict <file>` reports no Nesting/CyclomaticComplexity issue, then lower the matching count in `@register` and `@ceiling` of credo_config_contract_test in the same commit. If a fix would change behavior, re-register that one site with its exact count and a named post-v1.41 successor instead (never bulk).
- HALT clause: if a pin (migration sha, mechanical_checker constants, walkthrough text) fails, the change altered output or moved pinned text: revert and restructure. Never edit a pin to match, never raise a size pin, never add a size exception, never bypass hooks. Never `git add .planning/`.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — carve MechanicalChecker.Scorecards and .Parsing behind a widened Hex exclusion</name>
  <reversibility rating="reversible">Maintainer-only internal modules; not packaged, no public API (D-10).</reversibility>
  <files>lib/threadline/operator_surface/mechanical_checker.ex, lib/threadline/operator_surface/mechanical_checker/scorecards.ex, lib/threadline/operator_surface/mechanical_checker/parsing.ex, mix.exs, test/threadline/release_artifact_contract_test.exs, test/threadline/operator_surface/mechanical_checker_test.exs, test/threadline/source_size_contract_test.exs, test/threadline/credo_config_contract_test.exs</files>
  <read_first>
    - lib/threadline/operator_surface/mechanical_checker.ex (lines 1-140 whole; then only the section heads at the banner lines found by `grep -nE '^\s*#\s*-{3,}'`)
    - test/threadline/operator_surface/mechanical_checker_test.exs (lines 1-75: text pins)
    - mix.exs `package/0` `exclude_patterns`
    - test/threadline/release_artifact_contract_test.exs (lines 150-200)
    - lib/threadline/query/filter_params.ex (lines 1-15: internal sibling header convention)
  </read_first>
  <action>
    Per D-10, D-11, D-13 and Pitfall 4:

    (1) First commit (packaging + first sibling): widen the mix.exs pattern to `~r{^lib/threadline/operator_surface/mechanical_checker(\.ex$|/)}`; add `"lib/threadline/operator_surface/mechanical_checker/"` to `@maintainer_only_prefixes` and the new file paths to `@maintainer_only_paths` in release_artifact_contract_test; create `lib/threadline/operator_surface/mechanical_checker/scorecards.ex` (`Threadline.OperatorSurface.MechanicalChecker.Scorecards`, `@moduledoc false`) holding the scorecard loading/validation section (~:132-431, re-measure). Functions it exposes to the parent become `def` (documented with `@doc false` only if Credo needs it); the parent aliases it. Remove that section's banner.

    (2) Second commit: create `mechanical_checker/parsing.ex` (`MechanicalChecker.Parsing`) with the shared parsing helpers (~:804-end); remove its banner. Keep `linearize_channel` and the luminance helper textually in the parent (the gamma line is pinned).

    (3) Third commit: remove the remaining banners. The three constant-frame banners (~:26, :33, :37) become plain prose comments with no rule characters (for example "MODE-A locked WCAG constants, pinned by the meta-test"); update the stale "SSOT: …/style.ex" comment to name `lib/threadline/operator_surface/style/01_tokens.css`. The per-check sections (contrast, token conformance, ratchet metrics, accent hue) either become their own sibling modules (`MechanicalChecker.Contrast`, etc.) or lose the rule banner because each is one cohesive group; pick per section and record why. Drain the register site in mechanical_checker.ex in this or its own commit.

    (4) Each commit updates source_size_contract_test (file pin lowered, then deleted once under 800; banner count lowered to deletion) and, for the register site, credo_config_contract_test counts. mechanical_checker_test text pins keep reading the parent; if any pin's text moved, repoint that read to `Threadline.Test.SourceFamily.read!/1` in the same commit.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.mechanical && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/release_artifact_contract_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs test/threadline/source_comment_location_contract_test.exs</automated>
    <fails_when>non-zero exit from either command, or any failure in an ExUnit summary; a release-artifact failure naming `mechanical_checker/` means the exclusion did not cover the sibling</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix credo --strict lib/threadline/operator_surface/mechanical_checker.ex lib/threadline/operator_surface/mechanical_checker/</automated>
    <fails_when>non-zero exit or any credo issue listed</fails_when>
  </verify>
  <acceptance_criteria>
    - `wc -l < lib/threadline/operator_surface/mechanical_checker.ex` prints a number under 800
    - `grep -cE '^\s*#\s*(-{3,}|={3,})' lib/threadline/operator_surface/mechanical_checker.ex` prints 0 and the same grep over `lib/threadline/operator_surface/mechanical_checker/` prints 0 per file
    - `grep -c 'mechanical_checker(' mix.exs` prints 1 and `grep -c 'mechanical_checker/"' test/threadline/release_artifact_contract_test.exs` is at least 1
    - `grep -c 'Structural debt:' lib/threadline/operator_surface/mechanical_checker.ex` prints 0
  </acceptance_criteria>
  <done>The mechanical checker is legible, still green under `verify.mechanical`, and provably absent from the Hex archive.</done>
</task>

<task type="auto">
  <name>Task 2: Move query.ex private cursor/validation helpers into Threadline.Query.Cursors; drain query and filter_params register sites</name>
  <files>lib/threadline/query.ex, lib/threadline/query/cursors.ex, lib/threadline/query/filter_params.ex, test/threadline/source_size_contract_test.exs, test/threadline/credo_config_contract_test.exs, test/threadline/public_surface_contract_test.exs</files>
  <read_first>
    - lib/threadline/query.ex (lines 1-65, then 545-640 and 775-895 only)
    - test/threadline/code_walkthrough_doc_contract_test.exs (lines 15-30, 70-90)
    - test/threadline/query_test.exs (lines 610-625)
    - test/threadline/dialyzer_slice_contract_test.exs (lines 15-50: a recorded query.ex line coordinate)
    - test/threadline/public_surface_contract_test.exs (search for how `@moduledoc false` modules are enumerated, around line 630-650)
    - lib/threadline/query/filter_params.ex (the two register sites and their enclosing functions only)
  </read_first>
  <action>
    Per D-10, D-11, D-13 and RESEARCH "query.ex filter pipeline is pinned":

    (1) Create `lib/threadline/query/cursors.ex`, `Threadline.Query.Cursors`, `@moduledoc false`. Move only `defp` helpers: `actor_history_filter_from/…`, `actor_history_filter_to/…`, `actor_history_after_cursor`, `actor_history_before_cursor`, `validate_actor_history_cursor!`, `validate_timeline_page_size!`, `validate_timeline_cursor!`, `timeline_page_next_cursor` (re-measure the list; move whole clause groups). In the new module they become `def` with `@doc false` where Credo requires. Public `def`s of `Threadline.Query` (including `maybe_after_timeline_cursor`, `storage_opts`, `maybe_apply_scope`) do not move. Keep in query.ex: the `filter_by_*` group, the three `repo.preload(… storage_opts([], opts))` lines, `@allowed_timeline_filter_keys`, and everything above line ~550 (so the recorded Dialyzer coordinate near line 53 does not shift). Delete the single banner above `filter_by_*` (cohesive clause group, D-11). If public_surface_contract_test enumerates hidden modules, add `Threadline.Query.Cursors` there. Commit.

    (2) Drain the query.ex register site and the two filter_params.ex sites, one commit per file, following the register procedure. filter_params.ex parses URL filter input: keep every validation branch and error message identical; the existing query/filter tests are the proof.

    (3) Update source_size_contract_test (query.ex file pin removed; banner entry removed) and credo_config_contract_test counts in the commits that change them.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/query_test.exs test/threadline/code_walkthrough_doc_contract_test.exs test/threadline/public_surface_contract_test.exs test/threadline/operator_surface/exports_doc_contract_test.exs test/threadline/operator_surface/timeline_browse_doc_contract_test.exs test/threadline/dialyzer_slice_contract_test.exs test/threadline/layer_boundary_contract_test.exs test/threadline/source_size_contract_test.exs test/threadline/credo_config_contract_test.exs</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary (a missing file error means a listed path moved: re-derive with `ls test/threadline` and record the substitution)</fails_when>
    <automated>ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 sh -c 'mix xref graph --format cycles --label compile-connected --fail-above 0 && mix credo --strict lib/threadline/query.ex lib/threadline/query/'</automated>
    <fails_when>non-zero exit: a compile-connected cycle was introduced, or credo lists an issue</fails_when>
  </verify>
  <acceptance_criteria>
    - `wc -l < lib/threadline/query.ex` prints a number under 800
    - `grep -c 'defp filter_by_correlation' lib/threadline/query.ex` is at least 1 and `grep -c '@allowed_timeline_filter_keys' lib/threadline/query.ex` is at least 1
    - `D=$(git diff "$(cat "$(git rev-parse --git-dir)/gsd-204-04-base")" HEAD -- lib/threadline/query.ex) && ! printf '%s\n' "$D" | grep -qE '^-  def [a-z_!?]+'` exits 0 (no public def removed)
    - `grep -c 'Structural debt:' lib/threadline/query.ex lib/threadline/query/filter_params.ex` prints 0 for both
  </acceptance_criteria>
  <done>`Threadline.Query` keeps its entire public surface and walkthrough-pinned pipeline, is under 800 lines, and its register sites are drained.</done>
</task>

<task type="auto">
  <name>Task 3: Pin Governance.Migration.migration_content/0 output, then split it into ordered helpers</name>
  <files>test/threadline/storage_schema_migration_contract_test.exs, lib/threadline/governance/migration.ex, test/threadline/source_size_contract_test.exs</files>
  <read_first>
    - lib/threadline/governance/migration.ex (whole file, 145 lines)
    - test/threadline/storage_schema_migration_contract_test.exs (lines 1-70: `async: false`, env restore, `put_storage_schema!/1`)
  </read_first>
  <action>
    Per resolved open question 1 (pin first, then split; no second exception, D-12):

    (1) Commit `test(204-04): pin the generated governance migration output`: add a test to storage_schema_migration_contract_test that computes sha256 (lowercase hex) of `Threadline.Governance.Migration.migration_content()` under the default schema and under `put_storage_schema!("AuditLog")`, and asserts both equal module attributes `@governance_default_sha256` and `@governance_auditlog_sha256` measured now on the untouched source. On mismatch, flunk with both byte sizes and the first differing line, not the full text.

    (2) Commit `refactor(204-04): split the governance migration template into ordered parts`: keep `migration_content/0` public with the same return value; move the heredoc into ordered private helpers (for example the variable bindings, then per-table `up` fragments, then `down`), each clause at most 120 lines, joined in the original order. Watch heredoc dedent and trailing newlines at each join; the two pins decide. Remove the `{governance/migration.ex, :migration_content, 0}` function exception from source_size_contract_test in the same commit.
  </action>
  <verify>
    <automated>DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/threadline/storage_schema_migration_contract_test.exs test/threadline/source_size_contract_test.exs test/mix/tasks</automated>
    <fails_when>non-zero exit or any failure in the ExUnit summary; a sha mismatch on either pin means the generated migration changed</fails_when>
    <automated>D=$(git diff "$(cat "$(git rev-parse --git-dir)/gsd-204-04-base")" HEAD -- test/threadline/storage_schema_migration_contract_test.exs) && printf '%s\n' "$D" | grep -qE '^\+.*@governance_default_sha256 "[0-9a-f]{64}"' && ! printf '%s\n' "$D" | grep -qE '^-.*@governance_(default|auditlog)_sha256'</automated>
    <fails_when>non-zero exit: the pins were not added, or a pin value was edited after it was first committed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'governance/migration.ex' test/threadline/source_size_contract_test.exs` prints 0
    - `grep -c 'def migration_content do' lib/threadline/governance/migration.ex` prints 1
    - the pin commit precedes the split commit in `git log` (record both SHAs in the SUMMARY)
  </acceptance_criteria>
  <done>The adopter-visible generated migration is byte-pinned and its generator no longer needs a size exception.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → Hex package | `exclude_patterns` decide what adopters download |
| URL params → Query filters | filter_params parses operator-supplied filter input |
| library → adopter migration | `mix threadline.install` writes `migration_content/0` into the host app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-10 | Information disclosure | mechanical_checker siblings | high | mitigate | Directory-wide exclude pattern plus `@maintainer_only_prefixes` asserted against the unpacked tarball in the same commit as the first sibling |
| T-204-11 | Tampering | query/filter_params.ex validation | high | mitigate | Register refactor keeps every branch and error message; query and filter tests plus exports/timeline doc contracts must stay green; no behavior-changing rewrite (D-13 fallback) |
| T-204-12 | Tampering | generated governance migration | medium | mitigate | sha256 pins over two storage schemas land before the split |
</threat_model>

<verification>
- `DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test` → 0 failures.
- `MIX_ENV=dev ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.dialyzer` → exit 0; `mix verify.xref_cycles` → exit 0; `MIX_ENV=test mix verify.compile_no_optional` → exit 0.
</verification>

<success_criteria>
Three more oversized units are legible with no public-API, packaging, or generated-output change, and four register sites are drained.
</success_criteria>

## Artifacts this phase produces (this plan)

- `Threadline.OperatorSurface.MechanicalChecker.Scorecards` (`lib/threadline/operator_surface/mechanical_checker/scorecards.ex`)
- `Threadline.OperatorSurface.MechanicalChecker.Parsing` (`lib/threadline/operator_surface/mechanical_checker/parsing.ex`), plus any per-check sibling chosen in Task 1 step 3
- `Threadline.Query.Cursors` (`lib/threadline/query/cursors.ex`)
- Private helpers inside `Threadline.Governance.Migration` for the ordered migration parts
- Test attributes `@governance_default_sha256`, `@governance_auditlog_sha256` in storage_schema_migration_contract_test
- mix.exs exclude pattern `~r{^lib/threadline/operator_surface/mechanical_checker(\.ex$|/)}`

<output>
Create `.planning/phases/204-structure/204-04-SUMMARY.md` when done, with the register counts before/after and every sibling created.
</output>
