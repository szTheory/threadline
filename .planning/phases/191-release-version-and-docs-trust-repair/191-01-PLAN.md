---
phase: 191-release-version-and-docs-trust-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/upgrade-path.md
  - CONTRIBUTING.md
  - test/threadline/upgrade_path_doc_contract_test.exs
autonomous: true
requirements: [ADOPT-02]
must_haves:
  truths:
    - "An adopter on 0.6.x/0.7.x/0.8.x can read guides/upgrade-path.md and know, per minor bump, whether anything is required and what."
    - "Each of the four ADOPT-02 themes (storage-schema default, operator surface/theming, release proof lanes, migration expectations) has an orientation entry naming where it landed and the adopter action."
    - "upgrade-path.md states the storage-schema freeze-at-generation migration expectation per Phase 190 D-190-14, distinct from host table_schema."
    - "CONTRIBUTING.md and upgrade-path.md both carry the D-191-02a/12a backport-policy sentence."
  artifacts:
    - guides/upgrade-path.md (extended within the existing ## Upgrade by Threadline minor section)
    - CONTRIBUTING.md (backport-policy sentence)
    - test/threadline/upgrade_path_doc_contract_test.exs (extended: theme axis, per-minor spine, mandatory nothing-required callout)
  key_links:
    - "Each new per-minor bullet references its CHANGELOG.md [0.7.0]/[0.8.0]/[0.9.0] anchor (textual, not a live #-anchor link)."
    - "upgrade_path_doc_contract_test asserts theme keywords + per-minor coverage + mandatory nothing-required callout; version_truth Family C (plan 03) reads 0.8.x -> 0.9.x from this guide."
---

<objective>
Extend `guides/upgrade-path.md` to cover the 0.6.x -> 0.9.x adopter era (ADOPT-02), mirror the backport-policy sentence into `CONTRIBUTING.md`, and extend the structural doc-contract test so the coverage is drift-proof. This satisfies ROADMAP success criterion 2 ("Upgrade guidance covers 0.6.x through 0.9.x adopter effects: storage-schema default, operator surface/theming, release lanes, migration expectations").

Purpose: A stranger bumping N->M must know, per minor, whether they must act — and the overwhelmingly correct 0.6->0.9 answer is "nothing required" (CHANGELOG audit found zero host-breaking changes; all surface/DX/proof-lane work). Ambiguity here is the single biggest trust lever.
Output: An extended upgrade guide with a four-theme orientation table + per-minor spine + bounded per-bump bullets, a mirrored backport policy, and an extended structural guard.

## Artifacts this plan produces
- Extended `## Upgrade by Threadline minor` content in `guides/upgrade-path.md`: a four-theme orientation table, a per-minor "at a glance" spine table (`0.6.x->0.7.x`, `0.7.x->0.8.x`, `0.8.x->0.9.x`), per-bump detail bullets, a storage-schema migration callout, a CHANGELOG-vs-upgrade-path division-of-labor note, and the backport-policy sentence.
- Tense fix at `guides/upgrade-path.md` line 5 ("0.6.0 packages" -> "landed in 0.6.0").
- Backport-policy sentence in `CONTRIBUTING.md`.
- Extended `test/threadline/upgrade_path_doc_contract_test.exs` assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/191-release-version-and-docs-trust-repair/191-CONTEXT.md
@.planning/phases/191-release-version-and-docs-trust-repair/191-RESEARCH.md
@.planning/phases/191-release-version-and-docs-trust-repair/191-PATTERNS.md
@guides/upgrade-path.md
@CHANGELOG.md
@test/threadline/upgrade_path_doc_contract_test.exs
@test/threadline/semver_adopter_doc_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend guides/upgrade-path.md for the 0.6.x -> 0.9.x era</name>
  <files>guides/upgrade-path.md</files>
  <read_first>
    - guides/upgrade-path.md (full) — the `## Upgrade by Threadline minor` section (line 68) already lists only `0.5.x -> 0.6.x` (line 81) and `0.3.x -> 0.4.x` (line 82); line 5 is the present-tense claim to fix; note the nine locked `## ` headings.
    - test/threadline/upgrade_path_doc_contract_test.exs (full) — locked headings (lines 5-17), the compatibility-matrix header string and Phoenix version strings (line 24), refuted tokens `forthcoming` (86,97) and `Phoenix 1.7+` (71), and the scoped 0.5->0.6 assertion (165-180) that must keep passing.
    - CHANGELOG.md — confirm the `[0.7.0]`, `[0.8.0]`, `[0.9.0]` anchor text you will reference; heading format is inconsistent (compare-link vs plain), so reference anchors textually (backtick `[x.y.0]`), never as live `#`-anchor links.
    - 191-CONTEXT.md D-191-09..12 and 191-RESEARCH.md "Upgrade-Guide Extension Grounding".
  </read_first>
  <action>
    Extend the EXISTING `## Upgrade by Threadline minor` section (do NOT add or rename any top-level `## ` heading — all nine locked headings must survive verbatim, per upgrade_path_doc_contract_test lines 5-17). Add, inside that section and its neighbours, the D-191-09 hybrid shape:
    (a) A four-THEME orientation table mapping each ADOPT-02 theme — storage-schema default, operator surface/theming, release proof lanes, migration expectations — to where it landed (which minor) and the adopter action. Use the exact ADOPT-02 keyword phrasing for each theme so the structural test can assert their presence.
    (b) A per-MINOR "at a glance" spine table with rows `0.6.x -> 0.7.x`, `0.7.x -> 0.8.x`, `0.8.x -> 0.9.x` and columns: breaking? / migration required / config touch / nothing-required reassurance. Use the arrow char U+2192 (as the existing 0.5->0.6 bullet does) or ASCII `->` consistently.
    (c) Per-bump detail bullets (one per new minor), each <= ~5 lines, mirroring the existing 0.5->0.6 bullet form (arrow + effect + textual `CHANGELOG.md` `[x.y.0]` anchor). Per D-191-10 each bump documents exactly four things: breaking changes or "None"; required migration commands or "None"; config changes or "None"; and an explicit "nothing required" reassurance for the `capture-only` and `phoenix-surface` lanes. Because 0.6->0.9 was surface/DX/proof-lane only (zero host-DB migrations), affirmative "nothing required" is the default answer.
    KEEP the existing `0.5.x -> 0.6.x` bullet (the scoped test at lines 165-180 requires it plus `Threadline.Audit.transaction/3`|`Threadline.Evidence`, `CHANGELOG.md`, and `[0.6.0]` within that scope) — new bullets land in the same scope, before the `## What breaks when Phoenix/LiveView floors move` heading.
    (d) The storage-schema migration callout (D-191-11), phrased per Phase 190 D-190-14: storage_schema is frozen at generation time — set it before `mix threadline.install`; changing it later is deliberate migration work (move/recreate Threadline-owned tables, functions, triggers in the new schema and re-run `mix threadline.gen.triggers`), not a runtime config edit. Keep Threadline-owned `storage_schema` explicitly distinct from host `table_schema`/`--schema` so operators do not conflate them. Mirror the README Quick Start wording (locked by readme_doc_contract_test) for consistency; do not contradict it. Existing adopters keep their current schema; only NEW installs pick up the `threadline` default.
    (e) A short CHANGELOG-vs-upgrade-path division-of-labor note (D-191-12): CHANGELOG = chronological/complete (what shipped); upgrade-path = curated/incomplete-by-design (must I act, and how). Every bump bullet links its `[x.y.0]` anchor and never restates feature lists.
    (f) The D-191-02a/12a backport-policy sentence near the compatibility-matrix / "which lane you are on" framing: security and critical fixes ship as patch releases on the current minor (e.g. 0.9.1), which any three-segment pin picks up automatically; crossing a minor stays a deliberate, changelog-reading act.
    (g) Tense fix at line 5: change the present-tense "0.6.0 packages Evidence ... that landed in-repo after 0.5.0" to read as history ("landed in 0.6.0"), so it is not read as a "current" claim.
    Speak Hex semver ONLY. Do NOT write product-milestone labels (`v1.34`..`v1.39` etc.) anywhere — semver_adopter guards only `v1.2x` and will NOT catch a `v1.3x` slip, so this is a manual discipline. Avoid the refuted tokens `forthcoming`, `coming soon`, and `Phoenix 1.7+`. Do NOT edit the compatibility-matrix header row or its Phoenix version strings (locked verbatim). This is version/tense repair and additive coverage only — no guide-body rewrite beyond it (Phase 191 scope).
  </action>
  <verify>
    <automated>mix test test/threadline/upgrade_path_doc_contract_test.exs test/threadline/semver_adopter_doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - All nine locked `## ` headings still present; the existing `0.5.x -> 0.6.x` bullet and the compatibility-matrix header/Phoenix strings are unchanged.
    - The four ADOPT-02 theme keywords, the three new per-minor rows (0.6->0.7, 0.7->0.8, 0.8->0.9), the storage-schema freeze-at-generation callout, the division-of-labor note, and the backport sentence all appear in the guide.
    - Line 5 reads as history, not a current claim.
    - No `forthcoming` / `coming soon` / `Phoenix 1.7+` / product-milestone `v1.3x` tokens introduced.
    - `mix test test/threadline/upgrade_path_doc_contract_test.exs test/threadline/semver_adopter_doc_contract_test.exs` passes (the pre-existing assertions stay green after the extension).
  </acceptance_criteria>
  <done>upgrade-path.md covers 0.6.x->0.9.x across all four ADOPT-02 themes and per-minor bumps with mandatory "nothing required" callouts and the storage-schema freeze block; locked headings and existing assertions stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the backport-policy sentence into CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md — locate the release/maintenance framing; grep confirms zero existing `backport`/`patch release`/`current minor` hits, so this is genuinely new prose.
    - 191-CONTEXT.md D-191-02a / D-191-12a; 191-RESEARCH.md "CONTRIBUTING.md" section.
  </read_first>
  <action>
    Add the same D-191-02a backport-policy sentence that Task 1 places in `upgrade-path.md`, mirrored into `CONTRIBUTING.md` near the release/distribution or maintenance guidance: security and critical fixes are backported as patch releases on the current minor (e.g. 0.9.1), which any `~> 0.9.0`-style three-segment pin picks up automatically; crossing a minor stays a deliberate, changelog-reading act. Keep it to a sentence or two; do not restructure CONTRIBUTING. This converts the tight-pin's only theoretical gap into stated policy so an install-once audit adopter is never stranded on an unpatched line.
  </action>
  <verify>
    <automated>mix verify.format && grep -qi 'backport' CONTRIBUTING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING.md contains a backport-policy sentence consistent with the one in upgrade-path.md (patch-backport on the current minor; minor-crossing is deliberate).
    - `mix verify.format` passes.
  </acceptance_criteria>
  <done>CONTRIBUTING.md carries the backport-policy sentence consistent with upgrade-path.md; format check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Extend upgrade_path_doc_contract_test.exs to lock the new coverage</name>
  <files>test/threadline/upgrade_path_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/upgrade_path_doc_contract_test.exs (full) — existing structural assertions, the scoped 0.5->0.6 block (165-180), and the existing refute style.
    - 191-CONTEXT.md D-191-12 (structural test must assert theme axis + per-minor bullets + refute aspirational tokens + refute any per-minor bullet lacking a None/nothing-required/explicit action).
  </read_first>
  <action>
    Extend the structural doc-contract test additively (keep every existing assertion). Add assertions per D-191-12: (a) each of the four ADOPT-02 theme keywords is present in the guide; (b) the three new per-minor coverages (`0.6.x -> 0.7.x`, `0.7.x -> 0.8.x`, `0.8.x -> 0.9.x`) are present, allowing the arrow as U+2192 or ASCII `->`; (c) `refute` the aspirational token `coming soon` (in addition to the existing `forthcoming` refute); (d) assert the mandatory "nothing required" reassurance is present for the new per-minor content. Optionally add `refute` on the `v1.3x` product-milestone label family (recommended by RESEARCH Open Question 2 — `semver_adopter` only catches `v1.2x`, and this guide narrates the v1.3x era). Do NOT hardcode a version literal for the current-minor check that belongs to version_truth Family C (plan 03) — this test owns the structural/theme axis; the derived current-minor coverage check lives in version_truth_doc_contract_test. Match the existing per-item failure-message style.
  </action>
  <verify>
    <automated>mix test test/threadline/upgrade_path_doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The extended test asserts the four theme keywords, the three per-minor coverages, the mandatory nothing-required callout, and refutes `coming soon` (and, ideally, the `v1.3x` label family).
    - `mix test test/threadline/upgrade_path_doc_contract_test.exs` passes against the Task 1 guide.
    - No assertion here duplicates version_truth Family C (the derived @version current-minor check).
  </acceptance_criteria>
  <done>upgrade_path_doc_contract_test asserts the theme axis, three per-minor coverages, and mandatory nothing-required callout, and refutes aspirational tokens; the test passes against the extended guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter -> upgrade guide | An adopter reading upgrade-path.md decides whether to run migrations or change config; wrong guidance causes a bad action or a skipped one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-191-04 | Information disclosure | upgrade-path.md per-minor guidance | medium | mitigate | Mandatory "nothing required" callouts (D-191-10) + storage-schema block phrased per Phase 190 D-190-14; structural test refutes aspirational tokens and any per-minor bullet lacking a None/nothing-required/explicit action. |
| T-191-05 | Tampering | semver-only versioning claim | low | mitigate | Hex-semver-only discipline; new test refutes `coming soon` and (recommended) the `v1.3x` product-milestone label family that semver_adopter's `v1.2x` guard misses. |
| T-191-SC | Tampering | package installs | low | accept | No new external packages are installed by this plan (docs + one test file only); nothing to audit. |
</threat_model>

<verification>
- `mix test test/threadline/upgrade_path_doc_contract_test.exs test/threadline/semver_adopter_doc_contract_test.exs` — extended structural guard + semver guard pass.
- `mix verify.doc_contract` — the broader doc-contract suite stays green (upgrade-path edits do not break sibling contracts).
- `mix verify.format` — CONTRIBUTING.md and guide edits are formatted.
</verification>

<success_criteria>
- ROADMAP criterion 2 met: upgrade guidance covers 0.6.x -> 0.9.x across all four themes with per-minor and theme axes, plus the storage-schema migration expectation.
- The backport policy appears in both upgrade-path.md and CONTRIBUTING.md.
- The structural test now fails on future loss of theme/per-minor/nothing-required coverage.
- No product-milestone labels or aspirational tokens leaked into adopter docs.
</success_criteria>

<output>
Create `.planning/phases/191-release-version-and-docs-trust-repair/191-01-SUMMARY.md` when done.
</output>
