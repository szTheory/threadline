---
phase: 171-audit-baseline-stress-lab-harness-idempotency-ledger
plan: 03
type: execute
wave: 3
depends_on:
  - 171-01
  - 171-02
files_modified:
  - lib/threadline/operator_surface/stress_router.ex
  - lib/threadline/operator_surface/live/stress_live.ex
  - lib/threadline/operator_surface/style.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - test/threadline/operator_surface/stress_router_test.exs
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
must_haves:
  truths:
    - "The stress route is a separate internal router macro, not a `stress: true` option on `threadline_operator_surface/2`, per D-01 and D-02."
    - "`/audit/__stress` reuses the authenticated operator shell, auth on_mount, coverage on_mount, data-tl-theme, and inline assets per D-03."
    - "The route fails closed in production and avoids live_session/helper collisions per D-04 and D-05."
    - "The selected stress story is the primary focal point and displays ledger score, target score, fixture key, and screenshot status from the JSON ledger per D-11, D-12, D-18."
  artifacts:
    - path: "lib/threadline/operator_surface/stress_router.ex"
      provides: "Internal dev/test stress route macro"
      exports: ["threadline_operator_surface_stress/2"]
    - path: "lib/threadline/operator_surface/live/stress_live.ex"
      provides: "Authenticated stress lab LiveView"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      provides: "Example app `/audit/__stress` mount inside authenticated scope"
    - path: "test/threadline/operator_surface/stress_router_test.exs"
      provides: "Route, auth, prod gate, and param normalization tests"
  key_links:
    - from: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      to: "Threadline.OperatorSurface.StressRouter.threadline_operator_surface_stress/2"
      via: "authenticated `/audit` scope mount"
      pattern: "threadline_operator_surface_stress\\(\"/__stress\""
    - from: "Threadline.OperatorSurface.Live.StressLive"
      to: "Threadline.OperatorSurface.StressFixtures"
      via: "story lookup and assign adapter"
      pattern: "StressFixtures"
    - from: "Threadline.OperatorSurface.Live.StressLive"
      to: ".planning/design-system-ledger.json"
      via: "ledger score and screenshot status lookup"
      pattern: "design-system-ledger.json"
---

<objective>
Mount and render the authenticated internal stress lab at `/audit/__stress`.

Purpose: Phase 171 needs a real Phoenix/LiveView render surface that downstream phases can ratchet against without changing the public operator-surface mount API.
Output: internal `StressRouter`, `StressLive`, example app mount, stress-specific styles inside the existing inline CSS source, and ExUnit route/security contracts.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-CONTEXT.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-RESEARCH.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-PATTERNS.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-UI-SPEC.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-VALIDATION.md
@examples/threadline_phoenix/AGENTS.md
@lib/threadline/operator_surface/router.ex
@lib/threadline/operator_surface/auth.ex
@lib/threadline/operator_surface/coverage/on_mount.ex
@lib/threadline/operator_surface/live/start_live.ex
@lib/threadline/operator_surface/components/surface_header.ex
@lib/threadline/operator_surface/components/unsupported_view.ex
@lib/threadline/operator_surface/style.ex
@lib/threadline/operator_surface/stress_fixtures.ex
@.planning/design-system-ledger.json
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@test/threadline/operator_surface/router_test.exs
@test/threadline/operator_surface/live/start_live_test.exs

<interfaces>
Create `Threadline.OperatorSurface.StressRouter.threadline_operator_surface_stress/2`.
It accepts `path` and the same auth/theme/coverage options as the existing operator surface macro.
It must use `live_session :threadline_stress` and mount `Threadline.OperatorSurface.Live.StressLive` at the supplied path.
It must raise or be omitted in `Mix.env() == :prod`.

Create `Threadline.OperatorSurface.Live.StressLive`.
Required test IDs: `stress-lab`, `stress-story-id`, `stress-preview`, `stress-ledger-score`, `stress-target-score`, `stress-screenshot-status`, `stress-story-list`, `stress-category-nav`, `stress-clear-filters`, `stress-empty-state`.
Required route param allowlists: `story`, `category`, `status`, `theme`, `viewport`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write stress route and LiveView contracts</name>
  <files>test/threadline/operator_surface/stress_router_test.exs</files>
  <read_first>
    test/threadline/operator_surface/stress_router_test.exs
    test/threadline/operator_surface/router_test.exs
    test/threadline/operator_surface/live/start_live_test.exs
    lib/threadline/operator_surface/router.ex
    lib/threadline/operator_surface/auth.ex
    examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-CONTEXT.md
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: compiling a throwaway router with `Threadline.OperatorSurface.StressRouter.threadline_operator_surface_stress("/__stress", authorize_fn: fn _ -> true end)` inside a secure pipeline succeeds in test and registers a LiveView route at `/__stress`.
    - Test 2: compiling a throwaway router with `threadline_operator_surface_stress("/__stress", stress_env: :prod, authorize_fn: fn _ -> true end)` raises `CompileError` containing `Threadline stress surface is dev/test-only`; the private `:stress_env` option is removed before auth/on_mount opts are quoted.
    - Test 3: source for `lib/threadline/operator_surface/router.ex` and `examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` contains no `stress: true`, proving the public macro option surface was not expanded per D-02.
    - Test 4: `Phoenix.Router.routes(ThreadlinePhoenixWeb.Router)` in test contains `/audit/__stress` and still contains `/audit`; the stress route uses a distinct `:threadline_stress` live session.
    - Test 5: `live(conn, "/audit/__stress")` for an authenticated operator renders `.threadline-ui`, `data-tl-theme`, `data-testid="operator-header"`, `id="tl-main"`, `data-testid="stress-lab"`, `data-testid="stress-story-id"`, and `data-testid="stress-preview"`.
    - Test 6: unknown `story`, `category`, `status`, `theme`, or `viewport` params never crash; the LiveView renders `No stress stories registered` or the default selected story with a safe status.
    - Test 7: unauthenticated LiveView access follows the existing example operator auth path and does not render `data-testid="stress-lab"`.
  </behavior>
  <action>Create `test/threadline/operator_surface/stress_router_test.exs` before route implementation. Reuse throwaway-router compile patterns from `router_test.exs` and LiveView route test helpers from `live/start_live_test.exs`. Test prod gating through a private macro-only option named `:stress_env`: the prod-gate test must pass `stress_env: :prod`, assert the compile error, and also assert normal test compilation strips `:stress_env` before quoting on_mount options. Do not implement the stress modules in this task.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_router_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `mix test test/threadline/operator_surface/stress_router_test.exs` fails before Task 2 because `Threadline.OperatorSurface.StressRouter` and `Threadline.OperatorSurface.Live.StressLive` do not exist.
    - Source assertion: the test file includes checks for `/audit/__stress`, `:threadline_stress`, `data-testid="stress-lab"`, and absence of `stress: true`.
    - Security assertion: the test file contains an unauthenticated access case that refutes rendered stress lab HTML.
  </acceptance_criteria>
  <done>The stress route contract captures prod gating, auth reuse, route namespace hygiene, shell/theme rendering, and safe param normalization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement internal stress router, LiveView, mount, and styles</name>
  <files>lib/threadline/operator_surface/stress_router.ex, lib/threadline/operator_surface/live/stress_live.ex, lib/threadline/operator_surface/style.ex, examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex, test/threadline/operator_surface/stress_router_test.exs</files>
  <read_first>
    lib/threadline/operator_surface/stress_router.ex
    lib/threadline/operator_surface/live/stress_live.ex
    lib/threadline/operator_surface/style.ex
    examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
    test/threadline/operator_surface/stress_router_test.exs
    lib/threadline/operator_surface/router.ex
    lib/threadline/operator_surface/auth.ex
    lib/threadline/operator_surface/coverage/on_mount.ex
    lib/threadline/operator_surface/live/start_live.ex
    lib/threadline/operator_surface/components/surface_header.ex
    lib/threadline/operator_surface/components/unsupported_view.ex
    lib/threadline/operator_surface/stress_fixtures.ex
    .planning/design-system-ledger.json
    DESIGN-SYSTEM.md
    examples/threadline_phoenix/AGENTS.md
  </read_first>
  <behavior>
    - Test 1: stress route compiles only for dev/test harness use and is mounted in the example app inside the existing authenticated `/audit` scope.
    - Test 2: `StressLive` renders the real operator shell and reads story, ledger score, and screenshot status from Plan 171-01/02 artifacts.
    - Test 3: user-controlled params are allowlist-normalized and never converted to atoms.
  </behavior>
  <action>Implement `lib/threadline/operator_surface/stress_router.ex` guarded with `if Code.ensure_loaded?(Phoenix.LiveView)`. Define `threadline_operator_surface_stress/2` as an internal macro that reads `stress_env = Keyword.get(opts, :stress_env, Mix.env())`, removes `:stress_env` from the opts passed to auth/on_mount, rejects prod use with `CompileError`, imports `Phoenix.LiveView.Router`, opens `live_session :threadline_stress` with `{Threadline.OperatorSurface.Auth, clean_opts}` and `{Threadline.OperatorSurface.Coverage.OnMount, clean_opts}`, and scopes `path` with `alias: Threadline.OperatorSurface.Live, as: false` before `live("/", StressLive, :index)`. Implement `Threadline.OperatorSurface.Live.StressLive` using the existing `.threadline-ui` wrapper, `<Threadline.OperatorSurface.Style.css />`, `SurfaceHeader.surface_header`, `#tl-main`, and the exact test IDs in the interfaces block. Read `Threadline.OperatorSurface.StressFixtures` for story data and `.planning/design-system-ledger.json` with `Jason.decode!/1` for `current_score`, `target_score`, `ratchet_score`, `status`, and `screenshot_baseline_refs`; if the ledger cannot be read in dev/test, render the UI-SPEC error copy instead of crashing. Normalize `story`, `category`, `status`, `theme`, and `viewport` params with closed string/integer allowlists. Add stress-lab selectors to `style.ex` under the existing inline token system only: use `--tl-space-*`, `--tl-color-*`, `--tl-font-*`, existing motion tokens, `data-tl-theme`, 4/8/16/24/32/48/64 spacing, and no new color literals except values already present in style/brand tokens. In the example router, require `Threadline.OperatorSurface.StressRouter` and mount `threadline_operator_surface_stress("/__stress", ...)` adjacent to the public `threadline_operator_surface("/", ...)` in both compile-time theme branches, inside the existing `scope "/audit"` and `pipe_through([:browser, :operator_browser, :operator_auth])`.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_router_test.exs test/threadline/operator_surface/stress_fixtures_test.exs test/threadline/operator_surface/stress_ledger_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the focused command exits 0.
    - Source assertion: `rg -n 'threadline_operator_surface_stress\\(\"/__stress\"' examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex` returns two matches, one in each compile-time theme branch.
    - Source assertion: `rg -n 'stress: true' lib/threadline/operator_surface examples/threadline_phoenix/lib test/threadline/operator_surface` returns no matches.
    - Source assertion: `rg -n 'String\\.to_atom|PhoenixStorybook|Tailwind|Chromatic|Percy|Applitools' lib/threadline/operator_surface/stress_router.ex lib/threadline/operator_surface/live/stress_live.ex lib/threadline/operator_surface/style.ex` returns no matches.
    - Source assertion: `lib/threadline/operator_surface/live/stress_live.ex` contains `data-testid="stress-story-id"`, `data-testid="stress-preview"`, `data-testid="stress-ledger-score"`, and `data-testid="stress-screenshot-status"`.
  </acceptance_criteria>
  <done>`/audit/__stress` renders an authenticated internal stress lab with real shell/theme assets, safe story params, fixture-driven content, and ledger-backed status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public browser -> `/audit/__stress` | User-controlled requests hit an internal LiveView route. |
| LiveView params -> story lookup | `story`, `category`, `status`, `theme`, and `viewport` params select render state. |
| JSON/fixtures -> HTML | Ledger and fixture data render inside the stress lab. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-01 | Information Disclosure / Elevation of Privilege | `StressRouter.threadline_operator_surface_stress/2` | mitigate | Compile-time prod rejection or omission, example route under existing authenticated `/audit` scope, and ExUnit route/prod-gate tests. |
| T-171-02 | Elevation of Privilege | `StressLive` LiveView navigation | mitigate | Use `live_session :threadline_stress` with existing `Threadline.OperatorSurface.Auth` and `Coverage.OnMount`; unauthenticated test must not render `stress-lab`. |
| T-171-03 | Denial of Service | `StressLive.handle_params/3` | mitigate | Validate params against registry allowlists; unknown strings render empty/default state and never call `String.to_atom/1`. |
| T-171-11 | Spoofing / Tampering | route namespace helpers | mitigate | Use `as: false` and `:threadline_stress`; route test verifies no collision with public `:threadline` session. |
| T-171-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; implementation uses existing Phoenix, LiveView, Jason, and inline CSS. |
</threat_model>

<verification>
Focused command: `mix test test/threadline/operator_surface/stress_router_test.exs test/threadline/operator_surface/stress_fixtures_test.exs test/threadline/operator_surface/stress_ledger_test.exs`.

Wave command after Plan 171-03: `mix ci.all && mix verify.example_browser`.
</verification>

<success_criteria>
DS-01 is satisfied server-side when `/audit/__stress` exists in test/dev example routing, is authenticated, renders the real operator shell/theme/assets with fixture and ledger data, rejects/omits prod use, avoids public macro option creep, and normalizes all user-controlled params safely.
</success_criteria>

<output>
Create `.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-03-SUMMARY.md` when done.
</output>
