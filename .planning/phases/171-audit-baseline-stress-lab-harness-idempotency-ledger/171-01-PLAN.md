---
phase: 171-audit-baseline-stress-lab-harness-idempotency-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/stress_fixtures.ex
  - test/threadline/operator_surface/stress_fixtures_test.exs
autonomous: true
requirements:
  - DS-04
must_haves:
  truths:
    - "Static library-side stress fixtures are canonical for stress stories, ledger IDs, and screenshots per D-06, D-08, D-09."
    - "The example seeded database remains an integration lane and is not required for DS-04 stress states per D-07."
    - "Fixture adapters are tested against existing operator-surface component shapes per D-10."
    - "Folded todos are represented only as future-owned stress cases per D-21, D-22, D-23."
  artifacts:
    - path: "lib/threadline/operator_surface/stress_fixtures.ex"
      provides: "Threadline.OperatorSurface.StressFixtures pure registry"
      exports: ["all/0", "by_id/1", "categories/0", "required_cases/0", "theme_modes/0", "viewports/0", "assigns_for/1"]
    - path: "test/threadline/operator_surface/stress_fixtures_test.exs"
      provides: "DS-04 matrix and adapter drift contract"
  key_links:
    - from: "Threadline.OperatorSurface.StressFixtures.all/0"
      to: "DS-04 required ugly-data matrix"
      via: "required_cases coverage test"
      pattern: "empty|one|many|long_id|long_string|non_ascii|permission_denied|pagination_boundary"
    - from: "test/threadline/operator_surface/stress_fixtures_test.exs"
      to: "Threadline.OperatorSurface.Components.SurfaceHeader.surface_header/1"
      via: "render_component adapter smoke"
      pattern: "render_component"
---

<objective>
Create the canonical Phase 171 stress fixture registry and adapter tests.

Purpose: Later ledger, route, and browser plans need stable synthetic story data before they can render or ratchet anything.
Output: `Threadline.OperatorSurface.StressFixtures` plus ExUnit contracts for DS-04 coverage, stable IDs, synthetic-only data, and representative assign adapters.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-CONTEXT.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-RESEARCH.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-PATTERNS.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-UI-SPEC.md
@.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-VALIDATION.md
@lib/threadline/operator_surface/presentation.ex
@lib/threadline/operator_surface/live/start_live.ex
@lib/threadline/operator_surface/components/surface_header.ex
@lib/threadline/operator_surface/components/unsupported_view.ex
@test/threadline/operator_surface/surface_header_test.exs

<interfaces>
Create this internal module contract for downstream plans:
- Module: `Threadline.OperatorSurface.StressFixtures`
- `all/0` returns a deterministic list of story maps sorted by string `id`.
- `by_id/1` accepts a string ID and returns `{:ok, story}` or `:error`; it must never call `String.to_atom/1`.
- `categories/0` returns sorted category strings.
- `required_cases/0` returns the DS-04 case strings listed in Task 1.
- `theme_modes/0` returns `["dark", "light", "system"]`.
- `viewports/0` returns `[320, 375, 768, 1024, 1440]`.
- `assigns_for/1` accepts a story map or story ID and returns `{:ok, assigns}` or `{:error, :unknown_story}` for renderer adapters.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write fixture registry contract tests</name>
  <files>test/threadline/operator_surface/stress_fixtures_test.exs</files>
  <read_first>
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-CONTEXT.md
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-RESEARCH.md
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-PATTERNS.md
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-VALIDATION.md
    test/threadline/operator_surface/surface_header_test.exs
    lib/threadline/operator_surface/components/surface_header.ex
    lib/threadline/operator_surface/components/unsupported_view.ex
    test/threadline/operator_surface/stress_fixtures_test.exs
  </read_first>
  <behavior>
    - Test 1: `required_cases/0` returns exactly these sorted strings: `empty`, `one`, `many`, `long_id`, `long_string`, `non_ascii`, `high_count`, `zero_count`, `null_fields`, `error`, `warning`, `mixed_severity`, `permission_denied`, `stale`, `reconnecting`, `timezone_boundary`, `pagination_boundary`.
    - Test 2: `theme_modes/0` returns exactly `["dark", "light", "system"]`; `viewports/0` returns exactly `[320, 375, 768, 1024, 1440]`.
    - Test 3: every `all/0` story has string `id`, `kind`, `category`, `scenario`, `fixture_key`, `ledger_id`, string-list `cases`, string-list `themes`, integer-list `viewports`, integer `owner_phase`, string `status`, map `data`, and map `metadata`.
    - Test 4: story IDs and ledger IDs are unique, sorted, and stable strings; `by_id/1` returns `{:ok, story}` for existing IDs and `:error` for unknown strings.
    - Test 5: DS-04 coverage is complete by unioning each story's `cases`; missing case failure messages name the missing case.
    - Test 6: folded todo stories exist with IDs `future.theme-picker-idiomatic-ui`, `footgun.coverage-schema-card-declutter`, and `footgun.transaction-page-left-push-desktop`, owned by phases `175`, `176`, and `178` respectively; each is represented as baseline/reserved data only per D-21, D-22, and D-23.
    - Test 7: source text for `lib/threadline/operator_surface/stress_fixtures.ex` contains no `ThreadlinePhoenix`, `Repo.`, `Ecto.Query`, `String.to_atom`, `PhoenixStorybook`, `Tailwind`, or external package registry strings.
    - Test 8: representative adapters render existing components with `Phoenix.LiveViewTest.render_component/2`: `primitive.surface-header.current` renders `data-testid="operator-header"` and `state.permission-denied` renders permission-denied copy through the existing unsupported-state component pattern.
  </behavior>
  <action>Create `test/threadline/operator_surface/stress_fixtures_test.exs` before implementation. Use `ExUnit.Case, async: true`, `Phoenix.LiveViewTest`, direct module calls, `File.read!/1` source assertions, and custom failure messages in the style of `brandbook_token_parity_test.exs`. Do not create database fixtures, example-app seed data, browser tests, ledger JSON, or route code in this task.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_fixtures_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/threadline/operator_surface/stress_fixtures_test.exs` contains the exact DS-04 case strings listed in the behavior block.
    - Behavior assertion: running `mix test test/threadline/operator_surface/stress_fixtures_test.exs` before Task 2 fails because `Threadline.OperatorSurface.StressFixtures` is not implemented or does not satisfy the contract.
    - Source assertion: the test file includes assertions for D-21, D-22, and D-23 reserved story IDs and owner phases.
  </acceptance_criteria>
  <done>The failing contract captures the DS-04 matrix, stable IDs, reserved cases, synthetic-only rule, and representative adapter smoke checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement static stress fixture registry</name>
  <files>lib/threadline/operator_surface/stress_fixtures.ex, test/threadline/operator_surface/stress_fixtures_test.exs</files>
  <read_first>
    lib/threadline/operator_surface/stress_fixtures.ex
    test/threadline/operator_surface/stress_fixtures_test.exs
    lib/threadline/operator_surface/presentation.ex
    lib/threadline/operator_surface/live/start_live.ex
    lib/threadline/operator_surface/components/surface_header.ex
    lib/threadline/operator_surface/components/unsupported_view.ex
    .planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `StressFixtures.all/0` returns deterministic static story data with no database dependency.
    - Test 2: `StressFixtures.assigns_for/1` returns renderable assigns for representative shell, permission-denied, stale/reconnecting, long-value, and pagination-boundary stories.
    - Test 3: stories cover all DS-04 cases and the required dark/light/system and viewport metadata.
  </behavior>
  <action>Implement `Threadline.OperatorSurface.StressFixtures` as a pure internal registry under `lib/threadline/operator_surface/stress_fixtures.ex`. Use module attributes or private pure functions for story data. Include categories `foundation`, `primitive`, `form_control`, `group`, `page`, `state`, `footgun`, and `future_reserved`. Include at least these concrete story IDs: `foundation.tokens.palette`, `primitive.surface-header.current`, `page.home.happy`, `page.timeline.empty`, `page.timeline.long-ids`, `page.coverage.permission-denied`, `state.empty`, `state.one`, `state.many`, `state.null-fields`, `state.mixed-severity`, `state.stale-reconnecting`, `state.timezone-pagination-boundary`, `future.theme-picker-idiomatic-ui`, `footgun.coverage-schema-card-declutter`, and `footgun.transaction-page-left-push-desktop`. Keep all fixture values synthetic: use fake UUID-like IDs, non-ASCII sample strings, explicit timezone labels, long path/email/correlation strings, high and zero counts, warning/error/mixed severity maps, and pagination boundary metadata. Return strings for user-selectable IDs and categories; do not convert user input to atoms.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/stress_fixtures_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `mix test test/threadline/operator_surface/stress_fixtures_test.exs` exits 0.
    - Source assertion: `lib/threadline/operator_surface/stress_fixtures.ex` defines `defmodule Threadline.OperatorSurface.StressFixtures`.
    - Source assertion: `rg -n 'ThreadlinePhoenix|Repo\\.|Ecto\\.Query|String\\.to_atom|PhoenixStorybook|Tailwind' lib/threadline/operator_surface/stress_fixtures.ex` returns no matches.
    - Behavior assertion: `StressFixtures.by_id("not-a-story")` returns `:error`, not an exception.
  </acceptance_criteria>
  <done>Static story data is canonical, deterministic, DS-04-complete, and adapter-tested without relying on the example database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture source -> rendered stress lab | Static fixture values become HTML in the stress route. |
| Fixture registry -> downstream ledger/browser plans | Story IDs become ledger IDs, screenshot names, and URL params. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-04 | Information Disclosure | `Threadline.OperatorSurface.StressFixtures` | mitigate | Use synthetic static data only; source test refutes `ThreadlinePhoenix`, `Repo.`, and `Ecto.Query` references so real seeded rows and secrets cannot enter the canonical fixture lane. |
| T-171-08 | Tampering | story ID registry | mitigate | Contract tests require sorted unique string IDs and exact folded-todo reserved IDs so downstream ledger/screenshot refs cannot drift silently. |
| T-171-09 | Denial of Service | `by_id/1` and `assigns_for/1` | mitigate | Unknown story strings return `:error` or `{:error, :unknown_story}`; no user input is converted to atoms. |
| T-171-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 171 installs no new packages; keep this plan free of package-manager install tasks per the RESEARCH Package Legitimacy Audit. |
</threat_model>

<verification>
Focused command: `mix test test/threadline/operator_surface/stress_fixtures_test.exs`.

Wave command after all Wave 1 work: `mix test test/threadline/operator_surface/stress_fixtures_test.exs`.
</verification>

<success_criteria>
DS-04 is satisfied when the fixture registry covers every required ugly-data case, exposes stable IDs and adapters, includes the D-21 through D-23 future-owned baseline cases without implementing those UI fixes, and passes the focused ExUnit contract.
</success_criteria>

<output>
Create `.planning/phases/171-audit-baseline-stress-lab-harness-idempotency-ledger/171-01-SUMMARY.md` when done.
</output>
