---
phase: 110-triage-narrow-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_html.ex
  - .planning/v1.23/findings/0001-landing-500-badmap.md
autonomous: true
requirements:
  - FIX-01
must_haves:
  truths:
    - "GET / returns HTTP 200 for logged-out visitors"
    - "Finding 0001 status is fixed with fixed_in SHA"
    - "mix ci.all passes at repo root"
  artifacts:
    - path: examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_html.ex
      provides: "Nil-safe @current_scope on landing page"
    - path: .planning/v1.23/findings/0001-landing-500-badmap.md
      provides: "Closed (a) finding with commit reference"
---

<objective>
Wave 1 — fix finding **0001** (`landing-500-badmap`) only. Commit nil-safe `@current_scope` handling in `PageHTML.home/1`, run L0 smoke + L1 `mix ci.all`, update finding frontmatter. No WR-001/WR-002 work in this plan.
</objective>

<context>
@.planning/phases/110-triage-narrow-fixes/110-CONTEXT.md (D-110-01a, D-110-04b, D-110-05a)
@.planning/phases/110-triage-narrow-fixes/110-RESEARCH.md
@.planning/v1.23/findings/0001-landing-500-badmap.md
@examples/threadline_phoenix/AGENTS.md
</context>

<scope_guard>
**ALLOWED:** `examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_html.ex`, finding 0001 frontmatter  
**FORBIDDEN:** `lib/threadline/**`, WALKTHROUGH edits, filing 0002/0003, any WR fixes
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-110-01 | Landing 500 blocks all walk sections | high | L0 curl 200 before plan complete |
| T-110-02 | Scope creep into lib/ or WR fixes | medium | scope_guard; commit message cites only finding 0001 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Ensure nil-safe landing template and commit</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_html.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_html.ex
    - .planning/v1.23/findings/0001-landing-500-badmap.md
    - examples/threadline_phoenix/AGENTS.md (current_scope guidance)
  </read_first>
  <action>
Ensure `PageHTML.home/1` guards logged-out visitors:

- Line 10: `:if={is_nil(@current_scope) or is_nil(@current_scope.user)}` for Register/Log in block
- Line 15: `:if={@current_scope && @current_scope.user}` for signed-in block

If already present, verify only. Commit from repo root with message:

```
fix(examples): nil-safe current_scope on landing (finding 0001)

WALK-01-04: logged-out GET / returned 500 BadMapError on @current_scope.
Host-owned Sigra wiring — no lib/threadline changes.
```

Record commit SHA as `FIX_0001_SHA`.
  </action>
  <verify>
    <automated>grep -q 'is_nil(@current_scope)' examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_html.ex</automated>
  </verify>
  <acceptance_criteria>
    - `page_html.ex` contains `is_nil(@current_scope)` in the logged-out `:if`
    - Git log -1 message contains `finding 0001`
    - No files under `lib/threadline/` in the commit
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: L0 smoke — curl landing 200</name>
  <files></files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (WALK-01-04 expected outcome)
    - .planning/v1.23/findings/0001-landing-500-badmap.md
  </read_first>
  <action>
From `examples/threadline_phoenix/` with Postgres available (Path A :5432 or Path B `DB_PORT=5433`):

1. `mix phx.server` (or use running server on port 4000)
2. Run: `curl -sS -o /dev/null -w '%{http_code}' http://localhost:4000/`
3. Expect output exactly `200` (not `500`)

Optional: run ConnCase landing test if present: `mix test test/threadline_phoenix_web/` filtering home/landing.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w '%{http_code}' http://localhost:4000/ 2>/dev/null | grep -q '^200$' || echo 'SKIP: server not running — run manually'</automated>
  </verify>
  <acceptance_criteria>
    - `curl` HTTP status code is `200`
    - No `BadMapError` in server log for `GET /`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update finding 0001 and run L1 ci.all</name>
  <files>.planning/v1.23/findings/0001-landing-500-badmap.md</files>
  <read_first>
    - .planning/v1.23/findings/TEMPLATE.md
    - .planning/v1.23/findings/0001-landing-500-badmap.md
  </read_first>
  <action>
Update finding frontmatter:

```yaml
status: fixed
fixed_in: <FIX_0001_SHA>
```

From repo root run `mix ci.all`. All steps must exit 0.
  </action>
  <verify>
    <automated>grep -q 'status: fixed' .planning/v1.23/findings/0001-landing-500-badmap.md && mix ci.all</automated>
  </verify>
  <acceptance_criteria>
    - `0001-landing-500-badmap.md` frontmatter `status: fixed` and `fixed_in:` populated with 40-char hex SHA
    - `mix ci.all` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Wave 1 complete before starting plan 110-02
- [ ] `git log --oneline -1` cites finding 0001
</verification>
