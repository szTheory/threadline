---
phase: 110-triage-narrow-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - "110-02"
files_modified:
  - .planning/phases/110-triage-narrow-fixes/110-RE-WALK-LOG.md
  - .planning/phases/110-triage-narrow-fixes/110-SUMMARY.md
  - .planning/v1.23/findings/README.md
autonomous: false
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
  - DEFER-01
requirements_addressed: FIX-01, FIX-02, FIX-03, DEFER-01
must_haves:
  truths:
    - "Validation re-walk logged from WALK-01-04 through §5 at post-Wave-2 SHA"
    - "110-SUMMARY.md lists all findings fixed/deferred and RUN matrix"
    - "Every deferred (d) or over-budget (b) has SEED-NNN in v1.24-seeds"
  artifacts:
    - path: .planning/phases/110-triage-narrow-fixes/110-RE-WALK-LOG.md
      provides: "L2 RUN acceptance attestation"
    - path: .planning/phases/110-triage-narrow-fixes/110-SUMMARY.md
      provides: "Phase closeout handoff"
---

<objective>
Wave 3 — **validation re-walk** on fresh isolated clone at `RE_WALK_BASELINE_SHA` (post-110-02 HEAD). Log results in `110-RE-WALK-LOG.md`. File **new** findings only for surprises. Produce `110-SUMMARY.md` with RUN matrix, deferred seeds table, and `lib/` commit audit. Process any **(d)** / over-budget **(b)** via v1.24-seeds per DEFER-01.
</objective>

<context>
@.planning/phases/110-triage-narrow-fixes/110-CONTEXT.md (D-110-01d–e, D-110-05c–e, D-110-06)
@.planning/phases/109-maintainer-walkthrough-dry-run/109-CONTEXT.md (clone discipline)
@examples/threadline_phoenix/WALKTHROUGH.md
</context>

<scope_guard>
**ALLOWED:** `.planning/phases/110-triage-narrow-fixes/*`, `.planning/v1.23/findings/*` (new surprises only), `.planning/v1.24-seeds/*` (on deferral), `examples/` and `guides/` fixes for **(a)** surprises only  
**FORBIDDEN:** Observe-only re-run of Phase 109; walking broken WR steps on purpose; `lib/threadline/**` unless new **(a)** with layer-first gate evidence
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-110-03 | Re-walk on dirty tree invalidates RUN | high | Fresh clone at `RE_WALK_BASELINE_SHA` |
| T-110-06 | Silent drop of deferred findings | medium | 110-SUMMARY § Deferred v1.24 seeds mandatory |
</threat_model>

<tasks>

<task type="manual">
  <name>Task 1: Record RE_WALK_BASELINE_SHA and create isolated clone</name>
  <files>.planning/phases/110-triage-narrow-fixes/110-RE-WALK-LOG.md</files>
  <read_first>
    - .planning/phases/110-triage-narrow-fixes/110-CONTEXT.md (D-110-01d, D-110-05c)
    - .planning/phases/109-maintainer-walkthrough-dry-run/109-EXECUTION-LOG.md (clone pattern)
    - examples/threadline_phoenix/WALKTHROUGH.md (§0)
  </read_first>
  <action>
From **main repo** after plans 110-01 and 110-02 merged:

1. `RE_WALK_BASELINE_SHA=$(git rev-parse HEAD)` — record full SHA in log header
2. `CLONE_DIR=${TMPDIR:-/tmp}/threadline-walk-110-$(git rev-parse --short HEAD)`
3. `git clone "$(git rev-parse --show-toplevel)" "$CLONE_DIR" && cd "$CLONE_DIR" && git checkout "$RE_WALK_BASELINE_SHA"`
4. Bootstrap Postgres per WALKTHROUGH §0 Path A or B; `cd examples/threadline_phoenix`
5. Create `110-RE-WALK-LOG.md` with sections: Baseline SHA, Clone path, Postgres path, **Validation mode** note, empty RUN checklist

**Validation mode:** fixes already landed; file new findings only for surprises; do not re-prove 0001/0002/0003.
  </action>
  <verify>
    <automated>test -f .planning/phases/110-triage-narrow-fixes/110-RE-WALK-LOG.md && grep -q 'RE_WALK_BASELINE_SHA' .planning/phases/110-triage-narrow-fixes/110-RE-WALK-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `110-RE-WALK-LOG.md` exists with 40-char `RE_WALK_BASELINE_SHA`
    - Clone `git rev-parse HEAD` equals `RE_WALK_BASELINE_SHA`
    - Log states validation mode (not observe-only Phase 109)
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: L2 re-walk WALK-01-04 through §5</name>
  <files>.planning/phases/110-triage-narrow-fixes/110-RE-WALK-LOG.md</files>
  <read_first>
    - examples/threadline_phoenix/WALKTHROUGH.md (§1–§5)
    - .planning/v1.23/findings/README.md
  </read_first>
  <action>
In clone at `examples/threadline_phoenix/`:

1. Execute from **WALK-01-04** through end of **§5** (minimum L2). Promote to **L3** full §0–§5 if §0–§3 bootstrap differs on fix SHA or L2 fails in §1.
2. For each section log: pass / fail / skipped + step ID
3. **RUN matrix** in log:

| Run | Scope | Result |
|-----|-------|--------|
| RUN-01 | §1 gate | pass/fail |
| RUN-02 | §4 four WALK-03 incidents | pass/fail/partial |
| RUN-03 | §5 evidence exercises | pass/fail |

4. Confirm WR-001/002 paths: WALK-03-02 non-empty window; WALK-03-03 CLI one-liner exits 0
5. File **new** findings only for surprises (classify at capture); fix **(a)** immediately if any

Update `110-RE-WALK-LOG.md` with timestamps and any new finding IDs.
  </action>
  <verify>
    <automated>grep -q 'RUN-01' .planning/phases/110-triage-narrow-fixes/110-RE-WALK-LOG.md && grep -q 'WALK-01-04' .planning/phases/110-triage-narrow-fixes/110-RE-WALK-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - Log contains RUN-01, RUN-02, RUN-03 rows with pass/fail
    - WALK-01-04 marked pass (landing 200)
    - Any new finding files have classification and walkthrough_step set
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 110-SUMMARY.md closeout and DEFER-01 seeds</name>
  <files>
    .planning/phases/110-triage-narrow-fixes/110-SUMMARY.md
    .planning/v1.24-seeds/README.md
  </files>
  <read_first>
    - .planning/phases/110-triage-narrow-fixes/110-CONTEXT.md (D-110-06)
    - .planning/v1.23/findings/README.md
    - .planning/REQUIREMENTS.md (FIX-01..03, DEFER-01)
    - .planning/phases/109-maintainer-walkthrough-dry-run/109-SUMMARY.md (handoff format)
  </read_first>
  <action>
Create **110-SUMMARY.md** with:

1. **Finding disposition table** — all IDs 0001+ with status fixed/deferred/open
2. **§ Deferred v1.24 seeds** — table `SEED-ID | kind | source_findings | trigger_when` (or "None" if no deferrals)
3. **RUN matrix** — copy from re-walk log
4. **Re-walk status** — `RE_WALK_BASELINE_SHA`, L2/L3 rung, WR confirmation
5. **`lib/` commit audit** — `git log` grep `lib/threadline` on phase branch; expect **zero** for current inventory

For each **(d)** or over-budget **(b)** deferred finding:

1. Create `.planning/v1.24-seeds/TEMPLATE.md` and `README.md` on first deferral (per D-110-06a)
2. Add `SEED-NNN-slug.md` with required frontmatter
3. Set finding `deferred_to: SEED-NNN`

Run `mix ci.all` from repo root after any surprise **(a)** fixes.
  </action>
  <verify>
    <automated>test -f .planning/phases/110-triage-narrow-fixes/110-SUMMARY.md && grep -q 'Deferred v1.24' .planning/phases/110-triage-narrow-fixes/110-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `110-SUMMARY.md` lists finding 0001, 0002, 0003 disposition
    - Section heading contains `Deferred v1.24 seeds`
    - Every deferred finding has `deferred_to:` in frontmatter matching a SEED file
    - `mix ci.all` exits 0 on closeout SHA
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Phase 110 complete per CONTEXT: every filed finding fixed or explicitly deferred
- [ ] ROADMAP criterion #5 satisfied via SUMMARY deferred seeds table
</verification>
