---
phase: "58"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/auth.ex
  - test/threadline/operator_surface/gating_test.exs
  - mix.exs
autonomous: true
requirements:
  - SURF-01
must_haves:
  truths:
    - "Operator surface modules are gated by LiveView availability"
    - "Operator surface modules are grouped in HexDocs"
  artifacts:
    - path: "lib/threadline/operator_surface/router.ex"
      provides: "Module skeleton with LiveView gating"
    - path: "lib/threadline/operator_surface/auth.ex"
      provides: "Module skeleton with LiveView gating"
    - path: "mix.exs"
      provides: "HexDocs grouping for Operator Surface"
  key_links:
    - from: "lib/threadline/operator_surface/router.ex"
      to: "Phoenix.LiveView"
      via: "Code.ensure_loaded?/1"
---

<objective>
Establish the module skeletons, HexDocs grouping, and conditional compilation gating for the Operator Surface.

Purpose: Prevent module compilation if Phoenix LiveView is absent, and group the new surface neatly in documentation. (D-03, D-05)
Output: Gated skeletons for Router and Auth, Mix config update, and gating verification test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/threadline/operator_surface.ex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Gated Skeletons and Update Mix Config</name>
  <files>lib/threadline/operator_surface/router.ex, lib/threadline/operator_surface/auth.ex, mix.exs</files>
  <action>
    Create the two new files with `if Code.ensure_loaded?(Phoenix.LiveView) do defmodule ... end` blocks around their `defmodule` declarations. Follow the file-scope gating pattern seen in the analog file `lib/threadline/operator_surface.ex`.
    In `mix.exs`, add an `"Operator Surface"` group to the `docs/0` function's `groups_for_modules` listing `Threadline.OperatorSurface.Router` and `Threadline.OperatorSurface.Auth`.
  </action>
  <verify>
    <automated>mix format mix.exs lib/threadline/operator_surface/*.ex && mix compile</automated>
  </verify>
  <done>Modules compile without errors and mix docs is prepared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Doc-Contract Gating Test</name>
  <files>test/threadline/operator_surface/gating_test.exs</files>
  <behavior>
    - Test 1: Verifies that `Threadline.OperatorSurface.Router` and `Threadline.OperatorSurface.Auth` are successfully loaded when LiveView is available, and explicitly validates they are absent when LiveView is missing using an `if Code.ensure_loaded?(Phoenix.LiveView) do assert ... else refute Code.ensure_loaded?(...) end` branch.
  </behavior>
  <action>
    Implement a simple unit test suite verifying the gating behavior. Ensure the test uses an `if Code.ensure_loaded?(Phoenix.LiveView)` check with an `else` branch that uses `refute Code.ensure_loaded?(...)` so the CI leg can verify the module is absent when LiveView is missing.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/gating_test.exs</automated>
  </verify>
  <done>Gating is validated via tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Compilation | Gating ensures modules are not present if dependencies are absent |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01-01 | Tampering | mix.exs | accept | Developer machine trust assumed |
</threat_model>

<verification>
mix test test/threadline/operator_surface/gating_test.exs
</verification>

<success_criteria>
Skeletons created and gating tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/58-mount-macro-and-auth-contract/58-01-SUMMARY.md`
</output>