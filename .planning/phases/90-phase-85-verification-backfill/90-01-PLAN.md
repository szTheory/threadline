---
phase: 90-phase-85-verification-backfill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/85-support-lane-surface-audit/85-VERIFICATION.md
  - guides/upgrade-path.md
  - guides/operator-surface.md
  - guides/getting-started-saas.md
  - guides/integration-contracts.md
  - examples/threadline_phoenix/README.md
autonomous: true
requirements:
  - SCOPE-03
  - AUTH-02
  - ADOPT-03
must_haves:
  truths:
    - "Phase 85 closes against the current-tree support-lane claim, not the earlier broader intent: timeline, actor, transaction, shared `/audit` mount, and admin-only export posture are the proven support-lane surfaces, while support-scoped row-history / as-of remains explicitly unclaimed unless separately proven."
    - "One shared `%{assigns: assigns}` callback remains the canonical operator-surface authorization contract across LiveView and HTTP export, with stable granted / denied / error telemetry outcomes."
    - "The v1.21 support lane still uses minimal additive controls only: `authorize_fn`, optional `scope_query_fn`, optional `export_authorize_fn`, and no Threadline-owned role DSL, tenancy DSL, policy engine, or separate `/support` product tree."
  artifacts:
    - path: .planning/phases/85-support-lane-surface-audit/85-VERIFICATION.md
      provides: "Current-tree proof that closes Phase 85 requirements"
      contains: "SCOPE-03"
    - path: test/threadline/operator_surface/auth_test.exs
      provides: "LiveView-face proof for the shared authorize callback and telemetry contract"
      contains: "shared %{assigns: assigns} callback"
    - path: test/threadline/operator_surface/export_auth_plug_test.exs
      provides: "HTTP export-face proof for the shared authorize callback and export override contract"
      contains: "synthetic %{assigns: conn.assigns} mirror"
  key_links:
    - from: guides/upgrade-path.md
      to: .planning/phases/85-support-lane-surface-audit/85-VERIFICATION.md
      via: "the lane taxonomy and row-history `unclaimed` wording must match the phase verdict exactly"
      pattern: "supported|reference|unclaimed|history / as-of"
    - from: test/threadline/operator_surface/auth_test.exs
      to: test/threadline/operator_surface/export_auth_plug_test.exs
      via: "the same `%{assigns: assigns}` contract and telemetry outcomes must hold across LiveView and HTTP export"
      pattern: "granted|denied|error|support_read_only"
    - from: mix.exs
      to: examples/threadline_phoenix/README.md
      via: "the example proof entrypoint and README must both teach one shared `/audit` mount with support-scoped reads and default export denial"
      pattern: "verify.example|/audit|support|forbidden|export_authorize_fn"
---

<objective>
Re-verify the Phase 85 support-lane claim boundary, shared callback contract,
and minimal-controls posture on the current tree, then write the missing
`85-VERIFICATION.md` artifact that closes `SCOPE-03`, `AUTH-02`, and
`ADOPT-03` honestly.

Purpose: replace the old Phase 85 context-only lock with current-tree proof.
Output: a finalized `85-VERIFICATION.md` plus any small literal truth repairs
needed in named docs/tests so the verification artifact does not overclaim.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.21-MILESTONE-AUDIT.md
@.planning/phases/85-support-lane-surface-audit/85-CONTEXT.md
@.planning/phases/85-support-lane-surface-audit/85-RECOMMENDATION.md
@.planning/phases/85-support-lane-surface-audit/85-01-SUMMARY.md
@.planning/phases/85-support-lane-surface-audit/85-02-SUMMARY.md
@.planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md
@.planning/phases/90-phase-85-verification-backfill/90-RESEARCH.md
@.planning/phases/90-phase-85-verification-backfill/90-PATTERNS.md
@guides/upgrade-path.md
@guides/operator-surface.md
@guides/getting-started-saas.md
@guides/integration-contracts.md
@examples/threadline_phoenix/README.md
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@mix.exs
@test/threadline/upgrade_path_doc_contract_test.exs
@test/threadline/operator_surface_doc_contract_test.exs
@test/threadline/getting_started_saas_doc_contract_test.exs
@test/threadline/integration_contracts_doc_contract_test.exs
@test/threadline/example_phoenix_readme_contract_test.exs
@test/threadline/operator_surface/auth_test.exs
@test/threadline/operator_surface/export_auth_plug_test.exs

<interfaces>
From `test/threadline/operator_surface/auth_test.exs`:
```elixir
authorize_fn: fn %{assigns: assigns} ->
  case assigns[:current_user] do
    %{role: :support, organization_id: org_id} ->
      {:ok, %{access: :support_read_only, organization_id: org_id}}
    _ ->
      {:error, :unauthorized}
  end
end
```

From `test/threadline/operator_surface/export_auth_plug_test.exs`:
```elixir
test "case 6b: when :export_authorize_fn is absent, :authorize_fn is called with the synthetic %{assigns: conn.assigns} mirror" do
```

From `mix.exs`:
```elixir
"verify.example": &verify_example/1
```

From `guides/upgrade-path.md`:
```text
history / as-of remains `unclaimed`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the named Phase 85 proof surfaces to the current-tree claim</name>
  <files>guides/upgrade-path.md, guides/operator-surface.md, guides/getting-started-saas.md, guides/integration-contracts.md, examples/threadline_phoenix/README.md</files>
  <read_first>
    <file>guides/upgrade-path.md</file>
    <file>guides/operator-surface.md</file>
    <file>guides/getting-started-saas.md</file>
    <file>guides/integration-contracts.md</file>
    <file>examples/threadline_phoenix/README.md</file>
    <file>test/threadline/upgrade_path_doc_contract_test.exs</file>
    <file>test/threadline/operator_surface_doc_contract_test.exs</file>
    <file>test/threadline/getting_started_saas_doc_contract_test.exs</file>
    <file>test/threadline/integration_contracts_doc_contract_test.exs</file>
    <file>test/threadline/example_phoenix_readme_contract_test.exs</file>
    <file>.planning/phases/85-support-lane-surface-audit/85-CONTEXT.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md</file>
    <file>.planning/v1.21-MILESTONE-AUDIT.md</file>
    <file>.planning/phases/90-phase-85-verification-backfill/90-RESEARCH.md</file>
    <file>.planning/phases/90-phase-85-verification-backfill/90-PATTERNS.md</file>
  </read_first>
  <action>Audit the named doc and doc-contract proof surfaces against the exact current-tree Phase 85 truth before writing the phase verdict. Keep or repair the following literals so every proof surface says the same thing: the support lane is one shared `/audit` mount; the host owns `authorize_fn`, optional `scope_query_fn`, and optional `export_authorize_fn`; support-scoped row-history / as-of remains `unclaimed` on the current repo tree unless separately proven; and Threadline does not introduce a role DSL, tenancy DSL, policy engine, or separate `/support` tree. If all surfaces already say this truthfully, leave them unchanged. If a mismatch exists, make only the smallest literal repair in the affected guide and its paired doc-contract test instead of broad rewrites.</action>
  <acceptance_criteria>
    <criterion>`guides/upgrade-path.md` contains `supported`, `reference`, `unclaimed`, and `history / as-of remains \`unclaimed\`` after the task.</criterion>
    <criterion>`guides/operator-surface.md`, `guides/getting-started-saas.md`, and `guides/integration-contracts.md` each contain `authorize_fn`, `scope_query_fn`, and `export_authorize_fn` and do not introduce any Threadline-owned role or tenancy DSL wording.</criterion>
    <criterion>`examples/threadline_phoenix/README.md` still contains `sigra-reference` and teaches one shared `/audit` mount with support read-scope plus default export denial.</criterion>
    <criterion>If any file is changed, its paired doc-contract test file is updated in the same task so the proof surface remains locked.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/threadline/upgrade_path_doc_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/integration_contracts_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The doc proof surfaces say exactly the same current-tree Phase 85 truth and are safe to cite from the verification artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the callback contract and write the Phase 85 verdict</name>
  <files>.planning/phases/85-support-lane-surface-audit/85-VERIFICATION.md</files>
  <read_first>
    <file>test/threadline/operator_surface/auth_test.exs</file>
    <file>test/threadline/operator_surface/export_auth_plug_test.exs</file>
    <file>test/threadline/operator_surface/controllers/export_controller_test.exs</file>
    <file>test/threadline/operator_surface/live/timeline_live_test.exs</file>
    <file>examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex</file>
    <file>mix.exs</file>
    <file>.planning/phases/85-support-lane-surface-audit/85-CONTEXT.md</file>
    <file>.planning/phases/85-support-lane-surface-audit/85-01-SUMMARY.md</file>
    <file>.planning/phases/85-support-lane-surface-audit/85-02-SUMMARY.md</file>
    <file>.planning/phases/89-contract-lock-final-verification/89-VERIFICATION.md</file>
    <file>.planning/v1.21-MILESTONE-AUDIT.md</file>
    <file>.planning/phases/90-phase-85-verification-backfill/90-RESEARCH.md</file>
  </read_first>
  <action>Create `.planning/phases/85-support-lane-surface-audit/85-VERIFICATION.md` as a current-tree verification artifact, not a restatement of the original Phase 85 intent. The artifact must explicitly verify three truths: `SCOPE-03` closes because the named support-lane claim now covers only the proven surfaces and leaves support-scoped row-history / as-of unclaimed; `AUTH-02` closes because the same `%{assigns: assigns}` callback shape and telemetry outcomes are proven across `Auth.on_mount/4` and `ExportAuthPlug`; and `ADOPT-03` closes because the current docs/example still use only minimal additive controls (`authorize_fn`, `scope_query_fn`, `export_authorize_fn`) with no Threadline-owned DSLs or separate route tree. Use the current runnable example entrypoint `mix verify.example` instead of trying to compile the nested example test file directly from the repo root. Include a commands-run section with the exact test commands used, plus a `Not closed here` section that explicitly lists `SCOPE-01`, `SCOPE-02`, `ADOPT-01`, `ADOPT-02`, `AUTH-01`, `UX-01`, `UX-02`, `DOC-01`, and `DOC-02` as later-phase work so the artifact cannot be mistaken for broader milestone closure.</action>
  <acceptance_criteria>
    <criterion>`.planning/phases/85-support-lane-surface-audit/85-VERIFICATION.md` exists and contains the phrases `SCOPE-03`, `AUTH-02`, `ADOPT-03`, `shared \`%{assigns: assigns}\` callback`, and `unclaimed`.</criterion>
    <criterion>`85-VERIFICATION.md` contains a commands-run section that cites `auth_test.exs`, `export_auth_plug_test.exs`, `timeline_live_test.exs`, `export_controller_test.exs`, and `mix verify.example` explicitly.</criterion>
    <criterion>`85-VERIFICATION.md` states that support-scoped row-history / as-of is not part of the currently proven support-lane claim unless separately proven.</criterion>
    <criterion>`85-VERIFICATION.md` contains a `Not closed here` section that explicitly lists `SCOPE-01`, `SCOPE-02`, `ADOPT-01`, `ADOPT-02`, `AUTH-01`, `UX-01`, `UX-02`, `DOC-01`, and `DOC-02`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/export_auth_plug_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs test/threadline/operator_surface/live/timeline_live_test.exs --max-failures 1 && mix verify.example && rg -n 'SCOPE-03|AUTH-02|ADOPT-03|shared `%\\{assigns: assigns\\}` callback|unclaimed|Not closed here|SCOPE-01|SCOPE-02|ADOPT-01|ADOPT-02|AUTH-01|UX-01|UX-02|DOC-01|DOC-02' .planning/phases/85-support-lane-surface-audit/85-VERIFICATION.md</automated>
  </verify>
  <done>Phase 85 has an explicit current-tree verification verdict that closes only its mapped requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public support-lane docs -> adopter expectations | Overstating the proven support-lane scope would cause adopters to rely on unsupported behavior. |
| LiveView auth contract -> HTTP export contract | If the callback shape or telemetry semantics diverge between the two faces, the product contract becomes ambiguous. |
| Phase verification artifact -> later milestone closure | A sloppy Phase 85 verdict could incorrectly mark later requirements as complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Information disclosure | support-lane claim boundary | mitigate | Keep support row-history / as-of explicitly `unclaimed` in the Phase 85 verdict unless same-pass proof exists. |
| T-90-02 | Repudiation | shared callback contract | mitigate | Cite the exact `auth_test.exs` and `export_auth_plug_test.exs` proof paths and record telemetry outcomes in `85-VERIFICATION.md`. |
| T-90-03 | Elevation of privilege | additive controls posture | mitigate | Verify that the support lane still uses only host-owned seams and default export denial, with no Threadline-owned role or tenancy DSL. |
</threat_model>

<verification>
Run the doc-contract suite, operator auth/export contract tests, and the
example-host operator-surface proof. The phase verdict should then cite only the
current-tree truths those commands actually proved.
</verification>

<success_criteria>
Phase 85 has a truthful current-tree verification artifact that closes
`SCOPE-03`, `AUTH-02`, and `ADOPT-03` without widening the support-lane claim
or smuggling later-phase closure into the record.
</success_criteria>

<output>
After completion, create `.planning/phases/90-phase-85-verification-backfill/90-01-SUMMARY.md`
</output>
