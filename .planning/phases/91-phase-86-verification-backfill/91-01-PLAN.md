---
phase: 91-phase-86-verification-backfill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/threadline/query_test.exs
  - test/threadline/investigation_test.exs
  - test/threadline/operator_surface/transaction_live_test.exs
  - lib/threadline/query.ex
  - lib/threadline/investigation.ex
  - lib/threadline/operator_surface/live/transaction_live.ex
  - lib/threadline/operator_surface/live/row_history_component.ex
autonomous: true
requirements:
  - SCOPE-01
  - SCOPE-02
must_haves:
  truths:
    - "Support-scoped operators can only see scope-allowed audit records across the proven `/audit` read paths on the current tree."
    - "The Phase 91 verdict uses the locked three-layer proof bar: query-level proof, helper/public API parity, and mounted `/audit` transaction-history proof."
    - "If support-scoped row history / as-of still lacks mounted or helper proof after a narrow same-pass repair, the execution outcome keeps that path explicitly `unclaimed` instead of stretching the support-lane claim."
  artifacts:
    - path: test/threadline/query_test.exs
      provides: "Scoped proof for `Threadline.history/3` and `Threadline.as_of/4`"
      contains: "scope_query_fn"
    - path: test/threadline/investigation_test.exs
      provides: "Helper/public API parity proof for `Threadline.row_history/4` and `Threadline.row_history_page/4`"
      contains: "row_history_page/4"
    - path: test/threadline/operator_surface/transaction_live_test.exs
      provides: "Mounted `/audit` transaction-history proof for support-scoped sessions"
      contains: "/audit_scoped/transactions/"
  key_links:
    - from: test/threadline/query_test.exs
      to: lib/threadline/query.ex
      via: "targeted scoped assertions against the existing `row_history_scope_opts/3` and `maybe_apply_scope/2` seam"
      pattern: "scope_query_fn|surface: :row_history"
    - from: test/threadline/investigation_test.exs
      to: lib/threadline/investigation.ex
      via: "helper/public API tests must prove the same scoped behavior as the query layer, not a different contract"
      pattern: "row_history/4|row_history_page/4"
    - from: test/threadline/operator_surface/transaction_live_test.exs
      to: lib/threadline/operator_surface/live/transaction_live.ex
      via: "route-driven mounted proof must exercise the shipped transaction-history flow"
      pattern: "scope={@threadline_scope}|scope_query_fn={@threadline_scope_query_fn}|/history/"
---

<objective>
Prove or narrowly repair the current-tree scoped read-path chain for Phase 86
without widening the support-lane claim beyond what the repo can fully verify
today.

Purpose: close the missing behavioral evidence for `SCOPE-01` and establish the
proof inputs that determine whether `SCOPE-02` is promoted or remains
`unclaimed`.
Output: strengthened scoped query/helper tests, mounted `/audit` transaction
history proof, and only the smallest code repair needed to make that proof
honest on the current tree.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/91-phase-86-verification-backfill/91-CONTEXT.md
@.planning/phases/91-phase-86-verification-backfill/91-RESEARCH.md
@.planning/phases/91-phase-86-verification-backfill/91-PATTERNS.md
@.planning/phases/91-phase-86-verification-backfill/91-VALIDATION.md
@.planning/phases/86-scoped-read-path-closure/86-RESEARCH.md
@.planning/phases/86-scoped-read-path-closure/86-VALIDATION.md
@.planning/phases/90-phase-85-verification-backfill/90-01-SUMMARY.md
@.planning/phases/90-phase-85-verification-backfill/90-02-SUMMARY.md
@lib/threadline/query.ex
@lib/threadline/investigation.ex
@lib/threadline/operator_surface/live/transaction_live.ex
@lib/threadline/operator_surface/live/row_history_component.ex
@test/threadline/query_test.exs
@test/threadline/investigation_test.exs
@test/threadline/operator_surface/transaction_live_test.exs

<interfaces>
From `lib/threadline/query.ex`:
```elixir
defp row_history_scope_opts(schema_module, id, opts) do
  [
    scope: Keyword.get(opts, :scope),
    scope_query_fn: Keyword.get(opts, :scope_query_fn),
    surface: Keyword.get(opts, :surface, :row_history),
    params: %{schema_module: schema_module, id: id}
  ]
end
```

From `lib/threadline/investigation.ex`:
```elixir
def row_history(schema_module, id, filters \\ [], opts \\ []) do
  schema_module
  |> Query.row_history(id, filters, opts)
  |> linked_changes(opts)
end
```

From `lib/threadline/operator_surface/live/transaction_live.ex`:
```elixir
<.live_component
  module={Threadline.OperatorSurface.Live.RowHistoryComponent}
  id="row-history"
  table={@history_table}
  record_id={@history_record_id}
  as_of={@history_as_of}
  base_path={@base_path}
  threadline_schemas={@threadline_schemas}
  repo={@threadline_repo}
  scope={@threadline_scope}
  scope_query_fn={@threadline_scope_query_fn}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove query-level scope enforcement and helper parity per D-01/D-02</name>
  <files>test/threadline/query_test.exs, test/threadline/investigation_test.exs, lib/threadline/query.ex, lib/threadline/investigation.ex</files>
  <read_first>
    <file>test/threadline/query_test.exs</file>
    <file>test/threadline/investigation_test.exs</file>
    <file>lib/threadline/query.ex</file>
    <file>lib/threadline/investigation.ex</file>
    <file>.planning/phases/91-phase-86-verification-backfill/91-CONTEXT.md</file>
    <file>.planning/phases/91-phase-86-verification-backfill/91-RESEARCH.md</file>
    <file>.planning/phases/86-scoped-read-path-closure/86-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Test 1: `Threadline.history/3` applies the host-owned support scope and excludes out-of-scope history rows.
    - Test 2: `Threadline.as_of/4` applies the same support scope and does not reconstruct an out-of-scope snapshot.
    - Test 3: `Threadline.row_history/4` and `Threadline.row_history_page/4` match the scoped query behavior instead of bypassing it.
  </behavior>
  <action>Add explicit scoped assertions to `test/threadline/query_test.exs` for `Threadline.history/3` and `Threadline.as_of/4`, and to `test/threadline/investigation_test.exs` for `Threadline.row_history/4` and `Threadline.row_history_page/4`, using support-style `scope_query_fn` inputs per D-02. If the current tree fails one of those proofs and the gap is small and local per D-05, repair only the existing query/helper seam in `lib/threadline/query.ex` or `lib/threadline/investigation.ex` and rerun the targeted slice immediately. Do not add a new policy abstraction, do not weaken the scope, and do not treat helper tests alone as closure.</action>
  <acceptance_criteria>
    <criterion>`test/threadline/query_test.exs` contains test descriptions with the exact strings `history/3 applies support scope` and `as_of/4 applies support scope`.</criterion>
    <criterion>`test/threadline/investigation_test.exs` contains test descriptions with the exact strings `row_history/4 applies support scope` and `row_history_page/4 applies support scope`.</criterion>
    <criterion>If `lib/threadline/query.ex` changes, it still contains `surface: Keyword.get(opts, :surface, :row_history)` and uses `maybe_apply_scope(` in the row-history path.</criterion>
    <criterion>No file introduced by this task contains `role DSL`, `tenant DSL`, or any new Threadline-owned policy layer wording or code path.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/threadline/query_test.exs test/threadline/investigation_test.exs --max-failures 1</automated>
  </verify>
  <done>The query and helper layers have repeatable scoped proof for all four historical read APIs, or the executor has made the smallest same-pass repair needed to make that proof green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the mounted `/audit` transaction-history path per D-03 and hold the truth-first fallback line</name>
  <files>test/threadline/operator_surface/transaction_live_test.exs, lib/threadline/operator_surface/live/transaction_live.ex, lib/threadline/operator_surface/live/row_history_component.ex</files>
  <read_first>
    <file>test/threadline/operator_surface/transaction_live_test.exs</file>
    <file>lib/threadline/operator_surface/live/transaction_live.ex</file>
    <file>lib/threadline/operator_surface/live/row_history_component.ex</file>
    <file>test/threadline/query_test.exs</file>
    <file>test/threadline/investigation_test.exs</file>
    <file>.planning/phases/91-phase-86-verification-backfill/91-CONTEXT.md</file>
    <file>.planning/phases/91-phase-86-verification-backfill/91-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Test 1: the scoped `/audit` transaction-history route renders in-scope row history through the shipped mounted flow.
    - Test 2: the same mounted flow does not expose out-of-scope row history or as-of content to a support-scoped session.
    - Test 3: any local same-pass repair preserves `scope` and `scope_query_fn` threading through the existing component path.
  </behavior>
  <action>Strengthen `test/threadline/operator_surface/transaction_live_test.exs` so Phase 91 has route-driven proof through the actual `/audit_scoped/transactions/:id/history/:table/:record_id` flow, not an isolated component surrogate, per D-03. If that mounted proof exposes a small local wiring gap, repair only the existing scope threading in `transaction_live.ex` or `row_history_component.ex` and rerun the mounted slice immediately per D-05. Keep coverage as a separate gated surface per D-11, and if mounted proof still cannot meet the same seriousness bar as timeline/transaction proof, leave a clean paper trail for Plan `91-02` to preserve `unclaimed` wording per D-06 instead of inventing new behavior here.</action>
  <acceptance_criteria>
    <criterion>`test/threadline/operator_surface/transaction_live_test.exs` contains the exact route literal `/audit_scoped/transactions/` and a test description containing `scoped transaction history route`.</criterion>
    <criterion>If `lib/threadline/operator_surface/live/transaction_live.ex` or `lib/threadline/operator_surface/live/row_history_component.ex` changes, the mounted path still contains `scope={@threadline_scope}`, `scope_query_fn={@threadline_scope_query_fn}`, and `Threadline.history(` plus `Threadline.as_of(`.</criterion>
    <criterion>No new isolated proof path replaces the mounted route proof; `test/threadline/operator_surface/transaction_live_test.exs` remains the primary mounted verification surface for this task.</criterion>
    <criterion>The task output leaves a clear pass/fail basis for whether support-scoped row history / as-of can be claimed or must remain `unclaimed` in Plan `91-02`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/threadline/operator_surface/transaction_live_test.exs --max-failures 1</automated>
  </verify>
  <done>The current tree has mounted `/audit` proof for the scoped transaction-history flow, or the same-pass repair attempt has established that the later artifact must preserve the narrower `unclaimed` truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host-owned scope callback -> query layer | Untrusted session meaning crosses into database filtering here; missing scope application leaks cross-scope audit history. |
| Query/helper layer -> mounted LiveView route | A lower-level scoped helper is not enough if the shipped `/audit` route bypasses or misthreads it. |
| Mounted proof -> support-lane claim | Overstating route proof would cause adopters to rely on unsupported support visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Information disclosure | `lib/threadline/query.ex` row-history queries | mitigate | Add scoped tests for `history/3`, `as_of/4`, `row_history/4`, and `row_history_page/4`; repair only the existing `maybe_apply_scope` seam if the proof fails. |
| T-91-02 | Elevation of privilege | mounted `/audit` transaction-history flow | mitigate | Prove the real scoped route in `test/threadline/operator_surface/transaction_live_test.exs` and keep `scope` / `scope_query_fn` threading intact through `TransactionLive` and `RowHistoryComponent`. |
| T-91-03 | Repudiation | Phase 91 proof verdict | mitigate | Preserve an explicit pass/fail record for the truth-first fallback so Plan `91-02` can keep `unclaimed` wording when mounted proof is insufficient. |
</threat_model>

<verification>
Follow the targeted proof slices already locked in `91-VALIDATION.md`: first
`query_test.exs` plus `investigation_test.exs`, then
`transaction_live_test.exs`. Do not substitute ad-hoc manual proof for the
mounted route requirement.
</verification>

<success_criteria>
Phase 91 has the full evidence needed to decide the truthful current-tree
support-lane claim for row history / as-of: query-level proof, helper parity,
and mounted `/audit` route proof, with only narrow same-pass repair allowed.
</success_criteria>

<output>
After completion, create `.planning/phases/91-phase-86-verification-backfill/91-01-SUMMARY.md`
</output>
