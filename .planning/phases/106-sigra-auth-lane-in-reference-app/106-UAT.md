---
status: complete
phase: 106-sigra-auth-lane-in-reference-app
source: [106-01-SUMMARY.md, 106-02-SUMMARY.md, 106-03-SUMMARY.md]
automated_by: examples/threadline_phoenix/test/threadline_phoenix_web/sigra_auth_flow_test.exs
started: 2026-05-27T18:00:00Z
updated: 2026-05-27T20:00:00Z
---

## Current Test

[testing complete — automated via CI]

## Tests

### 1. Cold Start Smoke Test
expected: From a stopped server, `mix ecto.setup` then `mix phx.server` in `examples/threadline_phoenix/` boots cleanly; home page loads at :4000 with no migration or Sigra errors in the console.
result: pass
automated_by: sigra_auth_flow_test.exs — users table query + logged-out home GET

### 2. Signup and First Login
expected: Logged out at `/`, follow Register, create a new account with a fresh email. After submit you land signed in (home shows you as logged in, not register/login links only). Help-desk workspace was provisioned (no error flash about org membership).
result: pass
automated_by: sigra_auth_flow_test.exs — POST /users/register + home signed-in assertion

### 3. Session Persists Across Reload
expected: While still signed in from test 2, hard-reload the home page. You remain signed in (session cookie intact); you are not bounced back to logged-out register/login-only state.
result: pass
automated_by: sigra_auth_flow_test.exs — second GET / on same conn

### 4. Admin Opens /audit Operator Surface
expected: Sign in with an email on the dev/test admin allowlist (see `config/dev.exs` `admin_emails`). Visit `/audit`. Page loads (not 403); operator audit UI renders. You can navigate at least one audit view without a crash.
result: pass
automated_by: sigra_auth_flow_test.exs — admin@example.com + GET /audit/transactions/...

### 5. Agent Role Gets 403 on /audit
expected: Sign in as a user who is an org agent but NOT on the admin allowlist (register a normal user or use a non-admin test account). Visit `/audit`. You receive 403 Forbidden, not the operator surface.
result: pass
automated_by: sigra_auth_flow_test.exs — agent membership + GET /audit

### 6. Logout Clears Session
expected: While signed in, use Log out (DELETE `/users/log_out` or the UI link). After logout, home shows register/login again; revisiting `/audit` while logged out does not show the operator surface as an authenticated admin.
result: pass
automated_by: sigra_auth_flow_test.exs — DELETE log_out + session cleared + audit 403

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
