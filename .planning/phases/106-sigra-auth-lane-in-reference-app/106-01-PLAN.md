---
phase: 106-sigra-auth-lane-in-reference-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/mix.exs
  - examples/threadline_phoenix/lib/threadline_phoenix_web.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/components/layouts.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/components/core_components.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/gettext.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_controller.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_html.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/registration_controller.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/accounts/**
  - examples/threadline_phoenix/lib/threadline_phoenix_web/user_auth.ex
  - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/session_controller.ex
  - examples/threadline_phoenix/priv/repo/migrations/*sigra*
  - examples/threadline_phoenix/config/config.exs
  - examples/threadline_phoenix/config/test.exs
  - examples/threadline_phoenix/test/support/fixtures/auth_fixtures.ex
  - examples/threadline_phoenix/test/support/conn_case_helpers.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
  - examples/threadline_phoenix/test/threadline_phoenix/help_desk_provision_test.exs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "Sigra users table exists after mix ecto.migrate"
    - "GET / shows register/login links when logged out"
    - "POST /users/register creates user + help-desk org membership"
    - "POST /users/log_in establishes session cookie"
  artifacts:
    - path: examples/threadline_phoenix/lib/threadline_phoenix/accounts.ex
      provides: "Sigra Accounts context"
    - path: examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/registration_controller.ex
      provides: "Register + provision hook"
  key_links:
    - from: registration_controller.ex
      to: help_desk.ex
      via: provision_default_workspace_for_user/2 after {:ok, user}
---

<objective>
Ship AUTH-01 and AUTH-02 foundation: Sigra install, Phoenix HTML bootstrap, walkthrough auth routes (register/login/logout), home page, and help-desk workspace auto-provision on registration.

Scope: `examples/threadline_phoenix/` only. Do not modify repo `lib/`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-sigra-auth-lane-in-reference-app/106-CONTEXT.md
@.planning/phases/106-sigra-auth-lane-in-reference-app/106-RESEARCH.md
@.planning/phases/106-sigra-auth-lane-in-reference-app/106-UI-SPEC.md
@guides/integrations/sigra.md
@examples/threadline_phoenix/mix.exs
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
</context>

<scope_guard>
**ALLOWED:** `examples/threadline_phoenix/**` except Plan 02/03 files

**FORBIDDEN:** `lib/threadline/**`, `OperatorUser` plug (Plan 02), `login_via_sigra` (Plan 03), enabling `--organizations` or `--live` on sigra.install
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-106-01 | Weak password hashing in test leaking to prod | medium | Keep Sigra argon2 test inject in `config/test.exs` only |
| T-106-02 | Mass-assignment of admin/org via registration params | high | Do not accept `organization_id` or `role` from form params; provision server-side only |
| T-106-03 | Dual org tables (Sigra + help-desk) | medium | Install with `--no-organizations` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Sigra install + migrate</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/accounts.ex, examples/threadline_phoenix/priv/repo/migrations/</files>
  <read_first>
    - examples/threadline_phoenix/mix.exs
    - .planning/phases/106-sigra-auth-lane-in-reference-app/106-RESEARCH.md (§1 Sigra Install)
    - .planning/phases/106-sigra-auth-lane-in-reference-app/106-CONTEXT.md (D-106-03a)
  </read_first>
  <action>
From `examples/threadline_phoenix/`:

```bash
mix sigra.install Accounts User users --no-live --no-organizations --no-passkeys --no-admin --yes
mix ecto.migrate
```

Ensure `{:sigra, "~> 0.2"}` remains in `mix.exs` (remove `optional: true` if CI needs unconditional compile).

Accept router injection: `import ThreadlinePhoenixWeb.UserAuth`, `plug :fetch_current_scope` in `:browser`, Sigra `/users/*` routes.

Do NOT enable `--organizations`, `--live`, `--passkeys`, or `--admin`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix ecto.migrate --quiet && mix run -e 'ThreadlinePhoenix.Repo.query!("SELECT 1 FROM users LIMIT 0"); IO.puts("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - `lib/threadline_phoenix/accounts.ex` exists
    - At least one migration filename contains `sigra` or `users`
    - `mix ecto.migrate` exits 0
    - Query on `users` table succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phoenix HTML stack bootstrap</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web.ex, examples/threadline_phoenix/lib/threadline_phoenix_web/components/layouts.ex, examples/threadline_phoenix/lib/threadline_phoenix_web/components/core_components.ex, examples/threadline_phoenix/lib/threadline_phoenix_web/gettext.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix_web.ex
    - .planning/phases/106-sigra-auth-lane-in-reference-app/106-UI-SPEC.md
    - examples/threadline_phoenix/AGENTS.md (Phoenix 1.8 layout rules)
  </read_first>
  <action>
Add missing Phoenix 1.8 HTML infrastructure (app was `--no-html`):

1. Add `:html` quote block to `threadline_phoenix_web.ex` with `use Phoenix.Component`, import `Layouts`, `CoreComponents`, `Gettext`.

2. Create `lib/threadline_phoenix_web/components/layouts.ex` with `Layouts.app/1` accepting `flash` and `current_scope` assigns (pass through to inner content).

3. Create `lib/threadline_phoenix_web/components/core_components.ex` with at minimum: `<.input>`, `<.button>`, `<.simple_form>`, `<.flash>`, `<.header>`, `<.icon>` (hero icons).

4. Create `lib/threadline_phoenix_web/gettext.ex` backend module.

5. Ensure Swoosh dev mailbox route when `dev_routes: true` in `config/dev.exs` (forward `/dev/mailbox`).

Copy patterns from Phoenix 1.8.5 defaults — do NOT run `phx.gen.auth`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `threadline_phoenix_web.ex` contains `def html do`
    - `layouts.ex` contains `def app(`
    - `core_components.ex` contains `def input(`
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: RegistrationController + provision hook</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/registration_controller.ex, examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/registration_html.ex (after install)
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk/org_membership.ex
    - .planning/phases/106-sigra-auth-lane-in-reference-app/106-CONTEXT.md (D-106-01, D-106-01b)
    - .planning/phases/106-sigra-auth-lane-in-reference-app/106-RESEARCH.md (§2 Registration)
  </read_first>
  <action>
Add `HelpDesk.provision_default_workspace_for_user/2` to `help_desk.ex`:

```elixir
@spec provision_default_workspace_for_user(String.t(), keyword()) ::
  {:ok, Organization.t()} | {:error, term()}
```

Implementation: `Ecto.Multi` creating `Organization` (slug from `Keyword.get(opts, :slug)` or `"org-" <> random suffix`), `OrgMembership` with `role: Keyword.get(opts, :role, "agent")`, `Agent` with same `user_id`. Idempotent on `(organization_id, user_id)` unique index — return existing org if membership exists. `user_id` is Sigra user id string (UUID).

Add `RegistrationController` (Sigra 0.2.5 gap):

- `new/2` renders `RegistrationHTML`
- `create/2` calls `Accounts.register_user/1`; on `{:ok, user}` call `HelpDesk.provision_default_workspace_for_user(to_string(user.id))`, then `UserAuth.log_in_user(conn, user)` or redirect to login per Sigra pattern; on error re-render with changeset

Do NOT rely on `on_register` config hook — Sigra does not invoke it on register.

Optionally update `session.active_organization_id` via `Sigra.SessionStores.Ecto.update_active_organization/3` after provision.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/help_desk_provision_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `help_desk.ex` contains `def provision_default_workspace_for_user(`
    - `registration_controller.ex` exists with `def create(`
    - `help_desk_provision_test.exs` passes (provisions org + membership for fixture user_id)
    - Second call to provision for same user_id returns same org (idempotent)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: PageController home + README setup steps</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_controller.ex, examples/threadline_phoenix/lib/threadline_phoenix_web/controllers/page_html.ex, examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex, examples/threadline_phoenix/README.md</files>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
    - .planning/phases/106-sigra-auth-lane-in-reference-app/106-UI-SPEC.md (Routes table)
    - examples/threadline_phoenix/README.md
  </read_first>
  <action>
Add `PageController` with `home/2`:

- Logged out (`current_scope.user` nil): links to `~p"/users/register"` and `~p"/users/log_in"`
- Logged in: link to `~p"/audit"`

Add route in `scope "/", ThreadlinePhoenixWeb` with `pipe_through :browser`:

```elixir
get "/", PageController, :home
```

Update `examples/threadline_phoenix/README.md` setup section with ordered steps:

1. `mix deps.get`
2. `mix sigra.install Accounts User users --no-live --no-organizations --no-passkeys --no-admin --yes` (if not already run)
3. `mix ecto.setup`
4. `mix phx.server`

Document register/login/logout URLs for walkthrough.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix compile --warnings-as-errors && rg 'PageController' lib/threadline_phoenix_web/router.ex && rg 'users/register' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `router.ex` contains `PageController, :home`
    - `page_html.ex` contains `/users/register`
    - `README.md` documents `mix sigra.install` command verbatim
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan-level: `cd examples/threadline_phoenix && mix test test/threadline_phoenix/help_desk_provision_test.exs && mix compile --warnings-as-errors`
</verification>
