---
phase: 65-exports-ui-parity
verified: 2026-05-07T16:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 65: Exports UI Parity Verification Report

**Phase Goal:** Operators can download the currently-filtered timeline view as CSV or JSON without leaving the surface, with the same filter vocabulary and the same file format Mix-task users get.

**Verified:** 2026-05-07T16:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1  | Raw timeline browse LV exposes Download CSV/JSON affordances; clicking redirects to a Phoenix controller endpoint that re-validates via `validate_timeline_filters!/1` and authorizes via the same `:authorize_fn` contract | ✓ VERIFIED | `lib/threadline/operator_surface/live/timeline_live.ex:236-238` renders three `<.link href={...} download>` anchors targeting `/exports/changes.{csv,json,ndjson}`. `lib/threadline/operator_surface/router.ex:107-109` emits `get(...)` routes for all three formats. `lib/threadline/operator_surface/controllers/export_controller.ex:228` calls `Threadline.Query.validate_timeline_filters!/1` via `safe_validate/1`. `lib/threadline/operator_surface/export_auth_plug.ex:37,51` authorizes via either `:export_authorize_fn` (Conn-shaped) or `:authorize_fn` with synthetic `%{assigns: conn.assigns}` mirror — preserves the v1.17 LV `:authorize_fn.(socket)` contract verbatim. The "HTTP 303" wording in the SC is intentionally implemented as a plain-anchor `<.link href download>` per CONTEXT.md D-15 (and PR #2611) — the GET hits the controller directly with no server-side redirect; this is a documented, superior approach (no LV teardown, identical end-to-end semantics). |
| 2  | Pre-flight `count_matching/2` renders match count + truncation banner; controller streams windows above 5,000 rows via `Plug.Conn.send_chunked/2` + `stream_changes/2`, sends iodata synchronously below | ✓ VERIFIED | `lib/threadline/operator_surface/live/timeline_live.ex:119-133` runs `count_matching(filters, cap: 10_001)` AND `Query.timeline_page/2` in parallel via `Task.async`/`Task.await(_, 8_000)`. `timeline_live.ex:253-267` renders the count line + two-band truncation banner (informational at >5_000 / warning at ≥10_001). `lib/threadline/operator_surface/controllers/export_controller.ex:67-71` dispatches: `count <= 5_000` → iodata `send_resp/3`; `count > 5_000` → `send_chunked/2` + `Stream.take(10_000)` + `Enum.reduce_while/3` + `Plug.Conn.chunk/2`. (Note: chunked path uses `stream_export_rows/2` rather than `stream_changes/2` to provide the join-projected fields the formatters need — `stream_changes/2` returns lean structs lacking `tx_*`/`aa_*` fields. The streaming spirit of the SC is fully met; the choice of helper preserves byte-equality with the iodata path.) `mix test --include slow` Case 4 passes with `conn.state == :chunked` after seeding 5,001 rows. |
| 3  | Downloaded files have UTC-ISO filenames; `Content-Disposition` per RFC 5987; RFC 4180 CSV with `text/csv; charset=utf-8` and no BOM; JSON wrapped + NDJSON variants matching Mix-task — identical filenames + bytes for identical filters | ✓ VERIFIED | `lib/threadline/operator_surface/exports/filename.ex:36-44` produces `threadline-changes-YYYY-MM-DDTHH-MM-Z.{csv\|json\|ndjson}` via `DateTime.shift_zone!/2` + `Calendar.strftime("%Y-%m-%dT%H-%MZ")` (HYPHEN between hours and minutes for Windows compatibility, minute granularity per EXPO-04 example). `export_controller.ex:206,209,212` sets exact literals `text/csv; charset=utf-8`, `application/json; charset=utf-8`, `application/x-ndjson; charset=utf-8` via `put_resp_header/3` (avoiding `put_resp_content_type/2`'s double-charset bug). `export_controller.ex:216` emits RFC 5987 dual-emit `attachment; filename="..."; filename*=UTF-8''...`. `export_controller.ex:221` sets `Cache-Control: no-store`. `test/threadline/operator_surface/exports_mix_parity_test.exs` proves byte-identical output: 3 cases (CSV / JSON wrapped / NDJSON) all pass. CSV and NDJSON use raw byte equality; JSON wrapped strips the `generated_at` ISO timestamp (intentional audit-trail provenance, captured at format time per call) before structural-equality comparison — both outputs independently asserted to contain the `generated_at` key as belt-and-braces. |
| 4  | Doc-contract test locks button labels, filename format, and content-type literals; integration test asserts chunked-stream above iodata threshold; parity assertion proves Mix-task and controller produce identical files | ✓ VERIFIED | `test/threadline/operator_surface/exports_doc_contract_test.exs` — 33 assertions across 11 describe blocks pinning button labels (Download CSV/JSON/NDJSON), route literals, content-type literals, filename format, file-scope optional-deps gates, atom-safety refutations (`String.to_atom\b`), `phx-change=` carry-forward refutation, FilterParams cross-references, chunked-stream pattern literals (`Stream.take(10_000)`, `Enum.reduce_while`, `Plug.Conn.chunk`), and telemetry event reuse. `test/threadline/operator_surface/controllers/export_controller_test.exs` — 6 cases including Case 4 (`@tag :slow`, seeds 5,001 rows, asserts `conn.state == :chunked`). `test/threadline/operator_surface/exports_mix_parity_test.exs` — 3 cases (CSV / JSON wrapped / NDJSON) asserting `File.read!(tmp_path) == response_body` (modulo `generated_at` for JSON wrapped). All 42 trifecta tests pass under `mix test test/threadline/operator_surface/exports_doc_contract_test.exs test/threadline/operator_surface/exports_mix_parity_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs --include slow`. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `lib/threadline/operator_surface/exports/filename.ex` | Pure-stdlib helper, `for/2` returns canonical UTC-minute filename for csv/json/ndjson; no file-scope `Code.ensure_loaded?` gate | ✓ VERIFIED | 47 lines; first line is `defmodule Threadline.OperatorSurface.Exports.Filename do` (no gate); contains `~w(csv json ndjson)`, `%Y-%m-%dT%H-%MZ`, `DateTime.shift_zone!`. |
| `lib/threadline/operator_surface/exports/filter_params.ex` | Pure-stdlib shared parser, `parse/1` + `filters_raw_from_params/1`; uses `String.to_existing_atom` only; no file-scope gate | ✓ VERIFIED | 174 lines; first line is `defmodule Threadline.OperatorSurface.Exports.FilterParams do` (no gate); 3 occurrences of `String.to_existing_atom`; `String.to_atom\b` regex finds zero matches; datetime-local pad rule (`if String.length(str) == 16`) preserved verbatim from TimelineLive private helpers. |
| `lib/threadline/export.ex` | `count_matching/2` accepts additive `:cap` opt via windowed-count subquery; `format_changes_iodata/3`, `csv_header/1`, `stream_export_rows/2` additive helpers; existing public surface byte-unchanged | ✓ VERIFIED | 436 lines; line 167 `Keyword.get(opts, :cap)`; line 179 `from(sub in subquery(capped), select: count())` — windowed-count subquery, NOT `EXISTS`; line 185 preserves `{:ok, %{count: count}}` return shape. New helpers at lines 202, 238, 316. Existing `to_csv_iodata`, `to_json_document`, `stream_changes` byte-unchanged; Mix-task `mix threadline.export` test passes (1/1). |
| `lib/threadline/operator_surface/controllers/export_controller.ex` | Phoenix.Controller with three actions, file-scope gated on `Phoenix.Controller`; threshold dispatch via `count_matching` `:cap`; chunked path uses `Stream.take(10_000)` + `Enum.reduce_while` + `Plug.Conn.chunk`; exact content-type literals; RFC 5987 dual-emit | ✓ VERIFIED | 242 lines; first line `if Code.ensure_loaded?(Phoenix.Controller) do`; three actions `def csv/2`, `def json/2`, `def ndjson/2`; `Stream.take(@max_rows)` (=10_000) at line 114; `Enum.reduce_while` at line 116; `Plug.Conn.chunk(conn, ...)` at line 119; three exact content-type literals at lines 206/209/212; RFC 5987 dual-emit at line 216; `Cache-Control: no-store` at line 221. |
| `lib/threadline/operator_surface/export_auth_plug.ex` | Conn-shaped twin of `Auth.on_mount/4`; same telemetry event; same five-arm authorize case-tree; same `:threadline_scope` assign key; halt-with-403 plain text (no redirect) | ✓ VERIFIED | 104 lines; first line `if Code.ensure_loaded?(Phoenix.Controller) do`; `@behaviour Plug` at line 28; same telemetry event `[:threadline, :operator_surface, :authorize]` at line 97; five-arm case at lines 56-75 (`:ok` / `true` / `{:ok, scope} when is_map` / `{:ok, scope}` / `_`); D-20 dispatch at lines 44-53 (`:export_authorize_fn` preferred / synthetic `%{assigns: conn.assigns}` mirror fallback); 403 plain-text halt at lines 81-88. |
| `lib/threadline/operator_surface/router.ex` | Macro grows sibling `scope <path>/exports` block with three GET routes; gated on `Code.ensure_loaded?(Phoenix.Controller)`; ExportAuthPlug pipeline; alias for hygiene | ✓ VERIFIED | 116 lines; first line `if Code.ensure_loaded?(Phoenix.LiveView) do` (UNCHANGED); macro at line 41 with new `:exports` opt at line 44; sibling scope branch at lines 78-111; pipeline `:threadline_exports` with `plug Threadline.OperatorSurface.ExportAuthPlug, unquote(opts)` at lines 85-87; three `get/3` routes at lines 107-109 with `ExportController, :csv\|:json\|:ndjson` per-line short form. |
| `lib/threadline/operator_surface/live/timeline_live.ex` | Renders three `<.link href download>` anchors; runs `count_matching` parallel with `timeline_page` via `Task.async`/`Task.await(_, 8_000)`; uses cap 10_001; renders count status line + two-band truncation banner; delegates parsing to FilterParams; existing file-scope gate preserved | ✓ VERIFIED | 350 lines; first line `if Code.ensure_loaded?(Phoenix.LiveView) do` (UNCHANGED); `alias Threadline.OperatorSurface.Exports.FilterParams` at line 7; `Task.async` × 2 at lines 120/125; `cap: 10_001` at line 121; `Task.await(_, 8_000)` × 2 at lines 132/133; three download anchors at lines 236-238 (each with bare `download` HTML attribute, PR #2611); count status line at line 253; band-1 informational at 258-260; band-2 warning at 264-266; canonical literals `Large export — will stream in chunks.` (em-dash U+2014) and `Truncated to first 10,000 rows.` confirmed. `phx-change=` finds zero matches (Phase 64 carry-forward). |
| `lib/threadline/operator_surface/style.ex` | CSS rules for `.download-button`, `.match-count-status`, `.truncation-banner.{informational,warning}` | ✓ VERIFIED | 255 lines; rules at lines 178, 207, 214, 221, 226 — namespaced under `.threadline-ui` (Phase 64 Footgun F-8); amber palette `#FEF3C7`/`#92400E`/`#F59E0B` distinct from `.filter-error` red. |
| `test/threadline/operator_surface/exports_doc_contract_test.exs` | Pure source-reading test pinning all EXPO-05 literals | ✓ VERIFIED | 12,325 bytes; 33 assertions across 11 describe blocks; runs in 0.07s. |
| `test/threadline/operator_surface/controllers/export_controller_test.exs` | Phoenix.ConnTest integration with nested test-Endpoint; six cases including chunked path at >5,000 rows | ✓ VERIFIED | 8,815 bytes; six tests; Case 4 `@tag :slow` seeds 5,001 rows, asserts `conn.state == :chunked`. |
| `test/threadline/operator_surface/exports_mix_parity_test.exs` | D-28 byte-equality across all three formats; uses `Mix.Task.reenable` | ✓ VERIFIED | 8,810 bytes; three byte-equality cases (CSV / JSON wrapped / NDJSON); `Code.require_file/2` at file top loads the sibling Endpoint so the file runs in isolation. |
| `test/threadline/operator_surface/exports/filename_test.exs` | Filename helper unit tests | ✓ VERIFIED | 2,172 bytes; 7 tests covering all three formats + non-UTC normalization + `FunctionClauseError` for unsupported formats. |
| `test/threadline/operator_surface/exports/filter_params_test.exs` | FilterParams unit tests including atom-safety refute | ✓ VERIFIED | 4,666 bytes; 16 tests across 3 describe blocks (`parse/1`, `filters_raw_from_params/1`, atom-safety regex refute). |
| `test/threadline/operator_surface/export_auth_plug_test.exs` | Six unit cases covering all five authorize arms + D-20 dispatch | ✓ VERIFIED | 5,259 bytes; 8 tests across 4 describe blocks. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `timeline_live.ex` (download anchors) | `router.ex` /exports/changes.{csv,json,ndjson} routes | Anchor href `#{@base_path}/exports/changes.<EXT>?#{@filter_query}` consumed by sibling controller scope | ✓ WIRED | Three href literals at `timeline_live.ex:236-238`; three matching `get(...)` routes at `router.ex:107-109`. |
| `timeline_live.ex` `handle_params/3` | `FilterParams.parse/1` + `count_matching/2` | `FilterParams.parse(params)` for parsing; `Task.async(fn -> Export.count_matching(filters, cap: 10_001, repo: ...) end)` for parallel pre-flight | ✓ WIRED | `timeline_live.ex:79` calls `FilterParams.parse(params)`; line 121 calls `Export.count_matching(filters, cap: 10_001)`; line 132 destructures `{:ok, %{count: count}}` (Pitfall 5 destructure). |
| `router.ex` macro | `ExportAuthPlug` | `pipeline :threadline_exports do plug Threadline.OperatorSurface.ExportAuthPlug, unquote(opts) end` + `pipe_through :threadline_exports` inside scope | ✓ WIRED | `router.ex:85-87` defines pipeline with plug; `router.ex:105` `pipe_through(:threadline_exports)` inside scope. |
| `ExportAuthPlug` | `Auth.on_mount/4` (telemetry contract) | Same `[:threadline, :operator_surface, :authorize]` event with `:granted\|:denied\|:error` results; same `:threadline_scope` assign key; same five-arm case-tree | ✓ WIRED | `export_auth_plug.ex:97` event literal; `:threadline_scope` assigned at lines 67/71; granted/denied/error telemetry at lines 58/62/66/70/74/77. |
| `ExportController` | `FilterParams.parse/1` + `validate_timeline_filters!/1` + `count_matching/2` + `to_csv_iodata`/`to_json_document`/`stream_export_rows` + `Filename.for/2` | Single dispatch path through all five lib calls per request | ✓ WIRED | `export_controller.ex:57` `FilterParams.parse(params)`; line 228 `Threadline.Query.validate_timeline_filters!/1`; line 62 `Export.count_matching(filters, cap: @max_rows + 1)`; line 83/89/96 `Export.{to_csv_iodata,to_json_document}`; line 113 `Export.stream_export_rows(filters, page_size: 1_000)`; line 215 `Filename.for(ext, DateTime.utc_now())`. |
| `ExportController` chunked path | `Plug.Conn.send_chunked/2` + `Plug.Conn.chunk/2` + `Stream.take(10_000)` + `Enum.reduce_while` | Headers BEFORE `send_chunked/2` (Pitfall 2); reduce_while halts cleanly on `:closed`/`:error` | ✓ WIRED | `export_controller.ex:65` headers set before line 104 `send_chunked(conn, 200)`; lines 113-124 implement `Stream.take(@max_rows)` → `Stream.chunk_every` → `Enum.reduce_while` → `Plug.Conn.chunk` with `:cont`/`:halt` per chunk. |
| Mix task vs ExportController | `Threadline.Export.{to_csv_iodata, to_json_document, stream_export_rows}` (same lib) + `FilterParams` (shared parser) | Both surfaces call identical lib helpers; FilterParams is byte-equivalent to TimelineLive's old inline parser; parity test asserts byte equality | ✓ WIRED | `exports_mix_parity_test.exs` 3/3 byte-equality cases pass; doc-contract assertion confirms BOTH `TimelineLive` AND `ExportController` reference shared `FilterParams` module. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `TimelineLive` render — `@match_count` | `socket.assigns.match_count` | `Task.await(count_task, 8_000)` calling `Export.count_matching(filters, cap: 10_001, repo: socket.assigns.repo)` which runs windowed-count subquery via `Ecto.Query.subquery` against `audit_changes` join `audit_transactions` | ✓ FLOWING | Real data: integration tests Cases 16-18 confirm `format_count(@match_count)` renders 7 / 5,001 / 10,000+ for seeded rows. |
| `TimelineLive` render — `@filter_query` | `socket.assigns.filter_query` | `build_canonical_query(socket.assigns.filters_raw)` from operator's URL-submitted filter raw map | ✓ FLOWING | Integration tests assert hrefs are non-empty and contain query-string values. Reset to `""` in error branches so stale state doesn't leak. |
| `ExportController` CSV body | `iodata` (sync) or chunk batches (chunked) | `Export.to_csv_iodata(filters, max_rows: 10_000)` for sync; `Export.stream_export_rows(filters, page_size: 1_000) \|> Stream.take(10_000) \|> Stream.chunk_every(500)` paginating `Query.export_changes_query/1` via `(captured_at, id)` keyset cursor | ✓ FLOWING | `export_controller_test.exs` Case 1 seeds 10 rows, gets `.csv`, asserts 11-line body (header + 10 rows). Case 4 seeds 5,001 rows, asserts body line-count >= 5,001 + `conn.state == :chunked`. |
| `ExportController` Content-Disposition | `disposition` (filename interpolation) | `Filename.for(ext, DateTime.utc_now())` → `Calendar.strftime("%Y-%m-%dT%H-%MZ")` | ✓ FLOWING | `exports_doc_contract_test.exs` live-executes `Filename.for/2` against `~U[2026-05-06 12:00:00.000Z]` and asserts canonical literal output. Integration test Case 1 asserts `Content-Disposition` header matches the canonical pattern. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Operator-surface tests pass including chunked-stream | `mix test test/threadline/operator_surface/ --include slow` | `122 tests, 0 failures` in 1.3s | ✓ PASS |
| EXPO-05 trifecta (doc-contract + integration + parity) passes | `mix test test/threadline/operator_surface/exports_doc_contract_test.exs test/threadline/operator_surface/exports_mix_parity_test.exs test/threadline/operator_surface/controllers/export_controller_test.exs --include slow` | `42 tests, 0 failures` in 0.5s | ✓ PASS |
| Optional-deps invariant preserved (capture-only adopters compile cleanly) | `mix verify.compile_no_optional` (= `compile --no-optional-deps --warnings-as-errors`) | EXIT=0, no warnings | ✓ PASS |
| Phoenix.Controller file-scope gate present on controller | `head -1 lib/threadline/operator_surface/controllers/export_controller.ex` | `if Code.ensure_loaded?(Phoenix.Controller) do` | ✓ PASS |
| Phoenix.Controller file-scope gate present on plug | `head -1 lib/threadline/operator_surface/export_auth_plug.ex` | `if Code.ensure_loaded?(Phoenix.Controller) do` | ✓ PASS |
| No file-scope gate on pure-stdlib helpers | `grep -c "Code.ensure_loaded?" lib/threadline/operator_surface/exports/filename.ex lib/threadline/operator_surface/exports/filter_params.ex` | both 0 | ✓ PASS |
| No `String.to_atom\b` (atom-leak vector closed) on HTTP-side parser | `grep -E 'String\.to_atom\b' lib/threadline/operator_surface/exports/filter_params.ex` | no matches | ✓ PASS |
| No `phx-change=` in TimelineLive (Phase 64 carry-forward) | `grep -c "phx-change" lib/threadline/operator_surface/live/timeline_live.ex` | 0 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | -------------- | ----------- | ------ | -------- |
| EXPO-03 | 65-02, 65-03 | Download CSV/JSON affordances on raw timeline browse LV; LV redirects (HTTP 303) to Phoenix controller endpoint that re-validates via `validate_timeline_filters!/1` and authorizes via the same `:authorize_fn` contract as the LV mount | ✓ SATISFIED | LV anchors (timeline_live.ex:236-238); controller validation (export_controller.ex:228); auth via `:authorize_fn` synthetic mirror (export_auth_plug.ex:51) preserves the v1.17 LV contract verbatim. The "303 redirect" wording is implemented as plain `<.link href download>` per CONTEXT.md D-15 + PR #2611 (avoids LV teardown — superior approach). |
| EXPO-04 | 65-01, 65-02, 65-03 | Pre-flight `count_matching/2` renders match count + truncation banner; controller streams above 5,000 rows via `send_chunked/2` + streaming helper; iodata below; UTC-ISO filenames; RFC 5987 `Content-Disposition`; RFC 4180 CSV with `text/csv; charset=utf-8` no BOM; JSON wrapped + NDJSON variants matching Mix-task | ✓ SATISFIED | Pre-flight at timeline_live.ex:121, banner at 253-267; threshold dispatch at export_controller.ex:67; chunked path uses `stream_export_rows/2` (the join-projected variant required for byte-equality with iodata path); Filename helper produces `threadline-changes-<UTC-ISO>.{csv\|json\|ndjson}`; RFC 5987 dual-emit at export_controller.ex:216; three exact content-type literals at 206/209/212; integration tests confirm body shape for all three formats. |
| EXPO-05 | 65-04 | Doc-contract test locks button labels, filename format, content-type literals; integration test asserts chunked-stream completes above iodata threshold; parity assertion proves Mix-task and controller produce identical files | ✓ SATISFIED | Doc-contract test (33 assertions) + chunked integration test (Case 4 with 5,001 rows + `conn.state == :chunked`) + parity test (3 cases byte-identical for CSV / JSON wrapped (sans `generated_at`) / NDJSON). All 42 trifecta tests pass. |

### Anti-Patterns Found

None. The implementation is substantive throughout. Spot checks for stub patterns (TODO, FIXME, placeholder, hardcoded empty returns) returned no false-positives within Phase 65 scope. Three socket assigns initialized to safe defaults (`:match_count = 0`, `:filter_query = ""`, `:base_path = nil`) are intentional first-render defaults that are overwritten by `handle_params/3` — not stubs.

### Human Verification Required

None. All success criteria are verifiable programmatically through the existing test suite (operator_surface tests, doc-contract test, integration test, parity test) and have all passed. No visual / real-time / external-service behaviors require human review for this phase — the surface is read-only CSV / JSON / NDJSON serving with no UI animation, no scheduled jobs, no third-party integrations.

### Gaps Summary

No gaps found. All four ROADMAP success criteria are observably true in the codebase:

1. Download CSV / JSON / NDJSON affordances render on the LV; clicking each routes to a Phoenix controller that re-validates filter params via `validate_timeline_filters!/1` and authorizes via the same `:authorize_fn` contract (preserved verbatim through the synthetic-mirror dispatch). The "HTTP 303" wording is intentionally implemented as a direct-anchor GET (D-15) — the controller is hit directly by the browser; this is functionally equivalent to a redirect-to-controller flow and avoids LV teardown.
2. Pre-flight `count_matching/2` (capped at 10_001 for the LV's "10,000+" approximation) runs concurrently with `timeline_page/2` on every Apply; the count line + two-band truncation banner render above the timeline; the controller dispatches on the 5,000-row threshold. The chunked path uses `stream_export_rows/2` rather than `stream_changes/2` (an additive Phase 65-02 helper) to preserve byte-equality with the iodata path — the streaming spirit of the SC is fully preserved.
3. Filenames use UTC-minute granularity in the canonical `threadline-changes-<UTC-ISO>.{csv|json|ndjson}` shape; `Content-Disposition` uses RFC 5987 dual-emit; CSV uses RFC 4180 (`text/csv; charset=utf-8`, no BOM); JSON wrapped + NDJSON match `mix threadline.export` flag output byte-for-byte (modulo the intentionally-different `generated_at` ISO timestamp on JSON wrapped, which is asserted to exist independently in both outputs).
4. Doc-contract test (33 assertions across 11 describe blocks) + chunked-stream integration test (Case 4 seeds 5,001 rows, asserts `conn.state == :chunked`) + Mix-task vs controller byte-equality parity test (3 cases). All pass.

The optional-deps invariant is preserved: `mix verify.compile_no_optional` exits 0; the controller and auth plug are file-scope-gated on `Phoenix.Controller`; the two pure-stdlib helpers (`Filename`, `FilterParams`) are intentionally NOT gated; the router macro's new sibling-scope branch is gated at host compile time on `Code.ensure_loaded?(Phoenix.Controller)`. The Mix task `mix threadline.export` is byte-unchanged in behavior — it calls `count_matching(filters, [])` with no `:cap` and continues to return the unbounded count.

---

*Verified: 2026-05-07T16:00:00Z*
*Verifier: Claude (gsd-verifier)*
