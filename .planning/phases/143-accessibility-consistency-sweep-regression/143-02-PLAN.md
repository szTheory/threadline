---
phase: 143-accessibility-consistency-sweep-regression
plan: "02"
type: execute
wave: 2
depends_on: ["143-01"]
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts
  - examples/threadline_phoenix/e2e/tests/operator.spec.ts
  - lib/threadline/operator_surface/live/start_live.ex
autonomous: true
requirements: [POLISH-A11Y]
must_haves:
  truths:
    - "Existing browser specs match shipped v1.31 behavior and no longer fail on stale copy/selectors."
    - "Home earned-flow form behavior is either fixed in app code or proven current in tests."
  artifacts:
    - path: "examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts"
      provides: "Current earned-flow browser assertions"
      contains: "EF1"
    - path: "examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts"
      provides: "Stable screenshot capture assertions"
      contains: "operator surface screenshots"
---

# Plan 143-02: Consistency Sweep and Browser Suite Repair

## Objective

Make the existing browser suite current with shipped v1.31 behavior so final accessibility and screenshot regression gates start from a green browser lane.

## Tasks

1. Investigate EF1/EF4 Home form failures. Fix app behavior if forms are genuinely not navigating; otherwise update stale selectors/assertions without weakening flow coverage.
2. Update `operator-home-nav-mobile.spec.ts` to expect the Phase 140 Home workflow forms and keep the orientation/nav/mobile assertions.
3. Fix strict-mode `[REDACTED]` assertions in `operator-screenshots.spec.ts` and `operator.spec.ts` by scoping to the intended drawer/transaction surface.
4. Update stale Timeline empty-state screenshot assertion to the locked Phase 138 empty copy.
5. Run focused affected specs, then `mix verify.example_browser`.

## Required Checks

- `cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-earned-flows.spec.ts tests/operator-home-nav-mobile.spec.ts tests/operator-screenshots.spec.ts tests/operator.spec.ts`
- `mix verify.example_browser`

## Success Criteria

- Existing browser suite is green or any residual failure is proven unrelated to Phase 143 with exact failure class.
- Stale assertions are updated to shipped behavior, not deleted.
- No screenshot baseline/guard work yet; this plan only clears the regression lane.
