---
phase: 143-accessibility-consistency-sweep-regression
plan: "03"
type: execute
wave: 3
depends_on: ["143-02"]
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-screenshots.spec.ts
  - .planning/milestones/v1.31-screenshots/final/
  - .planning/phases/143-accessibility-consistency-sweep-regression/143-SCREENSHOT-DIFF.md
  - .planning/phases/143-accessibility-consistency-sweep-regression/143-AUDIT-CLOSURE.md
autonomous: true
requirements: [POLISH-A11Y]
must_haves:
  truths:
    - "Final screenshots are captured against the Phase 134 state matrix."
    - "Every Phase 134 audit finding has a closure status with evidence."
  artifacts:
    - path: ".planning/milestones/v1.31-screenshots/final/"
      provides: "Final screenshot set"
      contains: ".png"
    - path: ".planning/phases/143-accessibility-consistency-sweep-regression/143-SCREENSHOT-DIFF.md"
      provides: "Baseline-to-final screenshot diff report"
      contains: "delta"
    - path: ".planning/phases/143-accessibility-consistency-sweep-regression/143-AUDIT-CLOSURE.md"
      provides: "Phase 134 finding closure registry"
      contains: "F-901"
---

# Plan 143-03: Final Screenshot Diff and Audit Closure

## Objective

Capture the final v1.31 screenshot set, compare it with the Phase 134 baseline, and close the Phase 134 audit registry with evidence.

## Tasks

1. Extend screenshot capture so final durable PNGs can be written to `.planning/milestones/v1.31-screenshots/final/` without committing Playwright `test-results`.
2. Capture the same 24 baseline screen/viewport names where possible.
3. Produce `143-SCREENSHOT-DIFF.md` with per-screen deltas, unchanged surfaces, and intentional changes tied to phases 135-143.
4. Produce `143-AUDIT-CLOSURE.md` listing every Phase 134 finding with closed/deferred status and evidence.

## Required Checks

- `cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-screenshots.spec.ts`
- `find .planning/milestones/v1.31-screenshots/final -type f -name '*.png' | wc -l`
- `rg -n "F-901|F-902|F-903|F-801|F-1001|closed|deferred" .planning/phases/143-accessibility-consistency-sweep-regression/143-AUDIT-CLOSURE.md`
- `rg -n "baseline|final|delta|intentional|unexplained" .planning/phases/143-accessibility-consistency-sweep-regression/143-SCREENSHOT-DIFF.md`

## Success Criteria

- Final screenshots exist and are named consistently with the baseline.
- Every delta is explained or explicitly marked as requiring follow-up.
- Every Phase 134 finding has a closure status.
