---
phase: 120-root-auth-integration-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/phx_gen_auth_fixtures.ex
  - test/threadline/integrations/phx_gen_auth_integration_test.exs
autonomous: true
requirements:
  - AUTH-PROOF-01
  - AUTH-PROOF-02
must_haves:
  truths:
    - "Root CI proves actor_ref from phx.gen.auth-shaped current_scope assigns without the Sigra example app."
    - "Root CI proves 1-arity admin authorize_fn allow/deny via ExportAuthPlug conn mirror."
    - "Exactly one Threadline.Plug smoke proves guide router callback wiring."
    - "plug_test.exs remains SSOT for header/override semantics 4–6."
  artifacts:
    - path: test/threadline/integrations/phx_gen_auth_integration_test.exs
      provides: "phx-gen-auth-reference lane integration proof"
    - path: test/support/phx_gen_auth_fixtures.ex
      provides: "nested map fixtures for scope assigns"
---

<objective>
Add root integration tests and fixtures proving the Phase 119 cookbook patterns (AUTH-PROOF-01, AUTH-PROOF-02).

Purpose: CI-backed living proof adopters can copy — symmetric honesty with `sigra_test.exs`, no `lib/threadline/integrations/phx_gen_auth.ex`.
Output: ~8–12 tests across three describes; test-local `Threadline.Integrations.PhxGenAuthReference.AuditActor`.
</objective>

<context>
@.planning/phases/120-root-auth-integration-proof/120-CONTEXT.md (D-120-01 through D-120-17, D-120-23 through D-120-26)
@.planning/phases/120-root-auth-integration-proof/120-RESEARCH.md
@test/threadline/integrations/sigra_test.exs
@test/threadline/plug_test.exs
@test/threadline/operator_surface/export_auth_plug_test.exs
@guides/integrations/phx-gen-auth.md
@lib/threadline/plug.ex
@lib/threadline/operator_surface/export_auth_plug.ex
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-120-01 | Tests imply Threadline ships phx.gen.auth | medium | Module comment: copy PhxGenAuthReference.AuditActor → MyApp.AuditActor; no lib adapter |
| T-120-02 | 2-arity authorize_fn tested, masking guide bug | high | Only 1-arity callbacks; mirror `%{assigns: conn.assigns}` per integration-contracts |
| T-120-03 | Duplicate plug_test semantics | low | Single Plug smoke; @moduledoc cites plug_test for items 4–6 |
| T-120-04 | False proof via Sigra example app | medium | No imports from examples/threadline_phoenix |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add phx_gen_auth_fixtures support module</name>
  <files>test/support/phx_gen_auth_fixtures.ex</files>
  <read_first>
    - test/threadline/integrations/sigra_test.exs (build_sigra_conn pattern, lines 287–309)
    - .planning/phases/120-root-auth-integration-proof/120-CONTEXT.md (D-120-05, D-120-06, D-120-08)
  </read_first>
  <action>
Create `test/support/phx_gen_auth_fixtures.ex` with:

1. `defmodule Threadline.PhxGenAuthFixtures` (or `Threadline.Test.PhxGenAuthFixtures` if project convention prefers `Threadline.Test.*` — match nearest existing `test/support/*` module naming).

2. `build_phx_scope_conn/1` accepting keyword opts:
   - `:scope` — map assigned to `:current_scope` (omit key to leave assign absent)
   - `:headers` — default `[]`, applied via `put_req_header/3`
   - `:current_user` — optional map assigned to `:current_user` for authorize tests

3. Use **string** user ids in default logged-in scope: `%{user: %{id: "u-42"}}`.

4. Export helpers:
   - `logged_in_scope/0` → `%{user: %{id: "u-42"}}`
   - `logged_out_scope/0` → `nil`
   - `admin_user/0` → `%{role: "admin"}`
   - `non_admin_user/0` → `%{role: "member"}`

5. Use `Plug.Test.conn/2` and `Plug.Conn.assign/3` only — no Ecto, no example app modules.
  </action>
  <acceptance_criteria>
    - `test -f test/support/phx_gen_auth_fixtures.ex`
    - `grep -F 'def build_phx_scope_conn' test/support/phx_gen_auth_fixtures.ex` exits 0
    - `grep -F '"u-42"' test/support/phx_gen_auth_fixtures.ex` exits 0
    - `grep -F 'examples/threadline_phoenix' test/support/phx_gen_auth_fixtures.ex` exits 1
  </acceptance_criteria>
  <verify>mix test test/support/phx_gen_auth_fixtures.ex 2>/dev/null || mix compile --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 2: AuditActor actor_fn semantics (reference items 1–2)</name>
  <files>test/threadline/integrations/phx_gen_auth_integration_test.exs</files>
  <read_first>
    - guides/integrations/phx-gen-auth.md (MyApp.AuditActor snippet, lines 23–35)
    - test/support/phx_gen_auth_fixtures.ex
    - lib/threadline/semantics/actor_ref.ex
    - .planning/phases/120-root-auth-integration-proof/120-CONTEXT.md (D-120-01, D-120-09, D-120-10)
  </read_first>
  <action>
Create `test/threadline/integrations/phx_gen_auth_integration_test.exs`:

1. Top-of-file `@moduledoc` stating: copy `Threadline.Integrations.PhxGenAuthReference.AuditActor` → host `MyApp.AuditActor`; reference semantics 4–6 proven in `test/threadline/plug_test.exs`.

2. Nested module `Threadline.Integrations.PhxGenAuthReference.AuditActor` with **identical logic** to guide:
   ```elixir
   def actor_ref_from_conn(conn) do
     case conn.assigns[:current_scope] do
       %{user: %{id: id}} -> ActorRef.new(:user, to_string(id))
       _ -> nil
     end
   end
   def audit_context_overrides_from_conn(_conn), do: %{}
   ```

3. `describe "guide AuditActor actor_fn"` with tests (~4–6):
   - absent `:current_scope` key → nil
   - `current_scope: nil` → nil
   - `current_scope: %{user: nil}` → nil
   - logged-in scope → `%ActorRef{type: :user, id: "u-42"}`

4. Optional single struct parity test (D-120-07): local `defstruct` with same keys as scope map; one assert actor_ref works — only if within test budget.

Use `Threadline.PhxGenAuthFixtures.build_phx_scope_conn/1` for conn building.
  </action>
  <acceptance_criteria>
    - `test -f test/threadline/integrations/phx_gen_auth_integration_test.exs`
    - `grep -F 'PhxGenAuthReference.AuditActor' test/threadline/integrations/phx_gen_auth_integration_test.exs` exits 0
    - `grep -F 'plug_test.exs' test/threadline/integrations/phx_gen_auth_integration_test.exs` exits 0
    - `mix test test/threadline/integrations/phx_gen_auth_integration_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/integrations/phx_gen_auth_integration_test.exs</verify>
</task>

<task type="auto">
  <name>Task 3: authorize_fn admin gate via ExportAuthPlug (reference item 3)</name>
  <files>test/threadline/integrations/phx_gen_auth_integration_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/export_auth_plug_test.exs (mirror pattern lines 62–79, deny/halt cases)
    - guides/integration-contracts.md (1-arity authorize_fn)
    - .planning/phases/120-root-auth-integration-proof/120-CONTEXT.md (D-120-14 through D-120-16)
  </read_first>
  <action>
Add `describe "guide authorize_fn admin gate"` to integration test file:

1. Define **1-arity** guide authorize callback (NOT 2-arity):
   ```elixir
   guide_authorize = fn
     %{assigns: %{current_user: %{role: "admin"}}} -> :ok
     _ -> {:error, :unauthorized}
   end
   ```

2. **Allow test:** conn with `current_user: %{role: "admin"}` → `ExportAuthPlug.call(conn, ExportAuthPlug.init(authorize_fn: guide_authorize))` → `refute conn.halted`

3. **Deny test:** conn with `current_user: %{role: "member"}` → plug halts with 403 (match export_auth_plug_test deny assertion style)

4. Do **not** mount `threadline_operator_surface` or LiveView `on_mount`.

Use fixtures helper for conn assigns.
  </action>
  <acceptance_criteria>
    - `grep -F 'ExportAuthPlug' test/threadline/integrations/phx_gen_auth_integration_test.exs` exits 0
    - `grep -F 'role: "admin"' test/threadline/integrations/phx_gen_auth_integration_test.exs` exits 0
    - `grep -F '_, _ ->' test/threadline/integrations/phx_gen_auth_integration_test.exs` exits 1 (no 2-arity authorize)
    - `mix test test/threadline/integrations/phx_gen_auth_integration_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/integrations/phx_gen_auth_integration_test.exs</verify>
</task>

<task type="auto">
  <name>Task 4: Single Threadline.Plug composition smoke</name>
  <files>test/threadline/integrations/phx_gen_auth_integration_test.exs</files>
  <read_first>
    - test/threadline/integrations/sigra_test.exs (describe "Threadline.Plug integration", lines 196–221)
    - lib/threadline/plug.ex
    - .planning/phases/120-root-auth-integration-proof/120-CONTEXT.md (D-120-03, D-120-04)
  </read_first>
  <action>
Add `describe "Threadline.Plug composition"` with **exactly one** test:

1. Build conn: logged-in scope + header `[{"x-request-id", "req-phx-1"}]`.

2. Call:
   ```elixir
   Threadline.Plug.call(conn, Threadline.Plug.init(
     actor_fn: &PhxGenAuthReference.AuditActor.actor_ref_from_conn/1,
     context_overrides_fn: &PhxGenAuthReference.AuditActor.audit_context_overrides_from_conn/1
   ))
   ```

3. Assert `%AuditContext{actor_ref: %ActorRef{type: :user, id: "u-42"}, request_id: "req-phx-1"}` in `conn.assigns.audit_context`.

4. Do **not** add second Plug test for correlation override or unknown keys.

Total file test count target: **8–12 tests** across all describes (`wc -l` on file is guidance only).
  </action>
  <acceptance_criteria>
    - `grep -c 'test "' test/threadline/integrations/phx_gen_auth_integration_test.exs` outputs integer between 8 and 12
    - `grep -F 'Threadline.Plug.call' test/threadline/integrations/phx_gen_auth_integration_test.exs | wc -l` equals 1
    - `mix test test/threadline/integrations/phx_gen_auth_integration_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/threadline/integrations/phx_gen_auth_integration_test.exs</verify>
</task>

</tasks>

<verification>
- [ ] `mix test test/threadline/integrations/phx_gen_auth_integration_test.exs` green
- [ ] No new files under `lib/threadline/integrations/`
- [ ] No edits to `examples/threadline_phoenix/`
</verification>
