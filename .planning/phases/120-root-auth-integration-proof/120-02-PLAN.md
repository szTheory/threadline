---
phase: 120-root-auth-integration-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 120-01
files_modified:
  - guides/integrations/phx-gen-auth.md
  - guides/upgrade-path.md
  - test/threadline/upgrade_path_doc_contract_test.exs
autonomous: true
requirements:
  - AUTH-PROOF-03
must_haves:
  truths:
    - "upgrade-path matrix includes phx-gen-auth-reference row with honest proof anchors."
    - "Guides cite real test path; no forthcoming/Phase 120 deferral wording."
    - "phx guide authorize_fn is 1-arity, matching runtime."
    - "sigra-reference matrix row literals unchanged."
    - "mix verify.test and mix verify.doc_contract green."
  artifacts:
    - path: guides/upgrade-path.md
      provides: "fourth compatibility matrix row"
    - path: guides/integrations/phx-gen-auth.md
      provides: "fixed authorize_fn + lane proof path"
    - path: test/threadline/upgrade_path_doc_contract_test.exs
      provides: "four-lane doc-contract locks"
---

<objective>
Land guide + upgrade-path proof honesty and doc-contract locks in the same changeset as integration tests (AUTH-PROOF-03, D-120-16 through D-120-22).

Purpose: Procurement/CI honesty — matrix cites `phx_gen_auth_integration_test.exs`; retire "forthcoming" deferrals.
Output: Fourth matrix row, fixed mount snippet, ~12–16 new doc-contract asserts.
</objective>

<context>
@.planning/phases/120-root-auth-integration-proof/120-CONTEXT.md (D-120-15 through D-120-22)
@.planning/phases/120-root-auth-integration-proof/120-RESEARCH.md
@test/threadline/integrations/phx_gen_auth_integration_test.exs (from 120-01 — must exist before doc literals)
@guides/upgrade-path.md
@guides/integrations/phx-gen-auth.md
@test/threadline/upgrade_path_doc_contract_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-120-05 | Matrix claims example-app or Sigra proof | high | Row literals exclude verify.example and examples/threadline_phoenix |
| T-120-06 | Guide still shows 2-arity authorize_fn | high | Fix mount fence + doc contract grep |
| T-120-07 | sigra-reference row regression | medium | Assert unchanged sigra row substring in doc contract |
| T-120-08 | Premature phx_gen_auth_doc_contract_test | low | Do not create separate phx doc contract file (Phase 121) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fix phx-gen-auth guide authorize_fn arity and lane proof</name>
  <files>guides/integrations/phx-gen-auth.md</files>
  <read_first>
    - guides/integrations/phx-gen-auth.md (lines 54–89)
    - guides/operator-surface.md (1-arity authorize_fn)
    - test/threadline/integrations/phx_gen_auth_integration_test.exs
    - .planning/phases/120-root-auth-integration-proof/120-CONTEXT.md (D-120-15, D-120-21)
  </read_first>
  <action>
Edit `guides/integrations/phx-gen-auth.md`:

1. **Surface mount fence** — replace 2-arity clauses with 1-arity:
   ```elixir
   threadline_operator_surface "/audit",
     authorize_fn: fn
       %{assigns: %{current_user: %{role: "admin"}}} -> :ok
       _ -> {:error, :unauthorized}
     end
   ```

2. **## Reference semantics** — update item 3 text if needed to say "1-arity authorize_fn" (optional clarity).

3. **## Lane and proof** — replace forthcoming wording with:
   - `test/threadline/integrations/phx_gen_auth_integration_test.exs` as root CI proof
   - Remove phrases: `forthcoming`, `Phase 120`, `forthcoming in Phase 120`
   - Keep: not a second example app

4. Add under Lane and proof or Reference semantics: pointer that semantics 4–6 are covered by `test/threadline/plug_test.exs` (one sentence).

Do **not** add `mix verify.example` or example app paths.
  </action>
  <acceptance_criteria>
    - `grep -F 'test/threadline/integrations/phx_gen_auth_integration_test.exs' guides/integrations/phx-gen-auth.md` exits 0
    - `grep -i forthcoming guides/integrations/phx-gen-auth.md` exits 1
    - `grep -F '_, _ ->' guides/integrations/phx-gen-auth.md` exits 1
    - `grep -F '%{assigns: %{current_user: %{role: "admin"}}} -> :ok' guides/integrations/phx-gen-auth.md` exits 0
  </acceptance_criteria>
  <verify>grep -i forthcoming guides/integrations/phx-gen-auth.md; echo exit:$?</verify>
</task>

<task type="auto">
  <name>Task 2: Add phx-gen-auth-reference matrix row and retire forthcoming in upgrade-path</name>
  <files>guides/upgrade-path.md</files>
  <read_first>
    - guides/upgrade-path.md (## How to tell, ## Supported compatibility matrix)
    - .planning/phases/120-root-auth-integration-proof/120-CONTEXT.md (D-120-18, D-120-20, D-120-22)
    - guides/upgrade-path.md sigra-reference row (preserve exactly)
  </read_first>
  <action>
Edit `guides/upgrade-path.md`:

1. **## How to tell which lane you are on** — in `phx-gen-auth-reference` paragraph, replace "forthcoming root integration tests" with citation of `test/threadline/integrations/phx_gen_auth_integration_test.exs` and `mix verify.test`.

2. **## Supported compatibility matrix** — insert new row **after** `phoenix-surface` row, **before** `sigra-reference` row:

   `| phx-gen-auth-reference | reference | Host-generated session auth (mix phx.gen.auth or equivalent) + phoenix-surface optional deps | Root mix.lock Phoenix/LV/HTML/PubSub versions (no Sigra) | guides/integrations/phx-gen-auth.md, test/threadline/integrations/phx_gen_auth_integration_test.exs, mix verify.test, verify-test |`

   Use same column style as existing rows (backticks on lane name if other rows use them — match `| \`phoenix-surface\` |` pattern).

3. **Release checklist** (if line 123 lists lanes) — ensure `phx-gen-auth-reference` already present from Phase 119; add test path if checklist mentions proof.

4. **Do not modify** the `sigra-reference` matrix row cell text.

5. `refute` targets: remove "forthcoming" from phx lane detection paragraph.
  </action>
  <acceptance_criteria>
    - `grep -F '| `phx-gen-auth-reference` | `reference` |' guides/upgrade-path.md` exits 0 OR `grep -F 'phx-gen-auth-reference | reference' guides/upgrade-path.md` exits 0 (match actual backtick style)
    - `grep -F 'phx_gen_auth_integration_test.exs' guides/upgrade-path.md` exits 0
    - `grep -i forthcoming guides/upgrade-path.md` exits 1
    - `grep -F '| `sigra-reference` | `reference` |' guides/upgrade-path.md` exits 0 (unchanged)
    - `grep -F 'mix verify.example' guides/upgrade-path.md` matches only sigra row context, not phx row — phx row must not contain verify.example
  </acceptance_criteria>
  <verify>grep -F 'phx_gen_auth_integration_test.exs' guides/upgrade-path.md</verify>
</task>

<task type="auto">
  <name>Task 3: Extend upgrade_path_doc_contract_test for four lanes</name>
  <files>test/threadline/upgrade_path_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/upgrade_path_doc_contract_test.exs (existing three-lane test lines 74–98)
    - guides/upgrade-path.md (after Task 2 edits)
    - guides/integrations/phx-gen-auth.md (after Task 1 edits)
    - .planning/phases/120-root-auth-integration-proof/120-CONTEXT.md (D-120-19)
  </read_first>
  <action>
Extend `test/threadline/upgrade_path_doc_contract_test.exs`:

1. Add test `"upgrade-path guide locks phx-gen-auth-reference lane detection and matrix row"` with asserts:
   - `You are on the \`phx-gen-auth-reference\` lane`
   - matrix contains `phx-gen-auth-reference` and `reference`
   - `test/threadline/integrations/phx_gen_auth_integration_test.exs`
   - `guides/integrations/phx-gen-auth.md`
   - `refute String.contains?(guide, "forthcoming")` for upgrade-path guide

2. Add test `"phx-gen-auth guide cites integration test proof"` reading `guides/integrations/phx-gen-auth.md`:
   - contains `test/threadline/integrations/phx_gen_auth_integration_test.exs`
   - `refute String.contains?(guide, "forthcoming")`
   - `refute String.contains?(guide, "_, _ ->")` (no 2-arity authorize)

3. In existing three-lane test, **keep** sigra/capture/phoenix asserts; optionally rename describe to "four named lanes" if clarity helps.

4. Add assert sigra row substring unchanged: `| \`sigra-reference\` | \`reference\` |` (exact match to current file).

5. Total **~12–16** new `assert`/`refute` lines across new tests (not counting existing file).

Do **not** create `test/threadline/phx_gen_auth_doc_contract_test.exs`.
  </action>
  <acceptance_criteria>
    - `grep -F 'phx-gen-auth-reference' test/threadline/upgrade_path_doc_contract_test.exs` exits 0
    - `grep -F 'phx_gen_auth_integration_test.exs' test/threadline/upgrade_path_doc_contract_test.exs` exits 0
    - `test ! -f test/threadline/phx_gen_auth_doc_contract_test.exs`
    - `mix verify.doc_contract` exits 0
    - `mix verify.test` exits 0
  </acceptance_criteria>
  <verify>mix verify.test && mix verify.doc_contract</verify>
</task>

</tasks>

<verification>
- [ ] `mix verify.test` green
- [ ] `mix verify.doc_contract` green
- [ ] AUTH-PROOF-01/02/03 satisfied
</verification>
