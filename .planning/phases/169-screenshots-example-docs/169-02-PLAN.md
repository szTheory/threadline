---
phase: 169-screenshots-example-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/operator-surface.md
  - README.md
  - test/threadline/operator_surface/theme_doc_contract_test.exs
autonomous: true
requirements: [EVID-02]

must_haves:
  truths:
    - "guides/operator-surface.md documents the theme: option with all three values :dark (default), :light, :system and what each renders"
    - "The guide carries the D-04 daytime-use recommendation framed as readability/accessibility — never a medical eye-strain claim"
    - "README.md points daytime/bright-environment teams to theme: :system near the 'dark, branded admin surface' line, WITHOUT changing the canonical mount snippet"
    - "A new doc-contract test pins the theme: literal + :dark/:light/:system + the daytime recommendation in the guide, and passes via mix test"
    - "test/threadline/readme_doc_contract_test.exs still passes UNCHANGED after the additive :system pointer — its readme_mount_block/0 normalized lock proves the root README.md operator-surface mount snippet is byte-identical"
    - "The guide's canonical mount snippet and the 3 existing snippet doc-contract tests (getting_started_saas, example_phoenix_readme, example_phoenix_schemas_mount) still pass UNCHANGED for the files THEY guard (the SaaS/schemas guides and the example-app README)"
  artifacts:
    - path: "guides/operator-surface.md"
      provides: "Theme subsection documenting theme: :dark|:light|:system + daytime recommendation"
      contains: "theme:"
    - path: "README.md"
      provides: "One-line theme: :system daytime pointer (additive, snippet unchanged)"
      contains: ":system"
    - path: "test/threadline/operator_surface/theme_doc_contract_test.exs"
      provides: "Literal-pin lock of the theme: option in the guide"
      contains: "guides/operator-surface.md"
  key_links:
    - from: "test/threadline/operator_surface/theme_doc_contract_test.exs"
      to: "guides/operator-surface.md"
      via: "File.read! + String.contains? literal pins"
      pattern: "String.contains\\?"
    - from: "README.md theme pointer"
      to: "guides/operator-surface.md Theme subsection"
      via: "adopter-facing pointer documenting the option beyond the guide"
      pattern: ":system"
---

<objective>
Document the `theme:` option truthfully and lock it with a new literal-pin doc-contract
test. Add a "Theme" subsection to `guides/operator-surface.md` covering `:dark` (default),
`:light`, and `:system` with the daytime-use recommendation; add a one-line `:system`
pointer to `README.md`; and add a NEW doc-contract test pinning the literals. The
canonical dark-default mount snippet and the 3 existing snippet doc-contract tests stay
untouched (D-05 — the new lock is ADDITIVE only).

Purpose: EVID-02 — the documentation half. The example app already demonstrates
`theme: :system` end-to-end through its env-gated e2e branch (exercised by Plan 01's light
lane), satisfying success-criterion-2; this plan delivers success-criteria 2 and 3's docs
+ doc-contract coverage.
Output: Updated guide + README + a passing new doc-contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-screenshots-example-docs/169-CONTEXT.md
@.planning/phases/169-screenshots-example-docs/169-PATTERNS.md
@.planning/milestones/v1.35-phases/165-light-mode-strategy/165-LIGHT-MODE-RECOMMENDATION.md

<interfaces>
<!-- Read-only source truth for accurate prose. Do NOT edit these source files. -->

lib/threadline/operator_surface/router.ex (~lines 52-72): `:theme` option doc +
  compile-time validation `:dark | :light | :system`, default `:dark`. Source wording:
  ":system follows the visitor's OS preference through scoped CSS only; Threadline does not
  add JavaScript, local storage, or a runtime theme toggle."
lib/threadline/operator_surface/style.ex (~:178 dark base, ~:188-237 [data-tl-theme="light"]
  forced lane, ~:239-289 @media (prefers-color-scheme: light) [data-tl-theme="system"]
  OS-auto branch): confirms :system = OS-auto pure-CSS no-FOUC, :light = forced, :dark = default.
lib/threadline/operator_surface/auth.ex (~:14, 177-179): normalize_theme/1 → data-tl-theme assign.

guides/operator-surface.md: "## 1-Minute Mount" (line 12); mount macro example
  (lines 46+ `threadline_operator_surface "/", ...`). NO existing theme/:dark/:light/:system
  mention — the Theme subsection slots near the 1-Minute Mount / options area.
README.md (line 126): "Threadline ships an optional, drop-in LiveView **operator console** —
  a dark, branded admin surface ..." — the insertion anchor for the one-line :system pointer.

ROOT-README MOUNT-SNIPPET GUARDIAN (the real lock — name it correctly):
  test/threadline/readme_doc_contract_test.exs is the guardian of the root README.md
  `threadline_operator_surface` mount block. Its `readme_mount_block/0` (~lines 258-268)
  holds the canonical `scope "/audit" ... threadline_operator_surface "/", actor_fn:..., authorize_fn:...`
  block and asserts it against `File.read!("README.md")` via normalized `String.contains?`
  (whitespace-collapsed). This is the test that proves the root README's mount snippet is
  byte-identical — so the additive :system pointer near line 126 MUST NOT perturb the mount
  block, and this test MUST run (and pass UNCHANGED) in Task 2's verify.

  The 3 snippet tests cited elsewhere guard OTHER files, NOT the root README mount block:
  - test/threadline/getting_started_saas_doc_contract_test.exs → guards guides/getting-started-saas.md
  - test/threadline/example_phoenix_readme_contract_test.exs → guards examples/threadline_phoenix/README.md
  - test/threadline/example_phoenix_schemas_mount_contract_test.exs → guards the example-app schemas mount.
  They remain relevant only as regression guards for the example-app README and the SaaS/schemas
  guides; they do NOT lock the root README.md mount snippet.

Daytime-recommendation precedent (165-LIGHT-MODE-RECOMMENDATION.md lines 36, 40-42, 67):
  precedent one-liner — "Dark stays the default and the brand; :system becomes the documented
  daytime-use recommendation." Frame light as readability/accessibility (dense audit text;
  astigmatism prevalence) — NEVER a medical eye-strain claim (165 lesson).

Doc-contract literal-pin analog: test/threadline/operator_surface/timeline_browse_doc_contract_test.exs
  (cleanest minimal pattern: `@path` constant + `File.read!` + `String.contains?` + named
  failure message, `use ExUnit.Case, async: true`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Theme" subsection to guides/operator-surface.md and the README :system pointer</name>
  <files>guides/operator-surface.md, README.md</files>
  <read_first>
    - guides/operator-surface.md (the file being modified — 1-Minute Mount section line 12, mount macro example ~line 46, options prose lines 12-53)
    - README.md (the file being modified — read the operator-console paragraph at line 126 AND the canonical mount snippet so it is left byte-identical; the root README mount block is locked by readme_doc_contract_test.exs `readme_mount_block/0`)
    - lib/threadline/operator_surface/router.ex (~lines 52-72, READ-ONLY — accurate :theme doc wording + the :dark|:light|:system triad)
    - lib/threadline/operator_surface/style.ex (~:178, 188-237, 239-289, READ-ONLY — confirm :system OS-auto pure-CSS, :light forced, :dark default)
    - .planning/milestones/v1.35-phases/165-light-mode-strategy/165-LIGHT-MODE-RECOMMENDATION.md (precedent phrasing + readability/accessibility framing)
  </read_first>
  <action>
    Add a new "### Theme" (or "## Theme") subsection to `guides/operator-surface.md` near the
    1-Minute Mount / options area (D-05 + Claude's-Discretion on exact heading/placement). It
    MUST: document the `theme:` option with all three values `:dark` (default), `:light`,
    `:system`; describe what each renders, grounded in source — `:dark` is the omitted default
    and brand-primary; `:system` auto-follows the OS via pure-CSS
    `@media (prefers-color-scheme: light)` with no JS, no localStorage, no FOUC (correct on
    first paint / dead render); `:light` forces light regardless of OS. Carry the D-04 daytime
    recommendation using the settled precedent one-liner ("Dark stays the default and the brand;
    `:system` becomes the documented daytime-use recommendation."). Frame light as a
    readability/accessibility choice (small dense audit text; astigmatism prevalence) — NEVER a
    medical eye-strain claim (165 lesson). Do NOT add `theme:` to the canonical mount snippet in
    the guide (keep dark-default clean — D-05).
    In `README.md`, add ONE additive sentence near line 126 ("dark, branded admin surface")
    pointing daytime/bright-environment teams to `theme: :system` and the operator-surface
    guide's Theme subsection. The root README's `threadline_operator_surface` mount block MUST
    stay byte-identical — it is normalized-locked by `readme_doc_contract_test.exs`
    `readme_mount_block/0`; keep the additive pointer outside that mount block so the lock
    still passes (Task 2 verify runs it).
    Do NOT touch any source under `lib/`, the example-app router, or the nav-overhaul files.
  </action>
  <verify>
    <automated>grep -c 'theme:' guides/operator-surface.md; for v in ':dark' ':light' ':system'; do grep -c "$v" guides/operator-surface.md; done; grep -ci 'daytime' guides/operator-surface.md; grep -c ':system' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/operator-surface.md` contains the literal `theme:` AND `:dark` AND `:light` AND `:system`
    - `guides/operator-surface.md` contains the daytime-recommendation phrase (e.g. "daytime-use recommendation")
    - The guide frames light as readability/accessibility (no "eye strain"/medical claim — `grep -i 'eye strain' guides/operator-surface.md` returns nothing)
    - The guide's canonical mount snippet still does NOT contain `theme:` (dark-default clean — D-05)
    - `README.md` contains a `:system` daytime pointer near line 126
    - The root README's `threadline_operator_surface` mount block is byte-identical to before — verified by `readme_doc_contract_test.exs` passing in Task 2's verify (its `readme_mount_block/0` normalized lock)
    - NO edits under `lib/`, NO example-app router edit, NO nav-overhaul file touched
  </acceptance_criteria>
  <done>The guide documents the theme triad + daytime recommendation; the README points daytime teams to :system; the canonical root-README mount block is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add theme_doc_contract_test.exs literal-pin lock and confirm the root-README mount lock + existing snippet tests pass unchanged</name>
  <files>test/threadline/operator_surface/theme_doc_contract_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/timeline_browse_doc_contract_test.exs (the literal-pin analog to mirror — module + @path constant + File.read! + String.contains? + named message, async: true)
    - test/threadline/operator_surface/coverage_doc_contract_test.exs (richer guide/source literal-pin variant, lines 56-62)
    - guides/operator-surface.md (the doc being pinned — read the Theme subsection added in Task 1 to assert exact literals)
    - test/threadline/readme_doc_contract_test.exs (READ-ONLY — the REAL root-README mount-block guardian; `readme_mount_block/0` ~lines 258-268 + normalized `String.contains?`; must NOT be modified and must pass unchanged)
    - test/threadline/getting_started_saas_doc_contract_test.exs, test/threadline/example_phoenix_readme_contract_test.exs, test/threadline/example_phoenix_schemas_mount_contract_test.exs (READ-ONLY — guard the SaaS guide / example-app README / example-app schemas mount; must NOT be modified; confirm they still pass)
  </read_first>
  <behavior>
    - Test: the guide documents the `theme:` option literal → assert `String.contains?(guide, "theme:")`
    - Test: the guide documents each of `:dark`, `:light`, `:system` (each asserted individually so CI pinpoints the regressed value)
    - Test: the guide carries the daytime-use recommendation phrasing (the D-04 precedent fragment, e.g. "daytime-use recommendation")
    - All assertions read `guides/operator-surface.md` via `File.read!` on a `@guide_path` constant; pure source-reading → `use ExUnit.Case, async: true`; NO runtime/Mix-task/capture_io assertions
  </behavior>
  <action>
    Create `test/threadline/operator_surface/theme_doc_contract_test.exs` mirroring the
    `timeline_browse_doc_contract_test.exs` literal-pin pattern: `defmodule
    Threadline.OperatorSurface.ThemeDocContractTest`, `@moduledoc false`,
    `use ExUnit.Case, async: true`, a `@guide_path "guides/operator-surface.md"` constant.
    Each assertion does `src = File.read!(@guide_path)` then `String.contains?(src, ...)` with
    a named failure message identifying the file and expected fragment — assert each literal
    individually (`theme:`, `:dark`, `:light`, `:system`, and a stable daytime-recommendation
    fragment) so CI pinpoints which line regressed. Use a separate file (recommended for
    clarity per Claude's-Discretion). Do NOT add runtime, Mix-task, or `capture_io` blocks
    (out of scope — keep it pure read-and-pin). Do NOT modify `readme_doc_contract_test.exs`,
    the 3 existing snippet doc-contract tests, or the canonical mount snippet (D-05). Do NOT
    touch source under `lib/` or the nav-overhaul files.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/theme_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/example_phoenix_readme_contract_test.exs test/threadline/example_phoenix_schemas_mount_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline/operator_surface/theme_doc_contract_test.exs` exits 0
    - The new test asserts (each individually) `theme:`, `:dark`, `:light`, `:system`, and the daytime-recommendation fragment against `guides/operator-surface.md`
    - The new test uses `use ExUnit.Case, async: true` and contains NO `capture_io` / Mix-task / runtime assertions
    - `test/threadline/readme_doc_contract_test.exs` passes UNCHANGED in the same run — its `readme_mount_block/0` normalized lock confirms the additive :system pointer did NOT perturb the root README.md mount snippet (it stays byte-identical)
    - The 3 existing snippet doc-contract tests (getting_started_saas, example_phoenix_readme, example_phoenix_schemas_mount) pass UNCHANGED in the same run for the files THEY guard (the SaaS guide / example-app README / example-app schemas mount)
    - `git diff --name-only` shows `readme_doc_contract_test.exs` and the 3 existing snippet tests are NOT modified
    - `mix format --check-formatted test/threadline/operator_surface/theme_doc_contract_test.exs` passes
  </acceptance_criteria>
  <done>The new doc-contract test pins the theme: literal + triad + daytime recommendation and passes; the root-README mount lock (readme_doc_contract_test.exs) and the 3 existing snippet tests pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter docs → adopter mount config | Docs instruct adopters to set `theme:`; the option itself is validated at compile time in router.ex (out of this plan's scope, already locked). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-02-01 | Information Disclosure | guide / README prose | accept | Prose documents a public, already-shipped option; no secrets, no PII, synthetic examples only. |
| T-169-02-02 | Tampering | doc-contract test | mitigate | The new literal-pin test locks the documented option so silent doc drift fails CI. The root README mount snippet stays locked by `readme_doc_contract_test.exs` (`readme_mount_block/0`), which Task 2 verify runs to confirm the additive :system pointer left the mount block byte-identical. |
| T-169-02-SC | Tampering | dependency installs | accept | No package installs (pure docs + ExUnit test reusing existing test harness); RESEARCH package-legitimacy gate N/A. |

Surface for this plan is minimal: additive docs + one read-and-pin test, no source code under `lib/`, no new dependencies.
</threat_model>

<verification>
- `mix test test/threadline/operator_surface/theme_doc_contract_test.exs` exits 0.
- `test/threadline/readme_doc_contract_test.exs` (the root-README mount-block guardian) passes UNCHANGED in the same `mix test` run — its `readme_mount_block/0` normalized lock proves the additive :system pointer did not perturb the root README.md mount snippet.
- The 3 existing snippet doc-contract tests pass unchanged in the same `mix test` run for the files they guard (SaaS guide / example-app README / example-app schemas mount).
- `grep` confirms the guide carries `theme:` + `:dark` + `:light` + `:system` + the daytime recommendation, and contains no medical "eye strain" claim.
- `git diff --name-only` for this plan touches ONLY guides/operator-surface.md, README.md, and the new test file — no `lib/`, no example-app router, no nav-overhaul files, no edits to readme_doc_contract_test.exs or the 3 existing snippet tests.
- `mix verify.format` passes on the new test.
</verification>

<success_criteria>
- The guide documents `theme: :dark|:light|:system` with the daytime recommendation (readability/accessibility framing).
- The README points daytime teams to `:system` without changing the canonical root-README mount snippet (readme_doc_contract_test.exs stays green).
- A new doc-contract test locks the option literals and passes; the root-README mount lock and the 3 existing snippet tests are unchanged and green.
- EVID-02 / ROADMAP success-criteria 2 and 3 satisfied.
</success_criteria>

<output>
Create `.planning/phases/169-screenshots-example-docs/169-02-SUMMARY.md` when done.
</output>
