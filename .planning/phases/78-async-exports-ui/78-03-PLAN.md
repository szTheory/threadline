---
phase: "78"
plan: "03"
type: "execute"
wave: 3
depends_on: ["78-02"]
files_modified:
  - lib/threadline/operator_surface/live/export_status_live.ex
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/live/timeline_live.ex
  - test/threadline/operator_surface/live/export_status_live_test.exs
  - test/threadline/operator_surface/live/timeline_live_test.exs
autonomous: false
requirements: [EXP-01, EXP-02, EXP-03]
must_haves:
  truths:
    - "Operator can trigger a background export from the timeline."
    - "Operator can view the status of their requested exports."
    - "Export status page updates without full page reload."
  artifacts:
    - path: "lib/threadline/operator_surface/live/export_status_live.ex"
      provides: "Real-time UI for export job monitoring"
  key_links:
    - from: "lib/threadline/operator_surface/live/timeline_live.ex"
      to: "Threadline.Governance.ExportJob"
      via: "Job creation and enqueuing via ExportQueue"
---

<objective>
Export Status UI & Trigger integration.
Purpose: Allow operators to trigger background exports and monitor their real-time progress.
Output: Export Status LiveView and updated TimelineLive.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-async-exports-ui/PATTERNS.md
@.planning/phases/78-async-exports-ui/78-UI-SPEC.md
@.planning/phases/78-async-exports-ui/78-RESEARCH.md
@lib/threadline/operator_surface/live/timeline_live.ex
@lib/threadline/operator_surface/router.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export Status LiveView</name>
  <files>lib/threadline/operator_surface/live/export_status_live.ex, lib/threadline/operator_surface/router.ex, test/threadline/operator_surface/live/export_status_live_test.exs</files>
  <action>
    1. Create `Threadline.OperatorSurface.Live.ExportStatusLive`.
    2. Add route in `Threadline.OperatorSurface.Router` under `live_session :threadline` block: `live "/exports", Live.ExportStatusLive, :index`.
    3. The LiveView should list `Threadline.Governance.ExportJob` records for the current actor (`socket.assigns.current_actor`).
    4. Implement real-time polling (e.g., `Process.send_after(self(), :refresh, 5000)`) to update the status of jobs. **Reference the `lib/threadline/operator_surface/live/retention_history_live.ex` analog (mapped in PATTERNS.md)** for standard layout and real-time polling patterns.
    5. Render the job status, started_at, completed_at, and a "Download" link pointing to `"/exports/download/#{job.id}"` if completed. Use UI guidelines from `78-UI-SPEC.md`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/export_status_live_test.exs</automated>
  </verify>
  <done>Export status page lists jobs and allows downloading.</done>
</task>

<task type="auto">
  <name>Task 2: Timeline "Request Background Export" Integration</name>
  <files>lib/threadline/operator_surface/live/timeline_live.ex, test/threadline/operator_surface/live/timeline_live_test.exs</files>
  <action>
    1. In `TimelineLive`, alongside existing sync download buttons, add a primary action button: "Request Background Export".
    2. Add a `handle_event` for this button. It should:
       a. Create a new `Threadline.Governance.ExportJob` struct with the current filters (`socket.assigns.filters`), status `"pending"`, and the current `actor_ref`.
       b. Insert it into the database.
       c. Enqueue it using the `Threadline.ExportQueue` behaviour (or the configured adapter).
       d. Redirect the user to `#{@base_path}/exports` with an info flash message: "Background export requested. View progress on the Export Status page."
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/timeline_live_test.exs</automated>
  </verify>
  <done>Timeline can successfully enqueue an async export job.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Export Status UI and Timeline Trigger</what-built>
  <how-to-verify>
    1. Start the Phoenix server.
    2. Navigate to the Threadline Timeline UI.
    3. Click "Request Background Export".
    4. Verify you are redirected to the Export Status page.
    5. Wait for the export to complete and verify the download link appears.
    6. Download the CSV and verify its contents.
  </how-to-verify>
  <resume-signal>Type "approved" if the flow works correctly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job Visibility | Viewing job status across different actors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04 | Information Disclosure | `ExportStatusLive` | mitigate | Ensure the query fetching jobs filters by the current actor's `actor_ref`. |
</threat_model>

<verification>
- View updates periodically.
- Jobs are actor-isolated.
</verification>

<success_criteria>
- The full background export lifecycle is available via the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/78-async-exports-ui/78-03-SUMMARY.md`
</output>