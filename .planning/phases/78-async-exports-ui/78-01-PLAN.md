---
phase: "78"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - lib/threadline/storage.ex
  - lib/threadline/storage/local.ex
  - test/threadline/storage/local_test.exs
  - lib/threadline/export_queue/task_adapter.ex
  - test/threadline/export_queue/task_adapter_test.exs
  - lib/threadline/export/orchestrator.ex
  - test/threadline/export/orchestrator_test.exs
autonomous: true
requirements: [EXP-01, EXP-02]
must_haves:
  truths:
    - "System can safely write local files via path reference instead of loading entirely in memory."
    - "System can retrieve a file path from Storage for safe streaming."
    - "System can stream DB changes into a CSV file in the background."
    - "Background export uses Task.Supervisor for safe execution."
  artifacts:
    - path: "lib/threadline/export_queue/task_adapter.ex"
      provides: "Threadline.ExportQueue behaviour implementation"
    - path: "lib/threadline/export/orchestrator.ex"
      provides: "Async export job execution logic"
  key_links:
    - from: "lib/threadline/export_queue/task_adapter.ex"
      to: "lib/threadline/export/orchestrator.ex"
      via: "Task.Supervisor.start_child/2"
---

<objective>
Core Async Export Engine & Defect Fix.
Purpose: Allow streaming of massive datasets to disk in the background, without memory bloat or blocking the UI, and fix a bug in Local storage. Adds safe local path retrieval for downloading.
Output: Updated Storage behaviour, fixed Local storage, built-in Task Adapter for queueing, and Orchestrator for executing exports.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-async-exports-ui/PATTERNS.md
@.planning/phases/78-async-exports-ui/78-RESEARCH.md
@lib/threadline/storage.ex
@lib/threadline/storage/local.ex
@lib/threadline/export.ex
@lib/threadline/export_queue.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Storage behaviour and fix Local adapter for paths</name>
  <files>lib/threadline/storage.ex, lib/threadline/storage/local.ex, test/threadline/storage/local_test.exs</files>
  <behavior>
    - Test 1: Writing raw string content works as before.
    - Test 2: When `content` is a valid file path (`File.regular?(content)` is true), the file is copied to the storage path instead of writing the path string literally.
    - Test 3: `path/1` correctly returns `{:ok, absolute_path}` for stored files in the local adapter.
  </behavior>
  <action>
    1. Add a new optional callback `c path(key :: String.t()) :: {:ok, String.t()} | {:error, term()}` to `Threadline.Storage`. Update `__using__` or just export the default if necessary.
    2. Update `Threadline.Storage.Local.put/2` to check if `content` is a path to an existing file (`File.regular?(content)`). If true, use `File.cp/2` (or `cp!`) to move the data. Otherwise, write it as a raw string. 
    3. Implement `path(key)` in `Threadline.Storage.Local` to return `{:ok, Path.join(storage_dir, key)}`.
    This allows `Orchestrator` to stream to a temp file and safely pass it to `Threadline.Storage.put/2`, and later allows `ExportController` to securely stream it back to the client via `Plug.Conn.send_file/3` without OOMing the node.
  </action>
  <verify>
    <automated>mix test test/threadline/storage/local_test.exs</automated>
  </verify>
  <done>Storage supports passing around file paths directly instead of loading contents into memory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrator to execute export jobs</name>
  <files>lib/threadline/export/orchestrator.ex, test/threadline/export/orchestrator_test.exs</files>
  <behavior>
    - Test 1: Marks job as "running", executes query via `Threadline.Export.stream_export_rows/2`, streams to temp file, stores via `Threadline.Storage`, and marks job "completed" with `file_path`.
    - Test 2: On failure, marks job as "failed" with `error_message`.
  </behavior>
  <action>
    Create `Threadline.Export.Orchestrator` with a `run(job_id, opts \\ [])` function.
    1. Fetch `Threadline.Governance.ExportJob`. Update status to `"running"`.
    2. Open a temp file. Write `Threadline.Export.csv_header/1`.
    3. Stream rows via `Threadline.Export.stream_export_rows(job.query_params)`.
    4. Chunk the stream, format with `Threadline.Export.format_changes_iodata(rows, :csv)`, and stream into the temp file.
    **Reference the streaming pattern from its analog `lib/threadline/operator_surface/controllers/export_controller.ex` (mapped in PATTERNS.md)** to ensure correct streaming behavior and backpressure handling during CSV generation.
    5. Pass the temp file to `Threadline.Storage.put/2`.
    6. Update job to `"completed"` with the returned `file_path`.
    7. Clean up the temp file.
    Use `timeout: :infinity` inside the Ecto transaction if needed for `Repo.stream`.
  </action>
  <verify>
    <automated>mix test test/threadline/export/orchestrator_test.exs</automated>
  </verify>
  <done>Orchestrator correctly streams export to file without blowing up memory.</done>
</task>

<task type="auto">
  <name>Task 3: Default TaskQueue Adapter</name>
  <files>lib/threadline/export_queue/task_adapter.ex, test/threadline/export_queue/task_adapter_test.exs</files>
  <action>
    Create `Threadline.ExportQueue.TaskAdapter` implementing `@behaviour Threadline.ExportQueue`.
    The `enqueue/2` function should take a `job_id` and spawn a background task.
    Use `Task.Supervisor.start_child/3`, defaulting the supervisor name to `Threadline.Export.TaskSupervisor` (or accept it in opts). The child runs `Threadline.Export.Orchestrator.run(job_id)`.
  </action>
  <verify>
    <automated>mix test test/threadline/export_queue/task_adapter_test.exs</automated>
  </verify>
  <done>Task adapter implements the behaviour and enqueues tasks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system | Writing massive files directly to the local FS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Tampering | `Threadline.Storage.Local` | mitigate | Ensure path checking uses strict file existence checks before attempting to copy. |
| T-78-02 | Denial of Service | `Orchestrator` | mitigate | Stream directly to disk in chunks to avoid memory exhaustion (OOM). |
</threat_model>

<verification>
- Verify large exports use constant memory.
- Verify `Local.put/2` correctly distinguishes file paths from literal CSV strings.
</verification>

<success_criteria>
- Core export engine works end-to-end for background jobs.
</success_criteria>

<output>
After completion, create `.planning/phases/78-async-exports-ui/78-01-SUMMARY.md`
</output>