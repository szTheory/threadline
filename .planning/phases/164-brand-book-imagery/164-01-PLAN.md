---
phase: 164-brand-book-imagery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/index.html
  - .planning/phases/164-brand-book-imagery/evidence/
autonomous: true
requirements: [IMG-01]

must_haves:
  truths:
    - "index.html has an Imagery section visually demonstrating brand-book.md's 'Imagery And Diagrams' guidance"
    - "3-4 inline-SVG acceptable specimens in the brand language, each captioned with when to use it"
    - "A banned-imagery don't strip at the misuse-gallery craft bar"
    - "All existing gates stay green (brand-gate, zero-network, <=300KB)"
  artifacts:
    - path: "brandbook/index.html"
      provides: "Imagery section between the misuse gallery and color system (placement: executor's craft call)"
      contains: "magery"
---

<objective>
Close UAT gap 2: the brand book documents imagery rules in `brandbook/brand-book.md` ("Imagery And Diagrams", lines ~361-387) but `index.html` never visualizes them. Add a visual Imagery section at the same craft bar as the misuse gallery. User verbatim: "it doesnt have an acceptable imagery section either which kinda sucks".
</objective>

<context>
@brandbook/brand-book.md (the "Imagery And Diagrams" section is the content authority — do not invent new rules)
@.planning/phases/162-brand-book-v2/162-02-SUMMARY.md (index.html structure, gate suite)
@brandbook/tokens.css (palette tokens for the specimens)

Constraints carried from Phase 162: zero `<text>` in committed SVG FILES applies to brandbook/*.svg assets; INLINE illustration SVG inside index.html may use HTML text for labels (it is page content, like the misuse gallery) but specimen LINEWORK is pure paths; zero network over file:// (no fetchable http refs; xmlns fine); brandbook/ ≤ 300KB; no process-history vocabulary; "all killer no filler".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Imagery section</name>
  <files>brandbook/index.html</files>
  <action>
Add an Imagery section to index.html (heading voice matching the existing section style, e.g. "Lines, not lock icons." — executor's craft call) containing:

1. **3-4 acceptable-imagery specimens**, drawn as inline SVG in the brand language (night-infrastructure palette, thread/stitch linework, restrained):
   - a line map / evidence-path illustration (points connected into an intelligible path);
   - a timeline-with-depth rail (layered narrative rail, not a gantt chart);
   - contour/cross-section linework (history as layers);
   - a labeled system-flow diagram using domain nouns (request → action → transaction → change), sparse arrows, layered rails.
   Each specimen captioned with one sentence on when to use it (docs hero, blog header, architecture explainer, etc.). Show at least one specimen on a light panel since imagery ships into light docs surfaces too.
2. **A "don't" strip** mirroring brand-book.md's banned list (stock people, courtroom imagery, locks, shields, police tape, random server racks, unlabelled node graphs) with the same visual treatment as the misuse gallery — small crossed-out glyph specimens or an equivalent treatment; keep it tight.
3. Diagram-style rules from brand-book.md restated briefly next to the specimens (lines/points/labels/layered rails; domain nouns; sparse arrows; actual flow over decorative complexity).

The specimens are themselves brand artifacts — they will be copied as starting points for real diagrams. Craft bar: "elegantly good".
  </action>
  <verify>
    <automated>sh -c 'grep -qiE "imagery|illustration" brandbook/index.html && grep -cE "(src|href)[[:space:]]*=[[:space:]]*[\"'"'"']https?://" brandbook/index.html | grep -qx 0 && ! grep -q "url(http" brandbook/index.html && node .planning/phases/162-brand-book-v2/tools/brand-gate.mjs brandbook && find brandbook -type f ! -name ".DS_Store" -print0 | xargs -0 du -ck | tail -1 | awk "\$1<=300{exit 0}{exit 1}"'</automated>
  </verify>
  <done>Imagery section present with 3-4 captioned specimens + don't strip; all gates green; budget held.</done>
</task>

<task type="auto">
  <name>Task 2: Evidence + commit</name>
  <files>.planning/phases/164-brand-book-imagery/evidence/</files>
  <action>
Playwright file:// screenshots (e2e createRequire pattern): imagery-desktop.png (1440 width, section scrolled into view, full section visible) and imagery-mobile.png (390 width) into the phase evidence/ dir. Eyeball both: nothing broken, light panel renders correctly. Stage by explicit path (brandbook/index.html + phase dir only; pre-existing lib//examples//test/ changes never staged). Commit "feat(164-01): add imagery section to brand book". Write 164-01-SUMMARY.md noting the pending mini-UAT, update REQUIREMENTS traceability for IMG-01 to "Complete — pending mini-UAT", commit.
  </action>
  <verify>
    <automated>sh -c 'test -s .planning/phases/164-brand-book-imagery/evidence/imagery-desktop.png && test -s .planning/phases/164-brand-book-imagery/evidence/imagery-mobile.png && git diff --cached --name-only | grep -cvE "^(\.planning/|brandbook/)" | grep -qx 0'</automated>
  </verify>
  <done>Screenshots captured and inspected; commits staged phase-dir + brandbook only; SUMMARY written.</done>
</task>

</tasks>

<success_criteria>
- IMG-01: visual Imagery section live in index.html, gates green, evidence captured; user mini-UAT pending as the phase gate.
</success_criteria>
