---
phase: 163
plan: 01
type: execute
autonomous: true
wave: 1
depends_on: [162]
requirements: [ROLL-01, ROLL-02, ROLL-03]
---

# Phase 163 Plan 01: Product Logo Rollout

## Objective

User opted IN at the end-of-milestone checkpoint ("Roll out now"). Roll the v1.35
winning identity (C13 stitch family — `brandbook/logo-mark.svg`, `logo-primary.svg`,
`logo-primary-light.svg`, `favicon.svg`, `logo-wordmark.svg` are the geometry
authority) into the three product surfaces named by ROLL-01..03.

## Context

- `brandbook/logo-primary.svg` — full lockup geometry (glyph outlines + stitch arc,
  viewBox `-80 60 5194 1140`).
- `brandbook/favicon.svg` — chipless 16px cut with internal prefers-color-scheme flip.
- `brandbook/index.html` "Dark and light" — documented README `<picture>` snippet.
- Working tree carries ~31 uncommitted files from other lanes (nav overhaul, example
  app). Only two uncommitted artifacts are THIS lane's targets: untracked
  `lib/threadline/operator_surface/components/logo.ex` and untracked
  `examples/threadline_phoenix/priv/static/images/` (old-mark content; replace
  wholesale, intentional). Never stage/edit/revert any other uncommitted file.
  Stage strictly by explicit path.
- `style.ex` stays untouched (v1.31 design-contract freeze; recorded exception
  covers only logo.ex).
- Baseline `mix verify.test` run BEFORE changes; gate is no NEW failures.

## Tasks

### Task 1 (ROLL-01): C13 lockup in operator-surface logo component — `type="auto"`

Rewrite the SVG body of `Logo.compact/1` to the C13 mark + wordmark using brandbook
geometry verbatim (logo-primary paths). Contract preserved: class
`tl-topbar__brand-logo`, `aria-hidden="true"`, `focusable="false"`, `{@rest}`,
theming via `var(--tl-*)` (glyph outlines `var(--tl-color-text)`, stitch arc
`var(--tl-color-accent)`; the brand constant #4781E6 stays a brand-asset value only).
Flat C13 — old gradient defs removed. Wordmark rendered as pure paths; a
non-rendering `<text class="tl-topbar__brand-wordmark" display="none">` compat node
is kept so the in-flight nav-overhaul lane's uncommitted test assertions
(`>Threadline</text>`, wordmark class) stay green — documented deviation, removable
when that lane lands.

**Gate:** `mix compile --warnings-as-errors` green; `mix verify.test` no NEW
failures vs baseline.

### Task 2 (ROLL-02): example-app admin favicon — `type="auto"`

Replace `examples/threadline_phoenix/priv/static/images/threadline-admin-favicon.svg`
content with `brandbook/favicon.svg` content.

**Gate:** `xmllint --noout` clean; no `<rect>` chip; no `<text>`.

### Task 3 (ROLL-03): README brand header — `type="auto"`

Add documented `<picture>` header at the top of root `README.md`
(dark → `brandbook/logo-primary.svg`, light fallback `brandbook/logo-primary-light.svg`,
width 420), preserving all existing content/badges. README literals are locked by
doc-contract tests — satisfy additively, never weaken assertions.

**Gate:** `mix verify.test` no NEW failures vs baseline.

### Task 4: Evidence — `type="auto"`

Render the new component SVG (dark tokens from style.ex) in a standalone HTML file,
screenshot via Playwright CLI as `topbar-mark-evidence.png` in this phase dir
(launching the full app is heavyweight; component-standalone render approved).
Favicon tab screenshot only if cheap (browser tab chrome is not page-screenshot
territory; skip with note if not).

### Task 5: Close out — `type="auto"`

`163-01-SUMMARY.md`; REQUIREMENTS.md ROLL-01..03 → Complete; ROADMAP Phase 163 row;
STATE.md; final docs commit.

## Verification / Success Criteria

- [ ] Logo component renders C13 lockup, `--tl-*` contract intact, style.ex untouched
- [ ] `mix compile --warnings-as-errors` green
- [ ] `mix verify.test`: no NEW failures vs pre-change baseline
- [ ] Favicon = brandbook chipless C13 cut, parses, no rect/text
- [ ] README header uses documented picture pattern, all prior content preserved
- [ ] Evidence PNG in phase dir
- [ ] Per-task commits `feat(163-01): ...`, explicit paths only
