---
phase: 67
plan: 03
type: execute
wave: 2
depends_on:
  - 67-01
files_modified:
  - lib/threadline/operator_surface/live/policy_redaction_live.ex
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/live/policy_redaction_live_test.exs
autonomous: true
requirements:
  - REDN-03
  - REDN-04
user_setup: []
tags:
  - elixir
  - threadline
  - liveview
  - operator-surface
  - policy
  - redaction

must_haves:
  truths:
    - "New LiveView route literal is `live(\"/policy/redaction\", PolicyRedactionLive, :index)` under the existing operator-surface scope"
    - "LiveView file uses the file-scope `Code.ensure_loaded?(Phoenix.LiveView)` gate; no new hard Phoenix dependency leaks into capture-only installs"
    - "The page renders explicit sections in locked order: `Drift detected`, `Could not introspect`, `Config matches deployed`"
    - "Each section header carries its count"
    - "Details show exact configured and deployed `exclude` / `mask` sets and placeholder metadata when relevant"
    - "Disclosure state is local UI state only; no URL contract is introduced"
    - "No sample values are rendered; only column names and placeholder metadata"
    - "Row-level copy uses the plain operator-safe status/hint wording from CONTEXT.md"
  artifacts:
    - path: "lib/threadline/operator_surface/live/policy_redaction_live.ex"
      provides: "Read-only drift-aware redaction operator page"
      contains: "defmodule Threadline.OperatorSurface.Live.PolicyRedactionLive"
    - path: "lib/threadline/operator_surface/router.ex"
      provides: "Sibling route wiring for `/policy/redaction`"
      contains: "live(\"/policy/redaction\", PolicyRedactionLive, :index)"
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "Additive policy-redaction table and disclosure styling under `.threadline-ui`"
    - path: "test/threadline/operator_surface/live/policy_redaction_live_test.exs"
      provides: "LiveView integration tests for ordering, copy, disclosure details, and no-sample-values invariants"
  key_links:
    - from: "lib/threadline/operator_surface/live/policy_redaction_live.ex"
      to: "lib/threadline/policy/redaction_presenter.ex"
      via: "single shared fact source"
      pattern: "Threadline\\.Policy\\.RedactionPresenter"
    - from: "lib/threadline/operator_surface/live/policy_redaction_live.ex"
      to: "lib/threadline/operator_surface/live/coverage_live.ex"
      via: "file-scope gate and `.threadline-ui` render pattern"
      pattern: "Threadline\\.OperatorSurface\\.Style\\.css"
---

<objective>
Build the read-only Phase 67 operator surface page that makes configured-vs-deployed redaction drift obvious at a glance while still allowing per-table exactness on demand.

Purpose: this is the human-first incident surface for Phase 67. It should look and behave like a sibling of `CoverageLive`, but the new facts come from the shared redaction presenter and the page is organized around drift-first triage.

Output: one new LiveView, additive route wiring, additive styling, and one LiveView integration test file.
</objective>

<context>
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-CONTEXT.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-RESEARCH.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-PATTERNS.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-01-PLAN.md
@lib/threadline/operator_surface/live/coverage_live.ex
@lib/threadline/operator_surface/router.ex
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/live/coverage_live_test.exs

Locked UX facts:

- summary counts first
- three sections in fixed order
- local disclosure only
- exact configured/deployed sets remain inspectable
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PolicyRedactionLive and route/style wiring</name>
  <files>
    lib/threadline/operator_surface/live/policy_redaction_live.ex,
    lib/threadline/operator_surface/router.ex,
    lib/threadline/operator_surface/style.ex
  </files>
  <action>
    Create `PolicyRedactionLive` as a sibling of `CoverageLive`:

    - mount under `.threadline-ui`
    - call the shared presenter on mount
    - render summary counts and three explicit sections
    - preserve alphabetical ordering within each section by consuming the presenter's ordered grouped output directly
    - use simple local disclosure for per-table exact details
    - show operator-safe hint copy for drift and introspection failure

    Add route:
    - `live("/policy/redaction", PolicyRedactionLive, :index)`

    Extend `style.ex` with additive classes for section headers, low-noise match rows, and detail tables while preserving the existing namespace.
  </action>
  <verify>
    - file-scope Phoenix gate remains on the LiveView file
    - no URL params or deep-linkable disclosure state are added
  </verify>
  <done>
    - `/policy/redaction` route is wired and renders summary + three locked sections
    - section order is fixed and row order within each section is alphabetical
    - details show exact configured/deployed `exclude` and `mask` sets without sample values
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LiveView integration tests for drift ordering and safe copy</name>
  <files>
    test/threadline/operator_surface/live/policy_redaction_live_test.exs
  </files>
  <action>
    Mirror the `CoverageLive` test harness and add runtime assertions for:

    - route mounts successfully at `/audit/policy/redaction`
    - section order is drift / could-not-introspect / matches
    - rows are alphabetical within each section
    - section headers include counts
    - row copy uses `Config matches deployed`, `Drift detected`, `Could not introspect`
    - per-table detail view shows exact configured/deployed `exclude` and `mask` sets
    - a parity-oriented fixture proves the rendered table inventory, statuses, and hints match the shared presenter output used by the Mix task
    - no sample values appear anywhere in rendered HTML
  </action>
  <verify>
    - LiveView tests do not require any new cross-page surface header behavior
    - fixtures exercise at least one row in each top-level status bucket
  </verify>
  <done>
    - LiveView integration test locks section order, alphabetical ordering, parity facts, detail disclosure, and no-sample-values behavior
  </done>
</task>

</tasks>

<verification>
- `mix test test/threadline/operator_surface/live/policy_redaction_live_test.exs`
- `mix verify.compile_no_optional`
</verification>

<success_criteria>
- Operators can scan drift first and still inspect exact per-table sets without guesswork.
- The new page compiles cleanly when Phoenix is absent.
- UI facts stay driven entirely by the shared presenter output.
</success_criteria>
