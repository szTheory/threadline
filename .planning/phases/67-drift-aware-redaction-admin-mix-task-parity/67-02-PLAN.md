---
phase: 67
plan: 02
type: execute
wave: 2
depends_on:
  - 67-01
files_modified:
  - lib/mix/tasks/threadline.policy.show.ex
  - test/threadline/operator_surface/policy_show_mix_test.exs
autonomous: true
requirements:
  - REDN-05
user_setup: []
tags:
  - elixir
  - threadline
  - mix-task
  - policy
  - redaction
  - parity

must_haves:
  truths:
    - "`mix threadline.policy.show` is a viewer, not a CI gate; drift does not exit non-zero"
    - "Default human output prints one summary line, one aligned table, and extra detail blocks only for `drift detected` and `could not introspect` tables"
    - "The default human table columns are `TABLE`, `STATUS`, `CONFIG`, `DEPLOYED`, `HINT`"
    - "`CONFIG` and `DEPLOYED` cells preserve the semantic split between `exclude` and `mask` and include placeholder metadata when relevant"
    - "`--json` emits a stable top-level object with summary fields plus a `tables` array; statuses are machine-stable enum-like strings `config_matches_deployed`, `drift_detected`, `could_not_introspect`"
    - "Mix task help text documents `mix threadline.policy.show` and `mix threadline.policy.show --json`"
    - "No sample values appear in human output or JSON"
    - "The task reuses `Threadline.Policy.RedactionPresenter` directly instead of rebuilding comparison logic"
  artifacts:
    - path: "lib/mix/tasks/threadline.policy.show.ex"
      provides: "Parity viewer task with compact aligned output and stable JSON"
      contains: "defmodule Mix.Tasks.Threadline.Policy.Show"
    - path: "test/threadline/operator_surface/policy_show_mix_test.exs"
      provides: "Mix integration test covering human output, JSON shape, and viewer-not-gate exit behavior"
  key_links:
    - from: "lib/mix/tasks/threadline.policy.show.ex"
      to: "lib/threadline/policy/redaction_presenter.ex"
      via: "single shared fact source"
      pattern: "Threadline\\.Policy\\.RedactionPresenter"
    - from: "lib/mix/tasks/threadline.policy.show.ex"
      to: "lib/mix/tasks/threadline.health.coverage.ex"
      via: "boot sequence and aligned-table viewer posture"
      pattern: "Mix\\.Task\\.run\\(\"app.config\""
---

<objective>
Ship the Phase 67 capture-only parity surface: `mix threadline.policy.show`, backed directly by the shared redaction presenter. The task must expose the same facts and state taxonomy as the LiveView while remaining terminal-native, compact, and stable for scripting via `--json`.

Purpose: Phase 67 explicitly requires parity for adopters who never mount Phoenix. This plan turns the shared presenter into a usable operator tool and locks the viewer-not-gate posture.

Output: one new Mix task and one integration test file.
</objective>

<context>
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-CONTEXT.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-RESEARCH.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-PATTERNS.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-01-PLAN.md
@lib/mix/tasks/threadline.health.coverage.ex
@lib/mix/tasks/threadline.verify_coverage.ex
@test/threadline/operator_surface/coverage_mix_test.exs

Locked output semantics:

- human copy uses `Config matches deployed`, `Drift detected`, `Could not introspect`
- rerun hint references `mix threadline.gen.triggers`
- JSON statuses use snake_case contract keys
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `mix threadline.policy.show` with human table + `--json`</name>
  <files>
    lib/mix/tasks/threadline.policy.show.ex
  </files>
  <action>
    Create a pure-stdlib Mix task that mirrors the Phase 66 boot sequence:

    - `Mix.Task.run("app.config", [])`
    - start `:ssl`, `:postgrex`, `:ecto_sql`
    - resolve and start the configured repo
    - call `Threadline.Policy.RedactionPresenter.build(repo: repo, schema: "public")`

    Default output:
    - summary line with counts
    - aligned table using `TABLE / STATUS / CONFIG / DEPLOYED / HINT`
    - detail blocks only for non-match rows

    `--json` output:
    - summary keys plus `tables`
    - stable status strings and additive structured fields
  </action>
  <verify>
    - task exits `0` when drift exists
    - task contains no Phoenix gating
    - task contains no sample value examples
  </verify>
  <done>
    - `mix threadline.policy.show` exists and exits `0` when drift exists
    - default output renders the locked `TABLE / STATUS / CONFIG / DEPLOYED / HINT` columns
    - `--json` emits stable summary fields plus a `tables` array with snake_case status enums
    - human rows preserve the canonical ordering delivered by the shared presenter
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Mix integration tests for parity and machine contract</name>
  <files>
    test/threadline/operator_surface/policy_show_mix_test.exs
  </files>
  <action>
    Add tests that cover:

    - default output includes the locked status literals and aligned columns
    - drift rows include the rerun-gen-triggers hint
    - `--json` top-level shape is stable and jq-friendly
    - `tables[*].status` values are exactly `config_matches_deployed`, `drift_detected`, `could_not_introspect`
    - row order matches the presenter's canonical section+alphabetical contract
    - task returns success even when drift exists
    - no sample values appear anywhere in output assertions
  </action>
  <verify>
    - use `Mix.Task.reenable/1` between invocations
    - capture output via `ExUnit.CaptureIO`
  </verify>
  <done>
    - Mix integration test proves viewer-not-gate behavior, stable JSON, canonical ordering, and no-sample-values invariants
  </done>
</task>

</tasks>

<verification>
- `mix test test/threadline/operator_surface/policy_show_mix_test.exs`
- `mix verify.compile_no_optional`
</verification>

<success_criteria>
- `mix threadline.policy.show` gives capture-only adopters the same facts as the LiveView.
- The JSON contract is stable, additive, and free of UI-only layout concerns.
- Drift does not cause a non-zero exit.
</success_criteria>
