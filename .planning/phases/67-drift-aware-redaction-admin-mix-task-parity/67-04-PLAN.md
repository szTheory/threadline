---
phase: 67
plan: 04
type: execute
wave: 3
depends_on:
  - 67-01
  - 67-02
  - 67-03
files_modified:
  - test/threadline/operator_surface/policy_show_doc_contract_test.exs
  - guides/operator-surface.md
  - guides/domain-reference.md
  - guides/production-checklist.md
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - REDN-05
user_setup: []
tags:
  - elixir
  - threadline
  - doc-contract
  - docs
  - policy
  - redaction

must_haves:
  truths:
    - "Doc-contract test is primarily pure source-reading and pins the new route literal, Mix-task help text, status literals, JSON status enums, and no-sample-values invariants"
    - "Doc-contract test locks `live(\"/policy/redaction\", PolicyRedactionLive, :index)` in `router.ex`"
    - "Doc-contract test locks human status literals `Config matches deployed`, `Drift detected`, `Could not introspect`"
    - "Doc-contract test locks JSON status values `config_matches_deployed`, `drift_detected`, `could_not_introspect`"
    - "Doc-contract test asserts no sample values appear in Phase 67 source literals or task output fixtures"
    - "README/guides/CHANGELOG document both the LiveView route and `mix threadline.policy.show` parity surface"
  artifacts:
    - path: "test/threadline/operator_surface/policy_show_doc_contract_test.exs"
      provides: "Source-reading Phase 67 contract test pinning literals and safety invariants"
    - path: "guides/operator-surface.md"
      provides: "New operator-surface section for redaction drift admin"
    - path: "guides/domain-reference.md"
      provides: "Updated domain semantics for configured-vs-deployed redaction drift"
    - path: "guides/production-checklist.md"
      provides: "Operational checklist items for rerunning triggers on drift"
    - path: "README.md"
      provides: "Top-level parity mention of the new policy viewer/task"
    - path: "CHANGELOG.md"
      provides: "v1.18 entry covering the new redaction admin surface and Mix task"
  key_links:
    - from: "test/threadline/operator_surface/policy_show_doc_contract_test.exs"
      to: "lib/threadline/operator_surface/router.ex"
      via: "route literal pin"
      pattern: "live\\(\"/policy/redaction\", PolicyRedactionLive, :index\\)"
    - from: "test/threadline/operator_surface/policy_show_doc_contract_test.exs"
      to: "lib/mix/tasks/threadline.policy.show.ex"
      via: "usage/help/JSON contract pins"
      pattern: "mix threadline\\.policy\\.show"
---

<objective>
Lock the Phase 67 contract in CI and close the adoption loop in docs. This plan makes future drift visible by pinning the route, user-facing status names, machine status enums, and no-sample-values guarantees, then updates the operator docs and changelog to match the shipped surface.

Purpose: Phase 67 adds a new operator-facing safety surface. The doc-contract test is the regression guard; the docs make the feature discoverable and operationally usable.

Output: one new doc-contract test plus additive updates to guides, README, and CHANGELOG.
</objective>

<context>
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-CONTEXT.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-RESEARCH.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-PATTERNS.md
@.planning/phases/66-coverage-dashboard-mix-task-parity/66-05-PLAN.md
@test/threadline/operator_surface/coverage_doc_contract_test.exs
@guides/operator-surface.md
@guides/domain-reference.md
@guides/production-checklist.md
@README.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 67 doc-contract test</name>
  <files>
    test/threadline/operator_surface/policy_show_doc_contract_test.exs
  </files>
  <action>
    Create a source-reading contract test that pins:

    - route literal in `router.ex`
    - LiveView status literals
    - Mix-task usage/help literals
    - JSON status enums
    - rerun-gen-triggers hint presence
    - alphabetical ordering and cross-surface parity invariants
    - no-sample-values invariants
    - optional-Phoenix gating posture (LiveView file gated, shared presenter/Mix task ungated)
  </action>
  <verify>
    - runtime assertions are used only where necessary for JSON output shape
    - refutes are included for sample-value leakage patterns
  </verify>
  <done>
    - doc-contract test pins route, human statuses, JSON enums, rerun hint, ordering/parity invariants, and no-sample-values behavior
    - the test distinguishes LiveView-gated files from ungated shared/Mix files
  </done>
</task>

<task type="auto">
  <name>Task 2: Update operator docs and changelog</name>
  <files>
    guides/operator-surface.md,
    guides/domain-reference.md,
    guides/production-checklist.md,
    README.md,
    CHANGELOG.md
  </files>
  <action>
    Document:

    - `/audit/policy/redaction` route and its read-only purpose
    - `mix threadline.policy.show` default output and `--json`
    - the meaning of the three top-level states
    - the rerun `mix threadline.gen.triggers` operational response on drift/introspection failure
    - the guarantee that sample values are never shown
  </action>
  <verify>
    - docs stay aligned with the exact route and command literals
    - docs do not overstate parser confidence; ambiguity is documented as `Could not introspect`
  </verify>
  <done>
    - operator docs, README, and CHANGELOG all mention the exact route and Mix command literals
    - docs explain the three states, rerun response, and no-sample-values guarantee without overstating parser certainty
  </done>
</task>

</tasks>

<verification>
- `mix test test/threadline/operator_surface/policy_show_doc_contract_test.exs`
- `mix verify.compile_no_optional`
- `mix verify.test`
</verification>

<success_criteria>
- CI will fail if route/status/JSON/no-sample-values contract drifts.
- Docs clearly tell an adopter where to inspect redaction drift and what action to take when drift is detected.
</success_criteria>
