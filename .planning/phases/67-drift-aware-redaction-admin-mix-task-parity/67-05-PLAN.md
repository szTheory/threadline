---
phase: 67
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - lib/mix/tasks/threadline.policy.show.ex
  - lib/threadline/policy/redaction_presenter.ex
  - lib/threadline/capture/trigger_capture_config.ex
  - test/threadline/operator_surface/policy_show_doc_contract_test.exs
  - test/threadline/operator_surface/policy_show_mix_test.exs
  - test/threadline/policy/redaction_presenter_test.exs
autonomous: true
requirements:
  - REDN-03
  - REDN-04
  - REDN-05
must_haves:
  truths:
    - "The Phase 67 Elixir source and test files called out in 67-UAT are `mix format` compliant."
    - "`mix ci.all` completes successfully after the formatting remediation."
    - "Phase 67 behavior stays unchanged while the repo-level verification gate returns to green."
  artifacts:
    - path: "lib/mix/tasks/threadline.policy.show.ex"
      provides: "Formatter-compliant Mix parity task source"
    - path: "lib/threadline/policy/redaction_presenter.ex"
      provides: "Formatter-compliant shared reconciliation presenter"
    - path: "lib/threadline/capture/trigger_capture_config.ex"
      provides: "Formatter-compliant trigger capture config source"
    - path: "test/threadline/operator_surface/policy_show_doc_contract_test.exs"
      provides: "Formatter-compliant doc-contract test"
    - path: "test/threadline/operator_surface/policy_show_mix_test.exs"
      provides: "Formatter-compliant Mix parity test"
    - path: "test/threadline/policy/redaction_presenter_test.exs"
      provides: "Formatter-compliant presenter behavior test"
  key_links:
    - from: "67-UAT.md"
      to: "lib/mix/tasks/threadline.policy.show.ex"
      via: "Gap 5 artifact list"
      pattern: "Formatting drift"
    - from: "67-UAT.md"
      to: "mix ci.all"
      via: "Gap closure verification"
      pattern: "mix ci\\.all"
---

<objective>
Close the diagnosed Phase 67 verification gap by restoring formatter compliance in the affected Elixir files and re-running the full repo verification gate.

Purpose: Phase 67 cannot be marked verified while `mix ci.all` fails at `mix format --check-formatted`. This plan fixes only that gap and re-proves the gate.

Output: formatter-clean Phase 67 source/test files plus fresh `mix ci.all` evidence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-UAT.md
@.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reformat the diagnosed Phase 67 files</name>
  <files>lib/mix/tasks/threadline.policy.show.ex, lib/threadline/policy/redaction_presenter.ex, lib/threadline/capture/trigger_capture_config.ex, test/threadline/operator_surface/policy_show_doc_contract_test.exs, test/threadline/operator_surface/policy_show_mix_test.exs, test/threadline/policy/redaction_presenter_test.exs</files>
  <action>Run `mix format` on the six files listed in Gap 5 of `67-UAT.md`. Keep the scope to formatter-driven changes only; do not alter behavior, literals, routing, docs, or test coverage while remediating the drift.</action>
  <verify>
    <automated>mix format --check-formatted lib/mix/tasks/threadline.policy.show.ex lib/threadline/policy/redaction_presenter.ex lib/threadline/capture/trigger_capture_config.ex test/threadline/operator_surface/policy_show_doc_contract_test.exs test/threadline/operator_surface/policy_show_mix_test.exs test/threadline/policy/redaction_presenter_test.exs</automated>
  </verify>
  <done>All six diagnosed Phase 67 files are formatter-compliant with no behavioral edits beyond whitespace / canonical formatting.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the repo verification gate</name>
  <files>none</files>
  <action>Run `mix ci.all` after formatting completes to verify the release gate named in `67-UAT.md` is green again. If the command fails for a new non-formatting reason, stop and record that blocker instead of broadening the fix scope.</action>
  <verify>
    <automated>mix ci.all</automated>
  </verify>
  <done>`mix ci.all` exits 0, confirming the Phase 67 formatting gap is closed without introducing new regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local source -> formatter | Automated formatting rewrites checked-in Elixir source without changing runtime behavior |
| local repo -> CI gate | The repo verification alias validates that the formatting-only remediation did not break the shipped operator surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-gap-01 | T | Phase 67 Elixir files | mitigate | Limit Task 1 to `mix format` on the diagnosed files only and reject any non-formatting edits during execution. |
| T-67-gap-02 | R | `mix ci.all` evidence | mitigate | Capture the exact `mix ci.all` result after formatting so the phase is re-verified against the canonical repo gate. |
| T-67-gap-03 | D | gap-closure scope | accept | Formatting remediation does not alter trust boundaries or data exposure; residual risk is limited to accidental scope expansion, which Task 2 explicitly blocks. |
</threat_model>

<verification>
Run the targeted formatter check on the six diagnosed files, then run `mix ci.all`.
</verification>

<success_criteria>
The six files named in the UAT gap are formatter-compliant, `mix ci.all` exits 0, and no unrelated Phase 67 behavior or docs are changed.
</success_criteria>

<output>
After completion, create `.planning/phases/67-drift-aware-redaction-admin-mix-task-parity/67-05-SUMMARY.md`
</output>
