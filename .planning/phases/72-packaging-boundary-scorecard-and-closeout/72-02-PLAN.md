---
phase: 72-packaging-boundary-scorecard-and-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - mix.exs
autonomous: true
requirements:
  - PKG-02
must_haves:
  truths:
    - Module documentation grouping explicitly names the operator surface as optional and in-tree.
  artifacts:
    - path: mix.exs
      provides: Module grouping alignment
  key_links: []
---

<objective>
Align module-grouping in package contracts.

Purpose: Fulfill PKG-02 by aligning the `mix.exs` docs config.
Output: Updated `mix.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align mix.exs docs groups</name>
  <files>mix.exs</files>
  <action>In `mix.exs`, locate the `groups_for_modules` key `"Operator Surface"`. Rename it to `"Operator Surface (Optional In-Tree)"` to match the closeout decision. This solidifies the package contract that these modules are part of the core package but optional.</action>
  <verify>
    <automated>grep -q "Operator Surface (Optional In-Tree)" mix.exs</automated>
  </verify>
  <done>mix.exs docs config reflects the optional in-tree nature of the surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | Package definition metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02 | Information Disclosure | mix.exs | accept | No PII or credentials; pure open source package metadata. |
</threat_model>

<verification>
mix verify.compile_no_optional
mix ci.all
</verification>

<success_criteria>
Module documentation grouping reflects the package boundary decision.
</success_criteria>

<output>
After completion, create `.planning/phases/72-packaging-boundary-scorecard-and-closeout/72-02-SUMMARY.md`
</output>
