---
phase: 72-packaging-boundary-scorecard-and-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/upgrade-path.md
  - README.md
autonomous: true
requirements:
  - PKG-01
  - PKG-02
must_haves:
  truths:
    - User can read objective triggers for extraction in the upgrade path guide.
    - User can see the clear "Stay in-tree for now" decision.
    - User is guaranteed that future package splits won't break the router integration API.
  artifacts:
    - path: guides/upgrade-path.md
      provides: Scorecard definition and API guarantee
    - path: README.md
      provides: Consistent stay in-tree messaging
  key_links: []
---

<objective>
Document the `threadline_web` extraction scorecard and closeout decision.

Purpose: Fulfill PKG-01 and PKG-02 by providing an evidence-based package boundary decision.
Output: Updated guides and README.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scorecard to upgrade-path.md</name>
  <files>guides/upgrade-path.md</files>
  <action>Add a new section `## Packaging Boundary Scorecard` that defines the objective triggers for a future `threadline_web` extraction: Version Matrix Pressure, Release Cadence Divergence, and Adopter Glue Burden. Document the v1.19 closeout decision as "Stay in-tree for now" based on current evidence. Add a firm guarantee that if a split happens in the future, it will not break the public `threadline_operator_surface/2` router integration API.</action>
  <verify>
    <automated>grep -q "Packaging Boundary Scorecard" guides/upgrade-path.md</automated>
  </verify>
  <done>Guide contains scorecard criteria and explicit stay-in-tree decision with API guarantee.</done>
</task>

<task type="auto">
  <name>Task 2: Align README wording</name>
  <files>README.md</files>
  <action>Update the `## Operator Surface` section to explicitly mention the "stay in-tree" decision for the optional LiveView UI, referencing the new Packaging Boundary Scorecard in `guides/upgrade-path.md`. Ensure that the wording reinforces the optional-deps nature.</action>
  <verify>
    <automated>grep -qi "stay in-tree" README.md</automated>
  </verify>
  <done>README Operator Surface section accurately reflects the in-tree decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | Internal project contract decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Information Disclosure | Documentation | accept | No PII or credentials; pure open source package strategy. |
</threat_model>

<verification>
mix verify.compile_no_optional
mix ci.all
</verification>

<success_criteria>
Scorecard and closeout decision are fully documented in `guides/upgrade-path.md` and `README.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/72-packaging-boundary-scorecard-and-closeout/72-01-SUMMARY.md`
</output>
