---
phase: 206-installer-migration-versions
plan: 02
type: execute
wave: 2
depends_on: [206-01]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [CR-01, WR-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CHANGELOG.md's `## Unreleased — highlights` section describes the fix in the file's own template: lead prose, `### Breaking changes` None, `### Required action`, then `### Fixed` (D-12)"
    - "A search for the literal Ecto error `migrations can't be executed, migration version` lands on the Unreleased entry, which says every release through 0.10.1 is affected and gives the semantics-then-governance rename workaround (D-12)"
    - "The entry mentions `mix threadline.gen.triggers` and the corrected partial re-run advice, names no hidden module in backticks (D-05), and carries no planning IDs (D-12 wording deviation, recorded below)"
    - "No hex.retire step and no new Troubleshooting guide section are added (D-13)"
    - "The full suite, format, credo, dialyzer, xref, the docs build with warnings as errors, and an end-to-end install-then-gen.triggers probe all pass on the combined phase code"
  artifacts:
    - path: CHANGELOG.md
      provides: "Unreleased entry for the migration-version fix"
      contains: "is duplicated"
  key_links:
    - from: "CHANGELOG.md Unreleased entry"
      to: "test/threadline/release_artifact_contract_test.exs vocabulary scan + test/threadline/changelog_contract_test.exs + public_surface_contract_test.exs reference validator"
      via: "packaged file scanned by the default mix test"
      pattern: "## Unreleased — highlights"
---

<objective>
Tell adopters about the fix in the human-owned CHANGELOG (D-12), keeping it inside the
packaged-file vocabulary and reference gates (D-05). Then run the phase gate over plan 01's
code plus this entry.

Purpose: adopters who hit the duplicate-version error need to find the answer by searching the
error text, and must be able to recover on earlier releases. The phase is not done until every
named gate is green on the combined change.

Output: one CHANGELOG.md edit and a `docs(changelog)` commit. The gate results are recorded in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/206-installer-migration-versions/206-CONTEXT.md
@.planning/phases/206-installer-migration-versions/206-RESEARCH.md
@.planning/phases/206-installer-migration-versions/206-01-SUMMARY.md
@CHANGELOG.md
</context>

## Recorded deviation (explicit, reasoned, not silent)

**D-12 says to mention that "the partial re-run advice was corrected (WR-03)".** CHANGELOG.md ships
in the Hex tarball and is scanned by `test/threadline/release_artifact_contract_test.exs:7-14`,
whose `requirement_id` pattern bans `WR-\d{2,}` (and `decision_id` bans `D-\d{2,}`). Writing the
ID would fail the default `mix test`. Per RESEARCH ("Conflict the planner MUST resolve in favor of
the archive gate"), the entry keeps the **meaning** (the installer's advice on a partial re-run was
corrected) and **drops the ID**. The same applies to CR-01, which means nothing to an adopter. The
executor records this deviation in the SUMMARY.

## Other recorded choices

- Spec-less probe fallback skipped (no requirement IDs). API-coverage and assumption-delta
  detectors returned `detected: false`. There are no schema-push paths. See plan 01.
- Following RESEARCH Open Question 1: keep the `## Unreleased — highlights` heading and its
  explanatory paragraph, and replace only the `_Nothing yet for the next release._` placeholder line.
- Reversibility: every change here is an ordinary local edit and commit (rating `reversible`). The
  release itself (release-please cutting 0.10.2) happens after this phase and is out of scope. No
  `checkpoint:decision` is needed.

## Guardrails (executor MUST follow)

- **Execution location (checked before Task 1 and before Task 2):** execute in the main checkout
  `/Users/jon/projects/threadline` with isolation `none`, never in a git worktree. Every
  `<automated>` command `cd`s into that path, and worktrees have no `deps/` or `_build/`, so no
  Elixir gate can run in one; a worktree run would verify the unmodified main tree. Run
  `git rev-parse --show-toplevel`: if it does not print exactly `/Users/jon/projects/threadline`,
  HALT and report the printed path. Never copy, move, or sync files between a worktree and the
  main checkout to make a verify pass, and do not edit `.planning/config.json` or
  `.gsd/dispatch-isolation-sentinel.json` to change the isolation mode.
- Do NOT edit any test, contract regex, `mix.exs`, `.credo.exs`, guides, or hook config to make a
  gate pass. Do NOT use `--no-verify`. If a gate fails, HALT and report the exact output.
  Exception: a failure caused by plan 01's code means reporting it, not fixing it silently here.
- Do NOT add a Troubleshooting guide section or run `mix hex.retire` (D-13). Do NOT touch
  `guides/getting-started-saas.md`.
- Stage explicit paths only (never `git add .planning/`, `.` or `-A`). Do not push.
- Temp files go under `/Users/jon/.claude/jobs/77cf1bdd/tmp`. Strip ANSI before grepping test
  output, under `bash -c`.
- Do NOT run Playwright or `mix ci.all`'s browser lane. Use the targeted gates below.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — Unreleased CHANGELOG entry, proven through the packaged-file contract tests</name>
  <files>CHANGELOG.md</files>
  <precondition>`git rev-parse --show-toplevel` prints exactly /Users/jon/projects/threadline (main checkout, not a worktree) and plan 01's three fix commits are in `git log`; otherwise HALT and report, never copy files between trees.</precondition>
  <read_first>
    - CHANGELOG.md:1-65 (ownership comment, the Unreleased block at :20-27, and the [0.10.1] template at :29-65)
    - test/threadline/changelog_contract_test.exs:33-46 (section-order and heading regexes)
    - test/threadline/release_artifact_contract_test.exs:7-14 (packaged vocabulary regexes)
    - test/threadline/public_surface_contract_test.exs:91 (CHANGELOG is a reference subject; backticked modules must be public)
    - deps/ecto_sql/lib/ecto/migrator.ex:708-712 (exact error text)
  </read_first>
  <action>
    Per D-12, replace only the italic nothing-yet placeholder line under "## Unreleased — highlights" (keep the heading and its paragraph byte-for-byte; never use a bracketed Unreleased heading). Write, in order, in the sober docs voice of the [0.10.1] entry, wrapped near 80 columns:

    1. A lead prose paragraph: `mix threadline.install` could give two or three of its generated migrations the same version, so `mix ecto.migrate` refused to run them. Every release through 0.10.1 is affected. The installer has written the audit and semantics migrations with a shared one-second timestamp since 0.1.0, and it has written the governance migration since 0.6.0. The installer now gives each migration a distinct version that sorts after every migration already in the directory.
    2. "### Breaking changes" then "None."
    3. "### Required action" then: None for an app that has already migrated, including one whose migration files were renamed by hand, because the installer runs once. Then a paragraph for an adopter on an earlier release whose migrate failed with the error: rename the `_threadline_semantics_schema.exs` migration's numeric prefix, and then the `_threadline_governance_schema.exs` one, to later timestamps so the order is audit, then semantics, then governance, and re-run `mix ecto.migrate`.
    4. "### Fixed" with bullets:
       - `mix threadline.install` no longer writes duplicate migration versions. Quote the error verbatim in backticks: (Ecto.MigrationError) migrations can't be executed, migration version <N> is duplicated. State that the versions are computed once, before any file is written, and sort after the newest existing migration, including a future-dated one.
       - `mix threadline.gen.triggers` had the same kind of bug: run in the same second as the installer, its migration could share a version. It now uses the same version logic.
       - The dedicated-schema advice now prints only after a fresh install that wrote all three migrations. A re-run that finds some Threadline migrations already present is told to keep `:storage_schema` unset, because switching then would split Threadline's tables across two schemas. The fresh-install advice no longer ends with a paragraph that contradicted its own steps.

    Constraints (D-05 and the packaged-file gates): name only the public Mix tasks in backticks. Describe the internal version helper in words, never by module name. Include no review-finding, decision, phase, or milestone identifiers (see "Recorded deviation"). Use no commit-link bullets and no "* " bullets (use "- "). Do not add a hex.retire note or a guide link to a troubleshooting section (D-13).

    Commit with an explicit path: git add CHANGELOG.md; subject "docs(changelog): note the installer migration-version fix"; Co-Authored-By trailer.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test test/mix/tasks/threadline/install_test.exs test/threadline/changelog_contract_test.exs test/threadline/release_artifact_contract_test.exs test/threadline/public_surface_contract_test.exs test/threadline/code_walkthrough_doc_contract_test.exs test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/upgrade_path_doc_contract_test.exs'</automated>
    <fails_when>non-zero exit, or a summary line reporting any failures (for example archive_vocabulary, reference-validator, or section-order failures naming CHANGELOG.md)</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && awk "/^## Unreleased/,/^## \\[0\\.10\\.1\\]/" CHANGELOG.md > /Users/jon/.claude/jobs/77cf1bdd/tmp/206-unreleased.md && grep -q "migrations can.t be executed, migration version" /Users/jon/.claude/jobs/77cf1bdd/tmp/206-unreleased.md && grep -q "is duplicated" /Users/jon/.claude/jobs/77cf1bdd/tmp/206-unreleased.md && grep -q "0.10.1" /Users/jon/.claude/jobs/77cf1bdd/tmp/206-unreleased.md && grep -q "_threadline_semantics_schema.exs" /Users/jon/.claude/jobs/77cf1bdd/tmp/206-unreleased.md && grep -q "_threadline_governance_schema.exs" /Users/jon/.claude/jobs/77cf1bdd/tmp/206-unreleased.md && grep -q "mix threadline.gen.triggers" /Users/jon/.claude/jobs/77cf1bdd/tmp/206-unreleased.md && ! grep -Eq "MigrationVersion|\b(WR|CR)-[0-9]{2,}\b|\bD-[0-9]{2,}\b|[Pp]hase[ _-]?[0-9]+|\bv1\.4[01]\b|hex\.retire" /Users/jon/.claude/jobs/77cf1bdd/tmp/206-unreleased.md'</automated>
    <fails_when>non-zero exit: a required string is missing from the Unreleased region, or a forbidden identifier or hidden module name appears in it</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## Unreleased — highlights$" CHANGELOG.md` prints exactly one line (the heading is kept and not duplicated), and `grep -c "_Nothing yet for the next release._" CHANGELOG.md` == 0.
    - In the Unreleased region (awk extract above), the headings appear in order: "### Breaking changes" before "### Required action" before "### Fixed".
    - The Unreleased region contains "migrations can't be executed, migration version", "is duplicated", "0.10.1", "_threadline_semantics_schema.exs", "_threadline_governance_schema.exs", and "mix threadline.gen.triggers".
    - The Unreleased region matches none of the forbidden patterns in the second <automated> command.
    - `git show --name-only --format= $(git log -1 --format=%H --grep='note the installer migration-version fix')` lists only CHANGELOG.md.
    - Both <automated> commands exit 0.
  </acceptance_criteria>
  <done>The Unreleased entry is written per D-12 (WR-ID wording deviation recorded), the contract batch is green, and the docs(changelog) commit exists locally.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full suite, static gates, docs build, and end-to-end probe</name>
  <files>(none; verification only, no source edits)</files>
  <read_first>
    - .planning/phases/206-installer-migration-versions/206-VALIDATION.md (sampling rate and phase-gate list)
    - .planning/phases/206-installer-migration-versions/206-01-SUMMARY.md (RED proof and commits to cite)
    - mix.exs:229-236 (why verify.release is not run in a dirty tree)
  </read_first>
  <action>
    Run each gate below in order. Save each command's ANSI-stripped output under /Users/jon/.claude/jobs/77cf1bdd/tmp/206-02-gate-NAME.txt, and record pass/fail plus the summary line in the SUMMARY. The gates: full suite; `mix verify.format`; `MIX_ENV=test mix verify.credo`; `MIX_ENV=dev mix verify.dialyzer` (if it goes red with a PLT cache miss rather than a real warning, run `MIX_ENV=dev mix dialyzer --plt` once and re-run; never edit the dialyzer ignore file); `mix verify.xref_cycles`; `MIX_ENV=dev mix release.pins --check`; `MIX_ENV=dev mix docs --warnings-as-errors` (run directly, because `verify.release` refuses a dirty tree); and the end-to-end probe (the third automated command) that runs install then gen.triggers from an empty dir and prints the four filenames.

    Full-suite expectations: the 205 baseline summary was "1787 tests, 0 failures, 1 excluded" (ExUnit counts the excluded pgbouncer_topology test inside the total), and this phase adds 13 tests (5 install cases + 8 helper tests), so the expected summary is "1800 tests, 0 failures, 1 excluded" unless unrelated tests landed since the baseline. A lone CriticTrustTest / critic.measure failure is a known flake; re-run that file once and record both runs. Any other failure is a HALT: report it and do not patch around it.

    The probe must show four distinct, strictly increasing prefixes across the three schema migrations and the triggers migration (this is the adopter-visible form of the fix, and the post-fix counterpart of the RESEARCH reproduction). Record the four filenames in the SUMMARY. Make no source edits and no commit in this task. If a gate needs a code change, HALT and report it so the fix goes through plan 01's files under review.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/jon/projects/threadline && DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix test'</automated>
    <fails_when>non-zero exit, or the summary reports failures other than a single known CriticTrustTest flake that passes on one re-run</fails_when>
    <automated>bash -c 'cd /Users/jon/projects/threadline && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.format && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=test mix verify.credo && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix verify.dialyzer && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix verify.xref_cycles && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix release.pins --check && ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 MIX_ENV=dev mix docs --warnings-as-errors'</automated>
    <fails_when>non-zero exit from any gate: unformatted files, any credo issue, a dialyzer warning (after at most one PLT rebuild), an xref cycle, pin drift, or an ExDoc warning such as a reference to a hidden module</fails_when>
    <automated>bash -c 'P=/Users/jon/.claude/jobs/77cf1bdd/tmp/206-probe; rm -rf "$P" && mkdir -p "$P" && cd /Users/jon/projects/threadline && MIX_ENV=test DB_PORT=5433 ASDF_ERLANG_VERSION=27.3 ASDF_ELIXIR_VERSION=1.17.3-otp-27 mix run --no-start -e "Application.delete_env(:threadline, :storage_schema); File.cd!(\"$P\", fn -> Mix.Tasks.Threadline.Install.run([]); Mix.Tasks.Threadline.Gen.Triggers.run([\"--tables\", \"posts\"]) end)" >/dev/null && D="$P/priv/repo/migrations" && ls "$D" && test "$(ls "$D" | wc -l | tr -d " ")" = 4 && : > "$P.prefixes" && for pat in _threadline_audit_schema.exs _threadline_semantics_schema.exs _threadline_governance_schema.exs _threadline_triggers_; do set -- "$D"/*"$pat"*; { test "$#" = 1 && test -e "$1"; } || exit 1; m="${1##*/}"; printf "%s\n" "${m%%_*}" >> "$P.prefixes"; done && cat "$P.prefixes" && ! grep -Evq "^[0-9]{14}$" "$P.prefixes" && sort -c -u -n "$P.prefixes"'</automated>
    <fails_when>non-zero exit: not exactly 4 files, a family (audit, semantics, governance, triggers) matching zero or more than one file, a prefix that is not 14 digits, or the prefixes taken in write order (audit, semantics, governance, triggers) not strictly increasing (`sort -c -u -n` rejects both a duplicate version, which means the defect is back, and an out-of-order version)</fails_when>
  </verify>
  <acceptance_criteria>
    - The full suite reports 0 failures (a single known CriticTrustTest flake is allowed only if it passes on one re-run), and the summary reads "N tests, 0 failures, 1 excluded" with N >= 1800 (1787 baseline total, which already includes the 1 excluded test, plus 13). A different excluded count means the default filter set changed: HALT and report.
    - The static/docs gate chain exits 0.
    - The probe's third automated command exits 0: it writes the 4 prefixes to `/Users/jon/.claude/jobs/77cf1bdd/tmp/206-probe.prefixes` in write order (audit, semantics, governance, triggers), each is 14 digits, and `sort -c -u -n` confirms that sequence is strictly increasing.
    - `git status --porcelain -- lib test CHANGELOG.md mix.exs` is empty after the task (no source edits).
    - The SUMMARY lists each gate with its result, plus the D-12 wording deviation and the D-13 confirmation (no retire, no guide section).
  </acceptance_criteria>
  <done>Every phase gate is green on the combined change, the probe shows four distinct increasing versions, and the results are recorded in the SUMMARY.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 02)

| Artifact | Kind | Shape |
|----------|------|-------|
| `CHANGELOG.md` `## Unreleased — highlights` | EDIT | lead prose, then `### Breaking changes` (None), `### Required action` (none for migrated apps; rename workaround for earlier releases), `### Fixed` (3 bullets: install versions, gen.triggers, partial re-run advice) |
| Commit | 1 local commit | `docs(changelog): note the installer migration-version fix` |
| Gate evidence (not committed) | files under `/Users/jon/.claude/jobs/77cf1bdd/tmp/` | `206-unreleased.md`, `206-02-gate-*.txt`, `206-probe/` + `206-probe.prefixes` |

## Source coverage audit (phase-wide)

| Source | Item | Plan / Task |
|--------|------|-------------|
| GOAL | unique, strictly increasing versions above existing; fresh migrate succeeds; regression tests on prefixes | 01/T1, 01/T3, 02/T2 probe |
| REQ-tag | CR-01 | 01/T1, 01/T3, 02/T1 |
| REQ-tag | WR-03 | 01/T2, 02/T1 (meaning kept, ID dropped) |
| CONTEXT | D-01, D-02, D-03, D-04 | 01/T1, 01/T3 |
| CONTEXT | D-05 | 02/T1 (no hidden module in CHANGELOG), 02/T2 (docs build) |
| CONTEXT | D-06, D-07, D-08 | 01/T2 |
| CONTEXT | D-09, D-10 | 01/T1 |
| CONTEXT | D-11 | 01 commit subjects |
| CONTEXT | D-12 | 02/T1 (with the recorded WR-ID wording deviation) |
| CONTEXT | D-13 | 02 guardrails + T2 SUMMARY confirmation |
| RESEARCH | Pitfalls 1, 2, 7 | 02/T1 region gate, 02/T2 direct docs build |
| Deferred | single versioned migration; WR-01/02/04; schema detection from file contents | excluded (Deferred Ideas) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → Hex tarball / HexDocs | CHANGELOG.md is published to adopters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-206-05 | Information Disclosure | CHANGELOG.md in the Hex tarball | low | mitigate | The archive vocabulary test and the region grep in Task 1 keep internal planning identifiers out of the published file |
| T-206-06 | Repudiation (misleading guidance) | adopter-facing recovery steps | medium | mitigate | The rename workaround states the required audit, semantics, governance order and the exact error text. Required action says an already-migrated app is unaffected (D-12) |
| T-206-07 | Denial of Service | release docs build | low | mitigate | `mix docs --warnings-as-errors` gate in Task 2. No backticked hidden module (D-05) |
| T-206-SC | Tampering | package installs | low | accept | No packages are added |
</threat_model>

<verification>
- The contract batch is green after the CHANGELOG edit.
- The full suite, format, credo, dialyzer, xref, release.pins, and docs all pass.
- The probe shows four distinct increasing prefixes.
</verification>

<success_criteria>
- Adopters searching the Ecto duplicate-version error find the Unreleased entry with the affected range and the workaround.
- All phase gates are green, and the phase ends at local commits (no push, no release).
</success_criteria>

<output>
Create `.planning/phases/206-installer-migration-versions/206-02-SUMMARY.md` when done. Include the gate table, the probe prefixes, the D-12 wording deviation, and the D-13 confirmation.
</output>
