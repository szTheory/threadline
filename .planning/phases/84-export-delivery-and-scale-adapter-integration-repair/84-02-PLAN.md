---
phase: 84-export-delivery-and-scale-adapter-integration-repair
plan: 02
type: execute
wave: 2
depends_on:
  - "84-01"
files_modified:
  - config/config.exs
  - config/test.exs
  - lib/threadline/application.ex
  - lib/threadline/export_queue/oban.ex
  - lib/threadline/storage/s3.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/application.ex
  - examples/threadline_phoenix/README.md
  - guides/operator-surface.md
  - guides/integration-contracts.md
  - test/threadline/application_test.exs
  - test/threadline/export_queue/oban_test.exs
  - test/threadline/storage/s3_test.exs
  - test/threadline/example_phoenix_readme_contract_test.exs
  - test/threadline/integration_contracts_doc_contract_test.exs
  - test/threadline/operator_surface_doc_contract_test.exs
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
must_haves:
  truths:
    - "Configured storage and queue adapters are validated at startup for dependency/config truth through their `init/1` contracts."
    - "Oban integration stays host-owned: Threadline targets a named/configured Oban instance and normalizes enqueue/runtime failures without auto-starting Oban itself."
    - "S3 integration exposes stable human-readable failure reasons and required static config instead of vague adapter errors."
    - "The example app and public docs teach the same repaired host-owned Oban/S3 posture that the runtime now enforces."
  artifacts:
    - path: lib/threadline/application.ex
      provides: "Startup-time adapter validation without stealing host runtime ownership"
      contains: "init"
    - path: lib/threadline/export_queue/oban.ex
      provides: "Configurable Oban targeting and truthful failure normalization"
      contains: "oban_name"
    - path: test/threadline/application_test.exs
      provides: "Proof that configured adapter `init/1` callbacks run during startup validation"
      contains: "Threadline.Application"
  key_links:
    - from: lib/threadline/application.ex
      to: lib/threadline/export_queue/oban.ex
      via: "startup validation invokes the configured queue adapter without supervising Oban itself"
      pattern: "export_queue_adapter|init|oban"
    - from: lib/threadline/application.ex
      to: lib/threadline/storage/s3.ex
      via: "startup validation invokes the configured storage adapter with static config only"
      pattern: "storage_adapter|init|bucket"
    - from: examples/threadline_phoenix/lib/threadline_phoenix/application.ex
      to: guides/integration-contracts.md
      via: "example app and docs tell the same host-owned Oban story"
      pattern: "Oban|threadline"
---

<objective>
Turn the Phase 79 adapter surfaces into truthful configured-path integrations by validating configured adapters at startup, proving host-owned Oban targeting, and aligning the example/docs with that repaired contract.

Purpose: satisfy the runtime portion of ADAPT-01 and the adapter-validation portion of ADAPT-02 after the delivery path itself is repaired.
Output: startup-time adapter validation, more truthful Oban/S3 integration behavior, and public docs/examples that match the repaired ownership model.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.20-MILESTONE-AUDIT.md
@.planning/phases/84-export-delivery-and-scale-adapter-integration-repair/84-CONTEXT.md
@.planning/phases/84-export-delivery-and-scale-adapter-integration-repair/84-RESEARCH.md
@.planning/phases/79-scale-adapters/79-RESEARCH.md
@.planning/phases/79-scale-adapters/79-VERIFICATION.md
@config/config.exs
@config/test.exs
@lib/threadline/application.ex
@lib/threadline/export_queue.ex
@lib/threadline/export_queue/oban.ex
@lib/threadline/storage.ex
@lib/threadline/storage/s3.ex
@examples/threadline_phoenix/lib/threadline_phoenix/application.ex
@examples/threadline_phoenix/README.md
@guides/operator-surface.md
@guides/integration-contracts.md
@test/threadline/export_queue/oban_test.exs
@test/threadline/storage/s3_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate configured adapters at startup without claiming host runtime ownership</name>
  <files>config/config.exs, config/test.exs, lib/threadline/application.ex, test/threadline/application_test.exs</files>
  <read_first>
    <file>config/config.exs</file>
    <file>config/test.exs</file>
    <file>lib/threadline/application.ex</file>
    <file>lib/threadline/export_queue.ex</file>
    <file>lib/threadline/storage.ex</file>
    <file>.planning/phases/84-export-delivery-and-scale-adapter-integration-repair/84-CONTEXT.md</file>
  </read_first>
  <action>Add a small startup-validation step to `Threadline.Application` that invokes the configured `export_queue_adapter.init/1` and `storage_adapter.init/1` whenever a repo exists. Feed each adapter only the static config it honestly owns: dependency presence, configured module callbacks, bucket/queue names, obvious malformed options, and similar non-runtime truth. Keep built-in runtime children and host-owned infrastructure boundaries unchanged: Threadline must not attempt to start Oban, probe AWS/network liveness, or turn host-owned failures into startup hard-blocks. Add focused tests proving the init callbacks run and that their failures surface as stable, actionable startup errors.</action>
  <acceptance_criteria>
    <criterion>`lib/threadline/application.ex` invokes configured adapter `init/1` callbacks during startup when a repo exists.</criterion>
    <criterion>`test/threadline/application_test.exs` proves both successful init and actionable failure behavior for configured adapters.</criterion>
    <criterion>`mix test test/threadline/application_test.exs --max-failures 1` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/application_test.exs --max-failures 1</automated>
  </verify>
  <done>Configured adapters now fail fast for static misconfiguration while preserving the correct host-owned runtime boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Oban and S3 adapter integration proof supportable on the repaired tree</name>
  <files>lib/threadline/export_queue/oban.ex, lib/threadline/storage/s3.ex, test/threadline/export_queue/oban_test.exs, test/threadline/storage/s3_test.exs</files>
  <read_first>
    <file>lib/threadline/export_queue/oban.ex</file>
    <file>lib/threadline/storage/s3.ex</file>
    <file>test/threadline/export_queue/oban_test.exs</file>
    <file>test/threadline/storage/s3_test.exs</file>
    <file>.planning/phases/79-scale-adapters/79-VERIFICATION.md</file>
  </read_first>
  <action>Refine the adapters so the configured integration path is explicit and supportable. For `Threadline.ExportQueue.Oban`, support host-provided targeting such as `oban_name` and queue name, and normalize enqueue/runtime failures into stable human-readable messages that can surface in job rows or support logs. For `Threadline.Storage.S3`, tighten `init/1` and `download_url/2` around required static config, stable failure reasons, and the delivery posture used by Plan 01. Preserve optional-dependency compile safety and `mix verify.compile_no_optional` compatibility throughout.</action>
  <acceptance_criteria>
    <criterion>`lib/threadline/export_queue/oban.ex` supports explicit configured Oban targeting and stable failure messages.</criterion>
    <criterion>`lib/threadline/storage/s3.ex` validates required static config and returns stable errors for download URL generation failures.</criterion>
    <criterion>`mix test test/threadline/export_queue/oban_test.exs test/threadline/storage/s3_test.exs --max-failures 1` exits 0.</criterion>
    <criterion>`mix verify.compile_no_optional` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/export_queue/oban_test.exs test/threadline/storage/s3_test.exs --max-failures 1 && mix verify.compile_no_optional</automated>
  </verify>
  <done>The Oban and S3 adapters now expose a truthful configured-path contract instead of isolated module behavior only.</done>
</task>

<task type="auto">
  <name>Task 3: Align the example app and docs with the repaired host-owned adapter contract</name>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/application.ex, examples/threadline_phoenix/README.md, guides/operator-surface.md, guides/integration-contracts.md, test/threadline/example_phoenix_readme_contract_test.exs, test/threadline/integration_contracts_doc_contract_test.exs, test/threadline/operator_surface_doc_contract_test.exs</files>
  <read_first>
    <file>examples/threadline_phoenix/lib/threadline_phoenix/application.ex</file>
    <file>examples/threadline_phoenix/README.md</file>
    <file>guides/operator-surface.md</file>
    <file>guides/integration-contracts.md</file>
    <file>.planning/phases/84-export-delivery-and-scale-adapter-integration-repair/84-CONTEXT.md</file>
  </read_first>
  <action>Update the canonical example and public docs so they teach the repaired truth directly: Threadline keeps one actor-owned export delivery route, the host app owns Oban supervision, configured adapters are validated at startup for static truth, and S3-backed downloads are delivered through the same operator action via backend-native redirect. Keep the docs explicit about what Threadline owns versus what the host still must configure. Lock that story with the existing doc-contract tests rather than relying on prose alone.</action>
  <acceptance_criteria>
    <criterion>The example app still shows host-owned Oban supervision and now documents the repaired export delivery/configuration story accurately.</criterion>
    <criterion>`guides/operator-surface.md` and `guides/integration-contracts.md` describe one actor-owned download action with backend-specific delivery hidden behind the controller.</criterion>
    <criterion>`mix test test/threadline/example_phoenix_readme_contract_test.exs test/threadline/integration_contracts_doc_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs --max-failures 1` exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>mix test test/threadline/example_phoenix_readme_contract_test.exs test/threadline/integration_contracts_doc_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>The public reference path teaches the same adapter ownership model that the repaired runtime now enforces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library startup -> adapter configuration | Threadline validates only the config and dependency truth that the adapter actually owns. |
| Threadline adapter -> host-owned Oban runtime | Queue integration must target host-managed Oban instances without silently changing supervision ownership. |
| Public docs/example -> adopter implementation | Canonical examples directly shape how adopters mount and operate the export lane. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-04 | Availability | startup validation | mitigate | Fail fast only for static adapter misconfiguration and dependency gaps; do not hard-claim host runtime liveness. |
| T-84-05 | Repudiation | Oban/S3 integration proof | mitigate | Normalize adapter failures into stable, human-readable messages and prove configured-path targeting explicitly. |
| T-84-06 | Spoofing / documentation drift | example/docs | mitigate | Keep the example app and contract docs aligned with the repaired host-owned adapter contract through doc tests. |
</threat_model>

<verification>
Run the startup-validation, Oban, S3, compile-no-optional, and doc-contract tests, then confirm the current tree now tells one coherent story: configured adapters are validated early, Oban remains host-supervised, remote delivery stays behind the actor-owned controller path, and the public docs teach exactly that posture.
</verification>

<success_criteria>
Configured Oban and S3 adapters are supportable on the repaired tree: they validate static setup early, keep host runtime ownership explicit, and match the canonical example/docs surface.
</success_criteria>

<output>
After completion, create `.planning/phases/84-export-delivery-and-scale-adapter-integration-repair/84-02-SUMMARY.md`
</output>
