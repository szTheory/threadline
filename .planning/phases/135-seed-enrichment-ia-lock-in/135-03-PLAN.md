---
phase: 135-seed-enrichment-ia-lock-in
plan: 03
type: execute
wave: 2
depends_on: ["135-01"]
files_modified:
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex
  - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/exports.ex
  - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs
autonomous: true
requirements: [POLISH-SEED]

must_haves:
  truths:
    - "≥1 UPDATE and ≥1 DELETE AuditChange land in the default 24h Timeline window (D-13)"
    - "An in-window variety pack delivers ~5 INSERT / 4 UPDATE / 2 DELETE across tickets, ticket_replies, org_memberships (D-11/D-12)"
    - "A ticket_replies reply-edit produces a rich before→after diff with internal_note_body [REDACTED] (D-12.1/D-14)"
    - "Non-human actor clusters (service_account, job, system, anonymous) are present and in-window (D-06)"
    - "Corpus/filler op-mix shifts from ~95% INSERT toward ~55/35/10 via a DELETE branch (D-11)"
    - "1–2 SavedView rows are seeded for admin@example.com's actor_ref (F-204 data)"
  artifacts:
    - path: "examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex"
      provides: "seed_variety_pack/1 — in-window 5/4/2 op + multi-kind actor cluster (D-10/11/12)"
      contains: "seed_variety_pack"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex"
      provides: "DELETE branch shifting corpus op-mix toward 55/35/10 (D-11)"
      contains: "Repo.delete!"
    - path: "examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/exports.ex"
      provides: "SavedView seed rows for admin (F-204 data)"
      contains: "SavedView"
    - path: "examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs"
      provides: "D-13 in-window UPDATE+DELETE guarantee assertion"
      contains: "D-13 in-window variety guarantee"
  key_links:
    - from: "anchors.ex seed_variety_pack/1"
      to: "Support.set_actor_guc!/2 + set_anonymous_actor_guc!/0 (Plan 01)"
      via: "non-human actor clusters via Manifest.actor_id/1"
      pattern: "Support\\.set_(anonymous_)?actor_guc!"
    - from: "demo_contract_test.exs D-13 block"
      to: "AuditChange.op + transaction.occurred_at"
      via: "join query over default 24h window asserting ≥1 update + ≥1 delete"
      pattern: "occurred_at >= \\^window_start"
    - from: "anchors.ex reply-edit"
      to: "ticket_replies custom trigger (store_changed_from + mask)"
      via: "HelpDesk reply INSERT then Repo.update! of body + internal_note_body"
      pattern: "internal_note_body"
---

<objective>
Build the in-window "variety pack" so all three op-colors and op-filters show above the fold in the default 24h Timeline, coordinate the non-human actor kinds onto those mutations (so op-color + actor-kind diversity land together, feeding F-703), shift the corpus filler op-mix toward 55/35/10, seed SavedView data (F-204), and back the in-window guarantee with the D-13 assertion. This is the core seed-enrichment deliverable.

Purpose: most seed is epoch-anchored (past) → variety is invisible in the wall-clock 24h default window (D-10). The variety pack is wall-clock-recent and op/actor-diverse so every screen demonstrates itself.
Output: `seed_variety_pack/1` in Anchors, a DELETE branch in Filler, SavedView rows in Exports, and a green D-13 assertion in demo_contract_test.exs.

DEPENDS ON Plan 01: requires `Support.set_actor_guc!/2`, `Support.set_anonymous_actor_guc!/0`, and `Manifest.actor_id/1` — do not start until 135-01 is merged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-seed-enrichment-ia-lock-in/135-CONTEXT.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-RESEARCH.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-VALIDATION.md
@CLAUDE.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be merged first) + verified live code. -->

Support (post-Plan-01):
  Support.set_actor_guc!(actor_id, kind \\ :user)   # kind in [:user,:admin,:service_account,:job,:system]
  Support.set_anonymous_actor_guc!()                # :anonymous, no id
  Support.audit_context(actor_id, opts)             # opts may include kind: <kind>
  Support.current_audit_transaction_id!()
  Support.put_timestamp(ctx, tx_id, %DateTime{})    # registers tx for Temporal.run backdate
  Support.stamp_org_meta!(%Organization{})

Manifest (post-Plan-01):
  Manifest.actor_id(:zendesk_sync) -> "zendesk-sync"
  Manifest.actor_id(:oban_retention_purge) -> "oban-retention-purge"
  Manifest.actor_id(:trigger_backfill) -> "trigger-backfill"
  Manifest.epoch() -> ~U[2026-05-27 12:00:00Z]

anchors.ex canonical in-window idiom (seed_active_agent_window lines 33-58) — DO NOT delete; extend run/1:
  ts = DateTime.utc_now() |> DateTime.add(-hours_ago, :hour)   # hours_ago in [1,3,6], N<=6
  Support.put_timestamp(acc, tx_id, ts)   # IN-WINDOW rows DO call put_timestamp with a wall-clock-recent VALUE
  # PATTERNS correction: in-window is a TIMESTAMP VALUE choice, NOT a put_timestamp skip.

anchors.ex DELETE/diff analogs:
  seed_acme_delete (lines 89-129) — HelpDesk.delete_reply + post-hoc tx-id lookup + put_timestamp
  seed_acme_close (lines 61-87) — HelpDesk.ticket_replied_and_closed for the ticket_replies rich diff

filler.ex (insert_filler_ticket lines 66-103) — Repo.transaction + set_actor_guc! + INSERT then UPDATE; uses Support.random_days_ago_timestamp() (epoch-relative — keep for filler).

ticket_replies trigger (config/dev.exs lines 78-90): store_changed_from: true, mask: ["internal_note_body","body"] — ONLY table with rich before→after + [REDACTED]. tickets/org_memberships/agents = default trigger (changed_from NULL, after-only — D-09, do NOT fake before).

exports.ex (lines 1-40) — already builds %ActorRef{type: :user, id: Manifest.user_id(:admin)} and Repo.insert_all with on_conflict; analog for SavedView seeding.

Threadline.Governance.SavedView (lib/threadline/governance/saved_view.ex):
  schema fields: name :string, actor_ref Threadline.Semantics.ActorRef, filters :map; required [:name,:actor_ref,:filters]
  table "threadline_saved_views"; Timeline queries it where: v.actor_ref == ^actor_ref at mount.

demo_contract_test.exs: use ThreadlinePhoenix.DataCase, async: false; @moduletag :demo_contract; seed runs in setup; all queries wrapped in Ecto.Adapters.SQL.Sandbox.unboxed_run(Repo, fn -> ... end). AuditChange/AuditTransaction aliased.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add seed_variety_pack/1 — in-window 5/4/2 op mix with multi-kind actors (D-10/11/12/14) + D-13 assertion</name>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex (the file being modified — run/1 line 17, seed_active_agent_window lines 33-58 in-window idiom, seed_acme_delete lines 89-129 DELETE idiom, seed_acme_close lines 61-87 reply rich-diff idiom, upsert_ticket! lines 182-194)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md (§ anchors.ex EXTEND — the seed_variety_pack/1 shape, the four-step idiom, the "in-window is a VALUE not a skip" correction, the DELETE and reply-edit patterns; § Pitfall 1 and § Pitfall 5)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-CONTEXT.md (D-11 two-layer op-mix, D-12 the six mutation stories, D-13 guarantee, D-14 redaction cohesion, D-06 actor roster)
    - examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs (the file being extended — unboxed_run wrapper, the AuditChange+AuditTransaction join idiom, @moduletag :demo_contract, async: false)
    - examples/threadline_phoenix/config/dev.exs (lines 78-90 — confirm ticket_replies is the only store_changed_from table; D-09)
  </read_first>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex, examples/threadline_phoenix/test/threadline_phoenix/demo_contract_test.exs</files>
  <behavior>
    - After seed, querying AuditChanges joined to transactions with occurred_at >= utc_now()-24h returns ≥1 with op "update" AND ≥1 with op "delete"
    - The variety pack contains a ticket_replies row whose UPDATE diff shows a before→after on body and a [REDACTED] internal_note_body
    - At least one in-window cluster carries each non-human kind: :service_account, :job, :system, :anonymous
  </behavior>
  <action>
    Add `seed_variety_pack/1` to Anchors and call it last in `run/1` (after `seed_globex_close_sample`). Implement the D-12 mutation stories with fixed deterministic in-window hour offsets (each `ts = DateTime.utc_now() |> DateTime.add(-N, :hour)`, N ≤ 6) passed to `Support.put_timestamp` — these rows ARE registered in ctx.timestamps with a wall-clock-recent VALUE (NOT skipped — Pitfall 1). Stories, distributed to roughly 5 INSERT / 4 UPDATE / 2 DELETE across tickets / ticket_replies / org_memberships:
      1. Reply edited (UPDATE + rich diff): INSERT a ticket_replies row (HelpDesk reply path or upsert), then in a new Repo.transaction with set_actor_guc! Repo.update! its `body` AND `internal_note_body` → canonical before→after + [REDACTED] (D-12.1/D-14). internal_note_body stays masked — use the EXISTING redaction config, introduce no second config.
      2. Ticket reopened/re-triaged (UPDATE): tickets UPDATE status closed→open + clear closed_at; a separate assignee_id reassignment UPDATE.
      3. Membership role change (UPDATE): org_memberships UPDATE role agent→support.
      4. Reply hard-delete (DELETE): re-anchor one DELETE in-window (Repo.delete! a ticket_replies row, or re-anchor an existing one via put_timestamp to an in-window value).
      5. Ticket delete (DELETE): Repo.delete! a duplicate/spam tickets row.
      6. (INSERTs) the new ticket_replies + any setup INSERTs needed to reach ~5 INSERT.
    Coordinate non-human actors onto these (D-06/D-13): the inbound ticket-sync INSERT/UPDATE cluster uses `Support.set_actor_guc!(Manifest.actor_id(:zendesk_sync), :service_account)`; a stale-ticket sweep uses `Support.set_actor_guc!(Manifest.actor_id(:oban_retention_purge), :job)`; a small backfill/correction uses `Support.set_actor_guc!(Manifest.actor_id(:trigger_backfill), :system)`; ONE deliberate public-form ticket-submission INSERT cluster uses `Support.set_anonymous_actor_guc!()` (renders as honest muted "unknown", in-window). Each mutation uses the four-step idiom: Repo.transaction → set_actor_guc!(...) (FIRST inside) → mutation → Support.stamp_org_meta!(org) → Support.current_audit_transaction_id!(); then put_timestamp with the in-window VALUE. Do NOT fake before values on tickets/org_memberships (D-09 — they use the default trigger, after-only is honest). Preserve determinism (no new :rand seeding; reuse fixed numbers/offsets).
    Then add `describe "D-13 in-window variety guarantee"` to demo_contract_test.exs inside unboxed_run: `window_start = DateTime.utc_now() |> DateTime.add(-24, :hour)`; for op in ["update","delete"] count AuditChanges joined to a transaction with `at.occurred_at >= ^window_start` and `ac.op == ^op`; `assert count >= 1, "expected ≥1 #{op} in default 24h window, got #{count}"`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs` exits 0 (D-13 describe block passes)
    - Post-seed default 24h-window query returns ≥1 `update` AND ≥1 `delete` AuditChange
    - `grep -c "seed_variety_pack" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex` returns ≥2 (definition + run/1 call)
    - `grep -c "Support.set_anonymous_actor_guc!" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex` returns ≥1
    - `grep -c "Manifest.actor_id" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/anchors.ex` returns ≥3 (zendesk_sync, oban_retention_purge, trigger_backfill)
    - A ticket_replies UPDATE in-window has a non-null changed_from with internal_note_body masked (rich diff present)
  </acceptance_criteria>
  <done>seed_variety_pack/1 plants the in-window 5/4/2 op mix with all non-human actor kinds and the reply-edit rich/[REDACTED] diff; the D-13 assertion is green; determinism preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Shift filler corpus op-mix toward 55/35/10 via a DELETE branch (D-11)</name>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex (the file being modified — insert_filler_ticket/4 lines 66-103, the Repo.transaction + set_actor_guc! + INSERT/UPDATE conditional)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md (§ filler.ex EXTEND — the DELETE branch via rem(number, 10), keeping the transaction wrapper and epoch-relative timestamp identical)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-CONTEXT.md (D-11 — corpus shift to ~55/35/10; Claude's Discretion allows ~50–60/30–40/10–15)
  </read_first>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex</files>
  <action>
    Extend `insert_filler_ticket/4` so the inner conditional yields roughly 55/35/10 INSERT-only / closed-UPDATE / DELETE instead of the current ~95% INSERT+UPDATE. Use a deterministic `status_roll = rem(number, 10)`: `0` → after the INSERT, `Repo.delete!(ticket)` (DELETE branch, ~10%); `1..3` → the closed UPDATE branch (~30–40%); else → open/in_progress UPDATE (~55–60%). Keep the `Repo.transaction` wrapper, the `Support.set_actor_guc!(user_id)` first-line call, `Support.stamp_org_meta!(org)`, `Support.current_audit_transaction_id!()`, and the `ts = Support.random_days_ago_timestamp()` (filler stays epoch-relative — NOT in-window; do not switch to DateTime.utc_now()). The DELETE produces a DELETE AuditChange in the same transaction for honest wide-date-range op counts. Preserve PRNG determinism (do not add new :rand seeding; the rem/number scheme is deterministic).
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Repo.delete!" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex` returns ≥1
    - `grep -c "rem(number, 10)" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex` returns ≥1
    - `grep "DateTime.utc_now" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/filler.ex` returns no matches (filler stays epoch-relative)
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs` exits 0 (seed still runs clean, determinism intact)
  </acceptance_criteria>
  <done>Filler corpus has a ~10% DELETE branch shifting op-mix toward 55/35/10; transaction/actor/timestamp idiom unchanged; deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Seed SavedView rows for admin (F-204 data half)</name>
  <read_first>
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/exports.ex (the file being modified — the run/1 actor_ref = %ActorRef{type: :user, id: Manifest.user_id(:admin)} pattern and Repo.insert_all with on_conflict idiom, lines 1-40)
    - lib/threadline/governance/saved_view.ex (the schema under seed — fields name/actor_ref/filters, all required; table threadline_saved_views)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-RESEARCH.md (§ Open Questions item 3 — SavedView seeding scope: 1–2 rows keyed to admin's ActorRef; render is Phase 139)
    - examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/retention_runs.ex (analog — fixed UUIDv5 id + Repo.insert_all + on_conflict for deterministic seed rows)
  </read_first>
  <files>examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/exports.ex</files>
  <action>
    In exports.ex `run/1` (reusing the existing `actor_ref = %ActorRef{type: :user, id: Manifest.user_id(:admin)}`), seed 1–2 `Threadline.Governance.SavedView` rows for that actor_ref so the Home "pick up where you left off" data exists (F-204 data; render defers to Phase 139). Use deterministic UUIDv5 ids (mirror retention_runs.ex `run_id/1` → e.g. `saved_view/recent-deletes`, `saved_view/closed-tickets`), set `name` (e.g. "Recent deletes", "Closed this week"), `actor_ref` (the admin ActorRef — must match what Timeline uses for the logged-in admin), and a `filters` map (e.g. `%{"op" => "delete"}`, `%{"status" => "closed"}`). Insert via `Repo.insert_all(SavedView, rows, on_conflict: {:replace, [...]}, conflict_target: :id)` with `inserted_at`/`updated_at` set, matching the Exports idiom. Add the `alias Threadline.Governance.SavedView`. Do NOT add a new route or render — data only.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SavedView" examples/threadline_phoenix/lib/threadline_phoenix/demo/seed/exports.ex` returns ≥2 (alias + insert)
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs` exits 0 (seed runs clean)
    - Post-seed: `threadline_saved_views` has ≥1 row whose actor_ref matches admin@example.com's ActorRef
    - `git diff --name-only` shows no migration/schema/route file touched (SavedView schema already exists in the library — data only)
  </acceptance_criteria>
  <done>1–2 deterministic SavedView rows seeded for the admin actor_ref; seed deterministic; no schema/route change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed code → audit triggers | Variety-pack mutations set the actor GUC (incl. :anonymous) before each insert/update/delete; triggers persist op + actor + (for ticket_replies) masked diff. All deterministic demo fiction. |
| masked field → audit_changes | The ticket_replies trigger masks internal_note_body into [REDACTED] in changed_from/changed_to. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-03 | Information Disclosure | ticket_replies reply-edit masked diff | mitigate | The reply-edit relies on the EXISTING dev.exs trigger mask (`mask: ["internal_note_body","body"]`). The seed must NOT write the raw internal_note_body into any unmasked column; the value lives only in the masked audit diff. Verify the in-window reply-edit's internal_note_body renders as [REDACTED] in the change diff (D-14 redaction cohesion). No second redaction config introduced. |
| T-135-04 | Spoofing | :anonymous public-form cluster | accept | The :anonymous cluster is deliberate honest "unknown" actor modeling (public ticket-submission form), not a bypass. Demo-only; no auth decision keys off it. |
| T-135-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs (Elixir source edits against existing deps + one test extension). |

No high security threats apply: deterministic dev/demo seed + a test; no real secrets/PII (example.com fixtures + Manifest.demo_seed_password only); the one masking concern (T-135-03) is mitigated by reusing the existing trigger mask and verifying [REDACTED].
</threat_model>

<verification>
- `cd examples/threadline_phoenix && mix verify.format` (per-commit gate)
- `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_contract_test.exs` (D-13 + seed determinism)
- `cd examples/threadline_phoenix && mix verify.test`
</verification>

<success_criteria>
- ≥1 UPDATE + ≥1 DELETE in the default 24h window, op-filterable (D-13; criterion #1 status-variety + edge states reachable).
- All non-human actor kinds demonstrable in-window (D-06; feeds F-703).
- Reply-edit rich [REDACTED] diff present (D-12.1/D-14).
- Corpus op-mix ~55/35/10 (D-11); SavedView data seeded (F-204).
- Determinism preserved; no schema/route/business-logic change.
</success_criteria>

<output>
Create `.planning/phases/135-seed-enrichment-ia-lock-in/135-03-SUMMARY.md` when done.
</output>
