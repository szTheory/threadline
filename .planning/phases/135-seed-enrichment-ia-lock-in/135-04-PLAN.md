---
phase: 135-seed-enrichment-ia-lock-in
plan: 04
type: execute
wave: 3
depends_on: ["135-01", "135-03"]
files_modified:
  - examples/threadline_phoenix/DEMO-MANIFEST.md
  - examples/threadline_phoenix/test/threadline_phoenix/demo_manifest_contract_test.exs
autonomous: true
requirements: [POLISH-SEED]

must_haves:
  truths:
    - "DEMO-MANIFEST.md contains a per-state recipe table (screen × state × login × filter/path) (D-03)"
    - "DEMO-MANIFEST.md documents the named non-human actor literals (D-06)"
    - "The recipe table notes the deferred Coverage fully-covered/all-empty state as Phase-138-owned (D-04)"
    - "DEMO-MANIFEST.md is the SSOT and matches the enriched seed (criterion #2)"
    - "A doc-contract test pins the recipe table + actor literals so they cannot drift from the seed"
  artifacts:
    - path: "examples/threadline_phoenix/DEMO-MANIFEST.md"
      provides: "Per-state recipe table + named actor literals section (D-03/D-06)"
      contains: "State recipes"
    - path: "examples/threadline_phoenix/test/threadline_phoenix/demo_manifest_contract_test.exs"
      provides: "Doc-contract test for the recipe table + actor literals (D-03)"
      contains: "State recipes"
  key_links:
    - from: "demo_manifest_contract_test.exs"
      to: "examples/threadline_phoenix/DEMO-MANIFEST.md"
      via: "Path.expand + File.read! + String.contains? per literal"
      pattern: "DEMO-MANIFEST.md"
    - from: "DEMO-MANIFEST.md recipe table"
      to: "the enriched seed (Plans 01+03)"
      via: "documents reachable states: offboarded-co empty, scoped/permission-edge, op/actor variety, future-date filter"
      pattern: "offboarded-co.example.com"
---

<objective>
Make DEMO-MANIFEST.md the single source of truth for what the enriched seed demonstrates: add a per-state recipe table (screen × state × login × filter/path) so each of the 8 downstream screenshot phases reaches a state by "log in as X, open path Y, apply filter Z" — never by re-seeding (D-01/D-03). Document the named non-human actor literals (D-06). Note the one genuinely seed-unreachable state (Coverage fully-covered/all-empty) as Phase-138-owned (D-04). Back the table with a doc-contract test so recipes can't silently drift from the seed (D-03).

Purpose: ROADMAP criterion #2 — DEMO-MANIFEST.md updated and matches the enriched seed as SSOT. The recipe table is the contract the 8 later phases consume.
Output: an extended DEMO-MANIFEST.md (recipe table + actor literals) and a green `demo_manifest_contract_test.exs`.

DEPENDS ON Plans 01+03: the recipe table documents the actor literals (Plan 01) and the reachable op/actor variety + SavedView states (Plan 03). Plan against the merged seed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-seed-enrichment-ia-lock-in/135-CONTEXT.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-RESEARCH.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-VALIDATION.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-01-SUMMARY.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-03-SUMMARY.md
@examples/threadline_phoenix/DEMO-MANIFEST.md

<interfaces>
<!-- Reachability mechanisms (verified) the recipe table must reference. -->

Empty / scoped / permission-edge reachability (D-02 — no new code, all existing):
  - offboarded-co: purged-empty org; login support@offboarded-co.example.com → honest empty scoped Timeline
    (RetentionTail.assert_org_y_audit_empty!/1 guarantees zero audit footprint)
  - scoped login: my_authorize_fn/1 + scope_operator_query/3 (router.ex) narrow Timeline/Transaction/Actor/RowHistory
    to a support user's org; admin-only screens (Coverage/Evidence/Exports/Policy) return {:error,:unauthorized}
    for the same support login = the permission-edge state for free
  - future-date filter: ?from=2030-... yields an empty Timeline against populated data

Status/op/actor variety (from Plan 03): in-window 5/4/2 op mix + non-human actor clusters above the fold in the default 24h Timeline.

Named actor literals (from Plan 01 — document under a new "## Named actor literals" section):
  service_account/zendesk-sync · job/oban-retention-purge · system/trigger-backfill · anonymous (public form, no id)

D-04 deferred state: Coverage fully-covered / all-empty depends on trigger registration + schema introspection
  (Health.trigger_coverage), NOT org rows → cannot be produced seed-only → note as Phase-138-owned in the table.

DEMO-MANIFEST.md current headings (extend, don't rewrite): # Demo manifest, ## Temporal anchors,
  ## Organizations, ## Hero tickets (Acme), ## Users (Sigra), ## Correlation and evidence.

Test analog: examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs
  (Path.expand("../../WALKTHROUGH.md", __DIR__) + File.read! + per-literal String.contains? loop, async: true).
  SEPARATE from the existing demo_manifest_test.exs (which tests the Manifest Elixir MODULE, not the .md) — do NOT merge.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the per-state recipe table + named actor literals to DEMO-MANIFEST.md (D-03/D-06/D-04)</name>
  <read_first>
    - examples/threadline_phoenix/DEMO-MANIFEST.md (the file being modified — full read; mirror the existing section/table style)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-CONTEXT.md (D-02 reachability mechanisms, D-03 recipe-table requirement, D-04 deferred Coverage state, D-06 actor roster + literal strings, D-01 one-command story)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md (§ demo_manifest_contract_test.exs — the literals the test will assert, so the table includes them verbatim)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-RESEARCH.md (§ Actor-Kind Generalization Surface — the named actor literal table; § Validation Architecture Surface 1)
  </read_first>
  <files>examples/threadline_phoenix/DEMO-MANIFEST.md</files>
  <action>
    Add a `## State recipes` section: a table with columns Screen | State | Login | Filter/Path | Notes, covering the operator-surface screens and their meaningful states reachable from the enriched seed. Rows must cover at minimum: an `empty` Timeline via `support@offboarded-co.example.com` (offboarded-co purged-empty / scoped); a permission-edge row (same support login → admin-only Coverage/Evidence/Exports/Policy return unauthorized); a future-date filter-empty row (`?from=2030-01-01`); op/status-variety rows showing the in-window 5/4/2 op mix is above the fold (op filters update/delete); actor-variety rows naming the non-human kinds; a long/paginated row (the ~50/org filler corpus). Include the named-actor literals either inline or in a dedicated `## Named actor literals` section listing `service_account/zendesk-sync`, `job/oban-retention-purge`, `system/trigger-backfill`, and the `anonymous` public-form cluster. Add a row (or note) for the Coverage fully-covered/all-empty state marked DEFERRED to Phase 138 with the D-04 rationale (trigger-registration dependent, not seed-reachable). Keep the one-command story explicit: states are REACHED via `mix demo.reset && mix demo.seed` then login/path/filter — never re-seeded, no `--profile` flags (D-01). Extend the file; do NOT rewrite existing sections.
  </action>
  <verify>
    <automated>grep -c "State recipes" examples/threadline_phoenix/DEMO-MANIFEST.md && grep -c "offboarded-co.example.com\|zendesk-sync\|Phase 138" examples/threadline_phoenix/DEMO-MANIFEST.md</automated>
  </verify>
  <acceptance_criteria>
    - `DEMO-MANIFEST.md` contains "## State recipes" and ≥3 screen-state-login triples including "support@offboarded-co.example.com" and "empty"
    - `DEMO-MANIFEST.md` contains each named actor literal: "zendesk-sync", "oban-retention-purge", "trigger-backfill"
    - `DEMO-MANIFEST.md` contains a Coverage fully-covered/all-empty row referencing "Phase 138" (D-04 deferral)
    - `DEMO-MANIFEST.md` contains a future-date filter literal (e.g. "?from=2030")
    - `DEMO-MANIFEST.md` references `mix demo.reset && mix demo.seed` and states no `--profile` flag is used
  </acceptance_criteria>
  <done>DEMO-MANIFEST.md is the SSOT: per-state recipe table + named actor literals + the D-04 deferral note, all matching the enriched seed; one-command story preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add the recipe-table doc-contract test (D-03)</name>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix/walkthrough_doc_contract_test.exs (analog — module header, Path.expand("../../<file>.md", __DIR__), File.read! + per-literal String.contains? loop, async: true, describe blocks)
    - .planning/phases/135-seed-enrichment-ia-lock-in/135-PATTERNS.md (§ demo_manifest_contract_test.exs NEW — the exact module shape, the recipe-table + actor-literal assertion list, the "do NOT merge into demo_manifest_test.exs" separation note)
    - examples/threadline_phoenix/DEMO-MANIFEST.md (the file under test — assert against the exact literals written in Task 1)
  </read_first>
  <files>examples/threadline_phoenix/test/threadline_phoenix/demo_manifest_contract_test.exs</files>
  <action>
    Create `ThreadlinePhoenix.DemoManifestContractTest` (`use ExUnit.Case, async: true`) with `@manifest Path.expand("../../DEMO-MANIFEST.md", __DIR__)`. Following the walkthrough analog, assert via `File.read!/1` + per-literal `String.contains?/2` (with assert messages) that DEMO-MANIFEST.md contains: "## State recipes"; "empty"; "offboarded-co.example.com" and "support@offboarded-co.example.com"; a future-date filter literal ("?from=2030" or the exact string written in Task 1); the screen names present in the recipe table (e.g. "Timeline", "Actor"); the named actor literals "zendesk-sync", "oban-retention-purge", "trigger-backfill"; and the Coverage deferral reference "Phase 138". Group with `describe` blocks (recipe table; named actor literals). This file is SEPARATE from the existing `demo_manifest_test.exs` (which tests the Elixir Manifest module) — do not merge. Assert only literals that Task 1 actually wrote.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_manifest_contract_test.exs 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_manifest_contract_test.exs` exits 0
    - The test asserts the "## State recipes" header, ≥3 screen-state-login literals, all three named actor literals, and the Phase-138 deferral
    - The file is a NEW module distinct from demo_manifest_test.exs
  </acceptance_criteria>
  <done>demo_manifest_contract_test.exs is green, pinning the recipe table + actor literals against drift; SSOT enforced in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (docs + test only) | Edits DEMO-MANIFEST.md and adds a read-only doc-contract test. No runtime code, no input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-05 | Tampering | recipe-table drift from seed | mitigate | The D-03 doc-contract test (`demo_manifest_contract_test.exs`) blocks silent drift between DEMO-MANIFEST.md and the seed across the 8 downstream screenshot phases — this is the milestone-required mitigation. |
| T-135-06 | Information Disclosure | documented login literals | accept | DEMO-MANIFEST.md documents example.com logins and the existing demo password convention only — no real secrets/PII (Manifest.demo_seed_password default, fixtures). |
| T-135-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs (markdown + one ExUnit test against existing deps). |

No high or medium security threats apply: docs + a read-only test, no secrets/PII, no runtime surface.
</threat_model>

<verification>
- `cd examples/threadline_phoenix && mix verify.format`
- `cd examples/threadline_phoenix && mix test test/threadline_phoenix/demo_manifest_contract_test.exs`
- Phase gate: `mix ci.all` (project root) green before /gsd:verify-work
- Seed-only diff guard (manual): `git diff --name-only main` — every path under examples/threadline_phoenix/lib/threadline_phoenix/demo/, DEMO-MANIFEST.md, .planning/milestones/v1.31-*.md, or test/; zero schema/migration/router/LiveView/business-logic files.
</verification>

<success_criteria>
- DEMO-MANIFEST.md is the SSOT matching the enriched seed (criterion #2).
- Every meaningful screen state is reachable via a documented recipe (criterion #1), with the one seed-unreachable state (D-04) noted as Phase-138-owned.
- The recipe table is drift-protected by a doc-contract test (D-03).
</success_criteria>

<output>
Create `.planning/phases/135-seed-enrichment-ia-lock-in/135-04-SUMMARY.md` when done.
</output>
