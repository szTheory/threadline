---
phase: 70
plan: "70-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/upgrade-path.md
  - guides/integrations/sigra.md
  - test/threadline/upgrade_path_doc_contract_test.exs
  - test/threadline/integrations/sigra_doc_contract_test.exs
autonomous: true
requirements:
  - INTEG-02
  - COMPAT-03
must_haves:
  truths:
    - "The lane/proof docs explicitly separate root-lane proof from example-lane proof instead of flattening them into one broad compatibility claim."
    - "`sigra-reference` remains a narrow first-party reference lane for Phoenix hosts already using Sigra, not a generic Sigra support promise."
    - "The Sigra guide and support matrix both keep Sigra as request-capture-only and keep `/audit` authorization host-owned."
  artifacts:
    - path: "guides/upgrade-path.md"
      provides: "Canonical lane/proof matrix with explicit root-vs-example proof sources and honest caveats"
    - path: "guides/integrations/sigra.md"
      provides: "Reference-lane guide that keeps the adapter concrete, soft-loaded, and request-capture-only"
    - path: "test/threadline/upgrade_path_doc_contract_test.exs"
      provides: "Focused support-lane wording guard for root-vs-example proof boundaries"
    - path: "test/threadline/integrations/sigra_doc_contract_test.exs"
      provides: "Focused Sigra guide wording guard for request-capture-only and host-owned auth boundaries"
  key_links:
    - from: "guides/upgrade-path.md"
      to: "mix.lock"
      via: "phoenix-surface lane uses root lock resolution as proof per D-107"
      pattern: "Phoenix `1.8.7`"
    - from: "guides/upgrade-path.md"
      to: "examples/threadline_phoenix/mix.lock"
      via: "sigra-reference lane uses example lock resolution as proof per D-107"
      pattern: "Sigra `0.2.5`"
    - from: "guides/integrations/sigra.md"
      to: "lib/threadline/integrations/sigra.ex"
      via: "guide remains a direct callback pair around the current adapter seam"
      pattern: "actor_ref_from_conn"
    - from: "guides/integrations/sigra.md"
      to: "lib/threadline/operator_surface/export_auth_plug.ex"
      via: "host-owned export auth parity is documented as separate from Sigra request capture per D-111"
      pattern: "mirror = %{assigns: conn.assigns}"
---

<objective>
Refresh the support-matrix and Sigra reference-lane docs so the current proof is explicit, current, and narrow instead of sounding like generic Sigra/Phoenix compatibility.

Purpose: satisfy the lane/proof honesty portions of `COMPAT-03` and keep `INTEG-02` anchored to the existing soft-loaded `Threadline.Integrations.Sigra` seam rather than a broadened adapter story.
Output: updated support/proof docs plus focused doc-contract coverage for the root-vs-example proof split and auth-boundary wording.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-CONTEXT.md
@.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-RESEARCH.md
@.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-PATTERNS.md
@guides/upgrade-path.md
@guides/integrations/sigra.md
@mix.exs
@mix.lock
@examples/threadline_phoenix/mix.exs
@examples/threadline_phoenix/mix.lock
@lib/threadline/integrations/sigra.ex
@lib/threadline/operator_surface/auth.ex
@lib/threadline/operator_surface/export_auth_plug.ex

Locked decisions implemented here:

- D-105/D-106/D-107/D-108: lane-split version wording, exact proof pins only in proof docs, root proof separated from example proof, and no range-only promise for the Sigra lane.
- D-109/D-110/D-111/D-112: Sigra stays request-capture-only; `/audit` and exports remain host-auth-owned.
- D-117/D-118/D-119/D-120: one maintained narrow Sigra lane only, no variants matrix, concrete small adapter, credibility over breadth theater.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the support matrix around explicit root-lane versus example-lane proof</name>
  <files>guides/upgrade-path.md, test/threadline/upgrade_path_doc_contract_test.exs</files>
  <action>
    Refresh `guides/upgrade-path.md` so the current proof sources are explicit and current:

    - keep the three lanes from Phase 69 (`capture-only`, `phoenix-surface`, `sigra-reference`)
    - make `phoenix-surface` proof explicitly come from root `mix.exs`, root `mix.lock`, root CI, and root doc-contract coverage per D-107
    - make `sigra-reference` proof explicitly come from `examples/threadline_phoenix/`, its lockfile, its README, and `mix verify.example` per D-107
    - preserve exact Phoenix/Sigra proof pins in this proof doc, not in generic install docs, per D-105 and D-106
    - tighten the caveat that `{:sigra, "~> 0.2", optional: true}` is a host install shape and not a promise covering all Sigra `0.2.x` hosts per D-108
    - keep the wording narrow: no broadened support matrix, no implied variants support, no ecosystem-theater language

    Update `test/threadline/upgrade_path_doc_contract_test.exs` in the same slice so it locks the refreshed root-vs-example proof wording and the exact current proof pins already present in the repo.
  </action>
  <verify>
    <automated>mix test test/threadline/upgrade_path_doc_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>
    The canonical support matrix now clearly distinguishes root-lane proof from example-lane proof and explicitly warns that the Sigra declaration range is not a blanket compatibility promise.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh the Sigra guide around the small soft-loaded adapter and host-owned auth split</name>
  <files>guides/integrations/sigra.md, test/threadline/integrations/sigra_doc_contract_test.exs</files>
  <action>
    Update the Sigra guide so it stays tightly aligned to the current adapter seam and auth boundary:

    - keep `Threadline.Integrations.Sigra` as the direct callback pair around `actor_ref_from_conn/1` and `audit_context_overrides_from_conn/1` per D-109 and D-119
    - explicitly state that Sigra covers request capture only; it does not secure `/audit`, exports, tenancy, roles, or admin policy per D-110 through D-112
    - document the dual-transport auth split as intentional:
      - request capture auth via `actor_fn` + `context_overrides_fn`
      - LiveView surface auth via `authorize_fn`
      - export HTTP auth via the `export_authorize_fn` override or the default mirror fallback to `authorize_fn`
    - keep the reference-lane claim narrow and concrete; do not add a variants matrix or a second adapter story

    Update `test/threadline/integrations/sigra_doc_contract_test.exs` in the same slice to lock the request-capture-only wording, host-owned surface/export auth wording, and the no-blanket-support language.
  </action>
  <verify>
    <automated>mix test test/threadline/integrations/sigra_doc_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>
    The Sigra guide now presents one narrow maintained reference lane: soft-loaded request capture through the current adapter seam, with surface/export auth explicitly left host-owned.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| support matrix -> adopters | If root and example proof are blurred together, readers will infer compatibility that the repo does not actually prove. |
| Sigra guide -> security posture | If request capture and `/audit` auth are conflated, adopters may deploy the operator surface with the wrong security model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04 | I | upgrade-path guide | mitigate | Explicitly separate root `phoenix-surface` proof from example `sigra-reference` proof and keep exact proof pins only in proof docs per D-105 through D-108. |
| T-70-05 | S | Sigra guide auth story | mitigate | Document Sigra as request-capture-only and keep `/audit` plus export auth explicitly host-owned through `authorize_fn` / `export_authorize_fn`. |
| T-70-06 | E | scope creep | mitigate | Preserve one narrow maintained Sigra lane only; reject variants-matrix or broadened compatibility language per D-117 through D-120. |
</threat_model>

<verification>
Run:
- `mix test test/threadline/upgrade_path_doc_contract_test.exs test/threadline/integrations/sigra_doc_contract_test.exs --max-failures 1`
- `mix verify.compile_no_optional`
- `mix ci.all`
</verification>

<success_criteria>
- The support matrix is current and explicitly separates root-lane proof from example-lane proof.
- The Sigra guide keeps the adapter concrete, soft-loaded, and request-capture-only.
- Neither doc implies a broader Sigra or Phoenix compatibility matrix than the repo currently proves.
</success_criteria>

<output>
After completion, create `.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-02-SUMMARY.md`
</output>
