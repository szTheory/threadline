---
phase: 70
plan: "70-03"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/README.md
  - test/threadline/example_phoenix_readme_contract_test.exs
autonomous: true
requirements:
  - ADOPT-09
  - COMPAT-03
  - INTEG-02
must_haves:
  truths:
    - "The example app README proves one surface-first canonical reference path instead of acting like a second onboarding owner or a generic compatibility promise."
    - "Exact Phoenix/Sigra proof pins remain in the example proof doc, where they belong, and match the current example lockfile."
    - "The reference-path README names equivalent Mix-task parity for capture-only operators while keeping `/audit` behind host-owned admin auth."
  artifacts:
    - path: "examples/threadline_phoenix/README.md"
      provides: "Runnable reference-lane contract for the current Sigra/Phoenix proof path"
    - path: "test/threadline/example_phoenix_readme_contract_test.exs"
      provides: "Focused contract guard for reference-lane proof pins, host-owned `/audit` auth, and parity reminders"
  key_links:
    - from: "examples/threadline_phoenix/README.md"
      to: "examples/threadline_phoenix/mix.lock"
      via: "exact proof pins remain example-lane evidence rather than generic install guidance"
      pattern: "Sigra `0.2.5`"
    - from: "examples/threadline_phoenix/README.md"
      to: "examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex"
      via: "surface mount snippet stays extracted from the real router source"
      pattern: "threadline_operator_surface"
    - from: "examples/threadline_phoenix/README.md"
      to: "guides/getting-started-saas.md"
      via: "example remains runnable proof while the canonical narrative stays in guides per D-116"
      pattern: "canonical first-hour walkthrough"
---

<objective>
Refresh the example app README so it remains the runnable proof contract for the current Sigra/Phoenix reference lane without becoming a second primary guide or implying broader compatibility than the repo proves.

Purpose: satisfy the end-to-end proof portion of `ADOPT-09`, keep `COMPAT-03` current in the example proof artifact, and keep `INTEG-02` anchored to the existing reference adapter path.
Output: refreshed example README plus focused contract coverage for its lane wording, proof pins, auth boundary, and parity reminders.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-CONTEXT.md
@.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-RESEARCH.md
@.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-PATTERNS.md
@examples/threadline_phoenix/README.md
@examples/threadline_phoenix/mix.exs
@examples/threadline_phoenix/mix.lock
@examples/threadline_phoenix/lib/threadline_phoenix_web/router.ex
@guides/getting-started-saas.md
@guides/integrations/sigra.md
@guides/operator-surface.md
@test/threadline/example_phoenix_readme_contract_test.exs

Locked decisions implemented here:

- D-107: exact Phoenix/Sigra proof pins live in support/example proof docs, not generic install docs.
- D-113/D-114/D-115/D-116: one canonical surface-first story, parity reminders at relevant steps, one obvious path, and the example stays proof rather than narrative owner.
- D-110/D-111/D-112: `/audit` remains behind host browser/admin auth and Threadline's authorization hooks; Sigra does not become the auth story.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the example README as the runnable proof of the single canonical reference path</name>
  <files>examples/threadline_phoenix/README.md</files>
  <action>
    Refresh `examples/threadline_phoenix/README.md` so it tells one coherent reference-path story:

    - Sigra-backed request capture through the existing callback pair
    - one audited request that returns `audit_transaction_id`
    - mounted `/audit` behind host-owned `:browser` + `:admin_auth`
    - investigation of the same incident in the shipped surface
    - explicit parity reminders for capture-only operators at the matching points:
      - incident bundle -> `mix threadline.incident`
      - trigger coverage -> `mix threadline.health.coverage`
      - policy drift -> `mix threadline.policy.show`

    Keep the README in the proof role rather than the canonical narrative role:

    - keep the exact current Phoenix/Sigra proof pins in the support/example proof section, sourced from the example lockfile
    - keep pointing readers back to `guides/getting-started-saas.md` for the canonical first-hour walkthrough per D-116
    - preserve the real router snippet extracted from source
    - explicitly keep `/audit` host-auth-owned and fail-closed through `authorize_fn`; do not imply Sigra secures the operator surface or exports

    Do not add a second reference lane, do not add adapter expansion, and do not flatten the example proof into a generic install guide.
  </action>
  <verify>
    <automated>rg -n 'sigra-reference|Sigra `0\\.2\\.5`|mix threadline\\.(incident|health\\.coverage|policy\\.show)|guides/getting-started-saas\\.md|admin_auth|authorize_fn' examples/threadline_phoenix/README.md</automated>
  </verify>
  <done>
    The example README reads as a runnable proof contract for one current reference lane, keeps exact proof pins where they belong, and names the equivalent non-surface operator paths without promoting a second onboarding story.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend the example README contract test to lock proof pins, auth boundaries, and parity reminders</name>
  <files>test/threadline/example_phoenix_readme_contract_test.exs</files>
  <action>
    Update the focused example README contract test so it locks the refreshed Phase 70 proof surface:

    - exact current proof pins sourced from the example lockfile section
    - the `sigra-reference` lane wording and the anti-overclaim disclaimer
    - the extracted real router mount block
    - host-owned `/audit` auth wording (`pipeline :admin_auth`, `authorize_fn`, authenticated admin boundary)
    - parity reminders for `mix threadline.incident`, `mix threadline.health.coverage`, and `mix threadline.policy.show`
    - the link back to `guides/getting-started-saas.md` as the canonical first-hour narrative

    Keep the test in the repository's existing source-reading style and avoid snapshotting the whole README. The assertions should fail if the example stops being a narrow proof artifact or if parity/auth wording drifts.
  </action>
  <verify>
    <automated>mix test test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>
    CI will fail if the example README drops its exact proof pins, overclaims compatibility, loses host-owned `/audit` auth wording, or stops naming the capture-only parity commands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example README -> adopters | If the proof README overclaims support or becomes the primary narrative owner, readers will infer a broader or different adoption path than the repo intends. |
| router proof -> surface auth expectations | If the real mount/auth boundary drifts from the README, `/audit` can be taught with the wrong security posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-07 | I | example README proof pins | mitigate | Keep exact Phoenix/Sigra pins only in the example proof section and lock them with focused source-reading assertions. |
| T-70-08 | S | `/audit` auth narrative | mitigate | Keep the README anchored to the real router snippet and explicit `admin_auth` / `authorize_fn` host-owned boundary. |
| T-70-09 | R | parity narrative | mitigate | Lock `mix threadline.incident`, `mix threadline.health.coverage`, and `mix threadline.policy.show` reminders so surface-first does not erase capture-only parity. |
</threat_model>

<verification>
Run:
- `mix test test/threadline/example_phoenix_readme_contract_test.exs --max-failures 1`
- `mix verify.example`
- `mix ci.all`
</verification>

<success_criteria>
- The example README proves one narrow Sigra/Phoenix reference path with current exact pins.
- The example remains a proof artifact, not a second canonical narrative owner.
- Host-owned `/audit` auth and capture-only parity are explicit and contract-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-03-SUMMARY.md`
</output>
