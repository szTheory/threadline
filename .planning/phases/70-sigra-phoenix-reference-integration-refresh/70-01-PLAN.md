---
phase: 70
plan: "70-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/getting-started-saas.md
  - guides/operator-surface.md
  - test/threadline/getting_started_saas_doc_contract_test.exs
  - test/threadline/readme_doc_contract_test.exs
  - test/threadline/operator_surface_doc_contract_test.exs
autonomous: true
requirements:
  - COMPAT-03
  - ADOPT-09
must_haves:
  truths:
    - "Generic install docs no longer teach stale `threadline ~> 0.3` snippets and instead reflect the current root package posture."
    - "Generic docs keep exact Phoenix/Sigra proof pins out of install snippets while still pointing adopters to the canonical lane/proof docs."
    - "The surface-first onboarding docs keep explicit capture-only parity reminders without promoting a second top-level reference lane."
  artifacts:
    - path: "README.md"
      provides: "Root package and discovery wording aligned to the current release without overclaiming support"
    - path: "guides/getting-started-saas.md"
      provides: "Canonical first-hour surface-first walkthrough with current root dependency posture and parity reminders"
    - path: "guides/operator-surface.md"
      provides: "Mount guide with current root package snippet and scoped references to lane/proof docs"
    - path: "test/threadline/getting_started_saas_doc_contract_test.exs"
      provides: "Focused source-reading guard for the canonical first-hour guide's root package posture and parity literals"
  key_links:
    - from: "README.md"
      to: "mix.exs"
      via: "root package range stays aligned to @version-driven release posture"
      pattern: "@version \"0.4.0\""
    - from: "guides/getting-started-saas.md"
      to: "guides/upgrade-path.md"
      via: "generic install docs defer exact proof pins to the lane/proof guide per D-105 and D-106"
      pattern: "sigra-reference"
    - from: "guides/operator-surface.md"
      to: "mix.exs"
      via: "optional Phoenix surface dependency ranges remain generic install posture, not example-lane proof pins"
      pattern: "{:phoenix, \"~> 1.7\""
---

<objective>
Refresh the generic install and discovery docs so they match the current root package posture and the Phase 69 lane model without leaking example-lane proof pins into broad install guidance.

Purpose: satisfy the stale root-package wording portion of `COMPAT-03` and the docs-side parity portion of `ADOPT-09` while preserving the locked boundary that there is still only one canonical reference lane.
Output: updated root/generic docs plus focused contract coverage for the canonical first-hour surfaces.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-CONTEXT.md
@.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-RESEARCH.md
@.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-PATTERNS.md
@README.md
@guides/getting-started-saas.md
@guides/operator-surface.md
@guides/upgrade-path.md
@mix.exs

Locked decisions implemented here:

- D-105/D-106: generic install docs use root package/range posture and keep exact Phoenix/Sigra proof pins out.
- D-113/D-114/D-115: keep one canonical surface-first path while naming Mix-task parity at the relevant steps.
- D-117/D-118: do not invent a second lane or broaden the support matrix from generic docs.

Observed repo drift to correct in this slice:

- `README.md` still teaches `{:threadline, "~> 0.3"}`
- `guides/getting-started-saas.md` still teaches `{:threadline, "~> 0.3"}`
- `guides/operator-surface.md` still teaches `{:threadline, "~> 0.3.0"}`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh generic install snippets and discovery copy to the current root posture</name>
  <files>README.md, guides/getting-started-saas.md, guides/operator-surface.md</files>
  <action>
    Update the three generic install/discovery surfaces so they reflect the root package posture from `mix.exs` instead of the stale `threadline ~> 0.3` wording discovered in research.

    Implement the locked boundaries directly:

    - replace stale root `threadline` dependency snippets with the current major/minor posture implied by root `mix.exs` (`0.4.x` release line)
    - keep exact Phoenix/Sigra proof pins OUT of these generic docs per D-105 and D-106
    - preserve `guides/getting-started-saas.md` as the canonical surface-first first-hour path per D-113 and D-115
    - add or tighten capture-only parity reminders at the same operator steps they correspond to per D-114:
      - incident drill-down -> `mix threadline.incident`
      - trigger coverage -> `mix threadline.health.coverage`
      - redaction drift / policy view -> `mix threadline.policy.show`
    - keep `/audit` explicitly host-auth-owned; do not imply Sigra secures the surface or exports
    - keep support/discovery pointers narrow: generic docs point to `guides/upgrade-path.md` and `guides/integrations/sigra.md` for lane/proof specifics instead of re-stating the matrix locally

    Do not add a second onboarding lane, do not broaden compatibility claims, and do not turn generic docs into proof-pin ledgers.
  </action>
  <verify>
    <automated>rg -n '{:threadline, "~> 0\\.(3|3\\.0)"}|mix threadline\\.(incident|health\\.coverage|policy\\.show)|guides/upgrade-path\\.md|guides/integrations/sigra\\.md' README.md guides/getting-started-saas.md guides/operator-surface.md</automated>
  </verify>
  <done>
    The generic install docs no longer teach stale `0.3` root package snippets, keep proof pins out of broad install prose, and explicitly name the capture-only parity commands while leaving the support matrix to the canonical lane docs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock the refreshed root and first-hour doc posture with focused contract tests</name>
  <files>test/threadline/getting_started_saas_doc_contract_test.exs, test/threadline/readme_doc_contract_test.exs, test/threadline/operator_surface_doc_contract_test.exs</files>
  <action>
    Extend focused source-reading contract coverage so Phase 70's root-doc repair cannot silently regress.

    Required assertions:

    - `README.md` no longer contains stale `threadline ~> 0.3` guidance and still points readers at `guides/upgrade-path.md` for named lane support claims
    - `guides/getting-started-saas.md` locks the current root package posture plus the three parity reminders (`mix threadline.incident`, `mix threadline.health.coverage`, `mix threadline.policy.show`)
    - `guides/operator-surface.md` locks the refreshed root package snippet and stays scoped to mount/auth/screens rather than example-lane proof pins

    Use the repository's existing narrow `String.contains?/2` / `refute String.contains?/2` doc-contract style rather than snapshots. Keep the assertions limited to the generic-doc responsibilities in this slice.
  </action>
  <verify>
    <automated>mix test test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs --max-failures 1</automated>
  </verify>
  <done>
    The root/generic docs now fail fast if stale package snippets return, if the first-hour guide drops parity reminders, or if the operator-surface guide drifts back into a proof-pin surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generic docs -> adopters | Stale or overly specific install snippets can mis-teach the current root package posture or imply broader support than the repo proves. |
| surface-first docs -> capture-only adopters | If parity reminders disappear, UI-first onboarding can look like the only supported operator path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | I | generic install docs | mitigate | Replace stale `0.3` snippets with current root package posture and keep exact proof pins out of generic docs per D-105/D-106. |
| T-70-02 | T | first-hour narrative | mitigate | Preserve one canonical surface-first path while naming `mix threadline.incident`, `mix threadline.health.coverage`, and `mix threadline.policy.show` at the matching operator steps per D-114/D-115. |
| T-70-03 | E | support claims | mitigate | Keep matrix/proof specifics delegated to `guides/upgrade-path.md` and `guides/integrations/sigra.md` so generic docs do not imply a broadened support lane. |
</threat_model>

<verification>
Run:
- `rg -n '{:threadline, "~> 0\\.(3|3\\.0)"}|mix threadline\\.(incident|health\\.coverage|policy\\.show)|guides/upgrade-path\\.md|guides/integrations/sigra\\.md' README.md guides/getting-started-saas.md guides/operator-surface.md`
- `mix test test/threadline/getting_started_saas_doc_contract_test.exs test/threadline/readme_doc_contract_test.exs test/threadline/operator_surface_doc_contract_test.exs --max-failures 1`
- `mix ci.all`
</verification>

<success_criteria>
- Generic install docs reflect the current `threadline` release posture instead of stale `0.3` snippets.
- Exact Phoenix/Sigra proof pins remain out of broad install docs.
- The canonical first-hour docs keep surface-first onboarding and explicit capture-only parity reminders.
</success_criteria>

<output>
After completion, create `.planning/phases/70-sigra-phoenix-reference-integration-refresh/70-01-SUMMARY.md`
</output>
