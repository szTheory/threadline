---
phase: 141-motion-micro-animation
plan: "03"
type: execute
wave: 3
depends_on: ["141-02"]
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts
autonomous: true
requirements: [POLISH-MOTION]
must_haves:
  truths:
    - "A real browser sees representative default motion using the named keyframes and locked token durations."
    - "A real browser with `prefers-reduced-motion: reduce` sees collapsed duration/delay behavior on representative animated and transition surfaces."
    - "Browser verification remains focused on motion and reduced motion, not screenshots or responsive layout."
  artifacts:
    - path: "examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts"
      provides: "Focused browser motion and reduced-motion UAT"
      contains: "reducedMotion"
  key_links:
    - from: "examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts"
      to: "examples/threadline_phoenix/e2e/playwright.config.ts"
      via: "Playwright reducedMotion use override"
      pattern: "reducedMotion"
    - from: "examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts"
      to: "lib/threadline/operator_surface/style.ex"
      via: "computed style assertions for CSS classes/keyframes"
      pattern: "animationName"
---

<objective>
Add focused Playwright verification for Phase 141 motion contracts: default-motion computed styles for representative branded motion and reduced-motion computed styles for representative animated/transitioned surfaces.

Purpose: source contracts prove coverage statically, but reduced-motion behavior must also be observed in a browser using the existing example app and Playwright preference emulation.
Output: `operator-motion.spec.ts` plus passing focused browser verification evidence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/141-motion-micro-animation/141-CONTEXT.md
@.planning/phases/141-motion-micro-animation/141-RESEARCH.md
@.planning/phases/141-motion-micro-animation/141-PATTERNS.md
@.planning/phases/141-motion-micro-animation/141-UI-SPEC.md
@.planning/phases/141-motion-micro-animation/141-01-SUMMARY.md
@.planning/phases/141-motion-micro-animation/141-02-SUMMARY.md
@.planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md
@examples/threadline_phoenix/e2e/playwright.config.ts
@examples/threadline_phoenix/e2e/package.json
@examples/threadline_phoenix/e2e/run-e2e.sh
@examples/threadline_phoenix/e2e/tests/operator-earned-flows.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-prove-mobile.spec.ts

<interfaces>
Existing E2E setup:
- `examples/threadline_phoenix/e2e/playwright.config.ts` sets `reducedMotion: "reduce"` globally for deterministic operator-surface tests.
- `examples/threadline_phoenix/e2e/package.json` exposes `npm test` as `playwright test`.
- Existing specs import `{ expect, Locator, Page, test }` from `@playwright/test`, log in through `/users/log_in`, and use `admin@example.com` plus `process.env.DEMO_SEED_PASSWORD ?? "password123456"`.
- Phase 140 browser spec discovers a real `ticket_replies` row id from Timeline/Transaction before opening row-history routes.
- `examples/threadline_phoenix/e2e/run-e2e.sh` can reset/migrate/seed, install Playwright chromium, start the example Phoenix server, and run the browser suite; for focused local reruns, start the same server and run `npm test -- tests/operator-motion.spec.ts`.

Browser assertions to prefer:
- Use `test.describe` blocks with `test.use({ reducedMotion: "no-preference" })` for default-motion checks and `test.use({ reducedMotion: "reduce" })` for reduced-motion checks.
- Inspect `getComputedStyle(element).animationName`, `animationDuration`, `animationDelay`, `transitionDuration`, and pseudo-element styles through `getComputedStyle(element, "::before")` / `getComputedStyle(element, "::after")`.
- Assert computed values on representative surfaces from the inventory rather than taking screenshots.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused operator motion Playwright spec</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts</files>
  <behavior>
    - The spec adds no dependency and does not modify Playwright config.
    - The spec logs in using the existing admin helper shape.
    - The spec has a default-motion block using `test.use({ reducedMotion: "no-preference" })`.
    - Default-motion assertions visit `/audit` and verify `.tl-home__card` uses `tl-rise-in` with `0.18s` duration, and `.tl-home__card--primary::before` uses `tl-thread-draw` with `0.24s` duration and the documented signature delay.
    - Default-motion assertions include at least one representative low-frequency proof/progression surface from the inventory when present in seeded data; if the seeded surface is conditional, the spec must assert the inventory-backed Home signature thread and drawer entry instead of making a flaky optional assertion.
    - The spec has a reduced-motion block using `test.use({ reducedMotion: "reduce" })`, matching the default config path explicitly.
    - Reduced-motion assertions visit `/audit` and verify Home card animation duration collapses to `0.001s`, signature thread delay collapses to `0s`, and pseudo-element duration collapses to `0.001s`.
    - Reduced-motion assertions open a real row-history drawer by discovering a seeded `ticket_replies` row id through the existing Timeline/Transaction path, then verify `.tl-subview` drawer animation duration collapses to `0.001s` and final visible transform is not an off-screen translation.
    - Reduced-motion assertions open a policy details row and verify `.tl-policy__row::details-content` transition durations collapse to the reduced-motion value.
    - The spec does not assert screenshot baselines, broad responsive layout, route changes, export semantics, or row-history data correctness beyond what is needed to reach motion surfaces.
  </behavior>
  <action>Create `operator-motion.spec.ts` using helper patterns from existing operator E2E specs. Keep the file focused on computed-style motion contracts. Use `no-preference` override only in the default-motion describe block because the global Playwright config already uses reduced motion. Reuse seeded navigation to reach row-history; do not add test-only routes or fixture data. Avoid waiting on arbitrary animation timing; assert computed styles after target elements are visible and use `expect.poll` only for settled style reads.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-motion.spec.ts</automated>
  </verify>
  <done>`operator-motion.spec.ts` proves representative default and reduced-motion behavior in a real browser without broadening the phase scope.</done>
</task>

<task type="auto">
  <name>Task 2: Run focused browser and source-contract gates</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts</files>
  <action>Run the focused source-contract and browser gates after the spec exists. If the example app server is not available on `127.0.0.1:4002`, use the existing test server setup from Phase 140: create/migrate the example test database, seed with `THREADLINE_E2E=1`, start `PORT=4002 MIX_ENV=test THREADLINE_E2E=1 mix phx.server`, then run the focused Playwright command from the e2e directory. Record the exact server setup and test counts in the summary. Do not run or create screenshot baselines, mobile matrix assertions, or unrelated E2E specs to satisfy this plan.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
    <automated>cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-motion.spec.ts</automated>
  </verify>
  <done>Source-contract tests and focused motion browser UAT pass; summary records exact commands, base URL, and result counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser preference emulation -> CSS media query | Playwright preference controls `prefers-reduced-motion` behavior. |
| seeded browser session -> operator surface | Tests log in as seeded admin and reach real surfaces. |
| computed style -> phase proof | Browser-computed values become acceptance evidence for motion behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-09 | Spoofing | reduced-motion preference | mitigate | Use explicit `test.use({ reducedMotion: "no-preference" })` and `test.use({ reducedMotion: "reduce" })` blocks so each assertion states its preference mode. |
| T-141-10 | Repudiation | browser proof | mitigate | Record exact command, base URL, preference modes, and test counts in the plan summary. |
| T-141-11 | Denial of Service | browser flake from animation timing | mitigate | Assert computed styles after visibility and avoid arbitrary sleep or screenshot timing. |
| T-141-12 | Tampering | E2E scope | mitigate | Add one focused spec only; do not edit routes, seed data, Playwright config, or production CSS in this plan. |
| T-141-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; use existing Playwright dependency and e2e scripts only. |
</threat_model>

<quality_gates>
- Browser proof must include both default and reduced-motion preference modes.
- Reduced-motion assertions must check at least Home card, signature thread pseudo-element, drawer entry, and policy details transition behavior.
- No screenshot baseline, broad responsive matrix, new route, seed/schema change, or Playwright config change is allowed.
- Existing untracked Phase 136/137 files are not touched.
</quality_gates>

<verification>
- `mix test test/threadline/operator_surface/style_contract_test.exs`
- `cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-motion.spec.ts`
- `rg -n "reducedMotion|no-preference|animationName|animationDuration|transitionDuration|tl-thread-draw" examples/threadline_phoenix/e2e/tests/operator-motion.spec.ts`
</verification>

<success_criteria>
- Browser default-motion checks prove representative keyframes and durations use the locked token-derived values.
- Browser reduced-motion checks prove representative animated and transition surfaces collapse duration/delay under `prefers-reduced-motion: reduce`.
- Phase 141 closes with static source contracts plus focused browser UAT, without adding dependencies or broadening into Phase 142/143 work.
</success_criteria>

<output>
Create `.planning/phases/141-motion-micro-animation/141-03-SUMMARY.md` when done.
</output>
