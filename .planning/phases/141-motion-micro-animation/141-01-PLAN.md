---
phase: 141-motion-micro-animation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md
  - test/threadline/operator_surface/style_contract_test.exs
autonomous: true
requirements: [POLISH-MOTION]
must_haves:
  truths:
    - "Every shipped operator-surface animation and non-trivial transition is listed in a motion inventory with trigger, JTBD, token, rationale, frequency, and reduced-motion behavior."
    - "Source-contract tests reject uninventoryed keyframes, uninventoryed animation consumers, literal duration drift, and missing reduced-motion coverage."
    - "The inventory treats motion as a design-system contract rather than decorative commentary."
  artifacts:
    - path: ".planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md"
      provides: "Testable motion inventory for Phase 141"
      contains: "M-01"
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "Static motion contract tests"
      contains: "motion inventory"
  key_links:
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: ".planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md"
      via: "File.read! inventory assertions"
      pattern: "141-MOTION-INVENTORY"
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "CSS source-read assertions"
      pattern: "tl-thread-draw"
---

<objective>
Create the auditable spine for Phase 141: a source-testable motion inventory plus ExUnit source-contract tests that keep CSS motion, tokens, keyframes, rationale, and reduced-motion coverage aligned.

Purpose: Phase 141 succeeds only if motion is governed as a design-system contract. This plan makes the current motion surface explicit before any CSS correction work happens.
Output: `141-MOTION-INVENTORY.md` and focused additions to `style_contract_test.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/141-motion-micro-animation/141-CONTEXT.md
@.planning/phases/141-motion-micro-animation/141-RESEARCH.md
@.planning/phases/141-motion-micro-animation/141-PATTERNS.md
@.planning/phases/141-motion-micro-animation/141-UI-SPEC.md
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/style_contract_test.exs

<interfaces>
Existing source-contract pattern:
- `test/threadline/operator_surface/style_contract_test.exs` uses `@style_path "lib/threadline/operator_surface/style.ex"` and `File.read!(@style_path)` in async ExUnit tests.
- Existing tests assert token-backed CSS through `String.contains?/2`, scoped `String.split/2` sections, selector loops, and `Regex.match?/2` refutations.

Motion source anchors:
- Motion tokens live in `style.ex`: `--tl-motion-fast: 120ms`, `--tl-motion-base: 180ms`, `--tl-motion-slow: 240ms`, `--tl-motion-stagger: 40ms`, `--tl-motion-distance-sm: 8px`, `--tl-motion-distance-md: 16px`, `--tl-ease-standard`, `--tl-ease-out`, `--tl-transition-fast`.
- Allowed keyframes are `tl-drawer-in`, `tl-rise-in`, `tl-thread-draw`, `tl-fade-in`, and `tl-copy-pulse` per D-03.
- Current animation consumers include `.tl-home__card`, `.tl-home__card--primary::before`, `.tl-subview`, `#retention-runs > tr`, `.tl-subview__timeline > *`, `.tl-record-list > .tl-record-card`, `#transactions-list > .tl-change`, `.tl-copy.is-copied`, `.tl-journey-rail::before`, and `.tl-policy__success::after`.
- Current non-trivial transition families include `.threadline-ui a`, `.tl-topbar .tl-topbar__nav-item`, `.tl-toolbar__control` / `.tl-control`, `.tl-button`, `.tl-change`, `.tl-policy__summary`, `.tl-policy__row::details-content`, `.tl-copy`, and `.tl-policy__summary::before`.
- Reduced motion is centralized in `@media (prefers-reduced-motion: reduce)` and covers `.threadline-ui *`, pseudo-elements, `.tl-policy__row::details-content`, collapsed duration/delay, iteration count, scroll behavior, and `.tl-button:active` transform reset.
</interfaces>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-141 | Restrained, research-backed micro-animation with inventory, token reuse, signature thread-draw, reduced-motion support, and no gratuitous motion | 141-01, 141-02, 141-03 | COVERED | Inventory and contracts start here; alignment and browser proof follow.
REQ | POLISH-MOTION | Documented inventory, rationale, reduced-motion support, no gratuitous motion | 141-01, 141-02, 141-03 | COVERED | Requirement appears in every plan frontmatter because each plan closes a distinct part of the same requirement.
RESEARCH | motion-inventory-contract | Add `141-MOTION-INVENTORY.md` with selector, trigger, JTBD/persona, token, reduced-motion behavior, rationale, and source anchor | 141-01 | COVERED | Task 1 creates this artifact.
RESEARCH | source-contract-governance | Extend `style_contract_test.exs` for token/keyframe/inventory/reduced-motion drift | 141-01, 141-02 | COVERED | Task 2 creates tests; Plan 02 resolves any failures.
RESEARCH | focused-browser-checks | Add computed-style Playwright verification for default and reduced motion | 141-03 | COVERED | Deferred to browser plan after source contracts.
RESEARCH | no-new-packages | No animation library or new package installs | 141-01, 141-02, 141-03 | COVERED | Threat model includes supply-chain gate with no installs.
CONTEXT | D-01 | Motion is a design-system contract, every animation has trigger/JTBD/token | 141-01 | COVERED | Inventory schema and tests implement this.
CONTEXT | D-02 | Keep authoritative motion token scale | 141-01, 141-02 | COVERED | Tests assert token names/values and forbid drift.
CONTEXT | D-03 | Reuse allowed keyframes, no ad-hoc one-off keyframes or literal durations | 141-01, 141-02 | COVERED | Tests define allowed keyframe/duration contract; Plan 02 resolves violations.
CONTEXT | D-04 | Preserve Signal Cyan thread-draw only for earned proof/progression moments | 141-01, 141-02, 141-03 | COVERED | Inventory rows must justify each `tl-thread-draw`; browser verifies representative default/reduced behavior.
CONTEXT | D-05 | Keep motion GPU-friendly with rare justified layout exception | 141-01, 141-02 | COVERED | Inventory properties and tests identify transform/opacity/box-shadow vs details-content exception.
CONTEXT | D-06 | Reduced motion covers every animation and transition | 141-01, 141-02, 141-03 | COVERED | Source tests cover all; browser plan proves representative real-browser behavior.
CONTEXT | D-07 | Inventory documentation is required and testable | 141-01 | COVERED | This plan creates and reads it from ExUnit.
CONTEXT | D-08 | Avoid broad visual redesign, route behavior, content hierarchy, mobile layout, export/row-history semantic changes | 141-01, 141-02, 141-03 | COVERED | Quality gates explicitly refute scope creep.
CONTEXT | D-09 | Browser verification is focused motion/reduced-motion, not screenshots or broad responsive matrix | 141-03 | COVERED | Browser plan is separate and scoped.
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create source-testable motion inventory</name>
  <files>.planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md</files>
  <action>Create `141-MOTION-INVENTORY.md` as the Phase 141 motion contract artifact per D-01 and D-07. Include a short scope section, the locked token/keyframe scale per D-02 and D-03, and a table with columns `id`, `selector_or_keyframe`, `surface`, `trigger`, `persona_jtbd`, `rationale`, `token`, `properties`, `frequency`, `reduced_motion`, `source`, and `status`. Inventory every current animation consumer and every non-trivial transition family named in the context interfaces. Group generic color/background/border/focus transitions only when they share one token contract and one JTBD; list transform transitions and `tl-thread-draw` consumers explicitly. Mark each row `keep`, `neutralize`, `justify`, or `remove`; do not use empty rationale cells, speculative rows, or broad Phase 142/143 checks.</action>
  <verify>
    <automated>test -f .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md</automated>
    <automated>rg -n "M-[0-9]{2}|selector_or_keyframe|persona_jtbd|reduced_motion|tl-thread-draw|prefers-reduced-motion" .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md</automated>
  </verify>
  <done>`141-MOTION-INVENTORY.md` maps every shipped motion surface to trigger, JTBD/persona, rationale, tokens, properties, frequency, reduced-motion behavior, source anchor, and disposition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add motion source-contract tests</name>
  <files>test/threadline/operator_surface/style_contract_test.exs</files>
  <behavior>
    - Tests read both `lib/threadline/operator_surface/style.ex` and `.planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md`.
    - Tests assert the locked motion token values from D-02.
    - Tests assert the only defined `@keyframes` names are `tl-drawer-in`, `tl-rise-in`, `tl-thread-draw`, `tl-fade-in`, and `tl-copy-pulse` per D-03.
    - Tests assert every current `animation:` consumer selector appears in the inventory and uses an approved keyframe with `var(--tl-motion-*)` duration.
    - Tests assert `tl-thread-draw` consumers appear in inventory rows with signature/proof/progression rationale per D-04.
    - Tests assert every non-trivial transition family from Task 1 appears in the inventory and uses `var(--tl-transition-fast)` or named motion/ease tokens.
    - Tests reject `transition: all`, new JavaScript animation library markers, new ad-hoc keyframes, and literal duration/delay values outside motion token definitions, the documented signature thread delay, and reduced-motion collapsed values.
    - Tests assert the reduced-motion block covers `.threadline-ui *`, `.threadline-ui *::before`, `.threadline-ui *::after`, `.tl-policy__row::details-content`, `transition-duration`, `animation-duration`, `animation-delay`, `animation-iteration-count`, `scroll-behavior`, and `.tl-button:active` transform reset.
    - Tests assert every inventory row has a non-empty rationale, trigger, token, and reduced-motion cell; no row may use `TBD`.
  </behavior>
  <action>Extend `style_contract_test.exs` with Phase 141 motion contract tests using the existing source-read style. Keep the tests deterministic string/regex assertions; do not introduce a CSS parser dependency or a new test harness. Make failures name the missing selector/keyframe/token clearly so Plan 02 can resolve real drift without rediscovery. Preserve existing Phase 136-140 tests and avoid production CSS edits in this plan.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <done>Focused ExUnit contracts fail on ungoverned motion drift and pass when the inventory and current CSS motion contract are aligned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS source -> inventory artifact | Source behavior is translated into human-readable contract rows. |
| inventory artifact -> ExUnit tests | Markdown content becomes a contract input for automated checks. |
| planner artifact -> future CSS edits | Later implementation must use inventory/test failures instead of subjective motion judgment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Tampering | `141-MOTION-INVENTORY.md` | mitigate | ExUnit reads the inventory and fails when source selectors/keyframes are missing from it. |
| T-141-02 | Repudiation | motion rationale | mitigate | Inventory rows require trigger, persona/JTBD, token, rationale, source, and status so shipped motion has durable rationale. |
| T-141-03 | Information Disclosure | planning artifact | accept | Inventory contains CSS selectors and operational rationale only; no secrets or private seed data. |
| T-141-04 | Denial of Service | test suite | mitigate | Use lightweight source-read ExUnit checks; avoid browser setup and dependency installs in this plan. |
| T-141-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; if an executor tries to add an animation/CSS parser package, stop and require a package legitimacy checkpoint. |
</threat_model>

<quality_gates>
- No production source file is modified by this plan.
- No new package dependency, animation library, screenshot baseline, responsive matrix, route change, or IA/content hierarchy change is introduced.
- Inventory rows must tie motion to operator JTBD, not decorative language.
- Existing untracked Phase 136/137 files are not touched.
</quality_gates>

<verification>
- `mix test test/threadline/operator_surface/style_contract_test.exs`
- `rg -n "tl-rise-in|tl-thread-draw|tl-drawer-in|tl-fade-in|tl-copy-pulse|prefers-reduced-motion" .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md`
</verification>

<success_criteria>
- A documented inventory maps each shipped animation and meaningful transition to trigger, JTBD/persona, token, reduced-motion behavior, and rationale.
- Source-contract tests enforce the locked token/keyframe scale, inventory coverage, reduced-motion coverage, and no ad-hoc motion.
- The plan closes D-01, D-02, D-03, D-04, D-06, and D-07 at the contract level without changing production CSS.
</success_criteria>

<output>
Create `.planning/phases/141-motion-micro-animation/141-01-SUMMARY.md` when done.
</output>
