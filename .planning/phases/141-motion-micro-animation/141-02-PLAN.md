---
phase: 141-motion-micro-animation
plan: "02"
type: execute
wave: 2
depends_on: ["141-01"]
files_modified:
  - lib/threadline/operator_surface/style.ex
  - .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md
  - test/threadline/operator_surface/style_contract_test.exs
autonomous: true
requirements: [POLISH-MOTION]
must_haves:
  truths:
    - "CSS motion either satisfies the Plan 01 contract or is narrowly corrected until it does."
    - "Every retained animation uses the locked 120/180/240ms token scale, the existing named keyframes, and documented signature thread-draw rules."
    - "No gratuitous or ungoverned motion remains in `style.ex` after the source contract passes."
  artifacts:
    - path: "lib/threadline/operator_surface/style.ex"
      provides: "Final tokenized operator-surface motion CSS"
      contains: "@media (prefers-reduced-motion: reduce)"
    - path: ".planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md"
      provides: "Final inventory matching any CSS corrections"
      contains: "status"
    - path: "test/threadline/operator_surface/style_contract_test.exs"
      provides: "Regression lock for motion corrections"
      contains: "motion"
  key_links:
    - from: "lib/threadline/operator_surface/style.ex"
      to: ".planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md"
      via: "selector/keyframe/source rows"
      pattern: "tl-copy-pulse"
    - from: "test/threadline/operator_surface/style_contract_test.exs"
      to: "lib/threadline/operator_surface/style.ex"
      via: "motion source-contract tests"
      pattern: "prefers-reduced-motion"
---

<objective>
Resolve any drift exposed by the motion inventory and source-contract tests, keeping changes narrowly scoped to CSS motion governance and inventory alignment.

Purpose: Plan 01 makes the contract executable; this plan ensures the shipped CSS actually satisfies that contract without adding new motion, redesigning screens, or changing operator workflows.
Output: token/inventory-aligned motion CSS when needed, updated inventory/test expectations, and passing source-contract gates.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/141-motion-micro-animation/141-CONTEXT.md
@.planning/phases/141-motion-micro-animation/141-RESEARCH.md
@.planning/phases/141-motion-micro-animation/141-PATTERNS.md
@.planning/phases/141-motion-micro-animation/141-UI-SPEC.md
@.planning/phases/141-motion-micro-animation/141-01-SUMMARY.md
@.planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/style_contract_test.exs

<interfaces>
Contract inputs from Plan 01:
- `141-MOTION-INVENTORY.md` is the disposition list: `keep`, `neutralize`, `justify`, or `remove`.
- `style_contract_test.exs` is the hard gate for token values, allowed keyframes, inventory coverage, literal timing drift, reduced-motion blanket coverage, and scope creep.

Allowed correction types:
- Replace literal motion durations/delays with existing `--tl-motion-*` / `--tl-transition-fast` tokens, except the documented signature thread delay when retained by inventory.
- Remove or neutralize uninventoryed/gratuitous motion rather than inventing a new rationale.
- Add targeted reduced-motion resets inside the existing `@media (prefers-reduced-motion: reduce)` block only for transformed surfaces that would otherwise remain visually ambiguous.
- Update inventory and tests only to match real source behavior and locked decisions; do not loosen tests to permit ad-hoc motion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve contract failures against motion CSS</name>
  <files>lib/threadline/operator_surface/style.ex, .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md, test/threadline/operator_surface/style_contract_test.exs</files>
  <behavior>
    - Start by running `mix test test/threadline/operator_surface/style_contract_test.exs` and treat failures as the correction list.
    - If the contract already passes, do not change `style.ex`; record the no-production-edit result in the plan summary after running the quality gates.
    - If an animation uses an unapproved keyframe, remove it or convert it to an existing allowed keyframe per D-03.
    - If a duration or delay is literal outside the locked token definitions, reduced-motion collapse values, or documented signature thread delay, replace it with an existing token or document the retained signature delay in inventory per D-02 and D-04.
    - If `tl-thread-draw` appears outside primary entry, completed path, evidence/proof progression, or trust-restored policy success, neutralize that use instead of broadening the motif per D-04.
    - If motion touches layout-affecting properties outside the existing `.tl-policy__row::details-content` disclosure exception, remove or neutralize it unless the inventory already marks it as the policy disclosure exception per D-05.
    - If reduced-motion coverage is incomplete, extend the existing reduced-motion block with targeted selectors and final-state resets; do not create separate media-query islands unless a source-contract test requires a named exception.
  </behavior>
  <action>Use the Plan 01 tests as the source of truth and make the smallest CSS/inventory/test alignment needed to pass them. Preserve the current BEM `.tl-*` / `--tl-*` architecture. Do not add new keyframes, new timing tokens, JavaScript animation code, dependencies, route behavior, content hierarchy changes, responsive layout work, screenshot-diff infrastructure, or earned-flow semantics. If a test is too broad and blocks a legitimate existing tokenized motion row, refine the test with a narrower source anchor rather than weakening the locked decisions.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <done>The source-contract tests pass because CSS, inventory, and tests agree on allowed motion; no uninventoryed or gratuitous motion remains.</done>
</task>

<task type="auto">
  <name>Task 2: Run motion quality gates and update final inventory dispositions</name>
  <files>lib/threadline/operator_surface/style.ex, .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md</files>
  <action>Run a final source scan and update `141-MOTION-INVENTORY.md` so every row's status matches the post-correction CSS. The scan must confirm no `transition: all`, no extra `@keyframes`, no JavaScript animation markers, no unlisted `animation:` selectors, and no broad Phase 142/143 work. Keep all rationale tied to persona/JTBD and research-backed operational value; remove decorative rationale if it appears. Leave unrelated untracked Phase 136/137 files untouched.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/style_contract_test.exs</automated>
    <automated>rg -n "transition: all|requestAnimationFrame|setTimeout\\(|setInterval\\(|scroll-behavior: smooth" lib/threadline/operator_surface/style.ex test/threadline/operator_surface/style_contract_test.exs .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md</automated>
  </verify>
  <done>The final inventory dispositions match the CSS source, the contract test passes, and any quality-gate matches are either absent or explicitly acceptable reduced-motion/source-contract references recorded in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory/test contract -> production CSS | Automated contract failures drive any CSS changes. |
| reduced-motion media query -> user preference | Browser preference changes how motion is delivered to reduced-motion users. |
| planning artifacts -> production scope | Documentation can accidentally invite broad redesign if not fenced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-05 | Tampering | `style.ex` motion rules | mitigate | Source-contract tests must pass after any CSS edit and assert allowed tokens/keyframes only. |
| T-141-06 | Denial of Service | operator scanning workflow | mitigate | Preserve no animation for high-frequency Timeline streams and remove gratuitous/repeated motion. |
| T-141-07 | Information Disclosure | motion feedback | accept | Motion does not reveal data beyond already-rendered operator-surface content. |
| T-141-08 | Elevation of Privilege | route/workflow edits | mitigate | This plan must not alter routes, auth, exports, row-history semantics, or LiveView event behavior. |
| T-141-SC | Tampering | npm/pip/cargo installs | mitigate | No installs. Adding an animation/CSS parser package is forbidden without a blocking legitimacy checkpoint. |
</threat_model>

<quality_gates>
- Scope gate: `git diff -- lib/threadline/operator_surface/style.ex test/threadline/operator_surface/style_contract_test.exs .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md` must show only motion contract, inventory, or reduced-motion changes.
- Motion gate: no new `@keyframes`, no new `--tl-motion-*` token names, no JavaScript animation runtime, and no `transition: all`.
- Reduced-motion gate: all transformed animated surfaces either collapse through the blanket or have a targeted final-state reset in the existing reduced-motion block.
- Product gate: no IA, route, content hierarchy, responsive/mobile layout, export, row-history query, or earned-flow semantic changes.
</quality_gates>

<verification>
- `mix test test/threadline/operator_surface/style_contract_test.exs`
- `rg -n "animation:|@keyframes|prefers-reduced-motion|transition:" lib/threadline/operator_surface/style.ex`
- `rg -n "M-[0-9]{2}|keep|neutralize|justify|remove" .planning/phases/141-motion-micro-animation/141-MOTION-INVENTORY.md`
</verification>

<success_criteria>
- `style.ex` contains only governed motion: locked timing tokens, allowed keyframes, justified signature thread-draw uses, and no uninventoryed animations.
- Reduced-motion source coverage applies to every animated/transitioned surface, with targeted resets where the blanket is not enough.
- Inventory rows and source-contract tests match the final CSS exactly.
</success_criteria>

<output>
Create `.planning/phases/141-motion-micro-animation/141-02-SUMMARY.md` when done.
</output>
