---
phase: 174-form-components
plan: "04"
type: execute
wave: 2
depends_on: ["174-01"]
files_modified:
  - lib/threadline/operator_surface/live/export_status_live.ex
  - lib/threadline/operator_surface/live/policy_redaction_live.ex
  - lib/threadline/operator_surface/live/retention_history_live.ex
  - lib/threadline/operator_surface/live/row_history_live.ex
  - lib/threadline/operator_surface/live/stress_live.ex
autonomous: true
requirements:
  - COMP-05
must_haves:
  truths:
    - "LiveView pages use the newly created form components instead of raw inline HTML."
    - "Template duplication for forms is reduced across the operator surface."
    - "Form component replacement introduces no regressions in application functionality."
  artifacts:
    - path: "lib/threadline/operator_surface/live/policy_redaction_live.ex"
      provides: "Forms refactored to use `<UI.field>` / `<UI.input>`"
    - path: "lib/threadline/operator_surface/live/export_status_live.ex"
      provides: "Forms refactored to use `<UI.field>` / `<UI.input>`"
  key_links:
    - from: "LiveView templates"
      to: "lib/threadline/operator_surface/ui.ex"
      via: "Function component calls `<UI.field ... />`"
---

<objective>
Adopt the newly created form components (`<.field>`, `<.input>`) across the final batch of core operator surface LiveView pages to eliminate inline class-soup and consolidate form logic.

Purpose: Complete the reduction of template duplication across the 11 operator-surface LiveView pages and guarantee WAI-ARIA compliance in production flows.
Output: Remaining core LiveView pages (`export_status_live`, `policy_redaction_live`, `retention_history_live`, `row_history_live`, `stress_live`) use structured form components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-form-components/174-CONTEXT.md
@.planning/phases/174-form-components/174-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator Input → Server | User input in forms must be passed to standard LiveView event handlers, retaining existing structural validations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-04 | Spoofing | Form submission | mitigate | Existing `phx-submit` and `phx-change` bindings use LiveView's secure WebSocket channels with built-in token verification. Do not bypass these standard mechanisms. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Adopt form components in settings and specialized LiveViews</name>
  <files>lib/threadline/operator_surface/live/export_status_live.ex, lib/threadline/operator_surface/live/policy_redaction_live.ex, lib/threadline/operator_surface/live/retention_history_live.ex, lib/threadline/operator_surface/live/row_history_live.ex, lib/threadline/operator_surface/live/stress_live.ex</files>
  <action>
    Replace raw inline `<input>`, `<select>`, `<textarea>` tags with `<UI.input>` or `<UI.field>`.
    - In each file, locate any hardcoded form elements, search inputs, filters, or inline `<form>`s.
    - Refactor them to use the new UI primitives `<UI.input>` and `<UI.field>`.
    - Maintain existing `phx-*` bindings, `name`, and `value` fields exactly as they are so application logic is unaffected.
    - Remove manually coded `<label>` and `<div class="tl-field">` wrappers where `<UI.field>` can absorb them.
    - For `stress_live.ex`, ensure it uses the final component forms.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/export_status_live_test.exs test/threadline/operator_surface/live/policy_redaction_live_test.exs test/threadline/operator_surface/live/retention_history_live_test.exs test/threadline/operator_surface/live/row_history_live_test.exs</automated>
  </verify>
  <done>All forms in these files use the new UI primitives and tests pass.</done>
</task>

</tasks>

<verification>
Run `mix test test/threadline/operator_surface/` to confirm no regressions were introduced during template refactoring.
</verification>

<success_criteria>
These core operator surface LiveViews successfully render and operate with the new internal `<.field>` and `<.input>` primitives. Template code duplication is materially reduced.
</success_criteria>

<output>
Create `.planning/phases/174-form-components/174-04-SUMMARY.md` when done
</output>
