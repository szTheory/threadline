---
phase: 105-help-desk-domain-expansion-in-reference-app
plan: 03
type: execute
wave: 3
depends_on:
  - 105-02
files_modified:
  - examples/threadline_phoenix/test/support/help_desk_fixtures.ex
  - examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs
autonomous: true
requirements:
  - DEMO-02
  - DEMO-03
  - DEMO-04
must_haves:
  truths:
    - "DataCase test proves tickets + ticket_replies changes share one AuditTransaction with :ticket_replied_and_closed action"
    - "audit_transactions.meta organization_id matches org UUID string"
    - "internal_note_body appears masked as [REDACTED] in capture payload (not plaintext)"
    - "Hard delete on ticket_reply produces op=delete audit row (field_changes empty)"
    - "mix verify.example passes (all pre-existing + new tests)"
  artifacts:
    - path: examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs
      provides: "database integration proof for DEMO-02/03"
    - path: examples/threadline_phoenix/test/support/help_desk_fixtures.ex
      provides: "org → agent → ticket factory"
  key_links:
    - from: help_desk_audit_test.exs
      to: help_desk.ex
      via: "calls ticket_replied_and_closed with AuditContext"
---

<objective>
Ship proof tests for DEMO-02/03/04 and ROADMAP success criteria 3–5 using DataCase only. Satisfies integration-test requirement without HTTP (D-05e).

Output: Green `mix test` in example app and green `mix verify.example` from repo root.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-help-desk-domain-expansion-in-reference-app/105-CONTEXT.md
@.planning/phases/105-help-desk-domain-expansion-in-reference-app/105-RESEARCH.md
@.planning/phases/105-help-desk-domain-expansion-in-reference-app/105-PATTERNS.md
@examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs
@examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
</context>

<scope_guard>
**ALLOWED:** `examples/threadline_phoenix/test/support/help_desk_fixtures.ex`, `examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs`

**FORBIDDEN:** ConnCase HTTP tests, router edits, repo root `test/`
</scope_guard>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-105-05 | Test asserts plaintext note leaks | high | Assert `data_after` for internal_note_body does NOT contain fixture secret string; DOES contain `[REDACTED]` |
| T-105-06 | Flaky sandbox hides GUC | medium | Use `Sandbox.unboxed_run` for capture assertions; truncate audit tables in cleanup like worker test |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: help_desk_fixtures.ex</name>
  <files>examples/threadline_phoenix/test/support/help_desk_fixtures.ex</files>
  <read_first>
    - examples/threadline_phoenix/test/support/data_case.ex
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk/*.ex
    - .planning/phases/105-help-desk-domain-expansion-in-reference-app/105-CONTEXT.md (D-05c, D-06)
  </read_first>
  <action>
Create `ThreadlinePhoenix.HelpDeskFixtures` (or similar) imported from DataCase tests.

**Functions (minimum):**
- `organization_fixture(attrs \\ %{})` — inserts Organization with unique slug `org-#{unique_integer}`
- `membership_fixture(org, attrs)` — `user_id` default `"user-#{unique_integer}"`, role `"support"` or `"agent"`
- `agent_fixture(org, attrs)` — required `user_id`, optional `display_name`
- `ticket_fixture(org, agent, attrs)` — sets `number` via `attrs[:number]` or `Repo.aggregate(:max, :number) + 1` scoped to org; default status `"open"`
- `audit_context_for_user(user_id)` — returns `%Threadline.Semantics.AuditContext{actor_ref: ActorRef.new!(:user, user_id), correlation_id: "test-corr", request_id: "test-req"}`

Use `ThreadlinePhoenix.Repo` directly. Do not use Sigra.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix compile --warnings-as-errors && test -f test/support/help_desk_fixtures.ex</automated>
  </verify>
  <acceptance_criteria>
    - File `examples/threadline_phoenix/test/support/help_desk_fixtures.ex` exists
    - Defines at least `organization_fixture/1` and `ticket_fixture/3`
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: help_desk_audit_test.exs — multi-table + mask + delete</name>
  <files>examples/threadline_phoenix/test/threadline_phoenix/help_desk_audit_test.exs</files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix/workers/post_touch_worker_test.exs
    - examples/threadline_phoenix/lib/threadline_phoenix/help_desk.ex
    - test/support/help_desk_fixtures.ex
    - .planning/phases/105-help-desk-domain-expansion-in-reference-app/105-CONTEXT.md (D-03, D-04, D-05)
  </read_first>
  <action>
Create `ThreadlinePhoenix.HelpDeskAuditTest`:
- `use ThreadlinePhoenix.DataCase, async: false`
- `import ThreadlinePhoenix.HelpDeskFixtures` (or equivalent)

**Test A — `:ticket_replied_and_closed` multi-table capture**

Inside `Ecto.Adapters.SQL.Sandbox.unboxed_run(Repo, fn -> ... end)`:

1. Setup org, agent (`user_id` `"agent-user-1"`), open ticket.
2. Secret string `note_secret = "super-secret-internal-#{unique_integer}"`.
3. Call `HelpDesk.ticket_replied_and_closed(audit_context, org, ticket, %{body: "public reply", internal_note_body: note_secret}, %{status: "closed", closed_at: DateTime.utc_now(:second)}, [])`.
4. Assert `{:ok, result}` with `audit_transaction_id`.
5. Query all `AuditChange` for that `transaction_id` where `table_name in ["tickets", "ticket_replies"]` — assert count == 2.
6. Assert `AuditTransaction` for id has `meta["organization_id"] == to_string(org.id)`.
7. Assert `actor_ref` type `:user`, id `"agent-user-1"`.
8. Assert `AuditAction` exists with `name == "ticket_replied_and_closed"` linked via `action_id`.
9. Find `ticket_replies` change row — assert `data_after` (or field_changes) for `internal_note_body` contains `"[REDACTED]"` and does NOT contain `note_secret`.

**Test B — hard delete (D-04 posture)**

Either call `HelpDesk.delete_reply/2` if implemented, or `Repo.delete!/1` after setting actor GUC in a transaction with org meta if documented in moduledoc.

Assert new `AuditChange` with `op == "delete"` on `ticket_replies` and empty/minimal `field_changes` per capture contract.

Cleanup: `TRUNCATE` help-desk + audit tables CASCADE inside unboxed_run (pattern from post_touch_worker_test).

**Do NOT** add ConnCase or HTTP tests.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test test/threadline_phoenix/help_desk_audit_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/threadline_phoenix/help_desk_audit_test.exs` exits 0
    - Test file contains `ticket_replied_and_closed`
    - Test file contains `organization_id` meta assertion
    - Test file contains `[REDACTED]` assertion
    - Test file uses `DataCase` not `ConnCase`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Regression — full example suite + verify.example</name>
  <files></files>
  <read_first>
    - examples/threadline_phoenix/test/threadline_phoenix_web/posts_audit_path_test.exs (must still pass)
    - mix.exs aliases at repo root (`verify.example`)
  </read_first>
  <action>
Run full example app test suite and repo-root gate:

```bash
cd examples/threadline_phoenix && mix test
cd /Users/jon/projects/threadline && mix verify.example
```

If failures in pre-existing tests, fix only regressions caused by this phase (likely missing sandbox cleanup). Do not weaken assertions in legacy tests.

Update `examples/threadline_phoenix/README.md` **only if** install section needs one line listing help-desk tables in gen.triggers step — optional, skip if README already says "all audited tables".
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix && mix test && cd /Users/jon/projects/threadline && mix verify.example</automated>
  </verify>
  <acceptance_criteria>
    - `cd examples/threadline_phoenix && mix test` exits 0
    - `mix verify.example` from repo root exits 0
    - `posts_audit_path_test.exs` still present and passing (no deletion)
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd examples/threadline_phoenix && mix test test/threadline_phoenix/help_desk_audit_test.exs --max-failures 1
cd examples/threadline_phoenix && mix test
mix verify.example
```
</verification>

<success_criteria>
- ROADMAP Phase 105 success criteria 3 and 5 satisfied
- DEMO-02/03/04 verified by automated tests
- No `lib/` edits at repository root
</success_criteria>
