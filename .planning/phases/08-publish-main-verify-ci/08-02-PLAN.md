# Plan 08-02: Live CI-02 proof + checklist & SUMMARY reconciliation

**Phase:** 8 — Publish main & verify CI  
**Plan:** 08-02 of 02  
**Status:** Ready to execute  
**Depends on:** 08-01 complete (REPO-03 true)  
**Blocks:** Phase 7 (Hex) confidence on green `main`  
**Requirements covered:** CI-01, CI-02, CI-03  
**Wave:** 2  
**requirements_addressed:** CI-01, CI-02, CI-03  
**files_modified:** `.planning/REQUIREMENTS.md`, `.planning/phases/06-ci-on-github/06-VERIFICATION.md`, `.planning/phases/06-ci-on-github/06-01-SUMMARY.md`, `.planning/phases/06-ci-on-github/06-02-SUMMARY.md`  
**autonomous:** false  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-08-02-01 | Marking CI-02 satisfied from wrong workflow / wrong branch | high | Require `headSha` from `gh run view` to equal `git rev-parse origin/main`; workflow name `ci.yml` / file `.github/workflows/ci.yml`. |
| T-08-02-02 | Checklist updated while Actions still running → false “green” | medium | Only update REQUIREMENTS CI-02 checkbox after all three job conclusions are `success` in the same run. |
| T-08-02-03 | Editing SUMMARY destroys audit trail | low | Use minimal YAML frontmatter edits; retain body text; note amendment in SUMMARY body. |

---

## Goal

Close **CI-02** with maintainer-grade evidence on the **same SHA as `origin/main`**, re-verify **CI-01** / **CI-03** by grep (no regressions), and fix **Phase 6 SUMMARY** frontmatter so **CI-02** is not claimed complete until GitHub proof exists.

---

## Context

- `.planning/phases/06-ci-on-github/06-VERIFICATION.md` — maintainer checklist placeholders.
- `.planning/v1.1-MILESTONE-AUDIT.md` — INT-SUMMARY-VERIFICATION, INT-CI02-SEMANTICS.
- `.planning/REQUIREMENTS.md` — definitions for CI-01–CI-03.

---

## Tasks

### Task 1: CI-01 contract greps (unchanged)

**What to do**

1. Re-run the same greps as plan **06-01** Task 1 on `.github/workflows/ci.yml`.

**Read first**

- `.github/workflows/ci.yml`
- `.planning/phases/06-ci-on-github/06-01-SUMMARY.md`

**Acceptance criteria**

- `grep -qE '^  verify-format:' .github/workflows/ci.yml` exits 0.
- `grep -qE '^  verify-credo:' .github/workflows/ci.yml` exits 0.
- `grep -qE '^  verify-test:' .github/workflows/ci.yml` exits 0.
- `grep -A2 '^  push:' .github/workflows/ci.yml | grep -F 'branches: [main]'` exits 0.
- `grep -A2 '^  pull_request:' .github/workflows/ci.yml | grep -F 'branches: [main]'` exits 0.

**Done condition:** All five succeed; if any fail, fix workflow in a **follow-up** task (out of scope unless trivial one-line restore) and re-grep.

---

### Task 2: CI-02 — Confirm green GitHub Actions run for `origin/main` HEAD

**What to do**

1. `git fetch origin main && git rev-parse origin/main` — record full SHA.
2. Using `gh` (preferred) or the GitHub UI, find the latest **`CI`** workflow (`ci.yml`) run on `main` whose `headSha` matches that SHA.
3. Confirm jobs **`verify-format`**, **`verify-credo`**, **`verify-test`** all **success** for that run.
4. Paste SHA and run URL into `.planning/phases/06-ci-on-github/06-VERIFICATION.md` maintainer sections (replace placeholders).

**Read first**

- `.planning/phases/06-ci-on-github/06-VERIFICATION.md`
- `.planning/phases/06-ci-on-github/06-CONTEXT.md` (D-11 if present — badge vs run URL rule)

**Acceptance criteria**

- File `06-VERIFICATION.md` contains a 40-char hex SHA matching `git rev-parse origin/main` (no literal `(paste` remaining for SHA line).
- File contains an `https://github.com/` Actions run URL (not badge-only).
- `gh run view RUN_ID --json conclusion,headSha,url` shows `conclusion: success` and `headSha` equals `origin/main`.

**Done condition:** Maintainer checklist in `06-VERIFICATION.md` is fully populated for CI-02.

---

### Task 3: CI-03 contributor doc greps

**What to do**

1. Confirm README still has `**CI:**` documentation and link to Actions as in Phase 6 verification.
2. Confirm CONTRIBUTING mentions the three job keys and Actions URL.

**Read first**

- `README.md`
- `CONTRIBUTING.md`
- `.planning/phases/06-ci-on-github/06-02-SUMMARY.md`

**Acceptance criteria**

- `grep -F 'github.com/szTheory/threadline/actions' README.md` exits 0 (or the canonical org/repo path if renamed — must match `origin` URL host/path).
- `grep -F 'verify-format' CONTRIBUTING.md` exits 0.
- `grep -F 'verify-credo' CONTRIBUTING.md` exits 0.
- `grep -F 'verify-test' CONTRIBUTING.md` exits 0.

**Done condition:** All greps pass.

---

### Task 4: REQUIREMENTS.md — checkboxes and traceability

**What to do**

1. Set checklist `[x]` for **REPO-03**, **CI-02** when Task 2 evidence is true (leave **CI-01** / **CI-03** consistent with grep truth — if already `[x]`, keep; if drift, align to evidence).
2. Update the **Traceability** table rows for REPO-03, CI-01, CI-02, CI-03: **Status** → `Done` (or equivalent used in this file) and **Phase** column accurate (Phase 8 closed the gap).

**Read first**

- `.planning/REQUIREMENTS.md`

**Acceptance criteria**

- After edits: `grep '\*\*REPO-03\*\*' .planning/REQUIREMENTS.md` line shows `[x]` when REPO-03 satisfied.
- `grep '\*\*CI-02\*\*' .planning/REQUIREMENTS.md` line shows `[x]` only after Task 2 complete.

**Done condition:** REQUIREMENTS reflect verified v1.1 state.

---

### Task 5: Reconcile Phase 6 SUMMARY frontmatter (CI-02 semantics)

**What to do**

1. **06-01-SUMMARY.md:** Under `requirements-completed`, **remove** `CI-02`. Keep `CI-01`. Add a bullet under `provides` clarifying: “Local `mix ci.all` parity (compile `--warnings-as-errors` before tests) — supports CI-02 reproducibility but is not GitHub CI-02.”
2. **06-02-SUMMARY.md:** Under `requirements-completed`, **remove** `CI-02`. Keep `CI-03`. Add `provides` bullet if needed: “Maintainer checklist *template* for CI-02; live CI-02 closed in Phase 8.”

**Read first**

- `.planning/phases/06-ci-on-github/06-01-SUMMARY.md`
- `.planning/phases/06-ci-on-github/06-02-SUMMARY.md`
- `.planning/v1.1-MILESTONE-AUDIT.md` (integration findings)

**Acceptance criteria**

- `grep -A5 'requirements-completed:' .planning/phases/06-ci-on-github/06-01-SUMMARY.md` block does not list `CI-02`.
- `grep -A5 'requirements-completed:' .planning/phases/06-ci-on-github/06-02-SUMMARY.md` block does not list `CI-02`.

**Done condition:** SUMMARY files no longer contradict `06-VERIFICATION.md` on CI-02 closure.

---

## Verification

1. All Task 1–5 acceptance greps satisfied.
2. `MIX_ENV=test mix ci.all` exits 0 (optional if no application code changed — record skip in SUMMARY if not run).
3. `08-02-SUMMARY.md` lists completed requirements **CI-01, CI-02, CI-03** in frontmatter.

---

## Success criteria

- Live **CI-02** documented on current `origin/main` SHA.
- **CI-01** / **CI-03** evidenced by greps; REQUIREMENTS checklists aligned.
- Phase 6 SUMMARY frontmatter no longer claims premature **CI-02**.
