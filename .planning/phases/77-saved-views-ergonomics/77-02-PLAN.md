---
phase: 77-saved-views-ergonomics
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - lib/threadline/operator_surface/live/timeline_live.ex
  - test/threadline/operator_surface/live/timeline_live_test.exs
autonomous: true
requirements:
  - VIEW-02
must_haves:
  truths:
    - Operators can save their current timeline filter combination
    - Operators can see and click their saved views to apply them
    - Operators only see views owned by their host actor
  artifacts:
    - path: lib/threadline/operator_surface/live/timeline_live.ex
      provides: Saved Views UI, save, and apply events
  key_links:
    - from: lib/threadline/operator_surface/live/timeline_live.ex
      to: lib/threadline/governance/saved_view.ex
      via: Ecto repo insert and query operations
---

<objective>
Implement the Saved Views UI inside `timeline_live.ex` to allow saving and applying filters.

Purpose: Satisfy VIEW-02 to give operators better ergonomics around repetitive search workflows.
Output: Working Saved Views UI toolbar/sidebar in TimelineLive, mapped to `Threadline.Governance.SavedView`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/77-saved-views-ergonomics/77-DISCUSSION.md
@lib/threadline/operator_surface/live/timeline_live.ex
@lib/threadline/governance/saved_view.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load and maintain Saved Views state</name>
  <files>lib/threadline/operator_surface/live/timeline_live.ex, test/threadline/operator_surface/live/timeline_live_test.exs</files>
  <action>
    Update `mount/3` to use the socket's assigned `@threadline_actor_ref` to fetch existing views for the user via `Threadline.Governance.SavedView` ordered by `inserted_at` desc.
    Assign these to `@saved_views` on the socket. Use `socket.assigns.repo` for Ecto queries (e.g., `repo.all`). If `@threadline_actor_ref` is `nil`, `@saved_views` should be an empty list.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/timeline_live_test.exs</automated>
  </verify>
  <done>Socket correctly populates `@saved_views` based on the host actor ref.</done>
</task>

<task type="auto">
  <name>Task 2: Implement UI and Event Handlers</name>
  <files>lib/threadline/operator_surface/live/timeline_live.ex, test/threadline/operator_surface/live/timeline_live_test.exs</files>
  <action>
    1. Add UI components inside `render/1`. You can add a simple "Saved Views" dropdown/select and a "Save View" button near the filter toolbar. The "Save View" should prompt for a `name` or reveal an input field.
    2. Add `"save-view"` event handler: takes the provided name, uses `@threadline_actor_ref` and `@filters_raw`, constructs a `Threadline.Governance.SavedView.changeset`, and inserts it using `repo.insert`. After insert, re-fetch the views and update `@saved_views`.
    3. Add `"apply-view"` event handler: receives the view ID, finds the view from `@saved_views`, and calls `push_patch` to the base path with the query string derived from the saved `filters`. You can reuse `build_canonical_query(saved_view.filters)` for this.
    4. Add `"delete-view"` event handler: deletes the specified view by ID using `repo.delete` (ensure you verify the view exists in `@saved_views` to prevent cross-actor deletion) and updates `@saved_views`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/timeline_live_test.exs</automated>
  </verify>
  <done>Operator can save, apply, and delete views via the UI, fully scoped to their actor_ref.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> LiveView | Operator inputs view name and applies view. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03 | Elevation of Privilege | timeline_live.ex | mitigate | When loading, applying, or deleting views, always use `where: [actor_ref: ^socket.assigns.threadline_actor_ref]` to enforce strict data isolation. Never blindly delete by view ID alone. |
| T-77-04 | Injection | timeline_live.ex | mitigate | `name` field is cast safely by Ecto changeset and escaped automatically by HEEx templates. |
</threat_model>

<verification>
mix test test/threadline/operator_surface/live/timeline_live_test.exs
</verification>

<success_criteria>
Timeline LiveView has a fully functioning Saved Views UI backed by `threadline_saved_views` DB table, filtered exclusively by `actor_ref`.
</success_criteria>

<output>
After completion, create `.planning/phases/77-saved-views-ergonomics/77-02-SUMMARY.md`
</output>
