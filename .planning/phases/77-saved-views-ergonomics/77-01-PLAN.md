---
phase: 77-saved-views-ergonomics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/session_plug.ex
  - test/threadline/operator_surface/session_plug_test.exs
  - lib/threadline/operator_surface/auth.ex
  - test/threadline/operator_surface/auth_test.exs
autonomous: true
requirements:
  - VIEW-01
must_haves:
  truths:
    - Adopters can inject the actor_fn into a session plug
    - LiveView reads the actor from the session cleanly
    - Legacy scope extraction still works for backwards compatibility
  artifacts:
    - path: lib/threadline/operator_surface/session_plug.ex
      provides: Session auto-injection Plug
    - path: lib/threadline/operator_surface/auth.ex
      provides: Session and scope fallback extraction
  key_links:
    - from: lib/threadline/operator_surface/session_plug.ex
      to: lib/threadline/operator_surface/auth.ex
      via: Plug.Conn.Session key "threadline_actor_ref"
---

<objective>
Implement SessionPlug to evaluate `actor_fn` and store the serialized ActorRef in the session, and update `Auth.on_mount/4` to read it.

Purpose: Satisfy VIEW-01 by providing a strict host actor lookup mechanism that works in LiveView without schema intrusion or brittle `conn` mocking.
Output: The `SessionPlug` and updated `Auth` module capable of parsing the actor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/77-saved-views-ergonomics/77-DISCUSSION.md
@lib/threadline/operator_surface/auth.ex
@lib/threadline/semantics/actor_ref.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Threadline.OperatorSurface.SessionPlug</name>
  <files>lib/threadline/operator_surface/session_plug.ex, test/threadline/operator_surface/session_plug_test.exs</files>
  <behavior>
    - Extracts `actor_fn` from opts.
    - If `actor_fn.(conn)` evaluates to an `ActorRef` struct, serializes it via `Threadline.Semantics.ActorRef.to_map/1` -> `Jason.encode!/1` and puts into session under `"threadline_actor_ref"`.
    - If `actor_fn.(conn)` evaluates to `nil` or error, the session is left unchanged.
  </behavior>
  <action>Create a standard Phoenix Plug module (`Threadline.OperatorSurface.SessionPlug`) that executes the user-provided `actor_fn` on the `conn`. Since LiveView mounts do not have access to full `conn` headers, this plug acts as a forwarder that runs in the router pipeline before `threadline_operator_surface`. It caches the `ActorRef` string in the standard `Plug.Conn` session.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/session_plug_test.exs</automated>
  </verify>
  <done>Plug correctly encodes the ActorRef and stores it in the session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Auth.on_mount/4 to extract session actor</name>
  <files>lib/threadline/operator_surface/auth.ex, test/threadline/operator_surface/auth_test.exs</files>
  <behavior>
    - Reads session for `"threadline_actor_ref"`.
    - If present, decodes it via `Jason.decode/1` and parses it via `Threadline.Semantics.ActorRef.from_map/1`, then assigns to socket as `threadline_actor_ref`.
    - If absent or invalid, falls back to the existing legacy scope extraction (`Map.get(scope, :actor_ref) || Map.get(scope, :user_id)`).
  </behavior>
  <action>Update `Threadline.OperatorSurface.Auth.on_mount/4` to read the session value added by Task 1. We must retain backwards compatibility by still extracting it from the `scope` map (returned by `authorize_fn`) if the session key is absent. Ensure `socket.assigns.threadline_actor_ref` is correctly populated with the `ActorRef` struct regardless of the source.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/auth_test.exs</automated>
  </verify>
  <done>Socket is successfully assigned `@threadline_actor_ref` from either session or scope fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter App -> Threadline | `actor_fn` resolves the trusted identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering | session_plug.ex | mitigate | Rely on cryptographically signed standard `Plug.Session` which cannot be modified client-side without invalidating the signature. |
| T-77-02 | Spoofing | auth.ex | mitigate | If `from_map` fails to parse a valid `ActorRef` structure from the decoded session, reject or ignore it rather than assigning a malformed token. |
</threat_model>

<verification>
mix test test/threadline/operator_surface/auth_test.exs test/threadline/operator_surface/session_plug_test.exs
</verification>

<success_criteria>
`SessionPlug` correctly injects `"threadline_actor_ref"` into session, and `Auth.on_mount` correctly extracts it to `@threadline_actor_ref`.
</success_criteria>

<output>
After completion, create `.planning/phases/77-saved-views-ergonomics/77-01-SUMMARY.md`
</output>
