---
phase: 196-forward-only-net-positive-gate-first-proven-iteration
plan: 03
type: execute
wave: 2
depends_on: [196-01]
files_modified:
  - examples/threadline_phoenix/e2e/critic/gate.ts
  - examples/threadline_phoenix/e2e/critic/run.ts
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]
must_haves:
  truths:
    - "The gate re-evaluates ALL blast-radius cells on the 4 blocking lenses and accepts iff the targeted lens improves beyond its noise floor AND no blocking lens regresses beyond −noise on any affected cell; advisory lenses (hierarchy, color_contrast) are scored and printed with an advisory badge but NEVER gate (GATE-01, 196-D1, 196-D2)."
    - "A shared token/primitive edit triggers a blast-radius-aware re-eval of every affected route cell (not just the edited one), derived from the deterministic re-capture diff (GATE-01d, 196-D1, Pattern 2)."
    - "Before accepting, the gate recomputes held-out oracle ρ via `mix critic.measure --source synthetic` and HALTS (non-zero exit) if any blocking lens ρ falls below its `critic_panel.trust_floors` floor — the Goodhart guard (GATE-03, 196-D4)."
    - "MODE-A mechanical fixes (snap-to-token, raise-contrast, replace-shadow) are surfaced as a suggested diff for the human to apply; no automated source rewriter and no structural auto-apply ships (GATE-02, 196-D3)."
  artifacts:
    - "gate.ts full ranking pipeline: multi-cell blast-radius re-eval, N-escalation, advisory reporting"
    - "gate.ts divergence-halt step reading `mix critic.measure --source synthetic`"
    - "gate.ts MODE-A fix-surfacing step (suggested diff, human-applied)"
  key_links:
    - "gate.ts divergence step ↔ critic_panel.trust_floors[lens] (the recorded floor; guarded against silent drop in 196-02)"
    - "blast-radius re-capture diff ↔ the affected route.* cell set fed to scoreCellLens on all 4 blocking lenses"
---

<objective>
Expand the tracer's one-lens/one-cell skeleton (196-01) into the full forward-only ranking gate: a
blast-radius-aware, 4-blocking-lens accept/reject with advisory reporting, a Goodhart divergence-halt against
the held-out synthetic oracle, and MODE-A mechanical-fix surfacing. All LLM work stays local/out of CI
(196-D9); this plan owns `gate.ts` + `run.ts` only, so it runs in parallel with the Elixir guard layer (196-02).

Purpose: turn the proven loop shape into the real GATE-01/GATE-03 decision engine the maintainer runs on a
real page in Wave 4, composing the existing `scoreCellLens` / `runNSamples` / `RankMetrics` primitives
(RESEARCH "Don't Hand-Roll") rather than rebuilding scoring or stats.
Output: the complete `gate.ts` pipeline (blast radius → mechanical floor → 4-lens ranking → advisory report →
divergence halt) with a deterministic `--dry-run` path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-CONTEXT.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md

# The skeleton this plan expands + primitives it composes:
@examples/threadline_phoenix/e2e/critic/gate.ts
@examples/threadline_phoenix/e2e/critic/refute.ts
@examples/threadline_phoenix/e2e/critic/client.ts
@lib/threadline/critic_trust/rank_metrics.ex
@lib/mix/tasks/critic.measure.ex
# critic_panel.trust_floors seeded in 196-01:
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-01-PLAN.md
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `gate.ts` multi-cell blast-radius re-eval | expanded fn | fan-out over affected cells × 4 blocking lenses |
| `gate.ts` advisory reporting | net-new step | hierarchy + color_contrast printed with badge, never gate |
| `gate.ts` divergence-halt step | net-new step | reads `mix critic.measure --source synthetic` ρ vs trust_floors |
| `gate.ts` MODE-A fix-surfacing | net-new step | suggested diff, human-applied; no rewriter |

<tasks>

<task type="auto">
  <name>Task 1: Full 4-lens blast-radius ranking gate + advisory reporting</name>
  <files>examples/threadline_phoenix/e2e/critic/gate.ts, examples/threadline_phoenix/e2e/critic/run.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/critic/gate.ts (the tracer skeleton from 196-01: `blastRadius`, `mechanicalFloor`, `rankReeval`, `verdict`, the `ROUTE_PAGE_TWIN` map)
    - examples/threadline_phoenix/e2e/critic/refute.ts (`scoreCellLens` :216-256; `LENS_DIMENSIONS` :258-266; `testGestaltTwin` verdict shape :309-368)
    - examples/threadline_phoenix/e2e/critic/client.ts (`runNSamples` — N-escalation + IQR + stable flag already implemented; the N=3→7 escalation knob)
    - examples/threadline_phoenix/e2e/critic/run.ts (`PERSONA_LENSES.all` and `ALL_LENSES` :64-87 — the 6 lenses; the blocking 4 vs advisory 2 split)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md (GATE-01 concrete flow steps 2-6; Anti-Patterns: absolute scores forbidden, advisory lenses never gate; R2 rhythm/scroll_cost jitter mitigation)
  </read_first>
  <action>
    Define `BLOCKING_LENSES = ["brand_fidelity","density","typography","rhythm"]` and
    `ADVISORY_LENSES = ["hierarchy","color_contrast"]` in gate.ts (mirroring 196-D2; do not import the
    ledger for this — the panel is frozen). Expand `blastRadius(page)` to run `npm run capture:pages`
    (deterministic, $0) and return every `route.*__dark-*` scorecard whose bytes changed vs the committed/
    prior copy; under `--dry-run`, report the intended affected set without capturing. Expand `rankReeval` to
    fan out over `affectedCells × BLOCKING_LENSES`: for each pair call `scoreCellLens(before)`/`scoreCellLens(after)`
    (reuse, do not rebuild) and apply the accept rule — targeted lens `Δ > noise_floor` on at least the edited
    cell AND every blocking lens `Δ ≥ −noise_floor` on EVERY affected cell (196-D1a/b/d). Use `runNSamples`'
    existing N=3→7 escalation for any unstable cell; an unstable targeted-lens verdict is VOID not PASS (R2).
    For rhythm specifically, revert scorecards whose bytes are unchanged after recapture before scoring, to
    avoid scroll_cost jitter false-regressions (R2 / Pitfall 2). Add an `advisoryReport(affectedCells)` step
    that scores `ADVISORY_LENSES`, prints each finding under an "ADVISORY — verify vs ground truth" badge, and
    NEVER feeds accept/reject (196-D2 / Pitfall 3). Compare only Δ direction vs the per-cell IQR noise floor —
    never an absolute score threshold (Anti-Pattern). Keep `--dry-run` fully wired (prints the plan, bills $0,
    works with no key). Update the run.ts `gate` usage help to document `--n`, `--dry-run`, advisory output.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npm run critic:gate -- --page route.timeline --lens density --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run critic:gate -- --page route.timeline --lens density --dry-run` exits 0 with no key and prints: the blast-radius affected-cell set, the 4 blocking lenses re-evaluated, and the advisory lenses reported separately.
    - `gate.ts` defines exactly `["brand_fidelity","density","typography","rhythm"]` as blocking and `["hierarchy","color_contrast"]` as advisory: `node -e "const fs=require('fs');const s=fs.readFileSync('examples/threadline_phoenix/e2e/critic/gate.ts','utf8');process.exit(/brand_fidelity/.test(s)&&/color_contrast/.test(s)?0:1)"` exits 0.
    - Advisory scoring output is labeled advisory and is not referenced by the accept/reject verdict computation (assert by reading the verdict function: advisory results are not an input).
    - The accept rule compares delta vs a noise floor (no absolute-score threshold constant appears in the verdict path).
    - Re-eval uses `scoreCellLens` and `runNSamples` (not a reimplemented scorer): `grep -Eq "scoreCellLens|runNSamples" examples/threadline_phoenix/e2e/critic/gate.ts`.
  </acceptance_criteria>
  <done>The gate re-scores every blast-radius cell on the 4 blocking lenses with N-escalation, accepts only on targeted-up + no-regress, and reports advisory lenses without letting them gate.</done>
</task>

<task type="auto">
  <name>Task 2: GATE-03 held-out divergence halt + GATE-02 MODE-A fix-surfacing</name>
  <files>examples/threadline_phoenix/e2e/critic/gate.ts</files>
  <read_first>
    - lib/mix/tasks/critic.measure.ex (`mix critic.measure --source synthetic` — recomputes per-lens ρ into `critic_trust`; note it never auto-commits)
    - lib/threadline/critic_trust/rank_metrics.ex (`spearman/2` — the ρ math; do not reimplement)
    - lib/threadline/operator_surface/mechanical_checker.ex (MODE-A fix strings: `snap <metric> <v> -> nearest token` :404, `raise contrast to >= X:1` :278, `replace box-shadow with a --tl-shadow-* token` :330)
    - .planning/design-system-ledger.json (`critic_panel.trust_floors` seeded in 196-01 — the halt floor per blocking lens)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md (GATE-03 divergence metric + halt semantics; A2 tolerance is a maintainer knob; GATE-02 A3 surface-a-diff not a rewriter; Pitfall 4 never optimize the oracle)
  </read_first>
  <action>
    Add a `divergenceHalt()` step to gate.ts that (under a key) shells `mix critic.measure --source synthetic`,
    reads the recomputed `critic_trust[lens].spearman` for the 4 blocking lenses from the ledger, and compares
    each to `critic_panel.trust_floors[lens]`; if ANY blocking lens ρ is below its floor, print
    "loop halted — held-out ρ diverged (<lens>: ρ=<v> < floor=<f>)" and exit non-zero BEFORE any accept
    (196-D4 Goodhart guard). Under `--dry-run`, read the currently recorded ρ and floors and print the pass/fail
    comparison without recomputing (deterministic, $0). Do NOT edit the synthetic set or add route cells to it
    to chase ρ (Pitfall 4 — the oracle is scored for validity ONLY, 196-D4). Add a `surfaceMechanicalFixes(twin)`
    step that runs `MechanicalChecker` over the affected `page.*` twin (via `mix verify.mechanical` output or a
    thin `mix` reporter) and prints any MODE-A `fix:` strings as a suggested unified-diff-style hint for the
    human to apply — this is the GATE-02 auto-apply path for THIS phase: surface-a-diff, human applies, then the
    gate + `verify.mechanical` re-gate it. Do NOT write source files and do NOT add any structural rewrite
    (196-D3 — no un-spiked structural auto-apply). Wire both steps into the verdict ordering: mechanical floor →
    ranking → divergence halt → advisory report → surfaced fixes.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npm run critic:gate -- --page route.timeline --lens brand_fidelity --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Under `--dry-run`, the gate prints the divergence comparison (each blocking lens: recorded ρ vs its `critic_panel.trust_floors` floor) and a HALT/OK verdict, with no API billing and no key required.
    - `gate.ts` reads `critic_panel.trust_floors` from the committed ledger for the halt floor (not a hardcoded number): `grep -q "trust_floors" examples/threadline_phoenix/e2e/critic/gate.ts`.
    - `gate.ts` invokes `mix critic.measure --source synthetic` only on the live (keyed) path, never under `--dry-run`: assert the `System`/spawn call is guarded by the dry-run flag.
    - The fix-surfacing step prints MODE-A fix hints and writes no source files: `grep -Eq "writeFileSync|fs.write" examples/threadline_phoenix/e2e/critic/gate.ts` finds no source-writing in the fix-surfacing path.
    - No route cell is added to `.planning/golden/synthetic-set.json` by the gate (oracle never optimized against).
  </acceptance_criteria>
  <reversibility rating="reversible">Halt threshold is a maintainer knob read from the ledger; tuning it is a one-line ledger change re-gated by 196-02's no-silent-drop guard.</reversibility>
  <done>The gate halts on held-out ρ divergence before accepting, and surfaces mechanical fixes as a human-applied diff with no automated rewriter or structural auto-apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optimized route.* surface → held-out oracle | The two must stay disjoint; divergence between them is the Goodhart signal |
| LLM ranking → accept/reject | Only Δ-vs-noise on blocking lenses may gate; advisory lenses and absolute scores must not |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-196-03-01 | Tampering | held-out oracle (Goodhart) | high | mitigate | Divergence-halt exits non-zero before accept if held-out ρ drops below the recorded floor; oracle never edited by the gate |
| T-196-03-02 | Spoofing | advisory-lens findings | medium | mitigate | Advisory lenses printed with a "verify vs ground truth" badge, never fed to accept/reject (Pitfall 3) |
| T-196-03-03 | Integrity | rhythm/scroll_cost jitter | medium | mitigate | N-escalation for route cells + revert-unchanged-scorecards + Δ-vs-IQR (never absolute) (R2 / Pitfall 2) |
| T-196-03-04 | Elevation | mechanical fix application | high | mitigate | Surface-a-diff only; no source rewriter, no structural auto-apply (whitelist stays empty, 196-D3) |
</threat_model>

<verification>
- `npm run critic:gate -- --page route.timeline --lens brand_fidelity --dry-run` and `--lens density --dry-run` both exit 0 (no key), printing blast radius, 4-lens ranking, advisory report, and divergence comparison.
- `mix ci.all` green (gate.ts is not in any alias — LLM stays local).
</verification>

<success_criteria>
- Full GATE-01 ranking gate: blast-radius-aware, 4 blocking lenses, advisory non-blocking, Δ-vs-noise only.
- GATE-03 divergence halt wired against the held-out oracle floors.
- GATE-02 mechanical fixes surfaced as a human-applied diff; no rewriter, no structural auto-apply.
</success_criteria>

<output>
Create `.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-03-SUMMARY.md` when done.
</output>
