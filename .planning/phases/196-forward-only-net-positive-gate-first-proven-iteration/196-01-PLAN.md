---
phase: 196-forward-only-net-positive-gate-first-proven-iteration
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/e2e/critic/gate.ts
  - examples/threadline_phoenix/e2e/critic/run.ts
  - examples/threadline_phoenix/e2e/package.json
  - .planning/design-system-ledger.json
  - test/threadline/operator_surface/critic_trust_test.exs
autonomous: true
requirements: [GATE-01, GATE-04]
must_haves:
  truths:
    - "Running `npm run critic:gate -- --page route.timeline --lens brand_fidelity --dry-run` wires the full propose→re-eval→guard path end-to-end (blast-radius re-capture diff → deterministic mechanical floor on the page.timeline.happy twin → accept/reject verdict shape) and exits 0 with NO ANTHROPIC_API_KEY set (GATE-01, 196-D1, 196-D9)."
    - "`mix verify.critic_trust` stays green with the new committed `critic_panel` ledger block and the vacuous-safe panel-membership guard clause (GATE-04, 196-D2, 196-D5)."
  artifacts:
    - "examples/threadline_phoenix/e2e/critic/gate.ts — the propose→re-eval→guard orchestrator skeleton (one lens × one cell path)"
    - "run.ts `case \"gate\"` dispatch + `critic:gate` npm script"
    - ".planning/design-system-ledger.json `critic_panel` block (blocking/advisory/trust_floors/oracle_inventory/signoff_ref)"
    - "test/threadline/operator_surface/critic_trust_test.exs panel-membership freeze clause (vacuous-safe)"
  key_links:
    - "gate.ts → Elixir MechanicalChecker floor via the page.* Tier-A twin of the route cell (route.timeline ↔ page.timeline.happy) — resolves the route.* mechanical-jurisdiction gap (Pattern 3 / Pitfall 1)"
    - "critic_panel.blocking ↔ critic_trust[lens].validated flags (each blocking lens must be validated:true; each advisory validated:false)"
---

<objective>
Land the thinnest **production-quality** end-to-end slice of the forward-only gate: a single command that
runs the whole propose→re-evaluate→guard loop for **one blocking lens (brand_fidelity) on one real route
cell (route.timeline)** and returns an accept/reject verdict — proving the architecture closes before any
expansion. This is the tracer; it is written for keeps, not as a prototype.

The slice touches every layer the phase will grow into: the TS orchestrator (`gate.ts`), the existing
LLM re-eval primitive (`scoreCellLens`), the deterministic Elixir mechanical floor (via the `page.*` twin),
the append-only ledger (`critic_panel` baseline), and the CI guard-the-guards test (one vacuous-safe clause).

Purpose: prove the loop wiring end-to-end on the agent's best early-context tokens (GATE-01 shape + GATE-04
guard host), so a wrong architecture is caught after one commit rather than five layers deep.
Output: `gate.ts` skeleton + `critic:gate` command + `critic_panel` ledger block + first panel guard clause.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-CONTEXT.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md

# Existing primitives this tracer composes (do NOT rebuild — RESEARCH "Don't Hand-Roll"):
@examples/threadline_phoenix/e2e/critic/refute.ts
@examples/threadline_phoenix/e2e/critic/run.ts
@lib/threadline/operator_surface/mechanical_checker.ex
@test/threadline/operator_surface/critic_trust_test.exs
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `examples/threadline_phoenix/e2e/critic/gate.ts` | net-new file | orchestrator skeleton: one lens × one cell |
| `run.ts` `case "gate"` | net-new dispatch arm | lazy-import pattern, mirrors `case "validate"` (run.ts:457) |
| `package.json` `critic:gate` script | net-new npm script | `tsx critic/run.ts gate` (mirrors `critic:score`) |
| `design-system-ledger.json` `critic_panel` | net-new ledger block | blocking/advisory/trust_floors/oracle_inventory/signoff_ref |
| `critic_trust_test.exs` panel-membership clause | net-new test clause | frozen `@blocking_panel` / `@advisory_panel`, vacuous-safe |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Thinnest end-to-end gate — one lens × one route cell, dry-run wired</name>
  <files>examples/threadline_phoenix/e2e/critic/gate.ts, examples/threadline_phoenix/e2e/critic/run.ts, examples/threadline_phoenix/e2e/package.json</files>
  <read_first>
    - examples/threadline_phoenix/e2e/critic/run.ts (the subcommand dispatcher at :428-520; the lazy-import `case "validate"` idiom at :457-473; `parseArgs`/flag parsing at :107-160; `--dry-run`/`--page`/`--lens` handling)
    - examples/threadline_phoenix/e2e/critic/refute.ts (`scoreCellLens` at :216-256 — the two-blind-score median+IQR primitive; `testGestaltTwin` at :278-368 — the directional + noise-floor-margin + metamorphic gate shape to mirror; `LENS_DIMENSIONS` at :258-266)
    - examples/threadline_phoenix/e2e/critic/bundle.ts (`loadBundle`, `committedCellIds` — imported by run.ts:30)
    - lib/threadline/operator_surface/mechanical_checker.ex (`run/1` at :92; route/story exclusion at :143-156; MODE-A fix strings at :269-406)
    - examples/threadline_phoenix/e2e/tests/operator-page-capture.spec.ts (`ROUTES` at :84-87; `cellId` at :89-90 → `route.timeline__dark-1280`)
    - examples/threadline_phoenix/e2e/package.json (the `critic:score`/`critic:validate` script pattern)
  </read_first>
  <behavior>
    - `npm run critic:gate -- --page route.timeline --lens brand_fidelity --dry-run` exits 0, prints the gate plan (which cells are in blast radius, which page.* twin the mechanical floor will use, the accept/reject rule), and bills ZERO API calls.
    - Missing ANTHROPIC_API_KEY under `--dry-run` degrades to the dry plan, never throws (196-D9).
    - The blast-radius step is a deterministic re-capture diff: the affected set = `route.*` scorecards whose bytes changed vs the committed/prior copy (Pattern 2); with no edit applied it reports "0 cells changed" cleanly.
    - The mechanical-floor step resolves the route cell to its `page.*` Tier-A twin (`route.timeline` → `page.timeline.happy`) and reports it will assert `mix verify.mechanical` on that committed twin — NOT on the gitignored route.* cell (Pattern 3 / Pitfall 1).
    - Without `--dry-run` and with a key, the accept/reject rule is: targeted lens `Δ > noise_floor(IQR)` AND every blocking lens `Δ ≥ −noise_floor`; an unstable targeted-lens verdict is VOID, not a pass (196-D1).
  </behavior>
  <action>
    Create `gate.ts` exporting `runGate(argv)`. Parse `--page <ledger_id>`, `--lens <blocking_lens>`,
    `--dry-run`, `--force`, `--n <int>` (reuse run.ts's flag-parsing style). Scope this tracer to exactly
    one lens and one page cell (`route.timeline__dark-1280`) — no multi-cell fan-out yet (that is Plan 03).
    Compose the EXISTING primitives per RESEARCH "Don't Hand-Roll": import `scoreCellLens` + `LENS_DIMENSIONS`
    from `refute.ts` for the before/after single-cell blind scores, and reuse the directional + margin +
    metamorphic verdict shape from `testGestaltTwin` (delta vs `polished.iqr` noise floor). Implement four
    ordered steps as separate functions so Plan 03 can expand each: (1) `blastRadius(page)` — deterministic
    re-capture diff over `.planning/scorecards/route.*.json` returning the changed cell list; under dry-run,
    skip `capture:pages` and report the intended diff; (2) `mechanicalFloor(routeCell)` — resolve the route→
    page twin using an explicit `ROUTE_PAGE_TWIN` map (`"route.timeline" -> "page.timeline.happy"`) and report/
    shell `mix verify.mechanical` on that twin, treating a non-zero exit as REJECT (196-D1c, GATE-01); (3)
    `rankReeval(cell, lens)` — under a key, call `scoreCellLens` before/after and apply the accept/reject rule;
    under dry-run, print the rule and skip billing; (4) `verdict()` — combine into ACCEPT/REJECT/VOID and print.
    Add `case "gate":` to run.ts's dispatch switch using the SAME lazy-import + `ERR_MODULE_NOT_FOUND` guard idiom
    as `case "validate"` (run.ts:457-473), importing `./gate.js` and awaiting `runGate(rest)`. Add a
    `"critic:gate": "tsx critic/run.ts gate"` script to e2e `package.json` next to `critic:score`. Update the
    run.ts default-usage help text to list `gate`. Keep the LLM strictly local/out of CI (196-D9): the gate is
    never added to any `mix` alias or `ci.all`.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npm run critic:gate -- --page route.timeline --lens brand_fidelity --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run critic:gate -- --page route.timeline --lens brand_fidelity --dry-run` exits 0 with no API key present in the environment.
    - Dry-run output states the resolved `page.*` twin (`page.timeline.happy`) that the mechanical floor will gate, and names the accept/reject rule (targeted-lens delta beyond noise floor; no blocking-lens regression).
    - `run.ts` dispatch includes a `gate` arm that lazy-imports `./gate.js`; the default-usage help lists `gate`.
    - `gate.ts` imports `scoreCellLens` from `refute.ts` (does NOT reimplement per-cell scoring) — assert by source: `grep -q "scoreCellLens" examples/threadline_phoenix/e2e/critic/gate.ts`.
    - No `mix.exs` alias references `gate`/`critic:gate` (LLM stays out of CI): `grep -Fq "gate" mix.exs` returns no critic-gate wiring.
  </acceptance_criteria>
  <done>The one-lens/one-cell gate runs end-to-end under dry-run without a key, resolving the mechanical floor via the page.* twin and printing a verdict — the loop architecture is proven and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Seed critic_panel ledger baseline + first vacuous-safe GATE-04 guard clause</name>
  <files>.planning/design-system-ledger.json, test/threadline/operator_surface/critic_trust_test.exs</files>
  <read_first>
    - .planning/design-system-ledger.json (top-level keys; `ratchet.signoffs: []`; `critic_trust` per-lens `validated` flags — brand_fidelity/density/typography/rhythm=true, hierarchy/color_contrast=false; `.planning/golden/synthetic-set.json` has 144 items / held_out_ids:[])
    - test/threadline/operator_surface/critic_trust_test.exs (the `@critic_lenses` frozen-constant idiom at :13-20; the "exactly the 6 frozen lenses" clause at :60-68; the `ledger/0` + `synthetic_set/0` helpers at :388-398 — REUSE them)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md (GATE-04 section: the `critic_panel` block schema and the three-clause design; A4 note that trust_floor values are maintainer knobs)
  </read_first>
  <action>
    Add ONE additive top-level `critic_panel` block to `design-system-ledger.json` (append-only — do not
    rewrite existing bytes, 196-D5): `blocking: ["brand_fidelity","density","typography","rhythm"]`,
    `advisory: ["hierarchy","color_contrast"]` (196-D2 panel membership — the recorded human sign-off),
    `trust_floors: {brand_fidelity: 0.85, density: 0.78, typography: 0.72, rhythm: 0.72}` (each = the recorded
    `critic_trust[lens].spearman` minus a ~0.05 tolerance, floored at the 0.70 validation bar — GATE-03 uses
    these as the divergence floor; flag in the SUMMARY that these are the maintainer knob confirmable at the
    PROOF-01 sign-off, RESEARCH A2/A4), `oracle_inventory: {synthetic_item_count: 144, held_out_ids: []}`
    (matches synthetic-set.json now), and `signoff_ref: "196-CONTEXT.md#196-D2"`. Then add ONE new test clause
    to `critic_trust_test.exs`: freeze `@blocking_panel ~w(brand_fidelity density typography rhythm)` and
    `@advisory_panel ~w(hierarchy color_contrast)` as module constants (mirroring `@critic_lenses` at :13),
    and assert `critic_panel["blocking"] == @blocking_panel`, `critic_panel["advisory"] == @advisory_panel`,
    each blocking lens has `critic_trust[lens]["validated"] == true`, and each advisory lens has
    `validated == false`. This clause is the panel-membership freeze (GATE-04): a silent membership change
    fails the frozen-constant equality. It is vacuous-safe because the seeded block already matches the
    frozen constants and the current `critic_trust` flags. Reuse the existing `ledger/0` helper.
  </action>
  <verify>
    <automated>mix verify.critic_trust</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.critic_trust` passes green.
    - `.planning/design-system-ledger.json` contains a `critic_panel` block with `blocking`, `advisory`, `trust_floors`, `oracle_inventory`, and `signoff_ref` keys: `node -e "process.exit(require('./.planning/design-system-ledger.json').critic_panel?0:1)"` exits 0.
    - The new test freezes the blocking and advisory panels as module constants and asserts ledger↔constant equality plus the blocking↔`validated:true` / advisory↔`validated:false` agreement.
    - Pre-existing `critic_trust` bytes are unchanged (append-only): only the additive `critic_panel` key is new — `git diff --stat .planning/design-system-ledger.json` shows an additive change.
  </acceptance_criteria>
  <reversibility rating="costly">The critic_panel baseline becomes an append-only committed contract; changing panel membership or floors later requires a recorded ratchet.signoffs entry (196-D5), so seed values deliberately.</reversibility>
  <done>The critic_panel baseline is committed and the panel-membership freeze clause is green — GATE-04's guard host now has its baseline, vacuous-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → committed ledger | Maintainer-authored JSON is the SSOT CI trusts; the gate must never let LLM output silently rewrite it |
| LLM API → CI | Nondeterministic, paid calls must never enter `ci.all` (196-D9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-196-01-01 | Tampering | `gate.ts` mechanical-floor step | high | mitigate | Floor is gated on the committed `page.*` twin via `mix verify.mechanical`, not the gitignored route.* cell (Pattern 3) — a route "fix" cannot bypass the deterministic block |
| T-196-01-02 | Elevation/Integrity | LLM re-eval | high | mitigate | Gate never enters any `mix` alias or `ci.all`; dry-run runs without a key; CI asserts only the recorded ledger (196-D9) |
| T-196-01-03 | Tampering | `critic_panel` seed | medium | mitigate | Panel-membership freeze clause pins blocking/advisory to frozen constants; a silent change fails `verify.critic_trust` |
| T-196-01-SC | Tampering | npm installs | low | accept | No new packages added (tsx + @anthropic-ai/sdk already devDependencies); Package Legitimacy Audit not required this phase |
</threat_model>

<verification>
- `cd examples/threadline_phoenix/e2e && npm run critic:gate -- --page route.timeline --lens brand_fidelity --dry-run` exits 0 (no key).
- `mix verify.critic_trust` green.
- `mix ci.all` still green (no regression to existing gates).
</verification>

<success_criteria>
- The propose→re-eval→guard loop closes end-to-end for one lens × one cell (dry-run proof).
- `critic_panel` baseline committed; panel-membership guard clause green and vacuous-safe.
- No LLM in CI; no new runtime dependency; ledger edited append-only.
</success_criteria>

<output>
Create `.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-01-SUMMARY.md` when done.
</output>
