---
phase: 196-forward-only-net-positive-gate-first-proven-iteration
plan: 04
type: execute
wave: 3
depends_on: [196-03]
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-page-capture.spec.ts
  - CONTRIBUTING.md
  - mix.exs
  - test/threadline/forward_only_gate_doc_contract_test.exs
autonomous: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "`npm run capture:pages` captures the five weakest-page candidates (timeline, coverage, retention, actor, evidence) as `route.*` cells, each with an existing committed `page.*` Tier-A twin for the mechanical floor (PROOF-01, 196-D7, 196-D8)."
    - "CONTRIBUTING.md documents the forward-only gate as a repeatable maintainer runbook (capture → score → gate → ratify → commit), keeping the critic OUT of ExDoc/hex adopter docs (PROOF-01, RESEARCH A1)."
    - "The stale `Krippendorff α ≥ 0.67` trust-bar line in CONTRIBUTING.md is corrected to the Spearman ρ ≥ 0.70 ranking bar (the Phase-195 validation pivot)."
    - "`mix verify.doc_contract` fails if the runbook's canonical commands drift from the pinned contract (new doc-contract test)."
  artifacts:
    - "operator-page-capture.spec.ts expanded `ROUTES` array (5 candidate pages)"
    - "CONTRIBUTING.md 'Forward-only gate — run one iteration' subsection + route↔page twin table + corrected ρ line"
    - "test/threadline/forward_only_gate_doc_contract_test.exs (new) wired into verify.doc_contract"
  key_links:
    - "each ROUTES entry ↔ its committed page.* twin in mechanical_floors (route.coverage → page.coverage.happy, etc.) — the mechanical floor the gate gates on"
    - "runbook canonical commands ↔ forward_only_gate_doc_contract_test.exs pins (runbook cannot drift silently)"
---

<objective>
Wire the loop end-to-end for the real proof: expand the nascent `route.*` capture lane from just timeline to
the five weakest-page candidates (196-D8), and document the whole propose→re-evaluate→guard loop as a
repeatable **maintainer runbook** in CONTRIBUTING.md (NOT `guides/` — the critic must not leak into
ExDoc/hex adopter docs, RESEARCH A1). Pin the runbook's canonical commands with a doc-contract test so it
cannot drift, and correct the stale Krippendorff-α trust-bar line left over from before the ρ-ranking pivot.

Purpose: PROOF-01's "wired + documented" half — everything the maintainer needs to run one real iteration in
Wave 4, with the route↔page twin mapping made explicit so the deterministic floor actually gates route work.
Output: expanded `ROUTES`, the CONTRIBUTING.md runbook subsection, and its doc-contract pin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-CONTEXT.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md

# What this plan expands / documents:
@examples/threadline_phoenix/e2e/tests/operator-page-capture.spec.ts
@CONTRIBUTING.md
@mix.exs
# The gate command surface being documented (from 196-01/196-03):
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-03-PLAN.md
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `operator-page-capture.spec.ts` expanded `ROUTES` | edited array | timeline + coverage + retention + actor + evidence |
| CONTRIBUTING.md "Forward-only gate — run one iteration" | net-new subsection | capture→score→gate→ratify→commit runbook + twin table |
| CONTRIBUTING.md corrected ρ trust-bar line | edited line (~132) | replaces stale "Krippendorff α ≥ 0.67" |
| `test/threadline/forward_only_gate_doc_contract_test.exs` | net-new test | pins runbook commands; added to `verify.doc_contract` |

<tasks>

<task type="auto">
  <name>Task 1: Expand the route capture lane to the five weakest-page candidates</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-page-capture.spec.ts</files>
  <read_first>
    - examples/threadline_phoenix/e2e/tests/operator-page-capture.spec.ts (`ROUTES` at :84-87; `cellId` at :89-90; the capture loop at :369-372; the login helper at :108-111; `captureCell` at :276-341)
    - .planning/design-system-ledger.json (`mechanical_floors` keys — confirm each candidate route has a committed `page.*` twin: `page.timeline.happy`, `page.coverage.happy`, `page.retention.happy`, `page.actor.happy`, `page.evidence.happy`)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-CONTEXT.md (196-D8 route.* source; the `/audit/*` route paths)
  </read_first>
  <action>
    Extend the `ROUTES` array from `[route.timeline, route.timeline.degraded]` to add the four other weakest-page
    candidates named in 196-D8, each with its real `/audit/*` path: `route.coverage` → `/audit/coverage`,
    `route.retention` → `/audit/retention`, `route.actor` → `/audit/actors` (confirm the actual seeded path in
    the router), `route.evidence` → `/audit/evidence`. Keep the existing `id`/`path`/`degrade?` shape and the
    `cellId(ledgerId, theme, breakpoint)` naming so `critic:score --page route.<x>` and `critic:gate` resolve
    them unchanged. Do NOT add a `page.*`-style twin here — the twins already exist in `mechanical_floors`
    (196-D8 / Pattern 3); this task only grows the live route-capture set. Verify each new route's page renders
    under the existing seeded-login flow (DEMO_SEED_PASSWORD). This capture lane stays local-only (route.* PNGs
    + JSON are gitignored, `.gitignore:56-61`) and never enters CI.
  </action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && npx playwright test --project=route-capture --list operator-page-capture.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ROUTES` contains entries for timeline, coverage, retention, actor, and evidence with correct `/audit/*` paths: `node -e "const fs=require('fs');const s=fs.readFileSync('examples/threadline_phoenix/e2e/tests/operator-page-capture.spec.ts','utf8');['route.coverage','route.retention','route.actor','route.evidence'].forEach(r=>{if(!s.includes(r))process.exit(1)});process.exit(0)"` exits 0.
    - Each new `route.<x>` has a committed `page.<x>.happy` twin in `mechanical_floors`: `node -e "const m=require('./.planning/design-system-ledger.json').mechanical_floors;['timeline','coverage','retention','actor','evidence'].forEach(p=>{if(!m['page.'+p+'.happy'])process.exit(1)});process.exit(0)"` exits 0.
    - `npx playwright test --project=route-capture --list` enumerates the expanded route set without error (does not require running the browser).
    - No `route.*` scorecard/PNG is committed (they stay gitignored): `git status --porcelain .planning/scorecards/route.* 2>/dev/null` shows nothing staged.
  </acceptance_criteria>
  <precondition>The seeded example app (`mix demo.seed`, DB_PORT 5433 via e2e/run-e2e.sh) must be reachable for an actual `capture:pages` run; the `--list` verify does not require it.</precondition>
  <done>The route capture lane covers the five weakest-page candidates, each backed by an existing committed page.* twin for the mechanical floor.</done>
</task>

<task type="auto">
  <name>Task 2: Forward-only gate runbook in CONTRIBUTING.md + ρ-line fix + doc-contract pin</name>
  <files>CONTRIBUTING.md, mix.exs, test/threadline/forward_only_gate_doc_contract_test.exs</files>
  <read_first>
    - CONTRIBUTING.md (the "Local-only critic" section :116-137; the stale trust-bar line at ~:132 "Krippendorff α ≥ 0.67, N ≥ 20, raw agreement ≥ 0.80"; the maintainer synthetic-oracle runbook :139-245 — the new subsection extends this house style)
    - mix.exs (the `verify.doc_contract` alias body listing every `*_doc_contract_test.exs` — add the new file to this exact list)
    - test/threadline/operator_surface_doc_contract_test.exs (an existing doc-contract test to mirror: it asserts canonical command/heading substrings are present in the doc)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md (PROOF-01(b) runbook location = CONTRIBUTING.md not guides/; the "State of the Art" note that α≥0.67 is superseded by ρ≥0.70)
  </read_first>
  <action>
    Add a "Forward-only gate — run one iteration" subsection to CONTRIBUTING.md under the maintainer critic
    section (196-D7 runbook; keep it OUT of `guides/` per RESEARCH A1 so it never reaches ExDoc/hex adopter
    docs). Document the repeatable loop as ordered steps with the canonical commands: `npm run capture:pages`
    (re-capture the route lane), `npm run critic:score -- --page route.<x>` (score the 4 blocking lenses to pick
    the weakest page + lens), `npm run critic:gate -- --page route.<x> --lens <blocking_lens>` (the accept/reject
    gate), then `mix verify.mechanical` on the page.* twin (the deterministic floor), then the ratchet.signoffs
    append + commit-evidence step. Include an explicit **route ↔ page twin mapping table** (route.timeline ↔
    page.timeline.happy, route.coverage ↔ page.coverage.happy, route.retention ↔ page.retention.happy,
    route.actor ↔ page.actor.happy, route.evidence ↔ page.evidence.happy) so the maintainer knows which
    committed twin the floor gates (Pattern 3 / Pitfall 1). State the invariants inline: LLM local-only/out of
    CI, advisory lenses never block (verify vs ground truth), route.* + CRITIQUE.md stay uncommitted. Then FIX
    the stale trust-bar line (~:132): replace the "Krippendorff α ≥ 0.67 … raw agreement ≥ 0.80" description
    with the current bar — Spearman ρ ≥ 0.70 (rank correlation) with n ≥ 20, α/auc/raw_agreement recorded as
    reported-only companions (the Phase-195 pivot; matches `critic_trust_test.exs:102`). Create
    `test/threadline/forward_only_gate_doc_contract_test.exs` mirroring the existing doc-contract idiom: assert
    CONTRIBUTING.md contains the canonical `critic:gate` command, the twin-mapping table, and the ρ ≥ 0.70 bar,
    and refute it still contains the stale "α ≥ 0.67" bar phrasing. Add the new test file to the
    `verify.doc_contract` alias list in mix.exs.
  </action>
  <verify>
    <automated>mix verify.doc_contract</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.doc_contract` passes green including the new `forward_only_gate_doc_contract_test.exs`.
    - CONTRIBUTING.md contains a "Forward-only gate" runbook subsection with the `npm run critic:gate` command and the route↔page twin table: `grep -q "critic:gate" CONTRIBUTING.md && grep -q "page.coverage.happy" CONTRIBUTING.md`.
    - The stale α trust-bar description is gone and the ρ ≥ 0.70 bar is present: the doc-contract test asserts the ρ bar substring is present and the "α ≥ 0.67" bar phrasing is absent.
    - `mix.exs` `verify.doc_contract` alias lists `test/threadline/forward_only_gate_doc_contract_test.exs`: `grep -q "forward_only_gate_doc_contract_test" mix.exs`.
    - The runbook lives in CONTRIBUTING.md only, not under `guides/`: `git diff --name-only guides/ 2>/dev/null` shows no change.
  </acceptance_criteria>
  <done>The forward-only loop is documented as a pinned, drift-guarded maintainer runbook and the stale α trust-bar line is corrected to the ρ-ranking bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer runbook → published hex docs | The critic is dev/maintainer tooling and must never leak into ExDoc adopter docs |
| documented commands → actual gate surface | The runbook must not drift from the real command interface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-196-04-01 | Information Disclosure | runbook placement | medium | mitigate | Runbook lives in CONTRIBUTING.md, never `guides/`; doc-contract test asserts no guides/ change (RESEARCH A1) |
| T-196-04-02 | Tampering | runbook command drift | low | mitigate | `forward_only_gate_doc_contract_test.exs` pins canonical commands; drift fails `verify.doc_contract` |
| T-196-04-03 | Repudiation | stale α trust-bar claim | low | mitigate | Correct the doc to the ρ ≥ 0.70 bar; refute the stale α phrasing in the contract test |
| T-196-04-SC | Tampering | npm installs | low | accept | No new packages; route capture reuses the existing route-capture Playwright project |
</threat_model>

<verification>
- `mix verify.doc_contract` green.
- `npx playwright test --project=route-capture --list operator-page-capture.spec.ts` enumerates the 5-page route set.
- `mix ci.all` green.
</verification>

<success_criteria>
- Route capture lane expanded to the five weakest-page candidates, each twin-backed.
- Forward-only gate documented as a pinned maintainer runbook in CONTRIBUTING.md (not guides/).
- Stale α trust-bar line corrected to ρ ≥ 0.70.
</success_criteria>

<output>
Create `.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-04-SUMMARY.md` when done.
</output>
