---
phase: 196-forward-only-net-positive-gate-first-proven-iteration
plan: 05
type: execute
wave: 4
depends_on: [196-02, 196-03, 196-04]
files_modified:
  - lib/threadline/operator_surface/style.ex
autonomous: false
requirements: [PROOF-01]
user_setup:
  - service: anthropic
    why: "The forward-only gate's LLM re-eval is maintainer-local (196-D9); selecting the weakest page + target lens and snapshotting before-scores requires running the paid critic locally."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys (local shell only; never committed)"
must_haves:
  truths:
    - "The single weakest `/audit` page and its weakest blocking lens are selected data-driven from the live `route.*` panel scores — not assumed (PROOF-01, 196-D7 discretion)."
    - "One real operator-surface presentation improvement is authored on that page (presentation-only: no component API, no capture/query/auth change — invariant), and its `page.*` Tier-A twin still passes `mix verify.mechanical` (PROOF-01, 196-D1c)."
  artifacts:
    - "the before-score snapshot (weakest page + target lens + 4-blocking-lens before-scores) recorded from the local loop"
    - "one real operator-surface presentation edit on the selected weakest page"
  key_links:
    - "selected weakest page ↔ its committed page.* twin (the mechanical floor that must stay green through the edit)"
    - "target lens ↔ the improvement's intent (the edit must move that blocking lens up, not just change pixels)"
---

<objective>
Begin PROOF-01's "first proven iteration": run the forward-only loop on the expanded route panel to pick the
single weakest `/audit` page and its weakest blocking lens (data-driven, 196-D7), then author ONE real
operator-surface presentation improvement targeting that lens — proving the loop drives real UI change, not
just tooling. The deterministic mechanical floor on the page.* twin must stay green through the edit.

Purpose: the LLM ranking that picks the weakest page is maintainer-local and paid (196-D9), so the selection
is a human checkpoint; the executor then authors the edit and re-asserts the deterministic floor. Ratification
of the improvement is Plan 06's required decision checkpoint.
Output: the recorded before-score snapshot + one real presentation edit whose page.* twin passes verify.mechanical.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-CONTEXT.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md

# The runbook + gate the maintainer runs; the mechanical floor the edit must not break:
@CONTRIBUTING.md
@lib/threadline/operator_surface/mechanical_checker.ex
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-04-PLAN.md
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| before-score snapshot | recorded evidence | weakest page + target lens + 4-lens before-scores (from local loop) |
| one operator-surface presentation edit | source change | presentation-only; targets the selected weakest lens |

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Maintainer selects the weakest page + target lens from live route scores</name>
  <what-built>The forward-only gate (196-01/196-03) and its runbook (196-04) are wired; the route capture lane covers timeline/coverage/retention/actor/evidence.</what-built>
  <how-to-verify>
    Run the local loop from the CONTRIBUTING.md "Forward-only gate — run one iteration" runbook with your
    ANTHROPIC_API_KEY set (this is maintainer-local; the executor has no key and CI never runs the LLM — 196-D9):
    1. `cd examples/threadline_phoenix/e2e && npm run capture:pages` (re-capture the 5-page route lane; needs the seeded dev server, DB_PORT 5433 via run-e2e.sh).
    2. `npm run critic:score -- --page route.timeline` … repeat for route.coverage / route.retention / route.actor / route.evidence — score the 4 BLOCKING lenses.
    3. Identify the single lowest-scoring page and its weakest blocking lens.
    4. For that page, verify any advisory-lens (hierarchy / color_contrast) findings against the committed scorecard/DOM before trusting them — they hallucinate specifics (196-D2 / Pitfall 3).
  </how-to-verify>
  <resume-signal>Report: the selected weakest `/audit` page (e.g. `route.coverage`), its target blocking lens (e.g. `density`), the 4-blocking-lens before-scores snapshot, the exact operator-surface source file to edit, and a one-line description of the intended presentation improvement. This becomes the input to Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author one real operator-surface improvement on the selected weakest page</name>
  <files>lib/threadline/operator_surface/style.ex</files>
  <read_first>
    - the operator-surface source file named in the Task-1 resume signal (the selected weakest page's template/component or page-scoped style) — READ it before editing
    - lib/threadline/operator_surface/mechanical_checker.ex (`run/1` at :92; MODE-A conformance rules :269-406 — the edit must not introduce off-grid px / low contrast / non-token shadow on the page.* twin)
    - .planning/design-system-ledger.json (`mechanical_floors` entry for the selected page's twin — the MODE-B ratchet floors the edit must not regress)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-CONTEXT.md (invariants: presentation-only; no public component API; capture/query/auth untouched; brand-token parity green)
  </read_first>
  <action>
    Using the Task-1 resume signal (selected page + target lens + intended improvement), author ONE real
    presentation improvement to that operator-surface page targeting the weakest blocking lens (e.g. tighten
    vertical cadence for rhythm, raise signal-to-chrome for density, sharpen role differentiation for
    typography, or a designed-not-recolored refinement for brand_fidelity). Keep it presentation-only: edit the
    page's template/component or a page-scoped style; do NOT change any public component API, and do NOT touch
    capture / query / auth semantics (196-D7 invariants). Prefer a page-scoped edit over a shared `--tl-*` token
    change unless the target genuinely requires the token (a token edit widens the blast radius — Plan 06's gate
    will re-eval every affected cell). If a MODE-A mechanical fix was surfaced by the gate for this page
    (196-03 fix-surfacing), apply it here as part of the improvement. After editing, run `mix verify.mechanical`
    and confirm the selected page's `page.*` twin still passes (MODE-A hard + MODE-B floor) — a red floor means
    the edit regressed the deterministic bar and must be revised (196-D1c). Do NOT commit any `route.*`
    scorecard or CRITIQUE.md (they stay uncommitted by design).
  </action>
  <verify>
    <automated>mix verify.mechanical</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.mechanical` passes green after the edit (the selected page's `page.*` twin holds MODE-A hard + MODE-B floors).
    - Exactly one operator-surface page's presentation changed; `git diff --name-only` shows only the named operator-surface source file (plus no `route.*`/CRITIQUE.md).
    - No public component API signature changed and no file under the capture/query/auth layers is modified (assert by diff scope).
    - `mix ci.all` stays green (brand-token parity, doc contracts, existing gates unaffected).
  </acceptance_criteria>
  <reversibility rating="reversible">A page-scoped presentation edit is trivially revertible; Plan 06's ratification checkpoint is the real gate before it becomes a proven, signed-off improvement.</reversibility>
  <done>One real presentation improvement is authored on the selected weakest page and its page.* twin still passes the deterministic mechanical floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local LLM selection → executor edit | The weakest-page pick is maintainer/LLM-produced; the executor acts only on the reported, human-verified selection |
| UI edit → deterministic floor | A presentation "improvement" must never silently regress the mechanical/a11y floor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-196-05-01 | Tampering | operator-surface edit vs floor | high | mitigate | `mix verify.mechanical` on the page.* twin must stay green; a MODE-A/MODE-B regression blocks the edit |
| T-196-05-02 | Spoofing | advisory-lens finding drives the edit | medium | mitigate | Advisory findings verified vs committed scorecard/DOM before acting (196-D2 / Pitfall 3) |
| T-196-05-03 | Elevation | edit escapes presentation scope | high | mitigate | Diff-scope check: no component API, no capture/query/auth change (196-D7 invariants) |
</threat_model>

<verification>
- Task-1 selection checkpoint resolved with a concrete page + lens + before-scores.
- `mix verify.mechanical` green after the edit (page.* twin).
- `mix ci.all` green.
</verification>

<success_criteria>
- Weakest page + target lens chosen data-driven from live route scores.
- One real presentation improvement authored, presentation-only, floor still green.
</success_criteria>

<output>
Create `.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-05-SUMMARY.md` when done.
</output>
