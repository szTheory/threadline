---
phase: 196-forward-only-net-positive-gate-first-proven-iteration
plan: 06
type: execute
wave: 5
depends_on: [196-05]
files_modified:
  - .planning/design-system-ledger.json
autonomous: false
requirements: [PROOF-01]
user_setup:
  - service: anthropic
    why: "Re-running the gate on the edited page for after-scores (the ratification evidence) is maintainer-local and paid (196-D9)."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys (local shell only; never committed)"
must_haves:
  truths:
    - "The improvement is human-ratified only if the targeted blocking lens advanced beyond its noise floor AND no blocking lens regressed on any affected cell AND the held-out divergence-halt is clear AND the page.* twin passes verify.mechanical (PROOF-01, 196-D1, 196-D7)."
    - "The evidence trail is committed (source diff + before/after score summary) and, if any bar changed, an append-only `ratchet.signoffs` entry records it; `route.*` scorecards and CRITIQUE.md stay uncommitted (PROOF-01, 196-D5)."
    - "`mix verify.critic_trust` and `mix ci.all` are green after the signoff append — the guard-the-guards layer re-asserts the ledger (GATE-04, 196-D5)."
  artifacts:
    - "an appended `ratchet.signoffs` entry (kind: ratchet_bump | target_change) if a bar changed"
    - "the committed evidence trail: the source diff + a before/after 4-lens score summary"
  key_links:
    - "ratification decision ↔ the ratchet.signoffs entry (the recorded human sign-off; 196-02's no-silent-drop guard requires it for any bar move)"
    - "after-scores ↔ the accept rule (Δ_target > noise AND no blocking regress) — the ratification is not vibes, it is the recorded delta"
---

<objective>
Close PROOF-01's first proven iteration: the maintainer re-runs the gate on the edited page, confirms the
target lens advanced with zero blocking regressions and a clear divergence-halt, and **ratifies** the change
at the required mid-phase human-ratification checkpoint (196-D7). The executor then commits the evidence trail
and appends the append-only `ratchet.signoffs` entry if any bar changed, and re-asserts the deterministic
guards. This is the moment the loop is proven to move a real page, net-positive, forward-only.

Purpose: the ratification is the one-way door of the phase — a committed, signed-off UI improvement — so it is
a `checkpoint:decision`, not an auto-accept (196-D7). The signoff is what makes 196-02's no-silent-drop guard
permit any bar change.
Output: the ratified improvement's committed evidence trail + ratchet.signoffs entry + green guards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-CONTEXT.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md

# The edit being ratified + the guard layer that re-asserts the ledger:
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-05-PLAN.md
@test/threadline/operator_surface/critic_trust_test.exs
@.planning/design-system-ledger.json
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `ratchet.signoffs` appended entry | append-only ledger entry | kind ratchet_bump/target_change; before/after/rationale/evidence_ref (only if a bar changed) |
| committed evidence trail | commit | source diff + before/after 4-lens score summary (route.*/CRITIQUE.md excluded) |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Mid-phase human ratification of the first proven iteration</name>
  <decision>Ratify (accept) or reject the operator-surface improvement authored in Plan 05 as the phase's first proven forward-only iteration.</decision>
  <context>
    Re-run the gate on the edited page locally (ANTHROPIC_API_KEY set; maintainer-local, 196-D9), following the
    CONTRIBUTING.md runbook: `npm run capture:pages` then
    `npm run critic:gate -- --page route.<selected> --lens <target_lens>`. The gate reports: the blast-radius
    affected cells, the 4-blocking-lens before/after deltas, the advisory report, and the held-out
    divergence-halt verdict. The deterministic floor (`mix verify.mechanical` on the page.* twin) is already
    green from Plan 05. Accept ONLY if ALL hold (196-D1): (a) the targeted blocking lens advanced beyond its
    noise floor; (b) NO blocking lens regressed beyond −noise on ANY affected cell; (c) the divergence-halt is
    clear (held-out ρ ≥ trust_floors); (d) the mechanical floor is green. If any advisory finding influenced the
    edit, confirm it was verified against ground truth first (196-D2).
  </context>
  <options>
    <option id="ratify-accept">
      <name>Accept — ratify the improvement</name>
      <pros>Target lens advanced, zero blocking regressions, divergence clear, floor green — the loop is proven to move a real page forward-only.</pros>
      <cons>Commits a real UI change + (if a bar moved) an append-only signoff; the ledger baseline shifts.</cons>
    </option>
    <option id="reject-revise">
      <name>Reject — revise or revert</name>
      <pros>Prevents a marginal or regressing change from landing; the forward-only gate did its job.</pros>
      <cons>Returns to Plan 05 to revise the edit or reselect the target; the proof iteration is not yet closed.</cons>
    </option>
  </options>
  <resume-signal>Select: ratify-accept or reject-revise. On accept, provide the before/after 4-blocking-lens score summary and whether any bar (target_score / minimum_score) changed (so Task 2 can append the correct `ratchet.signoffs` entry). On reject, describe the revision needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit the evidence trail + append the ratchet.signoffs entry</name>
  <files>.planning/design-system-ledger.json</files>
  <read_first>
    - .planning/design-system-ledger.json (`ratchet.signoffs: []` — the append-only sign-off home; `ratchet.minimum_scores`; `critic_panel.trust_floors`)
    - test/threadline/operator_surface/critic_trust_test.exs (the 196-02 no-silent-drop + append-only clauses — the signoff entry must satisfy them: a bar move needs a matching `kind` whose `after` equals the new value)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md (GATE-04 `ratchet.signoffs` entry schema: kind/date/actor/before/after/rationale/evidence_ref)
  </read_first>
  <action>
    Only on an `ratify-accept` decision. Record the before/after 4-blocking-lens score summary from the resume
    signal into the SUMMARY and the runbook evidence section (NOT into any gitignored route.* scorecard). If the
    ratified improvement changed any quality bar (a `minimum_scores` ratchet bump or a `trust_floors` /
    target_score change), append ONE entry to `ratchet.signoffs` with `kind` = `ratchet_bump` or `target_change`,
    `date`, `actor: "maintainer"`, `before`/`after` = the exact values, `rationale` = the one-line improvement
    description, and `evidence_ref` pointing at the commit / SUMMARY (196-D5 append-only). If NO bar changed
    (the improvement advanced the lens without moving a recorded floor), append no signoff — the guards stay
    vacuous-safe and green. Do NOT rewrite any existing `ratchet.signoffs` object or any other ledger bytes
    (append-only). Commit the operator-surface source diff (from Plan 05) plus this ledger change as the evidence
    trail; keep `route.*` scorecards and CRITIQUE.md uncommitted (they are regenerable projections). Re-assert
    the deterministic guards: `mix verify.critic_trust` (the 196-02 clauses accept the new signoff / stay
    vacuous-safe) and `mix ci.all` must both be green.
  </action>
  <verify>
    <automated>mix verify.critic_trust && mix verify.mechanical</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.critic_trust` and `mix verify.mechanical` both pass green.
    - `mix ci.all` is green (the full deterministic pipeline re-asserts the ledger + the improved page's twin).
    - If a bar changed, exactly one new `ratchet.signoffs` entry exists with `kind` in {`ratchet_bump`,`target_change`}, and its `after` equals the new bar value (so the 196-02 no-silent-drop clause is satisfied): `node -e "const s=require('./.planning/design-system-ledger.json').ratchet.signoffs; process.exit(Array.isArray(s)?0:1)"` exits 0.
    - No `route.*` scorecard or `CRITIQUE.md` is committed: `git status --porcelain | grep -E 'route\\.|CRITIQUE.md'` shows nothing staged.
    - Existing ledger bytes are unchanged except the additive signoff (append-only): `git diff` on the ledger shows only an added array element.
  </acceptance_criteria>
  <reversibility rating="costly">An appended `ratchet.signoffs` entry is an append-only committed record; it is not rewritten or deleted — revising later means a new counter-entry, not an edit. The preceding decision checkpoint is the one-way-door gate.</reversibility>
  <done>The ratified improvement's evidence trail is committed, any bar move is recorded by an append-only signoff, and the deterministic guards are green — the first proven forward-only iteration is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human ratification → ledger bar move | Any quality-bar change must be a recorded, append-only sign-off — never silent |
| local after-scores → committed evidence | Only the human-readable score summary + source diff are committed; gitignored route.* projections are not |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-196-06-01 | Repudiation | quality-bar change | critical | mitigate | A bar move requires an append-only `ratchet.signoffs` entry whose `after` matches; 196-02's guard fails otherwise |
| T-196-06-02 | Tampering | ledger append | high | mitigate | Only an additive array element is written; `git diff` verified additive; existing bytes untouched |
| T-196-06-03 | Spoofing | ratify without evidence | high | mitigate | Ratification requires the recorded before/after delta + clear divergence-halt + green floor, not subjective approval |
</threat_model>

<verification>
- Decision checkpoint resolved (ratify-accept or reject-revise).
- `mix verify.critic_trust && mix verify.mechanical` green; `mix ci.all` green.
- Evidence trail committed; route.*/CRITIQUE.md excluded; any bar move carries a signoff.
</verification>

<success_criteria>
- One real, human-ratified improvement lands: target lens advanced, zero blocking regressions, committed evidence trail.
- Any quality-bar change recorded by an append-only signoff; guards green (GATE-04 re-asserts).
- The forward-only loop is proven end-to-end on a real page.
</success_criteria>

<output>
Create `.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-06-SUMMARY.md` when done.
</output>
