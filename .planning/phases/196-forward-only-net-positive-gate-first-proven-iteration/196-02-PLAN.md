---
phase: 196-forward-only-net-positive-gate-first-proven-iteration
plan: 02
type: execute
wave: 2
depends_on: [196-01]
files_modified:
  - test/threadline/operator_surface/critic_trust_test.exs
  - .planning/design-system-ledger.json
autonomous: true
requirements: [GATE-02, GATE-04, GATE-05]
must_haves:
  truths:
    - "`mix verify.critic_trust` fails (red) if any `critic_panel.trust_floors[lens]` or `ratchet.minimum_scores[id]` drops below its committed baseline WITHOUT a matching `ratchet.signoffs` entry — a silent target/floor drop is blocked (GATE-04, 196-D5)."
    - "`mix verify.critic_trust` fails if the synthetic oracle inventory shrinks below `critic_panel.oracle_inventory.synthetic_item_count` or a recorded held_out_id disappears WITHOUT a `fixture_removal` signoff (GATE-04, 196-D4)."
    - "`mechanical_auto_apply.structural_whitelist` is asserted empty unless a `structural_whitelist_add` signoff exists; every `screenshot_allowlist.ci` entry requires a fresh clean `semantic_guard_stamp` resolving to a committed page.* scorecard (GATE-02, GATE-05, 196-D3, 196-D6)."
  artifacts:
    - "critic_trust_test.exs: no-silent-target-drop clause, no-fixture-removal clause, structural-whitelist clause, semantic-stamp clause (all vacuous-safe)"
    - ".planning/design-system-ledger.json `mechanical_auto_apply.structural_whitelist: []` block"
    - ".planning/design-system-ledger.json `semantic_guard_stamp` on each screenshot_allowlist.ci entry"
  key_links:
    - "ratchet.signoffs (append-only) ↔ every downward floor/target move and every whitelist/fixture change — the guard requires a matching signoff kind"
    - "semantic_guard_stamp.scorecard_ref ↔ a committed page.* scorecard whose MechanicalChecker verdict recomputes clean (never tied to a gitignored route.* cell — Pitfall/GATE-05 landmine)"
---

<objective>
Land the phase's central **deterministic deliverable**: the guard-the-guards layer that makes silent
quality-bar tampering impossible in CI. Extend `critic_trust_test.exs` (already in `ci.all` before
`verify.mechanical`) with four additive, pure-Elixir, vacuous-safe clauses backing the GATE-04 append-only
sign-off model (196-D5), the GATE-02 empty structural whitelist (196-D3), and the GATE-05 semantic-guard
stamp precondition on screenshot refresh (196-D6). No browser, no network, no LLM — CI stays deterministic.

Purpose: Pitfall 5 — today a floor drop or fixture deletion slips through green because no guard checks it.
This plan closes that gap. It is the "guard-the-guards" that lets the LLM stay advisory/local while the
committed ledger is what CI enforces.
Output: three ledger-tamper guards + one whitelist guard + one screenshot-stamp guard, all vacuous-safe
until Wave-4 baseline data lands, plus the two additive ledger blocks they read.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-CONTEXT.md
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md

# The guard host + ledger this plan extends:
@test/threadline/operator_surface/critic_trust_test.exs
@lib/threadline/operator_surface/mechanical_checker.ex
# critic_panel baseline seeded in 196-01:
@.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-01-PLAN.md
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `critic_trust_test.exs` no-silent-target-drop clause | net-new test clause | floors/minimum_scores ≥ baseline unless signed off |
| `critic_trust_test.exs` no-fixture-removal clause | net-new test clause | synthetic item count + held_out_ids non-shrinking unless signed off |
| `critic_trust_test.exs` structural-whitelist clause | net-new test clause | GATE-02: whitelist empty unless `structural_whitelist_add` signoff |
| `critic_trust_test.exs` semantic-stamp clause | net-new test clause | GATE-05: refresh requires fresh clean stamp over a committed page.* twin |
| `design-system-ledger.json` `mechanical_auto_apply.structural_whitelist: []` | net-new ledger block | GATE-02 empty whitelist |
| `design-system-ledger.json` `semantic_guard_stamp` per allowlist.ci entry | net-new ledger data | GATE-05 stamp |

<tasks>

<task type="auto">
  <name>Task 1: GATE-04 append-only guards — no silent target/floor drop, no fixture/oracle removal</name>
  <files>test/threadline/operator_surface/critic_trust_test.exs</files>
  <read_first>
    - test/threadline/operator_surface/critic_trust_test.exs (the frozen-constant + subset assertion idioms at :60-142; the held-out disjointness clause at :281-292; the `ledger/0`, `golden_set/0`, `synthetic_set/0` helpers at :388-398 — REUSE)
    - .planning/design-system-ledger.json (`ratchet.minimum_scores` map; `ratchet.signoffs: []`; the `critic_panel.trust_floors` + `critic_panel.oracle_inventory` blocks seeded by 196-01)
    - .planning/golden/synthetic-set.json (144 items; `held_out_ids: []`)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md (GATE-04 clauses 2 & 3 + the `ratchet.signoffs` entry schema with `kind`/`before`/`after`/`evidence_ref`)
  </read_first>
  <action>
    Add a frozen baseline-floors module attribute (`@baseline_trust_floors` mirroring the seeded
    `critic_panel.trust_floors`, and `@baseline_minimum_scores_floor` capturing the committed
    `ratchet.minimum_scores` snapshot) and a helper `signoff_records_drop?(signoffs, kind, id, new_value)`
    that returns true iff some `ratchet.signoffs` entry has that `kind` and its `after` equals the current
    value. Then add TWO clauses. Clause A (no silent target/floor drop, 196-D5): for each blocking lens assert
    `critic_panel.trust_floors[lens] >= @baseline_trust_floors[lens]` UNLESS a `target_change`/`ratchet_bump`
    signoff records that exact drop; likewise for each `ratchet.minimum_scores` id vs its committed baseline.
    Clause B (no fixture/oracle removal, 196-D4): assert `synthetic-set.json` item count
    `>= critic_panel.oracle_inventory.synthetic_item_count` AND every id in
    `critic_panel.oracle_inventory.held_out_ids` is still present in the synthetic set's `held_out_ids`,
    UNLESS a `fixture_removal` signoff records it. Both clauses are pure JSON reads + File.exists? (no browser/
    LLM), matching the existing test style. Both are vacuous-safe: with signoffs `[]` and current values equal
    to the frozen baseline, the `>=` assertions hold and no signoff is required. Add an append-only sanity
    assertion: every object currently in `ratchet.signoffs` (pin the known-good set, currently empty) is still
    present in the array — signoffs are never rewritten or dropped.
  </action>
  <verify>
    <automated>mix verify.critic_trust</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.critic_trust` passes green on the current (baseline == current, signoffs empty) ledger.
    - Temporarily lowering any `critic_panel.trust_floors` value below its baseline with no matching signoff makes `mix verify.critic_trust` FAIL (demonstrate in the SUMMARY via a scratch edit reverted before commit).
    - Reducing `synthetic-set.json` item count below the recorded `oracle_inventory.synthetic_item_count` with no `fixture_removal` signoff makes the test FAIL.
    - Clauses read only committed JSON files (no `System.cmd`, no HTTP, no LLM): `grep -Eq "System.cmd|:httpc|HTTPoison" test/threadline/operator_surface/critic_trust_test.exs` returns no new network/shell calls introduced by this task.
  </acceptance_criteria>
  <done>A silent floor drop, target drop, or oracle-item removal turns `verify.critic_trust` red; the current honest ledger stays green (vacuous-safe).</done>
</task>

<task type="auto">
  <name>Task 2: GATE-02 empty structural whitelist + GATE-05 semantic-guard stamp precondition</name>
  <files>.planning/design-system-ledger.json, test/threadline/operator_surface/critic_trust_test.exs</files>
  <read_first>
    - .planning/design-system-ledger.json (`screenshot_allowlist.ci` entries — 3 present, shape `{baseline_ref, ledger_id, story_id, theme, viewport}`; `mechanical_floors` keys include `page.home.happy`/`page.timeline.happy` etc.)
    - lib/threadline/operator_surface/mechanical_checker.ex (`run/1` at :92; `measure_mode_b/1` at :419; route/story exclusion at :143-156 — the stamp MUST resolve to a committed `page.*` scorecard, never a route.* cell)
    - test/threadline/operator_surface/critic_trust_test.exs (the "separation of concerns" File.exists? guard at :332-346; the `@scorecards_dir` constant at :8)
    - .planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-RESEARCH.md (GATE-02 empty-whitelist block; GATE-05 `semantic_guard_stamp` schema + the landmine: tie the stamp to the committed page.* twin, not the gitignored route.* cell)
  </read_first>
  <action>
    Ledger (append-only, 196-D3/196-D6): add `mechanical_auto_apply: {structural_whitelist: []}` (the narrow
    low-risk-structural whitelist starts EMPTY and only grows via spike + signoff), and add a
    `semantic_guard_stamp` object to each existing `screenshot_allowlist.ci` entry with fields
    `mechanical_ok`, `blocking_panel_no_regress`, `scorecard_ref` (the entry's `ledger_id`, which is a
    committed `page.*` id such as `page.home.happy`), `stamped_at`, `evidence_ref`. Seed each stamp with
    `mechanical_ok: true` / `blocking_panel_no_regress: true` referencing the already-passing committed
    scorecard (these baselines already pass `verify.mechanical`, so the stamp is honest at seed time).
    Then add TWO test clauses to `critic_trust_test.exs`. Clause C (GATE-02): assert
    `mechanical_auto_apply.structural_whitelist == []` UNLESS a `structural_whitelist_add` entry exists in
    `ratchet.signoffs` (no un-spiked structural auto-apply ships this phase — 196-D3). Clause D (GATE-05):
    for every `screenshot_allowlist.ci` entry, assert it has a `semantic_guard_stamp` whose `scorecard_ref`
    resolves to an existing committed scorecard under `@scorecards_dir` (File.exists?) AND whose
    `mechanical_ok == true` AND `blocking_panel_no_regress == true` — a baseline refresh therefore cannot land
    without a fresh clean semantic stamp over the page.* twin; the raw pixel delta never accepts/rejects alone
    (196-D6). Keep the pixel comparison itself in the advisory Playwright/Tier-C lane (out of scope here).
    All clauses are pure JSON + File.exists? — deterministic, CI-safe.
  </action>
  <verify>
    <automated>mix verify.critic_trust</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.critic_trust` passes green with the seeded `mechanical_auto_apply.structural_whitelist: []` and stamped allowlist entries.
    - Adding a non-empty entry to `structural_whitelist` with no `structural_whitelist_add` signoff makes the test FAIL (demonstrate + revert in SUMMARY).
    - Removing a `semantic_guard_stamp` from any `screenshot_allowlist.ci` entry, or pointing its `scorecard_ref` at a non-existent scorecard, makes the test FAIL.
    - Every seeded `semantic_guard_stamp.scorecard_ref` is a `page.*` id (never `route.*`): `node -e "const c=require('./.planning/design-system-ledger.json').screenshot_allowlist.ci; process.exit(c.every(e=>e.semantic_guard_stamp && /^page\./.test(e.semantic_guard_stamp.scorecard_ref))?0:1)"` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">The empty structural whitelist and the stamp schema become committed ledger contracts; expanding the whitelist or refreshing a baseline later requires a recorded signoff (196-D3/196-D6).</reversibility>
  <done>The structural whitelist is provably empty-unless-signed-off and every screenshot baseline is gated behind a fresh clean semantic stamp — GATE-02 and GATE-05 are enforced deterministically in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed ledger → CI verdict | The ledger JSON is the only thing CI trusts; a tamper here must fail a deterministic test |
| screenshot baseline → quality bar | A pixel refresh must never become the quality bar without semantic guards having passed first |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-196-02-01 | Tampering | `ratchet.minimum_scores` / `critic_panel.trust_floors` | critical | mitigate | No-silent-drop clause requires a matching `target_change`/`ratchet_bump` signoff for any downward move |
| T-196-02-02 | Repudiation | synthetic oracle inventory | high | mitigate | No-fixture-removal clause requires a `fixture_removal` signoff; append-only signoffs pin provenance |
| T-196-02-03 | Elevation | structural auto-apply whitelist | high | mitigate | Whitelist asserted empty unless a `structural_whitelist_add` signoff exists (no un-spiked rewriter ships) |
| T-196-02-04 | Tampering | screenshot baseline refresh | medium | mitigate | Semantic-stamp clause ties every allowlist entry to a fresh clean MechanicalChecker verdict over the committed page.* twin |
</threat_model>

<verification>
- `mix verify.critic_trust` green (all four new clauses vacuous-safe on the honest ledger).
- `mix ci.all` green.
- Scratch tamper demonstrations (floor drop, item removal, whitelist add, stamp removal) each turn the guard red, then reverted — recorded in SUMMARY.
</verification>

<success_criteria>
- Silent target/floor drops, oracle-item removal, un-signed structural whitelist growth, and un-stamped baseline refresh are each blocked by a deterministic clause.
- All clauses pure-Elixir over committed JSON; no LLM/browser/network; CI stays deterministic (196-D9).
- Ledger edits are additive/append-only.
</success_criteria>

<output>
Create `.planning/phases/196-forward-only-net-positive-gate-first-proven-iteration/196-02-SUMMARY.md` when done.
</output>
