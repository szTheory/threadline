---
context: phase
phase: 196-forward-only-net-positive-gate-first-proven-iteration
task: 1
total_tasks: 2
status: blocked
last_updated: 2026-07-29T03:29:52.462Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure THIS session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **worktree base divergence** — `origin/main` is 514 commits behind local HEAD, and this gsd-tools build has NO `worktree.base-check` verb, so the #683 auto-degrade never fires. `isolation="worktree"` executors would fork off `fresh`/origin/main (missing ~185 phases incl. all of 196). MITIGATION: run executors sequentially on the main tree (do not pass `isolation="worktree"`), OR set `worktree.baseRef:"head"` in `.claude/settings.local.json` BEFORE any worktree dispatch.
- [ ] CONSTRAINT: **GSD state.* handlers corrupt STATE.md progress** — `state.begin-phase` this session regressed `stopped_at` to a stale value and miscomputed the plan counts (12/11 → 17/10). MITIGATION: the orchestrator is the SOLE writer of STATE.md/ROADMAP.md; executors are told not to touch them; after ANY `state.*` call, re-read the frontmatter and hand-correct.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| worktree-stale-base | `isolation="worktree"` forks off origin/main (514 behind HEAD); base-check guard absent in this gsd-tools build → catastrophically stale worktree | blocking | Execute sequentially on main tree (no `isolation="worktree"`); or set `worktree.baseRef:"head"` before any worktree dispatch |
| state-handler-progress-corruption | `state.begin-phase` / `state.advance-plan` / `update-progress` miscompute STATE.md's bespoke progress block (observed: stopped_at regressed, counts flipped) | blocking | Orchestrator is sole writer of STATE/ROADMAP; hand-correct frontmatter after any `state.*` call; executors forbidden from writing tracking files |
| checkpoint-auto-resolve-fakes-proof | With `auto_advance=true`, the 196-06 decision checkpoint would auto-accept, ratifying a PROOF-01 iteration that was never actually run (T-196-06-03 spoofing) | advisory | Present BOTH PROOF-01 checkpoints (196-05 human-action, 196-06 decision) to the maintainer; never auto-ratify without real before/after scores |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 196 is PAUSED at the PROOF-01 first-iteration checkpoint. 4/6 plans complete (Waves 1-3 = the entire gate machinery, ~85% of the phase). Execution reached 196-05 Task 1, a **blocking `checkpoint:human-action`**: the maintainer must run the paid forward-only LLM loop locally to pick the single weakest `/audit` page + its weakest blocking lens. No agent/CI has an API key ([196-D9]), so this cannot be automated. Everything is committed; only the pre-existing `.planning/CRITIQUE.md` is uncommitted (not part of this phase). ROADMAP shows 4/6 In Progress.
</current_state>

<completed_work>

Completed Plans (Waves 1-3):
- 196-01 (tracer): one-lens×one-cell forward-only gate `gate.ts` + `critic:gate` wired + `critic_panel` ledger baseline + first GATE-04 clause — Done (`90d92d78`)
- 196-02 (guard-the-guards): GATE-04 no-silent-drop / no-oracle-removal + GATE-02 empty structural whitelist + GATE-05 semantic-guard stamp, all deterministic (LLM out of CI) — Done (`80c74c44`)
- 196-03 (full gate): 7-step engine — blast radius → mechanical floor → 4-lens ranking → GATE-03 divergence halt → advisory → MODE-A fix-surfacing → verdict — Done (`d1110a53`)
- 196-04 (wire loop): route lane → 5 weakest-page candidates + CONTRIBUTING.md runbook + Krippendorff α→Spearman ρ≥0.70 fix + `forward_only_gate_doc_contract_test` — Done (`a4037e8c`)

Orchestrator-level post-merge gates (all green): `compile --warnings-as-errors`, `verify.critic_trust` 22/0, `verify.mechanical` 18/0, `forward_only_gate_doc_contract` 6/0.
</completed_work>

<remaining_work>

- 196-05 Task 1 (BLOCKED, human-action): maintainer runs the local loop → reports {weakest page, target lens, 4-lens before-scores, source file to edit, one-line improvement}.
- 196-05 Task 2 (auto): author ONE presentation improvement on the selected page; `page.*` twin stays green under `verify.mechanical`; diff-scoped to one operator-surface file (no component API / capture / query / auth change).
- 196-06 Task 1 (decision checkpoint): maintainer re-runs the gate, ratifies accept/reject (accept only if target lens ↑ beyond noise, no blocking regression on any affected cell, divergence-halt clear, floor green).
- 196-06 Task 2 (auto): commit evidence trail (source diff + before/after summary; route.*/CRITIQUE.md excluded) + append-only `ratchet.signoffs` entry IF a bar moved; re-assert `verify.critic_trust` + `mix ci.all`. Then phase.complete + verifier.
</remaining_work>

<decisions_made>

- Sequential-on-main instead of worktree isolation (stale-base risk — see anti-patterns).
- Applied confirmed `search_path` fix to `threadline_test` (cleared ~81 phantom failures; 11 genuine pre-existing failures remain, 0 introduced).
- Orchestrator is sole writer of STATE/ROADMAP (state-handler corruption — see anti-patterns).
- Did NOT auto-advance the PROOF-01 checkpoints despite `auto_advance=true` (integrity — see anti-patterns).
</decisions_made>

<blockers>
- 196-05 Task 1: needs the maintainer's paid LOCAL forward-only loop (ANTHROPIC_API_KEY + seeded dev server, DB_PORT 5433 via `run-e2e.sh`). No workaround — this is the phase's designed human gate. Runbook verified known-good this session (gate dry-run ran the full pipeline; all 4 ρ floors clear).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable resume state (completed/remaining tasks, decisions, next_action).
2. `.planning/STATE.md` — the "Awaiting (PROOF-01 first-iteration checkpoint)" block lists exactly what the maintainer reports back.
3. `.planning/phases/196-*/196-05-PLAN.md` and `196-06-PLAN.md` — the two checkpoint plans (Task types, acceptance criteria, threat models).
4. `CONTRIBUTING.md` → "Forward-only gate — run one iteration" — the maintainer runbook (route↔page twin table, accept rule).
5. `.planning/phases/196-*/196-CONTEXT.md` — locked decisions [196-D1..D9] (esp. D1 accept-rule, D2 advisory-hallucination, D7 invariants, D9 maintainer-local LLM).

## Critical Anti-Patterns (do NOT repeat these)
- worktree-stale-base → run executors sequentially on main tree (no `isolation="worktree"`).
- state.* progress corruption → orchestrator sole-writes STATE/ROADMAP + hand-corrects after any `state.*` call.
- auto-ratifying the PROOF-01 decision → always present 196-06 to the maintainer with real before/after scores.

## Infrastructure State
- `threadline_test` Postgres: `search_path` now includes `threadline` (fix applied this session; persists at the DB level).
- No background servers/watchers running from this session. The maintainer must start the seeded Phoenix dev server (DB_PORT 5433 via `examples/threadline_phoenix/e2e/run-e2e.sh`) for `capture:pages`.
- Reusable executor prompt template (used for 196-01..04): sequential-on-main + "orchestrator owns STATE/ROADMAP" + test-env note. Reuse verbatim for 196-05 Task 2 / 196-06 Task 2.

<context>
The forward-only gate is fully built and self-consistent; what remains is the human-in-the-loop PROOF — one real page improvement, ratified with genuine before/after LLM scores. This is deliberately a human gate, not an automation gap. When the maintainer supplies the selection, the remaining work is small: one presentation edit + a mechanical-floor recheck + a ratification + an append-only ledger signoff. Keep the critic OUT of `mix ci.all` ([196-D9]) — the only Elixir wiring is the deterministic guards.
</context>

<next_action>
Start with: wait for the maintainer's 196-05 Task-1 reply {weakest page, target lens, before-scores, source file, improvement}. Then spawn a gsd-executor (sequential-on-main; orchestrator-owns-STATE/ROADMAP template) for 196-05 Task 2 to author the edit and confirm `mix verify.mechanical` stays green on the selected page's `page.*` twin. Do NOT re-run `state.begin-phase`; do NOT use worktree isolation.
</next_action>
