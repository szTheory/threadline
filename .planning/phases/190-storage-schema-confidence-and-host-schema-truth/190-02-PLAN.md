---
phase: 190-storage-schema-confidence-and-host-schema-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/capture/audit_transaction.ex
  - lib/threadline/capture/audit_change.ex
  - lib/threadline/semantics/audit_action.ex
  - lib/threadline/governance/evidence_record.ex
  - lib/threadline/governance/export_job.ex
  - lib/threadline/governance/retention_run.ex
  - lib/threadline/governance/saved_view.ex
  - test/support/data_case.ex
  - test/support/storage_schema_case.ex
  - test/threadline/storage_schema_prefix_contract_test.exs
autonomous: true
requirements:
  - SCHEMA-02
must_haves:
  truths:
    - "D-190-07: Threadline-owned Ecto schemas do not force the default storage prefix."
    - "D-190-08/D-190-10: source and test helpers make omitted prefix plumbing visible."
    - "D-190-06: test support can exercise per-call storage_schema overrides over app config and default."
  artifacts:
    - path: "test/threadline/storage_schema_prefix_contract_test.exs"
      provides: "static owned-schema prefix scan and representative prefix proof"
    - path: "test/support/storage_schema_case.ex"
      provides: "helpers for explicit Threadline storage prefixes and dual-schema cleanup"
  key_links:
    - from: "owned schema modules"
      to: "Repo operations in later plans"
      via: "schemas no longer declare fixed default prefix"
      pattern: "__schema__\\(:prefix\\)"
---

<objective>
Remove fixed Threadline-owned Ecto schema prefixes and create test support that makes storage-prefix use explicit.

Purpose: SCHEMA-02 cannot be trusted while owned schemas can silently override Repo prefix options with `threadline`.
Output: Prefix-free owned schema modules, static contracts against reintroduction, and storage-schema-aware test helpers for later implementation plans.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-CONTEXT.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-RESEARCH.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-PATTERNS.md
@lib/threadline/capture/audit_transaction.ex
@lib/threadline/capture/audit_change.ex
@lib/threadline/semantics/audit_action.ex
@lib/threadline/governance/evidence_record.ex
@lib/threadline/governance/export_job.ex
@lib/threadline/governance/retention_run.ex
@lib/threadline/governance/saved_view.ex
@test/support/data_case.ex
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | phase-190 | Make custom storage_schema Ecto behavior truthful | 190-02 | COVERED | This plan removes the fixed-prefix structural risk.
REQ | SCHEMA-02 | Configurable storage schemas do not silently hit hardcoded threadline | 190-02 | COVERED | Fixed owned prefixes are removed and test-locked.
RESEARCH | owned-prefix-removal | Remove fixed prefixes from seven owned Ecto schemas | 190-02 | COVERED | Applies Ecto prefix-precedence finding.
RESEARCH | test-support | Existing unprefixed fixtures must become explicit after prefix removal | 190-02 | COVERED | DataCase/helper updates happen with prefix removal.
CONTEXT | D-190-06 | storage_schema resolution order | 190-02 | COVERED | Test helpers exercise per-call and configured storage.
CONTEXT | D-190-07 | remove fixed owned schema prefixes | 190-02 | COVERED | Source contract rejects reintroduction.
CONTEXT | D-190-08 | every owned Repo path uses resolved prefix | 190-02 | COVERED | This plan creates the helper and static gate; later plans wire operations.
CONTEXT | D-190-10 | static and runtime omitted-prefix contracts | 190-02 | COVERED | Static source gate plus representative helper proof.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove owned fixed prefixes and add static prefix contracts</name>
  <files>lib/threadline/capture/audit_transaction.ex, lib/threadline/capture/audit_change.ex, lib/threadline/semantics/audit_action.ex, lib/threadline/governance/evidence_record.ex, lib/threadline/governance/export_job.ex, lib/threadline/governance/retention_run.ex, lib/threadline/governance/saved_view.ex, test/threadline/storage_schema_prefix_contract_test.exs</files>
  <behavior>
    - No Threadline-owned schema module declares a fixed storage prefix for the default schema.
    - Owned schema modules keep the same table source names, associations, changesets, primary keys, and fields.
    - A source contract fails if the fixed default prefix attribute is reintroduced.
  </behavior>
  <action>Write the failing source contract first per D-190-07 and D-190-10, then remove fixed owned schema prefix attributes from the seven Threadline-owned Ecto schema modules. Preserve public struct and changeset behavior. Keep table source names unchanged.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && bash -lc '! rg -n "@schema_prefix \\"threadline\\"" lib/threadline/capture/audit_transaction.ex lib/threadline/capture/audit_change.ex lib/threadline/semantics/audit_action.ex lib/threadline/governance/evidence_record.ex lib/threadline/governance/export_job.ex lib/threadline/governance/retention_run.ex lib/threadline/governance/saved_view.ex' && mix test test/threadline/storage_schema_prefix_contract_test.exs</automated>
  </verify>
  <done>Static contract proves no owned schema forces the default Threadline storage schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make storage test support explicit</name>
  <files>test/support/data_case.ex, test/support/storage_schema_case.ex, test/threadline/storage_schema_prefix_contract_test.exs</files>
  <behavior>
    - Tests can insert, fetch, delete, and clean Threadline-owned rows with `StorageSchema.repo_opts/1`.
    - Helpers restore `Application.get_env(:threadline, :storage_schema)` after temporary overrides.
    - Cleanup covers default `threadline` and custom `audit` storage in FK-safe order.
  </behavior>
  <action>Create or extend `Threadline.StorageSchemaCase` with repo option helpers, temporary config override helpers, and cleanup helpers. Update `DataCase` only where default cleanup would be wrong after prefix removal. Keep this as test support for D-190-08, not a production fallback.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/threadline/storage_schema_prefix_contract_test.exs test/threadline/query_test.exs</automated>
  </verify>
  <done>Existing tests can use explicit storage prefixes without relying on schema-module fixed prefixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema module to Repo | Ecto schema metadata influences where Repo operations read and write. |
| test helper to database | Test support creates, deletes, and restores rows across storage schemas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-05 | Spoofing | Fixed owned Ecto schema prefixes | mitigate | Remove fixed owned prefixes and add source contracts to prevent reintroduction per D-190-07. |
| T-190-06 | Tampering | Test cleanup across schemas | mitigate | Cleanup helpers target explicit storage schemas in FK-safe order and restore app config. |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run targeted contracts:
- `mix compile --warnings-as-errors`
- `mix test test/threadline/storage_schema_prefix_contract_test.exs test/threadline/query_test.exs`
- `mix format --check-formatted`
</verification>

<success_criteria>
SCHEMA-02 is structurally satisfied for owned schemas: no Threadline-owned schema forces the default storage prefix, and tests have explicit helper support for the remaining prefix wiring work.
</success_criteria>

<output>
Create `.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-02-SUMMARY.md` when done.
</output>
