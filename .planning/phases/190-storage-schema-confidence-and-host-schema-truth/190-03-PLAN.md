---
phase: 190-storage-schema-confidence-and-host-schema-truth
plan: 03
type: execute
wave: 2
depends_on:
  - 190-02
files_modified:
  - lib/threadline.ex
  - lib/threadline/audit.ex
  - lib/threadline/query.ex
  - lib/threadline/export.ex
  - test/threadline/semantics/audit_action_test.exs
  - test/threadline/query_test.exs
  - test/threadline/investigation_test.exs
  - test/threadline/export_test.exs
  - test/threadline/audit_transaction_test.exs
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
must_haves:
  truths:
    - "D-190-06: per-call storage_schema opts override app config and default."
    - "D-190-08: core Repo writes, reads, joins, export reads, and preloads use resolved prefix options."
    - "D-190-10: representative runtime tests fail when core prefix plumbing is omitted."
  artifacts:
    - path: "lib/threadline/query.ex"
      provides: "prefix-aware timeline, history, actor, correlation, transaction, joins, and preloads"
    - path: "lib/threadline/audit.ex"
      provides: "prefix-aware audited transaction helper and action linkage"
    - path: "lib/threadline/export.ex"
      provides: "prefix-aware export reads/counts/streams"
  key_links:
    - from: "lib/threadline.ex"
      to: "lib/threadline/semantics/audit_action.ex"
      via: "record_action/2 inserts with StorageSchema.repo_opts(opts)"
      pattern: "StorageSchema\\.repo_opts\\(opts\\)"
    - from: "lib/threadline/query.ex"
      to: "owned schema modules"
      via: "repo.all/one/preload calls receive storage_opts"
      pattern: "repo\\.(all|one|preload)"
---

<objective>
Thread `storage_schema:` through core public APIs, audited transaction helpers, query joins/preloads, and export read paths.

Purpose: SCHEMA-01 and SCHEMA-02 need the normal developer-facing API to use the selected storage schema after owned fixed prefixes are removed.
Output: Prefix-aware core Repo operations and tests covering semantic action linkage, timeline/history queries, investigation preloads, and export row reads.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-CONTEXT.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-RESEARCH.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-PATTERNS.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-02-SUMMARY.md
@lib/threadline.ex
@lib/threadline/audit.ex
@lib/threadline/query.ex
@lib/threadline/export.ex
@test/support/storage_schema_case.ex
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | phase-190 | Make custom storage_schema trustworthy through core APIs | 190-03 | COVERED | Core API/query/export reads are repaired here.
REQ | SCHEMA-01 | Custom storage_schema through capture/query/evidence/governance/operator-relevant paths | 190-03,190-04,190-05,190-06,190-10 | COVERED | This plan covers core capture/query/export read behavior.
REQ | SCHEMA-02 | Configurable storage schemas do not silently hit hardcoded threadline | 190-02,190-03,190-10 | COVERED | This plan adds core runtime proof after prefix removal.
RESEARCH | preload-prefix | Add prefix options to query preloads | 190-03 | COVERED | Closes known `repo.preload` omission.
RESEARCH | core-export | Export read/count/stream paths must use the selected storage schema | 190-03 | COVERED | Export jobs are handled separately in Plan 04.
CONTEXT | D-190-06 | storage_schema resolution order | 190-03 | COVERED | Per-call opts remain explicit for public APIs.
CONTEXT | D-190-08 | all owned Repo reads, writes, joins, bulk operations, preloads use resolved prefix | 190-03 | COVERED | Core API/query/export paths wired here.
CONTEXT | D-190-10 | static and runtime omitted-prefix contracts | 190-03 | COVERED | Runtime tests target joins/preloads/export reads.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread storage_schema through core writes and action linkage</name>
  <files>lib/threadline.ex, lib/threadline/audit.ex, test/threadline/semantics/audit_action_test.exs, test/threadline/audit_transaction_test.exs</files>
  <behavior>
    - `Threadline.record_action/2` inserts semantic actions with `StorageSchema.repo_opts(opts)`.
    - `Threadline.Audit.transaction/3` carries the resolved `storage_schema:` through action recording, transaction metadata updates, action linkage, and transaction lookup.
    - Explicit `storage_schema: "audit"` tests fail if action linkage or transaction updates fall back to `threadline`.
  </behavior>
  <action>Add failing tests that seed plausible default-schema sentinels before implementation. Pass `storage_schema:` through option normalization and every StorageSchema repo option call inside the audited transaction helper. Preserve existing capture/query/auth semantics except for correcting the storage target.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/threadline/semantics/audit_action_test.exs test/threadline/audit_transaction_test.exs</automated>
  </verify>
  <done>Semantic action linkage and audited transaction tests prove selected storage is used for writes and linkage updates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make query preloads and export reads prefix-aware</name>
  <files>lib/threadline/query.ex, lib/threadline/export.ex, test/threadline/query_test.exs, test/threadline/investigation_test.exs, test/threadline/export_test.exs</files>
  <behavior>
    - Timeline, row history, actor, correlation, and transaction context queries pass the selected prefix to reads and joins.
    - Investigation and transaction preloads use the same resolved prefix as the originating query.
    - Export CSV/JSON/count/stream reads return only selected-schema rows.
  </behavior>
  <action>Add tests that would return or preload default-schema sentinels if prefix opts are omitted. Update query preload helper signatures and export read paths to pass resolved StorageSchema repo options. Keep the public filter vocabulary unchanged.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/threadline/query_test.exs test/threadline/investigation_test.exs test/threadline/export_test.exs</automated>
  </verify>
  <done>Core query and export tests pass with explicit prefix plumbing and fail on wrong-schema reads or preloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API opts to Repo | Caller-provided `storage_schema:` influences Threadline-owned Repo operations. |
| Ecto query to PostgreSQL | Query prefixes determine which schema is read, joined, preloaded, or streamed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-07 | Information Disclosure | `Threadline.Query` joins and preloads | mitigate | Pass resolved prefix options through all core query and preload paths so custom storage reads do not leak default-schema rows. |
| T-190-08 | Tampering | `Threadline.Audit.transaction/3` linkage updates | mitigate | Carry `storage_schema:` through transaction metadata, action linkage, and transaction lookup per D-190-06 and D-190-08. |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run targeted contracts:
- `mix compile --warnings-as-errors`
- `mix test test/threadline/semantics/audit_action_test.exs test/threadline/audit_transaction_test.exs`
- `mix test test/threadline/query_test.exs test/threadline/investigation_test.exs test/threadline/export_test.exs`
- `mix format --check-formatted`
</verification>

<success_criteria>
SCHEMA-01 and SCHEMA-02 are satisfied for core APIs: writes, joins, preloads, and export reads carry resolved storage prefix options without changing the public filter or auth vocabulary.
</success_criteria>

<output>
Create `.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-03-SUMMARY.md` when done.
</output>
