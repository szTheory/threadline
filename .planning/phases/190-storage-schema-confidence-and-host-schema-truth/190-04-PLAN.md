---
phase: 190-storage-schema-confidence-and-host-schema-truth
plan: 04
type: execute
wave: 3
depends_on:
  - 190-03
files_modified:
  - lib/threadline/export/orchestrator.ex
  - lib/threadline/export/cleanup_task.ex
  - lib/threadline/operator_surface/controllers/export_controller.ex
  - test/threadline/export/orchestrator_test.exs
  - test/threadline/export/cleanup_test.exs
  - test/threadline/operator_surface/controllers/export_controller_test.exs
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
must_haves:
  truths:
    - "D-190-04: export jobs, status transitions, cleanup, and download lookup participate in custom storage-schema proof."
    - "D-190-08: background export Repo operations use resolved prefix options."
    - "D-190-09: queued export uses the global configured storage schema contract for Phase 190."
  artifacts:
    - path: "lib/threadline/export/orchestrator.ex"
      provides: "prefix-aware queued export job fetch/update and export stream orchestration"
    - path: "lib/threadline/operator_surface/controllers/export_controller.ex"
      provides: "prefix-aware export download lookup with existing actor authorization"
  key_links:
    - from: "lib/threadline/export/orchestrator.ex"
      to: "lib/threadline/export.ex"
      via: "export stream reads use the configured Threadline storage schema"
      pattern: "Export\\.stream_export_rows"
---

<objective>
Make queued export orchestration, cleanup, and download lookup use the deliberate configured-storage contract.

Purpose: SCHEMA-01 requires export jobs and downloads to avoid default-schema rows when `config :threadline, :storage_schema` is set to `audit`.
Output: Prefix-aware export background/status/download paths plus tests that protect wrong-schema reads and preserve actor authorization.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-CONTEXT.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-RESEARCH.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-PATTERNS.md
@.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-03-SUMMARY.md
@lib/threadline/export.ex
@lib/threadline/export/orchestrator.ex
@lib/threadline/export/cleanup_task.ex
@lib/threadline/operator_surface/controllers/export_controller.ex
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | phase-190 | Make custom storage_schema trustworthy for export governance paths | 190-04 | COVERED | Export job/status/download storage handled here.
REQ | SCHEMA-01 | Custom storage_schema through governance/export paths | 190-04,190-10 | COVERED | This plan repairs; Plan 10 proves with sentinels.
REQ | SCHEMA-02 | Configurable storage schemas do not silently hit hardcoded threadline | 190-04 | COVERED | Export job Repo operations become explicit.
RESEARCH | async-contract | Export jobs must use deliberate storage schema behavior | 190-04 | COVERED | Uses selected global configured schema for Phase 190.
CONTEXT | D-190-04 | Proof matrix includes export jobs/status/download | 190-04,190-10 | COVERED | Implementation here, closing proof later.
CONTEXT | D-190-08 | background Repo paths use resolved prefix | 190-04 | COVERED | Orchestrator, cleanup, and controller job lookup are wired.
CONTEXT | D-190-09 | async/export behavior deliberate | 190-04 | COVERED | Selected decision: app configured storage schema is runtime job contract; changing schema after queueing requires draining/rerunning jobs.
CONTEXT | D-190-10 | runtime contracts fail on omitted prefix plumbing | 190-04 | COVERED | Tests seed plausible export jobs in both schemas.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make queued export and cleanup storage-prefix deliberate</name>
  <files>lib/threadline/export/orchestrator.ex, lib/threadline/export/cleanup_task.ex, test/threadline/export/orchestrator_test.exs, test/threadline/export/cleanup_test.exs</files>
  <behavior>
    - Export orchestrator fetches, marks running, streams, marks completed/failed, and expires jobs using the configured storage schema.
    - Cleanup deletes expired export jobs only from the configured storage schema.
    - Tests fail when a worker picks up a default-schema sentinel job under configured `audit` storage.
  </behavior>
  <action>Add tests that seed plausible export jobs in both default and custom storage schemas. Update orchestration and cleanup Repo calls to use explicit resolved prefix options per D-190-08 and the selected D-190-09 global configured schema contract. Preserve existing queue adapter behavior and failure status transitions.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/threadline/export/orchestrator_test.exs test/threadline/export/cleanup_test.exs</automated>
  </verify>
  <done>Queued export and cleanup tests prove configured custom storage is used and default sentinel jobs are ignored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep export download lookup prefix-aware and authorized</name>
  <files>lib/threadline/operator_surface/controllers/export_controller.ex, test/threadline/operator_surface/controllers/export_controller_test.exs</files>
  <behavior>
    - Export download lookup reads the configured storage schema.
    - Wrong-actor download requests remain denied.
    - Default-schema sentinel export jobs are not downloaded under configured `audit` storage.
  </behavior>
  <action>Add controller tests that combine custom storage sentinels with the existing actor authorization contract. Update job lookup to use the configured storage prefix while leaving current route, content type, not-found, and wrong-actor behavior intact.</action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/threadline/operator_surface/controllers/export_controller_test.exs</automated>
  </verify>
  <done>Download tests prove the selected storage schema is used without weakening actor checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background worker to Repo | Queued export code runs outside request context and relies on app configuration. |
| controller request to export row | Authenticated operator requests fetch stored export jobs for download. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-09 | Information Disclosure | Export orchestrator and download controller | mitigate | Fetch and stream from configured storage schema and preserve actor authorization before download. |
| T-190-10 | Repudiation | Export status rows | mitigate | Update status rows in the same resolved storage schema as the export operation. |
| T-190-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run targeted export gates:
- `mix compile --warnings-as-errors`
- `mix test test/threadline/export/orchestrator_test.exs test/threadline/export/cleanup_test.exs`
- `mix test test/threadline/operator_surface/controllers/export_controller_test.exs`
- `mix format --check-formatted`
</verification>

<success_criteria>
Queued export, cleanup, and download paths follow the Phase 190 configured-storage contract: they use `audit` when app config selects it, ignore default-schema sentinel jobs, and keep actor authorization intact.
</success_criteria>

<output>
Create `.planning/phases/190-storage-schema-confidence-and-host-schema-truth/190-04-SUMMARY.md` when done.
</output>
