---
phase: 159-brand-audit-and-research
plan: 03
type: execute
wave: 2
depends_on: [159-01, 159-02]
files_modified:
  - .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md
autonomous: true
requirements: [RES-04]

must_haves:
  truths:
    - "User can read DESIGN-BRIEF.md stating the round-1 generation contract: motif rules, measurable hard constraints, degrees of freedom, four archetype lanes"
    - "Hard constraints are stated numerically: no subtitle in primary, no container chips, survives literal 16px, survives one color"
    - "Each of the four archetype lanes requires a distinct named motif strategy drawn from the research technique catalog"
    - "Every REWORK/ADD item from AUDIT.md is routed to a downstream v1.35 requirement or descoped with a recorded reason"
  artifacts:
    - path: ".planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md"
      provides: "Round-1 logo generation contract plus audit-item traceability table"
      min_lines: 150
  key_links:
    - from: ".planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md"
      to: ".planning/phases/159-brand-audit-and-research/159-AUDIT.md"
      via: "GAP-nn/UP-nn IDs in the traceability table"
      pattern: "GAP-0[0-9]"
    - from: ".planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md"
      to: ".planning/phases/159-brand-audit-and-research/159-RESEARCH.md"
      via: "named motif strategies citing the technique catalog"
      pattern: "[Tt]echnique"
---

<objective>
Synthesize `159-AUDIT.md` and `159-RESEARCH.md` into `159-DESIGN-BRIEF.md` — the round-1 generation contract for the Phase 161 logo tournament: motif rules, numerically-stated hard constraints, explicit degrees of freedom, and the four archetype lanes each requiring a distinct named motif strategy. Route every REWORK/ADD audit item to a downstream v1.35 requirement or descope it with a recorded reason.

Purpose: RES-04 and ROADMAP Phase 159 success criteria 4-5. This brief is what Phase 161 candidates are generated against — it must be executable without interpretation.
Output: `.planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md` (analysis artifact only — no modifications to `brandbook/` or `lib/`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-brand-audit-and-research/159-CONTEXT.md
@.planning/phases/159-brand-audit-and-research/159-AUDIT.md
@.planning/phases/159-brand-audit-and-research/159-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the round-1 generation contract</name>
  <files>.planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md</files>
  <action>
    Create `159-DESIGN-BRIEF.md` with the contract sections locked in CONTEXT.md "Convergence":

    1. "What we keep" — the brand DNA the audit verdicted KEEP/TIGHTEN (expected: voice, positioning, palette philosophy, tagline "Follow what happened."; confirm against the actual AUDIT verdicts, do not assume). Naming/tagline are settled — out of scope for candidates.

    2. "Motif rules" — derived from the research technique catalog and cross-case synthesis: each candidate must be built on one named motif strategy (technique names from RESEARCH.md used verbatim, e.g. "counter replacement", "stroke continuation", "negative space"); the motif must encode Threadline meaning (thread, line, evidence path, follow-what-happened) structurally, not decoratively; gradient-dependence and container chips are named antipatterns. Include the "Threadline" letterform hooks identified in research (Th, double-l, i dot, e/a/d counters) as suggested attack surfaces.

    3. "Hard constraints (every candidate, every round)" — stated numerically/mechanically so each is checkable, each citing its downstream requirement ID inline: no subtitle/tagline text in the primary lockup, zero subtitle glyphs, tagline only in a separate -subtitle variant [LOGO-03]; no rectangular or rounded-rect background container behind any mark, zero container shapes [LOGO-02]; no icon-beside-plain-set-type structure — mark and type demonstrably one unit via shared geometry or tight optical spacing [LOGO-01]; must survive literal 16px — restate the numeric thresholds from RESEARCH.md verbatim (minimum stroke px at 16px canvas, detail floor, counter minimums, silhouette test) [TOUR-02 contexts]; must survive one flat color with no opacity/gradient tiers, monochrome rendition shipped from round 1 [LOGO-04]; zero `<text>` elements — pure paths only, outlined via the Phase 160 glyph pipeline [LOGO-05, GLYPH-03].

    4. "Degrees of freedom" — per the locked v1.35 seed-drift decision: OFL-safe typeface exploration (list the candidate typefaces from RESEARCH.md by name) and palette shifts are allowed; current Geist + tokens are a seed, not a contract; tokens/product UI change only if the winner demands it.

    5. "Four archetype lanes" (round-1 quota, exactly 8 candidates) [TOUR-01]: 3 integrated typemarks, 3 unified mark+type lockups, 1 monogram/mark-led, 1 wordmark-only — as a table where each lane row states its count, definition, and the rule that every candidate carries a distinct named motif strategy (no two candidates share a motif).

    6. "Out of scope / flags" — one-line trademark/legal clearance flag for human review (the only legal mention); motion, print/CMYK/Pantone, merch, and naming/tagline alternatives excluded.

    Write rules, not essays: every constraint must be checkable by Phase 161/162 without judgment calls. No SVG markup in the brief — it is a contract document, not an implementation.
  </action>
  <verify>
    <automated>test -f .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md && grep -q '16px' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md && grep -qi 'subtitle' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md && grep -qiE 'container|chip' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md && grep -qiE 'OFL|Open Font License' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md && grep -q 'LOGO-0' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md && grep -qi 'monogram' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md && grep -qi 'wordmark-only' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md && grep -qi 'trademark' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md</automated>
  </verify>
  <done>DESIGN-BRIEF.md states the full round-1 contract: motif rules naming techniques from RESEARCH.md verbatim; all hard constraints numeric/mechanical with downstream requirement IDs (LOGO-01..05, TOUR-01); degrees of freedom listing named OFL typeface candidates; the four-lane archetype table (3+3+1+1=8) with the distinct-named-motif rule; trademark flagged for human review in one line.</done>
</task>

<task type="auto">
  <name>Task 2: Route every REWORK/ADD audit item — traceability table and final coherence check</name>
  <files>.planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md</files>
  <action>
    Append a "## Audit item traceability" section to `159-DESIGN-BRIEF.md`.

    Collect every item from AUDIT.md that carries a REWORK or ADD verdict (section-level verdicts) plus every GAP-nn/UP-nn item the audit raised. Render a markdown table with columns: Item ID | Audit finding (one line) | Disposition | Routed to | Reason. Disposition is exactly one of ROUTED or DESCOPED. ROUTED items name a concrete downstream v1.35 requirement (GLYPH-01..03, TOUR-01..04, LOGO-01..05, BOOK-01..07, ROLL-01..03) — most logo-system REWORKs route to LOGO-*/TOUR-*, brand-book structure ADDs route to BOOK-* (misuse gallery items to BOOK-05; audit backlog resolution is BOOK-04's job). DESCOPED items state a recorded reason consistent with REQUIREMENTS.md "Out of Scope" (e.g. trademark -> human review; landing page -> LANDING-01 deferred). Zero rows may have an empty Routed to/Reason cell.

    Then perform the final coherence pass over the whole brief: (a) every hard constraint in the brief is consistent with the locked v1.35 decisions in STATE/CONTEXT (no chips, no subtitle-in-primary, pure paths, user picks winner); (b) the brief contradicts nothing the audit verdicted KEEP; (c) the brief never weakens a constraint with hedge words — constraints use MUST/MUST NOT phrasing; (d) confirm `brandbook/` and `lib/` are untouched by this phase (`git status --porcelain brandbook/ lib/` empty). Close the document with a one-paragraph "Definition of done for round 1" that Phase 161 can quote.
  </action>
  <verify>
    <automated>grep -qi 'traceability' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md && [ "$(grep -ciE '\| *(ROUTED|DESCOPED) *\|' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md)" -ge 1 ] && [ "$(grep -iE '\| *(ROUTED|DESCOPED) *\|' .planning/phases/159-brand-audit-and-research/159-DESIGN-BRIEF.md | grep -cE '\|[^|]*\|[^|]*\|[^|]*\| *\|')" -eq 0 ] && [ -z "$(git -C /Users/jon/projects/threadline status --porcelain brandbook/ lib/)" ]</automated>
  </verify>
  <done>Every REWORK/ADD verdict and every GAP-nn/UP-nn item from AUDIT.md appears in the traceability table with disposition ROUTED (to a named v1.35 requirement ID) or DESCOPED (with a recorded reason); no empty routing cells; coherence pass complete; brandbook/ and lib/ unmodified. ROADMAP Phase 159 success criteria 4 and 5 are satisfiable by reading DESIGN-BRIEF.md alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Plan reads two phase-local artifacts and planning docs, writes one planning artifact; no untrusted input, no installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-04 | Tampering | brandbook/, lib/ | mitigate | Read-only phase; Task 2 verify gate asserts `git status --porcelain brandbook/ lib/` is empty |
| T-159-05 | Repudiation | Audit-item dispositions | mitigate | Traceability table records explicit ROUTED target or DESCOPED reason per item — no silent drops |
</threat_model>

<verification>
- `159-DESIGN-BRIEF.md` exists with all six contract sections plus the traceability table.
- Hard constraints carry numbers and downstream requirement IDs; archetype lanes total 8 (3+3+1+1); motif strategies are named techniques from RESEARCH.md.
- Every AUDIT REWORK/ADD and GAP/UP item has a ROUTED or DESCOPED row; `brandbook/` and `lib/` untouched.
</verification>

<success_criteria>
- RES-04 satisfied: DESIGN-BRIEF.md synthesizes audit + research into the round-1 generation contract (motif rules, numeric hard constraints, degrees of freedom, four archetype lanes with distinct-named-motif requirement).
- ROADMAP Phase 159 success criterion 5 satisfied: every REWORK/ADD item routed or descoped with a recorded reason.
- Phase 161 can generate 8 round-1 candidates from this brief without interpretation.
</success_criteria>

<output>
Create `.planning/phases/159-brand-audit-and-research/159-03-SUMMARY.md` when done.
</output>
