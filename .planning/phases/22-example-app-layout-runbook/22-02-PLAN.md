---
wave: 2
depends_on: [22-01]
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - test/threadline/phase06_nyquist_ci_contract_test.exs
  - test/threadline/readme_doc_contract_test.exs
autonomous: true
requirements_addressed: [REF-01, REF-02]
---

# Plan 22-02: Root `verify.example`, CI prelude, doc contracts

**Phase:** 22 — Example app layout & runbook  
**Plan:** 22-02 of 02  
**Status:** Ready to execute  
**Depends on:** Plan 22-01 complete (example tree and tests green locally)  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-22-03 | CI job drops example verification silently | high | Extend **`phase06_nyquist_ci_contract_test.exs`** so `ci.all` lists **`verify.example`** in documented order (**D-16**). |
| T-22-04 | Second DB name leaks into library tests | low | Use **`threadline_phoenix_test`** only in example config and CI `createdb`; root `config/test.exs` stays **`threadline_test`**. |

---

## Objective

Add **`mix verify.example`** to root **`mix.exs`**, append it to **`ci.all`** per **D-16**, extend **`.github/workflows/ci.yml`** `verify-test` job with a prelude that creates **`threadline_phoenix_test`** and runs the example gate (**D-16–D-17**), and update contract tests so **`main` cannot rot** `examples/` (**OSS DNA**).

---

## must_haves

- [ ] Root `mix.exs` defines alias **`verify.example`** running nested compile + test (and DB create/migrate if required by example tests).
- [ ] **`ci.all`** includes **`verify.example`** after library compile/tests per chosen order documented in **22-RESEARCH.md** §3.2.
- [ ] `test/threadline/phase06_nyquist_ci_contract_test.exs` asserts presence and ordering of **`verify.example`** relative to existing steps.
- [ ] `verify-test` job creates **`threadline_phoenix_test`** before running **`mix verify.example`** (or equivalent shell).

---

## Task 22-02-01 — Root aliases

<read_first>
- `mix.exs` (aliases, `preferred_envs`)
- `.planning/phases/22-example-app-layout-runbook/22-RESEARCH.md` §3.1–3.2
</read_first>

<action>
1. Add **`verify.example`** alias under **`MIX_ENV=test`** semantics consistent with **`cli/0`**: e.g. shell `cd examples/threadline_phoenix && mix deps.get && mix compile --warnings-as-errors && mix test` plus any **`ecto.create -r ThreadlinePhoenix.Repo`** steps required so CI passes without manual DB creation.
2. Insert **`verify.example`** into **`ci.all`** after **`verify.test`** (or after **`verify.doc_contract`** if you document why — default per research: after library tests, before or after doc_contract; pick one and keep **monotonic** order in contract test).
</action>

<acceptance_criteria>
- `grep -q 'verify.example' mix.exs` exits 0.
- In `mix.exs`, the first occurrence of the substring `\"verify.test\"` has a lower byte index than the first `\"verify.example\"`, and `\"verify.example\"` has a lower index than `\"verify.doc_contract\"` (same ordering `test/threadline/phase06_nyquist_ci_contract_test.exs` will assert).
- `mix.exs` `ci.all` list contains only standard `mix` task alias strings (no shell `curl` / network fetches).
</acceptance_criteria>

---

## Task 22-02-02 — GitHub Actions `verify-test`

<read_first>
- `.github/workflows/ci.yml` (`verify-test` job, postgres service, existing steps)
- `.planning/phases/22-example-app-layout-runbook/22-CONTEXT.md` (D-16, D-17)
</read_first>

<action>
1. In job **`verify-test`**, before **`mix verify.example`**, add steps to create database **`threadline_phoenix_test`** for the Postgres user (e.g. `createdb` or `psql -c 'CREATE DATABASE threadline_phoenix_test;'` idempotent).
2. Export env vars so **`examples/threadline_phoenix`** tests use the same host/port as the service (**`POSTGRES_HOST`/`DATABASE_URL`** pattern consistent with existing job env).
3. Add step running **`mix verify.example`** from repo root. Do **not** change stable job **`id:`** keys (**D-17**).
</action>

<acceptance_criteria>
- `grep -q 'threadline_phoenix_test' .github/workflows/ci.yml` exits 0.
- `grep -q 'verify.example' .github/workflows/ci.yml` exits 0.
- Job id `verify-test:` still appears exactly once as the job key (unchanged rename).
</acceptance_criteria>

---

## Task 22-02-03 — Nyquist CI contract + README doc contract

<read_first>
- `test/threadline/phase06_nyquist_ci_contract_test.exs`
- `test/threadline/readme_doc_contract_test.exs`
- `examples/README.md`
</read_first>

<action>
1. Extend **`describe "CI-02"`** (or add **`CI-02b`**) so `ci.all` is asserted to include **`verify.example`** in the correct order relative to **`verify.test`**, **`verify.threadline`**, and **`verify.doc_contract`**.
2. Add or extend doc contract tests so **`examples/README.md`** must contain **`threadline_phoenix`** and link or path to the nested README.
3. Add assertions on **`examples/threadline_phoenix/README.md`** for **REF-01** surface: file must contain **`mix phx.server`** or **`iex -S mix phx.server`**, **`mix test`**, and **`ecto.migrate`** substrings (machine drift-guard alongside Task 22-01-03 grep criteria).
</action>

<acceptance_criteria>
- `mix test test/threadline/phase06_nyquist_ci_contract_test.exs` exits 0 after edits.
- `mix test test/threadline/readme_doc_contract_test.exs` exits 0 after edits.
- New/edited tests reference **`verify.example`** string literal at least once.
- `grep -q 'threadline_phoenix/README' test/threadline/readme_doc_contract_test.exs` OR `grep -q 'examples/threadline_phoenix/README' test/threadline/readme_doc_contract_test.exs` exits 0 after edits.
</acceptance_criteria>

---

## Verification (plan level)

1. `MIX_ENV=test mix ci.all` exits 0 locally with Postgres (both DBs).
2. `MIX_ENV=test mix verify.example` alone exits 0 from repo root.

---

## Success criteria

- **REF-01 / REF-02:** CI and local **`ci.all`** prove the example stays buildable and documented workflows stay aligned (**machine-verified**).
