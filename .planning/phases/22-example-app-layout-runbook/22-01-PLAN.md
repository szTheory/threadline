---
wave: 1
depends_on: []
files_modified:
  - examples/threadline_phoenix/
  - examples/README.md
autonomous: true
requirements_addressed: [REF-01, REF-02]
---

# Plan 22-01: Scaffold `examples/threadline_phoenix` (path dep, posts, install, triggers)

**Phase:** 22 — Example app layout & runbook  
**Plan:** 22-01 of 02  
**Status:** Ready to execute  
**Blocks:** 22-02  

---

## Threat model

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-22-01 | Example seeds or docs contain realistic credentials or PII | medium | Use neutral synthetic `posts` titles/slugs only (**D-08**); no `users` table with secrets in Phase 22. |
| T-22-02 | Wrong path dep exposes fork to wrong package | low | `mix.exs` must use `{:threadline, path: "../.."}` exactly for `examples/threadline_phoenix/` depth. |

---

## Objective

Create the canonical Phoenix example at `examples/threadline_phoenix/` per **22-CONTEXT** D-01–D-13 and **D-18**: `:threadline_phoenix` app, `ThreadlinePhoenix` namespace, API-lean `mix phx.new` layout, `posts` table, checked-in migrations from `mix threadline.install`, triggers via `mix threadline.gen.triggers --tables posts`, `mix setup`, dedicated test DB **`threadline_phoenix_test`**, README with **`MIX_ENV`** caveat for `gen.triggers`, and `examples/README.md` index line pointing at the example README.

---

## must_haves

- [ ] Directory `examples/threadline_phoenix/` exists with standard Phoenix layout (Endpoint, Repo, Router, Application).
- [ ] `examples/threadline_phoenix/mix.exs` contains `{:threadline, path: "../.."}` and `app: :threadline_phoenix`.
- [ ] Example README contains the **exact** documented `mix phx.new` command block (flags per **D-05**) and a paragraph stating **`mix setup` does not start Postgres** (**D-10**).
- [ ] Example README contains the substring **`MIX_ENV`** in the context of `mix threadline.gen.triggers` (**REF-02**).
- [ ] Migrations exist for Threadline audit/semantics (from install) and for `posts`; SQL trigger artifacts applied via documented migrate + gen.triggers flow (**REF-02**).

---

## Task 22-01-01 — Generate Phoenix skeleton and path dependency

<read_first>
- `.planning/phases/22-example-app-layout-runbook/22-CONTEXT.md` (D-01–D-06, D-14)
- `README.md` (root — install / triggers / MIX_ENV wording to mirror)
- `mix.exs` (root — current Elixir/OTP constraints)
</read_first>

<action>
1. From repo root, ensure `examples/threadline_phoenix/` is created using **`mix phx.new threadline_phoenix --module ThreadlinePhoenix`** with flags exactly as locked in **D-05** (adjust only if the installed Phoenix version rejects a flag — then update **both** generated app and README to match HexDocs `Mix.Tasks.Phx.New` for that version).
2. Set example **`mix.exs`**: `app: :threadline_phoenix`, add `{:threadline, path: "../.."}`, add Phoenix/Bandit/Ecto deps compatible with repo’s Elixir version.
3. Wire `ThreadlinePhoenix.Application` to start `ThreadlinePhoenix.Repo` and `ThreadlinePhoenixWeb.Endpoint` per generator output; keep **no** `:trigger_capture` config (**D-18**).
</action>

<acceptance_criteria>
- `grep -q ':threadline_phoenix' examples/threadline_phoenix/mix.exs` exits 0.
- `grep -q 'path: \"../..\"' examples/threadline_phoenix/mix.exs` exits 0.
- `test -f examples/threadline_phoenix/lib/threadline_phoenix/application.ex` exits 0.
- `grep -q -- '--no-html' examples/threadline_phoenix/README.md` OR README documents equivalent API-only flags in a fenced `bash` block containing `mix phx.new`.
</acceptance_criteria>

---

## Task 22-01-02 — `posts` schema, install, triggers, test DB

<read_first>
- `lib/mix/tasks/threadline.install.ex`
- `README.md` (root lines for `mix threadline.install`, `mix threadline.gen.triggers`, `MIX_ENV`)
- `test/test_helper.exs` (root pattern for `storage_up` + migrate)
- `examples/threadline_phoenix/config/test.exs` (after Task 22-01-01 exists)
</read_first>

<action>
1. Add Ecto migration for table **`posts`** with boring columns (e.g. `title`, `slug` strings, timestamps) and **neutral synthetic** seed data only (**D-08–D-09**); no `users` table in Phase 22.
2. From **`examples/threadline_phoenix/`**, run **`mix threadline.install`** once; commit the generated `*_threadline_audit_schema.exs` and `*_threadline_semantics_schema.exs` migrations if not already present.
3. Run **`mix threadline.gen.triggers --tables posts`** then **`mix ecto.migrate`** per root README ordering; commit updated `priv/` SQL if the task emits files under the example app.
4. Configure **`config/test.exs`** database name **`threadline_phoenix_test`** (**D-15**). Implement **`test/test_helper.exs`** in the example mirroring root pattern: ensure DB storage exists and run migrations before `Ecto.Adapters.SQL.Sandbox` mode (adapt to `ThreadlinePhoenix.Repo`).
5. Add at least one ExUnit test proving Repo loads and `posts` schema exists (e.g. count query or insert).
</action>

<acceptance_criteria>
- `grep -q 'threadline_phoenix_test' examples/threadline_phoenix/config/test.exs` exits 0.
- `grep -q 'posts' examples/threadline_phoenix/priv/repo/migrations/*.exs` matches at least one migration file (shell glob).
- `grep -q 'threadline.install' examples/threadline_phoenix/README.md` exits 0.
- `grep -q 'threadline.gen.triggers' examples/threadline_phoenix/README.md` exits 0.
- `grep -q 'MIX_ENV' examples/threadline_phoenix/README.md` exits 0.
- From `examples/threadline_phoenix/` with Postgres env pointing at a server with DB created: `MIX_ENV=test mix test` exits 0.
</acceptance_criteria>

---

## Task 22-01-03 — `mix setup` and contributor README

<read_first>
- `.planning/phases/22-example-app-layout-runbook/22-CONTEXT.md` (D-10, D-12, D-13)
- `README.md` (root — Requirements section ~> 1.15, install/triggers/MIX_ENV paragraph for parity)
- `CONTRIBUTING.md` (DB_HOST, DB_PORT, `mix ci.all`)
- `docker-compose.yml` (service names / ports for doc cross-links)
</read_first>

<action>
1. In **`examples/threadline_phoenix/mix.exs`**, define alias **`setup`: `["deps.get", "compile", "ecto.setup"]`** (or equivalent order **D-10**).
2. In the example **README**, add a **Prerequisites** section listing **Elixir ~> 1.15** (or the repo’s actual constraint), **PostgreSQL**, and **OTP** as needed — mirror root **`README.md` §Requirements** intent (**REF-01**).
3. Document **run** and **test** explicitly: after migrate, **`mix phx.server`** (or **`iex -S mix phx.server`**) to boot the API; **`MIX_ENV=test mix test`** (or `mix test` with test env) for the example suite (**REF-01**).
4. Document **migrate** path: `mix ecto.create` (first time), **`mix ecto.migrate`** after install/gen.triggers, in command order matching root **`README.md`** §Installation (**REF-02**).
5. Copy or closely paraphrase the root **`MIX_ENV` / `Mix.Task.run("app.config", [])`** sentence for `mix threadline.gen.triggers` so parity with root README is obvious — include the literal substring **`Mix.Task.run("app.config"`** in the example README (**REF-02**).
6. Document: (1) path dep points to repo root, (2) run Mix commands **from `examples/threadline_phoenix/`**, (3) **`DB_HOST` / `DB_PORT`** when using root **`docker-compose.yml`** (**D-12**); optional **`pg_isready`** / health wait (**D-13**).
7. State explicitly that **`mix setup` does not start Postgres** — Postgres must already be reachable (**D-10**).
8. Replace placeholder **`examples/README.md`** body with a short index linking to **`threadline_phoenix/README.md`** and stating the canonical example path (**REF-01**).
</action>

<acceptance_criteria>
- `grep -q 'setup:' examples/threadline_phoenix/mix.exs` and `grep -q 'ecto.setup' examples/threadline_phoenix/mix.exs` both exit 0.
- `grep -qE 'Elixir|~> 1\.1[0-9]' examples/threadline_phoenix/README.md` exits 0 (prerequisites).
- `grep -qE 'PostgreSQL|Postgres' examples/threadline_phoenix/README.md` exits 0.
- `grep -qE 'mix phx.server|iex -S mix phx.server' examples/threadline_phoenix/README.md` exits 0.
- `grep -q 'mix test' examples/threadline_phoenix/README.md` exits 0.
- `grep -q 'ecto.migrate' examples/threadline_phoenix/README.md` exits 0.
- `grep -qF 'Mix.Task.run("app.config"' examples/threadline_phoenix/README.md` exits 0 (**REF-02** parity with root README).
- `grep -q 'MIX_ENV' examples/threadline_phoenix/README.md` exits 0.
- `grep -qiE 'reachable|does not start|already be running' examples/threadline_phoenix/README.md` exits 0 (Postgres not started by `mix setup`).
- `grep -q 'threadline_phoenix' examples/README.md` exits 0.
</acceptance_criteria>

---

## Verification (plan level)

1. `cd examples/threadline_phoenix && mix deps.get && mix compile --warnings-as-errors` exits 0.
2. With Postgres and `threadline_phoenix_test` created: `cd examples/threadline_phoenix && MIX_ENV=test mix test` exits 0.

---

## Success criteria

- **REF-01:** Runnable path-dep example with documented setup, run, test.
- **REF-02:** Install + gen.triggers workflow and **MIX_ENV** documentation in example README.
