---
phase: 60
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/threadline/query.ex
  - lib/threadline/query/actor_history_page.ex
  - lib/threadline.ex
  - test/threadline/query_test.exs
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - Actor history queries can be paginated forward and backward using keyset cursors on `occurred_at` and `id`.
    - Actor history queries can be bounded by a time window (`:from` / `:to`).
  artifacts:
    - path: lib/threadline/query/actor_history_page.ex
      provides: Pagination struct with cursor type
    - path: lib/threadline/query.ex
      provides: expanded actor_history/2 implementation
    - path: lib/threadline.ex
      provides: Delegated actor_history/2
  key_links: []
---

<objective>
Implement bidirectional keyset pagination and time bounds for actor history queries to power the Actor Window LiveView streams.

Purpose: LiveView infinite scrolling requires constant-time DB performance and bidirectional cursor tracking.
Output: A modified `actor_history/2` function and associated `ActorHistoryPage` struct.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/60-actor-window-screen/DECISIONS.md
@.planning/phases/60-actor-window-screen/60-PATTERNS.md
@.planning/phases/60-actor-window-screen/RESEARCH.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Expand Threadline.actor_history/2 API</name>
  <files>lib/threadline/query.ex, lib/threadline/query/actor_history_page.ex, lib/threadline.ex, test/threadline/query_test.exs</files>
  <behavior>
    - The `actor_history(actor_ref, opts)` accepts `:repo`, `:limit` (default 50), `:after` (cursor), `:before` (cursor), `:from` (DateTime), `:to` (DateTime).
    - It returns a `Threadline.Query.ActorHistoryPage` struct with `entries` (list of `AuditTransaction`) and `next_cursor` / `prev_cursor`.
    - Cursor structure must be `%{occurred_at: DateTime.t(), id: String.t()}`.
    - Sorting must be fully deterministic: `order_by: [desc: at.occurred_at, desc: at.id]`.
    - `:after` cursor fetches older records (`occurred_at < cursor.occurred_at OR (== and id < cursor.id)`).
    - `:before` cursor fetches newer records (`occurred_at > cursor.occurred_at OR (== and id > cursor.id)`).
  </behavior>
  <implementation>
    1. Create `lib/threadline/query/actor_history_page.ex` defining the struct `entries`, `next_cursor`, `prev_cursor`.
    2. Expand `actor_history/2` in `lib/threadline/query.ex` (reference analog file from PATTERNS.md) replicating the `maybe_after_timeline_cursor` fragment logic but using `occurred_at` instead of `captured_at`. Ensure you support bidirectional `:after` and `:before` options.
    3. Support `:from` and `:to` DateTime filtering (similar to `timeline/2`).
    4. Ensure the delegation of `actor_history/2` in `lib/threadline.ex` passes down options correctly.
    5. Update existing tests to expect the new `ActorHistoryPage` struct instead of a raw list.
  </implementation>
  <verify>
    <automated>mix test test/threadline/query_test.exs</automated>
  </verify>
  <done>Threadline.actor_history/2 accepts new cursor/limit options and returns an ActorHistoryPage struct with properly sorted entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→DB | Pagination parameters shape SQL queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Denial of Service | `Threadline.Query.actor_history` | mitigate | Hard cap the `:limit` param to prevent memory exhaustion |
| T-60-02 | Information Disclosure | `Threadline.Query.actor_history` | mitigate | Ensure cursor arguments strictly use parameterized queries (via `fragment` and Ecto syntax) |
</threat_model>

<verification>
`mix test test/threadline/query_test.exs` passes with new pagination tests.
</verification>

<success_criteria>
`Threadline.actor_history/2` exists, works, and handles fully deterministic keyset pagination.
</success_criteria>

<output>
After completion, create `.planning/phases/60-actor-window-screen/60-01-SUMMARY.md`
</output>