---
phase: 60
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - lib/threadline/operator_surface/router.ex
  - lib/threadline/operator_surface/live/actor_live.ex
  - test/threadline/operator_surface/live/actor_live_test.exs
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - User can visit `/audit/actors/:kind/:id` and see a paginated list of transactions.
    - User can change the time window (default 24h).
    - User sees an explicit empty state if there are no events in the window.
    - User sees a distinct empty state if the actor has never recorded an event at all.
    - Transactions deep-link to the incident drill-down screen (`/audit/transactions/:id`).
  artifacts:
    - path: lib/threadline/operator_surface/live/actor_live.ex
      provides: LiveView component for actor history
    - path: lib/threadline/operator_surface/router.ex
      provides: New `/actors/:kind/:id` route
  key_links:
    - from: lib/threadline/operator_surface/router.ex
      to: lib/threadline/operator_surface/live/actor_live.ex
      via: live/3 route definition
    - from: lib/threadline/operator_surface/live/actor_live.ex
      to: lib/threadline.ex
      via: actor_history/2 call
---

<objective>
Turn `Threadline.actor_history/2` into a one-click answer for "what did this actor do recently" by rendering a paged LiveView with time-window picker and incident deep-links.

Purpose: Fulfill UI-02 requirement, ensuring the surface never crashes due to memory exhaustion by using Keyset Pagination and LiveView Streams.
Output: A new addressable screen `/audit/actors/:kind/:id` and integration test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/60-actor-window-screen/DECISIONS.md
@.planning/phases/60-actor-window-screen/60-PATTERNS.md
@.planning/phases/60-actor-window-screen/RESEARCH.md

<interfaces>
From lib/threadline/query/actor_history_page.ex (shipped in wave 1):
```elixir
defmodule Threadline.Query.ActorHistoryPage do
  defstruct [:entries, :next_cursor, :prev_cursor]
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Route and Module</name>
  <files>lib/threadline/operator_surface/router.ex, lib/threadline/operator_surface/live/actor_live.ex, test/threadline/operator_surface/live/actor_live_test.exs</files>
  <action>
    1. In `lib/threadline/operator_surface/router.ex`, add `live "/actors/:kind/:id", ActorLive, :show` inside the `threadline` scope block.
    2. Create `ActorLive` module matching the structure of `TransactionLive` (see PATTERNS.md).
    3. The mount should extract `:kind` and `:id`, reconstruct `%Threadline.Semantics.ActorRef{}`, and fetch the initial `actor_history` (default time boundary: last 24 hours).
    4. Implement explicit empty states per D-04: if the time-bounded page is empty, do a fast unbound check (limit 1) to see if the actor has *never* acted vs just having no events recently.
    5. Set up the `test/threadline/operator_surface/live/actor_live_test.exs` scaffold.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/actor_live_test.exs</automated>
  </verify>
  <done>Router compiles and `ActorLive` mounts successfully with proper empty state detection.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LiveView Stream and UI</name>
  <files>lib/threadline/operator_surface/live/actor_live.ex</files>
  <action>
    1. Render the returned `entries` using `phx-update="stream"` on the container and DOM IDs `id={"tx-#{tx.id}"}`.
    2. Add `phx-viewport-top="prev-page"` and `phx-viewport-bottom="next-page"` on the viewport container (refer to RESEARCH.md Pattern 1).
    3. Implement `handle_event("next-page")` to fetch older data (using `after: next_cursor`) and stream append.
    4. Implement `handle_event("prev-page")` to fetch newer data (using `before: prev_cursor`) and stream prepend.
    5. Provide a basic time-picker UI (e.g., standard `<select>` or links for 1h, 24h, 7d, 30d) that assigns new `:from` bounds and resets the stream.
    6. Ensure each rendered transaction has an `a` tag deep-linking to `/audit/transactions/#{tx.id}`.
  </action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/actor_live_test.exs</automated>
  </verify>
  <done>LiveView renders streaming lists, loads next pages seamlessly, and tests assert correct deep-links and empty states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL→App | `:kind` and `:id` are user inputs that map to actor models |
| App→DB | Pagination handlers map client cursor events to DB queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Information Disclosure | `ActorLive` | mitigate | URL parameters are correctly parsed into `ActorRef` to prevent broad system lookups |
| T-60-04 | Denial of Service | `ActorLive` events | mitigate | Validate cursors and bounds in `handle_event` so malicious payloads can't cause invalid DB queries |
</threat_model>

<verification>
`mix test test/threadline/operator_surface/live/actor_live_test.exs` validates successful mount, distinct empty states, and LiveView DOM stream presence.
</verification>

<success_criteria>
Visiting `/audit/actors/user/123` correctly shows a paginated stream of their last 24h transactions, or the correct explicit empty state.
</success_criteria>

<output>
After completion, create `.planning/phases/60-actor-window-screen/60-02-SUMMARY.md`
</output>