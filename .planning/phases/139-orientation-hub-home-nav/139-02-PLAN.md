---
phase: 139-orientation-hub-home-nav
plan: "02"
type: execute
wave: 2
depends_on: ["139-01"]
files_modified:
  - lib/threadline/operator_surface/live/start_live.ex
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/live/start_live_test.exs
  - test/threadline/operator_surface/style_contract_test.exs
autonomous: true
requirements: [POLISH-HOME]
must_haves:
  truths:
    - "Home remains a GDS-style orientation hub, not a dashboard or new workflow screen."
    - "Each persona has an obvious next existing action: P1/P2 to Timeline, P3 to Prove and Exports, P4 to health/Coverage/Retention/Exports, P5 to scope/empty diagnostic copy."
    - "Home health is quiet when clear and uses warning/danger severity when capture, export, or retention signals need attention."
    - "Actor-owned saved views render as a real resume affordance and other actors' saved views do not leak."
    - "Home does not implement Phase 140 record-first lookup, correlation paste/deep-link, closed export loop, or first-class row-history route."
  artifacts:
    - path: lib/threadline/operator_surface/live/start_live.ex
      provides: "Persona-oriented Home render, health severity, saved-view resume links"
      contains: "health_chip_class"
    - path: test/threadline/operator_surface/live/start_live_test.exs
      provides: "LiveView tests for Home orientation, health, saved views, and Phase 140 non-leakage"
      contains: "Pick up where you left off"
    - path: lib/threadline/operator_surface/style.ex
      provides: "Token-backed Home orientation, resume, and Prove handoff card styling"
      contains: ".tl-home__resume"
    - path: test/threadline/operator_surface/style_contract_test.exs
      provides: "Home CSS contract for scoped Phase 139 primitives"
      contains: "tl-home__resume"
  key_links:
    - from: lib/threadline/operator_surface/live/start_live.ex
      to: lib/threadline/operator_surface/components/surface_header.ex
      via: "current={:start} and preserved header assigns"
      pattern: "SurfaceHeader\\.surface_header"
    - from: lib/threadline/operator_surface/live/start_live.ex
      to: Threadline.OperatorSurface.Exports.FilterParams
      via: "saved_view_path/2 canonical query encoding"
      pattern: "FilterParams\\.canonical_query"
    - from: lib/threadline/operator_surface/live/start_live.ex
      to: Threadline.Governance.SavedView
      via: "actor-owned resume query"
      pattern: "from\\(v in SavedView"
---

<objective>
Polish Home as the Phase 139 orientation hub after the header IA foundation lands.

Purpose: operators should land on `/audit`, recognize the job family that matches their arrival context, and choose an existing next action without Home becoming a Phase 140 workflow screen.
Output: StartLive tests, Home render changes, health severity behavior, saved-view resume affordance, and scoped Home CSS contracts.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/139-orientation-hub-home-nav/139-CONTEXT.md
@.planning/phases/139-orientation-hub-home-nav/139-RESEARCH.md
@.planning/phases/139-orientation-hub-home-nav/139-PATTERNS.md
@.planning/phases/139-orientation-hub-home-nav/139-01-SUMMARY.md
@.planning/milestones/v1.31-PERSONAS-IA.md
@.planning/milestones/v1.31-UI-AUDIT.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-03-SUMMARY.md
@.planning/phases/135-seed-enrichment-ia-lock-in/135-04-SUMMARY.md
@lib/threadline/operator_surface/live/start_live.ex
@lib/threadline/operator_surface/components/surface_header.ex
@lib/threadline/operator_surface/style.ex
@lib/threadline/governance/saved_view.ex
@lib/threadline/governance/export_job.ex
@lib/threadline/governance/retention_run.ex
@test/threadline/operator_surface/style_contract_test.exs

<interfaces>
From `StartLive`:
- `mount/3` assigns `:base_path`, `:health`, `:health_enabled`, and `:saved_views`.
- `handle_params/3` derives `base_path`, `health_warnings(socket)`, `any_subsystem_enabled?(socket)`, and `fetch_saved_views(socket)`.
- `saved_view_path/2` uses `FilterParams.canonical_query/1`; preserve this.

From governance schemas:
- `SavedView` requires `name`, `actor_ref`, and `filters`.
- `ExportJob` requires `status` and `query_params`; `actor_ref` controls actor-owned failed export count.
- `RetentionRun` requires `status`; latest failed run controls the retention health signal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add StartLive Home orientation contracts</name>
  <files>test/threadline/operator_surface/live/start_live_test.exs</files>
  <behavior>
    - Build a StartLive test router and endpoint following the existing `TimelineLiveTest`, `ExportStatusLiveTest`, `EvidenceLiveTest`, and `RetentionHistoryLiveTest` patterns.
    - Mount `/audit` with all relevant feature flags enabled by supplying `coverage_authorize_fn`, `policy_authorize_fn`, `evidence_authorize_fn`, `export_authorize_fn`, and `repo: Threadline.Test.Repo`.
    - Use a session `threadline_actor_ref` for actor-owned saved-view and failed-export tests.
    - Assert Home renders the headline, Find card, Verify card, Prove card, and existing-destination links only.
    - Assert the redundant hero eyebrow is absent per F-303 and D-06.
    - Assert healthy Home renders `All systems healthy` with `tl-chip--success`.
    - Insert a failed `ExportJob` for the current actor and assert the failed export health link points to `/audit/exports` and renders with `tl-chip--danger`; insert another actor's failed job and assert it does not affect the current actor count.
    - Insert a failed `RetentionRun` and assert the retention health link points to `/audit/policy/retention` and renders with danger severity.
    - Insert actor-owned `SavedView` rows named `Recent deletes` and `Closed this week`; assert `Pick up where you left off` renders and links use canonical `/audit/timeline?...` URLs. Insert another actor's saved view and assert it does not render.
    - Assert the empty saved-view state is honest when there are no saved views.
    - Add a static non-leakage test that reads `start_live.ex` and refutes `<form`, `phx-submit`, `phx-change`, `name="record`, `name="correlation_id"`, `record_lookup`, and `push_patch` additions in Home per D-07.
  </behavior>
  <action>Create `Threadline.OperatorSurface.StartLiveTest` and write failing tests before editing `start_live.ex`. Implement D-06 through D-09 in tests: Home remains an orientation hub, health is quiet when clear, unhealthy signals have distinct severity, and saved views are a resume affordance. Implement D-07 as a negative source contract: Home can guide operators to existing destinations, but it must not add record lookup, correlation paste/deep-link, closed export loop, or row-history route behavior in this phase.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/start_live_test.exs</automated>
  </verify>
  <done>StartLive tests prove persona orientation, health severity, actor-owned saved-view rendering, honest empty resume copy, and Phase 140 non-leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Home orientation, health severity, and resume styling</name>
  <files>lib/threadline/operator_surface/live/start_live.ex, lib/threadline/operator_surface/style.ex, test/threadline/operator_surface/style_contract_test.exs</files>
  <behavior>
    - Style contract asserts Home Phase 139 classes exist: `.tl-home__health`, `.tl-home__resume`, `.tl-home__resume-empty`, `.tl-home__card-links`, and the Prove handoff class used before Exports.
    - Style contract asserts the Home section remains scoped, token-backed, dark-only, and dependency-free.
  </behavior>
  <action>Update `StartLive` after Task 1 RED. Remove the redundant `Threadline` hero eyebrow per F-303 while preserving the headline and lede. Keep Home card labels aligned with D-01 and D-02: Find, Verify, and Prove; do not rename the nav and do not create new workflow controls. Make each card's primary action point to an existing destination. For the Prove card, keep Evidence/Redaction/Retention grouped as proof controls and visually separate Exports as the handoff/deliverable destination per D-03 without implementing a closed export loop. Change health items from label-only maps to severity-aware maps: coverage gaps are warning, failed exports are danger, failed retention is danger, and the clear state remains a quiet success per D-08. Use a helper such as `health_chip_class/1` instead of string-building in HEEx. Render the resume section for both states: actor-owned saved views as links via existing `saved_view_path/2`, and an honest empty message when none exist per D-09. Update `style.ex` only with scoped `.tl-home__*` / `.tl-*` token-backed classes per D-10.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/live/start_live_test.exs test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <done>Home orients P1-P5 to existing next actions, uses severity-aware health, renders saved-view resume states, and does not ship Phase 140 flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView assigns to Home DOM | Actor, feature flags, coverage snapshot, saved views, failed exports, and retention rows influence rendered links and health chips. |
| Database to Home health/resume | Read-only existing queries provide saved views and health signals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-05 | Information disclosure | `fetch_saved_views/1` | mitigate | Keep actor_ref filter and add tests proving other actors' saved views do not render. |
| T-139-06 | Information disclosure | `failed_export_count/1` | mitigate | Keep actor_ref filter and test other actors' failed jobs do not affect current health. |
| T-139-07 | Tampering | Phase 140 input leakage | mitigate | Source contract refutes forms, `phx-submit`, `phx-change`, record inputs, correlation inputs, record lookup hooks, and Home `push_patch`. |
| T-139-08 | Denial of service | Health queries | accept | Existing read-only queries remain individually fail-safe; no new query surface or polling loop is added. |
| T-139-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are planned; if an executor attempts one, stop and run slopcheck/package legitimacy gate first. |
</threat_model>

<verification>
Focused commands:
- `mix test test/threadline/operator_surface/live/start_live_test.exs test/threadline/operator_surface/style_contract_test.exs`
- `rg -n "phx-submit|phx-change|record_lookup|name=\"correlation_id\"|push_patch" lib/threadline/operator_surface/live/start_live.ex` should return no Home-owned Phase 140 behavior.
- `git diff --name-only -- lib/threadline/operator_surface/router.ex priv/repo/migrations lib/threadline/query.ex lib/threadline/governance` should show no Phase 139 route/schema/query/backend expansion.
</verification>

<success_criteria>
- Home has no redundant hero eyebrow and still leads with clear orientation.
- Find, Verify, and Prove cards guide P1-P5 to existing destinations only.
- Failed exports and failed retention render as danger health chips; coverage gaps render as warning; healthy state renders quiet success.
- Saved views render as actor-owned resume links with canonical Timeline query strings and no cross-actor leakage.
- Empty saved-view state is honest and does not pretend a saved view exists.
- No Phase 140 flow implementation, route, query, schema, migration, backend API, Tailwind, shadcn, icon dependency, or JS-heavy nav behavior is introduced.
</success_criteria>

<output>
Create `.planning/phases/139-orientation-hub-home-nav/139-02-SUMMARY.md` when done.
</output>
