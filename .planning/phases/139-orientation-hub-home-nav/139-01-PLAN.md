---
phase: 139-orientation-hub-home-nav
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/threadline/operator_surface/components/surface_header.ex
  - lib/threadline/operator_surface/style.ex
  - test/threadline/operator_surface/surface_header_test.exs
  - test/threadline/operator_surface/style_contract_test.exs
autonomous: true
requirements: [POLISH-HOME]
must_haves:
  truths:
    - "The shared header keeps the locked Find / Verify / Prove IA, stable Verify nav label, Home brand link, skip link, scope chip, coverage badge, feature-flag visibility, and aria-current active state."
    - "Exports is visually separated as the Prove handoff destination without adding routes, queries, APIs, schemas, migrations, JS nav state, or dependencies."
    - "At 375px the header exposes all enabled destinations and group labels through a deliberate scrollable grouped nav pattern."
  artifacts:
    - path: lib/threadline/operator_surface/components/surface_header.ex
      provides: "Feature-flag-aware grouped operator nav with active-state and Exports handoff semantics"
      contains: "tl-topbar__nav-handoff"
    - path: lib/threadline/operator_surface/style.ex
      provides: "Token-backed mobile nav reachability and Prove handoff separator styling"
      contains: ".tl-topbar__nav-handoff"
    - path: test/threadline/operator_surface/surface_header_test.exs
      provides: "Component-level header IA, active-state, feature-flag, and preservation contracts"
      contains: "aria-current"
    - path: test/threadline/operator_surface/style_contract_test.exs
      provides: "Scoped dark-token CSS contract for Phase 139 header primitives"
      contains: "tl-topbar__nav-handoff"
  key_links:
    - from: lib/threadline/operator_surface/components/surface_header.ex
      to: lib/threadline/operator_surface/live/start_live.ex
      via: "brand link and current={:start} render with no active nav item"
      pattern: "current=\\{:start\\}|tl-topbar__brand"
    - from: lib/threadline/operator_surface/components/surface_header.ex
      to: lib/threadline/operator_surface/live/*_live.ex
      via: "current atom drives exactly one aria-current active destination"
      pattern: "aria-current"
    - from: lib/threadline/operator_surface/style.ex
      to: examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
      via: "scrollable .tl-topbar__nav is the tested 375px reachability surface"
      pattern: "overflow-x: auto|tl-topbar__nav-handoff"
---

<objective>
Make `SurfaceHeader` the locked IA source for Phase 139.

Purpose: every operator screen depends on the shared header for orientation. This plan tightens that one component before Home and browser UAT consume it.
Output: component tests, grouped nav markup, token-backed CSS, and CSS drift contracts.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/139-orientation-hub-home-nav/139-CONTEXT.md
@.planning/phases/139-orientation-hub-home-nav/139-RESEARCH.md
@.planning/phases/139-orientation-hub-home-nav/139-PATTERNS.md
@.planning/milestones/v1.31-PERSONAS-IA.md
@.planning/milestones/v1.31-UI-AUDIT.md
@lib/threadline/operator_surface/components/surface_header.ex
@lib/threadline/operator_surface/style.ex
@test/threadline/operator_surface/style_contract_test.exs

<interfaces>
From `lib/threadline/operator_surface/components/surface_header.ex`:
- `surface_header/1` attrs: `coverage`, `base_path`, `error`, `coverage_enabled`, `policy_enabled`, `evidence_enabled`, `exports_enabled`, `current`, `scoped`.
- `nav_link/1` renders `class=["tl-topbar__nav-item", @current == @page && "tl-topbar__nav-item--active"]`, `aria-current="page"` when current matches, and `data-testid="operator-nav-#{@page}"`.
- Current page atoms in use: `:start`, `:timeline`, `:coverage`, `:evidence`, `:policy`, `:retention`, `:exports`.

From `lib/threadline/operator_surface/style.ex`:
- `.tl-topbar__nav` is already the mobile scroll container.
- `.tl-topbar__nav-group` currently separates Find / Verify / Prove groups.
- `.tl-topbar__nav-label` is hidden until desktop; Phase 139 must keep labels reachable at 375px per D-05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SurfaceHeader IA contracts</name>
  <files>test/threadline/operator_surface/surface_header_test.exs</files>
  <behavior>
    - Render `SurfaceHeader.surface_header/1` directly with `Phoenix.LiveViewTest.render_component/2`.
    - With all feature flags enabled, assert the nav renders group labels `Find`, `Verify`, `Prove` and links with test IDs `operator-nav-timeline`, `operator-nav-coverage`, `operator-nav-evidence`, `operator-nav-policy`, `operator-nav-retention`, and `operator-nav-exports`.
    - Assert nav label `Verify` remains unchanged per D-02.
    - Assert brand link points to `base_path`, skip link points to `#tl-main`, scoped chip renders when `scoped: true`, and the coverage badge renders when `coverage_enabled: true` per D-04.
    - For `current` values `:timeline`, `:coverage`, `:evidence`, `:policy`, `:retention`, and `:exports`, assert exactly one `aria-current="page"` and the matching `operator-nav-*` link is active.
    - For `current: :start`, assert the brand link still points Home and no nav item has `aria-current="page"`.
    - Assert Exports is marked as the Prove handoff destination with `tl-topbar__nav-handoff` per D-03.
    - Assert disabled feature flags remove only their governed destinations and do not remove unrelated groups or the preserved header affordances per D-04.
  </behavior>
  <action>Create `Threadline.OperatorSurface.SurfaceHeaderTest` using the existing ExUnit component-test style. Use a small coverage map such as `%{uncovered_count: 0, last_checked_at: ~U[2026-06-04 00:00:00Z]}` and direct component renders; do not mount routers for this component contract. Write failing tests before changing `surface_header.ex`. The tests must implement D-01 through D-05 directly: stable Find / Verify / Prove grouping, Verify nav label, Exports handoff separation, preserved brand/skip/scope/coverage/feature flags, and current-atom active states.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/surface_header_test.exs</automated>
  </verify>
  <done>Header IA contracts fail before implementation and cover grouping, preservation, active states, feature flags, and Exports handoff semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement grouped nav handoff and mobile reachability primitives</name>
  <files>lib/threadline/operator_surface/components/surface_header.ex, lib/threadline/operator_surface/style.ex, test/threadline/operator_surface/style_contract_test.exs</files>
  <behavior>
    - Style contract asserts `.tl-topbar__nav`, `.tl-topbar__nav-group`, `.tl-topbar__nav-label`, `.tl-topbar__nav-handoff`, and `.tl-topbar__nav-item[aria-current="page"]` exist.
    - Style contract asserts the Phase 139 nav section remains token-backed with `var(--tl-`, does not add `@tailwind`, `from shadcn`, `prefers-color-scheme`, `color-scheme: light`, or raw hex colors outside existing token declarations.
    - Style contract asserts the nav label is not hidden at the mobile base layer.
  </behavior>
  <action>Update `surface_header.ex` after Task 1 RED. Keep the public `surface_header/1` attrs and private `nav_link/1` architecture intact per D-04. Add stable test IDs for nav groups if needed, keep `Find`, `Verify`, and `Prove` as the group labels per D-01 and D-02, and add a subtle handoff wrapper or class named `tl-topbar__nav-handoff` around the Exports link per D-03. Do not add a Home nav item; the brand link remains the Home link. Update `style.ex` in the existing topbar section only: keep `.tl-topbar__nav` as the scroll container, make group labels reachable at 375px, add token-backed handoff separation before Exports, preserve focus/hover/active states, and avoid JS-heavy nav behavior or new dependencies per D-05 and D-10.</action>
  <verify>
    <automated>mix test test/threadline/operator_surface/surface_header_test.exs test/threadline/operator_surface/style_contract_test.exs</automated>
  </verify>
  <done>`SurfaceHeader` preserves existing architecture while exposing the locked IA, Exports handoff separation, active states, feature-flag visibility, and mobile-reachable grouped nav primitives.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 139 | Home and surface_header reflect locked persona/JTBD IA with active states, grouping, mobile nav, and persona next actions | 139-01, 139-02, 139-03 | COVERED | Header foundation, Home orientation, browser proof.
REQ | POLISH-HOME | Home + surface_header IA, active states, grouping, mobile nav, GDS Home next action | 139-01, 139-02, 139-03 | COVERED | Requirement appears in every plan frontmatter.
RESEARCH | Header active state and feature-flag visibility | 139-01 | COVERED | Component test and implementation tasks.
RESEARCH | 375px nav reachability using existing scrollable grouped nav before disclosure escalation | 139-01, 139-03 | COVERED | CSS primitive plus browser UAT.
RESEARCH | StartLive Home orientation, saved views, quiet health row | 139-02, 139-03 | COVERED | Home tests and mobile Home UAT.
CONTEXT | D-01 | Keep Find / Verify / Prove nav backbone | 139-01 | COVERED | Task 1 and Task 2.
CONTEXT | D-02 | Keep Verify in nav | 139-01 | COVERED | Task 1 assertion.
CONTEXT | D-03 | Separate Exports as Prove handoff | 139-01, 139-02 | COVERED | Header handoff class and Home Prove card treatment.
CONTEXT | D-04 | Preserve brand, skip, flags, scope, coverage, active state | 139-01 | COVERED | Task 1 preservation assertions.
CONTEXT | D-05 | Preserve every destination at 375 | 139-01, 139-03 | COVERED | CSS primitive and Playwright UAT.
CONTEXT | D-06 | Home remains GDS orientation hub | 139-02, 139-03 | COVERED | Home implementation and mobile Home UAT.
CONTEXT | D-07 | Do not implement Phase 140 flows | 139-02, 139-03 | COVERED | Static/source and browser guards.
CONTEXT | D-08 | Quiet health row, P4 scan-friendly | 139-02 | COVERED | Health severity task.
CONTEXT | D-09 | Saved views render as resume affordance | 139-02, 139-03 | COVERED | Actor-owned SavedView tests and seeded browser UAT.
CONTEXT | D-10 | Keep scoped BEM/tokens in Style | 139-01, 139-02 | COVERED | Style contracts.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView assigns to header DOM | Auth, feature flags, scope, coverage, and current page atoms control visible navigation. |
| Browser at 375px to scrollable nav | The browser must reach every enabled link without hidden JS state or dropped DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-01 | Elevation of privilege | `surface_header.ex` feature-flag nav | mitigate | Preserve `coverage_enabled`, `policy_enabled`, `evidence_enabled`, and `exports_enabled` gates and test hidden destinations when disabled. |
| T-139-02 | Information disclosure | `surface_header.ex` scoped chip | mitigate | Preserve `scoped` assign and `operator-scope` chip, with component coverage. |
| T-139-03 | Tampering | `current` active state | mitigate | Use `aria-current` from the existing current atom only; test exactly one active nav item for each destination. |
| T-139-04 | Denial of service | Mobile nav layout | accept | CSS-only scrollable nav adds no event loop or network surface; browser UAT in Plan 03 catches overflow regressions. |
| T-139-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are planned; if an executor attempts one, stop and run slopcheck/package legitimacy gate first. |
</threat_model>

<verification>
Focused commands:
- `mix test test/threadline/operator_surface/surface_header_test.exs test/threadline/operator_surface/style_contract_test.exs`
- `rg -n "tl-topbar__nav-handoff|aria-current|operator-nav-exports" lib/threadline/operator_surface/components/surface_header.ex lib/threadline/operator_surface/style.ex test/threadline/operator_surface/surface_header_test.exs`
- `rg -n "@tailwind|from shadcn|prefers-color-scheme|color-scheme: light" lib/threadline/operator_surface/style.ex` should report only the existing dark-only contract context, with no new light/theme/dependency additions.
</verification>

<success_criteria>
- Header groups remain `Find`, `Verify`, and `Prove`.
- Brand Home link, skip link, scope chip, coverage badge, feature flags, and `aria-current` active-state behavior are preserved.
- Exports has a token-backed handoff separator before it in the Prove group.
- Mobile base CSS keeps labels and all enabled destinations reachable through the scrollable nav pattern.
- No routes, backend APIs, queries, schemas, migrations, package installs, Tailwind, shadcn, or icon dependencies are introduced.
</success_criteria>

<output>
Create `.planning/phases/139-orientation-hub-home-nav/139-01-SUMMARY.md` when done.
</output>
