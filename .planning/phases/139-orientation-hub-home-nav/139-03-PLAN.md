---
phase: 139-orientation-hub-home-nav
plan: "03"
type: execute
wave: 3
depends_on: ["139-01", "139-02"]
files_modified:
  - examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
autonomous: true
requirements: [POLISH-HOME]
must_haves:
  truths:
    - "At 375px Home exposes the orientation headline, job cards, health row, and saved-view resume affordance without document-level horizontal overflow."
    - "At 375px every enabled header destination remains reachable from every representative operator screen."
    - "The browser UAT stays scoped to Home/nav orientation and does not broaden into Phase 142 table/filter/drawer responsive audits."
  artifacts:
    - path: examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
      provides: "Focused Phase 139 browser UAT for 375px nav reachability and Home orientation"
      contains: "operator-home-nav"
  key_links:
    - from: examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
      to: lib/threadline/operator_surface/components/surface_header.ex
      via: "data-testid operator-nav-* links and grouped labels"
      pattern: "operator-nav-exports|operator-nav-retention"
    - from: examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts
      to: lib/threadline/operator_surface/live/start_live.ex
      via: "Home headline, cards, health, saved-view resume assertions"
      pattern: "Pick up where you left off|Follow what happened"
---

<objective>
Prove Phase 139 in the browser without expanding into the Phase 142 responsive sweep.

Purpose: ExUnit contracts prove markup and behavior; this plan proves a real 375px browser can reach the nav and understand Home's next actions.
Output: one focused Playwright spec for Home/nav orientation and reachability.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/139-orientation-hub-home-nav/139-CONTEXT.md
@.planning/phases/139-orientation-hub-home-nav/139-RESEARCH.md
@.planning/phases/139-orientation-hub-home-nav/139-PATTERNS.md
@.planning/phases/139-orientation-hub-home-nav/139-01-SUMMARY.md
@.planning/phases/139-orientation-hub-home-nav/139-02-SUMMARY.md
@.planning/phases/138-find-cluster-polish/138-VERIFICATION.md
@examples/threadline_phoenix/e2e/tests/operator-find-mobile.spec.ts
@examples/threadline_phoenix/e2e/tests/operator-prove-mobile.spec.ts
@examples/threadline_phoenix/e2e/package.json

<interfaces>
From existing mobile specs:
- `test.use({ viewport: { width: 375, height: 812 }, isMobile: true })`.
- `login(page)` goes through `/users/log_in` with `admin@example.com` and `DEMO_SEED_PASSWORD ?? "password123456"`.
- `expectNoHorizontalOverflow(page)` checks `document.documentElement.scrollWidth - clientWidth <= 1`.
- The example app seeded admin has saved views named `Recent deletes` and `Closed this week` from Phase 135.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Home orientation mobile UAT</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts</files>
  <behavior>
    - Use the existing login and no-overflow helper patterns from `operator-find-mobile.spec.ts` and `operator-prove-mobile.spec.ts`.
    - At 375px, login as admin, visit `/audit`, and assert the Home headline `Follow what happened.` is visible.
    - Assert Home exposes the Find, Verify, and Prove card titles and existing destination actions.
    - Assert the Home health row is present and does not force document-level horizontal overflow.
    - Assert `Pick up where you left off`, `Recent deletes`, and `Closed this week` are visible or reachable.
    - Assert Home has no form, no record input, and no correlation input in the main content per D-07.
  </behavior>
  <action>Create `operator-home-nav-mobile.spec.ts` as a focused Phase 139 spec. Copy helpers from existing mobile specs instead of introducing shared helper churn. Keep assertions on Home/nav orientation only: headline, cards, health row, saved-view resume, existing links, and no document-level horizontal overflow. Implement D-06, D-07, D-08, and D-09 in browser terms; do not assert table stacking, filter drawer behavior, row density, or full responsive matrix details owned by Phase 142.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-home-nav-mobile.spec.ts</automated>
  </verify>
  <done>Mobile Home UAT proves orientation and saved-view resume behavior at 375px without adding Phase 140 controls or Phase 142 responsive scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 375px nav reachability across representative screens</name>
  <files>examples/threadline_phoenix/e2e/tests/operator-home-nav-mobile.spec.ts</files>
  <behavior>
    - Define a helper that targets `.tl-topbar__nav`, scrolls each `operator-nav-*` link into view, asserts it is visible, and asserts its `href` is the expected existing route.
    - From `/audit`, `/audit/timeline`, `/audit/coverage`, `/audit/evidence`, `/audit/policy/redaction`, `/audit/policy/retention`, and `/audit/exports`, assert Timeline, Coverage, Evidence, Redaction, Retention, and Exports are reachable at 375px.
    - Assert group labels `Find`, `Verify`, and `Prove` exist in the header DOM and are visible or reachable through the nav scroll container.
    - Assert `expectNoHorizontalOverflow(page)` after each representative screen visit.
    - From Home, click each enabled nav destination at least once and assert the expected URL path, then return to `/audit` for the next destination.
  </behavior>
  <action>Extend the same spec with a nav-focused test block for D-01 through D-05. Treat `.tl-topbar__nav` as the intentional horizontal scroll container; the document root must not horizontally overflow. Prove every enabled destination remains reachable from every representative screen, including Retention and Exports. Keep this as a reachability test, not a broad visual audit: no screenshot baselines, no table/drawer/filter assertions, no new dependencies, and no app code changes in this plan.</action>
  <verify>
    <automated>cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-home-nav-mobile.spec.ts</automated>
  </verify>
  <done>Browser UAT proves 375px nav reachability across representative screens and Home activation of every enabled destination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser automation to example app | Playwright exercises seeded demo state and authenticated admin navigation. |
| Header DOM to mobile viewport | CSS scroll container must expose links without hidden JS state or dropped destinations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-09 | Repudiation | Browser proof | mitigate | Spec asserts concrete routes, visible labels, saved-view names, and no-overflow calculations rather than relying on screenshots alone. |
| T-139-10 | Information disclosure | Scoped nav/status affordances | transfer | Component-level scoped-chip preservation is covered in Plan 01; this admin e2e path remains unscoped by design. |
| T-139-11 | Denial of service | Playwright spec runtime | accept | One focused spec with existing login helper and no new browser dependency keeps runtime bounded. |
| T-139-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are planned; use existing `@playwright/test` from the example app. |
</threat_model>

<verification>
Before running the Playwright command, ensure the example app is available on `http://127.0.0.1:4002` using the existing example-app workflow. Then run:
- `cd examples/threadline_phoenix/e2e && E2E_BASE_URL=http://127.0.0.1:4002 npm test -- tests/operator-home-nav-mobile.spec.ts`

Optional focused cross-check after Plans 01-03:
- `mix test test/threadline/operator_surface/surface_header_test.exs test/threadline/operator_surface/live/start_live_test.exs test/threadline/operator_surface/style_contract_test.exs`
</verification>

<success_criteria>
- Home at 375px shows orientation headline, Find/Verify/Prove next-action cards, health row, and saved-view resume content from the seeded admin path.
- The main Home content has no record/correlation form or input.
- Timeline, Coverage, Evidence, Redaction, Retention, and Exports are reachable through the header from every representative screen at 375px.
- The document root has no horizontal overflow after every checked screen.
- The spec does not attempt Phase 142 broad responsive checks.
</success_criteria>

<output>
Create `.planning/phases/139-orientation-hub-home-nav/139-03-SUMMARY.md` when done.
</output>
