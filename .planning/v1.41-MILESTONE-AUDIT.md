---
milestone: v1.41
name: "Green, Clean, and Honest"
audited: 2026-09-24
audited_at_head: 2fea3519
branch: fix/branch-protection-actions-capability
supersedes: "fe896464 re-audit at f74eb64f (tech_debt, W1/W2 open); 57c6fd58 re-audit at 1f834def (tech_debt); 7c548d1d audit at 51c1cb90 (gaps_found)"
status: gaps_found
scores:
  requirements: 46/54
  phases: 10/10
  integration: 19/20
  flows: 5/6
gaps:
  requirements:
    - id: "GATE-01"
      status: "unsatisfied"
      phase: "203"
      claimed_by_plans: ["203-01..203-10"]
      completed_by_plans: []
      verification_status: "passed at 203; regressed at HEAD"
      evidence: "`MIX_ENV=test mix credo --strict` exits 2 on 2fea3519 (re-run by the auditor, not only the checker): 2 Credo.Check.Design.AliasUsage findings at test/threadline/capture/trigger_rerun_test.exs:219:11 and :279:25 (`Mix.Tasks.Threadline.Gen.Triggers.run(`). Both came in with 207 review-fix commit 35ec16b8. CI job verify-credo (ci.yml:107,130) is a ci-required member (ci.yml:928), so a PR from this branch goes red."
    - id: "GATE-02"
      status: "unsatisfied"
      phase: "203"
      claimed_by_plans: ["203-01..203-10"]
      completed_by_plans: []
      verification_status: "passed at 203; regressed at HEAD"
      evidence: "Every credo finding must be fixed or registered. The two above are neither. The credo contract tests stay green because they pin config shape, not run output. 207-03-SUMMARY.md:175 ('no issues in 323 files') predates 35ec16b8 and is stale."
    - id: "GATE-03..GATE-05"
      status: "partial"
      phase: "203"
      claimed_by_plans: ["203-01..203-10"]
      completed_by_plans: []
      verification_status: "passed"
      evidence: "Dialyzer 0 errors, xref cycles none, compile_no_optional, and layer boundary re-run green at 2fea3519. 0 of 10 203 SUMMARY files carry requirements-completed frontmatter. Bookkeeping only."
    - id: "GREEN-07"
      status: "accepted-pending"
      phase: "198"
      claimed_by_plans: ["198-02", "198-03", "198-05", "..."]
      completed_by_plans: []
      verification_status: "passed (terminal disposition)"
      evidence: "Maintainer option-a in 198-39-DECISION.md. HEAD is 509 ahead / 0 behind origin/main, so 'origin/main contains every local commit' stays false until the branch lands."
  integration:
    - from: "207 (35ec16b8 review fix)"
      to: "203 credo gate / CI verify-credo"
      issue: "credo --strict red: AliasUsage x2 in trigger_rerun_test.exs:219,279. Fix: `alias Mix.Tasks.Threadline.Gen.Triggers` then call `Triggers.run(...)`"
      affected: ["GATE-01", "GATE-02"]
  flows:
    - name: "PR -> CI green -> release-please"
      breaks_at: "verify-credo (ci-required member)"
tech_debt:
  - phase: 207-gen-triggers-rerun-name-collision-storage-schema-default-doc
    items:
      - "W-A: 4 releasable subjects leak planning IDs (35ec16b8, 917418ff, 292c0173, 660e2ad3 `fix(207): CR-01/WR-0x ...`); 917418ff and 660e2ad3 touch only CHANGELOG.md but are typed fix"
      - "Deferred from review: two per-table tables whose suffixes share their first 36 bytes still collide on the 63-byte function name; this needs a public-behaviour decision (a clear Mix.raise, or a hashed name)"
      - "Review IN-01..IN-05 open: a re-enable after an explicit drop migration is classified as a rerun (false comment); a default->per-table rerun leaks a function after a full rollback chain; rerun? sees only scanned .exs (squashed or structure.sql originals); the doc-default guard is line-scoped with a loose floor; the checklist's rolled-back redaction 'Drift detected' claim is conditional"
      - "207-VALIDATION.md status draft (NOT-VALIDATED)"
      - "clean_checkout_contract_test.exs:276 temp-dir collision after an interrupted run (System.unique_integer restarts per VM); verifier observation, unrelated to 207"
  - phase: 206-installer-migration-versions
    items:
      - "W3 (still open): gen.triggers hardcodes priv/repo/migrations (gen.triggers.ex:161); the new TriggerMigration.scan/1 scans the same path, so they are at least consistent"
      - "W4: five `fix(206): WR-01/IN-0x` subjects"
      - "206-VALIDATION.md status draft (NOT-VALIDATED)"
  - phase: landing
    items:
      - "W-B: origin/main..HEAD holds 42 releasable subjects, 31 with a phase-ID scope; 202's feat commits already shipped squashed as #43. The branch MUST be squash-landed, or release-please re-announces them and bumps to 0.11.0 instead of a patch"
  - phase: 205-release-reconciliation
    items:
      - "WR-02: sync-pins contract test does not pin permissions, if: guards, or PUSH_TOKEN binding site"
      - "WR-04: bump-rehearsal hardcoded-pin negative control injects ~> $CURRENT (should be ~> ${CURRENT%.*}.0)"
      - "WR-01, IN-01..IN-04 carried byte-identical from origin/main"
  - phase: 198-green-bringup
    items:
      - "GREEN-12 hygiene: 2 local branches have no remote tip and no archive tag (fix/branch-protection-metadata-read 04b76a5b, fix/branch-protection-read-access 1abde8c7)"
      - "release.yml:5-6 comment still names deleted hex-publish.yml as a fallback path"
      - "REQUIREMENTS.md:14 GREEN-04 checkbox is [ ] while traceability says Complete"
      - "VALIDATION.md nyquist_compliant: false (GREEN-07 only)"
  - phase: 199-decouple
    items:
      - "deferred-items.md: 5 open (formatter drift, mechanical checker handoff, pair-label token TODO, locked dep advisories, full-suite dialyzer contract drift)"
  - phase: 202-release-0-10-0
    items:
      - "WR-06/07 open (env-protection check gap, stale rehearsal tarball); intermittent CriticTrustTest flake; 2 accepted overrides"
  - phase: 203-real-gates
    items:
      - "WR-01: credo config-shape contract does not pin files:/plugins:/requires:; this is also why B1 escaped every test (no contract asserts credo run output)"
      - "F5: layer-boundary gate is a substring scan"
      - "SUMMARY files lack requirements-completed frontmatter"
  - phase: 204-structure
    items:
      - "WR-02: ci.all dedup guard raises alias cycle on a legal self-referencing alias"
      - "WR-03: banner gate misses some comment-rule forms"
      - "IN-08: critic.synth path checks without symlink resolution"
  - phase: repo
    items:
      - "CLAUDE.md now points into .planning/PROJECT.md (not in the Hex tarball; planning-independence contract passes; harmless)"
resolved_since_prior_audit:
  - "W1 (gen.triggers rerun duplicated the Ecto migration name/module): fixed by 207. The checker independently ran install -> gen.triggers x4 -> migrate in a fresh host on the Hex-built package: _posts, _posts_2, _posts_3, _posts_4 all applied; rollback keeps capture on; all-pending migrate and rollback --all round-trip clean"
  - "W2 (storage_schema default stated as `threadline`): fixed by 207 in 4 guides, with a guard test over guides/ and README.md; grep finds no remaining claim"
nyquist:
  compliant_phases: [199, 200, 201]
  partial_phases: [198]
  not_validated_phases: [202, 203, 204, 205, 206, 207]
  missing_phases: []
  overall: partial
---

# v1.41 Milestone Audit: Green, Clean, and Honest (third re-audit)

**Status: gaps_found.** Re-audited 2026-09-24 at HEAD `2fea3519`, after Phase 207. This replaces the re-audit at `f74eb64f`.

## Summary

Phase 207 did its job. W1 and W2 are closed, and the integration checker confirmed W1 against a real database using a fresh host app on the Hex-built package. A rerun of `gen.triggers` now gets a distinct Ecto name and module, applies in place, and keeps capture on after a rollback. Every guide now states the real `public` default.

One 207 review-fix commit (`35ec16b8`, CR-01) regressed the Phase 203 credo gate. `mix credo --strict` exits 2 with two `AliasUsage` findings in `test/threadline/capture/trigger_rerun_test.exs:219` and `:279`. `verify-credo` is a `ci-required` member, so this branch cannot land green, and GATE-01/GATE-02 no longer hold at HEAD. The fix is a one-line alias. No test caught it because the credo contract tests check the config shape, not the run output (203 WR-01).

## Phases

| Phase | Verification | Nyquist |
|---|---|---|
| 198 Green Bringup | passed 6/6 | PARTIAL (GREEN-07) |
| 199 Decouple | passed 40/40 | COMPLIANT |
| 200 Public Surface | passed 8/8 | COMPLIANT |
| 201 Rendered Output | passed 6/6 | COMPLIANT |
| 202 Release 0.10.0 | passed 5/5 SC (2 overrides) | NOT-VALIDATED |
| 203 Real Gates | passed 6/6 (**credo regressed at HEAD**) | NOT-VALIDATED |
| 204 Structure | passed 6/6 | NOT-VALIDATED |
| 205 Release Reconciliation | passed 14/14 | NOT-VALIDATED |
| 206 Installer Migration Versions | passed 15/15 (no REQ-ID) | NOT-VALIDATED |
| 207 gen.triggers Rerun + storage_schema Docs | passed 16/16 (no REQ-ID; closes W1, W2) | NOT-VALIDATED |

## Requirements (3-source cross-reference)

| Group | Count | VERIFICATION | SUMMARY | REQUIREMENTS.md | Final |
|---|---|---|---|---|---|
| GREEN-01..06, 08..12 | 11 | passed | listed | [x] (GREEN-04 box unticked) | satisfied |
| GREEN-07 | 1 | terminal disposition | listed | [ ] | accepted-Pending |
| DECOUPLE-01..08 | 8 | passed | listed | [x] | satisfied |
| SURFACE-01..11 | 11 | passed | listed | [x] | satisfied |
| RENDER-01..06 | 6 | passed | listed | [x] | satisfied |
| RELEASE-01..05 | 5 | passed | listed | [x] | satisfied (landing warnings W-A/W-B) |
| GATE-01, GATE-02 | 2 | passed at 203, **red at HEAD** | missing | [x] | **unsatisfied** |
| GATE-03..05 | 3 | passed, re-run green | missing | [x] | partial (bookkeeping) |
| STRUCT-01..07 | 7 | passed | listed | [x] | satisfied |

46 of 54 are satisfied, 2 are unsatisfied (a regression, not a phase that failed its own verification), 5 are partial for bookkeeping reasons only, and 1 is accepted-Pending. Nothing is orphaned. Phases 206 and 207 map to no REQ-ID.

## Integration (19/20) and flows (5/6)

| Flow | Status |
|---|---|
| 1. Adopter install -> migrate | holds (fresh host, Hex-built package) |
| 2. install -> gen.triggers -> migrate -> gen.triggers again -> migrate (new) | holds, through 4 runs, a mode switch, rollback, and all-pending migrate |
| 3. Drift remediation as the guides instruct | holds (production-checklist.md:45 and domain-reference.md:52-55 match observed behavior) |
| 4. Release pins / Hex build | holds (0 of 20 pins differ; the tarball includes trigger_migration.ex) |
| 5. Fresh clone -> gates, earlier contracts | holds (1843 tests, 0 failures) |
| 6. PR -> CI green -> release-please | **breaks at verify-credo** |

The 207 code passes every other earlier contract: planning independence, public surface (`TriggerMigration` is `@moduledoc false`), vocabulary and copy, layer boundary, xref cycles, dialyzer (0 errors), and the 204 size limits. The CHANGELOG Unreleased block has no planning IDs.

## Gates re-run on HEAD

| Command | Result |
|---|---|
| `MIX_ENV=test mix test` (full) | 1843 tests, 0 failures, 1 excluded |
| `mix credo --strict` | **rc=2**: AliasUsage at trigger_rerun_test.exs:219, 279 (re-run by the auditor) |
| `mix verify.format`, `compile --warnings-as-errors --force` | rc=0 |
| `mix verify.xref_cycles`, `verify.compile_no_optional` | rc=0 |
| `MIX_ENV=dev mix verify.dialyzer` | 0 errors |
| `MIX_ENV=dev mix release.pins --check` | 0 of 20 differ (`~> 0.10.0`) |
| `mix hex.build --unpack` | OK; includes `lib/threadline/mix/trigger_migration.ex` |
| Temp host E2E (DB 5433) | flows 1-3 as above; the scratch DB was dropped afterwards |

The browser, example, and hex_evaluator lanes were not run.

## Closing the gap

1. **B1:** in `test/threadline/capture/trigger_rerun_test.exs`, add `alias Mix.Tasks.Threadline.Gen.Triggers` and call `Triggers.run(...)` at :219 and :279, then re-run `mix verify.credo`. This is a plain `test:` or `fix(test):` commit, and it should carry no planning ID.
2. **Optional hardening (203 WR-01):** add a contract that asserts `mix credo --strict` exits 0, so config-shape tests cannot hide a red run again.
3. **On landing:** squash-merge (W-B), with a PR title that carries no phase or finding IDs (W-A, W4).
