/**
 * label.ts — Golden-set authoring CLI stub (Plan 07).
 *
 * This module is implemented in Plan 07 alongside report.ts.
 * The stub is here so run.ts can lazily import it without TypeScript errors.
 */

export async function runLabel(_argv: string[]): Promise<void> {
  throw new Error(
    "[critic label] Not yet implemented — this subcommand lands in Plan 07.\n" +
      "See .planning/golden/golden-set.json for the current golden-set skeleton.",
  );
}
