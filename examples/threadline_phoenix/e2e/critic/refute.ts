/**
 * refute.ts — Refute-battery runner stub (Plan 06).
 *
 * This module is implemented in Plan 06 alongside panel.ts.
 * The stub is here so run.ts can lazily import it without TypeScript errors.
 */

export async function runValidate(_argv: string[]): Promise<void> {
  throw new Error(
    "[critic validate] Not yet implemented — this subcommand lands in Plan 06.\n" +
      "Run: npm run critic:score -- --refute-only once Plan 06 is complete.",
  );
}
