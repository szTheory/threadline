/**
 * rubric.ts — Rubric lint and hash checker stub (Plan 07).
 *
 * This module is implemented in Plan 07 (rubric hash computation + lint).
 * The stub is here so run.ts can lazily import it without TypeScript errors.
 */

export async function runRubric(_argv: string[]): Promise<void> {
  throw new Error(
    "[critic rubric] Not yet implemented — this subcommand lands in Plan 07.\n" +
      "Rubric files are in e2e/critic/rubrics/<lens>.md.",
  );
}
