defmodule ThreadlineTriggersShapeCodeKeyedShapeComposiF2f800f9eb80 do
  use Ecto.Migration

  def up do
    execute "CREATE OR REPLACE FUNCTION \"threadline\".\"threadline_capture_changes\"()\nRETURNS TRIGGER\nLANGUAGE plpgsql\nAS $threadline_trigger$\nDECLARE\n  v_txid           bigint;\n  v_tx_id          uuid;\n  v_row            jsonb;\n  v_data_after     jsonb;\n  v_table_pk       jsonb;\n  v_changed_fields text[];\nBEGIN\n  v_txid := txid_current();\n\n  -- Upsert the audit_transactions row keyed on the PostgreSQL transaction ID.\n  -- ON CONFLICT DO NOTHING is idempotent: multiple writes in the same transaction\n  -- reuse the existing row. This is PgBouncer-safe because txid_current() is\n  -- transaction-scoped, not session-scoped.\n  INSERT INTO \"threadline\".\"audit_transactions\" (id, txid, occurred_at, actor_ref)\n  VALUES (\n    gen_random_uuid(),\n    v_txid,\n    clock_timestamp(),\n    NULLIF(current_setting('threadline.actor_ref', true), '')::jsonb\n  )\n  ON CONFLICT (txid) DO NOTHING;\n\n  SELECT id INTO v_tx_id\n  FROM \"threadline\".\"audit_transactions\"\n  WHERE txid = v_txid;\n\n\n  IF TG_OP = 'DELETE' THEN\n    v_row := to_jsonb(OLD);\n    IF TG_NARGS = 0 THEN\n      IF v_row ? 'id' THEN\n        v_table_pk := jsonb_build_object('id', v_row ->> 'id');\n      ELSE\n        v_table_pk := '{}'::jsonb;\n      END IF;\n    ELSE\n      v_table_pk := '{}'::jsonb;\n      FOR i IN 0 .. TG_NARGS - 1 LOOP\n        IF (v_row ->> TG_ARGV[i]) IS NULL THEN\n          v_table_pk := '{}'::jsonb;\n          EXIT;\n        END IF;\n        v_table_pk := coalesce(v_table_pk, '{}'::jsonb)\n                      || jsonb_build_object(TG_ARGV[i], v_row ->> TG_ARGV[i]);\n      END LOOP;\n    END IF;\n    v_table_pk := coalesce(v_table_pk, '{}'::jsonb);\n\n    v_data_after     := NULL;\n    v_changed_fields := NULL;\n\n  ELSIF TG_OP = 'INSERT' THEN\n    v_row := to_jsonb(NEW);\n    IF TG_NARGS = 0 THEN\n      IF v_row ? 'id' THEN\n        v_table_pk := jsonb_build_object('id', v_row ->> 'id');\n      ELSE\n        v_table_pk := '{}'::jsonb;\n      END IF;\n    ELSE\n      v_table_pk := '{}'::jsonb;\n      FOR i IN 0 .. TG_NARGS - 1 LOOP\n        IF (v_row ->> TG_ARGV[i]) IS NULL THEN\n          v_table_pk := '{}'::jsonb;\n          EXIT;\n        END IF;\n        v_table_pk := coalesce(v_table_pk, '{}'::jsonb)\n                      || jsonb_build_object(TG_ARGV[i], v_row ->> TG_ARGV[i]);\n      END LOOP;\n    END IF;\n    v_table_pk := coalesce(v_table_pk, '{}'::jsonb);\n\n    v_data_after     := v_row;\n    v_changed_fields := NULL;\n\n  ELSE\n    -- UPDATE: capture changed field names\n    v_row := to_jsonb(NEW);\n    IF TG_NARGS = 0 THEN\n      IF v_row ? 'id' THEN\n        v_table_pk := jsonb_build_object('id', v_row ->> 'id');\n      ELSE\n        v_table_pk := '{}'::jsonb;\n      END IF;\n    ELSE\n      v_table_pk := '{}'::jsonb;\n      FOR i IN 0 .. TG_NARGS - 1 LOOP\n        IF (v_row ->> TG_ARGV[i]) IS NULL THEN\n          v_table_pk := '{}'::jsonb;\n          EXIT;\n        END IF;\n        v_table_pk := coalesce(v_table_pk, '{}'::jsonb)\n                      || jsonb_build_object(TG_ARGV[i], v_row ->> TG_ARGV[i]);\n      END LOOP;\n    END IF;\n    v_table_pk := coalesce(v_table_pk, '{}'::jsonb);\n\n    v_data_after := v_row;\n\n    SELECT array_agg(n.key ORDER BY n.key)\n    INTO   v_changed_fields\n    FROM   jsonb_each(to_jsonb(NEW)) AS n\n    JOIN   jsonb_each(to_jsonb(OLD)) AS o ON n.key = o.key\n    WHERE  n.value IS DISTINCT FROM o.value;\n  END IF;\n\n  INSERT INTO \"threadline\".\"audit_changes\" (\n    id, transaction_id, table_schema, table_name,\n    table_pk, op, data_after, changed_fields, changed_from, captured_at\n  ) VALUES (\n    gen_random_uuid(), v_tx_id, TG_TABLE_SCHEMA, TG_TABLE_NAME,\n    v_table_pk, lower(TG_OP), v_data_after, v_changed_fields, NULL::jsonb, clock_timestamp()\n  );\n\n\n  IF TG_OP = 'DELETE' THEN\n    RETURN OLD;\n  END IF;\n  RETURN NEW;\nEND;\n$threadline_trigger$\n"

    execute "CREATE OR REPLACE FUNCTION \"threadline\".\"threadline_capture_changes_shape_twin\"()\nRETURNS TRIGGER\nLANGUAGE plpgsql\nAS $threadline_trigger$\nDECLARE\n  v_txid           bigint;\n  v_tx_id          uuid;\n  v_row            jsonb;\n  v_data_after     jsonb;\n  v_table_pk       jsonb;\n  v_changed_fields text[];\n  v_changed_from   jsonb;\nBEGIN\n  v_txid := txid_current();\n\n  -- Upsert the audit_transactions row keyed on the PostgreSQL transaction ID.\n  -- ON CONFLICT DO NOTHING is idempotent: multiple writes in the same transaction\n  -- reuse the existing row. This is PgBouncer-safe because txid_current() is\n  -- transaction-scoped, not session-scoped.\n  INSERT INTO \"threadline\".\"audit_transactions\" (id, txid, occurred_at, actor_ref)\n  VALUES (\n    gen_random_uuid(),\n    v_txid,\n    clock_timestamp(),\n    NULLIF(current_setting('threadline.actor_ref', true), '')::jsonb\n  )\n  ON CONFLICT (txid) DO NOTHING;\n\n  SELECT id INTO v_tx_id\n  FROM \"threadline\".\"audit_transactions\"\n  WHERE txid = v_txid;\n\n\n  IF TG_OP = 'DELETE' THEN\n    v_row := to_jsonb(OLD);\n    IF TG_NARGS = 0 THEN\n      IF v_row ? 'id' THEN\n        v_table_pk := jsonb_build_object('id', v_row ->> 'id');\n      ELSE\n        v_table_pk := '{}'::jsonb;\n      END IF;\n    ELSE\n      v_table_pk := '{}'::jsonb;\n      FOR i IN 0 .. TG_NARGS - 1 LOOP\n        IF (v_row ->> TG_ARGV[i]) IS NULL THEN\n          v_table_pk := '{}'::jsonb;\n          EXIT;\n        END IF;\n        v_table_pk := coalesce(v_table_pk, '{}'::jsonb)\n                      || jsonb_build_object(TG_ARGV[i], v_row ->> TG_ARGV[i]);\n      END LOOP;\n    END IF;\n    v_table_pk := coalesce(v_table_pk, '{}'::jsonb);\n\n    v_data_after     := NULL;\n    v_changed_fields := NULL;\n    v_changed_from   := NULL;\n\n  ELSIF TG_OP = 'INSERT' THEN\n    v_row := to_jsonb(NEW);\n    IF TG_NARGS = 0 THEN\n      IF v_row ? 'id' THEN\n        v_table_pk := jsonb_build_object('id', v_row ->> 'id');\n      ELSE\n        v_table_pk := '{}'::jsonb;\n      END IF;\n    ELSE\n      v_table_pk := '{}'::jsonb;\n      FOR i IN 0 .. TG_NARGS - 1 LOOP\n        IF (v_row ->> TG_ARGV[i]) IS NULL THEN\n          v_table_pk := '{}'::jsonb;\n          EXIT;\n        END IF;\n        v_table_pk := coalesce(v_table_pk, '{}'::jsonb)\n                      || jsonb_build_object(TG_ARGV[i], v_row ->> TG_ARGV[i]);\n      END LOOP;\n    END IF;\n    v_table_pk := coalesce(v_table_pk, '{}'::jsonb);\n\n    v_data_after     := v_row;\n        v_data_after := v_data_after || jsonb_build_object('note', to_jsonb('[REDACTED]'::text));\n\n    v_changed_fields := NULL;\n    v_changed_from   := NULL;\n\n  ELSE\n    v_row := to_jsonb(NEW);\n    IF TG_NARGS = 0 THEN\n      IF v_row ? 'id' THEN\n        v_table_pk := jsonb_build_object('id', v_row ->> 'id');\n      ELSE\n        v_table_pk := '{}'::jsonb;\n      END IF;\n    ELSE\n      v_table_pk := '{}'::jsonb;\n      FOR i IN 0 .. TG_NARGS - 1 LOOP\n        IF (v_row ->> TG_ARGV[i]) IS NULL THEN\n          v_table_pk := '{}'::jsonb;\n          EXIT;\n        END IF;\n        v_table_pk := coalesce(v_table_pk, '{}'::jsonb)\n                      || jsonb_build_object(TG_ARGV[i], v_row ->> TG_ARGV[i]);\n      END LOOP;\n    END IF;\n    v_table_pk := coalesce(v_table_pk, '{}'::jsonb);\n\n    v_data_after := v_row;\n        v_data_after := v_data_after || jsonb_build_object('note', to_jsonb('[REDACTED]'::text));\n\n\n    SELECT array_agg(n.key ORDER BY n.key)\n    INTO   v_changed_fields\n    FROM   jsonb_each(to_jsonb(NEW)) AS n\n    JOIN   jsonb_each(to_jsonb(OLD)) AS o ON n.key = o.key\n    WHERE  n.value IS DISTINCT FROM o.value\n    AND NOT (n.key = ANY(ARRAY[]::text[]));\n\n        v_changed_from := NULL::jsonb;\n\n  END IF;\n\n  INSERT INTO \"threadline\".\"audit_changes\" (\n    id, transaction_id, table_schema, table_name,\n    table_pk, op, data_after, changed_fields, changed_from, captured_at\n  ) VALUES (\n    gen_random_uuid(), v_tx_id, TG_TABLE_SCHEMA, TG_TABLE_NAME,\n    v_table_pk, lower(TG_OP), v_data_after, v_changed_fields, v_changed_from, clock_timestamp()\n  );\n\n\n  IF TG_OP = 'DELETE' THEN\n    RETURN OLD;\n  END IF;\n  RETURN NEW;\nEND;\n$threadline_trigger$\n"

    execute "CREATE OR REPLACE FUNCTION \"threadline\".\"threadline_capture_changes_shapes_shape_twin_c9a9dcb238e7\"()\nRETURNS TRIGGER\nLANGUAGE plpgsql\nAS $threadline_trigger$\nDECLARE\n  v_txid           bigint;\n  v_tx_id          uuid;\n  v_row            jsonb;\n  v_data_after     jsonb;\n  v_table_pk       jsonb;\n  v_changed_fields text[];\n  v_changed_from   jsonb;\nBEGIN\n  v_txid := txid_current();\n\n  -- Upsert the audit_transactions row keyed on the PostgreSQL transaction ID.\n  -- ON CONFLICT DO NOTHING is idempotent: multiple writes in the same transaction\n  -- reuse the existing row. This is PgBouncer-safe because txid_current() is\n  -- transaction-scoped, not session-scoped.\n  INSERT INTO \"threadline\".\"audit_transactions\" (id, txid, occurred_at, actor_ref)\n  VALUES (\n    gen_random_uuid(),\n    v_txid,\n    clock_timestamp(),\n    NULLIF(current_setting('threadline.actor_ref', true), '')::jsonb\n  )\n  ON CONFLICT (txid) DO NOTHING;\n\n  SELECT id INTO v_tx_id\n  FROM \"threadline\".\"audit_transactions\"\n  WHERE txid = v_txid;\n\n\n  IF TG_OP = 'DELETE' THEN\n    v_row := to_jsonb(OLD);\n    IF TG_NARGS = 0 THEN\n      IF v_row ? 'id' THEN\n        v_table_pk := jsonb_build_object('id', v_row ->> 'id');\n      ELSE\n        v_table_pk := '{}'::jsonb;\n      END IF;\n    ELSE\n      v_table_pk := '{}'::jsonb;\n      FOR i IN 0 .. TG_NARGS - 1 LOOP\n        IF (v_row ->> TG_ARGV[i]) IS NULL THEN\n          v_table_pk := '{}'::jsonb;\n          EXIT;\n        END IF;\n        v_table_pk := coalesce(v_table_pk, '{}'::jsonb)\n                      || jsonb_build_object(TG_ARGV[i], v_row ->> TG_ARGV[i]);\n      END LOOP;\n    END IF;\n    v_table_pk := coalesce(v_table_pk, '{}'::jsonb);\n\n    v_data_after     := NULL;\n    v_changed_fields := NULL;\n    v_changed_from   := NULL;\n\n  ELSIF TG_OP = 'INSERT' THEN\n    v_row := to_jsonb(NEW);\n    IF TG_NARGS = 0 THEN\n      IF v_row ? 'id' THEN\n        v_table_pk := jsonb_build_object('id', v_row ->> 'id');\n      ELSE\n        v_table_pk := '{}'::jsonb;\n      END IF;\n    ELSE\n      v_table_pk := '{}'::jsonb;\n      FOR i IN 0 .. TG_NARGS - 1 LOOP\n        IF (v_row ->> TG_ARGV[i]) IS NULL THEN\n          v_table_pk := '{}'::jsonb;\n          EXIT;\n        END IF;\n        v_table_pk := coalesce(v_table_pk, '{}'::jsonb)\n                      || jsonb_build_object(TG_ARGV[i], v_row ->> TG_ARGV[i]);\n      END LOOP;\n    END IF;\n    v_table_pk := coalesce(v_table_pk, '{}'::jsonb);\n\n    v_data_after     := v_row;\n        v_data_after := v_data_after || jsonb_build_object('note', to_jsonb('[REDACTED]'::text));\n\n    v_changed_fields := NULL;\n    v_changed_from   := NULL;\n\n  ELSE\n    v_row := to_jsonb(NEW);\n    IF TG_NARGS = 0 THEN\n      IF v_row ? 'id' THEN\n        v_table_pk := jsonb_build_object('id', v_row ->> 'id');\n      ELSE\n        v_table_pk := '{}'::jsonb;\n      END IF;\n    ELSE\n      v_table_pk := '{}'::jsonb;\n      FOR i IN 0 .. TG_NARGS - 1 LOOP\n        IF (v_row ->> TG_ARGV[i]) IS NULL THEN\n          v_table_pk := '{}'::jsonb;\n          EXIT;\n        END IF;\n        v_table_pk := coalesce(v_table_pk, '{}'::jsonb)\n                      || jsonb_build_object(TG_ARGV[i], v_row ->> TG_ARGV[i]);\n      END LOOP;\n    END IF;\n    v_table_pk := coalesce(v_table_pk, '{}'::jsonb);\n\n    v_data_after := v_row;\n        v_data_after := v_data_after || jsonb_build_object('note', to_jsonb('[REDACTED]'::text));\n\n\n    SELECT array_agg(n.key ORDER BY n.key)\n    INTO   v_changed_fields\n    FROM   jsonb_each(to_jsonb(NEW)) AS n\n    JOIN   jsonb_each(to_jsonb(OLD)) AS o ON n.key = o.key\n    WHERE  n.value IS DISTINCT FROM o.value\n    AND NOT (n.key = ANY(ARRAY[]::text[]));\n\n        v_changed_from := NULL::jsonb;\n\n  END IF;\n\n  INSERT INTO \"threadline\".\"audit_changes\" (\n    id, transaction_id, table_schema, table_name,\n    table_pk, op, data_after, changed_fields, changed_from, captured_at\n  ) VALUES (\n    gen_random_uuid(), v_tx_id, TG_TABLE_SCHEMA, TG_TABLE_NAME,\n    v_table_pk, lower(TG_OP), v_data_after, v_changed_fields, v_changed_from, clock_timestamp()\n  );\n\n\n  IF TG_OP = 'DELETE' THEN\n    RETURN OLD;\n  END IF;\n  RETURN NEW;\nEND;\n$threadline_trigger$\n"

    execute "-- threadline: resolve the primary key of public.shape_code_keyed, then install its capture trigger\nDO $$\nDECLARE\n  tbl      regclass := to_regclass('\"public\".\"shape_code_keyed\"');\n  keys     text[];\n  idx_name text;\n  idx_cols text;\n  col      text;\n  coltype  text;\n  args     text;\nBEGIN\n  IF tbl IS NULL THEN\n    RAISE EXCEPTION 'threadline: table public.shape_code_keyed does not exist';\n  END IF;\n\n  SELECT array_agg(a.attname::text ORDER BY k.ord)\n    INTO keys\n    FROM pg_index i\n    CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n    JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n   WHERE i.indrelid = tbl AND i.indisprimary AND k.ord <= i.indnkeyatts;\n\n  IF keys IS NULL THEN\n    SELECT ic.relname\n      INTO idx_name\n      FROM pg_index i\n      JOIN pg_class ic ON ic.oid = i.indexrelid\n     WHERE i.indrelid = tbl AND i.indisunique AND i.indisvalid AND i.indisready AND i.indimmediate AND i.indpred IS NULL AND i.indexprs IS NULL\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n              WHERE k.ord <= i.indnkeyatts AND k.attnum = 0\n           )\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n             JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n              WHERE k.ord <= i.indnkeyatts AND NOT a.attnotnull\n           )\n     ORDER BY ic.relname\n     LIMIT 1;\n\n    IF idx_name IS NOT NULL THEN\n      SELECT string_agg(\n               chr(34) || replace(a.attname::text, chr(34), chr(34) || chr(34)) || chr(34),\n               ', ' ORDER BY k.ord\n             )\n        INTO idx_cols\n        FROM pg_index i\n        JOIN pg_class ic ON ic.oid = i.indexrelid\n        CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n        JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n       WHERE ic.relname = idx_name AND i.indrelid = tbl AND k.ord <= i.indnkeyatts;\n\n      RAISE EXCEPTION 'threadline: public.shape_code_keyed has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = format('The unique index %I could be used instead.', idx_name),\n              HINT = 'Add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shape_code_keyed' || chr(34) || ' => [primary_key: [' || idx_cols || ']]}, then run: mix threadline.gen.triggers --tables shape_code_keyed';\n    ELSE\n      RAISE EXCEPTION 'threadline: public.shape_code_keyed has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = 'No unique index over NOT NULL columns qualifies.',\n              HINT = 'Create a unique index over NOT NULL columns first. Then add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shape_code_keyed' || chr(34) || ' => [primary_key: [' || chr(34) || 'column_name' || chr(34) || ']]}, then run: mix threadline.gen.triggers --tables shape_code_keyed';\n    END IF;\n  END IF;\n\n  FOREACH col IN ARRAY keys LOOP\n    SELECT format_type(a.atttypid, a.atttypmod)\n      INTO coltype\n      FROM pg_attribute a\n     WHERE a.attrelid = tbl AND a.attname = col;\n\n    IF NOT EXISTS (\n          WITH RECURSIVE base_type AS (\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM pg_attribute a\n              JOIN pg_type t ON t.oid = a.atttypid\n             WHERE a.attrelid = tbl AND a.attname = col\n             UNION ALL\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM base_type bt\n              JOIN pg_type t ON t.oid = bt.typbasetype\n             WHERE bt.typtype = 'd'\n          )\n          SELECT 1 FROM base_type\n           WHERE typtype = 'e'\n              OR typname IN ('int2', 'int4', 'int8', 'text', 'varchar', 'bpchar', 'citext', 'uuid', 'date', 'timestamp')\n        )\n    THEN\n      RAISE EXCEPTION 'threadline: primary key column % of public.shape_code_keyed has type %, which has no stable text form for audit keys', col, coltype\n        USING HINT = 'Supported types: smallint, integer, bigint, text, varchar, char, citext, uuid, date, timestamp without time zone, enum types, and domains over these. Triggers installed by earlier releases keep capturing this table until it is regenerated.';\n    END IF;\n  END LOOP;\n\n\n  SELECT string_agg(quote_literal(k), ', ' ORDER BY ord)\n    INTO args\n    FROM unnest(keys) WITH ORDINALITY AS u(k, ord);\n\n  EXECUTE format('CREATE OR REPLACE TRIGGER \"threadline_audit_shape_code_keyed\"\nAFTER INSERT OR UPDATE OR DELETE ON \"public\".\"shape_code_keyed\"\nFOR EACH ROW EXECUTE FUNCTION \"threadline\".\"threadline_capture_changes\"(%s)', coalesce(args, ''));\nEND $$;\n"

    execute "-- threadline: resolve the primary key of public.shape_composite, then install its capture trigger\nDO $$\nDECLARE\n  tbl      regclass := to_regclass('\"public\".\"shape_composite\"');\n  keys     text[];\n  idx_name text;\n  idx_cols text;\n  col      text;\n  coltype  text;\n  args     text;\nBEGIN\n  IF tbl IS NULL THEN\n    RAISE EXCEPTION 'threadline: table public.shape_composite does not exist';\n  END IF;\n\n  SELECT array_agg(a.attname::text ORDER BY k.ord)\n    INTO keys\n    FROM pg_index i\n    CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n    JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n   WHERE i.indrelid = tbl AND i.indisprimary AND k.ord <= i.indnkeyatts;\n\n  IF keys IS NULL THEN\n    SELECT ic.relname\n      INTO idx_name\n      FROM pg_index i\n      JOIN pg_class ic ON ic.oid = i.indexrelid\n     WHERE i.indrelid = tbl AND i.indisunique AND i.indisvalid AND i.indisready AND i.indimmediate AND i.indpred IS NULL AND i.indexprs IS NULL\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n              WHERE k.ord <= i.indnkeyatts AND k.attnum = 0\n           )\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n             JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n              WHERE k.ord <= i.indnkeyatts AND NOT a.attnotnull\n           )\n     ORDER BY ic.relname\n     LIMIT 1;\n\n    IF idx_name IS NOT NULL THEN\n      SELECT string_agg(\n               chr(34) || replace(a.attname::text, chr(34), chr(34) || chr(34)) || chr(34),\n               ', ' ORDER BY k.ord\n             )\n        INTO idx_cols\n        FROM pg_index i\n        JOIN pg_class ic ON ic.oid = i.indexrelid\n        CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n        JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n       WHERE ic.relname = idx_name AND i.indrelid = tbl AND k.ord <= i.indnkeyatts;\n\n      RAISE EXCEPTION 'threadline: public.shape_composite has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = format('The unique index %I could be used instead.', idx_name),\n              HINT = 'Add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shape_composite' || chr(34) || ' => [primary_key: [' || idx_cols || ']]}, then run: mix threadline.gen.triggers --tables shape_composite';\n    ELSE\n      RAISE EXCEPTION 'threadline: public.shape_composite has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = 'No unique index over NOT NULL columns qualifies.',\n              HINT = 'Create a unique index over NOT NULL columns first. Then add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shape_composite' || chr(34) || ' => [primary_key: [' || chr(34) || 'column_name' || chr(34) || ']]}, then run: mix threadline.gen.triggers --tables shape_composite';\n    END IF;\n  END IF;\n\n  FOREACH col IN ARRAY keys LOOP\n    SELECT format_type(a.atttypid, a.atttypmod)\n      INTO coltype\n      FROM pg_attribute a\n     WHERE a.attrelid = tbl AND a.attname = col;\n\n    IF NOT EXISTS (\n          WITH RECURSIVE base_type AS (\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM pg_attribute a\n              JOIN pg_type t ON t.oid = a.atttypid\n             WHERE a.attrelid = tbl AND a.attname = col\n             UNION ALL\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM base_type bt\n              JOIN pg_type t ON t.oid = bt.typbasetype\n             WHERE bt.typtype = 'd'\n          )\n          SELECT 1 FROM base_type\n           WHERE typtype = 'e'\n              OR typname IN ('int2', 'int4', 'int8', 'text', 'varchar', 'bpchar', 'citext', 'uuid', 'date', 'timestamp')\n        )\n    THEN\n      RAISE EXCEPTION 'threadline: primary key column % of public.shape_composite has type %, which has no stable text form for audit keys', col, coltype\n        USING HINT = 'Supported types: smallint, integer, bigint, text, varchar, char, citext, uuid, date, timestamp without time zone, enum types, and domains over these. Triggers installed by earlier releases keep capturing this table until it is regenerated.';\n    END IF;\n  END LOOP;\n\n\n  SELECT string_agg(quote_literal(k), ', ' ORDER BY ord)\n    INTO args\n    FROM unnest(keys) WITH ORDINALITY AS u(k, ord);\n\n  EXECUTE format('CREATE OR REPLACE TRIGGER \"threadline_audit_shape_composite\"\nAFTER INSERT OR UPDATE OR DELETE ON \"public\".\"shape_composite\"\nFOR EACH ROW EXECUTE FUNCTION \"threadline\".\"threadline_capture_changes\"(%s)', coalesce(args, ''));\nEND $$;\n"

    execute "-- threadline: install the declared primary_key: override for public.shape_join, then install its capture trigger\nDO $$\nDECLARE\n  tbl        regclass := to_regclass('\"public\".\"shape_join\"');\n  declared   text[] := ARRAY['left_id', 'right_id']::text[];\n  keys       text[];\n  missing    text;\n  discovered text;\n  idx_name   text;\n  idx_detail text;\n  col        text;\n  coltype    text;\n  args       text;\nBEGIN\n  IF tbl IS NULL THEN\n    RAISE EXCEPTION 'threadline: table public.shape_join does not exist';\n  END IF;\n\n  SELECT string_agg(a.attname::text, ', ' ORDER BY k.ord)\n    INTO discovered\n    FROM pg_index i\n    CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n    JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n   WHERE i.indrelid = tbl AND i.indisprimary AND k.ord <= i.indnkeyatts;\n\n  IF discovered IS NOT NULL THEN\n    RAISE EXCEPTION 'threadline: public.shape_join already has a primary key, so primary_key: is not needed'\n      USING HINT = 'Remove primary_key: from this table''s :trigger_capture entry in config/config.exs; Threadline discovers (' || discovered || ') automatically.';\n  END IF;\n\n  SELECT d\n    INTO missing\n    FROM unnest(declared) AS d\n   WHERE NOT EXISTS (\n         SELECT 1 FROM pg_attribute a\n          WHERE a.attrelid = tbl AND a.attname = d AND a.attnum > 0 AND NOT a.attisdropped\n       )\n   LIMIT 1;\n\n  IF missing IS NOT NULL THEN\n    RAISE EXCEPTION 'threadline: primary_key: column % of public.shape_join does not exist', missing;\n  END IF;\n\n  SELECT ic.relname\n    INTO idx_name\n    FROM pg_index i\n    JOIN pg_class ic ON ic.oid = i.indexrelid\n   WHERE i.indrelid = tbl AND i.indisunique AND i.indisvalid AND i.indisready AND i.indimmediate AND i.indpred IS NULL AND i.indexprs IS NULL\n     AND NOT EXISTS (\n           SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n           JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n            WHERE k.ord <= i.indnkeyatts AND NOT a.attnotnull\n         )\n     AND (SELECT array_agg(a.attname::text ORDER BY a.attname)\n       FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n       JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n      WHERE k.ord <= i.indnkeyatts)\n\n         = (SELECT array_agg(d ORDER BY d) FROM unnest(declared) AS d)\n   ORDER BY ic.relname\n   LIMIT 1;\n\n  IF idx_name IS NULL THEN\n    IF NOT EXISTS (SELECT 1 FROM pg_index i WHERE i.indrelid = tbl AND i.indisunique) THEN\n      RAISE EXCEPTION 'threadline: no unique index on public.shape_join matches primary_key: [%]', array_to_string(declared, ', ')\n        USING DETAIL = 'no unique index exists',\n              HINT = 'Create a unique index over exactly the declared columns, all NOT NULL, then rerun mix ecto.migrate.';\n    END IF;\n\n  SELECT string_agg(\n           ic.relname || ' (' || coalesce(cols.col_list, '') || '): ' || reasons.text,\n           '; ' ORDER BY ic.relname\n         )\n    INTO idx_detail\n    FROM pg_index i\n    JOIN pg_class ic ON ic.oid = i.indexrelid\n    CROSS JOIN LATERAL (\n      SELECT string_agg(a.attname::text, ', ' ORDER BY k.ord) AS col_list\n        FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n        LEFT JOIN pg_attribute a\n          ON a.attrelid = i.indrelid AND a.attnum = k.attnum AND k.attnum <> 0\n       WHERE k.ord <= i.indnkeyatts\n    ) cols\n    CROSS JOIN LATERAL (\n      SELECT concat_ws(', ',\n               CASE WHEN i.indpred IS NOT NULL THEN 'partial' END,\n               CASE WHEN NOT i.indimmediate THEN 'deferrable' END,\n               CASE WHEN i.indexprs IS NOT NULL\n                      OR EXISTS (\n                           SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n                            WHERE k.ord <= i.indnkeyatts AND k.attnum = 0\n                         )\n                    THEN 'expression' END,\n               CASE WHEN EXISTS (\n                           SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n                           JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n                            WHERE k.ord <= i.indnkeyatts AND NOT a.attnotnull\n                         )\n                    THEN 'nullable column' END,\n               CASE WHEN NOT i.indisvalid OR NOT i.indisready THEN 'invalid' END,\n               CASE WHEN (SELECT array_agg(a.attname::text ORDER BY a.attname)\n       FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n       JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n      WHERE k.ord <= i.indnkeyatts)\n IS DISTINCT FROM\n                         (SELECT array_agg(d ORDER BY d) FROM unnest(declared) AS d)\n                    THEN 'column-set mismatch' END\n             ) AS text\n    ) reasons\n   WHERE i.indrelid = tbl AND i.indisunique;\n\n    RAISE EXCEPTION 'threadline: no unique index on public.shape_join matches primary_key: [%]', array_to_string(declared, ', ')\n      USING DETAIL = idx_detail,\n            HINT = 'Create a unique index over exactly the declared columns, all NOT NULL, then rerun mix ecto.migrate.';\n  END IF;\n\n  keys := declared;\n  FOREACH col IN ARRAY keys LOOP\n    SELECT format_type(a.atttypid, a.atttypmod)\n      INTO coltype\n      FROM pg_attribute a\n     WHERE a.attrelid = tbl AND a.attname = col;\n\n    IF NOT EXISTS (\n          WITH RECURSIVE base_type AS (\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM pg_attribute a\n              JOIN pg_type t ON t.oid = a.atttypid\n             WHERE a.attrelid = tbl AND a.attname = col\n             UNION ALL\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM base_type bt\n              JOIN pg_type t ON t.oid = bt.typbasetype\n             WHERE bt.typtype = 'd'\n          )\n          SELECT 1 FROM base_type\n           WHERE typtype = 'e'\n              OR typname IN ('int2', 'int4', 'int8', 'text', 'varchar', 'bpchar', 'citext', 'uuid', 'date', 'timestamp')\n        )\n    THEN\n      RAISE EXCEPTION 'threadline: primary key column % of public.shape_join has type %, which has no stable text form for audit keys', col, coltype\n        USING HINT = 'Supported types: smallint, integer, bigint, text, varchar, char, citext, uuid, date, timestamp without time zone, enum types, and domains over these. Triggers installed by earlier releases keep capturing this table until it is regenerated.';\n    END IF;\n  END LOOP;\n\n\n  SELECT string_agg(quote_literal(k), ', ' ORDER BY ord)\n    INTO args\n    FROM unnest(keys) WITH ORDINALITY AS u(k, ord);\n\n  EXECUTE format('CREATE OR REPLACE TRIGGER \"threadline_audit_shape_join\"\nAFTER INSERT OR UPDATE OR DELETE ON \"public\".\"shape_join\"\nFOR EACH ROW EXECUTE FUNCTION \"threadline\".\"threadline_capture_changes\"(%s)', coalesce(args, ''));\nEND $$;\n"

    execute "-- threadline: resolve the primary key of public.shape_twin, then install its capture trigger\nDO $$\nDECLARE\n  tbl      regclass := to_regclass('\"public\".\"shape_twin\"');\n  keys     text[];\n  idx_name text;\n  idx_cols text;\n  col      text;\n  coltype  text;\n  args     text;\nBEGIN\n  IF tbl IS NULL THEN\n    RAISE EXCEPTION 'threadline: table public.shape_twin does not exist';\n  END IF;\n\n  SELECT array_agg(a.attname::text ORDER BY k.ord)\n    INTO keys\n    FROM pg_index i\n    CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n    JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n   WHERE i.indrelid = tbl AND i.indisprimary AND k.ord <= i.indnkeyatts;\n\n  IF keys IS NULL THEN\n    SELECT ic.relname\n      INTO idx_name\n      FROM pg_index i\n      JOIN pg_class ic ON ic.oid = i.indexrelid\n     WHERE i.indrelid = tbl AND i.indisunique AND i.indisvalid AND i.indisready AND i.indimmediate AND i.indpred IS NULL AND i.indexprs IS NULL\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n              WHERE k.ord <= i.indnkeyatts AND k.attnum = 0\n           )\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n             JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n              WHERE k.ord <= i.indnkeyatts AND NOT a.attnotnull\n           )\n     ORDER BY ic.relname\n     LIMIT 1;\n\n    IF idx_name IS NOT NULL THEN\n      SELECT string_agg(\n               chr(34) || replace(a.attname::text, chr(34), chr(34) || chr(34)) || chr(34),\n               ', ' ORDER BY k.ord\n             )\n        INTO idx_cols\n        FROM pg_index i\n        JOIN pg_class ic ON ic.oid = i.indexrelid\n        CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n        JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n       WHERE ic.relname = idx_name AND i.indrelid = tbl AND k.ord <= i.indnkeyatts;\n\n      RAISE EXCEPTION 'threadline: public.shape_twin has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = format('The unique index %I could be used instead.', idx_name),\n              HINT = 'Add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shape_twin' || chr(34) || ' => [primary_key: [' || idx_cols || ']]}, then run: mix threadline.gen.triggers --tables shape_twin';\n    ELSE\n      RAISE EXCEPTION 'threadline: public.shape_twin has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = 'No unique index over NOT NULL columns qualifies.',\n              HINT = 'Create a unique index over NOT NULL columns first. Then add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shape_twin' || chr(34) || ' => [primary_key: [' || chr(34) || 'column_name' || chr(34) || ']]}, then run: mix threadline.gen.triggers --tables shape_twin';\n    END IF;\n  END IF;\n\n  FOREACH col IN ARRAY keys LOOP\n    SELECT format_type(a.atttypid, a.atttypmod)\n      INTO coltype\n      FROM pg_attribute a\n     WHERE a.attrelid = tbl AND a.attname = col;\n\n    IF NOT EXISTS (\n          WITH RECURSIVE base_type AS (\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM pg_attribute a\n              JOIN pg_type t ON t.oid = a.atttypid\n             WHERE a.attrelid = tbl AND a.attname = col\n             UNION ALL\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM base_type bt\n              JOIN pg_type t ON t.oid = bt.typbasetype\n             WHERE bt.typtype = 'd'\n          )\n          SELECT 1 FROM base_type\n           WHERE typtype = 'e'\n              OR typname IN ('int2', 'int4', 'int8', 'text', 'varchar', 'bpchar', 'citext', 'uuid', 'date', 'timestamp')\n        )\n    THEN\n      RAISE EXCEPTION 'threadline: primary key column % of public.shape_twin has type %, which has no stable text form for audit keys', col, coltype\n        USING HINT = 'Supported types: smallint, integer, bigint, text, varchar, char, citext, uuid, date, timestamp without time zone, enum types, and domains over these. Triggers installed by earlier releases keep capturing this table until it is regenerated.';\n    END IF;\n  END LOOP;\n\n\n    SELECT k INTO col\n      FROM unnest(keys) WITH ORDINALITY AS u(k, ord)\n     WHERE k = ANY(ARRAY['note']::text[])\n     ORDER BY ord\n     LIMIT 1;\n    IF col IS NOT NULL THEN\n      RAISE EXCEPTION 'threadline: primary key column % of public.shape_twin is listed in mask or exclude', col\n        USING HINT = 'Remove ' || col || ' from this table''s :mask or :exclude in config/config.exs; redacting a key column would erase row identity from the audit trail.';\n    END IF;\n\n  SELECT string_agg(quote_literal(k), ', ' ORDER BY ord)\n    INTO args\n    FROM unnest(keys) WITH ORDINALITY AS u(k, ord);\n\n  EXECUTE format('CREATE OR REPLACE TRIGGER \"threadline_audit_shape_twin\"\nAFTER INSERT OR UPDATE OR DELETE ON \"public\".\"shape_twin\"\nFOR EACH ROW EXECUTE FUNCTION \"threadline\".\"threadline_capture_changes_shape_twin\"(%s)', coalesce(args, ''));\nEND $$;\n"

    execute "-- threadline: resolve the primary key of shapes.shape_twin, then install its capture trigger\nDO $$\nDECLARE\n  tbl      regclass := to_regclass('\"shapes\".\"shape_twin\"');\n  keys     text[];\n  idx_name text;\n  idx_cols text;\n  col      text;\n  coltype  text;\n  args     text;\nBEGIN\n  IF tbl IS NULL THEN\n    RAISE EXCEPTION 'threadline: table shapes.shape_twin does not exist';\n  END IF;\n\n  SELECT array_agg(a.attname::text ORDER BY k.ord)\n    INTO keys\n    FROM pg_index i\n    CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n    JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n   WHERE i.indrelid = tbl AND i.indisprimary AND k.ord <= i.indnkeyatts;\n\n  IF keys IS NULL THEN\n    SELECT ic.relname\n      INTO idx_name\n      FROM pg_index i\n      JOIN pg_class ic ON ic.oid = i.indexrelid\n     WHERE i.indrelid = tbl AND i.indisunique AND i.indisvalid AND i.indisready AND i.indimmediate AND i.indpred IS NULL AND i.indexprs IS NULL\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n              WHERE k.ord <= i.indnkeyatts AND k.attnum = 0\n           )\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n             JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n              WHERE k.ord <= i.indnkeyatts AND NOT a.attnotnull\n           )\n     ORDER BY ic.relname\n     LIMIT 1;\n\n    IF idx_name IS NOT NULL THEN\n      SELECT string_agg(\n               chr(34) || replace(a.attname::text, chr(34), chr(34) || chr(34)) || chr(34),\n               ', ' ORDER BY k.ord\n             )\n        INTO idx_cols\n        FROM pg_index i\n        JOIN pg_class ic ON ic.oid = i.indexrelid\n        CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n        JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n       WHERE ic.relname = idx_name AND i.indrelid = tbl AND k.ord <= i.indnkeyatts;\n\n      RAISE EXCEPTION 'threadline: shapes.shape_twin has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = format('The unique index %I could be used instead.', idx_name),\n              HINT = 'Add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shapes.shape_twin' || chr(34) || ' => [primary_key: [' || idx_cols || ']]}, then run: mix threadline.gen.triggers --tables shapes.shape_twin';\n    ELSE\n      RAISE EXCEPTION 'threadline: shapes.shape_twin has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = 'No unique index over NOT NULL columns qualifies.',\n              HINT = 'Create a unique index over NOT NULL columns first. Then add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shapes.shape_twin' || chr(34) || ' => [primary_key: [' || chr(34) || 'column_name' || chr(34) || ']]}, then run: mix threadline.gen.triggers --tables shapes.shape_twin';\n    END IF;\n  END IF;\n\n  FOREACH col IN ARRAY keys LOOP\n    SELECT format_type(a.atttypid, a.atttypmod)\n      INTO coltype\n      FROM pg_attribute a\n     WHERE a.attrelid = tbl AND a.attname = col;\n\n    IF NOT EXISTS (\n          WITH RECURSIVE base_type AS (\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM pg_attribute a\n              JOIN pg_type t ON t.oid = a.atttypid\n             WHERE a.attrelid = tbl AND a.attname = col\n             UNION ALL\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM base_type bt\n              JOIN pg_type t ON t.oid = bt.typbasetype\n             WHERE bt.typtype = 'd'\n          )\n          SELECT 1 FROM base_type\n           WHERE typtype = 'e'\n              OR typname IN ('int2', 'int4', 'int8', 'text', 'varchar', 'bpchar', 'citext', 'uuid', 'date', 'timestamp')\n        )\n    THEN\n      RAISE EXCEPTION 'threadline: primary key column % of shapes.shape_twin has type %, which has no stable text form for audit keys', col, coltype\n        USING HINT = 'Supported types: smallint, integer, bigint, text, varchar, char, citext, uuid, date, timestamp without time zone, enum types, and domains over these. Triggers installed by earlier releases keep capturing this table until it is regenerated.';\n    END IF;\n  END LOOP;\n\n\n    SELECT k INTO col\n      FROM unnest(keys) WITH ORDINALITY AS u(k, ord)\n     WHERE k = ANY(ARRAY['note']::text[])\n     ORDER BY ord\n     LIMIT 1;\n    IF col IS NOT NULL THEN\n      RAISE EXCEPTION 'threadline: primary key column % of shapes.shape_twin is listed in mask or exclude', col\n        USING HINT = 'Remove ' || col || ' from this table''s :mask or :exclude in config/config.exs; redacting a key column would erase row identity from the audit trail.';\n    END IF;\n\n  SELECT string_agg(quote_literal(k), ', ' ORDER BY ord)\n    INTO args\n    FROM unnest(keys) WITH ORDINALITY AS u(k, ord);\n\n  EXECUTE format('CREATE OR REPLACE TRIGGER \"threadline_audit_shapes_shape_twin\"\nAFTER INSERT OR UPDATE OR DELETE ON \"shapes\".\"shape_twin\"\nFOR EACH ROW EXECUTE FUNCTION \"threadline\".\"threadline_capture_changes_shapes_shape_twin_c9a9dcb238e7\"(%s)', coalesce(args, ''));\nEND $$;\n"

    execute "-- threadline: resolve the primary key of public.shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok, then install its capture trigger\nDO $$\nDECLARE\n  tbl      regclass := to_regclass('\"public\".\"shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok\"');\n  keys     text[];\n  idx_name text;\n  idx_cols text;\n  col      text;\n  coltype  text;\n  args     text;\nBEGIN\n  IF tbl IS NULL THEN\n    RAISE EXCEPTION 'threadline: table public.shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok does not exist';\n  END IF;\n\n  SELECT array_agg(a.attname::text ORDER BY k.ord)\n    INTO keys\n    FROM pg_index i\n    CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n    JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n   WHERE i.indrelid = tbl AND i.indisprimary AND k.ord <= i.indnkeyatts;\n\n  IF keys IS NULL THEN\n    SELECT ic.relname\n      INTO idx_name\n      FROM pg_index i\n      JOIN pg_class ic ON ic.oid = i.indexrelid\n     WHERE i.indrelid = tbl AND i.indisunique AND i.indisvalid AND i.indisready AND i.indimmediate AND i.indpred IS NULL AND i.indexprs IS NULL\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n              WHERE k.ord <= i.indnkeyatts AND k.attnum = 0\n           )\n       AND NOT EXISTS (\n             SELECT 1 FROM unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n             JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n              WHERE k.ord <= i.indnkeyatts AND NOT a.attnotnull\n           )\n     ORDER BY ic.relname\n     LIMIT 1;\n\n    IF idx_name IS NOT NULL THEN\n      SELECT string_agg(\n               chr(34) || replace(a.attname::text, chr(34), chr(34) || chr(34)) || chr(34),\n               ', ' ORDER BY k.ord\n             )\n        INTO idx_cols\n        FROM pg_index i\n        JOIN pg_class ic ON ic.oid = i.indexrelid\n        CROSS JOIN LATERAL unnest(i.indkey::int2[]) WITH ORDINALITY AS k(attnum, ord)\n        JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = k.attnum\n       WHERE ic.relname = idx_name AND i.indrelid = tbl AND k.ord <= i.indnkeyatts;\n\n      RAISE EXCEPTION 'threadline: public.shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = format('The unique index %I could be used instead.', idx_name),\n              HINT = 'Add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok' || chr(34) || ' => [primary_key: [' || idx_cols || ']]}, then run: mix threadline.gen.triggers --tables shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok';\n    ELSE\n      RAISE EXCEPTION 'threadline: public.shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok has no primary key, so its audit rows could not be told apart. Declare one with the primary_key: option.'\n        USING DETAIL = 'No unique index over NOT NULL columns qualifies.',\n              HINT = 'Create a unique index over NOT NULL columns first. Then add to config/config.exs: config :threadline, :trigger_capture, tables: %{' || chr(34) || 'shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok' || chr(34) || ' => [primary_key: [' || chr(34) || 'column_name' || chr(34) || ']]}, then run: mix threadline.gen.triggers --tables shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok';\n    END IF;\n  END IF;\n\n  FOREACH col IN ARRAY keys LOOP\n    SELECT format_type(a.atttypid, a.atttypmod)\n      INTO coltype\n      FROM pg_attribute a\n     WHERE a.attrelid = tbl AND a.attname = col;\n\n    IF NOT EXISTS (\n          WITH RECURSIVE base_type AS (\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM pg_attribute a\n              JOIN pg_type t ON t.oid = a.atttypid\n             WHERE a.attrelid = tbl AND a.attname = col\n             UNION ALL\n            SELECT t.oid, t.typtype, t.typbasetype, t.typname\n              FROM base_type bt\n              JOIN pg_type t ON t.oid = bt.typbasetype\n             WHERE bt.typtype = 'd'\n          )\n          SELECT 1 FROM base_type\n           WHERE typtype = 'e'\n              OR typname IN ('int2', 'int4', 'int8', 'text', 'varchar', 'bpchar', 'citext', 'uuid', 'date', 'timestamp')\n        )\n    THEN\n      RAISE EXCEPTION 'threadline: primary key column % of public.shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok has type %, which has no stable text form for audit keys', col, coltype\n        USING HINT = 'Supported types: smallint, integer, bigint, text, varchar, char, citext, uuid, date, timestamp without time zone, enum types, and domains over these. Triggers installed by earlier releases keep capturing this table until it is regenerated.';\n    END IF;\n  END LOOP;\n\n\n  SELECT string_agg(quote_literal(k), ', ' ORDER BY ord)\n    INTO args\n    FROM unnest(keys) WITH ORDINALITY AS u(k, ord);\n\n  EXECUTE format('CREATE OR REPLACE TRIGGER \"threadline_audit_shape_long_name_padded_to_prove_sixty_byte_ide\"\nAFTER INSERT OR UPDATE OR DELETE ON \"public\".\"shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok\"\nFOR EACH ROW EXECUTE FUNCTION \"threadline\".\"threadline_capture_changes\"(%s)', coalesce(args, ''));\nEND $$;\n"

    execute "-- threadline: fail if another table's trigger uses this table's capture function\nDO $$\nDECLARE\n  fn       regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shape_twin\"()');\n  owner    regclass     := to_regclass('\"public\".\"shape_twin\"');\n  others   text;\n  retables text;\nBEGIN\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname),\n         string_agg(CASE WHEN n.nspname = 'public' THEN c.relname\n                         ELSE n.nspname || '.' || c.relname END, ',' ORDER BY n.nspname, c.relname)\n    INTO others, retables\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0 AND t.tgrelid <> owner;\n  IF others IS NOT NULL THEN\n    RAISE EXCEPTION 'threadline: triggers on % also use the capture function of public.shape_twin and may now apply another table''s redaction rules', others\n      USING HINT = format('Regenerate those tables in the same command: mix threadline.gen.triggers --tables shape_twin,%s (or regenerate %s first).', retables, retables);\n  END IF;\nEND $$;\n"

    execute "-- threadline: fail if another table's trigger uses this table's capture function\nDO $$\nDECLARE\n  fn       regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shapes_shape_twin_c9a9dcb238e7\"()');\n  owner    regclass     := to_regclass('\"shapes\".\"shape_twin\"');\n  others   text;\n  retables text;\nBEGIN\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname),\n         string_agg(CASE WHEN n.nspname = 'public' THEN c.relname\n                         ELSE n.nspname || '.' || c.relname END, ',' ORDER BY n.nspname, c.relname)\n    INTO others, retables\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0 AND t.tgrelid <> owner;\n  IF others IS NOT NULL THEN\n    RAISE EXCEPTION 'threadline: triggers on % also use the capture function of shapes.shape_twin and may now apply another table''s redaction rules', others\n      USING HINT = format('Regenerate those tables in the same command: mix threadline.gen.triggers --tables shapes.shape_twin,%s (or regenerate %s first).', retables, retables);\n  END IF;\nEND $$;\n"

    execute "-- threadline: drop this capture function only if no trigger still uses it\nDO $$\nDECLARE\n  fn    regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shape_code_keyed\"()');\n  users text;\nBEGIN\n  IF fn IS NULL THEN RETURN; END IF;\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname) INTO users\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0;\n  IF users IS NULL THEN\n    EXECUTE format('DROP FUNCTION %s', fn);\n  ELSE\n    RAISE WARNING 'threadline: kept % because triggers on % still use it; regenerate triggers for those tables', fn, users;\n  END IF;\nEND $$;\n"

    execute "-- threadline: drop this capture function only if no trigger still uses it\nDO $$\nDECLARE\n  fn    regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shape_composite\"()');\n  users text;\nBEGIN\n  IF fn IS NULL THEN RETURN; END IF;\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname) INTO users\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0;\n  IF users IS NULL THEN\n    EXECUTE format('DROP FUNCTION %s', fn);\n  ELSE\n    RAISE WARNING 'threadline: kept % because triggers on % still use it; regenerate triggers for those tables', fn, users;\n  END IF;\nEND $$;\n"

    execute "-- threadline: drop this capture function only if no trigger still uses it\nDO $$\nDECLARE\n  fn    regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shape_join\"()');\n  users text;\nBEGIN\n  IF fn IS NULL THEN RETURN; END IF;\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname) INTO users\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0;\n  IF users IS NULL THEN\n    EXECUTE format('DROP FUNCTION %s', fn);\n  ELSE\n    RAISE WARNING 'threadline: kept % because triggers on % still use it; regenerate triggers for those tables', fn, users;\n  END IF;\nEND $$;\n"

    execute "-- threadline: drop this capture function only if no trigger still uses it\nDO $$\nDECLARE\n  fn    regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shapes_shape_twin\"()');\n  users text;\nBEGIN\n  IF fn IS NULL THEN RETURN; END IF;\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname) INTO users\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0;\n  IF users IS NULL THEN\n    EXECUTE format('DROP FUNCTION %s', fn);\n  ELSE\n    RAISE WARNING 'threadline: kept % because triggers on % still use it; regenerate triggers for those tables', fn, users;\n  END IF;\nEND $$;\n"

    execute "-- threadline: drop this capture function only if no trigger still uses it\nDO $$\nDECLARE\n  fn    regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shape_long_name_padded_to_prove_sixt\"()');\n  users text;\nBEGIN\n  IF fn IS NULL THEN RETURN; END IF;\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname) INTO users\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0;\n  IF users IS NULL THEN\n    EXECUTE format('DROP FUNCTION %s', fn);\n  ELSE\n    RAISE WARNING 'threadline: kept % because triggers on % still use it; regenerate triggers for those tables', fn, users;\n  END IF;\nEND $$;\n"

    execute "-- threadline: drop this capture function only if no trigger still uses it\nDO $$\nDECLARE\n  fn    regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shape_long_name_padded__edab656ffa27\"()');\n  users text;\nBEGIN\n  IF fn IS NULL THEN RETURN; END IF;\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname) INTO users\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0;\n  IF users IS NULL THEN\n    EXECUTE format('DROP FUNCTION %s', fn);\n  ELSE\n    RAISE WARNING 'threadline: kept % because triggers on % still use it; regenerate triggers for those tables', fn, users;\n  END IF;\nEND $$;\n"
  end

  def down do
    execute "DROP TRIGGER IF EXISTS \"threadline_audit_shape_code_keyed\" ON \"public\".\"shape_code_keyed\""

    execute "DROP TRIGGER IF EXISTS \"threadline_audit_shape_composite\" ON \"public\".\"shape_composite\""

    execute "DROP TRIGGER IF EXISTS \"threadline_audit_shape_join\" ON \"public\".\"shape_join\""

    execute "DROP TRIGGER IF EXISTS \"threadline_audit_shape_twin\" ON \"public\".\"shape_twin\""

    execute "DROP TRIGGER IF EXISTS \"threadline_audit_shapes_shape_twin\" ON \"shapes\".\"shape_twin\""

    execute "DROP TRIGGER IF EXISTS \"threadline_audit_shape_long_name_padded_to_prove_sixty_byte_ide\" ON \"public\".\"shape_long_name_padded_to_prove_sixty_byte_identifiers_work_ok\""

    execute "-- threadline: drop this capture function only if no trigger still uses it\nDO $$\nDECLARE\n  fn    regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shape_twin\"()');\n  users text;\nBEGIN\n  IF fn IS NULL THEN RETURN; END IF;\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname) INTO users\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0;\n  IF users IS NULL THEN\n    EXECUTE format('DROP FUNCTION %s', fn);\n  ELSE\n    RAISE WARNING 'threadline: kept % because triggers on % still use it; regenerate triggers for those tables', fn, users;\n  END IF;\nEND $$;\n"

    execute "-- threadline: drop this capture function only if no trigger still uses it\nDO $$\nDECLARE\n  fn    regprocedure := to_regprocedure('\"threadline\".\"threadline_capture_changes_shapes_shape_twin_c9a9dcb238e7\"()');\n  users text;\nBEGIN\n  IF fn IS NULL THEN RETURN; END IF;\n  SELECT string_agg(format('%I.%I', n.nspname, c.relname), ', ' ORDER BY n.nspname, c.relname) INTO users\n    FROM pg_trigger t JOIN pg_class c ON c.oid = t.tgrelid\n    JOIN pg_namespace n ON n.oid = c.relnamespace\n   WHERE t.tgfoid = fn AND t.tgparentid = 0;\n  IF users IS NULL THEN\n    EXECUTE format('DROP FUNCTION %s', fn);\n  ELSE\n    RAISE WARNING 'threadline: kept % because triggers on % still use it; regenerate triggers for those tables', fn, users;\n  END IF;\nEND $$;\n"
  end
end
